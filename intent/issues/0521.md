---
issue: 0521
title: CI's Intent Tests workflow is red on both legs: every arm of prepush_push_range.bats fails since aba799696 (0518)
status: CLOSED
severity: high
created: 2026-09-23
closed: 2026-09-23
reporter: Matthew Sinclair
---

Seen (vc, 2026-09-23, from the CI logs; the mechanism is not driven yet):

- CI's Intent Tests workflow failed on both legs at the Run tests step, on 5fdc731f7 (run 35783294581) and on 4aa026a3c (run 35785712933), the last code push. The last green run is 35743714571 on f80d5a9ea.
- The not-ok set is identical on all four legs: `tests/unit/prepush_push_range.bats` arms 408 to 415, every arm in the file, and nothing else. The engaged arms fail `[[ "$output" == *"cargo not on PATH"* ]]` and the skip arms fail `[ "$status" -eq 0 ]`. The arms print no `$output`, so CI never said what the runner printed.
- In f80d5a9ea..5fdc731f7 the only commit touching the runner under test, `bin/.devbin/cmd/prepush`, is aba799696 (0518). Since then the runner takes `artefact_currency_verdict "$RELEASE_DIR" "$ROOT"` (line 204) before the path trigger, and the fixture sets `PROJECT_ROOT` to a temporary repo that holds no pair. 0512's edit to the file (99a449779) changed setup only, and setup passes on CI. devbin 0.1.7 (8b934cc82) touched `bin/.devbin/lib/` and not `cmd/prepush`.
- Hypothesis, unmeasured: every arm stops at the currency verdict before the range decision is read.
- 0518's judging record carries no run of this file: its commit names a two-sided drive against a scratch bare remote, and vc's verdict went over the live channel only.

Expected: the eight arms test the push-range decision again and pass on both legs.

Ruling (hv, 2026-09-23, taking vc's proposal): cc fixes it on the FIXTURE side, and the runner keeps 0518's judged placement (the verdict before the path trigger).

1. Make the arms speak and confirm the mechanism before any fix.
2. The fixture gives the verdict a pair it reads as current, so the arms test the range decision again.
3. One arm proves 0518's own refusal still holds, a board-only push over a stale pair included.
4. Every assertion prints `$output` on failure.

vc judges it on the whole bats suite, run on this macOS host. hv pushes, then vc reads both workflows on both legs. It blocks 3.2.1.

Closed (cc, 2026-09-23, on vc's word, after CI read green on hv's push of 9dac092ea):

- LANDED at 38db27487 at its judged patch-id 4650733e4 (blob c8564ae5a), on vc's PASS. The judging run on this macOS host gave 0 ok and 8 not ok on the baseline and 9 ok on the bank.
- The mechanism, made to speak first as ruled: the fixture's temporary repository held no pair, so every arm stopped at `BLOCKED: ... no intent binary at <repo>/native/rust/target/release/intent` from `artefact_currency_verdict` before the range was read. Driven on this host before the fix, all eight arms were red, so no local run of this file had followed 0518.
- The fix is on the fixture side, as ruled, and the runner is untouched, so 0518's placement of the verdict before the path trigger stands. The fixture stamps a pair at HEAD before every gate run, its repository ignores `native/rust/target/` as this one does, and every assertion prints `$output`.
- A new arm holds 0518's refusal, the ruling's point 3: a board-only push skips over a current pair and is refused over a stale one, and with the verdict block deleted it is the only arm that goes red.
- CI on 9dac092ea: the Intent Tests workflow, run 35899129295, succeeded on every job (Shell Script Analysis, Test on macOS, Test on Ubuntu, Test Summary), and the rust workflow, run 35899129244, succeeded on ubuntu-latest and macos-latest.
- In the Intent Tests job logs, all nine arms of `tests/unit/prepush_push_range.bats`, 410 to 418, are `ok` on both legs with none skipped, 418 ("a stale pair is refused before the path trigger, on a push that would skip") included. macOS: 1..743, 743 ok, 0 not ok, 1 declared skip. Ubuntu: 1..743, 743 ok, 0 not ok, 3 declared skips (166 NO WITNESS on both legs; 659 and 660 OTHER SYSTEM, the bash-3.2 controls). The last red run was 35785712933 on 4aa026a3c.

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
