---
issue: 0553
title: the explorer refuses a valid work-package address: its zero-padded item matches no row, because rows are named by the bare sequence
status: CLOSED
severity: medium
created: 2026-09-24
closed: 2026-09-24
reporter: Matthew Sinclair
---

Found by ic on 2026-09-24 in the 3.2.1 doc audit, driven in a pty on demo projects with the pair at `4c687eaad`.

Seen: `intent explore intent:///threads/ST0001/wp/02` opens an error row, "`02` is not in `wps`", and so does the same address typed into the explorer's box. Work package 02 exists and is WIP. It is the address form Intent's own remedies print, eg `wp done`'s warning names `intent set intent:///threads/ST0001/wp/01 objective --from <file>`.

Mechanism, corrected forward by ic at 10:08Z and read by vc at HEAD: `render.rs:6814-6816` builds each work-package row as `Row::named(format!("{}", w.seq), format!("WP-{:02}", w.seq), ...)`, so a row's NAME is the bare sequence (`2`) and only its label reads `WP-02`; `render.rs:6763-6768` then matches the address's zero-padded item `02` against the names, and it matches neither. (The first filing said the names read `WP-02`; that was the label.) The route the explorer builds for itself works: Enter on a work-package row in `/outstanding` opens `/thread/<ST>/wps/1`.

Expected: a work-package address that names a work package the store holds opens that work package, in the explorer as everywhere else the address is accepted.

Disposition: hv's, asked on 2026-09-24, against the shipping-grade test of hv decision 33 (1).


Fixed at `c667bdfb3`, landed on 2026-09-24 on vc's word; ships in 3.2.1.

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
