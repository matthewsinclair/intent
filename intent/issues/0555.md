---
issue: 0555
title: a work-package id collision between two clones plans only taking a side, where a thread or issue collision plans a renumber
status: OPEN
severity: medium
created: 2026-09-24
reporter: Matthew Sinclair
---

Found by cc on 2026-09-24, between cc's 10:00Z and 10:03Z clock reads, while driving the team guide for the 3.2.1 doc audit: two clones of one origin under short isolated HOMEs, on the dev pair at `4c687eaad`.

Seen: both clones ran `intent wp new ST0001` and both got `ST0001/01`. The pull conflicts in `intent/.canon/st/ST0001.json` and `intent/todo.md`, and `intent sync` plans only "take a side (non-reversible)", which drops the other person's work package. A thread or issue collision plans a renumber instead: measured on issues, "both sides minted issue 0001: this clone's moves to 0002", and `--apply --yes` fixes it.

The as-built remedy, measured by cc: `git merge --abort`, `git reset --hard origin/main`, `intent sync --apply`, `git pull`, then `intent wp new` again, which gets `02`. Nothing is lost when that is followed, and the team guide documents it.

Expected: a work-package id collision plans a renumber, as a thread or issue collision does.

Disposition: filed for after the stabilisation line. It is a missing capability rather than a remedy that loses work -- the plan it offers says "non-reversible" -- so it is outside the shipping-grade test of hv decision 33 (1), and the guide carries the measured remedy.

---

_Generated by Intent v3.2.1 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
