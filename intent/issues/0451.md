---
issue: 0451
title: cargo doc -p intentsvcs warns 67 times, 17 of them broken intra-doc links, so a doc build cannot gate under -D warnings
status: OPEN
severity: low
created: 2026-09-18
reporter: Matthew Sinclair
---

Measured by cc on 2026-09-18 at c5570f91c while judging 0444: `cargo doc -p intentsvcs --no-deps` exits 0 with 67 warnings, 17 of them `unresolved link` (broken intra-doc links such as `Sigil::accepts` at address.rs:823, bootstrap.rs:231, daemon.rs:341-342), none introduced by 0444. A doc build under `RUSTDOCFLAGS=-D warnings` fails on them, so nothing can gate on the doc build until they are fixed, and a reader following a doc link lands nowhere.

## The fix (S)

Fix every unresolved link to the item it names, or drop the link where the item is gone; fix the other warning classes the log names; then add `cargo doc --no-deps` under `-D warnings` to the workspace CI so the count cannot grow again. The log is cc's `judge-0444/doc-2.log`; the per-file tally is in this issue's first commit message.

---

_Generated by Intent v3.0.3 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
