---
issue: 0524
title: dispatch_ssot's shared outside-any-project TempDir is initialised by a sibling arm mid-run, so the outs alias arm reds at random
status: CLOSED
severity: medium
created: 2026-09-23
closed: 2026-09-23
reporter: Matthew Sinclair
---

Seen (dc, 2026-09-23 07:56Z to 07:59Z by date -u, in tmp/wt-0522 at c419e8719; not caused by 0522):

- A targeted run of 31 intent-cli arms went 30 passed, 1 failed. `dispatch_ssot::every_declared_alias_on_a_shipped_row_is_the_command_it_aliases` failed because `outs` answered "no Intent project found at or above .../T/.tmpfnTlkU" while `outstanding` answered "outstanding: 0 of 0 threads ...".
- `outside_any_project()` (`dispatch_ssot.rs:40`) is ONE static `OnceLock` `TempDir` shared by every arm in the file, and a static is never dropped. After the run that directory held a whole project whose `config.json` names `project_name` ".tmpfnTlkU", created 2026-09-23T07:56:27.083Z. So a sibling arm ran a bare `intent init` inside it during the run: `outs` ran before the init and `outstanding` after it.
- It had no subject to race until ST0079's `outs`, the first declared alias that reaches project discovery (the `at done` / `at notdone` aliases stop at clap's missing positionals).
- Not 0522's: the arm alone, single-threaded, is green 2 of 2, and the installed pair 099088acd, which carries ST0079 and none of 0522, answers `outs` and `outstanding` identically outside a project (both rc 1, the same refusal).
- Not yet identified: which arm runs the bare init. The directory is kept as evidence at `/private/var/folders/nn/p40vzghs67v8yq0p5y416yf80000gn/T/.tmpfnTlkU`.

Expected: an arm that means "outside any project" runs where no arm can create one, and the alias arm gives the same answer however the arms interleave. It can red CI's test step at random, so it is a 3.2.1 item.

Routing (vc, 2026-09-23): dc, after 0522, because dc holds the mechanism and the evidence. The fixer names the arm that runs the init before choosing the fix.

**LANDED (dc, 2026-09-23, on vc's word).** At 900f76a86 on aec0c3721, from refs/bank/dc/0524 (blob c31654a1f; `git apply --stat` reads 1 file changed, 25 insertions). The patch-id was read from the bank blob and again from the landed commit, 2f475e2c1 both times: the id cc's train carried and vc judged. It landed inside cc's crossed chain: cc's START of 08:47:56Z by cc's clock reached dc after the commit. The paths were disjoint, and cc was told at once.

**THE ARM THAT RAN THE INIT, named before the fix as the routing asked.** `outside_any_project()` and `fixture_home()` are each one static `TempDir` for the whole run. `an_unbuilt_leaf_does_not_send_the_reader_to_an_empty_help` ran every shipped verbless family bare inside them, `init` and `bootstrap` among them, so the shared cwd became a project mid-run and the shared `HOME` took bootstrap's home pointer. The alias arm then compared `outs` from before the init with `outstanding` from after it. `no_unbuilt_command_leaks_intents_own_project_state` already had a cwd of its own and still ran a bare `bootstrap` under the shared `HOME`.

**THE FIX.** Both sweeps now bring their own cwd and `HOME`, and both helpers carry the contract in their docs: they serve commands that only read, and an arm that runs a command able to write brings a directory of its own. The change is confined to `native/rust/crates/intent-cli/tests/dispatch_ssot.rs`.

**THE JUDGING.** Red on the baseline and green on the fix, by one instrument. After the baseline run the static cwd held a whole project and the static `HOME` held `.local/share/intent/home`. After the fix, under a private `TMPDIR`, `dispatch_ssot` passed 19 of 19 and both static directories were empty. Then cc's train, ended at 08:30:14Z and again at 08:39:53Z by cc's clock: `dispatch_ssot` passed whole in the intent-cli suite both times, and clippy all-targets passed with it.

**DEPLOYMENT.** Test-only, so nothing in the delivered pair changes. It is still a landing under `native/rust`, so the next push needs `bin/devbin build all` first (decision 30), and CI's test step reads it from the pushed tree.

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
