---
issue: 0523
title: intent wb has no verb to correct or redact an item's text, and the old text survives in its originating event file
status: CLOSED
severity: high
created: 2026-09-23
closed: 2026-09-23
reporter: Matthew Sinclair
---

Requested by gtools-vc on 2026-09-22, on Gtools hv's ruling "Intent verb, hold", and carried as Intent vc todo 61. Ruled by hv on 2026-09-23: "I need that fixed right now, please." It is a 3.2.1 item.

Seen: Gtools' pre-commit identifier gate refuses any added line carrying a client identifier or name, in any file a commit adds (boards, inbox views, `intent/.canon`, `intent/st`), because the text sits in the rendered views AND in its originating event file under `intent/.canon/events/`. No `intent wb` verb changes text a node has written. (gtools-vc, 2026-09-23: nothing in Gtools is blocked today; hv committed gtools-cc's todo 95 as it stood, and Gtools hv ruled engagement ids not sensitive, so the likely trigger is a client name. The next case is caught at commit time, while its event is still pending.)

Expected, two cases, both required:

1. Originating event NOT YET COMMITTED: the edit leaves no trace of the old text in anything a commit would carry. The pending event is amended before it lands, and no superseded file is left untracked beside its replacement, because nodes commit events by adding untracked event files.
2. Originating event ALREADY COMMITTED: an edit event is appended, and every later render honours it, `board.json` included. Git history is left to the repository's own publication process.

Not asked: weakening append-only history for committed events, or any gate exemption.

Rulings (vc under hv's pen, 2026-09-23, from cc's proposal and gtools-vc's answers):

- NAME: `intent wb edit <kind> <seq> <text> --node <moniker>`, facade `wb_edit`, MCP tool `intent_wb_edit` only if its row's recoverability admits it. `correct` already names `wb register --correct` (a node's name and role), and `amend` reads as git history rewriting.
- SCOPE: every free text a wb verb writes. Stage 1 (items): all six kinds, live AND archived, since `board.json` keeps an archived item's text. Stage 2: message bodies (ask and announce), addressed by recipient and anchor as `--re` is, with the sender as the only writer. Two messages to one recipient in one minute are refused with both named, and the refusal carries a remedy that lets the sender pick one, so no message is ever uneditable.
- FOCUS keeps its one door, `wb pickup --focus`: `wb.pickup` is machine-scoped, so the focus lives in no event file. Stage 2 adds a test proving the old focus is in no file.
- DECLARED OUT: `wb register --correct` records the old name and role (`held_name`, `held_role`) in its event, and the original `wb.register` event carries the old name as well. A node's name and role are participant configuration a human declares, not work text, so they are outside this verb.
- Re-serialisation: no production path rewrites a committed event file (cc's census: `backfill_event_files` skips existing paths, `init` writes a new file, `add_event_files` writes only new events and the amended draft, after git says HEAD does not hold it). The case-2 test asserts the committed file is byte-identical after the edit.

Rulings on v4 (vc, 2026-09-23, accepting cc's design after cc's probe reproduced a miss in v3; the wording is cc's):

- CARRIER WINDOW: the event carrying an item's current text is its newest `wb.edit`, or else its creator, taken only when exactly one of the node's `wb.add` or `wb.decide` events with the item's kind and text is stamped at or after the item's own stamp and strictly before the stamp of the next item of that node and kind. None or several is no carrier, and the edit is recorded. This replaces v3's first-instant rule. The probe (2026-09-23, forced stamps) showed that rule missing the creator whenever the previous transaction's event shared the item's millisecond and the creator landed in the next one: v3 recorded the edit and left the old text in the uncommitted draft while saying it reached no commit. Stage 2's message origin takes the same window, bounded by the next message from that sender to that recipient, with exactly one candidate.
- THE CLAIM IS A MEASUREMENT: after the write, every file under `intent/` that the next commit would or could carry is searched in process for the old text, in both spellings: the index bytes of staged files, which a plain `git commit` carries ("the next commit would carry the old text in ..."), and the work-tree bytes of files that are untracked and not ignored or that differ from HEAD ("the next commit could carry the old text in ..."). The answer gives its all-clear, "no file under intent/ holds the old text, at HEAD or in the next commit", only when those lists and the HEAD list are all empty; a scan that cannot run becomes a note and withholds it. The lists name what holds the text, not whose it is: another item that says the same thing is named too, and when the new text contains the old text a line says so (vc's ruling). The store's directory is left out by rule, since no commit carries it (D34).
- HEAD COVERS ALL OF `intent/` (vc, 2026-09-23, on ic's review of v4, replacing the declared limit vc had accepted): a commit's tree carries every unchanged file, so a peer's committed copy of the text is in the next commit too, and the claim was false while HEAD was read only under the editor's board directory and the carrying event's file. The HEAD list reads every file HEAD holds under `intent/`, the store's directory aside: one `git grep -F -l` over HEAD, fed the text's longest line and its JSON spelling so that it can only name too many, then each named file confirmed exactly in both spellings. Each hit is named on a line of its own ("HEAD already carries the old text in ..."). Outside `intent/`, and in commits before HEAD, nothing is searched.

Behaviours, one line each:

- Editing an item whose originating event is uncommitted leaves the old text in no file under `intent/`, events and renders both.
- Editing an item whose originating event is committed appends an edit event, and `board.json`, the board view and `wb show` print the new text, for archived items as well as live ones.
- Only the owning node edits its own item, and only the sender edits its own message; any other node is refused with a remedy.
- An unknown kind, seq or anchor is refused with a remedy naming `intent wb show <node>`.
- The verb has its dispatch row, help and exit codes, and ic reviews that surface before each bank.
- An edit whose carrier the rules cannot find names every file the next commit could carry that still holds the old text, and gives no all-clear; so does an edit that leaves the text in a copy `wb migrate` kept, or whose text a peer's committed item quotes.
- An earlier transaction's event that shares an item's millisecond does not hide that item's creator.

Traps: "committed" means present at HEAD, not in the index. intentd ingests disk edits and can revert the last write of a burst, so the edit is verified past the ingest. The verb reaches Gtools only after `bin/devbin build all` (decision 30), and gtools-vc is warned before and after it.

Routing: cc builds both stages, ic reviews the surface, and vc judges.

Closed (cc, 2026-09-23, on vc's passes of both stages):

- STAGE 1 (items) landed at 3d8d3b8d1 as v4: refs/bank/cc/0523-stage1/v4, blob 2b1dca5d8, patch-id 2a17fefc5, 18 files +1837 -30, judged by vc stacked on ce3dd1036 + 0526 in one run of CI's six rust lines (every rc 0; 2989 passed, 0 failed, 5 ignored), with ic's surface pass on the reworked v4. v1 to v3 are kept in refs/bank/cc/0523-stage1/: v2 answered ic's point A, v3 vc's hold on a borrowed creator, and v4 the millisecond miss cc's probe reproduced in v3 and ic's HEAD finding, as the rulings above record.
- STAGE 2 (messages) landed at d336abd8d: refs/bank/cc/0523-stage2/on-669fc767f, blob e78b86b13, patch-id 023b4ded9, 12 files +881 -34, judged by vc on 669fc767f with 0527 in the tree in one run of the same six lines (every rc 0; 3003 passed, 0 failed, 5 ignored), with ic's surface pass.
- Each patch-id was checked before and after its commit, and main's build inputs equalled the judged tree after each landing.
- NOT DEPLOYED until `bin/devbin build all` on hv's go: the installed pair predates both, and Gtools reaches the verb only after that build (decision 30), with gtools-vc warned before and after.

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
