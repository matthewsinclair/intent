---
issue: 0532
title: A board view does not indent a multi-line item's continuation lines: its sub-bullets render as sibling items, and a fenced block in an item can swallow the rest of the board
status: CLOSED
severity: medium
created: 2026-09-23
closed: 2026-09-23
reporter: Matthew Sinclair
---

Found (ic, 2026-09-23, in the review of 0525's `(edited)` surface). The instrument was `pandoc -f gfm`, positive-controlled on the same item text before and after an edit. `views::wb_board_body` renders each live board item as `- {text}`, with the text inserted raw (views.rs, its item loop). An item's text can run to several lines, and its continuation lines are not indented under the `- `, so they leave the list item:

- **Sub-bullets render as sibling items.** Take an item whose text is `two steps:` followed by `- first` and `- second`. The view renders it as three items, and nothing marks the last two as lines of the first.
- **A fenced block inside an item can swallow the rest of the board.** This is live today. Conflab's `intent/whiteboard/dc/wip.md` carries 5 `## ` headings, and `pandoc -f gfm` reads only 2 of them (`DevX Claude (dc)` and `DOING`), because dc's doing 10 is a three-line fenced block. Its opening fence sits inside the list item. The unindented lines after it leave the item, and its closing fence, at column 0, then opens a new block that runs to the end of the file. The board's other sections and its footer all render as code.
- **`wb show` and `wb pickup` also print the continuation lines at column 0**, below the item's `[kind] seq` line, where they read as unattached text.

The raw markdown, which is how a node reads a board, is complete in every case. The damage is to the rendered page.

**Population.** Measured over every live item on every board under `~/Devel/prj/*/intent/whiteboard`, at 2026-09-23 12:24Z by date -u:

- 77 of 1,118 live items are multi-line.
- They sit on 14 boards across 7 estates: Prolix 49, Gtools 18, Conflab 3, Baize 2, Intent 2, Laksa 2 and Devbin 1.
- By their last line, 1 ends with a fence, 17 with a list item and 59 with prose.
- The population moves as boards change. An earlier count, between 12:13Z and 12:15Z, read 78 multi-line items, 18 of them ending with a list item.

0525's `(edited)` mark met the same class from the other side. Appended after an item's last line, it turned a closing fence into "``` (edited)", which CommonMark does not accept as a closing fence. That half is fixed inside 0525, which moves the mark to the end of the item's first line (vc's ruling, 2026-09-23). This issue is what remains, and no placement of a mark addresses it.

**Fix direction and blast radius** (vc, 2026-09-23; confirmed in the source by ic). Indenting each continuation line by two spaces keeps an item's lines inside its list item, and it is the whole fix for the view. The cost is in doctor:

- `views::skew` compares every generated view byte for byte against the render, board views included.
- Its only tolerance is text the renderer owns (`differs_only_in_renderer_owned_text`: the footer, the Acceptance paragraph it writes, and since 0528 the todo view's generator marker). Indentation is not renderer-owned text.
- So a fix that changes the render alone would turn every board view holding a multi-line item into a counted `ViewSkew`, in every estate on this machine, until that board is re-rendered.
- With `doctor` on the pre-commit gate (0308), the gate would then refuse commits in those estates.

That holds unless the fix also teaches doctor the old shape, as 0528 did for `todo.md`: a board view that differs from the render only in how item continuation lines are indented would then read as a stale render, not a skew. `wb show` and `wb pickup` write no file, so their half of the fix carries no such cost.

Timing: vc is taking it to hv with a recommendation. hv's rule for 3.2.1 is every outstanding item.

**Ruled (hv first-hand, 2026-09-23, answering vc's question; hv's board, decision 32): fixed in 3.2.1.** hv, verbatim: "Yes, it's all got to go into 3.2.1. All of it." Board views indent a multi-line item's continuation lines, and doctor reads the old shape as an uncounted StaleRender, so no estate's commits are refused while its boards wait for a re-render. Routing: dc builds, on cc's 0525 stack; ic reviews the surface; vc judges.

**LANDED, NOT DEPLOYED (dc, 2026-09-23, on vc's word).** At 2e588c7cd on c90b4b1b9, and over its eight paths the landed tree equals the judged bank tree 199a11ece. The bank was refs/bank/dc/0532/v2 (blob 2c6442c07), cut on the stack of 0530 and 0525 and applying clean on their landed pair 084b154b9; `git apply --stat` reads 8 files changed, 613 insertions, 12 deletions. v2 is v1 (blob 60e20ebfc, patch-id c10efbc0d, the bank vc read) with ic's wording note (b) on doctor's note, and nothing else. The patch-id read 89d23af2d on the blob, on the staged paths before the commit, and on the landed commit: the id vc judged.

**THE FIX.** `views::item_lines` sets every line of an item after its first in by an indent, and leaves an empty line empty, so no trailing whitespace is left for a formatter. The board view renders each item as `- ` + `item_lines(edited_item_text(..), ITEM_INDENT)`, so it composes with 0525's mark at the end of the first line. `wb show` and `wb pickup` print the same lines set in four spaces under the item's own line. A one-line item is byte-identical in both.

**DOCTOR.** `views::board_before_0532` is the render with the indent taken back off every indented line, which is exact because the board render indents nothing else; `board_lines_outside_an_item_start_at_column_0` asserts that invariant over cc's board and hv's, every section, every header field, an empty section and the footer (vc's rider). `views::skew` asks `board_rendered_before_0532` straight after the renderer-owned-text arm and only of a node's board: a board an older Intent wrote reads as an uncounted StaleRender naming 0532, `<node>`'s next board write and `intent sync --to-disk`; any other difference is still ViewSkew.

**THE SCANS, AND THE 0525 GAP THEY CLOSE (vc's reading).** `text_spellings` carries four spellings: raw, JSON-escaped, the board view's (continuation lines set in), and the board view's with the `(edited)` mark after the first line. The fourth closes a gap in 0525 as banked: since F1 moved the mark to the end of an item's first line, re-editing a multi-line item that had been edited before dropped `wip.md` from the HEAD and next-commit listings. `board.json` still matched, so no all-clear went false, but the listing was short. Arm: `a_re_edited_multi_line_items_board_view_is_named_at_head`.

**THE JUDGING.** dc's run on 6a4d0fef9, from 13:13:17Z to 13:31:20Z by date -u. The base tree was e113f7eb9, HEAD's own; the bank tree was 199a11ece, HEAD plus v2.

- Rust: 3026/0/5 on the bank, against 3014/0/5 at baseline. The +12 are this issue's arms.
- bats: 743/743, with 1 skip, on both.
- fmt, clippy and doc: rc 0.
- Red sets: empty in both directions.
- The rlib grep found no member naming the other worktree, and each side's own path was its control.

vc passed the run.

**THE VERB, DRIVEN.** `intent sync --to-disk` rewrote a board in the older shape to this binary's render byte for byte, and doctor's note cleared; it writes as no node. It rewrites every generated view that differs, so the fleet sweep runs it only where doctor reads no counted ViewSkew first. The sweep also runs it only where `intent wb status` lists the estate's nodes, until 0535 lands: in a store holding no board, a `wb register` followed by `sync --to-disk` writes empty boards over every `board.json` (0535, measured by dc).

**THE FLEET AT DEPLOYMENT.** Read-only from every estate's `board.json` at 2026-09-23 12:43Z by date -u: 16 boards across 9 estates hold 101 live multi-line items, and those are the views that go StaleRender at deployment, uncounted. ic's 12:24Z scan of `~/Devel/prj/*/` did not reach the nested Arca estates (arca_cli vc 16, arca_config vc 9).

**DEPLOYMENT.** `native/rust/` deploys on neither save nor commit (0518). Boards take the new shape from hv's next `bin/devbin build all`, each at its node's next board write or at `intent sync --to-disk`; until then an older board reads as a stale render, which blocks nothing.

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
