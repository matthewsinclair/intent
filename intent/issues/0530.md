---
issue: 0530
title: wb claim and wb unclaim print a bare <ID> in --help, and the claim forms are said from a second home on MCP and not at all in the whiteboard skill
status: CLOSED
severity: low
created: 2026-09-23
closed: 2026-09-23
reporter: Matthew Sinclair
---

Found (ic, 2026-09-23, in the 3.2.1 pre-cut surface pass, and re-measured with `wb edit`'s arguments as the positive control). Since 0511 a board can claim an issue, so `intent wb claim <id>` takes three forms: a thread as `ST0000`, a work package as `ST0000/01`, and an issue as `ISSUE:0000`. The surfaces a reader consults say so unevenly, and from more than one home.

- **`--help` says nothing.** `wb claim` and `wb unclaim` print a bare `<ID>` under Arguments, because their `id` arguments carry no `help` in `surface/dispatch-table.json`. `wb edit`'s arguments print theirs, so the omission is the rows', not the renderer's. The issue form, which is 3.2.1's new surface, is discoverable at the CLI only by being refused. The refusal does name the forms, from 0519's `model::CLAIM_ADDRESS_FORMS`.
- **MCP says it from a second home.** `mcp.rs` hand-writes the `claim-address` description ("what a board can claim: a steel thread as ST0000, a work package as ST0000/01, or an issue as ISSUE:0000"). `CLAIM_ADDRESS_FORMS` does not feed it: 0519 gave that sentence one home for the two refusal doors, and this third door stayed outside it.
- **The whiteboard skill is stale.** The `claim` section of `intent/plugins/claude/skills/in-whiteboard/SKILL.md` reads "`intent wb claim <STxxxx> --node <you>` ... It takes a work package as well as a thread" and never mentions an issue, in the skill every node loads.

Not affected: neither the reference pages nor `intent llm guide` renders argument help for any verb, so neither is a door for this sentence.

**Ruled (vc under hv's pen, 2026-09-23): fixed in 3.2.1, widened to all three findings.** `mcp.rs` derives the `claim-address` description from `model::CLAIM_ADDRESS_FORMS`, which removes a real second home. The `id` help on `wb claim` and `wb unclaim` carries the one-home sentence, a copy made legitimate only by a drift arm holding it byte-equal to the constant, the way `every_form_the_refusal_names_is_one_the_validator_admits` holds the refusal's sentence to the validator. The same arm holds the skill's naming of each form, and the skill's line is corrected. A Fixed line in CHANGELOG. It banks and takes one judging run over this bank and cc's 0525 and 0529, which share SKILL.md and CHANGELOG.md. Routing: ic builds, vc judges.


**LANDED, NOT DEPLOYED (ic, 2026-09-23, on vc's PASS).** At 084b154b9 on d3b2250b6 (cc's 0525), from refs/bank/ic/0530/on-1ac4079f2 (blob 6890365f4). The patch-id read b4e176144 on the blob, on the applied tree before the commit, and on the landed commit. Over the 24 files the two banks touch, `git diff 9a5d8276b HEAD` equals `git diff 9a5d8276b 074c8da3d`, which is the judged tree.

**THE JUDGING.** cc's stacked run on 9a5d8276b with 0525 and this bank (tree 074c8da3d):

- Rust: 3014/0/5 on the bank, against 3007/0/5 at baseline.
- bats: 743/743, with 1 skip, on both.
- fmt, clippy and doc: rc 0.
- Red sets: empty in both directions.

vc passed the run. ic and cc each drove the composition in both orders, and both reached one tree.

**DRIVEN** on the judged binary, with the installed pair as the control:

- `wb claim --help` and `wb unclaim --help` print "What to claim: " and "What to drop: " followed by the forms sentence, where the pair printed a bare `<ID>`.
- MCP's intent_wb_claim and intent_wb_unclaim describe `id` from model::CLAIM_ADDRESS_FORMS, where the pair carried the hand-written second home.

**DEPLOYMENT.** `native/rust/` deploys on neither save nor commit, so the installed pair shows the help and the MCP description from hv's next `bin/devbin build all`. The skill text is read from this tree by `intent claude skills sync`, so it reaches a machine's `~/.claude/skills` at that machine's next sync.

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
