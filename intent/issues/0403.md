---
issue: 0403
title: wb migrate turns a board's ### sub-headings into items that carry a literal ###, so the grouping they recorded survives only as order within a kind
status: OPEN
severity: medium
created: 2026-09-16
reporter: Matthew Sinclair
---

`intent wb migrate` turns a board's `###` sub-headings into items whose text carries a literal `###`, and reports nothing, because nothing was dropped. Six on devbin's boards; carry-map reads the same six. The groups recorded whose work each item was, and they now survive only as order within a kind: `seq` carries no group, so the first `archive` or `add` that moves an item makes the grouping unrecoverable. Devbin letter A, measured by both devbin/dc and devbin/vc.

Found by devbin migrating its boards on Intent 3.0.3 (`d24487079`), sent through intent/vc on hv's routing, and ruled by hv on 2026-09-16 to be fixed in this release (hv decision 18). The full measurement record, with the instrument (`carry-map.sh`, driven red and green) and who measured what, is devbin `1824c75`, `intent/whiteboard/dc/.history/20260916/wb-upstream-defects-draft.md`.

**Shared cause with the other migrate carry issues filed with this one (devbin letters A, B, D, E and F):** `wb_item` is `id, node, kind, seq, text, state, archived_at, recorded_at, authored_at, updated_at`, and has no field for a board lead, a section lead, a sub-heading, a group, a table or a heading qualifier; the section a line renders under is derived from `kind`. Whether the answer is a richer model or a migrator that never coerces and reports everything it cannot carry is ruled once for all five.

Fix: carry the group, or report the sub-heading as uncarried with its line. A sub-heading never becomes an item.

---

_Generated by Intent v3.0.3 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
