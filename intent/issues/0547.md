---
issue: 0547
title: The hook shim's --where names an empty install pointer UNUSABLE, where its own gate path and bootstrap --check name it ABSENT
status: CLOSED
severity: low
created: 2026-09-23
closed: 2026-09-23
reporter: Matthew Sinclair
---

Noted by vc on 2026-09-23 and held as a follow-up until hv's ruling put everything found into 3.2.1. Read from source at 3dd2c6619.

**What happens.** An empty pointer file, `~/.local/share/intent/home` present with nothing in it, gets two names from the same install:

- The shim's own commit path (`lib/templates/hooks/pre-commit-shim.sh`, FAILURE 1) treats absent and empty as ONE state on purpose. Both mean `intent bootstrap` has not run, and both have one remedy.
- `intent bootstrap --check` follows that path and prints `state:    ABSENT (the pointer file exists and is empty)`.
- The shim's `--where` prints `root:     <empty>` and `state:    UNUSABLE (no lib/templates under that root)`.

Both exit 1 and both point at `intent bootstrap`, so only the word differs, and the word is what a reader acts on. UNUSABLE sends them looking for a broken root when no root was ever named. `bootstrap_check`'s doc comment in `render.rs` records the divergence ("ONE READING DIFFERS") rather than removing it.

**What the fix has to do.**

- The shim's `--where` names an empty pointer the way its own gate path and `bootstrap --check` do: ABSENT, saying the file exists and is empty.
- UNUSABLE stays for a pointer that names a root with no `lib/templates` under it.
- The doc comment in `render.rs` loses the paragraph about the difference.
- A test drives `--where` and `bootstrap --check` over the same empty pointer and asserts the same state.
- Intent's own installed carrier is regenerated with `intent claude upgrade --apply` before the cut, because the release preflight refuses a canon write.


**Built and landed.** The shim's `--where` now prints `state: ABSENT (the pointer file exists and is empty)` with no root line, and exits 1, when the pointer's first line is empty. That is the test `install::first_line` applies, so a zero-byte file and a lone newline both read ABSENT, as `bootstrap --check` and the shim's FAILURE 1 already named them. UNUSABLE stays for a named root with no `lib/templates` under it. The "ONE READING DIFFERS" paragraph is gone from `bootstrap_check`'s doc comment. `check_on_an_empty_pointer_says_it_is_empty` drives `bootstrap --check` and the shim's `--where` over one HOME, for both forms of empty, and asserts the same state and exit 1. The control is the shim at HEAD before the landing, which printed `root: <empty>` and UNUSABLE for the same pointer.

**Landed** at 75bbc1aae, as refs/bank/ic/0547 v2, on 3d3f04289. The patch-id reads d82be33e650c on the blob and on the landed commit. v2 dropped the shim comment's issue citation, which 0542's installed-payload arm refuses in shipped templates. It was judged in dc's stacked run over 0542, 0546 and 0547 v2, which vc passed: bats 747/747 on the stack, and no red was 0547's. The CHANGELOG line is vc's to land, with 0542's. Not deployed until the next build all; Intent's own carrier is regenerated with `intent claude upgrade --apply` straight after this close.

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
