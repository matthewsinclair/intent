---
issue: 0534
title: intent --version prints one shape for a release and a dev build, so its output cannot say which is installed
status: CLOSED
severity: low
created: 2026-09-23
closed: 2026-09-23
reporter: Matthew Sinclair
---

Reported on 2026-09-23 by molt-vc, sent to Intent's vc at hv's request. Filed into 3.2.1 on hv's ruling of the same day: "it's all got to go into 3.2.1. All of it." Not yet driven in Intent.

Seen, as reported: a brew-installed release and a dev-tree build both print `intent 3.2.0 (<hash>)`. The release carries its tag's hash, and a dev build carries the HEAD it was built at (hv decision 15). Nothing in the line says which kind of build it is, so telling them apart means knowing which hash a tag names.

Expected: `intent --version` says whether the build is a release or a dev build, and no consumer of today's form breaks.

Unmeasured:

- Every consumer of today's form, including `intent daemon status`'s comparison, the pre-push currency check, the brew formula's test, `int macos smoke`, the app, and the measurement steps in `.claude/restart.md` and `intent/restart.md`.
- How a build would know it is a release: from the release verb, or from HEAD sitting exactly on a `v<version>` tag.

Routing: cc takes it after 0525 lands, because cc owns the stamp in build-support. cc proposes the form and the measured consumers to vc before building.

Closed (cc, 2026-09-23, on vc's PASS):

- LANDED at 6fd9268ff from refs/bank/cc/0534/on-ca936e6c4 (patch-id d2008af6f, the same before and after the commit), judged in one stacked run with 0533, 0535, 0536 and 0537, and 0538 on ca936e6c4.
- The line ends in the build's kind: `release` for a clean build of a commit tagged `v<version>`, and `dev` for every other build, `dirty-` and `unknown` included, on `intent --version` and `intentd --version` alike. The commit value and its `[intent-source-commit:]` marker do not move. The kind has its own marker, `[intent-source-kind:]`, which `install::embedded_marker` reads with the key `kind`.
- vc ruled the form with five riders, and each was driven: the six tag scenarios in a scratch clone (a lightweight and an annotated tag read `release`; a dirty input at the tag, one commit past it, no tag and another version's tag read `dev`); `bin/int macos app-test`, 57 of 57, with the app's menu row keeping the word and still reading an older CLI's line; every consumer of the line on the built pair (`resolve_intentd` through daemon start, status, restart, stop and run, with a sibling reporting another version refused by name, the formula's assert, the release verb's stamp step, smoke's `staged_version` and the prepush libraries); the CHANGELOG line and the dispatch table's `version` row.
- Proved by `version_spellings_agree`, which holds the line to the embedded values and drives the decision table through `build_support`, the build scripts' own file, and by intentd's `arguments_do_not_start_a_daemon`.
- NOT DEPLOYED until the second `bin/devbin build all`.

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
