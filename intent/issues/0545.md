---
issue: 0545
title: wb edit in a project without git prints that no file holds the old text, though neither search ran
status: CLOSED
severity: low
created: 2026-09-23
closed: 2026-09-23
reporter: Matthew Sinclair
---

Found by cc on 2026-09-23 while verifying the 3.2.1 release notes against the landed tree.

**What happens.** `intent wb edit` (0523) reports whether any file under `intent/` still holds an edited item's old text: at HEAD, and in what the next commit would carry. In a project that is not a git repository, neither search can run. The answer still prints the all-clear, "no file under intent/ holds the old text, at HEAD or in the next commit" (facade.rs:8779-8783 and 8815-8819; render.rs:4496-4501), and `wb_edit_door.rs` asserts that sentence in a project with no git. So the verdict claims a search that never ran. It is true only vacuously, and it reads exactly like a clean result.

**What the fix has to do.** Where the project is not a git repository, the answer says the two searches did not run and why, and claims nothing about HEAD or the next commit. The arm in `wb_edit_door.rs` asserts that wording in place of the all-clear. A project with git reads exactly as before.

Closed (cc, 2026-09-23, on vc's PASS), in one bank with 0544:

- LANDED at eab995890 from refs/bank/cc/0544-0545/on-3be665d61 (patch-id d2e74c897), judged in the same two-bank run as 0544.
- Outside a git work tree the scan of what the next commit carries returns unmeasured, so no all-clear prints, and a new `EditUnsearched` note says: `note: this project is not a git repository, so neither HEAD nor the next commit was searched for the old text`. Its JSON kind is `edit-unsearched`. Nothing is claimed about HEAD or the next commit.
- Proved by `wb_edit_door::an_edit_says_where_the_old_text_went_and_the_board_reads_the_new`, which asserts the note and the absence of the all-clear in a project with no git; `a_clean_head_and_an_amended_draft_give_the_all_clear` is the control in a real repository.
- NOT DEPLOYED until the second `bin/devbin build all`.

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
