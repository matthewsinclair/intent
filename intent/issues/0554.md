---
issue: 0554
title: a pull brings a teammate's board files but not their rows into the store, and the counted skew it causes names a remedy that refuses
status: OPEN
severity: medium
created: 2026-09-24
reporter: Matthew Sinclair
---

Found by cc on 2026-09-24, between cc's 10:00Z and 10:03Z clock reads, while driving the team guide for the 3.2.1 doc audit: two clones of one origin under short isolated HOMEs, on the dev pair at `4c687eaad`. The logs are cc's `team/explore-1.log` and `team/explore-2.log`; vc read the pull and pickup in `explore-2.log` (lines 144-157).

Seen: a pull carries a teammate's board files (`board.json`, `wip.md`, the inbox views) and the `wb` event files, but the post-merge hook's `sync --apply` takes only threads, issues and event files into the store, never board rows. So:

- On a clone with no board of its own, every `wb` verb refuses with "board.json on disk records a migrated board that this store does not hold ... `intent sync --to-store`", while commits still pass, because store-stale is an uncounted advisory.
- On a clone whose store holds boards: Bob ran `intent sync --to-store`, registered `bo` and sent `al` a message; Alice pulled, and post-merge printed a COUNTED `doctor: 1 finding(s)` -- view skew on `intent/whiteboard/al/inbox.bo.md`, "246 bytes on disk, 198 rendered" -- so her commits are refused. That finding's printed remedy is `intent sync --to-disk`, which misdirects: run, it REFUSES and names `intent sync --to-store` ("board.json on disk records a migrated board that this store does not hold ... `intent sync --to-store` carries each board.json ..."), and the message stays on disk. When the sender's board is already held and a second message arrives, doctor counts nothing, and `--to-disk` is refused by the 0260 guard ("the canon on disk for board al changed after the store last wrote or read it ... run `intent sync --to-store`"). cc measured both on 2026-09-24 after first reporting, unmeasured, that `--to-disk` discarded the message; that was an inference from the remedy text, and it is false. Her `intent wb pickup --node al` shows `messages (0)` and `peers (0)`.

Why it matters: the whiteboard is committed but does not travel with the rest of the project -- a pull does not carry board rows into the store, by design (`ingest.rs:1176-1197`, the 0216 revert class) -- so a teammate's items and messages are invisible to `wb` verbs until `intent sync --to-store`, the recipient's commits are refused on first contact, and the printed remedy misdirects to a command that refuses. No message is lost. hv's fleet is not exposed: its nodes share one checkout and no teammate pulls a board into it.

Expected: a pull that brings board files brings their rows into the store the way it brings threads and issues, and a skewed board view's remedy names `intent sync --to-store`.

The doc half, whatever is ruled: the team guide says as built that boards are committed but a pull does not take them into the store, that `intent sync --to-store` after pulling a teammate's board is the step, and never `--to-disk` on a pulled inbox view. If a fix rides 3.2.1, the guide describes the fix instead.

Disposition: medium, not shipping-grade once measured -- vc's recommendation to hv is after the stabilisation line. cc's minimal design for carrying boards on the hooks' path is recorded for then.

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
