---
issue: 0573
title: three register rows declare an effect their verb does not have: export writes while declared read, ingest writes nothing while declared mutate, and todo
status: OPEN
severity: low
created: 2026-09-24
reporter: Matthew Sinclair
---

Found by ic on 2026-09-24 in the 3.2.1 doc audit's register sweep.

Seen: three rows in `surface/dispatch-table.json` declare an effect the verb does not have. `intent export --format md` writes `intent/.backup/text/<stamp>/` yet is declared `read`; `intent ingest` writes nothing yet is declared `mutate` and one-way; and `intent todo`'s read/mutate declaration disagrees with what it does. A row's declared recoverability decides whether the verb is offered on the MCP tier, and `intent_export` is an MCP tool today.

Expected: each row declares the effect its verb has.

Disposition: vc, 2026-09-24 -- filed for after the stabilisation line: correcting a declaration moves recoverability and with it MCP exposure, which is a surface change rather than text. The docs say what `export` and `ingest` actually do.

---

_Generated by Intent v3 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
