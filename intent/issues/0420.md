---
issue: 0420
title: A store write can meet a write lock held over 5s with intentd alone on the store
status: OPEN
severity: medium
created: 2026-09-16
reporter: Matthew Sinclair
---

## What happened

During dc's close run after migrate's landing (292414661, closes committed at 61a88ae21), 2026-09-16 between 16:46Z and 16:51Z, four of the run's store writes failed with `database is locked`: `issues edit 0407` twice (16:47:47Z and the re-issue at 16:48:00Z), `issues edit 0408` at 16:49:49Z, and `issues edit 0409` at 16:50:41Z. The re-issues for 0408 and 0409 succeeded. Each write was one command, with a settle read (`intent issues show`) in its own command before it.

The store's busy timeout is 5000 ms (`Store::BUSY_TIMEOUT_MS`, store.rs), so each failure was a write lock held for more than 5 s by another connection.

## What was measured

- `lsof` on `intent/.cache/intent.db` and `intent.db-wal` named only intentd 25378 (the delivered pair), at 0.0% CPU, with no other `intent` process. A short-lived process that took and released the lock before `lsof` ran would not appear there.
- The locks came about 5 s after the preceding settle read and 12 to 20 s after the preceding close. They did not come a few seconds after a write, so waiting for intentd's ingest to settle was not what freed them.
- `event_log` from 16:46:30Z shows only dc's own issue writes. The delivered pair predates the landed logging of whiteboard writes (vc decision 21 (2)), so a whiteboard or session-hook write in that window would not be logged.
- At vc's probe at 16:48:46Z, a `BEGIN IMMEDIATE` with a zero timeout took the lock at once and rolled back.
- `intent.db` was 152920064 bytes and `intent.db-wal` 98175512 bytes, the same size at both captures. That is a lead, not a cause.
- intentd's 3 s `sample`, taken about 5 s after each lock, holds no sqlite frames. The holder had already let go, so the capture proves nothing about intentd's own role.

Captures are in dc's scratchpad as `lock-0408a-{ls,lsof,sample}.txt` and `lock-0409a-{ls,lsof,sample}.txt`. They are lost at the next reboot.

## Not yet known

- Which connection holds the write lock. The candidates not yet excluded: intentd's own write path (an ingest or a checkpoint), short-lived `intent` processes from other sessions (the session hooks run on every prompt in every open Claude session), and the settle read itself.

## Next capture

Catch the holder while it holds the lock. Run `sample` and a process listing (`ps -axo pid,etime,command | grep -E 'intent'`) in the same command as the write, started just before it, rather than after the failure. Take it in the quiet window, after the rebuild, where whiteboard writes are logged to `event_log`.

---

_Generated by Intent v3.0.3 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._

## Added 2026-09-16: intentd was refused too

intentd's own log shows the daemon refused in dc's window, read by ic from hv's Console screenshots and confirmed by vc in `~/.local/state/intent/intentd.err.log`:

- 16:46:51.619Z, 16:53:18.568Z and 16:55:02.160Z: `ingesting ... after an external edit failed: error: could not read the committed canon / caused by: sqlite: database is locked`.
- 16:46:56.956Z and 16:47:41.167Z: `could not refresh the index under intent/..intentfiles.<pid>.0.intent-tmp, intent/.intentfiles: could not update the runtime store`.

So intentd's ingest was a waiter, not the holder, in at least those instances, which moves the suspicion away from the daemon's own writes and toward another connection that `lsof` did not show at the moment of capture.

A second question, whether or not it is related: the index refresh names atomic-rename temp files (`..intentfiles.<pid>.0.intent-tmp`, `.0362.json.<pid>.0.intent-tmp`) as paths to index. A write's temp file should be invisible to the watcher, so each rename may cost a refresh attempt on a path that no longer exists.

After the quiet window's rebuild (pair at 3e0a87f23), ic's 0419 landing, ic's 0421 and 0422 filing and landing, and cc's canon commit ran with the pre-write capture armed, and none met the lock. That is an absence on the rebuilt pair, not a fix.

## Added 2026-09-16: a refusal on the rebuilt pair, captured within seconds

On the rebuilt pair (3e0a87f23, intentd 72242), vc's `intent st done ST0057` started at 19:07:21Z and was refused with `database is locked`. vc's `intent st done ST0075` had succeeded at 19:07:08Z (logged `st.done` 19:07:08.871Z), and it wrote canon, views and `.intentfiles` to disk. vc's capture at 19:07:31Z, saved as `lock-st0057.txt` in vc's session scratchpad:

- `ps` showed no `intent` process except intentd 72242, the four sessions' `intent mcp` servers, and `intent wb archive doing 40 --node ic` (pid 54853, elapsed 00:04, so started about 19:07:27Z). Its `wb.archive` event is logged at 19:07:27.163Z.
- `lsof` showed only intentd 72242 and that `wb archive` with the store and WAL open. The `intent mcp` servers did not have it open.
- intentd logged no error in that minute.

The refusal window (19:07:21Z plus the 5 s busy timeout) closed before ic's `wb archive` started, so that process was not the holder. The capture shows no other process with the store open, which leaves intentd, 13 s after a close that wrote its files to disk, or a process that exited before 19:07:31Z. The `wb archive` still held the store open 4 s after its own logged write. Nothing here proves which connection held the lock.


## Added 2026-09-16: the refusals began the evening before

intentd.err.log carries the daemon's own refusals before dc's window, read by ic from hv's Console and confirmed by vc: 2026-09-15T22:42:02.572Z (ingest after an external edit), 2026-09-16T15:18:21.872Z (index refresh under `intent/..intentfiles.7530.0.intent-tmp`) and 2026-09-16T16:13:23.388Z (ingest). The intent-tmp refresh recurs with a different pid each time (7530, 78517, 20283). So the lock predates the close-out's landings of 2026-09-16 and is not caused by any one of them.



## Added 2026-09-16: a refusal seconds after the refused process's own write

vc filed 0427 (`issues.add`, logged at 19:54:05.153Z), then ran `intent issues edit 0427 --title ...`, which was refused with `database is locked` between 19:54:05Z and 19:54:23Z. The command was not stamped, so the refusal's exact time is not known.

- `event_log` from 19:53:30Z to 19:54:45Z holds only vc's two writes: the add, and the re-issued edit at 19:54:36.384Z. Whiteboard writes are logged on this pair, and none was logged in the window.
- intentd.log and intentd.err.log hold no line from 19:53Z to 19:55Z.
- `lsof` at 19:54:28Z showed only intentd 36146 with the store and WAL open. `ps` showed no `intent` process except intentd, the four sessions' `intent mcp` servers, and one `intent daemon logs --follow`.
- The single re-issue at 19:54:36Z succeeded. A watch running beside it saw only the re-issue's own process (pid 6637, 19:54:36Z to 19:54:39Z) and intentd with the store open.
- `intent.db` stayed at 152920064 bytes and `intent.db-wal` at 98175512 bytes, the same as at every earlier capture.

No other writer was logged or seen. The refusal fell within seconds of an add that realised a new canon file, a view and an `.intentfiles` line, which intentd ingests. That fits intentd's post-write ingest holding the lock past the 5 s busy timeout, but does not prove it. The captures are in vc's scratchpad as `lock-0427a-{lsof,ps,ls}.txt` and `lock-0427b-watch.txt`.

---

_Generated by Intent v3.0.3 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
