---
issue: 0541
title: Two wb remedies still name the bare roster form of wb register, which registers nothing on a generated board
status: CLOSED
severity: low
created: 2026-09-23
closed: 2026-09-23
reporter: Matthew Sinclair
---

Found by ic on 2026-09-23 while re-reading 0522's CHANGELOG entry, and measured by dc against 0535's bank, which leaves both texts as they are.

**What happens.** 0522's entry says every remedy that sends someone to register a node prints the same form, `model::register_form`, with `--name` and `--role`. Two still print the bare roster form:
- `WbNodeNotRegistered` (facade.rs:2431): "... put on the board by `intent wb register`, which reads the roster from each node's own `wip.md` header";
- `intent wb status` with no nodes (render.rs:4636): "remedy: `intent wb register` reads the roster from each node's own `wip.md` header".

On a generated board there is no hand-authored header for the roster form to read. So in a project that has no `board.json` yet, which is where these two remedies are reached once 0535 puts its own refusal in front of them for a clone that lacks its boards, the named remedy registers nothing.

**What the fix has to do.** Both print `model::register_form(..)` as the remedy, and keep the roster form as the second door, for a board whose `wip.md` is still hand-authored. The arms drive both texts in a project with no `board.json`.

**Ruled (vc under hv's pen, 2026-09-23, under hv's decision 32): fixed in 3.2.1, in one bank with 0539.** Both texts print `register_form`, and name the roster form as the second door, for hand-authored headers. The arms drive both in a project with no `board.json`.

**LANDED, NOT DEPLOYED (dc, 2026-09-23, on vc's word).** At a713eeb1f on 77e837b68 (0535 v4), from one bank for 0539 and 0541: refs/bank/dc/0539-0541/v1 (blob c195a5900), cut on 0535 v4's tree. `git apply --stat` reads 8 files changed, 197 insertions and 21 deletions. The patch-id read 99d3908bd on the blob, on the staged paths before the commit, and on the landed commit: the id vc judged. Over the twenty paths it and 0535 touch, HEAD equals the tail's judged tree cf898d342.


**THE FIX.**
- `WbNodeNotRegistered`'s remedy now names `intent wb register <moniker> --name "<display name>" --role <role>`, with the moniker that was typed. Only after that does it name `intent wb register` with no arguments, for boards whose `wip.md` is still hand-authored.
- `intent wb status` with no node registered prints the same, with `<moniker>`.
- `WbHeaderIncomplete` keeps the bare form, because its remedy re-runs the roster form that failed.
- 0535 put its `sync --to-store` refusal in front of both texts. A store lacking the boards on disk never reaches them, and every other empty roster now names the command that works.

**THE ARM.** intent-cli `wb_register_correct.rs`, `an_empty_roster_and_an_unregistered_node_name_the_whole_form`. In a fresh project with no `board.json`, `wb status` names `register_form("<moniker>")`, and `wb show zz` refuses naming `register_form("zz")`.

**THE JUDGING.** dc's tail run on f155641bd, from 14:38:56Z to 14:55:47Z by date -u, over five banks: 0535 v4, 0539-0541 v1, and ic's 0540 v1, dtable v1 and 0538-tune v1. The base tree was 4a68a6489, HEAD's own. The bank tree was cf898d342, and every bank applied at its own patch-id.

- Rust: 3068/0/5 with the five banks, against 3055/0/5 at baseline. The +13 are 0535's ten arms and 0539+0541's three.
- bats: 743/743, with 1 skip, on both.
- fmt, clippy and doc: rc 0. app-test: 57 tests, 0 failures.
- Red sets: empty in both directions.
- The rlib grep found no member naming another worktree, and each side's own path was its control.

vc passed the run.

**DEPLOYMENT.** `native/rust/` deploys at hv's next `bin/devbin build all`.

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
