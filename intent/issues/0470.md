---
issue: 0470
title: release: a silenced worktree remove, a VERSION that falls back to 0.0.0, and unchecked mktemp/cp under set -u only
status: OPEN
severity: low
created: 2026-09-19
reporter: Matthew Sinclair
---

Three places in the release driver still drop a failure without a word (IN-AG-NO-SILENT-001):

- `git worktree remove --force "$gate_dir/tree" >/dev/null 2>&1` (`bin/.devbin/cmd/build.d/release:680`): a failed removal leaves a registered worktree behind and says nothing.
- `CURRENT="$(cat "$PROJECT_ROOT/VERSION" 2>/dev/null || echo "0.0.0")"` (`:703`): an unreadable VERSION silently becomes 0.0.0, so the semver comparison that follows is made against a made-up number.
- The script runs under `set -u` only (`:71`), and several `mktemp` and `cp` calls in the stamping section are unchecked.

XS each. Found by critic-shell during dc's release-guard work on 2026-09-18.

---

_Generated by Intent v3.1.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
