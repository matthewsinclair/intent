---
issue: 0414
title: sync --to-store prints that nothing the store held was overwritten while it renumbers whiteboard rows and changes a node row
status: OPEN
severity: medium
created: 2026-09-16
reporter: Matthew Sinclair
---

`intent sync --to-store` prints "nothing the store already held was overwritten" while it changes whiteboard rows. In devbin/vc's scratch rehearsal (a copy of devbin, column diff against a `sqlite3 .backup`, Intent 3.0.3 `d24487079`) the command, printing that line, changed a node row's name and role (`devbin-dc` / `dc` to `DevX Claude` / `devx`), renumbered the `id` of 89 of 89 `wb_item` rows and 9 of 11 `wb_message` rows, and rewrote `updated_at` on all 104 `wb_*` rows. Order within each board and inbox was preserved and `authored_at` survived. Devbin letter N, measured by devbin/vc and not re-measured by devbin/dc.

Found by devbin migrating its boards on Intent 3.0.3 (`d24487079`), sent through intent/vc on hv's routing, and ruled by hv on 2026-09-16 to be fixed in this release (hv decision 18). The full measurement record, with the instrument (`carry-map.sh`, driven red and green) and who measured what, is devbin `1824c75`, `intent/whiteboard/dc/.history/20260916/wb-upstream-defects-draft.md`.

Related closed issue: 0111, "sync --to-store reported overwriting nothing and replacing the store", severity high. Same claim, narrower subject; this may be a recurrence through the whiteboard tables, which postdate that fix. Severity set to medium here rather than devbin's proposed low for that reason: it is a false statement about what a write changed.

Fix: the message is true of every table the sync writes, the whiteboard tables included -- either the sync preserves row identity and says nothing was overwritten, or it reports what it replaced.

---

_Generated by Intent v3.0.3 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
