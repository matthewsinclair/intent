---
issue: 0522
title: wb register's two argument refusals carry no remedy, so the form that works is never shown
status: CLOSED
severity: medium
created: 2026-09-23
closed: 2026-09-23
reporter: Matthew Sinclair
---

Seen (hv, 2026-09-23, in ~/Devel/prj/Gtools, hv's terminal as pasted to vc):

- `intent claude start dc` refused, correctly, and its remedy showed the form that works: `intent wb register dc --name "<display name>" --role <role>`.
- `intent wb register dc --role dc` printed ``error: a node named on the command line needs both `--name <display>` and `--role <role>` `` and nothing else.
- `intent wb register --name dc --role dc` and `intent wb register --name dc` printed ``error: `--name` and `--role` describe the node named as `<moniker>`; with no moniker, `wb register` reads every node's own header`` and nothing else.
- Neither refusal prints a remedy. hv could not get from either one to the command that works, and had to ask why dc could not be registered.
- `intent wb register --help` gives `[MONIKER]` no description, and `--name` reads "The node's display name, with `<moniker>`".
- Source: `native/rust/crates/intent-cli/src/render.rs:4176-4188` returns both refusals as bare `Failure::Error` strings. Project rule 3 says every error path carries a remedy.
- A second casualty of the same surface: Gtools' `ic` is registered with name `gtools-ic` and role `ic`, which are the session name and the moniker, the thing the `claude start` remedy warns against.

Expected:

- Each refusal carries a remedy printing the whole form, `intent wb register <moniker> --name "<display>" --role <role>`, with the moniker filled in where one was typed, and naming `intent wb status` for the nodes that exist.
- The flags-without-moniker refusal says the moniker is missing and that it comes first, as a positional.
- `--help` describes MONIKER (the node's handle, eg `dc`), `--name` (a display name, eg "DevX Claude") and `--role` (one word, eg `worker`).

Ruling (hv, 2026-09-23): fix it now. It is a 3.2.1 item.

**LANDED, NOT DEPLOYED (dc, 2026-09-23, on vc's word).** At 987268553 on e1d784dbc, from refs/bank/dc/0522 (blob 31bab441b, based at c419e8719; `git apply --stat` reads 11 files changed, 138 insertions, 24 deletions). The patch-id was read from the bank blob and again from the landed commit, 30c4618a8 both times: the id vc judged. The generator's half, `intent/st/ST0056/parity/tools/gen_dispatch_table.sh`, is an ST0056 attachment, and an attachment edit cannot be judged green in a worktree: the attachment-drift arm reds on it by construction until intentd has ingested it. So it landed on its own chain at b8321e77c, and the ingest was proven by three equal hashes: the file on disk, canon's recorded sha256 and the sha of canon's text.

**THE FIX.** `intentsvcs::model::register_form` is the one home for the form every remedy sends someone to, spelled as `intent claude start` prints it: `intent wb register <moniker> --name "<display name>" --role <role>`. The three argument refusals (the two above, and `--correct` without all three) print it whole, with the typed moniker filled in or `<moniker>` where none was typed, an eg, and `intent wb status` for the nodes already registered; the refusal for flags with no moniker says the moniker is missing and comes first, as a positional. The facade's `WbSendersNotRegistered` and `WbCorrectUnregistered` remedies print the same function's form, and `claude_cwi_door` holds the bash door's copy of it to that function. `--help` describes MONIKER, `--name` and `--role` from `surface/dispatch-table.json`, where an argument now carries an optional `help`, and the dispatch-table generator renders it.

**THE JUDGING.** dc's whole suites at the bank, in the private worktree tmp/wt-0522 with its own target and an isolated `HOME`: intent-cli 1067 passed, 0 failed; intentsvcs 1830 passed, 1 failed. The one was `attachment_drift_detected::every_realised_attachment_in_the_estate_still_matches_canon`, the worktree artefact named above; the drift module is 7 of 7 without the attachment edit. fmt, both CI clippy lines and the doc step were clean, and vc passed it. Then cc's train on 987268553, which carries this fix, with 0520, 0523 stage 1 and 0524 on top, running CI's rust lines: fmt, both clippy lines, the doc step and the workspace test. Its four reds were all 0523 stage 1's missing declarations, and cc's re-run, ended at 08:39:53Z by cc's clock, was green throughout.

**DEPLOYMENT.** `native/rust/` deploys on neither save nor commit (0518). This machine's `intent` prints the new refusals and help from hv's next `bin/devbin build all`; until then the gate's currency line reads REFUSING and a push is refused (decision 30).

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
