---
issue: 0552
title: intent explore with an address it cannot open lands at the top and wipes the reason before the first frame
status: CLOSED
severity: medium
created: 2026-09-24
closed: 2026-09-24
reporter: Matthew Sinclair
---

Found by ic on 2026-09-24 in the 3.2.1 doc audit, driven in a pty on demo projects with the pair at `4c687eaad`.

Seen: `intent explore nosuch`, `intent explore 0001` (an issue id), `intent explore ST0001/02` (a work-package specifier) and `intent explore 1` each open the thread list with the ordinary hint row, and no reason appears anywhere on screen. The same spellings typed into the explorer's box DO show the reason on the status row, eg "`ST0001/02` is neither an address nor an artefact id -- name a thread (`ST0000`) or a full ad…".

Mechanism, read by ic at HEAD: `render.rs:5876-5885` opens at the root and sets the reason with `.saying(...)`, and its own comment says a browser that silently opens somewhere other than you asked is the class the rewrite exists to remove. `run()` then calls `arrive()` for the first read (`run.rs:637-639`) with the root view's own note, which is none, and `arrive()` sets `app.notice = note.unwrap_or_default()` (`run.rs:938`), which wipes the reason before the first frame is drawn. It is issue 0435's class, a notice set and cleared before any frame draws it, and it breaks IN-AG-NO-SILENT-001: the operator takes the thread list for the thing they named.

Expected: an address the explorer cannot open lands where it lands WITH its reason on the status row, from the command line exactly as from the box.

Disposition: hv's, asked on 2026-09-24, against the shipping-grade test of hv decision 33 (1).


Fixed at `3f584ccea`, landed on 2026-09-24 on vc's word; ships in 3.2.1.

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
