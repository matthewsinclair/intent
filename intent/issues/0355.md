---
issue: 0355
title: the index watch registration is root-recursive and its debouncer cache walks the whole tree, target dirs included, with a stat per entry on every rescan
status: OPEN
severity: medium
created: 2026-09-13
reporter: Matthew Sinclair
---



The daemon's second watch registration, added for the index under ST0069 WP-18 (25ca2e9ac), watches the project ROOT recursively (`native/rust/crates/intentd/src/watch.rs:154-164`); at 3.0.1 only `intent/` was recursive (watch.rs:127). The registration goes through notify-debouncer-full 0.7.0, whose `RecommendedCache` on macOS is `FileIdMap` (cache.rs:64-65). That cache's `add_path` is a `WalkDir` with `follow_links(true)` and a `stat` per entry (file_id_map.rs:39-53), and it runs over the whole root at `watch()` (lib.rs:589), over the path on every Create event and every event on an uncached path (lib.rs:298-300, 335-337), and over ALL roots on any FSEvents MustScanSubDirs, user-dropped or kernel-dropped flag (notify 8.2.0 fsevent.rs:116-121, then lib.rs:282-283 `cache.rescan`). It runs inside the watcher callback under the debouncer lock (lib.rs:692-700), on the `notify-rs fsevents loop` thread. The ignore rules that bound the index's corpus apply to events after the fact; the cache walk does not read them, so on a tree with build directories it stats every entry under `native/rust/target/` and every worktree's target dir, and a burst of writes (a cargo build, a store mid-index) is exactly what makes FSEvents drop and flag a rescan.

Measured on the live daemon (pid 58837, e70c3528a) at 2026-09-13 15:10:52Z: the second `notify-rs fsevents loop` thread had 0:29.43 user and 0:03.16 system of CPU (`ps -M` row 23, mapped to the thread by a `sample` taken while the daemon was cool); the first had 0:00.52 and 0:00.07. That is the walk's cost so far on this tree, bounded and separate from issue 0354's spin (which is the store thread, 24 minutes at the same moment). cc's reading (reading-only, 2026-09-13) attributes the cost; the per-rescan cost has not been measured on its own, and `follow_links(true)` on a tree that carries symlinks has not been driven.

Remedy candidates, none ruled: a debouncer without the `FileIdMap` cache (`NoCache`; renames then arrive as Remove plus Create, which an index reconcile keyed on content may not need), a cache whose walk honours the corpus's ignore rules, or a registration that does not cover build directories. Per-directory registration was rejected on 2026-09-12 (decision 9). Fix shape to be ruled with cc; not the cut's hold.



## Measured again on the daemon that carries the 0354 fix (2026-09-13)

With every index refresh cheap (3110125b2, the delivered pair at 363b18db1, pid 87652), the thread that still burns during an event flood is the index registration's `notify-rs debouncer loop`: 1:53 of user CPU in the daemon's first seven minutes, which held three board commits, ic's 66-file landing and the view rewrites their ingests caused. A hot `sample` (`intentd-87652-hot-sample.txt` in vc's scratchpad) puts its working samples in `notify_debouncer_full`'s `handle_event` walking directories through `opendir`, the `FileIdMap` cache re-walking what a directory event names, `target/` and `.git/` included. The store thread is idle between refreshes and the daemon idles at a few hundred syscalls per four seconds, so this is a cost proportional to the flood and not a spin; it is the whole of what the daemon spends when the tree is busy.


## Measured on the promoted pair that carries the cut (2026-09-14)

The delivered pair at e3c67792c (842409d2b: `NoCache` on the index registration, `Scanned::reaches` gating what a directory or vanished path hands over), daemon pid 79593, the same quiet-gated probe, two runs, the second on a settled daemon. Read as syscalls and CPU seconds the daemon spent in the six seconds after the event, against a quiet baseline of about 390 syscalls per four seconds.

- A directory created under `native/rust/target`: about 8 thousand syscalls and 0.04 s (was 43 thousand and 0.10 s on 363b18db1, and millions on 3.0.1). Its removal the same. The cut did what it was built to do.
- A file created under `.git`: 16 thousand and 0.07 s on the clean arm; the other two `.git` arms each caught a peer's commit landing in their window, so they read nothing.
- A `touch` of one in-scope source file (`intentd/src/watch.rs`, metadata only, no content change): 250 thousand syscalls and 1.23 s in the second run, 209 thousand and 1.09 s in the first. On 363b18db1 this arm was not dispatched at all, so the cached debouncer had been dropping the metadata-only event and `NoCache` passes it through. What it then costs is the leaf refresh itself: `index_refresh(Some([path]))` reconciles through `rows_under`, which enumerates the whole repository and filters, so any batch that names an in-scope file costs one walk of the corpus. That is the remaining cost, the same one a real edit has always paid, now paid for a touch as well.

What is left of 0355 is therefore the leaf refresh, not the watcher: a refresh for one named path should survey that path, not the repository. Small; for hv's ruling whether it goes before the tag.

---

_Generated by Intent v3.0.1 from `the issue canon`. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
