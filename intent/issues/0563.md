---
issue: 0563
title: the session gate depends on jq, and without it blocks every prompt, /in-session included
status: OPEN
severity: medium
created: 2026-09-24
reporter: Matthew Sinclair
---

Found on 2026-09-24 by vc's subagent auditing `intent/docs/working-with-llms.md` for the 3.2.1 doc audit, driven on the pair at `4c687eaad`.

Seen: two hooks depend on `jq`, and only one says so.
- `require-in-session.sh` reads the prompt only when `jq` is on PATH (`:50-56`); without it the prompt reads as empty, so the UserPromptSubmit gate blocks `/in-session` itself -- the one prompt that releases it. Driven: `{"prompt":"/in-session"}` piped to the script with a PATH holding only bash and cat printed the block message and exited 2. The way out is touching the sentinel by hand.
- The pre-commit gate with no `jq` on PATH reads no languages and runs no language critic, and it SAYS SO on every commit (`pre-commit.sh:485-486`, printed at `:669-672`): "NO code critic ran in this commit. why: jq is not installed ..." -- issue 0242's fix. The first filing said it disarmed in silence; dc read the source and that was false.
Until the 3.2.1 doc audit, no doc named `jq` as a dependency; `docs/install.md` now does. macOS 15 and later ship `/usr/bin/jq`; earlier macOS and many Linux installs do not.

Expected: the session gate says `jq` is missing rather than blocking every prompt, `/in-session` included.

Disposition: vc, 2026-09-24 -- the docs half (`docs/install.md` names `jq`, driven by dc: rc 2 without it and rc 0 with it) is in the 3.2.1 audit; the session gate's behaviour without `jq` is filed for after the stabilisation line.

---

_Generated by Intent v3.2.1 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
