---
issue: 0478
title: post-tool-advisory.sh prints plain stdout at exit 0, which a PostToolUse hook's model may never see
status: OPEN
severity: low
created: 2026-09-19
reporter: Matthew Sinclair
---

`lib/templates/.claude/scripts/post-tool-advisory.sh:77` prints its critic findings as plain stdout at exit 0. The header of its sibling, `post-tool-symbol-context.sh` (lines 43-48), records that plain stdout from a PostToolUse hook never reaches the model. That was driven on 2026-09-16 with a headless session: a nonce printed at exit 0 fired the hook and the model saw nothing, while the same nonce as `hookSpecificOutput.additionalContext` was quoted back. Issue 0427 moved the sibling to JSON; the advisory hook was not moved.

If that holds for this hook too, the advisory has been silent to the model since it shipped. This is not yet driven for this hook: POSSIBLY CODE-WRONG. Found by vc during the 2026-09-19 doc sweep; cc filed it.

Fix: drive it first (edit a file with a critic finding in a headless session and see whether the model reports the advisory). If it is silent, emit the findings as `additionalContext` the way the sibling does. S.

---

_Generated by Intent v3.1.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
