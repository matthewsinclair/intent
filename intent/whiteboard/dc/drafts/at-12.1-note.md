# AT-12.1 -- rewritten note (drafted 2026-08-27, WAITING ON A DOOR)

Ruled by vc 2026-08-27: the row's note describes an INSTRUMENT ("greps the repo for
shell-intent references post-prune") and the instrument named cannot see class (2)'s
second edge. A row's note must name WHAT IS TRUE WHEN IT IS GREEN, not the command
that checks it.

**NOT YET LANDED, AND NOT BECAUSE NOBODY GOT ROUND TO IT.** `at new` is the only door
onto an AT row and it has no `--note` flag; `facade.rs:4257` hardcodes `note: None`
and full-replaces the row. There is no invocation that writes this. See the board.

File citation is unchanged: `tests/cutover_guard.bash`. It was the note that was the
ceiling, not the path.

## The note

no file surviving the prune depends on `bin/` -- neither by SOURCING
`bin/intent_helpers` nor by CALLING a symbol only `bin/` defines. The second edge is
invisible to any sweep spelled `source`, which is why this row names a PROPERTY and
not an instrument: an instrument named in a row becomes the row's ceiling, because
nobody re-derives the claim once there is a tool to run. **Driven in a fixture tree
with `bin/` ABSENT, both arms, because neither arm alone decides it.** SURVIVING ARM:
every surviving entry point runs to a correct answer with EMPTY STDERR. CONTROL ARM:
the pre-port form of each, in the same tree, must produce NON-EMPTY STDERR.
**THE CONTROL ARM'S SIGNAL IS STDERR -- NEVER `rc`, NEVER STDOUT -- AND ASSERTING
OTHERWISE YIELDS A PERMANENTLY-GREEN TEST.** Measured on `intent_claude_cwi`
2026-08-27: with `bin/` absent the pre-port file returns **rc=0 with stdout
BYTE-IDENTICAL to the ported file**, differing only on stderr, so under `2>/dev/null`
the broken and working forms are indistinguishable. The missing symbol is `error()`
ITSELF, so the No-Silent-Errors path is the one that goes silent and execution
continues past every guard it was protecting. **BOTH ARMS ARE REQUIRED** because the
surviving arm alone passes for a file that never had the dependency, so it cannot
tell PORTED from NEVER-COUPLED and offers no evidence the fixture exercises anything.
**THE SURVIVING POPULATION IS DERIVED FROM THE TREE AT TEST TIME, NEVER HAND-LISTED**,
or the row acquires a roster problem of its own. **AND THIS IS WHAT MAKES THE PRUNE'S
ORDERING CHECKABLE (vc's ruling, 2026-08-27):** `claude_plugin_helpers.sh` and its two
live sourcers are pruned in ONE commit, because the `ext_root_dir` dangle dies with
the prune if the lib leaves with them and goes LIVE if it outlives them -- and this
test cannot pass in either intermediate state, which converts a sequencing property
into a verifiable one.
