---
node: dc
name: DevX Claude
role: worker
session_id: 3f2f5de2-d774-44db-8f1f-c85588606969
heartbeat_at: 2026-08-30 20:53Z
status: active
focus: "FOUR LANDED TODAY: 1e19bdbb payload reshape, 73338d84 mirror guard + AC-01.6 withdrawn (cleared an estate-wide commit block), bbd7e1c0 AT-01.8 red->fiat + bats deleted + the contract.rs defect it found, 8a7ed88d release port -- 3 of 4 v2 edges gone, INTENT_BIN resolved by PATH-FREE path, refuses rather than falls back, both new arms positive-controlled on 3 mutants. ONLY THE IRREVERSIBLE HALF REMAINS AND IT IS WITH hv: delete 26 files/12415 lines, carry error+warning+stamp_project_version (deliberately NOT done yet -- would make the second copy release_sidecars.bats forbids; after the prune there is only one), and retire the :114/:116 arm WITH its subject rather than repairing it."
claims: [ST0056/04, ST0056/05, ST0056/07, ST0056/11, ST0056/12, ST0058, ST0066]
---

# DevX Claude (dc)

**COLD-SESSION MINIMUM. A rule is never dropped here, only its narrative.** Full reasoning for 29-30 Aug is in `.history/20260830/wip-fold-0837Z.md` and `.history/20260829/`.

## D42 -- TIME. Read before writing anything, anywhere.

- **You never ask what time it is** -- not the OS, not `date`, not the filesystem, **not the database.** The stamp is applied BY the write.
- **THE RULE IS ABOUT SIGNATURES: NO cli or intentsvcs function TAKES a time.** They may RETURN times.
- **A board stamp is a label, not data** -- read from `date -u` and PASTE, **per stamp, never per session.**
- **`stat`, `git log`, `ls -la` ALL PRINT LOCAL.** Appending `Z` to a local read is an ASSERTION, not a format.
- **A ULID is an IDENTITY and the `ts` is the STAMP** -- which is why `Envelope::minted` may generate the id in Rust without breaching D42, and why a cascade can carry its ancestor's event id without a second transaction.

## The truth model and the environment

- `design.md` (D01 reversed) + `data-model.md`. **The db is the durable SSOT; the typed API is the only door in.** Crates: `intent-cli`, `intentd`, `intentsvcs`. **`intentdb` IS RETIRED.**
- **`intent` ON PATH IS v3 AND RESOLVES INTO THIS TREE** -- `~/.local/bin/intent` and `~/bin/intent` both symlink `native/rust/target/release/intent`.
- **THE STORE IS PROJECT-RELATIVE**, `intent_dir()/.cache/intent.db`, found by walking up from CWD. **SCHEMA 16.**
- **THE INDEX IS SHARED.** `add` + `commit --only <paths>` is the only safe write.
- **CANON ORDER: SYNC FIRST, then commit the file and canon together.** A later sync fixes the NEXT commit and can never fix this one.
- **THE GATE FIGURE IS COMPUTED BY THREE VERB CALLS AND NEVER TRANSCRIBED.**

## DOING

### THE `claude` PAYLOAD RESHAPE IS BUILT AND GREEN AND NOT COMMITTED. THIS SECTION IS THE RECOVERY RECORD.

**hv RATIFIED THE SHAPE:** `claude` is an Intent PLUGIN and `skills/` + `subagents/` are payloads it ships, so keeping an operator's Claude Code config current is ONE concern. **No sacred cows in v3 if something is simpler and better.**

**ON DISK, UNCOMMITTED, ALL GREEN.** `intentsvcs/src/skills.rs` -> `payload.rs` parameterised by `Kind` (`Skills` | `Agents`); `lib.rs`; `userstate::payload_target/payload_manifest(kind)`; `render.rs` helpers `payload_lib(kind)` / `payload_fail`; `tests/skills_sync.rs`; NEW `tests/subagents_payload.rs`; both `plugin.json` files; `tests/command_rosters_are_derived_or_declared.rs`; `tests/plugin_surface.rs`.

**THE TWO CLAIMS THAT MATTER, CHECKED RATHER THAN ASSERTED:** `skills_sync.rs` is **25/25 across the change**, and `claude skills list` from my build **diffs byte-identically** against the shipped binary. The kind that already worked was not disturbed to make room.

**BLOCKED ONLY ON TWO SHARED FILES.** `render.rs` (ic's TUI hunks at ~3054-3233, mine at 6537+) and `userstate.rs` (cc's daemon-token hunk at 205, mine at 331+). `--only` is path-scoped, so committing takes their work. Both asked. **The change does not compile without both**, so a partial commit reds main on a missing module.

### WHAT THE GENERALISATION COST, AND WHY IT IS ONE HOME

**FOUR METHODS.** Canon subdir, marker filename, target subdir, and tree-vs-file shape. Every type -- `Entry`, `Manifest`, `Outcome`, `Step`, `Report` -- already mentioned nothing about skills. **A second module would have been two homes for the manifest, the checksums and the lifecycle, and two homes agree on the day they are written.**

**THE CHECKSUM SCOPE IS THE DESIGN DECISION AND IT IS MUTATION-CONTROLLED.** A subagent's canon dir holds `metadata.json` beside `agent.md` and only `agent.md` installs, so hashing the directory reports updates that change nothing a consumer can see. **Rule for both kinds: hash what the payload INSTALLS.** Planted `tree_checksum` for the single-file shape; exactly the one guarding arm reddened, alone.

**`uninstall` REMOVES ONLY WHAT THE MANIFEST RECORDED, and the proving case is on every real machine:** this one's `~/.claude/agents/` holds eight agents v2 installed in April and no v3 manifest, so matching on name would delete files this build never wrote.

### `plugin.json` STOPS CARRYING COMMANDS, AND THE GUARD FOR IT COULD NOT SEE IT

**Measured by INVOKING all eight entries across both shipped manifests: three were false, each differently.** `claude prime` rc=2 unwired (I pruned its script that afternoon); `audit` rc=1 _unrecognized subcommand_, absent from the binary entirely; `agents` rc=2 at the family root while `agents sync` and `agents validate` answer 0.

**`command_rosters_are_derived_or_declared.rs` ALREADY ENFORCES `AC-09.4`'s _no hand-maintained command list exists_ AND SCANS RUST STRING LITERALS**, so a list in shipped JSON was never in its population -- not missed, unreachable. New arm added there, phrased to fail on REGROWTH rather than pass over an empty list, driven both ways.

## TODO

### FIRST ON THE BOUNCE: LAND THE PAYLOAD RESHAPE

Chase ic (`render.rs`) and cc (`userstate.rs`). **Re-verify before committing** -- peers have been committing every few minutes and a `git status` expires in minutes here (W2).

### hv's THREE RULINGS, ROUTED BY vc, ALL MINE

- **BATS DELETE AUTHORISED, OPTION (3).** Delete `intent_claude_upgrade.bats` AND explicitly retire `AT-01.8` + `AC-01.6` with the reason: the property expired with its subject -- _no BSD `sed -i ''` in the canon engine_ has no canon engine. **Examine `AT-01.6`/`AC-01.4` in the same pass** (same expiry, already landed). **ONE COMMIT WITH THE REASON IN IT, not on a board** -- twice today a criterion's reasoning went to somebody else's commit.
- **LAND THE MIRROR GUARD WITH IT.** Two-sided on a planted fixture; header stating it covers ABSENT-artefact and NOT present-and-hollow. **`AT-01.8` is its real subject, so it does not red nothing.**
- **THE `bin/` DELETE IS IN THE 3.0.1 CUT AND IS MINE.** `AC-00.6` reworded: population A is 26 files / 12,415 lines (`bin/intent` + 25 `bin/intent_*`); population B **must survive** at 57 files / ~18,300 lines (`bin/int`, `bin/devbin`, `bin/.devbin/`) -- **B is bigger than A and `bin/devbin build all` is the only door producing a coherent binary pair.** BLOCKED ON FOUR CALL SITES, all in `bin/.devbin/cmd/build.d/release`: `:67` sources `bin/intent_helpers`; `:373` `bin/intent doctor`; `:663` `bin/intent agents sync`; `:690` `bin/intent claude upgrade --apply --skip-settings`. **The script that ships v3 invokes v2 three times.** Port the four, then delete the 26, three-population standard first.
- **THE FOURTH CALL SITE IS RULED IN MY FAVOUR ALREADY.** v3's `claude upgrade` has no `--skip-settings`, so the port makes the release rewrite `.claude/settings.json`. vc ruled it a no-op **using the arm I landed at `a29f04ec`** -- settings.json needs no substitution and is byte-identical to its template. Port to `--apply` and keep that arm.

### THE REST OF THE PAYLOAD RESHAPE, AFTER IT LANDS

Unified `intent claude list|install|sync|uninstall --kind`; `claude upgrade --apply` drives the sync; drop `init` (v2's creates a project-local manifest the table itself records as a defect); **narrow `claude subagents` off the dispatch table** -- it is in `shipped` (127) and `probeable` (124) while the binary answers rc=2. **Surface edits are ic's axis and ic has not replied.**

### STANDING

- **`AC-04.6`'s `--kind` flag** -- approved as I stated it; `ac edit`'s help line moves with it. **`--kind` stays a real flag**: `ST0053/AT-02.2` is the only mixed-kind cover in the estate and proves the two kinds are different facts.
- **The `at` invariant's REMAINING TWO enforcement points are hv's pen, not vc's.** Machine 5 was ratified 2026-08-29 with `--` in every Guard cell; the face clause cannot land alone (it would make the contract stricter than the API that writes it) and the facade half cannot either (a predicate with no declaration is the shape `data-model.md` rejects).
- **Axis B (AT kind x covered CRITERION kind) is POST-TAG and is the NORMAL FORM, not a defect.** Recorded in `permitted_for`'s comment where the next census author arrives.
- **The delivered pair is still split** -- gate reports `currency REFUSING`. Rebuild is announce-and-hold, never unilateral.
- **The phase split, still owed.**

### With hv, not with me

- **ST0058's `AC-00.1`**; `AC-07.7` / `AC-11.1` / `AC-11.4` / `AC-12.4` tag-gated.

## Watch-outs

**FOLDED FROM 23 FAMILIES TO 8, because most of them were one shape seen from different sides. Rule only; instances are evidence, not the entry.**

- **W1 -- THE INSTRUMENT ANSWERED A DIFFERENT QUESTION THAN THE ONE ASKED, AND ITS OUTPUT LOOKED LIKE AN ANSWER.** The dominant class, six instances in two days. **A partial read is the sharpest form and it is ASYMMETRIC: one that finds SOMETHING self-corrects; one that finds NOTHING yields a conclusion nobody can falsify without redoing the read.** So **a NEGATIVE from a truncated read is not a result** -- `head -30` said this repo uses no trailers (37 of 40 do); `tail -30` said a suite was green and **let a red reach main**; `tail -14` said a guard did not print paths it prints twice. **THE PROCEDURE: output to a file, then `grep -c '^test .* FAILED$'` over the whole of it.** **AND A FILTER OVER A TOOL'S OWN DIAGNOSTIC IS THE SAME DEFECT WEARING A COMMIT.** I ran `git commit ... | grep -E '^(error|fatal|ok:)'`, which matched NONE of the gate's lines, saw an unchanged HEAD and a staged file, and diagnosed the shared-index race that had genuinely happened ten feet away minutes earlier. The real refusal was prettier -- _markdown is not formatted -- REFUSED_ -- printed in full, twice, filtered away both times. **A real nearby instance of a class is the most expensive thing to have in mind while reading a filtered log**, and I nearly committed the fabricated mechanism as the record. **`--help` IS THE SAME SHAPE AS A WIREDNESS PROBE**: it renders for a DECLARED command whether or not an arm exists, so rc=0 answers a question about the table. Invoke the verb. A count over a complete file can be zero honestly; a tail cannot. Sibling forms: a check that cannot tell WRONG from COULD-NOT-MEASURE names innocent files and prints a remedy that runs clean (the rustfmt `mod common;` probe); one sentence covering two conditions picks neither fix (absent-vs-malformed table, UNMEASURED-vs-DISAGREE); an instrument that cannot vary with its subject (positive-control before believing any zero); `cargo check` does not compile test targets without `--all-targets`.
- **W2 -- THE MEASUREMENT'S SUBJECT WAS NOT THE ONE I NAMED.** **A sha and `git status` answer different questions and only one is in the label** (cc): "the clean tree at `6b32b71d`" ran against a peer's dirty worktree, and the test read the table from disk. A suite run in a shared tree measures the TREE, not HEAD -- **and so does the delivered binary.** `HOME` isolates USER state and the store is PROJECT-relative, so a spawned binary with no `current_dir` finds the real project. **A `git status` expires in minutes here**; re-run it in the same turn as the sentence resting on it.
- **W3 -- OWNERSHIP IS MEASURED, NEVER INFERRED, AND IT HAS TWO TELLS.** _Absent at HEAD, therefore someone else's_ (`fn fc` was mine; three nodes converged on the same wrong owner). _The subject is theirs, therefore the edit is theirs_ (the `values: null` was ic's, not cc's whose daemon rows they were). **`git log -S<symbol> -- <path>` and `git log -1 -- <path>` close both.** Neither leaves a mark when wrong: the peer accepts work they did not do, or spends time in a file they never touched.
- **W4 -- THE SHARED CHECKOUT PUNISHES EVERY ASSUMPTION ABOUT WHO ELSE IS WRITING.** Never `cp` a shared source aside to mutate; never format or bless a file carrying a peer's in-flight change. **A multi-step refactor is uncompilable between its steps and nothing says "mid-move"** (cc): a rename plus its use sites is ONE edit. **A staged intermediate of yours blocks every peer's commit**, because the gates read the index. A build window is silently also a commit freeze. **AND `--only` DOES NOT COVER THE TWO WAYS IT ACTUALLY WENT WRONG TODAY.** (a) **`git commit --amend` has NO PATH SCOPE AT ALL** -- vc reached for it to fix a message and it took my staged `skills.rs -> payload.rs` rename; caught on the stat line, `reset --soft`, re-committed with `--only`, nothing lost. The mitigation guards the ordinary commit and the message-fix reflex bypasses it. (b) **Holding your hunk UNSTAGED does not protect it**: cc's `6aa00235` took my `doctor.rs` check because a plain commit needs the file DIRTY, not staged. Four instances today. **The only thing that actually worked was telling the peer before committing a file we were both in.**
- **W5 -- A RESTRICTION THAT REDS NOTHING HAS NOT BEEN ADDED.** D7 went into a roster `put` does not read; 29/29 stayed green and the refusal moved nothing. **When you add a guard, something must go red** -- if nothing does, find the door before believing it. Corollary from the other side: **a rule that catches a failure mode it was NOT designed for is the strongest evidence for it** (arm 8 refused a binary that panics on startup, knowing nothing about dispatch tables). **AND THE PASS/FAIL CHANNEL CARRIES THIS TOO: A GUARD THAT SKIPS PRINTS `ok`.** `devbin_fmt_md`'s premise arm was RED four days ago and now SKIPS -- _no root fence currently holds content prettier would reformat_. Nothing changed about the guard; its POPULATION went empty. **An `ok` count is not an armed count, and a suite reporting totals without a skip census cannot distinguish a guard that held from one that could not fire.**
- **W6 -- A SECOND HOME FOR A FACT DRIFTS, AND THE COPY THAT DRIFTS IS THE REPEATED ONE.** Never transcribe an instrument's number -- drive the verb. A count in prose beside a checker that computes it went stale (FIVE state fields when there were six; "the four ratified machines" while a fifth was ratified underneath). **An anchor that resolves to the record's own RENDERING is circular and worse than a dangle** -- a dangle is visible, a circle certifies itself. **And two files can hold different READINGS of one value with nothing seeing it** until something uses the second (`from: &[]` meant _any state_ to one file and _never filled in_ to another).
- **W7 -- MECHANICS THAT RETURN A PLAUSIBLE WRONG ANSWER.** **AN INSTRUMENT'S OWN TIMING NUMBER CAN EXCLUDE THE THING UNDER INVESTIGATION** (cc): `cargo` prints `Blocking waiting for file lock on build directory` and THEN `Finished in Xs` -- **the wait precedes the figure and is not counted in it**, and the line scrolls past in the first moment, so anyone reading the tail sees a fast build. **Time from OUTSIDE the process.** **Never take `$?` through a pipe** -- it belongs to the last stage; `cargo fmt --check | head` prints a diff and reports rc=0 (four instances, three nodes). `cargo test` aborts after a failing target: `--no-fail-fast`, or two runs report different populations in the same units. **`cargo test` is not read-only: it migrates the shared store**, and the severity is BLAST RADIUS, not data -- one `cargo build --release` fixes every node. A detached worktree gets its OWN `CARGO_TARGET_DIR` **and that directory dies with the worktree**: sharing bakes a stale `CARGO_MANIFEST_DIR` into cached rlibs, and keeping one grew an abandoned worktree to 10G.
- **W10 -- I DOCUMENTED THE CLASS AND FIXED THE INSTANCE, AND THE COMMENT MADE IT LOOK CLOSED.** Twice in one block of `.githooks/pre-commit`, both found by ic, both signed by me the same morning. My comment said _beside the original, module resolution is the file's own_ -- **true of the DIRECTORY, false of the NAME**, because rustfmt resolves from the file's STEM; I fixed sibling resolution and left every module root with a subdirectory broken, and `lib.rs` passed by layout coincidence. Two paragraphs above, the same block names _a gate that cannot say I could not check will eventually say something false_ -- and its `rs_err` printed the LAST iteration's stderr. **A class-shaped comment over an instance-shaped fix is worse than no comment: the next reader, including me, reads the class as handled and does not look.** The tell is a fix whose test is the instance that prompted it. **Ask what ELSE is in the class and drive one of those instead.**
- **W9 -- A REMEDY WHOSE STATED REASON DOES NOT HOLD IN YOUR CASE IS NOT PERMISSION** (vc's naming, on my find). `doctor` said do not reach for `wp done` **because it is refused on a blocked gate** -- and my gate was not blocked, so `wp done` would have run. **The reason evaporating does not convert the advice into a licence**; it means the remedy was written for a different situation and has nothing to say about yours. Same shape as W1 from the other end: there an instrument answers a question you did not ask, here a remedy answers a situation you are not in, and both read as though aimed at you.
- **W11 -- I ARGUED FROM HOW THE SYSTEM BEING REPLACED WAS BUILT** (hv's correction, 2026-08-30). I costed `claude subagents` by measuring v2's decomposition -- a 582-line shared core, ten callbacks -- and concluded that copying `skills.rs` would leave v3 with two homes where v2 has one. **hv: it is not a PORT, it is FUNCTIONAL PARITY, and those are not the same thing.** Parity is a claim about what the tool DOES for an operator; v2's internals are not a constraint on v3. **The tell is that the argument could not be stated without naming v2** -- the legitimate version of the same concern ("would this be two homes IN v3?") never mentions it. **And the reframe moved the TARGET, not just the reasoning:** I was scoping seven verbs because v2 had seven, when the requirement was that an operator can get payload onto a machine that has none. Sibling: **vc's**, on their own withdrawn census -- **a census keys on SHAPE and a defect is about MEANING, and the bridge is asking what a row's ABSENCE would lose.**
- **W8 -- HOW TO REPORT.** Write reasoning at the resolution you MEASURED it, never at the one it suggests. **Verify a peer's correction yourself, including when it makes your own claim false.** A peer's quote of a ruling is still the peer. **Retract a finding the moment its basis fails, even one you were thanked for** -- and say which claims rested on what, because a negative and a sighting do not feel different at the time.

## Decisions

**FOLDED 2026-08-30 16:23Z, pre-fold at `.history/20260830/wip-fold-1623Z.md`. FOUR OF TODAY'S ENTRIES WERE TWO CLASSES: one absorbed into D-POP, one into W7, and two became D-EXPIRED. THE hv PROVENANCE ENTRIES ARE VERBATIM AND ARE NEVER COMPRESSED.**

- **D-DELETE -- A DELETE HAS THREE POPULATIONS AND I HAD ONE** (ruled with vc, 2026-08-30, after my prune cost three repairs in a day). **(1) WHAT EXECUTES THIS** -- a failure baseline; catches what goes RED. **(2) WHAT CITES THIS AS EVIDENCE -- AND THE POPULATION IS THE STORE, NOT THE TREE.** `AT-01.8` is a green row on a completed thread citing `intent_claude_upgrade.bats`; a grep over `tests/` is (1) with a wider net and would never have found it. **(3) WHAT CHECKS THIS -- A DELETE CAN HOLLOW A SURVIVOR**, and nothing else catches it: `intent_upgrade_orchestrator.bats:151` is PRESENT and passing, and one clause inside it stopped being able to fail. **Three faces of (3) so far**: a survivor's clause goes vacuous (AT-01.6); a survivor's ROW ABOUT the deleted thing is orphaned (the parity register's `5/10 partial burn` becomes a measurement of nothing); and the only thing that made a survivor able to fail is removed. **THE GENERAL FORM: blast radius measured as WHAT WENT RED misses everything that went VACUOUS, and vacuous leaves no corpse.** It earned its keep twice on the day it was written, once by overturning the ruling that produced it -- `no_absolute_home_paths.bats` turned out to carry five real conformance rows, and the three I migrated were exactly the three its own register already classed out-of-scope. **And the estate holds a 272-row invariant nothing guards: 336 cited AT rows, 52 absent, every one `to-write`, zero green.** `stale_at_check.sh` guards the opposite direction and `declared-kind` claims nothing about absent rows; the mirror is the half nobody built.
- **D-EXPIRED -- THE CLAIM WAS TRUE WHEN IT WAS MADE AND FALSE WHEN IT WAS USED, AND NOTHING BETWEEN THE TWO MOMENTS SAYS SO.** Distinct from D-POP: there the instrument cannot SEE the case; here it saw correctly and the world moved. **Four instances in one afternoon, three of them mine.** (1) **A measurement's timestamp is part of the measurement.** I read `help`'s `spelling_note` at ~14:50Z, ic edited it at 14:53Z, and I reported the old value at 15:02Z saying _I re-measured rather than assuming_ -- **that sentence was false, and ic offered a kinder account (two `spelling_note` fields, wrong level) whose acceptance would have put a FALSE entry in a class that is real.** (2) **A peer's "quiet" is a claim about THEIR processes, not about the box.** cc's _quiet from my side_ was true and was not the question; checking the box found load 19.56 with ZERO rustc and two cores pegged by a third node. (3) **A quiet-box check is a POINT-IN-TIME READ and the measurement it licenses OUTLIVES IT** -- I verified clean, started a five-minute split, and within 40 seconds all three nodes were building; **load 23.38, the worst of the day, produced by three nodes who had each just declared care.** (4) cc's own: _I am writing code_ and _I am not building_ were never compatible and were said in one sentence. **THE CURE IS NOT MORE CARE -- IT IS REMOVING THE NEED FOR THE PREMISE.** Two nodes isolated their build trees rather than coordinating harder, because **a remedy that removes the need for cooperation beats one that requires more of it**, and we have the failed experiment rather than the prediction.

- **ic's WORDING FOR WHY THE INNOCENT EXPLANATION WINS, AND IT IS THE BEST STATEMENT ANYONE PRODUCED TODAY: _a correct general model is what made me stop measuring._** ic told their operator four times, as fact, that they were blocked on the shared build lock, having never run `ps` -- which would have shown zero rustc. **Not laziness: a true and relevant model is genuinely good evidence, right up until it is the reason you stop looking.**

- **TWO hv RULINGS, 2026-08-30, ARRIVING BETWEEN THE 14:59Z AND 15:02Z CLOCK READS OF THIS SESSION -- THE MINUTE IS NOT RECOVERABLE AND IS NOT INVENTED, because a mid-turn message carries no stamp and the two reads either side are the tightest true bound. FIRST-HAND IN THIS dc SESSION -- hv's own words in answer to two questions dc put in the plan. NOT RELAYED, NOT RECONSTRUCTED, AND THE WORDING BELOW IS hv's RATHER THAN A PARAPHRASE.**

  **(a) EVERYTHING GOES IN 3.0.1, SO THE `bin/` PRUNE IS IN.** hv, verbatim: _"Sure. Everything is in 3.0.1. That is what Intent v3 is supposed to be. Everything we can get into 3.0.1."_ **CONSEQUENCE FOR dc: `AC-12.1` IS LIVE WORK, NOT DEFERRED.** Asked because `AC-14.12`'s descope to ST0069 that morning had measured the WP-14-blocks-WP-12 coupling away, and three documents still called it the live blocker -- so the row had been unblocked for hours with nobody reading it. `AT-12.1` is `to-write`: the row is unsatisfied because no test exists, not because the work is unknown.

  **(b) `shipped_surface_drift` IS AN EXPIRED TEST AND THE v2 GATES ARE DEAD.** hv, verbatim: _"Expired test. We don't care about gates for v2 any more. Don't waste any time on it, other than to remove those as blockers."_ **THE FILE'S OWN HEADER ALREADY CARRIED THE CONDITION** -- _TRANSITIONAL GUARD -- DELETE THIS WHOLE FILE WHEN v2 IS END-OF-LIFE_ -- so the ruling did not decide the action, it declared the trigger. **AND IT RESOLVES AN ITEM SITTING WITH hv UNDER SOMEBODY ELSE'S NAME:** `vc/wip.md:38` lists _`shipped_surface_drift`'s declaration kind_ as one of six unruled questions on `hv/inbox.vc.md`. A ruling that answers a question asked by a different node does not reach that node by being made; vc was told in the same turn.

**FOLDED 2026-08-30 14:44Z, pre-fold at `.history/20260830/wip-fold-1444Z.md`. SIX ENTRIES BECAME ONE, because today's dominant class was being held in six places and six copies of a fact is what this board keeps finding wrong elsewhere. THE hv PROVENANCE ENTRIES ARE VERBATIM AND ARE NEVER COMPRESSED -- a ruling's own words are the thing that cannot be re-derived.**

- **D-POP -- THE INSTRUMENT'S POPULATION EXCLUDED ITS OWN SUBJECT. SIX INSTANCES, AND NOT ONE OF THEM IS THE EMPTY-SET VACUITY THIS ESTATE ALREADY GREPS FOR** -- in every case the set was non-empty, the vacuity guard was present, and it passed. **The general form: a population that is DECLARED is chosen by the same judgement that would have prevented the defect, so it can only hold the failures somebody already suspected.** (1) `twin_of` -- `AC-00.6`'s own instrument walks rows declaring a twin; that is ONE row, filtered from `shipped_entries`, and the row's live falsifier `--help`/`help` is outside it twice over. Nobody declared that twin for exactly the reason nobody saw the bug. **CURE: derive the population from the BUILT surface.** (2) `rulings_check.sh` -- `declared` is printed as the SUM of its own buckets, so the partition closes by construction and the fix it was assigned could red nothing, ever. **CURE: compare the parts to the INPUT, not to a total derived from them.** (3) the `_note` enumerator -- keying on the `_note` suffix found 8 key names of the **137** that carry prose, `note` alone appearing 66 times; `arg_values_note` states this exact rule and I made the error while classifying that note. **CURE: key on a STRUCTURED field whose members you can name, and say what you do not reach.** (4) `critic-guard.sh` -- an exclusion recorded only in prose is one no instrument can tell from an oversight, **and the check must INVERT rather than lapse** or the declaration outlives the decision. (5) `devbin_fmt_md` -- named ONE member of a population, the member moved, and the guard reported the property failing. (6) `0159` -- three instruments green for three different reasons, none of them reading the durable value; **with the stamp suppressed, five of six arms still passed.** **THE TELL ACROSS ALL SIX: ask what the instrument CANNOT see before asking whether it is green, and positive-control the instrument before believing any zero.** **(7) THE SEVENTH IS THE CHEAPEST AND IT PREVENTS THE INSTRUMENT RATHER THAN CORRECTING IT: A PRECISION FIGURE DECIDES WHETHER A CHECKER SHOULD EXIST, AND IT COSTS LESS THAN THE CHECKER.** cc offered a critic rule for unbounded loops in test code. **Measured the corpus first: 31 loops, the naive rule flags 17, it is right about 2.** Twelve terminate structurally (`stack.pop()` drains, `find()` advances, `j < bytes.len()` bounds) and three are responder threads the test itself stops. **A guard firing seventeen times to be right twice is not noisy -- it teaches its readers to skip it, after which the estate believes the class is covered.** **AND THE CRUDE FORM WOULD HAVE HAD ME ACCUSE cc's OWN FILE, landed an hour earlier, of breaking cc's own convention** -- correct grep, false accusation, stopped by reading the bodies and not by a better pattern. **THE DISCRIMINATOR IS NOT GREPPABLE AND ITS POSITIVE FORM IS THE KEEPER (cc's wording): make the progress STRUCTURAL rather than CONDITIONAL.** `for _ in 0..ATTEMPTS` terminates whatever the subject does; `while cond` INHERITS ITS TERMINATION FROM THE THING BEING TESTED, which is exactly backwards in a test. Prose, never a checker; the mechanical form is a per-test TIMEOUT, which fires only on actual non-termination and so has no false-positive rate at all.

- **D-POP's SEVENTH, EIGHTH AND NINTH, ALL 2026-08-30, AND THEY SHARE ONE PROPERTY THE EARLIER SIX ONLY HINTED AT: THE WRONG ANSWER WAS THE REASSURING ONE.** (a) **`grep -q` downstream of `grep -v` under `set -o pipefail`**: the `grep -q` exits on first match, SIGPIPEs the producer, the pipeline returns 141 and the predicate answers **no**. Correct for every short file, wrong for the two LONGEST in the population -- **a census whose failure probability rises with file size and whose failure direction is "nothing found".** (b) **A BLIND VERIFICATION, which is worse than a blind search**: `intent st list --limit banana` returns rc=1 whether or not the defect was fixed, because `st list` has no `--limit` and clap refuses an unknown flag layers before the parse path. **A blind search wastes a measurement; a blind verification MANUFACTURES CONFIDENCE, and it does so at the exact step you took to stop being wrong.** The cure is a control that proves the probe reaches the code. (c) **A SITE-BASED CENSUS UNDERCOUNTS CAUSES WHEREVER ONE `map_err` MAPS AN ERROR ENUM** -- and those sites carry the most causes. Right about sites, therefore wrong about causes at the one place it mattered (ic's correction).
- **D-PROXY -- YOU CHECKED A RENDERING OF THE THING INSTEAD OF THE THING, AND BOTH DIRECTIONS HAPPENED IN ONE HOUR.** I grepped AC-12.1 for dated amendment BANNERS and concluded vc's reword had not landed; it had, and its banner simply opened with a word my alternation did not carry. **I held a prune for an hour on a proxy.** vc, from the other end, hand-edited `WP/08/info.md`, watched it hold, and lost it minutes later because the store is SSOT and that file is a generated view. **Same shape, opposite directions: one read a rendering as the artefact, one wrote a rendering as the artefact.** Distinct from D-POP -- there the instrument cannot see the case; here it saw a faithful rendering that was not the subject. **Cure: name the artefact of record and read THAT** -- for canon, the extract or the verb, never the banner list and never the view.
- **D-SHARED -- THE SHARED TREE BREAKS EVERY COMPARISON THAT LOOKS SELF-CONTAINED, AND IT COST ME THREE TIMES TODAY.** **A refused commit PARKS your paths in the shared index** (`0157`, first-hand twice): the second time the index was then locked by a peer mid-commit with their canon staged beside my files, and **a plain `git commit` from them takes the index AS IT STANDS.** That is what I did to cc at `c75a5b1a`, sweeping 313 lines of their in-flight `render.rs` under my subject line -- **with `commit --only <explicit paths>` written four lines from the top of this board. Knowing the rule is not running it.** And the damage that matters is not the wrong subject line: **a sweep takes what is TRACKED, tracked correlates with FINISHED, so it takes the half that cannot fail on its own and leaves behind the half that would have caught it.** Second limb: **a baseline must run in the SAME PASS as its mutations.** Twice a positive-control run came back all-RED on a peer's mid-refactor build error, and both times the baseline row was the only thing that stopped three broken builds being recorded as three passing mutations. Third limb: **a mutation that does not fire is not a passing guard** -- I planted a skip on an id no row carries, got a green, and had to re-drive it.

- **A PERMISSIVE READER MAKES A CONTRACT VIOLATION AND A CONFORMING IMPLEMENTATION INDISTINGUISHABLE** (mine, on vc's `read -ra` break; vc adopted it). The callback contract said SPACE-separated; `skills` honoured it and `subagents` returned newline-separated `jq` output and always had. The old split used the default IFS, which includes newline, **so it absorbed the violation for as long as it existed.** **A STRICTER READER DOES NOT INTRODUCE THE DEFECT, IT INHERITS ONE AND SURFACES IT UNDER ITS OWN NAME** -- which is the shape that makes tightening a reader feel dangerous when it is the opposite. **The fix is to correct the CONTRACT to what both implementations emit, not to make one conform**: a form truncating at the first newline leaves the next author one `echo` away from the same silent loss.
- **A VERB CAN BE SEMANTICALLY CORRECT AND STILL BE A SILENT FAILURE, BECAUSE CORRECTNESS IS ABOUT WHAT IT DID AND SILENCE IS ABOUT WHAT IT SAID** (vc's naming, on my `at fc` find; `0158`). I nearly talked myself out of the finding because reading (a) -- that `at fc` is a RECORDING surface and propagating would be laundering with extra steps -- is defensible and turned out to be right. **Reading (a) being right and the verb being broken were never in tension.**

- **THE STORE-MIGRATION GUARD LANDS AFTER THE CUT. PROVENANCE: hv, 2026-08-30 11:51Z, FIRST-HAND IN THIS dc SESSION, hv's own selection from four options dc authored.** Menu: **(A) extend the guard after the cut -- CHOSEN**; (B) extend it now, declined -- an estate-wide test refactor inside a cut vc had just reported as too wide, plus a commit freeze on nine files across three lanes; (C) fix the two proven members now and defer the guard, declined -- **instance-shaped fix under a class-shaped problem, which is W10 done knowingly**; (D) file it and do nothing, declined. **THE GROUND FOR WAITING IS THAT IT IS A DEV-TREE HAZARD WITH NO SHIPPED CONSEQUENCE**, not that it is small.

- **THE CUT GOES AHEAD AGAINST AN OPEN GATE. PROVENANCE: hv, 2026-08-30 11:41Z, FIRST-HAND IN THIS dc SESSION, hv's own selection from four options dc authored -- not relayed, not reconstructed.** **The menu is recorded in full because the choice alone is unrecoverable:** (A) **cut against an open gate -- CHOSEN**, tag 3.0.1 with ST0056 BLOCKED and the tag-gated rows named as such, satisfy them against the published artefact afterwards; (B) re-scope the five to a follow-on thread, declined -- it buys a green by changing the question, and `AC-00.4`'s own lesson is that a criterion moved out of the instrument is not thereby satisfied; (C) two-phase RC-then-real tag, declined -- unless the code is byte-identical the second artefact is not the one the criteria were driven against, so the green may not describe what shipped; (D) fiat-close the five, declined -- **they are not over-cooked, merely not-yet-possible, which is the case `fc` was explicitly not built for.** **THE MEASUREMENT THAT PROMPTED IT, driven this turn and not recalled: five ST0056 criteria require the artefact only the cut produces** -- `AC-00.5` (brew install on a clean machine), `AC-07.7` (every claude door reachable in an INSTALLED build), `AC-11.1` (tagged version yields a working tap formula), `AC-11.4` (published checksum matches published bytes), `AC-12.4` (tagged, released, formula live) -- plus `AC-14.12`, already ruled un-closable before the cut. **So the gate could never read green before the release existed, and `get ST0056 green, then cut` was not achievable in that order.** **CONSEQUENCE FOR dc, AND IT CONVERTS A BLOCKER INTO A RULED OUTCOME: WP-07 and WP-11 not closing before the cut is now the DECIDED shape, not something waiting on hv's hand.** `AC-07.7` stays RED and is recorded as tag-gated rather than as work outstanding.

- **D-FACES -- A DECLARATION HAS CONSUMERS YOU DID NOT WRITE, AND THE COMPILER GUARDS ONLY THE ONES IT CAN SEE.** **A published `///` is a CONTRACT**: `JsonSchema`/`SimpleObject` lift doc comments verbatim into `thread.schema.json` and the SDL. Three false ones found -- `FiatRecord`'s _kept forever_ (disproved by `ac_reinstate`), `AcceptanceTest::fiat`'s _present exactly when_ (disproved by `at_set`), and a `///` of mine publishing `ST0056` into both faces. **One was fixed by correcting the prose, one by correcting the CODE, one by demoting it to `//` -- and which is which is the judgement.** Reasoning for a maintainer is `//`; a `///` says what a consumer needs. **The FACE COUNT is a reading, not a chore**: three faces moving means PERSISTED AND PUBLISHED, two means published only, one is a store table with no modelled type -- the pin comments say what the count MEANS, which is what turned a number into a reading. **And the compiler is the oracle for a field addition; the HAND-ROSTERS are where it is blind** -- proptest strategies, SDL variant counts, `fully_populated_row()`. **A generator that pins a new field to `None` is a field the round-trip laws never exercise**, which is how `AcState::Fiat` shipped uncovered. Generate it.
- **A RULING'S PREMISE CAN BE CORRECTLY MEASURED AND STILL NOT COVER WHAT YOU BUILD.** hv's DO-NOT-BUMP measured `tests.status` as unconstrained TEXT -- a claim about the VALUE. `fiat` was a separate FIELD, so it cost a column, a rung and a bump. **Surface the change rather than absorbing it.**
- **hv HAD ALREADY RULED THE THING I WAS INSTRUCTED TO BUILD, THE OTHER WAY.** Read hv's board BODY at pickup, never just its header. **And an OMISSION in an instruction can cost more than an error in it** -- _with one required composer_ was in the ruling's title and never reached me.
- **#fixforward IS A RULING ABOUT WHICH DIRECTION IS CHEAP.** A one-way ladder only sounds unrecoverable while you read it as data; the stale thing was a BUILD ARTEFACT.
- **A DECISION THAT REACHES A QUESTION IT WAS NOT ABOUT IS THE STRONGEST EVIDENCE FOR IT** (vc). Three instances in one night: arm 8 catching a startup panic; one-event-per-entity making skip-derivability answerable; D42's identity/stamp split making a one-transaction cascade possible.
