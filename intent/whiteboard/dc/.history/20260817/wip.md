# dc -- archived 2026-08-17 09:33Z

## The packaging hold, as it stood while it was open

## DOING

- **THE PACKAGING HOLD -- THIS IS THE WORK. MEASURED AT THIS FOLD, RAISED BY cc FROM THE IMPLEMENTATION SIDE.** `intent claude hook` does not reimplement the session hooks -- it **execs `lib/templates/.claude/scripts/<name>.sh` out of the install root** -- and `intent info` prints that root for the pre-commit gate to parse back. The root is found by walking up from the binary's symlink-resolved `current_exe()` to the directory holding `lib/templates/`, with **no environment fallback, deliberately** (a stale v2 `INTENT_HOME` would otherwise make a v3 binary exec v2's hook scripts silently). **The formula installs two files: `bin.install` for `intent` and `intentd`.** Reproduced exactly what it produces -- binary under `Cellar/intent/3.0.0/bin/`, brew-style symlink in front, neutral cwd: `intent info` -> **exit 0** with `INTENT_HOME: <not set>`; `intent claude hook require-in-session` -> **exit 1**. **BOTH FAIL QUIETLY**: exit 1 does not block a prompt (vc's ARM1), and `info` returns **0 while printing an error**, so my own 0042 status check sees success and only the empty parse catches it. **Net effect of publishing today: every consumer project silently loses its session hooks AND its whiteboard guards, and nothing returns a failing code.** In `install.md` as the second hard hold beside 0036.
  - **THE OPEN DECISION, MINE TO PREPARE WITH COSTS MEASURED RATHER THAN TO PREFER:** ship `lib/templates/` inside the release asset -- which turns a bare Mach-O into an archive and **changes signing, notarisation and the checksum step**, all four of which I built against a single binary -- or lay it down another way. **Do not start building before that is costed**: the whole `int macos` chain assumes one file per binary.
  - **cc's CONSTRAINT ON THE FIX, AND IT NARROWS THE CHOICE USEFULLY: the marker IS `lib/templates/`**, chosen because it is what BOTH consumers actually reach into -- `claude hook` execs `lib/templates/.claude/scripts/<name>.sh`, and the gate builds `lib/templates/hooks/<guard>.sh` out of `info`'s output -- rather than a proxy like `bin/`. **So staging that directory is sufficient and staging anything else is not.** If a different prefix layout suits the formula better, the marker is one constant (`intentsvcs::install::MARKER`) and cc would rather move it deliberately than have the walk find a partial tree. **They also hold `info`'s exit-0 defect as item 0 and are not touching it while the suite runs** -- and the distinction they are keeping is worth knowing, because it is the one that could undo 0042: _"never gate on PROJECT state" is not the same as "always exit 0"._ An unmigrated project is not a failure of `info`; an unresolvable INSTALL is.
  - **`intent info` EXITING 0 WHILE FAILING IS cc's, AND I HAVE TOLD THEM.** It is 0044's class in the direction nobody swept: that issue is `1` meaning five things, this is `0` meaning "I could not do what you asked". Its exit code is a shipped consumer contract.
- **AWAITING hv, BOTH UNCHANGED AND NEITHER ROUTABLE AROUND ME.** (1) **The upstream freeze and v3 are in structural conflict** -- the tap formula points at a GitHub asset, so there is no shipping v3 without pushing the frozen remote; sent as the narrow question, lift for the cut itself, since the freeze was called on cost per commit and a release is not a commit. (2) **No `## [3.0.0]` CHANGELOG section**, the first gate to fire; I refused to create a placeholder, because it would carry the dry-run further and look like progress while turning a loud correct refusal silent.
- **`brew services` waits on WP-08. hv holds the toolchain-pin decision.**
- **CLOSED TODAY, kept as one line because the detail is in `install.md` and the issues:** 0043 (prompt lockout) closed by cc's `c6aee944`, verified on a rebuilt binary, hold lifted at `61724664`; 0042 closed empirically -- the guards **REFUSE** a bad stamp under the real v3 binary; the release-lock test, the `--help` window, the caller enumeration (**eleven**, eight of them resolving from `PATH`), and five unchecked-status sites including **both release sidecar regenerators**.
