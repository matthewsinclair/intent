# dc -- archived 2026-08-17 09:33Z

## The packaging hold, as it stood while it was open

## DOING

- **THE PACKAGING HOLD -- THIS IS THE WORK. MEASURED AT THIS FOLD, RAISED BY cc FROM THE IMPLEMENTATION SIDE.** `intent claude hook` does not reimplement the session hooks -- it **execs `lib/templates/.claude/scripts/<name>.sh` out of the install root** -- and `intent info` prints that root for the pre-commit gate to parse back. The root is found by walking up from the binary's symlink-resolved `current_exe()` to the directory holding `lib/templates/`, with **no environment fallback, deliberately** (a stale v2 `INTENT_HOME` would otherwise make a v3 binary exec v2's hook scripts silently). **The formula installs two files: `bin.install` for `intent` and `intentd`.** Reproduced exactly what it produces -- binary under `Cellar/intent/3.0.0/bin/`, brew-style symlink in front, neutral cwd: `intent info` -> **exit 0** with `INTENT_HOME: <not set>`; `intent claude hook require-in-session` -> **exit 1**. **BOTH FAIL QUIETLY**: exit 1 does not block a prompt (vc's ARM1), and `info` returns **0 while printing an error**, so my own 0042 status check sees success and only the empty parse catches it. **Net effect of publishing today: every consumer project silently loses its session hooks AND its whiteboard guards, and nothing returns a failing code.** In `install.md` as the second hard hold beside 0036.
  - **THE OPEN DECISION, MINE TO PREPARE WITH COSTS MEASURED RATHER THAN TO PREFER:** ship `lib/templates/` inside the release asset -- which turns a bare Mach-O into an archive and **changes signing, notarisation and the checksum step**, all four of which I built against a single binary -- or lay it down another way. **Do not start building before that is costed**: the whole `int macos` chain assumes one file per binary.
  - **cc's CONSTRAINT ON THE FIX, AND IT NARROWS THE CHOICE USEFULLY: the marker IS `lib/templates/`**, chosen because it is what BOTH consumers actually reach into -- `claude hook` execs `lib/templates/.claude/scripts/<name>.sh`, and the gate builds `lib/templates/hooks/<guard>.sh` out of `info`'s output -- rather than a proxy like `bin/`. **So staging that directory is sufficient and staging anything else is not.** If a different prefix layout suits the formula better, the marker is one constant (`intentsvcs::install::MARKER`) and cc would rather move it deliberately than have the walk find a partial tree. **They also hold `info`'s exit-0 defect as item 0 and are not touching it while the suite runs** -- and the distinction they are keeping is worth knowing, because it is the one that could undo 0042: _"never gate on PROJECT state" is not the same as "always exit 0"._ An unmigrated project is not a failure of `info`; an unresolvable INSTALL is.
  - **`intent info` EXITING 0 WHILE FAILING IS cc's, AND I HAVE TOLD THEM.** It is 0044's class in the direction nobody swept: that issue is `1` meaning five things, this is `0` meaning "I could not do what you asked". Its exit code is a shipped consumer contract.
- **AWAITING hv, BOTH UNCHANGED AND NEITHER ROUTABLE AROUND ME.** (1) **The upstream freeze and v3 are in structural conflict** -- the tap formula points at a GitHub asset, so there is no shipping v3 without pushing the frozen remote; sent as the narrow question, lift for the cut itself, since the freeze was called on cost per commit and a release is not a commit. (2) **No `## [3.0.0]` CHANGELOG section**, the first gate to fire; I refused to create a placeholder, because it would carry the dry-run further and look like progress while turning a loud correct refusal silent.
- **`brew services` waits on WP-08. hv holds the toolchain-pin decision.**
- **CLOSED TODAY, kept as one line because the detail is in `install.md` and the issues:** 0043 (prompt lockout) closed by cc's `c6aee944`, verified on a rebuilt binary, hold lifted at `61724664`; 0042 closed empirically -- the guards **REFUSE** a bad stamp under the real v3 binary; the release-lock test, the `--help` window, the caller enumeration (**eleven**, eight of them resolving from `PATH`), and five unchecked-status sites including **both release sidecar regenerators**.

---

## Fold snapshot (2026-08-17 10:45Z) -- DOING + TODO at the second fold of the day

## DOING

- **THE BOUNCE IS DONE -- 0042's regression fixed at `502ed63a`, refined at `069a95c4`, and 0042 STAYS OPEN on item 2.** The branch gates on `[ ! -d "$INTENT_HOME_RESOLVED" ]`. It subsumes the old emptiness test, catches `<not set>` without naming it, and is the property the loop actually needs. The `sed` strips trailing as well as leading whitespace (vc's measured hardening -- the line is column-padded on the LEFT, so a renderer padding the right would hand `-d` a path with spaces and block a working resolver).
  - **`wb_info_rc` REPORTS AND DOES NOT GATE, AND THAT IS A DEPARTURE FROM WHAT vc AND I AGREED**, written into the issue with the cost of the alternative rather than resolved quietly in code. Gating on it makes every guard conditional on a code whose meanings are still being settled: vc's 0045 has `Facade::open` gating EVERY command with the migration refusal at 1. The day `info` inherits that, rc is non-zero in every unmigrated project -- ie every consumer, immediately before it upgrades -- while INTENT_HOME resolves perfectly, and the guards stop estate-wide. **An unreachable branch under-enforces once; a gate keyed to a moving code under-enforces everywhere.** vc re-verified at `502ed63a` and WITHDREW their half of the endorsement.
  - **vc's sharper version, which is why the departure is worth more than it looks: `intent info` returns 0 TODAY while printing a hard error and a remedy** -- 0044's failure-at-0 class. So rc-gating would have been correct this morning **because of an open defect**, and whoever fixes that defect would have silently disabled the guards. The departure converts a dependency on an open defect into an independence from it.
  - **The noise was priced, not discovered in the field (vc).** The rc line now prints only in the else branch -- the one place the code is genuinely anomalous, where the resolution worked and the guards are running and the tool still said it failed -- and it says outright that nothing is owed. The total branch already names the code in its own message; saying it twice is how a block starts being skimmed.
  - **The coupling set is FIVE files, not the one I first named nor the two I corrected to (vc, measured).** Three EXECUTE the shipped hook (`exit_codes.rs`, `critic_dispatch.bats`, `critic_config.bats`) beside `pre_commit_hook.bats`; one ASSERTS ON ITS TEXT (`whiteboard_header_guard.bats` greps `WB_BLOCKED=1` and the exit line, so it reds on FORMATTING of that block and nothing on the hook's side shows it exists). All five verified green. **A shell edit here can red a rust test, and that direction is obvious from neither end.**
- **ic's RUNNER REFUSAL LANDED (`55e8857b`) -- THIS IS THE FIX FOR THE FALSE GREEN THAT OPENED THIS SESSION.** A gate's return code and the `.errors` it seals are two independent claims about one run and nothing compared them. It was UNREPRESENTABLE rather than unchecked: `record_seal` returned early on rc=0, so a green gate never entered the ledger and the row a false green would have to contradict was never written.
  - **Two departures from ic's staged design, both recorded.** The tail is not `if failed / elif disagreed` -- both print, because `elif` hides a false green behind an unrelated real failure and the sets are disjoint by construction. And both ledger walks moved into `lib/runlog` beside `record_seal`: one tab format had two homes in two files, and adding the rc field is exactly the change that makes that a silent mis-parse. **That move is also what makes the refusal DRIVABLE -- a CORRECT gate cannot produce a disagreement, so with the reader inline it had no fixture and would have shipped having never run once.**
  - **A NON-EMPTY SEAL IS NOT A FAILURE (ic's refinement, and the part most likely to matter).** `open_run_log` SEEDS the file, so non-empty covers "sealed a failure" AND "never sealed at all" -- different messages, because reporting an unsealed run as a sealed failure sends the reader hunting for a test that does not exist. A killed or timed-out run produces the second, so it is the arm that will fire most.
  - **VENDORING CARRY OWED TO hv.** `lib/runlog` and `lib/resolve` are files devbin OWNS -- both drift from `bin/.devbin/manifest.sha256` now (measured with two unedited lib files as CONTROLS, which still match; my first attempt returned an empty expected-hash and would have called every file drifted). **The manifest is deliberately NOT restamped** -- rewriting it erases the only signal this project carries a local patch. The detector lives in the Devbin source repo, not in this command surface, so nothing here reports it.
- **`int fmt md` WAS MANGLING README.md, FOUND BY RUNNING ic's CONTROL A FOR REAL (`b4d7d1e6`).** prettier formats code INSIDE fenced blocks by language; README carries a ```markdown fence holding an illustrative dialogue, and three lines collapsed into one. **Three other files it rewrote were NOT damage** -- `--prose-wrap never` unwrapping prose in files never swept is the gate doing its declared job, and I stated the finding wider than the measurement before going back to the artefact. Fixed with a project `.prettierrc.json`, NOT in `lib/cmd/fmt`, which devbin also owns: **one local fork of vendored code is a carry, two is a habit.**
- **THE PACKAGING HOLD IS CLOSED -- `7a41ff2e`. Detail in `install.md`, which keeps it in full rather than deleting it; the open-state text is in `.history/20260817/`.** A release ships THREE artefacts now: the two binaries plus `intent-support.tar.gz`. **Signing, notarisation and `verify` never had to change** -- `notarize` already submitted a directory zip rather than a bare binary, so a non-Mach-O artefact never reaches them. That was the cost I was told to measure before choosing, and measuring it dissolved it.
  - **THE ARCHIVE IS ROOTED AT THE INSTALL ROOT, not at the templates directory, and that is the decision worth defending.** `intent critic` and `intent claude rules` are declared-and-unimplemented (both exit 2) and **will need the rule library, which lives OUTSIDE the marker** at `intent/plugins/claude/rules/`. Rooting the archive at the install root makes that a CONTENT change rather than a formula change or a fourth asset. **RULED BY cc (09:35Z) AND THE ANSWER IS NEITHER OPTION I OFFERED: the premise was wrong. `MARKER` is a SENTINEL for locating the root, never a statement about what an install CONTAINS** -- it appears in `is_install`, the not-found message and `hook_script`, and `home()` returns the ROOT precisely so the marker never has to be a boundary. So the rule library does not move, the marker does not widen, and **the formula does not change when `critic` lands**: it will resolve `intent/plugins/claude/rules/` from `install::home()` exactly as `hook_script` resolves its subpath today. **My archive-at-the-install-root choice stands, and cc named a second reason for it I had not: rooting at the MARKER would have BUILT the boundary the question was asking about, making the answer forced by packaging rather than chosen.** Owed on cc's side when `critic` lands, not mine: a `rules_dir(home)` beside `hook_script` so one module knows where shipped assets sit.
  - **`libexec`, not `prefix/lib`, on measurement rather than taste:** both resolve, but `lib` is a brew-LINKED directory -- 858 keg symlinks in the shared prefix -- so `prefix/lib/templates` publishes a directory called `templates` into a global namespace. `bats-core` ships the exact shape I landed on.
  - **STILL UNEXERCISED, unchanged by any of this: `gh release create` and a real `brew install`.** Replaying the formula's install block by hand is evidence about the LAYOUT, not about Homebrew.
- **AWAITING hv, BOTH UNCHANGED.** (1) **The upstream freeze and v3 are in structural conflict** -- the tap formula points at a GitHub asset, so there is no shipping v3 without pushing the frozen remote; the freeze was called on cost per commit and a release is not a commit. (2) **No `## [3.0.0]` CHANGELOG section**, the first gate a cut would hit. Still refusing to create a placeholder: it carries a dry run further while the notes are absent, turning a loud correct refusal into a silent one.
- **`brew services` waits on WP-08. hv holds the toolchain-pin decision.**

## TODO

0. **ISSUE 0048 FILED ON hv's INSTRUCTION (`22b2b537`) AND THE DETECTOR IS BUILT (`ba90ee39`).** `bin/.devbin/manifest.sha256` names the 27 files devbin OWNS; **FOUR diverge, not the three named in conversation.** Two are mine from `55e8857b`; **`lib/cmd/check` and `lib/cmd/docs` are from ISSUE 0044 and predate the whole discussion -- nobody knew, including ic whose count it was.** The count was three because it counted the patches one node happened to know about.
   - **`int vendor` reports four states** (MATCHES / DIVERGED / MISSING / UNLISTED) and **REPORTS rather than refuses**, the `int hooks` bare-form call: the carry-vs-fork decision is hv's and a tool that made it by refusing would be taking it. MISSING does fail -- that is a broken install, not a local decision. **It never restamps the manifest**, with a control asserting the manifest's own checksum survives a run: restamping makes the report clean while changing nothing about the tree.
   - **Part two is hv's and untouched: carry upstream, or declare a deliberate fork.** ic's reason for upstreaming FIRST is sharper than mine -- the run-verdict is the largest and most general of the patches, so it is the likeliest to be accepted upstream, which makes it the worst one to land locally and forget.
1. **A RELEASE COULD HAVE PUBLISHED BYTES THE TAG IT NAMES NEVER PRODUCED -- FIXED `b853a171`, AND IT WAS MY OWN FILE.** `int macos` had NO tree-cleanliness check anywhere while `build.d/release` has one (that check was the calibration control for the grep that found it). **Every other control in that pipeline asks whether the bytes agree with EACH OTHER** -- the round trip, the sums file, the classification refusal, the tag-exists check -- **and bytes built from a peer's uncommitted work are perfectly self-consistent.** Nothing asked whether these are the bytes the TAG names.
   - **Two reachable routes, neither hypothetical.** `stage` COPIES out of `native/rust/target/release` rather than building, and that is shared mutable state in a four-session clone. **And the support tarball is archived with `tar -C "$ROOT"` from the WORKING TREE, so uncommitted shell would ship verbatim -- which I introduced this morning with the support asset**, on a day `lib/templates/hooks/pre-commit.sh` was dirty for hours.
   - **Recorded at `stage`, refused at `publish` -- my own rule about the control going where the HARM is, turned on my own file.** Refusing at stage would make the pipeline untestable without a pristine checkout, which is the kind of guard that gets worked around. Compared against the TAG's commit, never HEAD: publishing may legitimately happen after the branch moves on, and there is a mutant for that specifically because the HEAD version looks correct and would refuse every late publish.
   - **THE RECORD IS A SIBLING OF THE STAGING DIRECTORY BECAUSE MY OWN CLASSIFICATION REFUSAL REJECTED IT INSIDE.** First time a guard of mine caught something I did not write it for.
   - **Two of my four mutants were wrong in my hands** -- one deleted a `printf` and left a dangling `||` that swallowed the next assignment, one did not parse. The harness refused the second; the first showed as an arm reddening that I had PREDICTED would stay green. **Both were my imprecision, and both were visible only because the harness states an expectation per arm rather than "expect red".**
   - **Owed to vc, not taken: AC-11.4's evidence line is satisfiable by a pipeline that is internally consistent about the WRONG BYTES.** If the release record named the staged commit, the row would assert correspondence as well as consistency, which is what I think it always meant.
1. **THE FALSE GREEN'S ROOT CAUSE IS STILL OPEN, AND I MEASURED THAT AFTER LANDING THE FIX FOR IT.** `bin/int test shell` against a suite with one deliberate failure returns **rc=1 AND seals a non-empty, consistent `.errors`** -- the two sources AGREE. `tests/run_tests.sh` propagates (calibrated against a passing-only directory returning 0) and `run_gate` takes `PIPESTATUS[0]`, so the `tee` masks nothing. **So ic's control C describes a state this gate does not produce.** The guard is still right -- C covers a class and E (returned 0, never sealed) is reachable from any killed run -- but the incident it was built for has a different mechanism.
   - **Both branches land in the same place.** Under one `all` the shell gate returned 1, so `run_all` printed FAILED and "100% green" cannot have come from the tool; run as two separate commands there was no ledger, no summary and no tool claim at all. **The aggregation was done by a human reading two legs, and it was wrong -- twice in twelve hours, the second time to me at the top of this session.**
   - **THE STRUCTURAL GAP: nothing states the verdict of a RUN.** Every gate seals its own `.errors`; the aggregate exists only in `run_all`'s stdout, which scrolls away. "Did the whole suite pass" is answerable only by a human holding two files in their head or by a peer trusting them, and both failed. The ledger already knows every gate, its rc and its seal -- and is `mktemp`'d and deleted at the end of `run_all`. **Asked of ic rather than built: should the ledger outlive the run. It is their instinct that found the class, and this would be a THIRD patch to files devbin owns, which may itself argue for taking the carry upstream first.**
   - **CI CHECKED AND CLEAN on the same class**, verified by reading the invocation rather than the comment above it: `tests.yml` runs a bare `cd tests && ./run_tests.sh`, and its two historical `|| true` swallows were deliberately deleted with a note recording that they were measured to be hiding nothing at the time.
1. **WP-11 (Distribution) -- MINE, WIP. THE PACKAGING HOLD IS DONE; everything remaining here is blocked on a publication, not on buildable work.**
   - **BUILT AND PROVEN: `int macos <doctor|stage|sign|notarize|verify|checksum|prepare|formula|publish|env|store-creds>`.** Signing acts on STAGED copies in `target/dist`; `prepare` runs the pass. `publish` uploads, **re-downloads what it uploaded and hashes THAT**, and only then ships the formula; on a mismatch the release stays and no formula ships. Four refusals canaried both ways.
   - **UNEXERCISED SURFACE IS ONE CALL, `gh release create`.** Closed since: formula is valid Ruby and **lints clean at a tap path**; `curl -fsSL` follows redirects, hashes identically twice, 404s without writing; **tap clone, `Formula/intent.rb` write and commit all succeed against the REAL tap, nothing pushed.** No release here has ever carried an asset, so the upload cannot be rehearsed without publishing.
   - **AC-11.1 + AC-11.4 ARE BLOCKED ON A PUBLICATION, AND THE BLOCKER HAS NOW BEEN WRONG ON THIS BOARD THREE TIMES.** Not the tap (live since 2026-08-15T15:19:58Z, deliberately empty, one README saying why -- verified again 19:2xZ, `isEmpty:false` means "has a commit", NOT "has a formula"); not "a real version and nothing else" (necessary, not sufficient); and no longer 0038 (cc fixed it at `d2b8e76d`). **The live set is: 0036, 0043, the `## [3.0.0]` CHANGELOG section, and hv's freeze call.** The mechanism existing is not the criterion -- **a better-built mechanism is no more a satisfied AC than a built one was.**
   - **HELD until WP-12: `int build release` gaining `Cargo.toml` to its sidecar sync** (wiring it now makes a v2 release stamp its version into `Cargo.toml`). **`brew services` BLOCKED on WP-08.** ~~Install/upgrade docs still owed~~ **-- WRITTEN, at `intent/st/ST0056/install.md`, and it leads with the brew-SHADOWS warning. This TODO was stale and I only found that by going to write the thing.**
1. **Issue 0036 -- `brew install` SHADOWS a v2 install.** brew is PATH position 1, the v2 symlinks are 17 and 19. A v2 user meets the v3 unmigrated-project refusal without asking for anything, and its remedy names `intent upgrade`, which v3 lacks. `migration.md:3` says the migrator IS that verb, so it is WP-10 unbuilt. **vc ruled the control belongs on the BINARY as a build-time invariant, not in `publish`** -- so it is theirs, and WP-11 stays decoupled from WP-10.
1. **MEASURED CLEAN, do not re-investigate: `int prepush` 19s; warm `cargo test --workspace` 22s for 331 tests.** Both were my guesses at the friction and both were wrong. **ic asked me specifically NOT to optimise `int build cli` (25-37s) -- that cost buys correctness**, after a 14-minute-stale binary reported findings cc had already fixed and read as a regression.
1. **`testkit::project_fixture()` -- DEFERRED, AND NOT FOR THE REASON I QUEUED IT.** vc's urgent justification was that no fixture carries a non-default `st_prefix`. `retired_st_prefix.rs` now does -- but it is **UNTRACKED**, a peer's in-flight work, so it is not evidence about the repo and I nearly reported it as one. What remains is a Highlander tidy-up over 12 test files in two crates, and the Rust tree has ten files under active edit tonight. **Wrong time, low urgency, high conflict; hand it to cc rather than reaching into their tree.**
1. **Issues 0030 / 0031 (`intent upgrade`) -- DEFERRED under DEFAULT-DEFER.** 0030: backup dirs stamped in LOCAL time, so oldest-first retention deletes the newer artefact across a DST fall-back. 0031: `--backup-dir` basenames straight into `.backup/`.
1. Open for others: **`intent/.cache/`** (cc, D21); **`core.hooksPath`** (deferred, collides with `intent claude upgrade`); the **`bin/` boundary** (hv); ~~AC-12.1's wording~~ **RULED 2026-08-15: vc adopted my rewording essentially verbatim -- _"nothing in the repo EXECUTES or EMITS a `bin/` intent script path"_ -- with the four classes named in the row and the 133 historical records marked explicitly OUT OF SCOPE.** The parity-fixture question is recorded as a WP-12 call rather than answered.

---

## Folded at the 2026-08-17 second fold (the Devbin carry + the referent)

- **THE CARRY, LANDED IN DEVBIN.** Branch `issue-0016-dropped-status-carry`, two commits (`c432e27` issue, `c8a7dc7` code), unpushed and off main. All four were clean carries and the control was the match count: 26 of 26 manifest'd files matched upstream first, so each was an apply rather than a merge. A three-way hash survey (manifest = vendoring ancestor, Intent = ancestor+patch, Devbin = ancestor+upstream) said that rather than my assuming it.
- **A FIFTH INSTANCE, FOUND FOUR LINES ABOVE ONE OF THE PATCHES, SAME FUNCTION.** `check`'s jq read wore `|| true`, collapsing an ABSENT config, an UNPARSEABLE one, and a genuinely empty `languages` array into one message -- right about exactly one of the three, so it offered `intent lang init` to someone whose config is missing. Measured: jq exits 2 absent, 5 malformed, 0 on `[]`. It survived the sweep that fixed the line below it, whose commit message was literally "sweep the file the reported instance lived in, and the one next to it". **A sweep looks for the reported SHAPE**, and this one wore `|| true` where that one wore a discarded `$?`.
- **READING THE DESTINATION'S TRACKER FOUND THE HOLE IN MY OWN PATCH.** `DEVBIN_SEAL_LEDGER` is exported by `run_all` and nothing else, so the check fired only on `<cmd> all`. Devbin's open 0015 -- a completed run that sealed in-flight -- was a SINGLE GATE. The one instance on record upstream was on the path the patch could not see, and no test would have said so, because the check was green for the reason a check is always green when it is never asked.
- **PROVED THE BRANCH REACHABLE BEFORE BUILDING FOR IT.** `write_errors_file` truncates the seal at rc=0, so the state looks unconstructible -- except that when the truncation FAILS it warns and returns 0, and the marker survives. Driven against a read-only seal with controls either side: returned 0, marker survived, gate reported green. The warn was the entire defence.
- **THE RED SUITE WAS NOT A REGRESSION.** Both legs green at HEAD in a clean detached worktree, including the exact test that failed. Three nodes converged by three methods: my worktree at `b2173b1b`, vc's `git archive` extract at `78a12dce`, cc landing `61069b16`. I named a commit the measurement had not taken (HEAD moved mid-turn); provably harmless, `git log 78a12dce..HEAD` touching neither file, but harmless by luck.
- **hv REVERSED MY 0049 RULING IN ONE LINE AND WAS RIGHT.** I ruled the referent upstream; it is Intent-side. I had measured that `lib/` has zero git invocations and drew the wrong conclusion -- treating "devbin must not depend on git" as a constraint to design around rather than as a signal the concern was not devbin's. Devbin issue 0017 withdrawn; text at `scratchpad/withdrawn-0017.md`. **Filing a question is still putting it on someone else's plate.**
- **DEVBIN BASELINE, BOTH SIDES: 530 green at HEAD before the carry, 530 green after.**

---

# Folded 2026-08-17 (the run: hook, the canary, and the three defects in measured)

## DOING

- **THE 0048 CARRY IS LANDED IN DEVBIN AND AWAITS hv.** Branch `issue-0016-dropped-status-carry`, two commits (`c432e27` issue, `c8a7dc7` code), **UNPUSHED and off main**. Four clean carries (26 of 26 matched upstream first), a fifth instance found four lines from one of them, and the carried check corrected -- it fired only on `<cmd> all` and Devbin's own 0015 is a single-gate run. **Devbin baseline both sides: 530 green before, 530 green after.** Narrative folded to `.history/20260817/wip.md`.
- **`int measured` IS BUILT, COMMITTED (`981a2213`) AND PUSHED** -- vc's 0049, ruled Intent-side by hv after I wrongly ruled it upstream. Reports the current referent and, per run, its seal state and whether the source moved since it started. **The scope control caught my own first version reporting 164,142 files moved** (`$ROOT/target` matches nothing; cargo's output is at `native/rust/target`). Counts now always print against their population. 23 tests, 6 mutants, all exact.
  - **IT EARNED ITSELF WITHIN THE HOUR.** Run against the tree hv's current suite is measuring: **10 files dirty, including two UNTRACKED test files of cc's** (`self_loop_population.rs`, `self_loop_voice.rs`). So this run will not describe HEAD either -- and now something says so before the verdict is read rather than three sessions afterwards.
- **THE `run:` HOOK IS LANDED AND CANARIED (`dde8274e`).** Every `run:` gate in `config.yaml` is wrapped in `bin/.devbin/cmd/measured --exec`, which prints the referent and then BECOMES the command. First live line: `devbin-referent: commit=dde8274e tree=dirty:2`.
  - **A WRAPPER BECAUSE `run:` IS WORD-SPLIT AND NOT `eval`** (`resolve:605-612`) -- devbin's own comment states the rule: a command line that needs real quoting needs a handler. It lives project-side per hv's ruling; devbin has no business knowing what a commit is.
  - **THE SHAPE IS LOAD-BEARING, NOT COSMETIC.** The line lands in the stream the grammars parse, and two match ANYWHERE in a line -- credo on `[A-Z] `, xcodebuild on `: error: `. So: no brackets, no timestamp, and **specifically no commit subject** -- a real subject like `fix: error: handling` would forge a Swift diagnostic into a seal. `assert_referent_safe` REFUSES a bad line on every run rather than a test asserting it once.
  - **CANARIED BOTH DIRECTIONS, ALL SEVEN GRAMMARS AND ALL FIVE COUNTERS**, against synthetic fixtures AND the real logs in `tmp/`, **with a POISON arm per grammar** -- a green arm that cannot fail proved nothing. The one honest exception is named rather than discovered: on a red run no grammar can parse, `write_errors_file` falls to `tail -50` and the seal gains exactly the header line, which is the point.
  - **COVERAGE IS HALF THE SURFACE AND THAT IS STATED, NOT HIDDEN.** Five of ten gates are `run:` lines; `check critic`, the docs gates and `fmt md` are builtins with no `run:` line to wrap. Those report **NONE RECORDED**, never silence -- an unstamped run must not read like one that started clean. `devbin_rust_gates` now pins the wrapper on every declared `run:` line.
- **vc FOUND A REAL FALSE POSITIVE IN `measured` AND FIXING IT SURFACED TWO MORE OF MINE.** All three were this tool's own defect class, inside it.
  - **mtime is not content.** A suite that backs a file up and restores it moves the mtime without changing a byte (`rule_index.bats` does this to the LIVE canon index); three of eleven files on a real run were byte-identical to HEAD. Now split, compared against the run's own referent when it has one.
  - **`ls-files --others --exclude-standard` OMITS IGNORED FILES**, so an ignored file that moved landed in "byte-identical" -- a claim nothing had checked and nothing could. Membership of the ref tree is the test now: "identical" is only sayable of a file that is IN the reference.
  - **The comparison reference was named only when something DIFFERED**, so a run whose movement was all byte-identical said "9 are BYTE-IDENTICAL" without stating identical TO WHAT.
- **THE MUTATION PASS FOUND FIVE WEAK TESTS, ALL ONE SHAPE:** an assertion satisfied by text other than the thing under test -- two matched the refusal message (which quotes the offending line), one matched the new footer, two matched a filename that also appears in the movement section. **And it caught me re-deriving a prediction from a run taken before I changed the code.** Every mutant's failing set was named in advance and every one was exact -- which is the claim worth making, rather than how many there were.
- **0054 RULED, AND vc CORRECTED THE RULING.** The prettier flag must leave devbin core -- `.prettierrc.json` CANNOT fix it, because a CLI flag beating a config file IS the root cause. Measured: **246 of 898 tracked `.md` files change under `proseWrap: never`**, so the value question goes to hv with its price attached. Config line HELD until the core removal lands, so one fix arrives with one meaning. **0054 stays open.**

---

# Folded 2026-08-17 (second fold: the merge and the prettier fix)

## DOING

- **DEVBIN: MERGING `issue-0016-dropped-status-carry` TO MAIN, then working on main.** hv's call, and correct -- the branch had been parked for no reason. Two commits (`c432e27` issue, `c8a7dc7` code), six files, main has moved zero commits since the branch point so it is a fast-forward. **Full devbin suite re-running on the branch now** rather than trusting this morning's 530 -- a baseline is a claim about a revision.
- **NEXT, ON DEVBIN MAIN: delete `--prose-wrap never` from `builtin_fmt_md` (`lib/cmd/fmt:67`).** Three words. **Measured: it reformats ZERO files** -- the committed tree already IS bare prettier's output (`parity.md` 0 changed lines, `MODULES.md` 0), and `int fmt md` is the only thing in the estate that disagrees with the repo (110 and 489 lines respectively). Then propagate to Intent's vendored copy, which is byte-identical to devbin main, plus one manifest hash.
  - **THE 246 FIGURE DOES NOT APPLY TO THIS DIRECTION** and I nearly let it. That was the cost of ADDING `proseWrap: never` to `.prettierrc.json`. Removing the flag is free, and no `.prettierrc.json` change is needed at all -- prettier's default already matches the repo, so both of my earlier rulings were wrong: the config cannot fix it (vc), and nothing needs adding (measured).
  - **NOT via a `fmt md` override in Intent's `config.yaml`**, which looks smaller and is bigger: the builtin deliberately defaults to `./*.md` and warns, because prettier strips Markdown table column alignment and sweeping `intent/` and `docs/` would silently undo it. A word-split `run:` line cannot express that default, so the override trades a 3-word fix for losing the guard.
- **INTENT IS CLEAN AND PUSHED.** The `run:` hook and the mtime/content split landed at `dde8274e`; board at `57310729`. The full bats suite sealed GREEN against it and reported, of itself, that it started DIRTY at `dde8274e` with two peer files uncommitted -- so that verdict never described a commit, and said so before anyone read it.

## DOING as at the 15:5xZ fold (the devbin merge + upgrade arc, all landed)

- **DEVBIN MAIN NOW CARRIES BOTH: the 0016 carry (`5f32236`, fast-forward) and the 0054 prettier fix (`a23022f`).** hv ruled the branch should just be merged rather than sitting parked. Full suite running on main; nothing touches Intent's vendored tree until it seals.
- **MY "530 GREEN BOTH SIDES" WAS WRONG AND THE RE-DERIVATION IS WHY THE MERGE IS SAFE.** Re-run before merging rather than trusted: the branch was **551 green and ONE RED at `c8a7dc7`**. The carry changed `record_seal` to record EVERY gate with its rc -- necessary, because a ledger holding only runs that already said they failed is blind to the one disagreement that matters, a gate returning 0 while its seal records a failure -- and **left `tests/unit/runlog.bats` asserting the old "records only failures" contract.** So the branch carried two directly contradictory tests and was green. Fixed at `5f32236`; **552 green, 0 red**, and that number names its revision.
  - **TWO FAILURES OF MINE, BOTH ALREADY ON THIS BOARD AS LESSONS.** _A migrator must not do half of a two-ended migration_ -- I changed a contract, updated the caller and the new tests, and never ran `grep record_seal tests/`. And _a measured figure names its subject and revision_ -- the 530 was taken mid-work and carried forward across the commit that invalidated it, so it was true of something and reported of the branch. vc's framing is sharper than mine: **a number with no revision attached cannot be wrong when taken and cannot be right afterwards.**
- **0054 IS FIXED AT THE LAYER, AND IT REFORMATS NOTHING.** `builtin_fmt_md` no longer passes `--prose-wrap never`. A CLI flag beats a config file, so `fmt md` was imposing a prose style on every project that vendors devbin while that project's `.prettierrc.json` and its commit hooks used another. **Measured in the consumer: 110 changed lines on `parity.md`, 489 on `MODULES.md`, against a tree bare prettier reproduces byte for byte.** No `.prettierrc.json` change needed anywhere -- both of my earlier rulings on this were wrong, and vc priced the one I nearly took (246 of 898 files, and it would not have worked, because the flag outranks the file). **I grepped `tests/` for the flag BEFORE changing it** -- the step whose absence put the contradictory test on the branch two commits earlier.
- **NEXT: `bin/int upgrade` IN INTENT, then verify.** Intent has four locally-patched devbin files (`lib/cmd/check`, `lib/cmd/docs`, `lib/resolve`, `lib/runlog`) -- exactly the four the carry touches -- so the upgrade needs `--force` and resolves 0048's carry-upstream-vs-fork in favour of carry-upstream. **Diffed all four first: `resolve` is byte-identical to the carried version, and the only loss anywhere is an issue-number reference in one `die` string that devbin core genericised, correctly.** No behaviour lost.
  - **0048 STAYS OPEN and vc is right that I conflated the instance with the class**: reconciling four files is not the same as this project having a detector for local patches, and that absence survives either way. vc re-measures the manifest after the upgrade rather than taking my word.
- **SEQUENCED FOR ic (0059): ic writes the `cmd/precommit` wiring, AFTER my Intent-side upgrade, and I review before it lands.** Not after the devbin merge -- after the upgrade, because `precommit` runs against the vendored tree that is about to be replaced across four files, and two changes in one window are attributable to neither. **Not taking the work off ic deliberately**: they found the hole, proved it bites, and know which instruments are report-only; handing it to me would re-derive their classification from their summary. Review answers the shared-path concern; authorship is not what creates the risk.

## FOLD (2026-08-17 15:47Z) -- archived DOING

## DOING

- **THE SUITE SEALED AND THE NUMBER CAME FROM TWO SOURCES, WHICH IS THE ONLY REASON I CAN STATE IT.** Devbin full unit suite at `7a232e4`, clean tree, **exit 0** -- but the background capture retained **six lines**, so counting `not ok` in the log was not available and would have certified absence from a truncated file. The declared population is **572 `@test` blocks across 22 files**, statically counted, and the run's last leg is `ok 572`: two independent sources agreeing that the whole population ran and none failed. **The exit code is the seal; the static count is what says the seal covers everything.** A tail alone could not have told a full green from a suite that stopped early on a passing leg.
- **vc's `# WHY eval` note: WRITTEN, TESTED, PUSHED -- Devbin `254f63e`, comment only.** Three things recorded rather than assumed: **the SOURCE is what makes it sound, not the string** (`config.yaml` is project-owned committed source, as trusted as `cmd/`; anyone who can write the key can already write `cmd/anything` and name it from a `run:` line, so there is no privilege to escalate to); **`eval` rather than `run:`'s word-split** because a referent is a value composer and a pipeline is its natural spelling, while `run:`'s refusal exists to keep a GATE legible and a one-shot substitution has no verdict to read; and **the line it must never cross -- eval-ing the COMMAND is not parsing the ANSWER.** `bash -n` clean, `run_referent.bats` 20/20.
  - **THE DEFERRAL'S REASON HELD RIGHT UP TO THE MOMENT IT CLEARED, AND THAT IS THE POINT.** I did not take this at the fold because the suite was running against that tree, and editing mid-run makes the number describe a tree that changed under it -- the exact defect the day's work exists to expose. It cost one bounce and bought a number that names a commit.
  - **AN UPSTREAM EDIT CANNOT DIVERGE A CONSUMER, MEASURED BEFORE COMMITTING RATHER THAN ASSUMED.** `lib/install` writes the manifest by checksumming the **TARGET** at install time, so Intent's vendored tree and its manifest are untouched by a change to Devbin's `bin/devbin`. `int vendor` stays 27/27; Intent runs an older devbin until someone re-vendors. **The question was worth asking precisely because the last edit to this file was the divergence incident.**

- **0049 IS BUILT, BOTH SITES, LANDED, AND THIS PROJECT IS STOCK DEVBIN AGAIN.** Devbin `7a232e4`, Intent `58eee27f`, `int vendor` 27 of 27 with no local patches. hv ruled it in on vc's re-scope (_"Agree with VC. Just do it."_) plus the Devbin-first sequence; recorded on receipt at `84eecb35`. **devbin echoes an opaque string and never parses it -- a presence test and a `printf`, no validation, no trimming, no length cap**, because each of those needs to know what the string is FOR.
  - **THE SCOPE AS HANDED TO ME WOULD HAVE MISSED THE FOUNDING INCIDENT, and vc called it a correction to their own ruling rather than an extension.** The re-scope named `run_gate`/`verdict:`, which is right for a single-gate run -- but hv read `FAILED: rust` in a **SUMMARY**, and `lib/runlog` already says out loud that across a ten-option `all` the per-gate lines have scrolled away by the time the summary prints. **A remedy sited only where the reader is not looking is built, correct, and absent from the exact screen it was filed from -- which reads as a close.** Both sites now.
  - **THE TWO HALVES ARE COMPLEMENTARY AND ONLY A MEASUREMENT SAYS WHICH IS WHICH.** `:1039` is the only `tee` in the subsystem and `print_run_verdict` runs after it closes, so the terminal line never reaches the log and the log line never reaches the summary. **Reasoning about it gives the plausible wrong answer that one makes the other redundant.**
  - **Five mutants, predictions written before any ran, all five exact.** M4 removes the per-gate call and leaves the summary tests GREEN, which is what proves the two sites are independently covered rather than one test passing for both. **M5 is the acceptance condition's mutant** -- devbin taking the first field, the way anything reading a sha would -- and it reds eleven of twenty, so the verbatim assertions are load-bearing.

---

# Archived at the 2026-08-17 17:38Z localfold

## DOING

- **THE CUTOVER GATE IS LANDED: `same_end_state_check.sh` (`66ba461d`), tool + roster row + MODULES row in ONE commit.** hv authorised it: the cutover does not run until a second migration over an INTERRUPTED estate is measured to reach the same end state as a clean one. Four canaries green -- identical 0, ic's planted accretion 1, missing tree 2, empty tree 2. **The identical arm is load-bearing: a comparator that calls everything different is useless and nothing else catches it.**
  - **THE VERDICT IS A TREE DIFF AND CONSERVATION IS CORROBORATION -- vc's correction, and it is structural rather than a preference.** Every conservation predicate is anchored on the ESTATE side, which is why ic's accretion passed reachability, presence, DOUBLED **and prose conservation itself**. **Prose GAINED is invisible to a check that only asks whether what left arrived.** Built the other way round, this gate would have inherited that blind spot with nothing saying so.
  - **SIBLING, not an arm of `conservation_check.sh` -- vc ruled it and gave me the TEST rather than the verdict, which is why I can restate it: do the two tools have inputs that overlap?** Theirs takes a census and a tree, mine takes two trees; `--out-of-model` means nothing for a two-tree comparison and a census means nothing for one. **An exit code answers ONE question**, and folding them would put two unrelated modes behind one code.
  - **hv's NO-DUPLICATION CONSTRAINT SATISFIED BY MEASUREMENT, NOT ASSERTION**, and I had proposed the violation an hour earlier -- I offered ic "take the 60 lines and own it inside your harness", which hv ruled out in terms. Measured against vc's with comments stripped so prose about a mechanism is not counted as one: **section extraction 6/0, trim normalisation 11/0, path-to-entity 0/0, hashing 2/2** -- and that overlap is a bare `shasum` on different subjects.
  - **ic FIXED THE THING THAT WOULD HAVE MADE MY LANDING AWKWARD, BEFORE I LANDED.** Their roster and residue checks now read the INDEX (`ca71bd61`, `cac74720`), so one commit is the RIGHT shape rather than a workaround: an untracked tool freezes nobody and is caught the moment it is staged. **My index-vs-worktree fix propagated to a sibling tool I never touched.**

- **HEAD STOPPED BUILDING AND EVERY PUSH ON THE ESTATE WAS REFUSED -- and cc's account of the class is better than mine.** `74c4b357` committed `migrate.rs` (the consumer of `Scan.already_migrated`) while `legacy.rs` (the producer) stayed uncommitted: **0 occurrences at HEAD, 6 in the worktree.** Diagnosed in a clean `git archive` extract with a private `CARGO_TARGET_DIR`, so the shared `target/` was never touched. Fixed by cc at `7628a02b`.
  - **MY VERSION WAS THE SINGLE-NODE ONE: `--only` commits what you name and a two-ended change has two names.** **cc's is strictly stronger: THE TWO ENDS WERE ON TWO DIFFERENT NODES' DESKS**, so neither had a partial view -- ic committed the consumer from a tree where the producer existed, cc held the producer in a tree where the consumer existed, **and both were looking at a green build of the same tree, green precisely because it held both halves.** No vantage point inside either session could see it.
  - **"MY WORKTREE BUILDS" WAS TRUE FOR ALL FOUR OF US SIMULTANEOUSLY WHILE HEAD WAS BROKEN.** The general form: **a shared worktree makes every green a claim about the union of four people's uncommitted work.** The prepush guard is the only instrument positioned to see it, because it clones and builds what is about to be pushed rather than trusting where you stand.

- **hv RULED THE HOIST: _"We go big bang and fix forward."_ BOTH ASKS DISCHARGED, BOTH FIXES PUSHED.** Option A: migrator run and PATH repoint are ONE operation. **The tool end does NOT become a WP or an AC** -- hv carved it out to nobody and fix-forward is a directive against scaffolding for it. Relayed to all three peers.
  - **RE-RUNNABILITY IS THE PROPERTY, NOT ROLLBACK (cc's, and it is the sharper half).** Rollback covers a failure the run DETECTS; fix-forward must survive the one that kills the process between writes. So a second run over a partially-written estate must reach the same end state as a clean one -- idempotence over canon already on disk. **And it gives stamp-last a second job neither cc nor ic had credited it with**: while the config still says 2.19.0, an interrupted estate is one v3 treats as unmigrated and will migrate again; stamp early and a half-migrated project is one v3 believes is finished, so fix-forward has nothing to fix forward with.

- **`53f88757` -- v2 REFUSES A PROJECT FROM THE FUTURE, exit 2.** `version_gt` + `_vnum` moved to `intent_helpers` (they lived only in `intent_upgrade`, which is exactly why the dispatcher could not ask the question). **Nothing allowlisted: the guard sits past `--version`, past `help` and past the `GLOBAL_COMMANDS` exec, so every exemption a hand-written list would have argued for is structural** -- which is the test for whether it is a guard or an argument. Exit 2 matches cc's `critic` ruling, so v2 and v3 now speak one exit-code language to the gate.
  - **BLAST RADIUS MEASURED, NOT ASSUMED: sixteen adjacent Intent projects, every one at or below 2.19.0, and `version_gt` is strict** -- so it is a **no-op across the entire local fleet today** and fires only on a v3-migrated project. The one way it can surprise anyone is a STALE install meeting a newer project, which is the correct semantic and belongs in the release note rather than being discovered.
  - **IT REDDENED THREE OF cc's LIVE DIFFERENTIALS WITHIN THE HOUR AND WAS RIGHT TO.** `close_gate_parity.rs` shelled out to `bin/intent` over a fixture declaring 3.0.0 -- doing precisely what the guard exists to stop, since the day they were written. **A real fixture nobody planted, which is better evidence than the canary I would have built.**

- **`faaa2f0f` -- `INTENT_VERSION` NAMES THE INSTALLED TOOL, ALWAYS.** `intent_config:50` had been assigning the project's declared version to it inside `load_intent_config`: one identifier, two subjects -- the TOOL at `intent_config:17`/`bin/intent:41`, the PROJECT from line 50 on -- switched inside a function called for its side effects and stated nowhere. The declaration is now `PROJECT_INTENT_VERSION`, empty rather than defaulted, because "declares no version" and "declares the tool's version" are different facts.
  - **THE EVIDENCE IT HAD ALREADY COST SOMETHING, AND IT IS THE GENERAL LESSON: THREE SITES ALREADY RE-DERIVED THE TOOL VERSION LOCALLY TO WORK AROUND IT** -- `bin/intent:41`, `intent_info:44`, `intent_st:342` -- **each looking like ordinary initialisation rather than a workaround.** That is why `intent info` reports the right number today: not because the variable is correct, but because the display path stopped asking it. **A symptom that has been independently worked around three times reads as no symptom at all.** It also silently disarmed the FIRST version of my own guard, which compared 3.0.0 against 3.0.0 and passed -- in the direction that looks clean.
  - **Fixed at the CAUSE, and the three re-reads left in place deliberately**: each is a correct call to the one implementation rather than a second copy, so none is a Highlander breach, and removing them buys nothing while entering three files this change need not touch. Recorded so the next reader does not infer from three re-derivations that the variable is still untrusted. 62 tests across six bats files, 0 failed.

- **THE HOIST IS A TWO-ENDED MIGRATION AND ONLY ONE END IS BEING BUILT. This is the session's finding and it is a sequencing call for hv, not buildable work for me.** WP-10 converts the PROJECT end -- config + files, cc and ic. **Nothing converts the TOOL end** -- what `intent` on PATH resolves to -- and it appears in none of WP-10's ACs. The standing lesson already names it: _a migrator must not do half of a two-ended migration._ So `upgrade`'s contract is not "convert the project"; it is convert the project **and leave no writing v2 pointed at it**, or refuse to start.
  - **MEASURED, sacrificial project declaring `intent_version: 3.0.0`, v2 on PATH.** `upgrade` exit 1 -- _"refusing downgrade: project is at v3.0.0, target is v2.19.0"_. And then: `st list` 0, `info` 0, `doctor` 0 (_"doctor: intent v2.19.0"_, noticing nothing), `todo` 0 **and writing**, `st new` 0 **and writing SEVEN FILES** (the five ST docs + `steel_threads.md` + `todo.md`). The shipped gate then **LANDED** the commit.
  - **v2 KNOWS THE PROJECT IS FROM THE FUTURE, SAYS SO IN THOSE EXACT WORDS, AND APPLIES THAT KNOWLEDGE AT THE ONE COMMAND THAT WAS ALREADY SAFE.** `bin/intent_upgrade:93` already does the comparison with `detect_project_version` + `version_gt`, both existing helpers; `bin/intent` learns the TOOL version at :41, dispatches at :92, and never asks what the PROJECT declares. **An application gap, not a missing capability** -- ~6 lines at one site. Not mine to land: `bin/**` is sacrificial-worktree-only (three PATH entries resolve to it, four sessions live) and v2 maintenance is DEFAULT-DEFER. Whether it clears the show-stopper bar is hv's.
  - **THE ASYMMETRY IS THE FINDING: v3-in-a-v2-project REFUSES (exit 1); v2-in-a-v3-project OPERATES AND WRITES (exit 0).** The refusal exists in the direction that costs a user nothing and is absent in the direction that destroys their work -- under D01-as-reversed the md IS a generated view, so v2's seven files land where the db has no row and the next regeneration discards them silently. **`doctor` is the sharpest instance**: its whole job is reporting unmigrated residue and it reports its own version instead.
  - **AND hv's STANDING RULE HAS AN UNNOTICED EXPIRY.** "v3 is NOT on PATH" and "this repo is a v2 project" are the same rule stated twice, **and the hoist falsifies the second.** At the instant config.json says 3.0.0, keeping v3 off PATH stops being the safety measure and becomes the hazard. Cheap to honour, unrecoverable to miss, and it must be in front of hv BEFORE the migrator runs here.
  - **THE BLAST RADIUS IN MY OWN LANE IS MEASURED AND IT IS SMALL, WHICH IS WORTH KNOWING BEFORE THE FLIP RATHER THAN DURING IT.** The 12 repo-local gate guards are **unaffected** -- every one is invoked by explicit script path, and `int cli` / `int dogfood` / `int macos` / `int build` all resolve a binary by explicit path too. **Exactly two commands resolve `intent` from PATH and both fail SAFELY**: `int check` (`intent critic --languages`, which v3 answers at exit 1 -- a clap arg error, since v3's `critic` takes a positional) **DIES loud**, and `int docs` (`intent agents sync` exit 2 unimplemented; `intent treeindex` exit 2 **RETIRED in v3**) **warns and continues**, naming what was not regenerated.
  - **AND `int check` ALREADY CARRIES THE CORRECT DIAGNOSIS -- I PREDICTED IT WOULD MISLEAD AND IT DOES NOT.** Its `die` at `lib/cmd/check:59` names _"a v3 binary shadowing v2 on PATH"_ as the known cause, and the comment above it says so too. So the one command that fails hard at the hoist explains itself correctly. **The `int docs` treeindex arm is the only real work: v3 RETIRED the command, so that arm gets DELETED at the flip, not repaired** -- fail-forward, no compat shim -- and it cannot be done before the flip because v2 still has `treeindex` and `int docs` works today.

- **WP-10 (AC-10.4 / AC-10.9 / AC-10.10). AC-10.9 IS NOW MEASURED ON BOTH ITS POPULATIONS AND HOLDS; AC-10.10 HOLDS; AC-10.4's SUBJECT IS PINNED AND ITS CONSTRAINT FOUND.** Driven through the shipped `pre-commit.sh` with `intent` resolving via a PATH scoped to the `git commit` invocation alone -- never exported, session PATH verified after.
  - **AC-10.9 SAYS "MIGRATED OR NOT" AND I HAD DRIVEN ONLY THE UNMIGRATED HALF, THEN REPORTED THE ROW AS HOLDING. Third instance of the thing I have being narrower than the thing I said, and the third time it was the POPULATION rather than the premise.** Found by going to the contract instead of re-reading my own board -- which is the other rule, also third instance: **a board says what I was thinking, the AC says what was agreed.** Now driven as a full 2x2 (v2/v3 x 2.19.0/3.0.0), every cell a real `git commit` through the shipped hook. **All four LAND.** The two v3 cells are the criterion and it holds on both; the two v2 cells are the control, and they discriminate -- v3 refuses `st list` at 2.19.0 (exit 1, "this project has not been migrated") and accepts it at 3.0.0 (exit 0, renders a table), so the harness can tell the cells apart. **A matrix where everything commits and nothing discriminates is indistinguishable from a matrix that never ran.**
  - **AC-10.10 HOLDS, three arms, and ARM 1 IS THE LOAD-BEARING ONE.** Good stamp LANDS (exit 0); no trailing `Z` BLOCKED; a 2099 stamp BLOCKED, both carrying the real clock-guard text. **A guard that blocks everything and a guard that blocks the right thing are indistinguishable if you only try the bad case.**
  - **THE RELAYED HAZARD IS FALSE IN ITS SPECIFICS AND A WORSE ONE SITS UNDER IT.** "The refusal returns exit 1 and the gate reads it as findings" is true about the refusal and false about the gate. **The gate makes exactly TWO `intent` calls -- `info` and `critic <lang>` -- and the whiteboard guards make NONE.** Measured: `info` 0, `critic` 2 (`known command that is not implemented yet`), `st list` 1, `wp list` 1, `doctor` 1. **The refusing commands are not in the gate; the gate's command does not refuse, it is unbuilt.**
  - **SO AC-10.9 IS SATISFIED BY AN UNIMPLEMENTED FEATURE AND IMPLEMENTING IT BREAKS IT.** The hook declares its own contract in its header: 0 clean, 1 findings BLOCK, 2 invocation error FAIL-OPEN. **vc's corollary exactly: a control correct today BECAUSE OF AN OPEN DEFECT is one that whoever fixes that defect will silently break**, and nothing connects `critic`'s implementation to this gate's contract.

## TODO

1. **AND THEN THE CONSTRAINT I SENT TURNED OUT TO BE TOO WEAK AND SLIGHTLY WRONG-SHAPED. BOTH CODES ARE WIRED BACKWARDS AGAINST THE SHIPPED GATE, AND CHOOSING A CODE FOR THE REFUSAL CANNOT FIX IT.** Provable from two committed artefacts, no rebuild. **v3 declares** (`intent-cli/src/guide.rs:118`): `0` success; **`1` EVERY failure, explicitly including usage errors** (_"exits `1`, not clap's default of 2"_); **`2` `critic` FINDINGS -- and also unimplemented commands**, closing with _"So `2` is ambiguous and the stderr line is what tells the two apart."_ **The shipped gate branches** `0)` pass, `1)` BLOCK, `*)` fail-open. So: **findings=2 -> the gate FAILS OPEN and silently stops blocking on real findings**, and **every-failure=1 -> the gate BLOCKS and calls a migration refusal or an unknown flag "findings at severity >= X".** Both inverted. **The reason a code assignment cannot resolve it: findings and refusals need OPPOSITE gate behaviour -- block versus fail-open -- and v3 gives them the SAME code.** The value space is the defect.
   - **v3 HAS ALREADY NOTICED AND RESOLVED IT WITH A STDERR STRING, AND THE GATE NEVER READS STDERR.** So the discriminator exists and is structurally invisible to the only consumer that must act on it. **My own rule arriving from the other side**: parsing another program's rendering makes it an API nobody declared -- except here the CLI is _requiring_ a consumer to parse rendering, because the code cannot carry the distinction. `exit_code_consumers.rs:94` already tracks one instance (`|| true`, "will keep hiding real errors once `critic` is built. Latent rather than live").
   - **THIS IS WHERE IT BECOMES MINE: AC-10.4 SAYS THE HOOKS ARE BYTE-IDENTICAL PRE/POST.** The hook is `lib/templates/hooks/pre-commit.sh`. **If it survives the migration unchanged -- exactly what my AC requires -- the post-hoist gate is still branching 0/1/\*, so v3's `critic` must conform to THAT contract and not to guide.rs.** AC-10.4 and v3's declared exit codes are in direct conflict, and satisfying mine constrains cc's. Sent; no ask attached, it is cc's surface and hv's ruling if one is needed.
   - **UNMEASURED AND SAID SO: v2's `critic` exit code on real findings.** Four probes; I could not make the headless shell runner emit a finding even from a fixture built out of `IN-SH-CODE-001`'s own Detection heuristic, and the probe **refused to report numbers from two cases that failed to discriminate** -- uniformity across cases that should differ is evidence about the instrument. Likely ST0039's strict-proxy contract. **So "v2 returns 1 on findings" is the gate's DECLARED contract, not something I watched happen**, and the collision does not depend on it.

1. **THE `critic` EXIT-CODE CALL WENT TO cc, NOT hv -- AND THE ROUTING WAS THE DECISION.** My board said "put it to hv"; between writing that and acting on it, hv put cc on WP-10 and **cc's own board claims the exit codes** under the split with ic. So it needed no ruling: it is a constraint on a surface a peer owns, and a peer can accept it or escalate. Re-measured before sending rather than quoted from the fold -- `critic shell` and `critic nosuchlang` both exit 2, `info` 0, `st list` 1, `doctor` 1, `upgrade` 2. **The constraint: at the `critic` surface a migration refusal must return 2, not 1**, or the day cc implements it every unmigrated project stops committing, because the gate reads 1 as findings. It follows from the hook's OWN header (0 clean / 1 findings BLOCK / 2 invocation error FAIL-OPEN), so it does not need the broader 0044/0045 ruling. **And under the moratorium it needs no new artefact: `intent-cli/tests/exit_code_consumers.rs` already exists as exactly that policy file.**

1. **AC-10.4 -- THE SUBJECT IS NOW PINNED AND THE REAL CONSTRAINT IS FOUND, THOUGH THE COMPARISON STILL WAITS ON THE MIGRATOR.** Subject at HEAD, read from the index: **four files, 8039 bytes** -- `settings.json` (845), `scripts/post-tool-advisory.sh` (2432), `scripts/require-in-session.sh` (2555), `scripts/session-context.sh` (2207), sha-256 recorded in the commit that lands this. So the moment a migration runs, AC-10.4 is one command rather than a fresh investigation.
   - **THE CONSTRAINT cc AND ic NEED BEFORE THEY WRITE IT: v2's own upgrade REWRITES this subject by design.** `bin/intent_upgrade:156` invokes `intent claude upgrade --project-dir . --apply`, the sole canon engine. **If v3's migrator does the same thing, AC-10.4 fails by construction** -- and the naive reading of the row ("why would a migration touch `.claude/`?") is exactly what would let it.
   - **AND THE ROW'S CRITERION IS NECESSARY BUT NOT SUFFICIENT, which is worth knowing even though the gap is currently harmless.** `settings.json` contains nothing but three `intent claude hook ...` invocations, so byte-identity is a claim about the FILE and not about whether the hooks still work: a migration that leaves it untouched and drops one subcommand satisfies the row and breaks every session. **I checked and it does NOT reproduce** -- v3 answers `claude hook session-context` and `claude hook require-in-session` at exit 0, same first line as v2, in a 3.0.0 project. So it is closed by cc having wired it, which is a fact about the implementation rather than about the criterion. Recorded, NOT filed and NOT proposed as a row change -- moratorium.

1. **MY EVIDENCE FOR 10.9 / 10.10 IS EXPLORATORY AND UNREACHABLE, AND I AM NOT FIXING THAT BY ADDING FILES.** The drivers live in a session-scoped scratchpad, so my previous fold's citation of `scratchpad/drive_hook.sh` was **a claim about a file that no one else can open and that I will lose** -- my own rule (_a file without a commit is not a referent_) pointed at my own board. **The contract already answers where the durable form goes and it is not a shell script**: AT-10.9 cites `intent-cli/tests/exit_codes.rs` (green), **AT-10.10 cites `intentsvcs/tests/migrated_guards_still_refuse.rs` and is still `to-write`** -- so AC-10.10 is "measured and holds" on my board with no artefact in the contract, which is the vacuous green inverted: a real measurement nothing durable records. **Owed, and deliberately not discharged tonight** -- that file lands in the crate cc and ic are both editing (`legacy.rs`, `migrate.rs`), and adding shell instruments instead would be the immune system feeding itself under the exact moratorium that forbids it. The measurements and their method are on this board in full, which is what a reproduction actually needs. **Do NOT move an AT status without committing first (issue 0033 destroys the note).**

1. **Queued upstream in Devbin, small, NOT an instrument: `devbin upgrade` writes the manifest with the `lib/install` it has just replaced.** So a field added to the header is absent after the very upgrade that introduces it; two passes fix it, and **the pass that produces the correct record is the one whose output reads `0 updated, 27 unchanged, Already up to date`.** A tool reporting that nothing happened while rewriting its own provenance record. Fix with the reasoning in its commit body; hv has held new class entries.

1. **WP-11 (Distribution) -- MINE, WIP, GATE BLOCKED 2/4 (AC-11.1 + AC-11.4), AND BOTH NEED A PUBLICATION AND NOTHING ELSE.** hv on the cut: _"I hope so! But I'm waiting on VC, IC, and CC to sort that out."_ So the last two ACs are parked on other nodes, not on buildable work of mine.
   - **THE BLOCKER SET WAS STALE ON MY BOARD AND CORRECT IN `install.md` -- FOURTH INSTANCE, and this time the artefact was right and the memo was wrong.** My board still named **0043**, which closed on 2026-08-16 and whose publication hold **I lifted myself at `61724664`**. `install.md:5` has said ONE hard hold (0036) since. **Re-derive from `install.md` and `intent ac gate`, never from this board.**
   - Live gates: **0036** (the refusal's remedy names `intent upgrade`, which v3 lacks), the **`## [3.0.0]` CHANGELOG section** (absent; head is `## [2.19.1] - in progress`), and **hv's freeze call**. `Cargo.toml` is `3.0.0-dev` and `publish` refuses a dev formula, held to WP-12 deliberately.
   - **UNMEASURED AND MINE: `stage` COPIES from `target/release` and nothing proves those bytes were built from the commit it records.** `cmd_prepare` is stage/sign/notarize/checksum with **no build step**, and `grep` for `stat -f|mtime|newer|%ct` across `cmd/macos` returns nothing. Right now `intent` is 39 hours newer than `intentd` on disk. **AC-11.4's fix compares provenance to the TAG; it never asks whether the binaries were BUILT from it**, so a clean tree is silent about staleness. Same "consistency is not correspondence" one level in.

1. **0048 -- CARRIED. AWAITING hv: the branch is unpushed and off main, and merging it is hv's call, not mine.** Devbin `issue-0016-dropped-status-carry`: `c432e27` (issue 0016) + `c8a7dc7` (the four patches, the fifth instance, the correction, 22 tests). **ic's two asks are both in the issue text** -- the false-green stated as a MISSING ARTEFACT rather than a bug (additive, no consumer to migrate, which is the first thing an upstream maintainer asks), and the zsh `path`-clobber warning, in prose rather than only in a script, because anyone re-running the measurement hits it.
   - **INTENT IS DELIBERATELY LEFT UNSYNCED and this is the part to re-decide rather than drift on.** `bin/.devbin/**` here still carries the UNCORRECTED check -- the one that only fires on `<cmd> all`. Syncing now would fix a real gap in this project's own gates, which are single-gate almost always (`int test shell`); it would also re-diverge from the manifest in a NEW way, one week after hv ruled against exactly that. **hv's ruling wins on the direction; the cost of honouring it is that this repo keeps a known-narrow check until upstream lands.** Written down because a deferral recorded in one place has its precondition met somewhere else.
   - **`int vendor` stays here regardless** -- it is the detector, not the fix, and the manifest header promises a detector that consumers cannot reach. Raised upstream in 0016 as a candidate builtin (`devbin vendor`), separable and not blocking.
1. **THE REFERENT -- vc's issue 0049. I RULED IT UPSTREAM AND hv REVERSED ME IN ONE LINE. IT IS INTENT-SIDE, AND IT NEVER SHOULD HAVE BEEN A QUESTION.** The consistency half already landed upstream in `c8a7dc7` and is unaffected. **The referent half belongs in `bin/.devbin/cmd/` -- OURS, per the manifest header I quoted in my own issue text without applying: `config.yaml`, `cmd/` and `help/` BELONG TO THE PROJECT.** Eleven handlers already live there. So it needs no devbin change, no manifest divergence, and no upstream decision to wait on.
   - **THE ERROR WAS DRAWING THE WRONG CONCLUSION FROM A CORRECT MEASUREMENT.** I measured that `lib/` has ZERO git invocations, and treated "devbin must not depend on git" as a constraint to design around and file for a ruling. **It is not a constraint -- it is a signal that the concern is not devbin's at all. Intent knows it is a git checkout; a generic gate runner has no business knowing what a commit is.** I half-noticed: refused to build it, flagged the property, and filed the question anyway. **Filing a question is still putting it on someone else's plate**, and half-noticing an anomaly without following it one step further is the shape that cost this estate four separate findings today.
   - **Devbin issue 0017 WITHDRAWN** (one commit, unpushed branch, text preserved at `scratchpad/withdrawn-0017.md` -- the incident write-up in it is worth reusing here). The carry itself is untouched and added **zero git and zero `intent` invocations to devbin's core**; the two files that call `intent` were already calling it upstream before I touched them.
   - **THE STAMP CANNOT GO IN THE SEAL, and this is the correction vc accepted.** `.errors` is empty-exactly-when-a-completed-run-was-green (`write_errors_file` truncates at rc=0, and the file states the consequence itself twenty lines later), so writing a sha into it makes every green run read as red. vc had filed "stamp it into the log" and it would have inverted the one invariant the subsystem exists to hold. **It belongs where it is READ -- at the point of report -- because hv read `FAILED: rust` in a summary, and information buried in a log you only open once you already distrust the verdict arrives after it was needed.**
   - **vc's precedent, which I did not have and which removes the strongest objection: `print_run_verdict`'s `verdict:` line already fires whenever `keep=1`, independent of rc.** So printing `measured:` on green is an addition to an already-unconditional block, not new behaviour on the green path. **And printing on green is the point** -- vc's closing observation, buried at the bottom of their message and the more important half: that run produced four stale GREEN legs nobody has any reason to question. A red gets investigated and self-corrects; a green certifying a tree nobody has is believed and never revisited.
   - **ANNOTATE, NEVER SUPPRESS -- held, and cc gave it the sharpest defence.** Two of the five tests cc had to change were asserting behaviour hv's ruling retires, and `close_gate_parity` was the clearest statement in the estate of exactly the wrong semantics. **The red was noise about WHICH TREE and signal about WHAT.** A guard that had downgraded it would have lost the second.
   - **`LATEST_*` ARE ALREADY RELATIVE SYMLINKS** (`runlog:146,148`), so ic's "simpler and better" option was already the case -- measured rather than redesigned. **The residual is that the pointer answers WHICH run, never WHOSE, and it cannot be made to answer ownership without breaking empty-means-green.** So ownership is answered by the command PRINTING the path as it writes it, which `run_gate` already does, and the design says so out loud instead of leaving it an assumption. Cheaper than a `.RUN.meta` sibling and adds no artefact.
   - **The root cause it addresses is NOT the one it was first attributed to, and I measured that after landing the fix.** `int test shell` against a deliberately failing suite returns rc=1 AND seals a consistent non-empty `.errors` -- the two sources agree, so ic's control C describes a state this gate does not produce. C still covers a class and E is reachable from any killed run. **The incident's mechanism is a human aggregating two legs, twice, with no artefact stating the verdict of a run.** CI checked and clean on the same class, by reading the invocation rather than the comment above it.
1. **WP-11 (Distribution) -- MINE, WIP. THE PACKAGING HOLD IS DONE; everything remaining here is blocked on a publication, not on buildable work.**
   - **BUILT AND PROVEN: `int macos <doctor|stage|sign|notarize|verify|checksum|prepare|formula|publish|env|store-creds>`.** Signing acts on STAGED copies in `target/dist`; `prepare` runs the pass. `publish` uploads, **re-downloads what it uploaded and hashes THAT**, and only then ships the formula; on a mismatch the release stays and no formula ships. Four refusals canaried both ways.
   - **UNEXERCISED SURFACE IS ONE CALL, `gh release create`.** Closed since: formula is valid Ruby and **lints clean at a tap path**; `curl -fsSL` follows redirects, hashes identically twice, 404s without writing; **tap clone, `Formula/intent.rb` write and commit all succeed against the REAL tap, nothing pushed.** No release here has ever carried an asset, so the upload cannot be rehearsed without publishing.
   - **AC-11.1 + AC-11.4 ARE BLOCKED ON A PUBLICATION, AND THE BLOCKER HAS NOW BEEN WRONG ON THIS BOARD THREE TIMES.** Not the tap (live since 2026-08-15T15:19:58Z, deliberately empty, one README saying why -- verified again 19:2xZ, `isEmpty:false` means "has a commit", NOT "has a formula"); not "a real version and nothing else" (necessary, not sufficient); and no longer 0038 (cc fixed it at `d2b8e76d`). **The live set is: 0036, 0043, the `## [3.0.0]` CHANGELOG section, and hv's freeze call.** The mechanism existing is not the criterion -- **a better-built mechanism is no more a satisfied AC than a built one was.**
   - **HELD until WP-12: `int build release` gaining `Cargo.toml` to its sidecar sync** (wiring it now makes a v2 release stamp its version into `Cargo.toml`). **`brew services` BLOCKED on WP-08.** ~~Install/upgrade docs still owed~~ **-- WRITTEN, at `intent/st/ST0056/install.md`, and it leads with the brew-SHADOWS warning. This TODO was stale and I only found that by going to write the thing.**
1. **Issue 0036 -- `brew install` SHADOWS a v2 install.** brew is PATH position 1, the v2 symlinks are 17 and 19. A v2 user meets the v3 unmigrated-project refusal without asking for anything, and its remedy names `intent upgrade`, which v3 lacks. `migration.md:3` says the migrator IS that verb, so it is WP-10 unbuilt. **vc ruled the control belongs on the BINARY as a build-time invariant, not in `publish`** -- so it is theirs, and WP-11 stays decoupled from WP-10.
1. **MEASURED CLEAN, do not re-investigate: `int prepush` 19s; warm `cargo test --workspace` 22s for 331 tests.** Both were my guesses at the friction and both were wrong. **ic asked me specifically NOT to optimise `int build cli` (25-37s) -- that cost buys correctness**, after a 14-minute-stale binary reported findings cc had already fixed and read as a regression.
1. **`testkit::project_fixture()` -- DEFERRED, AND NOT FOR THE REASON I QUEUED IT.** vc's urgent justification was that no fixture carries a non-default `st_prefix`. `retired_st_prefix.rs` now does -- but it is **UNTRACKED**, a peer's in-flight work, so it is not evidence about the repo and I nearly reported it as one. What remains is a Highlander tidy-up over 12 test files in two crates, and the Rust tree has ten files under active edit tonight. **Wrong time, low urgency, high conflict; hand it to cc rather than reaching into their tree.**
1. **Issues 0030 / 0031 (`intent upgrade`) -- DEFERRED under DEFAULT-DEFER.** 0030: backup dirs stamped in LOCAL time, so oldest-first retention deletes the newer artefact across a DST fall-back. 0031: `--backup-dir` basenames straight into `.backup/`.
1. Open for others: **`intent/.cache/`** (cc, D21); **`core.hooksPath`** (deferred, collides with `intent claude upgrade`); the **`bin/` boundary** (hv); ~~AC-12.1's wording~~ **RULED 2026-08-15: vc adopted my rewording essentially verbatim -- _"nothing in the repo EXECUTES or EMITS a `bin/` intent script path"_ -- with the four classes named in the row and the 133 historical records marked explicitly OUT OF SCOPE.** The parity-fixture question is recorded as a WP-12 call rather than answered.

---

# archived at fold, 2026-08-17 18:50Z

## DOING

- **THE GATE'S BLOCKER IS GONE, MEASURED AND NOT READ OFF A SUBJECT LINE.** cc wired the dispatch arm at `8770cea3`; I built `e4634500` from a `git archive` extract with a private target and ran it from `/tmp`: `error: no Intent project found at or above /private/tmp`, **exit 1** -- not `not implemented yet` at exit 2. **So `intent upgrade` is reachable AT A COMMIT for the first time, and "run the gate" and "run the gate against uncommitted code" have stopped being the same sentence.** ic runs it. **I am still holding "strong evidence, gate not passed" until that run happens** -- it is now resolvable by a measurement rather than by argument, which is the whole point of having held it.

- **AC-11.5's BINARY ARM IS BUILT AND CANARIED 3/3, AND IT IS HELD OUT OF THE TREE ON PURPOSE.** `build.rs` embeds the source commit into `intent`; proven in a sacrificial `git init` repo, **read back out of the binary with `strings` rather than out of the build log** -- a build script that printed the right thing and a binary that carries it are different claims. clean names the bare sha / dirty names `dirty-<sha>` / no `.git` names `unknown`. **Both patches sit in the scratchpad (`build.rs.pending`, `binary_arm.sh.pending`) and land AFTER ic's gate run**, because ic's rig refuses on dirty parity tools and my WP-11 arm is not on the estate's critical path while the gate is.
  - **NO BUILD TIME, and D42 is only half the reason.** cc's sharpening: a build stamp can only agree with the commit's own time or contradict it, and there is no version of the second that is useful.
  - **NO `rerun-if-changed` ON `.git/HEAD` (cc's argument, taken).** In a clone four sessions commit into, that trigger means "a peer committed", not "the code changed" -- and `intent-cli`'s test legs spawn `CARGO_BIN_EXE_intent`, so they would relink on every whiteboard fold. **The trigger prevents exactly the state the fail-closed check already refuses.** Emitting NO line is not "no trigger": it restores cargo's default of re-running when any file in the PACKAGE changes, which is the trigger that tracks the code.
  - **IT REPORTS AND DOES NOT GATE, and the precedent is `ratified_in_check.sh`.** `self_provenance_check.sh` is gated in `int precommit`, so an arm failing on a binary lacking the marker would block every commit until every stale artefact in every `target/` was rebuilt -- and `target/` is shared mutable state. **A guard that must be bypassed is a guard nobody keeps.** The refusal that bites goes where the harm is: `publish`.
  - **HELD, NOT FORGOTTEN: the blocked-migration remedy.** ic's finding -- a BLOCKED migration writes nothing, so my unchanged-input arm refuses correctly and then prints _check that the migration door is wired_, **which is wrong advice for a correctly-blocking estate.** I cannot discriminate them (the tool sees trees, never exit codes), so the fix is to name BOTH causes and assert neither. **vc's exact catch from this morning, in a different clause of the same file, four hours later.**

- **THE GATE HAD A FALSE GREEN IN IT AND IT IS CLOSED: `24b8a59b`, pushed.** `same_end_state_check.sh` now takes THREE subjects -- `<input-estate> <clean-run-tree> <rerun-tree>` -- and refuses (exit 2) when the clean run left the input byte-identical. **Reachable the day it landed and nobody had run it yet.** Measured at `f5b5918c`: `intent upgrade` advertised in `--help`, `error: 'upgrade' is a known command that is not implemented yet`, exit 2. Both of ic's arms would have written nothing, both outputs would have been the pristine input, verdict IDENTICAL exit 0. **A green from a door that never opened -- ic's unfired-fixture trap one level up, not an early kill but a migrator that never ran.**
  - **THE CLASS, AND IT IS NEW AND SHARP: A REFUSAL DERIVED FROM A GENERAL ARGUMENT THAT THEN COVERS ONLY THE INSTANCE THAT PROMPTED IT.** The file already contained the whole reasoning -- _an empty tree and a matching tree compare equal, so an empty subject REFUSES rather than passing_, and explicitly _the ways this tool can be silently useless both look like success_. **I wrote the general form, closed the EMPTY case, and left the UNCHANGED case open**, and UNCHANGED is the reachable one, because two unmigrated trees are not empty -- they are full and equal. **The prose was right and the coverage was one case wide.** This is not "think harder about the argument"; the mechanism is to enumerate the ways the subject can be degenerate and close each, because the argument itself does not enumerate them.
  - **DERIVED, NOT NAMED, THROUGHOUT.** The cheap version tests for `thread.json` or `config.json`'s version stamp -- cc offered both as measured discriminators and they are good ones. Refused anyway: a needle keyed to today's canon stops discriminating silently the day it is renamed, which is the allowlist lesson wearing a different hat and the exact thing ic caught in my roster row. **Asking "did the migration change ANYTHING" needs no knowledge of what canon looks like** and reuses the comparison already in the file, which is also the Highlander half -- one `tree_delta`, two call sites, rather than a second inline copy of the same question.
  - **cc's 311 IS WHAT MAKES THE COUNT USEFUL.** The tool prints `the clean run altered N file(s)` on every run, pass or fail, as its own positive control. On the pinned canary that number is 311 (56 threads, 0 issues), so **0 is now a reading with a known meaning rather than a blank.** A verdict with no measurement behind it cannot be told apart from a verdict about nothing.

- **THREE PEERS EACH CLOSED A DOOR I HAD LEFT OPEN, INSIDE ONE HOUR, ALL BEFORE IT LANDED RATHER THAN AFTER.** Worth keeping as a group, because the common factor is that I told all three what I was about to do while it was still cheap to be wrong.
  - **vc -- THE MIGRATION RUNS IN PLACE, so `<input-estate>` must be a copy taken BEFORE the run, not the path handed to the migrator.** A rig passing the migrated path as its own input makes the unchanged-arm compare a tree with itself, and it then **refuses CORRECTLY FOR THE WRONG REASON**: the message blames the migrator and the defect is in the rig. **My own first canary fixture did exactly this** -- it passed, at the right exit code, with the right message, and was proving the wrong branch. Two subjects resolving to one directory now refuse with their own message. Same axis as naming ACCRETION apart from non-determinism, applied to a case I had built and not noticed.
  - **cc -- A COMPARISON THAT SILENTLY OMITS A FILE READS EXACTLY LIKE ONE THAT COVERED EVERYTHING.** Their own `find | xargs shasum` split on a filename containing spaces and reported IDENTICAL over 1360 of 1361; the skip was symmetric so the verdict survived **by luck, not by method**. My `tree_delta` compares line by line, so a literal newline in a path would have split an entry and miscompared in silence -- it now REFUSES. Spaces and non-ASCII are handled and canaried against a real curly-quote path, since vc measured two in Lamplight.
  - **ic -- `estate_corpus.sh capture` STAMPS `captured_at`, so two captures of ONE pinned revision differ in a file no migration touches** -- and "the input estate" naturally reads as a fresh capture, so my third argument invited the trap. **Caught mechanically rather than warned about**: ARM 1 already measures which paths the migration touched, so a path where the two runs disagree and the migration NEVER WROTE is the harness by construction. Prints `NOT THE MIGRATOR: N of the differing path(s) were never touched by the clean run`. **A red that sends someone hunting in the wrong repository costs more than a red that says nothing.** ic had already put capture-once into `interrupt_rig.sh` before warning me about it, and had already wired the three-subject call at line 363 -- I verified by reading their file rather than asking.

- **HEAD BUILDS, AND THE ESTATE-WIDE BELIEF THAT IT DID NOT WAS COSTING PEOPLE RUNS.** `git archive HEAD | tar -x`, private `CARGO_TARGET_DIR`, `cargo check --workspace --all-targets` -> clean, 37.84s at `f5b5918c`; vc independently ran `cargo build --workspace` at 44s. **`stamp_version` was never at HEAD -- it was inside cc's uncommitted +201 lines**, so "blocked on a compile error in cc's in-flight `facade.rs`" was true of the SHARED WORKTREE and false of the commit. **vc's sentence is the keeper: a red produced by uncommitted state is a red nobody else can reproduce.** Three boards carried it, ic and vc each lost a gate run to it.

- **STRONG EVIDENCE, GATE NOT PASSED -- unchanged, and the reason has moved.** It is no longer a compile error. `Facade::upgrade` landed (`d4648020`) but **the CLI dispatch arm is still uncommitted** (`render.rs` dirty, `tests/upgrade_command.rs` untracked). ic built from the worktree and ran the real binary against vc's pinned `42fb5269`: exit 0, 56 `thread.json` written. **So the door is fully working in the worktree and unwired at every commit**, and today "run the gate" and "run the gate against uncommitted code" are the same sentence. cc's plain-re-run idempotence is real and separately measured -- 1361 files byte-identical over two re-runs, run 2 reporting `already migrated: 56 thread(s) read from committed canon` -- **and that is the plain-re-run arm, NOT the interruption arm, which is hv's actual release condition.** Two different claims; only one is the gate.

- (superseded, kept for the ladder) **THE CUTOVER GATE IS LANDED AND HAS NOT BEEN RUN. Both halves matter and the second is the one to hold.** `same_end_state_check.sh` (`66ba461d`), rostered `manual`, MODULES row in, four canaries green (identical 0 / accretion 1 / missing 2 / empty 2). **hv authorised it as the cutover's release condition: no cutover until a re-run over an INTERRUPTED estate reaches the same end state as a clean one.**
  - **ic HAS RUN THE PROTOCOL AND EVERY ARM CAME BACK IDENTICAL -- WITH THEIR OWN THROWAWAY DIFF, NOT THIS TOOL, AND THEY FLAGGED IT THEMSELVES BEFORE ANYONE COULD QUOTE IT.** Real `SIGKILL` on a child mid-`WriteSet::commit`, confirmed by `ExitStatus::signal() == Some(9)`: clean 93 files/144-tree; plain re-runs 2 and 3 IDENTICAL; kills early/mid/late (123/132/142 files at kill) all IDENTICAL; and a rebuilt fixture with 5 of 5 threads in `COMPLETED/` IDENTICAL. **So: strong evidence, gate NOT passed.** I have told hv exactly that and will not report it otherwise.
  - **THE RE-RUN SHOULD USE vc's PINNED POPULATIONS, NOT A HAND-BUILT ESTATE.** ic's scope, stated by them first: 5 and 10 threads, one repo, all COMPLETED -- not the 56-thread canary (`42fb5269`) and not the fleet (`8e8f0611`, 16,380 prose sections, 67 LIVE threads across 436). Those exist and are captured byte-exact.
  - **ic's SENTINEL-VERSUS-DELAY MECHANISM, worth more than the result:** killing on a guessed delay never worked -- 0 files written even at 12.8ms, because harness startup outlasts every delay -- and killing on a sentinel file lands it every time. **A delay is a guess about someone else's timing; a sentinel is a fact about their progress.** And the failure direction is the dangerous one: **a kill landing too early writes nothing, so the re-run trivially matches and the arm reports IDENTICAL without having interrupted anything.** A green from an unfired fixture.
  - **DO NOT RE-FILE THE DOUBLING.** ic measured that a clean migration leaves 5 thread dirs at the flat path AND 5 in `COMPLETED/`, migrator content. That is vc's **DOUBLED** class, already named in `conservation_check.sh` as manufactured by the migration on 55 of 56 threads. ic's is the confirming measurement from the migration end, not a second finding.

---

## fold 2026-08-17 20:34Z -- AC-11.5 built on both binaries, and the pipeline gained its first artefact-subject check

Landed this session: `b11ca6ac` (the binary arm, three pieces not two), `a13bd338` (it shipped DEAD -- arm 1 no-manifest `exit 0` made it unreachable in every project without a vendored devbin), `0566da2c` (every pass now says what it is silent about), `ce78deac` (AT-11.5 named an EVENT while its criterion names a STATE), `4c839def` (both binaries embed; `artefact_commit_blockers` at publish), `ee9956fa` (the three-artefact asymmetry, and a comment stale within the hour), plus board and hv entries.

- **THE GATE ARM PASSED. Exit 0 against `252f9ed2`, run by ic, real SIGKILL at 293 of 295 writes with exit 137 asserted.** `IDENTICAL IN FILES -- 1371 file(s) match; 1 store(s) NOT judged here`. **Verified independently of both our tools** with `diff -r` over the kept trees: `intent/st/**` identical, `config.json` stamped `3.0.0-dev` in both arms, 56 `thread.json` in both. **The honest statement, and it is readable off the tool's own output rather than depending on anyone to append it: the gate passes ON FILES, with the store explicitly out of scope pending the typed test in `intentsvcs`** (ic's, with cc).
  - **The scope is in the VERDICT LINE and not in a footnote, which is the whole design.** `f48ed6e9` is this estate's own case: a commit subject claiming a gate measured passing over a body that scrupulously said otherwise. **A caveat in the body does not travel.**
  - **`same_end_state_check.sh` took four landings today and every one closed a way it could be silently useless**: three subjects so an unmigrated pair cannot report IDENTICAL (`24b8a59b`); `cmp` over two `shasum` runs so an unreadable file cannot compare equal (`11f66894`); the store reported UNJUDGED after I measured my own `sqlite3` dependency giving OPPOSITE VERDICTS ON TWO MACHINES IN SILENCE (`252f9ed2`); and a false `measured:` claim in its own header corrected rather than replaced.

- **LANDED: AC-11.5's BINARY ARM, `b11ca6ac`.** `build.rs` embeds the source commit into `intent`; arm 2 of `self_provenance_check.sh` reads it back out of the ARTEFACT. **Measured cold: `[intent-source-commit:dirty-b11ca6ac74d5...]` via `strings`, sha matching HEAD exactly, brackets terminating cleanly with no neighbouring rodata literal captured.** `dirty-` is correct and will be the normal state here -- three peer files were uncommitted, which is what a five-session clone looks like.
  - **IT WAS THREE FILES AND MY BOARD SAID TWO, AND THAT GAP IS THE WHOLE LESSON.** `build.rs` only sets an env var; a third piece has to `env!()` it into the binary or nothing is embedded. **Landing the two I was holding would have produced a build script nobody reads beside a check that always reports NO MARKER** -- each half of a two-ended change, which is the precise failure the "land as ONE commit" rule was written to prevent. **The rule was right and its SUBJECT was undercounted**, so obeying it literally would have caused the thing it forbids. Same class as everything else this week: the thing I had was narrower than the thing I said, and the population is where it hides.
  - **THE ARM FIRED ON ITS OWN LANDING COMMIT, WHICH IS THE ONLY REASON I BELIEVE IT WORKS.** `int precommit` ran it against the real stale `target/release/intent` and reported `carries NO source-commit marker -- Rebuild it; this binary predates the embed` at exit 0. **A fixture would have tested it against my model of the failure; that tested it against the world** (vc's framing, and it keeps being the right one).
  - **A RISK THE CANARY NEVER COVERED, CLOSED BEFORE I TOUCHED cc's CRATE: `clippy -D warnings` is gated here, and a `#[used]` static that nothing reads is the exact shape `dead_code` fires on.** Ran `cargo clippy --all-targets -- -D warnings` against the sacrificial tree carrying the same marker first: exit 0. **The canary proved the mechanism and was silent about the estate's own gates** -- a green canary is evidence about the subject, not about the tree it is going into.

- **THE PIPELINE NOW HAS ITS FIRST CHECK WHOSE SUBJECT IS THE ARTEFACT (`4c839def`), AND THE GAP WAS STRUCTURAL RATHER THAN AN OVERSIGHT IN ANY ONE CHECK.** `stage` writes `commit: $(git rev-parse HEAD)`; `publish` refused unless `traceable: yes` and `prov_commit == tag_commit`. **EVERY ONE OF THOSE IS A CLAIM ABOUT THE CHECKOUT, and nothing compared the BINARIES to anything** -- so a clean checkout, at the tag, with `target/release` holding bytes built forty-two hours earlier satisfied every gate that existed. **No individual check was wrong, which is why nobody saw it and why the remedy could never have been to check harder.** cc's sharpening, taken into the code and MODULES.md: **the number of controls says nothing; the number of distinct SUBJECTS does**, and here the count was one while the appearance was four. Both binaries embed now (`intentd` had none and was the forty-two-hour artefact); `artefact_commit_blockers` compares each staged binary's own marker against the TAG, driven 7/7 with the definitions EXTRACTED from the shipped file at test time so the canary cannot drift from what runs.
  - **ONE HOME FOR THE EMBED, and the rejected alternative is recorded with its real reason.** `native/rust/build-support/source_commit.rs`, `include!`d by three-line wrappers, because two byte-identical build scripts drift **silently -- a provenance marker's failure mode is being WRONG rather than absent.** Rejected: a single `build.rs` in `intentsvcs`, which both binaries already depend on. **Not because cc is working there** -- because `#[used]` stops the COMPILER dropping a static and whether the LINKER pulls an unreferenced object out of a dependency's rlib into a downstream binary is a different question nobody here has answered. **I was not going to bet the artefact on an untested linker behaviour**, and that is the only honest reason to keep two files.
  - **TWO DEFECTS THE BUILD CAUGHT THAT READING WOULD NOT HAVE, both in code I had just written and re-read.** `include!` of a file whose comments are `//!` fails with 53 errors -- **inner doc comments are valid only at the top of a FILE and an included file is not one** -- I had written the shared file as a module without noticing it would never be one. And `dead_code` fired on `intentd`'s `SOURCE_COMMIT` const under `-D warnings`, correctly: **nothing in a bin crate can ever read it.** Dropped the const rather than reaching for `#[allow(dead_code)]`, because silencing it keeps a declaration whose only purpose is to look symmetrical with a crate whose shape is different.

- **cc's COLD/WARM DISTINCTION IS REAL AND MY FIRST WARM ARM WAS INCONCLUSIVE.** Their point: **a build script that fails to RE-RUN is invisible on the cold pass, because a cold pass has nothing cached to fail to invalidate.** I rebuilt after touching a package file, read the same marker back, and that proves NOTHING -- **HEAD had not moved, so the correct value was unchanged either way and the arm cannot tell `re-ran` from `never ran`.** My own fixture-never-reached-the-branch class, in the direction that reads as confirmation. The real test needs HEAD to MOVE, and it has two cases that genuinely differ: a HEAD move that touches the intent-cli PACKAGE (cargo's default re-runs the script, marker tracks) and one that does not (a peer's `intentsvcs` commit, a whiteboard fold -- the script does not re-run and the binary keeps the old sha). **The second is the designed-for staleness that the consumer refuses on, and it must be MEASURED rather than assumed, because it is the actual cost of dropping the `.git/HEAD` trigger.**
  - **NO BUILD TIME (D42, and cc's better half: a build stamp can only agree with the commit's own time or contradict it).** **NO `rerun-if-changed` on `.git/HEAD`** (cc): in a four-session clone that trigger means "a peer committed", not "the code changed"; emitting NO line restores cargo's default of re-running when any PACKAGE file changes, which is the trigger that tracks the code. **REPORTS, does not gate** -- `self_provenance_check.sh` is gated in `int precommit`, so an arm reddening on a stale `target/` binary would block every commit in the clone; precedent is `ratified_in_check.sh`, and the biting refusal goes at `publish`, where the harm is.
  - **A PROVENANCE MARKER MUST BE SELF-DELIMITING.** Rodata packs literals with no separator, so the first version read `intent-source-commit:<sha>unsafe`. **The fix belongs in the artefact, not the consumer's grep** -- hardening one grep moves the trap to the next consumer. `#[used]`, because an unreferenced const is free for the linker to drop under `--release`, the one build it exists for.

1. **THE GATE, RUN UNDER THE ROSTERED TOOL. Still not mine to run, and the blocker has MOVED -- re-derive it, do not re-read this.** It is no longer a compile error: **HEAD builds** (mine 37.84s, vc's 44s, independently). It is that **cc's CLI dispatch arm is uncommitted**, so the door works in the worktree and is unwired at every commit. ic's rig now **refuses on a dirty `native/rust/` OR dirty parity tools**, which is the only thing preventing a green that would look exactly like the real one -- **their discipline, load-bearing beyond its design, and worth saying to hv in those terms.**
   - **ic's REFRAME OF WHAT ARM B IS FOR, and it is the best thing anyone has said about this gate: a working rollback makes the half-written estate UNOBSERVABLE BY CONSTRUCTION, and SIGKILL is the only instrument that can produce it, because SIGKILL cannot be caught so no unwind runs.** cc traced the sequence -- `commit()` lands the estate, `Store::open()` refuses, `rollback()` unwinds -- on a real 56-thread estate, by accident, which is how the rollback got its first live test. **So arm B is not "interrupt the migration"; it is "produce the state the rollback would have erased, and prove a re-run still lands it".** Corollary to hold: **the better cc's rollback gets, the more the rig is the only witness, and the more it will look like there was nothing to test.** A gate whose subject vanishes as the code improves is the kind that gets quietly dropped.
   - **SUPERSEDED (kept so the ladder reads): "blocked on a compiling crate, `error[E0425]: cannot find function stamp_version` at `facade.rs:627`".** That was true of the SHARED WORKTREE and false of the commit, and it sat on three boards while ic and vc each lost a run to it. **Everything else is ready and survives the wait**: ic has two pristine copies of the FULL canary (`git archive HEAD intent/`, 1063 files, 58 thread dirs) staged, plus a harness that produces A clean and B killed-then-rerun.
   - **ic TOOK THE SENTINEL POINT INTO THE HARNESS AND SHARPENED IT: the sentinel is a LATE write (`ST0030/acceptance.md`, a view), not the first canon file, so the kill lands deep.** An early kill writes nothing and the arm reports IDENTICAL without having interrupted anything -- the unfired-fixture green wearing the face of success. The harness also asserts `signal() == Some(9)` and fails if the child was not actually SIGKILLed, for the same reason.
   - `intent upgrade` wiring is cc's next item, ahead of the issues parser, precisely because the gate releases the cutover.

1. **AT-11.5 IS RE-WORDED AND RATIFIED, AND I GOT THE CONDITION WRONG TWICE -- SAME CLASS BOTH TIMES.** vc ruled the original condition named an EVENT ("moves when the binary arm lands") while AC-11.5 names a STATE; the event happened and nothing the criterion names improved. **I then re-worded it to "moves when `stage` REFUSES on a sha mismatch" -- which names a MECHANISM AND A LOCATION, and that is a state no more than an event is.** Corrected event-to-state and immediately substituted location-and-mechanism. **vc ratified my wrong version too, and named that as the finding worth more than the row: a contract holder who rules on a class and then fails to apply it to the very next sentence has ruled on an INSTANCE, not on a class.** Ratified now: _moves when a publish cannot attach bytes to a tag they did not come from -- established by reading each artefact's OWN embedded source commit and comparing it against the tag, for every binary published._
   - **AND THE LOCATION WAS WRONG ON THE FILE'S OWN TERMS, WHICH READING THE CODE FOUND AND NEITHER OF US DID.** `cmd_stage:589` already ruled the control goes where the HARM is. **In this five-session clone the tree is permanently dirty, so every binary reports `dirty-<sha>` and a stage-time mismatch refusal would have refused EVERY dev stage** -- the guard-that-gets-worked-around, which this estate has already paid for once. **A condition vc ratified would have required exactly the guard an existing ruling forbids.**
   - **THE HABIT THAT DID NOT PROTECT ME, STATED BECAUSE IT WAS PRAISED AN HOUR EARLIER: DECLARING AN INTEREST IS A DISCLOSURE, NOT A CONTROL.** I flagged that I was not neutral on AT-11.5, correctly. **The very next thing I wrote was a condition convenient in a different way -- it named work I had nearly finished rather than harm still standing** -- and the disclosure did nothing to stop it. vc's form: the thing that caught it was a check whose subject was the CODE rather than the AUTHOR.
   - **STILL OPEN and it is the row's actual remaining work:** `stage` copies from `target/release` and nothing builds; the refusal now exists at `publish` and has never run in a real release.

1. **SUPERSEDED, kept for the ladder: WP-11 -- MINE, WIP, GATE BLOCKED 2/4 (AC-11.1 + AC-11.4), BOTH NEEDING A PUBLICATION AND NOTHING ELSE.** Live gates: **0036**, the **`## [3.0.0]` CHANGELOG section**, and hv's freeze call. **Re-derive from `install.md` and `intent ac gate`, never from this board** -- the blocker set has been stale here three times.
   - **UNMEASURED AND MINE: `stage` COPIES from `target/release` and nothing proves those bytes were built from the commit it records.** `cmd_prepare` is stage/sign/notarize/checksum with no build step. **AC-11.4's fix compares provenance to the TAG and never asks whether the binaries were BUILT from it**, so a clean tree is silent about staleness. Consistency-is-not-correspondence, one level in.
