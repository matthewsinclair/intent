# dc fold 20260910 -- DOING narrative and struck TODO items

Archived by the localfold at 2026-09-10 06:48Z. The live board keeps only what is unfinished; this is the verbatim record of what was delivered and what died, so nobody re-derives either.

## DOING as it stood at the fold

**TONIGHT, 2026-09-09, UNDER vc's PEN WHILE hv AND THE HUMAN ARE AFK. THREE ROWS DROVE, TWO DELIVERED, AND THE ROW I WAS ROUTED DIED ON CONTACT FOR THE SECOND TIME.**

**`AT-00.20`'s INSTRUMENT FIXED (staged, blocked on a canon deadlock).** It printed `322 of 328`, `98%` and a named `ST0056/AC-00.7` as literal prose in its verdict while its own partition computed `337 of 343`. Cause is structural, not careless -- a QUOTED heredoc cannot interpolate, so the figures were literals by construction ([[W133]]). All three now print from the same variables the partition uses. Driven: rc=1 preserved, `337 of 343`, and the disagreeing claim printed from `some_list` with an explicit branch for when the sharp form is empty.

**`AC-00.16` IS NOT ENGINEERING AND HAS NOT BEEN SINCE 2026-08-31.** Its stated blocker -- _the union computed PER PROPERTY_ -- was discharged at `bbcae7593`, nine days before vc routed the row to me on it and before I recorded it as the remainder. **Side C is built.** What keeps it red is that AC-00.16's first conjunct is not machine-decidable as written; the remedy is a structural REACH block, which is a **CRITERION AMENDMENT and hv's**. The instrument refuses to write it -- the author of a check is not a safe source for that check's own denominator -- and I hold that refusal. Routed to hv via vc; vc is honouring it rather than overriding it.

**`AC-00.1`'s DESIGN HALF ANSWERED AND BUILT (`bin/.devbin/cmd/local`).** The currency verdict existed in three places and **none was a route a person types**; `int local status` -- the verb people type to ask what am I running -- printed the marker and HEAD as two hashes and stopped. **`cmd/local` already sourced `currency.lib` and `sharedtarget.lib` already declared the scopes**, so the verdict was one call away since the file was written: `AT-11.5`'s _a capability with no consumer distinguishes nothing_, third instance. It REPORTS and does not refuse -- `status` is read-only and rc stays 0 -- and that was checked against `build.d/release`'s ruling that _a fallback that warns is a fallback that gets ignored_, which is about falling back to a second-choice binary and does not reach this. All four render branches controlled on planted verdicts, unknown form included.

**`AC-12.1` CLASS (2) MEASURED AT HEAD, AND IT HAS A MEMBER THE ROW DOES NOT NAME.** Edge A: **one** real hit, `intent/plugins/agents/bin/intent_agents:14`. The row scopes class (2) to the **claude** family; this is the **agents** family, in the same position -- only executor is `bin/intent:137`, and `intent agents` already answers rc=0 from Rust. **One more file goes with the trunk and nobody had counted it.** Edge B: **empty**, seven candidates all false positives (awk regexes, grep pattern strings, a `case` label), control fired. `intent/plugins/claude/bin/` holds two files and neither sources anything. `tests/` partitioned separately as MEMBERSHIP UNDECIDED pending hv's bats-suite ruling -- its apparent hits are same-line `bash -c "source ...; sym"`, which is Edge A.

**`AC-00.3` HAS MOVED FROM FIVE UNWIRED VERBS TO ONE AND NOTHING RECORDED IT.** Driven via `canon_mandated_verbs_check.sh` rather than the row's hand-list, which is the row's own recorded defect. 44 canon-named verbs extracted, 14 skipped as writes, **one unwired: `intent claude rules validate`** -- and checked against this row's own stale-binary trap: `render.rs:9660`'s `validate` arm is inside `fn agents`, a different family, and the release pair and a near-HEAD debug build both answer rc=2. **Genuine gap, one verb from green, and cc sized it at `ec1f41aa4`.**

**THE PAIR IS GENUINELY STALE AND THE UPPER BOUND IS NOT WHAT SAYS SO.** `git rev-list` gave 17 over a narrower scope and proves nothing. **`self_provenance_check.sh`'s currency arm REFUSES: 15 non-test files under `native/rust`, `surface`, `docs/design`, `lib/templates` since `b070c158fbde`.** That arm decides. vc takes the rebuild once the commit lands.

**SECOND HALF OF THE NIGHT, UNDER vc's DIRECTION.** `0270` amended to a CLASS with THREE members from three surfaces -- `at` has no way back to `to-write`, `st attach` has no `detach`, `issues edit --body` replaces prose with no confirmation and no inverse. **That moves it from a bug list to a design finding: this CLI does not do inverses**, and both recoveries tonight came from outside the tool. Recorded not built, on vc's scope ruling; option 1 is correspondingly larger and better worth buying post-tag. `0216` gained a sighting on `issues edit --from` plus an ELIMINATION rather than a mechanism: non-atomic rewrite is the tidiest explanation for a transient `canon=0` and it did not reproduce when measured (248 -> 7144 bytes in one step across 25 samples).

**`docs/known-defects.md` DRIVEN IN FULL -- ALL FIVE FORWARD CLAIMS HOLD, ONE REMEDY CORRECTED, TWO REPORTS OF MINE WITHDRAWN.** The five _a build after v3.0.0_ claims were promises when written and are now verified with controls, in an isolated scratch project against a debug build at HEAD. `#0224`'s _nothing regenerates it unless you re-run `init`_ is genuinely wrong on BOTH binaries -- `init` refuses on an initialised project -- and is corrected in the page. **AND THREE REMEDIES ON THAT PAGE NAME ACTIONS THE TOOL REFUSES** (`0224`, `0063`, `0227`): a class, invisible to a check that reads whether a defect is described. `0299` filed -- `0267`'s fix landed and its diagnostic did not; the cross-thread refusal is byte-identical to the no-id refusal and its sentence is FALSE for that case.

**I REPORTED TWO PAGE CLAUSES AS REFUTED AND BOTH WERE CORRECT, AND vc AUTHORISED EDITS ON MY EVIDENCE BEFORE I CAUGHT IT.** `#0267`'s _though not to the tool_ and `#0119`'s _treat `ac new` as create-only_ are v3.0.0 statements sitting ONE SENTENCE before their own expiry notices, in a page whose first bold line says everything on it was run against v3.0.0 and not against `main`. **I drove a v3.0.0 claim against HEAD and called the page wrong.** Editing them would not have added an error -- **it would have destroyed the page's contract.** Refused to act on the authorisation once its basis was gone; vc withdrew it. **AN INSTRUCTION IS NOT A WARRANT: it inherits the evidence it was given for.**

**AND THE NIGHT'S SHARPEST SELF-INFLICTED FINDING IS [[W135]]:** three independent defects in one verifier, all three pointing at a data-loss event that never happened, one message from a false `0216` sighting.

**`0276` DRIVEN TO A VERDICT AND ITS AMENDMENT CANNOT LAND THROUGH THE VERB, SO IT LIVES HERE.** I went to refute the issue and confirmed it. **FIXTURE, because the outcome turns entirely on it:** an attachment **committed, clean and diverged** -- `st attach` wrote BYTES-A into canon, the disk file was changed and COMMITTED, `git status` empty, canon and disk disagreeing. **`sync --to-store` returns exit 0, prints `note: no thread the store already holds differs on disk, so this restore overwrites nothing`, and canon afterwards holds the DISK bytes.** The message does not merely omit the ingestion, **it asserts the opposite of what the run did.** **BOTH BINARIES, two independent fixtures** -- release pair at `b070c158` and debug at HEAD -- identical behaviour and identical message, so not a post-tag change and not a stale pair. **NEGATIVE CONTROL: canon does not move on its own** (sampled every 3s for 15s with no sync; nothing ambient ingests -- and three of us wrongly attributed this exact mechanism to a daemon tonight, which is why the control is there). **POSITIVE CONTROL: the same divergence left UNCOMMITTED warns in full, and its warning says `this run takes them into canon`** -- so the estate already owns the right sentence and it is attached to the narrower case; without this control "silent" would be indistinguishable from my reader being blind, and the committed run emitted ZERO warning lines against a reader that had just emitted two. **[[W126]] is corrected above rather than deleted.**

**`ST0058 AC-00.3` IS SATISFIABLE ON THE EVIDENCE AND BLOCKED ON THE STORE, NOT ON THE ROW.** `canon_mandated_verbs_check.sh` rc=0 on a pair `int local status` calls `currency ok` -- 44 canon-named verbs extracted from shipped canon rather than a hand-list, all the non-write ones answer, static arm confirms every canon-named flag spelling is declared. **Both of the instrument's declared blind spots closed by hand:** the four canon-named `--all` forms drive rc=0 with `HOME` redirected and a positive control (`--notaflag` rejected rc=1), so `0236`'s rc=1 class is genuinely fixed on this build; and `ext` is OUT of the population by executed hv ruling -- grep across `AGENTS.md`, `usage-rules.md`, `CLAUDE.md` and `lib/templates/llm/` names it nowhere, so `ext list` rc=2 cannot fire a falsifier scoped to verbs a shipped canon file NAMES. `claude rules validate`, the last verb this row waited on, answers rc=0 `68 ok`. **`ac satisfy` printed `ok:` three times and never persisted.**

**A MECHANISM CANDIDATE FOR `0216`, MEASURED NOT INFERRED: `intent/.cache/intent.db` is 21 MB and `intent.db-wal` is 251 MB.** A write-ahead log twelve times its database, never checkpointed. **AND THE FAILURE IS NOT UNIFORM, WHICH IS WHAT MAKES IT DIAGNOSTIC: creates SUCCEED and updates FAIL** -- `issues add` rc=0 while `issues edit` and `ac satisfy` both rc=1 `could not update the runtime store`, back to back. An unbounded WAL plus readers on a pre-write snapshot predicts `0216`'s whole signature. **NOT CLAIMED AS THE CAUSE** -- claimed as the first candidate that predicts the observed asymmetry. An open read transaction is what prevents a checkpoint, which would tie `0297`'s 28 unreaped daemons to this directly rather than leaving them adjacent.

**AND [[W135]] FIRED ON ITS OWN AUTHOR A THIRD TIME, ON THE SAME FAULT.** I wrote `grep -c` where `-i` was needed, against an uppercase bold heading, **inside the control built to catch that exact error** -- read the zero as `0299` being damaged and went hunting a data-loss event. `0299` is intact, 3727 bytes, committed at `dc447478b`. **The cure I wrote is to positive-control the reader; the control is what I broke.** LITTER I CANNOT REMOVE: `0300`, _write-path probe -- delete me_, created by the asymmetry probe. `issues close` is an update and updates are failing. Flagged rather than hidden -- a fourth member of the no-inverses class, arrived by accident.

**READ THIS SECTION AS A COLD NODE: every figure above is stale by construction. Run the verb.**

**NEXT UP: `AC-04.6`'s residual, IF vc ROUTES ITS RUST HALF HERE. ST0058 `AC-00.1` IS NOT ENGINEERING -- hv widened it to COHERENT AND CURRENT, so its third falsifier limb makes it closable ONLY AT THE CUT, by construction; `AC-00.3` is cc's (`claude rules validate`, the one canon-named verb still refusing). `AC-12.1`'s lib move IS DONE -- see the strike below.** Nothing of mine is uncommitted -- verified against `git status`, where the only dirt is peers' (`dispatch.rs`, `surface/dispatch-table.json`, `todo.md`, the disowned devbin manifest).

**`AC-02.3` IS GREEN: `docs-disposition: ok -- 239 of 239 dispositioned, every stated claim verified against bytes`** (`e9bc96040`). It opened the day at FAIL, 225 of 235. Four members were STATED because a reader meets them by following the published pages, each DRIVEN here before it was written rather than quoted from its filing (`0280`, `0283`, `0285`, `0291`); ten were recorded not-reader-reachable with their reason. **THE POPULATION RULE IS THE TRAP AND READING IT WRONG SHRINKS THE SET SILENTLY: a member is excluded only if it was ALREADY CLOSED AT THE CUT**, so `0279`/`0280`/`0282` -- closed today, filed long after v3.0.0 -- stay in, because a reader on the shipped pair still meets them. **`0288` turned on a measurement rather than a reading:** no published page names `.intent_critic.yml`, which reads as not-reader-reachable, but canon SEEDS it into every project and the critic's own refusal routes you to it by name. **NOT SATISFIED ON ST0068 -- that is vc's claim and the evidence is theirs to record.**

**`0287` IS CLOSED (`7f133373`, closed at `6da4edd8`), AND THE REPAIR IS A SECOND INSTRUMENT RATHER THAN A FIX TO ARM 6b.** A literal-`include_str!` census structurally cannot see a generated embed, so 6b keeps its population and its declared boundary and **arm 6d** takes the class it cannot reach. 6d reads the BUILD'S OUTPUT, not the generator: parsing Rust from shell cannot tell an INPUT it walks from the OUTPUT file it writes, and would have refused every commit in the repository over `embedded_templates.rs`. Its ABSENCE is reported as NOT EXAMINED, never scored as zero. `lib/templates` joins `SHARED_TARGET_DIRT_SCOPES` on `docs/design`'s grounds; it was clean when I widened, so nobody's build was refused. **`0285` IS NOT CLOSED BY THIS -- the MARKER's scope still excludes `lib/templates` and that half is hv's.**

**TWO SELF-INFLICTED FINDINGS FROM MY OWN FIX, KEPT BECAUSE EACH IS THE DEFECT I WAS REPAIRING:** the first form compared a path reduced to its ROOT against a predicate that asks for strictly-inside, so it red AFTER the gap was closed -- **a green taken then would have read as the fix working**, and discrimination had to be re-proven on the FIXED code with a real embed path. And the shell critic refused an unquoted expansion, correctly: a space in a build path turns one file into two that do not exist, grep opens neither, **and the arm reports a confident zero over a population it never read.**

**READ THIS SECTION AS A COLD NODE: every figure below is stale by construction. Run the verb.**


## Struck TODO items, verbatim

- ~~**`AC-12.1`'s NON-PRUNE HALF -- THE LIB MOVE.**~~ **DEAD 2026-09-09, MEASURED NOT ASSUMED: IT IS ALREADY DONE AND ITS TARGET FILE NO LONGER EXISTS.** `125f601d8` (2026-08-30) pruned `claude_plugin_helpers.sh` -- the home this item was to move symbols INTO -- along with six of the seven plugin bin scripts; `76e2e6801` brought `intent_claude_hook` back PORTED. Driven on today's tree: `intent_claude_cwi` and `intent_claude_hook` each DEFINE `error()`/`find_project_root()` and source nothing (cwi's own comment: _nothing is sourced now_), and `rules_lib.sh` carries the same treatment. **Edge A's three survivors are out of class (2)** -- `plugins/agents/bin/intent_agents` is another family, `lib_classify.sh` is a parity fixture (the row's own class (3), _a WP-12 call, not a sweep target_), `tests/run_tests.sh` goes with the cut. **Edge B -- the call-without-source edge the row says no `source` sweep can see -- IS EMPTY.** I went to build this and found it built. vc held it 2026-08-27 because building the lib move first would commit the seam before the thing it serves was scoped; hv scoped the prune on 2026-08-31 and vc confirmed the non-prune half startable 2026-09-09. Reversible and additive. All seven `intent/plugins/claude/bin/*` files source `bin/intent_helpers` in three spellings and take nine symbols, and `intent/plugins/claude/lib/claude_plugin_helpers.sh` is already the family's own home -- **the hard part, choosing the home, is already decided.**

- ~~**THE NO-`current_dir` TEST CLASS.**~~ **TRIAGED TO ZERO 2026-09-09, AND THE TRIAGE IS THE RECORD.** A crude first pass returned 46; most were DOC COMMENTS and string literals naming `CARGO_BIN_EXE`, which printing the evidence line exposed instantly ([[W130]]). Real spawns with no `current_dir` in an 8-line window: **11**. Every one resolved BY READING, not by the window: `critic_surface.rs:415` DOES set it, nine lines down -- **a false positive manufactured by my own window size**; `common/mod.rs`, `daemon_run_execs.rs`, `routing_is_opt_in.rs` pass `daemon run|stop`; `lang/modules/plugin/prefix/schema/version` helpers take 35 call sites between them and the ONLY mutating verb is `lang sync`, which the test asserts is RETIRED and exits 2, so nothing executes. **AND ONE FILE MY PATTERN MISSED ENTIRELY, WHICH WAS THE ONLY ONE CALLING MUTATING VERBS:** `all_flag_reaches_every_unit.rs` runs `claude <family> install --all` and `uninstall --all`, and my `run(&[` grep never saw it because its helper is `run(home, &[...])`. **Assuming one helper signature hid the only interesting file in the population.** It is SAFE: skills and subagents install under `$HOME` only (`payload.rs:41`, and `userstate.rs:59` states _the project commands do not need $HOME and are unaffected_), and the test redirects `HOME`. **NO TEST IN THIS ESTATE CAN MIGRATE THE LIVE STORE.** Both matcher controls fired (a planted `run(&["st","new"...])` matched, a planted `schema --versions` did not), so the zeros are earned. ORIGINAL: A test spawning the binary without `current_dir` inherits the runner's CWD and can migrate the LIVE store. **THIS BOARD SAID FOUR; A CRUDE SINGLE-CHAIN MATCHER RETURNS TEN CANDIDATES. NEITHER IS A COUNT** -- the four were point-in-time and the ten are UNTRIAGED real-versus-artefact. Triage before fixing. Touches files three nodes own, so it wants an `announce` and a quiet tree.

**STRUCK 2026-09-09 -- DEAD ON RE-DRIVE, NAMED SO NOBODY RE-DERIVES THEM:**

- ~~**`AC-12.1`'s IRREVERSIBLE `bin/` PRUNE -- DO NOT START IT.**~~ **STRUCK 2026-09-09: IT LANDED AT `125f601d8` ON 2026-08-30 AND I CARRIED A DO-NOT-START HOLD ON IT FOR TEN DAYS.** A hold outlives its subject in silence, because nothing re-reads a hold to ask whether the thing is still undone -- the whole point of the condition field is that YOU check it, and I checked the ruling instead of the tree. Found only because I went to touch the item next door. **(Original text kept below for its reasoning.)** ORIGINAL: hv 2026-08-31 13:20Z: _after the tag_ CHOSEN; _now_ declined; _never, keep the bash_ declined. **The load-bearing reason is my own finding and it is why the timing is not a preference:** `tests/run_tests.sh:24` sources `intent_helpers` above every `@test`, so a wrong move does not fail 112 bats files -- it stops them STARTING, and CI dies on a `source` rather than an assertion. The least legible failure available to us, in the worst possible window.
