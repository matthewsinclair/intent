## LOCALFOLD 4 -- 2026-08-19 00:21Z

Archived at the close of 2026-08-18/19. Everything below is carried by a COMMITTED artefact -- `critic-gate.md`, `output-contracts.md`, AC-07.4, AC-00.11, or the instruments themselves. Restating it on the live board is where the drift starts.

## Decisions

**The output-contracts class -- four mechanisms, one control, the same defect at four scales, and why care did not catch any of it -- is `intent/st/ST0056/output-contracts.md`, committed.** Restating it here is where the drift would start. What follows is what is NOT in that document.

- **THE DAY RULE, and every finding and every error of mine reduces to it: A GREEN PRODUCED BY SOMETHING OTHER THAN THE THING UNDER TEST.** **None was caught by care. Each was caught by MOVING something** -- the target dir, the subject, the clock, the desk.
- **PROVE THE DISCRIMINATOR DISCRIMINATES BEFORE TRUSTING IT.** Six of my own instruments failed this way in two days, each looking exactly like the finding it hunted: `bash -n` on a `.bats` file; `bats --count` on a copy that cannot resolve its helper; `info` printing one banner from both binaries; a sweep reporting `examined: 0` on an estate of 51; `grep -c ... || echo 0` printing two values; and **`across N file(s)` in critic output, which echoes argc -- a nonexistent path reports "1 file(s)" and clean.**
- **A ZERO IS NOT A RESULT UNTIL THE CHECK HAS PRODUCED A NON-ZERO.** I swept 51 shellcheck directives, nominated 16, and confirmed all 16 sound -- **but only reported it after deleting one directive and watching the finding reappear.** Same reason the bats estate-digest verdict is trustworthy: one byte moved it and `git checkout --` returned it.
- **A PROXY IS NOT THE PARSER.** My structural sweep nominated 16 and shellcheck cleared every one. **Nominate with a proxy; adjudicate with the real tool.**
- **A CLASSIFIER KEYED ON A MESSAGE IS BLIND TO THE POPULATION THAT NEVER PRINTS ONE.** 297/2 was really 299/0; `[[ ]]` prints nothing on failure. **Both classifications agreed on "2 remain".**
- **VERIFY THE PREMISE OF A QUEUED ACTION AT THE MOMENT YOU ACT ON IT.** I wrote that rule and then handed vc a task built on a stale one. **Today it paid: hv asked for shim install commands and the premise had gone false since "go for it".**
- **A CONFLATION CAN BE RE-RULED IN, AND A CORRECT EXIT CODE IS WHERE IT HIDES.** The v2 dark gate was an ACCIDENT of hoisting: two conditions sharing exit 2 and a hook that cannot separate them. The pending v3 ruling puts a THIRD condition in the same bucket **on purpose and for good reasons**, so the next instance arrives with an argument for it rather than as an oversight. **Defensible at the surface that emits it, fatal at the surface that reads it** -- and only visible by looking at both ends at once, which is the same two-sided blindness as test 2 and Half B.
- **A DEFERRAL NOBODY RE-VERIFIES IS PICKED UP AS LIVE WORK THAT WAS ALREADY DONE, AND THE PICKUP IS THE HARM RATHER THAN THE DELETION.** ic found that a pickup recorded only in `deferred.md` evaporates when the file dies. **The sharper form is the opposite direction: while the error sits in a file with an end condition it dies on schedule; copied onto a board it acquires a name, an owner and permanence -- and a named owner READS AS VERIFICATION to the next reader.** The prescribed remedy is what makes the stale premise durable. **One day old, and one of its four facts on my side was already false.** Cost of catching it: three git commands and a `strings`. **THE RE-VERIFICATION IS CHEAPER THAN THE WALK.**
- **AN INSTRUMENT THAT LIVES IN A SCRATCHPAD IS A CLAIM, NOT A CONTROL -- AND THE DOCUMENT DESCRIBING IT READS IDENTICALLY EITHER WAY.** `critic_global_rig.sh` was cited by name, its internals described in detail, in the paragraph telling a cold re-driver how to use it. **It was never committed at any point in history and was on no disk.** The other 39 instruments under `parity/tools/` are all committed; mine broke the convention and nothing caught it, **because prose about a thing is indistinguishable from prose about a thing that exists.** Same shape as ic's `deferred.md` finding one artefact over: the record survived and its subject did not.
- **THE CANARY IS THE DELIVERABLE, NOT THE TEST.** 6/6 with the fix says almost nothing on its own -- it is the same green the dark gate was emitting. **3/6 with the control, where the commit titled _"this commit must be REFUSED"_ gets CREATED, is the entire finding reproducible on demand by anyone, forever.** ic asked for a critic that FAILS on purpose; the more useful artefact turned out to be a gate that PASSES on purpose, on command.
- **SILENT IS A THIRD STATE, AND IT IS THE ONE THE GATE CANNOT REPORT.** Shell and rust rules are not "unarmed" -- they carry no proxy AND no declaration, and `critic_runner.sh:18` skips a proxy-less rule **silently**. Elixir's ten unarmed rules DECLARE the absence and name the subagent instead. **Armed / declared-none / silent are three states, the estate's vocabulary had two, and the missing word is where the defect lived** -- `critic shell` returns rc=0 because nothing ever asked a question, which is indistinguishable from clean.
- **A CEILING IS NOT DISCOVERED BY ATTEMPTING THE WORK; IT IS DISCOVERED BY READING WHAT THE INSTRUMENT ACCEPTS.** Four of thirteen rules were decided by `critic_proxy_is_simple`'s refusal of `-v` and `-L` before I formed any opinion about them. **Scoping the tool's contract first turned a 13-rule judgement call into a 9-rule one.**
- **MY OWN NARROW FINDING ACQUIRED ITS DEFECT IN BEING GENERALISED, WHICH IS THE THIRD TIME TODAY.** The `cargo test` one-liner, Half B as a one-liner, and now this. **The narrow form was a MEASUREMENT (two observed mis-sets); the general form swapped it for a structural property, and the property picked was the one shared with the working case.** A rule that covers less and holds beats one that reads better. **And I had to catch this one in my OWN rule after a peer improved it** -- the improvement is where I stopped checking.
- **A RECORD NAMES THE COMMIT IT COVERS, NEVER "HEAD".**
- **THE PUSH RESULT CARRIES NO INFORMATION ABOUT THE REMOTE IN EITHER DIRECTION.** Only `git ls-remote` plus `merge-base --is-ancestor` is evidence.
- **A DOCUMENT WRITTEN FOR COLD PICKUP MUST CARRY ITS OWN STALENESS.** vc's re-drive caution arrived in a peer message, which archives; the numbers it qualifies sat in a committed doc, which does not. **The caveat has to live in the same file as the figure, or the figure outlives it.**
- **EVERY MEASUREMENT THAT SURVIVED TODAY WAS SAVED BY A VALUE THAT CONTRADICTED THE DESIGN, NEVER BY CARE.** ic's marker holding still while behaviour moved. My 12-of-14 sitting inside a stretch that did not represent the population. This rc. **A merely PLAUSIBLE number gets published, every time** -- so the only usable discipline is to know in advance which values an instrument cannot return.
- **A PROPERTY OF A STATE BEATS A PROHIBITION ON AN OPERATION, AND THIS IS D42's TRADE AGAIN.** AC-03.6 was worded _"a sync must not ingest uncommitted bytes"_ -- an operation, so it covered only the operations someone had thought of, and **missed the direction that was live.** vc's restatement is a state: **_at every commit, every attachment's recorded bytes are obtainable from that commit_** -- indifferent to how the state arose, and checkable by inspection. Same reason D42's signature form held where its value form kept being reinterpreted.
- **THE SUBJECT MOVING IS THE DEFAULT CONDITION ON A FOUR-NODE ESTATE, NOT AN ACCIDENT.** vc nearly reported _"the modes disagree"_ off a pair of runs with HEAD moving between them **because I was committing**; they voided the measurement, re-ran with HEAD pinned and verified the pin either side, and said so rather than replacing the number quietly. **Three subjects moved under three instruments today** -- ic's binary rebuilt mid-run, vc's HEAD, my release target. **Any measurement here has an uncontrolled writer under it unless it pins its subject and checks the pin at both ends.**
- (2026-08-18) **`critic_proxy_is_simple` IS AN INJECTION BOUNDARY, NOT A CAPABILITY CEILING** (vc). It refuses `| ; & < > $` and backtick because a proxy block is DATA read out of a rule file, and executing it as shell in every fleet project's pre-commit gate is the whole point of the predicate. **My board had it filed as a ceiling, so the edit I would have reached for first is the hole.** hv authorised a capability; they did not authorise rule files becoming executable. The shape that honours both: **a NAMED-TOOL DECLARATION** -- the rule names WHICH tool answers it, the runner owns HOW, one invocation site, rule files never contribute shell.
- (2026-08-18) **ARMING MODE AND RUN CONTEXT ARE SEPARATE AXES, and that dissolves the exemption problem rather than resisting it** (vc, on my objection). I framed it refuse-vs-optional and predicted the three clippy rules would be marked optional to dodge the cost. **A whole-workspace compile does not belong in a per-commit hook at ANY arming mode** -- it belongs in CI, where the compile already happens and costs nothing extra. Move the context and the pressure is gone. **The numbers still decide WHERE the boundary sits; they no longer decide WHETHER there is one.**
- (2026-08-18) **THE AUTHOR OF A DECLARATION MUST NOT CHOOSE ITS ARMING MODE** (vc). **Optional is a property of the PROJECT, not of the RULE** -- the seam already exists at `.intent_critic.yml` (`bin/intent_critic:227`). A rule declares which tool answers it; a project declares whether that tool is required here. The rule author cannot exempt their own rule; a project owner doing it is visible and reviewable. **And the clause that stops the escape eating the rule: OPTIONAL IS ALLOWED, INVISIBLE IS NOT** -- a run that left N rules unarmed says so in its NORMAL output, EVERY run, not once per rule on stderr. **That is the positive control turned on the gate itself: it must distinguish CHECKED AND CLEAN from CHECKED NOTHING, and today it cannot.**
- (2026-08-18) **A DISTINCT EXIT CODE IS NECESSARY AND NOT SUFFICIENT** (ic, via vc, and it lands on my exit-code build). `surface_check.sh` returned rc=2 for exactly the right reason and **all four of us filed it as a chore, because TRIAGE HAPPENS ON THE PROSE** and it printed `rebuild it first (~30s)`. **A cannot-measure must be reported in the register of a FINDING. Build the code AND the register.** This is the output-contracts class with the roles swapped: the exit code is the author's string and the prose is the reader's view.
- (2026-08-18) **TWO CHANNELS, TWO INDIVIDUALLY-HARMLESS CONVENIENCES, AND NEITHER SUPPRESSION SUFFICIENT ALONE.** `grep <nonexistent path>` warns AND exits 2; `2>/dev/null` takes the message and leaves rc=2; `| head` takes the code and leaves the warning. **I wrote both and got silence at rc=0.** The remedy is stop destroying rc and stop discarding stderr -- NOT "check that paths exist", which was my first and less actionable diagnosis (corrected by vc). **AND THE ASYMMETRY IS THE PRIZE: `find ... | wc -l` returns `wc`'s rc, 0 either way, so THERE IS NO SECOND CHANNEL and `2>/dev/null` alone is total. `find|wc` is strictly the more dangerous idiom and vc and I had them filed as one mistake.**
- (2026-08-18) **AN ARM EARNS ITS STATUS BY HAVING BEEN DEMONSTRATED TO FIRE, NEVER BY BEING DECLARED** -- the same sentence as the positive control, said about arming instead of measurement, **which is the evidence they were always one rule.** vc's synthesis: three instruments reported success over an EMPTY POPULATION inside 24 hours in three languages, all green -- my `.canon` zero with no positive control, the runner's `return 0` over an unarmed rule, `sync --to-store` agreeing over 0 == 0 before `--to-disk` wiped 57 and 82 rows. **I had been building the general remedy while treating it as local.**
- (2026-08-18) **THE CONSTRAINT SURVIVED THE COPY AND THE REASON DID NOT, AND THAT IS WHY PROXIMITY IS NECESSARY AND NOT SUFFICIENT.** vc found the injection boundary was documented ten lines above the function and diagnosed it as my reading past it. **Measured, it is worse and more useful: I TRANSCRIBED that bullet into `critic-gate.md:124` and dropped five words -- `preventing pipelines disguised as args`.** Purpose occurrences in 228 lines of my scoping doc: **0** (positive control: 1 in the runner). `:126` then reads _the proxy can only express a POSITIVE match_ and `:176` _the gate is ARCHITECTURALLY BARRED_ -- pure capability framings, mine, and `:176` is the sentence hv ruled on. **Source -> lossy transcript -> scoping -> ruling -> the edit that opens the hole, each step faithful to the one before, the reason lost four steps upstream.** **So a purpose stated at the top of the new arming code reaches the next reader OF THE CODE and not the next writer of a DERIVED DOC, which is the step that actually fed the ruling.** **A PURPOSE THAT MUST SURVIVE TRANSCRIPTION TO WORK WILL EVENTUALLY MEET A TRANSCRIBER.** Proximity is a property of one artefact; the reader is downstream of a chain of them. **Only a mechanism that refuses is invariant under copying** -- build to ST0057 AC-02.5's shape, make the hole unrepresentable, and keep the comment as a courtesy rather than as the control. **This is SUPPRESSION, mechanism four of my own output-contracts document, in different clothes: the channel carrying the reason was discarded in transit and everything downstream read a clean, confident, complete-looking statement of what was left.**
- (2026-08-18) **YOU CANNOT COUNT A CONVENTION THAT DOES NOT EXIST, AND A DEFINITIONAL GAP DOES NOT ANNOUNCE ITSELF AS ONE.** vc measured reach statements across the 41 `parity/tools/` instruments and got **1**, then **9** (first probe matched `reach:`; the estate writes `REACH --`). I measured it myself rather than inherit it and got **16** on a wider pattern. **Three numbers, and only the FIRST gap was a defect.** 9 -> 16 is not a probe bug: neither of us wrote down what counts as a reach statement before counting them. **Both probes correct, both counts closing arithmetically, and they disagree.** The cause is measurable -- emitted forms are `REACH --` x3, `reach:` x2, everything else is variable names. **There is no convention, so every probe measures its own vocabulary rather than the estate's behaviour.** **Consequence for minting: a criterion saying _instruments state their reach_ would have its own check be a grep for an undefined thing -- the silent-armed defect one level up, returning clean because nothing could ask. THE FORM MUST BE MINTED BEFORE THE POPULATION CAN BE.**
- (2026-08-18) **THE TRANSMISSION CLASS SPLITS BY CHANNEL COUNT, AND THE REMEDY DIFFERS BY CLASS.** vc's line -- _a limit that is not in the OUTPUT is not a limit the reader has_ -- is exactly right for an INSTRUMENT and unavailable for a DOCUMENT. **Two channels (source + output): move the reason into the output.** That is literally what `addd4581` did, and it closes `stale_at_check.sh:28-29` today. **One channel (a document's source IS its output): _put it in the output_ reduces to _write the words_, and writing the words is what fails under transcription**, because the next writer copies the words and not the reason for them. **So one channel -> only a mechanism that refuses is invariant** (ST0057 AC-02.5's shape). **Carry BOTH forms, never the general one -- a general form is a transcript of two findings and loses the distinction on the next copy, which is this document's own thesis.**
- (2026-08-18) **A CORRECTION CONFERS UNEARNED CREDIBILITY ON ITS OWN OUTPUT, AND VERIFYING SOMEONE ELSE'S NUMBER CONFERS THE SAME THING ON YOURS.** The reach population ran **1 -> 9 -> 16 -> at most 4**. vc, on their own 9: _I corrected a broken probe into a probe that was measuring a different thing, and the corrected number felt more trustworthy precisely because it had just survived a correction._ **MY 16 WAS THE LOOSEST OF ALL FOUR** -- pattern `REACH|NOT EXAMINED|does not|DIAGNOSTIC|never fails`, where an emitted `does not` is overwhelmingly ordinary prose. I hedged it as an upper bound, which was right, and I still published it. **And its defect has a second source: I produced it while CHECKING vc's figure, and the act of independent measurement made it feel like adjudication when it was a third undefined probe.** vc's emitted-line count settles the ground: `REACH --` x3, `reach:` x1, plus `reachable` x5, `reached` x5, `reaches` x2, `reachability` x1, `reaching` x1 -- **eight lexical forms, most of them ordinary sentences containing the word.** **INDEPENDENT MEASUREMENT OF AN UNDEFINED THING IS NOT VERIFICATION; IT IS A THIRD OPINION ABOUT VOCABULARY** -- and it arrives wearing the authority of a cross-check. **A PROXY IS NOT THE PARSER is my own rule and I broke it while enforcing it on someone else.**
- (2026-08-18) **THE NAMED-TOOL FORM IS NOW hv's RULING, NOT vc's READING OF ONE -- RE-RULED WITH THE FACT THAT WAS MISSING THE FIRST TIME.** vc reported the transcription chain to hv directly: **the original authorisation was given over `critic-gate.md:176` (_the gate is ARCHITECTURALLY BARRED_), a description with the security rationale removed.** hv has re-ruled with it present and the form stands. **Binding, and this is the version to build to:** the rule names WHICH tool, the runner owns HOW, **rule files never contribute shell**, and a tool-armed rule **REFUSES when its tool is absent** rather than degrading to skipped. `critic_proxy_is_simple` stays exactly as it is. **Supersedes the earlier entry above, which attributes the boundary ruling to vc -- that attribution was correct when written and is no longer the standing of the rule.** **And the symmetry is the process working rather than a courtesy:** I declined to widen vc's contract on my own reading of a carry; vc then found they had preserved a constraint hv had never been told about, called that interpreting intent rather than executing it, and took it back for ratification instead of letting a safe outcome stand in for a sound one. **A SAFE OUTCOME IS NOT EVIDENCE OF A SOUND PROCESS, AND THE CHECK RUNS UPWARD TOO.**
- (2026-08-18) **THE REFUTING DATUM WAS IN THE AUTHOR'S OWN OUTPUT, HOURS BEFORE THE CLAIM, AND UNCOLLECTED AT THE MOMENT IT BECAME LOAD-BEARING** (ic, self-reported, withdrawing their own marker-churn evidence). They printed `intentd` at `20:33:26` local at ~19:53Z while checking staleness, then at ~21:29Z argued that binary had been REBUILT byte-identical -- **its mtime, in their own session output, predates their own 'before' reading by ten minutes.** Not missing data. Self-produced data, never re-read once its purpose changed. **A DISTINCT MECHANISM FROM THE FOUR IN `output-contracts.md`: not distance, not truncation, not an evidence-shaped token, NOT suppression -- the channel was open and the value was theirs.** **A VALUE PRINTED FOR PURPOSE A IS NOT CONSULTED WHEN PURPOSE B ARRIVES, EVEN IN THE SAME TRANSCRIPT AND EVEN WHEN DECISIVE.** My _verify the premise at the moment you act_ is the neighbouring rule and is weaker: it assumes the premise came from somewhere else. **This one you generated yourself, which is exactly why nothing prompts the re-read.**
- (2026-08-18) **MARKER LINE DRAWN, ic's WORDING, and it is the right shape for a PARTIAL withdrawal.** STANDS: `intent` changed and its hash changed -- one direction, one event. STANDS, the strong half: **three distinct artefacts carrying `dirty-18197aaf`, read independently by three nodes** (vc `cca08f4e` 19:40Z; ic before/after; me `f2e4d1f9` 21:31Z). **WITHDRAWN as untested: _the hash is identical exactly when the artefact is_.** The reclassification is carried by the positive direction and the count; **the negative direction is unmeasured and must not be claimed.** Experiment: private `CARGO_TARGET_DIR`, force a relink with no source change, compare across it -- cheap in RISK, NOT cheap in TIME (two cold release builds, three nodes active). **NEVER in `native/rust/target/release/`.** ic routed it to hv rather than self-authorising after overclaiming once.
- (2026-08-18) **FOURTH cwd FAILURE TODAY, AND THE WATCH-OUT IS NOT WORKING -- IT NEEDS A CONTROL, NOT A REREAD.** Three `git commit` pathspec failures from sitting in `intent/whiteboard/dc`, and one `NotADirectoryError` from sitting in `native/rust/target/release`, **where `intent` is a BINARY, so a relative `intent/whiteboard/...` fails with a message that reads like a corrupted repo rather than a wrong cwd.** I recorded _this is a control I should build_ and then hit it again in the next command. **USE ABSOLUTE PATHS IN EVERY COMMAND; NEVER LET A `cd` FROM A MEASUREMENT SURVIVE INTO A WRITE.**
- (2026-08-18) **BOTH BUILD ITEMS LANDED ON matts's RELEASE: `c51f10d5` (canon_commit_check.sh layout-agnostic) and `9f9167cd` (marker reclassified).** **THE LAYOUT IS DETECTED PER REVISION, NEVER CONFIGURED** -- a constant would have to be changed by someone who remembers the file exists, and the failure mode of forgetting is the silent zero. Ambiguous tree REFUSES. Id from canon CONTENT (ic). `att_dir_of` ASSERTS its strip changed the string. **THE MARKER IS PROVENANCE, NOT IDENTITY**: every binary line now prints a sha256 AND the reason, because the marker was the only distinguishing token on a line readers compare. Positive control LIVE in the tree -- `intent` and `intentd` share `dirty-18197aaf` byte for byte and differ in hash.
- (2026-08-18) **MY CANARY FOUND A DEFECT ALREADY COMMITTED IN MY OWN TOOL, AND IT IS THE CLASS THE TOOL EXISTS TO FIND.** `scoped` counted narrowing FILTER KEYS, not attachments EXAMINED. **Nested it only overstated (86 of 278 where 85 were examined) so it was invisible forever; flat it printed `EXAMINED 2 of 1 ... the other -1`.** A closing count over the wrong population, in the instrument gating AC-03.6, **and I had quoted ic's bar for exactly this while writing the line.** **A LATENT DEFECT IS ONLY VISIBLE FROM A POPULATION IT WAS NOT WRITTEN FOR** -- the flat layout did not cause it, it EXPOSED it. Which is the argument for canarying against a shape the tool has never seen, not just the shape it handles.
- (2026-08-18) **`exit` INSIDE `$( )` EXITS ONLY THE SUBSHELL, AND I WROTE THE COMMENT SAYING SO BEFORE MAKING THE MISTAKE IT DESCRIBES.** Both new refusals -- ambiguous layout, canon with no id -- PRINTED and the tool carried on regardless. **The no-id case then printed a population line that closed over a set silently reduced by one.** A refusal that does not stop is a diagnostic, and a diagnostic upstream of arithmetic is worse than no refusal, because the number afterwards looks measured. Every caller checks now. **Caught by the canary, not by the comment I had just written about it** -- fourth time today that a rule stated by its own author failed to protect its author.
- (2026-08-18) **A SIXTH MECHANISM, AND THE FIRST THAT OVER-PRODUCES: AN AGGREGATING FORMATTER CAN MANUFACTURE EVIDENCE THAT APPEARS NOWHERE IN THE SOURCE** (vc, self-reported, correcting AC-00.11 at `58b18051`). Their probe was `grep -oE 'ROOT=[^ ]*|\$\{?1\}?' | head -2 | tr '\n' ' '`. **The `tr` JOINED TWO INDEPENDENT MATCHES into what reads as one expression** -- `${1 ROOT="$(cd` -- and they stated the resulting claim (every parity tool takes its ROOT as `$1`) with confidence. Reproduced deliberately after the catch. **EVERY OTHER INSTANCE TODAY UNDER-REACHED** -- a narrowed regex, a line-range slice, a `// []` default, my own `does not` pattern. **This one produced a string that exists in no file**, and it was WELL-FORMED, which is why it survived: it reads like shell. **Under-reaching probes report less than exists and the gap is findable; a fabricating probe reports something that never existed and there is nothing to find it against.** Beware `tr`, `paste`, `xargs`, `head -n` + join -- **any step that COMBINES independent matches can synthesise a compound the source never contained.**
- (2026-08-18) **A CORRECT CONCLUSION CARRYING A FALSE SUPPORTING FACT IS THE `critic-gate.md` CHAIN, AND IT RAN AGAIN IN THE OPPOSITE DIRECTION ONE DAY LATER.** vc ruled correctly that the WP-01 audit window does not close, on the false fact that every tool takes ROOT as `$1`. **The conclusion is STRONGER without it: `git worktree add` needs no parameterisation at all**, because a worktree is a real checkout and a tool deriving ROOT from its own location resolves to the worktree root -- verified by vc on the one hard-wired instrument, whose upward walk finds the worktree's `intent/.config/config.json`. **Flag a wrong fact under a right conclusion: the next reader acts on the FACT.** Someone reading that row would reach for `stale_at_check.sh`, find nothing to pass, and blame the tool rather than the row.
- (2026-08-18) **I CARRIED AN UNMEASURED URGENCY NEXT TO A CAREFULLY MEASURED NOMINATION, AND THE UNMEASURED ONE WAS THE ONE DRIVING ACTION.** I measured that 10 of 41 instruments have the shape, stated the probe's reach, called it a floor not a ceiling -- **and then asserted the window closes when cc lands, having checked nothing.** It does not close: 2290 commits carry the nested layout and a worktree reconstitutes it permanently. **Racing it would have measured cc's IN-FLIGHT patch -- a finding about a tree that never existed**, which is the moving-subject failure ic asked not to have done to them. vc ruled a CRITERION (AC-00.11) rather than the sweep, on my own caveat: **a remedy sized to a floor is a remedy sized to the probe**, and a sweep finds today's ten and protects nothing from the eleventh. Order is GATEDNESS BEFORE COUNT.
- (2026-08-18) **THE RULE FAILS HARDEST AT THE MOMENT YOU APPLY IT TO SOMEONE ELSE. Three nodes, one day, each breaking their own most-cited rule WHILE ENFORCING IT.** Me: published `16 of 41` from a loose probe produced specifically to check vc's number. ic: argued from a binary their own earlier output showed had never been rebuilt. vc: twice -- fabricated a fact with a joining probe, then broke `never $? after a pipe` while verifying my correction of it, reading tail's rc=0 in place of the true rc=2. **The act of checking someone confers the feeling of having checked yourself.**

---

# Archived at the 2026-08-19 08:32Z localfold (Half B session)

## DOING

Nothing in flight. **Owed to me: vc's adjudication of AT-07.4's status and its cited path.** Owed by me: nothing.

## TODO

1. **HALF B: DONE. The arming axis is closed -- shell 2 armed / 4 declared / 0 undeclared, rust 4 armed / 3 declared / 0 undeclared.** `e7674053` (two grep arms), `35ab425e` (five tool arms + the two-axis census), `ca1d330c` (AT-07.4, 16 arms, both mutants red). **The shell critic now genuinely asks shellcheck** -- SC2086/2046/2206/2068 and SC2012 -- and it blocked one of my own commits on its first day.
   **WHAT REMAINS OF AC-07.4 IS (b), THE REFUSAL, AND IT IS DELIBERATELY NOT BUILT.** A tool-armed rule on a project that armed it must REFUSE when its tool is absent. Refusing needs an exit code the hook can tell apart from an invocation error, and **emitting one before the hook can read it puts a THIRD condition in the bucket that already conflates two, on purpose this time.** It lands with TODO 5, both ends in one commit.
   **I REVERSED MY OWN ROW AND THE MEASUREMENT IS THE ARGUMENT: `IN-SH-CODE-005` IS DECLARE-NONE, NOT GREP-ARMED.** `2>/dev/null` appears **395 times in 87 of 128 shell files -- 68%**. Arming it refuses two thirds of the estate on a CRITICAL rule over correct code. The reason is structural: the rule's content IS its qualifier (_without an adjacent comment_) and a proxy may carry no `-A`/`-B`. `critic-gate.md`'s row needs the correction.
2. **THE RUST-TEST SLOWNESS IS UNDIAGNOSED AND I WAS WRONG FOUR TIMES.** Link fan-out, then "Elixir has no link step" (Lamplight's CLI is Rust), then "Lamplight uses worktrees" (both branches 0 ahead, one 305 behind -- **filed with hv and retracted the same hour**), then binary count (**Lamplight: 1 crate + 19 binaries = ~10min; Intent: 2 crates + 80 binaries = 1m56s -- we are the FASTER project**).
   **ESTABLISHED**: slow run compiled, fast run did not, same command (matts's two logs). Lamplight source untouched 4 days, manifests since 31 Jul, rustc since 21 Jul -- **cargo recompiled what nothing had changed**. **RULED OUT**: disk, memory, swap, devbin-invokes-cargo-per-suite (`cmd/test.d/rust:199` is one call per project), global cargo config, toolchain update.
   **THE INSTRUMENT, and it must run WHILE THE SYMPTOM IS PRESENT** -- I ran it warm and learned nothing: `CARGO_LOG=cargo::core::compiler::fingerprint=info bin/ll test rust 2>&1 | grep -i dirty`. Cargo names the unit and the reason.
3. **Per-node `CARGO_TARGET_DIR` -- announced, mine is `native/rust/target/dc`.** Verified 646 passed / 0 failed / 0.22s warm. **MUST be inside the workspace**: `/tmp` breaks four install-resolution tests because `install.rs:91` walks up from `current_exe()` for a marker dir. Stops us invalidating each other; does NOT stop source churn.
4. **Test-binary consolidation: STOOD DOWN, cc told not to reorder WP-01 for it.** 80 top-level `tests/*.rs` -> modules of ~2 binaries; no file deleted, none moved out of its own file; 42 `mod common;` become `use crate::common;`. **Justification is gone** -- Lamplight has 19 and is slower. Worth doing as a tidy-up only, and it changes a spelling four nodes use (`--test X` becomes `--test suite X`, including the `INTENT_BLESS=1` re-pin).
5. **The hook's fail-open branch -- ruled: a DISTINCT exit code, proved by a RED. IT NOW CARRIES THREE JOBS, NOT ONE, AND THEY ARE ONE RE-CUT OF ONE BLOCK.** (i) the distinct exit code; (ii) **Half B's refusal**, which is the second consumer that makes the code worth having; (iii) **generalising `WB_GUARDS`' presence test per-guard**, because cc's `canon-ignore-guard.sh` (`91736056`) is opt-in by `intent/.canon/` and the whole fail-open reasoning sits inside `if [ -d intent/whiteboard ]`. **cc offered to do (iii) to my spec and I took it instead: two edits to that file means re-reasoning it twice and risking two half-compatible semantics.** A generalisation with one member is a rename; Half B supplied the second. Both ends in one change, and ic's condition: **a distinct code is necessary and NOT sufficient** -- `surface_check.sh` returned rc=2 for the right reason and all four of us filed it as a chore, because triage happens on the PROSE. Build the code AND the register.
6. **BASH-4 SWEEP OF `lib/templates/` -- cc's find, taken.** `env bash` resolves to homebrew 5.3 here while macOS ships 3.2.57, so a template can pass every local test and do nothing on a default shell -- **and a hook that silently does nothing reports success having checked nothing**, which is Half B's own sentence one layer down. Sweep `mapfile`, `readarray`, `declare -A`, `${var^}`, `&>>`.
7. **`canon_commit_check.sh` IS BLIND TO THE OPPOSITE DIRECTION -- BLOCKED ON vc's ELIGIBILITY RULING, NOT QUEUED.** Its subject is _canon must not name bytes not in its own commit_: canon -> bytes, one direction. **A file IN the commit that canon does not record is never iterated, so it cannot be missed -- it was never in the population.** Driven at `f789ae48 --exhaustive`: rc=0, 279 recorded attachments, **zero mentions of `of_n_population.sh`**, which was in that commit with no canon record. A second arm needs a definition of which files under a thread are attachment-ELIGIBLE, and guessing that set inside a checker would put the contract in the instrument.
8. **`doctor` = option 3, ruled** (`intent v2.19.0 auditing a 3.0.0-dev project`). XS. Check whether v3's doctor needs the mirror.
9. **WP-11 dist wiring** -- no `dist-workspace.toml`, no formula, and confirmed independent of ST0057 WP-01. **Publication is policy-unblocked and sequence-blocked**: it needs a tag, `int build release` correctly refuses a dev version, and `staged_version` is `3.0.0-dev`. AC-11.1/11.4 stay open.
10. **`output-contracts.md` owes four additions**: suppression restated as TRANSMISSION (prose is a channel); the fifth mechanism (**the refuting datum was in the author's own output, uncollected when it became load-bearing**); ic's class name (**a control that appears to fire and doesn't**); and the sixth (**an aggregating formatter can manufacture evidence that appears nowhere in the source**).

**STRUCK, ruled and needing nothing: `--skip-rust-tests` (DROPPED). The shim (HOLD -- defeats Half A). WP-12's clean-tree rebuild (not bankable, AC-11.5 closed).**

## Watch-outs

- **THIS REPO IS A v3 PROJECT** (`3.0.0-dev`; store `intent/.cache/intent.db`, gitignored). v2 refuses PROJECT verbs at exit 2; `GLOBAL_COMMANDS` run fine, and **`critic` is now one of them (my change, `92a51134`)**. Invoke v3 by explicit path and use `target/debug/`, not `release/`.
- **THE BUILT BINARIES ARE AT `native/rust/target/release/`, NOT `release/`** -- two days of my own records named a directory that does not exist.
- **PUSH `local` ONLY.** `upstream` closed (hv, CI/CD budget), frozen at `5765c5da`.
- **NEVER `git pull --rebase` here; a peer `.git/index.lock` means WAIT. ALWAYS `git commit --only <paths>`** -- a bare commit sweeps a peer's staged index, and `--amend` ignores `--only`.
- **NEVER mutate a file in place while anything runs it -- USE AN ATOMIC REPLACE (write beside it, `mv` over it).** Size is irrelevant, only timing: the pre-commit roster runs on every node's every commit. Both vc and I nearly shipped an in-place edit to a live gate file today.
- **A PARITY TOOL IS AN ATTACHMENT OF ST0056, SO COMMITTING ONE WITHOUT A SYNC LEAVES CANON STALE AND THE NEXT PEER'S SYNC SWEEPS IT INTO THEIR COMMIT.** Measured: `of_n_population.sh` file at `f789ae48` (mine), its canon record at `938ed7a3` (vc's, under a message about figure provenance). **ORDER: announce, `sync --to-store`, then commit file and canon together.** And `intent sync` TAKES NO SCOPE -- whole-estate only -- so the correct per-node workflow requires the one operation two nodes cannot run concurrently. Announce first, every time.
- **`git diff --stat` CANNOT EXPRESS SIZE ON CANON JSON** (vc). An attachment is ONE LINE, so a 9,740-byte tool entering canon and a one-word typo both read as `1 insertion`. **Compare structures, never the diffstat** -- the reviewer's cheapest instrument is the one that cannot see this class.
- **COMMIT BEFORE ANY `intent at` STATUS CHANGE** -- `intent at red|green|na` DESTROYS the row note (issue 0033). There is NO note verb; the whole family is done/green/lint/list/na/notdone/red.
- **`cargo test` IS A CONCURRENT WRITER ON THE ESTATE**, and `.gitignore:127` ignores the store, so `git status --porcelain` cannot see it.
- **A PEER CANNOT WAIVE AN hv RULE**, and being right on the substance is not the same as being entitled to say so.
- **MY SHELL IS zsh: it does NOT word-split an unquoted `$var`, and `~3` is a directory-stack reference, not a git suffix.** Both produced plausible wrong answers today. Quote, or use a bash array.
- **NEVER `$?` AFTER A PIPE.** I hit this in my own verification today after quoting it at two other people.
- **AN APOSTROPHE INSIDE THE SINGLE-QUOTED `ROSTER` STRING BREAKS `runner_roster_check.sh` AT PARSE.** Caught by running the consumer, not by `bash -n` on the tool.
- **`git log` prints LOCAL time**; reading one and appending `Z` is wrong by exactly the offset and looks perfect.
- **macOS: signing MUTATES the binary, notarisation does NOT -- checksum AFTER signing.** `target/release/` is shared mutable state; private `CARGO_TARGET_DIR`, never sign there.
- **A control refuses; documentation reminds; only one is load-bearing.** My own watch-outs are not controls -- which the day proved on me twice.
- **`cp` ONTO A SYMLINK FOLLOWS IT AND WRITES THROUGH TO THE TARGET.** My isolation rig symlinked every `bin/intent_*` into the real repo and then `cp`-ed over one -- so it wrote into the LIVE `bin/intent_critic` for two minutes. **A rig assembled by symlinking into the real tree is not isolated; it looks isolated right up until it writes to the file it existed to protect.** `rm` the symlink before writing, and only symlink what you READ.
- **`exit` INSIDE `$( )` EXITS ONLY THE SUBSHELL.** Two refusals printed and the tool carried on; one then closed its arithmetic over a population silently reduced by one. **A refusal that does not stop is a diagnostic, and a diagnostic upstream of arithmetic is worse than none, because the number after it looks measured.** Every caller checks.
- **`chmod --reference` IS GNU-ONLY.** On macOS it fails and a `||` fallback IMPOSES a mode rather than preserving one -- it flipped a sourced library to 755. A fallback that imposes is not a fallback.
- **A REPRODUCTION THAT IS NOT BYTE-FAITHFUL IS WORSE THAN USELESS.** Mine gave rc=2 where the live state gave rc=0, and I nearly announced the gate had been fail-opening -- a false alarm about the exact defect under investigation.
- **CLIPPY LISTS ITS LINTS HYPHENATED** (`clippy::needless-lifetimes`). My underscored grep reported 7 of 8 ABSENT from a list of 825. Third vocabulary error of the day.
- **THIS MACHINE'S TIMING NOISE FLOOR IS 6x.** Identical suite, back to back, warm: 16.12s then 100.10s. **I called it noise and moved on; it was the most informative number I had.** Any timing here needs repeats before it means anything.
- **MEASURE THE CASE THAT DISPROVES YOU, AND matts HANDED ME THE CONTROL GROUP TWICE BEFORE I USED IT.** Four wrong explanations on one question in one evening, each reasoned from a property I had not checked. **`.worktrees/cc` EXISTING told me nothing about whether anyone works in it** -- `git rev-list --count` did, in thirty seconds, and said zero.

## Decisions

- (2026-08-19) **A CHECK THAT WALKS THE RECORD CANNOT SEE WHAT THE RECORD OMITS, AND MY OWN TOOL SHIPPED WITH ONLY THE HALF THAT COULD NOT CATCH THIS.** `canon_commit_check.sh` asks whether every RECORDED attachment's bytes are in the commit. **A file in the commit with no record is not missed -- it is never in the population.** Same denominator failure as AC-00.11, inside the instrument built to police canon/commit agreement.
- (2026-08-19) **A WORKFLOW WHOSE CORRECT FORM REQUIRES AN OPERATION ONLY SAFE FOR ONE ACTOR IS A SINGLE-WRITER BOTTLENECK WEARING A PER-NODE PROCEDURE'S CLOTHES.** `sync --to-store` is unscoped and whole-estate; every node needs it to land its own work; two nodes running it clobber at rc=0. Serialising it through vc makes "my artefact, someone else's commit" permanent BY POLICY rather than by accident.
- (2026-08-19) **A COUNT INSIDE A CRITERION IS A RECORD, AND ITS SUBJECT CAN DIE WITHOUT ANYTHING NOTICING.** AC-00.11 carries `dc nominated 10 of 41 parity tools by a two-grep proxy` and **that probe was never committed** -- no tool, no board history, and my reconstruction returns 14. `of_n_population.sh` (`f789ae48`) enumerates the population instead. **ic found the same shape in the same criterion within the hour** (`EXAMINED 86 of 278` names no revision, so nobody can check it), so AC-00.11 carried TWO unreproducible figures in the criterion that rules figures must derive from what was examined. vc's remedy is the right one and it is not a third number: **NAME THE REVISION, or MARK IT `RECORDED`.**
- (2026-08-19) **TWO POPULATIONS THAT COINCIDE ONLY IN A NUMERAL READ AS A CROSS-REFERENCE AND ARE DOING NO SUCH WORK.** `10 gated of 17 rostered` and `10 of 41` are different sets; **five of the ten gated carry no `intent/st` path shape at all**, so half a `drive the gated ten` plan would have returned clean under a two-tree differential and meant nothing. ic was one step from building on it.
- (2026-08-19) **A FIFTH VALUE IN A KEY IS HOW AN AXIS GETS SMUGGLED IN, and vc caught mine before it landed.** Arming state and run disposition are two axes: `armed` + `not-run:out-of-context` says both true things at once, where a fifth arming value would have made `armed` mean two things depending on which member you read. **The general form vc then minted from three same-day instances: a checker that verifies MEMBERSHIP in a vocabulary never verifies that the vocabulary can EXPRESS the states in use** -- and the forced-wrong value is always the one that reads as MORE finished, which is why review never catches it.
- (2026-08-19) **I ARMED A CRITICAL RULE AGAINST MY OWN WRITTEN WARNING AND ONLY THE MEASUREMENT STOPPED ME.** `critic-gate.md` said IN-SH-CODE-005 would fire on every documented correct use. I armed it anyway on "noisy beats silent", and it blocked my own commit within the hour: 13 findings on one file, 13 false positives, 68% of the estate. **A cost I had written down did not function as a constraint until it was a number.**
- (2026-08-19) **A STRIP THAT SUCCEEDS AND PRODUCES A WELL-FORMED PATH TO NOWHERE IS INVISIBLE TO EVERY ASSERTION ABOUT THE STRIP.** `canon_commit_check.sh` already guards the failed-strip case (pattern absent, string returned unchanged). The move broke the OTHER direction and the tool reported **ADDS 279 of 279 at exit 1** -- its own resolution failure, in the vocabulary of a finding. **The flat arm was a PREDICTION written before the move, and the move did something else.**
- (2026-08-19) **A PROBE THAT MEASURES THE WRONG NAME REPORTS A REAL CAPABILITY AS MISSING, AND THE OUTPUT READS EXACTLY LIKE A TRUE ABSENCE.** `command -v clippy` is false on a machine with clippy 0.1.97, because it ships as `cargo-clippy`. **Second clippy-vocabulary error in two days** after the hyphenated-lint-name one. And the ORDER compounded it: testing availability before context answered a question about the invocation with a fact about the host, and a reader would have installed something that still would not run.
- (2026-08-19) **SIX CONFIDENT REDS IN MY OWN NEW TEST, ALL THE INSTRUMENT'S FAULT.** `bash -c` spawns a shell that has not sourced the library, so six arms asserted against `command not found`. **Same shape as a sweep reporting `examined: 0` over an estate of 51 -- the instrument answered a different question and the output looked like a result.**

**Today's 21 decisions are archived to `.history/20260819/` and every one is carried by a committed artefact** -- `critic-gate.md`, `output-contracts.md`, AC-07.4, AC-00.11, or the instrument itself. **Restating them here is where the drift starts.** What follows is only what is NOT in one of those.

- (2026-08-19) **A DIRECTORY THAT EXISTS IS NOT A DIRECTORY ANYBODY USES.** I filed a structural proposal with hv off a path listing and retracted it the same hour when `rev-list` showed both branches carried zero commits. **Same shape as a marker that names a commit but identifies no artefact, a proxy that is not the parser, and an instrument that lives in a scratchpad: the record survives and its subject does not.**
- (2026-08-19) **A GUARD BUILT FROM ONE INSTANCE DEFENDS AGAINST THAT INSTANCE, NOT THE CLASS** (ic, on truncating my inbox with a guard they wrote the same day against the opposite failure). **Its freshness is what makes it feel like coverage.**
