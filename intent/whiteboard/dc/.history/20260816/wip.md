# dc -- archived 2026-08-16 10:17Z

Fold 2 of this cycle. The evening of 2026-08-15 and the morning of 2026-08-16, which were one arc: **the tree moved and every thing that knew where it was did not follow.** matts found the worst of it by typing a command no instrument had run.

## What landed

0. **LANDED 2026-08-15 EVENING -- five things. The first three were one defect wearing different clothes: THE TREE MOVED AND THE THING THAT KNEW WHERE IT WAS DID NOT. The last two are the bounce's third item and a hole cc found.**
   - **THE FOUR `int` RUST GATES HAD BEEN DEAD SINCE `a1a949c` (`62a1bbeb`, `9f768a80`). matts found it, not me, and not any instrument.** `test rust`, `check clippy`, `fmt rust` and the `check format` builtin's rust arm all run in a subshell at PROJECT_ROOT -- the right default -- where there is no longer a manifest. **CI was green throughout, because `rust.yml` sets `working-directory: native/rust` and was updated by the move while devbin was not.** Same three checks, two homes, one followed the tree. Declared with `--manifest-path`, flags kept verbatim identical to CI, and the guard that matters is `every cargo check CI runs has a devbin twin with the same flags` -- it would have gone red the day `working-directory:` was added. **`--all` is load-bearing: on a VIRTUAL manifest `cargo fmt --manifest-path` exits 1 as `Failed to find targets`, a flag error wearing a finding's exit code.**
   - **`int prepush` NOW READS THE REF PAIRS ON STDIN (`58f9fdb5`).** The remote sha in each pair is what THAT remote holds, so the range is per-push and per-remote without needing the remote's name. Where a range cannot be computed honestly it pays for the check. By hand there are no pairs and no single right answer, so it answers for the remote **furthest behind** -- the union. **cc's ask phrased literally -- "gated regardless of which remote" -- PASSED ON THE BROKEN RUNNER**, because with both remotes behind the old range is non-empty and the defect never reads stdin at all, so the bug satisfies that property perfectly.
   - **`testkit` HOLDS THE TWO ROOTS, AND THERE WERE NINE COPIES NOT FOUR (`e75908cf`).** `repo_root()` x5 (searched, -> repo root) and `workspace_root()` x4 (counted, -> `native/rust`) -- two functions returning directories two levels apart under names similar enough that nobody noticed. **cc found four by grepping `repo_root`; the other five were invisible to the search that found the first four.** Both are searched now; one copy was still `ancestors().nth(2)`. Net -97 lines.
   - **ic's OFFER-2 PROPERTY IS A LIVE GATE (`9db4cf8e`).** `generator_inputs_check.sh`, guard 3 in `int precommit`. Four refusals, and **(1) a generator with NO declaration is the closing condition** -- without it the guard is decorative, because ic's stated failure is someone forgetting to declare. Enumerated AND read from the INDEX, so a peer's half-written generator cannot block my commit. **Found one level above the guard: three of the four generators still cannot reach their own committed inputs** -- they refuse without `SP` while the committed twin sits beside them, reachable only by a `cp` in a comment. Flagged to ic, not fixed: their lane, and one reading is that I misread their report.
   - **A WHITEBOARD HEADER THAT HAS BEEN YAML-ESCAPED IS NOW REFUSED (`aa983457`).** cc's board landed as `ic''s` and nothing caught it. **cc blamed the pre-commit formatter and I could not reproduce that** -- prettier at the binary the hook resolves, with the hook's invocation, against their input reconstructed from the committed result, is byte-identical; no PostToolUse formatter; no `ws` module writes a board. **So the likely author is a node that knows YAML, which is the side the ruling addresses.** Two mechanical signatures, never auto-corrects, prints the repair. Zero findings across all five real boards.

1. **LANDED 2026-08-16 MORNING -- WP-11's two unwritten deliverables, and one of them was hiding a defect.**
   - **NOTHING COULD CUT A v3 RELEASE (`ebee251d`).** `int build release` creates the tag AND the GitHub release; `int macos publish` required the tag to exist (the cut owns tagging) while refusing outright if a release for that tag existed. **Tag present and release absent is a state the only thing that creates the tag cannot leave behind.** Each command correct, each refusal right, composition impossible -- and it was hiding in exactly the deliverable nobody had written, so nobody meets it until the morning of the cut. Fixed by testing what publish's own comment says it protects: **ASSETS, not existence.** Zero assets = no bytes published = nothing to overwrite. Unreadable count FAILS CLOSED. **UNEXERCISED and unrehearsable here** -- `staged_version` is `3.0.0-dev` so the dev refusal fires first; nine input shapes canaried instead.
   - **INSTALL/UPGRADE DOCS (`6affb27d`), `intent/st/ST0056/install.md`.** Leads with brew SHADOWING a v2 install rather than replacing it (brew prefix at PATH 1, v2 symlinks at 17 and 19, re-measured). Marked at the top as describing a path that does not work yet. Names issue 0036 as a do-not-publish-before. Does not restate `migration.md`.
   - **`int check all` HAD BEEN RED FOREVER (`bbe73f69`).** `check toolchain` demands a `.tool-versions` this project deliberately has no answer to; clippy, format and critic all passed. **An always-red aggregate is worse than a slow one -- a slow gate gets bypassed, an always-red one gets ignored and the real failure hides in the noise.** Arm disabled, disabled-set pinned by test, pin decision sent to hv rather than settled by adding a file. **That test's own first mutation named a gate `clippy2` and the extractor silently did not see it** -- `[a-z-]` does not match a digit -- so it passed over exactly what it exists to catch.
   - **`testkit` IS MINE, NOT cc's (`2f42f150`).** vc read my uncommitted tree at 21:36Z, attributed it to cc, told me DO NOT BUILD and told cc they had built it. `git log --diff-filter=A` returns one commit, mine. **In a four-session shared clone an untracked file has NO AUTHOR** -- vc's method is right and is exactly the method that cannot answer "who". Corrected to both.

## Decisions, as they stood at this fold

## Decisions

Standing only. The day's full set is in `.history/20260815/wip.md`.

- (2026-08-15) **A TOOL GIVING A REAL-LOOKING ANSWER USUALLY MEANS IT WAS ASKED IN THE WRONG CONTEXT -- CALIBRATE AGAINST SOMETHING KNOWN GOOD BEFORE BELIEVING IT.** Three instances in one day: `spctl -a -t exec` calling a correctly signed binary rejected; awk on a fixed-column format; `brew style` reporting five offences on a correct formula, four of them the generic Ruby config applied outside a tap. **Each was answered in ONE command by running the tool against something already shipping.** - (2026-08-15) **"NOTHING REFERENCES X" IS FOUR DIFFERENT CRITERIA WEARING ONE SENTENCE.** Executes, emits, cites as provenance, records as history -- only the first two are defects, the third is load-bearing documentation and the fourth must never change. **A criterion that does not distinguish them gets satisfied by deleting the record.**
- (2026-08-15) **A MUTATION RUN THAT ONLY CONFIRMS ITS OWN HEADER WAS NOT A MUTATION RUN.** Mine deleted a test of mine: "never modifies the working tree" could not be made to fail, because on a path whose worktree already equals HEAD, `git reset` and the destructive `git checkout HEAD --` are observably identical -- **the guard removes the difference, so the test restated the guard's consequence and read as coverage.** Also: an unapplied mutation reports "nothing failed", which is indistinguishable from a test that does not check. Verify the fixture took.
- (2026-08-15) **REVERSIBLE BY CONSTRUCTION EARNS A CONTROL THE RIGHT TO ACT RATHER THAN REPORT.** The clock guard deliberately never auto-corrects, because the NODE made that error and needs to learn it. Issue 0028 is the opposite case -- the TOOLING makes it, on every markdown commit, and eight measured instances were reported and none cleared. **A sweep that prints the blob sha of everything it removes destroys nothing**, so the argument against acting does not apply.
- (2026-08-15) **TO ASSERT WHAT A USER RECEIVES, BE THE USER FOR ONE ROUND TRIP.** AC-11.4 is the published hash matching the published bytes; hashing the local file asserts that about a file nobody fetches. Publish re-downloads what it uploaded. **The cost is one download and it removes the class that never fails for us and fails for every `brew install`.**
- (2026-08-15) **AN ALLOWLIST FORBIDS EVERYTHING ITS AUTHOR DID NOT THINK OF; A NEEDLE LIST FORBIDS ONLY WHAT THEY DID.** AT-11.3 asked for `INTENT_HOME`. Asserting the read set is exactly `{COLUMNS}` costs the same and catches the variable nobody has invented yet -- **including one added by a command that is not implemented today**, which is where the risk actually is, because the unwritten commands are the ones that will want a home.
- (2026-08-15) **A STRUCTURE THAT CANNOT FAIL BEATS A CONTROL THAT CATCHES THE FAILURE, AND "RECORDED AS THE BETTER SHAPE, DELIBERATELY UNBUILT" IS USUALLY DEFERRAL WEARING A REASON.** I shipped the refusal and left the design written down. A refusal detects the race only at the END, after a notarisation round trip has been spent on bytes that no longer exist. Building it took under an hour.
- (2026-08-15) **THE DEFECT IS OFTEN THE GAP BETWEEN CORRECT STEPS, NOT ANY STEP.** Four individually-correct macOS subcommands, run by hand with a multi-minute wait in the middle. A note saying "run these as one sequence" is a reminder; `int macos prepare` is the sequence.
- (2026-08-15) **REFUSING TO SETTLE BY INFERENCE IS NOT A RESTING STATE -- IT OBLIGES YOU TO GO AND GET THE ANSWER** (vc's). An open question parked across three rulings is a decision made by default. **Three independent stops is one alarm, not three data points.**
- (2026-08-15) **MEASURE BEFORE YOU DESIGN, AND LOOK FOR THE ESTATE'S EXISTING ANSWER FIRST.** A well-formed-feeling question can rest on an unruled premise and name a tool that cannot do the job. Conflab had shipped the answer for four months.
- (2026-08-15) **A SUFFICIENT-LOOKING CHECK THAT ANSWERS A NARROWER QUESTION, AND FAILS GREEN.** Ask what a check actually proves, not what it is conventionally used for.
- (2026-08-15) **A WRONG ARTEFACT IS NOT A NEUTRAL PLACEHOLDER -- IT MAKES A CONFIDENT FALSE STATEMENT.** A formula pointing at a nonexistent release reads as "the tap is broken" rather than "the release is not out yet". **An empty tap says the true thing.**
- (2026-08-15) **AN AC NAMES THE OUTCOME; THE MECHANISM BELONGS IN THE WORK PACKAGE** (vc's). A criterion naming a tool can be invalidated by a measurement of that tool while the thing the project wanted is still achievable.
- (2026-08-15) **THE CONTROL GOES WHERE THE HARM IS.** A dev-version formula printed to a terminal harms nobody; the same bytes in a tap install a build nobody meant to ship.
- (2026-08-15) **A PEER CANNOT AUTHORISE WHAT A HARNESS REFUSED**, and a peer performing it on your behalf launders the refusal.

## DOING archived at 2026-08-16 14:07Z (localfold)

- **THE BOUNCE IS LANDED -- `12694f61`, pushed to `local`.** `lib/templates/hooks/whiteboard-header-guard.sh` is canon: opt-in by the presence of `intent/whiteboard/`, a SEPARATE guard per vc's ruling, registered in MODULES.md, documented in the skill and in `pre-commit-hook.md`, CHANGELOG under 2.19.1. **hv still owns the release timing** -- a v2 patch during the v3 rewrite is a timing call, not an architecture one.
- **THE LANDING NEEDED NO INSTALLER WIRING AT ALL, AND THAT WAS WORTH FINDING BEFORE BUILDING ANY.** Only `pre-commit.sh` is copied into a project; the guard BODIES are resolved at runtime from `INTENT_HOME` (issue 0016's pattern), and the upgrade ledger probes with `cmp`. So editing the shipped hook is the entire propagation mechanism: every board-running project picks the guard up on its next `intent upgrade`, and nobody touches `.git/hooks/`. **I had "installer wiring" and "`upgrade` propagation" on the board as two work items; both were already built, by someone solving a different problem.**
- **THIS REPO NOW RUNS THE BYTES THAT SHIP.** devbin's guard 0b was the same check as a second copy -- so it DELEGATES to the canon guard instead. Dogfooding a copy is not dogfooding. The inline version also had **two defects the port found, both over-refusing**: it reached `.history/` (21 files matched here, SIXTEEN archives) and its unanchored extractor scanned whole headerless files.
- **DONE, `2b697189`: `prepush` REFUSES A FROZEN REMOTE.** By name and by URL, `INTENT_ALLOW_FROZEN_PUSH=1` for a one-off, lifting is a tracked one-line delete. Canaried both ways including a real `git push upstream main --dry-run` (refused) and a real `git push local main` (worked). **Two structural things fell out of it and both were bigger than the arm.** (1) **The hook forwarded no argv at all**, so no runner could ever see its destination -- and I had written in that very file that the un-forwarded argv "is not needed here", which was true of the RANGE and false of the DESTINATION. (2) **`install_chain` could not upgrade anything**: it asked only "does this file invoke the runner", so once wired the answer was yes forever and a changed generator could never reach an existing clone. `.git/hooks` is never tracked, so nothing else would have caught up either. **I have re-installed the hooks in THIS clone, which is shared by four sessions -- announced.**
- **THE CUT PATH IS NOW BUILT, `25cdc639`.** `int build release` stamps `native/rust/Cargo.toml` + `Cargo.lock` for a 3.x target only -- without it a `v3.0.0` tag publishes a binary reporting `3.0.0-dev`, since the version is compiled in from `env!("CARGO_PKG_VERSION")`. Cargo writes the lock (my hand-written version missed `intentd`) and the diff is BOUNDED to version lines. And a frozen remote is now a PRE-FLIGHT refusal rather than a push failure after the tag is cut. Canaried four ways in a real clone.

<!-- archived at the 19:29Z localfold: 0039 closed (ic built the class check at fd961437), the v3 cut path walked and its one defect fixed -->

## DOING

- **0039 ANSWERED WITH A MEASUREMENT, SENT TO ALL THREE, NOT CLAIMED (14:16Z).** I ran vc's proposed comparison by hand rather than proposing a design for it, and it returned two things nobody had.
  - **A FIFTH INSTANCE: `Arg.default`, 8 rows** (`st show`/`st edit` = `info`; `issues`/`todo`/`plugin`/`ext`/`claude rules` = `list`; `init` = the current directory name). `pub struct Arg` has no `default`. **It is the COINCIDENCE shape and that is worse than vc's four DIVERGENCES**: `intent todo` bare runs `list` correctly and `--help` shows it, hand-written, with no mechanical connection to the declaration it matches. **A divergence eventually meets a user; an agreement by coincidence never surfaces and drifts the first time either side is edited.** Seven of the eight are on families answering `not implemented yet`, so seven get hand-built from a declaration no code reads.
  - **THE CHECK AS SPECIFIED CANNOT BE BUILT: "refuse on any key no type reads" refuses ~70 keys.** Distinct unread: `Entry` 19, `Flag` 8, `Arg` 4, **`Target` 43 -- it reads `state` of 44 declared**, the tail being vc's and ic's one-off ratification prose. **It fails in the over-refusing direction, which is the one that gets a guard bypassed rather than fixed.**
  - **NO MECHANICAL DISCRIMINATOR EXISTS between a declaration and a note, and I went looking because a guard needs one.** Not count (`read_or_mutate` 112 decides behaviour, `observed` 93 is a measurement block); not value type (both strings). **The split is semantic, so the list must be AUTHORED -- which makes it ic's, not a rule.**
  - **THE MECHANISM: ic's exemption is RIGHT and is being INHERITED BY THE LEAVES.** `dispatch.rs:56-72` reasons `Table` is a register, correctly. `Entry`/`Flag`/`Arg` carry the declarations that decide what ships, and **all five instances are keys on those three.** Shape offered: `#[serde(flatten)] rest: BTreeMap` on those three only, `Table`/`Target` untouched, plus one test asserting `rest`'s key set equals a ratified list. Not `deny_unknown_fields` (untenable, and cc ruled the composition at `model.rs:328-330`); **not a grep -- `surface_check.sh` is blind to `aliases` because text search must know its needle, and a flatten asks serde what it ACTUALLY DESERIALIZED, the only version that cannot itself go stale.**
  - **cc, ic AND I ALL HAD 0039 IN OUR FOCUS LINES.** Three nodes, one class, one file. Sent to vc to arbitrate before anyone starts. **The measurement was the contribution; the build is not mine.**
- **~~THE BOUNCE -- vc's ISSUE 0039~~ ANSWERED ABOVE.** Original: **LIVE IN MY INBOX (11:58Z), AND IT WAS THE ONLY ENTRY I HAD NOT ANSWERED.** `dispatch-table.json` declares `aliases` on five entries, four of them `disposition: keep`; **`pub struct Entry` has no such field**, so it is structurally invisible rather than merely unread. Live consequence: `at done` and `at notdone` answer `unrecognized subcommand` in v3 while **v2 documents them in its own help**. **The part that is worse than the bug: `surface_check.sh` and `dispatch_ssot.rs` contain ZERO occurrences of `aliases`** -- the tool whose job is comparing binary to table cannot see it, because **an unknown canon key is not a mismatch, it is invisible**, so adding a field to the canon silently adds an UNCHECKED one. **Fourth instance of the class in three files** (`Flag.required`/`accepts`/`default`/`value`, `Entry.exposed_on_mcp`, `Entry.read_or_mutate`, now `Entry.aliases`), four fixes proposed, none closing it. vc's ask is a check comparing the canon's authored key set against the types' DESERIALIZED key set, refusing on any key no type reads. **MY OPEN QUESTION BEFORE I BUILD IT: `surface_check.sh` is ic's tool and the Rust types are cc's** -- the guard shape is mine by track record, the two homes it touches are not. Ask, do not claim.
- **WALKED THE v3 CUT PATH ON hv's "SOONER IS BETTER". TWO GATES STAND IN IT AND BOTH ARE hv's; THE THIRD WAS MINE AND IS FIXED.** Ran `int build release --dry-run v3.0.0` rather than reading it, then exercised what a dry-run cannot reach.
  - **THE FREEZE AND v3 ARE IN DIRECT CONFLICT, and it is structural rather than mechanical.** The release pushes `local` AND `upstream`; my own pre-flight refuses a frozen remote in the push set. Exercised with real output: `frozen_hit='upstream'`, so **a v3 cut aborts before the tag** -- the control working as designed. **But AC-11.1's evidence is `brew install` from the tap, and the tap formula points at a GitHub release asset, and GitHub IS `upstream`.** `local` is a Dropbox path nobody installs from. **There is no version of shipping v3 that does not push the frozen remote.** Sent to hv framed as the narrow question -- lift for the cut itself, one push, then close -- because **the freeze was called on cost PER COMMIT and a release is not a commit.** Deliberately built no exception.
  - **NO `## [3.0.0]` CHANGELOG SECTION; the cut refuses without one, and that is the FIRST gate to fire.** **I deliberately did not create a placeholder heading even though it would have gotten me further and looked like progress**: an empty `## [3.0.0] - in progress` makes the gate pass while the notes are absent, **converting a loud correct refusal into a silent one.** Same argument that keeps the tap empty. Content spans all four nodes, so not mine to write alone.
  - **`TARGET` is stripped of its `v` at parse (`v3.0.0` -> `3.0.0`), so my `[3-9].*` case glob DOES match.** A real risk, now settled by running rather than reading -- a `v`-prefixed target would have silently skipped the native stamp.
  - **THE STAMP WORKS AND ITS REFUSAL DID NOT -- FIXED AT `b8787ffc`.** Ran the stamp against a fixture archived from HEAD: manifest `3.0.0-dev` -> `3.0.0` and cargo updates **all four** members, which is the failure my hand-written sed had (three stamped, `intentd` missed). **But the bounded-diff refusal beside it asserted "every changed line is a version line", and a dependency bump IS a lone version line** -- so it accepted the single most likely resolver change, the exact thing it exists to refuse. A lone `serde` bump sailed through; it only ever caught changes touching a non-version line. Now correlates each changed version with the package name above it, refuses non-members by name, and **derives the member list from the lockfile itself** (a path package carries no `source` line), so there is no roster to keep in step and no `jq` dependency. **Proven in five directions against the SHIPPED functions, extracted rather than copied**, including an empty-parse guard.
  - **THE TEST IS WRITTEN AND PROVEN BOTH WAYS; IT IS STAGED IN SCRATCH, NOT LANDED, AND THE REASON IS TIMING ONLY.** `scratchpad/cut30/` holds a refactor extracting `lock_nonmember_changes()` (the comparison had to become a function to be testable at all -- **an embedded pipeline can only be grepped, and a grep cannot tell whether what it matched is correct**, which is how the defect survived) plus 9 bats tests. **Mutation run with the precondition verified: against a mutant carrying the pre-`b8787ffc` semantics, tests 1-4 and 9 still PASS -- including "a member-only bump is accepted", so the mutant still does its baseline job and is a mutant rather than a broken file -- and 5-8 go RED.** Those four are the fix. **Not landing while hv's suite runs: the addition goes into `release_sidecars.bats`, which the running suite may be executing.** Lands the moment the result reports.
  - **`install.md` WAS ALREADY WRITTEN AND MY BOARD SAID IT WAS OWED -- third stale record of mine today.** It leads with the brew-SHADOWS warning my TODO claimed was outstanding, and it is accurate: 0038 needed no entry because **cc fixed it at `d2b8e76d` while I was measuring**. TODO item struck below.
  - **I NEARLY FILED A FALSE REGRESSION AGAINST cc ON A BINARY 57 SECONDS STALE.** About to measure 0038 from `target/release/intent`, checked its mtime against cc's fix commit first: binary 15:24:26, fix 15:25:23. **Measuring would have reported the OLD behaviour and I would have reported their fix as not working.** Exactly what ic hit with a 14-minute-stale binary, at a margin small enough that no habit catches it. Read cc's source change instead and said plainly that is what I did.
- **AC-11.1 / AC-11.4: THE BLOCKER WAS NEITHER OF THE TWO THINGS RECORDED.** My board said "blocked on hv: the tap is an account action" -- STALE, the tap has existed since 2026-08-15T15:19:58Z and vc was right to strike it. vc said the rows sat behind a real version "and nothing else" -- necessary, not sufficient. **AT-11.1 is a clean-machine install transcript FROM A PUBLISHED TAG and AT-11.4 compares the sha of a PUBLISHED artefact; there is no v3 tag or release.** Publishing is gated on **0036** (the unmigrated refusal names `intent upgrade`, which v3 lacks) and **0038** (mine, 11:33Z: a migrated project cannot commit at all). **Two stale records pointing at two different wrong blockers, and the real one was neither.**
- **`brew services` waits on WP-08. hv holds the toolchain-pin decision.**
