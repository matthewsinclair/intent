---
node: dc
name: DevX Claude
role: worker
session_id: 482cf2fc-6b49-4a0d-8d76-38b3c981924c
heartbeat_at: 2026-08-17 17:06Z
status: active
focus: "**hv HAS RULED BOTH ASKS AND BOTH ARE DISCHARGED AND PUSHED. (1) THE HOIST: _"We go big bang and fix forward."_** -- option A, the migrator run and the PATH repoint are ONE operation, no staged window and no pre-built safety machinery. **The tool end does NOT become a WP or an AC**; a cutover action does not become a work package because it is scary. **cc's sharpening is the load-bearing half: under fix-forward the property is RE-RUNNABILITY, not rollback** -- rollback covers a failure the run DETECTS, and fix-forward must survive the one that kills the process mid-write, so a second run over a partial estate must reach the same end state as a clean one. It also gives stamp-last a second job: while the config still says 2.19.0 an interrupted estate is one v3 will migrate again, so stamping early leaves fix-forward nothing to fix forward with. **(2) `53f88757` -- v2 now REFUSES a project declaring a version above its own, at exit 2**, using the comparator moved to `intent_helpers` under Highlander; nothing allowlisted, because every exemption was already structural. Blast radius MEASURED: sixteen adjacent projects, all at or below 2.19.0, `version_gt` strict -- **a no-op across the whole local fleet today**, firing only on a v3-migrated project. **It reddened three of cc's live differentials within the hour and was right to** -- they shelled out to `bin/intent` over a fixture declaring 3.0.0, since they were written. **(3) `faaa2f0f` -- `INTENT_VERSION` now names the installed TOOL always**; `intent_config:50` had been overwriting it with the project's declaration inside `load_intent_config`, one identifier and two subjects, and it had silently disarmed the FIRST version of my own guard. Earlier today: **THE HOIST IS A TWO-ENDED MIGRATION AND ONLY ONE END IS BEING BUILT.** WP-10 converts the PROJECT end (config + files, cc + ic); NOTHING converts the TOOL end -- what `intent` on PATH resolves to -- and it is in none of WP-10's ACs. Measured: `which -a intent` returns THREE entries, all v2.19.0, v3 deliberately off PATH, so the day this repo's config says 3.0.0 four sessions point a WRITING v2 at a v3 project. Driven in a sacrificial 3.0.0 project: `upgrade` exit 1 _'refusing downgrade: project is at v3.0.0, target is v2.19.0'_ -- and `st new` exit 0 WRITING SEVEN FILES, `todo` exit 0 writing, `doctor` exit 0 reporting its own version, and the shipped gate LANDS the commit. **v2 KNOWS the project is from the future, says so in those words, and applies that knowledge at the one command that was already safe.** The asymmetry is the finding: v3-in-v2 REFUSES, v2-in-v3 WRITES -- guarded in the harmless direction, unguarded where md is a generated view and the writes are discarded at the next regeneration. **AND hv's STANDING RULE HAS AN UNNOTICED EXPIRY: 'v3 is NOT on PATH' and 'this repo is a v2 project' are the same rule stated twice, and the hoist falsifies the second** -- at the instant config.json says 3.0.0, v3-off-PATH stops being the safety measure and becomes the hazard. Sequencing call for hv, needed BEFORE the migrator runs here. **AC-10.9 NOW MEASURED ON BOTH ITS POPULATIONS AND IT HOLDS** -- the row says MIGRATED OR NOT and I had driven only the unmigrated half, third instance of narrower-than-I-said and again the POPULATION. The `critic` exit-code constraint went to cc, not hv: cc owns the exit codes under the WP-10 split. Issue estate measured for cc's scanner: 40 CLOSED, ZERO OPEN, and no closed-date field in the v2 format at all. Earlier, 15:47Z: **hv HAS CALLED US OFF TRACK: accelerate the hoist onto Intent3; hold new classes, new AC rows and new instruments until it lands; findings go in the commit that fixes them.** vc measured 210 commits touching no code against 33 touching Rust today. My share, stated plainly: three peer messages, an instrument, a MODULES row and a 40-line commit body to fix a manifest line that was wrong because I did not type one path into a `git commit --only`. **TOOK WP-10 AC-10.4 / AC-10.9 / AC-10.10** -- WP-11 is 2/4 and both remaining ACs need a publication, so the capacity was real and not a favour. **AC-10.9 AND AC-10.10 BOTH HOLD, DRIVEN THROUGH THE SHIPPED HOOK RATHER THAN INSPECTED.** An unmigrated project with v3 installed COMMITS (exit 0); the guards still REFUSE a missing `Z` and a future stamp, with a GOOD board landing first as the control. **BUT AC-10.9 HOLDS BY ACCIDENT AND THE FIX BREAKS IT: the gate makes exactly two `intent` calls, `info` (exit 0) and `critic` (exit 2, NOT IMPLEMENTED, so fail-open). The commands that refuse are not in the gate and the command in the gate does not refuse.** The day cc implements `critic`, a migration refusal at exit 1 reads as FINDINGS and blocks every unmigrated project. **AC-10.4 is UNMEASURED and needs the migrator to exist.** Earlier: the vendored-tree repair and AC-11.5 vendor arm landed at `36c8900c` + `5b88e2c7`; HEAD had been self-inconsistent since `58eee27f` because a re-vendor is two facts and `--only` committed one."
claims: [ST0056/11]
---

# DevX Claude (dc)

## D42 -- TIME. Read this before writing anything, anywhere.

**DB records have a timestamp field. That is the source of truth for time. Nothing else. Ever.** No clauses. hv ruled it four times and it was reinterpreted after three of them, twice by me inside ten minutes.

- **You never ask what time it is.** Not the OS, not `date`, not the filesystem, **and not the database either.** Asking SQLite and then writing the answer is still writing a time you obtained.
- **The stamp is applied BY the write**, at INSERT/UPDATE/UPSERT/DELETE. Read-then-write leaves a gap that two writers interleave in, so two records get stamped in the opposite order to the one they were written in.
- **hv's structural close, and it is the form to build against: NO cli or intentsvcs function TAKES a time.** Functions may RETURN times, and every one returned was set by SQLite on a record. **Direction is not symmetric -- IN is forbidden, OUT is fine**: a returned time is evidence a record was written; an accepted time is a second clock with extra steps.
- **THE RULE IS ABOUT SIGNATURES, NOT VALUES, WHICH IS WHY THIS VERSION WILL HOLD.** Every earlier statement of D42 asked where a value came from and whether its source was legitimate -- judgement calls this estate failed three times in one day from three nodes. **A time-typed input parameter is a defect by inspection.** Asking where a caller got a value is a discussion; asking whether a signature accepts one is a grep. **A signature that accepts a time is a standing invitation that gets accepted eventually**, no matter how careful today's author is.
- **The defect is always one step earlier.** Reaching for a clock means you are about to write a time into something that is not a durable record. The fix is never a better clock; it is not writing the time.
- **Not exceptions:** test fixtures; "only reading it"; **"but it came from the database"**; "it's just a label". The third fooled cc, vc and me independently -- **better provenance is not the absence of a confection.**
- **SCOPE, hv: devbin is NOT Intent** -- external, vendored, no db, does what it likes with time. My whole D42 directive resolved to a no-op, and reporting that beat inventing work to look responsive.
- **A board stamp is a label, not data, and nothing may read it as one.** The ordering that exists and cannot be fabricated is the **commit**. The `## (...)` heading is legacy, kept only because nodes parse the shipped format live.

## The truth model -- canon, held not restated

Ratified in `design.md` (D01 as reversed) and `data-model.md`, and deliberately not duplicated here, because a second home is how two copies drift. The three points that change what I DO: **the db is the durable SSOT and files are re-creatable secondary artefacts**; **the typed API is the only door in**, so conformance is by construction rather than by anyone checking; **migrations are normal** -- anything justified by "we can never migrate" rests on a constraint nobody made.

## DOING

- **hv RULED THE HOIST: _"We go big bang and fix forward."_ BOTH ASKS DISCHARGED, BOTH FIXES PUSHED.** Option A: migrator run and PATH repoint are ONE operation. **The tool end does NOT become a WP or an AC** -- hv carved it out to nobody and fix-forward is a directive against scaffolding for it. Relayed to all three peers.
  - **RE-RUNNABILITY IS THE PROPERTY, NOT ROLLBACK (cc's, and it is the sharper half).** Rollback covers a failure the run DETECTS; fix-forward must survive the one that kills the process between writes. So a second run over a partially-written estate must reach the same end state as a clean one -- idempotence over canon already on disk. **And it gives stamp-last a second job neither cc nor ic had credited it with**: while the config still says 2.19.0, an interrupted estate is one v3 treats as unmigrated and will migrate again; stamp early and a half-migrated project is one v3 believes is finished, so fix-forward has nothing to fix forward with.

- **`53f88757` -- v2 REFUSES A PROJECT FROM THE FUTURE, exit 2.** `version_gt` + `_vnum` moved to `intent_helpers` (they lived only in `intent_upgrade`, which is exactly why the dispatcher could not ask the question). **Nothing allowlisted: the guard sits past `--version`, past `help` and past the `GLOBAL_COMMANDS` exec, so every exemption a hand-written list would have argued for is structural** -- which is the test for whether it is a guard or an argument. Exit 2 matches cc's `critic` ruling, so v2 and v3 now speak one exit-code language to the gate.
  - **BLAST RADIUS MEASURED, NOT ASSUMED: sixteen adjacent Intent projects, every one at or below 2.19.0, and `version_gt` is strict** -- so it is a **no-op across the entire local fleet today** and fires only on a v3-migrated project. The one way it can surprise anyone is a STALE install meeting a newer project, which is the correct semantic and belongs in the release note rather than being discovered.
  - **IT REDDENED THREE OF cc's LIVE DIFFERENTIALS WITHIN THE HOUR AND WAS RIGHT TO.** `close_gate_parity.rs` shelled out to `bin/intent` over a fixture declaring 3.0.0 -- doing precisely what the guard exists to stop, since the day they were written. **A real fixture nobody planted, which is better evidence than the canary I would have built.**

- **`faaa2f0f` -- `INTENT_VERSION` NAMES THE INSTALLED TOOL, ALWAYS.** `intent_config:50` had been assigning the project's declared version to it inside `load_intent_config`: one identifier, two subjects -- the TOOL at `intent_config:17`/`bin/intent:41`, the PROJECT from line 50 on -- switched inside a function called for its side effects and stated nowhere. The declaration is now `PROJECT_INTENT_VERSION`, empty rather than defaulted, because "declares no version" and "declares the tool's version" are different facts.
  - **THE EVIDENCE IT HAD ALREADY COST SOMETHING, AND IT IS THE GENERAL LESSON: THREE SITES ALREADY RE-DERIVED THE TOOL VERSION LOCALLY TO WORK AROUND IT** -- `bin/intent:41`, `intent_info:44`, `intent_st:342` -- **each looking like ordinary initialisation rather than a workaround.** That is why `intent info` reports the right number today: not because the variable is correct, but because the display path stopped asking it. **A symptom that has been independently worked around three times reads as no symptom at all.** It also silently disarmed the FIRST version of my own guard, which compared 3.0.0 against 3.0.0 and passed -- in the direction that looks clean.
  - **Fixed at the CAUSE, and the three re-reads left in place deliberately**: each is a correct call to the one implementation rather than a second copy, so none is a Highlander breach, and removing them buys nothing while entering three files this change need not touch. Recorded so the next reader does not infer from three re-derivations that the variable is still untrusted. 62 tests across six bats files, 0 failed.

- **THE HOIST IS A TWO-ENDED MIGRATION AND ONLY ONE END IS BEING BUILT. This is the session's finding and it is a sequencing call for hv, not buildable work for me.** WP-10 converts the PROJECT end -- config + files, cc and ic. **Nothing converts the TOOL end** -- what `intent` on PATH resolves to -- and it appears in none of WP-10's ACs. The standing lesson already names it: _a migrator must not do half of a two-ended migration._ So `upgrade`'s contract is not "convert the project"; it is convert the project **and leave no writing v2 pointed at it**, or refuse to start.
  - **MEASURED, sacrificial project declaring `intent_version: 3.0.0`, v2 on PATH.** `upgrade` exit 1 -- _"refusing downgrade: project is at v3.0.0, target is v2.19.0"_. And then: `st list` 0, `info` 0, `doctor` 0 (_"doctor: intent v2.19.0"_, noticing nothing), `todo` 0 **and writing**, `st new` 0 **and writing SEVEN FILES** (the five ST docs + `steel_threads.md` + `todo.md`). The shipped gate then **LANDED** the commit.
  - **v2 KNOWS THE PROJECT IS FROM THE FUTURE, SAYS SO IN THOSE EXACT WORDS, AND APPLIES THAT KNOWLEDGE AT THE ONE COMMAND THAT WAS ALREADY SAFE.** `bin/intent_upgrade:93` already does the comparison with `detect_project_version` + `version_gt`, both existing helpers; `bin/intent` learns the TOOL version at :41, dispatches at :92, and never asks what the PROJECT declares. **An application gap, not a missing capability** -- ~6 lines at one site. Not mine to land: `bin/**` is sacrificial-worktree-only (three PATH entries resolve to it, four sessions live) and v2 maintenance is DEFAULT-DEFER. Whether it clears the show-stopper bar is hv's.
  - **THE ASYMMETRY IS THE FINDING: v3-in-a-v2-project REFUSES (exit 1); v2-in-a-v3-project OPERATES AND WRITES (exit 0).** The refusal exists in the direction that costs a user nothing and is absent in the direction that destroys their work -- under D01-as-reversed the md IS a generated view, so v2's seven files land where the db has no row and the next regeneration discards them silently. **`doctor` is the sharpest instance**: its whole job is reporting unmigrated residue and it reports its own version instead.
  - **AND hv's STANDING RULE HAS AN UNNOTICED EXPIRY.** "v3 is NOT on PATH" and "this repo is a v2 project" are the same rule stated twice, **and the hoist falsifies the second.** At the instant config.json says 3.0.0, keeping v3 off PATH stops being the safety measure and becomes the hazard. Cheap to honour, unrecoverable to miss, and it must be in front of hv BEFORE the migrator runs here.
  - **THE BLAST RADIUS IN MY OWN LANE IS MEASURED AND IT IS SMALL, WHICH IS WORTH KNOWING BEFORE THE FLIP RATHER THAN DURING IT.** The 12 repo-local gate guards are **unaffected** -- every one is invoked by explicit script path, and `int cli` / `int dogfood` / `int macos` / `int build` all resolve a binary by explicit path too. **Exactly two commands resolve `intent` from PATH and both fail SAFELY**: `int check` (`intent critic --languages`, which v3 answers at exit 1 -- a clap arg error, since v3's `critic` takes a positional) **DIES loud**, and `int docs` (`intent agents sync` exit 2 unimplemented; `intent treeindex` exit 2 **RETIRED in v3**) **warns and continues**, naming what was not regenerated.
  - **AND `int check` ALREADY CARRIES THE CORRECT DIAGNOSIS -- I PREDICTED IT WOULD MISLEAD AND IT DOES NOT.** Its `die` at `lib/cmd/check:59` names _"a v3 binary shadowing v2 on PATH"_ as the known cause, and the comment above it says so too. So the one command that fails hard at the hoist explains itself correctly. **The `int docs` treeindex arm is the only real work: v3 RETIRED the command, so that arm gets DELETED at the flip, not repaired** -- fail-forward, no compat shim -- and it cannot be done before the flip because v2 still has `treeindex` and `int docs` works today.

- **WP-10 (AC-10.4 / AC-10.9 / AC-10.10). AC-10.9 IS NOW MEASURED ON BOTH ITS POPULATIONS AND HOLDS; AC-10.10 HOLDS; AC-10.4's SUBJECT IS PINNED AND ITS CONSTRAINT FOUND.** Driven through the shipped `pre-commit.sh` with `intent` resolving via a PATH scoped to the `git commit` invocation alone -- never exported, session PATH verified after.
  - **AC-10.9 SAYS "MIGRATED OR NOT" AND I HAD DRIVEN ONLY THE UNMIGRATED HALF, THEN REPORTED THE ROW AS HOLDING. Third instance of the thing I have being narrower than the thing I said, and the third time it was the POPULATION rather than the premise.** Found by going to the contract instead of re-reading my own board -- which is the other rule, also third instance: **a board says what I was thinking, the AC says what was agreed.** Now driven as a full 2x2 (v2/v3 x 2.19.0/3.0.0), every cell a real `git commit` through the shipped hook. **All four LAND.** The two v3 cells are the criterion and it holds on both; the two v2 cells are the control, and they discriminate -- v3 refuses `st list` at 2.19.0 (exit 1, "this project has not been migrated") and accepts it at 3.0.0 (exit 0, renders a table), so the harness can tell the cells apart. **A matrix where everything commits and nothing discriminates is indistinguishable from a matrix that never ran.**
  - **AC-10.10 HOLDS, three arms, and ARM 1 IS THE LOAD-BEARING ONE.** Good stamp LANDS (exit 0); no trailing `Z` BLOCKED; a 2099 stamp BLOCKED, both carrying the real clock-guard text. **A guard that blocks everything and a guard that blocks the right thing are indistinguishable if you only try the bad case.**
  - **THE RELAYED HAZARD IS FALSE IN ITS SPECIFICS AND A WORSE ONE SITS UNDER IT.** "The refusal returns exit 1 and the gate reads it as findings" is true about the refusal and false about the gate. **The gate makes exactly TWO `intent` calls -- `info` and `critic <lang>` -- and the whiteboard guards make NONE.** Measured: `info` 0, `critic` 2 (`known command that is not implemented yet`), `st list` 1, `wp list` 1, `doctor` 1. **The refusing commands are not in the gate; the gate's command does not refuse, it is unbuilt.**
  - **SO AC-10.9 IS SATISFIED BY AN UNIMPLEMENTED FEATURE AND IMPLEMENTING IT BREAKS IT.** The hook declares its own contract in its header: 0 clean, 1 findings BLOCK, 2 invocation error FAIL-OPEN. **vc's corollary exactly: a control correct today BECAUSE OF AN OPEN DEFECT is one that whoever fixes that defect will silently break**, and nothing connects `critic`'s implementation to this gate's contract.

## TODO

1. **AND THEN THE CONSTRAINT I SENT TURNED OUT TO BE TOO WEAK AND SLIGHTLY WRONG-SHAPED. BOTH CODES ARE WIRED BACKWARDS AGAINST THE SHIPPED GATE, AND CHOOSING A CODE FOR THE REFUSAL CANNOT FIX IT.** Provable from two committed artefacts, no rebuild. **v3 declares** (`intent-cli/src/guide.rs:118`): `0` success; **`1` EVERY failure, explicitly including usage errors** (_"exits `1`, not clap's default of 2"_); **`2` `critic` FINDINGS -- and also unimplemented commands**, closing with _"So `2` is ambiguous and the stderr line is what tells the two apart."_ **The shipped gate branches** `0)` pass, `1)` BLOCK, `*)` fail-open. So: **findings=2 -> the gate FAILS OPEN and silently stops blocking on real findings**, and **every-failure=1 -> the gate BLOCKS and calls a migration refusal or an unknown flag "findings at severity >= X".** Both inverted. **The reason a code assignment cannot resolve it: findings and refusals need OPPOSITE gate behaviour -- block versus fail-open -- and v3 gives them the SAME code.** The value space is the defect.
   - **v3 HAS ALREADY NOTICED AND RESOLVED IT WITH A STDERR STRING, AND THE GATE NEVER READS STDERR.** So the discriminator exists and is structurally invisible to the only consumer that must act on it. **My own rule arriving from the other side**: parsing another program's rendering makes it an API nobody declared -- except here the CLI is _requiring_ a consumer to parse rendering, because the code cannot carry the distinction. `exit_code_consumers.rs:94` already tracks one instance (`|| true`, "will keep hiding real errors once `critic` is built. Latent rather than live").
   - **THIS IS WHERE IT BECOMES MINE: AC-10.4 SAYS THE HOOKS ARE BYTE-IDENTICAL PRE/POST.** The hook is `lib/templates/hooks/pre-commit.sh`. **If it survives the migration unchanged -- exactly what my AC requires -- the post-hoist gate is still branching 0/1/\*, so v3's `critic` must conform to THAT contract and not to guide.rs.** AC-10.4 and v3's declared exit codes are in direct conflict, and satisfying mine constrains cc's. Sent; no ask attached, it is cc's surface and hv's ruling if one is needed.
   - **UNMEASURED AND SAID SO: v2's `critic` exit code on real findings.** Four probes; I could not make the headless shell runner emit a finding even from a fixture built out of `IN-SH-CODE-001`'s own Detection heuristic, and the probe **refused to report numbers from two cases that failed to discriminate** -- uniformity across cases that should differ is evidence about the instrument. Likely ST0039's strict-proxy contract. **So "v2 returns 1 on findings" is the gate's DECLARED contract, not something I watched happen**, and the collision does not depend on it.

1. **THE `critic` EXIT-CODE CALL WENT TO cc, NOT hv -- AND THE ROUTING WAS THE DECISION.** My board said "put it to hv"; between writing that and acting on it, hv put cc on WP-10 and **cc's own board claims the exit codes** under the split with ic. So it needed no ruling: it is a constraint on a surface a peer owns, and a peer can accept it or escalate. Re-measured before sending rather than quoted from the fold -- `critic shell` and `critic nosuchlang` both exit 2, `info` 0, `st list` 1, `doctor` 1, `upgrade` 2. **The constraint: at the `critic` surface a migration refusal must return 2, not 1**, or the day cc implements it every unmigrated project stops committing, because the gate reads 1 as findings. It follows from the hook's OWN header (0 clean / 1 findings BLOCK / 2 invocation error FAIL-OPEN), so it does not need the broader 0044/0045 ruling. **And under the moratorium it needs no new artefact: `intent-cli/tests/exit_code_consumers.rs` already exists as exactly that policy file.**

1. **AC-10.4 -- THE SUBJECT IS NOW PINNED AND THE REAL CONSTRAINT IS FOUND, THOUGH THE COMPARISON STILL WAITS ON THE MIGRATOR.** Subject at HEAD, read from the index: **four files, 8039 bytes** -- `settings.json` (845), `scripts/post-tool-advisory.sh` (2432), `scripts/require-in-session.sh` (2555), `scripts/session-context.sh` (2207), sha-256 recorded in the commit that lands this. So the moment a migration runs, AC-10.4 is one command rather than a fresh investigation.
   - **THE CONSTRAINT cc AND ic NEED BEFORE THEY WRITE IT: v2's own upgrade REWRITES this subject by design.** `bin/intent_upgrade:156` invokes `intent claude upgrade --project-dir . --apply`, the sole canon engine. **If v3's migrator does the same thing, AC-10.4 fails by construction** -- and the naive reading of the row ("why would a migration touch `.claude/`?") is exactly what would let it.
   - **AND THE ROW'S CRITERION IS NECESSARY BUT NOT SUFFICIENT, which is worth knowing even though the gap is currently harmless.** `settings.json` contains nothing but three `intent claude hook ...` invocations, so byte-identity is a claim about the FILE and not about whether the hooks still work: a migration that leaves it untouched and drops one subcommand satisfies the row and breaks every session. **I checked and it does NOT reproduce** -- v3 answers `claude hook session-context` and `claude hook require-in-session` at exit 0, same first line as v2, in a 3.0.0 project. So it is closed by cc having wired it, which is a fact about the implementation rather than about the criterion. Recorded, NOT filed and NOT proposed as a row change -- moratorium.

1. **MY EVIDENCE FOR 10.9 / 10.10 IS EXPLORATORY AND UNREACHABLE, AND I AM NOT FIXING THAT BY ADDING FILES.** The drivers live in a session-scoped scratchpad, so my previous fold's citation of `scratchpad/drive_hook.sh` was **a claim about a file that no one else can open and that I will lose** -- my own rule (_a file without a commit is not a referent_) pointed at my own board. **The contract already answers where the durable form goes and it is not a shell script**: AT-10.9 cites `intent-cli/tests/exit_codes.rs` (green), **AT-10.10 cites `intentsvcs/tests/migrated_guards_still_refuse.rs` and is still `to-write`** -- so AC-10.10 is "measured and holds" on my board with no artefact in the contract, which is the vacuous green inverted: a real measurement nothing durable records. **Owed, and deliberately not discharged tonight** -- that file lands in the crate cc and ic are both editing (`legacy.rs`, `migrate.rs`), and adding shell instruments instead would be the immune system feeding itself under the exact moratorium that forbids it. The measurements and their method are on this board in full, which is what a reproduction actually needs. **Do NOT move an AT status without committing first (issue 0033 destroys the note).**

1. **Queued upstream in Devbin, small, NOT an instrument: `devbin upgrade` writes the manifest with the `lib/install` it has just replaced.** So a field added to the header is absent after the very upgrade that introduces it; two passes fix it, and **the pass that produces the correct record is the one whose output reads `0 updated, 27 unchanged, Already up to date`.** A tool reporting that nothing happened while rewriting its own provenance record. Fix with the reasoning in its commit body; hv has held new class entries.

1. **WP-11 (Distribution) -- MINE, WIP, GATE BLOCKED 2/4 (AC-11.1 + AC-11.4), AND BOTH NEED A PUBLICATION AND NOTHING ELSE.** hv on the cut: _"I hope so! But I'm waiting on VC, IC, and CC to sort that out."_ So the last two ACs are parked on other nodes, not on buildable work of mine.
   - **THE BLOCKER SET WAS STALE ON MY BOARD AND CORRECT IN `install.md` -- FOURTH INSTANCE, and this time the artefact was right and the memo was wrong.** My board still named **0043**, which closed on 2026-08-16 and whose publication hold **I lifted myself at `61724664`**. `install.md:5` has said ONE hard hold (0036) since. **Re-derive from `install.md` and `intent ac gate`, never from this board.**
   - Live gates: **0036** (the refusal's remedy names `intent upgrade`, which v3 lacks), the **`## [3.0.0]` CHANGELOG section** (absent; head is `## [2.19.1] - in progress`), and **hv's freeze call**. `Cargo.toml` is `3.0.0-dev` and `publish` refuses a dev formula, held to WP-12 deliberately.
   - **UNMEASURED AND MINE: `stage` COPIES from `target/release` and nothing proves those bytes were built from the commit it records.** `cmd_prepare` is stage/sign/notarize/checksum with **no build step**, and `grep` for `stat -f|mtime|newer|%ct` across `cmd/macos` returns nothing. Right now `intent` is 39 hours newer than `intentd` on disk. **AC-11.4's fix compares provenance to the TAG; it never asks whether the binaries were BUILT from it**, so a clean tree is silent about staleness. Same "consistency is not correspondence" one level in.

1. **0048 -- CARRIED. AWAITING hv: the branch is unpushed and off main, and merging it is hv's call, not mine.** Devbin `issue-0016-dropped-status-carry`: `c432e27` (issue 0016) + `c8a7dc7` (the four patches, the fifth instance, the correction, 22 tests). **ic's two asks are both in the issue text** -- the false-green stated as a MISSING ARTEFACT rather than a bug (additive, no consumer to migrate, which is the first thing an upstream maintainer asks), and the zsh `path`-clobber warning, in prose rather than only in a script, because anyone re-running the measurement hits it.
   - **INTENT IS DELIBERATELY LEFT UNSYNCED and this is the part to re-decide rather than drift on.** `bin/.devbin/**` here still carries the UNCORRECTED check -- the one that only fires on `<cmd> all`. Syncing now would fix a real gap in this project's own gates, which are single-gate almost always (`int test shell`); it would also re-diverge from the manifest in a NEW way, one week after hv ruled against exactly that. **hv's ruling wins on the direction; the cost of honouring it is that this repo keeps a known-narrow check until upstream lands.** Written down because a deferral recorded in one place has its precondition met somewhere else.
   - **`int vendor` stays here regardless** -- it is the detector, not the fix, and the manifest header promises a detector that consumers cannot reach. Raised upstream in 0016 as a candidate builtin (`devbin vendor`), separable and not blocking.
1. **THE REFERENT -- vc's issue 0049. I RULED IT UPSTREAM AND hv REVERSED ME IN ONE LINE. IT IS INTENT-SIDE, AND IT NEVER SHOULD HAVE BEEN A QUESTION.** The consistency half already landed upstream in `c8a7dc7` and is unaffected. **The referent half belongs in `bin/.devbin/cmd/` -- OURS, per the manifest header I quoted in my own issue text without applying: `config.yaml`, `cmd/` and `help/` BELONG TO THE PROJECT.** Eleven handlers already live there. So it needs no devbin change, no manifest divergence, and no upstream decision to wait on.
   - **THE ERROR WAS DRAWING THE WRONG CONCLUSION FROM A CORRECT MEASUREMENT.** I measured that `lib/` has ZERO git invocations, and treated "devbin must not depend on git" as a constraint to design around and file for a ruling. **It is not a constraint -- it is a signal that the concern is not devbin's at all. Intent knows it is a git checkout; a generic gate runner has no business knowing what a commit is.** I half-noticed: refused to build it, flagged the property, and filed the question anyway. **Filing a question is still putting it on someone else's plate**, and half-noticing an anomaly without following it one step further is the shape that cost this estate four separate findings today.
   - **Devbin issue 0017 WITHDRAWN** (one commit, unpushed branch, text preserved at `scratchpad/withdrawn-0017.md` -- the incident write-up in it is worth reusing here). The carry itself is untouched and added **zero git and zero `intent` invocations to devbin's core**; the two files that call `intent` were already calling it upstream before I touched them.
   - **THE STAMP CANNOT GO IN THE SEAL, and this is the correction vc accepted.** `.errors` is empty-exactly-when-a-completed-run-was-green (`write_errors_file` truncates at rc=0, and the file states the consequence itself twenty lines later), so writing a sha into it makes every green run read as red. vc had filed "stamp it into the log" and it would have inverted the one invariant the subsystem exists to hold. **It belongs where it is READ -- at the point of report -- because hv read `FAILED: rust` in a summary, and information buried in a log you only open once you already distrust the verdict arrives after it was needed.**
   - **vc's precedent, which I did not have and which removes the strongest objection: `print_run_verdict`'s `verdict:` line already fires whenever `keep=1`, independent of rc.** So printing `measured:` on green is an addition to an already-unconditional block, not new behaviour on the green path. **And printing on green is the point** -- vc's closing observation, buried at the bottom of their message and the more important half: that run produced four stale GREEN legs nobody has any reason to question. A red gets investigated and self-corrects; a green certifying a tree nobody has is believed and never revisited.
   - **ANNOTATE, NEVER SUPPRESS -- held, and cc gave it the sharpest defence.** Two of the five tests cc had to change were asserting behaviour hv's ruling retires, and `close_gate_parity` was the clearest statement in the estate of exactly the wrong semantics. **The red was noise about WHICH TREE and signal about WHAT.** A guard that had downgraded it would have lost the second.
   - **`LATEST_*` ARE ALREADY RELATIVE SYMLINKS** (`runlog:146,148`), so ic's "simpler and better" option was already the case -- measured rather than redesigned. **The residual is that the pointer answers WHICH run, never WHOSE, and it cannot be made to answer ownership without breaking empty-means-green.** So ownership is answered by the command PRINTING the path as it writes it, which `run_gate` already does, and the design says so out loud instead of leaving it an assumption. Cheaper than a `.RUN.meta` sibling and adds no artefact.
   - **The root cause it addresses is NOT the one it was first attributed to, and I measured that after landing the fix.** `int test shell` against a deliberately failing suite returns rc=1 AND seals a consistent non-empty `.errors` -- the two sources agree, so ic's control C describes a state this gate does not produce. C still covers a class and E is reachable from any killed run. **The incident's mechanism is a human aggregating two legs, twice, with no artefact stating the verdict of a run.** CI checked and clean on the same class, by reading the invocation rather than the comment above it.
1. **WP-11 (Distribution) -- MINE, WIP. THE PACKAGING HOLD IS DONE; everything remaining here is blocked on a publication, not on buildable work.**
   - **BUILT AND PROVEN: `int macos <doctor|stage|sign|notarize|verify|checksum|prepare|formula|publish|env|store-creds>`.** Signing acts on STAGED copies in `target/dist`; `prepare` runs the pass. `publish` uploads, **re-downloads what it uploaded and hashes THAT**, and only then ships the formula; on a mismatch the release stays and no formula ships. Four refusals canaried both ways.
   - **UNEXERCISED SURFACE IS ONE CALL, `gh release create`.** Closed since: formula is valid Ruby and **lints clean at a tap path**; `curl -fsSL` follows redirects, hashes identically twice, 404s without writing; **tap clone, `Formula/intent.rb` write and commit all succeed against the REAL tap, nothing pushed.** No release here has ever carried an asset, so the upload cannot be rehearsed without publishing.
   - **AC-11.1 + AC-11.4 ARE BLOCKED ON A PUBLICATION, AND THE BLOCKER HAS NOW BEEN WRONG ON THIS BOARD THREE TIMES.** Not the tap (live since 2026-08-15T15:19:58Z, deliberately empty, one README saying why -- verified again 19:2xZ, `isEmpty:false` means "has a commit", NOT "has a formula"); not "a real version and nothing else" (necessary, not sufficient); and no longer 0038 (cc fixed it at `d2b8e76d`). **The live set is: 0036, 0043, the `## [3.0.0]` CHANGELOG section, and hv's freeze call.** The mechanism existing is not the criterion -- **a better-built mechanism is no more a satisfied AC than a built one was.**
   - **HELD until WP-12: `int build release` gaining `Cargo.toml` to its sidecar sync** (wiring it now makes a v2 release stamp its version into `Cargo.toml`). **`brew services` BLOCKED on WP-08.** ~~Install/upgrade docs still owed~~ **-- WRITTEN, at `intent/st/ST0056/install.md`, and it leads with the brew-SHADOWS warning. This TODO was stale and I only found that by going to write the thing.**
1. **Issue 0036 -- `brew install` SHADOWS a v2 install.** brew is PATH position 1, the v2 symlinks are 17 and 19. A v2 user meets the v3 unmigrated-project refusal without asking for anything, and its remedy names `intent upgrade`, which v3 lacks. `migration.md:3` says the migrator IS that verb, so it is WP-10 unbuilt. **vc ruled the control belongs on the BINARY as a build-time invariant, not in `publish`** -- so it is theirs, and WP-11 stays decoupled from WP-10.
1. **MEASURED CLEAN, do not re-investigate: `int prepush` 19s; warm `cargo test --workspace` 22s for 331 tests.** Both were my guesses at the friction and both were wrong. **ic asked me specifically NOT to optimise `int build cli` (25-37s) -- that cost buys correctness**, after a 14-minute-stale binary reported findings cc had already fixed and read as a regression.
1. **`testkit::project_fixture()` -- DEFERRED, AND NOT FOR THE REASON I QUEUED IT.** vc's urgent justification was that no fixture carries a non-default `st_prefix`. `retired_st_prefix.rs` now does -- but it is **UNTRACKED**, a peer's in-flight work, so it is not evidence about the repo and I nearly reported it as one. What remains is a Highlander tidy-up over 12 test files in two crates, and the Rust tree has ten files under active edit tonight. **Wrong time, low urgency, high conflict; hand it to cc rather than reaching into their tree.**
1. **Issues 0030 / 0031 (`intent upgrade`) -- DEFERRED under DEFAULT-DEFER.** 0030: backup dirs stamped in LOCAL time, so oldest-first retention deletes the newer artefact across a DST fall-back. 0031: `--backup-dir` basenames straight into `.backup/`.
1. Open for others: **`intent/.cache/`** (cc, D21); **`core.hooksPath`** (deferred, collides with `intent claude upgrade`); the **`bin/` boundary** (hv); ~~AC-12.1's wording~~ **RULED 2026-08-15: vc adopted my rewording essentially verbatim -- _"nothing in the repo EXECUTES or EMITS a `bin/` intent script path"_ -- with the four classes named in the row and the 133 historical records marked explicitly OUT OF SCOPE.** The parity-fixture question is recorded as a WP-12 call rather than answered.

## Watch-outs

Facts about this estate, not reminders. Everything amounting to "remember to" is worthless here -- three nodes broke rules they had personally written, on the day they wrote them.

- **A PATH-FILTERED `git log` IS A SUBSEQUENCE, AND ADJACENCY IN IT IS NOT ADJACENCY IN HISTORY.** I ran `git log -8 -- intent/issues/OPEN`, read the rows as consecutive commits, and told THREE peers "one commit earlier the population was 26". Measured: the commit before the clear holds **23**, and the one holding 26 is **FIFTEEN** commits back (`git rev-list --count`). vc caught it; I verified it independently and it was worse than they diagnosed. **The trap is that a path-filtered log is exactly the tool you reach for to see how a path evolved, so the rows genuinely ARE that path's history -- but the DISTANCES between them are not history's distances, and nothing in the output says so.** The spelling that cannot lie is `<commit>^`: a claim about adjacency is read from the DAG, never from a projection of it. **Fourth instance of the thing I have being narrower than the thing I said, and the first where the population was COMMITS rather than files** -- so the class is not "count the files carefully", it is that every filtered view silently redefines the population and the filter is the part nobody restates.
- **I WROTE A LABEL ASSERTING A RESULT AND THE RUN DIRECTLY ABOVE IT SAID OTHERWISE.** Probing whether devbin resolves `intent` from PATH, I appended `echo "(empty above = nothing resolves intent via PATH)"` -- a hardcoded conclusion, in the same command as the grep that then found **five**. Nobody was misled because the contradiction was two lines apart, which is the only reason this is a note and not an incident. **It is my own recorded rule applied to other people's code and broken in my own within the hour: a label that states the expected result is a comment, and a note cannot notice.** The mechanism is to print the COUNT, never the verdict -- `grep -c` and let the number speak, because a zero and a five render differently and a sentence does not.
- **COMMIT BEFORE ANY `intent at` STATUS CHANGE (vc's announce, 2026-08-16). `intent at red|green|na` DESTROYS THE ROW'S NOTE** -- issue 0033, measured at 1,560 bytes down to 106 on one row, and **`intent at lint` says `ok` immediately afterwards**, so the contract's own linter cannot see it leave. **The transition graph doubles it: `to-write -> green` is refused, so recording a passing test costs TWO rewrites.** 14,253 characters stand in front of a status change across 34 rows. **This is mine to hold because I move AT rows for WP-11.** A committed note is recoverable with `git show`; one written and moved in the same session is gone.
- **zsh TIES `path` TO `PATH`, SO `while read -r want path` DESTROYS PATH ON THE FIRST ITERATION -- AND THE BROKEN INSTRUMENT REPORTED MAXIMUM ALARM.** Measured the devbin manifest divergence and got **all 27 entries diverged**; `shasum` and `awk` were `command not found` from iteration one, so every comparison failed and every file reported as drifted. **I was one step from filing an issue saying the entire vendored tree had been modified.** `path`, `cdpath`, `fpath` and `manpath` are all tied to their uppercase array counterparts -- never use one as a loop variable. **The general half is the one to keep: a wrong ZERO certifies absence, and a wrong MAXIMUM certifies catastrophe, and the second is far more persuasive because it looks like diligence rewarded.** The control that caught it is the same one as always -- the corrected run leaves 23 files MATCHING, and it is the matches that say the tool works, not the mismatches.
- **MY SHELL IS zsh, AND zsh DOES NOT WORD-SPLIT AN UNQUOTED `$var`. IT BIT TWICE IN ONE EVENING AND BOTH TIMES IT LOOKED LIKE A FINDING ABOUT SOMETHING ELSE.** `for t in "test rust"; do bin/int $t; done` passed `test rust` as ONE argument, so four gates reported `exit=2 / unknown command` and I nearly recorded four broken gates with the wrong signature. Then `git commit --only $PATHS` sent the whole list as a single pathspec. **The mechanism: never expand a list through an unquoted scalar -- write the arguments out, or use an array.** The bash idioms in this estate's own scripts are not portable to the shell I type into.
- **THE ONLY macOS CHECK THAT MEANS ANYTHING IS A QUARANTINED COPY.** `stapler` reports no ticket on a bare Mach-O -- correct, there is nowhere to put one. `spctl -a -t exec` says "does not seem to be an app" -- correct, that policy is for bundles. And **`codesign --verify --strict` RETURNS 0 ON AN AD-HOC BINARY WE DID NOT SIGN**, because an ad-hoc signature is a valid signature. Use `spctl -a -t open --context context:primary-signature` on a copy carrying `com.apple.quarantine`.
- **`target/release/` IS SHARED MUTABLE STATE -- WHICH IS WHY NOTHING SIGNS THERE ANY MORE.** A peer's `cargo build --release` silently replaced a Developer ID signature with the linker's ad-hoc one and de-notarised a shipped binary inside the hour, with no signal and every artefact of the proof still reading as valid. Fixed structurally: `stage` copies to `target/dist` first. **Never sign anything in a directory someone else writes.**
- **SIGNING MUTATES THE BINARY; NOTARISATION DOES NOT. SIGN BEFORE YOU CHECKSUM.** A sha256 taken one step early **does not fail for us -- it fails for every `brew install`**, where we have the least visibility.
- **A CANARY PROVES NOTHING UNTIL YOU CONFIRM THE FIXTURE REACHED THE BRANCH.** I planted a stale sums file on already-notarised artefacts, so the command correctly PASSED and my check reported "BUG" about a branch that never ran. **A red-looking result from a green run reads exactly like a real defect** -- the direction nobody watches, because it feels like diligence working. Canary both ways.
- **GIT HANDS A HOOK A TEMPORARY INDEX DURING A PARTIAL COMMIT, AND REWRITES THE REAL ONE AFTER THE HOOK RETURNS.** `GIT_INDEX_FILE=.git/next-index-<pid>.lock`. So a pre-commit hook's `git add` correctly reaches the commit and CANNOT correct the real index -- anything it writes there is discarded. **This is why issue 0028 has no pre-commit fix**, and I know because I built one and measured it doing nothing.
- **MY OWN WATCH-OUTS ARE NOT CONTROLS AND I SHOULD STOP EXPECTING THEM TO BE. FOUR BROKEN IN ONE AFTERNOON, TWO WITHIN THE HOUR OF WRITING THEM.** A **masked exit status** -- `$?` after `binary | head` is the PIPELINE's, so I recorded the v3 refusal as `exit=0` and started composing an issue around a silent failure that does not exist (it is 1). **awk over a fixed-column format** -- `$1` collapses `git status --short`'s leading space, so a worktree-only ` M` reads as staged; it inflated an index count 9x. **A fabricated board stamp** -- `17:52Z` composed from context while the clock read `18:56Z`, and it carried a `Z`, landed in the past and increased monotonically, so **it would have passed all three guard checks.** **Each needed a mechanism and got a paragraph.** The mechanisms: capture output then read `$?`; read `${line:0:1}` for the index column, never a field splitter; **read the clock into a shell variable and SUBSTITUTE it -- a stamp I type is a stamp I composed.** Fifth, 2026-08-16, inside a test I wrote to catch exactly it: the disabled-gate extractor's needle was `[a-z-]`, so my own first mutation -- a gate named `clippy2` -- was **silently not seen**, and the test passed over the change it exists to catch. **A needle that skips the names nobody has invented yet is the allowlist lesson, one file over, on the same day.**
- **A FILE WITH A KNOWN DEFECT MAKES EVERY ADJACENT THING LOOK LIKE THAT DEFECT.** I had the sentence written filing a v2-path leak from `transitions.rs:264` -- the file AC-00.9 named for leaking PM state -- **and the field sits on the `Unbuilt` variant with ZERO read sites.** Find the read site before naming a leak.
- **A `case` GLOB AND A GIT PATHSPEC BOTH CROSS `/`, SO `a/*/c.md` IS NOT ONE LEVEL DEEP -- IT IS EVERY LEVEL.** Measured here 2026-08-16, because I did not believe either without running it: `git ls-files -- 'intent/whiteboard/*/wip.md'` matched **21** files, **SIXTEEN** of them under `.history/`; with `:(exclude)intent/whiteboard/*/.history/**` it matched 5. `**` returned the identical 21, so the two are equivalent at this shape and the `*` reads narrower than it is. **It fails in the over-refusing direction, which is the one that gets a guard bypassed** -- and it had already bitten the clock guard's port, where the trailing `**` on the exclude was dropped and the false-positive control did not catch it because that control's own fixture was missing. Never write a `*` in a path pattern meaning "one segment".
- **`--date=format:` IGNORES `TZ`; `--date=format-local:` RESPECTS IT.** `TZ=UTC git log --date=format:` prints LOCAL time with a `Z` appended -- wrong by exactly the offset and looking perfect. `git log` is local by default and is the usual source of that error.
- **MY BOARD IS A MEMO; THE AC IS THE CONTRACT -- TWICE NOW, AND THE SECOND WAS FOUND BY OBEYING THE FIRST.** My board's TODO stated what remained on WP-11 and did not mention AC-11.3 at all; `acceptance.md` had AT-11.3 `to-write`, naming a file that did not exist, in my own work package. **A board says what I was thinking when I wrote it; the contract says what was agreed.** The first instance: AC-11.4 already ordered the staged-copy restructure, conditional on hv ruling the matrix -- which hv did. I re-read my board instead and rebuilt an agreed case from scratch. **A deferral recorded in two places has its precondition met in only one, and the copy you re-read on a bounce is the wrong copy.**
- **A GUARD MUST BLOCK ON WHAT THE COMMIT ADDS, NOT ON THE WORKING TREE.** `provenance_check.sh` globbed the tree, so one node's untracked mid-generation file froze every node's commits on paths they had never touched. **A guard that must be bypassed is a guard nobody keeps.** Hold the commit and diagnose; never reach for `--no-verify`.
- **`--only` COMMITS WHAT YOU NAME, AND `--amend` IGNORES IT ENTIRELY.** A move is two facts; naming the new path leaves the deletion staged. And `--amend` with no pathspec re-commits the WHOLE INDEX -- ic's swept 19 files including peer boards. **A sweep does not move a file, it SPLITS A CHANGE**: a method and its test are one unit, each half reads as finished alone, and HEAD stopped building while every worktree stayed green. **Verify at HEAD with `git ls-tree`, and read `git status --short`, not the diff.**
- **NEVER mutate `bin/**` or `tests/**` in place.** `~/.local/bin/intent` AND `~/bin/intent` both symlink to `bin/intent`; several sessions are live. Sacrificial worktree only. **`bin/.devbin/**` and `native/**` are safe** -- and devbin is vendored external code, not Intent's.
- **THE FRICTION cc HAS STOPPED NOTICING, IN THEIR WORDS: FOUR OF US COMMIT INTO ONE CLONE**, so their worktree changes under them while they work and a peer's push lands between their two pushes. **`int postcommit` and the `prepush` range defect are both in that family** -- shared-clone concurrency, not speed. Worth treating as a class rather than as incidents.
- **NEVER `git pull --rebase` IN THIS SHARED TREE.** Push; if rejected as non-fast-forward, coordinate. A peer's `.git/index.lock` means a peer is running git: WAIT, never remove it.
- **THIS REPO IS A v2 PROJECT AND THE v3 BINARY REFUSES HERE BY DESIGN.** Every measurement taken with the v3 binary inside this tree measures the refusal path. `int dogfood` exists so there is a v3 project to measure against.
- **A control refuses; documentation reminds; only one is load-bearing.** Anything I can obey only by concentrating is an unfixed defect, not a discipline.
- **PUSH `local` ONLY. `upstream` IS CLOSED -- hv, 2026-08-16: the CI/CD budget has been hit. NOW ENFORCED, so this entry is a description rather than a reminder.** Every push to `upstream` (GitHub) triggers the rust + tests workflows, so the habit this estate has drilled into all four of us -- push both remotes, always -- now spends money on every commit. **`local` is a Dropbox path and costs nothing.** `cmd/prepush` REFUSES a push that reaches a remote in `FROZEN_REMOTES`, by name and by URL; `INTENT_ALLOW_FROZEN_PUSH=1` is the one-off override, and lifting the freeze is deleting the line and committing it, so the lift lands in the history beside the freeze. Verified on a real `git push upstream main --dry-run`. **I wrote this watch-out, announced it to three peers, and then built the control anyway -- because the reversal of a drilled instruction is the exact case where reading it changes nothing.**

## Decisions

Standing only, consolidated at the 2026-08-17 fold.

- **A CITATION IS A CLAIM ABOUT A FILE, AND A FILE WITHOUT A COMMIT IS NOT A REFERENT.** vc corrected my `resolve:477` to `:495`, filed it as their own rule pointed at a peer, and was right -- **about a different tree.** Mine was upstream, theirs was the patched vendored copy, the delta is exactly the patch's net `+30/-12`, and **the export is `:441` in BOTH, which is precisely what makes one party look wrong rather than the pair look under-specified.** Neither statement had named its tree. The general form is the same one the whole day was about: `HEAD` is a pointer, `rust` is a pointer at a tree, and a line number is a pointer into a file -- **each is read at a moment, and none of them says which moment.**
- **READ THE DESTINATION'S ISSUE TRACKER BEFORE CARRYING INTO IT, BECAUSE IT KNOWS THINGS ABOUT YOUR PATCH THAT YOU DO NOT.** Devbin's open 0015 -- a completed run that sealed in-flight, filed by cc from Prolix a week earlier -- is an instance of the exact class my patch addresses, **and its invocation was a single gate, which is the one path my patch could not see.** Nobody in the four-node discussion found that, and no test would have: the check was green because it was never asked the question. **The upstream repository was the fixture.**
- **PROVE THE BRANCH IS REACHABLE BEFORE YOU BUILD A CHECK FOR IT -- especially when it looks unconstructible, because that is when you will skip the step.** `write_errors_file` truncates the seal at rc=0, so "green run, non-empty seal" should not exist; the check would have been dead code. It IS reachable: **when the truncation fails the function warns and returns 0, and the marker survives.** Driven against a read-only seal with controls either side proving the probe drove the real function. **The warn was the entire defence, and a note cannot notice.** The probe took ten minutes and was the difference between a guard and a decoration.

- **"DIFFERS AND PARSES" IS NOT ENOUGH TO TRUST A MUTANT: AN EMPTY FILE DOES BOTH.** A `sed` that fails silently writes a zero-byte mutant, which differs from the original and which `bash -n` ACCEPTS, because an empty script is valid bash. It then reds every test -- **which reads like the mutation working spectacularly**, and is the same trap as the byte-identical mutant wearing the opposite face. The third condition is what caught it: **the UNRELATED tests must stay green, or the reds are about a broken file rather than about the branch.** It was printing that as a count for me to read, so it now REFUSES on a red baseline. **A note cannot notice** -- which is my own rule, and I had built the weak form of it that morning. Fourth condition learned the same hour: a harness deriving paths from `$0` behaves differently depending on how it is invoked, and the failure looked exactly like a finding about the mutant.
- **A WRONG ZERO CERTIFIES ABSENCE; A WRONG MAXIMUM CERTIFIES CATASTROPHE -- AND THE SECOND IS FAR MORE PERSUASIVE, BECAUSE IT LOOKS LIKE DILIGENCE REWARDED.** Nobody re-checks an instrument that has just found something big. My devbin measurement reported ALL 27 files diverged and I was one step from filing that as an issue; the cause was zsh tying `path` to `PATH`. **The control is identical in both directions and it is the MATCHING files, not the mismatching ones**: a run with zero matches is a broken run, not a wholly-forked tree, and `int vendor` now says exactly that rather than reporting the alarm.
- **INTERNAL CONSISTENCY IS NOT CORRESPONDENCE, AND A PIPELINE FULL OF CHECKS CAN BE UNANIMOUS ABOUT THE WRONG BYTES.** Four controls in the macOS release path all asked "do these artefacts agree with each other" -- the round trip re-downloads and re-hashes, the formula reads only the sums file, checksum refuses an unclassified artefact, publish verifies the tag exists. **Every one of them passes on a build made from a peer's uncommitted work, because that build is perfectly self-consistent.** The question none of them asked is the one the release actually makes: are these the bytes the TAG names. **The tell is a set of checks that all take their inputs from the same place** -- they cannot disagree, so their agreement carries no information. **Ask what OUTSIDE the set the artefact is supposed to correspond to.** Same shape as ic's seal-versus-return-code, one level up: two claims about one run, and nothing comparing them.
- **PARSING ANOTHER PROGRAM'S HUMAN-FACING OUTPUT MAKES THAT PROGRAM'S RENDERING AN API NOBODY DECLARED -- and rendering is the layer we change most freely and document least (cc's form, better than mine, adopted).** My `sed` over `intent info` was a contract with cc's formatter and neither of us signed it. Mine was the symptom; this is the cause. **The remedy is never vigilance on either side; it is the exit code, which is declared, tested, and cannot be improved into breaking you.** It ran BOTH directions inside one day: my fix falsified a policy row in cc's `exit_code_consumers.rs` that said "nothing branches on it", and nothing anywhere was watching that sentence either.
- **A GATE KEYED TO A MOVING EXIT CODE UNDER-ENFORCES EVERYWHERE; AN UNREACHABLE BRANCH UNDER-ENFORCES ONCE.** The asymmetry is that the first fails in one CONDITION and the second fails in one WINDOW -- and the window is every consumer immediately before it upgrades, which is when guards matter most. **Corollary vc measured and I would not have: a control that is correct today BECAUSE OF AN OPEN DEFECT is a control that whoever fixes that defect will silently break.** `intent info` returns 0 while printing a hard error; rc-gating would have been fine this morning for exactly that reason.
- **A MUTANT MUST DIFFER, MUST PARSE, AND MUST STILL PASS THE BASELINE -- THREE CONDITIONS, AND I HIT TWO FAILURES OF THEM IN ONE SESSION.** An anchor that silently misses produces a byte-identical copy that reports ALL-GREEN, which is what a working guard looks like. A mutation that deletes a line out of an `if` body leaves an empty block, and a file that does not parse reds EVERYTHING, which is what the strongest possible finding looks like. **Both directions are indistinguishable from the result you were hoping for.** The harness now refuses on all three; both refusals fired today on real mutants.
- **THE THING I HAVE IS NARROWER THAN THE THING I SAID -- twice in one session, both times the POPULATION rather than the premise.** Re-verifying a premise at the moment of acting is the right instinct and it caught a wrong reason; it does NOT widen a population, and the population is where this hides. My blocker went one file -> two -> five (vc measured the five). My fmt finding went four damaged files -> one, by going back to the artefact. **Both were corrected by someone else's measurement or by re-reading the diff, never by thinking harder about the claim.**
- **A STALE COPY OF A GUARD IS NOT A MISSING GUARD -- FIND THE OTHER CHAIN MEMBER BEFORE RAISING THE ALARM.** This clone's `.git/hooks/pre-commit.intent` is 148 lines behind HEAD and carries the pre-0042 resolver with the masked `$?`, and its guard roster has no header guard in it. I had the announce half-written for three peers. **`int precommit` runs the header guard from a different chain member**, so both guards are live and nothing is unenforced. One command's difference between a finding and a false alarm sent to everybody -- and it is the same shape as blaming a file for a defect its neighbour has.
- **RUNNING A GATE TO TEST IT MUTATES THE TREE, AND IN A SHARED CLONE THAT IS SOMEBODY ELSE'S TREE.** `int fmt all`, run purely to prove a summary still prints its green line, rewrote four tracked files I had no business touching. Reverted -- but the useful half is that ONE of the four was real damage I would never have found by reading. **A side effect is not automatically noise; look at what it did before undoing it.**

- **A GUARD'S PRECONDITION CAN BE INVALIDATED BY A CHANGE THAT IS CORRECT ON ITS OWN TERMS AND NEVER LOOKS AT THE GUARD.** My total-failure branch tested for an EMPTY resolution, which was the true signature of an unresolvable install on the day I wrote it. cc later implemented `info` to render `<not set>` -- better for a human, and non-empty -- and my branch became unreachable in the one condition it exists for. **Neither change was wrong and nothing connected them**, because the coupling is a `sed` over display text and is written down nowhere. **The tell to look for: a guard that keys off the ABSENCE of output from another tool is keyed to that tool's current silence, and silence is the least stable thing a program emits.** Prefer the exit code, which is a contract, over the rendering, which is prose.
- **A MUTATION THAT FAILS TO APPLY REPORTS ALL-GREEN, AND ALL-GREEN IS WHAT A WORKING GUARD LOOKS LIKE.** My first classification mutant used a Python anchor that did not match, so the "mutant" was a byte-identical copy -- and the run printed `ok 5`, `ok 6` beside two genuine mutants that had worked. **The AssertionError was three lines up the same output and I read past it, because the verdict underneath said what I expected.** This is the fixture-never-reached-the-branch trap in the direction that feels safest: not a red I would have investigated, a green I already believed. **The mechanism, and it is one line: assert the mutant DIFFERS from the original before running anything against it** -- `diff -q` and refuse. An anchor that silently misses is indistinguishable from a guard that holds.
- **A GUARD THAT CANNOT TELL INVOCATION FROM MENTION IS STILL RIGHT TO FAIL YOU, AND THE FIX BELONGS ON YOUR SIDE.** ic's retarget guard refused three of my test lines that carried the dispatcher path inside a grep pattern. The allowlist was RIGHT THERE with a precedent row reading "it contains the needle as data" -- exactly my case -- and taking it would have blanket-exempted the file forever, including any real bypassing invocation added later. **The literal bought nothing: those tests assert that a regenerator's status is CHECKED, and how the CLI is spelled is incidental.** Matching on structure is the stronger assertion anyway -- it survives the script moving to `$INTENT_BIN`. **A guard that can be argued with case by case is not a guard; the tell that you are arguing is reaching for its allowlist rather than for its point.**
- **THE WORKING TREE MOVES UNDER YOU IN A SHARED CLONE, AND A REBUILD SILENTLY ADOPTS A PEER'S UNCOMMITTED WORK.** I rebuilt and measured `intent info` at exit 1 where my own fold had recorded exit 0 -- and the honest first hypothesis was that I had mis-measured, since masked exit status is a failure I have made. It was neither: cc had the fix uncommitted in the tree my build compiled. **`git status` was empty when I started the turn and not ten minutes later.** So a measurement names a COMMIT, and if the tree is dirty it names nothing -- and I was one sentence from reporting a peer's fix as landed on the strength of a binary built from their in-flight edit.
- **THE COST I WAS TOLD TO MEASURE BEFORE CHOOSING DID NOT EXIST.** The asset-shape decision was held all session on "it changes signing, notarisation and the checksum step, all built against a single binary". `notarize` already staged a DIRECTORY and submitted a zip of it -- so a third non-Mach-O artefact never reaches signing or notarisation at all, and the only real cost was in `checksum`, where two sets had been silently one. **The hold was real; the reason recorded for it was three-quarters wrong, and reading the code I had written closed it in ten minutes.** A remembered cost is a guess about code that is sitting right there.
  The full 47-entry set for 08-15/08-16 is in `.history/20260816/wip.md`; nothing below is a summary of something still live elsewhere.

- **I FABRICATED A HEARTBEAT ON THIS FOLD, IN THE ONE FIELD THE WHOLE CLOCK DISCIPLINE EXISTS FOR, AND THE GUARD DID NOT CATCH IT.** Rewriting my board through a python heredoc I typed `heartbeat_at: 2026-08-17 03:05Z` as a literal in the script body. I had read `03:01Z` before it and read `03:04Z` after, so it was **in the future when I wrote it** -- and it committed clean, because it sat inside the guard's 120s jitter tolerance. **My recorded mechanism is "read the clock into a shell variable and SUBSTITUTE it", and I applied it correctly to the two messages in the same minute -- passing the stamp as `argv[1]` -- and not to the board, because the board was being generated rather than typed.** That is the mechanism-incomplete failure again, and the missing clause is exact: **when a script writes a stamp, the stamp is passed INTO the script as an argument; a stamp appearing anywhere in a script's own body is fabricated by construction.** Both forms were in one session, one right and one wrong, which is what makes the clause worth having rather than another resolution to be careful. The live value has been replaced by a real read; the fabricated one is in `267c5a00` and is recorded here rather than quietly corrected, because a corrected-looking fake is worse than an admitted one.
- **CALIBRATE AN INSTRUMENT AGAINST A KNOWN GOOD BEFORE BELIEVING IT, AND A ZERO IS A CLAIM LIKE ANY OTHER.** A tool giving a real-looking answer usually means it was asked in the wrong context -- `spctl` on a bare Mach-O, `brew style` outside a tap, bare `rustfmt` without the repo's config, prettier cleared twice from the wrong instrument. **The sharper half is the negative: a result from an unverified invocation is not evidence, and it is the shape that feels MOST like evidence, because nothing failed.** Measured cost: a grep asking which consumers invoke `intent` by explicit path returned zero; calibrating it against a call site I had already read by hand turned zero into four, **two of which were the release-regenerator defect.** A wrong zero does not merely fail to find things -- it certifies their absence, and the more sweeping the question, the more load the answer carries.
- **THE INSTRUMENT GOES STALE TOO, AND SO DOES A HOLD.** A binary 57 seconds behind a fix reports the old behaviour as confidently as one 14 minutes behind, and staleness is invisible in the output. `stat` the binary against `git log -1 --format=%ct <fix>` before believing a measurement about that fix. **Same rule one level up: a hold must be re-verified when it is HONOURED, not only when it is written.** Mine was stale in twelve minutes -- and the re-check had two staleness layers, the premise and the binary I would have re-measured with. **A positive result from a stale instrument is still a result about the past, and it is the direction that feels safe because the news is good.**
- **PROSE AND MECHANISM SHARE A SPELLING, AND EVERY WAY THAT GOES WRONG IS THE SAME DEFECT.** A check a comment can satisfy is not a check (anchor structural tests to `^` structure). A grep for a mechanism counts its prose (`deny_unknown_fields` returned three; all three were the doc comment explaining why the file has none). **And a comment that predicts its own defect is still a defect** -- `usage()`'s hardcoded `5,34p` window carried the words "it will move again if the header does", and the header moved, and `--help` printed no flags at all. **A note cannot notice.**
- **AGREEMENT IS NOT TRUTH; ONLY A CANARY IS.** Two things written in the same breath by the same author agreeing is not evidence -- my lock refusal's comment said "every changed line is a version line" and the code tested exactly that, and the INVARIANT was wrong, so it accepted the one change it exists to refuse. **Diffing a comment against its check tests agreement. Only running the refusal against a case it should refuse tests truth.**
- **A MUTANT THAT CANNOT REFUSE ANYTHING IS NOT A MUTANT, AND A FIXTURE THAT NEVER REACHED THE BRANCH PROVES NOTHING.** The mutant must still pass the BASELINE case before its verdict means anything, or a broken file reads as the strongest possible finding. **Corollary with a heuristic attached: uniformity across cases that should differ is evidence about the instrument, not the subject** -- three resolver conditions all returning nothing meant the hook was exiting before the block under test, not that the block was quiet.
- **A NEEDLE LIST CATCHES THE INSTANCE; DERIVING THE SET CLOSES THE CLASS.** An allowlist forbids everything its author did not think of; a needle list forbids only what they did. **Now with the mutant that proves the second door is real**: the hand-picked `--help` test passes on a parser arm the header never documents, and the derived one names the flag. The unwritten cases are where the risk lives.
- **"THE GUARD IS ABSENT" AND "THE TOOL THAT LOCATES GUARDS IS ABSENT" ARE DIFFERENT ABSENCES, AND ONE `else` CANNOT TELL THEM APART.** Benign absence and total absence took the same branch, so two mild warnings meant nothing was enforcing anything. **The general form: an absence treated as a property of the THING when it is a property of the RESOLVER.** Same shape appeared in `check critic --languages`, blaming a project's language declarations for a failure of the tool.
- **A GUARD THAT WATCHES FOR CHANGE IS STRUCTURALLY BLIND TO SAMENESS.** The release's dirty-tree check exists to stop an inconsistent tag; a failed sidecar REGENERATION leaves the file at the previous version, so the tree is clean and the cut proceeds. **The tests asserted the stamp happens BEFORE the commit and could not assert it WORKED** -- ordering was the fix everyone remembered, so ordering is what got tested. **Whenever a control asks "did something change", ask what it does when the answer is legitimately no and should not have been.**
- **A PARTIAL FIX LEAVES THE HALF YOU DID NOT CHECK ASSERTING THE OPPOSITE, AND AN EXTRACTION IS NOT A DELETION.** Replacing a claim obliges you to re-read what surrounded it, because its neighbours were written to agree with the thing that is now gone. **And "the old form must not survive" is the wrong predicate for a refactor** -- extraction MOVES code, so the right assertion is Highlander's: exactly ONE home, which also catches the copy pasted back inline. Both times the reasoning was already present one line away and simply not carried across.
- **A STRUCTURE THAT CANNOT FAIL BEATS A CONTROL THAT CATCHES THE FAILURE, AND "RECORDED AS THE BETTER SHAPE, DELIBERATELY UNBUILT" IS USUALLY DEFERRAL WEARING A REASON.** Building it took under an hour.
- **WHERE A MECHANISM CAN EXIST, WRITE ONE; WHERE IT CANNOT, ADMIT THE THING IS UNFIXED.** My fold archived a critical announce unread because the read and the archive were minutes apart; the replacement asserts the count is still what I just read. **It FIRED, on a real fifth message, same board, opposite outcome** -- and it failed closed and per-item. **Against that: zsh not word-splitting an unquoted `$var` has now beaten a written reminder four times**, because it fires where there is no gate and no test. That one is UNFIXED, not mitigated; the tell is a command that saw fewer arguments than I passed.
- **IN A FOUR-SESSION SHARED CLONE, THE WORKING TREE ANSWERS _WHAT_ AND IS SILENT ON _WHO_ AND ON _WHETHER THIS EXISTS YET_.** Two misattributions in one day, opposite directions, and the second carried an instruction -- "you are building `install.rs`, build on" -- for a module cc had finished. **The second face is worse: I was one sentence from telling vc their `st_prefix` concern was closed on the strength of an UNTRACKED file.** `git log -1 -- <path>` before naming an owner; `git ls-files --error-unmatch` before a working-tree file becomes a fact.
- **A DISAGREEMENT RESOLVED QUIETLY IN CODE IS INDISTINGUISHABLE FROM AN OVERSIGHT.** 0042's proposed fix says block on a dead resolver; I fail open, because that gate is 0043 rebuilt on the git side. **Written INTO the issue with the cost of the alternative and named as hv's call** -- a deliberate departure has to be recorded AS a departure, where the proposal lives.
- **A WRONG ARTEFACT IS NOT A NEUTRAL PLACEHOLDER -- IT MAKES A CONFIDENT FALSE STATEMENT, AND A PLACEHOLDER THAT SATISFIES A GATE CONVERTS A LOUD REFUSAL INTO A SILENT ONE.** A formula pointing at a nonexistent release reads as "the tap is broken"; an empty `## [3.0.0]` heading would have carried the dry-run further while the release notes were still absent. **Getting further is not getting closer.**
- **A CONTROL WORKING EXACTLY AS DESIGNED CAN BE THE THING BLOCKING THE GOAL, AND THAT IS NOT AN ARGUMENT FOR AN EXCEPTION.** My frozen-remote pre-flight correctly refuses a v3 cut and v3 cannot reach a user without pushing the frozen remote. The wrong move is a quiet exception in the tool; the right one is asking whether the policy's own reason -- cost per commit -- covers a release, which is not a commit.
- **A PEER CANNOT AUTHORISE WHAT A HARNESS REFUSED**, and a peer performing it on your behalf launders the refusal.
- **A `setup()` IN A BATS FILE SILENTLY REPLACES `test_helper.bash`'s, AND EVERY EXISTING TEST STILL PASSES.** They use absolute paths and never notice `TEST_TEMP_DIR` has stopped being created. The helper's own comment shows overriding is sanctioned, which is exactly why the override is invisible: it looks like house style.

- **I CHECKED THE OWNERSHIP BOUNDARY AND THEN EDITED A DIFFERENT FILE, FOUR HOURS AFTER WRITING THE RULING THAT EXISTS TO PREVENT IT.** I quoted the manifest header at myself correctly (_"config.yaml, `cmd/` and `help/` belong to the project"_), verified `bin/.devbin/**` was safe, recorded hv's Devbin-first sequence with the reason -- **and put the referent export in `bin/devbin`, which is manifest line 6.** `int vendor` reported DIVERGED on the very next run. **vc's sentence is the class and it is better than mine: the rule I had in hand did not fail, it was never applied to the thing I did.** Checking the boundary is not the same act as checking the file you are about to edit, and only the instrument could tell them apart. Same shape as everything else today -- the check's subject and the action's subject came apart.
- **A FIXTURE BUILT TO BE CAUGHT TESTS THE DETECTOR AGAINST THE AUTHOR'S MODEL OF THE FAILURE; A REAL ONE TESTS IT AGAINST THE FAILURE (vc's framing, adopted).** I had a sacrificial-copy canary planned for `int vendor`'s unfired arms. The DIVERGED arm fired first, on my own unplanted mistake, which is strictly better evidence than the canary would have been. **An instrument that catches something nobody arranged has been tested by the world rather than by its author.**
- **THE PROBE RAN THE THING, AND THE MITIGATION WRITTEN TO PREVENT THAT IS WHAT DID IT.** `cmd/hooks` asked whether a runner supports `--list-guards` by grepping its source for the string; `cmd/prepush` carries those words in a **comment** and implements no handler. So `int hooks` -- a read-only "what is wired?" report -- invoked `int prepush --list-guards`, prepush ignored the unrecognised flag and **cloned HEAD and built both binaries**, whose progress lines were then printed as guards. The comment four lines above the probe says _"a side effect is not a probe. Never ask a question by running the thing."_ **PROSE AND MECHANISM SHARE A SPELLING, and documentation is exactly what accumulates around a mechanism other files consume** -- so anchor on the dispatch arm, which no comment contains. From a clone-and-build to 0.8s.
- **A COUNT IN PROSE IS A SECOND COPY OF A LIST, AND THE COPY IS THE HALF NOBODY UPDATES.** My skip warning said _"those two gates SKIPPED this commit"_ from the day I wrote it, when there were two. There are **nine**. So at the one moment coverage is degraded, the message a human reads understated the gap by seven. **Name the class, never the count**, and say what still ran -- the danger is reading "gates skipped" as "no gate ran". ic hit the same class from the other side the same day and answered it properly with a roster that refuses an undeclared instrument.
- **THE TELL IS THE MEASUREMENT FIELD, NOT THE VERDICT (vc's, better than mine, taken).** Their manifest check printed 27 MISMATCH with 27 **empty** `actual:` values. Mine printed all-diverged and I caught it by the matched count being zero. **Theirs is one step earlier and needs no second run; mine needs no foreknowledge of the field layout, so it survives being applied to an instrument you did not write.** Keep both -- they fail in different directions.
- **AND THE CAUSE WAS THE SAME BUG IN BOTH OF US: `while read -r sha path`.** vc first diagnosed it as "unresolvable inside the loop's subshell", which fits the evidence and names no mechanism, so it generalises to nothing and the next loop is written identically. **A diagnosis that fits the evidence without naming the mechanism is a story, and it passes review exactly as well as an explanation does.** vc then reproduced both directions and added the half I did not have: **the corruption OUTLIVES the loop** -- a later loop in the same invocation still had no `cat` -- so a script that appears to recover is measuring with a `PATH` of one filename. **The bait is in the data format**: `read -r sha path` is the obvious spelling for a line of a `.sha256` manifest, so anyone who writes this loop writes this bug.
- **hv CLEARED THE ISSUE TRACKER, AND THE DIAGNOSIS WAS MINE BEFORE THE RULING WAS.** hv: _"The honest diagnosis is that the issue file is duplicating the agent channel. Exactly. So stop doing that, please."_ then _"Just clear the tracker, findings live in commits."_ Sharpened rule: **an issue is an external user's report against a released version; everything we find building v3 is just work.** Measured at the time: **11 filed today against 5 closed, and 41,780 words across 25 open rows.** The asymmetry is structural rather than anyone's fault -- filing is one act, closing is three (verify by execution, write the resolution, move the directory) -- so the queue could only grow. **My exemplar is 0054: a three-word flag deletion in a file I own that cost an issue, two options, a measurement of the option that could not work, two rulings and a merge.** From here: fix inline, put the reasoning in the commit against the diff, and if it describes what "done" means it is an AC row, because the gate can score an AC and cannot score an issue.
