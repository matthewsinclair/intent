---
node: dc
name: DevX Claude
role: worker
session_id: 482cf2fc-6b49-4a0d-8d76-38b3c981924c
heartbeat_at: 2026-08-15 09:56Z
status: active
focus: "Shipped a false-ABSENT defect in int hooks and fixed it (worktrees; ask git, never compute). cc severity split folded into int cache. core.hooksPath blocker WITHDRAWN by vc, so the architecture question reaches hv unblocked."
claims: []
---

# DevX Claude (dc)

## DOING

- **LANDED `f8948cc`, both remotes, verified at HEAD rather than on disk.** Four items, each measured before it was made. The gate fired in the real commit path on its own commit.
  - **The two guards are wired** (hv approved). `bin/int precommit` (tracked, my lane) runs both; chained LAST in `.git/hooks/pre-commit`, after the formatters re-stage, so the gate sees the set that actually lands rather than the pre-prettier one. **Not** in `lib/templates/hooks/pre-commit.sh` -- that file ships to every consumer and these guards check `intent/st/ST0056/parity/**`, which exists here and nowhere else.
  - **Mutation-tested, four canaries, sacrificial worktree.** Split measurement group -> refuses, names `provenance_check.sh`. Corrupted `dispatch-table.md`, full sweep -> refuses, names `view_skew_check.sh`. Same corruption with an unrelated path staged -> correctly NOT checked. Staged with its own path -> refuses again.
  - **CI could not fail on integration tests**; step deleted (redundant with `run_tests.sh`, which covers `integration/` and propagates status) along with a `bats || true` fallback.
  - **`*.bak` ignored as a class**; `/AGENTS.md.bak` pruned as redundant. vc's finding, verified here rather than taken on report.
  - **`rust-toolchain.toml` REFUSED**, not omitted -- see Decisions.
- **CI REPORTED, AS MEASURED.** `f8948cc`: `rust` **success** both legs; `Intent Tests` **success** all four (Ubuntu, macOS, ShellCheck, Summary). vc's instruction was right to make this the finding rather than a formality -- and the result carries a second fact worth stating, because it is the one the swallow was hiding: **the suite is green through a workflow that can now fail on integration tests.** Before, that step could not fail; a green meant nothing about it. It does now.
- **Local and CI toolchains AGREE, and this was previously unknown.** The recording step reports CI at `rustc 1.97.1 (8bab26f4f 2026-07-14)`; local Homebrew is `1.97.1 (8bab26f4f 2026-07-14)` -- same version, same commit hash. Not a guarantee (nothing pins it) but now an observation rather than a hope, and the mechanism that surfaced it survives the next bump.
- **`prepush` landed at `7acab9c`** -- clone HEAD, build it, run the binaries. vc left the pre-commit/pre-push/CI trade to me and the measurement settled it: ~16s cold, too slow per-commit and CI reports only after the bad state is already public. Path-triggered, so a whiteboard-only push skips in 0.5s. Canaried against a fixture reproducing `a1a949c`'s exact signature (root `Cargo.toml` plus `native/rust/Cargo.toml` both at HEAD): REFUSED, both manifests named.
- **The repository at HEAD is verified sound** -- something nobody had checked since the half-move. Fresh clone, cold build 14.02s, `intent` and `intentd` both produced and both running `3.0.0-dev`. Exactly one workspace manifest; no stray root manifest; no duplicate tree.
- **I SHIPPED A DEFECT AND IT LIVED FORTY MINUTES.** `int hooks` (`49f0676`) resolved the hooks directory by reading `core.hooksPath` with a fallback to `$ROOT/.git/hooks`. **In a linked worktree `.git` is a FILE**, so that path does not resolve and the check reported both hooks ABSENT on a worktree whose hooks are wired -- git uses the main repository's dir. A **false ABSENT**, and it is exactly what that file's own comment claimed the command existed to prevent, committed by the command itself via a route the comment did not consider. It matters practically: sacrificial worktrees are the mandated pattern here for anything that writes, and vc keeps a permanent one. Fixed `9defbbd` -- `git rev-parse --git-path hooks` covers default, redirect and worktree in one call; the rule was always git's to own and I had written a second implementation of it. **Found by following through on vc's WITHDRAWAL of the hooksPath defect instead of banking the good news** -- checking "your blocker is gone" is what walked me into my own code.
- **cc's severity discriminator folded into `int cache` (`0a2e72b`): presence is not use.** cc measured what I had only counted -- of 211 distinct stale-rooted crates, 181 had a correct-rooted sibling (superseded, dead weight) and **30 had none, all proc-macro or build-script HOST artefacts** (`serde_derive`, `schemars_derive`, `syn`, `quote`, ...). Not random leftovers: **exactly the code-generating half**, including the derives behind the JSON Schema face that `schema_faces_drift.rs` compares against. "228 artefacts" reads as noise; "30 with no current sibling, all proc-macro" reads as a reason to act, and it is what made cc clean rather than file it. Verified on a fixture reproducing that shape, since cc had already cleaned the real residue. **cc's clean cost 32s and 246 tests passed** -- the rebuild cost I had warned about barely existed, which is worth knowing next time.
- **cc's STALE-CACHE CLASS IS NOT CLOSED, and `int cache` (`73e5d64`) now says so.** cc cleaned 1.2G believing it fixed. Measured at `e8d689b`: **228 dep files in `native/rust/target` name `native/target`** -- the intermediate location from the earlier leg of the move -- **and that directory does not exist.** 304 name the correct root. Cargo has called all 228 fresh ever since, because it compares timestamps and input hashes and never the manifest root. **The discriminator, which makes it a measurement rather than my inference**: a cold build of a fresh clone at the same revision produced ZERO stale references and 222 correct ones. Same source, same revision, different cache -- so the difference IS the cache. Canaried both ways, since a check that is always red is worthless: clean clone -> ok/exit 0; planted stale root -> caught and named; plant removed -> ok again. **Not cleaned, deliberately** -- 3.1G of shared target/, cc is mid-build, and the moment is theirs. **It WARNS rather than refusing**, unlike everything else I landed today: the residue is latent (this tree built, tested and shipped green with all 228 present), and refusing to build over a usually-harmless condition is how a gate gets switched off.
- **THE PATH FILTER WORKS AND WILL HELP LESS THAN IT LOOKS, and I would rather say so than bank the win.** I pushed a board-only commit as a falsifiable check that `Intent Tests` would not fire. It fired. Diagnosis before conclusion: my push range `5412a53..f00ae31` carried ic's `20e8c4b` as well as mine, touching `surface/**` and `parity/tools/**`, so GitHub ran the suite exactly as designed -- any non-ignored path in the push means run. The mechanism is correct and fail-safe. **What the check actually exposed is that on this repo a board-only COMMIT is rarely a board-only PUSH**: five nodes commit into one branch, so a push carries whatever peers landed since your last pull. The saturation relief is real but partial, and proportional to how promptly a node pushes. Not a reason to revert it -- it costs nothing and fails safe -- but the eleven-minute queue will not vanish, and anyone reading the commit message alone would over-expect.
- **Picked up 08:57Z.** Charter is hv's words via `whiteboard/README.md` (uncommitted): _dev-x and build environment, so that cc concentrates on functionality for the CLI / daemon_. cc, ic and vc all wrote intros within two minutes of each other (08:54Z / 08:55Z / 08:55Z); their three independent readings of my lane agree, which is worth more than any one of them.
- **Operating provisionally under vc's UNRATIFIED boundary**: I own the environment the code builds and ships in (`native/` layout + workspace files, `.github/workflows/`, `.gitignore`, devbin, hooks + pre-commit wiring, toolchain pinning, release mechanics); cc owns `native/rust/crates/**`. Disputed file test: _does changing it change what the tool DOES, or only how it gets built?_ **`bin/` is an open collision and I have not assumed it.**

## TODO -- in this order

0. ~~**THE HOOKS ARE NOT TRACKED**~~ **CHECK BUILT AND LANDED `49f0676`. The hole is now VISIBLE, and it is not closed -- those are different things and I am not going to let them blur.** `int hooks` (bare form reports, per vc's re-ordering: the check is the load-bearing half). Three states -- WIRED / **UNWIRED** / ABSENT -- because the middle one is a legacy hook that is present, executable, invoking something else, and therefore looks installed while protecting nothing. Guard names are **read from the runner**, never listed in the file, so the roster cannot rot into naming a guard that does not run. Four canaries in a sacrificial clone: fresh clone -> both ABSENT (the hole, reproduced); legacy hook -> UNWIRED; `--install` -> wires both AND pre-existing content survives; `--install` x3 -> idempotent. Live hooks confirmed untouched, gate traced as invoked exactly once. **What it cannot do**: make the repository carry the wiring. Git will not adopt tracked hooks on its own -- a deliberate security property -- so a per-clone action is irreducible. **`core.hooksPath` at a tracked directory is the better architecture** and I did not take it, on the grounds that it would orphan `intent claude upgrade`'s output. **THAT GROUND IS GONE.** vc published the orphaning as a canon defect at `high`, cc refuted it, and vc withdrew it after re-running the reproduction: `intent claude upgrade` resolves through `canon_hooks_dir()`, which asks `git rev-parse --git-path hooks` and therefore follows a redirect automatically. vc's error was grepping for the string `hooksPath`, finding it only in my files, and concluding the mechanism was absent -- **the correct API never needs to name it.** So the architecture question now reaches hv with **no technical blocker attached**; what remains is purely that `lib/templates/` is cc's lane and the change is theirs or hv's to make, not mine. My lane caution was right in principle and wrong in its stated reason.
1. **The original statement of the hole, kept because it is the argument:** `bin/int precommit` and `bin/int prepush` are tracked, reviewable and tested; the two lines in `.git/hooks/{pre-commit,pre-push}` that CALL them are not, because `.git/hooks/` never is. **A fresh clone therefore has every guard and nothing invoking them** -- which is precisely the shape of failure the guards exist to refuse, now sitting under the guards themselves. Pre-existing for this whole hook rather than introduced by me, but it is mine now and it makes every gate I landed today opt-in-by-accident. Candidate fix: an `int install hooks` (devbin already has a `lib/cmd/install`) plus a check that reports unwired hooks, so the wiring is a command rather than a thing four people each remember once.
2. **`.claude/settings.json` auto-mode brief is DRAFTED AND UNAPPLIED.** The harness classifier refused the write to `~/.claude/settings.json`, correctly, and I did not route around it. Draft is at `scratchpad/automode-env.json`, verified to change only `.autoMode.environment` and to leave `permissions.defaultMode` untouched. Corrections in it, all measured: **repository visibility is PUBLIC, not "assume private"**; default branch `main`, not unknown; the project is bash-plus-Rust, not Elixir; plus the multi-session working-tree hazards, the `bin/intent` symlink, the unsafe stashes, and the fact that 60 whiteboard files are world-readable. hv applies it.

3. ~~**THE INDEX**~~ **CLOSED 09:02Z without an unstage, and the way it closed is the point.** At 08:57Z the staged `intent/whiteboard/README.md` had no `dc` row and said "the roster is four", so a bare commit would have deleted this node from the roster. By 09:02Z vc had committed the README and the charter is at HEAD (`git show HEAD:...` carries the DevX Claude row). What remains staged -- `intent/llm/MODULES.md`, `dc/inbox.vc.md`, `vc/inbox.dc.md` -- is formatting-only (MODULES.md's entire HEAD-to-index delta is **one table separator dash run, four characters**, by `git diff --word-diff`), and two of the three are vc's in-flight commit content. **So the correct action was none.** Held the whole time because it spanned two peers' files; the hazard was resolved by its owner while I measured.
4. **Wire the two guards into pre-commit.** `provenance_check.sh` (`9e7a7be`) and `view_skew_check.sh` (`d470f62`). ic: one authorisation, not two -- same slot, same argument -- and it needs hv rather than ic's re-derivation. vc calls it the cleanest first job and unambiguously mine. Precedent is the clock guard: opt-in by directory presence, fires only on what the current commit touches, refuses rather than auto-corrects, prints the right value so the fix is a copy-paste. `view_skew_check.sh` is already `--changed <paths>...` so it costs nothing on a commit touching no generated view. **Add to the gate, never replace** -- it already runs prettier and the critic.
5. **Port Conflab's `use` + `cli --bin`** (hv's ask, handed over by cc). `Conflab bin/.devbin/cmd/{use,cli}` exist to port from. **Settle the semantics before porting**: Conflab switches two builds of ONE program; Intent's axis is three-valued (v2-bash / v3-local / v3-brew) because `~/.local/bin/intent` points at the v2 bash CLI, a different PROGRAM. Measured additionally: `which -a intent` returns THREE reachable copies (`~/.local/bin/intent`, `~/bin/intent`, `Intent/bin/intent`) and the first two are both symlinks to the third -- so the ambiguity is already live. There is no brew formula (`brew list intent` -> no such formula), so the brew arm has nothing to link until WP-11.
6. **Pin the Rust toolchain.** `native/rust/` has `rustfmt.toml` and **no `rust-toolchain.toml`**; local is Homebrew rustc 1.97.1, CI is `dtolnay/rust-toolchain@stable`. Both gates (`cargo fmt --check`, `clippy -D warnings`) are version-sensitive, so a stable release can turn CI red with zero code change, and local-green/CI-red has no mechanism today.
7. **CI swallows failures in my lane.** `tests.yml` runs integration tests as `bats ... || echo "Integration tests completed with status: $?"` -- the job cannot fail, and the `$?` there reads the wrong command anyway (the `$?`-after-a-pipe trap already on vc's board). The `run_tests.sh`-absent fallback is `bats "$f" || true`. `IN-AG-NO-SILENT-001` applied to the build environment.
8. **Make cc's fresh-clone-and-build a standing post-move check** (vc's #2) rather than something cc now has to remember. This is the only check in the estate that reads the repository rather than the working tree.
9. **Stale-but-fresh build state.** 2.6G at `native/rust/target/`; cc lost an hour and 1.2G to artefacts compiled against the old `CARGO_MANIFEST_DIR` that cargo's own fingerprint called fresh.

## Watch-outs

Facts about this estate, not reminders. Everything amounting to "remember to" is worthless here -- three nodes broke rules they had personally written, on the day they wrote them.

- **A control refuses; documentation reminds; only one is load-bearing.** Anything I can obey only by concentrating is an unfixed defect, not a discipline.
- **`--only` commits what you NAME, and a move is TWO facts.** The add and the delete are separate index entries; naming the new path commits the addition and leaves the deletion staged. It put two complete copies of the Rust tree at HEAD, on both remotes, five files divergent. Every working-tree check was green throughout and structurally could not have seen it.
- **A green suite is evidence about the tree you HAVE, never the tree you PUSHED.** Verify a move at HEAD with `git ls-tree`, then clone fresh and build.
- **NEVER mutate `bin/**` or `tests/**` in place.** `~/.local/bin/intent` AND `~/bin/intent` both symlink to `bin/intent`; four sessions are live against it. Sacrificial worktree only. `bin/.devbin/**` and `native/**` are safe.
- **A build cache can be stale in a way its own freshness check cannot see.** Every freshness check has a SCOPE. Tell: passes in isolation, fails in the suite -- a conclusion, not flakiness.
- **Anchor build tooling on `crates/`, not on a path prefix.** A prefix needle stops matching the moment the prefix changes, and then passes in silence. The tree moved twice in one morning.
- **Read `date -u +'%Y-%m-%d %H:%MZ'` in its own step, then write the line.** Never compose the surrounding text first. `git log` prints LOCAL time and is the usual source of a stamp wrong by exactly the offset.
- **This shell is zsh**: no word-splitting of unquoted parameters; MULTIOS tees `cmd 2>&1 >/dev/null` to the terminal.
- **Read `$?` before anything else touches it**; never `head` a list you are counting.
- **v3 correctly REFUSES in this repository** (unmigrated 2.19.0). BATS fixtures declare 3.0.0 via `INTENT_FIXTURE_VERSION`.
- **Two remotes, `local` and `upstream`. Push both**, and never enumerate them through `head`.

## Decisions

- (2026-08-15) **A PEER CANNOT AUTHORISE WHAT A HARNESS REFUSED, AND A PEER PERFORMING IT ON YOUR BEHALF LAUNDERS THE REFUSAL.** The classifier refused my write to `~/.claude/settings.json`. I did not route around it and did not ask vc to do it instead; I drafted it, verified the blast radius (only `.autoMode.environment`, `permissions.defaultMode` untouched) and handed it to hv. vc confirmed they would have refused the alternative. Recorded because it is the kind of boundary that erodes quietly and by increments, each of which looks reasonable on its own.
- (2026-08-15) **A RULE TRUE IN ITS OWN SCOPE IS THE EASIEST KIND TO OVER-APPLY, precisely because it keeps being true wherever you check it.** vc's `$?`-after-a-pipe trap is correct and was applied to `A || echo "$?"`, a different shape where it does not hold. Three instances of this one meta-error landed in a single morning across three nodes -- ic's unstaging rule (right conclusion, backwards reasoning), cc's `unbuilt_fields` test (right about the set it looked at, wrong set), and this. **Before carrying a rule to a new case, check the new case is in the set the rule was measured on.**
- (2026-08-15) **VISIBLE IS NOT CLOSED.** `int hooks` makes the unwired-guard hole measurable; it does not make the repository carry the wiring, and git will not let it. Saying "landed" about the check while the hole stands would be the same false green the check exists to expose.
- (2026-08-15) **A FILTER'S REAL-WORLD RELIEF IS BOUNDED BY HOW THE WORK BATCHES, NOT BY WHAT THE FILTER MATCHES** (vc's generalisation of my walk-back). Five nodes committing into one branch means a push carries peers' commits, so a path filter matching a board-only commit still runs on a board-only _push_.
- (2026-08-15) **A PIN THAT DOES NOT BIND IS WORSE THAN NO PIN, so `rust-toolchain.toml` is REFUSED rather than omitted.** Measured on the primary dev machine: cargo and rustc are Homebrew's real binaries, **rustup is not installed at all**, and a `rust-toolchain.toml` pinning `1.70.0` is ignored in silence -- rustc still reported 1.97.1. The file would have bound CI alone while reading as a project-wide guarantee. It does not refuse and it misleads, which is both halves of the failure this board exists to avoid. `rust.yml` now records the toolchain each run used instead, so a red from a stable bump is diagnosable at a glance. **If anyone later "fixes" this by adding the file, the fix is to install rustup first -- the file is downstream of that, not a substitute for it.**
- (2026-08-15) **A CANARY THAT DOES NOT ENTER THE BRANCH PROVES NOTHING, AND LOOKS LIKE A FINDING.** My path-narrowing canary staged a `touch`ed file: `touch` makes no diff, so the staged set was empty, the gate took the full-sweep branch, and the run I was about to read as "narrowing is broken" had never entered the narrowing code at all. **Assert the fixture reached the branch before reading its verdict.** Same shape as the standing rule that applied is not reached.
- (2026-08-15) **`\+` IS A BSD SED NO-OP, AND A BROKEN NORMALISER FAILS AS A FALSE POSITIVE.** `sed 's/[[:space:]]\+/ /g'` does nothing on macOS -- BSD basic regex has no `\+` quantifier -- so my safety check silently compared unnormalised text and returned SUBSTANTIVE on three files that were formatting-only. I was one step from reporting "do not unstage, there is real content here" on the strength of a no-op. **A normaliser that fails to normalise does not fail loudly; it reports difference, which reads exactly like a finding.** Use `sed -E` (or `perl -pe`) and calibrate the normaliser against a case it must collapse before trusting a verdict from it. `cat -A` is GNU-only here too; `cat -e`. Corroborate any formatting-only verdict with `git diff --word-diff`, which needs no normaliser at all.
- (2026-08-15) **Re-measure at the moment of acting, not from the queued conclusion.** The staged set changed twice in five minutes and the dangerous file left it entirely. Acting on the 08:57Z measurement at 09:02Z would have disrupted a peer's in-flight commit to fix a hazard that no longer existed.
- (2026-08-15) **Append to an inbox, never overwrite it.** A full-file write clobbered the scaffold's `dc -> <peer>` header with the inbound direction on two of three intros. The path already encodes the routing; the header exists so the file is self-describing when read alone, and I broke exactly that.
- (2026-08-15) **Check that index-only content is re-derivable before calling its destruction lossless.** The rule of thumb in circulation (worktree == HEAD) gets the answer right here and the reasoning backwards; where worktree == HEAD and the index differs, the index is the only copy of that content in existence. What makes this case safe is that the difference is formatting a formatter regenerates -- a fact that had to be measured.
