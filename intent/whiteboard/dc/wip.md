---
node: dc
name: DevX Claude
role: worker
session_id: 5e0c098c-fe49-4647-a59d-07ba720ac5c3
heartbeat_at: 2026-08-26 20:31Z
status: active
focus: "**2b IS GREEN. THE PAIR IS STAMPED AT `03470c5a` AND THE HOLD IS LIFTED.** Five reads all green -- `intent --version` and the `intentd` marker both naming `03470c5a`, `intentd --version` rc 0, no `dirty-`, no `unknown`, HEAD identical at build start and end. **THE TWO READS I ADDED TONIGHT EARNED THEIR PLACE ON THEIR FIRST BUILD:** (0a) the dir the build NAMED, answered from the ABSENCE of the redirect line rather than an assumed path -- the read that would have caught the failure had the tree been dirty; (0b) mtime after build start, **where I nearly published a fabricated stamp because `stat -f %Sm` prints LOCAL time and I appended a `Z`.** **CENSUS 22 OF 22, 4 OF 4 CANARIES MISSING.** **THE PAIR CARRIES B1`s BYTES WITH TWO PROOFS UNARMED BY NAME -- NEVER B1 VERIFIED:** `force_without_a_tty_...` passes because the EOF confirm refuses too, and `default_removes_no_file_...` passes because the fixture makes the verb take the no-op arm. **BOTH ARE TONIGHT`S OWN `quiet.sh` SHAPE ONE LAYER DOWN: A GREEN THAT CANNOT TELL WHAT IT NAMES FROM SOMETHING ELSE PRODUCING THE SAME GREEN.** **`bin/.devbin` IS UNFROZEN; MY TWO 3.0.1 ITEMS ARE UNBLOCKED AND WAIT ONLY ON vc CONFIRMING THE SWEEP IS NOT OVER THE SAME FILES.** Open question raised, not claimed: the binary reports `3.0.0` while vc`s durable note says the version string stays `3.0.0-dev`."
claims: [ST0056/07, ST0056/11]
---

# DevX Claude (dc)

**Today's reasoning, dead ends and perishable measurements are verbatim in `.history/20260824/wip.md`. This file is the COLD-SESSION MINIMUM.**

## D42 -- TIME. Read this before writing anything, anywhere.

- **You never ask what time it is** -- not the OS, not `date`, not the filesystem, **and not the database either.** The stamp is applied BY the write.
- **THE RULE IS ABOUT SIGNATURES, NOT VALUES. NO cli or intentsvcs function TAKES a time; they may RETURN times.**
- **A board stamp is a label, not data** -- read it from `date -u` and PASTE, **per stamp, never per session.** The ordering that cannot be fabricated is the **commit**.
- **`stat` PRINTS LOCAL. `git log` PRINTS LOCAL.** Convert at the SOURCE and keep the local value beside it, or a reader appends a `Z` and lands an hour out. I did exactly that today and vc caught it.

## The truth model -- canon, held not restated

`design.md` (D01 as reversed) + `data-model.md`. **The SQLite db is the durable SSOT, files are re-creatable; the typed API is the only door in.** **`intentdb` IS RETIRED AND NAMES NO COMPONENT.** The crates are `intent-cli`, `intentd`, `intentsvcs`; `intentsvcs` solely owns the db and `intentd` is a CLIENT exactly as the CLI is. Diagram `design.md:12-17`.

## The environment

- **`intent` ON PATH IS v2.19.0 AND RESOLVES THROUGH `$INTENT_HOME` TO THE FROZEN `Intentv2`. v3 IS ALSO ON PATH, AS `intent3`** -- "DO NOT PUT v3 ON PATH" was retired 2026-08-22 by ST0058 and both restart files asserted it for two more days. The DISTINCT NAME is what leaves the fleet's gate untouched, by construction.
- **`intent3` NOW REFUSES A BINARY THAT CANNOT BE SHOWN TO DESCRIBE THIS TREE** (hv ruled 2026-08-24). Matrix and reasoning: `bin/.devbin/cmd/shared/currency.lib`, one home. **It keys on DECIDABILITY, never on dirt.**
- **THE GATE FIGURE IS COMPUTED BY THREE VERB CALLS AND IS NEVER TRANSCRIBED** -- `intent ac status ST0057`, `intent ac status ST0056/03`, `intent ac gate ST0057`. It lived in THREE homes at THREE values on 2026-08-24, one document disagreeing with itself twice. **Do not put the number on this board; put the calls.**
- **hv's FREEZE SCOPE (2026-08-24): Intentv2 is FROZEN FOR FEATURES and LIVE FOR SHIPPED-SURFACE DEFECTS.** A v3-only defect is a v3-only fix; a shipped-surface one lands in BOTH trees.
- **THE INDEX IS SHARED IN THIS CHECKOUT.** `git add` puts your file where a peer's bare `git commit` sweeps it. Always `git commit --only <paths>` -- **but the refusal is a property of naming a FILE, not of `--only`, and I had it as unconditional.** Driven both arms: `--only <untracked FILE>` exits 1 with `pathspec did not match any file(s) known to git`; **`--only <DIRECTORY>` exits 0, commits the tracked edits and leaves the untracked file behind in silence** (ic). **The success output enumerates what it TOOK, so the omitted file has no line to be missing from** -- this board's own absence rule, arriving at a commit instead of a grep. **`git status --porcelain -- <paths>` after every commit; `??` is the entire signal.** My own `6096e14c` used the directory form and was clean, which was luck rather than care.
- **`int hooks` ANSWERS ABOUT THE TREE THE `int` SCRIPT LIVES IN, NOT YOUR CWD.** To ask about another clone, run ITS `bin/int`.
- **A DEVBIN COMMAND RESOLVES ITS PROJECT FROM SOMETHING OTHER THAN YOUR CWD -- BUT THE v3 BINARY RESOLVES FROM CWD.** Both are true and confusing them cost a live incident today.

## DOING

**THE SMOKE ARM IS IN: `53699d66`, `int macos smoke` ARM 7. DRIVEN GREEN, THEN DRIVEN RED THREE WAYS, EACH RED LANDING ONLY ON THE ARM IT SHOULD.** Four sub-arms against the installed keg: the read verb RAN (its output names the thread id), the tree stayed clean, the planted divergence survived, and the WRITE control dirtied 4 paths.

**THE THREE NEGATIVE CONTROLS, THROUGH THE `--keg` DOOR SO NO SHARED FILE WAS EVER MID-EDIT** (a wrapper passing everything to the real keg except one verb):

    st triage swallowed     CONTROL fails, other three green
    st list swallowed       RAN fails -- and "tree clean" STILL PASSES
    st list re-renders      "tree clean" + "divergence survived" fail, RAN + CONTROL green

**CONTROL B IS THE ONE THAT PAID FOR THE WHOLE DESIGN, AND IT DEMONSTRATED THE POINT LIVE RATHER THAN IN ARGUMENT: WITH THE READ VERB SWALLOWED ENTIRELY, `read verb left the tree clean` STILL REPORTED OK.** Of course it did -- **a verb that never ran leaves the tree clean too.** Without the RAN proof beside it, a binary that did nothing at all would have shown three greens. **That is the null-population trap caught in the act, in my own instrument, before it shipped.**

**CONTROL C IS THE PROOF IT IS A GUARD AND NOT DECORATION**: a wrapper whose `st list` RUNS correctly -- right output, right rc -- and then silently strips the foreign line. Exactly the regression class. **Two arms red, and only the right two.**

**THE FIXTURE RE-DEMONSTRATED THE `init` GITIGNORE GAP ON ITS WAY PAST**: fresh `init` writes no `.gitignore`, so `git add -A` tracks `intent/.cache/intent.db`, and the arm has to lay the ignore down by hand. Stated in the file as a finding rather than absorbed as a fixture chore. Still ic's to fix, still `converge_gitignore` to CALL and never to rewrite.

**THE STAMP-VERSUS-SUBJECT FIX IS HELD, AND vc RULED THE ORDER I PROPOSED:** sweep completes on `03470c5a` -> the fix lands with arm 6 extended IN THE SAME COMMIT -> a build -> the five reads re-run -> **the marker's new meaning written into the runbook BEFORE anything consumes it**. Not caution: that change alters what the stamp MEANS on every artefact, and landing it between a stamp and the sweep that trusts it would leave the runbook reading correctly about a different question. **The expiring-sentence class, named before it happened rather than after.**

**2b IS GREEN AND THE PAIR IS STAMPED AT `03470c5a`. THE HOLD IS LIFTED AND `bin/.devbin` IS UNFROZEN FOR ME.** Build 20:22:06Z -> 20:23:12Z, 1m 05s, rc 0; `verify_pair` named `03470c5a` for both binaries; **HEAD read at start AND end and identical both times**; dirt under both scopes 0 at the moment of the reads.

**THE FIVE READS, VERBATIM:**

    0a  dir the build NAMED   no "redirecting to a PRIVATE target dir" line -> verdict ok -> SHARED
                              native/rust/target/release   (target/private/release: DOES NOT EXIST)
    0b  mtime after start     intent  20:23:12Z    intentd 20:22:11Z    floor 20:22:06Z    BOTH AFTER
    1   intent --version      intent 3.0.0 (03470c5afc5738e0998d52a822f1e2f39fa349ae)      rc=0
    2   intentd MARKER        [intent-source-commit:03470c5afc5738e0998d52a822f1e2f39fa349ae]
    3   intentd --version     intentd 3.0.0 -- not yet implemented                          rc=0

**THE TWO NEW READS EARNED THEIR PLACE ON THE FIRST BUILD THAT USED THEM, IN OPPOSITE DIRECTIONS.** 0a answered from the **ABSENCE of the redirect line** rather than from a path I assumed -- it is the read that would have caught tonight's failure had the tree been dirty. 0b is the stale-artefact read, **and it is where I nearly published a fabricated stamp**: `stat -f '%Sm'` prints LOCAL time, so my first pass rendered both mtimes **one hour ahead (local +0100) with a `Z` I appended to a local reading** -- the exact wrong-clock error the protocol names. **The bad values are deliberately NOT written here as `Z` stamps: they postdate this commit, and the clock guard would be right to refuse them.** The comparison was always sound (both sides epochs, no zone involved); **only the LABEL was wrong, and a wrong label is what reaches the board.** Caught before it left my hands. **Canonical UTC: 20:23:12Z and 20:22:11Z.**

**CENSUS: 21 OF 21 PRESENT, 3 OF 3 ABSENT CANARIES CORRECTLY MISSING**, definitions driven separately from usages (`ATTACHMENT_CAP_BYTES` project.rs:50, `within_attachment_cap` project.rs:64, `declare_default` facade.rs:2301, `default_declaration` intentfiles.rs:539). Canaries were the inverse of a real test name plus two invented constants, same shape and same grep path as the real ones.

**WHAT THE STAMP DOES NOT SAY, WRITTEN DOWN SO NOBODY READS IT IN.** The pair CARRIES B1; **the stamp says what is in the BYTES and never that it was mutation-proven.** vc's five B1 mutations are **UNRUN by vc's own word**; `AC-00.5` is **UNCENSUSED, not verified** (vc named a sha and a criterion, no symbol, and supplying one from context is the false-MISSING I filed against myself this morning); **`AC-11.3` has no proof test on ic's own admission**. And vc's S-guard mutation site `facade.rs:5709` is at **`:5855`** in HEAD -- ic's B1 floor pushed it 146 lines down, so **a `sed` at the stale line would hit a REAL line and mutate something else silently.**

**CENSUS CLOSED AT 22 OF 22, 4 OF 4 CANARIES MISSING.** vc supplied the `AC-00.5` symbol I had refused to invent: `a_declared_but_unmet_precondition_refuses_and_names_it`, **PRESENT at `facade_dehydrate.rs:251`**, with `59d94941` confirmed an ancestor of the stamped HEAD by `merge-base` and its own inverse-named canary correctly MISSING. **Holding the gap open for ninety minutes cost nothing and asking for the name cost one line.**

**vc's B1 MUTATION VERDICT: THREE ARMED, TWO PROOFS UNARMED -- AND BOTH UNARMED ONES ARE TODAY'S CLASS INSIDE A TEST.** ARMED: AC-11.1 (`intentfiles.rs:556`), AC-11.2/present (`facade.rs:2328`), AC-11.5 (`dispatch-table.json:7668`). **UNARMED, BY NAME:**

- **`force_without_a_tty_writes_nothing_and_exits_non_zero`** stays GREEN with the tty guard at `render.rs:1739` mutated to `if false`, **because the EOF confirm refuses too and the test reads only the exit code.** The evidence is true -- it did exit non-zero -- and the SUBJECT is wrong: the test cannot tell the tty guard from the confirm behind it.
- **`default_removes_no_file_belonging_to_an_undeclared_thread`** stays GREEN under both vc's removal mutation and ic's organize-after-write, **because the fixture's manifest is already PRESENT after `init` + `st new`, so the verb takes the no-op arm.** It asserts survival across a run that never acted -- **a check that cannot exhibit the failure it is named for.**

**THIS IS THE SAME SHAPE AS MY OWN `quiet.sh` FINDING TONIGHT, ONE LAYER DOWN: A GREEN THAT CANNOT DISTINGUISH THE THING IT NAMES FROM SOMETHING ELSE PRODUCING THE SAME GREEN.** ic confirms both as its own test defects and the fixes ride the next build with vc re-running the mutations. **So the pair CARRIES B1's BYTES WITH TWO PROOFS UNARMED BY NAME -- never "B1 verified", and vc asked for it in exactly those words.**

**AND THE LINE-VERSUS-NAME POINT IS SETTLED AS MECHANISM, NOT ETIQUETTE:** vc's `mutate-at.sh` addresses a site by **REGEX at the sha named**, never by line -- `facade.rs:5709` was `b47fc3ec`'s line and the ARMED result was taken there; `:5855` is the same predicate at HEAD. **The name is the address; the line is a courtesy that ages.**

**ONE QUESTION RAISED, NOT A FINDING.** `intent --version` reports **`3.0.0`** and `native/rust/Cargo.toml:6` is `version = "3.0.0"`, while vc's 11:14Z durable note says _Version string stays `3.0.0-dev`_. Not calling it wrong -- it may have been overruled, or `-dev` may name something else. Raised because **the artefact I just stamped is what ships and the two sentences disagree in front of me.**

**2b's REAL FAILURE MODE, FOUND AT 20:02Z WITH THIRTEEN MINUTES ON THE CLOCK, AND IT IS NOT THE ONE I HAD WRITTEN DOWN.** I had been carrying _a dirty `native/rust` is refused_. **IT IS NOT. `releasebuild.lib:53`, in its own words: A DIRTY TREE IS REDIRECTED, NEVER BLOCKED** -- deliberate and right, because a gate that refuses on a peer's half-finished file gets bypassed the first afternoon. The verdict string is `refuse:`, so the guard's own arm 1 is correctly named; what follows the refusal is a **redirect**, not a stop.

**SO A 2b ON A DIRTY TREE SUCCEEDS AND BUILDS NOTHING I WILL EVER READ:** verdict `refuse:` -> redirect to `native/rust/target/private/release` -> build -> stamp `dirty-<sha>` -> `verify_pair` matches `private:dirty-*` -> **`: # expected: this is what the redirect was for`** -> **`ok`, rc 0, `done.`** -- and **`native/rust/target/release` IS NEVER TOUCHED.**

**AND THEN THE THREE READS LIE IN THE DIRECTION THAT LOOKS LIKE A PASS.** Every consumer is hard-wired to the SHARED dir (`cmd/local:214-215` points `intent3`/`intentd3` at it, `cmd/macos:65` + `:881` stage COPIES out of it, `cmd/cli:43`, `cmd/hosting:54`). Measured at 20:00Z: `target/release/intent` mtime **19:27:35Z** marker **`43c3d2d0`**, `intentd` 19:26:44Z same marker, and `target/private/release` **DOES NOT EXIST**. That is **2a**. The lift would return three green stamps, a real sha, both binaries agreeing, `intentd --version` rc 0 -- **every one of them TRUE, and every one of them ABOUT THE PREVIOUS BUILD.**

**MY `dirty-` DEFENCE NEVER FIRES HERE, WHICH IS THE PART THAT MAKES IT DANGEROUS.** _A `dirty-` stamp is a REFUSAL, not a confirmation_ is still true and still useless: the dirty stamp goes to the private dir nothing reads, and the clean stamp I do read is last build's. **The eighth instance of the day's class, aimed at the instrument I was about to certify a release with.**

**TWO ADDITIONS TO THE LIFT, ACCEPTED BY vc INTO THE RUNBOOK, COSTING NO EDIT TO `bin/.devbin` (which stays frozen):** read the pair from **the dir the build actually named**, echoed off its own redirect line rather than assumed; and assert **mtime AFTER the build started**. A binary reading 19:27:35Z at 20:15Z is 2a whatever its marker says. **mtime is the one property a stale artefact cannot forge.**

**AND THE GATE IN FRONT OF ALL OF IT IS LOOSER THAN THE GATE BEHIND IT.** `quiet.sh:9` reads `git status --porcelain -- native/rust`; `sharedtarget.lib:120` declares `SHARED_TARGET_DIRT_SCOPES=(':(top)native/rust' ':(top)surface')`. **`quiet.sh` IS A THIRD HAND-COPIED COPY OF THAT SCOPE AND IT HAS ALREADY DRIFTED NARROW** -- and `sharedtarget.lib:72` says plainly that looser is the unsafe direction, in a paragraph written about the marker, never about this third copy. At vc's planned end-state (ic's floor committed or parked, **dispatch-table rows left uncommitted**) `surface/dispatch-table.json` is the only dirty path, so `quiet.sh` reads **0 and says QUIET** while `shared_target_verdict` reads **1 and redirects**. Both gates green, the build a no-op, the lift on 2a.

**AND MY PROTOCOL STEP 2 BUYS NOTHING AGAINST IT.** _I drive `quiet.sh` myself in the same minute_ was written so my confirmation would be independent of vc's. **It is the same instrument with the same blind spot: two reads, one hole.** I had been treating agreement as independence. **Currently moot -- 7 dirty under the guard's scope, 6 under the marker's, both non-zero -- and that is exactly the trap: it bites only at the state we are steering toward.** Reported to vc with the one-liner; **`quiet.sh` is vc's file and I did not touch it.** **vc LANDED THE WIDENING WITHIN THREE MINUTES AND I VERIFIED IT MYSELF RATHER THAN ACCEPTING IT** -- `quiet.sh` now reads `native/rust surface`, its header carries the mechanism and the reason, and driven at 20:04:32Z it reported **8 paths and 2 live cargo executables, NOT quiet, rc 1**, against my own independent counts of **8 over the guard's declared scope and 7 over the marker's**. **The tree moved between my two reads (7 -> 8, `views.rs` appeared) with two cargos live, which is the polling hazard demonstrating itself while I watched.** ic is told the 20:15Z park includes the dispatch table so BOTH scopes read 0. Nothing anywhere references it -- `grep -rl 'quiet\.sh'` across `bin/`, `lib/`, `intent/st/` returns **nothing** -- so the gate that decides whether a release build may proceed is in no roster, no test and no doc.

**`legacy.rs` IS PARKED, ANSWERED TO vc AS ASKED:** byte-identical to its HEAD blob under `cmp`, gone from `git status`, **with the same test on `facade.rs` returning DIFFERS as the positive control** so the identical is not a blind instrument.

**ONE READING OF MINE THAT MUST NOT BE QUOTED BACK: sourcing `sharedtarget.lib` standalone returned `refuse:cannot reach git ... undecidable`. THAT WAS MY HARNESS MISSING `$GIT_ISOLATED`, NOT THE TREE.** Every count above is plain `git status` over each declared scope. Said out loud because a `refuse:` string pasted into a runbook reads as the guard's verdict.

**NOTHING IN FLIGHT. 2b FIRES AT 20:15Z, HARD, ON WHATEVER IS IN HEAD** -- hv's clock, vc's earlier slack withdrawn. It is a **BUILD, NOT THE CUT**: more builds follow as the rest lands (bucket ingest + prune, the attachment walk, the issue-body write defect, B1's remaining criteria), and **the cut is when 3.0.1 is COMPLETE, on hv's go**.

**hv, VERBATIM: _THERE IS NO 3.0.2. There is 3.0.0 which we have made and then EVERYTHING ELSE IS GOING INTO 3.0.1._**

**THE PROTOCOL AT EVERY BUILD, TWICE DRIVEN NOW:**

1. vc calls QUIET with the shas, the function names to test **by DEFECT**, and an explicit **composition line** naming what the pair carries AND what it does not, by criterion id -- so an absence is written down rather than something someone has to notice.
2. I drive `intent/whiteboard/vc/quiet.sh` myself **in the same minute**. **vc says quiet; I do not poll** -- polling races a peer's commit into my compile window.
3. **Census by NAME, never by line number.** Grep the string the claim actually made and let the TREE say where it lives. Carry a deliberate **ABSENT canary**, or a probe that says yes to everything looks like a pass.
4. **HEAD frozen from the QUIET message, whiteboard included**, across the whole compile.
5. **Three reads, verbatim, never normalised:** `intent --version` (runs AND reports), the `intentd` **marker** (bytes carry), `intentd --version` rc 0 (**the only read that EXECUTES the daemon**). A `dirty-` or `unknown` stamp is a **REFUSAL**, not a confirmation.

**2a LANDED GREEN AT `43c3d2d0`** -- 18:27:35Z, 54.10s, `verify_pair` green, census 23/23 with the canary correctly MISSING, and **HEAD held across the entire window: the first compile that was PROTECTED rather than lucky.**

**`bin/.devbin` STAYS UNTOUCHED UNTIL THE PAIR IS STAMPED.** Arm 10 censuses it from the **worktree**, so a mid-edit file there refuses **every node's** commits. I did exactly that once today, during a release window.

**MY TWO 3.0.1 ITEMS ARE SPECCED, BLOCKED AND NOT STARTED** -- the smoke arm and the stamp-versus-subject fix, designs settled in TODO. The `init` gitignore call went to ic (`init.rs` is theirs); the three stranded `3.0.2` comments are re-worded by ic and cc in commits that already touch those files.

## TODO

- **hv HAS ABOLISHED 3.0.2 AND THAT TURNED MY BACKLOG INTO SCOPE.** Verbatim, first-hand via vc: _THERE IS NO 3.0.2. There is 3.0.0 which we have made and then EVERYTHING ELSE IS GOING INTO 3.0.1._ The 20:15Z QUIET is a **BUILD** (2b), not the cut; more builds follow as the rest lands (bucket ingest + prune, the attachment walk, the issue-body write defect, B1's remaining criteria), and **the cut is when 3.0.1 is COMPLETE, on hv's go**. **MY THREE ARE NOW 3.0.1 AND ALL THREE ARE BLOCKED UNTIL 2b's PAIR IS STAMPED**, because each touches `bin/.devbin` (arm 10 censuses it from the WORKTREE) or `native/rust` (arm 1): the smoke arm, the `init` gitignore call, and the stamp-versus-subject fix. Sequenced after 2b so nothing under `bin/.devbin` is ever mid-edit under a build.
- **THE RULING STRANDED THREE DEFERRALS IN SHIPPED SOURCE, AND I SWEPT FOR THEM RATHER THAN WAITING TO MEET ONE.** `native/rust/crates/intentsvcs/src/facade.rs:137` holds AC-11.3's other half (the migration realising ONLY declared threads) _to 3.0.2_; `tests/attachment_carry.rs:142` and `:242` defer the attachment allowlist the same way. **Each names REAL work whose destination no longer exists**, and a comment deferring to a release that was abolished reads to the next person as a live decision. The boards fold themselves; **source comments do not**. Reported to vc for hv -- not mine to edit, and `native/rust` is frozen for the build regardless.
- **SMOKE ARM SPEC, READY TO BUILD THE MOMENT 2b IS STAMPED (`int macos smoke`, WP-11, mine).** Fixture: a project converted by the **OLD keg** -- the only tree whose views are in a rendering the pair did not produce, and therefore the only one that can exhibit a re-render. **Its store must be gitignored or the db and its WAL swamp the assertion** (measured tonight: an untracked WAL beside a tracked db turns routine `git clean -fd` into silent data loss). Arm: run ONE read verb with the new binary, assert `git status --porcelain` is EMPTY. **AND THE ARM THAT MUST FALL SILENT NEEDS THE ARM THAT FIRES BESIDE IT, WHICH IS THE THING I LEARNED TWICE TODAY**: immediately run a WRITE verb (`st triage`) and assert `git status` is NON-empty, or the whole check passes just as well on a broken fixture. **Prove the read verb RAN before trusting its rc** -- assert its output carries a known thread id -- because an unparsed argument answers `unrecognized subcommand` at a perfectly plausible rc.
- **STAMP-VERSUS-SUBJECT: THE DESIGN, SETTLED IN THE QUIET RATHER THAN UNDER THE CUT (3.0.1, mine).** **THE DEFECT IS VISIBLE IN SEVEN LINES OF ITS OWN SOURCE.** `emit_source_commit` (`source_commit.rs:190`) asks TWO questions about TWO DIFFERENT SUBJECTS and packs both answers into one string: `git rev-parse HEAD` is **unscoped** and asks about the REPO, while `git status --porcelain -- ":(top)native/rust"` is **scoped to `DIRT_SCOPE`** and asks about the ARTEFACT. So the value means _the repo's HEAD, annotated with whether the subject was dirty_, and `releasebuild.lib:105` then compares it against `rev-parse HEAD`, unscoped again. **It is internally inconsistent TODAY, not merely awkward.**
  - **TWO FACES, ONE CAUSE.** (a) any commit outside `native/rust` landing during the ~60s build REDS A CORRECT PAIR -- it nearly took 2a and was prevented only by vc freezing every node including whiteboard; (b) the marker REWRITES THE STAMP OF BYTE-IDENTICAL CODE, so two builds of identical source carry different stamps -- which is exactly what forced the 2a re-name off `3d9ea45f`.
  - **FIX: ASK THE IDENTITY QUESTION ABOUT THE SUBJECT THE DIRT QUESTION ALREADY USES.** `git rev-list -1 HEAD -- <DIRT_SCOPE>` in place of `rev-parse HEAD`, and the same substitution at `releasebuild.lib:105`. **This introduces no new idea -- it makes the function self-consistent**, because the scoped half is already there and already right.
  - **WHAT IT COSTS, STATED RATHER THAN DISCOVERED.** The stamp stops identifying the REPO: two repo states with identical `native/rust` produce identical stamps, and _which commit was this built at_ becomes unanswerable from the artefact. **That is the correct trade and nothing is lost, because the two questions already have two homes** -- the release TAG records the repo, the marker records the subject. An artefact should carry what it IS.
  - **REJECTED: CARRYING BOTH VALUES.** Two embeds means every consumer must know which to read, and the failure is SILENT -- a consumer reaching for the subject and getting the build-time HEAD receives a plausible sha that is wrong. **One value with one meaning beats two with a convention.**
  - **BOTH SIDES MOVE IN ONE COMMIT**, which is this board's own rule paid for today: a guard's subject is part of the guard. `arm 6` already asserts the guard's scope CONTAINS the marker's `DIRT_SCOPE`; **it must also assert the marker's IDENTITY uses it**, or the two halves drift apart again with the arm still green.

- **RETRACTED WITHIN THE HOUR, AND WHAT DIED IS THE PREMISE, NOT THE MEASUREMENT. READ VERBS DO NOT REWRITE VIEWS -- NOT IN THE PAIR AND NOT IN THE 3.0.0 KEG EITHER.** vc's original sentence was that the shipped keg (`80d8b2ca`) silently re-rendered 167 tracked views on `st list` / `ac gate`. **Three independent measurements refute it**: lamplight-vc ran the KEG on a Lamplight clone (`st list`, `ac gate` x6 -- 0 files), vc ran the KEG on a Cdsync clone (0 files), and I ran the PAIR on convergent and divergent fixtures with a write control (0 files). vc has retracted it on their board and in the runbook. **WHAT REWROTE LAMPLIGHT'S 167 FILES AT 19:45:00 IS OPEN AND LABELLED AS OPEN.** `issues new` refuses before writing on that tree; the one hypothesis still standing is cc's _one emitter_ -- hop 2 emits views its own way and `write_all` re-renders them differently -- testable after 2b on a clone. **On `43c3d2d0`, in a scratch fixture, four read verbs (`st list --status all`, `st show`, `info`, `todo`) left the tree at 0 dirty paths against BOTH a convergent tree AND one whose tracked view diverges from the store's rendering** -- the foreign line survived all four -- **while `st triage` dirtied 4 paths in the same fixture**, so the fixture demonstrably shows tool writes and the zero is not a blind instrument. **Honest limit: my divergence is an inserted comment, not vc's older RENDER FORMAT**, so this shows the pair does not blindly rewrite, not that no divergence shape can make it. **THE SMOKE ARM REMAINS A REGRESSION GUARD; MY UPGRADE-NOTE POSITION IS REVISED BECAUSE ITS REASON IS DEAD.** _run `intent upgrade` before any other verb_ still stands as ADVICE and goes in the 3.0.1 notes, but it **must not carry the read-verb rewrite as its reason** -- that claim no longer exists. If the emitter hypothesis proves, the reason becomes **the first WRITE verb after migration re-renders every view**, which is a different sentence about a different verb class and must be written as one rather than edited into the old one. Until then the note is advice with no named mechanism, and saying so is the honest form. **THE SMOKE ARM IS NOT FOR TONIGHT AND THE REASON IS NOT CAUTION: it means editing `bin/.devbin/cmd/macos`, which arm 10 censuses, and a mid-edit file under `bin/.devbin` refuses EVERY peer's commits** -- I did exactly that once today, during a release window.
- **SHARPENED BY THE HIGHLANDER CHECK, AND THE MECHANISM IS NOT MISSING -- ONLY ONE POPULATION REACHES IT.** `converge_gitignore` EXISTS at `facade.rs:152`, is correct (a PATH rule not a class rule, no-op when present, appends without clobbering), and **its only caller is the migration path at `facade.rs:1376`**. So a MIGRATED project gets the rule and a **freshly `init`-ed project does not, while acquiring a store exactly the same way**. **THE DEFECT IS WRITTEN INTO THE FUNCTION'S OWN DOC: `facade.rs:111` says _this is the one moment a project acquires one_ -- true of the v2-to-v3 population, false of fresh init, and stated generally.** Today's class, in a docstring, where it will stop the next reader noticing. **The fix is to call the existing function from `init`, never to write a second one.** Driven: `init` in an empty dir leaves no `.gitignore`, and `git add -A` then tracks `intent/.cache/intent.db`. **Intent's own repo ignores it at `.gitignore:125` with a comment saying exactly this** -- _one `git add -A` from entering history as a binary_ -- so the hazard is known and the protection is not laid down at init. Mine (init/packaging surface); queued, not built.

### LIVE, MINE, UNSTARTED

- **THE KEG SHIPS NO RULE LIBRARY AND NO SKILLS. THE CODE FIX IS DONE (`0112b8c1`) AND IT IS NOW NAMED IN vc's 3.0.1 CUT LIST, SO THE SHIPPING HAS A DATE.** `SUPPORT_PATHS` carries the rules and skills trees; `support_paths_coverage` refuses at `stage` if v3 resolves an install-root path the tarball would not carry; `int macos smoke` is driven RED against the live keg and GREEN against a fixture at `total: 6 rule(s)`. **UNTIL THE RE-CUT PUBLISHES, THE ESTATE'S KEG STILL ARMS 0 OF 0 ON EVERY LANGUAGE** -- the fix and the shipping are different things and only the second one reaches a user. Cause found by devbin/vc as a PACKAGING gap, located to `:116` by vc, resolver enumerated by me.

- **FLEET WORK IS LIVE AND HALTED BY hv (2026-08-26 11:53Z, relayed devbin-cc -> vc). MY FOUR ARE DIRTY AND UNCOMMITTED -- see DOING for the exact table. hv LIFTS IT, NOT vc.**
- **FLEET WORK UNDER vc's RUNBOOK IS THE TOP ITEM AND hv AUTHORISED vc TO DIRECT IT.** `intent/whiteboard/vc/cutover-runbook.md`, plus `verify-canonical.sh` (self-test re-driven by me after vc's fix: **11 failures, threshold 11**). **THE CUTOVER IS 16 PROJECTS IN TWO SHAPES, NOT 9** -- Form B projects wire `session-context` and `require-in-session` but NOT `session-finish`, so the third hook is missing fleet-wide. **`Intentv2` IS NOT A MEMBER AND MUST NEVER BE MIGRATED** (hv ruled it unprompted; the tool warns unprompted too).
- **`use`'s PROD ARM: THE PRECONDITION EXPIRED 2026-08-26 AND THIS BULLET WAS WRONG WITHIN THE HOUR OF BEING WRITTEN.** It said _will stay undriven until a formula exists_; **a formula now exists in the tap and the keg is INSTALLED and UNLINKED**, so the prod arm is DRIVABLE and simply has not been driven. **I shipped this exact class in `use prod`'s own refusal message today (`0347aab7`) -- a sentence true when written that expires silently and reads as a working statement -- and then left it standing on my own board while fixing it in code.** Still true: it REFUSES rather than proceeding, which is what makes an unrun arm safe rather than merely admitted (cc). **Drive both arms against the installed keg; that costs a `brew link` and the flip is hv's, so it waits.**

- **WP-07's CHEAPEST REAL TARGET, HANDED TO ME BY vc WITH THE MEASUREMENT ATTACHED: `intent_claude_cwi:31` IS ONE LINE -- `source "$INTENT_ROOT/bin/intent_helpers"` -- AND IT IS THE FAMILY'S ENTIRE `bin/` DEPENDENCY.** Porting `cwi` off it **retires `AC-12.1`'s dependency without anyone deleting a verb.** **AND IT DOES NOT DISCHARGE `AC-14.12`, WHICH IS WRITTEN INTO BOTH ROWS SO NO LATER READER COLLAPSES THEM:** WP-14 retires the VERBS (served from the store per `AC-14.7`); WP-07 retires the `bin/` COUPLING. Two different things, two different closing conditions. **hv ruled the `claude ws` family SURVIVES the cut with an expiry the gate enforces (`97cc09ca`).**

- **A SIXTH FOR hv, RAISED BY vc FROM THE OTHER SIDE AND IT IS THE SAME CONVERSATION AS (2): `guarded_release_build`'s VERDICT IS ABOUT THE TREE AND SAYS NOTHING ABOUT THE MACHINE.** vc's point: a guard that reads only git fires green over a build already in flight. It is right, and the sharp form is about my own arm 7. **ARM 7 ASSERTS ORDER -- the verdict is taken before the first cargo invocation -- AND NOTHING ASSERTS LATENCY.** cargo does not fail on a locked target, it WAITS (`waiting for file lock on ` is in this cargo's own strings, driven), so a verdict taken at T can be followed by an unbounded block and then a compile of a tree that moved. **The ordering property I built and tested is true and insufficient, and it looks sufficient.** A process-list read narrows that window; it cannot close it, because the list can go non-empty one instruction after it is read. **What closes it structurally is queued item (2), the staging-dir build** -- build private, verify, move into place -- which needs neither a lock nor a process list. So (6) is evidence FOR (2), not a rival to it.
- **THE GATE ON MY OWN BUILD WAS BLIND AND IS NOW FIXED (vc, `dce909f7`) -- BUT THE HOUR IT SPENT BLIND IS THE ENTRY.** `quiet.sh` reported `cargo processes: 85` while the matching pids were three `claude` sessions, two `zsh` wrappers and ONE real cargo. **The peer sessions matched `pgrep -f` on the phrase `cargo fmt` carried inside their own `--append-system-prompt`**, from the restart text every node boots with; those processes never exit, so the gate was pinned NOT quiet independent of the tree and of cargo, and **could never have emitted the QUIET my rebuild waits on.** vc now reads the EXECUTABLE (`pgrep -lx cargo`) and counts pids. **I drove the negative arm myself rather than accepting the fix: the three sessions match 0, and `pgrep -x cargo` yields exactly the one real pid.**
- **NEITHER OF OUR POSITIVE CONTROLS COULD HAVE CAUGHT IT, AND THAT IS THE PART TO KEEP.** vc spawned a process with argv rewritten to a fake `cargo clean`, saw NOT quiet, and read again after it exited -- both reads NOT quiet, the first for vc's reason and the second for this one. **A control that ADDS a matcher to an instrument already matching everything exercises only the arm that FIRES, never the arm that must FALL SILENT** -- and the silent arm is the entire value of the gate to me. Mine was no better: I controlled the pattern SYNTAX with a `sleep 4` and proved alternation works, which says nothing about what else the pattern sweeps in. **Between us we controlled the same half twice.** The estate's rule wants sharpening: a control must drive the verdict the CALLER acts on, and here the caller acts on QUIET.
- **AND `cargo processes: 85` WAS A LINE COUNT OVER MULTI-LINE argv** -- six pids, reported 85, drifting to 57 between two reads because the prompt text differs. **Fourth wrong count reading as authoritative in one day**, after `smoke` saying fails 6 while listing 3, arm 10's 1-examined-of-5, and ic's population question.
- **THE GENERAL TRAP, WHICH IS NEW AND WILL BITE EVERY INSTRUMENT WE POINT AT THIS MACHINE: IN A FLEET OF LLM SESSIONS THE AGENTS' OWN INSTRUCTIONS ARE PART OF THE MACHINE'S OBSERVABLE STATE.** Our restart doc NAMES the commands our instruments hunt for, so any `pgrep -f`, any `ps | grep`, any log scrape over this box reads our prompts as evidence of the thing they describe. **A document warning about a command is indistinguishable, to a full-command-line matcher, from the command.** Remedy reported to vc and not patched by me -- it is vc's file: match the EXECUTABLE (`pgrep -x cargo`), and count pids rather than lines.
- **FIVE QUEUED FOR hv, NONE BUILT, AND ALL FIVE TOUCH INSTRUMENTS THAT GATE OR DESCRIBE THE BUILD.** (1) **roster symmetry** -- `runner_roster_check.sh` reads PRESENT from the COMMIT and ROSTERED from the WORKTREE; four occurrences on 2026-08-25 and the fourth was PREDICTED FROM THE WRITE-UP BEFORE IT HAPPENED. (2) **the staging-dir build** -- build into staging, verify, move into place: closes ic's absent-window AND vc's concurrency gap with no lock, so no stale-lock failure after a SIGPIPE. (3) **`verify_pair` compares the binary against HEAD when the binary's subject is `native/rust`** (ic; confirmed independently on my own build, which redded because a peer's canon commit landed during my 54-second compile) -- 104 commits that day, 11 touching `native/rust`, and the remedy it implies is a rebuild that races the same board traffic. (4) **D42 amended** -- the rule is NO CALLER AUTHORS A STAMP; the signature test is one sufficient condition and never the definition, and a generic setter reaching `created` evades the proxy while producing the forbidden behaviour. (5) **the MARKER's identical scope blind spot** -- `DIRT_SCOPE` in `source_commit.rs` is `native/rust` only, so a build dirty only in `surface/` is not stamped `dirty-` and **the artefact cannot disown what my guard now refuses**; that asymmetry is exactly why arm 6 is CONTAINMENT and not equality. Routed, not reached into.
- **THE ROSTER SYMMETRY FIX -- FOUND BY ME, QUEUED FOR hv, AND EXPLICITLY NOT MINE TO BUILD AT SPEED.** Read ROSTERED from the COMMIT as PRESENT already is. **It gates every commit in the estate and I caused two estate-wide blocks the day I found it**; vc endorsed the hold in those terms. It is the same conversation as the roster-charter ruling already with hv, because both are about WHAT POPULATION the roster is entitled to read.

- **`cmd/macos` provenance writer** so `provenance_fields_check.sh` (AT-11.7) has a green to reach. **STILL DECLINED ON SCOPE** -- WP-11 is RELEASE and hv asked for local usability. **TRAP: `codesign --force` REWRITES THE BINARY IN PLACE**, so nothing may hash until `verify_notarised` passes; and `cmd/macos:1294` parses `commit:` with a `sed` -- ADD fields, never rename that one.
- **`thread_view_skew_check.sh` roster admission** -- held on a staleness refusal that does not exist. **THE HOLD RESTS ON A MEASUREMENT FROM 2026-08-20 AND MUST BE RE-DERIVED BEFORE IT IS ACTED ON IN EITHER DIRECTION.** Build `lib_binstale.sh` as an EXTRACTION of `surface_check.sh`, never a copy.
- **THE ATTRIBUTION GUARD -- RULED IN BY hv IN vc's SESSION, AND I AM HOLDING FOR hv's WORD IN MINE.** One arm, gates, assert `^Claude-Session:` ABSENT, nothing about `(C)`. **Three conditions, none optional:** (1) key on the COMMIT, never the `wb|board|localfold` subject convention -- 29 of 90 carriers were topic-prefixed real work (ic); (2) ANCHOR it -- the bare string matches a message that merely DISCUSSES the trailer, and it matched two of ours (ic); (3) **a positive control the guard actually FAILS -- mine, and it is the one that makes it an instrument** (see Family 1). **ic's fourth, found by hitting it: the success condition is a `grep -c` INVERSION -- `grep -c` exits 1 when the count is ZERO, which is the PASSING case, so `grep -c ... && ok` blocks every clean commit. COMPARE THE NUMBER, NEVER THE EXIT STATUS.**
- **NEW, AND IT IS MINE BECAUSE I FOUND IT IN MY OWN FILE: NOTHING VERIFIES THAT A ROSTER ROW DESCRIBES WHAT ITS RUNNER DOES.** `runner_roster_check.sh` verifies row-to-file EXISTENCE in both directions and is structurally blind to the row's CLAIM. cc asked that a mechanism, if one is built, be mine. **No mechanism proposed yet, and naming it is not building it.** **AND vc HAS PUT A GATE IN FRONT OF BUILDING IT, WHICH I AM RECORDING BECAUSE IT WOULD OTHERWISE DIE WITH THIS SESSION: DO NOT BUILD BEFORE hv HAS RULED WHETHER A DESCRIPTION IS IN THE ROSTER CHARTER AT ALL.** vc framing: this is a guard whose POPULATION IS FILES where the CLAIM IS BEHAVIOURS, and **widening a guard contract silently is how a roster becomes the mechanism.** So the next step is a ruling, never a checker.

### ROUTED, MEASURED, NOT MINE TO TAKE

- **ISSUE `0086` (HIGH, ic's find, QUEUED NOT ASSIGNED): `intent --help` rc=0 while `intent help` rc=2 `retired`, with a remedy claiming no replacement exists.** **Its general defect is this board's own class from a third direction: an ABSENT `spelling` field is RENDERED as a confident negative, and a passing test REQUIRES it to be** -- absence-as-meaning, forbidden by the preamble of the very file it lives in. Not mine unless assigned.

- **`VIEW_NAMES` (`address.rs:357`) CLAIMS A COUPLING THAT IS NOT IN THE CODE** -- its doc says `Project::classify` _is the single answer_, and `classify` (`project.rs:744`) keeps its own inline list and never reads it. Different memberships (`todo.md`, `steel_threads.md` in one and not the other). **NO HARM DRIVEN AND NONE CLAIMED**: they may be legitimately different populations, in which case the DOC is the defect. **Routed to cc AS A QUESTION, not a finding, because `project.rs` was in cc's working set while I was writing it** -- _check whether this is still true after cc lands._ **ANSWERED AND FILED WHILE ALL FOUR NODES WERE COMPACTING: cc drove it (`e63813a2` was 250 pure insertions with zero `classify` occurrences, so STILL TRUE, UNCHANGED), vc re-drove it and filed **issue `0087`** (low) at `4f9ce518`. Three hands, no harm claimed by any of them. **The row exists because the sentence is un-greppable and reads as an architecture guarantee** -- the same shape as the unit-test doc that said _nothing but this test compares them_. Not mine to fix.
- **ISSUE `0085` -- THE ADVISORY HOOK FIRES ON EVERY WRITE REGARDLESS OF FINDINGS.** `[ -z "$findings" ]` can never fire: both binaries always put `critic:` header lines and an `ok:` line on STDOUT, the stream the hook captures. **Violates `session-finish.sh:46` -- _a hook that fires identically every time carries no signal_ -- three files from where that sentence is written.** v2 does it identically, so **INHERITED: a `corrected`-class contract decision in `AC-07.3(a)`'s shape, with the contract and not with me.** Opt-in twice. **OPEN QUESTION I DELIBERATELY DID NOT DRIVE (23:16Z, holding): `0085`'s premise rests on what the hook's FLAGGED invocation emits -- `--files <f> --severity-min warning --format text` -- and my drive used plain `--files` only.** vc verified independently and it is their filed issue, so re-driving it unasked is scope creep. **One command if anyone wants it, and worth knowing the premise has not been driven at the flags the hook actually passes.**

- **THE FROZEN-`$INTENT_HOME` MECHANISM: THE DETECTOR HALF IS CLOSED, THE ROUTING IS NOT.** vc landed the ref fix and the CI arm (`a38e884b`, `18ccfbbc`), measured in CI's own log rather than in simulation. **vc states plainly that their reason for `not discharged` expired but the ROUTING did not, and a guard cleared by a peer saying the ruling happened is not a guard.** It sits with hv. **What the guard should ASSERT under the freeze scope is still the unsettled half.**
- **THE SUITE POPULATION CHECK.** `git ls-files` 112 vs `find` 113, gap named, two commands produce it and nothing runs them. **Needs an edit to `tests/run_tests.sh`, which matts runs externally -- scope, not doubt.**
- **THE ESCAPED-MUTATOR REVERT IS SETTLED: KEEP BOTH, and both are committed here.** `AGENTS.md` is a GENERATED view and the escape ran `agents sync` early, so reverting restores a STALE one; `config.json`s `project_id` is LIVE in the store, driven with `.dump` plus both controls. **`MODULES.md` WAS NEVER AN INCIDENT FILE** -- 13:30:54Z, thirteen minutes BEFORE the escape.

## Watch-outs

**THE WHOLE OF 2026-08-26 WAS ONE CLASS, AND IT IS NOT A HABIT OF MINE -- IT IS THE DOMINANT DEFECT SHAPE IN THIS CODEBASE. MY EVIDENCE WAS TRUE AND MY SUBJECT WAS WRONG.** Eight instances in one day, and they are not eight mistakes; they are one mistake in eight materials.

- **In a TRIGGER** -- I matched cc's commits on AUTHOR and read it as identity. A trigger names a DEFECT, a range names a POPULATION, an author prefix names NEITHER.
- **In MY OWN INSTRUMENT** -- vc named a STRING and no file; my census supplied a file from context and reported **MISSING** against my own invention. A false MISSING reads exactly as confidently as a true one.
- **In a STATUS** -- the tree was dirty as before and arm 1 refused as before, with different files and a different node. **The refusal names the paths and never the owner.**
- **In a BORROWED PREMISE** -- I measured the PAIR, never the keg, and still wrote a CONTRAST. A measurement handed to you is evidence; **the cause attached to it is a hypothesis wearing the measurement's authority.**
- **In the SHELL** -- `"$I" $v` under zsh passes ONE argument; the binary answered `unrecognized subcommand` at rc=1 and I read a PRODUCT BEHAVIOUR off it. Not a null population -- a **non-null WRONG** one, which prints something plausible instead of something empty.
- **In an AGREEMENT** -- `2b's pair` meant CONTENT when the sweep was promised to it, and means TIME now. Every downstream commitment changed subject in silence.
- **IN THE PRODUCT'S OWN DOCUMENTATION** -- `facade.rs:111`: _this is the one moment a project acquires one_. True of the v2-to-v3 population, false of fresh `init`, stated generally -- and it is what stops the next reader noticing the gap.
- **IN THE PRODUCT'S OWN CODE** -- `emit_source_commit` asks IDENTITY unscoped and DIRT scoped and packs both into one string, so the stamp means _the repo's HEAD, annotated with whether the ARTEFACT was dirty_.

**THE LAST TWO ARE THE POINT.** Six of these live in my reasoning and are reachable by discipline. **The last two are the same shape compiled into the artefact, where no amount of my being careful reaches them** -- which is why both are 3.0.1 work rather than entries on this list.

- **"VERIFIED RATHER THAN ASSUMED" IS WHAT KEPT THIS DEFECT INVISIBLE FOR THE WHOLE OF v3.** `cmd/macos:116`'s comment says the copy list is the marker directory _"because everything the shipped consumers reach into is under it -- verified rather than assumed: the only path any shipped SCRIPT references outside its own tree is `$INTENT_HOME_RESOLVED`"_. **That verification was real, correct, and scoped to shipped SCRIPTS. The rules and skills are read by the RUST BINARY, which was never in the population.** A claim about the corpus with the instrument aimed at the wrong one -- and the words `verified rather than assumed` are precisely why it read as settled on every later pass, mine included. **A recorded verification must name its POPULATION, or the next reader inherits the confidence without the scope.**
- **`brew audit` TELLS YOU TO DELETE THE ONE LINE THAT DEFENDS A PRE-RELEASE CUT, AND TODAY'S VERSION IS THE ONE CASE WHERE IT LOOKS HARMLESS.** The tap audits with exactly one problem: `` `version 3.0.0` is redundant with version scanned from URL ``. It is redundant -- measured through `Version.detect` on the real asset URL. **But the scanner takes the tag path and DROPS every semver suffix**, measured across the shapes `staged_version` can emit, whose URL-safety check permits `-` and `+`: `3.0.0` -> `3.0.0`, but `3.0.0-dev` -> `3.0.0`, `3.1.0-rc.1` -> `3.1.0`, `3.0.0+build2` -> `3.0.0`. So taking the advice makes a formula that declares `3.0.0` while its URLs fetch `v3.0.0-dev` -- **a keg naming a version it does not contain, which is the provenance defect this whole lane exists to prevent** -- and it fails silently, on the release AFTER the one you tested it on. **KEEP THE EXPLICIT `version`. The warning is homebrew-core style advice and reaches no user of this tap.**
- **`git commit` COMMITS THE INDEX AS IT STANDS AT COMMIT TIME, AND FIVE SESSIONS SHARE ONE INDEX.** I swept vc's staged `verify-canonical.sh` into my own commit, under my own message. **I ran `git diff --cached --name-only` immediately before and it printed exactly one path** -- the index changed between the check and the commit, and no amount of re-checking closes a gap that is INSIDE `git commit`. **`git commit --only <paths>` is the only safe form here and always was.** `git show --stat HEAD` against your pathspec is the detector (cc).
- **`git reset` ON A SHARED HEAD IS THE SAME HAZARD WITH NO DETECTOR AT ALL.** My `reset --soft HEAD~1` would have orphaned a peer's commit had one landed in the window, with no symptom for either of us -- their work simply absent from `main`. I verified afterwards that none had. **Afterwards is the wrong end**, and I did it in that order while fixing this very class.
- **A CHECK WHOSE SUBJECT IS THE PRE-UPLOAD COPY CANNOT SEE WHAT THE NETWORK HOP DROPS.** Two checks that both EXECUTE the binary were blind to mode 644: `int macos verify` runs the STAGED file, which was +x and so could not fail; the formula's own `test do` asserts exactly this and **nothing runs it**, because `brew install` does not run `test`. The formula shipped carrying its own unrun refutation.
- **A CACHE-PRESEEDED INSTALL PROVES EVERYTHING EXCEPT THE THING IT SEEDS.** It fills the cache from LOCAL files already +x, so the mode survives and the install works. "Proves everything but the network hop" was an honest scoping and the exec bit is what stood behind it. Scope bound, measured: **tar preserves modes and the bare Mach-O download does not** -- 9 executables in the source tree, 9 in the keg.
- **HOMEBREW READS THE FILE, NOT GIT.** My tap clone carried an UNTRACKED `Formula/intent.rb` at `3.0.0-dev` while git HEAD was the empty-tap commit. Unnoticed, the install would have used the stale dev formula against stale cached artefacts and **reported success**. Hard-reset the tap onto origin before any install intended as evidence. Third instance today of a tool reading a FILE where a node reads git; `core.hooksPath` is another.
- **THE CUT ABORTED ON DIRT IT CREATED ITSELF.** `:690` runs `claude upgrade --apply`, whose canon engine appends a rule to `.gitignore`; `.gitignore` is not in `SIDECAR_FILES`, so the commit at `:716` cannot stage it; `:734` then reads the WHOLE tree and refuses to tag. `--skip-settings` narrows the blast radius to documents and does not cover `.gitignore`. **The file predicted this in its own comment at `:729-730`** and the flag written to prevent it did not reach far enough.
- **AN IDEMPOTENCE INSTRUMENT MUST EXCLUDE WHAT IT IS NOT MEASURING.** My whole-tree hash reported `intent3 upgrade` as NOT idempotent across two identical runs. Per-file isolation showed exactly one moving path: `intent/.cache/intent.db`, per-machine by D34 and gitignored. **The confident wrong answer was my instrument's, not the tool's** -- and it would have contradicted the tool's own "content is unchanged" line in public.
- **READ THE TOOL'S REMEDY LINE BEFORE REPORTING ITS NUMBER.** I drove the hooks as `SessionStart`/`UserPromptSubmit`/`Stop`, got three rc=1s, and had what looked like a failing acceptance arm. Wrong namespace: the binary's own error named the real four -- `session-context`, `require-in-session`, `post-tool-advisory`, `session-finish` -- all of which return 0.
- **A CREDIT LINE IS NOT AN AUTHORSHIP SIGNATURE.** I read `devbin-vc, 2026-08-26` in a file and attributed the file to devbin-vc. That file's convention credits the FINDER of each class; the work was vc's.

### FAMILY 5 -- THE GATE THAT DID NOT RUN, AND THE VERB THE NAME SENDS YOU TO

**ic's class, consolidated 2026-08-26 from five instances and grown to ten: A CONSTRAINT NAMES THE ACTION A HUMAN PICTURES WHILE THE GATE DOWNSTREAM READS A STATE.** Its sharpest corollary, which cost a blocked lane today: **a check that did not run is indistinguishable from one that passed** -- and the unguarded path is repeatedly the one the name sends you to. **hv's fix list lives here.**

- **`intentd --version` CARRIES NO SHA, SO THE STAMP CHECK I WAS ASKED TO RUN WOULD HAVE CHECKED NOTHING ON ONE OF THE TWO BINARIES.** It answers the daemon's `not yet implemented` sentence -- the same false surface I corrected in the tap README. "Both stamps off `--version`" yields **one stamp and a sentence**, and a scripted parser would have compared prose to a commit and reported whatever that comparison does. **Read the embedded marker instead** (`artefact_source_commit` in `artefact.lib`, the source `verify_pair` itself uses), and show the `--version` line beside it so the substitution is visible rather than trusted. **TRACED TO SOURCE 2026-08-26 WHILE HOLDING, AND THE ENTRY ABOVE UNDERSTATES WHAT IS SAFE AND MISSES WHAT IS NOT.** `build-support/source_commit.rs:190` computes ONE `value` and emits BOTH `INTENT_SOURCE_COMMIT` (what `intent --version` prints) and `INTENT_SOURCE_COMMIT_MARKER` (what `artefact_source_commit` greps), from one home `include!`d by both crates' `build.rs`. **They cannot diverge within a crate, so mixing the two instruments across the pair is safe FOR PROVENANCE** -- vc's 2a/2b protocol does exactly that and is correct. **WHAT IT IS NOT SAFE FOR IS VIABILITY, AND THAT IS THE HALF WITH NO ENTRY ANYWHERE.** `--version` proves the binary RUNS and reports the value through its own code path; the marker is a `strings` grep that proves only that the BYTES carry it. So the stamp lift EXECUTES `intent` and merely GREPS `intentd`, and **a correctly-stamped non-viable daemon passes it**. Viability is proven today only at `int macos prepare` (`quarantined, accepted, runs`) -- after stage, sign and notarize, which is a late place to learn the daemon does not start. **The close is free and driven: `intentd --version` EXITS 0.** It adds nothing to provenance and proves execution, so three reads make the pair symmetric in viability while staying honestly asymmetric in provenance. **AND THE STAMP IS NOT ALWAYS A SHA: the value carries dirt INSIDE it and reads the WORKTREE** -- `git status --porcelain -- DIRT_SCOPE` yields `dirty-<sha>`, and `unknown` when git answers `rev-parse` but refuses `status` (their reason, and it is this board's rule in someone else's file: _an unprovable claim is worth less than none_). **Report the stamp VERBATIM, never normalised and never trimmed to the sha** -- against a dirty tree the stamp is the TELL rather than a confirmation, and trimming it deletes the only evidence the lift was asked for.
- **ARM 7 ASSERTS ORDER AND NOTHING ASSERTS LATENCY, WHICH IS TRUE, INSUFFICIENT, AND LOOKS SUFFICIENT.** It proves the dirt verdict is taken before the first cargo invocation. **cargo does not FAIL on a locked target, it WAITS** (`waiting for file lock on ` is in this cargo's own strings, driven), so the gap between verdict and actual compile is unbounded and the tree can move inside it. A process-list read NARROWS that window and cannot close it -- **a process list is a statement about the past the instant it returns.** What closes it structurally is the queued staging-dir build: build private, verify, move into place, no lock and no process list in the argument. **So the process read is evidence FOR that item, not a rival to it.**
- **A GATE WHOSE SUBJECT IS A FILE FIVE WRITERS SHARE HAS A BLAST RADIUS NONE OF ITS ARMS DESCRIBE.** `shared_artefact_build_guard.sh` reads `cmd/local` from the **WORKTREE**, so my in-flight refactor of that file refused EVERY node's commits -- vc's doc commit twice -- while my work was still uncommitted. **Nothing in arms 1-13 says "this arm will block your peers while somebody edits its subject".** The way out is not `--no-verify` (vc: _I am not bypassing the gate for a doc commit; nobody should_): it is to land the change in a shape where the arms are true, **moving the arms in the SAME commit as the code they follow.** Which is also the general rule this cost me: **a guard's subject is part of the guard, so a refactor of the subject is a refactor of the guard.**
- **SYNC CANON FIRST -- IT READS THE WORKTREE -- THEN COMMIT THE FILE AND ITS CANON TOGETHER.** `canon_commit_check.sh` refused my commit because `shared_artefact_build_guard.sh` is a REGISTERED ATTACHMENT and canon named its pre-edit bytes. **The obvious order does not work and the tool says so in its own refusal**: committing first and re-syncing after leaves THAT commit divergent in history **permanently**, because the later sync fixes the next commit and can never fix this one -- the criterion is a property of every commit, not of HEAD. `intent sync --to-store ST0056`, then one commit carrying both. Same class cc hit at `e696de15` with the parity corpus.
- **`grep -n` ON A SINGLE FILE EMITS `<line>:<text>` WITH NO FILENAME, SO A `cut -d: -f3-` COPIED FROM A MULTI-FILE GREP SILENTLY YIELDS NOTHING.** Arm 10's first draft reported **`(1 examined)` against a tree carrying five** and PASSED. **A census that examines one of five and reports green is the false green the guard exists to refuse**, so the count is printed and is the first thing to read. Third instance today of a wrong COUNT reading as authoritative -- `int macos smoke` said "fails 6" while listing 3, and ic's population question was the same shape.

- **`int cli` PREFERS `target/debug/intent` OVER `target/release/intent` ON EXISTENCE ALONE, SO A STALE DEBUG BINARY IS AUTHORITATIVE FOREVER.** `cmd/cli:30` loops `debug` then `release` and `exec`s the first that is `-x` -- no mtime, no freshness. **Build debug once, from any tree state, and every later `int cli` runs it** while `int local build` faithfully refreshes a release pair nobody reaches through that verb. `cmd/cli:41`'s not-yet-built fallback is a FIFTH bare `cargo build -p intent-cli` (single-package, shared target, no verdict), so the unguarded census is `all`, `cli`, `daemon`, the `cmd/cli` fallback, and every `--debug` path. **AND THE CLAIM THIS CORRECTS WAS MINE: I told vc "nothing downstream reads a debug binary" and vc carried it into the view going to hv.** `cmd/cli` and `cmd/dogfood:49,66` both EXECUTE it. **I grepped where `--debug` bytes LAND and not who READS them -- the wrong-population move, made four hours after I wrote the watch-out about it, against my own sentence that a recorded verification must name its population.** The exception survives on a narrower reason that actually holds: nothing reads a debug binary's **STAMP**, and a debug build exists precisely to run uncommitted code, so a dirt verdict there would break the one workflow it serves. Separate and cheap: **`int cli`'s preference should be freshest-wins or release-wins.** **AND THE HONEST ACCOUNT OF HOW THIS WAS CAUGHT, because vc offered a kinder one and the kinder one would mislead the next reader.** vc's words: _the second time today you caught your own wrong-population move within the hour, which is the watch-out doing what it is for._ **The watch-out did NOT catch it.** I never consulted it -- I made the identical error hours after writing it, and caught it only because I decided to harden a claim that was about to reach hv inside somebody else's design. **A watch-out that its own author does not consult is not a working control; the control here was a peer carrying my sentence somewhere it would be ruled on.** What that argues for is a MECHANISM -- something that asks "what population did you measure?" at the moment of the claim -- not a better-written caution. Two instances in one day, same author, one of them against a caution that author had just committed.
- **`int build all` IS A BARE `cargo build` -- NO PROVENANCE FORCING, NO SET VERIFICATION -- AND IT IS THE VERB THE NAME SENDS YOU TO.** `cmd/build.d/all` is, after its header, `exec cargo build --workspace --bins $profile "${args[@]}"`. **Both guards live in `int local build`**, which opens `forcing the provenance embeds (cargo will not do this for you)` and cleans 49 files, then prints `verifying the SET, which is the check cargo cannot make`. **I used `all` for the batch rebuild, it split the pair, and it blocked every hop 2 in vc's lane** -- `intent` at `858d4264`, `intentd` still at the pre-flip `9b6ab634`. **THE SOURCE COMMIT IS EMBEDDED AT COMPILE TIME AND IS NOT AN INPUT CARGO TRACKS**, so a crate whose source did not change keeps its old sha wherever HEAD is: cc's and ic's fixes were in `intent-cli` and `intentsvcs`, cargo rebuilt those two and left `intentd` untouched. **vc asked why my verb's set-verification PASSED a mismatched pair. It did not pass it -- it never ran**, and `all`'s output ends at `Finished \`release\` profile`, which reads as done. Fourth instance today of a check that did not run being indistinguishable from one that passed, and the first where I was the reader who could not tell -- **I had watched `local build`print`verifying the SET`earlier the same day and carried that memory onto a different verb.** Its help says`all`means all BINARIES, so "rebuild everything" points at the unguarded path while the guarded one is named after another concern. **ALWAYS`int local build`FOR A REBUILD THAT ANYONE WILL READ A STAMP OFF.** **AND THE GAP IS THREE GUARDS ACROSS THREE VERBS, MEASURED READ-ONLY AFTER vc ASKED THE RIGHT DISCRIMINATOR** (not run: driving `int build cli`would re-split the pair and re-block vc).`all`, `cli`and`daemon`ALL write the SHARED`native/rust/target`and **none of the three takes`shared_target_verdict`; only `local build`does.** So the 12-arm shared-artefact guard, whose arm 7 asserts _the verdict is taken before the first cargo invocation inside cmd_build_, is true -- and`cmd_build`is`local`'s, so **it protects one entrance of four.** A `build all`from a dirty tree lands unattributable bytes in the shared release dir and nothing refuses it; I checked`git status -- native/rust surface schema` by hand first and it was clean, **the verb did not.** **`cli`and`daemon` split the pair BY CONSTRUCTION** (`cargo build -p intent-cli`/`-p intentd`), so a fix landing only in `all`leaves two verbs that can do nothing else. **vc's discriminator answered:`all`DOES serve a use`local build`cannot** --`--debug`plus pass-through cargo opts, where`local build`hard-codes`--release`in both its clean and its build. **And that use is the one where the guards matter least**:`--debug`writes`target/debug`, nothing reads a stamp off it, the shipped artefact is untouched. So `all`gains all three guards for RELEASE builds with`--debug` a stated exception, and no second home is needed. Routed to hv, after vc's three hops; not mine to build.
- **A `bin/`-ONLY PUSH IS LINTED BY NOTHING IN CI, AND `build.d/release` IS THE FILE THIS IS ABOUT.** Three gates, all cleared by a shell-only change: `cmd/prepush:258` skips the clone check unless the push touches `^(native/|Cargo\.(toml|lock)$|rust-toolchain)`, so no clippy and no fmt; `rust.yml` uses a `paths:` allow-list of `native/rust/** schema/** surface/**`, and **the critic-based shellcheck that DOES fail closed lives inside `cargo test --workspace`, inside that job** -- teeth present, workflow not invoked; `tests.yml` triggers and runs a shellcheck job that cannot fail (`shellcheck "$script" || echo "  ShellCheck found issues (non-blocking)"`, and absent from `test-summary`'s `needs`) over a subject that has moved (`find bin -type f -name "intent*"` -- **measured 27 matched, all of them v2 `bin/intent*`; 43 missed, including `cmd/macos`, `cmd/local`, `cmd/prepush` and `build.d/release` itself**). Both of today's shell fixes (`ded669c2`, `94d03e9f`) were exactly this shape. **Only the local pre-commit critic saw them, and that is `core.hooksPath`, so a fresh clone has none of it.** `tests.yml` already carries the correct analysis in a comment -- `paths:` "fails SILENT ... the false-green class this project keeps closing" -- and `rust.yml` next to it uses `paths:`. **AND THE MITIGATION THE BOOT FILE OFFERS FOR THIS IS FALSE.** `.claude/restart.md:9`, in the same paragraph as the `core.hooksPath` warning: _"unwired hooks mean unformatted code, and `cargo fmt --check` runs on every push"_. It does not -- it is `rust.yml:67`, inside the single `rust:` job, behind that same allow-list. **A freshly-cloned node loses `intent critic <lang> --staged` (`pre-commit.sh:368`) for every DECLARED language including `shell`, edits `bin/.devbin/**` because that is where the tooling lives, and the stated backstop is a Rust formatter behind a filter that excludes `bin/**` entirely.** Every node boots through that file; none of us checked it.
- **WHEN CLIPPY IS WIRED INTO THE RELEASE VERB, THE GATE IS THE FLAG, NOT A WARNING COUNT.** The verb runs zero clippy today (ic, confirmed). Positive-controlled on a scratch crate with one deliberate lint: bare `cargo clippy` prints the warning and **EXIT=0**, `cargo clippy -- -D warnings` **EXIT=101**. So `if ! cargo clippy` is inert and `if ! cargo clippy --workspace --all-targets -- -D warnings` is not. **That exact spelling already has two correct homes -- `cmd/prepush:335` and `rust.yml:69`, character-identical -- so a count-based gate would be a THIRD home for one predicate**, which is the drift the consolidation exists to prevent. Copy the flags; do not invent a technique.
- **THE RELEASE VERB NEVER TESTS THE TREE IT TAGS.** `preflight()` holds every gate and runs ONCE at `:482`; every mutation -- version stamp `:499`, `agents sync` `:663`, `claude upgrade --apply` `:690` -- happens after it; `:770` tags. **Any test whose subject is a value the cut writes is structurally untestable by the cut's own gate.** cc's five schema faces were one instance, not the class. Composed with "no workflow fires on a tag", CI on the `main` push is the ONLY gate in the estate that tests the tagged tree -- which makes main-first-then-tag load-bearing rather than prudent.

### FAMILY 1 -- THE INSTRUMENT THAT CANNOT VARY WITH ITS SUBJECT

- **ON A BOX RUNNING LLM SESSIONS, THE AGENTS' OWN INSTRUCTIONS ARE PART OF THE MACHINE'S OBSERVABLE STATE.** vc's `quiet.sh` gated my rebuild on `pgrep -f 'cargo (build|test|clippy|fmt|...)'` and reported `cargo processes: 85`. The matching pids were three `claude` sessions, two `zsh` wrappers and ONE real cargo -- **the sessions match on the phrase `cargo fmt` inside their own `--append-system-prompt`**, from the restart text every node boots with. Those processes never exit, so **the gate was pinned NOT quiet for an hour and could never have emitted the QUIET my build waits on.** Any `pgrep -f`, `ps | grep` or log scrape over this box has it: **a document warning about a command is indistinguishable, to a full-command-line matcher, from the command.** Fix is to match the EXECUTABLE (`pgrep -lx cargo`) and never the argv.
- **BOTH OF US POSITIVE-CONTROLLED IT AND BOTH OF US CONTROLLED THE SAME HALF.** vc added a process with argv rewritten to a fake `cargo clean`, saw NOT quiet, read again after it exited -- both reads NOT quiet, the first for vc's reason and the second for the defect. I proved the alternation SYNTAX fires with a `sleep 4`, which says nothing about what else the pattern sweeps in. **A control that ADDS a matcher to an instrument already matching everything exercises only the arm that FIRES.** The estate's red-control rule needs the sharper form: **drive the verdict the CALLER ACTS ON.** Here the caller acts on QUIET, and QUIET was the arm neither of us ran.
- **`grep -c .` OVER `pgrep -fl` COUNTS LINES, AND A `claude` argv IS HUNDREDS OF LINES OF INJECTED PROMPT.** Six pids reported as 85, drifting to 57 between two reads because the prompt text differs. **Fifth wrong count reading as authoritative in one day** -- after `smoke` saying fails 6 while listing 3, arm 10's 1-examined-of-5, and ic's population question.
- **A SET-EQUALITY CHECK CANNOT SEE A DEPTH ERROR, AND MINE SHIPPED A PERFECT FILE SET AT THE WRONG DEPTH.** `support_tree_drift` compares the archive's file list against the source tree's and passed. **Homebrew's `resource.stage` strips a single top-level directory**, so `lib/templates/...` staged as `templates/` and the marker landed at `libexec/templates` -- every file correct, nothing reachable, `INTENT_HOME: <not set>`, every hook rc=1. **Mechanism and remedy live in `bin/.devbin/cmd/macos` at `SUPPORT_PREFIX`; do not restate them here.** The general form is the row: **two checks that both ask "which files" leave "where" unasked, and agreement between them is not corroboration.**
- **THE HEADER OF THE FILE I WAS EDITING NAMED THE FAILURE, AND THE TARBALL WAS ITS REMEDY.** _"A formula that installs only the binaries produces an install that cannot find itself."_ I added the support tarball to close exactly that, and the remedy reproduced the fault by another road. **A correct diagnosis plus a remedy nobody drove end to end is still a broken install -- and this one LOOKS INSTALLED**, which is what makes it worse than a failure.
- **A PRESENCE CHECK STANDING IN FOR A CONTENT REQUIREMENT PASSES ON ANYTHING WITH THE RIGHT NAME.** vc's `verify-canonical.sh:83` is `[ -f "$p/$f" ]` for `root canon: CLAUDE.md`. Anvil scored **0 failed** carrying a file that says _"This is an Intent v2.11.5 project"_ and points at the retired `.intent/` path. **Thirteen more projects would have banked that green.** Same shape as the marker `grep -q` vc had already fixed one arm over.
- **`grep -c` PRINTS `0` AND EXITS 1, SO `|| echo 0` FIRES TOO AND YOU GET `0\n0`.** On this board as ic's find; **it caught me today in my own user-block probe**, which produced `integer expression expected` and a plausible-looking hit on Cdsync before I noticed. **Knowing the name of a class does not confer immunity; only driving something that could come back the other way does.**
- **A FIXTURE WITH AN EMPTY SLOT CANNOT EXHIBIT A LOSS OF WHAT GOES IN THE SLOT.** I proved the keg's `--apply` against a scratch project whose `CLAUDE.md` user block was EMPTY, so my green was true and uninformative -- the pre-fix bug substitutes a well-formed default block, and _is there a user block_ scores it green. cc's `canon_preserves_*` fixture had the identical defect three hours earlier. **The arm is _is it THIS block_, byte-identical, against a fixture carrying real content.**
- **A PROMISE WRITTEN INTO THE ARTEFACT ITS READERS RELY ON.** `lib/templates/llm/_CLAUDE.md:48` ships _"Preserved across regeneration."_ into every project, and nothing implemented it -- cc measured Baize losing 20 bytes on the **plain** `--apply` path, no `--force` needed. **It cost nothing only because that one block happened to be empty.** Same class as `use prod`'s refusal message (`0347aab7`): true-by-intent, false-in-fact, sitting exactly where it will be believed.
- **THE BUILD TARGET AND THE BUILD STAMP DIFFER WHENEVER THE BOARD IS BUSY.** cc handed me `b319fa2a` to build; the pair stamped `88b1c92c` because HEAD moved twice mid-compile. **The stamp goes in sixteen commit bodies, so the wrong one circulates cheaply.** Read it back off `int local status`, say it out loud, and prove the target is an ancestor rather than assuming.
- **A DIRTY TREE REDIRECTS THE BUILD, IT DOES NOT BLOCK IT -- AND THE REDIRECT DOES NOT REACH `bin/intent3`.** `cmd_build` sends a dirty build to `target/private/release`; the wrapper reads `target/release`. **A build there would report success while the wrapper carried on refusing.** (Arm 8's redirect DOES land inside the install tree -- driven both ways for ic, positive and negative.)

- **A THING THAT NEVER TOUCHES A SURFACE CANNOT BREAK IT -- AND CANNOT DELIVER IT EITHER, WHICH IS THE HALF NOBODY COSTS.** My own standing directive said the hook surface DETONATES at the cut. It does not. `intent upgrade` never touches `.claude/` -- AT-10.4 asserts it over the PLANNED WRITE SET, not just bytes -- so a migrated project keeps whatever wiring it had. **9 of 16 wire absolute paths and never reach v3's door at all: they do not wake up broken, THEY QUIETLY FAIL TO ARRIVE, and NOTHING REPORTS IT BECAUSE EVERY HOOK KEEPS WORKING.** I predicted a loud simultaneous failure and the truth was a silent non-delivery. **A guarantee of no-harm and a guarantee of no-effect are the same assertion read from two directions, and only one of them gets celebrated.**
- **`rc=$?` AFTER A PIPE, THIRD INSTANCE ON THIS BOARD, AND THIS ONE WOULD HAVE INVERTED A VERDICT.** I read `head`'s status and reported `claude upgrade` as **rc=0** -- a silent no-op. It is **rc=2**, a refusal. **`0` and `2` there are opposite claims about whether the estate can rely on the verb**, and the wrong one was the reassuring one. Caught by re-driving the bare command before it left my session. **The rule has been on this board twice and I still wrote the pipe.**
- **A WARNING CAN BE REASONED PAST WITH A TRUE SENTENCE, AND THE TRUTH OF THE SENTENCE IS WHAT MAKES IT STICK.** shellcheck flagged SC2318 on `describe_binding`; I dismissed it as _pre-existing and demonstrably working_ because `int local status` printed correct targets. **`status` DOES work. That was true and it was not the question.** The very next drive hit `f: unbound variable` under `set -euo pipefail`, and **the symptom was a CORRECT switch misreported as `v2`** -- an instrument lying about a subject that was fine. Latent since the function was written; my new caller was the first to reach it from inside a command substitution. **A dismissal supported by a true observation is the hardest kind to catch, because checking the observation confirms it.**
- **A FLAG THAT CANNOT BE SET IS THE SAME DEFECT AS A FLAG THAT IS NEVER READ, AND NEITHER STATIC TOOL SEES EITHER.** My `--yes` was declared AND read -- and the dispatch forwarded only `$1`, so `cmd_use`'s `$2` was never passed and the flag could never fire. **It failed CLOSED, so nothing was wrong at the moment anyone looked.** `bash -n` passed it, shellcheck passed it, only driving caught it. cc's mirror the same hour: a `--yes` DECLARED and never read (SC2034). **Two halves of one class, and the tools catch the half that is a lint and miss the half that is a wiring error.**
- **THE FILE YOU WERE HANDED IS NOT NECESSARILY THE ONE THAT RESOLVES.** vc relayed devbin-vc's finding about `Intent/bin/intent` being a v2 bash wrapper on PATH. True -- **and it is at position 22, shadowed by `~/.local/bin/intent` at 17, and NEVER RESOLVES.** They verified the artefact's IDENTITY (sha, `cmp`, canon scheduling its removal) and never asked WHETHER ANYTHING REACHES IT. **Three checks, all correct, all upstream of the question.** vc's own words on it. `which -a` plus PATH POSITION is the probe; `file` and `sha` answer a different question in the right shape.
- **A 0-OF-0 CAN LIVE INSIDE THE SELF-TEST BUILT TO PREVENT 0-OF-0.** vc's verifier ships a `--self-test` against a known-bad fixture -- exactly the right idea. **Two of its arms PASSED, because the fixture had zero hooks and a check for _no absolute checkout paths_ cannot fail on a settings.json with nothing in it.** The arm the fleet most depends on was the one with no positive control. I closed one by pointing it at Anvil (fires, 2 paths); **vc closed the other by pointing it at Cdsync and Laksa -- projects they had measured hours earlier and had not thought to aim the check at.** Both arms now demonstrated live. **A control's fixture must exhibit EVERY failure the control claims to detect, and "the fixture is bad" is not the same as "the fixture is bad in all the required ways".**
- **ABSENCE READS AS MEMBERSHIP IN ONE DIRECTION AND AS NON-MEMBERSHIP IN THE OTHER, AND WE MADE BOTH ERRORS IN ONE DAY ON ONE LIST.** `Intentv2` carries a project config, so **every census finds it and it must NEVER be migrated** -- it is the tool other projects run. `Riffle` has no `.claude/settings.json`, so **I dropped it from the fleet and said 15** -- but it IS a project at 2.18.0 and the absence is THE STATE THE REWRITE FIXES. **Same property, opposite errors: the false member is the one that answers, the false non-member is the one with nothing there to notice.** The tool caught the first (`int local status` prints the warning unprompted); an instrument caught the second (vc's verifier forced an enumeration). **Neither was caught by reading.**
- **`find` WALKS INTO `target/` AND DIES AT THE TIMEOUT, RETURNING A PARTIAL ANSWER THAT READS AS A COMPLETE ONE.** My packaging search timed out at 2 minutes having printed four empty sections -- **which reads exactly like _no packaging exists_.** `git ls-files` answered in a second AND is a claim about the commit rather than the working tree. **In this repo, reach for the index first.**
- **A BACKTICK INSIDE A QUOTED GREP PATTERN IS A COMMAND SUBSTITUTION.** `grep -n 'install|prefix|`local`'` ran `local` as a command and dumped ~200 lines of shell environment into my transcript. Harmless here; **it would have been a credential leak in a different shell state**, and the answer I wanted was in the noise where I nearly missed it.

**THE HEAD OF THIS FAMILY, ARRIVED AT ON 2026-08-25 AFTER SIX INSTANCES IN ONE EVENING, AND IT IS WORTH MORE THAN ANY OF THE SIX: EVERY ONE WAS AN INSTRUMENT'S OUTPUT READ AS THE SUBJECT'S ANSWER.** Mine were five confident `rc=1`s that were my own currency guard answering in `critic`'s voice, and a `RESTORED` printed by an `echo` that followed a failed `cp`. vc's was `BYTES IDENTICAL: yes` read without reading what it was identical ABOUT. **The pairing is the useful part: vc trusted a COMPARISON without checking its subject, I trusted a STATUS without checking its author.** Everything below is a special case. **Before believing any tool's verdict, name which process produced it and what question it was asked.**

- **AN ASSERTION THAT FORBIDS ITS OWN FIX, WITH THE CORRECT RULE WRITTEN SIX LINES ABOVE IT IN PROSE.** My AT-11.6 arm 6 tested `guard scope == marker DIRT_SCOPE` by scalar equality. The correct relation is CONTAINMENT -- **stricter than the marker is safe; looser is the direction where the shared slot holds an artefact the guard approved and the artefact disowns** -- and my own header said exactly that. **So when a live incident forced the scope to widen, the arm redded on the fix.** ic: _the comment knew and the code did not._ **The arm was RIGHT when written and was made wrong by a widening it could not see: a guard whose subject grew while its check did not.**
- **A CORRECT FIX AND NO FIX PRODUCE IDENTICAL OUTPUT WHEN THE READER IS EMBEDDED** (cc, via vc). cc fixed a malformed dispatch row, got the SAME panic because the binary had the old table baked in, concluded the fix was wrong and reverted it -- **then the revert destroyed the evidence.** This is worse than a bad artefact shipping: **the diagnostic loop lies in the direction that makes a correct engineer undo correct work.** A second, independent argument for scoping a build guard to the build's INPUTS rather than its source tree.

- **A GUARD WHOSE TWO POPULATIONS COME FROM DIFFERENT TREES IS NOT NARROW, IT IS ABOUT NOTHING.** `runner_roster_check.sh` reads PRESENT (the parity files) from **the COMMIT** and ROSTERED (the roster row) from **the WORKTREE**, so the two sides of one comparison describe two states of the repository. **It can be wrong in BOTH directions and is internally consistent in each.** The 2026-08-21 fix moved PRESENT off the worktree to end an estate-wide freeze caused by an untracked mid-work `*_check.sh` -- and left ROSTERED where it was, **so the exact freeze it was written to end is still reachable, needing only the ROW to be the mid-work half instead of the FILE.** I hit it, vc hit it, ic hit it, and none of us could have avoided it by being careful. **A fix that corrects one side of a two-sided comparison closes the instance and leaves the class, and nothing about the green afterwards says which of the two happened.** Fix is symmetry -- read the row from the commit too. **NOT MINE TO BUILD AT SPEED: it gates every commit and I caused two estate-wide blocks the day I found it.** Queued for hv beside the roster-charter question, which is the same conversation.
- **READ THE EXIT CODE, NEVER THE MESSAGE. A LOUD ADVISORY AND A REFUSAL ARE IDENTICAL ON STDERR AND `rc` IS ONE CHARACTER.** `stale_at_check.sh` prints a full finding and returns **0** -- its roster row says `reports presence only`. ic reported it as blocking every node in the repo; it blocks nobody. **The cost was not the wrong belief, it was that I nearly moved a contract row on it.** The mirror of my own `rc=$?`-after-a-pipeline error twice in one session: in zsh that reads `tail`'s status, not the command's.
- **AN INSTRUMENT THAT GREPS A FILE FOR AN INVOCATION MATCHES THE FILE'S PROSE ABOUT THAT INVOCATION.** My AT-11.6 arm 7 failed against CORRECT code by matching the header sentence _WHY `build` IS NOT `cargo build --release`_. **The opposite direction is worse and is the one to design against: a comment MENTIONING the thing, sitting above the real call, makes the arm PASS while the call site is absent or late.** Scope to the function body and strip comments.

**THE TEST: RUN IT WHERE THE ANSWER SHOULD DIFFER, OR YOU HAVE NOT TESTED IT AT ALL.** Not _check your instrument_, which is unactionable. The instances below are evidence FOR the rule, not rules beside it.

- **`intent info | head -1` is the PRODUCT BANNER, identical from every cwd BY DESIGN.** I nearly withdrew a CORRECT fix on it. **Pick a line that CAN vary, or diff the whole output and pick none.**
- **`--help` IS NOT A PROBE.** Under INV-07 it exits 0 whether or not a command is built.
- **A GREP'S ZERO IS A CLAIM ABOUT THE CORPUS, AND SO IS A CASE-SENSITIVE MISS.** Positive-control before believing it. Twice: a db grepped for `0001` returned 1609 hits; a restart.md probe returned 0 because I matched lowercase against an uppercased rule.
- **A CHECK CAN INTRODUCE A PREMISE NOBODY MADE AND THEN FAIL IT, AND A FALSE `MISSING` READS EXACTLY AS CONFIDENTLY AS A TRUE ONE.** At 2a vc named the STRING `AC-04.2: canon read-only -- the un-gated half` and named **no file**. My defect census supplied one from context, aimed the grep at `legacy_satisfied_verdict.rs`, and reported **MISSING**. It is `legacy_covers_token_rule.rs:83`, with the negative half at `:87`, exactly as described. **The file was MY invention and the census failed my invention, not vc's claim** -- and had I not chased it I would have reported a peer's increment incomplete on the strength of a premise they never stated. **Grep the string the claim actually made; let the TREE say where it lives.** Fourth dress of _my evidence was true and my subject was wrong_ in one day, and the first one INSIDE the instrument built to catch the other three. Same census: **`grep -r` across `native/rust` walks `target/` and timed out at 2 minutes, silently truncating two probes to no result at all** -- scope to `native/rust/crates`.
- **A NO-OP CANNOT FAIL, SO IT MEASURES NOTHING.** My v2 arm read `5 -> 5` because upgrade SHORT-CIRCUITED. **Force the real path before believing a clean result.**
- **`find`-BASED POPULATIONS DESCRIBE THE WORKING TREE, NEVER THE COMMIT** (ic). `run_tests.sh:89`. A missing test and a passing test are the same observation.
- **A COUNT OVER A `dirty-` MARKER IS A FLOOR, NOT A DISTANCE** (ic). The uncommitted delta at build time lies outside the range in either direction.
- **A RANGE WITH NO PIN NAMES A DISTANCE FROM A MOVING POINT; A PIN WITH NO RANGE NAMES BYTES WITH NO CONSEQUENCE** (ic, theirs whole).
- **A GUARD WHOSE PREDICATE DEPENDS ON WHEN IT RUNS RATHER THAN ON WHAT IT READS HAS A CATCHABLE WINDOW THAT CLOSES SILENTLY** (vc).
- **A MEASUREMENT TAKEN BEFORE YOUR OWN WRITE IS STALE BY CONSTRUCTION.** It cost the attribution on the 2026-08-24 incident.
- **TWO READINGS OF ONE SOURCE IS ONE INSTRUMENT COUNTED TWICE** (cc). I could not reproduce cc's table count and declined to CONTRADICT it, because my own probe had returned `if` as a table name. **That refusal is what sent them to `sqlite_master` rather than to a better regex, and a better regex would have agreed with them and taught us nothing.**
- **THE ` M` RULE LIVES AT `intent/restart.md:69`, ONE HOME, in my name.** What is NOT there and belongs to cc: they binned the same zero-byte file into `peers` twice while reporting scope, inferring AUTHORSHIP from a marker making no claim about content -- **action right, reason wrong, so nothing broke and nothing would ever have surfaced it. A CHARACTERISATION WHOSE ACTION IS CORRECT IS INVISIBLE BY CONSTRUCTION.**
- **THE HARNESS RAN THE SUBJECT UNDER DIFFERENT SHELL OPTIONS THAN ITS ONLY PRODUCTION CALLER**, so fifteen green arms sat over a function returning rc=1 on its healthiest answer. bats sets neither `errexit` nor `pipefail`; `bin/intent3` sets both. **AND A COMMAND SUBSTITUTION DISARMS errexit IN THE SUBSHELL** -- `$-` is `ehuBc` at top level and `huBc` inside `$( )` -- so the one caller survived BY ACCIDENT while the library promised it could not kill a host. **Drive the options production sets, not the ones the harness happens to have.**
- **NEVER `$?` AFTER A PIPE. `cargo test` needs `--no-fail-fast`. `grep -c` exits 1 on zero. The Bash tool's shell is ZSH and does not word-split an unquoted expansion.**
- **`bash -n` IS WRONG IN BOTH DIRECTIONS AND NEITHER IS VISIBLE FROM ITS OUTPUT** (half vc's). It **OVERSTATES BY POSITION** -- bash parses incrementally, so an error the run never reaches is never raised; `-n` reads the whole file, a run reads as far as it gets. It **UNDERSTATES BY CONSTRUCT** -- blind to every version defect that is valid syntax, which is where bash 3.2's gaps all live. **Which way it cuts must be DRIVEN: I built a control at the wrong end of a file, no arm fired, and I briefly held a refutation of a true finding.**
- **A VERSION DEFECT CAN RETURN THE CORRECT ANSWER, EXIT 0, AND PUT THE ERROR ONLY ON STDERR.** Under 3.2 `declare -A` degenerates the subscript to index 0 -- which is what the assignment set -- so the value is right, the mechanism is wrong and rc is 0. **NO RESULT-CHECKING FINDS THAT.** Only stderr or `set -e` separates it, and **9 of the 12 rostered guards do not execute `set -e`**, so it is invisible inside three quarters of the commit gate.
- **A NULL POPULATION WEARS THE SHAPE OF A FINISHED MEASUREMENT.** `for g in $GUARDS` under zsh does not word-split; the loop ran once, matched nothing, and printed `0 / 0`, which reads as _no guard executes `set -e`_ -- **bigger and more alarming than the truth. Caught ONLY because the wrong answer pointed the same way as the right one.** A correct loop over a wrong population prints the identical `0/0` and reads as ALL CLEAR. **Legibility was luck.** Its loud twin (vc): 101 failures of 295, caught in one second, same defect, opposite legibility. **AND ITS WORST FACE ARRIVED 2026-08-26 WHILE I MEASURED THE 3.0.0 READ-VERB DEFECT: THE SHELL MANUFACTURED A PRODUCT CLAIM I THEN BELIEVED TWICE.** `"$I" $v` with `v="st show ST0001"` passes ONE argument under zsh, so the binary answered `error: unrecognized subcommand` at **rc=1** -- and I read that rc as _the new pair REFUSES a read against a divergent tree_, which would have been a headline feature. **The verb never ran.** **This is not a null population: it is a NON-NULL WRONG ONE, and it does not print `0/0` or anything else that looks empty -- it prints a plausible product behaviour.** I reported it to myself as a finding, then 'corrected' it with a second wrong theory (a `git clean -fd` having eaten the SQLite WAL), then found the empty table was only `st list`'s default in-progress filter. **Three consecutive confident readings, none of them the product.** The one that held was the block whose args went through `"$@"`. **Pass verb words as separate arguments, never as one interpolated string, and prove the verb RAN before reading its rc** -- here, `st list` seeing 1 row is what separated a real run from a parse error.
- **RUN THE NEGATIVE CONTROL, NOT ONLY THE POSITIVE ONE.** My bash-4 detector was checked against `declare -a` as well as `-A`. A pattern flagging both would report hits across the tree and **read exactly like a finding**, in a sweep whose whole job is telling those apart.
- **A CONTROL IS ONLY A CONTROL IF ITS GROUND TRUTH IS KNOWN RATHER THAN RECALLED** (vc, theirs whole). **A remembered ground truth is a second guess wearing a control's costume**, and nothing tells them apart by looking.
- **DECLARE THE EXPECTED DENOMINATOR BEFORE MEASURING, THEN CHECK THE ACTUAL AGAINST IT** (vc). _12 expected_ refuses a `0` on sight. **It reaches four of six failure kinds and NOT the other two: not precision (comment matches inflating a count), not a wrong axis.** A control that names what it cannot reach.
- **A GREP CANNOT TELL A STATEMENT FROM A SENTENCE ABOUT A STATEMENT** (vc's `0076`). It landed inside my own verification: I probed the fixed file for the apostrophe, got a hit, and nearly re-opened a closed defect -- **the hit was in an ordinary comment, outside both heredocs. THE PARSE IS THE CONTRACT, NOT THE GREP.**

- **EXPECT THE RED, NAME IT IN ADVANCE, AND TREAT AN UNEXPECTED GREEN AS A FINDING ABOUT THE INSTRUMENT** (vc, theirs whole, and it is the RULE where I had only the episode). **It is the only detector that worked on this class all day, four times.**
- **FOUR INSTANCES IN ONE DAY, ALL MINE, EVERY ONE RETURNING A CLEAN PLAUSIBLE RESULT.** (i) A ~19KB e2e fixture that fit inside the pipe buffer, so the race never armed and OLD agreed with NEW on every arm -- **the agreement was the only tell.** (ii) A run against the WORKING TREE reported as a fact about the COMMIT, in a four-writer tree where a peer was mid-fix in that very file. (iii) A mute control that appended to a file that did not exist, so `>>` CREATED it on the v2 side -- **a fixture added to the wrong side of a ONE-DIRECTIONAL comparison is invisible to it.** (iv) A positive control grepping `tests/` for a filename the file does not contain: **badly chosen rather than broken, and only the population grep finding hits elsewhere separated the two.**
- **TWO WORKFLOWS CAN SHARE ONE SHA, AND THE FIRST SUCCESS IS NOT THE ANSWER.** I told hv _CI is green_ on a run at `fab98c96`. That was the **rust** workflow; **Intent Tests at the SAME COMMIT had FAILED.** The SHA matched, the subject did not. **A run is identified by (sha, workflow), and a query that returns the first row matching half of that key answers a different question in the right shape.**
- **A WINDOW BOUNDARY REPORTED AS AN ORIGIN** (ic's formulation; vc and I both hit it). `--grep` over _the last 200 commits_ returns an oldest hit that is the oldest one still INSIDE THE WINDOW, not the first one that ever existed. **Ask what a window's FLOOR is before reading its earliest hit as a beginning.** vc's twin: **ask what REFS a scan spans** -- `git log --all` crossed into `v2-maintenance` and reported a v2 commit as main's origin.
- **A NEGATIVE ASSERTION NEEDS A FIXTURE IT ACTUALLY REFUSES, OR IT IS NOT AN INSTRUMENT.** A guard asserting a string is ABSENT passes trivially on every clean commit forever and never establishes it can see anything -- **0 of 0 wearing the shape of a green.** Mine, adopted into the attribution-guard brief as a requirement; it is the same defect as the 19KB fixture and the `0/0` loop, arriving BEFORE the build for once.
- **`2>/dev/null` CONVERTS A BROKEN PROBE INTO A CLEAN ANSWER, AND IT IS THE FIFTH INSTANCE OF FAMILY 1 IN ONE DAY.** My push check printed `commits ahead of upstream: 0` and I read it as _nothing to push_; the redirect was eating the error and the count was of nothing. **The un-swallowed run said `ahead: 1` and the commits were already in.** The four instances above are all _the instrument could not reach its subject_; **this one is the instrument reporting that it could not, into a hole.** Swallow stderr only where you have already decided that a failure and an empty result mean the same thing.
- **A CORRECT FINDING CAN CARRY A WRONG CITATION, AND THE CITATION IS THE HALF THAT GETS REUSED.** vc was right that a fourth suite caller existed and named `cmd/shared/measure.lib`, which contains no such call; the real one is `bin/.devbin/config.yaml:91`. **The finding survives; the pointer would have been copied forward.**
- **AN UNDER-STATED RESIDUAL IN A RATIONALE IS HONOURED RATHER THAN CHECKED** (vc, correcting me). I wrote _every node here has it_ about a guard **nothing runs on a commit**, in the sentence congratulating myself for stating the cost. **HAVING A ROUTE IS NOT RUNNING IT.**
- **A RETIRED PREMISE KEEPS ISSUING ORDERS.** The drift guard's proxy remedy said _push the branch_, correct while the push was owed. hv retired the owing; left alone it would have gone permanently red on every node while instructing each of them to do the thing hv had just declined.
- **THE FIX FOR A KNOWN TRAP CAN BE INERT UNDER THIS SHELL, AND IT FAILS TO EMPTY RATHER THAN TO WRONG.** `rc=$?` after a pipeline measures the LAST stage -- already on this board, twice. The remedy I reached for is `${PIPESTATUS[0]}`, **which is bash; the Bash tool's shell is zsh, where the array is `pipestatus` and 1-INDEXED**, so the idiom expanded to nothing and printed `---rc=`. **It produced NO information wearing the shape of an answer** -- sibling of the `2>/dev/null` entry above, except that a blank is at least visible where a `0` is not. **In zsh: `${pipestatus[1]}`, or drop the pipe and read `$?` from the bare command** (which is what I did, and it is the form that needs no shell knowledge to be right). Found booting on 2026-08-25, reading my own claim's status.
- **A CLAIM OF MINE ABOUT A TEST'S COVERAGE WAS WRONG IN BOTH HALVES, AND I HAD BUILT A PLAN ON IT.** My board said `session_hook_lockout.rs` _covers the hooks' EXIT CODES and asserts nothing about output_. It asserts on the **MESSAGE** and explicitly **NOT** the exit code, and says so in its own header for a good reason (exit 2 there is the gate legitimately blocking). **The real gap is not a missing output assertion -- it is one needle for a class with two shapes**, and unimplemented-versus-refused-by-name are identical to the consumer. **A remembered description of an instrument is not the instrument**; I only found this by reading the file I had already characterised.
- **A BIDIRECTIONAL CLAIM IMPLEMENTED IN ONE DIRECTION -- SECOND INSTANCE IN A WEEK, AND THE FIRST IS ALREADY QUEUED FOR hv.** `every_declared_hook_ships_as_a_script` says _the roster and the filesystem agree_ and walks `for name in HOOKS`: it catches a NAME WITH NO SCRIPT and is structurally blind to A SCRIPT WITH NO NAME. Sibling of `runner_roster_check.sh` reading PRESENT from the COMMIT and ROSTERED from the WORKTREE. **Whenever a check names two rosters, ask which one it ITERATES -- the other side is free forever**, and it will be green the whole time.
- **MY OWN RESTORE PRINTED `RESTORED` AND RESTORED NOTHING, IN THE SAME SESSION I RE-RECORDED _read the exit code, not the message_.** A mutation harness with `trap restore EXIT` and a relative `$SUBJ`; the body then did `cd native/rust`, **so the trap fired in a different working directory and `cp` failed while the unconditional `echo` claimed success.** Two compounding causes worth separating: **a trap captures the CODE, never the working directory it will run in**, and **the Bash tool's cwd PERSISTS between calls**, so a `cd` silently rescopes every relative path in every later call. **Mutating a subject in a SHARED CHECKOUT and believing a restore you did not verify is the version of this that costs someone else their afternoon.** Absolute paths in cleanup, and re-read the bytes afterwards.
- **A PROBE'S CHOICE OF VERB SILENTLY SELECTS WHICH SUBJECT ANSWERS, BECAUSE THE CURRENCY GUARD IS PER-COMMAND.** Driven 2026-08-25 23:12Z across both binaries: **v2 `ac gate ST0057` -> rc=2, the GUARD; v2 `critic rust --files <f>` -> rc=0, the TOOL** -- `critic` is EXEMPT from the version guard (vc's find, ic drove it first, I re-drove both). **So `intent` refusing one verb tells you nothing about the next**, and a probe that picks the exempt verb gets the tool's answer while a probe that picks a guarded one gets the guard's, in the same tree, on the same binary. **THE GENERAL FORM IS THE PART THAT KEEPS: whenever a probe's failure code collides with a GATE's refusal code, the exit status has stopped being evidence** -- and `2` is now carrying FOUR meanings (ic drove it: guard-refusal, retired-path, two more, and `critic <lang>` with no files, which is `2`'s ORIGINAL sense). **`2` is the code the pre-commit gate FAILS OPEN on**, so every meaning riding it inherits _wave it through_. **I met a further one while folding: `intent3 claude ws hygiene` -> rc=2 `known command that is not implemented yet`.** I am NOT claiming that is a fifth distinct meaning -- two of ic's four are unnamed on this board and it may be one of them. **The point stands without the count: the gate cannot tell any of them apart, and waves all of them through.** **I CANNOT RECOVER WHAT THE FIVE `rc=1`s WERE AND I AM NOT INVENTING A BETTER ANSWER.** Re-driven now, bare `intent3 critic <lang>` returns **rc=0 for all five** and no guard exits 1 anywhere I can find, so the attribution _my own guard answering_ is UNVERIFIED and the reading `intent3's guard refuses at 1` is WRONG as written. **Annotated, not repaired -- the same rule as a fabricated stamp: a corrected-looking fake is worse than an admitted one.**

- **A COUNT IS NOT A MEASUREMENT UNTIL SOMETHING SAYS WHAT EACH HIT IS** (vc, 2026-08-25, theirs whole and it generalises my closed-list rule). vc verified _which boards carry this claim_ with `grep -c`, **which counts occurrences of a STRING, not instances of a CLAIM** -- so it scored MY SENTENCE REFUTING THE CLAIM as a hit. **The instrument reproduced the exact error it was checking for.** Classifying required reading all six by hand: 6 hits, 2 real, and both on the two boards vc holds the pen on.

- **AN ASSERTION PLACED AFTER THE WRITE IT GUARDS TESTS THE WRITER, NOT THE SUBJECT -- AND IT CANNOT FAIL.** Folding THIS board at 23:22Z I stamped `heartbeat_at` by LINE INDEX, clobbered `status: active`, and my guard `assert lines[6].startswith("heartbeat_at: ")` **passed on the value I had just written one line earlier.** Board left with two `heartbeat_at` keys and no `status` -- **the one key the protocol says must survive a compact (invariant 6).** Caught by printing the keys back OUT OF THE FILE. **Repaired BY KEY, never by index**, which is the actual fix: the header block is line-oriented `key: value`, so address it by key and the position stops mattering. **Third fold running that the fold's own landing produced the finding** -- and this one is the family head in its purest form, since the instrument WAS the write.

- **`ps | grep '[g]it commit'` IS NOT A PROBE FOR A RUNNING GIT IN THIS ESTATE -- IT MATCHES A PEER LLM SESSION WHOSE SYSTEM PROMPT QUOTES THE WORDS.** Hit an index lock clearing my inbox at 23:23Z, went to check whether it was LIVE before waiting, and my `ps` probe returned a `claude` process from ANOTHER PROJECT whose 40KB prompt text contains the phrase. **43KB of output, zero evidence, and it would have read as confirmation.** **Structural, not bad luck: every node on this machine carries its whole restart brief on its command line, so `ps` output here is mostly PROSE ABOUT commands.** The lock was in fact live and cleared on its own; **the probe that actually answered was `ls .git/index.lock` plus a retry.** Same shape as the instrument that greps a file for an invocation and matches the file's prose about it -- **but the corpus is other agents' prompts, which is new and gets worse as the board grows.**

- **A `sed -i` THAT MATCHES NOTHING IS A NO-OP AND A NO-OP `sed` EXITS 0, SO A WRITE THAT DID NOT HAPPEN REPORTS SUCCESS.** Found on this bounce, on myself, WHILE REPORTING THIS FAMILY: I wrote a lowercase pattern against uppercase text, read rc=0, and was one step from reporting the fix as landed. **Sibling of `A NO-OP CANNOT FAIL` above, and the DANGEROUS direction, because that one gives a wrong ANSWER and this one gives a false DONE.** The fix is not a better pattern: **assert the count before and after inside the writer** -- `before == 1 and after == 0` -- so a miss is an exception rather than a clean exit. Re-drive the sweep afterwards; never accept the writer's exit code as evidence the subject changed. **IT RECURRED AT THE NEXT BOOT WITH A CORRECT PATTERN, SO THE CAUSE ABOVE IS ONLY HALF OF IT: THE ADDRESS IS HALF THE MATCH.** I wrote `5s|^heartbeat_at: .*|...|` against my own header, where `heartbeat_at` is line **6** -- right pattern, wrong line, no match, rc=0. **A BARE LINE NUMBER IS A SECOND SILENT PREDICATE, AND IT DECAYS EVERY TIME ANYONE INSERTS A LINE ABOVE IT**, which on a header block that grows keys is constant. Address by PATTERN and scope with a RANGE (`1,10s|`); never by a bare line number. **AND THE ASSERT FROM THE FIRST INSTANCE IS WHAT CAUGHT THE SECOND** -- a fix landing on a cause it was not written for is the only evidence a rule of this shape ever gets.
- **A TRUNCATED LINE PREVIEW ANSWERS ABOUT THE LINE'S PREFIX, NOT THE LINE, AND ON THIS BOARD'S PROSE-LENGTH LINES THAT IS ALMOST ALWAYS THE WRONG QUESTION.** `grep -n | cut -c1-110` over hv's board showed me `21:33Z` and `17:16Z` -- real stamps, at the START of their lines -- and hid `21:3xZ` and `17:0xZ` further along the SAME lines. I read the count as two; `grep -on` said four. **The instrument's field of view was narrower than the claim I put on it, and the part it showed me was the part that looked correct.** Use `grep -o` whenever the question is _how many of X are in here_ rather than _which lines mention X_.

### FAMILY 2 -- THE CLAIM EXCEEDS THE MEASUREMENT, AND THE TRUE HALF IS WHAT CARRIES THE FALSE ONE

- **MY EVIDENCE WAS ENTIRELY TRUE AND MY SUBJECT WAS WRONG, AND THAT COMBINATION HAS NO TELL.** vc's rebuild trigger names cc's PARSER fix. I ran `git log bfa07975..HEAD -- native/rust`, got nothing, widened, found `e696de15` and `80cb8509`, and told vc cc's half was met. Every fact was true: both commits exist, both are cc's, both are ancestors, both touch the parser's own file. **They are cc's BATCH fixes and touch `field_end` ZERO times.** The parser fix was sitting uncommitted in the worktree. **A TRIGGER NAMES A DEFECT; A RANGE NAMES A POPULATION; AN AUTHOR PREFIX NAMES NEITHER** -- I matched on WHO and WHERE and read it as identity. **A check that did not run is at least silent; a check that RAN, on real data, and returned a TRUE result about something ADJACENT announces nothing at all.** The test that works is for the FIX: `fn split_field_value` present in **HEAD's** `legacy.rs`, which is how I cleared it for real at `1583d1ad`.
- **AND THE RANGE I BLAMED WAS A SECOND, SEPARATE DEFECT THAT IS ALSO REAL.** `bfa07975..HEAD` was empty because both commits predated my own last commit. **A range anchored on your own last commit cannot see work that predates it, and empty reads exactly like "they have not landed"** -- the wrong-population shape on the axis of TIME rather than of files. Finding it did not save me: I used what it surfaced to answer a question it does not answer. **A runbook warning only about the blind range would have let me make the identical error with a correct range.**
- **A FALSE CLAIM THAT LATER BECOMES TRUE FOR A DIFFERENT REASON IS THE WORST WAY FOR ONE TO AGE.** `bc38db85` said cc's half was met; it was wrong when written and true an hour later, for `1583d1ad`. **There is then nothing left to correct and no trace it was ever wrong.** Correct forward in its own commit (`a163ba56`) and leave the error in history, because a peer had already read the claim.
- **A SCOPE RULING SILENTLY NARROWS YOUR SEARCH POPULATION, AND NOTHING ANNOUNCES WHEN THE RULING EXPIRES.** I told hv the install artefact DID NOT EXIST. It exists: `bin/.devbin/cmd/macos` carries ten verbs including `formula` and `publish`, and **its header at `:81-96` states my own option-3 finding, MEASURED, before I derived it today.** I never looked there because hv had ruled `cmd/macos` out of scope that morning -- **and I am the node who DECLINED `AC-11.7` on scope grounds, so I of all people knew that file held the release mechanics.** The ruling was correct, it removed a file from my corpus, and when hv started release engineering the premise died with nothing to mark it. **The withdrawal reason had even written the trigger down -- _re-mint this row when release engineering starts_ -- so the expiry WAS recorded, on the row, and I still did not re-derive the population.** A decision that names its condition can be retired; **it does not retire itself.**

- **NOTHING CONNECTS A DECISION TO THE CONDITION IT WAS MADE UNDER, AND A DEFECT'S DISAPPEARANCE ANNOUNCES ITSELF TO NOBODY.** Three instances on 2026-08-25, three nodes, one shape (vc's count): AT-11.7 refused twice from a premise hv had retired; vc's `hv/wip.md` scope line reasoned from hv being unreachable, still standing after hv was reachable; **and mine -- I switched to a bare `git commit` because `--only` was dying on a cache-tree phantom, my own fixture leak, fixed an hour later in `48ba2103`, and I never switched back.** **A defect announces itself; the end of a defect does not**, so the workaround stays and the protection it disabled stays off. **THE EXPIRED PREMISE IS THE HARDEST CLASS HERE BECAUSE NOTHING IS EVER WRONG AT THE MOMENT ANYONE LOOKS AT IT.**

**A CONFIDENT UNMEASURED NUMBER IN A _RATIONALE_ IS LOAD-BEARING IN A WAY ONE IN A _REPORT_ IS NOT -- A REPORT GETS CHECKED, A RATIONALE GETS HONOURED** (vc's formulation). All three of my shipped defects are instances.

**AND ITS INHERITED FORM, PAID FOR 2026-08-26 AND WORSE THAN THE SELF-INFLICTED ONE: A PEER'S MEASUREMENT ARRIVES WITH A CAUSE ATTACHED, AND THE CAUSE IS THE PART NOBODY MEASURED.** vc's _167 tracked views rewritten_ was real. _By read verbs_ was the attribution, and it was the load-bearing half. I put the whole sentence into this board as established fact and built a release-note recommendation on it. **Worse, I then read my OWN result through the borrowed premise instead of testing it.** My fixture ran the PAIR and never once ran the 3.0.0 keg, so I held exactly zero evidence about the keg -- and I wrote vc the sentence _YOU MEASURED THE SHIPPED KEG; THE PAIR DOES NOT DO IT_, which asserts a CONTRAST whose other half I had not measured. The half that was mine (_the pair does not rewrite on read_) was sound and helped kill the claim. The half that made it a finding was borrowed. **A measurement handed to you is evidence; the cause attached to it is a hypothesis wearing the measurement's authority, and quoting the number is what launders the second into the first.** Restate a peer's figure with its cause marked as theirs and unverified, or measure the cause yourself before it becomes a premise in your own file.

- **`bin/intent3` claimed a coherence check would be "a MULTI-SECOND gate on every command".** Driven: **~85ms end-to-end**, against a component sum measured separately at ~110ms. **The two do not reconcile and I have not resolved them; neither is within two orders of magnitude of the claim.** It was the entire stated reason for not building the thing hv later ruled I should build, and it held the design shut for three days.
- **`cmd/hosting` claimed "in a throwaway clone a mutator harms nothing".** Never driven. Every verb ran from the LIVE tree's cwd and it emptied the durable store.
- **`currency.lib` PRINTED A FLOOR AS A DISTANCE** -- the overclaim in the error message of the file written to refuse overclaims, **and the fix for it added an arm nothing drove** (ic). **A fix that adds an arm adds a thing to drive.**
- **A WARNING IS NOT DISCHARGED BY BEING TRUE -- IT IS DISCHARGED WHEN THE REMEDY IT INVITES IS ALSO CHECKED** (ic, against themselves).
- **MY TELL vs cc's, AND THEY ARE MIRRORS:** I publish the claim the measurement INVITES (wrong at the READING); cc drives the measurement and publishes a stronger claim about what it measured (wrong at the WRITING). **Both rest on a real number, which is why neither gets challenged.**
- **A ROW CARRYING ONE TRUE SENTENCE AND ONE FALSE ONE IS HARDER TO CATCH THAN A WHOLLY WRONG ROW, BECAUSE THE TRUE HALF IS WHAT A READER CHECKS FIRST AND IT HOLDS** (cc, on my false roster row).
- **`checked against the schema` VERSUS `checked against a grep of the source` IS THE WHOLE DISTANCE, AND NEITHER OF US COULD SEE IT IN OUR OWN SENTENCE AT THE TIME OF WRITING.**
- **A STABLE CONCLUSION ACROSS THREE WRONG POPULATIONS IS NOT CORROBORATION -- IT IS THE POPULATION NOT BEING LOAD-BEARING FOR THAT CONCLUSION** (cc, on vc's withdrawal).
- **A HAND-MAINTAINED SET THAT NOTHING CHECKS IS THE ROSTER PROBLEM,** and I built one while explaining it. **Manifest plus a drift check, and a path in NEITHER the manifest nor a declared-exclusions list is an ERROR rather than a judgement call** (vc).
- **UNCHECKED IS NOT EMPTY, AND ONLY THE OUTPUT CAN CARRY THAT DISTINCTION.** An instrument that measures a narrow scope and reports in the vocabulary of the general category produces a **true sentence and a false belief**, so nothing it can check is wrong. Four instruments, one shape: `0069`, `0074`, `0075`, `0076`. **A silent gap gets found; a gap that files a clean report on its own behalf does not.**
- **A RULE IS HONOURED BY WHOEVER LEARNED IT AND DOES NOT PROPAGATE BY HAVING BEEN STATED.** `precommit` has 14 guard arms at three strengths; **7 assert a repository finding on ANY non-zero exit.** The arm BURNED by this class guards the tool being MISSING and is **still exposed to the tool being BROKEN** -- driven, not read. **Fixing one more arm in passing would be the same error a fourth time; the class needs one change across seven sites.**
- **A SUPERSEDES NOTE APPENDED BELOW WHAT IT SUPERSEDES LEAVES THE DEAD CLAIM AS THE HEADLINE.** `acceptance.md:199` still OPENS with the green arm vc amended away, with the correction far below it. **`intent/restart.md` leads with exactly this: if you find yourself writing a supersedes banner, DELETE WHAT IT SUPERSEDES INSTEAD.** It is the gate-figure defect at document scale -- three homes, three values -- and **a builder who reads the row's first sentence builds the wrong guard.** Raised with vc; the amendment's REASONING is worth keeping, the dead arm is not.
- **A GUARD CAN HAND OUT A REMEDY THAT CAUSES THE DEFECT ANOTHER GUARD EXISTS TO PREVENT, AND NEITHER CAN SEE IT.** The currency guard refuses a stale binary and prints `int local build`; that rebuilds into `native/rust/target/release/`, **shared by four nodes with no isolation underneath it**, from whatever the tree currently holds. AC-11.6 exists because exactly that emptied the store for every node on 2026-08-18. **Each guard is correct about its own property. The composition is the defect, and nothing in the roster has a view of it.**
- **A WRONG LINE NUMBER IS ONLY A TRAP IF FOLLOWING IT CAN REACH A GREEN.** vc warned the 3.2 error at `:216` would lead a fixer to gut the exemplar. Built it: the error MARCHES to `:225` and stays there. **Misleading, not trapping -- and the marching is what tells you to stop chasing and look up.**

- **A CORRECT MEASUREMENT DESCRIBED IN THE WRONG TERMS TRAVELS AS THE WRONG RULE -- MY OWN FORMULATION, ARRIVING BACK AT ME WITHIN THE DAY.** I reported that `settings.json` WIRES four hooks. **It wires three**; the scripts directory ships four and `post-tool-advisory` is unwired ON PURPOSE (ST0035 decision #4, stated in the script's own header). **MY CODE WAS RIGHT THE WHOLE TIME** -- it unions the two sources and the file header names post-tool-advisory as scripts-only -- **so the true half (a four-name canon against a three-name roster) carried the false half undetected, and the conclusion was unaffected, which is exactly what stops anyone checking.** vc had to go to the files to find out whether a THIRD roster disagreed. **AND THE WRONG FIGURE IS DURABLE: it is in the body of `a40cf763`, which is on shared `main` and will not be rewritten.** So it must not be inherited into the `AT-07.2` row text -- the correct sentence is _ships four, wires three, union four_.

- **A NUMBER WHOSE SUBJECT IS NOT ON THE LINE CANNOT BE CHECKED BY THE NEXT READER.** vc's board carried `Gate still PASS at 67 of 67`; **no thread gate is 67** (64/134, 51/53, 2/6). It was `ST0057` 51/51 **plus** `ST0056/03` 16/16 -- **two thread gates summed, with nothing on the line saying so.** I could tell it was uncheckable WITHOUT being able to tell whether it was wrong, and that is the whole finding. **`0088`'s citation half in numeric form**; vc's `wip.md` now prints all three calls rather than the sum.
- **A SUPERSEDES BANNER LEAVES TWO VALUES IN ONE HOME, AND THE TRUE HALF IS WHAT MAKES THE FALSE HALF READABLE.** `vc/wip.md:8` OPENED `ST0057's GATE IS BLOCKED AT 51/53` and CLOSED `Gate still PASS at 67 of 67` -- one `focus:` value, two gate states, written by the node whose announce was ABOUT a false gate figure. **`restart.md` names this exact defect and states the fix: _if you find yourself writing a supersedes banner, DELETE WHAT IT SUPERSEDES INSTEAD._** vc deleted both (`e63df597`), and I rewrote my own falsified watch-out in place rather than banner it, at 23:14Z. **The rule applies to the node that sends it first.**

- **A `git status` IS PERISHABLE IN EXACTLY THE WAY A TIMESTAMP IS, AND MINE WAS STALE AT BIRTH RATHER THAN EXPIRED.** I told vc _none of the dirty files are mine, my paths are clean_ while quoting the BOOT-TIME status -- having edited my own board two calls earlier. **The rest of the class is a sentence that was true and went stale; this one was never true**, and that is worse precisely because nothing about a file list reads as perishable. **Re-drive the status at the moment of the claim; never quote an earlier one.** Caught by vc, not by me.

### FAMILY 3 -- ROUTING, RELAY AND ATTRIBUTION

- **A CORRECT MEASUREMENT DESCRIBED IN THE WRONG TERMS TRAVELS AS THE WRONG RULE.** `git status` genuinely showed one line, `M  mutation_every_writable_field.rs`, staged -- so I wrote _one staged file_ and thereby implied **staged** was the predicate. It is **every uncommitted path**; my own guard measures it correctly and my sentence about my own guard did not. **It reached ic through vc inside a minute, and ic caught it only by DRIVING the build instead of reporting success.** The observation was true, the framing was wrong, and nothing in the observation's correctness slows the wrong rule down.

- **THE OFFER IS THE MOMENT TO CHECK, NOT THE SEND.** Once two messages are in flight the only tools left are racing and deduping, and only one works. Three instances, one mechanism.
- **A FORWARDING OBLIGATION DISCHARGED BUT NOT CANCELLED MANUFACTURES FALSE CORROBORATION** -- it arrives TWICE wearing TWO authorships and **the second announces itself as agreement. Tell the RECIPIENT to expect one copy; the recipient can dedupe, the relay can only be beaten.**
- **VOLUNTEERING A RELAY _IS_ CREATING THE OBLIGATION, AND THE COORDINATOR HABIT IS TO VOLUNTEER** (vc). A rule only the receiver can apply arrives one step too late.
- **NAME WHICH _HALF_ YOU ARE ATTRIBUTING** (vc, wholly theirs). An incident and its generalisation are separable and usually have different authors. **Nobody audits an attribution.**
- **A PEER TELLING YOU WHAT ONLY THE SOURCE CAN SETTLE IS TELLING YOU WHAT _THEY READ_** (ic). Fold instructions, quoted rows, relayed rulings alike.
- **A RULING DELIVERED AS A SELECTION AMONG OPTIONS YOU AUTHORED IS ONE WHOSE WORDING IS YOURS AND WILL BE QUOTED AS THEIRS. RECORD THE OPTIONS, NOT THE OUTCOME**, in an inbox where it survives the session.
- **A CORRECT ANSWER ABANDONED ON A PEER'S SAY-SO IS WORSE THAN THE PEER'S WRONG ANSWER, AND IT IS THE HALF YOU CONTROL.** I had the attribution start date right at 2026-08-24. vc said 08-21. **I corrected myself to hv without running the one command that settles it** -- `git merge-base --is-ancestor` -- and vc's commit was on `v2-maintenance` and never on main. **My own board already carried the rule: a peer telling you what only the source can settle is telling you what THEY read.** A wrong answer from a peer is ordinary traffic. **Moving off a correct one without a check is the failure, and no instrument catches it because both answers look like answers.**
- **I REFUSED TWO RELAYED AUTHORISATIONS TODAY AND BOTH REFUSALS WERE UPHELD.** vc relayed hv's ruling that the attribution guard was mine to build; hv had ruled it in **vc's** session answering **vc's** question, and hv has me holding in mine. **vc withdrew it without argument and called it the precise thing they had refused from devbin-cc that morning.** The argument that did the work is worth more than the instance: **the discipline does not get to bind you and not me on the same afternoon.**
- **THE INVERSE ALSO HELD: I RELAYED AN hv RULING TO vc AND FLAGGED IT AS A RELAY, AND TOLD THEM TO CONFIRM IT AT THE SOURCE. THEY DID, AND IT HELD.** Quote the ruling VERBATIM rather than characterising it -- a ruling paraphrased in your words gets quoted back as theirs.
- **THE BLOCKED PARTY TELLS THE BLOCKER, NOT THE SEQUENCER.**
- **A PROOF ONLY ITS AUTHOR CAN REPRODUCE IS NOT YET A PROOF THE ESTATE HOLDS** (ic). Survives the soundness being conceded. **Script the repro.**
- **A PEER'S READY-MADE COMMAND IS NOT AN APPROVAL, AND CONVENIENCE IS WHAT MAKES IT SLIP.** Distinct from a relayed approval: the peer claims no authority and is simply being helpful, so there is nothing to challenge and nothing that reads as a demand. **devbin/vc's own words on it: _that is the failure mode of convenience in this setting._** The test is not whether the change is sound -- theirs was -- **it is who gets to say yes.**

### FAMILY 4 -- THE SHARED CHECKOUT

- **`git add` THEN `git commit --only` IS NOT ATOMIC ACROSS THE INDEX LOCK, AND THE SECOND ERROR MASKS THE FIRST.** Landing this very fold, five nodes folded at once on hv's instruction and ic's `git commit` held `.git/index.lock` through its whole pre-commit gate. **My `add` failed on the lock; `--only` then refused the still-untracked archive and said `pathspec did not match any file(s) known to git`** -- which points at MY PATH and says nothing about a lock. **Read alone, the visible error accuses the wrong thing, and the wrong thing is plausible.** It is Family 1's head arriving through git: the LAST error read as THE cause. **Nothing landed, which is the `--only` refusal working.** The lock was LIVE, not stale (`ps` showed the peer's commit at 6 seconds) -- **never delete a lock you have not proved is stale**; wait and retry, and gate the commit on the `add`'s own rc rather than letting the two run blind.

- **`MM` IS A CLAIM ABOUT THE INDEX'S CACHED STAT, NOT ABOUT CONTENT, AND UNDER LOCK CONTENTION IT SAYS `staged` ABOUT A FILE WITH NOTHING STAGED.** My board commit collided with a peer's git process (`fatal: Unable to create '.git/index.lock'`) and the commit's own post-commit cleanup never ran. `git status` then showed `MM` on my `wip.md` -- **staged AND worktree-modified, which is the exact shape of the hazard that swept vc's file into `fcf59ec9`** -- so I went at it as an emergency. **All three diffs were empty**; `git update-index --refresh` cleared it. **DIFF BEFORE YOU REMEDIATE.** The remediation for a real `MM` would have been harmless here, but acting on a porcelain code without reading the CONTENT is how I caused an estate-wide block on Sunday.

- **`git commit --only <paths>` IS NOT A NICER SPELLING OF CHECKING THE INDEX FIRST -- IT IS THE ONLY FORM THAT IS ATOMIC WITH RESPECT TO PEERS.** Reading `git diff --cached --name-only` **measures a MOMENT, and the commit happens at a different one.** I read exactly my three files, committed, and took a fourth: vc had staged an archive in the seconds between. **Neither half does it alone -- their add lands harmlessly under `--only`, my bare commit takes nothing if they do not add** -- so it is not carelessness, it is two correct-looking local decisions composing into a wrong global one with neither party holding the information to see it. **The pathspec is the control; the reading never was.**
- **THERE IS NO SUCH THING AS A WORK-IN-PROGRESS EDIT TO A GATED GUARD IN A SHARED CHECKOUT.** The pre-commit gate runs the **WORKTREE** copy, so every intermediate save is instantly live for four other nodes. **Three estate-wide blocks on 2026-08-25, all mine, all from this: a dropped exec bit, a roster row live while its file was untracked, and a lib gone plural while its consumer still read the singular.** `bash -n` passing says nothing about whether the estate can commit. **Edit a copy, verify, move it into place** -- the same shape already queued twice today for the build path, which I did not connect to my own editing until ic handed me the third instance.

- **`awk ... > tmp && mv tmp file` DROPS THE MODE, SILENTLY, AND EVERY CHECK I OWN IS BLIND TO IT.** I used it three times, chmod'd two by reflex, and left `bin/.devbin/cmd/precommit` at 644 -- **every `git commit` in the repo failed until ic drove it to the cause.** `bash -n` passes, the content diff is clean, and invoking a tool as `bash tool.sh` runs a 644 file perfectly. **A second instance in `runner_roster_check.sh` was caught only because the roster check tests its own population's modes**, and its finding names exactly why nothing else can: _the runner invokes tools with `bash`, so a 644 instrument runs and the gate stays green_. Instruments 755, sourced libraries 644.
- **THE REMEDY FOR A SHARED-INDEX HAZARD CAN BE THE NEXT OUTAGE, AND BOTH MOVES WERE INDIVIDUALLY RIGHT.** vc flagged five staged files as a loaded gun; I agreed and unstaged; **unstaging left a live roster row naming an untracked file and refused every node's commit.** Neither staging nor unstaging was the fix -- **backing the ESTATE-WIDE CLAIM out of the worktree was.** A pathspec commit builds a temp index of HEAD plus that path, so a peer's staged files are invisible inside it while the peer's WORKTREE runner still declares them. **ic named the shape: the row landed and the instrument did not, the adopter-lands-first rule running backwards.**
- **PROVE AN UNBLOCK WITH A REAL COMMIT, NEVER BY ASSERTING IT.** I asserted twice and was wrong twice in twenty minutes. `4b500e62` was a real pathspec commit through the full gate, and **the empty index is better than the disarmed one**: `git diff --cached --name-only` returning nothing means a bare `git commit` from any node takes nothing at all.

- **FOUR WRITERS, ONE TREE, ONE INDEX.** Peers' dirty files are in every `git status` you read. **Name yours; `--only` is PATH-scoped, never hunk-scoped.** Peers commit concurrently -- **wait a lock out, never clear it.**
- **AN UNCOMMITTED EDIT WAS ERASED HERE WITH NO REFLOG TRACE AND hv RULED IT ACCEPTED RATHER THAN INVESTIGATED.** Live and undiagnosed BY DECISION. **A protective copy OUTSIDE the tree costs nothing; staging is NOT the remedy, because the index is shared.**
- **A FIGURE ABOUT HEAD BELONGS IN THE SINGLE-WRITER CLONE** (`int suite`, `int hosting`). A figure about the WORKING TREE must be defended by attribution instead.
- **`bin/` IS dc's LANE** (hv). `bin/.devbin/cmd/**` is Intent's own; **`bin/devbin` and `bin/.devbin/lib/**` are VENDORED and not this repo's to edit.**
- **A MONIKER NAMES WHERE A SESSION LIVES, NEVER WHERE ITS BYTES LAND.** `devbin/vc` works in `~/Devel/prj/Devbin` and its fleet sweep writes into **eleven checkouts including this one**. Five paths here went dirty mid-commit with no announcement, and **nothing on the wire marks the crossing.** They vendor-and-HOLD where a tree is dirty at pre-flight, so held bytes sit uncommitted and unattributed until someone asks. **ASK, DO NOT INFER: the two answers are `a peer is working` and `something writes to this tree unattributed`, and only one is survivable.**
- **`--only` IS WHAT MADE THAT A QUESTION RATHER THAN AN INCIDENT** (devbin/vc's words, and they are right). A bare `git add -A` sweeps a fleet vendor into your commit **silently**, and neither party learns until the log reads strangely.
- **THE FORMATTER IS A SECOND WRITER BETWEEN THE SYNC AND THE COMMIT, AND IT FIRED AGAIN TONIGHT.** It realigned a table I had just synced, so canon named bytes the file no longer held and the gate refused at `ADDS 1`. **Order: write, LET THE FORMATTER SETTLE, sync, commit.** Second time in this file's history, and this time inside the document about instruments misreporting.
- **THE SUBJECT CAN MOVE BETWEEN YOUR TWO READS, AND THE SECOND READ LOOKS LIKE AN ANSWER.** `git diff --numstat` reported changes to an issue's canon; `git diff` seconds later printed nothing. **Not a broken instrument -- a peer committed in between.** vc hit the identical trap twenty minutes earlier (`d07c94fd`). **In a four-writer tree a two-command measurement has no single subject unless you pin the revision.**
- **COMMITTED IS SELF-DESCRIBING; HELD IS NOT** (mine, adopted into devbin's sweep procedure at their `c6c30f9`). A committed change carries an author, a message and a log entry. **Bytes vendored into your tree and deliberately left uncommitted carry NOTHING**, which is exactly the state that reads as an intruder. **A fleet operation must name its HELD estates, not merely pre-announce itself.**
- **A FILTER OVER A COMMAND'S OUTPUT MUST BE ABLE TO EXPRESS ITS FAILURE, NOT ONLY ITS SUCCESS.** I piped `git commit` through `grep -E 'files changed|^\[main|error|refus'` and it printed NOTHING: a peer took the index lock, git said `fatal: Unable to create '.git/index.lock': File exists`, **and that message matches none of those patterns.** **A FAILED COMMIT PRESENTED AS SILENCE**, and only an independent `git log -1` caught it. **Same family as the whole evening: the instrument could report the outcome I expected and not the one I needed.** Read the rc, or do not filter.

- **A SCRATCH DIRECTORY INSIDE A GUARDED TREE IS INDISTINGUISHABLE FROM IN-FLIGHT SOURCE TO EVERY GUARD WE OWN.** cc's `native/rust/.cc-probe/` blocked my build and would have blocked any node's: **`verify_pair`'s arm 2 refuses on `??`, not only on modifications**, so ninety seconds of untracked scratch in a shared platform path is an estate-wide stop. The probe itself was the right instrument -- identical sha256, one path refuses and one resolves -- in the wrong place. Put scratch under `target/`, and **verify the ignore rather than assuming it** (`git check-ignore -v`), which is what cc did.
- **THE ISOLATION YOU REACH FOR TO AVOID DISTURBING A PEER'S BUILD IS EXACTLY WHAT INVALIDATES YOUR RESULT -- TWO NODES, ONE DAY, ONE INSTRUMENT DEFECT.** ic manufactured 13 false failures in the morning and cc 2 in the afternoon, both from a `CARGO_TARGET_DIR` in scratch. **`install::resolve()` walks `current_exe()` ancestors for the `lib/templates` marker with NO ENVIRONMENT FALLBACK**, so a binary built outside its install tree cannot locate itself BY CONSTRUCTION and every test that shells out to it reds for a reason unrelated to the change under test. **The safe-looking choice is the wrong one, which is why both of them took it.** An explanation of a red is not a green: re-run from a resolvable path before reporting totals.

## Decisions

- (2026-08-26) **VERIFY RESOLUTION, NEVER ENUMERATE POSITIONS.** `use` repoints what it owns and then measures what actually answers `intent`, dying if it is not the flavour asked for. **That catches PATH 19, and any position nobody has found yet, WITHOUT NEEDING TO KNOW IT EXISTS.** An enumeration goes stale the day someone adds a binding; a measurement of resolution cannot. This is the general form of the thing `cmd/local`'s own header had as an episode.
- (2026-08-26) **`use` LIVES IN `cmd/local`, ON HIGHLANDER GROUNDS, AND I TOOK IT BACK FROM cc TO PUT IT THERE.** hv assigned it to both of us in separate sessions. `path_hits`, `verify_pair`, `describe_binding`, `BINDIR` and `PAIR` all already live in `cmd/local`; a separate `cmd/use` duplicates five things or forces an extraction at speed on release day. **cc had independently reached the same design and deferred to `local status` for the reporting half -- the right call, and they handed the rest over without argument.** Their `verify_hook_door` is in the commit and attributed. **Neither of us was reaching; a double-assignment is not a land grab, and saying so is what kept it cheap.**
- (2026-08-26) **CHECKING WHICH BINARY RESOLVES AND CHECKING WHETHER THE DOOR OPENS ARE DIFFERENT PROPOSITIONS** (cc, and it is the thing my design was missing). They come apart on the PROD arm: the door walks `current_exe()` ancestors for `lib/templates/`, **a brew install is a COPY**, and a copy with no marker above it refuses every hook at exit 1 -- **which Claude Code does not block on, so the strict gate stops enforcing silently in every project at once.** `verify_pair` and a resolution check BOTH PASS while that is true. Driven with stdin closed: `require-in-session` reads its payload from stdin and **blocks forever on an inherited one.**
- (2026-08-26) **`publish` STOPS WITH hv.** hv authorised vc to direct my work on other projects, and I take that as covering fleet work and NOT a public push to a tap. **Stated by me rather than waited for.**

- (2026-08-25) **D42 AMENDED BY ME, AGAINST MYSELF: THE RULE IS THAT NO CALLER AUTHORS A STAMP.** The signature test -- _no function TAKES a time_ -- is one sufficient condition and was never the definition. `Facade::set(&addr, "created", <a time>)` satisfies the letter and produces the forbidden behaviour, because a generic setter routes around a proxy without touching the property. **A rule stated as a MECHANISM gets evaded by a different mechanism, and its author is the last to notice.** vc asked; AC-14.11 already said it project-wide. Closing condition 2 of 3 on AC-08.5.
- (2026-08-25) **AT-11.6's TWO REAL DEFECTS WERE BOTH FOUND BY LIVE INCIDENTS, NOT BY REVIEW OR MUTATION.** It shipped with nine arms and four mutations. **Mutation testing varies the SUBJECT and holds the ENVIRONMENT fixed**, so the git-env leak was unreachable by that method; and the scope gap was an INPUT outside the predicate's population. **Neither is a weak test suite -- both are the harness being narrower than the thing it certifies.**
- (2026-08-25) **`--only` IS THE ONLY COMMIT FORM ATOMIC WITH RESPECT TO PEERS.** Reading `git diff --cached` first measures a MOMENT; the commit happens at another. I read exactly three files and committed four. **The pathspec is the control; the reading never was.**
- **2026-08-25 -- vc HAS hv's PEN AND hv SAID SO IN MY OWN SESSION**, which is what makes it different from the two relayed authorisations I refused yesterday. **Scoped, and vc has honoured the scope unprompted: A2 the attribution guard STAYS HELD, because hv delegating COORDINATION does not make vc a source of hv's AUTHORISATIONS, and a delegation that laundered relays would be indistinguishable from one that did not.** vc refused to relay it and told me not to accept it from them if they did.
- **2026-08-25 -- vc's OPENING ASSIGNMENT WAS WRONG AND THE CONTRACT CAUGHT IT.** `claude ws` was routed to me as WP-07 work off ic's surface probe; **it is WP-14's (`AC-14.10`), and `AC-14.7` says every `/in-whiteboard` verb is served by `intent wb` FROM THE STORE.** Porting v2's file-based `intent_claude_cwi` would have built the thing WP-14 exists to replace, in the subsystem the four of us coordinate through. vc withdrew it in full. **The finding underneath is bigger than the assignment: `AC-12.1` prunes `bin/` at the cut, so if WP-14 is Not Started then, the whiteboard provisioner does not degrade -- IT DISAPPEARS. WP-14 blocks WP-12 and that dependency is stated nowhere.** With hv as a sequencing question.
- **2026-08-25 -- AC-07.2 INVESTIGATED AND DELIBERATELY NOT STARTED.** Writing `hook_compat.rs` dirties `native/rust`, which is the exact subtree four nodes are waiting to see clean and the only route back to a current shared binary. **My own guard's predicate, applied to my own next task. The first real test of a rule is whether its author follows it when it costs them the appearance of progress** -- `ST0056/11` stays at 3/7 and a true 3/7 beats a busy 4/7 built on a dirty tree. **DISCHARGED THE SAME DAY: the gate closed, the premise died, and AT-07.2 is green at `77878284`. KEPT RATHER THAN DELETED because the entry's subject expired and its lesson did not** -- the first real test of a rule is whether its author follows it when it costs them the appearance of progress, and that is still true of the next rule I write.

- (2026-08-25) **hv RULED, UNPROMPTED AND VERBATIM: the `(C)` line is NOT required and never was.** _"This isn't a problem, has never been a problem, and is not something that I suggested we go looking for. The only constraint is that I DO NOT WANT ANY CLAUDE EXHAUST IN MY COMMITS. EVER. The end."_ **Killed a three-node sweep in one line:** vc's 10 missing-`(C)` commits, cc's 3, hv's own 2 -- **not findings**, retracted rather than carried as closed. cc's structural claim that the two lines are one block keeps its refutation (`(C)`-absent-and-trailer-present is a population of ZERO) but **loses its consequence**, and my 12-of-12 counter-datapoint went with it. **The proposed guard is therefore ONE-DIRECTIONAL: absent-trailer only.**
- (2026-08-25) **hv's SCOPE FINDING, AND IT IS ABOUT US RATHER THAN ABOUT THE COMMITS:** _not something that I suggested we go looking for._ I raised the Claude exhaust; the `(C)` sweep grew out of it across three nodes with measurements, mutual corrections, a withdrawn hypothesis and a remedy recommendation, **and nobody asked for any of it.** Each of us measured carefully. **CAREFUL MEASUREMENT OF AN UNASKED QUESTION STILL COSTS A DAY.**
- (2026-08-25) **THE EXHAUST ITSELF IS A TIGHT DISJUNCTION AND ONLY hv CAN CLOSE IT** (mine, adopted by vc to hv in my words). `includeCoAuthoredBy: false` is set, `attribution.commit` is byte-identical to the injected instruction's first line -- **but NO settings key anywhere holds the `Claude-Session` string**, global or project. **So EITHER that key gates the generated line despite its name and manual omission expires, OR nothing in configuration gates it, the omission is PERMANENT, and every fleet project inherits it.** No third branch survives; I exhausted the settings locations. **The deciding check: one commit from a session STARTED AFTER the change, then `grep -c '^Claude-Session:'`.** Neither vc nor I can run it from a session predating it. **90 carriers, 0 `Co-Authored-By`, first on main `55fc4a50` 2026-08-24.**
- (2026-08-25) **vc AS CONTRACT STEWARD AMENDED AC-11.6 UPWARD ON MY ROUTING, AND MY PROPOSAL CARRIED AS WRITTEN** (`f68d397c`). **Their green arm was the cause of the row being unbuildable and they said so.** Branch not taken: leaving the row and building a discriminator -- refused on two independent grounds, either sufficient. **Protocol 3.0 invariant 3 forbids the ownership oracle** (_claims by ST ID, never glob paths_), **and cc's finding kills it anyway** (a shared-tree build carries the UNION whoever invokes it, so authorship is not recoverable from the artefact).
- (2026-08-25) **THE ROUTE FAILURE ON AT-11.6 IS MINE AND IT IS THE BETTER EXHIBIT.** I wrote _routes to vc_ in the same paragraph as the conflict, **on a board that archives at the fold, and then did not route it.** Four days invisible. vc said they had been sitting on it; **they had not, it was never sent** -- zero hits for `11.6` in their inbox from me. **THE WRITE SUCCEEDED AND THE DELIVERY NEVER HAPPENED**, which is sharper than any of the four filed 2026-08-24.
- (2026-08-25) **hv RULED: fix both trees LOCALLY, commit, and DO NOT PUSH v2.** _"The checked out v2 branch is only being used locally here by projects on this machine ... as long as [they] can work on a stable v2 branch and not have their project management functions screwed up while we build v3, I'm good."_ **The public drift is knowingly worn.**
- (2026-08-25) **hv CHOSE THE ROUTE-LEVEL DECLARATION over a per-file exception list.** Branch not taken: an entry per fix, which the guard's own text forbids and which would be FALSE of every file it named -- not PENDING (the v2 landing is done) and not V3-ONLY (v2 received it).
- (2026-08-25) **THE ENFORCEMENT LOSS IS A COST OF hv's NO-PUSH RULING, NOT OF THE ENGINEERING, AND NOBODY PRICED IT WHEN THE DECISION WAS MADE** (vc's framing). Routed to hv rather than absorbed in a test file. **Release is an automatic caller with ground truth, but it is the LAST place you want to first learn a fix reached one tree.**
- (2026-08-25) **A PEER'S FULLER QUOTE OF A RULING IS STILL THE PEER.** vc supplied the clause they had dropped -- the one authorising the commit -- and told me not to treat their reading as my authorisation. **I held for hv's word in my own session and vc was right that I should.**

- (2026-08-24) **THE BUILDER CARRIES THE ROW, IN BOTH DIRECTIONS.** vc built four things in my lane and carries all four (vc, applying my own rule to me); cc then offered me their v2-estate arm to PLACE because it landed in my lane, reading my wall warning as territorial when it was PROCEDURAL. **A row does not transfer because of where it lands.**
- (2026-08-24) **A PEER RELAYING AN APPROVAL IS NOT THE APPROVAL.** cc reported matts had ruled 0070 mine to close; I declined the relay and **cc withdrew the framing themselves** -- it answered a question asked in THEIR session.
- (2026-08-24) **hv RULED: `intent3` MAY REFUSE.** Branch not taken: _keep it a reporter, warn but exec_. **vc preserved the menu, which is the remedy for the `sync`-skip provenance gap.**
- (2026-08-24) **hv RULED: the local `v2-maintenance` branch DELETED.** A silent wrong answer became a loud absent one. **Fast-forwarding LOOKS equivalent and re-arms the moment upstream moves.**
- (2026-08-24) **hv's FREEZE SCOPE: frozen for features, live for shipped-surface defects.** `0070` was v3-only; `0071` is shipped-surface and lands in both.
- (2026-08-24) **TWO MECHANISMS ENFORCING DIFFERENT PROPERTIES ARE NOT TWO COPIES OF ONE.** `currency.lib` is EXEC-time; `cmd/local`'s `verify_pair` is BUILD-time and demands `sc == HEAD` exactly. **The convergence is RECORDED AND DELIBERATELY NOT DONE.**
- (2026-08-22) **A CLAIM OF UNIQUENESS IS A MEASUREMENT AND MUST BE GREPPED, NOT ASSERTED.**
- (2026-08-22) **SOUND-BUT-UNNECESSARY AND UNSOUND ARE DIFFERENT VERDICTS, AND ONLY ONE IS FAIR TO THE PROPOSER.**
- (2026-08-21) **A ROSTER ROW AND ITS RUNNER MUST BE ONE COMMIT.** Either disagrees alone.
- (2026-08-20) **A REPORTER FAILS OPEN; AN ACTOR REFUSES.** `intent3` is an ACTOR, on PATH.
- **2026-08-25 -- A TEST'S POPULATION IS NEVER READ FROM THE THING UNDER TEST.** `hook_compat.rs` assembles its hook roster from the shipped scripts and `settings.json` and never from `install::HOOKS`. Reading the constant would have been asking the suspect to describe itself, and it would have agreed with the defect in perfect silence. **The general rule: when the SUSPECT is a roster, the population comes from somewhere the suspect does not write.**
- **2026-08-25 -- I DID NOT WIDEN A PEER'S GUARD TO CLOSE MY OWN FINDING.** `every_declared_hook_ships_as_a_script` is one-sided and lives in cc's module. My file covers that direction from the SURFACE, so the class closes either way. **I corrected only the two prose claims MY change would otherwise have made false** -- a baked-in _three names_, and _nothing but this test compares them_. Widening someone else's guard quietly is the defect class rather than the fix; falsifying their doc and leaving it is just as bad.
- **2026-08-25 -- THE PRECONDITION I WROTE AGAINST MYSELF ON 2026-08-25 EXPIRED AND I CHECKED RATHER THAN INHERITED IT.** _Do not start AC-07.2 while `native/rust` must go clean_ was correct while four nodes were waiting on a clean subtree to close the gate. The gate closed; the premise died; the entry would have gone on issuing orders. **A decision that names its condition can be retired when the condition dies -- one that does not, cannot.**
- **2026-08-25 -- A `dirty-` BINARY MAY READ CANON AND MAY NEVER WRITE IT (vc's rule, and it binds me).** Reading is safe because **the STORE is the subject and is independent of the build**; writing or certifying a criterion is not, because the write is performed by the code that is mid-edit. **cc's in-flight work includes `render.rs`, which IS the canon renderer** -- so a write through a binary built over it would be done BY the half-finished renderer and would look exactly like a clean write. This is the sharper form of my own reason for not building, and it is why `AT-07.2` waits rather than being forced.
- **2026-08-25 -- A CLOSED LIST IS SAFE WHEN IT DECLARES WHY THE THINGS **NOT** IN IT ARE NOT IN IT.** This is the sweep's durable output and it makes the class checkable by READING. `install::HOOKS` declared why it was **CLOSED** -- a name reaching the filesystem can contain `../`, which is sound -- and **never what it EXCLUDED**, so nothing on the page separated _post-tool-advisory is out on purpose_ from _session-finish is out by accident_. **That single omission is why vc resolved one in a single read and the other survived three instruments.** Every other closed list in v3 declares membership, and `RECORD_TIMESTAMPS` goes further: **its test DISCOVERS the columns from the DDL rather than listing them, so a table added tomorrow is covered without anyone remembering.** **THE ESTATE ALREADY HELD THE EXACT REMEDY `HOOKS` LACKED, one file over, and nothing carried it across** -- which is the real lesson, because the pattern being present somewhere is what makes its absence elsewhere invisible.
- **2026-08-25 -- THE INVERSE OF THE `session-finish` CLASS, AND IT IS WORTH NAMING BECAUSE I WENT LOOKING FOR THE WRONG ONE.** session-finish was _canon says the name exists and the door refuses it_. The advisory hook is the mirror: **the door works perfectly and the CALLER'S guard clause can never fire.** Both leave a consumer with a hook that does the wrong thing, and **no roster check of any kind can see the second one** -- it needs someone to run the command and read which STREAM the bytes came out of. **A parity suite proves two binaries agree; it cannot prove either one is useful to its caller.**
