---
node: dc
name: DevX Claude
role: worker
session_id: 482cf2fc-6b49-4a0d-8d76-38b3c981924c
heartbeat_at: 2026-08-16 20:47Z
status: active
focus: "0043 IS CLOSED AND ITS PUBLICATION HOLD IS LIFTED (61724664) -- cc's c6aee944 closed it TWELVE MINUTES after I wrote the hold down, and I verified it rather than assuming, which took two passes because the binary was three sources stale. 0036 is now the ONLY hold: `intent upgrade` still exits 2. 0042 is closed empirically too -- the guards resolve and REFUSE a bad stamp under the real v3 binary, which is the canary that could not be run until `info` existed. THE CALLER ENUMERATION IS DONE AT LAST, ONCE, AND IT IS ELEVEN -- eight of them resolving `intent` from PATH, which is the whole explanation of why 0036's shadowing is machine-wide. That sweep found the evening's worst defect: BOTH release sidecar regenerators discarded their exit status and logged success anyway, and the dirty-tree guard is structurally blind to it because a failed regeneration produces SAMENESS, not change. Also landed: the release-lock test, `int build release --help` printing NO FLAGS AT ALL, 0042's split branch, and four more unchecked-status sites. testkit::project_fixture() DEFERRED -- its urgent reason rests on an UNTRACKED file, and the Rust tree has ten files under active edit. NOTHING OF MINE IS BLOCKED except hv on the freeze and the CHANGELOG section."
claims: [ST0056/11]
---

# DevX Claude (dc)

## D42 -- TIME. Read this before writing anything, anywhere.

**DB records have a timestamp field. That is the source of truth for time. Nothing else. Ever.** No clauses. hv ruled it four times and it was reinterpreted after three of them, twice by me inside ten minutes.

- **You never ask what time it is.** Not the OS, not `date`, not the filesystem, **and not the database either.** Asking SQLite and then writing the answer is still writing a time you obtained.
- **The stamp is applied BY the write**, at INSERT/UPDATE/UPSERT/DELETE. Read-then-write leaves a gap that two writers interleave in, so two records get stamped in the opposite order to the one they were written in.
- **hv's structural close, and it is the form to build against: NO cli or intentsvcs function TAKES a time.** Functions may RETURN times, and every one returned was set by SQLite on a record. **Direction is not symmetric -- IN is forbidden, OUT is fine**: a returned time is evidence a record was written; an accepted time is a second clock with extra steps.
- **THE RULE IS ABOUT SIGNATURES, NOT VALUES, WHICH IS WHY THIS VERSION WILL HOLD.** Every earlier statement of D42 asked where a value came from and whether its source was legitimate -- judgement calls this estate failed three times in one day from three nodes. **A time-typed input parameter is a defect by inspection.** Asking where a caller got a value is a discussion; asking whether a signature accepts one is a grep. **A signature that accepts a time is a standing invitation that gets accepted eventually**, no matter how careful today's author is.
- **The defect is always one step earlier.** Reaching for a clock means you are about to write a time into something that is not a durable record. The fix is never a better clock; it is not writing the time.
- **Not exceptions:** test fixtures; "only reading it"; **"but it came from the database"**; "it's just a label". The third fooled cc, vc and me independently -- **better provenance is not the absence of a confection.**
- **SCOPE, hv: devbin is NOT Intent** -- external, vendored, no db, does what it likes with time. My whole D42 directive resolved to a no-op, and reporting that beat inventing work to look responsive.
- **A board stamp is a label, not data, and nothing may read it as one.** The ordering that exists and cannot be fabricated is the **commit**. The `## (...)` heading is legacy, kept only because nodes parse the shipped format live.

## The truth model -- canon, held not restated

Ratified in `design.md` (D01 as reversed) and `data-model.md`, and deliberately not duplicated here, because a second home is how two copies drift. The three points that change what I DO: **the db is the durable SSOT and files are re-creatable secondary artefacts**; **the typed API is the only door in**, so conformance is by construction rather than by anyone checking; **migrations are normal** -- anything justified by "we can never migrate" rests on a constraint nobody made.

## DOING

- **0043 IS CLOSED AND I LIFTED THE HOLD (`61724664`). ONLY 0036 REMAINS.** cc's `c6aee944` implemented `info` and the `claude hook` family; verified at HEAD `304cd104` with `info`, `session-context`, `require-in-session` and `post-tool-advisory` all at **exit 0**. `critic` and `upgrade` still exit 2 -- the first fails open by design, the second IS 0036. **The verification needed two passes because the instrument was stale**: the release binary was three sources behind, including cc's new `session_hook_lockout.rs`, so I rebuilt before believing anything. **The section stays in `install.md` rather than being deleted -- the instance is closed, the class is not, and a document that erases a hold once it lifts teaches nobody why it was there.** vc's two findings that outlive the fix are in it: a blocked prompt exits the `claude` process with **0**, so the block is in-band and any wrapper checking exit codes sees success; and **`Stop` at exit 2 means "do not stop"** -- Intent's `Stop` is a bare `echo` and safe BY ACCIDENT OF ITS WIRING, so routing it through `intent claude hook` for consistency arms a third failure from the same constant.
- **0042 IS CLOSED EMPIRICALLY TOO, AND IT IS THE ASSERTION THAT COULD NOT BE MADE UNTIL `info` EXISTED.** A fixture board carrying a stamp with no trailing `Z`, committed through the shipped hook with the **real v3 binary** resolving `INTENT_HOME`: **REFUSED.** The guards resolve and enforce. That is 0042's own line 104 satisfied, built at the hook rather than at the migration so it needs no migrated fixture.
- **THE CALLER ENUMERATION IS DONE, ONCE, AND IT IS ELEVEN (`ea2eb2bb`).** vc asked twice for one person to list every consumer of `intent`'s exit codes; I am the one who said four was an undercount, and the count had already been revised four times -- one, four, "at least six". **EIGHT OF THE ELEVEN RESOLVE `intent` FROM `PATH`.** The three that do not are all in `int build release`, and they use an explicit path for an unrelated reason. **That is the whole explanation of why 0036's shadowing is machine-wide rather than per-project, and not one of the eight can tell it has happened.** Table is in 0044, with the two non-consumers recorded as traps rather than absences.
- **THE SWEEP FOUND A RELEASE DEFECT WORSE THAN THE ONE REPORTED (`7e877520`), AND THE DIRTY-TREE GUARD IS STRUCTURALLY BLIND TO IT.** Both sidecar regenerators -- `intent agents sync` and `intent claude upgrade --apply` -- fired in a bare subshell, discarded the status, and **logged success unconditionally on the next line**. A failed regeneration leaves the file carrying the PREVIOUS version, so the tree stays clean for it and the cut tags a release inconsistent with itself: **defect 1 at the top of `release_sidecars.bats`, arriving through the one door that file does not face.** Both abort now -- and aborting is right here where fail-open was right for the guards, because a release is a deliberate retryable act performed by one person watching, and a half-stamped tag is permanent. Three tests, mutation-proven: 20 pass, exactly the new three redden.
- **0044's DEVBIN INSTANCE IS FIXED (`a18010a8`), AND I SWEPT THE FILE IT LIVED IN AND THE ONE BESIDE IT (`68282648`).** vc closed their report with "I am NOT claiming `docs treeindex` is the only instance" and I had fixed the reported line without sweeping the function above it. `docs agents` propagated **by accident of position** -- last command in the function, one appended line from silence. `check critic --languages` discarded its status with `|| true`, so a CLI that could not answer produced an empty list and a `die` blaming the project's language declarations for a failure of the tool; it fails CLOSED, so the cost is a wrong diagnosis rather than a skipped check.
- ~~**0044's DEVBIN INSTANCE IS FIXED (`a18010a8`) -- vc found it in my file and it is the consumer I named.** `intent treeindex`'s status was discarded, so under a v3 surface where `treeindex` is retired both directories errored on screen and the function returned **0**, verdict and all. **The comment six lines above it had already reasoned the case out for the DIRECTORY check** -- "a typo in the config list would otherwise look like success" -- and never carried it one line down to the command doing the work. Proven both ways with stubs, nothing on PATH; the succeeding case still returns 0, because an always-red gate gets ignored and then hides the real failure.
- ~~**0043: MEASURED, WIDENED, LANDED AS THE SECOND HARD PUBLICATION HOLD (`ad46d014`), ANNOUNCED TO ALL FOUR.**~~ **The trigger is `brew install`, NOT migration.** `claude` is unimplemented as a FAMILY, so v3 refuses before it ever looks at project state: `intent claude hook require-in-session` exits **2** in an unmigrated v2 project and **2** outside any project at all -- measured on a binary I confirmed newer than `d2b8e76d` before trusting a word of it. So the blast radius is every Intent project on the machine carrying the canon hooks, and the condition to hold is **publication**. cc's "DO NOT MIGRATE THIS REPO" is right and insufficient: migration was never the door. **OPERATIONAL AND LIVE: every session in this estate survives only because PATH `intent` is still v2** -- v3 on the PATH of a shell a session runs in kills that session, unrecoverably from inside. Chain evidenced three ways (settings.json wiring with a bare `intent`; the shipped gate's own contract, `exit 0` passes / `exit 2` BLOCKS; the measured code). **Recorded as enough to hold publication and NOT enough to close: nobody has watched a session die of this, and the session that runs the test is the one that gets locked out.**
- **`int build release --help` WAS PRINTING NO FLAGS AT ALL, AND I FOUND IT BY RUNNING THE SUITE AROUND AN UNRELATED CHANGE (`0ef6e0a1`).** `usage()` printed a hardcoded `5,34p` window of its own header; the header grew and line 34 became the word "Usage:", so `--help` exited 0 having printed the title, the prose and that one word -- no flag, no example, no exit code, on the one morning it gets read. **Its own comment had predicted the drift exactly and written the prediction down instead of removing the coupling.** Anchored to the comment block's real boundaries now. The existing test caught it only because `--dry-run` was one of three hand-picked strings; the new one **derives the flag set from the parser's own case arms** and refuses an empty extraction.
- ~~**0043 (vc, CRITICAL) IS UPSTREAM OF PUBLICATION AND I HAVE TAKEN THAT HALF.**~~ **-- superseded by the measurement above.** Original framing kept for the chain that made it mine: `.claude/settings.json` wires `UserPromptSubmit` to `intent claude hook`, **Claude Code reads exit 2 as BLOCK THIS PROMPT**, and v3 answers unimplemented commands with 2 since `d2b8e76d` -- so **a migrated project refuses every prompt, and the refusal is self-sealing**: `/in-session` needs a prompt, and the sentinel path the gate would print is replaced by v3's not-implemented message. **The chain that makes it mine: 0036 shadows a v2 install machine-wide without asking, so a user meets this in a project they were not thinking about, and following the remedy costs them the session they would recover with.** 0036 gives a bad first contact; **this gives a lockout.** Goes into `install.md` as a hard publication hold beside 0036. **vc has NOT observed it live and said so; the confirmation wants a throwaway project and a spare session, and cannot be run here -- this repo is unmigrated by design and my session is the one that would die.**
- **0042 IS FIXED (`6e7812fa`) AND RECORDED AS PARTIAL (`8c5c7a7f`) -- IT STAYS OPEN.** The two absences are two branches now: resolution failure is detected ONCE before the loop, names every skipped guard, quotes the CLI's exit code (captured without a pipe, so `$?` is the CLI's), attributes the cause to the resolver, and points at 0036/0043. Per-guard warnings are now reached only with `INTENT_HOME` resolved, so when one fires it is telling the truth. **I DID NOT DO WHAT THE ISSUE PROPOSES AND SAID SO IN IT: it asks for the dead-resolver case to BLOCK, and that gate is 0043 rebuilt on the git side** -- triggered by `brew install`, during the exact migration window, turning a quiet control failure into an estate-wide commit outage. Fails open and says the true thing loudly instead; hv's call, three lines if they want the other one, and the tests pin both directions. **Still open and neither is mine: v3 implementing `info`/`critic`/`claude hook`, and removing the parse-the-display-output coupling rather than making its failure legible.**
- ~~**0042 IS MY FAIL-OPEN AND I HAVE CORRECTED vc's WORDING IN BOTH DIRECTIONS.**~~ Ran it rather than reading it: with `intent info` unimplemented the resolution is empty and **BOTH whiteboard guards stop enforcing and the commit proceeds** -- but **not silently**; each names itself and prints `looked in: /lib/templates/...`, whose bare leading `/` is the self-identifying symptom. **The defect is mine and it is one branch: "the guard file is absent" and "the tool that locates guards is absent" are different absences, and my `else` cannot tell them apart.** First is benign, second means nothing is enforcing anything -- **0043's own finding one directory over, an absence treated as a property of the GUARD when it is a property of the RESOLVER.**
- **THE RELEASE-LOCK TEST IS LANDED (`191bdb01`).** Hold released the right way: `ps` clean, all four peers paused, then refactor + 9 tests in ONE commit because the bats file sources the function out of the script. Re-mutated against the REFACTORED shape rather than trusting the earlier run -- baseline acceptance still PASSES on the mutant (so it is a mutant, not a broken file) and the four regression cases redden, the dependency-bump one emitting nothing where it must name `serde`. `INTENT_RELEASE_SCRIPT` now redirects both release bats files at another copy, so either can be re-mutated without editing what ships.
- ~~**THE RELEASE-LOCK TEST: WRITTEN, PROVEN BOTH WAYS, STAGED IN SCRATCH, HELD ONLY ON hv's SUITE RESULT.**~~ `scratchpad/cut30/` holds the refactor extracting `lock_nonmember_changes()` plus 9 bats tests. **Mutation run with the precondition verified: against a mutant carrying the pre-`b8787ffc` semantics tests 1-4 and 9 still PASS -- including the baseline acceptance case, so it is a mutant and not a broken file -- and 5-8 go RED.** Lands in `release_sidecars.bats`. **hv reported the RUST suite 100% green at 19:3xZ, and that does NOT release this hold** -- the addition is a bats file, so the shell suite is the one that could be reading it, and its result is unstated. **A green from the other half of the suite is the shape that reads exactly like clearance**, which is the same class as clearing prettier twice from the wrong instrument. **First act on the bounce: confirm the shell suite is not running, then land refactor + 9 tests together.**
- **TAKEN FROM vc's REVIEW: `testkit::project_fixture()`.** Nine test files hand-spell a v3 `config.json` in two spellings with five project names. **Not a bypassed home -- a missing one.** The urgent reason is vc's second: **no fixture anywhere carries a non-default `st_prefix`, which is exactly why 0040 had nothing that could catch it.** Queues behind the release-lock test.
- **AWAITING hv, BOTH FROM THE CUT-PATH WALK, NEITHER ROUTABLE AROUND ME.** (1) **The upstream freeze and v3 are in structural conflict** -- the tap formula points at a GitHub asset, so there is no shipping v3 without pushing the frozen remote; sent as the narrow question, lift for the cut itself, since the freeze was called on cost per commit and a release is not a commit. (2) **No `## [3.0.0]` CHANGELOG section**, the first gate to fire; **I refused to create a placeholder** -- it would have carried the dry-run further and looked like progress while turning a loud correct refusal silent.
- **`brew services` waits on WP-08. hv holds the toolchain-pin decision.**

## TODO

1. **WP-11 (Distribution) -- MINE, WIP, 2/4. NOTHING LEFT THAT I CAN BUILD.**
   - **BUILT AND PROVEN: `int macos <doctor|stage|sign|notarize|verify|checksum|prepare|formula|publish|env|store-creds>`.** Signing acts on STAGED copies in `target/dist`; `prepare` runs the pass. `publish` uploads, **re-downloads what it uploaded and hashes THAT**, and only then ships the formula; on a mismatch the release stays and no formula ships. Four refusals canaried both ways.
   - **UNEXERCISED SURFACE IS ONE CALL, `gh release create`.** Closed since: formula is valid Ruby and **lints clean at a tap path**; `curl -fsSL` follows redirects, hashes identically twice, 404s without writing; **tap clone, `Formula/intent.rb` write and commit all succeed against the REAL tap, nothing pushed.** No release here has ever carried an asset, so the upload cannot be rehearsed without publishing.
   - **AC-11.1 + AC-11.4 ARE BLOCKED ON A PUBLICATION, AND THE BLOCKER HAS NOW BEEN WRONG ON THIS BOARD THREE TIMES.** Not the tap (live since 2026-08-15T15:19:58Z, deliberately empty, one README saying why -- verified again 19:2xZ, `isEmpty:false` means "has a commit", NOT "has a formula"); not "a real version and nothing else" (necessary, not sufficient); and no longer 0038 (cc fixed it at `d2b8e76d`). **The live set is: 0036, 0043, the `## [3.0.0]` CHANGELOG section, and hv's freeze call.** The mechanism existing is not the criterion -- **a better-built mechanism is no more a satisfied AC than a built one was.**
   - **HELD until WP-12: `int build release` gaining `Cargo.toml` to its sidecar sync** (wiring it now makes a v2 release stamp its version into `Cargo.toml`). **`brew services` BLOCKED on WP-08.** ~~Install/upgrade docs still owed~~ **-- WRITTEN, at `intent/st/ST0056/install.md`, and it leads with the brew-SHADOWS warning. This TODO was stale and I only found that by going to write the thing.**
2. **Issue 0036 -- `brew install` SHADOWS a v2 install.** brew is PATH position 1, the v2 symlinks are 17 and 19. A v2 user meets the v3 unmigrated-project refusal without asking for anything, and its remedy names `intent upgrade`, which v3 lacks. `migration.md:3` says the migrator IS that verb, so it is WP-10 unbuilt. **vc ruled the control belongs on the BINARY as a build-time invariant, not in `publish`** -- so it is theirs, and WP-11 stays decoupled from WP-10.
3. **MEASURED CLEAN, do not re-investigate: `int prepush` 19s; warm `cargo test --workspace` 22s for 331 tests.** Both were my guesses at the friction and both were wrong. **ic asked me specifically NOT to optimise `int build cli` (25-37s) -- that cost buys correctness**, after a 14-minute-stale binary reported findings cc had already fixed and read as a regression.
4. **`testkit::project_fixture()` -- DEFERRED, AND NOT FOR THE REASON I QUEUED IT.** vc's urgent justification was that no fixture carries a non-default `st_prefix`. `retired_st_prefix.rs` now does -- but it is **UNTRACKED**, a peer's in-flight work, so it is not evidence about the repo and I nearly reported it as one. What remains is a Highlander tidy-up over 12 test files in two crates, and the Rust tree has ten files under active edit tonight. **Wrong time, low urgency, high conflict; hand it to cc rather than reaching into their tree.**
5. **Issues 0030 / 0031 (`intent upgrade`) -- DEFERRED under DEFAULT-DEFER.** 0030: backup dirs stamped in LOCAL time, so oldest-first retention deletes the newer artefact across a DST fall-back. 0031: `--backup-dir` basenames straight into `.backup/`.
6. Open for others: **`intent/.cache/`** (cc, D21); **`core.hooksPath`** (deferred, collides with `intent claude upgrade`); the **`bin/` boundary** (hv); ~~AC-12.1's wording~~ **RULED 2026-08-15: vc adopted my rewording essentially verbatim -- _"nothing in the repo EXECUTES or EMITS a `bin/` intent script path"_ -- with the four classes named in the row and the 133 historical records marked explicitly OUT OF SCOPE.** The parity-fixture question is recorded as a WP-12 call rather than answered.

## Watch-outs

Facts about this estate, not reminders. Everything amounting to "remember to" is worthless here -- three nodes broke rules they had personally written, on the day they wrote them.

- **COMMIT BEFORE ANY `intent at` STATUS CHANGE (vc's announce, 2026-08-16). `intent at red|green|na` DESTROYS THE ROW'S NOTE** -- issue 0033, measured at 1,560 bytes down to 106 on one row, and **`intent at lint` says `ok` immediately afterwards**, so the contract's own linter cannot see it leave. **The transition graph doubles it: `to-write -> green` is refused, so recording a passing test costs TWO rewrites.** 14,253 characters stand in front of a status change across 34 rows. **This is mine to hold because I move AT rows for WP-11.** A committed note is recoverable with `git show`; one written and moved in the same session is gone.
- **MY SHELL IS zsh, AND zsh DOES NOT WORD-SPLIT AN UNQUOTED `$var`. IT BIT TWICE IN ONE EVENING AND BOTH TIMES IT LOOKED LIKE A FINDING ABOUT SOMETHING ELSE.** `for t in "test rust"; do bin/int $t; done` passed `test rust` as ONE argument, so four gates reported `exit=2 / unknown command` and I nearly recorded four broken gates with the wrong signature. Then `git commit --only $PATHS` sent the whole list as a single pathspec. **The mechanism: never expand a list through an unquoted scalar -- write the arguments out, or use an array.** The bash idioms in this estate's own scripts are not portable to the shell I type into.
- **THE ONLY macOS CHECK THAT MEANS ANYTHING IS A QUARANTINED COPY.** `stapler` reports no ticket on a bare Mach-O -- correct, there is nowhere to put one. `spctl -a -t exec` says "does not seem to be an app" -- correct, that policy is for bundles. And **`codesign --verify --strict` RETURNS 0 ON AN AD-HOC BINARY WE DID NOT SIGN**, because an ad-hoc signature is a valid signature. Use `spctl -a -t open --context context:primary-signature` on a copy carrying `com.apple.quarantine`.
- **`target/release/` IS SHARED MUTABLE STATE -- WHICH IS WHY NOTHING SIGNS THERE ANY MORE.** A peer's `cargo build --release` silently replaced a Developer ID signature with the linker's ad-hoc one and de-notarised a shipped binary inside the hour, with no signal and every artefact of the proof still reading as valid. Fixed structurally: `stage` copies to `target/dist` first. **Never sign anything in a directory someone else writes.**
- **SIGNING MUTATES THE BINARY; NOTARISATION DOES NOT. SIGN BEFORE YOU CHECKSUM.** A sha256 taken one step early **does not fail for us -- it fails for every `brew install`**, where we have the least visibility.
- **A CANARY PROVES NOTHING UNTIL YOU CONFIRM THE FIXTURE REACHED THE BRANCH.** I planted a stale sums file on already-notarised artefacts, so the command correctly PASSED and my check reported "BUG" about a branch that never ran. **A red-looking result from a green run reads exactly like a real defect** -- the direction nobody watches, because it feels like diligence working. Canary both ways.
- **GIT HANDS A HOOK A TEMPORARY INDEX DURING A PARTIAL COMMIT, AND REWRITES THE REAL ONE AFTER THE HOOK RETURNS.** `GIT_INDEX_FILE=.git/next-index-<pid>.lock`. So a pre-commit hook's `git add` correctly reaches the commit and CANNOT correct the real index -- anything it writes there is discarded. **This is why issue 0028 has no pre-commit fix**, and I know because I built one and measured it doing nothing.
- **MY OWN WATCH-OUTS ARE NOT CONTROLS AND I SHOULD STOP EXPECTING THEM TO BE. FOUR BROKEN IN ONE AFTERNOON, TWO WITHIN THE HOUR OF WRITING THEM.** A **masked exit status** -- `$?` after `binary | head` is the PIPELINE's, so I recorded the v3 refusal as `exit=0` and started composing an issue around a silent failure that does not exist (it is 1). **awk over a fixed-column format** -- `$1` collapses `git status --short`'s leading space, so a worktree-only ` M` reads as staged; it inflated an index count 9x. **A fabricated board stamp** -- `17:52Z` composed from context while the clock read `18:56Z`, and it carried a `Z`, landed in the past and increased monotonically, so **it would have passed all three guard checks.** **Each needed a mechanism and got a paragraph.** The mechanisms: capture output then read `$?`; read `${line:0:1}` for the index column, never a field splitter; **read the clock into a shell variable and SUBSTITUTE it -- a stamp I type is a stamp I composed.** Fifth, 2026-08-16, inside a test I wrote to catch exactly it: the disabled-gate extractor's needle was `[a-z-]`, so my own first mutation -- a gate named `clippy2` -- was **silently not seen**, and the test passed over the change it exists to catch. **A needle that skips the names nobody has invented yet is the allowlist lesson, one file over, on the same day.**
- **A FILE WITH A KNOWN DEFECT MAKES EVERY ADJACENT THING LOOK LIKE THAT DEFECT.** I had the sentence written filing a v2-path leak from `transitions.rs:264` -- the file AC-00.9 named for leaking PM state -- **and the field sits on the `Unbuilt` variant with ZERO read sites.** Find the read site before naming a leak.
- **A `case` GLOB AND A GIT PATHSPEC BOTH CROSS `/`, SO `a/*/c.md` IS NOT ONE LEVEL DEEP -- IT IS EVERY LEVEL.** Measured here 2026-08-16, because I did not believe either without running it: `git ls-files -- 'intent/whiteboard/*/wip.md'` matched **21** files, **SIXTEEN** of them under `.history/`; with `:(exclude)intent/whiteboard/*/.history/**` it matched 5. `**` returned the identical 21, so the two are equivalent at this shape and the `*` reads narrower than it is. **It fails in the over-refusing direction, which is the one that gets a guard bypassed** -- and it had already bitten the clock guard's port, where the trailing `**` on the exclude was dropped and the false-positive control did not catch it because that control's own fixture was missing. Never write a `*` in a path pattern meaning "one segment".
- **`--date=format:` IGNORES `TZ`; `--date=format-local:` RESPECTS IT.** `TZ=UTC git log --date=format:` prints LOCAL time with a `Z` appended -- wrong by exactly the offset and looking perfect. `git log` is local by default and is the usual source of that error.
- **MY BOARD IS A MEMO; THE AC IS THE CONTRACT -- TWICE NOW, AND THE SECOND WAS FOUND BY OBEYING THE FIRST.** My board's TODO stated what remained on WP-11 and did not mention AC-11.3 at all; `acceptance.md` had AT-11.3 `to-write`, naming a file that did not exist, in my own work package. **A board says what I was thinking when I wrote it; the contract says what was agreed.** The first instance: AC-11.4 already ordered the staged-copy restructure, conditional on hv ruling the matrix -- which hv did. I re-read my board instead and rebuilt an agreed case from scratch. **A deferral recorded in two places has its precondition met in only one, and the copy you re-read on a bounce is the wrong copy.**
- **A GUARD MUST BLOCK ON WHAT THE COMMIT ADDS, NOT ON THE WORKING TREE.** `provenance_check.sh` globbed the tree, so one node's untracked mid-generation file froze every node's commits on paths they had never touched. **A guard that must be bypassed is a guard nobody keeps.** Hold the commit and diagnose; never reach for `--no-verify`.
- **`--only` COMMITS WHAT YOU NAME, AND `--amend` IGNORES IT ENTIRELY.** A move is two facts; naming the new path leaves the deletion staged. And `--amend` with no pathspec re-commits the WHOLE INDEX -- ic's swept 19 files including peer boards. **A sweep does not move a file, it SPLITS A CHANGE**: a method and its test are one unit, each half reads as finished alone, and HEAD stopped building while every worktree stayed green. **Verify at HEAD with `git ls-tree`, and read `git status --short`, not the diff.**
- **NEVER mutate `bin/**` or `tests/**` in place.** `~/.local/bin/intent` AND `~/bin/intent` both symlink to `bin/intent`; several sessions are live. Sacrificial worktree only. **`bin/.devbin/**` and `native/**` are safe** -- and devbin is vendored external code, not Intent's.
- **THE FRICTION cc HAS STOPPED NOTICING, IN THEIR WORDS: FOUR OF US COMMIT INTO ONE CLONE**, so their worktree changes under them while they work and a peer's push lands between their two pushes. **`int postcommit` and the `prepush` range defect are both in that family** -- shared-clone concurrency, not speed. Worth treating as a class rather than as incidents.
- **NEVER `git pull --rebase` IN THIS SHARED TREE.** Push; if rejected as non-fast-forward, coordinate. A peer's `.git/index.lock` means a peer is running git: WAIT, never remove it.
- **THIS REPO IS A v2 PROJECT AND THE v3 BINARY REFUSES HERE BY DESIGN.** Every measurement taken with the v3 binary inside this tree measures the refusal path. `int dogfood` exists so there is a v3 project to measure against.
- **A control refuses; documentation reminds; only one is load-bearing.** Anything I can obey only by concentrating is an unfixed defect, not a discipline.
- **PUSH `local` ONLY. `upstream` IS CLOSED -- hv, 2026-08-16: the CI/CD budget has been hit. NOW ENFORCED, so this entry is a description rather than a reminder.** Every push to `upstream` (GitHub) triggers the rust + tests workflows, so the habit this estate has drilled into all four of us -- push both remotes, always -- now spends money on every commit. **`local` is a Dropbox path and costs nothing.** `cmd/prepush` REFUSES a push that reaches a remote in `FROZEN_REMOTES`, by name and by URL; `INTENT_ALLOW_FROZEN_PUSH=1` is the one-off override, and lifting the freeze is deleting the line and committing it, so the lift lands in the history beside the freeze. Verified on a real `git push upstream main --dry-run`. **I wrote this watch-out, announced it to three peers, and then built the control anyway -- because the reversal of a drilled instruction is the exact case where reading it changes nothing.**

## Decisions

Standing only. The day's full set is in `.history/20260815/wip.md`.

- (2026-08-15) **A TOOL GIVING A REAL-LOOKING ANSWER USUALLY MEANS IT WAS ASKED IN THE WRONG CONTEXT -- CALIBRATE AGAINST SOMETHING KNOWN GOOD BEFORE BELIEVING IT.** Three instances in one day: `spctl -a -t exec` calling a correctly signed binary rejected; awk on a fixed-column format; `brew style` reporting five offences on a correct formula, four of them the generic Ruby config applied outside a tap. **Each was answered in ONE command by running the tool against something already shipping.** Extended 2026-08-16 and this is the sharper form (vc's wording, from two independently-wrong-then-corrected setups): **A NEGATIVE RESULT FROM AN UNVERIFIED INVOCATION IS NOT EVIDENCE, AND IT IS THE SHAPE THAT FEELS MOST LIKE EVIDENCE, BECAUSE NOTHING FAILED.** I cleared prettier twice from the wrong instrument -- `--prose-wrap never` (devbin's flags, not the hook's), then `npx` rather than the PATH binary -- and both said UNCHANGED. Right answer, wrong instrument, error invisible in the result. Same day: bare `rustfmt` without `native/rust/rustfmt.toml` called a clean HEAD unformatted. - (2026-08-15) **"NOTHING REFERENCES X" IS FOUR DIFFERENT CRITERIA WEARING ONE SENTENCE.** Executes, emits, cites as provenance, records as history -- only the first two are defects, the third is load-bearing documentation and the fourth must never change. **A criterion that does not distinguish them gets satisfied by deleting the record.**
- (2026-08-15) **A MUTATION RUN THAT ONLY CONFIRMS ITS OWN HEADER WAS NOT A MUTATION RUN.** Mine deleted a test of mine: "never modifies the working tree" could not be made to fail, because on a path whose worktree already equals HEAD, `git reset` and the destructive `git checkout HEAD --` are observably identical -- **the guard removes the difference, so the test restated the guard's consequence and read as coverage.** Also: an unapplied mutation reports "nothing failed", which is indistinguishable from a test that does not check. Verify the fixture took.
- (2026-08-15) **REVERSIBLE BY CONSTRUCTION EARNS A CONTROL THE RIGHT TO ACT RATHER THAN REPORT.** The clock guard deliberately never auto-corrects, because the NODE made that error and needs to learn it. Issue 0028 is the opposite case -- the TOOLING makes it, on every markdown commit, and eight measured instances were reported and none cleared. **A sweep that prints the blob sha of everything it removes destroys nothing**, so the argument against acting does not apply.
- (2026-08-15) **TO ASSERT WHAT A USER RECEIVES, BE THE USER FOR ONE ROUND TRIP.** AC-11.4 is the published hash matching the published bytes; hashing the local file asserts that about a file nobody fetches. Publish re-downloads what it uploaded. **The cost is one download and it removes the class that never fails for us and fails for every `brew install`.**
- (2026-08-15) **AN ALLOWLIST FORBIDS EVERYTHING ITS AUTHOR DID NOT THINK OF; A NEEDLE LIST FORBIDS ONLY WHAT THEY DID.** AT-11.3 asked for `INTENT_HOME`. Asserting the read set is exactly `{COLUMNS}` costs the same and catches the variable nobody has invented yet -- **including one added by a command that is not implemented today**, which is where the risk actually is, because the unwritten commands are the ones that will want a home.
- (2026-08-15) **A STRUCTURE THAT CANNOT FAIL BEATS A CONTROL THAT CATCHES THE FAILURE, AND "RECORDED AS THE BETTER SHAPE, DELIBERATELY UNBUILT" IS USUALLY DEFERRAL WEARING A REASON.** I shipped the refusal and left the design written down. A refusal detects the race only at the END, after a notarisation round trip has been spent on bytes that no longer exist. Building it took under an hour.
- (2026-08-15) **THE DEFECT IS OFTEN THE GAP BETWEEN CORRECT STEPS, NOT ANY STEP.** Four individually-correct macOS subcommands, run by hand with a multi-minute wait in the middle. A note saying "run these as one sequence" is a reminder; `int macos prepare` is the sequence. **Second instance 2026-08-16, and worse: nothing could cut a v3 release at all.** The cut creates tag AND release object; publish demanded tag-present-release-absent. Each command correct, each refusal right, composition impossible -- **and it was hiding in the one deliverable nobody had written, so it would have surfaced on the morning of the cut.** Now D43.
- (2026-08-15) **REFUSING TO SETTLE BY INFERENCE IS NOT A RESTING STATE -- IT OBLIGES YOU TO GO AND GET THE ANSWER** (vc's). An open question parked across three rulings is a decision made by default. **Three independent stops is one alarm, not three data points.**
- (2026-08-15) **MEASURE BEFORE YOU DESIGN, AND LOOK FOR THE ESTATE'S EXISTING ANSWER FIRST.** A well-formed-feeling question can rest on an unruled premise and name a tool that cannot do the job. Conflab had shipped the answer for four months.
- (2026-08-15) **A WRONG ARTEFACT IS NOT A NEUTRAL PLACEHOLDER -- IT MAKES A CONFIDENT FALSE STATEMENT.** A formula pointing at a nonexistent release reads as "the tap is broken" rather than "the release is not out yet". **An empty tap says the true thing.**
- (2026-08-15) **AN AC NAMES THE OUTCOME; THE MECHANISM BELONGS IN THE WORK PACKAGE** (vc's). A criterion naming a tool can be invalidated by a measurement of that tool while the thing the project wanted is still achievable.
- (2026-08-15) **THE CONTROL GOES WHERE THE HARM IS.** A dev-version formula printed to a terminal harms nobody; the same bytes in a tap install a build nobody meant to ship.
- (2026-08-15) **A PEER CANNOT AUTHORISE WHAT A HARNESS REFUSED**, and a peer performing it on your behalf launders the refusal.
- (2026-08-16) **A REFUSAL MUST TEST THE INVARIANT ITS OWN COMMENT STATES** (mine, promoted by vc above the release mechanics). `publish`'s comment said the danger was overwriting the **assets** of a release a formula points at -- an invariant about BYTES. The check tested EXISTENCE. **The comment was right and the code was stricter, and a refusal that is too strict looks like rigour, so no reader questions it.** Same family as a false doc comment disabling the guards around it: prose and mechanism disagreeing, with one of them believed. **Nobody diffs a comment against its check.**
- (2026-08-16) **IN A FOUR-SESSION SHARED CLONE, AN UNTRACKED FILE HAS NO AUTHOR.** `git status` gives a path, `stat` gives a time and not a node, and the working tree is the one surface all four write to at once. vc read my uncommitted `testkit`, attributed it to cc, and told us both. **Reading the tree is right for WHAT and structurally silent on WHO -- only a commit carries authorship.** And the near-harm ran quiet: the instruction was DO NOT BUILD, so obeying it would have produced no error, just two nodes each waiting for the other.
- (2026-08-16) **AN ALWAYS-RED AGGREGATE IS WORSE THAN A SLOW ONE.** A slow gate gets `--no-verify`'d; an always-red gate gets ignored, and the real failure then hides inside the noise. `int check all` failed on every run for a reason that could never change. **And the fix is not to make it green -- it is to remove the gate that does not apply, and send the underlying decision to whoever owns it.**
- (2026-08-16) **A DEFECT THAT SELF-REPAIRS LEAVES NO CORPSE; ONE PRODUCED BY COMPETENCE DOES NOT SELF-REPAIR AT ALL** (vc, inverting my own reasoning against me). The NOT-YAML ruling has two failure directions: a node writing INVALID YAML self-repairs, because the next reader sees something broken. **A node writing VALID ESCAPED YAML does not, and cannot, because nothing about it looks wrong.** Under a formatter-quirk diagnosis one occurrence is evidence of rarity; under "the author is any competent node" it is one OBSERVATION of default behaviour. **The diagnosis decides whether one instance means defer or ship.**
- (2026-08-16) **THE FIRST COUNT MEASURES WHAT THE FINDER WAS LOOKING AT, NOT WHAT THE FINDING IS ABOUT** (vc's, on the third count corrected in two days). cc said four `repo_root()`, a table said five, the truth was nine across two predicates under two names. **A number in a finding gets inherited and nobody re-derives it.**
- (2026-08-16) **A MUTANT THAT CANNOT REFUSE ANYTHING IS NOT A MUTANT -- AND AN EMPTY FILE PARSES.** My mutation harness checked only that the file CHANGED and that it still parsed, then reported two ESCAPED exemptions. Both were a BROKEN GUARD, not a removed exemption: one blanked an array element (leaving an empty pathspec that matched nothing) and one hit a sed delimiter collision that truncated the script. **A broken guard passes every false-positive control by construction, so the harness reported the strongest possible finding from the weakest possible evidence.** The fix is one precondition -- the mutant must still refuse the BASELINE case before its verdict on an exemption means anything. Same family as the fixture-must-reach-the-branch rule, one level up: **the thing checking the check needs checking too.**
- (2026-08-16) **DELEGATE, DO NOT COPY -- DOGFOODING A COPY IS NOT DOGFOODING.** devbin's repo-local guard now calls the shipped canon guard rather than holding its own version. A divergence between what we enforce here and what consumers receive cannot happen if there is one file. **And it was not hypothetical: the copy had two defects the port fixed**, so for a day this repo was enforcing something stricter than what it would have shipped.
- (2026-08-16) **A CHECK A COMMENT CAN SATISFY IS NOT A CHECK.** My installer's rewrite matched any line containing `bin/int <runner>`, so it edited the generated hooks' own PROSE -- `# The runner is bin/int precommit ...` became an invocation mid-sentence -- and on a hook whose only mention was a comment it would have reported the line updated with no invocation in the file at all. **Anchor a structural test to structure**: `^[[:space:]]*bin/int <runner>` cannot be satisfied by prose. Same family as a guard roster naming what it does not run, and as the AT reference that was "whatever sits inside the first pair of backticks".
- (2026-08-16) **AN INSTALLER THAT ONLY ASKS "IS IT WIRED" CAN NEVER UPGRADE ANYTHING, AND SAYS `unchanged` WHILE MEANING IT.** Two states where there are three: absent, wired-by-an-older-generator, current. The middle one is where every existing clone lives, and **`.git/hooks` is never tracked, so there is no second mechanism that would catch up** -- each clone keeps the line it was born with, forever, and the report is confidently green. **A state PROBE, not a presence test**, which is the estate's own upgrade philosophy one directory over.
- (2026-08-16) **A FOLD THAT ENUMERATES AND THEN ARCHIVES HAS A WINDOW IN IT, AND ON A FOUR-NODE BOARD THE WINDOW IS WHERE THE ANNOUNCE LANDS.** My localfold script counted my inboxes, archived what it found, and restored the sentinel. **Four messages arrived between the count I verified and the archive that ran, so it filed two of vc's -- including a CRITICAL announce -- without my having read either.** Caught only because the archived count disagreed with the one I expected; recovered from history. **"Read before you move" is not a discipline that survives this, because the read and the move were minutes apart and the board changed in between: the read and the move have to be the SAME ACT.** Same shape as verifying a premise at the moment you act rather than when you queued the action -- **which I have written down, and it did not save me, because I never thought of an inbox count as a premise.** **vc's addition, and it is the part neither of us said first: the CHECK WORKED.** Every instance of this on their board was caught the same way and nothing was ever lost -- **`.history/` capturing rather than deleting is doing more work than either of our disciplines**, which is the argument for reversible-by-construction over remembering.
- (2026-08-16) **"THE GUARD IS ABSENT" AND "THE TOOL THAT LOCATES GUARDS IS ABSENT" ARE DIFFERENT ABSENCES, AND ONE `else` BRANCH CANNOT TELL THEM APART.** My pre-commit roster fails open when a guard file is missing, which is right -- a guard that must be bypassed is one nobody keeps. **But when `intent info` is unimplemented the resolution is empty and EVERY guard is missing at once, so the same branch reports two benign-looking warnings and enforces nothing.** Benign absence and total absence take the same path. **The general form is 0043's: an absence treated as a property of the THING when it is a property of the RESOLVER.**
- (2026-08-16) **A BINARY 57 SECONDS STALE REPORTS THE OLD BEHAVIOUR AS CONFIDENTLY AS ONE 14 MINUTES STALE, AND NO HABIT CATCHES THE GAP.** I was one command from measuring 0038 against `target/release/intent` and filing a false regression against cc, who had fixed it a minute earlier. **What saved it was comparing the binary's mtime to the FIX COMMIT'S timestamp before trusting anything it said** -- not a rule about rebuilding, a check on the instrument. ic hit this at 14 minutes and it read as a regression; the margin does not matter, because staleness is invisible in the output either way. **Cheap and mechanical: `stat` the binary against `git log -1 --format=%ct <fix>` before believing a measurement about that fix.** It also settles ic's ask not to optimise `int build cli` -- that cost is what keeps the instrument honest.
- (2026-08-16) **AN EMBEDDED PIPELINE CAN ONLY BE GREPPED, AND A GREP CANNOT TELL WHETHER WHAT IT MATCHED IS CORRECT.** The lock refusal survived broken because it was inline: the existing tests around it are structural greps, on the stated grounds that the release script cannot be run end to end. **True of the script and false of its parts** -- the comparison is pure over two files, so extracting it made a real behavioural test available where only a grep had been. **"This cannot be tested end to end" is a fact about the whole and gets applied to the pieces, which is where the untested defect then lives.**
- (2026-08-16) **TWO THINGS WRITTEN IN THE SAME BREATH BY THE SAME AUTHOR AGREEING IS NOT EVIDENCE, AND IT DEFEATS THE CHECK I RECORDED TWO DAYS AGO.** My lock refusal's comment said "every changed line must be a version line" and the code tested exactly that. **Prose and mechanism agreed perfectly, and the INVARIANT was wrong**: a dependency bump is a lone version line, so the check accepted the one change it exists to refuse. My earlier Decision -- a refusal must test the invariant its own comment states -- **finds the case where the comment is right and the code is stricter, and is blind to the case where both are wrong together.** The only thing that caught it was running the refusal against a case it should refuse. **Diffing a comment against its check tests agreement; only a canary tests truth.**
- (2026-08-16) **A CONTROL WORKING EXACTLY AS DESIGNED CAN BE THE THING BLOCKING THE GOAL, AND THAT IS NOT AN ARGUMENT FOR AN EXCEPTION.** My frozen-remote pre-flight correctly refuses a v3 cut, and **v3 cannot reach a user without pushing the frozen remote** -- the tap formula points at a GitHub asset and GitHub is `upstream`. The wrong move is a quiet exception in the tool; the right one is naming the narrow decision for whoever set the policy. **The freeze was called on cost PER COMMIT, and a release is not a commit** -- which reframes it from "override my control" to "does the policy's own reason cover this case".
- (2026-08-16) **A PLACEHOLDER THAT SATISFIES A GATE CONVERTS A LOUD CORRECT REFUSAL INTO A SILENT ONE.** An empty `## [3.0.0] - in progress` heading would have carried my dry-run further down the cut path and read as progress. It would also have made the CHANGELOG gate pass while the release notes were still absent. **Getting further is not the same as getting closer**, and the tap-stays-empty argument is the same one: a wrong artefact makes a confident false statement where an absent one says the true thing.
- (2026-08-16) **AN AGREEMENT BY COINCIDENCE IS WORSE THAN A DIVERGENCE, AND IT IS THE ONE NOBODY FILES.** vc's four instances were all divergences -- the canon declared a thing the binary did not do -- and a divergence eventually meets a user. `Arg.default` is the fifth and it MATCHES: `intent todo` bare runs `list` exactly as declared, hand-written, with no mechanical connection to the declaration. **A divergence has a corpse; a coincidence has none, and it drifts the first time either side is edited by someone who never knew the other existed.** Seven of the eight rows are on unbuilt families, so the coincidences are still to be created, one hand-written default at a time.
- (2026-08-16) **A GREP FOR A MECHANISM COUNTS ITS PROSE.** `grep deny_unknown_fields dispatch.rs` returned **three** and I had a sentence written about which structs were strict; all three hits are inside the doc comment EXPLAINING WHY THE FILE HAS NONE. Zero structs there are strict. **The better-documented a decision is, the more its grep looks like an implementation** -- and this file is the most carefully-commented in the tree, which is precisely why it read as three. Inverse of "a check a comment can satisfy is not a check": there prose satisfied a structural test, here prose inflated a mechanism count. **Same defect, opposite direction, and both are counting the wrong thing because prose and mechanism share a spelling.**
- (2026-08-16) **A MECHANISM I WROTE FAILED BECAUSE IT WAS INCOMPLETE, WHICH IS THE FAILURE MODE OF A MECHANISM RATHER THAN A REMINDER, AND IS WHY IT IS FIXABLE.** Masked exit status, fourth instance. My recorded mechanism was "capture output then read `$?`" -- and I DID capture, into `out="$(cmd | head -2)"`, so `$?` was `head`'s and four commands reported `rc=0` while every one exited 1. **The captured command was itself the pipeline, which the mechanism as written does not exclude.** Corrected form: **capture WITHOUT a pipe, read `$?`, then trim.** A reminder would have been broken for the fifth time; a mechanism gets one more clause and stops failing.
- (2026-08-16) **A SENDER CANNOT TELL "NEVER SENT" FROM "SENT, READ, ARCHIVED" BY LOOKING AT THE INBOX.** I checked whether my 14:09Z ask to vc existed and found `_(empty)_`, which reads exactly like a message I only believed I sent. It was in `vc/.history/20260816/`, delivered and folded by them. **The live file is the recipient's workspace, not a sent-items record**, so the sender's only evidence is the recipient's history or the commit. Verify in history before re-sending -- a duplicate ask reads as pressure.
- (2026-08-16) **A GUARD THAT WATCHES FOR CHANGE IS STRUCTURALLY BLIND TO SAMENESS, AND THE THING IT GUARDS CAN FAIL BY NOT HAPPENING.** The release's dirty-tree check exists to stop an internally inconsistent tag. Both sidecar REGENERATORS discarded their exit status -- so a failed regeneration leaves the file carrying the previous version, **the tree is CLEAN for it**, and the cut proceeds. The guard is looking for an unexpected diff; this failure produces an unexpected absence of one. **The tests I had just extended assert the stamp happens BEFORE the commit and cannot assert that it WORKED** -- ordering was the fix everyone remembered, so ordering is what got tested. **Whenever a control asks "did something change", ask what it does when the answer is legitimately no and should not have been.**
- (2026-08-16) **A GREP THAT RETURNS ZERO IS A CLAIM, AND CALIBRATING MINE AGAINST A HIT I HAD ALREADY READ WITH MY OWN EYES TURNED ZERO INTO FOUR.** Asking which consumers invoke `intent` by explicit path rather than through `PATH`, I got no hits and nearly wrote "all of them use PATH" -- with a call site I had read minutes earlier sitting in the file. The pattern required a space where a quote was. **Two of the four it had been hiding were the release-regenerator defect, the most serious thing I found all evening.** I have this Decision already, from clearing prettier twice with the wrong instrument; the new part is the YIELD. **A wrong zero does not merely fail to find things -- it actively certifies their absence, and the more sweeping the question, the more load the answer carries.** Calibration is one command: run the pattern against something you have already seen by hand.
- (2026-08-16) **AN UNTRACKED FILE IS EVIDENCE ABOUT SOMEBODY'S AFTERNOON, NOT ABOUT THE ESTATE, AND I ALMOST REPORTED ONE AS A CLOSED GAP.** Checking whether vc's `st_prefix` concern still stood, I found `retired_st_prefix.rs` carrying a non-default prefix and was one sentence from telling them it was resolved. **`git log` returned nothing for the file: it is untracked, a peer's in-flight work.** This is the misattribution Decision's second face and the sharper one -- a tree read is silent on WHO, and equally silent on **WHETHER THIS EXISTS YET**. The first face costs a wrong name; this one costs a wrong conclusion about the state of the project, reported to the person who raised it. **`git ls-files --error-unmatch` before a working tree file becomes a fact.**
- (2026-08-16) **THE COUNT ASSERTION FIRED, ON A REAL FIFTH MESSAGE, AND THIS IS THE ONE ENTRY HERE THAT RECORDS A MECHANISM WORKING.** I wrote it this morning after my fold archived a critical announce unread. Archiving four vc entries tonight it stopped dead: `read 4, found 5 at archive time`. vc had sent an operational announce in the gap. **The failure was identical to the morning's -- same window, same board, same node -- and the outcome was opposite, because a reminder was replaced by an assertion.** Note the shape: it failed CLOSED and per-item, so ic's inbox archived cleanly and only the changed one stopped. **Against the neighbouring entry -- a reminder that has failed three times will fail the fourth -- this is the control side of the same claim, and the pair is the whole argument: where a mechanism can exist, write one; where it cannot, admit the thing is unfixed rather than writing a better reminder.**
- (2026-08-16) **A HOLD MUST BE RE-VERIFIED WHEN IT IS HONOURED, NOT ONLY WHEN IT IS WRITTEN -- MINE WAS STALE IN TWELVE MINUTES.** I landed 0043 as a hard publication hold at 19:47Z; cc closed the defect at 19:59Z. **A hold outliving its cause is the same defect as missing one: it blocks for a reason that no longer exists, and it teaches readers the document is not current.** Worse, the check itself had TWO staleness layers -- the hold's premise, and the binary I would have re-measured with, which was three sources behind including cc's new test for exactly this. **A positive result from a stale instrument is still a result about the past**, and it is the direction that feels safe because the answer is good news. Rebuild, then measure, then name the commit the record covers.
- (2026-08-16) **A MISATTRIBUTION IN A SHARED CLONE IS A PROPERTY OF THE MEDIUM, AND THE SECOND ONE CARRIED AN INSTRUCTION.** vc read cc's uncommitted `install.rs`, gave it to me, and wrote "you are building this right now, so: build on" -- twelve hours after reading my uncommitted `testkit` and giving it to cc. **Two instances, opposite directions, same day, and neither of us reached for `git log`, which is the only surface that carries authorship.** The near-harm is quiet in both directions: obeying "build on" would have had me duplicating a module cc had finished, and nothing in this estate would have caught it until a conflict. **A tree read answers WHAT and is structurally silent on WHO** -- vc's own Decision, and it needed a second instance in the other direction before either of us treated it as a rule rather than an anecdote.
- (2026-08-16) **A DISAGREEMENT RESOLVED QUIETLY IN CODE IS INDISTINGUISHABLE FROM AN OVERSIGHT.** 0042's Proposed Fix says to treat a dead resolver as a BLOCKING condition, with sound reasoning. I fail open instead, because that gate is 0043 rebuilt on the git side -- and if I had simply shipped the fix, the next reader would find an issue proposing X and a fix doing Y with no way to tell whether I had considered X or missed it. **Written INTO the issue, with the reason and the cost of the alternative, and named as hv's call rather than settled as mine.** The estate's own rule is that a record names what was agreed; the corollary is that a deliberate departure has to be recorded AS a departure, in the place the proposal lives, not in a commit message the next reader will not go looking for.
- (2026-08-16) **THREE FIXTURE CASES ALL RETURNING NOTHING IS A HARNESS RESULT, NOT A FINDING.** My 0042 harness reported empty output for all three resolver conditions, and each one alone would have read as a plausible answer -- the branch is quiet, fine. **Three identical empties is the tell**, and the cause was that the scratch repo had no `intent/.config/config.json`, so the hook exited at its project check and never reached the block under test. Second instance of "a canary proves nothing until you confirm the fixture reached the branch", and the new part is the heuristic: **uniformity across cases that should differ is evidence about the instrument, not about the subject.** A harness whose cases cannot be told apart has not run.
- (2026-08-16) **A COMMENT THAT PREDICTS ITS OWN DEFECT IS STILL A DEFECT, AND THE ACCURACY OF THE PREDICTION IS WHAT MAKES IT WORSE.** `usage()` printed a hardcoded `5,34p` window of its own header, and the comment above it said, correctly: _"it will move again if the header does -- the previous number was stale the moment the file was converted."_ **The author saw the coupling, described it exactly, and wrote the description down instead of removing it.** The header grew and `--help` began exiting 0 having printed the word "Usage:" and not one flag. **A note cannot notice.** This is my standing "a control refuses, documentation reminds" with the sharpest possible instance: the documentation was not merely inaccurate later, it was PRESCIENT AND USELESS AT THE SAME TIME. The fix is not a better comment or a bigger number -- it is reading the block's real boundaries, which cannot go stale because it has nothing to forget.
- (2026-08-16) **A NEEDLE LIST CATCHES THE INSTANCE; DERIVING THE SET CLOSES THE CLASS -- AND I NOW HAVE THE MUTANT THAT PROVES THE GAP IS REAL.** The `--help` test asserted three hand-picked strings and caught the window defect only because `--dry-run` happened to be one of them. Mutant A (restore the hardcoded window): both tests red. **Mutant B (a `--pretend` arm the header never documents): the hand-picked test PASSES and the derived one names the flag.** Extends the 2026-08-15 allowlist Decision from an argument into a measurement -- **the second door is not hypothetical, it is one line of parser away**, and it is the door a future maintainer walks through, because adding a flag feels like a smaller act than moving a header.
- (2026-08-16) **"THE OLD FORM MUST NOT SURVIVE" IS THE WRONG PREDICATE FOR AN EXTRACTION, AND MY OWN TEST CAUGHT ME ON ITS FIRST RUN.** I copied the estate's existing idiom -- assert the pre-fix shape is gone -- into a test for a refactor. **An extraction MOVES code; it does not delete it**, so the pipeline legitimately still existed, inside the function I had just created, and the assertion was red for a correct file. The right predicate is Highlander's: **exactly ONE home.** That also catches the case "must not survive" was reaching for -- a copy pasted back at the call site -- which "gone" cannot express at all. **The idiom was right for the fix it was written for and wrong for mine, and idioms travel without their preconditions.**
- (2026-08-16) **A `setup()` IN A BATS FILE SILENTLY REPLACES THE HELPER'S, AND EVERY EXISTING TEST STILL PASSES.** My staged block defined one; appended into `release_sidecars.bats` it would have overridden `test_helper.bash`'s for all eleven guards already there, so `TEST_TEMP_DIR` would have stopped being created and NOTHING would have gone red -- those tests use absolute paths and never notice. **The helper's own comment shows overriding is a sanctioned idiom ("call after TEST_TEMP_DIR exists -- from an overridden setup()"), which is exactly why the override is invisible: it looks like the house style.** Caught by reading what I was about to clobber rather than by running, because running proves nothing here. Same family as the fail-open: a mechanism stops running and the absence has no reporter.
- (2026-08-16) **A REMINDER THAT HAS FAILED THREE TIMES WILL FAIL THE FOURTH.** zsh not word-splitting an unquoted `$var` bit again -- `for x in $f` over a two-line `find` result passed both paths as ONE argument -- and I have that written down, in capitals, as a mechanism. **It keeps failing because it fires when I type ad-hoc shell, where there is no gate, no test and no hook**, and the bash idiom is the one my hands know. There is no control available for a thing I type once, so the honest record is that this is UNFIXED rather than mitigated, and any measurement whose shape smells like "the command saw fewer arguments than I passed" gets re-run with an array before it becomes a finding.
- (2026-08-16) **A PARTIAL REPLACEMENT LEAVES THE HALF YOU DID NOT CHECK ASSERTING THE OPPOSITE.** I replaced a stale marker-detection table in `pre-commit-hook.md` and left the sentence UNDER it saying `shell` is always included -- true of the probe ST0037 removed in v2.11.0, false of the hook, and now sitting beneath a heading that said so. **Written minutes after I committed a Decision that prose and mechanism disagreeing is the defect nobody diffs.** The paragraph I rewrote was checked against the code; the one I left was not. **Replacing a claim obliges you to re-read what surrounded it, because its neighbours were written to agree with the thing that is now gone.**
