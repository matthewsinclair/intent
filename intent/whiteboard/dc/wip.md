---
node: dc
name: DevX Claude
role: worker
session_id: 3f2f5de2-d774-44db-8f1f-c85588606969
heartbeat_at: 2026-08-31 07:52Z
status: active
focus: "THE DELETE IS HELD AND THE CENSUS IS WHY. hv gave the go after the census; the census found the population is bigger than my table by a factor. W14 fired THREE times on one prune, each a container higher: members, then run_tests.sh:24, then test_helper.bash:21 (INTENT_BIN defaults to bin/intent, set NOWHERE, 59 of 112 files reach it) and 147 direct intent_<sub> sites in 5 files -- 143 in treeindex+fileindex, 100 arms. My own census returned any=0 for BOTH of those files: a false zero on the two largest dependents, now W15. Reported to vc between the 07:51Z and 07:52Z reads. Next: read those 100 arms, then land the settled migrations."
claims: [ST0056/04, ST0056/05, ST0056/07, ST0056/11, ST0056/12, ST0058, ST0066]
---

# DevX Claude (dc)

**COLD-SESSION MINIMUM. A rule is never dropped here, only its narrative.** Full reasoning for 29-30 Aug is in `.history/20260830/wip-fold-0837Z.md` and `.history/20260829/`.

## D42 -- TIME. Read before writing anything, anywhere.

- **You never ask what time it is** -- not the OS, not `date`, not the filesystem, **not the database.** The stamp is applied BY the write.
- **THE RULE IS ABOUT SIGNATURES: NO cli or intentsvcs function TAKES a time.** They may RETURN times.
- **A board stamp is a label, not data** -- read from `date -u` and PASTE, **per stamp, never per session.**
- **`stat`, `git log`, `ls -la` ALL PRINT LOCAL.** Appending `Z` to a local read is an ASSERTION, not a format.
- **A ULID is an IDENTITY and the `ts` is the STAMP** -- which is why `Envelope::minted` may generate the id in Rust without breaching D42, and why a cascade can carry its ancestor's event id without a second transaction.

## The truth model and the environment

- `design.md` (D01 reversed) + `data-model.md`. **The db is the durable SSOT; the typed API is the only door in.** Crates: `intent-cli`, `intentd`, `intentsvcs`. **`intentdb` IS RETIRED.**
- **`intent` ON PATH IS v3 AND RESOLVES INTO THIS TREE** -- `~/.local/bin/intent` and `~/bin/intent` both symlink `native/rust/target/release/intent`.
- **THE STORE IS PROJECT-RELATIVE**, `intent_dir()/.cache/intent.db`, found by walking up from CWD. **SCHEMA 16.**
- **THE INDEX IS SHARED.** `add` + `commit --only <paths>` is the only safe write.
- **CANON ORDER: SYNC FIRST, then commit the file and canon together.** A later sync fixes the NEXT commit and can never fix this one.
- **THE GATE FIGURE IS COMPUTED BY THREE VERB CALLS AND NEVER TRANSCRIBED.**

## DOING

**THE `bin/` PRUNE, WITH hv's GO, AND THE DELETE IS HELD BY THE CENSUS hv ASKED FOR (2026-08-31 07:52Z).** hv chose the per-file property census over a wholesale retirement, and _go, take the delete AFTER the census_. **The census then found the population is bigger than the member table by a factor -- three containers, 59 files and 100 unread arms -- so the delete does not go until containers 2 and 3 have verdicts.** That is the instrument working, not a reversal.

**READ TODO 1's LAYER TABLE BEFORE THE MEMBER TABLE.** The member table stands and is NOT the population; a cold session acting from it alone would ship the defect this hold exists to stop.

**IN FLIGHT:** reported to vc, awaiting their read on whether the resized delete goes to hv through them or from me. Next action needs no ruling: read the 100 `treeindex`/`fileindex` arms.

## TODO

### 1. THE CENSUS -- REVERSIBLE, NO RULING NEEDED, IN PROGRESS

**THE STANDARD IS vc's, FROM `no_absolute_home_paths`: does v3 cover the PROPERTY.** Not _does the v2 file still pass_ -- a passing v2 test whose subject is being deleted is evidence about the past. Retire what is covered with the reason at the deletion site; migrate or leave-red-with-a-note what is not.

**2026-08-31 07:52Z: THE TABLE BELOW IS THE MEMBER CENSUS AND IT IS ONE LAYER OF FOUR. It stands; it is not the population.** Driven this morning, each layer found only because the one below it was already wrong:

| layer           | dependency                                                                                                                                                                                                                                                                            | state                          |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| members         | the 13 verdicted bats files in the table below                                                                                                                                                                                                                                        | **censused**                   |
| **container 1** | `tests/run_tests.sh:24` sources `bin/intent_helpers` above every `@test` in the estate                                                                                                                                                                                                | known; carry is `error` x3     |
| **container 2** | `tests/lib/test_helper.bash:21` -- `INTENT_BIN="${INTENT_BIN:-${INTENT_BIN_DIR}/intent}"`, and **`INTENT_BIN` is set NOWHERE** in `run_tests.sh` or either CI workflow. **59 of 112 files** reach it via `run_intent` (`:148`)                                                        | **NEW, unverdicted**           |
| **container 3** | **147 direct `${INTENT_BIN_DIR}/intent_<sub>` sites across 5 files** -- 143 in `treeindex_commands.bats` (75 sites, **53 arms**) and `fileindex_commands.bats` (68 sites, **47 arms**); rest in `intent_bin_retarget_guard` (1), `title_pipe_sanitize_guard` (1), `init_commands` (2) | **NEW, unverdicted, 100 arms** |

**`test_helper.bash:12` SAYS `INTENT_BIN` EXISTS SO THE ESTATE CAN RUN AGAINST ANOTHER BINARY, CITING ST0056** -- so repointing it is the ANTICIPATED route for the 59, not a workaround. **`:15-18` says the 147 direct sites bypass the dispatcher BY DESIGN and have no single-binary equivalent** -- a different case, and the one that decides the prune's real size. The 100 arms are unread; no verdict is asserted.

**TWO CORRECTIONS TO THIS TABLE, both smaller-than-claimed:** v3 critic coverage is **6 arms, not 7** (5 in `critic_refuses_an_empty_library.rs`, 1 end-to-end). And **only `--severity-min` is re-confirmed** in v3 (`render.rs:7689`, real parse-and-refuse); `--files` / `--staged` are NOT carried as established until the clap layer is read.

| v2 file                                | arms     | verdict                               | v3 home / reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------------------------- | -------- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `no_template_fallback.bats`            | 7        | **RETIRE, strongest reason**          | `embedded_init.rs` -- `no_arrangement_of_the_disk_produces_a_missing_template_error`. v3 carries templates INSIDE the binary, so the failure mode is structurally impossible, not merely covered                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `helpers.bats` id arms                 | 4        | **RETIRE**                            | `operator_id_spellings.rs` -- `a_leading_zero_is_decimal_and_not_octal`, `leading_zeros_are_spelling_and_not_width`, `the_v2_parity_contract_is_five_forms`. Names v2 parity explicitly                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `helpers.bats` `list_st_dirs`          | 3        | **RETIRE with subject**               | v3 enumerates from `.intentfiles` + the store. Scan-dirs-for-`info.md` is a MECHANISM that no longer exists, not a vehicle for a surviving property                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `intent_migrations_languages.bats`     | 7        | **RETIRE with subject**               | the v2 ledger-step machinery is gone; `migrate_v2_project.rs` + `unmigrated_project.rs` cover v3's migration                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `intent_migrations_relocate.bats`      | 7        | **RETIRE with subject**               | `.intent/` -> `intent/.config/` was v2.9->v2.10. v3's floor is above it. `migrate_refusal.rs` is about RESIDUE, not symlink/conflict/sentinel -- different subject, and correctly so                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `intent_upgrade_dispatcher.bats`       | 6        | **RETIRE**                            | `upgrade_command.rs` + the nine files vc enumerated                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `intent_upgrade_orchestrator.bats`     | 11 of 15 | **RETIRE**                            | same                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `output_width.bats`                    | 5 of 6   | **RETIRE**                            | `output_shape.rs` maps arm for arm, incl. `markdown_ignores_width_in_both_directions`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `output_width.bats` `dft_width`        | 1        | **RETIRE with subject**               | `dft_width` is ABSENT from v3 source -- a v2-only config key                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **`intent_critic.bats`**               | **21**   | **MIGRATE -- THE REAL GAP**           | v3 SHIPS `intent critic` (`--files`, `--staged`, `--severity-min` all present). v3's critic tests cover the EMPTY-LIBRARY REFUSAL and nothing else: 7 arms across `critic_refuses_an_empty_library{,_end_to_end}.rs`. Arg validation, `--format json`, `.intent_critic.yml` suppression, prose-discipline no-ops, severity thresholds have **no v3 home**                                                                                                                                                                                                                                                                                                        |
| `no_absolute_home_paths.bats`          | 2        | **MIGRATE (vc ruled)**                | to `no_pm_state_in_output.rs`. **And its vehicle is already hollow** -- `:60` reads `intent/plugins/claude/bin/intent_claude_upgrade`, WHICH DOES NOT EXIST, via `grep -cF ... \|\| true`. Not coverage we are losing; coverage we never had                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `helpers.bats` `stamp_project_version` | 4        | **MIGRATE -- I AM CARRYING THE CODE** | the function moves into `release`; its tests must move with it or I ship carried code with no test. `release_script.bats` / `release_sidecars.bats` both survive and are the home                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `helpers.bats` error/warning voice     | 3        | **CLOSED 08-31: 2 RETIRE, 1 MIGRATE** | `warning()`/`error()` arms (`:170`,`:182`) die with their subject. **The corpus arm `:198` MIGRATES.** `error_literal_shape.rs` reads exactly ONE file -- `renderer()` `:80` returns `CARGO_MANIFEST_DIR/src/render.rs` -- so it is a Rust-literal-ORDER guard with nothing to say about shell. `:198`'s corpus is `bin/` + both plugin bin dirs = post-prune population B + `intent_agents` + `intent_claude_cwi` + `intent_claude_hook`. Live, currently green, **and green inside population A too, so the delete changes only what is WATCHED**. Positive-controlled: the pattern fires in 4 files elsewhere, all correctly outside the arm's declared scope |
| 4 partial-arm files                    | 8        | arm deletions                         | `ac_offscope_states` (2), `acceptance_close_gate` (1), `at_grammar_lint` (3), `ambient_project_root_guard` (2) -- one-line reason each                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `release_sidecars.bats:114/:116`       | 2        | **RETIRE with subject**               | greps `bin/intent_upgrade`; name it retired-with-subject so nobody hunts for the coverage                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

**TWO PRODUCT OBSERVATIONS FOR hv, FOUND BY THE CENSUS AND NOT BY LOOKING FOR THEM.** v3 `upgrade` has **no `--dry-run`** -- v2's had four arms on it including a positive control, and _preview before an irreversible convergence_ is a safety property, not a test detail. And v3's critic has a surface v2 tested twenty-one ways and v3 tests seven, all seven on one refusal.

### 2. THE DELETE -- HELD 2026-08-31 07:52Z, AND THE CENSUS IS WHY

**hv's GO STANDS AND IS NOT WHAT IS IN QUESTION.** hv said _go, take it after the census_; the census is the instrument hv CHOSE over wholesale retirement, and it has reported that the delete as scoped is not the delete that exists. **Holding is the census working, not a reversal of the ruling.** Reported to vc; the resized shape goes back to hv.

**WHAT THE ONE-COMMIT PLAN WOULD HAVE DONE:** left **59 files** resolving `INTENT_BIN` to a deleted default and **100 arms in two files** calling deleted scripts directly. Neither is in any test, so no per-arm census could reach them.

**RESUMES WHEN containers 2 and 3 HAVE VERDICTS.** The commit shape below is still right for the members and is kept intact:

**ONE COMMIT: `git rm` population A + BOTH carries.** The carries cannot land earlier and that is the rule, not caution -- carrying the stamper while `bin/intent_helpers` still defines it puts two copies in the tree, and `release_sidecars.bats` exists to assert there is one.

- **Population A = 26 files / 12,415 lines** (`bin/intent` + 25 `bin/intent_*`). Population B = 57 files. `bin/` is exactly A + B.
- **`stamp_project_version` -> `bin/.devbin/cmd/build.d/release`, and it is the ONLY carry that file needs.** Symbol census over all 38 helpers: `release` calls exactly one. It already defines `log_info` `:117`, `log_warn` `:120`, its own error printer `:134`. **My board said three; three was wrong.**
- **`error` -> `tests/run_tests.sh`, dropping `:24`'s source line.** Symbol census: `error` x3, nothing else.
- **`tests/cutover_guard.bash` NEEDS NOTHING.** It reads `bin/intent_helpers` as a symbol roster, which LOOKS fatal; its own header already ruled the case and its static arm reports `n/a -- bin/ already pruned` rather than passing. I read the edge and not the header.
- Apply D-DELETE's three populations first.

### 3. STANDING

- **`at.recast`** -- one call moving `kind` and `status` together; `at_fc` is the precedent. **`data-model.md:751` declares the kind fold into Machine 5 and the table has no kind column** -- declared, never transcribed. **THAT TABLE IS hv's PEN**, receipt at `model.rs:1790-1794`. vc's correction stands: `(any) -> to-write` must NOT require a file (15 of 77 to-write rows carry none, so requiring one makes the legal landing state unreachable). Wanted for the nine ST0068 rows; not tag-blocking.
- **The nine ST0068 rows** -- mine, they want `at.recast` first.
- **The payload reshape remainder**: unified `claude list|install|sync|uninstall --kind`, `claude upgrade --apply` drives the sync, drop `init`. **ic AUTHORS the seven `claude subagents` narrowing rows when this lands** -- they are holding on it and asked to keep the dependency visible. hv ruled those rows NARROWED, not wired (W11).
- **`AC-04.6`'s `--kind` flag**; the delivered pair is still split -- `intent` and `intentd` are provably built from two different trees. Rebuild is announce-and-hold, never unilateral, never from a dirty tree.
- **The phase split, still owed.**

### 4. WITH hv, NOT WITH ME

- **`hv/wip.md:31` RECORDS `AC-12.1`'s BLOCKER AS LIVE AND ITS MECHANICAL HALF IS DISCHARGED.** WP-14-blocks-WP-12 was about the provisioner; vc's own `:108` predicted the discharge and named the hand (WP-07, mine). `cwi` carries its own primitives, `rules_lib.sh` carries `ext_root_dir`, and **no source or exec edge into population A survives under `intent/plugins/`**. What is left is `AC-14.12`, which never blocked the prune. Measured, not ruled.
- **ST0058's `AC-00.1`** -- its stated falsifier is FIRING (the pair names different commits). `AC-07.7` / `AC-11.1` / `AC-11.4` / `AC-12.4` tag-gated.

## Watch-outs

**FOLDED FROM 23 FAMILIES TO 8, because most of them were one shape seen from different sides. Rule only; instances are evidence, not the entry.**

**2026-08-30 21:31Z: TWO NEW CLASSES (W12, W13) AND NEITHER IS A CARELESSNESS CLASS.** Both bit while I was being careful, and in most instances the CONCLUSION WAS STILL RIGHT -- which is exactly why they survive. W13 alone had five instances across four nodes in one day.

- **W1 -- THE INSTRUMENT ANSWERED A DIFFERENT QUESTION THAN THE ONE ASKED, AND ITS OUTPUT LOOKED LIKE AN ANSWER.** The dominant class, six instances in two days. **A partial read is the sharpest form and it is ASYMMETRIC: one that finds SOMETHING self-corrects; one that finds NOTHING yields a conclusion nobody can falsify without redoing the read.** So **a NEGATIVE from a truncated read is not a result** -- `head -30` said this repo uses no trailers (37 of 40 do); `tail -30` said a suite was green and **let a red reach main**; `tail -14` said a guard did not print paths it prints twice. **THE PROCEDURE: output to a file, then `grep -c '^test .* FAILED$'` over the whole of it.** **AND A FILTER OVER A TOOL'S OWN DIAGNOSTIC IS THE SAME DEFECT WEARING A COMMIT.** I ran `git commit ... | grep -E '^(error|fatal|ok:)'`, which matched NONE of the gate's lines, saw an unchanged HEAD and a staged file, and diagnosed the shared-index race that had genuinely happened ten feet away minutes earlier. The real refusal was prettier -- _markdown is not formatted -- REFUSED_ -- printed in full, twice, filtered away both times. **A real nearby instance of a class is the most expensive thing to have in mind while reading a filtered log**, and I nearly committed the fabricated mechanism as the record. **`--help` IS THE SAME SHAPE AS A WIREDNESS PROBE**: it renders for a DECLARED command whether or not an arm exists, so rc=0 answers a question about the table. Invoke the verb. A count over a complete file can be zero honestly; a tail cannot. Sibling forms: a check that cannot tell WRONG from COULD-NOT-MEASURE names innocent files and prints a remedy that runs clean (the rustfmt `mod common;` probe); one sentence covering two conditions picks neither fix (absent-vs-malformed table, UNMEASURED-vs-DISAGREE); an instrument that cannot vary with its subject (positive-control before believing any zero); `cargo check` does not compile test targets without `--all-targets`.
- **W2 -- THE MEASUREMENT'S SUBJECT WAS NOT THE ONE I NAMED.** **A sha and `git status` answer different questions and only one is in the label** (cc): "the clean tree at `6b32b71d`" ran against a peer's dirty worktree, and the test read the table from disk. A suite run in a shared tree measures the TREE, not HEAD -- **and so does the delivered binary.** `HOME` isolates USER state and the store is PROJECT-relative, so a spawned binary with no `current_dir` finds the real project. **A `git status` expires in minutes here**; re-run it in the same turn as the sentence resting on it.
- **W3 -- OWNERSHIP IS MEASURED, NEVER INFERRED, AND IT HAS TWO TELLS.** _Absent at HEAD, therefore someone else's_ (`fn fc` was mine; three nodes converged on the same wrong owner). _The subject is theirs, therefore the edit is theirs_ (the `values: null` was ic's, not cc's whose daemon rows they were). **`git log -S<symbol> -- <path>` and `git log -1 -- <path>` close both.** Neither leaves a mark when wrong: the peer accepts work they did not do, or spends time in a file they never touched.
- **W4a -- THE INDEX CLASS HAS THREE MEMBERS AND ONLY ONE IS CLOSABLE BY THE COMMITTER** (cc's framing, adopted). **CONTENTION** -- taking a peer's bytes -- is closable: a **private `GIT_INDEX_FILE`** (`read-tree HEAD`, `git add` the whole-file paths, `git apply --cached` a hunk-filtered patch, commit with the env var set) gives hunk granularity AND isolation at once, which `--only` and `apply --cached` each give only one of. Used it to land `73338d84` past a canon extract carrying two peers' work. **COHERENCE** -- the tree I commit must make sense -- is NOT closable from your side: **my staged tool without its roster row blocked EVERY node's commits for ~20 minutes**, and no scoping of my own paths could have helped. **REVERSION is the nastiest and it leaves a correct-looking artefact**: a private index needs a HEAD PIN on BOTH sides, and **the post-commit `HEAD^ == base` assert matters more than the pre-check, because the entire gate runtime sits inside the window the pre-check cannot cover.** And **the ambient index is left holding pre-commit blobs for every path the private commit touched** -- your own commit then reads as a pending reversion; `git reset -q HEAD -- <paths>` belongs in the SAME turn as the commit, not as cleanup. **Never reset a peer's index entries: it is the same offence as taking their bytes.**
- **W4 -- THE SHARED CHECKOUT PUNISHES EVERY ASSUMPTION ABOUT WHO ELSE IS WRITING.** Never `cp` a shared source aside to mutate; never format or bless a file carrying a peer's in-flight change. **A multi-step refactor is uncompilable between its steps and nothing says "mid-move"** (cc): a rename plus its use sites is ONE edit. **A staged intermediate of yours blocks every peer's commit**, because the gates read the index. A build window is silently also a commit freeze. **AND `--only` DOES NOT COVER THE TWO WAYS IT ACTUALLY WENT WRONG TODAY.** (a) **`git commit --amend` has NO PATH SCOPE AT ALL** -- vc reached for it to fix a message and it took my staged `skills.rs -> payload.rs` rename; caught on the stat line, `reset --soft`, re-committed with `--only`, nothing lost. The mitigation guards the ordinary commit and the message-fix reflex bypasses it. (b) **Holding your hunk UNSTAGED does not protect it**: cc's `6aa00235` took my `doctor.rs` check because a plain commit needs the file DIRTY, not staged. Four instances today. **The only thing that actually worked was telling the peer before committing a file we were both in.**
- **W5 -- A RESTRICTION THAT REDS NOTHING HAS NOT BEEN ADDED.** D7 went into a roster `put` does not read; 29/29 stayed green and the refusal moved nothing. **When you add a guard, something must go red** -- if nothing does, find the door before believing it. Corollary from the other side: **a rule that catches a failure mode it was NOT designed for is the strongest evidence for it** (arm 8 refused a binary that panics on startup, knowing nothing about dispatch tables). **AND THE PASS/FAIL CHANNEL CARRIES THIS TOO: A GUARD THAT SKIPS PRINTS `ok`.** `devbin_fmt_md`'s premise arm was RED four days ago and now SKIPS -- _no root fence currently holds content prettier would reformat_. Nothing changed about the guard; its POPULATION went empty. **An `ok` count is not an armed count, and a suite reporting totals without a skip census cannot distinguish a guard that held from one that could not fire.**
- **W6 -- A SECOND HOME FOR A FACT DRIFTS, AND THE COPY THAT DRIFTS IS THE REPEATED ONE.** **AND THE PATTERN IS PROXIMITY, NOT DISTANCE** (vc's generalisation, on my `solely_covered_by` find, three instances in a day): **a second home three lines away does not READ as a second home -- it reads as CONTEXT**, so nobody applies the Highlander question to it. A duplicate in another crate at least LOOKS like one when you find it. Every instance had its correct statement within a few lines of the wrong one, in the same file. Never transcribe an instrument's number -- drive the verb. A count in prose beside a checker that computes it went stale (FIVE state fields when there were six; "the four ratified machines" while a fifth was ratified underneath). **An anchor that resolves to the record's own RENDERING is circular and worse than a dangle** -- a dangle is visible, a circle certifies itself. **And two files can hold different READINGS of one value with nothing seeing it** until something uses the second (`from: &[]` meant _any state_ to one file and _never filled in_ to another).
- **W7 -- MECHANICS THAT RETURN A PLAUSIBLE WRONG ANSWER.** **AN INSTRUMENT'S OWN TIMING NUMBER CAN EXCLUDE THE THING UNDER INVESTIGATION** (cc): `cargo` prints `Blocking waiting for file lock on build directory` and THEN `Finished in Xs` -- **the wait precedes the figure and is not counted in it**, and the line scrolls past in the first moment, so anyone reading the tail sees a fast build. **Time from OUTSIDE the process.** **Never take `$?` through a pipe** -- it belongs to the last stage; `cargo fmt --check | head` prints a diff and reports rc=0 (four instances, three nodes). `cargo test` aborts after a failing target: `--no-fail-fast`, or two runs report different populations in the same units. **`cargo test` is not read-only: it migrates the shared store**, and the severity is BLAST RADIUS, not data -- one `cargo build --release` fixes every node. A detached worktree gets its OWN `CARGO_TARGET_DIR` **and that directory dies with the worktree**: sharing bakes a stale `CARGO_MANIFEST_DIR` into cached rlibs, and keeping one grew an abandoned worktree to 10G.
- **W10 -- I DOCUMENTED THE CLASS AND FIXED THE INSTANCE, AND THE COMMENT MADE IT LOOK CLOSED.** Twice in one block of `.githooks/pre-commit`, both found by ic, both signed by me the same morning. My comment said _beside the original, module resolution is the file's own_ -- **true of the DIRECTORY, false of the NAME**, because rustfmt resolves from the file's STEM; I fixed sibling resolution and left every module root with a subdirectory broken, and `lib.rs` passed by layout coincidence. Two paragraphs above, the same block names _a gate that cannot say I could not check will eventually say something false_ -- and its `rs_err` printed the LAST iteration's stderr. **A class-shaped comment over an instance-shaped fix is worse than no comment: the next reader, including me, reads the class as handled and does not look.** The tell is a fix whose test is the instance that prompted it. **Ask what ELSE is in the class and drive one of those instead.**
- **W9 -- A REMEDY WHOSE STATED REASON DOES NOT HOLD IN YOUR CASE IS NOT PERMISSION** (vc's naming, on my find). `doctor` said do not reach for `wp done` **because it is refused on a blocked gate** -- and my gate was not blocked, so `wp done` would have run. **The reason evaporating does not convert the advice into a licence**; it means the remedy was written for a different situation and has nothing to say about yours. Same shape as W1 from the other end: there an instrument answers a question you did not ask, here a remedy answers a situation you are not in, and both read as though aimed at you.
- **W11 -- I ARGUED FROM HOW THE SYSTEM BEING REPLACED WAS BUILT** (hv's correction, 2026-08-30). I costed `claude subagents` by measuring v2's decomposition -- a 582-line shared core, ten callbacks -- and concluded that copying `skills.rs` would leave v3 with two homes where v2 has one. **hv: it is not a PORT, it is FUNCTIONAL PARITY, and those are not the same thing.** Parity is a claim about what the tool DOES for an operator; v2's internals are not a constraint on v3. **The tell is that the argument could not be stated without naming v2** -- the legitimate version of the same concern ("would this be two homes IN v3?") never mentions it. **And the reframe moved the TARGET, not just the reasoning:** I was scoping seven verbs because v2 had seven, when the requirement was that an operator can get payload onto a machine that has none. Sibling: **vc's**, on their own withdrawn census -- **a census keys on SHAPE and a defect is about MEANING, and the bridge is asking what a row's ABSENCE would lose.**
- **W12 -- I PROVED SOMETHING TRUE AND BESIDE THE POINT, AND EVERY STEP OF THE PROOF WAS SOUND.** Retiring `AT-01.8`: I read `AT-03.12` -- the estate's ONE retired row -- as _the_ shape of retirement, then correctly established that no verb produces it. All true. **The requirement was never a STATE, it was a PROPERTY: the row had to stop claiming a result**, and `AtStatus::Fiat` does that and is legal for both kinds. vc's general form, which is the entry: **_no verb produces state X_ is a claim about a ROUTE. Ask what the thing must STOP CLAIMING before asking what it must BECOME.** **The tell is damning and worth keeping: I had WRITTEN `permitted_for` and its comment and still did not read it**, because the question I was asking was _what does a retired row look like_. A sample of one is a shape, never a rule.
- **W13 -- A RULE OR CITATION TAKING ITS AUTHORITY FROM A NAME RATHER THAN FROM ITS MEMBERSHIP RULE. FIVE INSTANCES, FOUR NODES, ONE DAY.** Mine twice: I called `table_driven_tests_fixture_their_home.rs:86` _the guard on binary resolution_ when its membership rule is _does this test spawn the binary at all_ (for which its `||` is CORRECT, and narrowing it would have been a second defect); and I held hv's bats delete on `facade.rs`'s _a kind change belongs in a ruling_, which governs the CRITERION's kind, whose reason is that it decides where satisfaction comes from -- an AT row's kind moves no contract graph. Also vc's `populations.self_loop`, cc's arm-6b third home, cc's blast-radius claim from one member. **IN THREE OF THE FIVE THE CONCLUSION WAS STILL CORRECT, which is what hid the reasoning error completely.** Discriminator: **state the rule WITHOUT naming the thing. If the argument needs the name, it is resemblance.** Being right in OUTCOME and wrong in GROUNDS is the normal presentation, not the rare one.
- **W14 -- A CENSUS THAT ENUMERATES MEMBERS CANNOT SEE A DEPENDENCY HELD BY THE CONTAINER, AND THE CONTAINER IS WHERE THE FATAL ONE LIVES.** My board sized the `bin/` prune's test fallout at **two lines in one file**. Measured: **13 bats files plus the runner** -- and the runner is the point. `tests/run_tests.sh:24` sources `bin/intent_helpers` ABOVE EVERY `@test` IN THE ESTATE, so the prune does not fail 112 files, it stops them STARTING; and one level up again, `tests.yml` runs `./run_tests.sh` on both platforms, so the CI leg dies on a `source` rather than an assertion. **No per-test census could ever have found either, because neither dependency is in a test.** **THE FORM: when you census a population, separately census the thing that RUNS it, and the thing that runs THAT.** cc's variant the same day is the general case: a rule naming WHEN and not in WHICH ENVIRONMENT is satisfiable by the broken form. **Second half, from the same episode: a keyword's presence is not a property's coverage.** My v3-coverage probe returned 0 for every term -- including `critic`, visible in the listing -- because zsh did not word-split the path variable; the corrected probe then over-matched (`intent_version` hits 57 files, it is in every fixture). **Both directions of one instrument were wrong and only the positive and negative controls separated them.** Property coverage is a read, not a grep.
- **W15 -- MY CENSUS RETURNED A CONFIDENT `any=0` FOR THE TWO LARGEST DEPENDENTS IN THE ESTATE, AND ONLY A SECOND INSTRUMENT DISAGREEING FOUND IT.** Per-file edge census over 112 bats files, edge kinds separated, deduplicated -- a careful instrument. It reported `any=0` for `treeindex_commands.bats` and `fileindex_commands.bats`, which between them carry **143 direct call sites into population A and 100 arms**. Cause: they spell it `${INTENT_BIN_DIR}/intent_treeindex`, so the literal `bin/intent_` never appears -- **the path is assembled from a variable and my pattern matched the assembled form, which exists nowhere in the source.** **THE ASYMMETRY IS THE ENTRY, AND IT IS W1's SHARPEST FORM ARRIVING IN MY OWN HAND: the census self-corrected everywhere it FIRED and lied only where it found nothing.** 32 files came back non-zero and every one of those numbers was usable; the two zeros were the whole defect. **THE FORM: when an instrument searches for a PATH, first ask whether the codebase ever writes that path literally.** A grep for a composed string is a grep for a thing that does not exist, and it returns zero rather than an error. Cure that actually worked: a second instrument keyed on the VARIABLE (`\$\{INTENT_BIN_DIR\}/intent_`) rather than the value, driven for a count I did NOT transcribe from `test_helper.bash`'s own comment (it said ~146, the drive said 147). **Sibling of W14 and found in the same hour: W14 is a dependency held by the container, W15 is a dependency the member holds but SPELLS in a way the census cannot see.**
- **W8 -- HOW TO REPORT.** Write reasoning at the resolution you MEASURED it, never at the one it suggests. **Verify a peer's correction yourself, including when it makes your own claim false.** A peer's quote of a ruling is still the peer. **Retract a finding the moment its basis fails, even one you were thanked for** -- and say which claims rested on what, because a negative and a sighting do not feel different at the time.

## Decisions

**FOLDED 2026-08-30 21:31Z. Nine pre-folds for this day are in `.history/20260830/`; THIS IS THE ONLY BANNER AND THE EARLIER TWO WERE DELETED RATHER THAN STACKED** -- three banners in one section is the shape `restart.md` names, and what they described is archived. Four commits landed today; what is below is what survives them. **hv PROVENANCE ENTRIES ARE VERBATIM AND ARE NEVER COMPRESSED** -- a ruling's own words cannot be re-derived.

**2026-08-31 07:52Z: THAT FOLD PUT `D-POP` IN THIS SECTION THREE TIMES, BYTE-IDENTICAL, AND I DID NOT SEE IT UNTIL I READ THE BOARD COLD.** Three homes for one value, in the section that exists to hold rulings, produced by the operation whose whole purpose is removing duplicates -- **`restart.md`'s gate-figure defect at document scale, committed by me into my own artefact.** Deduplicated to one; the diff was verified to be exactly two deletions and nothing else. **The tell worth keeping: a fold is a WRITE, so it needs the same read-back any other write needs, and the writer is the worst reader of it in the same session.**

- **D-ADVICE -- A DEFECT THAT CORRUPTS NO NUMBER HAS NO INSTRUMENT WATCHING IT.** `contract::solely_covered_by` filtered on kind and cover-count and never on state, three lines above a `resolve` that carries the rule. Fixed at `bbd7e1c0`. **Advisory output is where defects live longest, because nothing downstream disagrees with them.** Detail in `.history/20260831/decisions-fold-0723Z.md`.
- **hv's RETIREMENT RULING, EXECUTED 2026-08-30 (`bbd7e1c0`), AND THE ROUTE IS THE RECORD.** `at red` -> `fc` -> delete, one commit. **The `red` step is HONEST, not procedural**: the green was unreachable-by-construction (`grep` on a deleted file errors, so `fail` could never fire), 25 of 27 tests failed loudly and 2 passed this way. **`at.fc`'s from-set excludes `green` deliberately, and that narrowing is ADVISORY rather than structural** -- `at.set` is `from: &[]`, so `green -> red -> fiat` walks around it in one step. Reported to vc, who corrected hv's note and credited it. **A guard that does not guard is one of the things being deferred past the tag.**
- **THE RELEASE RESOLVES ITS CLI BY PATH, NEVER BY `PATH` (`8a7ed88d`), AND REFUSES RATHER THAN FALLING BACK.** The v2 sites named a file IN the checkout, so the script was ALREADY deterministic -- **swapping that for a bare `intent` is not a port, it is a loss of determinism smuggled in as one**, invisible because both spellings work on a correctly-configured machine. Two of the three calls regenerate canon that SHIPS IN THE CUT, so a `PATH` lookup makes released bytes a function of the operator's shell. **The failure is live**: `intent` and `intentd` are provably built from two different trees today. **A fallback that warns is a fallback that gets ignored.**
- **A TEST WHOSE SUBJECT IS GONE IS DELETED WITH ITS REASON, NEVER PATCHED TO KEEP PASSING.** The `--skip-settings` arm could only fail after the port, and dropping the flag from the grep would have left it asserting nothing. **The property it protected is NAMED at the deletion site** (`no_pm_state_in_output.rs`, `a29f04ec`) so nobody hunts for retired coverage. Same shape for `release_sidecars.bats:114`/`:116`, which dies WITH population A rather than being repaired.
- **A NEW GUARD IS DRIVEN TWO-SIDED ON A PLANTED FIXTURE BEFORE IT IS BELIEVED, AND ITS LIMIT GOES IN ITS OWN HEADER.** `absent_at_check.sh`: nine mutants against a prediction written first, four of them REFUSALS, because its population can legitimately be empty and "0 examined" is ambiguous. **Its header states that it covers ABSENT-artefact and NOT present-and-hollow -- which is the case that PRODUCED it**, since `AT-01.8`'s file was present the whole time its green was empty. **A `fiat` arm is deliberately absent**: fiat-closing a `to-write` row legitimately cites an absent file and nothing on the row separates that from the dishonest case. Not mechanically decidable -- same conclusion vc reached independently about red-rows-that-now-pass. **Two directions is the right number.**
- **VERIFY A PEER'S CLAIM ABOUT YOUR OWN INSTRUMENT, EVEN WHEN THEY ARE RIGHT.** vc asserted `absent_at_check.sh` clears a `fiat` row "by construction" and explicitly asked me to check rather than accept it. Driven, two-sided, and it held. **A property asserted from a description is still an assertion.**

- **D-DELETE -- A DELETE HAS THREE POPULATIONS AND I HAD ONE** (ruled with vc, 2026-08-30, after my prune cost three repairs in a day). **(1) WHAT EXECUTES THIS** -- a failure baseline; catches what goes RED. **(2) WHAT CITES THIS AS EVIDENCE -- AND THE POPULATION IS THE STORE, NOT THE TREE.** `AT-01.8` is a green row on a completed thread citing `intent_claude_upgrade.bats`; a grep over `tests/` is (1) with a wider net and would never have found it. **(3) WHAT CHECKS THIS -- A DELETE CAN HOLLOW A SURVIVOR**, and nothing else catches it: `intent_upgrade_orchestrator.bats:151` is PRESENT and passing, and one clause inside it stopped being able to fail. **Three faces of (3) so far**: a survivor's clause goes vacuous (AT-01.6); a survivor's ROW ABOUT the deleted thing is orphaned (the parity register's `5/10 partial burn` becomes a measurement of nothing); and the only thing that made a survivor able to fail is removed. **THE GENERAL FORM: blast radius measured as WHAT WENT RED misses everything that went VACUOUS, and vacuous leaves no corpse.** It earned its keep twice on the day it was written, once by overturning the ruling that produced it -- `no_absolute_home_paths.bats` turned out to carry five real conformance rows, and the three I migrated were exactly the three its own register already classed out-of-scope. **And the estate holds a 272-row invariant nothing guards: 336 cited AT rows, 52 absent, every one `to-write`, zero green.** `stale_at_check.sh` guards the opposite direction and `declared-kind` claims nothing about absent rows; the mirror is the half nobody built.
- **D-POP -- THE INSTRUMENT'S POPULATION EXCLUDED ITS OWN SUBJECT.** Nine instances, none of them the empty set -- every one returned a plausible number. **THE CURES, WHICH ARE THE ONLY PART THAT TRANSFERS:** state the denominator before the finding; positive-control the instrument on a case it MUST catch; and when a narrowing flag is in the command, re-read what it narrowed. `--test X` excludes lib tests, so a flag that narrows the RUN never narrows the CLAIM. Full nine in `.history/20260831/decisions-fold-0723Z.md`.
- **ic's WORDING FOR WHY THE INNOCENT EXPLANATION WINS, AND IT IS THE BEST STATEMENT ANYONE PRODUCED TODAY: _a correct general model is what made me stop measuring._** ic told their operator four times, as fact, that they were blocked on the shared build lock, having never run `ps` -- which would have shown zero rustc. **Not laziness: a true and relevant model is genuinely good evidence, right up until it is the reason you stop looking.**

- **TWO hv RULINGS, 2026-08-30, ARRIVING BETWEEN THE 14:59Z AND 15:02Z CLOCK READS OF THIS SESSION -- THE MINUTE IS NOT RECOVERABLE AND IS NOT INVENTED, because a mid-turn message carries no stamp and the two reads either side are the tightest true bound. FIRST-HAND IN THIS dc SESSION -- hv's own words in answer to two questions dc put in the plan. NOT RELAYED, NOT RECONSTRUCTED, AND THE WORDING BELOW IS hv's RATHER THAN A PARAPHRASE.**

  **(a) EVERYTHING GOES IN 3.0.1, SO THE `bin/` PRUNE IS IN.** hv, verbatim: _"Sure. Everything is in 3.0.1. That is what Intent v3 is supposed to be. Everything we can get into 3.0.1."_ **CONSEQUENCE FOR dc: `AC-12.1` IS LIVE WORK, NOT DEFERRED.** Asked because `AC-14.12`'s descope to ST0069 that morning had measured the WP-14-blocks-WP-12 coupling away, and three documents still called it the live blocker -- so the row had been unblocked for hours with nobody reading it. `AT-12.1` is `to-write`: the row is unsatisfied because no test exists, not because the work is unknown.

  **(b) `shipped_surface_drift` IS AN EXPIRED TEST AND THE v2 GATES ARE DEAD.** hv, verbatim: _"Expired test. We don't care about gates for v2 any more. Don't waste any time on it, other than to remove it."_ **DONE, AND THE SECOND HALF WAS FOUND AT FOLD TIME 2026-08-30**: the test was deleted but the CI step that existed only for it was not, so both jobs still fetched `v2-maintenance` and ran `git rev-parse --verify` on it -- **not inert, that FAILS the job.** Removed from both. **A removal ruled in one sentence had a second site nobody looked for**, which is D-DELETE's third population arriving a week late.

- **D-SHARED -- THE SHARED TREE BREAKS EVERY COMPARISON THAT LOOKS SELF-CONTAINED.** Cost me three times. The rule is W4; the operational half: `git commit --only <paths>` in ONE call (path-scoped, no separate `add` window), and **`unset GIT_INDEX_FILE` BEFORE any reset, as its own call** -- cc, 2026-08-31, after a reset hit the private index and left 30 deletions of their own commit live in the shared one. Instances in `.history/20260831/decisions-fold-0723Z.md`.
- **A PERMISSIVE READER MAKES A CONTRACT VIOLATION AND A CONFORMING IMPLEMENTATION INDISTINGUISHABLE.** A reader that tolerates the malformed case removes the only signal the writer was wrong. Refuse at the read. Detail in `.history/20260831/decisions-fold-0723Z.md`.
- **A VERB CAN BE SEMANTICALLY CORRECT AND STILL BE A SILENT FAILURE, BECAUSE CORRECTNESS IS ABOUT WHAT IT DID AND SILENCE IS ABOUT WHAT IT SAID** (vc's naming, on my `at fc` find; `0158`). I nearly talked myself out of the finding because reading (a) -- that `at fc` is a RECORDING surface and propagating would be laundering with extra steps -- is defensible and turned out to be right. **Reading (a) being right and the verb being broken were never in tension.**

- **THE STORE-MIGRATION GUARD LANDS AFTER THE CUT. PROVENANCE: hv, 2026-08-30 11:51Z, FIRST-HAND IN THIS dc SESSION, hv's own selection from four options dc authored.** Menu: **(A) extend the guard after the cut -- CHOSEN**; (B) extend it now, declined -- an estate-wide test refactor inside a cut vc had just reported as too wide, plus a commit freeze on nine files across three lanes; (C) fix the two proven members now and defer the guard, declined -- **instance-shaped fix under a class-shaped problem, which is W10 done knowingly**; (D) file it and do nothing, declined. **THE GROUND FOR WAITING IS THAT IT IS A DEV-TREE HAZARD WITH NO SHIPPED CONSEQUENCE**, not that it is small.

- **THE CUT GOES AHEAD AGAINST AN OPEN GATE. PROVENANCE: hv, 2026-08-30 11:41Z, FIRST-HAND IN THIS dc SESSION, hv's own selection from four options dc authored -- not relayed, not reconstructed.** **The menu is recorded in full because the choice alone is unrecoverable:** (A) **cut against an open gate -- CHOSEN**, tag 3.0.1 with ST0056 BLOCKED and the tag-gated rows named as such, satisfy them against the published artefact afterwards; (B) re-scope the five to a follow-on thread, declined -- it buys a green by changing the question, and `AC-00.4`'s own lesson is that a criterion moved out of the instrument is not thereby satisfied; (C) two-phase RC-then-real tag, declined -- unless the code is byte-identical the second artefact is not the one the criteria were driven against, so the green may not describe what shipped; (D) fiat-close the five, declined -- **they are not over-cooked, merely not-yet-possible, which is the case `fc` was explicitly not built for.** **THE MEASUREMENT THAT PROMPTED IT, driven this turn and not recalled: five ST0056 criteria require the artefact only the cut produces** -- `AC-00.5` (brew install on a clean machine), `AC-07.7` (every claude door reachable in an INSTALLED build), `AC-11.1` (tagged version yields a working tap formula), `AC-11.4` (published checksum matches published bytes), `AC-12.4` (tagged, released, formula live) -- plus `AC-14.12`, already ruled un-closable before the cut. **So the gate could never read green before the release existed, and `get ST0056 green, then cut` was not achievable in that order.** **CONSEQUENCE FOR dc, AND IT CONVERTS A BLOCKER INTO A RULED OUTCOME: WP-07 and WP-11 not closing before the cut is now the DECIDED shape, not something waiting on hv's hand.** `AC-07.7` stays RED and is recorded as tag-gated rather than as work outstanding.

- **D-FACES -- A DECLARATION HAS CONSUMERS YOU DID NOT WRITE, AND THE COMPILER GUARDS ONLY THE ONES IT CAN SEE.** Every hand-written roster of a generated surface rots; the cure is to derive it or delete it. Executed on `plugin.json`: 3 of 8 command entries were false when removed. Detail in `.history/20260831/decisions-fold-0723Z.md`.
- **A RULING'S PREMISE CAN BE CORRECTLY MEASURED AND STILL NOT COVER WHAT YOU BUILD.** hv's DO-NOT-BUMP measured `tests.status` as unconstrained TEXT -- a claim about the VALUE. `fiat` was a separate FIELD, so it cost a column, a rung and a bump. **Surface the change rather than absorbing it.**
- **hv HAD ALREADY RULED THE THING I WAS INSTRUCTED TO BUILD, THE OTHER WAY.** Read hv's board BODY at pickup, never just its header. **And an OMISSION in an instruction can cost more than an error in it** -- _with one required composer_ was in the ruling's title and never reached me.
- **#fixforward IS A RULING ABOUT WHICH DIRECTION IS CHEAP.** A one-way ladder only sounds unrecoverable while you read it as data; the stale thing was a BUILD ARTEFACT.
- **A DECISION THAT REACHES A QUESTION IT WAS NOT ABOUT IS THE STRONGEST EVIDENCE FOR IT** (vc). Three instances in one night: arm 8 catching a startup panic; one-event-per-entity making skip-derivability answerable; D42's identity/stamp split making a one-transaction cascade possible.
