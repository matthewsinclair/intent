---
node: dc
name: DevX Claude
role: worker
session_id: f396ca31-ec6f-459e-9b7c-40e87aa93efb
heartbeat_at: 2026-08-19 19:55Z
status: active
focus: "**`organize` PREVIEWS BY DEFAULT AND `--apply` PERFORMS -- ic RULED IT, IT IS BUILT, AND IT CLOSES vc's QUIET-COLLISION PATH: the arming moment can no longer arrive with the bare spelling acting.** Measured on a clone: 744 -> 744 at rc 0, twice, byte-identical; `--apply` refuses at rc 1 naming 9 unmet of 19. **PREVIEW AND APPLY ARE ONE BODY TAKING A `Mode`** -- a preview computed by its own path is a promise about what the other path would do, and the two drift where nobody tests. **ic's optional ask found a real gap: stdout said `0 to remove` while stderr said 544 blocked**, so a stdout-only caller read a reconciled estate. **And `intent organize --zzz-not-a-flag` was telling users the command was retired and to delete it from their scripts, at the exit code the gate fails open on** -- seventh site of the reclaim, the one that answers for the word rather than forbidding it. **I broke the v2 CLI for every project on this machine for two minutes with a stash pop; Laksa caught it before I did.**"
claims: [ST0056/11, ST0057/04, ST0057/06]
---

# DevX Claude (dc)

## D42 -- TIME. Read this before writing anything, anywhere.

**DB records have a timestamp field. That is the source of truth for time. Nothing else. Ever.** hv ruled it four times and it was reinterpreted after three of them, twice by me inside ten minutes.

- **You never ask what time it is** -- not the OS, not `date`, not the filesystem, **and not the database either.** Asking SQLite and writing the answer is still writing a time you obtained.
- **The stamp is applied BY the write**, at INSERT/UPDATE/UPSERT/DELETE. Read-then-write leaves a gap two writers interleave in.
- **hv's structural close: NO cli or intentsvcs function TAKES a time.** Functions may RETURN times. **Direction is not symmetric -- IN is forbidden, OUT is fine.**
- **THE RULE IS ABOUT SIGNATURES, NOT VALUES, WHICH IS WHY THIS VERSION HOLDS.** A time-typed input parameter is a defect by inspection. Asking where a caller got a value is a discussion; asking whether a signature accepts one is a grep.
- **The defect is always one step earlier.** Reaching for a clock means you are about to write a time into something that is not a durable record.
- **Not exceptions:** test fixtures; "only reading it"; **"but it came from the database"**; "it's just a label". The third fooled cc, vc and me independently.
- **SCOPE, hv: devbin is NOT Intent** -- external, vendored, no db. My whole D42 directive resolved to a no-op, and reporting that beat inventing work to look responsive.
- **A board stamp is a label, not data.** The ordering that exists and cannot be fabricated is the **commit**.

## The truth model -- canon, held not restated

`design.md` (D01 as reversed) + `data-model.md`. Three points that change what I DO: **the db is the durable SSOT, files are re-creatable**; **the typed API is the only door in**; **migrations are normal.**

## DOING

**THE POLARITY IS RULED AND BUILT: `intent organize` PREVIEWS, `--apply` PERFORMS** (ic, AC-05.1). vc had named this as the one path where hv's _push on_ and _need an adult_ could collide quietly -- the arming moment arriving with the bare spelling still acting. **It cannot now: the default spelling is inert by construction rather than by nobody having typed it.** Table first (`--apply` is `keep` in `new_surface`, ic's ruling as its `disposition_basis`), reader second, which is the recoverable ordering `schema --versions` already records. **The generator refused four undeclared keys until I classified each as read-by-code or prose.**

**MEASURED IN ANGER ON A DISPOSABLE CLONE, 744 FILES UNDER `intent/st`:** bare run **744 -> 744, rc 0, twice, output byte-identical**; `--apply` **744 -> 744, rc 1**, refusal naming 9 unmet of 19 declared. Four mutations, four kills -- and the first killed TWO tests, correctly, because a preview that removes also makes the two reports disagree.

**AND A SHIPPED COMMAND WAS TELLING ITS USERS IT DID NOT EXIST.** `intent organize --zzz-not-a-flag` answered _was retired in Intent v3 ... remove it from any script that calls it_ at **exit 2, the code the pre-commit gate fails open on**. Seventh site of the `4e69acda` reclaim, and the one that ANSWERS for the word rather than forbidding it -- invisible to that sweep's search shape, and only observable once the wiring made `organize` reachable. `surface_check.sh` now reports **all 7 invariants holding across every declared non-retire path**, with the three retirements still absent as declared. Two new arms in `retired_commands.rs`, both driven from the table rather than naming `organize`. **`spine.rs` is ic's file and I opened it without asking; declared, and theirs to overturn.**

**`organize` IS WIRED AND ALL FIVE AT-04 ROWS ARE GREEN (vc, `8130f846`). GATE 41 of 64.** `fc7e25f2` wired the handler; vc verified the clone run independently and it reproduced exactly. **THE NUMBER IS THE DAY'S RESULT: 544 files would have been removed, ZERO were, 744 conserved under `intent/st`, twice, two runs byte-identical.**

**OPEN AND NAMED ON EVERY ONE OF THE FIVE ROWS: the greens cover the MECHANISM, not the WIRING.** The ATs drive `organize::plan` and `Plan::apply` directly, so a regression unwiring the handler leaves all five green with nothing saying so. **I assumed `dispatch_ssot` closed this and vc checked rather than trusting either of us: it does NOT** -- `:475` `continue`s past a command that says it is not implemented yet, so it audits the remedy text of UNIMPLEMENTED commands and never asserts a shipped one is implemented. Not minted, for consistency with ic's AC-03.15 disposition; queued with it. **The argument when the queue opens is that ST0056 AC-06.8 (an inert flag) and AC-05.7 (a discarded argument) already exist, so the COMMAND-level form is a missing member of a family rather than a new idea** (vc).

**AC-00.1's SHIP GATE IS LANDED (`edbd7640`, rebuilt `a3d74334`).** `native/rust/crates/intentsvcs/src/preconditions.rs`, registered in MODULES.md before it was written, plus `dehydration_ship_gate.rs` (21 tests) and the ship gate wired in front of `Plan::apply`'s removal loop. **The two gates are deliberately separate functions: `organize::gate` is AC-04.2's per-file re-render-and-compare, this is AC-00.1's whole-estate ship gate, and a green from either says nothing about the other.**

**OPEN AND NOT MINE TO CLOSE: AT-00.1's ROW CITES `parity/tools/dehydration_gate_check.sh`, WHICH I DID NOT BUILD.** A shell tool cannot drive the refusal without the handler wired, and if it resolved satisfaction itself in shell it would be the second answer the whole row forbids. Reported to vc, who holds the pen.

**Nothing else in flight. WP-04's plan/gate/apply and the `organize` reclaim are landed.** `23e95896` five rows + gate, `dd26d79e` attachment arm, `3dd5265c` apply + AT-04.2/04.4/04.5, `4e69acda` the reclaim across six sites, `ef4b9f16` issue 0033, `ae19c4ef` the WP-scoped exemption remedy. WP-11 archived to `.history/20260819/wip-wp11-signing.md` -- Apple accepted, and it cannot be re-run until 3.0.0 has a real version.

**FIVE WP-04 AT FILES EXIST AND PASS; vc MOVES THE ROWS IN ONE PASS.** AT-04.1 (4), AT-04.2 (6), AT-04.3 (7), AT-04.4 (2 + the positive control), AT-04.5 (4). All mutation-proven. **AT-04.6 is the one that cannot come off a fixture** -- the equality must be driven FROM A FULLY REALISED TREE, and its `ddf8a080` numbers are PROVENANCE, not operands: derive both populations at run time.

## TODO

1. **WIRING THE `organize` HANDLER IS NOW SAFE BY CONSTRUCTION, AND THE SEQUENCING IS STILL hv's.** vc's caution was "wire the gate before the verb"; the gate is wired, and with AC-00.3, AC-00.4, WP-03, WP-06 and WP-07 all unmet the ship gate refuses every removal and says which. **So the destructible moment is closed -- but a peer flagged it, hv sequences work, and I am not going to make the estate destructible-in-principle on my own reading of my own gate.** Asked, not done.
2. **ST0057 WP-06 -- the full text realisation. Assigned to me by vc, four rows, unowned before that.** Second estate-scale consumer of the hydration primitive that `organize`'s apply path already is. **D57 puts it on the critical path in its own words: the human fallback is a PRECONDITION of sparseness, not a nicety that follows it** -- no tree may hold less than everything until a human can get all of it back as text.
3. **THE HYDRATION PRIMITIVE NEEDS ONE NAME AND ic IS WAITING ON IT** for AT-05.1 and AT-08.1. `Plan::apply` is it today; whether that is the surface ic builds against, or something narrower factored out of it, is mine to decide and I have not.
4. **THE RE-CUT IS STILL TWO-THIRDS DONE.** Delivered: the hook's distinct exit code (`3` blocks, `2` still fails open) and the per-guard roster that let `canon-ignore-guard.sh` and `append-only-guard.sh` dispatch at all. **Still owed: `--skip-rust-tests`** -- though its premise moved, see Decisions -- **and Half B's design question** (7 of 13 rules name shellcheck or clippy and the runner can only grep).
5. **THE ACCEPTANCE-GATE FINDING IS NOW CORROBORATED ACROSS TWO PROJECTS AND ROUTED TO vc, WHO HOLDS THE PEN.** Lamplight (three joined sweeps) and Laksa (a live contract) filed the SAME finding this afternoon, independently, neither knowing of the other. **Verified at source: `ac_is_satisfied` at `bin/intent_acceptance:489-507` loops covering ATs and returns on the FIRST green** -- disjunctive, prose never read. **I had been citing `:252` for this since I ranked it; the line is 500, and that is my own _a pin proves identity, not currency_ entry catching me.** Lamplight: six instances, four declaring partiality in the row's own prose unprompted, all six SATISFIED, **zero avoidable gaps -- the instrument is discarding data careful authors already produce.** **They RETRACTED their own proposed workaround within the hour** (a named not-green AT row is a no-op against first-green-wins, proven on `ST0347`), which matters because a partial mitigation that is wrong consumes the appetite for the real one. **Laksa adds a SECOND, NEW defect: `intent at lint` does not check AT id uniqueness -- confirmed by absence, the concept is not in the file.** Joint framing: **the lint checks SHAPE, the gate checks SATISFACTION, neither checks ids are UNIQUE**, and a duplicate id is exactly where first-green-wins gets interesting. **Two homes: both projects measured v2.19.0 bash, which this team no longer runs; v3's `contract::resolve` already disagrees in one direction.** Not mine to rule.
6. **`canon_commit_check.sh`'s MISSING ARM -- bytes with no canon record.** Unchanged: the eligibility contract is single-homed at `Project::classify()`, so a shell restatement would be the fourth list, and **the arm needs the binary to EXPOSE the classification.**
7. **`output-contracts.md` owes a great deal more than five.** Standing: suppression as TRANSMISSION; the refuting datum uncollected in the author's own output; a control that appears to fire and does not; the aggregating formatter; BLOCKED versus BUSY. **Added today:** a pin proves identity not currency; an always-on marker occupies the slot where a signal would go; field count is not maturity; repairing an artefact destroys the specimen; a safe-looking value from a model that has stopped applying; **a test can encode a defect as its expected shape**; **a checker that cannot separate code from prose makes documenting a defect an instance of it**.
8. **`doctor` -- v3 mirror still unchecked.** XS. **And the overhead meter is silent on bats**, correctly, but with a known shape.

## Watch-outs

- **`git checkout -- <file>` IS A MUTATION-RIG TOOL WITH NO UNDO, AND I DESTROYED MY OWN UNCOMMITTED WORK WITH IT.** Cleaning up the `hydrate` mutation battery I reverted a mutation in `src/address.rs` that way and **took `Entity::form` and `Entity::artefact` -- unwritten anywhere else -- with it.** The revert was complete for the thing it undid and silent about the thing it destroyed, which is ic's mutation-residue class pointed the other way. It cost minutes because I had the patch in hand, and **it cost only minutes because no peer was uncommitted in that file** -- on a shared tree the same command discards a peer's work with no warning and no trace. **The rig copies every file it MUTATES; it must also copy every file it REVERTS.** Same rule, and I had written half of it.
- **`cargo test` STOPS AT THE FIRST FAILING BINARY, SO A PLAIN RUN IS A POPULATION THAT EXCLUDES THE ANSWER.** Measured today: `cargo test --workspace` reported **60 binaries and 494 tests**; `--no-fail-fast` on the same tree reported **120 and 825**. Half the estate was unmeasured and the summary line said nothing about it. **`--no-fail-fast`, always, when the question is "what is red".** Same family as `git log -3` and the filtered `grep -oE`, and it arrived the same day I recorded both.

- **`cargo fmt --all` IS A WHOLE-WORKSPACE SECOND WRITER, AND IN THIS SHARED TREE IT LANDS IN EVERY PEER'S UNCOMMITTED FILES.** Measured on me today: I wrote `dehydration_ship_gate.rs` at ~15:47Z and my own patch failed to find its anchor at ~15:49Z, because a peer's `cargo fmt --all` had rustfmt-wrapped a line I had written flat. I had run no formatter. **This is the markdown formatter's defect in a second language** -- and worse, because `--all` reaches crates the author is not working in. My own run at 15:52Z was harmless only by luck: five peer files predated it and rustfmt writes nothing when a file is already clean. **Use `cargo fmt -p <crate>`, and re-read a file before patching it if anything else has run since you wrote it.** (The `str.replace` anchor assertion is what turned this into a visible error rather than a silent no-op -- second time today that rule paid.)

- **`bin/**` IS LIVE ON PATH THROUGH A SYMLINK TO THE WORKING TREE, FOR SIXTEEN PROJECTS, AND UNCOMMITTED EDITS COUNT.** `~/.local/bin/intent -> /Users/matts/Devel/prj/Intent/bin/intent`, and `bin/intent:52` execs every subcommand from that same tree. There is no publication step: the bytes are live when they hit disk, commit or not. **I MUTATION-TESTED `bin/intent_acceptance` IN PLACE TWICE TODAY, INSIDE THE FIX FOR 0033, WITH THE NOTE-DESTROYING EXPRESSION RESTORED.** Two windows of 15-30s where any node running `intent at red` would have silently lost a note. Nothing happened, which is luck. **My own board already carried the rule -- never mutate a file in place while anything runs it -- and I had it aimed at the pre-commit roster, not at `bin/**`.** Mutation-test shell against the clone rig, which I already keep and did not think to use. Rust is harmless by comparison: source files, nobody's PATH. (lamplight-vc, who supplied the deployment fact I could not have seen.)
- **AN ANCHOR ASSERTION PROTECTS THE PATCH AND PROVES NOTHING ABOUT WHETHER THE PATCHER RAN** (ic, measured on themselves within ten minutes of reading my rule). They ran a four-anchor patch behind `cd native/rust && python3 ...` from a cwd that was ALREADY `native/rust`; the `cd` failed, `&&` short-circuited, python never executed, **every anchor assertion went unreached** -- and the next line's `sed` ran fine and printed plausible output. **A guard that is never REACHED is indistinguishable from a guard that PASSED**, which is the roster-nothing-dispatches defect wearing the assertion's own clothes. **The Bash tool's cwd PERSISTS between calls, so a relative `cd` is itself an unchecked anchor.** Absolute paths, always.
- **A `str.replace` THAT MATCHES NOTHING IS A SILENT NO-OP, AND I DIAGNOSED THE TOOL FOR FOUR COMMANDS BEFORE CHECKING MY OWN EDIT.** The test setup I thought I had added was never in the file. **Assert the anchor** -- `assert old in s` -- so a missed match is an error rather than a success that did nothing. Same family as the exit code eaten by `tail`: an operation that reports success while doing nothing.
- **THE CRITIC GATE'S GREPPABLE PROXY READS COMMENTS.** IN-RS-CODE-004 blocked my own fix because the doc explaining the remedy QUOTED the type expression it had removed. Describe, do not quote, in a gated file. **A checker that cannot separate code from prose makes documenting a defect an instance of it.**
- **`--only` SEPARATES FILES, NOT AUTHORS INSIDE ONE FILE.** Twice today two nodes held changes in one file -- `surface/dispatch-table.json` and `render.rs` -- and the only honest move was to carry the peer's work and NAME it in the commit body. The guard against sweeping is real and it stops at the file boundary.
- **A CLAIM ABOUT A MUTABLE SUBJECT MUST NAME ITS REVISION.** I wrote "every binary in this estate carries `dirty-`" and "notes already destroyed leave no trace" today, both unstamped, and the second was false within the hour -- git holds the prior text; it is true of the FILE and false of the REPOSITORY. lamplight-vc made the same error in the other direction and named the rule: **STAMP THE REF INTO THE CLAIM.** Two nodes' observations then compose on sight instead of colliding.
- **`bin/int build all` ALREADY DEFAULTS TO `--release`.** Passing `--release` produces `cargo build --release --release`, which cargo REFUSES. `--debug` is the only profile flag it takes.
- **A BACKGROUND TASK'S REPORTED "exit code 0" IS THE STATUS OF THE WHOLE CHAIN, AND MY CHAIN ENDED IN `tail`.** I was told exit 0 for a build that never ran, twice, and then reasoned about staleness for twenty minutes in binaries whose mtime had not moved. This is the pipe rule below wearing a different hat, and the notification is more convincing than a pipe because it looks like the harness telling you. **Write `echo "RC=$?" > file` on its own line and READ THE FILE.**
- **`native/rust/build-support/` IS OUTSIDE BOTH BINARY PACKAGES.** cargo's default `rerun-if-changed` covers files in the PACKAGE, so the build script's own shared source is not covered by the trigger `source_commit.rs` says tracks the code. Editing it does not re-run the script; touching a file inside the crate does.
- **CHECK THE mtime BEFORE CONCLUDING ANYTHING ABOUT A BINARY.** One `ls -la` would have ended the twenty minutes at the start. An artefact that was not rebuilt cannot tell you anything about the build you think you ran.
- **A `grep -oE` WITH AN ALTERNATION OF THE TWO VALUES YOU EXPECT IS A PROBE WHOSE POPULATION EXCLUDES THE ANSWER.** I filtered embedded shas to `df650a8f|1a8a3cbe`, so a third value could not have been seen. Same class as `git log -3`, committed the same day I wrote that one down.
- **A PIPE EATS THE EXIT CODE AND I DID IT WHILE VERIFYING A TEST SUITE.** `bats ... | tail -20` reported exit 0; that was **`tail`'s** status, and the suite had two real failures. It is on this board as a rule and I still shipped it. **Redirect to a file and read `$?` from the command itself** -- and a suite result you are about to commit on is exactly where the rule is load-bearing.
- **`git log -3` IS A PROBE WHOSE POPULATION CAN EXCLUDE THE ANSWER.** I investigated a moved HEAD with it, saw two commits, and concluded my day's work had vanished. There were FIVE. **Use `git log <known>..HEAD` when the question is "what happened since", never a fixed count.**
- **REPAIRING AN ARTEFACT DESTROYS THE SPECIMEN, AND THE REPAIR IS EXACTLY WHEN NOBODY IS THINKING ABOUT PRESERVATION.** My rebuild overwrote the last live copy of the stale binary without a thought; cc rescued it only because vc happened to mention a path. **Copy before you fix.**
- **`cargo clippy --release` DOES NOT BUILD TEST TARGETS.** I reported "clippy 0 errors" from it while a real error sat in a test file. True, narrower than it sounded, and **the narrowness is exactly where the error lived.** `int check clippy` sees them.
- **A LOCAL PATCH TO A VENDORED FILE IS SUPPORTED, DETECTED, AND STILL USUALLY THE WRONG ANSWER.** The manifest reports an edit rather than overwriting it, so nothing is lost -- but `devbin upgrade` then REFUSES to replace that file, and the fix stops reaching every other consumer. **Fix upstream and re-vendor; `int vendor` says carry-upstream-versus-declared-fork is hv's call and it means it.**

- **ELAPSED TIME CANNOT DISTINGUISH BLOCKED FROM BUSY, AND THAT IS WHY THE SLOWNESS SURVIVED FOUR WRONG EXPLANATIONS.** What settled it was a process STATE and an RSS of 32 KB -- available for two days, never looked at, because I was measuring duration.
- **EVERY TIMING FIGURE IN THIS ESTATE IS SUSPECT UNLESS IT RECORDS WHETHER A COMPILE PRECEDED IT.** Subject and revision are not enough; validation-cache state is a hidden variable that moves a number by 800x. My `16.12s / 100.10s` "noise floor" was this mechanism, not noise.
- **THIS REPO IS A v3 PROJECT** (`3.0.0-dev`; store `intent/.cache/intent.db`, gitignored). v2 refuses PROJECT verbs at exit 2; `critic` is a GLOBAL_COMMAND (my change, `92a51134`). Invoke v3 by explicit path; binaries are at `native/rust/target/release/`, not `release/`.
- **A PARITY TOOL IS AN ATTACHMENT OF ST0056.** Committing one without a sync leaves canon stale and the next peer's sync sweeps it into their commit -- measured: `of_n_population.sh` file at `f789ae48` (mine), canon record at `938ed7a3` (vc's). **Announce, sync, commit file and canon together.**
- **THE MARKDOWN FORMATTER IS A SECOND WRITER AND IT IS NOT A PEER.** It re-aligns tables on save, in the window between sync and commit; the longer the cell you just wrote, the bigger the divergence. `critic-gate.md` diverged by 15,428 bytes that way, 100% table padding.
- **`git diff --stat` CANNOT EXPRESS SIZE ON CANON JSON.** An attachment is ONE LINE, so a 9,740-byte tool and a one-word typo both read as `1 insertion`. Compare structures, never the diffstat.
- **A SINGLE `pgrep` SAMPLE CANNOT DISTINGUISH A DEAD OWNER FROM ONE BETWEEN SPAWNS.** I called a stale-lock reading "definitive" off one sample of an intermittent process. Same class as `bash -n`, same day.
- **`bash -n` IS NOT A SYNTAX CHECK FOR `.bats` FILES** (they are not bash) **and at 3.2 it does not reject bash-4 constructs either.** Two distinct false instruments wearing one command.
- **PUSH `local` ONLY.** `upstream` closed (hv), frozen at `5765c5da`.
- **NEVER `git pull --rebase`; a peer `.git/index.lock` means WAIT. ALWAYS `git commit --only <paths>`** -- a bare commit sweeps a peer's staged index.
- **NEVER mutate a file in place while anything runs it -- ATOMIC REPLACE (write beside it, `mv` over it).** The pre-commit roster runs on every node's every commit.
- **COMMIT BEFORE ANY `intent at` STATUS CHANGE, AND ON v3 THE REASON IS WORSE THAN ISSUE 0033.** v2 destroyed the row note (0033, fixed `ef4b9f16`). **v3 discards EVERY concurrent canon-file edit, on every mutating verb, always** -- vc lost two hand edits to `intent at red` then `at green`, both printing `ok:`. Mechanism verified at source, and it is ONE LAYER DEEPER than a stale in-memory copy: `Facade::open` (`facade.rs:714`) goes through `ingest::load_fresh`, which **returns the STORE's canon and never reads the files unless the store is COLD** (`ingest.rs:338-345`). `at_set` then clones that and applies the WHOLE DOCUMENT (`facade.rs:2477`). So there is no race and no window -- a canon-file edit made at any time before the command is invisible and gets overwritten. **`load_fresh`'s SUMMARY LINE PROMISES A FRESHNESS CONDITION ITS OWN BODY REMOVED** (`ingest.rs:301`: "from the STORE when the working tree has not moved, and from canon only when it has"); the body says the whole-tree hash was taken OFF this path deliberately. **And the design names `doctor` as the mitigation -- a REPORT nobody is obliged to run, standing in front of a WRITE that destroys, while the NON-destructive `sync --to-store` is the direction that warns.** vc's finding, verified and sharpened here; theirs to carry. **Route status changes through vc**, who holds the canon pen.
- **NEVER `$?` AFTER A PIPE.** **`grep -c` EXITS 1 ON ZERO**, so a `||` fallback fires on a true zero. **`exit` INSIDE `$( )` EXITS ONLY THE SUBSHELL.**
- **MY SHELL IS zsh: it does NOT word-split an unquoted `$var`.** Quote, or use a bash array.
- **`TZ=UTC git log --date=format:` DOES NOT RESPECT `TZ` -- it renders the COMMIT'S OWN recorded zone, so a format string ending in a literal `Z` is a lie you typed yourself.** Only `--date=format-local:` honours the environment. The two differ by exactly the local offset and the wrong one looks perfect (cc, 2026-08-19, committed while verifying MY timestamps in a message about being careful). **Cross-check with `date -u -r` or convert by hand from `--date=iso`; my own durable record survived only because I converted the offset rather than trusting a format string.**
- **`git log` PRINTS LOCAL TIME**; reading one and appending `Z` is wrong by exactly the offset and looks perfect.
- **`cp` ONTO A SYMLINK FOLLOWS IT AND WRITES THROUGH TO THE TARGET.** A rig assembled by symlinking into the real tree is not isolated.
- **A `git stash push` WITH A PATHSPEC THAT MATCHES NOTHING CREATES NOTHING, AND THE `pop` ON THE NEXT LINE TAKES SOMEBODY ELSE'S STASH.** I ran it from `native/rust`, so `surface/` resolved as `native/rust/surface/` and the push failed at exit 1 that I never read. The `pop` then applied a stash from **2023** -- conflict markers into `bin/intent_helpers`, `.intent/config.json` resurrected from before the v2.10.0 relocation. **`pop` PRINTS "The stash entry is kept in case you need it again", WHICH READS AS REASSURANCE AND IS THE FAILURE NOTICE** -- I read it and heard "your stash is safe" rather than "the apply did not complete". A failed push and a successful one are distinguishable only by an exit code, and the pop cannot tell it is popping work from another decade.
- **THIS WORKING TREE IS SHARED INFRASTRUCTURE FOR ONE FILE, AND MY OWN TREE COULD NOT SHOW ME THE BLAST RADIUS.** v2 `intent` is installed FROM `bin/`, so those conflict markers broke the CLI in every other project on this machine while I was inside a Rust build that touches no bash. **Laksa's ic reported it from another project before I noticed it in this one**, and their sharpest datum is the partial failure: `intent at help` died while `intent at lint` and `intent ac gate` kept working, so the CLI presents as ALIVE and the first thing a node in a consuming project does with an unexplained crash is doubt its own invocation. **The diagnosis lands on the wrong node, in the wrong project, with no way to see the cause.** A conflict-marker guard on `intent_helpers` would fire where the fault is experienced; proposed to vc, credited to Laksa.
- **A GUARD'S DOC COMMENT CAN ASSERT AN INVARIANT THE GUARD DOES NOT HAVE, AND THE COMMENT IS WHY NOBODY CHECKS.** `retired_refusal` opened with _a name clap can parse is dispatched and never reaches here, so this can only ever improve a message that was already a failure_. **True for a name clap parses SUCCESSFULLY; false for a USAGE ERROR on a live command**, which lands in the same branch carrying a name a retired row may also spell. The comment was load-bearing in review and wrong at the one input nobody probed.
- **THE FULL-SUITE FAILURE SET WAS DISJOINT ACROSS TWO RUNS AN HOUR APART -- 4 failures, then 9, WITH NO OVERLAP.** Several of these tests read the REAL estate (`..._in_the_estate_...`) while four peers write to it, so a red is as likely to be a peer mid-edit as a defect. **"What is red" is not answerable from one full-suite run on a shared tree**; a target run of the files you touched is the measurement, and the workspace number is context. Same family as `--no-fail-fast` below and worse, because this one is not fixed by a flag.
- **IN A FOUR-NODE CHECKOUT, PRESENCE IDENTIFIES A FILE AND NEVER ITS AUTHOR. FOUR MISATTRIBUTIONS BY ME TODAY, THREE ONTO cc** (their diagnosis, and it is right). Every one ran the same way: **a file appeared in the shared tree between two of my measurements and I inferred the author from who was most recently active in the area.** `git log` and `git status` structurally cannot answer it, because the file is untracked precisely while it is in flight. **THE REMEDY WAS ALREADY IN THE ESTATE AND MY OWN PRE-COMMIT GATE PRINTED IT AT ME:** `stale AT rows: AT-08.1 cites .../write_path_canon_always.rs`. **The AT row that cites a file names its owner.** Read the contract, not the working tree.
- **A COMMAND WHOSE BLAST RADIUS EXCEEDS THE THING IT WAS AIMED AT, RUN INSIDE A RIG, ON A TREE WITH FOUR WRITERS -- TWICE IN ONE AFTERNOON, BY TWO NODES.** Mine was the stash pop; cc's was `git reset --hard` in a throwaway worktree to clear a planted divergence, which took their uncommitted prototype. **`git checkout --` is the third member and I recorded it this morning as if it were one incident.** It is a class: the rig reaches for the biggest hammer because the rig's own state is what it means to clear, and the tree does not know that. **The scoped forms exist -- `git restore --staged <path>`, an explicit pathspec, a copy taken first -- and the rig has to prefer them by construction rather than by whoever is running it remembering.**
- **A control refuses; documentation reminds; only one is load-bearing.** Demonstrated on me today: I wrote `${INTENT_PROJECT_ROOT}/bin/intent` instead of `$INTENT_BIN` **inside the file arguing against greens that mean nothing**, and a guard caught it, first time, mechanically.

## Decisions

- (2026-08-19) **A COURTESY SPELLING AND A DOCUMENTED ALIAS ARE TWO DISPOSITIONS, SO THEY ARE TWO LISTS.** hv: _handle `organise` and `organize` but only ever show the `z` version._ `aliases` is registered VISIBLE and its comment argues correctly for that -- v2 documents `done|notdone`, so hiding one would be _a different way of not shipping it_. **That reasoning is exactly what does not apply to a courtesy spelling: rendered anywhere it becomes a second documented NAME for the verb**, which is the outcome the ruling exists to prevent. One list with a flag is a decision nobody remembers at the second call site; **two lists cannot be got wrong by forgetting.** And the TEST's subject is the LEAK, not the acceptance -- if a hidden spelling ever appears in output, something echoed the user's word back instead of naming the verb.
- (2026-08-19) **FIXING THE GENERAL SHAPE HELD A CASE THAT DID NOT EXIST YET, EIGHT HOURS LATER.** `retired_refusal`'s obvious cheap fix was to check the first token. I walked the BUILT SURFACE token by token instead. **That afternoon fix is the only reason hv's evening `organise` ruling did not silently un-retire the v2 `st organise` face** -- the same word now carries two dispositions by family, and the token walk is what tells them apart. Not luck: pattern-matching the word would have failed, and walking the structure could not.
- (2026-08-19) **THE TEST FOR ACTING ON A RELAYED INSTRUCTION IS REVERSIBILITY AND DIRECTION, NOT THE PRESENCE OF A CANON RECORD** (vc, ratifying my declaration). This morning I stopped on the `upstream` push: **outward-facing, irreversible once it leaves the machine, and a PERMISSION rather than a piece of work** -- and a permission relayed by a peer is not a permission. `organise` is additive, internal, reversible in one commit, touches no destructive path. **Same evidentiary standing, opposite risk profile, opposite correct answer.** Making the canon record the test would require every hv instruction to round-trip through canon before anyone could act -- a worse estate. **What a canon record buys is that a LATER reader can check it; what reversibility buys is that being wrong costs one commit.**
- (2026-08-19) **A ROW DECLARING A VERB THE MODEL CANNOT EXPRESS IS A ROW THAT LIES, SO I DID NOT AUTHOR IT.** hv ruled `st hydrate` / `st dehydrate` / `st new --dehydrate`, and that it must work for ISSUES as well as threads. **Two problems, both raised rather than guessed:** `st` IS the steel-thread family, so an issue id inside it is a second entity type in a family named for the first -- and `Facade::hydrate` already takes an `Address`, which spans both, so a top-level verb needs no duplication. **And `intentfiles.rs:62` `Sigil` is `SteelThread | Issue`: every line says REALISE THIS and there is no negative form.** Hydrating a completed thread is expressible; **dehydrating a wip one is not.** That is a model change touching `parse`, `render` and `organize::plan`'s five rows -- five green ATs of mine. **The override must be expressible in the AUTHORED region: one written into the generated region survives exactly until the thing it overrides next runs.**
- (2026-08-19) **`intent organize` PREVIEWS BY DEFAULT AND `--apply` PERFORMS** (ic's ruling on AC-05.1; built, mutation-proven, measured). The grounds are precedent, not taste: **AC-03.9 already ruled that the destructive direction states what it will overwrite BEFORE it costs it**, and a second destructive verb answering that differently is Highlander arriving in the surface. v2 shipped BOTH polarities for this one operation. **`--apply` rather than `--dry-run` because `--dry-run` makes safety opt-in, and the operator who most needs the preview is the one who does not know to ask for it.**
- (2026-08-19) **PREVIEW AND APPLY ARE ONE BODY TAKING A `Mode`, NOT TWO IMPLEMENTATIONS THAT AGREE BY INSPECTION.** A preview computed by its own path is a PROMISE about what the other path would do, and the two drift in the direction nobody tests -- **the preview stays green while the act changes underneath it.** So the central test runs the same plan over the same tree in both modes and requires the two `Report`s to be identical, which holds only if the preview classified everything the run classifies AND left the world alone. An enum rather than a `bool` because a `bool` at that seam must be remembered at every call site, which is how v2's two faces came to disagree.
- (2026-08-19) **A PREVIEW CARRYING REFUSALS EXITS 0; ONLY `--apply` MOVES THE CODE.** Nineteen preconditions are unmet, so **every preview on this estate carries one** -- a non-zero preview would make the bare, default spelling of a routine verb fail permanently on a healthy project. That is the always-on alarm the digest guard already refuses to be, arriving one layer up at the exit code, and an always-failing default is how an operator learns to stop reading this command's output at all.
- (2026-08-19) **THE REFUSAL COUNT BELONGS ON STDOUT, AND ic's "I WILL NOT INSIST ON IT" ASK IS WHAT FOUND IT.** Removals were already counted separately, so I nearly answered "already done". Then I ran it: **stdout read `0 to remove` while stderr read `would remove 544 file(s)`.** Both true, answering different questions -- but INV-01 puts the refusal TEXT on stderr, so **a caller capturing only stdout saw `0 to remove, 0 diverged` at exit 0 and had every reason to conclude the estate was reconciled.** It is 544 removals behind a shut gate.
- (2026-08-19) **A SHIPPED COMMAND TOLD ITS USER IT DID NOT EXIST, AND THE SWEEP THAT SHOULD HAVE CAUGHT IT COULD NOT SEE THE SITE.** `intent organize --zzz-not-a-flag` answered _was retired in Intent v3 ... remove it from any script that calls it_ at exit 2 -- **confident, actionable, wrong in the direction that costs working automation, and at the code the pre-commit gate FAILS OPEN on.** `4e69acda` is titled _six sites that forbade the word_; this is the seventh and it **answers for** the word rather than forbidding it, so the sweep's own search shape excluded it. It became observable only when the wiring made `organize` reachable and `surface_check.sh` began probing it. **Fixed by asking the BUILT SURFACE rather than the table** -- reachability walked token by token, so `st organize` stays retired while `organize` is live.
- (2026-08-19) **A RETURN VALUE DERIVED FROM THE WORK DONE INSTEAD OF THE STATE REACHED GIVES TWO ANSWERS FOR ONE QUESTION.** `hydrate` first returned the plan's steps; `plan` deliberately emits NO step for an attachment already agreeing with the store, **so the first call returned six paths and the second returned four -- same artefact, same tree.** A caller asking _does my file exist now_ is told no on the run where nothing needed doing. **My own test caught it, and only because ic insisted the contract be `paths that now exist` rather than `paths written`.** The set is now asked of the two OWNERS -- `views::render_all` for views, canon's `attachments` for attachments -- rather than reconstructed from the actions taken.
- (2026-08-19) **A PRESENCE CHECK GOVERNING TWO STEPS SKIPS THE SECOND ONE IN THE ORDINARY CASE** (ic, catching a defect in my plan before it existed). `hydrate` must materialise-if-absent and pin-if-not-pinned as INDEPENDENT idempotent steps. An early return on file presence skips the pin exactly when the artefact is on disk because it is `wip`, its id is in the GENERATED region, and it is not pinned -- **presence true, pinned-ness false, disagreeing on the common path rather than in a corner.** `edit_writes_pinned_region.rs` already red that, so the estate had the test before it had the caller. **Pinning is what makes the decision outlive STATUS.**

- (2026-08-19) **AN ADDRESS IS STRUCTURALLY SILENT ABOUT EVERYWHERE IT DOES NOT POINT, AND I CONCEDED THAT IN THE SENTENCE I THOUGHT WAS MY STRONG ONE** (vc's sharpening). My defence of the hard-coded address was _a wrong address finds nothing and finding nothing refuses_ -- entirely about the carrier you POINT AT. **AC-00.1's failure is a SECOND carrier existing somewhere you are not pointing.** So `two_carriers_anywhere_in_the_estate_refuse` is not a better test of the property; **it is the FIRST test of it.** The delimiter scan makes the declaration single by MEASUREMENT. The address that cannot go stale is the one that does not exist.
- (2026-08-19) **A GREPPABLE PROXY CAUGHT A SEMANTIC DEFECT NO SEMANTIC CHECK EXISTED FOR -- THE EXACT INVERSE OF ST0039.** `no_shipped_string_literal_carries_pm_state` fired on two constants and the real defect was three levels deeper: **this gate ships inside a binary other projects run, and in a consumer's estate there is no ST0057**, so my refusal would have named a thread they cannot read, satisfy, or tell is not about them. **A proxy is not weak because it is a grep; it is weak when it cannot be honoured** (vc). And cc declining to fix it -- rather than renaming the constants or adding an exemption, either of which would have gone green -- is what left the defect visible.
- (2026-08-19) **THE CAUTIOUS-LOOKING OPTION BOUGHT THE LEAST EVIDENCE WHERE EVIDENCE MATTERED MOST, AND BOTH vc AND I HAD IT BACKWARDS.** We treated wire-later as the safe choice. hv's ground: **wiring NOW means every run between here and the last green exercises the REFUSAL path in anger**, whereas wiring late puts the first run that matters next to the permitting state with the refusal barely driven. **Measured on the first contact with a real estate: 544 removals refused.** Wire-before-gate deletes them at rc=0.
- (2026-08-19) **THE UNIT OF AUTHORSHIP IS THE FILE AND EVERY FORMATTER WE REACHED FOR TAKES A BIGGER UNIT.** Four second writers today -- the markdown formatter over 266 views, `cargo fmt --all` over the workspace, `cargo fmt -p` over a crate, `.prettierignore`'s hand-written globs. **Each fix narrowed the blast radius by one level and each was still too wide**, and I used `-p` again within the hour of correcting it, touching only my own files by luck rather than discipline. **The durable form is not a narrower flag: derive the set from the OWNER and red when the tool's set disagrees** -- AT-03.18 asking `views::render_all` for its own roster is the shape (cc, via vc).
- (2026-08-19) **199 UNCLAIMED IS A POPULATION NO FIXTURE CONTAINS.** TAP baselines, probe TSVs, tooling output -- files a human put under a thread that the renderer cannot produce, found only by running the verb on a real tree. **The fifth row says `organize` must not be the thing that decides an unrecognised file is rubbish, and 199 is what makes that a rule rather than a scruple.** cc and vc independently ruled the same files out of WP-03 on the `.tap` question.

- (2026-08-19) **TWO CORRECT MEASUREMENTS OF A TREE THREE NODES ARE WRITING CAN DISAGREE, AND NEITHER IS AN ERROR** (vc's class, reached from my wrong reading of their claim). I saw ONE drifted schema face; vc saw THREE, with verbatim output, twenty minutes apart. **I checked their claim -- right -- and then explained the difference by inferring they had read the test's roster rather than its failure body. That inference was wrong, and I made it in the message where I was correcting them for making one.** cc regenerated two faces between the reads; both measurements were sound. **A test result normally carries reproducibility for free and in this estate it does not**, so a cross-node measurement claim must name its revision AND its clock, and a disagreement between nodes is a question about WHEN before it is a question about WHO. The checking was right; the explanation should have been "our trees differ" before "your reading was wrong". (Since: the arm passes and five faces are dirty -- the state three drifted faces predicts.)
- (2026-08-19) **MY GATE OPENS BY CONSTRUCTION THE MOMENT THE LAST PRECONDITION GOES GREEN, AND vc NAMED THE COST BETTER THAN I DID.** I argued the wiring decision was hv's on protocol grounds. vc's ground is stronger: **"wiring cannot dehydrate anything" is a claim about the ESTATE'S CURRENT STATE, not a property of the code** -- so the safety is a coincidence of today's board and evaporates the day WP-03 or WP-06 lands, with nobody re-deciding. Declaration-as-ratification is the intended semantics, but **an ARMING step separate from the declaration would answer it -- and would also be a second axis in one key**, which is the shape vc caught me smuggling in earlier today. Not built, not proposed; hv's to rule.

- (2026-08-19) **A GATE'S ADDRESS IS NOT A COPY OF WHAT IT GUARDS, AND THAT IS WHAT MADE AC-00.1 IMPLEMENTABLE AT ALL.** The row forbids a hand-copied subset of its own precondition list, so a gate holding the list is the defect. Two constants naming WHERE the declaration lives are not the same object: a wrong address finds nothing, and finding nothing refuses. **The general form: an address fails LOUD, a copy fails SILENT and in the safe-looking direction** -- a gate checking a stale subset still refuses things, so nothing reports it.
- (2026-08-19) **PRIVATE FIELDS ARE WHY REFUSE-BY-DEFAULT IS STRUCTURAL RATHER THAN POLITE.** `Verdict` has no `Result` a caller could `unwrap_or` into permission and no way to hand-write a permitting value -- so a test that wants removals must build canon the real declaration is really satisfied in. **The first draft had public fields and it would have been correct and unenforceable**: the first caller in a hurry is always a test, and a test that fabricates permission removes the gate for everyone downstream while staying green.
- (2026-08-19) **A MUTATION FOUND A VACUOUS ASSERTION THAT REVIEW HAD PASSED, AND IT WAS MINE.** Deleting the ship gate from `apply` left `one_refusal_covers_the_run` GREEN: the test left the tree empty, so the per-file gate could not READ the files, every removal was refused for an unrelated reason, and `report.dehydrated` was empty no matter what the gate did. **An assertion that cannot fail reads exactly like one that holds.** The files are now materialised from the plan's own render -- and from the plan's OWN bytes, because hand-written bytes would route the test through AC-04.2's refusal, which is a different gate.
- (2026-08-19) **DESCOPED AND WITHDRAWN ARE UNMET, OR `intent ac descope` IS A ONE-COMMAND GATE OPENER.** Dropping a precondition is not meeting it. The refusal names them distinctly so an operator sees the criterion was dropped rather than failed.
- (2026-08-19) **AN EMPTY DECLARATION REFUSES RATHER THAN OPENING, BECAUSE THIS GATE IS SCAFFOLDING WITH A DEFINED END.** An empty block is far likelier to be a deletion nobody noticed than a considered all-clear -- and the considered version DELETES the gate in the commit that ships dehydration, rather than emptying its input.
- (2026-08-19) **THE SHIP GATE IS CONSULTED BEFORE THE DIGEST GUARD, AND THE ORDER IS NOT COSMETIC.** A run already forbidden from removing anything has no irreversible step for the digest to protect, and `TreeMoved` is a hard `Err` that would take the safe hydration half down with it. **The same shape as putting the refusal at `publish` rather than `stage`: a guard standing over a step nobody is going to take is the guard that gets worked around.**

- (2026-08-19) **A TEST CAN ENCODE A DEFECT AS ITS EXPECTED SHAPE, AND THEN A GREEN SUITE IS EVIDENCE THE DEFECT IS REQUIRED.** Issue 0033: `intent at red|green` destroyed the row note on every transition, and TWO tests anchored `status: green$` against a fixture row carrying ` -- red-first`, so they passed ONLY when the note had been destroyed. Twelve green tests over a live data-loss bug were not a contradiction, they were a consequence. Three writers, three curators, ZERO readers of that field -- **a field nothing reads cannot be checked by anything.** Banked as canon in Lamplight.
- (2026-08-19) **A BLIND POSITIVE CONTROL IS WORTH MORE THAN ANY CONTROL ITS AUTHOR CAN WRITE.** Lamplight's ic exercised the 0033 fix on a real row with a real multi-clause note, hours later, not knowing the fix existed, and found out because their own restore script's assertion FAILED on a state it was not built to look for. **Every way an author's control can be unconsciously shaped to pass is removed by the author not being involved** -- and I hit three such shapings today from the inside.
- (2026-08-19) **MEASURE THE REFACTOR BEFORE ESCALATING IT.** The gate chased a fix out of `guide.rs` into `render.rs`, where seven pre-existing String-error signatures sat in a 2,329-line file a peer was editing. 53 conversion call sites reads as a refactor; `fail()` is the ONE conversion site, so changing IT left all 53 untouched. Seven signatures, six wraps, zero work. **I had the escalation half-written.**
- (2026-08-19) **THE POSITIVE CONTROL CAUGHT THE BUG, NOT ME, AND ONLY THE CONTROL COULD HAVE.** `apply` classified a `Verify` AFTER its write, so every rewrite reported itself unchanged -- and AC-04.4 is measured on exactly that distinction. Under the mutation **the quiet arm stays green and only the control fails**, because "zero mtimes moved" is produced equally by a correct skip and by an instrument that cannot see movement.
- (2026-08-19) **A RATIFICATION EXPRESSED AS A LITERAL CANNOT TELL RECLAMATION FROM RESURRECTION.** hv reclaimed `organize` for v3; SIX sites hard-coded the string and one was known. Five are now derived from the table. **And `path` stopped being unique across the dispatch table** -- it is a parity register AND a shipping declaration, and they now hold the same word, so every check keyed on path alone conflated them.
- (2026-08-19) **A REMEDY THAT IS EITHER INERT OR CATASTROPHIC, WITH NOTHING IN THE SENTENCE DISTINGUISHING THEM.** The WP-scoped gate told operators to declare `acceptance: exempt` "in the frontmatter" -- the WP's own `info.md` reaches nothing, and the thread's `acceptance.md` voids every row in the thread. Fixed to name the file AND **print the count it would void**, because a number in the operator's terminal is a different object from an argument in a report.
- (2026-08-19) **THE NARROWING IS PROVEN ON A SHIPPING ARTEFACT, NOT A MUTATION.** Whole tree dirty (three boards modified), `native/rust` clean, and the rebuilt binaries emit a BARE sha at HEAD -- the exact condition that used to set `dirty-` on every binary in the estate including a release build of precisely HEAD. cc confirmed the hashes independently. **First time in this estate a marker has carried information rather than occupied the slot where one would go.**
- (2026-08-19) **A COMMENT DESCRIBING CODE IT DOES NOT LIVE BESIDE HAS NO STRUCTURAL REMEDY** (cc's shape, my defect). I claimed in `source_commit.rs` that `int macos stage` keeps a whole-tree REFUSAL; it records and reports, and the refusal is at `publish`. **The argument was true and its evidence named the wrong step**, so a reader auditing the narrowing's safety went to `stage` and got one of two wrong answers -- cc took one within hours and told me my pipeline would stop. The sting: the first instance of this class in `cmd_stage` was fixed STRUCTURALLY, by renaming `traceable` to `checkout_clean`, on the explicit ground that a sentence can go wrong again -- and I put the same claim about the same step back into prose the same day.
- (2026-08-19) **PUT THE CONTROL WHERE THE HARM IS, AND THAT MAKES THE PIPELINE TESTABLE.** `stage` records `checkout_clean` and reports; `publish` refuses. A whole-tree refusal at `stage` would make every step untestable without a pristine checkout -- which is the guard that gets worked around rather than kept. It is why I could exercise the whole pipeline today with three boards uncommitted.
- (2026-08-19) **DO NOT HAND-PUSH A TAP FORMULA, AND MY OWN MORNING PLAN SAID TO.** A formula in a public tap is an INSTALLER, not a document; pointing it at assets that do not exist yet publishes a 404 to strangers. `cmd_publish` uploads, RE-DOWNLOADS, verifies the hashes it is about to publish, and only then pushes the formula -- atomically and in that order. I corrected the plan to vc before anyone built on it.
- (2026-08-19) **THREE TIMES TODAY A FRESHLY-MINTED CLASS MADE ME READIER TO SEE AN INSTANCE THAN THE EVIDENCE WARRANTED** (ic named the shape, having done it twice in the opposite direction). The overhead meter's "model has stopped applying"; a build-script staleness "defect" that was already measured, priced and deliberate with the naive fix analysed as strictly worse. **Both were caught by going back to the subject, neither by thinking harder about the class.**
- (2026-08-19) **A REAL EFFECT DOES NOT MAKE A REFUTED MECHANISM CORRECT.** My Gatekeeper finding was published to four nodes, acted on by two, and did not reproduce: 40 fresh never-executed binaries ran in 364ms against a claimed ~30s each. **The 99% overhead was real and my cause was not**, and the clean slate that removed it also produced the population that refuted me. **The apparatus that could settle it was unavailable for two days and arrived as a side effect of an unrelated instruction** -- so this could not have been refuted by care, only by materials.
- (2026-08-19) **A PIN PROVES IDENTITY, NOT CURRENCY.** `f2e4d1f9005d0334` matched its record exactly for ten hours while refusing every project verb. Minted as AC-11.7 with the demotion REFUSED (vc): the source commit fails at IDENTITY too -- one marker on two structurally different artefacts, measured -- so **two primary fields, each naming the question it answers.**
- (2026-08-19) **TWO DURATIONS, NEITHER BANKABLE ALONE, DIVIDED, GIVE A MEASUREMENT THAT IS** (ic's decomposition, from my own table). Summed test time is a property of the ARTEFACT, wall of the APPARATUS, **and the ratio measures the apparatus while naming no cause** -- which is why it works where five explanations did not. Shipped in devbin `dbc1564`.
- (2026-08-19) **A VALUE IN THE SAFE-LOOKING DIRECTION PRODUCED BY A MODEL THAT HAS STOPPED APPLYING.** The overhead meter reads below 1 the moment binaries parallelise, and that is not a quiet apparatus -- it is the model failing. It reports UNDEFINED. **The instrument detects its own obsolescence**, because the person who changes the runner will never read a note about a ratio.
- (2026-08-19) **A GATE FAILS OPEN ON ITS OWN BREAKAGE AND CLOSED ON YOURS.** Exit 2 (invocation error) still fails open; exit 3 (a rule the project ARMED could not be enforced) blocks. One code cannot say both, which is why the refusal and the hook branch had to land in one commit.
- (2026-08-19) **A GUARD NOTHING DISPATCHES IS INDISTINGUISHABLE FROM A GUARD THAT PASSES.** `canon-ignore-guard.sh` was written, shipped, and named in no roster. **Invisible non-enforcement occurring inside the mechanism built to end it.**
- (2026-08-19) **A WRITE SURFACE WITH NO NAMED READER IS A QUEUE, NOT A CHANNEL** (vc's diagnosis, sharper than mine). Four nodes wrote correctly into hv inboxes for four days and hv read none of it. **Not one write failed, so nothing reported the gap.** The protocol now names the reader; Intent's roster names vc.
- (2026-08-19) **FIELD COUNT IS NOT MATURITY** (vc). Three nodes cited `dist-provenance.txt` as the exemplar because it had three fields where the others had one. It states a commit truthfully and is silent that the commit is 805 behind -- the hash pin's failure one layer up.

- (2026-08-19) **AN INSTRUMENT THAT MEASURES DURATION CANNOT SEE WHY.** Four wrong explanations for the rust slowness over two days, every one reasoned from elapsed time. The answer came from a process STATE (`S`) and an RSS (32 KB) -- **the binary had not begun executing.** Duration is the one observable that cannot separate blocked from busy, and it is the one everybody reaches for.
- (2026-08-19) **A CONTROL GROUP CHOSEN WITHOUT KNOWING THE MECHANISM IS NOT A CONTROL.** Lamplight-versus-Intent was a real experiment that refuted my own hypothesis, and it was still void: I varied binary count and unknowingly varied validation-cache state. **It killed the right work for a wrong reason for a full day.**
- (2026-08-19) **THINKING HARD ABOUT A CLASS DOES NOT PROTECT YOU FROM IT.** I committed the dispatcher-spelling violation inside the file whose entire subject is greens that mean nothing, on the day I built the instrument for it, while holding the argument in my head. **No care reached it; a control did, first time.**
- (2026-08-19) **A COST WRITTEN DOWN IS NOT A CONSTRAINT UNTIL IT IS A NUMBER.** `critic-gate.md` said IN-SH-CODE-005 would fire on every correct use. I armed it anyway on "noisy beats silent", and it blocked my own commit within the hour -- 13 findings, 13 false positives, **395 hits in 87 of 128 shell files**.
- (2026-08-19) **A COUNT INSIDE A CRITERION IS A RECORD WHOSE SUBJECT CAN DIE UNNOTICED.** AC-00.11's `10 of 41` came from a probe that was never committed; my reconstruction returns 14. ic found the criterion's other figure unverifiable within the hour. **Remedy is not a third number: name the revision, or mark it RECORDED.**
- (2026-08-19) **TWO POPULATIONS THAT COINCIDE ONLY IN A NUMERAL READ AS A CROSS-REFERENCE AND DO NO SUCH WORK.** `10 gated of 17 rostered` versus `10 of 41`; five of the ten gated carry no path shape, so half a "drive the gated ten" plan would have passed vacuously.
- (2026-08-19) **A CHECK THAT WALKS THE RECORD CANNOT SEE WHAT THE RECORD OMITS.** `canon_commit_check.sh` asks whether every RECORDED attachment's bytes are present; a file with no record is never in the population. **A denominator drawn from the record rather than the estate**, inside the tool built to police canon/commit agreement.
- (2026-08-19) **A WORKFLOW WHOSE CORRECT FORM REQUIRES AN OPERATION ONLY SAFE FOR ONE ACTOR IS A SINGLE-WRITER BOTTLENECK WEARING A PER-NODE PROCEDURE'S CLOTHES.** And the second writer in the sync-to-commit window is not a peer at all -- it is the formatter, so no amount of peer coordination closes it.
- (2026-08-19) **A FIFTH VALUE IN A KEY IS HOW AN AXIS GETS SMUGGLED IN** (vc, catching mine before it landed). Arming and run disposition are two axes. vc's general form, from three same-day instances: **a checker that verifies MEMBERSHIP in a vocabulary never verifies that the vocabulary can EXPRESS the states in use** -- and the forced-wrong value always reads as MORE finished.
- (2026-08-19) **A CORRECT DECISION PROCEDURE THAT RETURNS "ASK" IS NOT INVALIDATED BY THE ANSWER BEING "YES"** (vc). Three nodes stood off a lock that turned out to be stale, on reasoning that was right anyway.
