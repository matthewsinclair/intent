---
node: dc
name: DevX Claude
role: worker
session_id: 3f2f5de2-d774-44db-8f1f-c85588606969
heartbeat_at: 2026-09-01 13:51Z
status: active
focus: "ON HOLD BY hv, STATED FIRST-HAND 2026-09-01 13:51Z -- every node is down while a devbin change to how rust is built and managed rolls out, and THE ALL-CLEAR COMES FROM hv, not from a quiet tree. Do not start work, do not build, do not touch native/rust. Status stays active; a hold is not a release. Board folded 2026-09-01 13:34Z, pre-fold sha-verified at .history/20260901/wip-prefold-1334Z.md (fe941c83), reasoning at rust-consolidation-narrative-1334Z.md. NO FIGURES HERE -- run the verbs."
claims: [ST0056/04, ST0056/05, ST0056/07, ST0056/11, ST0056/12, ST0058, ST0066]
---

# DevX Claude (dc)

**COLD-SESSION MINIMUM. A rule is never dropped here, only its narrative.** Today's reasoning: `.history/20260831/` -- `census-narrative-0931Z.md`, `day-narrative-1049Z.md`, `ac0201-narrative-1300Z.md`, `decisions-fold-0723Z.md`.

## D42 -- TIME. Read before writing anything, anywhere.

- **You never ask what time it is** -- not the OS, not `date`, not the filesystem, **not the database.** The stamp is applied BY the write.
- **THE RULE IS ABOUT SIGNATURES: NO cli or intentsvcs function TAKES a time.** They may RETURN times.
- **A board stamp is a label, not data** -- read from `date -u` and PASTE, **per stamp, never per session.**
- **`stat`, `git log`, `ls -la` ALL PRINT LOCAL.** Appending `Z` to a local read is an ASSERTION, not a format.
- **A ULID is an IDENTITY and the `ts` is the STAMP** -- why `Envelope::minted` may generate the id in Rust without breaching D42.

## The truth model and the environment

- `design.md` (D01 reversed) + `data-model.md`. **The db is the durable SSOT; the typed API is the only door in.** Crates: `intent-cli`, `intentd`, `intentsvcs`. **`intentdb` IS RETIRED.**
- **`intent` ON PATH IS v3 AND RESOLVES INTO THIS TREE** -- `~/.local/bin/intent` and `~/bin/intent` both symlink `native/rust/target/release/intent`. **A release build in progress makes it transiently ABSENT**, which reads as "not installed".
- **THE STORE IS PROJECT-RELATIVE**, `intent_dir()/.cache/intent.db`, found by walking up from CWD. **SCHEMA 16.**
- **THE INDEX IS SHARED.** `add` + `commit --only <paths>` in ONE call is the only safe write.
- **THE DELIVERED PAIR IS BEHIND HEAD AS OF 2026-08-31 16:54Z** -- the gate's currency arm flipped to REFUSING when `abe69906` and `5f58265b` landed under `native/rust` mid-session. **EVERY FIGURE I PRODUCED TODAY IS ABOUT `553ac304`, sha `d0568cb44d1e225d`, AND STAYS TRUE OF IT; NOTHING I RAN DESCRIBES HEAD.** Do not rebuild into the shared path with sessions live -- that is `0196`'s window. Re-drive after whoever owns those commits runs `bin/devbin build all`.
- **CANON ORDER: SYNC FIRST, then commit the file and canon together.** A later sync fixes the NEXT commit and can never fix this one.
- **THE GATE FIGURE IS COMPUTED BY THREE VERB CALLS AND NEVER TRANSCRIBED.**
- **THE PROVENANCE PIN IS NOT HEAD AND THAT IS BY DESIGN.** `emit_source_commit` runs `rev-list -1 HEAD` over `:(top)native/rust :(top)surface :(top)docs/design`, so it names the last commit touching the BUILD'S INPUTS. **`intentd --version` prints NO commit where `intent --version` does, so the pair cannot be compared through `--version` at all** -- the embedded marker is the only route. `dvb build all` forces the embeds and verifies the SET, which is the check cargo cannot make.
- **THE BASH TOOL'S SHELL IS zsh.** No word-split on unquoted `$var`; an unmatched glob aborts the command; **`mapfile` DOES NOT EXIST**, and in an `&&` chain its failure silently skips every later step, leaving a previous run's file to be read as this run's answer. **Write anything non-trivial as a `#!/usr/bin/env bash` script file.**

## DOING

- **NOTHING IN FLIGHT, AND THE HOLD IS hv'S OWN WORDS AT 2026-09-01 13:51Z RATHER THAN MY READING OF THEM.** Every node is on hold while a devbin change to the way rust code is built and managed is rolled out. hv: _we will let you know when it's safe to take the foil off the windows and come back out to play._ **SO THE RESUME SIGNAL IS hv SAYING SO.** An idle tree, a green gate, a peer resuming, or my own judgement that the coast looks clear are NONE of them the signal -- that substitution is how a hold ends early and quietly, and the thing being changed underneath us is the build itself, which is precisely what a node would touch first on resuming.
- **Rust consolidation: DONE and watched.** 257 targets -> 6, `[profile.dev] debug = "line-tables-only"`, `--no-fail-fast` on the live call sites, orphan guard in three crates proven two-sided. hv ran a full clean cycle: 113G -> 6.0G, 2022 passed / 0 failed / 14 suites, `dirty: 0` -- the first verdict all day that describes a real commit.
- **TN001 is current** (`73857a72`) and carries the verification section: N clean runs cannot detect an intermittent, and the isolate-vs-fix discriminator.
- **The devbin packet is delivered** -- four gates plus the `build fullcycle` spec, then amended twice by me and corrected three times by devbin-vc. See TODO for what came back.

## TODO

**THREE hv RULINGS PENDING, all raised by devbin-vc and all correcting me:**

- **Seam scope -- AND THE VERSION THIS BOARD CARRIED WAS ITSELF STALE, CORRECTED BY devbin-vc 2026-09-01 13:59Z.** I recorded their earlier claim that devbin's shipped seam has no `build`. **It has one** -- as a dotless catalogue row; they retracted it themselves. `local` remains Intent-local, and `resolve:323` resolving `$DEVBIN_DIR/cmd/` before `$DEVBIN_LIB/cmd/` is still how Intent carries verbs devbin never shipped. **My `release`-outside-`all` precedent was my own config's, not the seam's.** WHERE `fullcycle` attaches is still an open hv ruling -- and it is now blocked on the item below rather than merely undecided.
- **THE `fullcycle` ESCALATION IS CLOSED AND ITS REMEDY IS DEAD. hv RULED 2026-09-01 14:49Z.** `fullcycle` is a **TOP-LEVEL COMMAND** -- `bin/devbin fullcycle`, not `build fullcycle` -- which puts it **structurally** outside every `all`, since `all` sweeps a command's OPTIONS and a command is not one of them. **For a verb that deletes a build tree that is worth more than the `in_all` column was:** no config can opt it back in by accident and no later refactor can quietly re-include it. The column devbin-vc had already built is **reverted**, under D18's own discipline -- the option set grows only when a shipped built-in needs a flag, never speculatively -- because with `fullcycle` moved it had no user.
- **WHAT I SENT hv WAS CORRECT WHEN SENT AND IS NOW STALE. THAT IS THE THIRD TIME TODAY A VALUE OUTLIVED ITS SUBJECT, AND THE FIRST TIME IT WAS MINE IN FLIGHT TO hv.** The blast-radius correction (not 13 estates; exposure is every estate DECLARING `rust`; Baize and Gtools declare it while owning none) was true of the placement that no longer exists. **KEEP THE PROPERTY, NOT THE FIGURE: a lang-keyed option is activated by the DECLARATION, not by owning any code in that language** -- that outlives this ruling and is the reusable half.
- **devbin-vc's OWN FINDING ARGUED AGAINST devbin-vc's OWN PLACEMENT AND THEY DID NOT NOTICE UNTIL THE SUITE SAID SO.** They reported to me that `command_active` makes an option-less command inactive, so `build` would resolve for the first time on `fullcycle` alone and `build all` would be exactly one thing, the destructive one. **That is precisely what devbin's D18 test exists to stop** -- `builtins.bats` asserts `build` ships NO option set, because the intersection across Lamplight (`cli|deploy|grammar`), Conflab (`cli|daemon|native|app|assets`), Laksa (`release|assets|deploy|clean`) and Baize (none) is EMPTY, so shipping any imposes one project's shape on the others. **The disproof was in their own message. Fourth instance today of that shape, across three different nodes.**
- **WHAT SURVIVED IS BIGGER THAN WHAT WAS ESCALATED, AND IT IS LIVE IN EVERY ESTATE TODAY WITH NO NEW CODE: `run_all` RETURNED rc 0 WHEN IT EXECUTED NOTHING.** A project declaring one option and excluding it from `all` was told **all options passed** and handed **rc 0** -- so `devbin <cmd> all && deploy` **deploys on a run that did nothing.** devbin-vc verified it by READING THE TAIL rather than reasoning about it: `if [ -z "$failed" ] && [ -z "$disagreed" ]`. **And the function's own comment had already conceded the gap and called it covered by the skip report -- the skip report REPORTS, and the tail still returned 0.** That is `IN-AG-NO-SILENT-001` exactly: a failure surfaced in prose while the exit status says success. `run_all` now REFUSES on zero executed. **THIS is the item for hv, not the column.**
- **AND I RAISED THE WRONG VERB TWICE OVER.** I worried about `test all`; the hazard was structural to builtins generally; and the durable defect turned out not to be sweeping at all but a zero-executed success.
- **Gate 1's cost class.** `cargo test --no-run` BUILDS the test targets, so the authoritative count is not check-speed.
- **Gate 4 is NOT devbin's** -- verified here: the dangling-symlink refusal is `lib/templates/hooks/pre-commit.sh`, an Intent template; no devbin vendored file carries it. And devbin's shipped hook FAILS OPEN by design, so "the fix is blocked BY the outage" does not reproduce there. **Either Intent's hook has diverged from the shipped one, or my refusal is a different check -- and if it diverged, that is bigger than gate 4.** Unresolved; needs a read of both hooks side by side.

**LANDED DURING THE HOLD 2026-09-01 13:57Z -- measurement and replies only, nothing started:**

- **devbin-vc WAS RIGHT AND MY AUTOLOAD DIAGNOSIS WAS WRONG. THE DISCRIMINATOR IS SHELL-FUNCTION vs FORKED JOB, NOT AUTOLOADED vs PLAIN.** Their arm B -- a plain, freshly-defined, never-autoloaded function -- is just as silent under `time` as the autoload stub. **Reproduced here in both `zsh -c` and `zsh -ic` without a pty**, so it is neither pty- nor interactivity-dependent. I added **arm B2**: `pg() { command sleep 0.3 }` is STILL silent, which removes "the body resolved to a builtin so nothing forked" as a competing mechanism and leaves only their discriminator. **THE LOAD-BEARING CONSEQUENCE:** anyone told "it is the autoload stub" concludes that moving to devbin's documented `eval "$(devbin shell-init zsh)"` restores `time`. **It does not** -- that IS arm B. The staleness fix and the timing fix are different fixes. Correct statement: `time` reports nothing for ANY zsh function, however defined.
- **My own fault here is the seventh instance of the day's one class, and worse than devbin-vc put it.** I had already "positive-controlled" the pattern with a subshell and an external command -- which are arms D and A -- and **both controls were incapable of exhibiting the failure.** I then proposed job control, a mechanism I could not test, rather than running the one command that separates the variables.
- **devbin-vc's message was stamped an hour ahead** (14:55Z against a same-turn `date -u` of 13:55Z; local is BST). The documented +1h error, from the node writing fleet canon right now. Flagged to them.
- **vc's TN001 finding is STALE and TN001 needs no change.** They reported line 118 item 1 still leading with `grep -c '^[[test]]'`. Measured: line 118 IS item 1, it opens with the cargo command, demotes the greps to DIAGNOSE-not-ADJUDICATE, and already names the undercount by crate (`testkit`, grep 0 / cargo 1). Committed at `73857a72` -- the sha I gave them. **A claim about a FILE is a claim about a REVISION and goes stale exactly like a claim about a binary.**
- **cc's LockHeld blocker is gone and cc has been told, with the non-lift stated twice** (`32a9bb07` + live). Verified the artefact first-hand rather than relaying vc's reading.

- **devbin-vc STATES MY OWN ERROR CLASS BETTER THAN I DID, AND THIS IS THE FORM TO KEEP: A CONTROL CHOSEN FROM THE CASES YOU CAN DRIVE SELECTS FOR CASES THAT PASS.** My two controls were arms A and D, which are exactly the two configurations that CANNOT exhibit the failure -- so the untested-mechanism error was DOWNSTREAM of controls that had already told me there was nothing to explain. That is the earlier and more useful fault. They report doing the same thing an hour ago: four breaks driven at a gate, an all-green run read as a catch, when the break had never applied.
- **devbin-vc's stamp error has a source and it is not carelessness: `runlog` names sealed gate logs `20260901-1451-38.LINT.errors` in LOCAL time.** They read a true number off a real tool in a zone it never claimed. Their heartbeat in the same session is correct, because that one came from `date -u`. **The channel this arrived on sits under none of the three clock guards.**
- **vc withdrew the TN001 finding after verifying it themselves, and took the form as their 8g.** They also counted three of this shape in one hour, all theirs, and their corrective is the right one: **every figure gets its regenerating command written beside it AT THE MOMENT IT IS WRITTEN.**

- **cc CAUGHT ME AND BOTH MY PREMISES ABOUT THEM WERE WRONG. VERIFIED HERE 2026-09-01 14:00Z, NOT ACCEPTED ON REPORT.** (a) _Their board carries the broken-tool claim_ -- **IT NEVER DID.** `git show 726a715c:intent/whiteboard/cc/wip.md` greps one hit, line 55, and it is a watch-out about `2>/dev/null` on a source printing `command not found` returning an empty lookup that reads as data. Different subject. The broken-tool claim was on **vc's** board and hv's. (b) _Their LockHeld commit was refused for the missing artefact_ -- `git merge-base --is-ancestor 3fc1d152 HEAD` says **it landed**, and **that fact was already on THIS board**: I had recorded the daemon race as fixed by cc at 3fc1d152. **The disproof was in my own hand, again.** **AND IT IS NOT THE INSTRUMENT CLASS -- cc's distinction, kept: an instrument that cannot see the failure is a DESIGN fault; having the disproof in your own file and not opening it is an ATTENTION fault under load. One command answers both, which is the only sense in which the cure is shared. A count that merged them would be a worse record than two.**
- **THE CLASS, IN cc's WORDS, AND IT IS ONE OBJECT OVER FROM THE ONE I THOUGHT I WAS PREVENTING: A MESSAGE CORRECTING SOMEONE'S STATE IS ITSELF A STATE CLAIM.** I refused to relay vc's reading of the artefact and re-drove it -- correct -- then applied the re-drive **to the SUBJECT and not to the claims about the RECIPIENT**. Both of mine were one command away. **Re-drive the claims about the person you are correcting, not only the fact you are correcting them with.**
- **cc's DIRT_SCOPE CAUTION, WHICH IS THE ONE FORWARD-LOOKING ITEM IN ANY OF TODAY'S TRAFFIC.** The path list in the currency discriminator -- `native/rust surface docs/design` -- **IS the shared-artefact guard's `DIRT_SCOPE`** (`:(top)native/rust :(top)surface :(top)docs/design`). **A devbin change to how Rust is built is exactly the kind of change that moves it.** The PROPERTY survives that; the ARGUMENT LIST may not. **On resuming, re-read the scope out of the guard rather than off either of our boards** -- otherwise we come back through the window running a command whose corpus has quietly stopped matching its criterion.

- **devbin-vc'S TN001 FINDING DOES NOT REPRODUCE, AND THE SENTENCE THEY QUOTED EXISTS NOWHERE ON THIS MACHINE (2026-09-01 15:41Z).** They reported TN001's summary as reading _Three needed work: Intent (done), Lamplight and Conflab (untouched -- hv has every estate holding)_ and contradicting the note's own Lamplight correction. **TN001 says none of that.** The table reads `Lamplight | YES -- 1 of 1`, the correction narrative sits directly under it, and **line 7 already records `APPLIED IN INTENT AT 71a96213 (dc, 2026-09-01), AND THE FULL CYCLE HAS BEEN RUN AND WATCHED BY hv`.** A case-insensitive grep for `lamplight and conflab` and for `every estate holding` across every `.md` under `~/Devel/prj` returns **nothing**. My copy is clean at `73857a72`; there is no forked TN001 on the box (Conflab's `tn001` is an unrelated macos-release note, which is the collision TN001 item 4 already documents).
- **AND MY FIRST FLEET GREP WAS BROKEN IN THE EXACT WAY I HAVE BEEN CORRECTING ALL DAY -- MY OWN POSITIVE CONTROL CAUGHT IT.** I ran the fleet search WITHOUT `-i` against a corpus whose house style is SHOUTING CAPS, and it returned clean. **The control I ran next -- a phrase I knew was in TN001 -- ALSO returned nothing, which is what exposed the instrument rather than the subject.** With `-i` the control finds TN001 and the real query still finds nothing, so the conclusion stands, but it stands on the second instrument and not the first. **A clean result from an unproven grep is the same object as a green test that cannot go red.** Had I sent the first result I would have told a peer their quotation was fabricated, on the strength of a search that could not have found it.
- **WHAT devbin-vc DID DELIVER, AND IT IS REAL: FIELD MEASUREMENTS FROM THE SHIPPED GATE.** `Conflab native/daemon` = **10 auto-discovered targets, BREACH** -- TN001 names no Conflab state at all, so this is new and is the only estate their gate finds work in. Laksa and Lamplight clean; Intent's `testkit` reports **1 auto target and PASSES**, which validates the exact case I flagged (greps 0, cargo reports 1) **on real crates rather than on a fixture**. `check targetdir`: no fork anywhere on the fleet. **NOT WRITING ANY OF IT INTO TN001: it is their measurement of other estates, unverified here, and I am on hv's hold. It wants hv's sequencing or their gate output, not my transcription.**
- **THEIR OWN CORRECTION IS THE BEST INSTANCE OF THE DAY'S CLASS AND THEY FOUND IT THEMSELVES.** They told hv the fleet was 11 estates; it is **13**. They read the census with `tail -25`, and `Arca/arca_cli` and `Arca/arca_config` **sort FIRST alphabetically -- so `tail` is precisely the cut that removes them.** Then they hand-typed the estate list from that truncated output and swept `devbin --version` over the list, so **the instrument built to verify the fleet was constructed FROM the gap it was meant to find and could not have failed.** Their own words for the refinement: not a control chosen from the cases you can drive, but **a control chosen from the cases you had already seen.** Those two estates have been lost four times; the previous three were `-maxdepth`, which is why `cmd/fleet` warns about the SHAPE rather than a number.

- **THE TN001 FINDING IS RETRACTED AND THE SENTENCE WAS MINE. devbin-vc FOUND IT: IT IS FROM MY OWN PACKET MESSAGE AT 13:21Z, QUOTED ACCURATELY AND CITED TO THE WRONG ARTEFACT.** TN001 was clean throughout. **And the sentence they moved was the STALE one** -- my packet's census line predates my own note's Lamplight correction, so the honest report was never _TN001 is stale_ but _dc's packet is stale relative to dc's own note_, which needed no correction because the note had already made it. **I CANNOT VERIFY MY OWN SENT MESSAGE: it is on no disk here** (only my wip.md, quoting them quoting me), which is the finding rather than a footnote.
- **THE NEW EDGE ON THE DAY'S CLASS, AND IT IS THEIRS: A MESSAGE CITED AS A DOCUMENT. A cross-session message has no path, no revision, no verblock and no guard.** Moving a claim from the channel into the document did not merely get the location wrong -- **it upgraded the claim's authority to something checkable while removing everything that would let anyone check it.** The only refutation available to me was grepping for text that was never in a file, which is why my first instinct was a forked copy: **a documented claim implies a document.** The defence is to quote the channel AS the channel. **No control catches this** -- the instrument is fine and the SOURCE ATTRIBUTION is what is wrong.
- **LIVE ON ALL 13 ESTATES, devbin-vc's, AND IT IS MY SPEC THEY CUT: `fullcycle` DELETES THE RELEASE BINARY AND NEVER RESTORES IT.** `fullcycle` runs `clean rust` then `test rust`; **`cargo test` does not build `--release`**, so `target/release/intent` is deleted by phase 1 and nothing brings it back. **MY SPEC HAD `build all` AS PHASE 2 AND I ACCEPTED ITS REMOVAL WITHOUT ARGUMENT** -- I agreed two phases were right because devbin ships no `build` options, and in doing so agreed to drop the phase that restores what phase 1 deletes.
- **THE CONSEQUENCE CHAIN IS VERIFIED HERE FROM INTENT'S OWN GATE, NOT REASONED.** `~/.local/bin/intent` is a symlink into `native/rust/target/release/intent`; delete the tree and it is state **B -- link, target does not resolve** in `pre-commit.sh`'s own table, which reaches the blocking arm. **The gate's comment already describes this incident from 2026-08-27 in these words: _during a release build the CLI goes ABSENT rather than merely changing, so every node in the fleet hit this arm at once -- and the message told them to INSTALL INTENT, which races a build already in flight._** **THIS IS TODAY'S 50-MINUTE OUTAGE SHIPPED AS A COMMAND, and worse than the original: the outage was transient because a build was in flight, whereas `fullcycle` leaves it permanent because nothing rebuilds.** devbin-vc is fixing it now.

**OWED, AND hv'S CALL -- raised by vc, recorded here so it outlives both our sessions:**

- **A v3.0.0 user upgrading to v3.0.1 migrates the store 16 -> 17 IRREVERSIBLY and cannot roll back to the tag they came from.** Local exposure is nil (the keg is unlinked and pinned), and **that is not the finding** -- this is a shipped-artefact obligation that does not depend on this box at all. hv's call whether it is a release note, a refusal message, or a backup step.

**Corrections devbin-vc made that I verified:**

- `/usr/bin/time` on this fleet is **BSD, not GNU** -- `-f` is an illegal option, portable form is `-p`/`-o`. I offered `-f` unqualified; it would fail on every machine here.
- **My `SECONDS` amendment carries the class it fixes**: a `printf` after a phase does not run when the phase fails under `set -e`, so the timing is absent exactly on the failure case, and the absence looks like success. Capture rc first or trap.
- **The autoload stub is a HOST DIVERGENCE, not a devbin property.** devbin documents `eval "$(dvb shell-init zsh)"`, which yields a plain function `time` reports on normally. Our `dvb` is autoloaded from `$fpath` -- a shape devbin neither emits nor documents, and nothing detects the divergence. devbin-vc's `doctor`-check framing is better than my catalogue caveat.

**Open, not mine to close:**

- `daemon_subscriptions` event bleed across test boundaries -- characterised, test-level (cc established the watcher publish path is correctly scoped), unfixed, with cc.
- `0207` (`at green/red/na --note` replaces wholesale), `0203`'s remedy, `0208`'s remedy -- all hv's.
- AC-04.6 sits behind `0207`; AC-07.7 needs a published artefact.

## Watch-outs

**Rule only; instances are evidence, not the entry. Instances live in `.history/`.**

- **W1 -- THE INSTRUMENT ANSWERED A DIFFERENT QUESTION THAN THE ONE ASKED, AND ITS OUTPUT LOOKED LIKE AN ANSWER.** The dominant class. **A partial read is the sharpest form and it is ASYMMETRIC: one that finds SOMETHING self-corrects; one that finds NOTHING yields a conclusion nobody can falsify without redoing the read.** So **a NEGATIVE from a truncated read is not a result**. **THE PROCEDURE: output to a file, then count over the whole of it.** **A FILTER OVER A TOOL'S OWN DIAGNOSTIC IS THE SAME DEFECT WEARING A COMMIT**. **A real nearby instance of a class is the most expensive thing to have in mind while reading a filtered log.** **`--help` IS A WIREDNESS PROBE, NOT A VERB**: it renders for a DECLARED command whether or not an arm exists. Invoke the verb. **And rc=1 from clap ARG-VALIDATION is not wiredness either** -- validation runs BEFORE the unwired dispatch, so the refusal is only reachable by SUPPLYING the arguments.
- **W2 -- THE MEASUREMENT'S SUBJECT WAS NOT THE ONE I NAMED.** **A sha and `git status` answer different questions and only one is in the label** (cc). A suite run in a shared tree measures the TREE, not HEAD -- **and so does the delivered binary.** **A `git status` expires in minutes here**; re-run it in the same turn as the sentence resting on it.
- **W3 -- OWNERSHIP IS MEASURED, NEVER INFERRED, AND IT HAS TWO TELLS.** _Absent at HEAD, therefore someone else's._ _The subject is theirs, therefore the edit is theirs._ **`git log -S<symbol> -- <path>` and `git log -1 -- <path>` close both.** Neither leaves a mark when wrong. **THIRD TELL, 2026-08-31, vc's naming: _they RULED it, therefore they WROTE it_ -- AUTHORSHIP READ OFF AUTHORISATION.** I was told a harness was landing in `parity/tools` under vc's ruling and concluded the file was vc's; it was cc's, and I was one `git add` from staging a peer's file on the strength of it. **On a board where one node holds the pen this is systematic, not incidental** -- vc rules constantly, so _under vc's ruling_ attaches to many artefacts vc never wrote. **AND THE CURE ABOVE DOES NOT WORK FOR THIS ONE, WHICH IS THE HALF WORTH KNOWING: NODE AUTHORSHIP IS NOT RECOVERABLE FROM GIT IN THIS ESTATE.** Driven: **one git identity across all five nodes, and 177 of today's 312 commits carry no node tag at all** -- only board commits take the `wb(<node>)` prefix. So for a content file there is no mechanical answer to _whose is this_, and the only instrument is ASKING. Do not perform a check that cannot fire.
- **W4a -- THE INDEX CLASS HAS THREE MEMBERS AND ONLY ONE IS CLOSABLE BY THE COMMITTER** (cc). **CONTENTION** is closable: a private `GIT_INDEX_FILE` gives hunk granularity AND isolation, which `--only` and `apply --cached` each give only one of. **COHERENCE** is NOT closable from your side -- my staged tool without its roster row blocked every node for ~20 minutes. **REVERSION leaves a correct-looking artefact**: a private index needs a HEAD PIN on BOTH sides, and **the post-commit `HEAD^ == base` assert matters more than the pre-check**, because the gate runtime sits inside the window the pre-check cannot cover. **Never reset a peer's index entries.**
- **W4 -- THE SHARED CHECKOUT PUNISHES EVERY ASSUMPTION ABOUT WHO ELSE IS WRITING.** Never `cp` a shared source aside to mutate; never format or bless a file carrying a peer's in-flight change. **A multi-step refactor is uncompilable between its steps and nothing says "mid-move"** -- a rename plus its use sites is ONE edit. **`git commit --amend` has NO PATH SCOPE AT ALL.** **Holding your hunk UNSTAGED does not protect it** -- a plain commit needs the file DIRTY, not staged. **The only thing that actually worked was telling the peer before committing a file we were both in.**
- **W5 -- A RESTRICTION THAT REDS NOTHING HAS NOT BEEN ADDED.** When you add a guard, something must go red; if nothing does, find the door before believing it. Corollary from the other side: **a rule that catches a failure mode it was NOT designed for is the strongest evidence for it.** **AND A GUARD THAT SKIPS PRINTS `ok`** -- an `ok` count is not an armed count, and a suite reporting totals without a skip census cannot distinguish a guard that held from one that could not fire.
- **W6 -- A SECOND HOME FOR A FACT DRIFTS, AND THE PATTERN IS PROXIMITY, NOT DISTANCE** (vc). **A second home three lines away does not READ as a second home -- it reads as CONTEXT**, so nobody applies the Highlander question to it. Never transcribe an instrument's number -- drive the verb. **An anchor that resolves to the record's own RENDERING is circular and worse than a dangle** -- a dangle is visible, a circle certifies itself.
- **W7 -- MECHANICS THAT RETURN A PLAUSIBLE WRONG ANSWER.** **Never take `$?` through a pipe.** `cargo test` aborts after a failing target (`--no-fail-fast`) and **is not read-only: it migrates the shared store.** **THE HONEST-AND-BLIND-GREP FAMILY** (vc's name): **the output is INDEPENDENT OF WHAT IT MEASURES, and the control would pass just as well under the broken instrument** -- which is why a denominator assertion is the cure and a better predicate is not. **`[ status -ne 0 ]` after a `grep -r` reads a BROKEN INSTRUMENT as a CLEAN ESTATE** (grep exits 1 clean, 2 on an absent root). **THE RULE IS THE THREE-WAY CASE: 0 is a finding, 1 is clean, anything else is the instrument -- and the denominator is asserted separately or none of the three means anything.** **THE DOCUMENTATION MEMBER: A MUTATION PROOF IS PROSE UNTIL SOMEONE TYPES IT** -- every _we verified X by doing Y_ comment is an UNRUN TEST WITH A PASSING APPEARANCE; it WAS evidence when written and decays silently, and **its correct steady state is SILENCE**, so nothing reports it. **AND THE EXECUTABLE MEMBER, 2026-08-31: A GUARD FIRING CORRECTLY ON AN EXPIRED PREMISE.** `render.rs:4461` refuses `--lang` because _`intent lang init` is not implemented_ -- it IS. Conclusion right, reason false, remedy actively wrong. vc filed `0187` high. **Instances: `.history/20260831/`.**
- **W8 -- HOW TO REPORT.** Write reasoning at the resolution you MEASURED it. **Verify a peer's correction yourself, including when it makes your own claim false.** A peer's quote of a ruling is still the peer. **Retract a finding the moment its basis fails, even one you were thanked for.**
- **W9 -- A REMEDY WHOSE STATED REASON DOES NOT HOLD IN YOUR CASE IS NOT PERMISSION** (vc). **The reason evaporating does not convert the advice into a licence.** Same shape as W1 from the other end.
- **W10 -- I DOCUMENTED THE CLASS AND FIXED THE INSTANCE, AND THE COMMENT MADE IT LOOK CLOSED.** **A class-shaped comment over an instance-shaped fix is worse than no comment.** The tell is a fix whose test is the instance that prompted it. **Ask what ELSE is in the class and drive one of those instead.**
- **W11 -- I ARGUED FROM HOW THE SYSTEM BEING REPLACED WAS BUILT** (hv). **It is not a PORT, it is FUNCTIONAL PARITY.** **The tell is that the argument could not be stated without naming v2.** **And the reframe moved the TARGET, not just the reasoning.**
- **W12 -- I PROVED SOMETHING TRUE AND BESIDE THE POINT, AND EVERY STEP WAS SOUND.** vc's general form: **_no verb produces state X_ is a claim about a ROUTE. Ask what the thing must STOP CLAIMING before asking what it must BECOME.** **A sample of one is a shape, never a rule.**
- **W13 -- A RULE OR CITATION TAKING ITS AUTHORITY FROM A NAME RATHER THAN FROM ITS MEMBERSHIP RULE.** Five instances, four nodes, one day. **IN THREE OF THE FIVE THE CONCLUSION WAS STILL CORRECT, which is what hid the reasoning error.** Discriminator: **state the rule WITHOUT naming the thing. If the argument needs the name, it is resemblance.**
- **W14 -- A CENSUS THAT ENUMERATES MEMBERS CANNOT SEE A DEPENDENCY HELD BY THE CONTAINER, AND THE CONTAINER IS WHERE THE FATAL ONE LIVES.** **THE FORM: when you census a population, separately census the thing that RUNS it, and the thing that runs THAT.** Second half: **a keyword's presence is not a property's coverage.** Property coverage is a read, not a grep.
- **W15 -- AN INSTRUMENT'S NEGATIVE DIRECTION, ONCE SHOWN UNTRUSTWORTHY ANYWHERE, WITHDRAWS EVERY ZERO IT EVER RETURNED -- NOT ONLY THE ONES YOU RE-CHECKED.** **A GREP FOR A COMPOSED STRING IS A GREP FOR A THING THAT DOES NOT EXIST, AND IT RETURNS ZERO RATHER THAN AN ERROR.** **THE ASYMMETRY IS THE ENTRY: the census self-corrected everywhere it FIRED and lied only where it found nothing.** I then sized the class from the two instances in front of me and it was at least nine -- **W10 committed inside the entry whose subject is undercounting.**
- **W16 -- I CONTROLLED THE NEW INSTRUMENT ONLY IN THE DIRECTION THE OLD ONE FAILED, AND MY NEGATIVE CONTROL WAS ONE THAT COULD NOT FAIL.** **A control must be a case the instrument could PLAUSIBLY get wrong in the direction you have NOT yet been burned in.** **AND WHERE A CRITERION NAMES ITS OWN MEMBERS, THOSE ARE THE CONTROL** (2026-08-31): AC-02.3 names five live reader-reachable issues; driven against my candidate filter it **failed 2 of 4**, and the failure corrected the POPULATION rather than the predicate. **A filter that keeps 110 of 111 is not a filter** -- check what fraction it removes before believing it discriminates.
- **W17 -- A FACT WITH TWO HOMES, WHERE EITHER CAN OUTLIVE THE OTHER AND THE GUARD IS THE ONLY THING THAT NOTICES** (vc's statement, replaces mine). **Every isolation device we own is about what ENTERS A COMMIT, and none of them touches either home.** So **a create can ARM a guard against every node, needing NEITHER a stage NOR a commit** -- and **the create direction has the WEAKER precondition, since a delete must be committed to hurt anyone.** Corollary: **the author is the one node whose gate may not fire on it.** **`--only` BUILDS HEAD PLUS THE NAMED PATHS**, so a peer's staged-but-unnamed sibling is absent from the tree your commit constructs. **Ask instead of assuming on the file blocking you.**
- **W18 -- AN ARTEFACT REWRITTEN UNDER THE CHECK THAT READS IT LEAVES THE OPERATOR WITH NO PROGRESS AND NO DIAGNOSIS.** **Not the lost-edit form and worse in one way: the operator's correct action produces no progress and no diagnosis, so the natural next move is to doubt your own edit rather than the window.** Cure is cc's general one: do the whole sequence in ONE window, then **post-verify with `git show --name-only <commit>` EQUALLING your intended path set, never against your own staged list**, which cannot see a path you did not name.
- **W19 -- A DRIVE THAT STOPS AT ITS FIRST FAILURE REPORTS ONE DEFECT AND HIDES EVERY ONE BEHIND IT** (2026-08-31, AC-02.1). The published page's sixth defect -- _a cited file must carry the row's own literal id_ -- **was only reachable by fixing the fifth and running again.** Drive to green, then **count the passes: the count is the claim.** Sibling of W15's asymmetry, in the forward direction.

- **W20 -- A BOOLEAN PROBE HIDES ITS EVIDENCE.** A `yes/no` cannot be argued with. **When two runs of one probe disagree, NEITHER is evidence until you know why.** Print what the probe saw.
- **W21 -- WHEN EACH FIX FINDS ANOTHER DEFECT, THE METHOD IS THE DEFECT, NOT THE ENTRIES.** Patching entry by entry leaves a thing that is true only where you happened to look.
- **W22 -- A COUNT OF DIFFERENCES IS NOT A COUNT OF DAMAGE**, and a count of FILES is not a count of THREADS. Two spellings of one value differ. **Read the PAIRS before reporting the number.**
- **W23 -- NAME THE TREE / THE BINARY / THE DENOMINATOR, OR THE CLAIM CANNOT BE DISAGREED WITH.** The general form of W20: **omitting the thing a reader needs in order to tell you that you are wrong.**
- **W24 -- A GREP OVER A SERIALISATION ANSWERS A QUESTION ABOUT TEXT WHEN THE QUESTION WAS ABOUT STRUCTURE.** `grep legacy` over canon JSON returns 10; the structured query returns 0.
- **W25 -- AN EDIT THAT REPLACES A LINE RANGE DELETES WHAT IT DID NOT READ.** My own 15:09Z board edit destroyed W20 and W21 four hours after writing them -- a hand edit doing what a fold keyed on dated does. **Inventory the classes before and after any range replacement.**

- **W26 -- A PARTITION BUILT FROM THE DIRECTION YOU EXPECT THE DRIFT TO RUN HAS NO BUCKET FOR THE OPPOSITE DIRECTION, AND THE UNNAMED CASE SORTS TO THE WRONG ONE IN SILENCE** (vc's rule, corrected by my drive and adopted by them verbatim). Two dispositions were both keyed on the OLD build failing, so _new build fails where the old one passed_ had no home and defaulted to _the docs are stale_ -- which would have filed a regression in the release candidate as a documentation lag. **THE THREE CLASSES: old fails / new passes = the docs are ahead, note it. Both fail = the docs are wrong, file it. OLD PASSES / NEW FAILS = THE TREE BROKE WHAT THE TAG HAD RIGHT, and it outranks both.** General form: **before trusting a partition, name the case it has no bucket for and check where that case lands.**
- **W27 -- A FIGURE WITH NO HOME THAT DERIVES IT SURVIVES ON RESTATEMENT, AND EACH RESTATEMENT MAKES IT LOOK BETTER SOURCED.** vc corrected an issue NUMBER I had handed on; checking it found the FIGURE wrong too -- _twenty-one_ was not reproducible from anything in the estate, the durable record derived TEN, and the manifest carried no verdict column so it was never derivable there. It had walked across from an unrelated _21 of 56_. **A number that lives on a board and in a handover and in no instrument is not evidence.** Same family as the handover rule -- hand over PROPERTIES, re-measure, never trust a ledger -- arriving through repetition rather than through age. **Cure: state the figure with the command that produces it, so the next reader re-derives instead of inheriting.**

- **W28 -- THE INSTRUMENT ITSELF CAN GO ABSENT, AND ITS ABSENCE READS AS AN EMPTY ANSWER** (vc broadcast it; it cost me a write). The dev `intent` is a SYMLINK INTO A BUILD OUTPUT, so a rebuild deletes it mid-session and every call returns **rc=127 with nothing on stdout** -- indistinguishable from a verb that ran and found nothing. **Read the EXIT CODE, never the output, and never put `2>/dev/null` in a harness that drives it.** The keg is a real file and is unaffected, which is the second half of the rule: **know which of your instruments is a symlink.** **AND IT POINTS THE OTHER WAY TOO** (cc, 2026-08-31, measured on their own rebuild): the running `intentd` was started at 15:52Z on `553ac304` and **holds the DELETED INODE**, so it serves code that no longer exists on disk while `daemon status` answers live. **The artefact is absent and the thing depending on it reports healthy** -- so absence is invisible from BOTH ends, and a restart is the only thing that resolves which code is running. **Measured exposure of `0196`'s window: `Removed 49 files, 53.0MiB total` before a single line compiled, 1m18s to come back, benign ONLY because the build succeeded.**
- **W29 -- A WITHDRAWAL IS A CLAIM, AND IT NEEDS THE SAME EVIDENCE AS THE FINDING IT RETRACTS.** vc correctly withdrew a finding of mine and, in the same message, withdrew _every drive since 16:30Z_ -- which swept up a run that really happened, at rc=0, with substantive output, against the DEBUG binary that had not vanished. **Withdrawing a real result costs the same as keeping a fabricated one.** The right move is to name the limitation the evidence supports -- _that build is somebody's uncommitted tree_ -- rather than the widest one available.

- **W30 -- A PARKED ARTEFACT IS A MEASUREMENT WITH A TIMESTAMP ON IT, AND THE TREE MOVES UNDERNEATH IT** (vc's statement, from my near-miss). Text written, held because an instrument was unavailable, then landed later is **evidence about the moment it was parked, not about the moment it lands.** `0193`'s parked body named three builds and a behaviour that existed only in a peer's uncommitted tree; re-driving the one command before landing found `553ac304` refusing, which made it three-of-four and the outlier an outlier. **Had it gone in as written it would have asserted a v3 behaviour present in NO build anyone can install.** The rule: **re-drive before landing anything that waited**, and say which build the claim is about. Related: W23, W27, W28.
- **W34 -- DOING THE WORK IS NOT REGISTERING IT, AND THE INSTRUMENT READS THE REGISTER.** vc drove `0162` and `0194`, wrote both into `known-defects.md`, and committed -- and AC-02.3 still counted both UNDISPOSITIONED, because the tool reads a TSV manifest and neither commit touched it. **The hard half was done and the cheap half was invisible.** Nothing failed and nothing warned: two files, one fact, and only one of them is what the checker opens. **Cure: after stating anything, RUN THE INSTRUMENT and watch the number move.** Corollary that nearly cost me: **I was holding a working copy of that shared manifest across several appends**, so a peer's rows landing meanwhile would have been overwritten by my next `st attach`. **Check HEAD before appending to a shared data file, every time.**
- **W32 -- THE ESTATE MAY ALREADY OWN YOUR FINDING, AND A REDISCOVERY IS INVISIBLE TO YOU BECAUSE YOU SEARCHED YOUR MEMORY INSTEAD OF THE REGISTER.** Under an explicit instruction to FILE, I came within one command of filing a third record of `0151`/`0154`. What stopped it was reading the sibling rows my own board already NAMED -- the board said _same family as `0185` and `0154`_ and I had never opened `0154`. **A finding that arrives with a family attached is a finding to look up, not to file.** This is Highlander with the register as the registry: search before you create, and the search is `issues show` on the siblings you can already name. **The prize for looking is bigger than avoiding the duplicate** -- `0154` turned out to have PREDICTED the half-fix that then happened, which no new row could have said.
- **W33 -- A BROKEN INSTRUMENT THAT RETURNS THE SAME ANSWER ON BOTH ARMS READS AS A CLEAN TWO-SIDED CONTROL.** Checking `--note` across builds, I looped `for v in "at green" "at na"` and zsh passed each phrase as ONE argv token, so every invocation errored and every grep found nothing. Output: `(no --note declared)` in BOTH columns -- **symmetry, which is exactly what a negative control looks like** -- and it contradicted a driven rc=0 I already had. **A control is only a control if the instrument was ever capable of returning the other answer.** Cure: when both arms agree, positive-control the instrument before believing the agreement, and dump one arm RAW rather than through a filter. Third time this session on zsh's no-word-split; the new half is that this one FAILED SYMMETRICALLY and so looked like evidence. Related: W28, and the precision-is-a-claim-about-the-corpus family.
- **W31 -- A REWRITTEN ARCHIVE LOOKS CORRECT AFTERWARDS, WHICH IS WHY ONLY A GUARD CAN SEE IT** (vc's framing of the append-only class). I rewrote a parked file under `.history/` to add a row -- 2 removed, 3 added -- and the pre-commit guard refused, correctly. **Every other check passes and the file reads as a coherent record, because the act destroys its own evidence.** `.history/` only ever grows: restore to HEAD and APPEND, never edit. The guard's own words are the keeper -- _a removal means a write landed where an append was meant_.
- **W35 -- A BYTE CHECK VERIFIES THE QUOTE, NEVER THE CLAIM, AND A PAGE CAN CARRY YOUR STRING WHILE SAYING THE OPPOSITE.** Measured: two `stated` rows cited a real page and a real string that page uses to say the defect is NOT present. **The row was true about the bytes and false about the world**, and every check passed. **The cure is not a better quote -- it is asking where on the page the string LANDS.** Generally: when an instrument checks presence, ask what a hostile author could satisfy it with. **AND CONTROL THE ANCHOR**: a region check keyed on a heading must FAIL when the heading is gone, because a check that degrades to a no-op under a retitle returns green and means nothing.
- **W36 -- A GUARD ARM'S NAME DESCRIBES THE REFUSAL, NOT WHAT FOLLOWS IT. READ THE BODY.** Measured on myself: I told a peer a build needed a clean tree because an arm was named _a dirty tree is refused_. The body one file away **redirects to a private target dir and continues** (`releasebuild.lib:69`). **A refusal and a redirect look identical from the arm list**, and the arm list is what you read when you are in a hurry. Generally: a test name states the CONDITION it fires on, never the CONSEQUENCE -- and the consequence is the half you are about to act on.
- **W37 -- STATING THE TRUE HALF OF A DEFECT IS NOT STATING THE DEFECT, AND IT IS WORSE THAN SILENCE.** `0086`: the page named `intent help` among the retired commands, which is TRUE. The defect is that the refusal's remedy says _there is no v3 replacement_ while `intent --help` answers on the same build. **A reader who hits it, reads the refusal, then finds the page confirming the retirement comes away AGREED WITH and still wrong** -- where a silent page would have left them looking. **Ask what the reader BELIEVES after reading, not whether the sentence is accurate.**
- **W38 -- WHEN YOU PROPOSE A MECHANISM, CHECK WHETHER ANYTHING YOU ALREADY RAN BEARS ON IT. YOU OFTEN HOLD THE DISPROOF, FILED UNDER A DIFFERENT QUESTION.** (cc's formulation, correcting my harsher and less useful one.) I proposed that `impl.md` was a generated view; I had listed all 317 attachment names hours earlier for an unrelated question, and `impl.md` sat four lines from `design.md` in that output. **This is NOT the `0078` shape** -- there I had a fact and asked the wrong question; here the fact predated the hypothesis, and **you cannot join a fact to an argument that does not exist yet.** The rule is therefore forward-looking and cheap: a new mechanism is a prompt to re-read your own transcript, not a prompt to feel bad. It cuts both ways -- cc's own first drive held the refutation of MY hypothesis, and neither of us had looked.

- **W39 -- TWO INDIVIDUALLY-CORRECT RULES CAN COMPOSE INTO A DEADLOCK WHOSE ONLY SYMPTOM IS THAT NOBODY COMMITS** (with vc, measured 2026-08-31, both of us inside it). Canon regenerates WHOLESALE from the store, so any node write inherits every unextracted peer row; `canon_commit_check.sh` then requires a file and its canon row to move together. **Neither rule should be relaxed** -- wholesale regeneration is what makes the store SSOT mean anything, and file-and-canon-together is what stops a canon naming bytes nobody has. Composed, they make a single-node coherent commit impossible whenever two nodes have written to the store since the last canon commit. **THE DANGER IS THAT THE STALL IS SILENT: two nodes politely waiting looks exactly like two nodes working.** Nothing errors, nothing times out, and the only observable is the absence of a commit. **THE REMEDY IS A SENTENCE, NOT A TOOL: when a shared-store write blocks on a peer, say which option you are TAKING rather than which you prefer.** vc offered A or B, I picked and executed inside one turn, and the batch landed with all three nodes rows attributed.
- **W40 -- A CLASSIFICATION DERIVED FROM ONE AXIS CANNOT EXPRESS A FACT ON ANOTHER, AND A COMPLETE-LOOKING REGISTER IS WHAT STOPS ANYONE LOOKING.** The keep/retire/deviate register classifies on the BURN RATIO -- does the file reach the CLI -- so a v2 test whose every assertion quotes a pre-0023 v2 string burns fully, earns `keep`, and fails every test under v3. AC-05.3 asks for **no UNCLASSIFIED rows** and gets zero, so the criterion is satisfied while six core rows are substantively wrong. **A predicate that cannot match its subject returns the number that means success** (cc formulation, from their `disposition == "drop"` find). Same family as W35: the byte check verifies the quote and never the claim. **CURE: when a register is used to decide something, ask what its AXIS can physically observe, and whether the decision needs a fact on a different one.** `gen_register.sh` already says this about itself in its SECOND PREDICATE block -- the estate documented the gap and then read the register anyway.

- **W41 -- ONE WORD NAMING TWO ARTEFACTS SENDS TWO NODES HUNTING A DISCRIMINATOR THAT DOES NOT EXIST, AND THE EXPERIMENT COMES BACK CLEAN.** `extract` meant both the canon JSON `st attach` writes and the authored disk document it does not. cc and I independently budgeted the SAME fixture -- nested path against top-level, one variable -- and it would have worked perfectly: `parity/` cases fine, `impl.md` broken, exactly as predicted, because parity docs get authored on disk and `impl.md` got attached. **We would have shipped _path depth is the discriminator_ with a green experiment behind it.** A confirmed wrong cause is worse than an open question, and the thing that would have shipped it was a NOUN. **CURE: when two observations of one verb disagree, ask what each was actually pointed AT before designing anything to tell them apart** -- and when a term appears in the issue text, check it names one thing.

## Decisions

**hv PROVENANCE ENTRIES ARE VERBATIM AND ARE NEVER COMPRESSED** -- a ruling's own words cannot be re-derived.

- **AN AC REWORD THAT LEAVES ITS AT BEHIND CONVERTS A STRENGTHENED CRITERION INTO A WEAKER ONE** (my finding, vc's naming and sweep, `062a41c7` / `2868e301`). **The AT is what a driver EXECUTES and the AC is what a reader READS**, both stay individually coherent, and **the gate reads the AT**. Swept all 69 threads: 24 criteria carry a reword marker; **exposure is confined to NON-TEST rows, because a test row's bar is a cited artefact that gets re-driven while a non-test row's bar is prose nobody re-reads.** Two were weaker, both mine; `AT-11.1` checked and matches. **Class closed for this estate.**
- **A FIGURE TRANSCRIBED INTO A CRITERION IS A CLAIM ABOUT A CORPUS AT A MOMENT.** AC-02.3 carried _72 open of 128_; a day later it was **103 of 164**, and 105 of 166 an hour after that. **A parenthetical that rots in a day, inside a criterion whose own point is being AUDITABLE RATHER THAN ASSERTED, teaches the next reader to trust a number the row cannot keep true.** Struck by vc under the standing pen. **The count belongs in the derivation.**
- **D-DELETE -- A DELETE HAS THREE POPULATIONS AND I HAD ONE.** **(1) WHAT EXECUTES THIS.** **(2) WHAT CITES THIS AS EVIDENCE -- AND THE POPULATION IS THE STORE, NOT THE TREE.** **(3) WHAT CHECKS THIS -- A DELETE CAN HOLLOW A SURVIVOR**, and nothing else catches it. **THE GENERAL FORM: blast radius measured as WHAT WENT RED misses everything that went VACUOUS, and vacuous leaves no corpse.**
- **D-POP -- THE INSTRUMENT'S POPULATION EXCLUDED ITS OWN SUBJECT.** Nine instances, none of them the empty set. **THE CURES: state the denominator before the finding; positive-control on a case it MUST catch; and when a narrowing flag is in the command, re-read what it narrowed.**
- **D-SHARED -- THE SHARED TREE BREAKS EVERY COMPARISON THAT LOOKS SELF-CONTAINED.** `git commit --only <paths>` in ONE call, and **`unset GIT_INDEX_FILE` BEFORE any reset, as its own call.**
- **D-FACES -- A DECLARATION HAS CONSUMERS YOU DID NOT WRITE, AND THE COMPILER GUARDS ONLY THE ONES IT CAN SEE.** Every hand-written roster of a generated surface rots; derive it or delete it.
- **D-ADVICE -- A DEFECT THAT CORRUPTS NO NUMBER HAS NO INSTRUMENT WATCHING IT.** **Advisory output is where defects live longest, because nothing downstream disagrees with them.**
- **A TEST WHOSE SUBJECT IS GONE IS DELETED WITH ITS REASON, NEVER PATCHED TO KEEP PASSING**, and **the property it protected is NAMED at the deletion site.**
- **A NEW GUARD IS DRIVEN TWO-SIDED ON A PLANTED FIXTURE BEFORE IT IS BELIEVED, AND ITS LIMIT GOES IN ITS OWN HEADER.** `absent_at_check.sh`: nine mutants against a prediction written first, four of them REFUSALS, because **"0 examined" is ambiguous and a tool must refuse rather than choose the reassuring reading.**
- **VERIFY A PEER'S CLAIM ABOUT YOUR OWN INSTRUMENT, EVEN WHEN THEY ARE RIGHT.** **A property asserted from a description is still an assertion.**
- **A VERB CAN BE SEMANTICALLY CORRECT AND STILL BE A SILENT FAILURE, BECAUSE CORRECTNESS IS ABOUT WHAT IT DID AND SILENCE IS ABOUT WHAT IT SAID** (vc, on my `at fc` find; `0158`).
- **A PERMISSIVE READER MAKES A CONTRACT VIOLATION AND A CONFORMING IMPLEMENTATION INDISTINGUISHABLE.** Refuse at the read.
- **hv HAD ALREADY RULED THE THING I WAS INSTRUCTED TO BUILD, THE OTHER WAY.** Read hv's board BODY at pickup, never just its header. **AND THE BODY IS NO LONGER ENOUGH: `fileindex` was ruled RETIRE and lived in `.history/` alone, because the fold keyed on DATED where hv's rule keys on DONE.** **THE DURABLE FORM: BEFORE PUTTING A QUESTION TO hv, CHECK WHETHER hv HAS ALREADY ANSWERED IT.** Three findings on 2026-08-31 had that shape and none needed new measurement, only re-measurement.
- **A RULING'S PREMISE CAN BE CORRECTLY MEASURED AND STILL NOT COVER WHAT YOU BUILD.** Surface the change rather than absorbing it.
- **A DECISION THAT REACHES A QUESTION IT WAS NOT ABOUT IS THE STRONGEST EVIDENCE FOR IT** (vc).
- **ic's WORDING FOR WHY THE INNOCENT EXPLANATION WINS: _a correct general model is what made me stop measuring._** Not laziness -- **a true and relevant model is genuinely good evidence, right up until it is the reason you stop looking.**
- **A FOLD IS A WRITE, SO IT NEEDS THE SAME READ-BACK ANY OTHER WRITE NEEDS, AND THE WRITER IS THE WORST READER OF IT IN THE SAME SESSION.** A previous fold put `D-POP` in this section three times, byte-identical -- **the gate-figure defect at document scale, produced by the operation whose whole purpose is removing duplicates.**

- **TWO hv RULINGS, 2026-08-30, ARRIVING BETWEEN THE 14:59Z AND 15:02Z CLOCK READS OF THIS SESSION -- THE MINUTE IS NOT RECOVERABLE AND IS NOT INVENTED. FIRST-HAND IN THIS dc SESSION. THE WORDING IS hv's RATHER THAN A PARAPHRASE.**

  **(a) EVERYTHING GOES IN 3.0.1.** hv, verbatim: _"Sure. Everything is in 3.0.1. That is what Intent v3 is supposed to be. Everything we can get into 3.0.1."_

  **(b) `shipped_surface_drift` IS AN EXPIRED TEST AND THE v2 GATES ARE DEAD.** hv, verbatim: _"Expired test. We don't care about gates for v2 any more. Don't waste any time on it, other than to remove it."_ **DONE, and the second half was found at fold time**: the test was deleted but the CI step existing only for it was not, and it FAILED the job. **A removal ruled in one sentence had a second site nobody looked for.**

- **THE CUT GOES AHEAD AGAINST AN OPEN GATE. PROVENANCE: hv, 2026-08-30 11:41Z, FIRST-HAND IN THIS dc SESSION, hv's own selection from four options dc authored.** (A) **cut against an open gate -- CHOSEN**, tag 3.0.1 with ST0056 BLOCKED and the tag-gated rows named as such; (B) re-scope the five, declined -- it buys a green by changing the question; (C) two-phase RC-then-real tag, declined -- unless the code is byte-identical the second artefact is not the one the criteria were driven against; (D) fiat-close the five, declined -- **they are not over-cooked, merely not-yet-possible, which is the case `fc` was explicitly not built for.** **Five ST0056 criteria require the artefact only the cut produces**, so the gate could never read green before the release existed. **CONSEQUENCE: WP-07 and WP-11 not closing before the cut is the DECIDED shape, not something waiting on hv's hand.**

- **THE STORE-MIGRATION GUARD LANDS AFTER THE CUT. PROVENANCE: hv, 2026-08-30 11:51Z, FIRST-HAND IN THIS dc SESSION.** (A) extend the guard after the cut -- **CHOSEN**; (B) now, declined; (C) fix the two proven members and defer the guard, declined -- **instance-shaped fix under a class-shaped problem, W10 done knowingly**; (D) file and do nothing, declined. **THE GROUND IS THAT IT IS A DEV-TREE HAZARD WITH NO SHIPPED CONSEQUENCE**, not that it is small.

- **hv's RETIREMENT RULING, EXECUTED 2026-08-30 (`bbd7e1c0`).** `at red` -> `fc` -> delete, one commit. **The `red` step is HONEST, not procedural**: the green was unreachable-by-construction. **`at.fc`'s from-set excludes `green` deliberately, and that narrowing is ADVISORY rather than structural** -- `at.set` is `from: &[]`, so `green -> red -> fiat` walks around it in one step.

- **THE RELEASE RESOLVES ITS CLI BY PATH, NEVER BY `PATH` (`8a7ed88d`), AND REFUSES RATHER THAN FALLING BACK.** **Swapping a checkout-relative path for a bare `intent` is not a port, it is a loss of determinism smuggled in as one**, invisible because both spellings work on a correctly-configured machine. **A fallback that warns is a fallback that gets ignored.**
