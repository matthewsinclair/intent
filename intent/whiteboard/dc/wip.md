---
node: dc
name: DevX Claude
role: worker
session_id: 5e0c098c-fe49-4647-a59d-07ba720ac5c3
heartbeat_at: 2026-08-27 18:34Z
status: active
focus: "**FOLDED FOR THE BOUNCE, HOLDING FOR hv`s INSTRUCTIONS. 138.7KB to 113.2KB, and it carries MORE than it did.** The fail-open arc closed in one day: fired on my own heartbeat commit, blast radius measured at ONE gated instrument (cutting against my own 0043 counter), scoping premise found false and withdrawn from hv`s board by vc, ruled BUILD IT under vc`s pen, landed `022c7eef`. Clippy `08836393` alongside. **THE DAY`S KEEPERS ARE IN WATCH-OUTS FAMILY 0 AND ALL OF THEM CAME FROM BEING WRONG:** driving a peer`s uncommitted edit made me retract a TRUE finding; my estate sweep was ACCIDENTALLY RIGHT, a true green a blind grep could not have failed; and a partial fix made that sweep FEEL repaired while half stayed broken. **vc named three items that must survive the fold and my board never held any of them** -- now carried, attributed to vc, not measured by me. Parked and unstarted: `bin/devbin check doc` (9 unresolved links, rc=0, so it reports without gating for free)."
claims: [ST0056/07, ST0056/11]
---

# DevX Claude (dc)

**Today's reasoning, dead ends and perishable measurements are verbatim in `.history/20260824/wip.md`. This file is the COLD-SESSION MINIMUM.**

## D42 -- TIME. Read this before writing anything, anywhere.

- **You never ask what time it is** -- not the OS, not `date`, not the filesystem, **and not the database either.** The stamp is applied BY the write.
- **THE RULE IS ABOUT SIGNATURES, NOT VALUES. NO cli or intentsvcs function TAKES a time; they may RETURN times.**
- **A board stamp is a label, not data** -- read it from `date -u` and PASTE, **per stamp, never per session.** The ordering that cannot be fabricated is the **commit**.
- **`stat` PRINTS LOCAL. `git log` PRINTS LOCAL.** Convert at the SOURCE and keep the local value beside it, or a reader appends a `Z` and lands an hour out. I did exactly that today and vc caught it.

## The truth model -- canon, held not restated

`design.md` (D01 as reversed) + `data-model.md`. **The SQLite db is the durable SSOT, files are re-creatable; the typed API is the only door in.** **`intentdb` IS RETIRED AND NAMES NO COMPONENT.** The crates are `intent-cli`, `intentd`, `intentsvcs`; `intentsvcs` solely owns the db and `intentd` is a CLIENT exactly as the CLI is. Diagram `design.md:12-17`.

## The environment

- **`intent` ON PATH IS v3.0.0 AND RESOLVES INTO THIS TREE. `intent3` IS NOT ON PATH AT ALL.** Measured 2026-08-27: both `~/.local/bin/intent` and `~/bin/intent` are symlinks to `Intent/native/rust/target/release/intent`, repointed **26 Aug 23:53**, and `intent info` reports `INTENT_HOME: /Users/matts/Devel/prj/Intent`. **THIS ENTRY PREVIOUSLY SAID THE OPPOSITE IN BOTH DIRECTIONS** -- v2.19.0 routing to the frozen `Intentv2`, with v3 present under the distinct name `intent3` -- and it was stale for a day and a half while I read it every session. **IT WAS NOT CAUGHT BY USING IT; IT WAS CAUGHT BY MEASURING SOMETHING ELSE.** A board line stating an environment fact has no natural corrective, because the environment does not argue back. Consequence worth holding: every estate whose hook shells out to `intent critic` now gets the **v3** binary.
- **`intent3` NOW REFUSES A BINARY THAT CANNOT BE SHOWN TO DESCRIBE THIS TREE** (hv ruled 2026-08-24). Matrix and reasoning: `bin/.devbin/cmd/shared/currency.lib`, one home. **It keys on DECIDABILITY, never on dirt.**
- **THE GATE FIGURE IS COMPUTED BY THREE VERB CALLS AND IS NEVER TRANSCRIBED** -- `intent ac status ST0057`, `intent ac status ST0056/03`, `intent ac gate ST0057`. It lived in THREE homes at THREE values on 2026-08-24, one document disagreeing with itself twice. **Do not put the number on this board; put the calls.**
- **hv's FREEZE SCOPE (2026-08-24): Intentv2 is FROZEN FOR FEATURES and LIVE FOR SHIPPED-SURFACE DEFECTS.** A v3-only defect is a v3-only fix; a shipped-surface one lands in BOTH trees.
- **THE INDEX IS SHARED IN THIS CHECKOUT.** `git add` puts your file where a peer's bare `git commit` sweeps it. Always `git commit --only <paths>` -- **but the refusal is a property of naming a FILE, not of `--only`, and I had it as unconditional.** Driven both arms: `--only <untracked FILE>` exits 1 with `pathspec did not match any file(s) known to git`; **`--only <DIRECTORY>` exits 0, commits the tracked edits and leaves the untracked file behind in silence** (ic). **The success output enumerates what it TOOK, so the omitted file has no line to be missing from** -- this board's own absence rule, arriving at a commit instead of a grep. **`git status --porcelain -- <paths>` after every commit; `??` is the entire signal.** My own `6096e14c` used the directory form and was clean, which was luck rather than care.
- **`int hooks` ANSWERS ABOUT THE TREE THE `int` SCRIPT LIVES IN, NOT YOUR CWD.** To ask about another clone, run ITS `bin/int`.
- **A DEVBIN COMMAND RESOLVES ITS PROJECT FROM SOMETHING OTHER THAN YOUR CWD -- BUT THE v3 BINARY RESOLVES FROM CWD.** Both are true and confusing them cost a live incident today.

## DOING

- **THE FAIL-OPEN ARC CLOSED IN ONE DAY, FOUND TO FIXED, AND EVERY STEP OF IT WAS A CORRECTION OF SOMETHING I HAD ALREADY WRITTEN DOWN.** Index: fired on my own heartbeat commit (`e78c2730`); blast radius measured in the real window at **ONE gated instrument**, cutting against my own issue-0043 counter; scoping premise found false (vc withdrew the clause from hv's board at `1424b587`); vc ruled BUILD IT under hv's pen; landed `022c7eef`. Clippy `08836393` alongside. **Detail is in those commit messages and is not restated here.**
- **RULING 4's DELIVERY STATE, MEASURED ESTATE-WIDE AND STILL TRUE: canon-complete, in force in ZERO estates.** 17 carry the consumer gate; the carrier is `pre-commit.intent` in **all 17**, never `pre-commit`; the refusing arm is absent from all 17. Three `hooksPath` values (`.git/hooks` 14, `.githooks` both Intent trees, `bin/hooks` Laksa), **so any sweep hardcoding `.git/hooks` misses three.** That last line belongs in the sweep runbook, which is vc's; **not written there unasked.** The sweep is hv's and held.
- **NOTHING IN FLIGHT. Lane clean, both trees clean, nothing staged.** Parked and next in TODO.

## TODO

### PARKED, UNCOMMITTED, AND IT DIES WITH THIS SESSION IF NOT RECORDED

- **hv RATIFIED THE SHIM CONVENTION 2026-08-27 AND `bin/int` STAYS. THE RETIREMENT IS NOT A RETIREMENT.** Three rules: **(1)** the shim in a project is always `bin/devbin`; **(2)** a project MAY symlink a 2-3 char shortcut to it, by its own choice; **(3)** tools and process always use `bin/devbin` and NEVER the shortcut. **Verified against the enumeration rather than taken on report: `find bin -maxdepth 1 -type l` in Intent AND Intentv2 returns exactly one link, `int -> devbin`, direct, no project-named intermediate. Both estates are already correct for rules 1 and 2.** Only rule 3 is outstanding.
- **THE PLAN IS FIVE STEPS, NOT SIX, AND THE RISK THAT JUSTIFIED THE HOLD IS GONE WITH THE SIXTH.** Deleting the symlink was the last step; it is withdrawn. **Nothing is deleted at any point, so `bin/int` resolves throughout and a partially-repointed tree is never a broken tree.** There is no window to be inside. Remaining: roster check to the official name; `cmd/hooks` 14 emit sites; regenerate the three `.githooks/*` and CONFIRM the bodies changed; the other consumers (`implemented_check.sh:86`, `no_daemon_required.sh:133`, `migrate_hooks_continuity.rs`, the bats arm); `pre-commit-guards.sh` last and carefully, being shipped canon. **The five canon JSONs stay as records of what was true.**
- **STEP 1 IS PARKED AS `scratchpad/step1-roster-official-name.patch` (2253 bytes) AND WAS REGENERATED WHEN THE PLAN CHANGED.** The earlier both-spellings version existed to cover the deletion window; **with the symlink kept it was machinery guarding nothing**, and leaving a parked patch built for a retired plan is a trap for whoever picks it up. Now a straight rename, driven both ways: default passes, a nonexistent dispatcher refuses at exit 2. **PARKED BECAUSE THREE GATES AGREED:** `cli sync --to-store` refuses (binary behind HEAD), `canon_commit_check.sh` then blocks, and a rebuild is refused because `native/rust` carries a peer in-flight. **Re-apply when the pair is current; never `--no-verify`.** Steps 2-5 held by vc with no hour named -- cc is mid-build in the same neighbourhood, which is a reason that survives the plan change.
- **`bin/intent` IS NOT IN SCOPE.** It is the v2 bash CLI front door, 25 `intent_*` siblings, 197 referencing files; its removal is the v3 cut (AC-12.1), a different subject with a different closing condition. **A hazard that WOULD have been created by that cut is already closed by devbin: `link_alias` was DELETED outright rather than guarded on file existence, so pruning `bin/intent` cannot let a later `devbin install` create `bin/intent -> devbin`** -- the Lamplight collision arriving in the one estate whose product binary is called `intent`.
- **CHANGELOG:80 STATES AN UNCONDITIONAL IMPOSSIBILITY THAT IS CONDITIONAL, AND A FUTURE SWEEP WOULD REASON FROM IT.** It says the alias `in` was impossible, a bash reserved word, a syntax error in bash while working in zsh. **Driven: bare `in test all` is rc=2 in bash and rc=0 in zsh, so the claim holds for BARE invocation only. Path-qualified `bin/in test all` is rc=0 in BOTH.** Rule 2 shortcuts are always invoked path-qualified, so `in` is in fact available. **Nothing to change** -- `int` works and shortcuts are optional -- but the absolute form of the claim would rule out names that are fine.

- **hv HAS ABOLISHED 3.0.2 AND THAT TURNED MY BACKLOG INTO SCOPE.** Verbatim, first-hand via vc: _THERE IS NO 3.0.2. There is 3.0.0 which we have made and then EVERYTHING ELSE IS GOING INTO 3.0.1._ The 20:15Z QUIET is a **BUILD** (2b), not the cut; more builds follow as the rest lands (bucket ingest + prune, the attachment walk, the issue-body write defect, B1's remaining criteria), and **the cut is when 3.0.1 is COMPLETE, on hv's go**. **TWO OF THE THREE ARE DONE (`53699d66` smoke arm, `d6ddb874` stamp fix); THE `init` GITIGNORE CALL IS ic's AND STILL OPEN.** _(Was: all three blocked until 2b's pair is stamped -- true when written, expired 2026-08-26 20:23Z.)_, because each touches `bin/.devbin` (arm 10 censuses it from the WORKTREE) or `native/rust` (arm 1): the smoke arm, the `init` gitignore call, and the stamp-versus-subject fix. Sequenced after 2b so nothing under `bin/.devbin` is ever mid-edit under a build.
- **THE RULING STRANDED THREE DEFERRALS IN SHIPPED SOURCE, AND I SWEPT FOR THEM RATHER THAN WAITING TO MEET ONE.** `native/rust/crates/intentsvcs/src/facade.rs:137` holds AC-11.3's other half (the migration realising ONLY declared threads) _to 3.0.2_; `tests/attachment_carry.rs:142` and `:242` defer the attachment allowlist the same way. **Each names REAL work whose destination no longer exists**, and a comment deferring to a release that was abolished reads to the next person as a live decision. The boards fold themselves; **source comments do not**. Reported to vc for hv -- not mine to edit, and `native/rust` is frozen for the build regardless.

- **THE `init` GITIGNORE CALL IS THE SURVIVING FINDING FROM A PREMISE THAT DIED WITHIN THE HOUR.** I claimed read verbs rewrite views; they do not, and the smoke arm now proves it. What held up is narrower and real: `converge_gitignore` EXISTS at `facade.rs:152` and only the MIGRATION population reaches it, so a freshly `init`ed project gets no `.gitignore` convergence. ic's file, in 3.0.1 scope. **The mechanism was never missing -- one population reaches it.** **THE DEFECT IS WRITTEN INTO THE FUNCTION'S OWN DOC:** `facade.rs:111` says this is the one moment a project acquires one -- true of the v2-to-v3 population, false of fresh init, and the sole caller is the migration path at `facade.rs:1376`.

### LIVE, MINE, UNSTARTED

- **THE KEG SHIPS NO RULE LIBRARY AND NO SKILLS. THE CODE FIX IS DONE (`0112b8c1`) AND IT IS NOW NAMED IN vc's 3.0.1 CUT LIST, SO THE SHIPPING HAS A DATE.** `SUPPORT_PATHS` carries the rules and skills trees; `support_paths_coverage` refuses at `stage` if v3 resolves an install-root path the tarball would not carry; `int macos smoke` is driven RED against the live keg and GREEN against a fixture at `total: 6 rule(s)`. **UNTIL THE RE-CUT PUBLISHES, THE ESTATE'S KEG STILL ARMS 0 OF 0 ON EVERY LANGUAGE** -- the fix and the shipping are different things and only the second one reaches a user. Cause found by devbin/vc as a PACKAGING gap, located to `:116` by vc, resolver enumerated by me.

- **FLEET WORK IS LIVE AND HALTED BY hv (2026-08-26 11:53Z, relayed devbin-cc -> vc). MY FOUR ARE DIRTY AND UNCOMMITTED -- see DOING for the exact table. hv LIFTS IT, NOT vc.**
- **FLEET WORK UNDER vc's RUNBOOK IS THE TOP ITEM AND hv AUTHORISED vc TO DIRECT IT.** `intent/whiteboard/vc/cutover-runbook.md`, plus `verify-canonical.sh` (self-test re-driven by me after vc's fix: **11 failures, threshold 11**). **THE CUTOVER IS 16 PROJECTS IN TWO SHAPES, NOT 9** -- Form B projects wire `session-context` and `require-in-session` but NOT `session-finish`, so the third hook is missing fleet-wide. **`Intentv2` IS NOT A MEMBER AND MUST NEVER BE MIGRATED** (hv ruled it unprompted; the tool warns unprompted too).

- **WP-07's `cwi` PORT IS DONE (`1ad284b3`) AND IT DOES NOT DISCHARGE `AC-14.12` -- WRITTEN HERE SO NO LATER READER COLLAPSES THEM.** WP-14 retires the VERBS (served from the store per `AC-14.7`); WP-07 retired the `bin/` COUPLING. Two different things, two different closing conditions, and hv's ruling gave the `claude ws` family an expiry the gate enforces (`97cc09ca`).

- **THE `bin/` COUPLING HAS AN EDGE NO SWEEP FOR `source` CAN SEE, AND IT NOW LIVES IN CANON RATHER THAN ON THIS BOARD.** `intent/plugins/claude/lib/claude_plugin_helpers.sh:84` CALLS `ext_root_dir()`, defined only at `bin/intent_helpers:367`, and never sources it -- so the file is coupled to the tree `AC-12.1` prunes while every sweep spelled _sources `bin/intent_helpers`_ reports it CLEAN. Post-cut it fails at **`unknown`, rc=0**, with `command not found` on STDERR only, because `plugin_root_tag:85` short-circuits an empty `ext_base` deliberately. **Full two-sided drive, both arms, and the widened class (2) are in `AC-12.1`'s text (`434dded6`) -- read it there.** **REPORTED, NOT BUILT:** shipped plugin canon inside the cut scope, and vc runs the order.

- **THE QUIET GATE: BOTH ITS SCOPE AND ITS FIGURES WERE WRONG IN OPPOSITE DIRECTIONS, AND vc HAS FIXED AND RETRACTED BOTH** (`dce909f7`). **The half worth keeping is that NEITHER OF OUR POSITIVE CONTROLS COULD HAVE CAUGHT IT:** vc added a matcher to an instrument already matching everything, which exercises only the arm that FIRES; I controlled the pattern SYNTAX, which says nothing about what else it sweeps in. **Between us we controlled the same half twice, and the arm that must FALL SILENT is the entire value of a quiet gate.** The estate rule this sharpens: a control must drive the verdict the CALLER acts on. The argv trap itself is a one-liner in FAMILY 1.
- **QUEUED BY vc FOR MY LANE, VERIFIED BY ME, NOT STARTED -- `bin/devbin check doc` (2026-08-27 18:2xZ).** A whole defect class has a detector that ships with the toolchain, is on by default, and **has never been switched on here**: a doc comment linking to a symbol that no longer exists. **`cargo test` never resolves an intra-doc link**, so it is invisible to the entire suite. ic found six today from one hv ruling that reached the code and not the prose.
  - **THE FLAG IS THE FINDING, NOT THE COMMAND.** Private items are undocumented by default so rustdoc never resolves their links, and ~half of `intentsvcs` is private: a default `cargo doc` reports ZERO on a planted deleted-symbol link. `--document-private-items` names it immediately.
  - **MEASURED MYSELF RATHER THAN TAKEN ON REPORT: 9 unresolved links, and `cargo doc` exits rc=0 on them.** That second half is the design answer vc was reaching for -- **it reports without gating for free**, so a plain config entry satisfies "report the population, gate nothing yet" with no wrapper. Run in a PRIVATE `CARGO_TARGET_DIR`.
  - **CANNOT GATE ON DAY ONE:** the nine sit in `address.rs`, `bootstrap.rs`, `facade.rs`, `graphql.rs`, `organize.rs`, `store.rs` -- other nodes' files. Estate pattern applies: report the population, gate the property, name what is not gated. **The other 21 warnings are a DIFFERENT thing (public docs linking to private items) and must not inflate the count.**
  - Shape is a `check:` option in `bin/.devbin/config.yaml` (`summary:` + `run: bin/.devbin/cmd/measured --exec ...`). **hv holds this as a decision, not a discovery (`9335f7a5`); nothing waits on it.**

- **THE hv QUEUE: FIVE LIVE, NONE BUILT, ALL TOUCHING INSTRUMENTS THAT GATE OR DESCRIBE THE BUILD.** (1) **roster symmetry** -- `runner_roster_check.sh` reads PRESENT from the COMMIT and ROSTERED from the WORKTREE; four occurrences 2026-08-25, the fourth PREDICTED from the write-up before it happened. (2) **the staging-dir build** -- build into staging, verify, move into place: closes ic's absent-window and vc's concurrency gap with no lock, so no stale-lock failure after a SIGPIPE; **measured at 252 real refusals during one 66-second rebuild.** (3) **`verify_pair` compares the binary against HEAD when the binary's subject is `native/rust`** (ic, confirmed on my own build, which redded because a peer's canon commit landed during my 54-second compile) -- the remedy it implies is a rebuild that races the same board traffic. (4) **D42 amended** -- the rule is NO CALLER AUTHORS A STAMP; the signature test is one sufficient condition, never the definition, and a generic setter reaching `created` evades the proxy while producing the forbidden behaviour. (5) **arm 7 asserts ORDER and nothing asserts LATENCY** (vc's framing, sharpened onto my own arm): cargo WAITS on a locked target rather than failing, so a verdict taken at T can be followed by an unbounded block -- mechanism in Watch-outs, not restated here. **RETIRED FROM THIS QUEUE: the marker's scope blind spot, shipped as `d6ddb874` and now held by arm 6c.** Routed, not reached into.
- **THE ROSTER SYMMETRY FIX -- FOUND BY ME, QUEUED FOR hv, AND EXPLICITLY NOT MINE TO BUILD AT SPEED.** Read ROSTERED from the COMMIT as PRESENT already is. **It gates every commit in the estate and I caused two estate-wide blocks the day I found it**; vc endorsed the hold in those terms. It is the same conversation as the roster-charter ruling already with hv, because both are about WHAT POPULATION the roster is entitled to read.

- **`cmd/macos` provenance writer** so `provenance_fields_check.sh` (AT-11.7) has a green to reach. **STILL DECLINED ON SCOPE** -- WP-11 is RELEASE and hv asked for local usability. **TRAP: `codesign --force` REWRITES THE BINARY IN PLACE**, so nothing may hash until `verify_notarised` passes; and `cmd/macos:1294` parses `commit:` with a `sed` -- ADD fields, never rename that one.
- **THE ATTRIBUTION GUARD -- RULED IN BY hv IN vc's SESSION, AND I AM HOLDING FOR hv's WORD IN MINE.** One arm, gates, assert `^Claude-Session:` ABSENT, nothing about `(C)`. **Three conditions, none optional:** (1) key on the COMMIT, never the `wb|board|localfold` subject convention -- 29 of 90 carriers were topic-prefixed real work (ic); (2) ANCHOR it -- the bare string matches a message that merely DISCUSSES the trailer, and it matched two of ours (ic); (3) **a positive control the guard actually FAILS -- mine, and it is the one that makes it an instrument** (see Family 1). **ic's fourth, found by hitting it: the success condition is a `grep -c` INVERSION -- `grep -c` exits 1 when the count is ZERO, which is the PASSING case, so `grep -c ... && ok` blocks every clean commit. COMPARE THE NUMBER, NEVER THE EXIT STATUS.**
- **NEW, AND IT IS MINE BECAUSE I FOUND IT IN MY OWN FILE: NOTHING VERIFIES THAT A ROSTER ROW DESCRIBES WHAT ITS RUNNER DOES.** `runner_roster_check.sh` verifies row-to-file EXISTENCE in both directions and is structurally blind to the row's CLAIM. cc asked that a mechanism, if one is built, be mine. **No mechanism proposed yet, and naming it is not building it.** **AND vc HAS PUT A GATE IN FRONT OF BUILDING IT, WHICH I AM RECORDING BECAUSE IT WOULD OTHERWISE DIE WITH THIS SESSION: DO NOT BUILD BEFORE hv HAS RULED WHETHER A DESCRIPTION IS IN THE ROSTER CHARTER AT ALL.** vc framing: this is a guard whose POPULATION IS FILES where the CLAIM IS BEHAVIOURS, and **widening a guard contract silently is how a roster becomes the mechanism.** So the next step is a ruling, never a checker.

### ROUTED, MEASURED, NOT MINE TO TAKE

- **ISSUE `0086` (HIGH, ic's find, QUEUED NOT ASSIGNED): `intent --help` rc=0 while `intent help` rc=2 `retired`, with a remedy claiming no replacement exists.** **Its general defect is this board's own class from a third direction: an ABSENT `spelling` field is RENDERED as a confident negative, and a passing test REQUIRES it to be** -- absence-as-meaning, forbidden by the preamble of the very file it lives in. Not mine unless assigned.

- **`VIEW_NAMES` (`address.rs:357`) CLAIMS A COUPLING THAT IS NOT IN THE CODE -- ANSWERED, FILED AS ISSUE `0087` (low, vc, `4f9ce518`), NOT MINE TO FIX.** Its doc says `Project::classify` _is the single answer_; `classify` keeps its own inline list and never reads it. **No harm driven and none claimed** -- they may be legitimately different populations, in which case the DOC is the defect. **The row survives only because the sentence is un-greppable and reads as an architecture guarantee.**
- **ISSUE `0085` -- THE ADVISORY HOOK FIRES ON EVERY WRITE REGARDLESS OF FINDINGS.** `[ -z "$findings" ]` can never fire: both binaries always put `critic:` header lines and an `ok:` line on STDOUT, the stream the hook captures. **Violates `session-finish.sh:46` -- _a hook that fires identically every time carries no signal_ -- three files from where that sentence is written.** v2 does it identically, so **INHERITED: a `corrected`-class contract decision in `AC-07.3(a)`'s shape, with the contract and not with me.** Opt-in twice. **OPEN QUESTION I DELIBERATELY DID NOT DRIVE (23:16Z, holding): `0085`'s premise rests on what the hook's FLAGGED invocation emits -- `--files <f> --severity-min warning --format text` -- and my drive used plain `--files` only.** vc verified independently and it is their filed issue, so re-driving it unasked is scope creep. **One command if anyone wants it, and worth knowing the premise has not been driven at the flags the hook actually passes.**

- **THE FROZEN-`$INTENT_HOME` MECHANISM: THE DETECTOR HALF IS CLOSED, THE ROUTING IS NOT.** vc landed the ref fix and the CI arm (`a38e884b`, `18ccfbbc`), measured in CI's own log rather than in simulation. **vc states plainly that their reason for `not discharged` expired but the ROUTING did not, and a guard cleared by a peer saying the ruling happened is not a guard.** It sits with hv. **What the guard should ASSERT under the freeze scope is still the unsettled half.**
- **THE SUITE POPULATION CHECK.** `git ls-files` 112 vs `find` 113, gap named, two commands produce it and nothing runs them. **Needs an edit to `tests/run_tests.sh`, which matts runs externally -- scope, not doubt.**

## Watch-outs

### FAMILY 0 -- THE SHARED CHECKOUT AND THE BLIND INSTRUMENT (2026-08-27, all measured, most of them on me)

- **DRIVING AN INSTRUMENT TELLS YOU ABOUT THE WORKTREE AT THIS INSTANT, NEVER ABOUT THE RUN YOU ARE INVESTIGATING.** I settled a question with a clean, determinate, two-sided fixture drive and **retracted a TRUE finding** on it -- the file was a peer's mid-patch working copy, and the branch I was "confirming" did not exist during the incident. `git show <commit>:<file>` is the only way to interrogate a past run; a live drive is not a substitute. **vc's sharpening: a shared checkout manufactures FALSE NEGATIVES preferentially at the moments two nodes converge on one defect -- and the convergence is the CAUSE, not the coincidence.** So "I verified it and it is fine" is least trustworthy exactly when a peer is also looking. **Cheap habit: before concluding anything about a past run from a live drive, `git status <the tool>`.** If it is dirty, the drive is about somebody's edit.
- **A TRUE GREEN FROM A BLIND INSTRUMENT IS WORSE THAN A FALSE ONE, BECAUSE IT GETS CITED.** My estate figure "0 of 5 hooks carry the arm" grepped `pre-commit`, which **carries the gate in no estate** -- that grep could not have returned anything else under any state of the world. **It also survived the correction that felt like the fix**: I had already caught the `ls .git/hooks/` blindness and re-swept honouring `core.hooksPath`, which corrected WHERE it looked and not WHAT it looked at. **A repaired instrument is trusted more than an unexamined one, so the residue after a partial fix is more dangerous than the original defect.**
- **THE PARITY APPARATUS HAS FOUR ROOT CONTRACTS, TWO MUTUALLY INVERSE.** 7 honour `ROOT=`; 6 resolve unconditionally from the script's own location; **2 derive from CWD's git toplevel, where `cd` works and `ROOT=` is IGNORED**; 2 take a positional. **Nothing at the call site distinguishes them**, so the same technique means "fixture" on one tool and "the real tree, reported as a pass" on the next. **laksa-cc's fix-shape is the right one and better than mine: `${1:?...}` -- DELETE the default rather than add an override**, so an absent root is a loud startup error instead of a silent real-tree pass. Not built (contract widening across gated instruments; vc gated that class behind an hv ruling).
- **A CENSUS CANNOT REPORT THAT IT MEASURED THE WRONG PREDICATE.** My estate audit counted STRINGS (`102` in Laksa) and was structurally blind to the **7 sites that EXECUTE**, which is what turned it into a red suite. I led with the count that was large and blind over the one that was small and load-bearing. **Three of my own greps returned false negatives today**, one caught only because a 3.4s instrument scoring zero executions is not credible, and one caught by a peer checking that 7 + 6 is 13 of 18. **Check your own total.**
- **PROBE-BY-EXECUTION IS SAFE ONLY IF THE WRITE TARGET IS REDIRECTED AND THE REDIRECT IS HONOURED -- AND NOTHING VERIFIES THE SECOND HALF.** Three of my gated instruments execute (`$BIN doctor`; generators under `OUT="$tmp"`; `$DISPATCH precommit --list-guards`). None mutates real state today. But **6 of 13 tools here ignore `ROOT=` outright**, so "pass the variable and it redirects" is empirically false of half this apparatus. Hazard named, **not driven.**
- **A RESTORE MUST RESTORE EVERY INPUT THE CONSUMER KEYS ON, AND THE CONSUMER DECIDES WHAT THOSE ARE (ic).** A metadata-preserving restore puts bytes back with the ORIGINAL mtime; cargo keys on mtime, so the next run links the MUTATED artefact while `git diff` says clean. **Both my prescribed checks interrogate CONTENT and are structurally blind.** Only ic's baseline-re-run closes it, because it is the only check that exercises the actual consumer. **When the null case behaves like the mutated case, stop testing the mutation and go find the shared input.**
- **ADDITIONS MAY LEAD, REMOVALS MUST TRAIL (laksa-cc).** Laksa's suite went red at HEAD with nothing in `lib/` changed, because a removal landed ahead of its references. Conflab did the same change COUPLED and paid nothing. **This independently validates withdrawing the delete step from my five-step rename** -- I had it as "the risk is gone"; it is a rule.
- **AN ANOMALY THAT SURVIVES A GOOD CORRECTION IS NOT NOISE -- IT IS THE PART OF THE SUBJECT THE CORRECTION DID NOT REACH.** cc's correction of my stale-hook claim was better measured than mine and right about the repair. **The one fact it could not explain was a line in my own commit output**, and chasing that located `pre-commit.intent`. Neither of us had the answer alone.
- **CONTENTION MANUFACTURES STAGED-AND-STRANDED.** The index was locked continuously for ~15 minutes; `git add` succeeds and the commit then loses the race, so files sit staged in the shared index for as long as the queue lasts. **cc landed a commit sweeping a peer's work inside exactly that window.** Not carelessness -- the condition produces it. Lock was live not stale (grew 221699 -> 221827 bytes); **never delete a peer's lock.**

- **THE AUTHORITATIVE STATEMENT WAS IN THE FILE, ON MY SCREEN, AND A BELIEF ABOUT THE SEAM OUTLIVED IT.** I told devbin-vc that Intent repointing `bin/int` would be undone by devbin on the next re-vendor, because `bin/.devbin/cmd/hooks` emits it 14 times. **`bin/.devbin/cmd/` is the PROJECT's half of the seam, and `manifest.sha256:3` says so in its own header** -- _Files not listed here -- config.yaml, cmd/, help/ -- belong to the project_ -- **a line I had quoted in my own tool output an hour earlier while working out where `use` lived.** Confirmed after the fact three ways: 0 manifest lines under `bin/.devbin/cmd/`, 16 under `lib/cmd/`, 0 hits for `bin/int` anywhere in devbin's source. **The conclusion was wrong and the correction cost a peer a message; what makes it worth keeping is that reasoning harder would never have found it and going back to the artefact did, first try.**

- **AN OVERRIDABLE-LOOKING KNOB THAT IS NOT OVERRIDABLE REPORTS A PASS ABOUT THE WRONG SUBJECT.** `ROOT=<fixture> bash thread_view_skew_check.sh` ran against the REAL tree and printed `288 generated view(s) match the model`, rc=0 -- because line 70 is an unconditional `ROOT=$(...)`, not `${ROOT:-...}`. **I had written that down as the absent-binary arm.** What caught it: the absent arm and the present arm printed BYTE-IDENTICAL output, and **two arms that cannot disagree are not two arms.** The real drive puts a copy at the same directory depth so the `../../../../..` lands in the fixture. **General form, and it is one step past this morning's lesson: that one was reconstructing a question the subject already answered; this one is an instrument whose knob was connected to nothing. Before trusting a fixture drive, prove the FIXTURE is what ran.**

- **THE THIRD FACE OF THE SAME FAMILY, AND IT COST THE MOST BECAUSE IT LOOKED THE BEST: A CORRECT INSTRUMENT, CORRECTLY DRIVEN, POINTED AT THE WRONG SUBJECT.** I drove `lib/templates/hooks/pre-commit.sh` two-sided and reported `rc=1` as a fact about estates. It is a **template, installed in nothing**: the arm I was testing appears in ZERO of five installed hooks and the arm it replaced in all five, Intent's own included. **Nothing about the drive was weak, which is what makes this the dangerous one** -- every quality check I know how to apply passed, because they all interrogate the DRIVE and none of them ask what the artefact IS. **A template is a SOURCE, not a deployment**; `lib/`, `share/`, `dist/` and anything called `templates/` are sources by name. **Ask what EXECUTES before reporting a drive in the present tense**, and when the answer is a copy installed at some past upgrade, the finding is about the NEXT upgrade rather than about today.

- **NEVER MUTATE THE LIVE SUBJECT. COPY IT, MUTATE THE COPY, RUN THE COPY, DELETE THE COPY -- SO THERE IS NO RESTORE TO GET WRONG. AND THE UNIT OF THE COPY IS WHATEVER THE SUBJECT NEEDS AROUND IT TO RUN: a script is a FILE, a Rust source is the WHOLE TREE** (ic, correcting the file-only form I circulated). For compiled sources there is no invocation-time indirection to exploit -- `facade.rs` is compiled at its path in its crate, there is no `ROOT` to hand a copy -- **so the Rust form is a detached worktree at HEAD carrying only your files, mutated in there, deleted after.** Same three properties: the live tree is never touched, there is no restore, and no trap can lie. cc lost a mutation into `intentfiles.rs` because the `trap` used RELATIVE paths and fired after a `cd` into `native/rust`: both restores failed, **the trap printed `[restored]` anyway**, and a live mutation stayed in the tree. **A trap that announces a restore it did not perform is worse than no trap, because no trap makes you check** -- the census sentence pointed at cleanup rather than detection: it reports what it ATTEMPTED, so it reads identically on the day it restored nothing. **AND THE FAILURE MODE THE TREE FORM CLOSES IS NOT THE RESTORE, IT IS THE WINDOW.** ic's R6 restores were done RIGHT -- absolute paths, `cmp -s` against a pristine copy rather than a grep, a final `git diff` confirming their own fix survived -- and nothing leaked. **What was missing is the property the copy is actually for: for three compile-and-test cycles a mutant `facade.rs` sat in a tree three other nodes share one `.git/index` with, and nobody swept it. That is luck, not design.** A correct restore does not shrink that window; only not mutating does. **Today's three mutations were immune by construction** (copy in the scratchpad, `ROOT="$PWD"`, `rm` after), and the reason was avoiding a peer sweeping a stray file out of the shared index rather than restore-safety -- the immunity was a side effect worth making deliberate. **If a live mutation is unavoidable: absolute paths in the trap, and `git diff --quiet -- <file>` as the ASSERTION rather than a printed message.** Grepping for the mutation string proves the STRING is gone, not that the FILE is the file, and those come apart exactly when a restore half-worked, which is the case you need it for. (vc, relaying cc.)

- **A FLAG NAMED FOR SCOPE CAN DECIDE SEVERITY, AND GUESSING WHICH COSTS THE WHOLE ESTATE.** `thread_view_skew_check.sh --changed <paths>` does NOT narrow what is CHECKED -- all 288 views are examined either way and the cost is identical. It narrows what BLOCKS: skew in an untouched path is REPORTED as inherited, skew in a touched path blocks. **Dispatched unnarrowed, one stale view nobody in the session wrote stops all five nodes until its owner returns.** Read the flag's USE, never its name; `canon_commit_check.sh` is the opposite case (narrows internally, so a path trigger would be a second copy of the narrowing).

- **FIVE CROSS-PROJECT CONSTANTS WERE PROPOSED TODAY AND ALL FIVE FAILED ON MEASUREMENT -- AND THEY ARE NOT PROPERTIES OF CARGO, THEY ARE PROPERTIES OF HOW A TREE HAS BEEN BUILT OVER TIME.** (1) phantom-contamination of a duplication metric: Conflab 46%, Intent 11%. (2) _high `maxMult` implies `windows-sys`_: true in Conflab, false in Intent. (3) _heavy tails are near-universal_: Conflab top-10 29.2%, Intent 42.8%. (4) MINE -- _generations multiply shared and unshared equally_: false, they over-represent the UNSHARED side. (5) the redundancy ratio itself: Conflab cli 70.5%, Intent 62%, Conflab daemon 56.2%. **Each sounded like a fact about the toolchain and measured as a fact about one project's build history, which is why no two agree.** Operational rule, evidenced five times: **NO PROJECT ADOPTS ANOTHER'S NUMBER, ONLY ANOTHER'S METHOD.**

- **CARRIED FROM vc 2026-08-27, NOT MEASURED BY ME, AND RECORDED BECAUSE vc BELIEVED MY BOARD ALREADY HELD THESE AND IT DID NOT.** Attribution matters here: these are vc's findings and I am the second carrier, not a witness.
  - **STEP 0 IS UNSAFE WITHOUT ITS CORRECTION: CONFLAB IS DEBUG-LINKED, so the target-clean recipe does not hold there.** vc's words -- that correction is the whole value of the finding, and **a fold keeping the recipe while dropping the correction is worse than one dropping both.** Attached to the step-0 entry below rather than filed separately, so the two cannot be separated again.
  - **`ps -Ao comm=`, NEVER `pgrep -f cargo`.** In a fleet of LLM sessions **the agent prompts ARE process argv**, so a full-command-line match hits sessions merely DISCUSSING cargo. Same class as the anchored-grep condition on the attribution guard: a bare string matches the mention as readily as the subject.
  - **THE UNGATED-COMMIT WINDOW IS DEMONSTRATED, NOT ARGUED: `e5bf283`, and the window was opened by vc's own rebuild.** vc rates this the strongest thing on the staging-dir build's side, and **it arrived by a completely different route from my skew-check reasoning** -- which is what makes it worth keeping separately rather than merging into that argument.

- **NEVER DELETE A TREE SOMETHING ON `PATH` RESOLVES THROUGH, AND CHECK BEFORE, NOT AFTER. THIS IS STEP 0 OF ANY BUILD-CACHE CLEANUP.** `~/.local/bin/intent` and `~/bin/intent` are SYMLINKS into `native/rust/target/release/`, so Intent's PATH binary IS the tree's release artefact -- an unscoped `cargo clean` removes `intent` machine-wide. **AND CONFLAB IS WORSE: its four PATH symlinks (`conflab`, `conflabd`, in BOTH `~/.local/bin` and `~/bin`) resolve through `target/DEBUG`**, which is exactly what a debug-cache cleanup deletes. **I wrote and SENT an estate recipe saying _delete `target/debug`, preserve `target/release`_ -- safe where I derived it, destructive one project over.** The check: walk `~/.local/bin`, `~/bin`, `/usr/local/bin` for symlinks matching `*/target/*` BEFORE moving anything. **A recipe that is right in the project it was born in and catastrophic in the next is worse than no recipe, because it travels with my confidence attached.**

- **WHEN THE FAILURE IS "MY BOUND WAS TOO SMALL", A BIGGER BOUND REPRODUCES THE CLASS WITH A DIFFERENT CONSTANT -- THE FIX IS NO BOUND, PLUS A PRUNE FOR SPEED.** I went `-maxdepth 4` (found 7), widened to 6 (found 11), verified at 7 and **declared ALL COVERED. The true count is 12.** Every sweep was arithmetically correct and structurally identical, and **the fourth instance of the day landed inside the check I ran to CLOSE the class.** The working form: `find <root> \( -name .git -o -path '*/target/*' \) -prune -o -type d -name target -print` -- unbounded, pruned for speed, and classify by content (`CACHEDIR.TAG || debug/ || release/`) rather than by path shape. **Widening a limit is the reflex; removing it is the fix.**

- **INDEPENDENT DERIVATION IS NOT CORROBORATION WHEN THE INSTRUMENTS SHARE A SHAPE (vc's, 2026-08-27, and it is the sharpest thing either of us said).** vc ran their OWN census -- not a copy of mine -- with the same `-maxdepth 4`, got the same seven, and **we both read the agreement as confirmation.** It confirmed the SHAPE OF THE QUESTION, not the world. **This is WORSE than a single instrument: two matching numbers retire exactly the doubt that would have made either of us look again.** The defence is not more measurers, it is measurers whose FAILURE MODES DIFFER -- ours were identical, so we had one instrument reported twice, with double the confidence attached.

- **VERIFY THE THING THAT DEPENDS ON THE CHANGE, NOT ONLY THE THING BEING CHANGED (one entry, both ends, agreed with vc).** Mine: I checked `target/release` was intact and never asked what depended on `target/debug` -- which is how the Conflab recipe went out. **vc's, same day, opposite end:** taking the frozen Intentv2 off PATH, they checked what that entry SHADOWED and never what it PROVIDED, so `intent claude start <ws>` (v2 plugin, `intent_claude_cwi:299`, a stub in v3) stopped working and **hv could not start sessions.** One class, two directions.

- **ENUMERATE TARGET DIRS FROM THE FILESYSTEM, NEVER FROM THE LAYOUT YOU EXPECT -- AND `.worktrees/` IS THE ONE EVERY SWEEP MISSES.** My estate sweep used `find -maxdepth 4 -name Cargo.toml` and returned SEVEN. The real count is **ELEVEN**: a Tauri target at depth 5 (`Lamplight/native/macos/desktop/src-tauri`, **12G**) sat outside the depth bound, and **three more live inside `Lamplight/.worktrees/{cc,ic}/`** (**10.6G**, one with 44,113 deps files) because peer nodes run in git worktrees each carrying a FULL target tree. **22.6G and four trees invisible to a sweep that looked healthy.** Correct form: `find <project> -type d -name target -not -path '*/target/*'`. **Consequence for any fleet cleanup: a node cleaning the main checkout reports success while two-thirds of a peer's cache sits one directory over.**

- **THE GUARD'S OWN REFUSAL REMEDY BUILDS AN UNBOUNDED CACHE NOBODY COLLECTS, AND IT IS INVISIBLE TO EVERY `target/debug` MEASUREMENT.** `releasebuild.lib:189` tells a refused caller `CARGO_TARGET_DIR=<dir> int build $verb`; each node pointed it at `target/<node>/`. **`target/cc` reached 534,337 files and 33G** (409,142 in `debug/deps`) while `target/debug` was being measured as the whole problem. Laksa has the same shape (`target/witness`, `target/witness-identity`). **I WROTE ARM 10 AND DID NOT SEE IT** -- the guard is correct about what it refuses and says nothing about what its remedy accumulates. **A remedy is part of a guard's contract; if it creates state, something must bound it.**

- **THE BUILD-CACHE SWEEP METHOD, WHICH IS THE PART THAT TRANSFERS WHEN NONE OF THE NUMBERS DO.** Group `deps/` by `(crate, hash)` -- **a hash identifies ONE BUILD UNIT across its `.rlib`/`.rmeta`/`.d`/executable**, so a generation drops WHOLE -- keep the newest per crate, move to a trash dir **INSIDE `target/`** (so it stays under `.metadata_never_index` for the whole delete), verify, THEN delete. **`0 unmatched files` is the check that matters:** a dry run reporting only KEEP and DROP cannot tell _nothing fell outside the grouping_ from _something was silently spared_. **TWO PRE-FLIGHTS THAT DO NOT TRANSFER AND MUST BE RE-MEASURED PER TREE, because cargo's copy-versus-hardlink behaviour is a platform and version detail:** the live binary's generation must be IN the keep set (hash every `deps/<bin>-*` against it), and `target/release/<bin>` must be a separate inode with `links=1` -- a COPY, not a hardlink into `deps/`.

- **A ZERO AFTER A DELETE MUST BE CHASED, NOT REPORTED.** `bin/int cli ac list ST0056` returned zero rows straight after my sweep. It was the PAIR GUARD -- cc changed `legacy.rs` after the binary was built, so `int cli` correctly refuses a binary behind HEAD -- and bare `intent ac list` answered normally. **The condition predated the sweep.** Had I reported the count rather than reading the output, **I would have shipped a false all-clear about my own destructive action.**

- **A COMMENT IS NOT INERT IN SHELL: A LINE OPENING `# shellcheck` IS A DIRECTIVE, AND A MALFORMED ONE MAKES THE LINTER STOP RATHER THAN CONTINUE.** I wrote a prose comment explaining a shellcheck fix; it began with the word, so SC1072/SC1073 fired and **the whole file went unanalysed -- strictly worse than the SC2155 I was fixing, and it still reported as `1 finding` either way.** The count could not tell _one warning_ from _the linter gave up_. **Read the output; and positive-control a clean lint so a green is distinguishable from an instrument that stopped** (append a known-bad function to a copy and prove it is caught).

- **A SURVIVING FILE THAT STILL DEPENDS ON `bin/` FAILS OPEN AFTER THE CUT, AT rc 0, WITH STDOUT THAT CAN BE BYTE-IDENTICAL TO THE CORRECT ANSWER.** Driven in a fixture tree with no `bin/` at all: ported `cwi` gives rc=0 and ZERO stderr; **pre-port `cwi` gives rc=0 and the SAME stdout**, with five `command not found` lines on stderr only. **Under `2>/dev/null` they are indistinguishable.** And the sharpest part: **the missing symbol is `error()` ITSELF**, so the No-Silent-Errors mechanism is the thing that goes missing and execution continues past every guard it was protecting. Second instance of the `ext_root_dir` class in one day, same subsystem. **This is why AC-12.1 class (2) is a correctness gate and not a tidy-up.**

- **THE CLASS I WAS THE COMMON FACTOR IN THREE TIMES IN ONE DAY (2026-08-27): A TRUE ANSWER TO A NARROWER POPULATION THAN THE QUESTION.** (1) `grep 'INTENT_ROOT/bin/'` -> 2 of 7 coupled files. (2) _is `target/release` intact_ -> true, and never asked what depended on `target/debug`. (3) `find -maxdepth 4` -> 7 of 11 target dirs. **Every time the instrument was healthy, the number was correct, and the population was wrong -- so nothing looked broken and no positive control could fire.** The discriminator is not _is my instrument working_ but **_is my instrument pointed at the set the question is about_**, and that is a question about the QUESTION, which is why running the tool again never surfaces it.

- **A WRONG-BUT-ADJACENT CORRECTION FROM A PEER IS A SIGNAL TO WIDEN THE CHECK, NOT TO WIN THE POINT (2026-08-27, vc).** vc reported `Lamplight/pdf_extractor` unmarked. **The path does not exist and the real one was already marked** -- I could have closed it in one line. Chasing the general smell instead found **four unmarked trees and 22.6G**, including the 10G worktree neither of us had thought about. **vc was pointing at something real from the wrong coordinates.** Rebutting the coordinates would have banked a win and left the estate broken.

- **A DEFENSIVE BRANCH WRITTEN TO STOP ONE FAILURE BECOMES THE THING THAT HIDES AN ABSENCE, AND THIS IS THE TRANSFERABLE HALF OF 2026-08-27's FINDING -- BETTER THAN THE BUG IT CAME FROM.** `plugin_root_tag:85` short-circuits an empty `ext_base` to `unknown` DELIBERATELY, because an empty value would collapse the `case` pattern to `/*` and swallow every absolute path. Correct, careful, and **exactly what converts a missing dependency into a plausible wrong answer at rc=0** once `ext_root_dir()` stops existing. **THE GUARD DOES NOT DISTINGUISH _the value is legitimately empty_ FROM _the thing that computes it is gone_, and it was never asked to.** Ask it of every defensive default: **what ELSE produces the value this branch treats as normal?** vc's same-day instance is the sister case and the pair is the lesson -- a collector that found no files and **had no way to say _I looked in the wrong place_**, costing 23 attachments silently (repaired `5441945`, gated in `reconvert.sh` at `7f947de4`). **Absence-as-a-normal-value is the family; my `ext_root_dir` case and vc's bucket case are two members, and the board already holds a third in issue `0086`'s absent `spelling` field rendered as a confident negative.**

- **THE BLIND GREP THAT RETURNS A TRUE NUMBER FOR A NARROWER QUESTION THAN THE ONE ASKED (vc's framing, banked 2026-08-27 in vc's terms because mine were worse).** Sweeping the plugin family for its `bin/` coupling, `grep 'INTENT_ROOT/bin/'` returned **TWO files and read as a complete answer**. The real count is **SEVEN**, in three spellings: `$INTENT_ROOT/bin/` x2, `$INTENT_HOME/bin/` x1, and a two-step `INTENT_BIN="$INTENT_ROOT/bin"` then `source "$INTENT_BIN/..."` x4. **The instrument was not broken and the number was not false** -- it answered _how many files spell it THIS way_, and I asked it _how many are coupled_. **A positive control would NOT have caught this**: the pattern does match where it matches, so the control passes and the blind spot is untouched. **What catches it is enumerating the SPELLINGS of the thing before counting instances of one of them.** Same family as the four wrong counts of 2026-08-26 and as `pgrep -f` reading our own prompts as evidence.

- **`.prettierignore` IS GITIGNORE SYNTAX, WHERE A SINGLE `*` DOES NOT CROSS A `/`, AND A FENCE VERIFIED ONLY BY A GREEN IS NOT VERIFIED.** The obvious narrow list fences less than it looks like. **And proving prettier left a file alone proves nothing unless you also prove prettier was AWAKE** -- perturb the file, re-run, and check the probe line comes back changed; three of the fences I first called verified had never been exercised. Four repos fenced 2026-08-27 (Molt, MicroGPTEx, Utilz, Conflab); the standing rule that the formatter is a SECOND WRITER between sync and commit lives in FAMILY 4.
- **A FENCE VERIFIED ONLY BY A GREEN IS NOT VERIFIED.** Proving prettier left a file alone proves nothing unless you also prove prettier was awake: three of these four repos had ZERO files prettier would have rewritten, so the fenced-file check passes identically with the fence, without it, and with a typo in it. The pair is the test -- misformat the fenced view AND an unfenced file beside it, and require the second to come back reflowed.

- **A LOG LINE IS NOT A HEAD MOVE, AND ON A FIVE-NODE BRANCH THE NEWEST COMMIT IS USUALLY NOT YOURS.** On the bounce I read `git log --oneline -1`, saw ic's sha where I had left my own, and was one step from reporting a reset of a shared HEAD. **It had moved FORWARD:** `git branch --contains` put both my commits on `main` and the reflog showed ic's five landing on top. The check that settles it is `--contains` plus the reflog, never the tip alone -- and the shared-checkout rule already says a reflog read PRECEDES acting on a surprising HEAD, which is the only reason this cost nothing.

- **A PIN IS RETIRED BY A SCHEMA BUMP, AND IT FAILS ONLY ON THE WRITE PATH.** `pair-f7240814` spoke store schema 13; ic's WP-14 took the store to 14 at `9bd6b0a3`. It still READ everything, so it looked entirely healthy until a write verb refused. **The half that still works is what makes this dangerous** -- a pin gone bad on both paths would have announced itself the first time anyone touched it. **THE APPARATUS IS RETIRED AND THE INVENTORY WITH IT (vc withdrew the instruction); the two general rules are what survive.** The second is mine: a pinned copy parked OUTSIDE the checkout cannot run hop 3 at all -- `claude upgrade --apply` walks up from the exe looking for `lib/templates/` and refuses at rc 1 from anywhere else, so **_pin it out of tree_ and _hop 3 works_ are in direct conflict.** `bin/int cli` -- gate pair coherence, then exec the in-tree release binary -- is what satisfies both.
- **QUOTE CHARACTER IS NOT A PROSE/COMMAND BOUNDARY IN SHELL, AND I NEARLY INSTITUTIONALISED THAT IT WAS.** I had a fix queued that stripped backticked AND double-quoted spans as prose. cc drove the counterexamples: a `die` with UNESCAPED backticks round a build RUNS it, and a `printf` with a dollar-paren build inside double quotes RUNS it despite the first word merely emitting. My strip erased both. **A guard that hides a live build is worse than the false positive it was fixing**, and only single quotes are genuinely inert. The axis is _can this span execute_, never _which character wraps it_. What shipped is narrower pattern-matching, not a lexer -- cc's framing is the right test: _is this lexing, or pattern-matching that has been lucky so far_.

**2026-08-26, THE THREE THAT COST THE ESTATE SOMETHING TONIGHT. Read these before the long list.**

- **ONE COMMIT IS NOT ONE MOMENT, AND THE GATE READS THE WORKTREE.** This board already said _both sides move in one commit_ and that is insufficient. Changing `DIRT_SCOPE` and its reader in three separate tool calls left minutes in which `shared_artefact_build_guard.sh` correctly reported _could not read DIRT_SCOPE_ and **refused every node's commits.** **For a change spanning a declaration and a LIVE reader: write both halves in ONE write, or park the first until the second is ready.** The commit boundary is invisible to a guard that never looks at commits. **Same shape as arm 10 reading `bin/.devbin` from the worktree, which this board had warned about since that morning -- I had filed it as a fact about `bin/.devbin` rather than as a fact about guards that read worktrees, so the general form was not available when I needed it.**
- **A TIME THAT CAME OUT OF A TOOL CARRIES WHATEVER ZONE THAT TOOL CHOSE, AND APPENDING `Z` IS AN ASSERTION, NOT A FORMAT.** `stat -f '%Sm'` prints LOCAL. `git log --date=format:` prints the COMMIT'S OWN recorded zone and **IGNORES `TZ`**, so `TZ=UTC git log --date=format:` returns local and looks like it worked -- `--date=format-local:` is the form that honours `TZ`. **I hit both in one evening, the second inside the audit built to catch the first.** Use `date -u`, or `date -u -r <epoch>`, or say nothing. **Now skill canon (vc, `bf35c01f`).** And the audit finding: **7 of my 11 typed message stamps that evening were never read from a clock, while every board heartbeat was sound -- the discipline held exactly where a command produced it and failed everywhere a hand typed it.**
- **A GREEN THAT CANNOT TELL WHAT IT NAMES FROM SOMETHING ELSE PRODUCING THE SAME GREEN.** Three sightings in one evening in three people's work, none found by review, all found by driving: `quiet.sh` reading a narrower scope than the gate it fronts; vc's two unarmed B1 proofs (`force_without_a_tty_...` passes because the EOF confirm refuses too; `default_removes_no_file_...` passes because the fixture makes the verb take the no-op arm); and **my own smoke arm, where swallowing the read verb entirely left `read verb left the tree clean` reporting ok** -- of course it did, **a verb that never ran leaves the tree clean too.** **A mutation pass returning all-armed has told you nothing about which greens are load-bearing.**

**2026-08-26 WAS ONE CLASS IN NINE MATERIALS, AND IT IS NOT A HABIT OF MINE -- IT IS THE DOMINANT DEFECT SHAPE IN THIS CODEBASE: MY EVIDENCE WAS TRUE AND MY SUBJECT WAS WRONG.** Nine instances, not nine mistakes: in a TRIGGER (author != defect), in MY OWN CENSUS (a file supplied from context, reported MISSING against my own invention), in a STATUS, in a BORROWED PREMISE (a measurement is evidence; **the cause attached to it is a hypothesis wearing the measurement's authority**), in the SHELL (a non-null WRONG reading printing a plausible product behaviour), in an AGREEMENT (`2b's pair` changed from content to time in silence), **in the PRODUCT'S DOCUMENTATION** (`facade.rs:111` -- true of the migration population, false of fresh `init`, stated generally), **in the PRODUCT'S CODE** (`emit_source_commit`), and **in MY OWN PROPOSED FIX for that code**, caught before a line was written.

**THE TWO IN THE PRODUCT WERE THE POINT AND BOTH ARE NOW CLOSED**: the code by `d6ddb874`, **enforced by `shared_artefact_build_guard.sh` arm 6c**; the docstring routed to ic with `converge_gitignore`. **Seven live in reasoning and are reachable by discipline. The two that were compiled into the artefact were not, which is why they became code and guards rather than another line on this list.**

- **`ac new` IS AN IDEMPOTENT PUT, NOT AN INSERT, AND IT REWRITES `state` FROM `--kind` WHOSE DEFAULT IS `non-test`.** Amending a `kind: test` criterion without passing `--kind test` silently flips it and breaks its AT's coverage. **The word _idempotent_ could equally have meant no-op** -- read `ac_new`'s source (`facade.rs:4191` via `Facade::put`) rather than the adjective. **And the view is never the door:** `acceptance.md` is a `GeneratedView` (`address.rs:VIEW_NAMES`), so the edit goes through the store or the next render discards it.

- **AND `at new` IS THE SAME CLASS ONE NOTCH WORSE: IT HARDCODES `note: None` AND `legacy: None`, SO THERE IS NO CORRECT INVOCATION AT ALL.** `ac new` rebuilt `state` from `--kind` -- dangerous, but a flag existed to get it right, and reading the source first is what saved me. **`at new` has no `--note` flag, so reading the source does not save you**; the only door onto a row's `file` destroys its note on the way through, and the verb's own help says _idempotent_, which is exactly what makes it read as safe to repeat. **Six real losses measured in git history (ic's `3d9ea45f`), 248 of 334 rows exposed.** **THE GENERAL FORM: WHEN A WRITE VERB CONSTRUCTS A WHOLE RECORD AND PUTS IT, EVERY FIELD WITHOUT A FLAG IS SILENTLY DELETED -- and the fields without flags are exactly the ones a caller does not know to look for.** Preserve-on-PUT is the safer default of the two, because a caller ignorant of a field cannot then destroy it.

- **AND I THEN OVERCLAIMED IT INTO A BLOCKER, WHICH IS THE WORSE HALF AND IS MINE ALONE.** From _no verb writes `note`_ -- true -- I reported to vc, cc and ic that the work **could not be done** and that there was _no sequence of today's verbs_. **The verb sentence was right and the work sentence was wrong**: a canon edit plus `intent sync --to-store` carries `note` and `file` together, because they were never separated, and it is **documented in the thread I was working in** (`ST0056/info.md:52`). ic drove the counter-example; I had `sync` in a verb listing ON MY OWN SCREEN while writing the conclusion. **I was hunting a writer for the FIELD and missed the door for the ROW.** **A FALSE BLOCKER IS WORSE THAN A MISSED HAZARD: a hazard gets found again, a blocker just sits there looking prudent** -- and this one would have stopped a 168-row sweep for the wrong reason.

- **THE GENERAL FORM, AND IT IS THE DAY'S CLASS ARRIVING IN MY OWN WORK RATHER THAN IN SOMEONE ELSE'S: A TRUE ANSWER ABOUT THE MECHANISM YOU SEARCHED, PUBLISHED AS AN ANSWER ABOUT WHETHER THE THING CAN BE DONE.** I had it twice today from the other side -- the mentioning-grep that counted comments as calls, and vc's instrument-shaped `AT-12.1` note -- and did not recognise it in the sentence I was writing. **The tell is a negative claim whose scope word is a MECHANISM (_no verb_, _no flag_, _no `source` line_) doing duty for the OUTCOME. Widen to the outcome before publishing, and ask what OTHER door reaches the same row.**

- **AND IT ARRIVED FIVE TIMES IN ONE SESSION, ALL IN INSTRUMENTS I BUILT, SO IT IS NOT A LAPSE -- IT IS MY DEFAULT SHAPE.** (1) a grep that counted `error` in 8 files when call sites were 3, comments and string literals scoring as calls; (2) a rule classifier that guessed at how a declare-none is SPELLED and reported 6 undeclared where the runner's own report said 0 -- **the subject's report beats my reconstruction of its criteria, every time**; (3) a hand-picked tool farm that made v2 fail to PARSE, read as a refusal, **inside a test file whose own comment warns against exactly that**; (4) the NO-DOOR claim, true about verbs and false about the work; (5) `head -6` hiding the seventh line that would have stopped me calling lua/swift the founding defect. **FOUR PUBLISHED, ONE CAUGHT BEFORE SENDING.** The one I caught, I caught by READING THE WHOLE OUTPUT rather than the part I had asked for.

- **THE CHEAPEST CORRECTIVE, DERIVED FROM ALL FIVE: WHEN THE SUBJECT REPORTS ON ITSELF, READ ITS REPORT BEFORE BUILDING A SECOND OPINION.** The critic prints its own census; `git` prints what it committed; `shellcheck` prints which check fired. **In four of the five I built a text-level reconstruction of a question the subject answers directly** -- and the reconstruction is always the thing that guesses at spellings, populations and adjacency. Build the second instrument to CROSS-CHECK the subject's report, never to replace it, and when they disagree the subject wins until proven otherwise.

- **A FIELD IS ONLY DEAD IF SOMETHING SAYS SO, AND THAT CHECK IS WHAT SEPARATES A DATA-LOSS BUG FROM A DELIBERATE SUNSET.** Before reporting the above I tested the opposite hypothesis: `note` might be a migration-only field being retired, in which case nulling it is BY DESIGN. **It is not** -- `note` is first-class on `AcceptanceTest` in `model.rs` with no deprecation marker, while `legacy` RIGHT BESIDE IT documents its migration provenance in the struct. **The two readings predict identical behaviour and are told apart only by the declaration**, so the schema is the instrument, never the observed nulling.

### FAMILY 5 -- THE GATE THAT DID NOT RUN, AND THE VERB THE NAME SENDS YOU TO

**ic's class, consolidated 2026-08-26 from five instances and grown to ten: A CONSTRAINT NAMES THE ACTION A HUMAN PICTURES WHILE THE GATE DOWNSTREAM READS A STATE.** Its sharpest corollary, which cost a blocked lane today: **a check that did not run is indistinguishable from one that passed** -- and the unguarded path is repeatedly the one the name sends you to. **hv's fix list lives here.**

- **THE TWO STAMP INSTRUMENTS ARE INTERCHANGEABLE FOR PROVENANCE AND NOT FOR VIABILITY, AND THE VIABILITY HALF HAS NO ENTRY ANYWHERE ELSE.** `source_commit.rs` computes ONE value and emits both `INTENT_SOURCE_COMMIT` (what `--version` prints) and `INTENT_SOURCE_COMMIT_MARKER` (what `artefact_source_commit` greps) from one home `include!`d by both crates, so they cannot diverge within a crate -- mixing them across the pair is SAFE for provenance. But `--version` proves the binary RUNS through its own code path while the marker is a `strings` grep proving only that the BYTES carry it, so the lift EXECUTES `intent` and merely GREPS `intentd`, and **a correctly-stamped non-viable daemon passes**. Viability is otherwise proven only at `int macos prepare`, after stage/sign/notarize, which is a late place to learn the daemon will not start. `intentd --version` exits 0 and closes it free. **AND THE STAMP IS NOT ALWAYS A SHA -- the value carries dirt INSIDE it and reads the WORKTREE, so report it VERBATIM, never normalised or trimmed to the sha:** against a dirty tree the stamp is the TELL, and trimming deletes the only evidence the lift was asked for. `intentd --version` also still answers the daemon's `not yet implemented` sentence, so a parser told to take both stamps off `--version` gets one stamp and a sentence.
- **ARM 7 ASSERTS ORDER AND NOTHING ASSERTS LATENCY, WHICH IS TRUE, INSUFFICIENT, AND LOOKS SUFFICIENT.** It proves the dirt verdict is taken before the first cargo invocation. **cargo does not FAIL on a locked target, it WAITS** (`waiting for file lock on ` is in this cargo's own strings, driven), so the gap between verdict and actual compile is unbounded and the tree can move inside it. A process-list read NARROWS that window and cannot close it -- **a process list is a statement about the past the instant it returns.** What closes it structurally is the queued staging-dir build: build private, verify, move into place, no lock and no process list in the argument. **So the process read is evidence FOR that item, not a rival to it.**
- **A GATE WHOSE SUBJECT IS A FILE FIVE WRITERS SHARE HAS A BLAST RADIUS NONE OF ITS ARMS DESCRIBE.** `shared_artefact_build_guard.sh` reads `cmd/local` from the **WORKTREE**, so my in-flight refactor of that file refused EVERY node's commits -- vc's doc commit twice -- while my work was still uncommitted. **Nothing in arms 1-13 says "this arm will block your peers while somebody edits its subject".** The way out is not `--no-verify` (vc: _I am not bypassing the gate for a doc commit; nobody should_): it is to land the change in a shape where the arms are true, **moving the arms in the SAME commit as the code they follow.** Which is also the general rule this cost me: **a guard's subject is part of the guard, so a refactor of the subject is a refactor of the guard.**
- **SYNC CANON FIRST -- IT READS THE WORKTREE -- THEN COMMIT THE FILE AND ITS CANON TOGETHER, AND THE OBVIOUS ORDER IS THE WRONG ONE.** `canon_commit_check.sh` blocks a commit whose canon names bytes it does not carry, because canon stores an attachment's FULL TEXT rather than a hash. **Commit-then-resync leaves THAT commit permanently divergent** -- the later sync fixes the next one and can never fix this one, and the criterion is a property of every commit, not of HEAD. Order: `sync --to-store <ID>`, then commit both paths. **Scope the sync to the ID and snapshot both sides first: an attachment is AUTHORED, so nothing can re-derive either side, and doctor's own remedy points the OPPOSITE way from what an editor wants** -- `sync --to-disk` writes the store's copy over your working file. Met for real on 2026-08-27 committing the arm 10 fix.
- **A CENSUS THAT EXAMINES ONE OF FIVE AND REPORTS GREEN IS THE FALSE GREEN THE GUARD EXISTS TO REFUSE.** Arm 10's first draft printed `(1 examined)` against a tree carrying five, and PASSED -- so the count is printed and is the first thing to read. Third instance in one day of a wrong COUNT reading as authoritative: `int macos smoke` said fails 6 while listing 3, and ic's population question was the same shape. (The mechanism that caused it is in the tool traps below; this entry is about the count, not the grep.)

- **A `bin/`-ONLY PUSH IS LINTED BY NOTHING IN CI, AND THE BOOT FILE'S STATED BACKSTOP FOR IT IS FALSE.** Three gates, all cleared by a shell-only change: `cmd/prepush:258` skips the clone check unless the push touches `native/`, `Cargo.*` or `rust-toolchain`; `rust.yml` uses a `paths:` allow-list of `native/rust surface schema`, and the shellcheck that DOES fail closed lives inside `cargo test --workspace` inside that job, so its teeth are present and the workflow is never invoked; `tests.yml`'s shellcheck job cannot fail (it pipes to a non-blocking echo and is absent from `test-summary`'s `needs`) over a subject that has MOVED -- measured 27 matched, all v2 `bin/intent*`, and **43 missed including `cmd/macos`, `cmd/local`, `cmd/prepush` and `build.d/release` itself.** Only the local pre-commit critic sees these, and that is `core.hooksPath`, which a fresh clone does not inherit. **`.claude/restart.md:9` tells every booting node that unwired hooks are caught because `cargo fmt --check` runs on every push. It does not** -- it is `rust.yml:67`, inside the one `rust:` job, behind the same allow-list that excludes `bin/**` entirely. Every node boots through that file and none of us checked it.
- **WHEN CLIPPY IS WIRED INTO THE RELEASE VERB, THE GATE IS THE FLAG, NOT A WARNING COUNT.** The verb runs zero clippy today (ic, confirmed). Positive-controlled on a scratch crate with one deliberate lint: bare `cargo clippy` prints the warning and **EXIT=0**, `cargo clippy -- -D warnings` **EXIT=101**. So `if ! cargo clippy` is inert and `if ! cargo clippy --workspace --all-targets -- -D warnings` is not. **That exact spelling already has two correct homes -- `cmd/prepush:335` and `rust.yml:69`, character-identical -- so a count-based gate would be a THIRD home for one predicate**, which is the drift the consolidation exists to prevent. Copy the flags; do not invent a technique.
- **THE RELEASE VERB NEVER TESTS THE TREE IT TAGS.** `preflight()` holds every gate and runs ONCE at `:482`; every mutation -- version stamp `:499`, `agents sync` `:663`, `claude upgrade --apply` `:690` -- happens after it; `:770` tags. **Any test whose subject is a value the cut writes is structurally untestable by the cut's own gate.** cc's five schema faces were one instance, not the class. Composed with "no workflow fires on a tag", CI on the `main` push is the ONLY gate in the estate that tests the tagged tree -- which makes main-first-then-tag load-bearing rather than prudent.

### FAMILY 1 -- THE INSTRUMENT THAT CANNOT VARY WITH ITS SUBJECT

**THE HEAD, ARRIVED AT 2026-08-25 AFTER SIX INSTANCES IN ONE EVENING AND PAID FOR MANY TIMES SINCE: AN INSTRUMENT THAT RETURNS THE SAME ANSWER WHATEVER THE SUBJECT DOES HAS MEASURED NOTHING, AND ITS OUTPUT IS INDISTINGUISHABLE FROM A REAL RESULT.**

**THE TEST: RUN IT WHERE THE ANSWER SHOULD DIFFER, OR YOU HAVE NOT TESTED IT AT ALL.** Not _check your instrument_, which is unactionable. **Drive the case that MUST come back different.** Corollary paid for 2026-08-26: **a control that exercises only the arm that FIRES says nothing about the arm that must FALL SILENT**, and the silent arm is usually the whole value.

**THE REASONING TRAPS, ONE LINE EACH. Each was a separate incident; the narratives are in `.history/` and in git.**

- A **no-op cannot fail**, so it measures nothing. Force the real path before believing a green.
- A **negative assertion needs a fixture it actually refuses**, or it is not an instrument.
- **Run the NEGATIVE control, not only the positive one.** A pattern that matches what it should is half a test.
- **A control is only a control if its ground truth is KNOWN rather than RECALLED** (vc). A remembered ground truth is a hypothesis.
- **Declare the expected denominator BEFORE measuring**, then check the actual against it (vc). _12 expected_ refuses a `0` on sight.
- **Expect the red, name it in advance, and treat an unexpected GREEN as a finding about the instrument** (vc).
- **A count is not a measurement until something says what each hit IS** (vc).
- **An assertion placed AFTER the write it guards tests the writer, not the subject -- and it cannot fail.**
- **Two readings of one source is one instrument counted twice** (cc).
- **A measurement taken before your own write is stale by construction.**
- **Line-index surgery must be bottom-up WITHIN a block, not only across sections** -- an insert above a later delete shifts it, and **every pre-mutation assert still passes, because it ran before the shift.** Anchor on TEXT, or assert the deleted line's own byte count (2026-08-27, my own fold script: it removed the wrong watch-out and printed the wrong entry's size while doing it).
- **A grep cannot tell a statement from a sentence ABOUT a statement** (vc, `0076`) -- which is why arm 6c and arm 7 strip comments.
- **A window boundary reported as an ORIGIN** (ic) -- `--grep` over _the last N commits_ returns a boundary, not a first occurrence.
- **A range with no pin names a distance from a moving point; a pin with no range names bytes with no consequence** (ic).
- **A count over a `dirty-` marker is a FLOOR, not a distance** (ic).
- **A guard whose predicate depends on WHEN it runs rather than on WHAT it reads has a window that closes silently** (vc).
- **A probe's choice of VERB silently selects which subject answers**, because the currency guard is per-command.
- **The harness ran the subject under different shell options than its only production caller** -- fifteen green arms over a broken function.
- **A check can introduce a premise nobody made and then fail it; a false `MISSING` reads exactly as confidently as a true one.**
- **A correct finding can carry a wrong citation, and the citation is the half that gets reused.**
- **A retired premise keeps issuing orders** -- a remedy correct when written keeps being followed after its condition expires.

**THE OPERATIONAL TRAPS -- these are the ones that return a plausible wrong answer rather than an error.**

- **NEVER `$?` AFTER A PIPE.** `cargo test` needs `--no-fail-fast`. **`grep -c` exits 1 on zero**, so `grep -c ... && ok` inverts, and `|| echo 0` fires too and yields `0\n0`. **Compare the NUMBER, never the exit status.**
- **The Bash tool's shell is ZSH:** unquoted `$var` does NOT word-split (a `for` over one runs once and looks finished), an **unmatched glob ABORTS the whole command**, and an apostrophe inside a single-quoted `perl -e` runs nothing.
- **`2>/dev/null` converts a broken probe into a clean answer.**
- **A backtick inside a quoted grep pattern is a command substitution.**
- **`find` walks into `target/` and dies at the timeout, returning a partial answer that reads as complete.** Scope to `native/rust/crates`.
- **`find` here is bfs, not GNU find** -- `-newermt` is REFUSED and prints 0 at exit 0 under `2>/dev/null`.
- **A `sed -i` that matches nothing is a no-op, and a no-op `sed` exits 0** -- a write that did not happen reports success. **The address is half the match**, and a bare line number is a second silent predicate that decays the moment a line is inserted above it.
- **`bash -n` is wrong in BOTH directions and neither is visible from its output** (half vc's).
- **`--help` IS NOT A PROBE** -- under INV-07 it exits 0 whether or not the command is built.
- **`intent info | head -1` is the PRODUCT BANNER, identical from every cwd BY DESIGN.** I nearly withdrew a correct fix over it.
- **A grep's zero -- and a case-sensitive miss -- is a claim about the CORPUS.** Positive-control before believing it.
- **`find`-based populations describe the WORKING TREE, never the commit** (ic, `run_tests.sh:89`).
- **`grep -n` on a SINGLE file emits `<line>:<text>` with no filename**, so a `cut -d: -f3-` copied from a multi-file call eats the text.
- **A truncated line preview answers about the line's PREFIX, not the line** -- on this board's prose-length lines that is almost always the wrong question.
- **`ps | grep '[g]it commit'` is not a probe for a running git in this estate** -- it matches a peer LLM session whose prompt quotes the command. **On a box running LLM sessions the agents' own instructions are part of the machine's observable state:** match the EXECUTABLE (`pgrep -lx`), and count PIDs, never lines.
- **A version defect can return the correct answer, exit 0, and put the error only on STDERR** (bash 3.2 `declare -A`).

### FAMILY 2 -- THE CLAIM EXCEEDS THE MEASUREMENT, AND THE TRUE HALF IS WHAT CARRIES THE FALSE ONE

- **MY EVIDENCE WAS ENTIRELY TRUE AND MY SUBJECT WAS WRONG, AND THAT COMBINATION HAS NO TELL.** Every fact I cited checked out; the thing they were facts ABOUT was not the thing under discussion. **A reader verifying the evidence confirms it and learns nothing about the error.** Second half, separable and also real: **a commit range with no pin names a distance from a moving point**, so `A..HEAD` came back empty because both ends predated my own last commit -- an empty range reads as _nothing changed_ and means _you asked the wrong question_.
- **A FALSE CLAIM THAT LATER BECOMES TRUE FOR A DIFFERENT REASON IS THE WORST WAY FOR ONE TO AGE.** `bc38db85` said cc's half was met; it was wrong when written and true an hour later, for `1583d1ad`. **There is then nothing left to correct and no trace it was ever wrong.** Correct forward in its own commit (`a163ba56`) and leave the error in history, because a peer had already read the claim.
- **A SCOPE RULING SILENTLY NARROWS YOUR SEARCH POPULATION, AND NOTHING ANNOUNCES WHEN THE RULING EXPIRES.** I told hv the install artefact DID NOT EXIST. It exists: `bin/.devbin/cmd/macos` carries ten verbs including `formula` and `publish`, and **its header at `:81-96` states my own option-3 finding, MEASURED, before I derived it today.** I never looked there because hv had ruled `cmd/macos` out of scope that morning -- **and I am the node who DECLINED `AC-11.7` on scope grounds, so I of all people knew that file held the release mechanics.** The ruling was correct, it removed a file from my corpus, and when hv started release engineering the premise died with nothing to mark it. **The withdrawal reason had even written the trigger down -- _re-mint this row when release engineering starts_ -- so the expiry WAS recorded, on the row, and I still did not re-derive the population.** A decision that names its condition can be retired; **it does not retire itself.**

- **NOTHING CONNECTS A DECISION TO THE CONDITION IT WAS MADE UNDER, AND A DEFECT'S DISAPPEARANCE ANNOUNCES ITSELF TO NOBODY.** Three instances on 2026-08-25, three nodes, one shape (vc's count): AT-11.7 refused twice from a premise hv had retired; vc's `hv/wip.md` scope line reasoned from hv being unreachable, still standing after hv was reachable; **and mine -- I switched to a bare `git commit` because `--only` was dying on a cache-tree phantom, my own fixture leak, fixed an hour later in `48ba2103`, and I never switched back.** **A defect announces itself; the end of a defect does not**, so the workaround stays and the protection it disabled stays off. **THE EXPIRED PREMISE IS THE HARDEST CLASS HERE BECAUSE NOTHING IS EVER WRONG AT THE MOMENT ANYONE LOOKS AT IT.**

**A CONFIDENT UNMEASURED NUMBER IN A _RATIONALE_ IS LOAD-BEARING IN A WAY ONE IN A _REPORT_ IS NOT -- A REPORT GETS CHECKED, A RATIONALE GETS HONOURED** (vc's formulation). All three of my shipped defects are instances.

**AND ITS INHERITED FORM, PAID FOR 2026-08-26 AND WORSE THAN THE SELF-INFLICTED ONE: A PEER'S MEASUREMENT ARRIVES WITH A CAUSE ATTACHED, AND THE CAUSE IS THE PART NOBODY MEASURED.** vc's _167 tracked views rewritten_ was real. _By read verbs_ was the attribution, and it was the load-bearing half. I put the whole sentence into this board as established fact and built a release-note recommendation on it. **Worse, I then read my OWN result through the borrowed premise instead of testing it.** My fixture ran the PAIR and never once ran the 3.0.0 keg, so I held exactly zero evidence about the keg -- and I wrote vc the sentence _YOU MEASURED THE SHIPPED KEG; THE PAIR DOES NOT DO IT_, which asserts a CONTRAST whose other half I had not measured. The half that was mine (_the pair does not rewrite on read_) was sound and helped kill the claim. The half that made it a finding was borrowed. **A measurement handed to you is evidence; the cause attached to it is a hypothesis wearing the measurement's authority, and quoting the number is what launders the second into the first.** Restate a peer's figure with its cause marked as theirs and unverified, or measure the cause yourself before it becomes a premise in your own file.

- **`bin/intent3` claimed a coherence check would be "a MULTI-SECOND gate on every command".** Driven: **~85ms end-to-end**, against a component sum measured separately at ~110ms. **The two do not reconcile and I have not resolved them; neither is within two orders of magnitude of the claim.** It was the entire stated reason for not building the thing hv later ruled I should build, and it held the design shut for three days.
- **`cmd/hosting` claimed "in a throwaway clone a mutator harms nothing".** Never driven. Every verb ran from the LIVE tree's cwd and it emptied the durable store.
- **`currency.lib` PRINTED A FLOOR AS A DISTANCE** -- the overclaim in the error message of the file written to refuse overclaims, **and the fix for it added an arm nothing drove** (ic). **A fix that adds an arm adds a thing to drive.**
- **A WARNING IS NOT DISCHARGED BY BEING TRUE -- IT IS DISCHARGED WHEN THE REMEDY IT INVITES IS ALSO CHECKED** (ic, against themselves).
- **MY TELL vs cc's, AND THEY ARE MIRRORS:** I publish the claim the measurement INVITES (wrong at the READING); cc drives the measurement and publishes a stronger claim about what it measured (wrong at the WRITING). **Both rest on a real number, which is why neither gets challenged.**
- **A ROW CARRYING ONE TRUE SENTENCE AND ONE FALSE ONE IS HARDER TO CATCH THAN A WHOLLY WRONG ROW, BECAUSE THE TRUE HALF IS WHAT A READER CHECKS FIRST AND IT HOLDS** (cc, on my false roster row).
- **`checked against the schema` VERSUS `checked against a grep of the source` IS THE WHOLE DISTANCE, AND NEITHER OF US COULD SEE IT IN OUR OWN SENTENCE AT THE TIME OF WRITING.**
- **A STABLE CONCLUSION ACROSS THREE WRONG POPULATIONS IS NOT CORROBORATION -- IT IS THE POPULATION NOT BEING LOAD-BEARING FOR THAT CONCLUSION** (cc, on vc's withdrawal).
- **A HAND-MAINTAINED SET THAT NOTHING CHECKS IS THE ROSTER PROBLEM,** and I built one while explaining it. **Manifest plus a drift check, and a path in NEITHER the manifest nor a declared-exclusions list is an ERROR rather than a judgement call** (vc).
- **UNCHECKED IS NOT EMPTY, AND ONLY THE OUTPUT CAN CARRY THAT DISTINCTION.** An instrument that measures a narrow scope and reports in the vocabulary of the general category produces a **true sentence and a false belief**, so nothing it can check is wrong. Four instruments, one shape: `0069`, `0074`, `0075`, `0076`. **A silent gap gets found; a gap that files a clean report on its own behalf does not.**
- **A RULE IS HONOURED BY WHOEVER LEARNED IT AND DOES NOT PROPAGATE BY HAVING BEEN STATED.** `precommit` has 14 guard arms at three strengths; **7 assert a repository finding on ANY non-zero exit.** The arm BURNED by this class guards the tool being MISSING and is **still exposed to the tool being BROKEN** -- driven, not read. **Fixing one more arm in passing would be the same error a fourth time; the class needs one change across seven sites.**
- **A SUPERSEDES NOTE APPENDED BELOW WHAT IT SUPERSEDES LEAVES THE DEAD CLAIM AS THE HEADLINE.** `acceptance.md:199` still OPENS with the green arm vc amended away, with the correction far below it. **`intent/restart.md` leads with exactly this: if you find yourself writing a supersedes banner, DELETE WHAT IT SUPERSEDES INSTEAD.** It is the gate-figure defect at document scale -- three homes, three values -- and **a builder who reads the row's first sentence builds the wrong guard.** Raised with vc; the amendment's REASONING is worth keeping, the dead arm is not.
- **A GUARD CAN HAND OUT A REMEDY THAT CAUSES THE DEFECT ANOTHER GUARD EXISTS TO PREVENT, AND NEITHER CAN SEE IT.** The currency guard refuses a stale binary and prints `int local build`; that rebuilds into `native/rust/target/release/`, **shared by four nodes with no isolation underneath it**, from whatever the tree currently holds. AC-11.6 exists because exactly that emptied the store for every node on 2026-08-18. **Each guard is correct about its own property. The composition is the defect, and nothing in the roster has a view of it.**
- **A WRONG LINE NUMBER IS ONLY A TRAP IF FOLLOWING IT CAN REACH A GREEN.** vc warned the 3.2 error at `:216` would lead a fixer to gut the exemplar. Built it: the error MARCHES to `:225` and stays there. **Misleading, not trapping -- and the marching is what tells you to stop chasing and look up.**

- **A CORRECT MEASUREMENT DESCRIBED IN THE WRONG TERMS TRAVELS AS THE WRONG RULE, AND THE OBSERVATION BEING TRUE IS WHAT STOPS ANYONE CHECKING.** Twice in a day, both mine. **(a)** I reported `settings.json` WIRES four hooks; it wires THREE, the scripts dir ships four, and `post-tool-advisory` is unwired ON PURPOSE (ST0035 decision #4, in the script's own header). My code was right throughout -- it unions both sources -- so the true half carried the false half undetected and the conclusion was unaffected. **The wrong figure is durable: it is in the body of `a40cf763` on shared `main` and will not be rewritten, so it must not be inherited into the AT-07.2 row.** The correct sentence is _ships four, wires three, union four_. **(b)** `git status` genuinely showed one staged line, so I wrote _one staged file_ and thereby implied **staged** was the predicate; it is **every uncommitted path**. My own guard measures it correctly and my sentence about my own guard did not. It reached ic through vc inside a minute, and ic caught it only by DRIVING the build instead of reporting success.

- **A NUMBER WHOSE SUBJECT IS NOT ON THE LINE CANNOT BE CHECKED BY THE NEXT READER.** vc's board carried `Gate still PASS at 67 of 67`; **no thread gate is 67** (64/134, 51/53, 2/6). It was `ST0057` 51/51 **plus** `ST0056/03` 16/16 -- **two thread gates summed, with nothing on the line saying so.** I could tell it was uncheckable WITHOUT being able to tell whether it was wrong, and that is the whole finding. **`0088`'s citation half in numeric form**; vc's `wip.md` now prints all three calls rather than the sum.
- **A SUPERSEDES BANNER LEAVES TWO VALUES IN ONE HOME, AND THE TRUE HALF IS WHAT MAKES THE FALSE HALF READABLE.** `vc/wip.md:8` OPENED `ST0057's GATE IS BLOCKED AT 51/53` and CLOSED `Gate still PASS at 67 of 67` -- one `focus:` value, two gate states, written by the node whose announce was ABOUT a false gate figure. **`restart.md` names this exact defect and states the fix: _if you find yourself writing a supersedes banner, DELETE WHAT IT SUPERSEDES INSTEAD._** vc deleted both (`e63df597`), and I rewrote my own falsified watch-out in place rather than banner it, at 23:14Z. **The rule applies to the node that sends it first.**

- **A `git status` IS PERISHABLE IN EXACTLY THE WAY A TIMESTAMP IS, AND MINE WAS STALE AT BIRTH RATHER THAN EXPIRED.** I told vc _none of the dirty files are mine, my paths are clean_ while quoting the BOOT-TIME status -- having edited my own board two calls earlier. **The rest of the class is a sentence that was true and went stale; this one was never true**, and that is worse precisely because nothing about a file list reads as perishable. **Re-drive the status at the moment of the claim; never quote an earlier one.** Caught by vc, not by me.

### FAMILY 3 -- ROUTING, RELAY AND ATTRIBUTION

- **THE OFFER IS THE MOMENT TO CHECK, NOT THE SEND.** Once two messages are in flight the only tools left are racing and deduping, and only one works. Three instances, one mechanism.
- **A FORWARDING OBLIGATION DISCHARGED BUT NOT CANCELLED MANUFACTURES FALSE CORROBORATION** -- it arrives TWICE wearing TWO authorships and **the second announces itself as agreement. Tell the RECIPIENT to expect one copy; the recipient can dedupe, the relay can only be beaten.**
- **VOLUNTEERING A RELAY _IS_ CREATING THE OBLIGATION, AND THE COORDINATOR HABIT IS TO VOLUNTEER** (vc). A rule only the receiver can apply arrives one step too late.
- **NAME WHICH _HALF_ YOU ARE ATTRIBUTING** (vc, wholly theirs). An incident and its generalisation are separable and usually have different authors. **Nobody audits an attribution.**
- **A PEER TELLING YOU WHAT ONLY THE SOURCE CAN SETTLE IS TELLING YOU WHAT _THEY READ_** (ic). Fold instructions, quoted rows, relayed rulings alike.
- **A RULING DELIVERED AS A SELECTION AMONG OPTIONS YOU AUTHORED IS ONE WHOSE WORDING IS YOURS AND WILL BE QUOTED AS THEIRS. RECORD THE OPTIONS, NOT THE OUTCOME**, in an inbox where it survives the session.
- **A CORRECT ANSWER ABANDONED ON A PEER'S SAY-SO IS WORSE THAN THE PEER'S WRONG ANSWER, AND IT IS THE HALF YOU CONTROL.** I had the attribution start date right at 2026-08-24. vc said 08-21. **I corrected myself to hv without running the one command that settles it** -- `git merge-base --is-ancestor` -- and vc's commit was on `v2-maintenance` and never on main. **My own board already carried the rule: a peer telling you what only the source can settle is telling you what THEY read.** A wrong answer from a peer is ordinary traffic. **Moving off a correct one without a check is the failure, and no instrument catches it because both answers look like answers.**
- **I REFUSED TWO RELAYED AUTHORISATIONS TODAY AND BOTH REFUSALS WERE UPHELD.** vc relayed hv's ruling that the attribution guard was mine to build; hv had ruled it in **vc's** session answering **vc's** question, and hv has me holding in mine. **vc withdrew it without argument and called it the precise thing they had refused from devbin-cc that morning.** The argument that did the work is worth more than the instance: **the discipline does not get to bind you and not me on the same afternoon.**
- **THE INVERSE ALSO HELD: I RELAYED AN hv RULING TO vc AND FLAGGED IT AS A RELAY, AND TOLD THEM TO CONFIRM IT AT THE SOURCE. THEY DID, AND IT HELD.** Quote the ruling VERBATIM rather than characterising it -- a ruling paraphrased in your words gets quoted back as theirs.
- **THE BLOCKED PARTY TELLS THE BLOCKER, NOT THE SEQUENCER.**
- **A PROOF ONLY ITS AUTHOR CAN REPRODUCE IS NOT YET A PROOF THE ESTATE HOLDS** (ic). Survives the soundness being conceded. **Script the repro.**
- **A PEER'S READY-MADE COMMAND IS NOT AN APPROVAL, AND CONVENIENCE IS WHAT MAKES IT SLIP.** Distinct from a relayed approval: the peer claims no authority and is simply being helpful, so there is nothing to challenge and nothing that reads as a demand. **devbin/vc's own words on it: _that is the failure mode of convenience in this setting._** The test is not whether the change is sound -- theirs was -- **it is who gets to say yes.**

### FAMILY 4 -- THE SHARED CHECKOUT

- **`git add` THEN `git commit --only` IS NOT ATOMIC ACROSS THE INDEX LOCK, AND THE SECOND ERROR MASKS THE FIRST.** Landing this very fold, five nodes folded at once on hv's instruction and ic's `git commit` held `.git/index.lock` through its whole pre-commit gate. **My `add` failed on the lock; `--only` then refused the still-untracked archive and said `pathspec did not match any file(s) known to git`** -- which points at MY PATH and says nothing about a lock. **Read alone, the visible error accuses the wrong thing, and the wrong thing is plausible.** It is Family 1's head arriving through git: the LAST error read as THE cause. **Nothing landed, which is the `--only` refusal working.** The lock was LIVE, not stale (`ps` showed the peer's commit at 6 seconds) -- **never delete a lock you have not proved is stale**; wait and retry, and gate the commit on the `add`'s own rc rather than letting the two run blind.

- **`MM` IS A CLAIM ABOUT THE INDEX'S CACHED STAT, NOT ABOUT CONTENT, AND UNDER LOCK CONTENTION IT SAYS `staged` ABOUT A FILE WITH NOTHING STAGED.** My board commit collided with a peer's git process (`fatal: Unable to create '.git/index.lock'`) and the commit's own post-commit cleanup never ran. `git status` then showed `MM` on my `wip.md` -- **staged AND worktree-modified, which is the exact shape of the hazard that swept vc's file into `fcf59ec9`** -- so I went at it as an emergency. **All three diffs were empty**; `git update-index --refresh` cleared it. **DIFF BEFORE YOU REMEDIATE.** The remediation for a real `MM` would have been harmless here, but acting on a porcelain code without reading the CONTENT is how I caused an estate-wide block on Sunday.

- **`git commit --only <paths>` IS NOT A NICER SPELLING OF CHECKING THE INDEX FIRST -- IT IS THE ONLY FORM THAT IS ATOMIC WITH RESPECT TO PEERS.** Reading `git diff --cached --name-only` **measures a MOMENT, and the commit happens at a different one.** I read exactly my three files, committed, and took a fourth: vc had staged an archive in the seconds between. **Neither half does it alone -- their add lands harmlessly under `--only`, my bare commit takes nothing if they do not add** -- so it is not carelessness, it is two correct-looking local decisions composing into a wrong global one with neither party holding the information to see it. **The pathspec is the control; the reading never was.**
- **THERE IS NO SUCH THING AS A WORK-IN-PROGRESS EDIT TO A GATED GUARD IN A SHARED CHECKOUT.** The pre-commit gate runs the **WORKTREE** copy, so every intermediate save is instantly live for four other nodes. **Three estate-wide blocks on 2026-08-25, all mine, all from this: a dropped exec bit, a roster row live while its file was untracked, and a lib gone plural while its consumer still read the singular.** `bash -n` passing says nothing about whether the estate can commit. **Edit a copy, verify, move it into place** -- the same shape already queued twice today for the build path, which I did not connect to my own editing until ic handed me the third instance.

- **`awk ... > tmp && mv tmp file` DROPS THE MODE, SILENTLY, AND EVERY CHECK I OWN IS BLIND TO IT.** I used it three times, chmod'd two by reflex, and left `bin/.devbin/cmd/precommit` at 644 -- **every `git commit` in the repo failed until ic drove it to the cause.** `bash -n` passes, the content diff is clean, and invoking a tool as `bash tool.sh` runs a 644 file perfectly. **A second instance in `runner_roster_check.sh` was caught only because the roster check tests its own population's modes**, and its finding names exactly why nothing else can: _the runner invokes tools with `bash`, so a 644 instrument runs and the gate stays green_. Instruments 755, sourced libraries 644.
- **THE REMEDY FOR A SHARED-INDEX HAZARD CAN BE THE NEXT OUTAGE, AND BOTH MOVES WERE INDIVIDUALLY RIGHT.** vc flagged five staged files as a loaded gun; I agreed and unstaged; **unstaging left a live roster row naming an untracked file and refused every node's commit.** Neither staging nor unstaging was the fix -- **backing the ESTATE-WIDE CLAIM out of the worktree was.** A pathspec commit builds a temp index of HEAD plus that path, so a peer's staged files are invisible inside it while the peer's WORKTREE runner still declares them. **ic named the shape: the row landed and the instrument did not, the adopter-lands-first rule running backwards.**
- **PROVE AN UNBLOCK WITH A REAL COMMIT, NEVER BY ASSERTING IT.** I asserted twice and was wrong twice in twenty minutes. `4b500e62` was a real pathspec commit through the full gate, and **the empty index is better than the disarmed one**: `git diff --cached --name-only` returning nothing means a bare `git commit` from any node takes nothing at all.

- **FOUR WRITERS, ONE TREE, ONE INDEX.** Peers' dirty files are in every `git status` you read. **Name yours; `--only` is PATH-scoped, never hunk-scoped.** Peers commit concurrently -- **wait a lock out, never clear it.**
- **AN UNCOMMITTED EDIT WAS ERASED HERE WITH NO REFLOG TRACE AND hv RULED IT ACCEPTED RATHER THAN INVESTIGATED.** Live and undiagnosed BY DECISION. **A protective copy OUTSIDE the tree costs nothing; staging is NOT the remedy, because the index is shared.**
- **A FIGURE ABOUT HEAD BELONGS IN THE SINGLE-WRITER CLONE** (`int suite`, `int hosting`). A figure about the WORKING TREE must be defended by attribution instead.
- **`bin/` IS dc's LANE** (hv). `bin/.devbin/cmd/**` is Intent's own; **`bin/devbin` and `bin/.devbin/lib/**` are VENDORED and not this repo's to edit.**
- **A MONIKER NAMES WHERE A SESSION LIVES, NEVER WHERE ITS BYTES LAND.** `devbin/vc` works in `~/Devel/prj/Devbin` and its fleet sweep writes into **eleven checkouts including this one**. Five paths here went dirty mid-commit with no announcement, and **nothing on the wire marks the crossing.** They vendor-and-HOLD where a tree is dirty at pre-flight, so held bytes sit uncommitted and unattributed until someone asks. **ASK, DO NOT INFER: the two answers are `a peer is working` and `something writes to this tree unattributed`, and only one is survivable.**
- **`--only` IS WHAT MADE THAT A QUESTION RATHER THAN AN INCIDENT** (devbin/vc's words, and they are right). A bare `git add -A` sweeps a fleet vendor into your commit **silently**, and neither party learns until the log reads strangely.
- **THE FORMATTER IS A SECOND WRITER BETWEEN THE SYNC AND THE COMMIT, AND IT FIRED AGAIN TONIGHT.** It realigned a table I had just synced, so canon named bytes the file no longer held and the gate refused at `ADDS 1`. **Order: write, LET THE FORMATTER SETTLE, sync, commit.** Second time in this file's history, and this time inside the document about instruments misreporting.
- **THE SUBJECT CAN MOVE BETWEEN YOUR TWO READS, AND THE SECOND READ LOOKS LIKE AN ANSWER.** `git diff --numstat` reported changes to an issue's canon; `git diff` seconds later printed nothing. **Not a broken instrument -- a peer committed in between.** vc hit the identical trap twenty minutes earlier (`d07c94fd`). **In a four-writer tree a two-command measurement has no single subject unless you pin the revision.**
- **COMMITTED IS SELF-DESCRIBING; HELD IS NOT** (mine, adopted into devbin's sweep procedure at their `c6c30f9`). A committed change carries an author, a message and a log entry. **Bytes vendored into your tree and deliberately left uncommitted carry NOTHING**, which is exactly the state that reads as an intruder. **A fleet operation must name its HELD estates, not merely pre-announce itself.**
- **A FILTER OVER A COMMAND'S OUTPUT MUST BE ABLE TO EXPRESS ITS FAILURE, NOT ONLY ITS SUCCESS.** I piped `git commit` through `grep -E 'files changed|^\[main|error|refus'` and it printed NOTHING: a peer took the index lock, git said `fatal: Unable to create '.git/index.lock': File exists`, **and that message matches none of those patterns.** **A FAILED COMMIT PRESENTED AS SILENCE**, and only an independent `git log -1` caught it. **Same family as the whole evening: the instrument could report the outcome I expected and not the one I needed.** Read the rc, or do not filter.

- **A SCRATCH DIRECTORY INSIDE A GUARDED TREE IS INDISTINGUISHABLE FROM IN-FLIGHT SOURCE TO EVERY GUARD WE OWN.** cc's `native/rust/.cc-probe/` blocked my build and would have blocked any node's: **`verify_pair`'s arm 2 refuses on `??`, not only on modifications**, so ninety seconds of untracked scratch in a shared platform path is an estate-wide stop. The probe itself was the right instrument -- identical sha256, one path refuses and one resolves -- in the wrong place. Put scratch under `target/`, and **verify the ignore rather than assuming it** (`git check-ignore -v`), which is what cc did.
- **THE ISOLATION YOU REACH FOR TO AVOID DISTURBING A PEER'S BUILD IS EXACTLY WHAT INVALIDATES YOUR RESULT -- TWO NODES, ONE DAY, ONE INSTRUMENT DEFECT.** ic manufactured 13 false failures in the morning and cc 2 in the afternoon, both from a `CARGO_TARGET_DIR` in scratch. **`install::resolve()` walks `current_exe()` ancestors for the `lib/templates` marker with NO ENVIRONMENT FALLBACK**, so a binary built outside its install tree cannot locate itself BY CONSTRUCTION and every test that shells out to it reds for a reason unrelated to the change under test. **The safe-looking choice is the wrong one, which is why both of them took it.** An explanation of a red is not a green: re-run from a resolvable path before reporting totals.

## Decisions

- (2026-08-27) **hv AUTHORISED BOTH DISK SWEEPS DIRECTLY, AND THE SECOND ONE REVERSED A RULE I HAD WRITTEN MYSELF.** I put _never touch `release/`_ into the estate proposal; measurement then showed 62% of Intent's release rlib bytes redundant, ~446 MB per project left on the floor while feeling careful. **The rule that survives is _never break what `PATH` resolves through_ -- a different proposition, and the only true one.**

- (2026-08-27) **AC-12.1's CLASS (2) WIDENED IN THE STORE RATHER THAN ARGUED ON A BOARD, ON vc's RULING: _record it against the criterion now, build nothing._** The criterion as written could be discharged green while its subject stayed broken, **and a criterion that can be satisfied while its subject is broken is worse than a missing one because it is believed.** The finding now lives in canon (7,280 bytes, `kind: test`, `state: computed`), **so this board carries a pointer and not a copy.**

- (2026-08-27) **hv CHOSE TEST-TARGET CONSOLIDATION FROM A MENU I AUTHORED, ON A DIAGNOSIS I LATER CORRECTED.** I had the 167 test targets as the CAUSE of the blowup; the cause is **builds retained x size per build**, and consolidation addresses one term of the second factor. Then I over-corrected to _test targets are not the driver_, which lamplight-vc caught: **both terms multiply.** The ruling stands and its stated basis did not survive the day, **which is exactly the shape Family 3 warns about -- so the options are recorded here, not only the outcome.**

- (2026-08-26) **VERIFY RESOLUTION, NEVER ENUMERATE POSITIONS.** `use` repoints what it owns and then measures what actually answers `intent`, dying if it is not the flavour asked for. **That catches PATH 19, and any position nobody has found yet, WITHOUT NEEDING TO KNOW IT EXISTS.** An enumeration goes stale the day someone adds a binding; a measurement of resolution cannot. This is the general form of the thing `cmd/local`'s own header had as an episode.
- (2026-08-26) **`use` LIVES IN `cmd/local`, ON HIGHLANDER GROUNDS, AND I TOOK IT BACK FROM cc TO PUT IT THERE.** hv assigned it to both of us in separate sessions. `path_hits`, `verify_pair`, `describe_binding`, `BINDIR` and `PAIR` all already live in `cmd/local`; a separate `cmd/use` duplicates five things or forces an extraction at speed on release day. **cc had independently reached the same design and deferred to `local status` for the reporting half -- the right call, and they handed the rest over without argument.** Their `verify_hook_door` is in the commit and attributed. **Neither of us was reaching; a double-assignment is not a land grab, and saying so is what kept it cheap.**
- (2026-08-26) **CHECKING WHICH BINARY RESOLVES AND CHECKING WHETHER THE DOOR OPENS ARE DIFFERENT PROPOSITIONS** (cc, and it is the thing my design was missing). They come apart on the PROD arm: the door walks `current_exe()` ancestors for `lib/templates/`, **a brew install is a COPY**, and a copy with no marker above it refuses every hook at exit 1 -- **which Claude Code does not block on, so the strict gate stops enforcing silently in every project at once.** `verify_pair` and a resolution check BOTH PASS while that is true. Driven with stdin closed: `require-in-session` reads its payload from stdin and **blocks forever on an inherited one.**
- (2026-08-26) **`publish` STOPS WITH hv.** hv authorised vc to direct my work on other projects, and I take that as covering fleet work and NOT a public push to a tap. **Stated by me rather than waited for.**

- (2026-08-25) **D42 AMENDED BY ME, AGAINST MYSELF: THE RULE IS THAT NO CALLER AUTHORS A STAMP.** The signature test -- _no function TAKES a time_ -- is one sufficient condition and was never the definition. `Facade::set(&addr, "created", <a time>)` satisfies the letter and produces the forbidden behaviour, because a generic setter routes around a proxy without touching the property. **A rule stated as a MECHANISM gets evaded by a different mechanism, and its author is the last to notice.** vc asked; AC-14.11 already said it project-wide. Closing condition 2 of 3 on AC-08.5.
- (2026-08-25) **AT-11.6's TWO REAL DEFECTS WERE BOTH FOUND BY LIVE INCIDENTS, NOT BY REVIEW OR MUTATION.** It shipped with nine arms and four mutations. **Mutation testing varies the SUBJECT and holds the ENVIRONMENT fixed**, so the git-env leak was unreachable by that method; and the scope gap was an INPUT outside the predicate's population. **Neither is a weak test suite -- both are the harness being narrower than the thing it certifies.**
- (2026-08-25) **`--only` IS THE ONLY COMMIT FORM ATOMIC WITH RESPECT TO PEERS.** Reading `git diff --cached` first measures a MOMENT; the commit happens at another. I read exactly three files and committed four. **The pathspec is the control; the reading never was.**
- (2026-08-25) **vc HAS hv's PEN AND hv SAID SO IN MY OWN SESSION**, which is what makes it different from the two relayed authorisations I refused yesterday. **Scoped, and vc has honoured the scope unprompted: A2 the attribution guard STAYS HELD, because hv delegating COORDINATION does not make vc a source of hv's AUTHORISATIONS, and a delegation that laundered relays would be indistinguishable from one that did not.** vc refused to relay it and told me not to accept it from them if they did.
- (2026-08-25) **vc's OPENING ASSIGNMENT WAS WRONG AND THE CONTRACT CAUGHT IT.** `claude ws` was routed to me as WP-07 work off ic's surface probe; **it is WP-14's (`AC-14.10`), and `AC-14.7` says every `/in-whiteboard` verb is served by `intent wb` FROM THE STORE.** Porting v2's file-based `intent_claude_cwi` would have built the thing WP-14 exists to replace, in the subsystem the four of us coordinate through. vc withdrew it in full. **The finding underneath is bigger than the assignment: `AC-12.1` prunes `bin/` at the cut, so if WP-14 is Not Started then, the whiteboard provisioner does not degrade -- IT DISAPPEARS. WP-14 blocks WP-12 and that dependency is stated nowhere.** With hv as a sequencing question.
- (2026-08-25) **AC-07.2 INVESTIGATED AND DELIBERATELY NOT STARTED.** Writing `hook_compat.rs` dirties `native/rust`, which is the exact subtree four nodes are waiting to see clean and the only route back to a current shared binary. **My own guard's predicate, applied to my own next task. The first real test of a rule is whether its author follows it when it costs them the appearance of progress** -- `ST0056/11` stays at 3/7 and a true 3/7 beats a busy 4/7 built on a dirty tree. **DISCHARGED THE SAME DAY: the gate closed, the premise died, and AT-07.2 is green at `77878284`. KEPT RATHER THAN DELETED because the entry's subject expired and its lesson did not** -- the first real test of a rule is whether its author follows it when it costs them the appearance of progress, and that is still true of the next rule I write.
- (2026-08-25) **A TEST'S POPULATION IS NEVER READ FROM THE THING UNDER TEST.** `hook_compat.rs` assembles its hook roster from the shipped scripts and `settings.json` and never from `install::HOOKS`. Reading the constant would have been asking the suspect to describe itself, and it would have agreed with the defect in perfect silence. **The general rule: when the SUSPECT is a roster, the population comes from somewhere the suspect does not write.**
- (2026-08-25) **I DID NOT WIDEN A PEER'S GUARD TO CLOSE MY OWN FINDING.** `every_declared_hook_ships_as_a_script` is one-sided and lives in cc's module. My file covers that direction from the SURFACE, so the class closes either way. **I corrected only the two prose claims MY change would otherwise have made false** -- a baked-in _three names_, and _nothing but this test compares them_. Widening someone else's guard quietly is the defect class rather than the fix; falsifying their doc and leaving it is just as bad.
- (2026-08-25) **THE PRECONDITION I WROTE AGAINST MYSELF ON 2026-08-25 EXPIRED AND I CHECKED RATHER THAN INHERITED IT.** _Do not start AC-07.2 while `native/rust` must go clean_ was correct while four nodes were waiting on a clean subtree to close the gate. The gate closed; the premise died; the entry would have gone on issuing orders. **A decision that names its condition can be retired when the condition dies -- one that does not, cannot.**
- (2026-08-25) **A `dirty-` BINARY MAY READ CANON AND MAY NEVER WRITE IT (vc's rule, and it binds me).** Reading is safe because **the STORE is the subject and is independent of the build**; writing or certifying a criterion is not, because the write is performed by the code that is mid-edit. **cc's in-flight work includes `render.rs`, which IS the canon renderer** -- so a write through a binary built over it would be done BY the half-finished renderer and would look exactly like a clean write. This is the sharper form of my own reason for not building, and it is why `AT-07.2` waits rather than being forced.
- (2026-08-25) **A CLOSED LIST IS SAFE WHEN IT DECLARES WHY THE THINGS **NOT** IN IT ARE NOT IN IT.** This is the sweep's durable output and it makes the class checkable by READING. `install::HOOKS` declared why it was **CLOSED** -- a name reaching the filesystem can contain `../`, which is sound -- and **never what it EXCLUDED**, so nothing on the page separated _post-tool-advisory is out on purpose_ from _session-finish is out by accident_. **That single omission is why vc resolved one in a single read and the other survived three instruments.** Every other closed list in v3 declares membership, and `RECORD_TIMESTAMPS` goes further: **its test DISCOVERS the columns from the DDL rather than listing them, so a table added tomorrow is covered without anyone remembering.** **THE ESTATE ALREADY HELD THE EXACT REMEDY `HOOKS` LACKED, one file over, and nothing carried it across** -- which is the real lesson, because the pattern being present somewhere is what makes its absence elsewhere invisible.
- (2026-08-25) **THE INVERSE OF THE `session-finish` CLASS, AND IT IS WORTH NAMING BECAUSE I WENT LOOKING FOR THE WRONG ONE.** session-finish was _canon says the name exists and the door refuses it_. The advisory hook is the mirror: **the door works perfectly and the CALLER'S guard clause can never fire.** Both leave a consumer with a hook that does the wrong thing, and **no roster check of any kind can see the second one** -- it needs someone to run the command and read which STREAM the bytes came out of. **A parity suite proves two binaries agree; it cannot prove either one is useful to its caller.**

- (2026-08-25) **hv RULED, UNPROMPTED AND VERBATIM: the `(C)` line is NOT required and never was.** _"This isn't a problem, has never been a problem, and is not something that I suggested we go looking for. The only constraint is that I DO NOT WANT ANY CLAUDE EXHAUST IN MY COMMITS. EVER. The end."_ **Killed a three-node sweep in one line:** vc's 10 missing-`(C)` commits, cc's 3, hv's own 2 -- **not findings**, retracted rather than carried as closed. cc's structural claim that the two lines are one block keeps its refutation (`(C)`-absent-and-trailer-present is a population of ZERO) but **loses its consequence**, and my 12-of-12 counter-datapoint went with it. **The proposed guard is therefore ONE-DIRECTIONAL: absent-trailer only.**
- (2026-08-25) **hv's SCOPE FINDING, AND IT IS ABOUT US RATHER THAN ABOUT THE COMMITS:** _not something that I suggested we go looking for._ I raised the Claude exhaust; the `(C)` sweep grew out of it across three nodes with measurements, mutual corrections, a withdrawn hypothesis and a remedy recommendation, **and nobody asked for any of it.** Each of us measured carefully. **CAREFUL MEASUREMENT OF AN UNASKED QUESTION STILL COSTS A DAY.**
- (2026-08-25) **THE EXHAUST ITSELF IS A TIGHT DISJUNCTION AND ONLY hv CAN CLOSE IT** (mine, adopted by vc to hv in my words). `includeCoAuthoredBy: false` is set, `attribution.commit` is byte-identical to the injected instruction's first line -- **but NO settings key anywhere holds the `Claude-Session` string**, global or project. **So EITHER that key gates the generated line despite its name and manual omission expires, OR nothing in configuration gates it, the omission is PERMANENT, and every fleet project inherits it.** No third branch survives; I exhausted the settings locations. **The deciding check: one commit from a session STARTED AFTER the change, then `grep -c '^Claude-Session:'`.** Neither vc nor I can run it from a session predating it. **90 carriers, 0 `Co-Authored-By`, first on main `55fc4a50` 2026-08-24.**
- (2026-08-25) **vc AS CONTRACT STEWARD AMENDED AC-11.6 UPWARD ON MY ROUTING, AND MY PROPOSAL CARRIED AS WRITTEN** (`f68d397c`). **Their green arm was the cause of the row being unbuildable and they said so.** Branch not taken: leaving the row and building a discriminator -- refused on two independent grounds, either sufficient. **Protocol 3.0 invariant 3 forbids the ownership oracle** (_claims by ST ID, never glob paths_), **and cc's finding kills it anyway** (a shared-tree build carries the UNION whoever invokes it, so authorship is not recoverable from the artefact).
- (2026-08-25) **THE ROUTE FAILURE ON AT-11.6 IS MINE AND IT IS THE BETTER EXHIBIT.** I wrote _routes to vc_ in the same paragraph as the conflict, **on a board that archives at the fold, and then did not route it.** Four days invisible. vc said they had been sitting on it; **they had not, it was never sent** -- zero hits for `11.6` in their inbox from me. **THE WRITE SUCCEEDED AND THE DELIVERY NEVER HAPPENED**, which is sharper than any of the four filed 2026-08-24.
- (2026-08-25) **hv RULED: fix both trees LOCALLY, commit, and DO NOT PUSH v2.** _"The checked out v2 branch is only being used locally here by projects on this machine ... as long as [they] can work on a stable v2 branch and not have their project management functions screwed up while we build v3, I'm good."_ **The public drift is knowingly worn.**
- (2026-08-25) **hv CHOSE THE ROUTE-LEVEL DECLARATION over a per-file exception list.** Branch not taken: an entry per fix, which the guard's own text forbids and which would be FALSE of every file it named -- not PENDING (the v2 landing is done) and not V3-ONLY (v2 received it).
- (2026-08-25) **THE ENFORCEMENT LOSS IS A COST OF hv's NO-PUSH RULING, NOT OF THE ENGINEERING, AND NOBODY PRICED IT WHEN THE DECISION WAS MADE** (vc's framing). Routed to hv rather than absorbed in a test file. **Release is an automatic caller with ground truth, but it is the LAST place you want to first learn a fix reached one tree.**
- (2026-08-25) **A PEER'S FULLER QUOTE OF A RULING IS STILL THE PEER.** vc supplied the clause they had dropped -- the one authorising the commit -- and told me not to treat their reading as my authorisation. **I held for hv's word in my own session and vc was right that I should.**

- (2026-08-24) **THE BUILDER CARRIES THE ROW, IN BOTH DIRECTIONS.** vc built four things in my lane and carries all four (vc, applying my own rule to me); cc then offered me their v2-estate arm to PLACE because it landed in my lane, reading my wall warning as territorial when it was PROCEDURAL. **A row does not transfer because of where it lands.**
- (2026-08-24) **A PEER RELAYING AN APPROVAL IS NOT THE APPROVAL.** cc reported matts had ruled 0070 mine to close; I declined the relay and **cc withdrew the framing themselves** -- it answered a question asked in THEIR session.
- (2026-08-24) **hv RULED: `intent3` MAY REFUSE.** Branch not taken: _keep it a reporter, warn but exec_. **vc preserved the menu, which is the remedy for the `sync`-skip provenance gap.**
- (2026-08-24) **hv RULED: the local `v2-maintenance` branch DELETED.** A silent wrong answer became a loud absent one. **Fast-forwarding LOOKS equivalent and re-arms the moment upstream moves.**
- (2026-08-24) **hv's FREEZE SCOPE: frozen for features, live for shipped-surface defects.** `0070` was v3-only; `0071` is shipped-surface and lands in both.
- (2026-08-24) **TWO MECHANISMS ENFORCING DIFFERENT PROPERTIES ARE NOT TWO COPIES OF ONE.** `currency.lib` is EXEC-time; `cmd/local`'s `verify_pair` is BUILD-time and demands `sc == HEAD` exactly. **The convergence is RECORDED AND DELIBERATELY NOT DONE.**
- (2026-08-22) **A CLAIM OF UNIQUENESS IS A MEASUREMENT AND MUST BE GREPPED, NOT ASSERTED.**
- (2026-08-22) **SOUND-BUT-UNNECESSARY AND UNSOUND ARE DIFFERENT VERDICTS, AND ONLY ONE IS FAIR TO THE PROPOSER.**
- (2026-08-21) **A ROSTER ROW AND ITS RUNNER MUST BE ONE COMMIT.** Either disagrees alone.
- (2026-08-20) **A REPORTER FAILS OPEN; AN ACTOR REFUSES.** `intent3` is an ACTOR, on PATH.
