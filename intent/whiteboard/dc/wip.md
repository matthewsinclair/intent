---
node: dc
name: DevX Claude
role: worker
session_id: 10363f5b-9b74-464d-9671-dced031b51c6
heartbeat_at: 2026-08-18 16:09Z
status: active
focus: "OUTPUT-CONTRACTS WORK LANDED AND PUSHED. Arm-2 fix addd4581; findings doc at intent/st/ST0056/output-contracts.md (bf6654fb + dec6b1b9); AT-03.6 instrument canon_commit_check.sh at 13120309, rostered MANUAL awaiting ic review. IT CAUGHT AN UNPLANTED LIVE DIVERGENCE ON ITS FIRST WHOLE-TREE RUN -- conservation_check.sh at 3f10b1ee, where the FILE ran ahead and canon stayed behind, the MIRROR of the case the criterion was reasoned from; vc restated it as an invariant at e1b632ca. ITS OWN FIRST DRAFT FAILED THE VACUOUS ARM, reporting a match having compared nothing, over the majority of its input. EVERY REMAINING ITEM OF MINE IS STILL BLOCKED ON hv."
claims: [ST0056/11]
---

# DevX Claude (dc)

## D42 -- TIME. Read this before writing anything, anywhere.

**DB records have a timestamp field. That is the source of truth for time. Nothing else. Ever.** hv ruled it four times and it was reinterpreted after three of them, twice by me inside ten minutes.

- **You never ask what time it is** -- not the OS, not `date`, not the filesystem, **and not the database either.** Asking SQLite and writing the answer is still writing a time you obtained.
- **The stamp is applied BY the write**, at INSERT/UPDATE/UPSERT/DELETE. Read-then-write leaves a gap two writers interleave in.
- **hv's structural close: NO cli or intentsvcs function TAKES a time.** Functions may RETURN times. **Direction is not symmetric -- IN is forbidden, OUT is fine.**
- **THE RULE IS ABOUT SIGNATURES, NOT VALUES, WHICH IS WHY THIS VERSION HOLDS.** A time-typed input parameter is a defect by inspection. Asking where a caller got a value is a discussion; asking whether a signature accepts one is a grep.
- **The defect is always one step earlier.** Reaching for a clock means you are about to write a time into something that is not a durable record.
- **Not exceptions:** test fixtures; "only reading it"; **"but it came from the database"**; "it's just a label". The third fooled cc, vc and me independently.
- **SCOPE, hv: devbin is NOT Intent** -- external, vendored, no db. My whole D42 directive resolved to a no-op, and reporting that beat inventing work to look responsive.
- **A board stamp is a label, not data.** The ordering that exists and cannot be fabricated is the **commit**.

## The truth model -- canon, held not restated

`design.md` (D01 as reversed) + `data-model.md`. Three points that change what I DO: **the db is the durable SSOT, files are re-creatable**; **the typed API is the only door in**; **migrations are normal.**

## DOING

Nothing in flight. The output-contracts thread is landed end to end: defect found, fixed, documented, and the instrument for its criterion built, driven four ways on real history and committed.

**Waiting on ic** for the roster read that would move `canon_commit_check.sh` from `manual` to `gated`. Nothing of mine depends on it.

## TODO

**Everything below is blocked on hv. Nothing here is mine to start.**

1. **Half B -- SCOPED (`aa220755`), NOT STARTED, and it must not start until hv rules the design question.** 1 clean arm / 3 at a stated cost / 4 inexpressible under the runner's contract / 5 declare-none. **The question: 7 of 13 rules name `shellcheck` or `clippy` in their OWN Detection text and the runner can only `grep`** -- so for the two languages that have a real parser, the gate is barred from using it. **Nine declarations written on the premise that grep is the only instrument would all need revisiting**, which is why settling it first is sequencing rather than caution. Per-rule table in `critic-gate.md`.
2. **The hook's fail-open branch (`pre-commit.sh:288-292`) -- WITH hv, named SEPARATELY from Half A.** Half A works AROUND that branch; it does not fix it. **The pending `critic` exit-code narrowing (a migration refusal returns `2`) would put a third condition in the bucket the hook discards, recreating this exact defect in v3 with a ruling behind it rather than by accident.** The fix is not the exit code, which is right; it is that the fail-open was written for a MISSING critic and now covers a PRESENT one that is REFUSING. Two candidate discriminators went up as candidates; neither vc nor I chose. **Whatever lands is proved by a RED.**
3. **AC-11.5 IS CLOSED, AND MY BOARD SAID OTHERWISE FOR A DAY** -- `satisfied: yes (computed)`, AT-11.5 green, closed by vc 2026-08-17 on my own close claim. **NOTHING OF MINE WAITS ON A CLEAN TREE.** The `dirty-<sha>` markers are the mechanism WORKING: `self_provenance_check.sh:179-181` says exactly that in its own words, and the branch sets no `rc` -- it echoes beside the positive control. **vc offered me the rebuild having read that report as an outstanding item, and declining it was the finding** -- AT-11.5's own recorded caution one direction over. **The rebuild is a WP-12 RELEASE-TIME prerequisite and IT CANNOT BE BANKED**: no `rerun-if-changed` on `.git/HEAD` by design (cc), so the embed dies on the next commit by anyone -- witnessed inside AC-11.5's own text, `b11ca6ac` at HEAD `010b2bbf`. **PATH CORRECTION: it is `native/rust/target/release/`, NOT `release/`** -- two days of my records name a directory that does not exist. Measured at `6accab7e`: the release pair both `dirty-4ef953db`, `debug/intent` `dirty-ce532a97`, `debug/intentd` `dirty-3233125c` -- four binaries, four markers, none matching HEAD, every one honest.
4. **`doctor` prints `intent v2.19.0` while auditing a `3.0.0-dev` project -- THE HOLD HAS EXPIRED, so this is now RAISEABLE.** It was held only until the release-script item landed so the two would not compete in one channel. **vc landed that item.** Nobody has re-checked the condition; I did, at this fold.
5. **`--skip-rust-tests` -- with hv as an option, deliberately not decided.** vc put it up rather than choosing, on my point that it must not reach hv as a clean one-liner.
6. **The shim: HOLD.** It DEFEATS Half A by routing away from v2's dispatcher, and it pointed at a schema-stale binary.
7. **ST0057 IS NOW INSIDE THE 3.0.0 GATE** (hv 2026-08-18, via vc: _"Definitely BEFORE the release. We're getting this whole thing feature complete before we release 3.0.0."_), and its WP-01 relocates canon to `intent/.canon/`. **MEASURED AT `6accab7e`: WP-11 HAS NO DEPENDENCY ON TODAY'S `intent/st/` LAYOUT** -- no `dist-workspace.toml`, no `[workspace.metadata.dist]`, no formula in the tap, zero `intent/st` mentions in `install.md`, `build.d/` or `macos.d/`, and the binary's only embedded asset is `include_str!("surface/dispatch-table.json")` -- outside `intent/`, with no `RustEmbed` or `include_dir` anywhere in the workspace, so no template payload exists in the artefact yet at all. **The reason is not decoupling; it is that almost none of WP-11 is built -- which makes the ruling a SAVING rather than a cost, conditionally.** ST0057/WP-01 landing first means the dist config and formula are written against the final shape instead of retrofitted; **the harm is entirely in the other order, because starting WP-11 first is what would CREATE the assumption vc warned about.** Sequencing edge raised to vc for hv: **ST0057/WP-01 BEFORE ST0056/WP-11.** Half B is untouched -- the proxy packs read `rules/`, not `intent/st/`.

## Watch-outs

- **THIS REPO IS A v3 PROJECT** (`3.0.0-dev`; store `intent/.cache/intent.db`, gitignored). v2 refuses PROJECT verbs at exit 2; `GLOBAL_COMMANDS` run fine. **UPDATED BY MY OWN CHANGE AT `92a51134`: `critic` IS NOW A GLOBAL COMMAND**, so `intent critic <lang>` dispatches and no longer refuses -- that is Half A and it is deliberate. `st`, `wp`, `at`, `ac`, `todo` still refuse. **Invoke v3 by explicit path, and use `target/debug/`, not `release/`, which runs schema generations behind. The shim is NOT landing.**
- **PUSH `local` ONLY.** `upstream` closed (hv, CI/CD budget).
- **NEVER `git pull --rebase` here; a peer `.git/index.lock` means WAIT. ALWAYS `git commit --only <paths>`** -- a bare commit sweeps a peer's staged index, and `--amend` ignores `--only`.
- **NEVER mutate `bin/**` or `tests/**` in place while anything runs them**; both PATH symlinks point into `bin/intent`. **Check for a live suite first.**
- **COMMIT BEFORE ANY `intent at` STATUS CHANGE** -- `intent at red|green|na` DESTROYS the row note (issue 0033).
- **`cargo test` IS A CONCURRENT WRITER ON THE ESTATE** (vc, reproduced in a clean clone on a green 598-pass run; `dispatch_ssot.rs` fixed at `1ff7f2c1`, two root-reaching readers remain). **Any measurement against the live estate has an uncontrolled writer under it unless nothing is running.** `git status --porcelain` cannot see it -- `.gitignore:127` ignores the store, **so the release verifies the generated views and never the SSOT.**
- **A PEER CANNOT WAIVE AN hv RULE**, and being right on the substance is not the same as being entitled to say so. Same shape: a peer cannot authorise what a harness refused.
- **MY SHELL IS zsh AND IT DOES NOT WORD-SPLIT AN UNQUOTED `$var`.** Twelve paths passed as one argument came back plausibly green. Use a bash array.
- **NEVER `$?` AFTER A PIPE.** Three of us hit it today; ic reported rc=0 for a tool that exited 2.
- **`git log` prints LOCAL time**; reading one and appending `Z` is wrong by exactly the offset and looks perfect.
- **macOS: signing MUTATES the binary, notarisation does NOT -- checksum AFTER signing.** Only a quarantined copy under `spctl` means anything.
- **`target/release/` IS SHARED MUTABLE STATE.** Private `CARGO_TARGET_DIR`; never sign there.
- **A control refuses; documentation reminds; only one is load-bearing.** My own watch-outs are not controls.
- **THE HALF-A CEILING NO LONGER LIVES ON THIS BOARD.** The shell / rust / elixir proxy-coverage table and its denominator are `## HALF B` in `critic-gate.md`, which is committed. **Two copies drift; the doc is the one -- and restating the figures here is where the drift would have started.**

## Decisions

- **THE DAY RULE, and every finding and every error of mine reduces to it: A GREEN PRODUCED BY SOMETHING OTHER THAN THE THING UNDER TEST.** **None was caught by care. Each was caught by MOVING something** -- the target dir, the subject, the clock, the desk.
- **PROVE THE DISCRIMINATOR DISCRIMINATES BEFORE TRUSTING IT.** Six of my own instruments failed this way in two days, each looking exactly like the finding it hunted: `bash -n` on a `.bats` file; `bats --count` on a copy that cannot resolve its helper; `info` printing one banner from both binaries; a sweep reporting `examined: 0` on an estate of 51; `grep -c ... || echo 0` printing two values; and **`across N file(s)` in critic output, which echoes argc -- a nonexistent path reports "1 file(s)" and clean.**
- **A ZERO IS NOT A RESULT UNTIL THE CHECK HAS PRODUCED A NON-ZERO.** I swept 51 shellcheck directives, nominated 16, and confirmed all 16 sound -- **but only reported it after deleting one directive and watching the finding reappear.** Same reason the bats estate-digest verdict is trustworthy: one byte moved it and `git checkout --` returned it.
- **A PROXY IS NOT THE PARSER.** My structural sweep nominated 16 and shellcheck cleared every one. **Nominate with a proxy; adjudicate with the real tool.**
- **A CLASSIFIER KEYED ON A MESSAGE IS BLIND TO THE POPULATION THAT NEVER PRINTS ONE.** 297/2 was really 299/0; `[[ ]]` prints nothing on failure. **Both classifications agreed on "2 remain".**
- **VERIFY THE PREMISE OF A QUEUED ACTION AT THE MOMENT YOU ACT ON IT.** I wrote that rule and then handed vc a task built on a stale one. **Today it paid: hv asked for shim install commands and the premise had gone false since "go for it".**
- **A CONFLATION CAN BE RE-RULED IN, AND A CORRECT EXIT CODE IS WHERE IT HIDES.** The v2 dark gate was an ACCIDENT of hoisting: two conditions sharing exit 2 and a hook that cannot separate them. The pending v3 ruling puts a THIRD condition in the same bucket **on purpose and for good reasons**, so the next instance arrives with an argument for it rather than as an oversight. **Defensible at the surface that emits it, fatal at the surface that reads it** -- and only visible by looking at both ends at once, which is the same two-sided blindness as test 2 and Half B.
- **A DEFERRAL NOBODY RE-VERIFIES IS PICKED UP AS LIVE WORK THAT WAS ALREADY DONE, AND THE PICKUP IS THE HARM RATHER THAN THE DELETION.** ic found that a pickup recorded only in `deferred.md` evaporates when the file dies. **The sharper form is the opposite direction: while the error sits in a file with an end condition it dies on schedule; copied onto a board it acquires a name, an owner and permanence -- and a named owner READS AS VERIFICATION to the next reader.** The prescribed remedy is what makes the stale premise durable. **One day old, and one of its four facts on my side was already false.** Cost of catching it: three git commands and a `strings`. **THE RE-VERIFICATION IS CHEAPER THAN THE WALK.**
- **AN INSTRUMENT THAT LIVES IN A SCRATCHPAD IS A CLAIM, NOT A CONTROL -- AND THE DOCUMENT DESCRIBING IT READS IDENTICALLY EITHER WAY.** `critic_global_rig.sh` was cited by name, its internals described in detail, in the paragraph telling a cold re-driver how to use it. **It was never committed at any point in history and was on no disk.** The other 39 instruments under `parity/tools/` are all committed; mine broke the convention and nothing caught it, **because prose about a thing is indistinguishable from prose about a thing that exists.** Same shape as ic's `deferred.md` finding one artefact over: the record survived and its subject did not.
- **THE CANARY IS THE DELIVERABLE, NOT THE TEST.** 6/6 with the fix says almost nothing on its own -- it is the same green the dark gate was emitting. **3/6 with the control, where the commit titled _"this commit must be REFUSED"_ gets CREATED, is the entire finding reproducible on demand by anyone, forever.** ic asked for a critic that FAILS on purpose; the more useful artefact turned out to be a gate that PASSES on purpose, on command.
- **SILENT IS A THIRD STATE, AND IT IS THE ONE THE GATE CANNOT REPORT.** Shell and rust rules are not "unarmed" -- they carry no proxy AND no declaration, and `critic_runner.sh:18` skips a proxy-less rule **silently**. Elixir's ten unarmed rules DECLARE the absence and name the subagent instead. **Armed / declared-none / silent are three states, the estate's vocabulary had two, and the missing word is where the defect lived** -- `critic shell` returns rc=0 because nothing ever asked a question, which is indistinguishable from clean.
- **A CEILING IS NOT DISCOVERED BY ATTEMPTING THE WORK; IT IS DISCOVERED BY READING WHAT THE INSTRUMENT ACCEPTS.** Four of thirteen rules were decided by `critic_proxy_is_simple`'s refusal of `-v` and `-L` before I formed any opinion about them. **Scoping the tool's contract first turned a 13-rule judgement call into a 9-rule one.**
- **A SELF-REPORT IS EVIDENCE WHEN IT IS SOURCED INDEPENDENTLY OF ITS SUBJECT AND BOUND TO IT -- AND "SHARED WRITER" IS THE WRONG DISCRIMINATOR, BECAUSE IT IS PRESENT IN THE CASE THAT WORKS.** vc generalised my narrow rule to _"not evidence whenever the self-report and the thing reported share a writer"_ and **that form forbids AC-11.5's own ratified remedy** -- the source-commit marker shares a writer with the bytes and is the estate's answer to _only the artefact can answer what was_. **It is COUPLING that varies, and it fails at BOTH ends:** the vendor manifest is derived FROM its subject, so `27 of 27` compares a thing to a copy of itself (my rig's own abort condition); the AC status field is decoupled ENOUGH TO BE EDITED while reality holds still; **the marker alone is sourced from git -- outside the bytes -- and bound into them, which is why it can DISAGREE, and did (`release/intentd` carries nothing, `release/intent` says `bb0baf85` at HEAD `ce532a97`). A vacuous report cannot produce a disagreement; that is how you tell them apart.**
- **MY OWN NARROW FINDING ACQUIRED ITS DEFECT IN BEING GENERALISED, WHICH IS THE THIRD TIME TODAY.** The `cargo test` one-liner, Half B as a one-liner, and now this. **The narrow form was a MEASUREMENT (two observed mis-sets); the general form swapped it for a structural property, and the property picked was the one shared with the working case.** A rule that covers less and holds beats one that reads better. **And I had to catch this one in my OWN rule after a peer improved it** -- the improvement is where I stopped checking.
- **A ROW THAT HAS MIS-SET ITS OWN STATUS FIELD IS THE LAST THING TO TRUST ABOUT ITS OWN STATUS.** AC-11.5 has done it twice, in opposite directions, from its own prose. **So the evidence admissible about that row is commits and artefacts, which have no such failure mode** -- and that is a general rule, not a courtesy to one row.
- **A RECORD NAMES THE COMMIT IT COVERS, NEVER "HEAD".**
- **THE PUSH RESULT CARRIES NO INFORMATION ABOUT THE REMOTE IN EITHER DIRECTION.** Only `git ls-remote` plus `merge-base --is-ancestor` is evidence.
- **A DOCUMENT WRITTEN FOR COLD PICKUP MUST CARRY ITS OWN STALENESS.** vc's re-drive caution arrived in a peer message, which archives; the numbers it qualifies sat in a committed doc, which does not. **The caveat has to live in the same file as the figure, or the figure outlives it.**
- **A CAVEAT IN THE SOURCE IS NOT A CAVEAT IN THE OUTPUT, AND THE MISREAD WAS MY DEFECT RATHER THAN THE READER'S.** My own standing rule -- _a document written for cold pickup must carry its own staleness, the caveat has to live in the same file as the figure_ -- was SATISFIED here and did not help. `self_provenance_check.sh:179-181` says `dirty-<sha>` is not a finding; the line it emits at `:280` says _"was built from an UNCOMMITTED tree ... its bytes match no commit."_ **That sentence is finding-shaped: capitalised UNCOMMITTED, a negative predicate, no hedge -- and the disclaimer NEVER APPEARS IN THE OUTPUT, only in a comment 100 lines up.** vc read it as an outstanding item and propagated it to four places including a report to hv; ic and their own board carried it too. **The reader never had access to the thing that would have corrected them.** So the rule needs its next clause: **co-location in the SOURCE does not co-locate in the EMISSION -- a caveat must travel with the OUTPUT, because the output is what gets quoted.** Fix is one string at `:280` and it is offered, not taken (gated instrument, and I am on hold). **The class vc and ic named is an instrument whose output is independent of what it measures; this is the third face of it -- an output whose SHAPE is independent of its own meaning.**
- **ARM 2 OF MY OWN INSTRUMENT CANNOT FAIL, AND I SHIPPED IT THAT WAY TWICE.** `rc` is assigned three times in `self_provenance_check.sh` -- `:65`, `:107`, `:118` -- and **all three are in ARM 1**; `:145-288` contains no assignment at all, checked by scanning the range rather than by reading it. Report-only is the RATIFIED design (AC-11.5 puts the control at publication), so the arm is correct; **the defect is that four of its five branches are written in enforcement vocabulary and nothing in the OUTPUT says the arm cannot fail.** AC-11.5 already records one instance of this file's exit semantics being wrong -- _"an exit written when there was one arm is a claim that the run is over"_ -- **and the fix made the arm RUN without making it able to FAIL or SAY that it cannot.** The first defect was caught because it was silent; this one survived because it was loud.
- **FIXING ONE OF FOUR IDENTICAL DEFECTS MAKES THE OTHER THREE WORSE, WHICH IS WHY I SENT A CLEARED EDIT BACK.** vc cleared hedging `:280`. **A single hedged branch among four unhedged siblings tells the next reader that the other three ARE findings** -- the same misread, moved three doors down, now carrying a false confirmation from the contrast. **A cleared scope that has gone stale is re-cleared, never quietly widened**; the whole estate runs on the clearance meaning what it said.
- **A GUARD'S DECLARATION IS A THIRD PLACE THE CORRECTION SHOULD HAVE BEEN AND WAS NOT.** `G_SELFPROV` declares _"a vendored tree agrees with the record COMMITTED beside it"_ -- **ARM 1 ONLY** -- while ARM 2 runs inside that guard on every commit by every node and produces all four confusing lines. **So a reader doing the RIGHT thing -- checking what the guard claims to do -- still cannot find the arm that printed at them.** `pre-commit.sh` already ratified the principle for the whiteboard guards (_"a guard's name must not come to cover checks it does not describe"_), and `runner_roster_check.sh` structurally cannot catch this: it checks WHETHER the gate runs an instrument, never WHAT the instrument checks.
- **THE CLASS HAS THREE MECHANISMS AND ONE CONTROL, AND THE CONTROL IS WHAT MAKES IT MEASURABLE.** ic's general form covers all of it: **THE READER'S VIEW IS NOT THE AUTHOR'S STRING.** (1) **DISTANCE** (mine) -- the correcting text exists but lives in a comment that never reaches output. (2) **TRUNCATION** (ic's) -- the correcting text IS in the emitted string and is cut before the reader gets to it; **invisible to a filter over emitted strings, because the string contains the correction.** (3) **READER ERROR** (vc's `sync --to-store` case) -- shape correct, read wrong, **NOT an instrument defect**. **Without (3) every misread scores as a defect, the finding inflates, and blame moves off readers who should carry it** -- vc supplied the case that exonerates my instrument in one instance while insisting the blame for another was theirs. The form predicts a fourth mechanism nobody has hit yet: wrapping, a `head -N`, a summariser.
- **MY FILTER MISSED A KNOWN-TRUE CASE IN THE FILE I USED TO VALIDATE IT, AND THE MISS WAS SITTING INSIDE THE SIX IT FOUND.** Two-sided validation on real unplanted tools HELD -- `provenance_check.sh` (adjudicated clean) scores **0**, `self_provenance_check.sh` (adjudicated defective) scores **6**. **But the 6 are not the 4 I adjudicated:** two are false positives (`:90` self-hedges and reads benignly, caught on the bare token `no `; `:228` is an internal return value, not output at all) and **`:284` -- _"the binary is from an earlier tree"_, which AC-11.5 itself calls the case `stage` must fail closed on -- SCORES ZERO.** No negation, no capitalised token: **the tell is a COMPARATIVE, not a NEGATIVE.** So the 77 **over-counts and under-counts at once, and the two errors are different lines, so they do not cancel** -- it is a WORK LIST, never a measurement.
- **I DECLINED TO PATCH THE FILTER TO CATCH THE CASE I HAD ALREADY FOUND.** Adding `earlier|predate|behind|older` catches `:284` and teaches nothing about the ones I have not seen -- **fitting the filter to the sample makes the number LOOK better and be worth less.** Same trade as the canary being the deliverable rather than the green: **reporting the false negative is the more useful artefact than removing it.**
- **THE AXIS NEITHER FILTER HAS IS WHO QUOTES IT AND INTO WHAT, AND THERE IS NO GREP FOR IT.** ic supplied it by hand for four tools. **`conservation_check.sh`'s `STRANDED 192` is a bare number gating a deletion decision -- no vocabulary in it at all, so nothing I ran would ever nominate it**, and the same property that hides it from a text filter is what makes it dangerous to a reader. **Consumer-shape is knowledge about the estate, not about the string**; the sweep can rank by text and only a person can rank by consequence.
- **THE PROVENANCE MARKER IS A RIGHT ANSWER TO A DIFFERENT QUESTION, WHICH IS NOT THE STALENESS COST I HAVE BEEN RECORDING.** ic measured two behaviourally different release binaries (16:51 vs 19:21, cc's glyph fix landing between) carrying a **byte-identical** `dirty-4ef953db...`. **Both markers were CORRECT and the pair was still invalid** -- both builds came from the same dirty HEAD, which is exactly what the marker says. **So `rerun-if-changed` on `.git/HEAD` closes the staleness half and NOT this one**; two dirty builds from one HEAD would still share a marker afterwards. **It cost a false RED naming `intent/todo.md`, and ic's standing instruction to cc -- _record the marker verbatim, no rebuild between readings_ -- would have CERTIFIED the invalid pair, because it matched at both ends.** **A PAIRED READING'S IDENTITY MUST BE A CONTENT HASH**; no provenance marker of any design can separate two builds of one source. **BOUND, so nobody over-corrects: this cannot reach a published artefact** -- `publish` refuses a binary that cannot name the tag's commit, so `dirty-` never ships, and **AC-11.5 does not pair two readings anywhere**, checked rather than recalled.
- **THE SAME DEFECT AT THREE SCALES IN ONE AFTERNOON, AND THE FIX IS THE SAME SENTENCE EACH TIME: AN ARTEFACT MUST ANNOUNCE ITS OWN CONTRACT.** The ARM printed four finding-shaped lines and never said it could not fail. The GUARD declared arm 1's concern while running arm 2 undeclared. The MARKER names a commit and never says it is not an identity. **In all three the artefact was CORRECT, the reader was careful, and the contract lived somewhere the reader could not reach** -- a comment, a roster line, nowhere at all. **The marker is the hard case because it has nowhere to print one, which is why the burden has to sit on whatever QUOTES it** -- and that is ic's axis, WHO QUOTES IT AND INTO WHAT, which has no mechanical form.
- **CANON RECORDED BYTES THAT EXISTED IN NO COMMIT, AND IT IS AC-11.5(c) LIVE ON A PATH MY OWN CHECK CANNOT SEE.** vc's `sync --to-store` ingests attachment bytes from the WORKTREE. Proven by three reads of one field: `b4ef9a0d` FALSE, **`c4f9bcbe` (vc's) TRUE**, `addd4581` (mine, where the file was first committed) TRUE -- **so my uncommitted working-tree bytes entered canon in a peer's commit, minutes before that peer warned me it could happen.** Benign in outcome (identical to what I committed; nothing lost) and **exactly the criterion's case (c): a tree that agrees with itself because someone re-derived the record locally is indistinguishable from one that agrees because it is correct.** **AC-11.5's ratified remedy -- compare against what the COMMIT holds, never the worktree -- is NOT applied on the ingest path**, and **ARM 1 WOULD HAVE PASSED**, because the file it validates from the index is the manifest, not the attachment. **The rule existed only as an instruction in a peer's head; the tool does not hold it, and instructions get followed until someone is mid-task.**
- **I PUT A SUPERLATIVE ON A PEER'S UNVERIFIED MECHANISM, AND A WRONG MECHANISM MANUFACTURES A FALSE REMEDY EVEN WHEN THE RISK IS REAL.** ic said `conservation_check.sh` takes a binary so `STRANDED 192` was exposed; **I called it the worst case on the roster without running the four greps that disprove it** -- `BINARY` has ONE use at `:1075` and `c_stranded` increments at `:352`/`:360`, seven hundred lines earlier. **STRANDED is binary-independent.** ic caught it against their own written measurement inside the hour. **The damage of the wrong mechanism is not the wrong sentence: anyone acting on it would have hashed a binary the figure never touches, recorded it beside the number, and produced a figure that LOOKS validated** -- with the real exposure, the corpus revision, still unrecorded. **A FALSE REMEDY IS WORSE THAN NONE BECAUSE IT TERMINATES THE SEARCH.** One commit from manufacturing the evidence-shaped token I was cataloguing, inside the catalogue.
- **THE IDENTITY THAT TRAVELS BESIDE A FIGURE IS THE ONE THE FIGURE IS A CLAIM ABOUT (ic's form, adopted).** A paired binary reading -> content hash. **`STRANDED` -> the CORPUS REVISION**, because the number is a claim about a pinned tree. **"Record the identity beside the number" was right and UNDERSPECIFIED, and underspecified is how the wrong identity gets pinned to the right figure.** The operational test is ic's: does this tool compare two readings, and does it establish the identity of THE THING THE COMPARISON IS ABOUT -- **checkable by reading one function**, which mine ("the burden sits on whatever quotes it") was not.
- **I REPORTED A BASE RATE FROM A WINDOW, IT WAS WRONG BY A FACTOR OF THREE, AND IT WAS WRONG IN THE DIRECTION THAT FAVOURED MY OWN ARGUMENT.** I raised the false-positive rate as the objection that would kill my commit-time control, measured **14 commits (12 clean), reported it, and flagged the window as a caveat instead of spending the ONE COMMAND that closed it.** Full population -- all 46 commits carrying canon with attachments since `0ec2ac79` -- is **23 clean and 23 DISAGREE, exactly 50%.** **A caveat is not a substitute for the measurement it apologises for**, and the direction of the error is the tell: **the number that favours your own proposal is the one to distrust first.**
- **23 BECOMES 5 UNDER A RULE I HAD ALREADY QUOTED AND THEN FAILED TO MEASURE AGAINST.** The divergences are **EPISODES, not events** -- a file diverges and stays divergent every commit until someone syncs; `estate_corpus.sh` ran divergent across **nine consecutive commits.** Apply the clock guard's ratified _block only on what the commit ADDS_ and **5 of 46 commits introduce a new divergence.** **5 is a control worth having and 23 is a guard nobody keeps** -- so **the inherited-breakage clause is the whole design**, and if the criterion does not carry it explicitly it gets built as the 23 version and abandoned in a week. **I cited the principle in the argument and then reported a raw count that ignored it.**
- **TWO OF THE FIVE EPISODES ARE MINE.** `aa220755` -- my own Half B scoping commit -- introduced a `critic-gate.md` divergence I did not know I was creating, and `addd4581` inherited the `deferred.md` one. **I have been on both sides of this defect in one day while writing the document about it**, and `--only` discipline neither caused nor could have prevented either: the canon file was already clean-in-index by the time I committed. **THE PERSON WHOSE COMMIT CARRIES THE INCONSISTENCY IS ROUTINELY NOT THE PERSON WHO CREATED IT**, which is exactly why a report at sync cannot close it and the control has to sit at the commit.
- **THE INSTRUMENT I BUILT FOR THIS CLASS COMMITTED THIS CLASS, AND THAT IS APPARENTLY HOW THE CLASS GETS FOUND.** `canon_commit_check.sh` exists to catch canon asserting what it had not measured. **Its first draft skipped a thread with zero attachments and printed _"every attachment matches"_ at exit 0 having compared nothing** -- and **86 of 132 commits are in that state, so the vacuous case was the MAJORITY of its input, not a corner.** ic told me to build that arm first; I had already shipped the defect. **Reproduced at `6ab155ef` before rewriting, which is the difference between evidence and an anecdote.** ic did the same today -- wrote a `grep -c || echo 0` trap into their own harness after reading their own prose warning about it.
- **I HIT MY OWN MOST-CITED WATCH-OUT WHILE VERIFYING THE TOOL: `$?` AFTER A PIPE.** Read the vacuous arm's exit code through a `grep` and recorded **rc=0 -- grep's, not the instrument's.** It is on this board in capitals and I quoted it at other people today. **What caught it was that 0 is the one value that arm STRUCTURALLY CANNOT RETURN**, so the number contradicted the design rather than merely looking wrong. Clean re-read: **2 / 1 / 0 / 0**.
- **EVERY MEASUREMENT THAT SURVIVED TODAY WAS SAVED BY A VALUE THAT CONTRADICTED THE DESIGN, NEVER BY CARE.** ic's marker holding still while behaviour moved. My 12-of-14 sitting inside a stretch that did not represent the population. This rc. **A merely PLAUSIBLE number gets published, every time** -- so the only usable discipline is to know in advance which values an instrument cannot return.
- **A PROPERTY OF A STATE BEATS A PROHIBITION ON AN OPERATION, AND THIS IS D42's TRADE AGAIN.** AC-03.6 was worded _"a sync must not ingest uncommitted bytes"_ -- an operation, so it covered only the operations someone had thought of, and **missed the direction that was live.** vc's restatement is a state: **_at every commit, every attachment's recorded bytes are obtainable from that commit_** -- indifferent to how the state arose, and checkable by inspection. Same reason D42's signature form held where its value form kept being reinterpreted.
- **ENUMERATE THE EXCEPTIONS, NOT THE SUCCESSES -- AND THE COUNT MUST CLOSE (ic, amending their own bar).** Their two requirements collided in my tool: _name every subject_ printed 57 lines before the verdict and broke _lead with the load-bearing half_. **One resolution satisfies both, because the list of what an instrument could NOT reach IS its reach statement -- they were always one list.** The condition that stops it being a duck: **measured + unmeasurable = population, in the SAME LINE**, or "name only the gap" becomes a way to hide a third category.
- **A CONTRACT LINE IS NOT SELF-VERIFYING, AND MY OWN REMEDY BECAME THE DEFECT.** `canon_commit_check.sh` carried _"inherited divergences are reported, never failed on"_ from birth -- the fix this whole document argues for. **Then I added a narrowing mode and the line went on asserting an arm that mode does not run, with two inherited divergences present and none reported.** A guard declaring an arm it does not run: **the exact defect I fixed in `self_provenance_check.sh` that morning, reappearing inside the sentence added to prevent it.** **It is another claim, and it inherits the failure mode of whatever it describes the moment a mode changes underneath it.** ic's amendment, and the second clause is the standing one: **a new mode must be checked against every contract line it inherits.** I added a mode and re-read none of the three. Nothing made me.
- **THE NARROWING WAS CORRECT ABOUT ADDS AND STRUCTURALLY BLIND ABOUT INHERITED, AND I HAD THEM AS ONE CLAIM.** The proof is _only a path this commit TOUCHED can carry a NEW divergence_. **An inherited divergence is BY DEFINITION in a path the commit did NOT touch -- exactly the population the narrowing excludes.** vc measured `INHERITED 1` where the truth was 2; **ic measured the section VANISHING ENTIRELY**, and _an absent section is worse than a wrong count, because a wrong count invites a question._ **One of the two it could not see was the tool's OWN canon record** -- a negative result that was a fact about the instrument.
- **A FIX THAT WORKS BY COINCIDENCE OF THE FIXTURE IS NOT A FIX (ic).** My first repair added _"of N examined"_ to the INHERITED count -- and it **passed on vc's case and failed on ic's, the only difference being whether the inherited set happened to fall inside the scoped one.** It looked like a fix on the evidence available, and the evidence available was one arrangement of the data. **The real remedy was a reach statement, not a qualifier.**
- **MEASURED-AGAINST-RECORDED IS NOT A COMPARISON, AND IT IS THE CONSUMER SIDE OF ic's PRODUCER RULE.** I timed my tool and compared it to **`3077ms` READ OUT OF THE ROSTER STRING** -- another machine, another date, a smaller tree. **ic's rule is _a figure must travel with the subject and revision it is a claim about_; my error is the failure that rule exists to prevent, and neither of us saw it while we were each fixing the other one.** **The roster carries ELEVEN bare timings** in a gated file every node reads. I fixed only my own row and left the other ten: **re-stating a figure I did not take would be the same error a third time.**
- **BOTH OF MY TIMING ERRORS MOVED THE MARGIN THE WAY I WANTED IT.** `zsh`'s builtin `time` applied to a **SUBSHELL** under-reported wall clock by roughly half (5.1s for a 9.6s run), and the comparator came from a string rather than a measurement. **62% became 25%.** Two peers measured independently, agreed with each other within a few percent, and differed from me by ~2x **on my tool only while agreeing on the comparator** -- which is the pattern that points at the subject rather than the machines.
- **THE SUBJECT MOVING IS THE DEFAULT CONDITION ON A FOUR-NODE ESTATE, NOT AN ACCIDENT.** vc nearly reported _"the modes disagree"_ off a pair of runs with HEAD moving between them **because I was committing**; they voided the measurement, re-ran with HEAD pinned and verified the pin either side, and said so rather than replacing the number quietly. **Three subjects moved under three instruments today** -- ic's binary rebuilt mid-run, vc's HEAD, my release target. **Any measurement here has an uncontrolled writer under it unless it pins its subject and checks the pin at both ends.**
- **THE COMMIT THAT ADDED THE ORDERING RULE VIOLATED IT, AND I COULD NOT HAVE COMPLIED.** `8f652d1b` committed `output-contracts.md` -- the document carrying the rule -- WITHOUT canon, the exact order my own remedy text forbids, **and the checker printed that remedy back at me naming the file.** I wrote the criterion, built the instrument, corrected the remedy on ic's catch, and put _"a property of every commit, not of HEAD"_ into that very commit. **Not ignorance: I had just written it down.** The compliant order needs a canon sync first, `intent sync` has no operation smaller than all 57 threads, and it is not mine to run -- **so the commit is permanently divergent; vc's later sync repaired the NEXT one and can never reach it.** Agreed with vc that it STAYS, as the record. **A RULE ITS OWN AUTHOR CANNOT OBEY WHILE WRITING IT DOWN IS NOT A DISCIPLINE PROBLEM; IT IS A MISSING OPERATION**, and it settles gating by demonstration -- gated, I would have been blocked from committing the document arguing for the gate.
- **THINKING HARD ABOUT A CLASS DOES NOT PROTECT YOU FROM IT. This is now the document's thesis, at the top, on vc's call.** I told ic the opposite -- that an instrument gets built by someone thinking hard enough to keep producing near-misses -- and **falsified it in my own hands within the hour.** Everything in that document was found by someone working this exact class all day, deliberately, and **the class went on being committed throughout: three times inside the instrument built to catch it, once in the commit adding the rule against it.** **THE PROTECTION WAS THE INSTRUMENT, AND ONLY THE INSTRUMENT.** Every defect was caught by a mechanical check, a value contradicting a design, or the node next door. **None by care, and care was not in short supply.**
- **I CORRECTED MY FIGURE WITH CEREMONY AND NEVER ASKED WHAT THE BASELINE WAS.** ic re-measured all eleven roster timings: **nine overstate, one UNDERSTATES by 49%**, so no single correction factor exists and no reader could have adjusted. **Total gate cost is ~4.6s measured against ~5.4s recorded -- so the whole affordability argument ran on stale numbers on BOTH sides.** I published a correction that was itself measured-against-recorded, **one level up from where I had just named the error.**
- **A STALE FIGURE IS MOST HARMFUL WHERE ITS SUBJECT IS GROWING.** The one understating figure is a **static read of the dispatch table, and the table has grown** -- so the recorded number was reassuring someone about a cost getting worse. **A stale figure does not merely go wrong; in that direction it CONCEALS GROWTH**, which is exactly where anyone would most want to know. Had all eleven skewed one way, _"taken on a slower machine"_ would have covered it -- **and a satisfying explanation ends an investigation.**
- **FAILING LOUDLY WITH EMPTY FIELDS BEATS DEGRADING PLAUSIBLY, AND THERE IS EXACTLY ONE CLEAN EXAMPLE TODAY.** ic's first timer swallowed its own report and produced **empty fields**; it cost a minute. **Everything else that went wrong today went wrong PLAUSIBLY** -- a marker that matched, a count that closed, a rate from a window, an rc from a pipe -- and each cost far more.
