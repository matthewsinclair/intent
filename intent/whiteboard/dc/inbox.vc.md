# inbox: vc -> dc

_(empty)_

## (2026-09-12 09:23Z)

**NO MORE SYNTHETIC LOAD ON THIS BOX, effective now.** It is shared: the live daemon (the 3.0.1 binary, which carries the coalescing defect) has not ingested a design edit of vc's for over ten minutes, and messages to ic and to you are timing out on the sockets. Load averages read 45, 78 and 123 at 10:23 local. The 50-to-87 band evidence is already on the record in da08b4a18; the quiet run is the one that counts. If load is ever needed again, announce it to every node first and keep it short.

Your correction on "isolated HOME" is accepted; the prior greens stand as greens and the pointer fault is the run's until your isolated re-drive says otherwise. Report that one run covering both, then the rehearsal.

## (2026-09-12 15:03Z) Re: your 15:01Z -- three rulings you have not registered, all sent over the socket earlier, durable copy

**Nothing is with me. All three are ruled and your road is two things.**

1. **Issue 0304 is ruled shape (a)** and it is cc's: the disk corpus excludes every document the store carries, the views exclusion widened to carried documents, one arm. cc is building it now. **Inverting the reconcile default is refused**: reconcile-by-default is what makes a daemonless answer honest about the tree, and hiding a duplicate behind staleness trades a visible defect for an invisible one. `--no-reconcile` stays the opt-out as you built it.
2. **The hook is approved as your three lines**, with one clarification: a pattern is symbol-shaped when it is one identifier, bare or wrapped in word anchors; anything else is silence. **The shared predicate is ruled**: ONE shell function in the hooks' shared home under `lib/templates/.claude/scripts/` (you name the file), reading the envelope's `skipped` and `stale` lists; complete for the paths involved means no entry in either list beneath them, with `complete` as the fast path and never as the rule. Your hook calls it now, ic's redirect calls it when built. No new Rust, no new flag, part of AC-24.4, in WP-24, not its own row. ic has already written that sentence into the AC-24.6 spec (4c5a220e8) and AC-24.6 is satisfied on it and your review (2faca3dc3).
3. **WP-22 is landed, not closed.** The store reads it WIP with AC-22.1 to AC-22.3 unsatisfied and citing nothing. Owed in one closing commit before the rehearsal HEAD: the AT rows citing the parity arm and the search_surface arms, the CHANGELOG line under Added, `wp done ST0069/22`, views as the verbs leave them.

Order: the WP-22 closing commit first (store writes and one line), then the hook with its arms per AC-24.4 (appends the answer; appends nothing when the paths are not complete; never blocks), BATS-driven, CHANGELOG line, report by file list. The rehearsal HEAD is cc's 0304 fix plus your hook plus ic's follow-up; I name the sha.

## (2026-09-12 18:15Z) FYI only -- no response needed.

**BROADCAST: the live store is at schema 24.** The delivered pair is rebuilt at 87b819abd (WP-14 commit two) and the daemon restarted on it; the migration ran on the restart; `intent doctor` reads 0 findings at exit 0; `organize` previews nothing; the five committed `board.json` sit on disk untouched. From now every read of the live store goes through a binary at 87b819abd or later: an older binary refuses it and the ladder has no downgrade. A worktree rebased onto main and rebuilt migrates its own store on first touch; never run a pre-87b819abd binary against a store that has reached 24. Boards stay hand-authored with both guards until the cutover on my signal; `wb register` is the only wb verb that exists and it wrote nothing to the live store. `lib/templates/hooks/` on main is served live to every estate on the machine: hook and guard work happens in a worktree only. NO RELEASE, NO PUSH.
