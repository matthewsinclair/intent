# ic DOING, folded 2026-09-05 15:07Z -- localfold on hv's instruction, before a compact

**FOLDED ON EXECUTION, NEVER ON DATE.** Everything below is landed, committed and green. Unexecuted rulings stayed on the live board. Pre-fold board verbatim at `wip-prefold-1507Z.md` (104,164 bytes, prettier-clean at source before the copy and `cmp`-confirmed).

## `0244` -- built, closed, and split (the session's main work)

**BUILT** at `aa1b0ca0`: `crates/intent-cli/tests/routing_is_opt_in.rs`, `doing_what_the_refusal_told_the_operator_to_do_actually_gets_them_the_answer`. The S form on vc's standing authorisation. It parses the command out of the EMITTED remedy line and executes it, then re-drives the original argv. Nothing in it names `daemon start` -- a command is DERIVED as a backticked span beginning with the binary's name, so `--daemon` is excluded by the rule rather than by a name the test knows. No new fixture; `refuse_a_stale_sibling_daemon` became `pub`. The daemon it starts is detached by design, so a `Drop` guard stops it.

**THE MUTATION CRITERION WAS MET TWICE AND THE SECOND ONE IS THE RESULT.** M1 (remedy -> nonsense) went red on the vacuity arm and proves only that the test reads the string. M2 (`intent daemon start` -> `intent daemon status`) went red on the join: a WIRED verb that exits 0 and leaves the operator at rc=2, which the OLD test passes happily. `render.rs` carried both mutations transiently, announced to cc beforehand and restored from HEAD; cc verified it clean independently.

**CLOSED** at `687889c7` on vc's ruling -- their own `0238` precedent run backwards: an open row is read as work outstanding, so `0244, open` would go on claiming that nothing joins a remedy to its recovery.

## `0257` -- filed, then measured, and the premise was wrong twice

Filed at `687889c7` with the argument rather than the observation: an unjoined promise is a gap, and **a wired-but-wrong remedy is a gap that READS AS COVERED**.

**MEASURED at `d30e0e6f` on vc's split (count, do not rename): N = 0.** 406 AT rows in the estate; ONE cites `remedies_are_reachable.rs` (`AT-06.11`); ZERO cite the test's name. Controlled -- `every_command_reference_is_backticked`, a real `fn` in the SAME FILE, IS named in `AT-06.11`'s own note.

**BOTH HALVES OF MY BLAST-RADIUS PREMISE WERE FALSE and are struck on the row.** Acceptance rows cite FILES, not tests -- I assumed a granularity. And `intent at edit --file` exists, driven past clap to its own guard rather than read off `--help`. A rename costs TWO SOURCE LINES, one a rustdoc intra-doc link. Disposition is hv's and now takeable.

## `0258` -- filed on vc's ruling

_work handed to a node reaches no surface that node reads._ Spine: **the surface was INDEXED and indexing is not the fix** -- `intent search 'handed to ic as surface owner'` returns `0175.json` first. Distinguished on the row from cc's whiteboard-indexing half (`ST0056/14`) and from `0208` (authorship, not assignment). Fix deliberately not in the title.

## The `122 / 138` reconciliation -- stood down, and the definition step answered it

Offered a third count, authorised, and committed the population definition FIRST at `93455321`. **The count was never run:** dc reconciled it while vc was writing the protocol, and the definition file had already named the boundary from shape alone -- entries live in TWO places, `.families[].entries[]` and a top-level `.new_surface[]`. Driven: 122 + 16 = 138, all sixteen carrying one distinct `v2` value.

## Corrections against me that landed today

- **W70 corrected** -- it said the `0175` assignment was findable only by reading every issue body. False; search returns it first result. A watch-out carrying a wrong diagnosis is worse than none.
- **W76** -- I asserted `intent at` had no verb retargeting a citation; vc quoted it back approvingly and neither of us drove it.
- **W82 corrected** -- my count discriminator was right and my conclusion from it was backwards. The narrowing anchor was mine; `W4a` is a lettered sub-entry my pattern could not match.
- **W85** -- I verified that count with a "second, independent" pattern that shared the suspect clause. The agreement was guaranteed by construction.
- **W87** -- refuted dc's two-builds-disagree claim: both artefacts are pre-fix and both answer rc=2. Blast radius measured by dc at zero.

Instrument errors caught in-session: a zsh backtick in a double-quoted `echo` ran the POSIX `at` scheduler mid-evidence (W84); a node-local grep pattern returned 0 on three boards and nearly became a finding.
