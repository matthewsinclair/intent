# ic -- DOING / Live / TODO folded out 2026-09-05 20:26Z

Verbatim pre-fold board: `wip-prefold-2026Z.md` (114,252 bytes, prettier-clean at source, cmp-confirmed).
This is the EXECUTED narrative only. Every hold whose condition still stands remains on the live board.

## DOING

**WORKING vc's QUEUE AFTER hv's COMPACT, 2026-09-05 17:49Z.** Booted, pinged vc rather than re-deriving their state (their instruction), and took the three items they ranked.

**`0257` CLOSED (`0bdf2a70`).** vc's ruling under hv's pen, option 1: rename to what it checks. **It arrived with vc's own provenance correction attached, which is worth more than the rename** -- `0257` was hv-only on one focus line and nowhere else, no entry, no reasoning, no condition; the scope-call premise was one my own measurement had already struck. **The twin one screen down carried the SAME overclaim in its name** and its doc comment says so in as many words, so it was renamed too and CALLED OUT in the row, because the row's title names only the first. No mutation offered, deliberately: a rename alters no predicate.

**`0269` FILED (`cee20f52`).** The orphan class vc ranked above `0258`. **Every instrument in this space starts from a ROW** -- `stale_at_check.sh`, `absent_at_check.sh`, `declared_kind_check.sh` -- so an artefact no row cites is invisible to all of them, and the nearest one declares that gap outside its own reach in its own words. **151 of 279; 36 carry the estate's L3 coverage literal while nothing cites them.**

**I CORRECTED THE PREMISE I WAS HANDED RATHER THAN FILING ON IT.** The other direction was reported to me as gated and reporting 37 absent. The 37 reproduces exactly and is **not a defect count**: 36 are `to-write`, 1 is `fiat`, **0 are `green`**, and `at lint` is clean on every ST. A `to-write` row citing a file that does not exist yet is what `to-write` MEANS.

**AGGRESSIVE LOCALFOLD 2026-09-05 17:23Z ON hv's INSTRUCTION, BEFORE A COMPACT. STATUS STAYS `active`** -- a fold is not a session ending (`/in-whiteboard` invariant 6). Pre-fold board verbatim at `.history/20260905/wip-prefold-1723Z.md` (108,701 bytes, prettier-clean AT SOURCE before the copy and `cmp`-confirmed). Executed narrative: `.history/20260905/doing-folded-2026-09-05-1723Z.md`. Second fold of the day; the `1507Z` pair is beside it.

**NOTHING IS IN FLIGHT AND NOTHING OF MINE IS UNCOMMITTED.** `git status` is the check, not this line.

**WATCH-OUTS ARE NOT COMPRESSED BY THIS FOLD, AND THAT IS A JUDGEMENT RATHER THAN AN OMISSION.** They are standing by rule, and **W91 is the specific reason: compressing a peer's finding is the exact operation that manufactured a false attribution on vc's board today** -- `STALE` became `DANGLING` in a fold and a currency problem became an outage with my name still on it. If hv wants the section shortened, it wants a pass that copies severity-bearing words rather than paraphrases them, and that is its own piece of work.

**FOLDED OUT TODAY, AS POINTERS:** `0247` fixed and closed (`9e742cad` / `ca101335`); `0244`'s fixture leak fixed (`da5919e8`); W90-W93 banked. The day's theme was provenance -- two claims about my work were refuted or retracted, and one of mine about a peer was refuted, each by driving rather than by argument.

## Live, unfinished, not mine to close alone

**`AC-17.6` HAS ONE OPEN LIMB AND IT IS NOT MINE TO TAKE UNILATERALLY.** The second limb was `0238`, FIXED on both driven limbs 2026-09-04 (`79047e3d`, `5a3b22fd`) and folded into `.history/20260905/`.

1. **A WORK PACKAGE HAS NO BROWSER ADDRESS.** vc ruled Option 3 (`authority: vc`): give `wp` a shape in `nav.rs`. **The shape is `View::Child { kind, id, field, item }` -> `/thread/ST0056/wps/17`**, using the already-declared `wps` descent rather than an invented segment. vc's structural framing beat mine and I have adopted it: **`View::Item` carries ONE id and a work package needs TWO**, so the missing shape is the ITEM OF A CHILDREN COLLECTION -- `Collection -> Item`, `Children -> nothing`. **SIZE M WITH THE TUI ARM UNMEASURED**: today a wp is an EXPANDED ROW inside the wps collection, not a pushed view, so this adds a level the terminal does not navigate to either, and `AC-17.12` + `AC-17.7` mean shipping the browser half alone recreates the asymmetry vc ruled against, pointing the other way. **WHETHER IT RIDES 3.0.1 IS WITH hv.** DO NOT START (vc).

**`AT-17.6` ASSERTS THE `wp` REFUSAL, SO THE GAP CANNOT GO QUIET** -- that test fails the day `nav.rs` grows the shape, which is the intended signal rather than a nuisance.

## TODO

**NOTHING IS STARTABLE, AND THAT IS A MEASUREMENT RATHER THAN BREVITY.** Everything of mine is either closed, unclaimed by choice, or gated below with the condition that releases it.

**`0258` IS SIZED AND NOW RULED (vc, candidate C -- store-side assignment event, no interim mechanism). NOT STARTABLE AND NOT MINE TO WAIT ON:** gated behind `ST0056/14`, which vc holds to be in the 3.0.1 cut. **I RECORDED A CAVEAT ON THE ROW BECAUSE THE RULING RESTS ON IT:** `ST0056/14` still reads `Not Started`/`L` (driven), and the reinstated rows are about the FLEET CORPUS, not the coordination model, so `WP-14 is in the cut` does not follow from their subject -- and both rows are satisfiable by their residue disjunct, which weakens the dependency the ruling leans on. Raised to vc; theirs to weigh. Superseded sizing note:, not more measurement: the record half is **S** with no migration, and it **fixes nothing on its own** -- a field plus a filter makes the query possible and does not make anyone run it, which is the row's own diagnosis rather than its fix. Delivery is unsized on purpose; `ST0056/14` (whiteboard in the store, Not Started, **L**) is the real dependency. **HAZARD FOR WHOEVER TAKES IT: `render.rs` is cc's, and `--severity` shipped declared-but-unparsed in that same table, so a `--help`-reading test would pass throughout.** Was: -- the assignment channel; size still UNMEASURED, and the fix deliberately not named in its title. vc ranked it BELOW the orphan class and that ordering was right: `0269` is a class, `0258` is an instance. **`0257` is CLOSED**, released from hv-only by vc on the grounds that the reasoning behind it had gone missing.

**`ST0056/13` IS LEFT UNCLAIMED DELIBERATELY.** One defect fixed on its merits is not the first slice of a work package I have not scoped, and a claim is a signal to peers about what I am doing. vc offered it and adopted the reasoning when I declined.

**FILED AS `0269`, AND IT WAS A CLASS RATHER THAN THE INSTANCE I RAISED: `routing_is_opt_in.rs` is cited by NO acceptance row** -- the file whose first line declares it `AC-08.2`'s witness, carrying seven tests including hv's routing reversal and my `0244` join. `AC-08.2`'s only cover is `AT-08.2`, which cites `daemon_and_local_agree.rs`. It may be intended, so the INSTANCE is still a ruling rather than a defect -- but vc drove the generalisation before answering me and the CLASS is now a row: 151 test files on disk that no row cites, and no instrument that starts anywhere but a row.

**`0247` IS NOW CONFIRMED THROUGH THE DELIVERED PAIR, which is the one thing this row closed WITHOUT.** dc's `0196` fix landed, ran and promoted, so `intent` and `intentd` both report `3.0.0 (da5919e8)` -- and `merge-base --is-ancestor` says the fix is IN that build rather than merely newer than a tag, which is the three-point ordering error made once already today. All eight queries rc=0 through `~/.local/bin/intent`; `family-root` reaches `0175`; a malformed expression still refuses with the replacement remedy printing. **Hit counts deliberately not carried across from the debug run: the corpus moved between them, and a count of a live index is a measurement with a timestamp (W93).** **I never built a private pair to get here** -- the check waited for the artefact rather than manufacturing one.

1. **`0222` -- the explorer thread list's `Status | Created | Completed` columns. RELEASES WHEN `intent wp show ST0056/17` READS `done`** -- a state I check at pickup, needing no message from anyone. **SIZE UNMEASURED; do not quote one.** The blocker is the row type, not the columns: `Row` carries one name and one value, and the thread-collection arm is the one view that does not go through `form::triples`.
