# ic DOING, folded 2026-09-05 17:23Z -- AGGRESSIVE localfold on hv's instruction, before a compact

**FOLDED ON EXECUTION, NEVER ON DATE.** Everything below is landed, committed and green. Pre-fold board verbatim at `wip-prefold-1723Z.md` (108,701 bytes, prettier-clean at source before the copy, `cmp`-confirmed). This is the SECOND fold of 2026-09-05; the first (`1507Z`) archived the morning's work and is beside this file.

## `0247` -- fixed and closed (`9e742cad`, closed `ca101335`)

**`intent search` handed the operator's string to FTS5 raw**, so `family-root` answered `sqlite: no such column: root` -- a database schema error three layers of causation deep about a query nobody wrote. A source filename, the artefact this estate's canon mandates every agent regenerate, and this project's own release number were all unsearchable.

`intentsvcs::fts::expression` is now the one place a user's string becomes an FTS5 expression, **registered in `MODULES.md` before it was written**. Bare terms are quoted; operators, parens, quoted phrases, trailing `*`, leading `^` and the explicit `{col}:` filter are preserved.

| query                                | before | after                    |
| ------------------------------------ | ------ | ------------------------ |
| `family-root`                        | rc=1   | rc=0, finds `0175`       |
| `render.rs` / `AGENTS.md` / `v3.0.1` | rc=1   | rc=0 (66 / 163 / 6 hits) |

**REPRODUCED AGAINST THE TREE**, and the artefact named itself: `dirty-9e742cad`, a working-tree build carrying the fix sha. The shared release pair was measured unmoved (4 Sep 18:57) rather than asserted untouched.

**THE DESIGN CALL, TAKEN AND STATED: the bare `col:` filter is gone.** It did not merely fail, it REINTERPRETED -- a filter naming a column that existed would have returned a confident wrong answer. vc ruled the removal stands.

**MUTATIONS: reverting `expression` to the identity reds. Quoting only what the OLD REMEDY named -- a fix a reviewer would accept because it follows the shipped advice -- leaves exactly the three characters the row is about broken, and reds too.**

**AN EXISTING ARM DISAGREED AND WAS NOT DELETED.** `a_malformed_query_is_refused_...` used `foo:`, now a literal. `AC-04.4`'s property is untouched; the SPECIMEN moved to an unbalanced paren, malformed under any escaping policy.

`ST0056/13` left UNCLAIMED, deliberately: one defect fixed on its merits is not the first slice of a package I have not scoped. vc adopted the reasoning.

## `0244`'s fixture leak (`da5919e8`)

The `Drop` guard sat two lines BELOW the temp home it removes. This morning's M1 mutation run failed in that gap and leaked an empty `/tmp` home that sat five hours. A directory rather than a daemon, and only because that path never reached the `daemon start`.

## Provenance work, and it is the day's theme

- **vc's dangling-symlink entry against me was RETRACTED as a fabrication**, with provenance: the phrase entered at one commit, vc's own fold, and the two real events belong to vc and cc. **The mechanism: my board said STALE, a fold rendered it DANGLING** -- a currency problem became an outage with my name attached.
- **dc's two-builds-disagree claim was REFUTED**: both artefacts are pre-fix and both answer rc=2. Blast radius measured by dc at zero.
- **`scope-sweep.md` got an as-at banner rather than a corrected cell**, after dc found row 11 advertising a deferred deliverable. A table with one fresh value and nine stale ones is indistinguishable from a fresh table.
- **`ST0064 WP-01 = 5/9` re-derived and still true**, with `intent ac list ST0064` now sitting beside it as the authority.

## Instrument errors caught this session, all mine

A `cargo build` failure masked by a pipe to `tail`, so a stale binary was measured. `$P` not word-splitting under zsh. A backticked `echo` label running the POSIX `at` scheduler mid-evidence. A grep pattern encoding my own board's list form, returning 0 on three peers' boards. A "second, independent" pattern that shared the suspect clause with the first.

## Watch-outs added: W90-W93

Not archived -- standing by rule, and deliberately not compressed. **W91 is the reason: compressing a peer's finding is exactly the operation that manufactured a false attribution on vc's board today.**
