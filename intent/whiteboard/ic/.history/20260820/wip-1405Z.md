## DOING

**RULING 2 (AC-05.2) IS BUILT AND GREEN, BOTH HALVES, UNCOMMITTED.** Workspace **941 pass / 0 fail over 134 targets** with `--no-fail-fast`, rustfmt clean on every file I touched, clippy clean on mine.

- **The list edit, keyed on the OP string** (`declared_list_edit`). **Never on status, and that is arithmetic rather than principle:** `st.triage` and `st.reinstate` both land on `NotStarted`; `st.start`, `st.resume` and `st.reopen` all land on `Wip`. A status-keyed match makes `st triage` start listing threads and `st start` re-add one a human removed. **Two collisions in a vocabulary of eight ops.**
- **`edit_list` runs AFTER `apply`.** Both orders can be interrupted between the two writes, so the only question is which half-done state survives. Manifest-first leaves a live thread marked not-realised and the next `organize` removes its files; store-first leaves a closed thread listed, **which is exactly what `--keep` asks for on purpose.** One order degrades into a supported outcome, the other into a deletion nobody asked for.
- **ABSENT IS NOT EMPTY: no lifecycle verb creates the manifest.** Explicit `NotFound` arm; **any other io error is raised**, because an unreadable file answering as "nobody has said" is the No Silent Errors case exactly.
- **The closing note** via `sync_uncommitted`, read BEFORE the write, **tied to the removal and not to the verb** -- `--keep` cancels the dehydration so there is nothing to warn about. An attachment-less thread reports nothing: **zero attachments hold zero uncommitted bytes by arithmetic, in a repository or out of one.**
- **`Outcome::MovedWith` + `notes()`, printed from `reported()`.** A new variant does NOT force the nineteen arms -- they all ask `already()`, a method, which absorbs a variant in silence. **What makes a note undroppable is the single reporter, not the enum.** `moved()` added beside `already()`; `assert_movement` now asks the property rather than the variant.

**FOUR MUTANTS, EACH REDDENING EXACTLY ONE TEST AND ONLY THAT ONE:** triage/start keyed Add; hold/resume keyed Remove; the `NotFound` arm creating the file; the list edit hoisted ahead of the self-loop return.

**NOT DRIVEN, AND THE FILE HEADER SAYS SO:** `UnsyncedAttachments`'s payload needs a real git index and `common::Fixture` is a bare temp dir. **The warning's WIRING is proven and its PAYLOAD is not.**

## ON RESUME -- read this first

1. **COMMIT IS PENDING matts AND THE PATHSPEC MATTERS MORE THAN USUAL.** `render.rs`, `dispatch.rs`, `spine.rs`, `flag_reachability.rs`, `init.rs` and `build.rs` are all live in other sessions. **`--only` separates FILES, not AUTHORS** -- read `git diff --cached` before landing.
2. **I EDITED A PEER'S UNTRACKED `flag_reachability.rs` TWICE AND HAVE TOLD THEM.** Its ratchet reddened on my wiring, which is its contract working. Then its detector proved to have a **systematic false negative**: `render.rs` reads booleans through its own `flag()` helper -- **11 call sites against 4 direct `get_flag("..")`** -- and the helper spells the id as a VARIABLE, so nothing inside carries it. **10 of its 18 inherited-unread entries were false positives, including its own headline example `st new -s/--start`, read at `render.rs:587`.** Added `flag(` to MARKERS; baseline 18 -> 8.
3. **RULING 3 IS NEXT: top-level `intent edit <ID>`** (AC-05.1 / AT-05.1, `to-write`). `address::promote` already takes both id shapes, so it is promote -> hydrate -> print, and `st edit` delegates. **Do NOT land the dispatch-table row in a separate commit** -- declaring a verb one commit before implementing it adds a member to the declared-but-unwired roster for no reason.
4. **THE ABSENT-MANIFEST FINDING IS WITH vc (placement), hv (durable) AND dc (`init` faces the same decision).** Not urgent: on absence both verbs refuse, so nothing is written and nothing deleted -- **wrong answer, safe direction.**
5. **I MUTATION-PROVED FOUR ASSERTIONS BY `cp`-ing `facade.rs` ASIDE AND RESTORING IT, FOUR TIMES, IN A FOUR-WRITER TREE.** It held because nobody happened to be in that file, which is luck presented as method -- cc hit the same technique from the other side and nearly clobbered my `render.rs`. **From here: a detached worktree with its own in-tree `CARGO_TARGET_DIR`.**

## TODO -- LIVE ONLY

1. **DONE (`d855ea1f`, `95ffb84b`).** Ruling 1 closed end to end, including `Facade::hydrate`'s Issue arm -- the one that resolved into CANON.
2. **Ruling 2: AC-05.2.** `st new` adds / `--dehydrate` skips; `st done`/`st cancel` remove / `--keep` skips; `st reopen`/`st reinstate` add back; WARNING via `Facade::sync_uncommitted`. **Needs an `unpin` beside `pin`.** Then rewrite `edit_writes_pinned_region.rs` for real and green AT-05.2.
3. **Ruling 3: `intent edit <ID>`** -- AC-05.1 / AT-05.1 (`to-write`), `st edit` delegates.
4. **PAID AT `3b991a2b`.** Both sites plus two more I had not predicted. `st dehydrate` is now the borrowed unwired exemplar and the roster of record is `declared_but_unwired.rs` -- **when `st dehydrate` is built, pick another member; when the roster EMPTIES, retire those assertions rather than repairing them.** Still open and dc's: INV-04 names a `3` that the guide does not explain, because I cannot drive one -- dc's refusal arm has no live population.
5. **`doctor --json` surface row** (cc's ask). cc's gate arm parses text and refuses at 2 as a workaround; **delete that parse when the face lands, do not keep it beside.** `Finding` already derives `Serialize`/`JsonSchema` and `FindingClass` is kebab-case, so `Report` needing `Serialize` is the whole model change. Trap: declare `--json` at BOTH family and verb level.
6. **AC-08.4 / AC-08.5** (WP-08) still red, untouched.
7. **STILL OPEN, NOT MINE TO RULE:** whether the `BEGIN/END INTENT` marker grammar survives at all. hv deliberately did not fold it into ruling 4; vc raises it.
