# ic DOING, folded 2026-09-04 14:20Z

Archived verbatim from the live board at the localfold before a compact.

**IN FLIGHT: WP-17, TAKEN ON vc'S DIRECTION 2026-09-04 10:18Z -- AND MEASURED BEFORE TOUCHED, WHICH IS THE WHOLE LESSON OF W29 APPLIED ON THE SAME DAY I BANKED IT.**

**IT IS 17 OF 18 AC ROWS SATISFIED.** The `scope:` field still reads `XL` and the remaining work is ONE row. **A scope recorded when a WP was minted is a claim about the PLAN, and nothing re-reads it as the rows close** -- sibling of W26, and the reason I drove `intent ac list` before costing anything.

**THE ONE OPEN ROW IS `AC-17.6`, AND IT IS NOT MINE TO CLOSE ALONE.** It requires `intent edit <kind> <id>` AND `intent browse <kind> <id>` to reach ONE MODEL through ONE SERVICE. Driven against the shipped binary: `intent browse st 56` is `rc=2` with no dispatch arm in the crate; `intent edit st 56 --browser` refuses. **The serving half is `WP-08`, which is cc's**, and cc's own uncommitted comment says so in as many words. **Building it under WP-17 would be a second home for cc's criterion, so I have written NO CODE and will not until cc answers.** Asked them, with the one thing I could not resolve handed over rather than settled: `AC-08.9` reads `satisfied: yes` while cc's in-flight comment calls the same area unbuilt -- possibly two different things (the shell and `/op` DO ship; a per-entity page is a further thing), possibly the two-homes shape again. **NOT FILED -- it is cc's to rule.**

**A CORRECTION I OWED MYSELF AND ALMOST DID NOT MAKE:** `--browser` told me _needs a running daemon and none is running_ while `intent daemon status` answered `ok`. I was one step from filing it. **cc is fixing it in the working tree as I read it** -- my binary is the older build (embedded `6dac00f7`), so I was measuring a build the source had already left behind. **W6 in its live form: the binary names the commit it was built from, and currency is that against HEAD.**

**FILED, BOTH VERIFIED PAST THE DAEMON INGEST RATHER THAN ON THE `ok:` LINE (`0216`):** `0230` the residual the in-process drive does not reach (bytes -> `KeyEvent`, model -> cells), and `0231` the frame-boundary UX item. **`0231` is filed as UX, NOT as test infrastructure, on vc's ruling** -- filed as test infrastructure it would have died with A1, and the anti-flicker win stands on its own.

**A1: DROPPED 2026-09-04 10:18Z, vc VERIFIED THE REFUTATION INDEPENDENTLY AND SAID SO.** Kept below as the worked case.

**A1: TAKEN AND STOPPED IN ONE SESSION -- THE ANSWER IS THAT ITS PREMISE IS REFUTED (2026-09-04).** vc armed me to stop if paint sync went deep; it did not, and the stop is for a better reason. **A harness that drives an interactive selection ALREADY EXISTS, in-process, and is green.** `src/tui/run.rs` tests drive the caret key by key in BOTH vocabularies against concrete screen line numbers -- arrows, paging, clamp (16 green). `src/tui/app.rs` tests drive the FULL open-filter-move-commit path, including the argv that reaches the realiser, EXHAUSTIVELY over the command vocabulary rather than by sample (52 green). **This is also the harness now guarding hv's Up/Down inversion (W19/W20).** **THE RESIDUAL IS REAL BUT THIN AND IS NOT A HARNESS:** the in-process drive brackets `KeyEvent` -> `Screen` MODEL, so it does not reach crossterm's byte decode plus the `KeyEventKind::Press` filter (`run.rs:518`) or ratatui's render to actual cells. That is a smoke test; whether it is worth building is vc's call. **PAINT SYNC, ANSWERED ANYWAY FOR WHOEVER BUILDS IT:** a deterministic frame boundary IS available but needs an app opt-in -- crossterm 0.29 emits `ESC[?2026h`/`ESC[?2026l` via `Begin`/`EndSynchronizedUpdate` (READ AT SOURCE, `crossterm-0.29.0/src/terminal.rs:437` and `:490`, not inferred from type names), **ratatui 0.30.2's backend emits neither and our code emits neither**, so wrapping `run.rs:511`'s `term.draw(...)` is what would give a content-free frame delimiter you can count. Without it the choices are wall-clock quiescence, which house discipline forbids, or read-until-content-predicate, which couples every step to a string known in advance. **I TOUCHED NOTHING IN `tests/common/` AND RETRACTED THE ANNOUNCE TO ALL FOUR PEERS.**

**SUPERSEDED, KEPT ONE LINE: the plan was a composed PTY drive-and-observe helper, taken on vc's direction 2026-09-04 10:04Z.** Target is `native/rust/crates/intent-cli/tests/common/`, ANNOUNCED to all four peers first because it rebuilds every test binary in that crate. Additive by intent: a new module, not edits to helpers peers already depend on; if that proves impossible I announce again before moving an existing signature. **THE DELIVERABLE IS AN ANSWER ABOUT PAINT SYNC, NOT A HARNESS** -- knowing a full-screen frame landed before sending the next key. Type-ahead through the line discipline carries a line prompt and is not obviously enough for raw mode. **If it is deep I STOP AND REPORT** (vc's instruction, adopted): _we cannot observe paint deterministically_ is a real answer and is worth more early than a harness is late.

**HOLD STATE, 2026-09-04 09:59Z: hv RELEASED THE SESSION-LEVEL HOLD AND POINTED ME AT vc -- _wait for instructions from vc and then follow as advised_.** **THE RELEASE IS OF A POSTURE, NOT OF THE EIGHT HOLDS BELOW.** Their conditions are unchanged and none has been shown met; treating one instruction as clearing a list it never mentioned is W4 running in my own favour, which is the direction it is hardest to catch. **So: still nothing in flight, and the reason is now vc's sequencing rather than hv's hold.** **AND THE FIRST THING I DID WITH THE RELEASE WAS GET A FACT WRONG UPWARD (W28, mine, 09:59Z, corrected 10:0xZ).** I read vc's COMMITTED board as held-and-waiting, wrote the staleness bound down honestly, and then let the reading travel to hv as the headline anyway. `ListAgents` shows `intent-vc` live and BUSY. **The caveat was the load-bearing part and I demoted it to decoration** -- a bound that is stated but not acted on is not a bound, and the instrument that refuted it was cheaper than the one that formed the claim.

**Nothing in flight. This is a real state, not an empty section.** The eight items below are gated on cc, dc or hv, each with its condition; my SEQUENCING is now gated on vc, which is a different gate and is why the line above exists rather than a banner saying it supersedes this one.

**hv's GOVERNING DIRECTIVE STANDS AND IS NOT HISTORY: change any AC/AT needed, DO NOT yak-shave old guards or tests -- rewrite them to reality** (fail-forward; [[feedback_fail_forward_intent]]). Accepted design: `intent/st/ST0056/tui-redesign-proposal.md`; the ratified design doc is `intent/st/ST0056/tui-design.md`.
