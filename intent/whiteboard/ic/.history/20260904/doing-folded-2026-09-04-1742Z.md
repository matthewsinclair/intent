# ic DOING, folded 2026-09-04 17:42Z

**Archived because it was EXECUTED, not because it was old.** Verbatim pre-fold board: `wip-prefold-1741Z.md` (57,188 bytes, sha `3a6e3a74f6b672d7`, prettier-clean at source before the copy, byte-identity confirmed with `cmp`).

## DOING

**AGGRESSIVE LOCALFOLD 2026-09-04 15:48Z, ON hv's INSTRUCTION, BEFORE A COMPACT. STATUS STAYS `active` -- a fold is not a session ending (invariant 6).** Pre-fold board verbatim at `.history/20260904/wip-prefold-1548Z.md` (52,215 bytes, sha `7fa7d282a7ac2bd7`, prettier-clean at source BEFORE the copy so the copy is genuinely verbatim -- W25). The executed narrative is `.history/20260904/doing-folded-2026-09-04-1548Z.md`.

**FOLDED ON EXECUTION, NEVER ON DATE.** Everything cut is DONE AND SHIPPED. Every unexecuted ruling is still below, and gated work sits under Holds with the condition that releases it rather than in TODO looking like work I am declining to start.

**NOTHING IS IN FLIGHT AND NOTHING IS UNCOMMITTED.** The working tree carries none of my work; `git status` is the check, not this line.

**vc RULED THE FORM OP MINE (`authority: vc`, 2026-09-04) AND I AM BUILDING IT.** Ground: WP-17's own title says _CRUD through intentsvcs_. cc cleared the announce and handed me the `router()` deep-link fallback; vc ruled the token question -- URL fragment, and the page strips it with `history.replaceState` on load.

**`Op::Form` LANDED AT `969c488e`.** `Op::Form { url }` -> `Response::Form { entity, title, fields }`, answered by the same `form::triples` the in-process realiser calls. `Facade::entity_json` is the shared entity lookup that was left in `intent-cli` when `triples` moved down -- **the daemon could not reach it, so the August correction was half made**, and the missing half only became visible when something outside the CLI first needed a form. The `wp` arm resolves rather than inheriting `_ => None`.

**THE HELD HUNK LANDED AT `0f52cd5b` after dc's A1 (`8783243c`), applied clean, one hunk in the diff, checked.** What follows is why it was held, and the mechanism is dc's.

**~~ONE HUNK OF MINE IS UNCOMMITTED~~ -- DISCHARGED.** It was deliberate: `render.rs` collapsing `entity_json` onto the facade. **The file is THREE-WAY -- dc's A1 subagents refactor plus my hunk, nothing of cc's** -- and `--only` protects a file, not a hunk. dc lands first (142 insertions against my one), I follow. The committed tree compiles and is green without it.

### What landed today, as pointers rather than a retelling

- **`0234` -- the store grew without bound. FIXED, CLOSED, VERIFIED IN PRODUCTION.** The corrected mechanism lives in the issue body, not here. **My filing was wrong twice** and both corrections are recorded there: the `DELETE` was always fine, and my banked fix candidate `delete-all` is REFUSED by SQLite on a content-carrying table. Real causes: three disagreeing derivations of the prose index (now `ingest::sections_of`, one home) and FTS5 `DELETE` tombstoning rather than truncating (now the `rebuild` idiom). `WP-18` done.
- **The estate: 4,303 MB -> 182 MB.** Stores 1,837 -> 91, backups 2,466 -> 91. Repaired with the ORDINARY `sync --to-store` plus a vacuum -- no migration tooling was needed or written. Laksa `st list` 700 ms -> 40 ms.
- **The `explore` load indicator.** `tui/progress.rs` + `Borrowed::take_only`, wired into `explore()`, with a call-site witness on cc's finding. Driven under a pty: fires on a slow store, silent on every repaired project, `Esc` cancels without opening the TUI.
- **`0235` filed** -- a running `intentd` cannot say which build it is, and `daemon status` reads as though it answered that.

## Live, unfinished, not mine to close alone

**`WP-17` IS 17 OF 18 AND `AC-17.6` IS THE OPEN ROW.** It needs `intent browse <kind> <id>` and `intent edit --browser` to reach one model through one service. `browse` is rc=2 with no dispatch arm; `--browser` refuses. **vc ruled the ES renderer is WP-17's, ie MINE, and hv's own words are in `web.rs`: _it is ic's job to actually build the UX/UI for it._** **BLOCKER FOUND AND NOT YET FIXED: `intentsvcs::form::triples` is built, pure and shared, and there is NO FORM OP ON THE WIRE** -- ops are exhaustively `ThreadList`, `Graphql`, `Set`, `Registry`, `Subscribe`, `Shutdown`, and `Graphql` is reads-only. Design section 10a describes the daemon emitting `{entity, title, fields}` and nothing does. **A wire door comes before any renderer.**

**THE ROUTE QUESTION IS SETTLED: `intentsvcs/src/nav.rs` IS the ratified path contract** (`AC-17.7`, `AC-17.12`, vc 2026-08-30). `View::path()` gives `/`, `/{kind}`, `/{kind}/{id}`, `/{kind}/{id}/{field}`, `/settings`, `/help`. **Design section 9's example URL `/threads/ST0058?mode=edit` is wrong TWICE on that contract** -- pluralised, which `nav.rs` forbids by name, and a query the contract does not carry. **Recommendation is C (one axum `fallback` serving the same static shell), reversing my own earlier A.** Detail: `intent/whiteboard/ic/browse-proposal.md`.
