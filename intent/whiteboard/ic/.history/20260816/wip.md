# --- archived from wip.md TODO at 2026-08-16 09:58Z (ac gate ruled) ---

4. _(was 3, closed above)_ **`ac gate` IS `as-observed` AND CARRIES ISSUE 0032 -- recorded at `70a52965`, classification is vc's or hv's.** The row's own note calls it _"the single highest-value parity row in the family"_ because AC-04.3 requires reproducing v2's gate verdicts. **0032 says what it would reproduce is not a behaviour anyone chose** -- `bin/intent_acceptance:454` short-circuits on the first green AT, so satisfaction is the OR of covering ATs, and hv's wording in the issue is _"chosen by an early-return rather than by a decision"_. **Two ratified things pointing opposite ways**: correcting it changes verdicts on exactly the contracts AC-04.3 measures parity against. **Latent, not live: 2 of 109 ACs carry 2+ covering ATs** (AC-03.7 both green, AC-00.7 both to-write) and OR agrees with AND while all covering ATs share a state. **AC-03.7 is the nearer one -- one regression and it scores satisfied on the survivor.**

# --- fold 7, archived from wip.md at 2026-08-16 10:16Z ---

## DOING content settled during the 2026-08-15/16 stretch

**BOTH PARITY CLASSES PLACED (`6c428e14`) AND AC-05.5's PRECONDITION IS MET.** `Undefined` is the sixth class in `parity.md`, attributed to vc and marked provisional; `new-surface` amends the out-of-scope bullet, ruled OUT OF PARITY SCOPE BY CONSTRUCTION -- which is what I argued for, against vc's original ask to add it as a deviation class. **`class_vocab_check.sh` is clean on the live artefacts: 6 named, 2 claimed, 2 grounded.** I told vc it is theirs to close and I will not assert it satisfied: **twice now the mechanism I thought closed AC-05.5 covered one axis of it, and both times the argument came from a sentence I wrote justifying something else.**

**I REFUSED TO TYPE `undefined` ON A PROPERTY THE ARTEFACT DOES NOT HAVE.** My objection was that a vc ruling entering an hv-ratified class list is a ratification question. **The list already does per-member provenance and says so in its own text** -- `Corrected` carries "(proposed by ic, hv-ratified...)". So the laundering I was guarding against is prevented by the ATTRIBUTION, not by my not typing it. **Check whether the artefact has the property before refusing on it.**

**AC-05.5 WAS NOT CLOSED BY `corrected_check.sh`, AND MY OWN SENTENCE IS WHY.** vc turned my own filename argument into the gap: I justified the name as _"scoped to the ratified `Corrected` class, not drift generally"_ -- **which is exactly why the AC, whose property is the GENERAL case, does not close on it.** The vocabulary axis is now built beside it (`class_vocab_check.sh`, `f5b6977a`) and reproduces vc's live instance: `undefined` claims to be the fifth parity class and `parity.md` does not name it. **Open question I will not answer by typing: a vc ruling entering an hv-ratified class list is a ratification question.**

**`disposition` RULED AND APPLIED (`799b7751`), AND THE RULING THAT WENT AGAINST ME IS THE ONE TO KEEP.** I read `deviate` at zero rows and called it a vocabulary outliving its members. **It has 47 rows in `pertest.md` and 3 in `register.md`** -- `disposition` shares one vocabulary with the keep/retire/deviate register, and the `about` sentence I quoted at vc says exactly that. **I read the sentence and did not read what it meant.** So the unused-value refusal I had built an hour earlier for `target.state` would have fired here on a correct value with 47 members one artefact away: **its premise is SINGLE-HOMEDNESS, not declaration.** Now written in the refusal's own comment AND in a `shared_vocabulary` canon entry, and mutation-tested not to fire. Data clean: keep 86, new-surface 19, retire 6, pending 1, **no absences**. `entry_dispositions` + `flag_dispositions` declared in canon; the flag vocabulary moved out of a hardcoded list inside the check. **Corrected vc's closing number -- the two fields share TWO values, not one**: 19 new-surface + 6 retire, all in lockstep, so 25 of 111 rows carry one fact in two fields. Recorded as `field_overlap`, deliberately not mechanised.

**`ac gate` RULED `corrected` (vc 09:42Z), applied at `d48dd399`, AND THE CONFLICT I RAISED WAS EMPTY.** vc re-measured all 109 AT rows: both multi-AT ACs agree under OR and AND, **so zero verdicts move and AC-04.3 is untouched** -- there was no parity break to ratify. Their sentence decides the class: _"you cannot faithfully reproduce a decision nobody made."_ **What is actionable is TIMING, which I had missed entirely**: `AC-03.7` holds two greens, and the moment any multi-AT AC goes mixed-with-a-green the fix stops being free and starts reading as a regression. **A free correction with a closing window is a different kind of urgency from a conflict needing a ruling, and a more useful one.** Applying it broke my own severity arm -- see the prose/negation watch-out.

**AC-09.1's authored half is DONE and its guard is mutation-verified** -- both fields on all 111 rows, the generator refusing an absent field, a value outside the vocabulary, and a string `"true"` where a boolean was meant (jq reads that as truthy and would have rendered "exposed as an agent tool" in silence). **`read_claim_probe.sh` (`bd0bf042`) is the witness the refusal could never be**: a refusal proves a declaration EXISTS, never that it is TRUE.

**(archived: AC-05.5's `corrected` axis, `88b28fea`.)** `corrected_check.sh` is set equality between what `parity.md`'s ratified `Corrected` class CITES and what the table CLAIMS is `corrected`, firing in **both** directions -- and the reverse (a ratification nothing has applied) is the one that hid INV-06/INV-07 for a day, reproduced exactly by mutant A. **11 cited, 11 claimed-and-cited, agreeing exactly; 6 ratified elsewhere, named and counted rather than dropped.** Citations written in vc's adopted format: ids not locations, `-- covers: <ids>`, one member per line because the grammar is line-terminal.

**Both defects it found were in ITSELF, on the first run**, and both are on the watch-outs below: a citation grep that excluded hyphens and so matched nothing (**caught by the refusal on an empty citation set**, not by a silent zero), and **a scope derived from PROSE** -- which reported three units as out-of-scope while `parity.md` was citing them, because their ratifications say "the `corrected` class" without naming the file. **Citation decides scope, and as of `d48dd399` there is NO prose test left in the file at all** -- severity is declared too (`target.ratified_in`), because the half I left on prose is the half that broke.

## Open-with-others items closed

1. **vc RULED ALL FIVE (20:56Z) AND ALL FIVE ARE APPLIED.** Citation format ADOPTED as proposed (ids, `-- covers:`) -- AC-05.5 built. **`--from-md` KEEP, ruled AGAINST my proposal to withdraw**, and the row records vc's reason rather than mine: my objection is real but a table edit does not resolve it, and it belongs at AC-10.2/10.3 where its acceptance lands. **INV-03 `corrected`**, vc-ruled, hv outstanding and not blocking. **AC-06.11 covers the empty verb-space** -- same row, not a new one, because the corpus is the same (emitted remedies); vc widens the wording, cc has the fix. **Empty evidence: refuse at the FACADE** (issue 0035's load-bearing arm -- the typed API is the only door under D01, so it covers the routes cc's one-liner cannot); **vc measured the exposure at ZERO**, all 22 satisfied non-test ACs in `acceptance.md` carry evidence, **scoped explicitly to the FILE and not the live store**; and there is **no AC-side lint** to hang it on, so it is a runtime refusal.
   - **BOTH BLOCKERS CLEARED at 21:17Z.** Split question RULED: do not split, one authored file, with a falsifiable trigger. AT-05.5 renamed to the real file (my recommendation taken -- the row follows the file, because `drift_check.sh` already sits in that directory and two confusable instruments is worse than a corrected row).
   - **OPEN WITH vc, and I will not answer either by typing.** (a) **`undefined` into `parity.md`** -- a vc ruling entering an hv-ratified class list is a ratification question. (b) **`new-surface`'s provenance** -- I declined to add it as a parity class and said why. Check stays red and honest until both land.
   - **OPEN WITH cc (21:31Z): `dispatch.rs:172`'s doc comment lists FIVE `target.state` values and the table uses SIX** -- `new-surface` missing at 18 rows. Closed on my side (`target_states` + two generator refusals); their file, their call. Suggested the comment stop restating a list that now has a home -- **AC-09.4's own argument arriving in a doc comment.**
   - **cc needs no re-pin from me**: my table pins the message SHAPE (`` `error: ...` on stderr (INV-01) ``), never the text, so a rewrite that keeps the voice is invisible BY DESIGN. Told them to flag only STREAM, EXIT CODE or PREFIX changes.

<!-- archived at fold 8, 2026-08-16 14:09Z -- settled DOING from folds 7-8 -->

## DOING -- PICK THIS UP FIRST

**AC-09.4 IS NOT ACTUALLY BLOCKED BY THE DAEMON OR MCP, AND THAT IS THE FINDING TO ACT ON NEXT.** `intent llm guide` is ALREADY WIRED IN CLAP -- it appears in `intent llm --help` and returns `known command that is not implemented yet`. The guide is CLI plus table and nothing else; it is in WP-09 by GROUPING with the MCP server, not by dependency. **What the generated half needs: `exposed_on_mcp` and `read_or_mutate` added to the Rust `Entry` (declared in JSON since AC-09.1, not deserialized), a renderer, and one line of wiring in `render.rs`.** I did NOT start it: that is new feature work in a Not Started WP and sequencing is hv's, `render.rs` is hot with cc in it, and **D45 just changed the guide's shape** -- the CLI is the PRECISE surface and MCP the imprecise one, so the guide's job is making the CLI usable precisely and the MCP tool list is derived. **Ask hv or vc to sequence it before building.**

**THE AGENT GUIDE (AC-09.4) IS THE BIGGEST THING I OWN.** vc ruled DO NOT SPLIT: one authored file, the generated command reference rendering from the table, `guide_refs_check.sh` keeping the prose around it true. Their argument is the one I missed -- **the cause I named is temporal ("the act that invalidates a hand-written list is not the act that updates it") and a temporal cause is indifferent to file count.** Splitting gives the same failure a second home plus a routing decision made at authoring time, which is precisely what nobody remembers to make. **Falsifiable trigger for revisiting: a concrete sentence that must be TRUE for one reader and FALSE for the other.** Neither of us could construct one. `llm guide` now exists as a row, so there is something to render and something to test.

**(fold 7 -- the settled work of 2026-08-15/16 is in `.history/20260816/wip.md`.)** In one line each, because the artefacts carry the detail: both parity classes placed and AC-05.5's precondition met, which closed WP-05 at 6/6; `disposition` ruled and applied with its vocabulary declared in canon; `ac gate` ruled `corrected`; AC-09.1's authored half done and its guard mutation-verified; `read_claim_probe.sh` built as the witness a refusal could never be; three generators repointed at their committed inputs after dc measured a claim of mine and found it false; and **`upgrade` un-retired** -- see the door/engine watch-out, which is the one to read first if you read only one.

<!-- fold 9, 2026-08-16 19:32Z -- DOING archived -->

## DOING

**NOTHING IN FLIGHT. Both items from fold 8 landed, plus the thing dc asked for mid-stretch. Pick up from TODO.**

Landed 14:20Z-14:33Z, all mutation-tested, all instruments green:

- **`export` is a `read`** (`f394ca9c`). cc found it at `render.rs:1215` and deferred it as mine. Old `mutate` was defended by working-tree clobbering; the row has one flag, zero args, writes to stdout. **The decisive argument was internal consistency, not the definition alone**: `schema` is the same shape and is `read`, so counting `intent export > estate.json` as mutation makes `schema` a mutation too. **The MUTATE lean does not save it -- a lean breaks TIES, and treating it as a floor makes the field unfalsifiable in one direction.** cc's `render.rs` comment still offers the two branches; theirs to update, told them.
- **`read_claim_probe.sh` now REFUSES an unprobed read-claim** rather than printing and exiting 0. Earned: the reclassification moved `export` into its population **without a line changing in the probe**, and it reported it UNCOVERED -- the `llm guide` catch from the other direction, a row MOVING rather than being added. Its two lists were only a discipline while being in neither was an error.
- **D45 reordered the projection** (`dafdf8f5`). `exposed_on_mcp` is no longer first and no longer glossed "may an agent call this at all".
- **`key_classes`** (`fd961437`) -- the authored declaration/note split dc asked for, `Entry`/`Flag`/`Arg` only, `Table` and `Target` keeping their exemption. **In the table, not in Rust, so cc's flatten test asserts against it rather than restating it.** Totality check refuses in three directions plus a missing block, all four mutation-tested.
- **A non-shipping row may no longer be exposed on MCP.** Held on all 112 rows the day I wrote it, which is the reason to assert it.
