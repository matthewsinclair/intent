AT-00.11 (mode 1 of AC-00.11): of_n_closes_over_examined.sh drives whether an emitted `N of M` closes over what was EXAMINED, not the FILTER that selected it. GREEN RESTS PRIMARILY ON THE POSITIVE CONTROL, not the sweep.

POSITIVE CONTROL -- the instrument CATCHES the defect. Fresh re-drive 2026-09-01, prediction lodged BEFORE the run (scratchpad/at0011-control-PREDICTION-fresh.md, AC-00.10). at0011-control-driver.sh drove the DEFECTIVE canon_commit_check (f2a2675f) against the REPAIRED (8bb47e49) over 6 confirmed-overstated revisions (48d48d20 5a0d0e00 bf6654fb fd2e4067 69033e4e 92a51134) -> 6/6 OVERSTATED (+1 +1 +1 +8 +2 +1), DRIVEN 6 of 6 CLOSES, zero UNDERSTATED (P3 held: the differential is one-directional). This is the arm that catches the founding defect `EXAMINED 86 of 278` -- which Arms A and B MISS because it closes arithmetically -- still firing at the current estate. Prior art (2026-08-19), cited not restated: at0011-control-{PREDICTION,SCORE,sweep-120}; at0011-mutants-{PREDICTION,SCORE} (M1 impossible `2 of 1` -> IMPOSSIBLE, M2 `-1` -> NON-CLOSING, M3 STABLE, M4 DIVERGED, R1-R3 refusals).

CLEAN SWEEP at sha b4ab069e (NAMED, not "HEAD": a sweep figure is a claim about a revision and rots as HEAD moves). of_n_closes over the AT-00.11 population: DRIVEN 28 of 28 = 0 finding + 1 diverged + 1 stable + 26 unmeasured, CLOSES. SCORED 2 of 28. The 1 diverged is canon_commit_check (nested 0 of 279 vs flat 279 of 279) -- the REACH #6 confound (two trees are two revisions), a NOMINATION, never a finding.

THE 26 UNMEASURED, split by reason and NOT filtered away (an earlier read of mine truncated the EXCEPTIONS block through a display filter -- the display-limit-as-population-filter this very row exists to catch; caught by vc, re-driven whole): 10 ABSENT from one of the two trees; 16 emitted NO literal `N of M` in either tree.

FROZEN-REACH LIMIT (Arm C only). The two-tree differential is the ONLY arm that catches the defect while it still CLOSES; Arms A and B fire only once it has become absurd (eg `2 of 1`). But NESTED_REV cannot advance -- the nested layout exists only BEFORE the ST0057 WP-01 relocation and survives in history alone after it -- so Arm C reaches ONLY instruments predating the relocation (here ~11), and that set never grows, only shrinks as tools are deleted. Every parity instrument written after the relocation gets Arms A and B alone.

THE GATE-CANNOT-FAIL TRUTH, stated because this note is the only place a later reader will ever learn it (nothing re-checks a green): AC-00.11 is `computed` from this AT, so this green SATISFIES the criterion -- and the criterion reads as a GENERAL requirement (every instrument printing `N of M` derives M from what it examined) while the evidence MEASURED 2 instruments. No phrasing closes that gap; it is the gate-cannot-fail property landing on this specific row. This green means THE INSTRUMENT WORKS -- it catches (positive control), it closes over what it examined, and it distinguishes UNMEASURED from PASSED -- and never that every parity instrument is checked.

THE 16 NO-LITERAL-RATIO stay IN the population, reported unreachable-by-mode-1, deliberately not excluded (REACH #2: this driver reads only the literal `N of M`; a ratio assembled across statements is still an AC-00.11 subject, just not one an output-reader can parse). NOT narrowed out: redefining the population to "tools that print a literal ratio" would derive the population FROM THE FILTER, which is AC-00.11's own defect committed by the tool feeding the instrument that enforces it. NOT AT-00.12 territory either: mode 2 asks whether operands are derived, recorded or laundered -- a different question. OWED separately, measured rather than suspected, NOT part of this green: 16 path-shape instruments make ratio claims no current mode adjudicates -- worth a row eventually.

NOT VACUOUS: the sweep asserts its closure in its own output rather than leaving a reader to add up; UNMEASURED is never scored as PASSED; and the positive control proves the arms CAN go red (6/6 fired on a confirmed defective tool) before any clean result was banked. vc rulings 2026-09-01: criterion text unchanged, population not narrowed, this AT note carries the reach limit and the 2-measured truth.
