# ic -- archived 2026-08-17

## fold 10 -- what landed 2026-08-16

## DOING -- NOTHING BLOCKING. AC-09.4 CLOSED; PICK THE NEXT THING UP FRESH.

**AC-09.4 IS COMPLETE END TO END.** `intent llm guide` renders 952 lines from the table compiled into the binary -- I built the renderer (`2a654db3`), **cc wired it (`e8f2e444`) rather than leave it unreachable**, and the exemption they added to my `unmigrated_surface.rs` is correct and stays as written. No committed guide file, no hand-maintained list; completeness is structural, and the spec records the one thing it asked for that could not be built (there is no generator run at which to refuse, so the refusal is a test).

**`recoverability` LANDED at `9cd9a9ba` -- field, consumer and checks in ONE change, which was vc's condition and it held.** Declared on all 63 shipped mutations; `check_vocabularies` refuses a mutation lacking it, a read carrying it, and any value outside `recoverability_values`, at BINARY LOAD, naming the row.

**THE OUTCOME THAT JUSTIFIES THE WHOLE DETOUR: the guide renders `intent at green` as ONE-WAY.** vc measured 1,447 characters of authored contract destroyed by that verb (issue 0033) and **14,253 standing at risk across 34 rows**. **An agent now meets that at the POINT OF CALL, in the guide, rather than in an issue nobody reading a guide will open** -- and nobody had to remember to write a warning, because the field derives it.

**`acts_upon` was DISPROVED by the canary before it shipped, and that is the canary working.** vc's condition exists for exactly this and it spent itself on their own proposal. The probe is DELETED, not kept: it carried 63 hardcoded paths the table now owns.

**Two rulings of vc's are implemented as ARMS and the second one is the one I would not have built**: an undeclared disagreement refuses, and a STALE anomaly -- a note surviving after its disagreement is gone -- ALSO refuses. That is the half a known-exceptions list never has.

## fold 10 -- closed questions, archived from the live board

Closed at fold 9, recorded only because re-opening them from the wrong end is the expensive mistake:

1. **The D45 safety residue is RULED, not pending** -- see TODO 1. What I sent as an open question came back as a field with a condition attached, and the condition is the deliverable-shaping half.
2. **0039 clause 2 is CLOSED, by me, at 19:32Z: `surface_check.sh` does NOT want its own copy of the class check.** cc's Rust guard asks serde what it deserialized; **a shell script can only search text, and a text search has to know its needle** -- which is exactly why `surface_check.sh` was blind to `aliases`. Not an oversight in the script, a limit of the mechanism. One witness, in the place that cannot go stale independently of the thing it measures.
3. **`critic`'s no-language divergence is recorded** at `bcfeb135` as `target.wp07_owes`, `target.state` left at `pending-hv`. v2 exits 2 from its own arg parsing, v3 exits 1 from clap under INV-02, and **v2's 2 was already in `observed.exit` -- what was missing was the v3 side and the obligation.**
4. **The window flag question is CLOSED and my question had a defective premise.** **All six `todo` verbs regenerate `todo.md`**, so a window flag on any single row is a silent-revert generator. hv wants a PERSISTENT PREFERENCE; vc ruled the home is `intent/.config/config.json`, default 24h, read by the one render path all six verbs share. **No surface row changes; the row that changes is a config key.**

---- fold 12, archived 2026-08-17 09:35Z ----

## DOING -- MIRROR ARM MEASURED AND DELIBERATELY NOT SHIPPED; NEXT IS THE MCP TOOL LIST

**The mirror arm is DONE as a measurement and REFUSED as a check, and the refusal is the finding.** Exactly **1 of 107** shipped rows promises a capability that exists nowhere: `backup` says `... for fast local restore` and no family ships a `restore`. Discriminator is structural, not a suppression list -- **the FIRST WORD of a help string is the command's own action, verified 107/107 with zero exceptions** -- so `ac rescope` / `ac reinstate` / `lang remove` correctly fall out and `backup` alone survives.

**NOT SHIPPED because neither available detector is defensible.** The DERIVED one (`a help word that is a shipped verb elsewhere but not here`) is blind to `backup`, since `restore` is shipped nowhere and there is no row to match. The one that SEES it needs a vocabulary of inverse verbs I invented, whose misses (`recover`, `roll back`, `undelete`) are silent. **You cannot derive, from a file describing what the tool HAS, a check for a promise of what it DOES NOT HAVE** -- the register is closed over its contents and the promise points outside it. Shipping it would assert coverage I cannot defend. Measurement sent to vc 03:28Z as evidence for hv's scope call.

**AC-09.1 AUDITED, AND THE ONE REAL GAP IS RAISED WITH vc (03:33Z) RATHER THAN EDITED.** Four things checked, three of them clean negatives I would otherwise have "fixed" redundantly:

- **The declaration half is SOUND at both ends.** All 112 rows carry `exposed_on_mcp` + `read_or_mutate`; both are deliberately non-`serde(default)` with the reasoning documented, AND `MCP_UNDECLARED` in the generator refuses type-or-value violations over the full `[.families[].entries[], .new_surface[]]` population with the trap named in its own comment. **I went looking for a missing refusal and found it asserted twice, covering different attack paths** -- a hand-edited JSON is caught by serde at load, a `serde(default)` added later is caught by the generator.
- **`guide_refs_check.sh`'s retirement hole is ALREADY FIXED** (`RETIRED` + `is_retired()`, both call sites). My board's note predated the fix; building the derived retired-command arm would have been a Highlander violation of my own making. **Measured the class anyway: ZERO live rows name a retired command in user-facing text.**
- **All four `recoverability_anomaly` rows RENDER IN FULL into the committed view**, note and all. The findings are visible, not buried in JSON.
- **THE GAP: AC-09.1 specifies `schemars-derived parameter schemas` and says NOTHING about the tool's own description.** `at green`/`at red`/`at na`/`ext new` are `exposed_on_mcp: true` AND `one-way`. **My guide projects safety FIRST so a CLI agent meets 0033 at the point of call; an MCP agent on the current AC text sees a parameter schema and silence.** The identical destructive verb warns on one surface and not the other, and the silent one has less context. **Free to close now because the tier is UNBUILT; a change to shipped agent behaviour after WP-09.** Recommendation invents no policy -- it applies the D45 projection order the guide already proves.

**THEN: vc's 0044 REVERSE SWEEP HAS THE POPULATION ERROR, AND I CLOSED IT RATHER THAN ONLY REPORTING IT.** The sweep says _"309 probes, the whole declared surface (104 entries)"_ -- **104 is `.families[].entries[]` alone; declared is 112 and SHIPPED is 107.** Too narrow by the 8 `new_surface[]` rows (all shipped, four of them `one-way`) and too wide by the 5 retired ones that cannot answer. **104 against 107, opposite signs, three apart -- no count-based check would flinch, which is vc's own sharper formulation of this class turned on vc's own rig.** Ran the 6 ordinary rows myself (18 probes, their method, sandboxed): **TYPE-A and TYPE-B both zero, so their RESULT holds** -- and the six **independently reproduce 0044's claim**, spending exit `1` on five distinct conditions (missing arg / unexpected arg / no project / no such schema face / file-not-found). `daemon` + `mcp` stay excluded for a stated reason. Sent 03:36Z + 03:38Z.

## DOING -- NOTHING IN FLIGHT, NOTHING BLOCKED, NOTHING OWED OUTWARD

**Nine commits pushed to local this session, ending `eea52021`.** Verified: generator clean at 105 entries with every refusal arm silent, nine parity checks exit 0, **surface_check exit 0 at 108 paths / 7-of-7 invariants from a `git archive` EXTRACT**, and `cargo test -p intent-cli --test self_loop_population` exit 0 at 3/3 with all three mutation-proved.

**THE EXTRACT IS THE POINT, NOT A WORKAROUND.** The working tree would not compile for most of this session -- cc's `Outcome` sweep had `render.rs`, `facade.rs`, `model.rs` and three test files dirty at once, and I hit E0533 then E0425. Rather than wait or measure their tree, I built from `git archive` of my own commit. **That is the discipline dc and vc used ON me this morning, applied to my own work for the first time.**

**hv's SUITE IS LIVE AS THIS IS WRITTEN and the FORMAT leg will go red.** `cargo fmt --check` shows diffs only in cc's in-flight `render.rs` and `self_loop_voice.rs`; my own files are clean. **Same shape as the 11:38 run: not a regression, cc's tree.**

**AND I HELD THIS WRITE FOR A REASON THAT TURNED OUT NOT TO EXIST, WHICH IS THE DAY'S CLASS ONE MORE TIME.** I stopped editing because `whiteboard_protocol_3_guard.bats` reads a board -- **it reads a board in a `create_test_project` temp fixture, and so do the clock and header guards. None of the three touches a live node's file.** Right instinct, unchecked premise. **Caution on an unverified premise is still an unverified premise; it just fails quietly** -- and had I not gone and looked, this watch-out would have entered the canon asserting the guard reads live boards.

**AND THE CORRECTED VERSION IS NOT "THE LEG CANNOT SEE MY WRITES", WHICH IS TRUE ONLY OF BOARDS.** vc measured five files matching `intent/whiteboard` and said so; the wider spelling (`whiteboard|wip\.md|inbox\.`) matches THIRTEEN, and their conclusion survives it -- no test reads a live node's `wip.md` or `inbox.*.md`. **But four DO resolve through `$INTENT_HOME` / `$INTENT_PROJECT_ROOT` to real files**: the `in-whiteboard` SKILL.md (`whiteboard_protocol_3_guard:22,140,142` and `claude_with_intent:172`), both hook templates (`whiteboard_clock_guard:21`, `whiteboard_header_guard:29,228`), and `usage-rules.md` (`docs_completeness:203`). **Those are exactly the files someone edits while thinking about whiteboard protocol, which is when they are least likely to ask.** Both of us verified the negative we were asked about; neither asked what the leg actually reads.

---- fold 16 DOING, superseded at fold 17 ----

## DOING -- NOTHING IN FLIGHT, NOTHING BLOCKED, NOTHING OWED OUTWARD

**Five commits pushed since the compact, ending `abbcaff1`.** `c3a54446` (all 27 `no_op` values + 0053), `d56305cc` (0056), `1d37702b` (coverage tool), `29d6f250` (deleting it), `abbcaff1` (`wp rescope` measured + 0056 sharpened). Generator clean, `surface_check` exit 0, `self_loop_population` 3/3, `dispatch_ssot` 18/18.

**THE WHOLE SESSION IS ONE ARGUMENT FOR MEASURING RATHER THAN TRANSCRIBING, AND IT WAS PAID FOR THREE TIMES OVER.** cc sent 21 `target.no_op` values from a real commit with a real sha -- the cheapest possible handoff to accept. Driving the binary instead found: **two values flatly wrong** (`ac rescope`/`ac reinstate` refuse at exit 1, 0053); **two rows with no reachable self-loop at all** (`todo notdone`/`todo toggle` refuse before any state is read, a third shape the `unexamined` sentinel could not express); and **one token that appears in no canon file** (`at na` prints `n-a`), which unwound into 0056. **None of the three is visible from the verb's source, and all three would have entered the register as clean `ok:` lines.**

**0053 IS THE MAXIMAL FORM OF vc's CLASS AND THEY HAVE RECORDED IT AS SUCH: a value cannot carry its own subject.** cc reasoned from `AcState::entry(kind)` -- correct about what the SUCCESS arm hands the setter, on a path the self-loop never reaches. Within one node the derivation is still in reach; **across a handoff only the result crosses, and a wrong-subject result is indistinguishable from a right one precisely because the reasoning that produced it was sound.**

**I BUILT A SECOND INSTRUMENT FOR A QUESTION vc ALREADY OWNED AND DELETED IT ONE COMMIT LATER.** `prose_row_coverage.sh` reported 4 uncovered rows; vc's reported 2; **three of my four were false zeros.** My needle knew `run_intent` and `bin/intent` and not `"$INTENT_BIN"` or `"$INTENT"` -- and `claude start` is driven as `run env ... "$INTENT" claude start cc`, seven times, in a file named for it. **Deleted rather than repaired: two instruments for one question is the violation, and a fixed second one is still a second one.**

**AND THE BINARY CHANGED UNDER MY READING, WHICH IS THE PROVENANCE LESSON OF THE DAY.** I verified `git status` clean, built, and started measuring. A peer then edited `render.rs` in this shared checkout and something rebuilt the binary **at the path I was reading** -- two binaries, one path, same name, no record in either of which tree it came from, separated by an mtime 31 seconds apart. **31 seconds establishes ORDER and not CONTENT.** Everything was re-driven against a `git archive` extract and all 27 reproduced, so nothing shipped wrong -- but I could not have told anyone that from anything on disk.

## FOLD 18 -- the fold-17 DOING, superseded by `0f87fc2c`

Kept verbatim. The second paragraph is the one that went stale: it says ONE deliberate red, and closing TODO-3 made it two -- the corrected rows are now asserted against their ruling instead of being unasserted. The day's argument in the third paragraph is unchanged and now has a second instance.

**Eleven commits pushed since the compact, ending `23dab98e`.** All 27 `no_op` values measured both halves; issues **0053** (filed, and cc has FIXED it at `1721e4bb`) and **0056** filed; the literal rows made executable; the five voices ruled; four wrong `observed` values corrected; `arg_values_note` corrected 3 -> 5; two rows corrected from cc's measurements.

**THE RUST LEG WILL SHOW ONE FAILURE AND IT IS MINE, DELIBERATE, AND MUST STAY.** `literal_stdout_parity.rs::every_literal_as_observed_row_matches_the_v2_bytes_it_declares`, 3 red of 7. cc asked for it to stay red and vc agrees; cc attributed it in one command with `cargo test --workspace --no-fail-fast` (71 legs, 510 passed, 1 failed, named). **The three reds are the three parity breaks I ruled BUGS** -- `ac satisfy` dropping ` by evidence`, `at red` and `at na` dropping v2's `-> ` arrow. It goes green when cc fixes them, and **a green here before that would mean somebody rewrote a template from the binary**, which is how `at na` came to carry `n-a` in the column that records v2.

**THE DAY'S ARGUMENT IN ONE LINE: EXECUTION IS THE ONLY FORM THAT CAN BE WRONG OUT LOUD.** Nine rows now carry an executable declaration -- setup argv, invocation argv, v2's measured bytes -- and running it found **five** parity breaks where prose had recorded none. `at red`'s `observed` column was CORRECT the whole time and the divergence sat unrecorded beside an accurate record of what it should have been, because nothing had ever compared the column to the binary.

**AND THE REGISTER'S OWN GROUND TRUTH WAS WRONG AT 4 OF 8 ON THE ONLY SUBSET ANYONE CHECKED.** Three dropped a suffix; **`at na` carried `n-a` -- v3's token in the column that records v2, on an `as-observed` row -- so a check comparing the two would have found agreement and CERTIFIED 0056 as correct parity.** 50 of 93 rows with an `observed` block are still `evidence_class: read`. vc's framing, adopted: an uncovered row is unknown, **a wrongly-recorded row is confidently wrong in v3's favour**, and a wrong entry does not fail -- it silently redefines the target.

## FOLD 19 -- the fold-18 DOING, superseded by cc landing all five voices at `d14cd0b5`

Kept verbatim. Every red it describes is now green, VERIFIED rather than accepted: no `template` line changed between `0f87fc2c` and HEAD, and `native/`+`surface/` were byte-identical to `d14cd0b5` when the legs ran, so the binary is what moved and not the column that records v2.

## DOING -- TODO-3 CLOSED, TWO DELIBERATE REDS, NOTHING BLOCKED

**`0f87fc2c` -- a ratified row is held to its RULING.** A `corrected` row now carries both templates: `observed.stdout_exact` is v2's measured RECORD, `target.stdout_exact` is v3's ruled REQUIREMENT. The target carries no `setup`/`argv` and **inherits the observed invocation**, so the two describe one command and their difference is the ratified deviation written in bytes instead of prose. Two tests, because the reds mean different things: an as-observed miss is a PARITY BREAK (fix the binary), a corrected miss is an UNIMPLEMENTED RATIFICATION (build the ruled voice), and restoring v2's bytes would be the wrong fix for the second.

**REQUIRING THE TWO TEMPLATES TO DIFFER IS WHAT FORCES THE RIGHT FIXTURE, AND THAT WAS NOT WHY THE GUARD WAS WRITTEN.** `ac rescope` on a NON-TEST criterion is ruled to print `back in scope (unsatisfied)` -- byte-identical to v2. Declared there the row would assert as-observed behaviour under a `corrected` label and look strict doing it. The difference-check drives the declaration onto the test-backed criterion, the only invocation where the correction is observable at all. **A corrected row must be declared where the correction is VISIBLE**, which is the fixture rule from this morning arriving on the other axis.

**AND RATIFYING THOSE TWO ROWS THIS MORNING KILLED `corrected_check.sh` AT EXIT 2 FOR ALL TWENTY CORRECTED ROWS.** It reads `target.ratified_in`; I invented `voice_ruling` where eighteen of twenty rows already used the canonical `ratification`/`ratified_in` pair. A Highlander violation in my own SSOT, and the blast radius was the whole axis rather than the two rows I touched. Fixed; rc=0, 11 cited / 24 claimed / 13 elsewhere.

**MY OWN RULING'S REASON WAS WRONG AND IT EXCUSED v2.** It said v2 "had no `computed` state to name". v2 HAS the concept -- `bin/intent_acceptance:16` and the refusal at `:1084`. What v2 lacks is the branch at the call site: `:1259` prints `(unsatisfied)` unconditionally under a comment at `:1252` reasoning only about the non-test case. **So v2 announces `unsatisfied` about a criterion whose satisfaction it computes elsewhere** -- vc's class again. Same destination, load-bearing difference in the grounds: v2 is wrong here, not model-limited. Measured against a fixture v2's OWN `at lint` validated first.

## FOLD 20 (2026-08-17 15:00Z) -- archived DOING

## DOING -- A ROSTER THAT REFUSES, AND A CORRECTION SWEPT ONE FIELD FURTHER THAN IT WAS REPORTED

**TWO LANDED AND PUSHED: `67814555` (0059) and `0a7cc84c` (0061).** Upstream still frozen at `5765c5da`; v3 still not on PATH.

**0059 -- THE GATE RUNS ELEVEN GUARDS NOW, AND IT RAN ITSELF ON ITS OWN COMMIT.** Six wired. But the mechanical half was never the point: 0059 says in as many words **do not add them without the disposition field**, so the deliverable is `runner_roster_check.sh` -- a `gated`/`manual` disposition per `*_check.sh` with a required reason, which **enumerates the directory every run and REFUSES on any tool it has no row for.** Naming the remainder in prose is a claim that rots on the day a tool is added; a roster fails on that day instead. Nine gated, four manual, zero undeclared.

**THE POPULATION HAD ALREADY MOVED BEFORE THE REMEDY WAS WRITTEN, WHICH IS THE ARGUMENT FOR THE REFUSAL.** 0059 counted eleven instruments at `0f87fc2c`; there are **thirteen**. `ratified_in_check.sh` landed hours after the census and `runner_roster_check.sh` is new -- both unwired, both unnoticed by any artefact. **My own tenth class, arriving inside the fix for it**: a correct measurement of a population that has silently acquired members. So the check never compares against a remembered count.

**DECLARED AND INVOKED ARE MEASURED SEPARATELY**, because the guard-0 rot in `precommit`'s own header was precisely their disagreement -- named in the roster, implemented inline, `int hooks` reporting three guards as two. It asks `--list-guards` rather than re-grepping the source (263ms of its 782ms), because re-grepping the source is what rotted the thing it is checking.

**AND `int hooks` IMMEDIATELY PAID FOR ITSELF: `corrected_check.sh` and `stale_at_check.sh` had no `+x`.** They run fine under `bash <path>`, which is exactly why nobody saw it -- the tool a human consults to find out was the thing that was wrong. Same shape as the rot above, two files over.

**0061 -- vc REPORTED TWO FIELDS; FOUR WERE STALE.** `at na`'s `help` was the only authored site left in the estate where the wire spelling reached a human, and it survived **because** it is authored: nothing renders help from the enum, so no code fix could pass through it. Corrected, then verified by **rebuilding from a clean extract of HEAD with only that edit applied** -- a grep proves the file changed, not the shipped string.

**`target.no_op` RE-MEASURED, NOT RETYPED, and the provenance rule earned itself twice in one day.** Three files under `native/` were dirty under cc, so the worktree binary would have attributed unlanded work to HEAD. Built from a `git archive` extract of `67814555`: movement `ok: AT-01.2 -> n/a`, self-loop `ok: AT-01.2 already n/a`.

**THE FOURTH FIELD WAS NOBODY'S REPORT: `at red`'s `voice_ruling` still asked for an arrow `d14cd0b5` had already restored.** Found by measuring the siblings of the row that WAS reported. **`at green` and `at red`'s `no_op` were checked the same way and are CORRECT** -- three rows measured, one stale, no assumption applied to a family. The line held throughout: **`target.*` must describe HEAD, `observed.*` must describe v2 as measured**, so the `n-a` in `observed.notes` stays.

**vc's OPEN QUESTION IS ANSWERED, AND THE ANSWER IS "NOT AS SPECIFIED".** 44 sha citations across 29 rows, 12 distinct shas. The cheap gate (`git log <sha>..HEAD` non-empty) would flag **11 of 12 shas, ~28 of 29 rows, on its first run** -- 0059's own warning turned on the remedy. **The discriminator it lacks is in the field names**: `no_op` (25) records a MEASUREMENT and goes stale; `ratification`/`ratified_in`/`scope_ruling` (12) record a DECISION and 94-commits-behind is their CORRECT state. Same cut as `target` vs `observed`, one level finer. Offered to vc, not built.

## (previous) DOING -- THE INSTRUMENT WENT GREEN FOR THE RIGHT REASON, AND THAT WAS CHECKED

**`literal_stdout_parity.rs` IS 4 OF 4 GREEN AT `d14cd0b5`, AND THE GREEN IS ATTRIBUTABLE.** cc landed all five voices: `ac satisfy` has ` by evidence`, the AT family has its `-> ` arrow, `at na` prints `n/a`, and both undo verbs print `ok: <AC> back in scope (<landing state>)` with the state computed from kind.

**A GREEN HERE IS THE ALARM CONDITION UNLESS IT IS ATTRIBUTED, AND THIS FILE SAYS SO IN ITS OWN DOC COMMENT.** Two cheap checks ruled it out rather than one expensive one: **no `template` line changed between `0f87fc2c` and HEAD** (so nothing was rewritten from the binary, which is how `at na` got `n-a` in the first place), and `git status --porcelain -- native/ surface/` was EMPTY, so the worktree WAS `d14cd0b5` and no extract was needed. **The provenance discipline earns its keep by telling the clean case from the dirty one, not only by refusing the dirty one** -- three times today it forced an extract, and this time it licensed the worktree in one command.

**cc BUILT THE RATIFICATION RATHER THAN RESTORING IT, WHICH IS THE DISTINCTION THE TWO TEST LEGS EXIST FOR.** The landing state is read back from the facade after the call rather than written as a literal, so the movement line and the no-op line take their spelling from one place and cannot diverge. No literal can name `entry(kind)`.

**AND THE THREE-SPELLINGS FINDING WAS FOUR.** cc found `views.rs::test_line` rendering `AtStatus` with `enum_str`, so the next projection over any thread with a non-test AT would have rewritten every such row from `n/a` to `n-a` -- **the wire form written into GENERATED CANON, on authored files, in a spelling v2's own linter rejects at L1.** 23 `status: n/a` against zero `n-a` across the estate. The other three sites damage a line of output; this one damages a file. **Their fix was unwitnessed until they checked** -- reverting it left 72 legs green -- and `view_determinism.rs` now derives the expected spelling from `display()`, so the mutant reds their view leg and my parity leg together. Two instruments, two layers, one spelling.

**FOUR OF SIX TODO ITEMS CLOSED THIS SESSION**, all pushed: the corrected-row template field (`0f87fc2c`), the arm- and line-scoped rows measured with two ruled `corrected` (`795a8ccb`), hv's 0052 ruling recorded (`5798af98`), and the provenance grammar guard (`81c3978c`, `38152015`). **Filed 0058 and 0060.**

## FOLD 21 (2026-08-17 15:35Z) -- archived DOING + TODO + open-with-others

## DOING -- FIVE LANDED, AND THE TRACKER WENT AWAY UNDER THE REGISTER THAT CITES IT

**PUSHED THIS SESSION: `67814555` (0059 + the roster), `0a7cc84c` (0061), `8a8d6179` (mutation proofs + a live false positive), `fef54072` (two half-applied closes), `337d7d84` (hv's `agents template` remedy).** Upstream still frozen at `5765c5da`; v3 still not on PATH.

**THE GATE RUNS ELEVEN GUARDS AND `runner_roster_check.sh` REFUSES ON ANY `*_check.sh` IT HAS NO ROW FOR.** 0059 asked for the disposition field in as many words, so the deliverable was never the six invocations -- it is that the remainder cannot silently become "covered". Nine gated, four manual with measured reasons, zero undeclared. **dc reviewed it and asked for no changes**, including the one I offered to reverse (do NOT quieten `ratified_in_check.sh`).

**0059 COUNTED ELEVEN INSTRUMENTS AND THERE WERE THIRTEEN.** Two landed after the census, unwired, unnoticed by any artefact -- my own tenth class arriving inside the fix for it. That is why the check enumerates rather than remembers.

**MUTATION PROOFS FOR THE TWO I GATED, AND ONE WAS RED.** `stale_at_check.sh` read the first backtick anywhere on a row as the citation, so an UNCITED to-write row with a backticked NOTE was reported as `cites <path> -- the file EXISTS`, pointing the reader at a test the row never named. Now scoped to the row head, before the first `--`. **Measured exposure when fixed: 0 of 53 -- latent, not live.** Prompted by cc from the other end: nine captured v2 rows carry a note that itself contains `--`. `class_vocab_check.sh` fired all four paths clean, including the heading mutation that would otherwise report every correct class as ungrounded.

**hv RULED THE ISSUE TRACKER OUT OF EXISTENCE AND THEY WERE RIGHT.** _"Issues are (generally) for other, external users of Intent, using a released version. We can just fix things inline as we find them."_ Then: _"Just clear the tracker, findings live in commits."_ vc cleared all 21 at `9a9c7799` -- REMOVED, not closed, because closing would assert they were resolved. My classification carried it: **19 of 21 were ST0056-internal, and the honest count of external reports against a release was zero.**

**MY SHARE OF THAT WAS REAL. 0060 AND 0061 WERE BOTH FIXED AND LEFT OPEN.** cc corrected my account of 0060 and the correction makes it worse, not better: they fixed it AFTER I filed, at `ffcc8764`, **with a comment citing the issue number** -- so the code knew and the tracker did not, and only re-driving the binary could tell the two apart. Finder and fixer both awake, both done inside the hour, neither closing.

**AND THE CLEARING BROKE A CITATION FORM THE REGISTER DEPENDS ON -- MEASURED, NOT SUSPECTED.** 24 `target` fields cite an issue number; **THIRTEEN cite only an issue with no sha, five of them `ratified_in`**, the field whose entire job is provenance. `ratified_in_check.sh` still accepts `issue NNNN` as a valid external record, **so it reports those five as conforming while their record leads nowhere.** The instrument has been confidently wrong since `9a9c7799`. One row re-anchored; twelve and the checker are TODO 1.

**hv's `agents template` REMEDY: `list` reports what each directory actually offers, `show` serves both kinds.** No name `list` prints is a name `show` refuses, and nothing becomes undiscoverable to achieve it -- narrowing `list` would have traded a false claim for a silent omission. Class and remedy kept as separate fields, because the class was decidable long before the remedy was.

## TODO -- ONE IS URGENT AND MINE; THE REST ARE SMALL

1. **RE-ANCHOR THE TWELVE `target` FIELDS THAT CITE ONLY AN ISSUE, AND TIGHTEN `ratified_in_check.sh` -- MINE, AND IT IS THE URGENT ONE.** The tracker is gone, so `issue NNNN` no longer resolves and the checker still calls it a valid record. **Five of the twelve are `ratified_in` itself.** Fix the checker first (an issue ref alone stops being a record; a sha beside it is fine), then re-anchor each field on the commit that holds the content -- recoverable with `git show 9a9c7799^:intent/issues/OPEN/<n>/...`. **Fixing the rows first would leave the instrument still wrong and green.**
2. **MEASURE THE OTHER TWO LAYERS OF THE OLD 0035 CLAIM, WHEN NEXT IN THAT CODE.** I drove the CLI arm of `ac satisfy --evidence ""` and it refuses at exit 1; the claim said "at every layer" and the declaration and facade were not measured separately. vc's read and mine agree it is strong evidence and not proof. **Not worth a special trip.**
3. **`st start` REFUSES FROM `triage` WHILE `st new --start` COMPOSES PAST IT. hv's, not mine.** Measurement still holds. Still the likeliest first-command bug report in v3.
4. **THE REGISTER-LAG DISCRIMINATOR -- MEASURED, OFFERED, DELIBERATELY NOT BUILT.** The cheap gate flags ~28 of 29 rows. The cut is measurement-vs-decision and probably wants a `records:` key declared on the field rather than a heuristic over field names -- vc agrees a declaration beats a guess, same argument as the roster. Schema change to my SSOT, so it wants a ruling before code.
5. **STANDING: mutation proofs CO-LOCATED, as each file is next touched.** `class_vocab_check.sh` and `stale_at_check.sh` came off the list this session; `runner_roster_check.sh` shipped with two in it.

## Open with others -- LIVE ASKS ONLY

**All four inboxes cleared to the sentinel at fold 20.** Peer traffic is on the agent channel at hv's instruction, and the inbox files are now durability only. **NOTHING IS OWED TO ME AND I OWE NOTHING BLOCKING.**

- **dc, CLOSED OUT AND THE BEST EXCHANGE OF THE DAY.** They reviewed the wiring, asked for no changes, and **agreed with all three of my push-back invitations including the one I offered to reverse** (do NOT quieten `ratified_in_check.sh`). Then they measured declared-vs-invoked themselves, got **9 against 11 from a hand-built pattern**, and were one step from reporting the guard-0 rot in the file that fixes it -- calibrating against the real command corrected it. Their three fixes at `79ca7eca` include one worse than what I reported: **`int hooks` was CLONING AND BUILDING to ask what was wired**, because its `--list-guards` probe matched the words in a COMMENT in `cmd/prepush`. 0.8s now.
- **cc, ONE CORRECTION FROM THEM THAT I ACCEPT AND SHOULD CARRY.** I said 0060 was already fixed when I raised it; **the order was the reverse** -- they fixed it after, at `ffcc8764`. So it is not a stale list, it is finder and fixer both awake and neither closing, which is the sharper version. They also warned that nine captured v2 AT rows carry a note containing a further `--`; **that warning is what found the `stale_at_check.sh` false positive.**
- **vc, LEVEL.** They cleared the tracker at `9a9c7799` and reversed themselves on my point 4 (whoever fixes it closes it). My register-lag measurement is recorded as the resolution rather than a note.
- **hv, ONE ANSWERED AND ONE STANDING.** `agents template`'s remedy is RULED and recorded (`337d7d84`). `st start` from `triage` is still theirs (TODO 3).

## FOLD 22 (2026-08-17 15:47Z) -- archived DOING + TODO + open-with-others, at the pivot to WP-10

## DOING -- THE DANGLING CITATIONS ARE GONE, AND THE ROOT COMMAND HAS NO ROW

**PUSHED: `b865db12` (the checker), `4cad12b8` (the provisional bucket + the re-anchors), `bd5dc51e` (EXP-09 + vc's `ac gate` rulings).** Upstream still frozen; v3 still not on PATH.

**TODO 1 IS CLOSED AND THE ORDER MATTERED.** Checker first, rows second. `ratified_in` now reads: 26 declared, **14 conform, 0 hv, 1 PROVISIONAL, 0 dangling, 0 non-conforming, 11 sentinel.** The only field waiting on anyone is `ac gate`, waiting on hv.

**THE ISSUE ARM BECAME A LOOKUP RATHER THAN BEING REVOKED, AND ONLY MEASURING SHOWED WHY.** `CLOSED/` survives at 40 and `at green` cites issue 0015, which is still on disk. A blanket revoke would have accused a live citation -- the false-alarm direction, the expensive one, and a failure this file already records itself making once. The test is whether the NUMBER resolves, which self-maintains: prune `CLOSED/` and the rows citing it turn amber with no edit.

**MY 13-AND-5 WAS TWO POPULATIONS IN ONE SENTENCE.** 13 is the no-SHA count; the grammar accepts a FILE too, so **7 fields lacked any record and 5 of those were `ratified_in`**. Both true of different sets, reading as 5-of-13. The denominator rule from `835bf848`, one hour old. Corrected in the register and in the commit.

**vc HELD MY REMEDY AND WAS RIGHT.** `ac gate` is `provisional pending hv`; a sha in `ratified_in` asserts a ratification happened, so the fix the report told me to make would have certified an unmade ruling -- **doing deliberately what the `AUTHORITY_HV` anchor had just stopped by accident, an hour apart, in the same field.** Third bucket built on their ruling, tested AHEAD of conformance so a provisional row with a good sha still does not read as conforming.

**AND vc TRACED AN hv ATTRIBUTION TO NOTHING, AGAINST THEMSELVES.** `why_corrected_and_not_as_observed` said "hv's own wording on issue 0032". Under it: the sentence is in the FILING text, the issue carries `reporter: matts`, vc's ruling quoted it as hv's, the field repeated it. Verified at source before I wrote the strike -- the diff introducing the sentence is `e23a8453`, a node's own commit. **It matters most because that row awaits an hv ruling, so the field manufactured the adjudicator's agreement on the question they have not been asked.**

**EXP-09, FOUND BY ANSWERING A QUESTION OF cc's.** They asked whether build metadata extends `intent --version --verbose` or takes a new row. **Neither: no entry declares the root intrinsics at all.** Measured against the built binary -- `--version` short-circuits before validation and **swallows everything after it at exit 0** (`--version --zzz`, `--version NOSUCHTHING`, `-V --zzz`), while `version --verbose` correctly exits 1. INV-02 unmet on the root command, and the `version` row's own `behaviour` asserts the opposite. Third instance after `info`'s catch-all slot and `st show`'s positional, and this one is the command every user types first.

## TODO -- HELD. hv HAS CALLED US OFF TRACK AND A RE-PRIORITISATION IS COMING

**0. THE MORATORIUM IS THE FIRST ITEM AND IT LANDS ON ME HARDEST.** vc gave hv a plain-language review and did not defend us: **210 commits today touched no code, 33 touched Rust; nothing moved from Not Started to In Progress; and the thing hv asked for -- Intent running on Intent3 -- needs WP-10, the migrator, which nobody is on and nobody ever has been.** vc's diagnosis is _we have built a very good immune system and it has started consuming the host_, and **my lane is the clearest exhibit**: every finding today generated a peer message, a class entry, a re-anchor and an instrument with eight mutation proofs. Correct for a shipping product, inverted for a rewrite that is half-built. **NOTHING BELOW IS OPENED UNTIL hv RULES.** Findings go in the commit that fixes them -- hv's own rule, which I have been decorating.

1. **THE ONE THING THAT IS ON THE SHIP PATH: declare `--verbose` on the `version` row when cc confirms with dc.** cc found the way through EXP-09 -- **`version --verbose` already exits 1**, so AC-11.5 lands on the subcommand, not on the swallowing root intrinsic, and it is a flag declaration on a row that already exists. **One row, no new class, no instrument.** Waiting only on cc confirming the spelling satisfies dc's provenance contract.
2. **HELD -- EXP-09's remainder.** (a) A home for the root intrinsics; (b) **`nothing_reaches_the_surface_that_is_not_in_the_table` passes with `-V` on the surface and undeclared, so its population excludes root flags** -- one line in the guard when it is touched, not a project; (c) the swallow-is-defect-or-intrinsic RULING, which is no longer on anyone's critical path now that cc is not building on it.
3. **HELD -- one schema change, two fields, vc has RULED YES and asked me not to build it yet.** `records:` on the field, and a declared `provisional` rather than a token in prose. Their reasoning is my own `AUTHORITY_HV` fix pointed at myself: a token in prose is substring-anywhere, and **a declared field cannot be negated into invisibility.** _Delete the word, do not negate it_ is the interim rule -- and it is a rule, which today established is not a control.
4. **HELD -- the other two layers of the old 0035 claim**, when next in that code. Not worth a special trip and never was.
5. **`st start` REFUSES FROM `triage` WHILE `st new --start` COMPOSES PAST IT. hv's, not mine.** Still the likeliest first-command bug report in v3.
6. **STANDING: mutation proofs CO-LOCATED, as each file is next touched** -- and **proportionate to the file's blast radius**, which is the correction the moratorium makes to this item. Eight on one register checker was more than the artefact was worth.

## Open with others -- LIVE ASKS ONLY

**Inboxes are durability only; peer traffic is on the agent channel at hv's instruction.**

- **vc, LEVEL AND THE BEST EXCHANGE OF THE DAY.** They held my `ac gate` remedy before I took it, then answered both follow-ups and struck their own attribution. Owed to them: nothing blocking. Owed by them: the schema ruling in TODO 2.
- **cc, AND THEY SOLVED THE THING I HANDED THEM.** I answered AC-11.5 with EXP-09 and three entangled blockers; **cc re-measured against their own binary rather than taking my message for the artefact, and found `version --verbose` exits 1** -- so AC-11.5 goes on the subcommand, off the swallowing root, and my three blockers come off their critical path. They are confirming the spelling with dc before I declare the flag. **Owed by me: one row, the moment they confirm.** AT-05.7 / `inert_arg_check.sh` is theirs, and they have already noted EXP-09 is a member it cannot reach, so the root intrinsics must be NAMED as excluded rather than silently outside its denominator.
- **hv, TWO STANDING AND ONE INCOMING.** `ac gate`'s ratification is provisional pending them; `st start` from `triage` is still theirs; and **the re-prioritisation is theirs to make** -- vc has recommended naming the hoist as the milestone with a moratorium on new classes and criteria until it lands.

## FOLD 23 (2026-08-17 17:37Z) -- WP-10 Phase B built; the accretion found, fixed by cc, verified by me

## DOING -- THE ACCRETION IS CLOSED AND THE CUTOVER GATE IS MEASURED PASSING

**hv gated the cutover on it: a second migration over an INTERRUPTED estate must reach the same end state as a clean one. It does.** Measured against committed HEAD `85ef4a72` with `git status --porcelain native/ schema/` returning **0 modified**, so no peer's in-flight work is in the reading -- the discipline I got wrong twice earlier today and right here.

```
CLEAN: 93 files written, 144-file tree
plain re-run 2 / 3        wrote 93, 10 already  ->  IDENTICAL
kill early (1st canon)    123 at kill; re-run 93,  1 already -> IDENTICAL
kill mid   (last canon)   132 at kill; re-run 93, 10 already -> IDENTICAL
kill late  (into views)   142 at kill; re-run 93, 10 already -> IDENTICAL
```

**Real `kill(SIGKILL)` on a child mid-`WriteSet::commit`, confirmed by `signal() == Some(9)`, not a simulated partial write.** Two attempts: killing on a GUESSED DELAY never worked -- 0 files at 12.8ms, because harness process startup outlasts every delay -- and killing on a SENTINEL FILE APPEARING lands it every time. No stray `.intent-tmp` survived any kill.

**AND THE REALISTIC SHAPE, WHICH MY FIRST FIXTURE COULD NOT SEE.** Mine put threads at the flat path; the real estate has 55 of 56 in `COMPLETED/`, and cc warned that is exactly what collides on 0011:

```
5 threads, all in COMPLETED/ -- run 1 wrote 55, 0 already; run 2 wrote 55, 5 already -> IDENTICAL
after: 5 thread dirs at the flat path, 5 still in COMPLETED/
```

No duplicate-id block, so cc's dedup holds. **And that last line is the bucket finding measured from the other end: the estate is doubled after a clean migration and the migrator is content.**

**WHAT IT DOES NOT ESTABLISH, RECORDED BEFORE ANYONE QUOTES IT.** Five and ten threads from ONE repo, all COMPLETED -- not the 56-thread canary, not the fleet. **And the verdict rests on my own throwaway tree diff, not on dc's `same_end_state_check.sh`**, which is me doing what I told dc three hours ago I would not do. Nothing landed, the scratch is deleted, but the gate has not been run under the gate's own tool and I am not reporting it as though it had.

**MY OWN MESS TODAY, BOTH OWNED:** a `zz_ic_*.rs` survived a 7-minute timeout in the shared `tests/` dir because my cleanup never ran -- my poll loop walked the whole tree 400k times, my bug, and it cost the sweep. Removed. And `74c4b357` **broke HEAD**: I committed the consumer of `Scan.already_migrated` while cc held the producer, both worktrees green because the tree held both halves. cc's `7628a02b` fixed it. **cc's generalisation is the durable one: every green here is a claim about the union of five people's uncommitted work.**

**LANDED AND PUSHED TODAY:** `ca71bd61` roster reads the index; `96d2bd4c` `migrate.rs`; `e591c9c4` `assemble` private; `94ef68b7` WP zero-padded path; `e0c813a8` `Blocked`'s remedy proof; `cac74720` **the residue check reads the index too -- the same defect in the sibling tool, which I fixed this morning in one and did not transfer**; `74c4b357` `already_migrated` through the join.

## TODO -- THE RE-RUN CONTRACT IS THE LANE

1. **THE RE-RUN CONTRACT IN `migrate.rs`, WAITING ON cc's `legacy.rs` HALF.** What `plan()` does over a partially-migrated estate. If the canon-wins design lands as agreed it should be a no-op on my side -- **and that is a claim to TEST, not to assert**, because "no change needed" is the shape that ships unverified.
2. **THE INTERRUPTION TEST, AND IT MUST BE A REAL KILL** (cc's condition, accepted): run, `SIGKILL` between writes, re-run, and the twice-run tree must equal a clean single run. Reading the code establishes nothing here -- the accretion was invisible to three careful readers and took one execution.
3. **HOLD BOTH TESTS UNTIL cc LANDS.** They are red until then, and I have landed red once today already for a reason that does not apply twice.
4. **DELETE THE COLLISION PRE-SCAN when `WriteSet::add` becomes fallible.** Still cc's, still recorded in the variant's doc.
5. **THE EVENT LOG QUESTION IS STILL UNASKED AND STILL MINE.** Deliberately not sent -- it blocks nothing today, and the moratorium is about not generating questions that block nothing.

## Open with others -- LIVE ASKS ONLY

- **cc -- owed to me: `export.rs` (minutes), then the sections fix.** Owed by me: nothing. Boundary holding well; they took all four `Blocked` variants and moved their guard on my placement argument, I took their `BTreeMap` correction and their `issue_add` framing (the door is a property of the ACT, not of the entity).
- **vc -- on the tools, and we are working the same seam from both ends.** They carry the `organize` warrant to hv. Their `conservation_check.sh` now predicates on reachability rather than presence, which is the correction that came out of Finding 1.
- **dc -- two-ended migrations.** Their AC-10.4 half and my Finding 2 are the same axis in opposite directions: **a record whose INTERIOR is dropped (mine) and a record that survives byte-for-byte whose REFERENTS stop resolving (theirs).** A conservation check built for either is silent about the other. vc has it.
- **hv -- FOUR, all via vc's batch or held for conversation.** The `organize` warrant; contract ownership; `ac gate`'s ratification still provisional; `st start` from `triage`.

## FOLD 24 (2026-08-17 18:47Z) -- the gate ran on a commit and PASSED on files

## DOING -- THE GATE RAN ON A COMMIT, AND THE ONE DIFFERENCE WAS A PREDICATE PROBLEM

**THE RUN, EVERY INPUT A REVISION.** Binary from HEAD `4770b6d9`, cc's real `intent upgrade` (`8770cea3`), dc's comparator `11f66894`, vc's pinned canary `42fb5269`. Real `SIGKILL`, not a simulated partial write.

```
arm A      wrote 295 files (1077 -> 1372)
arm B      SIGKILL at 293 of 295   ("Killed: 9", exit 137 asserted)
re-run     exit 0
clean-only 0   rerun-only 0   differing 1   ->  intent/.cache/intent.db
```

**1371 OF 1371 NON-STORE PATHS IDENTICAL after a real kill at 99% depth.** Repeated at a second depth (263 of 295), same result.

**I RAN THE TWO-CLEAN-RUNS CONTROL BEFORE THE GATE AND IT IS THE ONLY REASON THE EXIT 1 DID NOT READ AS A RECOVERY FAILURE.** Two migrations, no kill anywhere, also differ in `intent.db`. **The verdict was unreachable by construction** and nothing in the output said so. vc's framing, taken: had the kill run first, that exit 1 would have been believed by me and by everyone I told.

**THEN I GOT THE DIAGNOSIS WRONG AND dc CAUGHT IT.** I argued the store should leave the gate's subject, on two grounds, **both VOID in canon**: D29's _"a path git can never commit can never be canon"_ is marked VOID at `design.md:243`, and _"`rm intent.db` is safe"_ is on D01's do-not-cite list word for word. **D01 is REVERSED -- the DB is the SSOT and the FILES are re-creatable** -- so excluding the store would have dropped the one artefact the model calls truth. I verified both by reading the file rather than accepting the report.

**dc's `.dump` IDEA FOUND THE REAL ANSWER AND IT IS NOT PAGE LAYOUT.** Measured:

```
bytes                        DIFFER
sqlite3 .dump                DIFFER          <- so not a container artefact
.dump, ISO8601 ms normalised IDENTICAL       <- 827 lines, ZERO residual
created_at / updated_at      DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ'))
705 rows carry a stamp of when the migration ran
```

**The SSOT records wall-clock at insert, so two runs of a PERFECT migrator can never produce the same database** -- permanently, by construction. Not a subject to be narrowed: **a subject with the wrong predicate.** dc reproduced every number from a different harness (same byte 4796, same 705) and landed it at `884fd97f`, keyed on the file's magic bytes rather than its path.

**AND IT IS MY OWN PARKED QUESTION ARRIVING WITH TEETH.** Item 4 on this board all day -- _migrated threads restore an `st.new` carrying the authored date; `Envelope.ts` is ms-precision; `created` is a DATE_ -- filed as blocking nothing. **It blocked the gate**, and three of us hunted the symptom from outside for an afternoon while the cause sat on my board. dc's read of the tell is the durable part: **I could not say what would happen if the answer went either way**, which is a better test than "does this block anything".

**dc ASKED THE QUESTION THAT COULD UNPROVE THEIR OWN FIX AND IT HAS AN ANSWER.** A field that SHOULD hold an authored timestamp and instead holds the run time is swallowed: both runs substitute identically, both normalise, the gate goes green. General form -- **the normaliser cannot tell "correctly a machine stamp" from "should have been authored and was overwritten".** It does not unprove the fix: a two-run comparison cannot catch ANY deterministic defect, and this widens that pre-existing blind spot by exactly one bounded class. The right instrument is estate-versus-canon, which is one tree, and I am taking it.

**MY OWN RIG HAD A DEFECT OF THE SAME SHAPE AND I MEASURED IT ON MYSELF.** I replaced the `find`-based count with a single `test -e` for speed **and left a `date +%s` fork in the same loop** -- milliseconds, in a window where a file lands every 0.25ms. `--fraction 25` and `--fraction 90` both killed at 293. `$SECONDS` moved it to 263. **And the header claimed a control the rig does not have**: `--fraction` selects by MTIME order, but `commit()` temp-and-renames, so mtime is when the TEMP was written and `test -e` sees the rename -- the percentile lands late and approximately. Corrected in the file rather than reworded, because the claim had already gone out in a commit message.

## TODO -- ONE RE-RUN AND ONE TEST

1. **RE-RUN AGAINST `884fd97f` FOR A BARE VERDICT.** dc's content comparison should turn the single store difference into a match, and the result into exit 0 or a real finding with nothing to interpret. **Blocked only on the parity tools being clean** -- `same_end_state_check.sh` is dirty again as I write this. cc's floor fix has landed and `native/rust/` is clean.
2. **THE `intentsvcs` STORE TEST, cc's offer, taken.** `intent-cli` may never depend on rusqlite (D06, guarded by `dep_graph_guard.rs`) and shelling to `sqlite3` re-introduces the dependency canon removed (`design.md:290` -- Intent bundles SQLite precisely so it does not need the machine's binary). So the store comparison belongs in `intentsvcs`, which owns the store and may use rusqlite: compare row content directly, no shell, no regex over text. **Carries my parked event-log question with it, since it is the same measurement from the same side.**
3. **BACK cc's ARCHIVE-GROWTH GUARD and vc's CANON-HYGIENE ASK.** Mine is clean (checked by running the command, not by reasoning about my fold script).

## Open with others -- LIVE ASKS ONLY

- **dc -- owed to me: parity tools clean for one run.** They landed the content comparison at `884fd97f`, canaried three ways, and **the load-bearing arm is the one they nearly skipped**: a planted content change still caught. Checking what a filter neutralises proves nothing; checking what it must still catch is the whole thing. They also detect SQLite by magic bytes rather than path -- my own derive-don't-name advice applied one layer below where I had applied it.
- **cc -- owed to me: nothing.** Door at `8770cea3`, floor fix landed. **They cited D36 as ground for treating the store as disposable and D36 exists to forbid exactly that**; D34 (per-machine, never committed) carries it alone and always did. They found the void D29 reasoning at `sync.rs:132` four days ago and then rested a doc comment on the adjacent dead ruling -- **finding a struck premise once buys no immunity to the next one**, their words.
- **vc -- nothing owed either way.** Lamplight BLOCKED, both classes DECLARED, **5613 of 5613 byte-identical after the refusal** -- AC-10.2's atomicity turned from my argument-from-a-type-signature into a measurement, by an instrument built for something else. They traced the void-premise class to their own memory file still serving the pre-reversal model verbatim from a stale record.
- **hv -- bucket relocation (via vc); contract ownership; `ac gate` provisional.** New from today, vc holding: **how canon records a REPLACED derivation.** A struck premise beside a live conclusion has now caught three of five nodes in three places, and none of us was careless -- all three went looking for a citation first.

---

# FOLD 25 -- 2026-08-17 20:33Z

## DOING -- THE GATE I PASSED YESTERDAY CERTIFIED A DEAD ESTATE, AND THAT IS THE SESSION

**`intent upgrade` EXITS 0 AND LEAVES A PROJECT THE TOOL WILL NEITHER READ NOR WRITE.** Measured on the gate's own kept `a-clean` tree with a HEAD `53057087` extract build, then on Baize, then from scratch:

```
rc=1   st list / todo / export --format json / search / ac list / wp list / st new
rc=0   info      -- the only verb that answers, and it reads no project state
```

**THE REMEDY THE ERROR PRINTS IS A CLOSED LOOP.** A second `intent upgrade` exits 0, reports `311 file(s) written` and `ok: this project is now Intent v3.0.0-dev`, and the estate still refuses. A command that reports success, writes 311 files, and changes nothing about the operator's situation -- recommended by the error it does not clear. (It also narrates `a previous run of this command was interrupted` on a tree that completed cleanly: a true observation with an invented cause, and arm B prints the same sentence where it is correct.)

**MECHANISM.** v2 buckets a thread by status FROM CREATION, the migration writes canon to the flat `intent/st/<ID>/` and correctly leaves the v2 originals alone, and `Project::legacy_thread_ids` then counts those same originals as unmigrated -- it asks whether a `thread.json` sits BESIDE each `info.md` rather than whether the thread's id has canon anywhere. cc's note is the one to keep: the one-level descent was a CORRECT fix for a real hazard, and the migrator's ordinary output now puts every project into exactly the shape it was built to catch.

**CONFIRMED BY BOTH PEERS ON THEIR OWN SUBJECTS, AND vc's NUMBER IS THE HEADLINE: bucketed-only = 0 on utilz, baize and this repo.** Not one thread named `carrying v2 canon this binary cannot read` is unmigrated; every one has a `thread.json` written by the run being refused. **100% false positives, fleet-wide.**

**MY MINIMAL REPRO IS FOUR COMMANDS AND IT CORRECTED THE POPULATION.** `intent init` / `st new` / `upgrade` / `st list` -- 6 files, N=1, no corpus, no capture, no extract build. The empty project is the positive control: 0 buckets, migrates, reads clean. So the population is **not** "every project that has completed a steel thread" (cc's first reading, and mine when I repeated it) but **every v2 project with any thread in any status** -- the only unaffected estate is one nobody has used. vc led the hv escalation with it.

**AND I NEARLY SHIPPED THE OPPOSITE FINDING.** My bucket-free control returned exit 0 with a rendering table, and had migrated **1 thread instead of 56** because the buckets ARE the canon pre-migration. A green produced by an emptied subject; the naive reading of it is advice to delete 55 threads. **Caught only by checking what the run MIGRATED rather than what it RETURNED.** vc took it to hv as mine. Their correction completes it: deleting buckets AFTER migration leaves every thread readable, so the flat canon is complete and the two experiments differ by instant -- **delete before, lose the threads; delete after, lose the 192 STRANDED files.**

**THE STANDING ITEM IS DONE -- `38d5cda7`. THE SUBJECT IS A CLONED REVISION AND A DIRTY WORKTREE NO LONGER MATTERS.** `--rev` (default HEAD) is cloned into the workdir and built there, so the subject is a named commit by construction rather than by inspection. **The dirty-tree refusal is GONE**: right about the hazard, wrong about the remedy -- it made the gate unrunnable exactly when the estate was busiest, fired four times in one day, and I routed around it by hand-building extracts four times in one session. **A guard routed around that often is telling you where it should have been.** **dc caught a factual error in the design BEFORE it was built**: `git archive` leaves no `.git`, so cc's embed would read `unknown` every run and the provenance cross-check would return one answer regardless of input -- **I had inferred the mechanism from the OUTPUT of a worktree build.** It is a clone. Instruments come from a revision too (`--instruments-rev`, defaulting to the subject's, both named when they differ). **And I hit one of dc's own hazard class on the way, in the false-green direction**: `estate_corpus.sh` derives ROOT from its own location, so from a clone lamplight/utilz/baize resolve `no-repo` -- **but CANARY RESOLVES `here` EITHER WAY**, because canary IS this repo and a full clone carries its pin, so the default member could have gone green with the bug latent for every other one. dc's form: **when a fixture passes, ask whether it could have FAILED.** The durable half is the line it forced: **an instrument's LOGIC belongs to the pinned revision; its view of where repositories live on this machine does not.** Eight controls, and the one that matters is the gate green while the worktree was dirty with three files across two nodes.

**AND NOBODY WHO MIGRATED BEFORE THE FIX NEEDS TO RE-MIGRATE** -- measured on a Baize tree written at 19:02Z by the PRE-FIX binary and never touched since: same 25 `thread.json`, pre-fix binary refuses at rc=1, post-fix names all 25. **Same bytes, different reader**, so the estates written this morning were always correct and only the reader was wrong. vc's isolation (re-read, never re-migrate) answers that and my own method -- re-capture and re-migrate -- structurally could not. Getting there I hit **zsh word-splitting for the FOURTH time today**, and this instance returned `rc=126, threads named=0` on BOTH binaries, which reads as "the fix does not work on pre-fix trees": a false finding, in the expensive direction, distinguished from a real one only by the `126`.

**LANDED: `d487d30f` (the two arms) and `1898c22d` (the bound).**

- **LIVENESS** -- probe the migrated arm A with a read verb before the sentinel. **Refuses at exit 2, not exit 1, and the distinction is load-bearing**: exit 1 asserts non-idempotence, which this does not support -- the interruption property may hold on an unreadable estate, and yesterday it did. The refusal names the current cause and says to trust the output over the note when it goes stale.
- **STORE** -- closes dc's `1 store(s) NOT JUDGED BY THIS TOOL` with no `sqlite3` and no rusqlite in the CLI. `intent export --format json` reads the STORE (`facade.rs:1146`), not the files. Byte-deterministic across two independent migrations: 225553 bytes. **Bounded after dc's question to `the store as export sees it`** -- `file_index`, `doc_sections`, `snapshots` are named as out, all empty today, which is why the bound is written now.
- **FOUR CONTROLS, BOTH DIRECTIONS EACH**, because an arm seen in one state cannot be told from a hard-coded one: real `st list` refuses at exit 2 before the kill dance; `READ_CMD=true` passes through; an agreeing store read reports IDENTICAL at exit 0; a disagreeing one reports DIFFERENT at exit 1 **and overrides a files-identical verdict**.

---

## FOLD 26 -- the Watch-outs section as it stood at 113 entries, archived verbatim

Cut to 36 on the live board. **Nothing here was judged wrong; it was judged no longer able to change what I do next.** Roughly 70 of these are about the WP-09 surface register (`surface_check.sh`, `corrected_check.sh`, `ratified_in_check.sh`, `drift_check.sh`, `class_vocab_check.sh`, `key_classes`, `disposition`/`target`, the dispatch table) -- a closed workstream. **If that register is ever reopened, read this section first: it is the accumulated failure catalogue of building it, and it cost a fortnight to earn.** The rest were merged by class into the live board; the merged originals are here because the merge rewrote them.

## Watch-outs

Durable only. Everything settled lives in the artefact that carries it.

**THIS BOARD CARRIED 116 WATCH-OUTS AND I BROKE TWO OF THEM TODAY -- A LIST THIS LONG IS NOT A CONTROL, IT IS AN ARCHIVE.** The `IFS=$'\t'` collapse and zsh's lack of word-splitting were BOTH already written here, in my own words, and I walked into both inside one session. **Neither is a knowledge failure; both are retrieval failures, and the cure for a retrieval failure is not another entry.** The two rules I broke are the two that a MACHINE could have held -- a `-` placeholder convention, a bash file with real argv -- and the ones I kept are the ones that are genuinely judgement. **So the test for anything added here: could this be a control instead? If yes, build the control and do not write the line.** Directly downstream of vc's review to hv -- 210 commits touching no code, and a board that has grown past the point where its own author reads it is the same disease at the smallest scale.

**A BLOCKER IS A CLAIM, AND I HELD ONE FOR HOURS WITHOUT CHECKING IT.** I sat on the precommit wiring because a relayed message said `cmd/precommit` was in an `int upgrade` blast radius. **One command would have settled it**: `bin/.devbin/manifest.sha256` says in its own header that files not listed -- `config.yaml`, `cmd/`, `help/` -- belong to the project and are never overwritten, and `cmd/precommit` is not listed. The block turned out to be right for a DIFFERENT reason (the vendored tree moved under me, which dc named later), so nothing was lost -- but I did not know that, and I was deferring to an unverified claim while running a tool whose entire subject is unverified claims. **I apply the discipline to findings and had not been applying it to constraints.**

**REMOVING A POPULATION LEAVES ITS CONSUMERS CITING NOTHING, AND THE CHECKER GOES ON SAYING YES.** The tracker was cleared -- correctly -- and 7 `target` fields were left with no record that resolves, 5 of them `ratified_in`, which `ratified_in_check.sh` went on certifying. Nobody did anything wrong: the grammar was ratified when issues existed. **The tenth class with the sign flipped -- not a population that silently GAINED members my consumer cannot serve, but one that silently LOST the thing my consumer points at.** CLOSED at `b865db12` / `4cad12b8`, and the durable half is the rule: **when a shared artefact is deleted, sweep what cites it in the same breath**, and prefer a test that RESOLVES the citation over one that recognises its spelling -- `CLOSED/` survived, so revoking the spelling would have accused a live reference. **And the count I first put here was two populations glued together** (13 no-SHA, 7 no-record-at-all, 5 of the 7 in `ratified_in`), which is the denominator rule biting the hour it was ratified.

- **A PROOF ANCHORED ON A CORPUS ROW EXPIRES SILENTLY WHEN THE CORPUS MOVES -- AND MY SECOND FIX HID THE EVIDENCE FOR MY FIRST.** The `AUTHORITY_HV` anchor was proved by reverting it and watching `ac gate` get laundered into the hv bucket. An hour later the provisional bucket landed AHEAD of the hv test and caught that same row first, so **reverting the anchor now changes nothing measurable** -- the proof still passes and has lost the power to tell a working anchor from a redundant one. vc's eleventh class, arriving inside a proof. The fix is a SYNTHETIC row constructed to exercise the property (`vc ruling 2026-08-17, deferred to hv`), which no later corpus edit can absorb. **Prefer a fixture you built over a row you found, for anything meant to still discriminate next month.**
- **AN ATTRIBUTION NAMES A PERSON ONLY WHERE AN ACT OF THAT PERSON IS ON THE RECORD** -- a ruling, a message, a commit body (vc, 2026-08-17, ruling against their own field). **A metadata field recording who a row was filed UNDER is not evidence of who WROTE it**, and in this project it is specifically known not to be: all 21 cleared issues carried `reporter: matts` and every one was found by us, and every node commits as `Matthew Sinclair` by standing policy, so git author settles nothing either. **The severe case is an attribution to the ADJUDICATOR on a row awaiting their ruling** -- it manufactures their agreement on the question they have not been asked, and the node holding the pen cannot see it, because to them it reads as a citation. Sibling of my `AUTHORITY_HV` bug: that matched a name in the wrong POSITION, this took one from the wrong FIELD.
- **NOT ALL DIRT IS THE SAME WEIGHT, AND THE SEAL CANNOT TELL** (cc's sharpening, and it belongs on both boards). `+8 dirty` counts FILES; what matters is how many things READ them. My uncommitted `surface/dispatch-table.json` is read by three of cc's test files, theirs by nothing -- so the same banner covered a green over _proposed assertions_ (harmless: they pass or they do not) and a green over **a shared input in a state that never existed**. Re-run the readers at the committed sha and claim only those; I claimed 3 suites at `4cad12b8` and explicitly not the other 70 legs.
- **A CLAIM ABOUT A SIBLING'S CODE IS A CLAIM, AND I SHIPPED ONE UNRUN INTO A TOOL WHOSE SUBJECT IS UNRUN CLAIMS.** I warned in `ratified_in_check.sh` that rewriting a `parity.md` sentinel would break `corrected_check.sh`. **A mutation disproved it: the output is byte-identical.** That check reads `ratified_in` only to split rows CLAIMED but NOT CITED, and a cited row never reaches the split -- I had reasoned from the field ACCESS and not from the control flow, which is the same error as reading a `values` field and inferring what consumes it. **The corrected finding is better than the one I meant to make**: the rewrite is provably harmless today and disarms an alarm for tomorrow, because the day parity.md stops citing that unit the stamp reads ELSEWHERE instead of UNCITED. **A change with no observable effect whose entire effect is on which alarm fires after an unrelated later edit** -- vc's eleventh class, and the member that establishes its time axis, since the other two are already discriminating-blind today and this one only becomes so later. It wants a RULE (never rewrite a stamp an alarm reads, even one you have proved inert) rather than a check, because a defect whose only symptom is in a future diagnosis cannot be found by diagnosing the present.
- **A NEEDLE WRITTEN FROM THE AUTHOR'S HEAD ENUMERATES THE SPELLINGS THE AUTHOR REMEMBERS -- THIRD FIRING, AND THIS TIME BOTH OF US.** vc ratified the `ratified_in` record grammar as "a sha or a filename", on my proposal. The corpus also has `recorded on issue 0046`, in FIVE rows, all theirs. My first run reported eleven rows recordless when six were, and **five of the eleven would have sent someone to "fix" a row that was already correct** -- the false-alarm direction, the expensive one. vc's own note on it is the sharp version: they cited my corpus rule AT me in the same message where they ratified an enumeration written from memory. **The requirement was right and the enumeration of how it can be spelled was never ours to write** -- only the table knows that.
- **A GUARD THAT REFUSES LOUDLY INTO A ROOM NOBODY IS IN IS INDISTINGUISHABLE FROM ONE THAT PASSED.** `corrected_check.sh` died at exit 2 the moment I ratified two rows into a field name it does not read, and it stayed dead through a localfold, a compact, and a board of mine reading "nothing blocked". **It behaved perfectly** -- it refused on its own inability to measure, named both rows, and said why. **Nothing ran it.** The sibling of my earlier "a guard that does not run looks exactly like a guard that found nothing", and strictly worse: that one was silent, this one was shouting. **The output existed and the reader did not**, so the whole `corrected` axis went dark for twenty rows while two were being edited. Neither more care nor a better message fixes this; only something that runs the check does.
- **A WRONG FIXTURE ACCUSES A CORRECT IMPLEMENTATION, AND THAT IS THE EXPENSIVE DIRECTION** (vc's sharpening of my own finding, and they are right that I understated it). My `literal_stdout_parity` fixture seeded `AT-01.2` already at its target, so the declaration measured a self-loop and **MANUFACTURED a parity break against v3 that did not exist.** cc's sibling error failed loud -- eight of nine red, caught in minutes. **Mine was quiet in the dangerous direction: the natural next step from a manufactured parity break is to file a defect against the binary**, and a wrong fixture accusing correct code costs someone else a day. The rule has a fixture half and an instrument half and they are one rule: **arrange for the two instants to differ, then read them.**
- **RATIFYING A ROW NO LONGER REMOVES ITS COVERAGE -- CLOSED AT `0f87fc2c`, AND THE FIX HAS A PART PEERS MUST NOT UNDO.** A `corrected` row now carries `target.stdout_exact` (v3's ruled bytes) beside `observed.stdout_exact` (v2's measured bytes), and is asserted against the first. Ratifying changes WHICH template a row is held to; it no longer releases the row from being held to one. **The part that is fragile is the floor**: `ROWS_THAT_MUST_STAY_DECLARED` in `literal_stdout_parity.rs` names nine rows, because DELETING a declaration removes a row from every population at once and reds nothing -- which is exactly what happened to these two this morning. Adding a row to that list is free; removing one should cost an argument in a diff.

- **A CORRECTION MUST REACH EVERY FIELD THAT REPEATS THE VALUE, NOT THE FIELD THAT WAS REPORTED.** I fixed `at na`'s `observed.stdout` from `n-a` to `n/a` and left the `notes` beside it glossing v2's vocabulary with v3's token, so for one commit the row stated the right value and the wrong vocabulary. **vc found it, and it is the pair I had just written up in 0056** -- a correct statement and an incorrect one about the same property, with nothing to say which governs -- reappearing one artefact over, in the row the reasoning came from. Replacing rather than annotating is only half the discipline; the other half is enumerating every field that carries the value before declaring the fix done.
- **A FIXTURE SEEDED AT THE TARGET MEASURES A SELF-LOOP AND REPORTS IT AS A PARITY BREAK.** My first `literal_stdout_parity` fixture had `AT-01.2` already at `n-a`, so the `at na` declaration drove a no-op and the run blamed the binary for output the fixture had caused. **A template describes a MOVEMENT, so the fixture must leave the verb somewhere to go** -- cc's witness rule from the other end, and the reason theirs drives each verb twice rather than seeding at the target.

- **A CLEAN `git status` LICENSES A BUILD, NOT A MEASUREMENT TAKEN LATER AGAINST ITS OUTPUT.** I verified the tree clean, built, and measured. A peer then edited `render.rs` in this shared checkout and a rebuild replaced the binary **at the path I was still reading**. Two binaries, one path, same name, **no record in either of which tree it came from** -- separated only by an mtime 31 seconds apart, which **establishes order and not content**. The precondition was true when taken and had expired by the time it was relied on, and **nothing anywhere states how long such a check is good for**. Re-driven from a `git archive` extract; all 27 reproduced. **In a shared five-node checkout, build from an extract when the result is going in writing.** vc keeps this as `tree` with the target moved from source to ARTEFACT, deliberately NOT merged with cc's `instant`.
- **AN INDEPENDENT CHECK IS NOT INDEPENDENT WHEN THE SAME HEAD WROTE BOTH -- and I proved it twice in one round.** My coverage needle missed `"$INTENT"`. I then hand-wrote a second pattern to check my own four findings, **and it missed `"$INTENT"` too**, because the closing quote sits between the variable and the path and both patterns demanded whitespace there. `claude start` came back zero twice, and I only found the seven invocations by grepping the bare literal with no pattern at all. **vc's rule is that the calibrating case is written in one spelling; the corollary is that the checker is written from the same mental list as the needle.** The only honest enumerator of spellings is the corpus. **And the direction is the asymmetry: the blind needle lost nothing and MANUFACTURED three findings, all reading as serious** -- an untested launcher, an untested generator, an untested sync -- with nothing in the output separating them from the one real zero beside them.

- **I ASSERTED A MECHANISM INFERRED FROM A DECLARATION'S EXISTENCE, TWICE, AN HOUR APART, AND THE SECOND WAS A CORRECTION I VOLUNTEERED.** I told cc that `values` on an arg builds the clap enum -- it does not; the spine reads `values` only to expand `subcommand` slots. Then in 0052 I read `wp new --help`, saw no flags, and concluded no scope is entered -- `render.rs:574` hardcodes `TShirt::S`. **A surface is all declarations, so this is the failure the interface node is most exposed to**, and it is now canon as vc's seventh variant with my line as its statement: going to the source is not the same as reading the source. `grep` finds the field; only the reader finds what consumes it.
- **A GUARD THAT DOES NOT RUN LOOKS EXACTLY LIKE A GUARD THAT FOUND NOTHING -- and mine did not run, inside the harness built to prove guards run.** The mutation harness for `self_loop_population.rs` restored on RELATIVE paths while its own helper had changed directory, so the trap meant to undo each mutant in this live five-node clone silently failed and the mutations sat on disk until I checked. Recorded in the test file rather than fixed quietly. **The generalisation is not "use absolute paths": it is that a cleanup step produces no output when it works, so it is indistinguishable from one that never fired.**
- **A CORRECTION CAN MAKE A FINDING WORSE AND THAT IS A REASON TO CHASE IT, NOT TO RESIST IT.** vc corrected 0052's entrance half: scope IS entered, hardcoded `S`, deliberately, for template parity. My claim of a blank column was wrong -- **and a column of `S` is worse than a column of blanks**, because a blank is visibly unanswered and an `S` reads as a sizing decision on a project where sizing is house practice. **The migration refuses to invent a size for a package that has none, on the explicit grounds that substituting a medium is worse than recording an absence; `wp new` invents one for every package it creates.** Both positions in one codebase, one argued.

- **A CITATION DECAYS BY LOSING ITS SUBJECT, NOT ONLY BY LOSING ITS LINE NUMBER -- and this one decayed while being copied between three correct documents.** hv's self-loop ruling is justified by v2's measured `already CLOSED`. That string is `intent issues close`'s and ONLY `intent issues close`'s (`bin/intent_issues:282`). `data-model.md:373` attributes it correctly; `facade.rs:383` drops the subject; `render.rs:1132` puts it in `todo_done`, where it becomes a claim about `st done` and `wp done` **and is false of both** -- v2's `st done` has no state check at all, re-runs the gate, and re-stamps `completed:` from a fresh `date -u`. **Nobody wrote anything false; each step dropped one qualifier, and three steps was enough.** My own version this morning lost a line number to an uncommitted tree, which is the same failure with a different field missing.
- **A CLEAN COMMIT-TO-COMMIT DIFF IS THE STRONGEST AVAILABLE REASSURANCE ABOUT A QUESTION NOBODY ASKED** (vc's, earned the hard way, hours after catching me at it). They ran a byte-identity check between two commits _specifically_ to establish which file they were reading, got a true result that reproduces, and then read the working tree -- which that comparison is structurally incapable of seeing. **A negative result would have left them uncertain; this left them confident.** Third instance today across three nodes, and the two it has caught are the two who wrote the rule down, which is evidence for a mechanical check rather than more care. `git status --porcelain -- <path>` before any measurement that goes in writing, or read `git show HEAD:<path>` and never open the file.
- **AN `evidence_class` NAMES A METHOD AND NOTHING CHECKS THAT THE METHOD REACHED THE FIELD** (cc found one, I bounded it to three). `issues add` said `measured` and listed its four probes -- bare, `--help`, unknown flag, outside a project -- none of which supplies a title, while the row declared a success-path `stdout`. **The class was honest and the coverage was assumed**, which is exactly a criterion stated more broadly than the one applied. **And the first count was 25, which was wrong**: 22 are fine because bare IS their success path. The discriminator is `a required arg the matrix never supplied`, it is mechanical, and `critic` was not merely unverified but WRONG -- it declared the findings report, which is the non-empty case, and the clean case a green CI hits every time prints one line.
- **I ASSERTED A MECHANISM I HAD INFERRED FROM A FIELD EXISTING, TWICE IN ONE ROW, AN HOUR APART.** I wrote `value` on an enum arg where args take `values` (my own `key_classes` arm refused it); then told cc `values` builds the clap enum. It does not -- the spine reads `values` only to expand `subcommand` slots. **Going to the source is not the same as reading the source**, and the second was worse than the first because it was a correction I volunteered.

- **A VERDICT NAMING A SUITE IS A PROMISE THAT THE TREE STILL EXISTS, AND ALL FOUR OF US READ ONE THAT DID NOT** (vc's sentence; dc measured the timing; the general form is dc's and none of us had it written down). hv's 11:46 run went red on `close_gate_parity` and FORMAT and was brought to me as regressions. **Neither was.** `close_gate_parity.rs` and `facade.rs` were modified at 11:47:26 and 11:47:33, AFTER the leg finished at 11:46:45; nine paths were dirty when it started; and HEAD measured green in a sacrificial `git archive` extract, the failing test included. **The seal says which tests failed and NOTHING ANYWHERE SAYS WHICH TREE THEY FAILED AGAINST** -- so a run against a dirty tree and a run against HEAD produce artefacts identical in form and incomparable in meaning. **And the same applies to a GREEN**: my own legs sealed clean while my table was mid-edit, so that green is no more evidence about my work than the red was about cc's. **The gap in the discipline we all adopted: the edit-hold protects a run from edits made DURING it and says nothing about work already sitting in the tree when it STARTS.** A commit sha plus a dirty-tree flag in the verdict costs nothing; dc carries it into the Devbin change. **AND THE UNIFYING FORM IS dc's, EARNED BY CATCHING THEMSELVES INSIDE THE SAME HOUR: read the identity AT THE MOMENT YOU WRITE, never carry one forward.** They announced they were measuring `78a12dce`; the run took `b2173b1b`, because HEAD moved between their read and their run. **Their SCRIPT named the commit it actually took and their PROSE named one read minutes earlier -- same person, same ten minutes, mechanism right and discipline wrong**, which is exactly the argument for putting it in the artefact rather than in anyone's habits. Harmless here and provably so (`git log 78a12dce..HEAD` is empty for both files), and provable-harmlessness is luck rather than design. **This is the same shape as my own fabricated heartbeat, my retracted 103, and my citation of an uncommitted line: every one was an identity carried forward instead of read at the point of use.**
- **A WRONG ZERO CERTIFIES ABSENCE AND A WRONG MAXIMUM CERTIFIES CATASTROPHE, AND THE SECOND IS FAR MORE PERSUASIVE BECAUSE IT LOOKS LIKE DILIGENCE REWARDED** (dc, via vc's announce; two nodes hit it in opposite directions in one day, both from zsh). vc's probe loop wrote `$BIN $cmd` and zsh does not word-split, so every multi-word row answered `unrecognized subcommand 'st list'` -- **exactly what a surface where nothing is implemented looks like.** dc's `while read -r want path` destroyed `PATH` on the first iteration, `shasum` vanished, and all 27 vendored files reported as diverged -- one step from filing an issue saying the whole tree had been modified. **Nobody re-checks an instrument that has just found something big.** The exposure is inline only -- every parity tool has a bash shebang and is executed -- **which means the hazard lives exactly where we all take our first measurement of anything, and where a result is least likely to have a control beside it.**

- **I WROTE A PEER'S UNCOMMITTED WORKING TREE INTO THE REGISTER AS SHIPPED BEHAVIOUR, AND CITED A LINE NUMBER FOR IT** (vc, who stopped me within the hour and was right to). Told that hv had ruled self-loops legal, I opened `facade.rs`, found `if from == status { return Ok(Outcome::AlreadyThere) }` at `:1151`, verified it "at source", and rewrote three correct ratifications to match. **`AlreadyThere` occurs 7 times in the worktree and ZERO times at HEAD.** All three early returns are `Not Committed Yet`, stamped nine minutes AFTER the ruling they implement. The file moved twice more while I read it. **So the rows I "corrected" described a state that has never existed in any committed tree, and the rows I replaced were right.** The lesson I attached -- _I read the declaration and called it the behaviour_ -- is FALSE for this instance: at HEAD the declaration and the behaviour AGREE, because `permits` refuses an undeclared from-state and there was no gap to fall into. **A wrong lesson on a real correction is worse than the correction, because the lesson is what gets applied next time** -- my own rule from the same morning, and I broke it while writing it down. **The true rule: a row citing a line number is a PROMISE THAT THE LINE EXISTS, and a shared clone is the one place that promise can be false while the file is open in front of you.** It is my citation rule -- a citation naming a file is a promise a file exists -- one level down, at the line. **Going to the source is necessary and does not tell you WHICH source you reached; `git blame` and a HEAD read do.** Rows now state the ruling (committed, citable) and describe HEAD, with the pending change named rather than pre-written.
- **DEFERRING TO THE ARTEFACT IS NOT THE SAME AS READING IT, AND THE DEFERENCE FEELS EXACTLY LIKE DILIGENCE.** My board said the `info` bound was 1-of-104; the row said 103; I checked issue 0044, saw `flag refused: 103, pos refused: 103`, concluded the row was right, replaced my correct number with the row's, and wrote vc a paragraph congratulating myself for not inventing a figure. **That 103 pair is 0044's RETRACTED first run** -- the sweep whose hash included `intent info`'s own `Location: <cwd>` line, so no two runs could ever compare equal -- and the issue prints it as its example of what a dead instrument looks like. The corrected sweep probed 104 and found 1. **I had a citation, it named the right document, and it named the wrong sentence in it.** The rule I was applying -- recount, do not adjust, prefer the authored artefact to your own memory -- is correct and I applied it to a figure I had not gone and read. **A citation that names a FILE is not a citation; it is a promise that a file exists.** Cite the CLAIM. Same failure as my own cite-IDs-not-line-numbers note, one level up. It survived six hours because it looked like the careful move.
- **A KEY NAMED FOR ONE REASON CANNOT HOLD MEMBERS ADMITTED FOR ANOTHER, AND THE MEMBER THAT DOES NOT FIT THE NAME IS THE ONE THAT FALLS OUT** (vc, on their own `SURFACE_NONRETURNING`, caught 40 minutes before I would have committed it). The exclusion list has FOUR members for TWO reasons: `daemon` and `mcp` never return, `claude upgrade` and `claude start` write outside the sandbox. **`claude upgrade` returns perfectly well and installs into the operator's real `~/.claude`**, so it fell out of a list whose name was only ever entitled to define one of the two. **`claude start` survived only because it satisfies BOTH readings, and its two-word shape made the list look complete.** The fix is not a better name -- it is that admission stops being decided by the name: every member states its own grounds and the generator refuses an empty `why`. **Latent rather than harmless: `claude upgrade` is unimplemented today and WP-07 makes it live, at which point a harness reading `probeable` runs an installer against a real home directory.**
- **AN EXEMPTION GRANTED FOR ONE REASON GETS APPLIED TO EVERY REASON, AND NOBODY RE-READS THE PREMISE.** `Target` was excluded from `key_classes` entirely on the grounds that it carries 40 prose keys against 2 read ones. **That argues for exempting it from TOTALITY and says nothing about the two arms binding the declaration to the code, which cost two list entries.** `Target.spelling` was deserialised and READ and sat in no list at all -- the exact state the five lost fields were in, inside the one type the checks did not look at. vc's test is the one to apply: **the exemption was granted on a premise, so re-read the PREMISE rather than the exemption.** And the consequence nobody expects: **exempting a type from totality TIGHTENS what it may read**, because the note list is what makes `read but deliberately unclassified` expressible, and without one the only surviving distinction is declared-or-not. The word `exempt` invites the opposite conclusion, which is why the reasoning has to sit next to the strictness.
- **A PIPELINE'S EXIT CODE IS THE LAST STAGE'S, AND I READ `cargo test | tail` AS CARGO'S TWICE -- WHILE WORKING ON THAT EXACT CLASS.** The harness reported `[exited with code 0]` for a backgrounded `cargo test ... | tail -30`; that zero belonged to `tail`. Both runs happened to be genuinely green, so nothing was misreported -- **which is the whole problem, because a method that cannot distinguish green from red returns green either way and gets trusted on the strength of having been right.** Redirect to a file and read `$?`, or the claim is about the formatter. Note the doubled irony: dc and I spent the same afternoon building a refusal for a runner that trusted one source about a run.
- **A CONSOLIDATION IS WRITTEN UP AS DONE AT THE MOMENT IT IS DESIGNED, BECAUSE THE DESIGN IS WHEN YOU KNOW WHAT THE FINISHED STATE LOOKS LIKE** (mine, from vc's 0037; vc then produced its third instance in the same day). The write-up is not a lagging record of the work, it is a LEADING claim about it, and nothing marks the difference. Instances: 0037's Resolutions said the library "carries the exclusion list `implemented_check.sh` HAD BEEN re-deriving" and that copy was never removed; `implemented_check.sh`'s own comment says "One home for the list" about one of four homes; and vc wrote "Not claimed: that the four homes disagree today. They do not, I checked the predicates" **having checked the retire predicates and never compared the exclusion lists at all -- and two of them disagreed by the member that mattered.** vc's rule: **a sentence asserting two things AGREE names the comparison that was run, or it is a design note wearing a measurement's clothes.** All of these read exactly like the true sentences beside them.
- **A FIX CAN INHERIT AN INCIDENT IT DID NOT CAUSE, AND BOTH OF US WERE ABOUT TO RECORD THAT IT HAD** (dc, driving the gate I had only reasoned about). I diagnosed the 04:05 false green as rc-and-seal disagreement and staged a refusal for it. **dc then drove `bin/int test shell` against a deliberate failure: rc=1 AND a consistent non-empty seal. The two sources AGREE, so control C describes a state this gate does not currently produce.** The arm still earns its place -- control E, returned-0-never-sealed, is reachable, because a killed run produces exactly it -- but the incident is unexplained by it. **The real mechanism is that no artefact states the verdict of a RUN**, so the aggregation is done by a human holding two files, and that failed twice in one day. **A plausible story that fits the evidence is what a measurement is for, not a substitute for one.**

- **`git commit --only` SILENTLY SKIPS AN UNTRACKED PATH, AND MY FOLD ARCHIVED SIX MESSAGES INTO A DIRECTORY GIT WAS NOT TRACKING.** The fold commit landed the EMPTIED inboxes and left the new `.history/<date>/` behind, so **HEAD carried six messages deleted with nothing standing behind them.** The working tree was fine, which is what makes it dangerous -- everything looked archived because locally it was. **`--only` is a pathspec filter over TRACKED changes: naming an untracked path is not an error, it is a no-op**, and a commit that drops one of six named paths reports success on the five it took. Same reasoning as the protocol's `.gitkeep` rule, one step later: git does not track a directory, so a directory created MID-SESSION is invisible to the commit that was supposed to carry it. **`git add` the archive first, and verify against the COMMIT's file list rather than the working tree -- the only place the difference shows.**

- **A TRUE ANSWER TO A QUESTION THAT COULD ONLY EVER HAVE FOUND WHAT YOU ALREADY FOUND, AND I REPORTED IT AS `NO CLASS HERE`.** Having found `doctor`'s help naming a retired flag, I measured _"does any OTHER row's help name a non-shipping flag"_, got **N=1**, and concluded there was no class to build an arm for. **The measurement was correct and the question was wrong**: it could only ever have found help strings, and the class was one level up -- _when a flag becomes `retire`, what ELSE on that row still mentions it?_ `doctor` had THREE downstream artefacts (`read_or_mutate`, `mcp_review`, `help`) from ONE withdrawn subject, found by three separate accidents on three separate routes. **The tell is that I generalised along the AXIS I HAD JUST LOOKED AT rather than along the one the instances shared.** vc caught it. **Before reporting `N=1, no class`, ask what the three instances you already have would have needed a check to look like.**
- **A LIBRARY SOURCED PER-GATE IS RE-READ MID-RUN, SO EDITING IT CHANGES THE RUN IN FLIGHT RATHER THAN THE NEXT ONE.** I nearly landed the runner refusal while the suite was running, reasoning that `lib/runlog` is a sourced library and therefore already in memory. **It is in memory for the PARENT. Every gate is its own process -- `cmd/test` has a shebang and sources `gatecmd`, which sources `runlog` -- so the NEXT leg re-reads it from disk.** A ledger format change would have been written by post-edit legs and read by the pre-edit `run_all` still running above them, garbling the FAILED block of matts's own run. **The general form is worse than the mid-run-edit rule I already carried: it is not enough to ask whether the file is being executed, you have to ask whether anything RE-ENTERS it before the run ends.** An atomic `mv` protects the running process and does NOT protect the run.
- **THE VALUE THAT MEANS `NO DEVIATION` IS ALSO THE VALUE THAT MEANS `DO NOT LOOK HERE`, SO IT GOES STALE IN THE DIRECTION NOBODY CHECKS** (cc, on three of my rows). `st done`, `wp start` and `wp done` carry `target.state: as-observed` while v3 REFUSES all three where v2 accepted -- **a deviation AC-04.6 requires, asserted as parity.** `as-observed`'s own gloss says it asserts no deviation and there is nothing for `parity.md` to ratify, which is exactly why nobody re-reads those rows. **UNDER-REPORTING A DEVIATION IS WORSE THAN OVER-REPORTING ONE**, and the event that ought to move a row -- a guard landing in the facade -- is the event least likely to make anyone open the table. Measured against the facade, never inferred from the row.
- **A RUSTDOC COMMENT ON A MODEL TYPE IS CONSUMER-FACING PROSE, AND THE INTRA-DOC LINK IS THE TELL** (cc, extending my own finding past where I took it). I flagged that a `///` on `Thread::completed` reaches the generated SDL and reddens the committed face. **cc found the sharper half: the comment said `See [COMPLETED_RESOLUTION_HOURS]`, a Rust intra-doc link, which reaches a PUBLISHED schema face where a GraphQL client meets a dangling reference to a constant they cannot see.** So the coupling is not merely a regeneration chore. **`design.md:310-320` already ruled the principle -- a `///` on a derived type is an unreviewed publication channel, the author is writing a comment and the consumer is reading a contract** -- and the operational form is: state the fact, do not cite the source of it.
- **COUNT THE FIELD'S WRITERS -- AND CHECK WHAT THE GUARD ASSUMES.** One rule with a clause, not two, at vc's explicit request: _two rules of equal standing is how a reader ends up applying neither._ **The primary: a field with ONE writer cannot produce a standing disagreement, because disagreement needs two sources.** That is the test for DRIFT and it runs on every artefact with no incident needed -- `drift_check.sh` can report on verbs precisely because verbs have a measured inventory AND an authored table, while `help` has one writer and only downstream readers, so no edit to it can ever produce a permanent report. **The clause: a guard and its PRECONDITION are two writers of one assumption even where every field has exactly one writer, and the assumption is normally written down nowhere.** dc's 0042 branch tested for EMPTY; cc implemented `info` to render `INTENT_HOME: <not set>`, a correct rendering and a non-empty string, and the branch became unreachable in the one condition it existed for.
- **THE WRONG ENUMERATOR IS THE SHORT ONE, THE NATURAL ONE, AND THE ONE THAT RETURNS A PLAUSIBLE NUMBER** (vc, on rewriting their own issue 0037's exact defect the next day). **IN THIS TABLE `.families[].entries[]` IS NEVER THE POPULATION: it is 104, DECLARED is 112, and SHIPPED is 107.** The other 8 are the top-level `new_surface[]` array -- all shipped, four of them `one-way`. **The same enumerator is TOO NARROW AND TOO WIDE AT ONCE** -- missing the 8 and including the 5 retired -- **and the two errors have opposite signs, three apart, so no count-based sanity check flinches.** Four instances now: vc and I caught it independently in flight; vc found the both-signs form in cc's test; vc's own 0044 sweep then reproduced it while carrying the issue in working memory. **That last one settles what kind of problem it is -- not knowledge and not attention, because nothing about the short enumerator looks partial.** Now has one home in `parity/tools/lib_surface.sh` (`surface_declared` / `_shipped` / `_retired` / `_probeable`); the Rust half is still open.
- **A PROBE NAMED FOR WHAT IT SUPPLIES MEASURES A DIFFERENT THING ON EVERY ROW WITH A DIFFERENT ARITY.** vc's rig probes each command with `one bogus positional`. **For `search`, `zzbogus` is a perfectly valid QUERY, so the command accepted it and failed later on environment; for `ingest` it is a valid PATH, so it got as far as reading `zzbogus/intent/.config/config.json`.** Only for a row taking no positional does that probe test rejection at all. **The classification held because it is by OUTPUT, which is what saved it** -- but the probe's NAME implies a uniformity it does not have, and a reader comparing rows is comparing three different tests. **Name a probe for what it TESTS, not for what it supplies.**
- **A REGISTER IS CLOSED OVER ITS OWN CONTENTS, SO IT CANNOT CHECK A PROMISE OF WHAT THE TOOL DOES NOT HAVE -- AND THE HONEST MOVE WAS TO MEASURE AND NOT SHIP.** `backup` promises `... for fast local restore` and nothing ships a `restore`. **The derived detector -- a help word that is a shipped verb elsewhere but not here -- is BLIND to it by construction**, because the promised thing has no row to match; the detector that sees it needs a hand-list of inverse verbs whose misses are silent. **Every artefact-vs-artefact check I own compares two things that both exist; this class has only one end.** The limit is structural, not effort, so a refusal here would assert a coverage I cannot defend -- the `guide_refs_check.sh` failure exactly. **A measurement I can reproduce beats a check I would have to excuse.** _(And the discriminator that made the measurement trustworthy was a checked premise, not an assumed one: help strings are imperative-first, 107/107, so an inverse verb in FIRST position is the command describing itself.)_
- **AN OMISSION HAS NOTHING ON THE ROW TO CONTRADICT, SO NO CONSISTENCY CHECK WILL EVER SEE ONE.** `backup`'s help is silent about the fact that it DELETES (`Prune` + rolling `Retention`). My retirement arm catches a row PROMISING what the tool does not do; this is the mirror and it is structurally invisible to the same machinery -- **a false promise contradicts a field, a missing warning contradicts nothing.** Keep the two classes apart; they look like one problem and only one of them is mechanisable.
- **A CHECK IS AN UN-NOTIFIED READER OF THE VERY DECISION IT CHASES, AND MINE WENT SILENT ON THE ARTEFACT IT WAS BUILT FOR.** The retirement blast-radius arm reads `.families[].entries[]` + `.new_surface[]` -- the population rule this toolchain states in every file, and the one I have been most careful about all week. **It went green the moment I fixed `doctor`'s ENTRY help, while `families[].help` still said `Diagnose and fix`.** Two writers of one string, one reader. **The population rule was not wrong: it answers `which rows are commands`, and this arm asks `which text faces a user`, and those are DIFFERENT QUESTIONS OVER ONE FILE.** The tell was not in the arm -- it was that a rebuild's `--help` still printed the old string after a grep said the table was clean, so **the artefact disagreed with my query and I believed the artefact.** Before trusting a population enumerator, ask what question it answers rather than which rows it returns.
- **A WORD SHARED BETWEEN A FLAG AND ITS COMMAND IS USUALLY THE SUBJECT, NOT THE CAPABILITY -- WHICH IS WHY THE OBVIOUS DETECTOR WAS THE WORSE ONE.** Word-overlap between flag help and entry help looks strictly better than matching the flag's spelling, because it catches a paraphrase. On the real corpus it reported `doctor (--fix -- still promised by: issues)` against the CORRECTED string -- `issues` is what the command AND the flag both act on -- and fired twice on `todo done` for the command's own name. **Each needed its own suppression, and a refusal that needs a growing exceptions list to stay quiet is the cry-wolf shape.** The spelling rule needs none: **a flag's spelling names its ACTION and its help names its SUBJECT.** Narrowing a detector until it never lies beat widening it until it caught everything -- and the arm now STATES the paraphrase it cannot see, rather than implying coverage.
- **A REPORT THAT CAN NEVER GO QUIET TEACHES ITS READERS TO SKIM EXACTLY AS FAST AS ONE THAT FIRES WRONGLY** (vc, on `drift_check.sh` and a legitimate `corrected` claim). A cited `corrected` row SHOULD show as inventory-vs-table drift -- that is measurement disagreeing with judgement, which is the tool working -- **but if the disagreement is permanent by construction, the instrument becomes noise rather than a finding.** Settle reconcilability BEFORE making the edit that creates it; if it is not reconcilable, that is a finding about the instrument and never a reason to skip the edit. **Same family as the negative control: a refusal firing on the wrong population and a report that never clears both fail by teaching people to look away.**

- **A RECLASSIFICATION IS THE CHEAPEST WAY TO ACQUIRE AN UNWITNESSED CLAIM, BECAUSE IT ADDS NO ROW AND NO CODE.** Moving `doctor` from `mutate` to `read` put it into the population `read_claim_probe` witnesses, and it had no invocation -- **my own refusal, hardened yesterday from a print into a REFUSAL, fired on my own change within the minute.** Nothing about that diff looks like new surface: one word changed, no row added, no function written. **Probed now and it HOLDS -- nothing on disk changed -- so vc's ruling is confirmed by measurement rather than by argument.** The general form: when you change what a row CLAIMS, you have changed which checks are obliged to cover it, and a diff that adds nothing looks like a diff that owes nothing.
- **A CORRECT ROW WITH A WRONG JUSTIFICATION IS MORE DANGEROUS THAN A WRONG ROW, BECAUSE THE JUSTIFICATION IS WHAT THE NEXT PERSON READS.** My probe classified `backup` as `one-way` and my prose to vc called it additive and harmless -- **`backup.rs` carries `Prune` and a rolling `Retention`, so taking a backup DELETES expired snapshots.** The classification was right by accident of a good first instinct and the stated reason was false. **And I read it off the help text in good faith, which is the trap: `Snapshot this machine's store for fast local restore` does not say it deletes.** A row whose help understates what it does will be misclassified again by the next person doing exactly what I did.
- **A CORRECT PREDICATE WITH A SECOND WRONG COPY AT THE ONLY CALL SITE IS INDISTINGUISHABLE FROM HAVING NO PREDICATE AT ALL** (cc, building the arity control I owed, and their reason is better than mine was). I was going to compare the built surface to `Arg::required()` per row. **They drove `try_get_matches_from` over every shipped row whose first positional is mandatory -- because a fix that repaired `lang init` BY NAME passes a two-row test and leaves the mechanism.** The extra test is worth it for the mechanism, never for the two commands that exposed it.
- **A TEST OR LINT FAILURE IN A PEER'S FILE MAY BE A RACE, NOT A DEFECT -- RE-RUN BEFORE REPORTING IT.** cc's `cargo clippy` reported an error at `guide.rs:496` while the file was 485 lines long: cargo read it mid-write, and it cleared on a re-run with no change to anything. They nearly sent me a message about an error I had not made. **A LINE NUMBER PAST THE END OF THE FILE is a tell you can actually check**, where a plausible type error is not -- which sharpens the note I already carried about a peer mid-save producing real-looking `error[E...]` output.
- **A `&&` THAT SHORT-CIRCUITS LEAVES NO TRACE, AND A STEP THAT NEVER RAN LOOKS EXACTLY LIKE ONE THAT DID.** I chained a `MODULES.md` edit behind a `git rm` that failed on a peer's `index.lock`; the edit never ran, printed nothing, and I only caught it by grepping for the row I thought I had deleted. **In a four-session clone the lock failure is NORMAL, so the chain breaks routinely rather than exceptionally.** Verify the effect, never the invocation.

- **DO NOT PUT v3 ON `PATH`. THIS SESSION IS ALIVE ONLY BECAUSE `intent` STILL RESOLVES TO v2** (dc, 2026-08-16, measured). `claude` is unimplemented as a FAMILY, so v3 refuses before it looks at project state -- `intent claude hook require-in-session` exits 2 in an unmigrated v2 project AND outside any project at all. **The door is `brew install`, not migration**, and it puts v3 at PATH position 1 without asking. **The moment v3 is on the PATH of a shell a Claude Code session runs in, that session stops accepting prompts and cannot be recovered from inside itself.** Confirmed live by vc across five arms against Claude Code 2.1.233. Second hard publication hold, beside 0036.
- **A BLOCKED PROMPT EXITS 0, SO THE LAYER YOU WOULD USE TO DETECT THE LOCKOUT REPORTS SUCCESS** (vc, and they did not anticipate it either). The block is IN-BAND, in the output stream. **Any wrapper or automation checking the `claude` process exit code sees success while the model never saw the prompt** -- a second silent-failure surface sitting exactly where you would look for the first. **Consequence for any test of this: assert on the OUTPUT, never the exit code.** And keep the `1` arm -- an assertion that `2` blocks passes equally on a build where every code blocks.

- **hv STEER, STANDING: _"The sooner we can get this project onto v3, the better."_** Said to ic 2026-08-16 11:35Z and **relayed verbatim to cc, vc and dc** because hv was AFK and no peer could hear it. Scope is NOT cut: _"We will get all of this done -- including the text search and code parsing -- and then push 3."_ **Read it as SEQUENCING, not quality**, and vc drew the boundary correctly: **prefer-closing-over-perfecting is a real trade at a day of work and a BAD one at a single line.** It is not a licence to skip verification -- the day it arrived is the day six silent-green defects were found.
- **A NEW DECLARED FIELD SHIPS WITH ITS CONSUMER AND ITS CHECK, IN ONE CHANGE -- OR IT IS NOT READY TO BE DECLARED** (vc, holding me to it on `acts_upon`, and it is the preventive form of everything below it). **A declared field landing ahead of anything that reads it is `aliases`, `st_prefix`, `Arg.default`, `Flag.required`/`default`/`value` and `Entry.exposed_on_mcp`/`read_or_mutate` -- six instances recorded in one day.** The rule is not "declare less"; it is that the declaration and its first reader are the same change. **Corollary that decides sequencing rather than taste: if the consumer is not built, the honest state is PROSE IN A SPEC, not a field in the canon.** vc, verbatim: they would rather have the policy in my spec for another week than a seventh undeserialized declaration.
- **A TEST WRITTEN BY READING THE IMPLEMENTATION IS A RESTATEMENT WEARING A GREEN TICK -- AND MINE PASSED ON A LIVE DEFECT.** Extracting the arity predicates onto `Arg`, I wrote a test driving all four declared spellings and asserted `!required()` for `1..n`, **because that is what the code did.** It went green. What caught it was a test in `guide.rs` asserting the MEANING of the delimiters -- `<x>` required, `[x]` optional -- which had no way to inherit the mistake, and it failed on the first run. **The defect is real: `1..n` declares a minimum of one, `spine.rs` reads `arity == "1"`, so `intent lang init` with NO language parses where v2 refuses it outright.** Latent only because `lang` is unwired. **The rule: derive the assertion from the CONTRACT or from a second artefact -- never from the function under test, which is the one source guaranteed to agree with it.** Same family as the consistency check blind to a shared error, one level in.
- **CHECKING WHO HOLDS A FILE BEFORE ENTERING IS NECESSARY AND IT IS NOT SUFFICIENT, AND I HAVE NOW PROVED THAT ON MYSELF.** I ran `git status` on `native/rust/crates/intent-cli/src/` (clean), said so, then edited `render.rs` -- and cc had started in it in between, mtime 40 seconds before my check. **My one line referenced a `fn llm` that did not exist, so for about a minute cc's working tree did not compile because of me.** Backed out within the minute and disclosed it. **What saved it was the EDITOR reporting the file had changed under me, not any discipline of mine** -- so the durable form is: in a shared clone the check is a snapshot, and the guard is to keep every cross-lane edit SELF-CONSISTENT so that a peer committing mid-flight cannot take a half-change. **Prefer landing your own file unreachable over landing one line in theirs.**
- **A ROW DECLARING A COMMAND SHIPS CANNOT SEE THAT IT ANSWERS `not implemented yet`, AND NO INSTRUMENT I OWN CLOSES THAT GAP.** `claude hook` is `keep` / `as-observed` with eight siblings, so the register asserts it survives into v3 -- and the binary returns exit 2 for it, which Claude Code's `UserPromptSubmit` contract reads as BLOCK (vc's issue 0043). **0043 is now CONFIRMED LIVE against Claude Code 2.1.233 across five arms, so the "contract-derived rather than observed" rider I was carrying is STALE and gone** -- and ARM1 is the one that matters, proving `1` does NOT block, which makes it a causal claim about `d2b8e76d` rather than a symptom. **Every instrument I have compares the table to clap's SHAPE, and the shape is correct: the command parses, it is reachable, it answers.** It answers wrongly. **The register distinguishes declared from retired and cannot distinguish wired from implemented**, and that is a hole in what my checks can witness, not a defect in any row. **Do not read a green surface_check as evidence a command works.**
- **A FIELD THAT NAMES A SURFACE CANNOT CARRY A PROPERTY -- IT STRANDS THE PROPERTY THE NEXT TIME THE SURFACE IS RULED ON** (vc, ruling the shape of `acts_upon`). `exposed_on_mcp` named MCP and carried "how much blast radius does this verb have"; **D45 changed the surface and the property fell out with it**, correctly applied to 13 rows and readable through nothing. **The repair is not a better-named exposure flag -- that rebuilds the fault at a new address.** Declare the INTRINSIC thing and let each surface DERIVE its own policy from it. **Test to apply before declaring anything: if a ruling about where this is used could invalidate the field, the field is about the wrong thing.**
- **A SINGLE WRONG FIELD MAKES A RULE LOOK LIKE A TENDENCY, AND A TENDENCY DOES NOT GET DECLARED.** The 26 withheld-from-MCP rows partition EXACTLY -- 13 mutations that are precisely the 13 leaves, 13 reads that are precisely the 13 roots -- and it read 14/12 while `config` carried `mutate` against its own help string. **I measured the approximate version and reported it, and the approximation is what makes a policy look like a habit not worth encoding.** vc found the bad row by independently verifying a claim of mine that was TRUE; the check found the defect beside it. **A derivation is only as sharp as every field it reads, and nothing was checking that one.**
- **BEFORE EDITING A FILE OUTSIDE YOUR LANE, CHECK WHO IS HOLDING IT (`git status <path>`) AND SAY SO BEFORE, NOT AFTER.** vc's ruling on my EXP-08 edit: the judgement was right, the process was one step short, and **the cost of disclosing after is not courtesy, it is MEASUREMENT.** vc could not verify cc's 0037 fix because cc had uncommitted work in the file under test; `git blame` said _"Not Committed Yet"_ and could not say whose. **dc's form is sharpest: in a shared clone an untracked file has no author.** cc held `render.rs` while I was in `spine.rs`; had they held `spine.rs`, the two edits would have collided in a file neither of us could attribute. **The outcome was clean because cc happened to be elsewhere, and "it worked" is not evidence it was right.**
- **COUNT, STOP, READ THE DIFFERENCE, THEN ACT -- IN SEPARATE STEPS.** vc archived one of my messages unread because they ran the both-sided count check and the archive in the SAME command: the check printed `ic live=5` against the four stamps they had named, and by then the archive had run. **A check whose result arrives after the act it was meant to authorise is not a check.** Same family as my own batched-heartbeat fabrication: **the batching IS the defect**, not the carelessness.
- **DECLARED-BUT-NOT-DESERIALIZED IS A CLASS, NOT FOUR BUGS, AND AN UNKNOWN CANON KEY IS INVISIBLE RATHER THAN WRONG.** `Table` is deliberately not `deny_unknown_fields` (correct -- it is a register carrying prose nobody should have to type), and the consequence is that **adding a field to the canon silently adds an UNCHECKED field rather than a failing one.** `surface_check.sh` contains zero occurrences of `aliases`; so does `dispatch_ssot.rs`. **The tool whose whole job is comparing the binary to the table cannot see a key the binary never reads.** vc's line is the one to keep: **a `keep` row that does not ship is worse than a `retire` row -- `retire` is a decision with a ratification, this is an accident with neither.**
- **A CHECK'S ERROR MESSAGE IS WHERE YOU LEARN WHAT IT DOES, AND NOTHING VERIFIES IT AGAINST WHAT IT DOES.** `guide_refs_check.sh` said _"a renamed or RETIRED command named in prose is a hand-maintained command reference"_ and could not see a retired command, because its valid set is every DECLARED path and **the dispatch table is a parity register before it is a command list: a row means the question was ASKED, never that the answer was yes.** Three retired commands, including the `st_zero` hv killed, passed green as _"all declared"_. **The file asserted the capability in the same sentence it lacked it**, so reading it to find out whether the case was covered returned a yes -- which is why I read past it twice. **Found by MEASURING the claim, not reading it.** Same shape in a third author's file the same day: `paths.len() > 20` reads as coverage and is a did-it-parse assertion, passing at 104, 107 and 112 alike.
- **AN EXCEPTION TO A UNIVERSAL IS A COUNTER-EXAMPLE, NOT A COVERAGE GAP** (vc, ruling against my AC-05.1 recommendation, and the distinction is sharper than the one I drew from my own observation). **AC-00.9's gap was UNMEASURED -- 104 of 112 commands, nobody knew what the other 8 said, and measuring showed the criterion TRUE and unestablished. AC-05.1's gap was a KNOWN FALSE INSTANCE.** I had the observation -- _the coverage argument has an exception the coverage argument itself does not cover_ -- and drew the weaker conclusion from it.
- **A PREDICATE ANSWERS THE QUESTION IT WAS WRITTEN FOR; REUSING IT MEANS CHECKING THE NEW QUESTION IS THE SAME ONE.** `Flag::ships()` is `disposition == "keep"` and answers _"must the renderer emit this"_; the agent guide asks _"does this flag exist at runtime"_. They part on `intrinsic` -- which SHIPS, clap supplies it -- and **agree for 66 of 76 flags, which is exactly why the substitution reads as obviously fine.** A guide built on `ships()` would never tell an agent that `--help` works on anything, and for a help screen that costs nothing because clap prints its own. **A guide is a document and nothing else in it will say so.**
- **A CONSISTENCY CHECK BETWEEN TWO ARTEFACTS IS BLIND TO A MISTAKE THEY BOTH MADE** (vc's, on my `upgrade` find). `surface_check.sh` compares table to binary; `corrected_check.sh` compares table to `parity.md`. **Both passed while `upgrade` was wrongly retired, because the declaration and the binary AGREED** -- clap correctly did not build what the table correctly said was retired. What found it was walking the REMEDY STRINGS the binary emits, an artefact neither check reads. **A test asserting every declared verb exists would also have passed.** Only a THIRD source can see a shared error.
- **A SUCCESSOR TO THE ENGINE IS NOT A SUCCESSOR TO THE DOOR, AND I RETIRED A COMMAND ON THAT CONFUSION.** `upgrade` said `retire`/`retire` citing D09 -- one line, "Migration floor v2.19.0, two-hop", saying nothing about the command. What retires is v2's LEDGER; `migration.md:3` says the verb survives. **The row's own `behaviour` field held both halves** and I acted on the engine half. **`is_shipped()` reads both fields, so WP-10's migration would have shipped with NO INVOCATION.** When a ratification cites a design decision, READ THE DECISION.
- **READING A FACT AGAINST THE WRONG FIELD PRODUCES A TRUE-SOUNDING "NOTHING HERE". THREE INSTANCES, ALL MINE, ALL BETWEEN THE SAME TWO FIELDS.** `disposition` asks whether the v2 command SURVIVES; `target.state` asks what v3 DOES. Three rows carried `disposition: corrected`, an answer to the other question. I wrote that D09 _"says nothing about the command"_ -- it says nothing about its EXISTENCE and everything about its BEHAVIOUR. And the same shape produced the retirement. **When a lookup comes back empty, check you asked the field that answers it before concluding the fact is absent.** _(vc has now hit the level version of this too: they used the entry-level word `new-surface` in a flag-level position, and `flag_dispositions` has no such value.)_ **FOURTH INSTANCE, cc's, AND IT CARRIES THE TELL THE OTHER THREE LACK:** their jq printed each FLAG's spelling beside the ENTRY's `.disposition`, so every flag on the row reported `keep` and they sent me work that was already done. **A per-item query printing the SAME value for every item is what a parent's field looks like from a child's position** -- checkable on any ad-hoc query, which none of my three instances were.
- **A PREMISE WRITTEN DOWN IS NOT A PREMISE CHECKED, AND WRITING IT IS WHAT MAKES IT STOP BEING QUESTIONED.** I exempted `disposition` from the unused-value refusal on the premise that it _shares a vocabulary with the keep/retire/deviate register -- 47 `deviate` rows in `pertest.md`_. **vc went and looked: `pertest.md` contains the word `disposition` zero times.** Those rows sit in a `class` column whose subject is a v2 BATS TEST, largest value `out-of-scope` at 195 -- **meaningless for a command, since every v2 command is in scope by definition of a parity register.** The vocabularies overlap on three values and are NOT shared. **The exemption was right and the reason was wrong**: `deviate` is empty STRUCTURALLY, because a deviating command survives and takes `keep`. **State the premise at first application AND go and measure it in the other artefact -- a premise about a file you did not open is a guess in the voice of a finding.** _(This supersedes the older "check its premise before the second application" note, which was itself built on the false claim.)_
- **A CHECK THAT CANNOT FAIL IS NOT A WEAK CHECK, IT IS A DECORATION -- and it hands you a reassuring result first.** Instances: a flag-completeness loop went green while 5 of 93 flags rendered nothing, because it greps for the LABEL and 88 supplied it; a TSV field shift made the surface check report **MORE coverage and FEWER findings at once**, which reads as a better run; and one arrived INSIDE the fix for another, my staleness refusal blocking its own mutation test. **THIRD INSTANCE IN ONE LOOP, and the freight was mine**: `surface_check.sh` shipped whole flag objects through `@tsv` -- **which escapes backslashes** -- so the `\"` I wrote into `upgrade`'s prose came back doubled, jq died, and **the row I had just corrected stopped being flag-checked while the script printed "the binary and the table agree on every flag of every reachable command."** Fixed by carrying LESS; class refused by **counting both sides**. **"No flag violated anything" and "no flag was examined" are the same output, and only a count can tell them apart.** **The principle the whole family reduces to: a missing measurement must present as a REFUSAL TO MEASURE, never as a measurement of nothing.**
- **A PROSE TEST CANNOT TELL AN ASSERTION FROM ITS NEGATION -- AND THE ROW THAT EXPLAINS WHY IT IS OUT OF SCOPE IS THE ROW THAT SAYS THE IN-SCOPE WORDS.** `corrected_check.sh`'s SEVERITY arm grepped a ratification for `parity.md`; `ac gate` says _"NOT cited by parity.md"_ and was reported `UNCITED` on the spot. **I had fixed the same defect in the same file the day before, one arm over** -- moved SCOPE off prose because that half had failed, left SEVERITY on prose because it had not failed YET. **The half you do not fix is the half that breaks, on the first row that exercises it.** When you fix a class in one arm, fix every arm sharing the mechanism: _"it has not failed there"_ is not a reason, it is a schedule. **And the SCOPE half of the same file failed the same way: `corrected_check.sh` matched the literal `parity.md` inside each ratification and reported three units as ratified ELSEWHERE while `parity.md` was citing all three. A check whose SCOPE is derived from prose has the defect it was built to catch, one level up -- scope must come from the machine-read citation.**
- **A SCAR GENERALISES TO THE FILE WHERE YOU EARNED IT AND NOWHERE ELSE, UNLESS YOU GO AND LOOK.** `class_vocab_check.sh` refuses on an empty citation set because I had been bitten there; the three vocabulary checks in `gen_dispatch_table.sh` did not -- **deleting `flag_dispositions` reported 95 flags undeclared.** A wall of offending rows reads as data corruption and sends its reader to fix the DATA when the fault is one missing key. **Ask of each new check "what does this print when its input is missing".**
- **A WRONG ANSWER THAT IS STILL IN-VOCABULARY READS AS A MEASUREMENT, NOT AN ERROR** -- vc's test: _"the tell is not the slip, it is that the wrong result is in-vocabulary."_ **(1) CHARACTER CLASS vs HYPHEN**: `[^-]*` on `status: to-write` yields `to`, which looks like a status. **(2) SUBSTRING vs WORD BOUNDARY**: a grep said `drift_check.sh` was cited -- it matches INSIDE `decision_drift_check.sh`, the very row that was wrong. **(3) SEPARATOR vs SPACE**: a space-joined offender list split on spaces, and 60+ paths contain one. **(4) PREFIX-STRIP WITH AN EMPTY PREFIX**: `${TSV#$SP/}` stripped the LEADING SLASH and printed an absolute path as relative. **(5) jq's `//` FIRES ON `false` AS WELL AS `null`** -- `.exposed_on_mcp // "(absent)"` reported **31 rows absent when every one of the 31 was an explicit `false` and nothing was absent at all.** I then wrote a follow-up query on that false premise and it returned empty, which is what exposed it. **The field was three-valued in my head (true/false/absent) and two-valued in the operator, so the answer was in-vocabulary and wrong.** Use `has("k")` for presence and `== false` for the value; never let `//` decide which of the two you are looking at. **These vocabularies are hyphenated and space-bearing, so character classes and substrings are the wrong tool by construction.**
- **DO NOT INFER A FIELD'S GRAMMAR FROM THE FIELD BESIDE IT. READ IT.** I extracted `covers:` **with a colon, because `status:` sits beside it and has one.** The grammar is `covers AC-05.5`. **It reported zero multi-AT ACs -- a completely plausible answer that would have retired the finding** -- in the file whose grammar I had just spent an hour parsing.
- **A NUMBER THAT HAS BEEN WRITTEN DOWN GETS TRUSTED BY THE NEXT PERSON, INCLUDING THE ONE CORRECTING IT.** I wrote "SEVEN citations" and listed six; cc corrected me to six **by subtracting from my seven rather than recounting**, so the correction inherited the error. It was five. **Recount, never adjust.** _(Live instance: my board carried "111 rows carry help; 82 args" -- stale AND measured on the DECLARED set. Real figures on the SHIPPED set: 112 declared, 107 shipped, 107 help, 79 args, 39 keep-flag.)_
- **THE POPULATION IS ALWAYS WIDER THAN THE MARKER, AND A `replace_all` IS WHERE THAT COSTS YOU.** I was about to rewrite `printf '%s' "$VAR"` on the strength of 8 sites; **the real count was 21.** **Count the population before the substitution, not after the diff.**
- **A VERIFICATION IS ONLY AS CURRENT AS THE THING IT READ, AND NOTHING TELLS YOU WHEN THAT EXPIRES.** I checked `jq --indent 2` reproduced the table byte-identically, then hand-wrote compact JSON into it, then reused the earlier conclusion -- and jq expanded 45 lines. **And the mirror: matts's green suite predated most of my commits**, because cargo compiles at start; I closed that gap by running the one target my change could affect rather than claiming the suite covered it.
- **"IT DOES NOT EXIST" IS A CLAIM ABOUT THE FILESYSTEM, SO GO AND LOOK -- AND THE FOSSIL OUTLIVES THE CORRECTION.** git answers questions about HISTORY. Three original instances, the worst being that the false claim became the stated REASON for not fixing two defects. **dc then found the FOSSIL**: `gen_inventory.sh` still said `probes/toplevel.tsv` "has NEVER been tracked" -- tracked since **my own commit recovering it.** **A stale "it does not exist" stops being a gap and becomes a licence.**
- **EVERY INPUT TO A DECISION CAN BE CORRECT AND THE DECISION STILL CONTRADICT A DISTANT ARTEFACT. GREP THE SPELLING BEFORE YOU WITHDRAW IT.** I ruled `ingest --from-md` dropped; cc's argument was sound and my own note agreed. **Then I grepped and found five live citations across four artefacts I do not own**, one of them `acceptance.md`, which RULES that its scaffolding ships. **Nothing about it felt uncertain, which is the point.**
- **A CITATION THAT NAMES A LINE ROTS SILENTLY, AND FAST.** My row cited `acceptance.md:298`; within the HOUR it was `:302`. **Cite IDS, not locations.**
- **A SKIP WHOSE REASON IS "HARD TO CHECK" IS A PROMISE NOBODY MADE, AND MINE WAS HIDING A FINDING.** `surface_check.sh` declared INV-03 unchecked because it "needs a probe run outside a project" -- true, never a claim that nothing was wrong, **but it reads like one.** **Revisit skips; do not trust their reasons.**
- **RE-DERIVABILITY IS NOT COMPLETENESS.** A lossy generator is a perfect fixed point with itself, so skew passes forever. It hid 15 of 20 authored fields.
- **`read -r a b c d` WITH `IFS=$'\t'` COLLAPSES AN EMPTY FIELD** -- bash and zsh alike, because tab is an IFS WHITESPACE character. Every column after shifts. **Emit a `-` placeholder, never an empty field**, or return the parts separately. **SECOND INSTANCE, 2026-08-17, WITH THIS RULE ALREADY ON THIS BOARD: `ratified_in_check.sh` returned `resolving<TAB>dangling`, the empty first field collapsed, and every DANGLING issue number landed in the RESOLVING variable -- so the fix for a false green WAS a false green, in the check whose whole subject is false greens.** Predicted 5, observed 0, and 0 was what the broken code produced. **And I had written `OBSERVED: exact` into the mutation-proof block before running a single proof**: a prediction written first is discipline, a result written first is fabrication, and in the diff they are identical.
- **`path` IS A SPECIAL ARRAY IN zsh, TIED TO `$PATH`, AND ASSIGNING TO IT DESTROYS THE SHELL.** An audit loop of mine did `read -r path disp arity flagjson`, setting `PATH` to `st new`; every subshell after printed `command not found: jq` and reported all 60 rows unparseable. **The bash script it was auditing uses `path` too and is FINE.** **When an ad-hoc harness reports a wall of findings, suspect the harness first.**
- **A SCRIPT THAT DERIVES ITS ROOT FROM ITS OWN LOCATION CANNOT BE MUTATION-TESTED FROM A SCRATCH DIRECTORY, AND IT FAILS IN A WAY THAT LOOKS LIKE THE TEST WORKING.** Copying `read_claim_probe.sh` to the job tmpdir made `REPO_ROOT` resolve to `/Users`, so it died on `no runnable binary at /Users/bin/intent` -- **a non-zero exit from a mutant, which is what I was hoping to see.** Second instance; the first was running HEAD's `surface_check.sh` from `/tmp` and comparing the result to a real run. **The fix is to pass the parameters the script already exposes** (`IN=`, `OUT=`, `BIN=`, `TABLE=`) rather than to move the file. **And check the mutant failed for the reason you mutated it for -- an arm that goes red for the wrong reason proves nothing and reads as proof.**
- **A STALE BINARY ARGUES HARDEST EXACTLY WHEN IT IS MOST WRONG** -- a plausible, well-formatted, entirely wrong report, **and what it invents is precisely what somebody just fixed**, so it reads as a regression by the peer who closed it. **MECHANISED**: `surface_check.sh` refuses on a binary older than its inputs. **Six correct firings now.** `find -newer`, never `stat`.
- **`warning: build failed, waiting for other jobs to finish` IS CARGO'S LOCK MESSAGE, NOT A COMPILE ERROR**, and separately **a peer mid-save produces real-looking `error[E...]` output that vanishes on re-run** -- I hit both today, once on `render.rs` (`todo` "not a function" while `fn todo` was right there at line 1017) and once on an untracked `legacy.rs`. **Re-run before believing it, and never report a peer's uncommitted work as broken** -- cc has taken that as the standing convention in both directions.
- **A SWEEP DOES NOT MOVE A FILE, IT SPLITS A CHANGE.** My unqualified `--amend` took cc's test without the `store.rs` methods it calls and **HEAD did not build for ten minutes.** **After a sweep the question is not "whose file is this", it is "does it still build."**
- **`--only` PROTECTS THE COMMIT AND NOT THE AMEND**, and never CLEARS the index, so the shared index accumulates across every node. **Name paths on the amend too.** Corollary that has now cost me at folds 6, 7 and 8: **`sed -i` by LINE NUMBER shifts under its own edits, and an Edit anchored on a non-unique prefix duplicates the text after it** -- I produced "vc ruled DO NOT SPLIT: vc ruled DO NOT SPLIT:" this way. **Anchor on unique text and check the result.**
- **`index.lock` HELD BY A PEER IS NORMAL IN A FOUR-SESSION CLONE, NOT AN ERROR.** `git commit` fails with _"Another git process seems to be running"_. **Retry in a loop; never remove the lock file** -- it belongs to a live operation.
- **D42, SETTLED: it is a rule about SIGNATURES and SOURCE DOCUMENTS. I over-applied it TWICE IN ONE DAY, both times towards MORE PROHIBITION, which looks like rigour.** Forbidden: confecting a time into a source document, and no CLI or `intentsvcs` function TAKES a time as a parameter. Permitted, all hv-ruled: RETURNING a time; reading a clock to make a DECISION; stamping when a command ran into a GENERATED artefact. hv: _"there is no need to be pathological about it."_ **All three of my D42 findings fell.**
- **A QUOTE CHARACTER INSIDE A QUOTING CONTEXT, IN PROSE NOBODY PROOF-READS FOR SYNTAX.** An apostrophe in a single-quoted jq program CLOSES it -- `vc's`, then `run's`, the second inside a comment explaining a different bug, **then a third (`row's`) at fold 8, twenty lines below the comment describing the trap.** Backticks in a double-quoted string are command substitution. **Put the program in a file, and write canon prose with NO embedded double quotes** -- backticks instead, since `\"` is what broke `surface_check` through `@tsv`. **And a jq-specific form that bit twice in one stretch: a string literal inside a `\(...)` interpolation CANNOT be escaped out of the surrounding string** -- `"\(map(select(.k == \"v\")))"` is a syntax error, not a quoting inconvenience. **Bind the value with `as $x` BEFORE the string and interpolate the binding.** Both instances were measurement queries, where the cost of a syntax error is nil and the cost of the workaround-that-parses is a wrong number.
- **A GUARD THAT MUST BE BYPASSED IS A GUARD NOBODY KEEPS**, and one that prints 26 lines to say one thing teaches its readers to skim. **A report is the right shape mid-ladder: `surface_check.sh` reports findings and gates nothing.**
- **I FABRICATED A TIMESTAMP AND THE GUARD CAUGHT IT.** Bumping a heartbeat inside a batched edit, I typed a plausible value wrong by nine minutes, read off nothing. **The tell is the BATCHING**: every stamp I read in its own step was right. **`date -u` is its own step, always.**
- **NO PUSHES TO `upstream` -- hv, 2026-08-16. The CI/CD budget has been hit.** `git push local main` only; never `git push upstream main`, never a bare `git push`. **Standing, not a one-session pause.** Last commit of mine that reached GitHub: `dcf9d3b6`. **dc has since re-installed this clone's hooks so `git push upstream` is REFUSED mechanically** -- belt and braces, and the mechanism is now the authority rather than my memory.
- **ic cannot certify a green suite.** matts owns the authoritative run; everything here is evidence. **Confirmed green by hv 2026-08-16 19:32Z, the full Rust suite, after cc's 0038/0039 work and my table changes had all landed** -- so the fold-9 state is verified by the only run that counts, not by my own `cargo test`.
- **Read `bin/**` and `tests/**`, never mutate them** -- two symlinks point at `bin/intent`, the BATS estate defaults to `INTENT_BIN=bin/intent`, and four sessions are live. `native/**` and `bin/.devbin/**` are safe.
- **This repo is PUBLIC and that is FINE and intended** (hv ruled). No secrets, and a bare commit in a shared tree publishes whatever is sitting in it.
- **This shell is zsh**: no word-splitting of unquoted parameters; an unmatched glob is an ERROR that aborts the command, not an empty expansion. Never enumerate remotes through `head`. **INSTANCE 2026-08-17, with this rule already on this board: a probe loop doing `"$B" $a` produced six clean rows saying `intent --version` refuses unknown arguments -- the exact opposite of the truth.** `--version --zzz` arrived as ONE argument and clap rejected the whole token. **The giveaway was in the output the whole time: the error quoted both words inside one pair of quotes.** Probe from a bash file with real argument vectors for anything whose result gets written down.

- **A GREEN IN A SHARED CLONE MAY BE GREEN AGAINST A PEER'S UNCOMMITTED TREE** (2026-08-17). Four sessions build one checkout. My seventh test went green because cc's `export.rs` fix was sitting in the working tree unstaged -- at HEAD it was still red. `git status` before reading any pass as a fact about the commit, and say which tree a number was measured on. cc's board carries the same scar from the other side.
- **A NEIGHBOURING COMMENT IS NOT A SPECIFICATION** (2026-08-17). I read the WP parser's _"so a section the template never named survives"_ as the description of correct behaviour and proposed copying it. It is accurate about what that code conserves and silent about the order it destroys. Read the code the comment sits on, especially when recommending it as the fix for the code next to it.

- **`cargo test --lib` DOES NOT RUN `tests/*.rs`, AND `cargo test` STOPS AT THE FIRST FAILING BINARY** (2026-08-17). I reported "9/9 green" three times off a `--lib` run that never touched the integration suite, and cc found two reds in my commit that I had not seen. cc hit the same class from the other end -- `--workspace` reporting 15 legs and looking complete while three failures sat in crates it never reached. **A chosen filter and an unknown default produce the same lie: a green over a population narrowed without saying so, and neither output says which it was.** `--no-fail-fast`, and sanity-check the leg count against the last run.
- **A `tail` ON A TEST RUN CAN CUT THE FAILURE OFF THE TOP** (2026-08-17). I piped two runs through `tail -40` and read "all green" from output whose first lines were the failing lib target. The pipeline that hid it was mine and the discrepancy only surfaced because a third run disagreed with the other two. Grep for the failure; never infer a pass from a truncated window.
- **READING THE CODE ESTABLISHES NOTHING ABOUT RE-RUNNING IT** (2026-08-17). The accretion was invisible to three people reading the same two functions all afternoon, and one execution found it in ninety seconds. Where a property is about what happens the SECOND time, run it twice.
- **A COMMIT SUBJECT IS A SUMMARY, AND IT MUST CLAIM NO MORE THAN THE BODY SUPPORTS** (dc, 2026-08-17). **`f48ed6e9`'s subject reads "the cutover gate is measured passing" and its body says the gate has NOT been run under the gate's own tool.** Both halves are mine, the body is scrupulous, and the body is the half nobody reads: `git log --oneline`, a changelog, and anything quoted to hv all carry the subject alone. dc caught it by reading `--oneline` while assembling their own fold, and **the only reason they checked the body is that they already had my message** -- anyone arriving next week has the subject and not the message. **It is my own `measured:` argument pointed back at me**: a caveat you only find after you already distrust the headline arrives after it was needed. The word doing the damage is _gate_, because the gate is now a named rostered artefact and "the gate is passing" reads as "that tool returned 0". A pushed commit is not amendable in a shared history, so this line is the correction; the honest subject was "the cutover condition measures IDENTICAL on my own diff".

- **"IT COMPILES" AND "IT IS REACHABLE" ARE TWO CLAIMS** (2026-08-17). My board tracked the first and reported the second, and the gate sat blocked for a day on a compile error that had been fixed. A green build says nothing about whether a door is wired, and nothing about whether either is true of a COMMIT. Name the blocker as a state of the repository, never as an error message.
- **A RIG'S FAILURE MODE IS A FALSE GREEN, SO BUILD THE REFUSALS FIRST** (2026-08-17). Every way the interruption rig can be vacuous now refuses: a run that finishes before the threshold interrupted nothing; an exit that is not 137 means the kill never landed; a zero delta leaves nowhere to put a kill. dc's sharpening is the general form -- **a kill that lands too early writes nothing, so the re-run trivially matches and the arm reports IDENTICAL without having interrupted anything.**
- **DISCOVERY WALKS UP, SO A TEST TREE MUST CARRY ITS OWN MARKER** (2026-08-17). `Project::discover` stops at the first ancestor holding `intent/.config/config.json`. Running the migrator from inside `native/rust` reached the LIVE checkout and wrote 311 files before unwinding them. Assert the marker in the tree and refuse a workdir inside the repository -- cc's point being that a wrongly-pointed arm tends to SUCCEED rather than fail.
- **CHECKING THE END STATE DOES NOT LICENCE NARRATING THE MECHANISM** (2026-08-17). I verified the live repo was clean after the near-miss and reported that the migrator "refused and wrote nothing". It wrote 311 files and rolled them back. The verification was sound; the sentence I built on it was not.

- **A COUNT IS A GUESS ABOUT HOW FAST YOU CAN OBSERVE SOMEONE** (2026-08-17). The interruption rig replaced guessed delays with a measured file-count threshold and it still could not land a kill, because the migration writes 295 files in a 73ms burst while a tree walk costs 20ms. Where the window is short, wait on a specific artefact whose appearance you can test with one stat -- and derive WHICH artefact from the observed order of a previous run, so "late" is measured rather than hoped for.
- **A GATE'S SUBJECT MUST EXCLUDE WHAT THE SYSTEM ITSELF CALLS REBUILDABLE** (2026-08-17). The cutover comparator counted `intent/.cache/intent.db`, which the migration writes into `.gitignore` in the same run being judged, and which two CLEAN runs disagree on. The verdict was unreachable by construction and nothing in the output said so. Run the two-clean-runs control BEFORE trusting any comparison of outputs.

- **A VOID PREMISE BESIDE A LIVE CONCLUSION IS THE WORST SHAPE FOR SOMETHING NOBODY MAY CITE** (2026-08-17). I cited D29's struck derivation; vc cited it an hour later; cc rested a committed doc comment on the adjacent dead D36 having personally found the same dead reasoning four days earlier. `design.md:243` is 1400 words in which VOID appears once, mid-paragraph. Nobody skimmed. **Before citing canon in an estate with rulings that reverse, grep the entry for VOID / REVERSED / do-not-cite rather than reading to the conclusion.**
- **REPLACING A SLOW POLL DOES NOT HELP IF A FORK SURVIVES IN THE LOOP** (2026-08-17). Swapped a `find` for a `test -e` and left `date +%s` beside it, so the loop stayed milliseconds in a window that moves every 0.25ms, and two different `--fraction` values killed at the identical depth. `$SECONDS` is a builtin. **Measure the loop, not the change.**
- **"IT BLOCKS NOTHING" IS THE WRONG TEST FOR PARKING A QUESTION** (2026-08-17). The event-log timestamp question sat at item 4 all day and was the cause of the gate's only failure, while three nodes chased the symptom from outside. **The better test is whether you can say what would happen if the answer went either way.** If you cannot, you do not yet know enough to park it.
