# ic fold 38 -- archived 2026-08-19

Everything below was LIVE at the morning fold and is closed, frozen or superseded by
hv's AC-00.x freeze and the re-lane onto ST0056 WP-03. Kept whole rather than summarised.

## TODO -- LIVE ONLY, in dependency order

1. **AT-00.11 -- KEEP BOTH ARMS (vc ruled).** The impossible-ratio and negative-remainder arms are revision-independent, need no second tree, and are what the positive control scored. **The row drops the two-tree differential AS THE DRIVE METHOD, not the arms that never depended on it.** `of_n_closes_over_examined.sh` survives rather than being rewritten. **STILL GATED ON TWO: vc's row text (their first block today) and cc's go for any write under `intent/st/`.** dc's binary gate was the third and it has lifted.
2. **RE-PIN IS QUEUED, NOT DONE, AND SHOULD NOT BE A NEW HASH.** The pin lives in canon at **AT-03.15** (fenced by vc); `acceptance.md`'s copy is a generated view. **A pin proves identity, not currency** -- swapping `f2e4d1f9005d0334` for `1604836a4c4470ab` reproduces the defect one revision later on a fresh decay schedule. **dc's embedded `intent-source-commit:<marker>-<sha>` is half the fix and already ships in every binary; one `strings` would have diagnosed this morning.** **vc REFUSED MY DEMOTION OF THE ARTEFACT HASH AND THEY ARE RIGHT, ON MEASURED EVIDENCE: `dirty-18197aaf` was carried by THREE DISTINCT BINARIES IN ONE DAY, and that instance is the ORIGIN of the pin limb.** Source commit alone is ambiguous IDENTITY; hash alone is invisible CURRENCY. **The estate holds a measured failure of EACH, so neither is secondary -- TWO PRIMARIES, each with the question it answers named AT the field.** **AND MY JUSTIFICATION FOR DECLINING THE REPLACEMENT HASH WAS THE WRONG ONE (vc): I cited the `10 of 41` precedent, which is about an UNVERIFIABLE figure. This hash is perfectly verifiable -- it ANSWERS THE WRONG QUESTION. Declining a CORRECT value needs its own stated reason, or the next author reads that precedent as covering only unreliable numbers and re-pins without hesitating.** I put the wrong reason in writing to both dc and vc; this is the correction.
3. **AWAITING hv: AT-01.2 + AT-01.4 OWNERSHIP. DECLINED THE HANDOVER RATHER THAN PICKED FROM cc'S THREE OPTIONS.** Verified off canon myself: both declare `.sh` under `intent/st/ST0057/parity/tools/` (which does NOT exist); the group is 4 Rust in `intentsvcs/tests/`, 1 shell-hook, 2 shell-parity. cc's drafts are Rust -- wrong kind, wrong directory. **THE CONTRACT IS RIGHT AND THE REASON IS STRONGER THAN `IT LOOKS DELIBERATE`: the 4 Rust rows test in-process `intentsvcs`; 01.2 and 01.4 test GIT'S behaviour, and observing git from Rust means shelling out to git from Rust. The KIND TRACKS THE OBSERVABLE.** **BUT THE OWNER DOES NOT FOLLOW FROM THE KIND** -- `parity/tools/` is a SHARED harness (dc wrote `of_n_population.sh`), so `shell parity tool, therefore ic's` infers an owner from an artefact property, which is the morning's shape again. **MY DECIDING FACT WAS FALSE AND I CORRECTED IT TO BOTH NODES: cc DOES CLAIM ST0057/01**, the very WP those rows sit in. At HEAD their board reads `[ST0056/10]`; on disk at their `09:19Z` heartbeat it reads `[ST0056/10, ST0057/01]`. **My pickup was 08:53Z -- I asserted their CURRENT claims from my PICKUP-TIME read.** **A CLAIMS BLOCK IS LIVE STATE; READING IT ONCE AT PICKUP IS READING A SNAPSHOT, AND AN ASSERTION ABOUT WHO CLAIMS WHAT _NOW_ MUST RE-READ AT ASSERTION TIME.** So the shape is the WP CLAIM-HOLDER offering out their OWN scope, not two unclaimed nodes trading -- much closer to `unclaim`+`claim`, which needs no hv. **`It is not close` was wrong.** **I will write them in shell if hv assigns them; the assignment is not cc's to hand over nor mine to take.**
4. **PARK RELEASED BY cc, AND THE MOVE STILL DOES NOT HAPPEN.** Their next commit under `intent/st/` invalidates the courtesy anyway. **`of_n_closes_over_examined.sh` stays `.pending` for an UNRELATED reason: AT-00.11 is still gated on vc's corrected row text.** Two gates; only one was cc's.
5. **OWED TO ME BEFORE matts: `intent sync` HAS NO SCOPE** -- vc routes it to me for SHAPE first, then matts for the yes.
6. **EXP-09 + the guard-population hole + the clap short-circuit ARE ONE ENTANGLED ENTRY.** Needs a RULING, not an AC. **ROUTED VIA vc 2026-08-19 AND IN FRONT OF matts. I was carrying it as an unrouted personal failure and it was NOT one** -- hv ruling today, AS CORRECTED: **THE INBOX STAYS AS THE DURABLE RECORD; WHAT WAS BROKEN IS THE RESOLUTION PROTOCOL** -- nodes wrote expecting hv to read out of band, which he cannot do. **vc MONITORS all four hv inboxes and surfaces to him in chat.**
7. **ROUTED VIA vc, NOT OWED AS A FILE-WRITE:** the roster globs `*_check.sh` only, so 26 of 43 tools can never hold a row -- including `interrupt_rig.sh`, which AC-00.10 is entirely about. **The naming convention is doing the work a declaration should do.** Found by dc, explicitly handed to me to route, framing mine. **KEEP WRITING TO `hv/inbox.ic.md` -- the channel STAYS (hv corrected vc's over-read within the hour).**
8. **STRUCTURAL, raised twice, not re-raising unprompted: my work sits under no WP**, so none of it counts toward the 3.0.0 gate.

**RULED CLOSED -- do not re-open:** the 29 register rows. Fix 2. The hoist pin. The todo-glyph defect. The `18 of 24` ratio. The rig's out-of-workspace `CARGO_TARGET_DIR`. **The `att_dir_of` defect -- dc fixed it at `3f218744`, which IS an ancestor of HEAD.**

## Open with others

- **vc** -- holds ST0056 + ST0057 and the mint pen. Minted 8 rows (123->131 criteria, 124->132 tests), several carrying my formulations. **Owes AT-00.11's corrected row, held deliberately so the partition's evidence could improve it.** **They commit my instrument and re-sync canon -- I did NOT commit it myself, so canon's stale copy has a named owner.** Their `sync --to-store` ingested my in-flight bytes twice today; second instance of the unscoped-sync hazard.
- **dc** -- `of_n_population.sh` committed clean at `0b484c58`, now prints both looseness lists by name. **Gatekeeper mechanism RETRACTED by them; my withdrawal stands on grounds that never depended on it.** The apparatus meter is joint and unbuilt.
- **cc** -- released the `intent/st/` park. Their AT-01.2/01.4 handover was withdrawn; they claim ST0057/01. Their kind-hole finding became AC-00.15/AT-00.16.
- **hv** -- 9 asks written to `hv/inbox.ic.md` (durable) and surfaced by vc in chat. **AT-01.2/01.4 assignment and the no-WP question are the two live ones, and they must be decided SEPARATELY.**

## ROUTED, AWAITING NEXT CYCLE

- **THE SYNC PRECONDITION: `sync READS DISK` IS FALSE, AND I READ THE SOURCE.** `Project::thread_files` (`crates/intentsvcs/src/project.rs:701`) builds `ignore::WalkBuilder` with **`.git_ignore(true)` and `.parents(true)`** -- **SYNC HONOURS .gitignore.** So a repo-ignored file is invisible to porcelain AND to sync; the two readers cannot disagree in that direction, and **my morning porcelain-blindness finding does NOT transfer to this question** (vc thought it did; so did I until I read it). **THE REAL DOOR IS THE OPPOSITE ONE: `.git_global(false)` and `.git_exclude(false)`** -- sync does NOT consult the user's global excludes, and **git DOES**. `core.excludesFile` is live here (`~/.gitignore_global`, 24 patterns); `.git/info/exclude` empty. **BUT `ATTACHMENT_EXTENSIONS` (`project.rs:41`) IS EXACTLY `["md","txt","sh"]` AND NOT ONE OF THE 24 PATTERNS MATCHES THOSE THREE. THE INTERSECTION IS EMPTY, SO THE PRECONDITION IS SOUND** -- and sound for a reason neither vc nor I gave. **I asserted the precondition without ever establishing porcelain and sync see the same set; I assumed it. vc challenged the assumption on a mechanism that does not exist, and the challenge was right anyway.**
- **THE CRITERION IS THE DISJOINTNESS, NOT A SECOND CHANNEL.** Soundness rests on two INDEPENDENTLY MAINTAINED LISTS nothing checks against each other: a Rust const, and **a dotfile in a home directory that is not in the repository at all, so no CI can ever see it.** Add `"log"` to the const, or `*.md` to any contributor's global ignore, and the precondition silently goes unsound with nothing going red. **DO NOT flip `.git_global`/`.git_exclude`: those settings are RIGHT -- they make sync machine-independent, which D34 requires. The divergence is the price of a correct decision.**
- **ATTRIBUTION I REFUSED: vc credited me with a worktree-vs-`git ls-files` instrument. I did not build it.** `ls-files` appears in `lib_classify.sh`, `generator_inputs_check.sh`, `runner_roster_check.sh` -- none mine -- and on vc's, dc's and cc's boards, not mine. **A mechanism attributed to a node who cannot produce it is a pin with no revision in different clothes.**
- **43 `.sh` in parity/tools, not 45** (vc's count).

- **AT-00.16 CANNOT CATCH THE CASE IT CITES, AND I FOUND IT BEFORE ANYONE BUILDS TO IT.** The row compares an AT row's declared artefact against the kind its path implies -- **both sides of that comparison come from the ROW**, so it only catches a row internally inconsistent with itself. **cc's actual case was a wrong-kind artefact at a path NO ROW MENTIONS: the declared path stays empty, `to-write` stays literally accurate, and there is no transition to detect.** That is direction 1 of the gate-visibility defect -- an orphan with no row -- **so the kind hole and the 29 unrostered instruments are one defect from two ends and the row looks at one.** Owed: a second mutant, build the right behaviour at an undeclared path and require the estate to notice. **My prediction, lodged: it cannot go red under AT-00.16 as worded.**
- **THE GATE SEES 6 OF 43, AND THE FICTION POPULATION IS A FLOW, NOT A STOCK.** 43 instruments on disk; 21 cited; 6 cited AND on disk; 15 cited but absent; 29 real instruments with no row. **It went 9 -> 15 in the mint that recorded the defect** -- it rises with every good criterion, fastest when the estate is most rigorous. **One mint is one datapoint, not a rate.**
