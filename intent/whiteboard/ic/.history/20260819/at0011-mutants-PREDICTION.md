# of_n_closes_over_examined.sh -- mutant predictions, lodged BEFORE the run

# Mutating the CRITERION, never the exemplar. An instrument tuned until the real

# estate passes is an exemplar-matcher wearing a criterion's name.

M1 impossible ratio `EXAMINED 2 of 1` -> FINDING (IMPOSSIBLE), exit 1
M2 negative remainder `The other -1` -> FINDING (NON-CLOSING), exit 1
M3 honest, identical in both trees -> no finding, STABLE, exit 0
M4 honest but ratio MOVES between trees -> no finding, DIVERGED (nomination), exit 0
R1 empty population -> REFUSE exit 2, and must NOT say "all match"
R2 the "flat" tree is actually nested -> REFUSE exit 2 (identity established, not inherited)
R3 every subject unmeasurable -> REFUSE exit 2, never "nothing is wrong"
