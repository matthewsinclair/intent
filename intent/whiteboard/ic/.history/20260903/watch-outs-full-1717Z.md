# ic watch-outs, FULL TEXT as at 2026-09-03 17:17Z, before the fold cut the worked examples

**THIS FILE IS A CONVENIENCE EXTRACTION AND IS WHITESPACE-NORMALISED BY THE FORMATTER. THE BYTE-EXACT RECORD IS `wip-prefold-1717Z.md` IN THIS DIRECTORY (sha256 `c6db63c78cd325b4...`), WHICH HOLDS THE WHOLE BOARD INCLUDING THESE.** Said explicitly because `.prettierignore` exempts `intent/whiteboard/*/inbox.*.md` and does NOT exempt `.history/`, so an archive is gate-checked even when its source never was -- see ic W25.

**THE LIVE BOARD KEEPS EVERY MECHANISM AND EVERY NUMBER. What was cut is the WORKED EXAMPLE under each -- the episode that produced it.**
A mechanism is re-usable without its example; the example is what makes the section unreadable. Nothing here is superseded, so nothing here was deleted.

---

## Watch-outs -- mechanisms only

1. **Non-test AC closes via `intent ac satisfy --evidence`, NEVER an AT; test AT rows close via `at green`, non-test ones via `at na`.** Re-drive + positive-control before satisfying. Source an existing note from the JSON extract (`jq -rj`), never `acceptance.md` (prettier is a second writer + the row carries a 114-char prefix). Assert contains-original-verbatim AND longer AFTER the write. **`at edit --note` REPLACES OUTRIGHT and is the deliberate door.**

   **THIS ENTRY SAID `at green --note` REPLACES THE NOTE WHOLESALE AND SILENTLY, AND THAT IS NO LONGER TRUE (0207, landed by a peer at `e331b7c8` TODAY).** The status verbs now REFUSE a `--note` that would drop the existing one, name the byte counts, quote what would be lost, and offer both remedies. **Measured, not read: I tried to extend `AT-17.18` and was refused.** The fix is better than the discipline it replaces -- a rule I had to remember became a guard that cannot be forgotten. **AND THIS IS WATCH-OUT 13 LANDING ON MY OWN BOARD WITHIN HOURS OF MY WRITING IT: a premise carries a version, my watch-out was true of the morning's binary, and it read identically after it stopped being true.** A watch-out is a claim about a build like any other.

2. **Canon on SHARED threads: SERIALISE ST0056 writes through vc; gather evidence OUTSIDE the window, make the window ONE coherent commit.** 0206 (concurrent facade verbs clobber), 0210 (a dirty parity/tools file blocks EVERY node's canon commit via the extract union, REPO-WIDE), 0212 (watcher ingest reverts a store write when disk lags; verify DISK not store before commit).

   **AND A DIRTY CANON FILE DOES NOT TELL YOU WHO WROTE IT** (cc's finding, 2026-09-02, and it is not derivable from my own side). **Canon on disk is a PROJECTION of the DB, so a canon-touching verb run by ANY node flushes whatever is pending in the store -- including YOUR writes -- onto disk under THEIR process.** cc's `issues close` is what put my `ST0056` bytes in the working tree. **`git status` is silent about the difference and the file list cannot answer it: only the DIFF can.** So read the diff before committing canon you did not expect to be dirty, and never assume a canon file you did not touch is a peer's -- **it may be yours, arriving by someone else's verb.** cc read the diff for exactly this reason and left mine alone.

3. **The macOS app is `native/macos/Intent/` (Swift, xcodegen, folder-GLOBBED).** `bin/devbin macos app-build` unsigned Debug off-tree; `app-test` the suite. IntentCLI = the one shell-out home. NOT `dvb test rust`.
4. **A value/claim carried ONE STEP past what it supports is the recurring class, and it is MINE too.** Hand/take PROPERTIES not values; re-measure at pickup; a retraction carries the same burden as the claim it retracts.
5. **`git commit --only <explicit paths>` is the only safe write** on this 5-writer tree (git add new files first). NEVER remove a peer's index.lock (WAIT+retry -- hit twice today, cleared in 3 and 11 tries). cc's POST-VERIFY IS STANDING: `git show --name-only HEAD` == your set. Every stamp from a `date -u` read THIS turn.

   **AND I BROKE THIS AT 21:55Z BY THE ONE ROUTE THE RULE DOES NOT DESCRIBE: A REFUSAL-AND-RETRY.** The gate refused my board for unformatted markdown -- a reason with nothing to do with staging -- I ran prettier, and then rebuilt the command as `git add` + PLAIN `git commit` instead of re-issuing `git commit --only`. **Plain commit takes the index AS IT STANDS and it took vc's staged `0216.json` with it** (`4c940ef6`, under my message and my session trailer). Nothing was lost and the gate ran green over it, but the commit record is misattributed and I told vc immediately.

   **THE MECHANISM IS THE KEEPER, NOT THE INCIDENT: A RETRY FEELS LIKE A CONTINUATION AND IS ACTUALLY A FRESH COMMAND.** I had the safe form, was refused for an unrelated reason, and re-derived the command from scratch -- which is where the discipline fell out. **A refusal is exactly where a rule gets dropped, because the second attempt is composed under the impression that the first one's care is still attached to it.** `--only` re-reads the working tree, so it was available and correct AFTER prettier and needed no rebuilding at all. **cc's POST-VERIFY is what caught it** -- the set did not match, one line of output, and it is the only reason this is a report rather than a discovery someone else makes days later.

   **AND IT HAS A SECOND INSTANCE UNDER A DIFFERENT GUARD, WHICH IS WHAT MAKES IT A CLASS RATHER THAN MY INCIDENT.** vc kept the diagnosis, and then the CLOCK guard refused one of their commits minutes later and did the same thing to them. **Two independent nodes, two unrelated guards, same shape** -- so the trigger is not the markdown guard and not carelessness about staging; it is that a refused command gets recomposed rather than re-issued. **The cure is mechanical and needs no memory: after ANY gate refusal, re-issue the SAME command, changing only what the refusal named.**

6. **`intent --version` names the commit the BINARY WAS BUILT FROM; CURRENCY is that against `git diff --name-only <that>..HEAD -- native/rust`.** Only the second decides staleness. **AND THE `-- native/rust` SCOPING IS LOAD-BEARING: the embed is the LAST COMMIT TOUCHING `native/rust`, NOT HEAD** -- a fresh build of a clean tree embedded a sha ten board commits back, every one touching zero Rust. **So `embed != HEAD` is NORMAL on a board-heavy day and proves nothing.** I raised it as staleness and the refutation was already in the same terminal ([[feedback_the_disproof_is_usually_already_in_hand]]).
7. **A MARKDOWN ATTACHMENT HAS TWO WRITERS -- ORDER: prettier FIRST, let the store settle, verify, THEN commit.** The pre-commit gate refuses unformatted markdown, so prettier can rewrite a file AFTER the store has taken your version ([[project_formatter_is_a_second_writer]]). **AND RE-DRIVE ANY TEST THAT PARSES THE FILE: prettier ALIGNS markdown tables.** "It happened to still parse" is a thing to measure, not assume.
8. **A TEST WRITTEN FROM ONE OBSERVED FAILURE INHERITS THAT FAILURE'S INCIDENTAL SHAPE, AND THE INHERITANCE IS INVISIBLE BECAUSE THE TEST PASSES ON THE CASE THAT MOTIVATED IT.** **8x: ANY STATE THE HARNESS ITSELF MOVES IS NOT EVIDENCE THAT THE THING UNDER TEST MOVED IT.** Cure: a baseline that ALREADY CONTAINS the bookkeeping. **Live again today from the other side: a first-occurrence search over a log containing SETUP measures the setup** -- locating the first `enter_raw` found the initial take rather than the re-take, so a CORRECT implementation failed. It fails in whichever direction the setup happens to sit, so the direction of the error carries no information either.
9. **A THING DECLARED, WRITABLE, VISIBLE AND READ BY NOTHING IS THE RECURRING DEFECT ON THIS SURFACE.** `Hotkey` (hv's find), `Command::group` (MINE, added in the same commit as the paragraph condemning `Hotkey`), and `explorer.editing.mode` would have been the third. **The cheap check is a grep for consumers of every field you declare, at the moment you declare it** -- no invariant catches this class, because a field with no reader breaks nothing. **INVERTED FORM, and it is the one with no cure but the compiler: a DEFAULT method on a trait with FORWARDING implementations is a trap.** A forwarder that inherits a default where it should have passed on is correct in isolation at every site; required methods make the compiler find all of them, a default makes it find none.
10. **A SHARED FILE CAN CARRY A PEER'S HUNK, AND `--only` COMMITS THE WHOLE FILE.** Cure is hunk-level staging, non-interactively (`git add -p` is unavailable here): `git diff -- <file>` to a patch, split on `@@`, drop the hunks that are not yours, `git apply --cached` the rest, `git add` your other paths, then **plain `git commit`** -- `--only` re-reads the WORKING TREE and would undo the selection. Index must be clean of peers first.
11. **A DEFECT'S BLAST RADIUS IS NOT ITS PACKAGE.** A base-crate signature moves and `cargo check -p <their crate>` stays green throughout; a generated `ddl.sql` fails tests in a package that did not change. **The node who can SEE it is not the node who caused it.** `cargo test --workspace` is the instrument that sees it and `-p` is the one that does not.
12. **THE SHARED-ARTEFACT GUARD REFUSING A REBUILD IS THE GUARD WORKING.** `bin/devbin build all` REDIRECTS to a private target dir when `native/rust` is dirty. **Wait for the peer; never force** -- forcing compiles their uncommitted work into the binary hv drives (`dirty-<sha>`). Measure delivery by SHA (symlink target vs built file), never by the marker. **AND THE "REBUILD WINDOW IS hv's" PREMISE IS EXPIRED (vc, 20:24Z, driven): `0196` is a DEFECT, not an authority constraint** -- `guarded_release_build` deletes the shared pair before it builds and no failure path restores it, so a FAILED build leaves every live session on this machine without a binary. **The real constraint is a QUIET TREE, not hv's permission.** I rebuilt twice today without asking and both were right; two nodes were reporting the wrong model upward while the correct one was in my hands. **A constraint nobody re-derives becomes an authority claim.**
13. **A PREMISE CARRIES A VERSION IN THIS ESTATE, AND A RE-DRIVE AIMED AT THE CITATION PASSES** (vc's `8an`, on my defect). I put a citation naming a file that does not exist into a contract proposal, lifted from my own draft five days older -- **and the SUBSTANCE was true anyway**, so minting would have been right for the wrong reason and rejecting would have lost a true row. **Neither verdict was available without driving it.** The quieter half: _upgrade cannot rescue it_ cited a line that is still present, still says exactly that, **and is v2**; in v3 `Payload::sync` has ONE caller, so nothing automatic runs it and the correction made the defect WORSE. **THE CLASS IS NOT ORDINARY STALENESS: the evidence STILL VERIFIES. A re-drive checking the CITATION passes; only one checking the SUBJECT fails** -- it survives "re-drive your citations" intact. **This estate is a port, so every inherited claim carries a version and establishing WHICH is the first move.**
14. **DRIVE A DESTRUCTIVE QUESTION IN A SANDBOX OR DO NOT DRIVE IT.** The `sync`-prunes question needed a skill installed and its canon source deleted -- against real state that is `~/.claude/skills/` and the live manifest, ie an incident rather than a measurement. `intentsvcs/tests/skills_sync.rs` carries a disposable install tree, Claude target and manifest. **A measurement that mutates shared durable state is not one anyone can afford to repeat**, and repeatability is most of its value.
15. **`ok:` IS THE TOOL'S REPORT, NOT EVIDENCE THE ROW SURVIVES (`0216`, HIGH). DRIVEN AND CENSUSED BY vc ON cc's DESIGN, 2026-09-02 21:4xZ -- MY OWN READING OF IT WAS WRONG IN TWO PLACES AND BOTH CORRECTIONS MATTER AT THE KEYBOARD.**

    **IT IS THE LAST WRITE OF A BURST, NOT THE THIRD. cc had this A PRIORI, before any evidence existed** -- a process exiting before its write settles exposes the final one and needs no positional mechanism. My board said the repeating ordinal was a stronger signal than the count; at N=3 the third and the last are the same row, so my three samples never discriminated them and I read a coincidence as a mechanism. **The real derivation is mechanistic: `watch.rs:51` sets a 250ms quiet period, a debouncer RESTARTS that period on every event, so a burst pushes the ingest to 250ms after its LAST write.** Any reproduction must use N != 3.

    **AND THE WRITE IS NOT LOST -- IT LANDS AND IS THEN REVERTED.** `event_log` shows the `at.put` for both of my episodes, followed ~1.05s later by `disk.sync_from_disk` under `intentd`, a DIFFERENT actor. Two writers, second wins. **So the `ok:` was honest**, which is why no discipline aimed at distrusting the tool's own report could have caught it.

    **THEREFORE MY DISCIPLINE ABOVE WAS NECESSARY AND NOT SUFFICIENT: A READ-BACK THAT VERIFIES PROMPTLY CAN SEE A ROW THAT IS ABOUT TO BE REVERTED.** Read back every id AND verify PAST the ingest window, not just after the call.

    **THE FIGURE: 15 at-risk across the store's ENTIRE LIFE** (the db was born 2026-08-18, so nothing is dark). One live loss found and repaired (issue `0202`, a triaged not-a-defect whose close was reverted and which sat open 37 hours); five lost and repaired by hand at the time, **two of them mine** (`AT-00.7`, `AT-00.8`); nine in their intended state, of which six are fully determined by their payloads, three are consistent-with only, and `AT-00.3` is UNVERIFIABLE by this method. **15 has a reason and the reason is CONTINGENT: the tightest write-to-write gap inside any at-risk burst is 0.251s against a 0.250s quiet period, so "only the last is exposed" holds by one millisecond of CLI slowness and a faster writer under-counts.**

    **THE OPEN FORK IS cc's AND IT DECIDES WHICH DEFECT THIS IS:** at ingest time, did the DISK file carry the row? Missing -> a store-commit/disk-flush ordering race and the debounce is incidental; present -> the ingest is wrong and the flush is innocent. **The log records that the ingest RAN, not what it READ.**

    **AND THE BELIEF OUTLIVES THE ROW**: all three of us wrote commit messages from `ok:` lines, so a reverted row leaves a durable FALSE record in git that no later reader has reason to re-check. **`event_log` is the only provenance surface in the store -- the ingest rewrites rows wholesale, so all eight `ST0065` test rows carry an identical `written_at` and no row-level timestamp here is provenance for anything.**

16. **BACKTICKS INSIDE A DOUBLE-QUOTED SHELL ARGUMENT ARE COMMAND SUBSTITUTION, AND THE FAILURE IS SILENT AND PLAUSIBLE.** A `--note` passed inline lost both its citations to it; the row was created, the tool printed `ok`, and the note read perfectly well with the two facts it existed to carry simply gone. **`intent init` ACTUALLY RAN against this project** -- it refused because the project exists, which was luck rather than design: a different verb would have executed against the live tree with no refusal to warn me. **Anything non-trivial goes to a file via a QUOTED heredoc and reaches the CLI as `"$(cat …)"`** -- the same cure dc reached for the apostrophe trap. Sibling of the zsh traps already known here: unquoted `$var` does not word-split, and an unmatched glob aborts the whole command.

17. **A PROBE WHOSE FIELD NAMES ARE GUESSED RETURNS `absent` FOR BOTH _not there_ AND _not where I looked_, AND THE TWO ARE INDISTINGUISHABLE IN THE OUTPUT.** Verifying `AC-00.8` I read `c["satisfied"]` and `c["evidence"]` out of the canon extract, got `None` and `0 bytes`, and had a loss report half-written -- **against a store that said `satisfied: yes` and a file that had been rewritten one minute earlier.** The real path is `c["state"]["is"]` and `c["state"]["evidence"]`. **The false negative looked EXACTLY like the `0216` revert I was watching for**, which is the trap: I was primed for a specific failure and my instrument produced its signature for an unrelated reason. **Cure, and it is the same one this estate keeps arriving at: dump the keys of a record you KNOW is in the wanted state before asserting over a record you are unsure about.** One `sorted(c.keys())` against `AC-00.1` would have cost nothing and settled it.

18. **ONE LOCAL MEASUREMENT BECAME AN ESTATE-WIDE CLAIM BY MY RECALLING A KNOWN FAILURE SHAPE AND REPORTING IT AS A READING (2026-09-02 23:2xZ, corrected by dc).** Arm 10 refused MY commit over a staged `bin/.devbin` re-vendor. That is the whole of what I measured. I told hv the gate was blocking EVERY node -- reasoning from `0210` (the gate reads the shared INDEX, so a peer's staged file blocks everyone), which is a true mechanism I had already written down. **The claim was false when I made it and refutable from commits already in front of me:** dc landed `3c59c966` and I landed `7ef65d86` inside the window I had declared blocked. dc was refused by the MARKDOWN FORMATTER over their own two files, never by arm 10. **I also attributed the staged file to dc on lane reasoning; `bin/.devbin` is VENDORED and it is devbin-vc's** -- and since every node commits as the same person, `%an` cannot discriminate us, so ownership here is a question about the LANE and the answer is in the tree, not in inference. **The shape: a remembered mechanism supplies the generalisation, and because the mechanism is real the conclusion never feels like a guess.** Cure is cheap and I did not spend it -- `git log --since` over the window would have shown peers committing through my "estate-wide" block. **Distinct from watch-out 15's premise-carries-a-version: nothing here was STALE. The mechanism was current and correctly recalled; the error was applying it to a population I had not measured.** Related: dc's own W53, a red about the ENVIRONMENT read as a red about the SUBJECT.

19. **TWO HALVES EACH CORRECT ABOUT ITS OWN DIRECTION, AND NOTHING OWNING THE RELATIONSHIP BETWEEN THEM (2026-09-03, hv found it).** `Omnibox::pick_move` walks an index over a best-FIRST list; `run::dropdown` renders best-LAST. Each documents itself accurately. Each is right. **Every screen-down keypress walked the caret UP**, and the defect is invisible from either file -- **it exists only at the seam, so no amount of reading either half finds it.** Two multipliers: one renderer serves BOTH vocabularies and says so in its own header, so the bug hv hit in the palette was equally live in the omnibox where nobody had been; and the right fix is a single declaration both sides derive from, because **inverting a boolean at each call site fixes the symptom and rebuilds the defect** -- two places obliged to agree, with nothing holding them to it. **Generalises past this file: when a bug survives a careful reading of every part, stop reading parts and ask what owns the relation.**

20. **A TEST CAN BE GREEN BECAUSE OF THE BUG, AND THAT IS WORSE THAN NO TEST BECAUSE IT READS AS COVER.** Two existing tests armed `MENU + Move` with `Down` and only appeared to move while the mapping was inverted -- the palette opens on the best match, which is the BOTTOM line, so `Down` is correctly inert there. **Neither looked wrong.** A missing test is visibly absent; a false one is visibly present (vc's framing, and the sharper half).

    **AND MY OWN CONTROL WAS NEARLY WORTHLESS IN THE SAME HOUR: A RED-THEN-GREEN PAIR PROVES NOTHING IF THE INSTRUMENT CHANGED BETWEEN THE RUNS.** My first test pressed `Down` from a position where `Down` is correctly a no-op, so I edited the test -- leaving a red and a green either side of TWO edits. **Cure: drive the FINAL form against the UNFIXED state.** vc has taken this into invariant 10 as a second axis: theirs is what a control CONTAINS (a positive it must find, a negative it must not match), mine is WHEN it runs, and **a control can be correct on one axis and worthless on the other.** Same family as watch-out 8 -- the harness moved, so the reading was not about the subject.

21. **MEASURE WHAT EXISTS BEFORE COSTING A REPLACEMENT: THE ASK NAMES A SYMPTOM AND THE CAUSE IS A GUESS UNTIL DRIVEN.** hv asked for open threads at the top of the explorer list; `index_order` has sorted open-before-closed all along and the only thing missing was a VISIBLE BOUNDARY. I had started sizing a sort. **One read of the function I was about to duplicate was the whole cost of not duplicating it** -- Highlander arriving from the other end: not _do not write a second copy_ but _check whether the first already does it_.

    **SIBLING, FROM cc ON THEIR OWN DEFECT (2026-09-03): AN EVIDENCE BAR HELD FOR ONE KIND OF CLAIM AND NOT ANOTHER, IN THE SAME SESSION.** They reconstructed a count and captioned it _a SECOND READER, not the gate's arithmetic_ -- correctly -- then four hours later published a t-shirt SIZE off a rendered `--help` with nothing driven. **A size is a claim about the world exactly as much as a count is**, and the fact that only one of them wears a number is what makes the asymmetry invisible. Mine to watch too: I size in t-shirts here and cite figures with commands.

22. **A MARKER THAT IS BOTH A STRUCTURAL ANCHOR AND ORDINARY PROSE WILL BE FOUND IN THE PROSE (mine, during this very fold).** I spliced this board by `index()` on a section heading, and my new banner mentioned that section by name in a sentence -- so the splice landed inside my own paragraph, duplicating two sections and truncating the banner mid-line. **Restored from the pre-fold snapshot; nothing lost, which is the entire reason the snapshot is taken BEFORE the first edit rather than after a good one.** Cure: anchor on the FULL heading line and assert the anchor occurs exactly once before using it. **The protocol already knows this class from the other side** -- `whiteboard-header-guard.sh` deliberately never scans prose, because nodes report these defects to each other by quoting them and a scanner would make reporting an offence.

23. **I WROTE A HOLD WHOSE CONDITION ASSERTED A PROPERTY OF A PROGRAM I HAD NEVER READ, AND IT STOOD FOR A DAY (mine, 2026-09-03).** Hold 5 said `skills sync`'s `4 need a decision` was _a standing count with no visible subject_. **The source says otherwise and always did**: `render.rs:7411` prints the total directly beneath a per-skill line that names each skill and its reason. One `sed` refuted it; I had run nothing and needed to run nothing.

    **THE TELL WAS THE SHAPE OF THE CONDITION.** _Releases when someone runs it in daylight_ made the release depend on an ACTION BY A PERSON, so every pickup that re-read the hold re-checked availability-of-a-person and never re-checked the CLAIM. **A condition phrased as somebody's future act is unfalsifiable at pickup by construction** -- which is the opposite of what the Holds section is for, since the whole point of a condition over a dependency is that I can check it myself without asking anyone.

    **AND IT MADE US BOTH MISPRICE THE RISK.** vc and I each declined to run the verb at 23:00 because it mutates. It does -- but not here: the four are precisely the outcomes a bare `sync` HOLDS. **We reasoned about the verb's class instead of its behaviour**, and the behaviour was one read away. Cheaper sibling of W21: measure what exists before costing a replacement -- here, read what it does before costing a run.

    **REFINED BY vc WITHIN MINUTES OF BEING WRITTEN, AND THE REFINEMENT IS BETTER THAN THE MINT: THE AXIS IS ACT VERSUS STATE, NOT MINE VERSUS SOMEONE ELSE'S.** A completed EVENT is fine -- `AC-07.7` waits on a published tag, names another node's work, and is already checkable. **What fails is an ACT, because an act leaves no trace until it happens.** vc found the second instance on their own board immediately (_hv authorises a daemon-down window_, whose real claim is _an intentd is running_, which vc can check alone), so the warrant is two boards rather than my argument.

    **AND THE SHARPER HALF: AN ACT-SHAPED CONDITION DOES NOT MERELY FAIL TO BE CHECKABLE, IT SUBSTITUTES A CHECKABLE-FEELING QUESTION FOR THE REAL ONE.** _Has hv done it yet_ feels exactly like checking the hold, which is why the claim underneath survives every pickup that nominally examines it. That is the silence, and it is the whole mechanism.

    **THIRD INSTANCE, FOUND BY AUDITING MY OWN HOLDS AGAINST IT: THE SECTION LEAD ALREADY SAID _not who I am waiting on_ AND THREE OF FOUR ENTRIES NAMED WHO.** **A rule stated at the top of a list is read as a property of the list**, so the paragraph asserting the discipline is what made the violations invisible -- the document-scale twin of a green test that passes because of the bug (W20). Holds 1/3/4 restated; 2 was already correct.

24. **A PARTIAL VIEW OF A RECORD READS EXACTLY LIKE THE RECORD (mine, 2026-09-03, twice in one episode and in opposite directions).**

    **DIRECTION ONE -- I NEARLY FILED A DEFECT THAT DID NOT EXIST.** I grepped `acceptance.md` for `AT-17.18`, read _NOT YET DRIVEN ... nobody has run it_ next to an `AC-17.18` marked satisfied, and had the contradiction written before I checked. **The note's very next sentence is `SUPERSEDED BY THE DRIVE BELOW`, followed by hv's full capture.** grep returns a LINE; a record's field is longer than the line, and the tail is exactly where a correction lives. **The disproof was one read against canon** ([[feedback_the_disproof_is_usually_already_in_hand]]) -- and note the asymmetry: reading the head of a stale-looking field feels like evidence, while reading the whole field feels like diligence nobody needs.

    **DIRECTION TWO -- MY OWN FOLD PRESERVED A VALUE THAT HAD GONE STALE THE DAY BEFORE.** The `AC-17.17`/`17.18` Decision said `AC-17.18` was _open_. hv drove it 2026-09-02; I folded on 2026-09-03 and kept the entry. **Correctly kept, too** -- the RULING (a row with two subjects cannot state its own reach) is live, so _fold on execution, never on date_ says keep it. **But a live ruling can carry a dead VALUE, and a fold checks the ruling.** That is a gap in my fold discipline that no amount of folding care closes, because the check the fold performs is the wrong check for that field.

    **THE FIX IS THE SAME BOTH WAYS AND IT IS NOT _READ MORE CAREFULLY_: A STATUS WORD INSIDE PROSE IS A SECOND HOME FOR A FACT THE TOOL ALREADY OWNS.** `open`, `satisfied`, `not driven` -- each is one `ac list` or `at list` away, and writing it into a sentence creates the copy that drifts. **Cite the verb, not the value** (the board rule already says NO FIGURE HERE IS EVIDENCE; this extends it from numbers to STATES).
