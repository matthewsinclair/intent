# ic -- archived 2026-08-25

## DOING (closed)

**(2026-08-25) `CLAUDE.md`'s VERSION DEFECT -- hv RULED IT, vc ROUTED ME THE VOICE AND THE REGENERATION, AND THE REGENERATION HAS NO VERB.** cc fixes `lib/templates/llm/_CLAUDE.md` (`:3` prose, outside the preserved region at `:47`/`:50`; `:54` footer). Mine is the generated view plus the consumer-facing voice. **Driven at `119387df`, not transcribed.**

- **`:54` IS PROVENANCE AND `:3` IS A CURRENT-STATE CLAIM, AND vc's FOOTER ALIGNMENT ONLY REACHES ONE OF THEM.** _Generated by Intent vX_ ages into a fact; _this project USES Intent vX_ is false the day after any upgrade until somebody regenerates. **I agreed vc's Highlander call on `:54` (align to `_AGENTS.md:268`'s agentive form) and recommended cc DROP the version from `:3` entirely** -- the sentence's job is to point at `AGENTS.md` and it does that with no number. **Nothing reads it: `This project uses Intent` is 0 hits across `.rs`/`.sh`/`.bats` outside `lib/templates/`.**
- **THE EXHIBIT IS THREE DISAGREEING CLAIMS, AND ONE IS BEYOND REGENERATION'S REACH.** `:3` `v2.19.0`, `:82` `v2.11.0 (current)`, `:92` `v2.19.0` plus a date, against a project at `3.0.0-dev`. **`:82` sits INSIDE `user:start`/`user:end` (`:47`-`:88`), so regeneration cannot touch it and must not** -- a version in prose has a home the renderer is FORBIDDEN to write, which is the whole argument for `:3`. Separate hand edit, declared.
- **~~BLOCKING AND IT IS MY LANE: NO VERB REGENERATES `CLAUDE.md`.~~ WRONG, AND CORRECTED BY ME SAME DAY: THE VERB IS `intent claude upgrade` AND IT IS UNWIRED IN v3.** Its `--help` is _Apply Claude canon to the project_ and its `--force` reads **_Overwrite user-edited canon files (CLAUDE.md without the marker, and `.intent_critic.yml`)_** -- **the surface names the file itself.** Driven with no `--apply`, nothing written: **`error: `claude upgrade` is a known command that is not implemented yet`, rc=2**; `render.rs:3084` wires `hook`/`rules`/`skills` and `upgrade` falls to `unwired`; control `claude rules list` rc=0. **SO IT WAS NEVER NEW SCOPE -- wiring an advertised, v2-shipped verb is PARITY work.** **hv RULED: wire it, as a ROW, NOT TODAY** -- declining the `agents sync` widen on one-writer grounds and the one-shot on vc's. **Issue 0077, committed `f658d43e`.** **THE CONSTRAINT ON THE ROW IS cc's AND IS NOT A FOOTNOTE: whoever wires it supplies a `RenderContext`, and that `version:` field decides the footer with NO COMPILE ERROR** -- through the facade it inherits `FacadeCtx.version`, a route nobody has followed. The superseded reading follows, kept because the way it was wrong is the finding: `agents sync` and `agents generate` both pass the literal `"AGENTS.md"` (`render.rs:3691`, `:3713`); `rootfiles::render`/`sync` take the name as a PARAMETER and `render_all` renders all three, **so the module can and no verb asks.** `render.rs` calls the boundary chosen, but its reason is the `AGENTS.md.bak`-vs-`intentsvcs::backup` collision in `sync`'s WRITE half **and `sync` is wired** -- so the reason does not cover the file-name axis. Options put to vc, no preference taken: widen the verb / one-shot out-of-band for this repo / hold.
- **`[[INTENT_VERSION]]` IS THE RUNNING TOOL'S VERSION, SO THE REGENERATION MUST RUN UNDER `intent3`.** `rootfiles.rs:337-343` says so against `config.intent_version`, and `RenderContext { version: env!("CARGO_PKG_VERSION") }` at `render.rs:3690` is the source. **Root `VERSION` says `2.19.0` and `intent/.config/config.json` carries NO `version` key** -- so neither is the source and neither would warn. Run it under `intent` on PATH and `:3` returns `v2.19.0` and the fix READS AS APPLIED.
- **AC-03.17 HAS A GAP, MEASURED: THE RULING CLASSIFIES BY WRITER AND THE IMPLEMENTATION CLASSIFIES BY LOCATION.** `prettier --file-info` in-repo: `CLAUDE.md` / `AGENTS.md` / `usage-rules.md` all `ignored: false`; `intent/todo.md` and `intent/st/*/acceptance.md` `ignored: true`. **Three GENERATED root views sit in the formatter's reach** while `.prettierignore`'s header calls the root canon hand-written. Costs nothing today -- all three are prettier fixed points -- **but a REGENERATED file is an untested one, and renderer-emits/formatter-rewrites/skew-reports-drift is the loop AC-03.17 is named for.** At regeneration: `prettier --check` the OUTPUT before committing, and a non-fixed-point is a finding against the template rather than something to format away.
- **AND MY FIRST INSTRUMENT HERE COULD NOT DISCRIMINATE, CAUGHT BEFORE IT WAS REPORTED.** `prettier --check` returns _All matched files use Prettier code style_ for an IGNORED file and for a CONFORMANT one alike, **so the control I ran to prove the check reached `CLAUDE.md` proved only that prettier ran.** `--file-info` answers the actual question. This is the blind-instrument table's own _positive control that proves the instrument RAN, not that it MATCHED_, arriving inside the hour I re-read it.
- **The date-drop at `:92` is not a cosmetic delta: `[[DATE]]` is REFUSED via `Fault::UnknownToken`** because a file stamping its own render moment **differs from itself on every run** -- AC-03.17's churn loop with a date in it. Same defect class being fixed, and it should be verified that way rather than waved through as intended.
