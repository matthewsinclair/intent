---
node: ic
name: Interface Claude
role: interface
session_id: 11cef60b-409e-4bcc-b0f5-808d43639e75
heartbeat_at: 2026-08-29 23:30Z
status: active
focus: "WP-17 PIECE 1 (the form DSL) IS BUILT, GREEN AND MUTATION-PROVED -- `form.rs` + `surface/forms.json` + 8 tests, three mutations each killing exactly one arm. Next: the `intent edit` + `intent browse` register rows (piece 2), then the TUI realiser. ST0064 menubar is sequenced to me after piece 1. Register-structure question owed to vc: `probeable` is a UNIT error, and twin-agreement has no home."
claims: [ST0065, ST0056/09, ST0056/17]
---

# Interface Claude (ic)

**Pre-fold board verbatim at `.history/20260829/wip-fold-2301Z.md`. This file is the COLD-SESSION MINIMUM: state, not story.**

## DOING

**WP-17 -- the form DSL, under vc's instruction (received this session; I have no clock read for when vc sent it, so it carries no stamp).** Three pieces in order: the DSL, the `intent edit` register row, the TUI realiser.

**BOTH BLOCKERS ON THE PRE-FOLD BOARD ARE GONE, AND NEITHER WENT THE WAY THE BOARD PREDICTED.** dc rebuilt the shared release artefact (`cargo build --release`, rc=0) and `intent st list` is rc=0 again -- re-measured here, not taken. **The repair was 58 seconds and needed no commit and no hv**, against a board paragraph that had it behind `0148`. What made it severe was that it broke peers, not that it moved data. **The rebuilt binary is built from the DIRTY TREE, not HEAD** -- it carries dc's ST0066, cc's `fc` arm and vc's D56 -- so **a parity run right now measures the WORKTREE and a green does not mean HEAD is green.** `0148` is NINE paths, eight vc's, and a dependency vc created rather than an ownership problem: canon's `data-model.md` sha matches dc's WORKTREE (`326875fa`) and differs from HEAD (`31742ff4`). It clears on dc's fix to cells 632-633. **Not mine, and I am not to `sync --to-store ST0056`.**

**`AC-17.6` IS RULED AND PIECE 2 IS UNBLOCKED -- `intent browse` SHIPS and `tui-design.md` section 9 was the wrong half.** vc's ground is better than a tie-break: **`ST0058 AC-00.6` refuses DISAGREEMENT between a flag and its subcommand twin, not duplication** -- it was driven off `--version` answering rc=0 while `version` answered the unwired marker. Two spellings that AGREE are what it asks for. Section 9's bullet turned _must agree_ into _must not both exist_, and hv had asked for both verbs in the opening ask. **The bullet is deleted rather than struck through.**

**THE CONSEQUENCE IS MINE AND IT LANDS ON THE SAME STRUCTURAL GAP AS `0142`.** `intent browse <kind> <id>` IS `intent edit <kind> <id> --browser`, and **the register row is where the agreement is asserted -- the twin cannot be present by one spelling and absent by the other.** The register has no home for a relation BETWEEN rows: not mutual exclusion, not twin-agreement, not a refusal. `legal_pairs` is a `(disposition x target_state)` table and nothing else. So piece 2 needs one, and it is the same missing structure `0142`/`0147` name. **`0142`'s structural half is not TODO item 4; it is encountered at piece 2.**

**MY OTHER TWO CORRECTIONS ARE APPLIED BY vc.** `WP-17` is rescoped **`S` -> `XL`**. `AC-17.10` now cites `launch_editor` BY SYMBOL, with the general rule quoted into the criterion: **a line number inside a criterion is a second home for a fact the file already carries** -- which is `AC-17.2`'s own argument about the field set, one level up. 62 lines of drift in a file three of us write, on a criterion days old.

**REGISTER: the `fc` row now records the measured wiring split (2026-08-29 23:11Z).** `fc ST9999 AC-99.9 --because <text>` reaches the arm and refuses on the unknown thread (rc=1); `fc ST9999 AT-99.9 --because <text>` answers _`fc` is a known command that is not implemented yet_ (rc=2). **The gap is dc's and already known. The finding is the SENTENCE: the marker names the COMMAND, and this command IS implemented** -- a caller is told nothing in this build provides `fc`, by a build that provides `fc`. `from_states` also stated the criterion machine only and now carries the AT pair (`to-write`, `red`) and hv's ST/WP no-variant ruling. **cc reproduced it and ruled their gate arm CANNOT reach it, which is worth more than a yes:** their arm is keyed on ROWS and this defect lives BELOW a row, so they named it as the arm's declared boundary instead of widening a sentence. **cc's sharper read on my half: `probeable` is not a missing field, it is a CATEGORY ERROR IN THE UNIT** -- a boolean that is genuinely both is being asked of the wrong thing, and `partially_wired` would encode the confusion rather than resolve it. **That is vc's to rule, not mine to patch.**

**PIECE 1 IS BUILT AND GREEN.** `intentsvcs/src/form.rs`, `surface/forms.json` (14 thread rows, 5 widgets), `tests/form_declares_layout_not_the_field_set.rs` (8 asserting + 1 derivation aid). `cargo test -p intent-cli` rc=0 across 63 targets; `cargo fmt --all --check` rc=0. **Three intentsvcs failures and NONE ARE MINE** -- `attachment_drift_detected`, `thread_prose_carried` and `machine_ratified_in_prose`, all three `0148`'s `data-model.md` and `design.md`. **Re-drive; these figures are spent the moment anyone writes.**

**MUTATION-PROVED, AND THE FIRST ATTEMPT MEASURED NOTHING.** Three mutations -- delete the unknown-widget refusal, delete the no-such-property refusal, make the converse arm return empty -- each killed EXACTLY ONE test, the right one, no collateral. **The first run was worthless and only the baseline said so:** I staged dirty `facade.rs` onto a HEAD `model.rs`, so the crate did not compile and every _mutation killed the test_ line was a compile error wearing a kill's clothes. **A mutation worktree must carry the WHOLE dirty state, and the baseline is the control that says whether anything ran at all.**

**TWO DESIGN FACTS WORTH KEEPING.** `schema/*.json` IS GENERATED FROM `model.rs` VIA SCHEMARS (`faces.rs`, `schema_json::<Thread>()`), so `AC-17.2` has no second home BY CONSTRUCTION -- the loader reads `faces::faces()`, the same rendered bytes committed under `schema/`, rather than a re-derivation that agrees today. And **editability is `facade::unsettable_kind`, NOT `settable_fields`**: my first version used the latter, which answers _can the NARROW SETTER write this_ -- a smaller question -- and it put `status` outside the writable set, the one field `AC-17.4` builds the `select` widget for. **The field was the claim and the note was its scope, again.**

## TODO -- vc's order, resume here

1. **WP-17 PIECE 2 -- the `intent edit` AND `intent browse` register rows.** **`intent browse <kind> <id>` IS `intent edit <kind> <id> --browser`, and the register row is where the two are held to AGREEING about whether the capability exists.** args become `kind` + `id` (today `intent edit st 68` reads `st` as the address and `68` as the file, and refuses about the wrong thing). **`--path` MUST survive** -- `docs/getting-started.md:52` has `$EDITOR "$(intent st edit ST0001 design)"` under test, and command substitution makes stdout a pipe. `--editor` becomes optional-value and **no flag in the register carries `arity` today** (measured: zero; the one `type: "value"` flag takes a MANDATORY value). **Then piece 3, the TUI realiser** -- adding `ratatui`/`portable-pty`/`tui-term` MOVES `Cargo.lock`, which three nodes build against, so ANNOUNCE first. Nothing of the sort is in the tree: no ratatui, no axum, no maud, and `intentd` is a single `main.rs`.
2. **ST0064 -- the macOS menubar app, sequenced to me by vc after piece 1.** hv named the reference to adopt as-is: `~/Devel/prj/Gtools/native/macos/Geodica`, 2,470 lines of Swift, **structure is the deliverable, not the code**. **Two paths, and that is the part that matters: LIFECYCLE SHELLS OUT to the CLI** -- starting a daemon that is not running cannot be a request TO that daemon, and `AC-00.3` already ratifies CLI-owned launchd lifecycle -- **and STATE COMES OVER THE WIRE.** **The transport is NOT settled and is with hv:** `AC-00.3` says unix-socket GraphQL, `tui-design.md:226` puts `--browser` on `http://127.0.0.1:<port>`, and `URLSession` does not speak unix sockets. **Build the client so the transport is ONE SEAM.** Control plane + console only in v1; the logo is `docs/design/intent-logo.*`, the turtle, named specifically. **Swift is NOT a declared language** -- `intent lang init swift` before the first `.swift` file or the app ships outside the critic gate. ST0064 has ZERO criteria, its WP is scoped `S`, and vc wants the size from the code.
3. **WP-16 (S)** -- `data-model.md` against the schema, both directions. **Groundwork done, no code:** the population derives to **11 entities / 67 properties** from the three JSON faces (each root plus its `$defs` objects), matching vc's ruling with `Invoker` and `Subject` included and the event root titled `Envelope`. **`lib_staged.sh` ALREADY IS the committed-read mechanism** -- it reads the INDEX via `git show :<path>`, not HEAD. Source it; do not grow a second. **`machine_table_check.sh` already parses this document's tables** -- read it before writing a second parser. First-run RED on vc's missing tables is CORRECT.
4. **WP-09 (L)** -- MCP + agent guide, 5 criteria, Not Started. `AC-09.4` first. **The cost is register-side and mine:** `AC-09.1` needs every row to declare `exposed_on_mcp` and `read_or_mutate`, and **two rows carry them today.**
5. **0142's structural half (S)** -- refusals have no declared home in the register, so the only place to state one is a help string nothing checks. Why `0147` exists.
6. **ST0065** -- OUT of the 3.0.1 cut. Three hv rulings owed. Catalogues written.

**OWED TO PEERS:** cc -- hand them `spine.rs` when the commit frees it, for the gate arm that closes `organize` executably; they declined a register row and were right, and the `fc` marker finding above is a member of their population. **dc: `st.fc` / `wp.fc` ARE STRUCK, not pending.** hv ruled 2026-08-28 that ST and WP get NO status variant, so those edges may never exist in the shape either of us assumed. **Declining to pre-add them was right for a reason better than the one I had:** `FANS_OUT`s guard checks the right-hand side only, so a speculative row would have passed in silence for exactly as long as it was wrong.

## Watch-outs -- mechanisms only

1. **In this register the FIELD is the claim and the NOTE is its scope.** Retracted twice in one day by notes sitting beside fields I had read.
2. **A partial read reports in the shape of a complete one, and an ABSENCE concluded from one cannot be falsified from inside.**
3. **YOUR INSTRUMENT'S OUTPUT IS A CLAIM ABOUT YOUR INSTRUMENT.** Three faces, all mine: a jq keyed on the wrong field returned 0 across every family; a harness that ran the subject twice printed `ok: created` at `rc=1`; and a population defined by a PREDICATE admits whatever later matches it. **Control every instrument, and give a predicate-derived population a member-level check against a second channel.**
4. **`at lint` exempts `to-write` from L2/L3, so `conform` can mean EXEMPT rather than CHECKED.** Drive a row off `to-write` before believing a green.
5. **Snapshot before a mutating verb whose effect you will want to characterise.** A HEAD-vs-now diff attributes to your run every change since HEAD, and this baseline moves under four people.
6. **A tool in a family does not inherit the family's behaviour.** Citing a sibling is not reading the tool.
7. **`gen_dispatch_table.sh` REFUSES on every derived count with a second home** -- `populations` is TABLE ORDER, never sorted. **Let it check you; do not pre-empt it.**
8. **An attachment edit is a SUITE-WIDE event, and its only fix adopts other nodes' uncommitted bytes.** The warning reaches the actor, never the node whose bytes moved.
9. **A MULTI-WORD SPELLING PASSED AS ONE TOKEN READS AS A COMMAND THAT DOES NOT EXIST, IN EVERY LANGUAGE.** zsh: `${=v}` splits, bare `$v` does not. Rust: `.args(&[path])` with `"st repair"` is ONE argv element and clap answers rc=1 _unrecognized_. **Never `$?` after a pipe.** An apostrophe in a single-quoted string is a hard syntax error. `bash -n` an edited shell file AND drive it.
10. **A mutation-proof runs in a DETACHED WORKTREE with its own target dir**, and **check WHICH assertion fired** when a test goes red. Removing a filter still COMPILES.
11. **A GREEN SUITE AND A GREEN CI ARE DIFFERENT GATES.** `cargo test` was rc=0 while `cargo fmt --check` was rc=1 on my own code, and hv moved the fmt/clippy arms into CI, so **there is no local alarm by design.** Run both.
12. **A DEBT YOU DECLINE TO PAY GOES IN THE FILE WITH THE FIX WRITTEN OUT -- AND YOUR COUNT OF IT WILL BE LOW.** I wrote _three homes_ for the unwired marker. cc measured **12 files**: one producer (`render.rs:767`), three inline literals, and **SIX named constants under FIVE DIFFERENT NAMES** -- `PHRASE`, `UNWIRED`, `UNIMPLEMENTED`, `UNWIRED_PHRASE`, `UNWIRED_MARKER`. **The five names ARE the finding: a divergent copy that shared a name would at least be greppable.** Naming the debt is what got it counted; the count only came from someone standing elsewhere. **It is cc's, it is a PRECONDITION of their gate arm rather than cleanup, and it is not mine to take.**

## Decisions

- **(hv) `intent surface retired`** -- spelling ruled over bare `retired` and `llm retired`; cost stated and taken, a 14th top-level root holding one leaf. **The verb must NEVER encode membership in its own exit code.**
- **(hv, 2026-08-25) Retirement is enumerable, not a new exit code.** `spine.rs` keeps its exit-code decision; a caller branches on MEMBERSHIP. **`rc=2` carries FOUR meanings and only PROSE separates the other three** -- the register has no built-ness field, by construction, so in a test the message is the ONLY oracle available rather than a lazy one. **ic reached this from the test side and cc from the register side without either knowing** -- second convergence of the day on one defect, and it is the hole cc's gate arm exists to fill.
- **(hv via vc) `fc` is 1 AND 3:** wire the arm before the tag, AND cc's built-ness gate as the class fix. Not alternatives.
- **(hv) 3.0.1 scope is ST0056 + ST0058/0066/0068. Docs ship WITH the tag, written against the CUT.**
- **(vc) Register writes are ic's.** `dispatch-table.json` is AUTHORED canon; `dispatch-table.md` is GENERATED -- regenerate, never hand-edit.
- **(ic) One definition, two doors.** `retired_and_unreachable()` serves the exec-path refusal and `surface retired`. Two doors onto one roster is the design; two answers to _is this retired_ is the divergent-copy shape. **`table.retired()` alone names `organize`, which RUNS.**
- **(ic) Share the FACT, not the prose.** `Replacement` is a three-arm enum; `null` (nobody said) and `""` (declared none) stay distinguishable across the wire.
- **(ic, on dc's finding) `surface` is EXEMPT from the unmigrated refusal, and the recorded ground is PURPOSE, not mechanism.** "It never opens a project" is the weaker half. **The unmigrated project is this verb's PRIMARY case** -- the caller who typed a retired v2 command is almost always standing in one. Third member of the `llm guide` / `claude rules` class.
- **(ic) A ROW MODELLED AS A FAMILY HEAD WITH A SUBCOMMAND SLOT HAS NO EXACT-PATH FORM.** `entry.path` and its family are the same string, so its exemption is exact and family-wide at once and a future sibling inherits it silently. **The precondition is a TEST, not a comment. A caution in a comment does not fail.**
- **(ic, adopted by dc) `FANS_OUT`: do not split it -- rename it, and each member states its shape.** A fiat-close edge is spelled `fc` top-level whatever machine it sits on; each machine keeps its own token.
- **(ic) A record may QUOTE what was said, but must POINT AT, never reproduce, what is currently true.**
- **(ic) A surface claim travels with what makes it checkable** -- the register answers _does this exist_, never _does it still do what the last doc said_.
- **(all) Fold archives are `wip-fold-HHMMZ.md`**, append-only. **`add + commit --only + reset` is NEW files only.**
