# ic pre-fold board, archived 2026-08-29 22:22Z

---

node: ic
name: Interface Claude
role: interface
session_id: 11cef60b-409e-4bcc-b0f5-808d43639e75
heartbeat_at: 2026-08-29 21:08Z
status: active
focus: "Register writes are mine. Landed: the `fc` row (top-level, hv/ic ruled), `fc` into self_loop for dc`s two red assertions, the `intrinsic` basis on ten rows, cc`s two parked edits. Assignment 2 doc half DONE -- five help-string guarantees driven TRUE, two findings filed 0146/0147. AC-00.6 is a BUILD blocked on 0086 with the twin population enumerated. NEXT: AC-00.5 predicate, then the four false doc homes."
claims: [ST0065, ST0056/09]
---

# Interface Claude (ic)

## DOING

**REGISTER WRITES ARE MINE (vc). `surface/dispatch-table.json` is AUTHORED canon; `dispatch-table.md` is a GENERATED view -- regenerate, never hand-edit.** Uncommitted and held: no commit without hv.

**LANDED THIS SESSION.** The `fc` row (`new_surface`, top-level per AC-00.1 and my surface ruling to dc); `fc` into `populations.self_loop`, which is what dc`s two red assertions wanted; the `intrinsic` `disposition_basis` on ten rows, pointing at the vocabulary instead of proposing it; cc`s two parked edits (`mcp` exemption false in the present tense, `daemon` `not_probed` -> `probeable`), their finding, landed here only because the file has one writer.

**THE GENERATOR REFUSED ME FOUR TIMES AND WAS RIGHT EVERY TIME.** new-surface count 11 vs 12; the MCP withhold list derived from `recoverability`; `legal_pairs` live census 32 vs 33; `populations` hand-sorted when the block is TABLE ORDER. **Each refusal was a second home for a count I had moved and not followed through.** The populations are now DERIVED, and the derivation was positive-controlled against the committed file first -- all four reproduced byte-identically before I trusted it.

**AND ONE REFUSAL CHANGED A DECISION RATHER THAN A NUMBER.** I authored `fc` `exposed_on_mcp: false` because ST0066 AC-00.6 forbids an LLM invoking it. The generator refused: withholding here is DERIVED from `recoverability`, and `fc` is reversible. **`recoverability_anomaly` was offered as the escape and I declined it -- a key named for one reason cannot hold members admitted for another**, which is the defect this estate hit three times today. **The row follows the rule that exists and asks hv the narrower question in `mcp_review.uncertain`: does the machine face need a SECOND withholding ground, distinct from irreversibility?**

**ASSIGNMENT 2 DOC HALF: DONE.** Five asserted guarantees driven and all TRUE -- `ac new`/`at new` refuse a taken id (rc=1, nothing written, both remedies naming the sibling edit verb); the two doors AGREE; `ac edit` leaves satisfaction alone; `at edit` changes no field you did not name; both edit verbs refuse unknown ids. **0142`s claim that `ac new`/`at new` are TRUE is now confirmed by someone who did not build them.** Two findings filed: **0146** (`at edit` is kind-blind and the refusal`s own remedy is the route into a row carrying both `file` and `prose`, which `at lint` has no rule for) and **0147** (the unguarded help-string population is at least 6, not 4).

**AC-00.6: A BUILD, NOT EVIDENCE, BLOCKED ON 0086.** `--version`/`version` agree; `--help` rc=0 vs `help` rc=2 fires the falsifier. **The twin population is ENUMERATED rather than sighted** -- exactly two genuine capability twins exist, both driven; `st new --start`/`st start` and `init --lang`/`lang init` also twin and both AGREE; the rest are name collisions I read rather than counted. When 0086 lands the row closes against a population instead of a third lucky sighting.

**AC-00.5 NEXT. THE FINDING THAT SIZES IT: A NAIVE DOOR PUBLISHING `table.retired()` PUBLISHES NINE PATHS AND ONE OF THEM RUNS.** `organize` is THREE rows -- `st organize` and `organize` under `families` (retire), `organize` under `new_surface` (new-surface) -- so it is in `populations.shipped` AND `populations.retired`. The exec path survives only because `retired_refusal` also requires `!reachable(spelling)`. **A caller branching on the naive list deletes a working verb from their automation.** So the door is `retired() AND NOT reachable`, calling the SAME predicate rather than a second implementation. **Spelling is hv`s: root vs leaf.**

**RETRACTED TWICE TODAY, BOTH TIMES BY THE REGISTER`S OWN NOTES.** `populations.shipped`--`migrating-from-v2`read the RIGHT field and the field was false.`st help` -- a v2 bare-word arm, documenting v2, not promising v3. **In this register the FIELD is the claim and the NOTE is its scope; a field read without its note is a claim read without its scope.**

## THE FOUR THAT COST ME TODAY

**1. A CLAIM WHOSE _BASIS_ IS WITHDRAWN READS EXACTLY LIKE ONE THAT STILL HOLDS.** I was handed "3.0.1 does not fix `ac new`" and carried it on my own board twice. It was true when given; hv's "fold everything into 3.0.1" removed the constraint under it. **No symptom, and the entry looked settled -- settled is what makes a thing stop being checked.**

**2. A RULING AND A LANDED CHANGE ARE INDISTINGUISHABLE IN A MESSAGE.** Two of four surface changes I was handed did not exist (`issues edit` absent from register, source AND `--help`; `st edit` still the path-printer). **Fix: a surface claim travels with what makes it checkable.** **AND THE FIX HAS A LIMIT I FOUND THE HARD WAY** -- "it is in the register" covers a NEW verb and **cannot cover a BEHAVIOUR CHANGE to an existing one.** `st edit` was an unchanged register row before and after `8aa83dd6` while what it does changed completely. **The register answers _does this exist_, not _does it still do what the last doc said_.** For that class the check is the source or the test.

**AND IT HAS ALREADY NEAR-MISSED IN A REAL GENERATED PAGE** (vc, tonight): the reference is emitted from the register at a revision, so **a behaviour change to an existing verb moves NOTHING the generator can see -- the pages regenerate identically and read as current.** cc's `intent-st.md` described `st edit` without the two flags, and **only the revision stamp made it visible.** The stamp is doing work the content cannot.

**3. FIXING THE INSTANCE IS NOT FIXING THE CLASS.** The `intent doctor` claim: **vc swept and found three; I reasoned about the document in front of me and fixed one of two.** My README kept the false sentence while my release notes had the correction. **The sweep is the check, not the care.**

**4. I DID NOT DERIVE A WRONG ANSWER -- I APPLIED A CORRECT GENERAL CONVENTION.** `user/tap/formula` is the shape everyone has seen; this project is `matthewsinclair/intent/intent` and says so in the file that publishes the formula. **Three of us wrote three different wrong spellings of a rule already written down.** A rule living where no author of the affected text would look is not three carelessnesses.

## Watch-outs

**POSITIVE-CONTROL THE INSTRUMENT, AND A CONTROL THAT PASSES UNDER THE BROKEN INSTRUMENT IS DECORATION.** My emoji test returned a clean `0` and its control **failed** -- the zero was blind. Working instrument: 28 at HEAD, 0 after. **Same shape as the register limit above: an instrument answering confidently where it structurally cannot see.**

**A FALSE SENTENCE PROPAGATES BY BEING BELIEVED, AND EVERY HOP LOOKS BETTER SOURCED** (vc's). Their doctor claim reached my release notes before I checked it. **The docs failure mode that code does not have.** And **a wrong claim of your OWN is the most trusted source you have**, because you know where it came from -- vc wrote it once then leaned on it twice as a premise.

**LATEST COMMIT AND INTRODUCING COMMIT ANSWER DIFFERENT QUESTIONS.** `git log -1 -- <path>` says _has this changed since the tag_; `--diff-filter=A ... | tail -1` says _did it ship in the tag_. I used the first for the second and got a different answer for one of four items.

**THE GENERATED-VIEW BANNER IS EVIDENCE IN ONE DIRECTION ONLY.** Presence means generated; **absence does not mean authored** -- one WP `info.md` carries none while 38 siblings do. `intent doctor` is the check. **Hand over the check, not the table.**

**REPORT, DO NOT EDIT, ANOTHER NODE'S FILE -- AND DO NOT WRITE INTO A THREAD THEY CLAIM.** I left AC-12.2/12.3 unsatisfied for vc even though I did the work; `doctor` was already reporting a status-gate disagreement on ST0056, which was a second reason.

**AN INDEX LOCK MEANS A PEER IS MID-COMMIT. NEVER DELETE IT** -- wait in a bounded loop. **`--only` SEPARATES FILES, NOT FACTS** and drops a peer's staged sibling.

**A HOLD WHOSE CONDITION HAS ALREADY BEEN MET IS NOT A HOLD, IT IS A NODE SITTING STILL.** Measure before folding; do not bank "holding" as state.

**A STAMP TYPED BY FEEL IS FABRICATION AND THE MESSAGE CHANNEL HAS NO GUARD** (`0099`). Cite the commit, never invent a better-looking minute. **zsh: an unquoted `--include=*.rs` ABORTS the command. A SCRIPT THAT ABORTS MID-WAY WRITES NOTHING AND THE LINES BELOW IT STILL PRINT SUCCESS.**

## Decisions

- **(hv) v3.0.1 IS THE NEXT RELEASE ACT; DOCS PRECEDE THE CUT AND SHIP WITH IT.** Scope: ST0056 and its feeders. **The cut is downstream of `8aebe2ce` by construction**, which is what makes "3.0.1 fixes `ac new`" true.
- **(hv) `intent/docs/` STAYS INTERNAL CANON**; `docs/` is the v3 set, `docs/v2/` the frozen archive, v2 blog canonicals preserved at their old locations.
- **(hv) The number stays `3.0.1`** over vc's `3.1.0` recommendation, cost stated. **The notes do not editorialise about it.**
- **(ic) THE README VOICE CALL WAS MINE: no emoji.** Not the site rule applied by fiat -- vc was right to refuse that -- the same judgement for the README's own reason.
- **(ic) A DOCUMENT WRITTEN AHEAD OF ITS SUBJECT CARRIES A PROVENANCE PARAGRAPH** naming what it was verified against and stating that ruled-but-unlanded items are deliberately absent. **It then needs no rewrite when they land** -- proved when `st edit` landed and the paragraph stayed true.
- **(all) Fold archives are `wip-fold-HHMMZ.md`**, append-only. **(vc) `add + commit --only + reset` is NEW files only.**
