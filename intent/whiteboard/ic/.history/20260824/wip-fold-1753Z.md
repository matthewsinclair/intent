---
node: ic
name: Interface Claude
role: interface
session_id: b555eb67-573c-4858-a788-a85677ad09ab
heartbeat_at: 2026-08-24 17:41Z
status: active
focus: "**FOLDED AGAIN 2026-08-24 AFTER THE COMPACT; NOTHING IN FLIGHT; HOLDING FOR vc.** The 16:10Z fold stands (`.history/20260824/wip-fold-1610Z.md`, 336 lines); this pass adds only what the compact itself measured. **THE COMPACT DID NOT ROTATE `session_id` -- MEASURED FIRST-HAND**, env still `b555eb67`, so compact/resume/relaunch is now three limbs and two of them are mine. **AND THE POST-COMPACT STRETCH COST ME TWO WARNINGS AND ONE NEAR-ACCUSATION, ALL THREE AGAINST vc, ALL THREE KILLED BY RE-MEASURING.** The worst was the clock: **I judged vc's `17:37Z` against MY OWN 81-MINUTE-OLD READ and made them 75 minutes fast; a fresh read said 17:39Z and vc was right to within two.** **THE GATE IS 66 OF 67, `AC-08.5` STILL THE ONLY ROW, MINE TO COVER AND NOT TO BUILD** (vc confirms it untouched). **FIRST WHEN WORK RESUMES: `issues list --kind` vs `st list --status`** -- vc reports it bit them a second time today, and it is my surface."
claims: [ST0057/02, ST0057/05, ST0057/07, ST0057/08, ST0056/03]
---

# Interface Claude (ic)

## DOING

**NOTHING IN FLIGHT.** Five commits landed, all path-scoped with `--only`, gate green each time. **The fold commit could not list itself, which is why the count read four.**

| sha        | what                                                                                                            |
| ---------- | --------------------------------------------------------------------------------------------------------------- |
| `ea84d0ae` | **AC-08.5's second limb, driven: 8 of 8.** A minimal legal `put` at the thread door clears every unasked scalar |
| `5d09fabb` | the sha replaces the worktree caveat; **dc's source-range instrument beat my mtime one, recorded against me**   |
| `abf4bffb` | a warning is not discharged by being true; the suite's population is the working tree, never the commit         |
| `75845aa9` | four names for one concept in `issues list`, and **our own restart docs teach the wrong flag**                  |
| `b0d5db10` | the 16:10Z localfold itself -- 336 lines to 111, verbatim snapshot kept                                         |

## TODO

**IN ORDER. Nothing below is started.**

1. **`issues list --kind` vs `st list --status` -- MY SURFACE, AND FIRST ON THE BOUNCE.** The same concept is spelled two ways, the value `all` is legal in both vocabularies, and each refusal names the FLAG rather than the CONCEPT so it never points at the sibling. **`--kind <open|closed|all>` is the wrong word: that is a STATUS axis, and a reader meeting `--kind` expects bug/chore/feature.** Flag says kind, help says bucket, values are statuses, error says bucket -- four names, three in one line of help. **Surface change: goes to hv with OPTIONS, never a preference.**
2. **`at green`'s no-op voice has no instrument.** `self_loop_voice.rs` drives `at red` (`:419`) and `at na` (`:444`); **`at green` appears nowhere**, while the dispatch table declares `ok: <AT> already green` exit 0 as SHIPPED on my measurement clause alone. Harness exists; nobody blocks this.
3. **AC-08.5's biconditional cover -- ROUTED AND UNBUILT, and deliberately so.** Today's test asserts the collateral set equals a DECLARED LITERAL: right for a REPORTING instrument, **not the cover fix**, and that disclosure is now in the commit body, my board AND the criterion. The fix shape is `worklist.is_empty() <=> AT-08.5 green`, read from CANON not the store. **I route rather than take it: the mechanism would later authorise greening my own gate row.**
4. **Two verbs accept an undeclared format at rc=0.** `critic rust --format=__bogus__` falls back to text; `events --format=__bogus__` ignores the flag; `export` refuses by name at rc=1 and is the model. Re-driven at HEAD. cc's crate, needs hv.
5. **ST0057/WP-05** recorded WIP, gate 3/3 PASS, deliberately not closed -- a gate that passes while a design question is open does not reach the question.
6. **Recognise `intent#0066`/`#0067`** in the surface record. Mine to recognise, cc's to build.
7. **Read `a38e884b` -- the shipped-surface drift guard now RUNS in CI instead of skipping**, and vc reports it was green over NOTHING for its whole first day. **Surface drift is my charter and vc built the guard**, so the review is mine: what population does it actually compare, and would it have caught `--kind`/`--status`?

## Watch-outs

### The blind instrument -- ONE RULE, EIGHT INSTANCES, AND IT IS THE ONLY ONE I NEED

**ASK WHAT THE INSTRUMENT WOULD SAY IF THE THING WERE ABSENT -- AND WHAT IT WOULD SAY IF IT WERE PRESENT. POSITIVE-CONTROL IT AGAINST A CASE WHOSE ANSWER YOU ALREADY KNOW.** One command, and it is the control nobody runs.

| instrument                        | what it actually answered                                                                                                                                                                                                                                                                    |
| --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `git rev-parse --short A B`       | `Needed a single revision` whether both resolve, one is absent, or neither -- **and it drops the ref name the bare form prints.** rc=128 in every case, so only stderr discriminates and `--short` destroys it. **Hit it TWICE in one session; diagnosed it only when the answer mattered.** |
| `grep currency`                   | matched `structured-**concurrency**`, `gate_currency`, `canon currency`. **Three hits, zero subjects, and it returned a plausible AFFIRMATIVE** -- the direction that stops you looking                                                                                                      |
| `awk '... END {print NR}'`        | the file's line count, not the match count -- reported 1392 targets where there were 105                                                                                                                                                                                                     |
| `pgrep -f 'intentd'`              | matched `intentdb` in every node's system prompt: **failed hardest when the most nodes were working**                                                                                                                                                                                        |
| `sqlite3 mode=ro` + `2>/dev/null` | a refusal became a green over an EMPTY SET. **A measurement over an empty population must REFUSE, not report**                                                                                                                                                                               |
| `git log --format=%an`            | one value estate-wide. **A field with one value carries zero bits**                                                                                                                                                                                                                          |
| `ListAgents` `started`            | SOCKET age, not session age. Four nodes, four correct self-reports, one unanimous wrong population                                                                                                                                                                                           |
| a needle naming its conclusion    | cc's `'UNSETTABLE\|"file"\|...'` could not have returned anything else                                                                                                                                                                                                                       |
| `ps -ax \| grep '[c]argo'`        | matched its OWN zsh wrapper -- the eval'd command line CONTAINS the pattern, so the probe is always inside its own population. **`pgrep -xl` matches the process NAME.** Identical shape to `pgrep -f 'intentd'` two rows up, **run by the node that wrote that row**                        |
| my own last clock read            | used as the REFERENCE for a peer's stamp 81 minutes later. A stale read cannot report itself as stale -- **it reports the other party as fast, in exactly the words a real defect would use**                                                                                                |

**AND THE COROLLARY THAT COST THE MOST: A SUBSTRING STANDING IN FOR A SYNTACTIC FACT IS ST0039's GREPPABLE PROXY ONE LEVEL UP.** An absence is only evidence within the scope you actually read, **and neither a call boundary nor a file-name pattern is a scope boundary.**

### Mechanical

- **ANY TIME I WRITE -- field, message, commit body, prose -- IS READ FROM `date -u` IN THAT ACT, OR WRITTEN AS A MEASURED INTERVAL. NO THIRD FORM.** Read PER STAMP, never per session: the failure is _read once, then advance by feel_, which **satisfies the monotonic guard BY CONSTRUCTION.** A fabricated stamp that verifies leaves no corpse. **`git log` prints LOCAL.** Never repair a fabricated stamp by inventing a better one.
- **CAPTURE THE REVISION AND THE DIRTY COUNT IN THE SAME COMMAND AS THE MEASUREMENT.** In a five-node shared checkout `git rev-parse HEAD` answers for the moment you ran it and nothing else. **HEAD moved under me four times today.**
- **BEFORE REPORTING A PEER'S NUMBER AS WRONG, STATE THE POPULATION YOURS CAME FROM AND ASK FOR THEIRS.** _Does not reproduce_ is a claim about an attempt you have to have actually made.
- **WHEN A COMMAND'S OUTPUT WILL BECOME A CLAIM, RUN IT WITH NO PIPE** -- including when the output is an EFFECT rather than a list. **A truncated COUNT is visibly partial; a truncated EFFECT looks complete.** Guards report at the TAIL, resolution speaks at the HEAD.
- **The Bash tool is zsh; cwd PERSISTS between calls -- absolute paths always.** `grep -c` exits 1 on zero; unquoted `$var` does not word-split; **never `$?` after a pipe.** A redirect plus a pipe turns a wrong-cwd miss into rc=0 with no output, which reads as _searched and found nothing_.
- **`cargo test` needs `--no-fail-fast`; write the WHOLE log to a file; count with `awk '/^test result:/'`.** Keep `CARGO_TARGET_DIR` inside the tree (`native/rust/target/ic`) -- `INTENT_HOME` walks up from the binary.
- **`intent` on PATH is v2.19.0 and answers for the FLEET.** **`intent3` IS on PATH now** (the no-v3-on-PATH ruling was retired by ST0058) -> `bin/intent3` -> `target/release/intent`, **sha `f85c07dc`, 2026-08-22 11:56Z, and dc measured it 7 non-test source files behind HEAD.** **Drive v3 explicitly: `./native/rust/target/debug/intent`.** **PIN BY HASH, NEVER BY THE MARKER.**
- **A BINARY IN A SHARED `target/` DESCRIBES NO REVISION YOU CAN NAME.** `target/debug/intent` and `target/release/intent` are written by whichever node built last, from whatever THEIR tree held -- committed or not. It runs, it answers, and the answer is about an input you did not choose. **Either rebuild into your own `CARGO_TARGET_DIR` or state the caveat with the number.**
- **A MARKER-AND-RANGE PAIR IS THE ANSWER; NEITHER ARM ALONE IS.** A range with no pin names a distance from a moving point; a pin with no range names bytes with no consequence. **Two builds a fortnight apart over an untouched subsystem ARE peers; two an hour apart across a rewritten one are not.**
- **The markdown formatter is a second writer.** The dispatch table is at repo-root `surface/dispatch-table.json` and the `.md` is GENERATED from it. A clean `git apply --3way` reports on TEXT, not meaning, and it STAGES.

### Estate

- **`git commit --only` is PATH-scoped, not hunk-scoped.** Drive `git diff -U0` on your own file BEFORE committing and confirm it carries nobody else's bytes. **And check which FILES a formatter will touch before running it** -- `cargo fmt -p <crate>` formats the whole crate.
- **THE INDEX IS SHARED IN THIS CHECKOUT, SO STAGING IS TOUCHING SHARED STATE.** A peer's bare `git commit` sweeps anything staged into their commit wearing their authorship. **So the obvious remedy for an untracked file -- stage it -- is WRONG here.** dc's answer is the right one for protecting SOMEONE ELSE's file: a protective copy OUTSIDE the tree. **But for your OWN untracked file the rule as written is unfollowable, and I proved that by breaking it twenty minutes after quoting it** -- `--only` is path-scoped and cannot commit what git does not know about, so there is no zero-staging route. **The honest form is an ORDERING, not a prohibition: stage the narrowest pathspec and commit in the same breath, never leaving the index dirty across a pause.** A rule its own author cannot obey while following it is a missing operation, not a discipline problem.
- **`tests/run_tests.sh:89` DISCOVERS BY `find`, SO THE SUITE'S POPULATION IS THE WORKING TREE AND NEVER THE COMMIT.** An untracked `.bats` is picked up and RUN. Measured: **113 on disk, 112 tracked.** On a clone or in CI the missing arms are simply absent and **the suite reports GREEN** -- `find` cannot distinguish _this test passed_ from _this test was not there_. **Two commands name the gap and nothing runs them.**
- **RUN THE SUITE THROUGH `tests/run_tests.sh`, NEVER `bats` DIRECTLY** -- the runner exports `INTENT_FIXTURE_VERSION` and a direct run builds a v3 fixture against a v2 binary.
- **UNCOMMITTED WORK IS NOT A CITABLE SUBJECT**, and **"FIXED" IS NOT A STATE**: worktree, index, HEAD and pushed are four, the speaker reports the first and the reader hears the third. **NAME THE STATE OR NAME THE SHA.**
- **AN UNCOMMITTED EDIT WAS ERASED IN THIS CHECKOUT WITH NO REFLOG TRACE, AND hv RULED IT ACCEPTED RATHER THAN INVESTIGATED.** The mechanism is live and undiagnosed BY DECISION. Committing is the only defence.
- **ANNOUNCE THE FILE, NOT THE THREAD** -- a pathspec-holder and a file-holder can both be careful and still collide. **Announce before touching the commit gate, always**: `.githooks/pre-commit.intent` is GITIGNORED, so the file with the most authority over every node's commit is invisible to `git status`.
- **A PEER'S THREAD-WIDE `sync --to-store` CARRIES YOUR UNCOMMITTED WORKTREE INTO CANON.** Fix YOUR thread and TELL them about theirs. **An attachment edit and its canon must land in ONE commit.**
- **MONIKERS ARE ESTATE-SCOPED AND NOTHING MARKS THEM.** Write `lamplight-ic`, never `ic`, whenever it can cross an estate.
- **AN INBOX IS NOT READ-ONCE-AT-PICKUP.** Re-read before any fold and before contradicting a peer about what they told you.
- **`SendMessage` CAN REPORT `success: false` AND DELIVER ANYWAY.** Prefer waiting to resending when the peer is busy; if you resend, SAY it is a resend.

### Judgement

- **A WARNING IS NOT DISCHARGED BY BEING TRUE -- IT IS DISCHARGED WHEN THE REMEDY IT INVITES IS ALSO CHECKED.** I flagged dc's untracked file correctly and the obvious remedy was wrong; **I had the ingredient on this board and had quoted it at two peers the same day.** **dc's generalisation is better and is the mechanism: THE TRUE HALF IS WHAT CARRIES THE FALSE ONE.** A wholly false claim gets challenged; a true premise with a wrong conclusion travels on the premise's credit, and the more carefully the true half is stated the further it goes.
- **THE DOOR NOBODY GUARDS IS YOUR OWN SUMMARY OF A DOCUMENT YOU ALREADY READ CORRECTLY** (vc's class, named against themselves). Not a stale record and not a peer's claim -- both of those have a document you failed to consult. **Here it was consulted, correctly, and the compression was what got used.**
- **A CHANGE THAT WOULD CONVENIENTLY GREEN YOUR OWN WORK IS THE ONE TO STOP AND ROUTE.** The tell, not the virtue.
- **CORRECTNESS AND CURRENCY ARE INDEPENDENT PROPERTIES AND ONLY ONE OF THEM IS EVER CHECKED.** A proof is about the input it was run against. **And a proof's VALIDITY and its REPRODUCIBILITY are independent too** -- dc's mutation proof is sound and unrepeatable by anyone else, because neither subject nor instrument exists outside this working tree.
- **REPORT THE CHECK THAT CAME BACK CLEAN.** Nobody is rewarded for it and it turns a later wall of failures into one explanation ruled out in one command.
- **NAMING AN OWNER INSTEAD OF ASKING ONE IS THE FAILED STEP IN EVERY MISATTRIBUTION ON THIS BOARD.** "Not mine" does not imply "yours". A correlation between two dirty files says they belong to each other and nothing about whose they are.
- **A FOLD INSTRUCTION IS NOT A TRUSTED SOURCE ABOUT YOUR OWN HISTORY.** A peer telling you what only you could know is telling you what THEY know.
- **ONE PLANTED WRITE PROVES A TEST FIRES; A PAIR PROVES NEITHER ROW IS THE OTHER WEARING A DIFFERENT ID.** An empty match arm (`Ok(_) => {}`) is a test touching its subject and asserting nothing, and it reads as coverage. **A population containing a case is not the same as a case being checked.**
- **A DECLARED LIST STOPS COVERING THE DAY A VARIANT IS ADDED** -- make an EXHAUSTIVE MATCH the witness so a new variant fails to COMPILE.
- **A ROW'S TITLE CAN PROMISE MORE THAN ITS BODY**, so ask _what does satisfying this completely still leave broken?_ against the BODY. Against a title it returns _nothing_ every time.
- **AN INSTRUMENT WHOSE OUTPUT WILL BE QUOTED TO ANOTHER NODE IS NOT A THROWAWAY, WHATEVER ITS LIFETIME**, and owes a stated denominator before its number is repeatable.
- **DOMAIN KNOWLEDGE IS THE INPUT TO A MECHANISM, NOT A COMPETITOR TO ONE.** Comprehension is what lets you WRITE the check that carries it; then the next reader gets the fact without needing it in their head.

## Decisions

- (2026-08-24) **I JUDGED A PEER'S TIMESTAMP AGAINST MY OWN 81-MINUTE-OLD CLOCK READ, AND THE STALE READ IS AN INSTRUMENT LIKE ANY OTHER.** vc's live message carried `17:37Z`; my last read was `16:18Z`; the arithmetic made vc ~75 minutes fast, which is almost exactly the Lamplight local-time-plus-`Z` signature, **and I was one message from reporting it.** A fresh read said `17:39Z` -- **vc was correct to within two minutes and the whole discrepancy was mine.** This is _read once then advance by feel_ wearing its other face: not fabricating my own stamp, but **holding a stale reference against which a correct peer stamp is indistinguishable from a fabricated one.** The board already said READ PER STAMP; what it did not say is that **judging someone else's stamp IS a stamp-sized act and owes its own read.** And the asymmetry is the danger: a stale reference never accuses ITSELF.
- (2026-08-24) **THREE ACCUSATIONS AGAINST ONE PEER DIED ON RE-MEASUREMENT INSIDE ONE STRETCH, AND EVERY ONE WAS TRUE WHEN I NOTICED IT.** vc's fold file WAS untracked -- and was tracked by the time I would have sent the warning (`c465093c`). vc's stamp WAS impossible -- against a clock I had stopped reading. **Rule 83 says a warning is not discharged by being true; this adds the other axis: a warning about a peer is not discharged by being CURRENT WHEN OBSERVED.** In a five-node shared checkout the subject moves between noticing and sending, **so the measurement that counts is the one taken at the moment of SENDING.** The cheap discipline: re-run the probe in the same breath as composing the message.
- (2026-08-24) **DISK IS NOT THE CONSTRAINT AND THE QUESTION SHOULD HAVE STARTED THERE.** 3.6Ti volume, **2.2Ti free, 40% used** -- so the 75G of `target/` is housekeeping, not a fix, and any answer that opened with what to delete would have been solving a problem nobody has. **Ownership and cost, measured:** `target/cc` 30G stale since 2026-08-23 13:48Z and `target/vc` 1.1G stale since 13:41Z are the cheap 31G and are their owners' call; `target/debug` 43G was written 17:10Z and costs the whole estate a rebuild; **`target/release` 881M must NOT be deleted -- `bin/intent3` serves it and `intent3` is on PATH estate-wide since ST0058**, and it was rebuilt at 17:36Z. My own `target/ic` is already gone, 24G.
- (2026-08-24) **AC-08.5's SECOND LIMB IS BROKEN AT THE THREAD DOOR: 8 OF 8 UNASKED FIELDS CLEARED BY A MINIMAL LEGAL `put`.** `slug / status_reason / acceptance / objective / context / body / preamble / related`. **LANDED `ea84d0ae`; vc amended the criterion at `7b49adc9` citing it, and I verified that by probing canon for five markers rather than reading vc's summary.** Mechanism: `posted_json` is a bare `from_str` with no merge, the arm parses the WHOLE document with `from_value::<Thread>`, **then grafts FOUR children off the clone and no scalars.** 18 fields = 5 required + 4 grafted + **9 neither**; one asked for, so the collateral denominator is 8 and the numerator was 8. **THE GRAFT CONTROL IS WHAT MAKES IT A DEFECT RATHER THAN A LIMITATION** -- had the children moved too it is the weak _parse-and-replace replaces the document_; they did not, **so the eight are a CHOICE made four lines apart on the same clone.**
- (2026-08-24) **AC-08.5 HAS TWO AXES AND MY TWO INSTRUMENTS COVERED THEM ASYMMETRICALLY.** The form instrument drives all 13 FORMS and asks only _does an arm exist_ (5 of 11 still unreachable); the field instrument drove FIELDS for exactly ONE entity. **`E::Thread => Reachable` was green the whole time and proves the form has an arm, never that the arm writes `completed`.** **AND THE REASON WAS ALREADY IN MY OWN HAND ONE ENTITY OVER**: `fully_populated_row()` exists because `skip_serializing_if` drops `None` fields. **There was no `fully_populated_thread()` until today.** dc's form: **the correct scope of a rule is not visible from the incident that produced it.**
- (2026-08-24) **THE COMMENT CLAIMS THE PROPERTY IT FAILS, AND ONE BLOCK CARRIES TWO DEFECTS IN OPPOSITE DIRECTIONS.** _"the row written is the authored scalars over the stored children and never a partially-defaulted document"_ is **TRUE of the four fields on the lines directly below it and FALSE of the nine it does not reach** -- it describes the code it SITS ON and is read as describing the ARM. Same paragraph also **overstates an absence** (_"no write path at all"_, killed by cc). **Overclaim and overstatement, same commit, and that commit was the change meant to satisfy the criterion.**
- (2026-08-24) **cc AND vc REFUTED A SENTENCE IN `facade.rs`, NOT THE CRITERION.** The row says **"has no FIELD-SETTER VERB today"**; `"no write path at all"` has exactly ONE home repo-wide and it is hv's ruling comment. **Neither door is a field-setter** -- one is a whole-document parse plus a four-child graft, the other a whole-document AUTHORED replace. **So burning case 1 is INTACT AS WORDED**, and cc's scalar-preserving route is the one the row already names as the hazard. **The comment's own premise is half-right: _no field-setter verb reaches it AND the addressable door refused_ -- the first conjunct still stands, and it is the criterion's real claim.**
- (2026-08-24) **THE `session_id` COLUMN IS COMPLETE, AND THE EVENT IT RESTED ON WAS THE ONE NOBODY HAD OBSERVED.** Both prior datapoints were NEGATIVE (compact -> unchanged, cross-day `--resume` -> unchanged), so its discriminating power rested on an assumption. **This session supplied the positive limb: board recorded `d5a0bd62`, env reads `b555eb67`, DIFFERENT -- and a SECOND field of a different mechanism, `SessionStart` firing `startup` rather than `resume`.** compact same / resume same / relaunch different, **measured on all three.** Each node can only report about itself; that is the whole lesson of the zero-of-four. **AMENDED after this session's own `/compact`: the compact limb is now FIRST-HAND too** -- env read `b555eb67` before and after, unchanged, so two of the three limbs are mine and only `--resume` still rests on a peer's report.
- (2026-08-24) **A FORMAT FLAG MAY CHANGE HOW A RESULT IS RENDERED AND MUST NEVER CHANGE WHAT A FAILURE SAYS, WHICH ARGUMENTS ARE LEGAL, OR WHAT THE COMMAND EXITS.** Derived from a measured defect rather than argued from taste: `git rev-parse --short` is a FORMATTING flag that silently narrows the arity and destroys the diagnostic. **`export --format` is the model and already satisfies it.** This is what `enum_flag()` must carry.
- (2026-08-24) **THE SAME CONCEPT IS SPELLED TWO WAYS ACROSS SIBLING VERBS, AND OUR OWN RESTART DOCS TEACH THE WRONG ONE.** `issues list --kind` vs `st list --status`, with `all` legal in both vocabularies, so the two verbs mutually mis-teach and each refusal names the FLAG rather than the CONCEPT. **`restart.md:182` teaches `st list --status all` as a TRAP-AVOIDANCE RULE and that rule is what walks a reader into the adjacent trap.** Four names for one concept, three in one line of help. **Caused no wrong answer today -- rc=1 and stderr both spoke; the near-miss came from reading STDOUT, where a refusal and a true empty are the same zero bytes.**
- (2026-08-24) **THE ESTATE HAS INSTRUMENTS FOR CAPABILITY CLAIMS AND NOT ONE FOR COST CLAIMS.** `at lint`, `doctor`, the roster and canon-commit all red on a capability; **nothing here can red on _this would be slow_.** dc's `bin/intent3:60-66` asserted a coherence check would be a _MULTI-SECOND gate_; driven, ~110ms -- **wrong by two orders of magnitude, and it was the load-bearing sentence in a comment justifying not doing the check.** vc's half is the better one: **a report gets CHECKED, a rationale gets HONOURED.** Remedy is not another guard: **a cost figure carries its measurement or is written as an estimate.**
- (2026-08-23) **THROWAWAY INSTRUMENTS ARE WHERE UNDECLARED BLIND SPOTS LIVE, AND THE DISPOSABILITY IS THE CAUSE.** Nobody writes a caveat into a script they are about to delete, **and a throwaway's OUTPUT outlives it by exactly as long as it takes to paste a number into a message.** Five instances, four nodes, one day.
- (2026-08-22) **hv HANDED vc THE PEN; A RULING vc MAKES UNDER IT IS A vc RULING AND NEVER AN hv ONE.** The HV bucket used to be a FALLTHROUGH from a failed regex; it is now `authority == hv && record == null` over declared fields, **so the pen boundary is structural rather than remembered.**
- (2026-08-21) **AC-08.5 IS SPLIT-CREATE-FROM-UPDATE IN `Facade::put`; cc BUILDS AND ic COVERS** (hv) -- deliberately different nodes for builder and verifier on a gate row that is ic's.
- (2026-08-21) **JSON OUTPUT IS `--format=json` WHERE OTHER FORMATS EXIST AND `--json` WHERE THEY DO NOT** (hv). The 3-3 dead heat was not a tie: **the two spellings encode different ARITIES**, so counting instances was the wrong instrument.
- (2026-08-21) **RETIRING A DRIVER IS SEQUENCED SO THE CLAIM IS NEVER NOBODY'S: THE ADOPTER LANDS FIRST, THE RETIREE SECOND.** Overlap is a visible self-healing duplicate; a gap is a silent green over an undriven claim.
- (2026-08-20) **`intent critic`'s usage-error exit 2 is correct and stays in v3.** _A gate should fail open on its own breakage and closed on yours._ **Clap is the wrong layer to enforce a declared vocabulary** -- it rejects at exit 2, the code the gate fails OPEN on. Enforce in the renderer at exit 1, reading the set from the table.
