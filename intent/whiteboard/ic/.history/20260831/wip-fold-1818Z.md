---
node: ic
name: Interface Claude
role: interface
session_id: 11cef60b-409e-4bcc-b0f5-808d43639e75
heartbeat_at: 2026-08-31 15:54Z
status: active
focus: "ST0064 int macos chunk 1 LANDED + proven end-to-end (d00956ec). NEXT, all hv-asked this session: the TUI design page (WP-17) HELD on hv's slash-fork answer; the intent app start/stop/restart CLI verb (NEW, Rust, coord cc); int macos chunk 2 (sign+notarize all three, .app like Conflab, dc's 3 header edits). AC-09.6 DELIVERED to vc (81/60/21, cleave, silent-drop finding) -- vc taking to hv. ST0064 is 0/9 GREEN (built != verified); AC-01.7 the build verb is the keystone and it now works."
claims: [ST0065, ST0056/09, ST0056/17, ST0064]
---

# Interface Claude (ic)

**Thirteenth fold (2026-08-31 15:54Z). Pre-fold verbatim at `.history/20260831/wip-fold-1554Z.md` (sha-verified). RE-MEASURE EVERY FIGURE AT PICKUP -- five nodes write this tree and the delivered binary lags HEAD / can be gone (W11). Coordinated by vc; hv drives ST0064 + WP-17 directly.**

## DOING

**ST0064 -- int macos chunk 1 LANDED (`d00956ec`), proven end to end.** `int macos app-doctor/icons/build/run/test/install/verify` -- xcodegen generates, xcodebuild REALLY compiles the Swift, 9 IntentTests pass, stamps substitute (3.0.0/build/commit/tree), the turtle asset catalogue is tracked + regenerated by app-icons. dc reviewed + acked; 4 findings applied (silent empty-hash, double-printf stamp, install temp+swap, app-test now stamps). The Swift app builds + tests FOR REAL now, not typecheck-claimed.

**ON THE BOUNCE, THE #1 PRIORITY IS THE EXPLORER IA/UX DESIGN DOC** (hv, reiterated 2026-08-31): a PROPER, DETAILED design doc as a BROWSER ARTIFACT -- the kind hv opens and reviews to VALIDATE how the explorer will actually work. Load `artifact-design` first; ground it in `tui/mode.rs` + `tui-design.md` section 3 (the reframe + the one fork are in TODO-1) + the three screenshots (OMNIBOX/MENU/NAV modes). Only blocking input: hv's `/`-fork call (Esc-toggle + guarded-`/` is my rec). The `intent app` verb + chunk 2 are unblocked, queued behind the doc.

## TODO

1. **TUI design page (WP-17 / ST0056-17)** -- hv wants a design page for the explorer IA/UX. THE REFRAME (grounded in `tui/mode.rs` + `tui-design.md` section 3): the machine is ALREADY a clean invariant-tested 5-mode graph (OMNIBOX/NAV/MENU/FIELD/EMBED) AND already folds `:`-cmd into the omnibox (hv ruled it 2026-08-30). So "dog's breakfast" = (a) hv running a STALE binary + (b) realiser legibility, NOT a design hole. Five of hv's six asks are already the ratified design. THE ONE FORK: hv wants `/` to CYCLE NAV->OMNIBOX->MENU, but `/` is a legal address char (`st/ST0056`), so the ratified machine uses Esc to toggle OMNIBOX<->NAV + `/` for guarded (empty-buffer) menu-entry. MY REC to hv: keep Esc-toggle + guarded-`/`, put any rotate-key on non-address data. BUILD the page once hv picks (load artifact-design skill first).
   - **hv's EXPLICIT SPEC the doc MUST address (verbatim intent, re-pasted 2026-08-31 so it survives compact):** (i) launching the explorer with an entity id DRILLS DIRECTLY to that entity in EDIT mode; (ii) edit mode is either FULL-SCREEN (editing an ST) or LIST/DETAIL where TAB swaps list<->detail; (iii) TAB switches between visible panel sections in the current window; (iv) BACKSPACE goes back to the previous page/screen; (v) ENTER selects and/or goes forward or into edit mode; (vi) two top-level modes -- EXPLORE/NAV (drill into entity, ARROW keys move up/down between fields) and MENU/COMMAND; (vii) `/`-mode = Lotus-123 menu, `:`-mode = type a `:command` to execute; (viii) `/` CYCLES between OMNIBOX, MENU, COMMAND; (ix) hv's own refinement: DROP `:`-mode -- put `:commands` in the omnibox and auto-execute, leaving EXPLORE(NAV) / OMNIBOX / MENU, where `/` in the omnibox goes to menu, a typed `:command` auto-executes, and `/` cycles the three. The design doc shows AS-IS (the ratified machine + where the realiser diverges) and TO-BE (hv's model with the fork resolved), plus the drill/edit/back model and the entry points. Three screenshots on hand: OMNIBOX (thread list, "type an address"), MENU ("Go: Back Threads Issues Packages Criteria"), NAV (ST0065 field detail, "enter edit, arrows move, backspace back").
2. **`intent app start|stop|restart`** (NEW, hv 2026-08-31) -- USER-FACING Rust CLI verb controlling the INSTALLED `/Applications/Intent.app` (open + osascript quit; Geodica's start/stop/quit shape). New_surface `app` family; distinct from `int macos app-*` (dev pipeline). Coordinate cc on the dispatch-table entry + handler. Do NOT drive `intent` in a harness with `2>/dev/null` while the binary is out (W10).
3. **int macos chunk 2** -- app-sign + app-notarize; ALL THREE artefacts signed+notarized (hv: .app same as Conflab; `~/Devel/prj/Conflab/bin/macos.d` = the bundle-walk + stapling reference). dc's 3 header-truth edits ride in the SAME commit as app-sign (dc brings exact wording incl the .app-disclaimer rewrite that keeps the stapling asymmetry). Reuse cmd/macos's `kc_get`/`IDENTITY`/`notarytool`. Read the bundle's EMBEDDED marker, never the manifest. Notarize = hv's ADC. Same bar: proven, dc reviews.
4. **AC-09.6 dispositions** -- DELIVERED to vc (81 exposed / 60 served == code SERVED / 21 gap / 0 one-way falsifiers; cleave 4 namespace + 7 read leaf + 10 recoverable-mutate leaf; the SHARPER half is vc's silent-drop at `mcp.rs:97` -- `exposed_on_mcp` promises a tool, the generator drops it with no error, IN-AG-NO-SILENT-001). vc taking BOTH to hv. When hv rules the 21: I disposition + update `intent/st/ST0056/parity/ac-09_6-mcp-facade-read.md`. 0190 (organize/help dup) resolved by vc; my 21 untouched.
5. **Project-model CWD wiring** (AC-01.5 runtime + AC-01.3 graphql discharge) -- vc ruled store-ROOT-PATH-only (the registry's `root`); IntentCLI sets the child CWD for project-scoped cmds; daemon control machine-level. Then AC-01.3's one real `intent graphql` query (thread count in the menu).
6. **Rest, mine, not tag-critical:** AT-01.6 (cc's bound-but-silent-socket fixture, construct stale end-to-end); console AC-01.4 (gated on cc's `intent daemon logs`); AC-09.4 clause-2 CLASS check (move scan primitives to `tests/common/mod.rs`, coord cc, don't fork or close-on-instance); guide.rs rc=2 residual (G `init`/H `--limit` exit 2 vs guide's 1, non-tag-blocking); AC-09.2 (`intent_graphql` unowned, hv scoping); parity hardening; AC-06.8 (row red, not mine).

**Honest status:** ST0064 is **0/9 GREEN in canon** (all `satisfied: no`) -- code-written is NOT verified. AC-01.7 (the build verb) is the keystone that makes greening possible and it now WORKS; greening still needs per-criterion verification against a built+running app.

## Watch-outs -- mechanisms only

1. **`daemon status --format json`: keys ALPHABETICAL** (endpoint before state on live). The three literals are cc's tripwire (a renamed variant compiles + serialises but SILENTLY stops decoding in Swift) -- mirrored in HealthTests. **REBUILD intentd + the debug intent before driving daemon-backed behaviour.**
2. **The `intent://` canonical form is THREE slashes** (`intent:///threads/ST0000` -- empty authority = this project). Two slashes -> `threads` parses as authority -> refused (now names the cause, `3388d0fe`).
3. **`intent explore` NEEDS A TERMINAL** -- never shell it from the app; the pipe-safe door is `intent edit <addr> --path`. Both `edit` and `graphql` resolve the project by CWD walk-up -- the app must set the child CWD (project-model wiring).
4. **A FILTERED test run cannot report a verdict; only the unfiltered cargo exit code can** -- a suite piped through grep reports grep's rc 0 over a red suite. Capture cargo's own rc.
5. **xcodegen scans sources BY PRESENCE** -- regenerate the .xcodeproj on any source-set change (app-build does it via the sources.sha256 stamp).
6. **A CROSS-CRATE GUARD is invisible to `cargo test -p <changed-crate>`** -- `cargo test -p intentsvcs` before address-touching landings.
7. **cc's POST-VERIFY IS STANDING: `git show --name-only HEAD` must EQUAL your intended set.** List paths explicitly. **NEVER remove a peer's index.lock** -- wait for it to clear.
8. **THE STORE IS SHARED BEFORE THE TREE IS; NEVER write a shared extract a peer is mid-edit on** -- coordinate the window. An attachment does not realise to disk on `st attach`; realise deliberately, never `sync --to-disk`.
9. **MARKDOWN PRETTIER BEFORE `git add`. THE FIGURES ROT AND THE PROPERTY DOES NOT** -- re-run tools, never quote a peer's count.
10. **rc=127 + suppressed stderr = "ran and answered nothing"** (vc+dc, 2026-08-31). Check the EXIT CODE, never the output; no `2>/dev/null` in any harness driving `intent`. My macOS harness audited clean (xcodebuild captures + gates on rc; `command -v ... || die`; app-verify's LSUIElement `die` guards the version read after it).
11. **The delivered binary lags HEAD and can VANISH** -- `~/.local/bin/intent` symlinks into `target/release` and passes NO currency check (ST0058). Measured `a854d7c3` (stale, 9 rust-files behind HEAD), then DANGLING (rc=127) ~16:45Z; hv rebuilds via `dvb build all`. Drive `target/debug/intent` or check `instrument_currency_check.sh` for recently-landed behaviour; NEVER assert what the shipped binary does from a date (dc measured 11 register rows false of it).

## Decisions

- **hv 2026-08-31: all three artefacts (`intent`, `intentd`, `Intent.app`) Developer-ID signed + notarized, the .app same as Conflab.** Makes cmd/macos's "ships no .app bundle" header false -> chunk-2 rewrite.
- **hv 2026-08-31: `intent app start|stop|restart`** = a user-facing CLI verb for the menubar app lifecycle, distinct from the `int macos app-*` dev pipeline.
- **TUI (hv 2026-08-30, in `mode.rs` + `tui-design.md` section 3): COMMAND is folded into OMNIBOX; OMNIBOX is the rest state; Esc toggles OMNIBOX<->NAV and never quits.** hv's 2026-08-31 re-proposal confirms it. Open fork: `/`-cycle vs Esc-toggle + guarded-`/`.
- **AC-09.6 (vc, delivered): _exposed implies servable_; closes by NARROW or BUILD, hv's call; a smaller honest tool list beats a larger one with holes.** The silent-drop refusal at `mcp.rs:97` is the class-fix.
- **cc: `intent edit <address> --path` is the AC-01.5 resolver door** (`9508788`), pipe-safe, no terminal, no kind. `intent explore` needs a tty (declined). The `edit issue N --path` self-referential remedy (`facade.rs:1130`, "a thread or an issue") is handed to cc with the fix (issue has no realised form -> "a steel thread" only).
- **cc: health is a PROJECTION above `route()`** (Route stays two variants); STATE IS THE REMEDY (no `removable` field), gate unlink on absent; stale/absent split on the kernel lock; connect-then-lock order load-bearing.
- **vc: the app project model is (a) a single configured project storing the ROOT PATH only** (the registry's `root`); child CWD for project-scoped commands, daemon control machine-level; a later picker fills the SAME field. Cheap to overturn.
- **vc: AC-01.3 divides cleanly from AC-01.6** -- liveness from the query (positive), the kernel lock names WHICH kind of stopped (negative). The row REQUIRES one real `intent graphql` query else it is vacuously green.
- **dc: `int macos` app pipeline = option A** + 3 header-truth edits + read the bundle marker. Chunk 1 landed; (B)'s DEVBIN_LIB refactor waits for the tag.
- **hv: console IN for 3.0.1** via cc's `intent daemon logs` verb (a direct logfile tail was declined). ST0064 is an M PORT; criteria bind to WP-01.
- **vc/hv: the bare-number ladder** (family verb resolves in-family, ladder is fallback, family-agnostic verb lists candidates; can return >1 -> a VIEW). **nav singular is INTERNAL; the app speaks addresses only.**
