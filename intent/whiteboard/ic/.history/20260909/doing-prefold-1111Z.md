# ic -- DOING / Live / TODO / Holds as they stood BEFORE the fold of 2026-09-09 11:11Z

Archived verbatim from `intent/whiteboard/ic/wip.md` lines 19-89. Superseded by the fold, kept because a fold moves content to history rather than deleting it.

## DOING

**POST-COMPACT BOOT 2026-09-08 18:44Z. NOTHING OF MINE IS IN FLIGHT AND NOTHING OF MINE IS UNCOMMITTED** -- `git status` is the check, not this line. Two files were dirty at boot and **both are vc's, established from bytes rather than from who-plausibly-owns-what** (`W115`): `intent/todo.md` is one line, `ST0056/WP-15` flipping `[ ]` -> `[-]`, and vc has said they take it in tonight's global fold; `bin/.devbin/manifest.sha256` is a devbin `source_commit` bump. **`%an` is blind to all five of us, so the only honest discriminators are the trailer, the message convention, or asking** -- and asking is what settled both.

**GATES RE-DRIVEN AT BOOT, NEVER TRANSCRIBED.** `ST0064` **7/9** (`AC-01.4` is issue `0281`; `AC-01.7` needs Apple Developer credentials only hv can supply -- **both remaining rows stopped being engineering work**). `ST0065` **8/8 PASS -- and status WIP with both work packages WIP**, which is the reading that matters: an acceptance gate passing is not a thread finishing, the WPs carry the work. `ST0056` 119/142.

### FINDING 1 -- `ST0056/WP-15` AND `ST0065/WP-02` ARE TWO HOMES FOR ONE CONCERN, BOTH OPEN

**WP-15** _Skills catalogue triage: KEEP, UPDATE or RETIRE every Intent2-era skill_, scope **L**, WIP, fully specified, **vc's and actively being worked** (`30d27724f` starts it; vc claims `20221c5df` in their own words to cc). **WP-02** _Audit and cleanup of the /in-\* skills: retire the senseless, correct the wrong, rewrite the misguided_, scope **S**, WIP, **Objective `_(not yet written)_`** -- mine, via the `ST0065` claim.

**WP-15's deliverable list already contains WP-02's entire subject, verbatim**: _a check that the catalogue's cross-references still resolve -- `chains_to:` frontmatter, the `/in-*` names skills cite in each other's prose, and the rule IDs they name._ That is what `20221c5df` built.

**THE DISPOSITION LOOKS OBVIOUS AND IS STILL NOT MINE: collapsing a work package is a SCOPE call and scope calls are hv's.** Routed to hv through vc. **What I am doing meanwhile is the only part I am sure of -- NOT STARTING WP-02** -- so that when the `ST0065` rulings land I do not duplicate work already under way. **A collision found before either party has sunk effort costs one message; found after, it costs the smaller of two builds.**

### FINDING 2 -- THE BOOT RITUAL HANDS EVERY NODE A BOUND DRESSED AS A MEASUREMENT

`intent/restart.md` makes `intent --version` plus `git rev-list --count <marker>..HEAD -- native/rust surface` one of **three measurements before any work**, for every node, at every boot, and frames the result as a distance. Under vc's `0285` the marker's refresh trigger is narrower than the scope it claims, so **the marker can only be an ANCESTOR of the binary's true content** -- the range picks up commits already compiled in and the figure **overstates** the drift.

**`0285`'s own summary sentence has the sign the other way** (_makes a binary look more current than it is_); cc worked it independently from the append-only-history and clean-tree premises and reached my reading. **The mechanism, the cargo fingerprint evidence and the two-crate repro are all unaffected -- it is one sentence, in vc's issue, so it was routed rather than edited.**

**IT DID NOT FIRE TODAY, AND THE LIMIT ON HOW I KNOW THAT IS THE INTERESTING PART.** Release binary mtime 13:06Z against marker commit `b070c158f` at 13:04Z -- two minutes, so the marker is truthful for the live build and the three drift commits are three real ones. **But that is a FILE MTIME, which is a clock reading and not the build system**; the instrument that would settle it is the fingerprint `0285` already read.

**THE FIX IS cc's WORDING AND IT IS BETTER THAN MINE (`W118`): LABEL THE BOUND, DO NOT REMOVE IT.** _N commits touch the build scope since the marker, which is an upper bound on unbuilt work._ True, cheap, and it keeps a useful check. `restart.md` is globalfold and therefore vc's -- flagged, not touched.

### THE ONE THING THAT IS UNBLOCKED, MINE, AND NOT SOMEONE ELSE'S -- DRIVEN AT THIS BOOT

**`coverage_map.sh` -- the instrument `AC-06.1` NAMES BY NAME for its coverage half -- REFUSES TO PUBLISH A FIGURE, CORRECTLY.** Driven 2026-09-08: _the burn TSV does not cover the on-disk .bats estate -- refusing to publish a figure computed over the wrong corpus._ **98 baseline rows against 116 files on disk: 19 never measured, 1 phantom row.** The refusal is `lib_corpus.sh`'s `corpus_require`, and the instrument names its own remedy: **re-run `burn.sh` over the current estate; do NOT hand-patch the TSV, because a baseline stitched from several revisions is the drift the check exists to catch.**

**THE COST IS A RUN, NOT A BUILD, AND I TOLD cc THE OPPOSITE EARLIER THIS EVENING.** I reported the coverage half as unmet and _nobody is building the estate-wide version_. **The estate-wide burn is BUILT** -- `burn.sh` works and carries a per-file timeout it earned on a 3.5-hour hang. What is stale is the BASELINE, 2026-08-15. **I priced a gap without measuring it, in the expensive direction, corrected to cc unprompted.**

**AND THE TWO HALVES OF cc's `AC-06.1` HOLD ARE COUPLED, WHICH NEITHER OF US HAD WRITTEN DOWN.** `burn.sh` runs each file twice -- once with the default `INTENT_BIN`, once with `INTENT_BIN=/usr/bin/false` -- and the DELTA is the coverage. **With the default binding ambiguous the baseline arm has no defined subject and the delta means nothing**, so `INTENT_BIN` is a precondition of trusting the sweep rather than a second unrelated item. Holding them as a conjunction was right for a better reason than either of us stated.

### CARRY FORWARD

**vc's evidence standard, which keeps paying: A GREEN TEST THAT _DECLARES_ A ROW IS EVIDENCE THAT WHAT THE TEST CHECKS IS TRUE, NEVER THAT THE ROW IS SATISFIED.** Enumerate the criterion's clauses, name the arm covering each, and **the MAPPING is the evidence** -- not a file name.

**AND `0280` IS CLOSED, WHICH I ONLY LEARNED BY BEING CORRECTED -- I CARRIED IT INTO THIS BOOT AS _flagged twice, actioned zero times_ AND PUT THAT IN FRONT OF TWO NODES.** Driven since: `intent issues show 0280` reads **CLOSED**, fixed by cc today, and vc independently verified the outcome -- both syncs run, 23 of 23 skills byte-identical, whole trees compared. **`W117` IS THE ENTRY I WROTE THIS MORNING AND THIS IS ME BREAKING IT BEFORE LUNCH**, so it gets a third limb rather than a new number.

## Live, unfinished, not mine to close alone

**`AC-17.6` HAS ONE OPEN LIMB (a work package has no browser address; `View::Child` over the declared `wps` descent, size M with the TUI arm unmeasured). DO NOT START -- vc.** Full reasoning in `.history/20260905/doing-folded-2026-09-05-2026Z.md`.

**AND ITS ROW IS A WORKED INSTANCE OF `0269`:** `AT-17.6` is `to-write` with **no `file` at all**, while `edit_and_browse_reach_one_model.rs` opens `//! AT-17.6 / AC-17.6` declaring itself the witness. **All three row-first instruments are blind to it for three different reasons.** **vc ruled DO NOT add the citation** -- `at edit --file` on a to-write row with no path is one step from `0270`'s door.

## TODO

**NOTHING IS BOTH MINE AND STARTABLE. That is a real state, not an omission** -- everything of mine is delivered, filed, handed off, or in `## Holds` with the condition that releases it. **Do not manufacture work from this section; read `## Holds` and drive the conditions.**

**IF YOU WANT THE NEXT THING AND hv HAS NOT SAID: ASK. Do NOT pick up `0279`** -- it is cc's and landed at `b71363d4b`. **Do NOT pick up `AC-01.7`** -- it needs Apple Developer credentials, which is an external dependency and not unbuilt work; a reader scanning for something startable takes it by mistake, which is exactly why it is named here.

## Holds -- work I am NOT doing, each with the condition that releases it

**A hold without a condition is an abandonment wearing a hold's name, so every line below names what must become true, not who I am waiting on** -- a dependency can be discharged by someone who never reads this board; a condition I can check myself at every pickup.

**THE AXIS IS ACT VERSUS STATE (vc's refinement of W23, 2026-09-03), NOT MINE VERSUS SOMEONE ELSE'S.** An ACT is unfalsifiable at pickup by construction; a STATE or a completed EVENT left a trace I can read. `AC-07.7` -- _needs a keg built from fixed code, which needs a published tag_ -- was already the right shape, and it names another node's work.

**AND WHERE THE ACT GENUINELY IS THE GATE, DO NOT FAKE A STATE -- WRITE THE CLAIM THE ACT IS PROTECTING**, so a pickup has something it can falsify. Some work really does need a person; what must never be missing is the reason, because THAT is the part that goes stale.

**THE PARAGRAPH ABOVE ALREADY SAID _not who I am waiting on_, AND THREE OF THE FOUR ENTRIES BELOW NAMED WHO.** A rule stated at the top of a list is read as a property of the list. Audited and rewritten 2026-09-03; item 2 was already correct.

1. **The `v2:`-label census** -- RELEASES WHEN hv's board no longer carries the hold: a STATE I read at pickup, not an act I wait on. **AND AT PICKUP 2026-09-03 THAT CONDITION TURNED OUT NOT TO BE CHECKABLE BY ME, WHICH IS ITS OWN DEFECT.** A proven-instrument grep (positive control found a line I knew was there) finds NO v2-label hold on hv's live board and none in hv's `.history/` either -- **so my probe is aimed at MY PARAPHRASE, not at hv's words, and `absent` here means _not where I looked_ exactly as much as _lifted_** (W17). A condition whose subject was never recorded verbatim cannot be falsified, so it fails the same test as a condition naming a person. **DO NOT READ THIS AS RELEASED. The re-word owed is: record hv's actual sentence, or restate the condition against dc's work rather than against hv's board.** It is ratified and it is dc's; **do not ask them to break it.** Load-bearing beyond `0215`: vc carries it as the candidate `AC-00.16` amendment -- _an instrument's reach is bounded not only by what it checks but by what the ESTATE CAN EXHIBIT, and that bound is declared nowhere because it is a property of the ENVIRONMENT rather than of any tool._ **`0215` is now its worked case: in a self-hosted checkout the install root and the project root are ONE DIRECTORY, so no drive taken here could ever have exhibited the difference.** **vc RE-DROVE IT INDEPENDENTLY 2026-09-03 17:4xZ AND THE RESULT REPLICATED -- WHICH IS WORTH NOTHING, AND vc IS THE ONE WHO SAID SO.** Their grep chose ITS OWN tokens too, so two nodes tested two guesses about hv's words and neither tested hv's words. **REPLICATION DOES NOT REPAIR A SHARED BLIND SPOT** (W27). **AND vc NAMED THE CLASS BETTER THAN I DID: THIS IS A NEW LIMB OF W23, NOT AN INSTANCE OF THE OLD ONE.** The family was a condition phrased as somebody's FUTURE ACT; **this one names a DOCUMENT, which SOUNDS checkable and is the trap -- it is checkable against MY WORDING of what the document says, never against the document.** vc endorses the re-word as it stands.
2. **`ST0064` WP-01's REMAINING TWO, AND NEITHER IS ENGINEERING WORK ANY MORE (re-driven at this cut: `intent ac gate ST0064` reads 7/9, open `AC-01.4` and `AC-01.7`).** **`AC-01.2` and `AC-01.6` ARE DISCHARGED** -- satisfied 2026-09-08 at `97f74262` on a full clause-to-arm mapping, verified past the ingest. **They were not lifted by anyone landing anything: the witnesses already existed and nothing had cited them**, which is the argument for a condition that names a state rather than a person. **`AC-01.4` -- RELEASES WHEN `0281` IS DISPOSITIONED**, which is a design call: either the verb gets built and earns `IntentCLI.swift:131`'s sentence, or the sentence stops allocating and states the gap, or the app takes the job with group signalling. **There is nothing to fixture until that is decided**, and the remedy carries a constraint (assert `child.pgid == child.pid` FIRST, because `kill(-pgid)` degrades to signalling the APP if Foundation ever stops making the child a leader). **`AC-01.7` -- RELEASES WHEN APPLE DEVELOPER CREDENTIALS EXIST FOR THE SIGNING AND NOTARY FLOW.** hv's to supply. **The `int macos` lane question is CLOSED** (option A; `cmd/macos` carries `app-build|run|test|install|verify|doctor|icons`), so the pipeline is not what is missing.
3. ~~**`intent app start|stop|restart`**~~ **RELEASED AND DISCHARGED AT THIS CUT 2026-09-08. DO NOT RE-HOLD IT.** The condition was a STATE -- _an `app` verb family exists in the CLI_ -- and I drove it rather than reading it: `intent app --help` lists `start`, `stop`, `restart`, `status`. **The claim underneath the hold was that no such surface exists, and that claim is now FALSE**, so the hold goes on execution and not on age. **cc BUILT IT, NOT ME**, which is exactly why the condition was written as a state rather than as a person: a dependency can be discharged by someone who never reads this board, and this one was. **THE ENTRY THAT WOULD HAVE ROTTED IS THE ONE I ALMOST WROTE** -- _waiting on hv to rule it in and cc to coordinate_ -- which names a route rather than an outcome and would still read as blocked today, with the work already shipped.
4. **`0218` remedy 1** -- RELEASES WHEN hv answers **what `uninstall` PROMISES: the files it wrote, or the directory it emptied.** Not _implement pruning_ -- `prune_empty_dirs` IS called (`payload.rs:1131`) and refuses `dir == root` (`:1413`), which is the skill's own directory. **Ruling 5 PERMITS the removal; permitted is not promised.** The patch is small either way; the promise is the decision.
5. **The palette `Home`/`End` flip** -- RELEASES WHEN hv rules it. **A product-feel call, not a sequencing one, which is why neither I nor the pen may take it.** The `/` sigil means the palette's buffer is never empty, so the empty-composer guard cannot fire there -- **which also makes _only when empty_ a no-op rather than a compromise.** Flipping costs the query line its caret keys.
6. **`ST0065/WP-01`** (declared S) -- RELEASES WHEN the three rulings in `hv/inbox.ic.md` are answered. **THAT IS THE WHOLE HOLD. THERE IS NO SCOPE QUESTION AND THERE NEVER WAS ONE TO RE-DERIVE: EVERYTHING IS IN 3.0.1, NO EXCEPTIONS** (hv, first-hand, 2026-09-08, and twice before). **THE CONTRADICTING SENTENCE IN hv's BOARD IS STRUCK AT `fc77dd090`** (vc, under the pen, on evidence inside hv's own file rather than on my relay -- `:146` and `:408` are hv first-hand and both say the opposite). **THIS ENTRY USED TO QUOTE THE WRONG CLAIM IN ORDER TO REPORT IT, AND THAT IS WHY IT IS REWRITTEN RATHER THAN TICKED.** A correction that reproduces the error verbatim leaves the error on the page: the next reader greps the phrase, finds it here, and cannot tell a citation from an assertion. **The remedy for a document defect is DELETION, never a fifth copy of the correction** -- which is the same rule that made the strike right, applied to my own board.

7. **`0222` -- the explorer thread list's `Status | Created | Completed` columns** -- RELEASES WHEN `intent wp show ST0056/17` READS `done`: a STATE I check at pickup, needing no message from anyone. **Re-driven at this cut 2026-09-08 and `intent wp show ST0056/17` still reads `WIP`, so the hold STANDS.** **SIZE UNMEASURED; do not quote one.** The blocker is the row type, not the columns: `Row` carries one name and one value, and the thread-collection arm is the one view that does not go through `form::triples`.

**TWO ENTRIES LEFT THIS SECTION IN THIS FOLD AND BOTH WERE EXECUTED, WHICH IS THE ONLY GROUND ON WHICH A HOLD MAY GO** -- the Explorer/Lotus menu (premise refuted, discharged) and `skills sync --dry-run` (condition met 2026-09-04). Their lessons are W29 and W23; the detail is in `.history/20260904/wip-prefold-1548Z.md`. **A fold keyed on a DATE rather than on execution is how an unexecuted ruling gets buried, and this section is where that would happen.**
