## DOING

**BOOT 2026-09-09 08:09Z, THEN A FOLD AT 11:09Z. hv RULED ON FOUR OF MY SIX OPEN ITEMS THIS MORNING AND I EXECUTED WHAT WAS MINE.** Every figure below was driven at the cut it names. **NO FIGURE ON THIS BOARD IS EVIDENCE; RUN THE VERBS.**

### RE-BOOT 19:08Z: `0294` FILED, AND THE SURFACE THAT WOULD HAVE REPORTED IT IS THE SURFACE THAT BROKE

**`bin/devbin cli` HAS REFUSED EVERY INVOCATION SINCE `86573ac17` (2026-09-05 10:30Z) -- FOUR DAYS.** `cmd/cli:69` sources `artefact` and `currency` and never `sharedtarget.lib`, which is where `SHARED_TARGET_DIRT_SCOPES` is declared. `_build_inputs_changed` tests that array in its FIRST statement -- before `base` is read, before git is called -- so it returns `noscope` and the verdict refuses.

**THREE CONTROLS, AND THE THIRD IS THE ONE THAT MAKES IT A DEFECT RATHER THAN A DEAD VERB.** POSITIVE: the identical call with `sharedtarget.lib` sourced computes a real verdict. DISCRIMINATING: with `base` set to HEAD -- **a perfectly current binary** -- it returns `0`/`ok` with the scope set and `noscope`/`refuse` without it, so **the refusal is independent of binary currency**. THIRD: **the remedy the message prints cannot clear the refusal**, because `int local build` rebuilds a pair and the fault is a missing `source`.

**AND THE MESSAGE ASSERTS A CONCLUSION THE INSTRUMENT NEVER COMPUTED** -- _the binary really does not describe this tree_ rides the generic `refuse:` branch. **Today that sentence is TRUE, which is the hazard**: it is right by luck, and a current binary draws the same confident words. My own W106 family, arriving in someone else's code.

**BLAST RADIUS IS NOT INTERACTIVE USE.** `intent/wip.md:71` already records that nobody types `bin/devbin cli`. **vc's cross-project instruments HARD-GATE on it**: `whiteboard/vc/lamplight-run.sh:8` and `reconvert.sh:16` both `exit 8` on refusal, and three more scripts instruct _gate coherence with `int cli --version` first_. **Read from source, not driven** -- running them reaches other projects.

### THE SHARED BINARY IS GENUINELY STALE, AND cc's MORNING LINE IS EXPIRED

**MEASURED TWO INDEPENDENT WAYS, NEITHER OF THEM THE VERSION MARKER.** Binary mtime **2026-09-08 13:06Z**, with all six code commits landing after it (earliest 13:21Z the same day). The tool's own verdict, once its input is supplied: **11 non-test files under `native/rust, surface, docs/design, lib/templates` changed since `b070c158f`**.

**cc's 08:08Z READING -- _the count is 3 and the compiled behaviour is unchanged_ -- IS NOW FALSE**, and not because cc was careless: it was true when driven and four code commits landed after it. **The upper bound is 7 commits (8 with `lib/templates`), and here it is TIGHT rather than loose** -- the mtime settles what the marker could only bound. `restart.md` is right that non-zero proves nothing; **it is the mtime, not the count, that proves this one.**

### WHAT LANDED, AND THE TWO WRITES ARE VERIFIED PAST THE INGEST

**`ST0065/WP-02` IS CANCELLED (hv's scope ruling, executed by me via `intent wp cancel` -- never `fc`).** Collapsed into `ST0056/WP-15`, which is vc's, L, WIP, fully specified, and already carried WP-02's whole subject verbatim. **CORRECTED AGAINST MYSELF 2026-09-09 19:08Z: I CITED _an S-sized cover with Objective never written_ AS THOUGH IT DISTINGUISHED WP-02, AND IT DISTINGUISHES NOTHING.** `ST0065/WP-01` HAS THE IDENTICAL EMPTY OBJECTIVE -- driven from canon: **17 of 168 WPs estate-wide carry an unwritten Objective**, `ST0065/1` and `ST0065/2` among them. **The load-bearing ground is untouched and it is the only one that was ever needed: WP-15 already carried WP-02's whole subject verbatim.** The empty Objective was a property of the POPULATION, not of the item I was cancelling -- my own precision-is-a-claim-about-the-corpus family, on my own filing. (**And the census reconciles rather than overturns my earlier _9 of 47_: that was LIVE THREADS ONLY -- `ST0056/18` + `ST0064`'s six + `ST0065`'s two = 9. Two correct answers, neither meaningful without its population; `ST0023`'s eight sit on a closed thread.**) `intent ac gate ST0065` now reads **8/8 PASS** with WP-02 out of the population. **I never started WP-02, which is why the ruling landed on nothing.**

**`0291` IS FILED, high.** `intent edit wp <thread>/<seq> --path` returns the PARENT THREAD's file at rc=0. Detail below; it is the thing I found by measuring `AC-17.6` before hv ruled on it, which is what the measurement was for.

### `AC-17.6` MEASURED, AND THE ANSWER IS A THIRD PLACE NEITHER CANDIDATE NAMED

**THE QUESTION WAS _is the existence defect in `--path` alone, or in resolution both arms share_. IT IS NEITHER.** Two controls put the seq inside the resolver: `wp ST0056/99` refuses with _ST0056 has no WP-99_ and `wp ST9999/17` refuses with _no steel thread ST9999_. **So both components arrive, both are validated, and the emitted path is the thread's.** The defect is in REALISATION -- downstream of parsing and downstream of resolution.

**AND IT IS A REGRESSION IN THE BAD DIRECTION.** `0153` records the same command REFUSING at rc=1 on 2026-08-30. Today it succeeds. **`intent/st/ST0064/WP/01/info.md` exists on disk**, so the right answer is present and a different one is handed back. A caller who opens that path edits the THREAD believing they are editing the work package, and rc=0 plus a real existing file is the pair that defeats every caller-side check.

**THE ADDRESS MODEL AGREES AND NOW SAYS SO IN ITS OWN WORDS:** `intent:///threads/ST0064/wps/01` refuses with _an address ends at the entity it names_. **A work package is not one.** That is the same shape I read in the browser grammar -- `View::parse` matches at most three segments, so `View::Item { kind: "wp", id: "ST0056/17" }` renders to `/wp/ST0056/17` and parses back as `Children { kind: "wp", id: "ST0056", field: "17" }`. **It neither round-trips nor refuses, against `parse`'s own docstring.** HONEST LIMIT: that half is read from source, not executed. The `edit --path` half is driven.

**THE CONTRACT TEST CANNOT EXHIBIT IT, AND ITS DOCSTRING CLAIMS OTHERWISE.** `every_view()` says it holds the round trip _over the corpus rather than over three hand-picked examples_. It enumerates KINDS from the real declaration and hard-codes **`id: "ST0056"` for every one**, `wp` included. **The kind axis is a corpus; the id axis is one literal with no separator in it.** A green is evidence that ids WITHOUT separators round-trip, never that ids do.

**SIZE, NOW MEASURED RATHER THAN BY ANALOGY: 11 exhaustive-match sites, 6 files, 2 crates, plus the contract test.** Consistent with the M I was carrying. **THE TUI ARM IS NOT AN UNMEASURED ARM -- IT IS NOT A SEPARABLE ARM AT ALL**, because it matches the same enum. That retires the qualifier I have been attaching to this size since 2026-09-05.

### THE BURN, AND WHAT MOVED UNDER IT TWICE IN ONE MORNING

**THE STATED CONDITION IS MET AND A DIFFERENT ONE NOW BINDS -- SAY SO PLAINLY RATHER THAN LET THE HOLD LOOK UNCHANGED.** cc's provenance line LANDED at `75f1c0a76`: `burn.sh` now refuses an unset `INTENT_BIN` and emits `SUBJECT=` and `VERSION=` on the TSV header, so the run can no longer mint a subject-less artefact. **WHAT BLOCKS THE RUN NOW IS A QUIET TREE, AND IT IS NOT hv's PERMISSION.** The subject must be a CURRENT binary or the sweep measures 2026-09-08; rebuilding is the precondition, and `0196` (open, high) has `int build all` delete the shared pair before building with no restore on failure. **Peers are live** (vc wrote their board minutes ago), and dc states the same constraint on their own board. **So: rebuild when the tree is quiet, then burn, then the baseline write goes through vc's ST0056 window (W2).** Not declined, and no longer hv's to release. Prior wording follows. **HELD BY hv: cc's PROVENANCE LINE FIRST, THEN THE RUN.** Not declined. My separability argument survives intact and vc said so explicitly -- an explicit `INTENT_BIN=<v3 binary>` is a parameter of the run rather than a change to the shipped default, and hv's flip is still separable. **What holds it is that `burn.sh` RECORDS NO SUBJECT** (cc's finding): the TSV header is `FILE TESTS DEFAULT_FAIL BURN STATUS` and the only `INTENT_BIN` in the emitting path is the mutant arm. **Burning today would mint a SECOND subject-less artefact.**

**MY OWN BOARD SAID THE DELTA _MEANS NOTHING_ AND THAT WAS THE SAFER ERROR, NOW OVERTURNED AGAINST MYSELF.** An ambient-default burn measures burning coverage of the **v2 shell CLI** -- `tests/lib/test_helper.bash:21` binds it -- which is a correct measurement of the WRONG SUBJECT. That is worse than meaninglessness, because it publishes.

**AND THE STANDING BASELINE'S SUBJECT IS RECOVERABLE, WHICH vc'S ROUTED WORDING HAD SAID IT WAS NOT.** `DEFAULT_FAIL` is `0` on all 98 rows; `BURN` is non-zero on 72. So the default arm was bound to a CLI the estate genuinely exercised AND that passed it whole -- which a mid-port v3 could not have been four days before self-hosting. **The BINARY CLASS recovers to v2; the BINDING MECHANISM and the exact identity do not.** That split is the correction that went to hv.

**AND THE ARTEFACT IS COMPLETE, SET-VERIFIED WITH A CONTROL.** At `f11e2001e`: 98 `.bats` files, all 98 eligible under burn.sh's own `[ "$total" -gt 0 ]` rule, 98 rows, `comm` returns 0 and 0 both ways. **Positive control against today: 116 files, 19 only-in-today** -- so the comparison detects a mismatch when one exists. `all 98 passed` is a real result, not a hand-filter. **burn.sh's no-filter guarantee was its own comment asserting its own behaviour, and it is now corroborated by an independent artefact.** vc reproduced every figure independently.

### `AC-17.6` BUILT ON vc's RULING, AND THE COMPILER FOUND TWO OF THE FIVE SITES

**`View::Child { kind, id, field, item }` LANDS THE FOURTH RUNG.** vc ruled BUILD IT under hv's pen (2026-09-09), on the ground that the old behaviour was the worst of three -- it neither round-tripped NOR refused -- which `IN-AG-NO-SILENT-001` forbids outright. `/thread/ST0056/wps/17` now renders and parses back to itself; the compound-id `Item` it replaces never could.

**MY M WAS RIGHT ON THE SIZE AND WRONG ON THE SHAPE OF THE WORK, WHICH IS THE FINDING.** I sized it at 11 exhaustive-match sites. **The compiler flagged TWO** (`render.rs:3805`, `tui/views.rs:111`); every other site matched through `..` or `_`, so it compiled clean and was silently wrong. **`rows_for`'s `_ => Vec::new()` would have rendered a work package as an empty collection** -- a level that looks like data with no members, which is the exact silent-partial this thread exists to remove. **A wildcard is exhaustive by construction, so the compiler cannot help; that is W9's inverted form arriving on my own change.** Five sites in the end: enum, `path`, `parse`, `present`, `rows_for`, plus `app_line` and the doors.

**AND THE DOOR IS THE HALF THAT WOULD HAVE SHIPPED HALF-DONE.** A `button` with no door visibly opens nothing, so without wiring `row.door` on the `wps` rows a work package would have been ADDRESSABLE by URL and UNREACHABLE from the surface that lists it. `children_of` now takes its `kind` rather than hard-coding `thread` for the door it hands out.

**THE CONTRACT TEST CLAIMED A CORPUS IT DID NOT HAVE, AND NOW SAYS SO.** `every_view()`'s docstring said the round trip was held _over the corpus rather than over three hand-picked examples_; kinds and descents were derived, **the id axis was the single literal `ST0056` for every one of them.** A green was evidence that ids WITHOUT a separator round-trip. It now carries `View::Child`, states the limit in its own docstring, and `a_separator_in_an_id_is_the_shape_view_child_exists_for` holds the half the walk structurally cannot.

**A LITERAL IN THE REFUSAL TEST HAD SILENTLY BECOME A LIVE PATH.** `a_path_that_names_nothing_is_refused_rather_than_guessed` listed `/a/b/c/d` as too deep. The fourth rung made it legal -- **the assertion went on passing while its meaning inverted**, from _this depth is refused_ to _this real view is unreachable_. The boundary is now DERIVED: take the deepest view the module can render, assert it parses, append one segment, assert refusal. The next rung moves it with nothing to remember.

**MUTATION-CONTROLLED, WHICH IS WHAT MAKES THE GREEN MEAN ANYTHING.** Deleting the 4-segment arm to restore the pre-fix grammar drives **three** tests red -- the new one, the corpus walk, and the derived boundary. Restored and green: 15 of 15 in the nav contract, and `cargo test --workspace` clean apart from work that is not mine.

### `AC-17.6`'s SECOND ARM: `browse` NOW REACHES WHAT `edit` COULD, AND A TEST THAT WATCHES FOR DRIFT COULD NOT SEE THE NEW MAPPING

**`view_for(Entity::Wp)` ANSWERED `None` AND NOW MAPS TO `View::Child`.** Its comment -- _`wp` is a declared kind whose item view nothing reaches_ -- **was true when written and went stale the moment `View::Child` landed**, which is the shape this estate keeps finding: a correct justification outliving the condition that made it correct. **While it answered `None` the browse arm could not reach what the edit arm could, and `AC-17.6` requires both to reach ONE model** -- so this is the criterion's second half, not a tidy-up.

**AND THE TEST WRITTEN TO CATCH EXACTLY THIS COULD NOT SEE IT.** `every_child_view_this_maps_to_is_a_descent_the_declaration_carries` holds the authored address-to-view mapping against the declaration, and its docstring names its own purpose: _an authored mapping beside a declaration is precisely the shape that goes stale in silence_. **It destructured `View::Children` alone and walked two COLLECTION urls**, so my new authored `wps` -- typed in `nav.rs` a few lines from the collection's -- was held against nothing. **Its own stated failure mode, arriving inside it, one enum variant away from the assertion.** Now walks both rungs.

**MUTATION-CONTROLLED, AND THE CONTROL IS WHAT MAKES THE EXTENSION MORE THAN A LINE.** Changing the item rung's authored field to `wpz` drives it red naming the exact url; restored, green. **A test extension without a control is a claim that the extension asserts something.**

**THE ROSTER MOVED BY RULING, NOT BY CONVENIENCE.** `WITH_VIEWS` in `explore_lands_where_the_address_names.rs` listed `wp` on the refused-by-name side. It moved to the has-a-view side **because the surface grew the level**, and the property the test holds -- every addressable form either opens a view or is refused BY NAME -- is unchanged. Recorded in the roster itself so the next reader sees a ruling rather than a fix-up.

**ONE RED IN THE WORKSPACE RUN AND I DID NOT ATTRIBUTE IT, WHICH IS THE NEW CANON WORKING ON ITS AUTHOR.** `daemon_watch::one_external_edit_costs_a_bounded_number_of_ingests` failed under the full suite and **passed twice on re-run** -- an ingest-counting test racing the suite's own parallel load. **A third category beyond _mine_ and _a peer's_: a timing-sensitive test failing under load.** Under the old habit I would have had a subject and a filing within the minute; `6c3c389` is vc's landing of the rule that stopped it.

### `AC-17.6`'s WP LIMB IS CLOSED, AND THE ROW'S OWN TEST FILE TOLD ME WHERE THE REMAINING WORK WAS

**THREE REFUSAL SITES, NOT ONE, AND I ONLY FOUND THE THIRD BECAUSE A TEST KEPT PASSING WHEN IT SHOULD HAVE BROKEN.** After mapping `view_for(Entity::Wp)` I re-ran the witness and `a_work_package_is_refused_by_both_spellings_and_says_why` was still GREEN. **Its docstring had predicted exactly that moment** -- _if a shape for `Entity::Wp` lands this test fails, which is the intended signal: the criterion's WP limb is then live and wants a real assertion instead._ It did not fail, so the refusal was somewhere else. **`browser_url` in `render.rs:1875` carried a hard `Entity::Wp` arm**, and `entity_for_item` was the second `nav.rs` function. Three sites; the compiler found none of them, and a green test found the last one.

**AND THE COMMENT ON THAT REFUSAL NAMED ITS OWN DISCHARGE CONDITION, WHICH IS THE BEST THING I READ TONIGHT.** It listed three routes, rejected two with reasons, and said of the third: _give `wp` a shape in `nav.rs`. That is a change to the `AC-17.12` contract and is vc's._ **vc ruled it tonight under hv's pen.** The two rejected routes stay rejected -- this is a contract that grew, not a relitigation.

**THE ASSERTIONS INVERTED RATHER THAN RELAXED, AND I WOULD RATHER BE JUDGED ON THAT THAN ON THE FEATURE.** `an_absent_work_package_...` required a present and an absent WP to get the SAME answer, on the sound ground that _a real WP-01 cannot be browsed either_. **WP-01 now works, so equality became the assertion that passes only if the fix did NOT land.** It now requires them to DIFFER. And INV-09 for work packages had been holding **by both spellings REFUSING** -- which the module header itself flagged as the fragile way for an invariant to hold. It now holds by both composing a URL, so the twin check finally discriminates.

**THE REAL WP ASSERTION IS THE ONE THE CRITERION ASKS FOR: `/thread/ST0001/wps/01` OVER THE WIRE EQUALS THE TERMINAL'S ROWS, BYTE FOR BYTE**, with the same vacuity guard the thread arm carries because two empty vectors are equal. The estate gained a real work package -- without one, the test would have measured the ABSENT path while claiming the present one. **Mutation-controlled: breaking `entity_for_item`'s `wps` rung drives it red.**

**ONE WORKSPACE RED THAT IS NOT MINE AND I DID NOT ATTRIBUTE IT TO A COMMIT.** `mutation_completeness::no_service_call_can_set_an_edgeless_field`, deterministic, about `AcceptanceTest.kind` -- committed at `587105cb0` (ST0066), nothing my diff reaches. **The test's own message says it may be a DECISION PENDING hv rather than a bug, and says _do not invent the ratification to clear the test_.** Routed to vc, not filed. **AND MY OWN CHECK FOR IT HIT MENTION-VERSUS-USE: `git diff --name-only | grep -E 'model|mutation'` matched `edit_and_browse_reach_one_model.rs` -- a FILENAME.** The fifth instance of that family tonight, in the check I ran to avoid the family.

### `WP-01` STARTED, AND ITS FIRST FINDING IS THAT hv's RULING IS UNEXECUTED AND SHARPER THAN THE PROPOSAL THAT ASKED FOR IT

**`init` DOES NOT LAY `AGENTS.md` DOWN, AND `init.rs:113-116` SAYS SO IN AS MANY WORDS: `NotByInit("generated by `intent agents sync`")`.** hv ruled the opposite on 2026-09-09. **The ruling is recorded on my board and in no code**, which is the third instance today of a ruling whose only home is prose (`ST0065` ruling 3 lived in a doc comment; `0218`'s lived at no call site; this one lives on a whiteboard).

**AND THE ESTATE ALREADY CALLS THE CURRENT STATE AN ERROR, WHICH THE PROPOSAL DID NOT KNOW.** `rootfiles.rs:473` declares a missing `AGENTS.md` **the one ERROR a missing file earns**. Driven in a sandbox on a fresh `intent init`: the tree gets `.prettierignore`, `CLAUDE.md`, `intent/.intentfiles`, `intent/llm/{ARCHITECTURE,DECISION_TREE,RULES}.md`, `intent/wip.md` and `config.json` -- **no `AGENTS.md`, no `usage-rules.md`** -- and `intent agents validate` then reports `error: AGENTS.md not found at project root`, **rc=1**, with the control (this project) rc=0. **So `intent init` ships a project that fails the estate's own check on its first run.** My proposal said only that the file was absent; the ERROR is the part that makes the ruling urgent rather than tidy.

**THE STALE BINARY IS A VALID SUBJECT HERE AND I PROVED IT RATHER THAN ASSUMING IT:** `git diff --name-only b070c158f..HEAD` over `init.rs`, `rootfiles.rs` and `lib/templates/llm/` is EMPTY, so the 2026-09-08 build carries today's code for this question. **That is the only honest way to use a stale instrument** -- not "it is probably fine", but a diff over the exact code the question exercises.

**AND I CAUGHT MY OWN INSTRUMENT ERROR BEFORE REPORTING IT: my first read said `rc=0`, which was `head`'s status through a pipe, not `intent`'s.** Re-driven with `out="$(...)"; rc=$?` it is 1, and the control is 0. **I nearly filed a silent-error defect (`IN-AG-NO-SILENT-001`) against a verb that is behaving correctly** -- the exact shape vc filed `0292` in, and W106 limb 2 with a pipe in place of an alternation.

**THE DESIGN ANSWER, AND IT DISSOLVES THE OBJECTION RATHER THAN OVERRIDING IT: `init` must GENERATE `AGENTS.md`, NOT SEED `_AGENTS.md`.** The `NotByInit` comment's objection is sound and hv's ruling does not dispose of it -- _seeding a file whose next regeneration overwrites it teaches the operator that it is theirs to edit_. **But that objects to SEEDING A TEMPLATE, not to `init` producing the file.** `rootfiles::render_all` already renders all three root files from project state. Seeding gives TWO shapes (a template copy at init, derived content after the first `agents sync`); generating gives one writer and one shape from the first moment. **Both the ruling and the objection are satisfied by the same change, and only by that one.**

**COST, HONESTLY BOUNDED: S, WITH ONE PART UNMEASURED.** `render_all(home, cfg, ctx)` wants a `Config` and a `RenderContext`; `init` holds `project_name`, `author` and `intent_version` and writes `config.json` itself at `init.rs:275`, so the inputs exist but the context construction does not, and **I have not measured that half.** **THE IMPLEMENTATION IS NOT MINE** -- `rootfiles.rs` and `init.rs` are cc's, as my own proposal section 7 already says. WP-01 is the review; this is the review's output, routed, not taken.

### `WP-01` CONTINUED: THE SEEDED SET MEASURED, AND `0224` AMENDED RATHER THAN DUPLICATED

**I NEARLY FILED A DEFECT THAT WAS ALREADY OPEN.** `0224` -- _init seeds an Elixir decision tree into every project and Destination cannot decline it_ -- has been open since 2026-08-3x with a driven test behind it. **The check that stopped me cost one grep of `intent/.canon/issues/` and I ran it because I duplicated nothing tonight only by luck twice already.** Amended it; did not re-file.

**TWO THINGS ARE GENUINELY ADDITIVE AND THE FIRST CHANGES THE REMEDY'S SIZE.** `0224` says `Destination` cannot express declining, and lists three dispositions -- a per-language variant, moving to the elixir pack, or dropping the artefact. **All three change a MECHANISM. A fourth needs none: the template engine ALREADY has `[[#lang <name>]]` and `[[#nolang]]` (`rootfiles.rs:293-298`), and `_AGENTS.md` already uses them across FIVE languages.** `_DECISION_TREE.md` uses zero.

**MEASURED ACROSS THE THREE SEEDED `llm/` TEMPLATES, AND THE CONTRAST IS THE FINDING RATHER THAN THE COUNT:** `_DECISION_TREE` 80 lines / 13 Elixir markers / 0 conditionals; `_RULES` 9 / 0 / 0; `_ARCHITECTURE` 9 / 0 / 0. **`init.rs`'s own comment records that `_RULES` and `_ARCHITECTURE` were REWRITTEN rather than carried over from v2** -- the v2 pair being _an incomplete template that reads like a spec_ -- and that both now _say what belongs in them, say that they are empty ON PURPOSE, and stop_. **The same review rewrote two of three and left the third, which is the one carrying another language's placement advice. Nothing recorded a decision to leave it.**

**THE SECOND HALF IS INDEPENDENT OF THE LANGUAGE QUESTION AND SURVIVES EVERY DISPOSITION BUT ONE.** `_DECISION_TREE.md` tells its reader to consult `MODULES.md` **three times** (lines 4, 47, 49), and `DESTINATIONS` refuses to seed `MODULES.md` **twenty-six lines from the entry that seeds this file**, on hv's 2026-08-24 Highlander ruling. **So a fresh ELIXIR project -- the case where the content is right -- still opens the file and is told to always cross-reference something the tool deliberately does not create.** The ruling reached the seeding table and never reached the seeded prose. **Two entries in one `const`, each correct alone, composing into an instruction nobody can follow** -- the same shape `0224` already names one level up.

## Live, unfinished, not mine to close alone

**`AC-17.6` IS NOW MEASURED BUT NOT DISPOSITIONED. DO NOT START THE BUILD.** hv has not ruled whether the `wp` addressing shape rides 3.0.1; what changed today is that the size is real and the TUI qualifier is retired. **`0291` is the live defect underneath it and is filed separately, deliberately** -- a silent wrong answer at rc=0 does not wait on a browser-address decision.

**AND ITS ROW IS STILL A WORKED INSTANCE OF `0269`:** `AT-17.6` is `to-write` with **no `file` at all**, while `edit_and_browse_reach_one_model.rs` opens `//! AT-17.6 / AC-17.6` declaring itself the witness. **vc ruled DO NOT add the citation** -- `at edit --file` on a to-write row with no path is one step from `0270`'s door.

## TODO

**`ST0065/WP-01` IS RELEASED AND IS MINE TO START.** hv answered ruling 1 -- **`init` lays `AGENTS.md` down** -- and DEFERRED ruling 2, which gates only Option 3 (M) and blocks nothing here. Ruling 3 was already dead. **The implementation home is cc's (`rootfiles.rs`, `canon.rs`); WP-01 is the review and rationalisation, which is mine.**

**NOTHING ELSE IS BOTH MINE AND STARTABLE, and that is a real state.** Everything else is delivered, filed, handed off, or in `## Holds` with the condition that releases it. **Do NOT pick up `AC-01.7`** -- it needs Apple Developer credentials, an external dependency and not unbuilt work; a reader scanning for something startable takes it by mistake, which is why it is named here.
