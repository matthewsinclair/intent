---
node: ic
name: Interface Claude
role: interface
session_id: 7c9b8dad-5c1f-49af-a9fd-9dbd287fc26d
heartbeat_at: 2026-08-19 18:56Z
status: active
focus: "**FIVE ROWS GREEN TODAY; ONE OF MY EARLIER GREENS WENT BACK TO RED ON MY OWN FINDING (AT-02.2, vc `29d4db5e`) AND I CONFIRMED IT RATHER THAN OVERTURNING IT.** **THE DAY'S BIGGEST FINDING: `intentfiles::render` HAS NO PRODUCTION CALLER** -- which is why the manifest declares 0 of 57, why `organize --apply` plans 544 removals, and why AC-02.2 cannot close. **BLOCKED ON A RULE THAT DOES NOT EXIST: nothing anywhere states WHICH STATUSES ARE REALISED.** Recommendation with vc; not building it on my own judgement because it changes what the gate does when it opens."
claims: [ST0057/02, ST0057/05, ST0057/07, ST0057/08, ST0056/03]
---

# Interface Claude (ic)

## DOING

**NOTHING IN FLIGHT.** AT-08.1 landed at `a11580b6`, row green. Everything of mine is committed.

**WHAT AT-08.1 CHANGED, because it is a SHARED WRITE PATH.** `Facade::projection` is manifest-aware: canon writes unconditional, only rendered views narrow. `apply` and both sync directions go through it. **Three states named rather than two** -- `Realised::NothingSaid` (no manifest, realise all), `Declared(set)` (an EMPTY set is somebody saying none and is honoured), `Unreadable` (fail-open, realises all, and has NO READER yet -- that gap is named in the source).

## ON RESUME -- read this first

0. **AT-03.14, AT-03.16, AT-03.13 AND AT-08.1 ARE DONE, GREEN AND COMMITTED (`46ab2220`, rows at `1808912e`). DO NOT REBUILD EITHER.** Next is AT-03.13. Read `## DOING` first; the mechanism, the three non-obvious decisions and the file list are all there. The only open action is the commit window and then moving the row.

1. **(DONE) AT-03.14** (`refused_ingest_blocks_egest.rs`). Red-first is EXACTLY REPRODUCIBLE and vc handed me the recipe: duplicate `id` in one thread's `tests`, `--to-store` refuses, `--to-disk` then DESTROYS the authored edit at rc=0. **A criterion whose failure I can construct on demand is the one to build first.**
2. **(DONE) AT-03.16 HAD A HARD REQUIREMENT, NOT A CAUTION: DO NOT DRIVE IT WITH OUTPUT SUPPRESSED.** The original data loss was silent ONLY because vc piped the write verb to `/dev/null`. **A test that reproduces the loss under suppression is measuring the suppression.** Assert on the STREAM, and say in the row how the run was driven.
3. **AT-03.13** (`mandatory_fields_reach_a_reader.rs`) -- every field the service layer REFUSES without must reach a human read face. Red-first arm is `status_reason`.
4. **DO NOT RE-DERIVE ANY OF TODAY'S WORK.** ST0057 WP-02/05/07/08 are built to their edges and committed. Four rows wait on dc (hydration primitive / dehydration) and each says so in canon.

## TODO -- LIVE ONLY

1. **ST0056 WP-03: AT-03.14, then AT-03.16, then AT-03.13.** Self-contained, none needs dc.
2. **AT-08.4 GOES GREEN WHEN `POST <collection>` CREATES AND RETURNS AN ADDRESS.** `fn post` has zero hits in `facade.rs`. **Do not offer the PUT refusal as coverage again** -- that is what I did, and vc measured it while I asserted it.
3. **`surface/dispatch-table.{json,md}` IS HELD, AND NOT FOR PERMISSION.** My edit is one string (hv's `edit` ruling). The diff is +45/-6 carrying dc's `organize` row -- `owner_wp`, `name_reclaimed`, an mcp block, and two fields addressed to me (`flag_polarity_open_to_ic`, `open_to_ic`). **`--only` would carry their line under my message.** Land it after dc does; answer the two open_to_ic fields then.
4. **`every_shipped_mutator_is_accounted_for` IS EXPECTED-RED UNTIL dc LANDS** -- "organize is in 0 buckets", 65 shipped mutators, 32 unproven. **Report it as expected, never as a break.**

## Open with others

- **vc** -- holds both threads and the mint pen; ran the whole day's row corrections. **Owes nothing.** Their method is the one to copy: they find my defects by asking WHAT THE CLAIM RESTS ON, not by knowing more.
- **dc** -- owns the hydration primitive (vc ruled), dehydration, `organize`, and the `organize` dispatch-table row. **Four of my rows and one held file are all waiting on them.** No handshake owed either way.
- **cc** -- unblocked by my `facade.rs` commit and now in that file. **`facade.rs`, `model.rs`, `lib.rs` are SHARED SURFACES and `--only` is not a defence on any of them** -- their finding, and it caught me once in `dispatch-table.json`.
- **hv** -- **corrected my commit discipline: the per-lane ruling WAS the authorisation.** My own lane, my own paths, no asking.

## Watch-outs

- **MY MUTATION DRIVER PRODUCED A FALSE RED AND A FALSE GREEN IN ONE SESSION, AND BOTH SCORED FROM `cargo test` rc ALONE.** False red: a mutant referencing an undeclared variable failed to COMPILE. False green: `cargo fmt` had reflowed the anchor, the injection silently did not apply, and the driver scored an UNMUTATED tree as a survivor. **CONFIRM THE INJECTION APPLIED AND THE MUTANT COMPILED BEFORE SCORING THE ARM.** rc!=0 is the same observable for "the guard bit" and "the arm never ran"; rc==0 is the same observable for "the mutant survived" and "there was no mutant".
- **RUN `cargo test --workspace`, NOT JUST THE CRATE.** `intentsvcs` was 510/510 green while the workspace was red on an estate guard in `intent-cli`.
- **EVERY RESTORE PATH IS ABSOLUTE.** Two `cp` restores failed on a relative path against a moved cwd; the second left a mutant live.
- **A RESTORE YOU DID NOT VERIFY IS A MUTATION YOU SHIPPED.** A `cp` restore failed on a cwd mismatch, I read past the error, and reported a 462-test green from a tree carrying an injected defect. **Grep for residue; never trust that the restore ran.**
- **A MUTANT THAT DOES NOT COMPILE IS NOT A RED.** My driver counted `cargo test` rc!=0 as "the guard bit"; one arm referenced an undeclared variable and scored RED for a reason unrelated to the guard. **Compile-check every mutant before scoring it** -- same shape as the partition driver's rc=127.
- **The Bash tool's cwd PERSISTS between calls.** `mkdir -p intent/st/...` from the crate dir created the tree under `crates/intentsvcs/`.

- **TWO DURATIONS NEITHER OF WHICH IS BANKABLE, DIVIDED, GIVE A MEASUREMENT THAT IS.** dc's three post-slate rows: summed test time 8.51 / 11.87 / 8.18 -- **barely moved. Only the RATIO to wall collapsed: 80x -> 115x -> 1.14x.** **So summed time is very nearly a property of the ARTEFACT, wall is a property of the APPARATUS, and the ratio between them MEASURES the apparatus** -- the instrument my own rule says a duration cannot be, reached by dividing two durations. **AND IT NEEDS NO CAUSE, which dissolves dc's refusal to name one rather than overriding it:** 1.14x says the machine is quiet, 115x says something is eating it, and the meter is silent on what. **A detector for the whole class with no mechanism named.**
- **THREE LIMITS ON THAT METER, ALL STATED BEFORE IT IS BUILT:** it needs a runner reporting both numbers; **a confound that slows the TESTS moves both terms so the ratio stays near 1 -- it detects OVERHEAD, not slowness**; and **it is only interpretable while test binaries run SEQUENTIALLY.** If they are ever parallelised (dc's 81-binary consolidation is in that neighbourhood) wall drops below summed and **a ratio under 1 means the MODEL NO LONGER APPLIES, not that the apparatus is idle -- it must be emitted as UNDEFINED, never as a good number.** Same shape as everything else today: a comfortable value produced by a model that has stopped applying.
- **AND FOUR NAMED CONDITIONS ARE NOT A RECIPE.** dc offered their post-slate figure as the form my rule specifies. **It is not: my claim is that the apparatus is NOT ENUMERABLE IN ADVANCE, so stating four conditions discharges the four you thought of and Gatekeeper is the standing proof the load-bearing one is the one nobody lists.** Theirs is also not reconstructible -- nobody re-takes it without a freshly built 3.6GB single-target tree. **A list of conditions is a better-decorated point observation; a recipe re-measures where a number replays.**

- **A CHANNEL'S ABSENCE IS NOT A DEFECT UNTIL SOMETHING IS OBLIGED TO READ IT** (vc, generalising my instance, and it is the best form of the day). My partition printed only on the CLEAN branch -- **cosmetic at one hour and a hole at the next, with NOTHING IN THE CODE CHANGING; vc's rc ruling changed it.** **So every `X must be in the output` ruling has to name WHICH PATHS, or it creates an obligation the FAILURE path does not satisfy.** vc has made that ruling twice today and named the paths neither time. **THE COVERAGE QUESTION IS NOT LESS URGENT WHEN SOMETHING IS WRONG, IT IS MORE.**
- **`MEASURED AND REJECTED` IS A DIFFERENT ARTEFACT FROM `ABSENT`, AND ONLY THE SECOND STOPS THE NEXT NODE REBUILDING IT.** I built class 3 (a ratio separated from its operand by a word) as a bounded 3-token window, drove it, and **REVERTED IT ON THE YIELD: 5 new rows, 3 plainly PROSE** -- `set of 0/1/2`, `48 bits of clock + 80 of randomness` read as `48 of 80`, and a `%d` from a different clause. **Majority false positive, refused on my own rule: a finding manufactured by an instrument's own reach is worse than a missed one, because it gets acted on.** **AND THE ADJACENT-TOKEN RULE TURNED OUT TO BE A PROXY FOR `THESE TWO NUMBERS STAND IN A RATIO` RATHER THAN A CRUDE SHORTCUT**, which is what justifies keeping it. The reach records it as built-driven-rejected, not as undone.
- **THE ADJUDICATOR WAS BLIND TO THE TOOL THAT SUPPLIES ITS OWN POPULATION.** `of_n_population.sh` emits ratios AT-00.12's instrument could not see, and neither dc nor I noticed across a full day of using both. **Cause: `%d`/`%s` format operands were invisible -- my numeric-ish test needed a digit or a `$` and a specifier carries neither, so printf-format, the estate's commonest emission shape, was ENTIRELY unreached.** vc verified five such shapes in that file; my three were the adjacent ones and the other two are the refused class 3. **Publishing the definition is what made that checkable by anyone but me.**

- **THE RELEASE GATE CAN SEE 6 OF 43 PARITY INSTRUMENTS, AND IT FAILS IN TWO DIRECTIONS THAT ARE ONE DEFECT.** Measured across BOTH threads (vc's 8/5 was ST0056 only and had already reached matts as `5 of 43`): **43 on disk; 15 cited by an AT row; 6 cited AND on disk; 9 cited but NOT on disk; 37 uncited, of which 8 are libs/generators and 29 are real instruments with no row.** **Direction 1 -- instruments the arithmetic cannot see (29).** **Direction 2 -- rows the arithmetic COUNTS but nothing can falsify (9 rows citing files that do not exist).** **29 UNCITED IS NOT 29 DEFECTS** (vc's honesty check, kept verbatim) -- the claim is about VISIBILITY, not missing rows; saying otherwise is the shrunken-roster error pointed at the estate.
- **AND DIRECTION 2 IS cc'S KIND HOLE WITH A POPULATION.** Their board: _the stale-AT guard catches a `to-write` row citing a file that EXISTS, and cannot catch one citing FICTION._ **Nine rows cite fiction now, and NOTHING checks that what eventually appears matches the KIND the row declared** -- which is how cc came within one clean slate of compiling two Rust files against two shell rows. vc had queued it as a capability gap with ONE member, that member an averted near-miss; **it has nine, measurable today.** **The right frame is not `none of the 16 WPs covers the instruments` (an absence, taken on trust) but `6 of 43` (a count matts can check).**

- **AN INBOX IS ALSO A DURABLE RECORD, AND THE ARGUMENT I MADE AGAINST RETIRING IT IS WHY THE RULING TOOK THE SHAPE IT DID.** **RESOLVED: the inbox STAYS; vc monitors and surfaces; hv rulings land in vc's `## Decisions

- (2026-08-19) **AT-02.2 IS BACK TO RED AND I CONFIRMED IT ON MEASUREMENT.** AC-02.2's subject is the VERB -- _survive an `organize` rewrite ... with the generated region changing IN THE SAME RUN_ -- and `intentfiles_two_regions.rs` calls `render(` seven times and `Facade|organize(` zero. **The verb provably cannot do it: no call site.** Not a coverage gap; a criterion whose subject does not perform its verb.
- (2026-08-19) **vc's DISCRIMINATOR AGAINST THE AT-04.x GAP IS MECHANICAL RATHER THAN A JUDGEMENT: IS THE CALL SITE PRESENT?** There the mechanism was wired and one limb was hand-evidenced. Here the wiring does not exist. Same-sounding shape, opposite verdict.
- (2026-08-19) **`intentfiles::render` HAS ZERO PRODUCTION CALLERS -- and that one fact explains the manifest, the 544, and the red row.** It is `views::write_all`'s shape (now repaired, so ONE live instance and one closed). **The class: an uncalled function with no tests looks suspicious; one with GREEN TESTS looks finished** -- the tests are what hide it.
- (2026-08-19) **CLOSING AC-02.2 NEEDS A RULE NOBODY HAS WRITTEN: WHICH STATUSES ARE REALISED.** The manifest header and `design.md:115` both say _a function of status_ and neither states the function. **So `render` has no caller partly because nobody could have written one.** My recommendation to vc: **realised iff NOT terminal** (not completed, not cancelled) -- the only reading `design.md:115` supports, and it makes the pinned region's purpose coherent, since you pin precisely to override the terminal case. **NOT built on my own judgement: it changes what the gate does on the day it opens.**
- (2026-08-19) **I MEASURED THE FALSIFYING FACT AND FILED IT WHERE NOTHING COULD COMPARE IT.** _No `intentfiles::render` call in `organize.rs`_ went into AT-02.4's note as support for a NEW row while AT-02.2 sat green one screen away. **Nothing was wrong; a true measurement was put somewhere it could not do its job.** vc's general form: **two artefacts disagree and no third thing reads both** -- it survived TWO validation reads.
- (2026-08-19) **AND I PAIRED IT WITH `views::write_all` WITHOUT CHECKING THE REPAIR.** I cited AC-03.14's note as describing the present when it described the moment it was written. **Measured-at versus asserted-about, inside the sentence naming that exact class.**

- (2026-08-19) **AC-05.1 POLARITY, RULED: `intent organize` PREVIEWS BY DEFAULT AND `--apply` PERFORMS.** vc raised it and declined to rule it; it is my row and this is the ruling. **The verb is dc's and the shape is handed to them rather than built by me.**
- (2026-08-19) **GROUND ONE, AND IT IS A PRECEDENT RATHER THAN A JUDGEMENT: AC-03.9 ALREADY RULED THIS SHAPE.** The destructive direction states what it will overwrite BEFORE it costs it -- _a summary afterwards is a receipt for a loss; the operator needed it one moment earlier_. `organize`'s removal half is destructive in exactly that way: **544 removals planned on the real estate**, measured by vc in a disposable clone. A second destructive verb answering the same question differently is the Highlander problem in the surface.
- (2026-08-19) **GROUND TWO: v2 SHIPPED BOTH POLARITIES FOR ONE OPERATION AND THE DISPATCH TABLE RECORDS IT AS A LIVE VIOLATION.** `intent organize` took `--dry-run` (acts by default); `intent st organize` took `--write` (previews by default). **Two faces, one operation, opposite defaults.** Resolving it toward preview resolves it in the direction that cannot lose data; resolving it toward acting resolves it in the direction that can.
- (2026-08-19) **GROUND THREE, MEASURED MYSELF RATHER THAN RELAYED: v3 CURRENTLY HAS THE WORST OF BOTH.** `intent organize --help` prints `Usage: intent organize` and `-h` alone -- **it kept the acting polarity AND dropped the preview entirely.** Its own one-line help says _remove what is not_, so the destructive half is advertised and unobservable in the same sentence. **There is no way today to find out what `organize` would delete except by letting it.**
- (2026-08-19) **WHY `--dry-run` IS THE WRONG SPELLING EVEN IF THE DEFAULT WERE RIGHT: it makes safety OPT-IN, and the operator who most needs the preview is the one who does not know to ask for it.** `--apply` is also the estate's own vocabulary -- `Plan::apply` already exists in `organize.rs`, so the surface and the model would use one word for one concept.
- (2026-08-19) **THE COUNTER-ARGUMENT, STATED AND ANSWERED HONESTLY.** `organize` is meant to be routine and idempotent, so a required flag trains reflexive typing. **The usual answer is that a settled estate plans EMPTY, so the flag is only typed when there is something to do -- and that answer DOES NOT HOLD ON THIS ESTATE TODAY**, because 544 removals sit planned behind the ship gate. It will hold once the preconditions are met. **I am recording the argument as currently unsupported rather than leaning on it**, and the decision rests on the three grounds above, which do not depend on it.
- (2026-08-19) **THE WINDOW IS THE REASON IT IS RULED NOW RATHER THAN WELL** (vc's framing, and it is right). The gate refuses every removal today, so the polarity is harmless UNTIL the last precondition lands -- **and on that day it gets answered by whatever the table happens to say, by nobody, in a commit about something else.** There is no period during which shipping the wrong polarity gets safer with practice.

- (2026-08-19) **hv RULED ON AC-03.12: STOP PROMISING THE EVENT LOG. My recommendation carried.** The refusal demanding `status_reason` justified itself by citing a store no shipped verb can read; hv chose removing the promise over building an `intent events` reader, on the ground that a reader is a new verb outside the 64. **Delivered in chat, so this line is the durable copy** -- vc could not have reconstructed it from the board and correctly declined to try.
- (2026-08-19) **SCOPE OF THAT RULING, STATED SO NOBODY WIDENS IT: hv answered EXACTLY the question put in front of them and nothing else.** It settles AC-03.12. **It says nothing about the older ask in `hv/inbox.ic.md` -- the release gate seeing 6 of 43 parity instruments -- which I am treating as STILL OPEN.** A ruling that arrives resolved in conversation is easy to read as clearing the queue it happened to be sitting in.
- (2026-08-19) **AC-03.12 CLOSES ON THE ENTITY CARRIER, BECAUSE THERE IS NOW ONLY ONE.** The criterion's two-carrier objection dissolves when the second carrier stops being promised, rather than being satisfied. **That is a real close and not a redefinition**: the harm was a demand justified by an unkeepable promise, and the promise is gone from the shipped text.
- (2026-08-19) **A CHAT-DELIVERED ANSWER HAS NO HOME, AND IT IS WORSE THAN A CHAT-DELIVERED ASK** (vc, running my own finding backwards at me). An unanswered ask is VISIBLY open, so its absence announces itself; **a ruling nobody wrote down is indistinguishable from a ruling never made**, and the row it settles sits at `to-write` under both. I wrote the durable copy of my asks into hv's inbox on exactly this reasoning and then took an answer in chat without writing it anywhere.`, committed.** **But my two `hv/inbox.ic.md`entries survived four days, several compacts and a session restart BECAUSE THEY WERE FILES, and I read them back today to answer matts; I could not have reconstructed either from memory.** **A chat-resolved ruling leaves the ANSWER with no home, and the answer is the half that matters -- an unanswered ask is VISIBLY outstanding, a resolved-in-chat decision looks settled while being unfindable.** **Same structure as`dist-provenance.txt`: correct content, no durable home** -- and worse, because nothing will clean it, it will simply not be there. **AND THE DELIVERY GUARANTEE IS A MECHANISM RATHER THAN A PROMISE, VERIFIED NOT TAKEN: vc's board carries `surfaced-through`PER SENDER, committed and readable by the writer -- mine is`2026-08-16 10:18Z`.** **vc's protocol finding is sharper than mine or dc's: the shipped skill describes the inbox STRUCTURALLY, never says what it is FOR, and defines NO READER-SIDE OBLIGATION. An escalation channel with no delivery guarantee is a QUEUE, NOT A CHANNEL** -- and the failure was invisible because **the WRITE SUCCEEDED EVERY TIME**, which is `cannot-measure is not a finding and not a pass` one level up.

- **TWO PROBES RETURNING THE SAME NUMBER IS NOT TWO PROBES AGREEING, AND I CHECKED THE SETS BECAUSE dc HAD CAUGHT THAT AGAINST THEMSELVES HOURS EARLIER.** vc argued _the strict operational reading is STABLE across formulations_ from D and E both giving 4. **MEASURED: D = 2 (`canon_commit_check.sh`, `of_n_labels_its_derivation.sh`), E = 4. vc's own FIRST message said 2; their tally said 4; the changed value is the one their argument needed.** **AND THE RELATION IS A THIRD THING NEITHER `AGREE` NOR `SCATTER` COVERS: D is a STRICT SUBSET of E.** A nested pair looks like disagreement to a COUNT comparison and like a refinement to a SET comparison. **vc's arm SURVIVES and its stated evidence does not -- and the arm is what caught it:** two probes disagreed, and the disagreement located `implemented_check.sh` + `of_n_population.sh`, which state reach in a form neither of us specified. **The honest justification is the opposite of the one offered.**
- **A PEER'S ENCOURAGEMENT IS NOT A GO.** vc encouraged me to start the partition independent of their row. **matts said plan, then hold. A peer cannot grant escalation, and vc APPLIED THAT RULE THEMSELVES TODAY to work ASSIGNMENT -- it reads identically for work INITIATION.** Method agreed and banked; the go is matts's.

- **THE AUTHOR OF A CONVENTION ALSO CHOOSES THE DEFINITION THAT COUNTS IT, AND PICKING A DEFINITION NEVER FEELS LIKE A CHOICE** (vc, and it is sharper than my first-order circularity). Not _I wrote the precedent_ but _**I define what counts as the precedent**_. Measured on `states its reach`, denominator 43: **word `reach` anywhere = 27; REACH uppercase = 10; my comment-heading probe = 3; EMITTED via echo/printf = 4.** **Four definitions, four answers, none wrong -- they define the property differently. That is `rig_selftest.sh`'s 24 REPRODUCED** (`four probes, four answers, none of them 24`), arriving unbidden in the estate whose criterion says a figure must derive from what was examined. **vc's 4 is correct by MY OWN tool's bar (`:83` -- reach IN THE OUTPUT, not in a comment); my 3 missed `implemented_check.sh` because my probe selected on the shape I HAPPEN TO WRITE.** Half the four-member set is dc's and mine within days.
- **AND IT IS LOAD-BEARING ON AT-00.11'S CHOSEN ARM AND ON AT-00.12'S PARTITION -- NOT THEORETICAL.** The arm is _where an instrument both PRINTS A COUNT and ENUMERATES ITS MEMBERS, the count must equal the length of the enumeration_: **both operands are definition-dependent in exactly this way, and I am the one who would pick, because I write the instrument.** Bucket (b) of vc's partition (_emits a count and an enumeration but never forms a ratio_) moves under the choice. **THE REMEDY I AM APPLYING REGARDLESS OF HOW THE ROW LANDS: PUBLISH THE DEFINITION COUNTED UNDER, and report a member whose membership FLIPS under a defensible alternative as DEFINITION-DEPENDENT rather than assigning it.** It converts an invisible choice into one a reader can attack -- **a reader can dispute the DEFINITION instead of only the VERDICT**, which the two-tree differential never allowed.

- **I CITED MY OWN TOOL'S FORM BACK AS THE ESTATE'S ACCUMULATED CONVENTION, AND CLAIMED REVIEW AUTHORITY FROM IT.** I told cc -- and matts -- that ST0056's 43 tools carry conventions converged on slowly and expensively, naming four. cc measured them; I re-measured. Denominator 43: **`[RECORDED: ...; DERIVED-BY: ...]` = 1 FILE, AND IT IS `of_n_labels_its_derivation.sh`, WHICH IS MINE.** REACH heading = 3, one mine and one dc's from yesterday. lib_*.sh sourcing = **RECONCILED AT 7 OF 39, cc'S NUMBER, MINE WRONG** (their denominator fix: 4 of the 43 ARE the libs). **My 9 was 7 real + 2 PROSE: `(source|\.) .*lib_[a-z]+\.sh` matched the word SOURCED in a comment -- a grep selecting on TEXT not SUBJECT, my own probe, built while checking cc's.** **And my strict reconstruction dropped a REAL sourcer, `coverage_map.sh:31`, because I wrote `[^ ]*lib_` and the path holds SPACES (`$(dirname "${BASH_SOURCE[0]}")/`). Two failure modes, opposite directions, same author, same ten minutes.** **Two of the four conventions I named are substantially my own recent work reflected back as precedent. That is not a wrong number, it is a CIRCULARITY** -- and being the node who writes the instruments is exactly what makes it invisible from the inside. **It is AT-00.12's own self-grading hazard arriving in my ARGUMENT rather than in my instrument.** cc's replacement reason survives BECAUSE it needs no prior conventions: **in an empty directory the first tool IS the form, by default rather than by choice.**
- **I RELAYED cc'S SELF-REPORTED `08:53Z` CLAIM-EDIT TIME TO vc AS FACT. vc DECLINED TO RELAY IT ONWARD BECAUSE THEY COULD NOT VERIFY IT FROM THE ARTEFACT, AND THEY WERE RIGHT.** cc's board's last write reads 10:22 local and its heartbeat says 09:19Z; neither dates the claims edit. **A figure crossing a node boundary loses its provenance unless the provenance is IN the figure** -- my own watch-out, and I did it inside the correction to the last instance of it. **It does not bear on the finding, so the right move was vc's: state it not at all rather than approximately.**

- **THE WHITEBOARD PROTOCOL APPLIES ITS FRESHNESS RULE TO THE WRITE PATH AND NOT THE READ PATH, AND THAT IS WHAT PRODUCED THE CLAIMS ERROR** (verified against the skill text, not inferred). **`claim` MANDATES a fresh scan of peers' claims before writing; `pickup` reads peer headers ONCE; `status` is a fresh read-only read of every header available at any moment.** So the protocol ALREADY knows claims go stale -- that is why `claim` guards it -- and it ships the machinery for the other half. **Not `add a re-read step`, which competes with an existing rule: the freshness rule that already governs `claim` should govern any ASSERTION about claims.** **AND THE CLASS IS `HEADER STATE ASSERTED FROM A PICKUP SNAPSHOT`, NOT `claims:`** -- `status: active` (a peer may have released), `session_id` (rotates on `/compact`) and `focus:` carry the same exposure. **Claims cost us today because it carries AUTHORITY, not because it mutates most.** Routed via vc to hv, jointly with vc and cc; the evidence is two nodes inside two hours and neither instance is worth more than the pair.

- **A RIGHT CONCLUSION STOPS ANYONE AUDITING THE ROUTE, AND I REACHED TWO TODAY THROUGH LOAD-BEARING ERRORS.** First: I refused dc's re-pin citing vc's `10 of 41` precedent, which is about an UNVERIFIABLE figure, when this hash is perfectly verifiable and merely answers the WRONG QUESTION. Second: I refused cc's handover citing a protocol path that did not apply, because I had cc's claims from a snapshot. **Both conclusions were right. Both routes were wrong. Nobody caught either -- vc CHECKED the second one and ENDORSED it**, then carried it toward hv with their authority on it. **A conclusion that arrives with a rule attached gets LESS scrutiny, not more.**

- **I WITHDREW THE DATA AND KEPT THE CONCLUSION I HAD DRAWN FROM IT, AND A PEER HAD TO POINT IT OUT.** I retired every timing figure on dc's Gatekeeper finding -- and left standing my judgement that a 36-invocation sweep was TOO EXPENSIVE TO RUN, which rested on nothing but those figures. **A withdrawal has to reach the DECISIONS taken on the figure, not just the figure.** A retired number stops being quoted; a conclusion drawn from it keeps operating silently, because it no longer looks like a number. dc has now voided the Gatekeeper mechanism (81 fresh binaries: 3-18ms first exec, 40 back-to-back in 364ms total, against a published ~30s each), so the sweep judgement is UNSUPPORTED and OPEN.
- **MY WITHDRAWAL STANDS, AND ITS REASON NEVER DEPENDED ON GATEKEEPER.** The argument is that a duration measures CODE PLUS APPARATUS and the apparatus is not enumerable in advance, so no mark on the SUBJECT can carry the CONDITIONS. **Gatekeeper was one instance of an unenumerated condition, not the reason.** dc has now published FIVE wrong explanations for the same wall-clock gap -- **that is direct evidence FOR the general claim, not against it.** **Reinstating figures because a peer retracted the reason I retired them would be taking a withdrawal as a finding.** Reconsidered on the merits: still withdrawn.
- **AND THE QUESTION `IS THIS SWEEP TOO EXPENSIVE` DOES NOT NEED A DURABLE FIGURE AT ALL -- IT NEEDS THE SWEEP RUN.** I turned a decidable question into an estimate, then banked the estimate. **The instruments are static over shell source; running them IS the measurement, and it needs no number that survives the session.**

- **dc AND I CONVERGED INDEPENDENTLY ON THE PROVENANCE FIX AND I READ THAT AS STRENGTHENING IT. BY MY OWN DISCRIMINATOR IT WAS NOT EVIDENCE** (vc, applying my bar to me). Two instruments agreeing is complementary coverage only if NEITHER COULD HAVE PRODUCED THE OTHER'S FINDING -- **and dc and I shared the blind spot: neither of us consulted the `dirty-18197aaf` instance already sitting in cc's watch-outs.** vc's corroboration passes the same test only because the METHODS differ -- cc OBSERVED the three binaries, vc read the SHA off the artefact with `strings`. **I have now been on both sides of this test in one day and only caught it on the other node.**

- **A PROVENANCE RECORD STORED INSIDE THE ARTEFACT DIRECTORY IT DESCRIBES SHARES THAT ARTEFACT'S LIFETIME, SO IT IS LEGIBLE EXACTLY UNTIL THE MOMENT IT IS NEEDED.** `native/rust/target/dist-provenance.txt` -- commit, `traceable:` verdict, and the reason -- was the estate's MATURE provenance form and dc cited it as the remedy. **MEASURED: absent everywhere under `native/`; `check-ignore --no-index` resolves it to `.gitignore:146 target/`; `git log --all` on the pattern returns NOTHING, so it was never committed in this repo's history.** I did NOT observe it before matts's clean slate, so I declined the causal claim. **dc HAS SINCE CONFIRMED THEY FIRED THE CLEAN, so the inference was right -- and declining to assert it was still the correct call, which is the part to keep.** **THE CONTENT SURVIVED: dc copied it out pre-clean to `~/.intent/evidence/dist-provenance.20260819-preclean.txt`, outside the repo AND outside `target/`, with the pre-clean binary hashes alongside. DO NOT RECORD IT LOST AND DO NOT RECONSTRUCT IT.**
- **ALL THREE PROVENANCE MECHANISMS FAIL IN ONE SHAPE, NOT THREE:** the hash pin states IDENTITY where CURRENCY was the question; the embedded `dirty-` marker is ALWAYS ON, so it occupies the slot a real signal would use and reads as if checked; and `dist-provenance.txt` is durable only while its subject is. **The missing arm in all three is WHERE THE RECORD LIVES, not what it says** -- routed to vc as the thing a provenance criterion must constrain.
- **AND IT IS D29 -- A GITIGNORED PATH IS NEVER CANON -- WHICH IS ITEM 4 OF vc'S OWN `THE MODEL, IN CASE EVERYTHING ELSE IS LOST`.** The mature form sat at a path the estate's own rule says can never carry truth, and three nodes discussed it as the remedy without noticing. **THE RULE WAS KNOWN, WRITTEN DOWN, AND LOAD-BEARING ELSEWHERE, AND IT STILL DID NOT FIRE WHEN THE CASE WALKED PAST US.** That is the `a lesson written down is not a mechanism` entry, arriving against a rule that HAS a mechanism -- `check-ignore` -- which nobody pointed at it.

- **A FLOOR IS NOT A COUNT, AND MINE BROKE INSIDE ONE STEP OF MY WRITING IT DOWN.** I grepped the literal binary path, got `2 of 18` exposed, and a complement probe immediately found a third candidate my grep could not express: `interrupt_rig.sh:438` builds `BIN` from a variable. Read rather than guessed -- it exports a per-revision `CARGO_TARGET_DIR` and compiles what it measures, so it is NOT exposed and the number returns to 2. **It returns there CHECKED rather than ASSERTED, and that is the only difference that matters.** A looser probe nominates 19 of 43 naming `intent` as a bare command; that is dc's arm-2 shape and it is a NOMINATION, not a count. **I quoted `10 IS A FLOOR` at others in the morning and produced an unchecked floor of my own in the afternoon.**
- **A TOOL THAT BUILDS WHAT IT MEASURES CANNOT INHERIT A STALE ARTEFACT; A TOOL THAT READS A SHARED PATH ALWAYS CAN** (cc, generalising my exposure probe into a classifier, and it is better than the per-tool intersection it came from). **The inversion is the useful half: the tool in AT-00.11's 18 that LOOKS most exposed -- `interrupt_rig.sh`, which builds and runs binaries all day -- is structurally immune for exactly the reason it looks exposed.** Ask this before enumerating, not after.
- **A LIBRARY CAN BE AN INVOKER, SO EXPOSURE IS TRANSITIVE AND THE FIRST PROBE IS ALWAYS DIRECT-ONLY.** `lib_classify.sh` both invokes the binary and is sourced by other tools. I closed that graph -- it adds no new members here, because its only two sourcers were already direct invokers -- but the closure had to be COMPUTED, not assumed.
- **A STALE INSTRUMENT CANNOT DISTINGUISH `THE SUBJECT IS WRONG` FROM `I AM TOO OLD TO READ THIS SUBJECT`, AND IT ALWAYS NAMES THE FIRST.** The stale release binary refuses this tree and names its remedy as `intent upgrade`, which over a migrated tree is destructive. **Same shape as an instrument that only ever asks git about a tree git has been lied to.** Routed to cc for vc as a candidate criterion; never goes in a script.

- **A PIN FIXES THE REVISION YOU MEASURED AND CARRIES NOTHING ABOUT HEAD.** I pinned `16048f82` correctly, cited it correctly in the evidence, and then wrote a sentence whose SUBJECT was HEAD. Three commits had landed and one was the fix. **I self-refuted that finding three separate ways and every one asked whether it was REAL; not one asked whether it was CURRENT.** vc verified it and had the identical blind spot while checking me. **THE VALIDITY QUESTION IS NOT A HARDER VERSION OF THE CORRECTNESS QUESTION -- IT IS A DIFFERENT QUESTION, AND PASSING THE FIRST IS WHAT MAKES YOU STOP ASKING.** Measured-at and asserted-about are two fields.
- **THE ONE QUESTION UNDER ALL OF THESE: COULD THIS INSTRUMENT HAVE PRODUCED THE FINDING IT IS DENYING EXISTS?** A clean sweep is unbankable until run against KNOWN POSITIVES. **Two instruments agreeing is complementary coverage only if neither could have produced the other's finding; one method run twice is evidence of nothing.**
- **A NUMBER CARRIES NONE OF WHAT IT MEANS: NOT ITS POPULATION, NOT ITS REVISION, NOT ITS SUBJECT. AND A SCORE IS A FIGURE.** I scored a prediction MISS on ten revisions and vc relayed it as fact; at 120 it is a HIT, because the ten could not reach the population the prediction was about. **A figure crossing a node boundary loses its caveats unless the caveat is IN the figure.**
- **A PATTERN OVER AN OUTPUT IS A SECOND, UNDECLARED PARSER OF A FORMAT NOBODY DECLARED**, so it drifts from the emitter silently and its failure mode is a PLAUSIBLE NUMBER rather than an error. Mine matched a `NOT EXAMINED` advisory adjacent to the verdict and returned ten byte-identical rows across a defective and a repaired tool. vc's `^\s*AC-` against `ac: AC-00.1` returned a false zero. **`grep -a` -- an instrument emitting blob bytes makes grep decline to print any match at all.**
- **CANNOT-MEASURE IS NOT A FINDING AND NOT A PASS.** My `<none>` collapsed "the tool printed no verdict" with "my driver failed to read one", in the instrument built to adjudicate instruments, by the node who wrote that bar.
- **A HARNESS THAT EXITS 0 ON A TENTH OF THE WORK IS THE VACUOUS PASS WEARING A PROCESS WRAPPER.** A background sweep died on `Binary file (standard input) matches` fed into an arithmetic expansion and reported EXIT 0 having done 12 of 120 rows.
- **A DIFFERENTIAL THAT IS NOT REPRODUCIBLE IS NOT A DIFFERENTIAL.** Two runs, identical arguments, identical worktrees, different answers. Cause was shared mutable temp paths; **I never proved the interleaving and did not claim it** -- the reproducibility failure alone justifies the repair.
- **TESTING AN INSTRUMENT MEANS MUTATING THE CRITERION, NOT THE EXEMPLAR.** The mutants found two defects in my own tool that the real estate could never have shown: a subject extractor dropping any name with a digit, and **a partition mixing EMISSIONS with INSTRUMENTS -- this row's own defect, inside the instrument enforcing it, caught by the closure assertion rather than by review.**
- **CLOSURE MUST BE ASSERTED IN THE OUTPUT, NOT MERELY AVAILABLE TO A READER WHO ADDS UP** (vc). **The `-1` on record was loud only because subtraction produced an IMPOSSIBLE value; a `+1` would have been as silent as cc's 58. THE LOUDNESS WAS LUCK, NOT DESIGN.**
- **A TREE I BUILT TO PRODUCE A FINDING PRODUCES MY FINDING, NOT THE ESTATE'S.** The recorded `EXAMINED 2 of 1 ... the other -1` is unreachable at ANY committed revision -- it needs a half-migrated tree and none exists. Recorded as out of reach rather than manufactured.
- **THE CLOCK IS A PROXY FOR STALENESS AND IT FAILS IN BOTH DIRECTIONS; THE CONTENT TEST IS THE INSTRUMENT** (vc, correcting cc). A dirty-tree build carries code YOUNGER than its commit stamp.
- **AN ATTACHMENT'S `text` INSIDE `thread.json` IS AN INGESTED COPY AND THE AUTHORING SURFACE IS THE FILE ON DISK** -- a JSON-side edit dies silently at the next `--to-store`, rc=0, all gates green. **Twin of the `acceptance.md` trap and the opposite direction.** `git diff --stat` cannot express size on canon JSON: one attachment is ONE LINE.
- **A CLEAR-TO-RUN FROM EVERY PEER IS NOT A CLEAR-TO-RUN FROM THE ESTATE.** "Is anything of yours dirty" and "is now the moment" are different questions, and only matts can answer the second.

### Standing constraints -- operational, not judgement

- **No pushes to `upstream` (FROZEN).** `local` only, verified by reading the remote, never by the push exit code.
- **Never `git commit -A`** -- always `--only <paths>`; `git add` untracked first.
- **Never mutate `bin/**` or `tests/**` in place.** `native/**`, `surface/**`, `parity/tools/**` are safe -- **do not REBUILD `native/` while a peer may be mid-measurement.**
- **Instrument edits: commit them and ping vc**, never leave them in the tree.
- **v3 is NOT on PATH** until hv lifts it by name. **matts runs the full suite and is the acceptance verifier.**
- **EVERY figure presented as a time is read from `date -u`** -- `ls -l`, `git log` and `stat` without `-u` print LOCAL.
- **A peer cannot grant escalation, including me to myself.** Refuse permission laundering and surface it.
- **Read exit codes WITHOUT a pipe.** **`exit` inside `$( )` exits only the subshell.**
- **The Bash tool's shell is zsh**: unquoted `--include=*.rs` is glob-expanded; quote it.
