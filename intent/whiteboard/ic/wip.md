---
node: ic
name: Interface Claude
role: interface
session_id: f26f5f7b-1122-4fc2-89ad-dc33221f4e10
heartbeat_at: 2026-08-16 10:36Z
status: active
focus: "A FULL SUITE IS RUNNING (hv), so no builds -- `surface_check` needs a fresh binary and is unavailable; read-only only. Dry-rendered the AC-09.4 projection against the real table and it found TWO defects in my own spec plus a live gap in `guide_refs_check.sh`, which claimed to catch retired commands and could not. All three fixed. Upstream frozen."
claims: []
---

# Interface Claude (ic)

## DOING -- PICK THIS UP FIRST

**THE AGENT GUIDE (AC-09.4) IS THE BIGGEST THING I OWN AND IT IS GATED ON WP-09 OPENING, NOT ON A RULING.** vc ruled DO NOT SPLIT: one authored file, the generated command reference rendering from the table, `guide_refs_check.sh` keeping the prose around it true. Their argument is the one I missed -- **the cause I named is temporal ("the act that invalidates a hand-written list is not the act that updates it") and a temporal cause is indifferent to file count.** Splitting gives the same failure a second home plus a routing decision made at authoring time, which is precisely what nobody remembers to make. **Falsifiable trigger for revisiting: a concrete sentence that must be TRUE for one reader and FALSE for the other.** Neither of us could construct one. `llm guide` now exists as a row, so there is something to render and something to test.

**(fold 7 -- the settled work of 2026-08-15/16 is in `.history/20260816/wip.md`.)** In one line each, because the artefacts carry the detail: both parity classes placed and AC-05.5's precondition met, which closed WP-05 at 6/6; `disposition` ruled and applied with its vocabulary declared in canon; `ac gate` ruled `corrected`; AC-09.1's authored half done and its guard mutation-verified; `read_claim_probe.sh` built as the witness a refusal could never be; three generators repointed at their committed inputs after dc measured a claim of mine and found it false; and **`upgrade` un-retired** -- see the door/engine watch-out, which is the one to read first if you read only one.

## TODO

1. **THE GENERATED HALF OF THE AGENT GUIDE (AC-09.4), when WP-09 opens.** The projection is specified (`agent-guide.spec.md`): constraints BEFORE description -- `exposed_on_mcp`, then `read_or_mutate`, then path/help/args/flags, then the surface-wide facts (exit codes AND `--help`) ONCE rather than per row. **Population re-measured 2026-08-16 on the SHIPPED set, which is the one the guide renders: 112 declared, 107 shipped, all 107 carry help, 79 carry args, 39 carry a `keep` flag.** The 107 agrees with `surface_check`'s independent path count. _(The board previously said "111 rows carry help; 82 carry args" -- stale, and measured on the DECLARED set, so it was an instance of the very confusion below.)_ The eventual home is `llm_guide_gen.rs` (AT-09.4); a shell precursor is the pattern `guide_refs_check.sh` already set. **Do NOT write the authored prose yet** -- that deferral is still right, and it is mine: its subject is v3 workflows, and prose written now arrives at WP-09 already needing the treatment the spec exists to prevent.
2. **`usage_rules` is one of exactly TWO underscore spellings in 111 commands**, and the other (`st_zero`) is retired because hv ruled "the root spelling dies". The underscore mirrors nothing -- the file it displays is `usage-rules.md`, hyphenated, so the command and its own subject disagree. **`as-observed` or `corrected` is a parity classification, so vc's or hv's.** Raised in the spec, row left alone.

## Open with others -- LIVE ASKS ONLY

1. **WITH vc, ONE open.** **Whether one of `disposition` / `target.state` should be DERIVED from the other.** The DRIFT is refused either way (`bdebf8d6`) -- the generator rejects a row where they disagree on `retire` or `new-surface`, in both directions -- so **that deliberately does not prejudge the design call.** _(CLOSED during fold 7: `deviate` ruled into `target_states` with `is_parity_class: false`, and `upgrade` is `keep` + `deviate` citing D09. Both applied and verified.)_
2. **WITH cc, NOTHING open, and both items closed better than I asked.** _(a)_ **AC-06.8's mitigation is BUILT** at `8306d0b2`, in `render.rs`'s `doctor()` -- the file I named, for the reason I named. **It NAMES all six withheld flags rather than counting them, and they are NOT findings**: `doctor: 1 finding(s)` is unchanged, so a ratified withholding never inflates the verdict. **Verified by running it, not read.** _(b)_ **Both doc comments are gone, one level past my suggestion**: the Rust now DESERIALIZES all three vocabularies from the committed table and REFUSES on load, so there is no list left to restate -- and **that makes the `is_shipped()`-fails-open versus `Flag::ships()`-fails-closed asymmetry stop mattering**, because an unrecognised value never reaches either reader.
3. **WITH dc: nothing open.** Their `generator_inputs_check.sh` guard is live; I fixed the three generators it exposed and corrected three stale "the input is gone" claims. **They asked me to check rather than take it and they were right on every point.**
4. **The hv queue is 8 real units.** `critic`'s exit-2 is still the only one with a LIVE CONSUMER -- the pre-commit gate reads it today.

## Watch-outs

Durable only. Everything settled lives in the artefact that carries it.

- **A SUCCESSOR TO THE ENGINE IS NOT A SUCCESSOR TO THE DOOR, AND I RETIRED A COMMAND ON THAT CONFUSION.** `upgrade`'s row said `retire`/`retire`, ratified against D09 -- which is ONE LINE, "Migration floor v2.19.0, two-hop", saying nothing about the command. What retires is v2's LEDGER. `migration.md:3` says the verb survives, in as many words. **The row's own `behaviour` field held both halves** -- _"the v3 migrator is the successor surface: `intent ingest --from-md` is its engine"_ -- and I acted on the engine half. **`is_shipped()` reads both fields, so WP-10's migration would have shipped with NO INVOCATION.** Two other artefacts were already right (the binary's remedy, vc's AT-06.11 note) and **I nearly reported vc's note as contradicting the table.** When a ratification cites a design decision, READ THE DECISION: mine cited a line that did not say what I claimed.
- **READING A FACT AGAINST THE WRONG FIELD PRODUCES A TRUE-SOUNDING "NOTHING HERE". THREE INSTANCES, ALL MINE, ALL BETWEEN THE SAME TWO FIELDS.** `disposition` asks whether the v2 command SURVIVES; `target.state` asks what v3 DOES. **(1)** Three rows carried `disposition: corrected` -- an answer to the other question; they were `keep` all along, with `target.state` holding the correction. **(2)** I wrote that D09 _"says nothing about the command"_ and used that to leave `upgrade` unclassified. **It says nothing about the command's EXISTENCE and everything about its BEHAVIOUR** -- `migration.md:3` answers the first, D09 answers the second, and `upgrade` is `keep` + `deviate` with no new D-number needed. **I went looking in D09 for the answer to a question D09 does not take.** **(3)** The same shape produced the retirement itself. **When a lookup comes back empty, check you asked the field that answers it before concluding the fact is absent.**
- **A CHECK'S ERROR MESSAGE IS WHERE YOU LEARN WHAT IT DOES, AND NOTHING VERIFIES IT AGAINST WHAT IT DOES.** `guide_refs_check.sh` said _"a renamed or RETIRED command named in prose is a hand-maintained command reference"_ -- and it could not see a retired command, because its valid set is every DECLARED path and **the dispatch table is a parity register before it is a command list: a row means the question was ASKED, never that the answer was yes.** Three retired commands, including the `st_zero` hv explicitly killed, passed green as _"all declared"_. **The file asserted the capability in the same sentence it lacked it**, so anyone reading it to find out whether the case was covered got a yes. Found by MEASURING the claim -- a scratch paragraph and one run -- not by reading, and reading is what I had done twice before. **The sibling defect: `Flag::ships()` answers "must the renderer emit this" and the guide asks "does this flag exist at runtime"; they agree for 66 of 76 flags, which is exactly why substituting one for the other reads as obviously fine.** A predicate answers the question it was written for; reusing it means checking the new question is the same one.
- **A CONSISTENCY CHECK BETWEEN TWO ARTEFACTS IS BLIND TO A MISTAKE THEY BOTH MADE** (vc's line, on my `upgrade` find, and it is going into `parity.md`'s measurement rules). `surface_check.sh` compares the table to the binary; `corrected_check.sh` compares the table to `parity.md`. **Both passed while `upgrade` was wrongly retired, because the declaration and the binary AGREED -- clap correctly did not build what the table correctly said was retired.** What found it was walking the REMEDY STRINGS the binary actually emits across 106 paths, an artefact neither check reads. **A test asserting every declared verb exists would also have passed.** When two artefacts are derived from each other, only a THIRD source can see their shared error.
- **A PROSE TEST CANNOT TELL AN ASSERTION FROM ITS NEGATION -- AND THE ROW THAT EXPLAINS WHY IT IS OUT OF SCOPE IS THE ROW THAT SAYS THE IN-SCOPE WORDS.** `corrected_check.sh`'s SEVERITY arm grepped a ratification for `parity.md`. I wrote `ac gate`'s ratification to say _"NOT cited by parity.md -- this row is ratified ELSEWHERE"_ and it was reported `UNCITED` on the spot. **This is the same defect I fixed in this file the day before, one arm over**: I moved SCOPE off prose because that was the half that had failed, and left SEVERITY on prose because it had not failed YET. **The half you do not fix is the half that breaks, on the first row that exercises it.** Declared now (`target.ratified_in`), with a refusal on absence, and **there is no prose inference left in the file.** When you fix a class in one arm, fix it in every arm that shares the mechanism -- "it has not failed there" is not a reason, it is a schedule.
- **A RULE IS NOT PORTABLE JUST BECAUSE IT IS TRUE WHERE YOU WROTE IT. CHECK ITS PREMISE BEFORE THE SECOND APPLICATION.** My `target_states` refusal fires on a declared-but-unused value, and that is correct **because that vocabulary has ONE HOME**. Applied to `disposition` it would have fired on `deviate` -- **a correct value, correctly declared, with 47 members in `pertest.md` and 3 in `register.md`** -- because `disposition` shares its vocabulary with the register, and the `about` block says so in the sentence I quoted while getting it wrong. **Same rule, same author, same evening, second application wrong.** The premise was single-homedness and I had never named it, so nothing stopped it travelling. **Write the premise down at the FIRST application; a rule whose precondition is unstated will be lifted by whoever likes the rule.**
- **A SCAR GENERALISES TO THE FILE WHERE YOU EARNED IT AND NOWHERE ELSE, UNLESS YOU GO AND LOOK.** `class_vocab_check.sh` refuses on an empty citation set because I had been bitten there. The three vocabulary checks in `gen_dispatch_table.sh` did not -- **deleting `flag_dispositions` reported 95 flags undeclared; deleting `target_states` reported every invariant and entry.** A wall of offending rows reads as catastrophic data corruption and sends its reader to fix the DATA when the fault is one missing key. **Same rule, mine, written down on this very board, absent from the three places built after it.** Fixed by asking of each new check "what does this print when its input is missing", not by remembering.
- **A CHECK THAT CANNOT FAIL IS NOT A WEAK CHECK, IT IS A DECORATION -- and it hands you a reassuring result first.** The sharpest was the check written to CLOSE this class: the flag-completeness loop went green while 5 of 93 flags rendered nothing, because it greps for the LABEL and 88 flags supplied it. **Presence-of-label and completeness-of-population are different questions.** The subtlest: a TSV field shift made the surface check report **MORE coverage and FEWER findings at once**, which reads as a better run, caught only because the earlier output was still on screen. **And one arrived INSIDE the fix for another**: my staleness refusal blocked its own mutation test, which would have made every later green unfalsifiable. **Count the population, assert both sides are non-empty, keep the previous result to compare against -- and when a long-red check goes green, mutate it before believing it.**
- **A WRONG ANSWER THAT IS STILL IN-VOCABULARY READS AS A MEASUREMENT, NOT AN ERROR. FOUR MECHANISMS, ALL MINE, ALL IN ONE STRETCH** -- and vc's framing is the test: _"the tell is not the slip, it is that the wrong result is in-vocabulary."_ **(1) CHARACTER CLASS vs HYPHEN**: `[^-]*` on `status: to-write` yields `to`, which looks like a status, so it reported 53 findings instead of erroring; same defect as `corrected_check.sh`'s citation grep, which matched nothing because every id contains a hyphen. **(2) SUBSTRING vs WORD BOUNDARY**: a grep said `drift_check.sh` was cited -- because it matches INSIDE `decision_drift_check.sh`, the very row that was wrong, so the stale row hid a second finding by being a superstring of it. **(3) SEPARATOR vs SPACE**: a space-joined offender list split on spaces, and 60+ paths in this table contain one, so `st start` printed as two rows. **(4) PREFIX-STRIP WITH AN EMPTY PREFIX**: `${TSV#$SP/}` shortened a path for display, and when `SP` became optional the pattern was just `/`, so it stripped the LEADING SLASH and printed an absolute path as a relative one. **These vocabularies are hyphenated and space-bearing, so character classes and substrings are the wrong tool by construction: split on the separator the grammar actually uses, and match on word boundaries.**
- **DO NOT INFER A FIELD'S GRAMMAR FROM THE FIELD BESIDE IT. READ IT.** Measuring how many ACs carry multiple covering ATs, I extracted `covers:` **with a colon, because `status:` sits beside it in the same row and has one.** The grammar is `covers AC-05.5`, no colon -- one occurrence of `covers:` exists in the whole file and it is prose. **It reported zero multi-AT ACs, a completely plausible answer that would have retired the finding**, in the file whose grammar I had just spent an hour parsing. The only one of the set that came from an ANALOGY rather than a regex.
- **A NUMBER THAT HAS BEEN WRITTEN DOWN GETS TRUSTED BY THE NEXT PERSON, INCLUDING THE ONE CORRECTING IT.** I wrote "SEVEN citations" and **listed six**. cc corrected me to six **by subtracting from my seven rather than recounting, so the correction inherited the error.** It was five. **Neither of us was careless; both of us trusted a written-down number**, inside an exchange about exactly that failure. **Recount, never adjust.** _(This watch-out carried its own stale number for an hour, and at fold 7 the `retire` count in the paragraph above it was stale by one for the same reason.)_
- **THE POPULATION IS ALWAYS WIDER THAN THE MARKER, AND A `replace_all` IS WHERE THAT COSTS YOU.** I was about to rewrite `printf '%s' "$VAR"` across a generator on the strength of 8 matching sites; **the real count was 21**, the other 13 being jq pipes, sed extractions and label rendering. **Count the population before the substitution, not after the diff.** Same shape as `jq '.families[].entries[]'` missing a whole top-level array, and as the D42 audit matching `age` and `date` on substrings.
- **A VERIFICATION IS ONLY AS CURRENT AS THE THING IT READ, AND NOTHING TELLS YOU WHEN THAT EXPIRES.** Two instances, one each way. **Mine**: I checked `jq --indent 2` reproduced `dispatch-table.json` byte-identically, then hand-wrote compact JSON into it, then reused the earlier conclusion -- and jq expanded 45 lines I had not meant to touch. **vc's, against me**: I measured `AC-00.7` as both `to-write`, and vc had flipped `AT-00.5` an hour before reading my message, making it the contract's first mixed-state multi-AT AC -- **the precondition for the divergence I was measuring the absence of.** My conclusion survived and my basis did not.
- **"IT DOES NOT EXIST" IS A CLAIM ABOUT THE FILESYSTEM, SO GO AND LOOK -- AND THE FOSSIL OUTLIVES THE CORRECTION.** git answers questions about HISTORY; if the question contains "exists", the answer comes from the filesystem. Three original instances, the worst being that the false claim became the stated REASON for not fixing two defects. **Then dc found the FOSSIL**: `gen_inventory.sh` still said `probes/toplevel.tsv` "has NEVER been tracked" -- tracked since `d9f76c5f`, **my own commit recovering it.** Reading that found two more, in `view_skew_check.sh`'s un-re-derivable declarations. **A stale "it does not exist" stops being a gap and becomes a licence**, and correcting the belief does not correct the sentences built on it.
- **A GENERATOR THAT DOCUMENTS WHERE ITS INPUT LIVES AND READS SOMEWHERE ELSE IS RE-DERIVABLE ONLY BY WHOEVER READS THE COMMENT.** dc's statement of it beats mine: **the input being committed is not the same fact as the generator being able to reach it.** `gen_register.sh` spent eighteen lines saying its input is committed and byte-identical, then read `$SP/burn.tsv`, with the connecting `cp` in a comment. **I had claimed two generators needed no env override; three of four refused on an unset `SP`.** What I had actually verified was one input of two, and generalised.
- **A CITATION THAT NAMES A LINE ROTS SILENTLY, AND FAST.** My row cited `acceptance.md:298`; within the HOUR it was `:302`, because vc added rows above it. Nothing errors, the number is still a number, and it points at the wrong sentence. **Cite IDS, not locations.**
- **EVERY INPUT TO A DECISION CAN BE CORRECT AND THE DECISION STILL CONTRADICT A DISTANT ARTEFACT. GREP THE SPELLING BEFORE YOU WITHDRAW IT.** I ruled `ingest --from-md` dropped: cc's argument was sound, my own note on the row said the same, and it really is a mode flag with one mode. **Then I grepped and found five live citations across four artefacts I do not own** -- one of them `acceptance.md`, which does not merely mention the flag but RULES that its scaffolding ships. **Nothing about it felt uncertain, which is the point** -- care does not scale to a citation four files and two thousand lines away, and I was being careful. A withdrawal is a claim about every reader of the name, so it is a `grep -rn` question before it is a judgement.
- **A CHECK WHOSE SCOPE IS DERIVED FROM PROSE HAS THE DEFECT IT WAS BUILT TO CATCH, ONE LEVEL UP.** `corrected_check.sh` decided which units it governed by matching the literal string `parity.md` in each ratification -- and reported `INV-08`, `info` and `version` as ratified ELSEWHERE **while `parity.md` was citing all three**, because their ratifications say _"the `corrected` class"_ without naming the file. **Scope must come from the machine-read citation, never from how a sentence happens to be phrased.** Found on the check's first run, in the check written to find exactly this. _(The severity half was fixed later and separately -- see the assertion/negation watch-out.)_
- **A SKIP WHOSE REASON IS "HARD TO CHECK" IS A PROMISE NOBODY MADE, AND MINE WAS HIDING A FINDING.** `surface_check.sh` declared INV-03 unchecked because it "needs a probe run outside a project" -- true, and never a claim that nothing was wrong, **but it reads like one, and it is the sentence anyone re-reading the file relies on.** Going to build the probe meant reading the declaration, which is how the gate-message divergence surfaced. **Revisit skips; do not trust their reasons.** Same family: **a skip list is a promise that something else renders the key**, and mine exempted four keys nothing rendered.
- **A MISSING MEASUREMENT MUST PRESENT AS A REFUSAL TO MEASURE, NEVER AS A MEASUREMENT OF NOTHING.**
- **RE-DERIVABILITY IS NOT COMPLETENESS.** A lossy generator is a perfect fixed point with itself, so skew passes forever. It hid 15 of 20 authored fields.
- **`read -r a b c d` WITH `IFS=$'\t'` COLLAPSES AN EMPTY FIELD** -- bash and zsh alike, both verified. Every column after the empty one shifts. **Emit a `-` placeholder, never an empty field**, and refuse on a row whose shape is wrong: a row that yields nothing to check is indistinguishable from a clean one.
- **ENUMERATE THE POPULATION; DO NOT SNIFF FOR A MARKER -- and a structured query is a needle too.** `jq '.families[].entries[]'` missed a whole top-level array; the D42 audit's first pattern matched `manage`/`validate`/`update` on substrings of `age` and `date` and reported 25 rows. **Word boundaries cut it to five, of which three were real.**
- **A STALE BINARY ARGUES HARDEST EXACTLY WHEN IT IS MOST WRONG.** It does not fail loudly -- it produces a plausible, well-formatted, entirely wrong report, **and what it invents is precisely what somebody just fixed**, so it reads as a regression by the peer who closed it. Nearly sent one to cc. **MECHANISED rather than remembered**: `surface_check.sh` refuses on a binary older than its own inputs, because a binary that is not the tree is an INABILITY TO MEASURE. It has fired correctly four times since. `find -newer`, never `stat` -- BSD and GNU take different format flags.
- **`warning: build failed, waiting for other jobs to finish` IS CARGO'S LOCK MESSAGE, NOT A COMPILE ERROR** -- four sessions share one `target/`, so a peer building at the same moment aborts your build and it reads exactly like a broken HEAD. **I nearly told the peers HEAD did not compile.** The tell is what is ABSENT: no `error[E...]`, no `-->` line. **Re-run before believing it.**
- **A SWEEP DOES NOT MOVE A FILE, IT SPLITS A CHANGE.** My unqualified `--amend` took cc's test without the `store.rs` methods it calls and **HEAD did not build for ten minutes**. Each half reads as finished alone. **After a sweep the question is not "whose file is this", it is "does it still build."**
- **`--only` PROTECTS THE COMMIT AND NOT THE AMEND**, and never CLEARS the index, so the shared index accumulates across every node. **Name paths on the amend too.** Corollary that cost me twice at fold 6 and 7: **`sed -i` by LINE NUMBER shifts under its own edits** -- I archived and deleted the wrong line twice. Anchor on unique text.
- **D42, SETTLED: it is a rule about SIGNATURES and SOURCE DOCUMENTS. I over-applied it TWICE IN ONE DAY, both times towards MORE PROHIBITION, which looks like rigour.** Forbidden: confecting a time into a source document, and **no CLI or `intentsvcs` function TAKES a time as a parameter**. Permitted, all ruled by hv: **RETURNING** a time; **reading a clock to make a DECISION**; **stamping when a command ran into a GENERATED artefact**. hv: _"there is no need to be pathological about it."_ **All three of my D42 findings fell** -- an over-strict reading would have withdrawn `backup --list` and every `created`/`completed` a `show` prints, **exactly the surfaces D42 exists to make trustworthy rather than remove.** And the audit still paid, because checking my own wrong claim is what found the two real defects.
- **A QUOTE CHARACTER INSIDE A QUOTING CONTEXT, IN PROSE NOBODY PROOF-READS FOR SYNTAX.** An apostrophe in a single-quoted jq program CLOSES it -- `vc's`, then `run's`, the second inside a comment explaining a different bug. Backticks in a double-quoted string are command substitution. **Put the program in a file.** Same family: **a literal `\n` in a dispatch-table prose value** breaks formatter idempotence, because those fields render as ONE markdown bullet (AC-03.2).
- **A GUARD THAT MUST BE BYPASSED IS A GUARD NOBODY KEEPS**, and one that prints 26 lines to say one thing teaches its readers to skim. Refuse on what the CURRENT COMMIT adds, read the INDEX not the worktree, and report once. **A report is the right shape mid-ladder: `surface_check.sh` reports findings and gates nothing.**
- **I FABRICATED A TIMESTAMP AND THE GUARD CAUGHT IT.** Bumping a heartbeat inside a batched edit, I typed a plausible value, wrong by nine minutes, read off nothing. **The tell is the batching**: every stamp I read in its own step was right, and the one I folded into a multi-command edit was invented. **`date -u` is its own step, always.** vc hit the seventh instance of their own version of this rule the same day and drew the conclusion: **the wording was never the problem, the control is.**
- **NO PUSHES TO `upstream` -- hv, 2026-08-16 10:16Z. The CI/CD budget has been hit.** `local` (the Dropbox remote) is fine and is where everything goes now. **`git push local main` only**, never `git push upstream main`, and never a bare `git push` that might carry a configured upstream. This is a standing constraint, not a one-session pause, and it outranks the both-remotes habit every commit of mine has used up to `dcf9d3b6` -- **which is the last commit of mine that reached GitHub.** Announced to all peers at 10:17Z.
- **ic cannot certify a green suite.** matts owns the authoritative run; everything here is evidence.
- **Read `bin/**` and `tests/**`, never mutate them** -- two symlinks point at `bin/intent`, the BATS estate defaults to `INTENT_BIN=bin/intent`, and four sessions are live. `native/**` and `bin/.devbin/**` are safe.
- **This repo is PUBLIC and that is FINE and intended** (hv ruled). No secrets, and a bare commit in a shared tree publishes whatever is sitting in it.
- **This shell is zsh**: no word-splitting of unquoted parameters. Never enumerate remotes through `head`.
