---
node: ic
name: Interface Claude
role: interface
session_id: 6bbf2186-4635-4ce4-8bd0-02c75f289528
heartbeat_at: 2026-08-27 18:04Z
status: active
focus: "Three landed since the bounce: hv's hold/triage ruling (`26111785`), AC-11.1/11.4/11.5 given their rows (`052a090f`, ST0057 now 58/59), and vc's Highlander F1 built as a test (`2ddecb33`) -- every `st.*` op now has a DECLARED list answer instead of a silent wildcard. THE DAY'S SHAPE: three greens that were not about what they looked like -- a gate holding up a criterion, a stale binary under a clean tree, and a wildcard answering for a ruling. AC-11.6 untouched and hv's."
claims: [ST0057/02, ST0057/05, ST0057/07, ST0057/08, ST0057/11, ST0057/14, ST0061]
---

# Interface Claude (ic)

## DOING

**THREE NOW.** `2ddecb33` builds vc's Highlander finding F1: every `st.*` op has a **DECLARED** answer in `declared_list_edit` instead of falling through `_ => None`. **The asymmetry was the finding, not the duplication** -- an op missing from the edge table is REFUSED loudly at `check_transition`; an op missing from the match SILENTLY makes no list edit, so the transition succeeds and the declaration quietly does not follow. That is what cost hv three rulings in one day. `st.new` and `st.reinstate` are now named as the legitimate `None`s with the ruling beside each.

**Built as asked and no further: NO `Op` enum and NO status-keying**, both being changes of FORM that hv has not ruled on. Population derived twice -- at runtime from the edge table, and from facade source for `st.new`, which is not a transition at all and would be invisible to the first. Three mutations, each revert re-run to a green baseline rather than assumed.

**TWO LANDED SINCE THE BOUNCE.** `26111785` -- hv's third ruling, `st.hold` and `st.triage` both REMOVE, so declared-iff-WIP closes in both directions. `052a090f` -- AC-11.1, AC-11.4 and AC-11.5 given the AT rows they had been missing. **ST0057 is 58/59 satisfied, and AC-11.6 is the only one left.** Suite 183 binaries / 1334 passed / 0 failed.

**THE RULING'S KEEPER: I FORECAST ONE RED AND GOT THREE.** The third was `a_non_closing_verb_carries_no_note`, which used `st hold` as its EXAMPLE of a verb that removes nothing -- and the ruling moved `st hold` into the other class. It redded rather than drifting only because the assertion was written about the CLASS. It also dragged an unnamed consequence with it: `closing_notes` keys on the same `Remove`, so `st hold` now WARNS about unsynced attachments where it was silent.

**THE AC-11 KEEPER IS BIGGER, AND IT IS ABOUT WHAT A GREEN WAS ACTUALLY HOLDING UP.** Three criteria sat unsatisfied while a green test file already covered them -- and two of the three were covered in their FIRST CLAUSE ONLY, so citing the file as it stood would have recorded a coverage that did not exist. **Then the survival clause turned out not to mean what it said:** making `--default` call `organize(Mode::Apply)` -- the exact defect AC-11.4 exists to prevent -- left EVERY test in the file green, because the dehydration gate refuses the removal on a project declaring no preconditions. The test could not tell _never removes_ from _tried and was stopped_. **Hydration is not gated, so the detector is CREATION, not removal**, and the new arm reds that mutation and nothing else.

**AND THE OLD POSITIVE CONTROL WAS DECORATION.** _the UNDECLARED thread must have files on disk, or this test cannot fail_ was satisfied by `intent/.canon/st/ST0002.json` -- a canon file the verb would never remove under any implementation. `st new` writes canon and does not realise.

## TODO

**ST0057 IS 58/59 AND AC-11.6 IS THE ONE.** Everything else on WP-11 is satisfied and cited as of `052a090f`.

**OWED, STILL BLOCKED: AC-11.6's arm** -- hv's adjudication, and vc has twice confirmed they cannot hand it over at any phrasing. **Do not start.** `organize`'s confirm text still promises _it removes no files_, which `--force` makes false; vc has carried that up attached to the criterion.

**FILED FOR hv, NOT MINE TO CLOSE:** `st new --dehydrate` is now an inert flag -- two branches that became one, a test passing vacuously, and `flag_reachability` structurally blind because it asks whether a flag is READ and this one is. Same family as `ingest --from-md`, different route.

**REPORTED, OTHERS' TO CLOSE -- RE-MEASURED 17:34Z RATHER THAN RECALLED:** an AT row's `note` had no verb writer and `at new` DESTROYED one (vc filed 3.0.1; R6 fixed both halves). Estates still need `intent/.canon/project.json` committed -- COUNT IT, do not carry the thirteen forward. `clippy --workspace --all-targets` warns twice on collapsible-if, both in peers' files (`doctor.rs` at `bc6c1637`, `default_declaration_help.rs` at `d2f70e03`); reported, not fixed, because they are not mine. **The staged-roster and undeclared-consumer reds I was carrying are GONE** -- cc rostered the shim at `822051f7` within the hour of my flagging it, and dc's roster check landed. A red on the shared HEAD is worth a message the moment you measure it.

## Watch-outs

**THE CLASS BEHIND MOST OF THESE: THE SCOPE IS CHOSEN BY A FLAG AND REPORTED BY NOTHING.** cc's general form -- **the tree is an input to the run, and an input nobody names is an assumption.** vc's, from three of my own failures: **the environment is part of the subject under test.**

**INSTRUMENTS THAT REPORT A CONFIDENT NUMBER ABOUT THE WRONG THING.**

- `cargo check -p X` builds the **lib target only**; `--test Y` runs ONE binary; neither sees `src/**`'s `mod tests`. Use `--workspace --all-targets --no-fail-fast`.
- **Capture a run to a FILE and measure the file**; `| tail -N` truncates the thing you are about to measure.
- **A zero from your parser is indistinguishable from a zero from the world.** Wrong awk fields returned `passed=0` on a populated run; the rc contradicting the count caught it -- **a second independent reading, never the instrument itself.**
- **A background job's reported exit code is the WRAPPER's**, and a trailing `echo` launders failure into success: put the rc IN the log. **A trailing `&` inside `run_in_background` is the worst form** -- the harness reports _completed, exit code 0_ while the build still runs.
- **`command -v` DISAGREES BETWEEN BASH AND ZSH.** Absent, dangling symlink and a DIRECTORY of that name are EMPTY in both; **a plain non-executable file and a link to one are FOUND in bash, EMPTY in zsh.** The shipped gate is bash; every probe I run is zsh, so I reported a reproducible number about the wrong environment and cc built two branches that could never print. **I had the right answer first and traded it for a peer's reasoning: never take a correction unmeasured, least of all one overturning something you measured.** **The survivor no PATH-shaped test reaches is executable-and-truncated** -- name it, never write a check that appears to cover it.
- **`CARGO_TARGET_DIR` PLACEMENT GAVE ME TWO CONVINCING FALSE REDS IN ONE AFTERNOON.** Outside the worktree, every test locating the install by walking up from the binary fails. MOVE the dir and `CARGO_BIN_EXE_*`, baked at COMPILE time, still points at the old path -- eleven tests died in code I had not touched. **A moved build tree is not the same build tree**, and both errors named the subject.
- `cmd | head; echo "RC=$?"` reads head's code; `${PIPESTATUS[0]}` is bash, **this shell is zsh** (`$pipestatus[1]`). `grep -c` exits 1 on no match. An unmatched glob (`--include=*.rs`) ABORTS the command. **zsh does NOT word-split an unquoted `$var`** -- it cost me twice in one day, `git add $FILES` sending twelve paths as one argument. **`cat -A` is GNU-only**; `sed 's/ /./g'` is portable.

**CONTROLS AND PROOF.**

- **A control that cannot go red is not a control** (IN-AG-RED-CONTROL-001); **a positive control that would also pass under the broken instrument is decoration**; and **a control is CONSUMED by being used as a subject**, one-way, with nothing saying so -- re-measure a fixture when you CITE it. Measured: my `error_remedies` repair cancelled the fixture's own thread, which provoked correctly and silently stopped a later arm provoking `GateBlocked`.
- **ASSERT THE CAUSE: an outcome is reachable by more than one path**, and a message naming one sends the next reader at the wrong fix. It caught me in my own round-trip test, which blamed dehydrate for a listing that had never existed.
- **A CRITERION CAN BE SATISFIED PERFECTLY BY A DEGENERATE IMPLEMENTATION, AND THE TEST THAT PROVES IT WILL SAY SO.** `default_declaration` returning NOTHING passes _all three callers agree_; a `dehydrate` that removes nothing passes _proven by round trip_. **A property holding VACUOUSLY is indistinguishable from one holding substantively, and only an assertion on the MIDDLE STATE tells them apart.** A criterion is a floor, never a ceiling.
- **GREEN ON THE FIRST RUN IS NOT EVIDENCE.** Mutate one change at a time until every assertion has been SEEN to fail by name, and put the table where a reader of the test meets it. vc wants this on every proof test. **Best form: when a RULING is the change, applying it IS the mutation** -- no synthetic mutant, and the arms that red are exactly the ones the ruling moves.
- **NEVER MUTATE THE LIVE FILE; FOR RUST THAT MEANS COPY THE TREE, NOT THE FILE** (dc). Then there is no restore to get wrong and no trap to lie to you -- cc's trap used relative paths after a `cd`, printed `[restored]`, and left a live mutation. dc's property to check first is whether the copy can still be pointed at the REAL subject; a Rust source file is compiled at its path inside its crate, so **the structure it needs is the whole crate around it.** **If a live mutation is unavoidable the assertion is `git diff --quiet` or a byte compare, NEVER a grep for the mutation string** -- the string being gone is not the file being the file, and those part company in exactly the half-restored case you need the check for.
- **UNDER A MUTATION, THE CONTROL THAT IDENTIFIES WHICH TEST HOLDS THE FORM IN PLACE IS WHAT STAYS GREEN.** A test can name the very op in question, read as being about it, and pass under BOTH the right implementation and the wrong one -- so its green says nothing about the property you think it guards. Measured: `st_reinstate_does_not_add_it_back...` covers the ADD side of the op whose REMOVE side is the whole argument for op-keying, and a status-keyed rewrite would have landed green past it. **The distinguishing case usually needs a state the happy path never reaches** -- here a cancelled thread still declared, which only `st cancel --keep` produces.
- **A RESTORE THAT PUTS THE BYTES BACK CAN LEAVE THE BUILD MUTATED, AND EVERY CHECK WE HAVE SAYS CLEAN.** A metadata-preserving copy (`shutil.copy`/`move`, `cp -p`) restores the source with its ORIGINAL mtime, so cargo's staleness check never moves and the next run LINKS THE MUTATED ARTEFACT while `git diff` reports the tree clean. Measured: a mutation table read as three tests redding under every mutation INCLUDING under no mutation at all, and it took two binaries built from identical source disagreeing to find it. **This is the hole in dc's copy-the-tree rule for a COMPILED language: the restore is of the source AND the artefact.** The check that catches it is re-running the BASELINE to green after every revert -- and it is the only one that does, because a byte compare of the source passes.
- **A GATE STANDING IN FRONT OF THE PROPERTY CAN HOLD UP THE TEST INSTEAD OF THE CODE.** AC-11.4's _never removes a file_ passed under a `--default` that called `organize --apply`, because the dehydration gate refused the removal -- so the green was the gate's and the criterion could not tell _never removes_ from _tried and was stopped_. **Look for the UNGATED sibling of the property**: removal was gated and creation was not, so creation is where an applying verb betrays itself.
- **THE RECORD OF WHAT A TEST CANNOT DO IS NEVER RECOVERABLE FROM THE TEST** (dc). An AT row's `note` is where a control and a known limitation live; lose it and the row reads as sound.

**CLAIMS THAT OUTLIVE THEIR MOMENT -- ONE CLASS, SIX FACES, ALL MET TODAY.**

- **A STALE DOC IN THE ONE PLACE A READER ARRIVES IS WORSE THAN NONE**, because a corrected sentence reads exactly like one that was never wrong. Rewrite it AS superseded, quoting what it used to say -- vc's standing convention. **Measured three times in one day in three files by three people:** cc's `--default` help string, my `#[ignore]` comment, and a whole test file whose central argument a ruling had dissolved.
- **A VERIFICATION IS A CLAIM ABOUT A MOMENT, AND `X VERIFIED IT` OUTLIVES IT CARRYING NO EXPIRY** (cc, who caught themselves checking my write path in the shared tree while it was still mine to change). **Pin a check to a COMMIT, never a working tree.**
- **AN INSTRUCTION CAN BE BUILT ON A STATE ITS AUTHOR WAS TOLD HAD CHANGED, AND OBEYING IT PRECISELY IS HOW THE FALSE THING GETS WRITTEN DOWN.** vc asked for AT-11.3's note because it was _red and cannot say why_; it was green, and I had told them so in the message they replied to. **THE RECEIVER'S DUTY IS THE HALF I CAN ACT ON: check the premise before executing, and do the thing the ask was FOR rather than the thing it said.**
- **A FINDING THAT RECORDS ITS CONCLUSION AND NOT ITS REPRODUCTION CANNOT BE RE-CHECKED.** Mine and vc's _`wp start` realises a Completed thread_ reached hv as the basis for a ruling, and neither of us could later say whether it was fixed or never existed. **Recorded VOID, not implemented: an "implemented" ruling with nothing behind it is worse than an open one, because it closes.**
- **A COMMENT IS NOT A MECHANISM, AND IT HAS COST ME FOUR TIMES.** The worst: **the DDL I edited is not the DDL the test reads.** And a code comment can carry an attribution no record substantiates -- `declared_list_edit` cites an hv ruling hv's board does not hold. Name the rule and give the test.
- **AN ASSERTION WRITTEN ABOUT AN EXEMPLAR SURVIVES THE EXEMPLAR LEAVING THE CLASS, AND SURVIVES IT SILENTLY** (vc's general form of my finding, banked as a rule). `a_non_closing_verb_carries_no_note` used `st hold` as its example of a verb that removes nothing; hv's ruling moved `st hold` into the removing class. **It redded only because the assertion was written about the CLASS** -- had it been written about the verb it would have gone on passing, green, about a proposition nobody holds. Name the class in the assertion, and when a ruling moves a member, go looking for the tests that used it as the example.
- **TRANSCRIBE A RULING FROM ITS SOURCE WITH ITS PROVENANCE CAVEAT ATTACHED** -- a quote that sheds its provenance is how a relay becomes a ruling. Never take a peer's paste for the blob. **Reading the ruling at source has paid four times in one day**, twice finding the relay accurate and twice finding the premise expired.

**A PREDICATE WITH A SECOND HOME IN PROSE DRIFTS, SO THE FIX IS A TEST REQUIRING THE PROSE TO NAME THE RULE POSITIVELY** -- not a help string that says nothing, and not a restatement kept in step by care. vc's synthesis of my AT-11.3 note and cc's `--default` wording as two halves of one finding.

**`at new` REFUSES A ROW WHOSE CITED FILE DOES NOT CARRY THE ID** -- provable from both ends or from neither. Write the id into the test's doc first, then create the row.

**CLOCKS: NOTHING BUT `date -u` IS ONE** -- not `git log` (local), not a session notice, not an mtime, not a heartbeat already on the board. `git log --date=format:` renders in the COMMIT's own zone, so appending a literal `Z` fabricates a stamp in the call meant to recover true times; use `%cI`. **I typed a heartbeat from expectation while `date -u` in the same command printed a different value** -- caught before commit. A wrong stamp is recoverable only while something real still bounds it.

**A SERIALISER THAT REWRITES WHAT YOU DID NOT TOUCH IS AN UNDECLARED SECOND WRITER** -- `json.dumps` defaults to `ensure_ascii=True`, so a load-and-dump of `dispatch-table.json` escapes a character in prose nobody edited (cc). `ensure_ascii=False` restores byte-identity. **The question is never whether the lines you MEANT to change are right; it is whether the edit touched anything else, and only a byte comparison of an UNMODIFIED round-trip answers that.** Same family as the markdown formatter.

**SHARED-CHECKOUT MECHANICS. A green over a shared tree is about whoever's files are in it -- verify in a detached worktree at HEAD carrying your files and nothing else.** `git commit --only` is PATH-scoped, not hunk-scoped. `git checkout -- <file>` in a dirty tree is a REVERT: copy aside, copy back. `.git/index.lock` means a peer is mid-commit -- wait, never clear it. `git show HEAD` after a failed commit shows someone else's and reads like success. **`cargo fmt --all` WRITES**, invalidating every anchored edit a peer has in flight. **`git status` here is a snapshot of whoever is mid-write, not a state.** **A PEER'S HALF-FINISHED EDIT REDS _YOUR_ RUN IN A FILE YOU NEVER TOUCHED, THEN REPAIRS ITSELF BEFORE YOU CAN READ IT** -- ask the owner before diagnosing a red in their file. **Verify a repair out of the COMMITTED BLOB** -- the formatter writes between tree and commit. **And never amend to satisfy a convention once a peer has committed on top: the trailer is a convention, a shared HEAD is other people's work.**

**TWO GITIGNORE TRAPS, OPPOSITE DIRECTIONS.** A worktree does not receive gitignored files, so `.githooks/pre-commit.intent` is absent and the clock, header and append-only guards are inert in it. And **`intent init` lays down no `.gitignore`, so a fresh estate TRACKS its store.** **The protection is an ORDERING: the ignore must precede the first v3 write.**

**ONE VOCABULARY, TWO CONSUMERS, OPPOSITE FAILURE DIRECTIONS -- AND ONLY ONE OF THEM IS LOUD.** A missing member REFUSED by one consumer and swallowed by the other's wildcard is not a duplication problem, it is a silence problem, and the silent side is the one to instrument. Measured on the `st.*` ops: three hv rulings in one day were each one member handled in one table and not the other, with no test and no error anywhere in between. **The tell is a `_ =>` arm that can be reached by a member of a closed vocabulary** -- name every member and let the wildcard answer only for things outside the vocabulary.

**DESIGN, FIVE THAT KEEP RECURRING.** **An unverified schema bump does not go on main** -- a version move migrates every store on the machine. **A mechanism can be complete and unreachable**, and a unit test calling a function directly says nothing about whether anything calls it. **Definition by exclusion acquires members by accident** -- `!is_closed()` swept Triage, Not Started and Hold into the realised set. **A hold stated by its scope invites compliance that defeats its purpose.** **AN ENCODING INVARIANT IS PER-FIELD** -- `attachments` carries `skip_serializing_if`, so `[]` is unrepresentable and absent is the ONLY none; `covers` is the one Vec without it, and 13 rows in 837 canon files are `[]`. **`default` then makes the distinction invisible through the MODEL**, so a gate must read raw JSON. **And absent is also exactly what TOTAL LOSS looks like**, so a totals gate reads 0 for both and the discriminator must come from outside canon.

**THE BACKTICK DEFECT IS UNQUOTABLE IN THE MEDIUM THAT CARRIES THE REPORT** -- writing it down reproduced it three times. A backtick is not an apostrophe; a plain `'` is correct in board prose, and the never-escape rule is about the HEADER block ONLY. **The odd-count check has a FALSE-POSITIVE arm to match its false-negative one:** splitting on the backtick and testing an even field count flags every EMPTY line, because awk sets NF=0 on an empty record -- 26 phantom hits on a clean board. Guard with a non-zero length test. **Both arms of one instrument wrong in opposite directions, neither announcing it.**

## Decisions

- **(hv, 2026-08-26) The realised set is WIP ALONE. THERE IS NO 3.0.2.** Ruling transcribed into WP-11's cover at `fe1a0de8` from `hv/wip.md` 19:48Z, provenance caveat attached.
- **(hv, 2026-08-26) The DONE heading is `## DONE:<T>`, <T> a full ISO 8601 UTC instant** -- the time the prune ran, hence the cutoff. Verbatim v2. **And a flush CLEARS same-day work**: a completion date widens to that day's midnight, which is below any flush later that day.
- **(hv, 2026-08-26) WP-14: the cutoff is CANON STATE, not history.** A flush HAPPENING at T is an event; the cutoff BEING T is a fact about the project now. It goes in `.canon/project.json`, not `config.json` -- config holds choices a person makes, and a machine-written value there gives one file two writers.
- **(hv, 2026-08-27 16:30Z + 16:43Z + 17:10Z; first-hand in vc's session, CHOSEN from options vc authored -- the choice hv's, the framing vc's) THE REALISED SET IS WIP ONLY, ENFORCED BY THE OP TABLE.** `st.new` stops declaring; `st.start` and `st.resume` declare; `st.reinstate` stops; `st.hold` and `st.triage` remove. **Accepted costs, told to hv before choosing:** `st reinstate` stops adding when nobody asked, `st resume` re-adds a thread a human deliberately dehydrated, and a held thread now LEAVES the manifest. **ALL THREE BUILT**: `cce816a4`, `6ff37c0f`, `26111785`.
- **(hv, via vc 2026-08-27) In development, move to the latest build of any dependency and fix forward; favour consolidation into the simplest coherent structure.** This is what retired vc's defer-to-3.0.2 hold.
- **(ic, 2026-08-26) `event::todo_watermark` is DELETED rather than kept as a fallback.** Two homes for one value is the gate-figure defect in miniature, and both answers look plausible.
- **(ic, 2026-08-26) A canon file committed with a field MISSING is not neutral.** `carry_project_state` is absent-leaves-the-store-alone, present-and-empty-WINS -- so shipping `project.json` without its watermark reads as "never flushed" and clears the value the migration just recovered.
- **(ic, 2026-08-27) The `project.json` commits are CRITICAL PATH, not tidy-up.** Measured: a storeless machine with no canon file renders `## DONE` and the value is gone. vc rescheduled 14 estates on this.
- **(vc, 2026-08-27) WP-14 LANDS FIRST, THEN ONE PAIR IS PINNED FROM IT.** Pinning first pins a v13 binary that then meets a store another binary walked to 14 -- and the v13 pair REFUSES a walked estate outright. **A stamp is honest when the code trees match.**
- **(ic + vc, 2026-08-26; MEASURED VOID 2026-08-27, hv's board `dfd07cfe`) `wp start` DOES NOT realise a Completed thread.** The superseded entry claimed _ten view files plus a line in an append-only manifest, which nothing in the normal path retracts_. Not reproducible four ways with a positive-controlled instrument, and `wp.start` has never been in `declared_list_edit`, so no code path could add the line. **Kept as a correction rather than deleted, because it reached hv as the basis for a ruling and the record has to show why that ruling is void.** The finding recorded a conclusion without its reproduction, so neither of us can say whether it was fixed or never existed -- which is now a Watch-out.
- **(ic, 2026-08-26) The record-timestamp convention is DISCOVERED, not listed** -- every table needs a stamp column with a `DEFAULT (`, so a new table inherits the requirement automatically. `updated_at` is when this DB wrote the ROW, not a second home for "a flush happened at T".
- **(ic + vc, 2026-08-26) Enumeration for dehydration is the CORPUS SCAN** (`sync::scan`, excluding only `Ignored::for_root`), never a recursive `read_dir`. A directory the run cannot empty is NAMED in the verdict.
- **(ic, 2026-08-26) A restore of a four-month-old commit is a MERGE, not a revert.** The buckets-and-view test counted `- [ ] `, written before rows carried a status glyph, so it had been matching nothing and asserting 0 against a populated view.
