---
node: ic
name: Interface Claude
role: interface
session_id: f26f5f7b-1122-4fc2-89ad-dc33221f4e10
heartbeat_at: 2026-08-15 21:55Z
status: active
focus: "WP-05 CLOSED 6/6. `disposition` ruled and applied -- and vc's ruling against me caught a refusal of mine that would have fired wrongly on a shared vocabulary. `ac gate` vs issue 0032 still needs a classification."
claims: []
---

# Interface Claude (ic)

## DOING -- PICK THIS UP FIRST

**THE AGENT GUIDE (AC-09.4). vc ruled DO NOT SPLIT (21:17Z) and it is the biggest open thing I own.** One authored file; the generated command reference renders from the table; `guide_refs_check.sh` keeps the prose around it true. Their argument is the one I missed: **the cause I named is temporal -- "the act that invalidates a hand-written list is not the act that updates it" -- and a temporal cause is indifferent to file count.** Splitting gives the same failure a second home plus a routing decision made at authoring time, which is precisely what nobody remembers to make. Falsifiable trigger for revisiting: **a concrete sentence that must be TRUE for one reader and FALSE for the other.** Neither of us could construct one.

**BOTH PARITY CLASSES PLACED (`6c428e14`) AND AC-05.5's PRECONDITION IS MET.** `Undefined` is the sixth class in `parity.md`, attributed to vc and marked provisional; `new-surface` amends the out-of-scope bullet, ruled OUT OF PARITY SCOPE BY CONSTRUCTION -- which is what I argued for, against vc's original ask to add it as a deviation class. **`class_vocab_check.sh` is clean on the live artefacts: 6 named, 2 claimed, 2 grounded.** I told vc it is theirs to close and I will not assert it satisfied: **twice now the mechanism I thought closed AC-05.5 covered one axis of it, and both times the argument came from a sentence I wrote justifying something else.**

**I REFUSED TO TYPE `undefined` ON A PROPERTY THE ARTEFACT DOES NOT HAVE.** My objection was that a vc ruling entering an hv-ratified class list is a ratification question. **The list already does per-member provenance and says so in its own text** -- `Corrected` carries "(proposed by ic, hv-ratified...)". So the laundering I was guarding against is prevented by the ATTRIBUTION, not by my not typing it. **Check whether the artefact has the property before refusing on it.**

**AC-05.5 WAS NOT CLOSED BY `corrected_check.sh`, AND MY OWN SENTENCE IS WHY.** vc turned my own filename argument into the gap: I justified the name as _"scoped to the ratified `Corrected` class, not drift generally"_ -- **which is exactly why the AC, whose property is the GENERAL case, does not close on it.** The vocabulary axis is now built beside it (`class_vocab_check.sh`, `f5b6977a`) and reproduces vc's live instance: `undefined` claims to be the fifth parity class and `parity.md` does not name it. **Open question I will not answer by typing: a vc ruling entering an hv-ratified class list is a ratification question.**

**`disposition` RULED AND APPLIED (`799b7751`), AND THE RULING THAT WENT AGAINST ME IS THE ONE TO KEEP.** I read `deviate` at zero rows and called it a vocabulary outliving its members. **It has 47 rows in `pertest.md` and 3 in `register.md`** -- `disposition` shares one vocabulary with the keep/retire/deviate register, and the `about` sentence I quoted at vc says exactly that. **I read the sentence and did not read what it meant.** So the unused-value refusal I had built an hour earlier for `target.state` would have fired here on a correct value with 47 members one artefact away: **its premise is SINGLE-HOMEDNESS, not declaration.** Now written in the refusal's own comment AND in a `shared_vocabulary` canon entry, and mutation-tested not to fire. Data clean: keep 86, new-surface 19, retire 6, pending 1, **no absences**. `entry_dispositions` + `flag_dispositions` declared in canon; the flag vocabulary moved out of a hardcoded list inside the check. **Corrected vc's closing number -- the two fields share TWO values, not one**: 19 new-surface + 6 retire, all in lockstep, so 25 of 111 rows carry one fact in two fields. Recorded as `field_overlap`, deliberately not mechanised.

**AC-09.1's authored half is DONE and its guard is mutation-verified** -- both fields on all 111 rows, the generator refusing an absent field, a value outside the vocabulary, and a string `"true"` where a boolean was meant (jq reads that as truthy and would have rendered "exposed as an agent tool" in silence). **`read_claim_probe.sh` (`bd0bf042`) is the witness the refusal could never be**: a refusal proves a declaration EXISTS, never that it is TRUE.

**(archived: AC-05.5's `corrected` axis, `88b28fea`.)** `corrected_check.sh` is set equality between what `parity.md`'s ratified `Corrected` class CITES and what the table CLAIMS is `corrected`, firing in **both** directions -- and the reverse (a ratification nothing has applied) is the one that hid INV-06/INV-07 for a day, reproduced exactly by mutant A. **11 cited, 11 claimed-and-cited, agreeing exactly; 6 ratified elsewhere, named and counted rather than dropped.** Citations written in vc's adopted format: ids not locations, `-- covers: <ids>`, one member per line because the grammar is line-terminal.

**Both defects it found were in ITSELF, on the first run**, and both are on the watch-outs below: a citation grep that excluded hyphens and so matched nothing (**caught by the refusal on an empty citation set**, not by a silent zero), and **a scope derived from PROSE** -- which reported three units as out-of-scope while `parity.md` was citing them, because their ratifications say "the `corrected` class" without naming the file. **Citation decides scope now; the prose test only chooses severity.**

## TODO

1. **`ac gate` IS `as-observed` AND CARRIES ISSUE 0032 -- recorded at `70a52965`, classification is vc's or hv's.** The row's own note calls it _"the single highest-value parity row in the family"_ because AC-04.3 requires reproducing v2's gate verdicts. **0032 says what it would reproduce is not a behaviour anyone chose** -- `bin/intent_acceptance:454` short-circuits on the first green AT, so satisfaction is the OR of covering ATs, and hv's wording in the issue is _"chosen by an early-return rather than by a decision"_. **Two ratified things pointing opposite ways**: correcting it changes verdicts on exactly the contracts AC-04.3 measures parity against. **Latent, not live: 2 of 109 ACs carry 2+ covering ATs** (AC-03.7 both green, AC-00.7 both to-write) and OR agrees with AND while all covering ATs share a state. **AC-03.7 is the nearer one -- one regression and it scores satisfied on the survivor.**
2. **THE GENERATED HALF OF THE AGENT GUIDE (AC-09.4).** `llm guide` now exists as a row, so there is something to render and something to test. The projection is specified (`agent-guide.spec.md`): constraints BEFORE description -- `exposed_on_mcp`, then `read_or_mutate`, then path/help/args/flags, then the exit-code contract ONCE for the surface rather than per row. **All 111 rows carry help; 82 carry args.** The eventual home is `llm_guide_gen.rs` (AT-09.4, WP-09); a shell precursor is the pattern `guide_refs_check.sh` already set. **Do NOT write the authored prose yet** -- that deferral is still right, and it is mine: its subject is v3 workflows and prose written now arrives at WP-09 already needing the treatment the spec exists to prevent.
3. **`usage_rules` is one of exactly TWO underscore spellings in 111 commands**, and the other (`st_zero`) is retired because hv ruled "the root spelling dies". The underscore mirrors nothing -- the file it displays is `usage-rules.md`, hyphenated, so the command and its own subject disagree. **`as-observed` or `corrected` is a parity classification, so vc's or hv's.** Raised in the spec, row left alone.

## Open with others -- LIVE ASKS ONLY

1. **vc RULED ALL FIVE (20:56Z) AND ALL FIVE ARE APPLIED.** Citation format ADOPTED as proposed (ids, `-- covers:`) -- AC-05.5 built. **`--from-md` KEEP, ruled AGAINST my proposal to withdraw**, and the row records vc's reason rather than mine: my objection is real but a table edit does not resolve it, and it belongs at AC-10.2/10.3 where its acceptance lands. **INV-03 `corrected`**, vc-ruled, hv outstanding and not blocking. **AC-06.11 covers the empty verb-space** -- same row, not a new one, because the corpus is the same (emitted remedies); vc widens the wording, cc has the fix. **Empty evidence: refuse at the FACADE** (issue 0035's load-bearing arm -- the typed API is the only door under D01, so it covers the routes cc's one-liner cannot); **vc measured the exposure at ZERO**, all 22 satisfied non-test ACs in `acceptance.md` carry evidence, **scoped explicitly to the FILE and not the live store**; and there is **no AC-side lint** to hang it on, so it is a runtime refusal.
   - **BOTH BLOCKERS CLEARED at 21:17Z.** Split question RULED: do not split, one authored file, with a falsifiable trigger. AT-05.5 renamed to the real file (my recommendation taken -- the row follows the file, because `drift_check.sh` already sits in that directory and two confusable instruments is worse than a corrected row).
   - **OPEN WITH vc, and I will not answer either by typing.** (a) **`undefined` into `parity.md`** -- a vc ruling entering an hv-ratified class list is a ratification question. (b) **`new-surface`'s provenance** -- I declined to add it as a parity class and said why. Check stays red and honest until both land.
   - **OPEN WITH cc (21:31Z): `dispatch.rs:172`'s doc comment lists FIVE `target.state` values and the table uses SIX** -- `new-surface` missing at 18 rows. Closed on my side (`target_states` + two generator refusals); their file, their call. Suggested the comment stop restating a list that now has a home -- **AC-09.4's own argument arriving in a doc comment.**
   - **cc needs no re-pin from me**: my table pins the message SHAPE (`` `error: ...` on stderr (INV-01) ``), never the text, so a rewrite that keeps the voice is invisible BY DESIGN. Told them to flag only STREAM, EXIT CODE or PREFIX changes.
2. **WITH dc (19:22Z): took their offer 2.** Property handed over: **every committed generated artefact's inputs are IN THE REPOSITORY** -- tracked by git, not merely documented. Four instances today; the pre-commit `skew` check already prints a "declared un-re-derivable" category to hang it off. Flagged that the declarations are PROSE headers today, so the first real work is a machine-readable declared-inputs line -- more than dc offered, and dc's to scope or refuse. Also asked dc **not** to optimise `int build cli` away: that ~30s is buying correctness and should stay visible.
3. **The hv queue is 8 real units** (was reported as 14). `critic`'s exit-2 is still the only one with a LIVE CONSUMER -- the pre-commit gate reads it today.
4. **WITH cc (21:31Z), one open item.** `dispatch.rs:172`'s doc comment lists FIVE `target.state` values and the table uses SIX -- `new-surface` missing at 18 rows. **Closed on my side, open on theirs**, and suggested the comment stop restating a list that now has a home. **They need no re-pin from me for their `ac satisfy` / `ac withdraw` message rewrites**: my table pins the message SHAPE (`` `error: ...` on stderr (INV-01) ``) and never the text, so a rewrite keeping the voice is invisible BY DESIGN while a stream, exit-code or prefix change still fails. Told them to flag only those three. **Their empty-evidence root cause beat both fixes I offered** -- `--evidence ""` satisfies an `arg(..)?` and a clap `required` alike; `evidence: String` makes the FIELD mandatory, not the evidence present.

## Watch-outs

Durable only. Everything settled lives in the artefact that carries it.

- **D42, SETTLED: it is a rule about SIGNATURES and SOURCE DOCUMENTS. I over-applied it TWICE IN ONE DAY, both times towards MORE PROHIBITION, which looks like rigour.** Forbidden: confecting a time into a source document, and -- vc's sharpest form -- **no CLI or `intentsvcs` function TAKES a time as a parameter** (a property of the API surface, so a time-typed input is a defect by inspection). Permitted, all three ruled by hv: **RETURNING** a time; **reading a clock to make a DECISION**; **stamping when a command ran into a GENERATED artefact**. hv: _"there is no need to be pathological about it."_ **All three of my D42 findings fell.** An over-strict reading would have withdrawn `backup --list` and every `created`/`completed` a `show` prints -- **exactly the surfaces D42 exists to make trustworthy rather than remove.** The rule takes from the WRITE path and gives the READ path its guarantee.
- **AND THE AUDIT STILL PAID, BECAUSE CHECKING MY OWN WRONG CLAIM IS WHAT FOUND THE REAL DEFECTS.** A pattern match produced three wrong verdicts; reading the code under them produced two right findings -- `backup --list` displaying a file mtime AS a record fact, and `todo.md` being the sole store of its own watermark.
- **A CHECK THAT CANNOT FAIL IS NOT A WEAK CHECK, IT IS A DECORATION -- and it hands you a reassuring result first.** Seven instances now. The sharpest was the check written to CLOSE this class: the flag-completeness loop went green while 5 of 93 flags rendered nothing, because it greps for the LABEL and 88 flags supplied it. **Presence-of-label and completeness-of-population are different questions.** The subtlest: a TSV field shift made the surface check report **MORE coverage and FEWER findings at once** -- 59 probed against 46, 11 findings against 13 -- which reads as a better run, caught only because the earlier output was still on screen. **And the seventh arrived INSIDE the fix for the sixth**: my new staleness refusal blocked its own mutation test, which would have made every later green unfalsifiable. **Count the population, assert both sides are non-empty, keep the previous result to compare against -- and when a long-red check goes green, mutate it before believing it.**
- **A CONTROL THAT DIFFERS FROM THE MUTANT IN A SECOND WAY IS NOT A CONTROL** -- run the copy as a SIBLING of the original, and print the control result FIRST. **The exception, from cc, is worth holding beside it: when two halves fail TOGETHER in the field, the honest control mutates both.** Their case was a generator that stops injecting a version marker plus a reader that reports constants -- either alone is caught, the pair is invisible to comparing against committed files.
- **A NUMBER THAT HAS BEEN WRITTEN DOWN GETS TRUSTED BY THE NEXT PERSON, INCLUDING THE ONE CORRECTING IT.** I wrote "SEVEN citations" and **listed six** -- an off-by-one against my own list, in the same sentence. cc corrected me to six (rightly: one had genuinely gone) **by subtracting from my seven rather than recounting, so the correction inherited the error.** It is five. **Neither of us was careless; both of us trusted a written-down number**, inside an exchange about exactly that failure. **Recount, never adjust.** _(This watch-out itself carried the stale SEVEN for an hour, in the line below, until fold 5.)_
- **A TRUNCATED VALUE THAT IS STILL A PLAUSIBLE MEMBER OF THE VOCABULARY READS AS A MEASUREMENT, NOT AN ERROR. FIVE INSTANCES, ONE SESSION, THREE DIFFERENT MECHANISMS.** (1) `[^-]*` on `status: to-write` yields `to` -- and `to` looks like a status, so it reported 53 findings instead of erroring. Same character-class-versus-hyphen defect as `corrected_check.sh`'s citation grep hours earlier. (2) A substring grep said `drift_check.sh` was cited -- because it matches INSIDE `decision_drift_check.sh`, **the very row that was wrong**; the stale row hid a second finding by being a superstring of it. (3) A space-joined offender list split on spaces, so `st start` printed as two rows -- **paths in this table contain spaces, and 60+ of them do.** **The vocabularies here are hyphenated and space-bearing (`to-write`, `INV-08`, `pending-hv`, `st start`), so character classes and substrings are the wrong tool by construction: split on the separator the grammar actually uses, and match on word boundaries.**
- **A RULE IS NOT PORTABLE JUST BECAUSE IT IS TRUE WHERE YOU WROTE IT. CHECK ITS PREMISE BEFORE THE SECOND APPLICATION.** My `target_states` refusal fires on a declared-but-unused value, and that is correct **because that vocabulary has ONE HOME**. Applied to `disposition` it would have fired on `deviate` -- **a correct value, correctly declared, with 47 members in `pertest.md` and 3 in `register.md`**, because `disposition` shares its vocabulary with the register and the `about` block says so in the sentence I quoted while getting it wrong. **Same rule, same author, same evening, second application wrong.** The premise was single-homedness and I had never named it, so nothing stopped it travelling. **Write the premise down at the FIRST application; a rule whose precondition is unstated will be lifted by whoever likes the rule.**
- **A SCAR GENERALISES TO THE FILE WHERE YOU EARNED IT AND NOWHERE ELSE, UNLESS YOU GO AND LOOK.** `class_vocab_check.sh` refuses on an empty citation set because I had been bitten there. The three vocabulary checks in `gen_dispatch_table.sh` did not -- **deleting `flag_dispositions` reported 95 flags undeclared; deleting `target_states` reported every invariant and entry.** A wall of offending rows reads as catastrophic data corruption and sends its reader to fix the DATA when the fault is one missing key. **Same rule, mine, written down on this very board, absent from the three places built after it.** Fixed by asking of each new check "what does this print when its input is missing", not by remembering.
- **DO NOT INFER A FIELD'S GRAMMAR FROM THE FIELD BESIDE IT. READ IT.** Measuring how many ACs carry multiple covering ATs, I extracted `covers:` **with a colon, because `status:` sits beside it in the same row and has one.** The grammar is `covers AC-05.5`, no colon -- one occurrence of `covers:` exists in the whole file and it is prose. **It reported zero multi-AT ACs, a completely plausible answer that would have retired the finding**, in the file whose grammar I had just spent an hour parsing. Sixth slip of the session and the only one that came from an ANALOGY rather than a regex.
- **THE POPULATION IS ALWAYS WIDER THAN THE MARKER, AND A `replace_all` IS WHERE THAT COSTS YOU.** I was about to rewrite `printf '%s' "$VAR"` across a generator on the strength of 8 matching sites; **the real count was 21**, the other 13 being jq pipes, sed extractions and label rendering. **Count the population before the substitution, not after the diff.** Same shape as `jq '.families[].entries[]'` missing a whole top-level array, and as the D42 audit matching `age` and `date` on substrings.
- **A CITATION THAT NAMES A LINE ROTS SILENTLY, AND FAST.** My row cited `acceptance.md:298`; within the HOUR it was `:302`, because vc added rows above it. Nothing errors, the number is still a number, and it points at the wrong sentence. **Cite IDS, not locations.**
- **EVERY INPUT TO A DECISION CAN BE CORRECT AND THE DECISION STILL CONTRADICT A DISTANT ARTEFACT. GREP THE SPELLING BEFORE YOU WITHDRAW IT.** I ruled `ingest --from-md` dropped: cc's argument was sound, my own note on the row said the same, and it really is a mode flag with one mode. **Then I grepped and found five live citations across four artefacts I do not own** -- one of them `acceptance.md`, which does not merely mention the flag but RULES that its scaffolding ships. **The drop would have put my table in contradiction with the ratified contract: AC-05.5's exact class, manufactured by me, an hour after vc landed the criterion to catch it.** Nothing about it felt uncertain, which is the point -- **care does not scale to a citation four files and two thousand lines away, and I was being careful.** A withdrawal is a claim about every reader of the name, so it is a `grep -rn` question before it is a judgement.
- **A CHECK WHOSE SCOPE IS DERIVED FROM PROSE HAS THE DEFECT IT WAS BUILT TO CATCH, ONE LEVEL UP.** `corrected_check.sh` decided which units it governed by matching the literal string `parity.md` in each ratification -- and reported `INV-08`, `info` and `version` as ratified ELSEWHERE **while `parity.md` was citing all three**, because their ratifications say _"the `corrected` class"_ without naming the file. **Scope must come from the machine-read citation, never from how a sentence happens to be phrased.** The prose test now only chooses the SEVERITY of an uncited claim, never whether it is looked at. Found on the check's first run, in the check written to find exactly this.
- **A SKIP WHOSE REASON IS "HARD TO CHECK" IS A PROMISE NOBODY MADE, AND MINE WAS HIDING A FINDING.** `surface_check.sh` declares INV-03 unchecked because it "needs a probe run outside a project" -- true, and never a claim that nothing was wrong, **but it reads like one, and it is the sentence anyone re-reading the file relies on.** Going to build the probe meant reading the declaration, which is how the gate-message divergence surfaced. **Revisit skips; do not trust their reasons.**
- **A SKIP LIST IS A PROMISE THAT SOMETHING ELSE RENDERS THE KEY**, and it is only as good as that promise. Mine exempted four keys nothing rendered. **Reading the list produced the bad list; the mutation test found it.**
- **`read -r a b c d` WITH `IFS=$'\t'` COLLAPSES AN EMPTY FIELD** -- bash and zsh alike, both verified. Every column after the empty one shifts. **Emit a `-` placeholder, never an empty field**, and refuse on a row whose shape is wrong: a row that yields nothing to check is indistinguishable from a clean one.
- **ENUMERATE THE POPULATION; DO NOT SNIFF FOR A MARKER -- and a structured query is a needle too.** `jq '.families[].entries[]'` missed a whole top-level array; the D42 audit's first pattern matched `manage`/`validate`/`update` on substrings of `age` and `date` and reported 25 rows. **Word boundaries cut it to five, of which three were real.**
- **"IT DOES NOT EXIST" IS A CLAIM ABOUT THE FILESYSTEM, SO GO AND LOOK. THREE TIMES, and the third cost the most.** git answers questions about HISTORY; if the question contains "exists", the answer comes from the filesystem. (1) The probe input, concluded gone from an empty `git log --all`; it was on disk, and a rule in `parity.md` plus a refusal in `gen_inventory.sh` were built on top. (2) `gen_register.sh`'s burn input, recorded as gone -- **committed at `tools/burn-baseline.tsv`, byte-identical, only the FILENAME differed.** (3) Its worktree and its 196-file TAP corpus, both still on disk. **The third is the worst because the false claim became the stated REASON for not fixing two defects, so it stopped being a gap and became a justification.** One `find` beat all three.
- **A STALE BINARY ARGUES HARDEST EXACTLY WHEN IT IS MOST WRONG.** It does not fail loudly -- it produces a plausible, well-formatted, entirely wrong report, **and what it invents is precisely what somebody just fixed**, so it reads as a regression by the peer who closed it. Nearly sent one to cc. **MECHANISED rather than remembered**: `surface_check.sh` refuses on a binary older than its own inputs, filed with the absent-binary refusals, because a binary that is not the tree is an INABILITY TO MEASURE. `find -newer`, never `stat` -- BSD and GNU take different format flags, and a portability check must not fail differently on the platform it is not being run on.
- **`warning: build failed, waiting for other jobs to finish` IS CARGO'S LOCK MESSAGE, NOT A COMPILE ERROR** -- four sessions share one `target/`, so a peer building at the same moment aborts your build and it reads exactly like a broken HEAD. **I nearly told the peers HEAD did not compile.** The tell is what is ABSENT: no `error[E...]`, no `-->` line. **Re-run before believing it.**
- **A SWEEP DOES NOT MOVE A FILE, IT SPLITS A CHANGE.** My unqualified `--amend` took cc's test without the `store.rs` methods it calls and **HEAD did not build for ten minutes**. Each half reads as finished alone. **After a sweep the question is not "whose file is this", it is "does it still build."**
- **`--only` PROTECTS THE COMMIT AND NOT THE AMEND**, and never CLEARS the index, so the shared index accumulates across every node. **Name paths on the amend too.**
- **RE-DERIVABILITY IS NOT COMPLETENESS.** A lossy generator is a perfect fixed point with itself, so skew passes forever. It hid 15 of 20 authored fields.
- **A MISSING MEASUREMENT MUST PRESENT AS A REFUSAL TO MEASURE, NEVER AS A MEASUREMENT OF NOTHING.**
- **A QUOTE CHARACTER INSIDE A QUOTING CONTEXT, IN PROSE NOBODY PROOF-READS FOR SYNTAX.** An apostrophe in a single-quoted jq program CLOSES it -- `vc's`, then `run's`, the second inside a comment explaining a different bug. Backticks in a double-quoted string are command substitution. **Put the program in a file.** Same family: **a literal `\n` in a dispatch-table prose value** breaks formatter idempotence, because those fields render as ONE markdown bullet (AC-03.2).
- **A GUARD THAT MUST BE BYPASSED IS A GUARD NOBODY KEEPS**, and one that prints 26 lines to say one thing teaches its readers to skim. Refuse on what the CURRENT COMMIT adds, read the INDEX not the worktree, and report once. **A report is the right shape mid-ladder: `surface_check.sh` reports findings and gates nothing.**
- **I FABRICATED A TIMESTAMP TODAY AND THE GUARD CAUGHT IT.** Bumping a heartbeat inside a batched edit, I typed `17:52Z` -- plausible, wrong by nine minutes, read off nothing. **The tell is the batching**: every stamp I read in its own step was right, and the one I folded into a multi-command edit was invented. **`date -u` is its own step, always.**
- **ic cannot certify a green suite.** matts owns the authoritative run; everything here is evidence.
- **Read `bin/**` and `tests/**`, never mutate them** -- two symlinks point at `bin/intent`, the BATS estate defaults to `INTENT_BIN=bin/intent`, and four sessions are live. `native/**` and `bin/.devbin/**` are safe.
- **This repo is PUBLIC and that is FINE and intended** (hv ruled). No secrets, and a bare commit in a shared tree publishes whatever is sitting in it.
- **This shell is zsh**: no word-splitting of unquoted parameters. Never enumerate remotes through `head`.
