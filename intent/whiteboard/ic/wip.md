---
node: ic
name: Interface Claude
role: interface
session_id: 0482e68a-709f-45b1-ab98-44bc9c962bd1
heartbeat_at: 2026-08-14T13:21Z
status: active
focus: "ST0056 parity deep pass COMPLETE and handed to vc: inventory (26 command files + findings), INTENT_BIN retarget (mutation-checked), 94-row register. Three contract gaps flagged. 711 of 1235 tests reach the CLI."
claims: []
---

# Interface Claude (ic)

## DOING

- **Nothing in flight.** The ST0056 parity deep pass is complete, committed and handed to vc (`vc/inbox.ic.md`, 13:19); cc notified of the shared-infrastructure change (`cc/inbox.ic.md`, 13:20). Awaiting vc's review and hv's rulings on the three gaps.

## DONE -- 2026-08-14, the parity deep pass

On vc's work order (`ic/inbox.vc.md` 12:45) against `st/ST0056/parity.md` "The IC handoff". Measured at `69d42a7` in a detached worktree, `INTENT_HOME` passed explicitly.

- **Inventory** -- `4d46835`, `b55b3f8`. 26 `cmd-<command>.md` files + `README.md`, all generated by `parity/tools/`. Six corrections to the command-level table, every one verified by invocation.
- **Retarget** -- `87a315b`. `INTENT_BIN` through `tests/lib/test_helper.bash`; 875 calls via `run_intent` plus 104 direct sites across **five** spellings. Idempotent, reproducible from pristine, applied to the main tree.
- **Register** -- `4da3e79`. 94 rows, nothing unclassified: 30 keep, 40 split, 19 out-of-scope, 4 retire, 1 deviate.

## TODO

1. **Re-run `burn.sh` to fold in the 95th file.** `e685e90` added `tests/unit/at_lint_wp_scope.bats` after my baseline; the register's 94 rows are the 94 files at `69d42a7`. Verified green and burning 5/5 in the main tree, but it has no row.
2. **Per-test rows for the 40 `split` files** -- the second pass, once vc rules on the `corrected` class. Deliberately not guessed in pass one.
3. Standing asks to hv, still unanswered: the ic charter, the `README.md` roster row (it still says "deliberately no interface node; the roster is three"), and the usage-convention ruling now that the census exists.

## Watch-outs

- **ic holds no claim and must not write into `intent/st/ST0056/**`.** vc claims ST0056 on direct hv assignment. ic produces raw material into `intent/analysis/` + `tests/conformance/` and hands it over by `ask`; folding it into WP-01 and editing `acceptance.md` are vc's writes.
- **ic cannot certify a green suite.** matts runs the full suite externally (standing convention); ic runs single-file bats only. Any estate-wide test rewrite therefore has a human gate in the middle of it, and the plan has to be sequenced around that rather than around a self-declared green.
- **Announce before the suite-wide sweep.** The BATS estate is shared infrastructure -- cc's pre-flight and `bin/release` both run it -- so a 94-file rewrite gets an `announce` first, same shape as cc and vc's standing announce-before-editing-`bin/` agreement.
- **Read `bin/**`, never mutate it.** `~/.local/bin/intent` symlinks into this repo, so every project on this machine runs whatever state those files are in. Capturing runtime help output is read-only execution and is fine; anything that modifies a binary goes in a sacrificial worktree. (cc's watch-out; it binds ic's runtime-capture pass directly.)
- **The two obvious sources for a command surface both lie.** `bin/intent`'s dispatch has explicit arms for 8 commands and a `*)` default that maps `intent <foo>` to `bin/intent_<foo>`, so the real surface is files on disk, not case arms. `bin/intent_help` hand-maintains its Core/Configuration/Utility lists behind a hardcoded skip list (`bin/intent_help:93`) and still describes `upgrade` as "Upgrade from STP to Intent v2.1.0" (`bin/intent_help:71`) at v2.19.0. Enumerate and run; do not read and transcribe.

## Findings -- 2026-08-14, first pickup

- **v2 has no single usage convention, measured not read.** Four commands at `9cdebe0`, three behaviours: `intent st` and `intent wp` bare emit `error: ... is required` on **stderr** with rc=1; `intent ac` bare emits a 1342-byte usage block on **stdout** with rc=1; `intent todo --help` emits 1077 bytes of usage on **stdout** with **rc=1** -- an explicit help request answered with a failure code. This is the same class as the stdout-error item already in cc's hv-ruling queue (the three plugin bins, plus `intent_claude_prime:212`): a voice becoming data in a caller's captured output. **The parity contract cannot be written until hv rules whether v3 reproduces this faithfully or deviates**, and the ruling wants the full census rather than the four sites now known. Producing that census is Phase 1.
- **`lib/help/` cannot serve as the machine-readable spec.** 11 help files for 27 `bin/intent_*` scripts; 18 commands have none. The `@`-section grammar the renderer supports (`@usage:`, `@options:`, `@arguments:`, `@examples:`) is used **exactly once each** -- one file uses it, the other ten carry only `@short:`. Naming drift too: `stzero.help.md` against `bin/intent_st_zero`, and `rules.help.md` documents a `claude` subcommand rather than a top-level one.
- **Static parsing cannot enumerate this surface.** Subcommand dispatch is formatted inconsistently across scripts -- `bin/intent_todo:384` puts its arms at zero indent, `bin/intent_st:295` nests them -- so one regex over 27 heterogeneous files silently undercounts. A probe of mine scored `intent_st` (1621 lines) at zero subcommands. Enumerate by running, cross-check statically, and treat every disagreement as a finding.
- **`bin/intent_migrations` is mode 644 and is not a command.** It exists only to be sourced by the upgrade orchestrator, and `bin/intent_help`'s auto-list requires `-x`, so it is correctly absent from help. Resolves the open question of whether `intent migrations` belongs in the surface: it does not.

## Findings -- BATS estate recon, 2026-08-14

Swept by sub-agent across 94 files / 1235 cases. Sizing that changes the retarget plan:

- **The retarget has one clean chokepoint and it covers most of the estate.** `run_intent()` at `tests/lib/test_helper.bash:99-102` carries ~865 of ~1075 CLI invocations (~80%) and is a one-line change; a second mechanical pass over `$INTENT_BIN_DIR/intent`, `$INTENT_HOME/bin/intent` and `$INTENT_PROJECT_ROOT/bin/intent` picks up ~110 more across ~20 files. **There is no `INTENT_BIN` variable in the repo today** -- only `INTENT_BIN_DIR`, which names a directory of many scripts and therefore does NOT map onto a single binary.
- **~151 invocations bypass `bin/intent` altogether**, concentrated in `treeindex_commands.bats` (53 tests), `fileindex_commands.bats` (47) and `intent_critic.bats` (21). They exec `bin/intent_<sub>` directly, so they never get the dispatcher's `PROJECT_ROOT` resolution, its `INTENT_ORIG_CWD` export (`bin/intent:217`) or its `cd "$PROJECT_ROOT"` (`bin/intent:218`). **These ~121 tests exercise an entry point v3 will not have**; pointing them at `intent treeindex ...` is a semantic rewrite, not a path swap, and it changes what they prove.
- **~87 tests (7%) test shell functions directly and die with bash** -- `critic_runner_proxies.bats` (33), `critic_runner_applies_to.bats` (19), `helpers.bats` (17), the two `intent_migrations_*` files (14). The migrations ones retire **by design**, not as a loss: D09's two-hop policy says v2's own upgrade ledger is never reimplemented in Rust.
- **The file-layout problem is about half what the raw count suggests.** Of 569 file/dir assertions, **244** point at CLI-written project artefacts (`intent/st/**`, `config.json`, `issues/**`, `.treeindex/**`) -- the population genuinely exposed to a JSON canon -- while **220** are repo-content pins (`RULE.md` x47, `AGENTS.md` x26, `SKILL.md` x25, `lib/templates/**`) that survive a binary swap untouched and are not CLI tests at all. Splitting those two populations is the first thing the register must do.
- **Assertion classes**: 58% pure output/exit-code, 20% mixed output+layout, 10% layout-only, 11% shell-internal. Frontmatter-literal coupling to the v2 markdown canon: `status:` 159, `id:` 69, `intent_version:` 32, `title:` 31, `created:` 30, `satisfied:` 29.
- **The voice contract is stricter than "same words".** 112 `assert_output` calls are exact whole-output matches, so any added banner, warning or trailing space breaks them; `output_width.bats:44-140` pins byte-exact table rendering and width-independent canonical markdown, and `title_pipe_sanitize_guard.bats` pins the `|`-to-`/` substitution because `render_table` splits on `IFS='|'`. v3 must reproduce column padding exactly, not merely say the same things.
- **A live vacuous-green risk.** `pre_commit_hook.bats` (15 tests) depends on the real `intent` being on PATH; without it `lib/templates/hooks/pre-commit.sh:48` fail-opens and the tests pass having proved nothing. CI adds `bin/` to PATH for exactly this reason (`.github/workflows/tests.yml:49-56`, with a comment saying so). Any v3 harness must install the binary as `intent` on PATH or inherit the same silent pass.
- **Two cheap unrelated wins found in passing.** CI clones `bats-support` / `bats-assert` / `bats-file` over the network on every run (`tests.yml:82-84, 152-154`) and **zero `bats_load_library` calls exist** -- every helper is hand-rolled in `test_helper.bash:105-195`, so that network dependency is dead weight today. And 46 `run_intent` calls carry no `run` wrapper (`intent_agents.bats:94-95`, `output_width.bats:48`), so under bats an exit-code drift aborts the test opaquely instead of failing an assertion -- which is precisely the diagnosability WP-05's triage will need.
- **Other traps for the harness**: the suite is not parallel-safe (global `/tmp/intent/` sentinels); `setup_fake_home` is called in only 10 files and some tests `cp` real content into `$HOME/.claude`; several tests assert against the live git working tree (`no_absolute_home_paths.bats:37, 100, 103`); `no_template_fallback.bats:26` copies the whole `bin/` tree to build a deliberately broken install, a shape a self-contained binary cannot have; `create_test_project` hard-codes `intent_version: 2.10.0` (`test_helper.bash:68`) while `st_commands.bats:63-64` asserts the live `VERSION` file. `tests/docs/` and `tests/releases/` are empty untracked directories holding no tests at all, and `intent/llm/MODULES.md:258` documents a `tests/fixtures/upgrade/` tree that does not exist.
- **Fixtures are not the migration surface; the test bodies are.** `tests/fixtures/` is rule- and extension-shaped (and `fixtures/critics/` is referenced by no `.bats` file at all), so it largely survives. The v2-markdown fixture surface is inline: 181 heredoc writes, 73 of them writing `info.md` and 13 writing `acceptance.md`.

## Decisions

- (2026-08-14) **The inventory is a generated, revision-stamped artefact, not a written document.** Directly from cc's ruling that a measured figure must name its subject and revision or it is a rumour with a decimal point -- the 314-vs-1639 AT-row error is the scar. The surface will be re-measured after cc's consumer sweeps and again during WP-06; a hand-typed list cannot be diffed and starts decaying the moment it lands.
- (2026-08-14) **Measure what a test EXERCISES by redirecting the thing under test, not by reading its assertions.** The burn ratio -- run twice, once with `INTENT_BIN=/usr/bin/false`, and diff the failures -- classified 94 files mechanically and twice found what reading could not: `treeindex_commands.bats` reads as 53 CLI tests and is 53 tests bypassing the dispatcher, and `claude_with_intent.bats` reported zero burn while looking like a CLI test because it aliased the binary through a spelling four grep passes had missed. **The mutation check is not only a guard on the harness; it is the classifier.**
- (2026-08-14) **My own tooling failed four times today in the exact shapes this project keeps recording, and every one was silent.** A `BASH_SOURCE`-derived path under zsh made 20 probes fail identically in `cd` and report a uniform rc=1 surface that read like data. An invalid ERE made 13 scripts report "no dispatch found" -- caught only because the error was not swallowed. `set -e` killed the sweep at the verification grep that SUCCEEDS by matching nothing, leaving the estate half-rewritten. A `printf` format beginning with `-` was parsed as an option and silently dropped a line from 26 generated files. **Instrument the instrument: every harness here now refuses loudly rather than emitting a plausible empty surface.**
- (2026-08-14) **A test the classifier cannot bucket is named, never guessed.** The refuse-lossy discipline from `at lint --fix` applies to classification as much as to migration: a wrong keep/retire call is silent lost coverage in v3, which is the same defect the AT grammar existed to kill.
