---
node: ic
name: Interface Claude
role: interface
session_id: f26f5f7b-1122-4fc2-89ad-dc33221f4e10
heartbeat_at: 2026-08-15 20:55Z
status: active
focus: "Folded (fold 5). Everything of mine is green and pushed; every remaining item of mine is blocked on a vc ruling. Nothing blocks cc or dc."
claims: []
---

# Interface Claude (ic)

## DOING -- PICK THIS UP FIRST

**THE ONE THING I OWE, and its criterion has landed: AC-05.5 (vc, `1793dc38`).** Decision drift -- a decision recorded in one contract artefact and absent from another, invisible from either side alone because both halves are individually correct. Reports rather than refuses, on my own reasoning.

Two axes:

- **state vs the BINARY** -- covered. `surface_check.sh` does flags, arity, and six invariants.
- **state vs `parity.md`** -- **NOT covered. This is the half AC-05.5 adds and the half I build.** INV-06 and INV-07 were both ratified there while the table went on asking hv for them.

**ONE THING BLOCKS THE FIRST LINE OF CODE, and it is a format ruling rather than a design question. Asked vc 19:06Z: what does a ratified `Corrected` member's CITATION look like?** My proposal, in vc's existing idiom rather than a new one: a trailing `-- covers: INV-07, INV-06` clause, spaced `--` separator, comma-separated ids with nothing fused -- **the same grammar the AT row already uses**, so the contract has one rule for "a machine-read list at the end of an authored line" instead of two. The check is then SET EQUALITY, firing in **both** directions: a row claiming `corrected` that nothing ratifies, and **a ratification nothing has applied** -- the second being what would have caught INV-06/07, and the one a check written from the defect would most likely have missed, because the defect presented as rows being stale.

**DO NOT author the citations before vc rules the format.** `parity.md` is the WP-01 spec and its contents are vc's. **Live evidence for ids-not-locations arrived after I proposed it:** my own row cited `acceptance.md:298` and it had rotted to `:302` within the hour.

## TODO

1. **The agent guide's AUTHORED half (AC-09.4)**, when the v3 workflows settle. Spec written (`surface/agent-guide.spec.md`), control built and mutation-tested (`parity/tools/guide_refs_check.sh`). **Waiting on vc: one file carrying `usage-rules.md`'s dual role, or split?** The measurement argues for splitting -- a document serving two readers was maintained for one of them.
2. **INV-03's probe, deliberately deferred with a condition.** Only ~5 declared paths reach the project gate bare, because clap's missing-argument error fires first and 40 more commands are unimplemented. **Revisit when the implemented surface makes the population mean something** -- not before, because a 5-member uniformity check that already passes is a decoration.

## Open with others -- LIVE ASKS ONLY

1. **WITH vc, FOUR OPEN (19:06Z / 19:26Z / 19:39Z).** In the order they block me:
   - **The `Corrected` citation FORMAT** -- blocks the AC-05.5 check above. The only one that blocks work.
   - **`ac satisfy` records an AC as Satisfied with EMPTY evidence, prints `ok:`, and it COUNTS TOWARD THE GATE.** `contract.rs` resolves `Satisfied { .. }` past the evidence and counts it. Its own header is the argument -- _"evidence is a human judgement with no green to read"_ -- so evidence is the whole substitute for a green, and this is a green with nothing behind it, on the verb whose job is recording that a criterion was met. **Root cause is EXP-07**: `required` never reaches clap, so the rule is hand-written per arm and it is **2 of 3 right**. cc has the one-line fix; **the questions that are vc's are whether the FACADE should refuse it (the CLI fix does not close the GraphQL door), and whether anything is already recorded that way.**
   - **INV-03: v3's project-gate message has diverged from what the invariant declares, with nothing ratifying it.** Proposed `corrected` rather than revert -- v3's message names the directory, says what it looked for, and carries a `remedy:` line, so reverting would make the surface worse to make a table right.
   - **`ingest --from-md`**: a one-mode mode flag, but cited in five places across four artefacts I do not own, one of them ratified. **If vc rules it withdrawn, four artefacts change in ONE commit or the drift is real** -- and one is `acceptance.md`, so ordering matters.
2. **WITH dc (19:22Z): took their offer 2.** Property handed over: **every committed generated artefact's inputs are IN THE REPOSITORY** -- tracked by git, not merely documented. Four instances today; the pre-commit `skew` check already prints a "declared un-re-derivable" category to hang it off. Flagged that the declarations are PROSE headers today, so the first real work is a machine-readable declared-inputs line -- more than dc offered, and dc's to scope or refuse. Also asked dc **not** to optimise `int build cli` away: that ~30s is buying correctness and should stay visible.
3. **The hv queue is 8 real units** (was reported as 14). `critic`'s exit-2 is still the only one with a LIVE CONSUMER -- the pre-commit gate reads it today.
4. **NOTHING IS OUTSTANDING WITH cc.** All four defects fixed; both their asks answered; `surface_check` green on both halves. Their standing note offering me the `ingest [PATH]` row crossed with my commit of it.

## Watch-outs

Durable only. Everything settled lives in the artefact that carries it.

- **D42, SETTLED: it is a rule about SIGNATURES and SOURCE DOCUMENTS. I over-applied it TWICE IN ONE DAY, both times towards MORE PROHIBITION, which looks like rigour.** Forbidden: confecting a time into a source document, and -- vc's sharpest form -- **no CLI or `intentsvcs` function TAKES a time as a parameter** (a property of the API surface, so a time-typed input is a defect by inspection). Permitted, all three ruled by hv: **RETURNING** a time; **reading a clock to make a DECISION**; **stamping when a command ran into a GENERATED artefact**. hv: _"there is no need to be pathological about it."_ **All three of my D42 findings fell.** An over-strict reading would have withdrawn `backup --list` and every `created`/`completed` a `show` prints -- **exactly the surfaces D42 exists to make trustworthy rather than remove.** The rule takes from the WRITE path and gives the READ path its guarantee.
- **AND THE AUDIT STILL PAID, BECAUSE CHECKING MY OWN WRONG CLAIM IS WHAT FOUND THE REAL DEFECTS.** A pattern match produced three wrong verdicts; reading the code under them produced two right findings -- `backup --list` displaying a file mtime AS a record fact, and `todo.md` being the sole store of its own watermark.
- **A CHECK THAT CANNOT FAIL IS NOT A WEAK CHECK, IT IS A DECORATION -- and it hands you a reassuring result first.** Seven instances now. The sharpest was the check written to CLOSE this class: the flag-completeness loop went green while 5 of 93 flags rendered nothing, because it greps for the LABEL and 88 flags supplied it. **Presence-of-label and completeness-of-population are different questions.** The subtlest: a TSV field shift made the surface check report **MORE coverage and FEWER findings at once** -- 59 probed against 46, 11 findings against 13 -- which reads as a better run, caught only because the earlier output was still on screen. **And the seventh arrived INSIDE the fix for the sixth**: my new staleness refusal blocked its own mutation test, which would have made every later green unfalsifiable. **Count the population, assert both sides are non-empty, keep the previous result to compare against -- and when a long-red check goes green, mutate it before believing it.**
- **A CONTROL THAT DIFFERS FROM THE MUTANT IN A SECOND WAY IS NOT A CONTROL** -- run the copy as a SIBLING of the original, and print the control result FIRST. **The exception, from cc, is worth holding beside it: when two halves fail TOGETHER in the field, the honest control mutates both.** Their case was a generator that stops injecting a version marker plus a reader that reports constants -- either alone is caught, the pair is invisible to comparing against committed files.
- **A NUMBER THAT HAS BEEN WRITTEN DOWN GETS TRUSTED BY THE NEXT PERSON, INCLUDING THE ONE CORRECTING IT.** I wrote "SEVEN citations" and **listed six** -- an off-by-one against my own list, in the same sentence. cc corrected me to six (rightly: one had genuinely gone) **by subtracting from my seven rather than recounting, so the correction inherited the error.** It is five. **Neither of us was careless; both of us trusted a written-down number**, inside an exchange about exactly that failure. **Recount, never adjust.** _(This watch-out itself carried the stale SEVEN for an hour, in the line below, until fold 5.)_
- **A CITATION THAT NAMES A LINE ROTS SILENTLY, AND FAST.** My row cited `acceptance.md:298`; within the HOUR it was `:302`, because vc added rows above it. Nothing errors, the number is still a number, and it points at the wrong sentence. **Cite IDS, not locations.**
- **EVERY INPUT TO A DECISION CAN BE CORRECT AND THE DECISION STILL CONTRADICT A DISTANT ARTEFACT. GREP THE SPELLING BEFORE YOU WITHDRAW IT.** I ruled `ingest --from-md` dropped: cc's argument was sound, my own note on the row said the same, and it really is a mode flag with one mode. **Then I grepped and found five live citations across four artefacts I do not own** -- one of them `acceptance.md`, which does not merely mention the flag but RULES that its scaffolding ships. **The drop would have put my table in contradiction with the ratified contract: AC-05.5's exact class, manufactured by me, an hour after vc landed the criterion to catch it.** Nothing about it felt uncertain, which is the point -- **care does not scale to a citation four files and two thousand lines away, and I was being careful.** A withdrawal is a claim about every reader of the name, so it is a `grep -rn` question before it is a judgement.
- **A SKIP WHOSE REASON IS "HARD TO CHECK" IS A PROMISE NOBODY MADE, AND MINE WAS HIDING A FINDING.** `surface_check.sh` declares INV-03 unchecked because it "needs a probe run outside a project" -- true, and never a claim that nothing was wrong, **but it reads like one, and it is the sentence anyone re-reading the file relies on.** Going to build the probe meant reading the declaration, which is how the gate-message divergence surfaced. **Revisit skips; do not trust their reasons.**
- **A SKIP LIST IS A PROMISE THAT SOMETHING ELSE RENDERS THE KEY**, and it is only as good as that promise. Mine exempted four keys nothing rendered. **Reading the list produced the bad list; the mutation test found it.**
- **`read -r a b c d` WITH `IFS=$'\t'` COLLAPSES AN EMPTY FIELD** -- bash and zsh alike, both verified. Every column after the empty one shifts. **Emit a `-` placeholder, never an empty field**, and refuse on a row whose shape is wrong: a row that yields nothing to check is indistinguishable from a clean one.
- **ENUMERATE THE POPULATION; DO NOT SNIFF FOR A MARKER -- and a structured query is a needle too.** `jq '.families[].entries[]'` missed a whole top-level array; the D42 audit's first pattern matched `manage`/`validate`/`update` on substrings of `age` and `date` and reported 25 rows. **Word boundaries cut it to five, of which three were real.**
- **"IT DOES NOT EXIST" IS A CLAIM ABOUT THE FILESYSTEM, SO GO AND LOOK. THREE TIMES, and the third cost the most.** git answers questions about HISTORY; if the question contains "exists", the answer comes from the filesystem. (1) The probe input, concluded gone from an empty `git log --all`; it was on disk, and a rule in `parity.md` plus a refusal in `gen_inventory.sh` were built on top. (2) `gen_register.sh`'s burn input, recorded as gone -- **committed at `tools/burn-baseline.tsv`, byte-identical, only the FILENAME differed.** (3) Its worktree and its 196-file TAP corpus, both still on disk. **The third is the worst because the false claim became the stated REASON for not fixing two defects, so it stopped being a gap and became a justification.** One `find` beat all three.
- **A STALE BINARY ARGUES HARDEST EXACTLY WHEN IT IS MOST WRONG.** It does not fail loudly -- it produces a plausible, well-formatted, entirely wrong report, **and what it invents is precisely what somebody just fixed**, so it reads as a regression by the peer who closed it. Nearly sent one to cc. **MECHANISED rather than remembered**: `surface_check.sh` refuses on a binary older than its own inputs, filed with the absent-binary refusals, because a binary that is not the tree is an INABILITY TO MEASURE. `find -newer`, never `stat` -- BSD and GNU take different format flags, and a portability check must not fail differently on the platform it is not being run on.
- **`warning: build failed, waiting for other jobs to finish` IS CARGO'S LOCK MESSAGE, NOT A COMPILE ERROR** -- four sessions share one `target/`, so a peer building at the same moment aborts your build and it reads exactly like a broken HEAD. **I nearly told the peers HEAD did not compile.** The tell is what is ABSENT: no `error[E...]`, no `-->` line. **Re-run before believing it.**
- **A SWEEP DOES NOT MOVE A FILE, IT SPLITS A CHANGE.** My unqualified `--amend` took cc's test without the `store.rs` methods it calls and **HEAD did not build for ten minutes**. Each half reads as finished alone. **After a sweep the question is not "whose file is this", it is "does it still build."**
- **`--only` PROTECTS THE COMMIT AND NOT THE AMEND**, and never CLEARS the index, so the shared index accumulates across every node. **Name paths on the amend too.**
- **RE-DERIVABILITY IS NOT COMPLETENESS.** A lossy generator is a perfect fixed point with itself, so skew passes forever. It hid 15 of 20 authored fields.
- **A MISSING MEASUREMENT MUST PRESENT AS A REFUSAL TO MEASURE, NEVER AS A MEASUREMENT OF NOTHING.**
- **A QUOTE CHARACTER INSIDE A QUOTING CONTEXT, IN PROSE NOBODY PROOF-READS FOR SYNTAX.** An apostrophe in a single-quoted jq program CLOSES it -- `vc's`, then `run's`, the second inside a comment explaining a different bug. Backticks in a double-quoted string are command substitution. **Put the program in a file.** Same family: **a literal `\n` in a dispatch-table prose value** breaks formatter idempotence, because those fields render as ONE markdown bullet (AC-03.2).
- **A GUARD THAT MUST BE BYPASSED IS A GUARD NOBODY KEEPS**, and one that prints 26 lines to say one thing teaches its readers to skim. Refuse on what the CURRENT COMMIT adds, read the INDEX not the worktree, and report once. **A report is the right shape mid-ladder: `surface_check.sh` reports findings and gates nothing.**
- **I FABRICATED A TIMESTAMP TODAY AND THE GUARD CAUGHT IT.** Bumping a heartbeat inside a batched edit, I typed `17:52Z` -- plausible, wrong by nine minutes, read off nothing. **The tell is the batching**: every stamp I read in its own step was right, and the one I folded into a multi-command edit was invented. **`date -u` is its own step, always.**
- **ic cannot certify a green suite.** matts owns the authoritative run; everything here is evidence.
- **Read `bin/**` and `tests/**`, never mutate them** -- two symlinks point at `bin/intent`, the BATS estate defaults to `INTENT_BIN=bin/intent`, and four sessions are live. `native/**` and `bin/.devbin/**` are safe.
- **This repo is PUBLIC and that is FINE and intended** (hv ruled). No secrets, and a bare commit in a shared tree publishes whatever is sitting in it.
- **This shell is zsh**: no word-splitting of unquoted parameters. Never enumerate remotes through `head`.
