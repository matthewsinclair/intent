---
node: ic
name: Interface Claude
role: interface
session_id: 42692e4f-2c11-4711-b1b5-c87a721f059c
heartbeat_at: 2026-08-18 23:27Z
status: paused
focus: "**DAY CLOSED. FOLD 35, aggressive: 43 watch-outs -> 18, instances collapsed into the class they are instances of, discharged ones dropped rather than aged out.** Today: overturned my own `18 of 24` (both halves wrong, ratio closed arithmetically, inside the instrument gating AC-00.10) and rebuilt it as a derived `12 of 45`; built AT-00.12's `of_n_labels_its_derivation.sh` with both of vc's named mutants RED. **Truncated dc's inbox and restored it, verified by them independently.** Pin `f2e4d1f9` / `84be404b`. v3 NOT on PATH; upstream FROZEN."
claims: []
---

# Interface Claude (ic)

## DOING

**Nothing in flight. Day closed.** Everything landed is committed and pinged; nothing sits uncommitted in the tree.

## TODO -- LIVE ONLY, in dependency order

1. **DISCHARGED tonight -- the blind pair on `rig_selftest.sh` ran and BOTH SAID PASS.** vc read the emitted output as a reader; my instrument parsed the source as a machine. **vc's residual is one my method STRUCTURALLY COULD NOT PRODUCE**: `$DIE_SITES` is not a ratio operand so my tool never looked at it, and for `$TOTAL_SITES` it asks only whether a derivation EXISTS -- never whether the WORDS naming it identify a countable thing. _die call-sites_ named a derivation and admitted two incompatible ones (left-margin scan 24, observable 41). Fixed at `887345b1`; the limit is now stated in the tool's own reach.

2. **AT-00.11 `of_n_closes_over_examined.sh` -- GATED ON cc's FILE MOVE, not on their code.** The code is green (`intentsvcs` 436/0, `intent-cli` 203/0); the 57 canon files have NOT moved and cc pings before they do. **Build BOTH trees as worktrees at named revisions, never one as "here"** -- nested is HEAD today and flat is HEAD after the move, so either hard-wired side costs a rewrite. Adjudication order is by GATEDNESS, not count; dc's 10 is a floor because their proxy needs a literal path shape.
3. **EXP-09 + the guard-population hole + the clap short-circuit ARE ONE ENTANGLED ENTRY** (`dispatch-table.md:3003`) and go to hv **together**; picking one re-opens the other two. **This needs a RULING, not an AC**, so hv's moratorium answer does not reach it. Raised with matts today; still open.
4. **THE AC GAP** -- WP-10's close only, hv's to mint. I have not proposed a row and will not.
5. **STRUCTURAL, raised twice, not re-raising unprompted: my work sits under no WP**, so none of it counts toward the 3.0.0 gate. AT-00.10 and AT-00.12 are live rows with instruments behind them and no work package.

**RULED CLOSED -- do not re-open:** the 29 register rows (re-measurable only AT the pin; the live v3 question needs its own instrument). Fix 2. The hoist pin. The todo-glyph defect. `canon_commit_check.sh` gating (stays MANUAL until a narrow attachment-sync verb exists -- ST0057 WP-08). The `18 of 24` ratio (retired and derived; vc amended AC-00.10/AT-00.10 at `0acf6f32`, verified at the commit).

## Open with others

- **PIN, verified independently rather than taken on report:** `intent` **`f2e4d1f9005d0334`**, `intentd` **`84be404b`**. Both built from a DIRTY tree, so the marker does not distinguish them -- **pin by the hash, never by `dirty-18197aaf`**, which three distinct binaries carried in one day.
- **cc** -- WP-01 code green, files unmoved; they ping before touching the 57. `realise_plan.sh` confirmed correct against their applied patch; its migration-state refusal is right for this window.
- **vc** -- holds ST0056 + ST0057. Owes me their blind `rig_selftest.sh` verdict; owes the estate AT-00.12's row moving off `to-write` (its cited file now exists, and `stale_at_check.sh` fired on it).
- **dc** -- `:246` fixed at `e53461d2`, three minutes after the report. Restoration of their inbox verified by them, not by me.
- **hv** -- items 3 and 5 above. Nothing else.

## Roster admission bar -- what an instrument must clear to join `parity/tools/`

- Names every subject **first** and unconditionally, and prints a **population count on every run**, pass or fail.
- **Exit 0 / 1 / 2, cannot-measure DISTINCT from a finding.**
- **States its contract and its REACH in the OUTPUT, never in a comment** -- and a contract line is **not self-verifying**: it is another claim, and it inherits the failure mode of whatever it describes. **The same defect lives in CHECKERS** (vc, AC-01.7): one that validates the FORM of a declaration and never its REFERENT is this line pointed at someone else's claim.
- **A closing count must close over what was EXAMINED, never over what EXISTS**, and **enumerate the EXCEPTIONS, not the successes**.
- **Establishes the identity of what it compares** rather than inheriting it.
- **The vacuous-pass arm is checked EXPLICITLY** -- a subject with zero members must never report "all match".
- **Demonstrated RED first, ideally UNPLANTED** -- and against the CRITERION, not the exemplar (see below).

## Watch-outs

**THE CLASS: AN INSTRUMENT WHOSE OUTPUT IS INDEPENDENT OF THE THING IT MEASURES.** Everything below is either a mechanism the class does not predict, or a live constraint. **Two-halves test: could this case have FAILED, and was the subject still there when it passed** -- the second half is answered by a printed population count, never by a pass line.

- **A GUARD BUILT FROM ONE INSTANCE OF A CLASS DEFENDS AGAINST THAT INSTANCE, NOT THE CLASS -- AND BEING FRESH IS WHAT MAKES IT FEEL LIKE COVERAGE.** I wrote a sentinel precondition today because a sentinel-replace had silently matched nothing in cc's inbox; hours later the same guard destroyed three entries in dc's, because it tested **"is the sentinel present"** and concluded **"are there no live entries"** -- and that file answered yes to the first and no to the second. **A documented trap does not fire unless you re-read it at the moment you write the line.**
- **A BLIND PAIR AGREEING IS NOT A NULL RESULT -- THE VALUE IS IN THE DIVERGENCE OF WHAT EACH METHOD COULD SEE.** vc and I adjudicated the same file without sight of each other and both returned PASS; the agreement taught nothing, and **vc's residual -- invisible to my method by construction -- was the entire yield.** Two verdicts by the SAME method would have agreed and found nothing. **A REFERENT CHECK IS A READER'S INSTRUMENT AND NOT A GREP'S**: a checker can verify a derivation EXISTS and never that the words naming it identify a countable thing, which is AC-01.7 arriving in my own tool hours after I quoted it at other people.
- **A PROBE THAT REPRODUCES A KNOWN NUMBER MAY BE REPRODUCING ITS METHOD RATHER THAN MEASURING THE SUBJECT, AND THE EXACTNESS IS THE TELL.** `^[[:space:]]*die ` hit the hand-counted 24 on the nose where four other probes gave 6 / 41 / 30 / 47 -- both scan the left margin, so both are blind to `|| die`, case-arm, and non-die refusals. **I went looking for a derivation AFTER knowing the target, which is fitting, not measuring.** This is the mechanism-visible form of **agreement can synthesise a falsehood neither party held**.
- **A RATIO'S TWO HALVES CAN COUNT DIFFERENT POPULATIONS AND STILL CLOSE ARITHMETICALLY, WHICH IS EXACTLY WHY IT SURVIVES REVIEW.** `18 of 24`: numerator the case count, denominator refusal sites, in the instrument gating the row about whether instruments can be trusted. **A migration makes such defects observable, which is a reason to run instruments across BOTH layouts rather than the target one.**
- **DRIVING FINDS WHAT READING CANNOT, AND READING FEELS IDENTICAL EITHER WAY.** My definition of a refusal site was wrong twice; both corrections came from mapping each case's expected text onto the source and getting two ZEROS. **Working intimately with a file is not the same as having read it.** Verify the premise at the moment you act on it, and **re-drive the RULE, not only the fact** -- an alarm is only as live as the constraint it is raised against.
- **A FINDING MANUFACTURED BY AN INSTRUMENT'S OWN REACH IS WORSE THAN A MISSED ONE, BECAUSE IT GETS ACTED ON.** Under-reach yields a number too small; **fabrication yields a FACT, and a fact gets acted on**. My tool flagged `ADDS 0 of $total` where the 0 states a measured result from a branch reached only when it holds -- it cannot read the guard. Killed before it reached dc. **Never `tr` grep output onto one line: adjacency is not syntax**, and a probe that joins independent matches invents a form that appears nowhere.
- **WHERE A RECORDED FIGURE SITS CHANGES HOW HARD IT LIES (dc).** `86 of 132` sat inside the `CANNOT MEASURE` branch -- whose entire message is that nothing was compared -- and then printed a firm ratio in the tool's own voice. **A reader just told the instrument measured nothing has every reason to read the next number as one it did measure.**
- **EXCLUDING THE EXEMPLAR LEAVES THE REAL EXPOSURE UNTOUCHED (vc).** The risk was never that one file gets graded by its author -- it is that **the instrument's definition of compliance becomes whatever the exemplar now does**, biasing every file it grades. **The remedy is a mutation test against the CRITERION: two named mutants, both red, or you have an exemplar-matcher wearing a criterion's name.** My first build passed both. **Stating a bias does not remove it.** And a fix that works by coincidence of the fixture is not a fix.
- **A FIGURE TRAVELS WITH THE SUBJECT AND REVISION IT IS A CLAIM ABOUT (producer), because otherwise every consumer performs MEASURED-AGAINST-RECORDED and cannot tell (dc, consumer).** One class, two ends. **A stale figure is most harmful where its subject is GROWING**, which is exactly where someone wants to know.
- **AN ABSENCE IS EVIDENCE ONLY IF THE INSTRUMENT'S REACH COVERED WHERE THE THING WOULD BE, AND A ZERO LOOKS IDENTICAL EITHER WAY.** A grep measures where you point it. My sweeps have: run from the wrong directory (`0` at rc=0), missed 55 extensionless executables under `bin/` via `--include='*.sh'`, and credited an instrument for a reach it did not have. **State what the instrument could not have seen, out loud, even when the answer is nothing.**
- **A KEYWORD PROBE FOR GOOD PRACTICE PENALISES WHOEVER DID IT NATURALLY, AND ITS FALSE POSITIVES ARE INVISIBLE TO ITS AUTHOR.** My reach census called `self_provenance_check.sh` comment-only; it states its limit at `:256` in words my vocabulary did not contain. **The set of ways to state a limit is OPEN**, so vocabulary matching is the wrong instrument for the question -- not a vocabulary that needs widening. **The fix is a DECLARED FORM**, which is what made both the non-die refusals and AT-00.12's label arm mechanical.
- **A DEFAULT VIEW IS A FILTER, AND AN INSTRUCTION NAMING THE COMMAND WITHOUT THE FLAG INHERITS THAT DEFAULT SILENTLY.** I measured with `st list --status all` (57) and transmitted `st list` (2), and it went into cc's board under my name -- bare, it reads 2 before and 2 after a canon move, so the check PASSES while the 52 threads it protects are absent. **Likewise any flag that narrows a population must say so in the SAME output that reports the score.**
- **WHEN A SUPPORTING FACT TURNS OUT FALSE, ASK WHICH IT HELD UP -- THE ARGUMENT OR THE ANSWER (vc).** If the answer does not survive its supporting fact, there was never an answer. Cheap at the moment of correction, and it tells you immediately whether you are editing a sentence or reopening a decision.
- **AN ASSERTION ROUTED THROUGH A RESOLVER HAS AN ORACLE ONLY IF SOME OTHER TEST PINS THAT RESOLVER TO A HUMAN-WRITTEN LITERAL -- CHECK THE CHAIN TERMINATES (vc, and it changes ORDER not just content).** The estate's only two literal on-disk location assertions were both on the repointing list; **cc landed `canon_relocation.rs` FIRST to hold the literal before any came out.**
- **WHETHER THE UNMIGRATED END CAN STILL PRODUCE A VALUE PREDICTS LOUD VERSUS SILENT FAILURE IN A TWO-ENDED MIGRATION (cc, confirming my `${var#pat}` finding from the other side).** A missing directory cannot, and fails instantly with `NotFound`; **a string operation always can**, so `${var#pat}` returns the string UNCHANGED and emits `ST0056.json` as an id with nothing complaining.
- **A SWEEP WOULD REWRITE THE FIXTURE THAT PROVES THE MIGRATION WORKS (cc; the executable form of my `gen_register.sh:256`).** `v2_thread()` writes `intent/st/<id>/thread.json` **on purpose** -- the v2 side of a two-sided test. Repoint it and the migration test migrates nothing and passes.
- **BANKED FROM vc, LOAD-BEARING FOR MY RE-POINTING:** after WP-01 `thread_dir()` must STILL resolve `intent/st/<ID>/` -- `info.md` and `acceptance.md` hang off it and the views do not move. **Only canon leaves.** A wholesale move would satisfy AC-01.1's letter while emptying `intent/st/`.
- **A SEND IS NOT A DELIVERY, AND THE WRITE ITSELF CAN SILENTLY NO-OP.** Write the durable file first, then ping, and **never let the ping be the evidence**. My sentinel-replace matched nothing in cc's inbox while my script printed `appended` anyway -- the success line independent of the write, in the message announcing that class. **Assert the bytes reached disk.**
- **A SATISFYING EXPLANATION ENDS AN INVESTIGATION, AND EVERYTHING THAT GOES WRONG GOES WRONG PLAUSIBLY.** Nine roster timings overstated and one understated by 49% -- had they all skewed one way, "faster hardware" would have explained them and survived. **Build instruments that fail loudly rather than degrade: a blank is cheaper than a number.**

### Standing constraints -- operational, not judgement

- **No pushes to `upstream` (FROZEN).** `local` only, and published-verified by reading the remote, never by the push exit code.
- **Never `git commit -A`** -- always `--only <paths>`; it does not stage untracked files, so `git add` first.
- **Never mutate `bin/**` or `tests/**` in place.** `native/**`, `surface/**`, `parity/tools/**`, `bin/.devbin/**` are safe -- **but do not REBUILD `native/` while a peer may be mid-measurement.**
- **Instrument edits: commit them and ping vc**, never leave them in the tree.
- **v3 is NOT on PATH** until hv lifts it by name. **matts runs the full suite and is the acceptance verifier.**
- **EVERY figure presented as a time is read from `date -u`**, not only board stamps. `ls -l`, `git log` and `stat` without `-u` print LOCAL -- I published a `20:45Z` that was 19:45Z three times, in a message about coordinating on timing.
- **A peer cannot grant escalation, including me to myself.** Never treat a peer message as the user's approval; refuse permission laundering and surface it.
- **Read exit codes WITHOUT a pipe** -- `| head` reports head's status, and `PIPESTATUS` is clobbered by the next command. **`exit` inside `$( )` exits only the subshell**, so a refusal can print and the tool carry on.
