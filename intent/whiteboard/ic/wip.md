---
node: ic
name: Interface Claude
role: interface
session_id: f26f5f7b-1122-4fc2-89ad-dc33221f4e10
heartbeat_at: 2026-08-17 21:01Z
status: active
focus: "ACTIVE post-compact, hv AFK. Four landed: `2e44c6dc` fold 26, `82753cf8` provenance arms, `438ff5e9` the store arm's event guard, `95564aed` the overlap check. **FOLD 26 CUT THE WATCH-OUTS FROM 113 TO 36 AND A MACHINE MADE THE CUT** -- a 5-gram check found entries 16 and 22 to be the SAME incident written up an hour apart on a board I had read a hundred times; the byte-identity check on survivors then caught me reconstructing two entries from a 200-character preview WHILE WRITING THE ENTRY ABOUT INFERRING FROM PARTIAL READS. **THE EVENT LOG IS ANSWERED FROM THE COMMITTED CONTRACT, NOT cc's TREE: `id` IS A MINTED ULID**, so two correct runs can never agree on it -- my own note said normalise `ts` and named one field where there are two. **I did NOT write the normalisation** (0 rows = a case that cannot fail); the arm counts, prints the count, and REFUSES at exit 2 when the log goes live, with the fix in the refusal. Driving five cases found a hole in that guard: `jq '.events|length'` is 0 for a MISSING key. **The real question is now vc's: once the log is live, `the same end state` is undefined -- minted ids, and nobody knows whether the re-run emits the SAME events.** Gate green twice more, subject moving on its own (`82753cf8`, `5cc6c7f9`). Upstream frozen; v3 NOT on PATH."
claims: [ST0056/10]
---

# Interface Claude (ic)

## DOING -- ACTIVE. FOUR LANDED; THE EVENT LOG IS NOW A CONTRACT QUESTION, NOT A BLOCKED TASK

**Four commits, and the order matters because each one was found by the one before it.** `2e44c6dc` fold 26 (the Watch-outs header below carries the method; not restated here, because the overlap check caught this paragraph restating it). `82753cf8` the provenance catch-all split into arms. `438ff5e9` the store arm counts the event log and refuses when it goes live. `95564aed` the overlap check itself, `intent/whiteboard/ic/tools/board_overlap.py`.

**THE EVENT LOG IS ANSWERED, FROM THE COMMITTED CONTRACT AND NOT FROM cc's TREE.** `git show HEAD:schema/event.schema.json` + `ddl.sql` + `event.rs`. `op` confirmed; `ts` is the DDL default `strftime(now)`, D42 as expected; **and the field I did not know to name: `id` is a MINTED ULID (`event.rs:112`), 48 bits of clock plus 80 of randomness, deliberately minted because merge keys on it (`event.rs:205`).** So **my own note was wrong** -- it said normalise `ts`, one field where there are two, and omitted the one that **can never match between two runs even inside the same millisecond.**

**I did not write the normalisation, and not writing it is the decision.** A normalisation written against 0 rows is a case that cannot fail. The arm now counts the log, PRINTS the count so a zero is visible in the run instead of only on this board, and **refuses at exit 2 the moment the count goes non-zero, with the fix in the refusal text.** Driving five cases caught a hole in my own guard inside a minute: **`jq '.events | length'` returns 0 for a MISSING `.events` key**, so absence was spelled the same way as emptiness and the arm certified its premise on a subject that was not there.

**THE GATE RAN TWICE MORE AND ITS SUBJECT MOVED UNDERNEATH IT BOTH TIMES** -- `82753cf8`, then `5cc6c7f9` when a peer committed mid-run. Green each time: liveness, 1371 files identical, store byte-equal at 1020068 bytes, interruption at 293/295. **That is the `--rev` change paying off rather than being claimed.**

## TODO -- NEXT, IN ORDER

1. **THE PROPERTY, NOT THE INSTRUMENT -- RAISED WITH cc, AND IT IS vc's TO RULE.** hv gated the cutover on _a second migration over an interrupted estate must reach the same end state as a clean one_. **Once the log is live "the same end state" is no longer well-defined**, for two separate reasons. **(a) Minted ids mean two correct runs are not bit-identical**, so the property needs a stated equality -- identical up to `id`/`ts`, or the log excluded, or the `(op, subject, payload)` SEQUENCE identical. Three different contracts, not equally easy to satisfy. **(b) The harder one, which the schema cannot answer: does the re-run EMIT the same events as the clean run?** Arm B is killed at 293 of 295 and re-runs over a partially-migrated estate; whether it re-emits everything, only the remainder, or dedupes is a property of the migrator. **If the sequences differ that is a real finding about the cutover, not a normalisation problem -- and my gate is what would catch it, which it cannot today because the log is empty.**
2. **NORMALISE WHEN THERE IS SOMETHING TO NORMALISE.** `.events[].id` and `.events[].ts` to ORDINAL POSITION -- `store.rs:1745` orders by `id` and a ULID sorts by time, so position is the log's own order. The other six columns carry no clock and no randomness. **Blocked on rows existing, deliberately, and the refusal is what holds the place.**
3. **RE-RUN AGAINST cc's CHANGESET WHEN THEY COMMIT.** Dirty across `store.rs`, `model.rs`, `views.rs`, `contract.rs`, `facade.rs`, `legacy.rs`, `schema/*` and a new `tests/template_pins.rs`. **I have not inferred what it does.** The gate picks a new subject up without being asked -- twice demonstrated today, not claimed.
4. **BOARD-TOOL PLACEMENT IS OPEN AND IS NOT MINE TO SETTLE.** `board_overlap.py` sits in a single-writer node dir. The natural durable home is `lib/templates/hooks/` beside `whiteboard-clock-guard.sh` -- **but that ships to every Intent consumer on upgrade, which is a fleet decision.** It cannot live at `intent/whiteboard/tools/` either: immediate subdirectories there are NODES and it would be discovered as one.

## Open with others -- LIVE ASKS ONLY

- **cc -- one question outstanding, sent 20:40Z: the event-log row shape.** Nothing else owed either way. They hold the fix for the supersession defect (`426416e1`), and the doctor-wording half is still theirs: the text reads as a violation notice for something vc ruled not a violation, and it cost vc a near-investigation and me a real one -- N=2 in an evening, both pointing at the migrator.
- **dc -- nothing owed either way; they are ACTIVE again on WP-11.** They report their board and the CONTRACT disagreeing about AT-11.5's ratified move condition and are measuring which is real before saying more, which is the right order. Both parity tools (`same_end_state_check.sh`, `self_provenance_check.sh`) remain theirs and untouched by me. **They took the two-halved test and gave back the sharper statement of its answer: the emptied-subject half is answered by a printed POPULATION COUNT and never by a pass line.**
- **vc -- nothing owed either way; paused at fold 9.** They carried the false-green catch to hv as mine, named, and led the escalation with my four-command repro. **Standing correction, twice now in the same direction: the Lamplight run is theirs, not mine.**
- **hv -- AFK, nothing pending.** The blocking defect went up through vc and is fixed. Raise anything new in conversation or via vc, never via the hv inbox, which is durability only.

## Watch-outs

**FOLD 26 CUT THIS SECTION FROM 113 ENTRIES TO 36, AND THE CUT WAS MADE ON EVIDENCE RATHER THAN TASTE.** The old section is in `.history/20260817/wip.md`, verbatim and complete -- a mechanical check proved every one of the 113 is either still here or there before the old text was removed. Three things justified the cut. **(1) A machine found duplicates I could not see: entries 16 and 22 share 21 five-word phrases -- they are the SAME incident written up twice, an hour apart, by an author who could not read his own board.** 9 and 17 are one class in two forms; 82 and 98 likewise. **(2) More than half the section -- 70 entries -- was about the WP-09 surface register, a workstream that has closed**; a caution about `corrected_check.sh` cannot change what I do inside a migration gate. **(3) The test I wrote here myself and then failed twice: could this be a control instead?** The two rules I broke in one session were both already written here in my own words, so neither was a knowledge failure -- both were retrieval failures, and **the cure for a retrieval failure is not another entry.**

So the rule for anything added below: **it earns a line only if no artefact can hold it.** If a script, a test or a refusal can hold it, build that and write nothing. **Today produced eight commits to `interrupt_rig.sh` and not one new line here** -- liveness, the store arm, the store's bound, the asymmetric branch, the cloned subject, and three before them. That is the pattern to keep.

### The class that governs the rest -- an instrument that cannot fail

- **A VACUOUS INSTRUMENT REPORTS EXACTLY LIKE A WORKING ONE, AND IT HANDS YOU THE REASSURING RESULT FIRST.** This is one class with four faces and I have now hit every one, several twice. **(a) IT CANNOT RUN:** the mutation harness for `self_loop_population.rs` restored on relative paths while its helper had changed directory, so the cleanup never fired -- inside the harness built to prove guards run. A cleanup step produces no output when it works, so it is indistinguishable from one that never fired. **(b) IT RUNS AND NOBODY READS IT:** `corrected_check.sh` died at exit 2 the moment I ratified two rows into a field it does not read, and stayed dead through a localfold, a compact, and a board of mine reading "nothing blocked". It behaved perfectly -- refused on its own inability to measure, named both rows, said why. **The output existed and the reader did not**, and that is strictly worse than the silent case because it was shouting. **(c) IT CANNOT FAIL:** a flag-completeness loop went green while 5 of 93 flags rendered nothing; a TSV field shift made a check report MORE coverage and FEWER findings at once, which reads as a better run. **(d) IT PASSES ON AN EMPTIED SUBJECT:** my bucket-free "control" exited 0 having migrated 1 thread of 56 -- it did not fail to execute, it executed perfectly on almost nothing.
- **SO THE TEST HAS TWO HALVES, AND THE SECOND IS THE ONE NOBODY ASKS** (first half dc's generalisation of my `estate_corpus` ROOT find; second half mine, from the emptied control; dc's framing of the answer). **Could this case have FAILED -- and was the subject still there when it passed?** Not "did the branch execute" but "is this case sensitive to the thing under test at all". The reason it survives review is that _the branch executed_ and _the case was sensitive_ are indistinguishable in a passing log. **The second half is answered by a POPULATION COUNT, printed, and never by a pass line** -- dc has hit the emptied-subject shape twice (a `sed` writing a zero-byte mutant, which reds everything and reads as the mutation working spectacularly; a fold whose carry loop stopped early) and **both were caught by a count, never by a verdict.** Corollary that costs nothing: **the default case is the one run most and inspected least**, which is why my ROOT hole survived -- the canary member resolved correctly from a clone AND from the repo, so the one case that ran every time was the one case that could not detect the bug.
- **A RIG'S FAILURE MODE IS A FALSE GREEN, SO BUILD THE REFUSALS FIRST.** Every way `interrupt_rig.sh` can be vacuous now refuses rather than passing: a run that finishes before the threshold interrupted nothing; an exit that is not 137 means the kill never landed; a zero delta leaves nowhere to put a kill; a tree the tool cannot open is compared but not certified. dc's sharpening is the general form -- **a kill that lands too early writes nothing, so the re-run trivially matches and the arm reports IDENTICAL without having interrupted anything.** **21 refusal sites in 907 lines, counted rather than remembered.**

### Inference standing in for reading

- **I ASSERT MECHANISMS I HAVE INFERRED FROM AN ARTEFACT'S EXISTENCE OR ITS OUTPUT, AND THIS IS MY MOST-REPEATED DEFECT BY A DISTANCE -- SIX INSTANCES, AND ONE OF THEM WAS WRITTEN ONTO THIS BOARD TWICE WITHOUT ME NOTICING.** The forms: **from a field existing** -- I told cc that `values` on an arg builds the clap enum; it does not, the spine reads `values` only to expand `subcommand` slots. **From help output** -- I read `wp new --help`, saw no flags, and concluded no scope is entered; `render.rs:574` hardcodes `TShirt::S`. **From a neighbour's grammar** -- I extracted `covers:` with a colon because `status:` sits beside it and has one, and it reported a completely plausible zero. **From a neighbouring comment** -- I read the WP parser's _"so a section the template never named survives"_ as a specification and proposed copying it; it is accurate about what that code conserves and silent about the order it destroys. **From a verified end state** -- I checked the repo was clean after the near-miss and reported that the migrator "refused and wrote nothing"; it wrote 311 files and rolled them back. **From a build's output** -- I designed a `git archive` extract for the gate's subject on the strength of having watched a worktree build stamp a real sha; an extract has no `.git`, so cc's `build.rs` would have embedded `unknown` every run and the provenance cross-check would have returned one answer regardless of input. **Going to the source is not the same as reading the source: `grep` finds the field, only the reader finds what consumes it.** dc caught the last one BEFORE it was built, which is the cheapest place this class has ever been caught.
- **AN INDEPENDENT CHECK IS NOT INDEPENDENT WHEN THE SAME HEAD WROTE BOTH -- and I proved it twice in one round.** My coverage needle missed `"$INTENT"`. I then hand-wrote a second pattern to check my own four findings, **and it missed `"$INTENT"` too**, because the closing quote sits between the variable and the path and both patterns demanded whitespace there. `claude start` came back zero twice, and I only found the seven invocations by grepping the bare literal with no pattern at all. **vc's rule is that the calibrating case is written in one spelling; the corollary is that the checker is written from the same mental list as the needle.** The only honest enumerator of spellings is the corpus. **And the direction is the asymmetry: the blind needle lost nothing and MANUFACTURED three findings, all reading as serious** -- an untested launcher, an untested generator, an untested sync -- with nothing in the output separating them from the one real zero beside them.
- **I WROTE A PEER'S UNCOMMITTED WORKING TREE INTO THE REGISTER AS SHIPPED BEHAVIOUR, AND CITED A LINE NUMBER FOR IT** (vc, who stopped me within the hour and was right to). Told that hv had ruled self-loops legal, I opened `facade.rs`, found `if from == status { return Ok(Outcome::AlreadyThere) }` at `:1151`, verified it "at source", and rewrote three correct ratifications to match. **`AlreadyThere` occurs 7 times in the worktree and ZERO times at HEAD.** All three early returns are `Not Committed Yet`, stamped nine minutes AFTER the ruling they implement. The file moved twice more while I read it. **So the rows I "corrected" described a state that has never existed in any committed tree, and the rows I replaced were right.** The lesson I attached -- _I read the declaration and called it the behaviour_ -- is FALSE for this instance: at HEAD the declaration and the behaviour AGREE, because `permits` refuses an undeclared from-state and there was no gap to fall into. **A wrong lesson on a real correction is worse than the correction, because the lesson is what gets applied next time** -- my own rule from the same morning, and I broke it while writing it down. **The true rule: a row citing a line number is a PROMISE THAT THE LINE EXISTS, and a shared clone is the one place that promise can be false while the file is open in front of you.** It is my citation rule -- a citation naming a file is a promise a file exists -- one level down, at the line. **Going to the source is necessary and does not tell you WHICH source you reached; `git blame` and a HEAD read do.** Rows now state the ruling (committed, citable) and describe HEAD, with the pending change named rather than pre-written.
- **"IT DOES NOT EXIST" IS A CLAIM ABOUT THE FILESYSTEM, SO GO AND LOOK -- AND THE FOSSIL OUTLIVES THE CORRECTION.** git answers questions about HISTORY. Three original instances, the worst being that the false claim became the stated REASON for not fixing two defects. **dc then found the FOSSIL**: `gen_inventory.sh` still said `probes/toplevel.tsv` "has NEVER been tracked" -- tracked since **my own commit recovering it.** **A stale "it does not exist" stops being a gap and becomes a licence.**

### Reading a verdict

- **A TEST RUNNER'S VERDICT IS SCOPED BY WHAT IT RAN AND CROPPED BY HOW YOU READ IT, AND I HAVE GOT BOTH WRONG THREE TIMES.** `cargo test --lib` DOES NOT RUN `tests/*.rs`, and `cargo test` STOPS AT THE FIRST FAILING BINARY -- **I reported "9/9 green" three times off a `--lib` run that never touched the integration tests.** Then I piped two runs through `tail -40` and read "all green" from output whose first lines were the failing lib target. **The two defects compose: a scope you did not notice, cropped at the end that carries the failure.** Name the target, read the head, and quote the summary line rather than the last line.
- **A WRONG ZERO CERTIFIES ABSENCE AND A WRONG MAXIMUM CERTIFIES CATASTROPHE, AND THE SECOND IS FAR MORE PERSUASIVE BECAUSE IT LOOKS LIKE DILIGENCE REWARDED** (dc, via vc's announce; two nodes hit it in opposite directions in one day, both from zsh). vc's probe loop wrote `$BIN $cmd` and zsh does not word-split, so every multi-word row answered `unrecognized subcommand 'st list'` -- **exactly what a surface where nothing is implemented looks like.** dc's `while read -r want path` destroyed `PATH` on the first iteration, `shasum` vanished, and all 27 vendored files reported as diverged -- one step from filing an issue saying the whole tree had been modified. **Nobody re-checks an instrument that has just found something big.** The exposure is inline only -- every parity tool has a bash shebang and is executed -- **which means the hazard lives exactly where we all take our first measurement of anything, and where a result is least likely to have a control beside it.**
- **A PIPELINE'S EXIT CODE IS THE LAST STAGE'S, AND I READ `cargo test | tail` AS CARGO'S TWICE -- WHILE WORKING ON THAT EXACT CLASS.** The harness reported `[exited with code 0]` for a backgrounded `cargo test ... | tail -30`; that zero belonged to `tail`. Both runs happened to be genuinely green, so nothing was misreported -- **which is the whole problem, because a method that cannot distinguish green from red returns green either way and gets trusted on the strength of having been right.** Redirect to a file and read `$?`, or the claim is about the formatter. Note the doubled irony: dc and I spent the same afternoon building a refusal for a runner that trusted one source about a run.
- **A STALE BINARY ARGUES HARDEST EXACTLY WHEN IT IS MOST WRONG** -- a plausible, well-formatted, entirely wrong report, **and what it invents is precisely what somebody just fixed**, so it reads as a regression by the peer who closed it. **MECHANISED**: `surface_check.sh` refuses on a binary older than its inputs. **Six correct firings now.** `find -newer`, never `stat`.
- **READING THE CODE ESTABLISHES NOTHING ABOUT RE-RUNNING IT** (2026-08-17). The accretion was invisible to three people reading the same two functions all afternoon, and one execution found it in ninety seconds. Where a property is about what happens the SECOND time, run it twice.
- **A COMMIT SUBJECT IS A SUMMARY, AND IT MUST CLAIM NO MORE THAN THE BODY SUPPORTS** (dc, 2026-08-17). **`f48ed6e9`'s subject reads "the cutover gate is measured passing" and its body says the gate has NOT been run under the gate's own tool.** Both halves are mine, the body is scrupulous, and the body is the half nobody reads: `git log --oneline`, a changelog, and anything quoted to hv all carry the subject alone. dc caught it by reading `--oneline` while assembling their own fold, and **the only reason they checked the body is that they already had my message** -- anyone arriving next week has the subject and not the message. **It is my own `measured:` argument pointed back at me**: a caveat you only find after you already distrust the headline arrives after it was needed. The word doing the damage is _gate_, because the gate is now a named rostered artefact and "the gate is passing" reads as "that tool returned 0". A pushed commit is not amendable in a shared history, so this line is the correction; the honest subject was "the cutover condition measures IDENTICAL on my own diff".
- **"IT COMPILES" AND "IT IS REACHABLE" ARE TWO CLAIMS** (2026-08-17). My board tracked the first and reported the second, and the gate sat blocked for a day on a compile error that had been fixed. A green build says nothing about whether a door is wired, and nothing about whether either is true of a COMMIT. Name the blocker as a state of the repository, never as an error message.

### The shared clone -- four Claude sessions, one checkout

- **A FAILURE IN A PEER'S FILE MAY BE THE CLOCK, NOT A DEFECT -- RE-RUN BEFORE REPORTING IT.** `cargo clippy` reported an error at `guide.rs:496` while the file was 485 lines long: cargo had read it mid-save. **`warning: build failed, waiting for other jobs to finish` is cargo's LOCK message, not a compile error.** A peer mid-save produces real-looking `error[E...]` output for code that is fine thirty seconds later, and reporting it costs the peer an investigation into nothing.
- **A SWEEP DOES NOT MOVE A FILE, IT SPLITS A CHANGE -- AND THE INDEX IS SHARED.** My unqualified `--amend` took cc's test without the `store.rs` methods it calls and **HEAD did not build for ten minutes.** The same shape from the other side: **`git commit --only` SILENTLY SKIPS AN UNTRACKED PATH**, so a fold of mine committed the EMPTIED inboxes and left the new `.history/<date>/` behind -- **HEAD carried six messages deleted with nothing standing behind them.** `git add` the untracked path first, name paths on the amend too, and verify the archive with `git diff --numstat` on the COMMIT rather than on the tree.
- **A GREEN IN A SHARED CLONE MAY BE GREEN AGAINST A PEER'S UNCOMMITTED TREE** (2026-08-17). Four sessions build one checkout. My seventh test went green because cc's `export.rs` fix was sitting in the working tree unstaged -- at HEAD it was still red. `git status` before reading any pass as a fact about the commit, and say which tree a number was measured on. cc's board carries the same scar from the other side.
- **`--only` PROTECTS THE COMMIT AND NOT THE AMEND**, and never CLEARS the index, so the shared index accumulates across every node. **Name paths on the amend too.** Corollary that has now cost me at folds 6, 7 and 8: **`sed -i` by LINE NUMBER shifts under its own edits, and an Edit anchored on a non-unique prefix duplicates the text after it** -- I produced "vc ruled DO NOT SPLIT: vc ruled DO NOT SPLIT:" this way. **Anchor on unique text and check the result.**
- **`index.lock` HELD BY A PEER IS NORMAL IN A FOUR-SESSION CLONE, NOT AN ERROR.** `git commit` fails with _"Another git process seems to be running"_. **Retry in a loop; never remove the lock file** -- it belongs to a live operation.
- **Read `bin/**` and `tests/**`, never mutate them** -- two symlinks point at `bin/intent`, the BATS estate defaults to `INTENT_BIN=bin/intent`, and four sessions are live. `native/**` and `bin/.devbin/**` are safe.

### Live to WP-10 -- the cutover gate

- **CONSERVATION, DETERMINISM AND LIVENESS ARE THREE PROPERTIES, AND EVERY ONE OF US VERIFIED TWO AND INFERRED THE THIRD.** The gate compares a clean migration against an interrupted-then-re-run one: that is DETERMINISM. That both arms preserved the estate is CONSERVATION. **That the migrated estate can be OPENED BY THE TOOL THAT PRODUCED IT is LIVENESS, and it is the one that was missing** -- comparing two trees neither of which the tool can read is a true statement about bytes and a false impression of a working cutover, and this rig produced exactly that result and it read as a pass. cc's version from their lane: they built the door, tested that it wrote the right bytes, and never opened the thing it produced with the tool that produced it. All three arms are now in the rig; **the liveness arm refuses at exit 2 rather than failing at exit 1, because exit 1 would assert a non-idempotence the measurement does not support.**
- **A GATE'S SUBJECT MUST EXCLUDE WHAT THE SYSTEM ITSELF CALLS REBUILDABLE, AND THEN BOUND WHAT IS LEFT.** The comparator counted `intent/.cache/intent.db`, which the migration writes into `.gitignore` itself. The store is now compared through `intent export --format json` -- byte-equal across both arms -- and the arm SAYS what it covers: **"the store as `export` sees it", with `file_index` / `doc_sections` / `snapshots` named as out**, because those derive from the working tree. **An unbounded green is a claim about everything.** dc's unclassified third case, worth a line rather than an arm: a provenance marker that exists, is well-formed, and is WRONG -- a bare sha naming a commit that is not the one built. Nothing in the embed can produce that today, **which is why the wrong instinct, if it ever appears, will be to debug the checker.**
- **AN INSTRUMENT'S LOGIC BELONGS TO THE PINNED REVISION; ITS VIEW OF WHERE REPOSITORIES LIVE ON THIS MACHINE DOES NOT.** The gate's subject is now a cloned revision (`--rev`, default HEAD; `--instruments-rev` when they differ), so **a dirty worktree stopped mattering** -- the old refusal fired four times in one day and I hand-built extracts around it four times. But `estate_corpus.sh` run from that clone resolved three of four members to `no-repo`, because the corpus needs to know where the real repositories are. `ROOT` is passed explicitly for exactly that reason.
- **DISCOVERY WALKS UP, SO A TEST TREE MUST CARRY ITS OWN MARKER** (2026-08-17). `Project::discover` stops at the first ancestor holding `intent/.config/config.json`. Running the migrator from inside `native/rust` reached the LIVE checkout and wrote 311 files before unwinding them. Assert the marker in the tree and refuse a workdir inside the repository -- cc's point being that a wrongly-pointed arm tends to SUCCEED rather than fail.
- **"IT BLOCKS NOTHING" IS THE WRONG TEST FOR PARKING A QUESTION** (2026-08-17). The event-log timestamp question sat at item 4 all day and was the cause of the gate's only failure, while three nodes chased the symptom from outside. **The better test is whether you can say what would happen if the answer went either way.** If you cannot, you do not yet know enough to park it.
- **A VOID PREMISE BESIDE A LIVE CONCLUSION IS THE WORST SHAPE FOR SOMETHING NOBODY MAY CITE** (2026-08-17). I cited D29's struck derivation; vc cited it an hour later; cc rested a committed doc comment on the adjacent dead D36 having personally found the same dead reasoning four days earlier. `design.md:243` is 1400 words in which VOID appears once, mid-paragraph. Nobody skimmed. **Before citing canon in an estate with rulings that reverse, grep the entry for VOID / REVERSED / do-not-cite rather than reading to the conclusion.**
- **D42, SETTLED: it is a rule about SIGNATURES and SOURCE DOCUMENTS. I over-applied it TWICE IN ONE DAY, both times towards MORE PROHIBITION, which looks like rigour.** Forbidden: confecting a time into a source document, and no CLI or `intentsvcs` function TAKES a time as a parameter. Permitted, all hv-ruled: RETURNING a time; reading a clock to make a DECISION; stamping when a command ran into a GENERATED artefact. hv: _"there is no need to be pathological about it."_ **All three of my D42 findings fell.**

### Standing constraints -- operational, not judgement

- **DO NOT PUT v3 ON `PATH`. THIS SESSION IS ALIVE ONLY BECAUSE `intent` STILL RESOLVES TO v2** (dc, 2026-08-16, measured). `claude` is unimplemented as a FAMILY, so v3 refuses before it looks at project state -- `intent claude hook require-in-session` exits 2 in an unmigrated v2 project AND outside any project at all. **The door is `brew install`, not migration**, and it puts v3 at PATH position 1 without asking. **The moment v3 is on the PATH of a shell a Claude Code session runs in, that session stops accepting prompts and cannot be recovered from inside itself.** Confirmed live by vc across five arms against Claude Code 2.1.233. Second hard publication hold, beside 0036.
- **NO PUSHES TO `upstream` -- hv, 2026-08-16. The CI/CD budget has been hit.** `git push local main` only; never `git push upstream main`, never a bare `git push`. **Standing, not a one-session pause.** Last commit of mine that reached GitHub: `dcf9d3b6`. **dc has since re-installed this clone's hooks so `git push upstream` is REFUSED mechanically** -- belt and braces, and the mechanism is now the authority rather than my memory.
- **ic cannot certify a green suite.** matts owns the authoritative run; everything here is evidence. **Confirmed green by hv 2026-08-16 19:32Z, the full Rust suite, after cc's 0038/0039 work and my table changes had all landed** -- so the fold-9 state is verified by the only run that counts, not by my own `cargo test`.
- **This repo is PUBLIC and that is FINE and intended** (hv ruled). No secrets, and a bare commit in a shared tree publishes whatever is sitting in it.
- **hv STEER, STANDING: _"The sooner we can get this project onto v3, the better."_** Said to ic 2026-08-16 11:35Z and **relayed verbatim to cc, vc and dc** because hv was AFK and no peer could hear it. Scope is NOT cut: _"We will get all of this done -- including the text search and code parsing -- and then push 3."_ **Read it as SEQUENCING, not quality**, and vc drew the boundary correctly: **prefer-closing-over-perfecting is a real trade at a day of work and a BAD one at a single line.** It is not a licence to skip verification -- the day it arrived is the day six silent-green defects were found.
- **This shell is zsh**: no word-splitting of unquoted parameters; an unmatched glob is an ERROR that aborts the command, not an empty expansion. Never enumerate remotes through `head`. **INSTANCE 2026-08-17, with this rule already on this board: a probe loop doing `"$B" $a` produced six clean rows saying `intent --version` refuses unknown arguments -- the exact opposite of the truth.** `--version --zzz` arrived as ONE argument and clap rejected the whole token. **The giveaway was in the output the whole time: the error quoted both words inside one pair of quotes.** Probe from a bash file with real argument vectors for anything whose result gets written down.
- **`read -r a b c d` WITH `IFS=$'\t'` COLLAPSES AN EMPTY FIELD** -- bash and zsh alike, because tab is an IFS WHITESPACE character. Every column after shifts. **Emit a `-` placeholder, never an empty field**, or return the parts separately. **SECOND INSTANCE, 2026-08-17, WITH THIS RULE ALREADY ON THIS BOARD: `ratified_in_check.sh` returned `resolving<TAB>dangling`, the empty first field collapsed, and every DANGLING issue number landed in the RESOLVING variable -- so the fix for a false green WAS a false green, in the check whose whole subject is false greens.** Predicted 5, observed 0, and 0 was what the broken code produced. **And I had written `OBSERVED: exact` into the mutation-proof block before running a single proof**: a prediction written first is discipline, a result written first is fabrication, and in the diff they are identical.
- **`path` IS A SPECIAL ARRAY IN zsh, TIED TO `$PATH`, AND ASSIGNING TO IT DESTROYS THE SHELL.** An audit loop of mine did `read -r path disp arity flagjson`, setting `PATH` to `st new`; every subshell after printed `command not found: jq` and reported all 60 rows unparseable. **The bash script it was auditing uses `path` too and is FINE.** **When an ad-hoc harness reports a wall of findings, suspect the harness first.**
- **I FABRICATED A TIMESTAMP AND THE GUARD CAUGHT IT.** Bumping a heartbeat inside a batched edit, I typed a plausible value wrong by nine minutes, read off nothing. **The tell is the BATCHING**: every stamp I read in its own step was right. **`date -u` is its own step, always.**
