---
node: ic
name: Interface Claude
role: interface
session_id: 6bbf2186-4635-4ce4-8bd0-02c75f289528
heartbeat_at: 2026-08-26 22:44Z
status: active
focus: "**`c14aa9bf` IS IN: `intent todo done --prune` now writes `## DONE:2026-08-26T21:40:25Z` and CLEARS THE WORK FINISHED TODAY.** hv ruled it at 21:40Z, parity with `bin/intent_todo`. **The date truncation in `event::todo_watermark` was the defect and the heading was the symptom** -- a completion recorded today compared EQUAL to a flush run today, so `--prune` on a day of work cleared nothing. Three new arms, each mutation-proven to red ALONE. **Verified in a WORKTREE at HEAD, because the shared tree is carrying a peer's in-flight backup-config change that does not compile:** workspace check clean, `cargo test --workspace` 1168 passed / 2 failed. **THE TWO REDS ARE THE SAME TWO AS BEFORE AND ARE CONSTANT ACROSS ALL THREE MUTATIONS -- they ask whether a flush survives a GIT CLONE, and under D53 it does not. That is hv's ruling to make, and it is the one thing I am waiting on.** Holding.
claims: [ST0057/02, ST0057/05, ST0057/07, ST0057/08, ST0057/11, ST0061]
---

# Interface Claude (ic)

## DOING

**`c14aa9bf` DELIVERED hv's 21:40Z ASK.** `event::todo_watermark` returns the full instant at second resolution (`2026-08-26T21:40:25.123Z`->`...25Z`, fraction dropped, not the time); `views::completed_instant` widens a `completed:` DATE to that day's `T00:00:00Z` before the lexical compare, which is v2's `normalize_completed`; the heading is `## DONE:<T>`; `render.rs`'s same-day note is replaced by the one still true.

**MUTATION-PROVEN, EACH REDDING A DIFFERENT ARM SET.** Restore the truncation -> the two integration arms red and NEITHER unit test. Remove the midnight widening -> ONLY the midnight arm reds, which is what says that arm is the only thing holding it. Restore the old heading -> only the heading arm. **The midnight case is the whole reason the widening is a mechanism and not decoration**: a bare date already sorts below a mid-day instant by accident, and the accident INVERTS at exactly `T00:00:00Z`, which is v2's zero-flush baseline.

**BLOCKED ON ONE RULING, AND IT IS THE ONLY THING I OWE hv AN ANSWER FROM.** `a_flush_survives_a_machine_that_has_no_database` and `doctor_does_not_report_a_flushed_view_as_hand_edited` both ask: does a flush survive a git clone? **Under D53 it does not** -- `f42987c7` took `events.jsonl` out of the working tree, so a clone carries no log and no watermark. Two answers, both defensible: (a) it does not travel, and the two tests assert something the architecture declines to provide, so they are rewritten to the real contract; (b) it travels via the committed `todo.md` heading -- now v2's exact shape -- read as a FALLBACK when there is no store, the log staying the writer. **(b) costs one thing and it must be said out loud: on a store-less machine `doctor` could no longer catch a hand-edited watermark, because it would be reading the value it is checking.** What (a) costs is that every fresh clone of every project that has ever flushed reports `todo.md` as hand-edited, permanently.

**hv DROVE IT AND IT WORKS:** `ok: DONE watermark advanced to 2026-08-26T22:27:46Z, 0 item(s) cleared`, and `intent/todo.md` now reads `## DONE:2026-08-26T22:27:46Z` -- committed as `7211cf5c`. The zero is right rather than a no-op: hv's earlier prune had already taken the 54.

**THREE BINARIES ON THIS MACHINE, THREE VINTAGES, AND ONLY ONE HAS TONIGHT'S CODE.** `intent` on PATH is a Homebrew symlink into `Cellar/3.0.0_1`, marker `80d8b2ca`, **mtime 15:03Z -- seven hours old**; `native/rust/target/release/intent` is `a1af59f3`, mtime 22:11Z, still behind `c14aa9bf`; `bin/int` is the dev wrapper and is what hv ran. **A stale `intent` never announces itself -- it answers every question fluently, about a tree from this afternoon.** I rebuilt nothing into the shared path: the guard refuses while `native/rust` is dirty, so it would have REDIRECTED to a private dir at rc 0 and I would have reported a build that never reached `target/release`.

## TODO

**THE THREE REDS, ALL ONE FIX.** `a_flush_survives_a_machine_that_has_no_database`, `doctor_does_not_report_a_flushed_view_as_hand_edited`, `the_structured_buckets_and_the_rendered_view_say_the_same_thing`. I dropped the dual-source read because D53 moved `events.jsonl` out of the working tree, and **wrote that down as an accepted limit in a code comment**. `export.rs` still pushes `event::JSONL` into the bundle, so the log travels and a flush must survive a clone with no database. **The comment asserted a limit the tests disprove -- the same shape as the `Plan::run` comment earlier the same day.** Follow-up commit, never a rewrite.

**OWED, in vc's order:** AC-11.3's proof test (the one mutation that moves all three callers of `default_declaration`); AC-11.6's arm (realise + dehydrate under the preconditions, WP-13's ingest/prune clause reported NOT ATTEMPTED); AC-11.3's migration clause; ST0061 AC-00.1's hydrate/dehydrate/hydrate round trip; the `organize --apply` exit-code pair in `exit_codes.rs`; `flag_reachability.rs` chaining `new_surface`.

## Watch-outs

**A BACKTICK IS NOT AN APOSTROPHE, THE MARKDOWN FORMATTER IS THE SECOND WRITER THAT COLLECTS THE BILL, AND MY FIRST CHECK FOR IT COULD NOT SEE THE STATE IT WAS LOOKING FOR.** A backtick standing in for an apostrophe makes its line carry an odd number of backticks; the formatter that runs at commit mispairs every inline code span after it and DELETES THE SPACES BESIDE THEM, so prose reads as a typo rather than as corruption and nobody traces it back. I had ten and fixed them. **Then dc found the two things that matter more, and my own board proved dc right twice over.** (1) THE ODD-COUNT CHECK SEES THE CAUSE, NEVER THE AFTERMATH: once the spaces are eaten and the spans re-pair, the count is EVEN again, so a green from it is not a clean board -- it reports the about-to-break state only. The damage itself is a letter, a backtick, a letter, with the contraction forms excluded. **My board returned odd=0 and carried four live damage sites on two lines**, repaired here. (2) THERE IS A SECOND, INDEPENDENT CAUSE NEITHER CHECK SEES: an escaped backtick inside an inline span. It keeps the count even, and the formatter re-eats the spaces on EVERY commit. **dc repaired fifteen sites, read the file back correct, committed -- and the damage landed anyway; only reading the committed BLOB caught it.** So: verify a board repair out of the blob, never out of the working tree, because the formatter writes between the two. dc owns the guard arm and it wants both causes, on ADDED lines only. **(3) dc's generalisation, and it is the one worth keeping longest: EVERY SPECIMEN OF THIS DEFECT IS UNQUOTABLE IN THE MEDIUM THAT CARRIES THE REPORT.** Three times now the act of writing the defect down has reproduced it in the sentence doing the writing -- dc's check written as a code span, dc quoting my site, and my own measurement that the damage grep scores one real hit in five on a board that documents the defect, with all four false positives being the paragraph explaining the bug. **That is a much stronger argument for the guard skipping quoted spans than telling authors to be careful, and it generalises past backticks: whenever the reporting surface cannot represent the thing being reported, the report is evidence about the surface as well as about the bug.** Verified from the committed blobs at `4f7f47ad`: all five boards odd=0, escaped=0, and every raw damage hit on the board is that same documenting paragraph. Zero real damage anywhere.

**ONE CLASS ATE THE DAY, AND EVERY OTHER ENTRY HERE IS A FACE OF IT: THE SCOPE IS CHOSEN BY A FLAG AND REPORTED BY NOTHING.** cc's formulation, which I take as the general one: **the tree is an input to the run, and an input nobody names is an assumption.** Faces measured on 2026-08-26, mine unless marked:

- `cargo test -p X --test Y` runs ONE integration binary and never `src/**`'s `mod tests`.
- `cargo check -p X` builds the **lib target only** -- never test targets. Reported `facade.rs` as "written and compiling" over a workspace that did not compile; **cc found it, not me.** I already held the `--test` rule and **matched it on the SYNTAX rather than the MECHANISM**, because `check -p` is spelled nothing like `--test`.
- `cargo check -p X` again, without `--all-targets`, missed three errors in `views.rs`'s own `mod tests`. **Same mistake, four hours after writing the note about it.**
- `--all` WIDENS into other people's uncommitted work. `cargo fmt --all` over a shared checkout silently invalidates every anchored edit a peer has in flight (cc: `matched 2` is the one that ships).
- **The shared working tree narrows by WHOSE FILES ARE IN IT.** Two numbers quoted today over a tree carrying cc's uncommitted work; both named, neither should have been taken.
- `cmd | head; echo "RC=$?"` reads **head's** exit code. Read RC=0 off a gate exiting 1. Use `$pipestatus[1]`, or no pipe.
- A `| tail` pipeline writes **0 bytes until it exits**, so "not started", "still running" and "finished silently" are one observation.
- `grep -c` **exits 1 on zero matches**, so a task ending in one reports FAILED having found nothing wrong.
- `grep -c '^error'` also matches `error: test failed` (cc). **`binaries=` is the discriminator: a compile stop and a clean suite differ by that count and by nothing else.**
- **A green from a run that predates your last edit is not a green.**
- An `echo` that ASSERTS a condition it never checked -- I printed `[shared tree clean = valid HEAD control]` unconditionally over a dirty tree.

**THE PROSE VERSION, AND IT COST ME TWICE IN ONE DAY: A COMMENT IS NOT A MECHANISM.** `Plan::run` returns `Ok` with its refusals INSIDE the report; the comment above the line that ignored them asserted the opposite, written from the SHAPE OF THE CALL without driving it. Then the `events.jsonl` limit, asserted in a comment and disproved by the tests I restored an hour later. **A written guarantee standing in for a mechanism.**

**A CONTROL THAT CANNOT GO RED IS NOT A CONTROL (IN-AG-RED-CONTROL-001), AND TWO OF MY OWN SIX B1 TESTS WERE THAT.** vc's mutations found one, reading my own fixture found the other. `force_without_a_tty_...` asserted an EXIT CODE that two different refusals share -- delete the tty guard and the empty-answer refusal produces the same 1. `default_removes_no_file_...` asserted survival across a run that **never acted**, because `init` writes the manifest and `st new` adds to it, so `--default` took the present-manifest no-op arm. **Both assert the OUTCOME; outcomes are reachable by more than one path. Assert the CAUSE.**

**A HOLD STATED BY ITS SCOPE INVITES COMPLIANCE THAT DEFEATS ITS PURPOSE.** vc's QUIET hold said "nothing under `native/rust`"; my whiteboard commit obeyed that and moved HEAD, which is what `verify_pair` actually reads, so dc refused a correct build. **State the mechanism and the scope follows; state the scope and the mechanism is left to be guessed.**

**A CLEAN-TREE WORD IS A HOLD ON MY OWN TREE UNTIL I SAY OTHERWISE.** Twice today I told vc the tree was clean and dirtied it within two minutes without a follow-up. The second time cost a QUIET call.

**SHARED-CHECKOUT MECHANICS.** `git commit --only` is PATH-scoped, not hunk-scoped -- committing a path a peer is also editing takes their work under your name (avoided on `facade.rs`, twice). `git checkout -- <file>` in a tree with uncommitted work is a REVERT, not a restore (cc, who lost a mutation to it): **copy aside, copy back.** `.git/index.lock` means a peer is mid-commit; wait, never clear it. **`git show HEAD` after a failed commit shows someone else's commit and reads exactly like success.** And a shared file every peer's gate reads live -- `surface/dispatch-table.json` -- must be **built in memory, parsed, then written**: the window between a bad write and the check that finds it is an outage for everyone. I broke vc's commit that way.

**A MECHANISM COMPLETE AND UNREACHABLE, THREE INSTANCES IN ONE EVENING.** cc's `Attachment::opaque` (a filename allowlist classified `.pdf` out one step earlier); `views::write_all`'s mtime guard (recorded in `organize.rs`'s own docstring); and **mine -- `intentfiles::default_declaration`, landed in `baba2d9d` and reported as done with ZERO production callers.** **A unit test that calls a function directly proves the function works and says nothing about whether anything calls it -- and the coverage is what makes the missing caller invisible.**

**DEFINITION BY EXCLUSION ACQUIRES MEMBERS BY ACCIDENT.** `!is_closed()` swept Triage, Not Started and Hold into the realised set, which is how a project realised 57 threads. hv: _"It should ONLY HAVE WIP STs"_. Stated positively (`status == Wip`) a new status is not realised until somebody decides it is.

**READING THE VOCABULARY AND CALLING IT THE GRAMMAR** (cc's name for cc's own error). A correct reading of a DEFINITION standing in for an UNREAD control flow. I did the same thing reading `state.is` off canon and calling it a verdict when `ac gate` answers the opposite.

**NOTHING BUT `date -u` IS A CLOCK** -- not `git log` (local time), not a session notice (crossed midnight once, so the error was a DAY not an hour), not file mtimes.

**A POSITIVE CONTROL THAT WOULD ALSO PASS UNDER THE BROKEN INSTRUMENT IS DECORATION.** And a stand-in built to audit another check must be shown to ask that check's QUESTION -- mine searched a bare string where the gate searches the quoted id, and "found" a defect that was a ruling of vc's.

## Decisions

- **(hv, 2026-08-26 21:40Z) The DONE heading is `## DONE:<T>` and <T> is a full ISO 8601 UTC instant** -- the time the prune ran, hence the cutoff. Verbatim v2 (`DONE_HEADING_PREFIX`). **Supersedes my own 2026-08-16 note that v3 "writes it for a person instead"**: v3 still never PARSES the line, so the shape was owed to a reader either way, and a reader who has seen a v2 `todo.md` should not have to learn a second one.
- **(ic, 2026-08-26, following from hv's ruling) A flush CLEARS same-day work, and the note saying it could not is gone.** v2 widens a `completed:` date to that day's midnight, which is below any flush later that day. The day-granular watermark that avoided this is what made a DONE bucket unemptiable on the day the work was done.

- **(hv, 2026-08-26, first-hand) `--default` never removes a file -- "unless it is used with --force, which does remove files, after a confirm."** Resolves AC-11.4 vs the WIDE ruling: not a contradiction, a missing qualifier. The confirm was already ratified in AC-11.2 (no tty -> writes nothing, exits non-zero; `y` on a tty proceeds). No flag, no env override: an escape hatch does not weaken that criterion, it removes the only thing it measures.
- **(hv, 2026-08-26) The realised set is WIP ALONE.**
- **(hv, 2026-08-26) `todo.md` shows DOING and TODO. DONE is bounded by a watermark that `--prune`/`--flush` advances.** PARITY with `bin/intent_todo`, explicitly: _"No hallucinated new or less functionality."_ **Two things I invented first and hv rejected both** -- collapsing finished WPs under DOING to `(7 of 10 done)`, and a "N completed items not shown" footer. Neither is v2 behaviour.
- **(hv, 2026-08-26) THERE IS NO 3.0.2.** Everything goes into 3.0.1.
- **(ic + vc, 2026-08-26) Enumeration for dehydration is the CORPUS SCAN** (`sync::scan`, excluding only `Ignored::for_root`), never a recursive `read_dir` -- which would see every `.DS_Store` and refuse dehydration for any thread dir opened in Finder. **A directory the run cannot empty is NAMED in the verdict**: git leaves ignored files behind too and says nothing, because git keeps no manifest to contradict, and we do.
- **(ic, 2026-08-26) The watermark is an EVENT, not a line in the generated file.** v2 grepped the flush instant back out of `todo.md`, so deleting a disposable file resurrected everything ever flushed. As an event it survives `rm intent.db`, merges across machines, and keeps every flush.
- **(ic, 2026-08-26) `todo.window_hours` is REMOVED.** With the watermark back it was a second answer to one question, and the estate forced it: `UnhonourableWindow` became unprovokable and a genuinely unreachable variant must not be exempted. `TodoConfig` still parses the key so no project's config breaks; nothing reads it.
- **(ic, 2026-08-26) A restore of a four-month-old commit is a MERGE, not a revert.** The pre-D44 renderer emitted work packages as flat label strings; the nesting landed after D44, so reverse-applying whole would have restored the watermark and regressed the thing hv was asking for.
