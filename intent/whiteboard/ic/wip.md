---
node: ic
name: Interface Claude
role: interface
session_id: 11cef60b-409e-4bcc-b0f5-808d43639e75
heartbeat_at: 2026-08-31 12:14Z
status: active
focus: "Eleventh fold, pre-compact on hv's localfold+compact+bounce call. WP-09 essentially CLOSED this session -- AC-09.1, AC-00.4, AC-09.4, AC-09.5, AC-09.3 all satisfied (gate 107/135). Then hv REVERSED ST0064 into 3.0.1: the macOS menubar app is mine, an M PORT from Gtools (~2470 LOC), NOT L/XL. WP-01 is the plan (rescoped M); the six I split it into are cancelled (b99e67bb) because the criteria bind to WP-01 by their AC-01.N prefix. On the bounce: port ST0064 (WP-01 + int macos, refusal arms first), and take what vc routes -- AC-09.6 when dc's table clears, the URI-resolver work when hv rules (now release-blocking for TWO threads)."
claims: [ST0065, ST0056/09, ST0056/17, ST0064]
---

# Interface Claude (ic)

**Pre-fold verbatim at `.history/20260831/wip-fold-1213Z.md` (eleventh fold). Cold-session minimum: state, not story. RE-MEASURE EVERY FIGURE AT PICKUP -- five nodes write this tree.**

## DOING

**NOTHING IN FLIGHT. Tree clean of me.** WP-09 closed this session, gate ST0056 **107/135**: `5bd42c95` AT-06.8's wiredness probe asks the ENTRY not the family (the `st bootstrap` trio defers, the mirror error surfaced two inert flags 0180/0181 at `3fecb18c`); `ae8f2983` AT-09.4 re-cited + green -> **AC-09.4**; `2b5c4ba0` mcp_surface.rs (published `tools/list` == generated roster) -> **AC-09.1 + AC-00.4**; **AC-09.5** in three (`257d0a4f` design artefact, `451b4d7f` the render seam = one `crate::show` renderer so contents==CLI read BY CONSTRUCTION, `2d05aa94` the resources primitive) + `f3a62df6` the resolver fix (my URIs hand-spelled the `intent://` scheme -- cc's cross-crate guard reded it) + `f9bad687` the design-doc URI correction; `ea1d0306` AT-09.3 bridge-survives-restart -> **AC-09.3**. `b99e67bb` ST0064 WP structure fixed.

## TODO

1. **ST0064 -- THE MENUBAR APP, NOW MY PRIMARY WORK (hv reversed it into 3.0.1).** An M PORT of Gtools/Geodica's `~/Devel/prj/Gtools/native/macos/Geodica/` (~2470 LOC, the right size AND shape ref). Conflab is the wrong size ref (41k, mostly vendored) but the RIGHT ref for the build verb. **WP-01 is the plan; port it. On the bounce: WP-01 (CLI shell-out + login-shell from Gtools) + the `int macos` port from Conflab's `bin/.devbin/cmd/macos` (125 lines + macos.d/), refusal arms FIRST (doctor/verify must refuse with no creds loaded -- an unsigned local build-pkg is the most anyone can drive).** THREE dependencies, none the port removes: **AC-01.2/06** cc widening the daemon predicate to three states (live/stale/absent) + a machine-readable face -- `daemon status --format json` is INERT and `daemon::route` is TWO states (cc's WP-08); **AC-01.5** the URI-resolver work (below); **AC-01.7** hv's ADC at a keyboard for the notarise arm. The genuinely-NEW piece is WP-01's `intent://` handler (neither estate has our addressing). design-menubar-app.md beside the thread is the handoff.
2. **THE URI-UNIFORMITY WORK -- co-designed with cc, HELD FOR hv's RULING, now release-blocking for TWO threads** (hv's bug + ST0064 AC-01.5). Three resolvers exist; only `promote` (address.rs) accepts the `intent://` scheme; `st show` uses `thread_spec` (bare-id, splits on `/` so `intent://` becomes `intent:`), `explore` uses `nav::land` (nav SINGULAR grammar). hv rules: (1) `intent://` PLURAL address is user-facing, nav singular goes internal; (2) strict + EMIT (cc measured NOTHING emits a canonical address, `--json` included); (3) cut or follow-on. Shape: ~40 sites through promote-THEN-NARROW-to-kind + a round-trip property test + a ratchet. **SPLIT: cc takes render.rs's narrow-door sites + the helper; I take explore + the address->view resolution + the grammar surface + the EMIT side (--json + refusal message).**
3. **AC-09.6** -- the one exposed=>servable flip. BLOCKED on dc's `dispatch-table.json` (their 0180 withdrawal in flight); take `git status` clean on it as the signal, build the flip from `git show HEAD:`. A 15-min interrupt, not active work.
4. **AC-06.8** -- instrument fixed, row RED by vc's note (two open scope decisions: 0180 `agents init --template` WITHDRAWN by dc; 0181 `llm usage_rules --symlink` mine, same single question -- did the capability move or was it never built; v2's --symlink LINKED, so it may be hv's not a withdraw). Not mine to close.
5. **Parity-tool hardening, unblocked, mine, sequenced (harmless-today wrong, not tag-blocking):** conservation_check.sh's `refiled` partition + the `UNACCOUNTED` 5-way split consuming cc's `intentsvcs::sync::NOT_CARRIED`/`NOT_YET_BUILT` consts (b3383f8c, key on `at` prefix, surface `owed_by`); gen_dispatch_table.sh's rotted mutation-proof (doctor gained 3 keep flags at cb78080d -- re-derive to a zero-keep-flag row).
6. **Standing:** 0142 guide.rs write (mine); TUI remainder (AC-17.1/17.6, hv drives); ST0065 (hv takes with me, out of cut); scratchpad/wt-tui worktree removal.

## Watch-outs -- mechanisms only

1. **A CROSS-CRATE GUARD IS INVISIBLE TO `cargo test -p <the-crate-you-changed>`** -- my resource code broke `intentsvcs`'s `no_second_resolver_exists` and `cargo test -p intent-cli` never showed it. **Run `cargo test -p intentsvcs` before any address-touching landing**; general form: a change's blast radius is not the crate it lives in, and the natural test selection is the one that cannot see it (cc hit the mirror from the other side).
2. **`git add $P` WITH A SPACE-JOINED VAR DOES NOT WORD-SPLIT UNDER zsh** -- the whole string becomes ONE pathspec, matches nothing, and the commit lands NOTHING while a peer's commit sits at HEAD. First time the zsh trap produced an empty ACTION not a wrong number. **List paths explicitly; cc's post-verify (`git show --name-only` == your intended set) is what catches it.**
3. **cc's POST-VERIFY IS STANDING NOW: after every commit, `git show --name-only HEAD` must EQUAL your intended path set** -- never your staged list (structurally cannot see a path you did not name). Verify the population the commit HAD, not the one you expected.
4. **THE `intent://` SCHEME IS `address.rs`'s -- PLURAL (`intent:///threads/ST0056`, 4-digit issue ids), routed through `address::parse`/`Entity`. The SINGULAR `/thread/` is `nav.rs`'s TUI/web path -- a DIFFERENT single-home grammar.** Do not hand-spell the scheme or conflate them; the guard reds it.
5. **A DELETE OR CREATE HAS THREE POPULATIONS AND THE SECOND IS THE ONE EVERYBODY SKIPS: what EXECUTES this, what CITES this, what CHECKS this.** I cancelled/created ST0064 WPs having checked the WP list and NOT the criterion binding -- the criteria bind to WP-01 by their AC-01.N id prefix, so WP-01 owns the whole acceptance boundary. Cancelling it would have orphaned all nine.
6. **AN ATTACHMENT RE-ATTACH MUST CARRY THE FINAL BYTES: prettier the .md BEFORE the last `st attach`**, or the store holds pre-prettier bytes while disk holds prettier'd (attachment-drift `intent doctor` names). And a `sync --to-disk` REALISES a peer's pending attachment into YOUR extract -- splice their leaf back to HEAD with jq (`git show HEAD:` base), restore the file, verify canon-sha==disk-hash for every attachment you carry.
7. **THE STORE IS SHARED BEFORE THE TREE IS** -- an `st attach`/`wp new`/`at green` you have not committed realises into every node's canon on their next sync, and `canon_commit_check` makes whoever commits canon carry your file. Land in the same breath; a path-scoped `--only` commit excludes peers' dirty files but the STORE already has your writes.
8. **RealDaemon REFUSES A STALE SIBLING intentd; `cargo test -p intent-cli` does NOT build intentd.** `cargo build -p intentd` before any daemon-backed run, AND a peer's uncommitted `facade.rs` can make your just-built intentd stale to the test's freshly-built CLI -- rebuild intentd immediately before the run.
9. **ABSOLUTE PATHS: the Bash cwd PERSISTS across calls.** zsh: unquoted `$var` does NOT word-split; an unmatched glob aborts the call; `$PIPESTATUS` is bash; a backtick in a double-quoted string runs a command. jq `//` swallows false.
10. **MARKDOWN PRETTIER-FORMATTED BEFORE `git add`** (gate refuses, rewrites nothing). **A WORKED EXAMPLE IN A REMEDY IS SHIPPED SURFACE** (`ST0000`, not `ST0001`). **A FLAG NOT IN THE USAGE LINE READS AS A FINDING.**
11. **REACH IS NOT A DOOR** -- serving match + two-sided gate (60 tools). The dispatch-table generator keeps LIVE CENSUSES in prose (`status` count, `legal_pairs[].n`, `recoverability_anomaly`) and refuses on each. `INTENT_BLESS=1` regenerates `schema/`.

## Decisions

- **2026-08-31 hv REVERSED ST0064 INTO 3.0.1** (via vc, verbatim: _"The intentd and the menubar app that controls it are linked and need to both land for 3.0.1... We need both."_). Supersedes D3's release-scope half; the WP-08/ST0064 SPLIT survives (intentd ships, ST0056 WP-08 owns it; the app is mine). **It is an M PORT, NOT L/XL** (hv: Gtools built in a few hours). A8 (`cmd/macos` OUT) REVERSED -- the signing path is IN.
- **2026-08-31 vc: ST0064's CRITERIA BIND TO WP-01 BY THEIR AC-01.N ID PREFIX** -- WP-01 owns the whole acceptance boundary, per-row dependencies live on the criteria, a WP layer above is a second home. My WP-02..07 cancelled; WP-01 rescoped L->M.
- **2026-08-31 vc bounded AC-09.5 (A): resources serve entity docs through EXISTING facade doors, ZERO new facade surface**; the render seam (one `crate::show`) makes contents==CLI read by construction, which is better than the criterion asked. wip.md/boards are a CLI-gap follow-on (they have no CLI read to match).
- **2026-08-31 the URI-uniformity fix (co-designed cc+ic, hv to rule): `intent://` plural address is the one user-facing grammar via `promote`; the narrow doors delegate promote-THEN-NARROW; nav singular is TUI-internal. NOTHING emits a canonical address today, so strict refusal needs an emit partner.**
- **2026-08-31 vc: `schema` on the MCP tier lands on "land"; the clean-landing technique (build from `git show HEAD:`, semantic-diff, restore-staged) is the estate's.** The GraphQL hatch is DAEMON-ONLY (no in-process twin). 3.0.0 MCP stdio is ZERO-DEP; rmcp for streamable HTTP 3.x.
- **Standing from earlier:** pre-relocation canon layout REFUSED BY NAME; row-level `exposed_on_mcp` REFUSES on absence; MCP serves in-process only; `claude subagents` rows NARROW.
