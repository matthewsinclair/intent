---
node: ic
name: Interface Claude
role: interface
session_id: b148e605-2046-46b1-9830-53a81fc2d54f
heartbeat_at: 2026-09-09 20:18Z
status: active
focus: "POST-COMPACT BOOT COMPLETE 2026-09-09 20:18Z; HOLDING FOR vc ON THE BOUNCE at hv's word (hv AFK, vc holds the pen, and all three peers came up holding the same way). ALL FOUR INBOXES EMPTY at pickup -- a read state, not an assumption. Board folded ON EXECUTION at 20:03Z and everything cut was banked: AC-17.6 SATISFIED (View::Child { kind, id, field, item } on vc's ruling; THREE refusal sites of which the compiler found NONE, a STILL-PASSING test found the last, assertions INVERTED rather than relaxed, mutation-controlled throughout; vc drove the row at 284c3f9d8). 0294 filed high and fixed by dc at cd912d29; 0295 filed high and CLOSED BY ME the same hour after I reported a peer's in-flight transient as a property of main, and vc landed the rule as project canon at 6c3c389. THE ONE THING BOTH MINE AND STARTABLE IS ST0065/WP-01, and what the review has NOT reached is named in TODO so the next pickup does not re-derive it. NO FIGURE ON THIS BOARD IS EVIDENCE; RUN THE VERBS."
claims: [ST0065, ST0056/17, ST0064]
---

# Interface Claude (ic)

**Aggressive localfold 2026-09-03 14:46Z (hv: localfold + compact, then reconvene). Pre-fold board verbatim + sha-verified in this day's history dir as `wip-prefold-1446Z.md` (`09fe527766ff4402`, 27582 bytes, prettier-stable before the sha was taken). STATUS STAYS `active` -- a fold is not the end of a session.**

**FOLDED ON EXECUTION, NEVER ON DATE.** Everything cut was DONE: today's three hv asks are landed, built and delivered, and the three expired figures I opened the day correcting were each discharged by the node that owned them. **Every unexecuted ruling is still here**, and gated work sits under Holds with the condition that releases it rather than in TODO looking like work I am declining to start.

## DOING

**LOCALFOLD 2026-09-09 20:03Z at hv's word, then compact, then hold for vc on the bounce. Pre-fold DOING + Live + TODO archived at `.history/20260909/doing-prefold-2003Z.md` (**121 lines, `3a410bee87283436`, verbatim but for ONE trailing blank line the formatter removed** -- the pre-commit gate refuses unformatted markdown and `.prettierignore` does not exempt `.history/`, so an archive has two writers like anything else. The sha and count here are POST-format, because a figure that does not reproduce is worse than none). STATUS STAYS `active` -- a fold is not the end of a session.**

**FOLDED ON EXECUTION, NEVER ON DATE.** Everything cut was DONE and BANKED: `AC-17.6` is satisfied and off the gate, `0294` is filed and fixed by dc, `0295` is filed and closed carrying its own record, `0224` is amended, and both `WP-01` findings are routed to the nodes that own the code. **Nothing unexecuted was archived, and every hold below still carries an unmet condition.**

### WHAT IS BANKED, ONE LINE EACH, SO NOTHING IS RE-DRIVEN LOOKING FOR IT

- **`AC-17.6` SATISFIED.** `View::Child { kind, id, field, item }` on vc's ruling. **Three refusal sites: the compiler found none of them and a STILL-PASSING test found the last.** Assertions INVERTED rather than relaxed; mutation-controlled throughout. vc drove the row at `284c3f9d8`; my commits are `f1a3abb3`, `18097b44`, `d637912f`.
- **`0294`** filed high -- `int cli` dead four days, refusing with a currency claim it never computed -- **fixed by dc at `cd912d29`**. **My headline overstated the blast radius: two executing consumers, not five** (dc's correction, taken).
- **`0295`** filed high and **closed by me the same hour**: I ran the workspace suite on a five-writer tree and reported a peer's in-flight transient as a defect. **vc landed the rule as project canon at `6c3c389`.**
- **`0224` AMENDED** at `36fc1465` -- a fourth disposition that changes no mechanism, plus the dangling `MODULES.md` half.
- **THE SHARED BINARY IS STALE, MEASURED NOT INFERRED** -- mtime 2026-09-08 13:06Z with every code commit after it. **RE-DRIVE BEFORE QUOTING; peers have committed since.**
- **`target/debug/intentd` REBUILT BY ME** at cc's hand-off, so their sibling-daemon guard stops refusing on my account. **Debug only -- the shared release pair is untouched.**

## TODO

**`ST0065/WP-01` IS THE ONE THING BOTH MINE AND STARTABLE, AND IT IS UNDERWAY RATHER THAN UNSTARTED.** Two files of the bootstrap set are reviewed and routed; the set is not finished.

**WHAT THE REVIEW HAS NOT REACHED, NAMED SO THE NEXT PICKUP DOES NOT RE-DERIVE IT:** `intent/wip.md`, `.prettierignore` and `intent/.intentfiles` from the seeded set, and the whole `NotByInit` half of `DESTINATIONS` -- `usage-rules.md`, `ARCHETYPES.md`, `DEPENDENCY_GRAPH.md` and the six ST view templates -- **whose stated reasons I have READ and not DRIVEN.** `0224`'s amendment names its own uncounted population: **how many templates outside the seeding set still cite `MODULES.md`.**

**`ST0064` IS RULED MINE** (vc, 2026-09-09, `authority: vc` under hv's pen -- and the overlap was already dead: vc unclaimed it at `4649c2b64` and a PROSE line outlived the `claims:` line). **7/9, and both unsatisfied rows are in `## Holds` with conditions that are not mine to meet.**

**NOTHING ELSE IS BOTH MINE AND STARTABLE, AND THAT IS A REAL STATE RATHER THAN AN IDLE ONE.** **Do NOT pick up `AC-01.7`** -- it needs Apple Developer credentials, an external dependency and not unbuilt work; a reader scanning for something startable takes it by mistake, which is why it is named here.

## Holds -- work I am NOT doing, each with the condition that releases it

**A hold without a condition is an abandonment wearing a hold's name, so every line names what must become true, not who I am waiting on.** **THE AXIS IS ACT VERSUS STATE (vc's refinement of W23), NOT MINE VERSUS SOMEONE ELSE'S.**

**AND A NEW LIMB, LEARNED THIS MORNING AT MY OWN COST: A HOLD MUST ALSO NAME WHOSE THE CONDITION IS.** Two entries below sat for days in a column that read as hv's while hv owed nothing on either. **A durable surface with no expiry lets an item's presence read as its own justification (vc); a durable surface with no OWNERSHIP CHECK manufactures load on someone who was never asked.** Both are struck from the hv column and kept here, where they belong.

1. **THE ESTATE-WIDE BURN** -- **RELEASES WHEN THE TREE IS QUIET ENOUGH TO REBUILD, because the subject must be a CURRENT binary.** **THIS ENTRY EXISTS BECAUSE THE FOLD NEARLY BURIED IT: the burn lived under `## DOING` all day and its condition is UNMET, so archiving that section without moving it here would have been exactly the disappearance this section is for.**

   **THE CONDITION IT USED TO CARRY IS SPENT AND THE ONE THAT REPLACED IT IS A STATE, NOT A PERSON.** cc's provenance line landed at `75f1c0a76`: `burn.sh` now refuses an unset `INTENT_BIN` and stamps `SUBJECT=` and `VERSION=` on the TSV header, so a second subject-less artefact is no longer possible. What binds now is that the shared binary is stale (mtime 2026-09-08 13:06Z, every code commit after it), so burning would publish a correct measurement of the wrong day. **vc UPHELD THE HOLD 2026-09-09 rather than releasing it** -- hv's sequencing puts the staging-plus-atomic-mv fix for `0196` FIRST and the rebuild through it, with no separate supervised window, and cc and dc each independently declined to rebuild on the same grounds. **`coverage_map.sh` stays correctly refusing until then (98 baseline rows against 116 on-disk `.bats`).** When it runs, the baseline write goes through vc's ST0056 window (W2).

2. **The `v2:`-label census** -- RELEASES WHEN dc's work makes it checkable. **NOT AN hv ITEM AND NEVER WAS; struck from my hv column 2026-09-09.** It is ratified and it is dc's; **do not ask them to break it.** The condition as originally written named hv's BOARD, which is checkable only against MY PARAPHRASE of hv's words and never against hv's words -- the W23 limb vc named better than I did. **DO NOT READ THIS AS RELEASED.** The re-word owed is: record hv's actual sentence, or restate the condition against dc's work. Load-bearing beyond `0215`: vc carries it as the candidate `AC-00.16` amendment -- an instrument's reach is bounded by what the ESTATE CAN EXHIBIT, and that bound is a property of the ENVIRONMENT rather than of any tool. **`0215` is its worked case: in a self-hosted checkout the install root and the project root are ONE DIRECTORY, so no drive taken here could ever have exhibited the difference.**

3. **`ST0064` WP-01's REMAINING TWO, AND NEITHER IS ENGINEERING WORK (re-driven 2026-09-09: `intent ac gate ST0064` reads 7/9).** **`AC-01.4` -- RELEASES WHEN `0281` IS DISPOSITIONED, AND THE CONDITION'S OWNER CHANGED 2026-09-09: hv IS AFK AND vc HOLDS THE PEN, so this is vc's to rule rather than a wait with no lifter.** A design call still unmade: build the verb, reword `IntentCLI.swift:131`, or give the app the job with group signalling. There is nothing to fixture until it is decided, and the remedy carries a constraint -- assert `child.pgid == child.pid` FIRST, because `kill(-pgid)` degrades to signalling the APP if Foundation ever stops making the child a leader. **`AC-01.7` -- RELEASES WHEN APPLE DEVELOPER CREDENTIALS EXIST.** hv's to supply, and it is an ACTION rather than a decision. The `int macos` lane question is CLOSED (option A).

4. **The palette `Home`/`End` flip** -- RELEASES WHEN IT IS RULED, **and with hv AFK that is vc under the pen rather than nobody. STILL UNRULED at this cut.** A product-feel call, which is why neither I nor the pen may take it. The `/` sigil means the palette's buffer is never empty, so the empty-composer guard cannot fire there -- which also makes _only when empty_ a no-op rather than a compromise. Flipping costs the query line its caret keys.

5. **`0222` -- the explorer thread list's `Status | Created | Completed` columns** -- RELEASES WHEN `intent wp show ST0056/17` READS `done`. **NOT AN hv ITEM AND NEVER WAS; struck from my hv column 2026-09-09** -- the condition is a STATE I check myself and hv is nowhere in it. **Re-driven 2026-09-09: still `WIP`, so the hold STANDS.** **SIZE UNMEASURED; do not quote one.** The blocker is the row type, not the columns: `Row` carries one name and one value, and the thread-collection arm is the one view that does not go through `form::triples`.

**ONE ENTRY LEFT THIS SECTION IN THIS FOLD ON EXECUTION, WHICH IS THE ONLY GROUND ON WHICH A HOLD MAY GO** -- `0218`, which hv released this morning and cc DELIVERED at `9f65beca`. **AND ONE ENTERED IT: the burn, which had been sitting in `## DOING` with an unmet condition and would have been archived with the rest.** `ST0065/WP-01` left at the previous fold and is in TODO. **A fold keyed on a DATE rather than on execution is how an unexecuted ruling gets buried, and this section is where that would happen.**

## Watch-outs -- mechanisms only

1. **Non-test AC closes via `intent ac satisfy --evidence`, NEVER an AT; test AT rows close via `at green`, non-test ones via `at na`.** Re-drive + positive-control before satisfying. Source an existing note from the JSON extract (`jq -rj`), never `acceptance.md` (prettier is a second writer + the row carries a 114-char prefix). Assert contains-original-verbatim AND longer AFTER the write. **`at edit --note` REPLACES OUTRIGHT and is the deliberate door.**

2. **Canon on SHARED threads: SERIALISE ST0056 writes through vc; gather evidence OUTSIDE the window, make the window ONE coherent commit.** 0206 (concurrent facade verbs clobber), 0210 (a dirty parity/tools file blocks EVERY node's canon commit via the extract union, REPO-WIDE), 0212 (watcher ingest reverts a store write when disk lags; verify DISK not store before commit).

3. **The macOS app is `native/macos/Intent/` (Swift, xcodegen, folder-GLOBBED).** `bin/devbin macos app-build` unsigned Debug off-tree; `app-test` the suite. IntentCLI = the one shell-out home. NOT `dvb test rust`.

4. **A value/claim carried ONE STEP past what it supports is the recurring class, and it is MINE too.** Hand/take PROPERTIES not values; re-measure at pickup; a retraction carries the same burden as the claim it retracts.

5. **`git commit --only <explicit paths>` is the only safe write** on this 5-writer tree (git add new files first). NEVER remove a peer's index.lock (WAIT+retry -- hit twice today, cleared in 3 and 11 tries). cc's POST-VERIFY IS STANDING: `git show --name-only HEAD` == your set. Every stamp from a `date -u` read THIS turn.

6. **`intent --version` names the commit the BINARY WAS BUILT FROM; CURRENCY is that against `git diff --name-only <that>..HEAD -- native/rust`.** Only the second decides staleness. **AND THE `-- native/rust` SCOPING IS LOAD-BEARING: the embed is the LAST COMMIT TOUCHING `native/rust`, NOT HEAD** -- a fresh build of a clean tree embedded a sha ten board commits back, every one touching zero Rust. **So `embed != HEAD` is NORMAL on a board-heavy day and proves nothing.** I raised it as staleness and the refutation was already in the same terminal ([[feedback_the_disproof_is_usually_already_in_hand]]).

7. **A MARKDOWN ATTACHMENT HAS TWO WRITERS -- ORDER: prettier FIRST, let the store settle, verify, THEN commit.** The pre-commit gate refuses unformatted markdown, so prettier can rewrite a file AFTER the store has taken your version ([[project_formatter_is_a_second_writer]]). **AND RE-DRIVE ANY TEST THAT PARSES THE FILE: prettier ALIGNS markdown tables.** "It happened to still parse" is a thing to measure, not assume.

8. **A TEST WRITTEN FROM ONE OBSERVED FAILURE INHERITS THAT FAILURE'S INCIDENTAL SHAPE, AND THE INHERITANCE IS INVISIBLE BECAUSE THE TEST PASSES ON THE CASE THAT MOTIVATED IT.** **8x: ANY STATE THE HARNESS ITSELF MOVES IS NOT EVIDENCE THAT THE THING UNDER TEST MOVED IT.** Cure: a baseline that ALREADY CONTAINS the bookkeeping. **Live again today from the other side: a first-occurrence search over a log containing SETUP measures the setup** -- locating the first `enter_raw` found the initial take rather than the re-take, so a CORRECT implementation failed. It fails in whichever direction the setup happens to sit, so the direction of the error carries no information either.

9. **A THING DECLARED, WRITABLE, VISIBLE AND READ BY NOTHING IS THE RECURRING DEFECT ON THIS SURFACE.** `Hotkey` (hv's find), `Command::group` (MINE, added in the same commit as the paragraph condemning `Hotkey`), and `explorer.editing.mode` would have been the third. **The cheap check is a grep for consumers of every field you declare, at the moment you declare it** -- no invariant catches this class, because a field with no reader breaks nothing. **INVERTED FORM, and it is the one with no cure but the compiler: a DEFAULT method on a trait with FORWARDING implementations is a trap.** A forwarder that inherits a default where it should have passed on is correct in isolation at every site; required methods make the compiler find all of them, a default makes it find none.

10. **~~A shared file can carry a peer's hunk~~ -- FOLDED INTO W39**, which now carries this rule, both directions it bit, and the hunk-level cure.

11. **A DEFECT'S BLAST RADIUS IS NOT ITS PACKAGE.** A base-crate signature moves and `cargo check -p <their crate>` stays green throughout; a generated `ddl.sql` fails tests in a package that did not change. **The node who can SEE it is not the node who caused it.** `cargo test --workspace` is the instrument that sees it and `-p` is the one that does not.

12. **THE SHARED-ARTEFACT GUARD REFUSING A REBUILD IS THE GUARD WORKING.** `bin/devbin build all` REDIRECTS to a private target dir when `native/rust` is dirty. **Wait for the peer; never force** -- forcing compiles their uncommitted work into the binary hv drives (`dirty-<sha>`). Measure delivery by SHA (symlink target vs built file), never by the marker. **AND THE "REBUILD WINDOW IS hv's" PREMISE IS EXPIRED (vc, 20:24Z, driven): `0196` is a DEFECT, not an authority constraint** -- `guarded_release_build` deletes the shared pair before it builds and no failure path restores it, so a FAILED build leaves every live session on this machine without a binary. **The real constraint is a QUIET TREE, not hv's permission.** I rebuilt twice today without asking and both were right; two nodes were reporting the wrong model upward while the correct one was in my hands. **A constraint nobody re-derives becomes an authority claim.**

13. **A PREMISE CARRIES A VERSION IN THIS ESTATE, AND A RE-DRIVE AIMED AT THE CITATION PASSES** (vc's `8an`, on my defect). I put a citation naming a file that does not exist into a contract proposal, lifted from my own draft five days older -- **and the SUBSTANCE was true anyway**, so minting would have been right for the wrong reason and rejecting would have lost a true row. **Neither verdict was available without driving it.** The quieter half: _upgrade cannot rescue it_ cited a line that is still present, still says exactly that, **and is v2**; in v3 `Payload::sync` has ONE caller, so nothing automatic runs it and the correction made the defect WORSE. **THE CLASS IS NOT ORDINARY STALENESS: the evidence STILL VERIFIES. A re-drive checking the CITATION passes; only one checking the SUBJECT fails** -- it survives "re-drive your citations" intact. **This estate is a port, so every inherited claim carries a version and establishing WHICH is the first move.**

14. **DRIVE A DESTRUCTIVE QUESTION IN A SANDBOX OR DO NOT DRIVE IT.** The `sync`-prunes question needed a skill installed and its canon source deleted -- against real state that is `~/.claude/skills/` and the live manifest, ie an incident rather than a measurement. `intentsvcs/tests/skills_sync.rs` carries a disposable install tree, Claude target and manifest. **A measurement that mutates shared durable state is not one anyone can afford to repeat**, and repeatability is most of its value.

15. **`ok:` IS THE TOOL'S REPORT, NOT EVIDENCE THE ROW SURVIVES (`0216`, HIGH). DRIVEN AND CENSUSED BY vc ON cc's DESIGN, 2026-09-02 21:4xZ -- MY OWN READING OF IT WAS WRONG IN TWO PLACES AND BOTH CORRECTIONS MATTER AT THE KEYBOARD.**

16. **BACKTICKS INSIDE A DOUBLE-QUOTED SHELL ARGUMENT ARE COMMAND SUBSTITUTION, AND THE FAILURE IS SILENT AND PLAUSIBLE.** A `--note` passed inline lost both its citations to it; the row was created, the tool printed `ok`, and the note read perfectly well with the two facts it existed to carry simply gone. **`intent init` ACTUALLY RAN against this project** -- it refused because the project exists, which was luck rather than design: a different verb would have executed against the live tree with no refusal to warn me. **Anything non-trivial goes to a file via a QUOTED heredoc and reaches the CLI as `"$(cat …)"`** -- the same cure dc reached for the apostrophe trap. Sibling of the zsh traps already known here: unquoted `$var` does not word-split, and an unmatched glob aborts the whole command.

17. **A PROBE WHOSE SHAPE IS GUESSED ANSWERS A DIFFERENT QUESTION, AND ITS ANSWER LOOKS EXACTLY LIKE A REAL ONE. THREE LIMBS, ONE MECHANISM (`W37` and `W60` folded in here 2026-09-08 under `W106`).** **The family is not _a wrong answer_ -- it is an answer to a question nobody asked, arriving in the shape of the one they did.**

**LIMB 1, THE FALSE NEGATIVE.** **A PROBE WHOSE FIELD NAMES ARE GUESSED RETURNS `absent` FOR BOTH _not there_ AND _not where I looked_, AND THE TWO ARE INDISTINGUISHABLE IN THE OUTPUT.** Verifying `AC-00.8` I read `c["satisfied"]` and `c["evidence"]` out of the canon extract, got `None` and `0 bytes`, and had a loss report half-written -- **against a store that said `satisfied: yes` and a file that had been rewritten one minute earlier.** The real path is `c["state"]["is"]` and `c["state"]["evidence"]`. **The false negative looked EXACTLY like the `0216` revert I was watching for**, which is the trap: I was primed for a specific failure and my instrument produced its signature for an unrelated reason. **Cure, and it is the same one this estate keeps arriving at: dump the keys of a record you KNOW is in the wanted state before asserting over a record you are unsure about.** One `sorted(c.keys())` against `AC-00.1` would have cost nothing and settled it.

**LIMB 2, THE FALSE POSITIVE (was `W37`).** **A GREP WHOSE PATTERN IS AN ALTERNATION RETURNS A COUNT THAT CANNOT SAY WHICH BRANCH MATCHED, AND I REPORTED ONE TO hv AS CONFIRMATION (mine, 2026-09-04).** Asked whether the shipped binary carried my fix, I ran `strings <bin> | grep -c "delete-all\|VALUES('rebuild')"`, got `2`, and told hv the fix was live. **Both matches were the pre-existing string `delete-all`. The count was never about the fix at all**, and the binary predated it by five hours. The number was plausible, non-zero, and answered a question I had not asked. **Cure: an alternation is only safe when EVERY branch would settle the question; if one branch can match for an unrelated reason, `grep -c` collapses the distinction and reports the wrong thing confidently. Grep for the discriminating literal alone, and prefer `grep -o` so the MATCH is visible rather than its tally.** Sibling of [[feedback_precision_is_a_claim_about_the_corpus]] -- there the population was wrong, here the PATTERN was, and both produce a figure with no error to notice.

**LIMB 3, AND IT IS THE LIMB THAT SHIPS (was `W60`).** **MY AD-HOC PROBE AND THE SHIPPED CLIENT GUESSED THE SAME FIELD NAME WRONG, AGAINST THE SAME PAYLOAD (mine, 2026-09-04, and the second one is a live defect).** Driving `Op::Form` through the delivered daemon I filtered errors with `if "error" in d` and reported a correct refusal as _ok, fields=0_. The wire is `#[serde(tag = "result")]`, so a refusal is `result: "error"` with the text in `message` -- **there is no `error` key at all.** Then I looked at `shell.html`, which I wrote, and it reads `answer.body.error`: **the identical wrong guess, shipped, so the page discards every refusal the daemon can produce** (`0241`, high). **THE TRANSFERABLE PART IS THE JOIN: when your throwaway probe guesses a field name wrong, GREP PRODUCTION FOR THE SAME GUESS.** The same wrong mental model wrote both, minutes apart, and only one of them was going to be noticed by me being confused at a terminal. **W17 says a guessed probe cannot distinguish _absent_ from _not where I looked_; this is its sharper limb -- the guess is not random, it is YOUR model of the payload, so it is exactly the guess your code already made.**

**ONE CURE COVERS ALL THREE: POSITIVE-CONTROL THE INSTRUMENT AGAINST A RECORD KNOWN TO BE IN THE WANTED STATE BEFORE ASSERTING OVER ONE YOU ARE UNSURE OF** -- dump the keys, or run the probe where you know the answer. **A control that would also pass under the broken instrument is decoration.** And limb 2 adds its own rider: an alternation is safe only when EVERY branch would settle the question, because a count cannot say which branch matched.

18. **ONE LOCAL MEASUREMENT BECAME AN ESTATE-WIDE CLAIM BECAUSE I RECALLED A KNOWN FAILURE SHAPE AND REPORTED IT AS A READING (2026-09-02, corrected by dc).** I measured MY commit being refused and told hv the gate was blocking every node -- reasoning from a true general mechanism I had not driven here. **DETECTION: name what you measured and what you inferred, in the same sentence.** _(full incident: `.history/20260904/watch-outs-full-1742Z.md`)_

19. **TWO HALVES EACH CORRECT ABOUT ITS OWN DIRECTION, AND NOTHING OWNING THE RELATIONSHIP BETWEEN THEM (2026-09-03, hv found it).** `Omnibox::pick_move` walks an index over a best-FIRST list; `run::dropdown` renders best-LAST. Each documents itself accurately. Each is right. **Every screen-down keypress walked the caret UP**, and the defect is invisible from either file -- **it exists only at the seam, so no amount of reading either half finds it.** Two multipliers: one renderer serves BOTH vocabularies and says so in its own header, so the bug hv hit in the palette was equally live in the omnibox where nobody had been; and the right fix is a single declaration both sides derive from, because **inverting a boolean at each call site fixes the symptom and rebuilds the defect** -- two places obliged to agree, with nothing holding them to it. **Generalises past this file: when a bug survives a careful reading of every part, stop reading parts and ask what owns the relation.**

20. **~~A test can be green because of the bug~~ -- FOLDED INTO `W44`**, which now carries the practice, this asymmetry, the vacuity-guard argument and the targeting heuristic.

21. **MEASURE WHAT EXISTS BEFORE COSTING A REPLACEMENT: THE ASK NAMES A SYMPTOM AND THE CAUSE IS A GUESS UNTIL DRIVEN.** hv asked for open threads at the top of the explorer list; `index_order` has sorted open-before-closed all along and the only thing missing was a VISIBLE BOUNDARY. I had started sizing a sort. **One read of the function I was about to duplicate was the whole cost of not duplicating it** -- Highlander arriving from the other end: not _do not write a second copy_ but _check whether the first already does it_.

22. **A MARKER THAT IS BOTH A STRUCTURAL ANCHOR AND ORDINARY PROSE WILL BE FOUND IN THE PROSE (mine, during this very fold).** I spliced this board by `index()` on a section heading, and my new banner mentioned that section by name in a sentence -- so the splice landed inside my own paragraph, duplicating two sections and truncating the banner mid-line. **Restored from the pre-fold snapshot; nothing lost, which is the entire reason the snapshot is taken BEFORE the first edit rather than after a good one.** Cure: anchor on the FULL heading line and assert the anchor occurs exactly once before using it. **The protocol already knows this class from the other side** -- `whiteboard-header-guard.sh` deliberately never scans prose, because nodes report these defects to each other by quoting them and a scanner would make reporting an offence. **SECOND INSTANCE 2026-09-04, and this time I BUILT the scanner: my `0241` seam test failed on the COMMENT explaining the bug**, which quotes the dead field by name. **A test that greps source for a bad pattern makes DOCUMENTING that pattern an offence** -- so it strips comment lines before scanning, and the protocol had already ruled this exact way for its own guard. I read that ruling, wrote it into a commit message the same hour, and still built the scanner without it. **vc RATES THIS AS STRICTLY WORSE THAN dc's _a rule you have to remember at the keyboard is not a control_, AND THEREFORE BETTER EVIDENCE: not forgotten, not unread, not unavailable -- CITED, AND STILL NOT APPLIED.** **QUOTING A RULE IS NOT APPLYING IT, AND CITING ONE IS NOT EVEN WEAK EVIDENCE THAT YOU DID.** The gap is not memory: the rule lives in PROSE and the violation lives in the SHAPE OF A COMMAND, and nothing carries one into the other. **The only fixes that have ever held here changed the SHAPE** -- this scanner stripping comments, dc's `POP_DIRS`, vc's `out=$(...); rc=$?`. A watch-out that cannot be turned into a shape is a hope. **THIRD INSTANCE THE SAME EVENING, MEASURED RATHER THAN SUFFERED: 14 OF THE 16 `remedy` MENTIONS IN `.bats` FILES ARE PROSE IN COMMENTS.** Counting them would have reported 16 remedy-readers where there are 2 -- an 8x overcount, in the direction that makes a real gap look COVERED. **Stripping comments is not tidiness on this estate; it is the difference between measuring the code and measuring the commentary about the code**, and this estate comments heavily by design.

23. **I WROTE A HOLD WHOSE CONDITION ASSERTED A PROPERTY OF A PROGRAM I HAD NEVER READ, AND IT STOOD FOR A DAY (mine, 2026-09-03).** Hold 5 said `skills sync`'s `4 need a decision` was _a standing count with no visible subject_. **The source says otherwise and always did**: `render.rs:7411` prints the total directly beneath a per-skill line that names each skill and its reason. One `sed` refuted it; I had run nothing and needed to run nothing.

24. **A PARTIAL VIEW OF A RECORD READS EXACTLY LIKE THE RECORD (mine, 2026-09-03, twice in one episode and in opposite directions).** I grepped a line whose field continued `SUPERSEDED BY THE DRIVE BELOW` and nearly filed a defect that did not exist; my own fold kept a Decision whose RULING was live and whose VALUE was dead. **THE FIX IS NOT _read more carefully_: a status word inside prose is a second home for a fact the tool already owns. CITE THE VERB, NOT THE VALUE.**

25. **A VERBATIM ARCHIVE AND A FORMATTER GATE ARE IN DIRECT TENSION, AND `.prettierignore` DECIDES IT WITHOUT SAYING SO (mine, 2026-09-03).** It exempts `intent/whiteboard/*/inbox.*.md` and does NOT exempt `.history/`, **so copying an exempt file into an archive subjects it to a check it never had to pass** -- and formatting it to pass destroys the byte-exactness that was the point. **THE ONLY TRUE VERBATIM RECORD IS A COPY OF SOMETHING ALREADY GATE-CLEAN AT SOURCE** (the live board is, so its snapshot is). Everything else is a convenience view and must SAY SO rather than claim verbatim. Snapshot first, then check `prettier --check` on the snapshot before believing the word.

26. **A STATED REASON FOR NOT DOING SOMETHING IS AN AUTHOR'S JUDGEMENT AT A MOMENT AND READS EXACTLY LIKE A PROPERTY OF THE SYSTEM (2026-09-03).** A comment declining a reader thread _because a thread is a second thing that can go wrong_ was sound when written, and the thread it declines now runs. **DETECTION: a rationale in a comment dates from its writing; check whether its premise still holds before treating it as a constraint.** _(full incident: `.history/20260904/watch-outs-full-1742Z.md`)_

27. **REPLICATION DOES NOT REPAIR A SHARED BLIND SPOT, AND A SECOND NODE AGREEING IS THE THING MOST LIKELY TO BE MISTAKEN FOR CONFIRMATION (vc aimed my own finding at their check, 2026-09-03).** Two nodes, two token-sets, one shared assumption: each grep tested a GUESS about hv's words and neither tested the words. **DETECTION: on agreement, ask what assumption BOTH probes share -- that question has no other prompt, because nothing about a confirmation feels like a reason to look.** _(full incident: `.history/20260904/watch-outs-full-1742Z.md`)_

28. **A STALENESS BOUND THAT IS STATED BUT NOT ACTED ON IS DECORATION, AND STATING IT MAKES THE CLAIM TRAVEL MORE EASILY (mine, corrected by me inside the hour).** I read a peer's committed board, wrote down honestly that it was a read with a known staleness bound -- and then reported it as their current state. **The caveat bought the claim credibility instead of restraining it.** _(full incident: `.history/20260904/watch-outs-full-1742Z.md`)_

29. **COSTING AN APPROACH IN DETAIL MAKES ITS PREMISE FEEL EXAMINED, AND MINE WAS NEVER TESTED AT ALL.** A1 was held for days on _no harness can drive an interactive selection_; I costed the solution thoroughly and never spent one command on the premise, which was refuted in one drive. **DETECTION: before costing a fix, drive the claim that says it is needed.** _(full incident: `.history/20260904/watch-outs-full-1742Z.md`)_

30. **A SHARED INDEX DESTROYS AUTHORSHIP AND THEN PRESENTS ADJACENCY AS IF IT WERE PROVENANCE (vc's framing, on my error).** I attributed a staged parity tool to vc because it sat beside two of vc's files; it was dc's. **Nothing in `git status` or the index carries an author** -- same family as W42 and W48. _(full incident: `.history/20260904/watch-outs-full-1742Z.md`)_

31. **THE ASYMMETRY THAT MAKES `unstage to protect a peer` A DESTRUCTIVE RULE (vc, same episode, worth keeping because it is counter-intuitive).** For a MODIFIED file, unstaging is free -- the working tree keeps the change. **For an ADDED file, the index is the only place git knows about it**, so unstaging it is the one move that can lose new work. I left my two new files staged in a shared index and TOLD the peer rather than unstaging, and that was right. **The safe move for a tracked file is the unsafe move for an untracked one**, so a blanket rule in either direction is wrong.

32. **CONTIGUOUS ROWIDS DO NOT DISCRIMINATE _NEVER DELETED_ FROM _DELETED AND REWRITTEN_, AND I FILED A HIGH DEFECT ON THAT EVIDENCE (mine, 2026-09-04).** I read `rowid 1 survived every mutation` off a contiguous `1..557` and concluded the `DELETE` never ran. **A table that IS emptied restarts its rowids at 1, so the observation is equally consistent with both hypotheses** -- it was never evidence for the one I attached it to. **THE SECOND ATTEMPT FAILED THE SAME WAY**: I then found duplicate rows sitting at CONSECUTIVE rowids and concluded the opposite, that whole generations could not have been appended -- but the reader orders `BY file, seq`, so any round-trip regroups copies regardless of how they arrived. **Two observations, two confident readings, neither instrument able to separate the alternatives.** Cure: before an observation becomes evidence, state what the OTHER hypothesis would have produced. If the answer is _the same thing_, it is not evidence yet. Distinct from W17 (_absent means not-where-I-looked_): the data here was present and correct, and the inference from it was invalid.

33. **A FIX CANDIDATE BANKED IN A FILING IS A GUESS WEARING A FINDING'S CLOTHES.** `0234` carried `INSERT INTO doc_sections(doc_sections) VALUES('delete-all')` as _the obvious candidate fix_, marked to be driven. It is **refused outright by SQLite** on a content-carrying table. **The marking is what saved it** -- and the marking is also what nearly did not survive, because a candidate stated in a filing beside real measurements reads as part of the measurement. Cure: a candidate goes in the record only with the drive that would falsify it named beside it, and it is written as a QUESTION rather than as a nomination.

34. **~~A vacuity guard found a second defect~~ -- FOLDED INTO `W44`** as the argument for writing the control even when the main assertion looks sufficient.

35. **A GATE VERDICT IS A READING, NOT A STATE, AND I ACTED ON ONE FOR HOURS WITHOUT RE-RUNNING IT -- SO DID BOTH PEERS (the day-long freeze, 2026-09-04).** The guard I was blocked on had gone green and the blocker had MOVED to a different guard, while three nodes quoted the original verdict to each other. **CURE: re-run the gate before quoting it, and quote the run rather than the memory.** _(full incident: `.history/20260904/watch-outs-full-1742Z.md`)_

36. **AN INSTRUMENT BUILT FROM THE WORKTREE CANNOT EXHIBIT A STALE-INDEX DEFECT, BY CONSTRUCTION (dc's finding, on their own error, and recorded here only as a pointer -- it is theirs to write up).** Every synthesised index in this episode was built with `git add`, which reads the WORKTREE, so all of them contained the fresh bytes and all of them returned a clean `rc=0` that looked like an answer. **The instrument answered a different question and the shape of the reply was indistinguishable from the right one.** Same family as [[feedback_precision_is_a_claim_about_the_corpus]]: a sample that cannot exhibit the failure. Kept on my board because I nearly repeated it -- my own first synthesised test would have had the same blind spot if I had built it from `read-tree HEAD` plus `add` rather than from a COPY of the ambient index.

37. **~~A grep whose pattern is an alternation returns a count that cannot say which branch matched~~ -- FOLDED INTO `W17`** as its false-positive limb, with the alternation rider kept intact.

38. **I EXTRAPOLATED AN ESTATE-WIDE FIGURE FROM ONE SAMPLE, AND THE SAMPLE WAS THE ONE I HAD ALREADY MEASURED AS UNUSUAL.** I told hv the backups would come down to ~50 MB. They came down to 698 MB, fourteen times my estimate. The prediction came from the single backup I had probed -- which had CLEAN rows and pure tombstone bloat, so the repair took it 936 MB -> 14 MB. Most backups also carried DUPLICATED rows, which the repair deliberately preserves because they are real content. **The atypical case is the one you probe, because it is the one that looked interesting enough to probe.** Cure: a figure quoted for a population needs either the whole population measured or an explicit `n=1, and here is why it may not generalise`. A single sample is a demonstration that the mechanism works, never a size for the outcome.

39. **`--only` IS PATH-SCOPED, AND EVERY WAY THAT BITES IS ONE MECHANISM. (mine and dc's; cc cites this number; dc banks it as their W70. W10 and W51 folded in here 2026-09-04 -- they were three copies of one rule.)**

    **THE RULE: `git commit --only <paths>` BUILDS HEAD PLUS THE NAMED PATHS, TAKING EACH NAMED FILE'S WORKING STATE ENTIRE.** It protects against an unnamed FILE and gives nothing against an unnamed HUNK inside a named one.

    - **BOTH DIRECTIONS BIT IN ONE DAY.** A bare `git commit -m` with no `--only` swept dc's board into mine; then my own explore-wiring hunk rode into dc's `9fe2ee464`, because dc correctly committed `render.rs` for their arm while my change sat uncommitted in the same file. **dc's _three files, post-verified_ was the right check and could not have caught it** -- verifying WHICH files a commit contains says nothing about what is inside them. **My share was larger: cc handed me that file specifically so the hunk would be mine, and I left it through a long sweep. A HANDOVER HAS A SHELF LIFE.**
    - **EXCLUDING A PATH KEEPS YOUR CHANGE OUT OF YOUR COMMIT AND NOT OUT OF THE FILE (dc, 2026-09-04).** I thought holding `render.rs` back had parked my hunk; it had parked only my AUTHORSHIP of it, and the next node to name that path would have taken it. **I had the right rule and the wrong model of what it protects.**
    - **THE FILE THAT BITES IS THE ONE YOU HAVE NO HABIT ABOUT.** dc and I watched `render.rs` for six hours and neither thought of `tests/suite.rs` -- the file every test either of us writes must touch, carrying both our module declarations, with dc's test file untracked so committing it would have declared a module with no file. **The habit attached to the FILE rather than to the PROPERTY**, and the property is: any file two people are both appending to.
    - **CURE, HUNK-LEVEL AND NON-INTERACTIVE** (`git add -p` is unavailable here): `git diff -- <file>` to a patch, split on `@@` **by CONTENT and not by line number** (numbers move under the other author's edits), `git apply -R --check` before `-R`, verify each branch SEPARATELY rather than as one alternation count (W37), then commit. Index must be clean of peers first.
    - **DETECTION: before naming any path on a commit, read its diff for AUTHORSHIP, not only for collision.** They are different questions of the same bytes, and I read one without the other and attributed dc's work to cc (W42).

40. **I NEARLY ACTED ON AN ASSUMED RETENTION MODEL, AND TESTING IT COST ONE COMMAND (mine, 2026-09-04).** Clearing 386 MB of pre-repair snapshots, my plan was _run `intent backup` twice, retention keeps the newest N, the old one falls off_. I drove it on one project first. **Retention is PER-DAY: the second run pruned the same-day snapshot I had just made and KEPT the pre-repair one from a different day.** The plan would have deleted exactly the file I wanted to keep and kept exactly the one I wanted gone. **Cure: a policy you have not read is a guess, and the cheapest test of a destructive plan is to run it once on the smallest instance and LOOK.** Related: `rm` on a snapshot file and the tool's own prune are not equivalent -- the store keeps a snapshot registry, so `rm` leaves rows pointing at nothing (harmless here; there is no restore verb).

41. **A BUILD THAT REMOVES ITS OUTPUTS BEFORE VERIFYING THEM CAN LEAVE THE TOOL UNUSABLE, AND I DID THAT TO hv's MACHINE.** `int local build` forces the provenance embeds by deleting the artefacts first ("Removed 49 files"), then builds, then verifies. Its verify refused -- _no intent binary at .../release/intent_ -- and the shared path was left EMPTY, so `intent` was `command not found` for everyone on this machine until I rebuilt it. **The failure mode of a delete-then-rebuild is strictly worse than the failure mode of a rebuild-then-swap**, and it is worst on the artefact the whole fleet resolves through. **I never established WHY the verify refused** -- my restore build reported `Blocking waiting for file lock on build directory`, so a concurrent cargo is the hypothesis and it is not measured. **Recorded as an unexplained refusal rather than as a diagnosed one**, because the tempting write-up here is the plausible cause.

42. **FILE OWNERSHIP DOES NOT IMPLY DIFF AUTHORSHIP IN A SHARED CHECKOUT. cc's, and they proved it against my claim.** I found `render.rs` dirty, knew cc owns it, remembered cc saying nothing of theirs was in flight, and concluded cc had started since. Every step reasonable; the conclusion wrong -- it was dc's A1. **`git status` CARRIES NO AUTHOR**, so the only instrument that answers _whose is this_ is reading the diff's CONTENT for its subject. **I read that diff for COLLISION and never for AUTHORSHIP, which are different questions of the same bytes.** **THE NASTY LIMB IS THAT THE WRONG ATTRIBUTION STILL PRODUCED THE RIGHT ACTION** -- I correctly held back, and told the wrong peer I was protecting them; nothing would have surfaced it if cc had not read their own file. **DETECTION: before naming a peer as the author of a diff, find the diff's SUBJECT (here: 9 added lines saying `subagents`, and `surface/dispatch-table.*` dirty alongside).** Same family as the attribution errors dc and cc hit twice today from the other side.

43. **A MECHANISM BANKED IN MEMORY HAS NO TRIGGER, AND MINE FIRED ON ME TODAY.** `P="a b c"; git add $P` -- zsh does NOT word-split an unquoted variable, so git received ONE pathspec containing spaces and refused. **That exact behaviour is in my persistent memory, written down after it cost someone else a wrong answer**, and I still wrote the line. A note is retrieved when something reminds you to retrieve it; nothing about typing a shell variable does. **The cure is not remembering harder -- it is not composing a path list in a variable at all**, which is why the retry listed every path literally.

44. **A GREEN TEST IS NOT EVIDENCE UNTIL I HAVE SEEN IT GO RED, AND THE ARM THAT CLAIMS TO BE THE LOAD-BEARING ONE IS WHERE TO LOOK FIRST (`W20`, `W34` and `W112` folded in here 2026-09-08 under `W106`).** **A missing test is visibly absent; a false one is visibly present, which is why this family costs more than no test at all.**

**THE PRACTICE.** **A GREEN TEST I JUST WROTE IS NOT EVIDENCE UNTIL I HAVE SEEN IT GO RED.** Practice, not a failure -- recorded because it is the step most easily skipped when the test passes first time. Disabling the `wp` arm turned both wp-sensitive tests red with the intended diagnostic and left both controls green, so **both arms of the instrument fire** and the green means something. **A test written in the same hour as the fix it covers is the case where this matters most**, because it was written by someone who already knew the answer.

**WHY IT COSTS MORE THAN NO TEST (was `W20`).** **A TEST CAN BE GREEN BECAUSE OF THE BUG, AND THAT IS WORSE THAN NO TEST BECAUSE IT READS AS COVER.** Two existing tests armed `MENU + Move` with `Down` and only appeared to move while the mapping was inverted -- the palette opens on the best match, which is the BOTTOM line, so `Down` is correctly inert there. **Neither looked wrong.** A missing test is visibly absent; a false one is visibly present (vc's framing, and the sharper half).

**WRITE THE VACUITY GUARD EVEN WHEN THE ASSERTION LOOKS SUFFICIENT (was `W34`).** **A VACUITY GUARD FOUND A SECOND DEFECT, WHICH IS AN ARGUMENT FOR WRITING ONE EVEN WHEN THE MAIN ASSERTION LOOKS SUFFICIENT (mine, 2026-09-04).** My duplication test asserted _no address appears twice_. That can pass on an index nothing wrote, so I added an arm checking the three new issue bodies had actually reached it. **They had not -- and that was a real, separate, pre-existing defect** (issue prose never derived on a mutation), not a fixture problem. **The control was written to protect the assertion and it is what found the second half of the bug.** Cure: the question _what would make this pass for the wrong reason_ is worth asking even when the assertion is clearly true, because the answer is sometimes another defect rather than another test. **SECOND INSTANCE 2026-09-04, and this one the guard saved the TEST rather than finding a defect:** my `browse` existence test runs with no daemon, so _it refused_ is true with AND without the fix -- the assertion would have passed against the very defect it exists to catch. The discriminating arm is WHICH refusal arrives. **Driven by mutation: with the check disabled the test fails on exactly that substitution.** A test whose subject and whose environment can produce the same symptom needs an arm that separates them, and the environment is the half nobody thinks to vary.

**AND THE FOURTH FACE, WHERE THE PROSE ASSIGNS A JOB RATHER THAN CLAIMING A PROPERTY (mine, filed as `0281`, 2026-09-08; vc asked for it as its own limb).** The first three faces are prose claiming the code HAS something it does not. This one allocates the work to a PARTY: `IntentCLI.swift:131` says the console tail's pipeline is _the verb's job, not the app's_ -- **and no `log`, `tail` or `console` verb exists anywhere**, positive-controlled before saying so. **It is more dangerous than a false property claim because it does not describe the code at all, so no reader checks it against the code.** A settled-sounding allocation is read as somebody else's finished business, and the next person builds on it.

**WHERE TO AIM FIRST (was `W112`).** **CHECK THE ARM THAT CLAIMS TO BE THE LOAD-BEARING ONE FIRST, BECAUSE THE CLAIM IS WHAT STOPS ANYONE CHECKING (vc's formulation, on my finding, 2026-09-08).** A test whose NAME or comment says it is the one holding a guarantee is the least-audited line in the file: everything else is read against it, and it is read against nothing. Two worked cases the same day -- `the_two_non_live_states_differ_in_what_they_give_an_operator`, whose comment claimed it stopped a projection degenerating _while every other arm stays green_ and whose body was an enum-variant tautology; and vc's `doctor` `Display` match, whose comment said _a match is refused by the compiler when the next variant forgets to choose_ while a `_` arm meant nothing was ever refused. **The tell is a comment asserting a protection, and the check is to ask what input would make the body fail.** vc's `every_emitted_remedy_names_something_this_build_can_do` and cc's detail-text arm are the same class found in autopsy; this one was found BEFORE the file was cited, which is the difference between a rule that catches and a rule that explains.

113. **BATCHING THE `date -u` READ INTO THE SAME TOOL CALL AS THE WRITE MEANS THE STAMP IS CHOSEN BEFORE THE READ RETURNS -- WHICH IS FABRICATION WITH A CLOCK READ SITTING NEXT TO IT (mine, TWICE on 2026-09-08, caught both times before commit).** The protocol says a clock value goes in only when the command that produced it is in front of you in this turn, verbatim. **I was satisfying the letter and breaking it in the same breath**: putting `date -u` and the board edit in ONE call means I compose the timestamp while writing the script and the real value arrives ALONGSIDE it, never before. First slip 12:55Z read against 12:56Z written; second 13:09Z read against 13:03Z written -- **and the second was worse, six minutes, because more prose sat between the guess and the read.** **THE TELL IS THE DRIFT SIZE: a typo is a digit, a guess drifts by however long the composing felt.** **CURE: the clock read is its OWN call, and its output is copied into the next one.** A stamp is the one value that cannot be composed in parallel with the thing it stamps, and batching -- correct everywhere else and the reason I keep doing it -- is what makes this invisible.

114. **I CORRECTED THE INSTANCE AND NOT THE CLASS, IN A FILING WHERE I HAD SPENT THE DAY TELLING OTHER NODES TO SWEEP THE CLASS AND NAME THE PATHS (mine, caught by cc, 2026-09-08).** I struck `critic_surface.rs` from `0279`'s population after reading its body -- and never re-applied the body-read to the rest of MY OWN list. **Two more files of identical shape stood in it unexamined**: `bootstrap_door.rs` and `exit_code_consumers.rs`, whose `lib/templates` lines are an `assert!` and a walk to the REAL shipped tree. All three are one shape -- **the real tree IS their subject** -- and I had already named that shape once, in writing, in the same paragraph that left the other two standing.

**AND cc's ROUTE WAS STRUCTURALLY BETTER THAN A CAREFULLER RE-READ.** They did not read the list harder; they NARROWED THE CRITERION, from the string `lib/templates` to the operation `create_dir_all(<x>.join("lib/templates"))`. **My criterion selected on a token that appears in a CONSTRUCTION and in an ASSERTION alike, so no amount of care applied to it could have separated them** -- reading bodies would have caught these two and missed the next pair. **When a correction keeps finding instances, the criterion is the defect.**

**THIRD LIMB, AND IT IS THE ONE I WOULD HAVE STAYED WRONG ABOUT INDEFINITELY: I HEDGED THE CRITERION IN THE DIRECTION THAT WAS STRUCTURALLY CLOSED.** I published it as a proxy that might MISS a builder and said so as its honest limit. It cannot miss one: `intentsvcs/src/install.rs:51` declares `pub const MARKER: &str = "lib/templates"` and `is_install` is exactly `dir.join(MARKER).is_dir()`, **so a directory without the marker is not an install root by the authority of the code that resolves one.** The criterion could not under-count. **It over-counted -- and my stated caveat pointed at the closed direction while the live one went unmentioned.** A caveat aimed at the wrong failure direction is worse than none: it reads as diligence and buys confidence in exactly the number that is wrong.

115. **A REASON FELT LIKE EVIDENCE, AND THE HEDGE LANDED ON THE CLAIM THAT DID NOT NEED IT (mine, 2026-09-08, the FOURTH wrong attribution in an estate where I had written _three nodes, three wrong attributions_ onto this board the same morning).** I told cc a staged edit to `0279.json` was theirs. It was not -- `55bc41a69` is not cc's, and I had NO evidence for the claim, only a REASON: cc had just claimed the issue. **Claiming a piece of work and writing a particular file are different facts, and the first made the second feel established.**

**THE TELL IS IN THE SAME MESSAGE, WHICH IS WHY THIS IS A MECHANISM AND NOT A LAPSE.** I hedged one attribution and asserted the other: _both `tests/common/mod.rs` are dirty; I assume yours, measure rather than take my word_ (hedged, and RIGHT -- they were cc's), against _YOUR REWRITE IS STAGED AND UNCOMMITTED_ (asserted, and WRONG). **I had no reasoning behind the first, so I hedged; I had reasoning behind the second, so I did not.** The caveat tracked HOW MUCH I HAD THOUGHT ABOUT IT rather than HOW MUCH I KNEW, which is exactly backwards -- and the message's own closing line told cc to measure attribution rather than take my word.

**W114's THIRD LIMB RECURRING INSIDE AN HOUR: a caveat aimed at the wrong failure direction reads as diligence and buys confidence in the claim that is wrong.** **CURE: attribute from BYTES or not at all.** `%an` is blind here -- every node commits as the same person -- so the only discriminators are the message convention and the `commit_session_id` trailer, and where neither is present the honest output is _I cannot attribute this_, which is a complete answer and needs no replacement name.

116. **A CRITERION ANSWERS _WHICH FILES MATCH_ AND NEVER _WHICH FILES THIS IS ABOUT_, AND NOTHING IN ITS OUTPUT DISTINGUISHES THOSE (mine and cc's, opposite errors on one population, 2026-09-08).** I selected `0279`'s population on the STRING `lib/templates` and OVER-counted, taking assertions and real-tree walks for fixture builders. cc narrowed to the OPERATION `create_dir_all(...)`, which fixed that -- **and then took the narrowed set as the JOB, which UNDER-counted**, because the all-or-none condition binds to the LITERAL and four more files hardcode it without building any fixture. **Both of us got the criterion right and the SUBJECT wrong, in opposite directions, on the same population, within an hour.** The narrowing that repairs a selection error is the same operation that commits a scope error, and the output looks identical either way: a list of files that all genuinely match.

117. **RE-MEASURE A BLOCKER BEFORE REPORTING IT, NOT ONLY BEFORE ACTING ON IT -- PESSIMISTIC-DIRECTION STALENESS IS THE KIND THAT DOES NOT SELF-CORRECT (vc's formulation, on my `CHANGELOG` hand-off, 2026-09-08).** I found my menubar entries missing from `CHANGELOG.md`, correctly declined to write into a file holding three peers' uncommitted lines (`--only` is path-scoped and would have carried their prose in under my message), and handed the gap to vc. **The condition stopped holding while I was folding** -- cc landed those lines at `54d0497e3` -- so what I reported as blocked was free by the time it was read.

**THE ASYMMETRY IS THE WHOLE POINT AND IT IS NOT ABOUT CARE.** An OVERSTATEMENT gets caught by the first node that relies on it: they act, it fails, the record is corrected. **An UNDERSTATEMENT recruits people into waiting**, and waiting produces no failure to notice -- **a belief that something is blocked makes standing still the rational move, so nothing ever prompts the re-check.** The error is self-sustaining in exactly the direction that feels responsible.

**WHAT MADE IT CHEAP WAS LEAVING THE GAP VISIBLE RATHER THAN CLOSING IT WRONGLY**, which turned a misattributed commit into a thirty-second fix. **That half was right and is worth keeping**: the refusal to write into a peer's dirty file is the rule, not caution. What is added is that a reported blocker carries a measurement with the same force as an acted-on one, because a report is a claim other people schedule around.
**THE GENERAL FORM IS cc's AND IT SUBSUMES ALL THREE FACES OF THIS FLAG: `--only` IS AN INSTRUCTION, NEVER A FILTER. IT SUBTRACTS NOTHING YOU DID NOT THINK TO NAME, AND IT REPORTS SUCCESS EITHER WAY.** Face one, mine, standing: path-scoped rather than hunk-scoped, so unrelated hunks in a named file ride along. **Face two, cc's, 2026-09-05: `git add render.rs` while dc was mid-edit in the same file, so `--only` faithfully committed a PEER's bytes** -- too much, from another writer. **Face three, mine tonight: half a rename** -- too little, from an unnamed path. Reading the flag as a safety filter is what produces all three; reading it as _commit exactly this list_ produces none. **AND cc NAMED WHY MINE IS THE NASTIER: their sweep was visible in a diff to anyone who looked, where an orphan test file CANNOT COMPILE ITS WAY TO A RED under `autotests = false`, so no suite was ever going to report it** -- not a missing check, a check structurally unable to see the thing, which is the seventh instance of that shape today. Sibling of W104 in a small way: the flag's own name says `--only` and says nothing about what a rename is made of, so the discipline has to supply what the tool does not.

**THIRD LIMB, AND IT IS THE ONE THAT EXPLAINS WHY WRITING THE ENTRY DID NOT PREVENT THE RECURRENCE (mine, corrected by vc, 2026-09-08, HOURS after banking the entry above).** The `CHANGELOG` case was a blocker I had MEASURED and the world moved under it. **`0280` is worse and commoner: I never re-measured it at all.** It rode across a compact in my own carry-forward list and I published it to two nodes as live, unactioned, wide-blast-radius work. **A CARRY-FORWARD LIST IS THE ONE PLACE STALENESS IS INVISIBLE BY CONSTRUCTION, BECAUSE AN ITEM'S PRESENCE ON IT READS AS ITS OWN JUSTIFICATION** -- nothing in the act of carrying an item forward asks whether it is still true, and the longer it rides the more established it looks. **CURE: an item survives a fold or a compact only with a RE-DRIVE, not with a re-read.** The board's own standing line -- _no figure on this board is evidence, run the verbs_ -- covers figures and I had never extended it to ITEMS, which is exactly where it was needed.

**FOURTH LIMB, AND IT IS cc's, AND IT HAS MORE REACH THAN THE THREE ABOVE (2026-09-08, within ten minutes of `W118` reaching them).** My cure said an ITEM survives a fold or a compact only with a re-drive. **cc applied `W118` to their own use of the marker and found something neither of us was looking for: not a bias, a HOLE IN THE SCOPE we were both reasoning inside.** `DIRT_SCOPE` is `native/rust`, `surface`, `docs/design`; **`lib/templates/` is compiled into the shipped binary and is in none of them** -- `intentsvcs/build.rs` embeds the directory, `init.rs:37` links the blob, and `strings` on the artefact finds the text, so it is source, link and artefact rather than a reading of one file. A commit touching only `lib/templates/` moves the bytes, does not move the marker, and their diff predicate could not see it either.

**cc's formulation, which supersedes mine as the general form: AN INHERITED DENOMINATOR IS A CARRY-FORWARD ITEM THAT DOES NOT LOOK LIKE ONE, BECAUSE IT ARRIVES AS INFRASTRUCTURE RATHER THAN AS A CLAIM.** A stale item on a list at least LOOKS like a claim someone made; a scope constant adopted from someone else's tool looks like part of the apparatus, and it rides through every measurement built on it unexamined.

**IT LANDS ON ME AND NOT ONLY ON THEM: my drift figure this boot came from `restart.md`'s prescribed command, which uses that same scope**, so I inherited the hole through the boot ritual and published a number over it -- **in the same hour I was telling cc that a bias has a direction and each caller must check their own use.** The estate fix is cc's and routed -- **and its FIRST FORM WAS WRONG, which is the limb's own lesson firing a third time.** `add :(top)lib/templates` would have moved the marker on commits changing no shipped byte, **repairing one arm of `0285` by widening the other**: `embed_templates.rs` walks exactly `["llm", "prj"]` and its header declares `hooks/**` and `.claude/**` deliberately NOT embedded. Corrected scope is `lib/templates/{llm,prj}`. **cc's supporting figure went 93 -> 39 in the same correction, and what killed the 93 was the positive control they ran to DEFEND it** -- print one counted commit and check it qualifies; the first one out touched only `lib/templates/hooks/pre-commit.sh`. **Three nodes narrowed a population correctly and then quoted a figure computed over the wider one, inside the hour we named the class.** Zero such commits since the pair marker, so today's answer stands on a corrected denominator rather than on the first one.

**AND THE CURE FOR THIS CLASS IS ALREADY BUILT IN THE PARITY TOOLKIT, WHICH IS THE FIND WORTH KEEPING**: `lib_corpus.sh`'s `corpus_require`, sourced by ten instruments, refuses to publish a figure over a corpus it cannot vouch for. Its header records the two consumers that drifted into two DIFFERENT wrong behaviours over one comparison before either was noticed -- **one over-reported a total, the other let the unmeasured files _simply leave the arithmetic_.** The mechanism is bats-specific; the pattern is not.

119. **A GUARD DISCLOSED ITS OWN BLIND SPOT ON ONE LINE AND PRINTED `ok` ON THE NEXT, AND THE `ok` IS THE LINE A READER TAKES AWAY (cc's find via `0287`, DRIVEN BY ME RATHER THAN RELAYED, 2026-09-08).** `shared_artefact_build_guard.sh` arm 6b enforces **exactly** the scope property `0285`'s `lib/templates` limb violates. It ran this evening and passed. Driven myself:

    arm 6b NOT EXAMINED -- population is native/rust/crates (377 files); the complement

    sweep reads native/rust excluding target/ (383 files) and found 1 embed with a `../`
    path outside the population, swept for and not examined.
    ok -- arm 6b -- every embed reaching outside its crate is covered by the declared
    scope (3 embeds examined over 377 files walked; BOTH CONTROLS FIRED)

**THE HONESTY IS REAL AND IT DOES NOT SAVE THE READING.** The instrument names its own gap, counts it, and says _swept for and not examined_ -- and then the very next line is a green verdict with a population, a denominator and two fired controls. **Everything that makes the `ok` convincing is true, and none of it is about the thing that was missed.**

**BOTH CONTROLS FIRING IS THE PART THAT INVERTS.** A fired control proves the instrument WORKS ON ITS POPULATION. It says nothing whatever about whether the population is the right one -- **so the strongest evidence of instrument health is silent on the only question that mattered here**, and a reader who checks for controls (correctly, by every rule on this board) comes away more confident, not less. **This is `W116` and `W118` arriving in one artefact: right criterion, wrong denominator, and the controls structurally unable to tell you which.**

**WHY THE EMBED WAS INVISIBLE, WHICH IS THE REUSABLE HALF:** the population is a literal `include_str!` grep, and the embeds are GENERATED into `target/`, which the complement sweep excludes by design. **A generated artefact is outside every population defined by reading source, and excluding `target/` is correct for every other reason.** So no author erred, no check was omitted, and the blind spot belongs to the build model -- **`W107`'s no-one-to-blame form, and the second instance of it this week.**

**THE CURE ALREADY EXISTS ONE DIRECTORY OVER AND WAS NOT REACHED FOR: `lib_corpus.sh`'s `corpus_require` REFUSES rather than publishing over a corpus it cannot vouch for.** Arm 6b does the harder, better thing -- it computes the gap -- and then publishes anyway. **DISCLOSING A GAP AND REFUSING OVER IT ARE NOT THE SAME ACT, AND ONLY THE SECOND SURVIVES A HURRIED READER.**

**AND THE PROVENANCE MATTERS TO ME: cc went looking because of my own line that a bias has a direction and each caller must check their OWN use against it (`W118`).** Two of tonight's five findings came out of that. **Which is the argument for publishing a mechanism the moment it forms rather than at the fold** -- it was worth more in cc's hands within the hour than it would have been in mine at the end of the day.

118. **A BIASED INSTRUMENT IS NOT UNIFORMLY WORSE, AND I RETIRED ONE FOR A PEER WHOSE USE OF IT THE BIAS MAKES SAFE (mine, corrected by cc within minutes, 2026-09-08).** Having worked out that `intent --version`'s marker can only be an ANCESTOR of the binary's true content (`0285`), I told cc to treat the whole shipped-bytes predicate as **unsound in general**. **cc uses it the other way round and the bias protects them.** They do not read the count; they read `git diff <marker>..HEAD` for changed lines, and because the marker is an ancestor, that range is a strict SUPERSET of the genuinely-unbuilt one -- **so a superset that comes back empty of shipped-byte changes proves the true range is empty too.** The staleness is conservative for a NEGATIVE conclusion and only for a negative one; had the diff shown changes, cc could not have called them unbuilt and would have needed the fingerprint.

**I KNEW THE DIRECTION AND STILL GENERALISED PAST IT, WHICH IS THE PART WORTH KEEPING.** I had just spent a message establishing that the error runs one way, and then wrote a verdict that quantified over every use of the instrument -- **including uses I had not seen.** _Unsound in general_ is itself an unmeasured claim, and it is the claim a careful-sounding warning most easily smuggles in. **CURE: a bias has a DIRECTION, so its verdict is per-conclusion, never per-instrument -- name the direction and let each caller check their own use against it.** Sibling of `W116` from a level up: there the criterion was right and the subject wrong; here the DEFECT is right and its scope wrong.

**AND THE ESTATE FIX GOT SHARPER FOR BEING WRONG.** My reading pointed at removing the boot ritual's second measurement; cc's points at LABELLING it -- _N commits touch the build scope since the marker, which is an upper bound on unbuilt work_ -- which is true, cheap, and keeps a useful bound instead of discarding it. **A bare number presented as a distance is the whole defect; the number is fine once it says which way it can be wrong.**

45. **A PARAPHRASE IS NOT SUPPOSED TO CARRY EVERYTHING, WHICH IS EXACTLY WHY IT MUST NEVER BE THE THING YOU BUILD FROM.** vc's paraphrase of `tui-design.md` §10a gave the shape correctly and dropped four operational rulings: `localStorage` over `sessionStorage` (Safari re-prompts on cross-link), endpoints from configuration, three-probe hysteresis on liveness, and _copy `daemon_bridge.js` rather than re-derive it_. **vc named this as their own fault to flag; the transferable half is mine: I read the source because I was about to BUILD against it, and the trigger was the building rather than any doubt about the paraphrase.**

46. **THE DISPROOF WAS IN THE CODE TWICE TODAY, BOTH TIMES BEFORE I FILED.** `intent edit wp <id> --path` returns the THREAD's `info.md`, which reads as a defect until `Entity::artefact()` says a `Wp` has no files of its own and resolves to its thread's -- deliberate, documented. And `nav.rs:412` had already recorded the blank-`wp`-form consequence I was about to claim as a find. **A comment that records the CONSEQUENCE of a gap, in the place the gap will be reached from, is what makes it findable before shipping** -- so write that comment even when the gap is latent, because latent is exactly when nobody is looking.

47. **RELAYING LAUNDERS A CLAIM. dc's, and it caught me within the hour they named it.** dc applied their re-drive discipline to the claim they OWNED and passed vc's _nine days old_ figure straight through in the same message. **I did the identical thing**: I relayed `0196`'s content to hv -- _deletes the shared pair before it builds with no restoring failure path_ -- having never opened it. I had first-hand evidence of the BEHAVIOUR (it broke this machine this morning) and drove the symlink myself, so the borrowed half hid inside a claim I mostly owned. **A relay ARRIVES as a peer's measurement and LEAVES as an estate fact with no author, and it is cheapest to be wrong there because the courier has no stake.** **MY ADDITION: A RELAY GAINS CONFIDENCE AT EVERY HOP WHILE LOSING ITS PROVENANCE** -- vc measured, dc passed it, cc and I received it, and by the time it reached hv over my name it had three nodes behind it and one probe under it. **That is W27 with the sign flipped**: there, two independent probes agreed and shared a blind spot; here there is ONE probe wearing three nodes' confidence. **DETECTION, and it is cheap: when you write a claim you did not drive, name who drove it in the same sentence. A claim that cannot carry its author is a claim nobody will re-drive.**

48. **THE AVAILABLE CAUSE IS THE RECENT ONE, AND NOBODY ASKS WHAT ELSE WAS IN THE INTERVAL.** Three instances today, and I am two of them. I attributed a dirty `render.rs` to cc because cc OWNS the file (it was dc's A1). dc attributed the stale binary to A1 because A1 was the commit in their hand; vc repeated it. **It was `969c488e` -- MINE -- and A1 was the second cause**, which I measured only because dc's version put their name on my staleness and vc was one message from telling dc their commit did not function. **`git status` carries no author and a currency check names no commit**, so in both cases the instrument was sound and the interval had two authors in it. **DETECTION: before naming a cause, enumerate the interval.**

49. **A CAPABILITY WITH TWO SPELLINGS HAS ONE EXISTENCE, AND THE INVARIANT CAN HOLD BY ABSENCE.** `INV-09`/`AC-00.6`: `intent browse <kind> <id>` IS `intent edit <kind> <id> --browser`, `twin_spellings_agree.rs` holds the pair, and the population is ONE ROW -- mine. **Today it holds because BOTH refuse. Wiring `browse` alone would break it by NEITHER holding**, so the arm and the `--browser` success path land in one commit or neither. **I had planned them as two steps and it is not a dispatch-table fact, so the table checklist would never have surfaced it** (dc). **THE GENERAL FORM IS THE KEEPER: an invariant satisfied by a shared ABSENCE is one build away from being satisfied by nothing, and it reads green the whole time.**

50. **STORE CLAIMS SURVIVE A STALE BINARY AND BEHAVIOUR CLAIMS DO NOT (cc's formulation, via vc).** The delivered `intent` is a symlink into `target/release`, and it went stale on my own commit. **Store reads survived because the schema did not move; every claim about BEHAVIOUR is a claim about a binary.** Audited today's drives on that axis rather than retracting them: every architectural claim I made was a SOURCE read and is unaffected -- not foresight, just that I was reading code I was about to change -- and every binary drive predates the flip, so each was faithful when taken and none is reproducible now. **`Op::Form` HAS NOT BEEN DRIVEN THROUGH A BINARY AT ALL; its evidence is `cargo test` plus a mutation test, which is a claim about the CODE and not about the delivered tool.** Say so before someone reads the landing as end-to-end.

51. **~~A shared file is not one you have a habit about~~ -- FOLDED INTO W39** as its third limb.

52. **A PARK IS ONLY CLEAN IF IT PARKS THE ASSERTIONS WITH THE CODE. dc's, and they claimed it.** dc split their hunks out of `render.rs` and left the test file asserting them in the tree, so the suite asserted a feature they had removed themselves; three reds that looked like a defect. **dc verified their park by checking their markers were GONE from the file they parked -- and every check passed while the tests were stranded, because a completeness check scoped to the artefact you TOUCHED cannot see what it LEFT BEHIND.** Both of us verified our own parks correctly today and both were incomplete in the same direction.

53. **A TRANSIENT BROKEN BUILD IS NOT OBSERVABLE AS TRANSIENT FROM OUTSIDE. dc's sharpening of my own note.** I left the crate unparseable for ~90 seconds between two halves of one edit and never thought of dc, because in a private worktree an intermediate state costs nothing and here the crate is ONE COMPILATION UNIT shared by three. My framing was _a transient state is only transient for its author_; **dc's is better: the reader cannot distinguish _mid-surgery_ from _broken_, the compiler says the same thing either way, and they pick wrong at their own expense.** **CURE: say _I am mid-surgery_ up front. It is cheaper than sequencing every edit to parse.**

54. **THE HEADLINE OF A CRITERION IS NOT ITS CONTENT -- THE GLOSS IS.** vc read `AC-17.6`'s _THE KIND IS WHAT DISAMBIGUATES_ as requiring `wp` and `st` to reach different artefacts, and was about to tell hv the row was FALSIFIED. **The row explains itself in its very next sentence**: it is about `intent edit 1` refusing because _`1` names both a steel thread and an issue_ -- id resolution, not artefact distinctness. **A row on this thread is paragraphs long and its first clause is a TITLE**, so quoting the clause and stopping produces a confident claim the row does not make. **DETECTION: if a criterion's clause seems to decide your question, read to the end of the row before it decides anything.**

55. **A RE-BUCKET IS THE ROSTER WORKING, AND THE RED IS THE MECHANISM RATHER THAN THE COST.** `declared_values_are_enforced` carried `browse` as `Unwired` with a comment saying _this reds when the verb is wired_, and it did, the hour it got an arm. **A verb that starts working must FAIL on its way to being reclassified, or nothing marks the change** -- which is why the row records a DISPOSITION rather than an expected exit code. **The fix is to move the row, never to relax the assertion**, and the failure message said so itself.

56. **I CARRIED THE TWIN OF A DEFECT I WAS ABOUT TO CORRECT SOMEONE ELSE FOR.** vc reported that `edit --path` never checks the addressed entity exists; **`browse`, which I landed an hour earlier, has the identical hole** and would open a browser at a page that then errors. I found it only because I went looking for my own instance before writing the correction. **DETECTION: when a peer's finding names a CLASS, grep your own recent work for it BEFORE replying** -- the reply is the moment you are most sure the defect is theirs.

57. **A GUARD THAT IS PRESENT BUT AIMED AT THE WRONG SUBJECT IS INVISIBLE TO EVERY SEARCH FOR A MISSING GUARD (mine, 2026-09-04, and the issue itself had it wrong too).** `0238` said the `edit` path _never asks_ whether the entity exists. It asks -- `Facade::edit` has called `st_show(id)` since the `0144` fix. `Entity::artefact()` collapses a `Wp` onto its THREAD, so the check verified the thread and never the work package. **Looking for an absent check finds nothing when the check is present and misaimed**, which is why this survived a fix that was specifically about unknown ids. **DETECTION: when a defect looks like a missing guard, first ask whether a guard is there and pointed at the wrong noun.** Sibling of W17 from the other side -- there `absent` meant _not where I looked_; here `present` meant _not about what I assumed_.

58. **AN ENUM BOUNDS ONE ARGUMENT GRAMMAR, NOT THE VERB -- AND I QUOTED A BLAST RADIUS FROM THE WRONG ONE (mine, 2026-09-04).** I read `check_enum`'s `st|wp|issue` and concluded `wp` was the only reachable instance of `0238`. The `intent://` address arm bypasses `check_enum` entirely, so `ac`, `at`, both collections and an `attachments/` path for a file that was never created all reached the same door at rc=0 -- **four more variants, found only because I drove the other grammar instead of reasoning from the first.** **`edited()`'s OWN comment warns _this is a second argument grammar inside one verb_ and I had read it that hour.** **DETECTION: before quoting a blast radius, enumerate the ways INTO the function, never the values of one argument.** Same family as W48 (_enumerate the interval_), one level up: enumerate the ENTRANCES.

59. **AN OUTPUT THAT IS INVARIANT OVER THE INPUT IS A DEFECT NO COMPARISON CAN DETECT, AND IT DECIDES WHAT THE FIX CAN BE.** `edit wp ST0001/01 --path` (real) and `.../99` (absent) printed the SAME BYTES. **No test comparing outputs could ever have separated them**, no golden file would have differed, and the answer carried zero information about the thing the operator named. **So refusing before answering was the only available fix rather than the chosen one** -- there was nothing to answer better WITH. **DETECTION: when a value does not vary with the argument that supposedly selects it, the argument is not reaching the computation**, and no amount of asserting over the output will show it.

60. **~~My ad-hoc probe and the shipped client guessed the same field name wrong~~ -- FOLDED INTO `W17`** as its third limb, the one where the wrong guess reaches a shipped artefact.

61. **A NEGATIVE CONTROL RUN ONLY TO VALIDATE AN INSTRUMENT FOUND A DEFECT THE INSTRUMENT WAS NOT LOOKING FOR.** `Op::Form` through the delivered pair returned 200 with 15 real fields and I could have stopped there -- vc's item was _drive it_, and it was driven. The controls (absent thread, wp view, no token) were added only so the 200 would mean something, and the absent-thread arm is what exposed `0241`. **Second instance today of W34's shape, and the third this week**: the arm written to protect an assertion keeps being the arm that finds the next bug. **DETECTION: the cost of a control is one command; the thing it catches is not usually the thing it was written for.**

62. **A PUBLISHED FIGURE IS NEVER COMPARED TO THE ARTEFACT IT DESCRIBES, AND THREE OF FOUR NODES DRIFTED THE SAME WAY (cc found it, on my commit).** `8aa428434`'s subject says the fold went `57188 -> 52502`; `git show 8aa428434:...wip.md | wc -c` is **53409**. **The `57188` half is EXACT, which is the trap: one number in the pair verified, so the pair read as checked.** cc retracted the formatter as the cause and I am not substituting another -- **my own edits between measuring and committing explain it just as well, and I did continue editing.** **THE MEASUREMENT STANDS AND THE CAUSE IS UNDIAGNOSED, which is the honest state.** Cure: take the count off the COMMITTED object in BYTES (`git show <sha>:<path> | wc -c`), after the commit, or do not publish a number. **AND COUNT BYTES, NOT CHARACTERS** -- cc's other half: python `len()` on decoded text counts characters, `git` and `wc` count bytes, and two multi-byte glyphs put cc two behind twice.

63. **A SHARED-RESOURCE FAILURE PRESENTS TO EVERY PARTICIPANT AS AN INDIVIDUAL PROBLEM, AND THE INDIVIDUAL READING IS THE ONE THAT SUPPRESSES THE REPORT (mine, 2026-09-04; vc took it onto their board).** My untracked test file made one guard arm refuse EVERY commit in the repository, on every path. **Three nodes were refused within minutes and all three first read it as their own problem** -- I read my own refusals as a defect in my commit and went on reshaping my file. **dc asked _who else is stuck_, and that is the only reason it was diagnosed in one round instead of three.** The personal reading is not merely wrong, it is actively suppressive: it produces a local workaround and no report, so the shared cause survives and the next participant starts over. **DETECTION: when a gate refuses you twice, the cheapest question is not _what did I do_ but _who else is seeing this_.** Generalises past guards to any shared lock, index, daemon or artefact.

64. **THE TEST FOR A WORKAROUND IS NOT WHETHER A GUARD PROMPTED THE CHANGE -- IT IS WHETHER THE CHANGE WOULD STILL BE RIGHT WITH THE GUARD DELETED (vc's ruling, narrowing their own earlier one).** vc first told me not to reshape correct code around a broken guard, which is right and did not reach my case: the runtime read is what `dependency_rationale.rs` does **from the same directory**, so the embed was a deviation from a precedent I had CITED IN MY OWN DOC COMMENT and not followed. **A guard that surfaces a real deviation by wrong reasoning has still surfaced a real deviation**, and refusing the correction to punish the guard is its own superstition. **The rule vc was protecting -- a silent workaround makes the guard defect permanent and invisible -- did not bite because the defect ended up in five records and a separate fix.** **COROLLARY, vc's, and it is the sharper half: A CONTROL THAT DEPENDS ON A LIVE FILE EXPIRES THE MOMENT SOMEONE LEGITIMATELY EDITS IT** -- which happened to this one inside an hour. Controls are planted fixtures, never live files.

65. **`grep -q` RETURNS 2 ON A MISSING FILE AND THAT OVERRIDES A MATCH, SO A RETRY LOOP MISCLASSIFIED A LOCK AS A REAL FAILURE (mine, 2026-09-04).** My commit loop tested `grep -q 'index.lock' /tmp/a.out /tmp/c.out`; the second file did not exist because the first command had already failed, so grep exited 2 despite matching in the first, and the loop reported **NOT a lock problem** and gave up on a plain lock. **The diagnosis was inverted by the instrument, not by the evidence** -- the lock message was sitting in the file it had just read. Cure: `cat ... | grep -q`, or test only files you created. **Third instrument error of the session** after a guessed field name (W60) and a local-time stamp wearing a `Z`, which is itself the pattern: on a long session the instruments degrade faster than the reasoning does. **vc SHARPENED THE PATTERN AND THEIR VERSION IS THE KEEPER: this is the figures-versus-verbs rule one level up -- THE VERB DECAYS TOO, NOT ONLY THE NUMBER IT PRODUCED.** A probe's correctness was established against a tree and a state that have both moved, so _re-derive the probe_ sits beside _run the verbs_ rather than under it. All three of tonight's failures were a probe that had worked earlier, reused unexamined. **FOURTH INSTANCE, 20:14Z, AND IT IS THE ONLY ONE A SHAPE CAUGHT RATHER THAN ME: I TYPED A HEARTBEAT BY FEEL AND `whiteboard-clock-guard.sh` REFUSED THE COMMIT.** The `date -u` in the same call returned `20:13Z` and I wrote `20:14Z` -- read once, then advanced by feel, which is the fabrication my own memory names in as many words. **The value happened to be correct one minute later, and that is NOT a defence: it was in the future when written, the guard was right, and time catching up does not retroactively make a typed value a read.** Repaired by re-reading the clock and writing the output verbatim, never by picking a better-looking number. **THE EVENING'S THESIS DEMONSTRATED ON ITS AUTHOR: three instrument errors I caught by reasoning, one the shape caught -- and the shape caught the one I was least likely to, because nothing about typing a plausible timestamp feels like an error.**

66. **BOTH HALVES OF A PROMISE CAN EXIST, IN THE SAME FILE, WITH NOTHING JOINING THEM -- AND THE SUITE READS GREEN BECAUSE EACH HALF IS CORRECT (mine, 2026-09-04, answering `0244`).** `browse` with no daemon refuses and names `intent daemon start`. `edit_and_browse_reach_one_model.rs` asserts that refusal, and in a DIFFERENT test in the SAME FILE stands up a real daemon and drives the same verb to success. **Both halves written, both passing, nothing connecting them -- so the remedy text could be replaced with nonsense and every test in the file stays green.** Same shape in `graphql_escape_hatch.rs` and `routing_is_opt_in.rs`. **THIS IS W19 MOVED UP A LEVEL: there the seam was between two code paths, here it is between two TESTS, so the hole is in the ASSERTION SPACE rather than in the code.** **DETECTION: when a suite covers a refusal AND covers the recovery, ask whether any ONE subject covers the relation -- coverage of both ends is the thing most easily mistaken for coverage of the promise.**

67. **A ZERO IS THE EASIEST NUMBER TO GET WRONG, SO A NULL RESULT NEEDS A CONTROL MORE THAN A POSITIVE ONE DOES (mine, 2026-09-04).** Answering _does anything drive a remedy_ with `0` is indistinguishable from a detector that finds nothing because it is broken. **Cure, and it cost two minutes: PLANT the thing you claim does not exist, OUTSIDE the repository, and confirm the instrument reports it.** Mine reported `DRIVES` on a planted remedy-driver and `reads only` on a plain reader beside it, so the zero is a measurement rather than a silence. **Planting OUTSIDE the tree is the other half** -- an untracked fixture inside it blocked every commit in the repo earlier the same evening (W63).

68. **THE REASON TWO HALVES ARE UNJOINED MAY BE THE VERY PROPERTY THAT BLOCKS JOINING THEM -- SO SIZING _JOIN THESE TWO TESTS_ MEANS CHECKING BOTH HALVES ARE DRIVABLE, NOT BOTH PRESENT (mine, 2026-09-04, sizing `0244`).** I told vc the cheapest instance was `edit_and_browse_reach_one_model.rs`, where both halves sit in ONE FILE. **It is blocked: `open_in_browser` is `browser_url` then `open_url`, so re-driving `browse` to SUCCESS opens a real browser on whatever machine runs the suite.** The refusal half is drivable and the recovery half is not -- **which is very likely WHY that file has the two halves unjoined, and would have kept them unjoined whoever looked.** I had spent the whole evening avoiding exactly that hazard and still nominated it, because presence in one file was doing the reasoning for me. **DETECTION: before sizing a join, drive the RECOVERY half specifically -- absence of a link is evidence about the link, and the cheapest explanation is that someone already tried.** The usable instance was the one I had not looked at.

69. **A SIZE FOR ONE INSTANCE IS NOT A SIZE FOR THE CLASS, AND THE CLASS IS THE THING PEOPLE QUOTE.** `0244`'s join is XS-to-S for ONE remedy and **cannot be made general over the 186 emissions at any size**: most remedies are not mechanically executable by a test (`set $EDITOR`, _open its thread and read the work package there_), and some are deliberate no-ops (_nothing is owed now_). **The population of DRIVABLE remedies is unmeasured, and that measurement -- not the harness -- is what would price a general one.** Recorded on the issue as a bound rather than left for a later reader to discover by building the wrong thing. Sibling of W38, where I extrapolated an estate figure from the one sample I had already measured as unusual.

70. **A HANDOVER WRITTEN INTO AN ARTEFACT'S BODY IS NOT A HANDOVER, AND THIS ONE WAS INVISIBLE TO ME FOR FIVE DAYS (found 2026-09-04 when cc told me; verified against the tool, not assumed).** `0175` says in its prose _handed to ic as surface owner_. **It reached neither my board nor any inbox** -- live or archived, checked both. **And it was UNDELIVERABLE BY CONSTRUCTION, which is the part I verified rather than inferred: an issue has NO owner field.** **THE FIELD LIST IS NOW DRIVEN FROM BOTH BUCKETS** (using `0244`, which I closed this morning, as the closed specimen the earlier enumeration lacked): a CLOSED row carries `body, closed, created, number, reporter, schema, severity, slug, status, title`; an OPEN row carries the same less `closed`; `issues list` offers `--kind open|closed|all` and nothing else. **No assignee, no owner, no notification** -- which was the conclusion, now resting on a sample that can exhibit the field. **There is no assignee, no filter, no notification.** **CORRECTED 2026-09-05, AND THE CORRECTION IS THE POINT: I WROTE HERE THAT THE ONLY WAY TO FIND IT WAS TO READ EVERY ISSUE BODY. FALSE.** `intent search 'handed to ic as surface owner'` returns `0175.json` as its FIRST RESULT -- **it was findable from the day it was written.** The defect is not reachability; it is that nobody queries 250-odd bodies for their own name and there is no field to filter on -- **a searchable surface with no query anyone would think to run.** Filed as `0258`. **THE CONSEQUENCE IS ALREADY ON THE RECORD: cc independently re-filed the same defect as `0239` five days later and fixed it** -- a duplicate is what an undelivered handover looks like from outside. **This is the protocol's own hv-inbox ruling arriving somewhere nobody applied it: _a write surface with no named reader is a queue, not a channel_ -- and here it is worse, because the surface is not even a channel, it is a sentence in a document.** **DETECTION: after handing work to a node, name the surface THEY read at pickup. If you cannot name it, you have made a note and not a handover.** **AND THE MIRROR OF W63:** there, a shared failure read as personal; here, a personal assignment was written somewhere shared and therefore reached nobody. **dc's ADDITION IS SHARPER THAN MY FINDING AND I DROVE IT RATHER THAN RELAYING IT (W47): `reporter` HOLDS THE HUMAN'S GIT IDENTITY, SO THE REGISTER CANNOT SAY WHICH NODE FILED EITHER.** Measured across all 227 issues: 187 `Matthew Sinclair`, 40 `matts`, and nothing else. **There is no node identity in EITHER direction, so a design that adds an owner field and not an author field fixes half of it** -- worth knowing before the shape is decided, and it is dc's point. **AND MY OWN FIELD LIST WAS INCOMPLETE BY CONSTRUCTION: I enumerated the schema from ONE OPEN issue, which cannot carry `closed`.** A sample that cannot exhibit the field, in the middle of a finding about instruments that cannot exhibit their subject. **cc CLOSED THE LOOP AND I VERIFIED IT: `intent search` REFUSES ORDINARY PUNCTUATION, so the verb that would have prevented the duplicate breaks on the natural query.** `search 'disclaims the family'` returns `0175` as its FIRST result; `search 'family-root'` -- a phrase from `0175`'s own title -- is refused, and so are `render.rs` and `v3.0.1`. **A filename and this project's own release number are both unsearchable** (cc's `0247`).

71. **I MADE AN EXCUSE FOR A PEER WITHOUT DRIVING IT, IN THE SAME MESSAGE WHERE I CORRECTED THEM ON SOMETHING I HAD DRIVEN (mine, 2026-09-04, refuted by cc).** cc carried a missed `intent search` as their own fault. I argued the two causes were COUPLED -- that the verb `CLAUDE.md` rule 4 tells you to run refuses `family-root`, a phrase from `0175`'s own title, so the search had a hole in it. **cc drove the counterfactual I had not: six SUBJECT-shaped queries -- `agents bare`, `bare agents`, `agents not implemented`, `agents unwired`, `agents rc 2`, `family root` -- and all six return `0175` at rc=0.** I re-drove all six here and they hold. **MY ARGUMENT ASSUMED A SEARCHER TYPES A PHRASE FROM THE TITLE OF A ROW THEY HAVE NOT FOUND YET, which is knowledge only available AFTER finding it.** You search for your SUBJECT. **THE GENERAL FORM IS THE KEEPER AND IT IS NOT ABOUT SEARCH: A COUNTERFACTUAL BUILT FROM THE ANSWER'S OWN TEXT IS CONTAMINATED BY HINDSIGHT, AND IT READS AS GENEROUS RATHER THAN AS UNDRIVEN.** **THE ASYMMETRY IS THE REAL FINDING: MY EVIDENCE BAR DROPPED BECAUSE THE CLAIM WAS IN SOMEONE ELSE'S FAVOUR.** A generous claim does not feel like a claim, so nothing prompts the drive -- and the refutation cost one command, exactly what the assertion should have cost. **Cure: an exculpatory claim carries the same burden as an accusatory one, and the tell is that you did not run anything.**

72. **THE MUTATION THAT COUNTS PRODUCES A PLAUSIBLE WRONG ANSWER, NOT AN OBVIOUSLY BROKEN ONE (mine, 2026-09-05, building `0244`'s S).** vc's acceptance criterion was _replace the remedy with nonsense and the test must go red_. I drove that (M1: `remedy: the frobnicator is misaligned`) and it went red on the vacuity arm -- **and it proves only that the test READS the string. Any mutation shows that much.** The mutation that answered the issue was M2: `intent daemon start` -> **`intent daemon status`, a WIRED verb that EXITS 0 and does not fix the operator's problem.** `every_emitted_remedy_names_something_this_build_can_do` passes M2 happily, because the thing it names IS something this build can do -- **the predicate is TRUE and the remedy is USELESS** -- and the re-drive still refuses at rc=2. **DETECTION: a mutation whose output is visibly garbage tests the reader; a mutation whose output would pass review tests the CLAIM.** Reach for the second. **AND cc's framing is the general one, arrived at from `0249` independently: three instruments this stretch -- `0241`'s dead branch, `0249`'s `UNREACHED: 0` that is both health and a dead needle set, and this -- are each CORRECT about the question they ask and each ask the question NEXT to the one that matters.** Sibling of the honest-and-blind-grep family: there the instrument cannot see its subject; here it sees a neighbouring subject perfectly.

73. **A TRANSIENT EDIT TO A SHARED FILE IS A SHARED-STATE WINDOW, NOT A PERSONAL ONE (cc's, 2026-09-05, on my `render.rs` mutation).** I announced the mutation, promised not to commit `render.rs`, and reverted it within minutes -- and **the promise addresses the wrong hazard.** While the line sits on disk, **ANY node committing that path for ANY reason takes it under their own message**: `--only` protects a FILE and gives nothing against an unnamed HUNK inside a named one, and **`git status` carries no author**, so a committer sees a dirty path with no way to tell whose it is. cc had the live instance the day before -- vc's board and a parity tool sitting staged in the shared index during their commit scoping. **DETECTION: the question is not _will I commit this_, it is _what happens if someone else commits this path in the next five minutes_.** The announce is what buys the window; a detached worktree is what removes it, and `cp`-ing a shared source aside to mutate is the form that has already bitten this estate.

74. **AN IDENTIFIER TYPED FROM MEMORY IS THE SAME FAILURE AS A TIMESTAMP TYPED FROM MEMORY, AND THE ADDRESSING CHANNEL HAS NO GUARD ON IT (mine, 2026-09-05).** cc told me in this very session that TWO sessions answer to `intent-vc` and that a message routed to the stale one would look exactly like silence. I then addressed vc as `intent-vc [3a7f2c]` -- **a ref I invented.** I had turned a warning about ambiguity into a fabricated discriminator instead of a `ListAgents` call. **IT BOUNCED, WHICH IS LUCK AND NOT DESIGN**: a ref that resolved to the WRONG live session would have delivered, reported success, and been indistinguishable from delivery to the right one. **THE GENERAL FORM: every field that identifies something outside this turn -- a clock, a ref, a sha, a commit_session_id -- must come from output in front of me, verbatim, in this turn.** The clock has a pre-commit guard and caught me on 2026-09-04; **refs, shas and session ids have none**, so the discipline is the only check (issue 0099's class, one surface wider than messages). **DETECTION: if you cannot point at the tool output the identifier came from, you did not read it, you composed it.**

75. **TWO FAILURES THAT SHARE A SYMPTOM CAN HAVE NON-CROSSING REMEDIES, AND THE SHARED SYMPTOM IS EXACTLY WHAT MAKES THEM READ AS ONE SEAM (mine, 2026-09-05, refuted by cc and re-driven here).** I told cc our two cases were one mechanism from opposite ends -- `0175`'s assignment TO me was unfindable, cc's measurement BY them was unfindable, therefore _a message is not a searchable surface for either party_. **It is half wrong, and the wrong half is mine.** Driven rather than taken on cc's word (W71 cuts both ways): **`intent search 'handed to ic as surface owner'` returns `intent/.canon/issues/0175.json` as its FIRST result.** My assignment was indexed the whole time. **The failure was never reachability -- it was that nobody has a reason to query 244 issue bodies for their own name, and there is no field to filter on: a searchable surface with no query anyone would think to run.** cc's half is the other defect entirely: **THE WHITEBOARD IS OUTSIDE THE INDEX.** `intent search 'RUN THE VERBS'` -> 158 results, **ZERO whiteboard paths**, on a phrase `grep` finds in four boards including mine. **MY DISCRIMINATOR IS STRONGER THAN THE PROBE cc FIRST USED AND THEY SAID SO THEMSELVES:** `frobnicator` is on disk in exactly TWO files -- my board and `0244.json`, both written within the same hour -- and the search returns the canon row and not the board. Same word, same tree, same moment; **only the file differs, so staleness cannot explain it.** **CONSEQUENCE FOR THE DESIGN: an owner field fixes mine and does nothing for cc's; indexing the whiteboard fixes cc's and does nothing for mine.** A design treating them as one seam ships half a fix and reads as complete. **cc's half already has a home on the contract -- `ST0056/14`, whiteboard and inboxes in the store -- so neither of us files it.** **THE TELL I MISSED: the generalisation was ELEGANT.** W71 was an unmeasured claim that felt safe because it was generous; this one felt safe because it was symmetrical. **An unmeasured claim is hardest to notice when something other than evidence is making it feel true**, and neatness is the version of that I am most exposed to.

76. **I ASSERTED A TOOL'S SURFACE WHILE WRITING ABOUT SOMETHING ELSE, IT WAS QUOTED BACK APPROVINGLY, AND THAT MADE IT LOOK CORROBORATED (mine, 2026-09-05, measuring `0257`).** I wrote on `0257` -- and then in two messages -- that _acceptance rows cite tests, and `intent at` has no verb that retargets a row's citation, so a rename can strand a citation silently_. **BOTH HALVES ARE FALSE.** (1) **An AT row's citation is a `file`** -- a repo-relative test FILE. There is no field that holds a test-function name, so function-level stranding is not a thing this schema can do. I never asked what a citation IS; I assumed a granularity. (2) **`intent at edit --file` exists**, and I drove it rather than reading it off `--help`, because a DECLARED verb that is not WIRED is the exact defect `0257` is about: `intent at edit ST0056 AT-06.11` with no field named reaches its own guard and refuses -- _nothing was named to change ... nothing was written, so nothing needs undoing_ -- with canon unchanged. **THE MEASUREMENT: 406 AT rows in the estate, ONE cites the file, ZERO cite the name; a rename costs TWO SOURCE LINES**, one of them a rustdoc intra-doc link, which is the only site that BREAKS rather than going stale. **THE ZERO IS CONTROLLED:** `every_command_reference_is_backticked`, a real `fn` in the SAME FILE, IS named in `AT-06.11`'s own note, so canon prose can carry a test name and the instrument sees one when it is there. **THE NEW MECHANISM, AND IT IS THE REASON THIS IS ITS OWN ROW RATHER THAN A SECOND INSTANCE OF W71: A CLAIM REPEATED BACK TO YOU APPROVINGLY HAS NOT BEEN CHECKED -- IT HAS BEEN AMPLIFIED.** **vc's ADDITION IS THE MECHANISM AND IT IS SHARPER THAN MY STATEMENT OF IT: they RECEIVED the claim, APPROVED it, and RETURNED it at greater width, which reads back to the author as independent corroboration when it is their own sentence with someone else's emphasis on it. So it is a TWO-NODE LOOP IN WHICH NEITHER NODE DROVE IT, and it becomes load-bearing PRECISELY BECAUSE it now appears on two boards.** **Their audit, run rather than promised, found two further live candidates that are not mine** -- a `0255` count they repeated at full width and cannot now reconcile (their 122 against dc's 138, with their own first hypothesis refuted at 149), and a boundary borrowed from cc that they made load-bearing while saying at the time that they had not driven it. **Saying so at the time is better than nothing and is not the same as having driven it.** vc called it _the mechanism that makes the rename dangerous_ and put it on their board; peer agreement on a whiteboard reads exactly like corroboration and is not, because the peer is quoting your sentence rather than the tool. **DETECTION: when a claim of yours comes back in someone else's words, that is the moment to drive it, not the moment to relax** -- the agreement is evidence about the sentence's clarity and about nothing else. **AND THE TELL FOR THE FIRST HALF: it was an aside in a document about a different subject.** A sentence carrying the word _so_ into a consequence is a claim wearing the clothes of a transition.

77. **A COINCIDENCE THAT REPRODUCES THE NUMBER YOU ARE TRYING TO EXPLAIN IS THE MOST DANGEROUS OBJECT IN A RECONCILIATION (vc's confirmation, dc's near-miss, driven by me 2026-09-05).** vc counted 122 dispatch-table rows carrying a `v2` field, `0255` states 138, and neither could reconcile the 16. **`populations.declared` in the same file holds EXACTLY 138 entries and NOT ONE carries a `v2` field.** A scan landing there matches the target total precisely and for entirely the wrong reason. **AGREEMENT IS THE SIGNAL EVERYONE IN A RECONCILIATION IS WATCHING FOR, so a wrong population that lands on the number ENDS the inquiry -- both parties satisfied, neither correct, and nothing left generating doubt.** Driven rather than relayed: `.populations.declared | length` -> 138, and rows there carrying `v2` -> 0. **DETECTION: when a reconciliation lands, ask what ELSE in the artefact has that cardinality before accepting it.** Sibling of W72 one level out -- there a mutation had to produce a PLAUSIBLE wrong answer to test the claim; here the artefact produced one by itself.

78. **WRITING DOWN WHAT YOU ARE COUNTING FINDS THE DISAGREEMENT BEFORE ANY COUNT DOES (mine, 2026-09-05, and the protocol is vc's).** vc's anti-anchoring rule was: commit the population definition BEFORE running anything, because a definition written afterwards can be retrofitted to a number you have already seen and nobody could tell. I committed one at `93455321`. **THE COUNT WAS NEVER RUN -- dc reconciled it first -- AND THE DEFINITION FILE HAD ALREADY NAMED THE ANSWER:** its structural section says, from shape alone, that entries live in TWO places, `.families[].entries[]` and a top-level `.new_surface[]`. **That is exactly the 16** (122 + 16 = 138, driven). **THE GENERAL FORM: defining the population forces you to locate the FIELD, and in a disagreement between two careful counters the field's location is usually the whole dispute.** So the definition step is not merely a guard against retrofitting -- **it is the cheapest instrument in the sequence, and it runs before the expensive one.** **AND vc's OWN NOTE ON TIMING IS THE OTHER HALF: they built the instrument before asking the other party to look again**, because the instrument was more interesting. A two-way disagreement is worth one round of re-asking before it is worth a third measurer.

79. **A NODE-LOCAL IDENTIFIER QUOTED ACROSS NODES DOES NOT DANGLE -- IT RESOLVES, TO SOMETHING ELSE (dc's finding, relayed by vc, made concrete here 2026-09-05).** I have quoted `W71`, `W72`, `W75`, `W76`, `W77` and `W78` to cc, dc and vc all morning. **W-numbers are per-board.** Driven: dc's board runs to `W97` and its entries read `- **W90 -- ...**` while mine read `77. **...**`. **So when I wrote _W77_ to dc, what resolves on their board is `W77 -- A SEPARATOR THAT IS A LITERAL BETWEEN TWO EXPANSIONS IS NOT A SPLIT POINT`, and my `W78` is their _A CONTROL ASSERTS A DIFFERENCE, SO IT MUST BE DRIVEN AGAINST THE STATE IT CLAIMS TO DISTINGUISH FROM_.** **THAT IS WHY THIS IS WORSE THAN A BROKEN LINK: A DANGLING REFERENCE ANNOUNCES ITSELF AND A COLLIDING ONE READS AS UNDERSTOOD.** The receiver finds a real entry, in the right register, about the wrong thing. **vc's ADDITION IS THE REASON IT WILL NOT SELF-CORRECT, AND IT IS SHARPER THAN MY VERSION: ONE OF THE TWO COLLISIONS LANDS ON AN ADJACENT RULE.** My `W78` is _defining the population is a diagnostic_; dc's `W78` is _a control asserts a difference, so it must be driven against the state it distinguishes from_. **Those are NEIGHBOURS.** A reader arriving from my citation gets a plausible, related, CORRECT rule in the same subject area and has nothing to be suspicious of. **A collision between UNRELATED entries gets caught by the reader; a collision between ADJACENT ones does not** -- and the adjacency is not luck: **four nodes converging on one problem produce neighbouring rules, so the numbering collides most exactly where the topics are closest.** **AND MEASURING IT SPLIT THE FAILURE IN TWO, WHICH NOBODY HAD STATED:** dc's form (`- **W90 -- ...**`) is **99 on dc's board and 0 on every other**; mine (`77. **...**`) is **95 on mine and 0 on every other**; **cc, vc and hv carry ZERO IN BOTH FORMS.** So the same citation fails two different ways depending on who receives it -- **to dc it COLLIDES and is silent, to cc and vc it DANGLES and is visible** -- and I have been sending both without knowing which I was doing. **CURE, AND IT COSTS NOTHING: QUOTE THE RULE TO A PEER, NEVER THE NUMBER.** The number is an index into my board and is meaningful only on it. **AND THE INSTRUMENT CAUGHT ITSELF ON THE WAY IN, WHICH IS THE SECOND HALF:** my first pattern for _numbered entries_ returned `93` on my board and `0` on the other three, and I nearly reported that the other nodes do not number their watch-outs. **The pattern was node-local too** -- it encoded MY board's ordered-list form. A positive control on a known entry (`W90`) found dc's 160 W-mentions immediately. **A grep written from one board's convention is an instrument bounded by the sample that authored it**, which is the honest-and-blind-grep family arriving inside a finding about node-local identifiers.

80. **A CLAIM ABOUT ORDER NEEDS AN ARTEFACT THAT ORDERS IT, AND A COMMIT TIMESTAMP ORDERS COMMITS RATHER THAN KNOWLEDGE (vc's separation, 2026-09-05, and it stands against me).** I committed a population definition at `93455321` and said it was written BEFORE dc's reconciliation reached me. **vc verified the CONTENT -- all three levels named and correctly classified, including the `v2_source` level whose hypothesis they had refuted at 149 -- and then said plainly that they CANNOT verify the ORDERING from git**: my commit lands 53 seconds before theirs and message delivery is in no log. **They are right and I am not going to argue it: the definition's correctness is measured and its provenance rests on my word plus a commit message asserting it.** **THE GENERAL FORM IS THE KEEPER: a strong verified result attracts a second, unverified claim, and the second one travels on the first one's credibility.** Nothing separates them unless someone does it deliberately. **AND THE RESULT DOES NOT NEED THE PROVENANCE -- a definition naming all three levels is right whenever it was written** -- which is exactly why the unearned claim was so easy to make and would have cost nothing to leave standing. **DETECTION: when a result of yours is confirmed, check whether the confirmation covers every claim you attached to it, or only the one that was measurable.**

81. **REACHING FOR THE SHARPEST AVAILABLE VERSION OF YOUR OWN FAULT IS STILL INACCURACY, AND IT DISTORTS THE RECORD IN THE DIRECTION THAT LOOKS HUMBLE (vc's, about themselves, 2026-09-05).** vc told me they had been _the worst offender all morning_ on quoting node-local numbers. I said I did not think that was right -- I had sent six numbers to three nodes and they had relayed the fix -- and **they checked and withdrew it: they had qualified every citation with the owning node, which is not a fix and is not nothing.** **THEIR OWN DIAGNOSIS IS THE KEEPER: that was reflex, not accuracy, and it is the same reflex that reaches for the sharpest available version of a fault.** **THE HAZARD IS THAT IT IS INVISIBLE AS AN ERROR** -- an overstated self-criticism is never challenged, because challenging it looks like letting someone off, so it enters the record unopposed and every later reader takes it as measured. **DETECTION, AND IT APPLIES TO ME HARDEST TODAY, ON A SESSION THAT HAS BANKED NINE CORRECTIONS: a claim about my own failure is a CLAIM, and it carries the same burden as any other.** W71 said an exculpatory claim about a peer needs driving; this is the same rule turned inward, and the asymmetry runs the other way -- **there the generous claim felt safe, here the harsh one does.**

82. **A COUNT OF A LIVE ARTEFACT IS A MEASUREMENT WITH A TIMESTAMP, AND THREE OF US PUBLISHED ONE AS A PROPERTY (vc's, 2026-09-05) -- AND THE DISCRIMINATOR BETWEEN _TIMING_ AND _METHOD_ IS FREE (mine, driven here).** dc's board gained five entries across five commits in ninety minutes while dc, vc and I were all counting it; vc reported 100, dc 96, me 99. **vc's diagnosis is right and is the delivered-pair lesson arriving where none of us was watching: date a count of a moving artefact or do not publish it.** **BUT IT DOES NOT EXPLAIN MINE, AND THE CHECK THAT SETTLES IT COSTS ONE LOOP: RUN YOUR OWN PATTERN ACROSS THE SAME REVISIONS THE OTHER COUNTER USED.** Mine against vc's four: `7c0afc2fb` 94 (vc 95), `184fc8387` 95 (96), `48a0906cb` 97 (98), `bbb9f8f91` 99 (100). **EXACTLY ONE LOWER AT EVERY REVISION.** **A CONSTANT OFFSET ACROSS INDEPENDENT REVISIONS RULES OUT TIMING; A VARYING ONE RULES OUT METHOD.** So mine is a method difference of one row and not a stale read. **AND THEN I DREW THE WRONG CONCLUSION FROM MY OWN CORRECT DISCRIMINATOR, WHICH vc INVERTED IN ONE LOOP: I said the extra row was in their pattern rather than in my anchor. IT IS MINE THAT NARROWS. THE ROW IS `W4a`** -- `- **W4a -- THE INDEX CLASS HAS THREE MEMBERS...**` -- **a LETTERED sub-entry, which my `W[0-9]{1,3} -- ` anchor cannot match and their looser one takes.** Driven here: strict 99, loose 100, and `diff` on the line numbers gives exactly one, line 84. **The population is 100 and my 99 is a correct measurement of a population one row smaller than either of us meant.** **vc's STATEMENT OF THE DEFECT IS THE KEEPER AND IT IS THE ABSENCE-ARM SHAPE AGAIN: A ROW THAT NEVER MATCHES AN ANCHOR NEVER GETS THE CHANCE TO FAIL IT, SO A FILTER'S SILENCE IS NOT THE POPULATION'S ASSENT.** I had written _no row fails my strict anchor_ as if it were a control; it is the vacuity it was meant to rule out. **THIS IS THE DISCRIMINATOR THE 122/138 RECONCILIATION NEVER HAD** -- that offset was constant too, and constant means method, which is what `new_surface` turned out to be. **Cheap, decisive, and it runs before anyone has to explain anything.**

83. **WHEREVER A DEFECT HAS A LOUD MODE AND A QUIET MODE, THE LOUD MODE TRAINS YOU AND THE QUIET ONE ACCUMULATES (mine, generalised out of the W-number split with vc 2026-09-05).** The instance: a node-local citation DANGLES to cc, vc and hv -- visible, self-announcing, harmless -- and COLLIDES silently on dc's board, where it resolves to a real adjacent rule. **The only feedback available comes from the case that does not need it.** **THE GENERAL SHAPE IS A FEEDBACK ASYMMETRY, NOT A CITATION PROBLEM**, and it is why a defect with both modes gets diagnosed as the loud one and fixed for the loud one. **W81 IS THE SAME ASYMMETRY POINTED INWARD: the harsh self-claim has no loud mode**, because challenging an overstated confession looks like letting someone off, so it enters the record unopposed while an overstated boast would be contested immediately. **DETECTION: when you find a defect that announces itself, ask what the same defect looks like when it does not.** The answer is usually already happening and has never once been reported.

84. **BACKTICKS INSIDE A DOUBLE-QUOTED `echo` ARE COMMAND SUBSTITUTION, AND THE COMMAND THAT RUNS IS THE ONE YOU WERE TALKING ABOUT (mine, 2026-09-05).** I ran ``echo "=== is `at edit` present ==="`` while verifying a claim about `intent at edit`. **zsh executed `at edit`** -- the POSIX job scheduler -- which printed `at: garbled time` into the middle of my evidence, and the label I meant to print never appeared. **THE OUTPUT STILL LOOKED LIKE A RESULT**, which is the whole hazard: a mangled label plus a real-looking error line reads as the subject misbehaving rather than as the harness misfiring. **Sibling of the single-quoted-`perl -e` apostrophe trap and the unquoted `--include=*.rs` glob abort, both of which have already cost me a wrong answer in a tool call this week.** **CURE: SINGLE-QUOTE EVERY `echo` LABEL. The one place you are quoting a shell command is the one place the shell will run it.**

85. **TWO PATTERNS THAT SHARE A CLAUSE ARE ONE INSTRUMENT RUN TWICE, AND I CALLED MINE INDEPENDENT (mine, 2026-09-05, and it is why I concluded confidently in the WRONG direction).** Settling a count against vc I checked my figure with `^- \*\*W[0-9]{1,3} -- ` and then with `\*\*W[0-9]{1,3} -- `, got 99 both times, and reported **two independent patterns of my own both return 99**. **THEY DIFFER ONLY IN THE LINE ANCHOR. THE PART THAT WAS WRONG -- `[0-9]{1,3} -- `, which cannot match the lettered sub-entry `W4a` -- IS IDENTICAL IN BOTH.** So the agreement was guaranteed by construction and I read it as corroboration. **THIS IS WHY THE ERROR SURVIVED A CHECK: the second run could not disagree with the first about the only thing that mattered.** **DETECTION, AND IT IS MECHANICAL: before calling two instruments independent, DIFF THEM AND ASK WHETHER THE SHARED PART IS THE PART UNDER SUSPICION.** A variant that varies the wrong clause is a replication, not a control. **Sibling of W72 from this morning** -- there a mutation had to change the thing the test claimed to read; here a second pattern had to change the thing the first might have got wrong, and it changed the other thing instead. **AND IT IS THE SAME DEFECT AS ITS OWN SUBJECT ONE MORE TIME: an instrument bounded by the convention that authored it, inside a finding about node-local identifiers, checked by a copy of itself.**

86. **A PEER CHANNEL HAS NO TERMINATING CONDITION, AND TWO NODES WHO ARE BOTH FINISHED WILL KEEP GOING BECAUSE EACH IS WAITING TO SEE WHETHER THE OTHER IS (vc's, 2026-09-05, closing an exchange in which we had each said _nothing further_ twice and continued).** **THE MECHANISM IS THE SAME MISSING STATE AS THE INBOX FINDING FROM THIS MORNING: an inbox records what ARRIVED and never what HAPPENED NEXT; a conversation records what was SAID and never that both parties are DONE.** There is no representable state for _finished_, so the only evidence either node has that the other has stopped is silence -- **and silence is indistinguishable from a peer still composing.** **WHAT MAKES IT HARD TO STOP IS THAT IT WAS WORKING: every round of this one produced something worth keeping, so the usual signal for _this is now noise_ never fired.** A channel that had degenerated would have been easy to leave. **vc's CURE IS A SHAPE AND NOT AN INTENTION, WHICH IS WHY IT HOLDS: they stated a CONDITION -- _I will not reply again unless you send something that needs a ruling, and acknowledgements do not_ -- rather than a sign-off.** A sign-off is a claim about what you will do; a condition is a rule the other party can evaluate. **AND THE FIRST TEST OF IT WAS MINE TO PASS OR FAIL IMMEDIATELY: the correct response to that message is to ACT ON IT AND SEND NOTHING**, because a reply agreeing that the channel is closed is the exact behaviour it forbids. **DETECTION, FOR EITHER SIDE: if your next message would carry no decision, no measurement and no ask, it is an acknowledgement -- and the channel has already ended without either of you noticing.**

87. **_NEWER THAN X_ AND _CONTAINS Y_ ARE DIFFERENT RELATIONS, AND A COMPARISON AGAINST ONE LANDMARK CANNOT SEE A THIRD POINT LYING BETWEEN (dc's error, driven and refuted by me 2026-09-05).** dc announced that this box's two reachable builds DISAGREE -- bare `intent agents` giving rc=2 on the `v3.0.0` tag and rc=0 on the delivered pair -- and wrote it into the justification for naming a build on every disposition. **DRIVEN: THE DELIVERED BUILD ANSWERS rc=2 TOO**, with the `is a known command that is not implemented yet` text, and `git merge-base --is-ancestor 99a66928f 92e4d914` says NO. **The ordering is `80d8b2ca` (tag, 08-26) -> `92e4d914` (DELIVERED, 09-04 18:16) -> `99a66928f` (cc's fix, 09-04 21:07), so the delivered build sits BETWEEN the tag and the fix and BOTH artefacts are pre-fix.** The inference was: the delivered build is newer than the tag, and the fix is not in the tag, therefore the fix is in the delivered build. **THE LANDMARK WAS THE TAG AND THE QUESTION WAS ABOUT THE FIX.** **THE POLICY SURVIVED THE REFUTATION AND THE EVIDENCE DID NOT, WHICH IS THE PART TO GET RIGHT WHEN CORRECTING SOMEONE:** naming the build a disposition was driven against is correct for a reason needing no divergence -- only that a reader cannot tell which artefact answered. **AND THE TRUE STATEMENT IS SHARPER THAN THE FALSE ONE: neither build carries cc's fix, so `0175` is FIXED IN THE TREE AND ABSENT FROM EVERY BINARY ON THIS BOX.** A defect closed in canon and live in every reachable artefact beats a disagreement. **DETECTION: when a claim rests on _newer than_, name every commit that could lie between, and use `merge-base --is-ancestor` against the THING IN QUESTION rather than against the nearest release marker.**

88. **A CLAIM RELAYED BETWEEN NODES CAN CHANGE SUBJECT IN TRANSIT WHILE STAYING TRUE AT BOTH ENDS, AND THE FALSEHOOD IS CREATED BY THE TRANSFER (dc's, self-reported 2026-09-05, after I refuted the result).** vc's boot message said cc's `99a66928f` **made that call rc=0** -- a statement about HEAD, and true. dc attached it to **the DELIVERED BUILD** and announced that the two artefacts disagree. **NEITHER NODE SAID ANYTHING FALSE IN ISOLATION.** vc described the fix; dc described a binary; the referent moved between the two and nothing marked the move. **THIS IS NOT W76: THAT ONE IS A CLAIM AMPLIFIED BY APPROVAL WITHOUT BEING CHECKED. THIS IS A CLAIM RE-POINTED AT A DIFFERENT SUBJECT ON THE WAY IN, WHICH SURVIVES CHECKING -- because checking it against ITS OWN subject passes.** dc's own account is the compounding one: undriven on arrival AND re-aimed. **AND THE DISPROOF WAS IN THEIR TRANSCRIPT ALL SESSION: their FIRST tool call drove the delivered pair and printed rc=2**, against a standing rule of theirs that a new hypothesis is a prompt to re-read your own transcript rather than to measure. **DETECTION: when you carry a peer's claim forward, restate WHAT IT IS ABOUT in your own words before using it -- a claim about a COMMIT is not a claim about a BINARY, and the two are one sentence apart.** **MY OWN ACCOUNT OF THE CATCH, KEPT HONEST BECAUSE W81 CUTS THIS WAY TOO: dc grepped and the blast radius was ZERO -- no disposition, page entry or board line had inherited it. I caught it at the point of spread, not after it. _Before 21 dispositions could inherit it_ is dc's generous framing and I am not adopting it.**

89. **A REFUSAL'S TEXT IDENTIFIES WHICH LAYER REFUSED, AND THAT IS THE DISCRIMINATOR BETWEEN A DEFECT AND YOUR OWN ENVIRONMENT (dc's, 2026-09-05, a near-miss they did not file).** They nearly filed a build defect when `agents generate` and `agents sync` returned rc=1 `cannot locate the Intent install this binary belongs to`. **It was their sandbox: the binary sat outside its install tree, and inside it both are rc=0.** **THE DISCRIMINATOR WAS IN THE OUTPUT AND ALMOST READ PAST: a refusal that NAMES A PATH is evidence about the ENVIRONMENT, where `not implemented yet` fires BEFORE any install lookup and is therefore evidence about the BUILD.** Two refusals, same rc-nonzero shape, from layers on opposite sides of the question being asked. **GENERAL FORM: before attributing a non-zero exit to the subject, ask WHICH LAYER PRODUCED THE MESSAGE -- the wording says, and it is free.** Sibling of the `at: garbled time` collision (W84), where a harness error named the same two letters as the verb under test: **both are cases where the ERROR TEXT is the only thing that separates the instrument from the subject.**

90. **A CLEANUP GUARD MUST BE CONSTRUCTED BEFORE THE FIRST LINE THAT CAN FAIL, NOT BEFORE THE THING IT CLEANS UP (mine, 2026-09-05, found by driving a claim about my own work).** My `0244` test created a temp `HOME` with `short_dir`, then parsed a remedy, then built the `Drop` guard that removes it. **The M1 mutation run failed on the vacuity arm -- ABOVE the guard -- and leaked an empty `/tmp` home that was still there five hours later.** It cost a directory and not a daemon, and only because that failure path never reached the `daemon start`. **THE SAME TWO LINES IN THE SAME ORDER LEAK A REAL DAEMON the moment a failure lands between the start and the guard, which is the exact case the guard was written for.** **THE REASON IT SURVIVED REVIEW IS THE KEEPER: A GREEN RUN REACHES THE DROP, SO THE LEAK IS INVISIBLE IN EVERY PASSING TEST.** The only runs that exercise it are the failing ones, and those are read for WHY they failed rather than for what they left behind. **DETECTION: for any RAII guard, ask which lines sit between the resource and the guard -- that gap is the leak, and its size is measured in lines rather than in probability.** **AND I FOUND IT ONLY BECAUSE I DROVE AN ACCUSATION RATHER THAN ACCEPTING OR DENYING IT** (vc recorded a dangling-symlink instance against me; nothing I changed creates a symlink and both delivered symlinks resolve, but looking turned up this). W81 says a claim about my own failure is still a claim -- **the corollary is that driving one is worth doing even when it is wrong, because the search is aimed at my own artefacts.**

91. **A FOLD COMPRESSES, AND COMPRESSION CAN CHANGE A CLAIM'S CATEGORY WHILE PRESERVING ITS ATTRIBUTION (vc's, self-reported and retracted with provenance 2026-09-05).** vc carried an entry saying I had inflicted a dangling symlink. **It was never true, and vc drove its origin rather than conceding it: the phrase enters the corpus at ONE commit -- their own aggressive localfold -- appears in no other file, and the two real dangling-symlink events belong to vc and to cc, each named in first person in their own record.** **THE MECHANISM IS WORSE THAN A SLIP AND vc NAMED IT: my board's own entry says the delivered `intent` symlink went STALE on my commit. A fold turned STALE into DANGLING.** A symlink resolving to an OLD binary became a symlink resolving to NOTHING -- **a currency problem became an outage, and my name stayed attached across the change.** **DETECTION, AND IT IS CHEAP: WHEN FOLDING SOMEONE ELSE'S FINDING, THE WORDS CARRYING SEVERITY ARE THE ONES THAT MUST BE COPIED RATHER THAN PARAPHRASED.** Stale/dangling, refused/ignored, silent/loud, missing/wrong -- a fold is compression, and compression is where a category quietly upgrades. **SECOND INSTANCE TODAY OF TRANSFORMATION-IN-TRANSIT: W88 was a claim RE-POINTED at a different subject between nodes; this is a claim RE-CATEGORISED inside one node's own fold. Both stayed true of something and false of what they now named.** **AND THE RETRACTION DISCIPLINE IS THE OTHER HALF, vc's words: the entry was already gone in an unrelated rewrite, and that is NOT GOOD ENOUGH, because a wrong entry that vanishes without a record is indistinguishable from one that was never made.** Retract on the record; do not let a rewrite launder it.

92. **A CLAIM THAT STAYED TRUE OF SOMETHING AND FALSE OF WHAT IT NOW NAMES HAS NO LOCAL TEST, SO THE VERIFICATION MOVE IS TO RE-DERIVE ITS ORIGIN RATHER THAN TO CHECK ITS CONTENT (vc's generalisation of my W88/W91 pairing, 2026-09-05).** Two instances in one day. **W88: a claim RE-POINTED at a different subject BETWEEN nodes** -- vc said cc's fix made a call rc=0, true of HEAD; dc attached it to the DELIVERED BUILD. **W91: a claim RE-CATEGORISED INSIDE one node's own fold** -- my board's `STALE` symlink became `DANGLING` in vc's compression, turning a currency problem into an outage with my name still on it. **NEITHER IS DETECTABLE BY CHECKING THE SENTENCE AGAINST ITS OWN SUBJECT, BECAUSE THE SENTENCE VERIFIES.** dc's checked out against the fix; vc's checked out against a real symlink event. **THE ORDINARY REVIEW MOVE -- is this true? -- RETURNS YES ON BOTH.** **SO THE MOVE THAT WORKS IS PROVENANCE, NOT CONTENT: where did this sentence enter the corpus, and was it about the same thing there?** vc found their own fabrication with `git log -S` over one phrase -- one commit, no antecedent, and the two real events attributed elsewhere in first person. dc found theirs by re-reading their own first tool call. **DETECTION: for any claim you did not personally drive, ask WHERE IT CAME FROM before asking whether it is true** -- and the tell that you need to is that the claim arrived already formed, in someone else's words. **THIRD CLASS THIS WEEK WITH NO NATURAL OBSERVER**, alongside W83 (a defect with a loud mode and a quiet mode, where only the loud one trains you) and W90 (a leak visible only in failing runs, which are read for why they failed).

93. **A GENERATED FILE HEALS ITSELF ON THE NEXT RENDER AND A HAND-AUTHORED ATTACHMENT CARRYING THE SAME VALUE DOES NOT, AND THE TWO ARE INDISTINGUISHABLE IN THE TREE (dc's, 2026-09-05).** dc retitled `ST0056/11` and the old title had **FIVE HOMES**: `todo.md`, `acceptance.md`, the thread `info.md` and the WP `info.md` are GENERATED and all four re-rendered the moment canon changed; `tasks.md` is an ATTACHMENT, hand-authored with its own copy of the WP table, **so no render touches it and it was still stale after every other home had healed.** **THE ASYMMETRY IS INVISIBLE FROM THE FILE: both kinds are markdown in the same directory, and nothing in either says which one a render owns.** **THIS LANDED ON MY OWN NODE DIRECTORY: `scope-sweep.md` transcribes `scope`, `status`, AC counts and titles for eleven work packages**, and dc found row 11 advertising a deliverable hv deferred on 2026-08-15. **I DID NOT CORRECT THE CELL, AND THAT IS THE JUDGEMENT WORTH RECORDING: a table with one fresh value and nine stale ones is indistinguishable from a fresh table**, so it got an as-at banner naming `intent wp list ST0056` as the authority instead. **Same move on my board's `ST0064 WP-01 = 5/9`: re-derived (`intent ac list ST0064`), still true, and the VERB now sits beside the value.** **DETECTION: for any figure you transcribed from canon, either name the verb that regenerates it or delete it -- and _it is still correct_ is not a reason to leave it bare, because it was correct by nothing having moved rather than by anything you did.** This is [[a handover outlives its subject]] arriving inside my own files: hand over PROPERTIES, not values.

94. **A COUNT HANDED TO YOU AS A DEFECT COUNT MAY BE A STATUS COUNT, AND IT REPRODUCES EXACTLY EITHER WAY (mine, 2026-09-05).** I was told the gated direction _reports 37 absent citations_. **The 37 is real and I reproduced it to the row.** It is not a defect count: 36 of the 37 are `to-write` rows and one is `fiat`, and **a `to-write` row citing a file that does not exist yet is CORRECT -- that is what the status means.** Zero `green` rows cite an absent file, and `intent at lint` is clean on every ST in the estate. **REPRODUCING A NUMBER CONFIRMS THE ARITHMETIC AND SAYS NOTHING ABOUT WHAT IT COUNTS**, and it feels like confirmation, which is the trap: I had the right total and the wrong subject, and would have filed a row overstating the estate's debt on a figure I had personally checked. **CURE: before a population becomes a defect count, split it by the field that would make membership legitimate.** Sibling of W93 -- a count is a measurement with a timestamp AND with a predicate, and the predicate is the half that is usually left implicit.

95. **SEARCHING BEFORE FILING FOUND THE OWNER OF A QUESTION I HAD ALREADY SPENT THE EFFORT ON (mine, 2026-09-05).** I tried to promote 36 orphan files into _work already done while canon says `to-write`_ by matching declared AT ids against `to-write` rows. It failed on its own terms -- AT ids are per-thread and collide across threads, so the match is coincidence of numbering. **Then the pre-filing search turned up `stale_at_check.sh`, whose header says it catches exactly that: a `to-write` row whose cited test already exists.** The question was owned before I asked it. **The search that exists to prevent a DUPLICATE ROW also prevents duplicate WORK, and I ran it in the right place only by habit** -- after the analysis, before the filing. **CURE: search the estate when the QUESTION forms, not when the row does.** Both results went into `0269` on purpose, because a negative result nobody records gets re-run.

96. **I COMMITTED THE DEFECT I HAD JUST CORRECTED IN A PEER, INSIDE THE SAME ROW, ONE SECTION LATER (mine, 2026-09-05).** I corrected vc's _37 absent_ (36 `to-write`, 1 `fiat`, zero `green`) and banked W94 about it. **Then my own L3 cut said 36 orphan files claim coverage. It is 13.** L3 counts a file that CONTAINS an `AT-NN.N`, and it cannot tell a CLAIM from a STRING: 16 carry the id as fixture data built in code (`id: "AT-99.1".to_string()`, and `AT-99.1` is in no canon file at all), 6 quote ANOTHER ESTATE verbatim as migration provenance (`Arca/arca_cli ST0011 AT-07.1`), and **the whole `legacy_*` family is the v2-migration corpus, so its ids are other estates' canon BEING CONVERTED.** **TAKING THE DISCRIMINATOR FROM THE ESTATE'S OWN MECHANISM IS WHAT MADE ME CONFIDENT, AND IT IS WHAT WENT WRONG:** L3 constrains what a CITED file must contain, and I read it backwards as a test of what an UNCITED file MEANS. **A borrowed instrument carries its own subject, and its subject is not automatically yours.** Cure: the working cut is whether the id is in a COMMENT -- strongest as a `//!` module doc opening with it -- and the negative space (fixture data, foreign estates) has to be enumerated before the count, not after.

97. **A FOLD CUTS BY WHERE SOMETHING LIVES, AND A HOLD'S CONDITION IS ABOUT WHAT IT SAYS (mine, 2026-09-05, caught mid-fold).** `0222` was a live hold -- _releases when `intent wp show ST0056/17` reads `done`_ -- sitting in the TODO REGION rather than in `## Holds`. **My aggressive fold was cutting the TODO region wholesale, so it would have taken an unexecuted ruling with it, and the resulting board would have been correct-looking and short one hold.** hv's rule (_a fold never archives an unexecuted ruling_) is stated about CONTENT and the operation that violates it is addressed by LOCATION -- so honouring the rule while reading only the section you are cutting is not enough. **CURE: before cutting any region, grep the region for a release condition (`RELEASES WHEN`, `until`, `blocked`) and re-home what you find BEFORE the cut, not after.** The lesson generalises past folds: any bulk operation scoped by position will silently violate a rule scoped by meaning.

98. **A RULING THAT IS RIGHT DOES NOT GET RE-EXAMINED, SO ITS PRICE RIDES ALONG UNCHECKED (vc's, on themselves, 2026-09-05, and I want it here because I was the instrument twice).** vc ruled correctly twice in an hour with a wrong cost attached: _the standard is already implemented and applied to the wrong class_ (the precedent does not exist -- the only reachable cap message is `attachment_cap.rs`, and `FindingClass` is its own governed mechanism, so the price is a new class plus a table row plus both directions of `residue_class_check.sh`), and calling the `0271` git refusal _additive_ over a population that excluded the estate's own test fixtures. **THE CONCLUSION SURVIVING IS WHAT MAKES IT DANGEROUS**: review attention goes to verdicts, and a correct verdict closes the file on everything bundled with it. **Sibling of W94 from the other side -- W94 is the right NUMBER counting the wrong noun; this is the right CONCLUSION carrying an unexamined price.** Cure is the same shape: a cost inside a ruling is a claim, and it needs its own evidence even when the ruling does not.

99. **A FILED ISSUE'S STATED CAUSE IS NOT ITS POPULATION, SO CHECKING YOUR EXPOSURE BY THE CAUSE RETURNS NIL ON AN ESTATE THAT HAS THE DEFECT (mine, 2026-09-05, on `0273`).** `0273` reports `at lint` counting rows it never examined and names `legacy.raw` as the route in; it measures its own estate by that route and reports the Intent-family exposure as effectively nil. **Intent canon carries ZERO `legacy.raw` rows and has the defect at 103 of 406.** The guard at `contract.rs:683` has three conjuncts and `legacy.raw` fails only the third -- `kind != test` (40) and a status that is not green/red (63) reach the same unexamined bucket without ever touching it. **THE CAUSE A REPORTER MEASURED IS THE ONE THEY COULD SEE FROM THEIR CORPUS, AND A CORPUS SELECTS WHICH CONJUNCT FAILS FIRST**, so an estate reading its exposure by that cause is running the reporter's instrument on its own population and getting a true answer to their question. **Sibling of W94 and W96 one level up: those are a NUMBER counting the wrong noun, this is a CAUSE standing in for a population -- and it is more durable, because the cause is written into the issue and survives every later reading of it.** Cure: read the GUARD, not the issue's summary, and enumerate every way to fail it before pricing your own exposure. **The tell is a filed defect whose exposure check comes back clean on a corpus where the reported symptom is reproducible** -- here `ok -- 173 AT row(s) conform` over 119 examined.

100.  **A SCHEMA PROBE WITH THE WRONG KEY NAMES IS INDISTINGUISHABLE FROM A MALFORMED ROW (vc's, 2026-09-05, and I hit it at boot the same day).** `jq` answers `null` at exit 0 for an absent key and never says _no such key_, so a probe that guesses wrong reads exactly like a broken record. **vc read `0273` with `jq -r '.id, .state'`, got `null, null`, and briefly had a well-formed canon row down as malformed.** **MINE WAS THE FIRST COMMAND I RAN AGAINST CANON THIS SESSION:** I guessed `.criteria[]?, .acceptance_tests[]?, .ats[]?` -- three names, none of them real, the key is `.tests` -- and got silence at exit 0. I caught it only because `jq 'keys'` is a habit, not because anything flagged it. **WHY IT IS THE WORST MEMBER OF THE BLIND-GREP FAMILY: WHEN THE PROBE IS ABOUT THE SCHEMA, THE FAILURE MODE AND THE FINDING ARE THE SAME OBSERVATION.** A blind grep returns nothing and you ask why; a blind schema probe returns `null` and hands you a plausible defect to report. Cure: `jq 'keys'` before any field probe, every time, and treat an all-`null` result as an instrument fault until the key list says otherwise.

101.  **A COUNT AND THE SENTENCE NAMING IT DRIFT APART, AND THE SENTENCE IS WHAT TRAVELS (mine, 2026-09-05, third and fourth instances in one day).** The count is checkable and the prose is not, so the prose is where the error goes to live. **FOUR NOW, ALL THE SAME SHAPE:** _37 absent_ was a status count sold as a defect count (W94); my _36 orphans_ was an occurrence count sold as a claim count (W96); vc's _0273's exposure is green/red rows carrying no file_ named a membership as a ROUTE -- `AT-07.7` fails the guard's FIRST conjunct (`kind`) and never reaches the path test, so the file conjunct's exposure here is **0**, not 1; and vc's _four non-test rows carrying a test result_ is right at four and wrong at the phrase -- the predicate is `kind == non-test AND status != n-a`, and three of the four are `to-write`. **EVERY ONE OF THE FOUR REPRODUCES EXACTLY.** Re-running the query confirms the arithmetic and never touches the noun, which is why this class survives verification by the person who wrote it. **CURE, AND IT IS CHEAPER THAN CHECKING: SHIP THE PREDICATE BESIDE THE NUMBER.** A count written as `kind == non-test AND status != n-a -> 4` cannot drift from its sentence, because it has none. **AND WHEN A PEER'S NUMBER DISAGREES WITH YOURS, GO LOOKING FOR THE PREDICATE THAT YIELDS THEIRS BEFORE REPORTING THEM WRONG** -- that move turned _your four is wrong_ into _both are right about different questions_, which took one command.

102.  **THE COST OF CHECKING SELECTS WHICH CLAIM GETS CHECKED, AND IT TRACKS WHAT INSTRUMENT YOU ARE ALREADY HOLDING RATHER THAN WHAT MATTERS (vc's self-report, sharpened here, 2026-09-05).** vc verified my Finding 1 against `init.rs` and my Finding 4 not at all, then confirmed both in one sentence. **Finding 4 was the CHEAPEST of the four to falsify** -- one `--help` against a keg sitting on the machine -- so the selection was not tracking difficulty and not tracking risk: it tracked which claim vc already had an instrument pointed at. **I did the identical thing to myself an hour earlier: I had L3 in hand, so L3 became my orphan discriminator** (W96). **A ruling inherits the verification budget of whatever the ruler happened to be holding**, and the claims that escape are the ones needing a DIFFERENT instrument, which is uncorrelated with how much they matter. **AND THE CURE IS NOT MORE DILIGENCE -- IT IS SCOPING, WHICH IS vc's POSITIVE CASE AND THE BEST RESULT OF THE DAY:** the page's `On v3.0.0` named its own subject, and that scoping survived TWO nodes failing to read the subject. **A claim that names what it is about is checkable by someone who has not thought about subjects at all**, where the discipline of remembering to check failed twice in one hour. Write the subject into the claim; do not rely on the reader supplying it.

103.  **AN UNBOUNDED ENDORSEMENT IS THE DEFECT; NAME WHICH FINDING IT COVERS (vc's, 2026-09-05, the operational form of W102).** When a report carries N findings and the reader confirms it in one sentence, the confirmation covers all N and the verification covered one. vc endorsed _file Finding 1 and fix §1 and §5_ having driven Finding 1 from `init.rs` and Finding 4 not at all -- and Finding 4 was WRONG, so a correct sentence in a shipped page was one commit from being edited away on my error. **THE RULE: verify all N, or say which one you checked.** Cheap, checkable, and it fails loudly -- where _be more careful_ is none of the three. **This is the cure W102 was missing:** W102 names the mechanism (the ruler's verification budget follows whatever instrument is already in hand) and mechanisms do not yield to diligence. Pair it with the positive case: **a claim that names its own subject is checkable by someone who has not thought about subjects at all**, which is why `On v3.0.0` survived two nodes walking past it.

104.  **A SUBJECT WRITTEN DOWN WORKS ON A READER WHO IS NOT TRYING; A DISCIPLINE WORKS ONLY ON A READER WHO IS (vc's, 2026-09-05, and it is the day's conclusion rather than one more entry).** Score the day honestly. **THE TWO SAVES WERE BOTH ARTEFACTS THAT NAMED THEIR OWN SUBJECT, AND NEITHER WAS ANYONE'S VIGILANCE:** `pipefail_sigpipe_check.sh:62` declares `grep -q` as the only early-exiting reader it knows and names an exiting `awk` as NOT its subject -- which killed my fifth-instance hypothesis in minutes, before I could credit myself with it; and `docs/getting-started.md`'s `On v3.0.0` scoping survived TWO nodes walking straight past the subject, and is the only reason a correct sentence was not edited away on my wrong finding. **AGAINST THAT, THE DISCIPLINES: vc's endorsement rule, cc's B5, my own W99 -- three rules that all depend on REMEMBERING, and every one of them failed at least once today.** The asymmetry is not close and it is not about care. **A written-down subject is checked BY THE ARTEFACT, at the moment of reading, by whoever happens to be reading -- including someone who has never thought about subjects.** A discipline is checked by the person who already has to be in the right frame of mind, which is exactly the frame that is missing when it matters. **THE OPERATIONAL FORM: when you catch a class, prefer writing the subject INTO the artefact over writing a rule about the class onto a board.** This board is 100+ rules deep and today its rules lost to two sentences of scoping. **Cross-cuts every prior entry: W94, W96, W99, W101, W102, W103 are all disciplines, and W104 is the reason they keep needing successors.**

105.  **`git commit --only` COMMITS EXACTLY WHAT IT WAS HANDED AND REPORTS SUCCESS, SO A RENAME COMMITTED BY ITS NEW PATH LANDS HALF (mine, 2026-09-05).** I renamed a test file, named the new path and `suite.rs` in `--only`, and the DELETION stayed staged: **HEAD carried BOTH files**, the orphan nothing referenced and the new one. **THE WORKING TREE HAD ONLY THE NEW FILE, SO NOTHING LOCAL LOOKED WRONG**, and under `autotests = false` the orphan cannot compile its way to a red -- so no test fails and the symptom is a duplicate sitting in shared history until someone reads `git ls-tree`. **THIS IS THE SAME FLAG AS THE HAZARD I ALREADY HELD, FAILING IN THE OPPOSITE DIRECTION.** That note says `--only` is path-scoped rather than hunk-scoped, which is about too MUCH landing; this is too LITTLE, because a rename is TWO paths and the flag will not say a path is missing. **Cure: after any `--only` commit that moves a file, read `git ls-tree -r HEAD` for BOTH names -- `git status` is the wrong instrument, because a clean working tree is exactly what a half-committed rename produces.**

106.  **A ROTTED RECORD IS ONE SOMETHING IS RESTING ON; A CLASS THAT CATCHES EVERY OUT-OF-DATE SENTENCE TRIAGES NOTHING (cc's, 2026-09-05, arrived at by REFUSING a correction I offered them).** I told cc their hour-old citation of a filename I had just renamed was not the rotted-citation class -- it was correct when written, and the cost of a rename lands on the renamer. **cc took it and produced the discriminator, which is better than either of our halves: the class is not _a sentence that has gone out of date_, because every sentence does that eventually.** It is a record something DEPENDS on. `AC-00.6`'s stale count was sizing the last pre-tag job; `0179`'s expired write-once was steering a live scope ruling on hv's desk; **nothing whatever rested on the filename.** So it is not a weak instance, it is OUTSIDE the class -- and a boundary is what makes the rule usable by someone who was not in the room. **THE GENERAL SHAPE, AND IT CUTS AGAINST MY OWN HABIT TONIGHT: a class you cannot draw a boundary around is not rigour, it is a filing cabinet.** Six of my hundred-odd entries would fail this test.

**cc MADE IT A FOLD OPERATION AND THAT IS THE PART THAT CHANGES PRACTICE (`2797abb2`): the boundary test runs FIRST, ahead of any archiving by date.** Their reasoning, which I am adopting: **cutting one undrawable family is worth more than compressing five drawable ones**, so a fold that optimises BYTES is optimising the wrong number -- and _the board is long_ never gets you there. **They ran it on their own three entries before handing it to me, on the ground that a rule you cannot pass yourself is not one to hand a peer.** All three drew. **NOT DOING MY OWN PASS TONIGHT, and the reason is itself the rule: an undrawable family costs nothing while it sits, where a rushed cut of a DRAWABLE one is a real loss** -- which is hv's _a fold never archives an unexecuted ruling_ arriving from the other direction.

107. **THE BLINDNESS THAT BELONGS TO NO AUTHOR, NO REVIEWER AND NO CHECK IS THE ONE THAT SURVIVES CARE (mechanism mine, ranking cc's, 2026-09-05 -- and cc asked for the split to be recorded exactly, having first had all seven as instruments).** Seven instances today of _an instrument structurally unable to see the thing_. **Six were built by someone**: a wrong population, a fixture that could not exhibit the condition, a control that was the present case in disguise, a count over rows nothing read. Each has an author and each is fixable by that author. **The seventh has none: `autotests = false` means an orphan test file CANNOT COMPILE ITS WAY TO A RED, so the blindness is a property of the COMPILATION MODEL.** No reviewer failed, no check was omitted, no discipline lapsed. **That is the version of this class that cannot be fixed by being more careful, and it is the one to put in front of hv if only one survives.** Sibling of W104 at the far end: W104 says a written-down subject beats a discipline; this says some blind spots have no one to write anything down.

108. **EDITING ONE OF AN ADJACENT PAIR IS THE OPERATION THAT CREATES A CONTRADICTION, AND THE FIXER IS MORE EXPOSED THAN THE AUTHOR (mine, 2026-09-05, committed BY ME while removing this exact class from the same page).** `getting-started.md` §2 asserted a refusal one paragraph above a line describing the build's real behaviour. I rewrote the first paragraph; **the second opened _the refusal you get if you try_ and its antecedent was the sentence I had just deleted.** Two correct-looking paragraphs with the defect living ONLY IN THE JOIN -- **and no hunk-scoped read of my own diff could show it**, because the diff contained one clean replacement. **WHY THE FIXER IS WORSE PLACED THAN THE ORIGINAL AUTHOR: the author wrote both paragraphs in one sitting and at least held both in mind; the fixer holds one.** That inverts the intuition that a small careful edit is the safe move. **DETECTION: after editing a paragraph, re-read the paragraphs on EITHER SIDE in the rendered file, never the diff.** Third instance of this page's signature defect and the first that was mine. Same shape as `--only` being path-scoped rather than hunk-scoped (`W39`, `W105`), arriving in prose instead of in git.

109. **`event_log` GIVES THE FILING INSTANT THAT AN mtime CANNOT, AND STILL DOES NOT GIVE THE ACTOR (mine, corrected by cc before it could be used, 2026-09-06).** Asked whether `0276` was mine, I offered mtimes as a discriminator -- `0274` 21:44Z, `0275` 21:46Z, mine; `0276` 22:05Z, after I stopped. **cc corrected it and they are right: an mtime is the LAST WRITE, not the creation.** Driven at the schema after guessing a column name wrong first (`W100`, on me again): `select ts, op, subject_id from event_log` shows **`0276` was FILED at 21:57:11Z and merely EDITED at 22:05:44Z** -- and 21:57 is INSIDE my active window, which ran to 22:31Z. **SO MY TIMING LEG WAS NEVER VALID AND THE ONLY THING EXCLUDING ME IS THE SUBJECT LEG** -- I never opened `sync --to-store` or an attachment path. **DO NOT ARCHIVE THE mtime METHOD WITHOUT THIS CAVEAT; in the broken form it reads as a working discriminator and will be picked up as one.** **AND THE THIRD SURFACE CONFIRMS THE REGISTER CARRIES NO NODE IDENTITY: `event_log.principal` is `local` for every node** (1447 rows) **and `intentd` for the daemon** (130) -- so it discriminates human-from-daemon, never node-from-node, exactly as `reporter` (the shared git identity) and an uncommitted row's absent trailer do not. **`event_log` fixes the INSTANT half of the question and not the ACTOR half, which is the half anyone actually asks.**

110. **BOTH RUST CRATES HAVE ONE `suite` TEST TARGET, AND THE HALF OF THIS ENTRY THAT SAID OTHERWISE WAS AN ASSUMPTION I WROTE AT 12:15Z AND DISPROVED AT 13:0xZ (mine, same session).** `cargo test -p <crate> --test <filename>` fails with `no test target named` in EITHER crate; the form is `--test suite <filter>`. **I had measured `intentsvcs` and then asserted `intent-cli has one target per file` having measured nothing** -- I had only ever run that crate's tests package-wide. It is hv's estate-wide ruling of 2026-08-27: `autotests = false` plus one `[[test]]` plus `#[path]`, chosen precisely SO THE FILES DO NOT MOVE, because relocating them would break every AC row citing a test by path and `intent at` has no verb that retargets a row. **So the per-file appearance is real and the per-target inference from it is false**, which is why the wrong half read so plausibly. **A NEW FILE MUST BE REGISTERED IN `tests/suite.rs` OR IT NEVER RUNS**; `no_orphan_suite_member` is the arm that catches it, and it is the reason an unregistered file is a red rather than a silent skip.

111. **THE macOS APP'S `CFBundleShortVersionString` RESOLVES TO THE LITERAL STRING `dev` (cc's measurement, handed to me 2026-09-08 because `ST0064` is mine).** `Info.plist:12` holds `$(INTENT_VERSION)`, `project.yml:31` sets `INTENT_VERSION: dev`, and xcodegen substitutes it into the pbxproj -- so `PlistBuddy -c "Print :CFBundleShortVersionString"` on the TRACKED file returns the variable, not a version, and the shipped app announces `dev`. **The trap is symmetry with Conflab**, whose plist holds a literal and IS a valid devbin `version_sidecar`; writing `3.0.1` into ours would REPLACE the substitution and silently un-wire the app from its build settings. **Whether `dev` is a defect is hv's call and nobody has filed it** -- it becomes mine to raise the moment the app shows its own version, which it does not yet do.

112. **~~Check the arm that claims to be the load-bearing one first~~ -- FOLDED INTO `W44`** as its targeting heuristic, one day old and folded on the family test rather than on age.

113. **AN ABSENT COLUMN IS INVISIBLE TO EVERY CHECK THAT READS ROWS, AND ITS INVERSE IS A QUESTION NOBODY ASKS THE ARTEFACT (mine + cc + vc, 2026-09-09, and the pair is the durable half).** cc found that `burn.sh` records no subject: I had the emitter open, read past the header, and did not see the missing column. **That failure returns content and looks like a successful read** -- strictly harder to catch than vc's file-not-found the same morning, which announced itself. The INVERSE settled the sibling question: asking how many rows the artefact SHOULD have had closed it in one call, and had gone unrun for a month. **Both are questions about the POPULATION rather than about the VALUES**, which is the same family as _precision is a claim about the corpus_, arriving from a direction that rule does not point at.

114. **A GREEN OVER AN ENUMERATED AXIS IS EVIDENCE ABOUT THAT AXIS ONLY, AND A DOCSTRING SAYING _over the corpus_ DOES NOT WIDEN IT (mine, 2026-09-09, `nav_is_the_shared_path_contract.rs`).** `every_view()` enumerates KINDS from the real declaration and then hard-codes `id: "ST0056"` for every one of them. **One axis is a corpus and the other is a single literal**, and the literal happens to lack the separator that breaks the property. **The test could not have caught `0291` and cannot catch it now.** Check which axes a population VARIES before reading its green as coverage -- and the trap is that enumerating one axis honestly is what makes the claim about the other one credible.

115. **A GUARD WHOSE _INPUT_ IS MISSING PRINTS ITS _SUBJECT IS BAD_ PROSE, AND THE TWO ARE INDISTINGUISHABLE AT THE TERMINAL (`0294`, mine, 2026-09-09).** `int cli` refused for four days with _the build-input scope is undeclared_ followed by **_the binary really does not describe this tree_** -- a claim about the artefact, printed on a branch that returned before any comparison ran. **The refusal was a missing `source` in the caller; the artefact was never examined.** Today the sentence happened to be TRUE, which is the whole trap: **right by luck is the state that produces confidence and teaches nothing**, and a perfectly current binary drew the same words (driven: `base=HEAD` still refuses with the scope unset).

     **TWO CURES, AND THE SECOND IS THE DURABLE ONE.** (a) Source what you depend on -- `cmd/cli`'s own comment two lines up already says _a wrapper that drops its own guard when a file goes missing is worse than one with no guard, because it still looks guarded_, and this is that sentence's own case, one level in: the guard did not go missing, its INPUT did. (b) **A refusal caused by a missing PRECONDITION must not borrow the words of a refusal caused by a FACT ABOUT THE SUBJECT.** Every other branch there reports something measured; one branch reported something assumed, in the same voice.

     **AND THE TELL THAT SCALES: THE PRESCRIBED REMEDY COULD NOT CLEAR THE REFUSAL.** `int local build` rebuilds a pair; the fault was a `source` line. **When following a message's own remedy leaves the message unchanged, the message is about the wrong object** -- that is checkable in one step, needs no knowledge of the code, and would have caught this on day one. Sibling of W106 (a probe whose shape is guessed) from the other side: here the probe never ran and the ANSWER was pre-written.

116. **ROUTING IS A MESSAGE I SEND, NOT A STATE I RECORD -- AND IF THE ONLY ARTEFACT IS ON MY OWN BOARD, NOTHING HAPPENED (cc's find, mine to own, 2026-09-09).** I wrote _`0218` released to cc ... Routed, not taken_ on this board and **sent nothing to any channel of cc's**. `cc/inbox.ic.md` was `_(empty)_` and no commit touched it. cc has it only because they read my focus line and then grepped a peer's board for their own moniker -- **which is not a procedure and cannot be relied on twice.**

     **THIS IS NOT THE hv-INBOX SHAPE AND CALLING IT THAT WOULD LET ME OFF.** That one is a write surface with no named reader: a real event, undelivered. **Here there was no event at all** -- a note on the SENDER's surface describing an act never performed. **And it self-conceals from the sender's side**, which is why it survived a fold: the item left `## Holds` with a correct-looking release note, so at every later pickup of mine the routing read as done.

     **CURE, AND IT IS ONE LINE: a release is not written until a message is sent or an inbox entry is appended. The board records that it happened; it is never the thing that happened.** Sibling of `W120` at one surface's remove -- there a guard printed a conclusion it never computed, here I recorded a delivery I never made, and **I did both on the same day.**

## Decisions

- **RELEASE IS v3.0.1, FEATURE COMPLETE, NUMBER CLOSED** (hv 2026-09-01, via vc). Everything outstanding goes in; no external consumer; scarcity register retired as a class.
- **A DECISION TAKEN ON SOMEONE'S BEHALF AND RECORDED AS SUCH IS DELEGATION; THE SAME DECISION RECORDED AS A FACT IS A SUBSTITUTION** (vc, ratifying two of mine). What made Esc-enters-vi-normal legitimate was naming the alternative, writing it into the design _as_ a decision made on hv's behalf, and telling hv in chat -- not the reasoning behind it.
- **A ROW WITH TWO SUBJECTS CANNOT STATE ITS OWN REACH** (vc, four times in one day, and the tool caught the fourth). `AC-17.17` was minted test-backed while its own reach clause said no test could witness it; the contract REFUSED the pair. Split: `AC-17.17` the ORDER (test-backed, satisfied), `AC-17.18` the PAINTING (non-test, **SATISFIED -- hv drove it in a real terminal 2026-09-02 and the capture is on the row**; this entry said _open_ until 2026-09-03, see W24). **The discriminator is who holds the screen, not which variant it is** -- a borrower that blocks needs no signal, one that returns does.
- **ST0064 WP-01 = 5/9 (ic), RE-DERIVED 2026-09-05 AND STILL TRUE -- `intent ac list ST0064` is the authority, not this line.** 01.1/01.3/01.5/01.8/01.9 done; 01.2/01.4/01.6/01.7 cc/dc/hv-gated. project-CWD wiring; vc RULED per-app-instance root, D07 registry UNBUILT.
- **`AC-17.1` IS SATISFIED AND THE _UNSATISFIABLE IN 3.0.x_ READING IS RETIRED (2026-09-03, measured not argued).** This entry said 17.1 could not be met while the wire was reads-only. cc built `Op::Set` overnight and `AT-17.1` is green. **`EmptyMutation` staying is not a counter-argument -- it is `Op::Graphql`'s bound and it is deliberate; the mutating door is a DIFFERENT door.** Kept as a decision rather than deleted, because three boards and `intent/wip.md` carried the dead reading and a reader may still meet it. **`AC-17.6` is the one open row in WP-17 and is still gated on cc's WP-08** -- `browsed()` refuses unconditionally, and cc has taken the missing half, unstarted. **No figure here: run `intent ac list ST0056`.**
- **WP-14 (whiteboard + inboxes in the store, `L`) -- THE QUESTION IS LIVE AND IT IS NOT WHETHER IT RETURNS.** hv reinstated `AC-00.2`/`AC-10.5` into ST0056 at `a19d8b5c` (driven: ancestor of HEAD, both rows `computed`). **`ST0056/14` itself still reads `Not Started`/`L`**, and those rows are about the FLEET CORPUS, not the coordination model -- so _WP-14 is in the cut_ does not follow from their subject. vc accepted that as a defect in the menu they put to hv. **Bears on `0258`, whose ruling is gated behind exactly this.**
- **A SIZE IS A CLAIM ABOUT THE WORLD EXACTLY AS MUCH AS A COUNT IS** (mine, taken onto cc's board 2026-09-03). An evidence bar held for figures but not for t-shirt sizes is the asymmetry that hides itself, because only one of the two wears a number.
- **`init` LAYS `AGENTS.md` DOWN** (hv 2026-09-09, ruling on ST0065's costed proposal). Option 2's index landed in `CLAUDE.md` at `0b5d46c96`, so the content argument is spent -- but `AGENTS.md` is the TOOL-AGNOSTIC contract, and a fresh project had none until someone ran a verb nothing told them to run while `CLAUDE.md` said _read AGENTS.md first_ four times. **Ruling 2 (what `AGENTS.md` mirrors) is DEFERRED: it gates only Option 3 and blocks nothing.** Ruling 3 was already dead -- built and green since 2026-09-04.
- **`ST0065/WP-02` COLLAPSES INTO `ST0056/WP-15`** (hv 2026-09-09). Cancelled with reason via `intent wp cancel`. Two homes for one concern, both WIP, found before either party sank effort.
- **`uninstall` PROMISES THE FILES IT WROTE, NOT THE DIRECTORY IT EMPTIED** (hv 2026-09-09, `0218` remedy 1).
- **A RULING RECORDED ONLY IN CODE IS INVISIBLE TO EVERY BOARD-LEVEL RE-DRIVE** (mine, adopted by vc into the standing recipe 2026-09-09). ST0065's ruling 3 was answered, built and green on 2026-09-04 and survived the issue tracker, the boards AND the gates, because its only home is a doc comment on a test. **vc's expiry defect is an item outliving its REASON; this is an item outliving its ANSWER**, and it fails in the direction that costs most -- a DEAD item passing as live indefinitely. **Fourth line of the re-drive recipe: for any ruling whose answer would be BUILT, grep the estate for the thread id before believing the board.**
