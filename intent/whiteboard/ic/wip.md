---
node: ic
name: Interface Claude
role: interface
session_id: 11cef60b-409e-4bcc-b0f5-808d43639e75
commit_session_id: 01NWtGVYUAFbTUS28AZsnzq9 -- the Claude-Session trailer id on commits authored from THIS session (0208). POINT-IN-TIME: one session per line, never a table -- a restart mints a new id, and the mapping accumulates in .history/ as folds archive the header that was live. Read off my own commit, not reconstructed.
heartbeat_at: 2026-09-02 20:22Z
status: active
focus: "TUI redesign (WP-17) and ST0065's contract. WP-17 rows 13-18: six covered, FIVE satisfied, AT-17.18 open BY DESIGN and waiting on hv at a terminal -- it is the only one a green suite cannot speak to. ST0065 went from ZERO criteria to seven ACs and seven ATs; AC-00.5 carries a driven instrument, four are to-write and name their instruments, two are non-test reads. MY QUEUE IS EMPTY OF THINGS I CAN START: everything open waits on hv, vc or dc. RE-MEASURE EVERY FIGURE AT PICKUP -- five nodes write this tree."
claims: [ST0065, ST0056/09, ST0056/17, ST0064]
---

# Interface Claude (ic)

**Aggressive localfold 2026-09-02 20:22Z (hv: checkin + localfold + compact). Pre-fold board verbatim + sha-verified at `.history/20260902/wip-prefold-2022Z.md` (`6f595f9c063fa2f6`, 26388 bytes, prettier-stable before the sha was taken). STATUS STAYS `active` -- a fold is not the end of a session. NO FIGURES CARRIED FORWARD WITHOUT RE-MEASURING: run the verbs.**

**FOLDED ON EXECUTION, NEVER ON DATE.** Everything removed below was DONE; every unexecuted ruling is still here. The narrative of a ruling is history, the ruling itself is live state, and a fold keyed on _dated_ instead of _done_ is how two directives got buried on this board once already.

## DOING

**hv's GOVERNING DIRECTIVE STANDS AND IS NOT HISTORY: change any AC/AT needed, DO NOT yak-shave old guards or tests -- rewrite them to reality** (fail-forward; [[feedback_fail_forward_intent]]). Accepted design: `intent/st/ST0056/tui-redesign-proposal.md`; the ratified design doc is `intent/st/ST0056/tui-design.md`.

**WP-17 IS BUILT OUT AND ITS CONTRACT IS CLOSED EXCEPT FOR ONE ROW.** `/{cmd} ...` runs any allow-listed `intent` command from the palette, mutation included, on hv's ruling. Rows 13-18 are six covered and five satisfied. **The one open row is `AT-17.18` and it is open BY DESIGN** -- see TODO 1.

**ST0065 HAD ZERO CRITERIA THIS MORNING AND NOW HAS A CONTRACT.** Seven ACs (`AC-00.1` .. `AC-00.7`, ST-level -- `gg` in `AC-<gg>.<n>` is the WP SEQUENCE and `00` means ST-level, which is why my proposed `AC-65.x` was malformed) and seven ATs. `AC-00.5` is satisfied by a driven two-arm instrument; `AC-00.2`/`AC-00.3` are non-test reads; four are to-write and each names its instrument AND its positive control.

**THE FOUR `ST0065` INSTRUMENTS ARE MINE AND ARE NOW STARTABLE -- hv RULED THE OPEN SET 2026-09-02 20:24Z ("go with your recs on all of the above", via vc), AND CUTTING `ST0065` WAS IN IT.** This paragraph said _not startable yet, blocked on hv's sequencing_ for about two minutes; the ruling arrived mid-fold and the block is gone. **`AT-00.6` LANDING RED IS RULED IN RATHER THAN TOLERATED**: the skill-sync checksum covers `SKILL.md` alone, so the criterion is genuinely unsatisfied and a row minted green by a test that avoided the case would be worse than an open row.

## TODO

**ITEM 1 IS THE WORK. EVERYTHING AFTER IT WAITS ON SOMEONE ELSE, AND THAT IS THE STATE, NOT A COMPLAINT.**

1. **BUILD `ST0065`'s FOUR INSTRUMENTS -- STARTABLE NOW, hv RULED THE SET 20:24Z.** `AT-00.1` one home per rule (**and the positive control is the half that matters: plant a PARAPHRASE, because prose forks by rewording, not by duplication -- a checker that catches only verbatim copies passes the case that actually happens**); `AT-00.4` transitive liveness (**plant a `chains_to:` naming a skill that does not exist AND an orphan nothing references; a checker walking only FROM the roster finds the second and never the first**); `AT-00.6` verified-at-the-machine, **expected RED**; `AT-00.7` derived population (**the control is the whole instrument: add a file and every enumeration must grow without an edit**). Fixture for 00.6 already exists -- `intentsvcs/tests/skills_sync.rs`, where `AT-00.5` was driven.
2. **`AT-17.18` WAITS ON hv AT A TERMINAL AND STAYS `to-write` UNTIL DRIVEN.** `intent explore`, `/`, `/st list`, and judge whether the output is LEGIBLE before pressing enter. **Not green on a green suite -- that is the exact reading the row was minted to refuse**, and the seam that makes everything else in the module provable is the same seam that puts painting permanently outside it. hv has been told twice.
3. **`0215` IS OPEN DELIBERATELY.** Remedy landed (`2d41e929`); vc has severity; **hv has the product call dc named -- whether to close the divergence toward v2 instead.** Closing on a fixed label would bury the question an operator scripting the verb cares about.
4. **THE `v2:`-LABEL CENSUS IS RATIFIED AND IS dc's WHEN hv RELEASES THEIR HOLD. DO NOT ASK THEM TO BREAK IT.** Load-bearing for more than `0215`: vc carries it as the candidate `AC-00.16` amendment -- _an instrument's reach is bounded not only by what it checks but by what the ESTATE CAN EXHIBIT, and that bound is declared nowhere because it is a property of the ENVIRONMENT rather than of any tool._
5. **`0181`** -- dc disposed it, recommendation WITHDRAW, vc to rule. Nothing of mine moves until they do; if ruled, the retire is a WP-09 write and therefore mine.
6. **ST0064 remaining (5/9), not mine to start:** 01.2/01.6 cc-gated; 01.7 dc pipeline + hv's ADC signing; 01.4 cc console.
7. **Explorer/Lotus menu SELECTION** (hv item, A1 on vc's list) -- needs hv at the keys.
8. **`intent app start|stop|restart`** (hv, new user verb): controls the INSTALLED app; new_surface `app` family; coord cc.

## Watch-outs -- mechanisms only

1. **Non-test AC closes via `intent ac satisfy`, NEVER an AT; AT rows close via `at green --note`.** Re-drive + positive-control before satisfying. **`at green --note` REPLACES the note WHOLESALE and silently** -- read the WHOLE existing note first and APPEND (0207/8c-bis; it ate AT-00.12's 7803 bytes once). Source the existing note from the JSON extract (`jq -rj`), never `acceptance.md` (prettier is a second writer + the row carries a 114-char prefix). Assert contains-original-verbatim AND longer AFTER the write. **`at edit --note` REPLACES OUTRIGHT and is the deliberate door.**
2. **Canon on SHARED threads: SERIALISE ST0056 writes through vc; gather evidence OUTSIDE the window, make the window ONE coherent commit.** 0206 (concurrent facade verbs clobber), 0210 (a dirty parity/tools file blocks EVERY node's canon commit via the extract union, REPO-WIDE), 0212 (watcher ingest reverts a store write when disk lags; verify DISK not store before commit).
3. **The macOS app is `native/macos/Intent/` (Swift, xcodegen, folder-GLOBBED).** `bin/devbin macos app-build` unsigned Debug off-tree; `app-test` the suite. IntentCLI = the one shell-out home. NOT `dvb test rust`.
4. **A value/claim carried ONE STEP past what it supports is the recurring class, and it is MINE too.** Hand/take PROPERTIES not values; re-measure at pickup; a retraction carries the same burden as the claim it retracts.
5. **`git commit --only <explicit paths>` is the only safe write** on this 5-writer tree (git add new files first). NEVER remove a peer's index.lock (WAIT+retry -- hit twice today, cleared in 3 and 11 tries). cc's POST-VERIFY IS STANDING: `git show --name-only HEAD` == your set. Every stamp from a `date -u` read THIS turn.
6. **`intent --version` names the commit the BINARY WAS BUILT FROM; CURRENCY is that against `git diff --name-only <that>..HEAD -- native/rust`.** Only the second decides staleness. **AND THE `-- native/rust` SCOPING IS LOAD-BEARING: the embed is the LAST COMMIT TOUCHING `native/rust`, NOT HEAD** -- a fresh build of a clean tree embedded a sha ten board commits back, every one touching zero Rust. **So `embed != HEAD` is NORMAL on a board-heavy day and proves nothing.** I raised it as staleness and the refutation was already in the same terminal ([[feedback_the_disproof_is_usually_already_in_hand]]).
7. **A MARKDOWN ATTACHMENT HAS TWO WRITERS -- ORDER: prettier FIRST, let the store settle, verify, THEN commit.** The pre-commit gate refuses unformatted markdown, so prettier can rewrite a file AFTER the store has taken your version ([[project_formatter_is_a_second_writer]]). **AND RE-DRIVE ANY TEST THAT PARSES THE FILE: prettier ALIGNS markdown tables.** "It happened to still parse" is a thing to measure, not assume.
8. **A TEST WRITTEN FROM ONE OBSERVED FAILURE INHERITS THAT FAILURE'S INCIDENTAL SHAPE, AND THE INHERITANCE IS INVISIBLE BECAUSE THE TEST PASSES ON THE CASE THAT MOTIVATED IT.** **8x: ANY STATE THE HARNESS ITSELF MOVES IS NOT EVIDENCE THAT THE THING UNDER TEST MOVED IT.** Cure: a baseline that ALREADY CONTAINS the bookkeeping. **Live again today from the other side: a first-occurrence search over a log containing SETUP measures the setup** -- locating the first `enter_raw` found the initial take rather than the re-take, so a CORRECT implementation failed. It fails in whichever direction the setup happens to sit, so the direction of the error carries no information either.
9. **A THING DECLARED, WRITABLE, VISIBLE AND READ BY NOTHING IS THE RECURRING DEFECT ON THIS SURFACE.** `Hotkey` (hv's find), `Command::group` (MINE, added in the same commit as the paragraph condemning `Hotkey`), and `explorer.editing.mode` would have been the third. **The cheap check is a grep for consumers of every field you declare, at the moment you declare it** -- no invariant catches this class, because a field with no reader breaks nothing. **INVERTED FORM, and it is the one with no cure but the compiler: a DEFAULT method on a trait with FORWARDING implementations is a trap.** A forwarder that inherits a default where it should have passed on is correct in isolation at every site; required methods make the compiler find all of them, a default makes it find none.
10. **A SHARED FILE CAN CARRY A PEER'S HUNK, AND `--only` COMMITS THE WHOLE FILE.** Cure is hunk-level staging, non-interactively (`git add -p` is unavailable here): `git diff -- <file>` to a patch, split on `@@`, drop the hunks that are not yours, `git apply --cached` the rest, `git add` your other paths, then **plain `git commit`** -- `--only` re-reads the WORKING TREE and would undo the selection. Index must be clean of peers first.
11. **A DEFECT'S BLAST RADIUS IS NOT ITS PACKAGE.** A base-crate signature moves and `cargo check -p <their crate>` stays green throughout; a generated `ddl.sql` fails tests in a package that did not change. **The node who can SEE it is not the node who caused it.** `cargo test --workspace` is the instrument that sees it and `-p` is the one that does not.
12. **THE SHARED-ARTEFACT GUARD REFUSING A REBUILD IS THE GUARD WORKING.** `bin/devbin build all` REDIRECTS to a private target dir when `native/rust` is dirty. **Wait for the peer; never force** -- forcing compiles their uncommitted work into the binary hv drives (`dirty-<sha>`). Measure delivery by SHA (symlink target vs built file), never by the marker. **AND THE "REBUILD WINDOW IS hv's" PREMISE IS EXPIRED (vc, 20:24Z, driven): `0196` is a DEFECT, not an authority constraint** -- `guarded_release_build` deletes the shared pair before it builds and no failure path restores it, so a FAILED build leaves every live session on this machine without a binary. **The real constraint is a QUIET TREE, not hv's permission.** I rebuilt twice today without asking and both were right; two nodes were reporting the wrong model upward while the correct one was in my hands. **A constraint nobody re-derives becomes an authority claim.**
13. **A PREMISE CARRIES A VERSION IN THIS ESTATE, AND A RE-DRIVE AIMED AT THE CITATION PASSES** (vc's `8an`, on my defect). I put a citation naming a file that does not exist into a contract proposal, lifted from my own draft five days older -- **and the SUBSTANCE was true anyway**, so minting would have been right for the wrong reason and rejecting would have lost a true row. **Neither verdict was available without driving it.** The quieter half: _upgrade cannot rescue it_ cited a line that is still present, still says exactly that, **and is v2**; in v3 `Payload::sync` has ONE caller, so nothing automatic runs it and the correction made the defect WORSE. **THE CLASS IS NOT ORDINARY STALENESS: the evidence STILL VERIFIES. A re-drive checking the CITATION passes; only one checking the SUBJECT fails** -- it survives "re-drive your citations" intact. **This estate is a port, so every inherited claim carries a version and establishing WHICH is the first move.**
14. **DRIVE A DESTRUCTIVE QUESTION IN A SANDBOX OR DO NOT DRIVE IT.** The `sync`-prunes question needed a skill installed and its canon source deleted -- against real state that is `~/.claude/skills/` and the live manifest, ie an incident rather than a measurement. `intentsvcs/tests/skills_sync.rs` carries a disposable install tree, Claude target and manifest. **A measurement that mutates shared durable state is not one anyone can afford to repeat**, and repeatability is most of its value.
15. **`ok:` IS THE TOOL'S REPORT, NOT EVIDENCE THE ROW EXISTS (`0216`, HIGH).** `ok: AT-00.7 created` produced NO ROW -- absent from `at list`, the extract and `acceptance.md`; the identical call alone landed. **Second independent instance of vc's `AC-17.17` loss, and BOTH were the THIRD of three rapid calls.** vc drove 18 rapid calls in a scratch project and lost nothing, so the naive reading is refuted; live hypothesis is a debouncer in the daemon's watch collapsing three renders into one late ingest, losing the LAST write and no other. **MINT ONE, WAIT, READ THE ARTEFACT, THEN COMMIT, THEN `git show HEAD:`.** And **the belief outlives the row**: both of us wrote commit messages from `ok:` lines, so a lost row leaves a durable false record in git.
16. **BACKTICKS INSIDE A DOUBLE-QUOTED SHELL ARGUMENT ARE COMMAND SUBSTITUTION, AND THE FAILURE IS SILENT AND PLAUSIBLE.** A `--note` passed inline lost both its citations to it; the row was created, the tool printed `ok`, and the note read perfectly well with the two facts it existed to carry simply gone. **`intent init` ACTUALLY RAN against this project** -- it refused because the project exists, which was luck rather than design: a different verb would have executed against the live tree with no refusal to warn me. **Anything non-trivial goes to a file via a QUOTED heredoc and reaches the CLI as `"$(cat …)"`** -- the same cure dc reached for the apostrophe trap. Sibling of the zsh traps already known here: unquoted `$var` does not word-split, and an unmatched glob aborts the whole command.

## Decisions

- **RELEASE IS v3.0.1, FEATURE COMPLETE, NUMBER CLOSED** (hv 2026-09-01, via vc). Everything outstanding goes in; no external consumer; scarcity register retired as a class.
- **A DECISION TAKEN ON SOMEONE'S BEHALF AND RECORDED AS SUCH IS DELEGATION; THE SAME DECISION RECORDED AS A FACT IS A SUBSTITUTION** (vc, ratifying two of mine). What made Esc-enters-vi-normal legitimate was naming the alternative, writing it into the design _as_ a decision made on hv's behalf, and telling hv in chat -- not the reasoning behind it.
- **A ROW WITH TWO SUBJECTS CANNOT STATE ITS OWN REACH** (vc, four times in one day, and the tool caught the fourth). `AC-17.17` was minted test-backed while its own reach clause said no test could witness it; the contract REFUSED the pair. Split: `AC-17.17` the ORDER (test-backed, satisfied), `AC-17.18` the PAINTING (non-test, open). **The discriminator is who holds the screen, not which variant it is** -- a borrower that blocks needs no signal, one that returns does.
- **ST0064 WP-01 = 5/9 (ic).** 01.1/01.3/01.5/01.8/01.9 done; 01.2/01.4/01.6/01.7 cc/dc/hv-gated. project-CWD wiring; vc RULED per-app-instance root, D07 registry UNBUILT.
- **WP-17 STOOD DOWN status is SUPERSEDED by the redesign** -- 17.1 was unsatisfiable-as-written in 3.0.x (D56 equalises downward, EmptyMutation). Re-measure WP-17 against the reworded `AC-17.9`/`AC-17.11` now that they have landed. 17.6 still blocked on cc's WP-08 browser-open stub.
- **WP-14 (whiteboard + inboxes in the store, L) MAY return from ST0069** -- hv's call, not mine.
