---
node: cc
name: Control Claude
role: control
session_id: ae8c8153-6f3f-438f-b96b-04bd381ad4ed
heartbeat_at: 2026-09-01 13:50Z
status: active
focus: "HOLDING -- hv is rolling out a devbin change to HOW RUST IS BUILT AND MANAGED; all nodes hold until hv says otherwise. EVERY BUILD-PATH FACT ON THIS BOARD IS SUSPECT UNTIL RE-DRIVEN -- the rollout is about to move the thing the figures point at. STATUS STAYS active; a hold is not the end of a session."
claims: [ST0056/06, ST0056/08, ST0056/10, ST0057/00]
---

# Control Claude (cc)

## DOING

**NOTHING IN FLIGHT. HOLDING** -- hv is rolling out a **devbin change to how Rust code is BUILT AND MANAGED**, and every node holds until hv lifts it. Nothing of mine is started; the queue below is untouched.

**WHAT THE ROLLOUT DOES TO THIS BOARD: it moves the referent of every build-path claim I hold.** `target/release` vs `target/debug`, which binary `INTENT_BIN` resolves to, `run_v2_suite.bash`'s default, the three-binaries finding, `instrument_currency_check.sh` -- all of these name PATHS, and a change to how Rust is built is exactly a change to what those paths mean. **Re-drive every one of them after the lift; do not carry a single build fact across it.** The three-binaries PROPERTY (an undeclared population of build targets) survives the rollout; its three paths may not.

**THE 13:27Z REBUILD WINDOW WAS TAKEN IN FULL, AND THE STORE MIGRATION WITH IT.** Driven 13:49-13:52Z: release, debug and the daemon all from one commit; the store carries `threads.revision`; `schema/ddl.sql` was already generated and committed, so the inert column is self-consistent and `AC-06.10` was never at risk. **The keg refuses a newer store correctly** -- both versions named, direction stated, rc=1. Recorded as a PROPERTY: the window closed cleanly and `SchemaMismatch` is smaller than the S I sized it at. **The version numbers are deliberately not written here; re-drive them.**

**vc's hv-board entry saying the tool is broken and the rebuild untaken went stale while it was being written.** Reported to vc with the derivation and the ask to re-drive before correcting; hv's board is vc's pen and I do not write a peer's area.

**LANDED TODAY:** daemon suite `9cf53e35`, config suite `e3110564`, CAS store half `544a83d3`, its other half `aa81ee58` + `41fe6c68`, the daemon race `3fc1d152`, `intentd --version` `361eff99`.

**NEXT WHEN THE HOLD LIFTS, in order:** `AC-06.11`'s corpus half (mine; dc took the string) -> `0206`'s `Mutation` half (needs dc's facade.rs signal) -> `SchemaMismatch` naming its artefact (S, and now smaller).

## TODO

- **`AC-06.11`'s CORPUS HALF IS MINE AND IT WILL GO RED ON A SET, NOT ONE STRING.** `remedies_are_reachable.rs` drives every path with `SENTINEL = "ST9999"` -- satisfies clap, resolves to NOTHING -- so the population is _remedies reachable from an EMPTY state_ and every state-dependent refusal is outside a corpus whose criterion claims all of them. dc's form is sharper than mine: **proving a harness can still FIRE says nothing about whether it can SEE.** The fix drives at least one path needing REAL state; dc warns it will provoke a FAMILY of state-dependent remedies at once. **Size for the set.** dc has the `ComputedSatisfaction` string; any that land in the `at`/`ac` facade arms go back to them.
- **`0206`'s CAS: STORE HALF IS IN (`544a83d3`), `Mutation` HALF IS NOT AND WAITS ON dc's facade.rs SIGNAL** (a new struct field breaks their compile). Ruled by vc: **REFUSE AND NAME, never retry** -- a silent retry re-derives from a snapshot the operator never saw, and **a fix must not be observationally identical to the defect**. The check goes INSIDE `commit_mutation`'s transaction: a compare in `apply_envelopes` NARROWS the window and does not close it, and shipping that as a compare-and-swap would overclaim. `Mutation.created_threads` is the same shape one door along (`0131` -- the DATABASE decides, inside the transaction, whether the key was free); cite it as precedent, not analogy.
- **`revision` MOVES ON WRITES THAT CHANGE NO CONTENT, AND THE CAS INHERITS THAT.** A sync round trip is such a write -- that is how `sync_direction` caught it. So a CAS on this token refuses a concurrent editor after a sync or a daemon ingest **with no semantic conflict to report**, and with the daemon ingesting constantly that may be the common case rather than the edge. **Flagged, not fixed. It belongs to the `Mutation` half.**
- **WRITING TESTS CANNOT CLOSE `AC-06.1`'s COVERAGE CLAUSE, AND THE HOLE IS A HIGHLANDER ONE.** `daemon` and `config` now have suites, each DRIVEN THREE WAYS (v3 all pass; `/usr/bin/false` all fail, so every arm reaches the CLI; the DEFAULT binding all-but-one fail) -- because the estate's default `INTENT_BIN` is `bin/intent`, **the v2 SHELL SCRIPT**. A non-zero DEFAULT failure count is UNSTABLE under burn.sh and carries no classification, so `coverage_map` cannot count either file however correct it is. **IT IS THREE BINARIES, NOT TWO** (vc): `test_helper.bash:21` -> v2 shell; `run_v2_suite.bash:55` -> v3 DEBUG; `~/.local/bin/intent` -> v3 RELEASE. An undeclared population of build targets. **Flip THEN re-baseline or the wall time is paid twice. NOT MINE TO TAKE.**
- **`coverage_map.sh` REFUSES TO PUBLISH, CORRECTLY.** The burn TSV no longer covers the estate (16 never measured, 1 stale row, 99-row baseline dated 15 Aug against 114 real `.bats` -- vc's corrected count, not 113). Remedy is re-running `burn.sh`, a DOUBLE full-estate sweep that hung 3.5h once. **I DO NOT LAUNCH IT** -- full-suite runs are the human's. **Both holes survive the refusal independently:** `grep -rn 'intent config' tests/` and the same for `daemon` are ZERO.
- **WP-08 IS 12/12 GREEN AND `wp done` IS AVAILABLE TO ME (XS). NOT TAKEN.** Closing it marks the daemon DONE while its family carries zero conformance coverage -- a green over an uncovered surface. hv's sequencing call.
- **`AC-06.1` IS RED ON TWO NAMED HOLES.** The 27 UNSTABLE are established (four causes, 25 = `125f601d`); read at `parity/ac-06_1-unstable-causes.md`. The COVERAGE half is above. **`config` is an ORDERING CONSTRAINT WITH AN EXPIRY** -- the rider binds `config` ALONE, so writing `daemon`'s tests late was legal where writing `config`'s late FAILS the clause. That is why `config`'s suite pins DECLARED-and-UNBUILT at rc=2 rather than waiting.
- **`AC-10.5` CHAINS BEHIND WP-14, UNOWNED AND UNSTARTED (L).** Red on substance: the MIGRATION does not name its residue. `0183`'s original remedy was wrong and is corrected -- 110 of baize's 135 are `intent/whiteboard/`, leaving the not-modelled set at **D30** (hv, WP-14 carries it): IN the model and NOT YET BUILT. Naming them would zero the loss by asserting the project meant to abandon what hv ruled it would carry.
- **`AC-06.8` IS NOT MINE AND MOVES ON `0181`.** `agents init --template` reads `retire` (vc's `0180` is EXECUTED); `llm usage_rules --symlink` is still `keep` and unread -- hv's. **Do not sequence anything behind me on it.**
- **THE `hoist` ROW'S RE-PIN RULE IS STALE AND UNEXECUTED.** It names `canary`, `lamplight`, `utilz`, `baize` as repositories whose migrations have NOT run; **all four declare 3.0.0 on disk**, canary included because canary IS this repo. Re-pinning would point the row at a tree with no v2 source -- the exact harm the rule prevents. **DO NOT RE-PIN.**
- **THE MIGRATOR DOES NOT COMMIT, SO THE DOCUMENTED ROLLBACK HAS NO SUBJECT.** `migration.md` Phase B step 7 says _one commit_ and `AC-00.8` says _one visible commit_; measured, `intent upgrade` returns rc=0 with HEAD unchanged and 23 paths dirty. **_One visible commit_ is an OPERATOR CONVENTION, not a migrator guarantee.** hv's call.
- **`0205` IS THREE BLOCKS FOR ic AND A FOURTH THAT IS NOT OURS.** `lib_classify.sh` x2 + `drift_check.sh`; `bin/.devbin/lib/builtins:66` is VENDORED under an integrity manifest, so it goes upstream or is accepted. **The remedy is the QUOTED HEREDOC, never single-quoting** -- single-quoting moves the hazard to the apostrophe, likelier in prose than a double quote.
- **`SchemaMismatch` SHOULD NAME ITS ARTEFACT (S, ruled in).** Path plus both versions, then point at `--version` for the commit -- inlining it would be a SECOND HOME for an identity that now has exactly one. General form (vc): **A REFUSAL THAT IS CORRECT AND DOES NOT SAY WHAT TO DO NEXT.**
- **WP-10 IS THE CHOKE POINT FOR vc's LARGEST BLOCK.** `AC-00.2`, `AC-00.8`, `AC-00.10` sit behind WP-10, dependent on WP-06 (mine) AND WP-07 (dc's). Read off the cover.
- **UNDISPOSITIONED, ALL hv's VIA vc:** `0086` and `0063` would have to be WRITTEN, not recovered. `0095`/`0096` are one-word stubs. `0192` -- **if hv rules it in, the refusal goes in `info_read_back`, NOT `authored_regions`.** **And the daemon-lock race has NO ISSUE** -- `issues add` regenerates canon as a side effect, so it was not filed mid-hold. It wants a record.
- **THE MCP DISPOSITION IS DECIDED, NOT DEFERRED.** SEVERITY EXPOSED (a classification over an enumerated domain); TITLE WITHHELD (authored text). My split was right; parking it _until after the tag_ was the phantom hv struck.

## Watch-outs

**THE INSTRUMENT ANSWERED A DIFFERENT QUESTION -- SEVEN INSTANCES IN ONE DAY, AND THE THREE THAT COST MOST WERE MINE.** A SELECTOR that resembles the population is not the population. `--help` RENDERS FOR A DECLARED COMMAND whether or not an arm exists. `bash -n` answers _is this valid bash_, never _is this the file I meant_ -- after any programmatic edit, **grep for what should now be there**. And the three from today: `grep -i FAILED` matches `0 failed` in every PASSING summary, so a detector reported REPRODUCED on a green run; a capture that grepped the Display strings **dropped the Debug line carrying the evidence, destroying a reproduction I had waited an hour for**; and `pkill -f 'cargo test --workspace --no-fail-fast'` is dc's recipe verbatim, so a pattern every session runs was used to kill "my" process. **THE COMMON SHAPE: the instrument is checked against what I MEANT rather than against a sample of what it will SEE. A planted three-line fixture catches every one of them in two seconds.**

**A VERIFICATION SCOPED TO THE THING CHANGED IS NOT A VERIFICATION** (dc's, and the one I would not have recognised). I ran `intentsvcs --test suite`, got 1174/0, reported green -- and the pin guarding my change lived in `intent-cli`, so the crate I edited reported green while the crate watching me reported red. **The blast radius of a published value is every crate that PINS it, not the crate that DEFINES it**; `grep -rn <constant> --include='*.rs'` finds that corpus in a second. Unlike a bad grep, this one LOOKS like diligence.

**THE CORPUS IS THE CLAIM; THE VERDICT IS NOT.** Everything three instruments reported one morning was TRUE and every corpus was narrower than its criterion. vc's minted form, and dc builds to it too: beside the verdict, in the OUTPUT and never a comment -- **POPULATION** (the set examined, with its derivation named; `RECORDED` sits AT the number where it cannot be derived), **FORMS** (per-form counts, never a single total), **REACH** (`COVERS` / `DOES NOT` / `UNOWNED`).

**A CONTROL THAT CANNOT DISTINGUISH _SAFE_ FROM _NEVER TRIED_ IS NOT A CONTROL.** Drive an instrument to BOTH verdicts or its green means nothing. Measured today: a `config set` no-mutation probe where BOTH shasums came back empty because `init` had failed on a flag it does not take, so _unchanged_ had no subject; and a fix whose test was negative-controlled by zeroing its budget until it FAILED. **State the bound's direction** -- the `0206` harness's trials are an UPPER bound, so its loss rate cannot flatter the finding.

**A CLOCK IS NOT A VERSION, AND THE PROBE THAT SAID SO FAILED.** `threads.updated_at` already moves on every change-door write. My hypothesis was that millisecond resolution is a hole; SIX writes gave SIX distinct stamps 19-37ms apart, so **IT DOES NOT REPRODUCE** and is NOT evidence against `updated_at`. What survives is a design argument: adequacy rests on PROCESS SCHEDULING, and my gap was dominated by process startup so it says nothing about the daemon path where both writes share one process. **A measurement taken on the slow path is not evidence about the fast one.**

**A REPRODUCTION PROFILE IS A DIAGNOSIS.** _Needs the full workspace, invisible with the subsystem alone_ says the cause is not in the code under test but in what SHARES ITS PROCESS. That is what put me on `fork`: an `flock` releases when the last descriptor on its open file description closes, `fork` copies every descriptor, and `O_CLOEXEC` clears the copy only at `exec`. This library shells out to git, shellcheck and launchctl from ORDINARY paths while 1174 tests share one process.

**THE SHELL LIES ABOUT SUCCESS ON THIS BOX.** `cmd | tail` reads tail's status -- **I read `config`'s rc through `head` and got 0 for what is really rc=2.** zsh has no `PIPESTATUS`, does NOT word-split `$var`, and an unmatched glob ABORTS the command (`--include=*.sh` ran nothing). `grep` is `ugrep`. `sed -i ''` SUCCEEDS on no match. `read -t N </dev/null` returns INSTANTLY. `find` is bfs, and `find .` walks `target/` until it TIMES OUT. **cwd persists between calls.** `2>/dev/null` on a source printing `command not found` returns an empty lookup that reads as data.

**THE SHARED CHECKOUT: CANON CANNOT BE SPLIT, SO EVERY CANON COMMIT IS SILENTLY MULTI-NODE.** ic's mechanism: with intentd auto-ingesting, `ST0056.json` names the STORE's union of every dirty-and-ingested attachment, **so any node holding a dirty file under `parity/tools/` blocks every other node's canon commit on that thread** -- and committing the attachment without the extract fails the other way. Four-way deadlock in twenty minutes with everyone following the rule. `--only` is the only safe write; a live `index.lock` is a WAIT, never a removal; never `--no-verify`. **PROVENANCE IS CARRIED BY PATH** -- every node commits as the same person.

**TWO CORRECT RULES CAN TAKE THE MACHINE DOWN.** A release build DELETES the shared pair before building (`0196`), and the shared-artefact guard REDIRECTS a dirty-tree build to `target/private/`. Together: the delete happened and the replace declined, and `intent` was gone estate-wide for fifty minutes. **The guard prints the dangling symlinks BEFORE the delete, so the information exists and the ORDERING defeats it.** Recovery needed BOTH nodes to stash: either one's single uncommitted file kept the build redirected, so **neither could restore service alone**. And the commit gate blocks on the dangling symlink, so committing was blocked BY the outage rather than being its cure.

**A LADDER DECLARED AS DATA IS READ FROM THE DATA, NEVER RECONSTRUCTED FROM HISTORY.** vc called the store ladder's rung COUNT unverifiable, on a correct premise -- `git log -S` counts occurrences of a substring and a value change alters no count -- and the conclusion is still wrong: `MIGRATIONS: &[(i32, &str)]` ENUMERATES every rung in source. Driven 14:00Z: rungs 2..17 contiguous, 16 of them, max == `SCHEMA_VERSION` == 17, so the keg's 13 to this store's 17 crosses exactly **14, 15, 16, 17 -- four rungs, measured rather than subtracted.** My own earlier `four rungs` was arithmetic on two endpoints wearing a measured message's marker, which is vc's 8f in my hand one clause deep. **General form: before going to history for a number, ask whether the thing declares itself.**

**AND THE INSTRUMENT MISSED THE LADDER TWICE ON THE WAY TO THAT, BOTH CAUGHT BY ONE ASSERTION.** First `awk '/pub const MIGRATIONS/,/^\];/'` never opened its range -- the decl is `const`, not `pub const` -- and returned 0 rungs. Then `grep -E '^  [0-9]+,$'` returned a plausible **13 rungs topping at 14**, because rungs 2-14 sit at 2-space indent and **15, 16, 17 sit at 4**. That second answer is the dangerous one: it looks like a result. **The control that killed both was `rung 17 MUST be present and max MUST equal SCHEMA_VERSION`** -- a rung I wrote myself, so its absence could only be the pattern. **Assert a member you KNOW is in the population, not just a plausible-looking total.**

**A PIN THAT TRAILS HEAD IS THE CORRECT STEADY STATE, NOT STALENESS** (dc's, and the sharpest form anyone has given it). The binaries pin the last commit that touched what they COMPILE, so on a board-heavy day HEAD runs ahead and a naive `HEAD vs pin` diff reads the binary as several commits behind while nothing it compiles has moved. The discriminator is one command: `git rev-list -1 HEAD -- native/rust surface docs/design` -- equal to the pin means CURRENT. Driven 13:57Z: both `361eff99` against HEAD `32a9bb07`. The gate's self-provenance arm rules the same way in its own words (_no non-test file under native/rust has changed since the commit the pair names_). **CAUTION ON THE COMMAND, NOT THE PROPERTY: that path list IS the guard's `DIRT_SCOPE`, so a devbin change to how Rust is built can move it. Re-read the scope from the guard before trusting the command; the property survives, the argument list may not.**

**A PEER RE-DRIVING ONE PREMISE CAN FORWARD ANOTHER UNDRIVEN, AND MINE WERE THE UNDRIVEN ONES.** dc correctly refused to relay vc's reading of the artefact -- _passing on a reading is how a stale-premise chain gets one link longer instead of ending_ -- and in the same message told me my board carried the broken-tool claim (it never did; the only `command not found` on it is a watch-out about `2>/dev/null` swallowing shell errors) and that my `LockHeld` commit stood refused (`git merge-base --is-ancestor 3fc1d152 HEAD` says it landed). **The re-drive was applied to the SUBJECT and not to the claims about the RECIPIENT.** Cheap general form: a message correcting someone's state is itself a state claim, and it is checkable in one command before sending. I have the same hole -- read this before writing my next correction to a peer.

**AN OPEN ESCALATION AGES INTO AN ASSERTION** (vc). True when written, never re-read because it is _already asked_, accruing authority it never earned -- my `sync` skip escalation sat on hv's desk for a week and `sync.rs:856` had already answered it. **Before anyone budgets against an escalation, its AUTHOR re-drives it.** Same family: **CONSTRUCTING BEATS READING, MEASURING BEATS BOTH, AND THE DISPROOF IS USUALLY ALREADY IN HAND** -- `0082` held `0199`'s mechanism, `sync.rs:148` refuted `0183` at the site.

**FACTS THAT LOOK LIKE OPINIONS.** `intent st attach` writes the store AND the canon extract and NEVER the disk file (`0082`); the daemon watches DISK, so a disk edit converges everything and a store-side write renders nothing back. A thread must be REALISED or it has no cover. `intent ac show` is not a verb. `intent init --name` is not a flag. **`--version` names WHICH BUILD and the version names only WHICH LINE** -- but a matching marker proves a pair agree only when neither is `dirty-`; under dirt, pin by the hash.

## Decisions

- (2026-09-01) **hv: v3.0.1 IS FEATURE COMPLETE, THERE IS NO TAG WINDOW AND NO EXTERNAL CONSUMER, AND COST IS NOT A CONSTRAINT.** The SCARCITY REGISTER is retired AS A CLASS -- _ship it red and name it_, _not in this cut_, _after the tag_, _new machinery in a tag window_ were all unstated claims that time was short. **A recommendation resting on _there is not time_ must be re-derived on its merits or withdrawn.** The release-number question is CLOSED and is not to be put again.
- (2026-09-01) **A REFUSAL THAT CANNOT SAY WHAT IT FOUND MAKES ITS OWN DEFECT UNDIAGNOSABLE.** Both `AlreadyRunning` branches rendered identically, so the first move on the race was making them distinguishable rather than editing the code under suspicion. dc reasoned to the probe branch and I reasoned to a branch-independent remedy; **the evidence field is the only reason either was caught.**
- (2026-09-01) **A REMEDY INHERITS ITS BRANCH'S ASYMMETRY.** Confirm-before-refuse is SAFE on the lock (a real daemon holds for life, so a re-check only rescues a false refusal) and WRONG on the probe (answering means refuse, silence means UNLINK, so confirming makes the DESTRUCTIVE outcome likelier -- the false negative `AC-08.12` forbids). **I concluded it was branch-independent and testing the claim is what stopped it shipping.**
- (2026-09-01) **EMBEDDED AND UNPRINTED IS THE WORST OF THE THREE STATES.** `intentd` carried its source commit all along and `--version` would not say it, so the tooling could answer _which build_ and the operator could not. Absent would have been honest.
- (2026-08-31) **A CRITERION IS NOT REWORDED TO WHAT THE CURRENT STATE SATISFIES** (vc). The ruled form must be HARDER than the original and bind the node holding the divergence.
- (2026-08-31) **A CLASS CHANGE WITHOUT ITS REASON IS A DELETION WEARING A NEW LABEL** (vc). A reclassification is a CLAIM ABOUT WHY: `deviate` must RETARGET rather than delete, recorded where the artefact is GENERATED FROM.
- (2026-08-31) **A DECLARED EXCLUSION CARRYING ITS REASON IS THE CURE; A SILENT DROP IS THE DENOMINATOR ATTACK.** The difference is entirely whether the reader is told.
- (2026-08-31) **AN OWNERSHIP SPLIT IS A PURPOSE, NOT A BOUNDARY** (ic, correcting me). Reverting so a peer can rebuild byte-identical code is churn wearing Highlander's clothes. Applied again today: dc took the `ComputedSatisfaction` string because they were already compiling that file, and **two hands in one file IS `0206` in miniature.**
- (2026-08-31, CORRECTED) **`close --note` STAYS RULED OUT ON A CONTINGENT FOOTING.** The keg has no `edit`; the ruling survives because v3.0.1 ships from the tree. **Two 3.0.0 artefacts still differ -- re-driven 2026-09-01 13:36Z: tree `98ccc62c` naming `361eff99`, keg `04e7dda8` naming `80d8b2ca`.**
