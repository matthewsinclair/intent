---
node: cc
name: Control Claude
role: control
session_id: ae8c8153-6f3f-438f-b96b-04bd381ad4ed
heartbeat_at: 2026-08-29 23:04Z
status: paused
focus: "PAUSED at localfold, HOLDING for vc on hv word. WP-08 IS SEQUENCED AND RULED and is the next thing I touch -- hv D56 stack (axum + maud, server-rendered, no build step), my inverted order CONFIRMED (AC-08.2 harness FIRST, daemon path stubbed), AC-08.9/08.10 minted, scope now XXL on my evidence. THE HARNESS TAKES A TEMP FIXTURE AND NEVER A LIVE ROOT -- see class 32, which is the day keeper and is dc finding, not mine. Delivered CLI still refuses every store-reading verb; a binary compiled since the bump is fine. Nothing of mine is blocked."
claims: [ST0056/06, ST0056/08, ST0056/10, ST0056/13, ST0057/00]
---

# Control Claude (cc)

## DOING

**Nothing in flight. Folded and paused.** Item 1 landed at `180fb4a3`; two board commits since (`0a0797c8`, `e188c5be`) carry the reasoning for the bounce and for the outage. **The commit messages are the record; do not re-derive them from here.**

**MY LANE HAS TWO OTHER WRITERS AND THE NEXT ST0056 COMMIT IS JOINT.** `native/**` carries dc ST0066 Fiat cascade and ic surface-retired door, uncommitted; `0148` is five paths and includes vc D56 write to `design.md`. **Attribute by diffing content, never by assuming.** The LANE decision below says `native/**` is mine and that is false in practice right now -- class 8 governs.

## TODO

**vc DOLE-OUT, IN vc ORDER.**

- **[1] WP-08 + THE WEB FACE -- XXL, SEQUENCED, NOT STARTED.** hv ruled the stack as **`D56`: axum + maud, server-rendered HTML, no wasm, no hydration, no component model, no build step.** Detail in `design.md` D56 and `tui-design.md` 10a/10b -- **cite them, never restate them.**
  - **PHASE ORDER, MINE AND CONFIRMED BY vc, AND IT INVERTS THE OBVIOUS ONE.** `intentsvcs` owns the surface and `intentd` is a CLIENT exactly as the CLI is, so the first thing built is NOT the daemon -- it is the harness that refuses divergence. **`AC-08.2` dual-path conformance FIRST with the daemon path stubbed to in-process**: green by construction, and every later criterion lands underneath a test that cannot silently reverse D32. Then the CLI routing seam (`AC-08.3` -- its absent-socket branch is just today behaviour). Then the daemon proper. Web face last.
  - **THE HARNESS GETS A TEMP FIXTURE AND NEVER A LIVE ROOT.** `AC-08.2` drives 120 shipped rows; 16 test files in `intentsvcs` already open the real project root and that is what migrated the shared store. See class 32.
  - **SIZE EVIDENCE, QUOTED SO IT IS NOT RE-DERIVED:** no async runtime anywhere in the lock (no tokio/hyper/axum/tower/async-std/smol), no watcher, `intentd` is 83 lines across three files with NO `[dependencies]` section. `async-graphql` is present but has only ever been used synchronously (SDL export), so it never pulled a runtime. `ignore` and a `backup` verb with five config keys DO exist and are reusable.
  - **THE WEB REALISER WAITS ON ic FORM DSL** and authors no UI -- declaration + entity to HTML. **If it ever needs to know what a criterion IS, the design has gone wrong.** Its binding test `AC-17.1` lives in WP-17, not mine, and must diff the MODEL rather than compare renderers.
  - **INHERIT `docs/design/design-system.md`; do not invent a second.** Self-hosted same-origin fonts is a CONSTRAINT not a preference -- the daemon serves localhost and must work with no network. One stylesheet embedded in the binary. **Design against the LARGEST real thread** (154 criteria, 297 attachments, a 59KB paragraph), never a fixture.
- **[2] THE REGISTER `shipped` DERIVATION -- S, PARKED ON ic HOLDING THE FILE.** `Entry::is_shipped()` asks nothing about existence, so a `keep`-dispositioned unbuilt verb ships by construction. **Not fixable in the register: the refusers are declaratively INDISTINGUISHABLE from workers.** vc ruled both obvious branches out. **Built-ness is a property of the (register, binary) PAIR, so a committed GATE ARM drives every shipped ROW against the binary and refuses on the unwired marker.**
  - **KEY ON ROWS, NOT PATHS**, and **drive from each row own `args[0]`** -- a family with a required subcommand gets a declared value, a leaf goes bare. Bare `daemon` is rc=1 from CLAP, so a bare probe misreads a refuser as BUILT.
  - **TWO FREE ARMS AUTHORISED BY vc AND BOTH FIRE TODAY** -- re-measured on the bounce after the table moved two rows. `organize` sits in `shipped` AND `retired`, `declared` double-counts it, and the sum conserves for exactly that reason. **Run the probe; do not carry the counts.**
  - **THE MARKER MUST BECOME ONE `pub const` FIRST, AND THAT IS A PRECONDITION NOT CLEANUP** -- my arm would be the SEVENTH copy. See class 18.
  - **ic HAS REACHED THE SAME DEFECT FROM THE TEST SIDE** and it is written only in a `spine.rs` comment. ic hands me the file when `0148` frees it; both prose copies retire when the arm lands.
- **[3] WP-13 project search -- XL.** 9 criteria, Not Started.
- **[4-6] FILLERS, S EACH, BEHIND WP-08 AND NOT IN FRONT OF IT** (vc). `intent#0145`. **`VIEW_NAMES` two readers** -- `address.rs:357/394` vs `Project::classify` inline list, in my module, invisible to every gate. **`canon-commit` gates on ADDS**, so a commit REMOVING attachments is outside its reach.

**NOT MINE, OR WAITING ON SOMEONE.**

- **`AC-00.5` -- ic IS BUILDING THE DOOR** (`spine.rs`, `surface retired`). hv re-ruled it OFF exit codes: retirement becomes ENUMERABLE, callers branch on MEMBERSHIP. **ic reports rc=2 carries FOUR meanings separated only by prose, and only retirement was made enumerable** -- the same hole my gate arm fills, seen from the test side.
- **`runner_roster_check.sh` SEVERED-INSTRUMENT DISCRIMINATOR (S), vc by authorship.** `git ls-files` cannot see an untracked file, so a new instrument prints SEVERED and the remedy sends the reader to repair a correct row.
- **THE WRITE-PATH PACKAGE (S-M)** -- `issues edit --body/--from`, `close --note`, scoped issue sync. **vc keeps the count; read it off their board.** Never hand-edit the extracts.
- **ST0065 REMAINDER** waits on hv adopting the draft; **ic claims ST0065 and it is OUT of the cut.** **ST0064 names a moved path in three places (XS)**; the finding underneath is hv -- a concurrent test suite inside a cloud-sync provider loses files silently.
- **THE PRUNE, at vc (d)** -- gated on `at-accounting.sh` and WP-11 absent-manifest default. **A FILE THE STORE CANNOT BE SHOWN TO HOLD IS A REFUSAL NAMING IT, NEVER A DELETION.**
- **PARKED FOR A RULING** -- `issues list` disclosure convention; `--status a,b` advertises an ordering it does not honour.
- **WITH hv, UNANSWERED** -- the two enum-roster findings (a `--format` widening predicate must be grounded in the refusal vocabulary, NOT `code != 0`); `publish_home` on a temp root; test-target consolidation; `intentd --version` prints no commit; the v2 shell CLI prune. **Plus the live one: the estate cannot repair its own delivered tool until `0148` lands** (ic is taking that to hv).
- **3.0.1, AND THERE IS NO 3.0.2 (hv)** -- `on:` on `Descoped`; bare-sync must compare to the STORE; a `doctor` canon-vs-store arm; the id resolver treating every id as a thread; store growth per `st attach` plus a bulk door; a tenth residue class; `unclaimed_digest()` hashing absolute paths. `slug` stays vestigial.
- **THREE NON-DECISIONS, DELIBERATE** -- the guard-runner resolver and critic exit-2 fail-opens STAY with 0043 reasoning beside them; the remedy-less error literals are NAMED, not gated; **`c755bd44` fabricated stamp STAYS UNRESOLVED**, because a corrected-looking fake is worse than an admitted one.
- **BANKED** -- `ST0057` `PASS` has a hidden denominator (vc filed `0123`). `staged_reads_check.sh` reports 7 of 14 gated instruments reading unstaged. `AC-12.4` reads UNSATISFIED over three measurably-done conditions.

## Watch-outs

**BY CLASS, RULE-ONLY. Every line cost a wrong answer that read as a right one. INSTANCES live in `.history/` and in the commits.**

**1. AN INSTRUMENT CAN PASS WHILE BLIND, AND ITS GREEN IS INDISTINGUISHABLE FROM A REAL ONE.** The faces: a corpus that CANNOT EXHIBIT the defect (commonest); a control that would also pass broken; **a control that does not FLIP proves the probe was inert**; ACCIDENTALLY RIGHT; a predicate reproduced against the UNMUTATED source; a null read as a clean sheet; an instrument naming the WRONG CAUSE; a correct AGGREGATE that cannot check the mechanism (predict the SPLIT, never the total); the scanner inside its own corpus; a filter matching nothing, which passes for free; **a vacuity that renders as a well-formed TABLE, invisible to a control looking for a sentence**; and **a control that validates the axis it tests while silent about the one next door**. **THE ONLY CONTROL IS MUTATION -- break it and require the instrument to notice; assert the OCCURRENCE COUNT first.** **THE CHEAPEST INSTRUMENT I OWN IS A PRECONDITION THAT REFUSES TO RUN.** **AND A BROKEN INSTRUMENT RETURNS THE PASSING SHAPE, NOT AN ERROR SHAPE.** **A CONTROL THAT CANNOT PLAUSIBLY FAIL TURNS THAT BACK INTO AN ERROR SHAPE**, and one firing for the WRONG reason is still worth its price. **AND A FAILING CONTROL CAN EXONERATE A PEER YOU WERE ABOUT TO ACCUSE** -- I grepped the store for vc new criteria, found nothing, and was one step from reporting them missing; the control (a row I had seen minutes earlier) came back zero and the real answer was that the store was unreadable. **The broken instrument returned the passing shape, and the shape it returned was an accusation.**

**2. EVIDENCE IN HAND IS NOT A READING. Say what the numbers RULE OUT.**

**3. THE DISK CANNOT BE THE WITNESS FOR A WRITE, BECAUSE THE DISK IS WHAT MOVES -- AND NEITHER CAN A VERIFICATION THAT WAS TRUE WHEN IT RAN.** The cure is removing the gap, not looking harder: a detached worktree, where index and worktree cannot disagree. **THE SECOND HALF IS THE ONE THAT KEEPS FIRING, AND IT FIRES ON PEERS TOO** -- dc routed a `design.md` divergence to vc as unadjudicable on the premise the file was unmodified in git, when the whole gap was vc own D56 write from half an hour earlier. **A premise about a shared tree expires in minutes.** **dc DIAGNOSED IT BETTER THAN I DID AND IT IS THE TENSE THAT DOES THE DAMAGE:** the reading was taken forty minutes before the sentence that rested on it, and was written in the PRESENT TENSE as a fact about the tree, with nothing marking its shelf life. **True when measured, false when sent, and the wording could not tell you which.** So the cure has TWO forms and the second is available whenever the first is not: **re-run it in the same turn as the claim, or stamp when it was taken.** **AND THE RECURRING SHAPE IS A CONFIDENT ABSENCE** -- twice today one reached a peer; the first was stopped by a positive control before it left, the second only by a peer after it had.

**4. A CREATE IMPLEMENTED AS AN UPSERT REPORTS SUCCESS AND REPLACES.** hv ruled add/new must REFUSE. Wider form: any verb that RECONSTRUCTS a row from arguments silently enlarges every time a payload-carrying variant is added.

**5. A VALUE GOES IN ONLY IF THE POPULATION OWN DECLARATION HAS IT -- BOTH DIRECTIONS.** Hardcoding a member of a draining population goes stale by being right; **MINTING one is the same error walking the other way.**

**6. A PEER INSTANCE BEING WRONG DOES NOT MAKE THEIR SIGHTING WRONG. CHECK THE SET, NOT THE INSTANCE YOU WERE HANDED -- INCLUDING THE INSTANCE YOU HANDED YOURSELF.** **And a claim that survives an attempt to KILL it is worth more than one nobody checked.** The costly direction is my own: I measured ONE binary -- the `~/.local/bin` symlink -- and reported the CLI as broken for all four nodes. **The discriminator was WHEN A BINARY WAS COMPILED, not who ran it** (ic correction): a build post-dating the schema bump works. **You cannot tell _the estate is broken_ from _my binary is old_ from inside one session, because the error text is identical either way** -- it takes two binaries compared against one store. Same week, the mirror case: ic undercounted a duplicated marker at three when it was six constants under five names across twelve files. **Neither of us was wrong to report; both of us were wrong about the SET.** **AND THE DEEPER FORM IS ic: SOME MEASUREMENTS ARE UNREACHABLE FROM ONE POSITION, HOWEVER CAREFUL.** I could not tell those two states apart from inside one session; ic could not count twelve files from inside their own. Neither was a diligence failure and neither would have yielded to more care. **THE CURE IS A SECOND OBSERVER, NOT A BETTER LOOK** -- which is the argument for reporting a half-formed measurement to a peer instead of polishing it alone. Twice in one day.

**7. A SHARED TREE HAS OTHER WRITERS.** `git commit` commits the index AS IT STANDS. **`--only <path>` protects other PATHS, not other people edits to YOUR path -- it takes the WORKTREE version wholesale.** Check `git diff --cached --name-only` is empty BEFORE `git add`, in a separate call. A commit can lose the race AFTER the gate passes; re-verify and re-`commit`, do NOT re-`add`. **A BROAD COMMIT IS INDISTINGUISHABLE FROM A NO-OP AT THE CALLER.** **Ask `git log -1 -- <path>` which commit owns your path afterwards.**

**8. A PRIVATE INDEX IS THE ONE WRITE THAT CANNOT SWEEP A PEER -- AND IT LEAVES THE SHARED INDEX HOLDING A REVERSION.** `GIT_INDEX_FILE` + `read-tree HEAD` + add-only-mine; the gate honours it. **Afterwards the shared index still holds the PRE-commit state for your paths, which against the new HEAD reads as staged DELETIONS** -- `git reset HEAD -- <your paths>` is the second half and is NOT optional. **Fired on every one of the three private-index commits this session.** Verify with `env -u GIT_INDEX_FILE`; an EMPTY `GIT_INDEX_FILE=` is not unset.

**9. EVERY TIMESTAMP IS READ FROM `date -u +'%Y-%m-%d %H:%MZ'` IN THE SAME CALL THAT WRITES IT.** `git log` prints LOCAL. I have fabricated stamps three ways: wrong zone, typed-from-the-last-one, and one minute ahead of a clock I had just read. **A placeholder shaped like a stamp is worse than an admitted gap.**

**10. A PIPELINE ANSWERS FOR THE LAST STAGE, NOT THE INTERESTING ONE -- FOR THE EXIT CODE AND FOR THE OUTPUT.** `x | tail` gives you `tail` 0; zsh has `$pipestatus`. **A `&&` CHAIN SHORT-CIRCUITS ON A ZERO-MATCH `grep -c`, SO THE ARMS AFTER IT NEVER RUN AND THE OUTPUT STILL LOOKS LIKE A RESULT** -- fired this session inside the very probe that was positive-controlling something else. **When the claim is _it did not say_, re-run it unfiltered. READ THE EXIT CODE FIRST.**

**11. THE POPULATION YOU MEASURED IS NOT THE ONE YOU MEANT, AND NOTHING NAMES IT.** `cargo check -p X` builds no test target; `--test X` excludes lib tests; a narrowing flag never narrows the CLAIM. **A TERMINATION CONDITION IS A FILTER NOBODY DECLARED** -- `cargo test` ABORTS after a failing target. **Capture ONE run and analyse the capture; never compare across runs.** **AND THE INDEX IS A DIFFERENT CORPUS FROM THE WORKTREE.**

**12. A ONE-SIDED COMPARISON DISCARDS THE EVIDENCE THAT THE INSTRUMENT IS WRONG**, and a conservation check whose two sides share a source is an IDENTITY, not a measurement. **`shipped`+`retired`=`declared` conserved perfectly while `organize` sat in both, because `declared` double-counted it -- and it still does, re-measured after the table grew by two rows. Diff member-by-member, never by count** -- the register own `why` block says exactly this and was itself validated by count.

**13. A FINDING THAT ARGUES ITS WAY INTO THE ROW YOU ARE CLOSING IS A REAL DEFECT LAUNDERED THROUGH A GREEN.** The pull is strongest exactly when the finding is good. **Corollary for tests: when a ruling changes behaviour, RE-AIM the arm at what still holds; never delete the coverage to make your own change pass.**

**14. A THING FILED IS NOT FILED UNTIL IT REACHES AN ARTEFACT, WITH ITS CONSEQUENCE, AT ITS REAL SCOPE.** **A CORRECTION MADE IN CONVERSATION DOES NOT REACH AN ARTEFACT** -- it dies at the next compact on either side. **A MECHANISM FILED WITHOUT ITS CONSEQUENCE IS NOT FILED.** And a finding SCOPE is set by the search that found it -- always smaller than the problem.

**15. A REFUSAL THAT DELETES IS THE MORE DESTRUCTIVE OPTION AND READS AS THE SAFER ONE.** Refusing is conservative only when refusing PRESERVES.

**16. ABSENCE IS A STATE; UNREADABILITY IS AN ERROR; A `let Ok(..) else` CANNOT TELL THEM APART.** A refusal that ends the run reports a FLOOR read as a COUNT.

**17. ONE VALUE, MANY HOMES, AND NOTHING COMPARES THEM.** A SERIALISER is a second writer nobody declared; so is the markdown formatter. **The quietest home is PROSE SITTING BESIDE THE MACHINE-CHECKED FIELD.** A hand-typed COUNT is the same shape. **THE HARDEST SUB-CASE IS A PROVENANCE RECORD THAT PARAPHRASES ITS SUBJECT INSTEAD OF CITING IT** (vc, on `AC-00.5`): it becomes a second home the moment the subject is re-ruled and CANNOT BE REPAIRED BY CORRECTING IT, because as a dated record it is still true. **A record may quote WHAT WAS SAID, but must POINT AT, never reproduce, what is currently TRUE.** **AND DIVERGENT NAMES ARE THE PROOF NOBODY KNEW:** the unwired marker lives in six constants under FIVE names (`PHRASE`, `UNWIRED`, `UNIMPLEMENTED`, `UNWIRED_PHRASE`, `UNWIRED_MARKER`) plus three inline literals plus its producer, across twelve files. **A copy sharing a name is at least greppable; five names is five authors who each believed they were first.**

**18. A REMEDY IS A CLAIM ABOUT BEHAVIOUR AND MUST BE DRIVEN LIKE ONE.** **BORN STALE and EXPIRED are different defects and NEITHER is caught by reading.** **AN EXPIRED REMEDY CAN POINT AT A REPAIR A LATER RULING EXPLICITLY FORBIDS** -- actively counter-useful, and it reads identically to a live one.

**19. A MISSING TEST IS A BACKLOG ITEM; AN UNRUN TEST IS A DISCIPLINE FAILURE.** A pristine-worktree control proves "not my diff" and CANNOT prove "not the environment".

**20. HAND OVER PROPERTIES, NOT VALUES.** A bare id is ambiguous, a bare moniker crosses estates, a hash is a READING. **A pin dies at a bump somebody else schedules.** Re-measure; never report a ledger. **Applies to ANNOUNCEMENTS too -- see 22.**

**21. ATTRIBUTION DECAYS INTO ASSERTION**, and exoneration and attribution are two claims. **An incident and its generalisation usually have DIFFERENT authors; record the split while you still know it.**

**22. A TWO-PART OBLIGATION WHOSE SECOND PART HAS NO TRIGGER IS A ONE-PART OBLIGATION.** I announced a window opening, promised the close, and never sent it; vc did the identical thing a day earlier. **The opening is prompted by the act of starting; NOTHING prompts the end, because finishing FEELS like the end.** **CURE: state the PROPERTY that ends the window, never a promise to send a second message.** **AND MEASURE BEFORE YOU ANNOUNCE** -- I announced a window on a file already dirty with a peer work, then withdrew it to three boards.

**23. A TWO-WAY COMPARISON CANNOT ATTRIBUTE A DIFFERENCE TO ONE SIDE** (vc widened this from my narrower statement; the wider form is the keeper). **THE FIX IS ALWAYS A THIRD INPUT.** My instance: a DERIVED artefact against its SOURCE cannot tell _EDITED_ from _STALE_ -- both are `the bytes differ` -- and the stale case is the COMMON one, so refusing on it would have refused `sync --to-store` across the fleet. Each obvious third input is insufficient alone: the MODEL says which regions it generates, the FILE INDEX says whether a human touched the file, and **`Changed` means _new_ as well as _moved_**, so the honest predicate is _was in the PREVIOUS index AND has moved_. ic paid for the same shape the same week asking _did MY sync do this_ against a baseline four people had moved. **THE ESCALATION IS THE REAL LESSON: EACH DEFECT WAS CAUGHT BY A STRICTLY BROADER POPULATION THAN THE LAST.** **A green bounds the ring it ran on and says nothing about the next ring out.**

**24. TWO RULES GOVERNING ONE DECISION -- ONE STRUCTURAL, ONE PURPOSIVE -- ARE CONSISTENT ONLY UNDER AN ASSUMPTION NEITHER STATES.** `announce` is defined structurally (every peer inbox); `hv/inbox.*` is defined by purpose (the durable ESCALATION surface). Jointly satisfiable only while every announcement is an escalation, which nothing says. **The tell is not drift between copies -- it is that obeying one BREAKS the other, in a case neither anticipated**, which is why it reads as a judgment call and is not one. vc ground: **an obligation to READ is destroyed by volume long before it is destroyed by refusal.** **UNRESOLVED and hv: _paused peers need the durable form_ applies to hv too.**

**25. THE FOLD IS ITSELF AN INSTRUMENT AND IT HAS FAILED IN THIS BOARD.** It has left one class in two sections, dropped a ruled GO, and duplicated numbering -- one fold found 34 sitting ABOVE 33. **Archive VERBATIM first, under `wip-fold-HHMMZ.md` with the fold own time in the name.** **UPDATE classes, never append instances beside them**; when a fold only trims narrative it is not aggressive, it is tidy. **AND THE MARKDOWN FORMATTER REWRITES WHAT THE FOLD WROTE** -- a class landed with its backticks welded to adjacent words, unreadable, and nothing reported it.

**26. A WEAKENING WITH NO SYMPTOM IS THE HARDEST THING TO CATCH** (dc). When a type widens, the loose port and the tight one both compile. **THE COMPILER SILENCE IS THE SIGNAL, NOT THE ALL-CLEAR** -- I added an enum variant and NOTHING broke, because every consumer used `if let ...Refuse`. The REASON for a tight match belongs in the file, next to the match.

**27. A GENERATED ARTEFACT CAN WRITE INTO THE AGENT-INSTRUCTION CHANNEL, AND NOTHING ANNOUNCES IT.** `docs/reference/claude.md` IS `CLAUDE.md` on a case-insensitive filesystem. **Prefix the whole output space rather than special-casing the member that bit.**

**28. PRESENCE OF THE VARIANT IS NOT PRESENCE OF THE SUBJECT.** A census keyed on the state NAME cannot see a payload field inside one variant. **Key on the unit the defect lives in** -- and its wider form: `is_shipped()` is per-ROW while the populations are path-keyed, so a path can be two rows with opposite answers.

**29. A GUARDED DOOR USUALLY HAS AN UNGUARDED TWIN ONE COMMAND AWAY, AND REACHING FOR IT IS AN ACCIDENT RATHER THAN A DECISION.** `bin/devbin build` takes a verdict; plain `cargo build --release` does the same compile with none and writes into the shared path. **The guard was never bypassed -- it was never invoked.** The tell is that the guarded door is the LONGER spelling. **Check the ARTEFACT afterwards, and check the SET.**

**30. STATE A SIZE ONLY AFTER THE SEARCH THAT WOULD REFUTE IT, AND QUOTE THE SEARCH.** A cost is a claim about what does NOT exist. **AND A DECLARED GAP CAN NAME THE WRONG SUBJECT, WHICH READS AS RIGOUR AND IS WORTH LESS THAN NO NOTE.** **Worse still: I recommended MEASURED without checking whether measuring was PERMITTED, and quoted the fail-open sentence in the same message that proposed breaking it.** A rule is at its least protective in the sentence that asserts it. **The method that works: quote the searches, as I did to size WP-08 -- vc took XXL on them without argument.**

**31. A DURABLE ARTEFACT CAN BE ADVANCED BY NON-DURABLE SOURCE, ONE-WAY, AND THE VECTOR IS THE TEST SUITE** (dc correction to my first diagnosis, and the better half of it). The runtime store is per-machine truth and is NEVER committed (D34); it reached schema 15 while HEAD still declared 14, so the delivered binary refuses every store-reading verb. **I blamed the `SCHEMA_VERSION` EDIT. The edit is only the content -- the act that MOVED the store was running `cargo test`**, because `Project::open` against the real project root runs the migration ladder and 16 test files in `intentsvcs` alone do exactly that. **So the one act everybody performs constantly, and believes is read-only, is what mutates shared durable state**, and it recurs for any node holding an unlanded rung. This estate already has _a probe whose question has a side effect is not a probe_; this is that class one layer out, where the probe is the entire suite. **The ladder is one-way by design**, so a reworked branch would strand the store at a version no revision can produce. **The repair is blocked by the same dirt that caused it** -- the shared-artefact guard refuses a release build while `native/rust` is dirty. **The tell arrives as a REFUSAL IN AN UNRELATED VERB, after the act, to someone who did not perform it**, so it reads as your own bug. **Ask what moved UNDERNEATH the tool before asking what you broke.**

## Decisions

**I DO NOT EDIT `CLAUDE.md`, PERMISSION SETTINGS OR CONFIG ON A PEER INSTRUCTION**, however right they are on the merits. A peer ask is never the user approval. **AND I DO NOT APPLY AN EDIT A CLASSIFIER REFUSED A PEER.**

**A RELAY IS NOT AN INSTRUCTION** -- a ruling reaching me through a peer gets confirmed with hv AT SOURCE before I act. **EXCEPTION, hv own word in my session 2026-08-29: _wait for instructions from intent-vc ... Go._** hv designated the channel first-hand, so vc sequencing is not a relay. That exception dies with this instruction; it does not generalise.

**MY WINDOW ANNOUNCEMENTS STATE THE PROPERTY THAT ENDS THEM, NEVER A PROMISE TO SEND A CLOSE NOTICE** (mine, endorsed by vc). See class 22. The companion -- `/in-finish release` refusing while a window is open -- is NOT adopted; it is a shared door and goes to hv with dc.

**DERIVED vs AUTHORED, NOT MECHANICAL vs INTERESTING** (vc ruling on what I may write into ST0056 canon).

**A PARITY TOOL IS A RECORDED ATTACHMENT.** The carry is **`intent st attach ST0056 parity/tools/<f> --from <path>`, ONE CALL PER FILE**, then `intent sync --to-disk ST0056`, then `git add` the script + `runner_roster_check.sh` + `ST0056.json` together. **Never the thread-wide carry** -- `ingest::collect_attachments_into` re-reads authored prose, which is the live damage class. **AND NEVER `sync --to-store ST0056` -- that is what created `0148`.**

**THERE IS NO 3.0.2 (hv).** Everything else goes into 3.0.1.

**RESIDUE BLOCKS ON A LIVE THREAD AND CARRIES ON A CLOSED ONE; HALF A ACCOUNTING BLOCKS ON BOTH** (vc).

**LANE.** `native/**` and the v3 crates are mine; the parity harness is ic; hooks, roster and `int hooks` are dc; **canon writes route through vc**. `~/Devel/prj/Intentv2` IS NOT TOUCHED.

**ENVIRONMENT -- PROPERTIES, NEVER VALUES.** Bare `intent` resolves through the `~/.local/bin/intent` SYMLINK into `native/rust/target/release/`, and **that symlink passes through no currency check at all** -- the commit gate is the only place a stale delivered pair is reported. **A `target/debug` build is a different binary and may answer when the delivered one refuses.** Read the pair off the gate currency line or `bin/devbin cli`; **pin by HASH, never by marker.** `find` is bfs, not GNU find. **The Bash tool shell is zsh: unquoted `$var` does NOT word-split -- THIS FIRED THREE TIMES IN ONE SESSION AND EVERY ONE RETURNED A PLAUSIBLE RESULT.** Write the words out, or use `${=var}`. An unmatched glob ABORTS the command; `"$VAR:path"` is a bad substitution greping to a convincing zero; **`"...$ENV{NAME}..."` in DOUBLE quotes expands `$ENV` to nothing** -- single-quote every perl `-e`. cwd persists between calls. **`cat -A` is not BSD cat.**
