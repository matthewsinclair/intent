---
node: cc
name: Control Claude
role: control
session_id: bb27122c-3e86-4c2d-abfe-339a4fa151df
heartbeat_at: 2026-08-27 08:42Z
status: active
focus: "**BOTH DEFECTS LANDED, MUTATED AND NOW CONFIRMED LIVE IN A REAL STORE.** vc re-converted arca_cli on a throwaway using the check I prescribed rather than one of their own: **0 of 55 stored `file` values carry a comma clause, findings 19 -> 6, all 16 phantom missing-file findings gone, and the AT-drop closed in the same run (29 -> 55 rows).** The 3 that remain are D2b exactly -- prose in the covers clause, SOURCE data, no reader fix reaches them. Mutations: 6 run, 12 arms each, every prediction held; **M5 as I proposed it was decoration and the run is what said so** -- the `is_criterion_id` GATE holds that property, not the leading-token cut. HOLDING for vc: devbin standardisation is landing (`int cli` becomes the one path, `bin/intent3` deletes) and **nothing of mine is scripted against it**."
claims: [ST0056/06, ST0056/10, ST0057/00, ST0057/01, ST0057/03]
---

# Control Claude (cc)

## DOING

**vc's ASSIGNMENT, ON hv's WORD. Both defects found by driving the serial port; order mine; measure before build.**

**D1 -- THE SCANNER PANICKED ON A MULTIBYTE CHARACTER, AND IT IS MINE.** `unread_field_keys` from `a1af59f3` walked `span.as_bytes()` and sliced `span[i..]` at every byte index, so an index inside a 3-byte char panicked: rc 101, nothing written, Conflab dead on `intent upgrade`. **`char_indices()` is the fix** -- it yields only boundaries, and `i + 4` stays safe because the `starts_with(" -- ")` proves four ASCII bytes follow. 8/8 green, 3 arms new. **THE PANIC IS IN THE WALK, NOT THE RESIDUE: the row that killed Conflab has no unread field at all.** Blast radius is **161 rows / 7 estates** (Lamplight 46, Baize 41, **Intent 63**), not the 2 reported.

**D2a -- THE CITATION SPLIT KEPT THE ANNOTATION IN THE PATH.** `path, describe "..."` was cut at the ` (` inside `(AC-07.1)`, so `file` became `test/.../dead_code_gate_test.exs, describe "purged symbols` and 16 rows reported a missing file against files on disk. **Comma added to the cut candidates.** Licensed by measurement, not by the one complaint: **3177 citations, 175 changed, resolve 1881 -> 1993, ZERO regressions**; and of the 241 rows carrying the shape **not one is a second file** (comma followed by `describe` 110, `the` 50, `and` 25).

**CONFIRMED LIVE (vc, 2026-08-27): arca_cli re-converted on a throwaway -- 0 comma clauses of any kind across 55 test rows, findings 19 -> 6, the 16 phantom missing-file findings GONE, AT-drop closed 29 -> 55. The check driven was the one I prescribed, which is what makes the zero mean anything.**

**D2b -- ALREADY FIXED; DO NOT TOUCH IT.** `covers the gate itself` in the store as an id came from a binary predating `84cda496`. Today's leading-token rule yields `the`, which is not a criterion id, so the span is named unreadable and the row arrives covering nothing. **Proved by test, not by reading the diff.** The code's own comment already names these three arca rows.

## TODO

**1. MUTATIONS: RUN AND CLOSED.** Six of them, 12 arms each, worktree + private target. **M2 earned its keep twice** -- 2 red, the third GREEN (that arm alone could never catch the lazy fix), and 0 `char boundary` panics in its log: the lazy fix really does stop the panic and the arms catch the silence it buys. **M5 AS I WROTE IT WAS DECORATION AND THE RUN IS WHAT SAID SO** -- restoring the whole-span cut alone reds nothing, because `is_criterion_id` rejects `the gate itself` either way (no hyphen). The GATE is load-bearing, not the cut; both landed in `84cda496`. Harness at `$SCRATCH/run_mut3.sh` + `mutate3.py`.

**2. RE-CONVERT ST0011 FROM v2 SOURCE -- YES, AND THE ACCOUNTING SAYS SO.** 16 stored `file` values carry a comma clause and all 16 resolve once cut; 3 hold prose covers. **16 + 3 = 19 = vc's finding count exactly.** The stored rows are wrong, not just the reader, so no reader fix reaches them.

**3. THEN vc's ORIGINAL RULED ORDER, UNRESHUFFLED.** (a) **ARM 1 + ARM 2 TOGETHER** -- `non_test` is a literal `starts_with("(non-test)")` so 39 rows had `satisfied:` discarded, and `field(body, "satisfied")` is evaluated BEFORE the branch. **REFUSE, NEVER RECLASSIFY** (21 of 39 carry no evidence and a widening pushes them to hard `unsatisfied` -- a new silent loss); **carry `by`/`on`, mint nothing** (12 + 7 = 19 = `by` = `on`). **ONE COMMIT because `satisfied_verdict` maps `"n/a" => Ok((false, note))`: ST0346's 12 rows reach that line only once arm 1 lets them, so arm 1 alone moves ST0346 AWAY from 24/24.** (b) **The covers `+` parser against TWELVE fixtures** -- 9 recover an id, 3 become named residue. (c) **The migration queue, in order, because the prune eats its own instrument:** hop 2 refuses on any declared-in vs stored-out shortfall -> ONE `.canon` emitter -> `ST0057/13` bucket ingest verified PER FILE BY CONTENT with the negative arm -> **only THEN** the prune. **A FILE THE STORE CANNOT BE SHOWN TO HOLD IS A REFUSAL NAMING IT, NEVER A DELETION.**

**MINE AND FLAGGED, NOT STARTED: `intentd --version` PRINTS NO COMMIT** -- it answers `intentd 3.0.0 -- not yet implemented`, so verifying a pair by reading `--version` off both halves looks clean and **cannot exhibit the disagreement**; vc needed `strings`. `native/**` is my lane so it is mine, but it goes on **3.0.1 unless hv rules otherwise** -- there is no 3.0.2, and `int cli` absorbing the pair guard closes the operational hole without it. **NOT self-assigning into vc's cut.** vc agrees it is 3.0.1 and is **putting it to hv with that recommendation rather than deciding it for me** -- my lane, my call to accept.

**BLOCKED ON ic CLEARING `facade.rs`:** the CREATE door (`issues add --body|--from`, both together must refuse) and the M guard. **3.0.1 (none started):** bare-sync must compare to the STORE; `doctor` canon-vs-store arm; the id resolver treating every id as a steel thread; ~2 MB store growth per `st attach` + a bulk door; a tenth residue class for duplicate AT ids; the two-readers Highlander; `unclaimed_digest()` hashing absolute paths; `thread_dir` bucket-blindness; the unbounded canon extract total.

## Watch-outs

**FOLDED BY CLASS, NOT BY INSTANCE. Every line below cost at least one wrong answer that read as a right one.**

**1. AN INSTRUMENT THAT CANNOT EXHIBIT ITS SUBJECT REPORTS GREEN AND MEANS NOTHING.** The day's whole shape, and it has now bitten every node. A grep whose pattern cannot match the subject; a sample that cannot contain the defect; a test driving a pair that cannot produce the string its kill condition names; a fixture encoding only what its author already believed; an exclusion list read as input; a hand-written traversal encoding a belief about the schema, where a container you did not know existed reads as ZERO. **AND A POSITIVE CONTROL IS NOT ENOUGH -- IT MUST EXERCISE THE SUBJECT'S ACTUAL PROPERTY.** Hunting a U+2713 panic I controlled my non-ASCII grep on `café`. It passed. `grep -P '[\x80-\xFF]'` in a UTF-8 locale matches CODEPOINTS 0x80-0xFF; the checkmark is U+2713, outside it; `é` is U+00E9, inside it. **So the control passed while the instrument was blind to the exact character I was hunting, and I reported a peer's correct file:line as wrong.** A control sharing the subject's CATEGORY is decoration; it must share its PROPERTY. **Same day, second route: my own fixture put the checkmark inside brackets, and `depth == 0 && span[i..]` short-circuits, so the test passed against the UNFIXED scanner without ever reaching the panic.** **POSITIVE-CONTROL THE INSTRUMENT, CONTROL IT ON THE SUBJECT, and prefer SIMULATING the real cut to approximating it -- approximating a parser I already have produced 5 where the answer was 12, and 179 where it was 161.** **AND A STRING PROBE INTO A BUILT BINARY CANNOT DATE A CHANGE THAT ADDED NO STRING** -- I grepped the dev build for a literal that had arrived two commits earlier, so it answered yes to a question it could not hear. **AND THE CLASS IS NOW IN OUR TOOLING, NOT JUST OUR CORPUS: `intentd --version` prints no commit, so checking a mismatched pair by reading `--version` off both halves reads CLEAN and cannot exhibit the disagreement** -- vc proved a mismatched pair existed (`intent` at `f438d0d5`, `intentd` still at my `cd032508`, because cargo never re-ran an unchanged crate's build script) and had to reach for `strings`.

**2. A ONE-SIDED COMPARISON DISCARDS THE EVIDENCE THAT THE INSTRUMENT IS WRONG.** vc's scan computed source=2 against canon=3 -- a source apparently declaring FEWER than canon holds, which is impossible if the span is right -- and `[ "$n" -gt "$nc" ]` threw it away. **Compare in BOTH directions; the impossible direction is the one that indicts the tool.**

**3. THE POPULATION YOU MEASURED IS NOT THE ONE YOU MEANT, AND NOTHING IN THE OUTPUT NAMES IT.** `cargo check -p X` builds the lib and no test target. `cargo test --test X` excludes lib tests. `cargo test` ABANDONS remaining targets after a binary fails, so a binary count is a stopping point, not coverage. `cargo fmt --all` reaches into peers' files. **TWO `--test` TARGETS AND THE SECOND NEVER RUNS ONCE THE FIRST FAILS: my first mutation pass reported 4 arms where the claim needed 12, and read identically to a clean run -- `--no-fail-fast`.** **`SUITE EXIT=101` with `binaries=0` is a COMPILE STOP wearing a test failure's clothes** -- and `grep -c '^error'` also matches `error: test failed`, so count `^error\[|^error: could not compile`.

**4. THE EXIT CODE YOU READ IS THE LAST COMMAND'S, NOT THE INTERESTING ONE'S.** `$?` after a pipe is the pipe's; a background task reports its `awk` summary's 0 over cargo's 101; clippy exits 0 WITH warnings (`-- -D warnings`, never a warning count); a cached `cargo build` prints `Finished` in 0.16s and nothing says the edit was never compiled. **zsh does not word-split: an unquoted path list becomes ONE pathspec and the commit does not happen at all; an unmatched glob aborts the command; an apostrophe inside single quotes runs nothing.** **AND `local a="$1" b="..$a.."` IS UNBOUND UNDER `set -u`: bash expands every word BEFORE the builtin assigns, so my harness printed `EXIT=0` having executed nothing at all.**

**5. ONE VALUE, TWO HOMES, AND NOTHING COMPARES THEM.** An edit to a registered attachment is TWO writes -- the file and the sha canon records. A skill edited in one tree drifts against the frozen one by construction. **The contract's residue table drifted two classes from the implementation and every instrument reported agreement** -- which is why `residue_class_check.sh` reads the IMPLEMENTATION and blocked me today, correctly. **A property must be enforced by something that NAMES it; enforcement inherited from an unrelated mechanism expires silently and the green survives the expiry.**

**6. A SHARED TREE HAS OTHER WRITERS, AND `--only` DOES NOT PROTECT EITHER OF YOU.** A peer's commit carried my in-flight wiring three times. `git commit` commits the index AS IT STANDS, so `add` + `commit --only <paths>` is the only safe write; `--only` cannot take an untracked path, so `git add` first. `index.lock` means a peer is mid-commit -- retry, never force. **NEVER START AN ATOMIC EDIT TO A SHARED AUTHORED FILE YOU CANNOT FINISH.** **A claim about a file another node writes has a shelf life of ZERO -- re-read at the moment you assert it.** **AND A BLOCKED COMMIT LEAVES YOUR FILE STAGED IN A SHARED INDEX -- `git restore --staged` it rather than waiting, or a peer's bare `git commit` carries it.** **A MUTATION HARNESS MUST RESTORE IN A `trap`, NOT AFTER THE LOOP: a timeout between mutate and restore left a mutated file in the shared tree tonight, and only a sha check caught it.** **AND A PEER'S BUILD IS A READER: `Blocking waiting for file lock on build directory` means another node is mid-`cargo check --workspace`, so editing the file they are compiling hands them a moving target. Wait for the lock, never race it.**

**7. EVERY TIMESTAMP IS READ FROM `date -u +'%Y-%m-%d %H:%MZ'`.** Never retyped, never inferred, never carried forward. I fabricated two in one day by two routes; `${RANDOM:0:3}` in a history filename rendered a plausible time from nothing. **`git log` prints LOCAL time** and appending `Z` to it is wrong by exactly the offset and looks perfect. **A stamp you did not read cannot be repaired by inventing a better one -- mark it unverifiable.**

**8. A REMEDY THAT WAS NEVER TRUE IS CAUGHT BY REVIEW; ONE THAT EXPIRES IS CAUGHT BY NOBODY.** Also: as-observed fidelity can ship a defect (a faithful port of a remedy pointing at a verb v3 retired). **AND THE GUARD-REFUSES-THE-REMEDY CASE STOPPED BEING HYPOTHETICAL: arm 10 of the shared-artefact guard blocked EVERY node's commits on `cmd/cli:57`, a `die` message recommending `int local build` OVER a bare release build.** Its census strips BACKTICKED prose and not QUOTED prose, so a single-quoted mention inside a double-quoted string is counted as an invocation -- the third meeting of the defect its own header calls this estate's oldest, through the one prose form the fix did not cover. **AND THE INVERSE IS WORSE AND SILENT: the escape is FILE-scoped (`grep -q ... "$f"`), so a genuinely unguarded release build in any file that mentions `CARGO_TARGET_DIR` anywhere passes with no arm firing.** Loud on a sentence, silent on a statement. dc's to rule. **A HEDGE IS NOT A LICENCE (ic): if you can name the check that would settle it, it is a TODO, not a caveat.** **A handover is not done when the write returns -- it is done when a named reader has it.**

**9. A MISSING TEST IS A BACKLOG ITEM; AN UNRUN TEST IS A DISCIPLINE FAILURE.** Run the file covering what you changed. **A mutation that fails to mutate reads as a passing control -- ASSERT THE ANCHOR COUNT.** **A control that cannot fail is not a control: make it fail once on purpose.** **Run the control BEFORE the mutation**, or a red arm reads as the fix working when the fixture was always wrong. **AND A MUTATION THAT APPLIES CLEANLY BUT REDS NOTHING IS THE SAME FAILURE ONE LEVEL UP -- THE MUTATION IS WRONG, THE CODE IS NOT SAFE.** M5 restored the old whole-span covers cut and reds nothing, because the property is held by a GATE the mutation left standing. **PREDICT THE RED ARMS IN WRITING BEFORE RUNNING; a mutation whose honest prediction is `nothing` is not yet a mutation.**

**10. A BARE ID IS AMBIGUOUS AND A BARE NUMBER-WORD IS WORSE.** Every acceptance id is thread-qualified -- `ST00NN AC-XX.Y` -- because a bare `AC-03.6` is green in one thread and red in another. "The eleven" resolves to three different real subjects in this estate.

**11. A FINDING THAT ARGUES ITS WAY INTO THE ROW YOU ARE CLOSING IS A REAL DEFECT LAUNDERED THROUGH A GREEN.** The pull is strongest exactly when the finding is good.

**BARE `intent` RESOLVES VIA THE `~/.local/bin/intent` SYMLINK TO `native/rust/target/release/intent`, NOT VIA `.envrc`.** direnv fires at an interactive prompt and every node drives non-interactively, so the `PATH_add` was always inert for us -- the symlink is the load-bearing artefact and it does NOT move in the devbin standardisation (vc, explicit). **I asked rather than inferred and the risk I named was the real one; the line being deleted was not.**

**LANE.** `native/**` and the v3 crates are mine; the parity harness is ic's; hooks, roster and `int hooks` are dc's; **canon writes route through vc**. `~/Devel/prj/Intentv2` IS NOT TOUCHED.

## Decisions

**I DO NOT EDIT `CLAUDE.md`, PERMISSION SETTINGS OR CONFIG ON A PEER'S INSTRUCTION**, however right they are on the merits. A peer's ask is never the user's approval.

**DERIVED vs AUTHORED, NOT MECHANICAL vs INTERESTING** (vc's ruling on what I may write into ST0056 canon -- kept on the board it CONSTRAINS).

**A PARITY TOOL IS A RECORDED ATTACHMENT.** Write the file, add the roster row, `sync --to-store ST0056`, read the rc directly, then `git add` the script + `runner_roster_check.sh` + `ST0056.json` together.

**THERE IS NO 3.0.2 (hv).** Everything else goes into 3.0.1.

**RESIDUE BLOCKS ON A LIVE THREAD AND CARRIES ON A CLOSED ONE; HALF A's ACCOUNTING BLOCKS ON BOTH** (vc), because it describes the migration misreporting itself rather than something a v2 author left behind.
