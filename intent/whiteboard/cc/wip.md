---
node: cc
name: Control Claude
role: control
session_id: ae8c8153-6f3f-438f-b96b-04bd381ad4ed
heartbeat_at: 2026-09-02 17:28Z
status: active
focus: "0206's MUTATION HALF IS IN (5ef0667c) and the SCHEMA REFUSALS NAME THEIR ARTEFACT (203d9a9c). THE DAY'S LESSON IS ONE LINE: ASSERT THE PREMISE YOUR FIX RESTS ON AS A TEST, IN THE DIRECTION THAT WOULD EMBARRASS YOU -- a non-vacuity control refuted my own store half. NEXT, vc under hv's pen: AC-17.1 -> WP-14 + AC-09.5 -> config's ordering expiry. RE-DRIVE EVERY FIGURE HERE; NONE OF THEM IS QUOTABLE."
claims: [ST0056/06, ST0056/08, ST0056/10, ST0057/00]
---

# Control Claude (cc)

## DOING

**NEXT, in vc's order (ruled 2026-09-02 17:03Z under hv's pen; hv reverses any with one word):** `AC-17.1`'s reads-only bound -> `WP-14` carrying `AC-09.5`'s wip/boards half -> `config`'s ordering expiry with the violation RECORDED in the row. **`WP-08` STAYS HELD** -- closing it marks the daemon DONE over zero conformance coverage, and vc endorsed the hold.

**`AC-17.1` IS `0206` ONE FLIGHT UP AND THAT IS WHY IT IS MINE.** The row wants an edit through EACH realiser reaching an identical store state; `wire.rs:85-141` is four reads plus `Shutdown`, `Graphql` carries `EmptyMutation`, **so the web realiser has no door on either transport.** `design.md:501` says the row gets STRONGER under D56 and cites Conflab's `daemon_bridge.js` writing against the daemon, while `wire.rs` scopes it to _READS ONLY in 3.0.x_. **Those two were decided independently and disagree; the criterion stays and the bound moves.**

**STANDING FROM TODAY: every `intent at green` carries `--note`.** vc's practice, binding every node, no build needed -- the verb already lands the assertion and its justification atomically, so there is no window where a row is green and unexplained. dc's `0207` turns the append discipline into a refusal shortly; keep appending by hand until it lands.

## TODO

- **WRITING TESTS CANNOT CLOSE `AC-06.1`'s COVERAGE CLAUSE, AND THE HOLE IS A HIGHLANDER ONE.** `daemon` and `config` now have suites, each DRIVEN THREE WAYS (v3 all pass; `/usr/bin/false` all fail, so every arm reaches the CLI; the DEFAULT binding all-but-one fail) -- because the estate's default `INTENT_BIN` is `bin/intent`, **the v2 SHELL SCRIPT**. A non-zero DEFAULT failure count is UNSTABLE under burn.sh and carries no classification, so `coverage_map` cannot count either file however correct it is. **IT IS THREE BINARIES, NOT TWO** (vc): `test_helper.bash:21` -> v2 shell; `run_v2_suite.bash:55` -> v3 DEBUG; `~/.local/bin/intent` -> v3 RELEASE. An undeclared population of build targets. **Flip THEN re-baseline or the wall time is paid twice. NOT MINE TO TAKE.**
- **`coverage_map.sh` REFUSES TO PUBLISH, CORRECTLY.** The burn TSV no longer covers the estate (16 never measured, 1 stale row, 99-row baseline dated 15 Aug against 114 real `.bats` -- vc's corrected count, not 113). Remedy is re-running `burn.sh`, a DOUBLE full-estate sweep that hung 3.5h once. **I DO NOT LAUNCH IT** -- full-suite runs are the human's. **Both holes survive the refusal independently:** `grep -rn 'intent config' tests/` and the same for `daemon` are ZERO.
- **WP-08 IS 12/12 GREEN AND `wp done` IS AVAILABLE TO ME (XS). NOT TAKEN.** Closing it marks the daemon DONE while its family carries zero conformance coverage -- a green over an uncovered surface. hv's sequencing call.
- **`AC-06.1` IS RED ON TWO NAMED HOLES.** The 27 UNSTABLE are established (four causes, 25 = `125f601d`); read at `parity/ac-06_1-unstable-causes.md`. The COVERAGE half is above. **`config` is an ORDERING CONSTRAINT WITH AN EXPIRY** -- the rider binds `config` ALONE, so writing `daemon`'s tests late was legal where writing `config`'s late FAILS the clause. That is why `config`'s suite pins DECLARED-and-UNBUILT at rc=2 rather than waiting.
- **`AC-10.5` CHAINS BEHIND WP-14, UNOWNED AND UNSTARTED (L).** Red on substance: the MIGRATION does not name its residue. `0183`'s original remedy was wrong and is corrected -- 110 of baize's 135 are `intent/whiteboard/`, leaving the not-modelled set at **D30** (hv, WP-14 carries it): IN the model and NOT YET BUILT. Naming them would zero the loss by asserting the project meant to abandon what hv ruled it would carry.
- **`AC-06.8` IS NOT MINE AND MOVES ON `0181`.** `agents init --template` reads `retire` (vc's `0180` is EXECUTED); `llm usage_rules --symlink` is still `keep` and unread -- hv's. **Do not sequence anything behind me on it.**
- **THE `hoist` ROW'S RE-PIN RULE IS STALE AND UNEXECUTED.** It names `canary`, `lamplight`, `utilz`, `baize` as repositories whose migrations have NOT run; **all four declare 3.0.0 on disk**, canary included because canary IS this repo. Re-pinning would point the row at a tree with no v2 source -- the exact harm the rule prevents. **DO NOT RE-PIN.**
- **THE MIGRATOR DOES NOT COMMIT, SO THE DOCUMENTED ROLLBACK HAS NO SUBJECT.** `migration.md` Phase B step 7 says _one commit_ and `AC-00.8` says _one visible commit_; measured, `intent upgrade` returns rc=0 with HEAD unchanged and 23 paths dirty. **_One visible commit_ is an OPERATOR CONVENTION, not a migrator guarantee.** hv's call.
- **`0205` IS THREE BLOCKS FOR ic AND A FOURTH THAT IS NOT OURS.** `lib_classify.sh` x2 + `drift_check.sh`; `bin/.devbin/lib/builtins:66` is VENDORED under an integrity manifest, so it goes upstream or is accepted. **The remedy is the QUOTED HEREDOC, never single-quoting** -- single-quoting moves the hazard to the apostrophe, likelier in prose than a double quote.
- **WP-10 IS THE CHOKE POINT FOR vc's LARGEST BLOCK.** `AC-00.2`, `AC-00.8`, `AC-00.10` sit behind WP-10, dependent on WP-06 (mine) AND WP-07 (dc's). Read off the cover.
- **UNDISPOSITIONED, ALL hv's VIA vc:** `0086` and `0063` would have to be WRITTEN, not recovered. `0095`/`0096` are one-word stubs. `0192` -- **if hv rules it in, the refusal goes in `info_read_back`, NOT `authored_regions`.** **And the daemon-lock race has NO ISSUE** -- `issues add` regenerates canon as a side effect, so it was not filed mid-hold. It wants a record.
- **THE MCP DISPOSITION IS DECIDED, NOT DEFERRED.** SEVERITY EXPOSED (a classification over an enumerated domain); TITLE WITHHELD (authored text). My split was right; parking it _until after the tag_ was the phantom hv struck.

## Watch-outs

**ASSERT THE PREMISE YOUR FIX RESTS ON AS A TEST, IN THE DIRECTION THAT WOULD EMBARRASS YOU.** I built `0206`'s CAS on `threads.revision`, having READ `rebuild` and READ `Door::Change` and reasoned correctly from both -- and I never read the `DELETE` two lines above, so I had the mechanism exactly backwards. **The control I wrote to stop an arm passing vacuously refuted the design it was protecting**: a sync does not BUMP the counter, `rebuild` deletes before it inserts so the conflict clause never fires and the counter RESETS. A CAS on it would have FAILED OPEN on the case it was built for -- facade at 0, peer write to 1, sync back to 0, stale facade reads `0 == 0` and overwrites. **No amount of care at the reading step fixes this; only an assertion does.** Distinct from the plan-versus-mechanism class: there the plan hid the mechanism, here I read the mechanism and stopped one line early.

**`cargo check --workspace --all-targets` -- AND THE FLAG IS THE HALF MEMORY DROPS.** A Rust enum makes an exhaustive `match` a DISTRIBUTED obligation and the compiler reports it from DOWNSTREAM, so **the node who breaks it is structurally the least likely to see it**: `-p <mycrate>` was green while ic's build was on the floor. `--workspace` is not enough either -- the exhaustive matches live in TEST targets, which a plain check does not build. **Four instances in this estate across three nodes** (ic at `db3f947a`, me, dc twice), and `error_remedies.rs` has named the exact command in its own comment the whole time. **A doc that names its own instrument does not make anyone run it** (dc's). Land the variant, its const, its remedy arm and every exhaustive match in ONE edit; run the full form before stepping away.

**A GREEN THAT SOMEONE ELSE REPORTS RED IS A WINDOW, NOT A DISAGREEMENT.** ic measured two `schema_command` failures that were real when taken and gone by the time I read the message -- they fell between my DDL edit and the deliberate re-bless. **The generated face means a `store.rs` defect surfaces in intent-cli's suite**, so the package boundary is not the blame boundary in either direction. Before disputing a peer's measurement, re-drive it and compare TIMES, not verdicts.

**A REFUSAL THAT NAMES NO ARTEFACT SENDS THE OPERATOR TO GUESS.** `SchemaMismatch` said _the runtime store_ to a human with two checkouts open. General form: name the FILE, name both VALUES, and point at `--version` for the build rather than inlining a commit that already has exactly one home. Fixed 2026-09-02 for both schema refusals.

**THE INSTRUMENT ANSWERED A DIFFERENT QUESTION -- THE MOST EXPENSIVE CLASS I HAVE, AND IT ARRIVES WHILE I AM BEING CAREFUL.** A SELECTOR that resembles the population is not the population. Specimens, each of which returned a plausible answer: `grep -i FAILED` matches `0 failed` in every PASSING summary; a capture grepping `Display` strings dropped the `Debug` line carrying the evidence; `pkill -f 'cargo test --workspace --no-fail-fast'` matches a pattern every session runs; an `awk '/pub const X/,/^\];/'` range never opened because the decl is bare `const`; a `^  [0-9]+,$` pattern missed rungs indented 4 instead of 2 and reported a tidy wrong total; a grep aimed at the wrong directory came back empty and read as _the thing is gone_. Also standing: `--help` RENDERS for a DECLARED command whether or not an arm exists, and `bash -n` answers _is this valid bash_, never _is this the file I meant_. **RULES: an EMPTY result is not a finding until the pattern is positive-controlled. Assert a MEMBER you know is in the population, never just a plausible total. After any programmatic edit, grep for what should now be there.** **THE COMMON SHAPE: the instrument is checked against what I MEANT rather than a sample of what it will SEE, and a planted three-line fixture catches every one of them in seconds.**

**A VERIFICATION SCOPED TO THE THING CHANGED IS NOT A VERIFICATION** (dc's, and the one I would not have recognised). **The blast radius of a published value is every crate that PINS it, not the crate that DEFINES it** -- `grep -rn <constant> --include='*.rs'` finds that corpus in a second. Unlike a bad grep, this one LOOKS like diligence.

**THE CORPUS IS THE CLAIM; THE VERDICT IS NOT.** Everything three instruments reported one morning was TRUE and every corpus was narrower than its criterion. Beside the verdict, in the OUTPUT and never a comment: **POPULATION** (the set examined, derivation named; `RECORDED` sits AT the number where it cannot be derived), **FORMS** (per-form counts, never one total), **REACH** (`COVERS` / `DOES NOT` / `UNOWNED`).

**A CONTROL THAT CANNOT DISTINGUISH _SAFE_ FROM _NEVER TRIED_ IS NOT A CONTROL -- AND IT MUST VARY THE AXIS THE CHECK ACTUALLY READS.** Drive an instrument to BOTH or its green means nothing -- a no-mutation probe whose two shasums are both EMPTY has no subject. **AND THE SHARPER HALF, WHICH COST ME A FALSE EXONERATION:** I attributed 23 `intent-cli` failures to _not mine_ because stashing my two paths reproduced the same 23. **`git stash` REWRITES the file, so the MTIME stayed fresh -- and the guard keys on MTIME, not content** (`common/mod.rs:782` refuses a sibling `intentd` older than the tree; `cargo test -p intent-cli` does not build another package's binaries). My arms differed only in the dimension the guard IGNORES, so it was guaranteed to fire in both and the matching counts read as innocence. `cargo build -p intentd` then re-run: **461/0**. **THE TELL WAS IN MY OWN OUTPUT AND I EXPLAINED IT AWAY** -- the baseline had one fewer pass, and I never asked why 23 failures survived removing the only change that could cause them. **Ask what the check READS before choosing what to vary.** **State the bound's DIRECTION** too: the `0206` harness's trials are an UPPER bound, so its loss rate cannot flatter the finding.

**A MEASUREMENT TAKEN ON THE SLOW PATH IS NOT EVIDENCE ABOUT THE FAST ONE.** `threads.updated_at` moves on every change-door write; my millisecond-resolution hypothesis DID NOT REPRODUCE (six writes, six stamps, 19-37ms apart) and is not evidence against it. What survives is a design argument: adequacy rests on PROCESS SCHEDULING, and my gap was dominated by process startup, so it says nothing about the daemon path where both writes share one process.

**A REPRODUCTION PROFILE IS A DIAGNOSIS.** _Needs the full workspace, invisible with the subsystem alone_ says the cause is not in the code under test but in what SHARES ITS PROCESS. That is what put me on `fork`: an `flock` releases when the last descriptor on its open file description closes, `fork` copies every descriptor, and `O_CLOEXEC` clears the copy only at `exec`.

**THE SHELL LIES ABOUT SUCCESS ON THIS BOX.** `cmd | head` reads head's status -- **done twice today, on `config`'s rc=2 and the keg's rc=1, both read as 0.** zsh has no `PIPESTATUS`, does NOT word-split `$var`, and an unmatched glob ABORTS the command. `grep` is `ugrep`. `sed -i ''` SUCCEEDS on no match. `read -t N </dev/null` returns INSTANTLY. `find` is bfs, and `find .` walks `target/` until it TIMES OUT. **cwd persists between calls.** `2>/dev/null` on a source printing `command not found` returns an empty lookup that reads as data.

**THE SHARED CHECKOUT: CANON CANNOT BE SPLIT, SO EVERY CANON COMMIT IS SILENTLY MULTI-NODE.** With intentd auto-ingesting, `ST0056.json` names the STORE's union of every dirty-and-ingested attachment, **so any node holding a dirty file under `parity/tools/` blocks every other node's canon commit on that thread** -- and committing the attachment without the extract fails the other way. Four-way deadlock in twenty minutes with everyone following the rule. **`add` + `commit --only` is the only safe write; a live `index.lock` is a WAIT, never a removal; never `--no-verify`; never unstage a peer's path.** **PROVENANCE IS CARRIED BY PATH** -- every node commits as the same person.

**TWO CORRECT RULES CAN TAKE THE MACHINE DOWN.** A release build DELETES the shared pair before building (`0196`) and the shared-artefact guard REDIRECTS a dirty-tree build to `target/private/`: the delete happened, the replace declined, and `intent` was gone estate-wide for fifty minutes. **The guard prints the dangling symlinks BEFORE the delete, so the information exists and the ORDERING defeats it.** Recovery needed BOTH nodes to stash -- either one's single uncommitted file kept the build redirected, so **neither could restore service alone** -- and the commit gate blocks on the dangling symlink, so committing was blocked BY the outage rather than being its cure.

**A LADDER DECLARED AS DATA IS READ FROM THE DATA, NEVER RECONSTRUCTED FROM HISTORY.** `git log -S` counts occurrences of a substring, and a value change alters no count -- so it cannot count rungs. `MIGRATIONS: &[(i32, &str)]` ENUMERATES them: driven 14:00Z, rungs 2..17 contiguous, max == `SCHEMA_VERSION`, so the keg's 13 to this store's 17 crosses four. **General form: before going to history for a number, ask whether the thing declares itself.**

**A PIN THAT TRAILS HEAD IS THE CORRECT STEADY STATE, NOT STALENESS** (dc's). The binaries pin the last commit touching what they COMPILE, so on a board-heavy day HEAD runs ahead and a naive `HEAD vs pin` diff reads them as stale while nothing they compile has moved. One command settles it: `git rev-list -1 HEAD -- native/rust surface docs/design`, equal to the pin means CURRENT. **THE GATE'S CURRENCY ARM IS THE DECIDER AND THE BARE pin/HEAD DIFFERENCE IS NOT** (dc): it says which non-test files moved since the pin, in its own words -- currently REFUSING on one, mine. **THE SCOPE HAS TWO HOMES BY DESIGN AND THEY ARE NOT A HIGHLANDER BREACH.** `DIRT_SCOPE` at `native/rust/build-support/source_commit.rs:174` decides the `dirty-` MARKER; `SHARED_TARGET_DIRT_SCOPES` at `bin/.devbin/cmd/shared/sharedtarget.lib:124` decides the GUARD's refusal. **The guard must stay a SUPERSET of the marker -- stricter is safe, looser lets the shared slot hold a build the marker called clean -- and arm 6 asserts CONTAINMENT, which the source says explicitly must NOT be tightened to equality.** **Re-read both on the bounce and check the CONTAINMENT, not just the values -- AND THE TRAP IS THAT TODAY THEY ARE EQUAL**, so an equality check passes right now and only fails the next time someone widens one side LEGITIMATELY. **THE CONTAINMENT FORM IS WHAT PERMITTED THE WIDENING THAT ADDED `docs/design`** (`source_commit.rs:157-161`, dc drove it): `intentd` embeds the logo from there under `AC-08.9`, so the scope grew by one path rather than by a second copy of the asset. A green equality check is therefore evidence of nothing, and it would have REFUSED the widening the project actually wanted. Both homes live at `sharedtarget.lib:124` and `native/rust/build-support/source_commit.rs:174` -- **build-support, NOT under `crates/`**.

**A MESSAGE CORRECTING SOMEONE'S STATE IS ITSELF A STATE CLAIM.** dc refused to relay vc's reading and re-drove it, then in the same message told me my board carried a claim it never had and that a landed commit stood refused. **The re-drive reached the SUBJECT and not the claims about the RECIPIENT** -- both were one command away. I have the same hole: read this before writing my next correction to a peer.

**A SILENT CORRECT DECLINE IS INDISTINGUISHABLE FROM AN ASSUMPTION** (dc's, and the failure is social rather than technical). Three careful nodes each DECLINING to drive a claim is correct; three each ASSUMING a fourth did is the accident, **and nothing observable separates them except whether someone recorded the decline.** When you decline, name yourself as a non-driver on the face of the claim.

**AN OPEN ESCALATION AGES INTO AN ASSERTION** (vc). True when written, never re-read because it is _already asked_, accruing authority it never earned. **Before anyone budgets against an escalation, its AUTHOR re-drives it.** Same family: **CONSTRUCTING BEATS READING, MEASURING BEATS BOTH, AND THE DISPROOF IS USUALLY ALREADY IN HAND** -- `0082` held `0199`'s mechanism, `sync.rs:148` refuted `0183` at the site.

**FACTS THAT LOOK LIKE OPINIONS.** `intent st attach` writes the store AND the canon extract and NEVER the disk file (`0082`); the daemon watches DISK, so a disk edit converges everything and a store-side write renders nothing back. A thread must be REALISED or it has no cover. `intent ac show` is not a verb. `intent init --name` is not a flag. **TWO `--version` OUTPUTS THAT LOOK IDENTICAL CAN BE TWO DIFFERENT QUANTITIES** (vc, and it cost this estate a morning). `intent --version` and `bin/devbin --version` both render `intent 3.0.0 (<sha>)`, but one names **the commit the binary was BUILT FROM** and the other **what the CHECKOUT IS AT NOW**. They coincide only in a tree nobody has committed to since the last build, so **the diff the rendering invites reads a CURRENT binary as a stale one.** Do not diff them; ask the currency question instead. **`--version` names WHICH BUILD and the version names only WHICH LINE** -- but a matching marker proves a pair agree only when neither is `dirty-`; under dirt, pin by the hash.

**AN INSTRUCTION IS WRITTEN AGAINST A SURFACE ITS AUTHOR MAY NOT HAVE RE-READ -- SO CHECK THE SURFACE BEFORE EXECUTING, NOT AFTER.** Twice on 2026-09-02 14:00Z vc's rulings were correct in intent and unexecutable as written, and both were caught by reading the verb rather than the plan: the `--note` recipe that would have destroyed 4,028 characters of provenance (ic), and D3's row re-keying (mine). **D3 IS THE SHARPER SPECIMEN BECAUSE THE RULING WAS SOUND AND THE CARRIER DID NOT EXIST.** hv ruled AT rows cite the TEST NAME; `at edit` offers `--file`, `--prose`, `--covers` and **no name field**, and `legacy.rs:1965` refuses the value outright -- `cited.contains('/') && !cited.contains(':')`, so a bare name has no slash and a qualified one has colons. **Both tiers refused as VALUES, not flagged as lint.** The model says it was deliberate: the `legacy` doc records `::name` as precisely what the 0017 grammar was built not to express. **AND THE HALF THAT WOULD HAVE BEEN INVISIBLE: the scoping said to parse-and-rewrite `intent/.canon/st/*.json`, which is a GENERATED PROJECTION.** The store is SSOT (D01); a hand-edit there is overwritten at the next sync and reports nothing while it lasts -- 187 rows, a clean diff, `at lint` refusing every one, and the store never touched. **A DIFF THAT LOOKS RIGHT IS NOT EVIDENCE THE WRITE REACHED THE SSOT.** **OUTCOME 2026-09-02 14:10Z: D3 WAS WITHDRAWN ENTIRELY (hv, option B) AND THE FIELD WAS NEVER BUILT.** Sizing found the tree already holding a reasoned position against the ruled direction -- `carries_id`'s doc: _the contract says to name a test by putting the AT's id INSIDE it, ... where a cited test NAME survives neither_. **THE TENSION RESOLVED BECAUSE THE TWO FAILURE MODES ARE DIFFERENT AND ONE MECHANISM COVERS BOTH:** a ruling protecting against a FILE MERGE breaks on a path; the trait's argument is about a RENAME, which breaks a name; **the id inside the test survives both**, so the existing mechanism DOMINATES the proposed one rather than trading off against it. vc reversed their own recommendation on it. **A DOMINANCE ARGUMENT ENDS A DESIGN DEBATE THAT COMPETING ASSERTIONS CANNOT** -- reach for it first. General form: **when an instruction names a mechanism, drive the mechanism's own `--help` and model before sizing anything against it** -- and prefer the ADDITIVE carrier (`Option` + `skip_serializing_if`, the `fiat` field's precedent) over relaxing a grammar, because additive is free under DO-NOT-BUMP and a grammar change alters what every existing reader accepts.

## Decisions

- (2026-09-01) **hv: v3.0.1 IS FEATURE COMPLETE, THERE IS NO TAG WINDOW AND NO EXTERNAL CONSUMER, AND COST IS NOT A CONSTRAINT.** The SCARCITY REGISTER is retired AS A CLASS -- _ship it red and name it_, _not in this cut_, _after the tag_, _new machinery in a tag window_ were all unstated claims that time was short. **A recommendation resting on _there is not time_ must be re-derived on its merits or withdrawn.** The release-number question is CLOSED and is not to be put again.
- (2026-09-01) **A REFUSAL THAT CANNOT SAY WHAT IT FOUND MAKES ITS OWN DEFECT UNDIAGNOSABLE.** Both `AlreadyRunning` branches rendered identically, so the first move on the race was making them distinguishable rather than editing the code under suspicion. dc reasoned to the probe branch and I reasoned to a branch-independent remedy; **the evidence field is the only reason either was caught.**
- (2026-09-01) **A REMEDY INHERITS ITS BRANCH'S ASYMMETRY.** Confirm-before-refuse is SAFE on the lock (a real daemon holds for life, so a re-check only rescues a false refusal) and WRONG on the probe (answering means refuse, silence means UNLINK, so confirming makes the DESTRUCTIVE outcome likelier -- the false negative `AC-08.12` forbids). **I concluded it was branch-independent and testing the claim is what stopped it shipping.**
- (2026-09-01) **EMBEDDED AND UNPRINTED IS THE WORST OF THE THREE STATES.** `intentd` carried its source commit all along and `--version` would not say it, so the tooling could answer _which build_ and the operator could not. Absent would have been honest.
- (2026-08-31) **A CRITERION IS NOT REWORDED TO WHAT THE CURRENT STATE SATISFIES** (vc). The ruled form must be HARDER than the original and bind the node holding the divergence.
- (2026-08-31) **A CLASS CHANGE WITHOUT ITS REASON IS A DELETION WEARING A NEW LABEL** (vc). A reclassification is a CLAIM ABOUT WHY: `deviate` must RETARGET rather than delete, recorded where the artefact is GENERATED FROM.
- (2026-08-31) **A DECLARED EXCLUSION CARRYING ITS REASON IS THE CURE; A SILENT DROP IS THE DENOMINATOR ATTACK.** The difference is entirely whether the reader is told.
- (2026-08-31) **AN OWNERSHIP SPLIT IS A PURPOSE, NOT A BOUNDARY** (ic, correcting me). Reverting so a peer can rebuild byte-identical code is churn wearing Highlander's clothes. Applied again today: dc took the `ComputedSatisfaction` string because they were already compiling that file, and **two hands in one file IS `0206` in miniature.**
- (2026-08-31, CORRECTED) **`close --note` STAYS RULED OUT ON A CONTINGENT FOOTING.** The keg has no `edit`; the ruling survives because v3.0.1 ships from the tree. **Two 3.0.0 artefacts still differ -- re-driven 2026-09-01 13:36Z: tree `98ccc62c` naming `361eff99`, keg `04e7dda8` naming `80d8b2ca`.**
