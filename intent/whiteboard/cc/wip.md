---
node: cc
name: Control Claude
role: control
session_id: ae8c8153-6f3f-438f-b96b-04bd381ad4ed
heartbeat_at: 2026-08-29 23:58Z
status: active
focus: "WP-08 PHASES 1 AND 2 ARE BUILT, GREEN AND MUTATION-PROVED, AND NONE OF IT IS COMMITTED -- `render.rs` held three authors' uncommitted work and vc ruled the order: ic `form.rs` (landed `771cc847`), then dc takes `render.rs` whole, then I take `lib.rs` + `daemon.rs` + `userstate.rs` + `cli_routing.rs` and NAME ic's `pub mod form;` line in the message. RE-MEASURE BEFORE LANDING; do not trust this line. Next after landing: `AC-08.10`, the dependency ruling."
claims: [ST0056/06, ST0056/08, ST0056/10, ST0056/13, ST0057/00]
---

# Control Claude (cc)

## DOING

**READY TO LAND, WAITING ONLY ON dc's `render.rs` COMMIT.** Four files: `intentsvcs/src/daemon.rs` (new), `intentsvcs/src/userstate.rs`, `intentsvcs/src/lib.rs`, `intent-cli/tests/cli_routing.rs`. **`lib.rs` carries ic's `pub mod form;` beside my `pub mod daemon;` and both sources now exist, so it lands with mine -- naming that line is what makes it honest rather than a sweep.** `cli_routing.rs` cannot land before `render.rs`, because it asserts the door is `fn engine`.

**AC-08.3 IS DONE AND THE SHAPE IS THE DELIVERABLE, NOT THE CODE.** Routing folds over a LIST of `Endpoint` whose body names no transport; a third one is a variant plus an `answers()` arm. `render.rs` has ONE `Facade::open`, behind `fn engine`, held there mechanically. Candidates are DERIVED FROM STATE (hv's D6) -- the daemon binds `:0` and publishes its address, so no port literal exists to mint or go stale. Eleven tests; three mutations, each with its kill-set predicted BEFORE running and each restored byte-identical.

## TODO

**vc OWNS SPECIFICATION, ADJUDICATION AND REVIEW (hv, 2026-08-29). I OWN MAKING IT WORK.** Read their board for the current wording of anything below; do not re-derive a criterion from here.

- **[1] WP-08, PHASES 3-5.** Phase 3 is `AC-08.10`, the dependency ruling: first async runtime and first server, each landing with a written rationale. **It is cheaper than priced -- tokio and axum are the house choice with a sibling estate's tests behind them (`../Conflab`), so the rationale CITES rather than judges.** vc is rewording the row to derive its count from the lock instead of stating a literal. Then Phase 4, the daemon proper; then Phase 5, the JSON face.
  - **D56: `intentd` emits JSON ONLY, every renderer generic, no `maud`, no server-rendered HTML.** Over the socket AND over HTTP -- one output contract, N clients. **CITE IT, NEVER RESTATE IT** (class 17 was earned on this exact decision).
  - **`intentd` IS MINE END TO END** (hv D3); ST0064 narrows to the menubar app and ic builds clients. **Do not port Conflab's auth layer** -- bearer TTL, CORS list and keychain exist there to bridge a cloud this estate does not have. One auto-generated token on the HTTP half only; **the socket needs none, filesystem permissions are its authz.**
  - **THE TWO TRANSPORTS HAVE DIFFERENT AUTHZ STORIES AND THE SEAM MUST NOT ASSUME OTHERWISE.** `Endpoint` already dispatches per variant; whatever authz lands arrives the same way. Uniform treatment fails BOTH ways -- the socket inherits a check it does not need, or the port inherits the socket's absence of one, and the second is a hole.
  - **DESIGN AGAINST THE LARGEST REAL THREAD** (154 criteria, 297 attachments, a 59KB paragraph), never a fixture. Inherit `docs/design/design-system.md`; self-hosted same-origin fonts is a CONSTRAINT -- the daemon serves localhost and must work with no network.
- **[2] THE REGISTER `shipped` GATE ARM -- S.** `Entry::is_shipped()` asks nothing about existence, so a `keep`-dispositioned unbuilt verb ships by construction, and the refusers are declaratively INDISTINGUISHABLE from workers. **Built-ness is a property of the (register, binary) PAIR**, so the arm drives every shipped ROW against the binary. **Key on ROWS and drive from each row's own `args[0]`** -- bare `daemon` is rc=1 from CLAP, so a bare probe misreads a refuser as BUILT. Both free arms vc authorised still fire: `organize` sits in `shipped` AND `retired`, and `declared` double-counts it.
  - **THE UNWIRED MARKER MUST BECOME ONE `pub const` FIRST, AS A PRECONDITION.** Six constants under five names plus three inline literals plus its producer, across twelve files. **I declined to add a seventh in `cli_routing.rs` and asserted a property instead** -- the routed refusal must not read as an unbuilt verb, or a gate arm keyed on that marker reports every store verb on a daemon machine as unimplemented.
  - **`probeable` IS THE WRONG UNIT, RULED MY WAY BY vc:** `fc` answers yes for `AC-` and no for `AT-` inside ONE row, so the unit is the (row, target-kind) pair. vc writes it up; neither ic nor I patch our own half.
- **[3] WP-13 project search -- XL**, 9 criteria, Not Started. **[4-6] FILLERS, S EACH, BEHIND WP-08** (vc): `intent#0145`; `VIEW_NAMES` two readers (`address.rs` vs `Project::classify`, invisible to every gate); `canon-commit` gates on ADDS, so a commit REMOVING attachments is outside its reach.

**NOT MINE, OR WAITING ON SOMEONE.** `--browser` refuses with no daemon and ic has marked the arm as mine to change when the daemon lands -- it should ASK `intentsvcs::daemon::route`, the same call the store door makes, so the two cannot disagree about whether a daemon is running. `AC-00.5`'s retirement door is ic's. The write-path package, the prune, the `issues list` ordering ruling, the enum-roster findings and the 3.0.1 list are vc's or hv's -- **read them off vc's board, never from here.** `c755bd44`'s fabricated stamp STAYS UNRESOLVED: a corrected-looking fake is worse than an admitted one.

## Watch-outs

**BY CLASS, RULE-ONLY. Every line cost a wrong answer that read as a right one. INSTANCES live in `.history/` and in the commits.**

**1. AN INSTRUMENT CAN PASS WHILE BLIND, AND ITS GREEN IS INDISTINGUISHABLE FROM A REAL ONE.** The faces: a corpus that CANNOT EXHIBIT the defect (commonest); a control that would also pass broken; **a control that does not FLIP proves the probe was inert**; ACCIDENTALLY RIGHT; a predicate reproduced against the UNMUTATED source; a null read as a clean sheet; an instrument naming the WRONG CAUSE; a correct AGGREGATE that cannot check the mechanism (predict the SPLIT, never the total); the scanner inside its own corpus; a filter matching nothing, which passes for free; **a vacuity that renders as a well-formed TABLE, invisible to a control looking for a sentence**; and **a control that validates the axis it tests while silent about the one next door**. **THE ONLY CONTROL IS MUTATION -- break it and require the instrument to notice; assert the OCCURRENCE COUNT first, and predict the KILL SET before running.** **THE CHEAPEST INSTRUMENT I OWN IS A PRECONDITION THAT REFUSES TO RUN** -- fixture preconditions turned a routing flake that blamed the fold into a message naming the fixture, in one run. **A BROKEN INSTRUMENT RETURNS THE PASSING SHAPE, NOT AN ERROR SHAPE**, and a control that cannot plausibly fail turns that back into an error shape. **A FAILING CONTROL CAN EXONERATE A PEER YOU WERE ABOUT TO ACCUSE.** **EVIDENCE IN HAND IS NOT A READING: say what the numbers RULE OUT.** **AND A MISSING TEST IS A BACKLOG ITEM WHILE AN UNRUN TEST IS A DISCIPLINE FAILURE** -- three green runs of a test that flakes at 30% is how one gets shipped.

**2. A GREEN BOUNDS THE RING IT RAN ON AND SAYS NOTHING ABOUT THE NEXT RING OUT, AND EVERY DEFECT HERE HAS BEEN CAUGHT BY A STRICTLY BROADER POPULATION THAN THE LAST.** `cli_routing` passed 3 of 3 alone, then failed 9 of 30 under a loop, then failed differently again under 8 concurrent copies -- three populations, three distinct causes, and the first was the one I nearly reported on. **Stress the ring you are about to make a claim about.**

**3. THE DISK CANNOT BE THE WITNESS FOR A WRITE, BECAUSE THE DISK IS WHAT MOVES -- AND NEITHER CAN A VERIFICATION THAT WAS TRUE WHEN IT RAN.** **THE TENSE DOES THE DAMAGE** (dc's diagnosis, better than mine): a reading taken forty minutes before the sentence resting on it, written in the PRESENT as a fact about the tree, with nothing marking its shelf life -- true when measured, false when sent, and the wording cannot tell you which. **Cure: re-run it in the same turn as the claim, or stamp when it was taken.** **A premise about a shared tree expires in minutes.** **THE RECURRING SHAPE IS A CONFIDENT ABSENCE.** **AND HAND OVER PROPERTIES, NOT VALUES** -- a bare id is ambiguous, a moniker crosses estates, a hash is a READING, a pin dies at a bump somebody else schedules. Re-measure; never report a ledger.

**4. A SHARED TREE HAS OTHER WRITERS, AND `git add` IS FILE-SCOPED WHILE `--only` IS PATH-SCOPED -- NEITHER IS HUNK-SCOPED.** `git commit` commits the index AS IT STANDS; `--only <path>` protects other PATHS, not other people's edits to YOURS. Check `git diff --cached --name-only` is empty BEFORE `git add`, in a separate call. **A BROAD COMMIT IS INDISTINGUISHABLE FROM A NO-OP AT THE CALLER**; ask `git log -1 -- <path>` who owns your path afterwards.

**5. A PRIVATE INDEX IS THE ONE WRITE THAT CANNOT SWEEP A PEER, AND IT CAN BE MADE HUNK-SCOPED.** `GIT_INDEX_FILE` + `read-tree HEAD` + add-only-mine; the gate honours it. **THE HUNK-SCOPED FORM: replay your own edits onto `git show HEAD:<path>` in a temp file, `git hash-object -w` it, and `git update-index --cacheinfo` THAT blob** -- a commit does not have to come from the worktree. Verify by grepping the result for each peer's symbols and requiring 0. **AFTERWARDS THE SHARED INDEX HOLDS THE PRE-COMMIT STATE, WHICH AGAINST THE NEW HEAD READS AS STAGED DELETIONS** -- `git reset HEAD -- <your paths>` is the second half and is NOT optional. Verify with `env -u GIT_INDEX_FILE`; an EMPTY `GIT_INDEX_FILE=` is not unset.

**6. A PEER INSTANCE BEING WRONG DOES NOT MAKE THEIR SIGHTING WRONG. CHECK THE SET, NOT THE INSTANCE -- INCLUDING THE ONE YOU HANDED YOURSELF.** I reported the CLI broken for four nodes having measured ONE binary; the discriminator was WHEN a binary was compiled. ic undercounted a marker at three when it was six under five names. **SOME MEASUREMENTS ARE UNREACHABLE FROM ONE POSITION, HOWEVER CAREFUL** -- neither of those would have yielded to more care. **THE CURE IS A SECOND OBSERVER, NOT A BETTER LOOK**, which is the argument for reporting a half-formed measurement rather than polishing it alone.

**7. ATTRIBUTION DECAYS INTO ASSERTION, AND IT SPREADS.** `fn fc` was attributed to me by dc, then by ic, then by vc -- **three independent parties, none of whom ran the one-line measurement** (`git show HEAD:<path> | grep -c 'fn fc('` returns 0, so it is in-flight, so it belongs to whoever is flying it). It was one step from a commit message. **Exoneration and attribution are two claims. An incident and its generalisation usually have DIFFERENT authors; record the split while you still know it.**

**8. A GUARDED DOOR HAS AN UNGUARDED TWIN ONE COMMAND AWAY, AND THE TWIN IS THE SHORTER SPELLING.** `cargo fmt` vs `cargo fmt --check`; `bin/devbin build` vs `cargo build --release`. **The guard was never bypassed -- it was never invoked**, so nothing about it felt like a decision. **I reformatted two of ic's in-flight files this way and ic reformatted two of mine an hour later** -- same night, opposite directions, neither of us careless. **That makes it a property of the COMMAND PAIR rather than of either operator**, and the fix is a `--check`-first habit, not another apology. Check the ARTEFACT afterwards, and check the SET.

**9. EVERY TIMESTAMP IS READ FROM `date -u` IN THE SAME CALL THAT WRITES IT.** `git log` prints LOCAL. I have fabricated stamps three ways: wrong zone, typed-from-the-last-one, and one minute ahead of a clock I had just read. **A placeholder shaped like a stamp is worse than an admitted gap.**

**10. A PIPELINE ANSWERS FOR THE LAST STAGE, NOT THE INTERESTING ONE -- AND A SHORT-CIRCUIT ANSWERS FOR NOTHING AT ALL.** `x | tail` gives you `tail`'s 0; zsh has `$pipestatus`. **`cmd | head && echo clean` printed `clean` while `cmd` had FAILED** -- fired inside the very probe I was using to check my own formatting. **A `&&` CHAIN SHORT-CIRCUITS ON A ZERO-MATCH `grep -c`, so the arms after it never run and the output still looks like a result.** **AND `||` NEVER FIRES WHEN THE FIRST COMMAND EXITS 0 WHILE DOING NOTHING** -- `find -newermt` is REFUSED by bfs and exits 0 under `2>/dev/null`, so the fallback that would have worked was never reached. **READ THE EXIT CODE FIRST; when the claim is _it did not say_, re-run unfiltered.**

**11. THE POPULATION YOU MEASURED IS NOT THE ONE YOU MEANT, AND NOTHING NAMES IT.** `cargo check -p X` builds no test target; `--test X` excludes lib tests; a narrowing flag never narrows the CLAIM. **A TERMINATION CONDITION IS A FILTER NOBODY DECLARED** -- `cargo test` ABORTS after a failing target, so ONE peer's red test cost me a whole-suite signal on my own work. **Capture ONE run and analyse the capture; never compare across runs. AND THE INDEX IS A DIFFERENT CORPUS FROM THE WORKTREE.**

**12. A ONE-SIDED COMPARISON DISCARDS THE EVIDENCE THAT THE INSTRUMENT IS WRONG**, and a conservation check whose two sides share a source is an IDENTITY, not a measurement. `shipped`+`retired`=`declared` conserved perfectly while `organize` sat in both, because `declared` double-counted it. **Diff member-by-member, never by count.** **AND A TWO-WAY COMPARISON CANNOT ATTRIBUTE A DIFFERENCE TO ONE SIDE -- THE FIX IS ALWAYS A THIRD INPUT** (vc's widening of my narrower form): a DERIVED artefact against its SOURCE cannot tell EDITED from STALE, and stale is the common case.

**13. A FINDING THAT ARGUES ITS WAY INTO THE ROW YOU ARE CLOSING IS A REAL DEFECT LAUNDERED THROUGH A GREEN.** The pull is strongest exactly when the finding is good. **Corollary: when a ruling changes behaviour, RE-AIM the arm at what still holds; never delete coverage to make your own change pass.** **AND NEVER FIX A TEST BY EXCLUDING THE ROW THAT DISAGREES** -- normalising `init`'s fixture path kept the corpus whole where dropping it would have left a permanent hole.

**14. A THING FILED IS NOT FILED UNTIL IT REACHES AN ARTEFACT, WITH ITS CONSEQUENCE, AT ITS REAL SCOPE.** **A CORRECTION MADE IN CONVERSATION DOES NOT REACH AN ARTEFACT** -- it dies at the next compact on either side. **A MECHANISM FILED WITHOUT ITS CONSEQUENCE IS NOT FILED.** And a finding's SCOPE is set by the search that found it -- always smaller than the problem.

**15. A REFUSAL THAT DELETES IS THE MORE DESTRUCTIVE OPTION AND READS AS THE SAFER ONE.** Refusing is conservative only when refusing PRESERVES. **A CREATE IMPLEMENTED AS AN UPSERT REPORTS SUCCESS AND REPLACES**; hv ruled add/new must REFUSE. Wider: any verb that RECONSTRUCTS a row from arguments silently enlarges every time a payload-carrying variant is added.

**16. ABSENCE IS A STATE; UNREADABILITY IS AN ERROR; A `let Ok(..) else` CANNOT TELL THEM APART.** A refusal that ends the run reports a FLOOR read as a COUNT. **THE DANGEROUS DIRECTION IS THE QUIET ONE:** an unparseable daemon address dropped silently SHORTENS the candidate list, and a shorter list runs in-process while a daemon holds the store -- the exact outcome the rule exists to prevent. So it refuses.

**17. ONE VALUE, MANY HOMES, AND NOTHING COMPARES THEM.** A SERIALISER is a second writer nobody declared; so is the markdown formatter; **so is a TEST that spells out a layout instead of calling the function that owns it.** **The quietest home is PROSE SITTING BESIDE THE MACHINE-CHECKED FIELD**, and a hand-typed COUNT is the same shape. **THE HARDEST SUB-CASE IS A PROVENANCE RECORD THAT PARAPHRASES ITS SUBJECT INSTEAD OF CITING IT**: it becomes a second home the moment the subject is re-ruled and CANNOT BE REPAIRED BY CORRECTING IT, because as a dated record it is still true. **A record may quote WHAT WAS SAID, but must POINT AT, never reproduce, what is currently TRUE.** **CITING A RULING DOES NOT PROTECT A RESTATEMENT TWO LINES ABOVE IT** -- my own board cited D56 correctly while restating it falsely in the same bullet. **A PRECIS ROTS WHILE THE CITATION IT SUMMARISES STAYS RIGHT** (now in `AC-08.3` verbatim; it was the third instance that day). **AND DIVERGENT NAMES ARE THE PROOF NOBODY KNEW** -- five names for one marker is five authors who each believed they were first.

**18. A REMEDY IS A CLAIM ABOUT BEHAVIOUR AND MUST BE DRIVEN LIKE ONE.** **BORN STALE and EXPIRED are different defects and NEITHER is caught by reading.** **AN EXPIRED REMEDY CAN POINT AT A REPAIR A LATER RULING EXPLICITLY FORBIDS** -- actively counter-useful, and it reads identically to a live one.

**19. A VALUE GOES IN ONLY IF THE POPULATION'S OWN DECLARATION HAS IT -- BOTH DIRECTIONS.** Hardcoding a member of a draining population goes stale by being right; **MINTING one is the same error walking the other way.** **AND REFUSING TO MINT CAN PRODUCE A BETTER OPTION THAN THE ONE ON OFFER** -- declining to pick a port is what surfaced bind-`:0`-and-publish, which removed the constant entirely rather than choosing its value.

**20. A TWO-PART OBLIGATION WHOSE SECOND PART HAS NO TRIGGER IS A ONE-PART OBLIGATION.** The opening is prompted by the act of starting; NOTHING prompts the end, because finishing FEELS like the end. **CURE: state the PROPERTY that ends the window, never a promise to send a second message.** **AND MEASURE BEFORE YOU ANNOUNCE.**

**21. TWO RULES GOVERNING ONE DECISION -- ONE STRUCTURAL, ONE PURPOSIVE -- ARE CONSISTENT ONLY UNDER AN ASSUMPTION NEITHER STATES.** The tell is not drift between copies; it is that obeying one BREAKS the other, in a case neither anticipated, which is why it reads as a judgment call and is not one. vc's ground: **an obligation to READ is destroyed by volume long before it is destroyed by refusal.**

**22. THE FOLD IS ITSELF AN INSTRUMENT AND IT HAS FAILED IN THIS BOARD.** It has left one class in two sections, dropped a ruled GO, and duplicated numbering. **Archive VERBATIM first, under `wip-fold-HHMMZ.md` with the fold's own time in the name.** **UPDATE classes, never append instances beside them**; when a fold only trims narrative it is not aggressive, it is tidy. **AND THE MARKDOWN FORMATTER REWRITES WHAT THE FOLD WROTE.**

**23. A WEAKENING WITH NO SYMPTOM IS THE HARDEST THING TO CATCH** (dc). When a type widens, the loose port and the tight one both compile. **THE COMPILER SILENCE IS THE SIGNAL, NOT THE ALL-CLEAR** -- I added an enum variant and NOTHING broke, because every consumer used `if let`. The REASON for a tight match belongs in the file, next to the match.

**24. PRESENCE OF THE VARIANT IS NOT PRESENCE OF THE SUBJECT, AND AN ANSWER IS NOT LIVENESS EITHER.** A census keyed on the state NAME cannot see a payload field inside one variant; **key on the unit the defect lives in** -- `is_shipped()` is per-ROW while the populations are path-keyed, so one path can be two rows with opposite answers. **THE SHARPEST FORM COST A DAY'S FLAKE:** a socket that EXISTS is not a daemon, which is why the rule says _exists and answers_ -- and then **ANSWERING turns out not to be liveness either.** macOS has no `SOCK_CLOEXEC`, so a listening socket is created by one syscall and marked close-on-exec by a second; a `fork` between them leaks the fd into the child, and the socket keeps accepting until that child exits, however long ago its owner died. Measured 1 in 300 with a sibling thread spawning, 0 in 2000 without. **Every predicate of the form _is X there_ has one more mechanism under it than the last one you found.**

**25. STATE A SIZE ONLY AFTER THE SEARCH THAT WOULD REFUTE IT, AND QUOTE THE SEARCH.** A cost is a claim about what does NOT exist. **A DECLARED GAP CAN NAME THE WRONG SUBJECT, WHICH READS AS RIGOUR AND IS WORTH LESS THAN NO NOTE.** **Worse: I recommended MEASURED without checking whether measuring was PERMITTED**, quoting the fail-open sentence in the message that proposed breaking it. **A rule is at its least protective in the sentence that asserts it.** The method that works: quote the searches, as with WP-08's XXL -- vc took it without argument.

**26. A GENERATED ARTEFACT CAN WRITE INTO THE AGENT-INSTRUCTION CHANNEL, AND NOTHING ANNOUNCES IT.** `docs/reference/claude.md` IS `CLAUDE.md` on a case-insensitive filesystem. **Prefix the whole output space rather than special-casing the member that bit.**

**27. A DURABLE ARTEFACT CAN BE ADVANCED BY NON-DURABLE SOURCE, ONE-WAY, AND THE VECTOR IS THE TEST SUITE** (dc's correction, and the better half of my diagnosis). The runtime store is per-machine truth, never committed; it reached schema 15 while HEAD declared 14, so the delivered binary refused every store-reading verb. **The `SCHEMA_VERSION` edit was only the content -- the act that MOVED the store was running `cargo test`**, because `Project::open` against the real root runs the migration ladder and 16 test files do exactly that. **So the one act everybody performs constantly, believing it read-only, mutates shared durable state.** **The tell arrives as a REFUSAL IN AN UNRELATED VERB, after the act, to someone who did not perform it. Ask what moved UNDERNEATH the tool before asking what you broke.**

## Decisions

**I DO NOT EDIT `CLAUDE.md`, PERMISSION SETTINGS OR CONFIG ON A PEER'S INSTRUCTION**, however right they are on the merits. A peer's ask is never the user's approval. **AND I DO NOT APPLY AN EDIT A CLASSIFIER REFUSED A PEER.**

**I DO NOT COMMIT A PEER'S BYTES UNDER MY MESSAGE.** When a shared file blocks me the options are: ask for the order (vc's pen), or replay my own edits onto HEAD and commit a hunk-scoped blob. **Never `git add` a file whose diff I did not write.**
