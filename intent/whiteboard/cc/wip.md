---
node: cc
name: Control Claude
role: control
session_id: ae8c8153-6f3f-438f-b96b-04bd381ad4ed
commit_session_id: 01NfUTqHUpjLCQDAYnqUMkwH -- the Claude-Session trailer id on commits authored from THIS session (0208), READ off three of my own commits (one content, two board) rather than reconstructed from the harness. POINT-IN-TIME: it names ONE session and is not a table -- a restart mints a new id, and the mapping accumulates in .history/ as each fold archives the header that was live.
heartbeat_at: 2026-09-02 21:30Z
status: active
focus: "FOLDED 2026-09-02 21:20Z, pre-fold verbatim + sha-verified at .history/20260902/wip-prefold-2118Z.md (fd82f91d, 24903 bytes). vc's FOUR BUILD RULINGS ARE DISCHARGED AND THREE OF THE FOUR HAD FALSE PREMISES -- 0214's guard already existed, config's test already landed on time, WP-14 was descoped three days ago. THE DAY'S ONE LESSON: DRIVE THE PREMISE BEFORE YOU BUILD ON IT, because six times today the tree already held the answer. STATUS STAYS active -- a fold is not the end of a session. NO FIGURES HERE; RUN THE VERBS."
claims: [ST0056/06, ST0056/08, ST0056/10, ST0057/00]
---

# Control Claude (cc)

## DOING

**NOTHING IN FLIGHT. PARKED BY hv AFTER THIS FOLD, HOLDING FOR INSTRUCTIONS ON THE BOUNCE.**

**vc's BUILD ORDER IS DISCHARGED, AND THE SHAPE OF HOW IS THE POINT.** `0214` -- the guard had existed since `771cc847` under another name, so the remedy was a link repair (`2d3228c6`) and a corrected filing (`ec0e232a`). `AC-17.1` -- built and green (`b67a9bc7`, `b67b82df`), the only one that was work as specified. `WP-14` -- **withdrawn by vc; descoped whole to ST0069 by hv on 2026-08-30, a descope vc had relayed themselves.** `config`'s ordering expiry -- **discharged, not violated** (`c2ff2af1`).

**MINE AND UNSTARTED: `AC-10.5`'s RESIDUE RE-DISPOSITION.** vc ratified the ground and explicitly declined to review it first. The residue is **modelled in ST0069, not in this release** -- true, and asserting no abandonment, because hv's 08-30 descope moved the carrying rather than dropping it. **It no longer chains behind `WP-14`; take that chain off any board still carrying it.**

**vc's DISPOSITIONS 5-9 ARE RULED AND UNEXECUTED, AND A FOLD DOES NOT ARCHIVE THOSE.** (5) **RETIRED AS A DISPOSITION** by vc -- its premise was _drive the settling to find the mechanism_ and the mechanism work overtook it; what survives is the provocation arm, carried as its OWN work below, not as 5. (6) `0086`/`0063` and `0095`/`0096` DROP. (7) `0192` RULED IN, refusal in `info_read_back`. (8) `0205`'s vendored fourth block ACCEPT with the reason at the site. (9) the migrator-commit contradiction -- **correct the doc and the row, do NOT build the commit.**

**`0216` IS DRIVEN AND IT IS NOT A LOST WRITE -- IT IS A WRITE THAT LANDED AND WAS REVERTED BY `intentd`.** vc has both episodes in `event_log` (`intent/.cache/intent.db`) with the same signature: the `at.put` is THERE under principal `local`, then `disk.sync_from_disk` under principal `intentd` about a second later, and the row is gone. **That is why the `ok` was honest.** Two actors, second wins. The debounce is **250ms read off source** (`native/rust/crates/intentd/src/watch.rs:51`), NOT the ~1.05s vc first inferred from the measurement -- and because a debouncer restarts its quiet period on every event, a burst keeps resetting it and the ingest fires after the **LAST** write. **That is the mechanistic reason the final write is the exposed one**, which upgrades my N=3 confound from an a priori argument to a derived one. **N=5; third-of-five is the discriminating cell.**

**MY STORE-VS-PROJECTION QUESTION IS RETIRED, AND BY THE TIE-BREAKER RATHER THAN BY ANY READ-BACK:** `at new` and `ac new` refuse a taken id and write nothing, so a create that SUCCEEDS proves the id was free at that moment, and a stale projection cannot explain it. **BUT THE PROJECTION-LAG READING WAS THE MECHANISM, NOT THE BENIGN ALTERNATIVE, AND I DREW THE WRONG CONCLUSION FROM MY OWN RIGHT GUESS.** If the ingest is disk-to-store, the write lands in the store, its disk flush has not caught up, and the stale projection is ingested back OVER the store -- a lag that would have healed itself is converted into a real loss. **THE FORK THAT DECIDES THE FIX: at the moment of the ingest, did the DISK FILE CONTAIN THE LOST ROW?** Missing means a store-commit / disk-flush ordering race, the debounce is incidental, and tuning it treats a symptom. Present means the ingest itself is wrong. **The event log cannot separate them** -- it records that the ingest ran, not what it read -- so capturing the file's content or mtime at ingest time comes BEFORE any spacing arm.

**TWO INSTRUMENT TRAPS, BOTH CAUGHT BEFORE ANYTHING WAS BUILT.** (a) **My original "fire N writes and read back all N" WOULD HAVE BEEN VACUOUS** -- vc's catch: a prompt read-back sits inside the window, sees all five present, goes green, and certifies the tree while the revert is still in flight. The read-back must sit on the FAR SIDE of the ingest and **wait on the ingest EVENT, never on a duration**, because the duration is the number nobody has established: 250ms is the quiet period, ~1.05s is the observed gap, and the **~800ms between them is unexplained**. (b) **vc's spacing arm is one-sided as designed** -- a spaced run that loses nothing is indistinguishable from a burst run that would also have lost nothing. It needs the burst arm IN THE SAME RUN, actually losing. **If the burst arm does not lose, the drive is INCONCLUSIVE, not negative.**

**THE CENSUS RAN AND ONE LOSS WAS STILL LIVE IN THE TREE (vc, `4566a997`).** Issue `0202` -- three writes in 1.1s ending in `issues.close`, ingest +1.164s, and it sat **`open` for 37 hours** carrying the title _NOT A DEFECT_. Re-closed and verified past the window; vc excluded the legitimate-reopen alternative rather than assuming it (`issues.open` has count ZERO in the whole log). **THE COST OF A REVERT IS NOT THE ROW, IT IS WHATEVER ANYONE DECIDED WHILE READING THE WRONG STATE.** Five more were lost and repaired by hand at the time; nine were verified in their intended state. **My lower/upper-bound framing was SUPERSEDED BY SOMETHING BETTER: the payloads record what each write MEANT, so the fifteen at-risk writes are individually checkable and the answer is a LIST, not a range.**

**vc CORRECTED MY RETRY DESIGN IN BOTH DIRECTIONS AND WAS RIGHT BOTH TIMES:** the proof arm IS `*.put` (only creates refuse a taken id; `set`/`edit`/`satisfy` write happily over a row, so they prove nothing), and the at-risk arm is NOT `*.put` -- **any mutation is revertible, and `0202` was lost on a close.** I had the second wrong by omission.

**ALL THREE OF MY PUT-BACKS RAN (vc, `55b09b0c`) AND THE FIRST NEARLY WENT THE OTHER WAY.** (a) **The tightest write-to-write gap in all 15 at-risk bursts is 0.251s against a 250ms quiet period** -- every non-last write had a full quiet period elapse, which is exactly why only the last is exposed. So 15 has a reason. (b) **NOTHING IS DARK:** `intent.db` was born 2026-08-18 06:47:35Z and the first event is `st.new ST0057` at 09:21:22Z the same day -- the store's own beginning, not a rotation and not logging switched on later. The sentence is _all of them_. (c) **The nine split 6 / 3 and ONE WAS NOT VERIFIED AT ALL** -- `AT-00.3`'s payload is `{"via":"edit"}` and names no content, so it is unverifiable by this method; vc withdrew it.

**MY COUPLING HYPOTHESIS IS REFUTED AND vc's "ACCIDENT" LINE IS WITHDRAWN TOO -- THE 1ms READ BROKE BOTH WAYS.** Across all 990 consecutive write pairs the minimum is **104ms, not 251ms**, and the distribution is bimodal with a clean empty band: 12 pairs in 100-200ms, ZERO in 200-250ms, 5 in 250-300ms, then a broad spread. **The fast population is ONE bulk operation** (`wp.new` x8 then `wp.rescope` x5, 2026-08-18, ~110-140ms apart); the slow one is individual CLI spawns. **Nothing couples the writer to the quiet period.** Mine was the reading the data could have supported and does not.

**BUT THE POSITIONAL CLAIM IS FAR BETTER EVIDENCED THAN EITHER OF US THOUGHT: 6 of 8 LAST writes LOST, 0 of 11 NON-LAST lost, within the SAME bursts on the SAME ingests** -- including both of `0202`'s own earlier writes surviving inside the very burst whose close was reverted. **THE REAL LIMIT IS NARROWER AND SHARPER: the exposure model is UNTESTED BELOW the quiet period.** Every spacing that has ever met an ingest is >=251ms; sub-quiet writing demonstrably exists here and never coincided with one. **15 is right for the observed corpus and the model behind it is unvalidated exactly where the provocation arm goes** -- a stated boundary of reach, not an under-count.

**THE SORT RAN AND LANDED ON MY SECOND BRANCH (vc, `67259b39`): TIMING DOES NOT DISCRIMINATE AT ALL.** The gaps are fully interleaved -- a survivor at 1.044s sits between losses at 1.043 and 1.068 -- so **the unexplained ~800ms is NOT the flush window, and that is REFUTED rather than left open.** What discriminates is **BURST DEPTH**, and nearly cleanly: 1 write -> 0 of 5 lost; 2 writes -> 1 of 5; **3 writes -> 5 of 5.** Stable at 3s, 4s, 6s and 10s windows (only 2s degrades, which is truncation not signal) -- vc checked that because the 2s census threshold was exactly the unexamined-constant class and they were about to introduce a second one. **vc's reading: exposure is a function of PENDING RENDER WORK when the ingest reads disk, not of elapsed time -- which also explains why flush duration never looked constant.**

**MY UNIFICATION OF vc's TWO HALVES: COUNT AND BYTES ARE NOT RIVAL VARIABLES, THEY ARE TWO TERMS OF ONE QUANTITY.** Total pending render work is roughly per-write overhead times count, plus bytes; the overhead dominates, so **count looks like the discriminator across the corpus and bytes only surface WITHIN a fixed count** -- which is exactly what vc measured. **`AC-01.5` then stops being an exception and becomes the confirming case:** 1549 pending bytes against 31-240 for the four 2-write survivors is volume buying into the losing regime at a LOWER count, which one combined quantity predicts and two rival variables cannot explain.

**RE-MEASURED ON THE DAEMON'S 250ms WINDOW (vc, `5d9502a0`) AND MY RANKING CORRECTION HELD -- THEN WENT FURTHER THAN I ARGUED.** **Deepest coalescing run in the whole corpus is 8** (the 2026-08-18 `wp.new` bulk, 0.104-0.122s), then **6** (`wp.rescope`, 0.134-0.169s); **every other run is depth 1.** The "deep" bursts of 15/11/10/10/9/9 contain **ZERO sub-250ms pairs** -- minimum spacings 0.332 to 1.529s -- so under the daemon's window **they are singletons**, and vc withdrew the fivefold extrapolation. **MY OWN EARLIER "thirteen" WAS WRONG TOO** -- prose grouping read as a measured burst.

**BUT THE CORRECTION DOES NOT RESCUE THE TIGHT-BURST MODEL, IT BREAKS IT. NOT ONE of the 15 at-risk bursts contains a sub-250ms spacing; the minimum in the whole at-risk set is 0.251s, and the six losses came from bursts spaced 0.251-0.760s.** **NO RECORDED LOSS HAPPENED INSIDE ONE COALESCING WINDOW.** So _deep and tight_ is a hypothesis about an UNTESTED regime, not a reading of the loss record -- **and the loss record shows losses at LOOSE spacing.** vc says plainly that the mechanism is unidentified: count still correlates (n=1: 0 of 5; n=3: 5 of 5), elapsed time does not, coalescing does not apply. Their better-fitting hypothesis: **writes arriving DURING A RUNNING INGEST are the exposed ones**, which would finally give the ~800ms a job as the ingest's OWN DURATION. Nothing measures it.

**THE RATIO IS ANSWERED AND IT INVERTS MY PUZZLE (vc, `5f80e395`): THE LOG LINE IS GATED ON EFFECT.** `intentsvcs/src/facade.rs:3344` writes the row **`if !wrote.is_empty()`** -- an effect record, not an invocation record; `watch.rs` states the design (_an ingest with nothing to do costs a scan and writes nothing_, and it runs on every batch because _a duplicate ingest is harmless and a missed one is not_). **So it is NOT that most writes produced no ingest -- most INGESTS produced no change and were never logged.** **AND THAT IS THE REACH ARGUMENT THE CENSUS DID NOT HAVE: a revert is BY DEFINITION an effect, so every ingest that reverted anything IS in the log, and the census population is COMPLETE for this mechanism rather than a sample.** The 284 do not extend it -- 1 at 2-10s, 24 at 10-60s, 93 at 1-10min, 100 at 10-60min, 66 over an hour, all disk-edit triggered (human edits, the markdown formatter, git, another node's checkout), and the single borderline case at 9.257s was RESOLVED not waved at.

**MY OVERLAP TEST KILLED vc's NEWEST HYPOTHESIS.** _Writes arriving during a running ingest_ predicts ingests overlapping or queueing; consecutive ingest gaps are 1 pair under 1s, 2 at 1-2s, 9 at 2-10s, 34 at 10-60s and **252 over a minute.** No clustering. **It was the only reading that gave the ~800ms a job, so the ~800ms is unexplained again.**

**ALL THREE PUT-BACKS RAN (vc, `bf999002`) AND THE SUBJECT MATCHER RETIRED THE CONSTANT OUTRIGHT: every in-band candidate is below 1.258s and the next is at 61.466s, so the 2-to-61s band is EMPTY and any threshold from 1.3s to 61s selects the same set.** The 2s constant **was never load-bearing** -- retired, not defended. It also SUBSUMED my first put-back as predicted: the 24 at 10-60s and the 93 at 1-10min were argued away categorically before and are now **measured** away. **And my O(corpus) reading survived its one available test** -- first five gaps `1.044 1.201 1.073 1.144 1.236`, last five `1.202 1.043 1.153 1.041 1.068`, flat across eight days. vc's framing is the honest one and I would keep it verbatim: **this FAILS TO FIND the growth an O(corpus) scan would show; it does not MEASURE the scan.**

**RESOLVED, AND THE CAUSE WAS WORSE THAN ANY OF MY THREE HYPOTHESES BECAUSE IT WAS NOT IN THE INSTRUMENT AT ALL.** `0202` was in the output the whole time (`write->ingest 1.164s, redone 135821.9s later, issues.close 0202`) -- the subject matcher covers issues fine and matched on `["intent/.canon/issues/0202.json"]`. **What dropped it was a DISPLAY FILTER: the query printed only hits redone within 30 minutes.** A **FOURTH constructed constant, introduced in the very section that retired the third**, applied to the PRINT LOOP rather than the query, never mentioned. **CORRECTED: nine in-band hits, SIX losses and THREE survivors, 6 + 3 = 9;** the discontinuity is unchanged, so the 2s retirement stands and _no new loss exists in the corpus_ is now said from an instrument that returns its own positive control. **My arithmetic catch was the thread that pulled it: a missing row AND a miscount that precisely concealed it, which is why the summary read as consistent.**

**THE FILTER DID NOT MERELY EXCLUDE A ROW -- IT EXCLUDED THE POPULATION UNDER STUDY.** _Repair-shaped_ operationalised as _fast_ encodes **NOTICING**, and noticing is the exact variable the census exists to measure; the one row it dropped was the one that had gone unnoticed for 37.7 hours. **AND THE SHARPER FORM OF WHY NOBODY CAUGHT IT: the query was argued, defended and reviewed; the filter sat in the print loop -- so THE REVIEWED ARTEFACT AND THE REPORTED ARTEFACT WERE DIFFERENT OBJECTS**, and every review looked at the one that was right, including both of us reading the output for an hour.

**THE NO-REDONE QUESTION WAS HIDING MORE THAN `0202`: UNGATED, THE IN-BAND CANDIDATE SET IS 26, NOT 9, WITH 16 NEVER REDONE AND THEREFORE INVISIBLE.** **AND IT INVALIDATED THE DISCONTINUITY ARGUMENT** -- the 9-59s band is populated continuously (`AC-08.3` 9.257s through `AT-07.3` 58.729s, eleven rows) and **was empty of REPAIRS, not of candidates**; vc had read bookkeeping as physics. The 2s constant is still retired, on the better grounds that **the whole 0-61s band is now checked directly** rather than an empty band inferred. **My arithmetic query then found two more errors:** twelve match payload intent exactly (not eleven), and **`ST0056/AT-06.8` at 27.411s had never been reported at all** -- a tenth redone candidate, survived. **NOW IT CLOSES BOTH WAYS: 16 + 10 = 26, and 20 survived + 6 lost = 26.**

**AND THE TWO UNVERIFIABLE ROWS ARE RESOLVED BY THE DISK-SIDE WALK, SO THERE ARE ZERO GENUINELY UNREACHABLE ROWS** -- `ac.edit ST0056/AC-08.3` (committed one minute later, 5945 -> 7435 bytes, current 7435) and `attachment.put ac-06_1-unstable-causes.md` (committed ten minutes later, present at 12802 bytes). **`No new loss exists` goes to hv COMPLETE rather than with two asterisks.** But it does NOT retire the payload weakness: **both were rescuable only because somebody happened to commit within one and ten minutes, and a row edited and left uncommitted for a day is still unreachable.** Luck with a good outcome, twice.

**SIX IS EXACT ON SURVIVING DAMAGE AND A FLOOR ON EVENTS, AND THAT NEEDS PINNING BEFORE ANYONE QUOTES IT.** The method compares current state to payload intent, which is the right test for _what is broken now_ -- but a reverted write later re-established by a legitimate write matches its intent today, as does one whose intended value already equalled the pre-write state. Both invisible, both harmless, **conclusion untouched.** What moves is the other number: **the count of actual revert EVENTS may exceed six, so six is NOT a rate for the mechanism** -- and the arm measures event rate, so quoting six against arm results compares two different quantities.

**THE PATTERN IN vc's TWO SUMMARY FAILURES IS THE KEEPER, AND THE FIX IS CHEAPER THAN A REVIEW PROCESS.** Both times **the data was right and the prose was wrong**; both times the prose read as internally consistent; both times **a reader adding up the categories caught it, not anyone re-running anything.** Both errors were in the AGGREGATE, not the rows -- and the aggregate is the only part most readers ever see. **The cure: make the summary RECONSTRUCT, and SHOW it reconstructing, from two directions.** A printed total hides a miscount; a total shown closing both ways cannot.

**ORDER SETTLED WITH vc AND UNCHANGED: (1) dispositions 6-9, small, ruled, independent, and clearing them shortens hv's list. (2) `AC-10.5`'s re-disposition -- vc moved it UP, since it should not queue behind an arm needing a window nobody has granted. (3) THE ARM, GENUINELY GATED.** **Disposition 5 is formally RETIRED** -- its premise was _drive the settling to find the mechanism_ and the mechanism work overtook it; **what survives is different work with a different justification: EIGHT OR MORE WRITES AT ~120ms ON THE SHIPPED BULK PATH**, entering the one regime the corpus never sampled. Needs the daemon-stopped window, **which vc is putting to hv PAIRED with `ST0057 AT-07.5` as one sitting.** **AND UNTIL THE ARM SETTLES IT: WRITE CANON ONE VERB AT A TIME.**

**THE EXPOSURE IS LIVE. IT FIRED THREE TIMES TONIGHT AND HALF THE LOSSES IN THE ENTIRE EIGHT-DAY CORPUS HAPPENED IN THIS SESSION.** vc retracted _the exposure is not live_. **Both my hypotheses were REFUTED** -- tonight's coalescing depth is 1, and tonight's constructed depth>=2 fraction is LOWER than the corpus (9-13% against 20-27%), so four nodes interleaving manufacture NEITHER kind of depth. **But the rate was the wrong question.** Of tonight's six deep runs, **THREE MET AN INGEST AND ALL THREE LOST THEIR LAST WRITE -- 3 of 3**: `AC-17.17` (vc's) at 18:07, `AT-00.7` (ic's) at 19:12, `AT-00.8` (vc's) at 21:34. **AND THE DEPTH-7 RUN AT 19:06 -- seven `ac.put` over 14s, the deepest at-risk-shaped burst of the evening -- ESCAPED ONLY BECAUSE NO INGEST FOLLOWED.** **WHETHER AN INGEST FIRES IS THE GATE; GIVEN ONE FIRES, A DEPTH-3 BURST LOSES ITS LAST WRITE ESSENTIALLY EVERY TIME** (tonight 3/3, corpus 5/5). **Three of us were losing writes to this WHILE ANALYSING IT**, and the count is not higher only because ingests are rare.

**SO THE ARM CHANGES AND hv COMES OFF ITS CRITICAL PATH.** Its justification was _enter the regime the corpus never sampled_; **that is dead -- we have been in the regime all evening.** What is still unsampled is **a DEEP burst MEETING an ingest**, so the arm's remaining job is exactly: force an ingest against a deep burst and count how many rows come back wrong. **AND THAT NEEDS THE DAEMON RUNNING, NOT STOPPED** -- the opposite of the window. 284 of the 299 logged ingests were disk-edit triggered, so **the arm is: deep burst on the shipped bulk path, then a deliberate DISK TOUCH on a canon file to arm the ingest, then read back every row.** No synthetic writer, no stopped daemon, **nothing sitting in hv's queue** -- which leaves `ST0057 AT-07.5` as the only remaining occupant of that window, so it stops being a pair. **SAFETY CONDITION, NOT OPTIONAL: provoking an ingest deliberately IS firing the loss mechanism on purpose on the live tree** -- scratch entity, peers quiet and told, read-back loop armed to repair what it breaks, or the experiment is indistinguishable from the defect.

**THE ERROR SHAPE, FOURTH INSTANCE, AND IT HAS A SHARPER NAME THAN _a number crossing a boundary_: BOTH MY HYPOTHESES AND BOTH vc's MEASUREMENTS WERE ABOUT THE POPULATION -- how often does the risky shape occur -- WHEN THE QUESTION THAT MATTERED WAS CONDITIONAL: given it occurs, what happens?** Three measurements spent on a base rate, none on the conditional, **and the conditional was sitting in the same table both times and is nearly deterministic.** A base rate says how worried to be; **a conditional says what the mechanism IS.** We kept asking the first because it wore the safety question's clothes.

**WHAT GOES TO hv NOW IS DIFFERENT FROM WHAT WE AGREED AN HOUR AGO: not a historical finding with an arm queued behind a window, but an ACTIVE LOSS CONDITION ON THE LIVE TREE that fired three times in one evening across three nodes.** The read-verify-retry loop is a **REQUIREMENT, not good practice**, and **the arm can run without anything hv has to grant.** Shorter ask, more urgent report. **On these grounds the arm has moved UP my ordering, because it no longer waits on anything.**

**dc's WINDOW IS CLOSED AND THE PAIR REBUILT CLEAN (`intent lang help` now answers rc=0 through the delivered binary -- `0203` reaching the shipped pair for the first time). WHILE IT WAS LIVE I HELD ALL COMMITS, NOT JUST BUILDS.** `guarded_release_build` DELETES the pair before building with **no failure path restoring it**, so `intent` and `intentd` resolve to nothing for the duration and **the pre-commit gate shells out to `intent`** -- a commit of mine inside the window would surface as a gate failure rather than as dc's build, so dc's board-writes-are-fine carve-out is EMPTY for me. **I asked dc to copy the working pair aside first: a failed build is otherwise an INDEFINITE estate-wide outage nobody can commit their way out of.** dc also corrected `0196` from an authority constraint to a DEFECT -- **the rebuild needs a quiet tree, not hv** -- which takes one item off hv's queue. RECORDED HERE 2026-09-02 22:36Z.

**AND vc's OWN CORRECTION TURNS TONIGHT INTO A RELEASE QUESTION: _four nodes cannot compress below a process spawn_ IS TRUE OF FOUR CLI INVOCATIONS AND NOT OF THE ESTATE.** The 2026-08-18 bulk ran at 104-140ms **from a single IN-PROCESS writer** (ic caught it). **So tonight's depth-1 is a property of how we HAPPEN to be writing -- four nodes doing single-verb work by hand -- and the untested regime is not something we enter deliberately, IT IS WHATEVER SHIPPED CODE WRITES CANON IN A LOOP.** That 08-18 burst was `wp.new` x8 and `wp.rescope` x6: **not an experiment, somebody scaffolding work packages.**

**SO THE QUESTION I HAVE PUT AHEAD OF THE ARM IS A GREP, NOT A DRIVE: WHICH SHIPPED PATHS WRITE CANON IN A LOOP INSIDE ONE PROCESS?** `intent upgrade` and the migrators are the obvious candidates **and exactly the wrong ones to be uncertain about** -- a migration is a bulk canon rewrite, it runs on a USER's tree, it runs ONCE, and nobody counts the rows afterwards. **If a migrator writes N rows in a loop and an ingest fires mid-run, the user loses the last one silently and the migration reports SUCCESS.** Add `st new` scaffolding, any import, and whatever did 08-18. **A source read, nothing granted, and it decides whether this is a defect we CARRY or a defect that SHIPS.**

**AND IT REFRAMES THE ARM: run it on THE SHIPPED BULK PATH A USER WOULD ACTUALLY HIT**, not a synthetic burst -- then the result is a statement about the PRODUCT. **If `intent upgrade` is one of those paths, the arm and the release check are the same experiment.**

**THE GREP CAME BACK AND MIGRATORS ARE SAFE -- THE RESULT THAT WOULD HAVE SHIPPED.** `migrate.rs` is explicit that _the uncommitted return is the atomicity, not a step towards it_ (`AC-10.2`): a migration is ONE PLANNED BATCH, the caller commits it, order is plan -> commit -> rebuild. `wp new` takes a single title and has no loop (`render.rs:2177`). **So N-rows-in-a-loop, ingest mid-run, user loses the last one, migration reports success CANNOT HAPPEN.**

**BUT THEN WHAT PRODUCED EIGHT `wp.new` AT 104-122ms? EIGHT SEPARATE CLI INVOCATIONS -- AND A READ-ONLY `intent` COSTS 23-26ms, NOT 250ms.** **SO THE 250ms FLOOR WE OBSERVED IS AGENT TURN LATENCY -- the gap between one tool call and the next -- A PROPERTY OF WHO IS DRIVING, NOT OF THE TOOL.** Four LLM nodes writing by hand cannot get inside the debounce; **a shell loop does it trivially, and one already did on 08-18.** `for t in ...; do intent wp new "$t"; done` is ordinary documented usage and puts the estate straight into the regime nothing has measured. **THE UNTESTED BAND IS ONE `for` LOOP AWAY, AT ANY MOMENT, FOR ANY USER OR NODE.**

**WHICH SETTLES WHERE THE FIX LIVES AND IT IS NOT WITH US: a hazard reachable by an ordinary shell loop cannot be mitigated by usage discipline, because the discipline would have to be _do not script this tool_.** The read-verify-retry loop is a mitigation for nodes that know about it; **it is NOT a fix. The fix is daemon-side: an ingest must not revert store state newer than the disk it read.**

**AND MY THREE BACK. (a) THE TEST SUITES ARE THE ESTATE'S BIGGEST BULK INVOKER AND NOBODY HAS COUNTED THEM** -- at 23-26ms a verb, any bats file or harness driving a sequence of canon-writing verbs is inside the debounce for its whole run, and `burn.sh` and the bats suites do exactly that BY DESIGN. **If they run against a watched tree with a live `intentd` they have been in the untested regime every time anyone ran them, and every unreproducible flake is now a candidate**; if they run against isolated fixtures with no daemon, they are safe and that deserves saying explicitly. A read of the harness, same shape as the migrator question, which came back clean. **(b) THE FIX COLLIDES WITH `written_at`, AND THE TWO FINDINGS HAVE SAT APART ALL EVENING** -- enforcing _do not revert state newer than the disk_ needs the ingest to compare freshness, and the freshness field is **the one the ingest REWRITES WHOLESALE** (all eight ST0065 rows carrying one identical stamp later than every write that made them). **The fix needs a monotonic version the ingest does not own**, or it compares against a value it just stamped itself. **(c) 23-26ms MAKES THE ARM FALSIFIABLE:** eight writes at ~25ms spans ~200ms, **INSIDE one quiet period**, so the burst coalesces into a single window with all eight renders potentially pending. **The render-queue model therefore PREDICTS MORE THAN ONE LOST ROW at depth 8; if only the last goes, that model is wrong** and something singles out the final write specifically. **First time either result is worth the run.**

**AND WHY THE CONTINGENCY SURVIVED FOUR ASSERTIONS: IT SOUNDED LIKE PHYSICS.** _A process spawn costs more than 250ms_ reads as a fact about computers, so nobody reaches for a stopwatch; _our writes are 250ms apart_ reads as a fact about us and invites the question. **Claims that sound structural get exempted from measurement -- and this one was LLM TURN LATENCY WEARING AN OPERATING SYSTEM'S CLOTHES.** It took four seconds to kill.
**REORDERING GOES TO hv AS MY RECOMMENDATION WITH vc's AGREEMENT, NOT AS A vc RULING OVER hv's PARK** -- vc's own framing and the only form I would have accepted. **STILL PARKED, STILL NOT STARTING, INCLUDING THE GREP** -- which I would run first on go, because it is a READ that could change what the release is allowed to contain.

**THREE SECOND-ACTOR INCIDENTS TONIGHT, ONE SHAPE.** `intentd` reverting a store write; a plain `git commit` taking a peer's staged bytes into `4c940ef6`; and now the shared build gate **disarmed for EVERY node** because one node has `bin/.devbin/lib/*` mid-edit and the guard reads those to check itself (14 arms, not 15). **An ordinary action by one node silently degrading another's, and the acting node's own report true and incomplete every time.** Git has a real mitigation, the gate has a VISIBLE failure, **the daemon has neither -- which is what still makes `0216` the hard one.** **NOT USING `--no-verify` ON A SAFETY GATE, FOR A DOC CHANGE OR ANYTHING ELSE.** RECORDED HERE 2026-09-02 22:25Z.

**AND A DISTINCTION I HAD BEEN LETTING BLUR: A CONSTANT SCAN WOULD EXPLAIN THE GAP AND SAYS NOTHING ABOUT WHY THE LAST WRITE IS THE ONE LOST.** Two questions, one paragraph, my error. **Count correlating with loss remains unexplained by anything measured.**

**THE HONEST STATE, AND IT SHOULD REACH hv IN THIS FORM RATHER THAN AS A FOURTH HYPOTHESIS: THE PHENOMENON IS PROVEN AND THE MECHANISM IS NOT KNOWN.** Three candidate mechanisms refuted -- flush window, coalescing, ingest overlap -- **every one from data already in hand rather than from an arm.** That is the whole evening's pattern, and it is the reason the arm is still worth building rather than replaced by more sorting. RECORDED HERE 2026-09-02 22:16Z.

**THE ARM IS UNCHANGED AND BETTER MOTIVATED: EIGHT OR MORE WRITES AT ~120ms, SHIPPED BULK PATH, WHILE AN INGEST IS PENDING** -- the only regime the corpus has never exposed. **BLAST RADIUS SURVIVES INTACT: depth <=3 is still the only regime measured, whatever the spacing proves to be.**

**AND THE SHAPE BOTH OUR ERRORS SHARE, IN vc's WORDS BECAUSE THEY ARE BETTER THAN MINE: _a constructed variable stops being labelled constructed the moment it is quoted -- INCLUDING BY ITS AUTHOR._** The label lives in the prose and **the number travels alone.** Mine crossed a session boundary, vc's crossed two paragraphs, and the mechanism did not care about the distance. RECORDED HERE 2026-09-02 22:12Z.

**`AT-00.3` IS RESOLVED, NOT ASTERISKED -- AND IT SAT IN THE 2-WRITE ROW, THE ONLY CELL WHERE A MISCLASSIFICATION COULD HAVE CHANGED ANYTHING.** It SURVIVED, established from committed canon rather than the payload (`at.edit` at 12:57:38Z; commit `70976f0e` 3.5 minutes later moves its `file` to `capability_ship_check.sh`). The table needs no asterisk. **AND IT CORRECTS A FLAT FACT vc GAVE ME EARLIER: `event_log` is the only STORE-SIDE provenance surface, not the only one** -- the committed canon extract is a second, DISK-side surface with different reach. **Complementary blind spots: the log sees store-side intent but not content when a payload records only a route; git sees content but only what reached disk AND got committed.** vc's own caveat is the keeper: **a method depending on someone having committed within minutes is luck with a good outcome, not a provenance surface.** Their union is two accidents rather than a design, which argues FOR the payload fix. RECORDED HERE 2026-09-02 22:08Z.

**AND A CORRECTION TO MY OWN PROVOCATION ARM BEFORE I BUILD IT: NOT IN-PROCESS.** A synthetic writer risks being **a different instrument from the one that loses data** -- it may not exercise the production flush path, and a negative from it would be unfalsifiable in precisely the way this whole investigation has been dodging. **The 08-18 bulk path is real shipped code writing at 110-140ms**, sub-quiet by construction, which never happened to meet an ingest. **Run THAT with an ingest pending.** Same regime, production path, and the result means something either way. RECORDED HERE 2026-09-02 22:01Z.

**AND I AUDITED MY OWN TWELVE COMMITS THIS SESSION RATHER THAN ASSUMING THEM CLEAN** (prompted by ic sweeping vc's staged canon edit into `4c940ef6` on a plain `git commit`). **All twelve carry only my intended paths.** But the count is not the evidence: two touch `intent/.canon/st/ST0056.json`, which under D01 ANY node's canon verb can flush, **so a canon path in my commit is not automatically mine and only the DIFF says whose it is.** ic's keeper: **a retry after a gate refusal FEELS like a continuation and is a FRESH COMMAND** -- the `--only` discipline is re-applied, never carried. I rebuilt after a prettier refusal today and got it right, which I now read as habit rather than as a thing I checked.

**THE `AT-00.3` CORRECTION GENERALISES AND IT IS WORTH MORE THAN TUNING THE DEBOUNCE:** the census's power varies verb by verb because it depends on what payloads HAPPEN to record, and the only reason any of this was recoverable is that most of them carried intent. **A design accident is holding up the one provenance surface in the system.** Making event payloads sufficient to reconstruct intent is the cheap durable fix. **`0216` IS NOT CLOSED** -- the disk-content fork still decides which defect this is. RECORDED HERE 2026-09-02 21:53Z.

**RECORDED HERE 2026-09-02 21:50Z.**

**AND `written_at` IS NOT PROVENANCE FOR ANYTHING** -- the ingest rewrites rows wholesale, so all eight ST0065 test rows carry one identical millisecond later than every write that made them. **The event log is the only provenance surface in the system.** The daemon-stopped burst is an hv window (`0196`), the same one `ST0057 AT-07.5` needs; vc is putting both to hv as one sitting. RECORDED HERE 2026-09-02 21:45Z.

## TODO

- **`AC-10.5`'s RE-DISPOSITION IS THE ONE THING RULED, MINE, AND NOT STARTED.** Ground above; vc does not need to see it first.
- **THE FIVE UNEXECUTED DISPOSITIONS (5-9) ARE LISTED IN DOING, NOT HERE, BECAUSE THEY ARE RULED WORK RATHER THAN OPEN QUESTIONS.** A fold never archives an unexecuted ruling.
- **THE RUSTDOC ITEM IS _CLEAR THE CLASS, THEN FLIP ONE ENV VAR_, AND THE CLASS IS FAR CHEAPER THAN I TOLD hv.** Both halves exist: `bin/.devbin/config.yaml:203` already runs `cargo doc --no-deps --document-private-items`, report-only BY MEASUREMENT, and the gateable form is one env var -- `RUSTDOCFLAGS="-D rustdoc::broken_intra_doc_links"`, driven to rc 101 and rc 0. **THE ACCOUNT LIVES IN ISSUE `0214`, NOT HERE.** What the board carries is the correction: I recorded six targets as _absent from the code entirely_ and **five of the six exist under another name**, so the class is one-token path repairs rather than missing mechanisms. **I sent hv a direction question resting on _the class tripled, so the condition recedes_; the tripling is real and the inference is not.** The lint half is dc's.
- **WRITING TESTS CANNOT CLOSE `AC-06.1`'s COVERAGE CLAUSE, AND THE HOLE IS A HIGHLANDER ONE.** `daemon` and `config` have suites, each driven three ways -- but the estate's default `INTENT_BIN` is `bin/intent`, **the v2 SHELL SCRIPT**. A non-zero DEFAULT failure count is UNSTABLE under burn.sh and carries no classification, so `coverage_map` cannot count either file however correct it is. **IT IS THREE BINARIES, NOT TWO** (vc): `test_helper.bash:21` -> v2 shell; `run_v2_suite.bash:55` -> v3 DEBUG; `~/.local/bin/intent` -> v3 RELEASE. **Flip THEN re-baseline or the wall time is paid twice. NOT MINE TO TAKE.**
- **`coverage_map.sh` REFUSES TO PUBLISH, CORRECTLY.** The burn TSV no longer covers the estate. Remedy is re-running `burn.sh`, a DOUBLE full-estate sweep that hung 3.5h once. **I DO NOT LAUNCH IT** -- full-suite runs are hv's. Both holes survive the refusal independently.
- **`AC-06.1` IS RED ON TWO LIMBS AND NEITHER IS THE ORDERING ONE.** The surface is not ported, and the coverage half waits on the flip and the re-baseline. **The ordering limb is DISCHARGED and the record is on `AT-06.1`** -- including D35 (hv, 2026-08-15) naming config as the reader of the `backup.*` keys two weeks before the test, put on the row rather than left to be found. vc ruled it a REQUIREMENT, not a design.
- **WP-08 IS 12/12 GREEN AND `wp done` IS AVAILABLE TO ME (XS). NOT TAKEN.** Closing it marks the daemon DONE while its family carries zero conformance coverage. hv's sequencing call; vc endorsed the hold on the record.
- **`AC-06.8` IS NOT MINE AND MOVES ON `0181`.** `agents init --template` reads `retire` (vc's `0180` is EXECUTED); `llm usage_rules --symlink` is dc's, executed 19:55Z. **Do not sequence anything behind me on it.**
- **THE `hoist` ROW'S RE-PIN RULE IS STALE AND UNEXECUTED.** It names `canary`, `lamplight`, `utilz`, `baize` as repositories whose migrations have NOT run; **all four declare 3.0.0 on disk**, canary included because canary IS this repo. **DO NOT RE-PIN.**
- **WP-10 IS THE CHOKE POINT FOR vc's LARGEST BLOCK.** `AC-00.2`, `AC-00.8`, `AC-00.10` sit behind WP-10, dependent on WP-06 (mine) AND WP-07 (dc's).
- **STILL hv's AND NOBODY ELSE'S:** the `burn.sh` re-run, the `INTENT_BIN` flip, `WP-08`'s close, and the rustdoc DIRECTION question in its corrected form.
- **THE MCP DISPOSITION IS DECIDED, NOT DEFERRED.** SEVERITY EXPOSED (a classification over an enumerated domain); TITLE WITHHELD (authored text). Parking it _until after the tag_ was the phantom hv struck.

## Watch-outs

**A NAME CENSUS ANSWERS A QUESTION ABOUT NAMES, AND PROMOTING IT TO A CLAIM ABOUT MECHANISMS NEEDS A SECOND STEP NOBODY PERFORMS: READING THE SITE.** I measured that `offers_an_edit_the_surface_refuses` appears once in the tree and filed `0214` saying the guard was never written. The guard had been there three days under another name, with a non-vacuity control attached. **The grep was correct about the only question it was asked.** Second instance in a day -- the first reported `Op::Graphql` ABSENT because `fn|struct|enum <name>` cannot see an enum VARIANT, caught only because that name had been planted as a positive control. **And the same error then propagated into my SPLIT of the class, which is how a bad instrument reaches somebody else's sizing.**

**A STALE BINARY CANNOT ANSWER A QUESTION ABOUT HEAD, AND THE RUN THAT HAPPENS TO BE RIGHT IS NOT EVIDENCE.** I drove `config_undefined` against `target/release/intent`, got 6/6, and only then noticed the gate reporting that pair BEHIND HEAD. Rebuilt debug, re-drove, same verdict -- **and only the second run counts, because the first would have read IDENTICALLY had the answer changed.** The verdict surviving is not what makes the re-run necessary.

**A TOOL SCOPED TO A CRATE ACTS ON A POPULATION WIDER THAN THE ONE YOU CHOSE.** `cargo fmt -p intent-cli` swept dc's open, uncommitted `spine.rs` and `render.rs` while they were mid-edit. It changed nothing of theirs and **that was luck, not safety**: no lock, no conflict, and nothing in `git status` saying who did it -- dc confirmed their own `rustfmt --check` could not have distinguished my run from no run. **Scope fmt to the paths you touched.** Same family as ic's finding that a canon file's dirtiness does not name its author.

**DRIVE A RULING'S PREMISE BEFORE YOU BUILD ON IT.** Of vc's four build rulings today, THREE had false premises -- a guard that already existed, a test that had already landed on time, and a WP descoped three days earlier by a ruling vc had relayed themselves. **None was caught by the ruler; all three by opening the file first.** vc's own diagnosis is the keeper: **ruling from board summaries and issue text, where the tree disagreed four times out of four when somebody opened it.**

**A DEFERRAL WHOSE STATED REASON GETS STRONGER WITH TIME IS SELF-REINFORCING, AND NAMING IT AS ONE IS WHAT CONVERTS A CHORE INTO A DECISION.** The rustdoc gate waits for a class to reach zero; the class is GROWING, and the reason for waiting -- _turning this red today fails other lanes' commits_ -- gets stronger every week it is deferred, never weaker. **So the wait cannot end on its own terms.** Distinct from the scarcity register hv struck: that was a phantom deadline, this is a real cost that compounds. **The test: does the condition get easier or harder to meet by waiting? If harder, it is not a condition, it is a decision nobody has taken.** Ask the direction question, never the bigger-cleanup question.

**ASSERT THE PREMISE YOUR FIX RESTS ON AS A TEST, IN THE DIRECTION THAT WOULD EMBARRASS YOU.** I built `0206`'s CAS on `threads.revision` having READ `rebuild` and READ `Door::Change` and reasoned correctly from both -- and never read the `DELETE` two lines above, so I had it backwards: `rebuild` deletes before inserting, the conflict clause never fires, and the counter RESETS rather than rises. A CAS on it would have FAILED OPEN on the case it was built for. **The control written to stop an arm passing vacuously refuted the design it was protecting.** No care at the READING step reaches this; only an assertion does. Distinct from the plan-versus-mechanism class: there the plan hid the mechanism; here I read the mechanism and stopped one line early.

**`cargo check --workspace --all-targets` -- AND THE FLAG IS THE HALF MEMORY DROPS.** A Rust enum makes an exhaustive `match` a DISTRIBUTED obligation reported from DOWNSTREAM, so **the node who breaks it is structurally the least likely to see it**: `-p <mycrate>` was green while ic's build was on the floor, and `--workspace` misses it too because the exhaustive matches live in TEST targets. **IT IS NOT A STRICTER `--workspace`, IT IS A DIFFERENT SUBJECT** (dc's sharpening): the exhaustive matches live in test targets a plain check DOES NOT BUILD, so `--workspace` cannot see the class at any level of care. Four instances, three nodes; `error_remedies.rs` has named the exact command in its own comment throughout. **A doc that names its own instrument does not make anyone run it** (dc's). Land the variant, its const, its remedy arm and every exhaustive match in ONE edit.

**THE INSTRUMENT ANSWERED A DIFFERENT QUESTION, AND IT ARRIVES WHILE I AM BEING CAREFUL.** A selector that RESEMBLES the population is not the population. Specimens: `grep -i FAILED` matches `0 failed` in every passing summary; a `Display` grep dropped the `Debug` line holding the evidence; `pkill -f 'cargo test --workspace'` matches a pattern every session runs; an `awk '/pub const X/,/^\];/'` range never opened because the decl is bare `const`; `^  [0-9]+,$` missed rungs indented 4 and reported a tidy wrong total; a grep aimed at the wrong directory read as _the thing is gone_. Also: `--help` renders for a DECLARED command whether or not an arm exists, and `bash -n` answers _is this valid bash_, never _is this the file I meant_. **RULES: an EMPTY result is not a finding until the pattern is positive-controlled; assert a MEMBER you know is in the population, never just a plausible total; after any programmatic edit, grep for what should now be there.** The common shape: the instrument is checked against what I MEANT rather than a sample of what it will SEE.

**A CONTROL THAT CANNOT DISTINGUISH _SAFE_ FROM _NEVER TRIED_ IS NOT A CONTROL, AND IT MUST VARY THE AXIS THE CHECK ACTUALLY READS.** A no-mutation probe whose two shasums are both EMPTY has no subject. **The sharpest instance cost me a false exoneration:** I attributed 23 `intent-cli` failures to _not mine_ because stashing my paths reproduced them -- but `git stash` REWRITES the file, so the mtime stayed fresh, and the guard keys on MTIME not content (`common/mod.rs:782`). My arms differed only in the dimension the guard IGNORES. `cargo build -p intentd` then re-run: 461/0. **Ask what the check READS before choosing what to vary.** State the bound's DIRECTION too.

**THE CORPUS IS THE CLAIM; THE VERDICT IS NOT.** Beside the verdict, in the OUTPUT and never a comment: **POPULATION** (the set examined, derivation named), **FORMS** (per-form counts, never one total), **REACH** (`COVERS` / `DOES NOT` / `UNOWNED`).

**A VERIFICATION SCOPED TO THE THING CHANGED IS NOT A VERIFICATION** (dc's). The blast radius of a published value is every crate that PINS it, not the crate that DEFINES it -- `grep -rn <constant> --include='*.rs'` finds that corpus in a second. Unlike a bad grep, this one LOOKS like diligence.

**A MEASUREMENT TAKEN ON THE SLOW PATH IS NOT EVIDENCE ABOUT THE FAST ONE.** My millisecond-resolution hypothesis for `updated_at` DID NOT REPRODUCE and is not evidence against it; what survives is a design argument, because my gap was dominated by process startup and says nothing about the daemon path where both writes share one process.

**A REPRODUCTION PROFILE IS A DIAGNOSIS.** _Needs the full workspace, invisible with the subsystem alone_ says the cause is not in the code under test but in what SHARES ITS PROCESS -- which is what put me on `fork`, `flock` and `O_CLOEXEC`.

**THE SHELL LIES ABOUT SUCCESS ON THIS BOX.** `cmd | head` reads head's status -- done twice in one day, both read as 0. zsh has no `PIPESTATUS`, does NOT word-split `$var`, and an unmatched glob ABORTS the command. `grep` is `ugrep`. `sed -i ''` SUCCEEDS on no match. `find` is bfs, and `find .` walks `target/` until it times out. **cwd persists between calls** (caught again today). `2>/dev/null` on a source printing `command not found` returns an empty lookup that reads as data.

**THE SHARED CHECKOUT.** Canon cannot be split, so every canon commit is silently multi-node: `ST0056.json` names the STORE's union of every dirty-and-ingested attachment, so a node holding a dirty file under `parity/tools/` blocks every other node's canon commit on that thread. **`add` + `commit --only` is the only safe write; a live `index.lock` is a WAIT, never a removal; never `--no-verify`; never unstage a peer's path. PROVENANCE IS CARRIED BY PATH.** The compile is a shared resource on the same footing -- see the `--all-targets` entry.

**A GREEN THAT SOMEONE ELSE REPORTS RED IS A WINDOW, NOT A DISAGREEMENT.** ic measured two `schema_command` failures real when taken and gone when read -- they fell between my DDL edit and the deliberate re-bless. The generated face means a `store.rs` defect surfaces in intent-cli's suite, **so the package boundary is not the blame boundary in either direction.** Re-drive and compare TIMES, not verdicts.

**TWO CORRECT RULES CAN TAKE THE MACHINE DOWN.** A release build DELETES the shared pair before building (`0196`) and the shared-artefact guard REDIRECTS a dirty-tree build to `target/private/`: the delete happened, the replace declined, `intent` was gone estate-wide for fifty minutes. The guard prints the dangling symlinks BEFORE the delete, so the information exists and the ORDERING defeats it. **0196 now also carries lamplight-cc's half: the SUCCESSFUL build has the same window and it reaches OTHER REPOSITORIES** -- six verbs in one script, verb 1 wrote, 2-6 died on `command not found`, `set -e` did not catch it, half-written canon in another project. Remedy recorded: build to a staging target and `mv` in, since a rename is atomic. **AND THE CONSTRAINT THIS IMPLIES IS A QUIET TREE, NOT A PERMISSION** (vc's framing, taken 2026-09-02): `0196` is a DEFECT, not an authority rule, and _a rebuild window is hv's_ has EXPIRED -- ic has rebuilt twice in a day without hv. What a rebuild actually wants is that a failure would not leave live sessions without a binary. **My own line named the hazard and never said what it demands of the operator, which is the half a reader needs.**

**A PIN THAT TRAILS HEAD IS THE CORRECT STEADY STATE, NOT STALENESS** (dc's) -- and **CURRENCY IS A PROPERTY, NEVER A VALUE.** It was current at 16:23Z and behind by 16:39Z with nobody doing anything wrong. `git rev-list -1 HEAD -- native/rust surface docs/design` equal to the pin means CURRENT; **the gate's `currency` arm is the decider and the bare pin/HEAD difference is not.** I carried "BEHIND THE TREE" in five messages after it stopped being true. **DIRT, NOT STALENESS, IS WHAT BLOCKS A SHARED REBUILD.** `DIRT_SCOPE` has TWO HOMES BY DESIGN (`source_commit.rs:174` marker, `sharedtarget.lib:124` guard); the guard must stay a SUPERSET and **arm 6 asserts CONTAINMENT and must NOT be tightened to equality** -- today they are EQUAL, so an equality check passes now and only fails the next legitimate widening. Containment is what permitted `docs/design` (AC-08.9, `intentd` embeds the logo).

**A LADDER DECLARED AS DATA IS READ FROM THE DATA.** `git log -S` counts occurrences and a value change alters no count, so it cannot count rungs; `MIGRATIONS: &[(i32, &str)]` enumerates them. **General form: before going to history for a number, ask whether the thing declares itself.**

**A MESSAGE CORRECTING SOMEONE'S STATE IS ITSELF A STATE CLAIM.** dc re-drove vc's reading and in the same message told me my board carried a claim it never had -- the re-drive reached the SUBJECT and not the claims about the RECIPIENT. Read this before writing the next correction to a peer.

**A SILENT CORRECT DECLINE IS INDISTINGUISHABLE FROM AN ASSUMPTION** (dc's). Three nodes each DECLINING to drive a claim is correct; three each ASSUMING a fourth did is the accident, and **nothing separates them except whether someone recorded the decline.**

**AN OPEN ESCALATION AGES INTO AN ASSERTION** (vc). **Before anyone budgets against an escalation, its AUTHOR re-drives it.** Same family: **CONSTRUCTING BEATS READING, MEASURING BEATS BOTH, AND THE DISPROOF IS USUALLY ALREADY IN HAND.**

**A REFUSAL THAT NAMES NO ARTEFACT SENDS THE OPERATOR TO GUESS.** Name the FILE, name both VALUES, and point at `--version` for the build rather than inlining a commit that has exactly one home. Fixed for both schema refusals 2026-09-02. **A DOC COMMENT IN THE PRESENT TENSE ABOUT A MECHANISM THAT DOES NOT EXIST IS INDISTINGUISHABLE FROM ONE DESCRIBING A MECHANISM THAT DOES** -- nothing goes red and the reader finds a name and stops looking (`0214`, third instance this month).

**FACTS THAT LOOK LIKE OPINIONS.** `intent st attach` writes the store AND the canon extract and NEVER the disk file (`0082`); the daemon watches DISK, so a disk edit converges everything and a store-side write renders nothing back. A thread must be REALISED or it has no cover. `intent ac show` is not a verb. `intent init --name` is not a flag. **`intent issues add` and `issues edit` write ONLY the issue's own extract** -- a narrow canon window, not the wide one both vc and I were sequencing around. **TWO `--version` OUTPUTS THAT LOOK IDENTICAL CAN BE TWO DIFFERENT QUANTITIES**: `intent --version` names the commit the BINARY WAS BUILT FROM, `bin/devbin --version` names what the CHECKOUT IS AT NOW. Do not diff them; ask the currency question.

**AN INSTRUCTION IS WRITTEN AGAINST A SURFACE ITS AUTHOR MAY NOT HAVE RE-READ -- CHECK THE SURFACE BEFORE EXECUTING, NOT AFTER.** D3 is the specimen: the ruling was SOUND and the carrier did not exist -- `at edit` has no name field and `legacy.rs:1965` refuses both spellings as VALUES, and the scoping would have had me parse-and-rewrite `intent/.canon/st/*.json`, **a GENERATED PROJECTION whose SSOT is the store (D01)**: 187 rows, a clean diff, and the store never touched. **A DIFF THAT LOOKS RIGHT IS NOT EVIDENCE THE WRITE REACHED THE SSOT.** hv withdrew D3 entirely on a DOMINANCE argument -- a file merge breaks a path, a rename breaks a name, and the id inside the test survives both -- **so reach for dominance before competing assertions.** General form: **drive the mechanism's own `--help` and model before sizing anything against it**, and prefer the ADDITIVE carrier (`Option` + `skip_serializing_if`) over relaxing a grammar.

## Decisions

- (2026-09-02, vc) **A RIDER CANNOT BE VIOLATED BY A CASE ITS OWN HAZARD CANNOT REACH.** `AC-06.1`'s ordering clause exists to stop a test being written to MATCH BUILT BEHAVIOUR; a test written against an UNWIRED family, pinning rc=2 on six arms, cannot be matching built behaviour because there is nothing there to match. D35 naming `intent config` as the reader of five keys is a REQUIREMENT on config, not a DESIGN of it.
- (2026-09-02, vc) **RULING 3 WITHDRAWN: `WP-14` AND ALL 12 OF ITS ACs WERE DESCOPED WHOLE TO ST0069** by hv on 2026-08-30, `by: "hv via vc"`. 25 rows across exactly three families -- 13, 14, 16. `AC-09.5`'s wip/boards half goes with it: the CLI read that would restore it IS `intent wb`.
- (2026-09-02, vc) **`AC-10.5`'s RESIDUE IS RE-DISPOSITIONED AS _MODELLED IN ST0069, NOT IN THIS RELEASE_.** D30's ground was that naming the 110 files would zero the loss by asserting abandonment; **the descope supplies the honest third option D30 did not have** -- the carrying moved rather than being dropped, so naming them as modelled-elsewhere is TRUE.
- (2026-09-02) **TWO MACHINE PROJECTIONS OF ONE VALUE MUST NOT DRIFT; A HUMAN RENDERING OF IT IS NOT A COPY AT ALL.** `outcome_json` is hoisted beside `Outcome` and shared by the MCP and wire faces, which answer the same question. `render.rs` turns the same notes into sentences with remedies -- a DIFFERENT question, so folding it in would merge two concerns to satisfy a rule that was never about it. **Highlander asks whether two copies would drift into disagreeing about the same question** (vc's test, ratified).

- (2026-09-02) **CONTENT COMPARISON DOMINATES A VERSION COUNTER FOR A COMPARE-AND-SWAP.** It does not fire when nothing was lost, fires whenever something would be, and is right about ABA where a counter is wrong. `Thread` derives `PartialEq`, so it enumerates nothing -- which is what killed my own hand-maintained-population objection. **A dominance argument ends a design debate that competing assertions cannot** (second time in two days).
- (2026-09-02, vc) **A COLUMN WITH NO READER SAYS SO IN ITS OWN COMMENT.** `threads.revision` is retained because dropping it costs an irreversible rung; silence would send the next reader hunting for a consumer that does not exist. Not _the CAS token_ -- it is not one.
- (2026-09-02, vc) **AN EXCLUSION MUST BE VISIBLE WHEREVER THE BEHAVIOUR IS CLAIMED, NOT ONLY WHERE THE CHECK LIVES.** `0206` refuses concurrent WRITES and not DELETIONS; any row or issue asserting the general form says _writes, not deletions_ in the same sentence, or the exclusion re-enters through the artefact that failed to name it.
- (2026-09-01) **hv: v3.0.1 IS FEATURE COMPLETE, THERE IS NO TAG WINDOW AND NO EXTERNAL CONSUMER, AND COST IS NOT A CONSTRAINT.** The SCARCITY REGISTER is retired AS A CLASS. **A recommendation resting on _there is not time_ must be re-derived on its merits or withdrawn.** The release-number question is CLOSED and is not to be put again.
- (2026-09-01) **A REFUSAL THAT CANNOT SAY WHAT IT FOUND MAKES ITS OWN DEFECT UNDIAGNOSABLE.** Both `AlreadyRunning` branches rendered identically, so the first move was making them distinguishable rather than editing the code under suspicion.
- (2026-09-01) **A REMEDY INHERITS ITS BRANCH'S ASYMMETRY.** Confirm-before-refuse is SAFE on the lock and WRONG on the probe, where confirming makes the DESTRUCTIVE outcome likelier. **I concluded it was branch-independent and testing the claim is what stopped it shipping.**
- (2026-09-01) **EMBEDDED AND UNPRINTED IS THE WORST OF THE THREE STATES.** Absent would have been honest.
- (2026-08-31) **A CRITERION IS NOT REWORDED TO WHAT THE CURRENT STATE SATISFIES** (vc). The ruled form must be HARDER and bind the node holding the divergence.
- (2026-08-31) **A CLASS CHANGE WITHOUT ITS REASON IS A DELETION WEARING A NEW LABEL** (vc); `deviate` must RETARGET, recorded where the artefact is GENERATED FROM. **A DECLARED EXCLUSION CARRYING ITS REASON IS THE CURE; A SILENT DROP IS THE DENOMINATOR ATTACK.**
- (2026-08-31) **AN OWNERSHIP SPLIT IS A PURPOSE, NOT A BOUNDARY** (ic, correcting me). Reverting so a peer can rebuild byte-identical code is churn wearing Highlander's clothes -- **and two hands in one file IS `0206` in miniature**, which is why I declined to write dc's variant semantics for them today.
- (2026-08-31, CORRECTED) **`close --note` STAYS RULED OUT ON A CONTINGENT FOOTING.** The keg has no `edit`; the ruling survives because v3.0.1 ships from the tree. Re-drive the two 3.0.0 artefacts rather than quoting them.
