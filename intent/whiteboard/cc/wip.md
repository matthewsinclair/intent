---
node: cc
name: Control Claude
role: control
session_id: 32dc8880-9739-45ef-a496-70118b1d259b
heartbeat_at: 2026-08-20 14:57Z
status: active
focus: "**FOLD 7. `intent upgrade` WAS DESTROYING 95% OF THE SSOT AND SAYING `ok:` -- found, ruled, fixed and green at `c73404c7`** (AC-10.14/AT-10.14, minted by vc off the finding). Index and store both went 57 -> 3 on a fresh clone; both now hold 57 while the disk still realises the 3 the manifest declares. **The instrument finding is bigger than the defect: a gitignored SSOT is invisible to `git status`, so a diff-based blast radius under-reports damage to exactly the artefact the model calls authoritative -- and reads as complete every time.** Also today: AT-06.8 built and un-parked, `doctor` gained the status-vs-gate arm hv ratified five days ago, D55 closed at zero, AC-01.5 driven. **Next: WP-10's remaining to-write tests.**"
claims: [ST0056/06, ST0056/10, ST0057/00, ST0057/01, ST0057/03]
---

# Control Claude (cc)

## THE MODEL -- canon, hv ratified

**D01 IS REVERSED: THE DB IS THE SSOT AND THE FILES ARE RE-CREATABLE.** Never cite the old "committed JSON durable / DB rebuildable / `rm` safe" wording; it is VOID. **D34: the committed extract is the interchange -- it TRAVELS while the DB never does.** D29: a gitignored path is never canon. **D54 (hv, 2026-08-20): Route A gives `intent/docs`, `intent/llm`, `intent/eng` a third sigil; Route B gives `wip/restart/done` the `todo.md` project-view mechanism; `history/`, `autopsy/`, `analysis/` stay out; the tool's 187 NEVER enter a project store.** Verified to partition my B class exactly: 33 + 3 + 23 = 59.

**D42 -- TIME, and it has no clauses.** `date -u +'%Y-%m-%d %H:%MZ'`, read in its own step, trailing `Z`. **I fabricated FOUR stamps on 2026-08-20 while carrying this in bold** -- one running backwards against a stamp I had already sent, one thirteen minutes in the future. **The clock guard reads header blocks and inboxes; those were in MESSAGE BODIES, on a channel with no guard.** The mechanism, since care is not one: **the stamp enters the message from the same shell command that reads it, or the message carries a revision and no time.**

## NEXT -- mine

**STATE at `c73404c7`. Today's narrative is in `.history/20260820/`; this is only what is still owed.**

**1. ST0056/WP-10 IS THE OPEN BLOCK AND IT IS MINE ALONE.** AT-10.12 and AT-10.14 are done. **Four `to-write` tests remain**: `migrate_refusal.rs` (AC-10.2, Phase A residue BLOCKED with the per-line classed report, atomic, nothing written), `migrate_v2_project.rs` (10.3, clean estate converts in one commit), `migrate_hooks_continuity.rs` (10.4, `.claude/settings.json` + `scripts/**` byte-identical pre/post, asserted not assumed), `egest_estate.rs` (10.8, egest symmetry with the out-of-model set NAMED rather than silently absent). **Plus AC-10.5 `fleet_corpus_ingest.rs`, which needs Lamplight/Utilz/Baize at named revisions and is the only one with an external dependency**, and AC-10.6's canary rollback which is `n/a` and wants DOING rather than writing.

**2. AC-01.5 IS DRIVEN AND STAYS RED, AND THE REMEDY IS dc's.** Five arms, `e79991fc`. The guard is correct and IS dispatched here; it is absent from every clone because `.githooks/pre-commit.intent` is gitignored by design. **vc refused green-with-an-exception on grounds better than my question: this row's subject is a gate that is not reached, so an exception converts that failure into a permission.**

**3. AT-06.8 IS UN-PARKED AND GREEN AT `7c584381`, WITH FIVE INHERITED FLAGS RATCHETED.** `st bootstrap --audit-only`/`--dry-run`/`--deliverable` are dc's when `bootstrap` lands; `claude subagents -v` and `claude skills -v` are ic's surface. **The ratchet is EQUALITY, so wiring any one reds the test and forces the list to shrink.** Two false negatives named in the file and unverified: `ac descope --by`, `critic --rules`.

**4. AC-03.6 AND WP-03: NOTHING UNBLOCKED OF MINE.** `--staged` is proven firing; admission is dc's and they want to rule once. WP-03 is four-of-four built with AC-03.6 its only open row.

**5. D54 ROUTE A IS JOINT WITH dc AND ITS MECHANISM HALF IS DELIVERED.** 11 sigil sites across 5 files; search is a call site (`prose::split` already takes `owner_type`); canon is a new model type; identity is MINTED (dc's ruling -- a path-derived id cannot survive dehydration, which makes identity a fact about the projection). **The minter is COPIED, not invented**: `Facade::next_thread_id` plus five identity functions in `model`, a pattern instantiated two and a half times. What canon stores is vc's; the verb surface is dc's.

**NOT MINE: the hooks, the roster and `int hooks` are dc's. `Sigil` and the issue verbs are ic's. The contract and WP-close are vc's.**

## Watch-outs -- the live set

**Folded 14:54Z on 2026-08-20. The day's incident detail is in `.history/20260820/`; these are the rules that survived it.**

**THE PROVENANCE-OF-EVIDENCE RULE, THREE LIMBS** (vc, from two instances I produced on myself inside one hour): **neither the INSTANCE, nor the CONTROL, nor the PREDICATE may be drawn from the thing under test.** An instrument that borrows a LIVE defect has made the defect a fixture and the estate is not free to fix it. A CONTROL drawn from the instrument's own enumeration confirms the enumeration -- my flag scanner's five pinned ids all came from the accessor shapes I had already listed as its markers, one list wearing two hats. A PREDICATE written around the instances in front of you is a list wearing a filter's syntax. **All three produce a green that proves nothing and all three look identical to a green that proves something.**

**MARK PROVENANCE PER CLAIM, NOT PER MESSAGE: DRIVEN, READ, OR INFERRED** (dc). I sent four claims in one voice saying I had _confirmed each in code_; three were true. **The cost lands on the READER, which is exactly why the writer never feels it.**

**A GITIGNORED SSOT IS INVISIBLE TO `git status`, SO A DIFF-BASED BLAST RADIUS SYSTEMATICALLY UNDER-REPORTS DAMAGE TO EXACTLY THE ARTEFACT THE MODEL CALLS AUTHORITATIVE.** Two files changed, said git; 54 SSOT rows went, said nobody. **Every one of us reaches for `git status` to answer _what did that change_, and under D01-reversed it is structurally blind in the one direction that counts while reading as a complete answer.** vc's correction to how I filed it is load-bearing: **this is not the carelessness class.** Reading the wrong SUBJECT is carelessness; this is the right subject read with an instrument that cannot see part of it, and **no amount of care produces the missing half.**

**A POPULATION KNOWN TO BE EMPTY BY DESIGN WILL KEEP PRODUCING ABSENCES THAT LOOK LIKE GAPS** (vc). I read the SAME ZERO correctly one day and wrongly the next: zero opaque attachments meant _the estate denominator is 0 of 0 by construction, so the coverage must be a constructed fixture_ -- then, wearing an empty `.canon/st/<ID>/`, meant _the mechanism is missing_. **The question to put to an empty directory is not _is the thing there_ but _was this population ever non-empty_, and that is answerable from the RECORD rather than from the listing.**

**A GREEN SUITE OVER SYNTHETICS AND A REAL POPULATION OF ZERO ARE COMPATIBLE** (dc). Five green tests on a path 131 tracked files have never touched. **Not in tension with the three-limb rule: synthesise the instance AND know the live population.**

**THE WORKTREE AT A NAMED REVISION IS THE ONLY REMEDY THAT CATCHES THE `--only` CLASS MECHANICALLY.** `--only` is **path-scoped, not hunk-scoped** (dc's wording; mine named the effect and not the mechanism). _Build the whole workspace_ cannot catch it -- in the shared tree everything compiles because the missing half is sitting there uncommitted. I found a broken HEAD that way and ic verified the fix that way. **It is also how a shared source gets mutated now: `cp file /tmp/keep` ... mutate ... `cp back` is a SECOND WRITER WITH A WINDOW, and a clobbered edit is indistinguishable from an edit not yet made.**

**AN INSTRUMENT KEYED ON A CODING IDIOM DEPENDS ON NOBODY IMPROVING THE CODE.** dc's `init` loop avoided duplicating a string -- the right instinct -- and made an id invisible to a scanner keyed on literals at call sites. **One level below ST0039's _a substring standing in for a syntactic fact_: the syntactic fact was itself an accident of style.**

**A FIX THAT CHANGES A TYPE LEAVES EVERY USE OF THAT VALUE UNVERIFIED, AND THE ONES THAT STILL COMPILE ARE EXACTLY THE ONES NOTHING REPORTS** (dc). **`.ok()` ON A LOOKUP THAT CAN FAIL TWO WAYS COLLAPSES _ABSENT_ AND _WRONG_, AND ONLY ONE IS A USER'S CHOICE** -- their `--with-st0000` guard read a misspelled id and `.ok()` turned clap's `UnknownArgument`, only ever a BUG, into `None`. **A one-character fix would have left the mechanism live for the next id.**

**A RECORDED REASON TRUE WHEN WRITTEN, MADE FALSE ELSEWHERE, WITH NOTHING WATCHING THE JOIN.** Five of mine on 2026-08-20, and **AC-10.12's had its expiry in the EVIDENCE rather than the blocker**: its recorded instance is unreproducible because issues were pruned, while its property stands. **And a correction lodged one level BELOW a defect looks, at the site, exactly like a correction that covers it** (vc, on `legacy.rs:222`).

**AN INSTRUMENT'S FALSE POSITIVE THAT CONFIRMS THE STORY YOU WERE ALREADY TELLING GETS QUOTED RATHER THAN CHECKED** (vc). `st new -s` sat in canon five days as _the instance the census missed_, repeated by three nodes, about a flag read at `render.rs:587`.

**TWO POPULATIONS, OPPOSITE DEFECTS, AND A TEST FOR EITHER PASSES ON THE OTHER.** Fixing `upgrade`'s truncated MODEL by feeding the full canon to the realisation hydrated 54 threads onto a disk declaring 3. **And nothing was consulting `.intentfiles` there before, while looking correct** -- the scan's disk population happened to EQUAL the declared population. **Breaking a coincidence is how you find there was no mechanism.**

**A CLAIM CAN BE VERIFIED IN ITS SUBSTANCE AND UNVERIFIED IN ITS LOCATOR, AND THE LOCATOR IS THE PART A READER USES.** I drove content at HEAD and typed the line number off the worktree, inside the message correcting vc for exactly that. **A worktree coordinate is stale before you finish typing it** -- it moved 4194 to 4211 while we discussed it. **Quote coordinates from a revision or do not quote them.**

**VERIFY THE RETRACTION, NOT JUST THE CLAIM** (dc, applied to me). A self-correction is still a report, from the one person already wrong about that subject -- **arriving with the momentum of having just been careful.**

**`grep -c` EXITS 1 ON ZERO, IN BOTH FORMS.** The `|| echo 0` form fires the fallback on a true zero. **The PIPE form is worse: `git grep -c <fn> | tr` has nowhere for the failure to show**, printed empty for two functions with five call sites, and empty read as absent nearly reversed a correct correction. **Drop `-c` and read the lines.**

**A ZERO FROM A DATA COMMAND IS SILENT; A ZERO FROM A MISSING FILE IS LOUD.** cwd drifted twelve times in one day; one instance produced three plausible zeros and what caught it was `partition.sh` REFUSING at exit 2 on its zero denominator. **Prefer the instrument to the pipeline even when the pipeline is three lines**, and **`cargo --manifest-path <abs>` beats `cd`** -- absolute paths are a habit, `--manifest-path` is a mechanism.

**A ZERO FROM A NAME SEARCH IS A FACT ABOUT THE SEARCH** (vc). `Facade::next_thread_id` is a private method on an impl block -- **invisible to a free-function sweep at ANY name.** Running the verb finds in one command what the grep cannot find at all. **And a peer's measurement repeated is a claim of mine**: I relayed that absence to dc unchecked.

**`rustfmt --edition 2021` WAS WRONG ALL SESSION AND RIGHT BY LUCK.** The workspace is **edition 2024**; the wrong edition errors loudly on let-chains and passes silently otherwise. **Read the edition from the workspace, never from habit.**

**THE HEARTBEAT IS THE ONLY FIELD ON THIS BOARD THAT DECAYS, AND ITS AUTHOR IS THE ONE PARTY WHO CANNOT SEE IT.** **The refresh is a DIFFERENT ACT from the edit**, so a field refreshed by a different action than the one that makes it stale WILL go stale -- and the clock guard checks forward, the `Z`, and backward, none of which is currency. **A COMMIT IS A RECORD; A HEARTBEAT IS A CLAIM. Prefer the record.**

**A REVISION NAMES SOURCE, NOT THE BINARY THAT ANSWERED.** The shared `intent` is rebuilt from the union of four people's uncommitted work, so `dirty-<sha>` cannot say _plus three colleagues' half-finished features_. **Hash it with `shasum -a 256` and quote the hash WITH the number** -- an instrument that has been replaced cannot be re-interrogated.

**A PEER'S RED CONCEALS MINE AND LOOKS EXACTLY LIKE AN ALL-CLEAR**, and **a run that reports no suites is not a run that reports no failures.**

**IN A FOUR-NODE CHECKOUT, PRESENCE IDENTIFIES A FILE AND NEVER ITS AUTHOR**, and **the working tree is nobody's tree** -- name revision, clock and dirty count on every measurement.

**A CRITERION MUST CLOSE EVERY DEGREE OF FREEDOM THAT LETS A PASSING TEST COEXIST WITH THE DEFECT** (vc): **INSTRUMENT**, **DEPTH**, **EXTENT**, **PIN** (a named commit, never `HEAD`).

## Standing rulings

- **EVERY ACCEPTANCE ID IS THREAD-QUALIFIED -- `ST00NN AC-XX.Y`, NEVER BARE.** **An id resolving to a SATISFIED criterion is the worst ambiguity: it reads as DONE.**
- **THE ISSUE TRACKER IS FOR EXTERNAL USERS AGAINST A RELEASED VERSION** (hv). Everything found building v3 is work.
- **An uncarried file is NOT a disposition** (vc). **A REFUSAL IS RETIRED BY THE CHANGE THAT EXPIRES ITS REASON -- and re-deriving the reason is how you find out the reason was wrong.**
- **`treeindex` and handover RETIRE** -- a retired command is PRESENT AND REFUSING. **`doctor --fix` is WITHDRAWN. `Outcome` is deliberately NOT `#[must_use]`.**
- **v3 STAYS OFF PATH until dc repoints `~/.local/bin/intent`.** v3 answers exit 2 for 14 of 32 families and `intent claude` implements 1 of its 8 verbs against ~230 call sites in this repo's own machinery.
- **`config.json` DOES NOT MOVE WITH `intent_dir`** -- something must be findable before anything is configured.
- **HIGHLANDER FORBIDS TWO ANSWERS TO ONE QUESTION; IT DOES NOT REQUIRE ONE ANSWER TO TWO** (vc, ruling the `upgrade` fix). `legacy::scan` answers _what must be converted_; `Project::thread_ids` answers _what exists_. Both correct. **The union belongs at the join.**

## Lane boundary

`native/**` and the v3 crates are mine. `bin/**` is not vc's to edit. The parity harness is ic's; `canon_commit_check.sh` and the pre-commit roster are dc's. **Canon writes route through vc.**

**`CARGO_TARGET_DIR=<an IN-REPO dir>` FOR ANY BUILD WHOSE PURPOSE IS VERIFICATION -- BOTH HALVES.** Out-of-repo isolation breaks `INTENT_HOME`: `install::home()` walks `current_exe()` ancestors for a marker directory, so a binary outside the repo has no repo ancestor and every hook test correctly reports it cannot resolve. **vc reported 14 workspace failures to hv and SIX were phantoms of an isolated target dir.** Shared path only for a deliberate PUBLISH.

**Every commit I make touching an attachment leaves canon divergent at that commit until vc syncs, and a later sync repairs the NEXT commit and never that one** -- so sync canon FIRST, then commit the file and canon together. **And a table edit is a two-file commit whose second file is one you never edited**: `surface/dispatch-table.md` is generated from the JSON. After editing any SOURCE, ask what renders FROM it before staging.
