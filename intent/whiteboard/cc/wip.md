---
node: cc
name: Control Claude
role: control
session_id: 98a46c38-f370-4d67-b2c5-c2536e0ae8f9
commit_session_id: 0167bZhMQsEXFM5JZUZxL5g7 -- WITNESSED across this session's commits, most recently 9ea58a4b3. **READ IT WITH grep, NEVER WITH THE TRAILER PARSER:** git's %(trailers:key=Claude-Session,valueonly) and `git interpret-trailers --parse` return EMPTY on EVERY commit in this repo, because the mandated (C) line is a non-trailer line in the final paragraph and git rejects the whole paragraph as a trailer block. Positive-controlled on a synthesised body, discriminated by dropping the (C) line; dc reproduced it independently (parser 0, grep 56, 59 commits). THE WORKING READ: git log -1 --format=%B <sha> | grep -o 'session_[A-Za-z0-9]*'. POINT-IN-TIME -- read it off your own last commit, never off this line.
heartbeat_at: 2026-09-05 22:25Z
status: active
focus: "AC-00.6 UNBLOCKED (92817292; vc rewrote the row with my number and re-drove every limb) AND `0275` HALF ONE DELIVERED. The v2 coupling was ONE function, not four call sites. `rules list` now states its population, so an ABSENT rule library reads as an incomplete install rather than an empty one -- driven in a keg-shaped fixture both ways, exit code deliberately unchanged because half two is not ruled. STILL PENDING hv: 0205, 0268, 0271, 0272, the 0271 fixture question; the deletion itself is AT THE CUT. NO FIGURE HERE IS EVIDENCE; RUN THE VERBS."
claims: [ST0056/06, ST0056/10, ST0057/00]
---

# Control Claude (cc)

## DOING

**`0275` HALF ONE IS DELIVERED, AND IT IS A MESSAGE RATHER THAN AN EXIT CODE ON PURPOSE.** `intent claude rules list` printed a well-formed table header above `total: 0 rule(s)` at rc 0 with nothing on stderr when the rule library is ABSENT -- the published 3.0.0 keg's actual shape, 3 of 4 support paths missing. **The output is not empty, it is structurally healthy**, which is what makes it convincing. `install.md` nominates this verb as THE install check on the ground that it fails on exactly that packaging fault; it does not fail, it succeeds emptily, so a reader who runs it and sees exit 0 concludes they are unaffected. The count now states its population, which is what every gate instrument here does unprompted.

**IT KEYS ON THE CANON ROOT AND NOT ON "the library".** An ext pack can supply rules while the shipped tree is missing, so a caller asking _is the shipped tree here_ must not be answered with a count. New `Library::canon_root()` exposes it; `Library::files` still returns empty for an absent root, which is correct there and is exactly what made the two states indistinguishable one layer up.

**DRIVEN IN A KEG-SHAPED FIXTURE BOTH WAYS, AND MY FIRST CONTROL WAS WORTHLESS.** I set `INTENT_HOME` and got 66 rules: the binary resolves its install root by walking up from its own canonicalised path and reads exactly one environment variable, which is not that one. **So the control never reached the absent case and would have passed whatever the code did.** The real one copies the binary into a tree carrying only the `lib/templates` marker -- absent names the missing root by path, the same root with a rules tree carried in reports a plain total -- and reverting the message reds exactly one arm.

**ADDED TO ic's `an_absent_rule_library_is_visible_at_the_process_surface.rs` (renamed by them at `8a6a7c8a` on my flag; **my board cited the old name for an hour, which is a stale citation of mine inside the section where I log stale citations** -- their rename note is what corrected it, not any check of mine) RATHER THAN A NEW FILE, DELIBERATELY.** That fixture IS the keg's shape, and a second copy would be a FOURTH home for "build a fake install root" in one directory: `critic_surface.rs` and `claude_cwi_door.rs` already carry their own. **The convergence is worth doing and was not this fix's to do**, so it is reported. The file's name is now narrower than its contents for the same reason, and renaming it is ic's call.

**HALF TWO -- REFUSING WITH A NON-ZERO EXIT -- IS NOT RULED AND I HAVE NOT TOUCHED IT.** Correct on `IN-AG-NO-SILENT-001` and able to break callers, so nobody proposes it without measuring them. The arms assert rc 0 explicitly, so the scope is recorded in the test rather than in a comment somebody can drop.

**SUITES: intentsvcs 1209/1209; intent-cli 490 of 491**, the one red being the pre-existing `main` red I measured at clean HEAD -- `intent agents` succeeding at exit 0 over an estate it cannot see, byte-identical message, not mine, with vc.

## TODO -- startable, mine, smallest first

**RULED TO ME BY vc AND NOT STARTED -- `0272`'s THIRD OPTION, THE EXPENSIVE FORM.** Emit `intent/whiteboard/`'s 1,386 files **per artefact, by name**, in the residue report, with class, reason (_the coordination model is unbuilt, tracked at WP-14_) and a class total. **SIZED BY ME AT `M`, DRIVEN, AFTER vc CHALLENGED MY OWN PRECEDENT AND I WENT AND READ THE CODE.** **MY CLAIM HOLDS AND THE MECHANISM IS THE RESIDUE EMITTER, NOT A SIDE PATH:** the cap REASON is written at `project.rs:1416` into a local `refused` vec, and `legacy.rs:590` converts every pair into `Finding::new(&name, FindingClass::UnknownFileShape, reason)` and `out.record`s it -- the same `out` the residue report is built from. **So _the run can print per-path lines_ and _the residue emitter can_ are the same claim here.** vc's counter -- _a standard already implemented would not need a new class_ -- conflates mechanism with reason: the mechanism is `Finding::new(name, CLASS, reason)` and is fully general; the whiteboard needs a new CLASS because it has a different REASON. **BUT THE REAL COST IS SOMETHING NEITHER OF US HAD: `legacy::scan` NEVER TOUCHES `intent/whiteboard/`** -- the only mention in the whole file is a comment about the header format -- **so the 1,386 artefacts are not in the scanned population at all and the walk has to be added.** That is what makes it `M` and not `S`. **DESIGN POINT NOW RULED (vc under hv's pen, 2026-09-05 20:39Z), AND vc's REASON IS STRONGER THAN MINE -- I DROVE IT RATHER THAN TOOK IT: `tests/the_migrator_says_what_it_did_not_carry.rs:42` ITERATES `NOT_YET_BUILT` AND ASSERTS EACH MEMBER'S `justified_by` APPEARS IN THE DOCUMENT**, so enumerating at the declaration INHERITS a contract already binding the reason to the doc -- which is exactly `AC-10.5`'s _the namer is the migration, not the check_, and exactly what keeps an exclusion DECLARED rather than drifting silent. Mine was only the negative half: **do NOT pull the whiteboard into `legacy::scan`'s population**, where other scan logic may treat it as thread files; enumerate at the `NOT_YET_BUILT` declaration, which `sync.rs` already argues is the one home, and record into the same `out`. **The shape already exists in the same run** -- eight oversized screenshots are excluded individually with class, reason and measured byte count. **A new `FindingClass` variant needs a row in `migration.md`'s class table**, and `residue_class_check.sh` compares table against implementation in BOTH directions, so the table is not optional. **This takes WP-14 out of the 3.0.1 cut IF hv confirms; vc is reporting it and one word reverses it, so WP-14 is NOT out until then.**

**`0271`'s FIXTURE QUESTION IS RULED -- FIXTURES BECOME GIT REPOS, AN `L`, vc under hv's pen, 2026-09-05 20:34Z. NOT STARTED, AND DELIBERATELY NOT STARTED BEFORE A COMPACT:** one place in `tests/common/mod.rs` changing fixture behaviour for ~1200 tests is not work to open minutes before a context reset, and I have just reverted `native/rust` precisely to leave the shared tree usable. Re-apply `.history/20260905/0271-preconditions-2028Z.patch` first. **vc IS REPORTING THE `L` TO hv AS RULED RATHER THAN ASKED, SO ONE WORD REVERSES IT** -- do not treat the fixture change as authorised until that lands.

**ALSO RULED TO ME (`0271`), THE IN-FLIGHT BUILD ABOVE:** git arm (additive, refuses 0 of 18), dirty arm SCOPED TO THE CONVERSION PATH and never to the verb. **`migration.md`'s Preconditions section is edited in the SAME change** -- three documented refusals and one implemented is a defect whichever disposition wins, and the dirty clause must say it gates the conversion, not the verb.

**`0270` OPTION 2 IS RULED TO ME AND dc:** guard the forward step so `at red` refuses or warns on an absent citation. **A guard defends every row; a note defends one until someone edits it.**

**OLDER, STILL LIVE:**

- **M** `0192` RULED IN -- refusal in `info_read_back`, placement decided.
- **S-M** `SERVED_BY_DAEMON` (`render.rs:235`) is ONE entry **and a `const`, so it can only ever hold PAYLOAD-FREE variants.** Project the dispatch table's payload-free arms, and **REFUSE on a daemon-eligible arm carrying arguments rather than skip it** -- silence there reports a clean table while a verb quietly loses daemon coverage.

## Holds -- mine, with the condition that releases each

- **RELEASED 2026-09-05 17:26Z, KEPT VISIBLE FOR ONE BOUNCE RATHER THAN ARCHIVED** (vc's convention, and it is my own silent-exclusion argument pointed at the good outcome: **a hold that vanishes the moment it is released is indistinguishable from one quietly dropped**). **`ST0056/10` BEHAVIOURAL HALF IS GO.** dc's staging fix landed, ran and PROMOTED: `intent` and `intentd` now BOTH report `da5919e8f` and the daemon was restarted with it. **DRIVEN BY ME, NOT TAKEN FROM vc.** **AND THE COUNT WAS NEVER THE RIGHT INSTRUMENT.** My own check was `rev-list --count <marker>..HEAD`, which now says **25** and would have left me judging a threshold by feel. **The question is whether the binary reflects the code a behavioural claim would exercise, so the discriminating form is PATH-FILTERED: `git rev-list --count <marker>..HEAD -- native/rust surface` -> 0.** Twenty-five whiteboard, canon and doc commits; not one build input. **`0216`'s write hazard is also gone with it, because the daemon is no longer the stale half of a mismatched pair.**

- **M** `AC-06.1`'s coverage half -- RELEASED WHEN a burn TSV **covering the estate** exists AND `INTENT_BIN` resolves to one binary. **Re-driven 16:00Z, unmet:** a burn TSV EXISTS (`parity/tools/burn-baseline.tsv`) and **existence is not coverage**, which is the distinction this hold keeps. `INTENT_BIN` unset; four `intent` binaries reachable. `coverage_map.sh` refuses to publish and is RIGHT to.
- **L** `0216`/`0226` fix -- RELEASED WHEN a monotonic version the ingest does not own exists. The obvious fix collides with `written_at`, which the ingest rewrites wholesale.

## Decisions owed by hv -- question, options, recommendation

**TWO CAME OFF THIS LIST BY vc TAKING THEM INSIDE THE PEN** (`at green`'s L3 arm and the rustdoc gate: exit-code and lint posture is tooling, not hv's). **THREE MORE WERE STRUCK OR WITHDRAWN BECAUSE THE PREMISE FAILED, NOT BECAUSE THEY WERE ANSWERED.**

- **`INTENT_BIN` flip and re-baseline -- which order?** (i) flip then re-baseline (ii) re-baseline then flip. **REC (i).**
- **`burn.sh` re-run, or accept `AC-06.1`'s coverage half red?** (i) run (ii) accept red on the row (iii) descope. **REC (i), and it is hv's because full-suite runs are.**
- **Close `0205`?** DELIVERED AND RE-DRIVEN TODAY. All three of our blocks now hold the safe `$(cat <<'EOF'` form; the repo-wide sweep, **both halves**, finds **zero** unsafe blocks in `*.sh` and exactly one outside it -- `bin/.devbin/lib/builtins:66`, the vendored block, **ACCEPTED as out of scope** on the recorded reason. **The zero is controlled three ways** -- a planted block found, the safe form correctly not flagged, and (the good one, and it was luck) my scratch v2 tree at a pinned revision still carries all three in their PRE-FIX form and the sweep found all three there, which is the pattern demonstrated against the real historical subjects. (i) close as fixed (ii) leave open on the vendored block. **REC (i)** -- the vendored block is somebody else's repository and holding our issue open for it never closes. **AND THE ISSUE'S OWN EVIDENCE HAD EXPIRED SILENTLY:** it quotes a manifest/disk hash pair as proof the vendored file is pristine, and devbin has been re-vendored since, so both values moved together. The property still holds; anyone re-verifying against the recorded value would read a tamper. Corrected in the body, with the verb that re-takes it.
- **Close `0175`?** Fixed at `99a66928f`, driven, 76 tests green, committed still OPEN. **REC: close as fixed.** One word.

## Open, no owner

- **`--all`: THE SEAM SURVIVES THE FIX, WHICH IS THE ONLY REASON THIS ROW IS STILL HERE.** The flag works on the delivered pair -- both families, both verbs, 32 files installed then 0 after uninstall. **But burn asks _does this reach the v2 CLI_ and `fixture_probe.sh` asks _does this hardcode a v2 PATH_; NEITHER ASKS WHETHER THE ARGV STILL PARSES**, so a future argv regression is invisible to both, and dc's static arm covers canon-vs-table and **still cannot reach `uninstall --all`, which canon never names.** Kept for the seam, not the flag.
- **Something WALKS the CLI surface.** Seven CLI-token-titled creations in `event_log`, two episodes eight days apart. **This is `0223`'s -- re-read it, do not restate from here.** **Episode 3 checked 2026-09-04: none.** **THAT SETTLES NOTHING** -- two episodes is ONE interval, one interval is not a period, and 1.1 days had elapsed against an 8.24-day gap. **The monitor is one query, so re-run it rather than reasoning about it.**
- **Is my unfiled daemon-lock race a duplicate of `0210`?** Adjacent ground, NOT compared.
- **Does one fix serve both `0216` and `0226`?** Same collision, opposite symptoms. Not driven.
- **The roster guard says _N parity file(s) in this commit_ and means the WORKING TREE.** Wording defect, unfiled.

## Watch-outs

**Rules only. Every worked example is in `.history/20260904/wip-prefold-1741Z.md` and `.history/20260903/wip-prefold-1718Z.md`.**
**A. THE INSTRUMENT ANSWERED A QUESTION ADJACENT TO THE ONE ASKED, AND ANSWERED IT CORRECTLY.** The dominant family; it arrives while I am being careful. **RULE: name the FIELD you read and the INSTRUMENT you read it with.** **REFINED by dc: the old form said flatly that `--help` is about the PLAN, and that is FALSE when help is derived from the parse source** (`spine.rs:624` declares `entry.flags` onto clap from a table compiled in by `include_str!`, so the declaration answering `--help` is the one that rejects the argument). **Not _distrust help_ but _establish what help is derived from_.** A declared row remains a statement about the plan.
**A2. FABRICATION WITH THE CORRECT VALUE PRESENT. SECOND TIME TODAY, IN THE COMMIT THAT ADDED A FAMILY ABOUT NOT DOING THIS.** I stamped `22:20Z` while the `date -u` in the SAME tool output read `22:17Z` -- typed by feel, three minutes out, in a heartbeat field whose whole contract is that it came off a clock. **The first instance this morning was 19:22Z against 20:20Z and I corrected it from a fresh read; this one is smaller, which is worse, because three minutes is inside the range a reader would never question.** Corrected from the read in front of me rather than by inventing a better-looking value. **THE TELL IS THE ORDER OF OPERATIONS: I read the clock, then wrote several hundred words, then typed the stamp from memory.** The cure is mechanical and is in `E`: the value goes in through a placeholder plus `sed`, or as a variable read in the SAME command that writes it -- never through my hands. TWICE TODAY.** A sha typed wrong with the real one four lines up in my own output; then **NINE DAYS OLD written into this board and two messages in the same call where I had echoed `rel binary mtime: 2026-09-04 15:33Z` myself.** **It is not a fact about clocks -- it is a fact about any opaque token a reader cannot check by looking**: shas, pids, issue numbers, line numbers, dates. **RULE: substitute the command, never the value.** And a **date travelling ATTACHED to a sha inherits the sha's authority and none of its checkability**.
**B. CONTROLS, OR THE READING IS NOT EVIDENCE (absorbs B5).** A control that cannot distinguish _safe_ from _never tried_ is not a control, and it must vary the axis the check reads. **An instrument inside its own population is CONFIRMATORY, not merely noisy** (`Op::Registry` is in `wire::UNCOUNTED` for this reason). **RULE 1: positive-control the instrument before its silence means anything.** **RULE 2, AND IT IS THE ONE THAT KEEPS GETTING ME: A CONTROL THAT EXERCISES ONE STAGE OF THE INSTRUMENT CERTIFIES ONLY THAT STAGE.** Twice on 2026-09-05, one layer apart: `froze|frozen` could not match _freeze_ and my control tested the jq path-walk; then a duplicate-AT scan globbed `intent/st/*/acceptance.md` -- **13 files of 156**, the offenders sitting in `COMPLETED/` because v2 expresses status as a DIRECTORY -- and **my control passed loudly, 320 AT rows matched**, having tested whether the PATTERN could match and never whether the FILE SET held the subject. **A control must be able to FAIL for the reason the measurement would be wrong. Before trusting a zero, state the DENOMINATOR out loud and take it a second way** -- `find` said 156 where the glob said 13, and one `find` was the whole cure. Both times the tool's own output corrected me, which is not a mechanism I can rely on. **AND THE THIRD, WHERE I HELD THE INSTRUMENT AND CAUGHT IT MYSELF:** to drive `0275`'s absent-library case I set `INTENT_HOME` at a rules-free fake root and got **66 rules back**. The binary walks up from its own canonicalised path for the `lib/templates` marker and reads exactly one environment variable, which is not that one -- **so my "absent" case was the PRESENT case wearing a different env var, and it would have passed whatever the code did.** The cure was the same one: the real control copies the binary INTO a tree carrying only the marker, so the two runs differ in exactly the thing under test. **I caught it only because I read the NUMBER rather than the verdict** -- rc was 0 either way, and 0 was the expected answer. **RULE 3: when a control passes, look at what it PRINTED, not at whether it passed.**
**B2. A DEFECT THAT IS THE DISAGREEMENT BETWEEN TWO INSTRUMENTS' POPULATIONS IS INVISIBLE TO BOTH, AND NO PREDICATE FIX REACHES IT (dc).** `implemented_check.sh` probes what the table DECLARES; `canon_mandated_verbs_check.sh` probes what canon NAMES; `--all` is in the gap. **Neither instrument is bad and neither population is wrong -- the PAIR has a seam and the defect IS the seam.** **RULE: ask what neither population contains, and remedy with a STATIC comparison of the two populations, never a better predicate in either.** **AND: read an instrument's STATED SCOPE before believing its silence** -- `pertest.md`'s zero rows mean _not pending_ (41 of 113), never _not covered_.
**B3. AN ASSUMED POPULATION -- INFERRING A PEER'S METHOD RATHER THAN READING IT. TWICE TODAY, BOTH DIRECTIONS, BOTH WITH dc.** **RULE: a correction to a peer's FIGURE requires reading their PATTERN, and a claim about what an instrument over-matches is settled by a negative control, never by reasoning about the regex.**
**C. ARITHMETIC AND SAMPLE SIZE.** **n=2 is not a result about a stochastic process.** **A total you did not enumerate is not a total you may publish.** **RULE: state n and state the variance; below n=5 on a process you have watched vary, the honest sentence is _observed twice, not characterised_.**
**D. PREMISES.** **Drive a ruling's premise before building on it** -- of four build rulings in one day, THREE had false premises. **Assert the premise your fix rests on as a test, in the direction that would embarrass you.**
**D2. A MEASUREMENT IS RE-TAKEN AT THE MOMENT IT IS QUOTED, AND THE RELAY IS WHERE IT ROTS.** **The tell is a possessive tense:** _the daemon IS stale_ is a claim about now sourced from a reading about then, and nothing in the sentence marks the gap. **RULE: a figure crossing into a message gets re-driven in the turn that sends it.**
**D3. AN OBJECTION HAS A PREMISE TOO, AND MINE GO UNDRIVEN BECAUSE THEY FEEL LIKE CAUTION.** Twice today I reasoned from D56's PRINCIPLE against a §10a DETAIL I had not read, with ic having read it both times -- and the second time the refuting text was **a doc comment I wrote myself** (`intentd/src/web.rs:28-37`). **The tell is that an objection does not feel like a claim; it feels like diligence, so its premise is never stated and therefore never driven.** **RULE: before an objection leaves this session, name its premise as a sentence and drive THAT.** When the premise is _the design does not permit X_, the drive is opening the design document.
**D5. AN EXCULPATORY CLAIM CARRIES THE SAME BURDEN AS AN ACCUSATORY ONE, AND THE TELL IS THAT YOU RAN NOTHING (ic, against themselves).** ic argued my duplicate filing was not my fault -- that the search verb was broken on the query that would have found the row -- **asserted without driving it, in the same message where they corrected me on something they HAD driven.** Their diagnosis: _my evidence bar dropped because the claim was in your favour; a generous claim does not feel like a claim, so nothing prompted the drive._ **THE COUNTERFACTUAL KILLED IT IN ONE COMMAND** -- six subject-shaped queries all find the row -- **which is exactly what the assertion should have cost.** **THIS IS D3's MIRROR AND THEY BELONG TOGETHER:** an objection escapes its evidence bar by feeling like caution; a generosity escapes it by feeling like fairness. **RULE: a claim that does not feel like a claim is the one to drive.** **AND IT CUT BOTH WAYS -- their wrong argument made me test a sentence in my OWN filed issue, which was also an overclaim, and I retracted it.**
**D4. _I DECLINED TO MEASURE BECAUSE OF SIDE EFFECTS_ IS ONLY HONEST IF I CHECKED WHETHER THEY WERE CONTAINABLE.** Unchecked, restraint produces an unmeasured claim wearing the costume of rigour -- and I then reasoned forward from the gap using **v2 evidence about a v3 binary**. **RULE: name the mechanism by which the side effect reaches the operator and ask whether it can be contained.** A scratch `HOME`, a tempdir root and a detached worktree are the three that work here.
**E. THIS BOX AND THIS SHELL.** **A probe whose EXIT CODE is the finding never goes through a pipe** -- `cmd | head` reports head's status, and zsh has `pipestatus`, not `PIPESTATUS`; reaching for either is already the wrong shape. Redirect to a file and read `$?`. Unquoted `$var` does not word-split; an unmatched glob aborts the whole command. **A heredoc carrying prose is ALWAYS `<< 'EOF'`** -- board prose is dense with backticks and an unquoted heredoc command-substitutes every one. **A clock value goes in through a placeholder plus `sed`, or as a `sys.argv` value read in the same call.** **A STAMP IN A FILENAME IS A STAMP, GUARDED BY NOTHING (`0261`):** `whiteboard-clock-guard.sh` excludes `.history/` by path AND reads only added CONTENT lines, so an archive's own NAME is checked by neither arm. `cargo check --workspace --all-targets` -- the flag is the half memory drops. **`stat -f %m` does NOT follow a symlink on macOS; `stat -Lf %m` does.** **`find -newermt` PARSES LOCAL TIME, so a window typed in UTC silently shifts by the offset and returns a plausible EMPTY answer at exit 0** (vc, 2026-09-05, who fed it UTC stamps and got nothing for a file squarely inside the window). Same family as appending `Z` to a local reading, in the other direction: the zone mismatch answers rather than erroring. **A LITERAL IS NEVER A SPLIT POINT; ONLY CHARACTERS PRODUCED BY AN EXPANSION ARE** -- driven: `IFS=/`, `a="x/y"`, `b="p/q"`, `set -- $a/$b` gives THREE fields `[x] [y/p] [q]`; the `/` inside each expansion split, the literal between them did not, and fused `y` to `p`. **The full ladder, the space-hazard variant, and the lesson that matters more than the shell detail are in `.history/20260905/wip-prefold-2028Z.md`:** I told dc their rule was not the one that bit them, on a test run under default `IFS` where **both hypotheses predict one field, so my test had ZERO discriminating power and I never checked that it had any.** **RUNNING A TEST IS NOT TESTING; the test must be able to come out the other way.** **THIRD BREACH OF THIS FAMILY IN ONE SESSION, ALL THREE MINE:** `--include=*.rs` unquoted aborted a grep at exit 0; `<<PY` unquoted command-substituted my own board prose; and `for sym in $SYMS` over a 38-line roster ran ONCE with the whole blob as `$sym`, printing 17 fake edges that I nearly read as a finding about the tree. **The rule was on this board before all three.** Recorded in `L2` as well, because having it changed nothing.
**F. THE SHARED CHECKOUT.** Canon cannot be split, so every canon commit is silently multi-node. **`add` + `commit --only` is the only safe write; a live `index.lock` is a WAIT, never a removal, and the retry is the SAME command re-issued, never recomposed.** A release build DELETES the shared pair before building (`0196`) while the dirty-tree guard REDIRECTS -- two correct rules that can take the machine down together.
**F2. FILE OWNERSHIP DOES NOT IMPLY DIFF AUTHORSHIP, AND `git status` CARRIES NO AUTHOR AT ALL.** `--only` protects a FILE and not a HUNK (W39); this is one step further -- **the natural way to decide whose a dirty hunk is, _who owns this file_, is not evidence.** ic attributed dc's A1 to me on exactly that inference and nearly had me commit it. **RULE: read the diff for its SUBJECT before accepting or asserting that a hunk is anyone's.** Four independent lines beat one ownership inference.
**F3. CURRENCY IS A PROPERTY, NEVER A VALUE -- AND SO IS EVERY COUNT AND EVERY SET (absorbs F5).** A pin trailing HEAD is the correct steady state. **I handed vc `214/214`; they adopted the wording and were carrying it to hv; it is `216/216` because the ESTATE moved.** **dc's LIMB: A VALUE YOU ARE PASSING ON IS ONE YOU HAVE NOT MEASURED** -- relaying launders a claim, and **a figure returning to its author via a third party reads as independent confirmation and is your own error wearing a different name.** **RULE: hand the PROPERTY and the verb that reproduces it, never the number.** **THE SET FORM, AND MY OWN REMEDY COULD NOT CATCH IT:** I reported six uncommitted canon changes by taking four read at pickup and adding my two. It was SEVEN -- one had left, one had arrived, **both ends moved and neither move was mine to see** -- and _strike the value, park the command_ is useless because **a membership claim contains no value to strike.** Re-drive a set at the moment it is stated, exactly like a count. **DISCRIMINATOR for a stale binary: a STORE read survives it; a claim about BINARY BEHAVIOUR does not.** **WORKED INSTANCE, 2026-09-05, STILL COSTING: `intent issues edit` SHIPS** -- the release binary's own help reads _Correct an issue's record: its prose, its title, its severity_, and `dispatch-table.json` carries it four times. **`0179`'s _an issue body is write-once_ is EXPIRED**, and `AT-10.5`'s canon note still routes a correction around it, calls it the second instance in one session of the missing verb blocking a fix, and sends it to hv as evidence for re-sequencing a package. **A limitation recorded as a fact outlives its subject exactly the way a count does**, and this one is being paid for by three nodes. Found by re-driving a query at the RIGHT path after dc caught the wrong one -- and `event_log` DATES precisely while attributing nothing, since `principal` is `local` for every row. **AND vc's TRIAGE RULE, WHICH IS THE ACTIONABLE HALF AND BETTER THAN MINE: HUNT THE ROTTED RECORDS CITED IN ARGUMENTS BEFORE THOSE CITED IN ESTIMATES.** `AC-00.6`'s four-call-sites rotted a SIZING and cost six days of everyone believing the last pre-tag job was four times its size. `0179`'s write-once rotted a PREMISE and was steering a live scope ruling on hv's desk. **Same class, and what separates them is what the record was being used FOR** -- so the question to ask of any stale figure is not how wrong it is but what is currently resting on it.
**G. `0216` AND WRITING CANON.** **The read-verify-retry loop is a REQUIREMENT, not good practice** -- it replaces _one verb at a time_, because **the debouncer sees WRITES, NOT AUTHORS, so spacing protects only against your own burst.** Verify on an OBSERVABLE, never on a duration, never on the tool's `ok`. **After a revert the DISK WINS, so store and disk agreeing AT THE OLD VALUE is the signature, not the exclusion** (vc). `intent st attach` writes store AND canon and NEVER the disk file (`0082`).
**I. A PEER CHANNEL HAS NO TERMINATING CONDITION, AND I AM THE WORKED INSTANCE (ic's W86; vc: _a sign-off is not a terminating condition_).** hv asked me to fold and compact; **I answered peers for hours instead**, and every one of my messages ended with an acknowledgement that invited another. **A reply carrying only agreement is a turn the OTHER node must take**, so courtesy compounds into an unbounded loop that each end reads as the other's initiative. **ic modelled the fix by NOT sending a reply and putting it on their board instead.** **RULE: when a reply adds no new measurement and no decision, it goes on the board, not into the channel** -- the board is the surface that survives, which this morning proved in both directions.
**H. FOLDING THIS BOARD (absorbs H2).** **Write the pre-fold copy BEFORE folding and `cmp` it** -- nothing compares a folded board to what it was folded from, so a reconstruction from memory would pass every check we have. **Anchor on exact strings, never a prefix**: `index("## Decisions")` matches `"## Decisions owed by hv"`, inverts the slice and duplicates the tail; vc hit the same bug an hour after I described it to them. **`hygiene: ok` PASSES ON A CORRUPTED BODY** -- its contract is the header block, deliberately -- so verify section and family counts by hand. **AND A FOLD ARCHIVES A COMPLETED MEASUREMENT WITHOUT ASKING WHO WAS STILL WAITING FOR IT.** vc asked me to size `WP-06`'s nine unsized rows; I grepped the live board and the archive and **told them it was not mine.** It was mine twice: the ask is in an entry **I SENT**, and the answer was already driven in an earlier pre-fold. **Neither end checks delivery -- an inbox records what ARRIVED and never what happened next; a fold records what is DONE and never who was waiting.** **RULE: before a fold archives an answer, confirm the ASKER has it -- and when challenged on a claim, GREP WHAT YOU SENT, not only what you kept.**
**A3. THE CORRECT READER, SILENTLY BLIND -- AND THE THING THAT BLINDS IT IS MANDATED.** git's own trailer parser finds **ZERO** `Claude-Session` trailers across today's 58 commits; **55 carry the line.** `%(trailers:key=...,valueonly)` and `git interpret-trailers --parse` both return empty. Cause isolated by discriminator, not reasoned: the `(C)` attribution line is a non-trailer line in the final paragraph, so git rejects the whole paragraph as a trailer block; drop that line and the same body parses. **The harness mandates the `(C)` line, so no node can fix this by being careful** -- and dc, ic and vc all keep _read it off your own commit_ instructions that the obvious verb cannot honour. **RULE: when a structured reader returns EMPTY, positive-control it on a synthesised input before believing the absence.** Empty is a claim about the READER as often as about the corpus.
**B4. A CENSUS KEYED ON AN EXIT CODE MERGES MEANINGS THAT THE MESSAGE KEEPS APART.** Re-driven today: **two pairs share a code and mean opposite things.** `doctor` rc=1 RAN and reported a real finding; `st` rc=1 is a usage error. `config` rc=2 is truthfully unbuilt; `agents` rc=2 is false. **This is why my three-option `agents` menu found no convention to rule toward -- the instrument had already collapsed the distinction the ruling needed**, so the menu was built on a partition that does not exist. Sibling of B2 one level in: **B2 is a seam BETWEEN two populations; this is a seam INSIDE one instrument's key.** **RULE: capture the message beside the key, then ask whether any bin's members disagree with each other.**
**F4. A PUBLISHED FIGURE IS NEVER COMPARED TO THE ARTEFACT IT DESCRIBES -- AND I GOT THE MECHANISM WRONG TWICE BEFORE dc GOT IT RIGHT.** **THE OBSERVATION, MEASURED AND UNDISTURBED:** three of four nodes published a fold figure that differs from the committed board (ic 52502 vs 53409, vc 113 vs 115 LINES, me 20934 vs 21042; dc's two are exact). **Nothing in our tooling compares the two, so the gap is invisible by construction.** **THAT SENTENCE WAS ALWAYS THE FINDING; THE MECHANISM NEVER WAS.**

**F4a. AND MY REMEDY IS BLIND TO THIS REPO'S COMMONEST REWRITE (dc).** _Take the count off the commit_ fixes the SIZE claim and leaves the CONTENT claim unverified: `*Safety checkin*` -> `_Safety checkin_` is **length-preserving**, and `.prettierignore:9-11` names that exact substitution as what cost the estate 266 views. **A byte figure can match the commit EXACTLY while the file still changed.** Same seam as `int canon`'s membership-versus-content, twice in one evening.

**F4b. THE COUNTER ITSELF REPORTED THE WRONG UNIT.** python `len()` on decoded text counts CHARACTERS; `wc -c` and git count BYTES; two `§` (U+00A7) put mine 2 behind, twice, and **I was one step from reporting a SECOND WRITER on a single-writer file with the disproof inside my own instrument.** **RULE: a size compared against `git`, `wc` or a hook is measured in BYTES** -- `len(open(p,'rb').read())`.

**F4c. vc RAN THE ISOLATED EXPERIMENT NEITHER OF US HAD, AND A REFUSING GATE IS A FREE CONTROL.** A refusal freezes the artefact at a known state, so measure, `prettier --write` with **no edit of your own between the two reads**, measure again. On vc's board: **+2 bytes, ZERO lines.** **So for vc's 113-vs-115 the formatter is not merely unproven, it is REFUTED.** One board, one content state -- it does not license _the formatter never adds lines_. **ic's 907 bytes remains unexplained by anything any of us has measured.**
**J. THE OUTAGE I REPORTED WAS THE SMALLER HALF (vc, 2026-09-05).** I reported `0196` as the FAILED-build window. There are two, and the second is that a SUCCESSFUL build leaves the delivered pair absent **for the whole build** -- measured from another repository by lamplight-cc: six verbs in one script, verb 1 wrote, verbs 2-6 died `command not found`, `set -e` did not catch it, and it left a half-written git-tracked acceptance contract in Lamplight's canon. **That is a data-integrity finding across every project on this machine, and my report covered the half that announces itself.** Watch-out: **a failure mode that reports itself is the one you will find; the one that completes successfully and removes something is the one you will not.**
**K. A REMEDY STRING CARRIES AUTHORITY IT WAS NEVER DESIGNED FOR (vc's class, from my `0263` and dc's `0262`).** Two readings, both wrong, both natural. **As EVIDENCE:** the only `prettier --write` in the hook chain is the gate's own remedy, so a sceptic grepping to DISPROVE the claim finds it and reads confirmation. **As SPECIFICATION:** `intent st attach <thread> <path> --from <file>` names no frame of reference, so the printed remedy taught a repo-relative invocation that mints a duplicate attachment at rc=0 which no verb removes and `doctor` never reports. **RULE: a remedy is written to help one human recover from one error, and is then grepped as evidence, read as a spec and pasted as a command under none of those constraints. NOBODY AUDITS A REMEDY, BECAUSE A REMEDY IS WHAT YOU REACH FOR WHEN YOU ARE ALREADY LOOKING FOR SOMETHING ELSE.** **AND A CHECK THAT VERIFIES A DEFECT IS _STATED_ CANNOT SEE THAT ITS REMEDY IS FALSE (dc's `0277`, from my own control failure).** `install.md` closed both keg paragraphs with _a source install with `INTENT_HOME` set to the repository is unaffected_. The conclusion is true and the mechanism is not read by v3 at all -- driven: `env -u INTENT_HOME` gives `total: 0`, `INTENT_HOME=<repo>` gives `total: 0`, and the rig that actually carries the trees gives 64. **So a reader with a broken keg follows the page's own instruction, changes nothing, and has been told they are fixed.** `AC-02.3` finds the defect's quote in the page's bytes and passes; **a false remedy satisfies it perfectly**, and dc's instrument certified that page an hour before this. **A remedy is the only sentence on a defects page a reader ACTS on, and it is the one nothing verifies.**
**G2. THE ASYMMETRY LICENSES THE ATTEMPT; VERIFYING PAST THE INGEST IS WHAT MAKES IT A CLAIM (vc, refining me).** I wrote canon through the stale pair where vc held theirs, on a real distinction -- their write made canon state the truth LATER and nothing was lost by waiting, mine REMOVED a falsehood so delay was not neutral. **vc confirmed the asymmetry and then drew the line I had not: those are TWO acts, and the asymmetry alone would license writing and walking away at the `ok:` line, which is precisely what `0216` makes worthless.** Recorded because the next node to cite the asymmetry will cite it WITHOUT the verification unless the pairing is written down.
**F6. A STAGED PATH ON A SHARED CHECKOUT RIDES ANY BARE `git commit`, WHOEVER RUNS IT.** **AND `git add <path>` CAPTURES WHATEVER THAT PATH HOLDS AT THAT INSTANT, INCLUDING A PEER'S IN-FLIGHT BYTES -- `--only` IS PATH-SCOPED, NEVER AUTHOR-SCOPED (dc, 2026-09-05, on me).** `e1a076d5b` swept dc's live `0273` edits to `fn at()` into my WP-10 commit: I ran `git add render.rs` while they were mid-edit in the same file, and `--only render.rs` then faithfully committed their work along with mine. **The discipline I was following is the one that failed** -- it protects other PATHS and does nothing about other AUTHORS inside a path I named. **The author-safe forms are `git add -p`, or a `git diff` of my own hunks applied to an index I control**, and on a file a peer may hold, ask first. **AND THE SAME FLAG FAILS IN THE OPPOSITE DIRECTION ON A RENAME (ic, 2026-09-05).** A rename is a DELETION and an addition; `--only` handed the new path and `suite.rs` committed exactly those, left the deletion staged, and **HEAD carried BOTH files** -- the orphan nothing points at, plus the new one. The working tree held only the new file so nothing local looked wrong, and under `autotests = false` an orphan cannot compile its way to a red. **`--only` commits exactly what it is handed, reports success, and never says a path is missing.** So the flag has three faces now: too much landing (a peer's bytes in a path I named), too little landing (half a rename), and path-scoped-not-hunk-scoped. **The rule under all three: `--only` is an instruction, never a filter -- it subtracts nothing you did not think to name.** dc's `ST0056.json` sat in the INDEX, not just the worktree; it was committed, then staged again within the hour. **`git status` counts do not distinguish `M ` from ` M` and the difference is who publishes it under whose name.** My commits and vc's were both clean of it -- **not by checking, but because `add` + `commit --only <paths>` is the default for both of us. A habit that holds before anyone looks at it is worth more than the near miss that prompted the look.**
**L. A PRECONDITION THAT IS CORRECT CAN STILL BE A MIGRATION, AND THE ONLY WAY TO TELL IS TO COUNT.** vc's ordering on `0271`, and it decided the shape of a ruling: **measure the blast radius BEFORE proposing the fix.** The git arm refuses 0 of 18 fleet members and is additive; the dirty arm refuses 1 of 1 scoped to the conversion and **11 of 18 hung on the verb**, because `intent upgrade` is the convergent orchestrator and gets run routinely. **Same check, same correctness, opposite dispositions -- and nothing about the check's own logic tells you which you are holding.** Then my own build reddened 16 tests because the fixtures are not git repos: **the blast radius of a refusal includes the estate's own fixtures, and I measured the fleet and not the suite.**
**L2. THE THING THAT WOULD HAVE CAUGHT IT WAS PRESENT AND INERT (vc's merge, and the general form of two findings that looked unrelated).** **A SUBJECT THAT CANNOT EXHIBIT THE CONDITION RETURNS A CLEAN PASS, AND SIXTEEN TESTS WERE GREEN ON THAT BASIS.** I read my 16 reds as the cost of the guard. **They are the FIXTURES being wrong:** a migration fixture that is a tempdir with no git repo **cannot exhibit the precondition the migrator exists to enforce**, so those tests passed because their subject could not fail. **THIRD SIGHTING OF THIS EXACT SHAPE IN ONE DAY** -- dc's fixture CREATED the files its rows cite, so absence had to be arranged; my duplicate-AT scan read 13 files of 156; this. **It is `B`'s rule moved from the instrument to the SUBJECT: a control must be able to fail, and so must the thing it is pointed at.** **RULE: when a new guard reddens existing tests, ask whether those tests could ever have gone red before -- a green from a subject that cannot reach the guarded state was never evidence.** And vc's tell is the transferable half: **_additive_ is a claim about a POPULATION and their ruling never named which one** -- _additive over the fleet_ would have shown the gap inside the ruling. **FOURTH SIGHTING, AND THE INERT GUARD WAS MY OWN BOARD.** Family `E` above already reads _a heredoc carrying prose is ALWAYS `<< 'EOF'` -- board prose is dense with backticks and an unquoted heredoc command-substitutes every one._ I wrote a board edit with `<<PY`, and zsh executed the backticks in my own prose: `intent agents` ran, an `intentd` start was attempted and refused. **Nothing was damaged and that is not the finding.** The finding is that a rule I had read this session, stating this exact failure in these exact words, did not fire -- so **a written rule is a guard with the same failure mode as a test: present, correct, and inert at the moment it was needed.** The cure stays in `E` where it is actionable; what belongs here is that having it changed nothing. **FIFTH SIGHTING, AND IT IS THE INSTRUMENT FOR A CRITERION:** `tests/cutover_guard.bash` asks exactly the right question -- does any file surviving the prune depend on `bin/` -- with a two-edge method and a control arm, and its population is four plugin/template directories with `bin/.devbin/` in none of them. It printed `ok: 16 file(s), none depending on bin/` throughout the period the release script sourced `intent_helpers` outright. **vc found the sharpest form of the whole class in that same file and it beats both our sentences:** `:66` reads `[ "${#POP[@]}" -gt 0 ] || die "the population is empty -- this instrument measured nothing"`. **IT GUARDS AGAINST MEASURING NOTHING AND NOT AGAINST MEASURING THE WRONG THING** -- not a careless instrument but a careful one whose care points at the wrong axis, and a non-empty WRONG population sails through it and renders as coverage.

**THE SECOND INSTANCE IS A WARNING RATHER THAN A TEST, AND THAT IS WHY IT GENERALISES.** vc hit the zsh unquoted-glob abort TWICE today -- `--include=*.rs` running nothing at exit 0 -- **with the trap written on their own board in the words that describe it exactly.** I carry it in `E` and it does not stop me either. **A DOCUMENTED TRAP IS NOT A PREVENTED ONE; ONLY QUOTING IT IS.** **THE MECHANICAL FIX STAYS IN `E` DELIBERATELY** -- merging it out would lose the actionable form, and what belongs here is the INSTANCE, not the cure. **RULE: a control, a test or a warning that exists but cannot act is indistinguishable from none, and the estate reads its presence as coverage.** Ask of any safeguard what it would take for it to FIRE; if the answer is _nothing that happens in practice_, it is decoration.

**P. A CLAIM'S TRUTH AND A CLAIM'S REACH ARE SEPARATE PROPERTIES, AND A CORRECT SENTENCE BORROWED FOR A DIFFERENT QUESTION CARRIES NEITHER (vc's half plus mine, and it is one rule).** vc's: **a ruling that is RIGHT is not re-examined, so its price rides along unchecked** -- twice in an hour they ruled correctly with a wrong cost attached. Mine: **I handed them the screenshot precedent as a COST argument when it was a CORRECTNESS argument.** It proves the residue emitter can name paths with class and reason; it never proved the whiteboard was REACHABLE -- and `legacy::scan` mentions it once, in a comment, so the 1,386 artefacts were never in the scanned population. **They priced off a sentence of mine that was not about price, and I never said which question it answered.** **RULE: when handing a peer a fact, name the question it answers.** A sentence that is true is not thereby true of the thing they are about to spend on.

**Q. A FIGURE SURVIVES A CONTEXT RESET AND ITS SUBJECT DOES NOT.** I carried `1,386 whiteboard files` across a compaction and shipped it in two source comments as _1,386 on baize_. **The count is real and the estate is wrong**: it is Lamplight `fe5dff3e6`, and baize's whiteboard is 110 -- a number sitting in the same paragraph of `AT-10.5` I had read. Nothing flagged it, because a number with a plausible estate name beside it reads exactly like a measured one. **It was caught only by re-driving the archive**, which produced 1,386 against Lamplight and would have produced 110 against baize. **RULE: a figure recalled rather than re-measured carries no subject, so it is not evidence for any estate -- re-drive it or drop the attribution.** This is the handover rule (`hand over PROPERTIES, not values`) arriving inside a single session rather than between two, which is the case I did not expect it in.

**R. AN ANOMALY YOU NOTICE AND EXPLAIN IS WORSE THAN ONE YOU MISS, BECAUSE THE EXPLANATION CLOSES THE QUESTION.** `git show` said my commit changed **76 lines of `render.rs` when my edit was 35**. I saw the gap, wrote _because rustfmt reformatted other parts of the file too_, and moved on. **rustfmt had not touched them: they were dc's in-flight `0273` edits, and my `git add` swept them into my commit.** The cause was invented, plausible, and never checked -- one look at the hunk headers would have named `fn at()`, a function I have never opened. **This is the migrator's own rule fired back at me:** `render.rs` says, in the very function I was editing, that an invented cause _is indistinguishable by inspection from a recorded one, and worse than silence because it sends the reader looking for something that never happened._ I was the reader. **AND THE FILE I MISSED WAS THE ONE I OWNED:** I checked `mcp.rs` and `contract.rs` for peer collateral and reasoned correctly about both from crate boundaries, then skipped the check on the only file I was myself writing. **RULE: a number that does not match your edit is a measurement, not a puzzle -- read the hunk headers before you write a reason.**

**S. TWO CORRECT LOCAL DECISIONS COMPOSE INTO A FALSE GLOBAL ANSWER, AND NO REVIEWER OF EITHER ONE CAN SEE IT (vc's account of my `0275` finding).** `Library::files` returns an empty list for a root that is not a directory, and that is RIGHT where it sits -- _no rules installed_ is an ordinary state, and erroring there would refuse every install that simply has none. `rules_list` prints `total: N rule(s)`, and that is RIGHT too. **Composed, they tell an operator with NO RULE LIBRARY AT ALL that they have zero rules, at exit 0, under a well-formed table header** -- and `install.md` nominates that verb as the install check precisely because it is supposed to fail on that fault. **Nothing in either function is wrong, so reading either function finds nothing**, which is what separates this from every other family here: the others are a wrong instrument, a wrong population or a wrong premise, and all of those are visible to someone reading the thing. **This one is only visible from the composition, which is the failure mode code review is structurally worst at.** **RULE: when a function answers _there are none of these_, ask what it returns when the PLACE they live is absent -- and if the answer is the same, the caller is the one that owes the distinction.** The fix is never to make the inner function wrong; it is to expose what the caller needs -- here `Library::canon_root()`, keyed on the SHIPPED tree rather than on a count, because an ext pack can supply rules while that tree is missing.

**M. I REACHED FOR THE SIDE I COULD EDIT AND MISTOOK IT FOR THE SIDE THAT WAS WRONG.** Two of WP-10's objective clauses had no row, and I proposed making the OBJECTIVE agree with the rows. vc held it and drove my question instead of answering it: the rows had not moved, they had VANISHED -- ST0069 stated the requirement nowhere and `ST0069.wps` is `null`, so the premise both descopes rest on names a destination that does not exist. **My principle survived and my application of it was backwards: it says the two sides must AGREE and says nothing about which one moves.** The objective was the one edit I could make without a ruling, which is a fact about my REACH. **When the rows left by mistake, making the objective agree with them is how the mistake becomes permanent.**
**N. THE STANDARD WAS ALREADY IMPLEMENTED, ON THE WRONG CLASS, IN THE SAME RUN.** `AC-00.2` asks for every unconverted artefact BY NAME; the migrator names 1,386 whiteboard files with one directory noun **and eight oversized screenshots individually, by path, with class, reason and measured size.** 167 of the run's lines already name a path. **The argument that closed it was not a reading of the row -- it was finding the row's own standard already working ten lines up the same output.** RULE: before arguing a tool cannot meet a standard, grep its own output for the standard.
**O. I REPORTED THE ANSWER THAT COST ME MORE, AND THAT IS THE ONLY REASON IT IS EVIDENCE.** I preferred the one-line count-and-reason form for `0272` and reported the 1,386-line form, because `AC-10.5` permits the cheap one and **`AC-00.2` says _by name_ and both rows have to hold.** vc had told me not to tune the drive toward the cheaper outcome. **A drive whose result matches what the driver wanted is not worthless, but it carries no information about the tuning -- so the caveat has to be written down at the moment it costs something, not recalled afterwards.**

**A2 (SECOND INSTANCE TODAY, SAME SHAPE, MINE).** I wrote `19:22Z` into board prose in the same command whose `date -u` output said `20:20Z`. **Heartbeat taken from the clock; prose typed by feel, inside an entry whose own section forbids exactly that.** vc committed the identical failure at 17:29Z. **THE CHECK CANNOT BE ATTENTIONAL, BECAUSE THE CLOCK WAS ALREADY IN FRONT OF BOTH OF US:** the stamp is a byte-for-byte substring of this turn's tool output, or it does not go in.

## Decisions

**NINE EXECUTED-NARRATIVE ROWS ARCHIVED THIS FOLD; every unexecuted ruling and every durable principle stays.** Execution is the discriminator, never the date -- and `0225`'s re-affirmation left this list only because it is now carried INSIDE `0223`'s body, which is execution rather than age.

- (2026-09-04, cc->dc, adopted) **A CONTROL THAT LOST ITS SUBJECT PASSES FOR THE WRONG REASON, SO CONTROL THE CONTROL.** dc's positive control was _ic's embed goes green_ and ic had removed the embed; it would have passed under the broken guard too. **dc's answer went past the catch: a META-CONTROL that drives the OLD logic against the same planted pair and refuses if the old logic also passes** (`positive-control-has-no-subject(old-logic-also-passes)`), mutation-tested. **The generalisation is theirs: a control asserts a DIFFERENCE, so it must be driven against the state it claims to distinguish FROM.**

- (2026-09-04, cc) **A CITATION INHERITS THE CITED CLAIM'S TRUTH AT CITATION TIME AND NEVER UPDATES.** `0223` rests on `0090`+`0151` to conclude a junk row _can only be closed_; all three are open, the underlying verb shipped, and **none of the three noticed.** Currency is a property of a CLAIM (F3) and equally of every REFERENCE to it -- and a reference is worse, because it reads as sourced.

- (2026-09-04, cc) **A PUBLISHED FIGURE IS A CLAIM ABOUT AN ARTEFACT, AND THE ARTEFACT IS THE COMMITTED ONE.**

- (2026-09-04, hv via cc) **`config`, `ext` AND `learn` SHIP DECLARED-AND-UNBUILT IN 3.0.1** (2026-08-31). **A general ruling stated later does not vacate a specific one.**
- (2026-09-04, vc) **THE ES RENDERER IS `WP-17`'s BY WP-17's OWN TITLE.** `D56` forbids server-rendered HTML and DESIGNS the client-rendered browser face.
- (2026-09-04, ic via §10a) **`widget` IS A SEMANTIC CLASSIFICATION, NOT A VIEW.** `prose` means the field is long; TUI to `$EDITOR`, browser to `<textarea>`, SwiftUI to `TextEditor`. **Renderers legitimately differ in the WIDGET and never in the model.**
- (2026-09-04, ic REFUTING cc) **`SERVED_BY_DAEMON`'s AXIS IS _REACHABLE BY A CLI VERB PATH_ VERSUS _WIRE-ONLY_, NOT DUAL-PATH.** The table is a `const`, so only payload-free variants fit. **`Op::Set` HAS the dual path and is absent** -- which refutes the axis on evidence I already held. **A one-row table cannot distinguish two axes.**
- (2026-09-04, cc+dc) **A HOLD DISCHARGED BY THE CONDITION IT NAMED TEACHES THE OPPOSITE OF ONE THAT ERODES.** File the protection BEFORE discharging the hold.
- (2026-09-04, cc+dc) **A DIRECTORY `add` PROTECTS A PATH AND A DIRECTORY PATH IS NOT A FILE.** And **a path that moves on its own is not a path either node can own** (`project.json`'s `todo_watermark`).
- (2026-09-04, cc) **THE CANON STORE AND DISK DO NOT DISAGREE.** The divergence is git-only. Filing writes store and disk atomically; **git is a separate human-gated act and NOTHING REPORTS THE GAP.**
- (2026-09-04, cc) **A SET PASSING TOGETHER IS NOT EVIDENCE ABOUT ITS SUBSETS.**
- (2026-09-03, ic->cc) **THE CLASS IS THE UNDRIVEN NUMBER, NOT THE WRONG ARTEFACT.**
- (2026-09-03, cc+vc) **CONTENTION IS `0216`'s VARIABLE** -- not spacing, not corpus size. **Refusals (`0226`) and silent losses (`0216`) TRADE OFF**, so a single counter prints _fewer losses under load_ and reads as improvement.
- (2026-09-02, cc+vc) **`0216`'s FIX IS DAEMON-SIDE, NOT USAGE DISCIPLINE.** A hazard reachable by an ordinary shell loop cannot be mitigated by how carefully nodes write.
- (2026-09-02, vc) **A RIDER CANNOT BE VIOLATED BY A CASE ITS OWN HAZARD CANNOT REACH.**
- (2026-09-02, vc) **`WP-14` AND ALL 12 OF ITS ACs WERE DESCOPED WHOLE TO ST0069** by hv on 2026-08-30. `AC-09.5`'s wip/boards half goes with it.
- (2026-09-02) **TWO MACHINE PROJECTIONS OF ONE VALUE MUST NOT DRIFT; A HUMAN RENDERING OF IT IS NOT A COPY AT ALL.**
- (2026-09-02) **CONTENT COMPARISON DOMINATES A VERSION COUNTER FOR A COMPARE-AND-SWAP.**
- (2026-09-02, vc) **AN EXCLUSION MUST BE VISIBLE WHEREVER THE BEHAVIOUR IS CLAIMED, NOT ONLY WHERE THE CHECK LIVES.**
- (2026-09-01, hv) **v3.0.1 IS FEATURE COMPLETE, THERE IS NO TAG WINDOW AND NO EXTERNAL CONSUMER, AND COST IS NOT A CONSTRAINT.** The scarcity register is retired as a class.
- (2026-09-01) **A REFUSAL THAT CANNOT SAY WHAT IT FOUND MAKES ITS OWN DEFECT UNDIAGNOSABLE.**
- (2026-09-01) **A REMEDY INHERITS ITS BRANCH'S ASYMMETRY.** Confirm-before-refuse is SAFE on the lock and WRONG on the probe.
- (2026-08-31, vc) **A CRITERION IS NOT REWORDED TO WHAT THE CURRENT STATE SATISFIES.** The ruled form must be HARDER.
- (2026-08-31, vc) **A CLASS CHANGE WITHOUT ITS REASON IS A DELETION WEARING A NEW LABEL.**
- (2026-08-31, ic correcting me) **AN OWNERSHIP SPLIT IS A PURPOSE, NOT A BOUNDARY.** Two hands in one file IS `0206` in miniature.
- (2026-08-31, CORRECTED) **`close --note` STAYS RULED OUT ON A CONTINGENT FOOTING.** The keg has no `edit`; the ruling survives because v3.0.1 ships from the tree.
