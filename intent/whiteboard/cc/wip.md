---
node: cc
name: Control Claude
role: control
session_id: ae8c8153-6f3f-438f-b96b-04bd381ad4ed
heartbeat_at: 2026-08-31 15:54Z
status: active
focus: "FOLDED 2026-08-31 15:54Z, pre-fold verbatim + sha-verified at .history/20260831/wip-fold-1554Z.md. NOTHING IN FLIGHT, tree clean of me. THE RESOLVER LANDED (e2fcf350) and vc accepted both deviations. 0192 IS MINE AND IS A BLOCKER: hv's edit-the-cover ruling is HALF-EXECUTED -- the cover opens, no verb carries the edit back, and sync --to-store destroys it at rc=0. I told vc to stand down on it and had to retract. NEXT: 0192 re-scope, the dvb build hazard, then scoped issue sync, close --note, 0086/0087."
claims: [ST0056/06, ST0056/08, ST0056/10, ST0057/00]
---

# Control Claude (cc)

## DOING

**NOTHING IN FLIGHT.** Tree clean of me, no worktrees, no daemons of mine, every landing post-verified against its intended path set. Delivered binaries rebuilt and healthy at `553ac304`; both PATH symlinks resolve.

## TODO

- **(hv, 2026-08-31 15:54Z, RULED AND NOT EXECUTED) `0192`: WIRE THE READ-BACK, AND COST IT FIRST.** Connect an operator-runnable sync direction to `carry_info_round_trip`, which already exists and is called at `ingest.rs:643`. Declined: re-closing the cover; shipping with a warning; shipping as-is. **hv's CONDITION IS PART OF THE RULING RATHER THAN A GLOSS: cost it BEFORE building, and if wiring is not small the fallback is RE-CLOSING THE COVER, not shipping the hazard. Come back with the cost rather than absorbing it.** vc's own drive adds the useful half: `st dehydrate` ALREADY notices, refusing at rc=1 and naming the divergence -- **the detection exists and is in the wrong verb.**
- **(hv, 2026-08-31 15:54Z, RULED AND NOT EXECUTED) `AC-09.6`: NARROW THE TABLE AND MAKE THE DROP LOUD**, and hv took the second half as the point. `tools()` REFUSES an exposed row with no facade instead of `continue`. **The refusal is mine; the 21 `exposed_on_mcp: false` table dispositions are ic's.** Declined: wiring all 21; loud-refusal-only with the 21 deferred; shipping as-is.
- **`0192` -- MINE, AND I CALLED IT A BLOCKER AFTER FIRST TELLING vc IT WAS NOT.** hv's 2026-08-29 ruling opened the thread cover (`st edit <ST> info`) so that _a sync knows it has been edited and updates the db_. **The first half is built and the second is reachable from no verb.** Driven per-verb in a temp project: `ingest` and `ingest --from-md` both write NOTHING at rc=0 -- that is `0097`, and it is broader than filed, because this was a FRESH estate rather than a migrated one -- and `sync --to-store` then DESTROYS the edit on disk and exits 0 saying _nothing the store already held was overwritten_. The model never receives it. **Re-scope the issue to the half-executed ruling and put the reason in the commit that closes it** (`0188`: the register cannot record a close reason). `carry_info_round_trip` exists and is called at `ingest.rs:643`, so the code is present and nothing operator-runnable reaches a write. That gap is the thing to find.
- **`dvb build all` DELETES THE SHARED BINARIES BEFORE BUILDING AND RESTORES NOTHING IF THE BUILD FAILS.** Not filed yet; file it. Its first step forces the provenance embeds by REMOVING the pair; my run (16:46 LOCAL -- `ls -la` prints local and I first wrote it as `16:46Z`, which is the exact +1h mechanism the protocol documents; vc independently dates the outage window 15:30Z-15:49Z) then exited 1 at the SET check having produced nothing, leaving both PATH symlinks dangling for four live sessions. The second run succeeded. **I have NOT established why the first produced no binaries and must not guess** -- but delete-then-build with no restore, against an artefact four sessions consume, is the hazard whether or not that cause turns out to be interesting.
- **`st_edit_opens_or_prints.rs`'s HEADER IS FALSE AND REVERSED.** It states that `st edit <ST> info` does not work at HEAD and has not since `c4709d3f`, and drives its property through `design` instead. Both claims are now wrong. **Fix the reasoning beside the verb, not only the test** -- and `design` is the wrong vehicle regardless, since a v3 thread carries `info.md` and `acceptance.md` only.
- **SCOPED ISSUE SYNC** -- `facade.rs`'s `Some(_) => Vec::new()` means any scoped sync writes ZERO issue files. `Scope` is `All | Threads` with no issue form. The refusal half is already built.
- **`close --note`** -- ruled MY way: `status_reason` on `Issue`, ungated, matching `Thread` and `WorkPackage`. **`0063` unblocks with it.** UNEXECUTED.
- **`0086`/`0087`** -- write the ARCHAEOLOGY RESULT as the body under the provenance marker (vc's ruling). `0095`/`0096` are hv's disposition and vc carries them. UNEXECUTED.

## Watch-outs

**THE EXIT STATUS YOU READ IS THE LAST THING THAT RAN, AND THIS FAMILY HAS THREE COSTUMES AND ONE PROPERTY.** `cmd | grep ...` reads grep's; `cmd > log; echo rc=$?` reads the ECHO's; `cmd 2>&1 | head` reads head's. **It always reports SUCCESS for a failed command, so it never announces itself.** The first and second both bit me in one day, the second on a background run whose harness notification said exit 0 over a compile error -- and that run had also raced with an edit I made while it was compiling, so it was doubly worthless. Capture the status directly off the command and let nothing stand between. **And `cargo test` STOPS AT THE FIRST FAILING TARGET** -- `--no-fail-fast` is the house idiom; `--manifest-path` immunises against cwd drift, which resets between calls and makes a bare `cargo test` exit 101 in a way that reads exactly like a test failure.

**A GREEN SUITE OVER A CORPUS THAT CANNOT EXHIBIT THE CHANGE IS NOT EVIDENCE ABOUT THE CHANGE.** I removed the width arm -- altering what `promote("0164")` means for the TUI, the explorer and `edit` -- and both suites stayed green, because **every existing test drives `ST0056`, which is tagged and unaffected either way.** Two real regressions sat under that green and were found by READING: the TUI would have refused to save any issue field, and its two sibling methods disagreed about whether to read the kind, one of them underscoring it under a doc arguing that was correct. **Only the removed arm had been holding them in agreement.** vc is recording this as AC-00.11's sibling with the TEST corpus as the population.

**I READ THE CODE INSTEAD OF DRIVING IT AND TOLD A PEER TO STAND DOWN ON A REAL BLOCKER.** On `0192` I quoted `views.rs`'s refusal logic as proof that nothing is lost silently. The code is real, it is correct, and **nothing reaches it on the path that loses the edit.** vc had invoked a rule without opening the file; I opened the file without running the verb. **One layer down, the same error, in the act of correcting it.** A code path is evidence about a code path; only a drive is evidence about behaviour.

**MEASURE THE SHIPPED BINARY INSTEAD OF INFERRING FROM CODE OR DATES -- AND CHECK WHICH TREE IT MET.** The v3.0.0 keg is read-only and drivable under a temp `HOME`; it corrected my own `0189`. **But a store-touching keg drive in THIS tree is refused before reaching the behaviour** (tree schema 16, keg speaks 13), while verbs that read no store answer honestly -- so `--help` and a tag blob are trustworthy where a store drive is not. Drive the keg in a tree the keg initialised. **And `merge-base --is-ancestor <fix> <tag>` is the question to ask before calling a keg/tree difference a regression**: it is how `0192`'s central claim collapsed.

**A REMEDY THAT IS ACTIONABLE AND WRONG IS WORSE THAN A BARE ONE.** `intent:///` -- scheme, EMPTY authority, path; the two-slash form appeared TEN times against ONE canonical use, by four people who work on the tool daily, because nothing emitted it. **Standards-correct and learnable are different properties.** The same class one step on: a remedy must not name the form it just refused (`NotHydratable` said _address a thread or an issue_ about an issue), and a remedy wrong in one instance is usually wrong for the whole ARM.

**A WORKED EXAMPLE MEETS GUARDS A LITERAL CANNOT SATISFY.** `address_resolution_single_home` refuses the scheme spelled outside a comment; `no_pm_state_in_output` refuses an id from the reader's own project; the two disagree about `ST0000`. **Render examples from `to_url()` with a placeholder** -- it satisfies both, and cannot go stale into teaching the form the tool refuses.

**A GUARD CAN NAME ITS OWN EXIT CONDITION, AND EXECUTING IT IS NOT WIDENING IT.** Three times this week -- `declared_values_are_enforced`'s kind row, `format_roster_is_honoured`'s wiring note, and `address_of`'s _this is the LADDER'S SEAT_ comment, which the resolver then occupied. **Move the row; do not widen it.**

**CONTESTED AUTHORITY GETS A SEPARABLE COMMIT, AND THE SEPARATION IS WHAT MADE THE REVERT ONE FILE.** `a854d7c3` landed alone and reverted alone. **AND A DIRECT INSTRUCTION FROM hv CAN BE THREE MINUTES BEHIND A BOARD RULING THAT DECLINED IT** -- mine was, and the tree shipped the declined option for an hour. When two peers point opposite ways, look for the SEQUENCE before the authority: ic's stated condition and vc's relay reconciled exactly.

**MY EXTRACT WRITES RENDER, SO A PEER'S STORE-ONLY EDIT LANDS ON DISK UNDER MY HAND.** Announce it and leave it staged for them; **I do not commit a peer's bytes under my message.** A peer's staged file blocks my unrelated commit, because guards read the index, and a live `index.lock` is a WAIT rather than a clear. Never `--no-verify`, never unstage a peer's path.

**PROVENANCE ON THIS BOARD IS CARRIED BY PATH AND BY THE MESSAGE, NEVER BY AUTHORSHIP.** Every node commits as the same person, so the git author field cannot distinguish us -- a direct consequence of the no-AI-attribution rule. I read a vc commit as an hv broadcast because of it. **Read the touched path.**

**zsh: an unmatched glob (`--include=*.rs`) ABORTS the command; `$var` does NOT word-split** -- both fired again today, and the second produced a plausible wrong answer rather than an error -- **and `||` after a pipeline binds to the LAST STAGE.** The tell is UNIFORMITY across a set that should have differed. **NEVER start `intentd` under the real `$HOME`. NEVER invoke `intent fc`.** rustfmt reformats what you just wrote, and touching a source file makes `RealDaemon` refuse a now-stale sibling -- rebuild `-p intentd` after formatting, which the apparatus catches BY NAME rather than as a misleading failure.

**`SERVED` IS NOT A HIGHLANDER VIOLATION AND MUST NOT BE RE-DERIVED AS ONE.** 60 hand-written strings held in sync with `tools()` by a test looks exactly like two homes for one fact. `mcp_surface.rs:30` says plainly it is anchored to an INDEPENDENT declaration on purpose, so the test is not self-referential -- the same reasoning as the dispatch table's `field_overlap` note. vc nearly carried it to hv and checked first. **Recorded here so the next reader meets the reason rather than the shape.**

**A GENERATED VIEW MUST BE REGENERATED WHEN ITS SOURCE MOVES, AND ITS GENERATOR REFUSES PROSE THAT IS NOT A FORMATTER FIXED POINT.** `dispatch-table.md` from `.json`; the repo normalises emphasis to `_underscores_`.

## Decisions

- (2026-08-31) **A LADDER IS WALKED IN FULL AND THE FIRST HIT DOES NOT WIN.** hv ruled thread-then-issue, which reads as precedence; precedence would silently pick the thread for 48 of 69 numbers on this estate -- `0189` one layer up. Every rung is probed, the HITS are counted, and order fixes only the REPORT's order. **A three-outcome resolution needs a three-outcome error type or the assumption just moves into the message** (vc, accepting `Unlanded::Unresolvable` as a correction of their own ruling).
- (2026-08-31) **ONE PRESENCE PROBE, DERIVED, NOT TWO INJECTED** (vc, accepting over their own ruled shape). For an ITEM view, _is this in the store_ and _does this view have content_ are one question. **The condition rides in the doc comment**: if a face ever needs them to DIFFER, the split is a ruling and not a quiet second injection.
- (2026-08-31) **A CONTRACT WITH A CONSUMER IS ASKED FOR, NEVER MINTED.** ic named `daemon status --format json`'s shape and I built that, with no `removable` field -- the state IS the remedy.
- (2026-08-31) **THE ARCHAEOLOGY IS THE FILING COMMIT, NOT AN EARLIER REVISION.** Every empty-body canon file appears EXACTLY ONCE in history; the prose survived in the commit that filed the empty record. **Transcribed verbatim under a provenance marker** -- paraphrase would make me the author of four nodes' findings. **And the empty-body count MOVED because the defect is still recruiting rather than because anyone miscounted: both numbers are right about their day, reconcile neither.**
- (2026-08-31) **`close --note` IS A HIGHLANDER GAP, NOT A NEW FIELD** -- `Thread` and `WorkPackage` carry `status_reason`; `Issue` is the one of three without it. **`body` is affirmatively wrong**: its doc says _carried whole and never parsed_, so a status verb would smuggle machine text into the author's field.
- (2026-08-31) **AN OWNERSHIP SPLIT IS A PURPOSE, NOT A BOUNDARY** (ic, correcting me). Reverting so a peer can rebuild byte-identical code is churn wearing Highlander's clothes; the split exists to stop DIVERGENT work. **I had read it as a fence.**
