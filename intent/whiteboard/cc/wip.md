---
node: cc
name: Control Claude
role: control
session_id: ae8c8153-6f3f-438f-b96b-04bd381ad4ed
heartbeat_at: 2026-09-01 08:52Z
status: active
focus: "hv RULED 2026-09-01 08:32Z (3183990a): v3.0.1 is FEATURE COMPLETE, no tag window, no external consumer, cost is not a constraint. THE SCARCITY REGISTER IS RETIRED AS A CLASS -- ship-it-red / not-in-this-cut / after-the-tag were all unstated claims that time was short. Landed: daemon suite 9cf53e35, config suite e3110564. BIGGEST FINDING: the estate default INTENT_BIN is the v2 SHELL SCRIPT, so no v3-only test can ever count as burning coverage -- AC-06.1 cannot close on tests alone. Next: 0206 CAS (refuse-and-name, ruled). RE-DRIVE EVERY FIGURE HERE."
claims: [ST0056/06, ST0056/08, ST0056/10, ST0057/00]
---

# Control Claude (cc)

## DOING

**BUILDING `0206`'s COMPARE-AND-SWAP.** Ruled by vc: REFUSE AND NAME, never retry -- a silent retry re-derives the write from a snapshot the operator never saw, and **a fix must not be observationally identical to the defect**. The CAS asks the **STORE**, not the disk: the authored-body guard beside it asks the DISK because the disk file is what loses prose, but the record race loses the DB ROW and D01 makes the DB the SSOT.

**THE DESIGN GOT SMALLER AND MORE IDIOMATIC ONCE I READ THE WRITE PATH.** The fix site is ONE function -- `apply_envelopes` (facade.rs:7571), reached by `apply` -> `apply_with_state` and two direct callers; the 19 `self.canon.clone()` occurrences are read-modify-WRITE sites and every one funnels through it. **AND THE PATTERN ALREADY EXISTS ONE DOOR ALONG:** `Mutation.created_threads` carries the CREATE door, where _the DATABASE decides, inside this transaction, whether the key was really free_ (issue 0131). The CAS is that exact shape for the CHANGE door. **THE COMPARE MUST BE INSIDE `commit_mutation`'s TRANSACTION or it narrows the window rather than closing it.**

**OPEN, AND IT IS A SCHEMA CALL RATHER THAN A CODING ONE:** there is no revision column on `threads`, so a textbook O(1) CAS needs one -- which moves `SCHEMA_DDL_VER` (13) and lands under AC-06.10. The alternative is comparing reconstructed `Thread` structs inside the transaction, which needs no schema change and pays a full child read per changed thread. **Put to vc before building; not stalling on it.**

## TODO

- **`daemon` AND `config` NOW HAVE SUITES, AND NEITHER CAN COUNT. LANDED `9cf53e35` + `e3110564`.** Ten arms on `daemon` (none starts one -- four sessions write this checkout; the fake `$HOME` isolates by construction because `$HOME` is read in exactly ONE module and the socket derives from it). Six on `config`, pinning it DECLARED-and-UNBUILT at rc=2 before anyone designs it, which is the ordering AC-06.1 dates. **DRIVEN THREE WAYS EACH: v3 all pass; `INTENT_BIN=/usr/bin/false` all fail (so every arm reaches the CLI); the DEFAULT binding all-but-one fail.** **BECAUSE THE ESTATE'S DEFAULT `INTENT_BIN` IS `bin/intent`, THE v2 SHELL SCRIPT** (`test_helper.bash:21`), and v2 has no `daemon`. A non-zero DEFAULT failure count is UNSTABLE under burn.sh and carries no classification, so `coverage_map` will not count either file however correct it is. **WRITING TESTS CANNOT CLOSE AC-06.1's COVERAGE CLAUSE.** The real hole is a HIGHLANDER one: `run_v2_suite.bash` defaults to v3, `test_helper.bash` defaults to v2, two homes for _which binary is under test_, disagreeing over one estate. vc adds that the same line is why `AC-12.1` is red (EMITS 114 / EXECUTES 85; the flip removes the dispatcher route, NOT the 35 direct `intent_<sub>` invocations). **Flip THEN re-baseline, or the wall time is paid twice. Not mine to take.**
- **`AC-06.11` READS SATISFIED AND ITS CORPUS IS NARROWER THAN ITS CRITERION (dc found it; I verified both halves).** `ComputedSatisfaction`'s remedy named `intent at set <AT> green`: **`at set` does not exist** (the family is list/lint/green/red/na/new/edit) and the grammar shows neither of the `<STID> <ATID>` every `at` verb takes. Wrong twice, on a path real operators reach. **MY GUARD COULD NOT SEE IT BECAUSE `remedies_are_reachable.rs` DRIVES EVERY PATH WITH `SENTINEL = "ST9999"`** -- an argument that satisfies clap and resolves to NOTHING, so the population is _remedies reachable from an empty state_ and every state-dependent refusal sits outside it. **dc's form is sharper than mine: proving the harness can still FIRE says nothing about whether it can SEE.** dc took the one-line string (they were already compiling that file, and two hands in one file IS 0206); **the corpus half is mine** and it will go red on a SET of strings, not one. Size for the set.
- **ITEM 4 IS SIZED AND MY OWN ESCALATION'S PREMISE WAS ALREADY FALSE WHEN I WROTE IT.** I told hv on 2026-08-25 that `sync`'s skip needs a STAGED-vs-UNSTAGED distinction and _there is no cheap middle_. **`sync.rs:856` already draws that line**: `ls-files --others` lists files NOT IN THE INDEX, so a `git add`ed file never appears, and `NotInIndex::Untracked` is documented as _not in the index at all_. It has a test. **S if the skip reuses the whole-repo predicate; M if the ingest walker needs it per-path** (that walker is `ignore`-crate-only today). Carrying M until driven. **AN OPEN ESCALATION AGES INTO AN ASSERTION** (vc's sharpening): true when written, never re-read because it is _already asked_, accruing authority it never earned. **Before anyone budgets against an escalation, its AUTHOR re-drives it** -- only the author knows what they did not check.
- **`coverage_map.sh` NOW REFUSES TO PUBLISH, SO EVERY COVERAGE FIGURE ON THIS BOARD IS STALE EVIDENCE.** The burn TSV no longer covers the on-disk estate -- 16 files NEVER MEASURED, 1 STALE ROW, baseline 99 rows dated 15 Aug against 113 `.bats` on disk. **It is refusing CORRECTLY** rather than computing over the wrong corpus, which is the instrument behaving as designed. The remedy is re-running `burn.sh`, a DOUBLE sweep of the full estate (each file twice, default + `INTENT_BIN=/usr/bin/false`) that has hung for 3.5h once. **I DO NOT LAUNCH IT** -- full-suite runs are the human's. **The two holes survive the refusal independently:** `grep -rn 'intent config' tests/` and `grep -rn 'intent daemon' tests/` are both ZERO, and the only estate-wide `daemon` hit is a COMMENT at `release_sidecars.bats:133`. **AC-06.1's ordering rider binds `config` ALONE**, so writing `daemon`'s tests late is legal where writing `config`'s late FAILS the clause. That makes the daemon suite (M) the one row I can move with no dependency.
- **WP-08 IS 12/12 GREEN AND `wp done` IS AVAILABLE TO ME RIGHT NOW (XS). I HAVE NOT TAKEN IT.** Closing it would mark the daemon DONE while its command family carries zero conformance coverage -- a green over an uncovered surface. That is a SEQUENCING call for hv and distinct from _does daemon ship uncovered_, which vc already has on the desk.
- **THE TWO 3.0.0 ARTEFACTS STILL DIFFER -- RE-DRIVEN, and this board's warning is LIVE not stale.** PATH `intent` is a SYMLINK to the tree binary, sha `9bae2a04`, naming `f6d37b18`; the keg at `3.0.0_1` is sha `04e7dda8`, naming `80d8b2ca`. **Pin by the hash, never by the version string.**
- **`AC-06.1` IS RED ON TWO NAMED HOLES AND NEITHER IS MINE ALONE.** The 27 UNSTABLE are established (four causes, 25 of them `125f601d`, AC-12.1's cutover prune); the read is `parity/ac-06_1-unstable-causes.md`. What is left is the COVERAGE half: with the corpus covered, `coverage_map` publishes and finds **`config` and `daemon` at 0 REAL / 0 TESTS**. Verified independently of the tool -- `tests/unit/config.bats` never invokes `intent config` (it runs `info`, `st list`, `doctor`), and nothing in `tests/` invokes `intent daemon`. **`config` IS AN ORDERING CONSTRAINT WITH AN EXPIRY, NOT A BACKLOG ITEM:** the row requires the test to land BEFORE the behaviour is designed, so writing it later FAILS the clause. **`daemon` is new surface in a tag window** -- v2 had none, so the conformance frame was never going to catch it; with hv via vc. **NO BASELINE WAS INSTALLED** -- figures came from a worktree copy and are re-derivable.
- **`AC-10.5` CHAINS BEHIND WP-14, WHICH IS UNOWNED AND UNSTARTED (L).** Not apparatus work: `fleet_corpus_conservation.sh` runs clean at 4 run + 0 unrun, all verdicts recorded at `dbb736ce`. Red on substance -- the MIGRATION does not name its residue. **`0183`'s ORIGINAL REMEDY WAS WRONG AND IS CORRECTED:** 110 of baize's 135 are `intent/whiteboard/`, which LEFT the not-modelled set at **D30** (hv, WP-14 carries it). Not out-of-model; **IN the model and NOT YET BUILT**. Naming them would zero the loss by asserting the project meant to abandon what hv ruled it would carry. No output formatting closes this row.
- **`AC-06.8` IS NOT MINE AND MOVES ON `0181`.** Driven: `agents init --template` reads `retire` (vc's `0180` ruling is EXECUTED, not merely ruled); `llm usage_rules --symlink` is still `keep` and unread, and that is hv's. `ingest --from-md` is ratified. **Do not sequence anything behind me on it.**
- **THE `hoist` ROW'S RE-PIN RULE IS STALE AND UNEXECUTED.** It names `canary`, `lamplight`, `utilz`, `baize` as _repositories whose migrations have NOT run_. **All four declare 3.0.0 on disk**, canary included because canary IS this repo. Re-pinning any to HEAD would point the row at a tree with no v2 source -- the exact harm the rule prevents. **DO NOT RE-PIN.** `parity/tools/` is ic's, so the edit is coordinated, not taken.
- **THE MCP DISPOSITION IS DECIDED, NOT DEFERRED** (vc, on hv's ruling). **SEVERITY EXPOSED** -- a classification over an enumerated domain, like the status fields already exposed. **TITLE WITHHELD** -- authored text, so the body's withholding argument reaches it. My split was right; what was wrong was parking it _until after the tag_, which was the phantom hv struck.
- **THE MIGRATOR DOES NOT COMMIT, SO THE DOCUMENTED ROLLBACK HAS NO SUBJECT.** `migration.md` Phase B step 7 says _one commit_ and `AC-00.8` says _one visible commit_; measured, `intent upgrade` returns rc=0 with HEAD unchanged and 23 paths dirty. **_One visible commit_ is an OPERATOR CONVENTION, not a migrator guarantee.** hv's call -- a behaviour change in a tag window.
- **`0205` IS THREE BLOCKS FOR ic AND A FOURTH THAT IS NOT OURS.** `lib_classify.sh` x2 + `drift_check.sh` hold authored prose in plain double-quoted shell strings; `bin/.devbin/lib/builtins:66` does too but is VENDORED under an integrity manifest, so it goes upstream or is accepted. **The remedy is the QUOTED HEREDOC, never single-quoting** -- single-quoting moves the hazard to the apostrophe, which is likelier in prose than a double quote.
- **`0206`'s HARNESS EXISTS; THE FIX DOES NOT.** `canon_race_check.sh` (`649cb542`, roster `manual`) drives it: same-thread 6 lost of 10 trials, cross-thread 0, zero non-trials, on the SHIPPING binary. **INTERIM DISCIPLINE, adopted estate-wide: announce before a canon verb on a thread a live peer is working, and commit in the same breath.** The canon gate's file-and-canon-together rule already delivers most of it.
- **WP-10 IS THE CHOKE POINT FOR vc's LARGEST BLOCK, TWO PACKAGES DEEP.** `AC-00.2`, `AC-00.8`, `AC-00.10` sit behind WP-10, which its own cover makes dependent on **WP-06 (mine) AND WP-07 (dc's)**. Read off the cover, not inferred.
- **`0201` IS ROOT-CAUSED AND DISPOSITIONED; `0202` IS CLOSED AS NOT A DEFECT.** `0201`: `bin/intent_critic:225` calls `find_project_root` without sourcing `bin/intent_helpers`, and `2>/dev/null || true` turns the missing symbol into a silently empty project root, so `.intent_critic.yml`'s `disabled:` is dropped. Deterministic, 3 runs each way. **v3 does NOT inherit it** -- driven three arms on the shipped pair (no config -> 4 findings; disabled that rule -> 0; disabled a DIFFERENT rule -> 4, which is what makes the zero mean anything). So it is v2-only and **dies with the prune**, taking `treeindex_commands.bats`'s disposition; neither remedy I first offered was right. `0202`: driven both sides -- v2 `claude upgrade` is a pruned plugin script, v3 writes `.claude/settings.json` byte-identical to the template. **It was the prune all along, and my `MIXED IN CAUSE` note in OVERRIDES was struck** -- I had split the file on the failing ASSERTION instead of the command two lines above it. **AC-06.1's read has TWO non-prune causes, not three.**
- **UNDISPOSITIONED, ALL hv's VIA vc:** `0086` and `0063` would have to be WRITTEN, not recovered -- a different task, cost and author. `0095`/`0096` are one-word stubs needing a disposition. `0192` (post-banner loss, rewritten by dc at `16cdd671`) -- **if hv rules it in, the refusal goes in `info_read_back`, NOT `authored_regions`**, so truncation keeps its single purpose.

## Watch-outs

**THE INSTRUMENT ANSWERED A DIFFERENT QUESTION, AND THIS IS THE FAMILY THAT COST MOST.** Four spellings, all live: a SELECTOR that resembles the population is not the population (my JSON walk read 61 exposed rows against ic's 60 by descending into `flags`; `shipped_entries` is the production selector and EXCLUDES flags -- and the same walk later found a "peer's path" in a canon diff that was a string INSIDE `runner_roster_check.sh`'s text blob, because the roster names every tool). `--help` RENDERS FOR A DECLARED COMMAND WHETHER OR NOT AN ARM EXISTS -- it produced three wrong readings in three hands in one evening and is baked into `AC-06.3`'s ruled text; **invoke the verb and take rc without a pipe**: bare `claude subagents` is rc=1 needs-a-subcommand, `list` is rc=2 NOT IMPLEMENTED, `--help` is rc=0 prose. `st show` prints id/title/status/created and NOTHING ELSE, so a correct drive can report a field missing while the db holds it. And `bash -n` is NOT a check on a quoted data block -- a literal `"` closes a double-quoted string and turns prose into shell, and the result is still valid bash (`0205`). **The general form: read what the tool actually reads, and positive-control the INSTRUMENT before believing its number.**

**A CONTROL THAT CANNOT DISTINGUISH _SAFE_ FROM _NEVER TRIED_ IS NOT A CONTROL, AND A GREEN OVER A CORPUS THAT CANNOT EXHIBIT THE CHANGE IS NOT EVIDENCE.** Drive an instrument to BOTH verdicts or its green means nothing, and the control must exercise the variable that can be wrong. The `0206` harness is the sharpest instance: a clean cross-thread run could equally mean the two edits never overlapped, so the arm was rebuilt to MEASURE overlap -- start gate, `$EPOCHREALTIME` either side of each exec, iteration counted as a TRIAL only when the intervals intersect. **State the bound's direction**: the wall interval is wider than the load-to-apply window, so trials are an UPPER bound and the loss rate a LOWER one -- it cannot flatter the finding. **A control you had by luck is not a control you can rely on** -- the corrupted OVERRIDES was caught only because an untouched row came back empty too, and an empty table would have shipped it.

**ANALYSIS GENERAL, PATCH INSTANCE-SHAPED, AND THE QUALITY OF THE WRITEUP IS WHAT MAKES IT STOP** (vc's formulation, four instances in a day). `apply_envelopes` carries the against-the-DISK-not-against-self.canon reasoning, correct and general, scoped to an authored body. `whiteboard-clock-guard.sh` documents BSD's unspecified-field fill at length and fixes the DATE path while leaving HH:MM carrying it. `0200`'s hunk comment named exactly the right question and addressed it to a human reader, where only a TEST looks -- **a comment cannot discharge an obligation to a mechanical check.** And `0205` enumerated the three blocks its author had tripped over while describing the general mechanism. **A hedge in a message is not a measurement**: I said the population "might" be larger instead of running the one grep, and it was.

**THE SHELL LIES ABOUT SUCCESS IN SEVERAL WAYS ON THIS BOX.** `cmd | tail` reads tail's status; `cmd > log; echo rc=$?` reads the echo's. zsh has no `PIPESTATUS` (`$pipestatus[1]`), does NOT word-split `$var` (it bit me twice more yesterday -- a probe passed three words as one argument and reported `unrecognized subcommand`), and an unmatched glob ABORTS the command. `grep` is `ugrep`. `sed -i ''` SUCCEEDS on no match. `read -t N </dev/null` returns INSTANTLY at EOF, so a "wait" loop built on it does not wait. `find` is bfs, not GNU find. **cwd PERSISTS between calls -- use absolute paths.** And **suppressing stderr discards the diagnosis**: `2>/dev/null` on a source that was printing `command not found` returned an empty lookup I nearly recorded as data.

**THE SHARED CHECKOUT: CANON CANNOT BE SPLIT, SO EVERY CANON COMMIT HERE IS SILENTLY A MULTI-NODE COMMIT.** `git commit` commits the index AS IT STANDS, and the canon extract regenerates WHOLESALE from the store, so a peer's in-flight edit lands in your staged canon without either of you acting. Measured both ways in one hour. **`--only` is the only safe write; verify composition by parsing both canon versions and diffing the attachment path->sha maps, NOT by grepping the diff text** (that is how the roster's text blob faked a peer path). **Declare it when canon must carry a peer's bytes.** A live `index.lock` is a WAIT, never a removal; never unstage a peer's path; never `--no-verify`. **PROVENANCE IS CARRIED BY PATH** -- every node commits as the same person, so the `reporter` field cannot answer _who wrote this_. **I do not write into a peer's issue or area.**

**A VENDORED DIRECTORY WITH AN INTEGRITY MANIFEST IS NOT YOURS TO FIX.** `bin/.devbin/` is re-vendored wholesale and `manifest.sha256` records `lib/builtins` at a hash that MATCHES the file. An in-tree edit breaks the manifest AND is overwritten at the next re-vendor -- it passes review, looks fixed, and comes back. **Check for a vendor manifest before assigning work in a directory you did not author.**

**CONSTRUCTING BEATS READING, MEASURING BEATS BOTH, AND THE DISPROOF IS USUALLY ALREADY IN HAND.** Things that read as settled and died when driven: the keg has no `issues edit`; the migrator does not commit; `st attach` never writes the disk file; all four corpus members are already migrated; three of the five "pruned" commands still ship. **And three times the refutation was already written down, filed under someone else's question** -- `0082` held `0199`'s mechanism while two nodes budgeted a fixture to hunt it; `sync.rs:148` refuted `0183` while `0183` was being filed, at the site. **Read the code you are filing ABOUT.**

**FACTS THAT LOOK LIKE OPINIONS.** A thread must be REALISED or it has no cover -- `st new` + `sync --to-disk` yields canon and no view; `st start` realises it. `intent:///` is scheme, EMPTY authority, path. An address ladder is walked IN FULL and the first hit does NOT win (precedence would silently pick the thread for 48 of 69 numbers here). `SERVED` is not a Highlander violation (`mcp_surface.rs:30` says so on purpose). **A non-test AC is never COMPUTED from a covering AT** -- it takes `intent ac satisfy` by named evidence; 23 of 27 non-test rows carry an AT anyway, so an AT is neither necessary nor forbidden. **`intent st attach` writes the store AND the canon extract and NEVER the disk file** (`0082`); the daemon watches DISK, so a disk-side edit converges everything and a store-side write renders nothing back.

## Decisions

- (2026-08-31) **A CRITERION IS NOT REWORDED TO WHAT THE CURRENT STATE SATISFIES** (vc, on AC-06.3). Rewording to match reality is how a green stops meaning anything; the ruled form must be HARDER than the original and bind the node holding the divergence.
- (2026-08-31) **A CLASS CHANGE WITHOUT ITS REASON IS A DELETION WEARING A NEW LABEL** (vc). A reclassification is a CLAIM ABOUT WHY: `deviate` must RETARGET rather than delete, and every row carries basis + note. Recorded where the artefact is GENERATED FROM, or the evidence clause cannot be checked against it.
- (2026-08-31) **A RULING THAT AN EXISTING RATIFIED DECISION COVERS A CASE IS ADJUDICATION; MINTING A NEW D-NUMBER IS A DESIGN ACT** (vc, on D10). What the rule forbids is supplying a PLAUSIBLE reference -- a traced chain (shared operative clause + a commit naming the AC) is evidence, not resemblance.
- (2026-08-31) **A DECLARED EXCLUSION CARRYING ITS REASON IS THE CURE; A SILENT DROP IS THE DENOMINATOR ATTACK.** Naming everything out-of-model would zero the loss; the difference is entirely whether the reader is told, so an exclusion with no reason is itself a failure.
- (2026-08-31) **AN OWNERSHIP SPLIT IS A PURPOSE, NOT A BOUNDARY** (ic, correcting me). Reverting so a peer can rebuild byte-identical code is churn wearing Highlander's clothes. `AC-09.6` is the split done right -- two halves, two hands, two commits, one order, nothing red in between.
- (2026-08-31) **A CONTRACT WITH A CONSUMER IS ASKED FOR, NEVER MINTED.** ic named `daemon status --format json`'s shape and I built that; `terminal` and `json` are real and `zzz` refuses at rc=1 naming the roster.
- (2026-08-31, CORRECTED) **`close --note` STAYS RULED OUT ON A CONTINGENT FOOTING, NOT BECAUSE THE CAPABILITY SHIPS.** The two-verb path exists ONLY on the tree binary; the keg has no `edit`. The ruling survives because v3.0.1 ships from the tree -- it was never redundant on what users have today. **Two 3.0.0 artefacts differ, so _does X ship in 3.0.0_ is unanswerable from the version string.**
