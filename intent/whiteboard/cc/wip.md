---
node: cc
name: Control Claude
role: control
session_id: 6551ed66-8c7f-4dbe-b512-eb5b65a0ca60
heartbeat_at: 2026-09-17 18:35Z
status: active
focus: "2026-09-17: issues 0442, 0443 and 0444 are cc's and outrank the rest. 0442's mechanism is found and banked -- one phantom document, rowid 2599, a partial delete through the scoped door -- and vc's order is the discriminating experiment FIRST after the compact, separating SQLite 3.46.0's secure-delete writer from rung 26's mixed state, then the rehearsals, then the notes. The 0438 check is green on the rebuilt pair at 58501a8a2. All nine release-notes readers are in and banked. Resume at doing 13. NO RELEASE, NO PUSH."
claims: []
---

# Control Claude (cc)

## DOING

- **RESUME HERE (cc, 2026-09-17, localfold for hv's compact, the third of the day).** No claim. **ISSUE 0442 IS CC'S AND OUTRANKS EVERYTHING ELSE** (vc, filed at 58501a8a2, high): the live store's FTS5 index for `src_sections` carried stale postings, so a query walking the damaged segment returned rows and then aborted, and doctor read 0 findings over it. **THE DAMAGE IS REAL, AND THE LIVE STORE HEALED ON ITS OWN.** dc read the damaged copies under three SQLites -- 3.46.0 `malformed inverted index`, 3.54.0 and 3.53.4 `fts5: checksum mismatch` -- so SQLite 3.46.1's fix ("in FTS5 secure-delete mode, fix false-positive integrity-check reports about corrupt indexes") does NOT explain it and a lying checker is ruled out; a SELECT on the copy also aborted after 4 rows where a rebuilt copy returns 6. **AND THE HEAL HAS AN AUTHOR: vc RAN `intent index rebuild` ON THE LIVE STORE at 18:15:45Z to 18:16:03Z**, which is dc's healing window between source mtimes 1789668343 (damaged) and 1789668963 (ok). dc read the window without knowing of that run and recorded it as self-repair; it is not, and nothing yet says this can recur on its own. cc ran no `index rebuild` and no verb that writes the index: this session's only `intent` runs were one `wb pickup`, `--version`, `st list`, two `ac gate`s and `daemon status`. **THE LIVE STORE IS REPAIRED, SO THE DAMAGED BYTES EXIST IN EXACTLY TWO FILES AND NO NEW COPY CAN BE TAKEN** (dc's corrected list, both read-only, both still reading `fts5: checksum mismatch for table "src_sections"` under 3.54.0, under `/private/tmp/claude-501/-Users-matts-Devel-prj-Intent/dfe2e58c-f5c6-4ebc-b5d4-bfea6e74dac1/scratchpad/guards/evidence`): `damaged-page-exact-1789668041.db` (with its `-wal` and `-shm`), page-exact at capture though doctor, `claude upgrade --apply` and one commit gate ran against the clone it sat in afterwards; and `damaged-logical-1789665647.db`, a `VACUUM INTO` of dc's first page-exact copy carrying the one `v_probe` fts5vocab table dc added, read only since. Both sha256s are in the mechanism note. **THERE IS NO PRISTINE PAGE-EXACT DAMAGED COPY, and dc said so rather than let a test run against a file believing it is one.** `evidence/repaired-page-exact-1789668963.db` is the repaired store, and dc's commands and driver are banked at refs/bank/dc/guards/ (`rehearse.sh`, `check.c`, `vocab-diff.txt`, `summary.md`), the driver being a C program against the vendored 3.46.0 amalgamation. **ITEM 4 IS MEASURED AND IS NOW ISSUE 0443, cc's, ruled beside 0442** (vc filed it on cc's evidence): a plain `intent search` IS a write path by design, because `served` answers in-process unless `--daemon` is passed (render.rs:440) and that closure calls `index_refresh(None)` over the whole scope unless `--no-reconcile` (render.rs:3703), so where intentd is running and watching a plain search still writes; and facade.rs:3843 maps a store error out of `search_hits` to `BadQuery`, whose remedy talks about FTS5 syntax, so a corrupt index met in a query reads as the operator's own query being malformed, which is the worse half. Watch-out 49 carries both, with the xIntegrity fact behind them. **THE MECHANISM IS FOUND AND BANKED at refs/bank/cc/0442/mechanism.md**, measured on cc's own copies of dc's damaged and repaired stores under `sqlite3` 3.54.0, read-only, no `intent`: **IT IS ONE PHANTOM DOCUMENT, POSTINGS WITH NO CONTENT ROW, AND THE DELETE THAT MADE IT WAS PARTIAL.** The damaged content table holds 829 rows over 829 paths and the repaired one 830 over 830; the one path in the repaired content and missing from the damaged content is `native/rust/crates/intentsvcs/tests/a_sync_writes_no_render_the_store_has_moved_past.rs`, with nothing missing the other way, and the damaged index names 829 documents of which rowid **2599** has no content row. In the repaired store that file is a 2,475-byte body carrying 141 distinct terms; the phantom retains **122** of them, which is exactly dc's 122 terms naming one more document each with none fewer. **`argmatches` is one of the 122**, so dc's symptom is explained end to end: the doclist names doc 2599, the content row is gone, fetching it for `highlight()` raises SQLITE_CORRUPT_VTAB 267, and the rows already produced come back first (4, then the abort, where a rebuilt copy returns 6). The phantom's reported instance count is far too large for that body, so its surviving position lists are mis-decoded too. Innocent asymmetry: rowid 1731 is path `Icon\r` with a zero-length body, so it has no postings and is not a second fault. **THE WRITE**: that file is 0441's own arm, landed at c649596ef 17:47:53Z while the damaged copy's source mtime is 17:20:47Z, so it was in the watched tree, indexed, and gone again before it was ever committed -- what a bank-and-restack does to a file in the live checkout -- and its removal asked the index to delete the row through the scoped door `Store::replace_sections_for` (store.rs:5618), secure-delete on with no `'rebuild'` after it. The wholesale writer cannot be the producer, because it empties the content table and rebuilds from it, which is why `index rebuild` repairs this in one pass. **H1 IS DISPLACED BY THE SINGLE-ROW SHAPE**: a moved column set or tokenizer would spoil every later delete, not one. What remains is **H2, upstream's write side**: 3.46.0's secure-delete may fail to remove postings, which the 3.46.1 note does not cover, since it names the checker alone; dc's `check-3460` driver plus this machine's 3.54.0 is the pair that separates it. **H3, the mixed state we built**: rung 26 turned secure-delete on without a rebuild (store.rs:1771), so pre-26 delete markers sit in the same index as secure-deleted pages, and the scoped delete at store.rs:5618 deletes by path with secure-delete on and no rebuild. THEN the fix, sized, and DETECTION: doctor gains an fts5 `'integrity-check'` finding over both tables with `intent index rebuild` as the remedy, advisory rather than blocking because the entities are intact and the index is derived, its cost measured first because doctor runs on the pre-commit gate. dc measured `intent index rebuild` returning a copy to a clean check in 18 s with the symptom cleared; the live repair runs before vc's rebuild, on vc's word. THE RELEASE NOTES ARE IN FLIGHT AND SECOND TO 0442 (vc, 2026-09-17): the release command refuses a cut with no `## [X.Y.Z] - in progress` section and nothing since v3.0.3 is written, so cc banks the CHANGELOG section and `docs/releases/<version>/RELEASE_NOTES.md` in 3.0.2's shape with an Upgrading section, the version left a placeholder for hv. Nine read-only readers work from `scratchpad/relnotes/BRIEF.md` over the 124 issues closed since v3.0.3 (`issues-since.tsv`), ST0076, ST0075, the commits naming no issue (`subjects.txt`), the upgrade facts and the known-defects page against HEAD, each writing one `slice-*.md` beside the brief with every claim citing the as-built; `slice-search.md` and `slice-wb-migrate.md` are in, and its three merge notes are that 0370 and 0430 are one entry, 0440 belongs inside ST0076's `index resolve` entry rather than as a fix, and the schema-rung refusal (27, 28, 29) is written once for the release. 0441 is landed at c649596ef and goes in. **THE REBUILD IS IN -- the pair is `intent 3.0.3 (58501a8a2...)`, which is HEAD, its in-scope unbuilt paths are 0 and intentd answers on the same commit -- AND THE 0438 CHECK IS DONE AND GREEN ON IT**: Courses hv carries 0 items with 4 `rendered:` files, 0 uncarried and rc 0, so **0439's refusal is gone**; Conflab cc refuses the 15 units its board genuinely offers, then `--drop-uncarried` carries 31 items, 3 messages and 27 snapshots with 15 dropped, and all three kept inbox copies are byte-identical to the live estate's, so **0438's line is kept**. Against the banked-binary records each estate differs by one line, its own heartbeat stamp. Next after 0442: `bash scratchpad/carry/rehearse-all.sh` (the ten rehearsals with this pair, then report.py and plan.py), then the notes. The carry itself waits on hv's rulings, with HOLDS=ask,table vc's recommendation. NO RELEASE, NO PUSH.

## TODO

- Read the lane column in `intent/wip.md`, never a copy here.

## Holds

- **POST-CUT:** `ext` x5, `learn`, `config` x3 ship declared-and-unbuilt (hv, 2026-08-31). **Released when hv opens work after the 3.0.1 cut**; `0177` is post-cut with no owner.

## Watch-outs

- **TRAPS WITH NO OTHER HOME (cc, localfold of 2026-09-17, the second of the day; the rest live in restart.md, a shared memory, a test or a guard).** A red arm must COMPILE on its base: assert on Display text or an existing variant. A leftover-code grep uses -F: an unescaped .generated() matched fn generated(). A landing symbol check greps a short token rustfmt cannot split. In scripts, no apostrophe inside ${1:?word}, and zsh runs a bare ==== as a command. A worktree sync-then-attach restores only whiteboard inbox renders and stops on any other tracked move. The critic gate lints the rule library's own bad examples, so that pair cannot be committed at all. `grep` on this machine is ugrep, and a long `.{0,N}` repetition fails with "exceeds complexity limits" instead of matching. `.gitignore` ignores `target/` only, so a worktree build must use the in-tree `native/rust/target`: a `target-cc` directory is not ignored and would ride into a `git add -A` bank patch. An unsynced bank stack reds attachment_drift_detected and thread_prose_carried whenever a bank carries attachment documents without their thread's canon: judge those two on the landed train, where each landing synced canon, never on the stack. `testkit::repo_root()` is the WORKTREE's root, so an estate test reads the tree it runs in. `at edit --note` replaces a row's note outright, and `at green/red/na --note` never appends either: it refuses with NoteWouldBeLost unless the passed text CONTAINS the existing note. `intent issues edit --from` replaces an issue's whole prose too, so a close note is the old body plus the note. A new AT row's cited file must carry the row's literal id, and `at new` and `at green` never read a citation (issue 0267), so only `at lint` and the gate see a missing one. A new row starts `to-write`, and `at green` is legal only from `red` (issue 0337). A daemon cost run needs an estate the size of the real one: a hookless clone of the repo is the scratch estate. `git grep -E` has no `\b`: search for a literal with `-F`. A script that runs `git -C <worktree> apply <patch>` resolves a relative patch path inside the worktree: pass it absolute. `sqlite3 -readonly` cannot open a WAL store that has no -shm beside it (error 14): read a store nothing writes as `file:<path>?immutable=1`. A CLI arm run from inside a Claude Code session inherits CLAUDE_CODE_SESSION_ID, and since 0433 a `wb pickup` with no `--session` records it, so an arm that asserts a session removes the variable. `rust-analyzer scip` has no switch that stops it running build scripts and proc macros (its `--config-path` settings change nothing, measured), without CARGO_TARGET_DIR it builds into the workspace's own target, and its cargo metadata step writes a Cargo.lock into a crate that has none. NEW 2026-09-17: a new verb row and new DDL text need the WHOLE suites, because targeted runs green across 263 arms missed three reds (the shipped-mutator roster in write_moves_only_what_changed.rs, and two no_pm_state_in_output arms); a tracker id (ST, WP, a ruling) never goes inside the DDL literal, which `intent schema ddl.sql` prints, so provenance belongs in the rung's Rust comment. The critic gate's IN-RS-CODE-004 proxy reads `Result<BTreeMap<String, String>, E>` in a turbofish as `Result<T, String>`: type the binding instead. `git reset --hard` in a worktree leaves a bank's NEW files behind as untracked, and `git apply --3way` then refuses them ("does not exist in index") and applies nothing until they are removed. A ref name may not end in `.lock`. cargo prints `Compiling`, not `Checking`, for a crate with a build script, so a gate script counting clippy members must accept both. A peer's git can hold `.git/index.lock` for a moment in the shared checkout: retry the commit once. A JUDGED PATCH-ID IS WHAT LANDS (vc, 2026-09-17): when the gate refuses a judged bank, stop, make the change in the worktree, re-bank, and send vc the new patch-id and the delta BEFORE committing, even for two lines.
 NEW AT THE THIRD FOLD OF 2026-09-17: a test-kind criterion cannot take `ac satisfy`, which refuses it with ComputedSatisfaction: it reads satisfied once every row covering it is green. The installed `intent` reads the rule library live from this checkout (the home pointer names it), so a RULE.md fix is in force at its commit, with no rebuild or sync. A proxy pattern change can WIDEN what the gate strikes: measure the old and new strike sets over every tracked file before choosing (0437's bracket-balancing pattern would have added six findings in four files; `[^<>]` is a strict subset of `[^>]`). A helper two suite files share goes in tests/common, because no suite file imports from a sibling. `sqlite3 <db> ".backup"` opens its source read-write, creating -wal and -shm and checkpointing on close: read a quiescent estate's store through `file:<db>?immutable=1`, with its mtime and -wal read before and after and `integrity_check` on the copy (vc's guard), and a live store through `?mode=ro`. `git clone --shared` gives a scratch clone that writes nothing in its source. Until 0438 and 0439 land, `wb migrate --drop-uncarried` snapshots only `wip.md` and loses a dropped inbox line, and a registered node's rendered views are read as hand-authored. zsh ran a bare `======` as a command twice more today: never print a separator that way.
- **TRAPS FROM THE LATER SESSIONS OF 2026-09-17 (cc's localfold before hv's compact; watch-out 47 holds the earlier ones).** `intent critic rust --files` takes ONE path per flag, repeated: several paths after one flag are refused as an unexpected argument, and a JSON reader of that refusal then fails to parse. `intent issues close` removes the issue's `.intentfiles` line and leaves its view on disk until `intent organize --apply`, and the deletion rides in the close commit. A ref cannot be created where a directory of refs already exists: bank under a sub-path such as `<issue>/patch`. `issues close` can warn that it overwrote canon bytes that were not the store's render: verify past the ingest that store and canon agree and doctor reads 0 before calling it a defect. A judge script's `cargo clean -p <members>` deletes the worktree's `intent` binary, so a scratch-clone rehearsal after a judging run needs `cargo build -p intent-cli --bin intent` first. A bank re-issued under the same ref can touch files the message about it does not name (dc's WP-06 re-bank changed the register as well as the Elixir files): diff the old and new bank per file before restacking on it. A register row is ONE JSON line, so two banks that edit the same row never apply over each other: re-apply the string replacement on the new base and regenerate the view. `wb migrate` of a node whose files are all rendered views still re-renders every stale whiteboard view in the estate (Courses: 16 of its peers' views), and its commit carries them. intentd answers `{"root":"/","op":"registry"}` on the socket `intent daemon status` names with every registered estate's `watched` and `ingested`, opening nothing (carry/quiet.py). zsh aborts on an unquoted glob in an argument, so `grep --include=*.rs` needs its quotes. Watch-out 47's apostrophe inside `${1:?word}` bit again in a new script: `bash -n` every script before its first run. Watch-out 47's 0438/0439 trap now reads: until the INSTALLED pair carries eac280b50, its `wb migrate --drop-uncarried` still snapshots only wip.md and still reads a registered node's rendered views as hand-authored.
- **TRAPS FROM THE FTS5 AND RELEASE-NOTES SESSION (cc, the third localfold of 2026-09-17; watch-outs 47 and 48 hold the earlier ones).** **A CORRUPT DERIVED STRUCTURE JUDGED ONLY BY THE PAIR'S OWN SQLITE CANNOT BE JUDGED AT ALL**: `PRAGMA integrity_check` has called a virtual table's own xIntegrity method since SQLite 3.44.0, so an integrity verdict on this store IS fts5's checker, and the pair bundles 3.46.0 (`libsqlite3-sys 0.30.1`), the one version whose FTS5 checker is known to report false corruption in secure-delete mode (3.46.1's only FTS5 note). Read the same object under this machine's `sqlite3` as well, which is 3.54.0, and prefer a reading a lying checker cannot produce: what a query RETURNS, and a term-dictionary diff against a rebuilt copy. **A REPAIR ERASES THE EVIDENCE, AND THE REPAIR IS ONE COMMAND**: vc's live `intent index rebuild` cleared this fault in 18 s and left the damaged bytes in two copies only, one logical and one page-exact at capture but written against afterwards, so there is no pristine damaged copy to test. Before repairing a corrupt derived structure, take a page-exact copy with its `-wal` and `-shm`, `chmod a-w` it, record its sha256 and the source mtime, and say plainly what has touched it since. **AN UNEXPLAINED HEAL WITH NO NAMED AUTHOR IS NOT SELF-REPAIR**: a ten-minute window between a damaged and a clean reading was reported as the store healing itself, and the author was a peer's live `index rebuild` inside it. Ask every session that could have written before recording a heal, because "it can recur on its own" is a much larger claim than "somebody repaired it". **ON THE SEARCH PATH A STORE ERROR AND A QUERY ERROR ARE DIFFERENT MESSAGES, AND THAT IS HOW YOU LOCATE AN ABORT**: `facade.rs:3843` maps any `StoreError::Sqlite` out of `search_hits` to `BadQuery`, whose remedy talks about unbalanced parentheses, so a corrupt index met in the query is reported as the operator's own query being malformed; "could not update the runtime store" is `FacadeError::Store` (facade.rs:1337), which the query path cannot raise, so that wording means the RECONCILE WRITE. **AND A PLAIN `intent search` IS A WRITE**: `served` (render.rs:440) answers in-process unless `--daemon` is passed, and that path calls `index_refresh(None)` over the whole scope unless `--no-reconcile` (render.rs:3703), even on an estate a daemon is already watching -- so no `intent search` inside a quiet window, and a verb being a read in ordinary speech is not evidence it is one (issue 0443). **A SHARED CHECKOUT MOVES UNDER A LONG READ**: HEAD went from 8441b9dc2 to 58501a8a2 while a reader was working through it, so any `path:line` taken before the move is re-derived afterwards or dropped, and the reader says which. **A HOLD'S SEQ DEPENDS ON THE SELECTION**: the carry rehearsal numbers holds in manifest order, so a rehearsal record made with every reading cannot gate a live carry run with hv's ruled subset -- re-run the rehearsal with the ruled `HOLDS` immediately before the carry.

## Decisions

- vc, 2026-09-13: the generated views a verb re-renders, boards and inboxes included and every node's, land in the commit of the verb that rewrote them. Nobody owns a generated file.

---

_Generated by Intent v3.0.3 from the whiteboard model. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
