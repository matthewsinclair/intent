---
node: cc
name: Control Claude
role: control
session_id: a674007a-9009-4ba1-80e9-3fbc159c18b0
commit_session_id: NOT YET READ -- this session has authored no commit, so there is no trailer to read it off. The harness offers a value and the rule (0208) is that it is READ off a commit rather than reconstructed, so this line stays honest rather than plausible until the first commit lands. The prior session's id was 01NfUTqHUpjLCQDAYnqUMkwH and it is archived with that header, not carried forward: it names ONE session and a restart mints a new one.
heartbeat_at: 2026-09-03 07:42Z
status: active
focus: "PICKED UP 2026-09-03 07:33Z. THE FIRST VERB I RAN REFUSED: `intent ac gate ST0056` no longer reports a count, it emits ONE acceptance-test CONTRACT finding over 173 rows, and the cause is MY OWN commit b67a9bc7 from 21:06Z last night -- one_declaration_two_realisers.rs cites AC-17.1 and never AT-17.1. I closed the row green and never re-drove the gate that reads it. A CONTRACT FINDING PRE-EMPTS THE COUNT, so the AGGREGATE refuses -- but `ac list` does not route through the check and the figure reconstructs from it, so _no reader at all_ was too strong and I said a version of it first. XS, mine, UNSTARTED -- still parked. THE PAIR IS CURRENT, driven two ways; the rebuild-window ask is closed on its INSTANCE and the arm that should have shown it could not have -- see watch-out 1. NO FIGURES HERE; RUN THE VERBS."
claims: [ST0056/06, ST0056/08, ST0056/10, ST0057/00]
---

# Control Claude (cc)

## DOING

**NOTHING IN FLIGHT. PARKED BY hv, HOLDING.** Nothing below is started and nothing starts on a peer's word.

**THE EVENING WENT ENTIRELY INTO `0216` AND IT IS DRIVEN END TO END. THE DETAIL LIVES IN THE ISSUE, NOT HERE** -- vc's commits `5f80e395`, `bf999002`, `4c940ef6`, `46f97c14`, `d82a9e55`, `6d2a5666`, `453a4f7b`; my board narrative is archived verbatim at `.history/20260902/wip-prefold-2312Z.md` (`2ebce483`). **THE SETTLED STATE: a canon write LANDS and `intentd`'s disk ingest REVERTS it, so the `ok` is honest and two actors race.** Debounce is 250ms read off `watch.rs:51` and resets on every event, so the ingest fires after the LAST write. **Whether an ingest FIRES is the gate; given one does, a depth-3 burst loses its last write essentially every time (3/3 tonight, 5/5 corpus).** Census is a POPULATION not a sample -- the log row is gated `if !wrote.is_empty()` at `facade.rs:3344`, and a revert is by definition an effect. **Six losses, all repaired, ZERO unreachable rows. FOUR candidate mechanisms REFUTED: flush window, coalescing, ingest overlap, and both of my depth hypotheses. THE MECHANISM IS STILL UNKNOWN and the write-up says so.**

**THE EXPOSURE IS LIVE AND IT IS NOT OURS TO MITIGATE.** It fired three times tonight across three nodes -- `AC-17.17` and `AT-00.8` (vc's), `AT-00.7` (ic's) -- half the corpus's losses in one session, while we analysed it. **The 250ms floor is AGENT TURN LATENCY, not process cost: a read-only `intent` is 23-26ms.** So four LLM nodes cannot get inside the debounce and **a shell loop does it trivially** -- `for t in ...; do intent wp new "$t"; done` is ordinary documented usage. **A hazard reachable by an ordinary shell loop cannot be fixed by usage discipline, because the discipline would have to be _do not script this tool_. THE FIX IS DAEMON-SIDE: an ingest must not revert store state newer than the disk it read.** Migrators are SAFE (one planned batch; `AC-10.2`), which was the scenario that would have shipped.

**MINE, RULED, AND UNSTARTED, IN THIS ORDER (settled with vc; goes to hv as MY RECOMMENDATION with vc's agreement, never as a vc ruling over hv's park).** (1) **Dispositions 6-9** -- small, ruled, independent, and clearing them shortens hv's list: `0086`/`0063` and `0095`/`0096` DROP; `0192` RULED IN with the refusal in `info_read_back`; `0205`'s vendored fourth block ACCEPT with the reason at the site; the migrator-commit contradiction corrected in doc and row, **NOT** built as a commit. **Disposition 5 is RETIRED** -- the mechanism work overtook its premise. (2) **`AC-10.5`'s residue re-disposition** -- ratified by vc, who declined to review it first; residue is modelled in ST0069, not this release, and hv's 08-30 descope MOVED the carrying rather than dropping it. **It no longer chains behind `WP-14`.** (3) **THE ARM.**

**THE ARM, AND hv IS OFF ITS CRITICAL PATH.** Its old justification (_enter the unsampled regime_) is dead -- we were in that regime all evening. **What is unsampled is a DEEP burst MEETING an ingest, which needs the daemon RUNNING, not stopped** -- the opposite of the window vc was going to ask for. 284 of 299 logged ingests were disk-edit triggered, so an ordinary disk touch arms it. **SHAPE: a shell loop over a shipped verb at ~25ms (8+ writes fit inside ONE quiet period), fenced to a scratch entity, peers quiet and told, deliberate disk touch, then read back EVERY row.** **FALSIFIABLE AT LAST: the render-queue model predicts MORE THAN ONE lost row at depth 8; if only the last goes, that model is wrong.** **SAFETY CONDITION, NOT OPTIONAL: this is firing the loss mechanism on purpose on a live tree** -- read-back loop armed to repair what it breaks, or the experiment is indistinguishable from the defect. `ST0057 AT-07.5` is now the ONLY remaining occupant of hv's window, so it stops being a pair.

**TWO OPEN READS THAT COULD CHANGE THE RELEASE, BOTH CHEAP, BOTH NEEDING NOTHING GRANTED.** (a) **THE TEST SUITES ARE THE ESTATE'S BIGGEST BULK INVOKER AND NOBODY HAS COUNTED THEM** -- at 25ms a verb, `burn.sh` and the bats suites drive canon-writing verbs in sequence BY DESIGN, so they are inside the debounce for their whole run. **Against a watched tree with a live daemon they have been in the untested regime every time anyone ran them, and every unreproducible flake is a candidate; against isolated fixtures they are safe and that deserves saying explicitly.** (b) **THE DAEMON-SIDE FIX COLLIDES WITH `written_at`** -- enforcing _do not revert state newer than the disk_ needs a freshness comparison, and **the ingest REWRITES that field wholesale**, so the fix needs a monotonic version the ingest does not own. Two findings that sat apart all evening and are one problem from both ends.

**THREE SECOND-ACTOR INCIDENTS IN ONE EVENING, ONE SHAPE:** `intentd` reverting a store write; a plain `git commit` taking a peer's staged bytes into `4c940ef6`; and the shared build gate reading a peer's live working tree, so **two nodes asking minutes apart get different verdicts with nothing saying so.** **An ordinary action by one node silently degrading another's, and the acting node's own report true and incomplete every time.** Git has a real mitigation, the gate has a VISIBLE failure, **the daemon has neither.**

## TODO

- **MY THREE RULED ITEMS (dispositions 6-9, `AC-10.5`, THE ARM) ARE IN DOING, NOT HERE, BECAUSE THEY ARE RULED WORK RATHER THAN OPEN QUESTIONS.** A fold never archives an unexecuted ruling. **Disposition 5 is RETIRED, not forgotten** -- its premise was overtaken and what survives is carried as the arm.
- **THE RUSTDOC ITEM IS _CLEAR THE CLASS, THEN FLIP ONE ENV VAR_, AND THE CLASS IS FAR CHEAPER THAN I TOLD hv.** Both halves exist: `bin/.devbin/config.yaml:203` already runs `cargo doc --no-deps --document-private-items`, report-only BY MEASUREMENT, and the gateable form is one env var -- `RUSTDOCFLAGS="-D rustdoc::broken_intra_doc_links"`, driven to rc 101 and rc 0. **THE ACCOUNT LIVES IN ISSUE `0214`, NOT HERE.** What the board carries is the correction: I recorded six targets as _absent from the code entirely_ and **five of the six exist under another name**, so the class is one-token path repairs rather than missing mechanisms. **I sent hv a direction question resting on _the class tripled, so the condition recedes_; the tripling is real and the inference is not.** The lint half is dc's.
- **WRITING TESTS CANNOT CLOSE `AC-06.1`'s COVERAGE CLAUSE, AND THE HOLE IS A HIGHLANDER ONE.** `daemon` and `config` have suites, each driven three ways -- but the estate's default `INTENT_BIN` is `bin/intent`, **the v2 SHELL SCRIPT**. A non-zero DEFAULT failure count is UNSTABLE under burn.sh and carries no classification, so `coverage_map` cannot count either file however correct it is. **IT IS THREE BINARIES, NOT TWO** (vc): `test_helper.bash:21` -> v2 shell; `run_v2_suite.bash:55` -> v3 DEBUG; `~/.local/bin/intent` -> v3 RELEASE. **Flip THEN re-baseline or the wall time is paid twice. NOT MINE TO TAKE.**
- **`coverage_map.sh` REFUSES TO PUBLISH, CORRECTLY.** The burn TSV no longer covers the estate. Remedy is re-running `burn.sh`, a DOUBLE full-estate sweep that hung 3.5h once. **I DO NOT LAUNCH IT** -- full-suite runs are hv's. Both holes survive the refusal independently.
- **`AC-06.1` IS RED ON TWO LIMBS AND NEITHER IS THE ORDERING ONE.** The surface is not ported, and the coverage half waits on the flip and the re-baseline. **The ordering limb is DISCHARGED and the record is on `AT-06.1`** -- including D35 (hv, 2026-08-15) naming config as the reader of the `backup.*` keys two weeks before the test, put on the row rather than left to be found. vc ruled it a REQUIREMENT, not a design.
- **WP-08 IS 12/12 GREEN AND `wp done` IS AVAILABLE TO ME (XS). NOT TAKEN.** Closing it marks the daemon DONE while its family carries zero conformance coverage. hv's sequencing call; vc endorsed the hold on the record.
- **`AC-06.8` IS NOT MINE AND MOVES ON `0181`.** `agents init --template` reads `retire` (vc's `0180` is EXECUTED); `llm usage_rules --symlink` is dc's, executed 19:55Z. **Do not sequence anything behind me on it.**
- **THE `hoist` ROW'S RE-PIN RULE IS STALE AND UNEXECUTED.** It names `canary`, `lamplight`, `utilz`, `baize` as repositories whose migrations have NOT run; **all four declare 3.0.0 on disk**, canary included because canary IS this repo. **DO NOT RE-PIN.**
- **WP-10 IS THE CHOKE POINT FOR vc's LARGEST BLOCK.** `AC-00.2`, `AC-00.8`, `AC-00.10` sit behind WP-10, dependent on WP-06 (mine) AND WP-07 (dc's).
- **STILL hv's AND NOBODY ELSE'S:** the `burn.sh` re-run, the `INTENT_BIN` flip, `WP-08`'s close, and the rustdoc DIRECTION question in its corrected form.
- **THE MCP DISPOSITION IS DECIDED, NOT DEFERRED.** SEVERITY EXPOSED (a classification over an enumerated domain); TITLE WITHHELD (authored text). Parking it _until after the tag_ was the phantom hv struck.

## Watch-outs

**I CERTIFIED AN INSTRUMENT I HAD NOT OPENED, IN THE SAME BREATH AS A MEASUREMENT I HAD DRIVEN, AND THE TWO ARE INDISTINGUISHABLE IN THE SENTENCE THAT CARRIES THEM.** Withdrawing the rebuild-window ask, I gave three grounds: `--version` equals the three-path `rev-list` (DRIVEN), `at edit --note` is in the binary (DRIVEN), and **_every `include_str!` site sits inside the currency arm's population_ (NOT DRIVEN, and FALSE -- `surface/` is at the REPO ROOT, and `currency.lib:107` diffs `native/rust` alone)**. The conclusion survived on the first two; the third was a free rider that no reader could tell apart from them. **The cost is not the withdrawn ask -- the instance was genuinely closed. It is that a claim ABOUT AN INSTRUMENT went out with the authority of the measurements standing beside it**, and it would have made a real gap invisible to the next person who trusted it. **RULE: a claim about what a checker COVERS is a claim about a file, and it is driven by opening that file -- never inferred from what the check is FOR.** vc caught it; the sharpening is mine and it is the keeper: **`source_commit.rs:233` derives the marker's identity over THREE paths (`DIRT_SCOPE`), so currency compares a THREE-path base to a ONE-path delta -- the two sides of the comparison do not answer the same question.** Three homes for _what is a build input_ (`source_commit.rs:174` 3, `sharedtarget.lib:124` 3, `currency.lib:107` 1), the odd one out is the only one on PATH, and **the direction is inverted: an UNCOMMITTED `surface/` edit is caught and a COMMITTED one is not.** Not firing today -- `1c80a627..HEAD` is 63 commits, zero under either reading, driven twice independently. `bin/.devbin` is dc's.

**A CONTRACT FINDING PRE-EMPTS THE COUNT, SO CLOSING A ROW GREEN CAN COST THE ESTATE THE WHOLE GATE.** `intent ac gate ST0056` and `ac status` both REFUSE on one AT row whose file omits its own id -- mine, from `b67a9bc7`. **The check that catches it is a SHIPPED VERB, one command, naming the defect verbatim (`intent at lint <ST>`), and `at green` does not run it.** I set the row green and never drove the reader. Same shape as dc's _printing the command is not running it_, one layer down. **AND THE AGGREGATE REFUSING IS NOT THE FIGURE BEING GONE:** `intent ac list` does not route through the contract check, and the number reconstructs from it two ways (118 + 24 = 142 live, + 27 descoped-or-withdrawn = 169). **Whether that second reader's arithmetic EQUALS the gate's is unconfirmed and must ride with the figure** -- it is a second reader of the same question, which is the shape that agrees until it does not.

**A NAME CENSUS ANSWERS A QUESTION ABOUT NAMES, AND PROMOTING IT TO A CLAIM ABOUT MECHANISMS NEEDS A SECOND STEP NOBODY PERFORMS: READING THE SITE.** I measured that `offers_an_edit_the_surface_refuses` appears once in the tree and filed `0214` saying the guard was never written. The guard had been there three days under another name, with a non-vacuity control attached. **The grep was correct about the only question it was asked.** Second instance in a day -- the first reported `Op::Graphql` ABSENT because `fn|struct|enum <name>` cannot see an enum VARIANT, caught only because that name had been planted as a positive control. **And the same error then propagated into my SPLIT of the class, which is how a bad instrument reaches somebody else's sizing.**

**A STALE BINARY CANNOT ANSWER A QUESTION ABOUT HEAD, AND THE RUN THAT HAPPENS TO BE RIGHT IS NOT EVIDENCE.** I drove `config_undefined` against `target/release/intent`, got 6/6, and only then noticed the gate reporting that pair BEHIND HEAD. Rebuilt debug, re-drove, same verdict -- **and only the second run counts, because the first would have read IDENTICALLY had the answer changed.** The verdict surviving is not what makes the re-run necessary.

**A TOOL SCOPED TO A CRATE ACTS ON A POPULATION WIDER THAN THE ONE YOU CHOSE.** `cargo fmt -p intent-cli` swept dc's open, uncommitted `spine.rs` and `render.rs` while they were mid-edit. It changed nothing of theirs and **that was luck, not safety**: no lock, no conflict, and nothing in `git status` saying who did it -- dc confirmed their own `rustfmt --check` could not have distinguished my run from no run. **Scope fmt to the paths you touched.** Same family as ic's finding that a canon file's dirtiness does not name its author.

**DRIVE A RULING'S PREMISE BEFORE YOU BUILD ON IT.** Of vc's four build rulings today, THREE had false premises -- a guard that already existed, a test that had already landed on time, and a WP descoped three days earlier by a ruling vc had relayed themselves. **None was caught by the ruler; all three by opening the file first.** vc's own diagnosis is the keeper: **ruling from board summaries and issue text, where the tree disagreed four times out of four when somebody opened it.**

**A DEFERRAL WHOSE STATED REASON GETS STRONGER WITH TIME IS SELF-REINFORCING, AND NAMING IT AS ONE IS WHAT CONVERTS A CHORE INTO A DECISION.** The rustdoc gate waits for a class to reach zero; the class is GROWING, and the reason for waiting -- _turning this red today fails other lanes' commits_ -- gets stronger every week it is deferred, never weaker. **So the wait cannot end on its own terms.** Distinct from the scarcity register hv struck: that was a phantom deadline, this is a real cost that compounds. **The test: does the condition get easier or harder to meet by waiting? If harder, it is not a condition, it is a decision nobody has taken.** Ask the direction question, never the bigger-cleanup question.

**ASSERT THE PREMISE YOUR FIX RESTS ON AS A TEST, IN THE DIRECTION THAT WOULD EMBARRASS YOU.** I built `0206`'s CAS on `threads.revision` having READ `rebuild` and READ `Door::Change` and reasoned correctly from both -- and never read the `DELETE` two lines above, so I had it backwards: `rebuild` deletes before inserting, the conflict clause never fires, and the counter RESETS rather than rises. A CAS on it would have FAILED OPEN on the case it was built for. **The control written to stop an arm passing vacuously refuted the design it was protecting.** No care at the READING step reaches this; only an assertion does. Distinct from the plan-versus-mechanism class: there the plan hid the mechanism; here I read the mechanism and stopped one line early.

**`cargo check --workspace --all-targets` -- AND THE FLAG IS THE HALF MEMORY DROPS.** A Rust enum makes an exhaustive `match` a DISTRIBUTED obligation reported from DOWNSTREAM, so **the node who breaks it is structurally the least likely to see it**: `-p <mycrate>` was green while ic's build was on the floor, and `--workspace` misses it too because the exhaustive matches live in TEST targets. **IT IS NOT A STRICTER `--workspace`, IT IS A DIFFERENT SUBJECT** (dc's sharpening): the exhaustive matches live in test targets a plain check DOES NOT BUILD, so `--workspace` cannot see the class at any level of care. Four instances, three nodes; `error_remedies.rs` has named the exact command in its own comment throughout. **A doc that names its own instrument does not make anyone run it** (dc's). Land the variant, its const, its remedy arm and every exhaustive match in ONE edit.

**THE INSTRUMENT ANSWERED A DIFFERENT QUESTION, AND IT ARRIVES WHILE I AM BEING CAREFUL.** A selector that RESEMBLES the population is not the population. Specimens: `grep -i FAILED` matches `0 failed` in every passing summary; a `Display` grep dropped the `Debug` line holding the evidence; `pkill -f 'cargo test --workspace'` matches a pattern every session runs; an `awk '/pub const X/,/^\];/'` range never opened because the decl is bare `const`; `^  [0-9]+,$` missed rungs indented 4 and reported a tidy wrong total; a grep aimed at the wrong directory read as _the thing is gone_. Also: `--help` renders for a DECLARED command whether or not an arm exists, and `bash -n` answers _is this valid bash_, never _is this the file I meant_. **RULES: an EMPTY result is not a finding until the pattern is positive-controlled; assert a MEMBER you know is in the population, never just a plausible total; after any programmatic edit, grep for what should now be there.** The common shape: the instrument is checked against what I MEANT rather than a sample of what it will SEE.

**A CONTROL THAT CANNOT DISTINGUISH _SAFE_ FROM _NEVER TRIED_ IS NOT A CONTROL, AND IT MUST VARY THE AXIS THE CHECK ACTUALLY READS.** A no-mutation probe whose two shasums are both EMPTY has no subject. **The sharpest instance cost me a false exoneration:** I attributed 23 `intent-cli` failures to _not mine_ because stashing my paths reproduced them -- but `git stash` REWRITES the file, so the mtime stayed fresh, and the guard keys on MTIME not content (`common/mod.rs:782`). My arms differed only in the dimension the guard IGNORES. `cargo build -p intentd` then re-run: 461/0. **Ask what the check READS before choosing what to vary.** State the bound's DIRECTION too.

**THE CORPUS IS THE CLAIM; THE VERDICT IS NOT.** Beside the verdict, in the OUTPUT and never a comment: **POPULATION** (the set examined, derivation named), **FORMS** (per-form counts, never one total), **REACH** (`COVERS` / `DOES NOT` / `UNOWNED`).

**A VERIFICATION SCOPED TO THE THING CHANGED IS NOT A VERIFICATION** (dc's). The blast radius of a published value is every crate that PINS it, not the crate that DEFINES it -- `grep -rn <constant> --include='*.rs'` finds that corpus in a second. Unlike a bad grep, this one LOOKS like diligence.

**A MEASUREMENT TAKEN ON THE SLOW PATH IS NOT EVIDENCE ABOUT THE FAST ONE.** My millisecond-resolution hypothesis for `updated_at` DID NOT REPRODUCE and is not evidence against it; what survives is a design argument, because my gap was dominated by process startup and says nothing about the daemon path where both writes share one process.

**A REPRODUCTION PROFILE IS A DIAGNOSIS.** _Needs the full workspace, invisible with the subsystem alone_ says the cause is not in the code under test but in what SHARES ITS PROCESS -- which is what put me on `fork`, `flock` and `O_CLOEXEC`.

**THE SHELL LIES ABOUT SUCCESS ON THIS BOX.** `cmd | head` reads head's status -- done twice in one day, both read as 0. zsh has no `PIPESTATUS`, does NOT word-split `$var`, and an unmatched glob ABORTS the command. `grep` is `ugrep`. `sed -i ''` SUCCEEDS on no match. `find` is bfs, and `find .` walks `target/` until it times out. **cwd persists between calls** (caught again today). `2>/dev/null` on a source printing `command not found` returns an empty lookup that reads as data.

**THE SHARED CHECKOUT.** Canon cannot be split, so every canon commit is silently multi-node: `ST0056.json` names the STORE's union of every dirty-and-ingested attachment, so a node holding a dirty file under `parity/tools/` blocks every other node's canon commit on that thread. **`add` + `commit --only` is the only safe write; a live `index.lock` is a WAIT, never a removal; never `--no-verify`; never unstage a peer's path. PROVENANCE IS CARRIED BY PATH.** The compile is a shared resource on the same footing -- see the `--all-targets` entry.

**A GREEN THAT SOMEONE ELSE REPORTS RED IS A WINDOW, NOT A DISAGREEMENT.** ic measured two `schema_command` failures real when taken and gone when read -- they fell between my DDL edit and the deliberate re-bless. The generated face means a `store.rs` defect surfaces in intent-cli's suite, **so the package boundary is not the blame boundary in either direction.** Re-drive and compare TIMES, not verdicts.

**TWO CORRECT RULES CAN TAKE THE MACHINE DOWN.** A release build DELETES the shared pair before building (`0196`) and the shared-artefact guard REDIRECTS a dirty-tree build to `target/private/`: the delete happened, the replace declined, `intent` was gone estate-wide for fifty minutes. The guard prints the dangling symlinks BEFORE the delete, so the information exists and the ORDERING defeats it. **0196 now also carries lamplight-cc's half: the SUCCESSFUL build has the same window and it reaches OTHER REPOSITORIES** -- six verbs in one script, verb 1 wrote, 2-6 died on `command not found`, `set -e` did not catch it, half-written canon in another project. Remedy recorded: build to a staging target and `mv` in, since a rename is atomic. **AND THE CONSTRAINT THIS IMPLIES IS A QUIET TREE, NOT A PERMISSION** (vc's framing, taken 2026-09-02): `0196` is a DEFECT, not an authority rule, and _a rebuild window is hv's_ has EXPIRED -- ic has rebuilt twice in a day without hv. What a rebuild actually wants is that a failure would not leave live sessions without a binary. **My own line named the hazard and never said what it demands of the operator, which is the half a reader needs.**

**A PIN THAT TRAILS HEAD IS THE CORRECT STEADY STATE, NOT STALENESS** (dc's) -- and **CURRENCY IS A PROPERTY, NEVER A VALUE.** It was current at 16:23Z and behind by 16:39Z with nobody doing anything wrong. `git rev-list -1 HEAD -- native/rust surface docs/design` equal to the pin means CURRENT; **the gate's `currency` arm is the decider and the bare pin/HEAD difference is not.** I carried "BEHIND THE TREE" in five messages after it stopped being true. **DIRT, NOT STALENESS, IS WHAT BLOCKS A SHARED REBUILD.** `DIRT_SCOPE` has TWO HOMES BY DESIGN (`source_commit.rs:174` marker, `sharedtarget.lib:124` guard); the guard must stay a SUPERSET and **arm 6 asserts CONTAINMENT and must NOT be tightened to equality** -- today they are EQUAL, so an equality check passes now and only fails the next legitimate widening. Containment is what permitted `docs/design` (AC-08.9, `intentd` embeds the logo).

**A LADDER DECLARED AS DATA IS READ FROM THE DATA.** `git log -S` counts occurrences and a value change alters no count, so it cannot count rungs; `MIGRATIONS: &[(i32, &str)]` enumerates them. **General form: before going to history for a number, ask whether the thing declares itself.**

**A MESSAGE CORRECTING SOMEONE'S STATE IS ITSELF A STATE CLAIM.** dc re-drove vc's reading and in the same message told me my board carried a claim it never had -- the re-drive reached the SUBJECT and not the claims about the RECIPIENT. Read this before writing the next correction to a peer.

**A SILENT CORRECT DECLINE IS INDISTINGUISHABLE FROM AN ASSUMPTION** (dc's). Three nodes each DECLINING to drive a claim is correct; three each ASSUMING a fourth did is the accident, and **nothing separates them except whether someone recorded the decline.**

**AN OPEN ESCALATION AGES INTO AN ASSERTION** (vc). **Before anyone budgets against an escalation, its AUTHOR re-drives it.** Same family: **CONSTRUCTING BEATS READING, MEASURING BEATS BOTH, AND THE DISPROOF IS USUALLY ALREADY IN HAND.**

**A REFUSAL THAT NAMES NO ARTEFACT SENDS THE OPERATOR TO GUESS.** Name the FILE, name both VALUES, and point at `--version` for the build rather than inlining a commit that has exactly one home. Fixed for both schema refusals 2026-09-02. **A DOC COMMENT IN THE PRESENT TENSE ABOUT A MECHANISM THAT DOES NOT EXIST IS INDISTINGUISHABLE FROM ONE DESCRIBING A MECHANISM THAT DOES** -- nothing goes red and the reader finds a name and stops looking (`0214`, third instance this month).

**FACTS THAT LOOK LIKE OPINIONS.** `intent st attach` writes the store AND the canon extract and NEVER the disk file (`0082`); the daemon watches DISK, so a disk edit converges everything and a store-side write renders nothing back. A thread must be REALISED or it has no cover. `intent ac show` is not a verb. `intent init --name` is not a flag. **`intent issues add` and `issues edit` write ONLY the issue's own extract** -- a narrow canon window, not the wide one both vc and I were sequencing around. **TWO `--version` OUTPUTS THAT LOOK IDENTICAL CAN BE TWO DIFFERENT QUANTITIES**: `intent --version` names the commit the BINARY WAS BUILT FROM, `bin/devbin --version` names what the CHECKOUT IS AT NOW. Do not diff them; ask the currency question.

**AN INSTRUCTION IS WRITTEN AGAINST A SURFACE ITS AUTHOR MAY NOT HAVE RE-READ -- CHECK THE SURFACE BEFORE EXECUTING, NOT AFTER.** D3 is the specimen: the ruling was SOUND and the carrier did not exist -- `at edit` has no name field and `legacy.rs:1965` refuses both spellings as VALUES, and the scoping would have had me parse-and-rewrite `intent/.canon/st/*.json`, **a GENERATED PROJECTION whose SSOT is the store (D01)**: 187 rows, a clean diff, and the store never touched. **A DIFF THAT LOOKS RIGHT IS NOT EVIDENCE THE WRITE REACHED THE SSOT.** hv withdrew D3 entirely on a DOMINANCE argument -- a file merge breaks a path, a rename breaks a name, and the id inside the test survives both -- **so reach for dominance before competing assertions.** General form: **drive the mechanism's own `--help` and model before sizing anything against it**, and prefer the ADDITIVE carrier (`Option` + `skip_serializing_if`) over relaxing a grammar.

**A BASE RATE IS NOT A CONDITIONAL, AND THE BASE RATE WEARS THE SAFETY QUESTION'S CLOTHES.** Four measurements went to _how often does the risky shape occur_ -- coalescing depth, constructed depth, tonight-versus-corpus proportions -- and produced a reassuring 9-13% while three writes were being eaten. **The question that decides a mechanism is _given it occurs, what happens_, and it was in the same table every time and nearly deterministic.** A base rate tells you how worried to be; **a conditional tells you what the thing IS.** Both my hypotheses and both of vc's measurements went to the wrong one.

**A CLAIM THAT SOUNDS LIKE PHYSICS GETS EXEMPTED FROM MEASUREMENT.** _A process spawn costs more than 250ms_ survived four assertions across three nodes because it reads as a fact about computers. It was **LLM TURN LATENCY WEARING AN OPERATING SYSTEM'S CLOTHES** -- the real figure is 23-26ms -- and **a stopwatch killed it in four seconds.** _Our writes are 250ms apart_ would have invited the question; the structural-sounding form did not. **Reach for the stopwatch precisely when the claim sounds too basic to need one.**

**THE REPORT IS THE ARTEFACT NOBODY REVIEWS, AND IT HAS TWO FAILURE MODES.** (a) **A DISPLAY FILTER IS A CLAIM ABOUT THE POPULATION.** An `if d<1800` in a print loop -- never in the query, never mentioned -- cut a complete sweep to 9 of 26 rows, and **the excluded population was exactly the one the census existed to find**, because _repair-shaped_ operationalised as _fast_ encodes NOTICING. The query was argued and defended; **the reviewed artefact and the reported artefact were different objects.** (b) **MAKE THE SUMMARY RECONSTRUCT, AND SHOW IT RECONSTRUCTING FROM TWO DIRECTIONS.** Two summaries in one hour read as internally consistent while concealing a missing row and an unreported row, each behind a compensating miscount. **A printed total hides a miscount; `16 + 10 = 26` and `20 + 6 = 26` stated together cannot.** Both were caught by a reader adding up categories, never by anyone re-running anything.

**A CONSTRUCTED VARIABLE STOPS BEING LABELLED CONSTRUCTED THE MOMENT IT IS QUOTED -- INCLUDING BY ITS AUTHOR** (vc's words). `burst` carried three different constructions in one evening -- a prose grouping, a 3-10s clustering window, and the daemon's 250ms coalescing -- and a number moved between them each time without its definition. **Mine crossed a session boundary; vc's crossed two paragraphs; the mechanism did not care about the distance.** **The label lives in the prose and the number travels alone.**

**SPACING IS NOT A MITIGATION AND `one verb at a time` PROTECTS ONLY AGAINST YOUR OWN BURST.** The debouncer sees WRITES, NOT AUTHORS, so a peer writing two seconds before you puts YOUR write in the exposed position and **nothing in your transcript shows it.** **The mitigation that survives having no constant: READ THE TARGET BACK, COMPARE IT TO INTENT, RE-WRITE IF IT DIFFERS, REPEAT UNTIL IT HOLDS** -- a loop on the observable, never a wait on a duration, because _past the ingest_ smuggles a number nobody has. **AND A SURVIVAL IS NOT EVIDENCE THE DISCIPLINE WORKS WHEN THE MECHANISM WAS NOT ARMED** (ic) -- reporting _verified, discipline holds_ from an unarmed run is a false confirmation built from a true observation.

## Decisions

- (2026-09-02, cc+vc) **`0216`'s FIX IS DAEMON-SIDE AND NOT USAGE DISCIPLINE.** A hazard reachable by an ordinary shell loop cannot be mitigated by how carefully nodes write, because the discipline would have to be _do not script this tool_. The invariant: **an ingest must not revert store state newer than the disk it read** -- and it needs a monotonic version the ingest does NOT own, since it rewrites `written_at` wholesale.
- (2026-09-02, cc+vc) **THE ARM NEEDS THE DAEMON RUNNING, NOT STOPPED, SO hv IS OFF ITS CRITICAL PATH.** The gate is whether an ingest FIRES; 284 of 299 were disk-edit triggered, so an ordinary disk touch arms it. **`ST0057 AT-07.5` is therefore un-paired and is the only remaining window ask.** vc had been putting them to hv as one sitting: correct an hour earlier, wrong now.
- (2026-09-02, vc, relayed to all nodes) **THE READ-VERIFY-RETRY LOOP IS A REQUIREMENT, NOT GOOD PRACTICE**, and it replaces `one verb at a time`, which protects only against your own burst. **The debouncer sees WRITES, NOT AUTHORS.**
- (2026-09-02, vc) **DISPOSITION 5 IS RETIRED AS A DISPOSITION RATHER THAN LEFT TO ROT.** Its premise was _drive the settling to find the mechanism_ and the mechanism work overtook it; what survives is the provocation arm, carried as its own work with its own justification.
- (2026-09-02, vc) **A RIDER CANNOT BE VIOLATED BY A CASE ITS OWN HAZARD CANNOT REACH.** `AC-06.1`'s ordering clause exists to stop a test being written to MATCH BUILT BEHAVIOUR; a test written against an UNWIRED family, pinning rc=2 on six arms, cannot be matching built behaviour because there is nothing there to match. D35 naming `intent config` as the reader of five keys is a REQUIREMENT on config, not a DESIGN of it.
- (2026-09-02, vc) **RULING 3 WITHDRAWN: `WP-14` AND ALL 12 OF ITS ACs WERE DESCOPED WHOLE TO ST0069** by hv on 2026-08-30, `by: "hv via vc"`. 25 rows across exactly three families -- 13, 14, 16. `AC-09.5`'s wip/boards half goes with it: the CLI read that would restore it IS `intent wb`.
- (2026-09-02, vc) **`AC-10.5`'s RESIDUE IS RE-DISPOSITIONED AS _MODELLED IN ST0069, NOT IN THIS RELEASE_.** D30's ground was that naming the 110 files would zero the loss by asserting abandonment; **the descope supplies the honest third option D30 did not have** -- the carrying moved rather than being dropped, so naming them as modelled-elsewhere is TRUE.
- (2026-09-02) **TWO MACHINE PROJECTIONS OF ONE VALUE MUST NOT DRIFT; A HUMAN RENDERING OF IT IS NOT A COPY AT ALL.** `outcome_json` is hoisted beside `Outcome` and shared by the MCP and wire faces, which answer the same question. `render.rs` turns the same notes into sentences with remedies -- a DIFFERENT question, so folding it in would merge two concerns to satisfy a rule that was never about it. **Highlander asks whether two copies would drift into disagreeing about the same question** (vc's test, ratified).

- (2026-09-02) **CONTENT COMPARISON DOMINATES A VERSION COUNTER FOR A COMPARE-AND-SWAP.** It does not fire when nothing was lost, fires whenever something would be, and is right about ABA where a counter is wrong. `Thread` derives `PartialEq`, so it enumerates nothing -- which is what killed my own hand-maintained-population objection. **A dominance argument ends a design debate that competing assertions cannot** (second time in two days).
- (2026-09-02, vc) **A COLUMN WITH NO READER SAYS SO IN ITS OWN COMMENT.** `threads.revision` is retained because dropping it costs an irreversible rung; silence would send the next reader hunting for a consumer that does not exist. Not _the CAS token_ -- it is not one.
- (2026-09-02, vc) **AN EXCLUSION MUST BE VISIBLE WHEREVER THE BEHAVIOUR IS CLAIMED, NOT ONLY WHERE THE CHECK LIVES.** `0206` refuses concurrent WRITES and not DELETIONS; any row or issue asserting the general form says _writes, not deletions_ in the same sentence, or the exclusion re-enters through the artefact that failed to name it.
- (2026-09-01) **hv: v3.0.1 IS FEATURE COMPLETE, THERE IS NO TAG WINDOW AND NO EXTERNAL CONSUMER, AND COST IS NOT A CONSTRAINT.** The SCARCITY REGISTER is retired AS A CLASS. **A recommendation resting on _there is not time_ must be re-derived on its merits or withdrawn.** The release-number question is CLOSED and is not to be put again.
- (2026-09-01) **A REFUSAL THAT CANNOT SAY WHAT IT FOUND MAKES ITS OWN DEFECT UNDIAGNOSABLE.** Both `AlreadyRunning` branches rendered identically, so the first move was making them distinguishable rather than editing the code under suspicion.
- (2026-09-01) **A REMEDY INHERITS ITS BRANCH'S ASYMMETRY.** Confirm-before-refuse is SAFE on the lock and WRONG on the probe, where confirming makes the DESTRUCTIVE outcome likelier. **I concluded it was branch-independent and testing the claim is what stopped it shipping.**
- (2026-09-01) **EMBEDDED AND UNPRINTED IS THE WORST OF THE THREE STATES.** Absent would have been honest.
- (2026-08-31) **A CRITERION IS NOT REWORDED TO WHAT THE CURRENT STATE SATISFIES** (vc). The ruled form must be HARDER and bind the node holding the divergence.
- (2026-08-31) **A CLASS CHANGE WITHOUT ITS REASON IS A DELETION WEARING A NEW LABEL** (vc); `deviate` must RETARGET, recorded where the artefact is GENERATED FROM. **A DECLARED EXCLUSION CARRYING ITS REASON IS THE CURE; A SILENT DROP IS THE DENOMINATOR ATTACK.**
- (2026-08-31) **AN OWNERSHIP SPLIT IS A PURPOSE, NOT A BOUNDARY** (ic, correcting me). Reverting so a peer can rebuild byte-identical code is churn wearing Highlander's clothes -- **and two hands in one file IS `0206` in miniature**, which is why I declined to write dc's variant semantics for them today.
- (2026-08-31, CORRECTED) **`close --note` STAYS RULED OUT ON A CONTINGENT FOOTING.** The keg has no `edit`; the ruling survives because v3.0.1 ships from the tree. Re-drive the two 3.0.0 artefacts rather than quoting them.
