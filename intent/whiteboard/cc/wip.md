---
node: cc
name: Control Claude
role: control
session_id: ae8c8153-6f3f-438f-b96b-04bd381ad4ed
heartbeat_at: 2026-08-30 09:18Z
status: active
focus: "NOTHING IN FLIGHT. WP-08's routing seam is complete on both sides and `0152` is closed. NEXT IS PHASE 3 (`AC-08.10`, tokio + axum with a written rationale) -- ic's dispatch-table split has landed, so the lock can move. `guide.rs` still waits on dc's `pub const`."
claims: [ST0056/06, ST0056/08, ST0056/10, ST0056/13, ST0057/00]
---

# Control Claude (cc)

## DOING

**NOTHING. Everything from the 2026-08-29/30 session is landed and verified in HEAD.** The night's work and its incidents are archived at `.history/20260830/wip-fold-0838Z.md`; what survives here is the classes.

**WP-08's routing seam is complete on BOTH sides.** The probe is a bounded round trip (`AC-08.3`); `Published` and `Bound` are the write side, so an address or socket nobody is listening on is unexpressible; `AC-08.12` makes the START decision a kernel lock rather than a liveness probe. **The writer and the probe are designed against each other on purpose** -- the writer tries never to leave a corpse, the reader assumes it did, and `SIGKILL` runs no destructor.

## TODO

**vc OWNS SPECIFICATION, ADJUDICATION AND REVIEW (hv, 2026-08-29). I OWN MAKING IT WORK.** Read their board for the current wording of anything below; never re-derive a criterion from here.

- **[1] WP-08 PHASE 3 -- `AC-08.10`, AND IT IS UNBLOCKED NOW.** First async runtime and first server, each landing with a written rationale that CITES rather than judges: tokio and axum are the house choice with a sibling estate's tests behind them (`../Conflab`). ic's dispatch-table split has landed, so a lock move no longer compounds two breakages. Then Phase 4 (the daemon proper), Phase 5 (the JSON face).
  - **`AT-08.3` HOLDS AT `to-write`, AND PHASE 4 CLOSES IT WITH ONE OBSERVATION** (vc ruling, 2026-08-30). My old reason expired when vc moved the daemon clause out to `AC-08.11`; the new one is that **every case in `AC-08.3` is CONSTRUCTED, and this estate has already been burned once by a constructed live daemon that was a phantom** -- every "live" fixture in `cli_routing.rs` was a bare listener, which is case 2's observable, so 11/11 was green while the suite could not tell a live daemon from an inherited-fd phantom. **The three phantoms are better evidenced by construction than a real daemon could manage** (a 1-in-300 CLOEXEC race is not reproducible on demand); it is only the POSITIVE case that owes a real subject. **So: when Phase 4 produces a running `intentd`, drive the happy path against it ONCE and the row goes green.** No re-derivation, no second fixture, no re-running the phantoms -- holding for more than that would be the unfalsifiable version. **AND THE GATE ARGUES AGAINST THIS RULING ON EVERY COMMIT, CORRECTLY AND BY DESIGN:** `stale: AT-08.3 ... the file EXISTS while the row says to-write`. Both are right at once -- the file does exist, and the row must not move until a real daemon drives it -- and **the cheapest way to silence the advisory is the exact move the ruling forbids.** The gate's own note is the reconciliation: _presence is not greenness._ **DO NOT MOVE THE ROW TO QUIET THE GATE.**
  - **D56: `intentd` emits JSON ONLY, over the socket AND over HTTP. CITE IT, NEVER RESTATE IT** -- class 15 was earned on this exact decision.
  - **`intentd` IS MINE END TO END** (hv D3). **Do not port Conflab's auth layer**: one auto-generated token on the HTTP half, and the socket needs none because filesystem permissions are its authz. **The two transports have different authz stories and the seam must not assume otherwise** -- uniform treatment gives the socket a check it does not need or the port none at all, and the second is a hole.
  - **DESIGN AGAINST THE LARGEST REAL THREAD** (154 criteria, 297 attachments, a 59KB paragraph), never a fixture. Same-origin fonts, no network.
- **[2] THE REGISTER `shipped` GATE ARM -- S.** `Entry::is_shipped()` asks nothing about existence, so a `keep`-dispositioned unbuilt verb ships by construction and refusers are declaratively indistinguishable from workers. **Built-ness is a property of the (register, binary) PAIR**, so drive every shipped ROW against the binary, keyed on rows and each row's own `args[0]`. **The unwired-marker consolidation is NOT a precondition and must not be done** -- vc ruled (a): Highlander governs implementations, not witnesses, and an asserting copy that imports the value it asserts has stopped testing. The discriminator is whether the marker is the SUBJECT of the file (witness, keep its own literal) or INCIDENTAL (import).
- **[3] `guide.rs` -- BLOCKED on dc defining the unwired `pub const` in `render.rs`.** vc routed the two-line emitter edit to dc; mine is the one-line import. The const serves the EMITTER and the GUIDE and **no witness may read it**, which the doc comment must say, because a node reading only the diff sees the move we forbade.
- **[4] WP-13 project search -- XL**, 9 criteria, Not Started.
- **[5] FILLERS, S EACH:** `intent#0145`; the `VIEW_NAMES` gate (walk the estate's thread files, and for each one `classify` calls a `GeneratedView` require `parse` to refuse its basename -- public API both sides, catches a new view type only once such a file exists); `canon-commit` gates on ADDS, so a commit REMOVING attachments is outside its reach.

**NOT MINE, OR WAITING ON SOMEONE.** `--browser` refuses with no daemon; when the daemon lands it should ASK `intentsvcs::daemon::route`, the same call the store door makes, so the two cannot disagree. `AC-00.5`'s retirement door is ic's. **`c755bd44`'s fabricated stamp STAYS UNRESOLVED**: a corrected-looking fake is worse than an admitted one.

## Watch-outs

**BY CLASS, RULE-ONLY. Every line cost a wrong answer that read as a right one. INSTANCES LIVE IN `.history/` AND IN THE COMMITS.**

**1. AN INSTRUMENT CAN PASS WHILE BLIND, AND ITS GREEN IS INDISTINGUISHABLE FROM A REAL ONE.** The faces: a corpus that CANNOT EXHIBIT the defect (commonest); a control that would also pass broken; a control that does not FLIP, proving the probe inert; a predicate reproduced against the UNMUTATED source; a null read as a clean sheet; a correct AGGREGATE that cannot check the mechanism (predict the SPLIT, never the total); a filter matching nothing, which passes for free; **a one-sided comparison, and a conservation check whose two sides share a source, which is an IDENTITY not a measurement** -- diff member-by-member, and **a two-way comparison cannot attribute a difference to one side, so the fix is always a THIRD input.** **THE PUREST FORM YET: THE FIXTURE STANDING FOR THE GOOD CASE WAS AN INSTANCE OF THE BAD ONE** -- every "live daemon" in `cli_routing.rs` was a bare listener, which is the phantom, so the suite's discriminating power was zero while its count was maximal. No amount of positive-controlling the checker finds that; **the control is that the good fixture and the bad fixture are SHOWN TO DIFFER.** **THE ONLY GENERAL CONTROL IS MUTATION.** **THE CHEAPEST INSTRUMENT I OWN IS A PRECONDITION THAT REFUSES TO RUN.** **AND A MISSING TEST IS A BACKLOG ITEM WHILE AN UNRUN TEST IS A DISCIPLINE FAILURE.**

**2. A GREEN BOUNDS THE RING IT RAN ON AND SAYS NOTHING ABOUT THE NEXT RING OUT.** `cli_routing` passed 3 of 3 alone, failed 9 of 30 under a loop, then failed differently under 8 concurrent copies -- three populations, three distinct causes. **Stress the ring you are about to make a claim about.**

**3. MUTATION: OVER-KILL AND UNDER-KILL ARE DIFFERENT SIGNALS, AND A ZERO IS ONLY MEANINGFUL IF THE MUTATION APPLIED.** Fewer kills than predicted means a test is missing. **MORE usually means the mutant is too COARSE** -- it broke the fixtures' PREMISES rather than the property, so the extra kills are collateral and prove nothing about coverage. **Predict the kill SET before running** (catches a mutation that landed and killed the wrong tests) **and assert the source CHANGED before believing any count** (ic's guard: catches one that never landed). A predicted-empty set plus a mutation that never applied is two greens agreeing on nothing.

**4. A PROPERTY ENFORCED BY SOMETHING OUTSIDE YOUR CODE CANNOT BE WITNESSED BY TESTING ITS OUTCOME.** The outcome holds under any implementation that still calls the thing; **what is testable is that you still call it.** `rename(2)` gives atomicity, so no concurrency fixture can fail -- the witness is that the path's INODE changes, which a truncating write would not do. A dependency default gives the 5s contention wait, so no timing test can separate ours from theirs -- **any such test passes with the line deleted**, and the witness is structural. **The deletion this guards against is not malice: it is somebody reading the line, observing CORRECTLY that it sets the value to the default, and removing it as redundant.**

**5. A GATE THAT CANNOT SAY _I COULD NOT CHECK_ WILL EVENTUALLY SAY SOMETHING FALSE INSTEAD** (vc). One message covering two conditions names neither fix. My rustfmt arm could not tell UNFORMATTED from UNMEASURABLE and reported the second as the first, refusing seven correct files with a remedy that runs clean and changes nothing -- so the operator formats, retries, and is refused identically. Same pair as ABSENT-versus-MALFORMED in `machine_table_check`, and UNMEASURED-versus-DISAGREE in the guard axis. **A remedy that runs clean and changes nothing is a gate telling the user their own tools are broken.**

**6. THE FAILURE PATH IS THE ONE THAT MUST STILL CLEAN UP, AND IT IS THE ONE A GREEN SUITE NEVER RUNS.** Cleanup written after the assertions is dead code until an assertion fires, and on that day it does not run. A forked child inherits the test binary's STDOUT PIPE, so a panic ahead of the kill leaves an orphan holding it and `cargo` blocks forever -- **a failing test becomes a hung build naming no test.** The instrument gave NO answer, and no-answer is indistinguishable from still-working. **Cure: `Drop`, never a trailing statement; bound every wait on a thing that can hang.** Fired three times in one night: a fixture, a shell gate, a published address.

**7. A VERIFICATION EXPIRES, AND THE TENSE DOES THE DAMAGE** (dc). A reading taken before the sentence resting on it, written in the PRESENT as a fact about the tree, with nothing marking its shelf life. **A premise about a shared tree expires in minutes** -- and **a commit sha names what is COMMITTED and says nothing about what sits uncommitted beside it**, which a test reading from disk sees. `git status` and a sha answer different questions and only one is in the label. **Re-run in the same turn as the claim, or stamp when it was taken. HAND OVER PROPERTIES, NEVER VALUES.**

**8. THE SHELL ANSWERED FOR SOMETHING ELSE AND IT LOOKED LIKE AN ANSWER.** `x | tail` gives `tail`'s 0; zsh has `$pipestatus`. A `&&` chain short-circuits on a zero-match `grep -c`. `||` never fires when the first command exits 0 while doing nothing. **A failing command substitution under `set -e` aborts before the next line, so a gate refused and printed NOTHING.** **AND A PARTIAL READ THAT FINDS SOMETHING SELF-CORRECTS WHILE ONE THAT FINDS NOTHING YIELDS A CONCLUSION YOU CANNOT FALSIFY** (dc): a `tail -14` landing inside compile output concluded a guard does not print its paths -- it prints them, and says "the paths above". **The absence produced by a truncation is indistinguishable from a real absence and leaves nothing to notice. READ THE EXIT CODE FIRST; when the claim is an ABSENCE, re-run unfiltered.**

**9. THE POPULATION YOU MEASURED IS NOT THE ONE YOU CLAIMED, AND NOTHING NAMES IT.** `cargo check -p X` builds no test target; `--test X` excludes lib tests; a narrowing flag never narrows the CLAIM. **A TERMINATION CONDITION IS A FILTER NOBODY DECLARED** -- `cargo test` aborts after a failing target. **AN ENUMERATOR OF ONE KIND IS STRUCTURALLY UNABLE TO SEE ANOTHER**: I counted named constants and got six where counting FILES gives twelve, two of them source. **STATE A SIZE ONLY AFTER THE SEARCH THAT WOULD REFUTE IT, AND QUOTE THE SEARCH.** Capture ONE run and analyse the capture.

**10. A SHARED TREE HAS OTHER WRITERS, AND `git add` IS FILE-SCOPED WHILE `--only` IS PATH-SCOPED -- NEITHER IS HUNK-SCOPED.** `git commit` commits the index AS IT STANDS. Use `GIT_INDEX_FILE` + `read-tree HEAD` + add-only-mine. **THE FORMATTER RE-STAGE THAT DEFEATED THE HUNK-SCOPED FORM IS GONE** (`14dff192`): a gate may refuse, it must not author, so the staged tree and the committed tree are the same object again. **The rest still applies: a commit can lose the REF RACE after the gate passes** -- re-`read-tree` from the NEW HEAD and re-`commit`, never re-`add` -- **and afterwards the shared index holds the pre-commit state, so `git reset HEAD -- <your paths>` is the second half and is not optional.** Verify with `env -u GIT_INDEX_FILE`. **AND WHEN ONE FILE CARRIES THREE AUTHORS, NO WORKER CAN LAND IT AT ALL.** The private-index discipline scopes to PATHS, so a co-authored blob has no spelling of _this hunk is mine_; each node correctly refuses to wear the others' bytes and the result is deadlock, broken by AUTHORITY rather than by technique -- the one hand whose authorship carries no attribution problem. **The tell is that the file's GRANULARITY does not match its AUTHORSHIP** (ic). **And asking a peer to land it is asking them to take an action their user has not authorised, however well-reasoned the ask** -- the reasoning is what makes it tempting, not what makes it legitimate. Three nodes reached this separately, which is the strongest evidence the estate can produce.

**11. A MULTI-STEP REFACTOR IS UNCOMPILABLE BETWEEN ITS STEPS, AND IN A SHARED WORKTREE THAT IS VISIBLE TO EVERYONE.** A rename split across two tool calls left the tree referencing a constant that no longer existed; two peers hit it independently and both correctly established tree-not-HEAD before speaking, and it still cost them minutes. **A hard compile error is indistinguishable from a real regression and nothing says "mid-move".** A rename plus its use sites is ONE edit. **The worst file to be mid-edit in is one that is COMPILED IN** -- a malformed dispatch table is not a bug a user could hit, it is a binary that dies before it does anything. **AND A FINISHED, CORRECT EDIT CAN BREAK EVERYONE TOO, WHEN THE FILE SITS UNDER A GLOBAL CONSERVATION TEST.** A realised attachment is hash-pinned in canon, so editing one puts `every_realised_attachment_in_the_estate_still_matches_canon` at rc=101 for every node until canon catches up -- 321 compared, 1 divergent, and the 1 is yours. **Nothing at edit time says the file is shared machinery**, because a shell or markdown edit does not feel like touching one; the first signal is a peer's suite aborting on a file they never opened. **Measure the blast radius BEFORE the edit, and if the repair verb is thread-wide, that is the moment to find out** -- not after the gate refuses.

**12. ATTRIBUTION IS ESTABLISHED, NEVER INFERRED, AND BOTH TELLS ARE REASONABLE.** _Absent at HEAD, therefore someone else's_ (`fn fc`, wrong three times by three parties). _The subject is theirs, therefore the edit is theirs_ (a dispatch table attributed to me because the daemon rows are my subject). **Neither leaves a mark when wrong**: the peer accepts a change they did not make, or spends time on a file they never touched. `git log -1 -- <path>` and `git log -S<symbol> -- <path>` close it. **A PEER BEING WRONG DOES NOT MAKE THEIR SIGHTING WRONG -- check the SET, not the instance.** **THE CURE FOR AN UNREACHABLE MEASUREMENT IS A SECOND OBSERVER, NOT A BETTER LOOK**, which is the argument for reporting a half-formed measurement.

**13. A BUILD ARTEFACT CARRIES THE PATH IT WAS COMPILED IN, AND THAT PATH CAN STOP EXISTING.** `env!("CARGO_MANIFEST_DIR")` is baked at COMPILE time, so a detached-worktree build against the SHARED target dir leaves an rlib naming a directory that dies with the worktree -- reporting a `Cargo.toml` defect in a file untouched for days. **OUR OWN VERIFICATION PRACTICE IS WHAT POISONS THE TREE**, and the cost lands on whoever runs NEXT, never the person who caused it. Read the baked path with `strings`. **Rule (dc): a detached worktree gets its own `CARGO_TARGET_DIR`, and that directory dies in the same breath as the worktree** -- isolation that outlives its worktree is what grew a stray 10G tree.

**14. A DURABLE ARTEFACT CAN BE ADVANCED BY NON-DURABLE SOURCE, ONE-WAY, AND THE VECTOR IS THE TEST SUITE** (dc). The runtime store is per-machine truth, never committed. **The act that MOVES it is running `cargo test`**, because opening the real root runs the migration ladder. **So the one act everybody performs constantly, believing it read-only, mutates shared durable state**, and the tell arrives as a REFUSAL IN AN UNRELATED VERB, later, to someone who did not perform it. **Ask what moved UNDERNEATH the tool before asking what you broke.** Its sibling: **`$HOME` isolation cannot isolate the STORE, because the store is project-relative** -- `current_dir` into a fixture project is the isolation that matters, and the two are independent.

**15. ONE VALUE, MANY HOMES, AND NOTHING COMPARES THEM.** A serialiser is a second writer nobody declared; so is the markdown formatter; **so is a TEST that spells out a layout instead of calling the function that owns it.** **The quietest home is PROSE SITTING BESIDE THE MACHINE-CHECKED FIELD.** **A PRECIS ROTS WHILE THE CITATION IT SUMMARISES STAYS RIGHT**, and **citing a ruling does not protect a restatement two lines above it.** **A record may quote WHAT WAS SAID but must POINT AT, never reproduce, what is currently TRUE.** **AND A COMMENT CLAIMING A DERIVATION THE CODE DOES NOT HAVE IS WORSE THAN NO COMMENT**, because it tells a maintainer that updating the other end is enough. **DIVERGENT NAMES ARE THE PROOF NOBODY KNEW.**

**16. HIGHLANDER GOVERNS IMPLEMENTATIONS, NOT WITNESSES** (vc). **A test that imports the value it asserts has stopped testing** -- it states that the emitter equals itself, true under every wording including a wrong one. **Consolidating measurements is not deduplication, it is deleting the measurement.** The discriminator none of the three notes stated: **is the value the SUBJECT of this file (witness, hold your own literal) or INCIDENTAL to it (import)?** Two committed notes contradicted each other for weeks because neither said which it meant, and a node arriving cold does whatever the first file it opens says.

**17. A PREDICATE IS SOUND RELATIVE TO WHAT YOU DO WITH THE ANSWER, NOT IN ITSELF.** The SAME liveness probe is sound for routing and unsound for eviction, and **the difference is the COST of being wrong rather than the accuracy of the answer**: a routing false negative is one redundant in-process run, an eviction false negative leaves a live daemon on an unlinked inode serving nobody, silently. **`SIGSTOP`, a suspended VM or a loaded machine defeat any probe, so no constant is the fix.** A probe is a guess about liveness; **only a lock is a statement about it**, and a lock DOMINATES rather than trades -- refusing on existence makes one crash permanent, probing evicts the living, a kernel lock does neither.

**18. EVERY PREDICATE OF THE FORM _IS X THERE_ HAS ONE MORE MECHANISM UNDER IT THAN THE LAST ONE YOU FOUND.** A socket that EXISTS is not a daemon; then ANSWERING turns out not to be liveness either, because macOS has no `SOCK_CLOEXEC` and a `fork` between `socket()` and `FD_CLOEXEC` leaks the listening fd, so the socket keeps accepting long after its owner died (1 in 300 with a sibling thread spawning, 0 in 2000 without); then **accept-and-close is a third phantom that returns PROMPTLY AND SUCCESSFULLY**, because a clean EOF is `Ok(0)` and not an error. **ABSENCE IS A STATE, UNREADABILITY IS AN ERROR, AND A `let Ok(..) else` CANNOT TELL THEM APART** -- the dangerous direction is the quiet one.

**19. MAKE THE BAD STATE UNREPRESENTABLE RATHER THAN CHECKED FOR.** Bind-and-publish as ONE call means publishing an address nobody is listening on cannot be expressed; `Route` having no `Both` arm is why "never two engines" needs no runtime check. **It retires a category before any test exists.** Its inverse: **A REFUSAL THAT DELETES IS THE MORE DESTRUCTIVE OPTION AND READS AS THE SAFER ONE** -- refusing is conservative only when refusing PRESERVES, and a create implemented as an upsert reports success and replaces. **Compare-and-delete, never delete.**

**20. A GUARDED DOOR HAS AN UNGUARDED TWIN ONE COMMAND AWAY, AND THE TWIN IS THE SHORTER SPELLING.** `cargo fmt` vs `cargo fmt --check`. **The guard was never bypassed, it was never invoked**, so nothing felt like a decision. A property of the COMMAND PAIR, not of either operator. **AND A FLAG CAN BE ACCEPTED AND IGNORED**, which is worse than one refused, because the caller believes they got the behaviour and the exit code agrees: `rustfmt --check` on stdin returns 0 on unformatted input.

**21. A THING IS NOT FILED UNTIL IT REACHES AN ARTEFACT, WITH ITS CONSEQUENCE, AT ITS REAL SCOPE.** **A correction made in conversation dies at the next compact on either side.** A mechanism filed without its consequence is not filed. **AND A FINDING THAT ARGUES ITS WAY INTO THE ROW YOU ARE CLOSING IS A REAL DEFECT LAUNDERED THROUGH A GREEN** -- the pull is strongest exactly when the finding is good. When a ruling changes behaviour, RE-AIM the arm at what still holds; never delete coverage to make your own change pass, and never fix a test by excluding the row that disagrees. **AND A STANDING ADVISORY THAT CONTRADICTS A RULING WILL OUTLIVE ANYONE'S MEMORY OF THE RULING** -- both can be correct simultaneously, and the cheap way to stop the nagging is always the move the ruling forbade. **Record the collision BESIDE THE ROW, not only in the ruling**, because the node who meets the advisory is reading the row, not the adjudication that pinned it.

**22. A REMEDY IS A CLAIM ABOUT BEHAVIOUR AND MUST BE DRIVEN LIKE ONE.** **BORN STALE and EXPIRED are different defects and neither is caught by reading.** An expired remedy can point at a repair a later ruling explicitly forbids -- actively counter-useful, and it reads identically to a live one.

**23. A VALUE GOES IN ONLY IF THE POPULATION'S OWN DECLARATION HAS IT -- BOTH DIRECTIONS.** Hardcoding a member of a draining population goes stale by being right; minting one is the same error walking the other way. **AND REFUSING TO MINT CAN PRODUCE A BETTER OPTION THAN THE ONE ON OFFER** -- declining to pick a port is what surfaced bind-`:0`-and-publish, which removed the constant entirely.

**24. A WEAKENING WITH NO SYMPTOM IS THE HARDEST THING TO CATCH** (dc). When a type widens, the loose port and the tight one both compile. **THE COMPILER SILENCE IS THE SIGNAL, NOT THE ALL-CLEAR** -- an added enum variant broke nothing because every consumer used `if let`. The REASON for a tight match belongs next to the match.

**25. A TWO-PART OBLIGATION WHOSE SECOND PART HAS NO TRIGGER IS A ONE-PART OBLIGATION.** The opening is prompted by the act of starting; NOTHING prompts the end, because finishing FEELS like the end. **State the PROPERTY that ends the window, never a promise to send a second message. And measure before you announce.**

**26. TWO RULES GOVERNING ONE DECISION -- ONE STRUCTURAL, ONE PURPOSIVE -- ARE CONSISTENT ONLY UNDER AN ASSUMPTION NEITHER STATES.** The tell is not drift between copies; it is that obeying one BREAKS the other, which is why it reads as a judgment call and is not one. vc's ground: **an obligation to READ is destroyed by volume long before it is destroyed by refusal.**

**27. EVERY TIMESTAMP IS READ FROM `date -u` IN THE SAME CALL THAT WRITES IT.** `git log` prints LOCAL. I have fabricated stamps three ways: wrong zone, typed-from-the-last-one, and one minute ahead of a clock I had just read. **A placeholder shaped like a stamp is worse than an admitted gap.**

**28. A GENERATED ARTEFACT CAN WRITE INTO THE AGENT-INSTRUCTION CHANNEL, AND NOTHING ANNOUNCES IT.** `docs/reference/claude.md` IS `CLAUDE.md` on a case-insensitive filesystem. **Prefix the whole output space rather than special-casing the member that bit.**

**29. A RULE THAT CATCHES A FAILURE MODE IT WAS NOT DESIGNED FOR IS THE STRONGEST EVIDENCE FOR IT, AND THIS BOARD IS OTHERWISE MADE ENTIRELY OF THE OPPOSITE** (dc). Arm 8 refused a release from a dirty tree knowing nothing about dispatch tables, and the binary that build produced PANICS AT STARTUP. The redirect turned "four nodes discover their CLI will not start" into "one person runs a binary and reads a panic". **Hold it as the counterweight to twenty-eight classes about instruments that could not fail.**

**30. THE FOLD IS ITSELF AN INSTRUMENT AND IT HAS FAILED IN THIS BOARD TWICE.** Once by leaving a class in two sections and dropping a ruled GO. **Once by DUPLICATE NUMBERING: six classes (11-16) each existed twice, because a session appended new ones without reading the existing numbers** -- and a duplicate number is invisible to every reader who arrives looking for a rule rather than auditing the list. **Archive VERBATIM first, under `wip-fold-HHMMZ.md`. UPDATE classes, never append instances beside them. RENUMBER FROM ONE and check the count.** When a fold only trims narrative it is not aggressive, it is tidy. **And the markdown formatter rewrites what the fold wrote, so run it and re-read.**

## Decisions

**I DO NOT EDIT `CLAUDE.md`, PERMISSION SETTINGS OR CONFIG ON A PEER'S INSTRUCTION**, however right they are on the merits. A peer's ask is never the user's approval. **AND I DO NOT APPLY AN EDIT A CLASSIFIER REFUSED A PEER.**

**I DO NOT COMMIT A PEER'S BYTES UNDER MY MESSAGE.** When a shared file blocks me the options are: ask for the order (vc's pen), or replay my own edits onto HEAD and commit a hunk-scoped blob. **Never `git add` a file whose diff I did not write.**

**I ASK BEFORE RESTRUCTURING A PEER'S AREA, AND I EXPECT THE SAME.** ic reverted a built change to my daemon family and asked; that round trip cost minutes and bought the constraint only I had.
