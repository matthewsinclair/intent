---
node: cc
name: Control Claude
role: control
session_id: 7a90ae04-61f6-4b2f-9de7-dd81eac9bb11
heartbeat_at: 2026-08-28 15:06Z
status: active
focus: "**ST0067 IS BUILT -- all four ACs, four green ATs carrying their own ids, gate BLOCKED on satisfaction alone, which is vc`s as validator.** `be3edf70` AC-00.1 + AC-00.3 (bare `intent llm` and `llm usage_rules` answer instead of exiting 2); `8a997c1e` AC-00.4 (AGENTS.md routes an arriving agent to the guide); `a63c1c2f` AC-00.2 (the workflow half written -- ST/WP/AC/AT, canon-vs-view, sync discipline, gate semantics); `2a721732` the ledger. **TWO CORRECTIONS AGAINST MYSELF, BOTH CAUGHT BY DRIVING: the AC-00.3 remedy named `intent init`, which does NOT seed usage-rules.md in this build; and I told vc `ingest --from-md` was the markdown-to-store route when it CANNOT WRITE -- `ingest_from_md` takes `&Project`, returns a Scan, and my 10/10 tested only the parse.** NEXT, NONE STARTED, vc`s order: Laksa thread-status Superseded, then `0119`, then `0118`; file the two sync findings (my pen, cross-ref `0090`); vc`s hydrate/sync/dehydrate hypothesis is next-window."
claims: [ST0056/06, ST0056/10, ST0057/00, ST0067]
---

# Control Claude (cc)

## DOING

**ST0067 DELIVERED -- all four ACs built, covered by four green ATs, and the gate BLOCKS on satisfaction alone.** `be3edf70` (AC-00.1 bare `intent llm` serves the guide, AC-00.3 `llm usage_rules` prints the root file and refuses at exit 1 when absent), `8a997c1e` (AC-00.4 the AGENTS.md pointer, one line, outside every `[[#lang]]` block), `a63c1c2f` (AC-00.2 the workflow half of the guide), `2a721732` (the ledger). **The criteria were minted `non-test`, so satisfaction is ASSERTED rather than computed from the green coverage -- and setting it on my own build is the validator`s call, not the builder`s.** Left for vc, flagged rather than done.

- **THE AC-00.1 SHAPE IS THE POINT, NOT THE EXIT CODE.** Bare `llm` and `llm guide` are ONE match arm, not two arms with the same body -- the cheap spelling drifts the moment one door grows a flag the other does not, and the guide is the single answer to what this build ships. **Driven as an IDENTITY: byte-identical output, with a length+content control so two empty strings cannot satisfy it.** Mutation control was free: the delivered `4479264f` predates the fix, so every assertion in the new tests fails against the real prior artefact rather than a synthetic mutant.
- **I WROTE A REMEDY THAT NAMED A COMMAND THAT DOES NOT DO THE THING.** The AC-00.3 refusal first said ``intent init` seeds one`. It does not: a bare init writes `CLAUDE.md` and `intent/` and no root rules file, `--lang` refuses because `lang init` is unwired, and `agents sync` writes only `AGENTS.md`. **`claude upgrade --apply` is the verb, and I only know that because I drove it.** A remedy is a claim about behaviour like any other, and I wrote this one from the seeder`s INTENT rather than from a drive. **The fix carries that reason in the source so the next author gets the class and not just the string.**
- **AND THE SAME ERROR, ONE STEP BIGGER, AGAINST vc.** I reported `ingest --from-md` as the markdown-to-store route on a dispatch-arm COMMENT plus a bucket-walk drive. lamplight-vc measured the disk: nothing is written. **`Facade::ingest_from_md(project: &Project) -> Result<Scan>` takes no `&mut self` and no store -- it structurally cannot write, and my 10/10 tested `legacy::scan``s PARSE side and never claimed persistence.** Already filed as `0097` (devbin-vc), now independently reproduced on a second estate. **Markdown-to-store does not exist; hv`s menu had NO working option, not one.** What survives is the narrower half: the bucket walk and attachment carry are real, and they are the half that works.
- **A TEST DETECTED A REFUSAL BY ITS WORDING AND THE GUIDE QUOTES EVERY MESSAGE THE TOOL EMITS.** `dispatch_ssot` scanned stdout+stderr for `is a known command that is not implemented yet`; the guide DOCUMENTS the exit-code contract and quotes that sentence, so bare `llm` answering correctly at exit 0 read as a refusal. Now detected by exit code plus stderr. **Detecting behaviour by its words is safe exactly until something describes the behaviour -- and the guide is the one output guaranteed to describe all of it.**
- **`guide_refs_check.sh` HAS A PRE-EXISTING FALSE POSITIVE AND IT IS A TWO-HOMES DEFECT IN THE SURFACE SSOT.** It reports `intent organize` as RETIRED. The dispatch table carries **two rows for that one path** -- one `retire`, one `new-surface` -- so the check reads one and the generator reads the other. **Driven: `intent organize --help` exits 0, so the command ships and the finding is false.** Control: the identical finding fires against the pre-change guide, so my prose adds none. Reported to vc, not fixed here.

**NOTHING IN FLIGHT. FOLDED FOR THE COMPACT -- the day's narrative is archived verbatim at `.history/20260828/wip.md`, and THE FINDINGS ARE ISSUES, so this board points rather than carrying a second copy.** Conflab is on v3 with zero authored-content loss (closed this morning). Issues today: **`0110`** renderer fixed and live; **`0115`** the second cause behind it, unfixed; **`0111`** re-premised at `10f48bc9` with its surviving half fixed at `557d220d`; **`0100`** option 1 landed; **`0102`**, **`0103`** open. `0077` closed.

**DELIVERED, and this is the fact a resumed session most needs:** the pair was rebuilt on hv's GO (`d68f0395`) through `bin/devbin build all`, and both binaries name **`4479264f`**. `f02fb55f` and `4479264f` are LIVE IN EVERY ESTATE. Properties on both sides are in `73b3d142`; do not re-derive them, re-measure if you need them.

**THREE THINGS OPEN AND MINE, NONE STARTED:**

- **`0103`, the `wp list` 28-vs-71 split.** 216 rows against 531 store objects, rc=0 with ZERO rows on 71 bucketed threads. **REACH, not loss.** Pre-existing v3 behaviour. hv's when they turn to it.
- **THE V2-STATUS-BUCKET DRIVE IS DONE, AND IT ANSWERS hv's MARKDOWN-TO-STORE MENU RATHER THAN JUST SIZING IT.** **There is exactly ONE markdown-to-store route and it is `intent ingest [PATH] --from-md`** -- `render.rs:2521` dispatches it to `Facade::ingest_from_md`, and the arm's own comment settles the vocabulary: rebuilding the store from the committed JSON canon is already `sync --from-disk`, so **"markdown is what is left, and it is the whole of what is left."** It takes a PATH, so it can be aimed at another estate without standing in it. **`sync --to-store` IS NOT A MARKDOWN ROUTE AT ALL** -- it reaches disk through `ingest::read`, which walks `intent/.canon/st/ST####.json` and opens no markdown file anywhere; that asymmetry is the same one that disproved `0111`'s filed mechanism.
- **AND THE BUCKETS ARE REACHED, DRIVEN RATHER THAN INFERRED.** `legacy.rs:850` walks `COMPLETED` / `NOT-STARTED` / `CANCELLED` via `thread_dirs()`, whose comment says the walk is not optional because `st done` MOVES a thread -- "on this estate is 54 of 56". **The drive is the two tests that already existed, which is a better instrument than the fixture I was about to hand-build:** `legacy_bucket_attachments` + `legacy_bucketed_residue`, **10/10 green**, on a fixture carrying a flat thread AND all three buckets, with `the_carry_covers_every_bucket_the_thread_walk_covers` tying the carried count to the fixture's own membership so a missed bucket shows up as a short count rather than a silent pass. **THE LIMIT, STATED BECAUSE IT IS THE PART THAT COULD MISLEAD: this is a SYNTHETIC fixture, not a live bucketed estate.** A real one (Lamplight, 54 of 56) carries mess a fixture does not, and `--from-md` WRITES, so driving one is not a measurement I can take unilaterally.
- **`view_skew_check` IS GREEN AND CLOSED AT `272b359b`, WHICH LANDED WHILE I WAS FOLDING.** 8/8 verified on my own drive, `a_title_change_skews_exactly_the_views_that_carry_the_title` included; the assertion now names `intent/st/steel_threads.md` and its message states the landed behaviour instead of contradicting it. **THE ITEM OUTLIVED MY LINE ABOUT IT AND vc's, IN OPPOSITE DIRECTIONS:** mine said `NOT MINE TO FIX`, vc's board says `GO given to cc for the one-assertion fix`, and the commit had already landed between the two. **That is the day's own class recurring on the item that was its example** -- an expectation whose premise a landed decision retired, with nothing connecting the decision to the expectation. **A board line about work is a ledger and goes stale exactly like any other; the drive is the only thing that settles it, which is why I ran the test before writing this line rather than after.**

**THREE THINGS D3 OPTION 1 DID NOT CLOSE, kept because a green beside `4479264f` will imply otherwise:** **21 of the 23 rows say `pending`** and were never on any menu, so 22 of 23 still default; **`Deferred` is RULED to wait for the structural fix** (hv, option 2) and my test already asserts that boundary, so it lands with zero code change; and **Laksa's THREAD-level `SUPERSEDED` is untouched** -- a different mapper on the arm that BLOCKS where a work-package one CARRIES, so widening it would unblock a migration as a side effect. hv's.

**RULE 2 IS LIFTED.** Ordinary ceremony: announce before and after **by properties**, never a bare `cargo build --release`, never into the shared target from a dirty `native/rust`. **A build IS the delivery, estate-wide, instantly** -- measured today rather than asserted.

**THE TOOLING LIVES OUTSIDE EVERY TREE IT READS**, at `~/Devel/prj/.hop-manifests/`: `manifest.sh` (single-walk hash manifest, control proven to fail on an unreadable file and on a newline path), `prose_loss.py` (three sentinels, one normaliser applied to both sides), `verify-attach.py` (sha256, **never length** -- `bytes` is UTF-8 bytes and `len()` is characters), `extract-before.sh`.

## TODO

**1. CLOSED -- KEPT ONLY FOR WHAT A RESUMED SESSION MUST NOT UNDO.** R1 end to end (shim `645beec0`, publisher `5fcfd314`, caller `431590a3`); ruling 4 `751b1302`; F4 both halves (`3a8cec35` + `4ea3b7be`). **THE SWEEP IS vc's AND HELD** -- hv reserved it specifically; my recommendation of ONE canary estate before the other fifteen stands. **RULING 4 IS CANON-COMPLETE AND WAS LIVE IN ZERO OF 17 ESTATES; IT IS NOW LIVE IN THIS ONE** -- hv's D1 landed the shim carrier here at `7fa3c013` on 2026-08-28 (7332-byte `pre-commit.intent`, byte-identical to the template, which carries the refusal arm), and I have seen it bite on my own commits today. **The other sixteen are unchanged and are still the sweep's cost** (dc measured): every estate carries the gate as `pre-commit.intent`, NEVER `pre-commit`, and `.git/hooks/` is not where hooks are when `core.hooksPath` is set (3 variants; a sweep hardcoding `.git/hooks` misses Intent, Intentv2, Laksa). That is the sweep's cost, not the fix's defect.

**THREE NON-DECISIONS THAT ARE DELIBERATE AND ARE NOT AN UNFINISHED HALF:** (i) the **guard-runner resolver** and **critic exit 2** fail-opens stay -- each carries 0043's reasoning beside it, and a 66-second rebuild window is a different population from a shadowed binary; (ii) the **31 remedy-less error literals** are NAMED and NOT gated -- forcing a remedy onto an error that has none buys the shape by writing advice nobody can act on; (iii) **`c755bd44` (2026-08-25 22:35Z) STAYS UNRESOLVED** -- do not tidy it into the confirmed case or into an explanation. You cannot recover a time you never read, and a corrected-looking fake is worse than an admitted one.

**2. vc's RULED ORDER, NOW AT (d): THE PRUNE, GATED ON TWO THINGS THAT ARE NOT MINE.** (a)(b)(c) landed, (c) at `2cf8fa63`. **(d) DOES NOT START ON MY SAY-SO:** `at-accounting.sh` runs BEFORE any prune ships (the bucket is its only source surface), and WP-11's absent-manifest default decides what `--apply` does to 19 trees, so it is written FIRST. **A FILE THE STORE CANNOT BE SHOWN TO HOLD IS A REFUSAL NAMING IT, NEVER A DELETION.** **MEASURED 2026-08-27: 2,115 authored bucket files on disk and in NO store, across 5 of the 14 estates the sweep could walk** -- Lamplight 1,584 (333 of its 353 threads have the `attachments` key ABSENT), Laksa 499, Devbin 24, Anvil 5, Baize 3. Nothing is lost; everything is in git. **But a dehydrate or prune before the ingest destroys all 2,115, which is the measured case for the ordering rather than a preference.** Probed by sha256 through `json.load`, never a grep, and controlled. **arca_cli validates the route: 23 of 23 byte-identical in canon, 0 missing, done BY HAND per file at `5441945` -- what `2cf8fa63` now does automatically.** Sweep walks `~/Devel/prj/*/intent/.canon/st` and MISSES nested estates (`Arca/arca_cli`): 14, not the full 19.

**3a. THE TWO ENUM-ROSTER FINDINGS ARE WITH hv AND ARE NOT MINE TO BUILD** (vc ruled the third one and it is built and closed at `07ad9876`). **(i) `declared_values_are_enforced.rs` walks `values` ARRAYS and is blind to the 12 pipe-string rosters** (`"value": "terminal|md|json"`) -- it is green and cannot see them, so widening it changes what a green MEANS across the surface. **(ii) `--format` IS VALIDATED AS AN ARGUMENT TO THE RENDERER (`render.rs:3846`), so a verb whose result set is EMPTY returns before it looks at the flag** -- `issues`, `issues list`, `wp list`, `critic` accept an unknown format at exit 0 when they have nothing to show, and `wp list ST0056` refuses while `wp list ST0029` accepts. **The verdict belongs to the RESULT SET, not the slot**, and moving it is a contract change. **WHEN THE WIDENING IS BUILT, THE PREDICATE MUST BE GROUNDED IN THE REFUSAL VOCABULARY** -- `is not a format`, `is not an issue bucket`, `no json projection` -- **and NOT in `code != 0`, which red-flags `doctor --format json` because doctor exits 1 on an unhealthy project.** That is a VERDICT, not a refusal; generalising the predicate is what broke it. A control per refusal shape is part of the build.

**3b. WITH hv AND NOT MINE TO BUILD: SHOULD `publish_home` REFUSE A TEMP OR SCRATCHPAD ROOT?** vc asked for it; I declined to build it off a relay and put it to hv at source. **It is a production change to the publisher fifteen estates depend on and it is NOT free: `install.rs`'s own fixture arms publish temp roots DELIBERATELY, so a naive path refusal breaks the tests that prove the publisher works.** The other half of vc's ask -- _a test run must not publish, anywhere_ -- is CLOSED and measured (`9c2ba9ed` + `c900ec7e`; decoy HOME over the whole crate pair writes nothing at all).

**3c. THE STORED `slug` COLUMN IS VESTIGIAL AND STAYS THAT WAY TONIGHT.** hv: _"let's not overcook this."_ It is a second home for a derived value -- a real defect and not an urgent one -- flagged in `facade::slugify`, in the model, and in the table's `disposition_basis`. **The 48-char cap is a DISPLAY choice while the slug never resolves, and becomes an ADDRESS hazard the day anything resolves by it.** That is the tripwire for whoever widens this.

**3. WITH hv, AND STILL UNANSWERED.** (a) **TEST-TARGET CONSOLIDATION** -- escalated at `b97afc24` for confirmation AT SOURCE; that entry holds the price list, do not restate it. **The list has collapsed since: the 97-file `mod common;` rewrite is ZERO and the 168-row re-cite is ZERO under `#[path]` (measured on the real crate at edition 2024, both controls); the compile saving vc quoted does not exist (97 binaries x 1 copy becomes 1 binary x 97 copies, distinct addresses); binary count 128 -> 1. Link time and artefact bytes are STILL UNMEASURED and the list does not go up until they are.** (b) `intentd --version` prints no commit. (c) the v2 shell CLI prune -- `pr-checks.yml:43` runs `./bin/intent st show` on every PR, so it is delete + rework a live CI job + move ~34 BATS files.

**5. 3.0.1:** `on:` as a field on `Descoped` **if hv rules a 3.0.1 canon extract need not stay readable by a shipped 3.0.0** (`deny_unknown_fields` + `additionalProperties: false` say it would not be); bare-sync must compare to the STORE; `doctor` canon-vs-store arm; the id resolver treating every id as a steel thread; ~2 MB store growth per `st attach` + a bulk door; a tenth residue class for duplicate AT ids; the two-readers Highlander; `unclaimed_digest()` hashing absolute paths; the unbounded canon extract total; **`canon-commit` gates on ADDS, so a commit that REMOVES attachments is outside its reach**. **`at new`'s note/legacy clobber is FIXED and this board asserted otherwise for a day** -- `facade.rs:4388` is `note: note.or(carried.0)` / `legacy: carried.1`, with the reasoning at the site: absence of a note argument means _not saying_, never _clear it_. **I nearly priced an option to vc off the stale entry and checked the source instead, which is the only reason that pre-costing was not wrong.** A create on an existing id is still a FULL REPLACEMENT -- that half stands. The safe re-cite route is a canon edit plus `sync --to-store`; `sync --to-disk <ID>` FIRST, and scope by id.

**6. THIS MORNING'S MEASUREMENTS, BANKED 2026-08-28 08:47Z BECAUSE THEY WERE MADE IN A SESSION AND WOULD OTHERWISE END WITH IT.** Five, each driven rather than recalled, each re-measurable by the command named.

- **`ST0057`'s GATE IS GREEN AND hv's BOARD STILL SAYS IT IS BLOCKED. RE-MEASURED AT 2026-08-28 14:26Z RATHER THAN CARRIED FROM THE MORNING:** `intent ac gate ST0057` -> `PASS -- 66/66 satisfied, 3 withdrawn`, while `hv/wip.md:161` still reads `BLOCKED -- 51/53 satisfied, 2 withdrawn; unsatisfied: AC-08.6 AC-08.7`. **It has survived a full day and two of vc's folds.** hv's `## DOING` records `BLOCKED -- 51/53 ... unsatisfied: AC-08.6 AC-08.7`; ic built both. **That is a document asserting finished work as pending -- the class vc's globalfold caught twice on the 27th, now on hv's own board.** vc holds that pen and the entry is a state summary, so it is inside it. (`ST0056` is 64/133, recorded as 63.)
- **AND THAT `PASS` HAS A HIDDEN DENOMINATOR, WHICH IS THE FINDING THAT OUTLIVES THE FIRST.** `ST0057` WP-12 and WP-13 are both **Not Started and carry ZERO acceptance criteria**. **POSITIVE-CONTROLLED BEFORE REPORTING: the instrument sees criteria for all thirteen OTHER WPs (`AC-00` through `AC-11`, `AC-14`), so its silence on 12 and 13 is an ABSENCE, not a blind spot.** `PASS` here means _every criterion that exists is satisfied_ and will be read as _the thread is done_. **This is our own gate-figure defect, in our own gate** -- and it is the uncovered-deliverable class `AC-06.6`'s text already names, one artefact over.
- **hv's STANDING DIRECTIVE (a) IS ALREADY DISCHARGED.** `install::publish_home()` has its caller at `431590a3`; live `intent bootstrap` returns rc=0 and reports the recorded root. It is queued on hv's board "for the bounce"; the bounce found it built.
- **`AC-06.11`'s HOLD HAS EXPIRED AND NOBODY NOTICED.** `AT-06.11` reads _held RED until WP-10 lands `upgrade`_. `intent upgrade --help` returns rc=0 today. **The stated dependency is gone, so the row moved from blocked to buildable and nothing announced it** -- the same shape as a delivery target that moves without announcing itself.

## Watch-outs

**FOLDED BY CLASS, NOT BY INSTANCE. Every line cost at least one wrong answer that read as a right one.**

**1. AN INSTRUMENT CAN PASS WHILE BLIND, AND ITS GREEN IS INDISTINGUISHABLE FROM A REAL ONE. THIS IS ONE CLASS WITH MANY FACES AND IT COST FOUR NODES A DAY EACH.**

The faces, each measured rather than reasoned:

- **CANNOT EXHIBIT THE SUBJECT.** A sample that cannot contain the failure; a pattern that cannot match it. Positive-control the INSTRUMENT, not the subject.
- **A CONTROL THAT WOULD ALSO PASS BROKEN IS DECORATION.** Ask: would this control pass if the instrument were doing the dumb thing I fear? If yes, it proves nothing.
- **CANNOT ISOLATE A SHARED NAME.** `flag_reachability` is whole-file; `organize` reads an id spelled `force`, so DELETING `bootstrap`'s `--force` read left it GREEN. Measured, by doing it. **The discriminator is `"<id>"` appearing MORE THAN ONCE** -- a flag mentioned exactly once is mentioned by its own accessor, and deleting it takes the mention too, so the gate DOES fire (driven on `at new --covers`). Coverage: 33 fire, 59 shielded, of 92 -- printed by the check itself now (`003544af`), plus the 29 held only by coincidence (`8a19e215`).
- **AND THE CORPUS IS THE COMMONEST BLINDNESS OF ALL: THE ESTATE YOU ARE STANDING IN CANNOT EXHIBIT THE DEFECT.** Twice on 2026-08-27, both times as the FIRST thing I did. P1: Intent stores 0 bracketed AT citations against 318 rows carrying one, so every test here passes whether or not the porter is fixed -- vc flagged it in advance and was right. The enum measurement: every `--format` slot refuses correctly IN THIS REPO, because Intent has issues, has threads with WPs, has critic findings; an empty scratch project exposed four slots accepting an unknown value at exit 0. **The repair is never a better search over the same corpus. It is a FIXTURE that carries the defect** -- real bytes from an estate that has it, or a throwaway project that lacks what the code path needs.
- **ACCIDENTALLY RIGHT: A TRUE NUMBER FROM AN INSTRUMENT THAT COULD NOT HAVE PRODUCED A DIFFERENT ONE.** dc's _0 of 5 hooks carry the arm_ was CORRECT, measured by grepping `pre-commit`, which carries the gate in NO estate. **A false green is easier to catch than a true one: the true one gets cited later and the citation carries no trace of the blindness.**
- **THE PREDICATE ITSELF WAS WRONG.** vc reproduced a gate's CONDITION against the UNMUTATED source. Coverage is a property of the MUTATED world -- the one where the defect exists. **A control confirming your instrument computes what you told it to cannot tell you that you told it the wrong thing.**
- **A NULL RESULT IS NOT A CLEAN SHEET.** Negative-or-zero drift is the ONLY thing an honest stamp can produce, so my 4 and vc's 646 measured the same nothing.
- **AN INSTRUMENT THAT NAMES THE WRONG CAUSE IS WORSE THAN ONE THAT IS BLIND, BECAUSE IT RECRUITS EVERY READER INTO THE WRONG SEARCH.** `schema_versioning` asserted the stripped SDL keeps a THIRD of the published lines and failed with _"the `\"\"\"` toggle did not close"_. The toggle closed: 114 delimiters, even, each alone on its line. It is a RATIO, and a long description fails it exactly as a runaway does, because prose raises the denominator and leaves the numerator alone -- margin went 7 lines to 0. **vc read the message, reasoned to a bless/strip disagreement, and handed it on as a lead within a minute.** The blind instruments above hide a defect; this one SEES a real failure and misdirects. **Fix an assertion whose message names a mechanism it cannot observe, not just the threshold.** **ic HAS THE SAME CLASS AGAINST THEMSELVES:** a real rebuild-window state reported as a false GREEN when it is a false REMEDY -- building to that description would have covered a condition that does not exist and left the one that does. **A correct sighting with a wrong explanation is not half right; it aims everyone downstream at the wrong fix.**
- **A CORRECT AGGREGATE CANNOT CHECK THE MECHANISM, AND THE MATCH IS WHAT EVERYONE READS.** My re-run prediction of 12 null / 62 bare was RIGHT and derived from ONE population; the 62 is 30 bracket rows plus 32 backtick rows. **A re-run matching my number would have certified a fix that left 32 rows broken, with ST0344's seven packages still blocked and a green beside them.** Predict the SPLIT, never the total.
- **A PARTIAL FIX MAKES AN INSTRUMENT FEEL REPAIRED AND HIDES THE REST.** dc corrected WHERE the sweep looked and kept the wrong FILENAME; the second fault survived BECAUSE the first was fixed. **A repaired instrument is trusted more than an unexamined one.**

- **THE SCANNER IS IN ITS OWN CORPUS.** A guard looking for `Command::new`, `CARGO_BIN_EXE_intent` and `.env("HOME"` must SPELL all three, so it matched itself and reported the guard as its own only violation -- twice, because tightening the predicate cannot help: **any predicate precise enough to describe the hazard is one the guard must write out in full.** Exclude by `file!()`, never by a literal name a rename can silently orphan.
- **AND A SCAN WHOSE FILTER MATCHES NOTHING PASSES FOR FREE.** Assert the POPULATION before the property, or a refactor that renames what you filter on turns the guard silently blind while reporting a clean estate.

**THE ONLY CONTROL THAT CATCHES ANY OF THEM IS MUTATION: break the thing and require the instrument to notice.** Not one was caught by inspection, review, or re-reading the pattern.

**AND WHEN YOU MUTATE, ASSERT THE OCCURRENCE COUNT FIRST.** My own mutation targeted `let force = flag(m, "force");` and the assert caught TWO occurrences -- a bare `sed` would have mutated `organize` and let me draw a conclusion about `bootstrap` from it.

**2. A ONE-SIDED COMPARISON DISCARDS THE EVIDENCE THAT THE INSTRUMENT IS WRONG.** vc's scan computed source=2 against canon=3 -- impossible if the span is right -- and `[ "$n" -gt "$nc" ]` threw it away. **Compare in BOTH directions; the impossible direction indicts the tool.**

**3. A CONSERVATION CHECK WHOSE TWO SIDES DERIVE FROM THE SAME SOURCE CANNOT DETECT A WRONG SOURCE.** `carried + refused == given` is an IDENTITY, not a measurement: hand the reader an empty directory and zero/zero/zero is perfectly consistent. **The two sides must derive their population INDEPENDENTLY** -- that is the whole difference between `56517758`'s accounting and the four gates that passed over arca_cli's `2f3e836` while it held 0 of 23 attachments. **AND READING THE RIGHT PLACE IS A DIFFERENT PROPERTY FROM MAKING THE WRONG PLACE AN ERROR.** Fixing the first and calling the class closed is how `2cf8fa63` still needed `56517758`.

**4. A REFUSAL THAT ENDS THE RUN REPORTS A FLOOR AND IS READ AS A COUNT.** Both per-thread refusals `?`-ed out of `scan()`, so the first thread that could not account for itself buried every thread after it -- and because `thread_dirs` yields the top level BEFORE the buckets, what survived read exactly like a reader that cannot see buckets, and was diagnosed as one by a peer with a real measurement. **Accumulate, then refuse once, naming every thread.** An operator who fixes 8 and meets 34 more learns the tool is unreliable. **THE SAME OBSERVATION HAD TWO EXPLANATIONS NEEDING DIFFERENT FIXES, AND ONLY A FIXTURE PUTTING THE SAME ROW IN BOTH PLACES SEPARATED THEM.**

**5. ABSENCE IS A STATE; UNREADABILITY IS AN ERROR; A `let Ok(..) else` CANNOT TELL THEM APART.** `acceptance()` returned empty on ANY read failure, so a thread whose file existed but could not be read migrated with zero criteria and zero tests at rc 0, identically to one that never had a file -- the whole AC/AT contract able to vanish with no symptom. **Found because a fixture built to FAIL came back clean:** a test failing for the wrong reason is a finding.

**6. THE POPULATION YOU MEASURED IS NOT THE ONE YOU MEANT, AND NOTHING NAMES IT.** `cargo check -p X` builds no test target; `cargo test --test X` excludes lib tests; **two `--test` targets and the second never runs once the first fails -- `--no-fail-fast`**; `cargo fmt --all` reaches into peers' files. **`SUITE EXIT=101` with `binaries=0` is a COMPILE STOP wearing a test failure's clothes.** **AND A LOG YOU FILTERED IS NOT THE RUN:** I tallied a subset off a file my own `tail -25` had already truncated, and grepped it for tests that had run.

**7. THE EXIT CODE YOU READ IS THE LAST COMMAND'S, NOT THE INTERESTING ONE'S.** **(Hit AGAIN 2026-08-28: I read a refusal's rc as 0 and nearly filed a no-silent-errors issue; the 0 was `sed`'s, from `intent ... | sed`. An already-recorded class still cost a near-miss.)** `cargo test ... | tail` reports TAIL's code -- a COMPILE FAILURE printed `[exited with code 0]`. Use `set -o pipefail` and read `$pipestatus[1]` (zsh; `${PIPESTATUS[0]}` is empty). Clippy exits 0 WITH warnings; a cached build prints `Finished` in 0.16s and nothing says the edit was never compiled. **zsh does not word-split** (an unquoted path list is ONE pathspec and the commit does not happen) -- **ic hit the same thing on `git add $FILES`, twelve paths sent as one argument, and it FAILED LOUDLY; mine returned a plausible empty answer, and the silent one is the one that cost something**, an unmatched glob aborts the command, and an apostrophe inside single quotes runs nothing. **`local a="$1" b="..$a.."` IS UNBOUND UNDER `set -u`.**

**8. ONE VALUE, TWO HOMES, AND NOTHING COMPARES THEM.** **AND A SERIALISER IS A SECOND WRITER NOBODY DECLARED: `json.dumps` defaults to `ensure_ascii=True`, which would have rewritten two unrelated disposition notes while I changed four flags -- a diff touching prose nobody edited. Prove a round-trip lossless by dumping with NO edits and comparing BYTES, then apply on a COPY first.** An edit to a registered attachment is TWO writes -- the file and the sha canon records. **A GENERATED VIEW IS THE SAME SHAPE: `view_skew_check.sh` refused my dispatch-table commit because `.md` had not been regenerated from `.json`. REGENERATE, NEVER HAND-EDIT THE VIEW TO MATCH.** The contract's residue table drifted two classes from the implementation with every instrument reporting agreement, which is why `residue_class_check.sh` reads the IMPLEMENTATION. **A property must be enforced by something that NAMES it; enforcement inherited from an unrelated mechanism expires silently and the green survives the expiry.** **AND PROSE ON A TYPE IS A COMPILED INPUT WEARING PROSE'S CLOTHES:** a doc comment on `Issue::severity` is generated into the committed face as its `description`, so `8174de80` drifted the schema and reddened main. Every suite I ran was real and green and none covers a generated face -- **I chose the set by what the CHANGE LOOKED LIKE, an enforcement door plus a comment.** Ask what GENERATES from the file, never what the change resembles. **And take the sanctioned regeneration route: `INTENT_BLESS=1` rewrote TWO faces, so hand-patching the one named in the diff would have left the other drifted under a green.** **AND A DERIVED VALUE THAT IS ALSO STORED IS THIS CLASS AT REST, WAITING.** `slug` was computed from the title AND kept in a column: stale on 43 threads, absent on 21, and rendered by TWO sites when the brief named one (`render.rs` and `views.rs`). hv's fix is the general one -- **the title is the SSOT and the slug is a rendering of it** -- so `--slug` derives at render time and the column is now vestigial, flagged rather than ripped out. **A brief naming one consumer of a value is a claim about a population somebody has to enumerate.**

**9. A SHARED TREE HAS OTHER WRITERS, AND `--only` DOES NOT PROTECT EITHER OF YOU.** `git commit` commits the index AS IT STANDS, so `add` + `commit --only <paths>` is the only safe write. **I BROKE THIS TWICE IN ONE HOUR AND THE SECOND TIME IT COST SOMETHING.** Retrying past a peer's `index.lock` I used a bare `git commit -F`; the first time it held exactly my seven files, by luck, and I recorded it as a near-miss. **I then wrote that near-miss into a fold and did the identical thing in the NEXT commit, taking two of vc's authored runbook bullets into `0ba48d85 wb(cc): aggressive localfold`.** Content intact, provenance wrong, and not undone -- rewriting a shared HEAD to re-attribute three lines is worse than the wrong title. **THE CAUSE IS THE RETRY WRAPPER: I built the loop around the LOCK and rebuilt the command without the safety while I was in there. A RETRY WRAPPER MUST NOT SIMPLIFY THE COMMAND IT IS RETRYING** -- `add` + `commit --only <paths>` is the safe write whether or not a lock is present, and the lock changes nothing about that. `index.lock` means a peer is mid-commit -- **retry, never force.** **NEVER START AN ATOMIC EDIT TO A SHARED AUTHORED FILE YOU CANNOT FINISH -- AND I BROKE THE NARROW FORM TODAY: I WROTE A NEW FUNCTION AND ITS `use` AS TWO EDITS, AND ic's BUILD WENT RED IN MY FILE FOR SOMETHING THEY HAD NO PART IN.** The edit was finishable; I just did not finish it in one write. **AN IMPORT A NEW FUNCTION NEEDS IS PART OF THAT FUNCTION'S EDIT, NOT A FOLLOW-UP** -- "be quicker" is not a rule and does not survive the next time. ic moved to a detached worktree for an attributable green rather than diagnosing my half-written file, which was right.**, and **a claim about a file another node writes has a shelf life of ZERO -- AND SO DOES A VERIFICATION PERFORMED AGAINST THEIR UNCOMMITTED WORK.** I checked ic's `at_set` write path to confirm a help string I am about to ship would be TRUE, and read it out of the shared tree where their work is live and unlanded. **What I verified was a snapshot of someone else's desk, and "cc verified it" is exactly the sentence that gets quoted later.** Re-run the check against the COMMITTED code, and say out loud which one you checked. **ic's FRAMING IS BETTER THAN MINE AND IS THE REASON IT IS NOT MERELY POLITENESS: the danger is not that the snapshot might be wrong, it is that "cc verified it" OUTLIVES THE SNAPSHOT AND CARRIES NO EXPIRY OF ITS OWN** -- which is how a checked thing silently becomes an assumed thing. Pin every check to a COMMIT, never a working tree. **A BLOCKED COMMIT LEAVES YOUR FILE STAGED.** **A MUTATION HARNESS MUST RESTORE IN A `trap` -- AND A TRAP WITH RELATIVE PATHS RESTORES NOTHING ONCE THE SHELL HAS `cd`-ED.** Mine fired from `native/rust` after the last `cd`, so both `cp` lines failed with `No such file or directory`, printed `[restored]`, and left a live mutation in `intentfiles.rs`. **THE TRAP ANNOUNCED SUCCESS IT HAD NOT PERFORMED**, which is worse than no trap, because no trap makes you check. Absolute paths in the trap, and **`git diff --quiet -- <file>` is the only statement that settles it** -- a grep for the mutation string proves the string is gone, not that the file is the file. **A PEER'S BUILD IS A READER.** Work in a DETACHED WORKTREE with a PRIVATE `CARGO_TARGET_DIR` and none of it applies -- but that dir is unbounded and nothing collects it, so clear it when done. **NEVER clear a peer's cache silently: MOVE then delete, and verify survival BY HASH.** **AND `cargo fmt -p <crate>` IS A WRITE TO EVERY FILE IN THE CRATE -- `git add` by path with a wider blast radius.** I ran it while `doctor.rs` and `finding.rs` carried ic's in-flight gate work; the diffs were additions-only so nothing was reformatted, which is **luck, not care**. `rustfmt --edition 2024 <file>` on my own files only.

**AND `--only <path>` PROTECTS OTHER PATHS, NOT OTHER PEOPLE'S EDITS TO YOUR PATH.** `003544af` carries 26 of its 64 added lines from vc -- their probe fix and its comment block -- because **`git add <path>` IS PATH-SCOPED AND TAKES THE WORKTREE AS IT STANDS.** vc was losing races for `.git/index.lock`; I staged by path in between, and my message described only my change, so the record attributed their work to me. **THE INFERENCE THAT LED THERE WAS A TRUE OBSERVATION READ BACKWARDS: I found the file clean and concluded vc had not edited it -- the tree was clean BECAUSE I had already committed their change. A CLEAN TREE IS AMBIGUOUS BETWEEN "NOTHING WAS DONE" AND "SOMEBODY ELSE ALREADY COMMITTED WHAT YOU DID", and the second is the LIKELY reading exactly when two nodes are in one file.** **THE FIX IS ONE COMMAND: READ `git diff --cached --stat` BEFORE EVERY COMMIT.** It caught dc's two staged files on the very next commit, where `--only` then correctly excluded them.

**10. EVERY TIMESTAMP IS READ FROM `date -u +'%Y-%m-%d %H:%MZ'`.** Never retyped, inferred, or carried forward. I fabricated two in one day by two routes. **`git log` prints LOCAL time** and appending `Z` is wrong by exactly the offset and looks perfect; **a harness notice saying "the date has changed" carries that tool's zone, not UTC.** **A stamp you did not read cannot be repaired by inventing a better one -- mark it unverifiable.**

**11. A REMEDY THAT WAS NEVER TRUE IS CAUGHT BY REVIEW; ONE THAT EXPIRES IS CAUGHT BY NOBODY.** As-observed fidelity can ship a defect -- a faithful port of a remedy naming a verb v3 retired. **arm 10 blocked every node's commits on a `die` message recommending `int local build`.** **`zz-livebacktick` IS STILL OPEN: a bare backtick is LIVE in a double-quoted string and under bare `<<EOF`, inert under `<<'EOF'`, `<<"EOF"`, `<<\EOF` and `<<-'EOF'` -- the DOUBLE-quoted delimiter is the trap.** **A HEDGE IS NOT A LICENCE (ic): if you can name the check that would settle it, it is a TODO, not a caveat.** **AND THE REBUILD WINDOW HAS NOW BEEN MET RATHER THAN ARGUED (vc, 2026-08-27): they went to probe the pair DURING my `int build all` and got `no such file or directory` on `target/release/intent` -- cargo had removed the artefact and not yet written the replacement.** That is a real absence of the binary, on this machine, during ordinary work, lasting as long as a release build. It is the population dc's fail-open exists for and the one my refusing arm would punish, and it is the first time any of us has observed it instead of reasoning about it. **A HANDOVER IS NOT DONE WHEN THE WRITE RETURNS -- it is done when a named reader has it.** **AND A STANDING CONSTRAINT MUST BE ABLE TO SAY WHAT IT IS PROTECTING, IN THE PRESENT TENSE (vc, 2026-08-27, against themselves).** They froze release builds to protect lamplight-vc's hop-2 measurement; lamplight-vc's own preconditions then REFUSED that run because Lamplight was already ported at `4ffd0fe1f`, and the freeze stood for an hour over a measurement that was never running. **A HOLD THAT OUTLIVES ITS REASON IS INDISTINGUISHABLE FROM A LIVE ONE, and so is a stale hash, and so is a hold on an already-ported tree -- one class, three costumes, all in a single day.** vc's ask, taken: if a hold runs past an hour, ask what it is protecting RIGHT NOW. **The instrument working is the keeper: a precondition that can refuse its own operator is the only kind that helps.** **AND A FALSE BLOCKER IS WORSE THAN A MISSED HAZARD (dc): the hazard gets found again, the blocker sits there looking prudent.** **A CHECK THAT RANGES OVER N STATES AND SILENTLY EXCLUDES AN N+1th IS WORSE THAN N STATES AND A COMMENT** -- the next reader takes the check's DOMAIN for the problem's domain. **AND THE SHARPEST FORM OF EXPIRY IS A TEST WHOSE SUBJECT CHANGES UNDERNEATH IT (2026-08-27, the night's keeper).** Two `dispatch_ssot` arms drive every shipped family BARE, hunting for the ones answering _is a known command that is not implemented yet_. `bootstrap` answered exactly that until `431590a3` implemented it -- at which point **a probe for a REFUSAL silently became a live setup command against the operator's machine**, and published `~/.intent/home` to a scratch worktree that was later deleted. **Nothing in the test changed and its author did nothing wrong.** Every other unbuilt verb those arms drive carries the same conversion, latent, until the day someone builds it. **A test that is correct today and wrong on somebody else's future commit, in a file neither of them touched, is not findable by reading it.**

**12. A MISSING TEST IS A BACKLOG ITEM; AN UNRUN TEST IS A DISCIPLINE FAILURE.** **AND A PRISTINE-WORKTREE CONTROL PROVES "NOT MY DIFF" AND CANNOT PROVE "NOT THE ENVIRONMENT" -- BOTH TREES SHARE ONE PATH.** I called two bats failures pre-existing after driving them on a clean worktree at HEAD. Correct, and incomplete: they were not a code defect at all, they were the Homebrew keg being on PATH, and they went green the moment `brew unlink` landed with my file unchanged but for a comment. **The control answered the question I asked and I read it as answering a bigger one.** When a control clears your diff, the next question is what BOTH sides of it were standing on. **A mutation that fails to mutate reads as a passing control -- ASSERT THE ANCHOR COUNT.** **A control that cannot fail is not a control: make it fail once on purpose**, and run it BEFORE the mutation or a red arm reads as the fix working when the fixture was always wrong. **A MUTATION THAT APPLIES CLEANLY BUT REDS NOTHING MEANS THE MUTATION IS WRONG, NOT THAT THE CODE IS SAFE.** **PREDICT THE RED ARMS IN WRITING BEFORE RUNNING; a mutation whose honest prediction is `nothing` is not yet a mutation.** **A GUARD NOTHING CAN MAKE FAIL IS NOT A GUARD** -- if the real path can no longer produce the failing input, drive the pure function with numbers that can.

**13. A BARE ID IS AMBIGUOUS, A BARE NUMBER-WORD IS WORSE, AND A BARE MONIKER CROSSES ESTATES.** **A HASH IS A READING; THE PROPERTY IS THE HANDOVER (vc, against themselves, 2026-08-27).** They passed lamplight-vc `38bd566c` and had to correct it, because the pair moved three times in a day. **"Carries `2cf8fa63` and `7e27e0ca`, and `git diff --name-only <pair>..HEAD -- native/rust surface` is EMPTY" is true across every rebuild; a hash is stale the moment anyone touches a compiled input.** Keep sending hashes to re-derive AGAINST, never as the test. **The two compose rather than conflict: PROPERTIES answer "is this the right build", the SHA answers "is this the same build as five minutes ago" -- and only the second is a job the marker cannot do.** **AND A COUNT INHERITED FOR ONE POPULATION AND REUSED FOR ANOTHER IS THE SAME FAILURE WEARING A NUMBER: I took 16 as an ESTATE count and spent it as a GUARD-GENERATION count. Different populations, same integer, and nothing flinches** -- the real answer was 14, across three generations, with this repo the only member of its own. Every acceptance id is thread-qualified -- `ST00NN AC-XX.Y` -- because a bare `AC-03.6` is green in one thread and red in another. **A NODE MONIKER IS UNIQUE ONLY WITHIN ITS ESTATE, so a bare `cc` sent across a boundary does not read as ambiguous -- it silently RE-ATTRIBUTES to the local node of that name** (vc's relay of my sweep had lamplight-cc denying a number it never produced, correctly, at length). Carry the estate on every cross-estate reference. **AND A POSSESSIVE RESOLVES AGAINST THE ADDRESSEE, NOT THE OWNER:** name the owner, never "your", when an attribution rides with a live finding -- the attribution is what routes the follow-up. **AND A SCOPED GREEN MUST CARRY ITS SCOPE INTO THE RESTATEMENT OR STOP BEING A GREEN.** I said "the three arms you named are green" -- true and narrow; vc restated it as "main went green". **Relaying is authoring in the direction nobody audits, because a RED invites scrutiny and a GREEN does not.** State the boundary in the same breath as the result rather than leaving it to be inferred -- and refuse the bigger claim out loud: four targets passing plus a reason they cannot reach further is not "main is green", and saying so is what keeps it from becoming that.

**14. A FINDING THAT ARGUES ITS WAY INTO THE ROW YOU ARE CLOSING IS A REAL DEFECT LAUNDERED THROUGH A GREEN.** The pull is strongest exactly when the finding is good.

**15. A MECHANISM FILED WITHOUT ITS CONSEQUENCE IS NOT FILED.** "`thread_dir(id)` is flat and bucket-blind" sat in my 3.0.1 list for a day as a true statement about a function; `every bucketed v2 project migrates with ZERO attachments and exits 0` is the SAME FACT with the blast radius attached, and only the second gets picked up off a backlog. The other half was two sentences away on the same board and I never joined them. **Re-read a backlog for the FIRST form and convert it.**

**16. A REFUSAL THAT DELETES IS THE MORE DESTRUCTIVE OPTION AND READS AS THE SAFER ONE.** Refusing is only conservative when refusing PRESERVES. In the legacy parser `Err(..)` DROPS THE ROW: refusing an ambiguous authored-field row deleted **19 criteria across 8 threads**. **I WROTE THE FIRST HALF OF THIS LESSON INTO A COMMIT MESSAGE AND SHIPPED THE SECOND HALF IN THE SAME COMMIT**, four lines apart. **The load-bearing half of `refuse, never reclassify` was always NEVER RECLASSIFY.** Where the row must survive: keep today's reading, NAME the ambiguity, claim nothing new -- and emit it AFTER the row accounting, because `out.record` counts a finding as a refusal. **NEITHER INSTANCE WAS FOUND BY READING THE DIFF** -- both came out of a BEFORE/AFTER ON REAL THREADS.

**17. THE FOLD IS ITSELF AN INSTRUMENT, AND IT FAILED THREE TIMES IN ONE DAY IN MY OWN BOARD.** It left class 1 in TWO sections with neither copy a superset; it DROPPED `S, ruled GO` and a measurement from the CREATE door, so a ruled item read as unauthorised; and it left a FINISHED guard sitting in TODO. dc's fold deleted the wrong watch-out the same day. **A FOLD EDITS THE THING THAT RECORDS HOW TO EDIT, SO IT IS THE ONE OPERATION WITH NO OUTSIDE READER.** Diff every fold against its own pre-fold snapshot for **DROPPED AUTHORITY and DROPPED NUMBERS**, never only for size. **All three were found by reading the snapshot, not the board.**

**ENVIRONMENT.** Bare `intent` resolves via the `~/.local/bin/intent` SYMLINK to `native/rust/target/release/intent`, never via `.envrc` (direnv fires only at an interactive prompt); the symlink is the load-bearing artefact. **A pinned binary is exposed only when it WRITES**, and a copy parked outside the checkout cannot run hop 3 at all. `target/cc` is the private dir the shared-artefact guard's REFUSAL PATH names (`releasebuild.lib:189`), unbounded and collected by nothing -- class 15 in the tool itself. **`target/release` is the shipped pair: never `cargo clean` unscoped, and verify survival BY HASH, never by directory size.**

**LANE.** `native/**` and the v3 crates are mine; the parity harness is ic's; hooks, roster and `int hooks` are dc's; **canon writes route through vc**. `~/Devel/prj/Intentv2` IS NOT TOUCHED.

**18. A FINDING'S SCOPE IS SET BY THE SEARCH THAT FOUND IT, AND THE SCOPE IS ALWAYS SMALLER THAN THE PROBLEM.** Six instances on 2026-08-27, from four nodes, each caught by a DIFFERENT node driving it: my shared-id hypothesis was HALF the flag population (the rest are single-family ids appearing for unrelated reasons); vc's any-mention predicate was wrong about a quarter; dc's census resolved the right directory and grepped the wrong FILENAME; I concluded not-installed from the wrong DIRECTORY entirely (`.git/hooks/` vs `core.hooksPath`); vc's F4 measured through ONE type (`Failure::Error`) where three produce the format (`format!`, `eprintln!` too -- 76 literals, not 67); and vc answered a drift question in stamp-vs-commit SECONDS when the only quantity the guard sees is the MINUTE. **A SEVENTH, AND IT IS THE ONE WHERE TWO NARROWINGS STACKED:** my AT census grep was `^\- AT-.* \[`, which requires a SPACE before the bracket -- and the form I was missing is a bracket following a BACKTICK, so **the PATTERN excluded the population by construction** -- while the glob separately reached 118 of 152 files. Either alone hides 31 rows; I reported the result as a property of the corpus. **THE HABIT: before quoting a finding, name the search that produced it and ask what that search structurally cannot reach.** A hypothesis generalised from one hand instance covers the half that instance came from. **AND THE TWO QUESTIONS ARE SEPARATE: does the PATTERN admit every form, and does the PATH reach every file?** A census answers neither by being careful about the other. **AN EIGHTH AND A NINTH, BOTH MINE, BOTH ABOUT A SCOPE NOBODY WROTE DOWN.** (i) **A TEST-ISOLATION GUARD IS SCOPED TO THE AMBIENTS IT NAMES, AND EVERY UNNAMED AMBIENT IS UNGUARDED BY CONSTRUCTION.** `outside_any_project` exists because of `1ff7f2c1` -- _the suite converted the estate it was running inside_ -- and it fixtures the WORKING DIRECTORY. `bootstrap` resolves per-user state from `$HOME`, so no amount of running elsewhere could contain it: one guard, one ambient, and cwd was named only because it had already bitten. (ii) **A SHARED HELPER'S CALLER SET IS WIDER THAN THE ONE YOU ARE EDITING FOR.** `st_rows` renders `st list` AND `st sync --dry-run`; I added `get_flag("slug")`, which PANICS on an id clap never registered for the second verb, and killed it at exit 101. **Only a test driving BOTH verbs could see it** -- the flag was right and the accessor was the defect.

**19. A PROBE THAT DECIDES BY RUNNING THE COMMAND HAS THE COMMAND'S SIDE EFFECTS.** `flag_reachability` decides wiring BEHAVIOURALLY -- it runs `intent <family>` bare and matches the unwired phrase. That is the right design and it is why the check cannot be fooled by a name. **But `bootstrap` bare WRITES to `~/.intent/`, so the test suite now mutates the operator's real home directory as a side effect of asking whether a command is wired.** It wrote the correct thing here and it is idempotent, which is exactly why nobody would notice. The table has a `not_probed` list for this; the fix is a disposition and it is vc's, routed 17:37Z. **dc's STATEMENT IS BETTER THAN MINE AND REPLACES IT: the shape is not "do not execute", it is "EXECUTE WITH THE WRITE TARGET REDIRECTED" -- and nothing verifies the redirect is honoured.** dc measured 7 of 13 sibling tools honouring `ROOT=` and 6 ignoring it, with nothing at the call site distinguishing them, so "pass the variable and it redirects" is empirically false about half this apparatus. **The general form: before adding a command whose bare invocation writes, ask what already runs it bare -- and if the answer is "it redirects", ask what proves that.** vc has the `not_probed` disposition.

**20. I TYPED A HEARTBEAT ONE MINUTE AHEAD OF THE CLOCK I HAD JUST READ, IN THE SAME TOOL CALL.** 2026-08-27: the `date -u` output said `17:51Z` and I wrote `17:52Z` -- not carried forward from earlier, not a wrong-zone error, just typed by feel with the right answer on screen. **The two failure modes on this board's protocol are fabrication and the wrong clock; this is a THIRD, and it is the one the guard cannot see** -- it carries a `Z`, it lands in the past, and it increases monotonically, so all three checks pass. **THE HABIT THAT CAUGHT IT WAS PRINTING THE CLOCK AND THE WRITE IN ONE CALL SO THEY SIT SIDE BY SIDE IN THE OUTPUT** -- and that is also what later PROVED it. **vc's 648-STAMP SWEEP FOUND EXACTLY TWO STAMPS AHEAD OF THEIR OWN COMMITS IN THE WHOLE CORPUS AND BOTH ARE MINE:** `40ed5241` (this one) and `c755bd44` (2026-08-25 22:35Z). **THE UNIT THAT MATTERS IS THE MINUTE, NOT THE SECONDS** (devbin-cc, via vc): BSD `date -j -f` fills unspecified seconds from the current clock rather than zero-filling, so the seconds cancel in the guard's arithmetic and drift is quantised to whole minutes. Both rows are guard drift +60s -- they PASS at the current 120s tolerance and would fire below 60. **`40ed5241` IS PROVEN, NOT INFERRED: one Bash call read `clock: 17:51Z` and wrote `17:52Z`, so skew and NTP are excluded by construction -- a single process cannot be skewed against itself.** `c755bd44` STAYS UNRESOLVED and must not be folded into the first: same signature, but two days old with no record of the read, and the sample is not independent of the hypothesis. **AND THE NULL RESULT IS NOT A CLEAN SHEET: negative-or-zero drift is the ONLY thing an honest stamp can produce**, so my four behind-drift stamps and vc's 646 measure the same nothing. Nothing in that exchange established honesty except a transcript showing what the clock actually said. Read it into a variable and substitute the variable -- never retype what you just read, however short the gap.

**10. A CONTROL VALIDATES THE AXIS IT TESTS AND IS SILENT ABOUT THE ONE NEXT DOOR.** (issue 0102, 2026-08-28.) My prose probe carried two sentinels -- **a canon string must read CARRIED, an impossible string must read LOSS.** Both passed on every run. **Both were correct. The probe was wrong anyway**, because it normalised the CANDIDATE and not the STORE in three places, and **nothing tested SYMMETRY.** A passing control feels like evidence about the instrument as a whole, and that inference is what lets this survive. **When you add a control, write down which axis it covers AND WHICH IT DOES NOT; an unnamed axis is not a covered one.** The repair generalises: **one normaliser applied to both sides**, never a patch per site. **And the reverse case is worth as much: a UNIFORM result is evidence about the instrument, not the subject** -- "all 23 rows absent" was my lookup keyed on a field that does not exist, and no control caught it.

**11. ATTRIBUTION DECAYS INTO ASSERTION, AND A SHARED IDENTITY DELETES PROVENANCE ENTIRELY.** Three instances in one day. **(i)** I opened a paragraph _"ic's gate-not-running finding"_ and closed it with a flat claim about a live estate -- **the assertion is the half that travels**, and it was false, on the one mechanism I own. **(ii)** `git log --author` CANNOT separate us: **all sessions commit as one git identity.** Attribute by the CREATING COMMIT -- `git log --diff-filter=A -- <path>` -- never by identity. **(iii)** I then asserted a peer's authorship from that same filter **in the message warning them it could not do that.** dc's synthesis is the keeper: **"did I write this" and "is this already known" are the SAME QUESTION, and neither of us asked it** -- both are provenance; a shared index deletes the first, skipping `issues list` deletes the second. **Corollary: a commit message can assert work the commit does not contain** (`7fa3c013`, empty, its content ignored by design).

**11b. I WROTE A PSEUDO-STAMP, STRUCK IT, AND WROTE ANOTHER ONE TWO HOURS LATER IN THE SAME SESSION.** `13:5xZ`, then `14:2xZ`. Neither is a fabricated time and neither is a real one -- **a placeholder shaped like a stamp is worse than either, because it reads as a clock value to anyone scanning and cannot be checked against anything.** **The recurrence is the finding, not the first instance:** knowing the rule, having just enforced it against myself, and having the correction commit in my own log did not stop it. **The reflex it comes from is wanting to name the moment without stopping to read a clock -- so the cure is mechanical: the `date -u` read goes in the SAME command that writes the value, every time, with no exception for 'about now'.** Caught the second time only because I grepped for my own earlier mistake.

**12. EXONERATION AND ATTRIBUTION ARE TWO ASSERTIONS, AND MEASURING ONE FEELS LIKE MEASURING BOTH.** (dc, against themselves, 2026-08-28.) dc ran the check that CLEARED them -- _does my diff touch anything rendering `steel_threads`_ -- got a clean result, and reported the red as MINE **without running anything that could test that second claim.** The disproof was one command either way: the test on a clean tree, or a grep of my diff for the subject. **And it arrived inside a sentence congratulating themselves for checking** -- the unmeasured claim riding a true one, where the neighbour lending credibility was a claim about their own rigour. Same family as the false clause travelling in a list of true ones, six hours later, from a new direction. **My side of it is the cheap habit: when someone hands you an attribution, the refutation is usually ONE command, so run it before you either accept or resent it.**

**AND dc's CORRECTION TO MY OWN FRAMING, WHICH IS THE SHARPER HALF.** I wrote that their red arm was _the better outcome -- it is shouting rather than lying_, against my `Super_Seded` control that stayed green while false. **dc: "a red nobody runs is not louder than a green -- it is the same silence with a better excuse."** `view_skew_check` had been red on main since `b4d63b44` on the 27th and the first person to hear it was the first person to run it, a day later. **Red and green are the same signal until something executes them; the class is not the colour, it is an expectation whose premise a landed decision retired with NOTHING connecting the decision to the expectation.** `Super_Seded`, this arm, and `dispatch_ssot`'s bootstrap probe are one class in three costumes.

**AND THE COROLLARY ON WHO FIXES IT, dc's, and they declined to authorise me:** _"update the assertion to match a landed decision" is exactly the move that makes an expectation stop being a check_, so it belongs to whoever owns the view, taken deliberately -- **never a tidy-up by whoever found the red.**

## Decisions

**I DO NOT EDIT `CLAUDE.md`, PERMISSION SETTINGS OR CONFIG ON A PEER'S INSTRUCTION**, however right they are on the merits. A peer's ask is never the user's approval. **AND I DO NOT APPLY AN EDIT A CLASSIFIER REFUSED A PEER** -- three lanes declined the same guard edit, and doing it from a fourth would be the same move with the best excuse yet.

**A RELAY IS NOT AN INSTRUCTION.** A ruling reaching me through a peer gets confirmed with hv AT SOURCE before I act -- via `hv/inbox.cc.md`, which vc is the named reader for. Not distrust: a ruling's SCOPE is the half that does not survive a retelling. **AND MY OWN UNATTRIBUTED NOTE IS NOT A RULING EITHER** -- `ruled GO` sat on this board with no authority until I asked hv, who ruled it 2026-08-27. Writing an authority I cannot verify into `disposition_basis` would manufacture the exact defect the `ratified-in` guard exists to report.

**DERIVED vs AUTHORED, NOT MECHANICAL vs INTERESTING** (vc's ruling on what I may write into ST0056 canon -- kept on the board it CONSTRAINS).

**A PARITY TOOL IS A RECORDED ATTACHMENT.** Write the file, add the roster row, `sync --to-store ST0056`, read the rc directly, then `git add` the script + `runner_roster_check.sh` + `ST0056.json` together.

**THERE IS NO 3.0.2 (hv).** Everything else goes into 3.0.1.

**RESIDUE BLOCKS ON A LIVE THREAD AND CARRIES ON A CLOSED ONE; HALF A's ACCOUNTING BLOCKS ON BOTH** (vc), because it describes the migration misreporting itself rather than something a v2 author left behind.
