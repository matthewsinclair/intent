---
node: cc
name: Control Claude
role: control
session_id: ae8c8153-6f3f-438f-b96b-04bd381ad4ed
heartbeat_at: 2026-08-31 18:43Z
status: active
focus: "FOLDED 2026-08-31 18:08Z, pre-fold verbatim + sha-verified at .history/20260831/wip-fold-1808Z.md. NOTHING IN FLIGHT, tree clean of me. ONE LIVE OBLIGATION: AC-09.6's refusal, written and compile-verified, held as a patch OUTSIDE the tree, blocked on ic's narrowing, WHICH IC REVERTED DELIBERATELY RATHER THAN RUSH TWO GATE REQUIREMENTS AT A WRAP -- a clean tree there is NOT a lost commit. FOUR THINGS I BELIEVED THIS MORNING WERE FALSE AND MEASURING KILLED ALL FOUR, one of them my own instrument. RE-DRIVE EVERY FIGURE: the delivered pair names abe69906, and dc re-drove that pin at c454ae80 -- it is CORRECT and there is nothing to rebuild."
claims: [ST0056/06, ST0056/08, ST0056/10, ST0057/00]
---

# Control Claude (cc)

## DOING

**TREE CLEAN OF ME.** Delivered pair correct at `abe69906`; dc re-drove the pin at `c454ae80` and my own commit gate re-confirmed it (`currency ok -- no non-test file under native/rust has changed since the commit the pair names`). NOTHING TO REBUILD.

**`AC-09.6` IS CLOSED, BOTH HALVES.** ic narrowed the 21 at `f435cc10`; my refusal landed on top at `ea68dddd`; `mcp_surface` green throughout, because the narrow got there first. **THE `mcp_surface.rs:71` RED I PROMISED TO OWN NEVER HAPPENED, AND THAT IS THE SEQUENCING WORKING RATHER THAN A MISSED PREDICTION** -- it was conditional on landing ahead of ic.

**WAITING ON vc (pen, hv afk) FOR ONE DISPOSITION:** whether the 8 `keep`-classified UNSTABLE rows get reclassified, and how `AC-06.3` is reworded. Sweep + TAP held in scratchpad so nothing is re-run. See TODO.

## TODO

- **`AC-06.1` IS BLOCKED BY A MEASURED FACT, NOT BY REMAINING WORK, AND `AC-06.3` IS FALSIFIED AS WORDED.** (driven 2026-08-31 18:43Z, pinned revision `d978d8c8`, in a DETACHED WORKTREE so three live peers could not contaminate it.) `coverage_map.sh` REFUSES to publish a figure -- the committed burn baseline (2026-08-15, `c60cdbd`) no longer covers the on-disk estate: 16 never measured, 1 stale row. **ITS OWN PRESCRIBED REMEDY DOES NOT FIX THE AC.** Re-running `burn.sh` yields 113 rows with **27 UNSTABLE where the committed baseline had ZERO** -- UNSTABLE means the default run is not green, so the burn delta carries no information and about a quarter of the estate is unmeasurable by this method.
  **8 OF THE 27 ARE CLASSIFIED `keep`**, the class whose whole promise is _runs unmodified against `INTENT_BIN`_. At least 2 (`agent_commands.bats`, `plugin_commands.bats`) fail because `intent claude subagents` was **DELIBERATELY PRUNED at `125f601d`** (AC-12.1's cutover prune). The prune is right; the register still says `keep`. **THAT IS A DEVIATION DISCOVERED AFTER LAND TIME, WHICH IS EXACTLY WHAT `AC-06.3` ASSERTS NEVER HAPPENS** -- one confirmed instance falsifies it as worded, and the honest exits (reword / descope / withdraw) are hv's, not mine.
  **NOT CLAIMED: the other 6.** They show generic status-1 and their causes are NOT established. Two instances do not license a claim about eight.
  **CONFOUND ELIMINATED BEFORE REPORTING**, because I nearly shipped this as an artefact of my own isolation: a fresh worktree has no build output and no DB, so I ran a failing file in BOTH trees and got the IDENTICAL error. The failures are real.
  **I HAVE DELIBERATELY NOT INSTALLED THE NEW BASELINE.** Swapping a zero-UNSTABLE baseline for a 27-UNSTABLE one changes what every downstream consumer reports, and reclassifying those 8 is a disposition. Reported to vc; awaiting the ruling.
- **MY OPEN GATE ROWS, UNTOUCHED TODAY AND ALL FIVE STILL RED**: `AC-06.1` (conformance green + `coverage_map.sh` reporting no gaps), `AC-06.3` (register complete, every deviation recorded at land time), `AC-10.5` (fleet corpus artefact conservation), `AC-10.6` (rollback exercised for real on the canary). **`AC-06.8` IS NOT MINE TO CLOSE** -- `flag_reachability` is 6/6 green, all 152 declared flags carry a disposition (116 keep / 26 retire / 10 intrinsic / **zero pending**), and both remaining unread flags are elsewhere: `0180` ruled withdrawn to dc/ST0058, `0181` hv's call via ic. **Do not sequence anything behind me on it.**
- **WP-10 IS THE CHOKE POINT FOR vc's LARGEST BLOCK, AND THE CHAIN IS TWO PACKAGES DEEP.** `AC-00.2`, `AC-00.8` and `AC-00.10` sit behind WP-10, which its own cover makes dependent on **WP-06 (mine) AND WP-07 (dc's)** -- _the corpus snapshot depends on cc's v2.19.0 consumer sweeps completing first_. Read off the cover rather than inferred; vc has taken it and re-sequenced.
- **`0086` IS THE ONLY DEDICATED-BUT-EMPTY ISSUE AND ITS COMMIT CARRIES ZERO PROSE.** If it needs a body somebody has to WRITE one from the code -- a different task, a different cost, a different author. Same for `0063`. `0095`/`0096` are one-word titles (`help`, `severity`) that read as stubs needing a DISPOSITION rather than records needing a body; hv's, vc carries them.
- **THE POST-BANNER LOSS IS FILED AND SCOPED TO hv, NOT TO ME.** `0192` rewritten by dc at `16cdd671`; vc carries the fix-before-the-tag recommendation. **If hv rules it in, the refusal goes in `info_read_back`, NOT in `authored_regions`** -- truncation keeps its single purpose, and a function that both truncates and refuses is two jobs in one home.

## Watch-outs

**MY OWN INSTRUMENT REPORTED A PROXY AS THE SUBJECT, IN A SCRIPT I WROTE TO BE CAREFUL.** I asked _does the filing commit carry prose beyond its subject line_ and reported the answer as _does this issue have recoverable archaeology_. **Those are one question only when the commit is ABOUT the issue**, and an issue filed incidentally inside a larger commit is the COMMON case. It cost a real downstream error: vc re-sequenced work on my figure before I caught it. **The discriminator is whether the filing commit's SUBJECT names the issue** -- and on that reading the archaeology task is EMPTY rather than smaller. This is the same defect as the `st show` blind spot below, committed by me, hours later, with the lesson already on this board.

**CONSTRUCTING BEATS READING, AND IT WENT THREE FOR THREE TODAY.** `0192` settled only when I built the estate that could exhibit it; `0078`'s disposition came from constructing a hand-placed skill where dc had read a listing and correctly concluded the wrong thing; the scoped-sync claim died when driven. **A code path is evidence about a code path. Only a drive is evidence about behaviour** -- and the corollary that cost the most: **a drive on a corpus that cannot exhibit the change is not evidence either.**

**AN INSTRUMENT CAN REPORT THE RIGHT SHAPE OF ANSWER FROM THE WRONG PLACE, AND NO AMOUNT OF CARE CATCHES IT.** `st show` prints id, title, status, created and NOTHING ELSE -- no Objective, no Context -- so a correct `0192` drive reported the edit missing while the db already held it. **vc hit the identical blind spot independently on the same issue.** Two of my three errors on `0192` were instrument errors, not reasoning errors, and one sqlite query was the only thing that could have caught either. **Reach for a DIFFERENT instrument, not a more careful reading of the same one.**

**A GREEN OVER A CORPUS THAT CANNOT EXHIBIT THE CHANGE IS NOT EVIDENCE ABOUT THE CHANGE**, and its sibling: **assert the denominator, never print it and trust it.** A walk that finds nothing satisfies every per-item assertion vacuously and goes on passing after a relocation moves the estate out from under it. `generated_views_are_unaddressable.rs` panics on an empty population for exactly this reason.

**DRIVE AN INSTRUMENT TO BOTH VERDICTS OR ITS GREEN MEANS NOTHING.** The VIEW_NAMES gate was proven by REMOVING `info.md` from the constant and watching it fail with the right diagnostic, and by planting a non-view segment that must pass -- so a probe that refused everything could not masquerade as success. A one-sided witness is decoration.

**A CLAIM IN A COMMENT OUTLIVES ITS BASIS AND NOTHING WATCHES THE JOIN.** `st_edit_opens_or_prints.rs` asserted the getting-started invocation had been broken since `c4709d3f` -- **false two days later**, by a ruling its own header cited a few lines earlier -- and the false claim was LOAD-BEARING, being the stated reason every arm routed around `info`. `0087`'s row asserted a coupling the prose had already been corrected to deny. **When a comment names its own exit condition, EXECUTE it; when it names a fact, re-drive it.**

**PROVENANCE IS CARRIED BY PATH, AND ADJACENCY IS NOT PROVENANCE.** Every node commits as the same person and every issue reports `reporter: Matthew Sinclair`, so the one field built to answer _who wrote this_ is unreadable on both surfaces. Read the touched PATH. **When the path does not resolve it -- `fab98c96` touches no board -- say UNRESOLVED and hand over the signals as signals.** Commit adjacency is the same species of inference as reading an author field, and treating it as evidence would destroy the method's only value.

**I DO NOT WRITE INTO A PEER'S ISSUE, AND vc ADOPTED THIS AFTER RULING THE OPPOSITE.** Rewriting a peer's issue under my hand is the register equivalent of committing their bytes under my message. `0192` was dc's (`7219dfc3` touched `whiteboard/dc/.history/`), so the measurement went to dc and dc landed the rewrite. **And dc improved on my own recommendation**: they APPENDED under a dated rule rather than rewriting, because the body was marked RESTORED ARCHAEOLOGY and a rewrite would have quietly broken the restorer's promise that nothing in it was theirs.

**THE SHARED BUILD DELETES BEFORE IT BUILDS (`0196`), AND THE WINDOW IS SURVIVABLE ONLY IF YOU CHECK AFTER.** `guarded_release_build` runs `cargo clean -p intent-cli -p intentd --release` first -- measured, `Removed 49 files, 53.0MiB`, 1m18s to return -- and every non-success exit leaves both `~/.local/bin` symlinks dangling for every live session. **The delete is sound** (the stamp is embedded at compile time and cargo does not track it); the restore is what is missing, and **the dirt verdict is already right and must not be undone by a fix.** ANNOUNCE BEFORE ENTERING IT, and verify the pair after -- the run that caused the outage exited 1 at `verify_pair` and I moved on without reading it.

**A MISSING ARTEFACT READS AS AN EMPTY ANSWER, WHICH IS WORSE THAN AN OUTAGE** (dc's W28, and `0196` is its mechanism). A deleted `intent` returns rc=127 with nothing on stdout, indistinguishable from a verb that ran and found nothing. **Pointed the other way too: after the rebuild the running `intentd` held the DELETED inode and went on answering `live`** -- absent artefact, healthy report, nothing observable saying they had diverged.

**THE EXIT STATUS YOU READ IS THE LAST THING THAT RAN, AND IT ALWAYS REPORTS SUCCESS.** `cmd | tail` reads tail's; `cmd > log; echo rc=$?` reads the echo's. **It fired FOUR more times today**, twice in one command. **zsh has no `PIPESTATUS` -- it is `$pipestatus[1]`, 1-indexed** -- and `cargo fmt` takes no `--manifest-path`, so it fails while a piped `rc=` prints 0. Capture the status directly off the command with nothing between.

**`sed -i ''` SUCCEEDS WHEN ITS PATTERN DOES NOT MATCH, SO A `||` FALLBACK NEVER FIRES.** That made a negative control VACUOUS -- test C of the `0192` confirmation made no edit at all and I read its rc=0 as a finding. **Assert the precondition before the act**: count the marker on disk BEFORE running the verb.

**`grep` IS `ugrep` ON THIS BOX AND A PATTERN STARTING WITH `-` IS READ AS A FLAG.** `grep -F "- AC-00.1 "` returns `invalid option`, 27 times, looking like 27 missing rows. Use `-e`, or awk. **And `cwd` PERSISTS BETWEEN Bash CALLS** -- it bit three times today, including `git` refusing a path that existed. Absolute paths, or `--manifest-path`.

**A THREAD MUST BE REALISED OR IT HAS NO COVER, AND A FRESH `.intentfiles` DECLARES NOTHING.** `init` writes a manifest of comments only, and **_a file present and declaring nothing means keep nothing_** -- so `st new` + `sync --to-disk` produces canon and no view. `st start` realises it. **This is why a fresh-estate drive of anything view-shaped measures the wrong thing.**

**`SERVED` IS NOT A HIGHLANDER VIOLATION AND MUST NOT BE RE-DERIVED AS ONE.** `mcp_surface.rs:30` says plainly it is anchored to an INDEPENDENT declaration on purpose. Recorded so the next reader meets the reason rather than the shape.

**A REMEDY THAT IS ACTIONABLE AND WRONG IS WORSE THAN A BARE ONE.** `intent:///` -- scheme, EMPTY authority, path. A remedy must not name the form it just refused, and one wrong in a single instance is usually wrong for the whole ARM.

**MEASURE THE SHIPPED BINARY, AND CHECK WHICH TREE IT MET.** The keg is read-only and drivable under a temp `HOME`; it settled `0078` and, in dc's hands, ruled out a main-only regression on the post-banner loss. **A store-touching keg drive in THIS tree is refused before reaching the behaviour** (schema 16 vs 13), so drive it in a tree the keg initialised. **And `merge-base --is-ancestor <fix> <tag>` before calling any keg/tree difference a regression.**

**MY EXTRACT WRITES RENDER, SO A PEER'S STORE-ONLY EDIT LANDS ON DISK UNDER MY HAND.** Announce it and leave it staged for them. A peer's staged path blocks my unrelated commit because guards read the index, and a live `index.lock` is a WAIT. Never `--no-verify`, never unstage a peer's path. **NEVER start `intentd` under the real `$HOME`. NEVER invoke `intent fc`.**

## Decisions

- (2026-08-31, CORRECTED 2026-08-31 18:27Z) **`close --note` STAYS RULED OUT, BUT NOT BECAUSE THE CAPABILITY ALREADY SHIPS -- THAT WORD WAS FALSE.** `issues edit --from <file>` -> rc=0 then `issues close <id>` -> rc=0 is real, and I drove it. **I DROVE IT ON THE TREE BINARY, WHICH IS THE ONLY PLACE IT EXISTS.** Driven by me on the keg: `/opt/homebrew/opt/intent/bin/intent issues --help` lists `list / add / show / close / open` and NO `edit`. **THE RULING SURVIVES ON A WEAKER AND CONTINGENT FOOTING** (vc, who caught their own justification and sent the correction rather than leaving it): it holds only because v3.0.1 ships FROM THE TREE, so the tree's surface is what users will get. It was never redundant on what users have today. **AND MY `verified independently` WAS WORTH LESS THAN IT READ.** I was independent of vc's REASONING and not of their INSTRUMENT -- we both drove the tree, agreed, and the agreement looked like corroboration. Two nodes measuring the same wrong artefact is one measurement reported twice. **THE VERSION STRING CANNOT ARBITRATE THIS AND THAT IS THE REUSABLE HALF:** the keg says `intent 3.0.0 (80d8b2ca)` and the tree pair says `intent 3.0.0 (abe69906)`, SAME VERSION, DIFFERENT VERB SETS -- keg is an ancestor 1516 commits back, dated 2026-08-26. So _does X ship in 3.0.0_ is not answerable from the version at all while two 3.0.0 artefacts differ; only the commit answers it. Same shape as the pair marker being provenance and not an identity, one layer up, and it bites anything that cites a version in release notes or an AC. **A schema change plus a store migration to collapse two working verbs into one still buys keystrokes**, which is not what a tag window is for, so the conclusion is unchanged.
- (2026-08-31) **hv's `0192` RULING HAD A DEAD PREMISE AND COSTS ZERO BUILD.** The read-back is wired end to end: `sync --to-store` -> `render.rs:1047` -> `Facade::sync_from_disk` -> `facade.rs:3137` `ingest::resync` -> `resync_inner` -> `carry_info_round_trip` -> assigns `objective`/`context` -> `store.rebuild`. **Three independent drives agreeing, two of which started from a wrong instrument.** The surviving defect is narrower and is silent loss at rc=0 on post-banner text; **`intent doctor` DOES catch it** (dc), so the honest form is _the verb you ran reports success_, never _nothing catches it_.
- (2026-08-31) **A LADDER IS WALKED IN FULL AND THE FIRST HIT DOES NOT WIN.** Precedence would silently pick the thread for 48 of 69 numbers on this estate. Every rung is probed, HITS are counted, and order fixes only the REPORT's order.
- (2026-08-31) **ONE PRESENCE PROBE, DERIVED, NOT TWO INJECTED** (vc, accepting over their own ruled shape). The condition rides in the doc comment: if a face ever needs them to DIFFER, that is a ruling and not a quiet second injection.
- (2026-08-31) **A CONTRACT WITH A CONSUMER IS ASKED FOR, NEVER MINTED.** ic named `daemon status --format json`'s shape and I built that. **Driven at fold: `terminal` and `json` both real, `zzz` refuses at rc=1 naming the roster** -- so vc's narrow-it ruling is superseded by the projection, and `AC-06.8` no longer has this row against it.
- (2026-08-31) **AN OWNERSHIP SPLIT IS A PURPOSE, NOT A BOUNDARY** (ic, correcting me). Reverting so a peer can rebuild byte-identical code is churn wearing Highlander's clothes; the split exists to stop DIVERGENT work. **AC-09.6 is the same split done right** -- two halves, two hands, two commits, one order.
- (2026-08-31) **THE TWO ENCODINGS OF "GENERATED VIEW" ARE NOT TWO HOMES FOR ONE FACT.** `VIEW_NAMES` carries names `classify` never sees; only their OVERLAP must agree, one-way. Asserted at `5f58265b` by a test that **deliberately does not import the constant it tests**, because reading the list would prove only that a list equals itself.
