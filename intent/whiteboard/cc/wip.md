---
node: cc
name: Control Claude
role: control
session_id: de593af7-4532-475b-8570-2eaa09bba904
commit_session_id: NONE ON THIS SESSION'S COMMITS -- read off my own 981a55049 with the grep below and it came back EMPTY, while vc's b13d58d2c four commits earlier carries session_01QdJZysgcMJ1SEeyo7wAUpE. So the marker is written by SOME commit paths and not mine, and the previous value on this line (0167bZhMQsEXFM5JZUZxL5g7) is a different session's and has been deleted rather than carried. UNEXPLAINED, not investigated -- it is a lead for whoever owns the stamper. READ IT WITH grep, NEVER WITH THE TRAILER PARSER: git's %(trailers:key=Claude-Session,valueonly) and git interpret-trailers --parse return EMPTY on EVERY commit here, because the mandated (C) line is a non-trailer line in the final paragraph and git rejects the whole paragraph. THE WORKING READ: git log -1 --format=%B <sha> | grep -o 'session_[A-Za-z0-9]*'. POINT-IN-TIME -- read it off your own last commit, never off this line.
heartbeat_at: 2026-09-08 15:03Z
status: active
focus: "AGGRESSIVE LOCALFOLD 2026-09-08 14:59Z-2026-09-08 15:03Z FOR A COMPACT -- STATUS STAYS ACTIVE. Pre-fold verbatim at .history/20260908/wip-prefold-1459Z.md, cmp-verified BEFORE the first edit AND USED: my first merge pass deleted the `## Decisions` header and the restore was byte-exact. FOLDED ON EXECUTION AND BY BOUNDARY TEST, not by bytes -- 49 watch-out families to 46, A19 into A14, A23 and N into A21. NOTHING OF MINE IN FLIGHT. hv OWES: the version scope (18 of 18 added paths declare new-surface), the CHANGELOG `- unreleased` form, and dispositions on 0175 0205 0268 0271 0272. NO FIGURE ON THIS BOARD IS EVIDENCE; RUN THE VERBS."
claims: [ST0056/06, ST0056/10]
---

# Control Claude (cc)

## DOING

**NOTHING IN FLIGHT. NOTHING OF MINE DIRTY.** Verified, not recalled.

**THE PAIR: I published at `b070c158f`, verified as a SET. Whether it is CURRENT is not a fact this board can hold** -- peers land under `native/rust` continuously. Ask, never read: `intent --version`, then `git rev-list --count <marker>..HEAD -- native/rust surface docs/design`.

**DELIVERED 2026-09-08. The commits carry the reasoning and the controls; this is a pointer list, not a summary.**

- `37e799f0` `organize -v/-q` -- `30561a54` `st list` project heading -- `10b4232e` `0271` -- `4d296181` `0192` -- `290261fc` `daemon restart` and the `stop` postcondition it exposed.
- `2386163d`..`70a92018` **the version mechanism**, five commits: sidecars declared, a build-time `VERSION == CARGO_PKG_VERSION` assertion so a drifted tree does not compile, the release commit scoped off the shared index, `version set` delegation, and a dry run that named a flag the code never passed.
- `b31ecbc7f` + `b070c158f` **`0280`** -- skills sync held byte-identical skills as conflicts, and the hold asserted edits it could not know about. Published; live state healed.
- `b71363d4b` **`0279`** -- eight tests hardcoded `install::MARKER`'s value, plus `marker_has_one_home.rs` so there is no ninth. Test-only, so no republish.
- `bd4d63dff` four intra-doc links of mine that resolved to the wrong `Outcome`. `int check doc` 77 -> 73; **the 73 are pre-existing and estate-wide, deliberately untouched** -- a sweep of them is a decision, not a tidy to slip into a fix for my own four.
- `54d0497e3` the 3.0.1 CHANGELOG described none of my delivered work; six entries now.

## TODO -- startable, mine, smallest first. EVERYTHING HERE IS UNBUILT.

**FOLDED ON EXECUTION AT 2026-09-08 14:59Z. Every _CUT BY EXECUTION_ banner from the previous fold is DELETED rather than restated -- a banner recording what a past fold removed is the supersedes-marker `restart.md` forbids, and five of them had accumulated.**

**THE VERSION-PLAN CORRECTIONS ARE CUT FROM HERE BECAUSE THEY LIVE AT THE SITE**, which is where I deliberately put them: `bin/.devbin/config.yaml` carries why `version_from` is not declared and why Intent's `Info.plist` is not a sidecar though Conflab's is. A board is not the home for a fact recorded in the file it governs.

**THE TWO NOT-MINE ITEMS ARE CUT BECAUSE THEY HAVE A NAMED OWNER, NOT BECAUSE THEY ARE DONE.** vc has taken both and said so: the narrower `claude upgrade` verb (a flag, a dispatch-table row, tests) and devbin's `version check` printing the same green over one sidecar and over none, routed to devbin-vc.

- **S** `Verbosity` HAS TWO HOMES AND vc AGREED I TAKE IT ONCE `--scope` LANDED, WHICH IT HAS. `organize` resolves quiet-wins-over-verbose in `Verbosity::of`; `fn doctor` resolves the identical rule inline. Not unified at the time on purpose -- vc was inside `fn doctor` and a shared symbol is the `A4` coupling. **That condition is now spent, so this is startable.**
- **S-M** `SERVED_BY_DAEMON` (`render.rs`) is ONE entry **and a `const`, so it can only ever hold PAYLOAD-FREE variants.** Project the dispatch table's payload-free arms, and **REFUSE on a daemon-eligible arm carrying arguments rather than skip it** -- silence there reports a clean table while a verb quietly loses daemon coverage.

**UNCLAIMED `ST0057/00`, AND THE QUESTION IS STILL OPEN WITH vc.** `intent ac gate ST0057/00` answers _WP-00 does not exist in ST0057_; the thread runs WP-01..WP-13, 11 Done, `12` and `13` Not Started. **A claim that resolves to no work package reads as coverage at every pickup and reserves a scope nobody can open.** vc claims `ST0057` whole, so it is not unowned. **If `12` or `13` was meant to be mine, vc says which and I re-claim it** -- I am not guessing an id back into existence.

## Holds -- mine, with the CONDITION that releases each

**Neither is released. A hold whose condition still stands is never archived by a fold.**

- **M** `AC-06.1`'s coverage half -- RELEASED WHEN a burn TSV **covering the estate** exists AND `INTENT_BIN` resolves to one binary. **Re-driven 2026-09-05 16:00Z, unmet:** a burn TSV EXISTS (`parity/tools/burn-baseline.tsv`) and **existence is not coverage**, which is the distinction this hold keeps. `INTENT_BIN` unset; four `intent` binaries reachable. `coverage_map.sh` refuses to publish and is RIGHT to.
- **L** `0216`/`0226` fix -- RELEASED WHEN a monotonic version the ingest does not own exists. The obvious fix collides with `written_at`, which the ingest rewrites wholesale.

**ARCHIVED AT THIS FOLD BECAUSE ITS CONDITION WAS MET AND ITS ONE-BOUNCE VISIBILITY IS SPENT:** `ST0056/10`'s behavioural half, released 2026-09-05 17:26Z once `intent` and `intentd` agreed at one marker. **The discriminating form is in the pre-fold and is worth re-reading before any binary-currency claim: `git rev-list --count <marker>..HEAD -- native/rust surface`, PATH-FILTERED.** The unfiltered count said 25 and would have had me judging a threshold by feel; the filtered one said 0.

## Decisions owed by hv -- question, options, recommendation

**FIRST, AND IT IS NEW AS OF 2026-09-05: an escalation already on your desk was resting on a false constraint.** `AT-10.5`'s canon note justifies a package re-sequencing as _the second instance in one session of the missing `issues edit` verb blocking a correction_. **The verb SHIPS** -- the release binary's help reads _Correct an issue's record: its prose, its title, its severity_, and `dispatch-table.json` carries it four times. vc has struck the premise in place rather than moving the note. **The re-sequencing now stands on its own merits or does not; no recommendation from me, because the question changed rather than the answer.**

- **THE FOUR CANON ROWS ARE COMMITTED, NOT UNCOMMITTED -- THIS LINE SAID THE OPPOSITE UNTIL 2026-09-08 AND IT WAS ABOUT TO GO INTO A STATUS REPORT AS FACT.** Re-measured when vc asked for state rather than repeated from here: `0205`, `0268`, `0271`, `0272` all read `status: OPEN`, all four canon files were last written by `fc3ce58d9`, and the tree is clean -- so they landed at EOD and the board never caught up. **What you owe is a DISPOSITION, not a commit decision**, which is what `restart.md` said all along while this line disagreed with it. `0205` and `0272` are DELIVERED and want a word; `0268` and `0271` are filings that want one.
- **WHAT VERSION IS THE NEXT CUT? 3.0.1, OR LARGER?** `CHANGELOG.md` already says `## [3.0.1] - unreleased`, so the estate has declared it -- but **2383 commits** since the tag include NEW SURFACE (the `app` family, the daemon web face, `daemon restart`), and your own rule is that shipped-as-broken fixes are patches while new surface is not. **(i) 3.0.1, following the CHANGELOG (ii) 3.1.0, and the CHANGELOG heading moves in the same change. NO RECOMMENDATION** -- it is a release-scope call and the evidence points both ways. **AND THE NUMBER IS NOW A CLASSIFICATION RATHER THAN A COUNT, WHICH IS WHAT MAKES IT USABLE:** `surface/dispatch-table.json` declares 142 command paths at HEAD against 124 at `v3.0.0`, and vc measured that **all eighteen added declare `disposition: new-surface`** -- not one is a pre-existing surface finally declared. The table IS the surface: `spine.rs` builds clap from it, so a path absent at the tag had no subcommand. **HONEST LIMIT, vc's and mine: neither of us built the v3.0.0 binary and drove it, so the argument is structural rather than observational.** **NO RECOMMENDATION FROM EITHER OF US; the scope call is yours.** **THE MECHANISM NO LONGER BLOCKS EITHER ANSWER:** `bin/devbin version bump patch|minor` writes VERSION and the Rust workspace together, the lock follows in the release handler, and a drifted tree stops compiling. **Your word is the only input left.**
- **FIFTEEN DECLARED COMMAND PATHS HAVE NO RELEASE NOTE.** Of the eighteen added since the tag, 3.0.1 now documents three (`ac edit`, `at edit`, `daemon restart` -- the third is mine, written at `54d0497e3`). The other fifteen are `app` and its four subcommands, `browse`, four `daemon` subcommands, `explore`, `fc`, `graphql`, `issues edit` and `surface`. **I have NOT written them and will not without an instruction: they are other nodes' work, and a release note is a claim about what a change does.** Routed to vc. (i) the owning nodes write theirs (ii) one node writes all fifteen from the commits. **REC (i)**, and it is the same argument that stopped me doing it.
- **THE CHANGELOG SAYS `- unreleased` AND THE RELEASE HANDLER EXPECTS `- in progress`.** Driven, not read: `int build release --dry-run --patch` stops at `CHANGELOG header is '## [3.0.1] - unreleased' but today is 2026-09-08`, and only `--allow-stale-date` gets past it -- so the cut has a hand-flag in it on the first attempt, and the message misdescribes the cause (it is not a stale DATE, it is a word the parser has no case for). **(i) the CHANGELOG adopts `- in progress` (ii) the handler learns `- unreleased` as a third legal form. REC (ii)** -- `unreleased` is the Keep-a-Changelog word this file's own header cites, and it is the CHANGELOG that has the outside convention behind it. **Small either way; not done because it changes a convention.**
- **CLOSE `0271`?** DELIVERED at `10b4232e`: both refusals built, provoked in `error_remedies`, driven end to end on a real v2 estate. **REC: close as fixed.** Note the issue's author argued disposition **(c)** -- implement no-git, STRIKE dirty -- and what shipped is **(a)**, both, scoped to the conversion path so the convergent re-run is untouched. **If you meant (c), the dirty arm comes out in one edit.**
- **`0271`'s FIXTURE QUESTION IS SPENT** -- ruled and executed with the patch; `v2_estate_in_git` + `git_commit_all` is the shape that landed, surgical rather than shared.
- **THE v2 DELETION** -- `AC-00.6`'s row says _at the cut_, so it is not owed before the tag. The coupling that blocked it is gone.
- **Close `0205`?** DELIVERED AND RE-DRIVEN. All three blocks hold the safe `$(cat <<'EOF'` form; the repo-wide sweep, both halves, finds **zero** unsafe blocks in `*.sh` and exactly one outside it -- `bin/.devbin/lib/builtins:66`, vendored, **ACCEPTED as out of scope**. **The zero is controlled three ways**, the good one being that my scratch v2 tree at a pinned revision still carries all three in their PRE-FIX form and the sweep found all three there. (i) close as fixed (ii) leave open on the vendored block. **REC (i)** -- the vendored block is somebody else's repository and holding our issue open for it never closes. **AND THE ISSUE'S OWN EVIDENCE HAD EXPIRED SILENTLY:** it quoted a manifest/disk hash pair as proof the vendored file was pristine, and devbin has been re-vendored since, so both values moved together -- anyone re-verifying against the recorded value would read a tamper. Corrected in the body, with the verb that re-takes it.
- **Close `0175`?** Fixed at `99a66928f`, driven, 76 tests green, committed still OPEN. **REC: close as fixed.** One word.
- **`INTENT_BIN` flip and re-baseline -- which order?** (i) flip then re-baseline (ii) re-baseline then flip. **REC (i).**
- **`burn.sh` re-run, or accept `AC-06.1`'s coverage half red?** (i) run (ii) accept red (iii) descope. **REC (i), and it is yours because full-suite runs are.**

## Open, no owner

- **`--all`: THE SEAM SURVIVES THE FIX, WHICH IS THE ONLY REASON THIS ROW IS STILL HERE.** The flag works on the delivered pair. **But burn asks _does this reach the v2 CLI_ and `fixture_probe.sh` asks _does this hardcode a v2 PATH_; NEITHER ASKS WHETHER THE ARGV STILL PARSES**, so a future argv regression is invisible to both, and dc's static arm cannot reach `uninstall --all`, which canon never names. Kept for the seam, not the flag.
- **Something WALKS the CLI surface.** Seven CLI-token-titled creations in `event_log`, two episodes eight days apart. **This is `0223`'s -- re-read it, do not restate from here.** Episode 3 checked 2026-09-04: none. **THAT SETTLES NOTHING** -- two episodes is ONE interval and one interval is not a period. **The monitor is one query, so re-run it rather than reasoning about it.**
- **ic's 907-BYTE FOLD DISCREPANCY IS STILL UNEXPLAINED BY ANYTHING ANY OF US HAS MEASURED.** Carried out of `F4` at this fold rather than archived with the investigation, because it is an open question and not a completed one.
- **MY UNFILED DAEMON-LOCK RACE IS PARTLY ANSWERED AND THE QUESTION HAS MOVED.** `290261fc` fixed ONE lock race -- `stop` returning while the daemon still held its advisory lock. **Whether that is the same thing as `0210`, or as the race I never filed, is still NOT compared.** Compare before filing anything new.
- **Does one fix serve both `0216` and `0226`?** Same collision, opposite symptoms. Not driven.
- **The roster guard says _N parity file(s) in this commit_ and means the WORKING TREE.** Wording defect, unfiled.

## Watch-outs

**Rules only. Every worked example is in `.history/20260904/wip-prefold-1741Z.md` and `.history/20260903/wip-prefold-1718Z.md`.**
**A. THE INSTRUMENT ANSWERED A QUESTION ADJACENT TO THE ONE ASKED, AND ANSWERED IT CORRECTLY.** The dominant family; it arrives while I am being careful. **RULE: name the FIELD you read and the INSTRUMENT you read it with.** **REFINED by dc: the old form said flatly that `--help` is about the PLAN, and that is FALSE when help is derived from the parse source** (`spine.rs:624` declares `entry.flags` onto clap from a table compiled in by `include_str!`, so the declaration answering `--help` is the one that rejects the argument). **Not _distrust help_ but _establish what help is derived from_.** A declared row remains a statement about the plan.
**A2. FABRICATION WITH THE CORRECT VALUE ALREADY IN FRONT OF ME. THREE TIMES ON 2026-09-05, ALL MINE.** `19:22Z` written into board prose in the same command whose `date -u` said `20:20Z`; `22:20Z` written while the same tool output read `22:17Z`; and a sha typed wrong with the real one four lines up. **THE THIRD IS THE INSTRUCTIVE ONE BECAUSE IT IS THE SMALLEST** -- three minutes is inside the range no reader would ever question, where an hour gets caught. **THE TELL IS THE ORDER OF OPERATIONS: read the clock, write several hundred words, then type the stamp from memory.** **THE CHECK CANNOT BE ATTENTIONAL, BECAUSE THE VALUE WAS ALREADY ON SCREEN EVERY TIME** -- vc committed the identical failure at 17:29Z. **It is not a fact about clocks; it is a fact about any opaque token a reader cannot check by looking**: shas, pids, issue numbers, line numbers, dates. A date travelling ATTACHED to a sha inherits the sha's authority and none of its checkability. **RULE: substitute the COMMAND, never the value** -- the stamp is a byte-for-byte substring of this turn's tool output, or it does not go in. Mechanical cure in `E`.
**B. CONTROLS, OR THE READING IS NOT EVIDENCE (absorbs B5 and A3).** A control that cannot distinguish _safe_ from _never tried_ is not a control, and it must vary the axis the check reads. **An instrument inside its own population is CONFIRMATORY, not merely noisy.** **RULE 1: positive-control the instrument before its silence means anything.** **RULE 2, AND IT IS THE ONE THAT KEEPS GETTING ME: A CONTROL THAT EXERCISES ONE STAGE CERTIFIES ONLY THAT STAGE.** `froze|frozen` could not match _freeze_ and my control tested the jq path-walk; a duplicate-AT scan globbed 13 files of 156 while **my control passed loudly at 320 matched rows**, having tested whether the PATTERN could match and never whether the FILE SET held the subject. **Before trusting a zero, state the DENOMINATOR out loud and take it a second way.** **RULE 3: WHEN A CONTROL PASSES, LOOK AT WHAT IT PRINTED, NOT AT WHETHER IT PASSED.** Driving `0275`'s absent-library case I got **66 rules back** from a rules-free fake root -- rc was 0 either way and 0 was the expected answer, so only the NUMBER showed that my absent case was the present case wearing a different env var. **RULE 4 (was A3): when a structured reader returns EMPTY, positive-control it on a synthesised input** -- empty is a claim about the READER as often as about the corpus. Worked instance: git's trailer parser finds ZERO `Claude-Session` trailers across 58 commits of which 55 carry the line, because the mandated `(C)` line makes git reject the whole paragraph. **No node can fix that by being careful; the working read is in this board's header block.**
**B2. A DEFECT THAT IS THE DISAGREEMENT BETWEEN TWO INSTRUMENTS' POPULATIONS IS INVISIBLE TO BOTH, AND NO PREDICATE FIX REACHES IT (dc).** `implemented_check.sh` probes what the table DECLARES; `canon_mandated_verbs_check.sh` probes what canon NAMES; `--all` is in the gap. **Neither instrument is bad and neither population is wrong -- the PAIR has a seam and the defect IS the seam.** **RULE: ask what neither population contains, and remedy with a STATIC comparison of the two populations, never a better predicate in either.** **AND: read an instrument's STATED SCOPE before believing its silence** -- `pertest.md`'s zero rows mean _not pending_ (41 of 113), never _not covered_.
**B3. AN ASSUMED POPULATION -- INFERRING A PEER'S METHOD RATHER THAN READING IT. TWICE TODAY, BOTH DIRECTIONS, BOTH WITH dc.** **RULE: a correction to a peer's FIGURE requires reading their PATTERN, and a claim about what an instrument over-matches is settled by a negative control, never by reasoning about the regex.**
**A4. I DIAGNOSED THE COLLISION AT THE WRONG LEVEL, AND THE REMEDY I OFFERED COULD NOT HAVE WORKED.** Two nodes' edits in one file read as a FILE-level collision, so I proposed splitting by path -- and the coupling was the SYMBOL: my `render.rs` hunks called functions that existed only in my other files, so the peer's half could not compile without mine either. **RULE: before proposing a split, ask what the contested hunk REFERENCES, not which file it sits in.** `--only` is path-scoped, which makes path the obvious unit and the wrong one. Caught by vc, not by me, and the tell was available -- I had written both symbols myself an hour earlier.
**A5. A CONTROL THAT CANNOT FAIL FOR THE REASON IT EXISTS -- TWICE IN ONE AFTERNOON, ONE EACH (with vc).** 1210 tests passed across a change to the largest finding class in the fleet, because the sibling test asserts DETAIL TEXT and the sentence is identical either way; vc's first fixture for the `[n/a` parser passed with the fix REVERTED, because the `(non-test)` marker they wrote into the row excludes it before the bug can be reached. **RULE: state what the test would have to SEE to fail, then check the fixture can produce it.** Distinct from `B` RULE 1: the instrument is armed and pointed at a population its own setup has excluded.
**A6. ON A SHARED CHECKOUT `git add` IS THE PUBLISH, AND THE COMMIT VERB DOES NOT PROTECT YOU. TWICE ON 2026-09-07, ONE EACH WAY.** Morning: I staged a new test so the index would be self-consistent for whoever committed -- correct, and it worked. Evening: the same staging made me a passenger in vc's commit, which carried my fold under their message. **I HAD ALREADY USED THE SCOPED FORM -- `git commit --only <paths>` -- AND IT CHANGED NOTHING**, because `--only` scopes MY commit and protects nothing against a peer's; mine then lost the ref-lock race at rc=128 having already handed over the files. **RULE: a staged file is a PUBLISHED file. Stage and commit in one uninterrupted act, or announce that you are holding the index.** The hazard is `git add`, not the commit verb, and every remedy aimed at the commit verb misses it.
**C. ARITHMETIC AND SAMPLE SIZE.** **n=2 is not a result about a stochastic process.** **A total you did not enumerate is not a total you may publish.** **RULE: state n and state the variance; below n=5 on a process you have watched vary, the honest sentence is _observed twice, not characterised_.**
**D. PREMISES.** **Drive a ruling's premise before building on it** -- of four build rulings in one day, THREE had false premises. **Assert the premise your fix rests on as a test, in the direction that would embarrass you.**
**D3. A CLAIM THAT DOES NOT FEEL LIKE A CLAIM IS THE ONE TO DRIVE (absorbs D4 and D5).** **THREE TELLS, ALL MEASURED, AND THE POINT IS THAT NONE OF THEM FEELS LIKE ASSERTING ANYTHING.** **An OBJECTION feels like diligence:** twice in a day I reasoned from D56's principle against a §10a detail I had not read, and the second time the refuting text was a doc comment I had written myself (`intentd/src/web.rs:28-37`). **A GENEROSITY feels like fairness (ic, against themselves):** ic argued my duplicate filing was not my fault because the search verb was broken -- asserted without driving it, in the same message where they corrected me on something they HAD driven. Six subject-shaped queries all find the row. Their diagnosis: _my evidence bar dropped because the claim was in your favour._ **A RESTRAINT feels like rigour:** _I declined to measure because of side effects_ is only honest if I checked whether they were containable -- unchecked, it produces an unmeasured claim in the costume of care, and I then reasoned forward from the gap using v2 evidence about a v3 binary. **RULE: before an objection, an exculpation or a refusal to measure leaves this session, name its premise as a sentence and drive THAT.** For a refusal, name the mechanism by which the side effect reaches the operator and ask whether it can be contained -- a scratch `HOME`, a tempdir root and a detached worktree are the three that work here. **AND IT CUTS BOTH WAYS:** ic's wrong generous argument made me test a sentence in my own filed issue, which was also an overclaim, and I retracted it.
**E. THIS BOX AND THIS SHELL.** **A probe whose EXIT CODE is the finding never goes through a pipe** -- `cmd | head` reports head's status, and zsh has `pipestatus`, not `PIPESTATUS`; reaching for either is already the wrong shape. Redirect to a file and read `$?`. Unquoted `$var` does not word-split; an unmatched glob aborts the whole command. **A heredoc carrying prose is ALWAYS `<< 'EOF'`** -- board prose is dense with backticks and an unquoted heredoc command-substitutes every one. **A clock value goes in through a placeholder plus `sed`, or as a `sys.argv` value read in the same call.** **A STAMP IN A FILENAME IS A STAMP, GUARDED BY NOTHING (`0261`):** `whiteboard-clock-guard.sh` excludes `.history/` by path AND reads only added CONTENT lines, so an archive's own NAME is checked by neither arm. `cargo check --workspace --all-targets` -- the flag is the half memory drops. **`stat -f %m` does NOT follow a symlink on macOS; `stat -Lf %m` does.** **`find -newermt` PARSES LOCAL TIME, so a window typed in UTC silently shifts by the offset and returns a plausible EMPTY answer at exit 0** (vc, 2026-09-05, who fed it UTC stamps and got nothing for a file squarely inside the window). Same family as appending `Z` to a local reading, in the other direction: the zone mismatch answers rather than erroring. **A LITERAL IS NEVER A SPLIT POINT; ONLY CHARACTERS PRODUCED BY AN EXPANSION ARE** -- driven: `IFS=/`, `a="x/y"`, `b="p/q"`, `set -- $a/$b` gives THREE fields `[x] [y/p] [q]`; the `/` inside each expansion split, the literal between them did not, and fused `y` to `p`. **The full ladder, the space-hazard variant, and the lesson that matters more than the shell detail are in `.history/20260905/wip-prefold-2028Z.md`:** I told dc their rule was not the one that bit them, on a test run under default `IFS` where **both hypotheses predict one field, so my test had ZERO discriminating power and I never checked that it had any.** **RUNNING A TEST IS NOT TESTING; the test must be able to come out the other way.** **THIRD BREACH OF THIS FAMILY IN ONE SESSION, ALL THREE MINE:** `--include=*.rs` unquoted aborted a grep at exit 0; `<<PY` unquoted command-substituted my own board prose; and `for sym in $SYMS` over a 38-line roster ran ONCE with the whole blob as `$sym`, printing 17 fake edges that I nearly read as a finding about the tree. **The rule was on this board before all three.** Recorded in `L2` as well, because having it changed nothing. **A SIZE COMPARED AGAINST `git`, `wc` OR A HOOK IS MEASURED IN BYTES:** python `len()` on decoded text counts CHARACTERS, and two `§` (U+00A7) put my figure 2 behind, twice -- **one step from reporting a SECOND WRITER on a single-writer file with the disproof inside my own instrument.** Use `len(open(p,'rb').read())`.
**F. THE SHARED CHECKOUT.** Canon cannot be split, so every canon commit is silently multi-node. **`add` + `commit --only` is the only safe write; a live `index.lock` is a WAIT, never a removal, and the retry is the SAME command re-issued, never recomposed.** A release build DELETES the shared pair before building (`0196`) while the dirty-tree guard REDIRECTS -- two correct rules that can take the machine down together.
**F2. FILE OWNERSHIP DOES NOT IMPLY DIFF AUTHORSHIP, AND `git status` CARRIES NO AUTHOR AT ALL.** `--only` protects a FILE and not a HUNK (W39); this is one step further -- **the natural way to decide whose a dirty hunk is, _who owns this file_, is not evidence.** ic attributed dc's A1 to me on exactly that inference and nearly had me commit it. **RULE: read the diff for its SUBJECT before accepting or asserting that a hunk is anyone's.** Four independent lines beat one ownership inference.
**F3. CURRENCY IS A PROPERTY, NEVER A VALUE -- AND SO IS EVERY COUNT, EVERY SET AND EVERY RECORDED LIMITATION (absorbs D2 and F5).** A pin trailing HEAD is the correct steady state. **RULE: hand the PROPERTY and the verb that reproduces it, never the number.** **dc's LIMB: a value you are PASSING ON is one you have not measured** -- relaying launders a claim, and a figure returning to its author via a third party reads as independent confirmation while being your own error in a different name. **D2, MERGED HERE BECAUSE THE RELAY IS WHERE CURRENCY ROTS: a measurement is re-taken at the moment it is QUOTED, and the tell is a possessive tense** -- _the daemon IS stale_ is a claim about now sourced from a reading about then, with nothing marking the gap. **THE SET FORM DEFEATS MY OWN REMEDY:** I reported six uncommitted canon rows by taking four read at pickup and adding my two; it was SEVEN, one had left and one had arrived, **and _strike the value, park the command_ is useless because a membership claim contains no value to strike.** Re-drive a set exactly like a count. **THE LIMITATION FORM IS THE WORST, BECAUSE NOTHING ABOUT IT LOOKS LIKE A FIGURE: `intent issues edit` SHIPS**, and `0179`'s _an issue body is write-once_ is EXPIRED while `AT-10.5`'s canon note still cites it to justify an escalation on hv's desk. **vc's TRIAGE RULE, WHICH IS THE ACTIONABLE HALF: HUNT THE ROTTED RECORDS CITED IN ARGUMENTS BEFORE THOSE CITED IN ESTIMATES.** `AC-00.6`'s four-call-sites rotted a SIZING and cost six days of everyone believing the last pre-tag job was four times its size; `0179` rotted a PREMISE and was steering a live ruling. **The question to ask of a stale figure is not how wrong it is but what is currently resting on it.** **DISCRIMINATOR for a stale binary: a STORE read survives it; a claim about BINARY BEHAVIOUR does not.** **And `event_log` DATES precisely while attributing nothing -- `principal` is `local` for every row.**
**G. `0216` AND WRITING CANON.** **The read-verify-retry loop is a REQUIREMENT, not good practice** -- it replaces _one verb at a time_, because **the debouncer sees WRITES, NOT AUTHORS, so spacing protects only against your own burst.** Verify on an OBSERVABLE, never on a duration, never on the tool's `ok`. **After a revert the DISK WINS, so store and disk agreeing AT THE OLD VALUE is the signature, not the exclusion** (vc). `intent st attach` writes store AND canon and NEVER the disk file (`0082`). **AND G2 IS MERGED HERE, BECAUSE THE ASYMMETRY AND THE VERIFICATION ARE TWO ACTS AND ONLY THE PAIR IS A CLAIM (vc, refining me).** I wrote canon through the stale pair where vc held theirs, on a real distinction -- their write made canon state the truth LATER and nothing was lost by waiting, mine REMOVED a falsehood so delay was not neutral. **vc confirmed the asymmetry and drew the line I had not: the asymmetry alone would license writing and walking away at the `ok:` line, which is exactly what `0216` makes worthless.** Recorded together because the next node to cite the asymmetry will cite it WITHOUT the verification unless the pairing is written down.
**I. A PEER CHANNEL HAS NO TERMINATING CONDITION, AND I AM THE WORKED INSTANCE (ic's W86; vc: _a sign-off is not a terminating condition_).** hv asked me to fold and compact; **I answered peers for hours instead**, and every one of my messages ended with an acknowledgement that invited another. **A reply carrying only agreement is a turn the OTHER node must take**, so courtesy compounds into an unbounded loop that each end reads as the other's initiative. **ic modelled the fix by NOT sending a reply and putting it on their board instead.** **RULE: when a reply adds no new measurement and no decision, it goes on the board, not into the channel** -- the board is the surface that survives, which this morning proved in both directions.
**H. FOLDING THIS BOARD (absorbs H2).** **THE NEXT FOLD APPLIES ic's BOUNDARY TEST TO EVERY FAMILY HERE, AND IT IS A KEEP/CUT CRITERION RATHER THAN A TIDYING ONE (ic, 2026-09-05, from my own discriminator turned back on me): _a class you cannot draw a boundary around is not rigour, it is a filing cabinet._** For each family, state what would NOT be an instance. A family with no answer catches everything and therefore triages nothing, and it goes -- **merged into the one it is really a case of, or cut.** **SPOT-CHECKED THE THREE I ADDED TONIGHT BEFORE ACCEPTING THE TEST, because a rule I cannot pass myself is not one to hand a peer:** `Q` excludes a figure re-measured in the same turn or carried with its subject; `R` excludes an anomaly whose explanation was CHECKED before it was written; `S` excludes the case where either decision is wrong on its own, which is an ordinary bug and is the discriminator the family is named for. All three draw. **AND THE ORDERING HAS AN INDEPENDENT DERIVATION, WHICH IS WHY IT SHOULD SURVIVE MY OWN IMPATIENCE AT THE NEXT FOLD:** hv's _a fold never archives an UNEXECUTED ruling_ reaches the same place from the other side -- theirs protects live content from a date-keyed cut, mine protects the board from a byte-keyed one, and neither was derived from the other. **A rule two people reached independently is harder to talk yourself out of at 2am than one you wrote once.** **This is the fold's FIRST pass, before any archiving by date** -- cutting an undrawable family is worth more than compressing a drawable one, and the byte count is not the thing being optimised. **Write the pre-fold copy BEFORE folding and `cmp` it** -- nothing compares a folded board to what it was folded from, so a reconstruction from memory would pass every check we have. **Anchor on exact strings, never a prefix**: `index("## Decisions")` matches `"## Decisions owed by hv"`, inverts the slice and duplicates the tail; vc hit the same bug an hour after I described it to them. **`hygiene: ok` PASSES ON A CORRUPTED BODY** -- its contract is the header block, deliberately -- so verify section and family counts by hand. **AND A FOLD ARCHIVES A COMPLETED MEASUREMENT WITHOUT ASKING WHO WAS STILL WAITING FOR IT.** vc asked me to size `WP-06`'s nine unsized rows; I grepped the live board and the archive and **told them it was not mine.** It was mine twice: the ask is in an entry **I SENT**, and the answer was already driven in an earlier pre-fold. **Neither end checks delivery -- an inbox records what ARRIVED and never what happened next; a fold records what is DONE and never who was waiting.** **RULE: before a fold archives an answer, confirm the ASKER has it -- and when challenged on a claim, GREP WHAT YOU SENT, not only what you kept.**
**B4. A CENSUS KEYED ON AN EXIT CODE MERGES MEANINGS THAT THE MESSAGE KEEPS APART.** Re-driven today: **two pairs share a code and mean opposite things.** `doctor` rc=1 RAN and reported a real finding; `st` rc=1 is a usage error. `config` rc=2 is truthfully unbuilt; `agents` rc=2 is false. **This is why my three-option `agents` menu found no convention to rule toward -- the instrument had already collapsed the distinction the ruling needed**, so the menu was built on a partition that does not exist. Sibling of B2 one level in: **B2 is a seam BETWEEN two populations; this is a seam INSIDE one instrument's key.** **RULE: capture the message beside the key, then ask whether any bin's members disagree with each other.**
**F4. A PUBLISHED FIGURE IS NEVER COMPARED TO THE ARTEFACT IT DESCRIBES, AND NOTHING IN OUR TOOLING DOES IT (absorbs F4a).** Three of four nodes published a fold figure differing from the committed board; **the gap is invisible by construction.** **THAT SENTENCE WAS ALWAYS THE FINDING; THE MECHANISM NEVER WAS -- I got the mechanism wrong twice before dc got it right.** **AND _take the count off the commit_ IS NOT ENOUGH (dc):** it fixes the SIZE claim and leaves the CONTENT claim unverified, because `*Safety checkin*` -> `_Safety checkin_` is **length-preserving** and `.prettierignore:9-11` names that exact substitution as what cost the estate 266 views. **A byte figure can match the commit EXACTLY while the file still changed.** **A REFUSING GATE IS A FREE CONTROL** (vc): a refusal freezes the artefact, so measure, `prettier --write` with no edit of your own between the two reads, measure again. Full investigation in `.history/20260906/wip-prefold-0025Z.md`.

**J. THE OUTAGE I REPORTED WAS THE SMALLER HALF (vc, 2026-09-05).** I reported `0196` as the FAILED-build window. There are two, and the second is that a SUCCESSFUL build leaves the delivered pair absent **for the whole build** -- measured from another repository by lamplight-cc: six verbs in one script, verb 1 wrote, verbs 2-6 died `command not found`, `set -e` did not catch it, and it left a half-written git-tracked acceptance contract in Lamplight's canon. **That is a data-integrity finding across every project on this machine, and my report covered the half that announces itself.** Watch-out: **a failure mode that reports itself is the one you will find; the one that completes successfully and removes something is the one you will not.**
**K. A REMEDY STRING CARRIES AUTHORITY IT WAS NEVER DESIGNED FOR (vc's class, from my `0263` and dc's `0262`).** Two readings, both wrong, both natural. **As EVIDENCE:** the only `prettier --write` in the hook chain is the gate's own remedy, so a sceptic grepping to DISPROVE the claim finds it and reads confirmation. **As SPECIFICATION:** `intent st attach <thread> <path> --from <file>` names no frame of reference, so the printed remedy taught a repo-relative invocation that mints a duplicate attachment at rc=0 which no verb removes and `doctor` never reports. **RULE: a remedy is written to help one human recover from one error, and is then grepped as evidence, read as a spec and pasted as a command under none of those constraints. NOBODY AUDITS A REMEDY, BECAUSE A REMEDY IS WHAT YOU REACH FOR WHEN YOU ARE ALREADY LOOKING FOR SOMETHING ELSE.** **AND A CHECK THAT VERIFIES A DEFECT IS _STATED_ CANNOT SEE THAT ITS REMEDY IS FALSE (dc's `0277`, from my own control failure).** `install.md` closed both keg paragraphs with _a source install with `INTENT_HOME` set to the repository is unaffected_. The conclusion is true and the mechanism is not read by v3 at all -- driven: `env -u INTENT_HOME` gives `total: 0`, `INTENT_HOME=<repo>` gives `total: 0`, and the rig that actually carries the trees gives 64. **So a reader with a broken keg follows the page's own instruction, changes nothing, and has been told they are fixed.** `AC-02.3` finds the defect's quote in the page's bytes and passes; **a false remedy satisfies it perfectly**, and dc's instrument certified that page an hour before this. **A remedy is the only sentence on a defects page a reader ACTS on, and it is the one nothing verifies.**
**F6. `--only` IS AN INSTRUCTION, NEVER A FILTER: IT SUBTRACTS NOTHING YOU DID NOT THINK TO NAME, AND IT REPORTS SUCCESS EITHER WAY.** **Reading it as a safety filter produces all three faces; reading it as _commit exactly this list_ produces none.** **Too MUCH landing:** `git add <path>` captures whatever that path holds at that instant, **including a peer's in-flight bytes** -- `e1a076d5` swept dc's live `0273` edits to `fn at()` into my commit, because `--only` is path-scoped and never AUTHOR-scoped. **Too LITTLE landing (ic):** a rename is a DELETION and an addition, so `--only` handed only the new path left the deletion staged and **HEAD carried BOTH files**, with the working tree looking perfect and `autotests = false` meaning an orphan cannot compile its way to a red. **And path-scoped, not hunk-scoped** (W39). **A STAGED PATH ALSO RIDES ANY BARE `git commit`, WHOEVER RUNS IT** -- dc's `ST0056.json` sat in the INDEX, not just the worktree, and **`git status` counts do not distinguish `M ` from ` M` while the difference is who publishes it under whose name.** **The author-safe forms are `git add -p`, or a `git diff` of my own hunks applied to an index I control**, and on a file a peer may hold, ask first.
**L. A PRECONDITION THAT IS CORRECT CAN STILL BE A MIGRATION, AND THE ONLY WAY TO TELL IS TO COUNT.** vc's ordering on `0271`, and it decided the shape of a ruling: **measure the blast radius BEFORE proposing the fix.** The git arm refuses 0 of 18 fleet members and is additive; the dirty arm refuses 1 of 1 scoped to the conversion and **11 of 18 hung on the verb**, because `intent upgrade` is the convergent orchestrator and gets run routinely. **Same check, same correctness, opposite dispositions -- and nothing about the check's own logic tells you which you are holding.** Then my own build reddened 16 tests because the fixtures are not git repos: **the blast radius of a refusal includes the estate's own fixtures, and I measured the fleet and not the suite.**
**L2. THE THING THAT WOULD HAVE CAUGHT IT WAS PRESENT AND INERT.** **A SUBJECT THAT CANNOT EXHIBIT THE CONDITION RETURNS A CLEAN PASS.** It is `B`'s rule moved from the INSTRUMENT to the SUBJECT: a control must be able to fail, and so must the thing it is pointed at. **RULE: when a new guard reddens existing tests, ask whether those tests could ever have gone red before -- a green from a subject that cannot reach the guarded state was never evidence.** **AND: a documented trap is not a prevented one; only quoting it is. Ask of any safeguard what it would take for it to FIRE; if the answer is _nothing that happens in practice_, it is decoration.** **SEVEN SIGHTINGS IN ONE DAY, EACH A DIFFERENT MECHANISM -- the ledger is the finding, details in the pre-fold:** (1) dc's fixture CREATED the files its rows cite, so absence had to be arranged; (2) my duplicate-AT scan read 13 files of 156, the offenders in `COMPLETED/`; (3) 16 migration fixtures are tempdirs with no git repo, so they cannot exhibit the precondition the migrator enforces; (4) **my own board** -- `E` states the `<<'EOF'` rule in the exact words, and I wrote `<<PY` anyway, so a written rule is a guard with a test's failure mode; (5) `tests/cutover_guard.bash` asks the right question with a two-edge method and a control arm over a population that excludes `bin/.devbin/`, where the only real coupling lived; (6) **my own control** -- `INTENT_HOME` at a rules-free root returned 66 rules, so the absent case was the present case wearing a different env var; (7) **ic's, and the only one with no author to fix it** -- under `autotests = false` an orphan test file cannot compile its way to a red, so the blindness is a property of the compilation model. **vc's sharpest form, from `cutover_guard:66`'s own `die "the population is empty"`: IT GUARDS AGAINST MEASURING NOTHING AND NOT AGAINST MEASURING THE WRONG THING** -- a careful instrument whose care points at the wrong axis, and a non-empty WRONG population sails through and renders as coverage.

**P. A CLAIM'S TRUTH AND A CLAIM'S REACH ARE SEPARATE PROPERTIES, AND A CORRECT SENTENCE BORROWED FOR A DIFFERENT QUESTION CARRIES NEITHER (vc's half plus mine, and it is one rule).** vc's: **a ruling that is RIGHT is not re-examined, so its price rides along unchecked** -- twice in an hour they ruled correctly with a wrong cost attached. Mine: **I handed them the screenshot precedent as a COST argument when it was a CORRECTNESS argument.** It proves the residue emitter can name paths with class and reason; it never proved the whiteboard was REACHABLE -- and `legacy::scan` mentions it once, in a comment, so the 1,386 artefacts were never in the scanned population. **They priced off a sentence of mine that was not about price, and I never said which question it answered.** **RULE: when handing a peer a fact, name the question it answers.** A sentence that is true is not thereby true of the thing they are about to spend on.

**Q. A FIGURE SURVIVES A CONTEXT RESET AND ITS SUBJECT DOES NOT.** I carried `1,386 whiteboard files` across a compaction and shipped it in two source comments as _1,386 on baize_. **The count is real and the estate is wrong**: it is Lamplight `fe5dff3e6`, and baize's whiteboard is 110 -- a number sitting in the same paragraph of `AT-10.5` I had read. Nothing flagged it, because a number with a plausible estate name beside it reads exactly like a measured one. **It was caught only by re-driving the archive**, which produced 1,386 against Lamplight and would have produced 110 against baize. **RULE: a figure recalled rather than re-measured carries no subject, so it is not evidence for any estate -- re-drive it or drop the attribution.** This is the handover rule (`hand over PROPERTIES, not values`) arriving inside a single session rather than between two, which is the case I did not expect it in.

**R. AN ANOMALY YOU NOTICE AND EXPLAIN IS WORSE THAN ONE YOU MISS, BECAUSE THE EXPLANATION CLOSES THE QUESTION.** `git show` said my commit changed **76 lines of `render.rs` when my edit was 35**. I saw the gap, wrote _because rustfmt reformatted other parts of the file too_, and moved on. **rustfmt had not touched them: they were dc's in-flight `0273` edits, and my `git add` swept them into my commit.** The cause was invented, plausible, and never checked -- one look at the hunk headers would have named `fn at()`, a function I have never opened. **This is the migrator's own rule fired back at me:** `render.rs` says, in the very function I was editing, that an invented cause _is indistinguishable by inspection from a recorded one, and worse than silence because it sends the reader looking for something that never happened._ I was the reader. **AND THE FILE I MISSED WAS THE ONE I OWNED:** I checked `mcp.rs` and `contract.rs` for peer collateral and reasoned correctly about both from crate boundaries, then skipped the check on the only file I was myself writing. **RULE: a number that does not match your edit is a measurement, not a puzzle -- read the hunk headers before you write a reason.**

**S. TWO CORRECT LOCAL DECISIONS COMPOSE INTO A FALSE GLOBAL ANSWER, AND NO REVIEWER OF EITHER ONE CAN SEE IT (vc's account of my `0275` finding).** `Library::files` returns an empty list for a root that is not a directory, and that is RIGHT where it sits -- _no rules installed_ is an ordinary state, and erroring there would refuse every install that simply has none. `rules_list` prints `total: N rule(s)`, and that is RIGHT too. **Composed, they tell an operator with NO RULE LIBRARY AT ALL that they have zero rules, at exit 0, under a well-formed table header** -- and `install.md` nominates that verb as the install check precisely because it is supposed to fail on that fault. **Nothing in either function is wrong, so reading either function finds nothing**, which is what separates this from every other family here: the others are a wrong instrument, a wrong population or a wrong premise, and all of those are visible to someone reading the thing. **This one is only visible from the composition, which is the failure mode code review is structurally worst at.** **RULE: when a function answers _there are none of these_, ask what it returns when the PLACE they live is absent -- and if the answer is the same, the caller is the one that owes the distinction.** The fix is never to make the inner function wrong; it is to expose what the caller needs -- here `Library::canon_root()`, keyed on the SHIPPED tree rather than on a count, because an ext pack can supply rules while that tree is missing.

**M. I REACHED FOR THE SIDE I COULD EDIT AND MISTOOK IT FOR THE SIDE THAT WAS WRONG.** Two of WP-10's objective clauses had no row, and I proposed making the OBJECTIVE agree with the rows. vc held it and drove my question instead of answering it: the rows had not moved, they had VANISHED -- ST0069 stated the requirement nowhere and `ST0069.wps` is `null`, so the premise both descopes rest on names a destination that does not exist. **My principle survived and my application of it was backwards: it says the two sides must AGREE and says nothing about which one moves.** The objective was the one edit I could make without a ruling, which is a fact about my REACH. **When the rows left by mistake, making the objective agree with them is how the mistake becomes permanent.**
**O. I REPORTED THE ANSWER THAT COST ME MORE, AND THAT IS THE ONLY REASON IT IS EVIDENCE.** I preferred the one-line count-and-reason form for `0272` and reported the 1,386-line form, because `AC-10.5` permits the cheap one and **`AC-00.2` says _by name_ and both rows have to hold.** vc had told me not to tune the drive toward the cheaper outcome. **A drive whose result matches what the driver wanted is not worthless, but it carries no information about the tuning -- so the caveat has to be written down at the moment it costs something, not recalled afterwards.**

**A7. THE SHARED-FILE TWIN OF `A6`, AND `cmp` CANNOT SEE THE HALF THAT MATTERS.** I mutated `render.rs` seven times to measure a mutation table, on a checkout where vc had UNCOMMITTED `--scope` work in that same file. I announced the hold first, snapshotted with `cp`, restored with `cp`, verified with `cmp` -- all correct. **But `cmp` against my own snapshot only proves MY mutation is gone.** The real hazard runs the other way: my snapshot froze vc's work at one instant, so a write from vc INSIDE my window would have been silently rolled back to my copy, and `cmp` would have read IDENTICAL while their edit was destroyed. **The announcement is the only thing protecting against that; the revert method protects against nothing of the sort.** vc conflated the two in their own warning and corrected it. **RULE: `cmp` proves your mutation is undone. Only the announcement protects a peer's concurrent write. Two failures, and the second is silent.**
**A8. `git checkout -- <file>` IS NOT A REVERT ON A SHARED CHECKOUT, AND `git diff` IS NOT A VERIFICATION.** vc's, and both halves keep. The checkout would have discarded a peer's uncommitted work in the same file along with my mutation. And the check I had planned -- `git diff` for byte-identity -- **answers a different question than the one asked**: with two nodes uncommitted in one file a non-empty diff is the EXPECTED state, so the read tells you nothing while looking like it told you something. **RULE: take your own byte snapshot, restore from it, verify with `cmp` against it. git is not in this loop.**
**A9. A CONTROL IS WORTH THE FIXTURE'S ABILITY TO TELL THE MUTATED QUANTITY FROM ITS NEIGHBOURS (vc's, and sharper than `A5`).** My `organize` fixture had 2 unclaimed directories and 2 action rows, so a withheld line printing the ACTION count instead of the DIRECTORY count read `2 == 2` and stayed green. vc hit the identical shape an hour earlier: their withheld-count arm asserted 2 where findings, threads AND admit-calls all equalled 2, so a counter broken from `+= found.len()` to `+= 1` still reached the expected number. **`A5` says the fixture must be able to EXHIBIT the failure; this says its NUMBERS must not collide.** Now 3 / 5 / 2, pairwise distinct, **and the positive control ASSERTS the distinctness rather than a comment claiming it** -- the mutation that reds only because of the correction is in the table.
**A10. THE TEST HARNESS HAS A SPLIT-PAIR GUARD AND IT PRESENTS AS 26 UNRELATED FAILURES.** `cargo test -p intent-cli` came back 26 FAILED across daemon and web arms; every one was `tests/common/mod.rs:784` refusing to spawn a debug `intentd` OLDER than the tree, naming the newer file. **`cargo test -p <pkg>` builds THAT package's binaries and not another package's**, and the daemon resolves as a sibling of the `intent` just built. `cargo build -p intentd` and it went 503/0. **This is the test-harness twin of the delivered-pair split I found on the release binaries the same morning** -- same class, different layer, and this one prints its own fix.

**A11. A MUTATION THAT REDDENS NOTHING IS EITHER A MISSING TEST OR DEAD CODE, AND THE DISCRIMINATOR IS WHAT INPUT CAN REACH IT.** Driving seven mutations against `st list`'s heading, six reddened an arm by name and the seventh -- breaking `clamp_heading`'s empty-name branch -- reddened nothing. **The reflex is to write a test; the right move was to ask what input reaches the branch, and nothing does**: `Path::file_name` answers `None` for a root with no final component and never `Some("")`, so the case was already handled one level up. **A test there would have pinned an impossible state -- coverage of the unreachable, which reads as diligence and is furniture.** The branch was deleted. vc hit the same class from the other end the same evening: a user-facing sentence changed and 1222 tests stayed green, because the string was unpinned. **Same signal, opposite causes, and only the reachability question tells them apart.**
**A12. THE PUBLISH WINDOW HAS NO SIGNAL, AND ONLY THE COMMITTER KNOWS IT IS OPEN.** Twice on 2026-09-08, opposite shapes: the pair was SPLIT in the morning (`intent` and `intentd` from different trees, caught by the provenance guard) and uniformly STALE in the afternoon, three commits behind -- **caught by hv typing the verb and reporting a working feature as broken.** The window opens at COMMIT and closes at PUBLISH. Inside it the board, the commits and the tests all say a thing works, and the person running the tool sees that it does not. **The self-provenance currency arm reports it only at commit time, to whoever is committing** -- never to the person typing `intent`. **RULE: after landing anything under `native/rust` or `surface`, publish the pair before saying it works, and when someone reports a feature not working, measure the marker BEFORE reading the code.** **REFINED BY ic 2026-09-08, AND THE REFINEMENT IS TO THE PREDICATE, NOT THE RULE: the scope that follows from the mechanism is SHIPPED BYTES, and `native/rust` is a PROXY for it.** I asked ic to republish over one commit of drift; they measured it -- thirteen `//!` lines in a test module -- and declined, correctly, because test files are not in the shipped binary, so the republish would move the marker and change nothing observable. **A stale binary matters because it shows OLD BEHAVIOUR; where there is no behaviour there is nothing to be old.** And the second half is theirs too: **report the CONTENT of the drift, not the count.** A figure going to zero would have told me nothing; "thirteen doc lines in a test file" told me everything, and it generalises -- a currency count is a proxy for a behavioural question, and when the two disagree the behavioural question wins.
**A13. A PATCH THAT WAS MEASURED WHEN IT WAS WRITTEN IS NOT MEASURED NOW.** `0271`'s patch sat three days, `cmp`-verified, with a blast radius I had measured and recorded. **The recorded figures were right and the patch was still wrong**: `tree_state` conflated _no git_ with _no commits yet_, which no amount of re-reading the diff would show, and which surfaced within a minute of running it. **The 16 reds I had recorded were 21, across 7 files, for a reason my note did not name.** RULE: a stored patch's measurements describe the tree it was written against; re-drive before trusting any of them, including your own.

**A14. A COMMENT ASSERTS THE DEFECT AWAY, AND ITS THOROUGHNESS IS WHY NOBODY CHECKS (absorbs A19).** Two instances, one mechanism. `fn app`'s doc read _the two long-running things on this machine now have the same four verbs_ while `app` had `restart` and `daemon` did not; and three lines in the release handler justified a `--skip-settings` the real call never passed and v3 does not implement -- **with the DRY RUN printing it**, which is the damaging half, because a dry run is read precisely when someone wants to know what a command will do. **Neither is a stale comment beside a defect; each asserts the defect away**, and both times a nearby explanation was thorough enough that nobody read the call under it -- ic's `DaemonService.swift:67` had documented the stop-then-start workaround for WEEKS. **BOUNDARY, what is NOT an instance:** a comment that describes what the code does, and a stale comment making no claim the code contradicts. **RULE: read the invocation, never the paragraph above it. A comment claiming two things are the same is a claim about a POPULATION -- enumerate both and diff them, `--help` against `--help` took ten seconds -- and a `log_dry` string is code that must match its sibling branch.**
**A15. A POSTCONDITION THAT IS TRUE OFTEN ENOUGH IS A RACE, AND TWO VERBS ASKING DIFFERENT QUESTIONS ABOUT ONE STATE IS HOW IT HIDES.** `daemon stop` waited for the SOCKET to stop answering and printed `ok: intentd stopped`; `daemon start`'s already-running guard reads the advisory LOCK the daemon holds until it EXITS. Between those is a window where the daemon answers nothing and still holds its lock. **`restart` composed them and left the daemon DOWN at rc=0, twice, with both halves printing `ok:`.** The race is PRE-EXISTING: a shell `&&` clears it on the process round-trip, so hv's hand-composed workaround worked and the menubar's own Restart button had the bug all along. **RULE: when two verbs act on one resource, check they agree on what LIVE means -- and a success line is a claim about a postcondition, so it must not print before the postcondition holds.** Fixed in `stop`, not `restart`, because every composition inherits it.
**A16. THE INSTRUMENTS ENUMERATE WHAT A NEW VERB OWES, SO LET THEM TEACH YOU THE CONTRACT RATHER THAN GUESSING IT.** Adding one subcommand was refused in turn by SIX checks -- the dispatch row, its `recoverability_anomaly`, the `legal_pairs` census, the `populations` triple, the dual-path `HAZARDS` roster and the `UNPROVEN` mutator bucket -- and every refusal was correct and named its remedy. **The right move was to run the generator, read the refusal and satisfy it; the wrong move would have been to design the declaration up front and then argue with a guard.** One of the six was my OWN family pin from that morning, firing on its author eight hours later -- which is the case for writing pins as EQUALITIES rather than lower bounds.
**A17. A CHECK CONFIGURED WITH NOTHING ANSWERS GREEN, AND ITS GREEN IS BYTE-IDENTICAL TO A REAL ONE.** `int version check` printed `version: 3.0.0, every sidecar agrees` at rc=0 over an EMPTY sidecar list -- and after declaring one sidecar it prints **the same string**. **A verdict that does not state its POPULATION cannot be told from a vacuous one by reading it**, which is why this survived in a repo whose every other instrument prints its denominator, its partition and its reach. It is `B`'s denominator rule aimed at a tool's OUTPUT rather than at my own measurement: the same discipline the estate applies to its guards was never applied to a vendored verb everyone trusted. **RULE: before believing any green, make the thing it examines disagree and watch it go red** -- and where you cannot patch the tool (vendored), report the missing population line as the defect it is.
**A18. THE POSITIVE CONTROL CAUGHT A FIX THAT WOULD HAVE BROKEN THE THING IT FIXED.** Replacing `git add` + bare `git commit` with `git commit --only -- $PATHS -m "release: v$T"` is obviously right and is wrong: **after the `--` every argument is a PATHSPEC**, so git looked for files named `-m` and `release: v9.9.9` and failed outright. **Reading it proves nothing -- it reads correctly.** Driving it in a scratch repo turned it red in one second, and the same harness's other arm showed the OLD form really did sweep a peer's staged file into the release commit, so neither the defect nor the fix rested on argument. **RULE: a one-line fix to a git invocation is a fix you have not tested, and the two-arm shape -- drive the OLD form too -- is what makes the change evidence rather than preference.**
**A20. A CONDITION THAT RETIRES ITSELF DOES NOT DELETE ITSELF, AND IT KEEPS READING AS A LIVE GUARD.** `STAMP_NATIVE` gated the Rust workspace stamp on a 3.x target and its own comment predicted the retirement -- _once 2.x stops being cut there is no other arm_. **It became UNREACHABLE the moment VERSION passed 3.0.0**, because `semver_gt "$TARGET" "$CURRENT"` aborts anything not greater, so no 2.x target can reach the line that would exclude it. It then sat for two weeks as a guard a reader would reason around. **The prediction is what made it dangerous: a comment that says _this will retire_ transfers the deletion to nobody.** **RULE: a self-retiring condition needs the CONDITION FOR ITS OWN REMOVAL written as something checkable, or it is a comment that outlives its code -- and when you delete one, say what would bring it back.**
**A21. THE RIGHT ANSWER WAS PRESENT, CAPABLE, AND NOT REACHED (absorbs A23 and N). THE DOMINANT FINDING OF 2026-09-08, AND IT ARRIVED FIVE TIMES.** `payload.rs` carried the rebaselining argument -- _here there is no distinction to discard_ -- in the `old == None` arm, and a merely STALE baseline is the same situation and could not reach it. `render.rs` already refused to name discarded bytes as an edit under `Baseline::Absent`, **one arm downstream of the message that asserted exactly that**. Two test files hardcoded `install::MARKER`'s value with `// install::MARKER -- what makes a tree an install.` **on the line directly above**. `AC-00.2`'s own standard was already working ten lines up the same tool output. **Then it took me: my memory says _never `cp` a shared source aside to mutate it_, it was loaded in this session, and I did it four times under traps that made each one safe by luck of timing rather than by construction** -- vc found the better method (plant a NEW UNTRACKED FILE, which has no window at all) while verifying my work. **A `missing case` framing hides what failed: nobody has to THINK of the reasoning, it is already written down. What fails is the REACH of the arm carrying it** -- a reviewer finds the argument, agrees with it, and has no reason to ask which states get to it. **A comment naming a constant beside its hardcoded value is worse than no comment: it proves the author knew, which is the strongest available evidence that knowing is not the mechanism that prevents this.** **BOUNDARY, what is NOT an instance:** the answer was ABSENT (an ordinary gap), or present but INCAPABLE of firing (that is `L2`). **RULE: when an arm carries a justification in prose, ask which STATES reach it and whether the argument is true of a state that does not; before arguing a tool cannot meet a standard, grep its own output for the standard; and proximity of the right answer -- in the file, on the line above, or in your own loaded memory -- does not cause it to be used.**
**A22. I NARROWED THE CRITERION CORRECTLY AND THEN TOOK THE NARROWED POPULATION AS THE JOB.** On `0279` ic's list of six over-counted, because their criterion selected on a string appearing in a construction AND in an assertion. **Narrowing the OPERATION -- from _mentions the string_ to _builds a fake install_ -- was the right move and fixed their count.** Then I treated the four builders as the work. **They are not: the literal is in EIGHT files, four of which build no fixture at all, and the issue's all-or-none condition binds to the LITERAL rather than to the fixture.** Converging four would have taken eight to four and made the finding read as done -- the exact defect the condition exists to prevent, one level down. vc caught it. **RULE: after narrowing a criterion, re-ask what the SUBJECT is. A narrower operation gives a cleaner population and does not tell you it is the right one** -- and the tell here was available: I had measured 25 mentions, 8 literals and 6 join-sites within the hour and reported only the number my narrowing produced. **Related and NOT the same: `B` is about stating the denominator; this is about the denominator being right and measuring the wrong thing.** **ic's statement of the symmetry is sharper than mine and is theirs:** they over-counted by selecting on a string, I under-counted by narrowing to the operation -- **both of us got the CRITERION right and the SUBJECT wrong. A criterion answers _which files match_; it never answers _which files is this about_, and nothing in the output distinguishes those two.**

## Decisions

**NINE EXECUTED-NARRATIVE ROWS ARCHIVED THIS FOLD; every unexecuted ruling and every durable principle stays.** Execution is the discriminator, never the date -- and `0225`'s re-affirmation left this list only because it is now carried INSIDE `0223`'s body, which is execution rather than age.

- (2026-09-04, cc->dc, adopted) **A CONTROL THAT LOST ITS SUBJECT PASSES FOR THE WRONG REASON, SO CONTROL THE CONTROL.** dc's positive control was _ic's embed goes green_ and ic had removed the embed; it would have passed under the broken guard too. **dc's answer went past the catch: a META-CONTROL that drives the OLD logic against the same planted pair and refuses if the old logic also passes** (`positive-control-has-no-subject(old-logic-also-passes)`), mutation-tested. **The generalisation is theirs: a control asserts a DIFFERENCE, so it must be driven against the state it claims to distinguish FROM.**

- (2026-09-04, cc) **A CITATION INHERITS THE CITED CLAIM'S TRUTH AT CITATION TIME AND NEVER UPDATES.** `0223` rests on `0090`+`0151` to conclude a junk row _can only be closed_; all three are open, the underlying verb shipped, and **none of the three noticed.** Currency is a property of a CLAIM (F3) and equally of every REFERENCE to it -- and a reference is worse, because it reads as sourced.

- (2026-09-04, cc) **A PUBLISHED FIGURE IS A CLAIM ABOUT AN ARTEFACT, AND THE ARTEFACT IS THE COMMITTED ONE.**

- (2026-09-04, hv via cc) **`config`, `ext` AND `learn` SHIP DECLARED-AND-UNBUILT IN 3.0.1** (2026-08-31). **A general ruling stated later does not vacate a specific one.**
- (2026-09-04, vc) **THE ES RENDERER IS `WP-17`'s BY WP-17's OWN TITLE.** `D56` forbids server-rendered HTML and DESIGNS the client-rendered browser face.
- (2026-09-04, ic via §10a) **`widget` IS A SEMANTIC CLASSIFICATION, NOT A VIEW.** `prose` means the field is long; TUI to `$EDITOR`, browser to `<textarea>`, SwiftUI to `TextEditor`. **Renderers legitimately differ in the WIDGET and never in the model.**
- (2026-09-04, ic REFUTING cc) **`SERVED_BY_DAEMON`'s AXIS IS _REACHABLE BY A CLI VERB PATH_ VERSUS _WIRE-ONLY_, NOT DUAL-PATH.** The table is a `const`, so only payload-free variants fit. **`Op::Set` HAS the dual path and is absent** -- which refutes the axis on evidence I already held. **A one-row table cannot distinguish two axes.**
- (2026-09-04, cc+dc) **A HOLD DISCHARGED BY THE CONDITION IT NAMED TEACHES THE OPPOSITE OF ONE THAT ERODES.** File the protection BEFORE discharging the hold.
- (2026-09-04, cc+dc) **A DIRECTORY `add` PROTECTS A PATH AND A DIRECTORY PATH IS NOT A FILE.** And **a path that moves on its own is not a path either node can own** (`project.json`'s `todo_watermark`).
- (2026-09-04, cc) **THE CANON STORE AND DISK DO NOT DISAGREE.** The divergence is git-only. Filing writes store and disk atomically; **git is a separate human-gated act and NOTHING REPORTS THE GAP.**
- (2026-09-04, cc) **A SET PASSING TOGETHER IS NOT EVIDENCE ABOUT ITS SUBSETS.**
- (2026-09-03, ic->cc) **THE CLASS IS THE UNDRIVEN NUMBER, NOT THE WRONG ARTEFACT.**
- (2026-09-03, cc+vc) **CONTENTION IS `0216`'s VARIABLE** -- not spacing, not corpus size. **Refusals (`0226`) and silent losses (`0216`) TRADE OFF**, so a single counter prints _fewer losses under load_ and reads as improvement.
- (2026-09-02, cc+vc) **`0216`'s FIX IS DAEMON-SIDE, NOT USAGE DISCIPLINE.** A hazard reachable by an ordinary shell loop cannot be mitigated by how carefully nodes write.
- (2026-09-02, vc) **A RIDER CANNOT BE VIOLATED BY A CASE ITS OWN HAZARD CANNOT REACH.**
- (2026-09-02, vc) **`WP-14` AND ALL 12 OF ITS ACs WERE DESCOPED WHOLE TO ST0069** by hv on 2026-08-30. `AC-09.5`'s wip/boards half goes with it.
- (2026-09-02) **TWO MACHINE PROJECTIONS OF ONE VALUE MUST NOT DRIFT; A HUMAN RENDERING OF IT IS NOT A COPY AT ALL.**
- (2026-09-02) **CONTENT COMPARISON DOMINATES A VERSION COUNTER FOR A COMPARE-AND-SWAP.**
- (2026-09-02, vc) **AN EXCLUSION MUST BE VISIBLE WHEREVER THE BEHAVIOUR IS CLAIMED, NOT ONLY WHERE THE CHECK LIVES.**
- (2026-09-01, hv) **v3.0.1 IS FEATURE COMPLETE, THERE IS NO TAG WINDOW AND NO EXTERNAL CONSUMER, AND COST IS NOT A CONSTRAINT.** The scarcity register is retired as a class.
- (2026-09-01) **A REFUSAL THAT CANNOT SAY WHAT IT FOUND MAKES ITS OWN DEFECT UNDIAGNOSABLE.**
- (2026-09-01) **A REMEDY INHERITS ITS BRANCH'S ASYMMETRY.** Confirm-before-refuse is SAFE on the lock and WRONG on the probe.
- (2026-08-31, vc) **A CRITERION IS NOT REWORDED TO WHAT THE CURRENT STATE SATISFIES.** The ruled form must be HARDER.
- (2026-08-31, vc) **A CLASS CHANGE WITHOUT ITS REASON IS A DELETION WEARING A NEW LABEL.**
- (2026-08-31, ic correcting me) **AN OWNERSHIP SPLIT IS A PURPOSE, NOT A BOUNDARY.** Two hands in one file IS `0206` in miniature.
- (2026-08-31, CORRECTED; **CONTINGENCY MEASURED 2026-09-05**) **`close --note` STAYS RULED OUT ON A CONTINGENT FOOTING.** The keg has no `edit`; the ruling survives because v3.0.1 ships from the tree. **THE FOOTING IS NOW DRIVEN RATHER THAN ASSUMED, AND A COLD READER SHOULD NOT RE-DERIVE IT: `intent issues edit` SHIPS ON THE TREE'S RELEASE BINARY** -- its own help reads _Correct an issue's record: its prose, its title, its severity_, and `dispatch-table.json` carries it four times. **I have measured the TREE and NOT the published keg**, so the first clause is still somebody else's to check. **This row is left ruled as it stands; what changed is that its condition is now a measurement.**
