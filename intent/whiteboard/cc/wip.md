---
node: cc
name: Control Claude
role: control
session_id: bb27122c-3e86-4c2d-abfe-339a4fa151df
heartbeat_at: 2026-08-26 15:19Z
status: active
focus: "**BOTH cc INGEST FIXES ARE IN: `e696de15` (empty-subject AT rows) and `80cb8509` (the AT-citation parenthetical) -- cc's 3.0.1 batch contribution is COMPLETE.** 114 binaries, 913 passed, 0 failed; clippy 0 warnings; `cargo fmt --check` clean. **The parked `DIVERGES_BY_RULING` amendment is now MUTATION-PROVED rather than merely green.** **CLIPPY EXITS 0 WITH WARNINGS PRESENT -- the remedy is `-- -D warnings` copied verbatim from `prepush:335`, NOT a warning count; dc corrected me with a measurement.** **THE AT `file` FIELD HAS NO DEFINED ROOT: 55 of 204 fleet-wide are not repo-relative, and nothing resolves it yet.** **DO NOT TRANSCRIBE FIGURES FROM HERE -- drive `ac gate ST0057`, `ac status ST0056`, `ac status ST0058`.**"
claims: [ST0056/06, ST0056/10, ST0057/00, ST0057/01, ST0057/03]
---

# Control Claude (cc)

## DOING

**BOTH OF cc's INGEST FIXES ARE IN. `e696de15` (empty-subject AT rows) and `80cb8509` (the AT-citation parenthetical).** cc's contribution to the 3.0.1 batch window is complete. **Suite on the second: 114 binaries, 913 passed, 0 failed** -- which reconciles exactly against fix 1's 907 (906 passed, 1 failed on attachment drift) plus the 6 new citation arms.

**FIX 2, WHAT IT ACTUALLY REPAIRS.** An AT row cites its test as a path plus, often, a human annotation -- `test/cdsync.bats (whole suite, 328 tests)`. The whole string went into `file`, so canon recorded a test file whose name ends in `328 tests)`: **a path no filesystem has, asserted as fact by the store.** `split_citation` cuts at the first of ` (` or a backtick and carries the remainder into the note via `append_note`, so nothing is dropped. **The cut is ` (` and never a bare `(`**, because Utilz has seven rows citing `each_utility()` by name -- a control, not a hypothetical.

**THE PARKED AMENDMENT IS NOW VERIFIED, AND VERIFIED MEANS MUTATED.** `DIVERGES_BY_RULING` had never been compiled, which is why I held it overnight in scratch. **A new assertion passing is not a new assertion working**: declaring a fifth ruling that can never match fails with `5 declared but 4 fired / did not fire`, and unmatching one real marker fails with `1 of 20 authored rows do not render back as themselves`. File restored byte-exact after both. **The staleness arm has teeth -- when the annotation field lands and those four rows round-trip again it goes RED, forcing deliberate retirement rather than an exception that quietly stops applying.**

**THE CORPUS APPENDS FOUR AND MOVES NONE.** Verified the sixteen ratified rows are a byte-identical prefix BEFORE applying, because silent re-punctuation of ratified rows is the defect vc ruled REVERT on for Utilz's hop 1.

**MY THIRD CANON DATAPOINT: `--to-store` PRESERVES ATTACHMENTS.** 94 before, 94 after, corpus record equal to the bytes on disk. With vc's throwaway-Prolix measurement (attachment-less extract -> `--to-store` -> `--to-disk` returns all five) the emitter question is settled: **the EXTRACT is the only disagreeing writer, and the one-emitter fix is an extract-writer fix.**

**CLIPPY CAUGHT A LINT IN MY NEW CODE AND EXITED 0 ANYWAY.** `items_after_test_module` -- I had appended both functions after the `#[cfg(test)] mod tests` block. Fixed by moving them above it and **verified as a pure reorder: sorted-content sha256 identical before and after, line count unchanged.** My proposed remedy -- gate on the warning count -- **was WRONG and dc corrected it with a measurement.** See Watch-outs.

**AND I RAN `cargo fmt --check` ONLY BECAUSE dc TOLD ME MY PATHS WERE IN SCOPE FOR IT.** `FMT EXIT=0`, clean. But I had committed without checking a push gate my change is definitely subject to.

**A PROCESS ERROR OF MINE, RECORDED BECAUSE I HAD JUST LECTURED ic ABOUT IT.** I launched the clippy+suite re-run while my first clippy job was still running, then edited `legacy.rs` underneath it. Two cargo processes on the shared target, started by the node holding the lock. **Job 1 finished clean and job 2 began after the edit, so the surviving result is uncontaminated -- established before reporting, not assumed.**

## TODO

1. **LAMPLIGHT FIRST, on the empty-subject fix alone.** `ingest` exit=0, 0 blocking, 180 carried -- driven on a scratch copy, not predicted. Live fleet, dirty `mix.lock` is a peer's: **commit `--only`, `git show --stat HEAD` against my own pathspec.** Block digest FULL-LENGTH by command, before and after.
2. **CONFLAB IS HELD BY RULING and hv is told it is the one of twenty-one held today.** 69 blocking: 61 `status:` rows needing the verbatim-field schema change, 8 `unknown-status` (`pending`/`Pending` -> `Not Started`, `DESCOPED` -> `Cancelled` with the word kept inline), 5 AT rows with **no `covers` clause at all** -- those five are Conflab's content, demoted from list rows to prose on vc's ruling, all five quoted with line numbers in the commit body.
3. **THE `status:` VERBATIM FIELD IS A SCHEMA MIGRATION, and I am to build nothing toward it until ruled.** `SCHEMA_VERSION` 13->14, DDL + table rebuild, INSERT and SELECT column lists, three regenerated faces (`ddl.sql`, `thread.schema.json`, `schema.graphql`), 22 construction sites. ic reviews the design.
4. **NAMED, NOT FIXED:** `covers()` at `legacy.rs:1376` cuts its id list with the same unbounded `find(" -- ")` that `field_end` replaced. **Nothing in the estate hits it today -- I checked** -- so it rides the batch list only if someone is already in that function.
5. **STILL PARKED FROM WP-06:** `claude prime`, `subagents`, `ws`, `start` unwired; `fileindex` retirement in ic's `dispatch-table.json`; `sync` untracked-bytes held by hv. **None fleet-blocking, none cleared.**

6. **hv's TODO (2026-08-26): a start/stop switch for the menubar app in the toolbar, between the `⌘K` button and the separator.** **NOT ACTIONABLE YET, AND I CHECKED RATHER THAN FILING IT BLIND.** The macOS menubar app is a **parked 3.x stretch goal** -- `ST0056/info.md:21` lists it beside the TUI dashboard, the agent bus, the Laksa web page and `intent_ex` as its own future steel thread -- and there is **no `cmd/macos` in this checkout and no `⌘K` toolbar anywhere in it**. `AC-11.7` was withdrawn on hv's own ruling that `cmd/macos` is OUT OF SCOPE for the cut. So the surface in the screenshot belongs to something outside this repo. **BLOCKED ON ONE ANSWER: which project owns that toolbar.** **A STRONG LEAD, FOUND AT 15:19Z AND NOT YET CONFIRMED: `ST0064` (`intentd: the Intent daemon, and a menubar app that controls it`, status `Triage`, vc's, landed in `0f79f42b`) carries `design-menubar-app.md` -- a PURE RENAME of `vc/handoff-geodica-menubar-app.md`, 0 changed lines, so it is Geodica's design verbatim.** Its line 54: _"we put a small on/off switch in the WEB app's header that starts and stops the menubar app (state from `/_status`, click POSTs a whitelisted action)"_ -- **past tense, describing a switch Geodica ALREADY HAS.** **SO THE TOOLBAR IN hv's SCREENSHOT IS PROBABLY GEODICA'S WEB HEADER, NOT ANYTHING IN THIS REPO -- WHICH WOULD MAKE THIS SOMEBODY ELSE'S TOOLBAR AND NOT cc's WORK AT ALL.** **AND THE ONE PIECE THAT LOOKS LIKE A MATCH IS NOT ONE:** `design-menubar-app.md:19` has `Cmd-K`, but it is **Clear in a log-console FOOTER bar**, not a `⌘K` button in a toolbar. **Same two characters, different surface -- do not let it confirm the identification.** Still blocked on hv naming the app; the question is now much narrower.

7. **vc FILED TWO 3.0.1 CANDIDATES AT 2026-08-26 14:26Z, EXPLICITLY AFTER FIX 2 AND NOT IN THE OPEN BATCH.** Both already reverted in Prolix by vc; both in the runbook with their measurements. **Recorded here, not started.**
   - **(a) TWO EMITTERS OF `.canon` DISAGREE ON `attachments` -- one artifact with two authors, which is Highlander at the emitter rather than at the module.** The migration's extract and `upgrade`'s re-emit record NONE; `sync --to-disk` records every attachment with full text, bytes and sha256. vc measured Cdsync 0/4, Riffle 0/5, Courses 0/2, Prolix none at its migration commit, Intent 58/63, and Baize 4/25 + Laksa 1/110 -- **exactly the threads someone has synced since**, which is what makes it an emitter split rather than random. **So a fleet project's FIRST `--to-disk` rewrites every thread's canon**: Prolix, 27 threads and ~153 attachments, out of a one-row repair. **The store held them all along, so the EXTRACT is the half that disagrees and ONE emitter is the fix.**
   - **I MEASURED THE THIRD EMITTER, WHICH vc's LIST DOES NOT COVER, BECAUSE I RAN ONE TODAY.** `e696de15` wrote `intent/.canon/st/ST0056.json` through `sync --to-store`: `.attachments` is **94 at the parent, 94 in the commit, 94 in the worktree**, and the corpus record's sha256 equals the bytes on disk. **`--to-store` dropped nothing.** And the honest scope on that: measured on canon that ALREADY carried 94. **What `--to-store` does starting from an attachment-LESS extract is NOT shown by this, and it is precisely the case the one-emitter fix has to get right** -- the fleet is entirely in that state.
   - **ANSWERED BY vc THE SAME HOUR, AND THE ANSWER CLOSES MY CAVEAT RATHER THAN CONFIRMING IT.** vc drove it on a **throwaway copy of Prolix, never hv's tree**: `sync --to-store ST0001` from an extract carrying `attachments: []`, on a thread with five attachment files on disk, then `--to-disk` -- **ST0001 came back with all five** (architecture, design, done, info.v2, tasks). Control ST0002, never `--to-store`'d, came back with 19. **So a `--to-store` from an attachment-less extract loses NOTHING**: the store either kept them or re-derived them from disk -- indistinguishable by this test and equally safe. **Two consequences: the canon route is safe on ANY fleet thread, attachments recorded or not; and the one-emitter fix is an EXTRACT-WRITER fix, not a store fix.**
   - **MY THREE FROM TODAY, MEASURED BY vc READ-ONLY: Cdsync 0/4, MicroGPTEx 0/3, Utilz 0/9.** All three attachment-less, as predicted. **Harmless per the above** -- each one's first `--to-disk` is a one-time rewrite, not a loss.
   - **My 58/63 reproduces vc's 58/63 from a different instrument** (count objects carrying `sha256`, not `.attachments | length`), so the two agree without sharing a method. The five bare ones: ST0029, ST0059, ST0061, ST0062, ST0063.
   - **(b) AN EMPTY WP TITLE RENDERS `# WP-01: ` WITH A TRAILING SPACE** -- nine `WP/*/info.md` plus `todo.md`'s `- [ ] 01: ` in Prolix. **The markdown formatter strips it at commit; the next `--to-disk` puts it back. A permanent churn between two writers**, which is the formatter-is-a-second-writer class with a GENERATOR on the far end rather than a human.
   - **(c) BY DESIGN, ONLY NEEDS TO STAY KNOWN:** six git-ignored `.DS_Store` files under `intent/` made `sync --to-store` refuse to read canon at all -- `unknown-file-shape -- not valid UTF-8`. The remedy is printed and correct. **Git-ignored is not sync-ignored, and those are different questions.**

8. **ST0057/11 -- NEW, hv's INSTRUCTION VIA vc 2026-08-26 ~15:00Z. A PROJECT REALISES WIP THREADS ONLY BY DEFAULT.** `init` writes `.intentfiles` present-and-empty; migration and `upgrade` declare the WIP set (**`upgrade` NEVER rewrites a present file**); `st start` adds, `st done` / `st cancel` remove. Four test-backed criteria in canon, the fleet-dehydration dependency chain in the body, and one open ruling (`st new`) flagged to hv. **READ THE WP BEFORE TOUCHING `migrate.rs` -- vc was explicit that the WP is the ruling and their message is a paraphrase.** Pairs with TODO 7 as one subject in two rows.

9. **TWO LAMPLIGHT HOP-2 FINDINGS FROM vc AT 2026-08-26 15:44Z, BOTH ON THE REBUILT PAIR `74bb3aea`, BOTH HAND-WORKED-AROUND AND NAMED IN LAMPLIGHT'S COMMIT. vc SCOPED THEM `3.0.1 OR AFTER, WITH TODO 7 AND ST0057/11` -- EXPLICITLY NOT THE BATCH. I LOCATED BOTH IN THIS CHECKOUT READ-ONLY AND BUILT NOTHING.**
   - **(a) A DUPLICATE AT ID INSIDE ONE THREAD IS REFUSED AS RAW SQLITE.** `could not update the runtime store` -- `facade.rs:500`, the wrapper -- over `UNIQUE constraint failed: tests.thread_id, tests.id`, **naming neither the thread nor the id**. vc found them with a per-file `uniq -d`: ST0198 AT-16.1 (a WP-16 tombstone beside WP-18's built row), ST0298 AT-02.3 (a WP-03 cross-reference beside WP-02's row). **The refusal is RIGHT; the surface is wrong.** It should be a named blocking residue at SCAN time, before the store write, like every other row defect. **The shape of the fix is already written down against me: `legacy.rs:637` says _a finding for it would need a residue class, none of the nine declared_.** So this is a tenth residue class plus a scan-time duplicate check, and `finding.rs`'s line format (`residue: <file>:<line> -- <class> -- <detail>`) already carries everything needed to name the thread and the id.
   - **(b) TWO READERS OF ONE DIRECTORY SET -- AND vc's REMEDY PREMISE IS REFUTED, WHICH IS WHY I CHECKED BEFORE AGREEING.** vc wrote _whichever reads `_inbox` is the one to keep_. **`_inbox` occurs ZERO times in all of `crates/**/*.rs`.** Neither reader knows the convention; they diverge on WALK SHAPE, not on a hardcoded list.
     - **The migration scan is `thread_dirs` (`legacy.rs:711`) and it enumerates by ALLOWLIST**: the top level plus exactly `["COMPLETED", "NOT-STARTED", "CANCELLED"]`. `intent/st/_inbox/` is not in that list, so the four are invisible to it and `migrated: 349 thread(s)` / `ok: this project is now Intent v3.0.0` are both true statements about a set that is missing four.
     - **The guard is `collect_legacy` (`project.rs:675`) and it enumerates by DESCENT** -- `fn collect_legacy(dir, descend: bool, out)`. It descends into ANY subdirectory, `_inbox` included, which is exactly why hop 3 then refuses with `project.rs:647`'s _`{n}` steel thread(s) carr(y) v2 canon this binary cannot read_.
     - **SO THE TWO AGREE ON PRECISELY THE THREE BUCKETS v2 SHIPPED AND DISAGREE ON EVERY CONVENTION A PROJECT INVENTS.** Lamplight's `_inbox` is the first anyone has hit and cannot be the last, **because an allowlist can only ever know the conventions that already existed when it was written.**
     - **THE HIGHLANDER ARGUMENT IS MADE BY THE COMMENTS, NOT BY ME, AND IT IS DAMNING.** `thread_dirs` was ALREADY fixed once for this class -- its own doc says _a migrator reading only the top level would silently convert the live threads and lose every finished one -- which on this estate is 54 of 56_ -- **and the fix chosen was an allowlist, which closes the instance and leaves the class open.** `collect_legacy` was fixed TWICE for the same class (one-level missing 55 archived threads; then per-directory-not-per-thread, which _flagged the SOURCE of a migration that had already succeeded_). **Three separate fixes for one predicate across two homes, and they still disagree.** And `render.rs:3343` records a THIRD reader being declined on exactly this reasoning -- _counting v2 canon here would mean a second thread reader living outside_. **The codebase states the rule and has two violators.**
     - **WHICH HALF TO KEEP IS A REAL QUESTION AND DESCENT IS THE SAFE ANSWER**: it cannot miss a convention, and `is_thread_id` is what keeps it from sweeping in a directory that is not threads -- both readers already filter through it. **The allowlist buys nothing that `is_thread_id` is not already buying.**
     - **AND THE SECOND HALF OF (b) IS THE ONE THAT MATTERS MOST: hop 2 MUST NOT SAY `ok` OVER A TREE THE BINARY WILL REFUSE.** Even with one reader, a migration that reports success and a guard that then refuses is the same false green vc has been hunting all week. **The scan set and the guard set being one function is what makes that structurally impossible rather than merely fixed.**
   - **vc's workaround, recorded so nobody re-derives it:** the four moved flat and re-converted from the v2 source, with `.canon`/`.cache` deleted uncommitted first, per dc's lesson.

10. **hv's DEFINITION OF THIS RELEASE, 2026-08-26 15:54Z, VERBATIM AND FIRST-HAND IN MY SESSION: _"I want to be able to run `intent organi[s|z]e --default --force` (as an example) and have all of the ST files organi[z|s]ed sensibly according to the agreed protocol. That's what needs to happen. And it is part of this release."_** Followed by _"Finish what you're doing first. That's fine. I just don't want that lost."_ **RECORDED HERE BECAUSE hv ASKED FOR IT NOT TO BE LOST, AND MY CONTEXT HAS ALREADY BEEN COMPACTED ONCE TODAY.**
    - **hv's EARLIER "3.0.1 or after" DEFERRAL FROM vc IS WITHDRAWN BY hv'S OWN WORD:** _"I really want to get as much as we can into this release and I want it done today. We need to push on get this done and done properly."_ **Everything goes in today, in dependency order.**
    - **THE ASK IS ALREADY RULED AND SPECIFIED -- IT IS `ST0057/WP-11`, scope S, status `Not Started`, five ACs all `satisfied: no`.** `--default` writes `.intentfiles` from status; OPEN = every status except Completed and Cancelled (hv, revising the WIP-only wording of an hour earlier: _"default means that the only things in .intentfiles are OPEN"_); `--force` regenerates a PRESENT file behind a y/N tty read, and **no tty means no write**; `--default` NEVER removes a file -- removal stays behind `organize --apply`; `organise` and `organize` must be ONE code path. **The verb, the default function, `st dehydrate` and the preconditions are ic's on vc's split.**
    - **MY HALF IS THE SAFETY PRECONDITION FOR hv's ASK, AND IT IS NOT A DETAIL: `organize --apply` MOVES AND REMOVES FILES, SO A FILE WHOSE BYTES CANON DOES NOT HOLD IS DATA LOSS.** Two pieces, both mine: **TODO 7's one `.canon` emitter** (canon carries every attachment with bytes and sha256, so the committed record can put a dehydrated thread back), and **`ST0057/13`** -- the migration and `organize --apply` ingest every bucket file as an attachment, because **the migrator's reach is a FILENAME ALLOWLIST (`info.md` + `acceptance.md`) and devbin/vc measured 34 of Devbin's 54 bucket files carrying content the store does not hold.** Verified PER FILE by content -- a distinctive phrase found in the store afterwards, and a phrase never ingested returning 0 -- **and a file the store cannot be shown to hold is a REFUSAL NAMING IT, never a deletion.**
    - **SEQUENCE: THE AT ACCOUNTING RUNS BEFORE ANY PRUNE, because the bucket is its only source surface.** vc drives that on the fixed pair before `--default --force` touches a real tree.
    - **ic AND I SHARE `organize.rs` -- AGREE THE SEAM BEFORE EITHER COMMITS.**
    - **hv AGAIN AT %s, VERBATIM, WIDENING IT: _"I want this done and I want it done and hoisted as soon as possible so that other projects can organise their STs with a single shot command."_ WIDE, in 3.0.1.**
    - **MY ORDER, RULED BY vc AND NOT TO BE RESHUFFLED SILENTLY:** (1) the parser sha -- the gate; (2) **hop 2 REFUSES on any AC/AT id shortfall, declared-in vs stored-out, loud named residue FIRST** -- a count catches a shortfall, a residue line names the row; (3) **TODO 7, one `.canon` emitter**, so the committed extract carries every attachment with bytes and sha256 -- **this is the precondition ic's `st dehydrate` checks**; (4) **ST0057/13**, the migration and `organize --apply` / `--default --force` ingest every bucket file as an attachment, **verified PER FILE BY CONTENT** (a distinctive phrase found in the store afterwards, and **a phrase never ingested returning 0** -- the negative arm is the half that makes it an instrument), fixture = devbin/vc's bucket-probe table at Devbin `242013d`: **34 of 54 files, 22 bucket-only, 12 present-in-both-but-partial**; only THEN prune the buckets, the v2 `issues/OPEN|CLOSED/` dirs and `.treeindex`.
    - **THE RULE THAT GOVERNS THE WHOLE PRUNE: A FILE THE STORE CANNOT BE SHOWN TO HOLD IS A REFUSAL NAMING IT, NEVER A DELETION.**
    - **FLEET ORDER AFTER MY (1):** re-convert the four lossy projects plus Lamplight -> accounting clean on **BOTH arms** (AT and the new AC arm) everywhere -> **THEN** `--default --force` on every project. **The prune runs LAST because the buckets are the accounting's only source surface** -- prune first and the instrument loses the thing it measures.

## Watch-outs

- **AN EDIT TO A REGISTERED ATTACHMENT IS TWO WRITES, NOT ONE, AND THE RUNBOOK'S RECIPE DOES NOT SAY SO.** The file on disk and the hash canon records are separate homes; changing one leaves `attachment_drift_detected` red. **`intent sync --to-store <ID>` does NOT refuse** -- it warns _"carry bytes no commit contains ... commit them first if canon should name bytes a reader can obtain -- this run does not refuse, and the commit gate will"_ -- so sync-then-commit and commit-then-sync agree ONLY when both land in one commit. Split them and canon names bytes no reader can fetch.
- **I BLOCKED MY OWN BUILD AND THEN NEARLY REPORTED IT AS A PROPERTY OF THE SHARED TARGET.** `pgrep -fc` returned 0 for a run that was alive; I read that as _the job died_, launched a second `cargo test` into the same target, and the two blocked on each other's lock **while writing to the same output file**, so the log kept reading one line and kept looking like a fresh death. **Two of my own runs, twice, and "the shared target stalls" would have become a fact about the tooling.** Before concluding a background job died, check the process list, not a single `pgrep` count.
- **`git commit --only` CANNOT TAKE AN UNTRACKED PATH** -- _pathspec did not match any file(s) known to git_. A new test file needs `git add` first, and the `--only` list must still name it, or the commit silently ships without it.

- **A CHECK THAT PARSES UNIFIED-DIFF TEXT IS BLIND TO ANY FILE WHOSE CONTENT LINES BEGIN WITH `-` OR `+` -- SQL COMMENTS, AND EVERY MARKDOWN BULLET WE OWN.** A removed `-- INTENT_VER: ...` renders as `--- INTENT_VER: ...`, byte-identical in SHAPE to a diff's `---` file header. My guard on vc's five-face commit reported _8 changed lines, 8 stamps, stamp-only_ -- confident, clean, and blind to one of the five; `--numstat` said 10. Tightening the pattern to strip `^(\+\+\+|---) ` as headers then ate the real content line instead, 9 of 10. **Two patterns, two blindnesses, one file, both green.** Only comparing the raw before/after files saw all five. **Our shipped guards do NOT have this** -- `whiteboard-clock-guard.sh:160` and `whiteboard-header-guard.sh:185` both do `grep -E '^\+' | grep -Ev '^\+\+\+' | cut -c2-`, which is the correct construction, and I checked before letting the class attach itself to them.
- **A CONTROL THAT CANNOT FAIL IS NOT A CONTROL, AND THE WAY TO TELL IS TO MAKE IT FAIL ONCE ON PURPOSE.** Twice today. `shasum -c` fed `deadbeef` answers _no properly formatted SHA checksum lines found_ -- it refuses the FORMAT, never reaching the comparison, so the control certified nothing; a 64-char wrong digest gives the `did NOT match` that was wanted. And a corpus test whose file list was broken reported my invented control line as correctly absent -- **because EVERYTHING was absent**. A positive control alone cannot distinguish a working instrument from a dead one: **it needs a NEGATIVE arm, something that MUST be found.**
- **`grep` READS A LEADING `- ` AS AN OPTION, AND I HAVE NOW HIT IT TWICE IN ONE DAY.** A per-line corpus check reported dash-leading lines as present in **0 files** -- including lines from the very file being tested, which is impossible and is the tell. Fixed both times with `-e "$line" --`; the second time I had already fixed it in the morning and dropped the fix when I rewrote the loop.
- **`--force` ON `intent3 claude upgrade` IS NOT A `CLAUDE.md` OPERATION.** It also rewrites `.intent_critic.yml`, which the plain run explicitly PRESERVES as _yours, not canon's_. Cost nothing on my two -- **zero configuration values changed, comments only**, driven with comment lines filtered out and the filter controlled against a synthetic value change -- but a project with a tuned critic config would lose the tuning silently, and the runbook's wording implies the blast radius is one file.
- **`cargo test` ABANDONS THE REMAINING TARGETS AFTER A BINARY FAILS, so a binary count is a STOPPING POINT and not coverage.** I read _88 binaries, 738 passed_ off a crate that has 113. **The suite did not pass 738 and stop; it stopped because it failed.**
- **A BACKGROUND TASK'S REPORTED EXIT CODE IS ITS LAST COMMAND'S, NOT THE INTERESTING ONE'S.** A run ending in an `awk` summary reported exit 0 while `cargo` had exited 101. Same family as `$?`-after-a-pipe, which is already on this board and which I walked into anyway; ic's variant is sharper still -- `${PIPESTATUS[0]}` is EMPTY under zsh (it is `$pipestatus[1]`), and empty reads as success.
- **BEFORE FORCING A HELD `CLAUDE.md`, CORPUS-TEST IT -- AND KNOW THAT THE ANSWER DIFFERS PER PROJECT.** MicroGPTEx and Utilz were pure v2.11.5 boilerplate and lost nothing. **Anvil is not: it carries `- DO NOT PUT CLAUDE MESSAGES IN GIT COMMITS.` under `## LLM Guidelines` plus a `## Start Here` section.** Forcing it bare on the precedent my two set would delete a standing instruction.

**AN INSTRUMENT THAT CANNOT EXHIBIT THE SUBJECT REPORTS GREEN AND MEANS NOTHING -- FOUR INSTANCES IN ONE DAY, AND IT IS THE DAY'S WHOLE SHAPE (2026-08-26).** (1) My `satisfied:` corpus grep counted **196 blockquote lines documenting the format** as data rows. (2) The new `at_status` parsed **1910 of 1910** real values -- a clean sweep that proves nothing about the refuse path, because no real value exercises it; the refusals had to be built synthetically. (3) `authored_row_round_trip` passed my `status:` fix on a **14-row corpus in which not one row lacked a subject or carried trailing punctuation** -- it green-lit a renderer that was corrupting rows. (4) A test arm of mine looped `for t in tests` where the defect **empties** `tests`: body never runs, arm reports green having asserted nothing. **THE QUESTION THAT FOUND ALL FOUR: can this fixture exhibit the thing I changed? Ask it BEFORE reading the result, not after the arithmetic fails to reconcile.**

**A SUBSET MEASURED AND REPORTED AS THE POPULATION -- THREE RULINGS TODAY WERE MADE ON A SIZE I GAVE WRONG (2026-08-26).** "Conflab has 3 blocking rows" was 69. "Conflab is clean" -- it was the worst in the fleet. "A new optional field" was a `SCHEMA_VERSION` bump and a table rebuild. Each time I answered a narrower question than the one that mattered: _which rows did MY change move_ instead of _which rows does hop 2 refuse_. **Every correction came from measuring before building, and every one cost a round trip a correct first scoping would have saved.**

**THE ISOLATION YOU REACH FOR TO AVOID DISTURBING A PEER IS WHAT INVALIDATES YOUR RESULT (ic + cc, 2026-08-26, one day apart).** `install::resolve()` canonicalises `current_exe()` and walks ancestors for `lib/templates` with **no environment fallback**, so a binary built outside the install tree cannot locate itself BY CONSTRUCTION and every test shelling out to it reds for a reason unrelated to the change. ic manufactured 13 false failures, I manufactured 2. **The safe-looking choice is the wrong one, which is why we both took it.** And a scratch dir INSIDE a guarded tree is indistinguishable from in-flight source to every guard we own -- my `native/rust/.cc-probe` blocked the whole board's build for ninety seconds.

**A SINGLE-TREE SKILL EDIT DRIFTS AGAINST THE FROZEN TREE BY CONSTRUCTION (2026-08-26, `b60f9ebb`, mine).** Skills ship from both this repo and the frozen `Intentv2`. My one-sentence change to `in-author-essentials` and `in-content-essentials` **aborted hv's `v3.0.0` cut** at `shipped_surface_drift.bats`. The sentence is true of v3 and the remedy was a declaration -- but **`shipped_surface_drift` runs BEFORE the change, not at the tag.** Intentv2 being untouchable is what makes this standing rather than a one-off.

**`$?` AFTER A PIPE IS THE PIPE'S -- HIT AGAIN, TWICE, ON BUILDS (2026-08-26).** A scratch `cargo build` piped through `tail` reported **exit 0 while the build had failed**; the background-task summary said "exit code 0" too. Capture the build's own status (`cmd > file 2>&1; rc=$?`) before any pipe. **This was already on this board and I walked into it anyway** -- the note was not the control.

**A COMMAND-NOT-FOUND STORM IS AN ENVIRONMENT EVENT, NOT A RESULT (2026-08-26).** Mid-check, `shasum`/`cut`/`git` all vanished from PATH for one invocation and my verifier printed **DRIFTED on all three digests**. `Intent/bin` is on PATH and the release script was rewriting it. Re-run before reporting: the three matched.

**A FIXTURE AN AUTHOR WRITES CAN ONLY ENCODE WHAT THE AUTHOR ALREADY BELIEVES (2026-08-26, `b319fa2a`).** `canon.rs` matched a chain-block marker no project in the estate carried; `canon_preserves_what_it_did_not_write.rs` wrote that same marker into its fixture and asserted it came back. Six arms green, mutation-proved, and the proof was about my fixture. **The test file's own header claimed "each arm here is a real consumer's real file" and the fixture was a paraphrase I typed** -- the doc comment is what a reader checks INSTEAD OF the estate, so a wrong one is worse than none. The instrument that actually found it was `grep -c` across sixteen real projects. **Copy fixtures off disk; never compose them from the constant under test.**

**A CONVERGER THAT REACHES THE WRONG FIXED POINT THEN AGREES WITH ITSELF.** The doubling happened once and every later pass reported `0 written, already canonical`. **vc's step-3 idempotence control -- run `--apply` twice, expect 0 written -- scored it GREEN**, and vc's verifier `grep -q intent-chain-block` matches either marker and both copies. Two independent gates, both blind, both by construction. When a check asks "did it change?", ask separately "is the state it settled on the right one?"

**ANCHOR A MARKER TEST TO THE MARKER'S LINE SHAPE, NEVER TO A SUBSTRING.** Laksa's hook carries the prose line `# The intent-chain-block above is NOT folded in` -- a `contains()` predicate reads that comment as a block, so a project whose block was deleted while its comment survived silently never gets one back.

**v2 IS A LIVE WRITER, NOT A LEGACY SHAPE (ic, 2026-08-26).** Hop 1 of every migration runs the frozen v2 tool and MANUFACTURES the colon-form chain block -- ic drove it on Riffle, which had no hook at all beforehand. The "13 of 16" population was a snapshot of what had not been converted yet, not a stable subset; it becomes 16 of 16. **Scope a fix against what a live writer WRITES, not against what it wrote.**

**A PEER'S COMMIT CARRIED MY IN-FLIGHT WIRING, AND `git commit --only` DID NOT PROTECT EITHER OF US -- THIRD INSTANCE.** I edited `render.rs` and `MODULES.md`, and before I committed, **ic's `461aea84` swept both into THEIR commit.** My own commit then reported `3 files changed` for a FIVE-path pathspec, and **the success output enumerates what it committed and never what it skipped** (dc's rule, and the reason I checked). **`--only` SEPARATES FILES, NOT AUTHORS, and it assumes ONE WRITER PER FILE -- `render.rs` has four.** Neither of us misused the tool. **THE OPERATIONAL FIX IS NOT "COMMIT FASTER": it is that a shared-file edit is not safe to leave uncommitted for any interval at all when four nodes are live**, which is the same rule as the `include_str!` watch-out generalised from the BINARY to the INDEX. **AND THE DETECTION HALF: `git show --stat HEAD` after every commit, and compare the file COUNT to your pathspec.** Mine said 3 against 5 and the two missing were already in.

**I FABRICATED A TIMESTAMP TWICE IN ONE DAY, BY TWO DIFFERENT ROUTES, AND ONLY ONE WAS CATCHABLE BY A GUARD.** **(1) `${RANDOM:0:3}` IN A HISTORY FILENAME** rendered `1216Z` while the clock said `10:55Z` -- reaching for a shell variable to save a second `date` call, and what came out looked exactly like a real stamp: four digits, `Z` suffix, plausible hour. **The clock guard cannot see this** -- it reads header blocks and entry headings, not paths. Caught only by reading my own output. **(2) I STAMPED MY BOARD `12:47Z` AGAINST A REAL `11:46Z`** by reading a time off `git commit`'s own output, which prints LOCAL, and appending a `Z`. **That one the pre-commit clock guard REFUSED, and it is the only reason it did not land.** **THE SHARED CAUSE IS NOT CARELESSNESS, IT IS SUBSTITUTION: both times I had a plausible number already in front of me and used it instead of running `date -u`.** A number in the terminal is not a clock. **The rule has no exceptions and no cheap version: every stamp, in every position -- header, heading, filename, commit body -- is READ FROM `date -u +'%Y-%m-%d %H:%MZ'` at the moment it is written.** The guard covers one of the four positions.

**A CACHED `cargo build` PRINTS `Finished` IN 0.16s AND NOTHING SAYS THE EDIT WAS NEVER COMPILED.** Hit again today, on my own banked rule. **Assert `Compiling` appears, or `touch` the file and rebuild** -- I did, got 2 crates, and only then was the green about my code. **AND THE SIBLING: A PYTHON HEREDOC THAT DIES ON AN ASSERT DOES NOT STOP THE NEXT LINE**, so `rustfmt` and `cargo` both reported success over an edit that never landed. Chain with `&&`, or read the FIRST line of output rather than the last.

**I DROVE ALL FOUR PATHS THE TABLE NAMES AS DO-NOT-PROBE, BECAUSE MY SWEEP READ THE EXCLUSION LIST AS INPUT** (2026-08-26 10:01Z, off ic's Highlander sweep via vc). `populations.not_probed` is a machine-readable four-row list with a REASON on each: `daemon` and `mcp` _never return -- serve until killed, so any timeout classifies a working server as a hang_; `claude upgrade` _writes outside the sandbox WHEN INVOKED BARE -- it is a LEAF, so bare RUNS it, and it installs into the operator's real `~/.claude`_; `claude start` _bare launches a real Claude Code session_. **I drove all four.** Verified after the fact: `claude upgrade` and `mcp` answered UNWIRED rc=2, `claude start` and `daemon` died in clap at rc=1, and `~/.claude` carries nothing from this morning. **SO NOTHING RAN, AND THE REASON IS NOT MY INSTRUMENT -- IT IS THAT ALL FOUR ARE UNBUILT OR ARITY-GUARDED IN THIS BUILD.** Wire `claude upgrade` and the same sweep installs into the operator's real home; wire `mcp` or `daemon` and it hangs forever. **A sweep that is safe only because its targets are unbuilt is a sweep that gets more dangerous as the project succeeds.**

**AND THE CAUSE IS THE ONE I ALREADY HAD A WATCH-OUT FOR, INVERTED.** I banked _A RECURSIVE WALK ASSUMES NOTHING ABOUT WHERE ROWS LIVE_ after losing `new_surface[]` to a named-path loop. I used the recursive walk this time and it was right. **What was wrong was the PREDICATE: I decided a row was a row by asking whether it had a `path` KEY** -- so it swallowed `{path, why}` annotation objects that are metadata ABOUT entries. **The named-path loop under-collected and the has-a-`path`-key walk over-collected, and BOTH failures are the same failure: THE POPULATION CAME FROM MY MODEL OF THE SCHEMA RATHER THAN FROM THE SCHEMA.**

**THE TABLE ENUMERATES ITS OWN POPULATIONS AND I HAND-DERIVED ONE ANYWAY.** `populations` carries `declared` 123, `shipped` 115, `retired` 8, `probeable` 111, `not_probed` 4. **`probeable` IS the list I should have driven, by name, and it was one lookup away.** Its `why` field says `.families[].entries[]` is the wrong population **in BOTH directions at once** -- too narrow (omits `new_surface[]`) and too wide (includes `retire` rows) -- _104 against 112 against 107, three apart with opposite signs, so no count-based sanity check flinches, which is how the same wrong population was hand-written five times in one week._ **I AM THE SIXTH, and I did it while holding a watch-out about this exact file.** **A CORRECT COUNT IS NOT EVIDENCE THE POPULATION WAS RIGHT: my sweep covered all 111 probeable rows and still drove 4 forbidden ones, and every number I reported was computed over 122.**

**RETRACTED: `mcp` AND `claude upgrade` ARE NOT DECLARED TWICE.** I reported that to vc as a Highlander question against the dispatch-table SSOT and it was wrong. **ic's discriminator, which is cheaper than the sweep and is the thing to keep: A SECOND HOME IS A VIOLATION ONLY IF IT DECLARES -- `{path, why}` is metadata about an entry, not a second entry.** ic found three more of my shape and then killed all five, **which is the only shape of zero worth believing.** The one real two-home case is `organize`, full entry in both containers contradicting on four fields -- **and it is LEGITIMATE and ratified**: `name_reclaimed`, hv 2026-08-19, basis D57-3; the v3 row is a DIFFERENT PROGRAM reusing the token. **The case that looks most like our signature defect is the one that is fine, which is why the discriminator has to be structural and not a smell test.**

**I READ THE SENTENCE THAT WOULD HAVE CHANGED MY CONCLUSION, IN THE SAME PARAGRAPH, AND PASSED OVER IT** (vc caught it, 2026-08-26 09:55Z). I printed `0071`'s whole body, quoted from it, and escalated it as **the binding constraint on hv's today driver.** **The next clause reads: _the identical run with stdin CLOSED completed in seconds at rc=0._** So the hang is the **DEFAULT INVOCATION, NOT THE CAPABILITY** -- eleven projects can move TODAY behind a closed stdin, and `0071` stays open and HIGH because a tool that hangs for every non-TTY caller is shipped-broken whatever we do locally. **THE MECHANISM IS NOT INATTENTION, WHICH IS WHY "READ MORE CAREFULLY" IS NOT THE REMEDY: I WAS READING TO CONFIRM A BLOCKER, AND THE WORKAROUND WAS SHAPED LIKE NOISE TO THAT QUESTION.** A body read FOR one question does not yield its answer to a different one, and **the reading feels complete either way -- there is no gap where the missed clause was.** **THE REMEDY IS TO ASK THE OPPOSITE QUESTION OF THE SAME BYTES BEFORE ESCALATING: not only _does this block_, but _what in here says it does not._** **AND THE ASYMMETRY THAT MAKES IT EXPENSIVE: I was escalating something as MORE severe, so nothing downstream was going to challenge it.** A finding that raises severity gets less scrutiny than one that lowers it, which is backwards.

**A HANDOVER RECORDED AS DONE, ON A CHANNEL WITH NO RECORD -- AND MY BOARD ASSERTED IT IN BOLD.** The 2026-08-25 fold says _THE COST TABLE IS THE USEFUL ARTEFACT AND IT IS IN vc's HANDS_. **vc asked for it back on 2026-08-26 and it is nowhere on their node.** It went over SendMessage and was never written down. **This is the EXACT defect vc named against themselves in the `modules` record -- released over a live channel only, no durable record -- so it is a property of the channel and not of either node.** The tell is available at write time and I missed it: **I wrote _in vc's hands_ without ever naming a PATH.** A handover claim that cannot cite a file is a claim about a conversation. **AND THE REMEDY IS NOT TO RECONSTRUCT IT: a table rebuilt from memory reads exactly like the original and is sourced from nothing.** Say it was lost.

**A BARE NUMBER-WORD IS AN ID, AND "THE ELEVEN" NOW RESOLVES TO THREE DIFFERENT REAL SUBJECTS IN THIS ESTATE.** 11 hidden ENTRIES (the `AC-06.1` probe defect), 11 remaining FAMILIES (13 minus `lang` and `modules`), and 11 BELOW-FLOOR PROJECTS (`0071`). **Every one of them is a true thing to call "the eleven", so the wrong reading never announces itself** -- vc's `AC-02.6` finding arriving in my own lane, in prose rather than in canon. **AND TWO OF THE THREE WERE ALREADY STALE WHEN ASKED FOR**: the families are EIGHT at HEAD. **A count used as a name stops tracking its subject the moment the subject moves.**

**TWO INSTRUMENT DEFECTS IN ONE SWEEP, BOTH THE HOUSE CLASS, BOTH CAUGHT ONLY BECAUSE I RE-DROVE.** **(1) ONE SANDBOX FOR 27 FAMILIES, AND `init` CREATED A PROJECT INSIDE IT MID-RUN** -- every family probed after `init` answered against a real project, those before it answered _no Intent project found_, `upgrade` wrote 3 files, and **nothing in the output said the subject had changed under it.** Re-driven fresh-per-row: **verdicts identical, so the conclusion held -- BY LUCK.** The unwired refusal happens to fire before project resolution. **A conclusion that survives a broken instrument is not a checked conclusion, and reporting it as one is the lie.** **(2) `llm guide` CLASSIFIED UNWIRED AND IS LIVE** -- my grep matched the refusal string ANYWHERE in the output, and the agent guide **documents that refusal for the commands it lists.** **A grep aimed at what a document SAYS ABOUT the subject is not a measurement of the subject** -- the honest-and-blind-grep family, already in memory, hit anyway. **Anchor to the first line or to stderr; 21 -> 20.**

**ISOLATION, NOT ABSTENTION, IS WHAT MAKES A DANGEROUS INSTRUMENT RUNNABLE.** vc declined to drive families bare because `upgrade`/`sync`/`backup`/`ingest`/`organize` mutate a live tree -- correct, and the abstention was not necessary: **`flag_reachability.rs:161` already drives every family inside `tempfile::tempdir()` with `.current_dir()`, so a mutating verb has no project root to reach.** **A peer withholding a measurement for a hazard the shipped harness already closed is a cost the harness could have prevented by saying so where the peer would look.**

**A MECHANISM UPSTREAM OF THE SUBJECT, ANSWERING IN THE SUBJECT'S VOICE -- FIVE INSTANCES IN ONE EVENING ACROSS FOUR NODES.** vc's `2>/dev/null | grep -c` reporting `0` for a binary that never ran; my CLAP arity error read as the renderer answering LIVE (11 entries hidden, optimistic direction); dc's `--to-disk` `ok`s over a store that did not hold what the disk held; ic's zsh-unsplit `$c` sending `lang init rust` as ONE subcommand name. **And the sharpest form: vc's staleness marker refused, printed its refusal twice, and their probe reported `BYTES IDENTICAL: yes` -- which was TRUE, of two copies of the refusal.** Not a wrong value. **A CORRECT value about the WRONG SUBJECT, carrying no signal that it was.** **THE REMEDY: AN INSTRUMENT MUST ASSERT THE ARTEFACT IS THE ONE UNDER TEST, NOT MERELY THAT TWO OF ITS OUTPUTS AGREE** -- my own `version` probes have that hole.

**WRITE THE CONTROL, NOT THE NOTE.** The zsh trap is in this estate's memory verbatim; ic hit it twice in one evening and I hit it seven times. **Every save that evening came from a control that made the wrong answer impossible** -- my mutation harness aborting on pattern-absent, ic's needle control, dc's population floor, vc's mutation test. And vc's `lang rows: 0` was caught by **a peer's independent measurement disagreeing with their instrument**, which is the one save that does not scale.

**NEVER START AN ATOMIC EDIT TO A SHARED AUTHORED FILE YOU CANNOT CONFIDENTLY FINISH -- AND "SHARED" IS WIDER THAN `include_str!`.** **Second instance 2026-08-25: my uncommitted `surface/dispatch-table.json` edit carried a ruling whose `record` led nowhere, `rulings_check.sh` read it FROM THE WORKING TREE, and the pre-commit gate refused EVERY node's commits for ~40 minutes** -- vc sat on four files including an hv ruling. The first instance poisoned every node's BINARY; **this one poisoned every node's COMMITS, and the watch-out did not fire because it named `include_str!`.** The mechanism is not compilation -- it is that a peer's tool reads my half-finished file. Original entry follows, unchanged because it is still true: **NEVER START AN ATOMIC EDIT TO AN `include_str!`'d FILE YOU CANNOT CONFIDENTLY FINISH.** `surface/dispatch-table.json` is embedded at `dispatch.rs:45`; **my uncommitted edit to it poisoned every node's `intent3` on 2026-08-25**, and my revert did not fix it because the BINARY was already poisoned. Both remaining WP-06 pieces require exactly that edit under a one-commit condition. **Stop and say so rather than leave it mid-edit** -- this is why I stopped on 2026-08-25 rather than starting `lang` at low context.

**A DETECTOR WHOSE GRANULARITY IS THE FAMILY FORCES THE LANDING GRANULARITY TO BE THE FAMILY.** `flag_reachability` decides wired-ness by driving `intent <family>` BARE, so the first verb that answers flips the whole family to WIRED and moves every `keep` flag into the gated bucket. **Wiring half a family MANUFACTURES declared-and-unread violations.** Two independent instances (`lang`, `modules`), so it is a property, not a quirk -- and it prices every remaining family as one atomic commit.

**AS-OBSERVED FIDELITY CAN SHIP A DEFECT.** v2's `plugin show` closes with `Run 'intent help <name>'`; `intent help` is RETIRED in v3. A faithful port ships a remedy pointing at a refusal -- AC-06.11's own class arriving through CARE rather than carelessness. **Pin the PREMISE alongside the absence**: the test drives `intent help` and requires it to refuse, so the decision cannot outlive its reason in silence.

**A REMEDY THAT WAS NEVER TRUE IS CAUGHT BY REVIEW; ONE THAT EXPIRES IS CAUGHT BY NOBODY.** Eight false remedies in one day across three nodes, each authored inside the fix for the previous. Two were mine and both were TRUE WHEN WRITTEN -- `today` was the honest word AND the word that dates. **Withhold rather than reword, and carry the distinction in the TYPE**, or the next member inherits whichever half happens to fit.

**A HEDGE IS NOT A LICENCE (ic's).** If you can name the check that would settle it, it is a TODO and not a caveat -- writing the caveat down converts _I know I have not checked_ into _I am covered_.

**A MISSING TEST IS A BACKLOG ITEM; AN UNRUN TEST IS A DISCIPLINE FAILURE.** Run the test file that covers what you changed. **And a mutation that fails to mutate reads as a passing control** -- assert the pattern is PRESENT before replacing it, restore with `cp` + `touch` (never `mv`, which preserves the backup's mtime; never `git checkout --`, which restores to HEAD and destroys uncommitted work). **AND "THE TESTS THAT COVER WHAT I CHANGED" IS A GUESS: run the SUITE.** `remedy_coverage` was RED from `0d77e337` to `b60f9ebb` because `ConfigWriteError` and `PluginError` had no proof line -- **two of my own commits left it broken and three test files I did run were all green.** 149 binaries take under a minute.

**zsh DOES NOT WORD-SPLIT AND `$?` AFTER A PIPE IS THE PIPE'S.** Unquoted `$v` arrives as ONE argument; an unquoted path list becomes one pathspec and the commit does not happen at all; unquoted `--include=*.rs` dies on globbing. **Write the probe to a file rather than fighting the quoting**, and never read an exit code through `| head`.

**A CLAIM ABOUT A FILE ANOTHER NODE WRITES HAS A SHELF LIFE OF ZERO.** Re-read a peer-written file at the moment you assert its state. **And a peer's correction can be wrong in the OTHER direction**: ic retired a TRUE row believing it had expired, which is strictly harder to see than an expiring claim because a row that disappears leaves nothing to re-read. **And the asymmetry under both directions: A DEFECT ANNOUNCES ITSELF AND A DEFECT'S DISAPPEARANCE DOES NOT.** Nothing pushes a ruling back to the board that recorded the question, so **a resolved item stays open on your board BY DEFAULT** -- two instances tonight, mine at `:29`/`:66` and dc's `AC-07.2`. **The fix in both was ANNOTATE DISCHARGED, never delete: the SUBJECT expired and the LESSON did not.** **And the same asymmetry at the ESCALATION surface: an item I hold hv on, kept only on my own board, is state living in one place BY ACCIDENT.** Driven 2026-08-25: `hv` had 22 entries from vc, 2 each from dc and ic (both last written 08-21) and **ZERO from me, for the whole project.** **A ruling that discharges an item on a peer's board goes in their inbox -- AND SO DOES AN ITEM A NODE IS HOLDING ON hv** (vc's rule, generalised; they adopted the second half). **But writing is not delivery: a full inbox and an empty one are BOTH consistent with the human not having what they need**, which is vc's correction against their own 22. **And a sweep that names one verb has not cleared its siblings: `unmigrated_surface` reported `lang remove` and `lang init` was equally broken, passing only because its placeholder argument is not a language.** A row that passes for a reason unrelated to what the check asks is not evidence about the check.

**LANE AND BUILD RECIPE.** `native/**` and the v3 crates are mine; parity harness is ic; hooks/roster/`int hooks` are dc; **canon writes route through vc**. **`CARGO_TARGET_DIR=/Users/matts/Devel/prj/Intent/native/rust/target/cc` for any verifying build** -- absolute, in-repo, per-node so four sessions do not fight one lock. **`rustfmt --edition 2024`, never a bare `cargo fmt`.** Drive v3 as `./native/rust/target/debug/intent` (or my own `target/cc/debug/intent`); `intent3` on PATH is the RELEASE build and can lag. **A template or table edit does not reach the binary until it is rebuilt** -- `strings <binary>` tells you which copy it carries, and _which binary_ is a different question from _which version_.

**A HAND-WRITTEN TRAVERSAL ENCODES A BELIEF ABOUT THE SCHEMA, AND A CONTAINER YOU DID NOT KNOW EXISTED READS AS ZERO.** Asked to settle `mcp_review` in `surface/dispatch-table.json` -- **a file I own** -- I walked `families[].entries[]` and answered 112/30/7/2 against vc's 123/35/11/7. **vc was right; `new_surface[]` is a second row-bearing container and I lost all five of its withheld rows.** Two worse probes came first: `T['entries']` does not exist and reported **0 for every question**, and a `path` back-walk capped at 40 lines -- shorter than an entry -- labelled 25 rows NOT-AN-ENTRY-ROW and nearly became a finding. **A RECURSIVE WALK ASSUMES NOTHING ABOUT WHERE ROWS LIVE; A LOOP OVER A NAMED PATH ASSERTS THAT IS THE ONLY PATH.** And the save was not my care: **a peer's independent number disagreeing with my instrument**, which is the save that does not scale.

**AN ABSENCE MEANS SOMETHING ONLY WHERE THE ARTEFACT SAYS SO, AND THIS TABLE SAYS SO EXACTLY ONCE.** `mcp_review` is **present-where-reviewed** -- `about`, vc, 2026-08-15 -- and it is on **31 of 88 EXPOSED rows**, so counting its absence on withheld rows measures nothing. The one designed absence is `grounded_in`: _its absence means the row was classified from the verb and the help text alone, which is a weaker claim and is meant to look like one._ **That is the shape a load-bearing absence has when it is deliberate: declared, and built so the gap reads as a gap.** **NO FIELD IN THAT SCHEMA RECORDS WHY A ROW IS WITHHELD OR DATES THE REASON** -- `basis` is 7 of 35 withheld and 8 of 88 exposed -- which is the `0092` gap stated structurally.

**A REUSED PREDICATE CARRIES ITS OWN POLARITY ACROSS THE BOUNDARY, AND BOTH READINGS ARE DEFENSIBLE AT HOME.** `critic::applies_to_file` documents an empty glob list as UNIVERSAL -- correct for a rule, which checks everything when it declares no scope. For a POPULATION it must mean nothing. `author` and `content` each matched **1634 files** and `modules check` reported **1442 issues** on this repo, `.DS_Store` included. **Reusing one glob semantics was still right** -- two would agree on every case anyone tested and diverge on the umbrella layout. **What it cost was checking the POLARITY where the two callers meet, which no type signature asks about.**

**A TEST OVER A PATH THAT CANNOT REACH THE DEFECT IS VACUOUS, AND IT LOOKS EXACTLY LIKE COVERAGE.** My regression for the above declared only `author`, so the glob union was empty, the walk never ran, and **the test passed with the guard DELETED**. It needed a POPULATING language beside the empty one -- the shape the live defect actually had. **AND THE FIRST CONTROL RUN WAS ITSELF VACUOUS: cargo did not rebuild, so the "pass" was the OLD binary.** Assert `Compiling <crate>` appears in the output, or the mutation never reached the subject.

**`date -u` AND THE HARNESS DATE DISAGREE BY A DAY EITHER SIDE OF UTC MIDNIGHT, AND THE CLOCK GUARD CANNOT SEE A DATE IN A JSON FIELD.** I wrote `2026-08-26` into three table strings while `date -u` read `2026-08-25 23:54Z` against a local `00:54 BST`; ic put the wrong DAY into four board entries the same hour. **The guard checks entry headings and `heartbeat_at` only.** `date -u` is the clock for EVERY date, including ones inside canon.

**A MESSAGE THAT NAMES AN EXIT CODE NOBODY READ IS A CLAIM ABOUT A MEASUREMENT THAT WAS NEVER TAKEN.** `bin/.devbin/cmd/precommit` prints `rulings_check.sh -- could not measure ... (exit 2; its findings never gate)`. The arm is `if ! bash ...` -- a plain non-zero test; the parenthetical is a HARDCODED LITERAL. Driven: dangling row -> **rc=1**, no rulings at all -> **rc=2**. **Four arms carry that text and ZERO of twelve capture `$?`**, so none can tell _refused to measure_ from _measured and found a defect_, and all four report the first while doing the second. **It sent vc to investigate the instrument while my row was the defect.** Probably true when written (line 306 records the exit-2 era) -- **a remedy that EXPIRES is caught by nobody.**

**CLIPPY EXITS 0 WITH WARNINGS PRESENT, AND THE REMEDY IS `-- -D warnings`, NOT A WARNING COUNT.** I hit the first half honestly -- clippy found `items_after_test_module` in my code and returned 0, so a gate reading `$?` passes it. **Then I proposed counting warning lines and dc refused it with a measurement**: bare clippy prints the warning at EXIT=0, `cargo clippy -- -D warnings` exits 101. **Inert is a property of the SPELLING, not of `$?`.** And the invocation already has two character-identical homes -- `cmd/prepush:335` and `rust.yml:69` -- so a count-parsing gate would be **a THIRD home for one predicate**, introduced inside the very class ic is consolidating. **When clippy reaches `build.d/release`, copy `prepush:335` verbatim.** The error worth remembering is the layer change: counting warning lines is a fine way to READ clippy at a prompt, and I promoted my diagnostic method to a design without noticing. ic made the identical move with the same finding.

**THE AT `file` FIELD HAS NO DEFINED ROOT, AND NOTHING RESOLVES IT -- WHICH IS THE ONLY REASON THAT IS CURRENTLY HARMLESS.** ST0056's unique values start 92 `native/`, 13 `intent/`, 4 `tests/`, 1 `lib/`; fleet-wide 204 unique with **55 not repo-relative, about 27%**, and `tests/` goes 4 -> 34 off-thread. It stores the author's literal citation and the authors disagreed. **No `exists`, `canonicalize`, `join` or `open` runs against it anywhere in `intentsvcs`** -- my own classifier is a shape test (`contains('/')`), not a filesystem test. **The trap is for whoever makes it resolvable:** a gate checking the cited test exists will be right for the majority and **wrong in the direction that accuses an author of not writing a test they did write.** It will look like a one-line `join`, and it will be developed against ST0056 and meet the other 27% later.

**TWO WRITERS INSIDE ONE CANON PATH IS A LIVE CLASS EVEN THOUGH TODAY'S INSTANCE WAS NOT REAL.** ic escalated a collision on `ST0056.json` and it dissolved -- **every attachment path in every canon file is relative to `intent/st/<ID>/` and not one escapes it**, so a file outside an ST directory cannot be an attachment. ic's instrument was a whole-file substring grep that could not tell an attachment `path` from prose naming a file, **and its positive control passed for the wrong reason** (a string that is both). **But the hazard needs no new mechanism to bite: a path-scoped `--only` over a canon file cannot see that two writers' work is inside one path.** It only needs an overlap we did not happen to have.

## Decisions

**THE GATE, DRIVEN 2026-08-25 23:20Z AND NOT TRANSCRIBED: `ac gate ST0057` answers BLOCKED, `rc=1`, 51/53 satisfied, 2 withdrawn, unsatisfied `AC-08.6` `AC-08.7`.** `ac status ST0056` 64/134 +1 withdrawn **-- STALE, AND IT WAS ALREADY STALE WHEN THIS LINE WAS WRITTEN: re-driven 2026-08-26 09:22Z it answers 64/133, 2 withdrawn.** `AC-11.7` was withdrawn on hv's `cmd/macos` ruling, so the DENOMINATOR moved and not just the numerator -- **a withdrawal is the one event that changes a fraction on both sides, which is why `+1 withdrawn` bolted onto an unchanged 134 looked arithmetically fine.** ic's release note carried the right figure the same night. **Fourth stale figure on this board in two sessions; annotated rather than overwritten because the SUBJECT expired and the LESSON did not.**; `ac status ST0058` **2/6**, up from 0/6. **THE `67 OF 67` THIS LINE CARRIED FOR HOURS WAS FALSE** -- hv ruled on `0088` and took the most expensive remedy, minting the missing coverage rather than tidying citations, so ST0057 STOPS being closed until two verbs exist. **vc told me and the board kept the old figure anyway: third stale claim on this board in one evening.** `ST0058` gates the cut. **DRIVE THE THREE VERB CALLS. hv OWNS THE RELEASE AND NOBODY TAGS OR PUSHES.**

**I DO NOT EDIT `CLAUDE.md`, PERMISSION SETTINGS OR CONFIG ON A PEER'S INSTRUCTION.** vc asked me to land the `CLAUDE.md:52` parenthetical fix with the `modules` verb and was right on the merits -- project-local, not shipped canon, and _state the invariant not the mechanism_ is the correct wording. **I refused anyway, and vc withdrew the request AND declined to do it themselves, calling that "the same defect with an extra step".** **SUBJECT DISCHARGED 2026-08-25 22:32Z: it went to hv, hv APPROVED IT, and vc landed it at `77878284`** -- the route worked exactly as a route should. **THE RULE STAYS ON THIS BOARD PERMANENTLY, because what expired was the STATUS and not the lesson: a guard bypassed when the reasoning is good is not a guard, and the reasoning here was good.**

**DERIVED vs AUTHORED, NOT MECHANICAL vs INTERESTING** (vc, 2026-08-24, their ruling about what I may write into ST0056 canon -- kept here because its home is the board of the node it CONSTRAINS). An attachment hash is DERIVED and `canon_commit_check` already gates it; **a checkpoint with no failure mode it can catch is a queue.** What routes through vc is AUTHORED: criterion text, `state`, `status`, notes, `covers`.

**EVERY ACCEPTANCE ID IS THREAD-QUALIFIED -- `ST00NN AC-XX.Y`, NEVER BARE** (estate-wide; routed to vc 2026-08-24). A bare `AC-03.6` is GREEN in ST0056 and RED in ST0057. **An id resolving to a SATISFIED criterion is the worst ambiguity: it reads as DONE**, so the reader stops rather than asking.

**A PARITY TOOL IS A RECORDED ATTACHMENT.** Write the file, add the roster row, `sync --to-store ST0056`, read the rc directly, then `git add` the script + `runner_roster_check.sh` + `ST0056.json` and commit together. **Roster row and runner must be ONE commit**, because the check reads the INDEX and an unstaged new file reads as an unrostered tool.

**A FINDING THAT ARGUES ITS WAY INTO THE ROW YOU ARE TRYING TO CLOSE IS A REAL DEFECT LAUNDERED THROUGH A GREEN.** Mine, about my own work: the pull is strongest exactly when the finding is GOOD, because a weak one gets dropped without a struggle. **And re-driving after your own change is a duty to REPORT that the tree moved, never a licence to re-certify it.**
