---
node: cc
name: Control Claude
role: control
session_id: 18bdb7ad-9dee-4a90-9994-4f7b63ebc463
heartbeat_at: 2026-08-24 17:55Z
status: active
focus: "**LEANED HARD AND COMMITTED 2026-08-24 17:53Z. NOTHING IN FLIGHT.** **STATUS STAYS ACTIVE: `/compact` is transparent (invariant 6); the `release` in localfold is for `/in-finish`.** **THE LEAN IS THE POINT: ~60 watch-out bullets collapsed into EIGHT RULES, because today proved they were never sixty things.** Every instance is verbatim in `.history/20260824/wip-lean-1751Z.md` -- read it when a rule looks arbitrary; the instance is what makes it not arbitrary. **NEXT WORK: `AC-08.5`, the only open gate row (66 of 67), mine to build, ic covering.** **intent#0070 is FIXED at `3f367cf8` and closed by dc.** **THE DAY'S RULE, AND IT COST SIX INSTANCES OF MINE TO LEARN: THE MEASUREMENT IS NOT THE CLAIM.**"
claims: [ST0056/06, ST0056/10, ST0057/00, ST0057/01, ST0057/03]
---

# Control Claude (cc)

**THIS FILE IS THE COLD-SESSION MINIMUM AND IT IS DELIBERATELY SHORT.** Earlier days: `.history/20260820/`, `.history/20260821/`. Today verbatim: `.history/20260824/wip-fold-1735Z.md` (intent#0070 in full) and **`.history/20260824/wip-lean-1751Z.md` (every watch-out INSTANCE the eight rules below were derived from).**

## The model -- canon, hv ratified

**D01 IS REVERSED: THE DB IS THE SSOT, THE FILES ARE RE-CREATABLE.** Three layers: canon (`intent/.canon/`, committed, never sparse) / store (`intent/.cache/intent.db`, gitignored, durable SSOT) / views (`info.md`, `acceptance.md`, generated -- **a row authored in a view is discarded**). **`intentdb` NAMES NO COMPONENT** (hv, `513642e7`): `intent-cli` and `intentd` are BOTH clients of `intentsvcs`, which solely owns the db. **D34:** the committed extract travels, the db never does. **D29:** a gitignored path is never canon. **`.intentfiles` is DURABLE STATE, and ABSENT IS NOT EMPTY** -- a missing manifest keeps everything, a manifest declaring nothing keeps nothing.

**D42 -- TIME.** `date -u +'%Y-%m-%d %H:%MZ'`, own step, trailing `Z`. **TWO GENERATORS, AND ONLY ONE HAS A READ-BASED REMEDY.** (i) ARITHMETIC -- a true reading advanced by feel; monotonic by construction, so it defeats check C; **a second read catches it, and the read is per-stamp, never per-session.** (ii) FABRICATION WITH THE CORRECT VALUE ALREADY PRESENT -- vc measured this on themselves, reading `17:39Z` and writing `17:45Z` in the same tool call. **No read defeats (ii), because the read already happened.** **THE REMEDY IS TO REMOVE THE HAND, NOT TO INFORM IT: never type the value, substitute it from `date -u` into the edit.** The hand already had better information. **A peer's stamp is typed of necessity -- attribute it, never assert it.**

## AC-08.5 -- THE ONLY OPEN GATE ROW. 66 of 67. MINE TO BUILD, ic COVERS.

**THE GATE IS COMPUTED BY A VERB, NEVER HAND-TALLIED:** `intent ac status ST0057` + `intent ac status ST0056/03`, and `ac gate ST0057` names the outstanding ids. **Publish N-of-M with its revision AND which store answered** -- the store is gitignored, so `ac status` answers about state no clone can reach.

**THE METHOD IS THE PART TO KEEP: SPLIT BY DOOR, NOT BY ROW.** vc offered me their three absence claims; I declined, because two nodes reading the same CLI source is **one instrument counted twice, and if we agree the agreement is worth nothing.** vc took source-side, I took binary-side. **The point of the split is the DISAGREEMENT.** This row's history already holds four dead absence claims, the sharpest being a pin asserting no creator existed **while `put` created both thirty lines away in the same file.**

**STATE.** Limb 2 is measured, not predicted: ic's `ea84d0ae` clears **8 of 8** unasked scalars through a minimal legal `put` at the thread door, with the graft control showing wps/criteria/tests/attachments unmoved. vc amended the criterion at `7b49adc9`. **Claim (3) -- no CLI verb creates an AC or an AT -- HOLDS BY REFUSAL rather than by enumeration:** `st done` demands an AC the CLI ships no verb to define. **Three burning cases live; only the attachment-setter clause is refuted.**

**MY RETRACTION STAYS ON THIS BOARD BECAUSE DELETING IT WOULD MAKE THE RECORD READ AS THOUGH THE ERROR NEVER HAPPENED.** I claimed the row's wording was wrong when written. **It was not.** `"no write path at all"` has exactly ONE home -- `facade.rs:4123`, hv's ruling COMMENT -- and the criterion's own words are that `completed` has no FIELD-SETTER VERB, which neither door is. **Burning case 1 is intact as worded.** The generator is rule 7 below.

**PIN THE BINARY: HASH TO IDENTIFY, MARKER TO ADMIT OR REFUSE** (my correction of vc, completed by ic). Neither does the other's job, and **both are required** -- a range with no pin names a distance from a moving point; a pin with no range names bytes with no consequence. **A dirty marker makes a committed range a FLOOR, never a distance** (dc). **`intent3` on PATH is 73 commits behind, 8 in `native/rust/crates` including the setter path** -- and a stale binary would have found the setters ABSENT, **which is what the row already says, so it would have read as CORROBORATION.**

## TODO

**1. `AC-08.5`.** The only open gate row. Everything operative is in the section above.

**2. U3's BUILD queue** -- vc under hv's pen: drive v3 to LOCALLY USABLE across the 17 projects here. ic measures the daily-use population, cc builds. Start on the half depending on nobody: the family-and-subcommand delta driven **FROM THE TWO BINARIES**, never from `dispatch-table.json` -- `shipped: 115` claims what is IN the binary, never what WORKS. **Confirm `treeindex` absent BY POPULATION, not by one grep.**

**3. HELD ON hv -- DO NOT BUILD.** `sync` skipping untracked bytes. The relay wording is close to what AC-03.6 names as insufficient. **dc's caution is the sharp part: the skip must tell STAGED-BUT-UNTRACKED from UNTRACKED-AND-UNSTAGED, or a legitimate two-step workflow becomes a silent no-op.**

**4. `AT-00.6` stale `to-write`** while `migrate_v2_project.rs` exists -- the gate reports it every commit. Canon correction, **route to vc**.

**5. Two `intentdb` doc comments:** `intentsvcs/src/lib.rs:11`, `project.rs:786`. Fold into the next edit in those files.

## BLOCKED

**NOTHING OF MINE IS BLOCKED, AND NOTHING IS BLOCKED ON ME.** This section claimed otherwise for a full day after it stopped being true, while the contradiction sat twelve lines above it. **A board can hold its own contradiction and read fine, because nobody reads two sections against each other** -- and the entry that goes stale is never the one being edited.

## The eight rules -- what ~60 watch-outs collapse into

**THEY WERE NEVER SIXTY THINGS, AND THE LIST WAS HIDING THAT.** Each rule carries ONE instance, chosen as the sharpest rather than the first; the rest are verbatim in `.history/20260824/wip-lean-1751Z.md`.

**1. THE MEASUREMENT IS NOT THE CLAIM.** A true measurement of a DIFFERENT property, offered as proof. **Six firings of mine in one day.** Sharpest: I struck a recorded reason as expired, having driven that `pgrep -x` is the correct needle -- true, reproducible, both directions -- **and offered it as proof the FILE CONTAINED it. The file did not.** The lesson is not _re-derive_; it is **RE-DERIVE THE SUBJECT THE CLAIM IS ABOUT.** The measuring being real is what makes it persuasive.

**2. THE POPULATION IS THE CLAIM** (vc's fold; extended here to RULES). A fix, a figure, a roster and a revert list each name a SET, **and the set is what nobody checks.** Sharpest: a correct ETXTBSY fix, correctly diagnosed, with the right `raw_os_error` match, **applied to one site of three** -- 4 of 12 CI runs red, Linux only. **The fix BEING CORRECT is what kept it alive: a right helper with a right doc comment reads as finished work, so nobody asks how many places it belongs.** **A remedy asserting completeness is a population claim about the failure modes it covers; a remedy that NAMES a failure mode without covering it is a population claim of ZERO dressed as a finding.**

**3. A CLEAN RESULT IS NOT EVIDENCE UNTIL THE INSTRUMENT HAS BEEN SEEN TO REFUSE.** Positive-control every zero. Sharpest: I greped `native/rust/intent-cli/src`, **which does not exist**, and got a zero reading exactly like _nothing touched the CLI_ -- one step after telling vc I would guard against this. **A zero from a wrong path and a zero from a true absence are the same characters on screen.** Corollaries: `grep -c` exits 1 on zero; **a zero from a data command is silent, from a missing file loud, from a name search a fact about the SEARCH.**

**4. SILENCE READS AS SUCCESS.** An absent signal taken for a positive one. Sharpest: `rust.yml` is **path-filtered** on `native/rust/**`, so a day of whiteboard commits accumulates green while the flake is never sampled -- **one green against a 1-in-3 flake is 1.5-to-1, and eight consecutive greens is the first below 0.05.** Same class: an hv inbox with no named reader; a gitignored SSOT invisible to `git status`. **The active form is worse -- a guard printing `All tests passed!` while skipping every test.** **COUNT THE RUNS, NOT THE COLOUR.**

**5. A RECORDED REASON IS RETIRED BY AN UNRELATED CHANGE, AND NOTHING WATCHES THE JOIN.** Eight instances in a week. `at lint` exempting `to-write` is CORRECT, so it cannot see this -- **every one surfaced because a builder picked the reason up in order to USE it, which is scheduling, not an instrument. The undiscovered ones are those nobody has needed yet.** **RE-DERIVE A RECORDED REASON BEFORE RELYING ON IT, AND AN ATTRIBUTED ONE HARDEST.** Worst instance: `intentdb`, wrong from birth, sitting inside two quoted hv rulings for six days across five nodes -- **attribution is what stopped anyone checking.**

**6. STATE IS NOT A POINT.** **`FIXED` IS NOT A STATE: worktree, index, HEAD and pushed are four** (vc). Every node here holds dirty files and commits only on hv's word, so **a peer saying _I fixed it_ reports the FIRST while the reader hears the THIRD -- that is this estate's DEFAULT CONDITION.** I did it myself six messages after agreeing with the rule. **A REPORT CARRIES THE STATE AT OBSERVATION AND IS READ AS THE STATE AT DELIVERY.** So: **name revision, clock and dirty count on every measurement**, and **a ` M` in `git status` is a claim about the INDEX, not about CONTENT** (dc -- `AGENTS.md` showed ` M` for a day with zero changed bytes; I put it in the peers bucket twice).

**7. A QUOTATION IS TESTIMONY ABOUT A DOCUMENT, NOT THE DOCUMENT.** Sharpest, and it is mine: **I took a peer's quotation of a row AS the row**, amplified it into a heading, a focus line and a report to hv, and it was three documents deep before ic checked six words. **The criterion was one `grep` away the entire time.** ic's rule generalises past fold instructions: _a peer telling you what only the source can settle is telling you what THEY read_ -- **and quotations are the commoner case BECAUSE THEY LOOK LIKE EVIDENCE RATHER THAN TESTIMONY.** Same family: **THE ENVELOPE BEATS THE BYLINE** (a relayed rule landed in the relayer's name despite a byline in the same sentence); **UNANIMITY ACROSS NODES IS NOT CORROBORATION WHEN ALL READ THE SAME INSTRUMENT** -- four nodes read `ListAgents` _started_, which is SOCKET age, and all four concluded the other three had bounced. Zero had.

**8. THE TRAP IS SELECTED FOR BY THE VEHICLE.** The dangerous wrong answers arrive in the slots where scrutiny is lowest, **and those slots are created by the APPEARANCE of rigour** -- a clean security sweep, a green CI board, **a peer checking your arithmetic.** vc computed a likelihood ratio as 2.0 and was a keystroke from correcting my 1.5; **both numbers are true and 2.0 answers a question nobody asked.** It would have arrived wearing diligence. **A result nobody re-checks is where a wrong answer survives.**

## Shared checkout -- four nodes, one tree

- **PRESENCE IDENTIFIES A FILE, NEVER ITS AUTHOR.** The working tree is nobody's tree; a peer's red conceals mine and reads as an all-clear. **A peer can sweep a file they do not write -- the single-writer rule only says who was WRONG.** The tell is **a file you just wrote reporting no diff**; check `git log -- <path>` before re-writing.
- **ALWAYS `git commit --only <paths>`.** It is path-scoped, not hunk-scoped, **and silently skips untracked files inside a named directory** -- `git add -N`, or `git add` the new file first (an untracked path is not _known to git_ and `--only` refuses it).
- **`--only` BOUNDS WHAT IS COMMITTED AND BOUNDS NOTHING ABOUT THE GATE, BECAUSE THE GUARDS READ THE INDEX** (vc). Sharper: **a guard that describes ITSELF is shared state** -- my half-landed roster row blocked every other node's commit. **Staging a guard's own body is a project-wide act, and nothing about `git add` says so.**
- **A LIVE CHANNEL IS UNGUARDED; ALL THREE CLOCK CHECKS RUN AT COMMIT.** The hazard is TRANSCRIPTION -- quoting a peer's live stamp launders it into the record past a guard at the wrong door.
- **ORDER IS FORMAT, THEN SYNC, THEN COMMIT.** `prettier` runs inside the commit window and re-stages **after** `sync --to-store` hashed the worktree.
- **A PARITY TOOL IS A RECORDED ATTACHMENT.** Write the file, add the roster row, `sync --to-store ST0056`, **read the rc directly**, then `git add` the script + `runner_roster_check.sh` + `ST0056.json` and commit together. **Roster row and runner must be ONE commit; either alone disagrees.**
- **`sync --to-disk <ID>` PROJECTS THE WHOLE THREAD, INCLUDING A PEER'S UNCOMMITTED STORE STATE.** Thread-scoping bounds the THREAD, never the AUTHORSHIP.
- **A CORRECT REFUSAL IS NOT A SAVE.** A refused `--to-store` leaves the authored bytes ONLY on disk, so the next `--to-disk` destroys them at rc=0 -- **correct under the model, which is what makes it dangerous. The refusal guarantees the loss and arrives wearing the costume of a save.** Fix the canon and re-drive `--to-store` to rc=0 first. **`sync --to-store` is disk-authoritative for ATTACHMENTS; for a typed field canon wins.**

## Standing rulings

- **EVERY ACCEPTANCE ID IS THREAD-QUALIFIED -- `ST00NN AC-XX.Y`, NEVER BARE.** A bare `AC-03.6` is GREEN in ST0056 and RED in ST0057. **An id resolving to a SATISFIED criterion is the worst ambiguity: it reads as DONE.**
- **DERIVED vs AUTHORED, NOT MECHANICAL vs INTERESTING** (vc, 2026-08-24 16:57Z -- their stamp, attributed). An attachment hash is DERIVED from a file I committed and `canon_commit_check` already gates it; **a checkpoint with no failure mode it can catch is a queue.** What routes through vc is AUTHORED: criterion text, `state`, `status`, notes, `covers`.
- **NO CHANGELOG ENTRY FOR A DEFECT INTRODUCED AND FIXED INSIDE THE SAME UNRELEASED CYCLE** (vc's ruling, verified here). The `[3.0.0]` block has **no `Fixed` section** while 35 release headings below it do -- **having to create the section is the tell.** Not a general rule: **`intent#0071` is a SHIPPED v2 defect and IS a changelog question, routed to hv.**
- **HIGHLANDER FORBIDS TWO ANSWERS TO ONE QUESTION; IT DOES NOT REQUIRE ONE ANSWER TO TWO.** Prevention and refusal are different criteria. **TWO ROWS RATHER THAN ONE WIDENED ROW** keeps two assertions separately falsifiable.
- **A CONTROL THAT DEPENDS ON THE AUTHOR REMEMBERING IS NOT A CONTROL, IT IS A HOPE WITH A FILENAME.**
- **THE ISSUE TRACKER IS FOR EXTERNAL USERS AGAINST A RELEASED VERSION.** Everything found building v3 is work.
- **`treeindex` and handover RETIRE** -- a retired command is PRESENT AND REFUSING. **`doctor --fix` is WITHDRAWN.**
- **`DO NOT PUT v3 ON PATH` IS RETIRED** (hv, 2026-08-21); `.envrc` puts `native/rust/target/debug` first inside this checkout. **THE AUTOMATION HALF IS STILL OPEN AND hv RULED IT SO:** direnv does not reach a tool-driven shell, so **every NODE commit still resolves guards out of the frozen `Intentv2`** -- and the nodes do most of the committing. dc's exposure. **`config.json` does not move with `intent_dir`.**

## Lane and build recipe

`native/**` and the v3 crates are mine. Parity harness = ic. Hooks, roster, `int hooks` = dc. **Canon writes route through vc.**

- **`CARGO_TARGET_DIR=/Users/matts/Devel/prj/Intent/native/rust/target/cc` FOR ANY VERIFYING BUILD** -- absolute and in-repo. Out-of-repo breaks `INTENT_HOME` resolution (`install::home()` walks `current_exe()` ancestors for a marker) and manufactures phantom failures; relative under a drifted cwd once built **1.2G** where gitignore hid it.
- **`rustfmt --edition 2024`, NEVER a bare `cargo fmt`.** **`cargo test --no-fail-fast`, ALWAYS** -- it halts at the first failing target otherwise, and a stopped run's denominator looks exactly like a denominator. **Confirm each subject appears in the `Running` list**; zero failures across a workspace does not prove a binary ran.
- **Drive v3 as `./native/rust/target/debug/intent`.** `intent` on PATH is v2.19.0 and answers for the FLEET.
- **Run the shell suite through `tests/run_tests.sh`, NEVER `bats` directly** -- the runner exports `INTENT_FIXTURE_VERSION` from `VERSION`; a direct run builds a v3 fixture against the v2 binary and dies on the version guard, **300 refusals reading as 300 failures**.
- **THE SHELL cwd PERSISTS BETWEEN CALLS -- use absolute paths.** **The Bash tool's shell is zsh and it does NOT word-split unquoted `$var`**; hooks run bash. **Never read `$?` after a pipe.** **`find` is bfs, not GNU find.** **An unquoted heredoc is a shell** -- keep the body quoted and substitute afterwards.
- **`intent st list` defaults to in-progress; use `--status all`. `issues list` takes `--kind open|closed|all`, NOT `--status`.**
