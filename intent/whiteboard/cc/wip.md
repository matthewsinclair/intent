---
node: cc
name: Control Claude
role: control
session_id: dd0650f6-a3a7-4513-99da-3842c2c1373e
heartbeat_at: 2026-08-17 20:25Z
status: active
focus: "ACTIVE, 19:41Z, hv AFK. **EIGHT LANDED, plus `959b0190`: `426416e1` `32708b02` `27c4ec98` `3233125c` `8adc6526` `4a4b05c4`.** 76 legs / 551 passed / 0 failed, clippy 0, fmt clean. **THE DAY BELONGED TO A SHOW-STOPPER ic FOUND AND I HAD SHIPPED: `intent upgrade` exited 0 and then EVERY VERB BUT `info` REFUSED -- reads AND writes -- with a remedy that looped forever.** `legacy_thread_ids` asked a per-DIRECTORY question where the real one is per-THREAD, so the detector was flagging the SOURCE of a migration that had already succeeded. The population is every v2 project with at least one thread in ANY status (v2 buckets from creation), so **the only unaffected project is one with no threads** -- which is why none of us could find the bucket-free control we were all hunting: it is empty by construction. ic reproduced at N=1 in four commands, confirmed on Baize (21 -> 0, 25 named against 25 on disk), and ran Utilz as the NEGATIVE control (0 thread.json, all 9 still correctly named) -- **the arm that proves supersession does not over-correct, which Baize could not show.** vc cleared all three members without re-migrating. **AND THREE INSTRUMENTS WERE GREEN OVER IT**: vc ALTERED 0 / ADDED 0, dc same-end-state IDENTICAL, my own door tests -- all reading FILES, none opening the estate. **Conservation, determinism and liveness are three properties; I verified two and inferred the third.** **SECOND, ALSO MINE, ALSO ic: the migrator invented a cause** -- `a previous run was interrupted` on a tree that never was. Canon existing is evidence that canon exists. **THIRD: 40 of 140 migrated WP views shipped TWO `## Acceptance` sections** (104 fleet-wide, every one that heading), because `legacy.rs` filtered only `Objective`. Fixed by comparing against the template WITH `bin/intent_wp:113`s `WP-NN` substitution -- against the RAW template it matches 0 of 40. 40 -> 0, 80 disposition rows reconciling exactly. **vc TOOK MY DECISIVE EVIDENCE AWAY AND WAS RIGHT: `40 -> 40 and nothing else moves` is a TAUTOLOGY** -- the other sections placeholders are not in the substitution set, so their counts could not move. A control that cannot move is not a control. **Their negative control then caught the one mutation nothing else could see: drop-by-heading-name reds ONLY that test.** **FOURTH: a hygiene note describes a STATE and mine described an OBLIGATION** (ic), costing vc and ic an evening each hunting a hole in a block arm that was intact. v3 off PATH; push local only."
claims: [ST0056/10]
---

# Control Claude (cc)

## THE MODEL -- canon, hv ratified

**The db is what is true. Everything on disk is an EXTRACT** -- `thread.json`, the `.md` views, `events.jsonl` are the same kind of object and none of them asserts anything. **One door in: the typed Rust API, and ingest is a CALLER of it.** **Sync's two directions are different operations**: db -> disk re-derives and cannot lose; disk -> db is a RESTORE that replaces truth -- except the event log, which MERGES, because nothing derives history. **The standing requirement is PLATFORM AND DATA-MODEL OPENNESS** (AC-02.6). **D34**: the extract is the interchange, the DB is per-machine and never committed. **D35**: snapshot = same-schema rollback ONLY. **D36**: `rm intent.db` is not an operation. **D37**: our ST/WP/AC ids never reach Intent's output, including the published schema faces.

## D42 -- TIME. THE WHOLE RULE, AND IT HAS NO CLAUSES

**DB records have a timestamp field. That is the source of truth for time. Nothing else. Ever.**

**You never ask what time it is** -- not the OS, not `date`, not the filesystem, **and not the database either**. Asking SQLite and then writing the answer is still writing a time you obtained. **The record is stamped BY the write.**

**hv's sharpening, verbatim:** _"intent3 won't have any cli or intentsvcs functions that TAKE a time. There will be cli and intentsvcs functions that RETURN times, but those will have gone end-to-end thru the db where the time was SET BY SQLite, not confected in an LLM hallucination."_ **That is a property of the API surface, not of the call sites.**

**Creating vs restoring is the split that makes it workable**: create -> the DB stamps; restore -> the recorded stamp is carried. **Re-stamping on restore or migration destroys history and every stamp still looks valid.**

- **THE DOOR IS A PROPERTY OF THE ACT, NOT OF THE ENTITY** (vc ruled, on my `write_issue` finding). Every entity carrying a domain date needs BOTH doors, because both acts reach every such entity. `write_issue` was not a smaller `write_thread`, it was `write_thread` with the create door missing -- and it was correct only because every caller was `rebuild`. **Clean by luck and clean by construction look identical in a diff**; the register's word for it is `known_exposures`.
- **THERE ARE FOUR DOMAIN DATES, NOT THREE.** `issues.closed` was missing from both enumerations, including the DDL comment a future author reads to decide whether a new column needs a door. **A rationale narrower than the use it is later asked to justify survives because nobody re-derives an inherited one** (vc): "created stays AUTHORED" answers a migration question and is silent on who fills it when nobody authored it.
- **Four things that are NOT exceptions**: a test fixture; "I'm only reading it"; "the value came FROM the database"; "it's just a board label".

## THE SELF-LOOP RULE -- hv ratified 2026-08-17; implemented `61069b16` + `b504d91b`

**A self-loop is legal, accepted and reported at exit 0, and it does NOT re-run the guard.**

- **WHAT DECIDES A SELF-LOOP IS WHETHER THE CURRENT STATE EQUALS THE VERB'S TARGET** -- not whether the verb is declared from the current state. Test the target, never the declared origins, or a verb declared from many states self-loops from all of them. `wp start` on `Done` has target `Wip`, `Wip != Done`, so it never reaches the self-loop arm and dies at the declared-edge test -- **which is 0046 staying refused by construction rather than by a special case.** In `data-model.md` as vc's reading, argued rather than deferred to.
- **"Without re-running the guard" is structural.** A self-loop must not be able to fail for a reason that did not exist when the state was entered. `wp done` on a done package returns 0 **even where the gate would now BLOCK**.
- **`AlreadyThere` is a NO-OP, not a repeated write**, and it now CARRIES the state. An envelope for a non-movement would stamp a second `st.done` at a second time.
- **It does NOT license an undeclared edge.** Same state with a different payload falls through to the machine and is refused.
- **A self-loop is not a transition, so no walk may enumerate `from == edge.to`.**
- **THE NO-OP VOICE (ruled, mine; ic and vc concurred independently): `ok: <subject> already <state>`.** It names the STATE, not the verb -- `was already done` coincides with the state only when verb and state share a word, and breaks on `st hold` (`On Hold`) and `st triage` (`Not Started`). **NO third prefix**: INV-01 names `ok:` and `error:`, and v2's `skipped: <ID> already in progress` is carried as a deviation on that one row. The state comes from the FACADE, never a literal in the renderer -- `ac rescope` lands on `entry(kind)` and cannot be named statically, and seventeen literals would be seventeen spellings a rename could not reach.

## DOING -- WP-10, hoist-first

### `7628a02b` -- CANON WINS ON RE-READ. The most important thing landed today and it was not on any list.

**ic measured re-runnability against real v2 bytes and the migration ACCRETED WITHOUT BOUND**: `ST0035/WP/01/info.md` at 8562 -> 8840 -> 9190 -> 9540 bytes over three runs, monotonically, no fixed point, 22 files differing between runs 1 and 2 -- and **every run reported 0 blocking, 0 carried, 28 files planned.** Every count reconciled while the documents grew. **The banner reading "do not edit this file, it is rendered from the model" ended up IN the model, three copies deep, in committed canon.**

- **THE MECHANISM IS THE D28 CATCH-ALL MEETING ITS OWN OUTPUT.** A rendered view ends with generated sections and the banner; the catch-all cannot tell the renderer's sections from authored ones, so it absorbs them and the next render appends fresh ones. **The catch-all is NOT the bug** -- it is the fix for the 178 dropped sections, and closing that hole is what armed this. dc's framing, kept: **a correct fix that arms a worse failure is an argument that the layer below it was wrong, not an argument to revert it.**
- **IT STOPPED ME SHIPPING THE SAME BOMB AT TEN TIMES THE SCALE.** I was mid-implementation of `Thread.body` as a catch-all, with the model field, DDL, rung and render slot scoped. `views::info` generates `## Work Packages`, `## Acceptance`, `## Related Steel Threads` and the banner, so it would have armed the identical accretion **on 56 threads instead of 140 work packages**. **So the sequencing was backwards: the section hole and the accretion are one mechanism from two ends, and closing the hole first is strictly wrong.** `Thread.body` is HELD behind the re-read contract.
- **dc's SYNTHESIS IS THE PART NEITHER ic NOR I HAD: there was NO stamp ordering that made an interrupted big-bang recoverable.** Stamp-last keeps the estate re-runnable and re-running was destructive; stamp-early leaves it locked out of BOTH toolchains (v2 refuses the future, v3 sees complete canon). **Two safety mechanisms composing into the failure rather than covering for each other.** That makes canon-wins a **precondition of hv's ruling**, not an improvement to it -- and dc took my point that the premise being RESTORED has to reach hv attached to the report that it was falsified, or hv reconsiders a ruling that no longer needs it.
- **A PROVENANCE MARKER IN THE VIEW WOULD HAVE BEEN THE WEAKER FIX**, and the reason is specific rather than aesthetic: **the view is precisely the artefact a user hand-edits** -- that is why `ViewSkew` exists -- so a marker lives in the file whose editing is the hazard, and stripping it turns a view back into an apparent source. **A discriminator belongs OUTSIDE the artefact it discriminates.** dc accepted this as a correction to their own 0049 axis: the axis says where information is missing, and it does not follow that the information belongs where it is missing.
- **NO HEADING-LEVEL RULE CAN EXIST.** `## Acceptance` is both a generated section and a legitimate authored v2 one, on 12 threads here.
- **LOADED, NOT DROPPED -- ic's correction, and it is the whole fix.** My first version used `continue`. `views::render_all` renders `steel_threads.md` and `todo.md` FROM THE WHOLE THREAD LIST, so a re-run of a mostly-migrated estate would have rewritten both globals with two rows and reported **`2 converted, 54 already` -- true, and reading as success.** Trading unbounded accretion for a silently truncated index is not a fix.
- **AND MY FIRST FIX WOULD HAVE MADE EVERY RE-RUN REFUSE.** The migration does not empty v2's status buckets, so afterwards a migrated thread has a directory at `st/<ID>/` AND `st/COMPLETED/<ID>/`; both resolve the same canon, both were loaded, and **the 0011 duplicate-id check blocked on the ordinary shape of a re-run.** Found by READING THE WALK -- no fixture had a thread in both places, because no fixture had ever been through a migration. 0011 is not weakened: two v2 artefacts claiming one id have no canon, so both still collide.
- **CANON THAT WILL NOT READ IS AN ERROR, NOT RESIDUE, and vc refusing to write the contract row is what produced that.** They asked which of `block`/`carry`/`record` it routes through; the answer is none. Every residue class describes something a **v2 author** left behind, with a fix environment and a carry disposition. This describes canon the migration wrote. **Second reason: `residue_class_check.sh` greps THIS FILE for `FindingClass::`, so a class constructed in `ingest` and passed through would be emitted by the migration and invisible to the check -- a laundered finding is a false green in someone else's instrument.** My first cut did exactly that and would have made `schema-invalid` reachable and undeclared at 9/9 green.
- **vc's TWO-DIRECTIONAL CHECK CLOSED THE ESCAPE ROUTE AND THEY DID NOT DESIGN IT TO.** My instinct was to DECLARE `schema-invalid`; the declared-but-not-emitted arm refuses that. **An equality refuses the documentation fix as well as the code fix, and only one of those is what you wanted.**
- `ingest::read_thread` extracted, `ingest::read` calls it: **one strict reader rather than two agreeing by transcription.**

### VERIFIED BY EXECUTION -- ic, against committed HEAD with a verifiably clean Rust tree

**The accretion is CLOSED and hv's release condition is MET by measurement rather than by argument.** Same estate that went 8562 -> 8840 -> 9190 -> 9540 three hours earlier:

- **Plain re-runs 2 and 3: `wrote 93, 10 already` -> IDENTICAL.** The fixed point exists.
- **Real `SIGKILL` mid-`commit` at THREE depths** -- first canon written, last canon written, and into the views -- **every re-run reaches the tree a clean run produces. IDENTICAL, all three.** `already_migrated` read 1, 10, 10, which is the reconciliation doing exactly its job: **it tracks how much canon survived the kill.**
- **The duplicate-id warning was the one that mattered and ic's first fixture could not have seen it** -- it had threads at the flat path; the real estate has 55 of 56 in `COMPLETED/`. Rebuilt on the realistic shape: `run 1: wrote 55, 0 already` / `run 2: wrote 55, 5 already -> IDENTICAL`, 5 dirs at the flat path and 5 still in `COMPLETED/`, **no block.**
- **ic's own correction, and it is why the verdict is worth what it is worth: the diff above is THEIR throwaway diff, not dc's gate tool.** They told dc they would not write a second comparator, then wrote one in scratch to get the answer, deleted it, and said so rather than letting it be quoted as the gate having passed. **The landed harness calls dc's.**

### THE DOOR IS BUILT -- `Facade::upgrade`, the one entry point that does NOT go through `Facade::open`

`open` calls `readable`, which refuses an unmigrated project, and the migrator runs on an unmigrated project by definition -- so the usual door would refuse the only estate the operation exists for. An associated function rather than a method: there is no `Facade` to be had until it has run.

**Order: plan (writes nothing) -> commit files -> rebuild store -> converge gitignore -> STAMP LAST. THREE separate arguments land on that last step and they are not the same argument:**

1. A stamp before the canon leaves a project claiming v3 with no canon, and `readable`'s gate stops firing -- **the state that tells the operator what is wrong is the state the premature stamp destroyed** (v2 learned this in ST0043).
2. **v2 now REFUSES a project declaring a newer version than itself** (`53f88757`), so between a premature stamp and complete canon the estate is locked out of BOTH toolchains -- v2 because the project is from the future, v3 because the canon is absent. **No tool left to fix it with.**
3. **It is what makes the re-run possible at all**, which is what fix-forward depends on: while the config still says v2, an interrupted estate is one v3 will migrate again. **Stamp early and a half-migrated project is one v3 believes is finished.**

Also in it: `FacadeError::MigrationBlocked` (delegating its remedy to `Blocked`) and `MigrationHalted { step }` -- **the step is carried because the remedy depends on it and the operator cannot see where it stopped**, and its remedy is _run it again_, which **is only honest because canon-wins made re-running idempotent**; before that, following the remedy would have corrupted the project. `converge_gitignore` is a PATH rule and deliberately not `*.db`. `stamp_version` rewrites config.json from its parsed form with one key replaced, so an unknown key a project carries survives -- it is the operator's file.

### AND IT IS WIRED -- `intent upgrade` OPENS. `d4648020` + the arm.

**The gap was REACHABILITY, not compilation, and both peer boards had it wrong in the direction that understates the work.** ic's said "blocked by a compile error in cc's `facade.rs`"; dc measured HEAD building clean and `intent upgrade` exiting 2 as _a known command that is not implemented yet_. Both true of different subjects, and **the door being 201 uncommitted lines in a shared worktree was the actual blocker** -- a gate measured off a dirty tree measures five people's in-flight work, which is the fourth instance of that class in two days.

- **`context()` AND NOT `open()`, and the seam already existed.** `open` calls `readable`, which refuses an unmigrated project; `context()` was split out for `doctor`, which needs to run on a project that cannot be opened. **Identical shape, so no new resolution path** -- and no `--path` flag: cwd, `Project::discover`, walking up to the config marker, same as every other verb.
- **EXIT CODES 0 AND 1, NEVER 2.** `Failure::Error` is _the command ran and the answer is no_, which is what a blocked migration is. `Unavailable` (2) says the build cannot answer at all and **consumers written against v2 read 2 as FAIL-OPEN** -- so reporting a data-loss refusal as 2 would invite exactly the wrong response.
- **`--backup-dir` / `--no-backup` NOT WIRED**, because the table holds them at `disposition: pending` and pending does not ship. v3 rolls back through git so v2's backup flags may not apply at all; **wiring them to satisfy the shape would answer a live question by accident, in a renderer.**
- **MEASURED ON INTENT'S OWN ESTATE, sacrificial copy, not a fixture**: 56 threads, 311 files, exit 0, `2.19.0 -> 3.0.0-dev`, gitignore converged as a PATH rule, store created. **Plain re-run byte-identical across 1361 files over three runs**, run 2 reporting `already migrated: 56`.
- **THREE CLI TESTS, BOTH GUARDS MUTATION-TESTED RED BEFORE GREEN.** Removing the one-line gitignore idempotence guard reds the re-run test and names the file and the growth (`.gitignore changed on the second run (93 -> 186 bytes)`); removing the residue from the blocked fixture reds the refusal test. **`upgrade_command.rs` rather than a new fixture in `cli_end_to_end.rs`** -- one file per command is the convention and integration tests are separate crates that each carry their own.
- **AND I CAUGHT MY OWN MEASUREMENT SKIPPING FILES.** First tree comparison piped `find` into `xargs shasum`, which split on a filename containing spaces: IDENTICAL over 1360 of 1361. Same skip both sides so the verdict survived **by luck, not by method.** Redone null-delimited. **A comparison that silently omits a row reads exactly like one that covered everything** -- dc's green from a door that never opened, and vc's 509-of-2289 swallowed by a `continue`, are the same defect in three languages on one day.

### hv's RELEASE CONDITION for my door (via dc, authorised)

**The cutover does not run until a second migration over an INTERRUPTED estate is measured to reach the same end state as a clean one.** Clean run -> tree A; run, kill mid-write, run again -> tree B; **A and B byte-identical**, then conservation on both.

- **THE VERDICT IS A TREE DIFF, NOT A CONSERVATION RUN** (vc's correction): conservation compares an ESTATE against CANON and every predicate is anchored on the estate side, so **prose GAINED is invisible to a check that asks whether what left arrived.** ic's accretion passed reachability, presence, DOUBLED and prose conservation -- four predicates, all true, all blind to the same thing because they all ask one direction.
- **Byte-identity of two independently produced trees is strictly stronger than idempotence as usually tested**, which compares run 2 to run 1 and so passes for a fixed point reached from a corrupted state.
- **THE INTERRUPTION POINT IS A WIRED `intent upgrade` PLUS A PROCESS KILL, NOT AN EXPOSED INTERNAL.** dc measured that `facade::apply` is private and asked; **do not widen it and do not add a test seam** -- a second door into the operation whose promise is atomicity, and it would measure a failure mode nobody has. Migrations die because the process dies. **So CLI wiring moves AHEAD of the issues parser and ahead of `Thread.body`.**

### THE TWO-ENDED COMMIT, and it is a five-node hazard rather than a discipline failure

**HEAD did not build and every push was refused: ic committed the CONSUMER (`74c4b357`) while I held the PRODUCER uncommitted.** `--only` commits what you name and a two-ended change has two names -- dc's own vendored-tree defect one directory over. **But here the two ends were on two different nodes' desks, so neither of us had a partial view to notice: both of us were looking at a green build of the same tree, green because it held both halves.** No vantage point inside either session could see it.

- **dc's prepush clone-and-build is the only reader that could, and its message is the finding**: _your working tree does, which is the whole point of this check_. **"My worktree builds" was true for all four of us simultaneously while HEAD was broken.**
- **THE GENERAL FORM: in a shared worktree every green is a claim about the union of five people's uncommitted work.** Same sentence as vc's census subject and dc's manifest. **Three of my four measurement-contamination scars today are this shape**, and `git archive HEAD | tar -x` into a scratch tree is the answer -- vc's and dc's habit, adopted, for anything I write down about HEAD.

## Earlier today -- landed

**`f357f99a`: `Scan.issues` landed BARE ahead of its parser, at ic's ask and on their argument** -- without it `migrate.rs`'s door needs a second public entry point added for a reason that expires this afternoon, and a public seam added for a temporary reason is one nobody removes. ic's `plan()` is wired onto it and MEASURED: **311 planned files on the canary, 56 `thread.json` + 1 event log + 254 views, no block, tree untouched after.** That is AC-10.3's engine running on the real estate.

**IN FLIGHT, all green locally, one suite away from commit:**

1. **`Issue.reporter`.** The v2 issue format has six keys and **`reporter` was the only one with no v3 home -- 40 of 40 carry it.** Dropping it is a lossy migration of the one estate WP-10 exists to migrate, and neither alternative recovers it: a silent drop loses it, a carried finding loses it AND reports that it did. hv's own ruling decides it rather than analogy -- _an issue is an external user's report against a released version_, so a tracker that cannot record who reported cannot be the thing that ruling describes. **Not the `Medium-Large` marked-legacy case**: that is a value outside a vocabulary and needs vc's spec; this is a field with no column, free text either side, nothing to spec. Cost: DDL, `SCHEMA_VERSION` 5 -> 6 with an `ALTER` rung, `write_issue`, both `SELECT`s, and all three faces re-pinned.
2. **BOTH DOORS, and the create half is the part I nearly shipped missing.** The migration is the RESTORE door. `issue_add` was going to be the CREATE door with nothing in it, so `intent issues add` would write `None` where v2 fills it. **That is the defect this estate already paid for one field over** -- `write_issue` was `write_thread` with the create door missing, correct only because every caller was `rebuild`. `issue_add` now takes `reporter: Option<&str>`; no dispatch row moves, because v2 has no `--reporter` flag.
3. **`canon_parts` emitted `issues/46.json` while every reader resolves `issues/0046.json`** (ic found it; I verified it against the file, not the message). **Not cosmetic: it breaks a migrated project at its next read.** `issue_numbers()` is tolerant and enumerates either spelling; `issue_json()` is not, so the open fails on a file whose neighbour is right there. Fixed padded, with the guard AT THE CROSSING on ic's argument -- writes what `canon_parts` names to a real disk, requires the set to equal what `Project` resolves, **then drives `ingest::resync` over it**, because two path builders agreeing is still two builders agreeing. Mutation-verified.
4. **`sections()` was a `BTreeMap`, so every reassembled document came back ALPHABETICAL.** vc measured **140 of 140 work packages** differ from their authored order; the `Vec` also stops a repeated heading being swallowed by `insert`. Both mutations driven, each reds exactly one test and not the other.

**NEXT, and it is the biggest conservation hole in the estate: `Thread` HAS NO `body` CATCH-ALL.** ic and vc found it independently within the hour. `legacy.rs` reads `Objective` and `Context` and drops every other section in silence -- **178 of 283 sections, 106,678 bytes, 56 of 56 threads.** `## Context for LLM` on 41 threads, `## Related Steel Threads` on 55. **Phase A reports 0 blocking and 9 carried and not one of them is about this**, which is worse than the issue estate: there the count reconciled against zero, here it reconciles against a real number that is simply the wrong number.

- **`related: Vec::new()` is a defect against the DECLARED model, not a design choice** -- vc checked data-model.md, which models `related` as `{id, note?}` read from that block. **And the render side already exists**: `views::info` emits `## Related Steel Threads` from `thread.related`. Only the parse is missing, so this half is small.
- **FOR ST0056 IT IS DESTRUCTION, NOT STRANDING, AND IT IS THE THREAD ALL FIVE OF US WORK IN.** It is the one thread already at the flat path, so `views::render_all` overwrites its own source: 3947 bytes carrying five sections become a ~2.2KB render carrying two. Recoverable from git is not conserved.
- **DO THE ORDERING BEFORE THE CATCH-ALL.** Copying `WorkPackage`'s body line into `Thread` would have closed the hole while propagating the `BTreeMap` defect -- and made the section counts reconcile on the way. ic caught themselves mid-proposal doing exactly that.

**THEN the issues parser**, which is no longer blocking anyone: ic's door is wired and honestly reports 0 issues against my empty field. When it lands the canary reads 40, all CLOSED, and the canon count goes 57 -> 97.

## TODO -- HOIST FIRST. hv ruled everything below the line parked.

0. **DONE `27c4ec98` -- THE DROP RULE IS BUILT AND PROVEN, AT WORK-PACKAGE LEVEL. `Thread.body` inherits a rule that now has a working implementation and two mutation arms rather than only a ruling.** I went to build `Thread.body`, measured the collision I was about to create, and found **THE SAME COLLISION ALREADY LANDED ONE LEVEL DOWN**: `legacy.rs` filtered only `Objective`, so 40 of 140 migrated WP views shipped TWO `## Acceptance` sections -- 104 fleet-wide (Utilz 11, Baize 53, Intent 40) and **every one of them that heading**, because it is the only template section whose body carries a substituted placeholder. **THE RULED RULE CAUGHT NONE OF THEM: against the RAW template `## Acceptance` is 0 of 40 while `Deliverables`/`Dependencies` are 20 each.** `bin/intent_wp:113` is `sed -e "s/WP-NN/WP-$WP_NUM/g"`, so **the artefact a file was copied from is the template WITH the substitution applied, and the raw template is a source no file was ever a copy of.** vc ruled substitution IN-RULE, not a widening. 40 -> 0 measured, 80 disposition rows reconciling exactly against the section census. **CARRY THREE THINGS UP TO THREAD LEVEL: (a) the template is EMBEDDED and cited by path + revision + substituting line, never read from the install, or the drop set becomes a function of which Intent is on the machine; (b) the drop is keyed on BYTES and never on the heading name -- mutation-verified, and the heading-name rule reds ONLY the negative control; (c) the negative control is not optional.**

0b. **AND THE DOUBLING WAS TWO PROBLEMS, WHICH I FRAMED AS ONE AND HAD THE EVIDENCE AGAINST. `8adc6526`.** I told vc to expect Baize 53 -> 0 and to flag a drop count HIGHER than the doubled count. **It came back 53 -> 20 and the drop count was LOWER -- a way of being wrong that was not on my list at all**, because framing it as one problem left over-dropping as the only imaginable error. 33 dropped + 20 authored = 53, closing exactly. **The 20 survivors are prose an author wrote and the drop rule is RIGHT to keep them: template-identity exists to refuse to touch authored prose, so it could never reach an authored-versus-generated collision.** My own board already said `## Acceptance` is a legitimate authored section on 12 threads -- **I had both halves and never put them together.** And I could not have seen it here: **Intent has ZERO authored survivors, so 53 -> 0 is what this estate predicts and it generalises to nothing.** **THE DECIDING ARGUMENT IS NOT DUPLICATION -- the generated line asserts the ACs sit under a `WP-NN` heading and Baize's authored pointer says `AC-01`, so on those 20 the GENERATED copy is FALSE** (vc's D47: the major number is an AC-group ordinal on some projects and a WP number on others). The tool was asserting a convention over an author who had already stated the right one. Fix: the generated default DEFERS when `body` carries that heading. `carries_heading` is line-anchored, not a `contains`, and is already shared because **the identical collision arrives at thread level the moment `Thread.body` exists.**

0c. **vc's DECLARED-DROP arm does NOT believe my dispositions, and that is the strongest form of our separation rule.** Their words, kept: **the disposition is my CLAIM; emptiness is their OBSERVATION.** A drop is excluded from loss only if the migrator named it AND canon verifies empty -- **without that, a future drop could launder real alteration through a disposition line, and my records would have been the channel.** Baize's true loss is now 0 (was ALTERED 39, all 39 mine). Their first cut matched 2 of 39 (a `sed` stripping the leading zero from `WP/06`) and **still produced the class, so "DECLARED-DROP appears in the output" would have read as working** -- now `N matched of M declared`.

1. **`Thread.body` -- NEXT, and it now carries `## Related Steel Threads` too. See hv item 2 for why `related` is NOT the cheap half.** `Thread` has NO `body` field (`model.rs:345`'s `body` belongs to `WorkPackage`), so the full cost stands: model field, DDL column, a rung, three face bumps, every `Thread` fixture, and a render slot. **THE DESIGN IS SETTLED AND IT COST ME ONE FALSIFIED DISCRIMINATOR. body = every section but `Objective` and `Context`, verbatim, in authored order, MINUS any section BYTE-IDENTICAL to the v2 template's section of the same name.** (Subtraction sent to vc for agreement, since a drop is theirs to price.)

**THE DISCRIMINATOR I NEARLY SHIPPED WAS BUILT ON A FALSE PREMISE.** Worried that a naive catch-all sweeps sections whose names the renderer also generates, I invented _was this section DERIVED from something the model now holds?_ -- on which `## Work Packages` is v2's generated index, the model holds the WPs, so the carried copy is stale and droppable. **Then I read the bytes. All 8 are hand-written prose** -- ST0019's `**WP02**: ... **SKIPPED** (folded into WP03...)` is a decision record existing nowhere else; ST0023's is a markdown table; ST0020's carry editorial annotations. **And `grep -rln "Work Packages" lib/templates bin` returns NOTHING: v2 never generated that section at all.** The rule would have destroyed 7458 bytes of authored narrative on the grounds a tool produced it, and no tool ever did. **A discriminator invented to protect content is the most dangerous kind, because it is trusted.**

**MEASURED against `lib/templates/prj/st/ST####/info.md`, section-by-section, all 56 threads:**

    section                 files   ==template   bytes
    Context                    50            0   53172   -> modelled
    Context for LLM            41           20   23641
    Objective                  55            0   17646   -> modelled
    Scope                       9            0   13946
    Related Steel Threads      55            3    9832
    Work Packages               8            0    7458
    Acceptance                 12           12    2556

**`## Acceptance` is 12 OF 12 BYTE-IDENTICAL to the template, and its text is the same sentence `views::info` already emits.** `## Context for LLM` is 20 of 41 identical and is v2 workflow scaffolding (_update related documents (design.md, impl.md, etc.)_) -- instructions for a document shape v3 replaces.

**vc RULED: DROP, and their principle is better than my argument for it.** _"Conservation is owed to what an AUTHOR WROTE, and byte-identity to the template is evidence that no author wrote it"_ -- so this is not a lossy drop needing an excuse, it **correctly identifies that there was nothing authored there.** And the half I had backwards: **CARRYING IS NOT THE CONSERVATIVE OPTION.** Carrying scaffolding into `body` files it as authored prose and the renderer emits it forever as though someone wrote it -- **that FABRICATES AUTHORSHIP**, and it is worse in the harder-to-detect direction because it looks like conservation.

**THREE CONDITIONS, accepted:**

1. **PER-SECTION disposition records, never a count** -- thread id, heading, reason. A count reconciles arithmetically and tells nobody which section went. Declared drops then move out of `LOST-PROSE` through vc's existing out-of-model arm; an undeclared one stays a finding, so their side needs no new class.
2. **CITE THE TEMPLATE BY PATH AND REVISION.** The path alone is a moving target: the same estate migrated at two revisions would drop different sections with nothing saying why -- the subject moving under the instrument. **A pinned citation makes the drop set re-derivable by someone who was not there.**
3. **EXACTLY ONE template version, and no-match carries.** Recorded so it does not get "improved" later into matching every historical template, which converts a safe rule into a wide one by increments.

**vc's SECOND CLUSTER IS THE EVIDENCE FOR CONDITION 2 AND I DID NOT HAVE IT**: section hashes across the canary show the 20 I found, **plus 8 MORE `## Context for LLM` sharing a DIFFERENT hash** -- byte-identical to each other, not to today's template, almost certainly an older generation. No author wrote them either, and **under this rule they CARRY, which is the correct and safe outcome.** The estate already contains more than one template generation, so the drop set is a function of which revision you compare against, measured rather than hypothetical.

**WHY TEMPLATE-IDENTITY IS NOT THE CLASSIFICATION migration.md FORBIDS, and the distinction is epistemic rather than a loophole.** _Looks like metadata_ is a guess -- I banned my own 13-against-7 preamble split on exactly that ground. **Byte-identical to the artefact that created it is a fact with a citable source.** And **every error goes toward CARRYING**: a thread seeded from an OLDER v2 template fails the match and is carried; authored text byte-identical to the placeholder IS the placeholder. There is no failure mode that drops authored bytes. **The drop must still carry a disposition** -- vc's AC-10.5 finish line applied one level down: a SECTION leaving the estate with no disposition is the same defect as a file doing it. It was the biggest conservation hole (178 of 283 sections, 106,678 bytes, 56 of 56 threads) and it stayed held because a thread-level catch-all would have armed the accretion on 56 threads; **ic has now verified re-runnability by execution, so the layer below it is fixed and the catch-all is safe to add.** `related` is the cheap half -- **`views::info` ALREADY renders `## Related Steel Threads` from `thread.related`, so only the parse is missing.** `body` is the expensive half: model field, DDL, a rung, three faces, every `Thread` fixture, and **a render slot in `views::info` or the field drops again on the first projection.**

2. **THE PREAMBLE, and it is measured on both sides.** Prose between the frontmatter and the first `## ` reaches neither `objective` nor `body` -- **canary 20 regions / 6213 bytes; fleet 248 regions / ~66KB** (vc's enumeration and mine agree exactly on the canary, arrived at independently in two languages). **CARRY IT VERBATIM AND DO NOT CLASSIFY IT.** My own canary split -- 13 metadata restatements against 7 carrying real prose, 4524 bytes, including two CANCELLED threads whose deprecation note is the only thing explaining the cancellation and one that sits ABOVE the `# Title` line -- **is good analysis that must not be used**: a rule dropping a block because it LOOKS like metadata is content-pattern classification, which migration.md forbids, and it was tuned on 20 regions and would meet Lamplight's 193. **Zero disagreements against frontmatter today is not a guarantee: carrying preserves the evidence if one ever disagrees, dropping destroys it silently.** Needs its own field, not `body` -- `wp_info` renders `body` AFTER `## Objective`, so carrying a preamble there relocates it.
3. **`wp_status`: ABSENT AND UNREADABLE ARE ONE FINDING AND MUST BE TWO** (vc, fleet-measured: 131 unmappable WPs, **79 with no `status:` line at all** against 52 carrying a value outside the vocabulary; 9 of the 131 are in LIVE threads and are the fleet's only blockers). `front.get("status").unwrap_or_default()` collapses them. **The fix already exists twenty lines away**: `scope` was given the three-state treatment (`Some/None`, `None/Some(raw)`, `None/None`) for exactly this reason. `FieldNotRecorded` vs `UnknownStatus` are both already declared classes. **Not a new decision -- one decision applied to one of two fields.**
4. **ONE NORMALISER FOR BOTH VOCABULARIES.** `scope()` strips ` -_` then parses; `wp_status()` matches literals, so the same file accepts `not started` and `not-started` and rejects `not_started` -- 10 fleet WPs. **Adding an arm is the WRONG fix**: it leaves two rules different in one file and the next spelling finds the same crack. One normaliser, both callers, underscore family closed rather than enumerated.
5. **THE ISSUES SCANNER** -- no longer blocking anyone, so it comes after the conservation work. `Scan.issues` is landed (`f357f99a`); this fills it from `intent/issues/{OPEN,CLOSED}/<nnnn>/<nnnn>-<slug>.md`. Mapping: frontmatter `id` (**quoted in v2 -- `id: "0015"` -- so strip quotes before parsing `u32`**) -> `number`, filename tail -> `slug`, `title` (**contains colons; `frontmatter` splits on the FIRST one, which is correct**), OPEN/CLOSED -> `IssueStatus`, `severity`, `date` -> `created`, `reporter` -> `reporter`. **`closed` stays `None`, never back-filled from mtime.** Findings route through `record(closed, ..)`. **The body is 503 sections and 641,893 bytes across 61 files at vc's pin -- more prose than the whole thread estate outside Objective and Context -- and `Issue` has no body field, so Phase A must carry it to Phase B or the migrator re-reads v2's format outside the one place allowed to.**
6. **THE 171 STRANDED AUTHORED FILES -- MINE, AND IT IS A DEFECT RATHER THAN vc's DISPOSITION QUESTION.** After a migration a thread has canon + generated views at `intent/st/<ID>/` and its v2 originals still at `intent/st/<BUCKET>/<ID>/`. **`info.md` is genuinely DOUBLED (superseded, safe to encode); `design.md` / `impl.md` / `tasks.md` are the ONLY copy and are reachable from nothing** -- 54 + 53 + 53 + 11 one-offs = 171 of 269 bucket files, measured on Intent's own estate at `d4648020`. **Half of a two-ended migration, which is the standing lesson on this board arriving from the other side.** Told vc NOT to encode it out-of-model; asked them to split the finding on has-a-counterpart so the DOUBLED half stops competing with the STRANDED half for attention. **Do not reach for a fix before hv rules on relocation** -- moving authored files is exactly the two-ended hazard in item 1 of the hv list.
7. **THE MIGRATOR DECLARES NOTHING OUT-OF-MODEL, so vc's `--out-of-model` arm has nothing to read** (their diagnosis, and it is a gap in MY lane not theirs). 347 UNACCOUNTED files on their run -- `intent/analysis/`, `intent/autopsy/`, `intent/docs/`, the whiteboard, `config.json` -- and **the migrator knows which paths it deliberately does not own and tells no one, so deliberate and forgotten are the same output.** Right shape is a declaration emitted the way the residue classes are. **Not scoped yet -- one list or a predicate is an open question and I will not promise a shape before looking.**
8. **THE `critic` EXIT CODES: 1 = FINDINGS, 2 = CANNOT RUN. See the hv list.** Measured: `critic shell` = 2 and `critic nosuchlang` = 2, so the pre-commit gate's fail-open branch is reached and that is the only reason any of us can commit in an unmigrated project. **The gate reserves 1 for your-code-has-findings and 2+ for the-tooling-is-unavailable.** A `critic` that lands exiting 1 for unavailable puts every migrated project in the fleet into refusing every commit with a remedy nobody can follow. Two lines in my lane, and it is on the ship path.
9. **AC-10.8's EGEST SIDE opens when the migrator writes** -- `Facade::projection` is the one place the db -> disk direction is expressed and it is private, so egest symmetry becomes `git diff --exit-code` the moment `upgrade` runs. **vc is building the conservation checks and wants to know the instant it is drivable.**
   9b. **DONE `959b0190`.** ~~THE `covers` PARENTHETICAL QUALIFIER IS PARSED INTO THE ID (vc, 3 instances fleet-wide). `AT-04.2`'s `covers` holds the literal `"AC-04.1 (render contract)"`, so referential integrity compares that whole string against criterion ids and finds nothing -- **and the AC it names IS a criterion**, at `ST0009/acceptance.md:47`. Take the id, keep the qualifier beside it.~~ **vc RULED THE HOME AND THEIR MIXED ROW IS THE WHOLE DESIGN: the note, KEYED to the criterion.** I proposed appending to the note and never asked whether a row can cover more than one criterion. `ST0009/AT-09.3` is `covers AC-09.3, AC-09.1 (render)` -- **a bare `render` attaches to the wrong criterion on a COIN FLIP, silently, with the note looking reasonable.** **AND THE DISPOSITION HERE IS THE ONLY EVIDENCE THAT WILL EVER EXIST**: vc's census declares `test` rows UNCOMPARED (it hashes the whole authored row, canon holds `.tests[].note`, no common bytes), so text moving between two fields of one row yields no ALTERED and no ADDED. **So the three verdicts now have genuinely different epistemic status and the enum says so: `dropped` is refutable by emptiness, `deferred` by non-emptiness, `refiled` BY NOTHING.**

9c. **HELD PENDING hv RE-RULE -- THE MEASUREMENT SAYS THE QUESTION WAS WRONG, NOT THE RULING.** Lamplight: 1788 WP rows, and **34 are not expressible as not-started/wip/done even after normalising case, `-` and `_`. `Cancelled` is 10 of them.** The rest are `TODO` 5, `PENDING` 4, `Superseded` 3, `Relocated`/`MOVED` 3, `proposed` 2, and eight singletons including `ABSORBED`, `PARTIAL`, `Dropped`, `CLOSED`. **Adding one variant fixes 29% and leaves 24 rows across 15 concepts that are not spelling variants of anything in the enum -- v2's WP status was FREE TEXT and an enum cannot grow to 29.** **AND IT IS NOT ONE VARIANT EVEN FOR THE 10**: `data-model.md:442` ratifies Machine 2 at three states, `transitions.rs:379` transcribes the rationale (_a WP that stops mattering is a scope change on the thread, not a state on the package_), and vc's own `data-model.md:383` forbids terminal states -- so it costs an enter verb, **a LEAVE verb**, a ratified table row, ic's surface rows and three transcribing tests. **THE RATIONALE WAS IN A COMMENT ABOVE THE EXACT LINE I WOULD HAVE EDITED and I did not connect it until I went to write the match arm** -- a ratified reason living in a transcription is found only by whoever next touches that transcription. My reading, vc's to carry: **`status_legacy`, mirroring `scope_legacy`, covers all 34, changes no machine and reverses nothing.** **CAUTION I DECLARED WITH THE NUMBERS: the entire tail is ONE ESTATE** -- Baize and Intent are clean at three spellings each -- so _the fleet needs 15 concepts_ and _Lamplight is unusually messy_ produce the identical table. ~~`WpStatus` GETS `Cancelled` -- hv RULED IT, and the ruling is shorter than my question was.** hv verbatim: _"Then *add* it. Why is that so hard? It's another value on the status enum for WPs?"_ Ten fleet work packages currently have no representable status. **I had been treating `the type is ratified` as a reason to escalate rather than a reason to ask** (vc's framing of their own version of the same habit); adding an enum variant reverses nothing. One variant plus its match arms plus the three transcribing tests.

10. **THE MIGRATION WRITES NO EVENTS AT ALL, so `threads.created` is CARRIED rather than DERIVED -- and the derivation was the whole point. RANKS SECOND; numbered last only to avoid renumbering a list I just had to recover.** ic measured the canary through the committed door: **56 of 56 threads have `created` populated, 0 `st.new` events, 0 events of any kind.** `acceptance.md:99` + `data-model.md:30` carry MY OWN proposal, ADOPTED because it satisfies hv's rule literally rather than by proxy: `threads.created` = the `ts` of that thread's `st.new`, DB-set via the `event_log.ts` DEFAULT, and **a v2-migrated thread has an authored `created:` and no `st.new`, so migration RESTORES an `st.new` carrying the authored date through the door `restore_event` already opens.** **The value is right and the PROVENANCE is absent** -- copied frontmatter, so not a time that went end-to-end through the DB where SQLite set it, which is exactly what AC-02.8(b) asks. **Not a bug in the restore path**: `restore_event_log` is reached only from `ingest` and reads an `events.jsonl` a v2 estate does not have, so it correctly restores nothing. **A specified step that was never built**, and the specified door (`restore_event`, NOT `append_event`) is already open carrying the D42 reasoning. **A migrated project starts with an audit trail that has no beginning.** **AND IT HELPS THE GATE**: restored events carry the authored date so they are DETERMINISTIC, unlike `created_at`/`updated_at` with their `strftime` defaults -- 56 rows comparing EQUAL across runs, the opposite kind of stamp, kept apart by D42's create/restore split.
11. **NOTHING TO DO -- WITHDRAWN ENTIRELY, and the withdrawal is the finding.** dc reported that `intent upgrade` leaves `intent.db-wal` + `intent.db-shm` behind, so `intent.db` alone would not be the whole store and any copy of an estate would silently lose frames. **Both halves are now retracted by dc, in two escalating steps, and `Facade::upgrade` leaves a SINGLE CLEAN DATABASE with no residue.** First the consequence fell: the `-wal` is ZERO BYTES, fully checkpointed, and a db-only copy reports `threads=56` identically to db+wal+shm. Then the PREMISE fell: **the sidecars were dc's own `sqlite3` queries** -- a fresh `intent upgrade` leaves `intent.db` alone, and one `SELECT` conjures both. **I added no checkpoint call, which is the only reason this cost nothing.** Three things to carry: **(1) dc's own rule, walked into twice on consecutive messages -- _a wrong MAXIMUM certifies catastrophe and nobody re-checks an instrument that has just found something big_;** **(2) a committed file was asserting the false version with the word `measured` in it, which is exactly the word that makes the next reader stop checking** -- corrected in the tree by NAMING the false claim rather than replacing it silently; **(3) ic caught it as a DISCREPANCY between two nodes measuring one estate -- their `.cache/` held one file where dc's held three -- so neither could have seen it alone.** That is the two-desks class arriving where none of us expected it: **the shared-worktree hazard has a mirror, where two nodes measuring separately is the only thing that CAN see the truth.**

**SEPARATION RULE WITH vc, AGREED BOTH WAYS: we do not review each other's side. The migrator meets the corpus and the result is the verdict.** My migrator enumerates what it PRODUCED; vc's conservation checks enumerate what the ESTATE CONTAINED. **Their side goes wrong by being too SMALL, mine by being too NARROW, and neither error can hide in the other's green.**

**THE CORPUS IS CAPTURED AND IT IS THE WHOLE FLEET, NOT THE CANARY** (vc, `8e8f0611`): canary 1077 files / lamplight 5613 / utilz 101 / baize 381, all verifying byte-exact against pins, **7172 files, 436 threads, 2140 WPs, 3124 ACs, 2075 ATs, 71 issues, 20106 prose sections.** Three things in it are load-bearing for me:

- **It is pinned at `42fb5269`, not HEAD, because that is the last revision where `intent/issues/` populates BOTH arms** (23 OPEN + 38 CLOSED). At HEAD OPEN is empty, so a corpus captured there gives my BLOCK path no input.
- **67 LIVE threads across the fleet, against the canary's 2 of 56.** The block arm had almost no possible input and its zero was unreadable: **a corpus that cannot reach an arm and one that reached it and found nothing produce the identical zero.**
- **vc's own instrument had a defect the fleet found on first capture and Intent's tree never could**: `git ls-tree` C-quotes non-ASCII paths, so two Lamplight filenames containing a curly quote were compared as a DISPLAY RENDERING against real filenames and reported as data loss. **My parser meets those same estates**, and four sessions measuring Intent's own tree would never have produced that filename. Also: 1852 WPs and 16380 sections is where anything quadratic or estate-in-memory shows, and **60% of fleet files sit under a v2 status bucket**, so the relocation finding is structural rather than a canary quirk. **Utilz declares 2.18.0**, so AC-10.1's refusal arm has a real subject.

---

**PARKED UNDER hv's MORATORIUM -- no new classes, AC rows or instruments until the hoist lands.**

6. **AT-05.7 / `inert_arg_check.sh` -- PUT DOWN, and it is a new INSTRUMENT, which is exactly what the moratorium names.** The `st show` DEFECT below is a one-line ship-path fix; the detector for the class waits. Told vc and ic.
7. **THE TWO PAIRED `kind` CONVERSIONS.** `Criterion.kind` folds into Machine 3 as a `(kind, state)` pair; `AcceptanceTest.kind` into the **`AcceptanceTest.status`** machine -- **each `kind` folds into the machine of the entity that OWNS it** (vc's correction to their own shorthand; transcribing it literally would have put a field in the wrong machine). **Flipping `kind` alone is schema-INVALID** (`model.rs:414-432`, held by `ac_kind_state_invariant.rs`), so the verb moves two fields in one act. ic has no notation for a multi-field atomic move, so the spelling needs them.
8. **AC-11.5 -- an artefact asserts its own provenance** (vc, `bdfaed6b`; dc's ask, measured: `intent` and `intentd` in this tree were built **42 hours apart from each other**). `build.rs` in each binary crate emitting `build-commit` (**FULL 40-char sha, dc's correction -- `publish` compares against `git rev-list -n 1 <tag>` by string equality, and a short sha puts a fuzzy compare exactly where the release's one correspondence check lives**), `build-dirty`, `schema-version`. **`dirty` is the STRONGER half, not a caveat on the sha** (dc): a commit field alone cannot separate _built from this commit_ from _built from a tree that agreed with this commit's record_. **NO VALUE IS EVER EMPTY -- `unknown`**, because `sed` on a blank value and on a missing key both yield nothing. **`build.rs` must not panic when git is absent** (a `cargo install` tarball has no `.git`). **Surface is `intent version --verbose`, NOT `intent --version --verbose`** -- see the watch-out; ic's call is a flag on an existing row, and the subcommand already refuses unknown flags at exit 1. **dc also refuses when the two binaries report DIFFERENT commits: a release is one tree or it is not a release**, so both must answer in one format from one shared source. **AND THE HAZARD THIS WORK SITS IN, dc's, measured on their own repair: a self-updating tool writes its record with the OUTGOING code.** `devbin upgrade` replaced `lib/install` -- the code that writes the manifest -- then recorded its work using the version it had just retired; **the second pass, the one that produced the CORRECT record, reported `0 updated, 27 unchanged, Already up to date`.** A `build.rs` writes the field describing the build it is part of, so it is in that position by construction: **the run whose output says nothing happened is the one that fixed it.**
9. **THE GUARDS-UNMET DRIVER IS DONE** (was issue 0051, then TODO 2). Committed `5ccac147`. Left open by it: **`Guard::NonTestOnly` on `ac.unsatisfy` was the one genuinely unwitnessed guard** and is now witnessed only here, so do not merge this test into another.
10. **`Table` GETS FOUR DERIVES, AND I TOOK IT FROM ic.** `Serialize` on `Table`, `Family`, `Invariant`, `Vocab`. ic measured the premise: 24 top-level keys, 10 read, 14 stable structural blocks -- nothing like `Target`'s 40-against-2. Six keys are silent AND unwitnessed; deleting `flag_dispositions` reports 95 flags undeclared, **and a wall of offending rows sends its reader to fix the DATA when the fault is one missing key.**
11. **`st show` DISCARDS ITS `file` POSITIONAL AND EXITS 0** -- was issue 0055, mine, `severity: high`. **`9a9c7799` DOES enumerate it (body line 50) with a behavioural measurement I did not have -- ZZ_DESIGN_MARKER seeded into `design.md`, `st show ST0001 design` printed `info.md`, marker count 0.** I claimed it did not, from vc's MESSAGE naming three of theirs, without reading the commit. Full text at `git show 9a9c7799^:intent/issues/OPEN/0055/...`; **vc is writing an AC row for it on the bounce and this row stays the live copy until they ping.** `intent st show ST0001 design` prints the four-line info summary where v2 prints `design.md`; `st show ST0001 nonsense` prints the same thing, also at exit 0. The row is `keep` / `as-observed` and declares four exit codes; v3 produces one. **The operator is told it succeeded and shown a different question's answer.** Found probing every dispatch slot declaring `values` -- `st show` is the one WIRED command in that population and it never reads the slot.
12. **AC-06.1's surface tail** (NOT the installer/canon block).
13. **WP-03 and WP-05 pass their gates and are still `WIP`** (vc measured). Whether the work is done is mine to say; the contract already says yes.

## FOR hv -- six decisions I will not take on my own, in the order they bite

1. **THE HOIST IS A TWO-ENDED MIGRATION AND ONLY ONE END IS BEING BUILT. dc's, and it is the sharpest thing anyone sent me today.** Measured on a sacrificial project: v3-in-a-v2-project REFUSES at exit 1; **v2-in-a-v3-project OPERATES AND WRITES at exit 0** -- `st new` lands seven files, `todo` rewrites `todo.md`, `doctor` prints its own version and exits 0, and the shipped gate commits it all. v2 KNOWS the project is from the future and says so in those words, at `upgrade`, **the one command that was already safe**, and at none of the ones that write. **So the refusal exists in the direction that costs a user nothing and is absent in the direction that destroys their work** -- md is a generated view under D01-as-reversed, so v2's seven files land where the db has no row and the next projection discards them. **AND YOUR STANDING RULE HAS AN UNNOTICED EXPIRY: "v3 is not on PATH" and "this repo is a v2 project" are the same rule stated twice, and the hoist falsifies the second.** At the instant `config.json` says 3.0.0, keeping v3 off PATH stops being the safety measure and becomes the hazard, with four sessions pointing a writing v2 at a v3 project. **This needs your sequencing call BEFORE the migrator runs on this repo, not after.** The v2-side fix is ~6 lines at one site using two helpers that already exist, and whether it clears the show-stopper bar is yours.
2. **`Related { id, note }` CANNOT HOLD 42% OF THE ROWS THE ESTATE ACTUALLY CONTAINS -- SAME CLASS AS `WpStatus` BELOW, AND IT REVERSES A PRIORITY I SET ON vc's ARGUMENT.** vc ruled `related: Vec::new()` a defect against the DECLARED model rather than an absent feature; I agreed and promoted it to #1 as the cheap half (model type exists, render slot exists, only the parse missing). **Then I read the authored bytes instead of the model.** 55 threads, **126 rows: 73 are `- ST0001: Title`, 12 are `- **ST0034** (title) -- prose`, and 41 are neither** -- including `- (none -- standalone change to the skill)` with NO ID, `- ST0034/ST0035 -- ...` with TWO IDS IN ONE ROW, and `- Related to Elixir development practices` which is prose and not a relation. **A row with no id has no `id`; a row with two ids is one entry or two and both choices INVENT structure the author did not write.** And `views.rs:282-291` renders `- {id}: {note}`, so parsing structurally would rewrite 53 authored rows into a form nobody wrote -- **converting vc's LOST-PROSE into ALTERED-PROSE on 55 threads, shipped as a conservation FIX.** That is the AT-note case exactly. **INTERIM NEEDS NO RULING: carry the block VERBATIM under `Thread.body`** -- 55 threads go LOST -> CONSERVED with zero fabrication and `related[]` stays empty and honest. Same rule as the preamble: carry it, do not classify it. **The ruling needed is whether the declared type widens, and it is a `data-model.md` question, so it is vc's before it is mine.**
3. **RULED BY hv -- `WpStatus` GETS `Cancelled`, just add it. Moved to TODO 9c.** Original framing kept below because the SHAPE of my error is the reusable part: I escalated a one-variant change on the grounds that the type was ratified, and hv's answer was that adding a value to an enum is not a reversal of anything. **The cost of asking was a fleet's worth of work packages with no representable status, waiting on a question that took one line to answer.** ~~`WpStatus` HAS NO `Cancelled`, so ten fleet work packages cannot be represented by ANY spelling** (vc measured across four captured estates). `ThreadStatus` has six variants including `Cancelled` and `Hold`; `WpStatus` has three. **No match arm can fix this** -- a parser that carries a cancelled WP into a model with nowhere to put it has done half a two-ended migration. It is a `data-model.md` question (vc's) plus a ratified machine (three tests transcribe `WpStatus`), so I am not adding the variant on my own reading. **The scanner's remedy text waits on this rather than being written twice.**
4. **THE `critic` EXIT CODES ARE INVERTED RELATIVE TO THE SHIPPED GATE, BOTH OF THEM** (dc found it; I verified both artefacts myself). `guide.rs:118` declares findings = **2** and every failure = **1**. `pre-commit.sh` branches `0` clean / `1` findings -> **BLOCK** / `*` invocation error -> **FAIL-OPEN**. So on the day `critic` is implemented as declared, **the gate silently stops blocking on real findings** while printing "fail-open", and every refusal blocks the commit described to the user as a code-quality finding. **Picking a code for the refusal cannot fix it: findings and refusals need OPPOSITE gate behaviour and v3 gives them the same code.** MY RULING, which I believe needs no hv because it is the hook's own declared contract: **`critic` exits 1 on findings, 2 when it cannot run.** What DOES need you and ic: that makes `guide.rs`'s INV-04 prose wrong, and INV-04 is declared in `surface/dispatch-table.json`. **Also note dc's AC-10.4 constrains this** -- if `.claude/**` is byte-identical across the migration, the post-hoist gate is still branching `0/1/*`, so a contract the consumer cannot be updated to match is not a contract.
5. **AC-11.3 PERMITS THE SHIPPED SURFACE EXACTLY ONE ENVIRONMENT VARIABLE AND v2's REPORTER CHAIN NEEDS THREE MORE.** v2 resolves who filed an issue from `INTENT_AUTHOR` / `AUTHOR` / `git config user.name` / `$USER`. I implemented all four; **the guard refused it on the first run and its own message says why it has to -- every machine here has those variables set, so a quiet addition would have failed nothing.** So `intent issues add` now resolves `git config user.name` only (a subprocess, not an environment read), and **a user who sets `INTENT_AUTHOR` is ignored until you rule.** The divergence is stated in the code, the commit and here rather than being silent. Adding the three needs a row in `ALLOWED`, which the guard says is yours.
6. **DISSOLVED BY hv -- post-process it in the DB, do not build carry semantics for it.** hv verbatim: _"Anything unanswerable doesn't make sense. If this is something we can just post-process in the db, then lets do that and not get too caught up in trying to fit a square peg into a round hole."_ **Under reversed D01 the DB is the SSOT, so post-processing truth is just editing truth** -- the whole question was an artefact of treating ingest as the only place a value can be corrected. Simplifies the issues scanner rather than complicating it. ~~DOES THE CARRY RULING REACH A CLOSED ISSUE? Your policy is about CLOSED THREADS. I had this as BLOCK-for-all pending you, and **I changed it, because dc measured that `OPEN/` is empty at HEAD -- so BLOCK-for-all shipped the block arm unexercised on the very estate it was built for.** It now routes through `Scan::record(closed, ...)`, the single place your thread ruling is already applied, so issues inherit it rather than getting a second parallel policy. **Reversible in one boolean.** At HEAD the choice has no population either way; all 40 issues parse clean.

## Operational -- carry into the next session

- **AC-04.6: `Issue.status` IS PAID (Machine 4). `Thread.acceptance` IS PAID TOO, as `Disposition::Immutable` (`2aa9f453`) -- TWO `Unbuilt` rows remain and both are TODO 1.** The row stays red until they land AND the walk agrees. **vc SHARPENED THE CLOSING CONDITION on my finding and it is stated by BEHAVIOUR, not test name** -- table matches ratified, every declared edge drives, every undeclared pair refuses -- because I had renamed the test the old condition named. **Three tests carry that property and no one of them carries it alone; do NOT merge them.**
- **AND THE POPULATION THAT CONDITION RANGES OVER WAS TWO THIRDS OF THE MACHINES UNTIL 2026-08-17.** `RATIFIED` in `mutation_completeness.rs` is now bound to `transitions::FIELDS`, so a machine cannot land in the code without reaching all three walks. Six State fields exist; `AcceptanceTest.status` and `WorkPackage.scope` are ratified IN PROSE by `data-model.md` ("it does not need a table because the graph is 'any value, one verb, any value'") and are held to that claim by `a_machine_ratified_in_prose_is_actually_trivial` -- **one verb, no from-restriction, reaching every state, and the failure message says WRITE THE TABLE rather than fix the code.** vc ruled that this checks the ratification rather than altering it (`0f4c7f27`, provisional pending hv). **`data-model.md:487` says FIVE and I measured six: correct at its own revision, staled by my own Machine 4 commit hours before I measured against it.**
- **`the_transition_table_transcribes_the_ratified_machines_edge_for_edge`** (was `the_implemented_graph_is_...`). It compares the declaration table to `RATIFIED_*` consts from data-model.md; **neither side is the implementation**, so it was green with Machine 4's edges declared and no code driving them. `RATIFIED_ISSUE` now holds Machine 4 to transcription like the other three.
- **ISSUE 0033 IS NOT MINE.** v3's `at_set` touches only `status`; the note is a reified field, so v3 cannot destroy it by construction. The destruction is v2's row-rewriting and v2 is FROZEN. **Keep committing before `intent at` while v2 is the tool in hand.**
- **v3 STAYS OFF PATH.** The door is publication, not migration. **0036 is the only publication hold left.**
- **PUSH TO `local` ONLY.** upstream frozen (hv, CI/CD budget). **CI is no longer the watcher for the Linux leg** -- a `set -e` or path-separator break that only shows on Linux has nothing checking it. That is the class that shipped v2.11.12 broken.

## Watch-outs -- grouped by MECHANISM

### MISATTRIBUTION HAS A DIRECTION THAT NEVER SELF-CORRECTS

- **dc CREDITED ME WITH A GUARD THEY WROTE, IN A MESSAGE WHOSE POINT WAS THAT MECHANISMS BEAT DISCIPLINES.** The prepush clone-and-build is theirs -- `.git/hooks/pre-push` is repo-local and delegates to `bin/int prepush`, which is their lane; they then verified it as their own commit `7acab9c8`, two days old, **with their own node tag in its subject line.** I checked with `head -12` before replying rather than after being pushed back, which is the scar already on this board (_a peer's message about an artefact is not the artefact_) finally being paid forward instead of re-earned.
- **THE DIRECTION IS THE FINDING, and it is dc's**: their standing rule is `git log -1 -- <path>` before naming an owner, earned twice, **both times misattributing a PEER's work. They had never once applied it to their own, because the rule has an unwritten exception -- you do not check whether YOU wrote something, since you assume you would remember.** Two days was enough to defeat that. **And the giving-away direction cannot self-correct: if someone claims your work you will say so; if they hand you theirs, the only person positioned to object is the one who already believes it is yours.** It survives indefinitely and goes into the record as a fact about who built what.
- **SO THE ACCURATE VERSION STANDS: my slowness to adopt the clean-extract habit is slowness, and is not covered by anything I shipped.** Three of my four measurement-contamination scars today are the shared-worktree class and the fix for it is dc's mechanism, not my habits.
- **What survives the correction and gets stronger** (dc's, better than the version they built it with): **a single-node two-ended commit is reachable by looking harder at your own diff, so discipline can reach it; a two-node one cannot be reached by any amount of care inside either session.** That is the argument for the guard being load-bearing rather than belt-and-braces.

### A PUBLISHED FACE IS WIDER THAN THE `///` RULE I ALREADY HAD

- **THE DDL STRING IS A PUBLISHED FACE, SO A `--` SQL COMMENT SHIPS EXACTLY LIKE A `///`.** I had "a `///` doc comment is shipped output" on this board and applied it correctly to `model.rs` -- then wrote `WP-10` and `D42` into a DDL comment block in the same commit. **D37's guard caught both, in both faces** (`intent schema` and `intent schema ddl.sql`), and named the remedy. The rule is not about the comment SYNTAX, it is about whether the string is in a face: `store.rs`'s `DDL` const, `model.rs`'s `///`, and anything schemars or async-graphql lifts. **Reasoning about our own ST/WP/D ids goes in a `//` beside the string, never inside it.**
- **A ONE-FIELD MODEL CHANGE MOVES ALL THREE FACE CONTRACTS AT ONCE, and that is the case that looks like a broken instrument.** `Issue.reporter` is one field and three faces -- a SQL column, a JSON property, an SDL field -- so `SCHEMA_DDL_VER`, `SCHEMA_SDL_VER` and `SCHEMA_JSON_VER` all bumped together for the first time. **The discrimination is still working and the same commit proves it**: the DDL comment block was rewritten at length and the DDL contract hash did not move a bit, because the hash is taken over the STRIPPED contract. Documentation moving while the contract holds still, in the same commit as the contract moving loudly.
- **`SCHEMA_VERSION` (the store) and `SCHEMA_*_VER` (the faces) are different counters and a field change moves both.** Two guards, two remedies: `store_schema_version.rs` wants a rung, `schema_versioning.rs` wants a version bump plus a re-pinned hash IN THE SAME COMMIT.

### A GREEN I DID NOT EARN, AND THE THREE WAYS I ALMOST TOOK ONE TODAY

- **I COMMITTED `7497398a` WITH A TEST RED IN ANOTHER CRATE AND REPORTED IT VERIFIED.** I verified it by DRIVING `intent ingest --from-md` on the canary and reading the output, which was the right check for the behaviour and no check at all for the estate. `ingest_command.rs` asserted the remedy line `nothing to fix` on a CARRIED finding, and that commit is what removed the remedy from carried findings. **My own board already said "commit at GREEN rather than at finished" and I read a binary's stdout instead of a suite's exit code.** The assertion was not wrong, it was asserting the mechanism the ruling retired -- so the fix is to state the requirement against the new mechanism and assert BOTH halves, since the header alone passes on a report that prints a remedy underneath it.
- **`cargo test --workspace` STOPS AT THE FIRST FAILING TEST BINARY, so its output is a SUBSET and reads like a whole.** The first run reported 15 legs and died at `no_intent_home`; `--no-fail-fast` found three failures in two more crates. **A run that aborted and a run that passed both end without saying which they were** -- capture cargo's own exit code, never a pipeline's, and use `--no-fail-fast` when the question is "what is red".
- **I CONTAMINATED MY OWN MEASUREMENT TWICE MORE AND KILLED BOTH RUNS.** A full suite launched, then I kept editing -- so the run measured a tree that no longer existed. Killing it is right and cheap; reporting it would have been a verdict describing no tree. **The rule that actually works is FREEZE THE TREE, RUN, COMMIT -- in that order, with no edits inside it**, because "I will remember which files moved" is the thing that fails. (ic then ran `cargo fmt -p intentsvcs` across my dirty `legacy.rs` mid-run and told me; I verified the semantics rather than assuming, which is the only reason that was cheap.)

### THE MEASUREMENT'S SUBJECT AND THE REPORT'S SUBJECT DIFFER, and the output cannot tell them apart

vc has this as seven variants in `parity.md` (`bee0a0dd`): tree, process, corpus, layer, population, spelling, declaration. Four nodes, one day. Mine below.

- **CORPUS.** **There are no `thread.json` files in this estate at all**, so my first measurement of multi-AT criteria ran over v3 canon and returned an EMPTY SET -- which I could have written up as "no multi-AT criteria exist" and been catastrophically wrong. **Zero rows and zero rows matching are the same output.**
- **PROCESS. A PIPELINE'S EXIT CODE NAMES THE PIPELINE.** `cargo test --workspace | grep -E ...` reported exit 0 while `mutation_completeness` was RED. Caught because the leg count looked wrong, not because I was careful. **Capture the subject's own status; never a pipe's.**
- **INSTANT.** `twice(root, args, subject, &thread_state(root))` evaluates the reader as an ARGUMENT, ie before the verb runs, so every case compared the no-op line to the state the entity was in beforehand. Eight of nine failed -- **and a green would have meant the verb had not moved anything.**
- **TREE. AN UNCOMMITTED TREE IN A SHARED CLONE WILL BE MEASURED AND READ AS SHIPPED BEHAVIOUR, INCLUDING BY hv's OWN SUITE.** Three instances today were all my tree; ic wrote my worktree into the register and retracted it, and vc cited `render.rs` line numbers that exist in no commit. **My fix is committing at GREEN rather than at "finished"; vc's is `git status --porcelain` before any measurement whose result gets written down. Both are real and neither substitutes for the other.**

### AN ARCHIVE ONLY EVER GROWS -- I DESTROYED 492 LINES OF MY OWN HISTORY AND IT TOOK A FULL SESSION TO NOTICE

**My fold before the compact WROTE `.history/20260817/inbox.vc.md` instead of appending to it**, so the one entry being archived replaced the twenty already there: **492 lines, 2026-08-16 19:54Z through 2026-08-17 14:45Z, gone in a commit whose subject said the board had been folded.** Recovered from `d2b70c69^`, re-merged in timestamp order, **both halves diffed rather than eyeballed** -- recovered 492 byte-identical to what was destroyed, the displacing 16:40Z entry byte-identical to what the fold wrote. `6560be27`.

- **EVERY HUMAN-VISIBLE SIGNAL READ CORRECT.** Live inbox `_(empty)_`; the archived entry correctly present in `.history/`; the board reading as folded; `ws hygiene` clean. **The only signal anywhere was `514 deletions against 75 insertions` in `git show --stat`, on a commit I had no reason to expect deletions in** -- and I only read it because a number looked wrong.
- **THE RULE IS THE DURABLE HALF AND IT IS BETTER THAN THE DIAGNOSIS (ic's sharpening): an archive only ever grows, so a diff that REMOVES lines from one is wrong regardless of what the commit was for.** Checkable by a machine, needs no knowledge of what the fold intended, cannot false-positive because archiving only ever adds. **Writing where you meant to append is a one-character difference with no visible symptom.**
- **AND IT HAPPENED AGAIN FOUR HOURS AFTER I WROTE THE RULE, TO MY OWN TODO LIST.** An edit replacing TODO item 1 removed **items 2-9** with it, and I committed the loss in `79c3defa` -- **under the WRONG MESSAGE FILE, so the subject duplicated `d2b70c69`'s and described a different change entirely.** Recovered verbatim from `d2b70c69`. **The duplicate subject is why nothing flagged it: a `git log` reader sees two identical subjects and infers a re-commit, and the one signal that would have shown it -- a deletion count -- is the same signal I had just written down and was not looking at.** The archive rule generalises: **any file that only accumulates (an archive, a TODO, a watch-out list) should refuse a diff that shortens it**, and the fix for the wrong-message half is that `-F <file>` silently accepts a stale message, so **the message file must be written in the same step as the commit, never reused.**
- **NOT amended, deliberately.** Force-pushing a rewrite into a repo four sessions are pulling from is worse than a wrong subject; the record is corrected FORWARD, which is this project's convention (ic did exactly this today with `f48ed6e9`).
- **Raised with hv (`18:34Z`) as a guard beside the clock guard, with the measurement attached rather than as a proposal** -- same class: a protocol invariant that fails silently and leaves a plausible-looking artefact. ic checked their own board by RUNNING `git log --stat -- ic/.history/` rather than reasoning about their fold script (insertions only, zero deletions) and backs it. **Not built -- it touches `lib/templates/hooks/`, which is a shipped-consumer surface and under hv's moratorium on new instruments.**

### A FILE WHOSE MERE PRESENCE CHANGES BEHAVIOUR IS INVISIBLE TO EVERY REVIEW THAT READS DIFFS -- dc's, and it cost me a 65-leg run

**`build.rs` at a package root is in cargo's build graph whether or not `Cargo.toml` names it, and whether or not git tracks it.** dc wrote one into `crates/intent-cli/` while my full suite was running, thinking of it as inert until wired. **There is no wiring step -- the file IS the wiring.** They found it and told me; I killed the run at 65 legs, verified no `build.rs` and no `build` key, `cargo clean -p intent-cli` (260354 files, 7.0GiB) and re-ran from cold.

- **THREE ORDINARY SAFETY INTUITIONS FAIL AT ONCE.** _It is untracked so it is not really there_ -- cargo reads the filesystem, not the index. _It is unwired so it is inert_ -- there is nothing to wire. _`git status` will show it_ -- it does, as `??`, **which is the line four sessions have trained themselves to skim past because it is normally someone's scratch file.** The one mechanism that would have surfaced it is the one whose output we all ignore.
- **IT GENERALISES PAST `build.rs`**: any file whose PRESENCE at a conventional path changes behaviour is invisible to review that reads diffs, and a diff is what all four of us read.
- **THE RUNNER CANNOT DETECT THIS AND THAT IS WHY THE BURDEN IS NOT THEIRS.** I had told dc I was running; that is a claim about my process and does nothing to their filesystem. **cargo does not report "your build graph gained a file mid-run", it quietly uses it.** dc's framing: _a peer saying "running now" is a lock nobody implemented_. Their fix -- write in the scratchpad, move in at the end -- makes the hazard unreachable rather than agreed, which is the only kind that survives.
- **WEAK EVIDENCE IS NOT EXONERATION.** `target/debug/build/` held no `intent-cli` script artefacts before the clean, consistent with the script never having run. **I re-ran anyway.** A green I would have to explain is not a green I can commit on, and the cost was ten minutes.

### A CHANGE INVISIBLE TO ITS OWN SUITE

- **A SWEEP WHOSE GREEN IS IDENTICAL EITHER SIDE OF IT HAS NOT BEEN MEASURED, IT HAS BEEN PERFORMED.** 485 passed before the nineteen arms changed and 485 after: no test asserted the old spelling and none asserted the new one. **Building the witness is what surfaced all three defects in that change** -- the two missed arms, `ac unsatisfy`'s refusal, and the `wp show` spelling.
- **"THE FIX IS FREE" AND "THE FIX IS UNWITNESSED" ARE THE SAME FACT READ TWO WAYS** (vc, on themselves). A correction that moves zero verdicts needs a fixture constructed for the shape where the two rules differ, or a later revert is silent.
- **ONE VALUE HAD FOUR SPELLINGS AND THE FOURTH DAMAGED A FILE RATHER THAN A LINE.** `at na` printed `na` (the subcommand name echoed as the movement phrase), `n-a` (the wire tag, via `AlreadyThere`), against v2's `n/a` -- and `views.rs` wrote the wire form into GENERATED `acceptance.md`, so the next projection would have rewritten 23 authored rows into a spelling v2's linter rejects at L1. **Reverting that fix left 72 legs green: unwitnessed until `view_determinism.rs` stood on it.** `green` and `red` are byte-identical to v2's tokens, so echoing the wrong source was correct twice and wrong once.
- **A DEFECT CAN MASK ITS OWN DETECTOR, AND THEN THE FIX AND THE TEST MUST LAND TOGETHER.** `ac_rescope`'s hand-written from-state check refused everything, which kept green a walk whose filter excluded a verb's target PER EDGE -- so a kind-dependent verb was being asked to refuse from its own OTHER target. 0053's defect is what kept the walk that should have caught 0053 green.
- **A TEST WHOSE NAME ASSERTS THE DEFECT IS THE PUREST FORM, BECAUSE ONLY THE BODY RUNS.** `reinstating_an_in_scope_criterion_is_refused` named the behaviour hv's ruling retired. Two more today: `a_no_op_scope_change_is_refused_rather_than_silently_accepted` said the opposite of its own (correct) body, and `a_test_backed_criterion_is_satisfied_only_by_a_green_test` required a criterion to STAY satisfied with one covering AT red. **A test that passes under both the defect and the correct implementation is not weak, it is unbuilt** -- assert both halves.
- **A STRUCTURAL ZERO AND AN UNVISITED SUBJECT PRODUCE THE SAME OUTPUT: NONE.** Declare the zero with its reason, require the declared-total subject to have been VISITED, and require every undeclared subject to contribute some. Both directions, or the declaration covers an absence.
- **A GUARD COMPARING A RENDERER TO THE FUNCTION THAT DEFINES ITS SPELLING IS SELF-CONSISTENCY WEARING A PARITY NAME** (vc, on my 0056 guard). It asserted `view.contains(display())`, so `display()` returning `n@a` passed; the only external anchor pinned one WRONG value. **The property is not that the view speaks the display vocabulary, it is that the view speaks the vocabulary a HUMAN AUTHORED** -- and the only witness for that is bytes neither side of the repo produced. **A transcription is not a corpus**: transcribe a spelling wrongly and the test enforces the wrong one just as confidently.
- **TWO SELF-CONSISTENT ENDS CAN DISAGREE ONLY ON A CASE NOBODY ROUND-TRIPPED.** `legacy.rs` parsed the AT note as a keyed `-- test:`; `views.rs` wrote it back unkeyed. Each end was defensible alone. Against vc's captured v2 corpus, **14 of 14 authored rows failed byte-equality -- 12 notes dropped outright, 2 de-keyed -- and `status`, the only field the issue was about, was the only one that survived.** Nothing in 73 legs was pinning it. **Assert the WHOLE LINE: a status comparison passes on a row whose covers or note was destroyed.**
- **A LOADER THAT REFUSES BEATS ONE THAT SKIPS, AND THE REFUSAL IS CHEAPEST WRITTEN FIRST.** Mine caught my own `..` depth miscount on its first run -- three levels from `crates/intentsvcs` is `native/`, not the repo root. **A skip would have printed a clean green over a test that had read nothing.**
- **DO THE CHECK BEFORE THE ACT THAT WOULD MAKE IT UNFALSIFIABLE -- `date -u` BEFORE YOU WRITE, NOT BEFORE YOU COMMIT** (vc, drawing this to the loader above, and the connection is the point). I wrote a board stamp of `15:03Z` estimated from how long an edit had felt, **in the same act as recording a lesson about fabricated scope**, and it survived until the clock read `15:02Z`. **The pre-commit guard would not have caught it**: it refuses a stamp in the FUTURE and cannot see an estimate that lands in the past, which is the failure the protocol names first. Reading the clock before committing rescued it by luck of ordering; reading it before writing removes the window. **The generalisation is that a guard positioned after the write can only catch the subset of fabrications that are also implausible.**
- **A TEST THAT PINS A DEFECT AS EXPECTED OUTPUT MAKES THE DEFECT PERMANENT.** `contains("status: wip")` was asserting that the reopen moved the package -- a correct subject -- and captured the wrong spelling on the way.

### AN ENUMERATION THAT HAS GONE SHORT CANNOT BE CHECKED AGAINST ITSELF

- **AN INSTRUMENT'S POPULATION IS THE THING NOBODY CHECKS, BECAUSE A POPULATION THAT HAS GONE SHORT REPORTS NOTHING AT ALL.** Two instances one hour apart in two lanes on 2026-08-17. Mine: I wrote a refusal FOR the two edges carrying `from: &[]` (`at.set`, `wp.rescope`) and populated it from `RATIFIED`, **which by design has no entry for the machines those two edges live in** -- so the refusal written for exactly those edges was structurally blind to exactly those edges. ic's: `nothing_reaches_the_surface_that_is_not_in_the_table` passes with root `-V` on the surface and undeclared, **because its population excludes root flags**. **Both instruments were RIGHT about the population they had.** Fix is an assertion that the population is the bigger one -- `machines > RATIFIED.len()` -- so shrinking it is a red rather than a quieter walk.
- **REQUIRING A REFUSAL IS A THRESHOLD; REQUIRING THE REFUSAL THAT CAUSE OWES IS THE MEASUREMENT** (vc, on my design, and I had published the weak version while writing the strong one -- **a design published wrong is the one that gets copied**). Measured: **three of eight guard mutants refuse with the WRONG variant and survive `is_err()`** -- a kind check deleted, and the from-state check refuses instead, so the test credits a guard that is gone. **Every guard has its own `FacadeError` variant because somebody went out of their way to keep the remedies apart; asserting refusal-ness discards exactly that distinction.**
- **ONE `(verb, guard)` PAIR IS NOT ONE FIXTURE, AND THE DECLARATION TABLE CANNOT TELL YOU THAT** (vc). A guard can be unmet several ways with a different refusal each; `transitions.rs` knows only that the edge is guarded. **So the declaration table is the honest source for WHICH pairs exist and the ERROR ENUM is the honest source for HOW MANY WAYS each is unmet**, and neither completes the table alone. Under a `(verb, guard)` key the blank-target mutant **is not driven at all -- unreachable, not surviving -- and coverage arithmetic counts it as covered.** Mode counts get declared with their reason, like `TOTAL_MACHINES`.
- **THE ESTATE ALREADY HELD THE OPPOSITE-DIRECTION ENUMERATOR AND I NEARLY BUILT A DUPLICATE.** `error_remedies::every_variant_is_provoked_or_declared_elsewhere` walks from the REFUSAL side and caught **four of five** guard mutants, because deleting a guard orphans its variant. **It cannot catch the fifth and the reason is exact: `ComputedSatisfaction` stays provoked at `ac.satisfy`, so deleting the identical check at `ac.unsatisfy` orphans nothing.** A variant-completeness walk is blind to the SECOND site of a guard whose variant is provoked at the first. **Declaration-side and refusal-side; neither is redundant and the ground they differ on is that one case.**
- **A SECOND WITNESS THAT REDS FOR AN UNRELATED REASON IS NOT A WITNESS** (vc). Read the CAUSE of a red before counting it as coverage -- my own `ac.unsatisfy` mutant reds as `NotSatisfied`, the mapped variant, from the wrong cause.

- **A LINE-ORIENTED SCAN MISSES A CALL BROKEN ACROSS LINES.** 0050 counted nineteen dropped sites; `ac descope` and `ac withdraw` split `open()?\n  .verb(..)`, so **the real count was twenty-one**. Found by driving every verb twice, which is the only method that could have found them.
- **`measured` BOUNDS WHETHER AN ACT HAPPENED AND NOTHING BOUNDS WHETHER THE ACT REACHED THE FIELD THE ROW THEN DESCRIBES** (ic). `issues add` claimed a success-path stdout from a four-probe matrix that never supplies a title; `critic` declared its findings report, ie the non-empty case, which is the one a green CI never hits.
- **A DECLARED GUARD ENFORCED BY HAND AT THE CALL SITE IS A DECORATIVE DECLARATION** -- and there were TWO. `Guard::GatePass` in two verbs (deleting it from the table changed nothing) and **`ac_unsatisfy`'s from-state check, which made the shared setter's self-loop test unreachable and refused a legal self-loop at exit 1.** Enforcing from the declaration also fixes an ORDERING nobody would have to remember. Filed as 0051.
- **THREE COUNTS OF ONE POPULATION, THREE ENUMERATORS, EACH SHORT OF THE NEXT.** `arg_values_note` says three slots declare `values`; my probe walked `entry.args` + `entry.flags` and said four; a recursive walk of the raw table text says FIVE. **None of the three was careless** -- ic's named slots in prose, mine named two container keys by hand, and the fifth (`config`'s `backup.schedule`) lives in a third. The population must be a walk, never a list of the places a thing is expected to be.
- **A TYPED MODEL IS STRUCTURALLY UNABLE TO REPORT A FIELD IT DROPS.** `dispatch::Flag` has no `values`, so a `values` array on a flag row deserialises into nothing and a sweep over `dispatch::table()` would report the population complete. **Read the raw text when the question is "what does the register declare".**
- **AN ENTITY LIST AND A VERB LIST ARE DIFFERENT AXES, AND HARDENING ONE READS AS HARDENING BOTH.** `mutation_completeness.rs` derived its VERB axis from the ratified tables after `Criterion` went missing from a loop -- an ENTITY omission -- and its comment then read as though both were done. Six State machines existed, four had transcriptions, and **AT-04.6's central clause was being discharged over two thirds of its population.**
- **A THRESHOLD RECORDS THAT SOMEONE ONCE COUNTED; AN EQUALITY BETWEEN TWO ENUMERATIONS RECORDS THAT THEY STILL AGREE** (mine, generalised by vc into `parity.md`) -- **and `independently derived` is NOT the load-bearing clause: they must fail in OPPOSITE directions.** Two by-name enumerators go short together and agree perfectly. One enumerates the known, the other the complement: `edgeless()` matches each variant by name and OMITS a new one until an arm is added, the check counts `!State` and INCLUDES one immediately, so a new variant breaks the equality from whichever side the author forgot. **`unbuilt_fields > 0` surviving 4 becoming 3 is the cleanest instance of the class -- the guard is not wrong, it is true, it was true before, and it will be true at 1.**
- **A NEW ENUM VARIANT IS ALMOST INVISIBLE TO THE COMPILER, so rustc is not the enumerator you think it is.** Adding `Disposition::Immutable` flagged ONE exhaustive match out of eleven sites touching the enum; every other site was `let-else`, `if-let` or `_ =>`, which SKIP the new variant in silence. **The move left `Thread.acceptance` out of four populations at 20 passed / 0 failed.** Where the compiler cannot ask, one population function that DOES match exhaustively is the only place the question gets asked once.
- **A MACHINE CAN BE CLOSED AND UNREACHABLE AT THE SAME TIME.** Closure is a question about EXITS, so no graph instrument could catch a verb the surface cannot reach -- `wp_rescope` satisfied the closure property and had no dispatch row. **The register cannot see a verb neither side declares** (0037's blind spot, one layer along).

### INFERRING A MECHANISM FROM A FIELD'S EXISTENCE

- **GOING TO THE SOURCE IS NOT THE SAME AS READING THE SOURCE** (ic). `grep` finds the field; only the reader finds what consumes it. Three instances in an afternoon: ic's `values` "builds the enum" (it does not); my `wp.rescope` comment premise "a value the caller supplies at creation" (false at HEAD, and stated as the premise of the fix it justified); my `issues add` test asserting `severity: null` on the architecture I wanted rather than the one that exists.
- **A PEER'S MESSAGE ABOUT AN ARTEFACT IS NOT THE ARTEFACT.** I wrote here that `9a9c7799` enumerated 0032/0033/0058 and not 0055. **It enumerates 0055 at body line 50, with a behavioural measurement I did not have.** vc's sentence was _three that were yours in substance are enumerated in the clearing commit_ -- every word true, with the whole scope carried by a four-word subordinate clause while the salient half says a list is enumerated in the commit. **vc claims the failure as theirs and the phrasing did invite it; the board claim is still mine, because I asserted a fact about an artefact I had not opened rather than reporting what a peer said.** One `git show` settled it and I ran it only after vc pushed back. **Keep the rule, not the filing: it is true whoever wrote the message badly, and it generalises go-to-the-source-is-not-reading-the-source.**
- **A TEST WRITTEN FROM THE FACADE'S VERB NAMES DOES NOT REACH THE BINARY.** `at_set` is one facade verb and THREE surface verbs (`green` / `red` / `na`). `at set` answers `unrecognized subcommand`.
- **ONE FACADE CALL REACHED TWO WAYS MUST GIVE ONE ANSWER.** `todo done` read the outcome and `st done` did not, so the WRAPPER was more honest than the thing it wrapped. That delegation is what made 0050 a defect rather than a style preference.

### Prose that is a build input, or a claim nothing checks

- **A `///` DOC COMMENT IS SHIPPED OUTPUT.** schemars lifts it into the JSON Schema face, async-graphql into the SDL, both COMMITTED and drift-checked. **Plain `//` for reasoning.**
- **A DOC COMMENT THAT AGREES WITH ITS OWN DEFECT IS HOW THE DEFECT SURVIVES REVIEW.** `satisfied_by_tests`'s comment was accurate about `.any` and false about the requirement, so auditing the function against its documentation returned agreement -- **and the test covering it asserted the same wrong rule.** Fixed at `b759101d`; the standing rule is that a doc comment states the REQUIREMENT and the code is what has to match it.
- **SCOPE GOES IN A DENOMINATOR, NOT AN ADJECTIVE** (vc's sharpening, and it is the fix for the whole class). A subset published as a set is invisible to review because every word is true -- **and the scope is usually PRESENT, just not load-bearing in the sentence.** So `state the scope` is not the remedy; we both stated it. **Put it where the reader cannot skip it:** `12 of 14` catches vc's 12, `three of twenty-one` catches the enumeration above, `22 on column-0 AT rows at <sha>` catches the 23. Three instances in one day, three authors, every one a true sentence. **AND THE REASON IT WORKS IS THE GENERAL RULE: PREFER THE FORM YOU CANNOT COMPLETE WITHOUT THE WHOLE SET IN HAND.** You cannot write `three of twenty-one` without having counted twenty-one, so the honest form is UNWRITEABLE from the subset -- and the subset is where all three of these came from. That generalises past counting and it is the same shape as equality-over-threshold: the construction is chosen because producing it requires the work, not because the author remembered to be careful. Canon in `parity.md` at `835bf848` with all three tabulated by failure mode.
- **A COUNT IN A COMMENT IS A DATED MEASUREMENT, AND THE DOCUMENT DEFINING A ROW GETS COUNTED AS AN INSTANCE OF THE ROW.** `23` was `22`: the 23rd `status: n/a` was the blockquoted GRAMMAR SPECIFICATION in `ST0056/acceptance.md`. It had reached two shipped code comments. **Write the claim that does not decay** -- every authored AT row spells it `n/a` and none spells `n-a` -- and if a number is needed, name its subject and revision. A spec is the densest source of well-formed examples in the estate, so it is where this class hides best.
- **A NAME CAN TEACH THE WRONG EDIT.** `data-model.md` calls `Thread.acceptance` an attribute of a thread AND `WorkPackage.scope` an attribute of the package -- and scope is a `State` that `wp rescope` moves freely. **`Disposition::Attribute` would have invited exactly one wrong edit: moving scope into it because the document appears to say so.** `Immutable` picks out the one row. **And cite a peer's live document by QUOTED PHRASE, never by line number** -- their file moves without anybody touching mine.
- **A TEST NAME IS A COVERAGE CLAIM** and reading the list is how it gets believed. **A ROW OR COMMENT CITING A LINE NUMBER IS A PROMISE THE LINE EXISTS**; `git blame` + `git show HEAD:` is the two-second version.
- **A SPELLING WHOSE ONLY COPY LIVES IN A TEST IS UNAVAILABLE TO PRODUCTION AND LOOKS COVERED.** `AcState`'s five state names lived in `mutation_completeness.rs::state_name_of`; `enum_str` cannot answer it because three variants carry payloads.

### Greps, shells and measurement

- **A GREPPABLE PROXY THAT CANNOT SEPARATE CODE FROM TEXT ABOUT CODE FAILS TOWARD THE CLEAN-LOOKING ANSWER.** `contains("rm ")` matching "form"; a `Completed` COLUMN HEADER read as a thread's status. **The fix is a better discriminator or an authored classification -- never an exemption -- and the discriminator needs its own two-sided canary.**
- **REDUNDANT WITNESSES: ONE STOPS SILENTLY AND THE OTHER MASKS IT.** Stubbing half my status-line classifier left all six tests green because a status renders in TWO places. Require each source separately.
- **THE SHELL'S ANSWER IS NOT THE ONE YOU THINK, and the Bash tool runs zsh 5.9.** `$?` after a pipe is the last command's. `grep -c` exits 1 on zero. **zsh does NOT word-split an unquoted `$c`** -- use `${=c}`, and quote `--include='*.rs'`. **`path` is tied to `PATH`.** **Prefer a script with a shebang for anything whose result you write down.**
- **`cd` PERSISTS BETWEEN Bash CALLS AND THIS PROJECT HAS TWO ROOTS.** Cargo runs from `native/rust`, the repo one level up. Absolute paths for reads. `INTENT_BLESS=1 cargo test -p intentsvcs --test schema_faces_drift` re-pins the faces.
- **`\b` IS A GNU EXTENSION AND BSD `sed` MATCHES NOTHING AND SAYS NOTHING** (vc, who was bitten by it fixing a bug their own guard had just caught). **The pattern does not fail -- it silently succeeds at matching ZERO**, which is a wrong-zero generator sitting inside every regex any of us writes on this platform. `design.md` already names BSD/GNU sed splits as one of the four reasons v2 was hostile, so it is a KNOWN class we walked into anyway.
- **A PARSER REPORTS ITS OWN LIMITS AS THE ESTATE'S DEFECTS. ABSENT IS NOT INVALID.**

### THREE GREEN INSTRUMENTS OVER ONE SUBJECT ARE ONE INSTRUMENT

- **CONSERVATION, DETERMINISM AND LIVENESS ARE THREE PROPERTIES, AND I VERIFIED TWO AND INFERRED THE THIRD.** `intent upgrade` exited 0 and left an estate where every verb but `info` refused -- reads AND writes -- while vc reported ALTERED 0 / ADDED 0, dc's same-end-state reported IDENTICAL, and my own door tests passed. **All three read FILES. None of them opened the estate with the tool that made it.** dc's framing, and it is the general form: **a pipeline of controls is unanimous when they all take their inputs from the same place, so the number of controls tells you nothing and the number of distinct SUBJECTS does.** Their version is `stage` and `publish` both reading the CHECKOUT and silent about the binaries.
- **AND IT IS A SEQUENCING FAULT IN MY LANE, NOT ONLY AN INSTRUMENT GAP.** I built the door, tested that it wrote the right bytes, and never once opened the thing it produced. **The remedy can never be "check harder", because no individual check was wrong** -- it has to be a check whose subject is the artefact.
- **A CONTROL THAT CANNOT MOVE IS NOT A CONTROL** (vc, taking away evidence I had offered as decisive). I reported `40 -> 40 and nothing else moves` as proof a rule change was not a widening. The unmoved sections carry placeholders that are NOT in the substitution set, so substituted-template IS raw-template for them and **their counts could not have moved.** It produced a confident 20 rather than an obviously empty result, which is exactly why it read as evidence.
- **A DETECTOR SHOWN ONLY POSITIVES HAS NOT BEEN TESTED.** 40 of 40 is a 100% hit rate; on a population with no negatives, "matches the substituted template" and "matches anything" are the same detector. **Measured: mutating the rule to drop by HEADING NAME reds ONLY the negative control** -- every other test in the file passes against it.

### THE OUTPUT CANNOT DISTINGUISH WHAT HAPPENED FROM WHAT DID NOT -- three times in one day, one shape

- **A CLASS DECLARED AND NEVER EMITTED IS INDISTINGUISHABLE FROM ONE THAT WORKS** (ic). **AN ARM THAT CANNOT BE REACHED AND ONE THAT RAN AND FOUND NOTHING PRODUCE THE SAME ZERO.** **AND A DECISION THAT LEAVES NO RECORD CANNOT BE TOLD FROM A DECISION THAT WAS NEVER MADE** (vc, on my own deferral, `4a4b05c4`).
- **In all three the reassuring reading is the DEFAULT one**, which is why none of them is caught by looking harder at the output.
- **vc's version is the sharpest and is the one to reuse: THEIR ZERO WAS UNREADABLE.** `DOUBLED-SECTION 20 -> 0` is produced identically by a migrator that deferred to the author and one that stopped generating the section entirely. **They separated the two only by going and reading which pointer survived -- which is not a verification, it is one person being careful, and it does not survive being handed to anyone else.**
- **The symmetry argument for the same fix is WEAK and I should not reach for it**: "a drop records, so a deferral should" invites the true answer that canon is unchanged so nothing was lost. **The argument that lands is about what a reader can tell apart, never about balance.**
- **`N matched of M declared`** (vc's, after their first cut matched 2 of 39 and still PRODUCED the class). Assert the denominator, not the presence.

### OUTPUT THAT ASSERTS MORE THAN THE CODE KNOWS -- twice in my text in one day, both found by ic

- **A FABRICATED CAUSE.** `already migrated ... a previous run of this command was interrupted`, printed on trees that completed cleanly. **Canon existing is evidence that canon exists**; interruption is one of at least three ways to get there. Worse than silence, because it sends the reader after an interruption that never happened -- and ic's gate prints the same line where the cause IS interruption, with nothing telling the two apart.
- **A FABRICATED OBLIGATION.** `the carry policy is for CLOSED threads; a live one is FIXED, not carried` -- a hygiene note phrased as a refusal. **A hygiene note describes a STATE; a refusal describes an OBLIGATION** (ic). At 66 instances on Baize it read as 66 refusals that had not fired: vc nearly filed a hole in AC-10.2's block arm, ic actually went and read the `Blocked` enum, the nine residue classes and the class gate to prove there was none. **N=2 in one evening, on the two readers most likely to read a residue report carefully, and it fired toward suspecting the migrator both times.**
- **The mechanism is identical and it is invisible because it reads as ordinary.** Same class as a fabricated timestamp: an invented claim is indistinguishable by inspection from a recorded one.

### Git in a four-node clone

- **`git commit --amend` REWRITES WHOEVER COMMITTED LAST, AND IN A FIVE-NODE CLONE THAT IS NOT RELIABLY YOU. I destroyed dc's commit `4cb9fe58` this way and the log looked perfectly ordinary afterwards.** dc committed on top of my `6bac18e4` in the gap between my commit and my amend, so my amend replaced THEIR commit with a new version of MINE. Their tree kept the change, which is the only reason this is a message and not a loss (re-committed as `69de6eb0`). **This is the OTHER DIRECTION of the rule I already had**: my `--amend` watch-out is about sweeping a peer's staged files IN, and says nothing about rewriting a peer's commit OUT. **The tell is two commits with an identical subject and a `reset` line in a reflog nobody reads.** The check is one command before any amend -- `git log --oneline -1` -- and **the answer changes without warning because none of us announces a commit.** dc's framing, kept: I was mid-recovery from my own earlier destruction when it happened, which is exactly the state where an amend is the natural reflex, **so the argument is for a mechanism and not for care.**
- **`--only` COMMITS WHAT YOU NAME, and a hand-typed pathspec is a roster that is wrong where its author was distracted.** Check `git status` in your own lane afterwards. **`--only` cannot reach an untracked file** -- `git add` it in the same command.
- **STAGE NOTHING UNTIL THE MOMENT YOU COMMIT.** `git stash` is unsafe here; sacrificial `git worktree` only for `bin/intent*`, **with its OWN target dir** or it contends for `target/`.
- **`git checkout -- <path>` REVERTS TO THE INDEX**, so with an uncommitted baseline the revert destroys what you were testing. `cp` to snapshot, `diff -q` both back.
- **A sha carries NO authorship here** -- `git log --format=%an` says `Matthew Sinclair` for all five of us. Do not attribute a finding from a commit; read the artefact.
- **NEVER PUT A `"` INSIDE A BOARD HEADER VALUE** -- measured trigger, cause unknown.

### A CLAIM ABOUT A BINARY IS NOT A CLAIM ABOUT A BEHAVIOUR

- **`intent --version --verbose` PRINTS THE VERSION AND EXITS 0, AND SO DOES `--version --zzz`** (ic's EXP-09, `bd5dc51e`; I re-measured all eight rows against my own binary from a bash file). **clap's `DisplayVersion` action fires during parse and exits before the unknown-argument check**, so a flag added there sits on a path clap never reaches. **`version --verbose` exits 1**, which is the way through: the subcommand is reached normally and already honours INV-02. **So INV-02 is UNMET on the command every user types first, while `version`.target.behaviour says it is refused -- true of the row, and the row is not what the user types.** Third instance after `info`'s catch-all slot and my `st show` positional.
- **A `///` DOC COMMENT RECORDING A COVERAGE CLAIM MUST NAME WHAT IT DID NOT ADD.** I measured my own new driver against the workspace with itself skipped and **4 of 5 guards were already witnessed** -- so it is a second witness four times and a hole closed once, which is materially weaker than the claim that was available. **A negative result about your own work is worth more than a positive one because nobody is checking it** (dc's phrasing).

- **IN A DUAL-IMPLEMENTATION ESTATE, "LANDED" NEEDS A SUBJECT.** I told vc `.all` had landed and AT-00.5 was theirs to move. True of v3; **the gate that SCORES this thread is v2, on PATH, frozen and unfixed by ruling.** vc verified the fix correctly, released the row, and the gate scored the false green the row exists to prevent. **During a rewrite the incumbent governs for as long as it is the one on PATH** -- and "v3 stays off PATH" was already on this board as a rule about publication, not read as a rule about what my verdicts mean.
- **"NOTHING RAN" AND "NOTHING FAILED" RENDER IDENTICALLY.** A mutation check run after a `cd` into a scratch project never invoked cargo, and `grep -E FAILED || echo "nothing red"` fired the reassuring branch because `grep -c` on an empty log is `0`. **The conclusion happened to be TRUE, so a broken instrument would have produced the right answer and taught me nothing.** Assert the run's own leg count before reading its result.
- **A `///` IS A BUILD INPUT, AND I BROKE THAT RULE HOURS AFTER WRITING IT ON THIS BOARD.** Reasoning in a doc comment on an enum variant drifted both committed schema faces and reddened three tests. schemars and async-graphql lift TYPE and VARIANT docs; method docs are not lifted. The drift check catching it in a minute is the system working.
- **A FIXTURE WHERE THE CORRECT AND INCORRECT IMPLEMENTATIONS AGREE CANNOT CARRY THE RULING** (ic, on their own fixture choice). `back in scope (unsatisfied)` is byte-identical to v2 on a NON-test criterion, so a declaration written there would assert as-observed behaviour under a `corrected` label. Same shape as the `green`/`red` coincidence, one layer up.

### THE ANSWER MAY ALREADY BE IN THE CODEBASE, AND REASONING TALKS OVER IT

- **BEFORE ROUTING AN AMBIGUITY TO A PEER, LOOK FOR THE RULE THAT ALREADY DECIDES IT.** 0032's ruling is ambiguous under AND about whether an `n-a` row blocks. I had drafted a refinement and was about to send it as an open question -- **and `lint`'s L5 had already ruled it**, refusing a non-test AT over a test-backed criterion because "a non-test AT is never green, so it can never satisfy it". My refinement would have been a second, weaker rule invented to avoid a conflict that did not exist.
- **A SEARCH FOR A FIXTURE CAN FIND A SUBJECT THAT ANSWERS A DIFFERENT QUESTION.** Looking for "a test-kind criterion with no covering AT" found a DESCOPED one, so `resolve` answered on scope and the arm handed a `Descoped` verdict to an assertion about the empty guard -- **and it would have been green with the guard absent.** Construct the case; do not search for it.

### Mechanics worth keeping

- **A surface built from a table cannot be unbuilt from the renderer**: the TABLE row moves first, the renderer second. **`values` on a non-subcommand slot is a DECLARATION and the facade enforces it** (`arg_values_note`, ic, `6d3e814e`).
- **SQLite refuses `ADD COLUMN` for a NOT NULL column with a non-constant default**, so any DB-stamped column means a table rebuild. **`IF NOT EXISTS` makes a schema change invisible until a query fails** -- any DDL change bumps `SCHEMA_VERSION`.
- **A fallback value can stand in for two different facts, and the second is the one nobody ruled on.** **A migration fixture must be a store that could actually have existed.** **An error swallowed in a fixture is a silent error** -- `expect()` always.

## Lane boundary

`dc` owns dev-x, build, CI, release, git workflow, install. **cc is services and app functionality**: intentsvcs, the facade, the model, ingest/views/store, **and the CLI's behaviour -- including wiring dispatch rows to the facade.** `surface/dispatch-table.json` is ic's; `acceptance.md` + `design.md` + `data-model.md` are vc's. **`bin/intent*` is cc's and FROZEN.** `bin/int` + `bin/.devbin/**` are dc's.

## Standing rulings

- **THE ISSUE TRACKER IS FOR EXTERNAL USERS AGAINST A RELEASED VERSION** (hv, 2026-08-17, verbatim: _an issue is an external user's report against a released version_ and _just clear the tracker, findings live in commits_). **Everything we find building v3 is work: fix it inline, put the reasoning in the commit against the diff, message the owner if it crosses a lane.** vc cleared all 21 rows at `9a9c7799` -- REMOVED not closed, because closing asserts they were resolved and all 21 carried `reporter: matts`. **My contribution to the pathology was 0060: I fixed it at `ffcc8764` with a comment citing the number and left it OPEN -- the code knew and the tracker did not.** Recoverable at `git show 9a9c7799^:...`; anything of mine that mattered is now a TODO row here.
- **`treeindex` and handover RETIRE.** A retired command is PRESENT AND REFUSING, not absent. **`fileindex` is NOT covered.**
- **`EdgeKind::Incidental` STAYS despite having no user** -- deleting it collapses `exits` into `leaves` and the trap check silently accepts technicality exits again.
- **`owner_wp` stays carried and unread.** **`doctor --fix` is WITHDRAWN, not deferred** (hv). **`install::MARKER` IS A SENTINEL, NOT A BOUNDARY**, so the rule library does not move under it.
- **`Outcome` is deliberately NOT `#[must_use]`** -- it fired on 65 sites, nearly all tests where ignoring it is right, and 65 `let _ =` is how an annotation stops carrying information. **It now carries the no-op state, so it is `Clone` and not `Copy`; read it with `.already()` rather than matching the variant at nineteen call sites.**
- **`ComputedSatisfaction` is NOT delegated to `Guard::NonTestOnly`.** A test-backed criterion is always `Computed`, so it can never be at `ac unsatisfy`'s target -- there is no self-loop for that guard to shadow, and the message is better than either alternative.
- **ANNOTATE, NEVER SUPPRESS, on a run verdict.** A guard that had downgraded my red would have hidden a genuine finding.
