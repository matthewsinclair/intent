---
node: cc
name: Control Claude
role: control
session_id: a674007a-9009-4ba1-80e9-3fbc159c18b0
commit_session_id: 01DC5cfhHTdPu8XB73gyKgJx -- READ off my own commits 441c2b8b and 8406af74, agreeing. HONEST CAVEAT: I authored those trailers, so reading them back is not independent of the harness. What it IS: the value a later reader attributes my commits by, confirmed present on them. POINT-IN-TIME, one session; a restart mints a new one.
heartbeat_at: 2026-09-03 14:57Z
status: active
focus: "BOUNCED THROUGH A COMPACT 2026-09-03 14:57Z; session_id UNCHANGED and status STAYS active -- invariant 6, a compact is not a session ending. Pickup re-ran and identity is CONFIRMED off my own board's session_id agreeing with the gate release, not assumed from the wake prompt. Inboxes: dc/hv/ic empty; vc still carries 17 entries from 08-30/31, UNCLEARED DELIBERATELY -- the precondition is per-entry and I have certified none of them. NOTHING OF MINE IS STARTED. PARKED BY hv AND HOLDING; nothing below starts on a peer's word. NO FIGURES HERE; RUN THE VERBS."
claims: [ST0056/06, ST0056/08, ST0056/10, ST0057/00]
---

# Control Claude (cc)

## DOING

**NOTHING IN FLIGHT. PARKED BY hv, HOLDING.** Nothing below is started and nothing starts on a peer's word.

**MINE, RULED, AND UNSTARTED, IN THIS ORDER** (settled with vc; goes to hv as MY recommendation with vc's agreement, never as a vc ruling over hv's park). A fold never archives an unexecuted ruling, which is why these are here and not in history. (1) **Dispositions 6-9** -- `0086`/`0063` and `0095`/`0096` DROP; `0192` RULED IN with the refusal in `info_read_back`; `0205`'s vendored fourth block ACCEPT with the reason at the site; the migrator-commit contradiction corrected in doc and row, **NOT** built as a commit. **PREMISE ALREADY MOVED: `0086` is CLOSED** (driven 2026-09-03) -- re-drive each before executing. **Disposition 5 is RETIRED**, its premise overtaken; what survives is the arm. (2) **`AC-10.5`'s residue re-disposition** -- ratified by vc; modelled in ST0069, not this release. **No longer chains behind `WP-14`.** (3) **THE ARM.**

**THE ARM, AND hv IS OFF ITS CRITICAL PATH.** The gate is whether an ingest FIRES, which needs the daemon RUNNING -- 284 of 299 logged ingests were disk-edit triggered, so an ordinary disk touch arms it. **SHAPE: a shell loop over a shipped verb at ~25ms (8+ writes inside ONE quiet period), fenced to a scratch entity, peers told, deliberate disk touch, then read back EVERY row. FALSIFIABLE: the render-queue model predicts MORE THAN ONE lost row at depth 8; if only the last goes, that model is wrong. SAFETY CONDITION, NOT OPTIONAL** -- this fires the loss mechanism on purpose on a live tree, so the read-back repair loop is armed or the experiment is indistinguishable from the defect. Wants a quiet tree and peers told; **not a window from hv.**

**`browse`'s DAEMON HALF IS MINE AND UNSTARTED (S-M).** `AC-17.6` requires `edit` and `browse` to reach ONE MODEL through ONE SERVICE; the page is daemon-served and the daemon is WP-08. **ic's row cannot green until it exists.** S-M rather than larger because `intentd` already has a web face (`web.rs` serves a shell, embeds the logo) -- an entity page and an open path, not a server. **Second defect at the same site: `browsed()` (`render.rs:1594`) refuses UNCONDITIONALLY and its message is FALSE** -- driven with `intent daemon status` answering, it still says _needs a running daemon and none is running_, sending the operator to fix what is not broken.

**0216 IS A LIVE LOSS CONDITION.** A canon write LANDS and `intentd`'s disk ingest REVERTS it, so the `ok` is honest and two actors race. Debounce 250ms (`watch.rs:51`), resets on every event. **Detail lives in the issue, not here.** THE FIX IS DAEMON-SIDE -- an ingest must not revert store state newer than the disk it read -- and it collides with `written_at`, which the ingest rewrites wholesale, so it needs a monotonic version the ingest does not own.

## TODO

- **THE CLI+MCP SURFACE AUDIT IS DELIVERED. LIVE RESULT, not narrative:** 138 declared / 128 shipping; **CLI 101 of 116 implemented, 15 unmet; MCP 60 of 60 with ZERO gap and ZERO intersection with the CLI gap.** Nine of the 15 are mine (WP-06: `ext` 5, `config` 3, `learn` 1). **TWO OF THE 15 ARE hv's RULING, NOT ANYONE'S HANDS** -- `config` bare is `target: undefined`, `agents` bare is `pending-hv`. Populations from the table's own `populations` block, which warns against unioning the arrays you happen to know about.
- **`implemented_check.sh` HAS A STRUCTURAL FALSE-POSITIVE CLASS AND I SHOULD FILE IT.** It classifies on the unwired MARKER ALONE, deliberately not on exit code. **`intent llm` / `llm guide` return rc=0 and print the marker as CONTENT**, because the guide documents exit code 2 by interpolating `UNWIRED_PHRASE` -- via the very import dc asked for, so the coupling is BY DESIGN and not a wording accident. Any row whose job is to document the surface defeats this probe. **Fix is a conjunction it does not use: marker AND rc=2.** Its 99/17 is really 101/15.
- **MY OPEN ACs: `AC-06.1`, `AC-06.8`, `AC-06.13`, `AC-10.5`.** `AC-06.1` is red on two limbs and neither is the ordering one -- the surface is not ported, and the coverage half waits on the `INTENT_BIN` flip AND a re-baseline. **The ordering limb is DISCHARGED, recorded on `AT-06.1`.** `AC-06.8` is not mine and moves on `0181`.
- **`coverage_map.sh` REFUSES TO PUBLISH, CORRECTLY** -- the burn TSV no longer covers the estate. Remedy is a `burn.sh` re-run, a double full-estate sweep that hung 3.5h once. **I DO NOT LAUNCH IT.**
- **WP-08 IS 12/12 GREEN AND `wp done` IS AVAILABLE TO ME (XS). NOT TAKEN.** Closing it marks the daemon DONE while its family carries zero conformance coverage. hv's call; vc endorsed the hold.
- **STILL hv's AND NOBODY ELSE'S:** the `burn.sh` re-run, the `INTENT_BIN` flip (three binaries, not two), `WP-08`'s close, the rustdoc DIRECTION question in its corrected form, and **whether `at green` should run the L3 arm** (my recommendation: warn, do not refuse -- refusing breaks the legitimate write-then-cite order).
- **THE `vc` INBOX HAS 17 ENTRIES FROM 08-30/31, PREDATING TWO FOLDS, AND I HAVE NOT CLEARED THEM.** The precondition is per-entry -- answered, actioned, or re-stated live -- and I cannot certify 17 without reading them. **Not cleared rather than quietly cleared.** `dc`'s two ARE discharged and are cleared this fold.
- **THE MCP DISPOSITION IS DECIDED, NOT DEFERRED.** SEVERITY EXPOSED (a classification over an enumerated domain); TITLE WITHHELD (authored text).

## Watch-outs

**A NUMBER REACHES hv WITH NOTHING DRIVEN UNDER IT** (ic's framing, taken over my own, 2026-09-03). I sized `browse` **XS** for hv's audit on the ground that it is the spelling of `edit --browser`, _which already ships_ -- and established _ships_ by reading `--help` and seeing the flag rendered. My first write-up called this _reading a rendered flag as a build_; **ic's is better and it is the keeper: `XS` was a claim about an ARM and `--help` was the only evidence under it, so the class is a SIZE PUBLISHED WITH NOTHING DRIVEN.** The test of which framing wins: **reading the RIGHT artefact would not have saved it.** **SIZING OFF `--help` AND SIZING OFF A GREEN GATE ARE THE SAME SHAPE.** **AND THE STING: I APPLIED THAT STANDARD FOUR HOURS EARLIER, TO A FIGURE** -- the `ac list` reconstruction went to vc with an explicit _second reader, not the gate's arithmetic_ caveat and was discharged by measurement. **I hold the bar for COUNTS and did not hold it for SIZES, same session, same board. A t-shirt is a claim about the world exactly as much as a count is.** RULE: `--help`, a clap subcommand and a declared row are statements about the PLAN; only an arm or a drive is evidence about the BUILD. `AC-17.6` already carried the neighbouring form -- _a declaration is a statement about the surface PLAN, never about the arm_.

**I ASSERTED WHAT AN INSTRUMENT COVERS WITHOUT OPENING IT, TWICE IN FORTY MINUTES, AND THE SECOND TIME MY OWN COMMIT REFUTED ME IN ITS PRE-COMMIT OUTPUT.** (a) _Every `include_str!` site sits inside the currency arm's population_ -- FALSE; `surface/` is at the REPO ROOT and `currency.lib:107` diffs `native/rust` alone. It rode out beside two DRIVEN grounds, indistinguishable from them to any reader. (b) _Nothing else covers this class_ -- FALSE; `shared_artefact_build_guard.sh:294` is **arm 6b**, positively controlled (finding NO outside embeds is a FAIL). **In both cases the disproof was in hand or one command away.** What survives, driven at source and routed to dc: **`source_commit.rs:233` derives the marker's identity over `DIRT_SCOPE` (3 paths) and currency diffs 1, so the two sides of the comparison do not answer the same question.** Three homes for _what is a build input_ -- `source_commit.rs:174` (3), `sharedtarget.lib:124` (3), `currency.lib:107` (1) -- odd one out is the only one on PATH, **direction inverted: an UNCOMMITTED `surface/` edit is caught, a COMMITTED one is not.**

**A GATE VERDICT READ FROM THE WORKING TREE IS NOT A PROPERTY OF A REVISION, AND NOTHING IN THE OUTPUT SAYS WHICH TREE IT READ** (vc's, 2026-09-03). For ~90 seconds while my `AT-17.1` fix sat uncommitted behind a peer's `index.lock`, the gate answered **118/142 to every node on this box** while git held the refusing bytes. **The shared-checkout hazard with the SIGN FLIPPED: our other entry says one node's in-progress state is another's OUTAGE, and an outage gets investigated -- this is one node's in-progress state being another's GREEN, and a green gets RECORDED.** vc had the readings composed for hv and caught it on `git status`. **RULE: before a gate figure goes anywhere durable, confirm the paths it read are COMMITTED.** Lock discipline held: WAIT, never remove, re-issue the SAME command. **And I over-reported the hold as _since 08:50 local_ -- I read an MTIME and called it an ELAPSED TIME.** It was about a minute.

**A CONTRACT FINDING PRE-EMPTS THE COUNT, SO CLOSING A ROW GREEN CAN COST THE ESTATE THE WHOLE GATE.** `ac gate` and `ac status` both refused on one AT row whose file omitted its own id -- mine, from `b67a9bc7`. **The check that catches it is a SHIPPED VERB naming the defect verbatim (`intent at lint <ST>`), and `at green` does not run it.** I set the row green and never drove the reader. **The aggregate refusing is NOT the figure being gone:** `ac list` does not route through the check and the figure reconstructs from it -- but whether that second reader's arithmetic EQUALS the gate's must ride with it. **It did, confirmed exactly, family by family, when the fix landed.**

**A NAME CENSUS ANSWERS A QUESTION ABOUT NAMES; PROMOTING IT TO A CLAIM ABOUT MECHANISMS NEEDS A SECOND STEP NOBODY PERFORMS -- READING THE SITE.** I filed `0214` saying a guard was never written; it had been there three days under another name with a non-vacuity control attached. **The grep was correct about the only question it was asked.** Same day: `fn|struct|enum <name>` cannot see an enum VARIANT, caught only because that name was a planted positive control -- **and the error then propagated into my SPLIT of the class, which is how a bad instrument reaches somebody else's sizing.**

**THE INSTRUMENT ANSWERED A DIFFERENT QUESTION, AND IT ARRIVES WHILE I AM BEING CAREFUL.** `grep -i FAILED` matches `0 failed`; a `Display` grep drops the `Debug` line holding the evidence; `pkill -f 'cargo test --workspace'` matches a pattern every session runs; an `awk` range never opens because the decl is bare `const`; `^  [0-9]+,$` misses rungs indented 4 and reports a tidy wrong total. **RULES: an EMPTY result is not a finding until the pattern is positive-controlled; assert a MEMBER you know is in the population, never just a plausible total; after any programmatic edit, grep for what should now be there.** The shape: the instrument is checked against what I MEANT, never against a sample of what it will SEE.

**A CONTROL THAT CANNOT DISTINGUISH _SAFE_ FROM _NEVER TRIED_ IS NOT A CONTROL, AND IT MUST VARY THE AXIS THE CHECK ACTUALLY READS.** I attributed 23 failures to _not mine_ because stashing my paths reproduced them -- but `git stash` REWRITES the file, mtime stayed fresh, and the guard keys on MTIME not content (`common/mod.rs:782`). **My arms differed only in the dimension the guard IGNORES.** Ask what the check READS before choosing what to vary.

**A STALE BINARY CANNOT ANSWER A QUESTION ABOUT HEAD, AND THE RUN THAT HAPPENS TO BE RIGHT IS NOT EVIDENCE.** Driven three times today across two rebuilds of the shared pair (`1c80a627` -> `3f037021` -> `6dac00f7`), figures identical each time. **The verdict surviving is not what makes the re-run necessary.** Re-drive the FINDING, not only the aggregate, when the rebuild touched the file the finding lives in.

**`cargo check --workspace --all-targets` -- AND THE FLAG IS THE HALF MEMORY DROPS.** A Rust enum makes an exhaustive `match` a DISTRIBUTED obligation reported from DOWNSTREAM, so **the node who breaks it is structurally least likely to see it.** **NOT a stricter `--workspace`, a DIFFERENT SUBJECT** (dc): the exhaustive matches live in TEST targets a plain check does not build. Land the variant, its const, its remedy arm and every exhaustive match in ONE edit.

**THE SHELL LIES ABOUT SUCCESS ON THIS BOX.** `cmd | head` reads head's status -- **done again today, reporting rc=0 for a command that exits 1.** zsh has no `PIPESTATUS`, does NOT word-split `$var`, and an unmatched glob ABORTS the command. `grep` is `ugrep`. `sed -i ''` SUCCEEDS on no match. `find` is bfs. **`cwd` PERSISTS BETWEEN CALLS** -- caught again today, where a `cd` two calls back made `ls bin/` report the project root as missing.

**THE SHARED CHECKOUT.** Canon cannot be split, so every canon commit is silently multi-node. **`add` + `commit --only` is the only safe write; a live `index.lock` is a WAIT, never a removal; never `--no-verify`; never unstage a peer's path. PROVENANCE IS CARRIED BY PATH.** A tool scoped to a crate acts on a population wider than the one you chose -- `cargo fmt -p intent-cli` swept dc's uncommitted files mid-edit and changed nothing of theirs by LUCK, not safety. Scope fmt to the paths you touched.

**TWO CORRECT RULES CAN TAKE THE MACHINE DOWN.** A release build DELETES the shared pair before building (`0196`) and the dirty-tree guard REDIRECTS to `target/private/`: the delete happened, the replace declined, `intent` was gone estate-wide for fifty minutes. **The guard prints the dangling symlinks BEFORE the delete, so the information exists and the ORDERING defeats it.** **The constraint this implies is a QUIET TREE, not a permission** (vc, taken 2026-09-02) -- `0196` is a DEFECT, not an authority rule, and _a rebuild window is hv's_ has EXPIRED.

**A PIN THAT TRAILS HEAD IS THE CORRECT STEADY STATE, NOT STALENESS** (dc) -- and **CURRENCY IS A PROPERTY, NEVER A VALUE.** The gate's `currency` arm is the decider; the bare pin/HEAD difference is not. **DIRT, NOT STALENESS, BLOCKS A SHARED REBUILD.** `DIRT_SCOPE` has two homes BY DESIGN and **arm 6 asserts CONTAINMENT and must NOT be tightened to equality** -- today they are EQUAL, so an equality check passes now and fails at the next legitimate widening.

**DRIVE A RULING'S PREMISE BEFORE YOU BUILD ON IT.** Of vc's four build rulings one day, THREE had false premises -- a guard that already existed, a test that had landed on time, a WP descoped three days earlier by a ruling vc had relayed themselves. **None was caught by the ruler; all three by opening the file first.** Same family: **AN OPEN ESCALATION AGES INTO AN ASSERTION** (vc) -- before anyone budgets against one, its AUTHOR re-drives it. **CONSTRUCTING BEATS READING, MEASURING BEATS BOTH, AND THE DISPROOF IS USUALLY ALREADY IN HAND.**

**ASSERT THE PREMISE YOUR FIX RESTS ON AS A TEST, IN THE DIRECTION THAT WOULD EMBARRASS YOU.** I built `0206`'s CAS on `threads.revision` having READ `rebuild` and `Door::Change` and reasoned correctly from both -- and never read the `DELETE` two lines above, so `rebuild` deletes before inserting, the conflict clause never fires, and the counter RESETS rather than rises. **A CAS on it would have FAILED OPEN on the case it was built for.** No care at the READING step reaches this; only an assertion does.

**A BASE RATE IS NOT A CONDITIONAL, AND THE BASE RATE WEARS THE SAFETY QUESTION'S CLOTHES.** Four measurements went to _how often does the risky shape occur_ and produced a reassuring 9-13% while three writes were being eaten. **The question that decides a mechanism is _given it occurs, what happens_** -- in the same table every time, and nearly deterministic.

**A CLAIM THAT SOUNDS LIKE PHYSICS GETS EXEMPTED FROM MEASUREMENT.** _A process spawn costs more than 250ms_ survived four assertions across three nodes because it reads as a fact about computers. It was **LLM TURN LATENCY WEARING AN OPERATING SYSTEM'S CLOTHES** -- the real figure is 23-26ms -- **and a stopwatch killed it in four seconds.** Reach for the stopwatch precisely when the claim sounds too basic to need one.

**THE REPORT IS THE ARTEFACT NOBODY REVIEWS.** (a) **A DISPLAY FILTER IS A CLAIM ABOUT THE POPULATION** -- an `if d<1800` in a print loop, never in the query, cut a complete sweep to 9 of 26 rows, and **the excluded population was exactly the one the census existed to find.** (b) **MAKE THE SUMMARY RECONSTRUCT FROM TWO DIRECTIONS.** A printed total hides a miscount; `16 + 10 = 26` and `20 + 6 = 26` stated together cannot. Both caught by a reader adding up categories, never by re-running anything. **Beside every verdict: POPULATION, FORMS (per-form counts, never one total), REACH (COVERS / DOES NOT / UNOWNED).**

**SPACING IS NOT A MITIGATION AND `one verb at a time` PROTECTS ONLY AGAINST YOUR OWN BURST.** The debouncer sees WRITES, NOT AUTHORS, so a peer writing two seconds before you puts YOUR write in the exposed position and **nothing in your transcript shows it.** **The mitigation that survives having no constant: READ THE TARGET BACK, COMPARE TO INTENT, RE-WRITE IF IT DIFFERS, REPEAT** -- a loop on the observable, never a wait on a duration. **AND A SURVIVAL IS NOT EVIDENCE THE DISCIPLINE WORKS WHEN THE MECHANISM WAS NOT ARMED** (ic).

**A CONSTRUCTED VARIABLE STOPS BEING LABELLED CONSTRUCTED THE MOMENT IT IS QUOTED -- INCLUDING BY ITS AUTHOR** (vc). `burst` carried three constructions in one evening -- a prose grouping, a 3-10s clustering window, and the daemon's 250ms coalescing -- and a number moved between them each time without its definition. **The label lives in the prose and the number travels alone.**

**A MESSAGE CORRECTING SOMEONE'S STATE IS ITSELF A STATE CLAIM.** dc re-drove vc's reading and in the same message told me my board carried a claim it never had. Read this before writing the next correction to a peer. **And a SILENT CORRECT DECLINE IS INDISTINGUISHABLE FROM AN ASSUMPTION** (dc) -- three nodes each DECLINING is correct; three each ASSUMING a fourth did is the accident, and only a recorded decline separates them.

**FACTS THAT LOOK LIKE OPINIONS.** `intent st attach` writes the store AND the canon extract and NEVER the disk file (`0082`); the daemon watches DISK. `intent ac show` is not a verb. `intent init --name` is not a flag. **`intent issues add`/`edit` write ONLY the issue's own extract** -- a narrow canon window. **`intent --version` names the commit the BINARY WAS BUILT FROM; `bin/devbin --version` names what the CHECKOUT IS AT NOW.** Do not diff them; ask the currency question.

## Decisions

- (2026-09-03, ic->cc) **THE CLASS IS THE UNDRIVEN NUMBER, NOT THE WRONG ARTEFACT.** Recorded above; ic's framing supersedes mine and carries their name.
- (2026-09-02, cc+vc) **`0216`'s FIX IS DAEMON-SIDE AND NOT USAGE DISCIPLINE.** A hazard reachable by an ordinary shell loop cannot be mitigated by how carefully nodes write, because the discipline would have to be _do not script this tool_.
- (2026-09-02, vc, relayed to all nodes) **THE READ-VERIFY-RETRY LOOP IS A REQUIREMENT, NOT GOOD PRACTICE.** It replaces `one verb at a time`. **The debouncer sees WRITES, NOT AUTHORS.**
- (2026-09-02, vc) **A RIDER CANNOT BE VIOLATED BY A CASE ITS OWN HAZARD CANNOT REACH.** `AC-06.1`'s ordering clause exists to stop a test being written to MATCH BUILT BEHAVIOUR; a test against an UNWIRED family cannot be doing that. D35 naming `intent config` as reader of five keys is a REQUIREMENT on config, not a DESIGN of it.
- (2026-09-02, vc) **`WP-14` AND ALL 12 OF ITS ACs WERE DESCOPED WHOLE TO ST0069** by hv on 2026-08-30. `AC-09.5`'s wip/boards half goes with it.
- (2026-09-02) **TWO MACHINE PROJECTIONS OF ONE VALUE MUST NOT DRIFT; A HUMAN RENDERING OF IT IS NOT A COPY AT ALL.** **Highlander asks whether two copies would drift into disagreeing about the same question** (vc's test, ratified).
- (2026-09-02) **CONTENT COMPARISON DOMINATES A VERSION COUNTER FOR A COMPARE-AND-SWAP.** **A dominance argument ends a design debate that competing assertions cannot.**
- (2026-09-02, vc) **AN EXCLUSION MUST BE VISIBLE WHEREVER THE BEHAVIOUR IS CLAIMED, NOT ONLY WHERE THE CHECK LIVES.** `0206` refuses concurrent WRITES and not DELETIONS; any row asserting the general form says _writes, not deletions_ in the same sentence.
- (2026-09-01) **hv: v3.0.1 IS FEATURE COMPLETE, THERE IS NO TAG WINDOW AND NO EXTERNAL CONSUMER, AND COST IS NOT A CONSTRAINT.** The SCARCITY REGISTER is retired AS A CLASS. **A recommendation resting on _there is not time_ must be re-derived on its merits or withdrawn.** The release-number question is CLOSED.
- (2026-09-01) **A REFUSAL THAT CANNOT SAY WHAT IT FOUND MAKES ITS OWN DEFECT UNDIAGNOSABLE.** Making two branches distinguishable came before editing the code under suspicion. **A REFUSAL THAT NAMES NO ARTEFACT SENDS THE OPERATOR TO GUESS** -- name the FILE and both VALUES. `browsed()` is the live instance.
- (2026-09-01) **A REMEDY INHERITS ITS BRANCH'S ASYMMETRY.** Confirm-before-refuse is SAFE on the lock and WRONG on the probe. **I concluded it was branch-independent and testing the claim is what stopped it shipping.**
- (2026-08-31) **A CRITERION IS NOT REWORDED TO WHAT THE CURRENT STATE SATISFIES** (vc). The ruled form must be HARDER.
- (2026-08-31) **A CLASS CHANGE WITHOUT ITS REASON IS A DELETION WEARING A NEW LABEL** (vc). **A DECLARED EXCLUSION CARRYING ITS REASON IS THE CURE; A SILENT DROP IS THE DENOMINATOR ATTACK.**
- (2026-08-31) **AN OWNERSHIP SPLIT IS A PURPOSE, NOT A BOUNDARY** (ic, correcting me). **Two hands in one file IS `0206` in miniature.**
- (2026-08-31, CORRECTED) **`close --note` STAYS RULED OUT ON A CONTINGENT FOOTING.** The keg has no `edit`; the ruling survives because v3.0.1 ships from the tree.
