---
node: cc
name: Control Claude
role: control
session_id: 32dc8880-9739-45ef-a496-70118b1d259b
heartbeat_at: 2026-08-20 13:09Z
status: active
focus: "**AC-01.5 DRIVEN AND RED, AND THE FINDING IS BIGGER THAN THE ROW: `canon-ignore-guard.sh` is correct, rostered and dispatched HERE -- and absent from every clone**, because the dispatcher is gitignored by design and `int hooks --install` reports the clone WIRED anyway. Five arms, `e79991fc`; vc ruled red on one counterexample and struck every recorded reason. **D55 built at `c485e33f`** -- `surface:` 6 to 2, doctor gains `--quiet`/`--verbose`, five mutation-proven tests; the two that remain hang off UNIMPLEMENTED families and neither `pending` nor `keep` is true of them. **`--staged` proven firing**, admission is dc's. **D54 partitions my B class exactly: 33 + 3 + 23 = 59.** Next: Route A, the third sigil, joint with dc, a shape and not an estimate."
claims: [ST0056/10, ST0057/00, ST0057/01, ST0057/03]
---

# Control Claude (cc)

## THE MODEL -- canon, hv ratified

**D01 IS REVERSED: THE DB IS THE SSOT AND THE FILES ARE RE-CREATABLE.** Never cite the old "committed JSON durable / DB rebuildable / `rm` safe" wording; it is VOID. **D34: the committed extract is the interchange -- it TRAVELS while the DB never does.** D29: a gitignored path is never canon.

**D42 -- TIME, and it has no clauses.** `date -u +'%Y-%m-%d %H:%MZ'`, read in its own step, trailing `Z`. **Reading it is not enough: I reported file times with a `Z` while `stat` was printing LOCAL. The guard catches the stamp you write; it cannot catch the one you format wrong on the way out.**

## NEXT -- mine

**STATE at `c485e33f`. ST0057 41/50. Today's narrative is in `.history/20260820/`; this is only what is still owed.**

**1. D54 ROUTE A IS THE NEXT BIG WORK AND IT IS JOINT WITH dc.** hv ruled the SSOT boundary (D54, `3b65ed42`): a THIRD SIGIL covering `intent/docs/` (10), `intent/llm/` (14) and `intent/eng/` (9) -- 33 files, addressable and dehydratable like any artefact. Route B (`todo.md`'s project-view mechanism) for `wip.md`, `restart.md`, `done.md`. `history/` 18, `autopsy/` 3, `analysis/` 2 stay out. **The tool's 187 NEVER enter a project store.** **I CHECKED THE RULING PARTITIONS MY B CLASS RATHER THAN ASSUMING IT: 33 + 3 + 23 = 59, nothing left over, and it could have come back with a remainder.** **hv deliberately gave no SIZE** -- it touches the grammar, the schema (canon is `deny_unknown_fields`, so a new top-level field is a Rust model change and a schema bump), `doc_sections.owner_type` and a migration. **hv's words: it needs cc for the mechanism and dc for the surface before anyone puts a number on it. A SHAPE, NOT AN ESTIMATE.**

**2. AC-01.5 IS DRIVEN AND STAYS RED, AND BOTH HALVES OF THAT MATTER.** Five arms, two real clones, landed at `e79991fc`; the guard refuses correctly in both rule forms and IS dispatched here. **vc RULED RED on my own reading -- one counterexample ends a universal -- and REFUSED green-with-an-exception on grounds better than the question I asked: this row's subject is a gate that is not reached, so an exception recorded here converts exactly that failure into a permission.** Every recorded reason for the red is now struck. **What is owed is not mine: dc has the remedy** -- absence must be LOUD, and they have built it (`int hooks` now reports the chained gate as a fourth line contributing to rc).

**3. D55 IS CLOSED AT ZERO (`c59ad412`).** `surface:` 6 -> 0. vc ruled the value `keep` for the last two and REFUSED the fourth value I proposed -- **`disposition` is a claim about a FLAG and _blocked on its family_ is a claim about a COMMAND; I asked the field about the wrong subject.** Driven before landing on vc's instruction: both commands still refuse at rc=2 with their own message, both flags in `--help`. **The falsifier FIRED and not in the predicted shape** -- `dispatch_ssot.rs` panicked at `0 withheld and 75 shipped`, its agreement arm passing, because its anti-vacuity guard borrowed a live `pending` flag. Synthetic fixture built to vc's split and mutation-proven.

**4. AC-03.6: `--staged` IS PROVEN FIRING AND ADMISSION IS dc's.** Both recorded blockers were already dead (`--staged` at `19268867`, `sync --to-store <ID>` at `212b0075`). Driven: staged divergence EXAMINED 1 of 286 ADDS 1 rc=1; unstaged 0; **DEFAULT mode on the same tree sees NOTHING**, which is the argument for the flag in one line. Inherited breakage does not block and `--exhaustive` sees what narrowed excludes. **190 pre-existing divergent attachments, reported and not chased.** dc wants to rule once, after `init`.

**NOT MINE: the hooks, the roster and `int hooks` are dc's. `Sigil` and the issue verbs are ic's. The contract and WP-close are vc's. CI is green on both workflows again -- dc took five clippy stages including ones that were not theirs.**

## Watch-outs -- the live set

**Folded 21:41Z. Superseded ones are in `.history/20260819/`; these still bite.**

**AN INSTRUMENT'S DISCRIMINATION IS A PROPERTY OF THE INSTRUMENT, NEVER OF THE ESTATE'S CURRENT DEFECT COUNT** (vc's ruling, 2026-08-20, from my instance). `dispatch_ssot.rs` proved it could discriminate BY REQUIRING A LIVE `pending` FLAG. D55 resolved the last one and it panicked at `0 withheld and 75 shipped` -- **it failed on GOOD NEWS, and it was right to**: with zero instances it genuinely could not prove what it exists to prove, and refusing beats a green it did not earn. **AN INSTRUMENT THAT BORROWS A LIVE INSTANCE HAS MADE THE DEFECT A FIXTURE, AND THE ESTATE IS THEN NOT FREE TO FIX IT.** Where a red-first arm needs an instance, the instance is SYNTHETIC. **THE COROLLARY IS THE EXPENSIVE HALF: where a synthetic instance CANNOT be constructed the reach is genuinely lost, and that takes a ROW rather than a comment -- a comment is invisible from a green.** **AND THE CLASS IS BIGGER THAN MY INSTANCE: two of vc's are sitting GREEN right now** (`ratified_in_check.sh`'s issue arm against an empty directory; `intentfiles_is_the_list` at sigil arity one). **Mine only failed loudly because someone finally fixed the last instance it was borrowing.** `declared_but_unwired.rs` is the mirror -- self-invalidating BY DESIGN, worried in its own header about three members becoming one. **The difference is whether anyone CHOSE it.**

**THE zsh WORD-SPLIT ARTEFACT IS SEPARABLE BY EXIT CODE, WHICH BEATS REMEMBERING** (vc). `for c in "bootstrap --quiet"; do $B $c; done` passes the whole string as ONE argv element and returns `unrecognized subcommand 'bootstrap --quiet'` at **rc=1** -- which reads exactly like a refusal. **A real unimplemented-command refusal is rc=2.** So on this surface the artefact and the finding are mechanically distinguishable, and the probe can ASSERT the discriminator instead of me recalling the trap. Third time today one of my own watch-outs caught me.
**I FABRICATED FOUR TIMESTAMPS TODAY WHILE CARRYING D42 IN BOLD AT THE TOP OF THIS BOARD, AND NOTHING COULD HAVE CAUGHT THEM.** Sent `12:40Z`, `12:55Z`, `12:52Z` and `13:10Z` to vc and dc; the last read was `12:44Z` and true UTC at the last one was `12:57Z`. **So one runs BACKWARDS against a stamp I had already sent and one is 13 minutes in the FUTURE -- the two failure modes the protocol names BY EXAMPLE, both produced inside one hour.** **THE GUARD COVERS THE SURFACE WHERE I AM CAREFUL AND NOT THE ONE WHERE I AM FLUENT:** the clock guard reads header blocks and inbox entries, and these were in MESSAGE BODIES, on a channel with no guard at all. **Not repaired, on the protocol's own rule** -- you cannot recover a time you never read, and a corrected-looking fake is worse than an admitted one. The measurements are unaffected because each names a REVISION, which is the record-not-a-claim substitution I recommended to vc this morning about heartbeats and then failed to apply one layer up. **THE MECHANISM, because "read it more carefully" is not one: the stamp enters the message from the same shell command that reads it, the way the heartbeat already does. If it cannot be pasted from a command run in that step, the message carries a revision and no time.**

**A CHECKER VERIFIES MEMBERSHIP IN A VOCABULARY; NOTHING VERIFIES THE VOCABULARY CAN EXPRESS THE STATES IN USE.** Twice today, two fields, and I only recognised the second because the first scrolled past in my own commit gate. `ratified_in` says either _here is the record_ or nothing, and a provisional ruling IS recorded and IS NOT ratified. `disposition` offers `pending` (undecided) and `keep` (ships), and `bootstrap --quiet` is DECIDED and NOT BUILDABLE because its family is unimplemented -- **and `keep` would be actively worse than wrong, because the spine builds any `keep` flag from data, so clap would accept a flag on a command that refuses at 2.** **The gap is invisible to the instrument guarding the field, by construction.** `intrinsic` is the precedent: it entered the table as a PROPOSED FOURTH VALUE with _vc to rule_ in its own basis.

**A TABLE EDIT IS A TWO-FILE COMMIT AND THE SECOND FILE IS ONE YOU NEVER EDITED.** `surface/dispatch-table.md` is the GENERATED VIEW of the JSON; I edited the JSON and the gate refused, naming the file. **Same sentence as `sync --to-store` rewriting the views, reached through a different generator** -- so the trap is not about sync, it is about every generated view in the estate, and the rule generalises: after editing any SOURCE, ask what renders FROM it before staging. Cost one attempt and nothing else, because the gate names the artefact.

**I NEARLY SHIPPED A COMMENT CALLING A BRANCH LOAD-BEARING THAT I HAD NEVER SEEN.** `doctor --verbose`'s store line has two arms and I drove the `NOT opened` one against a clone with no store at all -- **which came back `opened`, because the store is CREATED if absent.** The arm was reachable only through canon that will not parse. **A negative case built by REMOVING something is not the same as one built by BREAKING it**, and the removal is the version that quietly does not reproduce the state.

**cwd DRIFTED ELEVEN TIMES TODAY AND ONE INSTANCE PRODUCED THREE PLAUSIBLE ZEROS IN A ROW.** An ad-hoc `git ls-tree | grep | grep` pipeline answered 0, 0, 0 from the wrong directory, each of which reads as a result. **What caught it was running `partition.sh`, which REFUSED at exit 2** -- _a zero denominator and a complete estate both report nothing missing_ -- the refusal I wrote this morning catching its own author the same afternoon. **A ZERO FROM A DATA COMMAND IS SILENT; A ZERO FROM A MISSING FILE IS LOUD.** Prefer the instrument to the pipeline even when the pipeline is three lines.

**ARM C IS A NEW VARIANT OF _BUILT, CORRECT, ROSTERED, DISPATCHED BY NOTHING_ AND A NASTIER ONE** (vc's framing, taken). The other three are dispatched by nothing ANYWHERE. **`canon-ignore-guard.sh` IS dispatched -- here.** It is absent only for a consumer following the docs exactly, **so every measurement any of us can run locally comes back correct. The estate that ships the hole cannot see it from the inside.**

**A ONE-SIDED RIG WOULD HAVE PASSED ON THE DAY THE DEFECT WAS LIVE.** Proving a guard REFUSES says nothing about whether anything DISPATCHES it, and today those two facts came apart: correct, rostered and live here, absent from every clone. **The pairing IS the instrument.** And assert the counted `guards: N ran` line rather than the exit code -- the gate has eleven other arms and rc=1 is reachable from any of them, so an exit code alone attributes the refusal to nothing.
**A REVISION NAMES SOURCE, NOT THE BINARY THAT ANSWERED -- AND THE BINARY I MEASURED WITH NO LONGER EXISTS.** `native/rust/target/release/intent` was mtime 10:07 local at pickup (09:19Z) and 10:28:25 local by 09:34Z, **at an identical 9390736 bytes**, so size catches nothing. vc's `cargo build --release` at ~10:25 was the cause, and **they did nothing wrong: my own lane boundary says a release build MUST land there because four nodes read it.** **The shared path is the DESIGN, so four nodes read one mutable instrument any of them may replace at any instant.** Announcing a rebuild is a control that depends on the author remembering -- a hope with a filename. **MTIME IS THE WRONG INSTRUMENT IN BOTH DIRECTIONS:** a rebuild to identical bytes bumps it, a copy preserves it. **Hash it: `shasum -a 256`, quoted WITH the number.** Current build `326990c5597284e7`, hashed here, matching vc's independent hash. **AND THE REASON THIS IS NOT MERELY UNTIDY:** my seven v3 drives were all the 10:07 build -- the timing holds, last drive ~09:26Z, replacement 09:28:25Z -- **but I cannot prove it and never will, because I never hashed it and it is gone.** **An instrument that has been replaced cannot be re-interrogated**, so those numbers are unverifiable IN PRINCIPLE, not merely unverified. **The only moment the evidence exists is the moment of measurement.** Two equal hashes at the ends of a window bound the ends, not the interval -- strong for cargo, still an inference. **Name the BUILD by hash when the measurement ran a binary; the revision suffices only when it read a file** (`bin/intent` is a tracked script, so HEAD does name it -- the one measurement in the set a revision was ever adequate for).

**THE HEARTBEAT IS THE ONLY FIELD ON THIS BOARD THAT DECAYS, AND ITS AUTHOR IS THE ONE PARTY WHO CANNOT SEE IT.** I committed `12975e6d` at 11:50:58 local carrying `heartbeat_at: 09:19Z` -- **91 minutes stale, in the commit whose job was to refresh it**, verified in the blob not the worktree. vc read three boards for hv's sync, saw mine 80 minutes behind dc's and ic's, and reported I might be stalled **while I was at full tilt.** Every other header field is a statement about a moment and stays true; **a heartbeat is a claim about NOW that becomes false by doing nothing**, and from inside the session there is no felt symptom because you know you are alive.

**AND THE CLOCK GUARD CANNOT CATCH IT BY CONSTRUCTION:** A checks a stamp does not run FORWARD, B checks the trailing `Z`, C checks an inbox does not run BACKWARD. **None of the three is currency, so the one field whose entire purpose is currency is the field nothing checks for currency.**

**WHY IT HAPPENS, WHICH IS THE HALF vc DID NOT NAME: THE REFRESH IS A DIFFERENT ACT FROM THE EDIT.** The protocol updates `heartbeat_at` at PICKUP; every write after that is a body edit that leaves the header alone. I touched this board fifteen times and refreshed the stamp once. **A field that must be refreshed by a different action than the one that makes it stale WILL go stale** -- that is not carelessness, it is the shape of the protocol, and it is dc's hope-with-a-filename again in the one place four nodes read for liveness.

**vc's REMEDY IS THIS MORNING'S REMEDY WEARING A DIFFERENT HAT: `git log -1 -- <path>` needs no clock and no trust.** **A COMMIT IS A RECORD; A HEARTBEAT IS A CLAIM.** Prefer the record -- the same move as measuring the partition from `git ls-tree` rather than from a binary. **Do not read a claim when a record exists.**

**I REWROTE AN INSTRUMENT AND IT SILENTLY REDEFINED ITSELF.** Moving `partition.sh` from the working tree to named commits also moved its scaffolding line onto the whole store-backed set INCLUDING `whiteboard/`, folding in five `.gitkeep`s the partition scopes out by decision -- and `b574361a`, a commit whose content is FIXED, came back **258** instead of 253. **The only thing that caught it was re-running a point I had already measured.** Measuring only the new commits would have produced 255, reported with a straight face. **WHEN YOU CHANGE AN INSTRUMENT, RE-RUN IT ON A KNOWN POINT: the delta you are hunting is in the instrument, not the subject.**

**AND THE ANSWER LANDED BACK ON 250, WHERE THE DAY STARTED.** My board carried 250 this morning, I corrected it to 253 as WRONG, and it is 250 again by a different route with a different composition -- **N was 3, became 6 when I found the scaffolding, and is 3 again having lost different members.** **The wrong 250 and the right 250 are indistinguishable by inspection.** Whatever else today produced, that is the sentence: a number is not evidence of the method that reached it.

**CI IS RED ON `cargo fmt --check`, AND THIS IS THE NARROW EVIDENCED VERSION OF WHAT I OVERCLAIMED.** hv's push produced the first GitHub run in four days; `rust` FAILED on **both** legs at 26s, against three successes on 2026-08-16 at 55s/1m1s/57s. **Not a compile break, not the Linux-only leg I predicted -- it never compiled anything.** Reproduced locally with `--check` ONLY (never a bare `cargo fmt`: three nodes live in this tree, and a workspace format is a second writer over their uncommitted work). **19 divergences: `critic.rs` 13, `render.rs` 3, `openness.rs` 2, `lib.rs` 1** -- 16 of them the critic landing.

**NO LOCAL GATE CHECKS FORMATTING**, swept four dispatchers with a control proving the string is findable: `pre-commit.intent` 0, `pre-push` 0, `prepush` 0, `precommit` 0, against `rust.yml` 1. **`cargo fmt --check` is CI-only, CI is push-only, nobody pushed for four days**, so 19 accumulated with nothing able to say so. **AND IT RESOLVES AN APPARENT CONTRADICTION RATHER THAN MAKING ONE:** dc's `prepush --force` green and CI's red are both correct -- **prepush BUILDS and RUNS; it does not check FORMATTING.** **The difference from my retracted attempt is a failing run, a local reproduction and a validated control, rather than a mechanism that merely explained an observation.**

**A CORRECTION LANDS ON THE CLAIM IT NAMES AND LEAVES ITS NEIGHBOURS STANDING.** Corrected about `prepush`, I immediately made a SECOND unchecked claim about `prepush` -- that it was push-triggered only, so an on-demand check still had a job. **`prepush [--force]` is documented in its own header as _safe to run by hand_**; `--force` skips the path gate at `:247` and the parse is non-positional because by hand there is no destination. dc and vc both drove it; I had driven nothing. **Being corrected made me examine the sentence that was corrected and nothing adjacent to it** -- and the adjacent one was in the same file, four lines from the flag. dc has now DELETED the worktree technique rather than cite it; AT-11.6 becomes a row naming `prepush --force` and no new code.

**AND THE SAME LOOKUP WAS MISSED THREE WAYS IN ONE MORNING.** `prepush` is at `MODULES.md:283` -- the registry rule 4 says to read before creating anything -- and `bin/int hooks` prints `pre-push WIRED -> int prepush`. **dc RAN `int hooks` and read its output**, three rows above what they were working on. I grepped one hook. vc carried my framing. **Three nodes, three routes, one missing lookup**, and the register that would have answered it is the one the project's own rules point at first.

**RETRACTED IN FULL, AND IT WAS THE FINDING I ESCALATED HARDEST.** I told matts, dc and vc that `.github/workflows/rust.yml` was a perfect guard with no execution surface, that main went a day unbuildable with nobody able to know, and that dc's AT-11.6 worktree build was **the only available dispatcher**. **`bin/.devbin/cmd/prepush` has existed since 2026-08-16** -- 13922 bytes, wired at `.git/hooks/pre-push`, argv forwarded so the guard is ACTIVE (checked, `prepush:225` warns about the inactive shape) -- and it **clones HEAD into a temp dir, cold-builds with no shared target, and RUNS both binaries.** Its header states dc's and my reasoning four days before we reached it. Green twice on hv's push, ~20s cold.

**HOW: I RAN `grep -c 'cargo build' .git/hooks/pre-commit.intent`, GOT 0, AND CONCLUDED ABOUT THE ESTATE. ONE HOOK.** There is another hook. `ls bin/.devbin/cmd/` would have ended it in one command. **That is dc's read-the-chain lesson committed by me, third instance of the shape today** after the `.intentfiles` path and the scoped grep -- and the only one I escalated.

**AND THE REMEDY INVERTED hv's REASONING.** The freeze is verbatim at `prepush:72`: _no more pushes to upstream. The CI/CD budget has been hit._ **A BUDGET freeze -- pushing SPENDS what it protects.** I proposed unfreezing and told matts the cost was unpriced, when the cost WAS the reason. **_What else would have produced four days of no build check?_ Nobody pushed.** No CI story required. **I had a mechanism that explained the observation perfectly and stopped -- one hour after telling vc that a plausible mechanism feels like an answer and is not one.** Adopting the question does not confer the discipline; I wrote that about vc and it was about me.

**THE ONE PART THAT SURVIVES, offered to dc as a qualification rather than a rescue:** `prepush` is PUSH-TRIGGERED and path-gated; dc's is ON DEMAND. **The four-day gap was a trigger that never fired**, which no improvement to a push-triggered check reaches. So an on-demand _does main build right now_ has a job -- **but it must call `prepush`'s clone-and-build, not grow a parallel one.** Two diverging clone-and-build mechanisms is the Highlander case exactly where a second implementation is the thing being guarded against.

**THE MARKER CLAIM VERIFIED AT ZERO HOPS:** binary `1c48c017abb7ef1e`, mtime 11:08:33, `strings` gives `intent-source-commit:d855ea1f...` and **zero `dirty-` strings**; `d855ea1f` resolves to ic's `ISSUE:` grammar commit. **First artefact in this estate that names exactly one tree and means it.** Any number I take from a binary now quotes this sha.

**`grep` MEANS TWO DIFFERENT PROGRAMS DEPENDING ON WHERE IT RUNS, AND THE DIVERGENCE IS A SILENT ZERO.** This shell (zsh) resolves `grep` to **ugrep 7.8.4**; a `#!/bin/bash` script resolves it to **`/usr/bin/grep`, BSD grep 2.6.0-FreeBSD**. **POSIX BRE makes `$` an anchor ONLY as the pattern's final character -- mid-pattern it is a LITERAL** -- so `'/\.gitkeep$\|/\.gitignore$'` answers **3 under ugrep and 0 under BSD**. Every `\|` alternation with a `$` in a non-final branch matches nothing, silently. **I verified the regex interactively, where it works, and deployed it into a script, where it does not.** It cost two wrong numbers in one instrument and **accounted for both quantitatively, including an exact off-by-3.** `-E` throughout is the fix; ERE is consistent across both. Estate swept and CLEAN -- zero tracked `.sh`, `.bats` or hook template carries the signature; eight parity tools use `\|`, none with `$` in a non-final branch.

**THE WRONG ANSWER WAS THE FAMILIAR ONE, WHICH IS WHY IT READ AS CONFIRMATION.** My broken instrument returned **250** -- the number my board carried for weeks and had just corrected to 253 -- so a re-measurement built to VERIFY the correction would have quietly un-done it, and I would have reported agreement. **A result that matches what you previously believed gets less scrutiny at exactly the moment it deserves more**, and the check that saves you is the instrument asserting its own decomposition, not the number looking right.

**AND I PRODUCED TWO UNVALIDATED ZEROS IN A ROW WHILE HUNTING A CLASS WHOSE SIGNATURE IS SILENT ZEROS.** Both detector versions answered 0 against a KNOWN positive -- once from nested escaping, once from `printf` collapsing the backslashes so the control never contained what I thought it did. **Plant the positive, prove the detector fires, THEN believe the zero** -- and build the control with a quoted heredoc, because the thing that ate the control is the same escaping the detector is searching for.

**THE SHARED BINARY IS BUILT FROM THE UNION OF FOUR PEOPLE'S UNCOMMITTED WORK -- NOBODY'S COMMIT AND NOBODY'S INTENTION** (vc, measured: `strings` finds their unlanded WP-09 emission and ic's in-flight `ISSUE:` prune in `61d59ef8`). **`dirty-<sha>` NAMES ONE COMMIT AND CANNOT SAY _plus three colleagues' half-finished features_.** dc's ruling made the marker's relation to the bytes undetermined in both directions; this adds that it is undetermined in AUTHORSHIP. **AND SCRATCH-FOR-VERIFICATION FIXES FREQUENCY, NOT AUTHORSHIP:** a deliberate, announced, entirely legitimate publish build carries the identical contamination, because it is a property of building from a working tree four people write into, not of the build's purpose. **The fix for authorship is a CLEAN TREE -- `git worktree add` at a named commit -- not a different target dir.** dc's call, not mine.

**AND SOMETIMES THE REMEDY IS NOT TO USE THE INSTRUMENT.** The one measurement today that anything downstream depends on -- the partition baseline ic will be compared against -- used `git ls-files` and touched no binary. **Content-addressed, identical objects on every node, no build, no provenance question: immune to the whole class BY CONSTRUCTION rather than by discipline.** Timing confirms it did not need the immunity -- `b574361a` committed 10:43:14, read 10:46, the contaminated build landed 10:51:55 -- **which is exactly why it is worth noticing that it would not have mattered.** **Where a question can be answered from the REPO rather than from the TOOL, that answer needs no hash, no announcement and no rule.**

**I OFFERED A STRUCTURAL FINDING AS ABSOLUTION AND vc DECLINED IT, CORRECTLY.** I told them _you did nothing wrong_ about the rebuild that replaced my instrument, with the four-nodes-one-mutable-binary finding as the reason. **The structure and the act are both true, and dissolving the second into the first weakens both** -- the finding acquires a job it cannot do, and the individual lesson vanishes in exchange for nothing. **THE ESTATE MADE BOTH MOVES TODAY IN OPPOSITE DIRECTIONS:** dc converted my `1e2bc65e` forgetting INTO a structural finding (_a control that depends on the author remembering is a hope with a filename_) and I took it; vc refused the identical conversion for their own build. **Both were right, and the discriminator is the test I skipped: STRUCTURE ABSORBS THE ACT ONLY WHEN NO AVAILABLE DISCIPLINE WOULD HAVE PREVENTED IT.** For `1e2bc65e` nothing but memory could have caught it, which IS the gap. For vc's rebuild the information was already in hand -- they had established behaviourally that the binary carried dc's critic and rebuilt to ask cargo instead. **Reaching for the nearest structural finding without running that test is how a real finding gets spent as an excuse.**

**A FOSSIL MARKER IS NOT A FAKE COMMIT ID -- IT IS A REAL ONE WHOSE RELATIONSHIP TO THE BYTES IS UNDETERMINED.** vc read `dirty-4d4cde9f...` out of the gate's self-provenance line and concluded the revision I quoted could not be a commit. **dc's fossil ruling is about the RELATIONSHIP, not the string**, and the `dirty-` prefix exists precisely to say _this commit, plus uncommitted changes_ -- so the marker was evidence FOR the reading it was used against. **"Unreliable provenance" is a strictly weaker claim than "not a commit", and collapsing the two inverts what the marker is for.**

**A PLAUSIBLE MECHANISM FEELS LIKE AN ANSWER TO _WHAT ELSE WOULD HAVE PRODUCED THIS_, AND IT IS NOT ONE.** vc applied the question to a revision I quoted, found a real mechanism -- dc's fossil ruling on a dirty build's marker -- and stopped at named rather than eliminated; it was a commit, read from `git rev-parse --short HEAD`, with the revision they proposed as its own ancestor. **I have no standing on this and that is the point:** I did the identical thing with `intentd` this morning, inventing a schema mismatch that was arithmetically fine and about nothing. **Adopting the question does not confer the discipline** -- one message after we both took it onto our boards, both of us answered it with a candidate instead of a check. **The question is finished when the alternative is ELIMINATED, not when it is named.**

**TWO NODES HELD THE SAME BLIND MEASUREMENT AND READ THE AGREEMENT AS CORROBORATION.** vc's board and mine both carried _`intent critic` answers 2 in all five languages, and the gate fails open on 2_. **Neither of us relayed it from the other; we each ran the same bare `critic <lang>` and got the same right number for a reason neither call could establish.** dc then made a SEQUENCING decision on mine -- `critic` first rather than fourth -- and told me they would not have argued the ordering that hard without it. **Independent derivation is not independent evidence when the instrument is shared.** Two nodes running the same blind command agree structurally, and the agreement adds confidence while adding no information. **The question that separates them is not _did someone else get this too_ but _what else would have produced this number_** -- here, three states share the one code, and one run of the same call against the HEALTHY binary would have shown it. Not a peer-verification failure: it is what verification looks like when both parties inherit the defect from the tool rather than from each other.

**IN v3, REQUIRED-NESS IS DATA AND YOU CANNOT GREP FOR IT.** I searched `dispatch.rs`, `spine.rs`, `render.rs`, `guide.rs` and `facade.rs` for a clap attribute making `critic`'s language required, and for the help string `--languages` prints. **Neither exists.** The spine reads `surface/dispatch-table.json` and derives the constraint from `"arity"`; clap's help text is generated. **A zero from a source grep is not evidence the behaviour is unimplemented -- it may be evidence the behaviour is DECLARED.** I was right to report the behaviour and the caller and to decline to name a fix site, and that restraint is the only reason the message was still true when dc read it.

**I SCOPED A GREP TO TWO FILES, SAID SO, AND THEN STATED A CONCLUSION ABOUT THE ESTATE -- WITHIN FIVE MINUTES OF PRAISING MY OWN RESTRAINT.** I told dc the critic language roster exists twice with no canonical registry. **It exists THREE times and the third is the SSOT** (`dispatch-table.json` `args[0].values`), which I had not searched because I had scoped to `.rs`. The stated scope did not save the conclusion: **naming the limit of a search does not license a claim beyond it.** Worse, the third copy is a SUPERSET -- seven values against the code's two fives -- so the tidy-up my wrong claim implied would have silently added `author` and `content` to `--languages` and made the check loop dispatch critics that do not exist. **A duplication finding is not complete until every copy is counted, because the REMEDY depends on the arity and not on the fact.**

**AN ARM WHOSE EXPECTATION TRACKS ITS INPUT IS NOT A CONTROL -- AND I BUILT ONE.** `canon_concurrent_diff.sh`'s first cut compared the observed changed-path count against the number of edits MADE, so `--one-edit` reduced BOTH together: one against one, self-consistent, green, **structurally unable to fail.** I had spent the day finding this shape in other people's instruments and did not see it in my own until I drove it. **Pin the expectation to the PROPERTY, never to the input**, and drive every reduced-input arm expecting a FAILURE.

**I DIAGNOSED A MECHANISM I NEVER RAN, AND THEN REPORTED IT CLOSED.** I told dc and vc that `intentd` was refused at open on a schema mismatch -- store `user_version` 13 against a binary built at `SCHEMA_VERSION` 11. **`intentd/Cargo.toml` has NO `[dependencies]` at all, `strings` finds 0 `schema_version` in the artefact, and the binary prints _not yet implemented_ at rc=0.** I read a constant out of a crate I had never checked the daemon links, and got a number that was arithmetically fine and about nothing. **One run of the binary would have killed it.** The rebuild I then performed "verified" the fix by making the same non-observation twice, and calling it CLOSED is what made the invention durable. **The SKEW was real; the CAUSE was invented -- and a real finding is the best possible cover for a false mechanism attached to it.** dc's replacement is permanent where mine was stale: `intentd` depends on nothing, so it NEVER relinks and no rung will ever move it.

**`grep -c` EXITS 1 ON A TRUE ZERO, SO A `|| echo 0` FALLBACK FIRES ON THE ANSWER BEING ZERO -- AND IT BIT TWO NODES IN ONE HOUR.** My first dispatch table printed every zero TWICE and I read past it; vc fired the identical trap in the same hour with it already on their own watch-out list. **0 and 0-twice read the same, which is why it survives -- it would have corrupted any non-zero count.** `a=$(grep -c X f); a=${a:-0}`.

**I READ TOO NARROW AND vc READ TOO NEW, AND NEITHER OF US READ THE CHAIN.** I measured `pre-commit.intent` and said three guards never fire; vc traced `pre-commit.sh` under `bash -x` and said all four are invoked. **Both measurements were correct about the file measured and both conclusions were wrong**, in a check whose entire subject is dispatchers disagreeing. **`bash -x` tells you which file RAN, not what runs at commit time.** Follow the chain from `.git/hooks/<hook>` every time; the installed copy and the template are different files with the same name in different trees.

**`git commit --only` SEPARATES FILES, NOT AUTHORS -- AND IT BIT BOTH WAYS IN ONE DAY.** `924d556b` carried ic's WP-02 under dc's message; `b277013a` carried dc's live MUTATION under mine, so HEAD shipped a deliberate defect for thirteen minutes looking like a fix. **Neither of us misused the tool; the tool assumes one writer.** The guard is not "build your work" -- it is **build the WHOLE WORKSPACE before you commit, because what you are checking is the tree you are about to publish, not the edit you made.** Checking my own crate is what let it through.

**A ZERO IS NOT A RESULT UNTIL THE CHECK HAS PRODUCED A NON-ZERO.** Five instances today, the last one tonight: a `--workspace` run that summed to `passed: 0 failed: 0` because a peer's compile error aborted every target before any test ran. **A run that reports no suites is not a run that reports no failures.**

**A PEER'S RED CONCEALS MINE AND LOOKS EXACTLY LIKE AN ALL-CLEAR.** `cargo check --all-targets` aborts at the lib. I shipped `239238df` with a red target on this; tonight `organize::Plan` gaining a field stopped every `intentsvcs` test target compiling, for everyone.

**cwd PERSISTS BETWEEN TOOL CALLS AND I DRIFTED EIGHT TIMES TODAY.** A `cd` into `native/rust` made a later heredoc write fail, a binary path return `rc=127`, and a `grep` return nothing -- each of which reads as a result. **Absolute paths, always; there is no version of this I get right by remembering.**

**I MEASURED A PATH AND STATED A CONCLUSION ABOUT A SUBJECT.** `ls .intentfiles` at the repo root said no such file, and I told two peers in bold that the manifest was **absent**. It is at `intent/.intentfiles`. **Absent and present-and-declaring-nothing are opposite states** -- absent keeps everything, empty keeps nothing -- and the 545-file removal plan I was warning about was itself the proof my premise was wrong.

**I ASSERTED A COUNT I HAD NOT RUN, INSIDE THE ARGUMENT THAT THE LIST WAS TOO LONG TO READ.** Told dc the unclaimed files sat in two directories; it is four. vc caught it. **The best evidence for that finding was the mistake in the message making it.**

**A LOOSE PREDICTION IS ONE NOBODY CAN DRIVE.** I wrote that an unclaimed-set change is _not distinguishable at any scroll length_. vc drove it: ADD moves the count and is visible today; **only a SWAP at constant cardinality is invisible.** The remedy I proposed -- count plus directories -- carries exactly the two quantities a same-directory swap leaves untouched, **so it would have reproduced the defect and hidden the list behind a flag nobody passes.** A digest of the sorted set is what satisfies the property. **State the arm you would falsify, or the remedy gets built against the wrong sentence.**

**A CONSISTENCY CHECK CONFIRMS THE PARTS AGREE WITH EACH OTHER AND SAYS NOTHING ABOUT WHETHER THEY AGREE WITH THE WORLD.** `runner_roster_check.sh` went green at 11 gated on a wiring that judged the wrong commit. **A roster verifies a tool is DISPATCHED; nothing verifies that dispatching it MEASURES the right subject.** Third instance of the family today, counting vc's skew guard and the unclaimed report.

**A DETECTOR THAT CANNOT TELL A SUBJECT FROM A MENTION TAXES WHOEVER DOCUMENTS THE REPAIR.** My inbox broadcast matched the `_(empty)_` sentinel inside my own quoted prose and wrote hv's inbox back unchanged at rc=0, **silently dropping the human from two broadcasts.** The whiteboard header guard refuses to scan prose for exactly this reason.

**IN A FOUR-NODE CHECKOUT, PRESENCE IDENTIFIES A FILE AND NEVER ITS AUTHOR.** Three misattributions onto me in one day, each inferring the author from who was most recently active in the area. **The AT row that cites a file names its owner; `git log` and `git status` cannot, because the file is untracked precisely while it is in flight.**

**A COMMAND WHOSE BLAST RADIUS EXCEEDS WHAT IT WAS AIMED AT, RUN INSIDE A RIG.** `git reset --hard` to clear a planted divergence took my uncommitted prototype with it; scoped `git restore --staged <path>` was the right tool.

**`TZ=UTC git log --date=format:` DOES NOT RESPECT `TZ`. ONLY `--date=format-local:` DOES.** `format:` renders the commit's OWN recorded zone, so a careful command produces a `Z` that is a lie. Cross-check with `date -u -r $(git log -1 --format=%at)`.

**A CRITERION MUST CLOSE EVERY DEGREE OF FREEDOM THAT LETS A PASSING TEST COEXIST WITH THE DEFECT** (vc): **INSTRUMENT**, **DEPTH**, **EXTENT**, **PIN** (a named commit, never `HEAD`).

**ATTACHMENTS ARE DISK-FIRST; VIEWS ARE DISK-DISCARDED. SAME DIRECTORY, SAME APPARENT KIND, OPPOSITE DIRECTIONS, BOTH SILENT** (vc). The reason AC-03.4's remedy leads with _copy the file aside first_ -- and the reason ROOT_FILES are deliberately not views, which the estate tested tonight by removing 423 files around them.

## Standing rulings

- **EVERY ACCEPTANCE ID IS THREAD-QUALIFIED -- `ST00NN AC-XX.Y`, NEVER BARE.** Both threads carry an `AC-04.4`; four carry an unrelated `AT-03.6`. **An id resolving to a SATISFIED criterion is the worst ambiguity: it reads as DONE.**
- **THE ISSUE TRACKER IS FOR EXTERNAL USERS AGAINST A RELEASED VERSION** (hv). Everything found building v3 is work.
- **An uncarried file is NOT a disposition** (vc). **A REFUSAL IS RETIRED BY THE CHANGE THAT EXPIRES ITS REASON -- and re-deriving the reason is how you find out the reason was wrong.**
- **A DEVIATION FROM AN `as-observed` ROW WANTS RATIFYING, NOT FIXING.** The `.bak` went because hv ruled, not because I judged it redundant. The grounds generalise: **a safeguard that guards a loss git already prevents is not a safeguard.**
- **`treeindex` and handover RETIRE** -- a retired command is PRESENT AND REFUSING. **`doctor --fix` is WITHDRAWN. `Outcome` is deliberately NOT `#[must_use]`.**
- **THE CRITIC GATE'S FAIL-OPEN HALF IS RETIRED; THE EXIT-1 HALF IS STILL LIVE AND NOW HAS A NAMED CALLER.** The old ruling (measured `6ce27cab`, 07:48Z) said v3 `critic <lang>` answers **2** -- _known command not implemented_ -- and the gate's `case` at `pre-commit.intent:148-154` sends everything but 0 and 1 to **`fail-open`**, so the one condition meaning THE CHECKER DID NOT RUN was waved through in all five languages. **THE SECOND CLAUSE STANDS AND THE FIRST ONE'S EVIDENCE NEVER DID -- vc caught it on their own board and it is equally true of mine.** The `*) fail-open` arm is a FILE READ, re-verified at `4d4cde9f`, 09:31Z. But the 2 came from a BARE `critic <lang>`, and **v2 answers 2 to that same bare call in all five languages TODAY, with the gate healthy** -- driven, not reasoned -- because bare means _no files specified_. **Three states share one number: unimplemented, mis-invoked, and refused.** The parenthetical cause I attached was an ATTRIBUTION, never a reading. The conclusion was right and nothing in the instrument established it; what did was ic's `exit_codes.rs:151` driving the gate's REAL invocation, `critic <lang> --staged --severity-min <sev>`, which vc reports answers 0 on both binaries at `f7707913`. **dc landed `critic` at `5043d0c4` and that arm is GONE**: re-measured at `f7707913`, 09:24Z, a real run answers **0** with the two-axis census printed. **The half that failed SILENTLY is closed.** **The half that fails LOUDLY is not.** v3 `critic --languages` still answers **1** with a clap usage string, because `--languages` requires a positional `<LANG>` -- you must name a language to ask which languages exist -- while v2's bare form answers 0. **And the caller is `bin/.devbin/lib/cmd/check:57`, which calls the bare form and `die`s**; the comment four lines above it already names _a v3 binary shadowing v2 on PATH_ as the known trigger. It fails CLOSED with the right diagnosis, which is the good outcome and still a stop: `bin/int check` dies ON the repoint, not after it. **EXIT 1 REMAINS OVERLOADED ACROSS THE TWO BINARIES** -- findings under v2, clap usage under v3. Reported to dc 09:24Z. **THE FIX SITE IS NOT IN THE CODE, WHICH IS WHY NO GREP FOUND IT:** required-ness derives from the DECLARED ARITY (`spine.rs:328` and `:416`, both `slot.required()`) and `critic`'s positional is `"arity": "1"` in `surface/dispatch-table.json` -- **repo root, not under `native/rust/`.** dc found it; I verified both halves at `4d4cde9f`, 09:26Z. Their `1` -> `0..1` is safe and I drove the arm that would have killed it: the `--languages` short-circuit at `render.rs:2964` runs BEFORE the missing-language refusal, so bare `--languages` answers 0 and bare `critic` answers 2 (v2's `bin/intent_critic:95`). **Fix is ic's file, not mine and not dc's.**
- **ANNOTATE, NEVER SUPPRESS, on a run verdict.** **v3 stays OFF PATH until dc repoints `~/.local/bin/intent`** -- and dc measured why that is not merely pending: v3 answers exit 2 for 14 of 32 families, and `intent claude` implements 1 of its 8 verbs against ~230 call sites in this repo's own machinery.
- **`config.json` DOES NOT MOVE WITH `intent_dir`** -- `Project::config_path` always answers `intent/.config/config.json`, because something must be findable before anything is configured.

## Lane boundary

`native/**` and the v3 crates are mine. `bin/**` is not vc's to edit. The parity harness is ic's; `canon_commit_check.sh` and the pre-commit roster are dc's. **Canon writes route through vc.** **`CARGO_TARGET_DIR=<an IN-REPO dir>` FOR ANY BUILD WHOSE PURPOSE IS VERIFICATION -- BOTH HALVES, AND I DROPPED ONE THIS MORNING** -- not merely for tests. **The old rule said a release build MUST land at `native/rust/target/release/` because four nodes read it, and that `must` is what licensed vc's rebuild under me.** vc has been carrying that as their avoidable act; a share of it was a standing rule of mine instructing exactly that. **Shared path only for a deliberate PUBLISH.** **AND `<scratch>` WAS THE WRONG WORD: OUT-OF-REPO ISOLATION BREAKS `INTENT_HOME`.** `install::home()` calls `current_exe()` and walks ANCESTORS for a marker directory (`install.rs:90-113`, read at source) -- a binary in an out-of-repo target dir has no repo ancestor, the walk reaches the filesystem root, and every hook test correctly reports it cannot resolve. **vc reported 14 workspace failures to hv; SIX were phantoms of their own isolated target dir, and at HEAD with an in-repo one the workspace is 910 pass 0 fail.** **MY ORIGINAL RULE HAD THE LOCATION RIGHT AND THE SCOPE WRONG (`native/rust/target/cc`, _for tests only_); MY AMENDMENT HAD THE SCOPE RIGHT AND DROPPED THE LOCATION.** An amendment aimed at one clause silently discarded a qualifier that was load-bearing for a different reason -- **and the reason it survived unexamined is that I was correcting WHEN to isolate and never re-read WHERE.**

**Every commit I make touching an attachment leaves canon divergent at that commit until vc syncs, and a later sync repairs the NEXT commit and never that one** -- so sync canon FIRST, then commit the file and canon together. Proven on myself: I committed `--staged` into `canon_commit_check.sh`, which IS an attachment of ST0056, and my own AT-03.4 caught the stale hash.
