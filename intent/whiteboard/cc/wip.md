---
node: cc
name: Control Claude
role: control
session_id: ae8c8153-6f3f-438f-b96b-04bd381ad4ed
heartbeat_at: 2026-08-31 21:35Z
status: active
focus: "WORKING 2026-08-31 21:25Z. AC-06.3 GREEN (gate 111). AC-06.1 COVERAGE HALF NOW ANSWERED: with the corpus covered, coverage_map PUBLISHES and finds TWO FAMILIES AT ZERO BURNING COVERAGE -- `config` and `daemon` -- both verified independently of the tool. NO BASELINE INSTALLED; figures came from a worktree copy and vc's ruling is untouched. 27 UNSTABLE reproduced a THIRD time. 0206 DEMONSTRATED for vc: same-thread 19 of 20 CONFIRMED-OVERLAPPING trials lose a write, cross-thread 0 of 20. 0183 retitled, 0205 population is FOUR not three. RE-DRIVE EVERY FIGURE HERE."
claims: [ST0056/06, ST0056/08, ST0056/10, ST0057/00]
---

# Control Claude (cc)

## DOING

**NOTHING IN FLIGHT.** Tree clean of me. Delivered pair correct at `abe69906`; dc re-drove that pin and my own commit gate re-confirmed it -- **nothing to rebuild, and do not let a HEAD-vs-pin difference talk you into it.**

**LANDED THIS SESSION.** `AC-09.6` closed both halves (ic's narrow `f435cc10`, my refusal `ea68dddd`); `AC-10.6` closed by exercise (`3951d72e`); `0198` and `0199` filed.

**WAITING ON OTHERS, NOT ON ME:** vc holds the 27-UNSTABLE disposition; hv holds three scope calls (behaviour-level register check, whether the migrator should commit, MCP exposure for `issues edit --title`/`--severity`). dc is building the `--title` flags. **None of these is blocked on cc.**

## TODO

- **`AC-06.1`'S COVERAGE HALF IS ANSWERED AND IT IS RED ON TWO NAMED HOLES.** Full two-binding sweep, 114 rows, detached worktree, **NO BASELINE INSTALLED** -- the fresh TSV went into the worktree copy only, so vc's ruling stands and the figures are re-derivable rather than banked. **27 UNSTABLE for the THIRD time** (pin `d978d8c8`, HEAD `e0cdc389`, and now both bindings) -- a property of the estate, not of a revision. With the corpus covered `coverage_map` PUBLISHES: **`config` and `daemon` both at 0 REAL / 0 TESTS**, `fileindex` THIN. **VERIFIED INDEPENDENTLY OF THE TOOL:** `tests/unit/config.bats` never invokes `intent config` -- it runs `info`, `st list`, `doctor` -- so it is NAMED for the family it does not exercise, and nothing in `tests/` invokes `intent daemon` at all. **`AC-06.1`'s OWN TEXT NAMES `config` AS ITS SPECIAL CASE** and that clause is an ORDERING constraint, so it cannot be discharged after the design lands. **`daemon` is new surface with zero burning coverage in a tag window** -- v2 had no daemon, so the conformance frame was never going to catch it. Release call, hv's and vc's.
- **`AC-06.3` IS GREEN** (`e0484d4b`, gate 110 -> 111). 24 rows recorded `deviate` in `lib_classify.sh` OVERRIDES citing **D10** -- vc ruled the chain, and I verified it before citing: D10's _shell pruned at the cut_ IS AC-12.1's operative clause verbatim, and `125f601d` names AC-12.1 in its title. Non-prune divergences FILED: `0200`/`0201`/`0202`, plus `0205`. **THE EVIDENCE CARRIES A CAVEAT RATHER THAN OMITTING IT:** this criterion's own text cites `Manage Claude Code subagents` as the binary answering, and that is the `--help` string -- driven, bare is rc=1, `list` is rc=2 not-implemented, `--help` is rc=0. vc's text, vc's to correct.
- **`AC-10.5` IS NOT APPARATUS WORK AND MY BOARD WAS BADLY STALE ABOUT IT.** It said _apparatus not built, three members remain_; **both false.** `fleet_corpus_conservation.sh` runs clean and reports **4 run + 0 unrun** -- all four verdicts recorded at `dbb736ce`, whose own message says filling in the last three would have turned the instrument green on a corpus that fails. It is RED ON SUBSTANCE: 2 members carry residue the MIGRATION does not name, 0 of 2 scored conserved outright. **AND `0183`, WHICH I FILED FOR IT, HAS THE WRONG REMEDY:** `sync.rs:148` already refuted it -- 110 of baize's 135 are `intent/whiteboard/`, which LEFT the not-modelled set at **D30** (hv, WP-14 carries it) and is modelled as `wb_node`/`wb_item`/`wb_message`. **Not out-of-model; IN the model and NOT YET BUILT** -- opposite claims, identical UNACCOUNTED count, and naming them would be the denominator attack. **AC-10.5 CHAINS BEHIND WP-14**; no output formatting closes it. Corrected at `c06f2378`.
- **THE `hoist` ROW'S RE-PIN RULE IS STALE AND IT NAMES FOUR MEMBERS, NOT THREE** (vc found it; I sharpened the count). It says `canary`, `lamplight`, `utilz` and `baize` are _repositories whose migrations have NOT run_, so re-pin-on-HEAD-move still governs them. **DRIVEN: ALL FOUR NOW DECLARE 3.0.0 ON DISK, canary included, because canary IS this repo and it is self-hosted.** Re-pinning any of them to HEAD would point the row at a tree with no v2 source, making STRANDED a claim about nothing -- the exact harm the rule was written to prevent. **DO NOT RE-PIN THEM.** `parity/tools/` is ic's harness, so the edit is coordinated, not taken.
- **THE MCP DISPOSITION FOR `issues edit --title` / `--severity` IS MINE AND hv's, AND IT MUST NOT BE DECIDED BY BUNDLING** (dc's split, adopted). A title is AUTHORED TEXT and falls under the same withholding argument as the body -- the verb replaces prose a human wrote. A severity is a CLASSIFICATION over an enumerated domain, the same kind of thing as the status fields already exposed. **Separate dispositions.** dc builds both flags `exposed_on_mcp: false`, which is the reversible direction: narrowing later is a table edit, un-shipping an exposed mutation is not.
- **THE MIGRATOR DOES NOT COMMIT, SO THE DOCUMENTED ROLLBACK HAS NO SUBJECT** (found by exercising AC-10.6; recorded in `impl.md`). `migration.md` Phase B step 7 says _one commit_ and `AC-00.8` says _one visible commit_; measured, `intent upgrade` returns rc=0 with HEAD unchanged and 23 paths dirty, telling the operator to commit. **_One visible commit_ is an OPERATOR CONVENTION, not a migrator guarantee**, and nothing enforces that it is one commit or that it holds only the migration. **hv's call** -- a behaviour change in a tag window.
- **`0199` IS RESOLVED AND THE FIXTURE IS CANCELLED** (`4368872a`). Probed by round-trip on my own attachment: `st attach --from` moved store 9158->9164 and canon extract 9158->9164 and left **DISK at 9158, title unchanged**. **THE WORD `extract` WAS DOING TWO JOBS** -- attach DOES write `intent/.canon/st/*.json`; what it leaves stale is the AUTHORED DOCUMENT ON DISK, which is what the original drive actually measured on `impl.md`. **attach NEVER writes the disk file, so there was no discriminator to find**; the cases that looked like it worked were files edited ON DISK, where disk was already right. **THE MECHANISM WAS ALREADY IN `0082`** -- _attachments flow disk -> canon only ... there is no canon -> disk writer_. dc and I had each budgeted the nested-vs-top-level fixture; it would have produced a clean green and pinned it on the wrong cause. Fix is unchanged: the gate's REMEDY TEXT.
- **MY OPEN GATE ROWS:** `AC-06.1` and `AC-06.3` above. **`AC-06.8` IS NOT MINE TO CLOSE** -- `flag_reachability` 6/6 green, all 152 declared flags carry a disposition, zero pending; the two unread flags are elsewhere (`0180` withdrawn to dc/ST0058, `0181` hv's via ic). **Do not sequence anything behind me on it.**
- **WP-10 IS THE CHOKE POINT FOR vc's LARGEST BLOCK AND THE CHAIN IS TWO PACKAGES DEEP.** `AC-00.2`, `AC-00.8`, `AC-00.10` sit behind WP-10, which its own cover makes dependent on **WP-06 (mine) AND WP-07 (dc's)**. Read off the cover, not inferred; vc has re-sequenced.
- **`0086` AND `0063` WOULD HAVE TO BE WRITTEN, NOT RECOVERED** -- a different task, cost and author. `0095`/`0096` are one-word stubs needing a DISPOSITION, hv's via vc.
- **THE POST-BANNER LOSS IS FILED AND SCOPED TO hv** (`0192`, rewritten by dc at `16cdd671`). **If hv rules it in, the refusal goes in `info_read_back`, NOT `authored_regions`** -- truncation keeps its single purpose.

## Watch-outs

**`bin/.devbin/` IS VENDORED AND CARRIES AN INTEGRITY MANIFEST, SO AN IN-TREE FIX THERE LOOKS RIGHT AND SILENTLY REVERTS.** Every commit touching it is a re-vendor, and `manifest.sha256` records `lib/builtins` at `af07aaa3...1471` -- which MATCHES the file, so it is pristine upstream content. An edit breaks the manifest AND is overwritten at the next re-vendor: it passes review and comes back. **Fixes go UPSTREAM or are accepted as out of scope**; `0205`'s fourth block is there, and ic was about to hunt for an owner this repo does not have. **Check for a vendor manifest before assigning work in a directory you did not author.**

**`--help` RENDERS FOR A DECLARED COMMAND WHETHER OR NOT AN ARM EXISTS, AND IT PRODUCED THREE WRONG READINGS TODAY IN THREE DIFFERENT HANDS.** dc caught themselves on it, I inherited it through an inference, and **it is baked into `AC-06.3`'s own ruled text** as proof a command answers. **INVOKE THE VERB, TAKE rc WITHOUT A PIPE.** The three forms disagree and only one of them is the question: bare `claude subagents` rc=1 needs-a-subcommand, `list` rc=2 NOT IMPLEMENTED, `--help` rc=0 prints prose. **AND `bash -n` IS NOT A CHECK ON A QUOTED DATA BLOCK** -- a literal `"` inside a double-quoted string closes it and turns prose into shell, and the result is still valid bash (`0205`). The thing that caught it was a row I had not touched coming back empty too: **a control I had by luck, which an empty table would not have given me.**

**CANON CANNOT BE SPLIT, SO EVERY CANON COMMIT ON THIS ESTATE IS SILENTLY A MULTI-NODE COMMIT.** `git commit` commits the index AS IT STANDS, and `intent/.canon/st/ST0056.json` is one file the daemon re-renders from whatever is on disk -- so a peer's in-flight edit lands in my staged canon without either of us acting. **MEASURED BOTH WAYS IN ONE HOUR:** I caught a peer's `capability_ship_check.sh` bytes in my index and unstaged rather than commit them, and minutes later vc's `48533319` carried MY parity doc and MY `view_skew_check.sh` row under their message. Content was intact both times; what is lost is the RECORD of whose work it was. **`--only` is the only safe write, the diff is read for paths that are not mine BEFORE staging, and when canon must carry a peer's bytes it is DECLARED in the message** -- `337d6451` declared it, `48533319` could not, and nothing distinguishes the two afterwards.

**A SELECTOR THAT RESEMBLES THE POPULATION IS NOT THE POPULATION, AND IT FIRED TWICE TODAY IN OPPOSITE DIRECTIONS.** My JSON walk counted 61 exposed rows against ic's 60 by descending into `flags` and counting `graphql --variables` -- a flag legitimately carrying its own `exposed_on_mcp` -- as a command. **`shipped_entries` is families' entries plus `new_surface` and EXCLUDES flags; use the PRODUCTION selector.** ic's anomaly selector over-matched the other way by not excluding retiring rows. **Then it fired a third time in prose:** I grepped the first matching error line out of a 50-test file and reported it as the file's cause -- it came from a different test than the one that mattered, and the wrong cause implied a repair that would have deleted a shipping command from the register.

**INDEPENDENT OF THE REASONING IS NOT INDEPENDENT OF THE INSTRUMENT.** I recorded a ruling as _verified by me independently rather than accepted_; vc and I had both driven the TREE binary, so the agreement was one measurement reported twice. **THE VERSION STRING CANNOT ARBITRATE IT:** keg says `intent 3.0.0 (80d8b2ca)`, tree pair says `intent 3.0.0 (abe69906)` -- same version, different verb sets, ancestor 1516 commits back. **_Does X ship in 3.0.0_ is unanswerable from the version while two 3.0.0 artefacts differ.** vc is carrying this to hv as an argument against numbering the release 3.0.x at all.

**DRIVE AN INSTRUMENT TO BOTH VERDICTS OR ITS GREEN MEANS NOTHING, AND A CONTROL MUST EXERCISE THE VARIABLE THAT CAN BE WRONG.** The AC-09.6 refusal was proven by REVERTING the production arm and watching the planted-victim test fail; its companion passes under both forms and is recorded as discriminating nothing. **vc's near-miss is the sharper half:** their positive control passed because it never touched the variable that was broken. **A green over a corpus that cannot exhibit the change is not evidence about the change** -- assert the denominator, never print it and trust it.

**CONSTRUCTING BEATS READING, AND MEASURING BEATS BOTH.** Four things I believed died today when driven: the keg has no `issues edit`; the migrator does not commit; `st attach` does not write the extract; all four corpus members are already migrated. **Every one of them read as settled until something was run.**

**AN INSTRUMENT CAN REPORT THE RIGHT SHAPE OF ANSWER FROM THE WRONG PLACE.** `st show` prints id/title/status/created and nothing else, so a correct `0192` drive reported an edit missing while the db held it -- **vc hit the identical blind spot independently.** Same family: a remedy that is actionable and wrong is worse than a bare one, and one wrong in a single instance is usually wrong for the whole arm.

**A CLAIM IN A COMMENT OR A GUARD OUTLIVES ITS BASIS AND NOTHING WATCHES THE JOIN.** `0199` is the live instance: a guard's instructions to the human are the one part of it no instrument reads. Same shape as `0198` -- nothing compares an `as-observed` register row to behaviour, which is a THIRD gap on `implemented_check.sh`'s own line (it already records that it cannot tell WIRED from WIRED-AND-IMPLEMENTED).

**THE SHELL LIES ABOUT SUCCESS IN FOUR SEPARATE WAYS ON THIS BOX.** `cmd | tail` reads tail's status; `cmd > log; echo rc=$?` reads the echo's -- **it fired again today**. zsh has no `PIPESTATUS`, it is `$pipestatus[1]`; zsh does NOT word-split `$var` (vc fabricated three PIN MISSING results this way); an unmatched glob ABORTS the command; `grep` is `ugrep` and a pattern starting `-` reads as a flag; `sed -i ''` SUCCEEDS on no match so a `||` fallback never fires. **And `cwd` PERSISTS BETWEEN CALLS** -- it bit three more times today. Absolute paths.

**PROVENANCE IS CARRIED BY PATH; ADJACENCY IS NOT PROVENANCE.** Every node commits as the same person and every issue reports `reporter: Matthew Sinclair`, so the field built to answer _who wrote this_ is unreadable. Read the touched PATH. **I DO NOT WRITE INTO A PEER'S ISSUE OR AREA** -- `0192` was dc's, so the measurement went to dc and dc landed the rewrite.

**THE SHARED BUILD DELETES BEFORE IT BUILDS (`0196`) AND A MISSING ARTEFACT READS AS AN EMPTY ANSWER, WHICH IS WORSE THAN AN OUTAGE.** A deleted `intent` returns rc=127 with empty stdout, indistinguishable from a verb that ran and found nothing. **Check AFTER.** `NEVER start intentd under the real HOME while peers are live`; never `--no-verify`; a live `index.lock` is a WAIT, never a removal; never unstage a peer's path. **My extract writes RENDER, so a peer's store-only edit can land on disk under my hand** -- announce and leave it staged for them.

**A THREAD MUST BE REALISED OR IT HAS NO COVER.** `init` writes a manifest of comments only, and _a file present and declaring nothing means keep nothing_ -- `st new` + `sync --to-disk` yields canon and no view; `st start` realises it. **`SERVED` IS NOT A HIGHLANDER VIOLATION** (`mcp_surface.rs:30` says so on purpose). **`intent:///` is scheme, EMPTY authority, path.**

## Decisions

- (2026-08-31) **A CONTROL THAT CANNOT DISTINGUISH _SAFE_ FROM _NEVER TRIED_ IS NOT A CONTROL** (vc, on my `0206` harness). My cross-thread arm returned 0 losses, which could equally have meant the two edits never overlapped -- a clean run over a population that cannot exhibit the failure. **The repair was to MEASURE the overlap, not to argue it:** a start gate to synchronise launch, `$EPOCHREALTIME` either side of each exec, and an iteration counted as a TRIAL only when the intervals intersect. Result on the SHIPPING binary: **same-thread 19 lost of 20 trials, cross-thread 0 of 20, ZERO non-trials in either arm.** The wall interval is wider than the load-to-apply window, so trials are an UPPER bound and the loss rate a LOWER one -- the bound can only understate the finding, and its direction is stated because that is what separates a claim from a guess.

- (2026-08-31, CORRECTED 18:27Z) **`close --note` STAYS RULED OUT, BUT NOT BECAUSE THE CAPABILITY ALREADY SHIPS -- THAT WORD WAS FALSE.** The two-verb path exists ONLY on the tree binary; the keg has no `edit`. **The ruling survives on a weaker, contingent footing:** v3.0.1 ships from the tree, so the tree's surface is what users get. It was never redundant on what users have today.
- (2026-08-31) **AN OWNERSHIP SPLIT IS A PURPOSE, NOT A BOUNDARY** (ic, correcting me). Reverting so a peer can rebuild byte-identical code is churn wearing Highlander's clothes. **AC-09.6 is the split done right** -- two halves, two hands, two commits, one order, nothing red in between.
- (2026-08-31) **A CRITERION IS NOT REWORDED TO WHAT THE CURRENT STATE SATISFIES** (vc, on AC-06.3). Rewording to match reality is how a green stops meaning anything; the ruled form is HARDER than the original and binds the node holding the divergence.
- (2026-08-31) **A DECLARED EXCLUSION CARRYING ITS REASON IS THE CURE; A SILENT DROP IS THE DENOMINATOR ATTACK.** The v2-formula reinstall in the rollback procedure is NOT executed on this machine (brew `intent 3.0.0_1` is pinned; PATH `intent` is the tree binary), and it is recorded as an exclusion with that reason rather than skipped.
- (2026-08-31) **hv's `0192` RULING HAD A DEAD PREMISE AND COSTS ZERO BUILD.** The read-back is wired end to end: `sync --to-store` -> `render.rs:1047` -> `Facade::sync_from_disk` -> `facade.rs:3137` `ingest::resync` -> `resync_inner` -> `carry_info_round_trip` -> `store.rebuild`.
- (2026-08-31) **A LADDER IS WALKED IN FULL AND THE FIRST HIT DOES NOT WIN.** Precedence would silently pick the thread for 48 of 69 numbers on this estate.
- (2026-08-31) **THE TWO ENCODINGS OF GENERATED VIEW ARE NOT TWO HOMES FOR ONE FACT.** `VIEW_NAMES` carries names `classify` never sees; only the OVERLAP must agree, one-way. Asserted at `5f58265b` by a test that **deliberately does not import the constant it tests**.
- (2026-08-31) **A CONTRACT WITH A CONSUMER IS ASKED FOR, NEVER MINTED.** ic named `daemon status --format json`'s shape and I built that; `terminal` and `json` are real and `zzz` refuses at rc=1 naming the roster.
