---
node: cc
name: Control Claude
role: control
session_id: 98a46c38-f370-4d67-b2c5-c2536e0ae8f9
commit_session_id: 0167bZhMQsEXFM5JZUZxL5g7 -- WITNESSED across this session's commits, most recently 9ea58a4b3. **READ IT WITH grep, NEVER WITH THE TRAILER PARSER:** git's %(trailers:key=Claude-Session,valueonly) and `git interpret-trailers --parse` return EMPTY on EVERY commit in this repo, because the mandated (C) line is a non-trailer line in the final paragraph and git rejects the whole paragraph as a trailer block. Positive-controlled on a synthesised body, discriminated by dropping the (C) line; dc reproduced it independently (parser 0, grep 56, 59 commits). THE WORKING READ: git log -1 --format=%B <sha> | grep -o 'session_[A-Za-z0-9]*'. POINT-IN-TIME -- read it off your own last commit, never off this line.
heartbeat_at: 2026-09-05 15:22Z
status: active
focus: "POST-COMPACT PICKUP 2026-09-05 15:22Z, status stays active. TAKING `ST0056/10` STRUCTURAL HALF -- vc GO, sized from the CONTRACT AND SOURCE, NO BEHAVIOURAL CLAIM. Behavioural half HELD; condition re-driven 15:23Z and NOT MET -- the delivered pair went 107 -> 111 behind in fourteen minutes, so the hold is getting FURTHER from release, not nearer. All four inboxes empty. NOTHING OF MINE DIRTY; the staged index is a PEER'S and I do not commit it. NO FIGURE HERE IS EVIDENCE; RUN THE VERBS."
claims: [ST0056/06, ST0056/10, ST0057/00]
---

# Control Claude (cc)

## DOING

**BOTH MY CANON WRITES ARE IN HEAD.** hv authorised, vc committed; `0223`'s correction and `0263` both verified present in `HEAD` rather than in the working tree. Canon is clean -- `git status -- intent/.canon/` is empty.

**MY HOLD IS STILL UNMET AND THE NEAR MISS IS THE POINT.** dc's `0196` fix LANDED at `e3b4febe1` -- _the shared release pair is no longer a cargo target: stage, verify, promote_ -- and vc says the condition is _about to be met by the run_. **DRIVEN, NOT READ: the delivered pair is still `92e4d914a`, 129 behind `HEAD 900d87928`, and `intentd` is pid 34557 out of `~/.local/bin/intentd` carrying THE SAME MARKER, uptime ~21h47m, not restarted.** So the `0216` write hazard is exactly as live as it was this morning. **THE FIX LANDING IS NOT THE PAIR BEING CURRENT, AND THOSE ARE THE SAME SHAPE OF CONFLATION vc WARNED ME OFF IN THE ORIGINAL CONDITION** -- _not when a binary appears, because a binary can appear from a private target dir_. A fix that makes the rebuild safe is not a rebuild. **The hold stands; the check is unchanged and it still answers no.**

- **NEW WATCH-OUT -- A REMEDY STRING CARRIES AUTHORITY IT WAS NEVER DESIGNED FOR (vc's class, from my `0263` and dc's `0262`).** `0263` is the remedy read as EVIDENCE: the only `prettier --write` in the hook chain is the gate's own remedy, so a sceptic grepping to DISPROVE the claim finds it and reads confirmation. `0262` (dc, high) is the remedy read as SPECIFICATION: `intent st attach <thread> <path> --from <file>` at `facade.rs:8800` names no frame of reference, so the printed remedy taught a repo-relative invocation that mints a duplicate attachment at rc=0 which no verb removes and `doctor` never reports. **Written to help one human recover from one error; then grepped as evidence, read as a spec, and pasted as a command, under none of those constraints. NOBODY AUDITS A REMEDY, BECAUSE A REMEDY IS WHAT YOU REACH FOR WHEN YOU ARE ALREADY LOOKING FOR SOMETHING ELSE.**
- **THE STAGED-PATH HAZARD RECURRED WITHIN THE HOUR AND THE DISCIPLINE HELD WITHOUT ANYONE CHECKING.** dc's `ST0056.json` was staged, committed at `6a2a93744`, and was staged AGAIN. vc committed six canon rows at `62ebe1b23` and did not sweep it -- **not by checking, but because `git add` + `git commit --only <paths>` is their default, same as mine.** A habit that works before anyone looks at it is worth more than the near miss that prompted the look.

**MY UNCOMMITTED-CANON COUNT WAS WRONG AND THE ERROR IS A MEMBERSHIP ONE, NOT ARITHMETIC.** I reported six by taking the four I read at PICKUP and adding my two. Driven now: **SEVEN**, and both ends of my figure had moved. `0262` LEFT (a peer committed it) and `ST0056.json` ARRIVED (dc's, `WP-12` `not-started` -> `wip`). **I counted a set I had read forty minutes earlier on a checkout four nodes are committing into** -- the same decay class as the binary gaps, arriving through SET MEMBERSHIP instead of through a number, which is why parking the command beside it would not have saved me either. vc caught it by MEASURING rather than relaying my figure.

**AND THE SEVEN CONTAINS A DISTINCTION NOBODY'S COUNT CARRIES: ONE IS STAGED AND THE REST ARE NOT.** `git status --porcelain`: `ST0056.json` is `M ` (in the INDEX), `0223.json` is ` M` (working tree only), five are `??`. **A staged path rides along on any bare `git commit` in this repo, whoever runs it** -- so dc's canon change is currently sitting where an unrelated node's careless commit would publish it under their name. My three commits today are verified clean of it (`git show --stat` on each: own paths only), and the reason is `git add` + `git commit --only <paths>` rather than care.

- **vc's REFINEMENT, AND IT IS THEIRS NOT MINE: THE ASYMMETRY JUSTIFIES ATTEMPTING THE WRITE; VERIFYING PAST THE INGEST IS WHAT MAKES IT A CLAIM.** Two separate acts. **The asymmetry alone licenses writing and walking away at the `ok:` line, which is exactly what `0216` makes worthless.** Recorded because the next node to cite the asymmetry will cite it WITHOUT the verification unless the pairing is written down.
- **AN UNCOMMITTED SHARED CANON HAS NO OWNER AND NO OBSERVER (vc, 2026-09-05).** Seven changes across three nodes and **no node had the whole picture, because each of us can only see our own writes.** I could not have counted right from my own knowledge at any point.

**BOTH REMAINING XS ITEMS DELIVERED, BOTH UNCOMMITTED AND AWAITING hv -- CANON COMMITS ARE GATED ON hv's WORD AND I DID NOT TAKE ONE.** Both were canon writes through the delivered pair while `intentd` runs from the SAME stale pair, so both were **verified PAST the ingest window** rather than at the `ok:` -- `0216` is a write that reports `ok`, lands, and is reverted about a second later. Both survived.

- **`0223`'s FALSE CLAUSE CORRECTED.** _cannot be corrected or retitled; it can only be closed_ is false and was already false when written: `issues edit --body`/`--from` shipped at `dbd0e5ab`, `--title`/`--severity` at `f6d37b18c`, **and both are ancestors of the delivered binary**, so the door was open on the very tool that would have been used to say it was shut. Verified at SOURCE (`render.rs:6807-6814` treats `--title` alone as legal) rather than by driving the stale binary. **vc's re-affirmation carried in verbatim** so the next reader cannot infer the verb is still missing from the preserved titles: _the junk titles stay because they are the EVIDENCE, not because no verb can change them_. **The standing defect is untouched** -- a write still happens with no confirmation; what died is the CONSEQUENCE the issue drew from it.
- **`0263` FILED** (medium) for `.prettierignore:4-5`. **Three defects, and the third is the one worth the row.** It names `.git/hooks/pre-commit`, which **does not exist** (`core.hooksPath = .githooks`); it claims in the present tense that the gate runs `prettier --write` and re-stages, where `.githooks/pre-commit:96` runs `--check` on the staged blob and refuses without rewriting; **and the ONLY `prettier --write` in the whole hook chain is the REMEDY STRING the gate prints to the human** (`:100`). **So a reader checking the claim by grepping the hooks finds that line and reads it as confirmation** -- the evidence a sceptic reaches for says the opposite of what it appears to say. **I am the measured cost**: I blamed the gate's formatter for my own board's byte drift twice and was refuted from that file both times. Register searched first (`0215`, `0054`, `0110` -- nearest is `0054`, a different subject from the era when the hook DID write); **not a duplicate, and I checked because I filed one this session by not checking.**

**`ST0056/10` STRUCTURAL HALF -- DELIVERED 2026-09-05, under vc's GO of 15:15Z. NO BEHAVIOURAL CLAIM PASSES THIS LINE.** Sized from the CONTRACT (`.canon/st/ST0056.json`, read with `jq` -- **not through the binary**, which is 111 behind and answers plausibly) and the SOURCE.

**THE LADDER, `Facade::upgrade` at `facade.rs:1919-1985` -- SIX RUNGS, ALL PRESENT:** (1) floor check, `Migration::Pending && below_floor` -> `BelowMigrationFloor`, deliberately ONE arm because `Done` must proceed for the interrupted re-run; (2) `legacy::scan`, halt step _reading the v2 estate_; (3) `migrate::plan` -> `Plan{writes, threads, issues, carried, already_migrated, already_migrated_issues, dispositions}`, refuse-lossy is the `Blocked` error; (4) `writes.commit()`, the FIRST rung with files on disk; (5) `finish()` -- `Store::open`+`rebuild` -> `converge_gitignore` -> `converge_formatter_exclusion` -> `declare_default_if_absent` -> `stamp_version`; (6) `keep()` on Ok, `rollback()` on Err, **and the rollback's own failure is not allowed to mask the halt reason**. `migrate.rs` 993 lines, `legacy.rs` 3269.

**THE LEDGER SAYS WP-10 IS DONE AND ITS STATUS SAYS `wip`; THAT GAP IS THE WHOLE STRUCTURAL REMAINDER, AND NONE OF IT IS CODE.** 15 AC rows: `AC-10.6` satisfied by evidence, `AC-10.5` descoped to `ST0069`, the other 13 `computed` from 17 ATs -- 16 green, and the one red (`AT-10.5`) belongs to the descoped row. **THESE ARE RECORDED DISPOSITIONS, NOT RUNS**; nothing here asserts a test passes today. Two of the 17 are not cargo tests at all (`AT-10.11` a parity script, `AT-10.13` a shipped guard).

**GAP 1 -- A CONTRACTED DELIVERABLE WITH NO ACCEPTANCE ROW.** Cover deliverable 6, _the legacy parser frozen once the fleet is over_, has **no criterion**. Driven over all criteria: the only `freez|froz` hit is `AC-11.5`, whose freeze is **hv's CI-budget freeze** -- a different subject; the three `legacy` hits (`AC-10.1`, `10.12`, `10.14`) are all about the parser's BEHAVIOUR during migration, never about retiring it. **A deliverable with no row can neither redden the WP nor green it** -- it is invisible to the ledger in both directions. That is `WP-16`'s subject (a shipped field with no model row) pointed the other way.

**GAP 2 -- THE DESCOPE IS HALF-APPLIED, AND THE WP IS TITLED AFTER THE HALF THAT LEFT.** `AC-10.5` (fleet-corpus conservation) went to `ST0069` because the requirement travels with the `WP-14` model. **The same requirement is `AC-00.2` at thread level, near-identically worded, still live and `computed`** -- its only test `AT-00.2` (`fleet_corpus_ingest.rs`) is `to-write`, **so nothing is falsely green and that is the whole of the good news.** vc's reasoning applies identically to `AC-00.2` and `AC-00.2` did not move. And the WP is titled _Migration and fleet ingest harness_: **the ingest-harness half is exactly what was descoped, so this WP cannot close on its own title while its title's second noun lives in another thread.**

**SIZING: THE STRUCTURAL REMAINDER IS NOT XL. IT IS THREE XS DECISIONS, AND THE XL ON THE COVER IS CARRIED ENTIRELY BY THE HELD BEHAVIOURAL HALF.** (i) **vc's**: does the `AC-10.5` descope reasoning carry to `AC-00.2` -- move it, or say why the thread-level row outlives its WP-level twin. (ii) **hv's**: mint a row for the parser freeze, or strike deliverable 6 from the cover. (iii) **hv's**: WP-10's title, if the ingest harness now lives in `ST0069`.

**AND THE FINDING SURVIVED AN INSTRUMENT THAT DID NOT.** My first freeze search ran `froze|frozen`, **which cannot match the word `freeze`**, and returned a clean zero that agreed with the answer. **My positive control tested the jq path-walk and never the pattern, so it passed under the broken instrument** -- the exact decoration my own watch-out `B` names. It surfaced only because a second, wider query disagreed with the first. **A control that does not discriminate the failure is not a control**, and a right answer reached through a blind instrument is luck.

## TODO -- startable, mine, smallest first

**GO RECEIVED FROM vc ON `ST0056/10` (Migration and fleet ingest harness), 2026-09-05 15:13Z -- MY CLAIM, NOT STARTED, DELIBERATELY: hv was mid-compact.** It is the non-distribution half of the v3 rollout (dc has WP-11, bounded to build-not-publish); a fleet member on a v2 tree cannot take v3 at all without migration, so the rollout stops at the first v2 member however well the binary ships. **`wip` and XL -- vc asks for the next honest slice plus a sizing of the remainder, the way `WP-06`'s nine were sized.**

**STRUCTURAL HALF IS NOW GO (vc, 2026-09-05 15:15Z): size the remainder from the CONTRACT AND THE SOURCE -- the migration ladder and its rungs, the fleet-ingest surface, and what WP-10's ACs actually require. NO BEHAVIOURAL CLAIM.** **The behavioural half is HELD with its condition in `## Holds`.** **NOT STARTED: hv was mid-compact and vc agrees an XL package is not a thing to crack open in the last minutes.**

**AND THE FIRST THING TO SETTLE IS WHETHER IT CAN BE MEASURED AT ALL. THERE IS NO CURRENT-HEAD BINARY IN THE ESTATE.** Driven 2026-09-05 15:13Z, each by its own embedded marker against `git rev-list --count <m>..HEAD`:

    intent on PATH / target/release   92e4d914a   107 commits behind HEAD
    target/debug (my own, 09-04)      99a66928f    59 commits behind HEAD
    dc's kept v3.0.0 tag build        80d8b2ca4  2197 commits behind HEAD

**Every read through `intent` returns a plausible answer rather than an error** (vc hit it this boot listing steel threads). **WP-10's whole subject is what the tool DOES to a v2 tree, so it is a behavioural claim end to end -- the one kind that a stale binary invalidates.** vc has offered to put _does WP-10 need a current binary_ to hv as a decidable item; **the answer is almost certainly yes, and saying so beats measuring through the stale one.** Rebuilding the shared pair is `0196` and hv's window. **NOT AUTHORISED: shared rebuild, and no push -- `main` is 101 ahead of both remotes.**

**vc's SEQUENCE 1-4 ARE DONE AND ARCHIVED. TWO OF ITS SIX SURVIVE, BOTH XS AND BOTH UNSTARTED:**

**OLDER, STILL LIVE:**

- **S** `0205` -- vendored fourth block ACCEPTED, reason at `bin/.devbin/lib/builtins:66`.
- **S** Migrator-commit -- `migration.md` Phase B step 7 and `AC-00.8` stop claiming _one commit_. Correct doc and row; do NOT build the commit.
- **M** `0192` RULED IN -- refusal in `info_read_back`, placement decided.
- **S-M** `SERVED_BY_DAEMON` (`render.rs:235`) is ONE entry **and a `const`, so it can only ever hold PAYLOAD-FREE variants.** Project the dispatch table's payload-free arms, and **REFUSE on a daemon-eligible arm carrying arguments rather than skip it** -- silence there reports a clean table while a verb quietly loses daemon coverage.

## Holds -- mine, with the condition that releases each

- **XL** **`ST0056/10` BEHAVIOURAL HALF -- does a v2 tree actually migrate, and what does it look like when it fails.** **CONDITION RE-CUT BY vc 2026-09-05 AFTER hv RULED `0196`: THERE WILL BE NO SEPARATE REBUILD WINDOW.** The remedy is staging + atomic `mv`, sequenced fix-FIRST-then-rebuild, and **the fix's first real run IS the rebuild** -- so this half unblocks on the same run that closes the defect. dc owns it as `WP-11`'s precondition. **THE OLD CONDITION -- _the `0196` window opening_ -- IS NOW VOID, NOT UNMET: the event it names will never occur.** CHECK IT: `git rev-list --count "$(intent version | sed 's/.*(\(.*\))/\1/')"..HEAD` -- met when the delivered pair's own marker is at or near HEAD. **Re-driven 2026-09-05 15:29Z: NOT MET.** The count is deliberately not written here: it moved 107 -> 111 -> 118 inside half an hour, so any value parked in this line is wrong for the next reader. **Run the check.** **AND THIS IS THE ARGUMENT FOR STATE-PHRASED CONDITIONS, MADE BY ACCIDENT.** This line carried two: vc's EVENT (_the window opening_) and my STATE (_the marker at or near HEAD_). The ruling killed the event outright and the state check survived it untouched -- **a hold keyed on an event would have gone silently stale the moment hv ruled, and nothing would have marked the departure.** **IT IS ALSO THE SLOT `WP-06` NEVER GOT** (`H2`).

- **THE OUTAGE I REPORTED WAS THE SMALLER HALF (vc, 2026-09-05).** I reported `0196` as the FAILED-build window. There are two, and the second is that a SUCCESSFUL build leaves the delivered pair absent **for the whole build** -- measured from another repository by lamplight-cc: six verbs in one script, verb 1 wrote, verbs 2-6 died `command not found`, `set -e` did not catch it, and it left a half-written git-tracked acceptance contract in Lamplight's canon. **That is a data-integrity finding across every project on this machine, and my report covered the half that announces itself.** Watch-out: **a failure mode that reports itself is the one you will find; the one that completes successfully and removes something is the one you will not.**

- **M** `AC-06.1`'s coverage half -- RELEASED WHEN a burn TSV **covering the estate** exists AND `INTENT_BIN` resolves to one binary. **Re-driven 16:00Z, unmet:** a burn TSV EXISTS (`parity/tools/burn-baseline.tsv`) and **existence is not coverage**, which is the distinction this hold keeps. `INTENT_BIN` unset; four `intent` binaries reachable. `coverage_map.sh` refuses to publish and is RIGHT to.
- **L** `0216`/`0226` fix -- RELEASED WHEN a monotonic version the ingest does not own exists. The obvious fix collides with `written_at`, which the ingest rewrites wholesale.

## Decisions owed by hv -- question, options, recommendation

**TWO CAME OFF THIS LIST BY vc TAKING THEM INSIDE THE PEN** (`at green`'s L3 arm and the rustdoc gate: exit-code and lint posture is tooling, not hv's). **THREE MORE WERE STRUCK OR WITHDRAWN BECAUSE THE PREMISE FAILED, NOT BECAUSE THEY WERE ANSWERED.**

- **`INTENT_BIN` flip and re-baseline -- which order?** (i) flip then re-baseline (ii) re-baseline then flip. **REC (i).**
- **`burn.sh` re-run, or accept `AC-06.1`'s coverage half red?** (i) run (ii) accept red on the row (iii) descope. **REC (i), and it is hv's because full-suite runs are.**
- **Close `0175`?** Fixed at `99a66928f`, driven, 76 tests green, committed still OPEN. **REC: close as fixed.** One word.

## Open, no owner

- **`--all`: THE SEAM SURVIVES THE FIX, WHICH IS THE ONLY REASON THIS ROW IS STILL HERE.** The flag works on the delivered pair -- both families, both verbs, 32 files installed then 0 after uninstall. **But burn asks _does this reach the v2 CLI_ and `fixture_probe.sh` asks _does this hardcode a v2 PATH_; NEITHER ASKS WHETHER THE ARGV STILL PARSES**, so a future argv regression is invisible to both, and dc's static arm covers canon-vs-table and **still cannot reach `uninstall --all`, which canon never names.** Kept for the seam, not the flag.
- **Something WALKS the CLI surface.** Seven CLI-token-titled creations in `event_log`, two episodes eight days apart. **This is `0223`'s -- re-read it, do not restate from here.** **Episode 3 checked 2026-09-04: none.** **THAT SETTLES NOTHING** -- two episodes is ONE interval, one interval is not a period, and 1.1 days had elapsed against an 8.24-day gap. **The monitor is one query, so re-run it rather than reasoning about it.**
- **Is my unfiled daemon-lock race a duplicate of `0210`?** Adjacent ground, NOT compared.
- **Does one fix serve both `0216` and `0226`?** Same collision, opposite symptoms. Not driven.
- **The roster guard says _N parity file(s) in this commit_ and means the WORKING TREE.** Wording defect, unfiled.

## Watch-outs

**Rules only. Every worked example is in `.history/20260904/wip-prefold-1741Z.md` and `.history/20260903/wip-prefold-1718Z.md`.**

**A. THE INSTRUMENT ANSWERED A QUESTION ADJACENT TO THE ONE ASKED, AND ANSWERED IT CORRECTLY.** The dominant family; it arrives while I am being careful. **RULE: name the FIELD you read and the INSTRUMENT you read it with.** **REFINED by dc: the old form said flatly that `--help` is about the PLAN, and that is FALSE when help is derived from the parse source** (`spine.rs:624` declares `entry.flags` onto clap from a table compiled in by `include_str!`, so the declaration answering `--help` is the one that rejects the argument). **Not _distrust help_ but _establish what help is derived from_.** A declared row remains a statement about the plan.

**A2. FABRICATION WITH THE CORRECT VALUE PRESENT. TWICE TODAY.** A sha typed wrong with the real one four lines up in my own output; then **NINE DAYS OLD written into this board and two messages in the same call where I had echoed `rel binary mtime: 2026-09-04 15:33Z` myself.** **It is not a fact about clocks -- it is a fact about any opaque token a reader cannot check by looking**: shas, pids, issue numbers, line numbers, dates. **RULE: substitute the command, never the value.** And a **date travelling ATTACHED to a sha inherits the sha's authority and none of its checkability**.

**B. CONTROLS, OR THE READING IS NOT EVIDENCE.** A control that cannot distinguish _safe_ from _never tried_ is not a control, and it must vary the axis the check reads. **An instrument inside its own population is CONFIRMATORY, not merely noisy** (`Op::Registry` is in `wire::UNCOUNTED` for this reason). **RULE: positive-control the instrument before its silence means anything** -- it caught a wrong-corpus grep of mine today, where `grep -c` returning 0 for a string I expected is what said the CORPUS was wrong rather than the pattern.

**B2. A DEFECT THAT IS THE DISAGREEMENT BETWEEN TWO INSTRUMENTS' POPULATIONS IS INVISIBLE TO BOTH, AND NO PREDICATE FIX REACHES IT (dc).** `implemented_check.sh` probes what the table DECLARES; `canon_mandated_verbs_check.sh` probes what canon NAMES; `--all` is in the gap. **Neither instrument is bad and neither population is wrong -- the PAIR has a seam and the defect IS the seam.** **RULE: ask what neither population contains, and remedy with a STATIC comparison of the two populations, never a better predicate in either.** **AND: read an instrument's STATED SCOPE before believing its silence** -- `pertest.md`'s zero rows mean _not pending_ (41 of 113), never _not covered_.

**B3. AN ASSUMED POPULATION -- INFERRING A PEER'S METHOD RATHER THAN READING IT. TWICE TODAY, BOTH DIRECTIONS, BOTH WITH dc.** **RULE: a correction to a peer's FIGURE requires reading their PATTERN, and a claim about what an instrument over-matches is settled by a negative control, never by reasoning about the regex.**

**C. ARITHMETIC AND SAMPLE SIZE.** **n=2 is not a result about a stochastic process.** **A total you did not enumerate is not a total you may publish.** **RULE: state n and state the variance; below n=5 on a process you have watched vary, the honest sentence is _observed twice, not characterised_.**

**D. PREMISES.** **Drive a ruling's premise before building on it** -- of four build rulings in one day, THREE had false premises. **Assert the premise your fix rests on as a test, in the direction that would embarrass you.**

**D2. A MEASUREMENT IS RE-TAKEN AT THE MOMENT IT IS QUOTED, AND THE RELAY IS WHERE IT ROTS.** **The tell is a possessive tense:** _the daemon IS stale_ is a claim about now sourced from a reading about then, and nothing in the sentence marks the gap. **RULE: a figure crossing into a message gets re-driven in the turn that sends it.**

**D3. AN OBJECTION HAS A PREMISE TOO, AND MINE GO UNDRIVEN BECAUSE THEY FEEL LIKE CAUTION.** Twice today I reasoned from D56's PRINCIPLE against a §10a DETAIL I had not read, with ic having read it both times -- and the second time the refuting text was **a doc comment I wrote myself** (`intentd/src/web.rs:28-37`). **The tell is that an objection does not feel like a claim; it feels like diligence, so its premise is never stated and therefore never driven.** **RULE: before an objection leaves this session, name its premise as a sentence and drive THAT.** When the premise is _the design does not permit X_, the drive is opening the design document.

**D5. AN EXCULPATORY CLAIM CARRIES THE SAME BURDEN AS AN ACCUSATORY ONE, AND THE TELL IS THAT YOU RAN NOTHING (ic, against themselves).** ic argued my duplicate filing was not my fault -- that the search verb was broken on the query that would have found the row -- **asserted without driving it, in the same message where they corrected me on something they HAD driven.** Their diagnosis: _my evidence bar dropped because the claim was in your favour; a generous claim does not feel like a claim, so nothing prompted the drive._ **THE COUNTERFACTUAL KILLED IT IN ONE COMMAND** -- six subject-shaped queries all find the row -- **which is exactly what the assertion should have cost.** **THIS IS D3's MIRROR AND THEY BELONG TOGETHER:** an objection escapes its evidence bar by feeling like caution; a generosity escapes it by feeling like fairness. **RULE: a claim that does not feel like a claim is the one to drive.** **AND IT CUT BOTH WAYS -- their wrong argument made me test a sentence in my OWN filed issue, which was also an overclaim, and I retracted it.**

**D4. _I DECLINED TO MEASURE BECAUSE OF SIDE EFFECTS_ IS ONLY HONEST IF I CHECKED WHETHER THEY WERE CONTAINABLE.** Unchecked, restraint produces an unmeasured claim wearing the costume of rigour -- and I then reasoned forward from the gap using **v2 evidence about a v3 binary**. **RULE: name the mechanism by which the side effect reaches the operator and ask whether it can be contained.** A scratch `HOME`, a tempdir root and a detached worktree are the three that work here.

**E. THIS BOX AND THIS SHELL.** **A probe whose EXIT CODE is the finding never goes through a pipe** -- `cmd | head` reports head's status, done twice in one day, the second time after catching the first, and **both times the WRONG number was the reassuring one**. zsh has `pipestatus`, not `PIPESTATUS`, and reaching for either is already the wrong shape. Unquoted `$var` does not word-split; an unmatched glob aborts the whole command. **A heredoc carrying prose is ALWAYS `<< 'EOF'`** -- board prose is dense with backticks and an unquoted heredoc command-substitutes every one (it executed `intentd daemon` against the real HOME once; `intentd`'s refusal of an unrecognised argument is the only reason nothing happened). **A clock value goes in through a `2026-09-04 17:43Z` placeholder plus `sed`.** **AND A STAMP IN A FILENAME IS A STAMP, GUARDED BY NOTHING (`0261`):** I typed `wip-prefold-1132Z.md` while `date -u` in the same call said `15:06Z`. `whiteboard-clock-guard.sh` excludes `.history/` by path AND reads only added CONTENT lines, so an archive's own NAME -- the stamp a reader navigates folds by -- is checked by neither arm. `cargo check --workspace --all-targets` -- the flag is the half memory drops. **`stat -f %m` does NOT follow a symlink on macOS; `stat -Lf %m` does.** **A LITERAL IS NEVER A SPLIT POINT; ONLY CHARACTERS PRODUCED BY AN EXPANSION ARE (dc's rule, dc's bug, and I CORRECTED IT WRONGLY FIRST).** **THE DISCRIMINATOR, driven: `IFS=/`, `a="x/y"`, `b="p/q"`, `set -- $a/$b` -> THREE fields `[x] [y/p] [q]`.** The `/` INSIDE each expansion split; the literal `/` between them did NOT, and fused `y` to `p`. **Same character, same `IFS`, different ORIGIN, different behaviour.** The ladder: **A** default `IFS`, `$dir/$two` -> 1 field. **B** `IFS=/` -> `[tests/..] [foo]`, `tests` welded to `..` so a resolver hunting a `..` segment never sees one -- **the bug.** **C** join FIRST, then expand (`j="$dir/$two"; $j`) -> `[tests] [..] [foo]` -- **the fix.**

**AND THE LESSON IS MINE, AGAINST ME, AND IT IS WORTH MORE THAN THE SHELL DETAIL. I TOLD dc THEIR TRUE RULE WAS NOT THE ONE THAT BIT THEM, ON A TEST THAT RAN UNDER DEFAULT `IFS` -- WHERE `/` CANNOT SPLIT FROM ANY ORIGIN.** Their function sets `IFS=/`. **Under default `IFS` both hypotheses predict one field, so my test had ZERO discriminating power and I never checked that it had any.** **RUNNING A TEST IS NOT TESTING; the test must be able to come out the other way.** **This is the meta-control generalisation I handed dc AN HOUR EARLIER -- _a control asserts a DIFFERENCE, so it must be driven against the state it claims to distinguish FROM_ -- arriving back at its author, uninvited, on the very next thing I measured.** **A sample that cannot exhibit the failure is the oldest item on this board and it still caught me while I was being careful.**

**THE SPACE HAZARD IS REAL AND CHANGES SHAPE WITH `IFS` (mine, extended by dc).** Default `IFS`: `$dir/$two` with `two="../a ../b"` gives `[tests/../a] [../b]` -- **the literal prefix attaches to the FIRST field only.** Under `IFS=/` it gives `... [tests/..] [a ..] [b]` -- the space never makes a bare field, it makes a SEGMENT WHOSE NAME CONTAINS A SPACE, and a `..` inside such a segment matches no `..` case. **Different failure, same family; neither reachable from an `include_str!` path in this tree today.**

**F. THE SHARED CHECKOUT.** Canon cannot be split, so every canon commit is silently multi-node. **`add` + `commit --only` is the only safe write; a live `index.lock` is a WAIT, never a removal, and the retry is the SAME command re-issued, never recomposed.** A release build DELETES the shared pair before building (`0196`) while the dirty-tree guard REDIRECTS -- two correct rules that can take the machine down together.

**F2. FILE OWNERSHIP DOES NOT IMPLY DIFF AUTHORSHIP, AND `git status` CARRIES NO AUTHOR AT ALL.** `--only` protects a FILE and not a HUNK (W39); this is one step further -- **the natural way to decide whose a dirty hunk is, _who owns this file_, is not evidence.** ic attributed dc's A1 to me on exactly that inference and nearly had me commit it. **RULE: read the diff for its SUBJECT before accepting or asserting that a hunk is anyone's.** Four independent lines beat one ownership inference.

**F3. CURRENCY IS A PROPERTY, NEVER A VALUE -- AND SO IS EVERY COUNT.** A pin trailing HEAD is the correct steady state. **I handed vc `214/214`; they adopted the wording and were carrying it to hv; it is `216/216` because the ESTATE moved.** The property (store and disk agree exactly, both directions) is true and re-confirmed; the number was stale before it was read. **dc's LIMB, and it is theirs: A VALUE YOU ARE PASSING ON IS ONE YOU HAVE NOT MEASURED** -- relaying launders a claim, which arrives as a peer's measurement and leaves as an estate fact with no author, and the courier has no stake in being right. **AND THE LOOP: a wrong figure came back to its own author through two hops** (vc measured, dc relayed, I published it back to both) -- **a claim returning via a third party reads as independent confirmation and is your own error wearing a different name.** **RULE: hand the PROPERTY and the verb that reproduces it, never the number.** **DISCRIMINATOR for a stale binary: a STORE read survives it; a claim about BINARY BEHAVIOUR does not.**

**G. `0216` AND WRITING CANON.** **The read-verify-retry loop is a REQUIREMENT, not good practice** -- it replaces _one verb at a time_, because **the debouncer sees WRITES, NOT AUTHORS, so spacing protects only against your own burst.** Verify on an OBSERVABLE, never on a duration, never on the tool's `ok`. **After a revert the DISK WINS, so store and disk agreeing AT THE OLD VALUE is the signature, not the exclusion** (vc). `intent st attach` writes store AND canon and NEVER the disk file (`0082`).

**I. A PEER CHANNEL HAS NO TERMINATING CONDITION, AND I AM THE WORKED INSTANCE (ic's W86; vc: _a sign-off is not a terminating condition_).** hv asked me to fold and compact; **I answered peers for hours instead**, and every one of my messages ended with an acknowledgement that invited another. **A reply carrying only agreement is a turn the OTHER node must take**, so courtesy compounds into an unbounded loop that each end reads as the other's initiative. **ic modelled the fix by NOT sending a reply and putting it on their board instead.** **RULE: when a reply adds no new measurement and no decision, it goes on the board, not into the channel** -- the board is the surface that survives, which this morning proved in both directions.

**H2. A FOLD ARCHIVES A COMPLETED MEASUREMENT AND NEVER ASKS WHO WAS STILL WAITING FOR IT. FOUND 2026-09-05, AGAINST ME, ON MY OWN WORK.** vc asked me to size `WP-06`'s _nine unsized CLI rows_ and attributed the note to my board. I grepped the live board and the pre-fold archive, found nothing, and **told them it was not mine.** It was mine twice over: the ask is in an inbox entry **I SENT** (`vc/inbox.cc.md`, 2026-09-03), and **THE ANSWER WAS ALREADY DRIVEN** -- `.history/20260904/wip-prefold-1547Z.md`: _all nine drive to three top-level verbs at rc=2 unimplemented (`ext`, `config`, `learn`) ... six of the nine are unreachable by construction and cannot be sized independently of their verb._ **So the nine ARE the three ruled-out families, the aggregate `L` was the wrong SHAPE rather than the wrong number, and there is no complement to size.**

**THE DEFECT IS NOT THE FOLD AND NOT vc's UNCLEARED INBOX -- IT IS THAT NEITHER END CHECKS DELIVERY.** An inbox records what ARRIVED and never what happened next; a fold records what is DONE and never who was waiting on it. vc kept an ask that had been answered; I archived an answer without confirming it landed. **`clear <sender>` is their control and a `## Holds` condition is mine, and both are downstream of the same missing step.** **RULE: before a fold archives an answer, confirm the ASKER has it -- and when challenged on a claim, GREP WHAT YOU SENT, not only what you kept.** I searched every surface I own and denied my own measurement to the person holding it.

**H. FOLDING THIS BOARD.** **Write the pre-fold copy BEFORE folding** -- it is what made this morning's corruption a restore rather than a rewrite from memory, and **nothing compares a folded board to what it was folded from**, so a reconstruction would have passed every check we have. **Anchor on exact strings, never a prefix**: `index("## Decisions")` matched `"## Decisions owed by hv"`, inverted the slice and duplicated the tail. **`hygiene: ok` PASSES ON A CORRUPTED BODY** -- its contract is the header block, deliberately -- so **verify the section and family counts by hand.** vc hit the same prefix bug an hour after I described it to them and survived only because their data lacked a prefixed sibling.

**A3. THE CORRECT READER, SILENTLY BLIND -- AND THE THING THAT BLINDS IT IS MANDATED.** git's own trailer parser finds **ZERO** `Claude-Session` trailers across today's 58 commits; **55 carry the line.** `%(trailers:key=...,valueonly)` and `git interpret-trailers --parse` both return empty. Cause isolated by discriminator, not reasoned: the `(C)` attribution line is a non-trailer line in the final paragraph, so git rejects the whole paragraph as a trailer block; drop that line and the same body parses. **The harness mandates the `(C)` line, so no node can fix this by being careful** -- and dc, ic and vc all keep _read it off your own commit_ instructions that the obvious verb cannot honour. **RULE: when a structured reader returns EMPTY, positive-control it on a synthesised input before believing the absence.** Empty is a claim about the READER as often as about the corpus.

**B4. A CENSUS KEYED ON AN EXIT CODE MERGES MEANINGS THAT THE MESSAGE KEEPS APART.** Re-driven today: **two pairs share a code and mean opposite things.** `doctor` rc=1 RAN and reported a real finding; `st` rc=1 is a usage error. `config` rc=2 is truthfully unbuilt; `agents` rc=2 is false. **This is why my three-option `agents` menu found no convention to rule toward -- the instrument had already collapsed the distinction the ruling needed**, so the menu was built on a partition that does not exist. Sibling of B2 one level in: **B2 is a seam BETWEEN two populations; this is a seam INSIDE one instrument's key.** **RULE: capture the message beside the key, then ask whether any bin's members disagree with each other.**

**F4. A PUBLISHED FIGURE IS NEVER COMPARED TO THE ARTEFACT IT DESCRIBES -- AND I GOT THE MECHANISM WRONG TWICE BEFORE dc GOT IT RIGHT.** **THE OBSERVATION, MEASURED AND UNDISTURBED:** three of four nodes published a fold figure that differs from the committed board (ic 52502 vs 53409, vc 113 vs 115 LINES, me 20934 vs 21042; dc's two are exact). **Nothing in our tooling compares the two, so the gap is invisible by construction.** **THAT SENTENCE WAS ALWAYS THE FINDING; THE MECHANISM NEVER WAS.**

**MECHANISM ATTEMPT 1 -- _the gate's formatter rewrote it_: FALSE, and its own source says so.** `.githooks/pre-commit` `formatter_refused()`: _is not formatted -- REFUSED, and NOTHING WAS REWRITTEN. your staged bytes are exactly as you staged them._ **MECHANISM ATTEMPT 2 -- _dc measured after the gate, so dc is the control_: FALSE**, read off dc's outcome rather than dc's method, B3 for the third time today and the third time with dc; dc hit three refusals and re-verified after each. **dc's ACTUAL FINDING: THE SECOND WRITER WAS MY OWN HANDS** -- I ran `prettier --write` after a refusal I saw, so the rewrite is not silent and never was. **dc's RULE, WHICH IS THE KEEPER: after a formatter refusal, every figure you have published about that file is stale, INCLUDING ones you took carefully.** Actionable at the refusal, unlike mine.

**F4a. AND MY REMEDY IS BLIND TO THIS REPO'S COMMONEST REWRITE (dc).** _Take the count off the commit_ fixes the SIZE claim and leaves the CONTENT claim unverified: `*Safety checkin*` -> `_Safety checkin_` is **length-preserving**, and `.prettierignore:9-11` names that exact substitution as what cost the estate 266 views. **A byte figure can match the commit EXACTLY while the file still changed.** Same seam as `int canon`'s membership-versus-content, twice in one evening.

**F4b. THE COUNTER ITSELF REPORTED THE WRONG UNIT.** python `len()` on decoded text counts CHARACTERS; `wc -c` and git count BYTES; two `§` (U+00A7) put mine 2 behind, twice, and **I was one step from reporting a SECOND WRITER on a single-writer file with the disproof inside my own instrument.** **RULE: a size compared against `git`, `wc` or a hook is measured in BYTES** -- `len(open(p,'rb').read())`.

**F4c. vc RAN THE ISOLATED EXPERIMENT NEITHER OF US HAD, AND A REFUSING GATE IS A FREE CONTROL.** A refusal freezes the artefact at a known state, so measure, `prettier --write` with **no edit of your own between the two reads**, measure again. On vc's board: **+2 bytes, ZERO lines.** **So for vc's 113-vs-115 the formatter is not merely unproven, it is REFUTED.** One board, one content state -- it does not license _the formatter never adds lines_. **ic's 907 bytes remains unexplained by anything any of us has measured.**

## Decisions

- (2026-09-04, cc->dc, adopted) **A CONTROL THAT LOST ITS SUBJECT PASSES FOR THE WRONG REASON, SO CONTROL THE CONTROL.** dc's positive control was _ic's embed goes green_ and ic had removed the embed; it would have passed under the broken guard too. **dc's answer went past the catch: a META-CONTROL that drives the OLD logic against the same planted pair and refuses if the old logic also passes** (`positive-control-has-no-subject(old-logic-also-passes)`), mutation-tested. **The generalisation is theirs: a control asserts a DIFFERENCE, so it must be driven against the state it claims to distinguish FROM.**

- (2026-09-04, vc, `authority: vc`) **FOUR CLOSES AUTHORISED -- `0090`, `0151` as FIXED; `0095`, `0096` as ARTEFACTS OF `0223` on `0225`'s precedent.** vc drove all of it independently rather than taking my report, and **checked a limb I did not**: `0090`'s title carries TWO claims and closing a two-limb issue on one limb is how a defect returns wearing a fixed label. Limb 2 is satisfied too -- `fields_of` now reads `AddrEntity::Issue { .. } => declared(...)`, so Issue is no longer fieldless, the by-form arm is unreachable for it, and `FieldNotWritable { url, field, why }` is a refusal BY NAME.
- (2026-09-04, vc correcting THEMSELVES) **`0225`'s _the title is the evidence, do not rename it_ WAS RULED WHEN RENAMING WAS IMPOSSIBLE -- a constraint dressed as a choice.** Renaming works now, so it is **re-affirmed deliberately**: the junk titles stay because they are the evidence, NOT because no verb can change them. **An expired limitation obeyed out of habit is not a decision.**
- (2026-09-04, dc REFUTING cc) **THE PRE-COMMIT GATE REWRITES NOTHING AND SAYS SO.** A second writer that you invoked yourself, after a refusal you read, is not a silent one. **The rule is dc's: a formatter refusal staleness-marks every figure already published about that file.**

- (2026-09-04, cc) **A CITATION INHERITS THE CITED CLAIM'S TRUTH AT CITATION TIME AND NEVER UPDATES.** `0223` rests on `0090`+`0151` to conclude a junk row _can only be closed_; all three are open, the underlying verb shipped, and **none of the three noticed.** Currency is a property of a CLAIM (F3) and equally of every REFERENCE to it -- and a reference is worse, because it reads as sourced.

- (2026-09-04, cc) **THE BARE-NOUN CENSUS HAS FIVE STATES, NOT FOUR, AND `rc` IS THE WRONG KEY.** `doctor`/`st` both rc=1, `config`/`agents` both rc=2, each pair meaning the opposite of its partner.
- (2026-09-04, cc) **`agents` rc=2 IS A HALF-FIX, NOT AN UNFIXED ARM.** The remedy line is already subcommand-aware; the sentence above it is not. **A fix is smaller than the board said, and it is blocked on a CONVENTION, not on `agents`.**
- (2026-09-04, cc) **GIT'S TRAILER PARSER IS BLIND TO THIS REPO'S SESSION TRAILERS**, and the mandated `(C)` line is why. Read them with `grep`.
- (2026-09-04, dc's fix, cc measuring) **THE `--all` PARSE LIMB IS CLOSED; THE SEAM THAT HID IT IS NOT.** A fix can discharge a prediction without touching the instrument gap that made it necessary.
- (2026-09-04, cc) **A PUBLISHED FIGURE IS A CLAIM ABOUT AN ARTEFACT, AND THE ARTEFACT IS THE COMMITTED ONE.**

- (2026-09-04, hv via cc) **`config`, `ext` AND `learn` SHIP DECLARED-AND-UNBUILT IN 3.0.1** (2026-08-31). **A general ruling stated later does not vacate a specific one.**
- (2026-09-04, vc) **THE ES RENDERER IS `WP-17`'s BY WP-17's OWN TITLE.** `D56` forbids server-rendered HTML and DESIGNS the client-rendered browser face.
- (2026-09-04, ic via §10a) **`widget` IS A SEMANTIC CLASSIFICATION, NOT A VIEW.** `prose` means the field is long; TUI to `$EDITOR`, browser to `<textarea>`, SwiftUI to `TextEditor`. **Renderers legitimately differ in the WIDGET and never in the model.**
- (2026-09-04, ic REFUTING cc) **`SERVED_BY_DAEMON`'s AXIS IS _REACHABLE BY A CLI VERB PATH_ VERSUS _WIRE-ONLY_, NOT DUAL-PATH.** The table is a `const`, so only payload-free variants fit. **`Op::Set` HAS the dual path and is absent** -- which refutes the axis on evidence I already held. **A one-row table cannot distinguish two axes.**
- (2026-09-04, ic) **ENTITY-TO-JSON HAD TWO HOMES IN `render.rs`**; moved to `intentsvcs` beside `triples`, with the `wp` arm RESOLVING rather than inheriting `_ => None` so `nav.rs:412`'s blank-form prediction is fixed rather than relocated.
- (2026-09-04, cc+dc) **A HOLD DISCHARGED BY THE CONDITION IT NAMED TEACHES THE OPPOSITE OF ONE THAT ERODES.** File the protection BEFORE discharging the hold.
- (2026-09-04, cc+dc) **A DIRECTORY `add` PROTECTS A PATH AND A DIRECTORY PATH IS NOT A FILE.** And **a path that moves on its own is not a path either node can own** (`project.json`'s `todo_watermark`).
- (2026-09-04, cc) **THE CANON STORE AND DISK DO NOT DISAGREE.** The divergence is git-only. Filing writes store and disk atomically; **git is a separate human-gated act and NOTHING REPORTS THE GAP.**
- (2026-09-04, ic) **`git update-index --add` READS THE WORKTREE**, so a synthesised index contains the fresh bytes BY CONSTRUCTION -- it answers _would this path set pass_ and **cannot diagnose any defect whose subject is the index/worktree difference.**
- (2026-09-04, cc) **A SET PASSING TOGETHER IS NOT EVIDENCE ABOUT ITS SUBSETS.**
- (2026-09-03, ic->cc) **THE CLASS IS THE UNDRIVEN NUMBER, NOT THE WRONG ARTEFACT.**
- (2026-09-03, cc+vc) **CONTENTION IS `0216`'s VARIABLE** -- not spacing, not corpus size. **Refusals (`0226`) and silent losses (`0216`) TRADE OFF**, so a single counter prints _fewer losses under load_ and reads as improvement.
- (2026-09-02, cc+vc) **`0216`'s FIX IS DAEMON-SIDE, NOT USAGE DISCIPLINE.** A hazard reachable by an ordinary shell loop cannot be mitigated by how carefully nodes write.
- (2026-09-02, vc) **A RIDER CANNOT BE VIOLATED BY A CASE ITS OWN HAZARD CANNOT REACH.**
- (2026-09-02, vc) **`WP-14` AND ALL 12 OF ITS ACs WERE DESCOPED WHOLE TO ST0069** by hv on 2026-08-30. `AC-09.5`'s wip/boards half goes with it.
- (2026-09-02) **TWO MACHINE PROJECTIONS OF ONE VALUE MUST NOT DRIFT; A HUMAN RENDERING OF IT IS NOT A COPY AT ALL.**
- (2026-09-02) **CONTENT COMPARISON DOMINATES A VERSION COUNTER FOR A COMPARE-AND-SWAP.**
- (2026-09-02, vc) **AN EXCLUSION MUST BE VISIBLE WHEREVER THE BEHAVIOUR IS CLAIMED, NOT ONLY WHERE THE CHECK LIVES.**
- (2026-09-01, hv) **v3.0.1 IS FEATURE COMPLETE, THERE IS NO TAG WINDOW AND NO EXTERNAL CONSUMER, AND COST IS NOT A CONSTRAINT.** The scarcity register is retired as a class.
- (2026-09-01) **A REFUSAL THAT CANNOT SAY WHAT IT FOUND MAKES ITS OWN DEFECT UNDIAGNOSABLE.**
- (2026-09-01) **A REMEDY INHERITS ITS BRANCH'S ASYMMETRY.** Confirm-before-refuse is SAFE on the lock and WRONG on the probe.
- (2026-08-31, vc) **A CRITERION IS NOT REWORDED TO WHAT THE CURRENT STATE SATISFIES.** The ruled form must be HARDER.
- (2026-08-31, vc) **A CLASS CHANGE WITHOUT ITS REASON IS A DELETION WEARING A NEW LABEL.**
- (2026-08-31, ic correcting me) **AN OWNERSHIP SPLIT IS A PURPOSE, NOT A BOUNDARY.** Two hands in one file IS `0206` in miniature.
- (2026-08-31, CORRECTED) **`close --note` STAYS RULED OUT ON A CONTINGENT FOOTING.** The keg has no `edit`; the ruling survives because v3.0.1 ships from the tree.
