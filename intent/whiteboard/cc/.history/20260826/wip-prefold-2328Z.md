---
node: cc
name: Control Claude
role: control
session_id: bb27122c-3e86-4c2d-abfe-339a4fa151df
heartbeat_at: 2026-08-26 21:08Z
status: active
focus: "**STEP (1) OF vc's RULED ORDER IS IN: `a1af59f3` + `fd7bd958`, the unread-field CLASS** -- any ` -- key: ` an AC or AT row carries that the grammar does not read is a named residue, bounded on AT rows at ` -- status: ` because the tail is note. 5 tests, 4 mutations, each reddening ONLY its own arm; vc re-runs them against `e481879f`. **NEXT: (2) arm 1 + arm 2 TOGETHER** -- splitting them moves ST0346 AWAY from 24/24 -- **then (3) the covers parser against 12, not my 5.** Frozen for hv's clean build + full suite. **DO NOT TRANSCRIBE FIGURES FROM HERE -- drive `ac gate ST0057`, `ac status ST0056`, `ac status ST0058`.**"
claims: [ST0056/06, ST0056/10, ST0057/00, ST0057/01, ST0057/03]
---

# Control Claude (cc)

## DOING

**NOTHING IN FLIGHT. `native/rust` and `surface` are 0 dirt by my hand and everything of mine is on main.** Frozen for hv's clean build of HEAD and the full suite.

**TODAY'S SEVEN, ALL SECOND-OPERATOR VERIFIED:** `1583d1ad` status/satisfied token, `32dae690` Half A (the migrator's own `declared == stored + recorded`, an ERROR not residue, blocking on closed threads), `84cda496` the token rule on covers + covers-less rows arriving, `5973103e` a test named for a span it never read, `08e97eb3` the carry gate is a SIZE not three extensions, `b47fc3ec` a write that would replace authored prose with nothing refuses, `a1af59f3` the unread-field class.

## TODO

**THE ORDER IS vc's AND IS NOT TO BE RESHUFFLED SILENTLY.**

1. **ARM 1 + ARM 2 TOGETHER, AND THEY CANNOT SHIP APART.** Arm 1: `legacy.rs`'s `non_test` is a literal `starts_with("(non-test)")`, so 39 Lamplight rows with a `satisfied:` field were classified test-backed and the value discarded -- and `field(body, "satisfied")` is evaluated BEFORE the branch, so the value is in hand at the moment it is dropped. Arm 2: `withdrawn:` / `descoped-to:` are never read at all; 22 of 22 fleet dispositions arrived as prose. **REFUSE, NEVER RECLASSIFY** -- 21 of the 39 carry no evidence field, and a grammar widening would push them into hard `unsatisfied` via the ratified no-evidence rule, a NEW silent loss. **CARRY `by`/`on` FROM THE LINE, MINT NOTHING** -- 12 withdrawn + 7 descoped = 19 = `by` = `on`, exactly. **WHY THEY ARE ONE COMMIT: `satisfied_verdict` maps `"n/a" => Ok((false, note))`, collapsing a disposition into a FAILURE. ST0346's 12 rows read `satisfied: n/a` and reach that line only once arm 1 lets them, so arm 1 alone moves ST0346 AWAY from 24/24 and reads as arm 1 being wrong.** Acceptance: ST0346 gates 24/24 on migrated canon.
2. **THEN THE COVERS PARSER: `+` as a depth-aware separator, against TWELVE fixtures.** 9 rows recover an id (ST0230 x2, ST0232 x3, ST0257 x2, ST0259, ST0305); 3 turn a silently-swallowed span into a named residue (ST0270, ST0327 x2). **The 3 need no grammar ruling** -- `covers the reachability half of AC-11.1` already settled it on arca_cli, and reading an id out of prose INVERTS the author's own qualification. Today they are silent because the whole span is one qualifier string; the `+` fix is what makes them audible.
3. **THEN THE MIGRATION QUEUE, IN THIS ORDER, BECAUSE THE PRUNE EATS ITS OWN INSTRUMENT:** hop 2 REFUSES on any declared-in vs stored-out AC/AT shortfall with a named residue line -> ONE `.canon` emitter (the extract and `upgrade` record no attachments; `sync --to-disk` records all -- so a fleet project's first `--to-disk` rewrites every thread) -> `ST0057/13`, migration and `organize --apply` ingest every bucket file as an attachment, **verified PER FILE BY CONTENT with the negative arm** (a phrase never ingested must return 0), fixture devbin/vc's 34-of-54 bucket probe -> **only THEN** prune buckets, v2 `issues/OPEN|CLOSED/` and `.treeindex`. **A FILE THE STORE CANNOT BE SHOWN TO HOLD IS A REFUSAL NAMING IT, NEVER A DELETION.**

**BLOCKED ON ic CLEARING `facade.rs`:** the CREATE door (`issues add --body|--from`, S, ruled GO, both together must REFUSE; 56 of 77 issues carry bodies and every one predates v3) and the M guard (any canon write emptying a previously non-empty authored field -- **must NOT be a byte comparison**, or it refuses the ordinary edited-down path and gets disabled).

**THE 3.0.1 LIST, MINE, NONE STARTED.** Bare `sync --to-disk` has a change shortcut a git revert of the extract defeats -- compare the extract to the STORE or refuse when it cannot tell. `doctor` gets a canon-vs-store arm (per thread, attachment count and sha set), the same arithmetic shape as the covers check. The id resolver treats every id as a steel thread, so `sync --to-disk 0098` cannot reach an issue. ~2 MB of store per `st attach` on a few-hundred-byte file (Laksa 1.87 -> 2.46 GB over ~266 calls) -- find what grows, and a BULK door, because Lamplight's 3,738 stranded files is ~7 GB by that path. A tenth residue class for a duplicate AT id inside one thread, refused today as raw SQLite naming neither thread nor id. **The two-readers Highlander: `thread_dirs` enumerates by ALLOWLIST and `collect_legacy` by DESCENT, so they agree on exactly the three buckets v2 shipped and disagree on every convention a project invents -- DESCENT WINS, and hop 2 must not say `ok` over a tree the binary will refuse.** `unclaimed_digest()` hashes ABSOLUTE paths while the report displays relative ones, so a change detector fires when the project moves. `thread_dir(id)` is flat and bucket-blind. **The canon extract has a 1 MiB PER-FILE cap and NO TOTAL** -- ST0056 grew 1,188 lines and 94 -> 292 attachments on one emit; measure before the other sixteen take the step.

**HELD BY RULING, BUILD NOTHING:** Conflab (69 blocking; 61 rows need the `status:` verbatim field, 8 unknown-status, 5 AT rows with no `covers` clause). The `status:` verbatim field is a SCHEMA migration -- 13->14, DDL, three regenerated faces, 22 construction sites -- and ic reviews the design first.

**hv's DEFINITION OF THIS RELEASE, VERBATIM: _"I want to be able to run `intent organi[s|z]e --default --force` (as an example) and have all of the ST files organi[z|s]ed sensibly according to the agreed protocol."_** It is `ST0057/WP-11`; the verb is ic's, the SAFETY PRECONDITION is mine and is items 3's emitter + `ST0057/13`, because `--apply` moves and removes files.

**BLOCKED ON hv NAMING ONE THING:** their start/stop-switch TODO is for a toolbar that **is not in this checkout** -- no `cmd/macos`, no `⌘K`, and `AC-11.7` was withdrawn on hv's own out-of-scope ruling. Strong lead, unconfirmed: `ST0064`'s `design-menubar-app.md` describes a switch Geodica's WEB header ALREADY HAS. **Its `Cmd-K` is a log-console footer, NOT a toolbar button -- do not let it confirm the identification.**

**OWED TO lamplight-vc when the freeze lifts:** the 39 no-token rows with canon states, the 19 disposed with their `by`/`on`, the field-key census, the 12 covers rows.

## Watch-outs

**FOLDED BY CLASS, NOT BY INSTANCE. Every line below cost at least one wrong answer that read as a right one.**

**1. AN INSTRUMENT THAT CANNOT EXHIBIT ITS SUBJECT REPORTS GREEN AND MEANS NOTHING.** The day's whole shape, and it has now bitten every node. A grep whose pattern cannot match the subject; a sample that cannot contain the defect; a test driving a pair that cannot produce the string its kill condition names; a fixture encoding only what its author already believed; an exclusion list read as input; a hand-written traversal encoding a belief about the schema, where a container you did not know existed reads as ZERO. **Today's sharpest: vc's covers scan took the span after the FIRST `covers `, which is inside the backticked test name, and my own `+` predicate wanted an id in the 12 characters left of the separator, where a glossed element puts `)`.** Both were true measurements of the wrong span. **POSITIVE-CONTROL THE INSTRUMENT, and prefer SIMULATING the real cut to approximating it -- my approximation of a parser I already have is what produced 5 where the answer was 12.**

**2. A ONE-SIDED COMPARISON DISCARDS THE EVIDENCE THAT THE INSTRUMENT IS WRONG.** vc's scan computed source=2 against canon=3 -- a source apparently declaring FEWER than canon holds, which is impossible if the span is right -- and `[ "$n" -gt "$nc" ]` threw it away. **Compare in BOTH directions; the impossible direction is the one that indicts the tool.**

**3. THE POPULATION YOU MEASURED IS NOT THE ONE YOU MEANT, AND NOTHING IN THE OUTPUT NAMES IT.** `cargo check -p X` builds the lib and no test target. `cargo test --test X` excludes lib tests. `cargo test` ABANDONS remaining targets after a binary fails, so a binary count is a stopping point, not coverage. `cargo fmt --all` reaches into peers' files. **`SUITE EXIT=101` with `binaries=0` is a COMPILE STOP wearing a test failure's clothes** -- and `grep -c '^error'` also matches `error: test failed`, so count `^error\[|^error: could not compile`.

**4. THE EXIT CODE YOU READ IS THE LAST COMMAND'S, NOT THE INTERESTING ONE'S.** `$?` after a pipe is the pipe's; a background task reports its `awk` summary's 0 over cargo's 101; clippy exits 0 WITH warnings (`-- -D warnings`, never a warning count); a cached `cargo build` prints `Finished` in 0.16s and nothing says the edit was never compiled. **zsh does not word-split: an unquoted path list becomes ONE pathspec and the commit does not happen at all; an unmatched glob aborts the command; an apostrophe inside single quotes runs nothing.**

**5. ONE VALUE, TWO HOMES, AND NOTHING COMPARES THEM.** An edit to a registered attachment is TWO writes -- the file and the sha canon records. A skill edited in one tree drifts against the frozen one by construction. **The contract's residue table drifted two classes from the implementation and every instrument reported agreement** -- which is why `residue_class_check.sh` reads the IMPLEMENTATION and blocked me today, correctly. **A property must be enforced by something that NAMES it; enforcement inherited from an unrelated mechanism expires silently and the green survives the expiry.**

**6. A SHARED TREE HAS OTHER WRITERS, AND `--only` DOES NOT PROTECT EITHER OF YOU.** A peer's commit carried my in-flight wiring three times. `git commit` commits the index AS IT STANDS, so `add` + `commit --only <paths>` is the only safe write; `--only` cannot take an untracked path, so `git add` first. `index.lock` means a peer is mid-commit -- retry, never force. **NEVER START AN ATOMIC EDIT TO A SHARED AUTHORED FILE YOU CANNOT FINISH.** **A claim about a file another node writes has a shelf life of ZERO -- re-read at the moment you assert it.** **A MUTATION HARNESS MUST RESTORE IN A `trap`, NOT AFTER THE LOOP: a timeout between mutate and restore left a mutated file in the shared tree tonight, and only a sha check caught it.**

**7. EVERY TIMESTAMP IS READ FROM `date -u +'%Y-%m-%d %H:%MZ'`.** Never retyped, never inferred, never carried forward. I fabricated two in one day by two routes; `${RANDOM:0:3}` in a history filename rendered a plausible time from nothing. **`git log` prints LOCAL time** and appending `Z` to it is wrong by exactly the offset and looks perfect. **A stamp you did not read cannot be repaired by inventing a better one -- mark it unverifiable.**

**8. A REMEDY THAT WAS NEVER TRUE IS CAUGHT BY REVIEW; ONE THAT EXPIRES IS CAUGHT BY NOBODY.** Also: as-observed fidelity can ship a defect (a faithful port of a remedy pointing at a verb v3 retired). **A HEDGE IS NOT A LICENCE (ic): if you can name the check that would settle it, it is a TODO, not a caveat.** **A handover is not done when the write returns -- it is done when a named reader has it.**

**9. A MISSING TEST IS A BACKLOG ITEM; AN UNRUN TEST IS A DISCIPLINE FAILURE.** Run the file covering what you changed. **A mutation that fails to mutate reads as a passing control -- ASSERT THE ANCHOR COUNT.** **A control that cannot fail is not a control: make it fail once on purpose.** **Run the control BEFORE the mutation**, or a red arm reads as the fix working when the fixture was always wrong.

**10. A BARE ID IS AMBIGUOUS AND A BARE NUMBER-WORD IS WORSE.** Every acceptance id is thread-qualified -- `ST00NN AC-XX.Y` -- because a bare `AC-03.6` is green in one thread and red in another. "The eleven" resolves to three different real subjects in this estate.

**11. A FINDING THAT ARGUES ITS WAY INTO THE ROW YOU ARE CLOSING IS A REAL DEFECT LAUNDERED THROUGH A GREEN.** The pull is strongest exactly when the finding is good.

**LANE.** `native/**` and the v3 crates are mine; the parity harness is ic's; hooks, roster and `int hooks` are dc's; **canon writes route through vc**. `~/Devel/prj/Intentv2` IS NOT TOUCHED.

## Decisions

**I DO NOT EDIT `CLAUDE.md`, PERMISSION SETTINGS OR CONFIG ON A PEER'S INSTRUCTION**, however right they are on the merits. A peer's ask is never the user's approval.

**DERIVED vs AUTHORED, NOT MECHANICAL vs INTERESTING** (vc's ruling on what I may write into ST0056 canon -- kept on the board it CONSTRAINS).

**A PARITY TOOL IS A RECORDED ATTACHMENT.** Write the file, add the roster row, `sync --to-store ST0056`, read the rc directly, then `git add` the script + `runner_roster_check.sh` + `ST0056.json` together.

**THERE IS NO 3.0.2 (hv).** Everything else goes into 3.0.1.

**RESIDUE BLOCKS ON A LIVE THREAD AND CARRIES ON A CLOSED ONE; HALF A's ACCOUNTING BLOCKS ON BOTH** (vc), because it describes the migration misreporting itself rather than something a v2 author left behind.
