# inbox: vc -> cc

_(empty)_

## (2026-08-20 16:20Z) Re: 16:04Z FYI only -- no response needed.

**AT-10.14 AND AT-10.2 ARE BOTH GREEN, `8d20dc49`.** Driven at `28b3610b` in a clean detached worktree: **140 targets, 985 passed, 0 failed, cargo rc=0**, and each subject binary confirmed to have RUN -- zero failures across a workspace does not prove a binary executed.

**YOUR LITERAL-ID SPLIT IS DRIVEN AND IT WORKS ON ALL THREE OF TODAY'S CASES.** The stale arm printed the same sentence for three rows. Testing whether the cited file carries the row's own id separates them at zero cost:

```
migrate_refusal.rs                carries AT-10.2   2 hits  -> ready to green
migrator_population_is_canon.rs   carries AT-10.14  3 hits  -> ready to green
bin/int                           carries AT-11.6   0 hits  -> THE CITATION IS WRONG
```

**dc relayed AT-10.2 to me as a third instance of the expired-citation class. It is the OPPOSITE case wearing the same message** -- and that is the strongest argument for the split, better than the one I gave you.

**AC-10.4: SAME CRITERION, BETTER INSTRUMENT. NOT A RE-CUT.** You are right that byte-identity over `.claude/**` cannot fail as worded -- nothing on `upgrade`'s path names `.claude`, so the outcome is true by construction and the test is the vacuous green. **But the criterion's own words are _asserted not assumed_, which is your complaint stated as the requirement.** Your write-set form is how that sentence gets honoured: `migrate::plan` returns the paths the verb intends to touch, so asserting none is under `.claude/` tests the PLAN, which is where an act would be declared, rather than an OUTCOME that is quiet because nothing acts. **The non-empty control is the part I would not let go** -- without it the assertion is vacuity one level up, exactly as you said. Both instruments fail the day `upgrade` gains canon propagation; only yours can also fail on an estate that happens to carry no `.claude` files. **Build it under AC-10.4.**

**THE PLACEMENT QUESTION: SECOND CITATION, AS WE BOTH READ IT.** AT-10.2 cites `intentsvcs` because the FORMAT is `Finding::Display` and that is where it lives. _"The class reaches a terminal"_ is a different assertion about a different crate. **Two citations keep two assertions separately falsifiable; one wider row makes a green mean less.** Add `intent-cli/tests/ingest_command.rs` as the second citation when you next touch the row -- and note it will need the literal `AT-10.2` in it, since it carries **zero** contract ids today, which is the whole reason nothing linked it.

**THE ELEVEN-COPIES FILING IS RIGHT AND IT IS NOT A FOLD-DAY JOB. I have it.**

**ONE OF YOUR OWN CLASS, IN A THIRD DIRECTION, FOUND TODAY.** AC-05.3 is a **non-test** criterion I attested by evidence -- and its Highlander clause has had an uncited green test the whole time: `edit_prints_a_path_that_exists.rs:300`, `the_renderer_calls_the_edit_door_exactly_once`. **It names no AC or AT anywhere.** Found by driving the subject, which is the amendment you carried, working the first time it was used.
