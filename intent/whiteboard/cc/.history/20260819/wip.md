# cc -- archived 2026-08-19

Content folded out of the live board. Kept because it is the day's record, not because it governs tomorrow.

## THE MOVE LANDED -- `16048f82`

**97 files moved, byte-identical, keyed by the id read from CONTENT.** 57 -> `intent/.canon/st/<ID>.json`, 40 -> `intent/.canon/issues/<NNNN>.json` (zero-padded four). Verified AT HEAD with `git ls-tree`, never `git grep` -- git grep reads the INDEX, so a staged-but-uncommitted move reads as landed (vc). 42 legacy `{OPEN,CLOSED}` correctly did not move. `check-ignore` rc=1. Required step: 57 rows, 52 Completed / 2 Cancelled / 2 WIP / 1 Not Started, byte-identical to the pre-move listing. vc re-verified independently across every rename's blob SHA: 97 examined, 97 identical.

**AT-01.7 CROSSED THE MOVE RED-TO-GREEN AND THE WINDOW WAS ALMOST GONE.** `f41d6760` repointed `schema/ddl.sql` in the SAME commit as the resolver, so 7 of 8 `carried by` declarations had been dangling since then and the move REPAIRED them rather than breaking them. The row prescribed "apply the relocation, require RED"; applying it now yields GREEN. **A red-first arm phrased as _apply X, require RED_ carries an UNDECLARED EXPIRY** -- it encodes an assumed starting state, and X landing in two parts puts the red in the gap between them, unobserved and unclaimed. Written before the move by one decision.

## AC-03.16 / AT-03.17 -- LANDED at `4304d8f4`, regenerated at `54735e34`

Three sites, not the two vc named; the third was `ACCEPTANCE_PREAMBLE` itself. Red-first TAKEN by reverting the fix: 205 claims across 266 rendered views, clean with it in place. 205 and not 206 because ST0056's own occurrence is excused by AC-03.16 quoting it -- the documented blind spot confirming itself on its first run. 263 skewed views regenerated after a clear-to-run from all three peers; `doctor` skew 263 -> 0. One attachment's canon text repaired separately at `79570563`.

## Watch-outs retired into code or canon -- the remedy now lives somewhere that cannot be forgotten

- **A CHECK THAT VALIDATES THE FORM OF A CLAIM AND NEVER ITS REFERENT.** `openness.rs` required a declaration to start with `carried by ` and never looked at what it named. Now `every_carried_by_declaration_resolves_to_something_on_disk` (AT-01.7).
- **A PROBE THAT IS RED FOR A REASON UNRELATED TO ITS CRITERION IS WORSE THAN ONE THAT IS SILENT**, because it will be read as the criterion. `declarations_in` absorbs following comment lines, so taking the remainder as the path dangles red-before AND red-after, indistinguishable from a failed move. Now in `carried_paths`'s doc comment.
- **A DETECTOR THAT READS A RENDERED VIEW CANNOT TELL THE GENERATOR'S WORDS FROM THE AUTHOR'S, AND REPORTING THE DEFECT BECOMES THE OFFENCE.** First AC-03.16 checker found 3 of 266, all authored, one of them AC-03.16's own row. Now the attribution rule in `no_view_claims_to_be_truth.rs`.
- **A GENERATED FILE THAT ASSERTS ITS OWN CANONICITY IN ITS HEADER WHILE ITS FOOTER SAYS DO NOT EDIT** is one artefact making two incompatible claims. Fixed in `views.rs`, held by AT-03.17.
- **`bash -n` AT 3.2 IS BLIND TO THE CLASS IT LOOKS LIKE IT COVERS** -- it validates neither parameter-expansion operators nor builtin names, so `${v^^}` and `mapfile` both parse at rc=0. I published "bash-3.2 clean" on the strength of it. Now dc's `tests/unit/templates_bash32.bats`, with a comment arm so documenting the class is not itself an offence.

---

## Localfold 2026-08-19 11:28Z -- the settled narrative of 2026-08-19, archived out of the live board

**INCIDENT 2026-08-19: I RAN `intent upgrade` ON THE LIVE TREE -- THE EXACT COMMAND I HAD TOLD ic AND dc THAT MORNING WAS DESTRUCTIVE AND MUST NOT BE RUN.** I meant to probe whether the verb was still unwired (a three-day-old inbox entry of mine said so). **`upgrade --help` ANSWERED THAT QUESTION. I then ran the bare verb, which is not a probe, it is the command.** Fully reverted -- canon, `events.jsonl` and views byte-identical to HEAD, verified independently by vc. **THE GENERALISABLE PART: a `--help` probe and the verb are one keystroke apart, and I had ALREADY GOT MY ANSWER when I ran the second one. The dangerous command was not needed even for the purpose I ran it for.**

**IT FOUND TWO REAL DEFECTS, BOTH MINE UNDER WP-10:**

1. **`intent/events.jsonl` TRUNCATED TO ZERO -- 19 events destroyed**, 2026-08-18 09:21Z (ST0057 creation) through 2026-08-19 00:30Z (WP ST0057/01 start). **And it wrote NO event for its own run, so the operation that destroyed the audit trail left no trace in it.** **SAME CLASS AS MY 2026-08-17 `.history/` FINDING -- WRITE WHERE APPEND WAS MEANT, silent, plausible artefact afterwards -- and the second instance arrived WHILE THE GUARD PROPOSAL SAT UNRULED.** That is now the argument for the guard, and it is stronger than the incident.
2. **NOT IDEMPOTENT ON ISSUE CANON.** A re-run over an already-migrated project added a leading AND trailing `\n` to **all 40** issue bodies, uniform `1+/1-`. **It reported _already migrated ... their content is unchanged_ WHILE CHANGING IT** -- a verb asserting a property of its own output that its own output refutes.

**AND IT RAN THE AC-00.10 EXPERIMENT vc AND I BOTH DECLINED TO RUN.** Regenerated view: **2** double spaces, `rots silently.**  **AND`. Committed view: **0**, `.** **AND`. Canon: 2. **Renderer emits two; the formatter ate it after generation.** We had it by mechanism and neither of us would rewrite 263 views to confirm it; I confirmed it by accident. Preserved at `~/.intent/evidence/ac-00.10-churn-loop.20260819.txt` with all three states and both binaries recorded, **captured BEFORE the revert** -- the one thing I did right in the sequence.

**ROUTING -- SUPERSEDED WITHIN THE HOUR, AND THE FIRST VERSION WAS WRONG. `hv/inbox.cc.md` IS ALIVE. KEEP WRITING TO IT.** vc broadcast "there is no hv channel", I recorded it as "stop writing", and vc then corrected themselves: **matts's point was that the RESOLUTION protocol was broken, not the channel.** Nodes wrote to `hv/inbox.*.md` expecting him to read it out of band, **which he cannot do**. The file stays as the durable record -- **my three entries survived four days, multiple compacts and a restart, which is exactly why the six `surface:` flags were still findable today** -- and **vc is now the READER, pumping all four inboxes into his chat.** I do not need to double-send to vc; writing the inbox is enough, ping for urgent. **My high-water mark on vc's board is `2026-08-17 18:34Z`** -- everything up to and including the archive-guard proposal is delivered. hv rulings land in vc's `## Decisions`, committed.

**THE PROTOCOL DEFECT, SHARPENED -- AND vc'S FORM IS ONE CLAUSE TOO STRONG.** They said the skill "defines NO READER-SIDE OBLIGATION". **It does: `SKILL.md:59` -- _read + cleansed only by `<node>` (the owner)_ -- and `:107` -- _the recipient is the sole reader and owns its lifecycle_.** And `:16` even anticipates delegation: _the human maintains it (**or has it maintained on their behalf**)_. **So the reader IS named. The defect is worse and more specific than a gap: the obligation is assigned to THE ONE NODE THE PROTOCOL EXEMPTS FROM THE MECHANISM THAT WOULD DISCHARGE IT.** `:16` says hv "is read like any other node" and `:22` exempts hv from the `pickup` loop by which any other node reads. **The escape hatch names no one and requires nothing.** An escalation channel whose reader is structurally unable to read is a queue, not a channel -- **and the failure is invisible because WRITING SUCCEEDS EVERY TIME.** Four nodes wrote diligently for days.

**HOME: ST0056 WP-14, _Coordination model: whiteboard and inboxes in the store, with a bounded API_ -- Not Started, unclaimed.** Who reads, what obligation, how delivery is marked, is design and not prose. vc agrees it lands harder here now. **Interim is a documented local deviation; if matts wants it settled properly that is an argument for pulling WP-14 forward, not for patching the skill.**

**SETTLED TODAY -- AC-00.10 IS THE CHURN LOOP, AND IT DID NOT NEED THE RENDER** (vc). My prediction held, my mechanism was wrong: `criterion_line` (`views.rs:463`) pushes `c.text` VERBATIM and **never calls `cell()` at all**, so the renderer is provably incapable of collapsing it rather than merely observed not to. **I read `cell()`, found it non-collapsing, and generalised from a function that is not on this path -- had it collapsed I would have concluded the opposite branch with equal confidence. Right answer, unearned.** **The discriminator is INLINE CODE SPAN vs PROSE**: ST0050 AC-01.1's surviving pair sits inside a backtick span, ST0056 AC-00.10's two eaten pairs are prose at a `**bold**  **bold**` boundary. **A renderer-side collapse CANNOT be backtick-sensitive; a formatter necessarily is.** Staleness excluded at ONE revision -- at `54735e34` canon=2 / view=0 with ST0050's view regenerated in the same sweep, so both went through the identical pipeline at the identical moment and **ST0050 is a CONTROL, not a counterexample.** **Therefore `doctor`'s 0 was true when taken and false when committed.** The render is now CONFIRMATION and is NOT urgent -- do not rewrite 263 views for it.

**MY OWN PROBE ERROR, AND IT IS THE ONE I QUOTED AT ic THE SAME MORNING.** I placed ST0050's double space in `.tests[].text` by SUBTRACTION across two probes with different reaches; the second greps `.\{20\}  .\{20\}`, a CONTEXT WINDOW, invisible to any hit without 20 chars either side, and never shown capable of returning non-zero for that field. It is in `.criteria[].text`; **`[.tests[]|select(has("text"))]|length` = 0, so the field I attributed it to does not exist on that thread.** **A ZERO IS NOT A RESULT UNTIL THE CHECK HAS PRODUCED A NON-ZERO -- and concluding by subtraction across two probes of unequal reach is how the rule gets violated while looking careful.**

**NEW AND MINE: 114 VIEWS NAME A FILE THAT EXISTS NOWHERE.** `finish(..., "thread.json")` at `views.rs:429` emits _Generated by Intent v3.0.0-dev from `thread.json`_ into every regenerated view; `intent/st/*/thread.json` matches **0 files** post-move. AC-01.7's class -- parses, does not resolve -- **in the footer that tells the reader where truth lives**, caused by my move. **Checked, not assumed, that it is DISTINCT from ST0056 AC-03.16 rather than a gap in it:** AC-03.16's instrument resolves what each named path IS, and it should PASS this -- `thread.json` is canon, not a generated artefact, so naming it as truth is CORRECT under the model. **Truth-claim-wrong and referent-dead are different properties.**

**THE `dirty` MARKER IS ALWAYS ON, SO IT CARRIES NO INFORMATION** (dc). Every binary embeds `intent-source-commit:<marker>-<sha>`; the SHA half is perfect (dc's build stamps `1a8a3cbe` = HEAD, mine stamped `4304d8f4`), **but `dirty` is computed over the WHOLE worktree, so another node's uncommitted board sets it -- and on a five-node board somebody always has one.** My promote-nothing-dirty call was right and rested on the SHA half; had I cited the `dirty` flag as evidence I would have been leaning on a flag that can never be off. **Fix is scope: compute it over `native/rust/**`.** **VERIFIED IN MY OWN CUSTODY off the rescued specimen -- the only readable binary left after the slate:** marker `dirty-18197aafbdb29941fa9c140204d0f5a8c8fae7b5`, ancestor of HEAD, **158 commits behind**, its commit dated 2026-08-18 20:30 local against the binary's 22:27 mtime. **SO THE SPECIMEN CARRIES THREE FAILURE MODES, NOT TWO: currency (158 behind, hash blind, one `strings` diagnoses it); identity (`dirty-18197aaf` is the marker my PIN limb records on THREE distinct `intent` binaries in one day, so source-commit cannot separate them); and DRIFT.** **WITHDRAWN AND REPLACED (vc): I claimed the artefact CONTAINS ~2h of uncommitted code. That is not established -- the 20:30-to-22:27 window gives OPPORTUNITY, never OCCURRENCE, and I reasoned from a clock to a fact about content in a message whose own subject was instruments failing in opposite directions.** **The established finding is stronger: the specimen demonstrates that DRIFT IS UNDETECTABLE.** Both bearing instruments fail and they fail in OPPOSITE directions -- the clock gives opportunity without occurrence, the `dirty` flag is pinned open and gives occurrence without discrimination. Neither separates the two worlds. **vc's resolution, better than mine and adopted whole: drift is NOT a third field, it is THE JOB THE HELD MARKER HALF WAS ALWAYS SUPPOSED TO DO.** Once dc's `native/rust/**` scope fix lands, `dirty` expresses exactly _this artefact contains uncommitted code in its own subtree_. **THE GAP AND THE HOLD ARE THE SAME THING**, so the row is two primary fields (hash = identity, source commit = currency) plus ONE HELD FIELD with a NAMED RELEASE CONDITION, and the partition then CLOSES over all three properties. **A HOLD WITH A NAMED CONDITION IS A COVERED PROPERTY; A HOLD WITHOUT ONE IS THE SHRUNKEN ROSTER IN A DIFFERENT HAT.** **RECORDED, NOT RE-DERIVABLE: the original binary's mtime 2026-08-18 22:27 local (21:27Z), read once by cc via `stat` in this session before dc's rebuild overwrote it and the slate removed it. No artefact carries it now.** **And the rescue was NOT the loss: I used `cp -p` and `stat %m` is IDENTICAL across source and copy -- the 22:27 died in the PREVIOUS session's copy into the scratchpad at 08:28. `cp -p` FAITHFULLY PRESERVES WHATEVER THE PREVIOUS COPY ALREADY DESTROYED; loss compounds through careful copies and every copy after the first looks correct and IS. Be careful at the FIRST copy -- by the second, the evidence of what was lost is gone too.** The `dirty` flag exists to say exactly that and cannot, being always-on. **Minting `two primaries` as complete would be the closure error again; the row should name the property the pair jointly does not cover.** vc's row, flagged not demanded. And dc's corollary needs one sharpening -- a SHA behind HEAD is a cheap SCREEN, not a currency test: **behind-HEAD and cannot-read-THIS-tree are different properties.** We had two signals available and read neither.

**SPECIMEN RESCUED AND IT WAS THE LAST COPY.** dc's rebuild overwrote `target/release/intent` (now `1604836a...`), so the orphaned previous-session scratchpad held the ONLY `f2e4d1f9` binary. Now at `~/.intent/evidence/intent-f2e4d1f9-pinned-but-blind-to-canon`, sha verified, outside the repo and outside `target/` -- **matts has called a CLEAN SLATE and `native/rust/target/` including `target/cc/` is going.** A durable home is still owed; that path is a harbour, not a convention I am claiming.

**EXPOSURE OF THE STALE BINARY WAS 2 OF 18, A FLOOR, NOT "A FULL SHEET"** (ic, measuring a number I had invented and passed to dc as urgency). **`interrupt_rig.sh` builds its own subject per-revision and is structurally immune.** General form, ic's, banked: **a tool that BUILDS what it measures cannot inherit a stale artefact; a tool that READS a shared path always can.** Metadata implicates; content decides -- dc nearly reported `intentd` as a second stale artefact on its hash and checked its contents instead (no `[dependencies]` on intentsvcs; a stub cannot be stale about canon).

0. **FIRST THING NEXT SESSION -- THE FORMATTER CLASS HAS LANDED ON A GENERATED FILE, AND IT IS ON A COMMIT OF MINE.** vc checked canon against the regenerated views, every row, both threads: AT clean (124 + 46 on id/file/covers/status/note, mutation-proven four ways), AC id sets identical 123/123 and 46/46, **exactly ONE real divergence.** **ST0056 AC-00.10: canon carries `rots silently.**  **AND` with TWO spaces; the committed view carries ONE, and canon's exact substring is ABSENT from the view.** Whole committed view carries zero double spaces; canon carries exactly one. This crossed `54735e34`, my 263-view regeneration, in which **`doctor` went 263 skew to 0**. **THE DISCRIMINATOR IS ONE COMMAND: render through `views::render_all` and grep the OUTPUT for the double space.** Present in the render -> the formatter ate it AFTER generation, so **`doctor`'s 0 was true when taken and false by the time it was committed**, and worse, it is a CHURN LOOP -- generate two spaces, formatter collapses to one, doctor sees skew, regenerate two spaces, forever, with each pass looking like a legitimate repair. Absent from the render -> the renderer itself collapses it, canon and view legitimately differ on whitespace, and the real question is why `doctor` calls that zero skew. **Do not guess between them; one grep decides it.** Note this is a NEW VENUE for the class: previously it hit attachments, which are disk-FIRST; a view is disk-DISCARDED, so the formatter's edit is destroyed by the next `--to-disk` and the loop is invisible in any single observation.

1. **AC-01.5's guard is BUILT and UNWIRED; vc has AT-01.5 `red` on that ground.** `lib/templates/hooks/canon-ignore-guard.sh` at `91736056`, six arms driven in a real clone, both load-bearing halves mutation-proven. **Nothing consults it, and a capability with no consumer is not a gate.** Wiring means generalising `WB_GUARDS` past its `intent/whiteboard/` presence test; **dc takes that with the distinct-exit-code re-cut of the same block -- one change, both ends, one commit -- and will not start without matts**, because it re-cuts fail-open semantics every consumer inherits on upgrade.
2. **REDIRECTED -- AT-01.2 AND AT-01.4 ARE NOT MINE AND ARE NOT RUST. READ OFF CANON, NOT REMEMBERED:**

   AT-01.2 intent/st/ST0057/parity/tools/canon_clone_completeness.sh
   AT-01.4 intent/st/ST0057/parity/tools/canon_concurrent_diff.sh

**Both declared SHELL PARITY TOOLS in ic's harness; my drafts are Rust integration tests for `intentsvcs/tests/`. Wrong kind, wrong directory, wrong lane.** The WP-01 group splits **4 RUST / 1 SHELL-HOOK / 2 SHELL-PARITY** and `intent/st/ST0057/parity/tools/` does not exist yet while ST0056's holds 45 -- **the split reads as considered, not stale.** My read, offered to ic as a read: **the contract is right and the drafts are wrong.** Every arm is git-shell work -- real `clone`, `check-ignore --no-index`, worktree vs `ls-files`, two diff sets for disjointness -- and in Rust I would shell out to git for all of it and ship a worse parity tool. **The shell form is native; the Rust was me reaching for the tool I had used most recently.** Handed to ic with three landings and no stake in which; a repoint to Rust would be a canon write and vc's, and I would argue against it.

**AND IT IS THE MIRROR BLIND SIDE I WROTE AS ITEM 4 AND THEN WALKED INTO.** The stale-AT guard catches a `to-write` row citing a file that EXISTS and cannot catch one citing FICTION -- these correctly cite fiction. **What nothing checks is whether the artefact eventually built matches the KIND the row declares.** Green guard, right behaviour, wrong form, wrong lane; only `at lint` at close, or nobody. **Had the clean slate not blocked me this morning my first act would have been to compile two Rust files that no AT row names.** Raised to vc as criterion-shaped: adjacent to AC-01.7 but distinct -- 01.7 is parses-but-does-not-resolve, this is **resolves-to-the-wrong-KIND-of-thing**.

**COORDINATION: whoever writes them writes under `intent/st/` -- the path ic kept clean for me by parking `of_n_closes_over_examined.sh` as `.pending`. The reason for the park dies with this either way; I have told ic to consider it released from my side rather than sit on a clear-to-run my own next commit would break.**

3. **(was 2) AC-01.1 .. AC-01.4 are measurable for the first time.** AT-01.1 and AT-01.3 exist and are green; **AT-01.2 (clone completeness) and AT-01.4 (concurrent diff) are unwritten.** This is the next real block of work. **Both DRAFTED AND PRESERVED at `intent/whiteboard/cc/.history/20260819/drafts/` with a README** -- moved out of session-scoped scratchpad so a terminal restart cannot take them. **NEITHER HAS EVER BEEN COMPILED; treat every API call as unverified** (`Fixture::new()` is an EMPTY project, which was already one correction). AT-01.2 is three arms, AT-01.4 asserts disjointness with both sets guarded non-empty. **MEASURED, and it re-divides AC-01.2 and AC-01.5: git's ignore rules apply ONLY TO UNTRACKED PATHS, so adding `intent/.*/` to `.gitignore` leaves every existing canon file tracked and cloning perfectly, while `git add -A` silently skips every artefact minted afterwards.** Demonstrated end-to-end in a throwaway repo: existing file still tracked, new one 0 files staged, clone carries only the old one, `check-ignore --no-index` reports the rule at rc=0. **So AC-01.2's clone arm CANNOT SEE the AC-01.5 hazard at check time either -- not for a long while, not until enough new artefacts accumulate to be noticed.** The criterion's own framing -- _AC-01.2 checks that canon is committed at check time, by cloning; this one refuses the edit, and the gap between those two moments is where the class lives_ -- **understates it: the gap is not merely temporal, the clone is structurally blind to the rule.** `check-ignore --no-index` is the only arm that sees it, and it is doing work the clone arm cannot, rather than being a cheaper version of it. **`--no-index` is load-bearing: without it git never reports a TRACKED path as ignored.**
4. **ROTTED REFERENCE ON MY OWN BOARD, FOUND BY ME LOOKING FOR WORK -- `ST0057 AT-03.15` DOES NOT EXIST.** ST0057 has no AT-03.15 and no AC-03.15 (its AT-03.x stops at 03.6). **ST0056's AT-03.15 is a different subject entirely**: status `red`, file `write_moves_only_what_changed.rs`, covering **AC-03.14**, and ST0056 AC-03.15 is _a verb that would reduce a population to zero must refuse or name it_. **So my item was mis-qualified in the THREAD and does not match the row in either thread** -- the ten-verb `todo list` / `at lint` hold is somewhere else and is currently unlocated. **A WRONG QUALIFICATION IS WORSE THAN A BARE ID: it reads as precise, so nothing asks.** This is the fifth rotted reference in a durable record and it is under my own standing ruling that every id be thread-qualified -- **qualifying it was not enough; qualifying it CORRECTLY is the requirement, and nothing checks that.** Confirmed the ruling's premise while finding it: **AT-03.6 exists in BOTH ST0056 and ST0057.** Re-locate the real row before citing it again.
5. **The stale-AT guard's mirror blind side** -- catches a `to-write` row citing a file that EXISTS, cannot catch one citing FICTION. Wants its own red-first.
6. **The cold-warm `doctor` arm stays PARKED** -- "a file on disk canon does not know about" changes meaning under sparseness.
7. **`critic rust` and `critic shell` arm ZERO rules.** A green from either means "nothing asked a question". dc owns Half B.

8. **VOID -- dc RETRACTED THE GATEKEEPER FINDING OUTRIGHT AND THE 81 -> 3 CASE HAS NO MEASURED SUPPORT. DO NOT CITE `81 binaries x ~17s` ANYWHERE.** On the clean slate, against 81 freshly compiled never-executed binaries -- the exact population the claim was about -- **first exec is 3-18ms, and 40 back-to-back is 364ms TOTAL. The published figure was off by ~3,300x.** syspolicyd spiked to 50.4% during the burst and blocked nothing. **What survives: the wall-clock gap is real and now UNEXPLAINED (22min40s against 11.87s of summed test time, measured twice); syspolicyd's compile-phase load is real and controlled (14-17% building vs 0.2-0.6% idle) and does not appear to block. The bridge between them is dead.** dc's boundary, kept exactly: _I cannot say matts's 22 minutes was not Gatekeeper; I can only say the mechanism does not reproduce and every number I published for it is void_ -- today's machine is a fresh 1.3GB tree with no concurrent nodes, and refuting a mechanism on a clean baseline says nothing about a loaded one. **The RSS-32KB blocked-before-load observation is still real; it has lost its proposed cause, not been explained away.** My `target/cc` duplication reading stands on its own and is unaffected.

**AND IT TOOK MY RECOMMENDATION WITH IT.** I had told matts to land AT-01.2 and AT-01.4 as ONE test file rather than two, **and the reason I gave was dc's per-binary figure**. Third time today a conclusion rode a number that did not survive, and the FIRST time the conclusion does not survive either. Moot in the end for a bigger reason -- see below.

8. **SUPERSEDED (was: THE 81-TEST-BINARY CONSOLIDATION IS REVIVED AND IT IS IN MY LANE)** (dc, measured, with a retraction of their own earlier stand-down). **`cargo test` here is ~99% macOS code-signature validation and ~1% testing**: 11m20s wall for 8.51s of summed `finished in`, no `rustc` running, test binaries in state `S` at **RSS 32 KB** blocked before their own code loads, same binary first-exec vs second **20,633ms -> 26ms**. Causally confirmed: `syspolicyd` moved exactly 2.00s across one 17.5s first-exec while amfid, XProtect and trustd moved ~zero. **Cargo builds ONE TEST BINARY PER `.rs` FILE DIRECTLY UNDER `tests/`** -- 25 in intent-cli + 56 in intentsvcs = 81 signatures per build, and the same cause behind `deps/` holding 291 executables and `target/debug` at 15GB. **I have asked dc for one measurement BEFORE this becomes a WP: validation is per-binary-per-first-exec, so 81 small -> 3 large is clearly right for a full run and may INVERT on the inner loop**, where a one-line edit would relink and re-validate a 27-file binary instead of a small one. Flat per binary -> consolidate; scales with size -> a few grouped binaries chosen so a typical edit hits the smallest. **My `target/cc` split does NOT escape this and multiplied the tax** -- each node's dir holds its own copies each needing its own validation; I took it as isolation and it is duplication.

**Queued with vc, not mine to land:** AT-01.1 green, AT-01.6 green, AT-03.17 green, AT-01.7 green -- riding with the AC-03.16 row. **Canon writes route through vc.**

---

## FOLD 2 -- 2026-08-19 15:29Z, post-crash. THE GATE MOVED AND IT MOVED IN MY LANE.

**THE RELEASE GATE IS BIDI SYNC PLUS THE INTENTFILES REALISER, hv VERBATIM: _NOTHING ELSE MATTERS._** I recorded it at `e438f755` as the first time it appeared on ANY board as a directive -- measured zero across five boards, present once on vc's as the QUESTION hv asked, and `hv/wip.md` has no `## Standing directives` section at all. **A DIRECTIVE WITH NO WRITE SURFACE IS A MEMORY, NOT A DIRECTIVE** -- the exact inverse of dc's `b645767a` finding that a write surface with no named reader is a queue. **My own morning plan is the proof it reaches nobody: I led with AT-10.12 and AC-10.13, which are `upgrade`, not sync, and nothing in the mechanism contradicted me.**

**THE CHURN LOOP IS CLOSED AT ITS CAUSE AND THE ESTATE PROVED IT.** `.git/hooks/pre-commit:11-16` ran `prettier --write` over every staged markdown file and `git add`ed the result, so all 266 generated views had two writers and the second was silent. `.prettierignore` at `91380693` plus `generated_views_are_not_formatted.rs`, which asks `render_all` for its own roster and reads `prettier --file-info` so it agrees with the formatter by construction. **vc then regenerated 263 views: exactly ONE differs from HEAD, for an unrelated canon amendment, and ZERO emphasis flips. AT-03.18 GREEN.**

**AC-03.17's STATED REMEDY WAS WRONG AND I FOUND OUT BY BUILDING IT.** Over 6,095 authored lines the renderer-side normaliser produced EIGHT changes where the formatter makes SEVEN; the extra was AC-10.9, whose prose runs backticks into words so the real parser's code-span boundary sits where no hand-rolled segmenter can see it. **Rewriting a site the formatter leaves alone is the same loop with the arrow reversed.** `view_determinism.rs:248` had recorded the constraint before either of us looked -- authored prose passes the renderer VERBATIM by rule -- and named the structural fix and declined to take it unilaterally. **vc's re-cut: the row REFUTED ITSELF, arguing the renderer never operates on the string and then three sentences later prescribing that it operate on the string.** And the durable framing is theirs: **FIXED-POINT-NESS WAS THE SYMPTOM. A fixed point is a coincidence somebody maintains forever; an exclusion is structural.**

**AND THE INSTRUMENT THAT SHOULD HAVE CAUGHT IT WAS THE THIRD VACUOUS GREEN OF THE DAY.** `view_determinism.rs`'s prettier arm runs the REAL formatter over a REAL render -- from `sample_thread("ST0056") + sample_thread("ST0043")`, synthetic canon carrying none of the emphasis the estate does. Right verb, right depth, a population that cannot contain the failure. Same as `upgrade_command.rs:222` (fixture with zero issues and no `events.jsonl`, so neither measured member of the incident is expressible) and the stale-AT guard's blind direction (it examines `to-write` rows, never `red` ones that now pass).

**SYNC SCOPE IS DONE, END TO END: `27769d1c` type, `2a08cd93` facade, `212b0075` CLI.** **THE COMPOSITION IS THE FEATURE AND NARROWING THE READ WOULD HAVE BEEN CATASTROPHIC** -- `resync` ends in a whole-store `rebuild`, so handing it only the named threads DELETES every thread not named, silently, in the hand of a node saving its own work. A standing arm asserts the unnamed thread still EXISTS. **AND PROSE COMPOSES TOO, WHICH I NEARLY MISSED: `doc_sections` feeds the FTS index, so a scoped restore replacing every section from disk takes a peer's uncommitted prose into SEARCH while correctly keeping it out of canon -- the same leak one table over, invisible because nothing about canon would look wrong.** vc's words: the defect the work existed to prevent, arriving through the door nobody was watching.

**`--only` DOES NOT PROTECT TWO NODES IN ONE FILE, AND IT BIT ALL THREE OF US IN THREE FILES.** It commits the file AS IT STANDS ON DISK. `facade.rs` (ic's 149 lines against my sync work), `surface/dispatch-table.json` (dc's organize row against my sync row), `render.rs` (dc's one line inside my five). **ic's ordering rule is the right one: a finished region must not land under a moving one.** I refused to go first on `render.rs` because my line without dc's `guide.rs` would have put a red in HEAD -- which is the difference from the table case, where dc carried my row and was right to.

**I BROKE THE SHARED BUILD TWICE AND ic PAID FOR ONE OF THEM.** A red-first test left in a shared working tree made `cargo test --workspace` rc=101 for four nodes. **RED-FIRST IS A PROPERTY OF MY LOOP; A BROKEN BUILD IS A PROPERTY OF EVERYONE'S.** ic's parked `.pending` precedent was on my board and I had not applied it. **And ic's diagnosis is the sharper half: `cargo test --workspace` rc=101 is the SAME OBSERVABLE for _my work is broken_ and _a peer's in-flight file will not compile_ -- an aggregate agreeing with two different worlds, arriving in the build system rather than in a criterion.** Per-target runs are the only thing that discriminates, and ic verified their own fifteen binaries individually rather than inferring from the aggregate.

**TWO `Scope` TYPES, ONE CRATE, AND THE COMPILER ONLY SAVED ME BECAUSE THEY SHARE NO METHODS.** `contract::Scope` selects an AC group WITHIN a thread; `sync::Scope` selects WHICH THREADS a sync takes from its source. `use crate::contract::{self, Scope}` made the bare spelling theirs, so mine bound silently to the wrong type. **dc's point is the durable one: aliasing BOTH removes the class, aliasing one removes the instance** -- the day they share a method with a compatible signature it binds silently. Recorded at the import seam, left as a separate change.

**A REMEMBERED MEASUREMENT RE-SENT IS NOT A SECOND MEASUREMENT** (vc, catching me). I sent AT-01.1 / AT-01.6 to vc twice as measured; both were ONE observation taken before the crash, while canon had carried them green since `f14cfea0` hours earlier. **It reads exactly like corroboration and carries nothing.** vc measured it three times against my two sends. **Read canon, not your notes.** I verified their correction from canon rather than accepting it, which is the same lesson one turn later.

**ROWS LANDED: AT-01.3 (`9c160af3`, 6,268 FIELDS across 97 artefacts, zero differences) and AT-00.2 (`d4a8fb41`, 166 of 166 prose files carried and byte-compared).** Both carry a discriminating arm. **AT-01.3's drops an OPTIONAL field on purpose: a required one is caught by schema validation and refuses loudly, so the LOST arm is only ever exercised by optional fields, which is exactly where silent loss lives.** AT-00.2's names an INVENTED filename that must classify identically to the three real ones -- behavioural, not a source grep -- with `info.md` as the control, because "everything is an Attachment" satisfies the agreement completely.

**AND MY OWN PRECONDITIONS EARNED THEIR KEEP TWICE.** `sync_scope.rs`'s overwrite arm failed on its precondition because I made divergence by mutating through the facade, which PROJECTS AS IT WRITES, so store and disk agreed and the warning correctly had nothing to say. The fixture proving itself, not the feature failing.

---

## FOLD 2 -- THE LIVE BOARD SECTION THIS FOLD REMOVED, VERBATIM

The narrative above is a REWRITE. This is the record: the `## NEXT -- mine` section exactly as it stood
before the fold, so nothing depends on my paraphrase being faithful. Three of the removed lines are header
fields (`heartbeat_at`, `status`, `focus`) and are legitimately updated rather than archived.

```
heartbeat_at: 2026-08-19 12:13Z
status: active
focus: "**RE-AIMED. THE 3.0.0 GATE IS BIDI SYNC AND MY MORNING PLAN WAS NOT POINTED AT IT.** hv had to repeat a directive that appears on ZERO of five boards as a directive -- it is on vc's once, as the QUESTION hv asked -- and `hv/wip.md` has no `## Standing directives` section at all. **A directive with no write surface is a memory**, the inverse of dc's queue-with-no-reader finding. **Recorded as a standing ruling here, which is the only surface I actually read at pickup.** Doing now: AC-03.17's churn loop, which needs no ruling and is the TRUST half of vc's verdict -- make `views.rs` emit what the formatter would leave alone, as `kv()` already does. Blocked on hv: `sync` has no scope."
**AT PICKUP 2026-08-19 (post-compact): hv RULED ALL FIFTEEN CONSOLIDATED ASKS while I was down, and they are recorded on vc's BOARD rather than sent to me -- read them there.** **Evidence store REJECTED** (_more process for the sake of process; the point is the db as SSOT_), so the pinned specimen gets NO durable home and that ask is CLOSED rather than pending. **Footer: use `intent://`** -- ST0057 WP-07's, confirmed. **Surface flags: the four verbosity flags SHIP (`bootstrap --quiet/-q`, `doctor --quiet/-q`, `doctor --verbose/-v`, `fileindex -v`); `--no-backup` AND `--backup-dir` BOTH WITHDRAWN**, the second on measured grounds -- `.gitignore` protects backups by HARD-CODED PATH, so a flag that relocates one writes it where no ignore rule names it. **`doctor` banner: both, explicitly. Toolchain: NO pin, nothing imposed on a user.** **Protocol fix LANDED at `b645767a`**: `SKILL.md:32` now requires the roster to name who reads `hv/inbox.*` and `:34` names vc in matts's own words, so that half of my WP-14 item is DISCHARGED.
**THE FINDING, AND IT IS MINE TWICE OVER: A GREEN TEST ASSERTS EXACTLY THE PROPERTY THE LIVE INCIDENT VIOLATED, AND IT PASSES BECAUSE ITS FIXTURE CANNOT EXPRESS THE FAILURE.** `intent-cli/tests/upgrade_command.rs:222` `running_it_twice_leaves_the_tree_byte_identical` -- driven at pickup, real 8.9s compile, 5 of 5 pass, and it carries a CORRECT anti-vacuity guard. **Its fixture is `config.json` plus `info.md` files (`v2_project` / `v2_thread`, `:47-70`): ZERO issues, NO `events.jsonl`.** Both measured members of the incident -- 40 issue bodies rewritten, 19 events destroyed -- are outside its population BY CONSTRUCTION. **This is the EXTENT limb of my own table: right verb, right depth, a population that cannot contain the failure.**
**AND THE SECOND HALF IS WORSE: AC-10.13 READS `satisfied: yes` WHILE ITS OWN DIAGNOSED CAUSE IS UNTOUCHED.** `migrate.rs:306` still passes `Vec::new()`; `render.rs:1244` still prints _their content is unchanged_. **AT-10.13 IS THE COMMIT GUARD -- it refuses the COMMIT, it does not stop the VERB.** The criterion's two conjuncts are _must not be truncated by any shipped verb_ and _a verb that writes history must record its own run_, and **NEITHER IS TRUE TODAY.** I supplied the criterion and I built the guard, so this is my own row reading DONE over a live defect -- **the worst ambiguity, in my own words, in the standing ruling below.**
**BUILDABLE NOW, IN ORDER:**
1. **TURN THE VACUOUS GREEN RED FIRST.** Extend the `upgrade_command.rs` fixture with an issue and a POPULATED `events.jsonl`, then add two arms: re-run after a hand edit to an issue body in canon (what the backfill did), and re-run over a non-empty event log. **Expect RED on both.** That red is AT-10.12's positive control, which the row demands and I do not currently hold. **THE ISSUE ARM IS NOT migrator-VERSUS-ITSELF**: two runs from one v2 source AGREE, so the arm must perturb canon the way a later legitimate edit does -- which canon permits by design, and which is how a criterion is minted at all.
2. **THEN THE TWO FIXES.** Events: carry the existing log and write the run's own event. Report: stop asserting _content is unchanged_ when it is not. Both `native/**`, both small. **NOTHING RUNS AGAINST THE LIVE TREE** -- AT-10.12's row records that I will not run this verb here again for any reason including verifying my own fix, and that holds.
3. **ROUTE TO vc, WHO WRITES CANON AND I DO NOT.** **AT-03.17 IS GREEN, NOT RED** -- driven at pickup, two tests, real 13s compile, positive-control arm present, and `views.rs` now carries ZERO `source of truth` strings, so **AC-03.16 should close at 51 of 131**. **AC-10.13 needs a row for its own cause, or a re-cut.** **AT-10.12's file name describes a WITHDRAWN property** (`migrator_determinism.rs`) and its crate cannot read the CLI claim the re-cut row is now about.
4. **THE STALE-AT GUARD HAS A BLIND DIRECTION.** It flags `to-write` rows whose file EXISTS; it does NOT flag `red` rows that now PASS. **Already 1 for 1 on a sample of one.** Sweep both threads: every `red` or `to-write` row whose file exists, driven.
**STILL BLOCKED, EACH ON A NAMED PERSON:**
5. **AT-01.2 / AT-01.4 -- hv on the assignment, AND vc STILL OWES THE AC-01.2 RULING** (their board: _rule before minting_). cc measured it: git ignore rules apply only to UNTRACKED paths, so existing canon stays tracked and clones perfectly while only artefacts minted AFTER the bad edit vanish -- **`--no-index` is load-bearing on any AT-01.2.** In my claimed scope (ST0057/01), so no ownership dispute -- **my handover to ic was an owner-from-kind error.** Declared as SHELL parity tools; my drafts are Rust. **The directory does not exist, so the first tool written there BECOMES its form by default** -- `lib_*.sh` at **7 of 39 (settled)** is the only inheritable precedent. **ic's no-WP gap settles in the same breath but must NOT decide it.**
6. **The footer -- 114 views name `thread.json`, which resolves to 0 files.** Mine, from my move. **hv RULED `intent://`**, which canon specifies at 20 occurrences (triple slash) and `native/rust` implements NOWHERE. **ST0057 WP-07 is Not Started.** Scope it; do NOT build a one-off resolver in a footer to dodge the WP.
7. **AT-03.18 -- buildable, but it lands in `intent/st/ST0056/parity/tools/`. ANNOUNCE FIRST.** Two controls, opposite directions, one pass: ST0056 AC-00.10's text must come back from the formatter with the double space EATEN (positive), ST0050 AC-01.1's must SURVIVE inside its backtick span (negative). **REACH must be stated in the output: whitespace only, nothing about table re-padding.**
8. **AC-01.5's guard is BUILT and UNWIRED.** dc takes the wiring plus the distinct-exit-code re-cut, one commit, and will not start without matts.
9. **AT-03.16 (`egest_refuses_to_empty_the_estate.rs`) is MISSING and it is in MY crate.** AC-03.15 is ic's finding carrying the only positive control of that evening that actually works -- 57 and 82, non-zero and observable BEFORE the verb runs, so they cannot be confused with a correct answer.
10. **Parked, unchanged:** the cold-warm `doctor` arm (meaning changes under sparseness); `critic rust` / `critic shell` arm ZERO rules, dc owns Half B.
**VOID -- DO NOT CITE: the 81 -> 3 test-binary consolidation.** dc retracted the Gatekeeper mechanism outright (3-18ms first exec against a published ~30s), and it has since lost its SUBJECT too: post-slate `int test rust` is **9.3s wall / 8.18s summed, 12% overhead**, so there is no multi-minute run left to shorten. **The phenomenon was always real and the cause was wrong; a real effect does not vindicate a refuted mechanism** (dc). `target/cc` stays TESTS-ONLY and I will not produce a release build under a node dir again.
```

---

# FOLD 3 -- 2026-08-19 20:17Z

**Everything below left the live board in the third fold of the day, VERBATIM.** The live board keeps the model, the standing rulings, the lane boundary, the unresolved hv question, and a lean watch-out set; today's settled narrative is here.

**WP-03 closed as work in this window: AT-03.1 `46ab2220`, AT-03.2 `49aa64db`, AT-03.4 `3c6d20f6`, AT-03.5 `18a9ab6b`, AT-03.3 `61b93440`, AT-03.6's `--staged` `19268867`.** Plus `239238df` (export typed errors) and `fb333464` (the daemon that could not open the store).

## NEXT -- mine

**18:19Z. WP-03 IS FIVE OF SIX GREEN. AT-03.6's BUILD IS DONE AND THE ROW WAITS ON A DECISION THAT IS DELIBERATELY NOT MINE.**

| row     | commit     | what it turned on                                                                             |
| ------- | ---------- | --------------------------------------------------------------------------------------------- |
| AT-03.1 | `46ab2220` | opaque bytes in canon as a file; **estate denominator 0 of 280, printed as a zero**           |
| AT-03.2 | `49aa64db` | form follows content BOTH ways; **one sentence, two claims, denominators three orders apart** |
| AT-03.4 | `3c6d20f6` | drift by COMPARISON; **the sharpest arm detects nothing**                                     |
| AT-03.5 | `18a9ab6b` | bytes in no commit; **the staged arm proves index-not-HEAD**                                  |
| AT-03.3 | `61b93440` | the naming gate, **derived from its two consumers**                                           |
| AT-03.6 | `19268867` | `--staged`, so the tool can judge **the commit being made**                                   |

Plus `239238df` (export typed errors) and `fb333464` (the daemon breakage).

**AT-03.6 GOES GREEN WHEN THE TOOL IS GATED, AND ADMITTING IT IS dc's CALL.** vc's condition 3 and I have not been near it -- roster disposition is untouched at 10 gated / 7 manual. My half is built, byte-proven and approved by dc.

**THE FINDING WAS BIGGER THAN THE ROW: EVERY INSTRUMENT THAT WATCHES THE WIRING SAID IT WAS CORRECT.** `runner_roster_check.sh` green at 11 gated / 6 manual, roster and runner and disposition all agreeing. **Only a planted divergence disagreed.** A roster verifies a tool is DISPATCHED; nothing verifies that dispatching it MEASURES the right subject. dc has recorded it; vc has queued it.

**AND A LIVE BREAKAGE FOUND WHILE REBUILDING: `intentd` COULD NOT OPEN THE STORE.** `user_version` 13 against a daemon built from `8d00a490` at `SCHEMA_VERSION` 11, refused at open by design. ic's rung 12 and my rung 13 landed and nothing rebuilt it. **Nobody noticed because nobody had run it.** Both rebuilt -- `intent` `b99789aff383f8bb`, `intentd` `b672a608d56e984d` -- and **both are dirty-tree builds, which is structural in a four-node checkout**; the two even came out of ONE `cargo build` carrying DIFFERENT commit markers, so the marker does not identify the build invocation, let alone the tree.

**NEXT:**

1. **ST0057 AC-00.4 -- CLAIMED, AND vc CONFIRMED IT IS MINE.** It is one of the eight unmet dehydration preconditions and **it was on NOBODY's claims** -- my board carried `[ST0056/10, ST0057/01]` and AC-00.4 is WP-00, so it fell between. vc's point is the finding rather than the assignment: **each node tracked its own preconditions correctly and nothing tracked the union.** Claims now carry `ST0057/00` and `ST0057/03`.
2. **AT-03.6's row move**, once dc decides the gating. My half is landed at `19268867`.

**THE AC-00.4 DESIGN, DECIDED AND NOT YET BUILT, so a handover does not have to re-derive it:**

**The prose goes in a TEMPLATE, never in the generator**, and the project's own rule settles it: _all generated content comes from `lib/templates/` via substitution; no inline heredocs duplicating template content._ v2's generator predates that rule and carries 19 sections of prose inline across `intent/plugins/agents/bin/intent_agents` (835 lines, 15 language-conditional references).

**Porting that prose into Rust would be wrong twice**: it bakes v2's shipped-and-frozen content into the v3 binary, and it bakes THIS project's content into a tool every consumer installs. **AGENTS.md is per-project content generated from a shipped SKELETON plus project state** -- which is what v2 actually does, with the skeleton living in the wrong place.

So: `lib/templates/llm/_AGENTS.md` as the skeleton (v2 ships `_CLAUDE.md` and `_usage-rules.md` already and no `_AGENTS.md`), substitution on the four tokens the existing templates use -- **`[[PROJECT_NAME]]`, `[[INTENT_VERSION]]`, `[[DATE]]`, `[[AUTHOR]]`** -- and language-conditional sections keyed on `config.languages`.

**NOT joining `views::render_all`, and the reason is a hazard rather than a preference.** A `GeneratedView` under a thread DEHYDRATES; root files escape only because `thread_relative` returns `None` for them and `organize` skips them. Making them views would put three root files one classifier change away from being removed, and **`AGENTS.md` is the file this criterion exists because something already emptied.**

**AT-00.4 must NOT assert byte-equality against the files on disk.** Those are v2 v2.19.0 output, stamped as such in their own footers; requiring v3 to reproduce them exactly would target the wrong thing. The property is _v3 can produce each of the three_, plus **the emptied-file case is detected** -- that being the live instance the row cites.

**FOUR THAT ARE MINE TO NOT REPEAT, ALL FROM TODAY:**

**A PEER'S RED CONCEALS MINE AND LOOKS LIKE AN ALL-CLEAR** -- `239238df` shipped with a broken test target because the workspace check aborted on a peer's file before reaching mine.

**A ZERO IS NOT A RESULT UNTIL THE CHECK HAS PRODUCED A NON-ZERO** -- and it bit four times: a short-circuited `cd` leaving `awk` on a stale file, a `| tail` before `$?`, an empty grep after a cwd-drifted command, and **a byte-identical comparison of two identical failures.**

**I FOUND THE SUBJECT-VERSUS-MENTION DEFECT AND THEN BUILT IT.** The critic reported my own doc comment as an instance of what it documented; within the hour my broadcast helper tested for the `_(empty)_` sentinel with a SUBSTRING SEARCH, matched the token quoted inside my own earlier prose, and **wrote hv's inbox back UNCHANGED at rc=0 -- silently dropping the human from two broadcasts.**

**A STAMP AUTHORED IN THE SAME COMMAND THAT READS THE CLOCK IS FABRICATED.** Twice: `16:45Z` for `16:38Z`, caught by the guard; `18:14Z` for `18:12Z`, caught by me. **D42 says read it in its own step and that is the whole of the fix.**

## THE TWO-ENDED CLASSIFIER -- still live, and MORE dangerous after the move

**The discriminator (canon at `c88a6f06`): WHETHER THE UNMIGRATED END CAN STILL PRODUCE A VALUE.** A missing directory cannot -- `NotFound`. A string operation always can. **The move made the silent kind WORSE: `${var#pat}` returning the string unchanged yields `ST0056.json`, which against a FLAT layout is now a plausible-looking filename rather than obvious nonsense.**

**AND THE ID AND THE FILENAME AGREE FOR ALL 57 TODAY -- measured, zero mismatches.** A path-derived identity and a content-derived one are **indistinguishable on the current estate**, so any instrument deriving identity from the path passes every check anyone can run right now. `jq -r '.id'` is correct for a reason nothing in the tree will presently demonstrate.

## Watch-outs -- what is NOT yet encoded anywhere

**A COMMENT DESCRIBING A CONTROL THAT LIVES IN ANOTHER FILE IS A LABEL, AND I TOOK IT AS THE SUBJECT. THIRD TIME TODAY, AND THIS TIME THE LABEL WAS IN SOURCE RATHER THAN IN A GREP.** **AS AT `2870b99d`** `source_commit.rs` stated that **`int macos stage` keeps its own WHOLE-TREE refusal** -- **REPAIRED BY dc AT `db35e178`, so DO NOT GO LOOKING FOR IT AT A LINE NUMBER: it is not there any more and its absence is the fix, not evidence this never happened.** It never refused. `cmd_stage` (`bin/.devbin/cmd/macos:755-795`) records `checkout_clean: no` plus the blocker list, prints `DIRTY -- publish will refuse this`, **and carries on** -- `:810` says _THIS REPORTS AND DOES NOT REFUSE, deliberately_. **I told dc their pipeline would stop in ninety seconds without opening the file that would have told me it would not.** The control is real and it is at `publish`, which has a SECOND wall behind it: a dev version cannot publish, so `3.0.0-dev` is unpublishable by design.

**AND THE SENTENCE SHIPPED IN THE SAME COMMIT AS THE THING IT WAS DEFENDING.** `git log -S` puts it at `2870b99d` 2026-08-19 12:25:12 +0100 -- **the commit that narrowed `DIRT_SCOPE`**. **dc took the fix within the hour (`db35e178`) and wrote the whole class into the replacement comment rather than just the correct sentence** -- which is the only reason a future reader meets it at all, since the repaired file reads as though the defect never existed. So the narrowing's safety argument and the false claim offered as its evidence were authored together. **The argument survives with its reason replaced (SIXTH time today, and this one is dc's rather than mine): _the release gate does not weaken_ is TRUE because the refusal is DOWNSTREAM at `publish`, not BESIDE it at `stage`.** A reader auditing the narrowing gets one of two wrong answers -- _the gate weakened_ (false alarm) or _the comment is right, stop looking_ -- **and I took the second.**

**THE FIXED FIRST INSTANCE OF THIS EXACT CLASS SITS TWENTY LINES ABOVE THE CODE I SHOULD HAVE READ.** `cmd/macos:766` records a field once called `traceable` -- _a word about the ARTEFACTS_ -- on which `publish` refused with a sentence this file's own `artefact_commit_blockers` contradicts. **The remedy chosen there was STRUCTURAL and the reason given was that prose goes wrong again**: _a sentence can go wrong again, and a field named for its own subject cannot carry the other claim in the first place._ Then the same day the same claim went back into prose, in another file, about the same step. **There is no structural remedy available for a comment describing code it does not live beside**, which is why this is worth naming and not only fixing.

**AND `dirty-` KILLS BOTH HALVES FOR DIFFERENT REASONS, WHICH IS BETTER THAN MY INVERSION** (dc, correcting my correction). The `dirty-` prefix kills IDENTITY -- two behaviourally different dirty builds at one commit share one value. Naming `1a8a3cbe` at HEAD kills CURRENCY. I collapsed that into _the halves are swapped_, **which is tidier than the truth and therefore worse.** **CONFIRMED INDEPENDENTLY: the narrowing now works on a shipping artefact** -- `377d5267769a9673` / `99a74328014407f2` at HEAD `26fe1aea`, bare shas with `native/rust` clean and two boards still modified, **which is exactly the condition that used to set the flag on every binary in the estate.** First time a marker in this estate has carried information.

**A LESSON ON MY BOARD IS NOT A MECHANISM FOR ANYONE ELSE, AND TODAY THAT COST dc TWENTY MINUTES.** They ran `bin/int build all --release`, which already defaults to `--release`, producing `cargo build --release --release`; cargo refused and their chain ended in `tail`, so they were told exit 0 twice and spent twenty minutes reasoning about staleness in binaries that had never been rebuilt. **_Never `$?` after a pipe_ is three lines from _`grep -c` exits 1 on zero_ on this board -- same family, both a shell answering a question nobody asked -- and neither reached them.** Twenty minutes is the cheap version; the expensive one is the run where the eaten exit code is a test result.

**`TZ=UTC git log --date=format:` DOES NOT RESPECT `TZ`. ONLY `--date=format-local:` DOES.** `format:` renders the commit's OWN recorded zone, so a format string ending in a literal `Z` produces local time wearing a `Z` I typed myself -- **wrong by exactly the local offset, and it looks perfect.**

    format:        2026-08-19 08:31:17Z   <- the Z is a lie
    format-local:  2026-08-19 07:31:17Z   <- true
    date -u -r $(git log -1 --format=%at) <- independent cross-check

**Committed 2026-08-19 while VERIFYING A PEER'S TIMESTAMPS, in the message where I was being careful, against the trap already on this board in my own words.** Caught only because the figures disagreed with dc's by exactly an hour and dc had no reason to be wrong. **THE FIX IS dc'S AND IT IS BETTER THAN MINE: CONVERT THE OFFSET BY HAND FROM `--date=iso`, DO NOT ASK A TOOL FOR A FORMATTED `Z`.** Reading `+0100` and subtracting an hour **cannot silently produce a `Z` that was never checked, because the offset is in front of you**; asking for a `Z` outsources the one step that has to be verified. **Measured: both hand-converted stamps in dc's durable record survived this trap -- right BY A METHOD, not by luck** (`18197aaf` 19:30:12Z, `53f0c7f8` 10:20:43Z, both confirmed against `format-local`). My `date -u -r $(git log -1 --format=%at)` cross-check is the second line of defence, not the first: **it catches the error after it is made; dc's method cannot make it.**

**MY FLAT-ARM WARNING WAS CONFIRMED, NOT RETIRED, AND I SPENT A DAY DESCRIBING THE STRONGEST OUTCOME AS THE WEAKEST.** I wrote that `canon_commit_check.sh` _will break the moment I move them_. **It broke at `16048f82` 07:31:17Z and a previous dc session fixed it by `3f218744` 07:38:48Z -- seven minutes.** dc's 08:50Z report said it _survived the move_: **true of the file they read, and structurally unable to contain the seven minutes in which it did not.** Neither of us was wrong; the observation could not hold the event. **A REPAIRED ARTEFACT REPORTS AS NEVER-BROKEN, AND THE REPAIR IS INVISIBLE IN THE ONLY THING ANYONE INSPECTS.** Same class as the specimen -- repairing destroys the evidence -- except here it destroyed the evidence for MY OWN claim, which is why I did not go looking for it. **Nobody audits a record that flatters them less than it should; I simply accepted a weaker version of my own prediction and moved on.**

**AN OBJECTION CAN BE INDEPENDENT OF THE CLAIM THAT PROMPTED IT** (minted by vc from three instances today, two mine). My closure objection to the two-primaries row was RIGHT; the drift claim that prompted it was UNESTABLISHED. The rebuild-from-HEAD call was RIGHT on provenance; the urgency I put behind it was a blast radius I invented (ic measured 2 of 18). vc's mtime causation claim was WRONG while the finding it carried was right. **BEING RIGHT FOR A REASON THAT DOES NOT HOLD LEAVES THE CONCLUSION STANDING AND THE REASONING UNBANKED, WHICH IS THE STATE THAT PRODUCES A CONFIDENT REPEAT.** Nothing in the outcome asks to be checked, so the correction only ever arrives unprompted -- pair with _a wrong answer recruits a checker; a right one from a wrong path recruits nobody_.

**LOSS COMPOUNDS THROUGH CAREFUL COPIES.** `cp -p` faithfully preserves whatever the previous copy already destroyed, so **every copy after the first looks correct and IS**. Proven on the specimen: my harbour copy and the scratchpad source share epoch `1787124517` exactly, and the live artefact's 22:27 died at the FIRST copy, in a session that no longer exists. **The place to be careful is the FIRST copy -- by the second, the evidence for what was lost is gone too**, which is why neither vc nor I could inspect our way to it and both had to measure. Corollary that bit vc while correcting me: reading ONE arm of an available two-arm comparison and asserting causation is the clock-to-content move with different operands.

**A CRITERION MUST CLOSE EVERY DEGREE OF FREEDOM THAT LETS A PASSING TEST COEXIST WITH THE DEFECT** (vc):

| limb                                                      | the freedom                                                                 | instance                                                                          |
| --------------------------------------------------------- | --------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| **INSTRUMENT** -- which tool                              | git and mtime give OPPOSITE answers                                         | AC-01.4; the fix was in the criterion, not my reading                             |
| **DEPTH** -- how far in, of SUBJECT and DENOMINATOR alike | an internal subject lets the test reach PAST the thing tested               | `view_determinism.rs` drove `write_all` and stayed green while the estate churned |
| **EXTENT** -- how much of the right KIND                  | a subset of the right kind, **both figures observable, so DEPTH passes it** | 20 of 20 views against a writer population of ~364                                |
| **PIN** -- at which revision                              | measure at a NAMED COMMIT, never `HEAD`                                     | three `intent` binaries carried `dirty-18197aaf` in one day                       |

**A PIN FIXES WHICH ARTEFACT YOU MEASURED WITH AND SAYS NOTHING ABOUT WHETHER IT CAN SEE THE CHANGE UNDER TEST -- AND THE TEST FOR THAT IS A CONTENT TEST, NEVER A CHRONOLOGICAL ONE.** `f2e4d1f9` reported the whole estate unmigrated after the move because it predates the resolver; `strings` gives 0 `.canon/st/` against 9 `/thread.json`. **I had that content evidence and reached past it for the clock, which condemns the wrong binaries: a DIRTY-TREE BUILD CARRIES CODE YOUNGER THAN ANY COMMIT STAMP.** Both debug binaries predate `f41d6760` by 45 minutes while CONTAINING the repoint. Specimen preserved as `intent-f2e4d1f9-pinned-but-blind-to-canon`, and **hv REJECTED a durable evidence store outright** -- _more process for the sake of process; the point is the db as SSOT_ -- **so the specimen has no home and the lesson has to survive in this board rather than in an artefact.** The ask is CLOSED, not outstanding. ic hit the mirror image within the hour -- pin correct, sentence written about HEAD, three commits in between and one of them the fix. **A pin is a claim about ONE revision and carries nothing about HEAD.**

**AND THE DIRECTIONALITY IS WHAT HID IT, ON THE REQUIRED STEP ITSELF.** Before the move a stale binary and a current one AGREE at 57, because the v2 paths still exist; they diverge only at the instant the change lands. **So a before/after conservation check has DIFFERENT INSTRUMENT VALIDITY AT ITS TWO ENDS, and the "before" reading is precisely what certifies the stale instrument taking it.** It then fails as a false positive that reads as catastrophe: names 57 threads unmigrated, prints a remedy that would run `upgrade` over a correctly-migrated estate. **THE STEP CANNOT TELL "THE ESTATE IS BROKEN" FROM "YOUR BINARY IS OLDER THAN THE ESTATE", AND IT REPORTS THE FIRST.**

**A PARTITION MUST CLOSE OVER THE POPULATION THE CRITERION IS ABOUT, AND CLOSING IT OVER THE WRONG WHOLE IS ITS OWN DEFECT.** Three instances, three nodes, one morning. Mine twice: a `st list` breakdown read **53 Completed over 57 rows** because the table's own HEADER carries the word `Completed` -- **only the failure to close exposed it; at one Completed fewer I would have matched the recorded figure and been WRONG WITH CORROBORATION.** Then, applying that fix, a partition over `^CREATE TABLE` read 10 against 11 because `doc_sections` is `CREATE VIRTUAL TABLE`. **A REMEMBERED FIGURE IS NOT INDEPENDENT CONFIRMATION WHEN THE SAME METHOD PRODUCED IT.**

**ATTACHMENTS ARE DISK-FIRST; VIEWS ARE DISK-DISCARDED. SAME DIRECTORY, SAME APPARENT KIND, OPPOSITE DIRECTIONS, BOTH SILENT** (vc). An edit made to the copy inside canon is discarded at the next `--to-store`; a row authored into a view is discarded at the next `--to-disk`. **Nothing in the path tells you which kind a file is.** Corollary: **`git diff --stat` CANNOT EXPRESS SIZE ON CANON JSON** -- an attachment is ONE LINE, so a 15KB re-pad and a typo read identically. Review a canon commit by comparing STRUCTURES.

**AND THE AUTHORING SURFACE HAS A SECOND WRITER THAT IS NOT A NODE, SO `edit -> sync -> commit` CANNOT BE CLOSED BY CARE** (vc, measured). The markdown formatter re-pads tables on save: `critic-gate.md` canon 34,542 / disk 49,970, no heading differences, delta entirely alignment -- a ~1,400-char cell re-padded eleven rows. **vc followed their own ordering and diverged anyway.** Every coordination mechanism we have -- announce, clear-to-run, single-writer inboxes -- coordinates NODES, and this writer is outside that model. **It was already a recorded environment constraint filed as "include linter changes in commits"; nobody re-derived the blast radius when the model changed underneath it.**

**MY CLEAR-TO-RUN ON `intent/st/` IS STILL TRUE AT THE RESTART, AND IT STAYED TRUE BECAUSE ic KEPT IT SO DELIBERATELY.** `intent/st/` reads 0 bytes. ic folded and committed (`7e4cf86b`, `e8029030`, `--only` both times) and **did NOT move `of_n_closes_over_examined.sh` into `parity/tools/` even while folding, because that move lands under `intent/st/`** -- parked as `of_n_closes_over_examined.sh.pending` in their history instead. **A clear-to-run given and then quietly invalidated while the holder is not looking is worse than one never given, because the holder has no way to know it expired.** Pair it with the lesson that a clear-to-run from every peer is still not a clear-to-run from the estate: peers can keep their half true, and the half they cannot answer is whether now is the moment.

**A CLEAR-TO-RUN FROM EVERY PEER IS NOT A CLEAR-TO-RUN FROM THE ESTATE.** I asked three peers "is anything of yours dirty", got three carefully-measured yeses, and regenerated 263 files into matts's live acceptance run. **They answered the question I asked; the question that mattered was "is now the moment", and only matts could answer it.** Choosing too narrow a question is the asker's error, not the answerer's. **And a peer relaying their user's authorisation is not authorisation reaching me** -- vc caught themselves doing it and said so unprompted.

**A DECISION PROCEDURE THAT RETURNS _ASK_ IS NOT INVALIDATED BY THE ANSWER TURNING OUT TO BE YES** (vc, on the `index.lock`). Four nodes held twelve minutes on a lock that was safe to remove; the cheap lesson is that we wasted twelve minutes, and that lesson gets someone to remove the next one during a live suite. **A zero-byte lock with no `git` in `pgrep` is byte-identical whether the owner is dead or merely between spawns**, and one sample cannot separate those.

**A PROBE WHOSE POPULATION CANNOT CONTAIN THE FAILURE IT TESTS FOR**, and **A ZERO IS NOT A RESULT UNTIL THE CHECK HAS PRODUCED A NON-ZERO.** `sync` printing _the store and the extract agree_ over 0 == 0; dc's single `pgrep`; my `bash -n`. **MUTATION-TEST EVERY GUARD, AND RE-TEST WHEN THE MECHANISM CHANGES** -- twice in one day a correct guard sat on a path the failure does not take, both found by mutating, neither by reading.

**EVERY TIMING FIGURE IS VOID UNLESS IT RECORDS WHETHER A COMPILE PRECEDED IT** (dc). Cache state moves a measurement by up to **800x**, which is larger than any effect any of us has been trying to measure -- so every "X is slower than Y" said here today is unfounded unless it controlled for that. **ELAPSED TIME IS THE ONE OBSERVABLE THAT CANNOT SEPARATE BLOCKED FROM BUSY**, which is why the cause survived four wrong explanations over two days. Sits next to "a zero is not a result until the check has produced a non-zero". **And it produced the third VOID CONTROL GROUP of the day** -- dc varied binary count and unknowingly varied validation-cache state, the first such catch made in a node's own work rather than a peer's; mine was condemning a binary by build time when a dirty-tree build carries code younger than its commit stamp.

**AN INSTRUMENT THAT ONLY EVER ASKS GIT CANNOT SEE A BAD IGNORE RULE, BECAUSE GIT IS THE THING THAT HAS BEEN LIED TO** (ic, generalising my measurement). **The disk is the second channel and it is the one nobody consults.** Measured both ways: `check-ignore --no-index` fires at zero new artefacts; worktree-count versus `ls-files` fires from the first new artefact; **clone completeness, `git status --porcelain`, and plain `check-ignore` NEVER fire** -- status returns zero bytes over an untracked new canon file, which is the observable almost every human and script actually uses.

**AN INSTRUMENT THAT WAS JUST REPAIRED AFTER A WRONG ANSWER MUST BE MUTATION-PROVEN BEFORE ITS NEXT CLEAN RESULT IS BANKED** (vc). Their canon-versus-view check returned THIRTEEN differences and **TWELVE WERE THE INSTRUMENT**: a criterion's `state` is an OBJECT with evidence at `state.evidence` and reading it as a scalar was one step from reporting CANON IS LOSSY; `jq | @tsv` DOUBLES BACKSLASHES and puts every later offset out of phase. **Chasing each to a byte is the only reason the thirteenth survived -- reporting the set would have buried the real one, and dismissing the set would have lost it.** Corollary they proved and I should copy: run an UNMUTATED CONTROL through the same path, because a clean result from a freshly repaired instrument is not yet a result.

**GENERALISING AN ASSERTION MOVES ITS FAILURE MODE FROM LOUD-AND-WRONG TO SILENT-AND-VACUOUS, AND THAT IS A PRICE PAID, NOT A MISTAKE MADE** (ic). `changed.len() == 2` is over-specified and brittle and rejecting it was right -- **but its degenerate case fails LOUDLY, because 0 is not 2.** Disjointness is correctly general, refutes `threads.jsonl` by construction, and silently admits the empty model, because **the empty set is disjoint from everything.** Every weakening that makes an assertion robust to a future shape admits new models, and the degenerate one is almost always among them. **So a non-emptiness guard is the SECOND HALF OF THE GENERALISATION, not hygiene that might be forgotten -- an assertion generalised without it is not finished.** And review will never catch it: **`0 == 0` looks silly and gets caught; _the two sets are disjoint_ looks careful, and SOPHISTICATION READS AS RIGOUR.** Met twice in one fold -- a `git ls-tree HEAD` denominator that is empty exactly when canon is ignored, and the disjointness arm -- before it had a name. Prior art to copy rather than reinvent: `realise_plan.sh:79`, `refusing to plan over an empty estate` (ic).

**A GREP HIT COUNT IS NOT A DEPENDENCY COUNT, AND I HELD THE BOARD ON ONE.** I told matts that "seven Rust test files reference `intent/whiteboard/`" made a board write unsafe during a live suite. Measured after the compact: **ZERO tests, Rust or bats, read the live tree.** Six of seven hits are comments or assertion prose; the two real code sites read guard SCRIPT NAMES out of `INTENT_HOME` and build their own board under `mktemp -d`. **This is the AT-01.7 class exactly -- form matched, referent never resolved -- committed by me hours after encoding the detector for it, in prose, where no detector runs.** The COMMIT hazard is real and separate (the gate spawns git into the tree the suite reads); the WRITE hazard never existed. **The cost of a false hazard is invisible, which is why it survives: nothing fails, so nothing asks.**

**IDENTITY AND EXPECTED VALUE MUST COME FROM OUTSIDE THE THING UNDER TEST** (ic). **A TOOL'S DEFAULT VIEW IS A FILTER** -- `st list` defaults to in-progress and returns 2; `issues list` defaults to open and says "no open issues" over 40. **THIS WORKSPACE HAS TWO CRATES AND ITS MEASUREMENTS KEEP HAVING ONE.** **`target/release/` IS A SHARED MUTABLE ARTEFACT four nodes read** -- announce before rebuilding; a rig assembled by SYMLINKING into the real tree is NOT isolated. **`cd` DRIFT: absolute paths. NEVER TRUNCATE A TEST RUN** -- sum every `test result:` line, and `--no-fail-fast` or the run stops at the first failing binary. **`git commit --only` ON AN UNTRACKED PATH STAGES NOTHING.**
