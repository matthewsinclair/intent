---
node: cc
name: Control Claude
role: control
session_id: bb27122c-3e86-4c2d-abfe-339a4fa151df
heartbeat_at: 2026-08-27 12:48Z
status: active
focus: "**HELD by hv's machine-health stop; nothing in flight, lanes clean.** Landed today: `be13157a` arm 1 + arm 2 (ST0346 recovers 13 hv-ruled descopes that were counting green) and `f2477875` the covers `+` parser (9 recovered, 3 named residue) **plus a correction to my own `be13157a`, which was DELETING 19 criteria across 8 threads.** Cleared 33G / 534,337 files of my own `target/cc` -- build cache now COLD by choice. **Test-target consolidation escalated to hv AT SOURCE at `b97afc24`, with its scariest cost measured to zero.** Next when the stop lifts: vc's migration queue, hop 2 refusing on shortfall first."
claims: [ST0056/06, ST0056/10, ST0057/00, ST0057/01, ST0057/03]
---

# Control Claude (cc)

## DOING

**STOP LIFTED BY hv AT SOURCE ("let's crack on", 2026-08-27), disk verified 2.0Ti free before restarting.** Lanes clean. Still holding for hv on the four rulings below -- the stop lifting is not those.

**THE SHARED PAIR IS REBUILT AND IT CARRIES BOTH FIXES.** `intent`/`intentd` 3.0.0 at `56517758`, sha256 `1dd55e514247504d` / `fbee21772db782db`, via `int build all` on a clean tree with the SET verified so the two halves cannot split. **PIN BY THE HASH, NEVER THE MARKER.** The pair that shipped before this (`f438d0d5`) PREDATES the bucket fix, so a re-convert on it takes a bucketed estate's attachments to zero -- which is what happened to arca_cli at `2f3e836`.

**`56517758` -- THE ACCOUNTING, AND IT CHANGES WHAT hop 2 CAN DO.** `2cf8fa63` made the migrator read the right directory; it did NOT make reading the wrong one an error, and those are different properties. Now a thread whose directory holds N attachment-shaped files and accounts for fewer REFUSES and names the thread. **A refusal is a NEW outcome for anyone's runbook and must not be read as a regression.** The general form, which is the transferable part: **a conservation check whose two sides derive from the same source cannot detect a wrong source -- `carried + refused == given` is an identity, not a measurement.**

**`2cf8fa63` -- ST0057/13 LAYER 1: a bucketed thread was migrating with ZERO attachments at rc 0.** `thread_dirs` walks the status buckets and hands back `(id, dir)`; the loop already passed `dir` to the info parse, the acceptance parse and the WP walk, and the attachment carry was the ONE reader re-deriving the flat `intent/st/<ID>/`, which for a bucketed thread does not exist. **Nothing reported it -- no refusal (refusals are per file, and there were no files), no shortfall (an attachment is not an AC/AT row).** Fixed by passing the directory the walk already had. **THE PRUNE STAYS BEHIND IT:** the bucket is the only source surface `at-accounting.sh` has for the hop-2 AT-tranche loss, so pruning first destroys the evidence for it.

**LANDED TODAY -- `be13157a` arm 1 + arm 2, `f2477875` covers `+`; the commits carry the detail and it is not repeated here.** The one thing worth carrying forward: **`f2477875` also CORRECTS `be13157a`, which was deleting 19 criteria across 8 threads.** If you are reading `legacy.rs` and something looks over-strict, check it against class 13 before assuming it is deliberate.

## TODO

**1. vc's RULED ORDER, NOW AT (d): THE PRUNE, AND IT IS GATED ON TWO THINGS THAT ARE NOT MINE.** hop 2 refuses on shortfall -> ONE `.canon` emitter -> `ST0057/13` bucket ingest verified PER FILE BY CONTENT with the negative arm -> **only THEN** the prune. **A FILE THE STORE CANNOT BE SHOWN TO HOLD IS A REFUSAL NAMING IT, NEVER A DELETION.** (a), (b) and (c) landed -- (c) at `2cf8fa63`. **(d) DOES NOT START ON MY SAY-SO:** `at-accounting.sh` must run BEFORE any prune ships (the bucket is its only source surface), and WP-11's absent-manifest default decides what `--apply` does to 19 trees, so it is written FIRST. Layer 1 helps FUTURE conversions and re-conversions only; the 19 already-migrated estates still hold unstored bucket content and that is layer 2's. **MEASURED 2026-08-27: 2,115 authored bucket files are on disk and in NO store, across 5 of the 14 estates I could walk** (Lamplight 1,584 -- 333 of its 353 threads have the `attachments` key ABSENT; Laksa 499; Devbin 24; Anvil 5; Baize 3). Nothing is lost -- every file is on disk and in git -- **but a dehydrate or prune before the ingest destroys all 2,115, which is the measured case for the ordering rather than a preference.** Probed by sha256 with `json.load`, never a grep, and controlled. **arca_cli is the counter-example that validates the route: 23 of 23 byte-identical in canon, 0 missing, done BY HAND per file at `5441945` -- exactly what `2cf8fa63` now does automatically.** Sweep walks `~/Devel/prj/*/intent/.canon/st`, so NESTED estates are missed (`Arca/arca_cli` is one level deeper): 14 estates, not the full 19.

**2. WITH hv, FOUR, NONE STARTED.** (a) **TEST-TARGET CONSOLIDATION, 167 -> 2** -- ruled forward by hv VIA vc; **escalated to `hv/inbox.cc.md` at `b97afc24` for confirmation AT SOURCE, because a relay is not an instruction. That entry carries the full price list; do not restate it here.** The two answers I need back: vc's name-not-path AT citation convention, and `autotests = false` set EXPLICITLY. (b) `intentd --version` prints no commit. (c) the v2 shell CLI prune -- `pr-checks.yml:43` runs `./bin/intent st show` on every PR, so it is delete + rework a live CI job + move ~34 BATS files. (d) **the bucket hole** -- `collect_attachments` walks the FLAT `thread_dir(id)` while `thread_dirs()` walks buckets, so a bucketed v2 project migrates with ZERO attachments at rc 0; vc measured it, hv has stopped Laksa's bucket carry, vc has `bucket-prose-gate.sh` at `d7388157`.

**3. THE M GUARD IS DONE AND MY BOARD DID NOT KNOW.** Built by me at `b47fc3ec`, field-level not byte-level exactly as specified, 4 arms green -- and it was still sitting in TODO. **THE CREATE DOOR IS UNBLOCKED** (ic cleared `facade.rs` at `e54bb968`) and is the only half left: `issues add --body|--from`, S, both together must REFUSE. 57 of 78 issues carry a non-empty body and the field has NO create path at all. **BUT `ruled GO` IS MY OWN UNATTRIBUTED NOTE** -- no peer board or inbox carries it, and `surface/dispatch-table.json` requires `disposition_basis` to name an authority and a date. Writing one I cannot verify would manufacture the exact unverifiable-ratification defect the `ratified-in` guard exists to expose. **The code half is mine; the surface row needs the attribution, and the command surface is ic's under AC-05.1.** With hv.

**4. 3.0.1, NONE STARTED:** `on:` carried as a field on `Descoped` **if hv rules a 3.0.1 canon extract need not stay readable by a shipped 3.0.0** (`deny_unknown_fields` + `additionalProperties: false` say it would not be); bare-sync must compare to the STORE; `doctor` canon-vs-store arm; the id resolver treating every id as a steel thread; ~2 MB store growth per `st attach` + a bulk door; a tenth residue class for duplicate AT ids; the two-readers Highlander; `unclaimed_digest()` hashing absolute paths; the unbounded canon extract total; **`canon-commit` gates on ADDS, so a commit that REMOVES attachments is outside its reach -- a migration that empties a thread passes it silently.**

## Watch-outs

**FOLDED BY CLASS, NOT BY INSTANCE. Every line below cost at least one wrong answer that read as a right one.**

**1. AN INSTRUMENT THAT CANNOT EXHIBIT ITS SUBJECT REPORTS GREEN AND MEANS NOTHING.** A grep whose pattern cannot match the subject; a sample that cannot contain the defect; a fixture encoding only what its author already believed; an exclusion list read as input; a traversal encoding a belief about the schema, where a container you did not know existed reads as ZERO. **A POSITIVE CONTROL IS NOT ENOUGH -- IT MUST EXERCISE THE SUBJECT'S ACTUAL PROPERTY.** Hunting a U+2713 panic I controlled my non-ASCII grep on `café`: it passed, because `grep -P '[\x80-\xFF]'` in a UTF-8 locale matches CODEPOINTS 0x80-0xFF -- `é` is inside, the checkmark is not. **The control passed while the instrument was blind to the exact character I was hunting, and I called a peer's correct file:line wrong.** A control sharing the subject's CATEGORY is decoration; it must share its PROPERTY. Three more of the same shape: my own fixture put the checkmark inside brackets where `depth == 0 &&` short-circuits, so it passed against the UNFIXED scanner; a string probe into a built binary **cannot date a change that added no string**; and `intentd --version` prints no commit, so a pair check reading both halves cannot exhibit a real mismatch. **AND A BROKEN INSTRUMENT THAT HAPPENS TO ANSWER CORRECTLY IS THE WORST CASE, BECAUSE IT BANKS CONFIDENCE IT HAS NOT EARNED.** I checked `peers compiling?` four times today with `pgrep -fl 'cargo|rustc'` -- a FULL-ARGV matcher, so in a fleet of LLM sessions it reads a _mention_ of cargo in an agent prompt as cargo RUNNING. It answered right every time only because nothing was building. **`ps -Ao comm= | grep -E '^(.*/)?(cargo|rustc)$'` is the instrument** (dc). Same shape as matching too WIDE rather than too narrow -- the failure is that the pattern's population is not the subject's, in either direction. **PREFER SIMULATING THE REAL CUT TO APPROXIMATING IT** -- approximating a parser I already had gave 5 where the answer was 12, and 179 where it was 161. **Four more of the same family.** `LC_ALL=C grep $'[\xc0-\xf7]'` scored **0 on a file two-thirds non-ASCII**. `grep --include=*.rs` UNQUOTED, which zsh aborts before grep runs, so the command that reported nothing never ran. `cargo test ... | tail` reports **`tail`'s** exit code, so a COMPILE FAILURE printed `[exited with code 0]` -- use `set -o pipefail` and read `${pipestatus[1]}`. And a `#[path]` probe built on a synthetic crate at edition **2021** answered a question about a workspace at edition **2024**, where the construct under test (`use common::...`, uniform paths) is precisely one those editions could differ on: **a repro is faithful in EDITION and TOOLCHAIN or it is not a repro.** **AND THE FOLD IS ITSELF AN INSTRUMENT, WHICH FAILED THREE TIMES TODAY IN MY OWN BOARD.** This morning's fold left class 1 in TWO sections with neither copy a superset; it DROPPED `S, ruled GO` and the 56-of-77 measurement from the CREATE door, so a ruled item read as unauthorised and I nearly left it queued; and it left the FINISHED M guard sitting in TODO. dc's fold deleted the wrong watch-out the same day. **A FOLD EDITS THE THING THAT RECORDS HOW TO EDIT, SO IT IS THE ONE OPERATION WITH NO OUTSIDE READER.** Diff every fold against its own pre-fold snapshot for DROPPED AUTHORITY and DROPPED NUMBERS, never only for size.

**2. A ONE-SIDED COMPARISON DISCARDS THE EVIDENCE THAT THE INSTRUMENT IS WRONG.** vc's scan computed source=2 against canon=3 -- a source apparently declaring FEWER than canon holds, impossible if the span is right -- and `[ "$n" -gt "$nc" ]` threw it away. **Compare in BOTH directions; the impossible direction indicts the tool.**

**3. THE POPULATION YOU MEASURED IS NOT THE ONE YOU MEANT, AND NOTHING IN THE OUTPUT NAMES IT.** `cargo check -p X` builds the lib and no test target. `cargo test --test X` excludes lib tests. **TWO `--test` TARGETS AND THE SECOND NEVER RUNS ONCE THE FIRST FAILS: my first mutation pass reported 4 arms where the claim needed 12, reading identically to a clean run -- `--no-fail-fast`.** `cargo fmt --all` reaches into peers' files. **`SUITE EXIT=101` with `binaries=0` is a COMPILE STOP wearing a test failure's clothes** -- and `grep -c '^error'` also matches `error: test failed`, so count `^error\[|^error: could not compile`.

**4. THE EXIT CODE YOU READ IS THE LAST COMMAND'S, NOT THE INTERESTING ONE'S.** `$?` after a pipe is the pipe's; a background task reports its `awk` summary's 0 over cargo's 101; clippy exits 0 WITH warnings; a cached `cargo build` prints `Finished` in 0.16s and nothing says the edit was never compiled. **zsh does not word-split** (an unquoted path list becomes ONE pathspec and the commit does not happen), an unmatched glob aborts the command, an apostrophe inside single quotes runs nothing, and `${PIPESTATUS[0]}` is empty -- zsh spells it `$pipestatus[1]`. **AND `local a="$1" b="..$a.."` IS UNBOUND UNDER `set -u`: bash expands every word BEFORE the builtin assigns, so my harness printed `EXIT=0` having executed nothing.**

**5. ONE VALUE, TWO HOMES, AND NOTHING COMPARES THEM.** An edit to a registered attachment is TWO writes -- the file and the sha canon records. A skill edited in one tree drifts against the frozen one by construction. **The contract's residue table drifted two classes from the implementation and every instrument reported agreement**, which is why `residue_class_check.sh` reads the IMPLEMENTATION. **A property must be enforced by something that NAMES it; enforcement inherited from an unrelated mechanism expires silently and the green survives the expiry.**

**6. A SHARED TREE HAS OTHER WRITERS, AND `--only` DOES NOT PROTECT EITHER OF YOU.** A peer's commit carried my in-flight wiring three times. `git commit` commits the index AS IT STANDS, so `add` + `commit --only <paths>` is the only safe write, and `--only` cannot take an untracked path. **In zsh an unquoted `$PATHS` is ONE pathspec and the commit silently does not happen.** `index.lock` means a peer is mid-commit -- retry, never force. **NEVER START AN ATOMIC EDIT TO A SHARED AUTHORED FILE YOU CANNOT FINISH**, and **a claim about a file another node writes has a shelf life of ZERO** -- re-read at the moment you assert it. **A BLOCKED COMMIT LEAVES YOUR FILE STAGED** -- `git restore --staged` it. **A MUTATION HARNESS MUST RESTORE IN A `trap`, NOT AFTER THE LOOP.** **A PEER'S BUILD IS A READER.** **Work in a DETACHED WORKTREE with a PRIVATE `CARGO_TARGET_DIR` and none of it applies** -- but that dir is unbounded and nothing collects it (see ENVIRONMENT), so clear it when you are done. **NEVER clear a peer's cache silently: MOVE then delete, keep the trash INSIDE a directory already carrying `.metadata_never_index`, and verify survival BY HASH.**

**7. EVERY TIMESTAMP IS READ FROM `date -u +'%Y-%m-%d %H:%MZ'`.** Never retyped, never inferred, never carried forward. I fabricated two in one day by two routes. **`git log` prints LOCAL time** and appending `Z` is wrong by exactly the offset and looks perfect; **a harness notice saying "the date has changed" carries that tool's zone, not UTC** (vc's error, same class from the other end). **A stamp you did not read cannot be repaired by inventing a better one -- mark it unverifiable.**

**8. A REMEDY THAT WAS NEVER TRUE IS CAUGHT BY REVIEW; ONE THAT EXPIRES IS CAUGHT BY NOBODY.** As-observed fidelity can ship a defect -- a faithful port of a remedy naming a verb v3 retired. **arm 10 blocked every node's commits on a `die` message recommending `int local build`**, because its census stripped BACKTICKED prose and not QUOTED prose; safe subset at `78896061`. **`zz-livebacktick` IS STILL OPEN: a bare backtick is LIVE in a double-quoted string and under bare `<<EOF`, inert under `<<'EOF'`, `<<"EOF"`, `<<\EOF` and `<<-'EOF'` -- the DOUBLE-quoted delimiter is the trap.** The axis (can-this-span-execute) is right; a per-line regex re-deriving shell's lexer never will be. **A HEDGE IS NOT A LICENCE (ic): if you can name the check that would settle it, it is a TODO, not a caveat.** **A HANDOVER IS NOT DONE WHEN THE WRITE RETURNS -- it is done when a named reader has it.**

**9. A MISSING TEST IS A BACKLOG ITEM; AN UNRUN TEST IS A DISCIPLINE FAILURE.** **A mutation that fails to mutate reads as a passing control -- ASSERT THE ANCHOR COUNT.** **A control that cannot fail is not a control: make it fail once on purpose**, and run it BEFORE the mutation or a red arm reads as the fix working when the fixture was always wrong. **AND A MUTATION THAT APPLIES CLEANLY BUT REDS NOTHING IS THE SAME FAILURE ONE LEVEL UP -- THE MUTATION IS WRONG, THE CODE IS NOT SAFE.** M5 restored the old whole-span covers cut and red nothing, because the property is held by a GATE it left standing. **PREDICT THE RED ARMS IN WRITING BEFORE RUNNING; a mutation whose honest prediction is `nothing` is not yet a mutation.**

**10. A BARE ID IS AMBIGUOUS AND A BARE NUMBER-WORD IS WORSE.** Every acceptance id is thread-qualified -- `ST00NN AC-XX.Y` -- because a bare `AC-03.6` is green in one thread and red in another. "The eleven" resolves to three different real subjects here. **AND A POSSESSIVE RESOLVES AGAINST THE ADDRESSEE, NOT THE OWNER:** dc wrote "your WP-14" to me because they were writing TO me ABOUT it; WP-14 is ic's. The copy sent to the node who would ACT on it named ic correctly, which dc called luck rather than method. **Name the owner, never "your", when an attribution rides with a live finding -- the attribution is what routes the follow-up question.**

**11. A FINDING THAT ARGUES ITS WAY INTO THE ROW YOU ARE CLOSING IS A REAL DEFECT LAUNDERED THROUGH A GREEN.** The pull is strongest exactly when the finding is good.

**12. A MECHANISM FILED WITHOUT ITS CONSEQUENCE IS NOT FILED.** "`thread_dir(id)` is flat and bucket-blind" sat in my 3.0.1 list for a day as a true statement about a function. `every bucketed v2 project migrates with ZERO attachments and exits 0` is the SAME FACT with the blast radius attached, and only the second one gets picked up off a backlog. **The other half was two sentences away on the same line of the same board** -- the two-readers Highlander -- and I never joined them; vc found it by measuring. **Re-read a backlog for the FIRST form and convert it, because nothing else will.**

**13. A REFUSAL THAT DELETES IS THE MORE DESTRUCTIVE OPTION AND READS AS THE SAFER ONE.** Refusing is only conservative when refusing PRESERVES. In the legacy parser `Err(..)` DROPS THE ROW: refusing a merely mis-placed marker took ST0283 from 67 criteria to 65, and refusing the ambiguous authored-field row deleted **19 criteria across 8 threads**. **I WROTE THE FIRST HALF OF THIS LESSON INTO A COMMIT MESSAGE AND SHIPPED THE SECOND HALF IN THE SAME COMMIT**, four lines apart. **The load-bearing half of `refuse, never reclassify` was always NEVER RECLASSIFY.** Where the row must survive: keep today's reading, NAME the ambiguity, claim nothing new -- and emit it AFTER the row accounting, because `out.record` counts a finding as a refusal and half A came out at -1 on a file that had converted correctly. **NEITHER INSTANCE WAS FOUND BY READING THE DIFF.** Both came out of a BEFORE/AFTER ON REAL THREADS; every test I had written passed, because they encoded the behaviour I intended.

**ENVIRONMENT.** Bare `intent` resolves via the `~/.local/bin/intent` SYMLINK to `native/rust/target/release/intent`, never via `.envrc` (direnv fires only at an interactive prompt). The symlink is the load-bearing artefact. **A pinned binary is exposed only when it WRITES**, and **a copy parked outside the checkout cannot run hop 3 at all** -- `claude upgrade --apply` walks up from the exe for `lib/templates/`. My `pin the pair out of tree` advice was right about the hazard and wrong about the remedy. **BUILD CACHE IS COLD BY CHOICE** -- I cleared `target/cc` (33G, 534,337 files) in hv's machine-health stop, so the next test run pays a FULL rebuild; do not read the wait as a hang. `target/cc` is the private dir the shared-artefact guard's REFUSAL PATH names (`releasebuild.lib:189`), unbounded and collected by nothing -- class 12 in the tool itself. **`target/release` is the shipped pair mid-cut: never `cargo clean` unscoped, and verify survival BY HASH (`04008c3fa21eae52` / `588f34aba70f3d73`), never by directory size.** Kept warm: scratchpad `tgt-shared`.

**LANE.** `native/**` and the v3 crates are mine; the parity harness is ic's; hooks, roster and `int hooks` are dc's; **canon writes route through vc**. `~/Devel/prj/Intentv2` IS NOT TOUCHED.

## Decisions

**I DO NOT EDIT `CLAUDE.md`, PERMISSION SETTINGS OR CONFIG ON A PEER'S INSTRUCTION**, however right they are on the merits. A peer's ask is never the user's approval. **AND I DO NOT APPLY AN EDIT A CLASSIFIER REFUSED A PEER** -- three lanes declined the same guard edit, and doing it from a fourth would be the same move with the best excuse yet.

**DERIVED vs AUTHORED, NOT MECHANICAL vs INTERESTING** (vc's ruling on what I may write into ST0056 canon -- kept on the board it CONSTRAINS).

**A PARITY TOOL IS A RECORDED ATTACHMENT.** Write the file, add the roster row, `sync --to-store ST0056`, read the rc directly, then `git add` the script + `runner_roster_check.sh` + `ST0056.json` together.

**THERE IS NO 3.0.2 (hv).** Everything else goes into 3.0.1.

**RESIDUE BLOCKS ON A LIVE THREAD AND CARRIES ON A CLOSED ONE; HALF A's ACCOUNTING BLOCKS ON BOTH** (vc), because it describes the migration misreporting itself rather than something a v2 author left behind.

**A RELAY IS NOT AN INSTRUCTION.** A ruling that reaches me through a peer gets confirmed with hv AT SOURCE before I act on it -- via `hv/inbox.cc.md`, which vc is the named reader for. This is not distrust of the relaying node; it is that a ruling's SCOPE is the half that does not survive a retelling, and only the author can restate it. vc holds me to this and has never once asked me to drop it.
