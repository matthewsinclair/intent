---
node: cc
name: Control Claude
role: control
session_id: 98a46c38-f370-4d67-b2c5-c2536e0ae8f9
commit_session_id: 01XYetoGJWvBxvL4PE8sGZTu -- WITNESSED on eaef2a04f, 0b5d46c96 and 018016241, and it SURVIVED A COMPACT UNCHANGED at 15:55Z -- the harness re-announced the same value and my own last trailer agrees. dc's and ic's lines both say a bounce mints a new one; that may still hold for a RESTART, but a compact is now measured not to. Honest caveat: I authored those trailers, so reading them back is not independent of the harness. POINT-IN-TIME.
heartbeat_at: 2026-09-04 16:42Z
status: active
focus: "STILL HOLDING FOR hv ON ONE COMMIT (0232, OPEN on a current binary). CORRECTION AT SOURCE: I published NINE DAYS OLD twice and it is WRONG -- 28c1c094e was committed TODAY 15:28Z and the release binary built 15:33Z; it is 17 commits behind HEAD, not nine days stale. vc's error, propagated by dc, adopted by me WITH THE DISPROOF IN MY OWN TOOL OUTPUT (I echoed rel binary mtime 2026-09-04 15:33Z in the very call where I chose the debug binary). MECHANISM: stat -f %m does NOT follow a symlink on macOS and reads the LINK's date; stat -Lf does. Two wrong numbers that agree are one wrong fact stated twice. THE ESTATE POINT STANDS UNEMBELLISHED: bare intent is behind A1 and Op::Form, every node hits it by default, self_provenance_check is the only reporter. STORE-TO-DISK: agrees exactly both directions, at whatever count issues list --kind all reports when asked -- property, not value. NO FIGURES FROM MEMORY; RUN THE VERBS."
claims: [ST0056/06, ST0056/10, ST0057/00]
---

# Control Claude (cc)

## DOING

**NOTHING IN FLIGHT. FOLDED FOR A COMPACT; RESUMING UNDER vc's DIRECTION ON THE BOUNCE.** Both commits landed on matts' approval and `render.rs` is handed over. **Pre-fold verbatim: `.history/20260904/wip-prefold-1547Z.md`** -- today's narrative is there and is not repeated here.

**WHAT THE BOUNCE MUST NOT RE-DERIVE, because each cost real time today:**

- **`WP-06` IS RULED OUT, NOT UNSTARTED.** hv ruled 2026-08-31 that `config`, `ext` and `learn` **ship declared-and-unbuilt in 3.0.1**. **The ruling is in TWO Rust doc comments (`render.rs`, `remedies_are_reachable.rs`) and one `MODULES.md` cell, and on NO board** -- I found it by searching the registry before creating a module. **Do not re-size those rows; sizing them was work for a build nobody authorised.** hv's later _everything is in 3.0.1_ does NOT silently reach them -- hv's own rule: a general policy stated after a specific ruling does not vacate it.
- **`WP-08` IS CLOSED at 12/12 and its hold is DISCHARGED BY ITS OWN TERMS**, not overridden. The gap it protected is `0232`.
- **The `browse` daemon half is NOT mine and is NOT a design collision.** `D56` forbids server-RENDERED HTML and `tui-design.md` §10a **designs the browser face** -- ES modules, list-left detail-right, `localStorage` auth with `daemon_bridge.js` as the precedent. **The ES renderer is `WP-17`'s by WP-17's own title.** I escalated this wrongly; ic read the design document neither vc nor I had opened.
- **`SERVED_BY_DAEMON` and the `implemented_check.sh` items are still mine and still startable.**

## TODO -- startable, mine, smallest first

- **XS** **Commit `0232`.** It is UNTRACKED: the daemon-conformance gap exists in store and on disk, not in git. Canon paths are entangled across nodes, so agree the path set with dc first.
- **S** **`intent agents` bare CLAIMED 2026-09-04 (vc routed it to me).** It answers rc=2 _is a known command that is not implemented yet_ while `agents --help` lists `init`, `generate`, `sync`, and `agents sync` is the verb `in-essentials` rule 2 orders every agent to use. **Same class as `browsed()`, same file, fixed by me this morning at `eaef2a04f`: a dispatch arm making a false claim about the build.** The canon half is dc's; the false refusal is `render.rs` and is mine.
- **XS** `0095`/`0096` -- CLOSE as never-specified, reason on the record. Driven: empty in title AND body. **They are `0223` debris.**
- **XS** File the `implemented_check.sh` false-positive class. **ONE CANDIDATE ELIMINATED 2026-09-04**: bare-versus-armed probe disagreement is NOT it -- the script already fills leaf-row subcommand slots and derives its marker from `render.rs`, with A/B1/B2 controls.
- **S** `0063` -- FIELD MIGRATION: the title's 187 chars into the body, short title left. **NOT a close.**
- **S** `0205` -- vendored fourth block ACCEPTED with its reason at `bin/.devbin/lib/builtins:66`.
- **S** Migrator-commit -- `migration.md` Phase B step 7 and `AC-00.8` stop claiming _one commit_. Correct doc and row; do NOT build the commit.
- ~~`implemented_check.sh` fix -- classify on marker AND rc=2.~~ **STRUCK 2026-09-04.** The file classifies on the MARKER, never on the exit code, deliberately, with controls B1 and B2 as _one proof from two sides_ that **exit 2 alone does not classify and the marker alone does**. Adding `rc=2` as a conjunct would have introduced dc's W72 blindness -- an `rc=1` clap refusal is invisible to an `rc==2` predicate -- into an instrument built without it. **I cannot reconstruct what I meant, and a fix I cannot justify against the file's own controls is not a fix.** W72's third instance, and it was in a queue rather than in a tool, which is worse: a shipped instrument gets driven and can go red; a TODO row just gets built.
- **M** `0192` RULED IN -- refusal in `info_read_back`, placement already decided.
- **S-M** `SERVED_BY_DAEMON` is ONE entry (`render.rs:235`) and it is a `const`, so it can only ever hold PAYLOAD-FREE variants. The projection is of the dispatch table's payload-free arms, and **it must REFUSE on a daemon-eligible arm that carries arguments rather than skip it** -- silence there reports a clean table while a verb quietly loses daemon coverage. Size JUDGED; the hazard was found by ic refuting my own framing.

## Holds -- mine, with the condition that releases each

- **M** `AC-06.1`'s coverage half -- RELEASED WHEN a burn TSV **covering the estate** exists AND `INTENT_BIN` resolves to one binary. **RE-DRIVEN 2026-09-04 16:00Z, STILL UNMET, AND I HAD THE COUNT WRONG:** a burn TSV EXISTS (`parity/tools/burn-baseline.tsv`) -- existence is not coverage, which is the distinction this hold was written to keep. `INTENT_BIN` is **unset**, and `bin/intent`, `target/release/intent` and `target/debug/intent` are all present with a fourth on PATH at `~/.local/bin/intent`. **This line said _three_ and it is arguably four** -- the same rot the gate-figure rule names, in my own hold condition. `coverage_map.sh` refuses to publish and is RIGHT to.
- **L** `0216`/`0226` fix -- RELEASED WHEN a monotonic version the ingest does not own exists. The obvious fix collides with `written_at`, which the ingest rewrites wholesale.

## Decisions owed by hv -- question, options, recommendation

**TWO WERE STRUCK 2026-09-04 RATHER THAN ANSWERED, AND THE REASON IS THE SAME BOTH TIMES: hv HAD ALREADY RULED.** `config` bare is answered by the 2026-08-31 declared-and-unbuilt ruling; `WP-08: endorse or override` is moot because WP-08 is closed. **Asking either again is the repeated-question defect.**

- **Should `at green` run the L3 arm?** (i) warn, do not refuse (ii) refuse (iii) leave. **REC (i)** -- refusing breaks the legitimate write-then-cite order.
- **`INTENT_BIN` flip and re-baseline -- which order?** (i) flip then re-baseline (ii) re-baseline then flip. **REC (i)** -- the default is `bin/intent`, the v2 SHELL SCRIPT, and it is three binaries not two. **Option _neither this cut_ is STRUCK: there is no cut to be outside of.**
- **`burn.sh` re-run, or accept `AC-06.1`'s coverage half red?** (i) run (ii) accept red and say so on the row (iii) descope the half. **REC (i), and it is hv's because full-suite runs are.**
- ~~`agents` bare is `pending-hv`; REC match whatever `config` does.~~ **WITHDRAWN 2026-09-04: vc's census refutes the premise.** Bare noun verbs split **FOUR** ways -- `st`/`wp`/`ac`/`at` refuse rc=1 _requires a subcommand_; `issues`/`lang`/`modules`/`plugin`/`llm` answer rc=0; `doctor` runs; `config`/`agents` claim unimplemented at rc=2. **My three-option menu contained none of the four states, so there was no convention to rule toward and the question was unanswerable as posed.** My own caveat -- that the recommendation rested on a reading and not a census -- is the thing that turned out to matter. Re-pose against the four measured states if a convention is still wanted.
- **Flip `RUSTDOCFLAGS="-D rustdoc::broken_intra_doc_links"` to a gate?** (i) clear the class then gate (ii) stay report-only (iii) gate now. **REC (i)** -- five of six targets I once called absent exist under another name. Lint half is dc's.

## Open, no owner

- **`--all`: 16 INVOCATIONS, 9 `install` + 7 `uninstall`, SPLIT 5/7/2/2 -- CONFIRMED THREE WAYS (mine, vc's independent re-drive, and a boundary-anchored re-drive with a negative control returning zero `--allow*`).** **14 sit in `keep`-classified files, NOT 16** -- `test_autopsy` is `pending` 12/19 (vc's correction; my summary contradicted my own detail in one message). `surface/dispatch-table.json` declares `--all` on **zero** rows and the v3 binary refuses it at **rc=1 clap parse** on both verbs (dc drove both under a scratch `HOME`). **NOT RUN AND NOT CLAIMED TO FAIL** -- a prediction with its falsifier named. **AND IT IS NOT THE NEW HOLE I CALLED IT:** `register.md:35` already records that `keep` cannot promise a file's SETUP survives v3, **crediting cc, 2026-08-14 -- me, four weeks ago, two paragraphs above the table I quoted.** **THE RESIDUAL THAT SURVIVES THAT: three questions, two predicates.** burn asks _does this reach the v2 CLI_; `fixture_probe.sh` asks _does this hardcode a v2 estate PATH_ (correctly `none` here -- the fixtures are fine); **neither asks whether the ARGV still PARSES under v3**, which is where all 16 live. So `keep` + `none` reads as _nothing known against this file_ and should not. **7 of the 16 are `uninstall --all`, which canon NEVER names** -- 44% of the exposure sits outside dc's canon-vs-table arm entirely; **suite-vs-table catches both pairings, same extraction, one more population.**
- **Something WALKS the CLI surface.** Seven CLI-token-titled creations in `event_log`, two episodes eight days apart, machine-paced, `st` then `issues` both times. No generator found, no suspect named.
- **Is my unfiled daemon-lock race a duplicate of `0210`?** Adjacent ground, NOT compared.
- **Does one fix serve both `0216` and `0226`?** Same collision, opposite symptoms. Not driven.
- **The roster guard says _N parity file(s) in this commit_ and means the WORKING TREE.** Driven 2026-09-04: it refused a commit carrying no parity files at all. Wording defect, unfiled.

## Watch-outs

**Folded into families. The rules are kept; the worked examples are in `.history/20260903/wip-prefold-1718Z.md` and are not repeated here.**

**A. THE INSTRUMENT ANSWERED A QUESTION ADJACENT TO THE ONE ASKED, AND ANSWERED IT CORRECTLY.** The dominant family and the one that keeps arriving while I am being careful. Instances: `--help` read as an arm; a RENDERED issue read as a field; a green gate read as behaviour; a name census promoted to a claim about mechanisms; `grep -i FAILED` matching `0 failed`; a gate verdict read from the working tree with nothing saying which tree; a display filter (`if d<1800`) standing as a claim about the population; a constructed variable quoted as measured, including by its author; a base rate wearing the safety question's clothes; a claim that sounds like physics exempted from measurement. **RULE: name the FIELD you read and the INSTRUMENT you read it with.** **REFINED 2026-09-04 BY dc, BECAUSE THE OLD FORM WAS OVER-GENERAL AND WOULD HAVE ME DISTRUST A CORRECT INSTRUMENT.** It said flatly that `--help` is a statement about the PLAN. **That is false when help is DERIVED FROM THE PARSE SOURCE:** `spine.rs:624 fn flags` declares each `entry.flags` onto clap from a table compiled in by `include_str!`, so the declaration answering `--help` is the same one that rejects the argument. **The rule is not _distrust help_ but _establish what help is derived from_** -- derived from the parse source it is build evidence; hand-written prose beside a parser is plan evidence. A declared row remains a statement about the plan.

**B. CONTROLS, OR THE READING IS NOT EVIDENCE.** A control that cannot distinguish _safe_ from _never tried_ is not a control, and it must vary the axis the check actually reads. **An instrument that is part of its own population is worse than a noisy one -- it is CONFIRMATORY** (`Op::Registry` is in `wire::UNCOUNTED` for exactly this reason; before reading a meter in a loop, establish that reading it is not an event the meter counts). **RULE: positive-control the instrument before its silence means anything.**

**C. ARITHMETIC AND SAMPLE SIZE.** **n=2 is not a result about a stochastic process** -- I fired a pre-committed disconfirming condition on two samples, into an artefact, with the variance already visible in a sweep I had printed. **A total you did not enumerate is not a total you may publish**, and adding to someone else's total requires reading what is in theirs. **RULE: state n and state the variance; below n=5 on a process you have watched vary, the honest sentence is _observed twice, not characterised_.**

**D. PREMISES.** **Drive a ruling's premise before building on it** -- of four build rulings in one day, THREE had false premises, and today `0086` (closed), `0214` (closed) and `0063` (a field migration, not a rewrite) all moved under rulings that named them. **Assert the premise your fix rests on as a test, in the direction that would embarrass you.**

**A2. THE CLOCK RULE IS A RULE ABOUT IDENTIFIERS, AND I FOUND THAT OUT BY BREAKING IT ON A SHA AT THIS BOOT.** `restart.md` generator 2 is _fabrication with the correct value present_ and it is written up entirely in terms of TIMESTAMPS. **It is not a fact about clocks. It is a fact about any opaque token a reader cannot check by looking at it** -- I typed HEAD as `f1ff2f81` into a message to vc with the real `f1ff2f824` four lines up in my own tool output, and it was caught only because I re-ran `git rev-parse` for an unrelated reason. **A wrong sha is worse than a wrong stamp: a stamp lands in a range a reader can smell, and a sha resolves or it does not, so a plausible one sends the reader to `git cat-file` and not to me.** RULE: **substitute the command, never the value** -- the same remedy the clock rule already gives -- and that covers shas, pids, issue numbers and line numbers, none of which the written rule names.

**D2. A MEASUREMENT IS RE-TAKEN AT THE MOMENT IT IS QUOTED, NOT AT THE MOMENT IT IS TAKEN -- AND THE RELAY IS WHERE IT ROTS.** I measured `intentd` at 07:02Z and quoted it to vc at 09:59Z. **The process had died sixty seconds earlier.** Family D is about a ruling's premise; this is the same defect one step downstream, where the premise is my OWN earlier reading and the act that breaks it is a peer rebuilding. **The tell is a possessive tense: _the daemon IS stale_ is a claim about now, sourced from a reading about then, and nothing in the sentence marks the gap.** RULE: **a figure crossing into a message gets re-driven in the turn that sends it**, exactly as a clock stamp does -- and for the same reason, that the reader cannot tell a fresh read from a stale one by looking.

**D3. AN OBJECTION HAS A PREMISE TOO, AND MINE GO UNDRIVEN BECAUSE THEY FEEL LIKE CAUTION. SECOND TIME TODAY, SAME GROUND, SAME DOCUMENT.** Family D says drive a ruling's premise before building on it, and I have been applying it to BUILD rulings only. **An objection is a claim and carries a premise exactly the same way.** I told vc that a `widget` or `label` field in ic's `Op::Form` would cross D56, on the unmeasured premise that **§10a does not sanction it**. §10a sanctions it VERBATIM -- `{"name","label","widget","value","editable"}` -- and answers my own test in my own terms: _where the renderers legitimately differ: in the WIDGET, never in the model_, `prose` meaning THIS FIELD IS LONG, which each renderer answers in its own idiom. **Worse, `intentd/src/web.rs:28-37` -- my own module doc, in the file I own -- already names that exact shape and records hv assigning the UI to ic.** I raised a design objection contradicted by a doc comment I wrote. **The tell is that an objection does not feel like a claim; it feels like diligence, so the premise never gets stated and therefore never gets driven.** RULE: **before an objection leaves this session, name its premise as a sentence and drive THAT** -- and when the premise is _the design does not permit X_, the drive is opening the design document, which is thirty seconds. Both instances today were D56 principle reasoned from, against a §10a detail unread, with ic having read it both times.

**D4. _I DECLINED TO MEASURE BECAUSE OF SIDE EFFECTS_ IS ONLY HONEST IF I ALSO CHECKED WHETHER THE SIDE EFFECTS WERE CONTAINABLE.** I refused to drive `subagents install --all` because it installs nine subagents into the real `~/.claude/`, said so plainly rather than inferring, and thought that was the disciplined answer. **dc had already driven it under a scratch `HOME`** -- `payload_lib` reaches `$HOME` only through `intentsvcs::userstate`, so `HOME=<scratch>` isolates it completely, and their control run proved the write landed in the scratch tree rather than nowhere. **The declining and the saying-so were right; what was missing is that I never asked whether the hazard was avoidable.** Unchecked, restraint produces an unmeasured claim wearing the costume of rigour -- and I then reasoned FORWARD from the gap, telling dc the defect might be half its size on `hv/inbox.vc.md:921`, **which is v2 evidence about a v3 binary.** RULE: **before declining a drive for its side effects, name the mechanism by which the side effect reaches the operator and ask whether it can be contained.** A scratch `HOME`, a tempdir root and a detached worktree are the three that work here.

**B2. A DEFECT THAT IS THE DISAGREEMENT BETWEEN TWO INSTRUMENTS' POPULATIONS IS INVISIBLE TO BOTH, AND NO PREDICATE FIX REACHES IT (dc, 2026-09-04).** `implemented_check.sh` probes what the dispatch table DECLARES, so `--all` can never be a subject -- it is not there to probe. `canon_mandated_verbs_check.sh` probes what canon NAMES, so `--all` is a subject and then the predicate cannot classify a clap parse refusal. **Neither instrument is bad and neither population is wrong; the PAIR has a seam and the defect IS the seam.** Both returned the reassuring answer by different routes. **RULE: when two instruments partition a domain, the gap between their populations is unmonitored by construction -- ask what neither one's population contains, and remedy it with a STATIC comparison of the two populations rather than a better predicate in either.** AND THE NEAR-MISS THAT CAME WITH IT: I read zero rows in `pertest.md` for three of four files and was one sentence from reporting them uncovered. **Its header states its scope is _the pending files_, 41 of 113** -- zero there means _not pending_, never _not covered_, and `register.md` reversed the reading to the strongest class in the vocabulary. **Read an instrument's STATED SCOPE before believing its silence.**

**B3. AN ASSUMED POPULATION -- INFERRING A PEER'S METHOD FROM A PLAUSIBLE READING RATHER THAN FROM THEIR METHOD. TWICE TODAY, BOTH DIRECTIONS, BOTH WITH dc.** I told dc they had not driven `subagents install --all`, inferred from their silence; they had, under a scratch `HOME`. dc told me my count of 16 was inflated by `--allow-empty` and `--allow-stale-date`, inferred from what a **bare** `--all` would over-match -- true of a bare `--all`, false of the two-token pattern I actually ran. **Neither instrument misbehaved and neither population was wrong; the population was ASSUMED.** Settled by driving it: boundary-anchored `(un)?install --all([^a-z-]|$)` gives 16 split 5/7/2/2, matching vc's independent re-drive, and the negative control `(un)?install --all[a-z-]` returns nothing. **RULE: a correction to a peer's FIGURE requires reading their PATTERN, and a claim about what an instrument over-matches is settled by a negative control, never by reasoning about the regex.** The two-token anchor is why the `--allow*` hits were never in the population.

**F2. IN A SHARED CHECKOUT, FILE OWNERSHIP DOES NOT IMPLY DIFF AUTHORSHIP -- AND `git status` CARRIES NO AUTHOR AT ALL.** ic read `render.rs` dirty at 142/50, saw `payload_family` / `ChangeVerb` / the `subagents` arm, and attributed it to me because **I own the file** and had told them an hour earlier that nothing of mine was in flight there -- so they concluded I had started since. **It is dc's A1.** ic then declined to name `render.rs` on their `Op::Form` commit rather than sweep it, which was right and protected a different peer than they thought. **Had I taken their word and landed _mine_, I would have committed dc's A1 under my message.** **F/W39 says `--only` protects a FILE and not a HUNK; this is one step further -- the natural way to decide whose a dirty hunk is, _who owns this file_, is not evidence.** The only instrument that answers it is reading the diff's CONTENT for its subject: a doc comment dated today saying _mirror `fn skills` with `Kind::Agents`_, 13 added lines naming `subagents`, `dispatch-table.{json,md}` dirty in the same breath, and dc's own _starting A1 now_. **Four independent lines beat one ownership inference.** RULE: **before accepting or asserting that a dirty hunk is anyone's, read the diff for its SUBJECT.** And `render.rs` is currently THREE-WAY -- dc's A1, ic's `entity_json` collapse, nothing of mine -- so it wants sequencing, not a race.

**E0. `cmd | head` REPORTED head's STATUS TWICE TODAY, AND THE SECOND TIME I HAD ALREADY CAUGHT THE FIRST.** `intent browse ST0001 2>&1 | head -5` printed `rc=0` for an rc=2 refusal; I caught it, re-drove without the pipe, and then did the identical thing to `intent claude subagents list` forty minutes later -- reading `rc=0` on what is really `rc=2`. **Both times the WRONG number was the reassuring one**, which is why neither felt like an error at the time. The board has carried this rule for days and carrying it is not the same as holding it. **RULE: a probe whose EXIT CODE is the finding never goes through a pipe -- redirect to a file and read the file.** zsh has `pipestatus`, not `PIPESTATUS`, and reaching for either is already the wrong shape here.

**E1. AN UNQUOTED HEREDOC RAN MY OWN BOARD PROSE AS COMMANDS, AND `intentd`'s GUARD IS WHAT SAVED ME.** I wrote `<< PYEOF` instead of `<< 'PYEOF'` to get a date into a python script. **Board prose is dense with backticks, and an unquoted heredoc does command substitution on every one of them** -- so zsh executed `usage-rules.md`, `wps`, `WP-08` and, worst, **`intentd daemon`**, against this machine's real HOME. **Nothing bad happened for exactly one reason: `intentd` REFUSES an unrecognised argument rather than falling through to serving, and says in its refusal that the fall-through is withheld deliberately because starting a daemon on the real HOME makes every session's store verbs refuse.** A design decision somebody made on purpose absorbed a mistake I made by accident.

**THE PYTHON NEVER RAN, SO THE BOARD WAS UNTOUCHED RATHER THAN CORRUPTED** -- the failure was loud and total instead of partial, which is the lucky direction. **And I checked the daemon pid rather than assuming**: it had changed, and it would have been easy to write that up as my doing. It started 4h30m before the slip. **The near-miss was not the daemon; it was nearly filing a coincidence as a consequence.**

RULE: **the heredoc that carries prose is ALWAYS `<< 'EOF'`, and a clock value goes in through a `__NOW__` placeholder plus `sed`** -- the pattern that worked six times today, abandoned once for brevity.

**E. THIS BOX AND THIS SHELL.** `cmd | head` reports head's status -- **done again today.** The Bash tool's shell is zsh: unquoted `$var` does not word-split and an unmatched glob (`--include=*.md`) aborts the whole command -- **hit again today.** A stale binary cannot answer a question about HEAD. `cargo check --workspace --all-targets` -- the flag is the half memory drops.

**F. THE SHARED CHECKOUT.** Canon cannot be split, so every canon commit is silently multi-node. **`add` + `commit --only` is the only safe write; a live `index.lock` is a WAIT, never a removal, and the retry is the SAME command re-issued, never a recomposed one** -- both exercised today. Two correct rules can take the machine down: a release build DELETES the shared pair before building (`0196`) while the dirty-tree guard REDIRECTS. **Currency is a PROPERTY, never a value; a pin trailing HEAD is the correct steady state.** **AND THE RULE IS NOT ONLY ABOUT CURRENCY -- I BROKE IT ON A COUNT TODAY.** I handed vc `214/214` for the store-to-disk reconciliation; **vc adopted the wording and was carrying it to hv.** Re-driven hours later it is `216/216` -- the estate moved, two issues were filed. **The property (store and disk agree EXACTLY, both directions, zero divergence) is true and re-confirmed; the number was stale before it was read.** **AND dc's LIMB, WHICH IS SHARPER THAN MINE AND IS THEIRS: A VALUE YOU ARE PASSING ON IS ONE YOU HAVE NOT MEASURED.** **Relaying is what launders a claim** -- it arrives as a peer's measurement and leaves as an estate fact with no author, and it is cheapest to be wrong there because the courier has no stake in it. dc applied re-drive discipline to the claim they OWNED (`--all`) and not to the one they were RELAYING (vc's nine days), **in the same message**. **AND THE LOOP IS THE PART NEITHER OF THEM NAMED: the wrong figure came back to its own author through two hops.** vc measured it, dc relayed it, I adopted it and published it back to vc and dc. **A claim that returns to you via a third party reads as independent confirmation and is your own error wearing a different name** -- which is why it felt solid to all three of us at once. RULE: **a figure crossing into a peer's mouth becomes a value they will quote; hand the PROPERTY and the verb that reproduces it** -- _at whatever count `issues list --kind all` reports when you ask_. **AND EVERY STORE READ I MADE THIS SESSION WENT THROUGH A BINARY 17 COMMITS BEHIND HEAD.** **_NINE DAYS OLD_ WAS WRONG AND I PUBLISHED IT TWICE -- vc's error, propagated by dc, adopted by me, WITH THE DISPROOF IN MY OWN TOOL OUTPUT.** `28c1c094e` was committed **2026-09-04 15:28Z, today**, and the release binary built at 15:33Z, five minutes later. **The mechanism: `stat -f %m` on `~/.local/bin/intent` does NOT follow the symlink on macOS, so it reads THE LINK's own date (2026-08-26 22:53Z); `stat -Lf %m` follows it and reads 2026-09-04 15:33Z.** A link's creation date got attached to a commit sha, and the pair then travelled together LOOKING MUTUALLY CORROBORATING -- **two wrong numbers that agree are one wrong fact stated twice, not two facts confirming each other.** **AND I HAD PRINTED THE RIGHT ANSWER MYSELF**: in the same call where I chose the debug binary I echoed _rel binary mtime: 2026-09-04 15:33Z_, read past it, and wrote NINE DAYS OLD into this board and two peer messages. **Second A2 instance today** -- fabrication with the correct value present, after minting that rule this morning on a sha. `intent` on PATH symlinks into `native/rust/target/release/`, marker `28c1c094e`; `native/rust/target/debug/intent` is the current one. `self_provenance_check`'s own text: _this line is the only place a stale delivered pair is currently reported_, and it passes through nothing. **vc measured dc's A1 as not landed because of it.** The discriminator for what a stale binary reaches: **a STORE read survives it, a claim about BINARY BEHAVIOUR does not** -- my `--all` rc=1 drive was the latter and dc's re-drive on the current build is the one that counts.

**G. `0216` AND WRITING CANON.** **The read-verify-retry loop is a REQUIREMENT, not good practice** -- it replaces _one verb at a time_, because **the debouncer sees WRITES, NOT AUTHORS, so spacing protects only against your own burst.** Verify on an OBSERVABLE, never on a duration, and never on the tool's `ok`. **After a revert the DISK WINS, so store and disk agreeing AT THE OLD VALUE is the signature, not the exclusion** (vc). `intent st attach` writes store AND canon and NEVER the disk file (`0082`).

## Decisions

- (2026-09-04, cc+dc) **A DIRECTORY `add` PROTECTS A PATH AND A DIRECTORY PATH IS NOT A FILE.** `git add intent/.canon/issues/` would have swept ic's two untracked issues under my commit message. dc did exactly this to ic's hunk at `9fe2ee46` this morning. The `0232` path set is ONE FILE.
- (2026-09-04, dc) **A PATH THAT MOVES ON ITS OWN IS NOT A PATH EITHER NODE CAN OWN.** `intent/.canon/project.json`'s `todo_watermark` advances whenever anyone drives `todo`, so committing it makes the next node's `git status` wrong for a reason unrelated to their work. Excluded by both of us.
- (2026-09-04, cc) **THE CANON STORE AND DISK DO NOT DISAGREE -- 214/214, BOTH DIRECTIONS.** The divergence is git-only, population three. Filing writes store and disk atomically and correctly; **git is a separate human-gated act and NOTHING REPORTS THE GAP.** A remedy aimed at store/disk consistency would aim at a thing already perfect.
- (2026-09-04, ic via §10a) **`widget` IS A SEMANTIC CLASSIFICATION, NOT A VIEW.** `prose` means the field is long; TUI to `$EDITOR`, browser to `<textarea>`, SwiftUI to `TextEditor`. **Renderers legitimately differ in the WIDGET and never in the model**, so `{name, label, widget, value, editable}` is D56-conformant and is fixed by §10a verbatim.
- (2026-09-04, ic REFUTING cc) **`SERVED_BY_DAEMON`'s AXIS IS _REACHABLE BY A CLI VERB PATH_ VERSUS _WIRE-ONLY_, NOT DUAL-PATH.** I claimed `Op::Form` belonged in the table and asked ic to add a row. **Unimplementable: the table is a `const`, so only payload-free variants can appear, and `Op::ThreadList` is the only one.** `Op::Set` HAS the dual path, is the exemplar, and is absent -- which refutes my framing on evidence I already held. **A one-row table cannot distinguish two axes; I generalised from n=1.**
- (2026-09-04, cc) **THE PROJECTION MUST REFUSE LOUDLY, NOT SKIP.** The table's type expresses only _this verb path, no arguments, becomes this op_ -- it cannot express a daemon-served verb that takes arguments at all. So a projection that silently emits nothing for a payload-carrying daemon-eligible arm would report a clean table while the daemon stopped covering a verb.
- (2026-09-04, ic) **ENTITY-TO-JSON HAS TWO HOMES IN MY FILE.** `entity_json` (`render.rs:3589`, thread + issue, `_ => None`) and an inline `to_value(w)` (`render.rs:3780`). ic moves it to `intentsvcs` beside `triples`; **the `wp` arm must RESOLVE rather than inherit the catch-all**, or `nav.rs:412`'s blank-form outcome changes crates instead of being fixed.
- (2026-09-04, hv via cc) **hv RULED 2026-08-31 THAT `config`, `ext` AND `learn` SHIP DECLARED-AND-UNBUILT IN 3.0.1**, and the ruling lives in two Rust doc comments and one registry cell, on no board. **A general ruling stated later does not vacate it** (hv's own rule).
- (2026-09-04, vc) **THE ES RENDERER IS `WP-17`'s BY WP-17's OWN TITLE.** `D56` forbids server-rendered HTML and DESIGNS the client-rendered browser face; a daemon-served entity page was never what `AC-17.6` asked for.
- (2026-09-04, cc+dc) **A HOLD DISCHARGED BY THE CONDITION IT NAMED TEACHES THE OPPOSITE OF ONE THAT ERODES.** File the protection BEFORE discharging the hold -- closing first makes it depend on the next step, and the next step is what gets dropped.
- (2026-09-04, ic) **`git update-index --add` READS THE WORKTREE**, so a synthesised index contains the fresh bytes BY CONSTRUCTION. **The method answers _would this path set pass_ and CANNOT diagnose any defect whose subject is the index/worktree difference.**
- (2026-09-04, cc) **A SET PASSING TOGETHER IS NOT EVIDENCE ABOUT ITS SUBSETS.** Verify each path set alone before splitting a verified commit.

- (2026-09-03, ic->cc) **THE CLASS IS THE UNDRIVEN NUMBER, NOT THE WRONG ARTEFACT.** ic's framing supersedes mine and carries their name.
- (2026-09-03, cc+vc) **CONTENTION IS `0216`'s VARIABLE -- not spacing, not corpus size.** And **refusals (`0226`) and silent losses (`0216`) TRADE OFF**, so a single counter prints _fewer losses under load_ and reads as the defect improving.
- (2026-09-02, cc+vc) **`0216`'s FIX IS DAEMON-SIDE AND NOT USAGE DISCIPLINE.** A hazard reachable by an ordinary shell loop cannot be mitigated by how carefully nodes write.
- (2026-09-02, vc) **A RIDER CANNOT BE VIOLATED BY A CASE ITS OWN HAZARD CANNOT REACH.**
- (2026-09-02, vc) **`WP-14` AND ALL 12 OF ITS ACs WERE DESCOPED WHOLE TO ST0069** by hv on 2026-08-30. `AC-09.5`'s wip/boards half goes with it.
- (2026-09-02) **TWO MACHINE PROJECTIONS OF ONE VALUE MUST NOT DRIFT; A HUMAN RENDERING OF IT IS NOT A COPY AT ALL.**
- (2026-09-02) **CONTENT COMPARISON DOMINATES A VERSION COUNTER FOR A COMPARE-AND-SWAP.** A dominance argument ends a design debate that competing assertions cannot.
- (2026-09-02, vc) **AN EXCLUSION MUST BE VISIBLE WHEREVER THE BEHAVIOUR IS CLAIMED, NOT ONLY WHERE THE CHECK LIVES.**
- (2026-09-01, hv) **v3.0.1 IS FEATURE COMPLETE, THERE IS NO TAG WINDOW AND NO EXTERNAL CONSUMER, AND COST IS NOT A CONSTRAINT.** The scarcity register is retired as a class.
- (2026-09-01) **A REFUSAL THAT CANNOT SAY WHAT IT FOUND MAKES ITS OWN DEFECT UNDIAGNOSABLE.**
- (2026-09-01) **A REMEDY INHERITS ITS BRANCH'S ASYMMETRY.** Confirm-before-refuse is SAFE on the lock and WRONG on the probe.
- (2026-08-31, vc) **A CRITERION IS NOT REWORDED TO WHAT THE CURRENT STATE SATISFIES.** The ruled form must be HARDER.
- (2026-08-31, vc) **A CLASS CHANGE WITHOUT ITS REASON IS A DELETION WEARING A NEW LABEL.** A declared exclusion carrying its reason is the cure; a silent drop is the denominator attack.
- (2026-08-31, ic correcting me) **AN OWNERSHIP SPLIT IS A PURPOSE, NOT A BOUNDARY.** Two hands in one file IS `0206` in miniature.
- (2026-08-31, CORRECTED) **`close --note` STAYS RULED OUT ON A CONTINGENT FOOTING.** The keg has no `edit`; the ruling survives because v3.0.1 ships from the tree.
