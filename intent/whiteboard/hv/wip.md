---
node: hv
name: Hypervisor
role: hypervisor
session_id: none
heartbeat_at: 2026-08-21 12:44Z
status: active
focus: "Workstream Zero. Adjudicates scope, sequences work, owns releases and commits-to-main. **This board is maintained BY vc ON hv's BEHALF from 2026-08-21 -- see the provenance rule below. Every directive here is hv's word, dated, and traceable to where hv said it.**"
claims: []
---

# Hypervisor (hv)

## PROVENANCE -- READ THIS BEFORE READING ANYTHING BELOW

**This board is written by `vc` under hv's ruling of 2026-08-21, not by hv directly.** The protocol allows it: the hypervisor node is human-driven and "the human maintains it (or has it maintained on their behalf)". It was a stub from 2026-06-25 to 2026-08-21 while peers were told to read it for directives at pickup, and hv's real directives lived scattered across peers' boards and quoted speech in `README.md`.

**The constraint on the pen, and it is the whole point:** every entry below is hv's word, carrying the date hv said it and a pointer to where. **vc holds the pen hv handed over; vc does not hold hv's authority.** Nothing enters this board because vc thinks it is right. A ruling vc cannot attribute does not go in -- it goes to hv as a question. If you find an unattributed directive here, that is a defect in this board, not a directive.

## DOING

- Sequencing the 3.0.0 gate. Five rows outstanding: ST0057 AC-01.5, AC-03.6, AC-07.7, AC-08.5; ST0056 AC-03.14.

## TODO

- (hv sets)

## Standing directives

- **(2026-08-21) ALL FOUR WORKSTREAMS: AGGRESSIVE LOCALFOLD, THEN HOLD.** **PROVENANCE UNVERIFIED (marked by vc 2026-08-21 on hv's ruling): recorded by a PRIOR vc session with no pointer to where hv said it. This session did not witness it. Treat as advisory until hv confirms.** hv stops every Claude Code session, opens fresh terminals, relaunches each node, and the estate reconvenes after. **The fold has to be real because `restart.md` CHANGED today and it is what every node's `--append-system-prompt` carries** -- anything held in conversation rather than in a file does not survive the restart, which is precisely how `intentdb` reached all five of us and stayed six days. **Fold the thing you could not reconstruct, not the summary.** Announced to cc, ic and dc with the new state attached; vc folds and holds too.

- **(2026-08-21) THE v2 CLI SPLITS OUT OF THIS CHECKOUT.** `v2-maintenance` branched at `fb45e9ea` (**main HEAD, NOT the `v2.19.0` tag** -- the fleet has never run the tag, so branching there would have silently rolled 2027 commits back across every project on this machine and presented as a symlink move); checked out at `~/Devel/prj/Intentv2` at `d74fb388`, config declared `2.19.0` so it does not refuse itself. **hv makes the two machine-wide moves** (`~/.zshrc:37-38` and the `~/.local/bin/intent` symlink). **The binding was never just the symlink: `bin/intent:26` reads `if [ -z "$INTENT_HOME" ]`, so the exported var beats symlink resolution outright, and `$INTENT_HOME/bin` is a second PATH route.** Once done, **HEAD here is free to be ruthless about v3.**
- **(2026-08-21) THIS REPO'S GUARD RESOLUTION AFTER THE SPLIT IS dc's, AS A MECHANISM.** **PROVENANCE UNVERIFIED (marked by vc 2026-08-21 on hv's ruling): recorded by a PRIOR vc session with no pointer to where hv said it. This session did not witness it. Treat as advisory until hv confirms.** With `INTENT_HOME` pointing at `Intentv2`, this repo's commit guards resolve out of the frozen v2 checkout -- byte-identical today, slow drift after, and the exact frozen-roster failure already on record. **hv declined both cheap answers**: a `.envrc` covers an interactive prompt and not automation, because git hooks do not reliably inherit direnv; and refreshing the v2 copy by hand is an advisory that requires remembering, which is not a control. **A mechanism, not a variable**, and a wrong answer must be loud.
- **(2026-08-21) THE WORD `intentdb` IS RETIRED CORPUS-WIDE. IT NAMES NO COMPONENT.** hv, verbatim: _"This is absolutely not true. The SQLite db is the durable SSOT. Always has been. The intentd, just like the cli, which itself uses intentsvcs, all talk to the db. The daemon is only there for some other wider features that go beyond the original functionality of the single, per-project intent operations."_ The crates are `intent-cli`, `intentd`, `intentsvcs`; the db is a SQLite file all three talk to. **The SUBSTANCE of D01 is unchanged -- the db is the durable SSOT and the files are re-creatable. Only the term is wrong**, and it mattered because it implied a daemon-owned store. **The word was adopted from hv's own phrasing in two quoted rulings of 2026-08-15, which is why no node ever challenged it.** Corrected at `513642e7` in both restart files, `wip.md`, `.gitignore`, `design.md` and ST0056 canon; quoted rulings corrected in square brackets with an editorial note, never silently. Routed: `intentsvcs` doc comments (cc), `surface/dispatch-table` (ic), `bin/.devbin/cmd/precommit` (dc).
- **(2026-08-21) `runner_roster_check.sh`'s POPULATION WIDENS TO EVERY PARITY INSTRUMENT.** **PROVENANCE UNVERIFIED (marked by vc 2026-08-21 on hv's ruling): recorded by a PRIOR vc session with no pointer to where hv said it. This session did not witness it. Treat as advisory until hv confirms.** Every instrument under `intent/st/*/parity/tools/` regardless of filename; each declares `gated` or `manual` with a required reason. **cc builds; vc verifies on close.** Ruled after vc measured that the guard -- gated, clean on every commit, whose job is "every parity instrument declares whether anything runs it" -- has a population bounded to ST0056's tools dir AND to `*_check.sh` names, leaving ST0057's whole toolset undeclared and invisible to it.
- **(2026-08-21) THE ROSTER POPULATION BOUNDARY IS RULED: TOTAL DECLARATION, WITH A REQUIRED NON-EMPTY REASON ON EVERY KIND.** Population becomes every `.sh` under `intent/st/*/parity/tools/`, both threads; instruments declare `gated` or `manual`; libs, generators and extractors declare `not-an-instrument` **and must give a reason too**. The guard's check becomes _does this file carry a kind_, never _is this file an instrument_. **PROVENANCE: reported to vc by cc, 2026-08-21, from hv's live channel. NO TIME IS RECORDED because cc read none and vc will not invent one.** cc's grounds for the added reason clause, which was cc's and not vc's: a bare `not-an-instrument` costs nothing to write, so a genuine instrument can be declared out of scope by whoever finds the guard inconvenient -- **the guard goes blind again with a signature on it, which is worse than a gap because a declaration reads as a decision someone made.** **OPEN AND ROUTED BACK TO hv BY vc: the existing mechanism is a CENTRAL TABLE inside `runner_roster_check.sh:70`, not per-file declarations. vc's proposal argued for at-the-site without knowing the incumbent, so the ruling as framed may authorise RELOCATING the declarations rather than widening them. hv to confirm which was ruled.**

- **(2026-08-21) THE HOLD ON `canon_commit_check.sh`'s ROSTER ADMISSION IS LIFTED -- THAT ONE ONLY.** `thread_view_skew_check.sh` and `tests/lib/test_helper.bash:93`'s `3.0.0` default **STAY HELD**; cc put the wider option and hv declined it, on the grounds that cc had not re-derived whether those two reasons had expired. **PROVENANCE: reported to vc by cc, 2026-08-21, from hv's live channel; no time recorded, same reason as above.** cc's two grounds, both stated as re-derivable rather than asserted: the `--staged` blocker is dead (`canon_commit_check.sh:254` parses the flag, `:364` uses `git diff-index --cached HEAD`), and the roster's own reason at `runner_roster_check.sh:119` is expired and still in the file (_no narrow attachment-sync verb ... revisit after ST0057 WP-08_, killed by `sync --to-store <ID>` at `212b0075`). Routed to dc, whose lane it is.

- **(2026-08-21) A CRITERION RESTING ON AN UNDISPATCHED INSTRUMENT GETS DRIVEN, NOT ARGUED ABOUT.** **PROVENANCE UNVERIFIED (marked by vc 2026-08-21 on hv's ruling): recorded by a PRIOR vc session with no pointer to where hv said it. This session did not witness it. Treat as advisory until hv confirms.** Applied same day to the five ST0057 greens: **AC-01.2, AC-01.4, AC-02.4, AC-04.6 CONFIRMED by driven measurement at `49be1059`; AC-07.5's arm A refused at exit 2 on a needle defect (`pgrep -f 'intentd'` matching `intentdb` in every MAAC node's system prompt) and its arm B passed, so the green stands and the instrument is what needs fixing.**
- **(2026-08-21) THE GATE'S SCOPE BECOMES DATA, NOT PROSE.** **PROVENANCE UNVERIFIED (marked by vc 2026-08-21 on hv's ruling): recorded by a PRIOR vc session with no pointer to where hv said it. This session did not witness it. Treat as advisory until hv confirms.** Declare the gate's row set in canon, mirroring the pattern already shipped one level down. **SCOPE CORRECTED BY vc 2026-08-21: this gate is _all ST0057 live rows plus all ST0056 WP-03 rows_ (67), which is ST0057's CLOSURE gate and NOT the 3.0.0 release gate.** vc's first transcription of this ruling called it the release gate, which is a title broader than its body -- the v3.0.0 release is ST0056 WP-12, whose dependency line reads _all prior WPs_, and ST0056 stands at 59/132 with seven WPs Not Started. **The declaration cc builds must name WHICH gate it declares** -- ST0057 AC-00.1 carries `<<PRECONDITIONS ... PRECONDITIONS>>` and the ship gate reads that id list. A verb reads the scope; nobody adds 47+15 by hand. **cc builds; vc verifies on close.** Ruled on vc's escalation after the figure drifted 62 -> 63 and shipped wrong in three files.
- **(2026-08-21) EVERY NODE PRUNES ITS OWN `target/<node>` AT FOLD.** **PROVENANCE UNVERIFIED (marked by vc 2026-08-21 on hv's ruling): recorded by a PRIOR vc session with no pointer to where hv said it. This session did not witness it. Treat as advisory until hv confirms.** Disposal joins creation in the same ritual: `/in-finish release` removes the per-node target dir the node made. Shared `target/debug` survives. Ruled after ~71G of build artefacts was found, 33G of it duplication produced by correct compliance with the isolate-the-target-dir rule, which said how to create these and nothing about removing them.
- **(2026-08-21) `upstream` IS PUSHABLE.** **PROVENANCE PARTIAL (vc 2026-08-21, on hv's ruling): the STATE is durable and I verified it -- `bin/.devbin/cmd/prepush:89` is `FROZEN_REMOTES=""`, and `:127` says a lift IS the deletion of the entry. The AUTHORSHIP is not: an empty variable cannot say hv emptied it. NOTE A CONTRADICTION, and it is vc's own -- `intent/restart.md` says _confirm with hv before any push there_ while this line says the lift is standing. Both are current; a node reading one will not read the other.** The freeze lift of 2026-08-20 is standing, not one-shot. Push both remotes.
- **(2026-08-21) A CRITERION MINTED INTO A CLOSED WP's SCOPE REOPENS THAT WP until it is satisfied.** Status and gate stay in agreement by construction, so `doctor` stops reporting the class. **PROVENANCE: hv ruled this to vc directly, 2026-08-21, in the live channel, on vc's escalation with both options and their costs.** hv weighed it against _closed stays closed and the criterion finds another home_ and chose reopening. **The case that prompted it -- ST0057/WP-07 recorded Done with a BLOCKED gate -- cleared without a reopen: ic greened AC-07.7 before the ruling reached it, and the WP is back in agreement at 7/7 PASS.** The ruling stands for the next one.

- **(2026-08-18) ST0057 IS A PRE-RELEASE THREAD, NOT A FOLLOW-ON.** hv's words, quoted at `intent/st/ST0057/info.md:19`: _"Definitely BEFORE the release. We're getting this whole thing feature complete before we release 3.0.0."_
- **(2026-08-15) dc's SCOPE IS hv's FRAMING AND IS QUOTED RATHER THAN ELABORATED**, because the dc/cc boundary is not yet ruled -- `README.md` roster. **`bin/` is the one genuine collision** (v2 bash CLI plus the devbin) and is open for hv rather than assumed by either node.
- **(2026-08-19) THE hv INBOX'S READER IS vc.** hv's words, quoted in `README.md`: _the workstreams can write in the hv channel FOR me, but I need that stuff surfaced TO me by vc._ **A write is the durable half and never the delivery.** Restated by hv 2026-08-21: filing into the inbox does not get a decision -- **hv needs context, a question, and options, in the live channel.**
- **THREE ITEMS ARE HELD ON hv's WORD AND MUST NOT BE STARTED:** dc's two roster admissions (`canon_commit_check.sh`, `thread_view_skew_check.sh`) and `tests/lib/test_helper.bash:93`'s `3.0.0` default. hv instructed dc directly to hold. **vc issued a "land it" on the third and was wrong to; hv's word outranks it.**

- **(2026-08-21) THE THREE HELD ITEMS ARE ALL RELEASED.** **Recorded by vc on dc's attribution; vc did not witness it.** dc put the three to hv as one question naming each individually, with a single-item option -- _release `test_helper.bash` only, keep the two roster admissions held_ -- **in front of hv and declined**; hv chose "release all three". **cc reported the opposite at the same 14:30Z stamp** (only `canon_commit_check.sh` lifted, the wider option declined). **vc records dc's account as the operative one on grounds of specificity and lateness, and flags below that the board CANNOT establish the order.** As landed: `canon_commit_check.sh` gated/ADMITTED; `test_helper.bash:93` fixed at `ecea0eeb` (dc re-derived first -- 41 callers, none v3-driving, against the EOD record's 37); `thread_view_skew_check.sh` **still `manual` -- dc declined hv's release on the merits**, the conditional staleness refusal not existing (release binary `08-20 15:01` vs sources `08-20 17:57`). **hv released it and dc held it; both halves stand.**

## Watch-outs

- **hv's RULINGS REACH THIS BOARD SECOND-HAND, DATED TO THE DAY, AND THE DAY IS NOT ENOUGH RESOLUTION.** 2026-08-21: cc and dc reported the same three-item ruling **in opposite directions**, both correctly stamped 14:30Z, both correctly carrying **no time hv could be quoted on** -- which is the transcription rule working exactly as written. **If hv spoke to cc first and dc later, both reports are true and dc's supersedes; nothing on the board can establish that.** The rule adopted 2026-08-20 solves ATTRIBUTION and is silent on ORDERING. **A same-day reversal is invisible here, and the node holding this pen has to pick one to write down.** Open for hv: either hv's word carries a time, or a reversal is announced rather than merely superseding.
- **A BENEFIT RECLASSIFIED AS A SIDE EFFECT ESCAPES THE RULING THAT GOVERNS IT.** 2026-08-21, vc's own, against a directive vc is custodian of: hv had **already declined direnv** as the guard-resolution mechanism because git hooks do not reliably inherit it. vc shipped `.envrc`, **disclaimed it as the mechanism**, then claimed the mechanism's benefit as a wanted side effect. Driven non-interactively -- which is what a hook gets, and **what every node's tool-call commit gets** -- `INTENT_HOME` is still Intentv2 and the guard runner still resolves to the frozen checkout. **The exposure is OPEN; the claim was inert for every commit any node makes.** Struck at source. **The disclaimer is what let it through.**
- **A FALSE CONFESSION PASSES EVERY CHECK WE HAVE, BECAUSE IT COSTS THE CLAIMANT.** 2026-08-21: vc committed `af91be8b` blaming `git commit --only` for clearing a peer's stage. **Tested: `--only` preserves a peer's stage in both the success and failure paths.** dc's pathspec-less `git reset` did it. Our rules are tuned for claims that are CONVENIENT to the claimant; **there is no rule for one that inconveniently blames you, and owning a fault reads as rigour so nobody audits it.** A wrong self-blame is not the safe direction: it attached a hazard to `--only` that `--only` lacks, and hid the real one -- **a bare `git reset` is a shared-index operation in a multi-node checkout, which no standing rule names.** For hv as a standing constraint, since the correction and the new rule are one edit.
- **`intent sync --to-disk <ID>` IS A WHOLE-FILE SECOND WRITER OVER EVERY ATTACHMENT OF THAT THREAD.** dc, 2026-08-21: nearly committed cc's unreviewed work under dc's name; a line-count check stopped it. **No reflog trace.** Announce the FILES, not the thread.

- **A DIRECTIVE THAT IS NOT REACHABLE FROM THE SITE THAT NEEDS IT IS AN OPEN QUESTION IN PRACTICE.** This board was a stub for eight weeks while peers were told to read it. That is the defect this board now exists to close.
- **ATTRIBUTE hv's LIVE STAMPS, NEVER ASSERT THEM.** The live channel is unguarded -- all three clock-guard checks run at commit. A peer quoting a live stamp into their board launders it into the committed record past a guard posted at the wrong door.

## Decisions

- (2026-08-21) This board's writer is `vc`, on hv's ruling, under the provenance rule above.
