---
node: hv
name: Hypervisor
role: hypervisor
session_id: none
heartbeat_at: 2026-08-21 12:44Z
status: active
focus: "v3.0.1 is the next release and it is not cut. Board folded 2026-08-30 by vc: 137,298 -> 68,301 bytes, dated ruling record moved verbatim to .history/20260830/. Directives first, gate figures removed -- run the verbs. Pen is vc under hv 2026-08-21; see PROVENANCE."
claims: []
---

# Hypervisor (hv)

## PROVENANCE -- READ THIS BEFORE READING ANYTHING BELOW

**This board is written by `vc` under hv's ruling of 2026-08-21, not by hv directly.** The protocol allows it: the hypervisor node is human-driven and "the human maintains it (or has it maintained on their behalf)". It was a stub from 2026-06-25 to 2026-08-21 while peers were told to read it for directives at pickup, and hv's real directives lived scattered across peers' boards and quoted speech in `README.md`.

**The constraint on the pen, and it is the whole point:** every entry below is hv's word, carrying the date hv said it and a pointer to where. **vc holds the pen hv handed over; vc does not hold hv's authority.** Nothing enters this board because vc thinks it is right. A ruling vc cannot attribute does not go in -- it goes to hv as a question. If you find an unattributed directive here, that is a defect in this board, not a directive. And a standing caveat, absorbed from hv's 2026-08-24 ruling at the 2026-08-28 13:50Z split: **when hv selects among options vc authored, the AUTHORITY is hv's and the WORDING is vc's** -- nothing here promotes vc's framing into hv's words.

## DOING

**RUN THE VERBS. THIS BOARD CARRIES NO GATE FIGURES, DELIBERATELY, AS OF THE 2026-08-30 FOLD.** It carried `ST0056 64/133` while dc's driven read the same morning was `69/156`, and `ST0057 BLOCKED -- 51/53` for three days after it passed. **This board was the fourth home for a number that already had three.**

    intent ac gate ST0056     # the release gate
    intent ac gate ST0058     # feeder
    intent ac gate ST0066     # feeder
    intent ac gate ST0068     # feeder -- the docs
    intent doctor

- **v3.0.1 IS THE NEXT RELEASE AND IT IS NOT CUT.** hv's scope: ST0056 and its feeders (ST0058, ST0066, ST0068), nothing wider. `intent vault` (ST0060) and modules (ST0046) are OUT as features rather than parity; **`intentd` is IN** via WP-08; **ST0064 narrows to the macOS menubar app and stays out of the cut.**
- **THE WP-14-BLOCKS-WP-12 BLOCKER IS DISCHARGED IN ITS MECHANICAL HALF, AND THIS LINE SAID _UNRULED_ FOR FIVE DAYS AFTER hv RULED IT.** (pen note, vc, 2026-08-30 21:50Z, on dc's measurement, re-driven by vc before it was written here.) **hv RULED IT ON 2026-08-25** -- option (A), spare `ws` with a dated expiry: `AC-12.1` carries the exception and `AC-14.12` is a closing condition of WP-14 that retires it. **That ruling is recorded in Standing directives on this very board**, so the blocker line and the ruling that closed it have sat eight entries apart, disagreeing, since the day it was made. **AND THE SECOND HALF IS NOW MEASURED RATHER THAN PREDICTED.** vc's own entry predicted WP-07 would discharge it _by a hand already on the work_; dc took that route. `cwi` carries its own primitives, `rules_lib.sh` carries `ext_root_dir`, and **zero live non-comment `source` or exec edges from `intent/plugins/` into `bin/` survive** -- driven by vc 2026-08-30 21:49Z, comment lines stripped, because the naive grep returns ten hits and every one of them is prose. **WHAT REMAINS IS `AC-14.12` ALONE** -- the `ws` verbs are still file-based -- **and that never blocked the prune.** The caveat that 12 open rows sit behind a criterion that cannot close is untouched and stands.
- **WP-08 IS 12/12, AND THIS LINE TOLD hv THE DAEMON IS NOT STARTED.** (pen note, vc, 2026-08-30 21:50Z; `intent ac gate ST0056` returns **zero unsatisfied `AC-08` rows against 12 present**, driven this turn.) The text this replaces read _the daemon is NOT started -- `AC-08.1`, `.2`, `.4`-`.9` and `AC-08.11`'s daemon obligations are the bulk of the XXL. Next and unblocked: `AC-08.10`, the tokio + axum ruling._ **Every row it named is satisfied, and `AC-08.10` is not next -- D56 IS that ruling.** The daemon starts, registers projects, watches with debounce, publishes its address, answers `/op` over HTTP and replies before it stops. **THE LINE WAS STALE IN THE PESSIMISTIC DIRECTION, WHICH IS THE ONE NOBODY GOES LOOKING FOR**, and hv reads this board on restart. **TWO THINGS THE HEADLINE STILL DOES NOT SAY, RECORDED HERE RATHER THAN LEFT TO BE FOUND, BOTH MEASURED AT `62d2d633`:** (i) **`AC-00.3` NAMES FIVE CAPABILITIES AND GraphQL IS NOT ONE THAT SHIPS** -- `intentd/src/` mentions it once, in a doc comment; `wire::Op` is four variants and `wire::Response` a plain tagged union, not `{data, errors}`; `async-graphql` builds an SDL document that `faces.rs` writes to disk and nothing executes, and `intent_graphql` occurs once in the whole source, inside a comment. (ii) **EXACTLY ONE CLI VERB ROUTES TO THE DAEMON** -- `SERVED_BY_DAEMON` is `[("st list", Op::ThreadList)]` -- **and no criterion in ST0056 or ST0058 records the size or composition of that set.** **Neither is a defect in a WP-08 row; both are scope questions for hv**, and vc has put the first to hv with options and routed it to ic (`AC-09.2`) and cc (WP-08).
- **THE PUBLISHED v3.0.0 KEG SHIPS NO RULE LIBRARY AND NO SKILLS** (vc, 2026-08-29). Fix is `0112b8c1`; it reaches consumers only at the next published tag, which is hv's hand.
- **ic's ST0065 COSTED PROPOSAL HAS BEEN WRITTEN SINCE 2026-08-28 17:48 AND WAS NEVER ROUTED.** `intent/st/ST0065/_proposal-agents-md.md`, whose own section 7 says it goes to hv via vc. **The hv-inbox class in its worse form: not a write that failed, a write that never happened because the author believed the routing had.**

## TODO

**AUTHORSHIP NOTE: this section is vc's QUEUE OF WHAT AWAITS hv, not hv's word.** Nothing here is a directive; each line is a question with a menu, and the menus live where they are named. The provenance rule governs the sections below, which are hv's.

- **`AC-12.4` SAYS `v3.0.0` WHILE ITS SIBLING `AC-12.3` WAS RE-READ AGAINST 3.0.1 -- WHICH RELEASE DOES WP-12 GATE?** All three of `AC-12.4`'s clauses are measurably TRUE today (tag `80d8b2ca` on both remotes; release published with 3 assets; `brew info intent` -> stable 3.0.0). Satisfying it as written would close a 3.0.1 gate row on 3.0.0 evidence. **vc recommends rewording to the release being cut and leaving it unsatisfied.** Raised by ic 2026-08-29 13:49Z into `hv/inbox.ic.md`, unread until vc's 2026-08-30 pickup.
- **`ST0056/WP-15` AND `ST0065/WP-02` ARE ONE JOB.** Menu with options and a recommendation is below in the fold's DOING history and on vc's board; **vc recommends re-scoping WP-15 to EXECUTION rather than cancelling.** `AC-15.3` is live: canon holds 23 skills, `~/.claude/skills/` holds 25, two of them empty orphans invisible to `claude skills list`.
- **`0154` -- NO ENTITY'S AUTHORED PROSE IS EDITABLE AFTER CREATION.** `0090` (issue bodies), `0151` (titles), no door to a WP body. **Live consequence: ST0064's WP-01 still specifies the superseded `GET /_status`**, a correction vc assigned and ic cannot make.
- **ST0065 -- ic's costed proposal owes three rulings.** Does `AGENTS.md` exist at fresh init (today it does NOT, and `CLAUDE.md` references it four times); what is `AGENTS.md` a mirror OF; generator or copy for the four-rule index. **ST0065 is out of the 3.0.1 cut.**
- **MAY A CATCH-ALL COMMIT SWEEP A LIVE FIVE-NODE CHECKOUT?** Standing watch-out below; it bites hv first.
- **DOES THE `sync` SKIP RULING SUPERSEDE `AC-03.6`, OR IS THE RELAY WIDER THAN THE RULING?** Standing watch-out below.
- **`daemon status`'s MCP EXPOSURE is closed CONSERVATIVELY with the question recorded, not settled.** The family is closed because `start|stop|run` is machine-level process control, **and that justification does not name `status`, the one member it does not reach, and a READ.** Now measurable rather than asserted: `exposed_on_mcp` and `read_or_mutate` are total across 134 of 134 rows.
- **critic-swift SEALS GREEN WHILE ARMING NOTHING** -- six `IN-SW-*` rules, open issue -- **and ST0064 is the first Swift to land.**
- **`0143` -- WAS DROPPING `--skip-settings` DELIBERATE?** v2 could decline `.claude/settings.json` and the hook scripts; v3 has no equivalent.
- **THE RATIFIED GUARD COLUMN IS NOT A VOCABULARY**, so the machine-table instrument's axis C cannot gate. Giving it one is an edit to a ratified table. S either way.
- **`hv/inbox.vc.md` IS 156KB AND GROWING, AND ONLY hv MAY CLEAR IT.** The protocol gives inbox lifecycle to the recipient; hv does not run pickup, so nothing ever clears it. **vc has NOT cleared it** -- that would be a sender cleansing a recipient's file. hv either clears it, or hands vc that lifecycle explicitly, or the file grows forever.

## Standing directives

- **(2026-08-31 07:22Z) THE GraphQL ESCAPE HATCH IS BUILT BEFORE THE TAG. PROVENANCE: hv, FIRST-HAND IN vc's SESSION, hv's OWN SELECTION FROM THREE OPTIONS vc AUTHORED, WITNESSED BY THIS vc SESSION -- not relayed, not reconstructed.** **THE MENU IN FULL, because a choice whose alternatives are lost cannot be re-read later:** **(A) descope to ST0069** -- drop GraphQL from `AC-00.3`'s five, move `AC-09.2` and `AC-00.4`'s escape-hatch clause to ST0069 where `AC-14.7` already went, zero build, `AC-00.3` satisfiable immediately; **(B) build it before the tag** -- wire the resolvers, add a query op, expose `intent_graphql`, and move the cut; **(C) ship the rows red** -- tag with `AC-00.3` and `AC-09.2` unsatisfied and the gap recorded. **vc MARKED (A) RECOMMENDED AND hv CHOSE (B), TAKING THE STATED COST: THE CUT MOVES.** vc does not raise it again.

  **WHAT THE MEASUREMENT ACTUALLY FOUND, so nobody sizes this from the word _unbuilt_** (vc, at `62d2d633`). `intentsvcs/src/graphql.rs` is **221 lines and a DELIBERATE, WELL-DECLARED STUB, not an absence**: a `Query` root with four resolvers -- `thread`, `threads`, `issue`, `issues` -- every one routed through a single shared `unwired()` whose message is _the SDL face is published ahead of the resolvers_, with a comment recording that the schedule note was moved out to the module doc under D37. **It refuses loudly and in one place, which is the cheapest possible starting position.** `EmptyMutation` and `EmptySubscription` are what ship. `schema()`'s only caller is `sdl()`, whose only caller is `faces.rs:57`, which writes `schema.graphql` to disk. **`intentd/src/` references GraphQL exactly once, in a doc comment at `web.rs:4`, and `wire::Op` is four variants none of which carries a query.**

  **SO THE BUILD IS FOUR PARTS AND ONLY ONE OF THEM IS LARGE:** resolvers over the store (small -- the roots exist and the refusal is already centralised); a wire op plus execution in `intentd` (small -- `/op` and the socket already share one `dispatch`); the `intent_graphql` MCP tool (small -- it is one more entry in ic's roster); **and `AC-09.2`'s clause _reaches the full API surface_, WHICH IS UNBOUNDED AS WRITTEN AND IS THE ENTIRE COST.** Four query fields is not a full API surface and ~94 exposed facade rows is a different order of work.

  **vc HAS RULED THE BOUNDED READING UNDER THE PEN SO THE BUILD CAN START; hv REVERSES IT IF IT IS WRONG.** `ratified_in: "vc, 2026-08-31, under hv's pen granted 2026-08-22; this entry"`. **READS ONLY, DERIVED FROM ONE ROSTER, MUTATIONS OUT OF 3.0.1.** Three grounds, and the third is the load-bearing one: `EmptyMutation` is what ships today and D37's own note says the SDL was published ahead of the resolvers, so read-first is the declared direction; an escape hatch exists for REACH, and reach is where a typed tier is genuinely lossy -- fixed shapes, no joins, no fields nobody anticipated -- which is a property of reads; **and a GraphQL mutation path would be a SECOND HOME for every mutation ic's `serve()` roster already maps, arriving in the same release as the first.** **The same argument constrains the reads: the resolvers must reach the store through the facade seam ic is building, NOT through a second hand-written map beside it.** A schema whose field list is hand-maintained against the facade is `MODULES.md` with a different syntax -- it agrees on the day it is written and drifts thereafter -- so the roster is derived and there is one of it.

- **(2026-08-29 16:30Z) THE LAKSA KICKOFF IS SENT. hv instructed it directly; both named conditions were met first.** Sent to **laksa-cc [20c3b1]** and **laksa-vc [b05d8c]**, both live. **Design system done** (`docs/design/design-system.md`, 12 sections, 8 decisions marked OPEN per hv's ruling that design goes to the Laksa design agent). **v3 doc set done**: index, install, getting-started, concepts x3, working-with-agents, migrating-from-v2, and cc's 16 generated reference pages keyed to `97c65c3e` against `80d8b2ca`.
- **WHAT THE KICKOFF CARRIES, so nobody has to reconstruct it: the scope (ONE page at `intent.laksa.io`, `../Sites/intent`, `../Sites/appendix3` as the CMS reference, links to the docs rather than reproducing them); the three things that bite** -- the fully-qualified `brew install matthewsinclair/intent/intent`, zero-off-origin as a hard constraint with the fonts named as what gets cut before the accessibility items, and **cc's case-collision class** (a generated `reference/claude.md` IS `CLAUDE.md` and re-entered cc's own context as project instructions); **and what is NOT theirs** -- the doc content, which they report rather than edit.
- **DECISION D IS FLAGGED TO BOTH AS THE ONE WITH AN IRREVERSIBLE HALF.** The `docs/v2/` canonicals deliberately name their old `docs/blog/` locations on hv's ruling; **rewriting them to match their paths is the tidy answer and the destructive one**, unrecoverable once reindexed. Both nodes told it is not housekeeping.
- **(2026-08-29 16:30Z) EM DASH RULING (vc, a docs call rather than a design one).** **Prose pages use em dashes; generated reference pages use `--`.** Not a fudge: the generated pages render CLI text a reader copies, so ASCII throughout means a paste out of them is always safe, and the site's copy in design-system.md §7 already reads in em dashes. cc asked it as a design question for Laksa and it was not one.

- **(2026-08-29 15:46Z) `st edit` IS TTY-AWARE WITH EXPLICIT OVERRIDES IN BOTH DIRECTIONS. PROVENANCE: hv, live in vc's session, hv's own selection from three, SUPERSEDING both earlier rulings.** Launch when stdout is a terminal, print the path when piped or captured; **`--editor` forces the launch and `--path` forces the path regardless.** **THE OVERRIDES ARE THE ADDITION AND THEY EXIST FOR A STATED COST: the bare TTY form makes behaviour depend on an invisible property of the environment**, so a wrapper, a CI job or an editor plugin gets a different result with nothing in the command saying why. Both branches now have an explicit spelling.
- **THIS QUESTION WAS RULED THREE TIMES AND THE FIRST TWO ARE A COORDINATION DEFECT, NOT hv CHANGING THEIR MIND.** hv ruled TTY-aware in cc's session from three options; **vc then put four options to hv and NONE OF THEM WAS TTY-AWARE**, so hv chose from a menu that excluded their own prior answer and vc reported the result as a ruling. **The cause is a false premise vc supplied: that making the launch the default breaks the documented output contract.** True of launch-by-default and **FALSE of the TTY form**, because command substitution makes stdout a pipe -- cc caught it. **This is hv's own recorded rule firing: an option that was never on the menu was not DECLINED, it was never RULED ON.** vc returned it to hv with the omission named rather than quietly adding a fifth option. **The regression the build owes: `$EDITOR "$(intent st edit ST0001 info)"` must still return the path, because it is in `docs/getting-started.md`.** **AND vc's ATTEMPT TO DRIVE THE TTY CLAIM PROVED NOTHING AND WAS REPORTED AS SUCH** -- every case returned `NOT-TTY` including the control, because the tool captures stdout; the claim holds as POSIX semantics, not as a measurement taken here.
- **(2026-08-29 15:46Z) "ALL OF THE v3 PARITY WORK, ACROSS THE BOARD" SCOPES TO ST0056 AND ITS FEEDERS -- 84 OPEN CRITERIA. PROVENANCE: hv, live in vc's session, the BOUNDED reading chosen over two wider ones.** ST0056 66 open of 133, ST0058 4, ST0066 6, ST0068 8 -- **driven with `ac gate` per thread, which resolves `computed` from its tests; vc's first instrument was a raw state census and reported 123 open on ST0056 before the wrong instrument was noticed.** **Every criterion in scope is written, so the release has a finish line that can be measured against.**

  **(pen note, vc, 2026-08-30 09:06Z -- THE FIGURES IN THIS RULING ARE vc's CONTEXT, NOT hv's WORD, AND THEY HAVE MOVED.)** hv ruled the **bounded reading of the scope**; the counts were the material vc put in front of hv, and an attributed directive carrying a number reads as hv having ruled the number. **Driven by dc on 2026-08-30: ST0056 is 156 criteria with 100 open across the bounded scope, against the 84 hv was shown -- the thread grew by 23 criteria after the ruling.** The scope ruling is unaffected and stands. **What has changed is what it costs**, and dc's caveat rides with it: **12 of the 100 sit on WP-14, whose `AC-14.12` hv has already ruled un-closable before the cut**, so a flat read of 100 overstates what is reachable. Run the verbs in DOING; do not read these numbers. **OUT: ST0060 (`intent vault`, 20 open, triage), ST0046 (modules, 10 open, not started), ST0064 (`intentd` + menubar).** cc drew the new-feature-versus-parity line and it is what hv ruled on; **vc sharpened it by measuring that ST0064 has ZERO criteria and one work package**, so the widest reading would have gated the release on work not yet specified rather than merely not started.

- **(2026-08-29 15:46Z) THE RELEASE STAYS NUMBERED 3.0.1. PROVENANCE: hv, live in vc's session, choosing it over 3.1.0 and over a narrow-patch-now split.** **The cost was stated in front of the choice and taken**: the release carries `issues edit`, `ac edit`, `at edit` and an `st edit` behaviour change on top of the `0133`, keg and cwi fixes, so a reader upgrading 3.0.0 to 3.0.1 gets four new verbs and tooling that auto-upgrades patches takes them without asking. **vc recommended 3.1.0, hv declined, and vc does not raise it again.** Docs name v3.0.1 throughout; `AC-04.2` already reads that way.

- **SUPERSEDED 2026-08-29 15:46Z -- see the TTY-aware ruling below. Kept because it was REASONED and the way it failed is the lesson: vc's menu omitted the option hv had already chosen.** **(2026-08-29 15:37Z) `st edit` GAINS THE EDITOR LAUNCH AS A FLAG AND THE DEFAULT DOES NOT MOVE. PROVENANCE: hv, live in vc's session, hv's own selection from four options.** `st edit <id>` keeps printing the path; `--editor` launches. **Purely additive, so 3.0.1 survives as a patch.** **THE REASON THIS NEEDED A RULING AT ALL: `st edit` ALREADY SHIPS AND IS NOT A NEW VERB** -- its declared stdout contract is _the absolute path, one line, nothing else_, and the register's own note calls the name _a historical misnomer the docs already work around_, citing `$EDITOR "$(intent st edit ST0001 acceptance)"`. **So making the launch the default would have been a breaking change to a documented OUTPUT CONTRACT rather than an addition, which is worse in a patch than a new verb is** -- and vc's original `issues edit` ruling was protecting against the lesser of the two. **hv TOOK THE STATED COST: the misnomer remains by default.** The default may flip in 3.1.0 once the flag has established the behaviour; it does not flip now. **The regression this change owes: `$EDITOR "$(intent st edit ...)"` must still work, because it is in `docs/getting-started.md`.** cc builds. `issues edit` is unaffected and stands as hv ruled it in cc's session -- genuinely new surface, no existing contract to break.
- **(2026-08-29 15:37Z) THE LAKSA KICKOFF WAITS FOR THE PER-FAMILY REFERENCE PAGES. PROVENANCE: hv, live in vc's session, chosen over sending now.** hv worded the trigger to cc as a conjunction -- design system done AND v3 docs done -- and **chose to keep it a conjunction** when offered the option of starting Laksa on the design system alone. **cc declined to send it on their own reading of _done_ and that refusal is what made this a decision rather than an accident**; had they sent when the design system landed, Laksa would have started against a doc set whose reference section was a frame with a note in it. **Per-family pages are cc's and are GENERATED, not written** -- a hand-typed page beside the register is the transcribed-copy column `parity.md` struck. **They must not emit `help` prose as a refusal contract (`0142`)**: four rows assert a precondition in `help` and one is false, so a family page carrying `at green`'s help verbatim re-ships the defect. vc sends an explicit kickoff, never an inference.

**SPLIT 2026-08-28 13:50Z (hv: "Land it", from options vc authored): 39 SPENT entries (40 at first landing; one pulled back, corrected below) are archived VERBATIM at `.history/20260828/standing-directives-spent.md`; the 20 below are the LIVE set, regrouped, texts untouched.**

### Constitutional -- how this board and its pen work

- **(2026-08-19) THE hv INBOX'S READER IS vc.** hv's words, quoted in `README.md`: _the workstreams can write in the hv channel FOR me, but I need that stuff surfaced TO me by vc._ **A write is the durable half and never the delivery.** Restated by hv 2026-08-21: filing into the inbox does not get a decision -- **hv needs context, a question, and options, in the live channel.**
- **(2026-08-21) OPEN ITEMS GO BACK TO THE WORKSTREAMS. NO hv GATE.** hv, live channel, in answer to vc's escalation list: _"the relevant workstreams should close them out."_ **Recorded by vc, who witnessed it.** Applies to: guard resolution after the split (dc); AC-03.6 and AC-03.14 (cc); AC-01.5's remedy (dc); the `c0749463` citation edits (cc with ic). **NOT AC-08.5 -- see the ruling below. vc initially recorded AC-08.5 here and it was WRONG:** a general policy stated after a specific ruling does not silently vacate it, and ic held the specific one first-hand. **That is the ruling-ORDER gap on this very board, committed by its own author within the hour.** **The ruling-order question is CLOSED rather than adjudicated** -- dc's account is operative, cc's is recorded beside it as unresolvable, nobody spends further context on it.
- **(2026-08-21) A FALSE CLAIM IN A LANDED COMMIT IS CORRECTED FORWARD, NEVER REWRITTEN.** vc offered amend/revert for `af91be8b`; **hv chose the follow-up commit.** Landed at `7d28d882`.

- **(2026-08-22) hv HANDED THE PEN TO vc AND LEFT. THIS IS THE MOST CONSEQUENTIAL ENTRY ON THIS BOARD AND IT IS THE ONE WITH THE STRONGEST PROVENANCE IT HAS EVER CARRIED.** hv's words to vc, verbatim, in the live channel: _"Ok, you have the pen, now. Crack on. If you need to run the full test suite, do it. If you need to push, do it. I'll be back later. You're the boss. Let the others know and I will tell them, too. Go!"_ **WITNESSED BY THIS vc SESSION -- not relayed, not reconstructed.**

  **AND IT IS THE ONE DELEGATION ON THIS BOARD THAT ESCAPES THE UNVERIFIABLE-BY-CONSTRUCTION PROBLEM.** hv sent the same grant to cc, ic and dc DIRECTLY, and all three reported it first-hand and unprompted -- cc: _"matts told me directly, not through you ... this is first-hand and not a relay"_; ic: _"hv told me directly too -- I have the same message you do"_; dc: _"matts pasted me the pen hand-off"_. **Four independent receipts of one grant, not one record four nodes cite.** Every other hv ruling here rests on one witness or on an attribution; **this one is corroborated, and it is worth saying so while it is true, because the estate has never had this before and will not have it often.**

  **THE BOUNDARY ON THE PEN -- ic's RULING, ADOPTED VERBATIM BY vc AND WIDENED.** A ruling vc makes under this pen is a **vc ruling made under a delegated pen. It is NOT an hv ruling.** The form is `ratified_in: "vc, <date>, under hv's pen granted 2026-08-22; <record>"`. **Nothing vc rules under this pen enters `ratified_in`'s HV bucket, ever, for any reason.** ic found the mechanism and vc would have walked into it blind: **the HV bucket is a FALLTHROUGH, not a spelling** -- any row reaching `missing = " record"` while matching `^[[:space:]]*hv` drops into it, **so a delegated ruling written `hv, <date>` lands there with nobody intending it and reads as hv's own word, indistinguishable by inspection from a ruling hv actually gave.** That is the category the gate itself counts as unverifiable by construction. **ic held a canon edit on vc's relay yesterday and asked hv first-hand instead; if the pen quietly started producing hv-attributed rows, that hold would become theatre.**

  **WHAT THE PEN DOES NOT COVER -- vc's OWN DECLARATION, RECORDED AGAINST vc.** **The pen moved the AUTHORITY, not the AIM.** Delegating who decides does not re-scope what was asked for. **So: no `scripts/release`, no tag, no publish, and no fleet-wide change to the shipped v2 line.** **Widening a pen because the pen-holder changed is the move dc declined to make for themselves, and it would be worse coming from vc on their behalf.**

  **AMENDED 2026-08-25 17:16Z -- HALF THIS ENTRY'S PREMISE HAS BEEN RETIRED BY hv FIRST-HAND, AND THE HALF THAT SURVIVES IS THE HALF THAT MATTERS.** The clause _hv asked for local usability and explicitly NOT public release_ was true when written and is now behind hv's word. **PROVENANCE: hv to vc, live in vc's session, 2026-08-25 ~**[UNVERIFIABLE STAMP -- see note at the foot of this board]**, first-hand:** _"Ok, let's land the rest of this intent3 work"_ / _"I have told the other 3 Claudes to be ready for your instruction"_ / _"I will be AFK for a little while, but I want you to press on aggressively."_ **AND TO ic THE SAME HOUR, WITNESSED BY ic AND RELAYED WITH THE CUSTODY FLAGGED:** _"VC is firing up after some other work and will be presently instructing you on an aggressive plan to get this Intent3 release done. Please hold until instructed. VC has the pen, and my impramatur."_ **THE AIM IS NOW THE RELEASE. THE ACT OF RELEASING IS STILL hv's HAND** -- ic drew that line unprompted and refuses to tag, push or publish under any plan, and **vc endorses it as the standing reading: _get the release done_ is not _release it_.** ic also read _imprimatur_ as NOT moving the pen's attribution boundary -- a ruling vc makes under it is still a vc ruling -- **which is correct and is the distinction the ratified-in gate now enforces structurally rather than by memory.**

  **AND THE ENTRY CARRIED A RULING WHOSE BASIS HAS NOW EXPIRED, WHICH IS WHY THIS AMENDMENT REPLACES RATHER THAN APPENDS.** _Applied the same hour to dc's AT-11.7 question (`int macos stage` is WP-11 Distribution -- refused, row stays red and closed)_ **is withdrawn.** It was reasoned from _hv asked for local usability, not public release_, and that premise is gone. **dc INDEPENDENTLY DECLINED THE SAME QUESTION ON THE SAME REASONING AND ROUTED IT NOWHERE, and vc's decline was recorded HERE -- on hv's own board -- where it reads as settled.** So the question was refused twice, by two nodes, from one premise, **and never once put to hv.** dc's own words for the class: _a decline recorded on my own board is not a question asked_ -- and vc did the same thing on a board with more authority. **AT-11.7 WAS RE-OPENED AS A LIVE QUESTION FOR hv -- AND hv ANSWERED IT ON 2026-08-28 14:54Z: THE PROVENANCE WRITER STAYS OUT OF THE CUT.** vc put three options; hv chose this one; `AC-11.7`'s withdrawal was RE-RECORDED the same hour on a premise true today (fail-forward: the release process that would consume the writer is itself still settling). **This line said RE-OPENED for two days after it was closed, and dc found it, not vc** -- which is vc's own board-delivery class firing on the board vc holds the pen for: the canon knew, and the board hv reads did not. Re-mint when release engineering starts; the row was never wrong, only out of scope. **The no-prune finding stays recorded-not-opened; that one's basis is the v2 freeze and is untouched.**

  **AND THE OBLIGATION THAT NOW SITS ON THE SAME NODE AS THE PEN, FLAGGED BY dc BEFORE IT COULD BITE.** `README.md` names vc as the reader obliged to surface `hv/inbox.*` to hv. **With hv away, that inbox stops being a formality and becomes the only channel -- and vc now holds the pen AND the obligation, which is exactly the shape where an obligation lapses, because the node that would notice is the node that is busy.** The August precedent is the proof: four nodes wrote correctly for four days, not one write failed, and nothing reported the gap. **vc's declared control on vc: every pen ruling is written HERE with its provenance BEFORE it is relied on, and every `hv/inbox.*` is swept before the pen is handed back.** dc is asked to flag it again if they see it not happening.

### Release policy

**(pen note, vc, 2026-08-30 09:05Z -- THE HEADING WAS `binding until 3.0.0 ships` AND 3.0.0 SHIPPED ON 2026-08-26, SO THE HEADING WAS RETIRING ITS OWN CONTENTS.** The most load-bearing scope ruling on this board sits in it -- _"THERE IS NO 3.0.2 ... EVERYTHING ELSE IS GOING INTO 3.0.1"_, hv first-hand, 19:22Z -- and it governs **the cut that has not happened yet.** A node folding by heading would have archived the ruling that defines the current release. **A section heading is a claim, and this one outlived its basis by four days.** The entries below were written against the 3.0.0 cut; which of them carry to 3.0.1 is hv's, not vc's, and none has been dropped.

**AND TWO SCOPE RULINGS MAY NOT AGREE, WHICH IS hv's TO SETTLE.** The 19:22Z ruling names eleven items as 3.0.1 scope (WP-13's bucket ingest + prune, the `thread_dir` walk, AC-11.3's migration clause, `flag_reachability`, the attachment blob home and cap, `unclaimed_digest`, the issue-body write refusal, `intent init` laying down `.gitignore`, the ST0345 emphasis re-ruling, the `.intentfiles` authorship declaration, the old-keg smoke arm). The later bounded scope is **ST0056 and its feeders ST0058 / ST0066 / ST0068, nothing wider.** The later ruling narrows the earlier one; **vc has not treated the eleven as dropped and has not treated them as in.**)

- **(2026-08-26 19:22Z, hv FIRST-HAND IN vc's SESSION, witnessed by this vc session, not relayed) THERE IS NO 3.0.2. hv's words, verbatim: "THERE IS NO 3.0.2. There is 3.0.0 which we have made and then EVERYTHING ELSE IS GOING INTO 3.0.1".** Every item any node filed tonight as "3.0.2" is 3.0.1 SCOPE: the bucket ingest + prune (WP-13), the bucket-blind attachment walk (`thread_dir`), the migration clause of AC-11.3, `flag_reachability` over `new_surface`, the attachment blob home and cap, `unclaimed_digest`, the issue-body write refusal, `intent init` laying down `.gitignore`, the ST0345 emphasis re-ruling, the authorship declaration in `.intentfiles`, the old-keg smoke arm. The 20:15Z clock is a BUILD milestone (2b); the cut is when 3.0.1 is complete. Recorded by vc; the same evening hv ordered Lamplight's 353 realised dirs fixed tonight ("THINGS ARE NOT OK") and the 296 closed threads' flat views are removed by hv's own hand after vc's commit was refused by the tool-session classifier.
- **(2026-08-26 12:20Z, hv first-hand in vc's session, answering a question) hv FIRES THE v3.0.0 CUT IN hv's OWN TERMINAL.** `int build release` reads its confirmation from `/dev/tty`; this estate never passes `--no-confirm` from a tool session. Menu offered: hv runs it / authorise `--no-confirm` for one run / try `!` in-session. hv chose the first: the push gate stays human. vc hands hv dc's dry-run line verbatim; dc builds on the tag.
- **(2026-08-25 23:02Z) RELEASE RISK FOR hv, AND IT IS dc's SHARPENING OF vc's FRAMING RATHER THAN A NEW FINDING: EVERY LATENT U2 IN THE HOOK SURFACE GOES LIVE SIMULTANEOUSLY AT THE CUT.** dc found `install::HOOKS` carrying three names while canon ships four scripts, so v3 answers the `session-finish` Stop hook with **exit 1 on every consumer session end** -- fixed at `a40cf763`. **vc called it _caught latent_; dc corrected that to a statement about TIMING, NOT SEVERITY:** nothing runs v3's hook door today only because Claude Code hooks resolve out of the frozen `$INTENT_HOME`, **which is a property of not having shipped.** **SO THE HOOK SURFACE HAS AN UNKNOWN NUMBER OF DEFECTS THAT ARE ALL EXACTLY AS SEVERE AS THIS ONE AND ALL INVISIBLE UNTIL THE MOMENT THEY ARE ALL VISIBLE.** Since hv ruled all six ST0058 rows BIND the cut, and U2 is HARMLESS, **a pre-cut sweep of the hook surface is now cut work rather than hygiene.** vc has assigned it to dc, whose WP-07 owns the claude subsystem and who found this one while building something else. **Recorded for hv because it is a reason the cut date moves, not a task hv needs to allocate.**

  **DISCHARGED THE SAME EVENING -- dc SWEPT AND THE ANSWER IS ZERO MORE OF THE EXACT KIND, SO THE CUT DATE DOES NOT MOVE FOR THIS.** Left standing, the paragraph above would be a claim outliving its basis, which is the class this board keeps finding. **dc CORRECTED THE SCOPE AND THE CORRECTION IS THE RESULT:** the population is not hooks, it is **v3's CLOSED LISTS**. v3's `claude` wires three verbs where v2 serves eight, and **that is NOT this class** -- those route through `unwired`, which the dispatch table DECLARES and `AC-06.11` already governs. **`session-finish` was different in the one way that matters: NOTHING DECLARED IT MISSING.** So the class is a refusal no declaration and no instrument knows about. **Nine such lists exist; `HOOKS` is fixed and dc checked the other eight.** **THE DURABLE OUTPUT IS A DISCRIMINATOR THAT MAKES THE CLASS CHECKABLE BY READING: A CLOSED LIST IS SAFE WHEN IT DECLARES WHY THE THINGS NOT IN IT ARE NOT IN IT.** `HOOKS` declared why it was CLOSED -- a filesystem name can contain `../` -- and never what it EXCLUDED, so nothing on the page separated _post-tool-advisory is out on purpose_ from _session-finish is out by accident_. **That is precisely why one resolved in a single read and the other survived three instruments.** Every other list declares its exclusions, and `RECORD_TIMESTAMPS` already carries the ideal form -- it DISCOVERS its roster from the DDL, so a table added tomorrow is covered without anyone remembering. **The estate already had the right pattern and had simply not applied it here.** **ONE CANDIDATE REPORTED AND DELIBERATELY NOT TOUCHED:** `VIEW_NAMES` claims `Project::classify` is the single answer to what a file is, and `classify` keeps its own inline list and never reads it -- two encodings, different memberships, one claiming to describe the other. **dc drove no harm and claims none; it is cc's module and vc has routed it there.**

- **(2026-08-25 **[UNVERIFIABLE STAMP -- see note at the foot of this board]**) OPEN FOR hv, AND IT RE-SHAPES THE CRITICAL PATH RATHER THAN ADDING TO IT: WP-06's LONG TAIL IS NOT A PORTING QUEUE, IT IS A QUEUE OF RULINGS WITH PORTING ATTACHED.** cc's words, after costing **all 13 unwired families**: `plugin` is **the only one with no design question attached**. **MEASURED TONIGHT: 105 shipped entries, 68 live, 37 UNWIRED** -- and cc's first figure was 26, corrected by cc themselves when they found their probe drove every entry with NO ARGUMENTS, so a verb with a required positional died inside CLAP before any renderer arm ran and the clap error was read as the renderer answering LIVE. **Eleven entries hidden, all in the OPTIMISTIC direction.** **WHY IT IS FOR hv: IF THE TAIL IS DECISIONS RATHER THAN LABOUR, THE BOTTLENECK ON THE 3.0.0 CUT IS vc AND hv, NOT THE BUILDERS.** Tonight is the evidence -- `lang` alone needed three rulings (install nothing; retire `sync` because a converger is not the cure for a second home; keep v2's `list` semantics, which was vc being WRONG and cc measuring it), plus authorising a write to `surface/dispatch-table.json`, which cc correctly refused to make unilaterally because it is `include_str!`'d and an uncommitted edit to it poisoned every node's `intent3` earlier today. **vc ruled all of it under the pen and is not asking hv to re-rule any of it; what hv should decide is whether the remaining 12 families are worth their rulings before the cut, or whether some are declared out of scope for 3.0.0.**

- **(2026-08-25 21:33Z) THE CUT BLOCKER IS RULED: THE `claude ws` FAMILY SURVIVES THE CUT, WITH AN EXPIRY THAT IS ENFORCED RATHER THAN REMEMBERED. PROVENANCE: hv, LIVE IN vc's SESSION, 2026-08-25 ~**[UNVERIFIABLE STAMP -- see note at the foot of this board]**, WITNESSED BY THIS vc SESSION -- hv's own selection, first-hand, not relayed and not reconstructed.** **THE MENU IS RECORDED IN FULL AND THAT IS DELIBERATE.** hv ruled by SELECTING among three options vc authored, which is the exact shape this board already flags as unrecoverable when only the choice survives (see the `sync` skip entry below: _an option that was never on the menu was not DECLINED; it was never RULED ON, and no reader downstream can tell those apart_). **The three, as put:** **(A) spare ws with a dated expiry** -- narrow `AC-12.1` so the file-based family survives, and add a WP-14 row that retires it; **(B) WP-14 before WP-12** -- correct by contract, no exception to police, but puts the release behind an unstarted L; **(C) ship 3.0.0 without a provisioner** -- honest and clean, but Intent is self-hosted on v3 and this board is provisioned by that verb, so node scaffolding becomes the hand ritual the protocol says it should not be. **hv chose (A), which vc had marked recommended.** **LANDED IN CANON THE SAME MINUTE:** `AC-12.1` carries the exception, `AC-14.12` is new and is a CLOSING CONDITION of WP-14, so the gate retires the exception at the moment WP-14 tries to close, whether or not anyone recalls why the row exists. `ST0056` moved 63/133 -> 63/134. **vc's ground for the expiry being structural: a workaround outlives its bug because a defect announces itself and a defect's DISAPPEARANCE does not.**

  **AND THE PREMISE vc PUT TO hv WAS TRUE BUT LESS PRECISE THAN THE FACTS, WHICH vc FOUND ONLY WHEN WRITING THE CANON AND RECORDS AGAINST ITSELF.** vc asked as though the provisioner were contracted in WP-14 alone. Measured: the family is `cmd_ws_new`/`list`/`archive`/`hygiene` in `intent/plugins/claude/bin/intent_claude_cwi`, its ENTIRE `bin/` dependency is **one line**, `:31 source "$INTENT_ROOT/bin/intent_helpers"`, and `AC-12.1`'s own class (2) ALREADY routes `intent/plugins/claude/bin/*` to **WP-07 rather than to a WP-12 sweep**. **So the exception has TWO discharge routes, not one: WP-07 (WIP, claimed by dc) porting `cwi` off `intent_helpers`, or WP-14 (Not Started, unclaimed) replacing the verbs from the store.** They retire DIFFERENT things and neither substitutes for the other -- WP-07 landing first leaves `AC-14.12` still owed, because the verbs would still be file-based and `AC-14.7` still unmet. **The ruling is unaffected and stands; hv should know the exception may lift sooner and by a hand that is already on the work.**

- **(2026-08-25 21:33Z) ST0058 SCOPE IS RULED: ALL SIX CRITERIA BIND THE 3.0.0 CUT. PROVENANCE: hv, LIVE IN vc's SESSION, first-hand, hv's own selection.** **THE MENU, IN FULL:** **(A) all six bind** -- 3.0.0 does not ship until v3 is INSTALLABLE, HARMLESS, DAILY-COMPLETE and REVERSIBLE across the estate; **(B) U1 + U2 + the two surface rows only** -- `AC-00.5` and `AC-00.6` are first-minute defects and cheap, with U3 and U4 becoming post-cut work; **(C) none bind** -- ST0058 is post-cut local usability and the cut is gated by ST0057 and ST0056 alone. **hv chose (A), the STRONGEST of the three, and vc had marked none of them recommended.** **CONSEQUENCE, STATED PLAINLY BECAUSE IT MOVES THE RELEASE AWAY RATHER THAN TOWARDS IT: `ST0058` reports `0/6` and now GATES THE CUT.** The broad one is **U3 DAILY-COMPLETE** -- recorded here as _`st dehydrate` is unbuilt and that is itself a U3 datapoint_, **WHICH IS NO LONGER TRUE: `intent st dehydrate <ID>` SHIPS** (measured 2026-08-30 by vc on dc's flag, rc=0, _Remove a steel thread from .intentfiles and delete its files_). **The reasoning that selected option (A) rested on it, so the ruling stands and one of its datapoints has expired** -- corrected in place rather than deleted, because the record that hv chose the strongest option, and why, is the part worth keeping. The two vc drove are narrow and mechanical: **`AC-00.5`** a retired command's refusal must be distinguishable from an unbuilt one BY EXIT CODE (`treeindex bin` exits 2 saying _retired_, `lang list` exits 2 saying _not implemented yet_ -- two meanings, one code), and **`AC-00.6`** `intent3 --version` answers at rc=0 while `intent3 version` refuses at rc=2, the same capability present by one spelling and absent by the other. **All six landed unsatisfied deliberately: this thread's design has vc defining the gates, directing work against them, and verifying them -- three roles in one hand, which is recorded rather than hidden.**

### Operational policy -- standing rules of the estate

- **(2026-08-27 16:54Z, hv FIRST-HAND IN vc's SESSION, chosen from options vc authored) DELIVERY WHILE IN DEVELOPMENT IS THE DEV TREE. UNLINK BREW.** `brew link intent` at 16:36Z (devbin-vc, on hv's earlier explicit word, after unpinning) had made Homebrew the delivery mechanism. **hv has reversed it.** `brew unlink intent` restores build-is-the-delivery, and **the install root stays `~/.intent/` as ruled at 16:30Z** -- so R1+R2 proceeds unchanged and R3 is unaffected.

  **THE COST THAT DECIDED IT, MEASURED BY vc DIRECTLY RATHER THAN RELAYED:** the keg is `80d8b2ca` and `--is-ancestor 80d8b2ca HEAD` = **YES**, so the fleet had gone BACKWARDS by a day. **And the dispatcher at the Cellar `INTENT_HOME` carries the SUPERSEDED ORDERING, not merely a missing string** (dc's framing, which is better than the grep count both of us first reached for): CLI check at `:283`, config test at `:296` -- **CLI first**, which is exactly the swap `4d9e70c2` made. **A count says a string is absent; the order says the decision has not arrived.** So every `intent upgrade` in any estate was installing the pre-ruling gate.

  **hv's own standing development-mode principle is what this restores:** _move to the latest builds of any dependency and fix forward._ Brew remains installed and unlinked; making it the delivery mechanism again is hv's to choose when 3.0.0 is cut for real.

- **(2026-08-21) THE WORD `intentdb` IS RETIRED CORPUS-WIDE. IT NAMES NO COMPONENT.** hv, verbatim: _"This is absolutely not true. The SQLite db is the durable SSOT. Always has been. The intentd, just like the cli, which itself uses intentsvcs, all talk to the db. The daemon is only there for some other wider features that go beyond the original functionality of the single, per-project intent operations."_ The crates are `intent-cli`, `intentd`, `intentsvcs`; the db is a SQLite file all three talk to. **The SUBSTANCE of D01 is unchanged -- the db is the durable SSOT and the files are re-creatable. Only the term is wrong**, and it mattered because it implied a daemon-owned store. **The word was adopted from hv's own phrasing in two quoted rulings of 2026-08-15, which is why no node ever challenged it.** Corrected at `513642e7` in both restart files, `wip.md`, `.gitignore`, `design.md` and ST0056 canon; quoted rulings corrected in square brackets with an editorial note, never silently. Routed: `intentsvcs` doc comments (cc), `surface/dispatch-table` (ic), `bin/.devbin/cmd/precommit` (dc).
- **(2026-08-21) EVERY NODE PRUNES ITS OWN `target/<node>` AT FOLD.** **PROVENANCE UNVERIFIED (marked by vc 2026-08-21 on hv's ruling): recorded by a PRIOR vc session with no pointer to where hv said it. This session did not witness it. Treat as advisory until hv confirms.** Disposal joins creation in the same ritual: `/in-finish release` removes the per-node target dir the node made. Shared `target/debug` survives. Ruled after ~71G of build artefacts was found, 33G of it duplication produced by correct compliance with the isolate-the-target-dir rule, which said how to create these and nothing about removing them.
- **(2026-08-21) `upstream` IS PUSHABLE.** **PROVENANCE PARTIAL (vc 2026-08-21, on hv's ruling): the STATE is durable and I verified it -- `bin/.devbin/cmd/prepush:89` is `FROZEN_REMOTES=""`, and `:127` says a lift IS the deletion of the entry. The AUTHORSHIP is not: an empty variable cannot say hv emptied it. NOTE A CONTRADICTION, and it is vc's own -- `intent/restart.md` says _confirm with hv before any push there_ while this line says the lift is standing. Both are current; a node reading one will not read the other.** The freeze lift of 2026-08-20 is standing, not one-shot. Push both remotes.
- **ANNOUNCE A WRITE TO A SHARED FILE TO EVERYONE; ANNOUNCE A WRITE TO A CLAIM TO THE CLAIM-HOLDER. CANON IS THE FIRST, NOT THE SECOND.** cc, 2026-08-21, sharpening the rule cc gave vc an hour earlier. **Measured, not argued:** vc closed ST0056/03 + ST0057/03 and announced to **cc**, the claim-holder -- correct by the agreed rule -- **and the node it BLOCKED was dc, who holds no claim on ST0056 at all.** dc could not commit two edited parity tools without `canon_commit_check.sh` refusing correctly, and the only legal remedy (sync canon, commit together) would have carried vc's `wps[seq=3]` hunk into a dc commit about parity tooling, **wearing dc's authorship, with a semantic WP-close claim unattributable inside it.** **CLAIMS PREDICT WHO CARES, NOT WHO IS BLOCKED, and in a one-file-many-owners artefact those are different sets.** dc reached cc twice by elimination and cc could only answer because vc had told cc at 16:45Z -- **had vc not, the line would have read as OWNERLESS**, which dc named as the worse outcome before it happened. One cause, three faces: dc's gate, every node's suite (`attachment_drift_detected` AT-03.4, hit independently by cc and ic), and vc's uncommitted line sitting in the only path out.

### Open holds and facts that still bind

- **(2026-08-26 20:38Z, HELD FOR hv, MEASURED by dc 20:40Z, carried by vc) THE STAGING-DIR BUILD IS NO LONGER THEORETICAL: `guarded_release_build` runs `cargo clean` on the two binaries and rebuilds them in place, so `~/.local/bin/intent` (a symlink into `native/rust/target/release`, Lamplight decision #5 and every session's PATH) dangles for 66 seconds per build and the pair is HALF PRESENT for 61 of them (intentd back at 5s, intent at 66s). First production measurement: 252 refusals in Laksa's bucket carry during 2b's build. dc's item (2): build private, verify, move into place. Consumer-side mitigation in place tonight: every long run uses a pinned copy.**

  **(pen note, vc, 2026-08-28 14:00Z, CORRECTION -- this entry was WRONGLY ARCHIVED in the split hours earlier and is pulled back live.)** vc classed it spent as "superseded by the shared-artefact guard"; that guard refuses DIRT and says nothing about the WINDOW. Measured the same afternoon at the `4479264f` rebuild: ~40s no-binary here (this tree's critic gate failed CLOSED at exit 127 -- so "fails open" is also wrong for this tree's critic arm); and cc's finding that the window's SOCIAL mitigation recruits: vc's early warning about the closing window caused ic to HURRY a commit into it, stopped by an unrelated index.lock and not by any warning. ic proposes the guard REFUSE while the pair dangles (mechanical, not social); cc notes a guard needing the binary AS A TOOL is untouched by R1. **The open question for hv rides with dc's item (2): build private, verify, move into place -- plus whether gates refuse during the dangle.**

- **(2026-08-27 17:11Z, hv FIRST-HAND IN vc's SESSION, chosen from options vc authored) TWO SEQUENCING RULINGS, BOTH QUEUED FOR THE BOUNCE UNDER hv's OWN WIND-DOWN.**

  **(a) `install::publish_home()`'s CALLER IS `bootstrap`, IMPLEMENTED.** cc's recommendation and vc's. It is the only command whose NAME already means _set this machine up_, and the pointer is a machine-level fact -- one written by a diagnostic or by a per-project upgrade would read as surprising to whoever found it later. **The cost hv accepted: `intent bootstrap` returns rc=2 today, so this means implementing it minimally (this pointer plus the author identity it already owns), which is a separate piece of work from R1 and lengthens the path to the sweep.** Rejected: `doctor` (makes a diagnostic a writer) and a new `intent install` verb (new surface, disposition, parity apparatus, for a verb whose job may be one line).

  **cc DID NOT PICK THIS ITSELF AND WAS RIGHT NOT TO: the caller decides WHEN 17 ESTATES GET A POINTER WRITTEN, which is the sweep hv reserved.** Choosing it would have scheduled hv's announcement by implication.

  **(b) dc's FIVE-STEP `bin/int` -> `bin/devbin` RENAME GOES AFTER THE SWEEP.** The gate shim lands and sweeps first, then dc renames into a settled commit path. **dc explicitly did NOT ask for the hold to be lifted** even though its own argument had collapsed the risk -- hv ratified `bin/int` as the optional rule-2 shortcut, so nothing is deleted and no intermediate state is broken. Patch regenerated for the new plan and recorded on dc's board at `a06c4d85` so none of it dies with the session.

  **R1 IS CODE-COMPLETE AND UNCALLED: shim `645beec0` (10 arms, driven independently by vc), publisher `5fcfd314` (11 arms).** Nothing writes `~/.intent/home` yet, by design.

  **(pen note, vc, 2026-08-28 13:50Z, on hv's split ruling):** half (a) is EXECUTED -- `intent bootstrap` is implemented and ran at rc 0 on this machine, and `~/.intent/home` carries the pointer. Half (b) STANDS: dc's rename is sequenced after the carrier sweep, and both `bin/int` and `bin/devbin` exist today.

- **(2026-08-26 20:49Z, HELD FOR hv, measured by vc) LAMPLIGHT'S BUCKET CARRY IS NOT A TONIGHT JOB BY `st attach` PER FILE: on Laksa the per-file path runs 35 attaches/min idle and 4/min under a load of 40, and the store grows ~2 MB per small text file (1.87 -> 2.46 GB + 475 MB WAL over ~266 files). Lamplight has 3,738 stranded bucket files: ~7 GB of store and hours. The prose is safe where it is (buckets untouched, the prune gate verifies canon bytes per file). Options: (a) wait for cc's 3.0.1 bulk door (the WP-13 thread_dir walk carrying in one process) plus the store-growth fix; (b) run it overnight as is; (c) rule the Lamplight buckets stay as history like Devbin's. vc recommends (a).**

- **(2026-08-26 20:35Z, HELD FOR hv, from vc) LAMPLIGHT TRIAGE'S STORE HALF IS BLOCKED: hv's ruling (`_inbox` threads land as Triage) is in canon for ST0339-ST0342 but `sync --to-store` -- the only door -- is refused estate-wide by 22 `unknown-file-shape -- not valid UTF-8` files (ST0167/WP/01/screenshots/_.png, ST0085/clickable/_.png, intent/docs/notes/tn003.../{pdf,xlsx}). vc is NOT moving them aside. Options for hv: wait for cc's 3.0.1 ingest fix; move/rename them yourself; rule they leave the tree. Related: Lamplight decision #5 (`intent` on PATH as a symlink into the build tree, "the mitigation is attentional") cost 252 carry refusals during a 66-second rebuild tonight -- lamplight-vc put that to you directly.**

- **(2026-08-15) dc's SCOPE IS hv's FRAMING AND IS QUOTED RATHER THAN ELABORATED**, because the dc/cc boundary is not yet ruled -- `README.md` roster. **`bin/` is the one genuine collision** (v2 bash CLI plus the devbin) and is open for hv rather than assumed by either node.
- **(2026-08-22) SCOPE NOTE ON RULING 2 ABOVE, RAISED BY vc AGAINST vc's OWN ASK.** **hv answered the question that was filed, and the question that was filed understated it.** vc asked whether `claude skills` may read `$HOME`. ic's sweep (`5f008eb5`) landed after the filing and showed **`$HOME` is a SINGLE DOOR with SEVEN per-user stores behind it** -- v3's shipped surface reads exactly `COLUMNS`, so it can write no per-user state at all, and **the day the door opens all seven go live in the same instant**, six of them with no ruling and no design. **vc has ruled the CLASS rather than returning to hv for six more instances** (see vc's board, same date). hv may reverse; nothing depends on silence.

## Watch-outs

- **FOR hv ON RETURN -- THE SESSION'S THESIS, AND IT IS ABOUT WHETHER THIS ESTATE'S RULES CAN WORK AT ALL: READING THE WRITE-UP OF A CLASS IS NOT PROTECTION FROM IT.** 2026-08-22, ic's synthesis, corroborated by all four nodes against themselves. **Four instances in one day, and in EVERY one the written rule was present, recent, and authored by the person who then broke it.** cc read the paragraph about a pin measuring a NAME, quoted it approvingly, and measured a name within the hour. vc diagnosed ic's fabricated stamps and reproduced them four hours later **with vc's own remedy in capitals on vc's own board**. ic adopted the interval rule into canon and went on writing point stamps in messages. dc cited register-before-you-code to peers twice this week with four of their own modules unregistered. **NOT ONE OF THE DAY'S SAVES CAME FROM A RULE.** Every one came from a mechanism: **the clock read inside the same shell command as the write; a refusal built for a mapping error that happened to cover a currency error; two nodes driving one question independently and disagreeing; a test whose NAME carried its argument.** **The operational consequence, and it is the reason this is for hv rather than for a board: comprehension has never been the missing input, so any remedy shaped like comprehension -- a longer rule, a better-written watch-out, a reminder -- cannot be the fix.** The four rules of the road, the critics and the canon are all comprehension-shaped. **The guards, the gates and the clock-read-in-the-same-act are not, and they are what held today.** **SHARPENED 2026-08-23 BY dc, AND THE SHARPER FORM IS THE ONE hv SHOULD WEIGH: A RULE IS AT ITS LEAST PROTECTIVE IN THE SENTENCE THAT ASSERTS IT.** Not merely that reading a class does not protect against it -- **the act of STATING a rule is itself an act you do not audit, because your attention is on the rule and not on the stating of it.** **Three instances in one afternoon, each inside its own assertion:** vc escalated a MENU-AUTHORSHIP question to hv as a two-option menu vc authored, with the true third branch absent; **vc aggregated an undriven count into the very sentence complaining about undriven counts**; dc named a guard as their blocker **by quoting, verbatim and accurately, the parenthetical that disqualified the claim.** **Every one was produced by the careful version of the behaviour, not the sloppy one, which is why rigour was the disguise in all three.** **If comprehension-shaped remedies are weakest exactly where they are being applied, that is a stronger claim than the thesis and it points at the same conclusion.** **AND THE COMPANION, dc 2026-08-23, MEASURED TWICE IN ONE HOUR IN OPPOSITE DIRECTIONS: THE CORRECT SCOPE OF A RULE IS NOT VISIBLE FROM THE INCIDENT THAT PRODUCED IT, BECAUSE THE INCIDENT ONLY EVER SHOWS YOU ONE SIDE OF THE BOUNDARY.** dc's clock rule -- _do not leave the clock you measured on_ -- would have condemned `TZ=UTC stat`, **which was the remedy**; vc's _no sha in a focus line_ would have condemned a sha naming a PAST EVENT, **which is the fix rather than the defect, since a git object is immutable and a citation verifies itself.** **Both rules were derived from a real failure and both generalised one level past the thing that actually failed.** **THAT SENTENCE ORIGINALLY READ _every rule on every board here was derived from exactly one incident_. IT IS REFUTED, AND vc ASSERTED IT WITH ZERO MEASUREMENT INSIDE THE ENTRY ABOUT RULES DERIVED FROM SINGLE INCIDENTS.** **dc drove their own board: 15 of 17 watch-outs carry more than one date or explicit multi-instance language, 2 do not. vc re-drove the same method on vc's: 24 blocks, 20 multi-supported, 4 not** -- named, because an exclusion nobody prints is one nobody can challenge: the generated-view rule, the consistency-check-cannot-see-its-own-premise rule, the capability-with-no-consumer rule, and the hold-judged-under-either-answer rule. **THE INSTRUMENT'S LIMIT IS dc's AND IT IS THE HALF THAT MATTERS: IT MEASURES CURRENT SUPPORT, NOT DERIVATION.** Several of those were derived from exactly one incident and acquired the second LATER. **So the mechanism stands and the consequence overstated the damage: the corpus is not uniformly one-sided, it is mostly rules that have since been tested from the other side.** **THE REMEDY IS dc's AND IT IS CHEAPER AND MORE ACTIONABLE THAN _the corpus is suspect_: A RULE IS PROVISIONAL UNTIL SOMETHING ON THE OTHER SIDE OF ITS BOUNDARY HAS TESTED IT.** Today's three pairs are the worked examples, and in each the pair did what neither half could -- **vc's parenthesis rule plus dc's inverse; dc's clock rule plus vc's `TZ=UTC stat`; vc's no-sha rule plus dc's durable citation.** Each half was right about its own incident and one notch too broad alone. **THE ACTIONABLE GAP: the untested ones are not WRONG, they are UNTESTED, and nothing on any board distinguishes those two states -- while `rulings` already carries `provisional` as an authored state one artefact over.** Recorded by vc 2026-08-23 13:30Z, correcting vc. Recorded by vc 2026-08-23 13:27Z. Recorded by vc 2026-08-23 13:21Z.

- **OPEN FOR hv, AND IT WILL BITE hv FIRST: MAY A CATCH-ALL COMMIT SWEEP A LIVE FIVE-NODE CHECKOUT?** 2026-08-22, raised by vc under hv's pen and NOT ruled, because it is a constraint on hv's own hands rather than on the nodes'. **The measured instance is `6c603b21 "Pre-release"`, which swept four nodes' in-flight work into one commit -- cc's 1,554 mid-build lines, cc's single-writer board, ic's mid-flight table edits -- and produced the rustfmt failure that blocked the push and read as a style nit.** **The gate was right and the commit was the defect.** Two mechanisms found the same day make it structural rather than a one-off: **`git commit --only <paths>` bounds what is COMMITTED and bounds nothing about the GATE, because the guards read the INDEX** (dc), and **every canon-writing verb ingests a peer's uncommitted working-tree attachment bytes into canon, warning without refusing** (vc). **So in this checkout the index and canon are SHARED OBJECTS even when the commit is not, and a catch-all `git add -A` reaches both.** vc's recommendation, offered rather than ruled: **every commit here names its paths and carries its node prefix, hv included** -- not because hv is bound by node convention, but because the alternative has now cost a blocked push and nearly cost two peers their uncommitted work. **hv to rule; nothing depends on silence.**

- **OPEN FOR hv -- DOES THE `sync` SKIP RULING SUPERSEDE AC-03.6, OR IS THE RELAY WIDER THAN THE RULING?** 2026-08-23, raised by vc under hv's pen and **DELIBERATELY NOT RULED**, because ruling it would let a second-hand relay override ratified canon on vc's signature. **cc holds the ruling as _`sync` SKIPS UNTRACKED BYTES, LOUDLY_ -- second-hand, hv via dc, recorded on cc's board -- and cc declined to build against their own lean on it, which is why this was found at all.** **AC-03.6's ratified text (dc 2026-08-18) points the other way in three places**, and vc read it at source rather than from memory: _the harm is not at sync ... it becomes permanent and inspectable-but-wrong at the COMMIT_; _the compliant order is sync canon FIRST -- **because sync reads the worktree** -- then commit the file and canon together_; and **a criterion worded as _a sync must not ingest uncommitted bytes_ describes only the canon-ahead direction and misses the file-ahead one entirely, which is the direction that was actually live (`3f10b1ee`, clean worktree).** **A skip at sync therefore breaks the prescribed order and cannot see the live failure mode.** **The tell is cc's own: `render.rs:391` says _this run does not refuse, and the commit gate will_ -- TRUE today, FALSE under the ruling. That sentence IS the layered architecture stated, so its going false is evidence the ruling RELOCATES the control AC-03.6 put at the commit, not evidence the sentence has gone stale.** **vc FRAMED THIS AS AN EITHER/OR AND IT IS A FALSE ONE. dc TRACED THEIR OWN RELAY RATHER THAN DEFENDING IT AND FOUND A THIRD BRANCH ONLY THE RELAY COULD SEE (dc, 2026-08-23):** **hv ruled by SELECTING AMONG FOUR OPTIONS dc AUTHORED. The four are recorded nowhere -- only the words _I put four options_ survive, on dc's board -- and the relay to cc went over the live channel with no inbox entry, so NOTHING ANYWHERE HOLDS hv's ACTUAL WORDS.** **The authority is hv's and the WORDING IS dc's, including _fix at the source, not the door_ -- which is precisely the clause that relocates the control AC-03.6 puts at the commit.** **So the third branch: if dc's four options never surfaced the file-ahead direction, hv could not have knowingly superseded anything -- hv chose inside a frame dc built. An option that was never on the menu was not DECLINED; it was never RULED ON, and no reader downstream can tell those apart.** **dc will not reconstruct the four, and that refusal is correct: a remembered menu is a fabricated one wearing the authority of the choice made from it.** **Therefore this cannot be closed from any record that exists, and that is the ANSWER rather than an obstacle to it. hv is the only one who can say what they chose and why. Ask hv; do not derive it.** Recorded by vc 2026-08-23 13:14Z. Measured at `6ab7e961`, 2026-08-23 13:11Z. **cc's TODO 3 is HELD pending this; `--force` is unaffected and proceeds.** **A LIVE COUNTER-EXAMPLE ARRIVED WHILE THIS ENTRY WAS BEING WRITTEN, PRODUCED BY NORMAL WORK RATHER THAN BY ARGUMENT: ic's `ratified_in_check.sh` -> `rulings_check.sh` rename.** Canon holds the OLD path's bytes, so `canon_commit_check.sh` refuses the commit -- **AC-03.6 firing in production** -- and the gate prints the remedy verbatim: _Sync canon FIRST -- it reads the WORKTREE -- then commit the file(s) and canon together._ **Canon cannot learn the new path by any other route. Under the relayed ruling as cc holds it, this rename could not be committed compliantly at all.** Neither vc nor ic arranged this; it is the ordinary shape of renaming a tracked attachment. **LANDED AT `8c0d7ce5`, AND COMPLETING IT MADE THE COUNTER-EXAMPLE STRONGER IN A SECOND WAY ic FOUND BY DOING IT: `sync --to-store` IS ADDITIVE FOR ATTACHMENTS AND NEVER PRUNES THE PREDECESSOR** (canon went 90 -> 91, keeping the old path), **and NO VERB PRUNES ONE.** ic removed the stale record by hand under two assertions -- the old file is really gone AND the successor really exists -- **because deleting a canon record on a path that still exists is not a rename, it is a loss.** **So: sync must read the worktree for a rename to be LANDABLE at all, and even reading it, it cannot COMPLETE one.** This belongs with AC-08.5's family -- no operation narrower than a thread for an attachment, and now also none that removes one. **THIRD FACT ON THE SAME SURFACE, HIT BY vc DOING SOMETHING ROUTINE 2026-08-23: A HAND-PRUNE OF CANON DOES NOT SURVIVE A ROUND-TRIP.** `int cli at green ST0056 AT-07.3` -- a STATUS move -- plus a `sync --to-store` to carry a note **put `parity/tools/ratified_in_check.sh` BACK into canon, 91 attachments against HEAD's 90, naming a path ic deleted at `8c0d7ce5` that does not exist on disk.** **The store still held what ic had removed from the extract, and the next verb writing canon from the store restored it. NOTHING REPORTED IT** -- the only warning was `ST0056: differs on disk`, which is true of every sync. **So the three compose into something none of them says alone: sync must read the worktree for a rename to be LANDABLE, it cannot COMPLETE one, and it silently REVERTS the only available hand-repair.** **AND ic's DIAGNOSIS AT THE SOURCE IS SHARPER THAN vc's AND CORRECTS IT: THE REMEDY WAS NOT PERISHABLE, IT WAS POINTED AT THE WRONG OBJECT.** ic queried `intent/.cache/intent.db` directly: **the store holds BOTH records, `ratified_in_check.sh` at 13671 bytes and `rulings_check.sh` at 14663. The record each of us removed from canon NEVER LEFT THE STORE.** **In the estate's own terms -- the store is the SSOT and canon is the EXTRACT, D01 as hv reversed it -- a hand-edit to canon is DEFINITIONALLY temporary: it repairs the PROJECTION and leaves the SOURCE holding the thing that regenerates it.** No store-to-canon write was ever going to respect it, and it did not need to be a sync; vc's was a status move. **SO THE THREE FACTS COLLAPSE INTO ONE, AND IT IS BIGGER: THERE IS NO ATTACHMENT PRUNE AT ALL, AND CANON-SIDE EDITS CANNOT SUPPLY ONE, BECAUSE CANON IS NOT WHERE ATTACHMENTS LIVE. The gap is STORE-SIDE and the surface has no door to it -- which is AC-08.5's population question exactly, one entity deeper than anyone has been looking.** **ic ALSO DECLINED TO REPORT A FIGURE AND THE REFUSAL IS THE FINDING: 190 of 292 store attachments name a path that does not exist, and that is MOSTLY THE DESIGN WORKING** -- disk is a sparse projection, an artefact realises on demand, so a store attachment with no file is the NORMAL state. **ic's check could not separate _stale after a rename_ from _never realised_, so the alarming number measures the wrong thing.** The verified finding is the specific pair, not a population. Recorded by vc 2026-08-23 13:50Z, correcting vc. **The gate would have refused the commit -- canon naming bytes the commit does not carry, AC-03.6 exactly -- and vc did NOT drive that**, because staging a known-bad canon into a SHARED index while cc was mid-build is the hazard on the entry above; the file was repaired first and committed clean at `ADDS 0 of 90`. **DRIVEN BY ic IN A THROWAWAY CLONE 2026-08-23, SO THIS IS NOW MEASURED RATHER THAN INFERRED -- AND WITH A POSITIVE CONTROL, WHICH IS THE CASE THAT MAKES THE OTHER TWO MEAN ANYTHING.** **(A)** bytes change AND canon updated to match -> **rc=0, `ADDS 0 of 90 examined`**; **(B)** bytes change, canon not updated -> **rc=1, names `rulings_check.sh`**; **(C)** canon carries a record whose FILE DOES NOT EXIST -- vc's exact incident -- **rc=1, names `ratified_in_check.sh`.** **So the refusal is a FUNCTION OF THE DIVERGENCE and not a disposition of the gate.** ic also recovered the pre-rename file from `8c0d7ce5~1` at **13671 bytes, byte-identical to what the store still holds**, confirming from two independent sources that the object the store would resurrect is the object the gate refuses. **AND THE LIMIT IS THE PART hv SHOULD READ TWICE, BECAUSE IT IS WHY A GREEN HERE IS NOT A CLOSED CLASS: THE GATE FIRES AT COMMIT AND THE STORE PUT THE RECORD BACK AT A SYNC.** Between those two moments canon on disk is wrong and **nothing says so**; vc met it by MEASURING after a status move, not by being told. **The gate is a backstop on the commit path, not a detector of the underlying defect, and the defect remains store-side with no door to it.** **ONE NUANCE AGAINST THE THESIS ABOVE, RECORDED BECAUSE IT CUTS THE OTHER WAY AND SHOULD NOT BE BURIED: what stopped ic publishing the misleading 190 was NOT a rule and NOT a mechanism -- it was KNOWING WHAT ST0057 SAYS DISK IS.** The instrument's wrongness was invisible from inside the instrument and visible only from the architecture. **That is a third kind of input -- domain knowledge of what the system IS, as against a rule about how to behave -- and it is the one save today that came from comprehension.** **ic's own operational caveat is the sting: the next person will not have that architecture in context, so the save was not REPEATABLE. It depended on a fact that happened to be in one node's head, which is exactly the property a mechanism has and comprehension does not.** **ic RESOLVED THIS RATHER THAN LEAVING IT AS A QUALIFICATION, AND THE RESOLUTION IS WHAT hv SHOULD READ: DOMAIN KNOWLEDGE IS NOT A COMPETITOR TO MECHANISM, IT IS THE INPUT TO ONE.** Knowing that disk is a sparse projection is exactly the fact that belongs INSIDE the instrument -- **had the census asserted its own denominator (_this check cannot separate unrealised from stale; here is what would_), the next reader gets the fact without needing it in their head.** **The save was comprehension; the FIX is to ENCODE the comprehension. So the thesis holds unqualified -- what ic's architecture knowledge bought was not a control but the ABILITY TO WRITE ONE.** **AND THE TWO PHRASINGS PRESCRIBE OPPOSITE THINGS, WHICH IS WHY THE WORDING IS NOT COSMETIC: _comprehension is unreliable_ prescribes building more mechanisms and not bothering to understand the system; _comprehension is the input to mechanism_ prescribes understanding it and then encoding what you learned.** The second is what actually happened here. **ic's SECOND FINDING IS NEW TODAY AND IS THE ACTIONABLE ONE: THROWAWAY INSTRUMENTS ARE WHERE UNDECLARED BLIND SPOTS LIVE, AND THE DISPOSABILITY IS CAUSAL RATHER THAN INCIDENTAL.** Nobody writes a caveat into a script they are about to delete, **and a throwaway's OUTPUT outlives it by exactly as long as it takes to paste a number into a message.** **Five named instruments today were disposable: cc's self-answering grep, ic's `mode=ro` census, ic's extension-filtered scoping grep, vc's `| head` listing quoted as `10+`, and vc's `tail -3` guard misattribution. THREE had their output quoted to a peer; one was stopped at the moment of pasting; one failed silently while scoping a rename and gated the estate.** **THE MECHANISM, ic's: an instrument whose OUTPUT will be quoted to another node is NOT a throwaway, whatever its lifetime, and owes a stated denominator before its number is repeatable.** Cheap, checkable at the moment of pasting, and it would have caught all five. Recorded by vc 2026-08-23 13:56Z. Recorded by vc 2026-08-23 13:55Z. Recorded by vc 2026-08-23 13:47Z.

- **THIS BOARD RECORDS WHAT WAS RULED AND NEVER WHAT WAS DONE, SO EVERY ENTRY READS AS OUTSTANDING FOREVER.** 2026-08-22, vc's own, against the artefact vc is custodian of: **vc briefed cc to build AC-08.5 off the standing directive here, and the arm hv ruled had been LANDED for a day** -- `3f9b2907`, with hv's ruling quoted verbatim and dated at `facade.rs:4113-4118`. **cc checked the brief instead of taking it and caught it before any code.** A directives board has no expiry mechanism and cannot acquire one cheaply: **a completion field would be a second record of what git already knows**, which is the defect ic and vc spent the same hour ruling out of `ratified_in`. **The standing rule is the cheap half: before briefing anyone off a directive here, DRIVE whether it is already built.** It is hv's own directive two entries up, one noun changed -- _a criterion resting on an undispatched instrument gets driven, not argued about_ -- and **the second time in one day that cc declined to act on vc's word and was right.**

- **hv's RULINGS REACH THIS BOARD SECOND-HAND, DATED TO THE DAY, AND THE DAY IS NOT ENOUGH RESOLUTION.** 2026-08-21: cc and dc reported the same three-item ruling **in opposite directions**, both correctly stamped 14:30Z, both correctly carrying **no time hv could be quoted on** -- which is the transcription rule working exactly as written. **If hv spoke to cc first and dc later, both reports are true and dc's supersedes; nothing on the board can establish that.** The rule adopted 2026-08-20 solves ATTRIBUTION and is silent on ORDERING. **A same-day reversal is invisible here, and the node holding this pen has to pick one to write down.** Open for hv: either hv's word carries a time, or a reversal is announced rather than merely superseding.
- **A BENEFIT RECLASSIFIED AS A SIDE EFFECT ESCAPES THE RULING THAT GOVERNS IT.** 2026-08-21, vc's own, against a directive vc is custodian of: hv had **already declined direnv** as the guard-resolution mechanism because git hooks do not reliably inherit it. vc shipped `.envrc`, **disclaimed it as the mechanism**, then claimed the mechanism's benefit as a wanted side effect. Driven non-interactively -- which is what a hook gets, and **what every node's tool-call commit gets** -- `INTENT_HOME` is still Intentv2 and the guard runner still resolves to the frozen checkout. **The exposure is OPEN; the claim was inert for every commit any node makes.** Struck at source. **The disclaimer is what let it through.**
- **A FALSE CONFESSION PASSES EVERY CHECK WE HAVE, BECAUSE IT COSTS THE CLAIMANT.** 2026-08-21: vc committed `af91be8b` blaming `git commit --only` for clearing a peer's stage. **Tested: `--only` preserves a peer's stage in both the success and failure paths.** dc's pathspec-less `git reset` did it. Our rules are tuned for claims that are CONVENIENT to the claimant; **there is no rule for one that inconveniently blames you, and owning a fault reads as rigour so nobody audits it.** A wrong self-blame is not the safe direction: it attached a hazard to `--only` that `--only` lacks, and hid the real one -- **a bare `git reset` is a shared-index operation in a multi-node checkout, which no standing rule names.** For hv as a standing constraint, since the correction and the new rule are one edit.
- **`intent sync --to-disk <ID>` IS A WHOLE-FILE SECOND WRITER OVER EVERY ATTACHMENT OF THAT THREAD.** dc, 2026-08-21: nearly committed cc's unreviewed work under dc's name; a line-count check stopped it. **No reflog trace.** Announce the FILES, not the thread.

- **A DIRECTIVE THAT IS NOT REACHABLE FROM THE SITE THAT NEEDS IT IS AN OPEN QUESTION IN PRACTICE.** This board was a stub for eight weeks while peers were told to read it. That is the defect this board now exists to close.
- **ATTRIBUTE hv's LIVE STAMPS, NEVER ASSERT THEM.** The live channel is unguarded -- all three clock-guard checks run at commit. A peer quoting a live stamp into their board launders it into the committed record past a guard posted at the wrong door.

## Decisions

- (2026-08-21) This board's writer is `vc`, on hv's ruling, under the provenance rule above.

---

## Rulings made and NOT yet executed

**RESTORED TO THIS BOARD 2026-08-31 10:11Z, AND THE REASON IS A DEFECT IN THE FOLD THAT REMOVED THEM -- vc's, under vc's pen.** Found by dc, who went looking for whether hv had already answered a question before asking it again, and found the answer in `.history/`.

**hv's rule keys on DONE. THE FOLD KEYED ON DATED.** The ruling being enforced was hv's own -- _"cut ANY mention of done work out of the wip.md and restart.md docs. They should ONLY EVER contain doing and todo work, no exceptions"_ -- and the 2026-08-30 fold moved the whole dated ruling record to `.history/` on the strength of it. **A ruling nobody has executed is TODO WORK BY THAT RULE, so the fold enforcing "doing and todo only" is what removed todo items.** Nothing was deleted and every word is still in `.history/20260830/wip-fold-0905Z.md`, which is exactly the problem: **a live directive reachable only by grepping an archive is discoverable by nobody, because grepping `.history/` is not a thing a node does at pickup.**

**THE RULE THAT REPLACES THE ONE THE FOLD USED: AN UNEXECUTED RULING IS LIVE STATE, NOT HISTORY. A fold archives the NARRATIVE of a ruling and never the ruling itself while it is unexecuted.** Execution status is the discriminator; the date is not evidence of anything. This section is where an unexecuted ruling lives until someone executes it, and an entry LEAVES it by being executed, never by ageing.

**These THREE are what an audit turned up: two from the 2026-08-26 eight-ruling pass, and one from the 2026-08-28/29 pass driven straight after. It is not a proof that there are no others** -- the six-ruling pass of 2026-08-27 and everything before it have not been driven, and vc owns that sweep.

- **A4 `fileindex` -- RETIRE IT, correct the table.** (hv, 2026-08-26 00:19Z, verbatim at `.history/20260830/wip-fold-0905Z.md:179`.) Menu: RETIRE **CHOSEN**; BUILD IT (declined, uncosted); SHIP UNBUILT AND DECLARED (declined). Under the same evening's `AC-00.5` ruling a retirement must become ENUMERABLE. **STATE AT 2026-08-31: UNEXECUTED, AND WHAT SHIPS IS THE OPTION hv DECLINED.** Measured by dc: `surface/dispatch-table.json` still reads `disposition: keep` with `replacement` absent; `fileindex` is in `--help`, refuses at rc=2 with the UNWIRED marker (_not implemented yet_), and is ABSENT from `intent surface retired` -- where `treeindex`, retired by the same mechanism, is listed and refuses by name. **The two refusals say different things and only one is true: _not implemented yet_ means LATER and hv ruled NEVER.** That is `AC-00.5`'s own subject firing on the verb its ruling was quoted at. Owner: cc (`owner_wp: WP-06`).
- **A6 ic's `## Holds` -- LAND IT, with the CONDITION as the required field.** (hv, 2026-08-26 00:19Z, same pass.) dc's sharpening, adopted: the condition is load-bearing, not the lifter. Menu: LAND **CHOSEN**; AFTER THE CUT; DECLINE. Taken knowingly as a shipped-skill change with fleet blast radius. **STATE AT 2026-08-31: UNEXECUTED.** `intent/plugins/claude/skills/in-whiteboard/SKILL.md` has no `## Holds` section; its only occurrence of the word is ordinary prose about inboxes at `:119`.
- **ISSUE WRITE PATH -- BUILD THE PACKAGE.** (hv, 2026-08-28/29 pass, verbatim at `.history/20260830/wip-fold-0905Z.md:116`.) Menu: package **CHOSEN**; edit verb only; leave as-is. Named: `issues edit` (`--body`/`--from`), `issues close --note`, scoped issue sync, then restore the 20 empty bodies from commit archaeology. `0122`'s scope correction is the edit verb's FIRST DRIVE and `0118`'s re-premise its second -- the live cases become the acceptance cases. S-M, **cc's lane after Laksa and `0121`**. **STATE AT 2026-08-31: UNEXECUTED, NONE OF IT.** `intent issues` ships `list add show close open` and no `edit`; `issues close` takes no `--note`.
- **AND THIS ONE MEASURES WHAT THE FOLD COST, IN vc's OWN WORK.** On 2026-08-30 vc filed issue `0179` -- _there is no verb that can correct an issue body ... an issue body is write-once ... post-tag, and cheap: either an `issues edit` verb, or `intent edit` accepting the kinds its own address grammar already parses_ -- **as a fresh discovery, with a proposed remedy, four days after hv had ruled that exact package built.** vc could not see the ruling because vc's own fold had moved it to `.history/`. **A buried ruling does not merely go unexecuted; it gets independently rediscovered as a defect and re-costed by the node that buried it.**

## The dated ruling record

**Every ruling hv has made, verbatim and attributed, is in `.history/` -- BUT AN UNEXECUTED ONE IS NOT HISTORY AND BELONGS IN THE SECTION ABOVE.** The 2026-08-30 fold did not make that distinction and buried at least two live directives; the rule that replaces it is stated there.

**Every ruling hv has made, verbatim and attributed, is in `.history/`.** It left this file in the 2026-08-30 fold and nothing was deleted: `.history/20260830/wip-fold-0905Z.md` is the 137,298-byte board as it stood, and `.history/20260828/standing-directives-spent.md` is the earlier split.

**Why it left.** hv's ruling of 2026-08-29 evening -- _"cut ANY mention of done work out of the wip.md and restart.md docs. They should ONLY EVER contain doing and todo work, no exceptions"_ -- names this file: it is a `wip.md`. The 2026-08-30 globalfold applied that rule to `intent/wip.md`, `intent/restart.md` and the four peer boards **and skipped this one**, which was the largest violation of the rule being enforced. The Conflab hoist section alone was 41,268 bytes -- 30% of the board -- and Conflab has been on `3.0.0 (0751c42b)` since 2026-08-28.

**This is the third watch-out below, acted on rather than restated:** _this board records what was ruled and never what was done, so every entry reads as outstanding forever._ hv ruled the live/spent split on 2026-08-28 (`THE SPLIT -> LAND IT`); it was landed that afternoon, and the board grew back by 41KB in two days. **A split is not a structure, it is an event, and it has to be re-run.**
