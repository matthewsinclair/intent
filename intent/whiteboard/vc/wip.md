---
node: vc
name: Validation Claude
role: validation
session_id: 1aa05d4a-6da2-4c42-98c6-de024aebab69
heartbeat_at: 2026-09-02 08:17Z
status: active
focus: "FOLDED 2026-09-02 08:12Z, pre-fold verbatim + sha-verified at .history/20260902/wip-fold-0809Z.md (ec84100b, 59476 bytes). THE FIRST FOLD ATE THE NUMBERED hv LIST -- restored from the archive byte-identical and re-cut with a boundary that respects numbered items; the id check is what caught it. FIVE THINGS WAIT ON hv, each with context+options+recommendation below. THE BOARD RULE: printing the command is not running it, so a live figure here is the COMMAND ALONE."
claims: [ST0056, ST0057, ST0060, ST0064, ST0068]
---

# Validation Claude (vc)

**PROJECT-WIDE RULES IN `intent/restart.md`. Every incident narrative is in `.history/`. What follows is MECHANISMS and UNEXECUTED WORK.**

**AND THE RULE THAT GOVERNS THIS BOARD, ESTABLISHED 2026-09-01 AND THE MOST IMPORTANT THING ON IT: PRINTING THE COMMAND IS NOT RUNNING IT.** A figure with its regenerating command beside it is AUDITABLE, not CURRENT, and no reader can tell which they are holding -- **proved by TN001's crate table going stale UNDER a caption boasting it was cargo-verified, with three revisions reading past it.** **SO: A LIVE FIGURE APPEARS HERE AS THE COMMAND ALONE, WITH THE NUMBER DELETED** -- a printed number gets read instead of run. **A HISTORICAL FIGURE IS DATED, and stops being a live claim.** Every entry below is written to that rule or is owed a fold.

## DOING

### THE ACCEPTANCE GATE CANNOT GO RED FROM CODE CHANGING -- baize-vc's FINDING A REPLICATES HERE, AND THE PROOF IS STRONGER THAN THEIRS

**(vc, 2026-09-01 17:58Z, driven at HEAD. TODO item 8 discharged, and item 8 said it was bigger than anything else on this board. It was.)**

**baize-vc falsified this in Baize on 2026-08-27 with ONE positive control** -- broke a test `AT-02.1` covers, proved the mutation real, and the gate still returned `15/15 PASS` with the covered test RED. **I did not re-run their control. I established the WRITER SET instead, which is exhaustive where a single control is one instance.**

**THE CHAIN, EVERY LINK DRIVEN:** `intent ac gate <ST>` resolves a test-backed criterion at `crates/intentsvcs/src/contract.rs:137` -- `if covering.iter().all(|t| t.status == AtStatus::Green)`. **That is the entire computation.** `t.status` is a stored column in the `tests` table (`store.rs:1968`). **Every writer of `AtStatus::Green` in the tree is a human verb parsing the literal string `"green"`** -- `render.rs:2615` and `:2706`, their MCP twins at `mcp.rs:842` and `:866`, plus the v2 importer at `legacy.rs:1909`; the remainder are `contract.rs`'s own unit fixtures. **And `intent at` offers `list/lint/green/red/na/new/edit` -- NO verify, NO run.** Re-drive with `intent at --help` and the greps above; do not read this off the board.

**SO NO CODE CHANGE CAN TURN THE GATE RED. It goes red when a person types `intent at red`, and at no other time.** Every gate figure this project has quoted is a claim about what someone typed -- **including the `110/135 satisfied` on hv's board, which is the number the v3.0.1 cut is being measured by.**

**WHAT THE GATE DOES DO, because the finding is not _the gate is worthless_ and must not be reported as that.** It enforces real contract invariants: an AT must cover a criterion that exists; a non-test AT can never be green so it can never satisfy a test-backed criterion (`contract.rs:716`); a test-backed criterion recording its own satisfaction is refused as double truth (`doctor.rs:707`); an AT covering nothing is flagged as unable to satisfy anything (`doctor.rs:569`). **It is a CONTRACT-CONSISTENCY gate and it is a good one. It is not a BEHAVIOUR gate, and both its name and this project's use of it say it is.**

**FOR hv, AND IT BEARS ON WHAT _FEATURE COMPLETE_ MEANS:** the 3.0.1 finish line is measured by a gate that cannot detect a regression. That is a scope question, not a defect vc may fix.

### THE CUT -- RUN THE VERBS, NEVER READ A FIGURE OFF THIS BOARD

`intent ac gate <ST>`; `bash intent/st/ST0056/parity/tools/gen_cut_surface.sh`. **`intent --version` names the commit the binary was built from, which is NOT HEAD -- and that difference ALONE IS NOT STALENESS.** Currency is whether any non-test file under `native/rust` moved since that commit. **Pin by the sha256, never by the marker** -- three distinct binaries have carried one marker in a day, and **two artefacts both answer `3.0.0` with 1516 commits between them**, so _does X ship in 3.0.0_ is unanswerable from the version string.

### THE RELEASE IS `v3.0.1`, FEATURE COMPLETE, AND THE NUMBER QUESTION IS CLOSED

**hv, first-hand, 2026-09-01 08:32Z: everything outstanding goes into `3.0.1`, cost is not a constraint, there is no external consumer. NOT TO BE PUT AGAIN, IN ANY FORM.** I asked three times across three days by re-deriving it from the command delta -- **which is not evidence about the NUMBER and never was.** **THE SCARCITY REGISTER IS RETIRED AS A CLASS**: _ship it red_, _not in this cut_, _after the tag_, _new machinery in a tag window_ all protected a date that does not exist. **Any recommendation of mine resting on _there is not time_ is withdrawn unless re-derived on its merits.** **After striking a class, GREP THE ESTATE FOR THE CLASS'S OWN PHRASES** -- I struck it, carried it to three nodes, and left its clearest live instance on hv's desk two minutes downstream of the ruling.

### `AC-00.16` -- SIDE C LANDED, RED FOR A NEW REASON

The per-property union exists. **What keeps it red is that the criterion's first conjunct is NOT MACHINE-DECIDABLE.** The fix is an amendment and hv's -- **a delegated pen does not cure it, because holding the pen does not change who benefits from my own red row going green.** **AND MINTING THE POPULATION/FORMS/REACH SHAPE MAY MAKE IT DECIDABLE:** the row is red because the set of ways to state a limit is open, so any keyword probe undercounts and **its false positives are precisely the instruments that stated their limit WELL**. Conformance to a MINTED form is greppable where good practice is not. `THE FORM MUST BE MINTED BEFORE THE POPULATION CAN BE` (ic) is the prerequisite that row was missing.

## TODO

1. **DISCHARGED 2026-09-01 19:17Z. IT WAS NEVER A BUILD -- IT WAS A RULING vc OWED, AND vc WAS THE GATE FOR A FORTNIGHT.** `AC-00.11`'s false citation is withdrawn at `dec1554a` (the row named `c51f10d5` as the defect live; that commit is the FIX and the defect was never reproduced anywhere, so it is withdrawn rather than replaced). **LANDED `83c512c2` 2026-09-02 (verified here independently: 4 paths exactly, note 4028 -> 8462 with the original preserved VERBATIM at offset 0, status `to-write` -> `green`). THE PROVENANCE DESTRUCTION DID NOT HAPPEN -- see 8t for the recipe defect that would have caused it.** The instrument was landed with the header -- header reframed to match the body's verdict logic, two-tree machinery KEPT, and a line added that the ruling never had: A+B are revision-independent but fire only post-absurdity, **C is the only arm that catches the defect while it still closes**, `86 of 278` named as the proof. See 8p and 8q.
2. **`AT-00.2`, `AC-00.8`, `AC-00.10`** -- behind cc's WP-10, behind WP-06 and WP-07. Sequence around it.
3. **`0136`** (hv ruled: after the tag); **`intent/wip.md`** before the tag.
4. **RE-DRIVEN 2026-09-01 20:46Z, AND THE CHARACTERISATION WAS WRONG: IT IS NOT BLOCKED ON A JUDGEMENT, IT IS BLOCKED ON A FULL-SUITE RUN ONLY hv MAY TAKE.** This item read _BLOCKED ON THE RECLASSIFICATION_, which reads as adjudication somebody here could do. **`coverage_map.sh` states the remedy itself: _re-run `burn.sh` over the current estate. Do not hand-patch the TSV -- a baseline stitched from several revisions is the drift this check exists to catch._** So partial repair is REFUSED BY NAME and the only legal move is a whole fresh burn. **`burn.sh` runs every `.bats` file TWICE** -- once at the default `INTENT_BIN`, once at `/usr/bin/false`, the delta being the tests that actually reach the CLI -- so the cost is two passes over the whole estate. **That is the full-suite class, which is hv's hand, not a node's.** Run `bash intent/st/ST0056/parity/tools/coverage_map.sh` for the live discrepancy set and `grep -vcE '^#|^$'` the baseline against `find tests -name '*.bats' | wc -l` for the gap; **no figures here, and the one this item used to carry was already wrong by one.** Ordering is unchanged and still correct: fresh burn -> UNSTABLE drops -> regenerate. **`gen_register.sh` reads the COMMITTED baseline, so regenerating first would refresh the _Measured at_ line over 2026-08-15 classifications -- laundering the staleness rather than fixing it.**
5. **`design.md:88`** still says `rmcp (official SDK): stdio ... now`, which my ruling reversed. Design prose is hv's hand -- **a flag, not an edit.**
6. **OWED, DELIBERATELY NOT FILED: the INDEX layer of the three-layer property has neither a row nor a name.** One observed instance, no measurement. **Filing it now would give it a row before anyone drove it.** It earns a row on evidence.
7. **FILED 2026-09-01 18:08Z AS `0211`, HIGH. DISCHARGED.** _A criterion cannot name what would discharge it until it is discharged._ **The filing rests on the INTERFACE, not on the census I had been holding**: `ac satisfy` is the only verb that takes evidence, `--evidence` is REQUIRED on it, and no verb attaches evidence to an unsatisfied criterion -- so evidence and discharge are one event. **What made it a defect rather than a modelling preference is that the SIBLING HAS THE SLOT**: `at green --note` is optional and lands atomically, so an AT is never green-and-unexplained. The model is inconsistent between its two halves. **Cost cited from baize-vc's port** (31 fragments destroyed, 28 unrestorable) **because it is measured on a real estate rather than predicted here.** The issue carries NO census figure and says so -- `intent ac list <STID>` is the re-drive.
8. **RE-DRIVE baize-vc's FINDING A AGAINST OUR HEAD -- IT IS BIGGER THAN ANYTHING ELSE ON THIS BOARD.** Claim: **the acceptance gate CANNOT FAIL.** Falsified in Baize 2026-08-27 with a positive control and a prediction written first -- broke the test `AT-02.1` covers, proved the mutation real through a calibrated seal, and the gate returned `15/15 PASS` with the covered test RED. **Mechanism corroborated HERE from data already in hand: the `at` family is `list/lint/green/red/na/new/edit` -- no verify, no run mode -- and `status: green` is a STORED STRING that stands until a human types `intent at red`.** **NOT re-driven here; their figure is five days old and Intent has moved.** Records at Baize `intent/whiteboard/vc/.history/20260827/session-20.md` and `session-21.md`. **If it holds, every gate figure I have quoted is a claim about what someone typed.**

## Holds

**Each carries the CONDITION that releases it. A hold with no condition is an abandonment.**

- **HOLD RELEASED 2026-09-01 17:58Z by hv, unqualified, and DISCHARGED.** Every expiry it named was re-driven rather than recalled; the rollout is committed at `03d40d30`. Nothing rests on it.
- **`ST0068` AC-02.1 -- and the thread prefix is NOT decoration: `ST0056` ALSO has an `AC-02.1`.** I read my own bare note onto the wrong thread. **CONDITION: A RELEASE, NOT A BUILD.** `--note` landed `6fa22a79`; `docs/getting-started.md:124,126,135` instruct it; the keg has none.
- **`AT-07.5`'s behavioural arm is not re-verifiable while a daemon runs**, and one always is. Green with the caveat; the tool REFUSES rather than fails. **Condition: hv authorises a daemon-down window. A node must not take one.**

## hv items

**FOLDED 2026-09-02 16:37Z. Everything below is OPEN. Executed items went to `.history/20260902/wip-fold-1637Z.md` (sha `5730cc70`, 85607 bytes) with their reasoning; a fold never archives an UNEXECUTED ruling.**

**THE FOLD WAS VERIFIED TWO-SIDED PER 8u, AND THE CENSUS EARNED ITS COST AGAIN: the first cut ate FOUR LIVE ITEMS** -- the irreversible `13 -> 17` migration note, `WP-08`'s sequencing call, `AC-07.7` (dc's genuine hold), and `AC-02.3`/`WP-13`/`WP-16`'s standing under the scarcity strike -- **all restored above from the sha-verified archive.** Six ids remain deliberately dropped and each has a home: `AC-17.4`/`17.9`/`17.10`/`17.12` were an _unaffected by the redesign_ list, `ST0058` rode the discharged delivered-pair item, `WP-17` lives on ic's board. **One id APPEARED with no pre-fold ancestor and is named at item 8.**

### Needs hv's HANDS, not a ruling

1. **`flip` THEN `burn`, IN THAT ORDER, ONE SITTING.** **`flip` = rebind the default `INTENT_BIN`**, which today is `bin/intent`, the v2 SHELL SCRIPT (`tests/lib/test_helper.bash:21`); `run_v2_suite.bash:55` defaults to `target/debug/intent` and `~/.local/bin/intent` is the v3 RELEASE -- **three binaries, an undeclared population of build targets.** **`burn` = re-run `intent/st/ST0056/parity/tools/burn.sh`**, which runs every `.bats` twice (default binding, then `INTENT_BIN=/usr/bin/false`) and classifies each file by the delta; it hung 3.5h once. **`coverage_map.sh` currently REFUSES to publish and names the re-run as the only legal remedy, refusing a hand-patched TSV by name.** **ORDER IS LOAD-BEARING: a burn before the flip produces a baseline the flip invalidates and the wall time is paid twice.** **NEITHER NEEDS A BUILD** -- the release pair is current at `4be902e1`.
2. **`ST0057 AT-07.5` NEEDS A STOP-THE-DAEMON WINDOW.** Arm B structural passes; arm A behavioural **REFUSES rather than fails** while an intentd is up -- _that is a refusal and not a failure, which is why the row stays green_ -- **so the green rests on a moment nobody can reproduce on demand.** Thread-qualified deliberately: `AT-07.5` is carried by TWO threads and MENTIONED in three; `ST0056`'s is a green `agents_sync_parity.rs` row about heading structure with no daemon in it.
3. **`ST0064` 01.7 SIGNING NEEDS hv's ADC AND NOBODY ELSE'S.** dc owns the devbin pipeline half. A legitimate deferral in the strict sense.

### Needs a RULING

4. **`AC-17.1` CANNOT BE SATISFIED IN 3.0.1 AS WRITTEN.** The row requires _an edit made through EACH realiser reaches an identical store state_. **`Op` is complete at `wire.rs:85-141` -- four reads plus `Shutdown` -- and `Graphql` carries `EmptyMutation`, so the web realiser has no door through which to change the model on EITHER transport.** But **`design.md:501` says `AC-17.1` GETS STRONGER under D56** and cites a browser GraphQL client writing to the daemon, while `wire.rs` scopes the bound to _READS ONLY in 3.0.x_. **Two decisions made independently and inconsistent.** **(A) lift the bound for 3.0.1** -- vc RECOMMENDS; **(B)** move the row to ST0069; **(C)** ship it red. **B and C both retire a criterion the design says is strengthening, to preserve a bound the source marks temporary.** Untouched by the TUI redesign.
5. **`config`'s ORDERING EXPIRY.** `AC-06.1`'s rider requires the conformance test BEFORE the behaviour is designed, so writing it later FAILS the clause rather than satisfying it; `tests/unit/config.bats` does not invoke `intent config` once. **The rider binds `config` ALONE** -- `daemon`'s tests are legal late, which is why cc could start there. **No vc recommendation: this is hv's reading of hv's own rider.**
6. **`AC-00.16`'s AMENDMENT.** vc's, so hv rules it.
7. **ST0065's EMPTY CONTRACT.** hv's directly; the thread has no criteria.
8. **`AC-06.8` / `0181`** -- `llm usage_rules --symlink` is still `keep` and unread. hv's. **PROVENANCE (8u): this id has NO pre-fold ancestor on this board -- it came from cc's TODO (_`AC-06.8` IS NOT MINE AND MOVES ON `0181`_) via vc's consolidation to hv, and is recorded here for the first time.**
9. **`0203`'s AND `0208`'s REMEDIES.** `0203` blocks dc retargeting six `keep` rows and unblocks WP-05. Both need hv's word on the remedy, not just on the priority.
10. **SHOULD `hv/inbox.*` BE EXEMPT FROM PRETTIER?** The gate refuses unformatted markdown, so cc's append-only D5 commit was FORCED to normalise one heading's internal spacing in a delivered entry (`ab4b1733`, three spaces to one, no content changed, disclosed in the message). **hv ruled prior entries untouched; the formatter made one byte-level exception unavoidable.** **vc RECOMMENDS exempting `hv/inbox.*`** -- an append-only surface and a normalising formatter cannot both be absolute.

### Owner needed on work hv has already ruled IN

11. **`WP-14` UN-DEFERRED (D2 = A) AND `WP-15` STILL UNOWNED.** cc's safety claim did not verify: **`AC-08.5` is the daemon-watch criterion, and a sweep of every ST0056 criterion for `partition` returns two, neither of which pins anything.** cc's formulation: _a pin exists to make a deferral safe, so a missing pin is not a citation to repair, it is the deferral losing its justification._ **`AC-10.5` chains behind WP-14. Four live red rows sit on no board.**
12. **THE GUARD BLAST-RADIUS FIX (D4 = D, with C as the cheap immediate).** `declared_kind_check.sh` reads COMMITTED canon, so a half-landed citation makes the tree uncommittable for nodes with no part in it -- **it caught two uninvolved nodes inside four minutes.** **D = let the guard see the WORKING TREE; C = make the refusal name the in-flight landing.** Unowned.
13. **`0207` -- BUILD THE REFUSAL (dc, `S`).** `--note` replaces a note wholesale and silently, on the verb you reach for AT CLOSE. **It is the sharp edge under the gate ruling below.**
14. **PRUNE THE v2 BATS SUITE WITH THE TRUNK (ruled in, unowned, `M`).** Dissolves the `AC-12.1` deadlock.
15. **START RECORDING WHAT EACH GREEN RESTS ON (the gate ruling, C now / B as the decision).** **`intent at green --note` already lands assertion and justification ATOMICALLY, so there is no window in which a row is green and unexplained, and no build is required to start.** The standing decision is that the gate cannot go red from code changing -- `status: green` is a STORED STRING and the `at` family has no verify verb.
16. **`AC-09.5`'s wip/boards HALF NEEDS RE-RULING IN.** It was ruled _3.1 not 3.0.1_ on the ground that its value is highest for this project -- **which is now the reason TO build it -- and it names a release that no longer exists.**

17. **THE v3.0.0 -> v3.0.1 STORE MIGRATION IS IRREVERSIBLE AND OWES A RELEASE NOTE (ruled: note + the existing refusal + `SchemaMismatch` naming its artefact).** **13 -> 17**, endpoints measured by dc: `80d8b2ca` IS `v3.0.0` (`git tag --points-at`) and its `store.rs` reads 13; the tree reads 17. **The intermediate rung count is NOT measured and is NOT claimed.** Local exposure is nil (keg unlinked and pinned) **and that is not the finding** -- it is a shipped-artefact obligation independent of this box. Unowned.
18. **`WP-08` IS 12/12 GREEN AND `wp done` IS AVAILABLE TO cc (`XS`) -- NOT TAKEN, AND THAT IS hv's SEQUENCING CALL.** Closing it marks the daemon DONE while its family carries ZERO conformance coverage -- a green over an uncovered surface. **cc was right to hold.**
19. **`AC-07.7` IS dc's GENUINE HOLD AND IT IS NOT A TREE QUESTION.** It needs a keg built from fixed code, which needs a published tag. Nothing anyone here can unblock.
20. **`AC-02.3`'s FIXTURE DECLINE IS HALF-AFFECTED BY THE SCARCITY STRIKE AND STANDS ANYWAY** -- census-first is right regardless of what time costs. **dc's `bin/` prune also SURVIVES the strike**, on irreversibility plus the CI failure mode rather than on time. **`WP-13` and `WP-16` STAY IN ST0069 on hv's own dated sequencing sentence**, not on scarcity -- stated so the strike is not read as reversing them too.
21. **THE `partition` SWEEP THAT KILLED cc's WP-14 SAFETY CLAIM, NAMED SO IT IS RE-DRIVEABLE:** the only two ST0056 criteria matching `partition` are **`AC-00.12`** (an instrument asserting its own closure) and **`AC-11.7`** (artefact records). Neither pins anything in either direction.

### vc's own, not hv's

22. **THE ESTATE-TREE QUESTION IS UNSOURCED AND vc RE-DERIVES IT BEFORE ANYONE SPENDS.** Born in a fold (`3e9b59e0`), on no other node's board or archive ever, and absent from the sha-verified pre-fold body. **The holder count is struck; the question is retained only because prolix-vc's provenance reads as a real receipt and cross-session messages sit under no guard (`0099`).**
23. **`hv/inbox.vc.md` LIFECYCLE IS vc's NOW** (hv approved the recommendation). 208KB to sweep.
24. **`design.md:88`** still says `rmcp (official SDK): stdio ... now`, which vc's ruling reversed. **Design prose is hv's hand -- a flag, not an edit.**

### RULED AND ASSIGNED 2026-09-02 16:5xZ -- hv: _"Just do it order, and go with your recs."_

**hv DELEGATED THE WHOLE OPEN SET TO vc's RECOMMENDATIONS AND TOLD vc TO SEQUENCE IT. Every ruling below is a vc ruling under hv's pen granted 2026-08-22, NOT an hv ruling; `ratified_in: "vc, 2026-09-02, under hv's pen; this entry"`.** Three of hv's items are NOT ruled here and are named at the foot, because delegation does not manufacture an answer vc does not have.

**ORDER.** ic finishes the section 2 window first (in flight, five things). Everything else runs in parallel by owner; nothing below is sequenced behind anything else except where stated.

| #   | item                                   | ruling                                                                                                                                                                                                                                                                                                                               | owner                                                      |
| --- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------- |
| 4   | `AC-17.1` vs the reads-only bound      | **(A) LIFT THE BOUND for 3.0.1.** `design.md:501` says the row gets STRONGER under D56 and cites a browser GraphQL client writing to the daemon; `wire.rs` scopes reads-only to _3.0.x_, a staging note. **B and C retire a criterion the design says is strengthening, to preserve a bound the source marks temporary.**            | **cc** -- the mutation door is `0206`'s half one flight up |
| 10  | prettier vs `hv/inbox.*`               | **EXEMPT `hv/inbox.*` FROM THE FORMATTER.** An append-only surface and a normalising formatter cannot both be absolute; the gate FORCED cc to edit a delivered entry's bytes (`ab4b1733`).                                                                                                                                           | **dc**                                                     |
| 11  | `WP-14` un-deferred                    | **BUILD IT.** _A pin exists to make a deferral safe, so a missing pin is not a citation to repair -- it is the deferral losing its justification_ (cc). `AC-10.5` chains behind it.                                                                                                                                                  | **cc**                                                     |
| 11b | `WP-15` (skills catalogue triage)      | **BUILD IT.** KEEP / UPDATE / RETIRE every Intent2-era skill -- triage IS vc's role and vc has been routing rather than doing.                                                                                                                                                                                                       | **vc**                                                     |
| 12  | guard blast radius                     | **(D) let the guard see the WORKING TREE, with (C) -- the refusal names the in-flight landing -- as the cheap immediate.** D widens what it SEES rather than narrowing what it CHECKS, so the inherited-case objection does not apply.                                                                                               | **dc**                                                     |
| 13  | `0207` build the refusal               | **BUILD IT** (`S`). It is the sharp edge under item 15.                                                                                                                                                                                                                                                                              | **dc**                                                     |
| 14  | prune the v2 bats suite with the trunk | **PRUNE** (`M`). Port is `XL` and most of it tests a dispatcher that will not exist. Dissolves the `AC-12.1` deadlock.                                                                                                                                                                                                               | **dc**                                                     |
| 15  | recording what a green rests on        | **STANDING PRACTICE FROM NOW: every `intent at green` CARRIES `--note`.** No build; the verb already lands assertion and justification atomically. Binds every node.                                                                                                                                                                 | **all**                                                    |
| 16  | `AC-09.5`'s wip/boards half            | **RE-RULED IN.** It was declined _3.1 not 3.0.1_ on the ground its value is highest for this project -- **which is the reason TO build it -- and it names a release that no longer exists.**                                                                                                                                         | **cc**, with `WP-14`                                       |
| 17  | `13 -> 17` migration                   | **RELEASE NOTE + the existing refusal + `SchemaMismatch` naming its artefact.** dc measured both endpoints.                                                                                                                                                                                                                          | **dc**                                                     |
| 18  | `WP-08` `wp done`                      | **HOLD.** Closing marks the daemon DONE over zero conformance coverage. cc was right.                                                                                                                                                                                                                                                | --                                                         |
| 5   | `config`'s ordering expiry             | **(B) WRITE THE TEST NOW AND RECORD THE ORDERING VIOLATION IN THE ROW.** The rider exists to stop tests being written to MATCH built behaviour; recording the violation keeps that signal. **(A) -- holding `AC-06.1` permanently red on a process fault -- would make a row that CANNOT PASS, which is watch-out 5 on this board.** | **cc** (owns `AC-06.1`)                                    |
| 8   | `AC-06.8` / `0181`                     | **READ IT AND DISPOSE.** `llm usage_rules --symlink` has sat `keep`-and-unread; a disposition nobody has read is not a decision.                                                                                                                                                                                                     | **dc** (owns the `keep` rows)                              |
| 9   | `0203` / `0208` remedies               | **dc PROPOSES THE REMEDY, vc RATIFIES.** They were blocked on hv's word on the REMEDY, not the priority; that is a review vc can do. Unblocks six `keep` rows and `WP-05`.                                                                                                                                                           | **dc -> vc**                                               |

**NOT RULED HERE, AND SAYING SO RATHER THAN INVENTING AN ANSWER:**

- **`AC-00.16`'s AMENDMENT (item 6) -- vc RE-DERIVES IT BEFORE RULING IT.** vc has been carrying _an amendment is owed_ without the amendment's CONTENT in front of it. **That is watch-out 8e -- an open escalation ageing into an assertion -- and its author re-drives it.** vc's, this week.
- **`ST0065`'s EMPTY CONTRACT (item 7).** A thread with no criteria needs somebody to say what it is FOR, and vc cannot invent that. **ROUTED TO ic, who claims it, to PROPOSE the contract; vc ratifies; hv reverses if wrong.** Better than blocking on hv for a definition ic already holds.
- **THE ESTATE-TREE QUESTION.** Still vc's to re-derive; unsourced and born in a fold.

**Re-run the verbs; do not read figures off this board.**

## Standing directives from hv

- **WATCH THE RUST FOR HIGHLANDER, THIN COORDINATOR AND PFIC on every review.** A posture, not a gate.
- **THE MENUBAR ICON IS THE INTENT TURTLE**, state DERIVED at paint time.
- **FULLY SHIP v3. intentd is a priority. Then tree-sitter and full search. Push.**
- **DO NOT REINVENT THE WHEEL** -- port from `../Gtools`, `../Conflab`. **Read the thread's own attachments first.**
- **EVERY PROJECT GETS THE WRAPUP AS ITS OWN TECHNOTE** (2026-09-01). Sequence: pristine -> devbin-vc FIRST -> hv drives the devbin rollout while every other estate chills -> only then do the rust-using estates hear about it.

## Watch-outs

- **8r -- A SUCCESS CHECK THAT PIPES REPORTS THE PIPE'S STATUS, AND vc SHIPPED ONE ON THE DAY vc CATALOGUED THEM.** (vc, 2026-09-01 19:47Z.) The commit retry loop was `if git commit ... | tail -2; then echo COMMITTED`. **A pipeline's exit status is its LAST element's, and `tail` always succeeds** -- so it printed COMMITTED whether or not the commit landed, for hours. **It surfaced only because HEAD had not moved when vc checked by hand; nothing in the loop could ever have said so.** Same shape as cc's stash control and the filter-matched-nothing green, both of which vc named the same afternoon. **The correct form captures the status before anything consumes it: `out=$(cmd 2>&1); rc=$?`.** The general rule is not about `tail`: **anything downstream of the thing being tested becomes the thing being tested.**

- **8p -- A STALE FIGURE MISINFORMS; A STALE TODO STOPS WORK, AND NOBODY RE-READS A TASK THEY HAVE NOT STARTED.** (vc, 2026-09-01 19:17Z, against vc's own board.) TODO item 1 read _`of_n_closes_over_examined.sh` DOES NOT EXIST -- it is a BUILD_. **It existed: 272 lines, `.pending` in ic's history since 2026-08-19, dependency landed, sibling landed, positive control run, mutant predictions lodged BEFORE the run and scored after.** ic's fold said why it stopped: _AT-00.11 is still gated on vc's corrected row text._ **vc was the gate for a fortnight while recording the item as unbuilt labour.** Every other staleness caught today was a figure that had MOVED; this is the opposite and worse, because **a wrong figure gets re-read and a wrong TODO gets skipped.** The corrective is that an untouched TODO earns a re-drive precisely BECAUSE it has been untouched.

- **8q -- A RULING RECORDED IN A FOLD CARRIES A CRITERION'S AUTHORITY AND GETS NONE OF ITS REVIEW.** (vc, 2026-09-01 19:17Z, found by ic reading one back.) vc ruled _A+B are revision-independent and need no second tree_. **True of the ARMS and false of the CRITERION: they need no second tree to RUN, and the criterion needs one to CATCH.** `AC-00.11`'s own founding defect, `EXAMINED 86 of 278`, has N < M and a closing remainder, **so neither mechanical arm fires -- the row says four drive-arms and every run against real history missed it.** Acting on the literal ruling would have shipped an instrument blind to the defect it exists to find. **It sat in a fold for two weeks: no gate reads a fold, no critic lints one, and its author never re-reads it.** A criterion gets `ac lint`, peer review and a gate; **a fold ruling gets a peer happening to read it back.**

- **8q-bis -- AND IT IS THE SAME INFERENCE vc CORRECTED IN ic AN HOUR EARLIER.** ic: _one contract, both transports_, therefore the web face can mutate. vc: _the arms need no second tree_, therefore the method can go. **Both are a true structural statement carried one step past what it supports, and vc caught ic's inside a minute while vc's own ran for a fortnight.** The asymmetry is not skill: **ic's claim was made in a live message to a reader, and vc's was made in a fold nobody was reading.** The lesson is about WHERE a claim is made, not who makes it.

- **8n -- A WRONG MECHANISM PRODUCING THE RIGHT SYMPTOM HAS NO NATURAL CORRECTIVE.** (devbin-vc, 2026-09-01 18:25Z, and it is the sharpest form of the class this estate has circled all day.) They filed 0046 on vc's ROUTING story; dc measured ORDERING. **The symptom was identical under both, so nothing downstream of the symptom could ever have caught the wrong one.** Generalises past the issue: a mechanism is only ever tested by predictions it makes that the symptom does not -- **where the fix goes, and which cases are UNAFFECTED.** vc's story made neither prediction and was therefore unfalsifiable by anything short of reading the code.

- **8o -- A CONTROL THAT MOVES A VARIABLE THE INSTRUMENT IGNORES IS GUARANTEED TO REPRODUCE, AND READS AS PROOF.** (cc, 2026-09-01 18:25Z, against themselves.) cc stashed their two paths, got the SAME 23 failures, and reported the failures as not theirs. **`git stash` REWRITES the file, so the mtime stayed fresh -- and the harness staleness guard keys on MTIME, not content.** The two arms differed in exactly the dimension the guard ignores. **The tell was present and unread: the baseline had one FEWER pass, explained away as the new test, and nobody asked why 23 failures survived removing the only change that could cause them.**

- **8j -- A TRUE FACT FROM AN ADJACENT CONVERSATION, HANDED TO A DOCUMENT THAT MAKES NO SUCH CLAIM, IS SCOPE CREEP WEARING THE CLOTHES OF A CORRECTION.** (vc, 2026-09-01 18:06Z, on dc's refusal.) vc told dc to fix TN001's `--version` claim. **`grep -- '--version' tn001` returns NOTHING; the note never made the claim.** The fact was true, freshly driven, and belonged to devbin's rendering rather than to dc's note. dc: _a technote that starts absorbing every true nearby fact stops being about one test-target rule_ -- **the failure the estate status table already gave that file once, and vc endorsed removing that one.** The tell is that the correction cites no line.

- **8j-bis -- AND THE SAME TURN CARRIED A STALE READ OF THE SAME FILE, FOR THE SECOND TIME TODAY.** vc raised the proxy/adjudicator split as missing; **TN001 has carried it at `:36-37` since v0.9, with item 1 at `:135` RULING it rather than formatting it.** The first instance was twenty minutes after taking the identical lesson from dc pointing the other way. **A claim about a FILE is a claim about a REVISION, and vc checked no revision either time.**

- **8k -- THE PROXY THAT IS WRONG IN THE DIRECTION THAT PRODUCES WORK IS THE ONE NOBODY AUDITS.** (dc, adopted 2026-09-01 18:06Z.) vc had the manifest proxy as _cheap but weaker_. dc's case is sharper: **`grep -c` for a `[[test]]` header returns 0 for `testkit` while cargo reports 1** -- no `autotests` key, one file, auto-discovered. **An estate reading the proxy alone sees zero, concludes it must consolidate, and consolidates something already correct.** A proxy wrong toward MORE WORK generates its own confirmation; one wrong toward less gets caught by the thing that breaks.

- **8l -- ABSENT BINARIES ARE NOT AN OUTAGE UNTIL YOU HAVE ASKED WHETHER A BUILD IS RUNNING.** (cc, adopted 2026-09-01 18:06Z.) **`0196` was delete-happened + replace-DECLINED** (redirected to private by the dirty-tree guard). **A normal rebuild is delete-happened + replace-IN-FLIGHT.** Identical by inspection; **one `pgrep -fl 'cargo build.*release'` separates them.** This estate has escalated on an absence twice; **vc did it first, seven minutes after hv had already rebuilt.**

- **8m -- A GATE'S PROVENANCE LINE IS WRITTEN AFTER ITS LOG IS SEALED, SO IT LANDS IN A PARENT'S LOG AND NEVER ITS OWN. ORDERING, NOT ROUTING -- AND vc's FIRST MECHANISM WAS WRONG.** (dc measured it across hv's two `fullcycle` runs; vc had inferred routing from ONE artefact and devbin-vc had already FILED 0046 on vc's version.) **Nested gates are fine because the parent captures the child's stdout; a TOP-LEVEL gate has no parent, so its provenance is terminal-only -- and the top-level artefact is the one anybody keeps.** The corrected mechanism says where the fix goes and predicts which gates are unaffected; vc's did neither. **Correction sent to devbin-vc, credited to dc.**

**MECHANISMS ONLY. Instances are in `.history/`.**

1. **AN INSTRUMENT ANSWERS A DIFFERENT QUESTION THAN THE ONE ASKED AND ITS OUTPUT LOOKS LIKE AN ANSWER.** **CONVERGENCE IS NOT CORROBORATION WHEN BOTH METHODS SHARE AN INSTRUMENT.** **A PROBE THAT CANNOT EXHIBIT THE FAILURE RETURNS THE NUMBER THAT MEANS SUCCESS.** **A ZERO WITH NO PLANTED POSITIVE IS A SILENCE.** **A RECORDED PROOF IS PROSE UNTIL SOMEONE TYPES IT.**

   **1b. THREE QUESTIONS, AND PASSING EITHER OF THE FIRST TWO STOPS YOU ASKING THE THIRD** (dc). **VACUITY:** can this instrument ever say no? **CORPUS:** is it looking at the right thing? **FORM:** does the SUBJECT have more than one shape? `AT-12.1` could have been pointed at `tests/`, fired correctly, and still missed 35 files -- corpus right, pattern wrong.

   **1c. AND baize-vc's TERMINAL CASE IS NOT A MEMBER OF THE CORPUS CLASS.** A narrowed-corpus instrument still HAS an execution path; put the mutation inside its corpus and it goes red. **A verdict that is a typed string has none.** **ASK EACH GATE WHAT KNOWN-BAD INPUT MAKES IT GO RED, AND TREAT _NONE EXISTS_ AS A SEPARATE AND WORSE ANSWER THAN _FEWER THAN CLAIMED_** -- widen-the-corpus fixes the first and does nothing for the second.

2. **AN ABSENCE NEVER LOOKS LIKE A BUG IN THE QUERY.** Output to a FILE, then count. **NEVER SUPPRESS A COMMIT'S ERROR OUTPUT.**

   **2b. A PROBE AIMED AT A PATH THAT DOES NOT EXIST RETURNS EMPTY, AND EMPTY READS AS ABSENCE.** Two instances in four hours: dc grepped `cmd/clean.d/` -- a directory that does not exist, because `clean` is a builtin -- and reported the warning missing; lamplight-vc did the same shape with `stat -t`. **A DEPTH OR PATH FILTER IS PART OF THE CLAIM**: my own `-maxdepth 4` census silently missed three estates and I reported what the filter could reach.

   **2c. `cargo test <filter>` EXITS 0 WHEN THE FILTER MATCHES NOTHING** (dc). Their first guard proof filtered `--exact` on a bare name, the test lives in a module, it matched nothing, ran zero tests, exited 0 -- and the control passed the same vacuous way. **ANY HARNESS THAT SELECTS BY NAME AND CHECKS ONLY `$?` IS GREEN FROM THE MOMENT THE NAME DRIFTS. Assert the COUNT.**

3. **A CLAIM OUTLIVES ITS BASIS AND NOTHING WATCHES THE JOIN.** **A GREEN ROW WHOSE TEXT OVERCLAIMS IS WORSE THAN A RED.** **`closed` DOES NOT MEAN FIXED.** **EXIT 2 SPANS COULD-NOT-RUN AND PROPERTY-IS-FALSE.**

   **3b. RECORDS THAT CANNOT GO STALE-FLAGGED ARE THE SIBLING OF INSTRUMENTS THAT CANNOT GO RED** (baize-vc). **Not measurement errors -- correct measurements that EXPIRED, in files whose job is to be read at boot.** Mine: hv's board carried three stale claims today, and TN001's own verblock said `v0.1` after five revisions. **Cure -- AND IT IS NECESSARY, NOT SUFFICIENT, WHICH TOOK ALL DAY AND THREE NODES TO ESTABLISH: state the figure with the command that regenerates it. PRINTING THE COMMAND IS NOT RUNNING IT** (intent-dc, 2026-09-01 16:20Z, and it undercuts the remedy every node here had been prescribing to each other since lunchtime). **PROOF: TN001's crate table carried `Verified by asking cargo, not by counting files` DIRECTLY UNDER IT and went stale anyway** -- right about METHOD, stale about RESULT -- **and three revisions read past it, one of them naming in its own subject line the commit that broke it.** A printed command makes a figure AUDITABLE; it does not make it CURRENT, and nothing in the document distinguishes the two. **SO THE STANDING ADVICE CHANGES: for a LIVE figure, prefer the COMMAND ALONE and DELETE THE NUMBER -- a printed number gets read INSTEAD of run, which is the whole mechanism. For a HISTORICAL figure, DATE IT, and it stops being a live claim at all.** That second half is the tense test arriving from the other direction, and the two agree: **the only figures that belong on a page are ones that already happened.** **AND dc's SHARPER FORM, ADOPTED 2026-09-01: A BOARD CARRYING A VALUE IS A CLAIM WITH AN EXPIRY THAT NOTHING ENFORCES -- so record the PROPERTY, which re-measures, not the VALUE, which rots.** _The pair is current iff no build input has changed since the commit it names_, driven by `git rev-list -1 HEAD -- native/rust surface docs/design`. **A derivation the reader must perform is not a cure; a command they can run is.** **3g's proof: I escalated _nobody has rebuilt_ seven minutes after hv rebuilt, ic picked it up eleven minutes later and made it a HOLD LEG, and dc caught it independently before I did.** **AND ic's SHARPENING, ADOPTED: A COUNTER-VALUE SWALLOWED ON FAITH IS THE SAME FAILURE POINTED THE OTHER WAY** -- they re-drove my retraction before striking their own hold leg, which is the only reason the correction is worth more than the error. **A retraction is a claim and carries the same burden as the claim it retracts.**

   **3g. STALE IN THE PESSIMISTIC DIRECTION IS THE ONE NOBODY GOES LOOKING FOR, AND IT IS MINE.** **A board that OVERSTATES gets caught by the first person who relies on it; one that UNDERSTATES never prompts anyone to check.** **Do not inflate the tally; a class that absorbs every adjacent mistake stops discriminating** -- which is the selector-resembling-its-population defect wearing different clothes.

   **3h. A DECLARED RISK IS NOT A LIVE RISK UNTIL YOU FIND THE PATH BY WHICH IT REACHES SOMETHING** (baize-vc, correcting me). I asserted that a declared-but-unowned language arms a critic over vendored code. **In Baize it does not: `/deps/` is gitignored so that Rust can never be staged, and the critic gate is staged-scoped.** Two estates, one asserted hazard, zero live instances -- **and the note was asking both to change a config against a standing ruling to fix an exposure neither had.**

4. **THE SHARED TREE MAKES ORDINARY OPERATIONS MEAN SOMETHING ELSE.** `git commit --only <explicit paths>`, **PATH-scoped not hunk-scoped**. **NEVER REMOVE AN INDEX LOCK; RETRY** -- and a lock whose holder is dead is hv's to clear, not mine to judge an exception for.

   **4b. AN ATTACHMENT IS AUTHORED AND NO SYNC DIRECTION REWRITES IT** -- `intent st attach`, then commit file AND extract together. **KNOWING A RULE DOES NOT FIRE IT; ONLY A GUARD FIRES.**

   **4c. ONE PROPERTY, THREE HOMES, AND NONE REPORTS ITSELF** (dc): canon writes racing at the STORE, a canon commit carrying peers' rows at GIT, a staged instrument racing its roster row at the INDEX. **Contention is routable with a private index; COHERENCE is not.**

   **4d. THE EXTRACT IS A SERIALISATION POINT NOBODY DECLARED** (ic, filed `0210` HIGH). With intentd auto-ingesting, `ST0056.json` names the STORE's union of every dirty-and-ingested attachment, **so one node's dirty file under `parity/tools/` blocks every other node's canon commit** -- and committing the attachment without the extract fails the other way. **Coherent-with-git across ALL nodes, or no commit.** The fixable half: **the guard names the FILE, not the owning peer**, so the victim needs three inferences to reach _wait for a peer_.

   **4e. ONE WRITER PER FILE, NAMED, UNTIL HANDBACK -- AND I BROKE IT ON MY OWN NOTE.** dc announced they were taking TN001; I said I would stay out and then landed two commits, **then told dc not to work from a stale read while I was the one making it stale.** hv called it. **The rule was available the whole time -- it is the one I had been enforcing on canon all morning.**

   **4f. KILL BY PID OR PROCESS GROUP, NEVER BY A PATTERN THAT DESCRIBES WHAT EVERYONE ELSE IS RUNNING** (cc, self-reported). `pkill -f 'cargo test --workspace --no-fail-fast'` is dc's published recipe verbatim, on a box with four sessions.

5. **A CRITERION MUST BE ABLE TO FAIL -- AND ALSO TO PASS.** A universal negative over an open future is a trap, not a bar. **A MEMBER THE PROPERTY CANNOT BE EVALUATED OVER IS EXCLUDED WITH ITS REASON NAMED AND THE DENOMINATOR MOVING VISIBLY.**
6. **MAKE THE BAD STATE UNREPRESENTABLE; WHERE YOU CANNOT, WITNESS THE MECHANISM.** **WIREDNESS MUST BE DERIVED OR DRIVEN, NEVER A HAND-MAINTAINED FLAG.**
7. **THE FAILURE PATH IS THE ONE THAT MUST STILL WORK AND A GREEN RUN NEVER EXERCISES IT.** **SCREEN AN INSTRUMENT FOR SIDE EFFECTS BEFORE DRIVING IT** -- `intentd --help` starts a real daemon.
8. **vc's OWN.**

   **8a. SHELL QUOTING EATS CONTENT AND THE COMMAND STILL SUCCEEDS.** zsh does not word-split; an unmatched glob aborts the call; an apostrophe in a single-quoted program runs nothing; **`$?` after a pipe reads the last stage** -- I piped `git commit` through `grep | head`, read `head`'s status as success, and left a lock that blocked two peers. **THE BASH TOOL'S CWD PERSISTS BETWEEN CALLS** and a relative path then resolves somewhere else silently. **`bash -n` answers _is this valid bash_, NEVER _is this the file I meant_.** **Assert on the input AND grep the output** -- nothing checks an artefact for unsubstituted placeholders. **Three delimiter shapes for authored prose in shell data blocks: quoted HEREDOC safe; SINGLE-quoted vulnerable to an apostrophe; plain DOUBLE-quoted is the filed hazard (`0205`).**

   **8b. I RULE AGAINST STATES THAT HAVE ALREADY MOVED. RE-MEASURE BEFORE RULING; when a premise dies, SAY SO AND REVERSE IT.**

   **8c. I MEASURE SOMETHING TRUE AND RULE ON SOMETHING WIDER.** Five instances on 2026-08-31, every one caught by a peer DRIVING rather than by me re-reading; two more today (`AC-12.1`'s corpus, the WP-14 scope item). **The measurement was TRUE every time, which is what made each ruling feel driven.** **Corrective: state what the measurement does NOT establish, BEFORE ruling on it.** **AND CO-OCCURRENCE IS NOT CAUSATION:** I told ic their commit was spinning on my dead lock; it had exited, on the gate. Two true facts joined by a claim I never measured.

   **8c-bis. `--note` REPLACES A ROW'S NOTE WHOLESALE AND SILENTLY (`0207`), AND I DESTROYED 7803 BYTES OF `AT-00.12`'s PROVENANCE THAT WAY.** Absent KEEPS, present OVERWRITES -- on `at red`/`green`/`na`. **The verb that records WHY A ROW MOVED deletes WHY IT EXISTS, at the moment you are most likely to type it.** **LIVE EXPOSURE, LARGEST FIRST: `AT-11.5` 16977 bytes, `AT-04.6` 13007, `AT-03.15` 8831.** **Until fixed: read the note first and APPEND -- and READ THE WHOLE BLOCK, because a multi-line note renders across many lines and `sed -n '<line>p'` returns only the first.** `ac satisfy` is safe BY CONSTRUCTION, not by care.

   **8f. I STAMPED A PEN NOTE `driven` FOR A CLAIM I HAD NOT DRIVEN, AND THE MARKER IS THE WHOLE VALUE OF THAT BOARD.** hv's board carries `(pen note, vc, <stamp>, driven.)` as its provenance contract. The 13:38Z note's OTHER clauses were driven; **the breakage clause rode along on their marker** and asserted a release directory I had not looked at since much earlier. cc flagged the ordering -- entry stamped 13:38Z, binaries stamped 13:27Z, _both cannot be right as I read them_ -- and the resolution is not a clock error, it is that **`driven` was false for that clause.** **A PROVENANCE MARKER IS PER-CLAIM, NOT PER-NOTE. If one clause in a note is recalled, the note is not driven** -- and on a board whose only defence is attribution, an unearned marker is worse than an unmarked claim, because it spends credibility the measurement never bought.

   **8g. A CLAIM ABOUT A FILE IS A CLAIM ABOUT A REVISION, AND IT GOES STALE EXACTLY LIKE A CLAIM ABOUT A BINARY** (dc, 2026-09-01, catching me). I read TN001 once, held the reading, and raised it as a live finding after the document had moved -- **twenty minutes after taking the identical lesson from dc pointing the other way, between the two nodes who had just agreed the rule.** **THE CURE IS THE ONE I HAD JUST ADOPTED FOR THE BINARY, AND I DID NOT GENERALISE IT: name the sha you read, or re-grep the file rather than the memory.** `git log -1 --format=%H -- <path>` is the one-command form.

   **8g-bis. THE CORRECTIVE IS NOT MORE CARE AT THE MOMENT OF WRITING.** Three instances in one hour, all mine, all a value carried across a boundary where the world moved. **EVERY FIGURE GETS ITS REGENERATING COMMAND WRITTEN BESIDE IT AT THE MOMENT IT IS WRITTEN -- AND SEE 3b, BECAUSE THAT IS NECESSARY AND NOT SUFFICIENT.**

   **8h. A DOCUMENT APPLIES ITS OWN RULE TO THE CLAIMS ABOUT OTHER PEOPLE AND NOT TO THE CLAIMS ABOUT ITSELF.** TN001's opening line is _every figure below carries the command that regenerates it_. Its v0.6 revision fixed the ESTATE table -- other estates' state -- for exactly that, **and left its own consolidation table reading `total 5` when `33ac4348` had made it 6 four hours earlier, under a caption boasting that this one was verified by asking cargo.** Three revisions passed over it, one of them NAMING the commit that broke it. **THE FIGURES ABOUT YOUR OWN ESTATE ARE THE ONES NOBODY AUDITS, BECAUSE EVERYONE ASSUMES THE AUTHOR WOULD KNOW.** Sibling of 3g: the direction nobody goes looking in.

   **8i. I READ A `head -10` TRUNCATION AS A COMPLETE POPULATION AND ALMOST PUT A FALSE GAP IN OUR OWN TEST COVERAGE ON hv's DESK.** Grepping `orphan` across the crates, piped through `head -10`, returned only `intentd` hits -- **because intentd's ten hits FILLED THE LIMIT** -- and I read that as _the orphan guard exists in one crate of three_, which against TN001's _the only thing standing between a consolidated suite and a silently shrinking one_ is a live exposure in the estate that authored the rule. **Driven properly: `no_orphan_suite_member.rs` is present in intentsvcs, intent-cli AND intentd.** Only testkit lacks it and testkit auto-discovers one file, so it has nothing to orphan. **A DISPLAY LIMIT IS A POPULATION FILTER WEARING NO CLOTHES AT ALL** -- `-maxdepth` at least appears in the command; `head` reads as _formatting_. Sibling of 2b, and the fourth time today verification killed a claim of mine before it shipped.

   **8h-ter. I TYPED `16:20Z` INTO A PEN NOTE IN THE TURN WHOSE CLOCK READ `16:17Z`, WHILE WRITING THAT PEN NOTE ABOUT FIGURES THAT DRIFT.** Caught before the commit, corrected to the read value. **THE GUARD WOULD PROBABLY NOT HAVE CAUGHT IT**: check A tolerates 120s of jitter and a three-minute lead against a commit landing a minute later is inside the noise -- so this is the fabricated-stamp case the guard's own documentation says stays open, and the only thing that closed it was re-reading my own output. **A CLOCK VALUE IS COPIED FROM THIS TURN'S TOOL OUTPUT OR IT IS NOT WRITTEN.** Nothing about the number felt invented.

   **8h-bis. AND THE CONVERSE INSTRUMENT ERROR, FROM conflab-vc VIA devbin-vc, ADOPTED: FOR A WORKSPACE-SCOPED KEY, CHECKING THE MEMBER MANIFEST REPORTS ABSENT-AND-WRONG INDISTINGUISHABLY FROM ABSENT-AND-RIGHT.** devbin-vc reported Conflab's `[profile.dev]` absent, having grepped the member manifest and the project root; **cargo honours it only in the WORKSPACE ROOT, where Conflab has had it since `f9875541`.** **This is a CORRECT instrument aimed at the WRONG ARTEFACT, and unlike a proxy adjudicating it leaves NO TELL IN THE OUTPUT.** 2b was about a probe aimed at a path that does not exist; this one is aimed at a path that does, and reads it faithfully.

   **8d. A PHRASE OF MINE BECOMES A SPEC THE MOMENT SOMEONE BUILDS AGAINST IT.** **An artefact named in a ruling must be verified to exist at the moment of writing.**

   **8e. AN OPEN ESCALATION AGES INTO AN ASSERTION** (generalising cc's third instance). It was true when written, nobody re-reads it because it is _already asked_, and it accrues authority it never earned -- a week of hv's queue on a question `sync.rs:856` had already answered. **BEFORE ANYONE BUDGETS AGAINST AN ESCALATION, ITS AUTHOR RE-DRIVES IT.** Mine on hv's board date to 2026-08-25 and I put them there.

   **8s. THE SAME WRONG NUMBER REACHED ONE BOARD BY TWO UNRELATED ROUTES AND WAS INDISTINGUISHABLE ON THE PAGE** (dc, 2026-09-02, W47, sharpening my own class rather than softening it). dc's board carried `16` twice: once relayed from my withdrawn ancestry inference, and once as ordinary decay of their own -- **and that second one was TRUE when written**, driven here: `git log -S 'SCHEMA_VERSION: i32 = 16' -- native/rust/crates/intentsvcs/src/store.rs` returns `d4526c1b` (introduced) and `544a83d3` (replaced by 17). **A BOARD CANNOT TELL YOU WHERE A NUMBER CAME FROM, SO IT CANNOT TELL YOU WHICH ONES TO RE-DRIVE** -- and a recorded figure looks identical whether or not its author still believes it. **The only thing that carried my retraction was me going back to say so, which has no structural fix.** Discriminator: record HOW a number was got, or drive it yourself. **AND THE PRECISION dc PUT ON MY CAVEAT IS ACCEPTED: `git log -S` cannot count migration RUNGS (a substring does not change when a value does), but it answers _did this exact literal ever exist_ cleanly, because the occurrence count moves at BOTH ends.** Keep those separate or the caveat retires a tool that answers the adjacent question.

   **8t. MY OWN WRITTEN RECIPE WOULD HAVE CAUSED THE EXACT HARM MY OWN WATCH-OUT EXISTS TO PREVENT, AND ic CAUGHT IT BY RE-MEASURING RATHER THAN EXECUTING** (2026-09-02 08:24Z). The `AT-00.11` landing recipe said _pass verbatim to `--note`_, **written assuming an empty note**, on a row whose note is **4028 chars** of driving guidance and minting provenance. Executed, it repeats 8c-bis -- the `AT-00.12` destruction, 7803 bytes down to 683, which I did myself. **A WATCH-OUT PROTECTS THE HAND THAT REMEMBERS IT AT THE KEYBOARD; IT DOES NOT PROTECT GUIDANCE I WRITE FOR SOMEBODY ELSE TO EXECUTE LATER.** The recipe was reviewed for correctness of SEQUENCE and never checked against my own board. **DRIVEN WHILE RULING IT, AND IT KILLED THE THIRD OPTION OUTRIGHT: `acceptance.md`'s `AT-00.11` row is 4142 chars, the store note is 4028, and the note appears in the row VERBATIM at offset 114** -- so the acceptance doc is the RENDERING of the note, not a second home, and _move the guidance elsewhere_ has nowhere to move to. Ruled APPEND, with two riders: **source the existing note from the STORE not the extract** (prettier is a second writer; D01 says store is SSOT), and **assert containment AFTER the write, not before** -- a length check alone passes a note that grew while the guidance was mangled. **THE GENERAL FORM: A PROCEDURE I AUTHOR IS AN INSTRUMENT, AND AN INSTRUMENT GETS A POSITIVE CONTROL.** Mine asserted a precondition -- an empty note -- that it never checked and that was false.

   **8u. MY FOLD CONTROL CAN ONLY EVER DETECT A DELETION, AND dc PROVED IT BY RUNNING MY OWN CLASS AGAINST THEIR FOLD** (dc's W48, 2026-09-02 08:27Z, adopted). My id-diff catches what a fold ATE and is structurally blind to what a fold ADDED -- which is precisely how `B5` survived three folds. dc ran the reverse diff (`grep -vxF -f <prefold> <postfold>`) and found **15 post-fold lines with no pre-fold ancestor: fourteen bookkeeping or rewording, and the fifteenth their `AT-07.5` item, which had NO board ancestor because its source was a cross-session message from me.** **SO THEIR ADDED-LINE WAS REAL AND MINE WAS INVENTED, AND THE CONTROL CANNOT TELL THEM APART** -- messages sit under no guard (`0099`), so a genuine cross-session origin leaves exactly the same trace as no origin at all: none. **CURE, TWO-SIDED AND ADOPTED FOR EVERY FOLD FROM HERE: diff BOTH directions, and any post-fold line with no pre-fold ancestor NAMES WHERE IT CAME FROM.** An id census answers _did I lose anything_; it was never able to answer _where did this come from_, and I had been reading it as if it were both. **AND dc's RIDER IS TAKEN, BECAUSE IT IS WHAT KEEPS THE CONTROL ALIVE: the fourteen EXONERATING lines matter as much as the fifteenth.** Their reverse diff said _inherited, not manufactured_ about the migration line. **A control that only ever produces findings is abandoned inside a week**; one that can clear a fold is one people keep running.

   **8v. A CANON ATTACHMENT THAT IS ALSO A FORMATTED MARKDOWN FILE HAS TWO WRITERS AND THEY RUN AT DIFFERENT TIMES** (ic, 2026-09-02, landing `a8981480`). ic checked the extract before committing -- my habit, on its first use -- and it caught something neither of us named: **the pre-commit gate REFUSED on unformatted markdown, and prettier then rewrote `tui-design.md` AFTER the store had already ingested ic's version.** The formatter-is-a-second-writer divergence, arriving in the window between sync and commit. **THE ORDER THAT SURVIVES: prettier FIRST, then let the store settle, then verify, then commit.** ic verified three ways -- committed extract, store, and disk file all `772ed6b7`. **AND THE SECOND HALF IS SHARPER: prettier ALIGNS MARKDOWN TABLES, so it rewrote the very table `parse_ratified` reads.** It survived only because the parser trims each cell. **_It happened to still parse_ is a fact to measure, not to assume** -- the formatter had its hands on the RATIFIED ARTEFACT between the write and the commit, and nothing in the pipeline would have said so.

   **8w. I RE-RAN THE AUTHOR'S CENSUS AND CALLED IT AN INDEPENDENT CHECK, AND REPORTED A ZERO TO hv THAT WAS 13** (2026-09-02, dc's W50, my half). After dc's sweep landed at `7f1bfb53` I drove `grep -rn 'printf.*| *grep -q'` and told hv zero live sites remained. **That is dc's pattern.** The wider `| *grep -q` against that same commit returns 13. **My own rule, committed while checking someone else's work: CONVERGENCE IS NOT CORROBORATION WHEN BOTH METHODS SHARE AN INSTRUMENT.** dc offered to carry it jointly on the true ground that the pattern was theirs and I could not have re-run what did not exist; **the half that stays mine is that a check whose whole value is independence must not borrow its instrument from the thing it checks.** **AND dc's ROOT CAUSE IS THE KEEPER, TAKEN VERBATIM: a defect named after the SPELLING OF ONE INSTANCE gets a fix shaped like that spelling, and every census downstream inherits the blindness rather than reporting it.** `36` was never a measurement -- it was the count of sites matching a phrase four artefacts had been repeating since August. **Nothing downstream of the naming could have caught it: every later step takes the name as its population, so MORE DILIGENCE AT ANY LATER STEP PRODUCES A BETTER-VERIFIED WRONG NUMBER.** **THE CURE, ADOPTED: WHEN YOU NAME A DEFECT, STATE THE MECHANISM AND NEVER THE INSTANCE.** _An early-exiting reader downstream of a writer under pipefail_, not _printf piped into grep -q_. **The first cannot be sharpened into a smaller population by anybody repeating it; the second already had been, before it reached hv.**

   **8x. WHEN AN ACTION'S REALISER ALSO PERFORMS BOOKKEEPING, _SOMETHING CHANGED_ IS SATISFIED BY THE BOOKKEEPING, AND THE CHECK SILENTLY BECOMES A TEST THAT THE TIDY-UP HAPPENED** (ic, 2026-09-02, `95abeab5`; their formulation, adopted). ic's first `AC-17.13` test was **GREEN AND WRONG**: running any command clears the buffer and returns the mode to OMNI, so that CLOSING counted as _the command did something_, **and the test would have passed for a command whose act was a complete no-op -- the exact defect the row exists to catch, sitting inside the test written to catch it.** **IT SURFACED ONLY BECAUSE ic DROVE THE CONTROL** -- un-arming the view stack makes `/back` a true no-op at the root and the test stayed green. **Otherwise it would have reached me as evidence.** Cure: a baseline that ALREADY CONTAINS THE BOOKKEEPING -- compare against an app that closed the palette WITHOUT running anything (Esc, not Enter), isolating the ACT from the CLOSING. **WIDENED 2026-09-02 15:21Z ON ic's OWN CORRECTION, BECAUSE THE FIRST WORDING WAS TOO NARROW: it said the ACT's realiser, and the second instance's bookkeeping was the FRAMEWORK's** -- applied by the dispatch tail for every trigger uniformly, nowhere near the code under test. **THE GENERAL FORM, ic's WORDS: ANY STATE THE HARNESS ITSELF MOVES IS NOT EVIDENCE THAT THE THING UNDER TEST MOVED IT. The framework case is the one that is invisible from the test.** **CONFIRMED BY PLANT, NOT BY READ (`de5401c3`).** vc raised it as reasoned-and-not-driven against `every_trigger_the_machine_answers_is_acted_on_by_the_realiser`; ic planted `Edge::new(Omni, "Ghost", Menu)` bound to `F(1)` with NO realiser arm anywhere -- **the invariant PASSED it.** `on_key`'s tail applies `self.mode = next` itself, so the transition alone satisfies `app != before`. **THE ONLY REASON THE INVARIANT EVER WORKED IS THAT `MENU Hotkey -> MENU` IS A SELF-LOOP: the one instance it was built from happened to be the one shape it could catch.** A test fitted to its founding example, sample of one. Fix is the same cure one layer up -- a baseline carrying the POST-state's mode, so an effect BEYOND the transition is required. **AND THE MOVE IS THE KEEPER, WITH ic's REASON FOR WHY IT IS RARE: a class named at 15:17 has no natural reason to be checked against a test written at 15:12, nothing in either file connects them, and the author had just finished congratulating themselves on the control that found the first -- _which is precisely the state in which you stop checking_. The class is cheap to name and expensive to apply.** **SIBLING OF _a check whose two sides are the same object_, with a shorter fuse: here the two sides were the act and the tidy-up that ALWAYS follows it.** **AND THE STANDING MOVE THIS TEACHES: APPLY A NEWLY-FOUND CLASS TO THE ARTEFACT WRITTEN BEFORE IT** -- `every_trigger_the_machine_answers_is_acted_on_by_the_realiser` asserts `step != Continue || app != before` an hour earlier, which is the same before/after shape; whether a dead trigger declaring a MODE CHANGE would pass it on the transition alone is a question put to ic, reasoned and NOT driven here.

   **8y. KNOW WHICH SIDE OF A SEAM A DEFECT CLASS LIVES ON, BECAUSE THE SEAM THAT MAKES A THING PROVABLE ALSO MAKES ONE CLASS UNREACHABLE** (ic, 2026-09-02, `94cd1047`). The whole `tui` tree is built so the screen is provable WITHOUT a terminal, and that pure-data seam is exactly what makes `AC-17.11` assertable. **hv found a page returning UNPAINTED from an editor handoff: ratatui was diffing against the buffer it painted BEFORE the child ran, the child had blanked the screen, every cell _matched_, and almost nothing was emitted.** **THAT DEFECT EXISTS ONLY INSIDE A TERMINAL, so no test in the module could ever have seen it.** **NOT AN ARGUMENT AGAINST THE SEAM -- an argument for naming, per surface, which classes the seam puts out of reach**, so that _the module is fully covered_ is never read as _the surface is_. Three of hv's finds today were of this kind, all from driving the binary. **AND WRITING THE TEST FOUND A SECOND BUG AGAIN (third time today): `C-w` on `/quit` had no whitespace to stop at, walked to zero, took the SIGIL with it and silently closed the palette.** Cure: **the sigil is a PROMPT, not content, and bulk acts do not cross a boundary the operator cannot see** -- while Backspace still crosses it deliberately, because that is the declared exit and it is one visible keystroke.

   **8z. FOUR TIMES IN ONE DAY THE SURFACE DISAGREED WITH A PLAN I AUTHORED OR ENDORSED, AND EVERY ONE WAS CHEAP TO CHECK AND INVISIBLE FROM THE PLAN** (cc's count, cc's conclusion, 2026-09-02). **(1)** the canon-JSON parse-and-rewrite -- the extract is a GENERATED projection and the store is SSOT; **(2)** the 0017 grammar at `legacy.rs:1965` refusing all three tiers of a key scheme I had just ruled; **(3)** `carries_id`'s doc comment holding a reasoned argument AGAINST the direction I recommended to hv; **(4)** _live, not hypothetical_ about the `file: None` gap -- **driven independently here and the population is ZERO**: 391 AT rows, 54 without a `file`, and every one exempt by design (35 non-test/n-a, 1 non-test/red, 18 test/to-write, which `:678` exempts BY NAME because a missing file is the CORRECT state for an unwritten test). **The `legacy` path we BOTH cited as making it live has 0 rows in the entire canon.** **cc's CONCLUSION, ADOPTED AS A STANDING RULE: READING THE MECHANISM BEFORE SIZING IS LOAD-BEARING HERE, AND WE HAVE BEEN DISCOVERING THAT REPEATEDLY RATHER THAN HOLDING IT.** Four is not four coincidences. **AND THE ISSUE-WRITING COROLLARY IS cc's TOO: an issue that overstates its own urgency is the same defect as an escalation that ages into an assertion -- it just starts out that way instead of getting there.** File it LATENT with the population measured at zero, and leave REACHABILITY open and NAMED rather than asserted in either direction. **AND cc's REFRAMING IS ACCEPTED AS THE HALF WORTH KEEPING, OVER MY TALLY: _a plan is exactly where the mechanism is not_ is a fact about PLANS, not about the node holding the pen.** A plan is a compression, **and what it compresses away is precisely the detail that decides whether it can be executed** -- so the node writing it CANNOT see what the compression dropped, and only the node opening the file can. **A structural property of the two roles, not a difference in care. The check ran four times today and caught four things: that is the division of labour working.** The count stays mine; the lesson is not a scoreline.

   **8aa. `issues add` IS A NARROW CANON WRITE, NOT A WIDE ONE, AND BOTH OF US HAVE BEEN SEQUENCING AROUND IT AS IF IT WERE WIDE** (cc, 2026-09-02). **Driven: `99d3fbdf` touched exactly ONE path, `intent/.canon/issues/0213.json`. No ST extract regenerated, no thread canon moved.** **My sequencing has treated the verb's canon side effect as a reason to serialise through the window, and on this evidence that is over-caution with a real cost** -- it held a node's filing behind a lock it did not need. **A verb's blast radius is a property to DRIVE, not a rule to remember.**

   **8ab. A DIRTY `native/rust` REFUSES A SHARED RELEASE BUILD, AND THAT -- NOT STALENESS -- IS WHAT STANDS IN THE WAY OF A REBUILD.** (cc flagged it; driven here: 11 paths, `render.rs`, five `tui/` files, `bootstrap.rs`, `lib.rs`, `nav.rs`, a nav test, plus an untracked `settings.rs` -- ic building `/settings`.) **THE FLIP AND THE BURN ARE UNAFFECTED**: `burn.sh` drives `bats` against `INTENT_BIN`, the flip points that at the v3 release binary, and that binary is CURRENT at `4be902e1` -- neither needs a build. **What the dirt forbids is taking a rebuild WHILE A NODE IS LIVE IN THE TREE**: the resulting pair would name a commit while describing uncommitted work, which is the `THIS VERDICT DESCRIBES NO COMMIT` class arriving in an artefact that outlives the session.

   **8ac. A SECOND HEREDOC IN A RETRY LOOP SILENTLY ATE THE COMMIT MESSAGE, AND THE COMMIT SUCCEEDED** (vc, 2026-09-02 17:01Z, `ada61d19`). The loop was `git commit -F /dev/stdin <<'MSGEOF' ... MSGEOF` followed by a stray `<<'X'` on the same compound command; **the second heredoc won stdin, so the message became the two junk lines `X` and `git log --oneline -1`.** The board content is CORRECT and the message is meaningless. **THE SHAPE IS THE FAMILY THIS BOARD ALREADY CARRIES: THE OPERATION REPORTED SUCCESS. `rc=0`, the loop printed `committed`, and nothing anywhere said the message was junk** -- exactly the `tail` pipeline that reported COMMITTED whether or not the commit landed. **A commit message has no validator, so it is the one commit artefact where garbage is indistinguishable from intent to every check we run.** **CORRECTED FORWARD, NOT AMENDED**, per hv's own standing ruling on `af91be8b` -- the tree is shared and an amend rewrites history peers may already reference. **`ada61d19` carries the 2026-09-02 rulings-and-assignments table; this commit is its real message.** **CURE, AND IT IS THE ONE ALREADY ON THIS BOARD FROM dc's APOSTROPHE TRAP: ANYTHING NON-TRIVIAL GOES TO A FILE, NEVER INLINE.** A commit message in a retry loop is non-trivial by construction.

   **8ad. THE CANON WINDOW IS SCOPED BY THE EXTRACT'S UNION, NOT BY THE VERB'S NAME** (ruled 2026-09-02 on dc's disclosure, driven). dc took a one-path `intent st attach ST0056` outside the window because `canon_commit_check` blocked a commit vc had assigned, disclosed it rather than mentioning it, and **explicitly declined to extend vc's `issues add` concession to a verb vc had not measured.** Correct: **8aa was a MEASUREMENT of one verb, not a licence.** **MEASURED HERE: the `st attach` extract diff at `eff993b2` is SIX changed lines -- one attachment's `text`, `bytes` and `sha256` -- confined to one thread.** Narrow. **SO THE RULE IS NOT ABOUT THE VERB AT ALL: `st attach` DOES NOT NEED THE WINDOW WHEN THE EXTRACT DIFF IS CONFINED TO THE AUTHOR'S OWN CHANGE. `git status` the extract FIRST; if it carries a peer's uncommitted attachment, THAT is when the window applies, because the extract is a UNION that cannot be split (0210).** The hazard was never the write's size -- it is that regenerating an extract sweeps up whatever else is sitting in it, which is exactly what bit dc and ic this morning. **A window scoped by verb name is a window scoped by a NAME, which is the class this board spent the day on.**

## Decisions

**Standing rulings. Every entry was EXECUTED before it was archived; an UNEXECUTED ruling never leaves this board. Full reasoning for anything dated 2026-08-31 or earlier is in `.history/`.**

- **A REFUSAL ADDED TO A SURFACE WITH NO INVERSE IS A ONE-WAY DOOR** (2026-09-01, on dc's drive). **`kind` is settable at MINT on both `ac` and `at` and changeable NOWHERE** -- `at edit` has no `--kind`, `ac edit` says _leaving its kind alone_ in its own help, `at new` refuses a taken id, and the `at` family has **no removal path**. **So `0146`'s fix (2) IS A REGRESSION IF IT LANDS ALONE**: on a mis-kinded row it closes the LAST door and freezes it permanently. **This is the INVERSE of _one field short_ and it is worse** -- that class under-reaches; this over-reaches into the escape hatch, wearing the clothes of hardening. **DETECTION: before adding a refusal, ask what verb undoes the state it will now trap. If none, you are not hardening the surface, you are welding it.** **RULED: fix (2) lands WITH `AC-04.6` or not at all; fix (1) is safe alone.**
- **`AT-07.7` STAYS RED AND INCONSISTENT; THE `na` EXIT IS REFUSED.** It would record _n/a -- nothing ran_ about a row whose instrument DEMONSTRABLY RUNS two-sided, **trading a TRUE inconsistency for a FALSE statement** -- and `na` reads as resolved. **The `doctor` finding IS the evidence for `AC-04.6`**, so the na exit makes the finding vanish while the gap stays open.
- **`AC-12.1` IS RED AND THE CRITERION IS NOT REWORDED** (2026-09-01). It says _nothing in the repo EXECUTES or EMITS a `bin/` intent script path_; **`bin/intent` stands with 26 v2 scripts, and the test estate is the largest executor** -- `EMITS` 114, `EXECUTES` 85 by two routes. **AT-12.1's evidence named two plugin directories: every word true, corpus narrower than the criterion it scored** -- and I took that row off dc to avoid a self-scored one, then scored it against the wrong population. **Rewording to what the current state satisfies is barred by `AC-06.3`'s own rule.**
- **AN INSTRUMENT STATES ITS POPULATION BESIDE ITS VERDICT -- ONE SHAPE, THREE PARTS, IN THE OUTPUT.** **POPULATION** (the set EXAMINED, derived, with the derivation named; where it cannot be derived, `RECORDED` sits AT the number with what would derive it). **FORMS** (per-form counts where the subject has more than one shape, never a single total). **REACH** (`COVERS` / `DOES NOT` / `UNOWNED`). Built on the existing convention -- 25 of 69 instruments already emit a REACH block. **THE CORPUS IS THE CLAIM; THE VERDICT IS NOT.**
- **THE `0206` CAS: (a) A `revision` COLUMN, REFUSE-AND-NAME, INSIDE `commit_mutation`'s TRANSACTION.** Not struct comparison -- **(b) makes the fix a member of the class it fixes**, because a struct equality is a HAND-MAINTAINED POPULATION that fails OPEN on every field added after it. **A counter enumerates nothing and cannot be too narrow.** Not retry: **the defect is the SILENCE, and a retry that succeeds quietly reproduces it through a different mechanism** -- a fix must not be observationally identical to the defect. A compare before the transaction narrows the window and does not close it; **compare-then-hope does not ship.**
- **0206's FRAME IS TOO NARROW AND I FOUND IT IN MY OWN HANDS.** `ac new` returned `rc=0`, `ok: created`, and the row was in NEITHER store NOR canon; the next verb in the same shell could not see it. **ONE node, ONE shell, sequential verbs, no peer -- with intentd running.** Retried in isolation, it persisted. **The criterion, the harness and hv's risk framing all describe two concurrent nodes; nobody has measured one process writing quickly with the daemon up, which is the configuration we are in all day.**
- **COMPARE AGAINST SOMETHING INDEPENDENT OF THE THING UNDER TEST.** The disk for a write; the POPULATION for a diff. **A diff of one artefact across a verb cannot tell a REGRESSION from a MIGRATION.** The general form of every 8c error.
- **THE ESTATE DOCUMENTS ITS MECHANISMS CORRECTLY AND APPLIES THE FIX ONE FIELD SHORT.** **Detection: ask which CALL SITES the remedy reaches.**
- **A RECLASSIFICATION IS A CLAIM ABOUT WHY; A CLASS CHANGE WITHOUT ITS REASON IS A DELETION WEARING A NEW LABEL.** `deviate` RETARGETS never deletes; `retire` CARRIES ITS BASIS.
- **UNWIRED IS NOT RETIREMENT.** `125f601d` deleted the v2 PLUGIN SCRIPTS, not the commands; `prime`/`subagents` return _a known command that is not implemented_. `surface/dispatch-table.json` disposes every `claude` verb `keep` and the register shares that vocabulary VERBATIM, so a register `retire` against a table `keep` is two artefacts contradicting each other. **Nothing cross-checks them -- `0204`.**
- **`D10` RATIFIES THE PLUGIN-SCRIPT PRUNE. TRACED, NOT PICKED** -- a shared operative clause word for word, plus a commit naming the AC. **Resemblance is not evidence.** Minting a NEW D-number is a design act and hv's; ruling that an existing one covers a case is adjudication and mine.
- **v2 MESSAGE STRINGS ARE OUTSIDE THE NARROWED PARITY CONTRACT.** `parity.md` names stderr VOICE, not wording, and cites `0023` -- the ruling that RETIRED the capitalised voice -- so it cannot also bind the pre-change literals.
- **ST0064 PROJECT ROOT: (a) -- the app stores a configured root and sets the child's CWD.** `D07` RATIFIES A REGISTRY and it is unbuilt, so the ground is SEQUENCING not novelty. **CONDITIONS: validate and refuse LOUDLY; mark the store IN THE CODE as INTERIM.**
- **ANY CANON COMMIT HERE IS SILENTLY A MULTI-NODE COMMIT.** Canon regenerates wholesale and cannot be split. **Whether it is DECLARED depends on whether the committer looks.**
- **THE MCP RULESET.** MCP tools call the FACADE, never the CLI dispatch arm. **The test for exposure is NEED, not provenance.** `severity` exposed (enumerated domain); `title` withheld (authored text).
- **`AC-06.3` IS REWORDED, NOT WITHDRAWN, AND THE NEW FORM IS HARDER.** **Every KNOWN deviation is recorded -- a `keep`/`as-observed` row found to differ is a recorded deviation or a filed defect, never silence.** **FILING IS RECORDING; a deferred filing is silence wearing a schedule.**
- **AN ISSUE'S AUTHOR DISPOSITIONS IT** -- and that is why ic wrote `0210` rather than me. **A title's job is not to state the final cause, it is to NOT MISDIRECT.**
- **A CRITERION THAT COULD FORCE SCOPE IS WRITTEN AS AGREEMENT, NOT COVERAGE.** ST0057's four new rows bind that a decision is TAKEN and BUILT without picking it.
- **A DELETE HAS THREE POPULATIONS AND EVERYONE ASKS ONLY THE FIRST:** what EXECUTES this; what CITES this as evidence -- **population is the STORE, not the tree**; what CHECKS this.
- **AN IDENTIFIER IS ONLY UNIQUE WITHIN ITS SCOPE, AND EVERY BOARD WRITES IT BARE.**
- **A SECOND HOME IS NEVER ACCEPTABLE AT A TAG.** The escape is not _accept two homes_, it is _need less machinery_.
- **A CITATION'S AUTHORITY COMES FROM ITS MEMBERSHIP RULE, NEVER FROM ITS NAME.** **DERIVED CENSUSES MULTIPLY FREELY; AUTHORITATIVE COPIES DO NOT.**
- **A TEST GOING RED BECAUSE A FIX LANDED IS THE NOTIFICATION WORKING.**
- **THE DAEMON'S PUBLISHED PORT SERVES BOTH PROTOCOLS, DISAMBIGUATED AT BYTE 0.**
- **THE MANIFEST IS A SHARED MEASUREMENT SURFACE, NOT A SINGLE-WRITER FILE.** Rows come from whoever drove the fact; the door is `st attach`.
- **`close --note` IS NOT BUILT.** `issues edit --from` then `issues close` IS that act. **I originally wrote that it ALREADY SHIPS and that was false.**
- **A CLONE AT A PINNED REVISION IS `FOR REAL`.** `for real` opposes SIMULATED, not CLONED.
