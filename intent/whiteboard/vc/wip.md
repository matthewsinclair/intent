---
node: vc
name: Validation Claude
role: validation
session_id: e48565a9-8dc8-4718-bb68-37a3462a0a36
heartbeat_at: 2026-08-15 15:25Z
status: active
focus: "WP-02 CLOSED 7/7. hv ruled D39 (macOS arm64 only) + D40 (the tap). I corrected a check I had put in canon AND recommended to hv: codesign --verify --strict returns 0 on an ad-hoc binary. Two ACs were falsely satisfied and are now open. 31/104."
claims: [ST0056]
---

# Validation Claude (vc)

## DOING

- **WP-02 CLOSED, `gate: PASS -- 7/7`.** AC-02.7 verified by re-running it (6 green), not by reading cc's account; cc had built it before it was contracted. **cc's LIMIT on it is accepted and permanent: the stamp buys DETECTION, never recoverability for what already exists** -- `user_version = 0` is the ABSENCE of a version, not schema zero, so a pre-stamp store is refused forever and no migrator can reach it. In `migration.md`: **the ladder starts at 1, do not write a `0 ->` rung.** Read AC-02.7 as "no store is ever silently misread", never "no store is ever lost".
- **THE CORRECTION THAT MATTERS MOST TODAY IS MINE AND IT WAS IN CANON.** D38 recorded, and I sent hv as a recommendation, that **`codesign --verify --strict` is the check that means anything for a bare CLI. It returns 0 on an ad-hoc signed binary** -- an ad-hoc signature IS a valid signature, so it answers "is this intact", never "is this OURS". dc proved it on live bytes within the hour: `target/release/` is shared mutable state, a peer rebuild silently replaced the Developer ID signature on one of two shipped binaries, and the recorded check still returned 0. **Only verification from a QUARANTINED copy discriminates.** Corrected in `design.md` with a named notice and retracted to hv in writing -- a bad check that has been READ is not fixed by being edited.
- **hv RULED, direct, via dc: D39 macOS arm64 ONLY; D40 the tap is `matthewsinclair/homebrew-intent`, no `-dist` repo. cargo-dist DEFERRED.** D39 is recorded **as a scope call WITH its reversibility, deliberately not as an architectural constraint** -- on cc's warning, because this estate already converted "no DB migrations, ever" from a decision into a principle people defended and D01's reversal is what unwinding it cost. **A Linux leg is purely additive: a Linux artefact has no signing seam at all.**
- **TWO ACs WERE SCORED SATISFIED WHILE THEIR OWN PROSE SAID THEY DID NOT CLOSE (AC-06.4, AC-06.7), and both were mine.** Both texts were stale in cc's favour -- search works, the WP view landed -- and both now credit it. What remains in each is **an UNGUARDED arm, not an unbuilt one**: nothing asserts an unpopulated index is distinguishable from a genuine miss, and the WP-**body** search arm was verified once by hand and is tested nowhere (`search_surface.rs` covers a WP **title**, which cannot discriminate a WP hit from its parent thread's). Held red with notes saying the FILES are green and the CRITERIA are what is red. **Issue 0032**: `ac gate` satisfies an AC on the FIRST green AT covering it, so the honest repair (a second row at `to-write`) has no effect on the verdict.
- **cc's flag-reachability CLASS contracted as AC-06.8 + AC-06.9** rather than left as a test cc invents, because each remedy is a scope decision not a wiring job. **Ruling: wired or withdrawn, never advertised-and-inert.** `doctor --fix` is the instance where wiring it would BE the defect -- spec first (which findings it may touch, which it refuses, which side of the store/extract boundary) or it comes off the surface.
- **D36 (`rm intent.db` is not an operation) and D37 (no Intent PM state in Intent's output) are hv rulings, both swept.** D37's live half is `owed_by`/`owner_wp` -- a model field designed to carry our roadmap into a consumer's terminal. **D37 REACHES the published schema faces** (`intent schema` prints them), and the sweep runs on **referent, not identifier shape**: `ST0000` in help text is the reader's own retrofit and stays; `ST0056` as a sample id becomes neutral; "carried under the WP-10 policy" goes. **AT-00.8's guard is cc's, named so ic does not write it too.**
- **ESCALATED TO HV AND THE ONLY THING WAITING ON A HUMAN: Machine 3 has a FIFTH state.** `computed`, for a test-backed criterion in scope. **The design is right and I reversed my own ruling to say so** -- `ac rescope` on a test-backed AC has to land somewhere and `Unsatisfied` would store a satisfaction claim; no fourth value fits. **The defect is that four-becomes-five in a machine hv ratified lives only in `transitions.rs` and the test that checks it.** Nothing blocked; cc builds on.
- **hv gave every node the pen** (_"crack on and communicate between yourselves as needed"_), so the commit-permission block is gone and dc has pushed. **vc still does not build.**
- **vc is TRIGGER-DRIVEN, but hv has released me to keep working without checking in.** Fire on a peer question, a WP close, a green, or an hv request. **Not on in-flight edits, and do not chase.**
- **cc is still the critical path on every open WP** (03 AC-03.9 + AC-03.10 | 04 AC-04.1 + AC-04.6 | 06 now FIVE, having gained AC-06.8 and lost two false greens). Nothing ic or dc does can unblock a gate. **A WP that slips, slips here, and the boards will not show it.** WP-11 is the exception and it is blocked on hv alone: the tap repo does not exist and the binary still reports `3.0.0-dev`.

## THE MODEL, in case everything else is lost

1. **The intentdb is the durable SSOT. Everything else is a secondary artefact. Nothing on disk is truth.**
2. All of `intentsvcs` works FROM the db; sync runs both ways.
3. The typed Rust API is the ONLY door in, so conformance is **by construction**.
4. Re-creation from an extract is a **capability**, not a licence to treat the db as disposable.
5. Ingest passes the **hard gate** of the intentsvcs API; the gate does the work, not the file format.
6. **Migrations are normal.** "No DB migrations, ever" is DELETED and was never hv's constraint.
7. The real standing requirement is **platform and data-model openness** (AC-02.6): always a 1-1 db-entity-to-`.json`/`.md` mapping, lossless, usable elsewhere. **That is what bidirectional sync is FOR.**
8. **(D34) The committed extract is the INTERCHANGE; the DB is per-machine truth and is NEVER committed.** Authority is not bidirectional just because transport is. **So AC-02.6 is the durability mechanism** -- a field that does not round-trip is data loss at the clone boundary, not a gap -- and **`event_log` is the only table that is both durable truth AND not reconstructible from the files**, which makes `events.jsonl` a precondition of the model rather than a WP-04 detail.

## TODO

- **`wp reopen` does not exist and three of five WPs disagree with their own gate** (WP-02 and WP-04 say `Done` against BLOCKED; WP-05 says `WIP` against PASS). I caused two by adding ACs to closed WPs. Cannot be repaired through the tool until the verb exists -- do NOT hand-edit the status field to paper over it.
- **AC-00.1 carries the 28 deferred non-core `pending` rows.** ic's to name, gated here, not forgiven.
- **THREE THINGS ON HV'S DESK, none blocking**: Machine 3's fifth state (`computed`) since 14:08Z, still unruled -- **one word**; the unversioned published schema faces (a D-number candidate, wanted before WP-06 closes); and `doctor --fix`'s specification under AC-06.9. **Plus one ASK that is hv's alone: create `matthewsinclair/homebrew-intent`** -- outward-facing, so dc asked rather than did.
- **Issues 0026-0032 under hv's DEFAULT-DEFER, none gating.** **0032 is new and is mine**: `ac gate` ORs its covering ATs instead of ANDing them, so a criterion with two acceptance tests scores satisfied on one. Not fixed -- v2 tooling that WP-04 replaces, `bin/**` not mutated with sessions live -- but **whoever builds the v3 close-gate must not port the early-return.** 0026 was corrected from my own false `high` version -- read the Correction notice first. 0028 is one sentence into the shipped whiteboard `SKILL.md` and touches every node's commit habit. **0029 is a DECISION before it is a fix, and cc must check AC-03.6 first** -- that AC may be green through the copy 0029 proposes to delete.
- **hv rulings not yet built**: devbin `bin/.devbin` -> `devbin/` (dc); `intent doctor` flags an unwired hook and **`--fix`** repairs it (**NOT `--repair`** -- `--fix` already exists, do not add a second); auth model for WP-08 (local password at first install, Conflab-style cli-to-daemon).
- **WP-10 precondition, from cc**: measure L2/L3 failures per fleet member at its named revision before ruling whether a broken reference in a CLOSED thread carries or blocks.

## Verification kit

- `$CLAUDE_JOB_DIR/tmp/v3fix` is a **migrated** v3 fixture with its own `git init`. Recipe: `config.json` at 3.0.0, `st new`, prose into `thread.json` (**never** a generated view), `sync`.
- **Falsify before flipping.** Perturb the artefact the test asserts against and watch the right subset go red.
- **Refuse at partial coverage. CORRECTED 2026-08-15: hold the AT at `red` WITH AN EXPLICIT NOTE, not at `to-write`.** I had this the other way round on the grounds that `red` lies about a passing suite. It does not: **the AT's status is the CRITERION's state, not the file's**, and `to-write` is the worse lie because it claims no file exists when one does. Write the note so anyone running the suite and seeing green knows why the board is red. AT-00.5 is the precedent for the refusal; AT-06.4 / AT-06.7 are the corrected form.
- **A second AT row at `to-write` does NOT hold a gate open** (issue 0032): `ac gate` satisfies on the FIRST green AT covering the AC. Until that ANDs, the covering AT itself has to come off green.
- **The D34/D35 numbers are in canon, not here** -- `design.md` D34 carries the FTS ratios, the GitHub ceiling, the pack sizes and the `dbstat` split; D35 carries the WAL measurement and the rejected `.sql` dump. **Cite them; do not re-derive them.**

## Watch-outs

Measurement rules live in `intent/st/ST0056/parity.md`. What follows is operational to this node.

- **THE CLOCK -- in force at full strength, and on death row.** D33 deletes these rules the moment WP-14's API is the only writer; until then the class is constructible. **SIX fabrications.** Run `date -u +'%Y-%m-%d %H:%MZ'` **in its own step** and paste it. A stamp 2 minutes out passes the guard (issue 0027). **`stat` and `git log` both print LOCAL time** -- reading one and appending a `Z` gives a stamp wrong by exactly the offset and looking perfect.
- **The sixth fabrication has a mechanism, and it is the useful part: I BATCHED `date` into the same command as the write.** The heredoc was composed before the command ran, so the stamp was generated and the clock read in the same breath -- and the generated one was a minute ahead. **Batching the clock read with the write looks exactly like compliance and defeats the rule entirely.** That is why "its own step" is the wording. **Correcting it to the reading I actually took is legitimate; inventing a plausible replacement is not** -- the ban is on invention, not on using a measurement you hold. Declare the correction in the entry.
- **Refusing to settle by inference is NOT a resting state.** It obliges you to go and get the answer. A question parked across three rulings is a decision made by default -- that is how D01 stayed wrong through four hv statements of it.
- **When you inherit a rule with a rationale attached, the rationale is the part most likely to be wrong** -- nobody re-derives it. Twice measured now: "no DB migrations ever" was a consequence defended as a requirement, and the binary-dirtiness case against committing the DB was true-sounding and not the reason.
- **Absence of a mechanism's NAME is not absence of the mechanism -- and it is not presence of it either.** I grepped `hooksPath` and filed a false `high`; I grepped `from: &[]` against a positional constructor, got zero, and nearly filed a refutation of a real finding.
- **A clean result is only as current as the needle set that produced it, and a needle set has a timestamp** (dc). **A structured query is a needle like any other and reports on the subtree it TRAVERSED** (ic) -- and it feels exhaustive in a way a grep never does, so it is trusted harder than it has earned.
- **A rule true in its own scope is the easiest to over-apply.** Check the SHAPE matches the shape the rule was measured on.
- **Verify at HEAD (`git ls-tree`), never on disk -- better, clone fresh and build.** **A green suite is evidence about the tree you HAVE, never the tree you PUSHED.**
- **AND THE OPPOSITE HALF, measured 2026-08-15: `git grep` reads the INDEX, not the worktree.** Searching for hv's leaked identifiers returned **ZERO** and I nearly reported the estate clean; plain `grep` over the same tree found 69 in `src/` alone plus all six sites hv was pointing at, because they are in peers' uncommitted live edits. **The two rules are opposite halves and the QUESTION picks the tool**: "what did we ship?" is HEAD, "what is a peer writing right now?" is the worktree. In a four-session shared tree the second question is the common one, and the tool's default answers the first.
- **ARCHIVE BY NAMING THE STAMPS. NEVER `sed -n '/^## (/,$p'`. THE GREEDY RANGE IS THE BUG.** FIVE times today the fold swept an entry I had never read (dc 13:33Z, ic 14:22Z, cc 14:30Z, then cc 15:14Z + dc 15:13Z) -- and **the last two happened AFTER I wrote the rule "archive only up to a stamp you have recorded as read" and while I believed I was obeying it.** I then implemented the exact opposite: a range from the first `## (` to EOF, which archives *everything present* rather than *what I have read*. **The prose rule was right and the mechanism contradicted it, so the rule bought nothing.** dc's 15:13Z carried hv's two rulings and cc's 15:14Z carried a whole defect class -- I read both only because I printed the stamps as the script ran and did not recognise them. **A control refuses; a resolution reminds. This one had had three resolutions.**
- **Why it stays invisible: a fold CAPTURES into `.history/` rather than deleting**, so nothing is ever lost and no error is ever raised. The only signal is counting entry headings afterwards -- and by then you have answered the wrong message.
- **`git commit --only <paths>`, never `-A`** -- and **a move is TWO facts**: naming only the new paths commits half a move, silently, with a green working tree. **`--only` also never CLEARS the index** (measured, issue 0028): a stale entry survives every later commit, is invisible to `git diff HEAD`, and shows only as the left `M` of `MM`. **Read `git status --short`, not the diff.** When peers are live, scope any `git reset` to your own paths.
- **THIS REPOSITORY IS PUBLIC** and hv has ratified the whiteboards as part of the public record, intentionally. The `-A` hazard is therefore a _publication_ hazard into a history nobody can rewrite.
- **Never mutate `bin/**` or `tests/**` in place** -- `~/.local/bin/intent` symlinks here and the BATS suite reads the live tree. Sacrificial worktrees only.
- **Never `head` a list you are counting**; a frequency-sorted list puts the RARE value last. **Read `$?` before anything else touches it** -- but the pipe trap is the pipe trap; `A || echo "$?"` reports A correctly.
- **This shell is zsh**: no word-splitting of unquoted parameters.
- **BACKTICKS NEVER GO INSIDE A DOUBLE-QUOTED `-m` BODY.** They are command substitution: the words are EXECUTED and then silently deleted from the message. **Control, not caution: single-quote the body, or `git commit -F <file>`.** Demonstrated on myself at `0e84acc9` -- a pushed, public commit reading _"the state tag is , not "_, in the sentence whose whole job was naming which of two tags won -- **inside the commit replying to ic's report of this exact defect.** Not amended; rewriting public history costs more than a wrong log line.
- **A detector's false-positive set is part of the measurement, and you find it by READING the hits, not counting them.** Chasing the above: first detector said 23 of 60 commits scarred, every node affected, and I was one step from publishing it. Almost all were ` .treeindex` / ` .backup/` -- **a space before a dot is normal when the next token is a dotfile.** Re-measured with a pattern dotfiles cannot trigger: **exactly one, mine.** Same shape as the `hooksPath` claim I filed at `high` and withdrew; the only difference was checking before publishing.
- **The live channel does not survive a peer's restart; the inbox does.** Durable copy first, live ping as accelerant.

## Decisions

Archived once a committed artefact carries them -- see `.history/`.

- (2026-08-15) **A SUFFICIENT-LOOKING CHECK THAT ANSWERS A NARROWER QUESTION THAN THE ONE BEING ASKED, AND FAILS GREEN.** Named by dc; three instances in one day from three nodes. cc's store that opened cleanly and could not be read (`CREATE TABLE IF NOT EXISTS` makes the DDL a no-op). dc's doctor blank-field bug. **And mine: `codesign --verify --strict`, which I put in canon and recommended to hv, returning 0 on a binary we did not sign.** The tell is that the check is the one anybody would reach for -- that is what makes it survive review.
- (2026-08-15) **AN AC NAMES THE OUTCOME; THE MECHANISM BELONGS IN THE WORK PACKAGE.** AC-11.1 named cargo-dist and was invalidated by a measurement OF cargo-dist, while the thing the project actually wanted was perfectly achievable. **A criterion that names a tool can be killed by a fact about the tool.** Cost a rewrite instead of a rethink only because dc measured before wiring.
- (2026-08-15) **A STALE MEASUREMENT INSIDE A CRITERION IS WORSE THAN NO MEASUREMENT, BECAUSE IT READS AS CURRENT EVIDENCE.** AC-03.9 said db-to-disk "does not exist at all -- measured 2026-08-15"; `Facade::sync_to_disk` existed and `st sync --write` had been calling it. True when written, and nothing made it stop being true loudly. **Three instances today, all mine** -- the two vacuous greens are the same defect: my criteria acquire facts and never re-check them. More diligence will not fix it; a measurement needs to carry what it was measured against so it can go stale visibly.
- (2026-08-15) **AN ADVERTISED FLAG THAT NO RENDERER READS IS WORSE THAN AN ABSENT FEATURE** (cc, five live instances). Help lists it, the parser accepts it, the exit code is 0 -- **an inert flag is indistinguishable from a working one at the surface an operator actually reads.** Ruling: wired or withdrawn. And the census that finds them must walk the DECLARED surface, because cc's grep missed `st new -s` -- the long spelling is `start`, which is all over the renderer as a verb, so the grep found the spelling asked for while the claim had another one.
- (2026-08-15) **A GENERATED ARTEFACT WITH A HAND-KEPT COMPANION THAT MUST MOVE WITH IT.** Three instances today: cc's `SCHEMA_VERSION` beside the DDL (got a tripwire), dc's sha256 beside the tap formula (got one), and the five published schema faces beside **nothing at all** -- measured, zero occurrences of "version" in any of them. **The internal, never-committed store is versioned and the external contract is not, which is exactly backwards**: the store rebuilds from the extract, a consumer's code rebuilds from nothing we hold.
- (2026-08-15) **RECORD A SCOPE CALL WITH ITS REVERSIBILITY, NEVER AS A PRINCIPLE** (cc's warning, taken on D39). This estate has already converted a decision into a principle once -- "no DB migrations, ever" hardened into something people defended, and D01's reversal is what unwinding it cost. **A cost accepted knowingly is a decision; it becomes a defect the moment someone cites it as a reason.**
- (2026-08-15) **A SIGNED BINARY IN A SHARED BUILD DIRECTORY IS TRANSIENT AND CANNOT CARRY A CLAIM.** `target/release/` is shared mutable state; one `cargo build` from any peer replaces a Developer ID signature with the linker's ad-hoc one, silently. **An AC is satisfied by a decision recorded and a mechanism implemented, never by an artefact on disk.**
- (2026-08-15) **An INSTRUCTION that was correct when written becomes an ATTACK when its precondition silently vanishes** (ic). Every generated inventory header says "re-run it rather than editing this file" -- correct advice that, once the input TSV went missing, rewrote all 26 files with empty fields **carrying the good data's revision stamp**. **It aims itself at whoever is most conscientious**, because they are the ones who follow it. Same family as D36's "a phrase can do damage while it is still officially correct", one level up.
- (2026-08-15) **A missing measurement must present as a REFUSAL to measure, never as a measurement of nothing** (ic). Third instance today: a `cp` backup that opens cleanly with zero rows, a hooks roster reporting fewer guards than the gate enforces, and an inventory regenerated from an absent input.
- (2026-08-15) **A collapse makes the new representation obvious and the OLD INVARIANT INVISIBLE** (cc). The guarantees a multi-field version enforced must be RE-DERIVED after a collapse, never assumed to survive -- the fields that carried them are gone and nothing points at what they were for.
- (2026-08-15) **Deleting a discriminator does not delete the distinction; it deletes the ability to DETECT it** (cc, reversing their own board on `EdgeKind::Incidental`). "Unused" is the right reading of the code and the wrong reading of the design.
- (2026-08-15) **A guard can be CONSTITUTIVE rather than procedural, and that is a stronger defence than ratification.** `ac descope`'s target-exists check is what makes Descoped differ from Withdrawn -- a dangling pointer is withdrawal with a nicer story. Ratification alone leaves a rule re-litigable; a constitutive argument closes it.
- (2026-08-15) **Two artefacts transcribed by ONE author from ONE document in ONE session are not two witnesses.** They agree with each other and both differ from the source. Found `computed`, a fifth state in a machine hv ratified with four, living only in the code and the test that checks the code.
- (2026-08-15) **Reverse on evidence, including your own ruling, and say which grounds survived.** I ruled the AC shape at 14:04Z and reversed at 14:07Z when cc's form turned out to satisfy my own two strongest grounds better. **The grounds were right; I had applied them to a candidate list that was missing the winner.** Go and read what was BUILT before ruling on what was described.
- (2026-08-15) **Do not downgrade an AC on every finding.** ic's inventory finding does not touch AC-01.3, which claims the contract EXISTS and does. A verifier who downgrades reflexively is as useless as one who upgrades.
- (2026-08-15) **A GUARD CAN LIE IN THE AFFIRMATIVE, not only by silence.** dc's D34 pre-commit refusal fired green on my commit and I reported it to hv as evidence the ruling had teeth -- it is UNCOMMITTED, so it is a property of this laptop and a fresh clone of a public repo gets nothing. **Until work is committed, every observation any of us makes of it is an observation of one machine.** Stronger than the unwired-guard case, because that one merely says nothing.
- (2026-08-15) **A phrase can do damage while it is still officially correct, and the damage is that it PRICES things** (from dc's D36 measurement). `rm intent.db` was never safe -- `event_log` had no canon path even under old D01 -- yet three canon sites justified a deferral or a rollback by how cheap it was. **Check what a vocabulary is being used to BUY, not just whether it is true.**
- (2026-08-15) **A model field that carries internal provenance will be rendered, because the renderer is doing its job.** D37's leak was not six bad strings; it was `owed_by` existing and `render.rs` faithfully printing it. **"We just do not print that field" is a reminder; unreachable-from-a-surface is a control.**
- (2026-08-15) **A one-sided test against a failure event cannot see an event that never happened.** AC-03.10 said "a failed backup surfaces" and a schedule that never fires produces no failure to report -- so the clause written to prevent the nothing-is-wrong / nothing-ran ambiguity contained it. **Reach for the two-sided test**: staleness against a schedule, like the clock guard's check C comparing two stamps to each other and needing no clock. Found by ic asking for a read path I had not specified.
- (2026-08-15) **A hazard reachable through SUPPORTED CONFIGURATION is worse than one reachable by misuse** (ic). Hence the fixed snapshot directory, and no key that silences a backup failure -- a switch to turn a warning off manufactures the silent failure and gives it a supported name.
- (2026-08-15) **GROUND THE NUMBER BEFORE RULING ON IT.** hv's instruction, and it changed the ruling's REASONING rather than confirming it. Everyone assumed binary dirtiness would kill DB-in-git; measured, git deltas SQLite well and the size ceiling decides it. **A correct conclusion resting on a false reason is the same defect as a wrong conclusion -- it just fails later, when someone leans on the reason.**
- (2026-08-15) **A hazard demonstration that fails to reproduce has told you NOTHING about the hazard -- it has only told you about your harness** (dc's wording, adopted over my weaker "suspect the setup"; his names why it is dangerous, which is that the two are indistinguishable from the green). The `cp`-of-WAL probe looked clean until I noticed it read the DB first, which checkpoints. Corollary worse than the lesson: a `cp`-based backup passes every hand-check and fails only under production concurrency. **And a test that has only ever been green proves as little as one that has only ever been red.**
- (2026-08-15) **Three nodes reaching one question from three unrelated entry points is the strongest evidence available that the gap is real.** One node asking is a node's confusion; three converging is a hole in the design.
- (2026-08-15) **A board field is a claim about a session's last fold, not a fact about the present.** I inferred authorship of a live edit from `status: active` and was wrong -- the editing node was `paused`.
- (2026-08-15) **`corrected` PRESERVES a ratified guard; "aspirational" reinterprets it.** When a ratified requirement and a measured v2 behaviour disagree, the requirement governs v3 and the gap is a parity class -- never a reason to widen the guard to fit what the code already does, which is invisible because the result is green.
- (2026-08-15) **The satisfied-count going DOWN during a spec change is the contract working.** A rising count while the spec moves is the thing to distrust.
- (2026-08-15) **Correct in place with a named Correction notice; never a quiet edit.**
- (2026-08-15) **A control refuses; documentation reminds; only one is load-bearing** (cc). Treat a rule you can obey only by concentrating as an unfixed defect.
- (2026-08-15) **File a defect under its own noun, even when that reopens a closed WP.**
- (2026-08-15) **Report what you MEASURED, never what you INTENDED** (ic and dc, independently).
- (2026-08-14) **Verify a claim by re-running its evidence, never by reading its account.**
- (2026-08-14) **The contract leads the build or it trails it, and trailing costs more.**
- (2026-08-14) **hv standing authorisation is not review, and does not reach a ratified decision.**
- (2026-08-14) **hv ruling: cc and ic write the code; vc ensures.** vc holds the ST0056 claim as steward and does not build.
- (2026-07-02) vc fires on a close/green/freeze trigger or an hv request, never on in-flight edits. Advisory only; findings to the owner's inbox, compounding risk to hv.
