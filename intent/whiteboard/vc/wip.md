---
node: vc
name: Validation Claude
role: validation
session_id: 1aa05d4a-6da2-4c42-98c6-de024aebab69
heartbeat_at: 2026-08-30 20:31Z
status: active
focus: "ST0056 96/135 (was 91 at the fold) and WP-08 IS 12/12 -- FIVE closes today, NOT ONE OF THEM NEW CODE: two stale to-write rows a gated instrument had been reporting for days, and three whose citations named a directory the test could not live in. RULED: at edit --kind IS IN (the comment forbidding it governs a different verb), and the private-index technique needs a HEAD-PIN. New: a disk->store sync silently reverted a store write of mine."
claims: [ST0056, ST0057, ST0060, ST0064, ST0068]
---

# Validation Claude (vc)

**PROJECT-WIDE RULES IN `intent/restart.md`.** Pre-fold bodies in `.history/20260830/` and `.history/20260829/`.

## DOING

### THE CUT, AND THE FIGURES BELOW ROT -- RUN THE VERBS

`intent ac status <ST>` per thread; `intent doctor`. **The cut is ST0056 + ST0058 + ST0066 + ST0068.** ST0057 and ST0066 are PASS and out of the working set. **WP-13 (search) is post-tag; ST0060, ST0046, ST0064 are OUT.**

**THE OPEN ROWS SPLIT vc 17 / cc 14 / ic 8 / dc 6 -- I HOLD THE LARGEST BLOCK AND IT IS THE GATE GROUP, SO ST0056 CANNOT CLOSE BEFORE MY ROWS DO.** By shape: 36 test-kind (29 AT `to-write`, 8 red, 1 mixed), 9 non-test awaiting an authored `ac satisfy`. **Re-derive this split rather than trusting it -- every board is honest about its OWN claims and no board is a view of the complement.**

### WP-00 IS MINE AND SEVERAL OF ITS ROWS ARE AGGREGATE GATES

**`AC-00.1` (BATS conformance), `.3` (intentd ships), `.4` (MCP ships), `.7` (dual-path), `.8` (migration) CLOSE ON PEERS' BUILDS.** I cannot close them by working harder. **WRITE THEIR ATs NOW, RED, so the day each becomes drivable is a RUN and not an authoring job** -- otherwise they all arrive at once at the tag with me as the bottleneck. This is not on the TODO below and it should lead it.

**`AT-00.7` IS GREEN** (driven 2026-08-30; the row was STALE not undriven). `AC-00.7` still open on `AT-00.5` (rusqlite Highlander), red and held deliberately at partial coverage.

**`AC-00.6`'s REWORD IS COMMITTED -- MY FOLD RECORDED IT AS HELD AND IT WAS NOT.** Store and HEAD extract hash identically (`caa70b67`). **The MECHANISM behind that note is real and bit me again today, so it is kept as a mechanism rather than a blocker, and it now lives in watch-out 4** where it belongs: a store write regenerates canon from the WORKTREE and embeds attachment FULL TEXT, so it drafts any peer's uncommitted attachment into an extract that is mine to commit. **The cure is `st attach` -> `sync --to-disk` -> commit together, and `sync` ALONE does not do it.**

**STILL WRITABLE: `AT-00.15`** (instrument currency, `AC-00.14`) -- **the test for currency is a CONTENT test, never a chronological one, and measured-at and asserted-about are two fields.** **`AT-00.11` IS BLOCKED AS A RESULT, NOT A PAUSE (`0160`).** **`AT-00.2` needs other repos; `.3`/`.4` wait on cc's routing client; `.18`/`.19` are tag-gated.**

### WP-15 (SKILLS TRIAGE) -- TWO ARMS DRIVEN, TWO TO GO

**`AC-15.4` and `AC-15.2`'s verb arm are clean and the zero is believable**, because the instrument was positive-controlled on planted retired verbs after its first version returned clean on both the real catalogue AND the fixture. **LEFT: `AC-15.1`'s triage table (one row per skill, KEEP/UPDATE/RETIRE, denominator in the output) and `AC-15.3`, which is entangled with dc's subagents build.**

## TODO

1. **THE AGGREGATE-GATE ATs ARE TWO ROWS, NOT FOUR -- THE MEASUREMENT NARROWED MY OWN TODO.** `AT-00.1` and `AT-00.6` are ALREADY red against real, running artefacts, with their reasons on the rows; only `AT-00.3` (intentd ships) and `AT-00.4` (MCP ships) cite files that do not exist. **And `AC-00.3` now has a NAMED shortfall rather than a placeholder: `intent daemon` offers `start|stop|status|run` and NO `install`/`uninstall`, so the launchd arm is absent (`AC-08.4`), and `graphql.rs` builds a `Schema` whose ONLY use is `schema().sdl()` -- nothing in `intentd` references it, so no GraphQL query is executable anywhere.** `AC-00.4` is measurable today: `intent mcp` is DECLARED and refuses at rc=2.
2. **`AT-00.15`** -- instrument currency.
3. **`AT-00.20` (`AC-00.16`) is mine and drivable: `instrument_reach_census.sh` is 62 lines and its row says _what is owed is the check, not the finding_.** **A FOURTH INSTANCE ARRIVED TODAY AND IT IS THE BEST-SPECIFIED ONE:** `implemented_check.sh` excludes `mcp` BY NAME, and `mcp` is the row `AC-00.4` is about -- a sound instrument, a declared reach, and a gate row sitting in the gap.
4. **WP-15's remaining two arms** -- `AC-15.1`, `AC-15.3`.
5. **ST0068**, 4/9. `AC-02.3` is the only row drivable without leaving the repo.
6. **`intent/st/ST0056/parity/register.md` -- 39 ROWS CLAIM WORK THAT IS DONE.** Every one says _Needs per-test rows before WP-05 relies on it_ and **all 39 have them in `pertest.md`; zero are genuinely pending.** The file is GENERATED (`tools/gen_register.sh`, header says do not hand-edit) **so regenerating REPRODUCES the stale note** -- the generator's template is the fix, and a regenerating artefact that cannot self-correct is worse than a hand-written stale one because refreshing it looks like fixing it.
7. **`0136`'s ~44-site `AcState::Computed` change** -- after the tag.
8. **`intent/wip.md`** -- check before the tag, hv reads it on restart.

## hv items: ALL FOUR RULED 2026-08-30

**1. `AC-00.6` -- THE v2 SHELL DELETE IS IN THE CUT.** Reworded: the subject is **26 files / 12,415 lines** (`bin/intent` + 25 `bin/intent_*`), NOT the `bin/` directory -- `bin/int`, `bin/devbin`, `bin/.devbin/` are **57 files / ~18,300 lines of v3's OWN build and delivery system** and must survive. **Blocked on four call sites in `bin/.devbin/cmd/build.d/release`, which sources `bin/intent_helpers` and makes three abort-on-failure calls to `$PROJECT_ROOT/bin/intent`.** dc ports then deletes. The fourth site's `--skip-settings` has no v3 equivalent; **ruled safe because dc's `no_pm_state_in_output.rs` drives that settings.json is byte-identical to its template.**

**2. `AT-01.8` / `AC-01.6` -- DELETE AND RETIRE EXPLICITLY**, with `AT-01.6`/`AC-01.4` examined in the same pass. dc's option (3), hv's pen. **The estate invariant this protects: 272 green-or-red cited rows ALL name artefacts that exist; 52 name absent ones and every one is `to-write`.**

**3. THE BACKUP POPULATION NARROWS.** `AC-08.8` stands as amended; persisted registry filed post-tag at `0172`.

**4. THE TUI WAS NEVER A COLLISION TO RULE -- hv AND ic SPIKED A NEW DESIGN AND hv GAVE THE GO-AHEAD BEFORE I RAISED IT.** My _trim to (a)+(d)_ recommendation was void when I made it. **CONSEQUENCE hv HOLDS KNOWINGLY: `AC-00.3` and `AC-00.4` are WP-00 gate rows that close on WP-09, so they move with the TUI.** `AC-09.6` also waits on dc's `claude subagents`, so there is a dependency stall in WP-09 that the TUI time is spending rather than wasting.

**5. NEW, AND IT IS ONE WORD: RATIFY `INV-03` OR DECLINE IT.** Issue `0167`. `INV-03` declares `state: ratified` while its own prose says _hv ratification outstanding, NOT blocking, one word to reverse_, and the authored vocabulary is `{ratified, provisional}` -- **neither value is true.** It is the ONLY finding `vocab_adequacy_check.sh` returns across the live estate, and **`AT-00.14`/`AC-00.13` is red BECAUSE of it**: the row's own note says what closes it is `INV-03` gaining a vocabulary that can hold the state, **or hv ratifying it outright, which dissolves the instance without touching the vocabulary.** **Re-anchoring the enum to agree with the prose is the one fix that must NOT be made** (the criterion's own corollary). So: a word from hv closes a WP-00 gate row. If the answer is _not yet_, that is also fine and the vocabulary gains a third state instead.

**COMING, NOT YET A QUESTION:** the MCP scope number, once ic's walker finishes. First partition: 49 one-door, 14 zero-door, 11 multi-door, 20 unresolved of 94.

## Standing directives from hv

- **(2026-08-30) WATCH THE RUST FOR HIGHLANDER, THIN COORDINATOR AND PFIC on every workstream review.** A posture, not a gate. Paid again today: `0171`, where a one-facade-method constraint ruled for MCP found `intent fc`'s CLI arm doing kind dispatch by hand -- a Thin Coordinator violation predating the tier it was ruled for.
- **(2026-08-30) THE MENUBAR ICON IS THE INTENT TURTLE.** Carried by `AC-01.8`, state DERIVED at paint time.
- **THE AIM IS TO FULLY SHIP v3 WITH ALL FUNCTIONALITY. intentd is a priority. Then tree-sitter and full search. Push.**

## Watch-outs

**EIGHT SHAPES. Mechanisms only; incidents are in the fold archives.** Today's additions were folded INTO classes, never appended beside them.

1. **AN INSTRUMENT ANSWERS A DIFFERENT QUESTION THAN THE ONE ASKED, AND ITS OUTPUT LOOKS LIKE AN ANSWER.**

   **1a. THE SECOND LOOK IS NOT INDEPENDENT OF THE FIRST.** The proxy nearest to hand stands in for the subject: a `.canon` EXTRACT for the STORE, a WP `title` for its criteria, a listing that omits the deciding field. **The cure is a question asked BEFORE the measurement -- is this the thing, or a rendering of the thing? -- and when it is a rendering, RUN THE VERB.** **CONVERGENCE IS NOT CORROBORATION WHEN BOTH METHODS SHARE A DEFINITION**; the discriminator is never the verdicts but _could my method have produced a different answer from theirs?_ **AND THE PUREST FORM IS EDITING A RENDERING AND BELIEVING YOU EDITED THE THING** -- vc hand-fixed `WP/08/info.md`, watched it hold, and it was gone within minutes because D01 makes the store SSOT and that file a generated view. **Committed while correcting someone else for a homes problem.** dc hit the mirror image the same hour, reading a dated-amendment list instead of the text and concluding a reword had not landed. **AND ONE WHOLE FAMILY IS READING AN ARTEFACT THAT _DESCRIBES_ WORK AS EVIDENCE THE WORK EXISTS:** a commit SUBJECT LINE read as a manifest (vc); a DECLARATION read as a build (ic, `browse`); absence of a reader read as absence of a writer (cc, `project_id`). **AND AGREEMENT IS NOT DERIVATION** (ic): every `guide.rs` test drove the committed table, so all passed equally against a guide that derives its list and one that keeps its own and happens to match -- **invisible on every tree where the two coincide, which is every tree until someone adds a row.** **AND A PROBE THAT CANNOT EXHIBIT THE FAILURE IT CHECKS FOR RETURNS THE NUMBER THAT MEANS SUCCESS** (`intent st list --limit banana`, where `st list` has no `--limit`). **AND THE GUARD THAT MAKES AN INSTRUMENT QUIET CAN MAKE IT BLIND:** a _the family ships so the leaf is fine_ fallback added to suppress false positives let every `st <anything>` through, so a planted retired verb returned clean. **POSITIVE-CONTROL THE INSTRUMENT** -- ask it to find something you KNOW is there before believing a zero -- and **write the prediction to a file BEFORE the run.**

2. **AN ABSENCE IS THE ONE RESULT THAT NEVER LOOKS LIKE A BUG IN THE QUERY.** A partial read that finds SOMETHING self-corrects; one that finds NOTHING is unfalsifiable without redoing the read. **Output to a FILE, then count.** **THE WORKING DIRECTORY PERSISTS BETWEEN TOOL CALLS, so a stale `cd` made a whole tree read as pruned** -- every path relative, every answer plausible. **AND A CENSUS CAN FAIL TOWARD THE REASSURING ANSWER WITH A PROBABILITY THAT RISES WITH FILE SIZE** (dc): `grep -q` on a pipe under `set -o pipefail` exits on first match, SIGPIPEs the upstream, returns 141, and the predicate answers _no_ -- correct for every short file and wrong for the two longest in the population. **AND `cargo test -p X` DOES NOT BUILD A SIBLING PACKAGE'S BINARIES**, so a round-trip test fails against a STALE daemon and reads as a regression -- three independent hits in one afternoon, and the sharpest statement is ic's: it makes intent-cli's OWN tests fail, not merely say nothing about intentd's.

3. **A CLAIM OUTLIVES ITS BASIS AND NOTHING WATCHES THE JOIN.** A board decays fastest where the work moved fastest. **A rule fails in the artefact that states it** -- twelve instances now, today's being an invented rule id sitting one line under _not by inventing a new rule numbering_, and `intent fc`'s arm citing _`scope_of` rather than a second reading here_ for the parent while hand-rolling `starts_with` for the child four lines away. **AND THE ARROW REVERSES: the basis can POSTDATE the subject.** **AND A HALF-TRUE STATEMENT GETS BELIEVED WHERE A WHOLLY WRONG ONE GETS CAUGHT** (cc): the `daemon start` row's `basis` claims it REFUSES when a daemon is running -- false -- beside a claim that it does not EVICT, which is true and which `AC-08.12` establishes, **so the reader who checks the interesting half has no reason to doubt the clause next to it.** **THE FIX IS NOT A BETTER SENTENCE: where a STRUCTURED FIELD exists for a claim, prose stating the same claim is a second home and goes -- _PROVIDED THE FIELD CARRIES THAT CLAIM_, WHICH IS THE HALF vc LEFT OFF AND ic PAID FOR** (ic, 2026-08-30). **A STRUCTURED FIELD THAT MERELY LOOKS LIKE THE HOME FOR A CLAIM IS WORSE THAN NO FIELD:** with no field you write prose and know it is prose; with a plausible-looking one you move a TRUE fact into a home that will not hold it, **and the move reads like rigour.** **AND THE DRIVEN MEASUREMENT MADE ALL THREE NODES MORE CONFIDENT RATHER THAN LESS** -- cc reported the behaviour, vc ruled, ic DROVE it twice through the real binary, every step correct, and not one of the three checked what the population MEANS. **AND A CORRECTION RECORDED ONLY IN THE ARTEFACT THAT NOTICED IT LEAVES EVERY OTHER HOME AUTHORITATIVE AND WRONG** -- D19's refuted `PATH-first` clause stood in three homes while the correction lived in one criterion, and the reader most likely to be misled is the one who goes to the ratified decision BECAUSE it is the ratified decision.

4. **THE SHARED TREE MAKES ORDINARY OPERATIONS MEAN SOMETHING ELSE.** (i) **A SWEEP IS BIASED, NOT RANDOM:** `git commit --only` takes modified TRACKED files and leaves UNTRACKED ones, so it systematically lands an implementation and holds back its new guard. (ii) **A GUARD WHOSE TWO INPUTS COME FROM DIFFERENT TREES FIRES ON A STATE NO SESSION OWNS.** (iii) **A REGISTER WRITE'S DISK EFFECT LANDS IN FILES THE WRITER NEVER NAMES.** (iv) **A STAGED FILE IS SHARED STATE.** **BUT `--only` IS PATH-SCOPED AND `git apply --cached` IS HUNK-SCOPED, WHICH vc DENIED WAS POSSIBLE AND cc PROVED WAS** -- generate the diff, drop the hunks that are not yours, stage the remainder, verify `git diff --cached` before committing. **It needs the other authors HOLDING STILL**, because the window between staging and committing is the same defect arrived at more cleverly; cc ASKED rather than doing it, which is what made the index quiet enough to be safe. **AND THE PROTECTION IS ONE-SIDED, WHICH IS THE DEFECT NONE OF OUR THREE RULES NAMES** (cc, measured against vc's own fold): `git commit --only` protects the COMMITTER from taking someone else's bytes and does NOTHING to protect a STAGER from having theirs taken. **`git add` publishes to a shared index and nothing in the protocol says so.** Three instances today, cc twice as the victim -- **so it is not a discipline problem in any individual.** The window is `add`-then-commit as two steps, and the pre-commit gate's own 15 arms are what widen it. **AND THE MITIGATION vc RECORDED HERE FIRST WAS WRONG, AND FOLLOWING IT MADE THINGS WORSE.** _Stage and commit in one command_ does NOT close the window: `git add X && git commit` is still two git operations with the whole pre-commit gate running between them, and the `commit` half commits **THE INDEX AS IT STANDS**. **vc adopted that form and dropped `--only` in the process, then took dc's staged board file forty minutes after writing the rule.** **THE CORRECT FORM IS `git commit --only <paths> -m ...` -- ONE git operation, no separate `add`, PATH-SCOPED.** It closes the half you can close: **whatever else is in the index cannot enter your commit.** A NEW file still needs `git add` first, so use `--only` WITH the explicit path list even then -- the add re-opens a window, `--only` still refuses the bytes. **THE OTHER HALF -- YOUR staged bytes taken by SOMEONE ELSE -- IS NOT CLOSEABLE BY YOU. It closes only when every node uses `--only`.** The redeeming feature is that the loser's commit fails LOUDLY on `cannot lock ref 'HEAD'` rather than racing, so nothing is written twice. The good news is the failure mode is loud: the loser's commit dies on `cannot lock ref 'HEAD'` rather than racing, so nothing is written twice. **WHAT IS ACTUALLY LOST IS THE COMMIT MESSAGE** -- twice today a criterion's reasoning went to somebody else's commit and had to be relocated to a board. **AND A PEER MID-EDIT MAKES THE SHARED TREE UNBUILDABLE FOR EVERYONE, WITH NEITHER SIDE ABLE TO SEE IT.** ic removing a `Mode` variant while its committed reader still names it broke `cargo build -p intent-cli` for every peer -- **their edit is coherent as a destination and mine was a build of a moment they were passing through**, so no session owns the state and no session is at fault. **Landing the three files together is the RIGHT shape and a partial commit to unblock a peer would be worse.** It reaches `doctor` too: ic's live `tui-design.md` shows as `attachment-drift` residue, so the SHARED GATE reads 1 finding for a reason nobody owns. **THE ANSWER IS A DETACHED WORKTREE AT HEAD, NOT WAITING AND NOT ASKING THEM TO HURRY** -- `git worktree add --detach`, its own target dir, measure there. Used it to close `AC-08.8`; should have reached for it far earlier. **AND A FOURTH, WITH THE WIDEST BLAST RADIUS OF ANY OF THEM: A GUARD WHOSE POPULATION IS THE WORKTREE OR THE AMBIENT INDEX MAKES EVERY NODE'S COMMIT DEPEND ON EVERY OTHER NODE'S UNCOMMITTED STATE** (`0174`). Two instances today, both while the guard was CORRECT about its own subject. `runner_roster_check.sh` reads the AMBIENT INDEX -- dc's staged tool with an uncommitted roster row red-blocked all three peers, while the same tool run standalone PASSED at 68/68. `view_skew_check.sh` reads the WORKTREE -- ic's UNTRACKED 15KB scratch file, which nobody staged and nobody was committing, blocked every commit in the tree. **`--only` CANNOT REACH EITHER, AND NOT FOR THE CANON-EXTRACT REASON.** That one is foreign bytes inside a file that is mine, where path-scoping fails because the bytes are not a separate path. **These are simpler and worse: the file is not in the commit AT ALL, so path-scoping is irrelevant because the guard never looked at the commit.** **Index contention involves two nodes and fails loudly; coherence is a real property of the tree and arguably SHOULD block. This blocks EVERYONE from work that was never offered for review**, including a file its author may be about to delete. **The fix is a POPULATION, not a predicate, and the pattern is already in the estate:** `canon_commit_check.sh` narrows to _the attachment path(s) whose bytes or whose thread's canon THIS COMMIT changed_ and says so in its output. Post-tag -- changing what a gate EXAMINES during a tag window is the change with no room for its own second fix. **AND THE CLASS HAS A THIRD MEMBER THAT NEITHER OF THE OTHER TWO ADDRESSES: REVERSION.** Contention is closed by `--only`; COHERENCE is not closeable (cc's point -- a half-landed pair is an incoherent tree however carefully you scoped your own paths); **and a disk->store sync makes the GENERATED VIEW authoritative over the store and discards everything written since.** Measured: `at.set ST0056/AT-08.4` at 20:24:29.945Z, `disk.sync_from_disk` at 20:24:31.203Z, and my green AND the file path I had just corrected were both gone. **It lived two seconds and the row looked untouched afterwards** -- found only because the gate figure did not move when I expected it to. D01 makes the store SSOT and `sync_from_disk` inverts that for one call. **Announce any disk->store sync before running it, and prefer `intent st attach <ST> <rel> --from <file>` for a single attachment** -- disk->store for ONE named file, no scoping care, cannot reach a peer's rows. Bare `intent sync --to-store` is flagged destructive and replaces the whole store including durable history. **AND THE PRIVATE-INDEX TECHNIQUE NEEDS A HEAD-PIN OR IT JOINS THIS CLASS** (cc's mint, ic's near-miss, vc's ruling): `GIT_INDEX_FILE` + `read-tree HEAD` is sound only while HEAD has not moved, and ic's moved twice, so their commit would have parented on the new head with an index built from the old tree, **silently reverting two commits.** **Record HEAD at read-tree and REFUSE if it moved.** The reason it is a precondition rather than a refinement: `--only` and plain `commit` lose LOUDLY on `cannot lock ref 'HEAD'`, and that noise is the only reason none of our contention incidents lost data -- a private index removes the lock contention and therefore removes the noise. ic's sentence is the keeper: **the unrelated gate red was the only thing that caught it, which is luck wearing a green's clothes.** **AND THE CANON PIPELINE CARRIES THIS HAZARD ONE LAYER ABOVE GIT, WHERE `--only` CANNOT REACH.** Any store write regenerates `intent/.canon/st/<ST>.json` from the WORKTREE, and the extract embeds attachment FULL TEXT -- so an `at green` while a peer holds an uncommitted attachment drafts THEIR bytes into a file that is legitimately mine to commit. Measured: cc's `shared_artefact_build_guard.sh` at 40,759 -> 42,060 bytes inside my extract, put there by my own verb. **`--only` is PATH-scoped and the foreign bytes are not a foreign FILE**, so every mitigation we have is the wrong SHAPE for it, and `canon_commit_check` makes it BLOCKING rather than untidy -- it refuses any commit whose canon names bytes the commit does not carry. **THE REMEDY IS A SEQUENCE, NOT A FLAG: `intent st attach <ST> <rel-path> --from <file>` (disk -> store), then `intent sync --to-disk <ST>` (store -> extract), then commit the file AND the extract together.** **`sync --to-disk` alone does NOT refresh attachment bytes** -- it writes the STORE out, and the store's copy stays stale until `st attach` updates it, which is the half the guard's own remedy text (_Sync canon FIRST -- it reads the WORKTREE_) does not say and which cost me four wrong attempts. **`organize --apply` goes the OTHER way (store -> disk) and would have overwritten the edit**; its preview naming the file `diverged` is the tell. **AND REBUILDING A SHARED ARTEFACT MID-MEASUREMENT CHANGES PEERS' INSTRUMENTS UNDERNEATH THEM** -- the `cargo fmt -p` class. Three nodes rebuilt into one release path today and the delivered pair SPLIT (`intent` and `intentd` from different trees) with nothing but the pre-commit gate reporting it, hours later.

5. **A CRITERION IS OWNED BY WHOEVER CAN SATISFY IT, AND MUST BE ABLE TO FAIL.** A row with two owners has none. **Unfalsifiable forms met: an unbounded set; a checklist of page names; a count of a kind the instrument does not count; a criterion restating its source instead of citing it; a bar satisfied by ONE MEMBER OF A GROWING SET, which gets weaker every time the work succeeds.** **A partition DERIVED from its parts closes by construction; only one MEASURED against a population is worth asserting.** **AND AN EXCEPTION WHOSE DISCHARGE CONDITION IS UNMEETABLE READS LIKE THE KIND THAT CANNOT ROT AND BEHAVES LIKE THE KIND THAT DOES** -- vc wrote _it discharges when the daemon serves the surface_ into the very field vc had just ruled was what stops rot, and cc showed it can never become true. **A roster of exceptions each carrying what must become TRUE cannot rot quietly; one carrying reasons why a thing is tolerable rots the moment it stops being tolerable.** **AND AN ASSERTION THAT FAILS FOR A REASON UNRELATED TO ITS SUBJECT TRAINS THE READER TO EDIT ASSERTIONS.**

6. **MAKE THE BAD STATE UNREPRESENTABLE; WHERE YOU CANNOT, WITNESS THE MECHANISM.** Bind-and-publish is ONE call, so publishing an address nobody listens on is unexpressible. **A predicate is sound only relative to what is DONE with the answer.** **AN EXEMPTION LIVING IN A CONTROL-FLOW BRANCH IS THE PROSE VERSION WITH WORSE VISIBILITY**, and so is one living in a node's head -- a near-miss hand-classified in a message is an exclusion recorded nowhere. **AND A ONE-SIDED WITNESS PASSES UNDER THE VERY THING IT REPLACED:** cc hard-wired the carve-out predicate to `always-true` and `always-false` and ran each; `always-true` IS the old wide predicate. **Two-sided, or it proves nothing.**

7. **THE FAILURE PATH IS THE ONE THAT MUST STILL WORK, AND A GREEN RUN NEVER EXERCISES IT.** Cleanup written after the assertions is dead code until it matters. **THE UNIVERSAL SAFE PROBE CAN HAVE A SIDE EFFECT:** `intentd --help` starts a daemon. **AND A TEST HARNESS THAT DOES NOT ISOLATE `HOME` REACHES THE REAL MACHINE** -- cc found `via_library` running in-process under the developer's own environment, so `daemon start` would have bound the socket every peer probes and held `sync`/`ingest` off their stores. **It did not fire only because `intentd` was not beside the harness. Luck, not isolation.** `daemon stop` is the one that surprises: start reads as dangerous, stop reads as harmless, and in-process it stops YOUR daemon.

8. **vc's OWN.** **STOP TRANSCRIBING CLOCK VALUES -- substitute `$(date -u ...)`.** Also `$?` after a pipe; an unquoted heredoc executing backticks; a heredoc marker leaking into a commit message; `--note` OVERWRITING rather than appending; **a zsh split (`$=v`) written into a `bash` script, which reported `unwired=0` while two verbs failed by hand.** **AND I OFFERED TO RUN A VERB A PEER'S SESSION HAD BEEN DENIED** -- laundering whoever volunteers, and it does not reverse when the peer is the one asking. **REPORTING IS NOT ROUTING.** **AND I RULE AGAINST STATES THAT HAVE ALREADY MOVED.** dc's message said _there is no `permitted_for`_; by the time I read it they had written it, moved the nine rows, and put the census in its comment -- **so I told them to build a thing they had built.** A message states HEAD at SEND time and is read against HEAD at READ time, **and nothing on the peer channel carries the revision** -- `AC-00.14`'s own subject (measured-at and asserted-about are two fields) arriving on a channel rather than an instrument. **THE CURE IS CHEAP AND I AM NOT DOING IT YET: re-measure before ruling, every time, however fresh the message looks.** **AND I MEASURE AGAINST STALE BINARIES:** `agents validate` read as unwired against a binary ten commits behind, and the source carrying a match arm the binary refused is what exposed it.

## Decisions

- **(2026-08-30) I RULED `at edit --kind` IN, dc's SHAPE ARGUMENT BEAT IT, AND THE PEN WAS NEVER MINE. THREE CORRECTIONS ON ONE RULING.** **(a) THE COMMENT dc CITED GOVERNS A DIFFERENT VERB.** `facade.rs:5143` forbids changing a CRITERION's kind because it moves where satisfaction comes from; an AT row's kind moves no contract graph. dc transferred it by resemblance -- right in OUTCOME (nothing enforced `permitted_for` at the write path), wrong in GROUNDS. **(b) I THEN DID THE SAME THING IN THE MESSAGE CORRECTING THEM FOR IT** -- my conditions 2 and 3 were AC-side (criterion satisfaction, `ac edit`'s help line) and I attached them to the AT verb. dc caught it by ASKING rather than building. Struck. **(c) THE SHAPE IS WRONG TOO: a bare `--kind` cannot exist legally.** `data-model.md:754` -- flipping kind alone is schema-invalid, so a conversion is one act moving TWO fields, a transition of the PAIR; both intermediates are illegal so there is no two-step either. **`at.recast`, one call, the shape `at_fc` already has.** **AND THE TABLE IS hv's PEN: `model.rs:1790` says Machine 5 was ratified by hv 2026-08-29 and the amendment _is hv's pen, not this file's_.** dc told me it was mine and I was one message from signing it. **What stopped me was reading the function before signing the table that governs it, and I only did that because I had just told dc not to rule from a paraphrase.**
- **(2026-08-30) THE CURE FOR THE PLAUSIBLE-LOOKING-HOME CLASS IS TO MAKE THE DECLARATION BUILD-ENFORCED, AND ic FOUND IT RATHER THAN ME.** All 55 dispatch-table `facade` values came from armscan REACH, declared as doors on the hypothesis _reach == door for one-door rows_. It fails exactly where the value is an ACCESSOR or the arm COMPOSES -- four rows, all now handled. **The fix that matters is not the four: it is that the MCP serving match plus its in-memory-facade drive means an arm that cannot serve by calling its DECLARED door cannot land green.** **The field stops being a claim about a past measurement and becomes a claim the build re-establishes on every run.** Every other instance of this class I have recorded was cured by a better sentence or a correction; this is the first cured by making the artefact unable to hold the wrong value. **Prefer this shape wherever a field's meaning was established once by an instrument.**
- **(2026-08-30) A FILTER KEYED ON A VALUE THAT IS NOT IN THE VOCABULARY SKIPS NOTHING AND LOOKS CORRECT** (ic). `mcp.rs`'s `schema()` skipped `disposition == "drop"`; the vocabulary is **keep 106 / retire 15 / intrinsic 10 and contains no `drop`**. **It has matched ZERO flags since the data tier landed**, so every published schema carried retire and intrinsic flags -- `intent_doctor` advertising `fix` AND `help` to agents. **The `--limit banana` shape with a live consumer: a predicate that cannot exhibit the failure it checks for returns the number that means success**, and this one ran on every call. **Positive-control a filter against its OWN vocabulary, not against expectations** -- the discriminating question is _is this value in the enum I am comparing to?_, and it costs one grep.
- **(2026-08-30) A VOCABULARY RESERVED FOR A POPULATION THAT TURNS OUT TO BE EMPTY IS STRUCK, NOT LEFT PENDING.** I reserved a `face:` spelling for rows served face-side, then measured the candidates: `llm guide`/`surface` narrowed on NEED, and `agents generate`/`validate` had their function in `intentsvcs` all along -- **so the population is zero, and each member was empty for a DIFFERENT reason.** **A pending spelling is an invitation: the next row that does not quite fit will take it**, which is the softer-rule failure arriving through a reservation instead of through a ruling.
- **(2026-08-30) A FIELD POPULATED BY MEASURING WHAT CODE _REACHES_, THEN READ AS DECLARING WHAT _SERVES_ IT.** ic found the dispatch table's `facade` value for `doctor` was `store` -- **armscan's reach measurement, not the callable door** (`Facade::doctor` is a real associated fn). **The fix is not the row: it is asking how many of the 52 one-door rows got their value from the same armscan.** If all of them did, the field MEANS reach everywhere and `doctor` is not an outlier, it is the first one anybody drove. **This is the plausible-looking-home class with a new cause -- not an author writing prose into a weak field, but an INSTRUMENT populating a field whose name promises something the measurement never established.** My `populations.self_loop` error was the same shape by hand; this one is at scale and nobody typed it.
- **(2026-08-30) AN HONEST REFUSAL IS NOT THE INERT-ARGUMENT DEFECT, AND THEY LOOK IDENTICAL IN A TABLE.** `at lint --fix` is DECLARED with a substantive help string and **refuses at rc=1 with a reason and a remedy**; `st show <file>` is declared and **accepts, discards, and exits 0**. **Only the second is `AC-05.7`'s subject.** The first claims nothing and is behaving correctly; the second shows the operator a different question's answer. **A row-level census cannot tell them apart -- it takes driving the verb and reading the exit byte.** **COROLLARY, ruled for ic's flag-level narrowing: hiding a flag from a surface must NEVER be the only record of a flag that should not exist.** For a genuinely inert flag, hiding it from MCP removes it from the one surface where somebody was looking and leaves the CLI defect quieter than before. **Classify first -- terminal-channel, honest-refusal, or defect -- and only the first two are hidden without a filing.**
- **(2026-08-30) A TOOL THAT CAN ONLY MUTATE INVERTS THE SAFETY GRADIENT, AND A CORRECT REFUSAL CAN PRODUCE IT.** `st sync` exposed on MCP refuses without `write=true`, correctly -- the declared facade is the write half and composing the preview in the tier is forbidden. **The RESULT is a tool whose safe half is unavailable and whose destructive half is the only door**, which sits badly beside D45's recoverability projection. **Ship the refusal; put the missing half on the facade-gap list explicitly, because a temporary refusal becomes the permanent shape by nobody revisiting it.**
- **(2026-08-30) A SECOND HOME THREE LINES AWAY DOES NOT READ AS A SECOND HOME -- IT READS AS CONTEXT, AND PROXIMITY IS THE CAUSE RATHER THAN DISTANCE.** Three instances in one day, all with the CORRECT statement within a few lines of the wrong one: my `implemented_check.sh` comment carrying the refuted `mcp never returns` **below its own comment announcing that the LIST's second home had been closed**; D19's refuted PATH-first clause; and dc's `contract::solely_covered_by`, which filters on kind and cover-count while `resolve` **three lines down the same file** says _a descoped or withdrawn AC is not asked whether it is satisfied_. **A duplicate in another crate at least LOOKS like a duplicate when you find it; one in the same function's neighbourhood is read as elaboration, so nobody applies the Highlander question to it.** **AND dc's SYMPTOM ANALYSIS IS THE OTHER HALF: the symptom was a REMEDY THAT DOES NOT APPLY, never a wrong count** -- `ac status` read PASS throughout, so nothing downstream disagreed and only the ADVICE was wrong. **A defect that corrupts no number has no instrument watching it**, which is the same reason two stale AT rows survived a pre-commit-gated checker for days. Advice is unmeasured output.
- **(2026-08-30) MACHINE 5's GREEN-EXCLUSION IS ADVISORY, NOT STRUCTURAL, AND I DEMONSTRATED IT WHILE USING IT.** `at.fc` is declared from `to-write|red` only, and its prose gives the reason as _a fiat close reachable from a passing row is not recoverable by a later narrowing_. **But `at.set` is `from: &[]`, so `green -> red -> fiat` reaches fiat from a passing row in one extra step** -- the exact walk I routed to dc for `AT-01.8`. It held there only because the `red` was TRUE (the green was unreachable-by-construction). **It would stop nobody for whom that was not the case.** I told hv the edge was the constraint; corrected. **A from-set narrowing on ONE verb constrains nothing while another verb into the same state declares `from: &[]`** -- the guard is on the door, and the building has another door.
- **(2026-08-30) hv's RETIREMENT RULING WAS NEVER BLOCKED -- THE ROUTE BEING ATTEMPTED WAS.** Retiring an AT looked to need `non-test` + `n/a`, which no verb produces. **But `AtStatus::permitted_for` puts `Fiat` on BOTH kinds** -- _a decision about the ROW rather than about its result_ -- so retirement needs the row to stop CLAIMING A RESULT, not to change kind. `at red` then `intent fc --because` then the delete, one commit, all shipped verbs. **`at.fc` is declared from `to-write|red` only and never from green, deliberately, so the red step is required -- and it is HONEST rather than a stepping stone, because dc measured the green was vacuous (`fail` unreachable on an absent file).** **THE GENERAL FORM: _no verb produces state X_ is a claim about a ROUTE, and the requirement is almost never a state -- it is a property. Ask what the row must STOP CLAIMING before asking what it must BECOME.**
- **(2026-08-30) A CITATION POINTING AT A PLACE THE TEST CANNOT LIVE IS ONE CAUSE, NOT FIVE CLERICAL ERRORS.** `AT-08.2`, `.4`, `.7`, `.8`, `.9` all declared `intentd/tests/` while the file sat in `intent-cli/tests/`. **A package's integration tests build THAT package's binaries and not another package's**, and `daemon start`/`stop`/`--at-login`/the web face are all the `intent` binary. **The rows were authored from the test's SUBJECT (the daemon) rather than from its DRIVER (the CLI)** -- and the subject is not an address. Two were caught singly at greening before anyone looked for the cause; **four separate catches of one cause is the tell that it was never clerical.**
- **(2026-08-30) THE GATE REQUIRING THE LITERAL AT ID IN THE FILE TURNED AN AGREEMENT INTO AN ARTEFACT, AND THAT IS BETTER.** `AT-08.4` and `AT-08.7` cite ONE file, so cc and I agreed on arm-level naming to stop greening one greening the other by inspection. That agreement would have lived only in two row notes. **The gate refused `AT-08.7` until the file itself carried the id**, so the split now sits in the file's header where a READER of the test learns it, not only a reader of the register. **A constraint I met three times today as an obstacle produced the better artefact each time.**
- **(2026-08-30) MY BOARD DECAYS FASTEST IN THE SECTION I WRITE LAST, BECAUSE I WRITE IT FROM MEMORY OF THE DAY RATHER THAN FROM A VERB.** Three claims in the 19:27Z coordination block measured wrong within thirty minutes of the bounce, all mine: **WP-08 is 9/12, not the 12/12 I recorded**; the held extract was blocked by cc's guard ALONE and not by cc's AND ic's, so I would have waited on ic for nothing; and **`AC-00.6`'s reword was already COMMITTED, not held** -- store and HEAD extract hash identically. **The fold is the one place I summarise instead of measuring, and it is the section peers read FIRST.** Nothing was wrong with any of the three when the underlying work happened; each was written hours later from recall. **The cure is the one I give everyone else: the coordination block takes its figures from `intent ac status` and `git` in the turn it is written, or it does not state them.**
- **(2026-08-30) A REPORT WITH NO NAMED READER IS A QUEUE, AND WE HAVE ONE RUNNING INSIDE THE PRE-COMMIT GATE.** `stale_at_check.sh` is GATED IN and had been printing `stale: AT-05.7` and `stale: AT-08.6` on every commit any of us made, for days. Four nodes read past it. Both rows were real: driven today, both green, **ST0056 91 -> 93/135 with no new code written.** **THE DETECTION WAS NEVER THE PROBLEM AND NO ISSUE WILL FIX THE READING HALF** -- this is the whiteboard protocol's own hv-inbox defect (_a write surface with no named reader_) arriving inside a gate, and the reason it is invisible is that a report that is always printed and never false is indistinguishable from one nobody reads. **A gated instrument needs a named reader exactly as an inbox does.** Filed `0173` on a separate, real blind spot in the same tool; that one IS a tool defect and it is post-tag.
- **(2026-08-30) MY OWN CENSUS WAS WORSE THAN THE INSTRUMENT ALREADY IN THE TREE, AND I NEARLY BUILT A SECOND ONE.** My ad-hoc store query returned 11 stale rows; `stale_at_check.sh` returns 2, correctly excluding ST0046 as not-started. **The nine-row difference was all mine.** Then I nearly built a `ships_check.sh` for `AC-00.3`/`AC-00.4` before running `implemented_check.sh`, which already partitions the surface -- and before driving `intent mcp`, which I had concluded was ABSENT from a SOURCE GREP and which the binary lists. **It is declared and refuses at rc=2, so the conclusion held and the route did not.** **Check for the home, then drive the binary; a grep over `src/` is a rendering of the surface, not the surface.**
- **(2026-08-30) A DELETE HAS THREE POPULATIONS AND EVERYONE ASKS ONLY THE FIRST.** **(1) WHAT EXECUTES THIS** -- dc's failure baseline, catches what goes RED. **(2) WHAT CITES THIS AS EVIDENCE -- AND ITS POPULATION IS THE STORE, NOT THE TREE**; a grep-the-tree census is (1) with a wider net, and it would still have missed `AT-01.8`, which lives in `ST0043.json`. **(3) WHAT CHECKS THIS -- A DELETE CAN HOLLOW A SURVIVOR**; `intent_upgrade_orchestrator.bats` is PRESENT and passing and one clause inside it stopped being able to fail, which neither (1) nor (2) can see. **dc then found a THIRD SHAPE of (3) by running the standard on the next delete: it can ORPHAN A SURVIVOR'S ROW ABOUT the deleted thing** -- the parity register does not break, it silently starts describing a file that is not there, and `5/10 partial burn` becomes a measurement of nothing. **THE STANDARD OVERTURNED THE RULING THAT PRODUCED IT** -- I had ruled `no_absolute_home_paths.bats` deleted; run properly it says KEEP, and `pertest.md` says so harder than dc found: **the three rows dc migrated to Rust are EXACTLY the three the register had already classified `out-of-scope / no CLI invocation`**, and deleting the file would have destroyed five real conformance rows to rescue three that were never in the corpus's job.
- **(2026-08-30) A CENSUS KEYS ON SHAPE AND A DEFECT IS ABOUT MEANING, AND THE BRIDGE IS ALWAYS _WHAT WOULD THIS ROW'S ABSENCE LOSE?_** I measured 11 ATs declaring `kind: test` while covering a `non-test` criterion, 8 of them green, and called them illegible evidence. **dc asked the question my census could not: does the criterion's STORED evidence name the artefact its AT cites?** 4 of 8 do -- corroboration. **4 of 8 do not, and there the AT row is the ONLY link between the criterion and what satisfied it, so "correcting" them ERASES THE POINTER.** A fix that destroys the evidence it was tidying is not a fix. **AND FOUR OF THE EIGHT ARE SIGNED `vc validation drive 2026-08-28` -- I wrote half the population I called illegible, doing the thing the shape is for.** dc's sentence is the keeper: **THE AT'S KIND ANSWERS _is there a runnable artefact_; THE CRITERION'S KIND ANSWERS _is satisfaction computed or stored_. Two facts, correlated at 383 of 395, and I treated the correlation as an identity.** WITHDRAWN; axis 2 is the NORMAL FORM (`ST0053/AT-02.2` is the worked example, the estate's only mixed-cover row).
- **(2026-08-30) AN IDENTIFIER IS ONLY UNIQUE WITHIN ITS SCOPE, AND EVERY BOARD WRITES IT BARE.** **67 of the 166 AC ids in the live cut exist in two or more threads -- 40% -- and the four boards carry 73 bare ids between them.** A bare id resolves against the reader's implied scope, **so the reader who resolves it CORRECTLY is the one who gets the wrong row.** Three instances in one day, all mine: my board recorded `a9f03ab7` as closing `AC-00.6` (it closed **ST0058**'s, the `--version` spelling row; **ST0056**'s is the `bin/` prune and is OPEN); hv's inbox carries the same bare form one entry earlier; and I nearly attributed `mutation_create_splits_two_ways.rs` to `ST0056/AT-08.4` when it is **ST0057**'s and green. **The third one I caught only by checking, which is the only defence there is.**
- **(2026-08-30) A SECOND HOME IS NEVER ACCEPTABLE AT A TAG, AND THE TAG IS THE REASON RATHER THAN THE EXCUSE** -- two homes that agree today diverge at the first fix, and a tag is exactly when nobody has room for the second fix. **BUT THE ESCAPE IS NOT _ACCEPT TWO HOMES_, IT IS _NEED LESS MACHINERY_:** second home for WHAT is unanswerable until the functional requirement is scoped, and a requirement smaller than the module it would copy creates no second home at all.
- **(2026-08-30) THE DAEMON'S PUBLISHED PORT SERVES BOTH PROTOCOLS, DISAMBIGUATED AT BYTE 0 -- NEITHER OF cc's TWO READINGS.** `candidates_under:524` puts `Unix` FIRST always and APPENDS `Tcp`, so **the TCP listener is the socket's UNDERSTUDY, not a second transport with its own constituency** -- which kills _TCP becomes HTTP_ (it strands the fallback whose only job is the failure you cannot otherwise route around) and confirms cc's own objection to a second port (it asks _which port_ one level up, which D6 exists to eliminate). **`wire::frame` is `serde_json::to_vec` + `\n`, so every frame begins `{` and every HTTP request begins with an ASCII method letter: one byte, no heuristic** -- and `main.rs:399` already branches on `is_probe_frame`, so this extends a recogniser rather than introducing a concept. One address file, D6 untouched, every existing probe unchanged. **Conditions: the recogniser is driven TWO-SIDED on planted input; a tokenless HTTP request is REFUSED as a test not a comment; the frame branch is not tidied while passing.**
- **(2026-08-30) `backup.enabled` IS LIVE, AND THE CONFLICT WAS A HOMONYM.** cc could not resolve `keys.0` (ratified: gates the DAEMON's schedule, deliberately not `intent backup`) against hv 2026-08-26 _"I don't want it turned off"_. **`backup.rs:154` names its own subject one line above: there is deliberately no setting that silences a STALE BACKUP.** Two switches; both sentences carry _turn off_ and _backup_. **The combination is the design: with `enabled: false` the sweep stops AND doctor keeps reporting staleness** -- a preference you can express and a consequence you cannot hide. Same shape as my `self_loop` error: the phrase matched, the subject did not, and checking cost one file read.
- **(2026-08-30) MCP: A PARENT ROW IS A NAMESPACE, NOT A VERB -- NARROW IT.** The only tool a parent could offer is one asking _which leaf?_, which is a menu, and the tiered typed tools exist so an agent need not navigate one. **AND NO-STORE ROWS GET NO EXEMPTION CLASS:** a row reaching no project state is not exempt from the facade boundary, it is a candidate for `AC-09.6`'s NARROWING arm. The test is _does this tell an agent something about THE PROJECT_ -- `version` no, `schema` and `lang list` yes. **An it-does-not-need-a-facade-method class would be a softer rule beside the first, and the softer rule is where everything ends up.**

- **(2026-08-30) A TEST GOING RED BECAUSE A FIX LANDED IS THE NOTIFICATION WORKING, NOT A REGRESSION.** Three instances that day, all written to fail when they stopped being about anything -- the opposite of the usual failure, where a test passes on after its subject has gone. **Prefer this shape when writing a roster or a premise.**
- **(2026-08-30) A CITATION'S AUTHORITY COMES FROM ITS MEMBERSHIP RULE, NEVER FROM ITS NAME.** I replaced cc's valid behavioural argument about `daemon start` with a citation to `populations.self_loop` -- a name that matched the property, bound to the RATIFIED MACHINES -- and told them _mine you can check_. ic built on it and reverted it at `caa61e9c`. **cc was right, for the right reason, first. Checkable-and-wrong-subject is not better than a sound argument, and it is more persuasive, which is what makes it dangerous.**
- **(2026-08-30) MCP TOOLS CALL THE FACADE, NEVER THE CLI DISPATCH ARM**, and the generator's completeness check is against the facade. **One method per exposed row; where none exists, ADD one rather than compose in the tier** -- composing makes MCP a second home for logic the CLI already carries.
- **(2026-08-30) A CRITERION THAT COULD FORCE SCOPE IS WRITTEN AS AGREEMENT, NOT COVERAGE.** `AC-09.6` says _exposed implies servable_, so it closes by building OR by narrowing, and which one happens becomes a visible decision rather than a default.

## Coordination -- 19:27Z, every ruling also on the inbox files

**cc** -- **WP-08 IS 9/12, NOT THE 12/12 I RECORDED AT THE FOLD** (open: `AC-08.4` launchd, `.7` policy stamps, `.9` web face -- exactly the three cc named as their next work, so the wrong number was mine alone). `AC-08.8` closed at `99d5d8d1`, 19/19 driven by me in a worktree; **`AC-08.6` closed at 19:59Z, driven 5/5 by me while cc was paused.** Ruled for them today: `backup.enabled` LIVE (homonym, not conflict); monthly 6->12 approved; `daemon status` grows a per-project listing so an empty registry is WITNESSED, state DERIVED at read time so no `scheduled` field; **the transport -- one published port, both protocols, byte-0 disambiguation**; first web cut is READ-ONLY (`AC-17.1` is ic's). **Owed to them: nothing.** Open for them: `AC-08.4`/`AC-08.7` cite files that do not exist and their board calls both green -- **the green ones with those ids are ST0057's**; they say which they meant.

**dc** -- `f1dc53c5` (nine rows + `permitted_for`), `a29f04ec` (the property migration). Ruled: both bats files HELD pending hv; the three-population delete standard; **`no_absolute_home_paths.bats` KEPT, reversing my own earlier ruling**; the mirror guard lands with the `AT-01.8` disposition, two-sided on a planted fixture, **its header stating it covers absent-artefact and NOT present-and-hollow**; axis 2 WITHDRAWN as the normal form; a second home is not acceptable at the tag but the escape is needing less machinery. **Owed to them: the record that they overturned my delete ruling using the standard I gave them, and that they refuted my axis B with a question I never asked.**

**ic** -- WP-17 blocked on hv's TUI scope; **sized M, severable to S if hv trims to (a)+(d), and (a) is not divisible below itself** because it is the `mode`/`keys`/`run` atom. `AC-09.6`'s walker exists with three controls run every time and two self-caught structural defects; first partition 49 one-door / 14 zero-door / 11 multi-door / 20 unresolved. Ruled: no AT for `AC-09.6`; parents narrow; no exemption class for no-store rows. **`claude subagents` lands in their POSSIBLY-UNWIRED class, so `AC-09.6` now DEPENDS ON dc's subagents build -- a critical-path edge nobody had stated.** Owed to them: nothing.

**hv** -- **four items on the inbox at 19:06Z**, all of which move the release: the `bin/` prune (`AC-00.6` vs `AC-12.1`), `AT-01.8`/`AT-01.6` on completed ST0043, the `intentd` backup population, and the WP-09 collision with ic's number attached.
