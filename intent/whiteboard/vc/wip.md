---
node: vc
name: Validation Claude
role: validation
session_id: 1aa05d4a-6da2-4c42-98c6-de024aebab69
heartbeat_at: 2026-08-31 09:40Z
status: active
focus: "FOLDED 2026-08-31 09:40Z, pre-fold at .history/20260831/wip-fold-0940Z.md. ST0056 102/135. hv ANSWERED FOUR DECISIONS, all to recommendation. I ARMED TWO REPO-WIDE GUARDS AGAINST EVERY NODE TODAY and both were mine to clear. Nothing owed to any node."
claims: [ST0056, ST0057, ST0060, ST0064, ST0068]
---

# Validation Claude (vc)

**PROJECT-WIDE RULES IN `intent/restart.md`.** Pre-fold bodies in `.history/`.

## DOING

### THE CUT -- RE-RUN THE VERBS, THESE ROT

**ST0056 102/135** (25 descoped, 2 withdrawn), from 97 at the 07:31Z fold. **ST0057 66/66 PASS. ST0066 6/6 PASS. ST0058 4/6. ST0068 4/9.** Run `intent ac gate ST0056` and the three feeders; do not read these.

Closed today: `AT-06.6`, `AT-10.8`, `AT-10.12`, `AT-00.14`, `AT-06.11`. Landed red-with-a-reason: `AT-00.15`, `AT-10.5`.

## TODO

1. **`AT-00.20`** -- instrument-reach census. Its own declared gap is the work: it reads `crates/*/tests/*.rs` and NOT the BATS suite, the shell instruments, the hooks or the critic library. **Closing that gap is what AC-00.16 asks (own the union), and it stops the instrument being an instance of its own class.**
2. **`AT-00.3`** -- `AC-00.3` names five capabilities. GraphQL now SHIPS (ic, `dbfc1eb1`), so the row is unblocked for the first time. **The artefact belongs in `parity/tools/` as a shell instrument** -- four of five are daemon-side and the fifth is a claim about the CLI, so no single-crate test witnesses all five. **Do NOT re-drive WP-08's behaviours; the row's claim is that they SHIP, which is the release-level question WP-08 does not ask.**
3. **`AT-00.4`** -- `mcp_surface.rs` does not exist and **`AT-09.1` SHARES it**, so one artefact closes two rows.
4. **WP-15's `AC-15.1` / `AC-15.3`**; **`AC-09.6`'s remaining OPEN classes**; **ST0068** 4/9.
5. **`parity/register.md`'s 39 stale rows** -- GENERATED, so the generator's template is the fix.
6. **`0136`'s ~44-site `AcState::Computed` change** -- after the tag. **`intent/wip.md`** -- check before the tag; hv reads it on restart.
7. **`design.md:88` still says `rmcp (official SDK): stdio ... now`**, which my 2026-08-31 ruling reversed. Flagged by ic. Design prose is hv's hand.

## hv items

**ANSWERED TODAY, ALL FOUR TO RECOMMENDATION.** (1) The four declared-and-unbuilt verbs SHIP in 3.0.1 with `--help` NARROWED. (2) `INV-03` RATIFIED -- landed, `AT-00.14` green. (3) Machine 5's table DEFERRED past the tag. (4) Facade gaps: build `schema` only -- landed by ic at `0a6f7784`.

**OPEN.** **dc's irreversible `bin/` delete**, held with hv directly, and **not ready for a decision**: dc's census found the population bigger than their own table by a factor -- 59 bats files resolve `INTENT_BIN` to a deleted default, 147 direct call sites with 143 in two files carrying 100 arms -- and their instrument returned a confident `any=0` on the two largest dependents. **They have withdrawn every zero it returned, not just the re-checked ones.** Next step is theirs: read the 100 arms.

**RAISED BY cc, NOT YET PUT: does a row move want an undo verb, or a ruling that it should not have one?** See 8c.

## Standing directives from hv

- **WATCH THE RUST FOR HIGHLANDER, THIN COORDINATOR AND PFIC on every workstream review.** A posture, not a gate.
- **THE MENUBAR ICON IS THE INTENT TURTLE.** Carried by `AC-01.8`, state DERIVED at paint time.
- **THE AIM IS TO FULLY SHIP v3 WITH ALL FUNCTIONALITY. intentd is a priority. Then tree-sitter and full search. Push.**

## Watch-outs

- **THE DELIVERED `intent` IS A SYMLINK INTO A BUILD OUTPUT DIRECTORY.** Mid-`cargo build --release` that path does not exist, so an instrument shelling out to it gets 127 where it expects a refusal -- **a wrong answer, not an error.** And it goes STALE: measured 2026-08-31, the delivered pair could not see 14 commits while the debug builds of the same tree could. dc's verb partition caught one member (`mcp`) flipping between the two builds.
- **A NOTE THAT NAMES ITS OWN EXPIRY IS A LIVE INSTRUCTION TO MOVE THE ROW.** Write the CURRENT reason; record a dead one as dead.
- **THE ROSTER AND THE TREE ARE TWO HOMES FOR ONE FACT, SO EITHER CAN OUTLIVE THE OTHER AND THE GUARD IS THE ONLY THING THAT NOTICES** (dc's formulation, adopted). Both signs met in one day: a file with no roster row, and a roster row with no file.

**EIGHT SHAPES. Mechanisms only; incidents are in the fold archives.**

1. **AN INSTRUMENT ANSWERS A DIFFERENT QUESTION THAN THE ONE ASKED, AND ITS OUTPUT LOOKS LIKE AN ANSWER.**

   **1a. THE SECOND LOOK IS NOT INDEPENDENT OF THE FIRST.** The proxy nearest to hand stands in for the subject: a `.canon` EXTRACT for the STORE, a WP `title` for its criteria, a listing that omits the deciding field. **The cure is a question asked BEFORE the measurement -- is this the thing, or a rendering of the thing? -- and when it is a rendering, RUN THE VERB.** **CONVERGENCE IS NOT CORROBORATION WHEN BOTH METHODS SHARE A DEFINITION.** **AND ONE WHOLE FAMILY IS READING AN ARTEFACT THAT _DESCRIBES_ WORK AS EVIDENCE THE WORK EXISTS.** **AND A PROBE THAT CANNOT EXHIBIT THE FAILURE IT CHECKS FOR RETURNS THE NUMBER THAT MEANS SUCCESS** -- newest member, and it is a property of the tool rather than of care: **clap's ARGUMENT VALIDATION RUNS BEFORE THE UNWIRED DISPATCH**, so a bare `config get` answers _required arguments were not provided_ at rc=1 and reads exactly like a wired subverb demanding input. dc and I hit it within an hour of each other from opposite directions. **Argument validation is part of the DECLARATION, not part of the implementation.** **POSITIVE-CONTROL THE INSTRUMENT and write the prediction to a file BEFORE the run** -- ic did exactly that on the DOUBLED fix and it is why the corrected figure is believable.

   **1b. A NEEDLE THAT AGREES WITH THE NUMBER YOU ARE CHECKING IS THE MOST DANGEROUS KIND** (cc). Their first probe grabbed a `verblock:` line, got NOT IN CANON, and would have CONFIRMED the file as stranded -- it is frontmatter, parsed into fields, so its absence proves nothing. **Four instrument slips of cc's in one day, all four caught by a control rather than by care. The rate is not the problem; the controls are working.**

   **1c. A ZERO WITH NO PLANTED POSITIVE BEHIND IT IS A SILENCE, NOT A MEASUREMENT.** `conservation_check.sh` printed `DOUBLED 0` beside `STRANDED 386` -- and it was the FIRST run in which DOUBLED could be non-zero, because the tool had been blind and every counter vacuous. Sight was restored and one counter came back still blind, **in the direction that inflates the residue.** Root cause: the counterpart moved from disk to canon under D01-reversed, so a counter asking the realised tree could never fire. Corrected to DOUBLED 194 / RELOCATED 192 / STRANDED 0, **every one of the 386 individually classified rather than subtracted**, with both zeros shown able to fire by planted mutations.

2. **AN ABSENCE IS THE ONE RESULT THAT NEVER LOOKS LIKE A BUG IN THE QUERY.** A partial read that finds SOMETHING self-corrects; one that finds NOTHING is unfalsifiable without redoing the read. **Output to a FILE, then count.** The working directory persists between tool calls. **A census can fail toward the reassuring answer with a probability that rises with file size.** **`cargo test -p X` does not build a sibling package's binaries.**

3. **A CLAIM OUTLIVES ITS BASIS AND NOTHING WATCHES THE JOIN.** A board decays fastest where the work moved fastest. **A rule fails in the artefact that states it.** **AND THE ARROW REVERSES: the basis can POSTDATE the subject.** **A HALF-TRUE STATEMENT GETS BELIEVED WHERE A WHOLLY WRONG ONE GETS CAUGHT.** **WHERE A STRUCTURED FIELD EXISTS FOR A CLAIM, PROSE STATING IT IS A SECOND HOME -- PROVIDED THE FIELD CARRIES THAT CLAIM.** **A STRUCTURED FIELD THAT MERELY LOOKS LIKE THE HOME FOR A CLAIM IS WORSE THAN NO FIELD.**

   **3a. A FINDING PARKED IN A FIELD ABOUT TWO OTHER FIELDS DISAGREEING DIES WHEN THEY AGREE.** `ext new`'s `recoverability_anomaly` carried a live product gap -- `ext` ships list/show/validate/new and no `ext remove`, so nothing undoes a scaffold -- and my narrow RESOLVED the disagreement as a side effect, so the generator correctly refused to let the note stand. **Rehomed to `0177` before deleting.** The record-outliving-its-premise class inverted.

   **3b. A GREEN ROW WHOSE TEXT OVERCLAIMS IS WORSE THAN A RED.** Under `0032` a green CLOSES the criterion and nothing re-examines a closed one, so the overclaim is inherited rather than caught. `AC-09.2` read _reaches the full API surface_ while reads-only shipped, **and the bound lived on hv's board while the row stayed unbounded -- two homes, and the row is the one people read.**

4. **THE SHARED TREE MAKES ORDINARY OPERATIONS MEAN SOMETHING ELSE. FOUR MEMBERS, ONLY ONE CLOSED.**

   **(i) CONTENTION -- CLOSED.** `git commit --only <explicit paths>` is ONE path-scoped operation. **`--only` IS PATH-SCOPED AND NOT HUNK-SCOPED, WHICH IS THE HALF THAT KEEPS BITING**: naming two canon paths for one row carried THREE hunks, two of them not mine. **THE CURE IS NOT A BETTER RULE, IT IS A STEP: SEMANTICALLY DIFF THE STAGED CONTENT AGAINST HEAD BEFORE COMMITTING** -- parse both, report differing leaf keys, confirm every one is yours. Used twice, found something both times. **cc's private index closes both axes and must be pinned on BOTH sides.**

   **(ii) COHERENCE -- NOT CLOSEABLE BY THE COMMITTER.** Land the pair, or wait. **Measured form: cc's `render.rs` and the table narrowing are one commit or neither, because while the table declares `json` the code refusing it is a RED on `format_roster_is_honoured`.**

   **(iii) REVERSION -- FAILS SILENTLY.** Announce any disk->store sync; prefer `intent st attach <ST> <rel> --from <file>`.

   **(iv) A GUARD WHOSE POPULATION IS THE WORKTREE OR THE INDEX MAKES EVERY NODE'S COMMIT DEPEND ON EVERY OTHER NODE'S UNCOMMITTED STATE** (`0174`) -- **the widest blast radius, and it fired THREE times today by three mechanisms, none of them staged and none committed.** An untracked file arming `declared_kind_check`; a canon edit moving `AT-10.5` off `to-write` and out of the exemption holding its citation; a roster row landing before its file was staged. **A CREATE CAN ARM A GUARD AGAINST A SURVIVOR, and a delete has to be committed to hurt anyone while a create only has to touch disk.**

   **AND THE MIRROR: EVERY INSTRUMENT READS THE WORKTREE, SO A DEFECT EXISTING ONLY IN THE COMMIT HAS NO WITNESS BUT A CLONE NOBODY HAS MADE** (`0178`). Three mechanisms: a private-index commit leaving the ambient entry ABSENT for a created path and STALE for a modified one (so a plain commit reverts your own change -- `0179`, and **presence of an entry is not currency**); `core.hooksPath` giving a clone every hook body and no hook; and a path-scoped commit landing canon without its artefact, which I did twice while warning two peers about the mechanism. **`canon_commit_check.sh` is the ONLY instrument reading the index, and its own output declares its reach as attachments only.** **THE CHEAP REMEDY IS NOT A TOOL: `git clone` into a tempdir costs four seconds and is the only witness the class has. Nobody thinks to run it.**

   **THE CANON PIPELINE CARRIES THE HAZARD ONE LAYER ABOVE GIT.** **AN ATTACHMENT IS AUTHORED AND NO SYNC DIRECTION REWRITES IT** -- `--to-disk` re-derives generated views and leaves it exactly as it stands, so editing a parity tool leaves the store holding old bytes and `canon_commit_check` refuses. **The sequence is `intent st attach`, then `sync --to-disk`, then commit file AND extract together**; `intent doctor` names the drift first. **The guard's own remedy says _sync canon FIRST, it reads the WORKTREE_, and for attachments that is FALSE.** **AND THE STORE IS SHARED BEFORE THE TREE IS** (ic): a peer's `st attach` puts their bytes in the store, so the next node to `sync --to-disk` realises them into a file that is legitimately theirs -- and the guard then forces that node to carry the peer's FILE too. **Land an attachment in the same breath as the attach, or expect it under the next canon committer's message.**

5. **A CRITERION IS OWNED BY WHOEVER CAN SATISFY IT, AND MUST BE ABLE TO FAIL.** A row with two owners has none. **Unfalsifiable forms met: an unbounded set; a checklist of page names; a count of a kind the instrument does not count; a criterion restating its source instead of citing it; a bar satisfied by ONE MEMBER OF A GROWING SET.** **A partition DERIVED from its parts closes by construction; only one MEASURED against a population is worth asserting.** **AN EXCEPTION WHOSE DISCHARGE CONDITION IS UNMEETABLE READS LIKE THE KIND THAT CANNOT ROT AND BEHAVES LIKE THE KIND THAT DOES.** **AND A FLEET CRITERION SCORED OVER THE ONE MEMBER SOMEBODY HAPPENED TO RUN IS AC-00.11's WRONG-M WITH ESTATES AS THE POPULATION** -- cc stated that limit themselves, unprompted, and refused to extrapolate from one corpus to four. **The discipline now lives in `fleet_corpus_conservation.sh` rather than in their restraint, which is the right home for it.**

6. **MAKE THE BAD STATE UNREPRESENTABLE; WHERE YOU CANNOT, WITNESS THE MECHANISM.** **A predicate is sound only relative to what is DONE with the answer.** **AN EXEMPTION LIVING IN A CONTROL-FLOW BRANCH IS THE PROSE VERSION WITH WORSE VISIBILITY.** **A ONE-SIDED WITNESS PASSES UNDER THE VERY THING IT REPLACED. Two-sided, or it proves nothing.** **AND A SELF-EXPIRING NOTE IS BETTER THAN A REMEMBERED ONE:** `gen_dispatch_table.sh` arm 2 refuses a STALE `recoverability_anomaly`, so a re-exposure must delete its own justification in the same act or the generator refuses.

7. **THE FAILURE PATH IS THE ONE THAT MUST STILL WORK, AND A GREEN RUN NEVER EXERCISES IT.** Cleanup written after the assertions is dead code until it matters. **THE UNIVERSAL SAFE PROBE CAN HAVE A SIDE EFFECT.** **A TEST HARNESS THAT DOES NOT ISOLATE `HOME` REACHES THE REAL MACHINE.**

8. **vc's OWN.**

   **8a. SHELL QUOTING EATS CONTENT AND THE COMMAND STILL SUCCEEDS.** Five mechanisms now: no word-splitting under zsh; an unmatched glob aborting the whole call; an apostrophe inside a single-quoted program being a PARSE error that runs nothing; `$?` after a pipe reading the last stage; and **a BACKTICK inside a DOUBLE-quoted string running a command** -- it shipped a truncated sentence into `26a35a11` and the commit succeeded. **The dangerous case is the word that IS a command (date, test, env, printf), which silently substitutes its output.** Write messages to a file and use `-F` with a quoted heredoc.

   **8b. I RULE AGAINST STATES THAT HAVE ALREADY MOVED.** A message states HEAD at SEND time and is read against HEAD at READ time, and nothing on the peer channel carries the revision. **RE-MEASURE BEFORE RULING, EVERY TIME, HOWEVER FRESH THE MESSAGE LOOKS.** Paid again today: I ruled `schema` behind the DOUBLED fix, and ic had already built it. **When a ruling's premise dies, SAY SO AND REVERSE IT rather than making a peer follow a rule whose reason has evaporated.**

   **8c. A ROW MOVE CAN BE IRREVERSIBLE, AND I FOUND OUT BY MAKING ONE.** `to-write` is the exemption `absent_at_check` grants; `red` requires the cited artefact to EXIST. I moved `AT-10.5` off `to-write` to reach a note that had become false -- **the note is only writable through the status verbs** -- and armed a repo-wide guard against three nodes. **THERE IS NO VERB THAT SETS A ROW BACK: `at` ships green/red/na, Machine 5 declares `(any) -> to-write` and the CLI exposes no spelling, `at new --status` refuses a taken id by design.** Issue `0033`'s class arriving operationally. **The repair had to be the ARTEFACT the row was owed, never a label.** **AND A RED ROW CITES A SHELL INSTRUMENT ON PURPOSE: a red Rust test breaks `cargo test` for everyone and a red shell tool does not.**

   **8d. REPORTING IS NOT ROUTING**, and I offered to run a verb a peer's session had been denied. **AND I MEASURE AGAINST STALE BINARIES.**

## Decisions

- **(2026-08-31) FOUR hv DECISIONS, ALL TO RECOMMENDATION.** The four declared-and-unbuilt verbs SHIP with `--help` narrowed; `INV-03` RATIFIED; Machine 5's table DEFERRED; build `schema` only. **`INV-03`'s fix was NOT the one the criterion forbids: nothing about the vocabulary changed, the outstanding EVENT happened and the prose now records it.** Re-anchoring the enum to the prose would have certified the unfinished state as finished -- the defect committed inside its own remedy.
- **(2026-08-31) THE ELEVEN UNWIRED MCP ROWS NARROW AS ONE FLIP**, on ic's principle that **the flag tracks the BINARY, not the roadmap.** Every one live-verified. **A HAND-MAINTAINED WIREDNESS FLAG IS WHAT `exposed_on_mcp` WAS** -- so where `unwired()`'s predicate needs wiredness, DERIVE it or declare it with a test that DRIVES every path. Never a hand-maintained flag.
- **(2026-08-31) `unwired()`'s REMEDY PREDICATE: THERE IS NO SPEC QUESTION, AND THE SEQUENCING IS THE RULING.** `is_shipped()` is a property of the DECLARATION while the sentence promises _the verbs that ARE_. **The else-branch already reads _nothing in this build provides it_, so a correct predicate routes `config` and `ext` into a branch that already says the right thing.** **THE PREDICATE FIX LANDS BEFORE OR WITH hv's `--help` NARROWING, NEVER AFTER** -- narrowing first points the remedy at an EMPTY list, which is ic's original zero-verbs defect by another road and strictly worse than today.
- **(2026-08-31) A BLIND INSTRUMENT MUST NOT ACQUIRE A QUIET FALLBACK.** `conservation_check.sh` refuses a pre-relocation layout BY NAME rather than accepting it. **A fallback that passes an old tree as current converts a loudly-blind instrument into a silently-wrong one -- strictly worse than what was there.**
- **(2026-08-31) THE GraphQL ESCAPE HATCH IS BUILT BEFORE THE TAG** (hv, over my recommendation to descope). **I bounded _the full API surface_ under the pen -- reads only, one derived roster, mutations out -- and the bound is now IN THE ROW rather than on hv's board.**
- **(2026-08-31) ZERO-DEP STDIO LOOP FOR 3.0.0; rmcp STANDS FOR STREAMABLE HTTP IN 3.x.** **The deciding fact is the one ic MEASURED, not the one they argued:** the ratified row cites Lamplight's `mcp.rs` as the stdio proof point, and it uses no rmcp.
- **(2026-08-31) `AC-10.8` -- QUALIFY THE CLAIM, option (d), a fourth cc had not put.** All three of their readings argued WHERE to print a list; the sentence already there was making a claim it could not support. **`AC-10.5` is the same question asked of a different verb**, so the sequencing was ruled too.

**FOLDED 2026-08-30 21:33Z from 26 entries to 11, and again 2026-08-31 09:40Z. Today's incidents went INTO the mechanisms, never beside them.**

- **THE MCP RULESET, ONE ENTRY BECAUSE THE CLAUSES ONLY MAKE SENSE TOGETHER.** **(a) MCP tools call the FACADE, never the CLI dispatch arm.** **(b) A PARENT ROW IS A NAMESPACE, NOT A VERB -- narrow it.** **(c) THE TEST FOR EXPOSURE IS NEED, NOT PROVENANCE.** My first spelling was _does it tell an agent about the project_, and it was WRONG. **(d) NO EXEMPTION CLASS AND NO `face:` SPELLING** -- when I measured the candidates the population was EMPTY, and **a vocabulary reserved for a population that turns out empty is STRUCK, not left pending.** **(e) `exposed_on_mcp` REFUSES ON ABSENCE**, which is stronger than the default-false I ruled for: default-false would convert a forgotten decision into a quiet non-exposure instead of a refusal that makes somebody decide. **(f) THE TIER SERVES IN-PROCESS ONLY IN 3.0.0.** **(g) DIRECT FACADE CALL NOW, `dispatch(op)` AS THE 3.x DESTINATION -- sequencing, not principle.** **(h) A TOOL THAT CAN ONLY MUTATE INVERTS THE SAFETY GRADIENT.**
- **A CRITERION THAT COULD FORCE SCOPE IS WRITTEN AS AGREEMENT, NOT COVERAGE.**
- **A DELETE HAS THREE POPULATIONS AND EVERYONE ASKS ONLY THE FIRST.** (1) what EXECUTES this. (2) what CITES this as evidence -- **and its population is the STORE, not the tree.** (3) what CHECKS this -- **a delete can hollow a survivor, or ORPHAN a survivor's row ABOUT the deleted thing.** **The standard overturned the ruling that produced it.**
- **A CENSUS KEYS ON SHAPE AND A DEFECT IS ABOUT MEANING; THE BRIDGE IS _WHAT WOULD THIS ROW'S ABSENCE LOSE?_** **The AT's kind answers _is there a runnable artefact_; the criterion's answers _is satisfaction computed or stored_ -- correlated at 383 of 395, and I treated the correlation as an identity.**
- **AN IDENTIFIER IS ONLY UNIQUE WITHIN ITS SCOPE, AND EVERY BOARD WRITES IT BARE.** 40% of live AC ids exist in two or more threads. **The reader who resolves it CORRECTLY is the one who gets the wrong row.**
- **A SECOND HOME IS NEVER ACCEPTABLE AT A TAG, AND THE TAG IS THE REASON RATHER THAN THE EXCUSE.** **The escape is not _accept two homes_, it is _need less machinery_.**
- **A SECOND HOME THREE LINES AWAY READS AS CONTEXT. PROXIMITY IS THE CAUSE, NOT DISTANCE.** **AND ITS SYMPTOM IS A REMEDY THAT DOES NOT APPLY, NEVER A WRONG COUNT. A defect that corrupts no number has no instrument watching it.**
- **A CITATION'S AUTHORITY COMES FROM ITS MEMBERSHIP RULE, NEVER FROM ITS NAME.** **Checkable-and-wrong-subject is more persuasive than a sound argument, which is what makes it dangerous.**
- **THE CURE FOR THE PLAUSIBLE-LOOKING-HOME CLASS IS A DECLARATION THE BUILD RE-ESTABLISHES** (ic). **Every other instance was cured by a better sentence; this is the first cured by making the artefact unable to hold the wrong value.**
- **A TEST GOING RED BECAUSE A FIX LANDED IS THE NOTIFICATION WORKING.** **And a constraint met as an obstacle often produces the better artefact** -- the gate demanding the literal AT id in the file turned an agreement into something a reader of the TEST learns.
- **THE DAEMON'S PUBLISHED PORT SERVES BOTH PROTOCOLS, DISAMBIGUATED AT BYTE 0.** Every frame begins with a brace and every HTTP request with a method letter: one byte, no heuristic.

## Coordination -- 2026-08-31 09:40Z

**NOTHING IS OWED BY ME TO ANY NODE AND NOTHING OF MINE IS UNCOMMITTED.** Last: `71539ab4`.

**cc** -- FOLDED AND PAUSED at `5ce65a5c`, 27272 -> 11730 bytes. Five landed. **Held `AT-10.5` twice rather than build a residue report against a figure they could not reproduce, and both holds were right.** Their `AC-10.5` work produced the row's first real number. **Sat blocked twice by my guards and did not reach for `--no-verify` either time.** On their bounce: the `AT-06.11` predicate fix, then `AC-10.5` with me.

**ic** -- the GraphQL hatch, the stdio server, `conservation_check.sh`'s relocation fix, the DOUBLED predicate, and `schema`. **Four clean landings using a technique the estate should adopt: build the blob from `git show HEAD:` rather than the shared worktree, regenerate the view from that same JSON, and `git restore --staged` every committed path afterwards.**

**dc** -- holds the `bin/` prune, resized upward twice by their own census. **Reported both of my guard outages rather than bypassing.** Their formulation of the roster/tree class is in the watch-outs above under their name. Fold shrank 68,014 -> 46,159 bytes while GAINING three classes.
