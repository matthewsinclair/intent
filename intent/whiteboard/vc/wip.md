---
node: vc
name: Validation Claude
role: validation
session_id: 5f420db0-02b8-4e46-9bb3-cf691eaac52c
heartbeat_at: 2026-09-17 21:40Z
status: active
focus: "2026-09-17 post-bounce: vc holds hv's pen for THE CLOSE-OUT and owns the lean-out of intent/wip.md and intent/restart.md that hv called for -- those two files are vc's alone and no lane writes them. LIVE CONDITIONS, each discharged by a check and not by reading this line: hv's full test suite is RUNNING, so nobody builds anything until hv says it is finished; the docs/reference baseline command is wrong in four homes until --tags is driven into each; cc's SQLite move is rusqlite-lockstep and comes to vc as two priced candidates before a line of it lands; 0443 is dc's; 0444 lands before the cut's regeneration or the register carries it wrong. Read a peer's state by asking the peer -- vc's own stale focus line made dc misreport to hv at 21:31Z. NO RELEASE, NO PUSH."
claims: [ST0056, ST0060]
---

# Validation Claude (vc)

## DOING

- THE CLOSE-OUT (hv, 2026-09-15): every open issue and thread closes on evidence or by ruling. Lanes and rules in intent/wip.md, rulings in intent/history/20260915-hv-rulings.md. vc: judge every bank, train and landing; impl.md and the ST0056 provisional markings; AC-07.6 reworded in the 0338 (i) landing; the quiet window on the word of hv. NO RELEASE, NO PUSH.

## TODO

- RESUME HERE (vc, 2026-09-17 post-bounce, replaces todo 34). WRITTEN AS CONDITIONS: every line below is discharged by running something, never by having read this. (1) THE LEAN-OUT IS vc's AND UNSTARTED: intent/wip.md 19 KB and intent/restart.md 9.6 KB back to bare bones on hv's instruction, the test being that a line stating a state comes out and a line carrying a command or a condition stays; the moves are Highlander moves, with the bank/train/landing rules merged out of wip.md into restart.md where half of them already live, 0442's characterisation left to `intent issues show 0442`, the regeneration ordering left to intent/docs/releasing.md, and every hardcoded count struck. ic has the verification half: after the lean, ic DRIVES every command and pointer in both files and reports what does not run. (2) NOBODY BUILDS while hv's full test suite runs -- check with hv, not with this line. (3) THE BASELINE COMMAND is wrong in four homes until --tags is driven into each: releasing.md twice (ic's, on vc's standing word), wip.md (vc's, inside the lean-out), this board (vc's, this item is that correction), hv board decision 25 (vc's, corrected forward by the decision beside this item and never rewritten). (4) cc's SQLITE MOVE IS NOT A dep BUMP: rusqlite pins libsqlite3-sys by caret so the two move together, and latest is eight rusqlite minors away on 0.x semver where every minor may break the API. vc has PRE-COMMITTED the decision rule before seeing the numbers: the requirement is a checker without the hard-coded emptied-leaf special case, which 3.46.1 supplies, so the MINIMUM candidate wins unless cc's read shows it carries a behaviour change on live stores that the latest fixes, or the minimum is itself the larger API move. Being current is not a requirement. (5) 0443 IS dc's on vc's ruling and 0444 must land before the cut's regeneration or docs/reference carries the wrong claim through the register. (6) dc's hold 15 needs its condition rewritten by vc, because hv routed devbin#0083 and the int hooks finding to the Devbin estate and ruled neither gates this release. MUST NOT: do not build anything while hv's suite runs; do not let any lane write intent/wip.md or intent/restart.md; do not rewrite hv board decision 25, which records what hv said and is corrected forward only; do not reopen 0442's cause, closed at eight negatives; never `intent fc`; NO RELEASE, NO PUSH.

## Holds

_(none)_

## Watch-outs

- **A parent build cannot read a newer build's fixture** (store 18 vs 17). Build each arm's fixture with its own binary.
- **Count `intentd` by executable**, `ps -axo pid=,command=` on argv[0]'s basename.
- A renderer change (a footer, a view's format) skews every generated view that organize does not touch: the whiteboard boards and inboxes, steel_threads.md and todo.md. Doctor refuses every commit until they match, intent sync --to-disk refuses while intentd watches, and the restarted intentd re-renders them itself, so after the rebuild and organize --apply re-read git status and commit those views with the rest (37e961bdb, 2026-09-15).
- **THE INSTALLED PAIR IS THIS TREE'S BUILD ARTEFACT, SO ANY RELEASE BUILD HERE SWAPS EVERY NODE'S BINARY MID-FLIGHT** (dc measured it 2026-09-17, vc drew the consequence): `~/.local/bin/intent` is a SYMLINK to `native/rust/target/release/intent` in this checkout, so "drive it on the installed pair" and "drive it on this tree's release build" are one run and a copy into a clone is a `cp`. The consequence nobody had written down is that `cargo build --release` in this checkout silently replaces the binary all four nodes are running, with nothing in the tree changing to mark it -- which is the MECHANISM under the rule that no commit lands while a rebuild builds, and it also means a scratch estate that resolves its binary through the symlink can have its subject swapped between an ancestry check and a verdict. A run that must name the version that produced its result pins the resolved file or copies it.
- **`intent st hydrate <ID>` RE-LISTS A CLOSED THREAD, AND `st done` OFFERS IT AS THE REMEDY FOR ITS OWN UNLISTING** (dc found and stopped on it, vc verified, 2026-09-17). Closing a thread removes its `STEELTHREAD:<ID>` line from `intent/.intentfiles` entirely, so nothing reconciles its tree afterwards and a disk edit to one of its attachments reads as `attachment-drift` rather than being ingested -- correct behaviour, and surprising after a close. The close then prints `intent st hydrate <ID>` as the way to write the files back, and `Facade::hydration` PINS UNCONDITIONALLY at `facade.rs:5555` ("STEP ONE: PIN. First, and unconditionally, because it is the step the obvious ordering skips"); `intentfiles::pin` has exactly two callers in the tree, that one and `edit_list`'s explicit `ListAction::Add`. So the offered remedy silently reverses half the close. Use the attachment route in vc decision 33 instead, and never reach for `hydrate` on a closed thread to fix one file.
- **A SOURCE READING IS A CLAIM ABOUT CODE, NOT A CLAIM ABOUT WHAT RUNS** (vc's own error, 2026-09-17, caught by cc within the hour). vc read that FTS5's config cookie is bumped only under `if( rc==SQLITE_OK && pVal )` and that all three version writes pass `pVal = 0`, which is true of every line; inferred from it that another connection's cached format version is never invalidated, which is false; and labelled the inference "a source fact rather than a hypothesis" -- the strongest label available -- before testing it once. It reached two lanes, an issue body and a commit message inside ten minutes, and a peer was repeating it back as the night's cleanest finding. cc read the cookie off disk with a positive and a negative control and measured it MOVING. **THE CONFIDENCE WAS NOT THE DEFECT; SHIPPING AN UNTESTED INFERENCE INTO DURABLE PLACES WAS.** A reading that predicts something testable gets tested BEFORE it is written anywhere durable, and the cleaner the reading the more that applies, because a clean reading that predicts the wrong thing is harder to let go of than a sloppy one. Corollary, from dc's own correction the same night: **a correct rule with the wrong cause under it is as dangerous as a wrong rule**, because only the cause is load-bearing when someone builds a fix on it.
- **AN INSTRUMENT CAN REPORT A CLEAN IT HAS NO STANDING TO REPORT, AND THE FIX IS TWO-SIDED** (2026-09-17, three instances in one night; dc named the pattern and dc's own correction made it institutional. Replaces vc watchout 21, which stated only the operator half). (1) dc's phase A read `PRAGMA integrity_check` = ok on a store still at FTS5 version 4, so no delete had gone through the secure-delete writer and the ok measured NOTHING. (2) A `.backup` path opening its source read-write, mutating the artefact it copies. (3) cc's `report` helper opening the database read-write and thereby CHECKPOINTING the 1.18 GiB WAL it was about to measure, after which five mid-checkpoint kills "completed" over nothing and three of four cases printed a clean rather than saying they had nothing left to test. **THE COMMON SHAPE IS AN INSTRUMENT THAT EITHER MUTATES WHAT IT MEASURES OR MEASURES A CORPUS THAT COULD NOT HAVE EXHIBITED THE FAULT, AND ITS OUTPUT IS THEN INDISTINGUISHABLE FROM A REAL NEGATIVE.** THE RULE HAS TWO SIDES AND THE ASKER'S IS LOAD-BEARING: **whoever ASKS for a measurement states in advance what reading would make a clean meaningless, and whoever RUNS it reads that byte before reporting.** The first catch of the night happened because the director set that condition before the run and not because the operator gated their own instrument -- so personal diligence is the weaker mechanism, and the asker's half is the one that survives a tired node at 3am. Every node is sometimes the asker: of a peer, of a subagent, of its own future self after a compact. cc kept its broken script banked rather than deleting it, because the failure mode is the finding.
- **A CONSOLIDATION IS WHERE A RETRACTED CLAIM GETS QUIETLY REINSTATED, AND FOLLOWING hv DECISION 7 CORRECTLY IS WHAT BUILDS THE TRAP** (dc, 2026-09-17, on vc's batched 0442 pass. Replaces vc watchout 24, whose check named the wrong instrument). Decision 7 says a false claim is corrected FORWARD and never rewritten, so a long-lived issue body carries every dead reading stated as confidently as the live ones, with its refutation somewhere further down. A consolidator reading FORWARD from that record meets each claim before its retraction and reinstates one. 0442 carried five in a single night -- vc's permanently-stale `iVersion`, cc's phantom-as-a-named-file, cc's frequency enrichment, dc's verb-versus-bare framing, and vc's promotion of 0443 -- every one of which had been in an inbox or a commit message as fact. **TWO MECHANISMS, KEPT TOGETHER BECAUSE THEY FAIL DIFFERENTLY: draft the consolidation from the RETRACTIONS first, listing the dead claims before a word of the body is written, which prevents at the start; and check the finished draft against that list, which catches at the end.** **AND THE CHECK'S NEEDLES COME FROM THE CLAIM, NEVER FROM THE CORRECTION** (dc's refinement, which is the difference between a check and a formality): grepping the phrasings the author used to RETRACT a claim only catches a reinstatement in the author's own retraction vocabulary, which is the least likely form -- a consolidator paraphrasing from memory reaches for different words by definition. dc checked vc's pass with seven needles drawn from the original claims and not from vc's five retraction phrasings. Decision 7 is still right; the consequence is that a consolidation is a different ACT from a read-forward and has to be worked differently. This estate has form at document scale -- the entry point was three copies of one narrative, each claiming to supersede the others, every one accreting rather than replacing.

## Decisions

- **NO OVERTESTING, NO YAK-SHAVING** (2026-09-12, on handing vc the pen): build what the STs need; tests are the AC rows; never a test that tests a test. Applies to every lane and to the director.
- **WATCH THE RUST FOR HIGHLANDER, THIN COORDINATOR AND PFIC on every review.** A posture, not a gate. **PFIC is _Pure Function, Impure Coordination_** -- deterministic core, I/O at the boundary.
- (2026-09-13) hv, verbatim: NO UNNECESSARY OVERTESTING, NO TESTING TESTS, NO YAK-SHAVING. Expeditious delivery of working code; do not relitigate the speed of light for every decision; be RUTHLESS. New code, good code, good tests, done. Applied: build the cheap fix and let the live system judge it; one run decides a question; never a positive control of an instrument; ACs one line per user-facing behaviour, ATs the test that proves it once.
- (2026-09-15) vc, under the close-out pen: after 0321, every line intentd writes to intentd.log or intentd.err.log, remedy: lines included, opens with an RFC 3339 UTC timestamp with a Z suffix, then one space, then the line byte-identical to today, through one writer module whose clock read is one_clock.rs's single exemption, its reason recorded once in ST0056 design.md's D42; ST0075's ConsoleLine skips exactly that token, when present, before it classifies a line from either log (in WP-02's bank), so 0321 lands with no Swift change. The intentd notices that carry a remedy: line but no severity token get intentd's existing warning: or error: token in dc's 0321 bank, which closes ic's new issue with 0321 (the critic advisory's option (iii); the Console does not guess). Two banks that share a file (MODULES.md, dispatch-table.json and its view) stack in the order they bank green, nobody holds a bank for the other, and the second proves its bank on top.
- (2026-09-16) vc, under the pen, on dc's costing read from the source at d24487079: devbin's migrate carry defects are fixed by a LOUDER MIGRATOR, not a richer model. The model is unchanged: no store rung, no face change, no new item verbs, because every new field would be data only migrate creates and no verb changes, which is devbin letter I recreated. wb migrate carries the pre-migration wip.md verbatim as a snapshot through the existing snapshot path, so nothing is lost; anything it cannot carry (a ### sub-heading, a table, a heading's text after the kind word, a board lead) is named as a unit BEFORE any write and the carry is REFUSED, the way WbSendersNotRegistered already refuses, unless an explicit drop flag is passed; prose it carries as an item is reported on a coerced: line and never silently; a .history file left in place gets its own word, not uncarried:. Devbin letter C (empty sections emitted, fixed section order) closes as intended: both are documented renderer rules in views.rs, and its real harm is the pickup gap, fixed separately. Size M, one crate.
- (2026-09-16) vc, under the pen, on cc's grouping of devbin letters J, O, N (store and audit) and H, I, M (identity): (1) a node's name and role are corrected by `intent wb register <moniker> --name --role --correct`, a flag on the one identity door rather than a new verb: registered monikers only (it never creates a node), name and role only, rc 0 reporting the node unchanged when nothing differs, WbRegisteredDifferently's remedy names it with both values, recoverability as register's, its dispatch row, legal pairs and table regen in the same change; hv may respell it before release. (2) Every whiteboard write logs an event, wb touch included, through one store door that stamps updated_at and appends the event in the same transaction; events are not carried into intent/.canon (issues/, project.json, st/), so a logged heartbeat dirties no tracked file.
- (2026-09-16) vc, under the pen, answering cc's replace_boards question for bank 1 (0411, 0415, 0414): sync --to-store APPLIES THE DIFFERENCE BY NATURAL KEY and never carries id or updated_at, so board.json and schema/board.schema.json do not move. replace_boards becomes delete-missing, update-changed, insert-new, the precedent replace_file_index already sets: wb_node keyed on moniker, wb_item on (node, kind, seq), wb_message on (sender, recipient, recorded_at, body) counted as a multiset so two identical rows stay two. An unchanged row keeps its id and updated_at; a changed row is updated through the one store door vc decision 21 (2) rules, stamping updated_at; an inserted row takes the next id in board.json order, which is today's order on a full restore. sync_overwrite lists the whiteboard differences (a node whose fields differ, an item or message added, changed or removed) beside threads and issues, so the no-overwrite line is printed only when it is true, and a run that differs names what it replaced. Size M with decision 21 (2), one crate.
- (2026-09-16) vc, under the pen: when two CHAIN STARTs cross, the moniker earlier in the alphabet goes first (cc < dc < ic < vc) and the other waits for its CHAIN END, with no further messages about order. ic's and vc's starts crossed repeatedly on 2026-09-16 and both sides yielded each time.
- (2026-09-17) vc, under the pen, for ST0076. (1) The brace form `alias MyApp.{Repo, Mailer}`, the `__MODULE__` forms of every directive and `alias :crypto, as: Crypto` are all inside AC-03.1 as written: each directive entry is a level-1 row named by its whole module, by one mechanism in cc's shapes bank (@name.base and @self). Laksa has none of the `__MODULE__` or atom-alias forms (measured) and they stay in, because a row naming the wrong module is a claim wider than the build and the pattern costs less than a published sentence excusing it. (2) WP-03 and WP-04 share the published sentence of what a reference misses: each lander's text is true of the HEAD it lands on, decision 19 orders them, and the second lander rewrites the Elixir gap and level_words(2) with the register regen. (3) WP-05 measures the SCIP export with build scripts and proc macros off as well as on, and the join counts; if the export runs project code it runs only on an explicit verb and never from intentd unasked, and AC-05.1 is reworded to say so. (4) Level 3 is one core written once for both languages: a side table (replace_symbols_for deletes and re-inserts a file's rows, so columns on symbols would lose resolved rows at every re-extract), one join, one schema step, one envelope field for a missing or stale tier and one verb; its contract and its builder are ruled on cc's report. (5) Each train has one whole-suite judging run, in the warm worktree that already holds its stack, and every write to the live store goes through the installed pair. (6) The guards adoption pass is dc's, beside the trawl.
- (2026-09-17) vc, under the pen, on cc's WP-05 measurement report (one default export, one build-scripts-off export and one join on a fresh clone at c800b8319) and dc's Elixir requirements: LEVEL 3 FOR ST0076. (1) WHO RUNS IT: the SCIP export runs the project's build scripts and proc macros and this rust-analyzer has no switch that stops it, so level 3 runs only on one explicit verb for both languages (working name `intent index resolve [--lang] [--full]`; hv may respell it), never from intentd, reconcile or a hook, and it is not offered on the MCP tool tier in this release. (2) POPULATION: every written reference row, not level 2 alone (level 2 alone would store 26,167 of 144,554 Rust matches, and Elixir's unqualified rows are the ones level 3 exists for); AC-05.1 and AC-06.1 say `a written reference`. (3) ONE CORE, built once by cc as WP-05's first bank, which dc's WP-06 stacks on: a side table of one resolved row per (path, line, name, target), existence semantics, no tie-break at write, so a key naming two targets stores both and is counted as ambiguous; the target is one printable name per language with no tool or crate version in it, with the definition's path and line when the index holds it; staleness facts (the file's sha256 at trace, language, run) live once per file and not per row; a run joins only against the bytes the tool read, replaces exactly the files it resolved in one transaction, writes nothing when it fails, and purges rows whose path has left the index; reconcile never deletes resolved rows; one schema step. (4) COUNTS in `intent index status`, per language, from the last run: matched, unmatched, dropped, ambiguous and stale paths. (5) The SCIP decoder is hand-written in intentsvcs as a pure module, fields by number, unknown fields skipped. (6) Toolchain output goes to a directory of Intent's own under intent/.cache, never the project's target. (7) ARMS: decode, join, replace, staleness and counts are proven hermetically from a small checked-in fixture; the absent tool is proven with the tool off PATH; an arm that needs a real toolchain is `#[ignore]` with its reason and is run with --ignored where the tool exists. Whether CI installs Elixir and rust-analyzer on one leg to run those is hv's call.
- (2026-09-17) vc, under the pen, on cc's three questions for the Rust reader, ruled from a read of cc's drafts against the landed resolved.rs. (1) OPERATOR: a reference occurrence whose text is non-empty and holds no letter, digit or underscore is excluded by the reader as `operator`, declared in its roster; rust-analyzer's export puts an overloaded operator's reference on the operator and on the spaces beside it. It is the one word both languages publish for the class (dc's tracer measurement already uses it). The test is on the text and not on being a Rust identifier: a tuple-field index, `Self`, `crate`, `super` and a raw identifier stay in the join and count as unmatched, because those are the toolchain-only references hv's population ruling is about; an empty text is not an operator and goes to the join. (2) CARGO.LOCK: where a workspace root holds no Cargo.lock, cargo writes one there during the export, as any cargo command would; the reader leaves it and never deletes or moves a file in the project tree, no stable switch moves it, and AC-05.1's claim is about the build directory and stands. The sentence is published in `index resolve`'s recoverability_anomaly and the module doc; no detector, no note in the outcome; the fixture crate keeps a checked-in Cargo.lock. (3) THE PRINTABLE RUST TARGET stands as cc drafted it (0 collisions over 12,520 symbols here): `crate::path::Type::method()`, fields and types plain, `<Type as Trait>::method()`, `name!`, `#[Derive]`, `<impl Type>`; the `()` is part of the name because a field and a method of one name are two definitions; an impl's module is the one its block is written in, said in the doc and not canonicalised. ic's near-target refusal therefore compares with a trailing `()` removed as well as a trailing `/arity`.
- (2026-09-17) vc, under the pen, on dc's WP-06 design points and ic's AC-07.2 question. ELIXIR READER: (1) the tracer records call events (remote_, imported_ and local_, function and macro) printed inspect(module).name/arity, a local call on env.module, and module references (alias_reference, alias, import, require) with the module as name and target, plus :start and :stop per file; no other event kind is recorded. (2) Its roster is `expansion`, `operator` and `quoted`. `expansion` is a call or module event whose meta carries no column, checked first: dc measured on Elixir 1.20.4 that every written reference kind carries a column and generated code carries none, and that a written `to_string(x)` traces both Kernel.to_string/1 with a column and the macro's own String.Chars.to_string/1 without one on the same line and name, so the join alone would store the twin; a trace whose reference events carry no column at all is Failed, never a run that drops everything; the doc says the test cannot see a macro that reuses a written node's meta. `operator` is one word with one test for both languages (decision 26), so OPERATOR and its test move into resolved.rs in dc's landing. `quoted` is an imported_quoted event, which names a list of arities and asserts none singly: counted, never a row per arity. (3) A file is read only if it ran :start to :stop and hashes the same before and after the compile; a non-zero exit, a :start with no :stop or a tracer fault record is Failed. (4) A run that seeds or re-seeds the cache forces the compile whatever --full says, because a seeded cache holds the user's compile state and would never trace what the user already compiled. (5) mix.exs is read at the index root only in this release, and NotApplicable's detail and the register's words say so. (6) dc corrects WP-06's body now (ruling 4 struck for AC-06.1's clause), with wp start. AC-07.2 (ic): stored rows answer whatever this build's readers are, and there is one refusal, keyed on the store alone, with the remedy `intent index resolve`; the readers()-dependent wording is dropped and the AC's last clause reworded at 41c8f9ad1.
- (2026-09-17) vc, under the pen. (1) ISSUE 0437 took the strictly narrower pattern, `Result<[^<>]*, *String>` and its Box<dyn Error> twin: cc measured that the old `[^>]*` stops at an inner `>`, so a generic first parameter never fired, and that vc's balanced pattern would strike six existing lines in four files (render.rs three, daemon.rs, graphql.rs, legacy.rs), five of them not violations by the rule's own words; a subset pattern strikes nothing new anywhere, `Result<Vec<u8>, String>` is pinned as not firing so that widening is a decision, and the six lines are in the issue's close note and not filed. (2) WHETHER A LANGUAGE APPLIES IS DECIDED AT ANSWER TIME, NEVER STORED: a search answer names a declared, carried language as unresolved, failed or missing only where the index holds the manifest its reader resolves from (Rust: any indexed Cargo.toml; Elixir: mix.exs at the root), through one seam on the Resolver trait that the reader's own not-applicable test also reads; a run of a language this build does not carry is named as recorded. The core still records a named not-applicable run as failed and index status says so; the answer does not, because no run can ever store there and the alarm would be permanent. This estate declares elixir and holds no mix.exs. (3) The words for unresolved name the verb as a person's (it runs the project's build and is withheld from the MCP tier), never as a next step. (4) AC-05.1's last clause is the run's record and AC-07.1 alone carries the envelope. (5) The all-estate board trawl's first pass is cc's, in scratch clones over the registry's estates, with the frozen v2 checkout named as excluded, read-only store copies checked by mtime, -wal and integrity_check, and a worklist by estate and by class of uncarried unit for hv.
- (2026-09-17) vc, under the pen, on cc's 0438/0439 bank (landed eac280b50 at the judged patch-id 1d3a0d9c, closed 5a0c34c98) and on ic's read of gen_reference.sh. (1) 0438: every hand-authored inbox holding a line the model cannot carry is kept byte for byte at <node>/.history/pre-migration/inbox.<sender>.md beside the board's copy, on disk and in the store's prose; every copy is checked against a file already at its path before any is written; the uncarried refusal names each copy a drop would keep, as a list. (2) 0439: a view Intent rendered is recognised by one predicate in the renderer's home (views::view_body, beside BANNER_MARKER, which finish now pushes itself), asked by both of migrate's readers, which set the banner aside; a view offering no unit once its banner is gone is named on a rendered: line and not kept, and a node whose files are all such views carries nothing at exit 0. A VIEW THAT STILL OFFERS UNITS IS CARRIED AS ANY BOARD IS: a fresh clone has no store, so its rendered views are the only record of its board. That is the one delta from the issue's body, and vc accepted it. (3) THE docs/reference REGENERATION FOLLOWS THE LAST LANDING THAT CHANGES THE REGISTER, NOT THE REBUILD: gen_reference.sh runs no binary and reads surface/dispatch-table.json at a revision (ic's pre-run is banked at refs/bank/ic/refgen-prerun/), so it is ic's commit (ic hold 10) once vc says the register is final, which is after WP-06's dispatch-table hunk lands; its baseline is named on purpose, --baseline v3.0.1, the one the committed pages used, unless hv's release version moves it. (4) vc's own no-columns guard in decision 27 was wrong as ruled and is corrected: every defmodule traces a columnless :elixir_utils.noop/0, so an incremental compile of a module with no written reference records events and no column legitimately; the run fails only where a FORCED compile traced references and none carried a column (dc's bank ac00b98e, the unit arm asserting both directions).
- (2026-09-17) vc, under the pen, on dc's WP-06 judging run and Laksa drive, and on ic's diagnosis of the 0439 canon warning. (1) THE ELIXIR READER READS NO ENVIRONMENT VARIABLE AND ALWAYS BUILDS dev, narrowing decision 27's <env>: AC-11.3's census (intent-cli no_intent_home::the_shipped_surface_reads_exactly_one_environment_variable) went red on its read of MIX_ENV in the judging run of bank c4b326db, and Intent's own build must not change with the shell it was started from; MIX_ENV=dev is set on the child so the build directory's name and the compile's environment cannot disagree, the build is resolve/elixir/dev seeded from _build/dev/lib, and code only another environment compiles (test/support under elixirc_paths(:test)) is not traced; a project that needs another environment asks, and hv grants it with an ALLOWED row. (2) LEVEL 3's COUNTS STAY THE STORED RUN'S CONSERVATION LAW (issue 0440, cc's): an incremental Elixir run that traced nothing printed matched 0 while every row stayed stored; no sum over files can reproduce the counts because a trace's excluded counts carry no path, so status says what the stored run covered, `joined K of F files` from resolved_file.run, with no schema change. (3) A TOOLCHAIN-ONLY REFERENCE JOINING A WRITTEN ONE ON THE SAME LINE AND NAME (Access.get beside Map.get, to_string from interpolation) and A TARGET'S PLACE BEING THE RUN'S (an incremental compile leaves placements in files it did not recompile) are ST0077's, recorded in its context with dc's Laksa numbers; both are consequences of the ruled key and staleness model, both degrade honestly, and WP-06's body names each in one sentence. (4) ISSUE 0441 (ic's): intentd's ingest pass must commit its files only if no other connection committed since its snapshot, judged by PRAGMA data_version, with the check, the file commit and record_landed under one hold of the store's write lock; a moved store re-renders at most three times, then a logged line names the subjects not written; the command-line side's check, store commit and file commit are confirmed to hold the lock the same way or fixed in the same bank; no narrowing; one arm with a seam. The command-line side stays as it is (a sync in its post-commit gap either discards its render or renders the write's own rows byte for byte), with two writers crossing on one subject named in 0441's close note as a residual, and `Load::Restore` takes no guard. (5) THE TRAWL'S LIVE CARRY DOES NOT WAIT ON 0441: intentd's registry (one line, {"root":"/","op":"registry"}, to its socket) names every estate it holds open, and an estate that reads not watched before its carry and before the carry's commit, with ingested unchanged, had no sync that could revert a board (ic's probe; only this estate was watched when ic drove it).
- (2026-09-17, after hv's compact) vc, under the pen. (1) ONE REBUILD CARRIES EVERYTHING: the rebuild waits for 0441's landing rather than preceding it, so level 3's surfaces, the Elixir reader, the counts, both migrate fixes and 0441 install together and the 3 s spacing rule retires with it; had the bank gone red the rebuild would have gone first without it. (2) docs/reference IS ONE COMMIT OF BOTH HALVES AT ONE REVISION: cut-surface.md is gen_cut_surface.sh's, not gen_reference.sh's, and it reads ALL_VARIANTS in intentsvcs/tests/error_remedies.rs at the revision, which 0441 moved, so the regeneration followed 0441's landing (d21550ae5 at 98c580d12, 21 pages, prettier clean). Between cuts the pages name a dev revision, which is the practice ic measured; at the cut both halves are regenerated at the TAG against the previous release, and ic writes that step's durable home beside the notes at the cut, because it survives in the commit record alone. (3) ISSUE 0442 IS cc's (dc's find: the live store's FTS5 index for src_sections malformed, a plain search failing on an affected term, doctor and index status both reading clean over it): it answers the cause with evidence before any change, the fix that stops it being produced, detection, and whether a plain search is a write path by design or only in its error's wording. (4) THE LIVE REPAIR RAN BEFORE THE REBUILD, intent index rebuild at 18:16Z through the installed pair, because the restart re-indexes the files 0441's landing changed and would write into the damaged index; integrity is ok on a page-exact copy and the failing search exits 0, and a repair is not a fix. (5) THE NEXT RELEASE'S NOTES ARE cc's, banked and not landed until hv names the version: bin/devbin build release refuses a cut with no '## [X.Y.Z] - in progress' section in CHANGELOG.md, and nothing has been written since v3.0.3.
- (2026-09-17, vc under hv's pen, answering dc on the guards adoption pass) THE INLINE FORMATTER STANZAS ARE NOT EXTRACTED INTO DECLARED GUARDS, and the reason is a category error rather than a preference: a guard JUDGES and refuses, a formatter WRITES. Declaring a formatter as a guard puts a second writer behind a door whose name promises a check, and this estate has already paid for that class once -- the markdown formatter auto-aligning tables on save, between sync and commit, produced a real canon/disk divergence that was hard to see precisely because nobody expected a writer there. If there is a case for moving them it is a case for a `formatters` declaration that says what it does, and that is a design question for after the close-out rather than a rider on the guards pass.
- (2026-09-17, vc under hv's pen, adopting dc's route after dc refused the one vc proposed) AN ATTACHMENT IS EDITED AT ITS OWN PATH AND ATTACHED FROM THAT SAME PATH, AND THE FILE IS COMMITTED WITH ITS CANON TOGETHER. `intent st attach <ID> <name>.md --from <that same path>` then makes the store, canon and disk converge in one move, and no realisation verb is involved at all -- which matters because the realisation verb would pin (see vc watchout 20). Writing the draft to a scratch file and attaching from there also works and leaves the tracked disk file holding the old text, which is drift in the other direction. Driven on ST0076's `ac-06.2-drives.md` at `b9cf92502` with all four readings taken either side: disk sha moved to the draft's, canon sha moved, `.intentfiles` stayed at zero lines for the thread, doctor stayed at 0 findings. **`st attach` does NOT pin; the pin is `hydration`'s alone.** dc carried this rule from ST0074's `design.md` going out of step, which is why dc reached it and vc did not.
- (2026-09-17 21:40Z, vc under hv's pen, correcting vc's own transcription forward) THE BASELINE COMMAND NEEDS --tags AND hv's RULING IS UNTOUCHED. hv ruled "3: Tag" -- the docs/reference baseline is the previous release tag -- and vc rendered it as `git describe --abbrev=0` in hv board decision 25, in this board's line 28 and in intent/wip.md, from where ic wrote it into intent/docs/releasing.md twice. Driven independently by vc and by ic at 21:35Z: the bare form answers v2.3.2 and the --tags form answers v3.0.3. `git describe` reads ANNOTATED tags only unless --tags is passed, this repository has 56 tags of which 6 are annotated and all 6 are v1.x or v2.x, and every v3 tag is lightweight because bin/.devbin/cmd/build.d/release:1221 is `git tag "$TAG"` with no -a. Reachability is not the cause; the default tag filter is. THE CONSEQUENCE, traced rather than assumed: surface_resolve_rev resolves v2.3.2 but surface_table_at then fails because surface/dispatch-table.json does not exist before v3.0.0, so gen_reference.sh:136 prints one note to stderr, clears BASE_SHA and ships pages reading "none -- no register at the baseline" in the one column those pages exist for. Nothing landed is wrong: both prior regenerations passed v3.0.1 by hand, so the ruled command has never been run, which IS the defect. THE CLASS, third instance in one night after vc's FTS5 cookie reading and ic's grep-hits-as-control-flow: a command written into a durable document is a claim and gets driven like one. ic argued in that very page that a command beats a sentence because it cannot drift with anyone's reading, wrote it in on that basis, and did not run it -- AN UNRUN COMMAND IS A SENTENCE IN A MONOSPACE FONT. ic also found the page's OTHER check command broken (a table-cell boundary defeats the -o pattern; the working form is `grep -rh 'reported against' docs/reference/ | sort | uniq -c`, reporting 19 pages at v3.0.1), which is the same defect twice in one page and the strongest argument yet for ic's banked staleness check, since a check computing its own baseline would have failed loudly on the missing register the first time it ran.

---

_Generated by Intent v3.0.3 from the whiteboard model. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
