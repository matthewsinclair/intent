---
node: vc
name: Validation Claude
role: validation
session_id: 1aa05d4a-6da2-4c42-98c6-de024aebab69
heartbeat_at: 2026-08-31 18:21Z
status: active
focus: "FOLDED 2026-08-31 18:11Z, pre-fold sha-verified at .history/20260831/wip-fold-1809Z.md. STATUS STAYS active -- a compact does not end a session. THREE THINGS WAIT ON hv AND TWO ARE ONE FLAG ON A SHIPPING VERB. I was corrected four times today and every correction was a peer measuring instead of complying; three of the four were claims I had relayed or reasoned onward without measuring. RE-RUN EVERY FIGURE."
claims: [ST0056, ST0057, ST0060, ST0064, ST0068]
---

# Validation Claude (vc)

**PROJECT-WIDE RULES IN `intent/restart.md`. Pre-fold bodies and every incident narrative in `.history/`. What follows is MECHANISMS; the stories are archived on purpose and this section has drifted back into narrative twice, so cut it again when it does.**

## DOING

### THE CUT -- RUN THE VERBS, NEVER READ A FIGURE OFF THIS BOARD

`intent ac gate <ST>`. **And `intent --version` names the commit the binary was built from, which is not HEAD**: four sessions commit into one tree, so the pair goes stale within minutes of anyone's push and a rebuild is a shared-artefact window someone has to own.

### `AC-00.16` -- SIDE C LANDED, AND THE ROW IS RED FOR A NEW REASON

`bbcae759`. The per-property union exists, derived from the register's AC/AT pairing. **What keeps it red is that the criterion's first conjunct is NOT MACHINE-DECIDABLE**: three calibrated spellings each failed in a DIFFERENT direction over 178 files. **The fix is a STRUCTURAL declaration and that is an amendment, so it is hv's -- mine would make my own red row greenable.**

## TODO

**Items 1 and 2 are two of my ten thread-level criteria; the rest are not on the cut's critical path and yield when they compete.**

1. **`AC-00.11`: decide whether cc's width-arm green wants its own criterion.** Its population is the TEST corpus, not the measured one. `AT-00.11` is `to-write` and the move is one-way.
2. **`AT-00.2` (fleet corpus) and `AC-00.8`/`AC-00.10`** -- all three sit behind cc's WP-10, which sits behind WP-06 (cc) and WP-07 (dc). **Read off WP-10's own cover by cc, not inferred.** Sequence around it rather than in parallel.
3. **THE THIRD ABSENCE CLASS IN `capability_ship_check.sh`** -- it carries dangling-symlink, not-on-PATH and no-daemon, and NOT _a build ran and left nothing behind_.
4. **WP-15's four criteria have NO OWNER** (`Skills catalogue triage`, L, Not Started) -- hv's to assign; **`parity/register.md`'s 39 stale rows** (GENERATED, the template is the fix); **`0136`'s ~44-site change** (hv ruled: after the tag); **`intent/wip.md`** before the tag.
5. **`design.md:88` still says `rmcp (official SDK): stdio ... now`**, which my ruling reversed. Design prose is hv's hand -- a flag, not an edit.

## Holds

**Each carries the CONDITION that releases it. A hold with no condition is an abandonment.**

- **`AC-02.1` cannot be satisfied against the keg.** **Condition, now precise rather than vague: `--note` lands on `at green` and `at na`.** Not "until v3.0.1 ships" -- one flag, two verbs. Honestly red until then and NOT to be scored against the tree to make it green.
- **The `dvb build all` delete-then-fail hazard is UNFILED.** Held by cc, who has the run. **Condition: cc files it.** It deletes the binary pair to force the provenance embeds and does not restore them if the build then fails, against a shared artefact four sessions consume.
- **dc's manifest derivation is not built.** **Condition: hv sequences it.** dc will build it and has deliberately not started -- they had grown enough surface today, which is the right instinct in a tag window.

## hv items

**THREE THINGS WAIT ON hv AND TWO ARE ONE FLAG ON A VERB THAT ALREADY SHIPS. All measured by me on both binaries.**

1. **`--note` on `at green` and `at na`.** Keg declares neither; tree declares both. **That flag is the entire difference between `docs/getting-started.md` terminating RED and GREEN on what a user installs today.** dc's finding, twice.
2. **`--title` on `issues edit`.** Ships with `--body`, refuses `--title`. Closes `0151`, completes what `0154` asked for. dc's.
3. **`AC-00.16`'s AMENDMENT** -- structural reach declaration rather than lexical. Mine, and mine is exactly why hv rules it.

**AND hv's 0192 RULING OF 15:54Z HAS A DEAD PREMISE** -- the read-back is wired, driven twice independently. Recorded on hv's board ABOVE the ruling, never inside it. **I supplied that premise on a measurement `st show` could not make.** Awaiting hv's re-decision; the real defect is narrower and is dc's to rewrite.

## Standing directives from hv

- **WATCH THE RUST FOR HIGHLANDER, THIN COORDINATOR AND PFIC on every workstream review.** A posture, not a gate.
- **THE MENUBAR ICON IS THE INTENT TURTLE**, state DERIVED at paint time.
- **FULLY SHIP v3 WITH ALL FUNCTIONALITY. intentd is a priority. Then tree-sitter and full search. Push.**
- **DO NOT REINVENT THE WHEEL** -- port from `../Gtools`, `../Conflab`. **And read the thread's own attachments first.**

## Watch-outs

**MECHANISMS ONLY.**

1. **AN INSTRUMENT ANSWERS A DIFFERENT QUESTION THAN THE ONE ASKED AND ITS OUTPUT LOOKS LIKE AN ANSWER.** The proxy nearest to hand stands in for the subject. **CONVERGENCE IS NOT CORROBORATION WHEN BOTH METHODS SHARE A DEFINITION** -- and the shared definition can be an INTUITION rather than a method, in which case agreeing witnesses corroborate nothing. **A PROBE THAT CANNOT EXHIBIT THE FAILURE RETURNS THE NUMBER THAT MEANS SUCCESS**; positive-control the instrument and write the prediction down first. **A CONTROL CAN BE UNABLE TO FAIL.** **A ZERO WITH NO PLANTED POSITIVE IS A SILENCE.** **A COUNTER THAT BECOMES ABLE TO FIRE ONLY WHEN THE WORK FINISHES SAYS PASS AT THAT INSTANT** -- drive an instrument against the future it demands, not its current state. **A DEFECT CAN HAVE A GUARD ASSERTING IT**, consuming the search that would have found the hole. **A RECORDED PROOF IS PROSE UNTIL SOMEONE TYPES IT**, and its correct steady state is silence, so nothing reports the rot.

2. **AN ABSENCE NEVER LOOKS LIKE A BUG IN THE QUERY.** A partial read that finds SOMETHING self-corrects; one that finds NOTHING is unfalsifiable without redoing it. **Output to a FILE, then count.** **A change's blast radius is not the crate it lives in.** **AND I SUPPRESSED A COMMIT'S ERROR OUTPUT FOR TWELVE ATTEMPTS** reading a prettier refusal as a lock contention, because `>/dev/null 2>&1` in a retry loop turns every distinct failure into the same silence.

3. **A CLAIM OUTLIVES ITS BASIS AND NOTHING WATCHES THE JOIN** -- and the arrow reverses: the basis can POSTDATE the subject. **A GREEN ROW WHOSE TEXT OVERCLAIMS IS WORSE THAN A RED**, because a green closes the criterion. **The repair splits on WHY: rewording to match an unfinished build is the defect; rewording because a RULING moved is the correct repair.** **AN AC REWORD THAT LEAVES ITS AT BEHIND WEAKENS THE CRITERION** -- the gate reads the AT. **A RATIFICATION WHOSE SUBJECT WAS RETIRED IS SUPERSEDED, NOT UNAPPLIED.** **AN ERROR INSIDE A JUSTIFICATION FOR A DECISION THAT IS CORRECT ANYWAY HAS NO NATURAL CORRECTIVE** -- it surfaces only when the next thing is priced against it. **A DRIVE'S ANSWER DEPENDS ON WHICH TREE IT MET AND `I RAN IT` DOES NOT SAY WHICH**; a keg claim carries its tree the way a count carries its denominator.

   **3g. STALE IN THE PESSIMISTIC DIRECTION IS THE ONE NOBODY GOES LOOKING FOR.** Three instances now in one neighbourhood of hv's board -- the daemon, ST0064's release scope, GraphQL -- each understating what shipped. **Structural, not careless: a board that OVERSTATES gets caught by the first person who relies on it, and a board that UNDERSTATES never prompts anyone to check.** hv reads that board on restart.

4. **THE SHARED TREE MAKES ORDINARY OPERATIONS MEAN SOMETHING ELSE.** `git commit --only <explicit paths>`, **which is PATH-scoped, not hunk-scoped**; re-pin at commit time and **post-verify against `git show --name-only` EQUALLING your intended set**, never against your own staged list. **A HEAD-pinned private index INVERTS contention rather than closing it** -- it discards a peer's bytes silently. **Coherence is not closeable by the committer: land the pair or wait.** **A GUARD WHOSE POPULATION IS THE WORKTREE OR THE INDEX MAKES EVERY NODE'S COMMIT DEPEND ON EVERY OTHER NODE'S UNCOMMITTED STATE**, and the mirror: a defect existing only in the commit has no witness but a clone nobody has made. **AN ABSENT CITATION IS TOLERATED AND A PRESENT ONE IS JUDGED**, so CREATING an artefact turns a tolerated row into a violated one. **AN ATTACHMENT IS AUTHORED AND NO SYNC DIRECTION REWRITES IT** -- `intent st attach`, then `sync --to-disk`, then commit file AND extract together. **AND NEVER REMOVE AN INDEX LOCK; RETRY.**

5. **A CRITERION IS OWNED BY WHOEVER CAN SATISFY IT AND MUST BE ABLE TO FAIL.** Unfalsifiable forms: an unbounded set; a checklist of page names; a count of a kind the instrument does not count; a criterion restating its source; a bar satisfied by ONE MEMBER OF A GROWING SET. **A PARTITION DERIVED FROM ITS PARTS CLOSES BY CONSTRUCTION.** **A TOKEN THAT SPANS THE ACCEPTABLE AND UNACCEPTABLE CASE CANNOT DISCRIMINATE.** **A MEMBER THE PROPERTY CANNOT BE EVALUATED OVER IS EXCLUDED WITH ITS REASON NAMED AND THE DENOMINATOR MOVING VISIBLY** -- a silent drop is the denominator attack. **THE AUTHOR OF A CHECK IS NOT A SAFE SOURCE FOR THAT CHECK'S OWN DENOMINATOR.** **A DISCRIMINATOR CAN BE RIGHT ABOUT THE SYMPTOM AND WRONG ABOUT THE PROPERTY, AND THE WRONG ONE SOUNDS LIKE A SAFETY RULE** -- ask what the CHANGE touches, never what the CALLER meant. **`closed` DOES NOT MEAN FIXED AND `open` DOES NOT MEAN BROKEN.**

   **5f. A CRITERION DERIVED FROM A REGISTER ANYONE CAN ADD TO RECEDES AT THE SPEED YOU WORK** (dc). Filing ENLARGES the population, so a node that files without dispositioning pushes its own criterion away from itself. **Not a wrong-M variant -- the M is right every time.** Converges only because the population is every defect open AT THE CUT, so it stops moving when filing stops. **Cure is procedural: file and disposition in one act.**

6. **MAKE THE BAD STATE UNREPRESENTABLE; WHERE YOU CANNOT, WITNESS THE MECHANISM.** **A predicate is sound only relative to what is DONE with the answer.** **AN EXEMPTION IN A CONTROL-FLOW BRANCH IS THE PROSE VERSION WITH WORSE VISIBILITY.** **A ONE-SIDED WITNESS PASSES UNDER THE VERY THING IT REPLACED.** **WIREDNESS MUST BE DERIVED OR DRIVEN, NEVER A HAND-MAINTAINED FLAG** -- and a hand-maintained INDEX of work done elsewhere is the same defect: doing the substantive half does not register, because the instrument reads the index and not the work.

7. **THE FAILURE PATH IS THE ONE THAT MUST STILL WORK AND A GREEN RUN NEVER EXERCISES IT.** **THE UNIVERSAL SAFE PROBE CAN HAVE A SIDE EFFECT** -- and its sharpest form is **A DEFECT WHOSE VERIFICATION REPRODUCES THE HARM**: `intentd --help` starts a real daemon and takes every session on the machine down, so confirming it costs what it documents. **Use a static instrument and SAY IN THE ENTRY which one you used**, with a control both subjects carry so an unreadable subject cannot masquerade as an unfixed one. **A TEST HARNESS THAT DOES NOT ISOLATE `HOME` REACHES THE REAL MACHINE, AND THE FIXTURES WERE GUARDED WHILE THE FRONT DOOR WAS NOT.**

8. **vc's OWN.**

   **8a. SHELL QUOTING EATS CONTENT AND THE COMMAND STILL SUCCEEDS.** No word-splitting under zsh; an unmatched glob aborting the whole call; an apostrophe inside a single-quoted program being a parse error that runs nothing; **`$?` after a pipe reading the last stage**; a backtick inside a double-quoted string running a command. **The dangerous case is the word that IS a command.** Write messages to a file and use `-F`. **The no-split trap can land NOTHING** -- an empty action that looks completed. **AND A DATA BLOCK PARSED BY TWO MECHANISMS BREAKS ONE AND NOT THE OTHER, IN SILENCE.**

   **8b. I RULE AGAINST STATES THAT HAVE ALREADY MOVED.** A message states HEAD at SEND time and is read against HEAD at READ time. **RE-MEASURE BEFORE RULING. When a ruling's premise dies, SAY SO AND REVERSE IT** -- done twice today, on `at lint` and on `0192`, both times because a peer re-drove rather than complied.

   **8c. A ROW MOVE CAN BE IRREVERSIBLE AND THERE IS NO UNDO VERB.** `to-write` is an exemption; `red` requires the cited artefact to EXIST. **The repair is the ARTEFACT the row was owed, never a label**, and the documentation owes the operator a ROUTE.

   **8d. REPORTING IS NOT ROUTING**, and never perform an action a peer's session was denied. **AND I MEASURE AGAINST STALE BINARIES.**

   **8e. A RULING IN TRANSIT IS A RULING NOBODY HAS.** **Execution status is the discriminator, never the date** -- a fold keyed on DATED buries live TODO items. **A live directive reachable only by grepping `.history/`, or living only in a peer message, is discoverable by nobody -- AND SO IS ONE ON A BOARD THE OTHER NODES DO NOT READ.** dc spent a sandboxed drive and a Cellar listing rediscovering the keg-ships-no-skills gap that was recorded, with its fix, on hv's board. **BEFORE PUTTING A QUESTION TO hv, CHECK WHETHER hv HAS ALREADY ANSWERED IT.**

   **8f. I RELAY CLAIMS I HAVE NOT MEASURED, AND IT IS MY MOST PERSISTENT DEFECT.** Four peers have now caught me. **Reporting a peer's finding across a boundary is right; repeating it as FACT is the defect -- and REASONING ONWARD from it is worse**, because the conclusion outlives the correction. I told cc that `0063` never needed `close --note` because it was one of three recoverable bodies; the scope half was mine and right, the recoverable half was cc's and wrong. **Every node that declined to act on a relay from me was right to.**

   **8g. EVERY NODE COMMITS AS THE SAME PERSON AND PROVENANCE IS CARRIED BY PATH, NEVER BY AUTHOR.** The one field built to answer _who wrote this_ is unreadable by design. **The register has the same hole -- every issue reads `reporter: Matthew Sinclair`.** **AND THE PATH METHOD ONLY WORKS WHEN THE FILING COMMIT TOUCHES THE AUTHOR'S OWN DIRECTORY**; where it does not, the issue's own prose carries what the metadata cannot, because we write _Found by <node>_ out of habit. A convention nobody designed as an instrument was the only one that answered.

   **8h. A REVERT LANDS IN THE TREE AND TAKES EFFECT IN THE ARTEFACT, AND A BUILD NOBODY SCHEDULES SITS BETWEEN THEM.** Any commit's effect on what a peer DRIVES is deferred until someone rebuilds, and the person who lands the fix is rarely the person who rebuilds.

   **8i. A CLOCK VALUE GOES IN ONLY WHEN `date -u` IS IN THIS TURN'S TOOL OUTPUT, VERBATIM.** A time read off any tool that prints local (`ls`, `stat`, `git log`) is that tool's zone, so appending `Z` is an ASSERTION rather than a format. **Three nodes made this exact error in one hour and every one was caught by another node rather than by a guard**; the messages channel carries the same ordering claims under no check at all (`0099`).

   **8j. I MOVED FROM A TRUE CONCLUSION TO A FALSE ONE BY REASONING BETTER.** The lesson is not _trust the calm reading_ -- storing that leaves the opposite error. **RE-MEASURE WITH AN INSTRUMENT THAT CAN ANSWER**, and never draw a conclusion from a pattern that has just proven it cannot.

   **8k. I ADDRESSED THREE OF FOUR MESSAGES TO THE WRONG NODE.** I held a correct derived mapping and composed each BODY from it while typing each ADDRESS from intention, **so nothing joined the two inside a single call** -- the first one's own summary named the right node while its address named another. **Cure: pin the mapping to a FILE and read the address off the file rather than off the intention.**

   **8l. A MENTION IS NOT AN INSTANCE, AND I COMMITTED THIS THREE TIMES IN ONE DAY.** Bare `reach` matching _reach for `sync`_ inside the census built to catch it; cross-references counted as statements against dc's manifest an hour later; **and a bolded-lead predicate over a page with one section where a bolded lead means the OPPOSITE** -- `## Recorded against v3.0.0 and NOT present in it`. **All three are a population nobody partitioned.** dc found the third by asking which SECTION, which is the question I never asked. **The general form: before counting matches, ask what ELSE produces this token.**

## Decisions

**Standing rulings. Dated narratives are in `.history/`; every entry here was EXECUTED before it was archived, and an UNEXECUTED ruling never leaves this board.**

- **THE MCP RULESET.** MCP tools call the FACADE, never the CLI dispatch arm. A parent row is a namespace, not a verb. The test for exposure is NEED, not provenance. No exemption class -- **a vocabulary reserved for a population that measures EMPTY is STRUCK.** `exposed_on_mcp` refuses on absence. The tier serves in-process only in 3.0.0. Direct facade call now, `dispatch(op)` as the 3.x destination. **A tool that can only mutate inverts the safety gradient.** SERVED implies there is a CLI read to agree with.
- **`close --note` IS NOT BUILT -- THE CAPABILITY ALREADY SHIPS IN TWO VERBS.** `issues edit --from <file>` then `issues close <id>` IS that act; I used it on `0087`. **A schema change plus a store migration to collapse two working verbs into one buys keystrokes.**
- **AN ISSUE'S AUTHOR DISPOSITIONS IT.**
- **A CRITERION THAT COULD FORCE SCOPE IS WRITTEN AS AGREEMENT, NOT COVERAGE.**
- **A DELETE HAS THREE POPULATIONS AND EVERYONE ASKS ONLY THE FIRST:** what EXECUTES this; what CITES this as evidence -- **and its population is the STORE, not the tree**; what CHECKS this.
- **AN IDENTIFIER IS ONLY UNIQUE WITHIN ITS SCOPE, AND EVERY BOARD WRITES IT BARE.**
- **A SECOND HOME IS NEVER ACCEPTABLE AT A TAG.** The escape is not _accept two homes_, it is _need less machinery_.
- **A CITATION'S AUTHORITY COMES FROM ITS MEMBERSHIP RULE, NEVER FROM ITS NAME.**
- **DERIVED CENSUSES MULTIPLY FREELY; AUTHORITATIVE COPIES DO NOT.** The cure for a lossy projection is another projection, not another source of truth.
- **THE CURE FOR THE PLAUSIBLE-LOOKING-HOME CLASS IS A DECLARATION THE BUILD RE-ESTABLISHES.**
- **A TEST GOING RED BECAUSE A FIX LANDED IS THE NOTIFICATION WORKING.**
- **THE DAEMON'S PUBLISHED PORT SERVES BOTH PROTOCOLS, DISAMBIGUATED AT BYTE 0.**

## Coordination -- 2026-08-31 18:11Z

**NOTHING IS OWED BY ME TO ANY NODE.** Every inbox empty; peers talk over SendMessage.

**cc** -- drove `0192` to ground and killed their own blocker; built `generated_views_are_unaddressable.rs`, which I reviewed rather than accepted (it does not import `VIEW_NAMES`, so the assertion is not circular). Took the rebuild window because their commits made the pair stale. **Corrected my `0063` reasoning and their own figure three times, each by measuring.** Holds the `dvb build` filing.

**dc** -- withdrew the `at lint` filing I ruled for, after re-driving it. Found `--note` and `--title`, both now hv's. **Corrected my `nothing catches it` framing before it reached hv**, and found the section that made three of my four manifest divergences unreal. Wrote my two disposition rows when my docs work did not register. Will build the derivation when hv sequences it.

**ic** -- gave the ST0064 partition that replaced my 0/9: **0/9 VERIFIED is right, 0/9 BUILT is wrong, and a gate cannot tell them apart.** That is a property of every `0/N` I quote. Owns the 21 MCP dispositions and green-lights cc, not the reverse.

**STATUS STAYS `active` THROUGH THIS COMPACT.**
