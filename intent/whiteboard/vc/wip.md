---
node: vc
name: Validation Claude
role: validation
session_id: 1aa05d4a-6da2-4c42-98c6-de024aebab69
heartbeat_at: 2026-08-30 00:47Z
status: active
focus: "Criteria queue CLEAR and EIGHT RULINGS ISSUED while hv is AFK. ST0064 is nine criteria and rescoped L on ic's Geodica pricing; AC-08.3 split into client + AC-08.11 daemon on cc's finding that vc had put a daemon obligation in a client row. hv's standing Rust review job has paid twice: the `num_args` duplicate and the `.ok()` swallow family at four sites. Gate open on both arms; suite green."
claims: [ST0056, ST0057, ST0058, ST0060, ST0064, ST0066, ST0068]
---

# Validation Claude (vc)

**PROJECT-WIDE RULES IN `intent/restart.md`.** Pre-fold bodies in `.history/20260829/`; seven folds that day.

## DOING

**THE PEN, while hv is AFK.** vc specifies, adjudicates and reviews; cc, dc and ic make it work. A workstream with a question posts it with options; anything vc and the asker cannot resolve waits for hv.

**COMMIT SEQUENCE, TWO OF THREE DONE.** ic landed `form.rs` alone at `771cc847`; dc landed ST0066 at `587105cb`; **`render.rs` is still uncommitted, so dc has not taken it yet**, and cc lands `lib.rs` + `daemon.rs` + `userstate.rs` + `cli_routing.rs` last. `lib.rs` declares `pub mod form` (ic) and `pub mod daemon` (cc) and can only land when both sources exist.

## OPEN

1. **OWED BY ME:** the four missing `data-model.md` property tables (`Attachment`, `Legacy`, `Related`, `Envelope`) -- **asked dc whether they want them as text or would rather write them**, because that file is dc's claim and they are mid-commit. Nothing else on the criteria queue is outstanding.
2. **SUITE, MEASURED AT `cd1bf7d7` AND UNCHANGED SINCE:** `cargo test -p intentsvcs` rc=101, **one failure** -- `a_machine_ratified_in_prose_is_actually_trivial`, `left: 2 right: 1`. **The ruled Machine 5 trigger firing as hv ruled under D1(a)**, dc's own instrument catching dc's own commit. Not a regression.
3. **FOR hv, NOTHING BLOCKED ON IT:** the WP-15 menu (options + recommendation on `hv/wip.md`); `0143`'s `--skip-settings`; ST0065's three questions.
4. **HANDED TO cc, NOT MINE TO FIX:** `userstate.rs` says the whole of Intent's per-user state is one directory an operator can inspect or delete, and D19 puts the daemon's logs and plist under `~/.local/share/intent/`. D19 wins; **the sentence is false whichever directory it is read as naming** and it is in cc's commit already.
5. **HANDED TO ic:** two empty orphan directories in `~/.claude/skills/` from the `in-start` / `in-next` retirement, and whether that is `0078` with a body or `0078` plus a second issue. **Not removed by me** -- outside the repo, and the last two writes there each needed hv's explicit authorization.

## Standing directives from hv

- **(2026-08-30, hv direct) WATCH THE RUST FOR HIGHLANDER, THIN COORDINATOR AND PFIC, FRONT OF MIND WHEN REVIEWING THE OTHER WORKSTREAMS' WORK.** Not a gate and not a new WP -- a posture on every review vc does. **First application, same night: `spine.rs` writes `num_args` twice on one `Arg` thirty lines apart**, once from the `...` in a placeholder string and once from `dispatch::arity_bounds`. Benign only because the two carriers have disjoint populations today, and vc's own arity ruling ends that. Told ic, with the fix as a DELETION rather than a test.
- **(2026-08-30, hv direct) THE MENUBAR ICON IS THE INTENT TURTLE.** Already carried by `AC-01.8` -- `docs/design/intent-logo.*`, with the state semantics DERIVED at paint time and no cached `lastKnownState`.

## Watch-outs

**Mechanisms only. Incidents are in the fold archives.**

1. **A TRUE RESULT FROM AN INSTRUMENT THAT COULD NOT HAVE ANSWERED DIFFERENTLY.** The dominant class. Positive-control the INSTRUMENT, not the subject.
2. **A PREFIX MATCH FALLS BACK TO A SHORTER VALID PREFIX AND REPORTS THE BAD CHILD AS CLEAN.** Tonight's instance and the sharpest yet: a verb-staleness sweep over 23 skills returned ZERO, and the planted control caught **1 of 3** -- `intent st frobnicate` resolves to `st`, `intent modules list` resolves to `modules`. The zero was real only after the checker gained an arm asking whether the NEXT token is a legal child. **Same family as `grep -c arity` returning 134 hits on `parity`.** ic's sweep of the same corpus drove `--help` per invocation and does not have the hole: **two instruments agreed and only one had earned the agreement.**
3. **A GATE THAT REPORTS ON A COMMIT CANNOT BE EXERCISED BY A WORKING TREE, AND THREE INSTRUMENTS DEFAULT THE WRONG WAY.** `machine_table_check` and `canon_commit_check` read INDEX-else-HEAD; run bare they compare HEAD to HEAD and print a pass. `--staged` is the flag that makes the canon check answer about your commit; `DOC=`/`RS=` let the machine check read the worktree without touching the shared index.
4. **A BLOCKED COMMIT STILL RUNS THE FORMATTERS, SO A REFUSED COMMIT IS NOT IDEMPOTENT.** Re-sync before retrying. The formatter is a second writer between sync and commit and a refusal is one of the moments it writes.
5. **PROSE WRITTEN INTO A RENDERED VIEW IS DESTROYED SILENTLY BY THE NEXT SYNC.** The ruled path is the EXTRACT. Executed by the node that minted the criterion warning about it.
6. **A CORRECT ORDER OF OPERATIONS DOES NOT SURVIVE A VERB THAT DOES NOT REACH AS FAR AS THE PLAN ASSUMES.** ic sequenced uninstall BEFORE the canon delete precisely so no intermediate state could strand a skill; uninstall unlinks contents and leaves the DIRECTORY, so two orphans exist anyway. **And the reporter cannot see them: `claude skills list` enumerates the canon roster, so a directory canon has forgotten is invisible by construction** -- the `0115` shape.
7. **A CLAIM OUTLIVES ITS BASIS AND NOTHING ANNOUNCES IT.** True when measured, false when sent, with nothing in the wording saying which.
8. **RELAYING IS AUTHORING.** `fn fc`: three nodes independently converged on the same wrong owner. Not carelessness three times -- a property of a file nobody can see whole.
9. **A RULE FAILS IN THE ARTEFACT THAT STATES IT.** Nine instances. Newest: WP-15's objective states its denominator as 25 and 26 in consecutive sentences, and `AC-15.1` -- which refuses a count carried from when the WP was written -- is on that same WP.
10. **A CRITERION THAT RESTATES ITS SOURCE INSTEAD OF CITING IT ROTS SILENTLY.** All four caught by the node building against it. **The mirror is worth as much: `AC-15.3` was minted before anyone knew a retirement would strand a directory, and it is the row that caught it.**
11. **A CRITERION CAN BE TRUE OF A KIND BY THAT KIND BEING ABSENT FROM THE INSTRUMENT.** ST0066 `AC-00.4`: the thread gate never counts WPs, so _the gate counts a fiat-closed WP as closed_ held because it counts zero WPs either way. **A row of the form `X is counted as closed` needs a pair of counts across the change, or it is unmeasured rather than passing.**
12. **AN UNBOUNDED SET IN A CRITERION IS AN ABSENT CRITERION, NOT A STRICT ONE.** ST0068 `AC-02.3` said _every defect a reader can hit_; it can be neither satisfied nor refuted. Derived from the open-issue register (72 of 128) with a disposition per member, it can.
13. **A CHECKLIST OF PAGE NAMES CANNOT FAIL WHILE ITS PAGES ARE PRESENT AND WRONG.** ST0068 `AC-02.1`. The journey is the claim; an install page written by someone already installed cannot discover what it assumes.
14. **`$?` AFTER A PIPE IS THE LAST STAGE'S.** Fourth firing, mine.
15. **A TWO-WAY COMPARISON CANNOT ATTRIBUTE A DIFFERENCE; THE FIX IS A THIRD INPUT.**
16. **A CAPABILITY THAT WORKS AND IS UNLABELLED IS INDISTINGUISHABLE FROM ONE THAT DOES NOT EXIST.**
17. **THE ONE ACT EVERYBODY BELIEVES IS READ-ONLY MUTATES SHARED DURABLE STATE.** `cargo test` runs the migration ladder on the live root. **And _no downgrade_ is not _unrecoverable_: the remedy is forward, 58 seconds.**
18. **A SYNC WELDS A PEER'S FILE INTO YOUR COMMIT, AND THE ORDER IS NOT A PREFERENCE.** Sync FIRST, commit together -- committing first leaves THAT commit divergent forever, because the criterion is a property of every commit rather than of HEAD.
19. **A HUNK-SCOPED COMMIT EXISTS AND NOBODY KNOWS IT.** Replay onto HEAD's version, `git hash-object -w`, commit from a private index. **Ordering is better when the queue moves; this is for when it does not.**
20. **DESIGN AGAINST THE LARGEST REAL SUBJECT.**
21. **TWO WORK PACKAGES CAN BE ONE JOB AND NEITHER ONE SAYS SO.** `ST0056/WP-15` and `ST0065/WP-02` are the same corpus, the same verdict vocabulary and one synonym apart, in different threads with different owners. **Found by following a citation, not by any check** -- nothing in the estate compares WP objectives across threads.

22. **A CRITERION'S OWNER IS WHOEVER CAN SATISFY IT, AND A ROW WITH TWO OWNERS HAS NONE.** `AC-08.3` carried a DAEMON obligation inside a CLIENT row, so no client behaviour could close it and there is no daemon. **vc wrote that clause and then, hours later, ruled that a daemon obligation must not live in a client criterion** -- while defending the row that carried one. cc found it. Split into `AC-08.3` (client) and `AC-08.11` (daemon).
23. **THE GATE IS A SHARED RESOURCE WITH NODE-LEVEL BLAST RADIUS, AND THE PROTOCOL THAT PROTECTS US CAUSES IT.** `git commit --only` builds its tree from HEAD plus the named paths, so **a peer's STAGED fix is excluded from every other node's commit** -- the bare checker reads the index and says rc=0 while the same script refuses the commit seconds later. `--only` is exactly what stops us sweeping each other's work, and it is what makes one node's in-flight staged fix inescapable for all of us. **Not a defect in the rule; a property of it nobody had met.** ic said it out loud first.
24. **TWO CLOSURES LOOK LIKE ONE, AND THE FIRST FOUND IS REPORTED AS THE CAUSE.** The gate was shut by `machine_table_check` AND by `rulings_check` exiting 1 on four dangling ruling records that entered at hv's blanket checkin. **vc reported the first as the reason; dc measured the second and it was the one that explained the timing.** ic measured a third partial view. Three nodes, three honest measurements, three incomplete causes.
25. **THE FAILURE PATH IS THE ONE THAT MUST STILL CLEAN UP, AND IT IS THE ONE A GREEN SUITE NEVER RUNS** (cc's words, adopted). Cleanup written after the assertions is dead code until the day it matters, and on that day it does not run. **The second half is what makes it a class: cc's orphaned child held the test binary's stdout pipe, so a failing test became a HUNG BUILD naming no test.** Every other form of class 1 is a wrong answer; this is the first that is a MISSING one, and **no-answer is indistinguishable from still-working.** A `Drop` guard is the only form that runs on the panic path.
26. **IN A SHARED STORE, `sync --to-disk` WRITES EVERY NODE'S UNCOMMITTED EDITS TO CANON, SO WHOEVER COMMITS NEXT SIGNS FOR ALL OF THEM.** `ac edit` writes the STORE; canon is the extract; any node's sync flushes the lot. **vc's criteria work landed in three other nodes' commits tonight** -- hv's `0f41dce1`, dc's `213be93f`, and hv's earlier sweep -- under messages that do not mention it. Nothing is lost and provenance is: `rulings_check` gates on ratification records, and criteria are arriving under commit messages about something else.

27. **A CIRCULAR ANCHOR IS WORSE THAN THE DANGLE IT REPLACES: A DANGLE IS VISIBLE AND A CIRCLE CERTIFIES ITSELF** (dc, 2026-08-30, on vc's grep-before-you-anchor condition). Re-anchoring four dangling ruling records, the first word-perfect match for one of them was `surface/dispatch-table.md` -- **the GENERATED RENDERING of that same record.** It resolves, `rulings_check` goes green, and a reader landing there finds the citation rather than the ruling. **dc would have taken it, because it was word-perfect**, and the condition that stopped it was requiring the target be READ rather than matched. **The general form: a provenance check that only asks whether a reference resolves cannot tell a source from an echo of itself.**

28. **OUR VERIFICATION PRACTICE POISONS THE SHARED TREE, AND THE BLAME IS MISROUTED TO A THIRD PARTY** (cc, 2026-08-30). `env!("CARGO_MANIFEST_DIR")` is baked at COMPILE time, so a build run from a detached worktree against the shared target dir leaves rlibs walking from a directory that is later deleted -- four test binaries then failed reporting a `[workspace]` defect in a `Cargo.toml` untouched since Aug 26. **Detached-worktree verification is what dc, ic, cc and vc all reach for to measure main honestly.** **RULED: a detached worktree gets its own `CARGO_TARGET_DIR`** -- the same principle as the shared-artefact guard's arm 8, applied to a verification build. cc read the stale path out of the binary with `strings` rather than inferring it, and that four binaries carried a `wt-` path and were exactly the four failing is the two-sided evidence.
29. **BETWEEN THE STORE WRITE AND THE EXTRACT WRITE, THE ESTATE DISAGREES WITH ITSELF AND THE RED IS TRUE.** `ac edit` writes the STORE; canon is the EXTRACT; `sync --to-disk` closes the gap. cc's sweep caught `attachment_drift_detected` red at 00:30Z against vc's in-flight `ST0056` sync and it was green minutes later with nothing changed. **In a four-node checkout with one store, that window opens whenever anyone edits a criterion**, and nothing in the failure says it is transient.
30. **AN ENUMERATOR THAT COUNTS TEST CODE AS SOURCE OVERCOUNTS BY AN ORDER OF MAGNITUDE, AND vc's DID IT TWICE IN ONE MEASUREMENT.** A panic census read **197** in `intentsvcs/src`; stripping `#[cfg(test)]` blocks gave **21**; and 14 of those 21 were STILL test code the stripper missed, in one file. **The true signal was 2 / ~7 / 0 and the first number was 100x the last.** Reported here rather than to anyone, because the honest conclusion is that the Rust is clean on this axis and vc nearly filed a finding built on its own instrument -- one turn after telling ic that three enumerators gave three answers.

31. **HIGHLANDER GOVERNS IMPLEMENTATIONS, NOT WITNESSES, AND CONSOLIDATING WITNESSES IS DELETING THE MEASUREMENT.** cc stopped a refactor their own board called a precondition: six constants holding the unwired marker read as duplication, and folding them into one import would make every assertion say the emitter equals itself -- true under every wording including a wrong one. **A test that imports the value it asserts has stopped testing.**
32. **TWO COMMITTED NOTES CONTRADICTED EACH OTHER AND BOTH WERE RIGHT ABOUT DIFFERENT CATEGORIES.** One forbids promoting any copy; the other calls one `pub const` the right fix. **The discriminator neither states: is the marker the SUBJECT of this file, or INCIDENTAL to it?** Subject means witness -- holds its own literal, never imports. Incidental means import. **A node arriving cold does whatever the first note it opens says**, which is what makes an unstated discriminator worse than a disagreement.
33. **A DERIVED COPY IN PROSE HAS NO WITNESS AND DRIFTS IN SILENCE.** The emitter/witness framing has a blind spot: `guide.rs` quotes the unwired marker into GENERATED USER DOCUMENTATION. Change the wording and the guide tells the user to look for a string the tool no longer prints, with nothing red anywhere, **because prose is not asserted.** Neither a home nor a measurement -- a fourth kind, and the only genuine defect among the twelve holders.
34. **TWO ENUMERATORS, TWO ANSWERS, NEITHER CARELESS -- AND THIS TIME IT CHANGED THE SHAPE OF THE QUESTION.** cc counted six named constants; counting FILES carrying the string gives twelve, two of them SOURCE rather than test. Fifth instance this week; the first where the miss was a category rather than a count.

## Decisions

**LIVE ONLY -- superseded decisions are deleted, never struck through.**

- **hv RULED SEVEN on 2026-08-29 23:38Z and every vc recommendation was taken.** Fiat edges guarded `ReasonRecorded`, `### Machine 5`, a closed Guard vocabulary; `AC-08.9` ratified and `AC-08.10` reworded to derive its count; ST0064 narrowed to the menubar app with WP-08 building the daemon; the fiat record's accepted-unverified half is STRUCTURED; `AC-00.3` cites D56 rather than naming a transport; **the daemon binds `127.0.0.1:0` and publishes its address, so no port literal exists**; `Facade::put` refuses to write `fiat` fields. **Full menus and provenance on `hv/wip.md`, not here.**
- **`D56`: `intentd` emits JSON ONLY and every renderer is generic.** The deciding argument is ST0064's SwiftUI menubar app, which cannot consume HTML.
- **A CLIENT'S LIVENESS PROBE IS A COMPLETED ROUND-TRIP UNDER A BOUNDED DEADLINE, AND AN EXPIRED DEADLINE ROUTES AS ABSENT.** `AC-08.3` now names two false positives, not one: the stale socket file, and **an inherited listening fd -- no atomic `SOCK_CLOEXEC` on macOS, so a `fork()` between bind and `FD_CLOEXEC` leaves a socket that accepts and never answers** (cc, 1 in 300 / 0 in 2000). **The daemon narrows that race and cannot close it, so the client is where it is made harmless.** `AC-01.2` binds the menubar app to the same predicate through the same code path.
- **Conflab is the daemon template; Geodica is its simplified second iteration.** **Conflab's authn/authz does NOT transfer** -- it exists to bridge a browser app to cloud `conflabc`, and there is no `intentc`. **What does not drop out with the cloud: a loopback TCP port is reachable by every process on the machine, so the HTTP half carries one auto-generated token and the socket half carries none.**
- **`intent browse` SHIPS.** `ST0058 AC-00.6` refuses a flag and its twin DISAGREEING about whether a capability exists, not both existing.
- **Announce a write to a SHARED file to everyone; announce a write to a CLAIM to the claim-holder.**
- **An opening announcement states the PROPERTY that ends it, not a promise to send a second message.**
- **Docs are written against the CUT, never against `main`.** Em dash in prose pages; `--` in generated reference pages.
