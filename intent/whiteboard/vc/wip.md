---
node: vc
name: Validation Claude
role: validation
session_id: e48565a9-8dc8-4718-bb68-37a3462a0a36
heartbeat_at: 2026-08-18 06:20Z
status: active
focus: "**THE HOIST RE-RUN AT HEAD (`6248236e`) IS BYTE-IDENTICAL ACROSS ALL THREE: v2-over-files, v3-over-store, and the deleted-`.md` control all print `48/114, 66 unsatisfied` byte for byte, traversal quirks included.** Migration 56 threads / 40 issues / 352 files, exit 0; control asserts `MD_AT_SCORE=0` at the moment the store answers, which is cc's sharpening. **IT NEEDED RE-RUNNING BECAUSE MY OWN 46/114 GREEN WAS PINNED AT `bcbd02cd` AND ALL FIVE COMPONENTS PLUS THE SUBJECT HAD MOVED** -- so it was a claim about a tree nobody will migrate, sitting in the line a future session reads first. **Both halves on one tree or it is not a comparison. Timing is VOID this run (load ~160); do not re-quote 95.38s -> 0.02s.** **THE OPEN THING IS NOT MINE AND IS BIGGER: cc found `Facade::upgrade` has ONE test call in the whole workspace and it is the unmigrated DETECTION -- convert and refuse have no cargo coverage.** I verified it. **Composed: convert and refuse are exercised only by hand-driven instruments pinned before the migrator's largest change, and what re-runs on every commit covers neither.** **I RULED the moratorium does not cover writing an AT for a row already at `to-write`, told hv, and cc is holding for them.** OPEN WITH hv: that ruling, the interruption-property AC, `ac_is_nontest` anchoring, D50. Upstream FROZEN; v3 NOT on PATH; push local only."
claims: [ST0056]
---

# Validation Claude (vc)

## DOING

- **CONSERVATION AT `6248236e` OVER INTENT'S OWN ESTATE, THE FIRST TIME THAT ESTATE HAS EVER BEEN THE SUBJECT.** **ALTERED 0, ADDED 0, DOUBLED-SECTION 0, DEFERRAL-REFUTED 0, dispositions 115 of 115 parsed, liveness names all 56 threads.** Residue **STRANDED 192, UNACCOUNTED 354, LOST-PROSE 384** -- the bucketed-path class, unchanged in kind: those files sit at `intent/st/<BUCKET>/<ID>/` while canon is flat, so they are reachable from git and from a human and not from the model. **`conservation_check.sh` printed `SUBJECT unpinned @ /Users/matts` for an estate elsewhere -- field 3 is a REVISION when the estate is pinned and a PATH when it is not, and I truncated both to 12 chars. That arm had NEVER EXECUTED in the life of the tool** (every fleet member carries a `.CAPTURE` marker) **and the first estate to reach it was the one the hoist migrates.** Fixed at `31ff8204`, finding in the commit.
- **THE HOIST DOES NOT TOUCH `intent/whiteboard/` -- ZERO FILES -- SO FOUR NODES WRITING BOARDS DURING THE MIGRATION IS NOT A CONFLICT.** Measured on the migrated clone: it writes `intent/st` (74), `intent/issues` (40), `todo.md`, `events.jsonl`, `.config`, and nothing else. **This was the thing I understood to be blocking scheduling and it does not exist.**
- **THE MIGRATOR READS THE WORKING TREE, NOT `HEAD`, AND DOES NOT REFUSE A DIRTY TREE -- so uncommitted work is carried and the hoist can run while nodes hold files.** Three cells, one variable each: **tracked `info.md` edited and uncommitted -> CARRIED into canon and store; untracked `design.md` -> not carried; tracked non-canonical `notes.md` -> not carried; and the migrator NAMES none of the three.** **I PREDICTED THE UNTRACKED ONE WOULD BE CARRIED AND IT WAS NOT -- but a COMMITTED `design.md` is not carried verbatim either, so the cell is the STRANDED 192 my own check already counts. My experiment rediscovered my own number by another route: corroboration, not a discovery, and it should not be written up as one.** **THE ONE NEW EDGE: a tracked stranded file survives in git; an UNTRACKED one survives nowhere.** So the precondition is **commit everything under `intent/` before the hoist** -- checked, untracked count is currently ZERO.
- **MY VERBATIM-PHRASE PROBE HAS A FALSE-NEGATIVE MODE AND I NEARLY FILED ITS OUTPUT AS LOSS.** `acceptance.md` scored 0 of 3 probes and that is NOT loss: the migrator parses AC rows into `.criteria[].text` and `.tests[].note`, so a 70-character probe spanning a whole row cannot match by construction. **The probe answers 'is this prose in the store VERBATIM', not 'is this content CARRIED', and the two differ for anything structured.** Third proxy-read-as-parser of the day, after `[^-]*` eating every `intent-cli` citation and a rarest-token probe that compared common English words.
- **STANDARD RATIFIED, AND ic SHARPENED IT TWICE: AN AT ROW EARNS ITS GREEN FROM AN INSTRUMENT DEMONSTRATED **RED**, AND FROM A CRITERION NAMING THE **SUBJECT** IT WAS HANDED -- NOT ONLY THE VERDICT.** ic's rig killed a subshell, so its greens were never coupled to the property, **and a row minted from the DESIGN would have been satisfied by it** -- it interrupted, re-ran, compared, and said so. Their second point closes the other half: **a comparator that correctly reports IDENTICAL over a never-interrupted tree is behaving properly, so the verdict alone discriminates nothing.** **Given to cc as guidance, NOT written into the contract** -- that would be minting under the moratorium. **ic declined to let `rig_selftest.sh` in through my ratification and was right: cite the principle, leave the instrument to hv.**

- **THE PREAMBLE IS CLOSED BOTH SIDES, AND THE INTERESTING PART IS THAT I PRICED AN OUTCOME MY OWN INSTRUMENT COULD NOT PRODUCE.** cc proposed a `preamble` field believing the region was carried verbatim; **it was LOST** (`legacy.rs:1373` buffers only once `current.is_some()`), which my check had been reporting as `LOST-PROSE` all along -- **so it is a conservation fix and not an additive field**, and on ST0010 what was going was a cancelled thread's deprecation notice AND its supersession pointer, with no drop record. Specced at `83c2d48e` (own field, never `body`, since `wp_info` renders `body` after `## Objective` and would RELOCATE it -- trading a silent drop for a silent move), built by cc at `283dd00a`, **and every predicted number hit: LOST-PROSE 555, NORMALISED 287, MODELLED 257, 0 findings.** **BUT THE FIRST RUN SAID NOTHING HAD CHANGED**, because my preamble arm printed a literal and never consulted `.preamble` -- **a correct build and a no-op build produced identical output from my instrument.** Fixed at `0735947a`. **A prediction that requires a change to the INSTRUMENT is not a prediction about the SUBJECT**, and cc had correctly declined to check my half.
- **THE DAY'S RULE, cc's AND MINE TOGETHER, AND IT HAS THREE MODES WITH THREE DIFFERENT REMEDIES.** cc: _how many independent paths reach the number_ -- **a figure with two derivations can be falsified by disagreement; a figure with one is a REPORT, and a report from a broken instrument is indistinguishable from one from a working instrument.** Five-for-five today. **MY RIDER, from cc's own counterexample: two derivations falsify ONLY IF THEY SHARE A SUBJECT** -- ic's 10 and cc's 10 were independent, agreed numerically, and were disjoint populations, so the agreement carried zero information and **arrived wearing corroboration.** **cc's THIRD MODE, whose mechanism is CHANNEL SHARING: one subject, one derivation, and the subject's FAILURE MODE is itself valid input** -- my liveness arm ran `st list 2>&1` and read the refusal, which NAMES thread ids, as proof of life. **Fixed by `2>/dev/null`: a rule about which stream may be evidence.** **REMEDIES: same-subject disagreement -> a second derivation. Different-subject agreement -> "what is this a count OF". Failure-as-input -> answer and error on separate channels.** Derivation-counting is the remedy for exactly one of the three. **AND cc's CAUSAL ORDER CORRECTS MINE: the corroboration is accepted FIRST and the reconciliation is built after** -- which is why closing arithmetic feels like a check when it is the story told once the evidence has already been assumed.

- **OPERATIONAL, ALL NODES: THE PUSH RESULT CARRIES NO INFORMATION ABOUT THE REMOTE, IN EITHER DIRECTION.** I first told cc and dc _"a rejection can be false, so do not trust it"_ -- **n=1 generalised into a rule about the remote and shipped to two nodes, which is the denominator error inside my own warning about denominators.** **ic then hit the same `incorrect old value provided` TWICE and both were REAL.** Mine was false; theirs were true; **there is nothing in the message to tell you which you have, so a rule about how to READ it cannot exist.** Corrected to both nodes at `38765791`. **The only thing that is evidence: `git ls-remote local refs/heads/main` then `git merge-base --is-ancestor <yours> <tip>`.** **Three of us reached for three checks and TWO WERE THE SAME OBJECT: dc a cached tracking ref, cc the exit code, ic a fetch -- and a fetch IS the tracking ref.** One instrument counted twice, which is the shape that makes a second opinion feel like corroboration. **ic's asymmetry, kept in their words: the failure mode is ACTION rather than inaction, and it is invisible because the retry succeeds and looks like the fix.**

- **THE HOIST RE-RUN AT `6248236e` IS BYTE-IDENTICAL, AND THE REASON IT HAD TO BE RE-RUN IS THE POINT.** **v2 over FILES, v3 over STORE, and the deleted-`.md` control all return `48/114, 66 unsatisfied` -- and the three outputs are IDENTICAL BYTE FOR BYTE**, including the traversal quirk that prints `AC-08.8` before `AC-08.7` and `AC-14.11` before `AC-14.10`. **That ordering artefact is a fingerprint and it is far stronger than set equality: v3 reproduces v2's traversal, not merely v2's answer.** Migration: **56 threads, 40 issues, 352 files, exit 0**. **THE CONTROL NOW ASSERTS AT SCORE TIME, WHICH IS cc's SHARPENING AND IT CHANGED THE INSTRUMENT:** the surviving `.md` count is printed in the same breath as the score (`MD_AT_SCORE=0`), because a store quietly falling back to files still on disk returns the right number for the wrong reason. **Note the migration WRITES markdown -- 443 `.md` before, 679 after -- so deleting 'the files' now means deleting the generated views too, and it still returns 48.** **WHY IT WAS NEEDED: the 46/114 green was pinned at `bcbd02cd` and FIVE OF FIVE components had moved** -- `legacy.rs` +280/-17, `contract.rs` +187/-42, `store.rs` +42/-7, `model.rs` +40, `facade.rs` +32/-5 -- **plus `acceptance.md` itself, so the SUBJECT moved too and the v2 number was already 48.** Measuring v3 alone would have been uninterpretable: a 46 is ambiguous between 'v3 regressed' and 'the contract moved', **and halves measured at different times cannot separate them. Both halves, one tree, or it is not a comparison.** **TIMING IS VOID FROM THIS RUN AND I AM NOT QUOTING A SPEEDUP.** Machine load was ~160 on 16 cores (four Lamplight sessions building), so v2's 157s is inflated and my harness resolves only whole seconds. **What IS sound, because both halves ran minutes apart on one machine: v2 took 157 seconds and v3 and the control each returned in under one.** The old `95.38s -> 0.02s` pair is from the retired pin and must not be re-quoted.
- **RULED: WP-10 DOES NOT CLOSE UNTIL THE INTERRUPTION PROPERTY IS IN THE CONTRACT.** ic measured that **0 of 114 ACs cover the property hv gated the cutover on**, and 0 AT rows cite either instrument -- both could be deleted tomorrow and nothing would go red. **The gap is in the CONTRACT rather than in an instrument, which is why no instrument found it.** I ruled the half that is mine (the block) and did NOT mint the AC: the moratorium is hv's, and ic rightly declined to propose one while holding WP-10. **First row written when the moratorium lifts.** Escalated at `90438c62`.
- **RULED: A RULING GIVEN OVER THE LIVE CHANNEL IS NOT LANDED UNTIL IT IS IN THE ARTEFACT IT GOVERNS, AND THE NODE WHO GIVES IT LANDS IT -- NEVER THE NODE WITH THE INTEREST.** dc's board carried a wording of mine that `git log -S` finds in no commit and no inbox; **their board went AHEAD of the contract, and the one party who cannot check it is the one who remembered.** The general form binds all five nodes and is with hv to ratify. **It is the cost of the medium we are told to prefer: it writes nothing to disk.** I have been landing every ruling first and pinging second since.
- **LAMPLIGHT: BOTH FOLLOW-UPS ANSWERED, AND THE PHRASE I PUBLISHED IS WITHDRAWN.** Measured myself on the pinned corpus at `fcfa0ffd`, 5613/5613 verified, reproducing ic's 10 exactly (`unparseable-row: 9, broken-reference: 1`, ST0276 + ST0345). **(1) Both blocking threads ARE live (`status: WIP`, unbucketed), so the block is hv's carry policy working as designed.** **(2) Whether the carry policy accounts for the rest is NOT ESTABLISHED -- Phase A REFUSES before the carry stage runs, so zero carry lines is the refusal's SHAPE and not evidence about the other 1633 AT rows.** **`ten fixes in two files from migrating` is WITHDRAWN** -- ic coined it, I amplified it into hv's inbox and the shared `MEMORY.md`, and three OTHER live threads carry 49 rows of the same form (ST0201 6, ST0264 22, ST0276 21) while the migrator names 8 of ST0276's 21 and nothing for the other two. **A numerator with no denominator.** **The blockers are ONE class in three fields: a qualifier fused to a value where the grammar expects a bare token** (`covers AC-00.1 (see the correction on the AC)`, `status: green **(cc, ...)** Restoring...`, `status: green for Stage A`) -- same class as `959b0190`. **And a method note that is the useful half: `::` as a proxy for "unparseable" is WRONG -- I collapsed 21 `path::name` citations and the count moved ZERO, 9 before and 9 after, which is disjoint populations rather than an imprecise proxy.**

- **RULED: `related` MUST NOT LAND BEFORE THE THREAD-LEVEL DEFERRAL. IT IS NOT A COUPLING, IT IS A REGRESSION WAITING BEHIND A FIX.** cc found it and I confirmed all three premises at source: **`views.rs:282` guards on `if !thread.related.is_empty()`; `related` is EMPTY on all 56 migrated threads; 55 v2 threads authored a `## Related Steel Threads` section.** **So the renderer's Related block has NEVER been emitted on a migrated estate -- a path declared, implemented and never exercised, which is ic's rule exactly.** Fixing `related` runs it for the first time **and collides with 52 carried authored sections in the same commit**, converting an invisible carry into a visible doubling. **The deferral is a PRECONDITION of `related`, not a companion to it**, and the constraint is written down so nobody lands it later without the memory. **This is the `related` work I argued FOR and cc corrected me on -- and the correction now has a second half: not only does only 58% of 126 rows fit `{id}: {note}`, but parsing them at all doubles 52 threads unless the deferral is already in.**
- **I ASKED hv TO RE-RULE D50, BECAUSE MY FRAMING PRODUCED THE RIGHT ANSWER TO THE WRONG QUESTION. `WpStatus` IS HELD.** cc found it going to write the match arm; I verified all three claims at source rather than relaying, having shipped an unverified peer claim once already this week. **I told hv _"ten work packages have no representable status"_ -- TRUE, a FLOOR, and it invited exactly _"Then add it, what's so hard"_. hv gave the correct answer to _we need one more value_; the QUESTION was wrong, not the ruling.** **Counted independently of cc by a different method (grepping `status:` out of Lamplight's `info.md` and normalising, not parsing rows) and got the same total: 34 rows unexpressible, cancelled 10, todo 5, pending 4, superseded 3, relocated 2, proposed 2 and eight singletons.** **So `Cancelled` fixes 10 and leaves 24 -- and `Superseded`, `Absorbed`, `Relocated`, `Dropped`, `Partial` are DISTINCT IDEAS with as good a claim to statehood as `Cancelled`. v2's WP status was FREE TEXT and an enum cannot grow to 29.**
- **ON THE BOUNCE: I REPLACED A STALE HAND RUN WITH A FRESH HAND RUN, AND THE CLASS cc FOUND IS UNTOUCHED BY THAT.** **cc's finding, which I verified independently rather than took: `Facade::upgrade` is called by exactly ONE test in the whole workspace, and that call is AC-10.7's unmigrated DETECTION.** Convert and refuse have no cargo coverage; all five `to-write` AT rows for WP-10 name files that do not exist. **A symbol grep returns three test-file hits and two are comments, so the search that finds the understaffing understates it.** **THE COMPOSED SENTENCE, and it is the one to say to hv: convert and refuse are exercised ONLY by hand-driven instruments whose last runs predate the migrator's largest change, and the thing that re-runs on every commit covers neither.** Not 'untested' flatly -- ic's cutover gate drives convert twice and refuse twice and that is real evidence -- **but it is outside cargo and pinned against the same moved migrator, so it is the same class as my rehearsal rather than an answer to it.** **I RULED FOR cc THAT THE MORATORIUM DOES NOT COVER WRITING AN AT FOR A ROW ALREADY AT `to-write`** -- the row is already a criterion and the file is already cited, so writing it discharges rather than extends -- **with two conditions (cited path and covered AC stay as written) and with hv told, because a ruling that ENLARGES a restriction should be visible to whoever set it.** cc accepted both and is holding for hv rather than taking it as a green light. **RE-RUN RECIPE:** clone at the pin, assert `dirty=0` BEFORE the build, build inside the clone (own target dir), v2 gate -> `upgrade` -> v3 gate -> delete every `.md` and score again with the count printed beside the score. **The store is per-project at `intent/.cache/intent.db`, so a clone cannot collide with the live repo.**

- **AC-10.8 (EGEST SYMMETRY) IS THE STRONGEST SELF-GRADING-PROOF PIECE ON THE BOARD AND IS NOT DRIVABLE YET.** Its verdict is `git diff --exit-code` against a tree git already holds -- a comparison git performs, with no judgement in it to be wrong about. `Facade::projection` is the only expression of the db -> disk direction and it is PRIVATE, reachable through `apply` and both sync directions but not standalone. **The migrator is what makes it drivable**, because Phase B must produce exactly that projection from a scan. cc will say when the door opens.

- **`sync --to-disk` IS UNREACHABLE FROM A v2 ESTATE AT THIS BUILD, WHICH IS WHY THE HOIST NEEDS THE MIGRATOR AND NOT A FLAG.** `ingest` is read-only, `init`/`upgrade` are unimplemented (exit 2), and v3 refuses an unmigrated project -- so there is no path from a v2 estate to a written v3 store except WP-10. Measured again 2026-08-17 across 22 families: 8 run, 4 wired-but-gated, 10 unwired.

- **ic's RULE, STILL THE MOST LOAD-BEARING THING EITHER OF US WROTE: a field a test EXECUTES gets measured; prose drifts because nothing reads it back.** The register's `observed` column was 4 wrong of 8 checked when first executed -- including `at na` carrying v3's own token in the column that records v2, which would have certified a live defect as parity. Full incident in `.history/`.

- **THE CLASS IS TEN AND HAS A SIBLING CLASS, AND `parity.md` IS ITS HOME, NOT THIS BOARD.** Ten variants of _the measurement's subject and the report's subject differ_; the sibling class is _a true answer that has stopped discriminating_, whose detection question is **"what OTHER state would produce this same output?"** Members, remedy (cc's opposed-enumerator equality) and the third axis (dc: a REMEDY whose subject is not the report's) are all in `parity.md`.

## THE MODEL, in case everything else is lost

1. **The intentdb is the durable SSOT. Everything else is a secondary artefact. Nothing on disk is truth.**
2. All of `intentsvcs` works FROM the db; sync runs both ways.
3. The typed Rust API is the ONLY door in, so conformance is **by construction**.
4. Re-creation from an extract is a **capability**, not a licence to treat the db as disposable.
5. Ingest passes the **hard gate** of the intentsvcs API; the gate does the work, not the file format.
6. **Migrations are normal.** "No DB migrations, ever" is DELETED and was never hv's constraint.
7. The real standing requirement is **platform and data-model openness** (AC-02.6): always a 1-1 db-entity-to-`.json`/`.md` mapping, lossless, usable elsewhere. **That is what bidirectional sync is FOR.**
8. **(D34) The committed extract is the INTERCHANGE; the DB is per-machine truth and is NEVER committed.** Authority is not bidirectional just because transport is. **So AC-02.6 is the durability mechanism** -- a field that does not round-trip is data loss at the clone boundary, not a gap -- and **`event_log` is the only table that is both durable truth AND not reconstructible from the files**, which makes `events.jsonl` a precondition of the model rather than a WP-04 detail.

## TODO

- **AFTER THE HOIST -- THE ISSUE BODY HAS NO HOME (cc, `db1f9501`).** Measured source-side at `42fb5269`: **503 sections and 658,676 bytes across 61 issues, 30 distinct headings of which 21 appear EXACTLY ONCE.** Seven are the template; **the tail is the load-bearing half, same as `Thread.body`.** `Issue` has no body field, so every byte is dropped today. **cc deliberately did NOT add one -- a model change is mine, and inventing a field while writing a parser is how the preamble nearly went into `body`.** Same treatment as the preamble: spec first, price before and after, own field. **cc will not reconcile 641,893 on their board against 658,676 measured, and is right not to -- different boundary rules, earlier rule not in hand, stated as unestablished.** That is the 78-byte discipline applied without being asked.
- **AFTER THE HOIST -- A RESIDUE CLASS FOR SOURCE DISAGREEMENT (cc).** An issue whose BUCKET disagrees with its FRONTMATTER has no declared class: the status parses, the file classifies, nothing is unparseable. **`residue_class_check.sh` REFUSED cc's commit for emitting `model-inconsistent` undeclared -- my two-directional arm doing its job on a peer, which is the first time it has fired on anyone but me.** cc removed the finding rather than reaching for a declared-but-wrong class, and that is the right call: **`unknown-status` on a status v2 accepts sends an operator to fix a correct value, which is worse than silence.** Measured first -- bucket and frontmatter agree on all 61, so nothing is lost today. **cc ruled the precedence rather than deferring it, and the reason is right: the frontmatter is what an AUTHOR wrote, the directory is where a TOOL put it.**

## Verification kit

- `$CLAUDE_JOB_DIR/tmp/v3fix` is a **migrated** v3 fixture with its own `git init`. Recipe: `config.json` at 3.0.0, `st new`, prose into `thread.json` (**never** a generated view), `sync`.
- **`$CLAUDE_JOB_DIR/tmp/hookprobe` is the 0043 rig; it covers ALL THREE Claude Code hook events and needs NO migrated project and NO interactive session.** Per arm: a dir, a `settings.json` wiring `UserPromptSubmit` (matcher `""`) to one command, then `claude -p "<prompt>" --settings ./settings.json`. Arms are hook scripts exiting 0/1/2 plus one wired to the real v3 binary. **Assert on the OUTPUT, never the exit code -- a BLOCKED run exits 0**, which is the same silent shape the probe was built to find. **And keep the exit-1 arm**: an assertion that `2` blocks passes equally on a build where every code blocks, so only the arm that does NOT block ties the failure to the change that caused it.
- **MEASURE AT A PINNED SHA, AND NAME THE COMMIT IN THE FINDING.** `HEAD` is a POINTER, so a claim about it is a claim about whatever it points at WHEN READ -- pinning protects the measurement, only naming the commit protects the report. **And `rust` is a pointer at a tree the same way, which is the whole of 0049.** `git archive` the sha, build in a separate `CARGO_TARGET_DIR`. Incidents in `.history/`.
- **ASK HOW MANY WRITERS AN ARTEFACT HAS BEFORE RULING ON IT** (cc). _"A ruling enforced on one of two writers is enforced on neither RELIABLY"_ -- **the uncovered writer wins whenever it runs LAST, so it is a RACE, which passes in the suite and fails in the estate.** `todo.md` has two doors and all five tests reached it through the other one. Incident in `.history/`.
- **Falsify before flipping.** Perturb the artefact the test asserts against and watch the right subset go red.
- **COUNT, STOP, READ THE DIFFERENCE, THEN ARCHIVE -- IN THAT ORDER, IN SEPARATE COMMANDS.** I ran the both-sided count check and the archive in ONE command and archived an unread ic entry; it printed `ic live=5` against my four named stamps AFTER the archive had run. **A check whose result arrives after the act it was meant to authorise is not a check.** Recovered from `.history/` and nothing was lost. The check caught unread entries FIVE times today and failed only when I let it run too late.
- **SIGNATURE AND SCOPE FAIL IN OPPOSITE DIRECTIONS AND NEITHER IMPLIES THE OTHER** (dc, 2026-08-16, correcting my verification of their header guard). Signatures answer _does it fire on the right SHAPE_; scope answers _does it fire on the right FILES_. **I cleared that guard on signatures alone and reported it as verified**: `intent/whiteboard/*/wip.md` matches 21 files, SIXTEEN of them archived boards, so it would have refused the protocol's own housekeeping. Both `case` globs and git pathspecs cross `/`. **The identical trap had already bitten the clock guard's port**, which makes it a class.
- **THE DAY'S MEASUREMENT CLASS AND ITS INSTRUMENT RULES NOW LIVE IN `parity.md`, NOT HERE** -- nine variants, three properties, the calibration and chokepoint rules, the annotated-artefact bias, and the sibling class. Four entries that sat here (guard-proven-only-by-its-positive-case, a-check's-error-message, the-ok-line-must-be-conditional, put-the-population-on-the-ok-line) are folded into it verbatim and archived. **Read `parity.md` -> Measurement rules before building any instrument.** Keeping a second copy here is the divergent-copy defect the canon itself warns about.
- **SHARPENED 2026-08-16, by L2 refusing a row I wrote: the three states answer TWO questions, not one. `to-write` = the test is UNWRITTEN. `red` = the test EXISTS and does not pass. NEITHER means "the criterion is unmet" -- a criterion can be false with no instrument pointed at it, which is exactly AC-10.9.** I set that row `red` because issue 0038 makes its criterion measurably false, and L2 refused it because the cited file does not exist. **The refusal was right.** My partial-coverage rule below was always about a test that EXISTS and under-covers, and it does not generalise to any unmet criterion; reading it that way produced a red row citing a file that is not there. The exempt-state hazard on such a row is covered by `stale_at_check.sh` firing the moment the file appears.
- **Refuse at partial coverage. CORRECTED 2026-08-15: hold the AT at `red` WITH AN EXPLICIT NOTE, not at `to-write`.** I had this the other way round on the grounds that `red` lies about a passing suite. It does not: **the AT's status is the CRITERION's state, not the file's**, and `to-write` is the worse lie because it claims no file exists when one does. Write the note so anyone running the suite and seeing green knows why the board is red. AT-00.5 is the precedent for the refusal; AT-06.4 / AT-06.7 are the corrected form.
- **A second AT row at `to-write` does NOT hold a gate open** (issue 0032): `ac gate` satisfies on the FIRST green AT covering the AC. Until that ANDs, the covering AT itself has to come off green.
- **The D34/D35 numbers are in canon, not here** -- `design.md` D34 carries the FTS ratios, the GitHub ceiling, the pack sizes and the `dbstat` split; D35 carries the WAL measurement and the rejected `.sql` dump. **Cite them; do not re-derive them.**

## Watch-outs

Measurement rules live in `intent/st/ST0056/parity.md`. What follows is operational to this node.

- **`intent at red|green|na` DESTROYS THE ROW'S NOTE (issue 0033) -- COMMIT BEFORE ANY STATUS CHANGE, then restore the note by hand.** The note is where the discriminating case is written down, so losing it loses the reason the row is not green. Full incident in `.history/`.
- **NEVER ATTRIBUTE UNCOMMITTED WORK BY READING THE TREE. TWICE IN ONE DAY, IN OPPOSITE DIRECTIONS** (dc, 2026-08-16). I read dc's uncommitted `testkit` and credited it to cc; then read cc's uncommitted `install.rs` and credited it to dc, and TOLD dc to "build on" a module cc had already finished. **The near-harm was two nodes writing one file, which nothing in this estate would have caught until a conflict.** dc's diagnosis is the one on my own board: **in a four-session shared clone an untracked file has NO AUTHOR. Reading the tree is right for WHAT and structurally silent on WHO.** `git log` is the only surface carrying authorship and neither of us reached for it. **Before naming an owner: `git log -1 -- <path>`, or ask. A modified file in a shared clone is evidence of work, never of whose.**
- **MY PINNED-EXTRACT DISCIPLINE IS WRONG FOR INSTALL-DEPENDENT COMMANDS, and it produced a false rc=1 today.** `install.rs` resolves the install home from `current_exe()` by walking up to a marker, so **a binary built into a `CARGO_TARGET_DIR` OUTSIDE the extract cannot locate its install** and `claude hook` answers `1 -- cannot locate the Intent install` where dc measured `0`. Not a disagreement: my rig. **Two disciplines and I must pick: an external target dir is right for TESTS (isolation from peers) and wrong for anything reading its own install** -- for those, use the repo's own build or build into the extract's own `target/`.
- **DO NOT PUT v3 ON PATH. THIS SESSION IS ALIVE ONLY BECAUSE `intent` STILL RESOLVES TO v2** (dc, 2026-08-16, and it applies to all four nodes right now). 0043's trigger is **PATH, not migration**: `claude` is unimplemented as a FAMILY, so v3 refuses before it reads project state -- measured `rc=2` in a migrated project, in an unmigrated v2 project, and **outside any Intent project at all.** `brew install` does it unasked, at PATH position 1. **The moment v3 lands on the PATH of a shell a Claude Code session runs in, that session stops accepting prompts and cannot be recovered from inside itself.** Always invoke the v3 build by explicit path.
- **AND MY OWN CONFIRMATION ALREADY CONTAINED THAT PROOF AND I READ PAST IT.** ARMV3 -- the end-to-end arm -- ran in a throwaway dir with **no `intent/.config/config.json` on any ancestor**, and blocked. So I executed the decisive arm with the "migrated project" precondition ABSENT and reported it as end-to-end confirmation of a claim about migrated projects. **The result was right and the framing around it was never tested by anything** -- nothing failed, so nothing objected. **A passing measurement does not audit the scope you claim for it**, and dc found the widening by measuring the thing I had already measured and not looked at.

- **The four things that are NOT exceptions**, because each has already been used by one of us to smuggle a clock back in: "only for a test fixture" (guard is being widened to `tests/`); "only reading, not writing" (there is no read that stays a read); **"but the value came FROM the database"** (the read and the write are two acts with a gap -- better provenance is not the absence of a confection); "only a label" (then it needn't be a time).
- **Refusing to settle by inference is NOT a resting state.** It obliges you to go and get the answer. A question parked across three rulings is a decision made by default -- that is how D01 stayed wrong through four hv statements of it.
- **When you inherit a rule with a rationale attached, the rationale is the part most likely to be wrong** -- nobody re-derives it. Twice measured now: "no DB migrations ever" was a consequence defended as a requirement, and the binary-dirtiness case against committing the DB was true-sounding and not the reason.
- **Absence of a mechanism's NAME is not absence of the mechanism -- and it is not presence of it either.** I grepped `hooksPath` and filed a false `high`; I grepped `from: &[]` against a positional constructor, got zero, and nearly filed a refutation of a real finding.
- **A clean result is only as current as the needle set that produced it, and a needle set has a timestamp** (dc). **A structured query is a needle like any other and reports on the subtree it TRAVERSED** (ic) -- and it feels exhaustive in a way a grep never does, so it is trusted harder than it has earned.
- **A rule true in its own scope is the easiest to over-apply.** Check the SHAPE matches the shape the rule was measured on.
- **Verify at HEAD (`git ls-tree`), never on disk -- better, clone fresh and build.** **A green suite is evidence about the tree you HAVE, never the tree you PUSHED.**
- **AN UNTRACKED FILE HAS NO AUTHOR** (dc's correction of me): you cannot attribute it by reading the tree, in either direction. Pair it with **`git grep` reads the INDEX, not the worktree**. Incidents in `.history/`.
- **AND THE OPPOSITE HALF, measured 2026-08-15: `git grep` reads the INDEX, not the worktree.** Searching for hv's leaked identifiers returned **ZERO** and I nearly reported the estate clean; plain `grep` over the same tree found 69 in `src/` alone plus all six sites hv was pointing at, because they are in peers' uncommitted live edits. **The two rules are opposite halves and the QUESTION picks the tool**: "what did we ship?" is HEAD, "what is a peer writing right now?" is the worktree. In a four-session shared tree the second question is the common one, and the tool's default answers the first.
- **ARCHIVE BY NAMING THE STAMPS. NEVER `sed -n '/^## (/,$p'`. THE GREEDY RANGE IS THE BUG.** FIVE times today the fold swept an entry I had never read (dc 13:33Z, ic 14:22Z, cc 14:30Z, then cc 15:14Z + dc 15:13Z) -- and **the last two happened AFTER I wrote the rule "archive only up to a stamp you have recorded as read" and while I believed I was obeying it.** I then implemented the exact opposite: a range from the first `## (` to EOF, which archives _everything present_ rather than _what I have read_. **The prose rule was right and the mechanism contradicted it, so the rule bought nothing.** dc's 15:13Z carried hv's two rulings and cc's 15:14Z carried a whole defect class -- I read both only because I printed the stamps as the script ran and did not recognise them. **A control refuses; a resolution reminds. This one had had three resolutions.**
- **Why it stays invisible: a fold CAPTURES into `.history/` rather than deleting**, so nothing is ever lost and no error is ever raised. The only signal is counting entry headings afterwards -- and by then you have answered the wrong message.
- **THE WORST FORM OF THE HALF-A-MOVE, measured 2026-08-15 on cc's tree: AN UNTRACKED WORKSPACE MEMBER.** `native/rust/Cargo.toml` listed `crates/testkit` while the directory was `??`. **`git commit --only` does not stage an untracked path**, so naming the manifests lands a workspace declaring a member absent from HEAD. **This does not break a TARGET, it breaks cargo's ability to load the workspace** -- build, test, fmt and clippy all fail identically before reaching any code, **so the command you would run to diagnose it is the command that cannot run.** The author's worktree stays green because the directory is on their disk. **Every prior instance of this class cost one suite; this one costs every cargo invocation in every clone.** `git add` the new directory BEFORE naming the manifests.
- **`git commit --only <paths>`, never `-A`** -- and **a move is TWO facts**: naming only the new paths commits half a move, silently, with a green working tree. **`--only` also never CLEARS the index** (measured, issue 0028): a stale entry survives every later commit, is invisible to `git diff HEAD`, and shows only as the left `M` of `MM`. **Read `git status --short`, not the diff.** When peers are live, scope any `git reset` to your own paths.
- **DO NOT PUSH TO `upstream`. hv, 2026-08-16: the CI/CD budget is spent. `local` only.** `git push local main` yes; `git push upstream main` NO -- every push there triggers the GitHub Actions matrix, which is the thing that ran out. **Announced to all four nodes, because all four had it in their commit habits.** **`int prepush` will NOT save you**: its gate is about whether `native/` moved, not about which remote, so it passes an `upstream` push exactly as before. **This is a discipline and not a control** -- the natural home for one is a `prepush` arm refusing `upstream` unless overridden, which is dc's lane and is not built. Nothing needs rewinding: the two remotes were in sync at `99c66e8b` and the divergence starts from there.
- **THIS REPOSITORY IS PUBLIC** and hv has ratified the whiteboards as part of the public record, intentionally. The `-A` hazard is therefore a _publication_ hazard into a history nobody can rewrite.
- **Never mutate `bin/**` or `tests/**` in place** -- `~/.local/bin/intent` symlinks here and the BATS suite reads the live tree. Sacrificial worktrees only.
- **Never `head` a list you are counting**; a frequency-sorted list puts the RARE value last. **Read `$?` before anything else touches it** -- but the pipe trap is the pipe trap; `A || echo "$?"` reports A correctly.
- **NEVER `head` OR `tail` A LIST YOU ARE COUNTING, AND NEVER READ `$?` THROUGH A PIPELINE** -- `$?` is the LAST STAGE. **Six firings across three nodes in one day, the most-repeated defect on this thread**, and ic's diagnosis is why knowing it does not help: the pipe is added for DISPLAY, after the command already worked, so the edit that breaks it is cosmetic and arrives last. Capture the rc directly when a write depends on it.
- **This shell is zsh**: no word-splitting of unquoted parameters.
- **BACKTICKS NEVER GO INSIDE A DOUBLE-QUOTED `-m` BODY.** They are command substitution: the words are EXECUTED and then silently deleted from the message. **Control, not caution: single-quote the body, or `git commit -F <file>`.** Demonstrated on myself at `0e84acc9` -- a pushed, public commit reading _"the state tag is , not "_, in the sentence whose whole job was naming which of two tags won -- **inside the commit replying to ic's report of this exact defect.** Not amended; rewriting public history costs more than a wrong log line.
- **A detector's false-positive set is part of the measurement, and you find it by READING the hits, not counting them.** Chasing the above: first detector said 23 of 60 commits scarred, every node affected, and I was one step from publishing it. Almost all were ` .treeindex` / ` .backup/` -- **a space before a dot is normal when the next token is a dotfile.** Re-measured with a pattern dotfiles cannot trigger: **exactly one, mine.** Same shape as the `hooksPath` claim I filed at `high` and withdrew; the only difference was checking before publishing.
- **The live channel does not survive a peer's restart; the inbox does.** Durable copy first, live ping as accelerant.

## Decisions

- (2026-08-17) **hv SHARPENED THE RULING AND IT SUPERSEDES BOTH vc's AND ic's FORMULATIONS. Verbatim: _"Issues are (generally) for other, external users of Intent, using a released version. We can just fix things inline as we find them."_** The test is not "does it need a decision" (ic) or "does it cross a node boundary" (vc). **It is WHO reported it and AGAINST WHAT: an external user against a released version is an issue; anything we find building v3 is just work.** Fix inline; if it needs documenting it goes on the WP ladder. **Consequence: 21 of the 22 open rows carry `reporter: matts` and every one was found by us against unreleased v3 or against v2 internals WP-04 replaces -- so under this rule essentially none of them is an issue.** That includes 0058, which passes ic's decision test cleanly. **DO NOT MASS-CLOSE: the findings are real (0033, 0035, 0044, 0045, 0055, 0058) and the tracker is the wrong container, not the content.** Put to hv as ONE decision: convert the live findings onto the ST0056 ladder, clear the tracker of everything that is not an external report against a release.
- (2026-08-17) **AN ISSUE FIXED-AND-LEFT-OPEN AND AN ISSUE NOBODY HAS TOUCHED ARE THE SAME OPEN ROW** (ic, sweeping the tracker under hv's first ruling; 0060 was already fixed, with `render.rs:1833` carrying a comment citing it, and only a measurement could tell). **The sibling class arriving on the tracker itself** -- same shape as dc's unsatisfiable close condition. It is the strongest argument available for hv's ruling: the tracker was not merely redundant with the channel, it was accumulating rows whose state nobody could read without re-driving the code.

- (2026-08-17) **hv STANDING RULING, VERBATIM: _"The honest diagnosis is that the issue file is duplicating the agent channel. Exactly. So stop doing that, please."_ STOP FILING ISSUES THAT DUPLICATE THE AGENT CHANNEL.** Given after vc reported the rate: **17 issues filed today against 10 closed, the tracker up 39% in one day.** The "fix under issue" practice came from v2.19.0, when there was no steel thread and the issue WAS the unit of work. Under ST0056 there is a 14-WP thread with a 112-row acceptance contract, so the tracker had become a second work-tracking system running beside the one the close gate actually scores. **The practice from here: fix it inline and put the reasoning in the commit message; if it crosses a node boundary, say so on the agent channel; if it describes what "done" means, it is an AC row, not an issue.** A finding does not need a file to be real -- the commit is the record and the channel is the routing.

**CONSOLIDATED 2026-08-17 into five families; nineteen bullets, full text in `.history/20260817/wip.md`.** Folded because the list had stopped changing behaviour at the margin -- cc's move, taken. The families are what a next session needs; the instances are what the history is for.

- (2026-08-17) **The class is at NINE and the count is deliberately not moving for properties.** cc's `instant` is the ninth axis and it BREAKS the asymmetry -- reading too early returns the pre-state, which equals the post-state exactly on the cases where nothing moved, so a wrong-instant error converts a movement-detector into a NULL-detector. ic's handoff (0053) and licence-expiry findings are recorded as PROPERTIES of the class, not variants.
- (2026-08-17) **dc's line is the class's third property and the one that changes how work is done: NOT ONE OF THE NINE WAS FOUND BY READING.** Every one needed a mutant, a witness driving the moving case, or a peer measuring independently and getting a different number. **Review does not detect this class** -- guarded, at dc's ask, so it is not read as licensing skipped review.
- (2026-08-17) **A SIBLING CLASS, distinct from the above: a defect written down AS the specification passes every consistency check.** Four members now. The pair that matters is a correct NOTE beside an incorrect ASSERTION about one command at one time -- **the assertion wins because it RUNS**, and nothing anywhere compares a row's prose to the assertion covering it.
- (2026-08-17) **The parity contract's headline carries a silent quantifier**: "v2's suite cannot tell the difference" means _in the ways it looks_. All four mechanisms missed `wp show` -- including `wp_commands.bats:565`, **the nominated certifier**, which asserts title and objective and never the status line. Coarse coverage hole is 2 of 62 (`agents template`, `agents sync`); **the fine hole is ungreppable and rests on 60 unrecorded human judgements, the only checked one wrong.**
- (2026-08-17) **My own calibration rule is insufficient, and ic's form supersedes it**: the checker is written by the same head as the needle, so a check is not independent unless its SOURCE is the corpus. Enumerate the spellings from the corpus, then calibrate one per form -- never from recall. Both ic and I manufactured false zeros this way, in the direction that reads as serious.
- (2026-08-17) **State the population, not just the result.** A scope that is not stated is a scope nobody can disagree with; a reader disagreeing with a stated scope is the control working.
- (2026-08-17) **A PROXY IS ONLY WORTH HAVING WHEN THE POPULATION IS TOO LARGE TO STATE.** Fifty call sites felt mechanical; four is readable, and reaching for the proxy over four is how three false positives got in. **The felt-need was the error rather than the rule.** Sibling: **count where a defect MANIFESTS, not where it originates** -- and when the sites share no chokepoint, building one is a cheaper repair than an enumeration nobody can trust.
- (2026-08-17) **A defect masking its own detector is a real shape, not a curiosity** (cc): `ac_rescope`'s hand-written from-state check refused everything, which is exactly what kept the walk that should have caught it green. **So a fix and the test that would have found it have to land together**, because the test cannot be shown to work until the defect is gone.
- (2026-08-17) **THE `instant` AXIS HAS A FIXTURE HALF AND AN INSTRUMENT HALF AND THEY ARE ONE RULE: arrange for the two instants to DIFFER, then read them.** cc read the state before the verb ran; ic seeded an entity at the verb's target. Both make a movement-measurement into a null-measurement. **ic's is the worse direction -- it MANUFACTURED a parity break against v3 that did not exist**, which files as a defect in the binary rather than in the fixture.
- (2026-08-17) **EXECUTION IS THE ONLY FORM OF DOCUMENTATION THAT CAN BE WRONG OUT LOUD** (ic's, and the deepest thing on this thread). The case for a structured field was one-requirement-one-statement; **the better case is that the field forces the value to be MEASURED, and measuring is what found the four wrong entries.** Generalised: **every defect found on this thread today was in something nothing read back** -- a doc comment, a note, a prose column, a restore that is silent when it works, a seal nobody parses.

- (2026-08-16) **hv GRANTED A STANDING AUTHORISATION -- "go with your recs. Always." -- AND ITS BOUNDARY IS RECORDED HERE BECAUSE A GRANT NOBODY BOUNDED IS A GRANT SOMEBODY LATER OVER-READS.** **What it covers**: contract-shape calls, spec corrections, ratifying or folding a class, severity, AC/AT wording, and adopting my own recommendation where I have already stated the reasoning and the measurement. **What it does NOT cover, and I am stating this against my own interest so it cannot be quietly widened**: anything irreversible or outward-facing -- the release, pushing `upstream`, publication, running WP-10 Phase B against the real estate, or mutating `bin/**` / `tests/**`. **cc drew this exact line unprompted on the same day and their wording is the one to keep: "hv's 'work amongst yourselves' authorises work; it does not authorise an irreversible rewrite of their own project's canon."** A standing grant lowers the cost of a decision; it does not change which decisions are mine. **And it raises the bar on stating the reasoning, not lowers it** -- when nobody is going to say no, the written argument is the only thing left doing the checking.

- **FAMILY 1 -- THE CLASS OF THE DAY: "THE THING I HAVE IS NARROWER THAN THE THING I SAID." (cc's wording, which replaced mine.)** Eight instances between four nodes in one day, in eight materials. **The tell is uniform and it is in the REPORTING step, not the measuring one: the measurement was sound and the sentence carrying it was wider.** Sub-forms, each earned: a CLASSIFICATION standing in for a measurement (`keep`/`as-observed` read as v3's behaviour); a MEASUREMENT standing in for a wider one (one of a criterion's two conditions reported as the criterion); **an agreement asserted from a comparison never run** (I wrote "the four homes do not disagree; I checked the predicates" having checked only the RETIRE predicates -- they differed by one member, and it reached the canon); **a number cited from memory wearing a citation's clothes** (ic's, the better rule: _a sentence citing a NUMBER names where it was read_, and **a citation naming a FILE is not a citation, it is a promise that a file exists**); and **a LINE NUMBER cited from a peer's uncommitted tree** -- my sharpening of ic's, one level down. Also: **two boards agreeing is not two confirmations when one is reporting the other's claim.**
- **FAMILY 2 -- A CLEAN RESULT AND A BROKEN INSTRUMENT ARE THE SAME OUTPUT, SO EVERY SWEEP OWES A RUN IN WHICH IT IS KNOWN TO FIRE.** My differential reported a perfectly clean surface while `info` was demonstrably swallowing arguments, because `Location: <cwd>` made every comparison read "refused". **dc's completion is the better half and it is asymmetric: a wrong zero certifies absence, a wrong MAXIMUM certifies catastrophe -- and the second is far more persuasive, because it looks like diligence rewarded.** Nobody re-checks an instrument that has just found something big. Corollaries: **canary the POPULATION, not just the trigger**; put the population on the OK line, because _"nothing violated"_ and _"nothing was examined"_ are otherwise the same output; and **when the failure mode is silence, no classifier over the output can help -- you need two runs that OUGHT to differ.**
- **FAMILY 3 -- A MEASUREMENT IS BOUNDED BY THE QUESTION, AND THE MATRIX IS THE QUESTION.** A single instance gives you the wrong fix: from `wp start` reopening a Done WP I would have recommended one guard; the 18-cell matrix showed **no v2 lifecycle verb has a state guard at all**, so the fix is one shared guard and not seven. Same shape in the consumer sweep -- a list nobody swept **ends at last time's bug**, four callers became ELEVEN across seven files with five distinct policies. And in dc's vendoring count: **when the population is "what the people in the room have seen", every additional participant makes the count feel more reliable while leaving the frame exactly as narrow.** A detector beats a list because it does not have a room.
- **FAMILY 4 -- A GUARD'S PRECONDITION CAN BE INVALIDATED BY A CHANGE THAT IS CORRECT ON ITS OWN TERMS AND NEVER LOOKS AT THE GUARD.** 0042's total-failure branch became unreachable because `info` started rendering `<not set>` -- better for a human, non-empty for the parse. Same shape in 0047: **a machine ratification moved `st new` to Triage and silently defanged a display assertion that still reads as intentional coverage.** And in AC-11.4: **an evidence line that only relates artefacts to EACH OTHER can be satisfied about the wrong artefacts** -- consistency is not correspondence, so it needs a term binding the set to its origin. **A control refuses; documentation reminds.**
- **FAMILY 5 -- CLOCKS, AND I AM THE MEASUREMENT RATHER THAN THE AUTHOR OF IT.** Eight fabricated stamps, seven caught by me and **one refused by the guard before it could land**. The rule is not getting less true and I am not getting better at it, which is the entire case for a control over a rule -- measured on the author of the rule.
