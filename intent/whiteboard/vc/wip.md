---
node: vc
name: Validation Claude
role: validation
session_id: 7ae34f78-5b66-4872-a0b0-152af8cd6132
heartbeat_at: 2026-08-24 17:44Z
status: active
focus: "**FOLDED 2026-08-24 17:34Z FOR THE BOUNCE. Everything of mine committed and pushed, all three refs level at `24b396be`, ALL CI GREEN.** Landed today after the compact: **the drift guard now RUNS in CI instead of skipping** (`a38e884b` -- it reported green over nothing for its whole first day, positive control included); **`intent#0069` given a body**; **`0073`/`0074` filed** from prolix's consumer-estate findings; **the ETXTBSY flake fixed** (`6bbe287b` -- 4 of the last 12 rust runs, all one cause, and the remedy already existed in ONE of the THREE files that needed it). **Gate unmoved at 66 of 67 -- AC-08.5, cc builds / ic covers, untouched by me.** **TODAY'S ONE LESSON: FOUR TIMES A ZERO FROM MY OWN INSTRUMENT WAS THE INSTRUMENT, AND EVERY ONE WAS CAUGHT BY A POSITIVE CONTROL RATHER THAN BY RE-READING.**"
claims: [ST0056, ST0057, ST0058]
---

# Validation Claude (vc)

**Full traffic in long form: `.history/20260824/wip-fold-eod.md` (this fold) and `wip-fold-pre-compact.md` (the morning's). This file is the cold-session minimum.** Fold criterion is `laksa-vc`'s -- **a rule written around the shape of its past INSTANCES fails on a new shape of the same class** -- so instances are kept as EVIDENCE FOR a rule, never as rules beside it. This fold merges four separate watch-outs into one population rule and three into one instrument rule.

## DOING

**Nothing in flight. Folded for the bounce.** Tree carries nothing of mine: the only dirty paths are `AGENTS.md` + `intent/.config/config.json` (the escaped-mutator artefacts, routed to hv, deliberately untouched) and three peer boards.

## TODO

1. **PUSH DISCIPLINE IS A STANDING PROPERTY, NOT A TASK -- LEAVE THE REFS LEVEL.** Authority granted and standing.
2. **FIVE THINGS SIT WITH hv AND NONE OF THEM IS MINE TO CLEAR.** (a) dc's **routing question 2** -- I answered NOT DISCHARGED because the drift detector had a hole; the hole is closed, so my REASON is gone, **but a reason expiring is not a routing being discharged.** (b) The **escaped-mutator revert**: two files, both KEEP, both artefacts now correct. (c) `intent#0073`'s **six swift dispositions**. (d) `intent#0074`'s **heex-coverage question** -- WORK, not a fix. (e) `intent#0071` needs a **CHANGELOG entry and a v2 heading that does not exist**. **Reason for (c)/(d): `intent/plugins/claude/rules/` is SHIPPED SURFACE, so it needs hv before it needs an editor.**
3. **AC-08.5 is ST0057's last gate row.** cc builds, ic covers. Three burning cases live; only the attachment-setter clause is refuted.
4. **THE ETXTBSY FLAKE IS CLOSED ON THE POPULATION DERIVATION, NOT ON GREENS, AND MY EARLIER "watch the next few runs" WAS THE WRONG INSTRUCTION** (cc's correction, verified here). **The run count since `6bbe287b` is ONE and will stay one**: `rust.yml` is PATH-FILTERED to `native/rust/**`, `schema/**`, `surface/**` and itself, so whiteboard and canon commits **do not sample the flake at all** -- and a reader returning tomorrow sees an unbroken green board that has run the test ONCE. **One green is worth ~1.5-to-1, which is noise; 95% needs EIGHT consecutive** ((2/3)^8 = 0.039, driven). **So waiting on greens asks the run sequence to re-prove a MECHANISM that was never in doubt** -- the remedy was already proven in the sibling file over weeks. **What was ever wrong was the SET, and the set is now 3 of 3, derived workspace-wide with a positive control, all three verified to copy AND exec. Closed by construction rather than by observation.** Today's own rule arriving at its own verification.
5. **`--force` for `claude skills`** (ruled, queued); **`rulings_check.sh` is named after a field that no longer exists**; **the marker's per-crate staleness is NOT closed** -- both binaries agree today only because one change touched both packages.

## WATCH-OUTS -- RULES, NOT INSTANCES

- **THE POPULATION IS THE CLAIM, AND IT IS ALMOST ALWAYS ASSEMBLED FROM WHAT WAS IN HAND RATHER THAN FROM WHAT THE PROPERTY REACHES.** A fix, a figure, a roster and a revert list each name a SET, and the set is the part nobody checks. **Four instances, four different shapes, one day:** `eb4fe67c` fixed ETXTBSY correctly and its commit message says _"the ONE exec-of-a-just-copied-binary"_ -- **there were THREE, and the grep that would have said so was never run**; my own "three escaped-mutator files" were **TWO**, and a revert of my list would have destroyed dc's work; the parity roster outlived its instrument; a precision figure measured on a corpus that could not exhibit the failure. **THE FIX BEING CORRECT IS WHAT KEEPS IT ALIVE** (cc): a wrong helper gets rewritten by the next reader, **a right one with a correct diagnosis reads as finished work, so nobody asks how many places it belongs.** The true half is what a reader checks first, and it holds. **One `grep` for the discriminator settles it every time -- and the discriminator is rarely the obvious token** (cc: it is `fs::copy`, not `CARGO_BIN_EXE_intent` -- 26 of 29 files were never exposed).
  **AND IT REACHES THE RULES, NOT ONLY THE CODE -- cc's generalisation, and neither of us had said it out loud.** A REMEDY asserting completeness is a population claim about the failure modes it covers. **cc's clock rule said a second read is `THE ONLY THING` that catches a drifted stamp; my fabrication defeats it, because the read had already happened and the correct value was four lines above.** Two generators, one remedy: **theirs is ARITHMETIC -- a true reading advanced by feel, monotonic by construction, defeated by a second read. Mine is FABRICATION WITH THE CORRECT VALUE PRESENT, which NO read defeats.** Check A caught it only because the invention went FORWARD; **a fabricated stamp landing in the PAST passes all three checks, and the live channel has no door at all.** **The remedy for MY generator is not another read -- it is never typing the value: substitute it from `date -u` into the edit so the hand is out of the path.** Fifth firing of the population rule that day, and the first inside a REMEDY rather than inside a measurement.

- **A ZERO FROM YOUR OWN INSTRUMENT IS A CLAIM ABOUT THE INSTRUMENT UNTIL A POSITIVE CONTROL SAYS OTHERWISE. FOUR TIMES TODAY IT WAS THE INSTRUMENT, AND NOT ONE WAS CAUGHT BY RE-READING THE PATTERN.** Three distinct failure sites, and knowing which one you are in is the whole diagnosis:
  **(1) IT CANNOT REACH THE SUBJECT.** A wrong flag (`issues list --status all`; it is `--kind`, rc=1, empty stdout, and I nearly called a successful restore a failure). A wrong path. A corpus that cannot exhibit the failure. **Position blindness: Intent CONTAINS the rule library, so every check on it here passes trivially -- `intent#0073` was findable only from a consumer estate.**
  **(2) IT MATCHES REAL STRINGS THAT ARE NOT THE THING.** Branch ORDER, not pattern: a rule declaring _"No greppable proxy is authoritative"_ CONTAINS "greppable proxy", so testing the proxy first mis-binned all ten declarations as armed. **ANSI escapes between `Running` and the path, so `Running tests/` could never match a CI log.** cc's `CREATE ... TABLE [a-z_]+` stopping at a digit. **And the `"text"` key, which cc sharpened past my own statement of it: `criteria` uses THE SAME KEY as `attachments` -- 133 and 93 in ST0056.json, indistinguishable BY KEY and separable only BY PATH, so a structural comparison is not the careful option but the ONLY honest one, and a reader who "fixed" the grep lands on the same wrong answer with more confidence.**
  **(3) ITS INPUT IS NOT WHAT YOU THINK.** `gh run view --log` on an in-flight run returns _"logs will be available when it is complete"_ -- one line, rc=0 -- and searching THAT for the guard's tests found zero. **A conclusion about the SUBJECT drawn from a fact about the INSTRUMENT'S INPUT.**
  **THE ONE CONTROL FOR ALL THREE: ask it to find something you KNOW is there before believing it when it finds nothing.** Corollaries, each paid for: run a suspect flag alone and read ITS rc; **never `$?` after a pipe** (`|| echo` binds to `sed`, which exits 0 on empty input, so a confirmation arm can be dead code); confirm each subject appears in the `Running` list before believing zero failures; **and a contaminated control is invisible in its own output -- the only tell is that the answer was the one you expected.**

- **A HELPER CAN BE CORRECT IN EVERY RESPECT EXCEPT THE SHELL IT RUNS IN, AND THAT DEFECT REVIEWS CLEAN.** bats implements `skip` and `fail` by unwinding the shell they are called in, so the obvious factoring -- `v2="$(_require_surface)"` -- aborts the **COMMAND SUBSTITUTION'S SUBSHELL** and lets the test continue with an empty path. My first drift-guard draft did exactly that and compared 247 files against `""`: **maximum noise, in CI, unattended -- the precise failure dc warned me about, through a door neither of us was watching.** The old `|| skip` form was right and I "improved" it. **Keep the message in a variable and the `skip`/`fail` call in the test body.**
  **THE GENERAL FORM IS cc's AND IT PAID OUT ON MY OWN WORK: A REFUSAL SURVIVES WHAT A READING DOES NOT.** I read that helper carefully and it read as correct; **only attempting the failing configuration found it.** Same asymmetry cc's binary-side leg on AC-08.5 rests on.

- **A GUARD'S FAILURE MODES ARE NOT SYMMETRIC, AND A LOUD FAILURE IS NOT AUTOMATICALLY THE SAFE ONE -- the question is whether the loudness is TRUE.** A guard reddening on synced files is NOISY, and **noise is what gets dismissed: the real drift then hides inside the false ones, in an instrument everybody can see working** -- strictly worse than one that skips, because a skipping guard has not trained anybody to ignore it yet. **A rule that fires on correct code does not merely get ignored; it costs you the rules that ARE sound** (prolix-vc). **DETECTABILITY DECAYS: a guard whose predicate depends on WHEN it runs has a window that closes with nothing marking the moment.** **A capability with no consumer is not a gate, and a checkpoint with no failure mode it can catch is a QUEUE** -- which is why mechanical canon edits do NOT route through me and authored ones do: derived vs authored, not mechanical vs interesting.

- **A PATH-FILTERED WORKFLOW MAKES SILENCE AND SUCCESS IDENTICAL** (cc). `rust.yml` fires only on `native/rust/**`, `schema/**`, `surface/**` and itself, so a day of whiteboard and canon commits leaves an unbroken green board **that ran the suite once** -- and nothing distinguishes "passed repeatedly" from "was never asked". **It is the same shape as the hv inbox with no named reader: an absent signal reading as a positive one**, and as a guard that skips in CI while printing `All tests passed!`. **Before treating an accumulating green as accumulating evidence, check that the workflow SAMPLED the thing** -- count the runs, not the colour.

- **A WARNING IS NOT DISCHARGED BY BEING TRUE, AND NOT BY BEING CURRENT WHEN OBSERVED EITHER** (ic). **In a five-node checkout the subject MOVES between noticing and sending, so the measurement that counts is taken at the moment of SENDING.** Three accusations against me died on re-measurement inside one stretch and **every one was true when ic noticed it** -- my `wip-fold-eod.md` really was untracked when they looked and was tracked by the time the warning would have arrived. **Re-drive at send time, not at notice time**, and this is the same clock as ic's stale-reference finding: **the cost of a pause is that your evidence ages while your confidence does not.**

- **AN ESCALATION IS FINISHED WHEN A NAMED READER HAS IT, NEVER WHEN THE WRITE RETURNS.** The escaped-mutator revert sat on MY board as _"Decision still hv's"_ and in dc's commit message as _"the revert is hv's ruling"_ -- **correctly recorded by both of us, delivered by neither**, and hv's inbox held zero mentions. **Every write returned 0.** The roster names ME as the node obliged to surface hv-channel content TO hv, so this is not a gap in the mechanism but me not executing the obligation it assigns. **The tell: a TODO item whose text describes WHOSE decision it is. That phrasing feels like routing and is a description of routing.**

- **CORROBORATION IS ONLY REAL IF THE INSTRUMENTS COULD HAVE DISAGREED. ASK: COULD EACH HAVE PRODUCED THE OTHER'S FINDING?** Unanimity across nodes reading the SAME instrument is one reading counted four times. **`prolix-cc` and `prolix-vc` routed `intent#0073` an hour apart, different sessions and different file counts -- and are ONE INSTRUMENT REPORTED TWICE**, same estate, same runner; banking them as two estates confirming would carry one reading with twice the confidence. Recorded in the row as a duplicate. **AND THE DISAGREEMENT IS THE PAYOFF, NOT THE AGREEMENT:** my elixir control returned 19/0 against their 9/10 and **the disagreement located the bug -- in MY checker, not in the subject.** **What a real one looks like:** ic's `for-each-ref` taken at pickup for an unrelated purpose BEFORE the claim existed -- independent BY CONSTRUCTION, and it cannot be manufactured. **A relay is not a second witness; tell the RECIPIENT to expect one copy, because the recipient can dedupe and the relay can only be beaten.** **AND ATTRIBUTION FAILS BOTH WAYS THROUGH THE SAME CHANNEL: NAME WHICH HALF** -- an incident and its generalisation are separable and usually have different authors.

- **A TRUE MEASUREMENT OF A DIFFERENT PROPERTY, OFFERED AS PROOF, IS THE HARDEST ONE TO SEE -- THE EVIDENCE BEING REAL IS WHAT MAKES IT PERSUASIVE.** **CORRECTNESS AND CURRENCY ARE INDEPENDENT AND ONLY ONE IS EVER CHECKED**; **AGREEMENT is not CURRENCY** (ic); **a PROOF ABOUT ONE PATH IS NOT A PROPERTY OF A CLASS**; **a BRANCH POINT IS A FACT ABOUT HISTORY AND NEVER AN ANSWER ABOUT NOW**, like a released TAG read as evidence about a DEPLOYMENT. **A PRECONDITION THAT HOLDS TODAY IS NOT A PROPERTY**: `upstream/v2-maintenance` matches the live v2 checkout 245 of 245 **only because the 2-commit gap happens to sit in `bin/.devbin/`, which the walk excludes -- luck, and nothing pushes that branch.** **A QUOTATION IS TESTIMONY ABOUT A DOCUMENT, NOT THE DOCUMENT.** **AND A ONE-SIDED QUESTION OF A TWO-SIDED CRITERION IS COMPLETE, CORRECT, AND STRUCTURALLY BLIND.**

- **AN ATTESTATION CLOSES A QUESTION A GAP WOULD HAVE INVITED, AND RIGOUR IS THE DISGUISE.** **A record that CLOSES a question it did not ANSWER is worse than no record**, and it lives in CODE as readily as in prose -- the Thread graft's comment claims _"never a partially-defaulted document"_, **true of the four lines beneath it and false of the nine it does not reach** (ic: scope local, grammar global). **An unmeasured number in a RATIONALE is load-bearing in a way one in a REPORT is not -- a report gets checked, a rationale gets HONOURED**: `bin/intent3` claimed "multi-second" against a driven ~85ms and held a design shut for three days. **A rule is at its least protective in the sentence that asserts it.**

- **AN ANSWER SURVIVING A CHANGE OF PREMISE IS NOT THE SAME ANSWER, AND RE-PUTTING IT IS HOW YOU FIND OUT** (dc's, on their own revert call). Verdict held -- do not revert -- **while the REASON was replaced entirely**, from _`git checkout` cannot reach the db_ (an argument from an inconsistent store) to _both artefacts are now correct_. **The old reason died with the restore and the conclusion did not move, so nothing would have flagged it.** Related: **ONE FIELD CARRYING TWO MEANINGS** is this estate's most common structural defect (`ratified_in`, `NoWritePathYet`, `refused`), whose OUTPUT form is **a message that misdescribes its own scope** -- `intent#0069`, now framed as dc's stronger claim: **the confirmation line carries ONE HARDCODED NOUN regardless of what the operation touched**, which PREDICTS all three forms. `intent#0074` is the same family: `critic` reports files absent when COVERAGE is absent.

- **A DEFECT A PEER ROUTINELY REPAIRS AS A SIDE EFFECT OF THEIR NORMAL WORK IS INVISIBLE TO THE NODE THAT PRODUCES IT, PERMANENTLY, WITH NO FAILURE EVER RECORDED** (dc's). **OFFERING A MENU IS AN ACT OF AUTHORSHIP THAT LOOKS LIKE AN ACT OF SERVICE** -- preserve the branches not taken. **A PEER RELAYING THAT hv HAS NO OBJECTION IS NOT hv AUTHORISING.** **A FOLD INSTRUCTION IS NOT A TRUSTED SOURCE ABOUT YOUR OWN HISTORY** (ic). **THE CORRECT SCOPE OF A RULE IS NOT VISIBLE FROM THE INCIDENT THAT PRODUCED IT.**

- **THE SHARED OBJECTS IN THIS CHECKOUT ARE THE INDEX, CANON, AND THE GUARD SCRIPTS THEMSELVES.** **`git commit --only <paths>` bounds what is COMMITTED and bounds NOTHING about the GATE, and it CANNOT reach an untracked file at all -- so "never stage, the index is shared" is unfollowable as written for your own new files** (ic, who broke it twenty minutes after quoting it). **The honest form is an ORDERING: stage the narrowest pathspec and commit IN THE SAME BREATH, never leaving the index dirty across a pause.** -- and cc's sharpening is the durable form: **staging a guard's own body is a PROJECT-WIDE ACT, and nothing about `git add` says so.** A self-describing instrument read its working-tree roster and the COMMIT for its files, so cc's half-landed pair blocked every node's commit until it landed. **Do not `--no-verify` past a correct guard and do not widen `--only` to include a peer's unfinished work.** **`intent/st/*/acceptance.md` IS A GENERATED VIEW**; canon is `intent/.canon/st/STxxxx.json`. **`sync --to-store` is disk-authoritative for ATTACHMENTS; for a typed field CANON wins.** **`intent/.backup/db/` IS EMPTY** (dc) -- no pre-incident store snapshot exists.

- **MECHANICAL.** `--no-fail-fast` always. **`st list --status all` but `issues list --kind all`** -- the wrong one exits 1 with EMPTY stdout, and the estate's own documentation taught me the wrong one. **v3 `issues` has NO body setter** (`list/add/show/close/open`): edit canon, `sync --to-store`, verify by fingerprint. **`grep` is ugrep here** -- `-E` throughout, `grep -c` exits 1 on zero. **zsh does NOT word-split unquoted `$var`** -- a path list in a variable reaches `git commit` as ONE argument and fails; list paths explicitly. **NEVER `$?` after a pipe.** **`bash -n` cannot parse a `.bats` file** -- `@test` is not shell. **`cargo clippy --workspace --all-targets` exceeds a 2-minute tool timeout.** **Read the clock, then PASTE -- per stamp, never per session.** **PAID FOR AGAIN AT THIS FOLD: I ran `date -u`, read `17:39Z`, and wrote `17:45Z` into the same board edit -- six minutes ahead, fabricated, IN THE SAME TOOL CALL AS THE READ.** So the generator is not a stale session value and is not carelessness across time; **it is the hand typing a plausible number while a correct one sits four lines above it.** The clock guard's check A would have refused it at commit (a future stamp), so the mechanism works -- **but a guard at the commit door does not help a stamp sent over the live channel, and the estate has already measured that transcription is how those enter the record.**
  **AND ic HAS THE THIRD FACE, WHICH IS ABOUT JUDGING SOMEONE ELSE'S STAMP: A STALE REFERENCE NEVER ACCUSES ITSELF.** ic was one message from reporting my `17:37Z` as fabricated -- **their last read was `16:18Z`, making me look ~75 minutes fast, almost exactly the Lamplight local-plus-`Z` signature.** A fresh read said `17:39Z`; I was right to within two minutes and **the entire discrepancy was the reference.** The rule says READ PER STAMP and does not say that **JUDGING a stamp IS a stamp-sized act and owes its own read.** The asymmetry is what makes it dangerous: **a stale reference can only ever surface as THE PEER BEING WRONG, in exactly the words a real defect would use.**

## DECISIONS -- LIVE ONLY

- (2026-08-24) **hv: THE Intentv2 FREEZE IS SCOPED -- FROZEN FOR FEATURES, LIVE FOR SHIPPED-SURFACE DEFECTS.** Gives the drift guard its property: **not _these two trees agree_ but _either in both or declared_**, which decides which of its two exception kinds a new entry gets. Options were vc's, choice hv's; branches not taken are on hv's board.
- (2026-08-24) **hv: `intent3` MAY REFUSE AN INCOHERENT BINARY; dc DRAFTS IT.** **A dirty marker cannot ground a refusal and cannot be discharged -- it can only downgrade one to a permanent unclearable warning** (dc).
- (2026-08-24) **vc: NO CHANGELOG ENTRY FOR A DEFECT INTRODUCED AND FIXED INSIDE ONE UNRELEASED CYCLE.** There is no reader for it, **and the document agrees -- the `3.0.0` block has no `Fixed` section while 35 release headings below it do, so the line would require CREATING the section to hold it.** That is the tell. **`intent#0071` is the counter-example and is hv's: 2.19.0 SHIPPED it.**
- (2026-08-24) **vc: MECHANICAL CANON EDITS DO NOT ROUTE THROUGH ME; AUTHORED ONES DO.** The line is **DERIVED vs AUTHORED**, not mechanical vs interesting. An attachment hash is derived from a file the builder committed and `canon_commit_check` already gates it; criterion text, `state`, `status`, notes and `covers` are the contract and route to me.
- (2026-08-24) **RETIRED: the `git rev-parse v2-maintenance` trap.** hv ruled the stale local branch deleted; a silent wrong answer is now a loud absent one. **Use `upstream/v2-maintenance`.**
