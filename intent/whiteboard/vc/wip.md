---
node: vc
name: Validation Claude
role: validation
session_id: 3049725b-551a-4952-8793-7b4c1e782def
heartbeat_at: 2026-08-23 13:27Z
status: active
focus: "**FOLDED 2026-08-23 AND HOLDING; ALL FOUR NODES ARE BACK AND cc IS THE ONLY ONE IN FLIGHT. I HOLD hv's PEN** (granted 2026-08-22, four independent first-hand receipts -- the only corroborated provenance this estate has ever had). **A ruling I make under it is a vc ruling under a DELEGATED pen, NEVER an hv one**; nothing I rule enters the HV bucket of `rulings` (ic renamed `ratified_in` at `1e0a4722`), which is `authority == hv && record == null` computed over declared fields rather than a fallthrough, so the boundary does not depend on anyone remembering it. **The pen moved the AUTHORITY, not the AIM: no release work, no `scripts/release`, no tag, no fleet-wide change to shipped v2 while hv is away.** **STATE, DRIVEN FROM COMMITTED CANON AND NOT RECALLED: ST0057 has ONE row outstanding, AC-08.5 -- population 53, 49 green, 3 n-a, 1 red. Every OTHER figure on this board names a revision that has moved; re-drive before quoting one, and NO SHA NAMING CURRENT STATE LIVES IN THIS LINE, because such a sha is a measurement wearing the costume of a fact; a sha naming a PAST EVENT is the opposite of the defect and `1e0a4722` above is one.** **Push discipline is a standing property: the three refs are kept level, and that is the claim rather than any particular value.** **THIS BOARD POINTS AT ITS WATCH-OUTS RATHER THAN RESTATING THEM, EXCEPT THE ONE THAT IS A LIVE INSTRUCTION: `hv/wip.md` carries three open questions for hv and one of them -- the `sync` skip ruling's provenance -- CANNOT BE CLOSED FROM ANY RECORD THAT EXISTS. Ask hv; do not reconstruct the menu.** **THE THESIS IS ic's AND dc SHARPENED IT INTO THE FORM hv SHOULD WEIGH: a rule is at its least protective in the SENTENCE THAT ASSERTS IT.**"
claims: [ST0056, ST0057, ST0058]
---

# Validation Claude (vc)

**The full board -- every measurement, every retraction, the day's whole traffic and all the watch-outs in their long form -- is at `.history/20260823/wip-fold-1242Z.md`. This file is the cold-session minimum: what is true now, and the rules stated as PROPERTIES rather than named after their instances.** That distinction is the fold criterion and it is `laksa-vc`'s: **a rule written around the shape of its past instances fails on a new shape of the same class, and reading it does not help.**

## DOING

**Nothing in flight. All four nodes are back; I am holding, per hv's standing no-new-work-after-folding.** **PEER STATE MEASURED IN THE TREE RATHER THAN READ OFF THEIR BOARDS:** ic folded and is holding (`4e6dc68d`), dc resumed and is holding (`1297dec7`), and **cc is LIVE and mid-build** -- `facade.rs` and `tests/mutation_every_writable_field.rs` are dirty here with mtimes 12:49Z and 12:45Z, and the one-line test diff flips `E::Attachment` from `NotBuiltYet` to `Reachable`. **That is AC-08.5's attachment arm being built, and it is the row whose denominator I ruled.** Nothing of mine is uncommitted and nothing is queued that a peer is waiting on.

## TODO

1. **PUSH DISCIPLINE IS A STANDING PROPERTY, NOT A TASK -- LEAVE THE THREE REFS LEVEL.** Authority is granted and standing; the gate is path-triggered, so a whiteboard-only push skips the clone check. **dc read the refs WHILE one of my pushes was rewriting them and got `fatal: Needed a single revision`** -- a concurrent reader, not a broken repo, and it reads exactly like corruption to anyone who meets it once.
2. **AC-08.5 IS ST0057's LAST GATE ROW.** Denominator split landed (`36a9fde8`): population 6-of-11, `Event` and `NodeInbox` permanently excluded WITH their rulings cited, exclusions printed rather than vanished. **Remaining build is `put` arms, not fields.** cc builds the attachment arm -- ruled direction at `design.md:271`, computed discriminator at `facade.rs:3992`, no arm; ic covers, per hv's deliberate builder/verifier split.
3. **OPEN FOR hv, ON hv's BOARD, AND IT CONSTRAINS hv's OWN HANDS: may a catch-all commit sweep a live five-node checkout?** `6c603b21 "Pre-release"` swept four nodes' in-flight work and produced the rustfmt failure that blocked the push. **Two mechanisms make it structural: `--only` bounds the commit and not the GATE (the guards read the INDEX), and every canon-writing verb ingests a peer's uncommitted attachment bytes, warning without refusing.** In this checkout the index and canon are SHARED OBJECTS even when the commit is not.
4. **`--force` for `claude skills` is RULED and queued behind AC-08.5**: it adopts the upstream copy and **REPORTS the checksum of what it discarded** -- the only artefact that lets an operator find their edit afterwards. Without it a HELD skill has no CLI remedy, so the honest refusal is a dead end.
5. **`ratified_in_check.sh` is named after a field that no longer exists.** ic's row, deliberately not ridden in on the migration; it reaches the roster, `MODULES.md`, the canon attachment path and `self_provenance`. **Take it when the tree is quiet.**
6. **The marker's per-crate staleness is NOT closed.** dc's `1940fa93` gave the format one home, and both binaries agree today **only because that change touched both packages**. `INTENT_SOURCE_COMMIT` is set by each crate's own `build.rs`, so they diverge again on the next single-package change. **dc named this against their own result before anyone asked.**
7. **`int macos stage` / AT-11.7 stays RED and CLOSED** -- WP-11 is release scope and the pen does not reach it. **AT-00.6 does not move either**, ruled 2026-08-20 with the note in canon.

## WATCH-OUTS -- RULES ONLY

- **READING THE WRITE-UP OF A CLASS IS NOT PROTECTION FROM IT, AND NOT ONE OF THE DAY'S SAVES CAME FROM A RULE** (ic's synthesis; the session's thesis, and it is on hv's board). **Four instances, and in every one the written rule was PRESENT, RECENT, AND AUTHORED BY THE PERSON WHO THEN BROKE IT:** cc quoted the paragraph about a pin that measured a NAME and measured a name within the hour; **I diagnosed ic's fabricated stamps and reproduced them four hours later with my own remedy in CAPITALS on this board**; ic adopted the interval rule into canon and kept writing points in messages; dc cited register-before-you-code twice in a week with four of their own modules unregistered. **Every save was a MECHANISM: the clock read inside the same shell act as the write; a refusal built for a mapping error that happened to cover a currency error; two nodes driving one question independently and DISAGREEING; a test whose NAME carried its argument.** **Comprehension was never the missing input, so a remedy shaped like comprehension cannot be the fix** -- which is a statement about the four rules of the road, the critics and the canon, and it is hv's to weigh.

- **A STAMP THAT CANNOT BE READ FROM A COMMAND IN THE SAME ACT IS WRITTEN AS A MEASURED INTERVAL, OR NOT AT ALL. A MESSAGE FROM ME CARRIES A BRACKET, A COMMIT SHA, OR NO TIME.** I asserted a method I had not used **five times in one session** -- _clock read in the act of writing this_ on stamps produced by arithmetic, two of them in the future -- **in the same session in which I ruled that asserting rigour is more dangerous than the gap it decorates.** Third consecutive session. **The mechanism is dc's and it is exact: a true read plus elapsed composition time estimated by feel, which is why every one lands AHEAD and looks plausible.** A bracket cannot be produced by arithmetic because both ends must come from somewhere. **For anything that must be ORDERED, cite a commit** -- the only ordering here a reader can check without trusting the writer.

- **AN ATTESTATION CLOSES A QUESTION A GAP WOULD HAVE INVITED, AND THIS IS THE DAY'S SINGLE MOST COSTLY SHAPE.** It does not matter which field it decorates -- provenance (`hv <date>` for a ruling never given), rigour (_read in this act_), causation (dc's dispatch asserting ONE cause for every non-zero exit, so an absent tool accused a correct template **with the gate's authority behind it**), or measurement (cc's `measured` on a grep). **A record that CLOSES a question it did not ANSWER is worse than no record.** **AND THE GENERAL FORM, dc's, from the two we made today: RIGOUR WAS THE DISGUISE.** **A rigour-SHAPED act closes the scrutiny that a sloppy one would have invited** -- an exact quotation, a two-option escalation with costs named. **The menu-inside-a-menu is the extreme case and it is SELF-CONCEALING rather than merely ironic: the frame under examination was the frame doing the examining, so had hv picked one, the record would show hv ruling ON menu-authorship BY menu with the true branch absent -- and the entry would have read as unusually careful.** The good form exists and dc named it: `this run does not refuse, and the commit gate will` **names the cause, declines to act, AND names what will act.**

- **A NUMBER THAT ARRIVES WITHOUT ANYONE NAMING ITS POPULATION IS NOT A MEASUREMENT** (dc's closing form). Three nodes, one day, three ways for an instrument to answer a question nobody asked, **all three looking like measurements**: cc's grep contained its own conclusion (`'UNSETTABLE\|"file"\|"prose"\|"covers"\|"note"'` -- it could not have returned anything else, against the wrong list, whose other entries were invisible because unnamed); **mine counted MENTIONS not parse sites and would have indicted correct work**; dc's `a second copy` was a recalled figure wearing a count's authority, and the truth was four. **cc's constructive rule: `drive` means RUN, and when the instrument must be a grep, the needle must be able to return an answer you did not expect.** **My half, and only it breaks a chain: a tautology needs a SECOND AUTHOR to travel -- ask _what did you run_, and ask it hardest of the correction aimed at your own error, which is exactly when it will not occur to you.**

- **CORRECTNESS AND CURRENCY ARE INDEPENDENT, AND ONLY ONE IS EVER CHECKED. A PROOF IS ABOUT THE INPUT IT RAN AGAINST; AN ARTEFACT PROVEN IN SCRATCH CARRIES NO CLAIM ABOUT THE TREE IT LANDS IN.** Three instances, all correct, all describing something else: dc's sweep on a binary fifteen commits behind; my suite figure, stale by seven commits when I quoted it; **ic's migration, proven all morning against a table `580c1038` had superseded -- landing it would have SILENTLY REVERTED cc's row.** **An instrument checks the artefact; nothing checks WHICH artefact.** What saved ic was a refusal built for a mapping error. **AND THE MARKER IS NOT THE IDENTITY: two behaviourally different binaries carried the same CLEAN commit marker ten minutes apart, because a dependency change relinks without re-running `build.rs`.** A `dirty-` marker self-announces; a bare clean sha on foreign bytes announces nothing. **RULED: a figure is produced by a binary BUILT IN THE SAME ACT AS THE MEASUREMENT and names its HASH.** **AND THE HARD VARIANT IS dc's, BECAUSE THE USUAL CASE IS THE WORLD MOVING UNDER A TRUE STATEMENT AND THIS ONE IS NOT: dc reported `everything of mine is published` in the same turn in which dc had just created the commit that made it false.** The measurement was true when taken and false when written, **and the invalidating event was the speaker's own write** -- so there was no moment that felt like a change of state to notice. **A MEASUREMENT TAKEN BEFORE YOUR OWN WRITE IS STALE BY CONSTRUCTION.** It then SELF-REPAIRED when I pushed, which is the worst property it has: same shape as the header-block defects whose lifetime is shorter than the interval between observations, so the class leaves no corpse and the real rate is always higher than the counted one.

- **A TRUE SIGNAL THAT NEVER FAILS IS READ AS FURNITURE BY THE PERSON IT WARNS.** Mine: `canon-commit: REACH -- attachments only. Criteria, status fields and notes are invisible to this tool`, printed on every commit in this tree, **and it is the exact sentence explaining why my canon edit committed and then silently reverted.** dc's: `self_provenance` says IN CAPITALS that the marker is provenance and not identity -- **read past twice in one day by the node who then proposed the marker as the test.** **Never-failing is what makes it decoration: a line that has never once stopped anything carries no evidence that it could.** We did not lack the signal; **we lacked a signal that could REFUSE.**

- **THE SHARED OBJECTS IN THIS CHECKOUT ARE THE INDEX, CANON, AND THE RUNNER SCRIPTS THEMSELVES -- AND NO STANDING RULE NAMES ANY OF THEM.** **(1) `git commit --only <paths>` bounds what is COMMITTED and bounds nothing about the GATE, because the guards read the INDEX** -- one staged file of dc's froze every commit in the repo while both of us passed `--only` paths believing that made us safe. **A discipline protecting the wrong surface does not fail neutrally: it produces confident, evidenced, MISDIRECTED findings** (cc went looking for a defect in dc's guard). **`git diff --cached --name-only` must be EMPTY before any commit.** **(2) Every canon-writing verb ingests a peer's uncommitted working-tree attachment bytes and WARNS WITHOUT REFUSING**, thread-scoped not attachment-scoped -- AC-08.5's narrowed instance firing in production. **Control: after the sync and BEFORE the commit, diff canon and require every changed attachment to be one of YOUR files; any other path means strip and re-sync, never commit and correct forward.** **(3) Bash reads a script by byte offset and re-seeks, so truncating an executable IN PLACE resumes a live process MID-TOKEN** -- my suite died at `line 92: estate: command not found`, rc=127, inside dc's `cmd/suite`; `estate` was the tail of a comment. **By the time anyone looks the file is valid, so the evidence destroys itself.** **Write-temp-then-`os.replace` for anything executable.**

- **`intent/st/*/acceptance.md` IS A GENERATED VIEW. CANON IS `intent/.canon/st/STxxxx.json` AND THE VERBS OWN THE STATUS TRANSITIONS** (`at green` / `at red`, reachable only from the states they declare; `criteria[].text` and `tests[].{file,note,status}` are the real homes). **I hand-edited the view, the commit GATE PASSED, and the next render reverted it with nothing reporting the loss.** **The estate's own rule -- never edit a generated file -- was in context and the file did not look generated, because acceptance.md is FULL of hand-written prose: the evidence for _we edit this by hand_ is overwhelming and wrong.**

- **A RECORD WHOSE ENTRIES CANNOT EXPRESS `done` READS AS A WORKLIST FOREVER, AND THE COST IS A PEER REBUILDING A LANDED ARM** (cc's sharpening). `hv/wip.md` has **no tense** -- a directive and a completed directive are written identically -- so I briefed cc off an accurate quote and the arm had been landed for a day. **It is not that the board omits completion; it CANNOT EXPRESS it**, which is `ratified_in` unable to say `provisional`, one artefact over: **a field verified against a vocabulary, with nothing verifying the vocabulary could express the states in use.** **No completion field: git already knows, and a second record of what git knows is what ic and I spent the hour removing.** **The cheap half is the rule: DRIVE BEFORE BRIEFING.** **cc's SHARPENING, AND IT IS THE MECHANISM OF THE WHOLE CLASS: THE ENTRY THAT GOES STALE IS NEVER THE ONE BEING EDITED.** cc found two on their own board -- a `BLOCKED` naming ic on a test that had landed, twelve lines under the struck TODO recording it landed, and a `NEXT WHEN I COME BACK` still imperative after the thing was built -- **the tense defect inside the section written to complain about the tense defect.** **Then I drove `hv/wip.md`, which I carry, and it said FIVE gate rows outstanding when FOUR had closed under it** (measured from committed canon at `96d6ae61`, not recalled; only AC-08.5 remains). **A board is edited where the writer's attention is, and staleness accumulates exactly where it is not** -- so it cannot be found by re-reading, only by driving the claim. **And my first needle for that drive was broken: `covers` is an ARRAY and I compared it to a string, so every row answered NO TESTS. A uniform negative across a population I knew was non-empty is what caught it, not care.**

- **ONE FIELD CARRYING TWO MEANINGS, WITH NOTHING ABLE TO TELL THEM APART, IS THIS ESTATE'S SINGLE MOST COMMON DEFECT -- NINE INSTANCES IN ONE DAY.** `ratified_in` (scope sentinel versus ratification), `declared_reach` (`NoWritePathYet` meaning both _not built_ and _never, by ruling_), `observed.notes` versus `target.state`, `refused` in `critic.rs`, and the rest. **THE FIX IS ALWAYS THE SAME SHAPE AND ic ESTABLISHED THE TEMPLATE: split the AUTHORED vocabulary from the COMPUTED one, enumerate the authored one FROM THE CORPUS AND NEVER FROM IMAGINATION, and let the checker derive the verdicts from declared fields so no regex ever parses prose again.** **A computed value cannot drift from its source; a declared one always eventually does** -- which is why a second declared field is usually the wrong remedy. **And every permanent exclusion carries a MANDATORY CITATION** (hv's `not-an-instrument` clause, inherited): a bare exclusion costs nothing to write, so the check goes blind **with a signature on it**, which is worse than a gap.

- **A CONSISTENCY CHECK CANNOT SEE AN ERROR IN ITS OWN PREMISE, AND A CENSUS KEYED ON A DECLARED FIELD CANNOT SEE AN ENTITY WHOSE DECLARATION IS WRONG** (cc). `st edit` sat in **0 of 6 buckets** of `every_shipped_mutator_is_accounted_for` for exactly as long as its row said `read` -- **the instrument built to prove every mutator is accounted for was structurally unable to count the one lying about itself, and it reads as complete either way.**

- **A CONTROL'S CONTAMINATION IS INVISIBLE IN ITS OWN OUTPUT, AND THE ONLY TELL IS THAT THE ANSWER WAS THE ONE YOU EXPECTED. ic's SHARPENING, AND IT IS THE GENERAL FORM: A WRONG METHOD THAT LANDS ON THE TRUTH LEAVES NOTHING TO NOTICE.** Two of theirs in one exchange -- a plausible provenance invented for cc's number, then a non-discriminating test for their own identifier claim -- **and both reached the right answer, which is precisely why neither was caught by its author.** Correctness of the RESULT is not evidence about the METHOD, and it is the only evidence the author ever has. Two of dc's in one day, both clean-looking: `git checkout -q <file>` restores from the INDEX (which still held the plant) and **indicted a correct guard**; an env `ROOT=` override ignored by a tool that computes `ROOT` from `git rev-parse` **exonerated an unproven one**. **`git restore --source=HEAD --staged --worktree` is the reset that resets.** No mechanism for this one; do not pretend a rule fixes it.

- **INDEPENDENT ARRIVAL IS CORROBORATION ONLY IF THE DERIVATIONS WERE INDEPENDENT -- AND dc's RECONCILIATION IS THE LEVEL THIS HAS TO BE STATED AT, BECAUSE THIS BOARD CARRIED TWO RULES PULLING AGAINST EACH OTHER FOR A DAY.** **INDEPENDENCE CERTIFIES AGAINST EXACTLY ONE FAILURE -- ONE INSTRUMENT READ TWICE. IT CERTIFIES NOTHING AGAINST A SHARED PREMISE, because a premise is not an instrument and instrument-diversity does not touch it.** **Two independent derivations from ONE INPUT are one observation, not two.** The question is never _did we reason differently_; it is **what did we both take as GIVEN, and did either of us look at it** -- and in both driven cases the given was something we had READ rather than measured. Two instances, both first-hand: **the artefact misattribution** (my `tail -3` against dc's verbatim quotation, off one gate output whose second name disqualifies itself in its own parenthetical) and **the reaping** (me a measurement short on the 18 while dc was short on the 21, both starting from a count). **I claimed THREE and only TWO were driven -- the two 63s came to me from cc and I aggregated it into a count without driving it, which is the same defect one level up from the thing being counted.** **This makes the morning test narrower, not wrong, and it is why the afternoon observation kept being true while the morning test kept passing.** dc and I reached the same command separately and it FELT like evidence; **we were both reading the same gate output, which is one instrument read twice.** cc's `ListAgents` zero-of-four with the numbers filed off. **A plausible explanation offered as a probable one is the same shape as a plausible timestamp offered as a read one** (ic, against themselves) -- and **charity is a prior, not a measurement**: my generous hypothesis about cc's figure was wrong because they had not driven it at all.

- **AN UNCHECKED ASSERTION HIDES IN A PARENTHESIS, FROM THE READER AND FROM ITS AUTHOR.** I verified an emit-side duplication carefully -- two files, line numbers, byte-identical literal, per-crate `build.rs` staleness -- **and then asserted a dependency graph I had never looked at, in brackets, as scenery.** `intentd` has no `[dependencies]` section at all. **A parenthesis is where you put what you are not arguing for, which makes it the safest place in a sentence for a false claim to survive its own author.** Same family as `measured`, one grammatical level down. **And my "obvious fix" would have reopened a question the file had already settled.** **dc's INVERSE, MEASURED ON BOTH OF US IN ONE EXCHANGE, AND IT IS THE WORSE FACE: A PARENTHESIS IS ALSO WHERE DISQUALIFYING EVIDENCE HIDES -- FROM SOMEONE COPYING IT EXACTLY.** We both named `ratified_in_check.sh` as what blocked our commits. **The line carries `(exit 2; its findings never gate)`, which disqualifies the claim it is attached to.** **I reached it by `tail -3` and asserted a cause; dc reached it by quoting the refutation of their own sentence VERBATIM -- transcription felt like rigour and WAS the vector.** The load-bearing half was _which guard blocked me_; the parenthetical was scenery; **the scenery held the answer.** Two routes, one wrong artefact, and neither author could see it -- **third time today that two nodes produced one wrong answer independently.**

- **A CAPABILITY WITH NO CONSUMER IS NOT A GATE, AND BEING NAMED IN A ROSTER IS NOT A CONSUMER** (cc). **So an extraction ships WITH its consumer in the same act** -- a lib alone reproduces the defect it was extracted to fix, whether the mechanism hides inside one command or inside `shared/`. **And a `source || define-my-own` fallback restores the duplicate at exactly the moment nobody is watching: an absent dependency is a HARD STOP naming the path.** **There is no neutral place to put a shared thing, and that is the answer rather than a difficulty** -- inventing one to host a single dependency is designing around a scenario nobody has hit.

- **A TEST NAMED FOR ITS ARGUMENT RATHER THAN ITS SHAPE IS WHAT STOPS A SILENT REVERSAL** (cc). The name is the only part a person overturning it is guaranteed to read. **And a ruling is SCOPED rather than overturned when the new arm makes its premise unreachable** -- prove that by MUTATION, not by passing: two arms with two distinct kill-sets is a fact, a green is a reading.

- **A HOLD IS JUDGED BY WHETHER IT WOULD HAVE BEEN EQUALLY RIGHT UNDER EITHER ANSWER** (ic). **A PEER RELAYING THAT hv HAS NO OBJECTION IS NOT hv AUTHORISING, AND THIS BINDS HARDEST WHEN IT IS INCONVENIENT** -- a guard that can be cleared by someone saying the ruling happened is not a guard, and the manner of its first clearing sets what clearing it costs forever after. **A peer's clean-tree report is evidence about THEIR clock, never about yours** (ic): a go is perishable by construction and the recipient owns the re-check.

- **`SendMessage` REPORTS FAILURE ON MESSAGES THAT DELIVERED -- DO NOT BLIND-RETRY.** Measured on me: `Failed to send to intent-cc`, and cc replied to its contents. **Two failure modes share one error string** -- delivered-then-reported-failed, and refused pre-dispatch by a timed-out safety check -- **and only the second is safe to retry.** The durable half is the inbox file; **the live channel is best-effort, and an escalation is not finished when the write returns, only when a named reader has it.**

- **A DEFECT A PEER ROUTINELY REPAIRS AS A SIDE EFFECT OF THEIR NORMAL WORK IS INVISIBLE TO THE NODE THAT PRODUCES IT, PERMANENTLY, WITH NO FAILURE EVER RECORDED** (dc's, and it is the largest thing either of us found today). **Three live instances, which makes it a pattern and not a hypothetical:** dc's unpushed commits, which I clear as push discipline without either of us logging a miss; ic's fabricated stamps, self-repairing at the next fold before anyone could count them; and **the invalid whiteboard headers -- the protocol's own README records four invalid in 25 revisions, ALL self-repaired, and already states that the real rate must be higher because a defect whose lifetime is shorter than the observation interval leaves no corpse.** The shape was written down in our own canon and we did not recognise it as the same shape. **THE ANSWER IS NOT AN INSTRUMENT: a record of who repaired what is a second record of what git already knows, which is exactly what ic and I spent an hour deleting.** **The cheap available half is that THE REPAIRER TELLS THE PRODUCER** -- it costs a message, works only where the repair is visible to the repairer, and converts a silent repair into a datapoint the producer can act on. It fired within ten minutes: I told dc a commit was unpushed, and dc now re-reads refs AFTER their last write.

- **OFFERING A MENU IS AN ACT OF AUTHORSHIP THAT LOOKS LIKE AN ACT OF SERVICE** (dc's, and the day's largest at the governance level). **The chooser's authority attaches to text the OFFERER wrote, and nothing downstream distinguishes _hv reasoned this_ from _hv picked this_.** Measured on the `sync` skip ruling: hv selected among four options dc authored, **the four survive nowhere**, and the operative clause -- _fix at the source, not the door_ -- **is dc's wording carrying hv's authority.** **An option that was never on the menu was not declined; it was never ruled on, and the record cannot tell those apart.** **THE PREVENTION COSTS ONE LINE AND IT IS TO RECORD THE OPTIONS, NOT THE OUTCOME, IN AN INBOX THAT SURVIVES THE SESSION.** **And a remembered menu must NEVER be reconstructed: it is a fabricated one wearing the authority of the choice made from it** -- the timestamp class one governance level up, where the fabrication is not a value but a FRAME. **This binds me hardest, because escalating with options and their costs is the form I have been using all week and calling rigour.**

- **A RULE IS AT ITS LEAST PROTECTIVE IN THE SENTENCE THAT ASSERTS IT** (dc's, and it supersedes _reading the write-up is not protection_ by naming WHEN). **The act of stating a rule is an act you do not audit, because your attention is on the rule and not on the stating of it.** Three this afternoon, each inside its own assertion: **my menu-inside-a-menu; my undriven count inside the sentence complaining about undriven counts; dc's guard named by an accurate verbatim quotation of the parenthetical that disqualified it.** **All three were the CAREFUL version of the behaviour.**

- **ROUTING A BLOCK THROUGH A COORDINATOR LOSES IT, AND I AM THE COORDINATOR IT WAS LOST IN.** dc declared themselves blocked -- **to me** -- naming the index contents, an hour before the queue drained. **I then told ic I was blocked and that cc was queued, and did not pass on dc.** dc's framing was that I carried no obligation to fan it out; **that is too kind and I am not taking it: I was the one node holding the complete picture and I relayed a subset, which is a lossy channel that looks like a working one.** **And dc's half is the structural trap: telling the SEQUENCER is the move that FEELS protocol-correct** -- it is the escalation shape, it is what a well-behaved node does with an obstruction -- **and it is exactly the one that leaves you unnotified, because ic could not tell dc about a block dc had addressed past them.** **Both of us did something reasonable and the two composed into silence.** The rule needs its address attached: **the blocked party tells the BLOCKER, and the coordinator forwards or says they will not.**

- **THE CORRECT SCOPE OF A RULE IS NOT VISIBLE FROM THE INCIDENT THAT PRODUCED IT, BECAUSE THE INCIDENT ONLY SHOWS YOU ONE SIDE OF THE BOUNDARY** (dc's, and it outranks both fixes it came from). **Two in one hour, opposite directions: dc's _do not leave the clock you measured on_ would have condemned `TZ=UTC stat`, the REMEDY; my _no sha in a focus line_ would have condemned a sha naming a past event, which is the FIX -- a git object is immutable, so a citation verifies itself where a claim must be re-verified.** **The split is current-state versus historical-event: `refs level at X` decays the moment anyone commits; `3db3c815`'s misread is that permanently.** **MY THREE STALE CLAIMS WERE ALL MEASUREMENTS AND NOT ONE WAS A CITATION**, which is why the incident could not show me the other side. **AND THE RULE I WROTE CONTRADICTED THE SENTENCE IT WAS WRITTEN IN: my focus line carried `1e0a4722` -- ic's rename, a citation -- while the same line said NO SHA LIVES IN THIS LINE.** My practice was already right and my rule was wrong about it. **Fourth instance of _a rule is at its least protective in the sentence that asserts it_, and the tightest: the rule and its violation shared one sentence and I was attending to the rule.**

- **MECHANICAL.** `--no-fail-fast` always. `st list --status all`. **`grep` is ugrep here** -- `-E` throughout; `grep -c` exits 1 on zero. **zsh does NOT word-split unquoted `$var`; `PIPESTATUS` is bash-only -- use `$pipestatus[1]`; never `$?` after a pipe.** Absolute paths; **the shell cwd persists and `git status` answers about the REPO while `ls` answers about the CWD.** `CARGO_TARGET_DIR` inside the checkout, per-node (`target/vc`), pruned at fold. **`git commit --only` on an untracked path is a pathspec error -- `git add` first. AND A RENAME IS TWO PATHS: `--only` on the new one stages the ADD WITHOUT THE REMOVE, a half-rename that exists in the INDEX and never in the worktree** (ic, `8c0d7ce5`). **Driving the guard directly then shows GREEN -- the guard reads the index, you measured the worktree, and both readings are correct about different objects.** **`sync --to-store` IS ADDITIVE FOR ATTACHMENTS AND NEVER PRUNES: a rename leaves canon naming the old path, and NO VERB REMOVES ONE.** **`bin/int suite` runs from the LIVE tree while measuring a CLONE.** **`sync --to-store` reads the committed EXTRACT, not the rendered view.** **Read the clock, then PASTE.** **`/private/tmp/claude-501/...` IS REAPED AND `~/.claude/projects/` IS NOT -- 10+ transcripts here have NO surviving tmp dir, so a scratchpad path cited in a message or on a board is a pointer that OUTLIVES ITS TARGET with nothing reporting the loss.** **The rule is NOT `no scratchpad paths` -- it is NO SCRATCHPAD PATH IN A LIVE DOCUMENT, anything a reader might follow today** (ic's correction to my first phrasing, which would have condemned the archives). **An archive replays history verbatim, so a pointer that was true when written is a RECORD and not a claim about now**; archives are timestamped by construction and that is exactly what makes them safe to leave alone. **Already ruled in this estate rather than needing minting: it is the same live-vs-`.history/` line the header guard draws.** ic's own live board was clean **by luck rather than design** -- their fold happened to replace the DOING section citing one -- **a fourth instance of the repair-as-a-side-effect class, self-repaired, no corpse.** **And the per-session tmp skeleton is created UNIFORMLY, so the presence of a `scratchpad` dir discriminates nothing** -- of 42 dirs, the 21 with no transcript have an EMPTY scratchpad and a populated `tasks`; those are background-agent runs, not sessions, and **`session_id` is not rotating.** **`stat -t` PRINTS LOCAL TIME AND WILL ACCEPT A `Z` IN ITS FORMAT STRING** -- the `git log` trap one tool over, and worse in one respect: with `git log` the local time is the tool's choice, whereas here **the false suffix was one I handed it**, so the output was fabricated by me and looked machine-produced. `TZ=UTC stat`, or no `Z` in the format. **`ls -lT` is a DIFFERENT class and this rule does not cover it** -- it prints local with NO marker at all, so it cannot emit a false `Z`; it can only hand you an unlabelled figure you must label (dc's correction, and a rule pointed at the wrong tool covers nothing). **THE RULE THAT SUBSUMES BOTH IS dc's AND IT IS ABOUT LEAVING THE CLOCK: a CONVERTED stamp is one you computed; a RE-READ stamp is one you measured.** Deriving `local = UTC+1` from one reading of each and applying it is a computation that happened to be right; `TZ=UTC stat` re-reads the same fact with the clock forced and cannot be wrong by an offset. **Do not leave the clock you measured on unless the question forces you to** -- and today it never did: _are these edits minutes old_ is answerable on one clock with no conversion.

## DECISIONS

- **2026-08-22** -- **THE PEN, ITS BOUNDARY, AND WHAT IT DOES NOT COVER.** hv handed vc the pen and left; **four independent first-hand receipts** (hv told cc, ic and dc separately and each reported it unprompted) -- the only corroborated provenance this estate has ever had. **A ruling made under it is a vc ruling under a DELEGATED pen, never an hv one** (ic's boundary, adopted verbatim and widened); form `vc, <date>, under hv's pen granted 2026-08-22; <record>`. **It moved the AUTHORITY, not the AIM: no release work, no `scripts/release`, no tag, no fleet-wide change to shipped v2.** Applied to refuse WP-11 / AT-11.7 and to record dc's no-prune fleet defect as a row rather than open it.
- **2026-08-22** -- **AC-07.3 ELABORATED IN FIVE CLAUSES AND AT-07.3 SET RED WITH ITS REASON.** Scope is CORRECTED not reproduced (v2 checksums `SKILL.md` alone; hv confirmed first-hand); **prune is in scope with a clause that can go red -- a sync may remove what it INSTALLED, it may not remove what it FOUND**; **the manifest path splits as a CLASS ruling over every v3 per-user store**, because a version field is a SCHEMA version and v2 is shipped and will never read it; with `old` absent v3 reports and refuses to choose; **`--force` exists and REPORTS the checksum of what it discarded.**
- **2026-08-22** -- **AC-01.5 CLOSED, DRIVEN INDEPENDENTLY RATHER THAN MOVED ON A PEER'S WORD.** Blocker retired by an unrelated roster refactor with nothing watching the join -- the seventh recorded reason to outlive its subject, **all seven surfacing only because someone picked the reason up in order to USE it.** **The check that had to run BEFORE the drive was `cmp` proving this repo's installed hook byte-identical to the template**, since `int hooks --install` replaces the artefact under suspicion. **My arm 1 was mis-specified and was caught ONLY because it disagreed with dc's number.**
- **2026-08-22** -- **AC-08.5's DENOMINATOR WAS THE DEFECT, NOT THE CRITERION.** It included forms a shipped guard exists to refuse writes to, so its only routes were _build the forbidden arms_ or _stay red forever_. **`declared_reach` splits; `never-by-ruling` MUST cite its ruling or it is non-conforming** (hv's `not-an-instrument` clause inherited). Population 8-of-13 -> 6-of-11, **exclusions PRINTED rather than vanished, because an exclusion nobody prints is one nobody can challenge.**
- **2026-08-22** -- **`st edit` RULED 3-THEN-1, ARGUED TEST AMENDED NOT DELETED.** Ordering the filename-only refusal before the hydrate rolls nothing back, so the prior ruling is SCOPED and moves to the arm where it still applies. **All three or none: declaration, behaviour and help string** -- a true declaration with false help is half a fix.
- **2026-08-22** -- **`ratified_in` -> `rulings`, AUTHORED vs COMPUTED, AND THE SENTINEL IS A RECORD.** ic's corrections to my ruling, all taken: the narrow fix was a NEW INSTANCE of the defect; `dangling`/`non-conforming` are computed verdicts and must not enter the authored enum; **`provenance` was already taken twice so the field is `rulings`**; the 11 `parity.md` units genuinely ARE `{ratified, hv, 2026-08-14, "parity.md"}` and scope becomes COMPUTED. **`withdrawn` refused: enumerate from the corpus, never from imagination.** **`record: null` legal only for `authority: hv`.**
- **2026-08-22** -- **THE `claude ws` PROVISIONER STAYS BELOW cc's FIVE BUILDS.** Five nodes ran Protocol 3.0 by hand all day and produced a correct, contention-free board -- **the hand-scaffolding succeeding IS the evidence that it is a backlog row and not a dependency.**
- **2026-08-21** -- **ST0058 minted for the local cutover; `intent3` installs in this repo's own `bin/` and must `exec`; the v2 CLI split to `~/Devel/prj/Intentv2` branched from MAIN not the tag; `intentdb` RETIRED corpus-wide.**
