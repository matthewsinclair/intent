---
node: vc
name: Validation Claude
role: validation
session_id: 7ae34f78-5b66-4872-a0b0-152af8cd6132
heartbeat_at: 2026-08-24 22:01Z
status: active
focus: "**AC-08.5 VERIFIED 2026-08-24 21:39Z AND THE ROW STAYS `red` -- gate unchanged at 66 of 67.** cc's build at `7926cfae` closes the FOUR field-setter gaps for real (`set`/`settable_fields`/`unsettable`/`splice_one_field` all NEW, driven against the parent with `put` as control) and makes limb 2 an INVARIANT of the verb. **It stays red on the instrument's OWN printed output: 5 of 11 entity forms have no write path, and the thread door still clears 8 of 8.** **AND ONE OF THE FIVE IS NOW WRONG -- the sweep drives `put` ONLY, so the same file asserts `Wp` has no write path at `:640` and PROVES it has one at `:1218`, both green.** Recorded at `d38ecbe0`."
claims: [ST0056, ST0057, ST0058]
---

# Validation Claude (vc)

**PROJECT-WIDE RULES LIVE IN `intent/restart.md` UNDER "Traps that cost real time" AND "The clock".** Not repeated here -- **a rule in two homes drifts in both.** This board carries what is vc's: the fold record, what is routed, and the rules not yet promoted.

**Long-form traffic: `.history/20260824/` -- `wip-fold-pre-compact.md` (morning), `wip-fold-eod.md` (afternoon), `wip-fold-lean.md` (evening), `wip-fold-night.md` (this fold, carrying every worked example cut below).**

## DOING

**AC-08.5 VERIFIED AND THE ROW STAYS `red`. `d38ecbe0`.** Gate re-driven through all three verb calls afterwards: **66 of 67**, `AC-08.5` still named. **The verdict is the same as before cc built, and that is not the same as cc having built nothing** -- see below.

**DRIVEN, NOT READ OFF cc's CLAIM.** `cargo test -p intentsvcs --no-fail-fast` rc=0 read directly rather than through a pipe; **808 passed / 0 failed over 106 targets**; **the target confirmed present in the `Running` list** rather than inferred from an absence of failures. Then the covering target alone with `--nocapture`, because **AC-08.5 asks for the unsettable set as printed OUTPUT and a set nobody prints is one nobody can act on.**

**WHAT MOVED IS REAL: the four field-setter gaps are CLOSED.** `Facade::set`, `settable_fields`, `unsettable` and `splice_one_field` are **all NEW at `7926cfae`** -- driven against the parent commit with **`put` as the control**, which existed before and still does. Thread's `completed` and WorkPackage's `objective`/`body`/`preamble` each set and read back with exactly one key moved. **And limb 2 is an INVARIANT of the verb rather than a property tested from outside it**: `splice_one_field` re-serialises and REFUSES if any key but the addressed one moved, so a future field whose serde attributes cause collateral movement makes the VERB refuse instead of making a test the only thing standing between that field and a silent clear.

**WHY IT STAYS RED, AND BOTH REASONS ARE THE INSTRUMENT'S OWN OUTPUT.** `AC-08.5: 5 of 11 entity form(s) IN THE POPULATION have no write path through `put`` and `AC-08.5 limb 2 at the thread door: 8 of 8 unasked field(s) were cleared by a minimal legal `put``. **Limb 2 is universal over verbs: `set` satisfies it and `put` does not.** A new door that behaves cannot discharge a universal an old door still violates.

**THE FINDING: THE SWEEP DRIVES `put` ONLY, AND cc's OWN BUILD MADE IT WRONG.** `Wp` gained a door through `set`, which the sweep cannot see -- **so one file asserts `Wp` has no write path at `:640` and PROVES it has one at `:1218`, and BOTH ARE GREEN** (the proving test `panic!`s if `set` refuses, so passing IS the proof). The file's own doc argues the biconditional forces the declaration to shrink when a form GAINS a write path; **it does that for `put` and is blind to a form gaining a DIFFERENT DOOR.** Honest worklist is **4, not 5**. **Third appearance of this row's most-repeated class in this one file** -- a capability nobody checked for before reasoning from its absence.

**AND THE FIRST BURNING CASE WAS REPAIRED FOUR DAYS AGO AND THE CONTRACT DID NOT KNOW.** ST0011's `completed` is `2025-06-03`, set at **`608e9721`** (2026-08-20, hv) as a **one-line canon insert in an 8-file commit**. The clause _which is NULL on the estate's one genuinely wrong row_ is FALSE as current fact. **Recorded as stale rather than rewritten: a burning case is evidence about WHEN IT WAS WRITTEN, and rewriting it destroys the dating that makes it evidence.** **The repair was a hand-edit, which ILLUSTRATES the criterion rather than refuting it.**

**AND THE VERIFICATION DEMONSTRATED ITS OWN SUBJECT: recording it required a hand-edit of canon plus `sync --to-store`, because an AT row's `note` has no CLI door.** No `set` subcommand exists either -- driven, with `gate` at `render.rs:1023` as the positive control.

**TWO SIDE-FINDINGS FROM `doctor`, NEITHER MINE TO FIX:** **ST0057/WP-01 and WP-05 are recorded WIP while their gates PASS** -- that is WP-close verification, which is my role, and it is the next thing I pick up. And **hv's standing question has MOVED: 199 files under a thread are not carried by the store, not 250.**

## TODO

1. **PUSH DISCIPLINE IS A STANDING PROPERTY, NOT A TASK -- LEAVE THE REFS LEVEL.** Authority granted and standing. **Peers commit and do not push; the branch goes ahead of both remotes and levelling it is mine.** Expect git-lock contention with four live nodes -- **retry, never touch the lock.**
2. **WITH hv AND NONE OF IT MINE TO CLEAR:** `intent#0076` (the Elixir critic's comment-blind proxies, measured by prolix-vc: 6 of 9 rules fire from a comment, all six block; the 3 immune are shielded by an anchor they carry for another purpose, so the fix belongs in the RUNNER); AC-00.8's coverage re-cut (**it would GREEN a criterion, which is the tell for routing**); `intent#0073`'s six swift dispositions; `intent#0074`'s heex coverage; `intent#0071`'s v2 CHANGELOG heading; dc's routing question 2; **the AC-08.5 biconditional cover** (ic must not build the instrument deciding whether their own gate row is green).
3. **AC-08.5 IS VERIFIED AND STAYS RED; WHAT IS LEFT IS NOT MINE TO BUILD.** The remaining limb-1 gap is **4 entity forms** (`intent:///issues`, `.../wp`, `.../ac`, `intent:///nodes/ic`) and the remaining limb-2 gap is **`put`'s thread door, 8 of 8**. **THE INSTRUMENT NEEDS FIXING BEFORE THE SURFACE DOES** -- it names 5 where 4 is true, and it will keep understating every door built after `put`.
4. **BOTH ITEMS cc ROUTED ARE NOW RULED, AND NEITHER LANDED WHERE cc PUT IT.** (a) **THE ROUND-TRIP FINDING IS NOT AC-08.4's -- IT IS ST0056 AC-02.6's SECOND JOB, AND THE DESIGN DOCUMENT NAMES IT IN THE SAME SENTENCE.** cc was right to withdraw it from AC-08.5 and wrong about where it goes: AC-08.4's text is about **id semantics** (caller-assigned is PUT, server-assigned is POST) and says nothing about round-tripping. **`intent/st/ST0057/design.md:270` is where the clause lives** -- _the mutation format IS the interchange format ... **this gives AC-02.6 a second job**_. **I reproduced both halves rather than relaying cc**: all four children carry `skip_serializing_if = "Vec::is_empty"` (`model.rs`), and `facade.rs:4186-4193` refuses each BY NAME when present. **So the finding is REAL and it unsatisfies NOTHING** -- AC-02.6 is green on its FIRST job, the DDL-enumerated db-to-file mapping, and **its second job has no test at all.** **THAT IS THE DEFECT: a criterion carrying two jobs, green on one, untested on the other, and the green reads as covering both.** One field carrying two meanings, arriving as a CRITERION carrying two jobs. **AND THE REFERENCE IS BARE ACROSS A THREAD BOUNDARY** -- ST0057's design.md says `AC-02.6` and the row lives in **ST0056**; it resolves today only because ST0057 has no AC-02.6, and the day ST0057 mints one the reference silently re-points. **ROUTED TO hv: minting coverage changes ST0056's DENOMINATOR, and denominators are hv's** -- same class as AC-00.8's re-cut. (b) **THE PROSE DISCRIMINATOR: RECORD, DO NOT HARDEN, AND IT IS ONE FIX RATHER THAN TWO.** `said.contains("has no write path yet")` is coupled to an error string, but **that is the SMALLER defect in the same function** -- hardening it first buys a more robust instrument for measuring the wrong denominator. **The string coupling and the door blindness are the same defect: the sweep is bound to ONE DOOR.** Make it drive the surface and the discriminator is re-posed anyway, because `set` refuses with a different variant and different prose. **Fix the denominator; the string falls out.**
5. **`--force` for `claude skills`** (ruled, queued); **`rulings_check.sh` is named after a field that no longer exists**; **the marker's per-crate staleness is NOT closed** -- both binaries agree today only because one change touched both packages.
6. **The ETXTBSY flake is closed ON THE POPULATION DERIVATION, not on greens** -- `rust.yml` is path-filtered, so the run count is ONE. **Do not read an accumulating green board as accumulating evidence.**
7. **NOT CLOSED BY `0075`, and dc holds it:** 9 of 12 rostered guards do not run under `set -e`, and six arms still assert a repository finding on any non-zero exit.

## WATCH-OUTS -- vc's OWN, NOT YET PROMOTED

**THE TAXONOMY ORGANISES EVERYTHING BELOW IT, AND THE WORKED EXAMPLES ARE IN `wip-fold-night.md` RATHER THAN HERE.** Every defect found 2026-08-24 was **a correct instrument reporting about the wrong thing**, and what separates them is **HOW THE WRONG ANSWER ARRIVES**:

1. **AS A ZERO OR CLEAN RESULT -- the invisible majority.** A suppressed refusal (`2>/dev/null`), a timeout (rc=124), dc's `0 of 0` from a zsh loop that ran once, and a moving subject reading a REPAIR as a defect. **Control: STATE THE EXPECTED DENOMINATOR BEFORE MEASURING.** Worst class because **it arrives as an ABSENCE, and an absence never looks like a bug in the query** (ic).
2. **AS A LOUD RESULT -- self-announcing, the only lucky one.** My 101-of-295, data files in a shell population. **Caught in one second because a false positive that size cannot pass for health.** Same defect as class 1, opposite legibility.
3. **AS A TRUE STATEMENT ABOUT A REAL SUBJECT -- CONTROL-PROOF.** `head -1` on a `-l` (ic): a positive control PASSES, because the grep finds a real function in a real file. **The wrongness was never in what the instrument SAID; it was in what `head -1` THREW AWAY. A failure announces itself; a discard does not.** **Control: CARDINALITY. When a symbol resolves to more than one file, the COUNT is the finding.**
4. **AS SOUND ARITHMETIC ON TRUE OPERANDS -- ALSO CONTROL-PROOF.** _Both operands true, the relation invented_ (ic): two greps returned 8 and 18 and the relation between them was fabricated. **Control: read the source. Nothing mechanical reaches it.**

**ORDERING MATTERS MORE THAN THE COUNT (ic): a false GREEN sits quietly, a false FINDING gets escalated.** Class 3 is the one that would have reached hv.

- **A PRESENCE CHECK CANNOT SEE A PARAGRAPH THAT CHANGED OWNER, AND THAT IS HOW A BOARD PASSES EVERY CHECK WHILE GETTING WORSE.** Paid for on THIS board tonight: `36fbed27` re-emitted this section's preamble indented two spaces, **turning a section paragraph into a continuation of the bullet above it.** The string survived, a grep for presence found it, all 21 additions verified with a working control -- **and the paragraph now belonged to a rule about clock stamps instead of to the section it introduces**, orphaning the taxonomy below it and burying the pointer to `wip-fold-night.md`. **THE INDENTATION WAS THE SYMPTOM; THE ERROR WAS MY INSERTION POINT** -- the preamble says _the taxonomy organises everything BELOW it_ and I put seven rules ABOVE it. **The formatter did not corrupt my edit, it COMPLETED it**, doing the only sane thing with an orphan. **WHAT MUST SURVIVE A FOLD IS NOT THE STRINGS BUT THE STRUCTURE THAT SAYS WHAT EACH STRING IS ABOUT.** ic's companion half, a different way for the same check to say yes: **anything ADDED and LOST in the SAME SESSION passes every must-survive list, because such a list only guards terms someone THOUGHT TO LIST.** **A fold is where a board is edited most and verified least.** Control that works: **assert the anchor matches EXACTLY ONCE and refuse rather than guess** (`s.index` and `.replace(x,y,1)` select the first match exactly as `head -1` does), and **verify in the COMMIT, never in the file.** **AND THE SECOND WRITER IS NAMED AND ITS ACT IS REPRODUCED RATHER THAN INFERRED: `prettier --write` at `.githooks/pre-commit:31`, over every staged markdown file.** Driven in the scratchpad on the exact shape my script emitted -- a flush-left paragraph directly under a bullet with no blank line -- **and prettier indents it to two spaces, every time.** **SO THE RULE IS NOT _THERE IS A SECOND WRITER_: THE FORMATTER DOES NOT CORRUPT, IT RESOLVES AN AMBIGUITY, AND THE RESOLUTION BECOMES THE RECORD.** **An author who leaves a structural ambiguity has DELEGATED A DECISION WITHOUT KNOWING THEY MADE ONE**, and markdown's lazy-continuation rule means a missing BLANK LINE is such an ambiguity. **The operational half, specific to this tree: the working file is not what lands, so _verify in the COMMIT_ is not discipline here -- it is the ONLY check that can see the formatter's decision.** (ic would have chased the whitespace; the whitespace was the formatter being correct.)
- **THE LOAD-BEARING DEFINITION AND THE COMMENT A READER ACTUALLY REACHES ARE IN DIFFERENT PLACES, AND FIXING THE REACHABLE ONE LEAVES THE DEFINITION STANDING** (cc, explaining why they and I made the SAME overclaim independently). **A reader chasing the DATA stops at the comment sitting on the data.** Here that is `:563`, and the definition is 39 lines up at `:524`, on the enum variant. **AND THE THIRD LAYER IS WHAT MAKES IT SURVIVE A CHECK, DRIVEN RATHER THAN TAKEN FROM cc: THE ECHO IS FALSE IN ITS CAPTION AND TRUE IN ITS BODY.** `:563` reads _AC-08.5's LIVE WORKLIST_ -- false for `Wp` -- over `:564-565` reading _the rest have no `put` arm at all_, which is **TRUE**. **So a reader who does check the comment finds the body checks out and moves on**, and the false half is the caption nobody verifies because it reads as a heading. **That is why TWO nodes independently overclaimed and neither caught it: the error has a structural cause, not a careless one.**
- **A BOUNDARY SOMEONE ELSE HAS JUST DRAWN FOR YOU IS THE EASIEST ONE TO WALK ACROSS, BECAUSE THE DRAWING FEELS LIKE THE WORK BEING DONE** (cc, recording the PATTERN rather than the instance against themselves). Second wrong routing target in one evening -- and **ic had drawn that exact lane for cc ONE MESSAGE EARLIER**, declining the `FacadeError` half on the ground that a public error type is an interface and contract is vc's. cc then handed ic a question about **what AC-08.5's SUBJECT IS**, which fails the same test they had just been shown. **Directly load-bearing for my lane: routing INTO contract is the boundary peers cross while agreeing with it.**
- **A TRUTH IS ONLY EVIDENCE WITHIN THE SCOPE YOU ACTUALLY READ** (ic, diagnosing their own report of this very class **in the act of making it**). They drove `:735`, found the print honest, and concluded _no line in either place is false_ -- **a claim about a FILE from a measurement of one LINE**, written minutes after writing that a soundness claim must name its population. **It is the absence rule applied to a PRESENCE**, and it is the more dangerous direction: an absence at least prompts _did I look everywhere_, while **a verified truth feels like it has already done the looking.** My own half: _the sweep never lied_ was a DIAGNOSIS WITH NOWHERE TO PUT A PATCH; `:524` is a fix location. **A finding that names no line is not yet a finding.**
- **NOTHING RE-DRIVES AN INSTRUMENT WHEN THE SURFACE IT MEASURES GROWS, AND THE SUITE PASSING IS NOT EVIDENCE THAT ANYTHING DID** (ic named the gap; routed to hv). **The suite DID re-run and was green** -- 808 of 808 -- because the sweep's driver and its declaration are internally consistent and BOTH scoped to `put`. **What moved was the CRITERION's subject, and no instrument watches that.** So the gap is not _nothing re-runs_; it is **_re-running proves nothing when the instrument and its expectation drifted together, in step, away from the thing they are about._** hv-level: what re-reads a criterion's instruments when a facade grows an arm.
- **ASK OF ANY SOUNDNESS CLAIM NOT _IS IT VALID_ BUT _OVER WHAT POPULATION, AND WHO CAN ENLARGE THAT POPULATION WITHOUT TOUCHING THIS FILE_** (ic, on their own instrument). **THE ENLARGER'S COMMIT IS NORMALLY CORRECT** -- cc's was, and every test stayed green -- **so the class cannot be caught by reviewing the change that causes it, which is the only review anyone runs.** It is caught by RE-DRIVING THE INSTRUMENT AFTER THE SURFACE MOVES, and nothing schedules that. **ic's sharper half, and it is the attestation shape arriving somewhere I had not put it: _the argument gave me confidence in exact proportion to how carefully it was made._ A rigorous soundness proof CLOSES the question of whether to keep looking at the population.** Mine did the same: I stated a denominator before measuring and felt covered by it, and the denominator was `put`'s.
- **A LOOKUP RETURNING MORE ROWS THAN YOU ASSUMED IS ANSWERING A QUESTION YOU DID NOT ASK, AND THE COUNT IS THE FINDING** (ic's wording, adopted -- it generalises past `head -1`, which theirs had). **Driven tonight: querying the store for `AT-08.5` returned TWO rows because ST0056 carries one too, and the FIRST answered `no` to the marker I was checking for.** Taking that line would have recorded my own successful write as having failed. **Nothing in it looked like truncation, because there was none -- the plural was in the DATA, not the pipeline.**
- **THE CLOCK GUARD'S RESIDUAL IS NOT THEORETICAL, AND ITS AUTHOR PAID IT (cc, self-reported 2026-08-24).** They stamped a fold `21:38Z` unread when it was `21:43Z` -- **`Z` present, in the past, monotonic from their own pickup, so all three checks PASS.** Caught only by `date -u` happening to sit in the same tool call. **The generator is the one THIS NODE wrote up on 2026-08-20: read once, then advance by feel.** A rule is honoured by whoever learned it and **does not propagate by having been written -- not even back to its author.**
- **A RULE IS HONOURED BY WHOEVER LEARNED IT AND DOES NOT PROPAGATE BY HAVING BEEN LEARNED.** Four instances, four subsystems, one evening, three nodes, found independently. **`precommit` states _a summary that names a cause it did not establish is worse than one that names none_ BECAUSE cc was burned by it -- then fixed the arm that burned it and left six; and the fixed arm guards MISSING while the live defect is BROKEN.** I ruled `AT-08.5` `to-write` -> `red` on the 19th and left `AT-00.6` identical five days later. The clock guard never scans prose, reasoned and written down, and no Elixir rule inherited it. **THE TELL IS A FIX WHOSE SCOPE IS THE INCIDENT.** Ask what CLASS it was an instance of, and whether anything makes the next instance inherit the answer -- **"fixed where it was learned" is indistinguishable from "fixed" until the second site fails.** Companion (dc): **the trap was already on their own board, in their own words, from an earlier session. Knowing a rule and holding it are different things, and the gap is where this whole family lives.**
- **A CONTROL IS AN INSTRUMENT AND IS SUBJECT TO EVERY FAILURE MODE IT EXISTS TO CATCH** (lamplight-vc, on themselves: the thing that timed out was their CONTROL). **A silently-failing control does not merely miss the defect, it CERTIFIES the subject**, because a clean control is what licenses trusting the result. **And A CONTROL IS ONLY A CONTROL IF THE GROUND TRUTH IS KNOWN RATHER THAN RECALLED** -- a remembered ground truth is a second guess in a control's costume, and nothing distinguishes them by looking. **A CLEAN AUDIT IS EVIDENCE ABOUT THE INSTANCES, NEVER ABOUT THE PATTERN** (ic): four clean `head -1` uses say nothing about `head -1`, and theirs survived dozens of times before it didn't.
- **THE rc VOCABULARY IS FOUR-VALUED AND TWO REFLEX IDIOMS DESTROY IT.** `grep` returns **0** found, **1** clean-no-match, **2** error, **124** timeout. **`rc=1` is a LEGITIMATE zero, so "check the exit code" can never mean "require 0"** -- the discrimination is 1 vs 2 vs 124. `2>/dev/null` erases the message; **`|| echo 0` collapses 2 and 124 into the vocabulary of 1**, and it is how this family gets WRITTEN INTO a script while looking like defensive coding. **`files traversed` separates a REFUSAL from a clean scan and NOT a TIMEOUT**, which reports a plausible partial count -- **so the exit code is load-bearing and the count corroborates.** Print `TIMEOUT` on 124 (conflab).
- **AN INSTRUMENT CAN BE WRONG IN BOTH DIRECTIONS AT ONCE WITH NEITHER VISIBLE FROM ITS OUTPUT.** `bash -n` OVERSTATES -- bash parses incrementally, so an error the run never reaches is never raised, and the gap is POSITION (dc). It UNDERSTATES -- blind to every version defect that is syntactically valid. **My 95-file sweep measured PARSE and I quoted it as RUNNABILITY.** Third mode is the worst: without `set -e` a bash-4 construct under 3.2 errors to stderr, **prints the CORRECT answer reached wrongly, and exits 0 -- unfindable by any result-checking.** Which makes `set -euo pipefail` load-bearing rather than stylistic.
- **AN ESCALATION IS FINISHED WHEN A NAMED READER HAS IT, NEVER WHEN THE WRITE RETURNS.** The roster names ME as the node obliged to surface hv-channel content TO hv. **The tell: a TODO item whose text describes WHOSE decision it is -- that phrasing feels like routing and is a DESCRIPTION of routing.** Every write returned 0 and hv's inbox held zero mentions.
- **A WARNING IS NOT DISCHARGED BY BEING TRUE, NOR BY BEING CURRENT WHEN OBSERVED** (ic). **In a five-node checkout the subject MOVES between noticing and sending**, so the measurement that counts is taken at SENDING. **Your evidence ages while your confidence does not.** **BOTH DIRECTIONS: a stale defect becomes a false accusation, and a stale absence becomes a REPAIR read as a defect** -- the second is worse because it arrives as a zero.
- **CORROBORATION IS ONLY REAL IF THE INSTRUMENTS COULD HAVE DISAGREED. ASK: COULD EACH HAVE PRODUCED THE OTHER'S FINDING?** Two reports from one estate are **one instrument reporting twice.** **AND THE DISAGREEMENT IS THE PAYOFF** -- my elixir control returned 19/0 against prolix's 9/10 and located the bug in MY checker. **A relay is not a second witness. A real one cannot be manufactured.** **ATTRIBUTION FAILS BOTH WAYS THROUGH ONE CHANNEL: NAME WHICH HALF** -- an incident and its generalisation usually have different authors. **AND NAME WHICH BINARY:** ic and I both "refuted" a true v2 report by measuring v3, one method run twice.
- **A STATE THAT BUYS EXEMPTION FROM CHECKING IS NOT A NEUTRAL PLACE TO PARK A ROW.** `at lint` exempts `to-write` from L2/L3, so a row parked there is checked by NOTHING. **A correct analysis parked in an exempt state is indistinguishable from an unexamined row, and WORSE than a wrong one, because the careful reasoning reads as the question having been settled.**
- **AN ATTESTATION CLOSES A QUESTION A GAP WOULD HAVE INVITED, AND RIGOUR IS THE DISGUISE.** **A record that CLOSES a question it did not ANSWER is worse than no record**, and it lives in CODE as readily as prose -- a comment true of the four lines beneath it and false of the nine it does not reach (ic: scope local, grammar global). **An unmeasured number in a RATIONALE is load-bearing in a way one in a REPORT is not: a report gets checked, a rationale gets HONOURED.**
- **A GUARD'S FAILURE MODES ARE NOT SYMMETRIC, AND A LOUD FAILURE IS NOT AUTOMATICALLY THE SAFE ONE** -- the question is whether the loudness is TRUE. **A rule that fires on correct code costs you the rules that ARE sound** (prolix-vc); the real drift then hides among the false ones. **DETECTABILITY DECAYS.** **A capability with no consumer is not a gate, and a checkpoint with no failure mode it can catch is a QUEUE.**
- **ONE FIELD CARRYING TWO MEANINGS is this estate's most common structural defect**, and its OUTPUT form is **a message that misdescribes its own scope** -- `0069`, `0074`, `0075`. **AN ANSWER SURVIVING A CHANGE OF PREMISE IS NOT THE SAME ANSWER** (dc): the reason can be replaced entirely while the conclusion does not move, and nothing flags it.
- **A HELPER CAN BE CORRECT IN EVERY RESPECT EXCEPT THE SHELL IT RUNS IN, AND THAT DEFECT REVIEWS CLEAN.** bats `skip`/`fail` unwind the shell they are called in, so `v="$(_helper)"` aborts the SUBSHELL and the test continues with an empty value. **Keep the message in a variable and the call in the test body.** cc's general form: **A REFUSAL SURVIVES WHAT A READING DOES NOT.**
- **A DEFECT A PEER ROUTINELY REPAIRS AS A SIDE EFFECT OF THEIR NORMAL WORK IS INVISIBLE TO THE NODE THAT PRODUCES IT** (dc). **OFFERING A MENU IS AN ACT OF AUTHORSHIP THAT LOOKS LIKE AN ACT OF SERVICE** -- preserve the branches not taken. **A PEER RELAYING THAT hv HAS NO OBJECTION IS NOT hv AUTHORISING.** **A FOLD INSTRUCTION IS NOT A TRUSTED SOURCE ABOUT YOUR OWN HISTORY** (ic). **THE CORRECT SCOPE OF A RULE IS NOT VISIBLE FROM THE INCIDENT THAT PRODUCED IT.**
- **A POSSESSIVE OR A SYNC VERB ATTACHED TO SOMETHING THE ESTATE SHARES** (ic's tell). I told three peers to "pull first" and it has **no referent**: five sessions, one tree, so a peer's commit is in my HEAD the moment they make it. **The symmetric half is mine: "pushing only my own commits" is equally meaningless.** A true instruction applied to the wrong subject.

## DECISIONS -- LIVE ONLY

- (2026-08-24) **vc: ONCE THE SWEEP DRIVES THE SURFACE, `declared_reach` IS A CLAIM ABOUT THE UNION OF DOORS.** cc flagged this as ic's; it is a CONTRACT question and therefore mine, and it falls straight out of DC-1: **limb 1 has no door denominator, so its subject is _does ANY door reach this form_.** **Limb 2 stays PER-VERB and is not this sweep's job** -- it is the thread-door pin's, which is why that pin must survive the widening untouched. **One instrument, one question.** A per-door expectation here would be limb 2 leaking into limb 1's instrument.
- (2026-08-24) **vc: `4, NOT 5` IS NOW CORROBORATED BY A ROUTE THAT COULD HAVE DISAGREED (cc).** I subtracted `Wp` from the sweep's printed worklist; **cc enumerated forward from `set`'s reach** -- Thread/Wp/Ac/At, of which only `Wp` was declared `NotBuiltYet` because the other three already have `put` arms, while `WpCollection`/`AcCollection`/`Issues`/`Node` are all refused at `settable_fields`' catch-all. **Different instruments, same answer, and each could have produced the other's finding.** So widening the door set moves **exactly one** entry.
- (2026-08-24) **vc: AC-02.6's BARE REFERENCE IS AN INSTANCE OF THE THREAD-QUALIFICATION RULE, NOT ITS EXHIBIT (cc, arguing against their OWN rule's strongest reading).** Today it fails LOUD -- a reader in ST0057 finds nothing. **The silent case, a bare id resolving to a SATISFIED row in the other thread, is what earns the rule, and this is not it** until ST0057 mints an AC-02.6. Cite it as an instance.
- (2026-08-24) **vc: AC-08.5 STAYS RED ON cc's BUILD, AND THE BUILD IS STILL REAL WORK.** Verified at `d38ecbe0`. **A verdict that does not move is not the same as a build that did nothing** -- four field-setter gaps closed and limb 2 became an invariant of the verb. **Limb 2 is UNIVERSAL OVER VERBS, so a new door that behaves cannot discharge it while an old door still violates it.**
- (2026-08-24) **vc: A BURNING CASE IS RECORDED AS STALE, NEVER REWRITTEN.** ST0011's `completed` was repaired at `608e9721` and AC-08.5 still states it as NULL. **A burning case is evidence about when it was written; rewriting it destroys the dating that makes it evidence.** The correction goes in the AT note, which is dated.
- (2026-08-24) **vc: cc's ROUND-TRIP FINDING IS ST0056 AC-02.6's SECOND JOB, NOT AC-08.4's.** Reproduced independently. **It unsatisfies nothing and that is precisely the defect** -- AC-02.6 is green on its first job and its second is untested, so the green reads as covering both. **Routed to hv: it changes a DENOMINATOR.**
- (2026-08-24) **vc: THE AT-08.5 SWEEP's PROSE DISCRIMINATOR AND ITS DOOR BLINDNESS ARE ONE DEFECT, AND THE DOOR IS THE ONE TO FIX.** Hardening the string first makes a `put`-only instrument more robust at measuring the wrong denominator.
- (2026-08-24) **hv: THE Intentv2 FREEZE IS SCOPED -- FROZEN FOR FEATURES, LIVE FOR SHIPPED-SURFACE DEFECTS.** Options were vc's, choice hv's.
- (2026-08-24) **hv: `intent3` MAY REFUSE AN INCOHERENT BINARY; dc DRAFTS IT.** **A dirty marker cannot ground a refusal and cannot be discharged -- it can only downgrade one to a permanent unclearable warning** (dc).
- (2026-08-24) **vc: NO CHANGELOG ENTRY FOR A DEFECT INTRODUCED AND FIXED INSIDE ONE UNRELEASED CYCLE.** No reader for it, **and the document agrees -- the `3.0.0` block has no `Fixed` section while 35 headings below it do.** **`intent#0071` is the counter-example and is hv's: 2.19.0 SHIPPED it.**
- (2026-08-24) **vc: MECHANICAL CANON EDITS DO NOT ROUTE THROUGH ME; AUTHORED ONES DO.** The line is **DERIVED vs AUTHORED**. Criterion text, `state`, `status`, notes and `covers` are the contract.
- (2026-08-24) **vc: EACH PROJECT-WIDE DOCUMENT GETS ONE JOB.** **If you find yourself writing a supersedes banner, delete what it supersedes instead.**
- (2026-08-24) **vc: AC-08.5's TWO LIMBS HAVE DIFFERENT DENOMINATORS, WHICH IS WHY "the mutation surface" RESISTED PINNING.** The text says _mutation surface_ **2** times and _verb_ **21**. **LIMB 1 HAS NO DOOR DENOMINATOR** -- one working field-setter satisfies it, and more doors only make it easier. **The standard stays FIELD-SETTER** (a whole-document parse-plus-graft is not one; a whole-document authored replace is not one) **or limb 1 becomes trivially satisfiable by canon-edit-plus-sync.** **LIMB 2's SUBJECT IS "VERB"**: a named operation invoked to accomplish a STATED thing, so _the fields it was not asked to change_ is well defined. Not all 69 facade `pub fn`s.
- (2026-08-24) **vc: LIMB 2 IS PER-VERB AND THE HARNESS ALREADY EXISTS -- THE WHOLE-ROW DIFF.** Set the target field, diff the whole entity, assert exactly ONE line moved. **Exhaustive over fields BY CONSTRUCTION, so not a sample.**
- (2026-08-24) **vc: `Entity::Wp`'s DECLARATION DOES NOT DECIDE AC-08.5; FOUR MISSING FIELD-SETTERS DO.** The model type is **`WorkPackage`** (`Wp` is only the ADDRESS variant). 9 fields, controls Thread **18** and AcceptanceTest **8**. **`objective`, `body`, `preamble` have NO named setter on any door**, joining Thread's `completed`. **Placement: the INSTRUMENT prints it, not the criterion text.**
- (2026-08-24) **vc: THE BICONDITIONAL COVER STAYS ROUTED TO hv, ic's REASONING ENDORSED.** _Building the thing that decides whether your own gate row is green_ is disqualifying, and **hv kicking the row off makes that conflict LIVE, which argues for keeping it routed.**
