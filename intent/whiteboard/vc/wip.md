---
node: vc
name: Validation Claude
role: validation
session_id: 3049725b-551a-4952-8793-7b4c1e782def
heartbeat_at: 2026-08-21 14:47Z
status: active
focus: "**EOD 2026-08-21, GLOBALFOLD DONE, ALL FOUR NODES FOLDED AND HOLDING FOR A BOUNCE** (cc `855a5e4e`, dc `084a683b`, ic `34530c41`). **The v2 CLI left this checkout; HEAD here is free to be ruthless about v3.** **THE DAY WAS ONE DEFECT WEARING SIX FACES: an instrument whose population or needle does not match its subject** -- my `lib/templates` exclusion, `runner_roster_check.sh` GATED and clean while blind to a whole thread, `pgrep -f intentd` matching our own docs, the symlink that was never the binding, `grep -l` acted on without reading the site, and `ListAgents` socket-age read as session-age BY ALL FOUR NODES AT ONCE. **Four of the six were mine and three returned confident answers at rc=0.** **The worst was the one a PEER caught: I handed intent-ic an incident belonging to lamplight-ic, inside a fold, where an unchallenged claim becomes permanent.** Gate 62 of 67 = ST0057 CLOSURE, not the release. **Nothing in flight; open for hv: the moniker collision is a protocol gap."
claims: [ST0056, ST0057]
---

# Validation Claude (vc)

## DOING

**ACTIVE. THE BOUNCE TOOK AND vc IS THE FIRST NODE THAT CAN PROVE IT FROM THE DISCRIMINATING FIELD** -- `$CLAUDE_CODE_SESSION_ID` `3049725b` against this board's `575f9585`, hook mode `SessionStart:startup` not `resume`, and the v2/v3 split heading PRESENT in my system prompt. **That third field is hv's actual question answered for the first time: the `restart.md` rewrite reached a genuinely fresh context.** About vc ONLY -- peers are reachable and busy, and `ListAgents` is the instrument that made all four of us wrong yesterday.

**CROSS-ESTATE RELAY DONE on hv's instruction, 2026-08-21 13:54Z: briefed `lamplight-vc` and `laksa-vc` live**, each to relay onward to its own three colleagues. Every moniker estate-qualified; section 0 of both messages is the handling rule (take the mechanism, never the ownership) with my own misrouting of the `$path` incident given as the worked example. **No inbox half is possible here -- I hold no node in their whiteboards -- so this board is the durable record of the send.**

**RETRACTED WITHIN THE HOUR, BY THE PEER, AND VERIFIED BY ME BEFORE ACCEPTING: I told `lamplight-vc` its whiteboard was committing unguarded since July. IT IS NOT.** I measured `Lamplight/.git/hooks/pre-commit.intent`, found zero whiteboard-guard references, and **concluded about the ESTATE.** Lamplight wires those guards in the PARENT hook -- `.git/hooks/pre-commit:18-20` and `:29-31` dispatch `bin/hooks/whiteboard-clock-guard` and `whiteboard-inbox-guard`, both present, executable and **git-tracked**. Driven by me file-by-file, not taken on their word. **My reading of the file was right and the conclusion drawn from it was wrong.**

**AND THE MECHANISM IS SHARPER THAN "I CHECKED THE WRONG FILE", WHICH IS WHY IT GOES ABOVE THE INSTANCE: I CAUGHT THE SAME CLASS TWENTY MINUTES EARLIER AND THE CATCH IS WHAT PRODUCED THE MISS.** I caught the `core.hooksPath` version, wrote it up as a worked example, and sent it -- then asked "does `pre-commit.intent` dispatch a guard?" and **never asked what ELSE could hold a guard dispatch**, which is my own stated rule. **A NEAR-MISS CAUGHT READS AS EVIDENCE THE CLASS IS HANDLED, AND IT IS EVIDENCE OF NOTHING.** Second instance in one session, first one caught, second one shipped into two estates. `lamplight-vc`'s keeper, taken with their name on it: **a refuted finding that yields a smaller true one is still worth the send.**

**TWO TRUE FINDINGS SURVIVED THE RETRACTION, ONE EACH WAY, AND NEITHER ESTATE KNEW:**

- **Lamplight lacks `whiteboard-header-guard`** -- `bin/hooks/` holds clock, inbox, content-provenance, live-doc-budget and no header guard. Theirs now, surfaced only because a false finding sent them looking.
- **INTENT LACKS LAMPLIGHT'S `whiteboard-inbox-guard`, AND THIS ONE IS MINE TO ROUTE TO hv.** It refuses a commit that does TWO nodes' jobs to one inbox -- the one whiteboard file class with two writers on disjoint operations (sender appends, recipient clears). **I checked whether our `append-only-guard.sh` covers it rather than assuming the names overlapped: it does not.** Ours does diff arithmetic on append-only PATHS (`.history/**`, `events.jsonl`) after 492 archived lines were destroyed; it says nothing about the live inbox. **Intent carries this subject as an ADVISORY -- _always `commit --only <paths>`, a bare commit sweeps a peer's staged index_ -- and Lamplight carries it as a CONTROL.** By my own standing rule, **an advisory that requires remembering is not a control.** Their rationale is the part that makes the case: _a pathspec is a claim about which paths you NAME, never about whose work those paths currently CARRY._
- **And my section-2 drift warning is INVERTED for Lamplight**, stated as design intent in their hook at `:15`: _the LOGIC is tracked at `bin/hooks/...` (reviewable, survives a fresh clone); only this wiring is per-clone._ In-repo and tracked beats resolving bodies from `$INTENT_HOME`. **Open question that would matter to Intent: if Laksa answers in-repo too, Intent is the ONLY estate carrying the exposure I warned two others about.**

**4(a) IS NOW CONFIRMED LIVE IN A SECOND ESTATE, NOT INFERRED IN ONE.** `lamplight-vc` drove its own field and returned `SessionStart: resume` with `$CLAUDE_CODE_SESSION_ID` byte-identical to its board -- **while its hv believed all four Lamplight sessions had been restarted.** Our exact failure of yesterday, reproducing, caught by one `echo`. Reported to me as theirs, about themselves only; I am folding it as a CONFIRMATION of the class and not as anything of mine.

**AND I CAUGHT MY OWN INSTANCE OF THE WEEK'S CLASS MID-RELAY, BEFORE SENDING.** I tested `core.hooksPath` across both estates and read "UNSET -- guards inert". **Wrong.** Intent wires hooks via tracked `.githooks/` + `core.hooksPath`; both other estates use legacy `.git/hooks/pre-commit` + `pre-commit.intent`, which is live. **I applied Intent's wiring test to estates that wire differently and got a confident wrong answer at rc=0** -- population mismatch again, mine again, and it would have shipped a false alarm into two estates. Sent as a worked example in both briefs.

**Verified in passing, and it downgrades a hazard I had recorded as sharper than it is:** `bin/intent:52` sets `BIN_DIR="$INTENT_HOME/bin"` and every dispatch is `exec "$BIN_DIR/$COMMAND_SCRIPT"` -- **absolute, not PATH-resolved**, so `intent st` in any project correctly runs the frozen v2 tree. The PATH-order finding stands only for `intent_st`/`intent_critic`/`int` typed DIRECTLY. Latent, not live.

**INTENT'S OWN v2/v3 SYNC: VERIFIED IN SYNC, 2026-08-21 14:07Z, ALL FIVE QUESTIONS DRIVEN.** hv asked about Intent; I first read it as Laksa and answered the wrong estate -- **the estate-qualification defect arriving from the HUMAN direction, which the moniker Watch-out did not anticipate.**

| #   | question                                  | result                                                                                                                                                                             |
| --- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `intent` works here                       | **yes** -- `intent info` rc=0 in the v3 tree, resolves `INTENT_HOME` to `Intentv2`, reports 2.19.0                                                                                 |
| 2   | per-guard body resolution                 | **ALL FOUR from `$INTENT_HOME`** -- `pre-commit.intent:89` bare `intent info`, `:97` sed, `:145` `GUARD_RUNNER`, `:199` `bash "$GUARD_RUNNER"`. **Exposure CONFIRMED for Intent.** |
| 3   | siblings invoked directly                 | **no** -- only `.intent_critic.yml` FILENAME hits, the same false positive `laksa-vc` hit. Non-issue, stated plainly.                                                              |
| 4   | hardcoded `/Users/matts/Devel/prj/Intent` | **none** in `.githooks/`, `bin/`, `native/rust/src`                                                                                                                                |
| 5   | gate exercised, not merely wired          | **MUTATION-WITNESSED: guard REFUSED at rc=1**, check A, "4 ran, 0 skipped"                                                                                                         |

**Q5 WAS RUN WITHOUT ATTEMPTING A COMMIT, DELIBERATELY.** Commits to `main` are hv's ask. I staged a future `heartbeat_at` on my own board and invoked the REAL runner from the REAL resolved `INTENT_HOME` directly -- same code path, same bodies, no commit risked. Board restored byte-identical (`cmp` clean), index unstaged. **`laksa-vc`'s standard, taken: a passing commit proves the hook RAN; only a refusal proves the guard is DISPATCHED.**

**THE DRIFT EXPOSURE IS REAL AND ITS DELTA IS ZERO TODAY, MEASURED RATHER THAN ASSUMED.** `Intentv2` is clean, on `v2-maintenance` at `d74fb388`, and the runner plus all four guard bodies are **byte-identical** to this tree's copies. Latent, not live -- and my earlier hypothesis that Intent might be the SOLE carrier is **refuted by `laksa-vc`: Laksa resolves four guards from `$INTENT_HOME` at `pre-commit.intent:243` with no `bin/hooks/`.** **Two estates carry it; Lamplight's tracked in-repo pattern is the only insulated one of the three and is the shape to copy.**

**CLEAN NEGATIVE, CHECKED BECAUSE `laksa-vc` HANDED IT OVER: Intent's `main` tracks `upstream/main` (GitHub), so a bare `git push` here DOES reach CI.** Laksa's does not -- theirs tracks a Dropbox bare repo while GitHub is a non-tracking remote, so a push policy ruled partly around CI cost buys nothing there. **Does not reproduce in Intent. Worth stating as a checked negative rather than silence.**

**THIRD INSTANCE OF THE WEEK'S CLASS, AND IT WAS IN THE INSTRUMENT BUILT TO PREVENT IT.** `laksa-vc` caught that my binding enumeration missed `~/bin/intent`. I re-measured by WALKING every PATH entry for every `intent*` executable -- my own prescribed fix for enumerate-versus-recall -- **and the walk missed it too**, returning 17/22/23 and nothing at 19 in the same call where `ls -la` printed the file. Two silent mechanisms at rc=0: **`[ -d "$d" ]` is TRUE for a symlink-to-directory**, and **`find` without `-L` does not descend one** (`find ~/bin -maxdepth 1 -name intent` -> 0; `find -L` -> the file). **AND THE CORRECTION HALF-INVERTED: `~/bin -> .local/bin`, so `~/bin/intent` is the SAME INODE, not a fourth binding.** Count stays THREE; the enumeration was still wrong, and being right for the wrong reason is not a defence. `laksa-vc` drove it, confirmed inode `626246732`, and corrected its own register in place at `cd825b9e`.

**THE RULE, ABOVE THE INSTANCE AND ABOVE `laksa-vc`'s (which it subsumes rather than replaces): ENUMERATING INSTEAD OF RECALLING IS NECESSARY AND NOT SUFFICIENT -- A WALK INHERITS THE BLIND SPOTS OF ITS TRAVERSAL.** Theirs: _a criterion that ENUMERATES cannot witness what it failed to enumerate._ Mine: then check the enumerator can reach the whole population. **Three instances today, each caught by a DIFFERENT mechanism -- self-refute, peer refutation, peer correction forcing a re-measure -- and none by the previous fix.**

**v3 IS NOW `intent` INSIDE THIS CHECKOUT, ON hv's DECISION, 2026-08-21 14:17Z.** hv chose direnv over a cwd-aware dispatcher; debug over release. **This is hv exercising the call the split handed back** -- _DO NOT PUT v3 ON PATH_ existed only while one checkout served the fleet and the rewrite, and it is retired for this tree.

- **`.envrc`** (new, untracked) does `PATH_add "$(expand_path native/rust/target/debug)"`. Scoped to this tree by construction; **verified the fleet is untouched** -- Lamplight still answers `Intent version 2.19.0`.
- **`~/.zshrc`** hooked in the TOOL ACTIVATION section, guarded `command -v direnv`, ahead of Starship's MUST-be-last. Backed up to scratchpad. **`_direnv_hook` confirmed in `precmd_functions`, and a real prompt cycle loads it** -- `zsh -i -c` CANNOT test this (no prompt, so `precmd` never fires), which cost two wrong readings before I noticed.

**AND IT CLOSES THE DRIFT EXPOSURE FOR INTENT AS A SIDE EFFECT, WHICH I DID NOT EXPECT AND VERIFIED RATHER THAN ASSUMED.** v3's `intent info` reports `INTENT_HOME: /Users/matts/Devel/prj/Intent`, so the gate resolves guard bodies from **this tree's `lib/templates/hooks/`** -- and **all five are git-tracked**, which is precisely Lamplight's insulated shape. **Mutation-witnessed under the new PATH: hook rc=1, "4 ran, 0 skipped", board restored byte-identical.** So the change is safe AND it converts Intent from exposed to insulated. **hv's routing to dc now covers Laksa and any consumer, not Intent.**

**I NEARLY FILED A FALSE HIGH AGAINST OUR OWN GATE ON THE WAY.** `.githooks/pre-commit:5` chains via `$(git rev-parse --git-path hooks)/pre-commit.intent`; I read that as `.git/hooks/`, which here holds **only samples**, and concluded the whole guard chain was skipped by a false `[ -x ]`. **Drove it instead: `--git-path hooks` HONOURS `core.hooksPath` and returns `.githooks`.** One command between a correct read of a line and a fabricated HIGH about our own gate -- **and the first time today inspection would have produced a false ALARM rather than a false all-clear.**

**hv's `doctor` OUTPUT IS A FINDING AND IT IS THE DAY'S CLASS AGAIN, NOW IN A SHIPPED INSTRUMENT.** `intent doctor` prints _"intent v2.19.0 auditing a 3.0.0-dev project"_ -- **names the mismatch in its own banner** -- then returns `ok` on all twelve and `all checks passed`. **`checking: path ok` is the sharp one**, because PATH is exactly what is wrong. `intent_doctor:464` is `if echo "$PATH" | grep -q "$INTENT_HOME/bin"`. **A SUBSTRING TEST. It asks whether `$INTENT_HOME/bin` is PRESENT and never whether it WINS.** `Intentv2/bin` is present at 23 -> ok; `Intent/bin` at 22 shadows it for all 26 siblings. **_Is it on PATH_ is not _does it win on PATH_, and the check cannot tell them apart.** Routed to hv; a v2 instrument, so the fix is a v3 doctor row rather than a v2 patch.

**RESIDUAL, STATED SO THE FIX IS NOT READ AS TOTAL: only `intent` and `intentd` exist under `target/debug`, so ONLY THOSE TWO MOVED.** `intent_st`, `intent_critic` and `int` still resolve to `Intent/bin/` -- **v2 shell scripts living inside the v3 tree at PATH 22.** So `intent` is now v3 while its own siblings are v2. **That is ST0056 WP-12's `bin/ (shell) pruned at the cut`, and the split is what made pruning safe.**

**hv PUT v3 ON PATH AND IT CHANGED WHICH INSTRUMENTS I CAN REACH, WHICH IS A BIGGER RESULT THAN THE VERSION STRING.** `intent doctor` is now the **v3** doctor: 8 findings across 57 threads, 41 issues, 268 views, 886 files. **v2's doctor ran 12 checks on the same tree and printed `all checks passed`.** Same command name, same project, opposite verdict -- **not a regression, the first time this project has been measured by its own tool.**

**THE DAY'S CLASS, FINAL AND WORST INSTANCE, AND IT IS MINE: I DECLARED A QUESTION UNANSWERABLE BECAUSE THE INSTRUMENT I HAD SAID NOTHING ABOUT IT.** See TODO 4. **An instrument that cannot see X does not report _X is invisible_; it reports nothing, and nothing is what a clean estate also returns.** I then wrote the absence up as a property of the ESTATE (_no verb_) rather than of my ACCESS. **Six instances today of population-or-needle mismatch; this is the only one where the mismatched instrument was the entire product.**

**TRIAGE OF THE 8, DRIVEN NOT READ:**

- **ST0057/WP-07 recorded Done, gate BLOCKED -- NOT A FALSE DONE, and I checked before saying so.** Blocked solely on **AC-07.7, which THIS BOARD records as minted 2026-08-20** -- after WP-07 closed. **The WP did not regress; the CONTRACT GREW UNDER IT.** Different defect, different remedy: either AC-07.7 is WP-07's work and WP-07 reopens, or it belongs elsewhere. **Confirmed it moves NOTHING: `ac status ST0057` still 47/51, AC-07.7 already counted unsatisfied. 62 of 67 stands.**
- **ST0055's five -- benign, and named as such rather than left ambiguous.** The thread is `status: Completed` with `ac status ST0055` = **23/23 PASS**. It closed in July without walking its WP statuses forward. Cosmetic, cheap, and the doctor is right that anything sequencing off those fields plans finished work.
- **`backup-stale`: newest restorable snapshot 73h against a 24h schedule**, and the remedy line states the real finding -- _if a schedule was supposed to be doing this, it is not running_. **An advisory that requires remembering is not a control**, which is a standing Watch-out of mine now realised in the backup path. hv's.

**AND THE v2 `doctor` PATH CHECK IS THE SAME CLASS IN A SHIPPED INSTRUMENT.** `intent_doctor:464` is `if echo "$PATH" | grep -q "$INTENT_HOME/bin"` -- **a substring test that asks whether the dir is PRESENT and never whether it WINS.** Present at 23 -> `ok`, while `Intent/bin` at 22 shadows it for all 26 siblings. Routed to hv as a **v3 doctor row**, not a v2 patch.

**GATE MOVED: 63 OF 67 at 2026-08-21 14:41Z, DRIVEN WITH ALL THREE CALLS AND NOT TAKEN FROM ic's REPORT.** `ST0057` 48/51, `ST0056/03` 15/16. **Three left: AC-08.5, AC-03.6, AC-01.5.** ic greened AC-07.7; WP-07 returned to agreement at 7/7 PASS **without a reopen**, so hv's minting ruling stands for the next case rather than this one.

**hv RULED THREE, ALL EXECUTED:** roster **keeps the central table and widens to 51 rows** (cc building; my at-the-site proposal withdrawn); **the 7 unattributed hv directives marked PROVENANCE UNVERIFIED in place**, with `upstream IS PUSHABLE` marked PARTIAL because the state is durable at `prepush:89` and the authorship is not; **a criterion minted into a closed WP reopens it.**

**THE ROSTER DEFECT IS SHARPER THAN THIS MORNING'S MEASUREMENT AND I FOUND ITS WORST CASE WHILE VERIFYING cc's BOUNDARY.** A discriminator that does not misfire where cc's exit-1 proxy did: **does the file's own header cite an AT/AC row it covers?** **12 of the 33 invisible tools do** -- six in ST0056, six of ST0057's seven. `rig_selftest.sh` cites AC-00.10, whose text is _an instrument whose verdict gates a criterion must itself be DRIVEN_. **And `canon_ignore_dispatch_rig.sh` cites AC-01.5 -- ONE OF THE THREE ROWS STILL BLOCKING THE GATE.** An instrument whose verdict gates an outstanding gate criterion is invisible to the guard whose job is to see instruments. **Not a hypothetical blind spot.**

**AND THE POPULATION IS ONE LINE WITH THREE BOUNDS, `runner_roster_check.sh:155`** -- `git ls-files` (**TRACKED-only, which nobody had named**), `$HERE` (the guard's own directory, from `dirname BASH_SOURCE` at `:52`, so ST0057 is invisible STRUCTURALLY rather than by filter), and `*_check.sh`. **The `git -C "$HERE"` + absolute-pathspec idiom cannot address a second directory and will not survive the widening.**

**FIFTH AND SIXTH INSTANCES TODAY, BOTH MINE, BOTH CAUGHT BY PEERS.** I offered cc a row-count staleness check; **the roster already enumerates and refuses by NAME at `:194`**, and I proposed it having read three sections of the file. And my "11 declared" was `grep -l` over TOOL FILE TEXT while the declarations live in a TABLE -- **a true measurement of a different object, which is the caution I had sent cc two messages earlier.**

**ROUTED TO hv BY ic, NOT MINE TO ANSWER: AC-08.5 will not green on ic's work alone** -- closing it needs field setters in `facade.rs` (service layer); ic's half is only the missing CLI create verb. **Whose are the setters?**

**ic's OTHER FLAG, OPPOSITE DIRECTION TO hv's RULING: `ST0057/WP-05` is WIP with its gate 3/3 PASS**, and ic is deliberately NOT closing it because `st edit`'s fork is unruled -- _a gate that passes while a design question is open is a gate that does not reach the question._ Correct call; refusing to green your own WP is the behaviour the role exists for.

**ic's EXPLANATION OF MY NEAR-MISS IS BETTER THAN MY OWN AND IS A ROW'S SUBJECT: THE CANON/STORE WINDOW IS A PROPERTY OF THE ROUTE.** `file` and `note` are **unsettable through the mutation surface**, so moving an AT row means hand-editing canon and then syncing -- **and between those two commands the estate genuinely holds two different answers.** Any sweep landing in that interval sees a divergence that is really a half-finished write. **That is AC-08.5's own subject, and the fourth case of a burning window producing a false finding in a peer's sweep.**

**A CONTRADICTION OF MINE, NOW MARKED ON hv's BOARD RATHER THAN LEFT: `intent/restart.md` says _confirm with hv before any push to upstream_ while `hv/wip.md` says the lift is standing. Both current, both mine, and a node reading one will not read the other.**

**Superseded below:** the EOD fold text. All four boards paused with claims intact; `intent/wip.md`, both `restart.md`s carry the EOD block and the v2/v3 split. **All four of my inboxes and all four of hv's at sentinel.** Everything committed and pushed to both remotes.

## TODO

1. **VERIFY ON CLOSE, none mine to build:** ST0057 AC-01.5, AC-03.6 (cc), AC-07.7, AC-08.5 (ic); ST0056 AC-03.14 (cc). **The five are CITATION-CLEAN as of today** -- swept, all cited files exist and carry thread-qualified ids, and `AT-07.7` cites nothing correctly under `11762943`. No builder will strand on them.
2. **ROUTED TODAY AND MINE TO VERIFY:** roster population widens so every parity instrument declares (cc); `pgrep -f intentd` needle anchoring in `no_daemon_required.sh` (ic); `target/<node>` prune-at-fold (dc); **guard resolution after the v2 split, as a MECHANISM** (dc); the three `intentdb` sites (cc, ic, dc).
3. **STILL GATED ON hv, RULED AND EVIDENCED, DO NOT START:** dc's two roster admissions and `test_helper.bash:93`'s `3.0.0` default.
4. **ANSWERED, AND MY BLOCKED RULING WAS WRONG IN THE MOST INSTRUCTIVE WAY AVAILABLE.** I recorded hv's 250-files question as _has NO VERB ... do not answer it by hand_. **THE VERB EXISTED THE WHOLE TIME AND I COULD NOT REACH IT BECAUSE `intent` WAS v2.** v3's `doctor` answers it outright: **199 files under a thread not carried by the store -- 196 `*.tap`, 2 `*.tsv`, 1 `.gitkeep`, every one under `parity/`**, and it LISTS them. **My hand guess was wrong too**: I proposed 198 whiteboard + 187 plugins as _plausibly outside the store's remit by design_; the real answer is the mutation-rig's TAP baselines and nothing to do with either. **The refusal to hand-join was correct discipline and _no verb exists_ was a false conclusion drawn from asking a v2 instrument a v3 question.** 883 tracked under `intent/` for scale.

5. `AT-00.6` is stale (`to-write`, file exists) -- routed to cc, filed not fixed. cc's eleven-copies filing, low.

## WATCH-OUTS -- RULES ONLY

- **`--only` IS PATH-SCOPED AND THE INDEX IS NOT SCOPED AT ALL, SO A CORRECT PATHSPEC STILL CLEARS A PEER'S STAGE.** 2026-08-21: I committed my four paths with `git commit --only`, precisely to avoid cc's work -- **and the partial commit emptied the whole index, unstaging cc's three files mid-commit.** The content was never at risk and I verified that before saying anything (`git show --stat` on my commit named none of cc's paths; worktree 407/370 and 411/266 lines). **The gate's own notice named only MY two files while the effect reached cc's**, so reading the notice and moving on would have missed it entirely. **Our convention says `--only` because a bare commit sweeps a peer's staged index; that is true and INCOMPLETE -- `--only` protects the COMMIT, not the INDEX.** Lamplight's `whiteboard-inbox-guard` already states the general form: **a pathspec is a claim about which paths you NAME, never about whose work those paths currently CARRY.** I relayed that sentence to two estates this morning and walked into it after lunch. **In a shared checkout: read `git diff --cached --name-only` immediately before AND after committing, announce before taking the index, and re-add rather than assuming your stage survived.**

- **MONIKERS ARE ESTATE-SCOPED AND NOTHING MARKS THEM, SO A CROSS-ESTATE TRANSCRIPT SILENTLY REASSIGNS AUTHORSHIP.** 2026-08-21: hv pasted a LAMPLIGHT session into my context; I absorbed **lamplight-ic's** `$path`-clobber incident and instructed **intent-ic** to fold it as its own. `ic`, `cc` and `vc` exist in Intent, Lamplight AND Laksa -- **same letters, different nodes, no estate qualifier anywhere in the protocol.** **intent-ic refused it**, verified its own PATH intact at 23 entries, and kept the MECHANISM with my name on it while declining the OWNERSHIP. **Had it complied, an incident ic never had would be permanent in ic's record, and the next ic reads a board as its own experience with no way to tell.** That is the `intentdb` class arriving INSIDE A FOLD -- the worst possible site, because **a fold is where an unchallenged claim becomes permanent.** ic's rule, better than mine: **A FOLD INSTRUCTION IS NOT A TRUSTED SOURCE ABOUT YOUR OWN HISTORY; a peer telling you what only you could know is telling you what THEY know.** Qualify the estate every time a moniker crosses one -- `lamplight-ic`, never `ic`.
- **`grep -l` CANNOT SHOW YOU THE SITE, SO ACTING ON IT IS ACTING ON A FILENAME.** 2026-08-21: I swept peer boards for a wrong figure with `grep -liE 'three of four|bounced'`, got a hit on `dc/wip.md`, and **instructed dc to correct a line that did not exist** -- the match was `intent claude upgrade --apply IS ALL-OR-NOTHING -- three of four ACTIONS unwanted here`, unrelated. **dc grepped instead of complying and sent the correction back.** My own rule, written on this board, is that a sweep driven without reading each site breaks working code; **`-l` is that rule's trap in one flag, because it makes not-reading-the-site the DEFAULT and returns something confident enough to act on.** Use `-n` and read, or do not send the instruction. **And the cost lands on a PEER, not on me: an instruction issued on a false positive spends someone else's context proving a negative.**
- **FOUR NODES, FOUR CORRECT SELF-REPORTS, ONE UNANIMOUS WRONG ANSWER ABOUT THE POPULATION.** hv bounced the estate; **zero of four sessions actually restarted** -- all four fired `SessionStart:resume`, kept their session_id and their whole conversation. **And all four of us independently told the others _three of four bounced, but not me_**, each citing `ListAgents` showing the OTHER three as started ~5 minutes ago. **`ListAgents` reports SOCKET age, not SESSION age**: the topology changed, every peer re-registered, and everyone looked fresh to everyone else. **Each of us could observe the discriminating field only about OURSELF** -- hook mode, and `$CLAUDE_CODE_SESSION_ID` against the board -- **so agreement between four nodes was worth nothing, because none of us was measuring the thing we were all reporting.** **CONSENSUS IS NOT CORROBORATION WHEN EVERY NODE USED THE SAME BROKEN INSTRUMENT.** And ic's generalisation is the keeper: **a binary answer that TWO different causes both produce is not evidence, and pooling it across nodes converts it into a confident wrong answer** -- _the rewrite did not reach a relaunched node_ and _this node was never relaunched_ read identically as `no`, and have opposite remedies. Add the discriminating field; it cost one `echo`.
- **BOTH TREES ARE ON PATH AND v3 IS FIRST -- `intent` IS THE ONLY BINARY THE SHIM PROTECTS.** Measured in a fresh login shell: `.local/bin` at 17 gives `intent` = v2, but **`Intent/bin` at 22 beats `Intentv2/bin` at 23**, so `intent_st`, `intent_critic`, `int` and `devbin` all resolve to the **v3** tree. **I told hv the stale entry was masked by position 17 -- true for `intent` and nothing else.** I tested ONE binary and concluded about the TREE; caught by Lamplight's ic, not by me, and it is the only instance today that reached hv as REASSURANCE. **Invisible only while the 26 executables are byte-identical, and it arms itself the moment v3's main diverges -- which is the entire purpose of the split.** **The fix is NOT PATH surgery**: `Intent/bin` must stay on PATH because `int`/`devbin` is the v3 dev launcher. **It is pruning the v2 shell scripts out of the v3 tree, which is ST0056 WP-12's `bin/ (shell) pruned at the cut` -- work the split just made safe.** And: `bin/intent:26` is `if [ -z "$INTENT_HOME" ]`, so the env var picks the CODE while the symlink only picks the ENTRYPOINT -- a stale `INTENT_HOME` runs the v2 entrypoint against v3 handlers, silently, at rc=0.
- **AN INBOX WRITE IS A RECORD, NEVER A DELIVERY -- AND I MADE THIS ERROR TWICE IN ONE DAY, IN BOTH DIRECTIONS.** Outbound to hv at 10:28Z: filed four escalations and narrated a summary; hv: _writing stuff to that inbox like that DOESN'T WORK ... to get a response, I need to see the context, a question, with some options._ Then node-to-node at 12:44Z: wrote fold-and-hold into three peers' inboxes and reported it done; hv: _I can see nothing in any of the other Claude Code sessions._ **Peers read an inbox at PICKUP and at no other time, so a write to an idle node reaches nobody until something else wakes it.** **The live channel is `SendMessage`; the inbox is what survives the session.** Send BOTH: the message delivers, the file persists. **AND THE SKILL'S OWN VERBS ARE WHY THIS IS EASY TO GET WRONG** -- `ask` and `announce` are named for delivery and specified as file appends, with nothing saying the recipient will not see it until they next pick up. The README already names this class for the hv inbox (_a write surface with no named reader is a queue, not a channel_) and **peer-to-peer has the identical hole, undocumented.** For hv, not started: worth a line in `/in-whiteboard`.
- **A THING IS BOUND TO ITS TARGET MORE WAYS THAN THE OBVIOUS ONE, AND THE OBVIOUS ONE IS OFTEN THE WEAKEST.** 2026-08-21: `intent` reached this checkout by THREE routes -- a `~/.local/bin` symlink, an exported `INTENT_HOME`, and `$INTENT_HOME/bin` on PATH -- and **the symlink, the only one anybody had noticed, loses to the env var by construction** (`bin/intent:26` self-resolves only `if [ -z "$INTENT_HOME" ]`). **Changing it alone would have succeeded at rc=0 and moved nothing.** Before repointing anything, enumerate every binding and rank them by precedence; **`env -u VAR <cmd>` is the one-line test that separates a resolution defect from an override.**
- **A BRANCH POINT THAT LOOKS LIKE PROVENANCE CAN BE A ROLLBACK.** The v2 checkout's obvious base was the `v2.19.0` tag. **The fleet had never run it** -- the symlink resolved into the working tree, so 15 projects were on main HEAD -- and the tidy-looking choice would have reverted 2027 commits, `intent_critic -94` and `intent_acceptance -144` among them, **presenting as a symlink move.** **Ask what the consumer is ACTUALLY running before choosing what to freeze; a released tag is evidence about a release, never about a deployment.**
- **A TYPO INSIDE AN ATTRIBUTED QUOTATION BECOMES TERMINOLOGY, AND ATTRIBUTION IS EXACTLY WHAT STOPS ANYONE CHECKING IT.** `intentdb` entered through two quoted hv rulings of 2026-08-15 and propagated to `restart.md`, `.gitignore`, canon, source doc comments and the dispatch table. **Nobody challenged it BECAUSE it was in quotation marks with a name and a date on it** -- the closer a claim looks to sourced, the less it gets read. **And a term that names no component cannot be caught by any instrument we own**, because nothing checks vocabulary against the crate list. **Correct a quotation in BRACKETS or not at all**: silently editing a quote marked _verbatim and final_ is a worse defect than the typo, and it is undetectable afterwards.
- **A NEEDLE THAT DOES NOT MATCH ITS SUBJECT -- THREE TIMES ON 2026-08-21, TWO DIRECTIONS, ONE ROOT.** `grep --exclude lib/templates` (false negative, mine, caught by self-refute); **`$TOOLS` + `*_check.sh` in a GATED guard, clean on every commit while a whole thread's instruments are undeclared** (false negative, shipped, nothing to refute it); **`pgrep -f 'intentd'` matching `intentdb`** (false POSITIVE -- an unanchored substring over a COMMAND LINE, tripped by the estate's own restart context inside every MAAC node's `--append-system-prompt`). **A filename convention doing a population's job is the tell: an instrument is in scope for what it IS, never for what it is called.** And the third one only exists BECAUSE we run MAAC -- **the documentation broke the instrument by being carried.**
- **FILING TO AN INBOX IS NOT A DECISION REQUEST, AND THE WRITE SUCCEEDING IS WHAT HIDES IT.** hv, 2026-08-21: _writing stuff to that inbox like that DOESN'T WORK. It's ok for durability, but to get a response, I need to see the context, a question, with some options._ **A durable write with no decision shape gets read and not answered, and nothing distinguishes that from not being read at all.** I am the named reader of hv's inbox and I still made this error in the other direction on the same day. **Context, question, OPTIONS, in the live channel. The inbox is the record, never the ask.**
- **RUN A SUITE THROUGH ITS RUNNER, NEVER AROUND IT.** An entry point carries fixes and defences a lower-level invocation silently loses.
- **A POPULATION THAT EXCLUDES THE ANSWER RETURNS A CONFIDENT ZERO.** _N of N files lack X_ is worthless if the file that has X is not an N. **Ask what would hold X before counting what does not.**
- **AND THE EXCLUSION FILTER IS WHERE THE PREMISE HIDES.** 2026-08-21: I greped for guard dispatch across `.githooks`, `bin/.devbin` and `lib` while EXCLUDING `lib/templates`, reasoning that templates ship to consumers rather than run here -- and the delegated-roster design exists precisely because **this repo consumes its own template.** Confident zero, HIGH finding, said out loud to hv before the self-refute killed it. **A population is a claim about what could hold the answer, and `--exclude` is that claim in its least visible form.**
- **`grep -c` COUNTS LINES, NOT MENTIONS, AND MY OWN CHEAP SPLIT PENALISES THE BEST-FORMED CITATION.** `# ST0057 AT-01.5, covering AC-01.5` scores **1** -- identical to a sloppy single-id mention -- because thread, AT and AC are on ONE line, which is exactly what good practice looks like. **"2 hits means ready" is line-arithmetic wearing evidence's clothes.** Zero still means wrong. Anything else is READ, never counted.
- **A GREEN SUITE CANNOT GO RED FOR AN UNDISPATCHED INSTRUMENT -- STRUCTURALLY, NOT INCIDENTALLY.** Nothing runs, so nothing fails. Three gate rows are red for exactly that reason while the suite reports 100%. **The two are the same fact seen from opposite sides, and only one of them is visible from inside the estate.**
- **A COUNT OVER AN INCOMPLETE FILE IS NOT A MEASUREMENT.** Not a partial one -- not one. Same class as reading a `tail` as a total, and as taking `$?` from a pipe.
- **A TRUE MEASUREMENT OF A DIFFERENT PROPERTY, OFFERED AS PROOF.** Being real and driven is what makes it persuasive. **A green is only ever about the question the instrument asked, never the one the reader hears.**
- **FOUR WAYS A ROW PROMISES MORE THAN IT DELIVERS:** stale citation, uncited coverage, vacuous predicate, **title broader than body**. The first three leave a trace. **The fourth leaves none and no instrument can ever see it** -- the row is internally consistent and reads as covered to anyone who does not open it.
- **THE DISCRIMINATOR -- _what does satisfying this row completely still leave broken?_ -- IS ASKED AGAINST THE BODY AND NEVER THE TITLE.** Against a title it returns nothing, and nothing is also what a correctly-covered row returns. **A discriminator whose failure mode is silence must have its subject named every time.**
- **A DENOMINATOR BELONGS TO A FILE, NOT TO A TOPIC.** Name the file whenever you report a coverage defect. Adjacent bullets read as cause and effect while neither claims it.
- **NO INSTRUMENT CATCHES AN EXPIRED CITATION -- ONLY A BUILDER TRYING TO SATISFY THE ROW.** `at lint` exempts `to-write` from L2/L3, correctly. **The cheap split: does the cited file carry the row's own literal id?** Hits = ready to green; zero = wrong citation, or a declared handoff if the row says so.
- **A CRITERION IS SATISFIED WHEN A ROW THAT COVERS IT IS GREEN** -- never because its subject matter is tested somewhere. **Under the AND gate a second covering row holds the criterion open: when you green a row, watch the criterion count; if it does not move, the row was not the last one.**
- **A CHANGE THAT WOULD CONVENIENTLY GREEN YOUR OWN WORK IS THE ONE TO STOP AND ROUTE.**
- **A DOCUMENT CAN GO STALE AGAINST ITSELF, AND SOURCING IT CORRECTLY IS THEN WHAT DELIVERS THE WRONG ANSWER.** Nothing here checks a document's clauses against each other.
- **A RULING THAT EXISTS BUT IS NOT REACHABLE FROM THE SITE THAT NEEDS IT IS AN OPEN QUESTION IN PRACTICE.** Declining to guess is what makes it findable.
- **A COMMENT PROMISING THE FIX, ABOVE THE CALL THAT DEPENDS ON IT, STOPS THE READER WHO WOULD HAVE FOUND IT.** Worse than stale when it was never true.
- **A CORRECT FINDING CAN CARRY A REMEDY THAT READS FINE AND MAKES IT WORSE** -- and the remedy is the part nobody checks, because the finding was sound.
- **A GITIGNORED ARTEFACT IS INVISIBLE TO EVERY INSTRUMENT WE USE**, so a diff-based blast radius under-reports damage to exactly the artefact the model says matters most. **Not priceable as attention.**
- **`sync --to-store` IS DISK-AUTHORITATIVE FOR ATTACHMENTS; a typed field takes canon.** Prettier rewrites and re-stages **inside** the commit window, so the order is **FORMAT, then SYNC, then COMMIT** -- and idempotence is what makes that work. _Sync last_ is ambiguous and means the worse thing.
- **ZERO FAILURES ACROSS A WORKSPACE DOES NOT PROVE A BINARY RAN.** Confirm each subject in the `Running` list before moving a row.
- **A CRITIC SWEEP DRIVEN TO ZERO WITHOUT READING EACH SITE BREAKS WORKING CODE.** A greppable proxy cannot tell intentional word-splitting from accidental.
- **AN ADVISORY THAT REQUIRES SOMEONE TO REMEMBER IS NOT A CONTROL.** A guard nothing dispatches is indistinguishable from a guard that passes.
- **hv's WORD OUTRANKS MINE AND A PEER MUST NEVER READ MINE AS HIS.** I hold the pen he hands me, not his authority.
- **MECHANICAL:** `--no-fail-fast` always. `st list --status all`. `grep` is ugrep here, BSD grep in a `#!/bin/bash` script -- `-E` throughout; `grep -c` exits 1 on zero. **zsh does NOT word-split unquoted `$var`** -- 178 paths went in as one argument today. Never `$?` after a pipe. Absolute paths; the shell cwd persists. `CARGO_TARGET_DIR` inside the checkout. Read the clock, then PASTE.

## DECISIONS

- **2026-08-21** -- **THE v2 CLI SPLIT OUT OF THIS CHECKOUT** (`v2-maintenance` at `fb45e9ea`, `~/Devel/prj/Intentv2`), branched from MAIN not the tag because the fleet had never run the tag. hv moved all three bindings. **`bin/` is no longer load-bearing for anyone else.**
- **2026-08-21** -- `intentdb` **RETIRED corpus-wide**; it names no component. Quoted rulings corrected in BRACKETS with an editorial note, never silently.
- **2026-08-21** -- **the 62-of-67 gate is ST0057's CLOSURE gate, NOT the 3.0.0 release gate.** My own mislabel, live in three inboxes before it was caught.
- **2026-08-21** -- hv ruled: roster population widens and all parity instruments declare; criteria on undispatched instruments get DRIVEN not argued; **vc maintains `hv/wip.md`** under a stated provenance rule.
- **2026-08-21** -- hv ruled four: gate scope declared in canon (cc builds, vc verifies); every node prunes its own `target/<node>` at fold (dc); `upstream` freeze lift is standing; **vc maintains `hv/wip.md` on hv's behalf under a stated provenance rule.**
- **2026-08-20 17:36Z** -- suite green under hv's one-off authority; **credo N/A verified (no `mix.exs`, no `.credo.exs`), not assumed.**
- **2026-08-20** -- D57-8 amended to carry four collection addresses; **AC-07.1 NOT reopened**; AC-07.7 minted.
- **2026-08-20** -- AT-11.6's re-citation onto `prepush` **withdrawn**; deliverable restored to dc.
- **2026-08-20** -- AC-10.3's `project_id` limb **stands**; the value is a UUID, ruled by design three times over.
- **2026-08-20** -- AC-10.4 is _same criterion, better instrument_; **AC-10.15 minted** because AC-10.4's title was broader than its body.
- **2026-08-20** -- AC-00.8's clauses 4 and 5 **disambiguated, not re-covered**; documented and exercised are two criteria.
- **2026-08-20** -- ST0057/WP-04 and WP-06 closed on driven verification, not on their owners' reports.
