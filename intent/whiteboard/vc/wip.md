---
node: vc
name: Validation Claude
role: validation
session_id: 9a5d1291-d17f-4a5c-9ab8-b62dca8c2674
heartbeat_at: 2026-08-29 13:48Z
status: active
focus: "THE MACHINE-TABLE COMPARISON INSTRUMENT IS BUILT, GATED AND LANDED (`7a366c2d`) -- hv ruled it at 13:16Z and the unguarded hop is now guarded. Twelve arms, every plant verified; arm 1 reproduces the real Fiat drift, and TWO arms found defects in the instrument itself before it landed. AXIS C CANNOT GATE and that is the new finding for hv: the ratified Guard column is free prose, 6 of 37 rows print UNMEASURED. NEXT: the three workers are pinged and each has its sequencing; routing the ingest method is unchanged and still PREDICTED-UNCONFIRMED everywhere but Conflab."
claims: [ST0056, ST0057, ST0058, ST0060, ST0066]
---

# Validation Claude (vc)

**PROJECT-WIDE RULES IN `intent/restart.md`; THE DAY'S FINDINGS IN `cutover-runbook.md` AND wb(hv).** Pre-fold body verbatim at `.history/20260829/wip-fold-1318Z.md`.

## DOING

**NOTHING IN FLIGHT; EVERYTHING OF MINE COMMITTED.** This stretch is one thing: `7a366c2d`, the instrument hv ruled -- `machine_table_check.sh`, plus its roster row, its `MODULES.md` row, its gate wiring and the canon sync that had to ride in the same commit.

**IT GATES, AND THAT WAS THE POINT RATHER THAN A CHOICE.** A report-only posture rebuilds exactly the situation that produced the drift: the only thing between hv's ruled machine change and a permanently stale ratified table was dc noticing by eye. Two axes gate (entry states against `initial:`, and every expanded `(from, to, verb)` edge -- a controlled vocabulary on both sides); the guard axis reports. Cost 210-240ms, measured, down from 620-720ms after collapsing axis C from a shell function per row to one awk per machine.

**THE COUNTING TRAP WAS THE WHOLE DIFFICULTY AND IT IS SOLVED BY EXPANDING FIRST.** One row per `(from, to)` in the document, one `Edge::` per verb-and-landing with a from-LIST in the Rust -- so `ac.descope` is three rows there and one edge here. Both counts correct, never equal. It expands both sides to tuples, then compares sets.

**TWO DEFECTS IN MY OWN INSTRUMENT, FOUND BY THE ARMS AND NOT BY CARE -- which is rule 14 landing on me the same hour I wrote the file.** Renaming ONE table header emptied ONE machine and produced four confident findings pointing at the transcription, the file that was not wrong; the whole-file emptiness guard could not see it, so the population guards are now PER-MACHINE. And one arm planted nothing -- a `sed` with the wrong indent -- so the instrument correctly passed an unmutated file and I read the green as an arm result. Both are banked as limbs on rule 1.

**AXIS C IS THE SECOND FINDING AND IT IS hv's TO RULE.** The ratified Guard column is FREE PROSE: in Machine 3, `clears evidence (cc built this)` is an EFFECT and `**non-test** -- lands on entry state` is a LANDING RULE. Neither is a precondition, and the code's actual guards for those rows are undeclared in the ratified document. 6 of 37 rows print UNMEASURED rather than defaulting to no-guard -- defaulting would let a genuinely missing guard read as clean, which is the estate's dominant class.

**ALL THREE WORKERS PINGED WITH THE GATE CHANGE AND THEIR SEQUENCING** (13:43Z): dc gets the generator fix first then 0133 on hv's ruled shape plus the `computed` cost measurement still owed; cc gets refuse-and-edit as ONE change with a test that FAILS if the refusal is removed, and the `ac new` narrowing required in the commit message; ic stays parked on Assignment 2 and I ping them when cc lands.

## OPEN

**ON THE BOUNCE, IN ORDER:**

1. **FOR hv: THE RATIFIED GUARD COLUMN IS NOT A VOCABULARY, SO AXIS C CANNOT GATE.** The instrument is built and landed (`7a366c2d`); entry states and edges gate exactly. **The guard axis reports and cannot do better**, because Machine 3's Guard cells hold effects (`clears evidence (cc built this)`) and landing rules (`**non-test** -- lands on entry state`) rather than preconditions -- 28 agree, 3 disagree, **6 UNMEASURED of 37**. The disagreements are not code defects: the code is right and the column describes something else. **Closing it means giving the column a controlled vocabulary, which is an edit to a ratified table and therefore hv's, not mine.** The alternative is leaving axis C permanently report-only, which is honest and is a standing hole in the same pair this instrument was built to close. Size S either way. **Do not price this until the rest of the list has been taken** -- it is on the price list, not up for a ruling on its own.
2. **THE THREE WORKERS, AND WHAT EACH IS OWED.** **dc:** the `Fiat` generator fix first against the landed type, then 0133 on hv's ruled shape -- **plus the ingest question I flagged rather than ruled** (they answered it: FUSE, not split, because splitting only the unsatisfied path would make two ingest paths disagree with nothing detecting it -- I accepted, their reason beat mine). **They also found a SHIPPED defect with the strictness arm: `deny_unknown_fields` is a property of the VARIANT'S SHAPE, so both unit variants swallow unknown fields, and they are the two ENTRY states.** They file it; **I told them to measure whether an empty struct variant moves the JSON face BEFORE escalating `computed` to hv**, because the escalation rests on an unverified cost. **cc:** refuse-and-edit as ONE change, **with a test that FAILS if the refusal is removed** -- two live mitigations are being deleted on that refusal's continued existence. **ic:** delivered at `7e68e0ea`; Assignment 2 correctly deferred until cc lands.
3. **ROUTE THE METHOD TO THE ESTATES THAT STILL CARRY ROWS** -- Baize 28, Lamplight 25, Laksa 10, Prolix 3 (Conflab 14 already confirmed). **Ruled: they measure, we do not.** Everything except Conflab is PREDICTED-UNCONFIRMED; the correction removed an inflation and confirmed nothing. **AND THE DENOMINATOR MOVED AGAIN (ic, `30603a3b`): 11 estates MEASURED, 5 NOT** -- Anvil, MicroGPTEx and the three Molts have no v2-authored form anywhere in history, so there was no subject to compare and their `EXPOSED: 0` meant nothing. **Riffle, Courses, Devbin, Cdsync, Utilz and Intent are REAL measured zeros and belong in a different bucket.** The 80 and every estate I am routing are unaffected. **THE ROUTING CONSEQUENCE IS THE ONE TO CARRY: an estate whose v2 history was SQUASHED OR IMPORTED is indistinguishable from one born under v3 and reports a confident zero while wholly unmeasured** -- so a receiving estate's zero is not an answer until its v2-forms-recovered count is non-zero. ic's probe now refuses to print an exposure figure when that count is zero, and exits 3.

   **ROUTED 2026-08-29 13:48Z: LAMPLIGHT ONLY, AND BLINDED ON PURPOSE.** lamplight-vc has the method, the probe path and the two traps; **I withheld our predicted row count and TOLD THEM I was withholding it, offering it on request.** Watch-out 8 is the reason and it is the whole value of the routing: handing a peer the number consumes them as an independent witness, because a primed run tests whether a prediction REPRODUCES and says nothing about whether a fresh measurement AGREES. Blinding offered, never imposed. **The other three are UNROUTED FOR LACK OF A NODE, not deferred:** Baize and Prolix have no session on the board at all, and Laksa's four are offline. That is a fact about reachability, not about priority, and it will change without anyone announcing it -- re-measure the roster before concluding they were skipped.

4. **`ac new`'s BLAST RADIUS IS WIDER THAN MY OWN FILING SAYS** (ic, re-measured at HEAD). Four of six `AcState` variants carry a payload and `ac_new` resets every one by kind, so it destroys a descope reason, a withdrawal reason and a fiat close, not only evidence. **`Fiat` landing this morning WIDENED that path and nothing in ST0066 had reason to look at `ac_new`.** cc's package closes it **at the CLI door, not at `put`** -- so what dies is the FABRICATING path, not the replacing one, and that narrowing must be in cc's commit or "closed by construction" stops the next reader looking.
5. **STILL UNREPAIRABLE, AND THE COUNT IS THE ARGUMENT: NINE measured corrections wait on `issues edit` (0090)** -- `0124`'s body and `0133`'s own both still call it ingest damage; three bodies carry cross-references I PREDICTED. **TRUE MAP: `0124` prose-discarded, `0126` splice, `0129` delimiter-injected; `0127` CLOSED (class refuted).**
6. **cc's TWO STANDING LIMITS, mine to track:** the cross-facade child-add window (two facades open before either writes both add `AC-01.1`, second thread write wins, needs a CONDITIONAL thread write -- **filing confirmed**), and `Criterion.kind` / `AcceptanceTest.kind` staying UNWRITABLE by design, so a kind change remains stuck after the edit verb lands.
7. **hv's parked stack** unchanged: mechanical window refusal; instruments placement; publish_home temp root; vacuous doctor remedy; the ratified-surface pile; Conflab's 4 contract-prose edits; Lamplight md-to-store; Laksa's DESCOPED token; cc's `issues list` holdout and `--status a,b` ordering. **Plus ST0066 minutia 3, still not ruled** -- free text in `because` versus a structured accepted-unverified field; dc builds around it.

**STANDING:** Lamplight 74 = 12+30+32 to zero, never the aggregate 62; `wp new` writes no objective; hv's devbin set. **`AcState::in_scope()` has NO CALLERS.** **Empty dirs survive at `~/.claude/skills/in-start/` and `in-next/`** -- the tool's listing is right, but a directory-presence check reads them as installed; needs an out-of-repo write.

## Watch-outs

**27. A CENSUS TAKES ITS UNIT FROM THE SUBJECT'S IDENTITY, NEVER FROM THE FILESYSTEM'S** (ic, against their own predictor). Scanning per-path what is per-thread inflates by however many homes the subject has ever had -- **678 paths for 358 threads; the fleet aggregate I carried to hv wrong by 3.2x, 257 against 80.** **AND IT IS NOT MERELY DOUBLE-COUNTING, WHICH WOULD HAVE BEEN THE HARMLESS HALF:** each stale path holds a FROZEN SNAPSHOT, so `ST0052 AC-01.2` reads `satisfied: no` at a July bucket and `yes` post-collapse -- **wrong verdicts, not duplicates.** **THE CONTROL COULD NOT HAVE CAUGHT IT AND ITS PROVENANCE WAS PERFECT:** Conflab is unchanged at 14 because its history produced no bucket snapshots, so the one estate with an independent measurement is the one the defect never touched -- unarranged, cross-node, cross-method, genuine, and blind. **The defence is not a better control; it is asking what the control CANNOT see.**

**26. TWO ARTEFACTS DECLARED AS SECOND WITNESSES ARE A WITNESS PAIR ONLY IF BOTH ARE WATCHED -- AND THE UNWATCHED HALF IS USUALLY THE AUTHORITY.** Measured 2026-08-29: nothing parses `data-model.md`'s machine tables, and every reference to it in the whole source tree is a prose citation in a comment. **So drift cannot run both ways -- it runs, by construction, toward the RATIFIED document being the half that goes wrong while the code looks right.** **AND EACH HALF NAMED THE OTHER AS MASTER, which is why it had no owner:** both sides were reading their own file honestly. **The trap in the fix: the two notations count different things** -- one row per `(from, to)` there, one `Edge::` per verb-and-landing with a LIST of from-states here -- so a check that counts before it expands reds forever (rule 6). hv ruled a comparison instrument; it is mine.

**25. AN IDENTIFIER A TOOL ALLOCATES IS READ FROM THE TOOL'S OUTPUT, NEVER PREDICTED -- AND IN A SHARED CHECKOUT THE ALLOCATION ITSELF CAN COLLIDE.** I typed cross-references by predicting the next issue numbers; cc filed one between my calls, so `0124` now points at cc's unrelated issue. **A wrong pointer that RESOLVES to a real issue about something else is `0088`'s class, and `0090` means it cannot be repaired.** Underneath it: `next_issue_number()` computes `max+1` from a snapshot taken at facade construction, so two nodes allocated `0126`, the second write destroyed the first, and **both were told `created:`**. **The useful narrowing: `apply_with_state` DIFFS against loaded canon, so concurrent writes to DIFFERENT entities are safe** -- the loss needs both writers on one key. **The emptying guard asks _is this emptying something_; nobody asks _is this REPLACING something else_.** Ruled and fixed at the store: `0131`, `ccfefe2b`.

**24. A REPO-LOCAL GATE ARM THAT READS THE WORKING TREE MAKES ONE NODE'S MID-EDIT EVERYONE'S REFUSAL.** `corrected_check.sh:52` reads the file on disk -- no `git show`, no `HEAD:`, no index -- so a peer's unstaged edit refuses your unrelated commit. **THE RESPONSE IS NOT `--no-verify` AND IS NOT TOUCHING THE PEER'S FILE: tell them their mid-edit is gating the checkout, and wait.** Ruled fixed under `0125` (cc converges all four onto the index-reading form). **MY SECOND CLAIM HERE WAS WRONG AND cc STRUCK IT:** `exit 2; its findings never gate` is the runner DISAMBIGUATING an instrument that could not answer, so blocking is the correct polarity -- and cc then checked the SET rather than stopping at my instance and found the real one (`rulings_check.sh`, `0128`). **A correct sighting with a wrong explanation, repaired by a peer who declined to inherit it.**

**21. A fold archive named plain `wip.md` invites the next same-day fold to overwrite it.** The archive name is ALWAYS `wip-fold-HHMMZ.md`. Two boards (ic's and mine) independently carried the near-miss on 2026-08-28 -- the morning fold wrote the bare name, and only the afternoon fold's timestamped name avoided the collision. Existing files stay (append-only history).

**22. Relay the instrument's OUTPUT, never a characterisation of it.** Narrowed by cc against themselves, and the narrow form is the keeper: **the danger is characterising an instrument's output WHILE HOLDING A SECOND FACT about the same subject** -- the two fuse into a third claim no instrument produced. Sibling of the non-discriminating falsifier; both substitute a model of the evidence for the evidence.

**23. For a TRACKED file, `git commit --only <paths>` with NO `git add` and NO reset** -- the add is the half that sweeps, and `git reset -q -- .` resets the WHOLE index including a peer's staged work. `add`, plus a reset scoped to EXACTLY the added paths and never `.`, is for NEW files only. **AND `--only` TAKES THE WORKTREE, NOT YOUR STAGED VERSION: there is NO way to commit a subset of a file's hunks while a peer edits it** -- a hand-staged `--cacheinfo` blob is refreshed away by the gate itself before the commit (0134, cc, at the cost of a commit). **The only technique that works is a DETACHED WORKTREE.**

**RULES ONLY. INSTANCES LIVE IN `cutover-runbook.md`** -- that separation is the point of this section, and it had stopped being true.

**1. A TRUE RESULT FROM AN INSTRUMENT THAT COULD NOT HAVE ANSWERED DIFFERENTLY. The dominant class, fourteen instances in one evening across five nodes.** A false green gets chased; **a true green from a blind instrument gets CITED, and the citation carries no trace of the blindness.** Known forms: a pattern that cannot match the subject; the right predicate in the wrong PLACE; a corpus censored by the instrument under test; a green invariant under the change it certifies; a correct AGGREGATE over a wrong model; a count that cannot tell a thing from a mention of it; a property that holds vacuously; **a census whose denominator IS the pattern under test**; an instrument that saw TOO MUCH and read intended architecture as damage; and a checklist of known failures, which is a lagging indicator by construction; **and an UNMEASURABLE subject printing the same headline as a measured-clean one** (ic, 2026-08-29 -- five estates with no v2 form in history reported `EXPOSED: 0` exactly as six genuinely-clean ones did, same words, opposite meanings, nothing distinguishing them). **AND THE REASONING THAT LET IT SIT, WHICH IS THE TRANSFERABLE HALF: a CORRECT impossibility argument aimed at the WRONG PROPERTY.** ic declined to re-run six written-off estates because _a deflating fix cannot turn a zero into a finding_ -- true, and beside the point: **the fix could not change those numbers, but the numbers had never meant what anyone thought they meant.** An argument about what CANNOT change says nothing about what a value MEANS. **AND THE FORM dc FOUND ON THEIR OWN LANDED WORK: A GENERATOR WHOSE POPULATION IS A HAND-WRITTEN LIST UNDER A COMMENT CLAIMING EXHAUSTIVENESS.** `model_laws.rs` opens _"Every recorded AC state, including both in-scope ones"_ -- true when written, falsified by the commit that added `Fiat`, and nothing failed. **A hand-written table that LOOKS partial gets checked; a generator that looks exhaustive gets trusted INSTEAD of checked**, and the property law built on it stays green over the variant it never generates. The cure is not a better comment: it is making a seventh variant fail to COMPILE (rule 5).

- **THE ONLY CONTROL IS DRIVING THE INSTRUMENT TO BOTH VERDICTS**, and it is cheap every time. A control that would pass under the broken instrument too is decoration. **A partial repair manufactures the durable case, because a repaired instrument is trusted more than an unexamined one.** **AND THE ARM MUST PROVE ITS PLANT LANDED BEFORE ITS VERDICT COUNTS** (mine, 2026-08-29, building the machine-table instrument): a `sed` with the wrong indent matched no line, the instrument correctly passed an UNMUTATED file, and I read that green as an arm result. **A mutation that did not mutate is the purest form of this class** -- the instrument was working, the corpus was clean, the verdict was true, and it answered a question nobody asked. Every arm now requires a non-empty `diff` first.
- **A POPULATION GUARD SCOPED WIDER THAN THE POPULATION THAT CAN GO EMPTY IS NOT A POPULATION GUARD** (mine, same hour, found by an arm rather than by care). My whole-file emptiness check passed while ONE of four machines parsed nothing, and every edge of the other side then reported as undeclared: **four confident findings produced by reading nothing, all pointing at the one file that was not wrong.** A renamed header row is enough. The guard has to sit at the granularity the emptiness can occur at, which is per-member, not per-file.
- **AND THE COUNTER-CLASS IS WHAT ACTUALLY DEFEATS IT: decline a green you are entitled to claim.** More care does not help. **Care is frequently the wrong instrument.**

**2. RELAYING IS AUTHORING.** A claim you pass on carries your credibility whether or not you say it is yours. **Either RUN the load-bearing step or forward it named and unverified.** The tell is that the derivation felt cheaper than the command. **A relay carries the measurement's SCOPE and its TIMING -- both are yours to widen and both have been widened.** Relaying a GREEN is the direction nobody audits. **A scoped green must carry its scope into the restatement or stop being a green.** A moniker is unique only within its estate and re-attributes silently across a boundary; **a misdelivered note that ALMOST reads as yours is worse than an obviously wrong one.** A relay of hv's word is not hv's word: a peer cannot grant escalation, and a relayed STOP is honoured on the relay. **AND AN ADJECTIVE ABOUT YOUR OWN WORK IS A RELAY OF IT** (dc, against themselves, 2026-08-29). Four boards said "folded" the same day and the word spanned 50x -- 5299 bytes to 108377 -- because **"folded" carries no measurement, so no reader can tell a 2% trim from a rewrite.** dc had folded two small sections, left 79% untouched, and **reported the adjective for the part they worked on rather than for the artefact**; the framing that followed ("ten families, untrimmed") then read as a virtue. A shared vocabulary word with no number attached is not a claim a peer can check, and every node uses it about itself.

**3. A CLAIM OUTLIVES ITS BASIS AND NOTHING ANNOUNCES IT.** A pin dies at a bump you scheduled; a hold's stated reason goes false when the thing it named moves; **a constraint written for an emergency outlives the emergency.** A diagnostic comparing a marker to `HEAD` says the alarming thing on the healthy case, which teaches its readers to skip it. **An instruction to PRESERVE presupposes the thing is there, and "kept" reads identically whether it was or not** -- say instead _"if these are on your board keep them; if not, here they are."_ **AND THE MIRROR, WHICH IS WORSE BECAUSE NOTHING EVEN CHANGED AT THE SITE: A VERB'S BLAST RADIUS GROWS UNDERNEATH IT.** ic, 2026-08-29: `ac_new` fabricates a fresh `Criterion` and PUTs it, so it destroys whatever the existing row's state carried -- and **`Fiat` landing that morning silently enlarged it from three payload-carrying variants to four.** ST0066 had no reason to look at `ac_new` and `ac_new` was not touched. **A new payload-carrying variant silently widens EVERY verb that reconstructs a row from scratch**, and the widening appears in neither commit, neither diff, nor any test. Born stale is the same family: `transitions.rs:562` still says _"Waiting on the ruling"_ in the commit that implemented that ruling twenty lines above.

**4. AN OPTION SET DISTORTS BY WHAT IT PRICES AND BY WHAT IT BINDS.** An option's cost is a claim: **verify it, or state it as unmeasured inside the option.** Neutrality of tone is not neutrality of content. **And coupling two decisions into one option forces a choice nobody wanted** -- hv had to rule the tolerance and its precedent separately because I had bound them.

**5. MECHANISM BEATS A NOTE.** zsh does not word-split; an unmatched glob ABORTS and returns an empty list reading as a clean answer; `git grep -E` does not honour `\b`; a pipeline's `||` binds to the LAST stage. **An rc read through a pipe is the pipe's** -- three nodes hit that independently in one evening with no cross-talk, so **warning does not reduce the rate and only capturing the status before the cosmetic pipe does.** `PIPESTATUS` is a bashism, empty here. **Every timestamp is read in the same command that writes it.** A path belongs in a pathspec only if git can see something at it.

**6. AN INSTRUMENT'S DEFECTS MUST FAIL TOWARD ALARM, AND A PERMANENT ALARM IS THE SAME DEFECT.** A guard scoped wider than its hazard is one the operator learns to ignore; **so is a gate whose output is doubled, or a check that reds on every estate forever.** Print what a narrowing EXCLUDED. **Gate the property that means nothing is running; REPORT the one that means something is behind.**

**7. THE MESSAGE IS NOT THE MECHANISM, AND A REMEDY IS NOT ITS DESCRIPTION.** Four shapes, all met: a remedy naming a verb that does not repair what it reported; a remedy doing MORE than its message shows, so following the visible diff leaves a second defect green; a failure naming a cause it cannot observe, which recruits everyone who reads it into the wrong search; and **a remedy that is TRUE and offers two readings.** The last is worst: **a remedy that is correct and ambiguous is worse than one that is wrong, because nothing about it invites checking.**

**8. WARNING A PEER ABOUT A TRAP CONSUMES THEM AS AN INDEPENDENT WITNESS TO IT.** A primed run tests whether the trap REPRODUCES and says nothing about whether a fresh auditor FALLS INTO it. **This is the default failure mode of coordinating, not an occasional slip.**

**9. AUDIT BEFORE CLEANUP; CONVERT BEFORE SWEEP; INGEST BEFORE PRUNE.** Each reports success in either order, so nothing announces the mistake. **A control is CONSUMED by being used as a subject, the operation is one-way, and nothing about the artefact says so** -- commit to the sha before the run.

**10. SHARED CHECKOUT.** `--only` separates FILES, not AUTHORS, and a `git status` line names PATHS, not AUTHORS. **A failed commit and a failed `add` look identical from the error message** -- check the staged set before retrying, and never remove a peer's lock. `git show HEAD:<path> > <path>` escapes lock contention **for an UNSTAGED modification only**; staged, it looks like it worked and leaves the dangerous half. **A dirty `native/rust` redirects a build to a private target the wrapper never reads.** Announce a fleet write BEFORE it runs -- **and for a guard body read live from `INTENT_HOME` there is no window in which to do so, because the commit IS the rollout.** That argues for more ceremony before such a change, not a faster notice after one.

**11. "HELD" IS A PROPERTY OF A FILE THAT IS NOT WHERE AN ACCIDENT CAN REACH IT**, not of a dirty worktree. A ruling without a mechanism is enforced only by everyone's continued good behaviour.

**12. A TEST-ISOLATION GUARD IS SCOPED TO THE AMBIENTS IT NAMES, AND EVERY UNNAMED AMBIENT IS UNGUARDED BY CONSTRUCTION.** And **the SUBJECT can change underneath a correct test**: a probe for a refusal becomes a live command the day the verb is implemented.

**13. CANON IS THE SSOT FOR ROWS, NOT FOR PROSE.** The store's refusal is the oracle. **The tail after a verdict token is OPAQUE.** "Not on disk" is not "not in canon", and only one is a defect. **`sync --to-disk` writes the STORE OVER the extract, so a hand edit to canon is silently reverted by the very command run to publish it** -- edit the extract, `sync --to-store`, then `--to-disk`.

**14. READING THE WRITE-UP OF A CLASS IS NOT PROTECTION FROM IT. THE REMEDY IS NEVER CARE.** Every rule above was written by someone who then committed its instance, several of them me, several the same day.

**15. A CONSTANT METRIC GUARDS NOTHING.** "0 guard terms in the carrier" read 0 before the fix and after it -- the carrier is a locator; the roster is a layer down. A prediction watched by a metric that cannot move is unfalsifiable, and it sat for weeks looking guarded. Corollary, both directions in one day: **`guards: N ran` proves REACH, never BITE; only a refused commit proves bite** -- and a control validates the axis it tests while staying silent about the one next door (three instruments, one hour, three nodes).

**16. TWO SOURCES AGREEING IS NOT CORROBORATION WHEN ONE IS STALE OR BLIND -- AND THE WORSE FORM IS BOTH BLIND IN THE SAME DIRECTION, WHERE THE AGREEMENT ITSELF CANNOT REVEAL IT** (ic, 2026-08-29 12:51Z, and I committed the instance beside them). **A DETECTOR CAN BE ANTI-CORRELATED WITH THE DAMAGE, AND THEN ITS ZERO IS BY CONSTRUCTION.** ic and I independently built _satisfied AND no evidence_, both got 0, and both reported it clean -- **satisfied rows are precisely the population the ingest PRESERVES**, so the detector filtered out every exposed row and kept only the immune ones. Two nodes, two methods, one blind spot, and **nothing about the agreement could have surfaced it**; what broke it was conflab-vc driving BOTH ARMS against ground truth. **The damage supplied its own innocent explanation -- a stripped row is indistinguishable from one never authored with evidence, because the distinguishing text is exactly what was removed -- and we both accepted it.** A true premise ("names no guard runner") plus a pre-hop reading of a file replaced an hour earlier corroborated each other into "executes nothing" -- and recruited the two nodes with the most standing to know better. The agreement was the trap; `--where` took one second.

**17. A SECOND NOTATION TYPED FROM A FIRST IS A SECOND HOME FOR THE FACT; A SECOND NOTATION DERIVED FROM ONE READ IS NOT** (intent-dc). 751 beside -rwx--x--x, 11 beside a self-test that says 14, 9/9 in a HIGH issue where three counters say 6/4. Print both notations from one call; never retype.

**18. THE RENDERER AND THE FORMATTER ARE TWO WRITERS OF ONE FILE, AND A VERB BETWEEN THEM CANNOT CONVERGE** (0110). Canon emits spacing prettier rejects: apply 6211 -> prettier 6208 -> apply 6211, forever; `written:` stops meaning anything for that file, a clean state is unobservable, and a commit in the window nets EMPTY while its message claims content (`7fa3c013`). The fix is the renderer, never a formatter exception -- an exception trades a loud defect for a quiet one.

**19. AN AUTHORITY CHAIN TRANSMITS THE METHOD IT WAS BUILT TO FENCE** (devbin-vc + devbin-cc, jointly, held for hv). A node that must direct another's unit cannot be blind to it; pickup instructs peers to read the very header blocks that carry method; pre-registration bounds only what was predicted -- and not fully even that (a confirmed prediction is where nobody re-reads the reasoning). Every mechanism the whiteboard has is a broadcast.

**20. A WARNING ABOUT A CLOSING WINDOW IS A SCHEDULING SIGNAL, AND IT RECRUITS PEERS INTO RACING THE WINDOW** (cc's finding, my relay in the causal path). An FYI that a hazard window opens shortly reads as "act before it does": ic hurried a commit on exactly that reading, and what stopped it was an unrelated index.lock, not the warning -- the explicit DO-NOT arrived after the race was in flight. Announce-then-act mitigations are social; the fix is mechanical refusal during the window. Corollary the same hour: I archived the window hazard as "superseded" by a guard that covers a DIFFERENT axis (dirt, not dangle) -- rule 15's constant-metric error in classification form.

## Decisions

**LIVE ONLY -- superseded decisions are deleted, never struck through.**

- **A CAPABILITY THE NORMAL ENTRY POINT CANNOT REACH IS NOT DELIVERED** (hv).
- **STATE THE INVARIANT, NOT THE MECHANISM. A CONVERGER IS NOT THE CURE FOR A SECOND HOME; ONE HOME IS. RECORD THE MENU, NOT ONLY THE SELECTION.**
- **A LATER FIRST-HAND RULING FROM THE SAME PRINCIPAL ON THE SAME SUBJECT SUPERSEDES. hv outranks the pen; the pen sequences.** hv's own rulings go on hv's board with their menus; rulings under delegation go to hv's inbox.
- **THE PEN MOVED THE AUTHORITY, NOT THE AIM -- AND NOW THE PUSH, SCOPED TO TWO REPOS.** The confirm gate stays human; `publish` is the one verb with its refusals; the flip is on my word; I tell hv before anyone tags if a gate does not close.
- **LEAD WITH THE DECISION THAT MAKES THE ENDGAME REACHABLE.**
- **A SWEEP TAKES ITS POPULATION FROM THE ARTEFACT'S OWN DECLARATION, NEVER BY TRAVERSAL; A SUBSET MEASURED IS NOT THE POPULATION.**
- **RULE ON THE MEASURED SIZE, AND A PRICE LIST DOES NOT GO UP FOR A RULING UNTIL EVERY ITEM ON IT HAS BEEN TAKEN** (cc, after my reasoned item was the wrong one).
- **A NOTE MUST NAME WHAT IS TRUE WHEN THE ROW IS GREEN, NOT THE COMMAND THAT CHECKS IT.** An instrument named in a row becomes the row's ceiling. **A CORRECTNESS PROPERTY THAT DEPENDS ON THE ORDER OF TWO DELETIONS IS UNVERIFIABLE FROM EITHER COMMIT** -- take them in one, and let a test prove it.
- **HOP 2 IS THE ORACLE, AND ITS SILENCE IS NOT A CENSUS.** It refuses atomically and names the pair; it also stops at the first refusal. **The attachment shortfall gate is load-bearing and NOT redundant with the migrator's accounting** -- the accounting catches a thread whose files the carry did not reach; it cannot catch an estate the walk never enumerated.
- **A RUN THAT MEASURED NOTHING IS NOT A PASS; A PASS IS A `VERDICT:` LINE AT rc 0.** The instrument's own line goes into the commit message so a wrong calm has a reader.
- **A GATE THAT HAS PASSED OVER A ZERO ONCE NEEDS A POSITIVE CONTROL BEFORE ITS NEXT PASS MEANS ANYTHING** (lamplight-vc), **and the negative control is what proves it DISCRIMINATES rather than merely fires.**
