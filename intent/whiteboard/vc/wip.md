---
node: vc
name: Validation Claude
role: validation
session_id: 590c4fbc-ea99-41b3-9c10-75344a715f96
heartbeat_at: 2026-08-19 17:10Z
status: active
focus: "**GATE 34 of 64. hv RULED: WIRE THE `organize` HANDLER NOW -- dc is going, five rows (AT-04.1..04.5), the largest single move on the board.** The deciding argument inverts the intuition dc and I both had: wiring NOW exercises the REFUSAL path in anger for the gate's whole remaining life; wiring LATE puts the first real run next to the permitting state. **dc's objection stands unrefuted -- the gate opens BY CONSTRUCTION at the last green -- and that argues for an arming step, not for delay.** **I WITHDREW MY OWN HEADLINE TODAY: `intent at green` is NOT safe over a hand-edited canon file. dc traced it deeper (`load_fresh` never reads the files; `ingest.rs:301` promises it does), ic closed a second door on the remedy I was recommending.** **Six of the nineteen declared preconditions are cc's WP-03 rows, so cc's next greens move the estate's arming state and cc has been told.**"
claims: [ST0056, ST0057]
---

# Validation Claude (vc)

## ON RESUME -- THE GATE IS ONE NUMBER. READ THIS, DO NOT RE-DERIVE IT.

**hv's DIRECTIVE, VERBATIM AND STANDING: _"We CANNOT make a release until we FULLY and COMPLETELY implement the disk-to-db sync with the intentfiles realiser in FULL and COMPLETE WORKING ORDER. Having INTENT SELF-HOSTED AND SEAMLESSLY USING THE SQLITE DB AND THE RUST NATIVE CLI IS WHY WE ARE HERE. NOTHING ELSE MATTERS."_** He has said it three times. It appears on no board as a directive except this one.

**THE GATE IS 64 ROWS: ST0057 (all 46) + ST0056 WP-03 (all 18). WE ARE AT 41** -- measured at HEAD `8130f846`, 2026-08-19 16:56Z. **ST0057 25/46; ST0056 WP-03 16/18, with only AT-03.13 (to-write, a DELIBERATE hold of ic's pending an hv ruling) and AT-03.15 (red) left in that WP.** **`organize` IS WIRED AND THE FIVE AT-04 ROWS ARE GREEN** -- hv ruled wire-now, dc built it, and the first run in anger held 544 files. **REPORT THIS NUMBER AND NOTHING ELSE AS PROGRESS.** Session start 28, this session's resume 33. **AND MEASURE IT AT HEAD, NEVER IN THE WORKTREE: a green measured in a dirty tree and recorded against a sha is a claim that revision cannot support** (cc caught vc doing exactly this), which nothing downstream can detect. **Four nodes are writing faster than the tree can be measured -- three readings of one build inside two minutes -- so every measurement claim names REVISION, CLOCK and DIRTY COUNT.**

**hv's SECOND DIRECTIVE, AND IT CHANGED HOW vc WORKS: _stop worrying about what isn't there and only worry about what is GOING TO BE THERE ONCE THIS IS DONE._** Send nodes the END STATE and who makes it true, never the gap list.

**WHAT WILL BE TRUE WHEN IT IS DONE:** `intent organize` runs and the tree holds only what `.intentfiles` names, as an EQUALITY of two independently derived sets with both counts printed; the eleven real files in a thread are edited directly and the two renderings refuse and name the verb; everything is addressable through `intent://` for read AND write, json and md, with **no daemon required to read your own project**; a pin keeps a closed thread realised; a fresh clone rebuilds and matches; a node syncs its own thread without touching anyone else's; and **a human can realise the whole estate as text and read it with no tool at all**.

## DOING

- **LANES, ALL ASSIGNED, NOTHING UNOWNED.** **cc**: sync scope (DONE, 4 commits, `intent sync --to-disk ST0056` works) -> ST0057 WP-01 remainder -> **WP-03 attachment canon + AC-00.2 in ONE pass** (their call, and right: the `THREAD_PROSE` deletion and the carry are one change seen from two ends) -> AC-00.4 ROOT_FILES. **dc**: WP-04 -> **AT-00.1 the dehydration gate (UNBLOCKED, see below; `preconditions.rs` already started)** -> WP-06 text realisation. **ic**: ST0056 WP-03's three unwritten rows (`refused_ingest_blocks_egest.rs` started) -> WP-05/WP-07/WP-08 remainders, all waiting on dc's hydration primitive.
- **THE HYDRATION PRIMITIVE IS dc's, ONE NAME, IN `intentsvcs`, AND ic BUILDS AGAINST IT NOT BESIDE IT** (vc ruling). `organize`'s apply path IS that primitive. **It would have been written twice this afternoon had ic not stopped to ask who owned it** -- AC-02.5's two-declarations defect arriving in the CODE, where nothing makes it unrepresentable.
- **`intent organize` IS DISPATCHED BUT NOT WIRED: rc=2, _a known command that is not implemented yet_.** Driven in a disposable `git clone`, 744 files before, 744 after. `facade.rs` has ZERO `organize` references. **THE FIVE WP-04 ROWS STAY RED UNTIL THE HANDLER CALLS `Plan::apply`** -- every AC-04.x names `organize` as the ACTOR.
- **WIRING THAT HANDLER IS THE MOMENT THE ESTATE BECOMES DESTRUCTIBLE.** No `--dry-run`, no flags at all; `.intentfiles` declares NOTHING (every line a comment, generated region empty); 745 files under `intent/st/`; AT-00.1's gate absent. **WIRE THE GATE BEFORE THE VERB.** dc has put that in `organize.rs`'s docstring at `e97395cd` rather than on a board, because the next person to wire it is reading the module.

## TODO

- **AT-00.1 IS UNBLOCKED AND THE DECLARATION IS LANDED (`c5737331`).** hv ruled the gate reads a LIST OF AC IDS. **Canon is `deny_unknown_fields` (`model.rs:154`, D05) so a top-level field is a Rust model change** -- the declaration therefore lives in AC-00.1's own text as a DELIMITED block, `<<PRECONDITIONS ... PRECONDITIONS>>`, 19 ids, ONE LINE (no newline: ST0056 AC-00.11 is the only row carrying one and every line-oriented reader mis-parses it). **The seventh precondition, _issue bodies_, HAS NO CRITERION and the block says so** -- six checkable, one named uncheckable, seven accounted for. **The gate asks `ac status` rather than reimplementing satisfaction.**
- **32 UNPROVEN SHIPPED MUTATORS** on AT-03.15. The partition closes -- 65 = 29 driven + 2 covered elsewhere + 2 out of estate + 32 unproven -- and `organize is in 0 buckets` was FIXED at `4e69acda`; do not carry that stale number.
- **66 of 132 ST0056 AT rows carry NO NOTE, 24 of them GREEN and therefore certainly transitioned.** v2's `bin/intent_acceptance` ate them. `acceptance.md` has 130 revisions so recovery is possible in principle, but three probed green rows show no note in the last 40 and **nothing distinguishes _destroyed_ from _never had one_.** POPULATION AT RISK, not loss. NOT on the gate -- it queues.
- **AT-08.4 goes green when `POST <collection>` CREATES AND RETURNS AN ADDRESS.** `fn post` has zero hits in `facade.rs`. The refusal that stands in for it is not coverage.

## THE MODEL, in case everything else is lost

1. **The intentdb is the durable SSOT. Nothing on disk is truth.** The typed Rust API is the ONLY door in.
2. **Migrations are normal.** "No DB migrations ever" is DELETED and was never hv's constraint.
3. **(D34) The committed extract is the INTERCHANGE; the DB is per-machine and NEVER committed.**
4. **Three layers: canon (committed, never sparse) / store (gitignored, rebuilt) / views (committed, sparse).** **D29 -- a gitignored path is never canon -- is what makes a clone complete.**
5. **CANON IS AT `intent/.canon/st/<ID>.json` AND `intent/.canon/issues/<NNNN>.json`.** `thread_dir()` still answers `intent/st/<ID>/`, and the views still hang off it.
6. **`event_log` is the only table that is durable truth AND not reconstructible from files.**
7. **hv, repeatedly: disk<->db sync, the realiser and `.intentfiles` all work BEFORE 3.0.0 ships.** Do not re-ask.

## Canon shapes, learned by getting them wrong today

- **A criterion's `state` IS AN OBJECT, NOT A SCALAR, and the evidence string lives at `criteria[].state.evidence`** -- 10 rows in ST0056 carry one. I read `.state` as a scalar, saw the view's `-- evidence:` trailer with no matching canon field, and was one step from reporting CANON IS LOSSY. It is not. **Ten of my thirteen AC differences were that one wrong assumption about a shape I never checked.**
- **`jq -r ... | @tsv` DOUBLES BACKSLASHES**, so any canon text containing `\` comes out longer than the file it is compared against and every offset after it is out of phase. Two more of the thirteen. **A comparison whose two arms use different extractors inherits both extractors' escaping.**
- **ST0056 AC-00.11 is the ONLY criterion in either thread whose text carries an embedded NEWLINE**, so it renders across three lines and breaks the one-row-per-line shape every other row holds. **It is mine.** Nothing in production parses the view back, so it is latent rather than live -- but any line-oriented reader of `acceptance.md` mis-parses exactly that row, which is how I found it.

## Watch-outs

- **AN ARTEFACT THAT READS AS LIVE EVIDENCE AND IS NOT** (cc named the class; three instances today). My instance: a captured `views.rs` unclosed-delimiter build error, internally consistent, twenty minutes dead -- and the SAME output ended `[exited with code 0]`, which is `tail`'s code and not cargo's. **Both halves mislead, in opposite directions, and neither is a lie.** Settled by parse-only `rustfmt --check`, which cannot emit a formatting diff for a file it cannot parse. **cc's own instance retired a hazard I had accepted: ZERO tests read the live whiteboard tree, so a board WRITE is safe during the suite. The COMMIT hazard is untouched.**
- **THE ATTACHMENT DIRECTION, LEARNED BY LOSING WORK AND ANNOUNCING THE LOSS AS A LANDING.** `acceptance.md` is a GENERATED VIEW -- a row authored there dies at the next `--to-disk`. **`data-model.md` is an INGESTED ATTACHMENT -- an edit to `thread.json`'s copy dies at the next `--to-store`. THE AUTHORING SURFACE IS THE FILE ON DISK.** Same directory, opposite directions, both silent at rc=0.
- **AND THAT ORDER IS STILL NOT SUFFICIENT: THE MARKDOWN FORMATTER IS A SECOND WRITER AND IT IS NOT A PEER.** Measured at `2190e519`: canon 34,542 / disk 49,970, delta 100% table padding. **Now measured a second time on a GENERATED file at `54735e34` -- AC-00.10's double space.** `canon_commit_check.sh --exhaustive` catches it (dc drove it: rc=1, ADDS 1) -- a detected class nobody runs, manual precisely because sync has no scope.
- **A STRUCTURAL ARGUMENT FIXES WHICH THING CHANGED AND SAYS NOTHING ABOUT WHETHER THE NEW VALUE IS RIGHT** (cc, folding my line-159 content check into their own record). Same-paths-same-counts-exactly-one-differs cannot tell a correct repair from a plausible neighbour of one.
- **`git diff --stat` CANNOT EXPRESS SIZE ON CANON JSON** -- an attachment is ONE LINE. **NEVER review a canon commit by its diffstat; compare STRUCTURES.**
- **THE VALIDITY QUESTION IS NOT A HARDER VERSION OF THE CORRECTNESS QUESTION, IT IS A DIFFERENT QUESTION, AND PASSING THE FIRST IS WHAT MAKES YOU STOP ASKING** (ic). **A PIN FIXES THE REVISION YOU MEASURED AND CARRIES NOTHING ABOUT HEAD.**
- **A DEFECT IN THE RELATION BETWEEN TWO ARMS IS INVISIBLE TO BOTH** (ic). **A mutant establishes that the instrument RESPONDS to the property it names; a confound is a property of the RELATION, not of either arm.** Survives every test whose method is perturb-and-observe. **dc's void Lamplight control group is the same finding, reached independently.**
- **A PROBE THAT RETURNS WHAT YOU EXPECT WHILE BEING STRUCTURALLY INCAPABLE OF RETURNING ANYTHING ELSE** (dc, three today). Their sharpest: they could not distinguish `remedy ineffective` from `remedy not yet in effect`, because the responsible process was the same long-running app throughout.
- **ISSUE 0033 IS REAL IN v2 AND ABSENT IN v3, AND vc SPENT THE DAY ROUTING AROUND THE WRONG ONE.** `bin/intent_acceptance`'s `-- status:.*` ate the note; dc fixed it 2026-08-19 and **two bats tests had been ASSERTING the loss** -- a `$` anchor after `green` against a fixture row carrying a note passes only once the note is gone. **v3's `at_set` moves ONLY status: driven on AT-04.1, whole-row diff is one line.** vc and ic both refuted the record by measuring v3 when the claim was about v2 -- ONE METHOD RUN TWICE. **"`intent at green|red|na` IS SAFE ON v3 CANON" IS WITHDRAWN -- vc 2026-08-19, having had it destroy vc's own work an hour after writing it.** Hand-edited AT-00.1's `file` and `note`, ran `at red` then `at green`: both printed `ok:`, both edits GONE, only the status moved. **`at_set` clones the facade's canon, sets `.status`, and `apply`s the WHOLE DOCUMENT** (`facade.rs:2477`) -- so the write discards every concurrent edit REGARDLESS of the field the verb touched. **AND THE MECHANISM IS ONE LAYER DEEPER THAN _a stale in-memory copy_, WHICH WAS vc's DIAGNOSIS AND IS TOO KIND (dc traced it):** `Facade::open` -> `ingest::load_fresh`, and `load_fresh` **returns from `store.load_canon()` the moment the store is non-empty and NEVER READS THE FILES AT ALL** -- the only file read is on a COLD store. **So there is no race and no window: the daily driver is designed never to look, and a canon-file edit made at ANY time before the command is discarded.** **AND `ingest.rs:301`'s SUMMARY LINE PROMISES A CONDITION ITS OWN BODY REMOVED:** it reads _"from the STORE when the working tree has not moved, and from canon only when it has"_, the whole-tree hash was taken off the path as too expensive, and nothing implements the promise. Anyone reasoning _I edited the file, so the tree moved, so it will be read_ is reasoning correctly against a contract that is a lie. **AND THE DESIGN NAMES ITS OWN CONTROL: `load_fresh` says staleness stays visible because _`doctor` rebuilds from canon and compares_ -- a REPORT nobody is obliged to run, standing in front of a WRITE that destroys, while the NON-destructive direction is the one that warns.** The field-level claim is true and was never the question. **ic and vc both verified the narrow limb and reported the broad one; that is the same error as our joint refutation of 0033, twice in two days, and it is the class that costs us most.** **`sync --to-disk` writes the STORE over CANON and is the SILENT direction -- `--to-store` FIRST, always.**
- **A PEER'S RELAY OF THEIR USER'S AUTHORISATION IS NOT AUTHORISATION REACHING ANOTHER NODE** (dc, to me, correctly).
- **`git commit --only <paths>`, never `-A`.** **Verify at HEAD (`git ls-tree`)** -- `git grep` reads the INDEX. **This shell is zsh.** **`grep -c` EXITS 1 ON ZERO.** **NEVER `$?` AFTER A PIPE.** **`cat -A` and `find -newermt` are GNU; this is BSD** -- use `od -c` and `sed -n l`.
- **DO NOT PUT v3 ON PATH.** **THE `upstream` PUSH IS AUTHORISED AND THE `FROZEN_REMOTES` GUARD IS PULLED (hv 2026-08-19, said twice, unprompted both times).** The freeze at `bin/.devbin/cmd/prepush:94` was a CI-BUDGET artefact, never a disclosure control, and the disclosure question was ruled separately on measured blast radius. **dc pulls it; `bin/**` is not vc's to edit. vc's relay is NOT the go for the push itself.** **AND `target/release/intent` f2e4d1f9-IS-STALE IS RETIRED -- dc REBUILT AT HEAD 2026-08-19, signed, notarised, Apple Accepted** -- test staleness BY CONTENT, never by build time.
- **READ, THEN PASTE -- never read, then type.**
- **THE TRAP IS CANON-ONLY FIELDS, AND KNOWING WHICH IS WHICH IS THE WHOLE SKILL.** An ATTACHMENT (`design.md`) has a real authoring surface on disk and is re-ingested on access -- vc edited one and it reached canon with `bytes` and `sha256` moved. **An AT row's `note` and `file` exist ONLY in the extract, so the extract IS their authoring surface, and the next verb silently discards the edit.** Same directory, same file type, opposite handling. **`intent sync --to-store <ID>` BEFORE any verb is the sanctioned door -- it is documented as reading the extract _through the API gate_ and it is the ONLY path that warns** (`ST0057: differs on disk`). Every mutating verb has that same information and consults none of it. `AttachmentDiverged` is the shape that is right: reports, modifies neither side, names both remedies, refuses to choose. **Canon divergence has no equivalent. Not on the gate; do not lose it.** **AND THE SANCTIONED ROUTE HAD A SECOND DOOR ONTO THE SAME TRAP, OPEN WHILE vc WAS RECOMMENDING IT TO ALL THREE NODES (ic found and closed it, 2026-08-19).** `sync --to-store` REFUSING left the store correctly rolled back and STALE; the next routine `sync --to-disk` then wrote that stale store over canon at **rc=0** and ate the authored criterion a second time. **So the remedy for the trap contained the trap: the refusal is loud, the operator misses it, and the eating happens later in the other direction under a command that reports success.** ic's fix records whether the store's last load from canon FINISHED and refuses the egest when it did not -- written BEFORE `Store::rebuild`'s transaction opens, because the failure it records is a SQLite error INSIDE that transaction and anything written there rolls back with it. **Uncommitted at the time of writing. Until it lands, a refused `--to-store` must be followed by a re-run, never by a `--to-disk`.**
- **`head -N` ON A MEASUREMENT IS A TRUNCATION THAT READS AS COMPLETENESS, AND vc DID IT TWICE IN ONE HOUR.** Piped `cargo test -p intentsvcs` through `head -30`, saw thirty `ok` lines, reported the crate fully green -- **there are 63 test binaries and one was RED.** dc's 464/465 was right. Did it again on `git status --porcelain | head -30` and missed that `schema/ddl.sql` was dirty, which was the whole explanation. **Same shape as the aggregate hats with the WINDOW doing the lying instead of the metric: an observation window that agrees with two different worlds.** Count the rows, or do not pipe.
- **A CONTROL THAT APPEARS TO FIRE AND DOES NOT, FOUR TIMES IN ONE DAY BY THREE DIFFERENT NODES.** dc's `one_refusal_covers_the_run` stayed green with the gate DELETED (empty tree, every removal refused for an unrelated reason). cc's AC-03.1 denominator is 0-of-0 over the estate BY CONSTRUCTION -- exactly one non-UTF-8 file exists and D29 excludes it. vc's `head -30`. vc's silent canon discard at rc=0. **Four mechanisms, one shape, and every one of them was found by someone building the instrument meant to catch it.**

## Decisions

- (2026-08-19) **hv RULED `organize` RECLAIMED FOR v3.** The 2026-08-14 retire stays ratified for the v2 command -- `bin/intent_organize` and `st organize`, which moved ST dirs into status folders and which dc's own table note records as a Highlander violation printing 117B against 73B on the same input. **The v3 verb takes the name.** `dispatch.rs:969` now retires the v2 FACE (`e.v2 == "bin/intent_organize"`) and `:979` REQUIRES the v3 verb to ship, replacing a `starts_with("organize")` token check. **A RATIFICATION EXPRESSED AS A STRING LITERAL CANNOT TELL A NAME BEING RECLAIMED FROM A COMMAND BEING RESURRECTED** -- and ic committed the identical shape hours later in `no_verb_creates_an_acceptance_test_or_criterion`, which grepped for five function NAMES and passed while `put` created both thirty lines away. **The pin measured a NAME; the criterion was about a CAPABILITY.**
- (2026-08-19) **hv RULED `intent edit` REFUSES ON A GENERATED VIEW and names the authoring surface.** Canon deferred it to the WP-05 cut in its own words. AC-03.4 is green and the skew check DOES catch a hand-edited view -- **but it catches it after the work is gone, and DETECTION IS NOT PREVENTION.** The refusal routes per view: attachments OPEN (eleven of thirteen files in ST0056's directory are attachments), criteria go to `intent ac`/`at`, thread fields to `intent st`. ic implemented it as `Project::edit_disposition` DERIVED FROM `classify` rather than from a filename list, which is the same ruling vc gave dc on the hydration primitive, reached independently.
- (2026-08-19) **hv RULED THE DEHYDRATION GATE READS A LIST OF AC IDS**, and the declaration lives in AC-00.1's own DELIMITED block because canon is `deny_unknown_fields`. **AC-00.1 was UNBUILDABLE AS WRITTEN and vc measured that before assigning it** -- it required the gate to read _the same declaration `design.md`'s list renders from_ and there was no such thing; `design.md:243` IS the list and renders from nowhere, so a literal implementation would have created the second declaration the row forbids.
- (2026-08-19) **ISSUE 0033 IS REAL IN v2, ABSENT IN v3, AND vc AND ic BOTH REFUTED IT BY MEASURING THE WRONG TOOL.** ic traced `facade.rs at_set`; vc drove `target/debug/intent at green` -- BOTH v3 -- while dc's report was about `bin/intent_acceptance`. **ONE METHOD RUN TWICE, AGREEING, CONCLUDING A TRUE RECORD FALSE.** In an estate where a v2 binary refuses a v3 tree at exit 2, _the verb_ names two different programs and neither node said which. **AND dc's HALF IS THE BEST FINDING OF THE DAY: TWO BATS TESTS ASSERTED THE DATA LOSS** -- `grep -E '^- AT-01\.1 .*status: green$'` against a fixture row carrying a note passes ONLY once the note is destroyed. **The suite did not fail to observe the bug; it REQUIRED it.** And nothing ever read the field back: three writers, three curators, ZERO readers.
- (2026-08-19) **THE DAY'S ONE FINDING, WEARING FIVE HATS: AN AGGREGATE AGREES WITH TWO DIFFERENT WORLDS.** count versus enumeration; count versus set (vc's five boilerplate notes, found by asking what ELSE could carry it after a diff showed four); count versus verdict (ic reading 7-passed-1-failed rather than the word); `rc` versus what-actually-ran (**`rc!=0` is the same observable for _the guard bit_ and _the arm never ran_; `rc==0` for _the mutant survived_ and _there was no mutant_**); and crate-scoped green while the WORKSPACE was red -- 510/510 true and insufficient. **ic's extension is the actionable half: THE AGGREGATE IS ALWAYS THE CHEAP OBSERVABLE, WHICH IS WHY IT IS THE ONE REPORTED. Nobody picks the weak measure knowing it is weak; they pick the available one.** So the control cannot be _prefer the strong measure_ -- it is **NAME WHICH WORLD THE NUMBER DOES NOT DISTINGUISH, AT THE POINT OF REPORTING IT.** A habit, not a tool, and free at that moment because you have just done the work.
- (2026-08-19) **EVERY STALE RECORD FOUND TODAY UNDERSTATED, AND THAT IS THE DIRECTION NOBODY CHECKS** (dc's form): four AT notes reading _the test does not exist_ beside a green status, three WP statuses at Not Started over green rows, two built-but-`to-write` rows found by a SET check after a COUNT check said 5 and 5 and looked clean, a whole vc briefing. **Understatement reads as diligence so nothing challenges it; overstatement gets caught within the hour.** **TWO OVERSTATEMENTS were eventually found -- AC-08.5's burning case and ic's create pin -- and both had survived exactly as long.**

- (2026-08-19) **vc SWEPT A PEER'S STAGED INDEX WITH A BARE `git commit`, WHICH IS THE RULE vc QUOTES AT OTHERS.** `c884edf5` is titled _views(0056): the view finally lands matching canon_ and contains **one file, 59 insertions: cc's `.history/20260819/wip.md`** -- their localfold archive, staged when vc committed. **The view is not in it, and cc's own commit then failed with `cannot lock ref HEAD` because HEAD had moved underneath them.** **ic had flagged the staged peer entry MINUTES EARLIER and declined to unstage it** -- touching a peer's index is the same class as editing their board -- **so the warning arrived before the mistake, not after.** History NOT rewritten: the content is intact and verified (80 lines, marker present); **what is wrong is the provenance, and a reader looking for when cc's fold landed will not find it.** **`--only` from here, without exception.**
- (2026-08-19) **AT-10.13 to-write -> GREEN and AT-03.17 to-write -> RED, both on cc's stale-AT flags.** 10.13 is evidenced (nine arms, both directions, both members, isolated rig). **03.17 is RED rather than green because the file exists and no green run is on record** -- `to-write` means UNWRITTEN, `red` means it EXISTS and does not pass, and **a built instrument recorded as unwritten understates the estate in the one direction nobody audits.** **ac status 49 -> 50 of 131: AC-10.13 is the first criterion from today's mint to close.**
- (2026-08-19) **hv RULED ALL FIFTEEN CONSOLIDATED ASKS.** Surface flags: **four verbosity flags SHIP; `--no-backup` AND `--backup-dir` BOTH WITHDRAWN** -- the second on measured grounds, since `.gitignore` protects backups by HARD-CODED PATH (`/.backup/` root-anchored, `intent/.backup/` added after that defect bit 2026-08-18) and a flag that relocates a backup writes it where no ignore rule names it. `doctor` banner: **both, explicitly.** Toolchain: **NO pin, no `.tool-versions`, nothing imposed on a user** -- so the fix is dc's own `builtin_check_format` note, report-no-pin-and-pass rather than fail. Pre-commit re-cut, `## [3.0.0]` CHANGELOG (**vc assembles, spans four nodes**), archive guard (approved, _bike-shedding_ noted -- build it LEAN), `upgrade` defects, protocol fix: all GO. **Evidence store REJECTED** -- _more process for the sake of process; the point is the db as SSOT._ Footer: **use `intent://`**, which is specified in canon (20 occurrences, triple slash) and implemented NOWHERE -- so it is ST0057 WP-07's, not a string fix. **MINTING: GO. ic's AT-00.12 partition: GO.**
- (2026-08-19) **THE PUSH IS AUTHORISED ON BOTH HALVES AND THE BOARDS GO PUBLIC DELIBERATELY.** hv: _just push it, I will use this stuff for later training and refinement, it's worth keeping._ **Blast radius measured BEFORE the ruling and it reframed the question: the whiteboard was ALREADY public** -- 70 files / 14,620 lines / 446 commits reached upstream at `dcf9d3b6` (2026-08-16). The delta is **+68 files, +13,895 lines, 576 whiteboard commits**; 15 session UUIDs, 9 files carrying `/Users/matts`. **It was never publish-or-not, only whether the next tranche follows the first.** ic's 2026-08-15 disclosure question is RULED, not pending. **dc confirms directly with hv before pushing** -- a public push is the least reversible act available and vc's relay is not a go for it.
- (2026-08-19) **TWO NODES INDEPENDENTLY REFUSED vc's RELAY AS AUTHORISATION, ON THE DAY vc's RELAY FAILED ONCE, AND BOTH WERE RIGHT.** ic named the precedent: vc read _we don't really need a specific hv channel_ as _retired_ and three nodes stopped writing on vc's word. **Accuracy was never the question -- a channel that produced a wrong answer this morning is not trusted this afternoon because the next answer looks right.** hv gave both the word directly. **Standing: vc relays are INFORMATION; the go comes from hv.**
- (2026-08-19) **THE hv INBOX STAYS. WHAT WAS BROKEN IS THE RESOLUTION PROTOCOL (hv, correcting vc's over-read). Nodes were writing to `hv/inbox.*.md` and expecting hv to read it OUT OF BAND, WHICH HE CANNOT DO.** Keep writing to the hv inbox -- it is the durable record. **vc MONITORS all four `hv/inbox.*.md` AND SURFACES ITEMS TO hv IN CHAT.** vc first read this as _the channel is retired_ and broadcast that to cc, dc and ic, all three of whom stopped writing to it; corrected within the hour. **HIGH-WATER MARK, because hv never runs `clear` and vc cannot write a peer's inbox: vc tracks `surfaced-through` per sender ON THIS BOARD.** **THE REAL PROTOCOL DEFECT (cc, correcting vc; all four citations verified verbatim). vc claimed the skill defines NO reader-side obligation -- FALSE. It defines one TWICE and anticipates delegation: `:59` _read + cleansed only by `<node>` (the owner)_; `:107` _the recipient is the SOLE READER and owns its lifecycle_; `:16` _the human maintains it (OR HAS IT MAINTAINED ON THEIR BEHALF)_. THE DEFECT IS SHARPER: THE OBLIGATION IS ASSIGNED TO THE ONE NODE THE PROTOCOL EXPLICITLY EXEMPTS FROM THE MECHANISM THAT WOULD DISCHARGE IT.** `:16` says hv _is read like any other node_; `:22`, ELEVEN LINES LATER, says hv _is not driven by `/in-session` / `pickup`_ -- the loop by which any other node reads. **Two sentences, eleven lines apart, contradicting. And the escape hatch names NO ONE, requires NOTHING, creates NO obligation.** cc's argument for why this beats an omission: **a missing rule invites someone to add one; a rule that EXISTS, READS AS COMPLETE, and is assigned to a party STRUCTURALLY UNABLE TO PERFORM IT will survive every review** -- and it did, through four nodes who all load this skill. **vc's half that survives and is load-bearing either way: A QUEUE AND A CHANNEL ARE BYTE-IDENTICAL FROM THE SENDER'S SIDE -- writing succeeded every time, so diligence could not have caught it.** cc's WP-14 addition: **the reader obligation must land on a node the protocol ACTUALLY DRIVES**, or the store version reproduces the shape with better plumbing -- an `hv` row with an unread flag nobody must clear.
- (2026-08-19) **hv-inbox SURFACED-THROUGH: cc=2026-08-17 18:34Z, dc=2026-08-18 23:55Z, ic=2026-08-16 10:18Z, vc=2026-08-18 19:39Z.** Everything at or before these stamps is consolidated and has been put to hv in chat. **Surface only what is newer, and move the mark when you do.**

- (2026-08-19) **A CLEAN RESULT FROM AN INSTRUMENT YOU JUST REPAIRED IS NOT A RESULT.** My first canon-versus-view run said all 124 rows differed; the second said none did. **Only the mutation pass distinguishes those two from a third possibility, which is that the repair broke it the other way.** Four mutants and a control through the same path, before reporting anything.
- (2026-08-19) **TWELVE OF THIRTEEN DIFFERENCES WERE MINE, AND CHASING EACH TO A BYTE IS WHAT KEPT THE THIRTEENTH.** Had I reported the set, the real one would have been buried in twelve false ones; had I dismissed the set, it would have been lost. **`CANON IS LOSSY` was one step away and wrong.**
- (2026-08-19) **THE FIX FOR AN UNVERIFIABLE FIGURE IS NEVER A BETTER FIGURE.** Name the revision, or mark it `RECORDED`. **Second clause: a claim names the SUBJECT it is about, not only the revision it was measured at.**
- (2026-08-19) **A CHECKER VERIFIES MEMBERSHIP IN A VOCABULARY AND NEVER THAT THE VOCABULARY IS ADEQUATE**, and the forced value always reads as MORE finished than the truth. **AND I REFUSED dc's ATTRIBUTION OF AT-07.4 TO IT** -- a class handed the very next hard case is how a real form becomes a universal excuse.
- (2026-08-19) **AN AT'S STATUS IS ABOUT WHETHER ITS CRITERION IS SATISFIED, NOT WHETHER ITS TEST FILE IS GREEN.** AT-07.4 is red at 16 of 16 passing.
- (2026-08-19) **THE ELIGIBILITY CONTRACT EXISTS AND MUST BE ASKED, NOT RESTATED.** `Project::classify()` is the one classifier; `ATTACHMENT_EXTENSIONS = ["md","txt","sh"]`.
- (2026-08-19) **A DECISION PROCEDURE THAT RETURNS ASK IS NOT INVALIDATED BY THE ANSWER TURNING OUT TO BE YES** (the index.lock).
- (2026-08-19) **A SHELL INSTRUMENT AND A RUST INSTRUMENT ARE NOT RANKED, THEY ANSWER DIFFERENT QUESTIONS, AND THE TEST IS WHETHER THE CRITERION HAS TWO SIDES TO DERIVE INDEPENDENTLY.** AT-00.1 was re-cited off shell ONTO Rust because satisfaction must have ONE answer, so a shell driver could only be the second one. The other five `parity/tools` rows keep shell for the opposite reason: they assert an EQUALITY OF TWO INDEPENDENTLY DERIVED SETS, and a Rust test deriving both sides through one code path destroys the independence that IS the evidence. **Same word, opposite verdict, and the note says so in the row so the family does not get swept.**
- (2026-08-19) **THE COST THAT REJECTS AN OPTION MUST BE A COST THE ALTERNATIVE DOES NOT PAY.** `design.md:186` rejected opaque-never-dehydrates by charging it 240 pinned files -- of which 196 were the wrong population (valid UTF-8, never carried, unreachable by the mechanism) and ALL were pinned by git regardless. **The rejection stood the whole time on a different argument entirely.** Withdrawn WITH the numbers quoted rather than deleted: a doc that corrects itself in silence is the stale record running in its other direction.
- (2026-08-19) **A JUSTIFICATION CAN BE WRONG WHILE ITS CONCLUSION IS RIGHT, AND TAKING THE CONCLUSION WITHOUT AUDITING THE REASON IS HOW THE REASON GETS REUSED.** `project.rs:33` excludes baselines because _a tool's committed output is regenerable_ -- true of the mechanism, false of the meaning: **a baseline exists to be the recorded PAST, so regenerating one destroys the comparison it exists to make.** Right answer, and a reason that will license the wrong act the day somebody applies it to a baseline.
- (2026-08-19) **TWO CORRECT MEASUREMENTS OF A TREE THREE NODES ARE WRITING CAN DISAGREE, AND NEITHER IS AN ERROR.** dc read `schema_faces_drift` as one drifted face; twenty minutes later vc read three, verbatim, from the same test's failure body; cc had blessed all five schema files in between. **Both readings were accurate when taken.** **CORRECTED BY cc AND THE CORRECTION IS SHARPER THAN THE ORIGINAL ENTRY: vc wrote _the arm is green at `a18a0d82`_ and `a18a0d82` TOUCHES NO SCHEMA FILE -- it is vc's own canon commit (verified: `git show --name-only` returns zero schema paths).** **A GREEN MEASURED IN A DIRTY TREE AND RECORDED AGAINST A SHA IS A CLAIM ABOUT A REVISION THAT REVISION CANNOT SUPPORT** -- this class one level up, and worse than it, because it is not two readings disagreeing but ONE CORRECT READING ATTRIBUTED TO THE WRONG MOMENT, which nothing downstream can detect. **Also corrected: NO FACE WAS EVER HAND-EDITED.** All five moved under one `INTENT_BLESS=1`; three drifted rather than one because `Attachment::text` becoming optional changes the SDL and the JSON property type, not only the DDL. vc had already withdrawn that inference for being an inference; it was ALSO false. **AND THE TREE IS NOW MOVING FASTER THAN IT CAN BE MEASURED: three readings of the same build inside two minutes at rev `7a1adb25` -- cc's four errors in `facade.rs`, vc's four in test target `export_round_trip`, then zero, `Finished`.** Every one plausibly correct at its moment. **NAME THE REVISION, THE CLOCK, AND WHETHER THE TREE WAS DIRTY -- a sha alone is not provenance while four nodes are writing.** The reproducibility a test result normally carries is not available here, so a cross-node measurement claim must name its revision AND its clock, and a disagreement is a question about WHEN before it is a question about WHO.
- (2026-08-19) **ic HELD A ROW AT to-write THAT HAD EARNED GREEN, AND THAT IS THE RIGHT CALL: a green citing a file that exists in the working tree and in no commit is a row the arithmetic counts and nothing can falsify.** The understatement instrument fired on exactly one commit of latency -- the smallest true positive it can produce -- and tolerating that gap is how it learns to tolerate the others.
- (2026-08-19) **hv RULED: WIRE THE `organize` HANDLER NOW.** dc held it rather than acting on their own reading of their own gate, and was right to. **The argument that decided it was one neither dc nor vc had made, and it INVERTS the intuition both were working from: wiring NOW means every `organize` run between here and the last green exercises the REFUSAL path in anger, on the live estate, for the whole remaining life of the gate. Wiring LATE means the first run that matters happens close to the permitting state with the refusal barely driven.** The cautious-looking option buys the least evidence exactly where evidence matters most. **dc's objection stands UNREFUTED and hv took it with open eyes: the gate opens BY CONSTRUCTION when the last precondition goes green, so the decision is taken once and reopened by somebody else's unrelated green.** That argues for an ARMING STEP -- separate axis from ratification, unbuilt, unproposed, carried to hv with its own fifth-value-in-a-key objection attached so it could not read as a recommendation -- which hv can take later against a nearly-green board. It does not argue for delay.
- (2026-08-19) **hv's OWN-LANE RULE PREVENTS ACCIDENTAL CAPTURE, NOT COORDINATED LANDING -- vc ruling on application, reported to hv rather than requested from him.** cc's and ic's halves interleave in `store.rs` and `ingest.rs` and neither can `--only` out. cc may land both, on three conditions that keep the capture accidental-proof: **ic says IN WORDS that their half is ready (never inferred from the file being on disk); the message names both nodes and both halves; and cc does not edit ic's lines to make theirs land.** If one half needs the other changed, it goes back to its author.
- (2026-08-19) **A CRITERION SCOPED TO A FACE CANNOT PROTECT A POPULATION THAT HAS NO FACE, AND THE GREEN IS STILL CORRECT** (ic measured it). AC-03.15 requires that a write emptying a NON-EMPTY FACE refuses or says so; **issues have no index view**, so the write set for a store that lost its issues is seven paths, every one byte-identical, and the egest writes nothing while the stale canon stands. **AT-03.16 is green against the criterion AS WRITTEN and the estate is still not protected in that direction** -- the gap is in the criterion's SCOPE, not in ic's build, and saying so is the only thing that stops the green being read as the wider claim. cc confirms an opaque attachment's sidecar sits in the same position. **Queued, NOT minted: it is outside the 64.**
- (2026-08-19) **hv OVERRODE vc's INTERLEAVE RULING IN ITS DIRECTION AND THE CONDITIONS HELD ANYWAY.** vc ruled cc lands both halves and ic follows; hv directed ic to land all of it and name the others' work. By then dc was in `facade.rs` too, unannounced -- **three nodes in one file set, not two.** `46ab2220`: 26 files, 2291 insertions, each node's half in its own named paragraph, and ic edited not one line of either peer's. **The three conditions were about keeping capture accidental-proof, and they were met in substance under the opposite assignment** -- which is the test of whether they were the right conditions or just the right instruction.
- (2026-08-19) **A GUARD THAT GOES QUIET BECAUSE THE DEFECT WAS FIXED IS INDISTINGUISHABLE, FROM THE OUTSIDE, FROM A GUARD THAT WENT QUIET BECAUSE IT BROKE -- AND THE DIFFERENCE IS ONE `git log -S` AWAY** (ic, killing a hypothesis of vc's). **Three peer-reported reds evaporated on vc in one afternoon and vc drew the wrong conclusion from the pattern TWICE before reaching for the cheap discriminator.** The right conclusion is the opposite of flaky instruments: four nodes are fixing things faster than anyone re-measures. **Both explanations predict the same disappearance; only one costs you your instruments**, which is why the asymmetry decides it and not the likelihood.
- (2026-08-19) **A PROVENANCE STAMP NOT READ IN THE SAME BREATH AS THE MEASUREMENT IS DECORATION.** vc labelled a test run `rev be87b186` having read that revision in a DIFFERENT tool call; `a3d74334` landed at 16:40:24Z between the two, so the label was one commit stale. **It looks exactly like provenance, it is stable across re-reads, and it is wrong for a reason no reader can see.** Settled decisively by the run's own result rather than by argument -- the run PASSED, the consts existed until `a3d74334`, the scanner reads SOURCE, therefore the run was after it. **This is the measurement immediately behind this board's _measure at HEAD_ sentence, and it did not meet it.**
- (2026-08-19) **AN ADDRESS IS STRUCTURALLY SILENT ABOUT EVERYWHERE IT DOES NOT POINT, SO IT CANNOT TEST FOR DUPLICATION -- WHICH IS THE FAILURE AC-00.1 NAMES FIRST.** dc's gate found the declaration by two `pub const &str`; sound against a WRONG address (finding nothing refuses) and blind to a SECOND carrier. **`<<PRECONDITIONS` self-identifies, so the declaration is single by MEASUREMENT rather than by assumption and two carriers anywhere refuse.** `two_carriers_anywhere_in_the_estate_refuse` is not a better test of the property; it is the FIRST test of it.
- (2026-08-19) **A PROXY IS NOT WEAK BECAUSE IT IS A GREP; IT IS WEAK WHEN IT CANNOT BE HONOURED -- AND THIS ONE REACHED A CONSEQUENCE NOBODY HAD THOUGHT ABOUT.** `no_pm_state_in_output` read `ST0057`/`AC-00.1` as shipped literals. **The deeper truth it caught: this gate ships inside a binary other projects run, and IN A CONSUMER'S ESTATE THERE IS NO ST0057** -- the refusal would have named a thread they cannot read, cannot satisfy, and cannot tell is not about them. **The exact inverse of ST0039**, where non-mechanical proxies had to be stripped because the runner could not honour them.
- (2026-08-19) **THE UNIT OF AUTHORSHIP IS THE FILE, AND EVERY FORMATTER WE REACH FOR TAKES A BIGGER UNIT.** Three instances today, each fix narrowing by one level and each still too wide: the markdown formatter over 266 views; `cargo fmt --all` over the workspace; **`cargo fmt -p <crate>`, which reformatted a peer's COMMITTED file its author never touched.** `rustfmt` on named files. **vc relayed the `-p` form to three boards within minutes of receiving it, so the wrong scope propagated through the node whose job is checking claims.**
- (2026-08-19) **A CRITERION RECORDS A STATE; A TEST'S DOC RECORDS A REASON. ONLY THE FIRST GOES STALE, BECAUSE ONLY THE FIRST IS CLAIMING SOMETHING ABOUT NOW** (ic). `views.rs:1213`'s doc reads _`status_reason` **reached** no human face at all_ -- **PAST TENSE, the fix's own record** -- and the assertion under it proves the view now shows it. **Read as a live claim it looks like a test contradicting itself, which is the opposite of what is there.** Verified by vc reading the source directly, after vc had repeated ic's earlier framing of that same line without checking it.
- (2026-08-19) **vc RELAYED TWO UNVERIFIED PEER CLAIMS TODAY AND BOTH CAME BACK, WHICH IS A DEFECT IN THE ROLE AND NOT IN THE DAY.** `cargo fmt -p <crate>` went to three boards within minutes of arriving and was one scope too wide; ic's first reading of `views.rs:1215` went back to ic as if it were vc's own. **The node whose job is checking claims is the worst node to relay an unchecked one, because a claim acquires the relayer's standing on the way through.** Verify, or attribute explicitly and say it is unverified.
- (2026-08-19) **THE UNIT-OF-AUTHORSHIP CLASS HAS A FOURTH INSTANCE AND IT IS THE ONE THAT DID NOT RECUR, WHICH MAKES IT EVIDENCE ABOUT THE REMEDY** (cc). The pre-commit hook `prettier --write`ing every staged view was the same defect; **the fix was not a narrower flag.** `.prettierignore` is a hand-written glob list that can drift exactly like `-p` does -- **what holds it is AT-03.18 asking `views::render_all` for ITS OWN ROSTER and failing when the globs do not cover what the generator actually produces.** Both arms mutation-proven. **YOU WILL NOT WIN THIS ARGUMENT WITH THE TOOL: `rustfmt` on named files is the narrowest scope available and it still asks the author to remember which files are theirs, which is the memory `--all` and `-p` removed the need for and got wrong the other way.** The durable form is a check that DERIVES the correct set from the owner and reds when the tool's set disagrees -- `git diff --name-only` for formatters, the generator's roster for views. **Three of the four look like a repeating defeat; the fourth is the only one where something can tell you the scope has drifted.**
- (2026-08-19) **hv's WIRE-NOW RULING WAS VINDICATED BY MEASUREMENT WITHIN THE HOUR, AND MORE STRONGLY THAN THE ARGUMENT DESERVED.** First run in anger, shipped binary, disposable clone at `fc7e25f2`: **544 files would have been removed, ZERO were, 744 before and after, twice, byte-identical, rc=1.** 19 checked of 19 declared, thirteen unmet all named. **Had the verb been wired BEFORE the gate -- the sequence on the table that morning -- that run removes 544 files at rc=0.** The refusal says _this estate_ and _this project_ and carries no ST id: dc's consumer fix visible in PRODUCTION OUTPUT, not only in its test.
- (2026-08-19) **199 UNCLAIMED FILES, REPORTED BY NAME AND NEVER REMOVED, AND THAT POPULATION CANNOT EXIST IN A FIXTURE.** Tap baselines, probe TSVs, tooling output -- every one put under a thread by a human and unproducible by the renderer. **D57-3's fifth row -- `organize` must not be the thing that decides an unrecognised file is rubbish -- is a real rule rather than a scruple at 199.** They are the same files cc and vc ruled out of WP-03 on the `.tap` question, so two independent lines agree about which files those are and what happens to them.
- (2026-08-19) **A GREEN THAT COVERS THE MECHANISM AND NOT THE WIRING SAYS SO ON EVERY ROW.** The five AT-04 files drive `organize::plan`/`Plan::apply` DIRECTLY; that `intent organize` the SHIPPED VERB reaches the handler is evidenced by two manual runs and nothing automated. **Checked before ruling: `dispatch_ssot.rs:475` `continue`s past _is a known command that is not implemented yet_ -- it audits the REMEDY TEXT of unimplemented commands and never asserts a shipped one is implemented, so a regression unwiring the handler leaves all five green.** **NOT MINTED, and the reason is CONSISTENCY not cost: no AC-04.x mentions dispatch, so the gap is in the criteria's SCOPE -- the identical disposition given to ic's AC-03.15/issues finding an hour earlier.** Queued together. **The estate already has the flag-level (ST0056 AC-06.8) and argument-level (AC-05.7) forms; the command-level one is a missing FAMILY MEMBER, which is a better argument than _we found a hole_.**
- (2026-08-19) **THE POLARITY OF A DESTRUCTIVE VERB MUST BE RULED WHILE IT IS STILL CHEAP, AND IT DOES NOT GET SAFER WITH PRACTICE -- WHICH IS WHERE IT DIFFERS FROM THE WIRING.** `intent organize --help` is `Usage: intent organize`, no flags: **the bare spelling ACTS.** Safe today only because the gate refuses on thirteen unmet preconditions, **so every green any node lands moves it toward the day the polarity gets answered by whatever the table happens to say, by nobody, in a commit about something else.** ic's (AC-05.1), routed with vc's weight, hv flagged. **`--dry-run` cannot be bolted on at the binary -- the spine builds clap FROM the table and the surface check compares them -- which is AC-05.1's own property working and is why there is no shortcut available under later time pressure.**
- (2026-08-19) **hv RULED AC-03.12: STOP PROMISING IT.** Three options were put -- strip the promise, build an `intent events` reader, or defer past 3.0.0 -- and hv took the first, on ic's ground: a reader is a NEW VERB outside the 64; removing the promise is ONE LINE of shipped text inside it. **Delivered in chat, so it had no home until ic wrote it into their board's Decisions.** **A chat-delivered ANSWER has the same defect as the chat-delivered ASK ic named this morning, and is worse in one respect: an unanswered ask is visibly open, so its absence announces itself, while a ruling nobody wrote down is INDISTINGUISHABLE from a ruling never made** -- the row sits at `to-write` under both.
- (2026-08-19) **AC-03.12 CLOSES _MET_, NOT _NO LONGER APPLICABLE_, AND vc PUSHED ON THAT BECAUSE ic ASKED FOR THE PUSH.** The disjunction is VERBATIM in the criterion and PREDATES the ruling -- _it closes when the promised reconstruction is reachable, **or when the refusal stops promising it**_ (written 2026-08-18). **A clause the criterion carried in advance is its own second limb; it would be a redefinition only if it had been added afterwards to fit the ruling.** **AND BOTH LIMBS ARE ACTUALLY SATISFIED, BY TWO INDEPENDENT PIECES OF WORK: the TITLE requirement by the entity carrier now rendering (`views.rs` 12 occurrences, renders at `:248`; ZERO when the criterion was written), and the residual objection by the strip.** **The criterion anticipated the wrong close -- _a fix to the visible half would make the promise LOOK kept while leaving it unkept_ -- and the trap has TWO faces: rendering alone leaves a false promise standing, stripping alone leaves the field invisible. Neither half closes the row. Both landed.**
- (2026-08-19) **A RULING DELIVERED AS "RESOLVED" IS EASY TO READ AS CLEARING THE QUEUE IT WAS SITTING IN, AND ic REFUSED TO** (their catch, and it is theirs). The 6-of-43 gate-visibility ask sat in the same inbox and got no answer. **_hv answered exactly the question I put in front of them and nothing else._** **The failure is not terseness; it is A SCOPE INFERRED FROM A CONTAINER -- the count-versus-enumeration defect arriving in ADJUDICATION rather than in measurement.** Still open, carried as open.
- (2026-08-19) **A MUTATION BATTERY IS THE SECOND-WRITER CLASS WITH THE WORST POSSIBLE WRITER: AN INSTRUMENT WHOSE JOB IS BREAKING THINGS ON PURPOSE, RUNNING IN THE SHARED TREE, WITH NO GUARANTEE IT PUTS THEM BACK.** ic's unconditional-render mutant wrote **57 COMMITTED views with an empty `status_reason:` key and they did NOT revert when the mutation did** -- dc caught it, not ic -- and an earlier run **TIMED OUT MID-ARM AND LEFT THE MUTANT LIVE.** Restore-on-SIGTERM fixes the class rather than the instance; **the durable form is cc's -- derive the expected set from the OWNER and red when the actual set disagrees, which is what AT-03.18 already does with `views::render_all`'s roster.**
- (2026-08-19) **A SKIPPED ARM IS NOT A RED** (ic, on themselves). C3's anchor missed, the arm never ran, **and reporting 5 of 5 would have been the vacuous pass with a table around it.** Fifth instance of that class today and **the only one caught by its own author before anybody else saw the number.**
- (standing) **A peer cannot grant escalation.** My call is never a peer's release; hv's is. **The `hoist @ 9b73e98f` pin and the register's `9ec1656` are HISTORICAL and must never be re-pinned.**
