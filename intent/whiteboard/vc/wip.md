---
node: vc
name: Validation Claude
role: validation
session_id: 1aa05d4a-6da2-4c42-98c6-de024aebab69
heartbeat_at: 2026-08-30 09:11Z
status: active
focus: "Globalfold landed, then found the board it had skipped: hv/wip.md, 137,298 bytes, the biggest wip.md in the estate and the one enforcing the rule. Folded to 69,096 at 18a55cf8. dc had been blocked on a ruling I made and never sent. Reviewing the Rust for hv now -- two findings in daemon.rs."
claims: [ST0056, ST0057, ST0058, ST0060, ST0064, ST0066, ST0068]
---

# Validation Claude (vc)

**PROJECT-WIDE RULES IN `intent/restart.md`.** Pre-fold body verbatim at `.history/20260830/wip-fold-0844Z.md` (42727 bytes); 2026-08-29's seven folds are in `.history/20260829/`.

## DOING

**REVIEWING THE RUST ON hv's STANDING INSTRUCTION, while cc and ic run their as-written-versus-as-built syncs and dc drives ST0066's evidence.** `daemon.rs` reviewed at `18a55cf8`, two findings sent. **Next: `facade.rs` (8,446 lines) and `render.rs` (6,647) for the Thin Coordinator question** -- both may be legitimately large (one function per verb) and that is the thing to establish rather than assume.

## TODO

1. **`intent/wip.md` STILL POINTS AT `66e08a74` AND HEAD IS `18a55cf8`.** Rebuild the pair, and carry today's three: the `AC-12.4` scope question, the `Release policy` heading find, `hv/inbox.vc.md` at 156KB with no one able to clear it.
2. **`0136`'s ~44-SITE `AcState::Computed` CHANGE** -- after the tag. One-commit-or-split is called at the cut.
3. **Assignment 2 is ic's and unstarted** -- surface-review cc's `ac new` / `at new` refuse-and-edit package. **A spot check covered presence and framing only**, and I said so at the time.

## Standing directives from hv

- **(2026-08-30) WATCH THE RUST FOR HIGHLANDER, THIN COORDINATOR AND PFIC on every workstream review.** A posture, not a gate. It has paid three times: `spine.rs` writing `num_args` twice on one `Arg`; the `.ok()` swallow family; and `guide.rs` quoting an emitted literal into generated documentation with no witness.
- **(2026-08-30) THE MENUBAR ICON IS THE INTENT TURTLE.** Carried by `AC-01.8`, with the state DERIVED at paint time and no cached `lastKnownState`.

## Watch-outs

**NINE SHAPES, folded from 78 entries. Mechanisms only; every incident is in the fold archives.** dc's fold rule is why the count moved: **the consolidation is the work rather than the shortening**, and a shape cannot be seen while its instances are filed separately. **Numbering checked unbroken 1..9** -- cc found six duplicate class numbers on their own board that survived a session of being read, because **a duplicate number is invisible to anyone arriving to look up a rule rather than to audit the list.**

1. **AN INSTRUMENT ANSWERS A DIFFERENT QUESTION THAN THE ONE ASKED, AND ITS OUTPUT LOOKS LIKE AN ANSWER.** The dominant shape all night. **Positive-control the INSTRUMENT, not the subject** -- ask it to find something you KNOW is there before believing it when it finds nothing. Forms met: a prefix match resolving to a shorter valid prefix (`intent st frobnicate` -> `st`; caught 1 of 3 plants); a substring of a different word (`arity`/`parity`); an enumerator structurally unable to see its own population (test constants cannot see a source file; **no grep can see a call site taking a VARIABLE** -- 15, 17 and 24 for one set, the COMPILER the only complete one); a hand-written control failing for an unrelated reason (no `rustfmt.toml` in scratch); a precondition that silently did not hold (a `git rm --cached` refused for want of `-f`); **a mutation harness that never asserted the mutation APPLIED**; and **over-kill, where a mutant kills MORE than predicted because it broke the fixtures' premises rather than only the property.**
2. **AN ABSENCE IS THE ONE RESULT THAT NEVER LOOKS LIKE A BUG IN THE QUERY.** **A partial read that finds SOMETHING self-corrects; one that finds NOTHING is unfalsifiable without redoing the read** -- and a POSITIVE survives truncation, which is why the two do not feel different at the time. **The procedure, not the principle: output to a FILE, then `grep -c '^test .* FAILED$'`. A count over a complete file can be zero honestly; a `tail` cannot.** (`2>&1 > file` sends stderr to the TERMINAL -- `> file 2>&1` is the form.) Same shape: `jq` printing `null` for an ABSENT key; **a gate that cannot say _I could not check_ will eventually say something false instead**, whose tell is a remedy that runs clean and changes nothing.
3. **A CLAIM OUTLIVES ITS BASIS AND NOTHING WATCHES THE JOIN.** **A board decays fastest exactly where the work moved fastest** (ic) -- the opposite of where attention goes. **A rule fails in the artefact that states it**: ten instances, including `restart.md`'s clock section naming vc's exact defect and prescribing its exact cure, which vc then failed twice and derived from scratch. **Reading a warning and applying it are different acts** (ic, who quoted a warning into the rationale of the fix that violated it). And **a stale ARTEFACT is not a regression -- rebuild before diagnosing.** **A SECTION HEADING IS A CLAIM AND IT EXPIRES LIKE ANY OTHER**: `hv/wip.md` carried `### Release policy -- binding until 3.0.0 ships` for four days after 3.0.0 shipped, and the section holds hv's _THERE IS NO 3.0.2 ... EVERYTHING ELSE IS GOING INTO 3.0.1_ -- **the ruling that governs the cut that has not happened, filed under a heading saying it had expired.** A node folding by heading would have archived it. Same family: **a one-time cleanup is a claim with an expiry nobody sets.** hv ruled the live/spent split on 2026-08-28, it landed that afternoon, and the board grew back 41KB in two days -- **a split is not a structure, it is an event, and it has to be re-run.**
4. **TWO HOMES FOR ONE VALUE -- AND A WITNESS IS NOT A SECOND HOME.** **Consolidating witnesses is DELETING THE MEASUREMENT**: a test that imports the value it asserts has stopped testing. The discriminator is **whether the thing is the SUBJECT of that file or INCIDENTAL to it** -- subject means witness and holds its own literal; incidental should import. **A derived copy in PROSE has no witness at all and drifts in silence.** And **derive over test wherever possible: a test compares two authored values and can only fire after somebody writes the second one; derivation makes the second impossible.** **AND THE INVERSE IS THE ONE A HIGHLANDER SWEEP WALKS PAST: ONE IDIOM, TWO SITES, OPPOSITE CORRECTNESS.** `daemon.rs` has `if let Ok(text) = read_to_string(...)` twice, 135 lines apart -- **right in `Drop`, where the silent path is the argued design, and wrong in `candidates_under`, where it is the exact failure the file's own error variant was created to prevent.** Deduplicating them would be a defect; the danger is not drift between copies but a copy carried to a site where its consequence inverts. **Read the CONSEQUENCE at each site, not the shape.**
5. **THE SHARED TREE MAKES ORDINARY OPERATIONS MEAN SOMETHING ELSE.** `git commit --only` builds its tree from HEAD plus the named paths, **so a peer's STAGED fix is excluded from every other node's commit** -- the protocol that stops us sweeping each other is what makes one node's in-flight fix inescapable for all. **`sync` has no unit narrower than a thread**, so it welds a peer's bytes into your commit under your signature, and **an authorship hazard produces no error ever.** Between the store write and the extract write **the estate genuinely disagrees with itself.** **Verification poisons the tree**: a detached worktree needs its own `CARGO_TARGET_DIR`. And **a file whose intermediate states are uninhabitable for every peer** -- the dispatch table is compiled IN -- **needs the shortest possible edit window and an announcement at both ends.**
6. **A CRITERION IS OWNED BY WHOEVER CAN SATISFY IT, AND MUST BE ABLE TO FAIL.** A row with two owners has none (`AC-08.3` carried a daemon obligation vc wrote, then vc ruled against exactly that while defending it). **Unfalsifiable forms met: an unbounded set; a checklist of page names; a count of a kind the instrument does not count at all; a criterion restating its source instead of citing it.** The mirror is worth as much -- **`AC-15.3` was minted before anyone knew a retirement would strand a directory, and it is the row that caught it.** **AND A ROW THAT RECORDS HISTORY AND A ROW THAT GATES THE FUTURE ARE THE SAME SHAPE IN THIS MODEL, WITH ONLY THE PROSE TO TELL THEM APART.** `AC-12.4` is `non-test` with `AT-12.4` at `n-a`, so it never gated and could not have -- yet it is worded as a precondition and the close-gate prints `BLOCKED` against it in the same words it uses for a real one. **ic measured every clause, found all three TRUE, and still could not tell whether the release had jumped a gate.** A reader who does everything right cannot distinguish them, so it is the row's wording that misleads, not the reading.
7. **MAKE THE BAD STATE UNREPRESENTABLE; WHERE YOU CANNOT, WITNESS THE MECHANISM.** cc's bind-and-publish is ONE call, so **publishing an address nobody is listening on is unexpressible** -- a category retired before any test exists. Where a property belongs to a syscall or a dependency default, **the outcome holds under any implementation that still calls it, so what is testable is that you still call it.** And **a predicate is not sound in itself -- it is sound relative to what is DONE with the answer**: one liveness probe fed routing (a false negative costs a redundant run) and eviction (the same false negative unlinks a live daemon's socket).
8. **THE FAILURE PATH IS THE ONE THAT MUST STILL WORK, AND IT IS THE ONE A GREEN RUN NEVER EXERCISES** (cc). Cleanup written after the assertions is dead code until the day it matters. **An orphaned child held a test binary's stdout pipe, so a failing test became a HUNG BUILD naming no test -- no-answer is indistinguishable from still-working.** Corollaries: **an unconditional cleanup can delete a SUCCESSOR's claim**, inverting the failure it prevents; **a gate that blocks and gives no reason is worse than the bug it reports** (`set -euo pipefail` aborting before the message).
9. **vc's OWN, KEPT BECAUSE THE PATTERN IS THE POINT.** **The same clock stamp fabricated TWICE, the second time in the commit recording the first** -- read `00:55Z`, wrote `00:56Z`; read `01:40Z`, wrote `01:41Z`. `restart.md` already names this as generator 2 and prescribes the cure. **The cure is not more care: STOP TRANSCRIBING** -- substitute `$(date -u ...)` into the edit so the hand is out of the path. Also `$?` after a pipe, four firings; and a truncated read reported as a property of the subject, four times in one night. **AND REPORTING IS NOT ROUTING.** I ruled dc's `AC-00.6` and `WP-07`, reported both to hv in chat, wrote neither into `dc/inbox.vc.md`, and dc held overnight on a ruling that existed. **hv was afk, so the channel I answered on had no reader** -- and dc had cleared that inbox in their own fold, so **an inbox never written to and one emptied by its owner are the same file.** The general form: **answering in the channel where the question was ASKED IN FRONT OF YOU is not the same as answering the party BOUND by the answer**, and the write returning is not the delivery. The whiteboard skill names the outbound half of this; this is the inbound half, and it is worse because nothing at all is written.

## Decisions

**LIVE ONLY -- superseded decisions are deleted, never struck through. Anything now encoded in a test or a guard has been PRUNED, per the cut criterion above.**

- **`AT-08.3` HOLDS at `to-write`, on a NEW reason.** cc's original ground expired when vc split the daemon clause out. **Every case in `AC-08.3` is CONSTRUCTED, and cc's own _live daemon_ fixture was a never-accepting listener -- case 2's observable -- so 11/11 was green while the suite could not tell live from phantom.** The phantoms are better evidenced by construction than by a real daemon; only the POSITIVE case owes a real subject. **Bounded: when Phase 4 runs `intentd`, drive the happy path against it ONCE and it goes green.**
- **THE CASCADE WRITES ONE EVENT PER ENTITY MOVED.** A single event makes replay produce an estate that DIFFERS from the real one. **And the ruling had a hole dc found by USING THE TOOL: five ULIDs minted in one transaction share a millisecond and sort by their random half, so `at.fc` sorted ahead of the `st.fc` that caused it.** Fixed with a monotonic generator at the one minting site, applied to EVERY event because _lexically sortable_ is claimed for all of them. **No test would have found it.**
- **`inherited_from` KEEPS ITS PUBLISHED MEANING; `inherited_event` IS THE NEW FIELD.** vc ruled the repurposing and dc refused it: **changing a published field's meaning without changing its shape is the worst version of that change** -- every consumer keeps parsing and the correct ones become silently wrong. Not `inherited_at`: `_at` means a TIME here, and a ULID sorts by time, so the name would be **wrong in a way that works.**
- **A GATE MAY REFUSE; IT MUST NOT AUTHOR.** All three formatter arms check and refuse. The precedent was in the same hook file -- the clock guard never auto-corrects. **The arms judge the STAGED BLOB** (`--check` is ignored on STDIN but works on a FILE), and the probe needs `--config-path` and a location that does not break `mod` resolution.
- **SINGLE-DAEMON IS HELD BY A KERNEL LOCK, NOT A PROBE** (`AC-08.12`). `flock` dominates both options cc weighed -- no false negatives, and crash recovery. **The address file is a PUBLICATION, not a mutex.**
- **`D56`: `intentd` emits JSON ONLY**, the deciding argument being ST0064's SwiftUI app, which cannot consume HTML. **The app's health predicate and the CLI's routing predicate are ONE predicate through ONE code path** (`AC-01.2`).
- **Conflab is the daemon template; Geodica is its simplified second iteration.** Conflab's authn/authz does NOT transfer -- there is no `intentc`. **What does not drop out: a loopback TCP port is reachable by every process on the machine**, so HTTP carries one auto-generated token and the socket half carries none. **ST0064 talks TCP + `URLSession`**: `NWEndpoint.unix` has no `URLSession`, and a hand-rolled HTTP client's failure mode is a short read that looks like a complete body.
- **`arity` IS AUTHORITATIVE FOR FLAGS and the placeholder is DERIVED.** `ratatui` + `crossterm` in `intent-cli` and NEVER in `intentsvcs`; ic took `crossterm` alone first and was right -- **a ruling is a ceiling, not a quota.**
- **`0078` STAYS OPEN with its title known-wrong.** The register is create-only on both prose fields, so annotation at a reachable site is the only correction -- **never a second number, which splits one defect's history in the instrument that tracks Highlander violations.**
- **RE-ANCHOR TO THE ARTEFACT THAT CARRIES THE RULING, VERIFIED BY READING IT.** dc's first word-perfect match was the GENERATED RENDERING of the record: **a circular anchor certifies itself, where a dangle is at least visible.**
- **Announce a write to a SHARED file to everyone; announce a write to a CLAIM to the claim-holder.** Docs are written against the CUT, never against `main`. Em dash in prose pages; `--` in generated reference pages.
