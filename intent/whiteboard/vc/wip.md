---
node: vc
name: Validation Claude
role: validation
session_id: 1aa05d4a-6da2-4c42-98c6-de024aebab69
heartbeat_at: 2026-08-31 12:13Z
status: active
focus: "FOLDED 2026-08-31 12:13Z, pre-fold at .history/20260831/wip-fold-1213Z.md (sha-verified). ST0056 107/135. MY FOLD BURIED FOUR LIVE hv RULINGS and two shipped the option hv DECLINED; sweep now complete for this tree. hv put the MENUBAR APP INTO 3.0.1 and it is an M PORT, not a build. Three unmeasured relays by me today, all three caught by peers."
claims: [ST0056, ST0057, ST0060, ST0064, ST0068]
---

# Validation Claude (vc)

**PROJECT-WIDE RULES IN `intent/restart.md`.** Pre-fold bodies in `.history/`.

## DOING

### THE CUT -- RE-RUN THE VERBS, THESE ROT

**ST0056 107/135** (25 descoped, 2 withdrawn), from 97 at the 07:31Z fold. **ST0057 66/66 PASS. ST0066 6/6 PASS. ST0058 4/6. ST0068 4/9. ST0064 0/9 and now IN 3.0.1.** Run `intent ac gate <ST>`; do not read these.

## TODO

1. **`AT-00.3`** -- `AC-00.3` names five capabilities and GraphQL now ships. **The artefact belongs in `parity/tools/` as a shell instrument** -- four of five are daemon-side and the fifth is a claim about the CLI, so no single-crate test witnesses all five. **Do NOT re-drive WP-08's behaviours; the row's claim is that they SHIP.**
2. **`AT-00.20`'s SUCCESSOR: THE UNION PER PROPERTY.** Today's census computes a FILE-level union and says so. `AC-00.16` is scoped _for any property the estate claims_, and **the register already holds the AC/AT pairing that would key it.**
3. **WP-15's `AC-15.1` / `AC-15.3`**; **ST0068** 4/9; **`AC-09.6`** closes on ic's 21-row flip once dc's `dispatch-table.json` lands.
4. **`parity/register.md`'s 39 stale rows** -- GENERATED, so the generator's template is the fix.
5. **`0136`'s ~44-site `AcState::Computed` change** -- after the tag. **`intent/wip.md`** -- check before the tag; hv reads it on restart.
6. **`design.md:88` still says `rmcp (official SDK): stdio ... now`**, which my 2026-08-31 ruling reversed. Design prose is hv's hand.
7. **laksa-vc's `ST0090` AT-00.2 `model-inconsistent`** -- awaiting their verbatim row note and doctor line, then rule which of check or repair is wrong.

## hv items

**ANSWERED TODAY, ALL TO RECOMMENDATION.** Four verbs ship declared-unbuilt with `--help` narrowed; `INV-03` ratified; Machine 5's table deferred; `schema` only; **Laksa keeps its ritual** (their D3 is their own `.gitignore`, not our defect); **`at.recast` -- NO undo verb**, the one-way move documented instead; **the issue write-path package BROUGHT FORWARD** to cc, now; **the judgement-half ruling sweep DONE before the tag.**

**hv REVERSED THEMSELVES ON ST0064 AND IT IS THE LIVE SCOPE EVENT.** _"The intentd and the menubar app that controls it are linked and need to both land for 3.0.1... We need both."_ The `D3` SPLIT survives (WP-08 owns the daemon, ST0064 owns the app, ic builds); only the out-of-3.0.1 timing reversed. **`A8` reversed too, on a separate question: the macOS signing path is IN.** **It is an M PORT, not a build** -- Gtools ~2,470 LOC is the size AND shape reference; Conflab's 41k is mostly vendored and is the right reference for the build verb ALONE.

**OPEN.** dc's irreversible `bin/` prune, held with hv, now a third smaller because `fileindex` retired. **The URI-resolver work is release-blocking for TWO threads** (ic's finding): `AC-01.5` makes the app a CLIENT of the one resolver, so an address resolving in the CLI and not the app is that row's failure by definition. **`AC-09.5`'s `wip`/boards half** -- it needs a CLI read for `wip` and the boards, which is a product question about the CLI, not about MCP.

## Standing directives from hv

- **WATCH THE RUST FOR HIGHLANDER, THIN COORDINATOR AND PFIC on every workstream review.** A posture, not a gate.
- **THE MENUBAR ICON IS THE INTENT TURTLE.** Carried by ST0064 `AC-01.8`, state DERIVED at paint time.
- **THE AIM IS TO FULLY SHIP v3 WITH ALL FUNCTIONALITY. intentd is a priority. Then tree-sitter and full search. Push.**
- **DO NOT REINVENT THE WHEEL.** Where a sibling estate has solved it (`../Gtools`, `../Conflab`), port it. **And read the thread's own attachments first** -- ST0064's design handoff had been attached all along and neither vc nor ic had opened it.

## Watch-outs

- **THE DELIVERED `intent` IS A SYMLINK INTO A BUILD OUTPUT DIRECTORY**, and it goes STALE. Measured twice: the delivered pair could not see 14 commits; and `daemon status --format json` answered rc=0 from the delivered binary while the SOURCE refuses it by name.
- **A NOTE THAT NAMES ITS OWN EXPIRY IS A LIVE INSTRUCTION TO MOVE THE ROW.** Write the CURRENT reason; record a dead one as dead.
- **THE ROSTER AND THE TREE ARE TWO HOMES FOR ONE FACT** (dc), so either can outlive the other and the guard is the only thing that notices. Both signs met in one day.
- **DERIVED CENSUSES MULTIPLY FREELY; AUTHORITATIVE COPIES DO NOT.** Highlander forbids two HOMES for one fact -- two places that can disagree about what is true. cc's four population lists over one table are four CHECKS over one home, and only the per-pair census caught a reclassification that moved two counts in OPPOSITE directions while every total balanced. **A single total is a lossy projection; the cure for a lossy projection is another projection, not another source of truth.**

**EIGHT SHAPES. Mechanisms only; incidents are in the fold archives.**

1. **AN INSTRUMENT ANSWERS A DIFFERENT QUESTION THAN THE ONE ASKED, AND ITS OUTPUT LOOKS LIKE AN ANSWER.**

   **1a. THE SECOND LOOK IS NOT INDEPENDENT OF THE FIRST.** The proxy nearest to hand stands in for the subject: a `.canon` EXTRACT for the STORE, a WP `title` for its criteria, a listing that omits the deciding field. **The cure is a question asked BEFORE the measurement -- is this the thing, or a rendering of the thing? -- and when it is a rendering, RUN THE VERB.** **CONVERGENCE IS NOT CORROBORATION WHEN BOTH METHODS SHARE A DEFINITION** -- and measured in its purest form today: disk and HEAD AGREED that my edit was gone, both truthfully, and neither was the STORE, which is the SSOT and the only place the answer lived. **Two derived views agreeing is not evidence about the source, and it took two agreeing measurements to nearly produce a false accusation of a peer.** **A PROBE THAT CANNOT EXHIBIT THE FAILURE IT CHECKS FOR RETURNS THE NUMBER THAT MEANS SUCCESS.** **POSITIVE-CONTROL THE INSTRUMENT and write the prediction to a file BEFORE the run.**

   **1b. A NEEDLE THAT AGREES WITH THE NUMBER YOU ARE CHECKING IS THE MOST DANGEROUS KIND** (cc). **AND A CONTROL CAN BE UNABLE TO FAIL: cc reached for `st list` as a _shipping unclaimed unit_ and it is itself claimed, so the branch could never fire on it** -- caught by reading the output rather than `$?`, since the tool exits 0 either way.

   **1c. A ZERO WITH NO PLANTED POSITIVE BEHIND IT IS A SILENCE, NOT A MEASUREMENT** -- and the sharpest form is **A COUNTER THAT BECOMES ABLE TO FIRE ONLY WHEN SOMEONE FINISHES THE WORK, AND SAYS PASS AT THAT INSTANT.** `fleet_corpus_conservation.sh` reddened only on UNRUN or LOST, so **completing the corpus would have flipped it to PASS on an estate whose own recorded text says it fails.** Three unrun members were the only thing holding it red. **Driving an instrument at its CURRENT state cannot see this; you have to drive it against the future it is demanding.** I wrote it that morning and never asked what it would say at four members.

   **1d. BOTH NAIVE SPELLINGS OF "COVERED" ARE UNSOUND, AND THE ERROR MUST BE DIRECTED.** `named` is not coverage -- an exemption list names what it EXCLUDES. `not named` is not non-coverage -- **an instrument that walks a tree names no file in it**, which produced four confident false positives against shipped skill scripts. **A MENTION IS NOT AN INSTANCE: three instruments, three authors, one predicate -- `contains` over a corpus nobody partitioned** -- and cc's third was inside the instrument built to prevent it, whose own corpus states the exception beside the rule. **For an instrument whose output is a defect list, over-approximate so every error lands in false NEGATIVES: what it prints is real, what it prints is not everything.**

   **1e. A DEFECT CAN HAVE A GUARD ASSERTING IT** (cc). `dispatch_ssot.rs`'s arm had the RIGHT NAME and a predicate that could not see the defect, **so every review looking for coverage of it found coverage of it.** Worse than no test: it consumes the search that would have found the hole. **When fixing a defect, grep for the test holding it in place before reading a red as your own breakage.**

   **1f. A RECORDED PROOF IS PROSE UNTIL SOMEONE TYPES IT** (dc). A mutation proof in a comment stopped reproducing when its subject gained flags, **and its correct steady state is SILENCE, so nothing reported the rot.** Generalises: **every _we verified X by doing Y_ comment in this estate is an unrun test with a passing appearance.** A proof driven the day it was written and re-driven by the arm is a test; one recorded in prose is a claim.

2. **AN ABSENCE IS THE ONE RESULT THAT NEVER LOOKS LIKE A BUG IN THE QUERY.** A partial read that finds SOMETHING self-corrects; one that finds NOTHING is unfalsifiable without redoing the read. **Output to a FILE, then count.** **`cargo test -p X` does not build a sibling package's binaries** -- met from both sides in one afternoon: ic's intent-cli change broke a cross-crate guard `-p intent-cli` cannot see, and cc **verified the crate they changed CODE in, not the crate whose TESTS READ THE TABLE they changed.** A change's blast radius is not the crate it lives in.

3. **A CLAIM OUTLIVES ITS BASIS AND NOTHING WATCHES THE JOIN.** A board decays fastest where the work moved fastest. **A rule fails in the artefact that states it.** **AND THE ARROW REVERSES: the basis can POSTDATE the subject.** **WHERE A STRUCTURED FIELD EXISTS FOR A CLAIM, PROSE STATING IT IS A SECOND HOME -- PROVIDED THE FIELD CARRIES THAT CLAIM.**

   **3a. A FINDING PARKED IN A FIELD ABOUT TWO OTHER FIELDS DISAGREEING DIES WHEN THEY AGREE.** Rehome before deleting.

   **3b. A GREEN ROW WHOSE TEXT OVERCLAIMS IS WORSE THAN A RED**, because a green CLOSES the criterion and nothing re-examines a closed one. **AND THE REPAIR SPLITS ON WHY: rewording to match an UNFINISHED build is the defect; rewording because a RULING moved and the criterion is a stale second home is the correct repair.** `AC-09.1` named `schemars` while `mcp.rs` derives from the TABLE under D37 and says why; `AC-09.5` named three read surfaces of which two have no CLI read to match. **A clause with no subject is not a test that can fail.** **Cite the ruling, never restate it, or the row is the stale copy again next time.**

   **3c. A RATIFICATION WHOSE SUBJECT WAS LATER RETIRED IS SUPERSEDED, NOT UNAPPLIED.** Deleting it from the machine-read clause would make the record claim hv ratified something they did not. **A ratification is a historical fact; a `covers:` clause is a LIVE binding to units that still exist. They must not be spelled the same way.**

4. **THE SHARED TREE MAKES ORDINARY OPERATIONS MEAN SOMETHING ELSE. FOUR MEMBERS, ONLY ONE CLOSED.**

   **(i) CONTENTION -- CLOSED, BY cc's TWO-PART PROCEDURE AND NOT BY MY FILE-SPECIFIC RULE.** `git commit --only <explicit paths>`; **`--only` is PATH-scoped, not hunk-scoped.** **THE MECHANISM IS `git read-tree HEAD`, WHICH SNAPSHOTS THE ENTIRE TREE at pin time**, so committing that index after HEAD moved reverts every path any peer touched in the window **regardless of what you `git add`** -- the tell is that the revert set EQUALS the victim's commit set, path for path. **My _regenerate the extract before committing canon_ closed a different failure and pointed three nodes at the wrong file.** Cure: **re-pin at commit time until HEAD is unmoved across stage-and-commit, and post-verify against `git show --name-only <commit>` EQUALLING your intended path set** -- never against your own staged list, which cannot see a path you did not name. **A HEAD-pinned private index does not close contention, it INVERTS it**: instead of taking a peer's bytes it silently discards them, and the losing node gets no signal.

   **(ii) COHERENCE -- NOT CLOSEABLE BY THE COMMITTER.** Land the pair, or wait.

   **(iii) REVERSION -- FAILS SILENTLY.** Announce any disk->store sync; prefer `intent st attach <ST> <rel> --from <file>`.

   **(iv) A GUARD WHOSE POPULATION IS THE WORKTREE OR THE INDEX MAKES EVERY NODE'S COMMIT DEPEND ON EVERY OTHER NODE'S UNCOMMITTED STATE** (`0174`). **AND THE MIRROR: EVERY INSTRUMENT READS THE WORKTREE, so a defect existing only in the commit has no witness but a clone nobody has made** (`0178`). **SOME OF OUR TOOLS READ THE INDEX AND SOME READ THE WORKTREE, AND NOTHING TELLS YOU WHICH YOU ARE HOLDING** (dc's W17) -- `corrected_check.sh` reads the STAGED copy, correctly for a commit gate, and I re-drove a fix twice against an unstaged file getting the identical failure. **A private-index commit that CREATES files leaves the ambient index reading them as staged DELETIONS until the author restores them, and that window is observable to peers and invisible to the author** -- I raised an alarm inside one. **The author is the only node for whom it is not a surprise, and not the one who pays.**

   **THE CANON PIPELINE CARRIES THE HAZARD ONE LAYER ABOVE GIT. AN ATTACHMENT IS AUTHORED AND NO SYNC DIRECTION REWRITES IT** -- so `sync --to-disk` does NOT materialise one, and grepping the file on disk after a sync reports your own store-write as missing. **The sequence is `intent st attach`, then `sync --to-disk`, then commit file AND extract together.** **The guard's own remedy says _sync canon FIRST, it reads the WORKTREE_, and for attachments that is FALSE** (`0184`). **AND THE STORE IS SHARED BEFORE THE TREE IS** (ic): a peer's `st attach` puts their bytes in the store, so the next node to sync realises them. **ic's cure is the estate's: splice the peer's leaf back to HEAD in your extract, restore the file, verify canon-sha == disk-hash for every attachment you DO carry.**

5. **A CRITERION IS OWNED BY WHOEVER CAN SATISFY IT, AND MUST BE ABLE TO FAIL.** **Unfalsifiable forms met: an unbounded set; a checklist of page names; a count of a kind the instrument does not count; a criterion restating its source; a bar satisfied by ONE MEMBER OF A GROWING SET.** **A partition DERIVED from its parts closes by construction; only one MEASURED against a population is worth asserting.** **A FLEET CRITERION SCORED OVER THE ONE MEMBER SOMEBODY HAPPENED TO RUN IS `AC-00.11`'s WRONG-M WITH ESTATES AS THE POPULATION.**

   **5a. A TOKEN THAT SPANS THE ACCEPTABLE AND THE UNACCEPTABLE CASE CANNOT DISCRIMINATE, AND A CRITERION SATISFIED BY VOCABULARY IS NOT SATISFIED.** My `named` conflated _the CHECK found the residue_ with _the MIGRATION named it_, and only the second satisfies `AC-10.5`. `UNACCOUNTED` spanned five classes -- modelled-but-unbuilt, declared-out-of-model, generated projection, config, placeholder -- of which only one is a gap anyone should act on. **When a genuinely different case arrives, mint a token; never reuse one that already spans.**

   **5b. A MEMBER THE PROPERTY CANNOT BE EVALUATED OVER IS EXCLUDED WITH ITS REASON NAMED AND THE DENOMINATOR MOVING VISIBLY.** Conservation is a property OF a migration; with none it is UNDEFINED, not false. **A silent drop to a smaller corpus is the denominator attack; a declared exclusion carrying its reason is the cure, and the difference is entirely whether the reader is told** -- so an exclusion with no reason is itself a failure. **And two exclusions with different reasons must not be recorded alike**: structurally-out-at-its-pin versus in-population-and-remediable, or a temporary block becomes permanent.

   **5c. THE AUTHOR OF A CHECK IS NOT A SAFE SOURCE FOR THAT CHECK'S OWN DENOMINATOR.** cc was one step from emitting 110 files as out-of-model that an hv ruling had moved INTO the model -- **while quoting the warning against exactly that** -- and was stopped only by pinning the set to the authorising document. **Pin a denominator to its authorising sentence, with a test that reds when the document stops carrying the phrase**; a citation without that test is a copy, and a copy of a ruling goes stale the day the ruling moves.

6. **MAKE THE BAD STATE UNREPRESENTABLE; WHERE YOU CANNOT, WITNESS THE MECHANISM.** **A predicate is sound only relative to what is DONE with the answer.** **AN EXEMPTION LIVING IN A CONTROL-FLOW BRANCH IS THE PROSE VERSION WITH WORSE VISIBILITY.** **A ONE-SIDED WITNESS PASSES UNDER THE VERY THING IT REPLACED. Two-sided, or it proves nothing.** **A SELF-EXPIRING NOTE IS BETTER THAN A REMEMBERED ONE.** **AND WIREDNESS MUST BE DERIVED OR DRIVEN, NEVER A HAND-MAINTAINED FLAG** -- but **nothing in a binary can enumerate its own match arms**, so the honest form is a driven census over every declared path, with a call SITE as the declaration (cc).

7. **THE FAILURE PATH IS THE ONE THAT MUST STILL WORK, AND A GREEN RUN NEVER EXERCISES IT.** **THE UNIVERSAL SAFE PROBE CAN HAVE A SIDE EFFECT.** **A TEST HARNESS THAT DOES NOT ISOLATE `HOME` REACHES THE REAL MACHINE.** **AND WHERE A PIPELINE CANNOT BE DRIVEN AT ALL -- signing needs credentials no agent holds -- THE ARMS YOU CAN DRIVE ARE THE REFUSAL PATHS.** A signing pipeline whose failure mode nobody has seen is the shape we keep paying for.

8. **vc's OWN.**

   **8a. SHELL QUOTING EATS CONTENT AND THE COMMAND STILL SUCCEEDS.** Five mechanisms: no word-splitting under zsh; an unmatched glob aborting the whole call; an apostrophe inside a single-quoted program being a PARSE error that runs nothing; `$?` after a pipe reading the last stage; and a BACKTICK inside a DOUBLE-quoted string running a command. **The dangerous case is the word that IS a command.** Write messages to a file and use `-F` with a quoted heredoc. **And the zsh no-split trap has a NEW form: it can land NOTHING** -- ic's `git add $P` matched no pathspec and committed a peer's paths instead, an empty action that looked completed, caught only by post-verify.

   **8b. I RULE AGAINST STATES THAT HAVE ALREADY MOVED.** A message states HEAD at SEND time and is read against HEAD at READ time. **RE-MEASURE BEFORE RULING, EVERY TIME.** **When a ruling's premise dies, SAY SO AND REVERSE IT.**

   **8c. A ROW MOVE CAN BE IRREVERSIBLE, AND hv HAS NOW RULED THERE IS NO UNDO VERB.** `to-write` is the exemption `absent_at_check` grants; `red` requires the cited artefact to EXIST; **no verb sets a row back.** The one-way move stands and is DOCUMENTED instead -- **and the documentation owes the operator a ROUTE, not just a warning** (dc). The route is the proven one: **the repair is the ARTEFACT the row was owed, never a label.** **AND A RED ROW CITES A SHELL INSTRUMENT ON PURPOSE: a red Rust test breaks `cargo test` for everyone and a red shell tool does not.**

   **8d. REPORTING IS NOT ROUTING**, and I offered to run a verb a peer's session had been denied. **AND I MEASURE AGAINST STALE BINARIES.**

   **8e. A RULING IN TRANSIT IS A RULING NOBODY HAS, AND I COMMITTED BOTH FORMS IN ONE DAY.** **The fold form:** hv's rule keys on DONE and my fold keyed on DATED, so archiving the dated ruling record removed TODO items -- an unexecuted ruling is live state, and **four were buried, two of which shipped the option hv DECLINED.** **The message form:** I conveyed hv re-sequencing a package to one node and recorded it nowhere; cc grepped the board before starting, found the old sequence, and said so. **A live directive reachable only by grepping `.history/`, or living only in a peer message, is discoverable by nobody.** Cure: **execution status is the discriminator, never the date**, and an entry leaves the live record by BEING EXECUTED. **AND THE OPERATOR-SIDE CURE IS dc's, WHICH IS THE HALF A NODE CAN ACT ON: BEFORE PUTTING A QUESTION TO hv, CHECK WHETHER hv HAS ALREADY ANSWERED IT.** Three findings today had that shape and **none needed new measurement, only RE-measurement** -- `fileindex` ruled five days earlier, `no_absolute_home_paths` landed the night before while dc's board still called it owed, and two rc=2 contradictions already fixed under the binary being driven. **AND A BURIED RULING DOES NOT MERELY GO UNEXECUTED -- it gets independently rediscovered and re-costed by the node that buried it.** I filed `0179` proposing a remedy four days after hv had ruled that exact package built.

   **8f. I RELAY CLAIMS I HAVE NOT MEASURED, AND THREE PEERS CAUGHT ME IN ONE SESSION.** cc's file-ownership claim (passed to ic as fact; `git log --diff-filter=A` is a two-second instrument and I did not run it, and the file was cc's and already fixed). ic's L/XL sizing (passed to hv unchallenged; hv had built the reference app in a few hours). And an urgent index alarm measured inside a window that had closed before it landed. **Reporting a peer's finding across a boundary is right; repeating it as FACT is the defect.** **Three times today a node declined to act on a relay from me and was right to** -- and each one measured instead of complying, which is the protocol working and is worth more than the findings.

## Decisions

- **(2026-08-31) AN UNEXECUTED RULING IS LIVE STATE, NOT HISTORY -- AND THE DEFECT IS MINE.** Four buried across four passes: `fileindex` (shipping the option hv DECLINED), ic's `## Holds`, the issue write-path package, and ST0064's narrowing (also the declined option). `hv/wip.md` now carries `## Rulings made and NOT yet executed`; entries leave by being executed, never by ageing. **The sweep is COMPLETE for every ruling whose subject lives in this tree, and its reach is stated in it** -- Conflab's and Laksa's are not driven and are not cleared.
- **(2026-08-31) THE SWEEP'S VALUE WAS NOT CATCHING A NODE WHO HAD NOT DONE THE WORK -- IT WAS CATCHING A DECISION hv WANTED BACK.** Putting `D3` to hv produced a CORRECTION rather than an execution: the menubar app is in 3.0.1.
- **(2026-08-31) BOTH NAIVE SPELLINGS OF "COVERED" ARE UNSOUND** -- see 1d. Caught by opening the instrument the finding ACCUSED rather than trusting the finding.
- **(2026-08-31) A REWORD IS THE 3b DEFECT WHEN THE BUILD IS UNFINISHED AND THE CORRECT REPAIR WHEN A RULING MOVED.** Applied to `AC-09.1` and `AC-09.5`.
- **(2026-08-31) `named` DOES NOT PASS: THE NAMER IS THE MIGRATION, NOT THE CHECK** -- otherwise the second disjunct is satisfied by RUNNING the check, and the criterion met by measuring rather than by conserving.
- **(2026-08-31) A REFUSED CORPUS MEMBER IS EXCLUDED WITH ITS REASON NAMED AND THE DENOMINATOR MOVING VISIBLY**, and the two refusal classes are recorded differently -- see 5b.
- **(2026-08-31) ST0064's GRAIN: ONE WP, NOT SEVEN.** **The criteria carry no WP field -- they bind to WP-01 by their `AC-01.x` id prefix**, so cancelling WP-01 would have orphaned the thread's whole acceptance boundary, and six sibling WPs own no criteria and are invisible to the gate. **The dependency structure lives at the CRITERION level, where it already is and where the gate reads it.**
- **(2026-08-31, cc) A DEFECT CAN HAVE A GUARD ASSERTING IT** -- see 1e. **(dc) A RECORDED PROOF IS PROSE UNTIL SOMEONE TYPES IT** -- see 1f. **(cc) THE AUTHOR OF A CHECK IS NOT A SAFE SOURCE FOR ITS OWN DENOMINATOR** -- see 5c.
- **(2026-08-31) DERIVED CENSUSES MULTIPLY FREELY; AUTHORITATIVE COPIES DO NOT** -- in the watch-outs above, so today's Highlander findings are not read as an argument for fewer checks.

**Earlier decisions folded 2026-08-30 21:33Z, 2026-08-31 09:40Z and 2026-08-31 12:13Z. Bodies in `.history/`.**

- **THE MCP RULESET, ONE ENTRY BECAUSE THE CLAUSES ONLY MAKE SENSE TOGETHER.** **(a) MCP tools call the FACADE, never the CLI dispatch arm.** **(b) A PARENT ROW IS A NAMESPACE, NOT A VERB.** **(c) THE TEST FOR EXPOSURE IS NEED, NOT PROVENANCE.** **(d) NO EXEMPTION CLASS** -- the candidate population measured EMPTY, and **a vocabulary reserved for a population that turns out empty is STRUCK.** **(e) `exposed_on_mcp` REFUSES ON ABSENCE.** **(f) THE TIER SERVES IN-PROCESS ONLY IN 3.0.0.** **(g) DIRECT FACADE CALL NOW, `dispatch(op)` AS THE 3.x DESTINATION.** **(h) A TOOL THAT CAN ONLY MUTATE INVERTS THE SAFETY GRADIENT.** **(i) SERVED IMPLIES THERE IS A CLI READ TO AGREE WITH** -- the same principle one surface over, arriving on resources.
- **A CRITERION THAT COULD FORCE SCOPE IS WRITTEN AS AGREEMENT, NOT COVERAGE.**
- **A DELETE HAS THREE POPULATIONS AND EVERYONE ASKS ONLY THE FIRST.** (1) what EXECUTES this. (2) what CITES this as evidence -- **and its population is the STORE, not the tree.** (3) what CHECKS this. **Met again on ST0064: the second population was nine criteria bound by an id prefix, and I nearly ruled without asking.**
- **AN IDENTIFIER IS ONLY UNIQUE WITHIN ITS SCOPE, AND EVERY BOARD WRITES IT BARE.**
- **A SECOND HOME IS NEVER ACCEPTABLE AT A TAG, AND THE TAG IS THE REASON RATHER THAN THE EXCUSE.** **The escape is not _accept two homes_, it is _need less machinery_.**
- **A CITATION'S AUTHORITY COMES FROM ITS MEMBERSHIP RULE, NEVER FROM ITS NAME.**
- **THE CURE FOR THE PLAUSIBLE-LOOKING-HOME CLASS IS A DECLARATION THE BUILD RE-ESTABLISHES** (ic).
- **A TEST GOING RED BECAUSE A FIX LANDED IS THE NOTIFICATION WORKING.**
- **THE DAEMON'S PUBLISHED PORT SERVES BOTH PROTOCOLS, DISAMBIGUATED AT BYTE 0.**

## Coordination -- 2026-08-31 12:13Z

**NOTHING IS OWED BY ME TO ANY NODE AND NOTHING OF MINE IS UNCOMMITTED.**

**cc** -- write-path package (hv brought it forward), then the daemon-predicate widening: **`daemon::route` has TWO variants and ST0064's `AC-01.6` needs THREE**, with `AC-01.2` forbidding ic from inventing the third in Swift. Executed `fileindex` verified at SOURCE rather than from my relay. **Their masking finding is the best of the day.**

**ic** -- ST0064 is theirs, narrowed and WIP, grain ruled to ONE WP. Paused deliberately rather than port Swift against a structure I might change. **WP-09 done bar `AC-09.6`.** Measured their own sizing down from L/XL to an M port, and found the one genuine build in it: **Geodica's handler is a URL-to-verb MAP, not a resolver client -- a structural match that fails on the property the criterion is about.**

**dc** -- ST0068's nine rows; `at.recast` closed by hv's ruling. Found the fold defect by checking whether hv had already answered before asking again. **Took every ruling and corrected me twice.**

**laksa-vc** -- answered in full. Their D3 is their own `.gitignore`; D1/D2/D4 are already filed here (`0111`, `0082`+`0083`, `0145`); **D5 is genuinely new and is `0185`.** hv ruled they keep their ritual.
