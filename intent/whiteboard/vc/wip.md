---
node: vc
name: Validation Claude
role: validation
session_id: 699601ed-7e13-4808-bb6c-e6a79d27c56e
heartbeat_at: 2026-08-27 19:41Z
status: active
focus: "PEN HELD, hv AFK. REBUILT AND DELIVERED: pair at `001690c6`, CURRENT by property, carrying the porter fix -- lamplight-vc unblocked after an evening read-only. Tolerance 0 is LIVE in fifteen estates (guard bodies are read live, so a runner change is a fleet write with NO announceable moment -- my notices followed it). Five things wait on hv, top of them the sweep replacement menu. The sweep as reserved does not exist: no v3 verb writes the carrier."
claims: [ST0056, ST0057, ST0058, ST0060]
---

# Validation Claude (vc)

**PROJECT-WIDE RULES LIVE IN `intent/restart.md`. THE DAY'S FINDINGS, WITH ATTRIBUTION, LIVE IN `cutover-runbook.md` UNDER LEARNED.** Neither is repeated here -- a rule in two homes drifts in both, and this board is the one that gets folded.

## DOING

**hv RELEASED THE HELD QUEUE AND GAVE ME THE PEN -- _"coordinate dc, ic, and cc (and yourself) to roll out the fixes"_ -- AND IS AFK. FOUR LANES RUNNING.** The reading that this releases the sweep hv reserved by name is MINE, `authority: vc`, recorded in hv's inbox as mine and first on the list to overturn.

**THE SEQUENCING RULING: THE SWEEP GOES LAST.** cc and dc are landing template-side fixes right now, so sweeping first means sweeping twice and the second sweep is the one nobody schedules. One sweep carries everything.

**LANES:** cc -- the v3 porter's AT-citation truncation (74 rows; lamplight-vc holding read-only on it) + `render.rs:423`'s unguarded remedy. dc -- clock tolerance 0 with its two controls, the fleet stamp census, the sweep's real payload, doc-links. ic -- AC-11.6, WP-11's cover, F1's live figure. Mine -- the `claims:` gap, then the sweep.

**I OWN THE REBUILD AND ALL THREE ARE TOLD NOT TO.** `bin/devbin build all` deletes both binaries to force the provenance embeds, so for ~60s **every estate on this machine has no `intent` and every gate fails open** -- Devbin's `e5bf283` committed with no guard at all because of one of mine. One node rebuilds, announced by properties.

**THE PAIR IS `2eb6a8f8`, sha256 `6f77384d7f371c23` / `7bda5b08816e3928`, AND THE HASH IS NEVER THE TEST.** **VERIFY BY PROPERTIES:** `git diff --name-only <marker>..HEAD -- native/rust surface` empty means CURRENT. **NEVER compare the marker to `HEAD`** -- that is watch-out 0, the defect I fixed in the gate at `bc4f5052`.

**THE HIGHLANDER REVIEW IS RECORDED IN `cutover-runbook.md`** -- three axes, four findings. F1 built by ic, F2 parked as separately guarded, F4 built by cc, F3 corrected by cc. **The copy-paste axis is clean and it is the WEAKEST axis** -- say both halves or the sentence misleads. **Axis 3 is the one to repeat first and it is unfinished: `preconditions.rs` and `export.rs` are still undriven, and of the two I did drive, one claim was FALSE.**

## THE ROLLOUT -- LIVE STATE, SEQUENCED UNDER THE PEN

**Released by hv 2026-08-27; the sequencing and every ruling inside it are `authority: vc`.** Landed items carry their commit; nothing here is a plan any more except where it says so.

**LANDED TODAY UNDER THE PEN**

- **THE PORTER's AT-CITATION TRUNCATION -- `e935734d` (cc).** The subject was taken as everything before the first `--`. **cc corrected my brief: the bracket form I relayed is the MINORITY and Lamplight's majority form is BACKTICKS**, so a fixture written to my description would have tested a corpus that does not exist. Their prediction for the re-run is 12 rows to a null file, 62 to a bare path -- checkable, not admirable. **lamplight-vc unblocks when this is IN A BINARY, which is the rebuild below.**
- **AC-11.6 + WP-11's COVER -- `102af78f` (ic).** Both stale sentences were in hv's voice and one superseded the other.
- **THE DOC GATE -- `6c380e09` + `528ab18a` (dc).** Reports and says why it cannot yet refuse.
- **AXIS 3 CLOSED -- `02dbff24` (mine).** 4 of 4 registry claims driven, 3 verified, 1 false (`remedy.rs`, already fixed).

**THE SWEEP IS DEAD AS CONCEIVED AND THE REPLACEMENT IS hv's CALL -- `345104be`, `8ff38517`**

**No v3 code path writes `pre-commit.intent`.** Ruling 4 is in the one layer nothing ships, positive-controlled at 1 on the template and **0 on five estates including this one**. The good half: guard bodies AND the roster propagate live from `INTENT_HOME`, so **tolerance 0 reaches 15 estates the moment it is committed here, with no sweep at all.** Menu with hv: **(A2)** move the gate into the runner and have it translate its own rc -- cheap, measured, and it preserves the ruled fail-open that the naive move would silently kill; **(B)** build the missing verb, which **Baize proves is needed whatever hv picks about (A)**. Hand-copying is recorded to complete the menu, not as a candidate. **NO FLEET WRITE HAS HAPPENED AND NONE WILL WITHOUT hv.**

**IN FLIGHT**

- **dc -- P1 tolerance 0** (ruled, NOT landed until Intent's 648 re-drives at 0 and a +60 positive control passes on both `date` flavours) and **P2 the fleet stamp census**, now with the 2026-08-20 cutoff and the corrected framing: a guarded estate cannot carry a post-guard violation, so the informative corpus is the UNGUARDED one.
- **cc -- P2 `render.rs:423`** (unblocked, ic's file landed) **plus `issues --format`**, ruled BUILD under the pen: the tool advertises `json` and rejects it while accepting undeclared `text`/`markdown`.
- **ic -- the F1 live figure**, going into `doctor.rs` where a per-machine truth check belongs.

**MINE, AND BLOCKED ON A CLEAN TREE**

**THE REBUILD.** Delivered pair is `2eb6a8f8`, measured off the binaries: 9 compiled inputs changed since, and **it does NOT carry the porter fix.** cc and ic are both in `native/rust` right now and the shared-artefact guard correctly refuses a build while any compiled input is uncommitted. **One rebuild, by me, announced by properties before and after** -- it deletes both binaries, so for ~60s every estate on this machine has no `intent` and every gate fails open.

**WITH hv -- NOT RULED UNDER THE PEN**

1. **The sweep replacement menu**, (A2) and (B) above.
2. **cc's two structural enum findings.** `declared_values_are_enforced.rs` walks `values` ARRAYS only and is **green and blind to the 12 pipe-string slots**; and validation happens as an ARGUMENT to the renderer, so **the verdict belongs to the RESULT SET not the slot** -- `wp list ST0056` refuses, `wp list ST0029` accepts, same flag same binary. Widening the test changes what a green means across the surface; moving validation is a contract change.
3. **F1's form -- enum or test.** ic builds the NUMBER, hv keeps the ANSWER.
4. **AC-14.8** -- a fleet write, and every fleet write waits on the menu.

**AWAITING PEERS, NOT BLOCKED ON ME**

**The `claims:` file-claim gap.** Asked laksa-cc and devbin-cc to restate their own finding, separately and without telling either what the other says. **Both idle; neither has answered.** Intent's record carries only the six-word label, which I wrote myself, so **there is nothing here to build against but my own paraphrase** -- and I said I would not.

## Watch-outs

0e. **AN INSTRUCTION TO _PRESERVE_ PRESUPPOSES THAT THE THING IS THERE, AND "KEPT" READS IDENTICALLY WHETHER IT WAS OR NOT** (dc, 2026-08-27, on my own wind-down instruction). I sent three nodes a "these must survive your fold" list. dc **measured both sides** -- `HEAD` and post-fold -- and found that **three of my four items had never been on their board at all.** Not dropped: never there. Had they simply replied "kept", I would now believe their board carries findings it does not, and the fold would have been blamed for a gap it did not cause. **A preservation instruction is a claim about a state I did not check**, and the reply that confirms it is indistinguishable from the reply that would confirm a real preservation. Same family as the honest-and-blind grep, arriving as a HANDOVER rather than as a measurement -- and it is worse there, because a handover has no artefact to re-run. **THE FIX: say "if these are on your board, keep them; if not, they are mine and here they are" -- which costs one clause and converts an unverifiable instruction into a transfer.** dc did exactly that in reverse: recorded all three attributed to me and marked NOT MEASURED BY THEM, refusing to carry my findings as though they had driven them.

**vc's OWN -- durable, standing, never archived. Instances live in the runbook; what is here is the rule.**

0d. **"VERIFIED" FROM A RELAY IS A CLAIM ABOUT THE RELAY'S WORK, AND IT IS THE CHEAPEST WAY AN UNCHECKED ASSERTION GAINS REACH** (devbin-vc on themselves, 2026-08-27; **this one binds THIS NODE hardest and has already caught it twice today**). devbin-cc asserted a `date` truncation behaviour they had not run. devbin-vc answered _"both verify exactly"_ and forwarded the derived number to me -- having computed it in python FROM cc's assumption rather than running the four-line function, which was one command away and load-bearing for everything above it. **The assumption was wrong on macOS, and by then it had three carriers and my own signature on it in hv's inbox.** The relay is what converted one node's unrun reasoning into an estate-wide fact.
**MY OWN INSTANCES TODAY, BOTH RELAYS AND BOTH WRONG IN THE SAME WAY:** hv's brew-unlink ruling forwarded without the re-pin step (devbin-vc added it; unpinned it would have expired at the next `brew upgrade`), and lamplight-cc's _"Baize/Conflab/Laksa survive the drop"_ forwarded while hv's ruling cut the short aliases it assumed. **THE RULE: relaying is authoring.** A claim I pass on carries my credibility whether or not I say it is mine, so either RUN the load-bearing step myself or forward it named and unverified -- _"cc says X, I have not run it"_ -- which costs one clause and stops the laundering. The tell is that the derivation felt cheaper than the command; whenever that is true, the command is the thing to do.

0c. **A PHANTOM COST IN ONE OPTION LOADS A CHOICE EXACTLY AS EFFECTIVELY AS ADVOCACY, AND WITHHOLDING A RECOMMENDATION DOES NOT UNDO IT** (devbin-vc, 2026-08-27, on themselves). They put three options to me with no recommendation -- deliberately, correctly -- and one of them named a cost that CANNOT EXIST: _lower the tolerance and accept commit-lag false positives_, where the check computes `drift = stamp - now` and fires only on `drift > 120`, so lag makes drift NEGATIVE and is invisible to it at any threshold. The unverified causal claim made _leave it and document_ look cheaper than it is. **THIS BINDS ME HARDEST, because I am the node that puts option sets to hv** -- every `AskUserQuestion` I write is this shape, and neutrality of TONE is not neutrality of CONTENT. The discipline: **verify the cost in each option the way you would verify a finding, or state it as unmeasured in the option itself.** An option's cost is a claim, and an unchecked claim inside a choice is not softened by declining to recommend.

0b. **A TRUE RESULT FROM A BLIND INSTRUMENT IS THE UNCATCHABLE MEMBER OF THE FAMILY. FOUR INSTANCES IN ONE DAY, BY FOUR NODES, AND EVERY ONE WAS CHEAP TO PREVENT** (dc's consolidation, 2026-08-27). A false green gets caught -- somebody chases it. **A TRUE green from an instrument that could not have returned anything else gets CITED, and the citation carries no trace of the blindness.**

1. **dc's estate sweep was ACCIDENTALLY RIGHT** -- _0 of 5 installed hooks carry the refusing arm_ was correct, produced by grepping `pre-commit`, which carries the gate in NO estate. Right answer, impossible question.
2. **MY Baize/Conflab prediction was HALF A TAUTOLOGY** -- I told dc a dirty guarded estate would mean a broken instrument. **A guarded estate CANNOT carry a post-guard violation, because the guard refuses the commit.** True by construction, dressed as falsifiable.
3. **dc's GUARDED CORPUS IS CENSORED BY THE INSTRUMENT UNDER TEST** -- the guarded set IS the set of stamps that PASSED the guard, so it cannot contain a violation the guard refuses. Gross errors in that window would leave no trace.
4. **A SUITE WHOSE GREEN WAS INVARIANT UNDER THE THING IT CERTIFIED** -- `whiteboard_clock_guard.bats` was 14/14 after tolerance 0 and **equally green at 120, at 0, and at an hour**, because every check-A fixture is +2 HOURS. **Blind precisely where the defects are** (both known instances are +60s; 299 of the unguarded corpus is the 1-2 minute band) **and confident everywhere they are not.**

**THE ONLY CONTROL THAT SEPARATES A TRUE GREEN FROM A BLIND ONE IS DRIVING THE INSTRUMENT TO BOTH VERDICTS, and it is cheap every single time.** Four of us skipped it four times in one day. **A PARTIAL REPAIR IS WHAT MANUFACTURES THE DURABLE CASE** -- dc's directory half was fixed in the morning and **the FILENAME half survived precisely because that fix had been made**, since a repaired instrument is trusted more than an unexamined one. And **a control that would pass under the broken instrument too is decoration**: I controlled that my flag-coverage tool computed what I told it to, which cannot tell me I told it the wrong thing.

0. **A DIAGNOSTIC THAT COMPARES THE MARKER TO `HEAD` CANNOT TELL A STALE PAIR FROM A CURRENT ONE, AND IT SAYS THE ALARMING THING IN BOTH CASES.** The gate's `self-provenance` line prints `the binary is from an earlier tree` whenever the binary's commit is not literally `HEAD` -- which is TRUE after every board commit, every doc commit, every commit that compiles nothing. Measured this session: it said it on a genuinely three-commit-stale pair (17:18Z) and said the identical sentence on the freshly rebuilt current pair four minutes later, because my own commit had moved HEAD in between. **A warning that fires on the healthy case teaches its readers to skip it**, which is how a pair predating hv's own 16:30Z ruling on `st new` went unnoticed for the whole afternoon. The deciding question is never `is the marker HEAD` -- it is `git diff --name-only <marker>..HEAD -- native/rust surface`, empty meaning current. Same shape as the `--test X` narrowing and the honest-and-blind grep: **the instrument answers a question adjacent to the one asked, and both answers look like the same word.**

1. **AN INSTRUMENT SCOPED NARROWER THAN ITS SUBJECT RETURNS A CONFIDENT WRONG ANSWER, AND THE FAMILY HAS FIVE DISTINCT MEMBERS.** (a) **Wrong predicate** -- a classifier whose default bucket absorbs the unrecognised case cannot report that it failed; count the FIELD, never the file. (b) **Right predicate, wrong PLACE** -- my attachment gate sat below the `--commit` block, so it reported a loss it had just committed; **every property a review checks was correct.** (c) **Right predicate, surface legitimately inconsistent** -- lamplight-ac diffed a correct run mid-flight and had a real data-loss diff in hand; the intermediate state of a correct run is byte-identical to the failure. **A gate must key on the operation's own completion, never on a sample of the tree.** (d) **Correct instrument, lossy summary** -- I pre-filtered PATH symlinks as "release, therefore safe" and named only Conflab. (e) **Correct instrument, wrong corpus** -- my proxy sweep ran `test/` rules against `lib/`, inside the measurement of this very class. **A gate's silence is scoped to its question, and gates sharing a blind spot corroborate perfectly.**
2. **A PERFECT CORRELATION IN BOTH DIRECTIONS IS NOT A MECHANISM.** I reported hop 2's residue check bucket-blind on 8-of-8 found flat and 34-of-34 missed bucketed. cc put the same row in both places: the bucketed one IS reported. **Location and order were confounded because an abort fell exactly on the boundary. No further quantity of the same evidence separates them -- only an experiment does.** And the rc=1 was the tell I read past.
3. **A CORPUS ZERO CANNOT DISTINGUISH "NO INSTANCES" FROM "CANNOT MATCH"; ONLY A CANARY CAN.** Write the construct the rule targets, in the form the toolchain actually produces, and check the instrument fires. Its sibling: **an absence-assertion is satisfied by a harness that never ran** (devbin-vc's pty, two vacuous passes) -- assert something the harness MUST print if it ran at all. And **a property that holds vacuously is indistinguishable from one that holds substantively; only an assertion on the MIDDLE STATE separates them** (ic: a do-nothing inverse round-trips perfectly).
4. **A PERMANENTLY-RED INSTRUMENT IS THE MIRROR OF A FALSE GREEN, AND ONLY ONE LOOKS LIKE A DEFECT.** My `quiet.sh` went red for every estate on other estates' builds and lamplight-vc nearly waved it through -- **a guard scoped wider than its hazard is one the operator learns to ignore.** Print what a narrowing EXCLUDED: that printing caught my own second defect in the same edit, unreadable-cwd processes filed as somebody else's business, which resolves ambiguity toward calm. **An instrument's defects must fail toward ALARM.** **AND BOTH DIRECTIONS ARE AVAILABLE: devbin-vc's `set -- $t` under zsh made every launcher report rc=1 from a broken harness -- a confident FAILURE where mine produced a confident ZERO. A red from a broken harness is the cheaper failure ONLY because someone investigates it; nobody investigates a green.** And worst of the three: `git add` printing `fatal` while the commit SUCCEEDED, so the error and the partial success arrive together and both readers are wrong.
5. **MECHANISM BEATS A NOTE.** zsh does not word-split; an unmatched glob ABORTS the command and returns an empty list that reads as a clean answer; `${PIPESTATUS[0]}` is empty; **an rc read through a pipe is the pipe's** -- lamplight-vc reported six greens that were `tail`'s, one hiding an 18-error build failure. **`git grep -E` DOES NOT HONOUR `\b`** -- Lamplight 0 vs 603, Baize 0 vs 79, Laksa 0 vs 533 across 210 files; use `([^a-zA-Z0-9_-]|$)`, because it is one flag away from certifying an estate clean. **A pipeline's `||` guard binds to the LAST STAGE** -- `sed` exits 0 on empty input, so the "no match" branch can never fire (baize-vc). **Every rc to a file with no pipe; every loop over a newline list in a bash FILE; verify the last step's effect after every multi-step call**, because a parse error runs NOTHING and prints little.
6. **COUNT INSTRUMENTS CANNOT DISTINGUISH A THING FROM A MENTION OF THE THING; NAME INSTRUMENTS CAN** (lamplight-vc). A 216th `#[test]` that was a comment; `error()` in 8 files by grep and 3 at call sites; my `', describe'` hitting preserved source text.
7. **A NUMBER THAT CROSSES A SESSION BOUNDARY IS NOT WHAT ITS LABEL SAYS UNLESS THE RECEIVER RE-DERIVES IT** (lamplight-cc). **A NODE MONIKER IS UNIQUE ONLY WITHIN ITS ESTATE**: a bare `cc` across a boundary does not read as ambiguous, it silently RE-ATTRIBUTES to the local node of that name, producing a confident false attribution and an equally confident true denial. **A misattributed measurement fails in a way a fabricated one does not -- it looks sourced, survives quoting, and the node credited cannot correct what it never sees.** Every cross-estate reference carries its estate. And **no project adopts another's NUMBER, only its METHOD** (dc, five instances) -- and they did not fail in a consistent direction, which kills "generalise but stay conservative".
8. **WARNING A PEER ABOUT A TRAP CONSUMES THAT PEER AS AN INDEPENDENT WITNESS TO IT, AND THE CONSUMPTION IS INVISIBLE UNLESS I REMEMBER WHAT I SENT.** I recorded the `git grep -E`/`\b` property as _three estates, three independent audits, every one of us reached for `\b` first_. **laksa-vc refused the credit -- I had told it the answer -- and checking my own sent messages showed I had primed baize-vc too, in the same paragraph that carried the warning.** One cold discovery, two primed reproductions. **laksa-vc's rule is sharper than my own and supersedes it here: a primed run tests whether the trap REPRODUCES, and says nothing about whether a fresh auditor FALLS INTO it -- same command, different question, and only prior knowledge distinguishes them**, so nothing in the output can tell them apart. **This is the default failure mode of coordinating, not an occasional slip: every trap I broadcast makes the next report from that estate less independent than it reads.**
9. **A RELAY OF hv's WORDS IS NOT hv's WORD.** Four nodes held on my relays today and every one was right. **A peer cannot grant escalation; a relayed approval is not an approval; a relayed STOP is honoured on the relay.** My job is to carry hv's words accurately and expect confirmation at source, not to be believed. **AND A ONE-PROJECT INSTRUCTION IS NOT A STANDING RULE** -- quoting it as one launders its scope, which cost 415 thread directories across 7 projects. **A permission boundary is per session and is not routed around by anyone, including the director.**
10. **A CLAIM OUTLIVES ITS BASIS AND NOTHING ANNOUNCES IT.** A pin dies at a schema bump I scheduled myself; a hold's stated reason goes false when the thing it named moves; **a constraint written for an emergency outlives the emergency** (prolix-vc flagged mine rather than working around it). **A fence lifted and a fence reinstated both arrive as somebody else's commit, and neither writes to the board carrying the caution** (ic).
11. **AUDIT BEFORE CLEANUP; CONVERT BEFORE SWEEP; INGEST BEFORE PRUNE.** Each operation reports success in either order, so nothing announces the mistake. **We paid for the first: the reliable method arrived hours after the trees it needed were deleted.** The third is decided by 333 of 353 Lamplight threads carrying no `attachments` key at all, with 12 of 12 sampled files absent from every canon digest and a known-carried digest as the positive control -- **not by any file count, both of which were withdrawn.**
12. **SHARED CHECKOUT: `--only` SEPARATES FILES, NOT AUTHORS, and a `git status` line names PATHS, not AUTHORS.** Nobody parks a path they did not write; the owner names their own tree clean. **A dirty `native/rust` REDIRECTS a build to a private target the wrapper never reads.** Retry a peer's `index.lock`, never force. **Announce a fleet write BEFORE it runs** -- and a shared release path is a fleet-wide dependency: my rebuild took `intent` off PATH in every project and left **12 commits ungated**, because the critic gate fails open at `exit 0` on a missing CLI.
13. **CANON IS THE SSOT FOR ROWS, NOT FOR PROSE.** The store's refusal is the oracle; nobody edits a ratified contract off a grep's prediction. **The tail after a verdict token is OPAQUE** -- read the verdict, never decompose and reassemble. **"Not on disk" is not "not in canon", and only one is a defect.**
14. **EVERY TIMESTAMP IS READ IN THE SAME COMMAND THAT WRITES IT. A NUMBER IN THE TERMINAL IS NOT A CLOCK**, and a placeholder inside a stamp (`12:2xZ`) reads as a stamp.
15. **A CONTROL IS CONSUMED BY BEING USED AS A SUBJECT, THE OPERATION IS ONE-WAY, AND NOTHING ABOUT THE ARTEFACT SAYS SO** (ic). Read the defining property at CITATION time, off a COPY. **A control chosen after the fact proves nothing; commit to the sha BEFORE the run** (lamplight-ac). **A control that would pass under the broken instrument too is decoration.**
16. **A PARITY SUITE PROVES TWO BINARIES AGREE; IT CANNOT PROVE EITHER IS USEFUL TO ITS CALLER** (dc). **A bidirectional claim implemented in one direction is green forever on the side it does not walk.** **A closed list is safe only when it declares why the things not in it are not in it**; an absent field must be REFUSED, never rendered, and a known token with an EMPTY value substitutes silently where an unknown one refuses loudly.
17. **A DISK TEST WHERE A GIT TEST WAS NEEDED.** A path belongs in a pathspec only if git can see something at it -- an empty directory refused a whole commit **after every hop had run and passed.**
18. **READING THE WRITE-UP OF A CLASS IS NOT PROTECTION FROM IT. THE REMEDY IS NEVER CARE.** Every rule above was written by someone who then committed its instance, several of them me, several the same day.

## Decisions

**LIVE ONLY -- superseded decisions are deleted, never struck through.**

- **A CAPABILITY THE NORMAL ENTRY POINT CANNOT REACH IS NOT DELIVERED** (hv).
- **STATE THE INVARIANT, NOT THE MECHANISM. A CONVERGER IS NOT THE CURE FOR A SECOND HOME; ONE HOME IS. RECORD THE MENU, NOT ONLY THE SELECTION.**
- **A LATER FIRST-HAND RULING FROM THE SAME PRINCIPAL ON THE SAME SUBJECT SUPERSEDES. hv outranks the pen; the pen sequences.** hv's own rulings go on hv's board with their menus; rulings under delegation go to hv's inbox.
- **THE PEN MOVED THE AUTHORITY, NOT THE AIM -- AND NOW THE PUSH, SCOPED TO TWO REPOS.** The confirm gate stays human; `publish` is the one verb with its refusals; the flip is on my word; I tell hv before anyone tags if a gate does not close.
- **LEAD WITH THE DECISION THAT MAKES THE ENDGAME REACHABLE.**
- **A SWEEP TAKES ITS POPULATION FROM THE ARTEFACT'S OWN DECLARATION, NEVER BY TRAVERSAL; A SUBSET MEASURED IS NOT THE POPULATION.**
- **RULE ON THE MEASURED SIZE, AND A PRICE LIST DOES NOT GO UP FOR A RULING UNTIL EVERY ITEM ON IT HAS BEEN TAKEN** (cc, after my reasoned item was the wrong one).
- **A NOTE MUST NAME WHAT IS TRUE WHEN THE ROW IS GREEN, NOT THE COMMAND THAT CHECKS IT.** An instrument named in a row becomes the row's ceiling. **A CORRECTNESS PROPERTY THAT DEPENDS ON THE ORDER OF TWO DELETIONS IS UNVERIFIABLE FROM EITHER COMMIT** -- take them in one, and let a test prove it.
- **HOP 2 IS THE ORACLE, AND ITS SILENCE IS NOT A CENSUS.** It refuses atomically and names the pair; it also stops at the first refusal. **The attachment shortfall gate is load-bearing and NOT redundant with the migrator's accounting** -- the accounting catches a thread whose files the carry did not reach; it cannot catch an estate the walk never enumerated.
- **A RUN THAT MEASURED NOTHING IS NOT A PASS; A PASS IS A `VERDICT:` LINE AT rc 0.** The instrument's own line goes into the commit message so a wrong calm has a reader.
- **A GATE THAT HAS PASSED OVER A ZERO ONCE NEEDS A POSITIVE CONTROL BEFORE ITS NEXT PASS MEANS ANYTHING** (lamplight-vc), **and the negative control is what proves it DISCRIMINATES rather than merely fires.**
