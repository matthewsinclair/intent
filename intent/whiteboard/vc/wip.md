---
node: vc
name: Validation Claude
role: validation
session_id: 1aa05d4a-6da2-4c42-98c6-de024aebab69
heartbeat_at: 2026-08-30 21:37Z
status: active
focus: "LOCALFOLD 2026-08-30 21:37Z, pre-fold at .history/20260830/wip-fold-2133Z.md (49,677 -> 32,097). ST0056 96/135 and WP-08 12/12 -- five closes today, NONE of them new code. hv has FOUR decisions with options. Nothing owed by me to any node."
claims: [ST0056, ST0057, ST0060, ST0064, ST0068]
---

# Validation Claude (vc)

**PROJECT-WIDE RULES IN `intent/restart.md`.** Pre-fold bodies in `.history/20260830/` and `.history/20260829/`.

## DOING

### THE CUT, MEASURED 2026-08-30 21:33Z -- RE-RUN THE VERBS, THESE ROT

**ST0056 96/135** (25 descoped, 2 withdrawn) -- **up from 91 at the 19:44Z fold, and NOT ONE of the five closes was new code.** **ST0057 66/66 PASS. ST0066 6/6 PASS. ST0058 4/6** (dc's). **ST0068 4/9** (mine). **WP-13 (search) is post-tag; ST0060, ST0046, ST0064 are OUT.**

**ST0056's 39 open rows, by WP: WP-00 13, WP-09 6, WP-06 5, WP-15 4, WP-10 4, WP-17 2, WP-11 2, WP-12 1.** **WP-00 IS MINE AND IS THE LARGEST BLOCK, so ST0056 cannot close before my rows do.** **WP-08 IS 12/12** -- closed today.

### WP-00: THE AGGREGATE GATES ARE TWO ROWS, NOT FOUR

**`AT-00.1` and `AT-00.6` are ALREADY red against real, running artefacts with their reasons on the rows** -- the measurement narrowed my own TODO. **Only `AT-00.3` (intentd ships) and `AT-00.4` (MCP ships) cite files that do not exist.**

**`AC-00.3` HAS A NAMED SHORTFALL RATHER THAN A PLACEHOLDER, AND HALF OF IT MAY BE NEWS TO hv:** `intent daemon` offers `start|stop|status|run` and **no `install`/`uninstall`**, and **`graphql.rs` builds a `Schema` whose ONLY use in the estate is `schema().sdl()`** -- nothing in `intentd` references it, so **no GraphQL query is executable anywhere in the product** while D56 ratifies _GraphQL/JSON over the socket AND over HTTP_. The JSON half ships. **`AT-00.3` also carries the last misplaced citation** (`intentd/tests/` -> `intent-cli/tests/daemon_lifecycle.rs`), found by the estate-wide sweep.

**`AC-00.4` is measurable today: `intent mcp` is DECLARED and refuses at rc=2.** ic's tier is being built now.

**`AT-00.20` (`AC-00.16`) IS MINE AND DRIVABLE** -- `instrument_reach_census.sh` is 62 lines and the row says _what is owed is the check, not the finding_. **The population grew again today: `implemented_check.sh` excludes `mcp` BY NAME and `mcp` is the row `AC-00.4` is about.** **`AT-00.15`** (instrument currency) is still writable. **`AT-00.11` is blocked as a RESULT, not a pause (`0160`).**

### WP-15 -- TWO ARMS LEFT

`AC-15.4` and `AC-15.2`'s verb arm are clean and the zero is believable (positive-controlled on planted retired verbs). **Left: `AC-15.1`'s triage table and `AC-15.3`, entangled with dc's subagents build.**

## TODO

1. **`AT-00.3` and `AT-00.4`** -- the two real aggregate-gate authoring jobs, with `AT-00.3`'s citation corrected in the same pass.
2. **`AT-00.20`** -- the instrument-reach census; the population is fresh and I have five instances.
3. **`AT-00.15`** -- instrument currency; a CONTENT test, never a chronological one.
4. **WP-15's `AC-15.1` / `AC-15.3`.**
5. **ST0068**, 4/9 -- and the nine `at` rows dc will move once the recast verb exists.
6. **`parity/register.md`: 39 rows claim work that is done.** All say _Needs per-test rows_ and all 39 have them. **The file is GENERATED, so regenerating REPRODUCES the stale note** -- the generator's template is the fix, and a regenerating artefact that cannot self-correct is worse than a hand-written stale one because refreshing it looks like fixing it.
7. **`0136`'s ~44-site `AcState::Computed` change** -- after the tag.
8. **`intent/wip.md`** -- check before the tag; hv reads it on restart.

## hv items -- FOUR OPEN, ALL ON `hv/inbox.vc.md` WITH OPTIONS AND A RECOMMENDATION

1. **RATIFY `INV-03` OR DECLINE IT** (`0167`). It declares `state: ratified` while its own prose says _hv ratification outstanding_; the vocabulary is `{ratified, provisional}` and **neither value is true.** It is the ONLY finding `vocab_adequacy_check.sh` returns, and **`AT-00.14` is red because of it.** One word closes a WP-00 gate row; declining is equally fine and the vocabulary gains a third state.
2. **MACHINE 5's TABLE AMENDMENT.** `AtStatus::permitted_for` exists as a function **nothing calls on the way in**, and the four `at.set` edges declare no guard -- so adding enforcement without amending the table reds `machine_table_check.sh` for everyone. **`model.rs:1790` says the amendment is hv's pen, not mine.** Recommend deferring past the tag; nothing is blocked on it.
3. **THE 11 UNWIRED MCP ROWS -- ONE FLIP, NOT ELEVEN.** Recommend narrowing all eleven now. ic's principle is the reason: **the flag should track the BINARY, not the roadmap.**
4. **THE 12 FACADE GAPS -- BUILD `schema`, DEFER ELEVEN.** And **`todo notdone`/`todo toggle` are NOT a gap: they MUTATE through helpers that never pass the facade**, which is a boundary leak to record as architecture, not queue as a tool.

**ANSWERED SINCE, NEEDING NOTHING: which `intent` the release script invokes** -- dc built to the recommendation at `8a7ed88d`, verified by me. **AND dc HAS THE IRREVERSIBLE `bin/` DELETE WITH hv DIRECTLY** (26 files / 12,415 lines), which they declined to take on my word.

## Standing directives from hv

- **WATCH THE RUST FOR HIGHLANDER, THIN COORDINATOR AND PFIC on every workstream review.** A posture, not a gate. Paid again today: `agents validate`'s section checks are inline println-woven logic in a dispatch arm, extracted only because MCP needed a second caller.
- **THE MENUBAR ICON IS THE INTENT TURTLE.** Carried by `AC-01.8`, state DERIVED at paint time.
- **THE AIM IS TO FULLY SHIP v3 WITH ALL FUNCTIONALITY. intentd is a priority. Then tree-sitter and full search. Push.**

## Watch-outs

**EIGHT SHAPES. Mechanisms only; incidents are in the fold archives.** Today's additions were folded INTO classes, never appended beside them.

1. **AN INSTRUMENT ANSWERS A DIFFERENT QUESTION THAN THE ONE ASKED, AND ITS OUTPUT LOOKS LIKE AN ANSWER.**

   **1a. THE SECOND LOOK IS NOT INDEPENDENT OF THE FIRST.** The proxy nearest to hand stands in for the subject: a `.canon` EXTRACT for the STORE, a WP `title` for its criteria, a listing that omits the deciding field. **The cure is a question asked BEFORE the measurement -- is this the thing, or a rendering of the thing? -- and when it is a rendering, RUN THE VERB.** **CONVERGENCE IS NOT CORROBORATION WHEN BOTH METHODS SHARE A DEFINITION**; the discriminator is never the verdicts but _could my method have produced a different answer from theirs?_ **AND THE PUREST FORM IS EDITING A RENDERING AND BELIEVING YOU EDITED THE THING** -- vc hand-fixed `WP/08/info.md`, watched it hold, and it was gone within minutes because D01 makes the store SSOT and that file a generated view. **Committed while correcting someone else for a homes problem.** dc hit the mirror image the same hour, reading a dated-amendment list instead of the text and concluding a reword had not landed. **AND ONE WHOLE FAMILY IS READING AN ARTEFACT THAT _DESCRIBES_ WORK AS EVIDENCE THE WORK EXISTS:** a commit SUBJECT LINE read as a manifest (vc); a DECLARATION read as a build (ic, `browse`); absence of a reader read as absence of a writer (cc, `project_id`). **AND AGREEMENT IS NOT DERIVATION** (ic): every `guide.rs` test drove the committed table, so all passed equally against a guide that derives its list and one that keeps its own and happens to match -- **invisible on every tree where the two coincide, which is every tree until someone adds a row.** **AND A PROBE THAT CANNOT EXHIBIT THE FAILURE IT CHECKS FOR RETURNS THE NUMBER THAT MEANS SUCCESS** (`intent st list --limit banana`, where `st list` has no `--limit`). **AND THE GUARD THAT MAKES AN INSTRUMENT QUIET CAN MAKE IT BLIND:** a _the family ships so the leaf is fine_ fallback added to suppress false positives let every `st <anything>` through, so a planted retired verb returned clean. **POSITIVE-CONTROL THE INSTRUMENT** -- ask it to find something you KNOW is there before believing a zero -- and **write the prediction to a file BEFORE the run.**

2. **AN ABSENCE IS THE ONE RESULT THAT NEVER LOOKS LIKE A BUG IN THE QUERY.** A partial read that finds SOMETHING self-corrects; one that finds NOTHING is unfalsifiable without redoing the read. **Output to a FILE, then count.** **THE WORKING DIRECTORY PERSISTS BETWEEN TOOL CALLS, so a stale `cd` made a whole tree read as pruned** -- every path relative, every answer plausible. **AND A CENSUS CAN FAIL TOWARD THE REASSURING ANSWER WITH A PROBABILITY THAT RISES WITH FILE SIZE** (dc): `grep -q` on a pipe under `set -o pipefail` exits on first match, SIGPIPEs the upstream, returns 141, and the predicate answers _no_ -- correct for every short file and wrong for the two longest in the population. **AND `cargo test -p X` DOES NOT BUILD A SIBLING PACKAGE'S BINARIES**, so a round-trip test fails against a STALE daemon and reads as a regression -- three independent hits in one afternoon, and the sharpest statement is ic's: it makes intent-cli's OWN tests fail, not merely say nothing about intentd's.

3. **A CLAIM OUTLIVES ITS BASIS AND NOTHING WATCHES THE JOIN.** A board decays fastest where the work moved fastest. **A rule fails in the artefact that states it** -- twelve instances now, today's being an invented rule id sitting one line under _not by inventing a new rule numbering_, and `intent fc`'s arm citing _`scope_of` rather than a second reading here_ for the parent while hand-rolling `starts_with` for the child four lines away. **AND THE ARROW REVERSES: the basis can POSTDATE the subject.** **AND A HALF-TRUE STATEMENT GETS BELIEVED WHERE A WHOLLY WRONG ONE GETS CAUGHT** (cc): the `daemon start` row's `basis` claims it REFUSES when a daemon is running -- false -- beside a claim that it does not EVICT, which is true and which `AC-08.12` establishes, **so the reader who checks the interesting half has no reason to doubt the clause next to it.** **THE FIX IS NOT A BETTER SENTENCE: where a STRUCTURED FIELD exists for a claim, prose stating the same claim is a second home and goes -- _PROVIDED THE FIELD CARRIES THAT CLAIM_, WHICH IS THE HALF vc LEFT OFF AND ic PAID FOR** (ic, 2026-08-30). **A STRUCTURED FIELD THAT MERELY LOOKS LIKE THE HOME FOR A CLAIM IS WORSE THAN NO FIELD:** with no field you write prose and know it is prose; with a plausible-looking one you move a TRUE fact into a home that will not hold it, **and the move reads like rigour.** **AND THE DRIVEN MEASUREMENT MADE ALL THREE NODES MORE CONFIDENT RATHER THAN LESS** -- cc reported the behaviour, vc ruled, ic DROVE it twice through the real binary, every step correct, and not one of the three checked what the population MEANS. **AND A CORRECTION RECORDED ONLY IN THE ARTEFACT THAT NOTICED IT LEAVES EVERY OTHER HOME AUTHORITATIVE AND WRONG** -- D19's refuted `PATH-first` clause stood in three homes while the correction lived in one criterion, and the reader most likely to be misled is the one who goes to the ratified decision BECAUSE it is the ratified decision.

4. **THE SHARED TREE MAKES ORDINARY OPERATIONS MEAN SOMETHING ELSE. FOUR MEMBERS, AND ONLY ONE IS CLOSED.**

   **(i) CONTENTION -- CLOSED, AND THE FORM MATTERS.** `git commit --only <explicit paths> -m ...` is ONE git operation, path-scoped. **`git add X && git commit` does NOT close it** -- two operations with the whole pre-commit gate running between, and the `commit` half takes **THE INDEX AS IT STANDS**. I wrote the `add &&` form into this class as the cure, adopted it, dropped `--only` in the process, and took dc's staged board file forty minutes later. **A new file still needs `git add` first, so use `--only` WITH the path list even then.** It closes only the half you can close: **your bytes being taken by someone else is not closeable by you.** The redeeming feature is that the loser fails LOUDLY on `cannot lock ref 'HEAD'`, so nothing is written twice -- **what is lost is the COMMIT MESSAGE**, twice in one day. **cc's private index (`GIT_INDEX_FILE` + `read-tree`) closes both axes and MUST BE PINNED ON BOTH SIDES:** the pre-check catches the short window, and **the POST-verify is the half that matters, because the gate runs between the check and git resolving the parent.** Without it the technique REMOVES the loud failure it replaces -- ic nearly reverted two commits and only an unrelated gate red caught it, _luck wearing a green's clothes_. **And a post-verify must be gated on the commit SUCCEEDING**, or a blocked commit prints a reversion that never happened.

   **(ii) COHERENCE -- NOT CLOSEABLE BY THE COMMITTER** (cc). A half-landed pair is an incoherent tree however carefully you scoped your paths. The only fix is the guard's own: land the pair, or wait.

   **(iii) REVERSION -- NOT ADDRESSED BY EITHER MITIGATION, AND IT LEAVES A CORRECT-LOOKING ARTEFACT.** `at.set` at 20:24:29.945Z, `disk.sync_from_disk` at 20:24:31.203Z, **and my green plus the file path I had just corrected were both gone.** D01 makes the store SSOT and a disk->store sync inverts that for one call. **I found it only because the gate figure did not move.** Contention fails loudly, coherence fails at the gate, **this one fails silently.** **Announce any disk->store sync before running it, and prefer `intent st attach <ST> <rel> --from <file>`** -- disk->store for ONE named file, no scoping care, cannot reach a peer's rows. Bare `intent sync --to-store` is flagged destructive and replaces the whole store including durable history.

   **(iv) A GUARD WHOSE POPULATION IS THE WORKTREE OR THE AMBIENT INDEX MAKES EVERY NODE'S COMMIT DEPEND ON EVERY OTHER NODE'S UNCOMMITTED STATE** (`0174`) -- **the widest blast radius of the four.** `runner_roster_check.sh` reads the ambient index (dc's staged tool red-blocked three peers while the same tool passed 68/68 standalone); `view_skew_check.sh` reads the worktree (ic's UNTRACKED scratch file blocked everyone). **`--only` reaches neither, and NOT for the canon-extract reason: there the foreign bytes are inside a file that is mine; here the file is not in the commit AT ALL.** **The fix is a POPULATION, not a predicate, and `canon_commit_check.sh` already shows the pattern.**

   **AND THE CANON PIPELINE CARRIES THE HAZARD ONE LAYER ABOVE GIT, WHERE `--only` CANNOT REACH.** Any store write regenerates `intent/.canon/st/<ST>.json` from the WORKTREE and the extract embeds attachment FULL TEXT, so an `at green` while a peer holds an uncommitted attachment drafts THEIR bytes into a file that is legitimately mine. **`canon_commit_check` makes it BLOCKING rather than untidy.** **THE REMEDY IS A SEQUENCE: `intent st attach` (disk->store), then `intent sync --to-disk <ST>` (store->extract), then commit the file AND the extract together. `sync --to-disk` ALONE does not refresh attachment bytes** -- the half the guard's own remedy text does not say, and it cost me four wrong attempts. **`organize --apply` goes the OTHER way and its preview naming the file `diverged` is the tell.**

   **AND A PEER MID-EDIT MAKES THE SHARED TREE UNBUILDABLE FOR EVERYONE, WITH NEITHER SIDE ABLE TO SEE IT** -- their edit is coherent as a destination and mine was a build of a moment they were passing through, so no session owns the state and no session is at fault. **THE ANSWER IS A DETACHED WORKTREE AT HEAD, NOT WAITING AND NOT ASKING THEM TO HURRY.** **AND REBUILDING A SHARED ARTEFACT MID-MEASUREMENT CHANGES PEERS' INSTRUMENTS UNDERNEATH THEM** -- three nodes rebuilt into one release path and the delivered pair SPLIT, which is still true: `intent` and `intentd` name two different dirty trees today.

5. **A CRITERION IS OWNED BY WHOEVER CAN SATISFY IT, AND MUST BE ABLE TO FAIL.** A row with two owners has none. **Unfalsifiable forms met: an unbounded set; a checklist of page names; a count of a kind the instrument does not count; a criterion restating its source instead of citing it; a bar satisfied by ONE MEMBER OF A GROWING SET, which gets weaker every time the work succeeds.** **A partition DERIVED from its parts closes by construction; only one MEASURED against a population is worth asserting.** **AND AN EXCEPTION WHOSE DISCHARGE CONDITION IS UNMEETABLE READS LIKE THE KIND THAT CANNOT ROT AND BEHAVES LIKE THE KIND THAT DOES** -- vc wrote _it discharges when the daemon serves the surface_ into the very field vc had just ruled was what stops rot, and cc showed it can never become true. **A roster of exceptions each carrying what must become TRUE cannot rot quietly; one carrying reasons why a thing is tolerable rots the moment it stops being tolerable.** **AND AN ASSERTION THAT FAILS FOR A REASON UNRELATED TO ITS SUBJECT TRAINS THE READER TO EDIT ASSERTIONS.**

6. **MAKE THE BAD STATE UNREPRESENTABLE; WHERE YOU CANNOT, WITNESS THE MECHANISM.** Bind-and-publish is ONE call, so publishing an address nobody listens on is unexpressible. **A predicate is sound only relative to what is DONE with the answer.** **AN EXEMPTION LIVING IN A CONTROL-FLOW BRANCH IS THE PROSE VERSION WITH WORSE VISIBILITY**, and so is one living in a node's head -- a near-miss hand-classified in a message is an exclusion recorded nowhere. **AND A ONE-SIDED WITNESS PASSES UNDER THE VERY THING IT REPLACED:** cc hard-wired the carve-out predicate to `always-true` and `always-false` and ran each; `always-true` IS the old wide predicate. **Two-sided, or it proves nothing.**

7. **THE FAILURE PATH IS THE ONE THAT MUST STILL WORK, AND A GREEN RUN NEVER EXERCISES IT.** Cleanup written after the assertions is dead code until it matters. **THE UNIVERSAL SAFE PROBE CAN HAVE A SIDE EFFECT:** `intentd --help` starts a daemon. **AND A TEST HARNESS THAT DOES NOT ISOLATE `HOME` REACHES THE REAL MACHINE** -- cc found `via_library` running in-process under the developer's own environment, so `daemon start` would have bound the socket every peer probes and held `sync`/`ingest` off their stores. **It did not fire only because `intentd` was not beside the harness. Luck, not isolation.** `daemon stop` is the one that surprises: start reads as dangerous, stop reads as harmless, and in-process it stops YOUR daemon.

8. **vc's OWN.** **STOP TRANSCRIBING CLOCK VALUES -- substitute `$(date -u ...)`.** Also `$?` after a pipe; an unquoted heredoc executing backticks; a heredoc marker leaking into a commit message; `--note` OVERWRITING rather than appending; **a zsh split (`$=v`) written into a `bash` script, which reported `unwired=0` while two verbs failed by hand.** **AND I OFFERED TO RUN A VERB A PEER'S SESSION HAD BEEN DENIED** -- laundering whoever volunteers, and it does not reverse when the peer is the one asking. **REPORTING IS NOT ROUTING.** **AND I RULE AGAINST STATES THAT HAVE ALREADY MOVED.** dc's message said _there is no `permitted_for`_; by the time I read it they had written it, moved the nine rows, and put the census in its comment -- **so I told them to build a thing they had built.** A message states HEAD at SEND time and is read against HEAD at READ time, **and nothing on the peer channel carries the revision** -- `AC-00.14`'s own subject (measured-at and asserted-about are two fields) arriving on a channel rather than an instrument. **THE CURE IS CHEAP AND I AM NOT DOING IT YET: re-measure before ruling, every time, however fresh the message looks.** **AND I MEASURE AGAINST STALE BINARIES:** `agents validate` read as unwired against a binary ten commits behind, and the source carrying a match arm the binary refused is what exposed it.

## Decisions

**FOLDED 2026-08-30 21:33Z from 26 entries to 11. Today's incidents went INTO the mechanisms, never beside them; pre-fold verbatim in `.history/20260830/`.**

- **THE MCP RULESET, ONE ENTRY BECAUSE THE CLAUSES ONLY MAKE SENSE TOGETHER.** **(a) MCP tools call the FACADE, never the CLI dispatch arm; one method per exposed row, and where none exists ADD one rather than compose in the tier** -- composing makes MCP a second home for logic the CLI already carries. **(b) A PARENT ROW IS A NAMESPACE, NOT A VERB -- narrow it**; the only tool a parent could offer is one asking _which leaf?_, which is a menu, and the typed tiers exist so an agent need not navigate one. **(c) THE TEST FOR EXPOSURE IS NEED, NOT PROVENANCE** -- _does an agent need this to do its work on THIS project?_ `schema` yes (it is the shape of the entities it is about to manipulate, and getting it wrong corrupts the store), `lang list` yes, `version`/`llm guide`/`surface` no. **My first spelling of this test was _does it tell an agent about the project_, and it was WRONG: `schema` renders compiled-in faces exactly like the rows it narrows, so by that test `schema` narrows too.** ic could have held me to it. **(d) NO EXEMPTION CLASS AND NO `face:` SPELLING.** A softer rule beside the first is where everything ends up -- and when I measured the candidates the population was EMPTY, each for a different reason (`llm guide`/`surface` narrowed on need; `agents generate`/`validate` had their function in `intentsvcs` all along, so they are facade GAPS and rule (a) covers them). **A vocabulary reserved for a population that turns out empty is STRUCK, not left pending: a pending spelling is an invitation.** **(e) `exposed_on_mcp` REFUSES ON ABSENCE, WHICH IS STRONGER THAN THE DEFAULT-FALSE I RULED FOR AND WHICH WAS ALREADY THERE.** cc reported the field defaults true; I checked that the CONSEQUENCE would be bad and never checked the PREMISE. **`dispatch.rs:259` carries no `#[serde(default)]` and the doc block at `:1308` says the omission is deliberate; a row omitting it does not load.** ic's sentence is the keeper: **default-false would CONVERT A FORGOTTEN DECISION INTO A QUIET NON-EXPOSURE instead of a refusal that makes somebody decide** -- it fails in the direction that looks safe, which is the harder one to notice. **Flag-level stays default-true**: that key only trims parameters and can never add a tool, so guarding it would cost ~106 annotations against no defect -- cc withdrew the objection themselves once they saw the level split, and the concern AND its answer go in the field's doc. **(f) THE TIER SERVES IN-PROCESS ONLY IN 3.0.0, so `AC-08.2`'s dual-path property does NOT extend to the MCP face** -- a stated limit carrying its discharge condition (_false when the tier routes through the op dispatch_), not a gap. **(g) DIRECT FACADE CALL NOW, `dispatch(op)` AS THE 3.x DESTINATION, AND THE CHOICE WAS SEQUENCING RATHER THAN PRINCIPLE** -- ic's op-routing shape is the better architecture and I declined it because `Op` carries six variants of a daemon CONTROL protocol, so covering the surface lands ~55 variants on cc's lane ahead of hv's order. **Anti-drift is the estate's own declared-and-gated pattern (`dispatch_ssot.rs` and two siblings), NOT codegen** -- no `build.rs` here generates Rust. **(h) A TOOL THAT CAN ONLY MUTATE INVERTS THE SAFETY GRADIENT**, and a CORRECT refusal can produce it: `st sync` refuses without `write=true` because the declared facade is the write half, leaving the safe half unavailable and the destructive half the only door. Ship it; put the missing half on the gap list, because a temporary refusal becomes permanent by nobody revisiting it.

- **A CRITERION THAT COULD FORCE SCOPE IS WRITTEN AS AGREEMENT, NOT COVERAGE.** `AC-09.6` says _exposed implies servable_, so it closes by BUILDING or by NARROWING, and which one happens becomes a visible decision rather than a default.

- **A DELETE HAS THREE POPULATIONS AND EVERYONE ASKS ONLY THE FIRST.** **(1) WHAT EXECUTES THIS** -- the failure baseline, catches what goes RED. **(2) WHAT CITES THIS AS EVIDENCE -- AND ITS POPULATION IS THE STORE, NOT THE TREE**; a grep-the-tree census is (1) with a wider net and would still have missed `AT-01.8`, which lives in `ST0043.json`. **(3) WHAT CHECKS THIS -- A DELETE CAN HOLLOW A SURVIVOR**, and dc found a further shape by running the standard on the next delete: **it can ORPHAN A SURVIVOR'S ROW ABOUT the deleted thing**, so the parity register does not break, it silently starts describing a file that is not there. **THE STANDARD OVERTURNED THE RULING THAT PRODUCED IT** -- I had ruled `no_absolute_home_paths.bats` deleted; run properly it says KEEP, and the three rows dc migrated are exactly the three the register had already classified out-of-scope.

- **A CENSUS KEYS ON SHAPE AND A DEFECT IS ABOUT MEANING; THE BRIDGE IS _WHAT WOULD THIS ROW'S ABSENCE LOSE?_** I measured 11 ATs declaring `kind: test` while covering a non-test criterion and called them illegible. **dc asked what my census could not: does the criterion's STORED evidence name the artefact its AT cites?** 4 of 8 do not, and there the AT row is the ONLY pointer, so "correcting" them ERASES it. **Four of the eight are signed by me.** dc's sentence is the keeper: **the AT's kind answers _is there a runnable artefact_; the criterion's answers _is satisfaction computed or stored_ -- two facts correlated at 383 of 395, and I treated the correlation as an identity.**

- **AN IDENTIFIER IS ONLY UNIQUE WITHIN ITS SCOPE, AND EVERY BOARD WRITES IT BARE.** 67 of the 166 AC ids in the live cut exist in two or more threads -- 40% -- and four boards carry 73 bare ids. **The reader who resolves it CORRECTLY is the one who gets the wrong row.** Three instances in one day, all mine; the third I caught only by checking, which is the only defence there is.

- **A SECOND HOME IS NEVER ACCEPTABLE AT A TAG, AND THE TAG IS THE REASON RATHER THAN THE EXCUSE** -- two homes that agree today diverge at the first fix, and a tag is when nobody has room for the second fix. **The escape is not _accept two homes_, it is _need less machinery_:** a requirement smaller than the module it would copy creates no second home at all.

- **A SECOND HOME THREE LINES AWAY DOES NOT READ AS A SECOND HOME -- IT READS AS CONTEXT. PROXIMITY IS THE CAUSE, NOT DISTANCE.** Three instances in one day, each with the CORRECT statement within a few lines of the wrong one: `implemented_check.sh` carrying the refuted `mcp never returns` **below its own comment announcing the LIST's second home had been closed**; D19's refuted PATH-first clause; dc's `contract::solely_covered_by` filtering on kind and cover-count while `resolve`, three lines down, says a withdrawn AC is not asked whether it is satisfied. **A duplicate in another crate LOOKS like a duplicate; one in the same neighbourhood reads as elaboration, so nobody applies the Highlander question.** **AND ITS SYMPTOM IS A REMEDY THAT DOES NOT APPLY, NEVER A WRONG COUNT** (dc) -- `ac status` read PASS throughout, so nothing downstream disagreed and only the ADVICE was wrong. **A defect that corrupts no number has no instrument watching it. Advice is unmeasured output.**

- **A CITATION'S AUTHORITY COMES FROM ITS MEMBERSHIP RULE, NEVER FROM ITS NAME.** I replaced cc's valid behavioural argument about `daemon start` with a citation to `populations.self_loop` -- a name that matched the property, bound to something else -- and told them _mine you can check_. ic built on it and reverted it. **cc was right, for the right reason, first. Checkable-and-wrong-subject is more persuasive than a sound argument, which is what makes it dangerous.** **AND THE SAME ERROR HAS A MACHINE FORM:** the dispatch table's `facade` values all came from armscan REACH and were read as DOORS, failing exactly where the value is an accessor or the arm composes. **Mine was a person choosing a plausible name; that one is an instrument populating a field at scale with nobody typing it.**

- **THE CURE FOR THE PLAUSIBLE-LOOKING-HOME CLASS IS A DECLARATION THE BUILD RE-ESTABLISHES, AND ic FOUND IT.** Their MCP serving match plus its in-memory-facade drive means **an arm that cannot serve by calling its DECLARED door cannot land green** -- the field stops being a claim about a past measurement and becomes one the build proves every run. **Every other instance of this class I have recorded was cured by a better sentence; this is the first cured by making the artefact unable to hold the wrong value.** Prefer this shape wherever a field's meaning was established once by an instrument.

- **A TEST GOING RED BECAUSE A FIX LANDED IS THE NOTIFICATION WORKING, NOT A REGRESSION** -- the opposite of the usual failure, where a test passes on after its subject has gone. Prefer this shape when writing a roster or a premise. **AND A CONSTRAINT MET AS AN OBSTACLE OFTEN PRODUCES THE BETTER ARTEFACT:** the gate demanding the literal AT id in the file turned cc's and my arm-level-naming AGREEMENT into something a reader of the TEST learns, not only a reader of the register.

- **`backup.enabled` IS LIVE, AND THE CONFLICT WAS A HOMONYM.** `keys.0` gates the DAEMON's schedule, deliberately not `intent backup`; there is no setting that silences a STALE BACKUP. **Two switches, both sentences carrying _turn off_ and _backup_. The combination is the design: the sweep stops AND doctor keeps reporting staleness** -- a preference you can express and a consequence you cannot hide. Same shape as my `self_loop` error: the phrase matched, the subject did not, and checking cost one file read.

- **THE DAEMON'S PUBLISHED PORT SERVES BOTH PROTOCOLS, DISAMBIGUATED AT BYTE 0 -- NEITHER OF cc's TWO READINGS.** `candidates_under` puts `Unix` FIRST always and APPENDS `Tcp`, so **the TCP listener is the socket's UNDERSTUDY, not a second transport with its own constituency** -- which kills _TCP becomes HTTP_ (it strands the fallback whose only job is the failure you cannot otherwise route around) and confirms cc's own objection to a second port. **`wire::frame` is `serde_json::to_vec` + `\n`, so every frame begins `{` and every HTTP request begins with an ASCII method letter: one byte, no heuristic.** D6 untouched -- the port number is a PREFERENCE, never a promise.

## Coordination -- 2026-08-30 21:33Z, every ruling also on the peer inbox files

**NOTHING IS OWED BY ME TO ANY NODE, AND NOTHING OF MINE IS UNCOMMITTED.**

**cc** -- **FOLDED AND PAUSED at `d8fa3043`.** WP-08 12/12. Ruled for them today: `backup.enabled` LIVE (homonym); **the transport -- one published port, both protocols, byte-0 disambiguation**, which they built at `41f07d45` with all three conditions driven. **They are deliberately NOT starting WP-13 and NOT building into `AC-17.1` or the MCP tier, on the grounds that ACCEPTING WORK is the quiet way a human's sequencing gets overturned.** Owed to them: nothing. **They found my `AT-08.5` note recording a drive of a DIFFERENT FILE, on a row I drove personally -- repaired.**

**dc** -- ACTIVE. Executed hv's retirement at `bbd7e1c0` by the fiat route, ported three of four release-script edges at `8a7ed88d` to my recommendation, landed the mirror guard. Ruled for them: **the `at` kind ruling, THREE TIMES CORRECTED** -- the comment they cited governs a different verb, my conditions 2-3 were AC-side and I attached them to the AT verb in the message correcting them for the same class, the shape is `at.recast` not `--kind`, **and the pen was never mine.** Owed to them: nothing; the `bin/` delete is with hv.

**ic** -- ACTIVE, building the MCP serving match. Ruled: `llm guide`/`surface` NARROW; **no `face:` spelling, question CLOSED not deferred**; `info` STOPS (its door is `st_list` under a description promising an overview); direct facade call now with `dispatch(op)` as the 3.x destination; the in-process-only limit recorded with its discharge condition. **They refuted my `exposed_on_mcp` ruling with a measurement and were right** -- refuse-on-absence already stands and is stronger than the default-false I ruled for. Owed to them: nothing.

**hv** -- **four decisions on the inbox, all with options and a recommendation.** See the hv items section.
