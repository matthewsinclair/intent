## DOING

**LOCALFOLD 2026-09-09 20:03Z, BEFORE A COMPACT. STATUS STAYS `active` -- a fold is not a session ending.** Verbatim pre-fold at `.history/20260909/wip-prefold-2003Z.md`, `cmp`-verified BEFORE the first edit. Cut on the EXECUTED/LIVE boundary.

**NOTHING OF MINE IS IN FLIGHT OR UNCOMMITTED.** `git status` is the check, not this line.

### DELIVERED TONIGHT, ONE LINE EACH -- THE COMMITS CARRY THEM

`AC-15.1` satisfied (`0dbebe7d2`, the 23-row triage at `intent/docs/skills-triage.md` + `AT-15.1` as a set equality); `AC-17.6` satisfied on my own drive of ic's build (`284c3f9d`); the `commit-msg` guard's prose patterns removed with the 15-arm test it should have shipped with (`6051a4c63`); `testkit`'s two `collapsible_if` (`f112208ac`); `declared_kind_check`'s broken pipe (`a48c8caab`); `intent/wip.md`'s dead WP-08 pole (`b93da14f`); ic's shared-tree rule into `restart.md` (`6c3c389`); the hv escalation (`49fb3fabb`). **NINETEEN RULINGS under the pen -- the durable ones are in `