---
node: vc
name: Validation Claude
role: validation
session_id: 699601ed-7e13-4808-bb6c-e6a79d27c56e
heartbeat_at: 2026-08-26 09:25Z
status: active
focus: "**PICKED UP ON A COLD START. ALL THREE GATES RE-DRIVEN AT HEAD `14991ae2`, NOT TRANSCRIBED: `ST0056` 64/133 BLOCKED (69 unsatisfied), `ST0057` 51/53 BLOCKED on `AC-08.6`+`AC-08.7`, `ST0058` 2/6 BLOCKED on `AC-00.1`/`.3`/`.5`/`.6`. IDENTICAL TO LAST NIGHT'S CLOSE -- nothing moved overnight, and ST0058 is still the cut gate.** **THREE INSTRUMENT DEFECTS IN MY FIRST FOUR DRIVES AND ALL THREE ARE WRITTEN DOWN IN PLACES I HAD ALREADY READ:** rc read off a pipe's tail twice (`gate BLOCKED` reported `rc=0`), and zsh not word-splitting an unquoted `$cmd` once (`rc=1 unrecognized subcommand` for a verb that exists). **EVERY ONE RETURNED A PLAUSIBLE WRONG ANSWER, WHICH IS WATCH-OUT 2's POINT MADE AGAINST ME INSIDE FIVE MINUTES OF READING IT.** **hv IS PRESENT TODAY AND `hv/inbox.*` CARRIES 29 LIVE ENTRIES I AM OBLIGED TO SURFACE -- 7 peer, 22 mine -- AND MY OWN STANDING RULE IS TRIAGE BEFORE RULING.** Planning with hv; nothing started, nothing tagged, pushed or published."
claims: [ST0056, ST0057, ST0058, ST0060]
---

# Validation Claude (vc)

**PROJECT-WIDE RULES LIVE IN `intent/restart.md`.** Not repeated here -- a rule in two homes drifts in both.

## DOING

**THE MIGRATION IS RULED AND IS HAPPENING TODAY. hv: _"We're just DOING this migration today, regardless."_ I ASKED IT AS A GO/NO-GO AND IT WAS ALREADY ANSWERED -- hv ruled the driver at 09:4xZ and I re-put it as a decision two hours later. THAT IS THE DEFECT I SPENT THE MORNING CATALOGUING, COMMITTED AGAINST hv PERSONALLY: cc's `sync` question sat in hv's inbox a full day after hv answered it in eight minutes, and I then handed hv one of my own. A QUESTION ALREADY ANSWERED IS INDISTINGUISHABLE FROM AN OPEN ONE TO THE PERSON BEING ASKED.**

**HELP SURFACE: IN, hv's ruling. Background, parallelisable, not a blocker, may land last.** hv left the owner to vc or dc; **I TOOK IT rather than routing it** -- dc is on the critical path with the hook sweep, my canon work is blocked on the suite regardless, and 142 man-style pages of why/what is authoring work. **Starts after the migration lands, not before.**

## THE CUTOVER HAS TWO SWITCHES AND I TOLD hv IT HAD ONE

**CORRECTED AT 10:0xZ ON devbin-vc's MEASUREMENT. My earlier answer -- _migrate by absolute path, flip the symlink last_ -- WAS HALF THE MECHANISM.**

1. **`~/.local/bin/intent`** -- a SYMLINK into the Intentv2 checkout. Atomic, global, serves **seventeen trees** (lamplight-ic).
2. **`INTENT_HOME`** -- **HARDCODED at `~/.zshrc:37`, `export INTENT_HOME="$MOLT_PRJ_DIR/Intentv2"`. NOT DERIVED FROM THE SYMLINK.** (devbin-vc, confirmed by me at source.)

**SO THERE IS NO PER-PROJECT ROLLOUT AVAILABLE. One symlink plus one env var serve all sixteen; the cutover is FLEET-WIDE BY CONSTRUCTION.** Any plan that assumed staging does not hold on this machine's topology.

**AND SWITCH 2 IS THE DANGEROUS ONE BECAUSE IT FAILS QUIET.** Guard bodies load from `$INTENT_HOME/lib/templates/hooks/`, and **two of six ALREADY DIFFER between the trees** (`pre-commit.sh` 86 lines, `canon-ignore-guard.sh` 4). Leave `INTENT_HOME` at Intentv2 after migrating and **every estate commits through V2 GUARDS against a V3 TREE, with nothing saying so** -- perfectly usable, merely stale. **The LOUD failure (v3 renames `info`'s key, the sed returns empty, _NO guard ran for this commit_) is the SAFE one.** It is a SHELL-PROFILE edit: invisible to every per-project check, **effective only in NEW shells**, so every running session -- all nine of us and hv's own shells -- keeps the stale value until restarted.

**I AM STRUCTURALLY BLIND TO THIS AND SAID SO.** v3 resolves a SELF-HOSTED Intent checkout from itself (`6b60367c`), so `intent info` here prints `INTENT_HOME: .../Intent` while devbin's prints `.../Intentv2`. **Both true, different questions -- devbin-vc's own `intent_version` lesson in a second dimension. The producer cannot observe this defect; only a consumer can.**

## THE SEQUENCE

**HOP 1 -- ELEVEN OF SIXTEEN ARE BELOW v3's FLOOR** (`MIGRATION_FLOOR = (2,19,0)`, `project.rs:385`, refused at `facade.rs:1203`). Their first hop is v2 `intent upgrade`, **WHICH MUST BE DRIVEN WITH STDIN CLOSED** -- `0071`, HIGH: it blocks on an interactive read, has no `--yes`, and **HANGS rather than fails** for any non-TTY caller. `0071`'s own body carries the proof: _the identical run with stdin CLOSED completed in seconds at rc=0._

**AND THE TWO HOPS DISAGREE ABOUT WHAT A VERSION MEANS** (devbin-vc's question, my answer): **v2's upgrade is CONVERGENT and probes ON-DISK SHAPE** (`bin/intent_upgrade`, floor 2.9.0, no version branch); **v3's floor reads ONLY THE RECORDED INTEGER** -- `below_floor: parsed.is_some_and(|v| v < MIGRATION_FLOOR)` over `config.intent_version`, and the refusal says _it DECLARES Intent 2.11.5_. **So hop 1 converges shape and writes the stamp hop 2 reads. A converged estate whose stamp did not land is refused while being ready; a stamped-but-unconverged estate walks straight through.**

**HOP 2 -- MIGRATE EACH TREE WITH THE v3 BINARY AT ITS ABSOLUTE PATH.** A partially-migrated fleet is SAFE: v3 refuses an unmigrated tree at **rc=1** with an accurate message and **CREATES NO STORE** (driven on an isolated 2.11.5 copy). **Loud total outage of the CLI, never a wrong answer and never a v3-shaped write into a v2 estate.**

**HOP 3 -- FLIP BOTH SWITCHES TOGETHER, LAST. THEN EVERY SESSION RESTARTS**, because switch 2 only reaches new shells.

**THE CENSUS FRAMING I SHIPPED TO TWO NODES WAS WRONG AND devbin-vc CORRECTED IT: THERE ARE NOT FIVE v2 VERSIONS IN USE.** One tool at 2.19.0 serves all sixteen. The five numbers are each project's record of **when it last ran `intent upgrade`** -- a record of the past read as a description of the present. **It still matters, because v3's floor branches on exactly that recorded number.**

## TODO

0. **TWO CRITERIA TO MINT WHEN THE SUITE CLEARS, BOTH FROM TODAY AND BOTH REPLACING NOTES THAT DEMONSTRABLY DO NOT WORK.** (i) **`claude hook *` MAY ONLY EVER ANSWER 0 OR 1** -- Claude Code treats hook rc=2 as a BLOCKING error, v3 uses rc=2 for _not implemented_, and the only thing preventing a sixteen-project prompt lockout is a per-command guard exemption that `dispatch.rs:72` itself calls **undiscoverable**. Nothing records it as a safety property. Raised by lamplight-ic, driven by vc across the whole version spread, all clear. (ii) **ANY SWEEP OVER THE DISPATCH TABLE MUST ASSERT ITS POPULATION AGAINST A KNOWN FLOOR BEFORE REPORTING A ZERO** -- **a shallow traversal and an empty population are the SAME OUTPUT**, and `families[].entries` has now fooled cc (August) and ic (twice today, independently). **Three instances, two nodes, one data structure.** This is cc's own rule -- _an instrument must assert the artefact is the one under test_ -- applied to POPULATION rather than to identity.

1. **`write_config` RULED: FILE IT, DO NOT FIX BEFORE THE CUT. cc's LOW-to-MEDIUM STANDS AND I TRIED TO RAISE IT AND WAS WRONG.** I reasoned that tonight's migration ends in `stamp_version`, that a config write would materialise every default across 16 projects in one night, and that this turned a gradual private hazard into a simultaneous fleet-wide one. **Checked instead of filing: `facade.rs:152` reads raw JSON into `serde_json::Value`, inserts `intent_version` and mints `project_id`, and writes back. A SURGICAL KEY INSERT, NOT A STRUCT ROUND-TRIP.** No defaults materialised, the fleet event does not exist, and cc's scoping was right the first time -- `lang init` is the first and only consumer in anger. **THIS IS cc's OWN ASYMMETRY LANDING ON ME WITHIN THE HOUR: a finding that RAISES severity gets less scrutiny than one that lowers it, so nothing downstream would have challenged me.** The only thing that caught it was reading the function.

2. **HIGHLANDER ON THE DISPATCH TABLE: ZERO OF FIVE, AND cc's FINDING INVERTS.** ic found three cases MORE than cc reported and then killed all five -- **the only shape of zero worth believing.** **THE DURABLE PART IS THE DISCRIMINATOR: a second home is a violation only if it DECLARES; `{path, why}` is metadata about an entry, not a second entry.** `populations` is not an entry container, so `mcp`, `daemon`, `claude start` and `claude upgrade` all die on one step. **`organize` is the one I would have filed** -- a FULL entry in two containers contradicting on four fields including `disposition`, our signature defect exactly -- **and it is RATIFIED: `name_reclaimed`, hv 2026-08-19. The v2 face stays retired; the v3 row is a different program reusing the token.**

3. **ic IS HOLDING ON hv AND ic IS RIGHT; I HAVE AFFIRMED IT RATHER THAN PRESSED.** hv is live in ic's session and told ic to hold. ic declined to build my ruling 3 on the ground that **_a delegated ruling does not outrank the principal standing in the room_** -- which is my own board's rule turned back on me. **MY PEN DOES NOT BECOME MORE AUTHORITATIVE BECAUSE hv IS BUSY; hv BEING PRESENT MAKES MY RULING THE SECOND-HAND ONE IN THAT ROOM.**

4. **WITH hv, NEEDING A WORD:** the help surface **XL / 142 pages** in or out of the cut; tonight's **go/no-go on the two-hop behind a closed stdin** for the eleven below-floor projects; and **the symlink sequencing, which now has a derived answer rather than an open menu** -- migrate each tree by absolute path, flip `~/.local/bin/intent` LAST, and no project ever sits in the refusing state.

5. **CARRIED, UNCHANGED:** WP-15's split line (now strongest input is **the migration unit is the machine, not the project**); `A5`/`A7` retire as unrecoverable; `AC-00.1`'s CLEAN-BUT-BEHIND sentence is mine to fix; `AC-00.6` satisfy on ic's evidence; the **`pre-commit.intent:97` sed over human-rendered `intent info` output** that every Lamplight guard is reached through, which one structured verb would fix for all 16; **`intent claude ws` is documented in a SHIPPED skill and does not resolve at v2.19.0** -- ours, not Lamplight's.

## Watch-outs

**These are vc's OWN -- durable cautions, standing, not archived.**

1. **AN INSTRUMENT'S OUTPUT READ AS THE SUBJECT'S ANSWER** (dc's generalisation, over SEVEN instances in one evening across four nodes and three languages). **The worst form is cc's: a mechanism answering in the subject's voice WITH THE CORRECT ANSWER** -- `BYTES IDENTICAL: yes` was TRUE, of two copies of a staleness refusal. **The remedy is cc's and it is the only one that closes it: AN INSTRUMENT MUST ASSERT THAT THE ARTEFACT IS THE ONE UNDER TEST, NOT MERELY THAT TWO OF ITS OUTPUTS AGREE.** dc's is the input half: **establish you HAVE a subject before saying anything about one** -- a floor on the population read. **ic's is the shape: a classifier whose DEFAULT BUCKET absorbs the unrecognised case cannot report that it failed.**
2. **MECHANISM BEATS A NOTE -- AND A CONTROL THAT FAILS FOR ITS OWN REASON IS WORSE THAN NO CONTROL** (ic). Every save tonight came from a control that made the wrong answer impossible; **not one came from having read the warning**, and the zsh word-splitting trap is written down verbatim in the estate's memory and bit ic three times. **But mechanism must be built against the ACTUAL SHAPE OF THE DATA, which is a second place to be wrong**: ic's control used space-separated data where the sweep used newline-separated, reported a healthy instrument as broken, and bought a real investigation with a false alarm.
3. **A PARITY SUITE PROVES TWO BINARIES AGREE; IT CANNOT PROVE EITHER ONE IS USEFUL TO ITS CALLER** (dc, `0085`). **No roster disagreed with any roster and the hook was still wrong.** Every instrument the estate owns compares an implementation to another implementation or to a declaration; **none asks whether the output serves the consumer receiving it.**
4. **A CLOSED LIST IS SAFE WHEN IT DECLARES WHY THE THINGS NOT IN IT ARE NOT IN IT** (dc). Declaring why it is CLOSED is a different claim and does not substitute. **Sibling: AN ABSENT FIELD MUST BE REFUSED, NEVER RENDERED** -- `0086`, where a passing TEST requires an omission to print as _no replacement exists_, against the preamble rule in the file the violation lives in.
5. **A BIDIRECTIONAL CLAIM IMPLEMENTED IN ONE DIRECTION IS GREEN FOREVER ON THE SIDE IT DOES NOT WALK** (dc, two instances in a week). **And the cure is not widening someone else's guard quietly -- that is the defect class rather than the fix.**
6. **A CLAIM OUTLIVES ITS BASIS AND NOTHING ANNOUNCES IT.** A workaround outlives its bug because **a defect announces itself and a defect's DISAPPEARANCE does not** (dc). **Caught myself tonight: I told hv the hook surface was a reason the cut date moves, dc swept it to zero, and I struck it the same evening.**
7. **SHARED CHECKOUT: `--only <paths>` IS THE ONLY FORM ATOMIC WITH RESPECT TO PEERS.** Reading `git diff --cached` measures a MOMENT; the commit happens at another. **`MM` is a claim about the INDEX's cached stat, not about content** (dc) -- diff before you remediate. **A `dirty-` binary may READ canon and may NEVER write it or certify a criterion about the code.** **AND UNDER LOCK CONTENTION GIT ACCUSES THE WRONG THING** (dc): a peer's commit held `.git/index.lock` through its whole pre-commit gate, dc's `git add` failed on the lock, and `--only` then refused with _pathspec did not match any file(s) known to git_ -- **a message that points at YOUR path and says nothing about a lock.** Family 1's head arriving through git: **the LAST error read as THE cause.** Gate the commit on the `add`'s own rc; wait on a LIVE lock rather than clearing it.
8. **AGREEMENT AND DISAGREEMENT ARE BOTH UNINFORMATIVE UNTIL YOU KNOW WHETHER THE TWO INSTRUMENTS ASKED THE SAME QUESTION** (ic). **Denominators must never be summed across questions:** ic's 10 is FAMILIES, cc's 37 is ENTRIES.
9. **CANON IS THE SSOT.** Route: edit `.canon` -> `--to-store` -> `--to-disk`, **never `--to-disk` first.** **No CLI verb CREATES an AC or an AT** -- demonstrated again tonight writing `AC-14.12` by hand. **And the gate refuses a green AT row whose file does not carry the literal id.**

## Decisions

**LIVE ONLY -- superseded decisions are deleted, never struck through.**

- **A CONTROL THAT MISSES ITS SUBJECT IS WORSE THAN NONE, BECAUSE IT WOULD BE BELIEVED.**
- **A CAPABILITY THE NORMAL ENTRY POINT CANNOT REACH IS NOT DELIVERED** (hv). **CAPABILITY EXISTS != ROUTE EXISTS ON THE SURFACE.**
- **STATE THE INVARIANT, NOT THE MECHANISM.** A sentence describing a mechanism with two implementations across a cut is wrong on one side whichever side you pick. Applied to two SKILL.md lines and `CLAUDE.md:52`.
- **A CONVERGER IS NOT THE CURE FOR A SECOND HOME; ONE HOME IS.** Ground for retiring `lang sync`.
- **A DETECTOR WHOSE GRANULARITY IS THE FAMILY FORCES THE LANDING GRANULARITY TO BE THE FAMILY** (cc, two instances). Every remaining family is priced as one atomic commit.
- **I DO NOT ASK A PEER TO EDIT `CLAUDE.md`, PERMISSION SETTINGS OR CONFIG, AND I DO NOT DO IT MYSELF TO ROUTE AROUND THEIR REFUSAL.** cc refused me tonight and was right; **a guard bypassed when the reasoning is good is not a guard.** It went to hv, who approved it.
- **RECORD THE MENU, NOT ONLY THE SELECTION.** hv rules by choosing among options vc authors; **an option never on the menu cannot be told apart from one declined.**
- **A PEER RELAYING AN APPROVAL IS NOT THE APPROVAL, AND IT BINDS ME TOO.**
- **THE PEN MOVED THE AUTHORITY, NOT THE AIM.** _Get the release done_ is not _release it_. **hv owns tagging, pushing and publishing.**
- **A RULING THAT DISCHARGES AN ITEM ON ANOTHER NODE'S BOARD GOES IN THEIR INBOX, NOT ONLY OVER SendMessage** (cc, correcting vc). **SendMessage DOES NOT SURVIVE A COMPACT AND THE INBOX DOES** -- that is the protocol's own division: _inbox files carry only what must survive the session_, and a ruling that changes what is owed on a peer's board is the definition of it. **vc told hv the asymmetry was unfixable -- _nothing pushes a ruling back to the board that recorded the question_ -- AND THAT WAS WRONG. Something would have; it was available; it was empty.** Measured at fold: `cc`, `dc` and `ic`'s `inbox.vc.md` were ALL at the sentinel after an evening in which vc issued roughly fifteen rulings over the live channel and wrote NONE to the durable one. **hv then read a discharged item off cc's board because the only record of the discharge was in a message.** **AND IT IS THE SAME LESSON vc APPLIED TWICE TONIGHT TO FINDINGS AND NOT ONCE TO RULINGS:** `0085` and `0087` were filed precisely because an unfiled finding evaporates, and dc found four of five carried items living only in a peer's inbox. **The live send is right; it is not the whole of it.**
- **A RATIFIED RULING IS NOT AN EXECUTED ONE.**
- **A LABEL IS NOT AN ESCALATION, AND IT LOOKS EXACTLY LIKE ONE.** `A5` and `A7` sat on hv's queue for four days as names with no recoverable substance, because the name went into the durable channel and the content went over SendMessage. **TRIAGE BEFORE RULING: a third of hv's queue was not a live ruling** -- two stale, one answered by driving, two unrecoverable -- **and the stale one was the most alarming item on it** (`intent st resume` destroying a hold reason, fixed at `facade.rs:3082` and still being carried to hv as live).
- **A REFERENCE THAT RESOLVES TO THE WRONG SUBJECT READS AS COVERAGE; ONE THAT DANGLES ANNOUNCES ITSELF** (`0088`). `WP-08` cites `AC-10.11` for the create contract and it lands on ST0056's interrupted-migration row -- real, green, unrelated. **AND NO INSTRUMENT ANY THREAD OWNS CAN SEE IT: `ac gate` reads canon ROWS, and an id in prose is not a row.**
- **AN INSTRUMENT MUST NOT BE ABLE TO OBSERVE ITS OWN EARLIER OUTPUT.** cc's rule is that an instrument must assert the artefact is the one under test rather than that two of its outputs agree; **this is the same rule in the TIME dimension**, and it is what caught me reading `.cache` residue from my own positive control as the subject recreating a store. **A fresh tree per arm, always.**
- **A CRITERION'S TEXT IS A RECORD OF A PAST MEASUREMENT, AND THE TREE MOVES.** Re-drive the FALSIFIER, never re-read the EVIDENCE. Two ST0058 rows were satisfied and unmarked; the cost of finding out was one evening and no code. **AND THE CONVERSE IS THE DESIGN RULE: A ROW MUST NAME WHAT WOULD FALSIFY IT**, because a row reciting only its evidence can be believed and cannot be checked.
- **A ROW'S STATED FALSIFIER AND ITS STATED COUNTER-EVIDENCE CAN BE DIFFERENT CLAIMS, AND THEN IT CAN NEVER CLOSE.** `AC-00.1`: the falsifier was silent while the cited counter-evidence was true, of a property the row did not claim. **Resolved by hv, not by me -- widening a criterion changes what it means.**
- **CLEAN-BUT-BEHIND IS A THIRD BINARY STATE, AND IT IS ALREADY MECHANISED -- I SAID IT _REFUSES NOTHING AND WARNS NOBODY_ AND THAT WAS WRONG.** Driven twice within five minutes: with a TEST-ONLY delta the guard stays quiet and the binary answers; the moment cc's `lang` moved `render.rs` and `rules.rs`, **`intent3 version` REFUSED outright -- _2 non-test file(s) under native/rust changed since e63813a2_**. **The discrimination is exactly test vs non-test, which is the right line**, and its silence all evening was correct behaviour rather than a gap. **THE STATE IS REAL AND THE HAZARD IS REAL; MY CLAIM THAT NOTHING GUARDS IT WAS NOT.** cc's sharp form still stands as the reason the guard has to exist: `include_str!` bakes `surface/dispatch-table.json` in at build time, so a stale binary would answer correctly about a table it never compiled. **AND THE GUARD SAVED A REAL ERROR TONIGHT: a stale binary would have answered rc=2 for `lang` and I would have recorded cc's landing as not having happened.**
- **A BUILD IN A LIVE TREE RACES THE TREE, AND `AC-00.1` NOW BINDS ON THAT.** `int local build` compiled in 54s and then REFUSED to call the pair installable -- the binaries name `11d3a0a4`, HEAD had moved to `f9b2f337` while cargo ran. **hv widened `AC-00.1` to COHERENT AND CURRENT an hour before this happened, so the row is now falsified by any peer committing during anyone's build.** That is not an argument against the widening: it is the widening being exactly right, and it is why the row can only close at a quiet moment. **THE CUT NEEDS A QUIET TREE, NOT JUST A GREEN ONE.**
