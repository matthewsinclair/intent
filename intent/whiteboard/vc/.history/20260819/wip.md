---
node: vc
name: Validation Claude
role: validation
session_id: 17c9a8e8-607d-40e7-95db-47fa1a23450d
heartbeat_at: 2026-08-19 08:09Z
status: active
focus: "**THE ONE THING I OWED IS DISCHARGED.** cc landed the live move at `16048f82` -- 57 + 40 into `intent/.canon/`, verified by me at HEAD with `git ls-tree` and by 97 renames carrying 97 IDENTICAL BLOB SHAs -- and I fired the of-N trigger to dc and ic with both literals (`8bb47e49` nested, `16048f82` flat). Minted at `938ed7a3`: the MEASURED-versus-RECORDED form in data-model.md, AC-00.11's two figures MARKED not renumbered, AT-01.7 green 8 of 8, AT-00.11 carrying both literals, AT-00.10 no longer describing a landed rebuild as pending. **ST0011 SOLVED AS EVIDENCE AND STILL UNFIXED ON PURPOSE: the carry is exonerated (55 compared, 0 changed, 0 lost), v2 held the date in TWO HOMES and ST0011 is the one row where they diverged, value 2025-06-03. No field-setter verb exists and that absence IS AC-08.5.** Canon writes route through me; peers hold their status flips."
claims: [ST0056, ST0057]
---

# Validation Claude (vc)

## DOING

- **TRIGGER DISCHARGED at `16048f82`; canon reopened; SINGLE-WRITER ROUTING replaced the freeze.** I told dc they could flip AT-07.4 themselves and CORRECTED IT INSIDE THE MINUTE -- `intent at red|green|na` DESTROYS THE NOTE (0033) and two nodes syncing concurrently clobber by construction. Peers send me row text; I land it.
- **MINT QUEUE: EIGHT CANDIDATES, ONE LANDED.** Held at `<scratchpad>/mint-queue.md`. #5 (provenance) is in canon. Remaining: partition-closure-asserted-in-output, wrong-with-corroboration, red-first-arms-expire, AC-07.4(a)'s second axis (dc's wording, HOLD VERBATIM), coincidence-of-numeral, pin-says-nothing-about-visibility, and **#8 VOCABULARY ADEQUACY, which subsumes but does not discharge #4.**
- **EVERY ONE OF THE EIGHT CAME FROM A PEER BUILDING, NOT FROM ME SWEEPING.** Third day running. The cheapest detector I have is a peer with a compiler.

## TODO

- **ST0011's `completed` -> 2025-06-03 IS EVIDENCED AND DELIBERATELY UNAPPLIED.** Evidence at `<scratchpad>/ST0011-completed-evidence.md`. Fixing it by hand discharges the chore and DELETES THE WORKED EXAMPLE for AC-08.5. It gets fixed BY THE VERB, when the verb exists.
- **A CHECKER VERIFIES MEMBERSHIP IN A VOCABULARY AND NEVER THAT THE VOCABULARY IS ADEQUATE** -- and the forced value always reads as MORE finished, never less. Three instances 2026-08-19 (dc's census, `ratified_in`, figure provenance). Mint it.

- **AT-03.15's RED rests on ten named verbs**, two now measured: **`todo list` does NOT do its documented write and `at lint`'s mutate lives in `--fix`, undriven.** The ten do not reduce. Read the row.
- **THE MARKED-LEGACY FORM AND ST0011 WERE NEVER THE SAME QUESTION, AND I CARRIED "Same question" FOR THREE FOLDS.** `legacy.raw` is about MIGRATION and never appears on a v3-grammar row; the provenance mark is about PROVENANCE and AC-00.11 needed one while being a day old. Specced adjacent at `938ed7a3` with the distinction stated, so nobody merges them later.
- **Name the INSTRUMENT across 124 + 46 wherever two would disagree.** A row-by-row read, not a grep.
- **AC-03.16 is queued to cc as NOT-NOW** -- WP-01 first. Red-first is free; the count 206 is deliberately not asserted.
- **DO NOT RE-OPEN THE QUALIFICATION SWEEP** (measured, zero defects) and **DO NOT SWEEP THE 57 POSITIONAL CITATIONS** -- both would size a remedy to a probe. **A guard on NEW citations is hv's decision, not my discovery.**

## How this estate catches things

**The classes are IN CANON -- read the rows, never a board copy.** ST0056: **AC-00.10** (an instrument that gates must itself be driven), **AC-00.11** (an `N of M` derives M from what was EXAMINED), **AC-03.14/.15** (mtime moves on exactly what changed), **AC-03.16** (no generated view names a generated artefact as truth), **AC-07.4** (the runner reports what it could not check). ST0057: **AC-01.5**, **AC-01.6** (text sweep is the wrong instrument; half-migration classifier), **AC-01.7** (a declaration must RESOLVE, not merely parse), **AC-04.6**, **AC-08.5**. Measurement rules in `parity.md`; output contracts in `output-contracts.md`.

**These are CLASSES. The instances that produced them are in `.history/` and in the rows.**

- **THINKING HARD ABOUT A CLASS DOES NOT PROTECT YOU FROM IT. THE PROTECTION WAS THE INSTRUMENT, AND ONLY THE INSTRUMENT** (dc).
- **FOUR LIMBS, or a passing test coexists with the defect: INSTRUMENT** (two would answer oppositely -- name which), **DEPTH** (asked of the DENOMINATOR too), **EXTENT** (a subset of the right KIND passes), **PIN** (a named commit, never `HEAD`).
- **UNDER-REACH YIELDS A NUMBER THAT IS TOO SMALL; FABRICATION YIELDS A _FACT_, AND A FACT GETS ACTED ON** (ic). **Never `tr` grep output onto one line -- adjacency is not syntax.**
- **AN INDEX THAT RESOLVES AN AMBIGUOUS NAME TO AN ARBITRARY MEMBER FABRICATES CONFIDENTLY AND SILENTLY** (ic's generalisation of my instance). **Not under-reach -- a specific, actionable, WRONG number.** Different failure mode, different defence.
- **RUN THE SWEEP AGAINST THE CONFIRMED POSITIVES BEFORE BANKING THE CLEAN RESULT** (ic, from my withheld zero). **A principle gets nodded at; a step gets executed.** Now in AT-00.11, which is itself a sweep.
- **A CITATION IS A FIGURE** (ic) -- it travels with a subject and a revision, so **name the revision or name the text, NEVER the position.** Four of mine had rotted inside a 1215-line file.
- **AGREEMENT: SAME OBSERVATION, OPPOSITE VALUE, AND THE VERDICTS NEVER TELL YOU WHICH. The discriminator is _could each instrument have produced the other's finding?_** No -- complementary coverage, and the divergence is the yield. Yes -- one method used twice, evidence of nothing. **A probe found by searching for one that hits a KNOWN target is fitted to the answer.**
- **A HALF-MIGRATION IS SILENT EXACTLY WHEN ITS UNMIGRATED END STILL YIELDS A VALUE** (cc). **A missing directory is not a plausible wrong answer; a wrongly-derived filename is.**
- **THIS WORKSPACE HAS TWO CRATES AND ITS MEASUREMENTS KEEP HAVING ONE.** Three instances, two nodes, both directions. **No row -- no form to count.**
- **A PROBE WHOSE POPULATION CANNOT CONTAIN THE FAILURE IT TESTS FOR** (cc), and its transmission twin -- **a tool's DEFAULT VIEW is a filter, and a command sent without the flag it was MEASURED with inherits it** (ic). **Any observable that cannot move is not a check.**
- **THE REASON EXISTS SOMEWHERE AND THE READER IS NOT WHERE IT IS.** Move the reason into the OUTPUT -- **and it only works if the instrument is GATED** (dc). A document's source IS its output, so **only a mechanism that refuses is invariant under copying.** Corollary: **only the output is invariant under repair; a comment shadowing a number is a second copy on its own decay schedule.**
- **WHEN A SUPPORTING FACT TURNS OUT FALSE, ASK WHICH IT HELD UP: THE ARGUMENT OR THE ANSWER** (ic).
- **THE EDIT THAT READS AS CLEANUP REMOVES THE PROTECTION** -- the protection is an ABSENCE and nothing says an absence is load-bearing (ic).
- **THE FORM MUST BE MINTED BEFORE THE POPULATION CAN BE** (dc). **A keyword probe for GOOD PRACTICE false-positives exactly where the practice was done well** (ic).
- **THE SHAPE-CHANGING EVENT IS A RARE, NON-REPEATABLE AUDIT OPPORTUNITY.** WP-01 is one.
- **RE-MEASURE YOUR OWN SENTENCE INSTEAD OF CITING IT** (cc): **a false RATIONALE never goes red.**
- Mechanical: **`open(f,"w").write(open(f).read() + x)` DESTROYS THE FILE.** **AT and AC ids number INDEPENDENTLY.** **A REVERT OF SOURCE IS NOT A REVERT OF ARTEFACTS.**

## THE MODEL, in case everything else is lost

1. **The intentdb is the durable SSOT. Nothing on disk is truth.** The typed Rust API is the ONLY door in.
2. **Migrations are normal.** "No DB migrations ever" is DELETED and was never hv's constraint.
3. **(D34) The committed extract is the INTERCHANGE; the DB is per-machine and NEVER committed.** **AC-02.6 is the durability mechanism.**
4. **Three layers: canon (committed, never sparse) / store (gitignored, rebuilt) / views (committed, sparse).** **D29 -- a gitignored path is never canon -- is what makes a clone complete.**
5. **`event_log` is the only table that is durable truth AND not reconstructible from files.**
6. **hv, repeatedly: disk<->db sync, the realiser and `.intentfiles` all work BEFORE 3.0.0 ships.** ST0057 WP-01..06 are inside the gate. Do not re-ask.
7. **WP-01 makes canon FLAT (`.canon/st/<ID>.json`) and `thread_dir()` must STILL answer `intent/st/<ID>/`.** See AC-01.6.

## Verification kit

- **MEASURE AT A PINNED SHA AND NAME THE COMMIT. PIN A BINARY BY CONTENT HASH, NEVER BY ITS MARKER** -- enforced in `self_provenance_check.sh`'s output (dc). **It also names what it does NOT claim.** Mine: `f2e4d1f9005d0334`.
- **`intent st list` DEFAULTS TO IN-PROGRESS ONLY AND RETURNS 2. `--all` IS NOT A FLAG.** Use **`st list --status all`** -- 57, breakdown **52 Completed / 2 Cancelled / 2 WIP / 1 NotStarted**. **PRINT THE BREAKDOWN, NEVER THE BARE TOTAL.**
- **CHECK THE BINARY'S AGE AGAINST ITS INPUTS -- but NOT via `surface_check.sh`** (retracted, ic). **What caught the wipe was running `sync` and reading the row counts.**
- **A VERB'S RECORDED SIDE EFFECT IS NOT ITS MEASURED ONE.** `todo list` does not do its documented write and does an undocumented 10MB one. **Store materialisation cannot evidence `mutate` -- `st list`, a `read`, does it identically.** **Name the observable.**
- **`at lint` AND EVERY READ VERB READ THE STORE, NOT CANON.** A lint straight after a canon edit reads STALE -- **edit canon, `--to-store`, THEN lint.**
- **`to-write` = UNWRITTEN. `red` = it EXISTS and does not pass.** **A built instrument recorded as unwritten understates in the one direction nobody audits.** An AT earns green from an instrument DEMONSTRATED RED.
- **NEVER `$?` AFTER A PIPE** -- broken twice, the second time while verifying someone else's correction. Redirect to a file and read `$?` from the command itself.

## Watch-outs

- **THE ATTACHMENT DIRECTION, AND I LEARNED IT BY LOSING WORK AND ANNOUNCING THE LOSS AS A LANDING.** `acceptance.md` is a GENERATED VIEW -- a row authored there dies at the next `--to-disk`. **`data-model.md` is an INGESTED ATTACHMENT -- an edit to `thread.json`'s copy of it dies at the next `--to-store`.** THE AUTHORING SURFACE IS THE FILE ON DISK. Same directory, same apparent kind, opposite directions, both silent at rc=0. I edited the JSON, synced, linted clean, committed and told four people; every gate was green because nothing was wrong and the edit was simply not there. **EDIT THE FILE, SYNC, THEN COMMIT.**
- **`git diff --stat` CANNOT EXPRESS SIZE ON CANON JSON** -- an attachment is ONE LINE, so 9,740 bytes entering canon and a one-word typo both read as a single insertion. **NEVER review a canon commit by its diffstat; compare structures.** `938ed7a3` said 15 insertions and silently carried dc's `of_n_population.sh` in under my message. **A peer's attachment committed without a sync leaves canon stale, and the next person's sync sweeps it into THEIR commit.**

- **THERE IS NO VERB THAT CREATES AN AC OR AN AT.** Minting IS a hand-edit of `thread.json` plus a WHOLE-ESTATE `--to-store`, **so two nodes minting concurrently clobber by construction.** Route through me. **`acceptance.md` is a GENERATED VIEW.**
- **`intent at red|green|na` DESTROYS THE ROW'S NOTE (issue 0033).** **`sync --to-disk` writes the STORE over CANON and is the SILENT direction** -- canon-edit then `--to-store` is the safe order, and **a refused `--to-store` leaves a STALE store that `--to-disk` writes out at rc=0** (AC-03.13). **`intent backup` with no subcommand MUTATES.** **AND `--to-store` IS NOT READ-ONLY ON DISK: it REWRITES THE GENERATED VIEWS** (measured in a throwaway copy 2026-08-18, after cc handed back two `acceptance.md` I could not account for). **Every `--to-store` leaves view files dirty, so a mint is a two-file commit and the second file is one I never edited.**
- **Every attachment edit by any node leaves canon divergent until I sync**, and a later sync repairs the NEXT commit, never that one. **Commit first, then ping me.**
- **THE SHARED `native/rust/target/` COSTS hv ~2 MIN PER TEST RUN** (dc, verified by me). `CARGO_TARGET_DIR=native/rust/target/<node>` -- **MUST be inside the workspace**, or `install.rs`'s walk from `current_exe()` finds no marker and **four tests fail as manufactured defects.** **A HARNESS THAT RELOCATES THE BINARY MANUFACTURES FAILURES IN EXACTLY THE TESTS THAT RESOLVE PATHS FROM IT.**
- **DO NOT PUT v3 ON PATH. DO NOT PUSH TO `upstream`** -- this repository is PUBLIC and the remote is FROZEN except dc's one ruled publication.
- **Never mutate `bin/**` or `tests/**` in place; ATOMIC REPLACE anything a running process reads; never edit a peer's file while they are live in it.** **A rig assembled by SYMLINKING into the real tree is not isolated -- `cp` follows a symlink and writes through** (dc, live incident).
- **`git commit --only <paths>`, never `-A`.** **Verify at HEAD (`git ls-tree`)** -- `git grep` reads the INDEX. **Backticks never inside a double-quoted `-m`; use `-F`.** **This shell is zsh:** no word-splitting, a leading `-` reads as options. **`grep -c` EXITS 1 ON ZERO, so a `||` fallback fires on a true zero.**
- **A CLOCK YOU READ AND THEN DID NOT USE IS NOT A CLOCK** -- at `eb580296` I ran `date -u`, got `2026-08-19 00:29Z`, and wrote `2026-08-18 23:52Z`, a value composed before reading. **The guard passed it: a stamp in the PAST clears check A and check B, and only C could ever see it.** **Read, then PASTE -- never read, then type.** **Every timestamp READ FROM A CLOCK** -- `ls -l`, `git log`, `stat -f %Sm` print LOCAL, **and so does the harness: it said 2026-08-19 while `date -u` said 2026-08-18 23:05Z.** An ambient stamp would have been refused by the guard. **ARCHIVE BY NAMING THE STAMPS; COUNT, STOP, READ THE DIFFERENCE.** A fold never overwrites an earlier fold. **THREE fold-header conventions -- count with `grep -nEi '^#+ (FOLD [0-9]+|[A-Z]+ FOLD)'`.**

## Decisions

- (2026-08-18) **THE CRITIC RUNNER'S SILENT SKIP IS THE DEFECT, NOT THE DEFAULT** (AC-07.4, `af67065e`). dc posed refuse-by-default against optional-by-default; **both horns assume OPTIONAL implies QUIET, and it does not.** Report unconditional and named; refusal scoped to rules the project ARMED. **13 of 13 were in the third state and the runner returned 0 because nothing was asked.**
- (2026-08-18) **TWO DRIVE MODES WITH DIFFERENT POPULATIONS GET TWO ROWS** (AT-00.12, `8aa5e004`). Bundling makes a green ambiguous about which population was covered -- **AC-00.11's own defect committed by the row that enforces it.** **An unstated population is how a nomination's horizon travels into the drive.**
- (2026-08-18) **A GENERATED VIEW NAMING A GENERATED VIEW AS TRUTH IS A WORK-LOSS INSTRUCTION** (AC-03.16, `37295d62`, on hv's ruling). Not a wording nit -- **the doc instructs the exact action the model punishes.**
- (2026-08-18) **THE OF-N SWEEP IS A CRITERION, NOT A SWEEP, AND DOES NOT RUN BEFORE cc LANDS.** dc's "closing window" premise was FALSE and checked rather than accepted. **A sweep finds today's ten and protects nothing from the eleventh; ic then FOUND an eleventh from inside the set that had just adjudicated itself.**
- (2026-08-18) **hv AUTHORISED ONE `--no-verify`** (`11bb52da`). **The guard was RIGHT to refuse and nothing about it changes.**
- (2026-08-18) **NAMED-TOOL DECLARATION STANDS** (hv): the rule names WHICH tool, the runner owns HOW, **rule files never contribute shell**, and a tool-armed rule REFUSES when its tool is absent. **`critic_proxy_is_simple` is an INJECTION BOUNDARY.**
- (2026-08-18) **A RULING RELAYED BY ME IS NOT A GO** (cc, quoting my own precedent back after I broke it). **hv gives go-aheads on the node's own channel.**
- (2026-08-18) **ST0057 IS IN THE 3.0.0 GATE**; eight questions ruled. **D57-7** attachments as files under `intent/.canon/st/<ID>/`; **D57-8** `intent://` READ/WRITE, DB first, canon ALWAYS, views IF MARKED. **`canon_commit_check.sh` stays MANUAL.**
- (standing) **A peer cannot grant escalation.** My call is never a peer's release; hv's is. **The `hoist @ 9b73e98f` pin and the register's `9ec1656` are HISTORICAL and must never be re-pinned.**
