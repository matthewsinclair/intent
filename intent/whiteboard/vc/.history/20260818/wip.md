---

## FOLD 10 -- 2026-08-18 (vc, the hoist scored identically and my instrument priced what it could not measure)

5 entries. The rehearsal went green, three contract rows closed, and the two
defects worth keeping were both mine: a dispositions parser that reported complete
against its own blind spot, and a preamble arm that printed a literal where a
comparison belonged. Verbatim, nothing rewritten.

- **MY CONSERVATION CHECK REPORTED `80 matched of 80` OVER 115 DECLARED DROPS, AND THAT IS THE DAY'S WORST DEFECT BECAUSE IT IS MINE AND IT IS THE THIRD TIME.** `Thread.body` landed 35 thread-level drops; my sed requires `/WP/NN/info.md` and a thread drop is `.../ST0056/info.md`. **The denominator came from the parse, so completeness was certified against the blind spot.** The guard beside it refused only a ZERO parse and 80 is not zero. **I fixed this exact class in this exact instrument yesterday (stripped leading zero, 2 of 39) and the fix printed a count computed the same wrong way -- symptom fixed, cause kept.** Worse: the thread arm printed **"the migrator did not name it" as a hardcoded SENTENCE**, never reading the dispositions file it was asserting about -- 32 sections, 13,698 bytes of loss the migrator had explained. **My own claim/observation separation, broken in my own tool.** Fixed: both shapes parsed, the branch routed through `compare_prose` against `Thread.body`, the `related` branch consulted too, and **the denominator taken from the FILE with a strict equality.** Driven both ways: 115/115 passing; mangled paths refuse at exit 2 naming `115 ... parsed 80 ... 35 unparsed`.
- **TWO v3 PARITY DEFECTS FOR cc, both the week's class, both found by running the two engines side by side.** `ac status` prints **`gate: ST0056 BLOCKED -- ...` and exits 0** where v2 prints `ac: 46/114 satisfied -- BLOCKED`; the exit code is right and the PREFIX is wrong, and **a blocked-looking line beside a passing exit code is what a consumer misreads -- the pre-commit gate is a consumer.** `at lint` emits **ZERO BYTES, exit 0**, where v2 prints `lint: ok -- 114 AT row(s) conform`; **a check with no possible OUTPUT cannot be told from one that did not run**, and that line is what I trusted the contract on all session. `ac list` and `at list` are both correct, so neither is a convention.
- **AC-11.5 WAS UNCLOSABLE AND THE NOTE ABOUT IT CLOSED IT.** Marked `(non-test)`, and `ac_is_satisfied` returns on that branch before looking at any AT -- **the test could have gone green forever with no effect**, and the only route the marker left was hand-writing the flag, **which is a record written beside the artefact: the exact form the criterion forbids.** Then my warning note SPELLED the flag and the gate went 46 -> 47 on the first write. **The document is its own data format, so writing about a marker writes the marker.** Caught in the same minute by the scan; nothing else in the contract does it. **`at lint` reads all 114 rows as conforming either way: L5 catches the mirror pairing and names the symptom exactly -- the reverse pairing does the identical harm and is unchecked.** Reported to hv; `bin/**` is not mine.
- **RULED FOR cc ON THE `doctor` EXIT CODE, AND THE ANSWER IS THAT THE QUESTION NAMED THE WRONG THING: THE CLASS IS WRONG, NOT THE EXIT CODE.** cc asked whether an advisory finding should hold `doctor` at exit 1. **The 66 carry `ModelInconsistent` and the model is not inconsistent** -- the reference resolves, the thread parses, nothing contradicts anything. What is true of them is authoring hygiene, which is not a model property. **So exit 1 is CORRECT for a `ModelInconsistent` finding and the defect is that these are not ModelInconsistent findings.** Ruled: **give advisory findings their own class and let the exit code follow the CLASS rather than the count** -- advisory-only exits 0, one real inconsistency among a hundred advisories still exits 1. **Special-casing the exit code would leave a report whose severity vocabulary says one thing and whose exit code says another.** Same shape as the whole day: the report's subject (model consistency) and the actual subject (hygiene) are different and the exit code cannot tell them apart. **Bites the moment anyone wires `doctor` into a gate, and then permanently, because a gate that fails on hygiene has its exit code ignored within a week.**
- **AT-00.5 GOES GREEN WHEN THE GATE THAT _SCORES_ THIS THREAD IS THE FIXED ONE** -- v3's `ac gate` running here, or v2's early return at `bin/intent_acceptance:454` repaired. **LANDING A FIX IN THE REPLACEMENT DOES NOT RETIRE THE INCUMBENT.** cc has accepted the sentence was theirs and generalised it: **in a dual-implementation estate, "landed" without a subject is a DIFFERENT claim, not a weaker one** -- during a rewrite the incumbent governs for exactly as long as it is on PATH. Incident in `.history/`; class in `parity.md`'s `layer` row.

---

## FOLD 11 -- 2026-08-18 (vc, the day Intent hoisted itself onto Intent3)

11 entries, folded aggressively on hv's ask. The hoist LANDED, so every
entry about ACHIEVING it is spent: the one-shot parity constraint, the rehearsal
numbers, the whiteboard-safety check, the working-tree finding, the 192-constant,
and the pre-hoist conservation run whose subject no longer exists. All settled,
all narrated in commits, all superseded by the real thing. Verbatim, nothing
rewritten.

- **THE PARITY COMPARISON IS A ONE-TIME MEASUREMENT: THE HOIST MAKES IT UNRUNNABLE, AND I CONFIRMED THAT BY RUNNING IT RATHER THAN BY READING THE CODE.** The migration writes `"intent_version": "3.0.0-dev"` into `intent/.config/config.json`, and `bin/intent` grew a forward-compatibility guard yesterday (`53f88757`) that refuses a project from the future. **Run v2's gate on the migrated tree and it exits 2: `error: this project declares Intent v3.0.0-dev, and this is Intent v2.19.0`.** **That is CORRECT behaviour and it is not a defect. The consequence is procedural: after cutover there is no v2 half, so v2-versus-v3 parity can never be measured again.** **SO THE PROCEDURE CHANGES: the parity run must happen IMMEDIATELY BEFORE the hoist, on the actual tree being hoisted, because it is the last moment it can be obtained.** Today's run at `6248236e` is fresh and is not a substitute for that -- **it is a rehearsal of a measurement that gets exactly one real execution.** **Found because dc's bats work surfaced the guard; I connected it to the hoist and then drove it. Reading the guard would not have told me the gate refuses -- the guard is in `intent_helpers` and the gate is three files away.**
- **THE STANDARD IS THREE CLAUSES NOW, AND THE THIRD IS ic's WITH MY SECOND INSTANCE.** **An AT row earns its green from an instrument DEMONSTRATED RED; from a criterion naming the SUBJECT it was handed; and from a criterion naming the SHAPE of the input.** ic's case: `store_live` and `store_nokey` are the same subject -- same estate, same trees, same comparator -- and drive different refusals because the JSON shape differs. **Mine, four hours earlier and I missed the connection: my verbatim probe scored `acceptance.md` 0 of 3 and that is NOT loss -- AC rows are parsed into `.criteria[].text` and `.tests[].note`, so a 70-char probe spanning a row cannot match BY CONSTRUCTION.** Same file, same subject, same instrument; **shape decides whether the probe can possibly succeed.** Two nodes, two instruments, one axis, **and the second was not built to confirm the first.** GUIDANCE, not contract -- writing it in would be minting under the moratorium. **ic's bound on the causal rule taken verbatim: it is a question asked while READING, of an instrument with no ledger yet; once a tool has a ledger the ledger holds it.**
- **`192` IS A CONSTANT OF THE ESTATE AND CORROBORATES NOTHING -- cc ANSWERED THE QUESTION I PRE-REGISTERED, AND THE ANSWER IS STRONGER THAN MY REFUSAL WAS.** It is **54 `tasks.md` + 54 `impl.md` + 54 `design.md` (162) + 30 one-off authored files**, over a thread population of 56 at both revisions. **So my 192 at `6248236e` and cc's at `42fb5269` are the SAME FILES: a property of the estate's STRUCTURE, not of the migration's behaviour.** Any migrator that strands companion docs returns 192; **so would one that did nothing.** I refused the comparison as _unestablished_; **cc's framing is better, because unestablished implies it could be established and this cannot be, in either direction.** **The canary's whole prose loss decomposes into exactly two causes with no tail: 503 `kind=issue` (the issue body -- and cc's hand count last night gave the same 503, so two instruments, two code paths, ONE SUBJECT, one number: the only figure today that clears the bar cleanly) and 52 thread-side, every one `## Related Steel Threads`, which is `related: Vec::new()`.** **AT-10.5's argument is untouched -- the migrator still names none of the 555, 347 or 192 -- but the residual is no longer diffuse.**
- **CONSERVATION AT `6248236e` OVER INTENT'S OWN ESTATE, THE FIRST TIME THAT ESTATE HAS EVER BEEN THE SUBJECT.** **ALTERED 0, ADDED 0, DOUBLED-SECTION 0, DEFERRAL-REFUTED 0, dispositions 115 of 115 parsed, liveness names all 56 threads.** Residue **STRANDED 192, UNACCOUNTED 354, LOST-PROSE 384** -- the bucketed-path class, unchanged in kind: those files sit at `intent/st/<BUCKET>/<ID>/` while canon is flat, so they are reachable from git and from a human and not from the model. **`conservation_check.sh` printed `SUBJECT unpinned @ /Users/matts` for an estate elsewhere -- field 3 is a REVISION when the estate is pinned and a PATH when it is not, and I truncated both to 12 chars. That arm had NEVER EXECUTED in the life of the tool** (every fleet member carries a `.CAPTURE` marker) **and the first estate to reach it was the one the hoist migrates.** Fixed at `31ff8204`, finding in the commit.
- **THE HOIST DOES NOT TOUCH `intent/whiteboard/` -- ZERO FILES -- SO FOUR NODES WRITING BOARDS DURING THE MIGRATION IS NOT A CONFLICT.** Measured on the migrated clone: it writes `intent/st` (74), `intent/issues` (40), `todo.md`, `events.jsonl`, `.config`, and nothing else. **This was the thing I understood to be blocking scheduling and it does not exist.**
- **THE MIGRATOR READS THE WORKING TREE, NOT `HEAD`, AND DOES NOT REFUSE A DIRTY TREE -- so uncommitted work is carried and the hoist can run while nodes hold files.** Three cells, one variable each: **tracked `info.md` edited and uncommitted -> CARRIED into canon and store; untracked `design.md` -> not carried; tracked non-canonical `notes.md` -> not carried; and the migrator NAMES none of the three.** **I PREDICTED THE UNTRACKED ONE WOULD BE CARRIED AND IT WAS NOT -- but a COMMITTED `design.md` is not carried verbatim either, so the cell is the STRANDED 192 my own check already counts. My experiment rediscovered my own number by another route: corroboration, not a discovery, and it should not be written up as one.** **THE ONE NEW EDGE: a tracked stranded file survives in git; an UNTRACKED one survives nowhere.** So the precondition is **commit everything under `intent/` before the hoist** -- checked, untracked count is currently ZERO.
- **MY VERBATIM-PHRASE PROBE HAS A FALSE-NEGATIVE MODE AND I NEARLY FILED ITS OUTPUT AS LOSS.** `acceptance.md` scored 0 of 3 probes and that is NOT loss: the migrator parses AC rows into `.criteria[].text` and `.tests[].note`, so a 70-character probe spanning a whole row cannot match by construction. **The probe answers 'is this prose in the store VERBATIM', not 'is this content CARRIED', and the two differ for anything structured.** Third proxy-read-as-parser of the day, after `[^-]*` eating every `intent-cli` citation and a rarest-token probe that compared common English words.
- **THE PREAMBLE IS CLOSED BOTH SIDES, AND THE INTERESTING PART IS THAT I PRICED AN OUTCOME MY OWN INSTRUMENT COULD NOT PRODUCE.** cc proposed a `preamble` field believing the region was carried verbatim; **it was LOST** (`legacy.rs:1373` buffers only once `current.is_some()`), which my check had been reporting as `LOST-PROSE` all along -- **so it is a conservation fix and not an additive field**, and on ST0010 what was going was a cancelled thread's deprecation notice AND its supersession pointer, with no drop record. Specced at `83c2d48e` (own field, never `body`, since `wp_info` renders `body` after `## Objective` and would RELOCATE it -- trading a silent drop for a silent move), built by cc at `283dd00a`, **and every predicted number hit: LOST-PROSE 555, NORMALISED 287, MODELLED 257, 0 findings.** **BUT THE FIRST RUN SAID NOTHING HAD CHANGED**, because my preamble arm printed a literal and never consulted `.preamble` -- **a correct build and a no-op build produced identical output from my instrument.** Fixed at `0735947a`. **A prediction that requires a change to the INSTRUMENT is not a prediction about the SUBJECT**, and cc had correctly declined to check my half.
- **THE HOIST RE-RUN AT `6248236e` IS BYTE-IDENTICAL, AND THE REASON IT HAD TO BE RE-RUN IS THE POINT.** **v2 over FILES, v3 over STORE, and the deleted-`.md` control all return `48/114, 66 unsatisfied` -- and the three outputs are IDENTICAL BYTE FOR BYTE**, including the traversal quirk that prints `AC-08.8` before `AC-08.7` and `AC-14.11` before `AC-14.10`. **That ordering artefact is a fingerprint and it is far stronger than set equality: v3 reproduces v2's traversal, not merely v2's answer.** Migration: **56 threads, 40 issues, 352 files, exit 0**. **THE CONTROL NOW ASSERTS AT SCORE TIME, WHICH IS cc's SHARPENING AND IT CHANGED THE INSTRUMENT:** the surviving `.md` count is printed in the same breath as the score (`MD_AT_SCORE=0`), because a store quietly falling back to files still on disk returns the right number for the wrong reason. **Note the migration WRITES markdown -- 443 `.md` before, 679 after -- so deleting 'the files' now means deleting the generated views too, and it still returns 48.** **WHY IT WAS NEEDED: the 46/114 green was pinned at `bcbd02cd` and FIVE OF FIVE components had moved** -- `legacy.rs` +280/-17, `contract.rs` +187/-42, `store.rs` +42/-7, `model.rs` +40, `facade.rs` +32/-5 -- **plus `acceptance.md` itself, so the SUBJECT moved too and the v2 number was already 48.** Measuring v3 alone would have been uninterpretable: a 46 is ambiguous between 'v3 regressed' and 'the contract moved', **and halves measured at different times cannot separate them. Both halves, one tree, or it is not a comparison.** **TIMING IS VOID FROM THIS RUN AND I AM NOT QUOTING A SPEEDUP.** Machine load was ~160 on 16 cores (four Lamplight sessions building), so v2's 157s is inflated and my harness resolves only whole seconds. **What IS sound, because both halves ran minutes apart on one machine: v2 took 157 seconds and v3 and the control each returned in under one.** The old `95.38s -> 0.02s` pair is from the retired pin and must not be re-quoted.
- **LAMPLIGHT: BOTH FOLLOW-UPS ANSWERED, AND THE PHRASE I PUBLISHED IS WITHDRAWN.** Measured myself on the pinned corpus at `fcfa0ffd`, 5613/5613 verified, reproducing ic's 10 exactly (`unparseable-row: 9, broken-reference: 1`, ST0276 + ST0345). **(1) Both blocking threads ARE live (`status: WIP`, unbucketed), so the block is hv's carry policy working as designed.** **(2) Whether the carry policy accounts for the rest is NOT ESTABLISHED -- Phase A REFUSES before the carry stage runs, so zero carry lines is the refusal's SHAPE and not evidence about the other 1633 AT rows.** **`ten fixes in two files from migrating` is WITHDRAWN** -- ic coined it, I amplified it into hv's inbox and the shared `MEMORY.md`, and three OTHER live threads carry 49 rows of the same form (ST0201 6, ST0264 22, ST0276 21) while the migrator names 8 of ST0276's 21 and nothing for the other two. **A numerator with no denominator.** **The blockers are ONE class in three fields: a qualifier fused to a value where the grammar expects a bare token** (`covers AC-00.1 (see the correction on the AC)`, `status: green **(cc, ...)** Restoring...`, `status: green for Stage A`) -- same class as `959b0190`. **And a method note that is the useful half: `::` as a proxy for "unparseable" is WRONG -- I collapsed 21 `path::name` citations and the count moved ZERO, 9 before and 9 after, which is disjoint populations rather than an imprecise proxy.**
- **`sync --to-disk` IS UNREACHABLE FROM A v2 ESTATE AT THIS BUILD, WHICH IS WHY THE HOIST NEEDS THE MIGRATOR AND NOT A FLAG.** `ingest` is read-only, `init`/`upgrade` are unimplemented (exit 2), and v3 refuses an unmigrated project -- so there is no path from a v2 estate to a written v3 store except WP-10. Measured again 2026-08-17 across 22 families: 8 run, 4 wired-but-gated, 10 unwired.
