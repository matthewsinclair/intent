---

## FOLD 10 -- 2026-08-18 (vc, the hoist scored identically and my instrument priced what it could not measure)

5 entries. The rehearsal went green, three contract rows closed, and the two
defects worth keeping were both mine: a dispositions parser that reported complete
against its own blind spot, and a preamble arm that printed a literal where a
comparison belonged. Verbatim, nothing rewritten.

- **MY CONSERVATION CHECK REPORTED `80 matched of 80` OVER 115 DECLARED DROPS, AND THAT IS THE DAY'S WORST DEFECT BECAUSE IT IS MINE AND IT IS THE THIRD TIME.** `Thread.body` landed 35 thread-level drops; my sed requires `/WP/NN/info.md` and a thread drop is `.../ST0056/info.md`. **The denominator came from the parse, so completeness was certified against the blind spot.** The guard beside it refused only a ZERO parse and 80 is not zero. **I fixed this exact class in this exact instrument yesterday (stripped leading zero, 2 of 39) and the fix printed a count computed the same wrong way -- symptom fixed, cause kept.** Worse: the thread arm printed **"the migrator did not name it" as a hardcoded SENTENCE**, never reading the dispositions file it was asserting about -- 32 sections, 13,698 bytes of loss the migrator had explained. **My own claim/observation separation, broken in my own tool.** Fixed: both shapes parsed, the branch routed through `compare_prose` against `Thread.body`, the `related` branch consulted too, and **the denominator taken from the FILE with a strict equality.** Driven both ways: 115/115 passing; mangled paths refuse at exit 2 naming `115 ... parsed 80 ... 35 unparsed`.
- **TWO v3 PARITY DEFECTS FOR cc, both the week's class, both found by running the two engines side by side.** `ac status` prints **`gate: ST0056 BLOCKED -- ...` and exits 0** where v2 prints `ac: 46/114 satisfied -- BLOCKED`; the exit code is right and the PREFIX is wrong, and **a blocked-looking line beside a passing exit code is what a consumer misreads -- the pre-commit gate is a consumer.** `at lint` emits **ZERO BYTES, exit 0**, where v2 prints `lint: ok -- 114 AT row(s) conform`; **a check with no possible OUTPUT cannot be told from one that did not run**, and that line is what I trusted the contract on all session. `ac list` and `at list` are both correct, so neither is a convention.
- **AC-11.5 WAS UNCLOSABLE AND THE NOTE ABOUT IT CLOSED IT.** Marked `(non-test)`, and `ac_is_satisfied` returns on that branch before looking at any AT -- **the test could have gone green forever with no effect**, and the only route the marker left was hand-writing the flag, **which is a record written beside the artefact: the exact form the criterion forbids.** Then my warning note SPELLED the flag and the gate went 46 -> 47 on the first write. **The document is its own data format, so writing about a marker writes the marker.** Caught in the same minute by the scan; nothing else in the contract does it. **`at lint` reads all 114 rows as conforming either way: L5 catches the mirror pairing and names the symptom exactly -- the reverse pairing does the identical harm and is unchecked.** Reported to hv; `bin/**` is not mine.
- **RULED FOR cc ON THE `doctor` EXIT CODE, AND THE ANSWER IS THAT THE QUESTION NAMED THE WRONG THING: THE CLASS IS WRONG, NOT THE EXIT CODE.** cc asked whether an advisory finding should hold `doctor` at exit 1. **The 66 carry `ModelInconsistent` and the model is not inconsistent** -- the reference resolves, the thread parses, nothing contradicts anything. What is true of them is authoring hygiene, which is not a model property. **So exit 1 is CORRECT for a `ModelInconsistent` finding and the defect is that these are not ModelInconsistent findings.** Ruled: **give advisory findings their own class and let the exit code follow the CLASS rather than the count** -- advisory-only exits 0, one real inconsistency among a hundred advisories still exits 1. **Special-casing the exit code would leave a report whose severity vocabulary says one thing and whose exit code says another.** Same shape as the whole day: the report's subject (model consistency) and the actual subject (hygiene) are different and the exit code cannot tell them apart. **Bites the moment anyone wires `doctor` into a gate, and then permanently, because a gate that fails on hygiene has its exit code ignored within a week.**
- **AT-00.5 GOES GREEN WHEN THE GATE THAT _SCORES_ THIS THREAD IS THE FIXED ONE** -- v3's `ac gate` running here, or v2's early return at `bin/intent_acceptance:454` repaired. **LANDING A FIX IN THE REPLACEMENT DOES NOT RETIRE THE INCUMBENT.** cc has accepted the sentence was theirs and generalised it: **in a dual-implementation estate, "landed" without a subject is a DIFFERENT claim, not a weaker one** -- during a rewrite the incumbent governs for exactly as long as it is on PATH. Incident in `.history/`; class in `parity.md`'s `layer` row.
