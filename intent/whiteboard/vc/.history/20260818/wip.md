---

## FOLD 10 -- 2026-08-18 (vc, the hoist scored identically and my instrument priced what it could not measure)

5 entries. The rehearsal went green, three contract rows closed, and the two
defects worth keeping were both mine: a dispositions parser that reported complete
against its own blind spot, and a preamble arm that printed a literal where a
comparison belonged. Verbatim, nothing rewritten.

- **MY CONSERVATION CHECK REPORTED `80 matched of 80` OVER 115 DECLARED DROPS, AND THAT IS THE DAY'S WORST DEFECT BECAUSE IT IS MINE AND IT IS THE THIRD TIME.** `Thread.body` landed 35 thread-level drops; my sed requires `/WP/NN/info.md` and a thread drop is `.../ST0056/info.md`. **The denominator came from the parse, so completeness was certified against the blind spot.** The guard beside it refused only a ZERO parse and 80 is not zero. **I fixed this exact class in this exact instrument yesterday (stripped leading zero, 2 of 39) and the fix printed a count computed the same wrong way -- symptom fixed, cause kept.** Worse: the thread arm printed **"the migrator did not name it" as a hardcoded SENTENCE**, never reading the dispositions file it was asserting about -- 32 sections, 13,698 bytes of loss the migrator had explained. **My own claim/observation separation, broken in my own tool.** Fixed: both shapes parsed, the branch routed through `compare_prose` against `Thread.body`, the `related` branch consulted too, and **the denominator taken from the FILE with a strict equality.** Driven both ways: 115/115 passing; mangled paths refuse at exit 2 naming `115 ... parsed 80 ... 35 unparsed`.
- **TWO v3 PARITY DEFECTS FOR cc, both the week's class, both found by running the two engines side by side.** `ac status` prints **`gate: ST0056 BLOCKED -- ...` and exits 0** where v2 prints `ac: 46/114 satisfied -- BLOCKED`; the exit code is right and the PREFIX is wrong, and **a blocked-looking line beside a passing exit code is what a consumer misreads -- the pre-commit gate is a consumer.** `at lint` emits **ZERO BYTES, exit 0**, where v2 prints `lint: ok -- 114 AT row(s) conform`; **a check with no possible OUTPUT cannot be told from one that did not run**, and that line is what I trusted the contract on all session. `ac list` and `at list` are both correct, so neither is a convention.
- **AC-11.5 WAS UNCLOSABLE AND THE NOTE ABOUT IT CLOSED IT.** Marked `(non-test)`, and `ac_is_satisfied` returns on that branch before looking at any AT -- **the test could have gone green forever with no effect**, and the only route the marker left was hand-writing the flag, **which is a record written beside the artefact: the exact form the criterion forbids.** Then my warning note SPELLED the flag and the gate went 46 -> 47 on the first write. **The document is its own data format, so writing about a marker writes the marker.** Caught in the same minute by the scan; nothing else in the contract does it. **`at lint` reads all 114 rows as conforming either way: L5 catches the mirror pairing and names the symptom exactly -- the reverse pairing does the identical harm and is unchecked.** Reported to hv; `bin/**` is not mine.
- **RULED FOR cc ON THE `doctor` EXIT CODE, AND THE ANSWER IS THAT THE QUESTION NAMED THE WRONG THING: THE CLASS IS WRONG, NOT THE EXIT CODE.** cc asked whether an advisory finding should hold `doctor` at exit 1. **The 66 carry `ModelInconsistent` and the model is not inconsistent** -- the reference resolves, the thread parses, nothing contradicts anything. What is true of them is authoring hygiene, which is not a model property. **So exit 1 is CORRECT for a `ModelInconsistent` finding and the defect is that these are not ModelInconsistent findings.** Ruled: **give advisory findings their own class and let the exit code follow the CLASS rather than the count** -- advisory-only exits 0, one real inconsistency among a hundred advisories still exits 1. **Special-casing the exit code would leave a report whose severity vocabulary says one thing and whose exit code says another.** Same shape as the whole day: the report's subject (model consistency) and the actual subject (hygiene) are different and the exit code cannot tell them apart. **Bites the moment anyone wires `doctor` into a gate, and then permanently, because a gate that fails on hygiene has its exit code ignored within a week.**
- **AT-00.5 GOES GREEN WHEN THE GATE THAT _SCORES_ THIS THREAD IS THE FIXED ONE** -- v3's `ac gate` running here, or v2's early return at `bin/intent_acceptance:454` repaired. **LANDING A FIX IN THE REPLACEMENT DOES NOT RETIRE THE INCUMBENT.** cc has accepted the sentence was theirs and generalised it: **in a dual-implementation estate, "landed" without a subject is a DIFFERENT claim, not a weaker one** -- during a rewrite the incumbent governs for exactly as long as it is on PATH. Incident in `.history/`; class in `parity.md`'s `layer` row.

---

## FOLD 11 -- 2026-08-18 (vc, the day Intent hoisted itself onto Intent3)

11 entries, folded aggressively on hv's ask. The hoist LANDED, so every
entry about ACHIEVING it is spent: the one-shot parity constraint, the rehearsal
numbers, the whiteboard-safety check, the working-tree finding, the 192-constant,
and the pre-hoist conservation run whose subject no longer exists. All settled,
all narrated in commits, all superseded by the real thing. Verbatim, nothing
rewritten.

- **THE PARITY COMPARISON IS A ONE-TIME MEASUREMENT: THE HOIST MAKES IT UNRUNNABLE, AND I CONFIRMED THAT BY RUNNING IT RATHER THAN BY READING THE CODE.** The migration writes `"intent_version": "3.0.0-dev"` into `intent/.config/config.json`, and `bin/intent` grew a forward-compatibility guard yesterday (`53f88757`) that refuses a project from the future. **Run v2's gate on the migrated tree and it exits 2: `error: this project declares Intent v3.0.0-dev, and this is Intent v2.19.0`.** **That is CORRECT behaviour and it is not a defect. The consequence is procedural: after cutover there is no v2 half, so v2-versus-v3 parity can never be measured again.** **SO THE PROCEDURE CHANGES: the parity run must happen IMMEDIATELY BEFORE the hoist, on the actual tree being hoisted, because it is the last moment it can be obtained.** Today's run at `6248236e` is fresh and is not a substitute for that -- **it is a rehearsal of a measurement that gets exactly one real execution.** **Found because dc's bats work surfaced the guard; I connected it to the hoist and then drove it. Reading the guard would not have told me the gate refuses -- the guard is in `intent_helpers` and the gate is three files away.**
- **THE STANDARD IS THREE CLAUSES NOW, AND THE THIRD IS ic's WITH MY SECOND INSTANCE.** **An AT row earns its green from an instrument DEMONSTRATED RED; from a criterion naming the SUBJECT it was handed; and from a criterion naming the SHAPE of the input.** ic's case: `store_live` and `store_nokey` are the same subject -- same estate, same trees, same comparator -- and drive different refusals because the JSON shape differs. **Mine, four hours earlier and I missed the connection: my verbatim probe scored `acceptance.md` 0 of 3 and that is NOT loss -- AC rows are parsed into `.criteria[].text` and `.tests[].note`, so a 70-char probe spanning a row cannot match BY CONSTRUCTION.** Same file, same subject, same instrument; **shape decides whether the probe can possibly succeed.** Two nodes, two instruments, one axis, **and the second was not built to confirm the first.** GUIDANCE, not contract -- writing it in would be minting under the moratorium. **ic's bound on the causal rule taken verbatim: it is a question asked while READING, of an instrument with no ledger yet; once a tool has a ledger the ledger holds it.**
- **`192` IS A CONSTANT OF THE ESTATE AND CORROBORATES NOTHING -- cc ANSWERED THE QUESTION I PRE-REGISTERED, AND THE ANSWER IS STRONGER THAN MY REFUSAL WAS.** It is **54 `tasks.md` + 54 `impl.md` + 54 `design.md` (162) + 30 one-off authored files**, over a thread population of 56 at both revisions. **So my 192 at `6248236e` and cc's at `42fb5269` are the SAME FILES: a property of the estate's STRUCTURE, not of the migration's behaviour.** Any migrator that strands companion docs returns 192; **so would one that did nothing.** I refused the comparison as _unestablished_; **cc's framing is better, because unestablished implies it could be established and this cannot be, in either direction.** **The canary's whole prose loss decomposes into exactly two causes with no tail: 503 `kind=issue` (the issue body -- and cc's hand count last night gave the same 503, so two instruments, two code paths, ONE SUBJECT, one number: the only figure today that clears the bar cleanly) and 52 thread-side, every one `## Related Steel Threads`, which is `related: Vec::new()`.** **AT-10.5's argument is untouched -- the migrator still names none of the 555, 347 or 192 -- but the residual is no longer diffuse.**
- **CONSERVATION AT `6248236e` OVER INTENT'S OWN ESTATE, THE FIRST TIME THAT ESTATE HAS EVER BEEN THE SUBJECT.** **ALTERED 0, ADDED 0, DOUBLED-SECTION 0, DEFERRAL-REFUTED 0, dispositions 115 of 115 parsed, liveness names all 56 threads.** Residue **STRANDED 192, UNACCOUNTED 354, LOST-PROSE 384** -- the bucketed-path class, unchanged in kind: those files sit at `intent/st/<BUCKET>/<ID>/` while canon is flat, so they are reachable from git and from a human and not from the model. **`conservation_check.sh` printed `SUBJECT unpinned @ /Users/matts` for an estate elsewhere -- field 3 is a REVISION when the estate is pinned and a PATH when it is not, and I truncated both to 12 chars. That arm had NEVER EXECUTED in the life of the tool** (every fleet member carries a `.CAPTURE` marker) **and the first estate to reach it was the one the hoist migrates.** Fixed at `31ff8204`, finding in the commit.
- **THE HOIST DOES NOT TOUCH `intent/whiteboard/` -- ZERO FILES -- SO FOUR NODES WRITING BOARDS DURING THE MIGRATION IS NOT A CONFLICT.** Measured on the migrated clone: it writes `intent/st` (74), `intent/issues` (40), `todo.md`, `events.jsonl`, `.config`, and nothing else. **This was the thing I understood to be blocking scheduling and it does not exist.**
- **THE MIGRATOR READS THE WORKING TREE, NOT `HEAD`, AND DOES NOT REFUSE A DIRTY TREE -- so uncommitted work is carried and the hoist can run while nodes hold files.** Three cells, one variable each: **tracked `info.md` edited and uncommitted -> CARRIED into canon and store; untracked `design.md` -> not carried; tracked non-canonical `notes.md` -> not carried; and the migrator NAMES none of the three.** **I PREDICTED THE UNTRACKED ONE WOULD BE CARRIED AND IT WAS NOT -- but a COMMITTED `design.md` is not carried verbatim either, so the cell is the STRANDED 192 my own check already counts. My experiment rediscovered my own number by another route: corroboration, not a discovery, and it should not be written up as one.** **THE ONE NEW EDGE: a tracked stranded file survives in git; an UNTRACKED one survives nowhere.** So the precondition is **commit everything under `intent/` before the hoist** -- checked, untracked count is currently ZERO.
- **MY VERBATIM-PHRASE PROBE HAS A FALSE-NEGATIVE MODE AND I NEARLY FILED ITS OUTPUT AS LOSS.** `acceptance.md` scored 0 of 3 probes and that is NOT loss: the migrator parses AC rows into `.criteria[].text` and `.tests[].note`, so a 70-character probe spanning a whole row cannot match by construction. **The probe answers 'is this prose in the store VERBATIM', not 'is this content CARRIED', and the two differ for anything structured.** Third proxy-read-as-parser of the day, after `[^-]*` eating every `intent-cli` citation and a rarest-token probe that compared common English words.
- **THE PREAMBLE IS CLOSED BOTH SIDES, AND THE INTERESTING PART IS THAT I PRICED AN OUTCOME MY OWN INSTRUMENT COULD NOT PRODUCE.** cc proposed a `preamble` field believing the region was carried verbatim; **it was LOST** (`legacy.rs:1373` buffers only once `current.is_some()`), which my check had been reporting as `LOST-PROSE` all along -- **so it is a conservation fix and not an additive field**, and on ST0010 what was going was a cancelled thread's deprecation notice AND its supersession pointer, with no drop record. Specced at `83c2d48e` (own field, never `body`, since `wp_info` renders `body` after `## Objective` and would RELOCATE it -- trading a silent drop for a silent move), built by cc at `283dd00a`, **and every predicted number hit: LOST-PROSE 555, NORMALISED 287, MODELLED 257, 0 findings.** **BUT THE FIRST RUN SAID NOTHING HAD CHANGED**, because my preamble arm printed a literal and never consulted `.preamble` -- **a correct build and a no-op build produced identical output from my instrument.** Fixed at `0735947a`. **A prediction that requires a change to the INSTRUMENT is not a prediction about the SUBJECT**, and cc had correctly declined to check my half.
- **THE HOIST RE-RUN AT `6248236e` IS BYTE-IDENTICAL, AND THE REASON IT HAD TO BE RE-RUN IS THE POINT.** **v2 over FILES, v3 over STORE, and the deleted-`.md` control all return `48/114, 66 unsatisfied` -- and the three outputs are IDENTICAL BYTE FOR BYTE**, including the traversal quirk that prints `AC-08.8` before `AC-08.7` and `AC-14.11` before `AC-14.10`. **That ordering artefact is a fingerprint and it is far stronger than set equality: v3 reproduces v2's traversal, not merely v2's answer.** Migration: **56 threads, 40 issues, 352 files, exit 0**. **THE CONTROL NOW ASSERTS AT SCORE TIME, WHICH IS cc's SHARPENING AND IT CHANGED THE INSTRUMENT:** the surviving `.md` count is printed in the same breath as the score (`MD_AT_SCORE=0`), because a store quietly falling back to files still on disk returns the right number for the wrong reason. **Note the migration WRITES markdown -- 443 `.md` before, 679 after -- so deleting 'the files' now means deleting the generated views too, and it still returns 48.** **WHY IT WAS NEEDED: the 46/114 green was pinned at `bcbd02cd` and FIVE OF FIVE components had moved** -- `legacy.rs` +280/-17, `contract.rs` +187/-42, `store.rs` +42/-7, `model.rs` +40, `facade.rs` +32/-5 -- **plus `acceptance.md` itself, so the SUBJECT moved too and the v2 number was already 48.** Measuring v3 alone would have been uninterpretable: a 46 is ambiguous between 'v3 regressed' and 'the contract moved', **and halves measured at different times cannot separate them. Both halves, one tree, or it is not a comparison.** **TIMING IS VOID FROM THIS RUN AND I AM NOT QUOTING A SPEEDUP.** Machine load was ~160 on 16 cores (four Lamplight sessions building), so v2's 157s is inflated and my harness resolves only whole seconds. **What IS sound, because both halves ran minutes apart on one machine: v2 took 157 seconds and v3 and the control each returned in under one.** The old `95.38s -> 0.02s` pair is from the retired pin and must not be re-quoted.
- **LAMPLIGHT: BOTH FOLLOW-UPS ANSWERED, AND THE PHRASE I PUBLISHED IS WITHDRAWN.** Measured myself on the pinned corpus at `fcfa0ffd`, 5613/5613 verified, reproducing ic's 10 exactly (`unparseable-row: 9, broken-reference: 1`, ST0276 + ST0345). **(1) Both blocking threads ARE live (`status: WIP`, unbucketed), so the block is hv's carry policy working as designed.** **(2) Whether the carry policy accounts for the rest is NOT ESTABLISHED -- Phase A REFUSES before the carry stage runs, so zero carry lines is the refusal's SHAPE and not evidence about the other 1633 AT rows.** **`ten fixes in two files from migrating` is WITHDRAWN** -- ic coined it, I amplified it into hv's inbox and the shared `MEMORY.md`, and three OTHER live threads carry 49 rows of the same form (ST0201 6, ST0264 22, ST0276 21) while the migrator names 8 of ST0276's 21 and nothing for the other two. **A numerator with no denominator.** **The blockers are ONE class in three fields: a qualifier fused to a value where the grammar expects a bare token** (`covers AC-00.1 (see the correction on the AC)`, `status: green **(cc, ...)** Restoring...`, `status: green for Stage A`) -- same class as `959b0190`. **And a method note that is the useful half: `::` as a proxy for "unparseable" is WRONG -- I collapsed 21 `path::name` citations and the count moved ZERO, 9 before and 9 after, which is disjoint populations rather than an imprecise proxy.**
- **`sync --to-disk` IS UNREACHABLE FROM A v2 ESTATE AT THIS BUILD, WHICH IS WHY THE HOIST NEEDS THE MIGRATOR AND NOT A FLAG.** `ingest` is read-only, `init`/`upgrade` are unimplemented (exit 2), and v3 refuses an unmigrated project -- so there is no path from a v2 estate to a written v3 store except WP-10. Measured again 2026-08-17 across 22 families: 8 run, 4 wired-but-gated, 10 unwired.

---

# FOLD 12 -- board as at 2026-08-18 10:44Z, archived verbatim before rewrite

---

node: vc
name: Validation Claude
role: validation
session_id: e48565a9-8dc8-4718-bb68-37a3462a0a36
heartbeat_at: 2026-08-18 10:31Z
status: active
focus: "**INTENT IS HOISTED AND SELF-HOSTED ON INTENT3 -- `0ec2ac79`, flat layout `1af21f4e`.** v2-over-FILES 48/114 taken FIRST (one-shot), migrate 56 threads / 40 issues / 352 files exit 0, `sync --to-store`, v3-over-STORE 48/114 **BYTE-IDENTICAL**. **It refused first on a pre-versioning store at `intent/.cache/intent.db` -- a GITIGNORED path, so no clone could ever have held it and no rehearsal could have found it.** Then 192 authored files moved to flat paths and 194 superseded views removed, on the migrator's own declared-drop record after byte-equality proved the wrong control. **hv HAS RULED THE DISK MODEL: no status directories, disk becomes OPTIONAL, index plus render-on-demand.** **PRECONDITION, ABSOLUTE, AND I HAD IT WORDED WRONG: nothing may DELETE a file until `conservation_check.sh` returns a GREEN VERDICT WITH ITS DENOMINATOR -- exit 0, a printed `0 finding(s)` line, `compared N of N`, over a PINNED subject. ic drove both counters to zero on an unconserved estate (`a519398d`), so the two-counter phrasing was satisfiable while the estate leaked; the tool never went green and its own scope line is what shows it. Corrected in the tool header** -- today 380 of 485 `.md` are in the store, 52 are not, 53 are untestable, and all 40 issue bodies are disk-only. **TASKED: cc builds `attachments` (specced `1ffe8868`) + issue `body`; ic owns the conservation run that IS the green light; dc owns the index.** **OPEN WITH hv: the shim, the exit-code fix in `bin/**`, the moratorium (spent by its own terms), ic's residue question, D50, the interruption AC.** **THE GATE HAS ENFORCED NOTHING ACROSS ALL FIVE LANGUAGES SINCE THE HOIST and I caused it.** Upstream FROZEN; v3 NOT on PATH; push local only."
claims: [ST0056]
---

# Validation Claude (vc)

## DOING

- **ST0057 EXISTS, WAS CREATED THROUGH THE v3 CLI BY hv, AND CARRIES THE DISK-MODEL DESIGN. `442ef27d` + `e7c11f14`.** hv's ask: _"what better time to test all of the Intent3 mechanics than by using intent to create a new ST"_ -- one command at a time, hv typing, me reading the output. **SEVEN COMMANDS PRODUCED FOUR DEFECTS AND ONE DESIGN REVERSAL, and the suite could express NONE of the first four.** (1) **`st list` failed: `no such column: seq`** -- two different `attachments` shapes both stamped `user_version 10`, because a rung was EDITED after a store had run it; **every test starts from a fresh store so every test passed.** cc built rung 11; a REFUSING `sync` walked the ladder. (2) **doctor's remedy names `intent sync`, which has no bare form.** (3) **`st cancel` produces a thread `doctor` immediately calls broken** -- facade writes an end date for `Cancelled`, doctor calls any end date on a non-`Completed` status inconsistent, **and the remedy is unactionable because the next `st cancel` writes it back.** Firing on ST0010/ST0015 right now. (4) **`created: ST0057` wrote three files and regenerated TWO tracked indexes while printing one word.** Plus: **Triage-as-entry cost two extra commands and hv amended their own ratified ruling** (`53bc2c10`) -- `st.start` now legal from `Triage`.

- **hv RULED BOTH OPEN QUESTIONS AND ADDED THE HALF THE DESIGN WAS MISSING.** **Q7: the 165 are ATTACHMENTS, `THREAD_PROSE` deleted** -- subtractive, closes the 165 and the 166th together. **Canon relocates to `intent/.canon/`, still per-artefact** (one consolidated file is a merge-conflict generator with four writers). **AND D57-5, NEW: a full text realisation of the whole project into `.backup/text/<UTC>/` as a human fallback.** **The two assurances are DIFFERENT and only one was covered: the dehydration gate proves THE STORE holds it; a complete text export proves A HUMAN can get it back without the tool.** **It lands on `export --format md`, which is DELIBERATELY REFUSED today on the grounds that _"the views are already in the tree"_ -- correct now, false the moment this thread lands, so RULED withdrawn as PART of the thread and not before.** Precondition cc found: **`intent init` is NOT IMPLEMENTED**, and you cannot demonstrate a fallback from a clean directory you cannot create.

- **THE DAY'S RULE, IN cc's NARROWING RATHER THAN MINE, BECAUSE MINE WAS UNCOSTED.** Not _"check your premises"_ -- that would not have found any of this and prices out by day two. **_"When someone states a claim as the LOAD-BEARING reason for a decision, THAT claim is the one to check."_** One grep each, both times **aimed by someone else's stated reason**. **And the social half is the mechanism rather than a caveat: the person who states the reason is not usually the person who can see it is unchecked** -- which is why the rule is cheap, and why the peer structure is the instrument rather than overhead on the work. It read three times today. cc's closing line is the one to keep: **_the suite is not weak; it is answering a different question, and the day's work was finding out which question that is._**

- **THE BASELINE IS PINNED AND REPRODUCIBLE ACROSS THE WHOLE APPARATUS (ic).** Snapshot `2df3549c`, 0 uncommitted, revision published. **Two independent full passes -- separate snapshot, separate build, separate target dir, separate capture -- give IDENTICAL numbers**, which is reproducibility of the apparatus rather than a repeated read of one tree. `ALTERED 2, LOST-PROSE 0, UNCONVERTED 0, UNACCOUNTED 353, STRANDED 192, findings 741, byte-identical 1192, normalised 305, compared 1614 of 2127, LIVENESS 56/56, stderr clean.` **NOT gate-satisfying and ic does not claim it is -- exit 1, and what changed is the PROVENANCE rather than the verdict.** STRANDED 192 governs; the 748 behind it wait on hv's open question 7.

- **THE SCOPE LINE IS BUILT AND IT IS THE STRUCTURAL FIX FOR THE LINE-61 CLASS.** `estate_census.sh:61` has always said the model does not hold `design.md`/`impl.md`/`tasks.md`; **it is true, I wrote it, and it never reached the gate.** A note would not fix that and a bigger note would not either -- **a PRINTED LINE does.** The tool now names, in its own voice, that prose came from `[criterion issue test thread wp]` only and that every `design.md`, `impl.md`, `tasks.md` and one-off is **OUTSIDE the population every line above is about.** **And it declares the limit of its own denominator: `a PROSE row carries NO PATH, so this tool cannot name which contributed nothing`.** **MY FIRST VERSION RETURNED `1153 of 1` and `-1152`** because I assumed `$NF` was the path on both row kinds -- FILE is five fields with the path at `$2`, PROSE is seven with no path at all. **The nonsense number is why it was worth driving: a negative count is unmissable and a plausible wrong one would not have been.** `cf5e02fd`.

- **A NEW VARIANT, AND THE CLEANEST OF THE LOT: A PROBE WHOSE POSITIVE IS GENERATED BY THE DOCUMENTATION OF ITS OWN NEGATIVE.** ic's `is Issue.body VERBATIM?` greps `legacy.rs` for `trim` and reported `still mentions trim` at a snapshot with 0 uncommitted -- **and they refused to conclude from it, correctly.** Resolved by reading the carry: **`legacy.rs:723` is `body: body.to_string()` under _"VERBATIM, and not even trimmed"_. The change HAS landed.** `trim` appears fourteen times: once in the comment saying it does NOT, once at 499, a dozen unrelated. **So the act of RECORDING the fix is what makes the check report the bug, and the better the comment the louder the false positive.** **Real finding underneath: `legacy.rs:499` still says _"the trailing-newline trim `Issue::body` declares"_ -- it no longer declares one.** My section-7 class in cc's file an hour later: **the correction landed at the SITE and not at the CROSS-REFERENCE.** Flagged to cc.

- **MY CALL ON THE ESTATE REGENERATION: NOT YET, cc's WHEN IT HAPPENS, AND ST0011 WAITS FOR THE SAME WINDOW.** cc asked. **ic's baseline is method A on a PINNED corpus, so it is untouched by a live regeneration and the urgency from that side is not real.** Three reasons to wait: **hv's ruling on 163 changes what the migration WRITES**, so doing it now means doing it twice; **the live canon and store are currently the product of a DEFECTIVE TEST RUN** (schema 9, trimmed bodies, written by `dispatch_ssot` at 08:55:06) and **curing an unowned bulk write with another unowned bulk write is the same disease**; and it is a **bulk rewrite of COMMITTED canon on a public repo, so hv sanctions it -- I will not authorise that on my own signature and would refuse it from a peer.** **ST0011 gets no exemption for being one row.**

- **`LOST-PROSE 0` IS TRUE, AND THAT IS WHAT MADE THE SHORTHAND BUILT ON IT LETHAL. THE PHRASING WAS IN THE SPECIFICATION, NOT JUST ON BOARDS -- `data-model.md:200`, MINE. GONE AT `027bbc2c`.** ic closed my open question and the answer was neither hypothesis: **`ST0026/impl.md`'s duplicate is not silently collapsed, it NEVER ENTERS THE PROSE POPULATION** -- no `design`/`impl`/`tasks` section is in the census at all, so the duplicate-heading class has exactly ONE reachable member and `ALTERED 2` is the whole of it, not the visible half. **What that uncovered is the finding: 192 STRANDED files carry 748 authored sections enumerated as FILES ONLY and never as PROSE.** **Swept all of it rather than sampled: 163 `design.md`/`impl.md`/`tasks.md` probed for their longest distinctive line in their own thread's canon -- 0 FOUND. `thread.json` has no `design`, `impl` or `tasks` field.** **So `LOST-PROSE 0` means _every section that HAS a destination reached it_ and says NOTHING about sections with none -- which are exactly the population disk-optional deletes.** **A number becoming HONEST is what made the shorthand built on it dangerous, which is the least intuitive thing established today.** **AND MY OWN CENSUS HEADER SAID SO AT LINE 61 -- _"the model does not hold (`design.md`, `impl.md`, `tasks.md`)"_ -- written by me, never connected to the gate.**

- **THE REWRITTEN GATE IS SAFE AND ic CHECKED WHICH RATHER THAN ASSUMED: `report()` increments `findings` and STRANDED goes through it -- `353 + 194 + 192 + 2 = 741` exactly, the reported total (`NORMALISED-PROSE` and `DECLARED-DROP` print WITHOUT counting).** **So exit 0 is UNREACHABLE while one file is stranded, and it is unreachable BECAUSE the gate reads the verdict rather than the counters. This morning's finding is what makes tonight's safe.** **STRANDED 192 is the number the deletion rests on and it is now the ONLY one.**

- **OPEN QUESTION 7 TO hv, THE BIGGEST IN THE MODEL, AND MY OWN RULE 1 IS THE MECHANISM: 163 files are classified TYPED DOCUMENTS and there is no field for any of them.** _A file is a typed doc OR an attachment, never both_ -- **so being classified typed is what EXCLUDES them from being carried, and `.md` is already in the declared set.** The classifier is right, the spec is right, and together they route 163 files into a hole. **RECOMMENDED: reclassify as ATTACHMENTS, on my own rule 2 -- a typed field earns its parsing because the model has FIELDS FOR WHAT COMES OUT; these are freeform prose under arbitrary headings and the model has none, so parsing them would discard structure into nothing, which is precisely how `## Related Steel Threads` became 52 rows of LOST-PROSE.** **The design fails safe meanwhile (5.1's fourth disposition reports them UNCLAIMED and never removes them) -- but a thread whose `design.md` can never be carried can never be fully dehydrated, and that is 55 of 56.** ic's boundary kept as theirs: **the migration is NOT wrong; STRANDED is the correct loud report for a file with no destination, and whether those 748 SHOULD have one is hv's call.**

- **`LOST-PROSE 332 -> 0` AND THE RUN THAT PROVED IT WAS BROKEN -- ic's prediction was right TO THE NUMBER over a tool emitting 40 errors.** I put `issue_canon_path()` at line 577, **between its caller at 350 and its caller at 761**; bash resolves at CALL time, so the artefact arm got `command not found`, an empty path, and reported all 40 issues UNCONVERTED while the prose arm below the definition worked perfectly. ic found it, proved it on a copy, published the move at `7da82684`. **AND MY VERIFICATION COULD NOT HAVE CAUGHT IT: `bash -n` proves PARSEABILITY, NOT RESOLVABILITY, and I printed its `ok` as though it were a check.** The day's class, in my verification step, on a fix for the day's class. **Denominator closed rather than the instance: all 16 functions confirmed defined above their first call, 0 after.** **ic's RULE IS THE SHARPEST OF THE DAY AND IT IS THEIRS: _a prediction being right is not evidence the run was sound._ What makes it hard is that they applied the fourth part to their OWN CORRECT PREDICTION -- the exact moment nobody applies it. Scoring the prediction alone would have certified my broken fix as a clean success, with the prediction right the whole time.** Honest state: `ALTERED 2, LOST-PROSE 0, UNCONVERTED 0, UNACCOUNTED 353 (out-of-model 0, not loss), STRANDED 192, findings 741 (was 1123), stderr clean, LIVENESS 56/56`. **STRANDED 192 is still the number the deletion rests on.**

- **`ALTERED 2` IS REAL AND IT IS A CLASS, MEASURED: a duplicate `## ` heading is NOT REPRESENTABLE by a heading-keyed model -- in MY census and in cc's INGEST alike.** Verified independently: the v2 source for issue 0059 carries `## Related` at **lines 84 and 160**. `section_text` cuts by NAME, so two sections sharing a heading cannot both be addressed. **Enumerated so it is bounded: 2 files of 526 -- `issues/CLOSED/0059` (`## Related`) and `intent/st/ST0026/impl.md` (`## Test Status`).** **OPEN AND NOT CLOSED BY ME: ic's run surfaced the FIRST; the second is a thread file on a different arm and whether it is REPORTED or SILENTLY COLLAPSED is not established.** If it collapses in ingest that is prose lost with no finding anywhere. **An unchecked second member is how a class gets recorded as an incident** -- handed to cc, thirty-second check, `84773c55`.

- **A STANDING RULE cc's TWO OBSERVATIONS HAVE NOW EARNED: A SUBJECT THAT CANNOT EXHIBIT THE DEFECT CANNOT CLEAR IT.** Twice today a finding was structurally unavailable from the real estate -- **the gitignored store no clone could hold, and the attachment ordering the path-sorting migrator makes unobservable no matter how many times anyone measures the live tree.** In both cases the thing that found it differed from the subject **by ACCIDENT.** That is the argument for fixtures that differ ON PURPOSE, and it is now evidence rather than a principle. **Plus a third variant of the day's class from cc's `seq` near-miss: not an instrument that never ran, but one that ran on a SUBSET CHOSEN BEFORE THE ANSWER WAS KNOWN** -- five legs green individually, nearly reported as landed, while the failing case sat outside the sample. **Their technique is the keeper: four lines spelled the same way read as one thing, so the check is COUNTED, NOT READ.**

- **THE GREEN HALF LANDED HALFWAY, AND THE BLOCKER WAS A SECOND DEFECT I HAD INTRODUCED WHILE FIXING THE FIRST.** ic held everything constant but the tool: **`LOST 384 -> 332`, exactly -52, the related arm entire, with `ALTERED` staying 0** -- so the fallthrough guard holds on the green subject too, which is the half the baize tree structurally could not show. **The issue arm did not move because two arms of MY tool built one path differently: line 329 `${id##*/}` -> `issues/0001.json` (correct); line 724 stripped the zeros -> `issues/1.json` (never written).** The strip was right for the old unpadded `.md` home and **outlived it, carried across by my own rewire an hour earlier.** **THE REASON IT HID IS THE GENERAL FACT: the artefact arm's SUCCESS and the prose arm's FAILURE land in different counters and nothing compares them** -- `LOST-PROSE 332` against `UNCONVERTED 1`, two verdicts about the same 40 files disagreeing in silence, printed in one output, neither wrong. **Fixed as `issue_canon_path()` with both arms calling it, because deleting the `sed` fixes the instance and one construction closes the class -- the FOURTH time today three of us have made that argument** (`is_terminal()`, `outside_any_project()`, `Project::classify`, this). **Driven: new form resolves 40 of 40, old form builds an ABSENT path.** `e4d7092d`.

- **A FOURTH BLIND-SPOT AXIS, AND THE ESTATE COULD NOT HAVE PRODUCED IT (cc).** The store read attachments back `ORDER BY path` while canon carried them in production order -- **the record survives, the prose survives, every scalar is right, and the ORDER is different.** Under D34 that is the property the extract rests on. **My arms are structurally blind: sections compare by (kind, id, heading) and hash independently, so two that swap places both still match -- sequence is not a byte in any section.** Recorded beside dc's two axes and cc's title-quoting one. **cc's caveat is the frightening half: it would have been INVISIBLE on this estate, because their migrator sorts by path so walk order and path order coincide. Only a fixture written in a different order could ever have caught it** -- the strongest argument yet for fixtures that differ from the subject on purpose. **And it is one class with my gitignore observation, the INVERSE of the day's other one: everything else reported green because it NEVER RAN; these reported green because they ran against a subject where the property held FOR A DIFFERENT REASON.** A green from a subject that cannot fail is not evidence and looks exactly like one that can.

- **`.sh` LANDED IN cc's CLASSIFIER: carried 71 -> 110, uncarried 237 -> 198, remaining = 196 `.tap` + a 2-file `.tsv` tail. One declared class with one reason plus a tail, which is what a burn-down should look like.** cc recorded the PRINCIPLE at the constant rather than the list, _"no tool can make this again vs a tool made this and can again"_, because a list without its rule is what someone extends by feel. **MY RULING ON THE MODE BIT: do NOT model it. Set `+x` at HYDRATION for the declared-executable extension** -- a RENDER decision is reversible and does not step onto the second-filesystem road, it is the same mechanical line that already decides carriage, and **the cost is asymmetric: a sourced library wrongly given `+x` is harmless; a parity instrument that will not run is broken at the moment someone reaches for it.** **cc's handling of their own reversal is the shape to keep: they recorded that the objection has NOT stopped being true, it is now OUTWEIGHED** -- which leaves the next reader able to re-open it when the balance moves.

- **FIRST POST-`attachments` BASELINE (ic, method A, subject pinned / binary NOT): `ALTERED 0` -- NOTHING THAT ARRIVED, ARRIVED CORRUPTED.** `census 1072 FILE / 2127 PROSE`, migrate rc=0, `conserved byte-identical 827, whitespace-normalised 288`, `compared 1614 of 2127`, `LIVENESS ok 56/56`. Everything else is about DESTINATIONS, not about bytes being wrong. **ic labelled it as not the gate-satisfying run because clause 3 refuses an unpinned subject and cc's binary was unpinned -- the clause working on a subject I wrote it for and they applied to the thing that MEASURES the estate.**

- **ic's SYMMETRY IS THE FINDING OF THE DAY AND IT IS ABOUT MY TOOL: this morning both counters could be driven to ZERO while the estate was NOT conserved; tonight both are pinned ABOVE zero while the estate IS conserved. Same root -- the counters were indexed to DESTINATIONS the model has moved away from.** A gate phrased on them fails OPEN in the morning and CLOSED at night and measures conservation in neither. **`LOST-PROSE 384` decomposed into exactly two causes and NEITHER was byte loss.** **332 issue sections: the arm looked for `issues/N.md` because `data-model.md` declared that home WHEN FILES WERE CANON. D01 REVERSED AND MY INSTRUMENT DID NOT FOLLOW IT** -- cc put the prose in `issues/N.json`'s `body`, disk-optional deliberately does not write `issues/N.md`, and under `realisation.md` that file would exist only for REALISED issues, **so a conservation number would have depended on the realisation set. A check that reads disk is measuring the projection, not the truth.** 40 of 40 bodies non-empty, 434,437 bytes, issue 0001 byte-identical to source. **52 `Related Steel Threads`: I checked whether the HEADING survived or the BYTES did, because those are different claims -- `ST0010/thread.json` has `related: null` and a `.body` opening `## Related Steel Threads` with both rows, byte-for-byte the rendered view. The prose reached the D28 catch-all; what is missing is the MODELLING. That is my own `CARRIED` bucket and the arm was calling it LOST.**

- **BOTH ARMS REWIRED TO ASK THE STORE (`93b35c14`), AND THE NAIVE FIX WOULD HAVE BEEN WORSE THAN THE BUG.** Falling straight through to `compare_prose` on an absent section reports **ALTERED-PROSE** -- the line that calls itself _"the number that means loss"_ -- **so deleting the LOST branches would have converted 384 destination-misses into 384 claims that the bytes arrived CORRUPTED.** Both arms now test for an empty extraction first and report LOST themselves. **DRIVEN before/after, claiming only the half the subject can reach:** ic's baize `migrated/` carries no `issues/*.json` and a zero-length `.body`, so it shows the RED half alone -- **findings 522 either way, issue-LOST 8, related-LOST 25, ALTERED unmoved at 103 (the specific check that the fallthrough did not launder LOST into ALTERED); what moved is the REASON.** Green half is ic's, on tonight's subject.

- **hv's DISK MODEL IS DESIGNED, EXECUTABLE, AND HAS SURVIVED THREE PEERS ATTACKING IT.** `intent/st/ST0056/realisation.md` + `parity/tools/realise_plan.sh` (dry-run, writes nothing). **The one thing in hv's brief that cannot be true as written: the store is GITIGNORED, so "disk is a sparse copy of the DB" is safe for VIEWS and fatal for CANON** -- a manifest governing canon would leave an unrealised artefact inside a gitignored database, absent from a fresh clone. **Ruling: `.intentfiles` governs views only; canon is never sparse.** Measured: canon 1,602,351 bytes for all 96 artefacts, views 2,704,783, **and the ONE live thread is 38% of all rendered markdown** -- sparse views buy nearly everything, complete canon costs nearly nothing. Recommended **canon relocated to `intent/.canon/` but still per-artefact** (one consolidated file is a merge-conflict generator with four writers). Two manifest regions (generated + pinned) because otherwise the first status change silently deletes a human's pin; **a malformed line REFUSES rather than skips**, the shape v2.19.0 paid for twice. **`intent wip` rejected on Highlander grounds -- `wip` already means a WP status, a thread status, `intent/wip.md` and every node's board; proposed `intent edit`, which `st edit` almost already is.**

- **THREE RULINGS FORCED OUT OF THE DESIGN BY PEERS, and each one was a hole rather than a refinement.** **(1) cc: an ATTACHMENT is not a VIEW and one policy for both discards work.** Authority follows AUTHORSHIP -- a view is authored in the model so divergence means the FILE is stale; an attachment is authored on disk so divergence means the STORE is stale. **`organize` NEVER resolves an attachment divergence: it reports and names both verbs, because deciding silently is choosing whose work to discard -- right most of the time and catastrophically wrong occasionally, the worst profile available.** Implementable exactly because `Project::classify` is now the single answer to what a file is. **(2) cc asked whether the ~237 uncarried files should enter the disposition record as `dropped`. RULED NO, on my own tool's founding sentence: _"still on disk is not a disposition"_.** `dropped` means canon is VERIFIED EMPTY; `uncarried` means still on disk, not in the model, still the only copy. **The disposition record is a LICENCE not an account, and admitting them silences the exact population the check exists to find -- ic's `--out-of-model` attack through a different door.** **(3) ic asked which estate the baseline compares, and the question found a gap in MY precondition: it has TWO consumers and I wrote it for one.** The v2 residue needs the pinned-census-vs-fresh-migration run (A) and that is the whole answer for it; **rendered views under disk-optional are NOT licensed by any conservation run** -- they are licensed per-file at the moment of the act by 5.1, which is strictly stronger. **A census of the pinned v2 estate against TODAY'S LIVE REPO answers no question anyone has: it folds the migration together with every commit since and cannot separate them.** Recorded at `2c502fd3`.

- **cc RETIRED A PRECONDITION RATHER THAN ACCEPT IT, and the reasoning generalises: _a normalisation that requires a future component to compensate is a scheduled defect_.** Declaring the issue view path turned `Issue.body`'s trim into a defect with a date on it, so `body` is now VERBATIM and there is nothing for the renderer to remember. **Census consequence: these bucket `conserved byte-identical`, not `whitespace-normalised` -- the softer bucket WAS the trim.** **INDEPENDENT CHECK ON cc's CLASSIFIER, exact: 240 non-`.md` non-`.json` files under `intent/st` (196 `.tap`, 39 `.sh`, 2 `.txt`, 2 `.tsv`, 1 `.DS_Store`) minus 2 `.txt` as attachments minus 1 gitignored `.DS_Store` = 237. So `classify` honours the ignore rules and D29 holds AT the classifier -- confirmed from outside cc's code.** On `doctor` printing 237 lines: **summarise with a COMPLETE denominator, every path in `--json`, never truncate -- a summary carrying a complete count is not a truncation; a `head -20` with an ellipsis is.**

- **THE WRITER IS CLOSED, VERIFIED OVER THE FULL DENOMINATOR -- AND MY OWN SWEEP HAD A DENOMINATOR PROBLEM I THEN CLOSED.** ic fixed `dispatch_ssot` at `1ff7f2c1` with a shared `outside_any_project()` rather than three correct call sites, proved both arms, and said plainly their proof covered that target only. **Re-swept all 80 at their commit -- rig ASSERTS the treatment is present and REFUSES rather than sweeps if it is not, because sweeping an untreated tree is ic's reverted-treatment failure with a bigger blast radius. `dispatch_ssot` is gone; 2 writers remain (`info_exit_code`, `session_hook_lockout`), both store-creators, zero estate files; `--lib` clean.** **THEN THE PROBLEM IN MY OWN PROTOCOL: it removed the store before EVERY target, so I had only ever measured the CREATE path.** `store-created=YES` does not mean harmless -- it means the test reaches the real root and what it does there depends on what it FINDS, and I had guaranteed it never found anything. **Closed by running each twice and scoring the second: both INERT against an existing store -- identical sha, mtime UNMOVED, estate clean. The destructive path is closed.** Residual, stated rather than shrugged at: **the store those two create holds `threads 56, issues 40, file_index 1218` -- a full 4 MB ingest of the ambient project, from a test run, into a GITIGNORED path -- so dc's `git status --porcelain` gate at `release:702` structurally cannot see it. A gate that reads git cannot prove anything about a writer that only writes ignored paths.**

- **dc: THE BATS SUITE DOES NOT WRITE THE ESTATE, AND THE REASON IS STRUCTURAL RATHER THAN LUCK.** Clean clone, no store, 1403 tests to completion, digest identical either side, store still ABSENT -- **and the instrument was proven first (a one-byte append moves the digest, `git checkout --` returns it exactly), because a clean digest and a broken one look identical.** `tests/lib/test_helper.bash:40` `setup()` mktemps outside the project and says why in a comment. **The convention the rust harness needed already existed in this estate, one language over.** **So my "the suite writes the estate" was over-general: it is the RUST harness, not "the suite".**

- **THE ONE-WORD FIX IS NOW A THREE-WAY RESULT AND IT IS THE STRONGEST DECISION-READY ITEM ON THE BOARD (dc, both arms, same clone).** `critic` into `GLOBAL_COMMANDS` at `bin/intent:55`: **ARM A as-is = 8 failures / 20 passes; ARM B = 0 failures / 28 passes.** All 8 failures are the version guard (`critic_report_format.bats` 219-225, `intent_critic.bats` 644, verbatim `error: this project declares Intent v3.0.0-dev, and this is Intent v2.19.0`) -- **they can only appear in a HOISTED tree, so hv's "100% green" run this morning was pre-hoist and is not in conflict.** **One refusal does three things at once: the pre-commit gate fails open, the eight tests that would have noticed go red, and nobody had run the suite since the hoist to see them.** Plus 4/4 that `st list`/`wp list` still refuse at rc=2, so the guard is not weakened. **NECESSARY AND NOT SUFFICIENT, and both halves must reach hv together: a repaired gate still reports only Elixir, because 0 of 6 shell rules and 0 of 7 rust rules carry a greppable proxy on a project that is 114 `.rs` + 57 `.sh` + 71 `bin/` scripts + 108 `.bats`.**

- **`cargo test` CONVERTS THE PROJECT IT RUNS IN, AND REPORTS 598 PASSED / 0 FAILED WHILE DOING IT. 3 of 80 targets, named, denominator closed.** dc caught it live (PID 67151, cwd the REAL root, store to `user_version 9` and all 40 issue json given `body` 79s after suite start); I reproduced it **in a fresh clone with no store**, which is what makes it a shipping defect rather than a workshop artefact -- **a consumer who runs the suite has their project converted, silently, by a green run.** Bisect: `info_exit_code` (store, no files), **`dispatch_ssot` (store + `ST0010/info.md` + `ST0015/info.md`)**, `session_hook_lockout` (store, no files). **Denominator closed rather than left open: `--test` selects only INTEGRATION targets, so a lib unit test could never have been SELECTED and would have reported clean -- `--lib` (74 passed) and `--doc` run separately, both clean.** **MECHANISM: `dispatch_ssot.rs` invokes the binary five times and sets `current_dir` on two; lines 21, 419 and 789 inherit the test process's cwd -- the crate root inside the real project -- so the root walk climbs into the real estate. Line 419 runs `intent <family>` BARE rather than `--help`, which is why it is the one that opens the store, runs the ladder rung and renders.** ic's independent measurement -- **43 files all at mtime 08:55:06, one second, one pass** -- is what a single bare command predicts and what a suite dribbling writes does not. **The fix is not three `.current_dir` calls: three call sites spelling one isolation with one wrong is how it got in. One helper that cannot be constructed without a root.** **AND I ALMOST PUBLISHED A FALSE GREEN GETTING THERE: my first control copied in a store ALREADY AT 9 -- converted by the process under investigation -- against clone source at 8, so the rung could not fire and my rig printed `the suite did NOT touch the ambient estate`, with zero schema refusals in 1320 lines that I could not separate from isolation. THE CONFOUND WAS IN THE CONTROL, WHICH IS THE THING NOBODY RE-EXAMINES** (ic's phrasing, better than mine). The unconfounded form removed the store entirely -- **no version can refuse a store that is not there**; I did not build a better comparison, I removed the thing that was answering instead of the subject.

- **WHY EXACTLY THOSE TWO FILES: THE DEPRECATION BLOCKQUOTE, AND IT IS ONE DEFECT WEARING THREE FACES (dc).** `grep -rln '^> \*\*Deprecated' intent/st/*/info.md` returns ST0010 and ST0015 and nothing else, out of 56 threads. **The renderer emits a blank line after a blockquote, the authored file already has one, and the round-trip leaves two** -- deterministic, content-triggered, reproducible on any estate holding that construct. **dc's line is the week's class stated exactly: the population was never evidence about the writer, only about the corpus** -- which is why three of us measured "two changed" against three different suspected causes and all got the same two. **Same blockquote is also behind the view-skew chase and the cancelled-plus-date contradiction: whatever renders `info.md` mishandles a LEADING BLOCKQUOTE, and that is the defect rather than any of the three symptoms.**

- **THE 40 ISSUE BODIES ARE CONSERVED BUT NOT BYTE-EXACT, AND THE WORD MATTERS BECAUSE IT IS THE EVIDENCE FOR DELETING 443,643 BYTES.** cc reported 40/40 byte-exact at 434,357 bytes; **I derived it independently (strip the frontmatter myself, sha256 the remainder) and got 0 byte-exact / 40 mismatched, EVERY ONE at delta -1: `\ No newline at end of file`.** My total is 434,396 -- **two derivations, same subject, disagreeing by 39 bytes, which is the only case where deriving twice pays.** Not damage: **this is `whitespace-normalised`, which my own census already separates from `byte-identical` via its `raw-sha`/`trimmed-sha` pair** (last full run: `conserved byte-identical 827, whitespace-normalised 288`). **"Byte-exact" is the stronger claim and it is the false one**, and it matters because ic's baseline will report 40 in the softer bucket and that will read as a NEW defect unless the record says otherwise. Round-tripping store -> disk -> store will keep re-normalising until the render re-emits the newline.

- **THE 53-FILE BLIND SPOT IN MY PROBE DOES NOT EXIST IN THE REAL INSTRUMENT, SO THE PROBE IS RETIRED RATHER THAN FIXED.** My probe asked _"is this 50-character phrase present in the store"_ and reported 53 files as UNTESTABLE because they are shorter than the phrase -- a miss and an untestable indistinguishable through it. **The question was never "is this phrase present", it was "did this content arrive", and those are different subjects** -- the week's own class, pointed at my own instrument. `conservation_check.sh` answers the real question at any length: it hashes every `## ` section, and `estate_census.sh:165-174` emits a PREAMBLE row for content that is under the `# Title` and in no section at all, _"not a section that failed to convert: a region"_. **Verified rather than assumed: 484 of 485 `.md` under `intent/st` carry at least one `## `, and the one that does not is covered by the preamble arm plus its FILE row.** Highlander -- one instrument, not two, and the cheap one goes.

- **THE 08:45:33 WRITE NOBODY OWNS, AND THE OWNERSHIP GAP UNDER IT.** All 40 `intent/issues/*.json` gained `body` and the store went to `user_version = 9` -- **nine exists ONLY in cc's uncommitted worktree, so a binary built from in-flight source ran the v2 conversion against the LIVE estate.** cc established what and not who. **Not me: three live-tree writes all session (`AGENTS.md` via `git checkout --`, this board, `conservation_check.sh`), every v3 call the stale schema-6 release binary or a pinned copy inside a clone, no `cargo`, no `debug` binary, no `upgrade`/`sync` against live.** **MY HYPOTHESIS IS THAT THE ANSWER TO "WHO" IS "NOBODY": `cargo test` did it via a test inheriting the real project root (issue 0025's class) -- the build was 95 seconds earlier, and THE EVENT LOG IS EMPTY, which rules out a human running a verb.** Driving it: clone at HEAD, real store copied in, sentinel digest over the store plus every file under `intent/issues` and `intent/st`, `cargo test --workspace`. Cannot reproduce the 8 -> 9 rung (HEAD is 8) but answers whether the suite touches the ambient estate at all. **THE GENERAL FACT IS THE FINDING, and it is one class in two artefacts: we share one working tree, so a half-finished contract is on everyone's PATH the moment someone compiles, and under D01-reversed the store is a single shared mutable file four nodes write. The whiteboard makes ownership explicit for every artefact EXCEPT the two that are now load-bearing.** Put to hv as needing a ruling.

- **TWO RULINGS FROM cc THAT SUPERSEDE MY FRAMING, RECORDED AS THEIRS.** **(1) `ROOT_FILES` is NOT attachments and I withdraw my framing -- the axis is AUTHORSHIP.** Attachments carry what an author wrote and no tool reproduces; `AGENTS.md` / `CLAUDE.md` / `usage-rules.md` are GENERATED, so the answer is that `agents generate` must exist, not that they get attached. **Carrying a generated file as an attachment gives one file two truths and puts the store in permanent skew against its own renderer -- rule 1 of my own attachment spec, one level up, and I did not see it.** cc owns the generator. **My alarm holds on the other side: until v3 can produce them, those three files are NOT SAFE TO DELETE, and the closing test is generate-and-compare-byte-for-byte -- with a guard I added, that the reference be taken from the file AS COMMITTED and taken NOW, because something in this tree emptied `AGENTS.md` once today without anyone owning it.** **(2) `completed` on a terminal state: cc's reason beats mine and I am recording theirs.** I argued the rule flags its own writer's output. **cc argued the alternative forces the migrator to DROP the real `2026-04-24` -- destroying a true fact about the world to satisfy a field's NAME, on the day we are counting bytes to prove we destroy nothing.** That argument survives even if mine were wrong. Ruled: the field records when a thread reached a TERMINAL state, both terminal states qualify, doctor's arm becomes terminal-aware via a new `is_terminal()` rather than spelling the pair at a second site -- **spelling a vocabulary twice is how the contradiction got in.** **ST0011 stays MINE to write, after cc lands schema 9; I will not race the ALTER.**

- **`AGENTS.md` WAS SITTING AT ZERO BYTES AND THE STORE CALLED IT `clean`.** Restored from HEAD (6041 bytes) before anything could commit the truncation. **The `file_index` row read `AGENTS.md | 0 | e3b0c442...b855 | clean` -- that sha is the SHA-256 of the EMPTY STRING, and `created_at` shows the store's FIRST sighting was already empty, so it recorded the damage as its baseline and returned a truthful, useless green.** Only zero-byte anomaly in 1214 indexed files; the other three zeros are two `.gitkeep`s and `events.jsonl`. **THE PART THAT IS A DESIGN HOLE, NOT AN ACCIDENT: v3 HAS NO GENERATOR FOR IT.** `agents generate` exits 2 (`known command that is not implemented yet`) and writes nothing; `ROOT_FILES` appears once, at `sync.rs:121`, on the READ side. Meanwhile `CLAUDE.md` states the contract as _"NEVER edit root AGENTS.md directly -- it is auto-generated"_. **So after the hoist AGENTS.md is a file the canon forbids hand-writing and the tool cannot produce, surviving in exactly one place: git history.** Under disk-optional that is the attachments gap one level up, and the three `ROOT_FILES` are not even in the model -- `scan` indexes a path, a size and a hash, no body. Raised with cc; I would rather be told it is out of scope than have us both assume the other had it. **Driving v2's `agents sync` against a clean clone REFUTED my prediction that v2 was the emptier:** exit 0, 6041 -> 10483 bytes, wrote a `.bak`, and no `.bak` exists in the live repo. Two by-products worth keeping: **v2 `agents sync` does NOT refuse a hoisted project, so "v2 refuses v3 at exit 2" is per-verb rather than universal**, and what it writes is wrong (project name becomes the DIRECTORY name, Rust build steps replaced by `bats -r tests/` and _"No build step required"_). Nobody may "fix" AGENTS.md by running v2.

- **hv: _"There are still 61 `00??.json` files in intent/issues. They do not belong there."_ MEASURED: THERE ARE 40. 0061 is the highest ID, not the count** -- 21 numbers in the range are unallocated gaps, which is exactly the read dc warned the index must not permit, now live rather than hypothetical. **And the half hv named is the load-bearing half.** That directory holds two generations: **40 flat `NNNN.json`**, v3 canon by construction (`project.rs:453` builds `issues/{number:04}.json`; `export.rs` emits the same path), 8 metadata fields, which are what `sync --to-store` reads back and what `export` carries to another machine -- **not clutter**; and **`OPEN/` + `CLOSED/`**, 40 v2 markdown bodies totalling **443,643 bytes measured**, which IS the residue -- **except the `issues` table has NO `body` column (schema read, not inferred), so those 443KB are the only copy of every issue's tags, repro, analysis and resolution.** **THE ACTUAL DEFECT hv IS LOOKING AT IS AN ASYMMETRY I CREATED: `intent/st` got the flatten (56 bare `ST####/` + `steel_threads.md`, zero buckets) and `intent/issues` did not.** One estate, two disk models, and hv is reading the difference as mess -- correctly.

- **`native/rust/target/release/intent` IS TWO SCHEMA GENERATIONS STALE and every store-touching call through it refuses and does nothing** -- it speaks 6, the live store is at 8, cc's worktree is at 9 (`target/debug/intent` is current). **I found it the expensive way and it is the lesson, not the fact:** I built a three-round rig to answer whether the render ACCRETES, pinned that binary precisely so the subject could not move under me, and scored **zero delta in all three rounds** -- which was the REFUSAL's shape, not idempotence. Had I read the delta column alone, the column the rig existed to read, I would have published _"the render is idempotent"_ off a command that never executed. **Passed to dc in time to catch their shim, whose `INTENT_V3_BIN` defaulted to exactly that binary** -- had hv installed it as written, every store-touching call machine-wide would have refused wearing the hoist's face. **Open and NOT mine to close: `ST0010/info.md` and `ST0015/info.md` each gained one blank line at 08:17 local, the minute cc's debug binary was built.** They are ic's _"two changed"_, and they are the only two threads whose body opens with a deprecation blockquote before the status list. **A change COUNT that stays at 2 across four runs cannot separate "a one-shot normalisation failing to stick" from "two files growing a line every render" -- only the per-round DELTA can, and under disk-optional an accreting render is unbounded.**

- **INTENT IS HOISTED. `0ec2ac79`, and the flat layout landed at `1af21f4e`.** **v2 gate over FILES 48/114 taken FIRST (one-shot); migrate 56 threads / 40 issues / 352 files exit 0; `sync --to-store`; v3 gate over STORE 48/114 -- BYTE-IDENTICAL, traversal quirks included.** **It REFUSED on the first attempt: a pre-versioning store at `intent/.cache/intent.db`. That path is GITIGNORED, so no clone can contain it and NO rehearsal could ever have found it** -- not an untested branch, **an input the test environment is structurally incapable of holding.** cc closed the class in a minute: a clone plus the other fourteen gitignored artefacts converts identically, so it was the only one. Old store moved (never deleted) to `~/.intent-prehoist-store-20260818T064735Z`. **Then the buckets: 192 authored files MOVED to flat paths, 194 superseded views REMOVED. My first control was BYTE EQUALITY and it refused all 194 -- the flat file is a RENDERED VIEW, not a copy.** The right control was the migrator's own declared-drop record: 62 dropped sections swept independently, ZERO undeclared. **That record now lives at `parity/hoist-dispositions.txt` because it was in ephemeral scratch and it is the evidence for deleting 194 files.**
- **hv HAS RULED THE DISK MODEL AND IT RETIRES STATUS-AS-DIRECTORY FOR GOOD: no buckets, disk becomes OPTIONAL, `intent/st` and `intent/issues` become an INDEX plus render-on-demand.** hv: _"I only want disk stuff when I need it, not all the time."_ **A WIP/DONE + phase scheme was proposed and retired before anyone built it -- we avoided building the rod.** **THE PRECONDITION IS ABSOLUTE AND MEASURABLE: the moment disk is optional, anything the store does not hold is destroyed by the first render.** Measured over 485 `.md` under `intent/st`: **380 in the store, 52 NOT, 53 UNTESTABLE by my probe (reported as unknown, never as a pass).** Plus **all 40 issue bodies**, which exist only at `intent/issues/CLOSED/NNNN/NNNN-slug.md`. **THE GREEN LIGHT IS BUILT AND IS NOT A JUDGEMENT -- BUT I WORDED THE GATE ON THE WRONG THING AND ic PROVED IT BY DRIVING, NOT BY ARGUING (2026-08-18, baize `a519398d`; I re-read their three trees rather than took the numbers).** Stripping every PROSE row from the census takes `LOST-PROSE` 33 to 0 and `ALTERED` 103 to 0; naming the 135 unowned paths `--out-of-model` takes `UNACCOUNTED` 135 to 0 -- **and the second is my own designed behaviour, so a gate phrased on that counter re-admits the denominator-certification the flag exists to refuse, through the WORDING instead of the code.** **Neither attack produced a green:** emptying the prose population made the tool LOUDER (522 findings -> 789, the reverse arm rising `ADDED` 0 -> 403 caught in C2 exactly what C1 could no longer see), and the out-of-model run held at exit 1 / 387. The two-directional design is what defeated it; a forward-only check would have handed back a clean `LOST-PROSE 0` with nothing else moving. **THE CORRECTED GATE, now in the tool's own header: exit 0 AND a printed `conservation: 0 finding(s)` line (its ABSENCE is a refusal, not a zero) AND the denominator (`compared 700 of 1211` vs `compared 0 of 0` is the whole difference, and two counters cannot see it) AND a PINNED subject the deletion is demonstrated to share.** **NOTHING MAY DELETE A FILE UNTIL THAT VERDICT COMES BACK GREEN AT FULL SCOPE.**
- **TASKED, ALL THREE, AND WHAT EACH IS WAITING ON.** **cc:** `attachments` (specced `1ffe8868`) + the issue `body` field -- the two halves of the precondition; plus `upgrade` printing _"content is unchanged"_ while two files change. **ic:** owns the conservation re-run that IS the green light; warned not to reconcile against any pre-hoist figure, since every one of them describes a tree that no longer exists. **dc:** the index (`steel_threads.md` exists, generated, newest-first -- it needs links and an issues equivalent). **STILL WITH hv AND UNANSWERED: the shim, the exit-code fix in `bin/**`, the moratorium (spent by its own terms -- it said 'until the hoist lands'), and ic's residue question.** **The gate has enforced NOTHING across all five languages since the hoist** -- `intent` on PATH is v2, v2 correctly refuses a v3 project at exit 2, and 2 is the fail-open code. Three correct behaviours composing into a green that measures nothing. **I caused it by hoisting.**
- **`attachments` IS SPECCED AND THE POPULATION DROVE THE DESIGN, NOT THE ASK.** 304 files under thread dirs are not one of the canonical five: **196 `.tap` baselines, 66 `.md`, 38 `.sh` (my own parity instruments), 2 `.txt`, 2 `.tsv`.** Attaching everything would put generated baselines and executable shell into the record of intent -- **and a store holding executables needs mode bits, binary payloads and a merge story, which is a VCS, and there is one a directory up.** **So: DECLARED EXTENSION** (`.md`, `.txt` -- 68 files here), decidable without inspecting content. **AND THE PROPERTY THAT MATTERS MORE THAN THE LINE: a file that does not attach must be NAMED by `doctor`, never silently skipped** -- the 52 were found by measurement and no surface ever said they were uncovered.
- **MY OWN DEFECTS TODAY, ALL FOUR IN GUARDS RATHER THAN IN WORK, AND THREE ARE ONE SHAPE.** A sha256 compared against its own 16-CHAR DISPLAY TRUNCATION -- it refused while printing two identical-looking values, **failing safe for the wrong reason, which is the hardest variant because only the reason is broken.** `shasum` embeds the filename, so relative and absolute runs digest differently -- same content, two numbers, nearly read as the subject moving. `conservation_check.sh` printing `SUBJECT unpinned @ /Users/matts` because field 3 is a REVISION when pinned and a PATH when not (fixed `31ff8204`). **And I moved the old store to `intent/.cache.pre-hoist-<stamp>`, which the ignore rule does not match, turning a hidden object into a visible artefact inside a modelled directory.** **Plus one of judgement: I told dc a standing hv instruction did not reach their case -- a peer ruling on when a user's rule applies, which is exactly what I had praised them for refusing twice.** Their answer is the keeper: _"the fact that you happened to be right is luck, not method."__

- **THE COUNTER-PATTERN, AND IT IS THE ONLY THING TODAY THAT WORKED BY DESIGN RATHER THAN BY BEING CAUGHT.** cc ran `conservation_check.sh` WITHOUT `--dispositions` and got **`ALTERED 112` -- the number that means loss** -- all `## Context for LLM`, every canon hash `e3b0c442...`, which is the SHA-256 of the empty string and therefore the signature of a deliberate removal. **They did not send me a loss figure of 112 because line 793 says what the run did NOT measure**: `DECLARED-DROP not measured -- no --dispositions given, so every removal above is counted as loss whether or not the migrator named it`. **Every defect the four of us found today was an instrument whose output could not tell you what it was failing to measure. Line 793 is the inverse: an instrument that names its own blind spot in the same breath as its verdict.** One line, cheap, and worth generalising deliberately rather than leaving as a habit I happened to have.

- **THE UNTESTED PATH AND THE IMPORTANT PATH ARE THE SAME PATH, AND THE PAIRING IS CAUSAL RATHER THAN COINCIDENTAL.** **An arm goes untested BECAUSE ordinary subjects do not reach it. The subject that reaches it first is therefore the unusual one by construction -- and the unusual subject is the one the instrument was built for.** ic's kill path only runs when something is interrupted, which is the whole property; my unpinned branch only runs on an estate with no `.CAPTURE` marker, which is Intent's own, the hoist subject. **Rarity is what makes the path untested AND what makes the subject important, so twice in one morning is what you would expect rather than a fluke.** **THE PRE-FLIGHT CHECK IT YIELDS, and this is why it is a rule rather than an observation: before trusting an instrument on the subject that matters, ask which of its branches that subject will be the FIRST to execute.** Answerable by reading, cheap, would have caught both of ours before either ran -- **and it is the only form of the question that does not require already knowing the defect.** **Four instruments, four nodes, one morning: ic's rig killed a subshell; my conservation truncated a path as a revision; dc's bats fixture read matts's personal gitconfig; cc's migrator had one test call and it was the detection.** Every defect worth keeping today was in an instrument, not in the thing it measured.

- **STANDARD RATIFIED, AND ic SHARPENED IT TWICE: AN AT ROW EARNS ITS GREEN FROM AN INSTRUMENT DEMONSTRATED **RED**, FROM A CRITERION NAMING THE **SUBJECT** IT WAS HANDED, AND FROM ONE NAMING THE **SHAPE OF THE INPUT** -- NOT ONLY THE VERDICT.** **THE THIRD CLAUSE IS ic's WITH MY SECOND INSTANCE, AND I NEARLY LOST IT IN THIS FOLD.** ic's: `store_live` and `store_nokey` are the same subject -- same estate, same trees, same comparator -- and drive different refusals because the JSON SHAPE differs, so naming the subject cannot distinguish them. **Mine, four hours earlier and I did not see the connection until they wrote theirs: my verbatim probe scored `acceptance.md` 0 of 3 and that is NOT loss** -- AC rows are parsed into `.criteria[].text` and `.tests[].note`, so a 70-char probe spanning a row CANNOT match by construction. **Same file, same subject, same instrument; SHAPE decides whether the probe can possibly succeed.** Two nodes, two instruments, one axis, and the second was not built to confirm the first. ic's rig killed a subshell, so its greens were never coupled to the property, **and a row minted from the DESIGN would have been satisfied by it** -- it interrupted, re-ran, compared, and said so. Their second point closes the other half: **a comparator that correctly reports IDENTICAL over a never-interrupted tree is behaving properly, so the verdict alone discriminates nothing.** **Given to cc as guidance, NOT written into the contract** -- that would be minting under the moratorium. **ic declined to let `rig_selftest.sh` in through my ratification and was right: cite the principle, leave the instrument to hv.**

- **THE DAY'S RULE, cc's AND MINE TOGETHER, AND IT HAS THREE MODES WITH THREE DIFFERENT REMEDIES.** cc: _how many independent paths reach the number_ -- **a figure with two derivations can be falsified by disagreement; a figure with one is a REPORT, and a report from a broken instrument is indistinguishable from one from a working instrument.** Five-for-five today. **MY RIDER, from cc's own counterexample: two derivations falsify ONLY IF THEY SHARE A SUBJECT** -- ic's 10 and cc's 10 were independent, agreed numerically, and were disjoint populations, so the agreement carried zero information and **arrived wearing corroboration.** **cc's THIRD MODE, whose mechanism is CHANNEL SHARING: one subject, one derivation, and the subject's FAILURE MODE is itself valid input** -- my liveness arm ran `st list 2>&1` and read the refusal, which NAMES thread ids, as proof of life. **Fixed by `2>/dev/null`: a rule about which stream may be evidence.** **REMEDIES: same-subject disagreement -> a second derivation. Different-subject agreement -> "what is this a count OF". Failure-as-input -> answer and error on separate channels.** Derivation-counting is the remedy for exactly one of the three. **AND cc's CAUSAL ORDER CORRECTS MINE: the corroboration is accepted FIRST and the reconciliation is built after** -- which is why closing arithmetic feels like a check when it is the story told once the evidence has already been assumed.

- **OPERATIONAL, ALL NODES: THE PUSH RESULT CARRIES NO INFORMATION ABOUT THE REMOTE, IN EITHER DIRECTION.** I first told cc and dc _"a rejection can be false, so do not trust it"_ -- **n=1 generalised into a rule about the remote and shipped to two nodes, which is the denominator error inside my own warning about denominators.** **ic then hit the same `incorrect old value provided` TWICE and both were REAL.** Mine was false; theirs were true; **there is nothing in the message to tell you which you have, so a rule about how to READ it cannot exist.** Corrected to both nodes at `38765791`. **The only thing that is evidence: `git ls-remote local refs/heads/main` then `git merge-base --is-ancestor <yours> <tip>`.** **Three of us reached for three checks and TWO WERE THE SAME OBJECT: dc a cached tracking ref, cc the exit code, ic a fetch -- and a fetch IS the tracking ref.** One instrument counted twice, which is the shape that makes a second opinion feel like corroboration. **ic's asymmetry, kept in their words: the failure mode is ACTION rather than inaction, and it is invisible because the retry succeeds and looks like the fix.**

- **RULED: WP-10 DOES NOT CLOSE UNTIL THE INTERRUPTION PROPERTY IS IN THE CONTRACT.** ic measured that **0 of 114 ACs cover the property hv gated the cutover on**, and 0 AT rows cite either instrument -- both could be deleted tomorrow and nothing would go red. **The gap is in the CONTRACT rather than in an instrument, which is why no instrument found it.** I ruled the half that is mine (the block) and did NOT mint the AC: the moratorium is hv's, and ic rightly declined to propose one while holding WP-10. **First row written when the moratorium lifts.** Escalated at `90438c62`.
- **RULED: A RULING GIVEN OVER THE LIVE CHANNEL IS NOT LANDED UNTIL IT IS IN THE ARTEFACT IT GOVERNS, AND THE NODE WHO GIVES IT LANDS IT -- NEVER THE NODE WITH THE INTEREST.** dc's board carried a wording of mine that `git log -S` finds in no commit and no inbox; **their board went AHEAD of the contract, and the one party who cannot check it is the one who remembered.** The general form binds all five nodes and is with hv to ratify. **It is the cost of the medium we are told to prefer: it writes nothing to disk.** I have been landing every ruling first and pinging second since.

- **RULED: `related` MUST NOT LAND BEFORE THE THREAD-LEVEL DEFERRAL. IT IS NOT A COUPLING, IT IS A REGRESSION WAITING BEHIND A FIX.** cc found it and I confirmed all three premises at source: **`views.rs:282` guards on `if !thread.related.is_empty()`; `related` is EMPTY on all 56 migrated threads; 55 v2 threads authored a `## Related Steel Threads` section.** **So the renderer's Related block has NEVER been emitted on a migrated estate -- a path declared, implemented and never exercised, which is ic's rule exactly.** Fixing `related` runs it for the first time **and collides with 52 carried authored sections in the same commit**, converting an invisible carry into a visible doubling. **The deferral is a PRECONDITION of `related`, not a companion to it**, and the constraint is written down so nobody lands it later without the memory. **This is the `related` work I argued FOR and cc corrected me on -- and the correction now has a second half: not only does only 58% of 126 rows fit `{id}: {note}`, but parsing them at all doubles 52 threads unless the deferral is already in.**
- **I ASKED hv TO RE-RULE D50, BECAUSE MY FRAMING PRODUCED THE RIGHT ANSWER TO THE WRONG QUESTION. `WpStatus` IS HELD.** cc found it going to write the match arm; I verified all three claims at source rather than relaying, having shipped an unverified peer claim once already this week. **I told hv _"ten work packages have no representable status"_ -- TRUE, a FLOOR, and it invited exactly _"Then add it, what's so hard"_. hv gave the correct answer to _we need one more value_; the QUESTION was wrong, not the ruling.** **Counted independently of cc by a different method (grepping `status:` out of Lamplight's `info.md` and normalising, not parsing rows) and got the same total: 34 rows unexpressible, cancelled 10, todo 5, pending 4, superseded 3, relocated 2, proposed 2 and eight singletons.** **So `Cancelled` fixes 10 and leaves 24 -- and `Superseded`, `Absorbed`, `Relocated`, `Dropped`, `Partial` are DISTINCT IDEAS with as good a claim to statehood as `Cancelled`. v2's WP status was FREE TEXT and an enum cannot grow to 29.**
- **ON THE BOUNCE: THE HOIST IS DONE AND THE WORK IS NOW SOMEONE ELSE'S UNTIL ic's NUMBER COMES BACK.** **What I own next: the 53 files my probe could not test (it needs a 50-char phrase and they are shorter -- a miss and an untestable are indistinguishable through it, so they need a different instrument);** ST0011's frontmatter/body disagreement, which is the estate's ONE genuine data error; **and the cancelled-plus-date pair, which I believe is the model over-constraining again -- `st cancel` is a real verb and 2 of 2 cancelled threads carry the date they were cancelled on, so the field named `completed` is doing the damage.** **doctor is 3 findings at 0.59s, down from 77 at 10.62s.** cc found the cause and it was NOT my hypothesis: view-skew is 8.7ms, **`sync::scan` was 7.40 SECONDS** -- `Ignored::for_root` differencing two full walks, the second unfiltered, descending **601,783 paths into `native/rust/target/`.** **A correctness property before a performance one: doctor's runtime varied with a machine-local build artefact the answer does not depend on.**

- **AC-10.8 (EGEST SYMMETRY) IS THE STRONGEST SELF-GRADING-PROOF PIECE ON THE BOARD AND IS NOT DRIVABLE YET.** Its verdict is `git diff --exit-code` against a tree git already holds -- a comparison git performs, with no judgement in it to be wrong about. `Facade::projection` is the only expression of the db -> disk direction and it is PRIVATE, reachable through `apply` and both sync directions but not standalone. **The migrator is what makes it drivable**, because Phase B must produce exactly that projection from a scan. cc will say when the door opens.

- **ic's RULE, STILL THE MOST LOAD-BEARING THING EITHER OF US WROTE: a field a test EXECUTES gets measured; prose drifts because nothing reads it back.** The register's `observed` column was 4 wrong of 8 checked when first executed -- including `at na` carrying v3's own token in the column that records v2, which would have certified a live defect as parity. Full incident in `.history/`.

- **THE CLASS IS TEN AND HAS A SIBLING CLASS, AND `parity.md` IS ITS HOME, NOT THIS BOARD.** Ten variants of _the measurement's subject and the report's subject differ_; the sibling class is _a true answer that has stopped discriminating_, whose detection question is **"what OTHER state would produce this same output?"** Members, remedy (cc's opposed-enumerator equality) and the third axis (dc: a REMEDY whose subject is not the report's) are all in `parity.md`.

## THE MODEL, in case everything else is lost

1. **The intentdb is the durable SSOT. Everything else is a secondary artefact. Nothing on disk is truth.**
2. All of `intentsvcs` works FROM the db; sync runs both ways.
3. The typed Rust API is the ONLY door in, so conformance is **by construction**.
4. Re-creation from an extract is a **capability**, not a licence to treat the db as disposable.
5. Ingest passes the **hard gate** of the intentsvcs API; the gate does the work, not the file format.
6. **Migrations are normal.** "No DB migrations, ever" is DELETED and was never hv's constraint.
7. The real standing requirement is **platform and data-model openness** (AC-02.6): always a 1-1 db-entity-to-`.json`/`.md` mapping, lossless, usable elsewhere. **That is what bidirectional sync is FOR.**
8. **(D34) The committed extract is the INTERCHANGE; the DB is per-machine truth and is NEVER committed.** Authority is not bidirectional just because transport is. **So AC-02.6 is the durability mechanism** -- a field that does not round-trip is data loss at the clone boundary, not a gap -- and **`event_log` is the only table that is both durable truth AND not reconstructible from the files**, which makes `events.jsonl` a precondition of the model rather than a WP-04 detail.

## TODO

- **AFTER THE HOIST -- THE ISSUE BODY HAS NO HOME (cc, `db1f9501`).** Measured source-side at `42fb5269`: **503 sections and 658,676 bytes across 61 issues, 30 distinct headings of which 21 appear EXACTLY ONCE.** Seven are the template; **the tail is the load-bearing half, same as `Thread.body`.** `Issue` has no body field, so every byte is dropped today. **cc deliberately did NOT add one -- a model change is mine, and inventing a field while writing a parser is how the preamble nearly went into `body`.** Same treatment as the preamble: spec first, price before and after, own field. **cc will not reconcile 641,893 on their board against 658,676 measured, and is right not to -- different boundary rules, earlier rule not in hand, stated as unestablished.** That is the 78-byte discipline applied without being asked.
- **AFTER THE HOIST -- A RESIDUE CLASS FOR SOURCE DISAGREEMENT (cc).** An issue whose BUCKET disagrees with its FRONTMATTER has no declared class: the status parses, the file classifies, nothing is unparseable. **`residue_class_check.sh` REFUSED cc's commit for emitting `model-inconsistent` undeclared -- my two-directional arm doing its job on a peer, which is the first time it has fired on anyone but me.** cc removed the finding rather than reaching for a declared-but-wrong class, and that is the right call: **`unknown-status` on a status v2 accepts sends an operator to fix a correct value, which is worse than silence.** Measured first -- bucket and frontmatter agree on all 61, so nothing is lost today. **cc ruled the precedence rather than deferring it, and the reason is right: the frontmatter is what an AUTHOR wrote, the directory is where a TOOL put it.**

## Verification kit

- `$CLAUDE_JOB_DIR/tmp/v3fix` is a **migrated** v3 fixture with its own `git init`. Recipe: `config.json` at 3.0.0, `st new`, prose into `thread.json` (**never** a generated view), `sync`.
- **`$CLAUDE_JOB_DIR/tmp/hookprobe` is the 0043 rig; it covers ALL THREE Claude Code hook events and needs NO migrated project and NO interactive session.** Per arm: a dir, a `settings.json` wiring `UserPromptSubmit` (matcher `""`) to one command, then `claude -p "<prompt>" --settings ./settings.json`. Arms are hook scripts exiting 0/1/2 plus one wired to the real v3 binary. **Assert on the OUTPUT, never the exit code -- a BLOCKED run exits 0**, which is the same silent shape the probe was built to find. **And keep the exit-1 arm**: an assertion that `2` blocks passes equally on a build where every code blocks, so only the arm that does NOT block ties the failure to the change that caused it.
- **MEASURE AT A PINNED SHA, AND NAME THE COMMIT IN THE FINDING.** `HEAD` is a POINTER, so a claim about it is a claim about whatever it points at WHEN READ -- pinning protects the measurement, only naming the commit protects the report. **And `rust` is a pointer at a tree the same way, which is the whole of 0049.** `git archive` the sha, build in a separate `CARGO_TARGET_DIR`. Incidents in `.history/`.
- **ASK HOW MANY WRITERS AN ARTEFACT HAS BEFORE RULING ON IT** (cc). _"A ruling enforced on one of two writers is enforced on neither RELIABLY"_ -- **the uncovered writer wins whenever it runs LAST, so it is a RACE, which passes in the suite and fails in the estate.** `todo.md` has two doors and all five tests reached it through the other one. Incident in `.history/`.
- **Falsify before flipping.** Perturb the artefact the test asserts against and watch the right subset go red.
- **COUNT, STOP, READ THE DIFFERENCE, THEN ARCHIVE -- IN THAT ORDER, IN SEPARATE COMMANDS.** I ran the both-sided count check and the archive in ONE command and archived an unread ic entry; it printed `ic live=5` against my four named stamps AFTER the archive had run. **A check whose result arrives after the act it was meant to authorise is not a check.** Recovered from `.history/` and nothing was lost. The check caught unread entries FIVE times today and failed only when I let it run too late.
- **SIGNATURE AND SCOPE FAIL IN OPPOSITE DIRECTIONS AND NEITHER IMPLIES THE OTHER** (dc, 2026-08-16, correcting my verification of their header guard). Signatures answer _does it fire on the right SHAPE_; scope answers _does it fire on the right FILES_. **I cleared that guard on signatures alone and reported it as verified**: `intent/whiteboard/*/wip.md` matches 21 files, SIXTEEN of them archived boards, so it would have refused the protocol's own housekeeping. Both `case` globs and git pathspecs cross `/`. **The identical trap had already bitten the clock guard's port**, which makes it a class.
- **THE DAY'S MEASUREMENT CLASS AND ITS INSTRUMENT RULES NOW LIVE IN `parity.md`, NOT HERE** -- nine variants, three properties, the calibration and chokepoint rules, the annotated-artefact bias, and the sibling class. Four entries that sat here (guard-proven-only-by-its-positive-case, a-check's-error-message, the-ok-line-must-be-conditional, put-the-population-on-the-ok-line) are folded into it verbatim and archived. **Read `parity.md` -> Measurement rules before building any instrument.** Keeping a second copy here is the divergent-copy defect the canon itself warns about.
- **SHARPENED 2026-08-16, by L2 refusing a row I wrote: the three states answer TWO questions, not one. `to-write` = the test is UNWRITTEN. `red` = the test EXISTS and does not pass. NEITHER means "the criterion is unmet" -- a criterion can be false with no instrument pointed at it, which is exactly AC-10.9.** I set that row `red` because issue 0038 makes its criterion measurably false, and L2 refused it because the cited file does not exist. **The refusal was right.** My partial-coverage rule below was always about a test that EXISTS and under-covers, and it does not generalise to any unmet criterion; reading it that way produced a red row citing a file that is not there. The exempt-state hazard on such a row is covered by `stale_at_check.sh` firing the moment the file appears.
- **Refuse at partial coverage. CORRECTED 2026-08-15: hold the AT at `red` WITH AN EXPLICIT NOTE, not at `to-write`.** I had this the other way round on the grounds that `red` lies about a passing suite. It does not: **the AT's status is the CRITERION's state, not the file's**, and `to-write` is the worse lie because it claims no file exists when one does. Write the note so anyone running the suite and seeing green knows why the board is red. AT-00.5 is the precedent for the refusal; AT-06.4 / AT-06.7 are the corrected form.
- **A second AT row at `to-write` does NOT hold a gate open** (issue 0032): `ac gate` satisfies on the FIRST green AT covering the AC. Until that ANDs, the covering AT itself has to come off green.
- **The D34/D35 numbers are in canon, not here** -- `design.md` D34 carries the FTS ratios, the GitHub ceiling, the pack sizes and the `dbstat` split; D35 carries the WAL measurement and the rejected `.sql` dump. **Cite them; do not re-derive them.**

## Watch-outs

Measurement rules live in `intent/st/ST0056/parity.md`. What follows is operational to this node.

- **`intent at red|green|na` DESTROYS THE ROW'S NOTE (issue 0033) -- COMMIT BEFORE ANY STATUS CHANGE, then restore the note by hand.** The note is where the discriminating case is written down, so losing it loses the reason the row is not green. Full incident in `.history/`.
- **NEVER ATTRIBUTE UNCOMMITTED WORK BY READING THE TREE. TWICE IN ONE DAY, IN OPPOSITE DIRECTIONS** (dc, 2026-08-16). I read dc's uncommitted `testkit` and credited it to cc; then read cc's uncommitted `install.rs` and credited it to dc, and TOLD dc to "build on" a module cc had already finished. **The near-harm was two nodes writing one file, which nothing in this estate would have caught until a conflict.** dc's diagnosis is the one on my own board: **in a four-session shared clone an untracked file has NO AUTHOR. Reading the tree is right for WHAT and structurally silent on WHO.** `git log` is the only surface carrying authorship and neither of us reached for it. **Before naming an owner: `git log -1 -- <path>`, or ask. A modified file in a shared clone is evidence of work, never of whose.**
- **MY PINNED-EXTRACT DISCIPLINE IS WRONG FOR INSTALL-DEPENDENT COMMANDS, and it produced a false rc=1 today.** `install.rs` resolves the install home from `current_exe()` by walking up to a marker, so **a binary built into a `CARGO_TARGET_DIR` OUTSIDE the extract cannot locate its install** and `claude hook` answers `1 -- cannot locate the Intent install` where dc measured `0`. Not a disagreement: my rig. **Two disciplines and I must pick: an external target dir is right for TESTS (isolation from peers) and wrong for anything reading its own install** -- for those, use the repo's own build or build into the extract's own `target/`.
- **DO NOT PUT v3 ON PATH. THIS SESSION IS ALIVE ONLY BECAUSE `intent` STILL RESOLVES TO v2** (dc, 2026-08-16, and it applies to all four nodes right now). 0043's trigger is **PATH, not migration**: `claude` is unimplemented as a FAMILY, so v3 refuses before it reads project state -- measured `rc=2` in a migrated project, in an unmigrated v2 project, and **outside any Intent project at all.** `brew install` does it unasked, at PATH position 1. **The moment v3 lands on the PATH of a shell a Claude Code session runs in, that session stops accepting prompts and cannot be recovered from inside itself.** Always invoke the v3 build by explicit path.
- **AND MY OWN CONFIRMATION ALREADY CONTAINED THAT PROOF AND I READ PAST IT.** ARMV3 -- the end-to-end arm -- ran in a throwaway dir with **no `intent/.config/config.json` on any ancestor**, and blocked. So I executed the decisive arm with the "migrated project" precondition ABSENT and reported it as end-to-end confirmation of a claim about migrated projects. **The result was right and the framing around it was never tested by anything** -- nothing failed, so nothing objected. **A passing measurement does not audit the scope you claim for it**, and dc found the widening by measuring the thing I had already measured and not looked at.

- **The four things that are NOT exceptions**, because each has already been used by one of us to smuggle a clock back in: "only for a test fixture" (guard is being widened to `tests/`); "only reading, not writing" (there is no read that stays a read); **"but the value came FROM the database"** (the read and the write are two acts with a gap -- better provenance is not the absence of a confection); "only a label" (then it needn't be a time).
- **Refusing to settle by inference is NOT a resting state.** It obliges you to go and get the answer. A question parked across three rulings is a decision made by default -- that is how D01 stayed wrong through four hv statements of it.
- **When you inherit a rule with a rationale attached, the rationale is the part most likely to be wrong** -- nobody re-derives it. Twice measured now: "no DB migrations ever" was a consequence defended as a requirement, and the binary-dirtiness case against committing the DB was true-sounding and not the reason.
- **Absence of a mechanism's NAME is not absence of the mechanism -- and it is not presence of it either.** I grepped `hooksPath` and filed a false `high`; I grepped `from: &[]` against a positional constructor, got zero, and nearly filed a refutation of a real finding.
- **A clean result is only as current as the needle set that produced it, and a needle set has a timestamp** (dc). **A structured query is a needle like any other and reports on the subtree it TRAVERSED** (ic) -- and it feels exhaustive in a way a grep never does, so it is trusted harder than it has earned.
- **A rule true in its own scope is the easiest to over-apply.** Check the SHAPE matches the shape the rule was measured on.
- **Verify at HEAD (`git ls-tree`), never on disk -- better, clone fresh and build.** **A green suite is evidence about the tree you HAVE, never the tree you PUSHED.**
- **AN UNTRACKED FILE HAS NO AUTHOR** (dc's correction of me): you cannot attribute it by reading the tree, in either direction. Pair it with **`git grep` reads the INDEX, not the worktree**. Incidents in `.history/`.
- **AND THE OPPOSITE HALF, measured 2026-08-15: `git grep` reads the INDEX, not the worktree.** Searching for hv's leaked identifiers returned **ZERO** and I nearly reported the estate clean; plain `grep` over the same tree found 69 in `src/` alone plus all six sites hv was pointing at, because they are in peers' uncommitted live edits. **The two rules are opposite halves and the QUESTION picks the tool**: "what did we ship?" is HEAD, "what is a peer writing right now?" is the worktree. In a four-session shared tree the second question is the common one, and the tool's default answers the first.
- **ARCHIVE BY NAMING THE STAMPS. NEVER `sed -n '/^## (/,$p'`. THE GREEDY RANGE IS THE BUG.** FIVE times today the fold swept an entry I had never read (dc 13:33Z, ic 14:22Z, cc 14:30Z, then cc 15:14Z + dc 15:13Z) -- and **the last two happened AFTER I wrote the rule "archive only up to a stamp you have recorded as read" and while I believed I was obeying it.** I then implemented the exact opposite: a range from the first `## (` to EOF, which archives _everything present_ rather than _what I have read_. **The prose rule was right and the mechanism contradicted it, so the rule bought nothing.** dc's 15:13Z carried hv's two rulings and cc's 15:14Z carried a whole defect class -- I read both only because I printed the stamps as the script ran and did not recognise them. **A control refuses; a resolution reminds. This one had had three resolutions.**
- **Why it stays invisible: a fold CAPTURES into `.history/` rather than deleting**, so nothing is ever lost and no error is ever raised. The only signal is counting entry headings afterwards -- and by then you have answered the wrong message.
- **THE WORST FORM OF THE HALF-A-MOVE, measured 2026-08-15 on cc's tree: AN UNTRACKED WORKSPACE MEMBER.** `native/rust/Cargo.toml` listed `crates/testkit` while the directory was `??`. **`git commit --only` does not stage an untracked path**, so naming the manifests lands a workspace declaring a member absent from HEAD. **This does not break a TARGET, it breaks cargo's ability to load the workspace** -- build, test, fmt and clippy all fail identically before reaching any code, **so the command you would run to diagnose it is the command that cannot run.** The author's worktree stays green because the directory is on their disk. **Every prior instance of this class cost one suite; this one costs every cargo invocation in every clone.** `git add` the new directory BEFORE naming the manifests.
- **`git commit --only <paths>`, never `-A`** -- and **a move is TWO facts**: naming only the new paths commits half a move, silently, with a green working tree. **`--only` also never CLEARS the index** (measured, issue 0028): a stale entry survives every later commit, is invisible to `git diff HEAD`, and shows only as the left `M` of `MM`. **Read `git status --short`, not the diff.** When peers are live, scope any `git reset` to your own paths.
- **DO NOT PUSH TO `upstream`. hv, 2026-08-16: the CI/CD budget is spent. `local` only.** `git push local main` yes; `git push upstream main` NO -- every push there triggers the GitHub Actions matrix, which is the thing that ran out. **Announced to all four nodes, because all four had it in their commit habits.** **`int prepush` will NOT save you**: its gate is about whether `native/` moved, not about which remote, so it passes an `upstream` push exactly as before. **This is a discipline and not a control** -- the natural home for one is a `prepush` arm refusing `upstream` unless overridden, which is dc's lane and is not built. Nothing needs rewinding: the two remotes were in sync at `99c66e8b` and the divergence starts from there.
- **THIS REPOSITORY IS PUBLIC** and hv has ratified the whiteboards as part of the public record, intentionally. The `-A` hazard is therefore a _publication_ hazard into a history nobody can rewrite.
- **Never mutate `bin/**` or `tests/**` in place** -- `~/.local/bin/intent` symlinks here and the BATS suite reads the live tree. Sacrificial worktrees only.
- **Never `head` a list you are counting**; a frequency-sorted list puts the RARE value last. **Read `$?` before anything else touches it** -- but the pipe trap is the pipe trap; `A || echo "$?"` reports A correctly.
- **NEVER `head` OR `tail` A LIST YOU ARE COUNTING, AND NEVER READ `$?` THROUGH A PIPELINE** -- `$?` is the LAST STAGE. **Six firings across three nodes in one day, the most-repeated defect on this thread**, and ic's diagnosis is why knowing it does not help: the pipe is added for DISPLAY, after the command already worked, so the edit that breaks it is cosmetic and arrives last. Capture the rc directly when a write depends on it.
- **This shell is zsh**: no word-splitting of unquoted parameters.
- **BACKTICKS NEVER GO INSIDE A DOUBLE-QUOTED `-m` BODY.** They are command substitution: the words are EXECUTED and then silently deleted from the message. **Control, not caution: single-quote the body, or `git commit -F <file>`.** Demonstrated on myself at `0e84acc9` -- a pushed, public commit reading _"the state tag is , not "_, in the sentence whose whole job was naming which of two tags won -- **inside the commit replying to ic's report of this exact defect.** Not amended; rewriting public history costs more than a wrong log line.
- **A detector's false-positive set is part of the measurement, and you find it by READING the hits, not counting them.** Chasing the above: first detector said 23 of 60 commits scarred, every node affected, and I was one step from publishing it. Almost all were ` .treeindex` / ` .backup/` -- **a space before a dot is normal when the next token is a dotfile.** Re-measured with a pattern dotfiles cannot trigger: **exactly one, mine.** Same shape as the `hooksPath` claim I filed at `high` and withdrew; the only difference was checking before publishing.
- **The live channel does not survive a peer's restart; the inbox does.** Durable copy first, live ping as accelerant.

## Decisions

- (2026-08-17) **hv SHARPENED THE RULING AND IT SUPERSEDES BOTH vc's AND ic's FORMULATIONS. Verbatim: _"Issues are (generally) for other, external users of Intent, using a released version. We can just fix things inline as we find them."_** The test is not "does it need a decision" (ic) or "does it cross a node boundary" (vc). **It is WHO reported it and AGAINST WHAT: an external user against a released version is an issue; anything we find building v3 is just work.** Fix inline; if it needs documenting it goes on the WP ladder. **Consequence: 21 of the 22 open rows carry `reporter: matts` and every one was found by us against unreleased v3 or against v2 internals WP-04 replaces -- so under this rule essentially none of them is an issue.** That includes 0058, which passes ic's decision test cleanly. **DO NOT MASS-CLOSE: the findings are real (0033, 0035, 0044, 0045, 0055, 0058) and the tracker is the wrong container, not the content.** Put to hv as ONE decision: convert the live findings onto the ST0056 ladder, clear the tracker of everything that is not an external report against a release.
- (2026-08-17) **AN ISSUE FIXED-AND-LEFT-OPEN AND AN ISSUE NOBODY HAS TOUCHED ARE THE SAME OPEN ROW** (ic, sweeping the tracker under hv's first ruling; 0060 was already fixed, with `render.rs:1833` carrying a comment citing it, and only a measurement could tell). **The sibling class arriving on the tracker itself** -- same shape as dc's unsatisfiable close condition. It is the strongest argument available for hv's ruling: the tracker was not merely redundant with the channel, it was accumulating rows whose state nobody could read without re-driving the code.

- (2026-08-17) **hv STANDING RULING, VERBATIM: _"The honest diagnosis is that the issue file is duplicating the agent channel. Exactly. So stop doing that, please."_ STOP FILING ISSUES THAT DUPLICATE THE AGENT CHANNEL.** Given after vc reported the rate: **17 issues filed today against 10 closed, the tracker up 39% in one day.** The "fix under issue" practice came from v2.19.0, when there was no steel thread and the issue WAS the unit of work. Under ST0056 there is a 14-WP thread with a 112-row acceptance contract, so the tracker had become a second work-tracking system running beside the one the close gate actually scores. **The practice from here: fix it inline and put the reasoning in the commit message; if it crosses a node boundary, say so on the agent channel; if it describes what "done" means, it is an AC row, not an issue.** A finding does not need a file to be real -- the commit is the record and the channel is the routing.

**CONSOLIDATED 2026-08-17 into five families; nineteen bullets, full text in `.history/20260817/wip.md`.** Folded because the list had stopped changing behaviour at the margin -- cc's move, taken. The families are what a next session needs; the instances are what the history is for.

- (2026-08-17) **The class is at NINE and the count is deliberately not moving for properties.** cc's `instant` is the ninth axis and it BREAKS the asymmetry -- reading too early returns the pre-state, which equals the post-state exactly on the cases where nothing moved, so a wrong-instant error converts a movement-detector into a NULL-detector. ic's handoff (0053) and licence-expiry findings are recorded as PROPERTIES of the class, not variants.
- (2026-08-17) **dc's line is the class's third property and the one that changes how work is done: NOT ONE OF THE NINE WAS FOUND BY READING.** Every one needed a mutant, a witness driving the moving case, or a peer measuring independently and getting a different number. **Review does not detect this class** -- guarded, at dc's ask, so it is not read as licensing skipped review.
- (2026-08-17) **A SIBLING CLASS, distinct from the above: a defect written down AS the specification passes every consistency check.** Four members now. The pair that matters is a correct NOTE beside an incorrect ASSERTION about one command at one time -- **the assertion wins because it RUNS**, and nothing anywhere compares a row's prose to the assertion covering it.
- (2026-08-17) **The parity contract's headline carries a silent quantifier**: "v2's suite cannot tell the difference" means _in the ways it looks_. All four mechanisms missed `wp show` -- including `wp_commands.bats:565`, **the nominated certifier**, which asserts title and objective and never the status line. Coarse coverage hole is 2 of 62 (`agents template`, `agents sync`); **the fine hole is ungreppable and rests on 60 unrecorded human judgements, the only checked one wrong.**
- (2026-08-17) **My own calibration rule is insufficient, and ic's form supersedes it**: the checker is written by the same head as the needle, so a check is not independent unless its SOURCE is the corpus. Enumerate the spellings from the corpus, then calibrate one per form -- never from recall. Both ic and I manufactured false zeros this way, in the direction that reads as serious.
- (2026-08-17) **State the population, not just the result.** A scope that is not stated is a scope nobody can disagree with; a reader disagreeing with a stated scope is the control working.
- (2026-08-17) **A PROXY IS ONLY WORTH HAVING WHEN THE POPULATION IS TOO LARGE TO STATE.** Fifty call sites felt mechanical; four is readable, and reaching for the proxy over four is how three false positives got in. **The felt-need was the error rather than the rule.** Sibling: **count where a defect MANIFESTS, not where it originates** -- and when the sites share no chokepoint, building one is a cheaper repair than an enumeration nobody can trust.
- (2026-08-17) **A defect masking its own detector is a real shape, not a curiosity** (cc): `ac_rescope`'s hand-written from-state check refused everything, which is exactly what kept the walk that should have caught it green. **So a fix and the test that would have found it have to land together**, because the test cannot be shown to work until the defect is gone.
- (2026-08-17) **THE `instant` AXIS HAS A FIXTURE HALF AND AN INSTRUMENT HALF AND THEY ARE ONE RULE: arrange for the two instants to DIFFER, then read them.** cc read the state before the verb ran; ic seeded an entity at the verb's target. Both make a movement-measurement into a null-measurement. **ic's is the worse direction -- it MANUFACTURED a parity break against v3 that did not exist**, which files as a defect in the binary rather than in the fixture.
- (2026-08-17) **EXECUTION IS THE ONLY FORM OF DOCUMENTATION THAT CAN BE WRONG OUT LOUD** (ic's, and the deepest thing on this thread). The case for a structured field was one-requirement-one-statement; **the better case is that the field forces the value to be MEASURED, and measuring is what found the four wrong entries.** Generalised: **every defect found on this thread today was in something nothing read back** -- a doc comment, a note, a prose column, a restore that is silent when it works, a seal nobody parses.

- (2026-08-16) **hv GRANTED A STANDING AUTHORISATION -- "go with your recs. Always." -- AND ITS BOUNDARY IS RECORDED HERE BECAUSE A GRANT NOBODY BOUNDED IS A GRANT SOMEBODY LATER OVER-READS.** **What it covers**: contract-shape calls, spec corrections, ratifying or folding a class, severity, AC/AT wording, and adopting my own recommendation where I have already stated the reasoning and the measurement. **What it does NOT cover, and I am stating this against my own interest so it cannot be quietly widened**: anything irreversible or outward-facing -- the release, pushing `upstream`, publication, running WP-10 Phase B against the real estate, or mutating `bin/**` / `tests/**`. **cc drew this exact line unprompted on the same day and their wording is the one to keep: "hv's 'work amongst yourselves' authorises work; it does not authorise an irreversible rewrite of their own project's canon."** A standing grant lowers the cost of a decision; it does not change which decisions are mine. **And it raises the bar on stating the reasoning, not lowers it** -- when nobody is going to say no, the written argument is the only thing left doing the checking.

- **FAMILY 1 -- THE CLASS OF THE DAY: "THE THING I HAVE IS NARROWER THAN THE THING I SAID." (cc's wording, which replaced mine.)** Eight instances between four nodes in one day, in eight materials. **The tell is uniform and it is in the REPORTING step, not the measuring one: the measurement was sound and the sentence carrying it was wider.** Sub-forms, each earned: a CLASSIFICATION standing in for a measurement (`keep`/`as-observed` read as v3's behaviour); a MEASUREMENT standing in for a wider one (one of a criterion's two conditions reported as the criterion); **an agreement asserted from a comparison never run** (I wrote "the four homes do not disagree; I checked the predicates" having checked only the RETIRE predicates -- they differed by one member, and it reached the canon); **a number cited from memory wearing a citation's clothes** (ic's, the better rule: _a sentence citing a NUMBER names where it was read_, and **a citation naming a FILE is not a citation, it is a promise that a file exists**); and **a LINE NUMBER cited from a peer's uncommitted tree** -- my sharpening of ic's, one level down. Also: **two boards agreeing is not two confirmations when one is reporting the other's claim.**
- **FAMILY 2 -- A CLEAN RESULT AND A BROKEN INSTRUMENT ARE THE SAME OUTPUT, SO EVERY SWEEP OWES A RUN IN WHICH IT IS KNOWN TO FIRE.** My differential reported a perfectly clean surface while `info` was demonstrably swallowing arguments, because `Location: <cwd>` made every comparison read "refused". **dc's completion is the better half and it is asymmetric: a wrong zero certifies absence, a wrong MAXIMUM certifies catastrophe -- and the second is far more persuasive, because it looks like diligence rewarded.** Nobody re-checks an instrument that has just found something big. Corollaries: **canary the POPULATION, not just the trigger**; put the population on the OK line, because _"nothing violated"_ and _"nothing was examined"_ are otherwise the same output; and **when the failure mode is silence, no classifier over the output can help -- you need two runs that OUGHT to differ.**
- **FAMILY 3 -- A MEASUREMENT IS BOUNDED BY THE QUESTION, AND THE MATRIX IS THE QUESTION.** A single instance gives you the wrong fix: from `wp start` reopening a Done WP I would have recommended one guard; the 18-cell matrix showed **no v2 lifecycle verb has a state guard at all**, so the fix is one shared guard and not seven. Same shape in the consumer sweep -- a list nobody swept **ends at last time's bug**, four callers became ELEVEN across seven files with five distinct policies. And in dc's vendoring count: **when the population is "what the people in the room have seen", every additional participant makes the count feel more reliable while leaving the frame exactly as narrow.** A detector beats a list because it does not have a room.
- **FAMILY 4 -- A GUARD'S PRECONDITION CAN BE INVALIDATED BY A CHANGE THAT IS CORRECT ON ITS OWN TERMS AND NEVER LOOKS AT THE GUARD.** 0042's total-failure branch became unreachable because `info` started rendering `<not set>` -- better for a human, non-empty for the parse. Same shape in 0047: **a machine ratification moved `st new` to Triage and silently defanged a display assertion that still reads as intentional coverage.** And in AC-11.4: **an evidence line that only relates artefacts to EACH OTHER can be satisfied about the wrong artefacts** -- consistency is not correspondence, so it needs a term binding the set to its origin. **A control refuses; documentation reminds.**
- **FAMILY 5 -- CLOCKS, AND I AM THE MEASUREMENT RATHER THAN THE AUTHOR OF IT.** Eight fabricated stamps, seven caught by me and **one refused by the guard before it could land**. The rule is not getting less true and I am not getting better at it, which is the entire case for a control over a rule -- measured on the author of the rule.

# FOURTH FOLD 2026-08-18 16:09Z -- localfold 13, end of the day hv ruled five things (verbatim)

---

node: vc
name: Validation Claude
role: validation
session_id: a403ff04-5306-4855-84ee-e74f3d3ab96d
heartbeat_at: 2026-08-18 15:58Z
status: active
focus: "**"THE REGENERATION" WAS NEVER A PERFORMABLE OPERATION AND THAT WAS MY ERROR, FOUR BOARDS DEEP.** My criterion presumed a re-ingest from v2 source; `1af21f4e` collapsed the buckets, so no v2 source exists in the live tree and none has since -- predating every board that carried the plan. cc refused to run it and was right. **The write-back REPLACED it: 275/275 carried, 0 missing, 0 byte-mismatched, now 276 in canon.** **434KB OF ISSUE BODIES WERE DESTROYED AND RECOVERED** -- they lived only in the gitignored store, never canon under D29, never travelling under D34, absent from every clone since the hoist; recovered from the snapshot my own accidental `intent backup` created and which I MOVED rather than deleted. **hv RULED body-in-JSON; restored 40/40, 434,357 bytes; `data-model.md` now matches `issue.schema.json` exactly.** **D57-7 + D57-8 RULED AND LANDED; ST0057 has 8 WPs; ST0056 gained WP-16.** **THE CHURN LOOP IS BROKEN -- the tree can go clean for the first time, which unblocks AC-11.5's honest binary marker.** Upstream FROZEN; push `local` only."
claims: [ST0056, ST0057]
---

# Validation Claude (vc)

## DOING

- **hv IS BACK. The queue is dispatched, cc HOLDS FOR hv'S DIRECT GO, and I am not relaying that release again** -- cc set that precedent itself and it is right. All three peers identified over the live channel and durable copies are in their inboxes.

- **`release` NOW RUNS `cargo test` -- LANDED, and the fix found a second defect inside my own first draft.** `bin/.devbin/cmd/build.d/release`: the gate added inside `preflight()` beside `:380` inheriting `:381`'s abort; the two `NATIVE_*` path constants hoisted above preflight's CALL site; and **`:706` no longer recommends `--skip-tests`** (dc's finding, the load-bearing one -- the tool's own documented recovery from a dirty tree was _re-run with every correctness gate off_, on exactly the run that tags). **Driven four arms in a sacrificial rig that EXTRACTS the real block rather than retyping it: cargo-0 GREEN, cargo-1 ABORT (the red), cargo-absent ABORT, no-manifest SILENT SKIP** -- plus a pre-patch control on the ordering assertion which fails as it must. **NOT decided and gone to hv: whether a dedicated `--skip-rust-tests` should exist** (dc's point that this must not reach hv as a clean one-liner).

- **ST0057 -- the disk model, DESIGNED AND RULED.** `intent/st/ST0057/design.md`, six numbered decisions each with a measurement under it. **D57-1** canon to `intent/.canon/`, per-artefact (one consolidated file is a merge-conflict generator with four writers). **D57-2** `.intentfiles`, committed, generated + pinned regions, grammar REFUSES rather than skips. **D57-3** `organize` with four answers plus **UNCLAIMED: report, never remove**; the per-file dehydration gate; the attachment/view authorship asymmetry; idempotence by content; a lock against ANY process. **D57-4** `intent edit`, because `wip` already has four meanings. **D57-5** the full text realisation. **D57-6** the 165 as attachments.

- **D57-5 IS THE HALF THE DESIGN WAS MISSING AND hv SUPPLIED IT.** The dehydration gate proves **THE STORE holds it**; a complete text export proves **A HUMAN can get it back without the tool.** Different assurances; only the first was covered. **It lands on `export --format md`, DELIBERATELY REFUSED today because _"the views are already in the tree"_ -- correct now, false the moment the disk model lands, so RULED withdrawn as PART of that thread and not before.** Precondition cc found: **`intent init` is NOT IMPLEMENTED**, and you cannot demonstrate a fallback from a clean directory you cannot create.

- **THE REGENERATION IS cc's, APPROVED, AND THE SEQUENCE IS THE POINT: `THREAD_PROSE` CHANGES WHAT THE MIGRATION WRITES.** Regenerate before it lands and it happens twice. Asked cc for: one owner, digest before and after, **report exactly what moved rather than that it worked**, `views::info`'s blank line fixed BEFORE the run so the two effects land as one intended change, and a check that **`whitespace-normalised` FALLS and `byte-identical` RISES** -- if not, something is reading the store rather than the source. **ST0011 is MINE and goes AFTER the regeneration**, never before: one row into a store about to be rebuilt is a write into the thing being replaced.

- **THE CRITIC GATE, BOTH HALVES APPROVED, PRIMED, UNBUILT -- `intent/st/ST0056/critic-gate.md`.** **A:** one word, `critic` into `GLOBAL_COMMANDS` at `bin/intent:55`; 8 bats failures -> 0; `st list`/`wp list` still refuse at rc=2 so the guard is not weakened. **THE `4/4` IS 3/4 (dc): test 2 asserts `rc=0 or 1` and `rc=0` IS WHAT THE DARK GATE RETURNS -- and it runs `critic shell`, which has 0 of 6 rules armed, so it CANNOT FAIL under any fix.** **B:** repaired, it still only reports Elixir. **TWO FIGURES I CARRIED HERE WERE FALSE, both corrected by dc 2026-08-18 at `ce532a97`.** (1) _"Elixir 19/19 is the positive control"_ -- it is **9 armed + 10 DECLARED-NONE**, and the true property is the better control: every elixir rule has MADE the decision and RECORDED it. (2) _"ST0039 stripped them; only Elixir was re-armed"_ -- `git log --all -S'Greppable proxy'` gives **shell 0 commits, rust 0 commits, elixir 6**. Those packs **never carried a proxy at any point in history**; they shipped UNTRIAGED from ST0034, so **"re-arm" is the wrong verb and nothing is being restored.** **ARMED / DECLARED-NONE / SILENT are THREE states and our vocabulary had two** -- `critic_runner.sh:18` skips a proxy-less rule silently, so `critic shell` returns rc=0 because nothing ever asked a question, byte-identical to clean. **Re-arming rust and shell is REAL WORK, unscoped, and must not be quoted as a one-liner.** **THE HALVES ARE NOT INDEPENDENT AND I WAS CARRYING THEM AS IF THEY WERE: test 2 was pointed at the one pack that can produce nothing, so Half B is not a CEILING on Half A, it is what makes Half A's headline UNTRUSTWORTHY.** Re-drive on `elixir` -- the only armed pack -- against a STAGED violation, assert `rc=1`, and take the proof as **the hook BLOCKING a commit**, never the critic exiting 0. **The shim DEFEATS A** -- take the fix, hold the shim. Adjacent and NOT yet put to hv: **`release:373-383` never runs `cargo test` at all.**

- **STRANDED 192 IS THE ONLY NUMBER GATING ANY DELETION, and `LOST-PROSE 0` is the trap beside it.** It is TRUE and means only _every section that HAS a destination reached it_. **The two-counter shorthand would have authorised deleting 192 files holding 748 authored sections while printing a zero** -- it was in the SPEC, not just on boards, and is gone. **The gate is the VERDICT at full scope**: exit 0, a printed `0 finding(s)` line whose ABSENCE is a refusal, the denominator, a pinned subject, and a demonstrated red. Full text in `conservation_check.sh`'s header; do not restate it here.

## Rules that keep paying

- **I KEEP WRITING CRITERIA THAT ENCODE A PREDICTED VALUE RATHER THAN A MEASURABLE PROPERTY, AND THE THIRD ONE HAD A COST.** `STRANDED 192 will move` (wrong subject -- pinned corpus). `whitespace-normalised FALLS / byte-identical RISES` (described a re-ingest with no source; **this is the one that made `sync`'s accurate warning look like confirmation, and 40 issue bodies went**). `0 -> 164` (164 was the canonical-three; 275 were eligible). **The replacement form is the property with the count as an OUTPUT: every eligible file is carried, byte-identical, denominator printed.**
- **AN INSTRUMENT REPORTING RED ACCURATELY, READ PAST BY someone WHO ALREADY HAD AN EXPLANATION READY** (cc's, and they asked for it on the record). `sync --to-store` named all 40 files before touching anything. AC-03.9 worked perfectly. **The inverse of a false green, and the explanation it was read against was mine.**
- **EVIDENCE FOR AN ADJACENT PROPOSITION IS NOT EVIDENCE FOR THE ONE AT HAND.** I quoted `sync.rs:167` to prove my hand-written bodies would survive; it settles the CORPUS question and says nothing about whether a stale view outvotes canon. cc checked the second. Same shape as dc's signature-versus-scope correction. **Five instances in one day.**
- **dc, THREE TIMES, ABOUT ME: a true, useful, NARROW finding restated as a clean general rule, and the generality is where it acquires the defect.** My "shared writer" rule would have forbidden the very evidence that proved my own file stale. **Their form: a self-report is evidence only where it is sourced INDEPENDENTLY of its subject and bound to it -- derived-from-subject is vacuous, editable-without-the-subject-moving is unverifiable.**
- **THE ONE CAREFUL DECISION INSIDE A CARELESS ACT IS WHAT MADE 434KB RECOVERABLE.** A bare `intent backup`, typed expecting usage output, created a snapshot and pruned another. I MOVED it to scratchpad rather than deleting, because nothing should be destroyed to tidy a measurement. **That principle paid out inside the same day, to its own applier.**

- **THE DAY'S RULE LANDED ON ITS AUTHOR, WHICH IS THE ONLY REASON IT IS WORTH ANYTHING.** I wrote _"when someone states a claim as the LOAD-BEARING reason for a decision, THAT claim is the one to check"_ on this board this morning, then spent the day propagating an unchecked one of my own -- **that the regeneration would move `STRANDED 192`** -- through hv's approval, ic's refinement and cc's queue. **Three nodes improved the instruction and none of us checked its subject.** The social half is the mechanism: the person who states the reason is not usually the person who can see it is unchecked, so the peer structure is the instrument rather than overhead. Four separate corrections came back at me today (cc x2, ic x1, dc x1) and every one was right.
- **A BACKLOG ITEM IS A CLAIM, AND IT DECAYS LIKE ANY OTHER.** `intent/restart.md` was stamped 2026-08-14 and still named "spec the marked-legacy AT form" as the next action -- **shipped four days earlier**: specced at `data-model.md:320`, `legacy` in `schema/thread.schema.json`, `legacy: Option<Legacy>` on the `AcceptanceTest` struct. It also still said "WP-03 is cc's next" with WP-04 Done and five WPs WIP. **A cold session reads that file AS the next action, so a stale backlog is not untidy, it is instructions.** Verify a queued item's premise at the moment you act on it -- and re-stamp the file with the COMMIT it is current as at, because a date does not say what moved.
- **THE DAY'S RULE, in cc's narrowing rather than mine, because mine was uncosted.** Not _"check your premises"_. **_"When someone states a claim as the LOAD-BEARING reason for a decision, THAT claim is the one to check."_** One grep each time. **The social half is the MECHANISM, not a caveat: the person who states the reason is not usually the person who can see it is unchecked** -- which is why the peer structure is the instrument rather than overhead. It read three times on 2026-08-18.
- **A SUBJECT THAT CANNOT EXHIBIT THE DEFECT CANNOT CLEAR IT.** The gitignored store no clone could hold; the attachment ordering a path-sorting migrator makes unobservable. **Both found by something that differed from the subject BY ACCIDENT** -- the argument for fixtures that differ on purpose.
- **A FIXTURE THAT HOLDS THE STATE BUT SKIPS THE TRANSITION lets BOTH ARMS of a contradiction test green while the path between them is broken.** Distinct from a fixture that cannot hold the defect at all. `st cancel` -> `doctor` is the live instance.
- **THE COUNTER-PATTERN, the only thing that has worked by design rather than by luck: an instrument that names its own blind spot in the same breath as its verdict.** `conservation_check.sh:793`, the SCOPE line, and `sync`'s refusal enumerating all 40 issues it would overwrite before declining to.
- **AN AT ROW EARNS ITS GREEN from an instrument DEMONSTRATED RED, a criterion naming the SUBJECT it was handed, and one naming the SHAPE OF THE INPUT** (mine + ic's two sharpenings).
- **ic: a field a test EXECUTES gets a different grade from one a test merely READS.**
- **A prediction being right is not evidence the run was sound** (ic). They applied it to their OWN correct prediction, which is the moment nobody does.
- **A source-text grep measures CLAIMS. Only EXECUTION measures behaviour.** ST0039 already paid for this; a probe whose POSITIVE is generated by the DOCUMENTATION OF ITS OWN NEGATIVE is the sharpest form.
- **A ruling given over the live channel is NOT LANDED until it is in the artefact it governs.**
- **The push result carries NO information about the remote in either direction.** Only `git ls-remote` + `git merge-base --is-ancestor`.

## TODO

- **ST0011** -- `completed` NULL on the estate's one genuinely wrong row. Mine, AFTER cc's regeneration.
- **AC-10.8 (egest symmetry)** and **the WP-10 interruption property** -- hv said CRACK ON, and the item is SHARPER than I had it. **`parity/tools/same_end_state_check.sh` and `interrupt_rig.sh` EXIST, are committed, carry my own ruling in their headers, and are cited by NOTHING** -- not `acceptance.md`, not `parity.md`, not the ST tree, not the repo; the only files naming them are other files in `parity/tools/`. Verified by `git grep -l` at `ce532a97`. **So it is not "the property needs an AC": hv gated the cutover on a property, the instrument was built and self-tested, and the contract does not know it exists.** Falls-between-the-cracks, not queued-but-unbuilt. **Drive it BEFORE writing a row state** -- the test exists, so `to-write` is the wrong state by my own rule.
- **D50** -- context delivered to hv this session (the 34-vs-10 count, the contradicting ratified line at `data-model.md:454`, the no-terminal-states cost, and cc's `status_legacy` proposal). **Awaiting hv's ruling; nothing blocked meanwhile.**
- **The board format** -- a board is read as CURRENT STATE and written as a SNAPSHOT OF WHEN ITS AUTHOR TYPED, and nothing marks which. `as at <commit>` in the header block is the shape. Mine.

## THE MODEL, in case everything else is lost

1. **The intentdb is the durable SSOT. Everything else is a secondary artefact. Nothing on disk is truth.**
2. All of `intentsvcs` works FROM the db; sync runs both ways.
3. The typed Rust API is the ONLY door in, so conformance is **by construction**.
4. Re-creation from an extract is a **capability**, not a licence to treat the db as disposable.
5. Ingest passes the **hard gate** of the intentsvcs API; the gate does the work, not the file format.
6. **Migrations are normal.** "No DB migrations, ever" is DELETED and was never hv's constraint.
7. The real standing requirement is **platform and data-model openness** (AC-02.6): always a 1-1 db-entity-to-`.json`/`.md` mapping, lossless, usable elsewhere. **That is what bidirectional sync is FOR.**
8. **(D34) The committed extract is the INTERCHANGE; the DB is per-machine truth and is NEVER committed.** Authority is not bidirectional just because transport is. **So AC-02.6 is the durability mechanism** -- a field that does not round-trip is data loss at the clone boundary, not a gap -- and **`event_log` is the only table that is both durable truth AND not reconstructible from the files**, which makes `events.jsonl` a precondition of the model rather than a WP-04 detail.

## Verification kit

- `$CLAUDE_JOB_DIR/tmp/v3fix` is a **migrated** v3 fixture with its own `git init`. Recipe: `config.json` at 3.0.0, `st new`, prose into `thread.json` (**never** a generated view), `sync`.
- **`$CLAUDE_JOB_DIR/tmp/hookprobe` is the 0043 rig; it covers ALL THREE Claude Code hook events and needs NO migrated project and NO interactive session.** Per arm: a dir, a `settings.json` wiring `UserPromptSubmit` (matcher `""`) to one command, then `claude -p "<prompt>" --settings ./settings.json`. Arms are hook scripts exiting 0/1/2 plus one wired to the real v3 binary. **Assert on the OUTPUT, never the exit code -- a BLOCKED run exits 0**, which is the same silent shape the probe was built to find. **And keep the exit-1 arm**: an assertion that `2` blocks passes equally on a build where every code blocks, so only the arm that does NOT block ties the failure to the change that caused it.
- **CORRECTED 2026-08-18, AND THE OLD FORM WAS WORSE THAN THE THING IT REPLACED: `git archive` + a separate `CARGO_TARGET_DIR` DOES NOT GIVE A NAMED BINARY.** `native/rust/build-support/source_commit.rs` stamps `unknown` when `git rev-parse` fails, and an archive extract has no `.git` -- so the rule I have been carrying yields a binary naming NO commit, strictly worse than the `dirty-<sha>` one cc objected to. **A `git worktree` at a pinned sha is the form that works**: clean by construction, so `git status --porcelain` is empty and the bare sha is stamped. **But ASK FIRST WHETHER THE PURPOSE IS ATTRIBUTION RATHER THAN REPRODUCTION** (ic) -- if it is, no rebuild is needed at all and the whole question dissolves.
- **A VARIABLE'S DEFINITION SITE MUST PRECEDE ITS READER'S CALL SITE, NOT ITS READER'S DEFINITION.** `preflight()` is DEFINED at `release:307` and CALLED at `:448`; `NATIVE_MANIFEST_REL` was assigned at `:477`. My first draft of the `cargo test` gate guarded on it and **would have tested an empty string and skipped in silence on every release -- the exact defect class the fix exists to remove, shipping inside the fix.** `bash -n` passes on it. **What catches it is an ordering assertion WITH A PRE-PATCH CONTROL that FAILS**; without the control the assertion proves nothing.
- **MEASURE AT A PINNED SHA, AND NAME THE COMMIT IN THE FINDING.** `HEAD` is a POINTER, so a claim about it is a claim about whatever it points at WHEN READ -- pinning protects the measurement, only naming the commit protects the report. **And `rust` is a pointer at a tree the same way, which is the whole of 0049.** `git archive` the sha, build in a separate `CARGO_TARGET_DIR`. Incidents in `.history/`.
- **ASK HOW MANY WRITERS AN ARTEFACT HAS BEFORE RULING ON IT** (cc). _"A ruling enforced on one of two writers is enforced on neither RELIABLY"_ -- **the uncovered writer wins whenever it runs LAST, so it is a RACE, which passes in the suite and fails in the estate.** `todo.md` has two doors and all five tests reached it through the other one. Incident in `.history/`.
- **Falsify before flipping.** Perturb the artefact the test asserts against and watch the right subset go red.
- **COUNT, STOP, READ THE DIFFERENCE, THEN ARCHIVE -- IN THAT ORDER, IN SEPARATE COMMANDS.** I ran the both-sided count check and the archive in ONE command and archived an unread ic entry; it printed `ic live=5` against my four named stamps AFTER the archive had run. **A check whose result arrives after the act it was meant to authorise is not a check.** Recovered from `.history/` and nothing was lost. The check caught unread entries FIVE times today and failed only when I let it run too late.
- **SIGNATURE AND SCOPE FAIL IN OPPOSITE DIRECTIONS AND NEITHER IMPLIES THE OTHER** (dc, 2026-08-16, correcting my verification of their header guard). Signatures answer _does it fire on the right SHAPE_; scope answers _does it fire on the right FILES_. **I cleared that guard on signatures alone and reported it as verified**: `intent/whiteboard/*/wip.md` matches 21 files, SIXTEEN of them archived boards, so it would have refused the protocol's own housekeeping. Both `case` globs and git pathspecs cross `/`. **The identical trap had already bitten the clock guard's port**, which makes it a class.
- **THE DAY'S MEASUREMENT CLASS AND ITS INSTRUMENT RULES NOW LIVE IN `parity.md`, NOT HERE** -- nine variants, three properties, the calibration and chokepoint rules, the annotated-artefact bias, and the sibling class. Four entries that sat here (guard-proven-only-by-its-positive-case, a-check's-error-message, the-ok-line-must-be-conditional, put-the-population-on-the-ok-line) are folded into it verbatim and archived. **Read `parity.md` -> Measurement rules before building any instrument.** Keeping a second copy here is the divergent-copy defect the canon itself warns about.
- **SHARPENED 2026-08-16, by L2 refusing a row I wrote: the three states answer TWO questions, not one. `to-write` = the test is UNWRITTEN. `red` = the test EXISTS and does not pass. NEITHER means "the criterion is unmet" -- a criterion can be false with no instrument pointed at it, which is exactly AC-10.9.** I set that row `red` because issue 0038 makes its criterion measurably false, and L2 refused it because the cited file does not exist. **The refusal was right.** My partial-coverage rule below was always about a test that EXISTS and under-covers, and it does not generalise to any unmet criterion; reading it that way produced a red row citing a file that is not there. The exempt-state hazard on such a row is covered by `stale_at_check.sh` firing the moment the file appears.
- **Refuse at partial coverage. CORRECTED 2026-08-15: hold the AT at `red` WITH AN EXPLICIT NOTE, not at `to-write`.** I had this the other way round on the grounds that `red` lies about a passing suite. It does not: **the AT's status is the CRITERION's state, not the file's**, and `to-write` is the worse lie because it claims no file exists when one does. Write the note so anyone running the suite and seeing green knows why the board is red. AT-00.5 is the precedent for the refusal; AT-06.4 / AT-06.7 are the corrected form.
- **A second AT row at `to-write` does NOT hold a gate open** (issue 0032): `ac gate` satisfies on the FIRST green AT covering the AC. Until that ANDs, the covering AT itself has to come off green.
- **The D34/D35 numbers are in canon, not here** -- `design.md` D34 carries the FTS ratios, the GitHub ceiling, the pack sizes and the `dbstat` split; D35 carries the WAL measurement and the rejected `.sql` dump. **Cite them; do not re-derive them.**

## Watch-outs

- **`intent at red|green|na` DESTROYS THE ROW'S NOTE (issue 0033) -- COMMIT BEFORE ANY STATUS CHANGE.**
- **DO NOT PUT v3 ON PATH.** `claude` is unimplemented as a FAMILY, so v3 refuses before it reads project state -- in a migrated project, an unmigrated one, and outside any project. **A session whose shell has v3 on PATH stops accepting prompts and cannot be recovered from inside itself.** Always invoke by explicit path.
- **DO NOT PUSH TO `upstream`** (hv, 2026-08-16: CI/CD budget spent). `local` only.
- **THIS REPOSITORY IS PUBLIC** and hv has ratified the whiteboards as part of the record.
- **Never mutate `bin/**` or `tests/**` in place** -- sacrificial worktrees or clones only.
- **`git commit --only <paths>`, never `-A`.** A move is TWO facts: the name and the content.
- **Verify at HEAD (`git ls-tree`) or in a fresh clone, never on disk.** `git grep` reads the INDEX, not the worktree.
- **AN UNTRACKED FILE HAS NO AUTHOR** (dc's correction). Never attribute uncommitted work by reading the tree.
- **ARCHIVE BY NAMING THE STAMPS. NEVER a greedy range.** And a fold must not overwrite an earlier fold's archive.
- **NEVER `head` OR `tail` A LIST YOU ARE COUNTING, AND NEVER READ `$?` THROUGH A PIPE.** Three instances in one day between us.
- **This shell is zsh:** no word-splitting of unquoted parameters. `--include=*.sh` and `$B $c` both bit.
- **A leading `-` in a grep pattern is read as options.** `grep -F "$probe"` returned a right answer from a grep that never ran; use `-e`.
- **BACKTICKS NEVER GO INSIDE A DOUBLE-QUOTED `-m` BODY.** Single-quote the body or use `git commit -F`.
- **`bash -n` proves PARSEABILITY, NOT RESOLVABILITY.** It parsed a file whose helper sat between its own two callers.
- **The live channel does not survive a peer's restart; the inbox does.** Durable asks go in the inbox.
- **Every timestamp is READ FROM A CLOCK, passed as an argument, clock read FIRST.**

## Decisions

- (2026-08-18) **hv: THE ISSUE BODY LIVES IN THE JSON.** `issue.schema.json` declared `body`; `data-model.md` specified a sibling `issues/<n>.md`; **neither was implemented and 434,357 bytes lived only in the gitignored store.** A rendered `issues/<n>.md` is now a VIEW, never where content lives.
- (2026-08-18) **hv RULED D57-7: opaque attachments live as FILES under `intent/.canon/st/<ID>/<path>` and DEHYDRATE like everything else.** My "binaries never dehydrate" was wrong -- D57-3's _source sitting beside it in the same commit_ never said the source must be JSON. `.cache` REFUSED as a home: gitignored, 0 tracked files, D29's precondition, and `cache` naming an unregenerable file fails in the dangerous direction.
- (2026-08-18) **hv RULED D57-8 READ/WRITE: `intent://` is the address of a piece of data.** Write path is **DB first, canon ALWAYS, views IF MARKED** -- never "disk if marked", which collapses canon into views. Views get no URL. `PUT` takes json only; markdown-in is refused except attachments, where authorship runs the other way. **No daemon may be required to read your own project.**
- (2026-08-18) **hv LIFTED THE AC MORATORIUM.** Three prohibitions drop: no new defect classes, no new ACs, no new instruments. **It schedules NOTHING** -- lifting a prohibition is not a priority ruling, and the 3.0.0 gate got BIGGER the same day.
- (2026-08-18) **`.canon` is the name, measured rather than chosen.** `corpus` already means the INPUT SET here (111 tracked files: fleet corpus, ingest corpus); `canon` already means the authoritative record (673 files, a Rust type, `$CANON` in the tools). Same test D57-4 used to reject `wip`.

- (2026-08-18) **hv RULED THE ORDERING: the regeneration goes FIRST under the known-dark critic gate; dc's Half A lands after.** My recommendation, taken. **Consequence recorded as an ACCEPTANCE rather than left to be found (dc): the regeneration commit is UNLINTED BY CONSTRUCTION.** An acceptance recorded with only its upside is indistinguishable from an oversight a fortnight later.
- (2026-08-18) **hv RULED ST0057's DISK MODEL IS IN THE 3.0.0 GATE.** hv verbatim: _"I need to be able to sit in a project, work with disk versions of the relevant artefacts, and have the db kept in sync as things change. So all of that has to happen before we do the 3.0.0 release."_
- (2026-08-18) **hv: FIX the release script's missing `cargo test`. LANDED.**
- (2026-08-18) **CORRECTED TWICE IN ONE DAY, AND THE SECOND CORRECTION KILLED MY OWN PREMISE. `STRANDED 192`'s SUBJECT IS THE PINNED CORPUS `hoist` @ `9b73e98f`, NOT THE LIVE TREE -- SO THE REGENERATION CANNOT MOVE IT.** I put "the next conservation run moves that number" into my opening plan as time-critical; hv approved a queue built on it; ic refined it into attribution-versus-reproduction; cc took the corrected version; **and nobody read what the number's subject was until cc went to `estate_corpus.sh`, which pins it and calls it a real, named, immutable revision.** **TWO MEASUREMENTS, DIFFERENT SUBJECTS, and merging them is what made my constraint set inconsistent:** a live-estate digest before/after the regeneration (no binary -- `--binary` drives ONLY the liveness arm, verified: `BINARY` occurs at exactly four sites in `conservation_check.sh` and invokes `st list --status all` at `:1075`); and conservation on the pinned corpus before/after the CODE change, binary named on each half. **THE PIN IS THE CONTROL AND THE BINARY IS THE TREATMENT, so "do not rebuild between the readings" would have held the independent variable constant and varied nothing** -- two identical numbers and no attribution, the exact failure it was written to prevent. It also contradicted hv's own sequence, which REQUIRES a rebuild. **WITHDRAWN.**
- (2026-08-18) **The `THREAD_PROSE` inseparability ruling STANDS BUT IS SCOPED (cc's amendment, and it improves it).** Inseparable on the LIVE-ESTATE DIGEST, where one regeneration carries both causes. **SEPARABLE on the pinned-corpus conservation measurement**, where the subject is fixed and the binary is the treatment: build with `THREAD_PROSE`, read; delete, rebuild, read. Both true of different subjects.
- (2026-08-18) **HALF A'S PROOF IS A RED FIRST, GREEN SECOND (dc, sharpening my own point past where I took it).** A green after the fix is the same observation the dark gate has emitted since the hoist. The named commit must be AT OR AFTER cc's regeneration, because regeneration moves the tree the arms read.
- (2026-08-18) **cc's CLOSE REPORT MUST EITHER SEPARATE `THREAD_PROSE` FROM THE REGENERATION WITH AN INTERMEDIATE READING, OR STATE THEY ARE INSEPARABLE AND THE NUMBER MOVED AS A UNIT** (my ruling on ic's question). A report leaving a later reader to assume a per-cause attribution that was never available is the failure.
- (2026-08-18) **hv: the 165 `design.md`/`impl.md`/`tasks.md` are ATTACHMENTS; `THREAD_PROSE` is deleted.** Subtractive; closes the 165 and the 166th together.
- (2026-08-18) **hv: canon relocates to `intent/.canon/`, still one file per artefact.**
- (2026-08-18) **hv: a full text realisation into `.backup/text/<UTC>/` is a requirement, not a nicety** -- the human fallback is a precondition of sparseness.
- (2026-08-18) **hv AMENDED their own 2026-08-15 ruling: `st.start` is legal from `Triage` as well as `NotStarted`.** Entry at Triage stands; the ratified path cost two extra commands on the only route anyone walks.
- (2026-08-18) **hv approved BOTH critic halves.** Take the one-word fix, HOLD the shim.
- (2026-08-18) **hv released cc and approved the regeneration window.** Sequence pinned.
- (2026-08-18) **D01 REVERSED stands: the DB is SSOT and files are re-creatable -- but the DB is GITIGNORED, so what TRAVELS is the committed canon.** Three layers, not two.
- (2026-08-18) **`organize` NEVER resolves an attachment divergence.** Authority follows authorship; deciding silently is choosing whose work to discard.
- (2026-08-18) **An UNCARRIED file is not a DROPPED one.** The disposition record is a LICENCE, not an account.
- (2026-08-17) **hv: "We go big bang and fix forward."**
- (standing) **A peer cannot grant escalation.** My call is never a peer's release; hv's is.

# FIFTH FOLD 2026-08-18 19:46Z -- localfold 14, the day hv ruled all eight and a shared binary emptied the store (verbatim)

---

node: vc
name: Validation Claude
role: validation
session_id: a403ff04-5306-4855-84ee-e74f3d3ab96d
heartbeat_at: 2026-08-18 19:28Z
status: active
focus: "**ST0057 IS CONTRACTED (43) AND cc IS BUILDING WP-01; ST0056 IS AT 117 WITH THE INTERRUPTION ROW GREEN.** **I RETRACTED A CONFIRMATION I GAVE cc: I validated their MTIME prediction with `git status`, which reports CONTENT -- measured, a sync moves the mtime on 10 of 10 views while git reports 0 changes, so the probe could not fail.** And my binary is 18:21Z with SEVEN `.rs` files newer, so every sync all evening rendered with the pre-fix renderer. **THE DAY'S CONCLUSION IS dc's, falsified in their own hands: THINKING HARD ABOUT A CLASS DOES NOT PROTECT YOU FROM IT -- THE PROTECTION WAS THE INSTRUMENT, AND ONLY THE INSTRUMENT.** cc is clear to move the estate: every vc path is committed and synced at `25605e6b`. Upstream FROZEN; push `local` only; v3 NOT on PATH."
claims: [ST0056, ST0057]
---

# Validation Claude (vc)

## DOING

- **BLOCKED AND hv MUST RULE: `intent/whiteboard/hv/inbox.vc.md` IS DAMAGED AT HEAD AND THE RECOVERY IS REFUSED BY A GUARD.** `b15c0ddd` deleted 888 lines -- 39 of 40 entries -- via `open(q,"w").write(open(q).read() + entry)`, where `open(q,"w")` truncates BEFORE the inner read. **The full content is safe at `b15c0ddd^` and is RESTORED IN MY WORKING TREE, verified: 40 prior stamps present, 0 lost, prior bytes contained verbatim.** The commit is refused by the clock guard's check C, because restoring 888 lines makes every historical stamp look newly ADDED -- including **one out-of-order pair that ALREADY EXISTED** (`2026-08-16 19:54 -> 2026-08-15 17:12`). **The guard's own contract says check C never blocks on pre-existing breakage; a RESTORATION is the case where that promise fails.** I will not bypass a gate on my own say-so. **PEERS: DO NOT TOUCH `hv/inbox.vc.md` -- the working-tree copy is the good one.**
- **ST0057 is contracted (42 ACs/ATs) and with cc; ST0056's interruption row is green.** Next of mine: ST0011's `completed` NULL, and ic's `status_reason` finding.

## TODO

- **dc's sequencing ask is with hv, unruled**: ST0057/WP-01 before ST0056/WP-11, because WP-11 is largely unbuilt so the ruling is a SAVING -- but only if WP-01 lands first, else starting WP-11 CREATES the layout assumption.
- **The interruption-property AC.** The moratorium is LIFTED so it is finally writable. **`same_end_state_check.sh` and `interrupt_rig.sh` are built, committed, self-tested and cited by NOTHING** -- hv gated the cutover on a property whose instrument exists and whose contract does not know it. **AT row held `red` with an explicit note** until ic's run at a named commit: the instrument exists, so `to-write` is the wrong state by my own rule. **AC-10.8 already exists -- my backlog was wrong about that half.**
- **ST0011** -- `completed` NULL on the estate's one genuinely wrong row. Needs a field-setter verb that does not exist (ST0057 WP-08), or a canon hand-edit.
- **`deferred.md`: cc's section is the last un-re-driven one.** dc's and ic's are walked. **The file is NOT deleted until all three are re-driven**, and no pickup is copied to a board without re-driving its premise first.

## Open with hv

- **The `critic` exit-code discriminator.** dc showed the naive fix rebuilds the dark gate with hv's own ruling as the cause. **ic's rc=2 confusion while verifying Half A is the sharpest evidence: if a human cannot tell two rc=2s apart, the hook cannot.**
- **May the runner shell out to `shellcheck`/`clippy`?** Gates dc's nine written declarations -- writing them on the premise that grep is the only instrument means redoing them all if the answer is yes.
- **D50** -- `Cancelled` as ruled (fixes 10 of 34) vs cc's `status_legacy` (covers 34, no machine change, reverses nothing ratified). Full context delivered.
- **`--skip-rust-tests`** shape, and **`doctor` printing `intent v2.19.0`** on a 3.0.0-dev project.

## Rules that keep paying

- **`git status` REPORTS CONTENT. IT CANNOT SEE AN MTIME-ONLY REWRITE.** I confirmed cc's mtime prediction with it and the probe **could not have failed**: measured, a sync moved the mtime on 10 of 10 views while git reported 0 changes. **A confirmation from an instrument blind to the defect is worth less than no confirmation, because it is counted.** Retracted in full.
- **CHECK THE BINARY'S AGE AGAINST ITS INPUTS BEFORE TRUSTING ANYTHING IT RENDERS.** Mine was 18:21Z with SEVEN `.rs` files newer, including the `views.rs` and `facade.rs` fixes -- so every sync all evening rendered with the PRE-FIX renderer. **`surface_check.sh` refuses at rc=2 for exactly this and is the only thing that surfaces it.**
- **A STALE FIGURE CONCEALS GROWTH IN THE DIRECTION SOMEONE MOST WANTS TO KNOW** (ic). The roster's ten bare timings were wrong in BOTH directions -- nine overstated by up to 39%, one UNDERSTATED by 49% because it statically reads a table that has grown. **The affordability argument dc and I had ran on stale numbers at both ends.**
- **A CONVENTION EVERY SIBLING FOLLOWS AND ONE EXCEPTION BREAKS IS NOT A CONVENTION ANYONE WILL REMEMBER TO CHECK** (cc). Every `intent/.<x>/` is gitignored; `.canon/` must not be. **A tidy-looking `intent/.*/` edit would silently un-commit the estate** -- now AC-01.5, which refuses the EDIT because AC-01.2 only checks the STATE.
- **THINKING HARD ABOUT A CLASS DOES NOT PROTECT YOU FROM IT AT ALL. THE PROTECTION WAS THE INSTRUMENT, AND ONLY THE INSTRUMENT** (dc, retracting their own more generous version after falsifying it in their own hands). They committed the document ABOUT this class, carrying the ordering rule, **in the order the rule forbids -- while typing the sentence that forbids it.** This is the argument for every mechanical guard here, made by the person with the most reason to believe care would do.
- **A RULE ITS OWN AUTHOR CANNOT OBEY WHILE WRITING IT DOWN IS A MISSING OPERATION, NOT A DISCIPLINE PROBLEM** (dc). Better than my four-syncs-in-ninety-minutes: mine costs the gap in my time, theirs proves it is unfollowable by the node most motivated to follow it. **And it settles gating by demonstration -- gated, dc would have been blocked from committing the document arguing for the gate.**
- **RE-DERIVING A REASON IS ALSO HOW YOU FIND OUT THE REASON WAS WRONG** (cc). I refused a reversal citing a recorded reason; cc measured to answer and found **half their own reasoning false, in the direction that made the fix look obviously correct.** `.git/HEAD` does not move on a commit. **The wrong half was the load-bearing-looking one, and the commit message would have read like a fix.**
- **A REFUSAL CAN STAND ON ONE LIMB WHILE THE OTHER IS FALSE -- CORRECT IT, NEVER DELETE IT.** Deleting the false half would have left a refusal nobody could re-derive; deleting the whole would have lost a sufficient reason.
- **A CONTRACT LINE IS NOT SELF-VERIFYING** (dc). Their tool printed _inherited divergences are reported, never failed on_ -- a contract-in-output line added that morning AS THE REMEDY for the same class -- **and an optimisation made it false while it went on asserting.** The remedy became the defect. **A line describing behaviour is another claim, and it inherits the failure mode of what it describes.**
- **MEASURED-AGAINST-RECORDED IS NOT A COMPARISON** (dc, conceding their own figures). They compared a timing they measured against `3077ms` READ OUT OF A ROSTER STRING -- another machine, a smaller tree. Plus zsh's builtin `time` on a subshell reporting the TOTAL column as wall clock. **Both errors moved the margin their way; 62% was really 25%.**
- **THE SUBJECT MOVING IS THE DEFAULT CONDITION ON A FOUR-NODE ESTATE, NOT AN ACCIDENT** (dc, generalising my void measurement). Three subjects moved under three instruments today. **Any measurement here has an uncontrolled writer under it unless it PINS ITS SUBJECT AND VERIFIES THE PIN EITHER SIDE.** That is now my standing method, not a recovery.
- **A DIVERGENCE IS A PROPERTY OF EVERY COMMIT, NOT OF HEAD** (dc). Commit the file first and sync after, and THAT COMMIT IS DIVERGENT IN HISTORY PERMANENTLY -- a later sync repairs the next one and can never reach the one already made. **Sync canon FIRST, then commit file and canon together.**
- **A CLOSING COUNT MUST CLOSE OVER WHAT WAS EXAMINED, NOT OVER WHAT EXISTS** (ic, corrected by the defect their own admission bar generated). They are the same number only while an instrument examines everything, and **any optimisation separates them silently, because the arithmetic still closes.** Live instance: a narrowing left `INHERITED 1` where the truth is 2, verdict still correct.
- **REMOVE THE NEED FOR A PERMISSION RATHER THAN GRANTING IT.** ic asked whether they may run a narrow attachment sync so a proposed gate becomes satisfiable. **A gate whose only compliant path is closed to two of four nodes is one people learn to bypass** -- so the answer is the missing VERB, not a wider permission. **`intent sync` is whole-estate only, which is why the restriction exists at all: the rule is about the verb, not about the node.**
- **I MEASURED A SUBJECT MOVING UNDER MY INSTRUMENT AND NEARLY REPORTED IT** (ic's hazard, from this morning). Two runs of one tool across a HEAD that a peer moved between them read rc=1 then rc=0, which I almost sent as _the modes disagree_. **Pin the subject and verify it either side, or the comparison is of two different things.**
- **I READ `$?` THROUGH A PIPE AND TRUNCATED A VERDICT, IN ONE COMMAND, AN HOUR AFTER dc AND ic EACH REPORTED THOSE ABOUT THEMSELVES.** `tail -8` gave me rc=0 (tail's) where the instrument returned 1, and hid half its findings -- I recorded ADDS 1 where the truth was 2. **What saved it was that 0 contradicted the design**, which is the day's most reliable pattern: **the measurements that survived were saved by a value that CONTRADICTED THE DESIGN, never by care** (dc). A merely plausible number gets published every time.
- **THE AT-ID-IN-FILE CONVENTION IS AMBIGUOUS FOR A CROSS-THREAD CITATION.** Four threads carry an unrelated `AT-03.6`. A tool HOUSED under ST0056 and CITED by ST0057 carrying the bare id names a different test to any reader. **Qualify with the thread.**
- **`to-write` EXPIRES THE MOMENT THE TEST IS WRITTEN, and the next state is rarely green.** AT-03.6 went `to-write` -> `red`: the instrument works and NOTHING GATES ON IT. **A capability no consumer consults distinguishes nothing** -- AT-11.5's shape, one thread over.
- **STATE THE INVARIANT, NOT A PROHIBITION ON THE OPERATION YOU HAPPENED TO BE WATCHING** (dc). AC-03.6 was reasoned wholly from canon running AHEAD of a commit; dc's checker caught the mirror -- **the FILE ahead and canon BEHIND, clean worktree** -- and my wording missed the half that was live. Now: _at every commit, every attachment's recorded bytes are obtainable from that commit._
- **A `manual` ROSTER ROW IS A FACT, NOT AN ADMISSION.** dc could not commit an instrument without a row (the gate fails on an unrostered `*_check.sh`) and would not write a row whose admission is mine. **`manual` + reason is the literally true declaration that the gate does not run it** -- lands in one commit, presumes nothing, and leaves `gated` as the separate decision.
- **A REMEDY MUST BE AIMED AT THE SUBJECT THAT WAS HARMED** (ic). dc's canon-versus-commit checker governs ATTACHMENT bytes; my sync episode destroyed a CRITERION. **I was one message from citing a real instrument as the fix for a defect it cannot reach.** Two subjects, two instruments -- now AC-03.13.
- **AN INSTRUMENT IS NOT ADMITTED ON ITS DESCRIPTION** (ic, refusing me). I asked ic to give a roster read on a tool that is not in the tree, which would have made them judge my summary of someone else's uncommitted file. **Commit it, then read it** -- dc's own standard, applied to admission.
- **NEVER PIPE A WRITE COMMAND'S OUTPUT TO `/dev/null`.** `sync --to-store` was FAILING on a UNIQUE constraint through several calls and said so every time; I suppressed it. **The remedy it printed named `intent doctor`, and I never saw the sentence.**
- **A WRITE PATH WHOSE INPUT WAS REFUSED MUST NOT THEN BE A SOURCE OF TRUTH.** `--to-store` rejected the write, the store stayed stale, and `--to-disk` wrote that stale store over canon -- **destroying the same edit twice, silently, with rc=0.**
- **AT AND AC IDS NUMBER INDEPENDENTLY.** I minted `AT-03.12` for `AC-03.12`; `AT-03.12` was ALREADY a green row covering `AC-03.11`. **Derive nothing; take the next FREE id and assert against the array you are appending to** -- my guard checked `criteria` while the collision was in `tests`.
- **THE COUNT IS THE INSTRUMENT AND THE WORD IS NOT.** `at lint` said _115 AT row(s) conform_ when it should have said 116. **I read "conform" and not the number**, after a whole afternoon spent writing that a denominator which does not close is the thing to stop for.
- **`open(f,"w").write(open(f).read() + x)` DESTROYS THE FILE.** The write-mode open is evaluated FIRST and truncates; the inner read then reads the empty file it just made. **The append and the destruction are one expression.** I used the safe two-step form twice today and the inline form the third time. **What caught it was the commit's own +44/-893 stat -- not a check, not the code, not a guard.**
- **A GUARD THAT BLOCKS ON WHAT A COMMIT ADDS CANNOT TELL RESTORATION FROM REGRESSION.** Re-adding destroyed content re-adds its inherited defects, and the guard scores them as new. **The promise "never blocks on pre-existing breakage" holds for edits and fails for recoveries.**
- **I WARNED dc ABOUT AN ENTANGLEMENT I HAD ALREADY CAUSED.** `sync --to-store` reads the WORKTREE, so `c4f9bcbe` carried dc's uncommitted instrument into canon -- and for two commits canon held an artefact whose bytes existed in NO commit. **A rule that lives in my head and in a peer message is followed until someone is mid-task.** Now AC-03.5.
- **A CHECK THAT READS THE INDEX CANNOT SEE WHAT THE INGEST PATH READS FROM THE WORKTREE.** Arm 1 reads `git cat-file blob :$rel` precisely to catch this class, and validates the MANIFEST, never an attachment -- so it passes.
- **SETTABLE IS NOT THE ONLY TEST** (dc, catching AC-08.5 before it ratified). `at green|red|na` DO write the row and DESTROY the note doing it, so "can every field be set?" passes on the exact field that is being lost. **A destructive side effect is not an unwritable field.**
- **A ONE-STRING EDIT HAS A FAILURE MODE AND IT IS TIMING, NOT SIZE** (dc). Bash reads a script incrementally, so an in-place rewrite can land under a process already executing it. **I truncate-and-rewrote `cmd/precommit` -- which runs on EVERY node's every commit -- minutes before dc warned me, because a one-string change felt too small to have a failure mode.** Nothing broke; that is luck, not method. **Atomic replace into the same directory, always.**
- **A DEFERRAL NOBODY RE-VERIFIES BECOMES WORK ALREADY DONE** (dc, now measured). **Two of cc's three deferred rows were DELIVERED** -- the `build.rs` embed (AT-11.5 green) and both `kind` conversions (`AcKind`/`AtKind` are enums) -- and one row's blocker had expired with nobody striking it. **Re-drive the PREMISE, never copy the row.**
- **THE ROW DOES NOT GO GREEN ON A NUMBER I CANNOT ACCOUNT FOR.** ic's verdict read 1432 match over a population of 1433. The instrument passes; the denominator does not close; the row holds red WITH THE FIRST SENTENCE SAYING THE INSTRUMENT PASSES, so nobody hunts a defect that is not there.
- **TWO FACES AGREEING BECAUSE NEITHER MEASURED IS THE STRONGEST FORM OF THE CLASS** (cc). `TodoItem` carried no status, so the bucketing lost the fact BEFORE either renderer saw it -- three buckets, six states. **Agreement between two derived views is not corroboration when both derive from the same loss.**
- **ONE CLASS, THREE FACES, NAMED INDEPENDENTLY IN ONE HOUR.** **ic:** an instrument whose OUTPUT is independent of what it MEASURES. **vc:** a READING independent of what the instrument SAID. **dc:** an OUTPUT whose SHAPE is independent of its own MEANING. **All three shipped a wrong belief with the instrument working correctly.**
- **A CAVEAT MUST TRAVEL WITH THE OUTPUT, NOT THE SOURCE** (dc). `self_provenance_check.sh` says _"dirty is NOT A FINDING"_ at `:179-180` -- **in comments, which never print** -- while `:280` emits an unhedged negative. **Co-location in the source does not co-locate in the emission**, because the output is what gets quoted into a focus line, a peer message, and a report to hv. It cost four propagated wrong reads in a day.
- **AN ABSOLUTION CAN OVERSHOOT, AND ACCEPTING IT IS ITS OWN ERROR.** dc took the `:280` half honestly; the other half -- AC-11.5 open -- was a stale claim about a record I wrote myself, caused by nothing but me. **Split a bundled error by cause before accepting anyone's account of it**, or the half with no owner disappears.
- **AUTHORITY FOLLOWS AUTHORSHIP, AND IT HAS NOW HAPPENED FOR REAL.** ic's `estate_corpus.sh` edit landed on disk and in a commit; **canon kept the old text for hours and nothing could report it.** An attachment diverging means the STORE is stale. `--to-store` was the correct direction.
- **A CRITERION MUST ENCODE A PROPERTY, NEVER A PREDICTED VALUE. The count is an OUTPUT.** Three of mine failed this today: `STRANDED 192 will move` (wrong subject -- pinned corpus), `whitespace-normalised FALLS` (a re-ingest with no source; **this one cost 434KB**), `0 -> 164` (275 were eligible). The form that works: _every eligible file is carried, byte-identical, denominator printed._
- **THE LOAD-BEARING CLAIM IS THE ONE TO CHECK, AND THE PERSON WHO STATES IT IS THE LEAST ABLE TO SEE IT IS UNCHECKED.** "The regeneration" went four boards deep on my unchecked premise. **Three of us made the same error today at three depths** -- ic re-drove a fact but not the rule it rested on; dc found a `deferred.md` blocker that was already delivered.
- **AN ALARM IS ONLY AS LIVE AS THE CONSTRAINT IT IS RAISED AGAINST** (ic). A stale instruction copied to a board acquires an owner and reads as verified.
- **A RULE OUTLIVES THE WORLD IT WAS WRITTEN FOR.** `estate_corpus.sh:88` says re-pin whenever HEAD moves -- written while the migration was AHEAD, and following it now would aim `STRANDED` at a tree with no v2 content. **Retire a refusal with the change that expires its reason.** Same class as D57-5's `export --format md`.
- **EVIDENCE FOR AN ADJACENT PROPOSITION IS NOT EVIDENCE FOR THE ONE AT HAND.** Five instances today. dc's canonical form: signature and scope fail in opposite directions.
- **A NARROW MEASURED FINDING RESTATED AS A CLEAN GENERAL RULE ACQUIRES ITS DEFECT IN THE BROADENING** (dc, three times, about me). Their replacement: **a self-report is evidence only where it is sourced INDEPENDENTLY of its subject and bound to it** -- derived-from-subject is vacuous, editable-without-the-subject-moving is unverifiable.
- **AN INSTRUMENT REPORTING RED ACCURATELY, READ PAST BY SOMEONE WHO ALREADY HAD AN EXPLANATION READY** (cc, on the record at their request). `sync --to-store` named all 40 files first. The explanation it was read against was mine.
- **THE ONE CAREFUL DECISION INSIDE A CARELESS ACT IS WHAT MADE 434KB RECOVERABLE.** Nothing is destroyed to tidy a measurement.
- **A COMMIT CANNOT REPORT ON A FILE IT NEVER SAW** (cc, twice in one session). `git commit --only` on an UNTRACKED path stages nothing and reports a true count about what it did commit. `git add` first.
- **ARMED / DECLARED-NONE / SILENT ARE THREE STATES** (dc). A proxy-less rule is skipped silently, so `critic shell` returns rc=0 because nothing asked a question -- byte-identical to clean.

## THE MODEL, in case everything else is lost

1. **The intentdb is the durable SSOT. Nothing on disk is truth.** All of `intentsvcs` works FROM the db; sync runs both ways; the typed Rust API is the ONLY door in, so conformance is by construction.
2. **Migrations are normal.** "No DB migrations ever" is DELETED and was never hv's constraint.
3. **(D34) The committed extract is the INTERCHANGE; the DB is per-machine and NEVER committed.** So **AC-02.6 is the durability mechanism** -- a field that does not round-trip is data loss at the clone boundary. **Under D57-8's read/write it is also the completeness guarantee for the mutation surface: a field that does not round-trip cannot be written.**
4. **Three layers, not two: canon (committed, never sparse) / store (gitignored, rebuilt) / views (committed, sparse).** Sparseness applies to VIEWS. **D29 -- a gitignored path is never canon -- is what makes a clone complete**, and the 434KB loss is what happens when it is broken.
5. **`event_log` is the only table that is durable truth AND not reconstructible from files**, which makes `events.jsonl` a precondition of the model.

## Verification kit

- **MEASURE AT A PINNED SHA AND NAME THE COMMIT IN THE FINDING.** `HEAD` is a pointer. **`git archive` does NOT give a named binary** -- `source_commit.rs` stamps `unknown` with no `.git`; a `git worktree` at a pinned sha does. **But ask first whether the purpose is ATTRIBUTION rather than REPRODUCTION** (ic) -- if it is, no rebuild is needed.
- **A VARIABLE'S DEFINITION MUST PRECEDE ITS READER'S CALL SITE, NOT ITS READER'S DEFINITION.** `bash -n` passes on the failure. What catches it is an ordering assertion WITH A PRE-PATCH CONTROL THAT FAILS.
- **Falsify before flipping**, and **an AT earns its green from an instrument DEMONSTRATED RED**, a criterion naming its SUBJECT, and one naming the SHAPE OF THE INPUT.
- **`to-write` = the test is UNWRITTEN. `red` = it EXISTS and does not pass. Neither means "the criterion is unmet."** Refuse at partial coverage by holding `red` WITH AN EXPLICIT NOTE -- the AT's status is the CRITERION's state, not the file's.
- **The measurement rules live in `parity.md`, not here.** Read them before building any instrument.
- **$CLAUDE_JOB_DIR/tmp/hookprobe** covers all three Claude Code hook events with no migrated project. **Assert on OUTPUT, never the exit code -- a BLOCKED run exits 0.**

## Watch-outs

- **`intent at red|green|na` DESTROYS THE ROW'S NOTE (issue 0033) -- COMMIT BEFORE ANY STATUS CHANGE.**
- **`intent backup` with no subcommand MUTATES**: creates a snapshot, PRUNES an existing one under an unconsulted retention policy, and rewrites the store file. A bare family invocation reaching a mutation.
- **`sync --to-disk` is the SILENT direction and can be the destructive one** whenever canon is the richer side. `--to-store` shouts; `--to-disk` does not.
- **DO NOT PUT v3 ON PATH.** A session whose shell has it stops accepting prompts and cannot be recovered from inside itself.
- **DO NOT PUSH TO `upstream`** (hv). **THIS REPOSITORY IS PUBLIC.**
- **Never mutate `bin/**` or `tests/**` in place** -- sacrificial copies only. **`git commit --only <paths>`, never `-A`.**
- **Verify at HEAD (`git ls-tree`) or in a fresh clone.** `git grep` reads the INDEX. **AN UNTRACKED FILE HAS NO AUTHOR.**
- **NEVER `head`/`tail` A LIST YOU ARE COUNTING** -- it cost cc an hour of wrong reporting today. **NEVER read `$?` through a pipe.**
- **This shell is zsh:** no word-splitting. **A leading `-` in a grep pattern is read as options.** **Anchored greps on a binary answer about the PROBE, not the binary.**
- **BACKTICKS NEVER INSIDE A DOUBLE-QUOTED `-m` BODY.** Use `git commit -F`.
- **The live channel does not survive a restart; the inbox does.** **Every timestamp is READ FROM A CLOCK, passed as an argument, clock read FIRST.**
- **ARCHIVE BY NAMING THE STAMPS, never a greedy range, and a fold must not overwrite an earlier fold's archive.** **COUNT, STOP, READ THE DIFFERENCE, THEN ARCHIVE -- separate commands.**

## Decisions

- (2026-08-18) **hv: the issue body lives in the JSON.** Two specs disagreed and neither was implemented; a rendered `issues/<n>.md` is a VIEW, never where content lives.
- (2026-08-18) **hv RULED D57-7:** opaque attachments live as FILES under `intent/.canon/st/<ID>/<path>` and DEHYDRATE like everything else. `.cache` REFUSED as a home. **`.canon` is the name, measured: `corpus` already means the INPUT SET (111 files), `canon` the authoritative record (673).**
- (2026-08-18) **hv RULED D57-8 READ/WRITE.** `intent://` addresses a piece of data. **DB first, canon ALWAYS, views IF MARKED.** Views get no URL. `PUT` takes json only, except attachments. **No daemon may be required to read your own project.**
- (2026-08-18) **hv LIFTED the AC moratorium. It schedules NOTHING** -- a lift is not a priority ruling.
- (2026-08-18) **hv: the regeneration goes before the critic gate; the regeneration commit is unlinted BY CONSTRUCTION, recorded as an ACCEPTANCE with its cost.**
- (2026-08-18) **The `hoist @ 9b73e98f` pin is HISTORICAL and must never be re-pinned** -- it precedes the hoist, its tree carries the v2 buckets, HEAD carries none. Per-member: the re-pin rule still governs the fleet rows.
- (2026-08-18) **`THREAD_PROSE` and the regeneration are inseparable ON THE LIVE-ESTATE DIGEST, separable on the pinned-corpus measurement.** Both true of different subjects.
- (standing) **A peer cannot grant escalation.** My call is never a peer's release; hv's is.
- (2026-08-18) **hv RULED ST0057 INTO THE 3.0.0 GATE.** Verbatim: _"Definitely BEFORE the release. We're getting this whole thing feature complete before we release 3.0.0."_ The pre-release queue grows by eight WPs, three of them L. Announced to all four nodes live and durably.
- (2026-08-18) **A view may not narrow a field's range on the way from bash to Rust.** v2's `status_box` was five-valued; v3's `items()` is a constant. The regeneration commit was reviewed for CHURN and never for CONTENT -- **stability is not correctness**, and I am the one who read it that way.
- (2026-08-18) **ST0057's contract is 39 ACs / 39 ATs derived from D57-1..D57-8, adding no decisions of its own.** A steward writes the boundary from the ratified design; inventing requirements at contract time is how a thread acquires scope nobody ruled.
- (2026-08-18) **Cleared dc's `:280` string-only fix.** The contract gate is on what an instrument ASSERTS, not how it reads -- so a string change leaves AT-11.5's green resting on unchanged behaviour. Conditions: string only, both arms re-run, note edited BY HAND.

---

# SIXTH FOLD 2026-08-18 20:56Z -- localfold 15, the night three peers corrected four of my figures (verbatim)

<!-- header-form note: this archive carries THREE fold-header conventions -- `## FOLD 10/11`, `# FOLD 12 -- board as at`, and `# FOURTH/FIFTH/SIXTH FOLD`. No single grep finds them all; count with `grep -nEi '^#+ (FOLD [0-9]+|[A-Z]+ FOLD)'`. -->

---

---

node: vc
name: Validation Claude
role: validation
session_id: a403ff04-5306-4855-84ee-e74f3d3ab96d
heartbeat_at: 2026-08-18 20:51Z
status: active
focus: "**NOTHING IS OPEN WITH hv. The damaged inbox is RESTORED at `11bb52da`** -- 42 entries, 923 insertions, one `--no-verify` hv authorised, with the trace in the commit body. **hv RE-RULED the runner ruling with the fact they had been denied**: their original go was given over dc's `:176` framing, from a transcript with ZERO occurrences of injection or disguised, so they authorised relaxing what was described as a capability ceiling and is an INJECTION BOUNDARY. Named-tool form stands. **Contract 118 -> 121 rows plus one AC-08.5 extension, and EVERY ROW CAME FROM A PEER'S MEASUREMENT.** cc built AC-03.14's fix at `4577e18e` -- verified correct, and **AT-03.15 HOLDS RED ON COVERAGE: 6 verbs of 31, enumeration clause unmet.** Four of my own figures were wrong before they travelled and I caught three of them."
claims: [ST0056, ST0057]
---

# Validation Claude (vc)

## DOING

- **Nothing is open with hv and nothing of mine is in flight.** All eight rulings carried, both parked decisions ruled, the inbox restored.

## TODO

- **AT-03.15 HOLDS RED and cc owes two things before I can record it**: the `AC-03.14 / AT-03.15` marker in `write_moves_only_what_changed.rs` (lint refuses the moment the row cites it), then I re-point the row off `write_exactness.rs`, which never existed. **The hold is COVERAGE, not correctness -- the fix at `4577e18e` is verified right.** 6 verbs of 31; `upgrade` and 24 of the 27 `apply` verbs undriven; nothing prints an N-of-31. **cc's own `st start` vacuous pass is the argument: a verb whose precondition is unmet hands you a free green, so "one skip covers them all" does not license a subset.**
- **THE FORM BEFORE THE POPULATION** (dc). No row on instruments-state-their-reach until a form exists to count: four numbers were produced tonight across three nodes and only the first gap was a defect -- the rest were definitional, and the estate has no convention. **A criterion asserting it would grep for an undefined thing and return clean because nothing could ask.**
- **ST0011** -- `completed` NULL, AC-08.5's first burning case. And **spec the marked-legacy AT form in `data-model.md`** before WP-08; both are the same question.
- **Two sweeps owed across 121 + 43**: name the INSTRUMENT wherever two would disagree (from cc's AC-01.4 question), and qualify every bare `AC-XX.Y` with its thread (four threads carry an unrelated `AT-03.6`; ST0056 and ST0057 both carry an `AC-04.4`, and ST0056's is already SATISFIED, so a bare id resolves to a green row and reads as handled).
- **ST0057 WP-01 is cc's next.** ic measured it: 3 files genuinely break, and a mechanical sweep would corrupt 14 more whose `intent/st/` references are historically-correct v2 prose. **No criterion covers that yet -- mint AC-01.6 before cc sweeps.**

## Rules that keep paying

- **THINKING HARD ABOUT A CLASS DOES NOT PROTECT YOU FROM IT AT ALL. THE PROTECTION WAS THE INSTRUMENT, AND ONLY THE INSTRUMENT** (dc, after committing the document ABOUT this class in the order the document forbids, while typing the sentence that forbids it). **The argument for every mechanical guard here, made by the person with most reason to believe care would do.**
- **THE MEASUREMENTS THAT SURVIVED WERE SAVED BY A VALUE THAT CONTRADICTED THE DESIGN, NEVER BY CARE** (dc). A merely plausible number gets published, every time. Mine tonight: `0 thread(s)` in the one call I left visible.
- **A CRITERION ENCODES A PROPERTY, NEVER A PREDICTED VALUE. The count is an OUTPUT.** Three of mine failed this and one cost 434KB. **And RECORD A PREDICTION BEFORE THE FACT** -- tonight's mtime test failed honestly only because the expected result was in canon first.
- **NEVER PIPE A WRITE COMMAND'S OUTPUT TO `/dev/null`. NEVER `$?` AFTER A PIPE. NEVER `tail` A VERDICT.** All three cost me a wrong reading today; the third gave me `rc=0` from `tail` and hid half a finding.
- **A CLOSING COUNT MUST CLOSE OVER WHAT WAS EXAMINED, NOT OVER WHAT EXISTS** (ic, corrected by the defect their own bar generated). Any optimisation separates them silently, because the arithmetic still closes.
- **THE SUBJECT MOVING IS THE DEFAULT CONDITION ON A FOUR-NODE ESTATE.** Pin the subject and verify the pin EITHER SIDE, or the comparison is of two different things. Three subjects moved under three instruments today.
- **A VACUOUS PASS AGREES OVER NOTHING.** `sync --to-store` printed _the store and the extract agree_ over **0 == 0**, rc 0, with a destructive verb downstream -- and `--to-disk` wiped 57 and 82 rows out of two views.
- **`git status` REPORTS CONTENT.** It cannot see an mtime-only rewrite, so my "confirmation" of cc's prediction could not have failed.
- **A CONTRACT LINE IS NOT SELF-VERIFYING** (dc). It is another claim and inherits the failure mode of what it describes the moment a mode changes underneath it. **The remedy became the defect.**
- **MEASURED-AGAINST-RECORDED IS NOT A COMPARISON** (dc). And ic then showed the RECORDED side was wrong in BOTH directions -- one figure understated by 49% because it statically reads a table that has grown.
- **STATE THE INVARIANT, NOT A PROHIBITION ON THE OPERATION YOU HAPPENED TO WATCH** (dc). A prohibition covers only the operations someone thought of; a property of a state is checkable by inspection.
- **REMOVE THE NEED FOR A PERMISSION RATHER THAN GRANTING IT.** And **a rule its own author cannot obey while writing it down is a MISSING OPERATION, not a discipline problem.**
- **RE-DERIVING A REASON IS ALSO HOW YOU FIND OUT THE REASON WAS WRONG** (cc, withdrawing their own item after finding half their recorded reasoning false). **A refusal can stand on one limb while the other is false -- correct it, never delete it.**
- **A REVERT OF SOURCE IS NOT A REVERT OF ARTEFACTS**, and `target/release/` is shared by four nodes. **THE MARKER NAMES A COMMIT, EXACTLY AND ONLY; a commit does not determine an artefact when the tree is dirty** -- the binary that WIPED the estate and the one that FIXED it share `dirty-18197aaf` byte for byte.
- **AT AND AC IDS NUMBER INDEPENDENTLY.** Compute the next FREE id; never derive one from the other, and assert against the array you are appending to.
- **`open(f,"w").write(open(f).read() + x)` DESTROYS THE FILE** -- the write-mode open truncates before the inner read runs.
- **A REMEDY MUST BE AIMED AT THE SUBJECT THAT WAS HARMED** (ic), and **EVIDENCE FOR AN ADJACENT PROPOSITION IS NOT EVIDENCE FOR THE ONE AT HAND.** Five scope-of-probe instances across three nodes today, every one caught by a peer and none by a check -- **there is no grep for the scope of a probe** (dc), because the scope is what the probe did not look at.

## THE MODEL, in case everything else is lost

1. **The intentdb is the durable SSOT. Nothing on disk is truth.** The typed Rust API is the ONLY door in.
2. **Migrations are normal.** "No DB migrations ever" is DELETED and was never hv's constraint.
3. **(D34) The committed extract is the INTERCHANGE; the DB is per-machine and NEVER committed.** So **AC-02.6 is the durability mechanism**, and under D57-8's read/write it is also the completeness guarantee for the mutation surface.
4. **Three layers: canon (committed, never sparse) / store (gitignored, rebuilt) / views (committed, sparse).** **D29 -- a gitignored path is never canon -- is what makes a clone complete.**
5. **`event_log` is the only table that is durable truth AND not reconstructible from files.**
6. **hv, repeatedly: disk<->db sync, the realiser and `.intentfiles` all work BEFORE 3.0.0 ships.** ST0057 WP-01..06 are inside the gate. Do not re-ask.

## Verification kit

- **MEASURE AT A PINNED SHA AND NAME THE COMMIT IN THE FINDING.** `HEAD` is a pointer. **Pin a BINARY by content hash, never by its marker** -- AC-10.11, and it paid twice in one day.
- **CHECK THE BINARY'S AGE AGAINST ITS INPUTS BEFORE TRUSTING WHAT IT RENDERS.** Mine was 18:21Z with seven `.rs` files newer. `surface_check.sh` refuses at rc=2 for exactly this and is the only thing that surfaces it.
- **`to-write` = the test is UNWRITTEN. `red` = it EXISTS and does not pass.** Neither means the criterion is unmet. **`to-write` expires the moment the test is written, and the next state is rarely green.**
- **An AT earns green from an instrument DEMONSTRATED RED**, a criterion naming its SUBJECT, and one naming the SHAPE OF THE INPUT. **Hold red WITH AN EXPLICIT NOTE saying the instrument passes**, or a reader hunts a defect that is not there.
- **The measurement rules live in `parity.md`; the output-contract findings in `output-contracts.md` (dc's).** Read those, not a board copy.

## Watch-outs

- **`intent at red|green|na` DESTROYS THE ROW'S NOTE (issue 0033).** Hand-edit canon, then `sync --to-store`.
- **`sync --to-disk` writes the STORE over CANON and is the SILENT direction.** Canon-edit then `--to-store` is the safe order. **A refused `--to-store` leaves a STALE store that `--to-disk` will then write out at rc=0** (AC-03.13).
- **Every attachment edit by any node leaves canon divergent until I sync**, and a later sync repairs the NEXT commit and never that one. **Commit first, then ping me.**
- **`intent backup` with no subcommand MUTATES** -- snapshots, PRUNES, rewrites the store.
- **DO NOT PUT v3 ON PATH.** **DO NOT PUSH TO `upstream`** -- this repository is PUBLIC and the remote is FROZEN except for dc's one ruled publication.
- **Never mutate `bin/**` or `tests/**` in place -- sacrificial copies, and ATOMIC REPLACE for anything a running process reads.** A one-string edit's failure mode is timing, not size.
- **`git commit --only <paths>`, never `-A`.** **Verify at HEAD (`git ls-tree`) or in a fresh clone** -- `git grep` reads the INDEX.
- **This shell is zsh:** no word-splitting; a leading `-` in a grep pattern is read as options; anchored greps on a binary answer about the PROBE.
- **BACKTICKS NEVER INSIDE A DOUBLE-QUOTED `-m` BODY.** Use `git commit -F`.
- **Every timestamp is READ FROM A CLOCK, passed as an argument, clock read FIRST.** **ARCHIVE BY NAMING THE STAMPS; COUNT, STOP, READ THE DIFFERENCE, THEN ARCHIVE.** A fold must never overwrite an earlier fold.

## Decisions

- (2026-08-18) **hv AUTHORISED ONE `--no-verify` AND THE RESTORATION IS AT HEAD** -- `11bb52da`, 42 entries, 923 insertions, HEAD's body verified contained either side. **The guard was RIGHT to refuse and nothing about it changes on this evidence**: it cannot distinguish repair from regression, and the other reading would let any node launder a bad stamp through a revert. The commit body is the trace the bypass owes.
- (2026-08-18) **hv CONFIRMED THE RUNNER RULING WITH THE FACT THEY HAD BEEN DENIED.** Their original go was given over dc's `:176` framing, from a transcript carrying ZERO occurrences of injection or disguised -- **so they authorised relaxing what was described as a capability ceiling and is an INJECTION BOUNDARY.** Re-ruled with it in hand: **named-tool declaration stands.** The rule names WHICH tool, the runner owns HOW, **rule files never contribute shell**, and a tool-armed rule REFUSES when its tool is absent rather than skipping. `critic_proxy_is_simple` is not relaxed.
- (2026-08-18) **A RULING RELAYED BY ME IS NOT A GO** (cc, quoting my own precedent back after I broke it). A relay and a mistake are indistinguishable from the receiving end, so the accuracy of any particular relay is not evidence. **hv gives go-aheads on the node's own channel.**
- (2026-08-18) **hv RULED cc's SEQUENCING: ITEM 1 (the `WriteSet` unchanged-skip) FIRST, THEN ST0057 WP-01.** Carried to cc. **The order is also the only measurable one**: WP-01 moves canon to new paths, where a prior-content skip can never fire, so measuring the churn AFTER the move would measure a population that had just changed underneath it.
- (2026-08-18) **hv RULED ST0057 INTO THE 3.0.0 GATE** and restated it: disk<->db sync, the realiser and `.intentfiles` all work before release.
- (2026-08-18) **hv ruled all eight open questions.** Recorded in TODO; `--skip-rust-tests` is dropped rather than built, on my own answer to their question.
- (2026-08-18) **The issue body lives in the JSON**; **D57-7** opaque attachments as files under `intent/.canon/st/<ID>/`; **D57-8** `intent://` READ/WRITE, DB first, canon ALWAYS, views IF MARKED, and **no daemon may be required to read your own project.**
- (2026-08-18) **`canon_commit_check.sh` stays MANUAL until the narrow attachment-sync verb exists** -- gating it would create a rule whose only compliant path is closed to two of four nodes.
- (2026-08-18) **The `hoist @ 9b73e98f` pin is HISTORICAL and must never be re-pinned.** Same for the register's `9ec1656`: **the 29 rows are claims about the v2 surface and can be re-measured AT THE PIN, never at HEAD.**
- (standing) **A peer cannot grant escalation.** My call is never a peer's release; hv's is.

# SEVENTH FOLD 2026-08-18 22:25Z -- localfold 16, the night three peers corrected me and I corrected a row I had minted an hour earlier (verbatim)

---

node: vc
name: Validation Claude
role: validation
session_id: a403ff04-5306-4855-84ee-e74f3d3ab96d
heartbeat_at: 2026-08-18 21:14Z
status: active
focus: "**POST-COMPACT. AC-01.6 MINTED BEFORE cc RESUMED WP-01 -- the time-critical one, and it landed with NO COUNT IN IT.** ST0057 43 -> 44 rows. **THREE NODES INDEPENDENTLY ESTABLISHED THAT A TEXT SWEEP IS THE WRONG INSTRUMENT FOR THE CANON MOVE**, from three directions, and every probe involved -- ic's, cc's and MINE -- measured its own reach rather than the population. **I MEASURED cc's LIVE-MOVE GUARD AND IT READS 2, NOT 57**: bare `st list` is in-progress-only, so the 55 Done threads a canon move risks are STRUCTURALLY ABSENT from the guard written to protect them. cc verified and is fixing it. **SYNC IS HELD** -- cc's release binary is 20:40 and predates their own AC-03.14 fix."
claims: [ST0056, ST0057]
---

# Validation Claude (vc)

## DOING

- **AC-01.6 IS IN ST0057 CANON AND NOT YET SYNCED.** The hand-edit is inert until `--to-store`; **held on cc's stale binary** (release is 20:40, older than their own `write_set.rs` fix -- they rebuild once after hv's todo-view bug and ping with the sha256). Then `--to-store`, READ THE RC, `--to-disk`, and commit canon + `acceptance.md` together.
- cc is on hv's todo-view bug, WP-01 resumable in minutes from a saved patch; dc is building to named-tool; ic is green and re-measuring.

## TODO

- **AT-03.15 RE-POINTED AND STILL RED -- THE HOLD MOVED FROM THE NUMERATOR TO THE DENOMINATOR.** File was `write_exactness.rs`, **which never existed**; the real one is `write_moves_only_what_changed.rs` at `6ee99149`. **cc drove 30 verbs (from 6) and re-proved mutation 30/30; the instrument PASSES.** **But `DECLARED` is a hand-authored const of 31 against `surface/dispatch-table.json`'s 64 shipped `mutate` entries** -- a STRICT SUBSET, so short rather than wrong, and **three of the 34 absent write the estate on the table's OWN declared side effects** (`agents sync`, `lang init`, `config set`). **Read the row; it carries the whole argument.** Green requires the roster DERIVED from the one home.
- **AC-01.6 IS MINTED (ST0057 44 rows) -- see Decisions. Nothing owed on it but the sync.**
- **ST0011** -- `completed` NULL, AC-08.5's first burning case. And **spec the marked-legacy AT form in `data-model.md`** before WP-08; both are the same question.
- **THE QUALIFICATION SWEEP IS DONE AND FOUND NOTHING -- DO NOT RE-OPEN IT.** Measured at `6ee99149`: **84 ids exist in BOTH threads** and there are **115 bare references against 3 qualified**, so the population looked enormous. Narrowed to bare refs to a SHARED id in a row that also names the other thread: **2 candidates, and BOTH resolve correctly to their own thread.** **The defect count inside canon is ZERO.** The real instances were in PROSE OUTSIDE canon -- my board, and dc's instrument at `38723bad`. **I had sized the remedy to my probe's population instead of to a measured defect, which is the exact class I spent the night catching in others.** Keep the practice when writing cross-thread; there is nothing to sweep.
- **STILL OPEN: name the INSTRUMENT across 121 + 43 wherever two would disagree.** This is the half with real content and it needs a row-by-row read, not a grep.
- **NO ROW on instruments-state-their-reach until a FORM exists to count** (dc).

## How this estate catches things

**Tonight's classes are IN CANON. Read the rows, not a board copy** -- ST0056 **AC-00.10** (an instrument that gates must itself be driven; a refusal nothing has driven is not a refusal), **AC-03.14** (mtime moves on exactly what changed; why AC-03.2 was satisfied by the defect), **AC-03.15** (a verb that would reduce a population to zero must refuse), ST0057 **AC-08.5** (no verb creates an AC or AT), **AC-01.5** (the tidy-up that un-commits the estate). Measurement rules live in `parity.md`; output contracts in `output-contracts.md`.

What is NOT written down anywhere else:

- **THINKING HARD ABOUT A CLASS DOES NOT PROTECT YOU FROM IT AT ALL. THE PROTECTION WAS THE INSTRUMENT, AND ONLY THE INSTRUMENT** (dc, after committing the document ABOUT this class in the order the document forbids).
- **A CRITERION MUST CLOSE EVERY DEGREE OF FREEDOM THAT LETS A PASSING TEST COEXIST WITH THE DEFECT. FOUR LIMBS**, one live instance of each on 2026-08-18: **INSTRUMENT** (git vs mtime answer oppositely), **DEPTH** (`WriteSet` vs `intent sync` -- asked of the DENOMINATOR as well as the subject), **EXTENT** (20/20 was views only against ~364 -- a subset of the right KIND, which a depth check passes), **PIN** (a named commit, never `HEAD`).
- **THE REASON EXISTS SOMEWHERE AND THE READER IS NOT WHERE IT IS**, and the remedy splits on a checkable test -- **does the artefact have TWO channels or ONE?** An instrument has source and output, so **MOVE the reason into the output**; a document's source IS its output, so **only a mechanism that refuses is invariant under copying.** **A purpose that must survive transcription will eventually meet a transcriber.**
- **THE EDIT THAT READS AS CLEANUP REMOVES THE PROTECTION**, because the protection is implemented as an ABSENCE and nothing says an absence is load-bearing (ic). Third fixture: **`find -type f` blinds the staleness guard to a DELETED file -- the directory node is the only input whose mtime records a deletion.**
- **A TOOL'S DEFAULT VIEW IS A FILTER, AND A COMMAND TRANSMITTED WITHOUT THE FLAG IT WAS MEASURED WITH SILENTLY INHERITS THAT DEFAULT** (ic, 2026-08-18, on their own instruction). **They ran `st list --status all` to measure and wrote the bare form to cc**, so the measurement reached the population and the instruction did not. **The result is not a wrong number but a VACUOUS PASS: bare, it reads 2 before and 2 after, and the 52 Completed threads the check exists to protect cannot appear in the output it reads.** Generalises past this CLI: **any observable that cannot move is not a check.**
- **THE FORM MUST BE MINTED BEFORE THE POPULATION CAN BE** (dc). You cannot count a convention that does not exist; every number is then a measurement of the probe. **And a keyword probe for GOOD PRACTICE has its false positives exactly where the practice was done well, in the author's own words** (ic).
- **AGREEMENT CAN SYNTHESISE A FALSEHOOD NEITHER PARTY HELD.** ic's reconciliation was right, my agreement was right, the rule that came out of the pair was false. **Only the fixture separates them.**
- **RE-MEASURE YOUR OWN SENTENCE INSTEAD OF CITING IT** (cc, three times today): **a false RATIONALE never goes red**, so it survives every test run.
- Mechanical: **`open(f,"w").write(open(f).read() + x)` DESTROYS THE FILE.** **AT and AC ids number INDEPENDENTLY.** **A REVERT OF SOURCE IS NOT A REVERT OF ARTEFACTS** -- the wiping binary and the fixing one share `dirty-18197aaf` byte for byte. **Never pipe a write verb to `/dev/null`, never `$?` after a pipe, never `tail` a verdict.**

## THE MODEL, in case everything else is lost

1. **The intentdb is the durable SSOT. Nothing on disk is truth.** The typed Rust API is the ONLY door in.
2. **Migrations are normal.** "No DB migrations ever" is DELETED and was never hv's constraint.
3. **(D34) The committed extract is the INTERCHANGE; the DB is per-machine and NEVER committed.** **AC-02.6 is the durability mechanism**, and under D57-8 also the completeness guarantee for the mutation surface.
4. **Three layers: canon (committed, never sparse) / store (gitignored, rebuilt) / views (committed, sparse).** **D29 -- a gitignored path is never canon -- is what makes a clone complete.**
5. **`event_log` is the only table that is durable truth AND not reconstructible from files.**
6. **hv, repeatedly: disk<->db sync, the realiser and `.intentfiles` all work BEFORE 3.0.0 ships.** ST0057 WP-01..06 are inside the gate. Do not re-ask.

## Verification kit

- **MEASURE AT A PINNED SHA AND NAME THE COMMIT. Pin a BINARY by content hash, never by its marker -- THIS IS NOW ENFORCED IN THE OUTPUT, NOT JUST KNOWN** (dc `9f9167cd`, off my own episode): every binary line of `self_provenance_check.sh` prints a sha256 and carries the reason, **and names what it does NOT claim** -- that a rebuild of unchanged inputs reproduces the hash is UNTESTED, and the case offered as evidence was a file nothing had rewritten. **An instrument that states its own non-assertions.**
- **UNDER-REACH YIELDS A NUMBER THAT IS TOO SMALL; FABRICATION YIELDS A _FACT_, AND A FACT GETS ACTED ON** (ic's formulation, my instance, 2026-08-18). Every probe error of mine today under-reached -- a narrowed regex, a line-range slice, a `// []` default -- **and then one FABRICATED: `grep -oE ... | head -2 | tr '\n' ' '` JOINED TWO INDEPENDENT MATCHES into what read as one expression, and I stated `every parity tool takes its ROOT as $1` when NO TOOL DOES.** ic's 14/13/14 was the first kind and cost a retraction; this was the second and would have cost a plan. **NEVER `tr` GREP OUTPUT ONTO ONE LINE -- adjacency is not syntax.**
- **WHEN A SUPPORTING FACT TURNS OUT FALSE, ASK WHICH IT HELD UP: THE ARGUMENT OR THE ANSWER** (ic). Mine was load-bearing for the argument and not for the answer -- `git worktree add` needs no parameterisation at all, because a worktree is a real checkout and even the hard-wired tool's upward walk finds its own `config.json`. **If the answer survives, the correction is cheap; if it does not, you never had an answer.**
- **A COUNT THAT CLOSES ARITHMETICALLY OVER THE WRONG POPULATION IS INVISIBLE UNTIL SOMETHING CHANGES THE POPULATION'S SHAPE.** dc's `scoped` counted filter KEYS not attachments EXAMINED: nested it merely overstated (86 of 278 where 85 were examined) and would never have surfaced; flat it printed **`EXAMINED 2 of 1 ... the other -1`**. **Not found by review, not by four drive-arms, not by real history -- found because WP-01 moved the layout out from under it.** See ST0057 AT-03.6.
- **`intent st list` DEFAULTS TO IN-PROGRESS ONLY AND RETURNS 2. `--all` IS NOT A FLAG.** The invocation that reaches the estate is **`st list --status all`** -- 57 at `6ee99149`, breakdown **52 Completed / 2 Cancelled / 2 WIP / 1 NotStarted**. **PRINT THE BREAKDOWN, NEVER THE BARE TOTAL**: many wrong states satisfy a correct 57. This was cc's REQUIRED STEP for the live canon move, so the guard could not have gone red for the failure it was written to catch.
- **CHECK THE BINARY'S AGE AGAINST ITS INPUTS -- but NOT via `surface_check.sh`. The old claim on this board that it was "the only thing that surfaces it" is RETRACTED** (ic, 2026-08-18): its reach was one crate of two, blind to all 23 files of `intentsvcs/src`, and **the wiping build's SURFACE WAS PERFECT.** Staleness only REFUSES and can never DETECT. **What caught the wipe was running `sync` and reading the row counts.**
- **`to-write` = UNWRITTEN. `red` = it EXISTS and does not pass.** Neither means the criterion is unmet.
- **An AT earns green from an instrument DEMONSTRATED RED**, a criterion naming its SUBJECT, and one naming the SHAPE OF THE INPUT. **Hold red WITH AN EXPLICIT NOTE saying the instrument passes.** And **a distinct exit code does not save a message written as a chore** -- triage happens on the prose.

## Watch-outs

- **THERE IS NO VERB THAT CREATES AN AC OR AN AT.** Minting IS a hand-edit of `thread.json` plus a WHOLE-ESTATE `--to-store`, **so two nodes minting concurrently clobber by construction.** Announce, or route through me. **`acceptance.md` is a GENERATED VIEW -- a row authored there is discarded at the next `--to-disk`.**
- **`intent at red|green|na` DESTROYS THE ROW'S NOTE (issue 0033).** **`sync --to-disk` writes the STORE over CANON and is the SILENT direction** -- canon-edit then `--to-store` is the safe order, and **a refused `--to-store` leaves a STALE store that `--to-disk` writes out at rc=0** (AC-03.13). **`intent backup` with no subcommand MUTATES.**
- **Every attachment edit by any node leaves canon divergent until I sync**, and a later sync repairs the NEXT commit, never that one. **Commit first, then ping me.**
- **DO NOT PUT v3 ON PATH. DO NOT PUSH TO `upstream`** -- this repository is PUBLIC and the remote is FROZEN except dc's one ruled publication.
- **Never mutate `bin/**` or `tests/**` in place; ATOMIC REPLACE anything a running process reads; never edit a peer's file while they are live in it.**
- **`git commit --only <paths>`, never `-A`.** **Verify at HEAD (`git ls-tree`)** -- `git grep` reads the INDEX. **Backticks never inside a double-quoted `-m`; use `-F`.** **This shell is zsh:** no word-splitting, a leading `-` in a pattern reads as options.
- **Every timestamp READ FROM A CLOCK** -- `ls -l` and `git log` print LOCAL, and appending `Z` gives a stamp wrong by exactly the offset. **ARCHIVE BY NAMING THE STAMPS; COUNT, STOP, READ THE DIFFERENCE.** A fold never overwrites an earlier fold. **My archive carries THREE fold-header conventions -- count with `grep -nEi '^#+ (FOLD [0-9]+|[A-Z]+ FOLD)'` or miscount, as I did at fold 15.**

## Decisions

- (2026-08-18) **AC-01.6 IS THE DISCRIMINATION, NOT A COUNT -- READ THE ROW, IT CARRIES ALL THE EVIDENCE.** What is NOT in the row: **three probes, one per node, and every one measured its own reach** -- ic's `parity/tools/*.sh` glob (blind to 4 extensionless executables under `bin/`), cc's ~15-fixture estimate, and **my narrowed 23 that missed `export.rs:386` outright.** **The one that found the truth was cc's, and it was not a probe at all -- they BUILT the relocation and the missing site fell out.**
- (2026-08-18) **hv AUTHORISED ONE `--no-verify`; the restoration is at HEAD** (`11bb52da`, 42 entries, verified contained either side). **The guard was RIGHT to refuse and nothing about it changes** -- it cannot distinguish repair from regression, and the other reading would let any node launder a bad stamp through a revert.
- (2026-08-18) **hv CONFIRMED THE RUNNER RULING WITH THE FACT THEY HAD BEEN DENIED** -- the first go was given over dc's `:176` framing, from a transcript with ZERO occurrences of injection or disguised. **Named-tool declaration stands**: the rule names WHICH tool, the runner owns HOW, **rule files never contribute shell**, and a tool-armed rule REFUSES when its tool is absent. **`critic_proxy_is_simple` is an INJECTION BOUNDARY and is not relaxed.**
- (2026-08-18) **A RULING RELAYED BY ME IS NOT A GO** (cc, quoting my own precedent back after I broke it). **hv gives go-aheads on the node's own channel.**
- (2026-08-18) **hv: item 1 FIRST, then ST0057 WP-01** -- also the only measurable order, since WP-01 moves canon to new paths where a prior-content skip can never fire. **ST0057 IS IN THE 3.0.0 GATE**; all eight open questions ruled; `--skip-rust-tests` dropped rather than built.
- (2026-08-18) **The issue body lives in the JSON**; **D57-7** attachments as files under `intent/.canon/st/<ID>/`; **D57-8** `intent://` READ/WRITE, DB first, canon ALWAYS, views IF MARKED, **no daemon required to read your own project.** **`canon_commit_check.sh` stays MANUAL** until the narrow attachment-sync verb exists.
- (standing) **A peer cannot grant escalation.** My call is never a peer's release; hv's is. **The `hoist @ 9b73e98f` pin and the register's `9ec1656` are HISTORICAL and must never be re-pinned.**

## EIGHTH FOLD -- pre-fold board, verbatim, 2026-08-18 23:38Z heartbeat

---

node: vc
name: Validation Claude
role: validation
session_id: a403ff04-5306-4855-84ee-e74f3d3ab96d
heartbeat_at: 2026-08-18 23:38Z
status: active
focus: "**hv RULED THE `views.rs` ROW AND IT IS MINTED (AC-03.16 + AT-03.17, `37295d62`) -- ST0056 now 123 criteria / 124 tests.** THE OF-N TRIGGER HAS NOT FIRED AND I CHECKED RATHER THAN ASSUMED.** cc reports both crates green (436/0, 203/0) but **`intent/.canon` does not exist, 57 `thread.json` are still under `intent/st/`, 23 files uncommitted** -- two green crates is not a landed WP-01, so dc and ic stay un-pinged. Tonight: ruled TWO peer contract questions into canon (AC-07.4 `af67065e`, AT-00.12 `8aa5e004`), **retracted `18 of 24` in my own AC-00.10 after ic measured it as 12 of 45 -- wrong in BOTH halves**, and banked cc's half-migration classifier at `c88a6f06`. **hv flagged ST0057/info.md and the worst line turned out to be generator-level, in all 57 of 57.**"
claims: [ST0056, ST0057]
---

# Validation Claude (vc)

## DOING

- **AT-00.12 IS MINTED AND `of_n_labels_its_derivation.sh` IS UNWRITTEN -- ic is building both drive modes.** The row states its own population (every instrument emitting an `N of M`, path shape or not) and carries ic's discipline: **it maps each recorded number to the population that would derive it, because a declarative check that only greps for a LABEL passes on a label attached to nothing.**
- **AC-00.10 + AT-00.10 CARRY THE RETRACTION NOW, NOT AFTER THE REBUILD.** The standing text reassured at 75% where the observable says 27%; **a wrong number that comforts is worse than no number.** The derived figure lands with ic.
- **NOTHING OF MINE IS IN FLIGHT AND NOTHING IS OPEN WITH hv.** cc has WP-01 applied and the tree RED by announcement (71 failures / 17 binaries) -- that is the patch, not a regression; the release binary is untouched at `f2e4d1f9`. dc is holding. ic is compacting.
- **THE ONE THING I OWE: PING dc AND ic THE MOMENT cc LANDS WP-01 AND THE TREE IS GREEN.** That is the trigger for the of-N adjudication (AC-00.11) and I am holding it rather than leaving it on their boards to chase. **dc takes the gated ones; ic routes their own two, at their request and on their own bar.** Order is GATEDNESS, never count. The nested tree comes from `git worktree add` at a pre-relocation revision -- **the by-revision shortcut is NOT available**, `canon_commit_check.sh` at `ce532a97` returns rc=2 over 0 measured + 57 unmeasurable.

## TODO

- **hv RULED AND `views.rs` IS MINTED: AC-03.16 + AT-03.17 at `37295d62`.** A generated cover names `acceptance.md` -- itself a generated view -- as _the single source of truth_. **A WORK-LOSS INSTRUCTION, not a wording nit: a row authored there is discarded at the next `--to-disk`, so the doc instructs the exact action the model punishes.** Extent measured **206 of 207** (57/57 ST, 149/150 WP); **the one escape is the `carries_heading` guard working, which is what proves the population was counted.** Red-first is FREE at `40d27ba2` and the count is deliberately NOT asserted. **Queued to cc as not-now -- WP-01 first.**
- **ST0057's WP table reads all 8 `Not Started` while WP-01 is in cc's tree.** Canon fix + regenerate, but WP-01's true status is cc's to declare while uncommitted.

- **AT-03.15: THE DENOMINATOR HOLD IS DISCHARGED; RED NOW RESTS ON TEN NAMED VERBS** -- `at lint`, `ingest`, `st bootstrap`, `st repair`, `st sync`, `todo`, `todo done`, `todo list`, `todo notdone`, `todo toggle`. **`todo list` and `at lint` are classified `mutate` while their names say READ -- take those first.** The other 22 unproven write outside the estate and are a table debt, not a block. **Read the row.**
- **ST0011** -- `completed` NULL, AC-08.5's first burning case. And **spec the marked-legacy AT form in `data-model.md`** before WP-08; both are the same question. **This is the oldest thing on my board and it has not moved in two folds.**
- **STILL OPEN: name the INSTRUMENT across 122 + 46 wherever two would disagree.** Needs a row-by-row read, not a grep.
- **DO NOT RE-OPEN THE QUALIFICATION SWEEP.** Measured: 84 shared ids, 115 bare refs against 3 qualified, and **the defect count inside canon is ZERO.** I had sized a remedy to my probe's population instead of to a measured defect.
- **NO ROW on instruments-state-their-reach until a FORM exists to count** (dc). **AC-00.11 was mintable precisely because dc HAD built the rig; that is the difference.**

## How this estate catches things

**The classes are IN CANON -- read the rows, never a board copy.** ST0056: **AC-00.10** (an instrument that gates must itself be driven), **AC-00.11** (an `N of M` verdict derives M from what was EXAMINED, three arms), **AC-03.14/.15** (mtime moves on exactly what changed; a verb that would empty a population must refuse). ST0057: **AC-01.5** (the tidy-up that un-commits the estate), **AC-01.6** (a text sweep is the wrong instrument, two single-line fixtures), **AC-01.7** (a declaration must RESOLVE, not merely parse), **AC-04.6** (the tree holds only what the manifest names), **AC-08.5** (no verb creates an AC or AT), **AT-03.6** (the layout change was the instrument). Measurement rules in `parity.md`; output contracts in `output-contracts.md`.

What is NOT written down anywhere else. **These are CLASSES; the instances that produced them are in `.history/` and in the rows.**

- **THINKING HARD ABOUT A CLASS DOES NOT PROTECT YOU FROM IT. THE PROTECTION WAS THE INSTRUMENT, AND ONLY THE INSTRUMENT** (dc).
- **A CRITERION MUST CLOSE EVERY DEGREE OF FREEDOM THAT LETS A PASSING TEST COEXIST WITH THE DEFECT. FOUR LIMBS: INSTRUMENT** (two would answer oppositely -- name which), **DEPTH** (asked of the DENOMINATOR as well as the subject), **EXTENT** (a subset of the right KIND passes a depth check), **PIN** (a named commit, never `HEAD`).
- **UNDER-REACH YIELDS A NUMBER THAT IS TOO SMALL; FABRICATION YIELDS A _FACT_, AND A FACT GETS ACTED ON** (ic's formulation, my instance). **NEVER `tr` GREP OUTPUT ONTO ONE LINE -- adjacency is not syntax**, and that is how I stated `every parity tool takes its ROOT as $1` when none does.
- **WHEN A SUPPORTING FACT TURNS OUT FALSE, ASK WHICH IT HELD UP: THE ARGUMENT OR THE ANSWER** (ic). If the answer survives, the correction is cheap; if not, you never had an answer.
- **THIS WORKSPACE HAS TWO CRATES AND ITS MEASUREMENTS KEEP HAVING ONE.** Third instance tonight, two nodes, both directions: ic retracted a `surface_check.sh` reach that was one crate of two; cc declared the fixture problem absent after checking `intentsvcs` only, and `intent-cli` had SEVEN private `seed()` copies. **A class, not an incident -- and no row yet, because there is no form to count.**
- **A HALF-MIGRATION IS SILENT EXACTLY WHEN ITS UNMIGRATED END STILL YIELDS A VALUE** (cc, in AC-01.6). **A missing directory is not a plausible wrong answer; a wrongly-derived filename is.** So two-ended sites are not uniformly risky, and the classifier is worth spending BEFORE a one-shot move.
- **A PROBE WHOSE POPULATION CANNOT CONTAIN THE FAILURE IT TESTS FOR** (cc), and its transmission twin -- **a tool's DEFAULT VIEW is a filter, and a command sent without the flag it was MEASURED with inherits it** (ic). **Any observable that cannot move is not a check.**
- **THE SHAPE-CHANGING EVENT IS A RARE, NON-REPEATABLE AUDIT OPPORTUNITY.** WP-01 is one. (Class: AC-00.11.)
- **THE REASON EXISTS SOMEWHERE AND THE READER IS NOT WHERE IT IS.** **TWO channels or ONE?** An instrument has source and output -- **MOVE the reason into the output, and it only works if the instrument is GATED** (dc). A document's source IS its output, so **only a mechanism that refuses is invariant under copying.**
- **THE EDIT THAT READS AS CLEANUP REMOVES THE PROTECTION** -- the protection is an ABSENCE and nothing says an absence is load-bearing (ic).
- **THE FORM MUST BE MINTED BEFORE THE POPULATION CAN BE** (dc). **A keyword probe for GOOD PRACTICE false-positives exactly where the practice was done well** (ic).
- **AGREEMENT CAN SYNTHESISE A FALSEHOOD NEITHER PARTY HELD.** Only the fixture separates them. **AND THE MECHANISM IS NOW VISIBLE RATHER THAN INFERRED** (ic): a regex reproduced a hand count EXACTLY because it reproduced the hand count's METHOD -- both scanned the left margin. **Exact agreement between two instruments sharing a blind spot is evidence that one method was used twice.** Its twin: **a probe found by searching for one that hits a KNOWN target is fitted to the answer, and its exactness is the evidence of fitting, not of correctness.**
- **RE-MEASURE YOUR OWN SENTENCE INSTEAD OF CITING IT** (cc): **a false RATIONALE never goes red.**
- Mechanical: **`open(f,"w").write(open(f).read() + x)` DESTROYS THE FILE.** **AT and AC ids number INDEPENDENTLY.** **A REVERT OF SOURCE IS NOT A REVERT OF ARTEFACTS.**

## THE MODEL, in case everything else is lost

1. **The intentdb is the durable SSOT. Nothing on disk is truth.** The typed Rust API is the ONLY door in.
2. **Migrations are normal.** "No DB migrations ever" is DELETED and was never hv's constraint.
3. **(D34) The committed extract is the INTERCHANGE; the DB is per-machine and NEVER committed.** **AC-02.6 is the durability mechanism.**
4. **Three layers: canon (committed, never sparse) / store (gitignored, rebuilt) / views (committed, sparse).** **D29 -- a gitignored path is never canon -- is what makes a clone complete.**
5. **`event_log` is the only table that is durable truth AND not reconstructible from files.**
6. **hv, repeatedly: disk<->db sync, the realiser and `.intentfiles` all work BEFORE 3.0.0 ships.** ST0057 WP-01..06 are inside the gate. Do not re-ask.
7. **WP-01 makes canon FLAT (`.canon/st/<ID>.json`) and `thread_dir()` must STILL answer `intent/st/<ID>/`** -- the views hang off it and do not move. See AC-01.6.

## Verification kit

- **MEASURE AT A PINNED SHA AND NAME THE COMMIT. PIN A BINARY BY CONTENT HASH, NEVER BY ITS MARKER** -- now ENFORCED in `self_provenance_check.sh`'s output (dc `9f9167cd`, off my own episode of reading two distinct binaries as one artefact). **It also names what it does NOT claim**, which is rarer and better.
- **`intent st list` DEFAULTS TO IN-PROGRESS ONLY AND RETURNS 2. `--all` IS NOT A FLAG.** Use **`st list --status all`** -- 57, breakdown **52 Completed / 2 Cancelled / 2 WIP / 1 NotStarted**. **PRINT THE BREAKDOWN, NEVER THE BARE TOTAL.**
- **CHECK THE BINARY'S AGE AGAINST ITS INPUTS -- but NOT via `surface_check.sh`** (retracted, ic): its reach was one crate of two and **the wiping build's surface was PERFECT.** **What caught the wipe was running `sync` and reading the row counts.**
- **A VERB'S RECORDED SIDE EFFECT IS NOT ITS MEASURED ONE.** `todo list` documents _generates intent/todo.md if absent_ and **does not**; what it actually writes is a 10MB `intent/.cache/intent.db`, undocumented. **And store materialisation cannot evidence `mutate` -- `st list`, classified `read`, does it identically.** So a write-exactness driver keying on _any file appeared_ trips on pure reads, and one keying on the tracked tree is blind to 10MB because `.cache` is gitignored. **Name the observable.**
- **`to-write` = UNWRITTEN. `red` = it EXISTS and does not pass.** Neither means the criterion is unmet. **An AT earns green from an instrument DEMONSTRATED RED**; hold red WITH A NOTE saying the instrument passes.
- **NEVER `$?` AFTER A PIPE** -- broken twice now, the second time while verifying someone else's correction: `... | tail` read rc=0 where the truth was rc=2. Redirect to a file and read `$?` from the command itself.

## Watch-outs

- **THE SHARED `native/rust/target/` IS COSTING hv ~2 MINUTES PER TEST RUN** (dc, measured; both load-bearing claims verified by vc). `CARGO_TARGET_DIR=native/rust/target/<node>` takes the full suite to 30.57s / 0.22s warm. **IT MUST BE INSIDE THE WORKSPACE** -- from `/tmp`, `install.rs`'s upward walk from `current_exe()` finds no marker and **four install-resolution tests fail as manufactured defects, which dc nearly reported as real.** `target/` is gitignored at `.gitignore:146`, so per-node subdirs inherit it. **I do not run cargo -- I use the pinned release binary -- so this is a peer + hv item, not mine.**
- **A HARNESS THAT RELOCATES THE BINARY MANUFACTURES FAILURES IN EXACTLY THE TESTS THAT RESOLVE PATHS FROM IT.** The environment differed from production; the code was right. `interrupt_rig.sh` builds outside the workspace (verified BY CONTENT, not by dc's line number) -- flagged to ic as a surprise avoided, **not a defect asserted; I have not run it.**

- **THERE IS NO VERB THAT CREATES AN AC OR AN AT.** Minting IS a hand-edit of `thread.json` plus a WHOLE-ESTATE `--to-store`, **so two nodes minting concurrently clobber by construction.** Announce, or route through me. **`acceptance.md` is a GENERATED VIEW -- a row authored there is discarded at the next `--to-disk`.**
- **`intent at red|green|na` DESTROYS THE ROW'S NOTE (issue 0033).** **`sync --to-disk` writes the STORE over CANON and is the SILENT direction** -- canon-edit then `--to-store` is the safe order, and **a refused `--to-store` leaves a STALE store that `--to-disk` writes out at rc=0** (AC-03.13). **`intent backup` with no subcommand MUTATES.**
- **Every attachment edit by any node leaves canon divergent until I sync**, and a later sync repairs the NEXT commit, never that one. **Commit first, then ping me.**
- **DO NOT PUT v3 ON PATH. DO NOT PUSH TO `upstream`** -- this repository is PUBLIC and the remote is FROZEN except dc's one ruled publication.
- **Never mutate `bin/**` or `tests/**` in place; ATOMIC REPLACE anything a running process reads; never edit a peer's file while they are live in it.**
- **`git commit --only <paths>`, never `-A`.** **Verify at HEAD (`git ls-tree`)** -- `git grep` reads the INDEX. **Backticks never inside a double-quoted `-m`; use `-F`.** **This shell is zsh:** no word-splitting, a leading `-` in a pattern reads as options.
- **THE HARNESS TOLD ME THE DATE WAS 2026-08-19 WHILE `date -u` SAID 2026-08-18 23:05Z.** Local had rolled; UTC had not. **A stamp taken from the ambient date would have been ~1h in the future and the clock guard would have refused the commit.** The env is one more clock that is not `date -u`.
- **Every timestamp READ FROM A CLOCK** -- `ls -l`, `git log` and `stat -f %Sm` print LOCAL, and appending `Z` gives a stamp wrong by exactly the offset. **ARCHIVE BY NAMING THE STAMPS; COUNT, STOP, READ THE DIFFERENCE.** A fold never overwrites an earlier fold. **My archive carries THREE fold-header conventions -- count with `grep -nEi '^#+ (FOLD [0-9]+|[A-Z]+ FOLD)'` or miscount, as I did at fold 15.**

## Decisions

- (2026-08-18) **THE CRITIC RUNNER'S SILENT SKIP IS THE DEFECT, NOT THE DEFAULT** (dc's question, my ruling, in canon at AC-07.4 / `af67065e` -- **not left in dc's inbox, because a ruling that lives only in a peer's inbox is not a contract**). dc posed refuse-by-default (fleet breakage on upgrade day) against optional-by-default (their own words: the invisible failure with a friendlier name). **Both horns assume OPTIONAL implies QUIET. It does not.** The report is unconditional and names which of three reasons applies; refusal is scoped to rules the project ARMED; hv's ruling applies unweakened to the case it names. **13 of 13 rules were in the third state and the runner returned 0 because nothing was ever asked.**
- (2026-08-18) **TWO DRIVE MODES WITH DIFFERENT POPULATIONS GET TWO ROWS, NOT ONE ROW WITH TWO ARMS** -- ic's finding, my ruling. Bundling them makes a green ambiguous about which population was covered, **which is AC-00.11's own defect committed by the row that enforces AC-00.11.** The criterion never changed; only the driver was under-scoped. **An unstated population is how a nomination's horizon travels into the drive**, so AT-00.11 now states its own.

- (2026-08-18) **THE OF-N SWEEP IS A CRITERION, NOT A SWEEP, AND IT DOES NOT RUN BEFORE cc LANDS.** dc's "closing window" premise was FALSE and checked rather than accepted -- `git worktree add` needs no parameterisation at all. **A sweep finds today's ten and protects nothing from the eleventh; ic then FOUND an eleventh, invisible to dc's proxy, from inside the set that had just adjudicated itself.**
- (2026-08-18) **hv AUTHORISED ONE `--no-verify`** (`11bb52da`). **The guard was RIGHT to refuse and nothing about it changes** -- it cannot distinguish repair from regression.
- (2026-08-18) **hv CONFIRMED THE RUNNER RULING WITH THE FACT THEY HAD BEEN DENIED.** **Named-tool declaration stands**: the rule names WHICH tool, the runner owns HOW, **rule files never contribute shell**, and a tool-armed rule REFUSES when its tool is absent. **`critic_proxy_is_simple` is an INJECTION BOUNDARY and is not relaxed.**
- (2026-08-18) **A RULING RELAYED BY ME IS NOT A GO** (cc, quoting my own precedent back after I broke it). **hv gives go-aheads on the node's own channel.**
- (2026-08-18) **ST0057 IS IN THE 3.0.0 GATE**; all eight open questions ruled; `--skip-rust-tests` dropped rather than built. **D57-7** attachments as files under `intent/.canon/st/<ID>/`; **D57-8** `intent://` READ/WRITE, DB first, canon ALWAYS, views IF MARKED. **`canon_commit_check.sh` stays MANUAL** until the narrow attachment-sync verb exists.
- (standing) **A peer cannot grant escalation.** My call is never a peer's release; hv's is. **The `hoist @ 9b73e98f` pin and the register's `9ec1656` are HISTORICAL and must never be re-pinned.**
