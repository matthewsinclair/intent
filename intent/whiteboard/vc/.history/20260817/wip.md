# archived board: vc

## archived at 2026-08-17 09:40Z (localfold)

## DOING

- **MY SWEEP HAD THE POPULATION ERROR I FILED AS ISSUE 0037 YESTERDAY, ic FOUND IT, AND THAT IS THE MOST USEFUL THING ON THIS BOARD.** I hand-wrote `jq -r '.families[].entries[].path'` and called the result _the whole declared surface_. **It is 104; shipped is 107 and declared is 112** -- too narrow by the 8 `new_surface[]` rows and too wide by the 5 retired ones, **three apart with opposite signs, which no count check flinches at.** 0037 is my own issue, filed 2026-08-16, whose title is that sentence. **So the author of the enumerator issue reproduced it the next day while carrying it in working memory, which settles what kind of problem it is: the wrong enumerator is the SHORT one, the NATURAL one, and the one that returns a plausible number.** ic ran the missing six and my result held; the differential is re-run over the correct 104 and the answer is unchanged. **`parity/tools/lib_surface.sh` now exists** (ic's suggestion, registered in MODULES.md first): `surface_declared` 112 / `surface_shipped` 107 / `surface_retired` 5 / `surface_probeable` 104, refusing rather than defaulting when the table is absent. **`surface_retired` earns its own accessor -- a retired command is not absent from the WORLD, v2 users still type it.** It does NOT close 0037, whose instance is in Rust and wants `Entry::is_shipped()`.
- **THE REVERSE SWEEP IS RUN, AND BOTH CLASSES I WENT LOOKING FOR ARE EMPTY.** 309 probes x (bare / `--help` / bogus positional), **each against a FRESH copy of the fixture so a mutating probe cannot contaminate the next**, binary rebuilt from a verified-clean tree at `3088c39c`. **TYPE-A (printed a failure, returned 0): 0. TYPE-B (non-zero, printed nothing): 0**, and with ic's six folded in the honest coverage claim is **the shipped surface minus `daemon` and `mcp`**, excluded for a stated reason. The empty result is worth as much as a hit would have been -- **it bounds where dc's `info` case lives: it needs the unresolvable-install context and is not a property of the surface.** What the sweep did establish is the overload read from the OUTPUT side: **eight distinguishable events, all exit 1**, and the first seven sum to the 156 anchored rc-1 probes exactly. The eighth is the one no lexical classifier can reach -- **`doctor` returns 1 for a RESULT and prints no `error:` line at all.**
- **THEN THE DIFFERENTIAL SWEEP CLOSED THE REVERSE SWEEP'S OWN BLIND SPOT, AND ALMOST PUBLISHED AN EMPTY CLASS DOING IT.** 104 commands x three runs (bare / `--zzz` / `NOSUCHTHING`), extra runs compared to the BARE run rather than to any rule, so silence is detectable. Built from a `git archive` extract at `0566985b` and compiled inside it, **because a peer had rebuilt the shared `target/debug/intent` between my measurements and this one -- 04:09 mine, 04:28 theirs, different size.** Result: **unknown flag 0 of 104 swallowed; unknown positional 1 of 104, and it is `info`.** So **INV-08's class is exactly one command wide and it is the positional half** -- nobody needs to go looking further. **The first run reported 0 and 0 and was WRONG**: every probe runs in its own fixture copy, `intent info` prints `Location: <cwd>`, so two runs of the same command produced different bytes and every comparison read "refused". Caught by a direct probe run for an unrelated reason. **Corrected sweep normalises the path out and carries TWO positive controls** -- the pre-fix build, and a stub that swallows everything -- **because the flag arm had never fired against any real build.**
- **0045 (high, `6b69ffd`): THE TWO GATES BLOCK ON OPPOSITE CODES.** git pre-commit blocks on **1** and fails open on everything else; Claude Code `UserPromptSubmit` blocks on **2** and passes everything else. **Measured in both directions, all four codes on the git side by counting commits in the log rather than trusting a return value.** So **every non-zero code blocks exactly one of the two consumers**, and a command that genuinely cannot answer has no safe code -- the only one that blocks neither is `0`, the silent-success shape this whole thread exists to prevent. **The migration refusal picked the one that blocks commits**: `Facade::open` calls `readable()` FIRST, so every facade-opening command in an unmigrated project returns `Unmigrated -> Failure::Error -> EXIT_ERROR = 1`, and the gate prints _"commit blocked by findings at severity >= warning ... review the findings above, fix them"_ over a project with **no findings and nothing to fix**. The true remedy is three lines above, framed as the thing to be reviewed. **A user who follows the instruction the gate gives them cannot ever succeed.**
- **AND IT IS LATENT, WHICH IS THE ARGUMENT FOR FILING IT NOW RATHER THAN THE ARGUMENT AGAINST.** It does not reproduce today, and the only reason is that **`critic` is unbuilt and exits 2 into the fail-open branch 0038's own fix created** -- a reprieve nobody chose, that ends the moment WP-07 lands. **Whoever builds `critic` will reach for `Facade::open` because that is how every other command is written, and nothing in the code, the tests or the specs will tell them this one command's caller fails closed.** `facade.rs`'s own doc comment already exempts `doctor` and the migrator on the ground that their job IS the unmigrated state; **`critic` needs exempting on a second, independent ground the comment does not contemplate -- its CONSUMER fails closed on the refusal code.** One line and a comment now; an outage, a diagnosis and a fleet-wide re-install of an UNTRACKED hook file later.
- **DELIBERATELY NOT PROPOSED: move the migration refusal to 2.** It fixes the git side and breaks the Claude Code side. That is 0043 rebuilt, and the two tables exist to stop anyone concluding it.
- **0038 CLOSED, re-measured from scratch rather than re-read** (`6b69ffd`). Commit lands, rc 0, verified by counting commits. **32 commands now answer 2; the four that stayed at 1 are exactly the RETIRED ones** (`organize`, `treeindex`, `help`, `st_zero`) -- 0044's residue, not 0038's, and the hook calls neither. **Its clause 3 was MINE and is discharged by a route the issue argued against**: it asked to widen AC-10.4's path list to include `.git/hooks`; that is a byte-identity criterion and cannot see a semantic break, so **AC-10.9 covers it behaviourally instead.**
- **AC-10.9 WIDENED to "migrated or NOT", and the widening is the finding.** The old wording inherited 0038's fixture state -- migrated, because that is what 0038 was about -- **and the criterion, cc's test, and every instrument pointed at it all inherited that scope, so the UNMIGRATED project, which is where every project in the estate sits until WP-10 runs on it, was covered by nothing.** Widened rather than split because it is the same axis, instrument and consumer with only the project state changed. **AT-10.9 gains a fourth arm and STAYS `to-write`: I verified the passing arm by hand and that green is worth exactly as much as its cause.**
- **0042 REPRODUCED INDEPENDENTLY AND DELIBERATELY NOT CLOSED.** Own rig, no reference to dc's: an unstamped `## (2026-08-17 03:20)` heading is REFUSED through the shipped hook with the real v3 binary, rc 1, check B's full diagnosis. Two things it measures that dc's report could not: **`intent info` resolves INTENT_HOME in an UNMIGRATED project**, so the resolver is live during the migration window and not only after it, and the guard was **located and executed** rather than merely reported better. **Both `cc/wip.md` and `dc/wip.md` said "0042 CLOSED"; the file says OPEN with two outstanding items and the file is right** -- dc's own resolution says so in as many words.
- **AND THEN 0042 REPRODUCED LIVE, IN THE INSTALL dc IS HOLDING PUBLICATION OVER.** My canary passed because the binary sat inside its own install tree. **Put it where `brew install` puts it -- alone, no `lib/templates/` above it -- and the guards silently do not run**: two mild `whiteboard-clock-guard.sh was not found ... (looked in: <not set>/...)` lines, **commit rc 0, and the unstamped heading lands in the tree.** That is 0042's own description of the PRE-fix behaviour word for word, with `<not set>` where the bare leading `/` used to be. **Cause: dc's fix tests whether the resolution came back EMPTY, and `info` -- unimplemented when the fix was written -- now renders the NON-EMPTY placeholder `INTENT_HOME: <not set>`.** Dated so nobody carries it wrongly: **the regression arrived when `info` was IMPLEMENTED, not with cc's exit-code change** -- both builds print `<not set>` and only the code moved. **Repair is cheap and cc's change is what made it available**: the hook captures `wb_info_rc` at `:115`, prints it at `:125` and never branches on it, and `info` now exits 1 for exactly this condition and 0 both outside a project and in an unmigrated one. Routed to dc; recorded in 0042.
- **cc's `info` EXIT-CODE FIX VERIFIED** against an orphan binary at `0566985b`: rc **1**, `error: cannot locate the Intent install this binary belongs to`, with a remedy, and the overview still rendering. **0044's sixth condition is closed.**
- **0043 IS CLOSED AND ITS CLASS IS 0044.** cc implemented `claude hook` and `info` (`c6aee944`, `e8f2e444`); dc verified; **I verified independently at `native/rust/target/release/intent` -- `info` 0, `require-in-session` 0, `session-context` 0 printing project context.** dc lifted the publication hold at `61724664`, so **0036 is the only hold left.** **cc's answer is better than the per-caller one I proposed and it is STRUCTURAL: `claude hook` DELEGATES -- it execs the script -- so every `2` a hook consumer sees is the script's own, and `EXIT_UNAVAILABLE` is never reached on the Claude Code side.** There was nothing to choose. **My five arms are in `spine.rs`'s consumer table, and the first act of listing every consumer turned up a SIXTH nobody had named** -- `post-tool-advisory.sh:73` with its `|| true`, where `2` means nothing at all.
- **AND 0044 HAS A SIXTH CONDITION MY SWEEP WAS STRUCTURALLY BLIND TO: `intent info` EXITS 0 WHILE PRINTING AN ERROR** (found by dc, owned by cc). **dc's framing: 0044 is `1` meaning five things; this is `0` meaning "I could not do the thing you asked"** -- the worse half, because a wrong non-zero code stops a caller for the wrong reason and a zero on failure stops nothing. **My Reproduction table classifies conditions BY EXIT CODE, so a failure returning 0 lands in the success row by construction.** I asked _what code does each failure produce_ and never _does any failure produce success_ -- **the instrument answered my question completely and uselessly for the case that matters most.** The sweep that finds these runs the other way round: take every invocation that PRINTS an error and check its code. **Nobody has run it.** Recorded in 0044; the code choice is cc's and is deliberately not bundled.
- **0044 IS THE LIVE ONE (high): the TOOL overloads `1` as the CALLERS overloaded `2`.** Retired, missing-subcommand, missing-arg and a genuine runtime refusal are all `1`, and so is "your code has findings". **`2` is reliable -- 13 of 30 unimplemented commands all exit 2 -- so this rests on `d2b8e76d` being right.** Structural cause: **the code is decided by WHERE the failure lands in the parse tree, not WHAT went wrong**, so a RETIRED command never reaches dispatch and `d2b8e76d`'s careful work is unreachable for exactly the class a migration hits. dc fixed the devbin instance at `a18010a8`. **Not a blocker; must not be bundled under 0043's pressure -- that is how the first three arrived.**
- **0033 IS QUANTIFIED AND ANNOUNCED: 14,253 CHARACTERS across 34 AT rows.** `intent at red|green|na` destroys the row's note; `to-write -> green` is refused so recording a passing test costs TWO rewrites. **It arrives one row at a time over WP-04 to WP-14, every time somebody does exactly the right thing.** `AT-10.9` alone is 3,993 and is one small cc change from a transition. **ic gave it a warning label without anyone having to remember one**: classifying against SHIPPED behaviour makes `at green` `one-way`, so `intent llm guide` now renders _"nothing on this surface puts back what it changes"_ at the point of call.
- **WITH hv, NOT SETTLED BY ME: does D44's window apply to the committed `todo.md` after all?** hv's words were _"a longer done list **in the todo file**"_; I ruled terminal-only on the principle that a committed file is a record. **cc flagged the consequence I had not weighed -- an agent reads `intent/todo.md`, so hv's want is delivered to the surface they did not name.** My recommendation is still terminal-only; the question is in hv's inbox because **a principle read over hv's literal words is not something to settle by silence.** Nothing is blocked: built, tested and shipped as ruled (`def9ca44`).
- **RULED FOR cc AT THIS FOLD, all three theirs to overturn.** **(a) The window reads `completed`, the DOMAIN date, not the record stamps -- endorsed, and their reasoning is stronger than my ruling was**: under D36 the store is rebuildable, so a window over record time shows the whole estate as just-finished after every rebuild. **(b) The unit stays `hours` as hv ruled, AND the config REFUSES a value that is not a whole multiple of 24 while `completed` is date-resolution** -- `window_days` forecloses `completed` ever gaining precision, and hours-with-a-comment leaves a value that silently means something other than it says. A named refusal self-retires the moment the data gets finer. **(c) cc's two-writers escape is the best finding in the exchange and it is now in my kit.**

## TODO

- **AT-10.9 IS NO LONGER ONE SMALL CHANGE FROM GREEN, AND THAT IS THE RIGHT ANSWER.** I verified the migrated arm end to end by hand today -- real binary, no shim, shipped hook, commit lands. **Then AC-10.9 widened, so the row is now short an ARM as well as short the literal id** (still cc's). **Do not green it on the strength of the arm that passes**: the criterion has an arm whose instrument does not exist. `stale_at_check.sh` still fires on the row by construction, **which remains the known limitation of the instrument -- it sees the FILE exists, not that MY assertion inside it does.**
- **THE SWEPT SURFACE IS NOT THE WHOLE SURFACE, AND SAYING SO IS THE POINT.** 22 of the 309 probes never reached the command -- they were refused at the migration gate, so what got measured is the gate. **That slice CANNOT be measured in this build by anyone: `init` and `upgrade` are both unimplemented, so no v3 project can be brought into existence to run them in.** It becomes reachable the moment WP-10 lands, and nobody has swept it.
- **PROVISIONAL-vc PENDING hv**: the migration policy, D43, D46, the `new-surface` out-of-scope ruling, and the withheld-13 field ruling.
- **STILL OPEN, NOT GATING**: issue 0035's v2 layer (`bin/intent_acceptance:1059-1067` is the path every AC I satisfy goes through); **0032** (`ac gate` ORs its covering ATs, so an AC with two ATs satisfies on one -- **whoever builds the v3 close-gate must not port the early-return**); **0034** (the inbox is not single-writer: one appender, one clearer -- **cc's statement is the one to ship, the clearer commits both halves because it is the only node that can SEE both**); 0026-0031, 0033 under hv's default-defer. **0045** (the polarity conflict; latent, and cheap only while it stays latent). **0038 is CLOSED**; its residue -- `organize`/`treeindex`/`help`/`st_zero` still exiting 1, sharing the code that means "your code has findings" -- is 0044's Proposed Fix 1.
- **CORRECTED, AND THE CORRECTION IS THE FINDING (issue 0046): `wp reopen` does not exist, but THE TRANSITION DOES.** `intent wp start` on a `Done` work package writes `WIP` over it -- unconditional `sed` at `bin/intent_wp:208`, **rc 0, no warning, and a message that says `started:`, identical to starting new work.** Measured in a throwaway, not read. **So my own board's "the verb is the fix" was wrong: the remedy for WP-04 is available today and is silent**, and the one thing anyone would want written down -- WHY it reopened -- is the one thing the available verb cannot write. **`wp start` is `keep`/`as-observed`, so v3 reproduces it faithfully and ships `wp reopen` ("with a reason") beside it: two doors to one transition, one enforcing the requirement and one bypassing it.** cc's two-writers rule, and the cheap fix is a REFUSAL rather than a feature. **Measured on ST0056: three of seven started WPs disagree with their gate, in two opposite directions** -- WP-03 (PASS 11/11, still WIP) and WP-05 (PASS 6/6, still WIP) are done-by-contract and not closed; **WP-04 is `Done` against a contract it no longer meets** (closed 5/5 at `1fcf35e7`, then AC-04.6 ADDED on hv's D32 and AC-04.1 STRENGTHENED with AT-04.1 green->red). **Closing 03 and 05 is cc's call, not mine. WP-04 I am NOT hand-editing** -- the gate already reports the truth and a hand-edited status is what D32 exists to forbid.
- **AC-00.1 carries the 28 deferred non-core `pending` rows.** ic's to name, gated here, not forgiven.

## archived at 2026-08-17 10:43Z (localfold, second of the day)

Everything below was DOING or TODO and is settled: **hv ruled all three open questions**, cc built the AT-04.6 arm, 0040/0041 closed, the shell homes deleted. Kept because the reasoning is the record.

### DOING (settled)

- **AT-10.9 AND AT-04.1 ARE GREEN (`e827bd5`), both MUTATION-PROVEN rather than accepted, from a `git archive` extract at `29d32e71`.** AT-10.9: `exit_codes` 10/10, and forcing the shipped hook's `*)` fail-open branch to BLOCK reds EXACTLY the two arms and nothing else (8 passed, 2 failed). AT-04.1: making `Applied::rollback` report success over an unrestored file reds exactly one test. **0033 fired twice more doing it -- AT-04.1 729 -> 99, AT-10.9 5316 -> 97, 5,849 characters, the largest loss yet.** Both committed first and restored from captured text, which is the only reason it reads as an anecdote. Sixth and seventh instances.
- **WP-04'S GATE IS 5/6 AND THE REMAINING ONE IS REAL WORK, WHICH IS WHERE I DISAGREE WITH cc.** They reported the AC-04.6 half already true; **their measurement covers the FIRST condition and the row is red for the SECOND.** I re-ran `mutation_completeness` independently -- 16/16, the ratified-machine walk holds. **But AT-04.6's note asks for `no entity can hold a non-initial value by ANY path INCLUDING INGEST`, and `an_unbuilt_field_is_one_no_service_call_can_set` asserts the narrower claim** -- its own comment says `Criterion.kind` / `AcceptanceTest.kind` / `Issue.status` _"arrive only as authored canon"_ and treats that as an absence. **Arriving as authored canon IS the path the condition names. Measured: `legacy.rs:570,574` decides `AcKind::Test`/`NonTest` on the way in, so a criterion holds a non-initial `kind` no service call can change** -- an entity in a state nothing can leave.
- **0046 CORRECTED AND MY ERROR IS THE REUSABLE PART: I READ A CLASSIFICATION AS BEHAVIOUR.** I published _"v3 inherits all twelve undeclared edges"_ from `keep`/`as-observed`. **v3 REFUSES ALL SEVEN** -- cc measured it, I re-ran the walk. `Facade::check_transition` -> `transitions::permits` is already the shared guard I recommended as item 0. **A classification is a claim ABOUT the code and only the code answers for the code**; the v2 measurement was right because I measured it and the v3 conclusion was wrong because I inferred it, **in one issue, four paragraphs apart**. Third time today after `Disposition::Unbuilt` and `target.behaviour`.
- **THE LIVE HALF OF 0046 IS cc'S AND IT IS ROUTED TO ic: three register rows under-report a deviation the contract demands.** `st done`, `wp start`, `wp done` all say `target.state: as-observed` across a deviation AC-04.6 REQUIRES. **`as-observed` is the value that means "nobody needs to look at this", so under-reporting is worse than over-reporting.** `st cancel` is `corrected` one row away, same mechanism, same author. `st start` is `pending-hv`, which is honest.
- **0045 (high) IS #1 ON cc'S BOARD AND THEY WILL NOT BUILD `critic` ON `Facade::open` WITHOUT THE EXEMPTION FIRST.** Guarded at `6eb5f930` -- the shipped hook driven in an UNMIGRATED fixture, proven by wiring `critic` through the facade and watching it red with my measurement reproduced verbatim. **0044's structural half landed too (`ac84dc10`): a retired command is refused BY NAME at exit 2, and my two polarity tables are what decided the number** -- a third value would block neither gate.
- **0042 STILL OPEN AND STILL dc'S.** My live reproduction stands: with an orphan binary the guards silently do not run, because dc's fix tests for an EMPTY resolution and `info` now renders the non-empty `<not set>`. **The general form is the keeper -- a guard's PRECONDITION can be invalidated by a change that is correct on its own terms and never looks at the guard.** cc offered to take it; I said leave it with dc for now, and prefer `wb_info_rc` over a sentinel-string comparison when it lands.
- **0033 IS QUANTIFIED AND THE NUMBER IS NOW A FLOOR: 14,253 characters standing, 5,849 spent today in two transitions.** **COMMIT BEFORE ANY `intent at` STATUS CHANGE** -- that discipline is the only thing that made today recoverable.
- **WITH hv, TWO QUESTIONS.** **(1) NEW: are SELF-LOOPS legal transitions?** v2's `issues close` on a closed issue returns 0 with `already CLOSED`; v3's Machines 1-3 REFUSE a self-loop. Those cannot both be right and it crosses all four machines, so it is not mine. Recommendation: accept-and-report at exit 0 without re-running the guard, ruled once for all four. **(2) UNCHANGED: does D44's window apply to the committed `todo.md`?** Recommendation still terminal-only, and cc's measurement strengthens it -- the cutoff truncates to a date, so the same config gives a different DONE bucket depending on the hour it is read at.

### TODO (settled)

- **AT-04.6's SECOND ARM IS OWED (cc's)**: for each `Unbuilt` field, assert no entity can hold a non-initial value by ANY path including ingest. **Whichever of the five fail it are mutations owed rather than debt declared.** WP-04 closes on that one.
- **MACHINE 4 FOR `IssueStatus`, pending hv.** cc stopped at `issues add`/`close`/`open` rather than declaring edges on their own authority, which was right. The mechanical half is v2 parity; the self-loop half is hv's.
- **PROVISIONAL-vc PENDING hv**: the migration policy, D43, D46, the `new-surface` out-of-scope ruling, and the withheld-13 field ruling.
- **STILL OPEN, NOT GATING**: 0035's v2 layer; **0032** (`ac gate` ORs its covering ATs -- **whoever builds the v3 close-gate must not port the early return**); **0034**; 0026-0031, 0033 under hv's default-defer; **0044**, **0045**, **0046**; **0036 and dc's packaging hold are the two publication holds.**
- **THE SWEPT SURFACE IS NOT THE WHOLE SURFACE.** 22 of 309 probes never reached the command -- refused at the migration gate, so the gate was measured and not the command. **Unmeasurable in this build by anyone** (`init` and `upgrade` unimplemented, so no v3 project can be created); reachable the moment WP-10 lands, and nobody has swept it.
- **AC-00.1 carries the 28 deferred non-core `pending` rows.** ic's to name, gated here, not forgiven.

### Decisions, FULL TEXT, consolidated on the board at 2026-08-17 10:43Z

Nineteen bullets folded into five families because the list had stopped changing behaviour at the margin (cc's move, taken). **This is the only complete copy.**

Archived once a committed artefact carries them -- the two 2026-08-15 entries are now in `.history/20260816/wip.md`. Both were about the same thing and the short form is what survives:

- (2026-08-15) **A MESSAGE DELIVERED AND UNDERSTOOD IS NOT A LESSON TRANSFERRED, AND NOTHING DISTINGUISHES THEM.** dc warned cc in as many words that `git commit --only <three files>` is "exactly the shape that lands the half" of a two-ended change; **twenty-five minutes later cc was one `--only` from doing it, in the same tree, on the piece the message was about.** The warning arrived, was correct, was read, was acted on for the instance it named -- and did not generalise to the next commit.
- (2026-08-15) **A PAUSED NODE'S CLAIM IS INVISIBLE TO THE NODE THAT NEEDS THE WORK, AND BOTH BEHAVE CORRECTLY.** cc assigned `repo_root()` to dc, dc accepted and widened it, dc paused, cc built it anyway because they were blocked and dc was asleep. Nobody reached across a lane and nobody was careless. **The board records a claim; nothing makes a claim ARRIVE when someone else needs the same work.** The clearest argument this project has produced for the validation role existing at all.
- (2026-08-16) **hv GRANTED A STANDING AUTHORISATION -- "go with your recs. Always." -- AND ITS BOUNDARY IS RECORDED HERE BECAUSE A GRANT NOBODY BOUNDED IS A GRANT SOMEBODY LATER OVER-READS.** **What it covers**: contract-shape calls, spec corrections, ratifying or folding a class, severity, AC/AT wording, and adopting my own recommendation where I have already stated the reasoning and the measurement. **What it does NOT cover, and I am stating this against my own interest so it cannot be quietly widened**: anything irreversible or outward-facing -- the release, pushing `upstream`, publication, running WP-10 Phase B against the real estate, or mutating `bin/**` / `tests/**`. **cc drew this exact line unprompted on the same day and their wording is the one to keep: "hv's 'work amongst yourselves' authorises work; it does not authorise an irreversible rewrite of their own project's canon."** A standing grant lowers the cost of a decision; it does not change which decisions are mine. **And it raises the bar on stating the reasoning, not lowers it** -- when nobody is going to say no, the written argument is the only thing left doing the checking.
- (2026-08-17) **A PASSING MEASUREMENT DOES NOT AUDIT THE SCOPE YOU CLAIM FOR IT, AND THAT IS HARDER TO CATCH THAN A WRONG RESULT.** I confirmed 0043 with five arms and reported it as end-to-end proof of a _migrated-project_ lockout. **ARMV3 ran in a directory with no Intent project on any ancestor.** dc found the widening -- the trigger is PATH, not migration -- **by measuring the thing I had already measured and not looked at.** Nothing failed, so nothing objected. **The result was right and the framing around it was never tested by anything.** Corollary, from the same day: **the obvious count and the correct count differed by 12 on `spine.rs`'s absent-slot default** (12 families declare no slot; 0 reach the branch, because all 12 are leaves) -- **so canary the POPULATION, not just the trigger.**
- (2026-08-16) **A CONTROL REFUSES; DOCUMENTATION REMINDS -- AND I AM NOW THE MEASUREMENT RATHER THAN THE AUTHOR OF IT.** The clock rule failed SIX times while I kept sharpening its wording, and **each failure producing a sharper wording IS THE TELL that it was being hardened instead of questioned.** A two-line hook stopped the seventh in the second it was written, by refusing my own commit. **Corollary earned today: a new check has proven nothing until it has REFUSED something** -- my own `residue_class_check.sh` shipped green and its fourth canary arm found a silent failure in it.
- (2026-08-17) **A SWEEP THAT READS ONE SIGNAL FINDS ONLY THE DEFECTS THAT SIGNAL REPRESENTS -- AND THE CURE IS A DIFFERENTIAL, NOT A WIDER LEXICON.** The forward sweep classified by EXIT CODE and was blind to failure-at-0; dc found that. So I built the reverse sweep to classify by PRINTED ERROR -- and it is blind to **failure with no message at all**, which is the same hole rotated. **`intent info NOSUCHTHING` swallows the argument and returns 0 with byte-identical output, and no error lexicon can ever see it, because the defect IS the absence of the error.** It surfaced only because the harness ran the same command with and without a bogus argument and **compared the two outputs to each other rather than either to a rule.** The generalisation: **when the failure mode is silence, no classifier over the output can help; you need two runs that OUGHT to differ.** (Also: the swallowed positional is INV-08, already in the register -- **an instrument that knew nothing about it reproducing it is a pass for ic's register, not a finding against it** -- and v3 has half-fixed it, `--zzz` now exits 1 while the positional still swallows.)
- (2026-08-17) **TWO BOARDS AGREEING IS NOT TWO CONFIRMATIONS WHEN ONE IS REPORTING THE OTHER'S CLAIM.** `cc/wip.md` and `dc/wip.md` both carried "0042 CLOSED"; the issue file says OPEN with two outstanding items, and **dc's own resolution inside it says in as many words that it stays open.** I came within one command of closing an issue on the strength of two peers who were not independent. **The artefact under discussion is the tiebreak, and it is cheaper to read than either board.** Same shape as the untracked-file misattribution: **a board records what a node BELIEVES; only the artefact records what IS.**
- (2026-08-17) **A CLEAN RESULT AND A BROKEN INSTRUMENT ARE THE SAME OUTPUT, SO EVERY SWEEP OWES A RUN IN WHICH IT IS KNOWN TO FIRE.** My differential sweep reported `flag refused: 103, pos refused: 103` -- a perfect surface -- **while `intent info NOSUCHTHING` was demonstrably swallowing its argument.** Each probe ran in its own copy of the fixture so a mutating probe could not contaminate the next, and **`intent info` prints `Location: <cwd>`**, so two runs of the SAME command produced different bytes and every comparison read "refused". **The instrument could not have found a member, and that is indistinguishable by inspection from there being none.** Caught by a direct probe run for an unrelated reason, not by anything in the sweep. **The fix is not a better classifier, it is a CONTROL**: the corrected sweep carries a positive control on the pre-fix build AND a stub binary that swallows everything, **because the flag arm had never fired against any real build -- 0-of-103 from an arm never shown to work is not a measurement.** Generalises past sweeps: **whenever an instrument reports an empty class, the next question is not "is the code clean" but "can this instrument detect a member at all".**
- (2026-08-17) **A GUARD'S PRECONDITION CAN BE INVALIDATED BY A CHANGE THAT IS CORRECT ON ITS OWN TERMS AND NEVER LOOKS AT THE GUARD.** dc's 0042 fix separates "the resolver failed entirely" from "one guard is missing" by testing whether the resolution came back EMPTY -- correct when written, because `intent info` was unimplemented and produced no line to parse. **cc then implemented `info`, which renders `INTENT_HOME: <not set>` -- a good rendering and a NON-EMPTY string -- and dc's total-failure branch became unreachable in the one condition it was built for.** Measured with an orphan binary: the whiteboard guards silently do not run and an unstamped board time lands at commit rc 0, reproducing this issue's own description of the pre-fix behaviour word for word with `<not set>` where the bare `/` used to be. **Neither node could have seen it from their own side and neither did anything wrong.** cc's two-writers rule generalises: it is not only that two writers of one fact drift, it is that **a guard and its precondition are two writers of one assumption, and the assumption is usually written down nowhere.** Corollary for me: **when a fix lands and is verified, ask what its check ASSUMES, then ask who else can change that.**
- (2026-08-17) **SEVENTH CLOCK FAILURE, MINE, WHILE WRITING ABOUT INSTRUMENTS THAT FAIL SILENTLY.** I stamped a message `03:37Z` having read `03:36Z` -- **incremented, not read.** Recoverable only because two clock reads bracketed the write and both said `03:36Z`, so the correct value was MEASURED rather than re-guessed; had they differed I would have had to annotate it unverifiable. **The rule is not getting less true and I am not getting better at it, which is the argument for the guard and against the wording.** Check A would have caught it only on tolerance. **EIGHTH, twenty minutes later, and this time THE GUARD REFUSED THE COMMIT.** I stamped `04:01Z` against a clock reading `03:58Z` -- fabricated forward, three minutes, in a message whose subject was measuring things properly. **The commit never landed.** That is the whole case for a control over a rule, measured on the author of the rule: **eight failures, seven of which I had to catch myself and one the machine caught before it could reach the record.**
- (2026-08-17) **A CONSUMER LIST NOBODY SWEPT IS A CONSUMER LIST THAT ENDS AT LAST TIME'S BUG.** 0043 enumerated four callers; 0044 listed "the pre-commit critic loop, the three hook events, `int prepush`, the devbin gates". **The actual number is 11 call sites across 7 files with FIVE distinct policies**, and the one nobody had was **the RELEASE SCRIPT** -- three `abort`-on-non-zero sites, two of them invoking commands v3 does not implement, so `int build release` refuses outright with v3 on PATH. **Exit 1 currently means "block this commit", "let this prompt through", "abort this release" and "nothing at all" AT THE SAME TIME.** Four issues each diagnosed against whichever consumer was in view; **the sweep took twenty minutes and the four issues took three days.**
- (2026-08-17) **A SINGLE INSTANCE GIVES YOU THE WRONG FIX; THE MATRIX GIVES YOU THE RIGHT ONE.** From `wp start` reopening a Done WP I was going to recommend _"`wp start` refuses a Done work package"_. **Then I measured all 18 cells and found NO v2 lifecycle verb has a state guard at all** -- seven undeclared movements, twelve counting self-loops, including `st done` on a CANCELLED thread marking it Completed and `wp done` on a NOT-STARTED package marking it Done with the gate still running. **Per-verb, six of the seven get missed, because each looks like an edge case alone and only the matrix shows them as one omission.** The right fix is one shared guard reading the ratified machines at the facade. **The general rule: when a finding is a missing check, measure the whole matrix before recommending the check -- the instance tells you a guard is absent and only the matrix tells you whether it is one guard or seven.**
- (2026-08-17) **A CLASSIFICATION IS A CLAIM ABOUT THE CODE, AND ONLY THE CODE ANSWERS FOR THE CODE -- I BROKE THIS THREE TIMES IN ONE DAY AND ONCE INSIDE A CORRECT MEASUREMENT.** In 0046 I measured v2's transition matrix myself, 18 cells, and got it right; then read `keep`/`as-observed` off the register and published _"v3 inherits all twelve undeclared edges"_. **v3 refuses all seven.** Four paragraphs apart, in one issue: **the half I measured was right and the half I inferred was wrong, and nothing in the document distinguished them to a reader.** Same shape twice more -- reading `Disposition::Unbuilt` as "unenterable" (it means no declared edges, not that ingest cannot set it) and reading `target.behaviour` as what a row does. **The tell is a sentence about behaviour whose evidence is a field.** Operationally: **when a finding's consequence lives in the other version, measure the other version -- a register is where a claim is recorded, never where it is checked.**
- (2026-08-17) **"ALREADY TRUE" FROM A PEER IS A CLAIM ABOUT ONE CONDITION WHEN THE CRITERION HAS TWO.** cc reported AC-04.6 _"already true and I measured it"_, and their measurement was real -- I re-ran it, 16/16, the ratified-machine walk holds. **The row is red for the criterion's SECOND condition**, which asks that no `Unbuilt` field be enterable by ANY path including ingest; their arm asserts no SERVICE CALL can set it, and its own comment names ingest while treating it as an absence. **Both statements are true and only one is the criterion.** The general form: **when a peer reports a criterion met, re-read the criterion before re-running the test** -- a green measurement of the wrong condition is indistinguishable from a green measurement of the right one, and cc's own instinct to report the inputs rather than the verdict is what left room to catch it.
- (2026-08-17) **cc'S SHARPENING BEATS MY VERSION AND REPLACES IT: THE CLASS IS "THE THING I HAVE IS NARROWER THAN THE THING I SAID".** I had two halves of one failure and thought they were two failures. **Mine: a CLASSIFICATION standing in for a measurement** -- `keep`/`as-observed` read as v3's behaviour, `Disposition::Unbuilt` read as "unenterable". **cc's: a MEASUREMENT standing in for a wider one** -- four constructions covering one of two producers, and the first of a criterion's two conditions reported as the criterion. **The tell is uniform and it is in the REPORTING step, which is where it can be caught: the measurement was sound and the sentence carrying it was wider.** cc rewrote their own board from "the two-writers rule applies to arguments" to this, because the narrower wording reads as being about failure modes when it is about any claim of the form _I checked this_. **Six instances between us in one day, in six different materials -- a table row, a config field, a failure-mode enumeration, a two-clause criterion, a help string and an exit code.**
- (2026-08-17) **hv RULED ALL THREE OPEN QUESTIONS ON MY RECOMMENDATIONS, AND RECORDING ONE CORRECTLY MEANT CORRECTING MY OWN SHORTHAND FIRST.** Self-loops legal at exit 0 without re-running the guard, across all four machines; only `Issue.status` becomes a machine (Machine 4, from v2's measured behaviour, no guards) while both `kind` fields fold into their owning machine as `(kind, state)` pairs and `Thread.acceptance` is immutable; D44's window terminal-only. **My recommendation said "widen Machine 3" for BOTH `kind` rows and that is wrong as written** -- `AcceptanceTest.kind` pairs with `AcceptanceTest.status`, not with the criterion's state. **hv adopts the SHAPE of a recommendation, not its sentence, so the steward transcribing it owes a re-derivation rather than a copy** -- a literal transcription would have put a field in the wrong machine, in a ratified document, under an hv ruling. Also written in deliberately: the ruling does NOT license issue 0046, because `wp start` on a `Done` package is a movement and not a self-loop, and a reader will try to make it one.
- (2026-08-17) **I CERTIFIED AN AGREEMENT I NEVER MEASURED, AND THE SENTENCE READ EXACTLY LIKE ITS TRUE NEIGHBOURS.** In 0037 I wrote _"Not claimed: that the four homes disagree today. They do not; I checked the predicates."_ I checked the RETIRE predicates; I never compared the EXCLUSION lists, and they differed by one member -- `claude upgrade`, which `implemented_check.sh` excludes because it installs into the user's real `~/.claude`. **THE NAME CHOSE THE MEMBERS**: I called the constant `SURFACE_NONRETURNING`, and the row that returns fine (it just returns after writing to a home directory) fell out of a list the name was never entitled to define, while `claude start` masked the loss by satisfying both readings. **It reached the canon** -- ic generated `.populations` from it within the hour -- and was caught only because ic's handover made me read the call sites. **My rule: a sentence asserting two things AGREE names the comparison that was run, or it is a design note wearing a measurement's clothes.**
- (2026-08-17) **ic's SHARPENING, WHICH IS THE BETTER RULE AND CAME FROM THEIR OWN ERROR: A SENTENCE CITING A NUMBER NAMES WHERE IT WAS READ, OR IT IS A MEMORY WEARING A CITATION'S CLOTHES.** I told ic that 0044's `flag refused: 103` was my differential's population. **It is the RETRACTED first run** -- the dead instrument whose hash included `intent info`'s own `Location: <cwd>` line -- and my own issue prints it as the worked example of what a broken sweep looks like. ic replaced their correct 104 with it, then went to the artefact and found both errors. **Their form is stronger than mine because both of their failures pass my test and fail theirs**: they assert no agreement, they merely cite. **"A citation that names a FILE is not a citation; it is a promise that a file exists"** -- mine named the right document and the wrong sentence in it. **Deference to the artefact is not the same as reading it, and it FEELS like diligence, which is why a wrong number with a good reason attached outlives one without.**
- (2026-08-17) **CONSISTENCY IS NOT CORRESPONDENCE (dc's finding, my ruling, AC-11.4).** Every check in the publish pipeline asked whether the bytes agree with EACH OTHER -- round trip, sums file, classification refusal, tag-exists -- and **a set of bytes built from a peer's uncommitted work is perfectly self-consistent.** Nothing asked whether they are the bytes the TAG names. **The general form for a criterion: an evidence line that only relates artefacts to each other can be satisfied about the wrong artefacts**, so it needs a term binding the set to its ORIGIN. Compared against the tag's commit and not HEAD, because a late publish is legitimate and a HEAD check looks correct while refusing every one of them. **dc introduced the working-tree route that morning and then found it**, which is the second time in one day a node found a defect by pulling on something adjacent to it rather than by looking for it.

---

# SECOND FOLD OF 2026-08-17 (11:43Z) -- DOING + Decisions as they stood

Archived VERBATIM before the live board was cut. The morning fold is above this line.

## DOING

- **THE RISK I FLAGGED MATERIALISED AND IT IS DIAGNOSED, NOT A REGRESSION.** hv's suite went red on `check format` + `test rust`; hv ruled the legs to cc/ic. **`78a12dce` is green at 21 passed 0 failed**, the failing test included (`git archive` extract, own target dir); **dc measured its child `b2173b1b` green independently** from a detached worktree. `facade.rs` was modified **+41s** and `close_gate_parity.rs` **+48s AFTER the leg finished** -- the compiled tree is gone, and the assertion that panicked is the one hv's self-loop ruling retires. **Not verified and not mine: whether cc's CURRENT tree passes.**
- **cc LANDED THE SELF-LOOP RULING AT `61069b16` AND I VERIFIED IT AT THE SHA THEY NAMED**: fmt diff 0 bytes, clippy 0 warnings, **65 binaries + 3 doctests, 482 passed, 0 failed**, rcs captured directly. **They named a commit rather than "my tree" -- an hour after we agreed that rule, in the first place it could apply.** `Guard::GatePass` was declared in `transitions.rs` and enforced BY HAND in two call sites, so deleting the declaration changed nothing: **that is the precise test for whether a declaration is load-bearing.** `ScopeUnchanged` PRUNED not deprecated, payload-inclusive equality checked, **so no reported-success-with-no-effect path opens IN THE FACADE -- which is the only layer I checked, and I said it of the system. See 0050 below: it opens nineteen times at the surface.**
- **0047's MECHANISM 2 IS A CLASS, NOT A FILE (`d321a0c2`).** cc found five tests asserting the retired behaviour and **three passing for a reason other than their name** -- two of them mechanism 2 EXACTLY (`cli_end_to_end` asserting the gate's verdict from `triage` where `st.done` is not declared; `error_remedies` provoking with a verb that now self-loops), plus a third, new: an edge walk filtering on `accepts` not `leaves`, driving `at.set -> to-write` FROM `to-write` -- **a test comparing a value to itself and reporting coverage.** Two nodes, three binaries, two days, two unrelated methods. **Mechanism 2 is not about the display vocabulary at all: it is any assertion whose reach depends on which states a fixture visits, and the ratifications that move fixtures are still arriving.** Machine 4 and the two `kind` conversions are exactly such ratifications.
- **TWO RULINGS LANDED IN `data-model.md` (`606fcb1b`), both under the standing grant.** (1) **THE SELF-LOOP PREDICATE**, recorded as MY READING, not as ruling text, with a line saying so: _a verb applied to an entity already in that verb's TARGET state is a self-loop, accepted at 0 without guards; whether it is DECLARED from the current state is a separate question, asked only when current differs from target._ **Test the TARGET, never the declared origins** -- else a verb declared from many states self-loops from all of them. cc asked me to own it: a predicate living only in its test is the declaration-enforced-by-hand shape they had just deleted from `facade.rs`.
- **(2) THE DOOR IS A PROPERTY OF THE ACT, NOT THE ENTITY** -- cc's D42 finding, verified from source before ruling. `write_issue` (`store.rs:1012`) binds `created`/`closed` as raw `?6, ?7` = `CarriedFromTheExtract` HARD-CODED, no create door; `write_thread` (`:895`) has both. **AND THERE ARE FOUR DOMAIN DATES, NOT THREE: `issues.closed` is in the schema at `:187` and missing from BOTH enumerations** -- data-model's own bullet and the DDL's DEFINING comment at `:88-89`. **The comment that defines what a domain date IS is what a future author reads to decide whether a new column needs a door**, so a column missing from it is one that will be added without one. Both corrected. **The `stays authored` rationale is CORRECT and does not reach the case it is about to be asked** -- it answers a MIGRATION question and is silent on who fills `created` when nobody authored it. **The day's class in a code comment: a rationale narrower than the use it is now asked to justify, surviving because nobody re-derives an inherited one.** Return-channel SHAPE left explicitly to cc; preference recorded and marked overridable.
- **SEVEN VARIANTS, ONE SHAPE -- CONSOLIDATED INTO `parity.md`'s MEASUREMENT RULES (`bee0a0dd`), where the existing entries now read as instances rather than a list.** _The measurement's subject and the report's subject are different, and the output cannot tell them apart._ **tree** (0049; my `render.rs` citations to cc's disk; ic's `8963e5dd`) · **process** (a pipeline's exit code names the pipeline -- cc's `grep`-piped suite read 0 while a leg was red) · **corpus** (cc: _there are no `thread.json` files in this estate at all_, so a v3-canon measurement returns EMPTY and reads as "no violations") · **layer** (mine, twice) · **population** (`tail -40` at 20 of 68; my AT parse at 93 of 112) · **spelling** (`.kind` vs `.type`; zsh `"$p[]"`) · **declaration** (ic's, and the one this estate is structurally most exposed to: **a surface is all declarations**, so a question asked of the surface is answered from what is DECLARED while the report speaks about what RUNS -- four of the seven were surface questions for that reason). **Two properties make it expensive: every instance returns a well-formed RESULT rather than an error, and a wrong subject usually makes the finding STRONGER.** Operational form is ic's -- not _was it right_ but **"what was this measurement ABOUT, recorded in the artefact"**.
- **0032 IS PORTED INTO v3 AND cc HAS TAKEN IT.** `contract.rs::satisfied_by_tests` ends `.any(|t| t.status == Green)` -- v2's early return as an iterator adaptor -- **and its doc comment states the defect as the specification**, so auditing the function against its own documentation returns agreement. Blast radius measured at 112/112: two multi-AT criteria, neither mixed, **zero verdicts move**. **AC-00.7 is ONE status change away and the change is the obvious tidy-up**: AT-00.5 is a test that PASSES held at `red` by an author's choice, and marking it green satisfies the criterion with its second AT unwritten. **cc's addition is the thing I should have asked for and did not: a discriminating mixed-fixture test, because "zero verdicts move" ALSO means the fix is invisible to its own suite.** Those are one fact and I read only the flattering half.
- **0052 RULED (`e571f6bf`): ic and cc were both right about different layers, and neither statement named its layer.** `facade.rs:1353` takes `scope: TShirt` (cc's premise, true); `wp new` declares ZERO flags and `render.rs:574` hardcodes `TShirt::S` (ic's finding, true). **The surface can enter ONE of seven initial values and drive NONE of six edges; the facade can do all of it.** `State` stays. **The general rule, now in `data-model.md`: the mutation walk DRIVES THE FACADE, so closure walks establish a machine is closed TO THE API -- not to an operator -- and nothing in the walk can tell the difference, because the walk IS an API caller.** AC-04.6 is an API-closure criterion; surface reachability is the register's question; neither answers the other. Also stated: `WorkPackage.scope` has no machine table BY DESIGN -- **an omission and a gap are indistinguishable to the next reader**, which is how this reached ic at all.
- **I WAS ONE COMMAND FROM FILING A FALSE ACCUSATION AGAINST A PEER'S FILE, AND THE REFUTATION WAS SEVEN LINES ABOVE THE LINE I WAS INDICTING.** Chasing a Highlander finding (three generators each roll their own `REV="$(git rev-parse --short=7 HEAD)"` -- `gen_inventory:53`, `gen_pertest:189`, `gen_register:172`, no shared producer), I saw two do `cd "$WT" && git rev-parse` INLINE and one bare, and concluded `gen_pertest` stamps the caller's CWD rather than the measured worktree. **It does not: `cd "$WT"` is at `:182` as its own statement, and the comment at `:188` says _"Read after the `cd "$WT"` above, so this is the MEASURED tree and not the caller's."_** My grep was `^WT=|REV=` -- assignment lines only -- **so the mechanism was invisible to an instrument that could only see it if it were spelled the same way as its neighbours.** This is my own catalogued rule (_absence of a mechanism's NAME is not absence of the mechanism_) and the FIFTH instrument failure today -- **the first that would have produced a filed, wrong, public accusation of another node's work.** The Highlander point survives (three homes, all correct); the defect does not.
- **AND I DID NOT BUILD THE GUARD I OWE ic, ON PURPOSE.** A `lib_provenance.sh` created WITHOUT rewiring the three generators is precisely the trap `lib_surface.sh` fell into and that ic wrote up in that very file: _"Built as the one home, it then sat beside `implemented_check.sh`'s own `EXCLUDED` list and `surface_check.sh`'s four inline walks, none of which sourced it -- so it did not reduce the number of homes, it added one."_ **That was mine.** So: wire it or do not create it -- and ic is actively editing those generators, so the answer is SPEC IT AND HAND IT OVER, not two nodes in one directory.
- **0050 (ic's) IS THE SELF-LOOP RULING'S BLAST RADIUS AT THE SURFACE, AND IT CORRECTS ME (`71fac7ec`).** Nineteen render arms discard the facade `Outcome`, so `st done` prints `ok: done` for a no-op while `todo done` -- **which delegates to it** -- reports the no-op. **The wrapper is honest and the thing it wraps is not.** Confirmed independently at `61069b16` (render/facade byte-identical to HEAD): 20 Outcome verbs, 23 call sites, **19 end in `?;`**. ic said 18/2, actual 20/4 -- the delta is cc's two `issue_*` arms; **reconciled explicitly so a later reader does not redo the whole thing to find nothing is wrong.** **RULED both open decisions:** one spelling `ok: <subject> already <state>` (`todo done`'s `was` reports a PAST condition where a no-op reports a PRESENT one; `todo done` is the outlier that changes); and **NO third prefix -- which cc had already ruled at `render.rs:1643-1646` before ic wrote the issue. A decision living in a doc comment and not in the register is one the next person re-opens.** **MY ERROR, ON THE ISSUE UNDER MY NAME:** I told cc `ScopeUnchanged` pruned meant _"no reported-success-with-no-effect path opens"_ -- true of the FACADE, where I looked, **and I said it of the SYSTEM.** Second time today a property verified at one layer was reported as a property of the whole.
- **MY OWN SHIPPED DEFECT IS FIXED AND I VERIFIED IT BY CORRESPONDENCE, NOT COUNTS.** All four populations computed independently from the ROWS and diffed **member-by-member** against ic's `.populations` block: declared 112, shipped 107, retired 5, probeable 103, **identical sets**. Counts alone would have passed a block with the right arithmetic and the wrong rows -- which is exactly what `SURFACE_NONRETURNING` was. `claude upgrade` is in `not_probed`.
- **AND THE `not_probed` CRITERION IS SHARPER THAN ITS OWN `why` TEXT -- this is the guard against the list going short again.** I hunted a fifth member on the reasoning that the criterion which LOST `claude upgrade` may not have been applied exhaustively. Two candidates, both destructive: `intent_claude_skills:73` `rm -rf "$HOME/.claude/skills/$1"` and `intent_claude_subagents:79` `rm -f "$HOME/.claude/agents/$1.md"`. **Both are correctly probeable anyway, because bare `claude skills`/`claude subagents` dispatch to HELP; `claude upgrade` is a LEAF and bare RUNS it.** So the criterion is not "writes outside the sandbox" -- it is **"writes outside the sandbox WHEN INVOKED BARE, because bare is how the probe invokes."** A row gaining a bare side-effect, or a leaf named like a family, shortens the list silently. Reported to ic; their artefact, their call.
- **THE THIRD LEVEL IS DECLARED and I checked it because I thought it was not.** Nine `claude X` rows, zero `claude X Y` rows, nothing in `about` declaring a depth boundary, `families_outstanding` empty -- it looked like 0037's shape. It rides as `values` on the parent's `subcommand` arg, which is what `spine.rs:335` builds. **All four match their dispatchers exactly** (skills 5, subagents 7, rules 4, ws 4).
- **FILED 0049 (medium, `304cf05b`): a verdict names a pointer, so it decays.** The run pair records neither WHICH tree it measured nor whether that tree survived. **The four stale GREENs from that run are the worse half** -- dc's asymmetry, applied to their own report. **dc HAS TAKEN IT, RULED IT UPSTREAM INTO DEVBIN, AND CORRECTED MY PART 1 -- the correction is load-bearing and I verified it from source.** THE STAMP CANNOT GO IN THE SEAL: `write_errors_file` truncates `.errors` to empty on rc=0 (`runlog:210`) and the file says so itself at `:227`, so a sha written there makes **every green run read as red**. Right home is `print_run_verdict` (`:718`) -- because **hv read `FAILED: rust` in a SUMMARY**, and information reachable only by opening the log arrives after the reader already distrusts the verdict. **The precedent dc did not have: that function's `verdict:` line already fires independent of rc**, so `measured:` on green is an addition to an unconditional block. Part 2 (`find -newer` the run-start anchor) taken unchanged; idiom in-house at `runlog:665`. **dc's own finding, recorded there: `DEVBIN_SEAL_LEDGER` is exported only inside `run_all` (`resolve:441`, unset `:495` -- they said 477), so their approved rc-vs-seal check fires ONLY on `<cmd> all`, and devbin's one recorded instance was a single gate.** And `resolve:459-464` already records a measured false green from **04:05 THIS MORNING** reported to a human as "100% green", who acted on it. **I filed rather than built; 0049 stays OPEN here as the local record pointing at dc's Devbin work.**
- **hv RULED ALL THREE (`c634c7cb`) AND THE ADJUDICATION QUEUE IS EMPTY.** Self-loops legal at exit 0 across all four machines **without re-running the guard**; only `Issue.status` becomes a machine (Machine 4, v2-measured, no guards), both `kind` fields fold into their owning machine as `(kind, state)` pairs, `Thread.acceptance` immutable; D44 terminal-only. **cc is unblocked on `intent issues`.** AT-04.6 stays red for implementation, not adjudication.
- **0045 IS THE LIVE HIGH AND IT IS #1 ON cc'S BOARD.** The polarity conflict: exit 1 blocks the git gate, exit 2 blocks the Claude Code gate, **no non-zero code is safe in both**, and the migration refusal returns the one that blocks every commit. Guarded end to end at `6eb5f930`.
- **0042 STILL OPEN ON ITEM 2 (dc's), and their fix DEPARTED from what we agreed -- correctly.** `wb_info_rc` reports and does NOT gate; `! -d` alone. I withdrew my half: `intent info` returns 0 while printing a hard error, which is 0044's failure-at-0 class, so an rc gate would have been right only because of an open defect.

## Decisions

**CONSOLIDATED 2026-08-17 into five families; nineteen bullets, full text in `.history/20260817/wip.md`.** Folded because the list had stopped changing behaviour at the margin -- cc's move, taken. The families are what a next session needs; the instances are what the history is for.

- (2026-08-16) **hv GRANTED A STANDING AUTHORISATION -- "go with your recs. Always." -- AND ITS BOUNDARY IS RECORDED HERE BECAUSE A GRANT NOBODY BOUNDED IS A GRANT SOMEBODY LATER OVER-READS.** **What it covers**: contract-shape calls, spec corrections, ratifying or folding a class, severity, AC/AT wording, and adopting my own recommendation where I have already stated the reasoning and the measurement. **What it does NOT cover, and I am stating this against my own interest so it cannot be quietly widened**: anything irreversible or outward-facing -- the release, pushing `upstream`, publication, running WP-10 Phase B against the real estate, or mutating `bin/**` / `tests/**`. **cc drew this exact line unprompted on the same day and their wording is the one to keep: "hv's 'work amongst yourselves' authorises work; it does not authorise an irreversible rewrite of their own project's canon."** A standing grant lowers the cost of a decision; it does not change which decisions are mine. **And it raises the bar on stating the reasoning, not lowers it** -- when nobody is going to say no, the written argument is the only thing left doing the checking.

- **FAMILY 1 -- THE CLASS OF THE DAY: "THE THING I HAVE IS NARROWER THAN THE THING I SAID." (cc's wording, which replaced mine.)** Eight instances between four nodes in one day, in eight materials. **The tell is uniform and it is in the REPORTING step, not the measuring one: the measurement was sound and the sentence carrying it was wider.** Sub-forms, each earned: a CLASSIFICATION standing in for a measurement (`keep`/`as-observed` read as v3's behaviour); a MEASUREMENT standing in for a wider one (one of a criterion's two conditions reported as the criterion); **an agreement asserted from a comparison never run** (I wrote "the four homes do not disagree; I checked the predicates" having checked only the RETIRE predicates -- they differed by one member, and it reached the canon); **a number cited from memory wearing a citation's clothes** (ic's, the better rule: _a sentence citing a NUMBER names where it was read_, and **a citation naming a FILE is not a citation, it is a promise that a file exists**); and **a LINE NUMBER cited from a peer's uncommitted tree** -- my sharpening of ic's, one level down. Also: **two boards agreeing is not two confirmations when one is reporting the other's claim.**
- **FAMILY 2 -- A CLEAN RESULT AND A BROKEN INSTRUMENT ARE THE SAME OUTPUT, SO EVERY SWEEP OWES A RUN IN WHICH IT IS KNOWN TO FIRE.** My differential reported a perfectly clean surface while `info` was demonstrably swallowing arguments, because `Location: <cwd>` made every comparison read "refused". **dc's completion is the better half and it is asymmetric: a wrong zero certifies absence, a wrong MAXIMUM certifies catastrophe -- and the second is far more persuasive, because it looks like diligence rewarded.** Nobody re-checks an instrument that has just found something big. Corollaries: **canary the POPULATION, not just the trigger**; put the population on the OK line, because _"nothing violated"_ and _"nothing was examined"_ are otherwise the same output; and **when the failure mode is silence, no classifier over the output can help -- you need two runs that OUGHT to differ.**
- **FAMILY 3 -- A MEASUREMENT IS BOUNDED BY THE QUESTION, AND THE MATRIX IS THE QUESTION.** A single instance gives you the wrong fix: from `wp start` reopening a Done WP I would have recommended one guard; the 18-cell matrix showed **no v2 lifecycle verb has a state guard at all**, so the fix is one shared guard and not seven. Same shape in the consumer sweep -- a list nobody swept **ends at last time's bug**, four callers became ELEVEN across seven files with five distinct policies. And in dc's vendoring count: **when the population is "what the people in the room have seen", every additional participant makes the count feel more reliable while leaving the frame exactly as narrow.** A detector beats a list because it does not have a room.
- **FAMILY 4 -- A GUARD'S PRECONDITION CAN BE INVALIDATED BY A CHANGE THAT IS CORRECT ON ITS OWN TERMS AND NEVER LOOKS AT THE GUARD.** 0042's total-failure branch became unreachable because `info` started rendering `<not set>` -- better for a human, non-empty for the parse. Same shape in 0047: **a machine ratification moved `st new` to Triage and silently defanged a display assertion that still reads as intentional coverage.** And in AC-11.4: **an evidence line that only relates artefacts to EACH OTHER can be satisfied about the wrong artefacts** -- consistency is not correspondence, so it needs a term binding the set to its origin. **A control refuses; documentation reminds.**
- **FAMILY 5 -- CLOCKS, AND I AM THE MEASUREMENT RATHER THAN THE AUTHOR OF IT.** Eight fabricated stamps, seven caught by me and **one refused by the guard before it could land**. The rule is not getting less true and I am not getting better at it, which is the entire case for a control over a rule -- measured on the author of the rule.

---

## FOLD 3 -- 2026-08-17 13:06Z (vc localfold, third of the day)

Captured before trimming the live board. Verbatim; nothing rewritten.

### DOING -- landed and verified (superseded by the compressed record)

- **LANDED AND VERIFIED 2026-08-17 (was owed by cc)**: the **0050 sweep** and **Machine 4**. cc's verdict verified rather than taken -- `20260817-1253.RUST` seal 0 bytes, 70 legs, **499 passed / 0 failed**, and `int measured` says exactly ONE in-scope file moved under it (dc's own uncommitted `cmd/measured`), so **the green is attributable**. The sweep is verified from the OUTPUT end, deliberately not by cc's method: **`render.rs::reported()` is the only function in the CLI that prints a movement**; six `ok:` emissions crate-wide, four non-movements; `intentsvcs` emits none; `Verdict::line` emits `gate:`. There is no twenty-second arm and the reason is structural.

### DOING -- the self-hosting answer as first given (superseded by its own correction)

- **SELF-HOSTING, MEASURED 2026-08-17 at `c0c250b0`**: `ingest --from-md` over a `git archive` copy of our own estate reads **56 threads / 140 WPs / 281 criteria / 228 ATs, 0 blocking, 9 carried, exit 0** -- all 9 carried on COMPLETED threads. **The read half is done and the write half does not exist**: `intent upgrade` is exit 2, WP-10 is XL and Not Started, and `ingest --from-md` says so itself -- _"nothing was read into a store and nothing was written."_ Surface at **54 of 98 probed rows**. So: near on feasibility, far on volume -- you could migrate the estate and then not be able to work in it.

### Verification kit -- four entries now fully consolidated in parity.md

- **A GUARD PROVEN ONLY BY ITS POSITIVE CASE IS INDISTINGUISHABLE FROM ONE THAT FIRES ON EVERYTHING** (ic, 2026-08-16, building the guard I specified). _"A refusal that fires on the wrong population is worse than no refusal, because it teaches its readers to skim."_ **My near-miss paid for the arm**: I nearly reported 12 families as still exercising `spine.rs`'s absent-slot default when the true count is 0, because all 12 are single-entry LEAVES that never reach that branch. ic built the NEGATIVE control from it -- stripping a leaf's args must stay SILENT -- because "it refused when I broke `config`" is equally consistent with an arm that refuses on all twelve. **Always canary the population, not just the trigger.**
- **A CHECK'S ERROR MESSAGE IS WHERE YOU LEARN WHAT IT DOES, AND NOTHING VERIFIES IT AGAINST WHAT IT DOES** (ic, 2026-08-16). Reading a check to find out whether a case is covered returns a yes, because the prose asserting the capability is the same prose you are reading. **Three instances in one day, three authors**: `guide_refs_check.sh` claimed to catch retired commands and could not see one; `paths.len() > 20` reads as a coverage assertion because its message says _"the dispatch table declares the command surface"_, and passes at 104 and 112 alike; and **my own `ok: no to-write row cites a file that exists`, which asserted a search it had not performed.**
- **AND THE OK LINE MUST BE CONDITIONAL ON THE CHECK, 2026-08-17 -- mine was an unconditional `echo` sitting directly beneath 300 lines of disagreement.** Verifying ic's populations, `jq -r ".populations.$p[]"` in double quotes had zsh read `$p[]` as an **array subscript**; every block read came back empty, all four diffs blew up, and my script still printed `all four populations agree`. **Three instruments broke in that one check and TWO OF THE THREE PRODUCED OUTPUT SHAPED LIKE A RESULT**: that one, and `.kind=="subcommand"` where the field is `.type` -- which returned zero rows and put me one step from filing that the surface's third level is undeclared. (The third, an empty regex alternation, at least printed an error.) **Fix the ok line to a FAIL flag, and refuse when either side of a comparison is EMPTY** -- two empty lists diff clean and prove nothing. Fourth zsh bite today, of the class I announced to the estate this morning.
- **PUT THE POPULATION ON THE OK LINE.** _"Nothing violated anything"_ and _"nothing was examined"_ are the same output otherwise. **The empty-population failure sits BELOW the row match**, so a did-I-find-any-rows guard does not catch it -- refuse on a row that matched and would not PARSE. ic's flag sweep silently emptied three times from three different causes with one shape; mine emptied on a one-token grammar rename.

---

## FOLD 3b -- 2026-08-17 13:08Z (vc, same fold, second pass)

The incidents behind six board entries. Each entry keeps its operative rule
on the live board; the narrative is here. Verbatim, nothing rewritten.

- **~~THE CLOCK -- run `date -u` in its own step~~ RETIRED BY D42, 2026-08-15. YOU NEVER NEED THE TIME, SO NEVER WRITE ONE DOWN.** DB records carry the time; nothing else does. **SEVENTH FAILURE 2026-08-16, AND IT IS THE ONE THAT SETTLES THE ARGUMENT: dc's guard REFUSED MY COMMIT.** I stamped an inbox entry `09:45Z` having last read `09:39Z` -- _"3 minutes ahead of now"_ -- **on the message carrying my own ruling that this guard should ship to every consumer, four hours after making it.** Re-ran `date -u` rather than inventing a better-looking value, which is the only repair the rule permits. **Six failures produced six sharper wordings on this board and stopped nothing; a two-line hook stopped the seventh in the second it was written.** That is not an argument for better prose. **A control refuses; documentation reminds -- and I am now the measurement, not the author of it.** This rule failed SIX times while I kept sharpening its wording -- **and each failure producing a sharper wording IS THE TELL that it was being hardened instead of questioned.** The board `## (...)` heading stays only because live peers parse it; it is a LABEL, not data, and nothing may read it as truth. **Ordering comes from the commit** -- git records it, nobody types it.
- **MEASURE AT A PINNED SHA WHEN A PEER IS AWAKE, NOT AT "HEAD".** 2026-08-16 I ran the whole WP-03 suite green, then noticed dc's uncommitted `intentsvcs/src/lib.rs` and untracked `install.rs` were in the build -- **my first `git status` showed two modified files and by the time I had results it showed nine.** Those greens measured dc's half-written tree. **HEAD moved twice under me in four commands**, so even re-running "at HEAD" names a moving target: pin the SHA, `git archive` it, build in a separate `CARGO_TARGET_DIR`, and quote the SHA in the finding. **SHARPENED 2026-08-17, and the new half is about the SENTENCE, not the measurement: I did pin and quote the sha, and still wrote "HEAD (78a12dce) is GREEN" to cc -- true when sent, false within the hour when cc landed `b2173b1b`.** dc hit the identical thing in the same window against their own sha and flagged it unprompted, which makes it a class rather than a slip. **`HEAD` is a POINTER, so a claim about it is a claim about whatever it points at WHEN READ.** Pinning protects the measurement; only NAMING THE COMMIT protects the report. **And `rust` is a pointer at a tree in exactly the same way -- which is the whole of 0049.** **This is dc's invariant one level up -- I read the tree state and acted on it four commands later.** It also nearly produced a false `HEAD IS RED` filing: the compile error was entirely a peer's work in flight.
- **ASK HOW MANY WRITERS AN ARTEFACT HAS BEFORE RULING ON IT** (cc, 2026-08-16, and it is the best line of that exchange). _"A ruling enforced on one of two writers is enforced on neither RELIABLY"_ -- **cc sharpened their own line before it hardened here, and the extra word is the point: the uncovered writer wins whenever it runs LAST, so it is a RACE rather than a certainty. That is worse, not better -- a race passes in the suite and fails in the estate**, and "enforced on neither" invites someone to check whether the covered writer happens to run last and conclude they are fine. `todo.md` has TWO -- `Facade::todo_update` and `views::render_all` -- and cc mutated the projection to window the file while **all five tests passed, because every one of them reached the file through the other door.** **This is not about `todo.md`: it is the reason a set of green tests can cover a mutation completely and prove nothing.** I have been ruling on artefacts without asking what writes them.
- **AND THE THIRD HALF, WHICH I GOT WRONG 2026-08-15 AND dc CORRECTED: AN UNTRACKED FILE HAS NO AUTHOR.** I read `crates/testkit` off the worktree, saw it untracked and freshly stamped, and attributed it to cc. **It was dc's** -- `git log --diff-filter=A` returns exactly one commit and it is theirs. **`git status` gives a path, `stat` gives a time and NOT a node, and in a four-session clone the worktree is the one surface all four write to at once.** My two rules -- index versus worktree -- pick the right tool for WHAT and are structurally silent on WHO. **Only a commit carries authorship, and I inferred one from a tree that cannot express it.** I then told BOTH nodes, so the error was in two places. **The near-harm is the shape to remember: my headline was DO NOT BUILD THIS ON RESUME, so obeying it would have made dc stand down from their own half-finished work while cc, told they had built it, had no memory of doing so. An instruction to do NOTHING produces no error to notice** -- every other mistake this week announced itself by something failing. **And the same read found a REAL defect (the untracked workspace member, live ~20 minutes) while misattributing a real artefact: the method worked and the inference on top of it did not.**
- **AND THE TAIL HALF, 2026-08-17: `... | grep | tail -40` SHOWED 20 OF 68 RESULT LINES AND LOOKED COMPLETE.** Verifying cc's `61069b16` I nearly reported the workspace green off a third of the evidence -- **65 binaries ran, I saw 20, and the largest suite was among what was dropped.** The tell was not in the output, which was a tidy list of `test result: ok`; it was that no line showed a count as large as a suite I already knew existed. **The verdict would have been RIGHT and the coverage claim wrong by 3x, which is the hardest kind to catch, because nothing about a correct answer looks wrong.** Same command also printed `fmt rc=0` from `$?` after a pipe -- the artefact I have documented twice. **Re-ran with output to files and rcs captured directly: 482 passed, 0 failed, 0 fmt bytes, 0 clippy warnings.** For anything whose result you will write down: redirect to a file, count the file, and never let a display limiter stand between the measurement and the claim.
- **`intent at red|green|na` DESTROYS THE ROW'S NOTE, AND I DID IT TO MYSELF TODAY WITH 0033 IN WORKING MEMORY.** Moving AT-03.12 to green cost **1,560 bytes -> 106: 1,447 characters of authored contract**, including the three arms the criterion required and the explicit refusal of AT-02.8/AT-04.5 as coverage. Recovered from `git show`, **and recoverable ONLY because it had been committed first -- a note written and moved in one session is gone.** **`intent at lint` said `ok -- 112 rows conform` immediately afterwards**, so the contract's own linter cannot see it leave. **THE TRANSITION GRAPH MULTIPLIES IT: `to-write -> green` is refused, so recording a passing test costs TWO rewrites.** The safer the graph, the more often the note dies. **BEFORE ANY `at` STATUS CHANGE: capture the row, or commit first, and diff the byte count after.** **AND THE FORWARD EXPOSURE IS MEASURED AND ANNOUNCED: 14,253 CHARACTERS across 34 rows that carry a note and have not reached a terminal status** -- `AT-10.9` alone is 3,993 and is one small cc change from a transition. **It arrives one row at a time, over WP-04 to WP-14, every time somebody does exactly the right thing.** **I deliberately did NOT offer a note-preserving wrapper: it would be a second way to move an AT status, one safe and one not, and the one people reach for under pressure is the one the tool ships.** The fix belongs in `intent at`. Filed as 0033's fifth measured instance -- and the four before it were found by comparing against git, which nobody does routinely, so the count is a lower bound on a defect that leaves no trace in the file it damages.

---

## FOLD 4 -- 2026-08-17 (vc, after hv's issue-cleanup sweep)

Seven board entries whose narrative is now recorded in `parity.md`, on an
issue, or in a commit. The operative rule stays on the live board; the
incident is here. Verbatim, nothing rewritten.

- **AT-00.5: MOVED TO `green` ON cc's WORD AND REVERTED WITHIN THE HOUR, 2026-08-17 -- I VERIFIED THE WRONG GATE, AND IT IS THE ONLY LIVE FALSE VERDICT I HAVE CAUSED ON THIS THREAD.** cc landed 0032's fix in **v3** (`b759101d`, both arms, mutation-proved); I read `contract.rs` at HEAD, re-measured 112 rows against the **v3** predicate, saw zero verdicts move, and released the row. **The gate then scored AC-00.7 SATISFIED -- 45/112 -> 46/112, on one of two covering tests with AT-00.7 still `to-write`.** `intent` on PATH is **v2** and `bin/intent_acceptance:454` still carries the early return, unfixed **deliberately**. Reverted; 45/112 restored, AC-00.7 back in the list, lint clean.
- **THE RELEASE CONDITION IS CORRECTED AND IS NOT "0032 IS FIXED": AT-00.5 goes green when the gate that SCORES this thread is the fixed one** -- v3's `ac gate` running here, or v2's early return repaired. **LANDING A FIX IN THE REPLACEMENT DOES NOT RETIRE THE INCUMBENT.** In `parity.md`'s measurement rules and in the `layer` row. **Do not release this row on a peer's "it has landed" without asking which binary runs** -- neither cc nor I was careless; every step was correct about v3 and the conclusion was about the contract.
- **STILL OWED BY cc**: the `.all` + non-empty-guard fix for **0032** with its discriminating mixed-fixture test (**the fixture must be SYNTHETIC -- a mixed multi-AT AC does not exist in this corpus, 0 of 112**); Machine 4's remaining rows (two paired `kind` conversions, `Thread.acceptance` off `Unbuilt`); and **`AtStatus::display()` for 0056, at TWO call sites** -- `views.rs:461` and `render.rs:878`, the second being `intent at list`'s stdout on a keep-classified command.
- **AND BINDING THAT LIST TO THE CODE FOUND AT-04.6's CENTRAL CLAUSE DISCHARGED OVER TWO THIRDS OF ITS POPULATION.** `transitions.rs` classifies **SIX** `Disposition::State` fields; only **four** carried transcriptions. `AcceptanceTest.status` and `WorkPackage.scope` were driven and closed **and compared to nothing**. **CORRECTED by cc: `data-model.md:487` says FIVE and cc measured SIX, and BOTH ARE RIGHT AT THEIR OWN REVISION** -- `Issue.status` was `Unbuilt` until Machine 4 landed at `b504d91b` that same morning. **So the document was accurate when written and cc's own commit staled it hours before cc measured against it**, which is the tree variant with cc as cause and the document as victim. **It strengthens the conclusion rather than softening it**: a hand-list would have gone short again the same day, by the hand of the node maintaining it, so deriving from `transitions::FIELDS` is the fix and a number in prose is a dated measurement rather than a rule.
- **SELF-HOSTING, ASKED BY hv AND ANSWERED, THEN CORRECTED BY ME** (original in `.history/`): `ingest --from-md` over a copy of our estate reads 56 threads / 140 WPs / 281 criteria / 228 ATs at **0 blocking, 9 carried, exit 0** -- that figure is correct and stays correct. **What I got wrong is the layer**: I measured the read half and stated it of the round trip. See the next line.
- **CORRECTION TO THE ABOVE, AND IT IS MINE TO MAKE BECAUSE hv READ THE ORIGINAL (vc, 2026-08-17).** _"The read half is done"_ was measured on the read half and stated of the round trip. **Issue 0056 is the write half failing**, and it fails on exactly the corpus the sentence was about: `legacy.rs:608` ingests `"n/a" | "n-a" | "na"` liberally, `views.rs:461` emits only `n-a` through `enum_str`, so **`sync --to-disk` rewrites 23 rows of this estate into a token v2's linter refuses at L1 -- and L1 BLOCKS the close gate, while v2 is still the only tool that can close a thread.** So the honest answer is not "near on feasibility, far on volume": **the first migration of our own estate corrupts it, silently, in a direction `--fix` cannot repair because the generator puts it back.**
- **RULED 2026-08-17 (provisional pending hv), in AT-04.6 at `0f4c7f27`: `RATIFIED_WITHOUT_A_TABLE` CHECKS the ratification and does not alter it** -- cc offered to withdraw it and should not. Sentences verbatim from `data-model.md:489-490`; assertions map one-to-one onto the clauses; **"it does not need a table" is a claim about the graph's SHAPE, true only while the shape holds and never read back since authoring**; and every failure message names the remedy as _write the table_, not _fix the code_. It forbids a second verb going **unratified**, which is a trigger on a dormant assumption. **Both machines stay ratified WITHOUT tables** -- inventing transcriptions for graphs whose ratification says they carry no information manufactures the second artefact we spent the day counting.
