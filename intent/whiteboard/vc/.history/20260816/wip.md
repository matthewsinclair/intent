# vc board -- DOING archived 2026-08-16 (localfold)

Landed work rolled off the live board. Gate arithmetic at the fold: 40/109; WP-01 4/4, WP-02 8/8, WP-03 10/10, WP-05 6/6 PASS; WP-04 4/6, WP-06 5/11, WP-11 2/4.

- **THREE GATES PASS: WP-01 4/4, WP-02 8/8, WP-03 10/10. 37/109 overall.** Both closures verified by RUNNING the evidence -- nine tables walked from the DDL rather than checked against cc's list, `VACUUM INTO` and the two independent prune confinements read in source, 331 then 340 tests green with `git status --short native/ schema/` empty so the green is about HEAD. **AT-02.8 and AT-03.11 were both `to-write` while their tests existed and passed; both flipped, both notes (661 and 546 chars) restored by hand -- 0033 destroys them on EVERY transition.**
- **AC-06.9 SATISFIED on the WITHDRAWAL branch -- WP-06 4/11 -> 5/11, 40/109 overall.** Verified at HEAD rather than in the live worktree, since cc and ic both have files open in `native/` and the criterion is about what SHIPS. **`doctor --fix` is out by CONSTRUCTION, not by omission**: hv ruled the withdrawal 2026-08-15 (recorded in code at `finding.rs:76-83`, generalised as _a diagnostic naming the exact remedy beats one performing it_, which is why `doctor` carries remedy text at all); `Flag::ships()` is a POSITIVE match on `keep` so `retire` cannot pass it; `spine.rs` skips it; and `dispatch_ssot.rs` fails on `(ships=false, present=true)` against the RENDERED help block, with a boundary check written because `--fix` is a substring of `--fixup`. **Falsified: at HEAD `--fix` occurs in `native/rust` only in comments and the `at lint --fix` refusal remedy.**
- **D43 LANDED (provisional-vc pending hv): the release is THREE commands with THREE ownerships** -- `int build release` owns the TAG and creates the release object, `int macos prepare` owns the artefacts, `int macos publish` owns the ASSETS. **The defect that produced it is the argument for canon**: the cut created tag AND release; publish demanded the tag exist and refused if a release existed -- **a state the only thing that creates the tag cannot leave behind.** Each command correct, each refusal correct, the sequence impossible, **hiding in the one deliverable nobody had written.** **THE GENERALISATION IS NOT ABOUT RELEASES AND I PROMOTED IT ABOVE THEM: A REFUSAL MUST TEST THE INVARIANT ITS OWN COMMENT STATES.** The comment named ASSETS, the check tested EXISTENCE, **and a refusal that is too strict reads as rigour so nobody questions it.** Same family as `thread.schema.json`'s false doc comment -- prose and mechanism disagreeing with the prose believed -- except there the prose was wrong and the code right, and here the reverse. **Both fail the same way: nobody diffs a comment against its check.**
- **RULED 2026-08-16, both provisional-vc pending hv.** **(a) dc's whiteboard header guard SHIPS** -- upstream, opt-in by presence of `intent/whiteboard/`, and NOT folded into `whiteboard-clock-guard.sh` (that file's name and contract are timestamps; a guard whose name stops describing its checks is this thread's standing defect). **dc's own correction inverts dc's reasoning**: under cc's formatter diagnosis one occurrence is evidence of RARITY, but the real author is a node that knows YAML doing the correct YAML thing, which is the default behaviour of any competent node. **The NOT-YAML ruling has TWO failure directions and the shipped measurement covers one** -- invalid YAML self-repairs because the next reader sees something broken; VALID ESCAPED YAML cannot, because nothing about it looks wrong. **I verified both signatures myself without touching the index before ruling.** **(b) issue 0032 is `corrected`, not `as-observed`** -- you cannot faithfully reproduce a decision nobody made, and **the conflict ic raised is EMPTY: exactly 2 of 109 ACs carry multiple covering ATs and OR and AND agree on both, so zero verdicts move.** The actionable part is TIMING: correct it while it is free, because AC-03.7's two greens mean the fix starts moving a verdict the moment any multi-AT AC goes mixed.
- **THREE REFUSALS WERE CARRYING THE RIGHT RULING IN THE FORM I HAD ALREADY CORRECTED.** AT-00.5, AT-04.1 and AT-04.6 all refuse at partial coverage -- right -- at `to-write`, which is the form I reversed on 2026-08-15. **AT-04.6's note still argued that `red` "would be a false statement about a suite that passes", which is verbatim the reading I had overturned**, and AT-00.5 is the precedent row the other two cite, so it was propagating the uncorrected form by being the thing they copied. All three now `red`, notes restored by hand. **The mechanical payoff: L2/L3 are gated on `green|red`, so a `to-write` row is exempt from both checks that would test its own citation.** Lint stayed clean at 109, so no new gap -- but the checks are live on them now.
- **WP-05 CLOSED 6/6. FOUR GATES PASS: 01, 02, 03, 05. 39/109 overall.** The row I held open closed on ic's one edit -- the `Undefined` class landed in `parity.md` under its own vc provenance. **I mutation-tested `class_vocab_check.sh` against copies before flipping rather than reading its green**: striking the class reports `UNGROUNDED undefined` (my finding, mechanically), and rewording the class heading REFUSES at exit 2 rather than reporting every class ungrounded. **`ac satisfy` REFUSED my hand-satisfy because AC-05.5 is test-backed -- the machine caught me reaching for the wrong verb**, which is better than it accepting the evidence string I had already written.
- **DISPOSITION VOCABULARY RULED (2026-08-15), ic building to it: five values `keep · retire · deviate · pending · new-surface`.** ic found the `target.state` hole's sibling -- three homes, three vocabularies, and 8 rows encoding a fact by ABSENCE, which my own 2026-08-14 rationale forbids. **`deviate` STAYS against ic's recommendation: they read zero rows and called it a vocabulary outliving its members; it has 47 in `pertest.md` and 3 in `register.md`, and the `about` block says the vocabulary is SHARED with the keep/retire/deviate register.** A shared vocabulary is populated across both homes by construction. **WARNED ic that their new declared-but-unused refusal would fire on it wrongly -- that refusal's premise is single-HOMEDNESS, not declaration.** `new-surface` declared as the fifth: it was already canon in the `about` block, so my four-value ruling was stale when I made it the authority. **The 3 `disposition: corrected` rows are `keep`** -- `disposition` asks what becomes of the v2 command, `target.state` asks what v3 does, and `corrected` answers the second; all three have real v2 antecedents. **ic's design question dissolved rather than being decided.**
- **STILL OPEN: 04 red, 06 at 4/11, 11 at 2/4.** **I cannot move 06 tonight and the reason is the tree, not the work**: AC-06.8's `flag_reachability.rs` and AC-06.11's `remedies_are_reachable.rs` do not exist, and AC-06.10's `schema_versioning.rs` is mid-edit in cc's tree, so any run measures work in progress. **AC-11.1 + AC-11.4 remain behind hv's cutover version.**
- **AC-05.5 IS HELD OPEN ON A THIRD INSTANCE OF ITS OWN PROPERTY THAT I FOUND BY ACCIDENT WHILE VERIFYING THE MECHANISM BUILT TO STOP FINDING THEM BY ACCIDENT.** ic's `corrected_check.sh` is sound -- I re-ran both mutations against copies, control first, rather than reading their account. **It compares membership WITHIN a class both documents know about and is structurally blind to a class one document HAS and the other LACKS.** `config` carries `target.state: undefined`, my own 2026-08-14 ruling, defined in the dispatch table's `about` block while `parity.md` -- which says its class list is _"decided here"_ -- never names it. `new-surface` is the same on 18 rows and is also missing from `dispatch.rs:172`, **the only definition of this vocabulary in code, which lists five values against six in use.** `target.state` is a bare `String`: I put `banana` on `st start` and every check in the repo passed. **Closes when the deviation-asserting states (`corrected`, `retire`, `undefined`) must each be a class `parity.md` names.**
- **AT-05.5 RENAMED off a file that never existed and moved to RED-with-a-note -- and the move is what made the contract check the row for the first time.** L2 and L3 are both gated on `green|red`, so a `to-write` row is exempt from every check that would test its own citation. **L3 immediately found a missing back-reference that had been absent since `corrected_check.sh` was created**, on the one instrument in `parity/tools/` that names an AC in MODULES.md. ic's finding was that a `to-write` path is a prediction recorded in the same field as a fact; **this is its second half -- the exemption and the prediction expire together, and only if someone moves the status.**
- **AC-05.6 SATISFIED on all three conditions re-verified rather than the evidence line asserting them**: both `assert_failure` assertions unflipped, both comments amended to name the ratified deviation, and the load-bearing one -- `tests/lib/test_helper.bash:21` defaults `INTENT_BIN` to `bin/intent`, so they assert the INCUMBENT and reach v3 only under the runner's override. Ran both files green and checked the two assertions by name, not by reading a summary.
- **THE AGENT-GUIDE SPLIT RULED: ONE authored file, against ic's own argument for splitting.** Their case was _"a document serving two readers was maintained for one of them"_; **the cause their measurement names is TEMPORAL** (the act that invalidates a hand-written list is not the act that updates it) **and is indifferent to reader count and file count.** Splitting gives the same failure a second home plus a routing decision nobody remembers to make. **I expected to rule the other way** -- I went looking for `usage-rules.md` doing double duty as this repo's apparatus and the shipped guide, hv's `config` caution, the D37 family. **`intent_llm:55` resolves `$INTENT_HOME`, the INSTALL not the project, and the file is consumer-facing throughout.** Ruling rewritten after the check. **Trigger so it is not forever: split when a sentence must be TRUE for one reader and FALSE for the other. I could not construct one.**
- **cc's GUARD LIST RATIFIED, and it nearly repeated a defect four lines above itself.** `Edge::direct()` defaults `guard` to `&[]` -- the `from: &[]` shape whose failure cc's own comment describes in the same struct. **Closed, and verified at HEAD rather than in cc's twelve-file worktree**: `mutation_completeness.rs:952` compares every edge's guard to the ratified table with COUNT equality both ways across all three State fields, and a fourth field trips a panic. 19 of 21 edges take the default and every one is checked.
- **MIGRATION RULED provisional-vc pending hv: a blank-evidence v2 satisfaction arrives `Unsatisfied` with the claim recorded in the event log.** cc framed lossless-versus-clean as a dilemma; **it dissolves one step earlier -- carrying it as `Satisfied` is the LOSSY option**, because it destroys the distinction between satisfied-with-evidence and satisfied-with-nothing. **Synthesising an evidence string is the trap that looks lossless: it reads as evidence forever after. D42's family, one field over.**
- **RULED THIS STRETCH, all unblocking peers:** `Corrected` citations are **IDS not locations** (ic's accident proved it -- their `acceptance.md:298` rotted to `:302` within the hour because I added rows above, silently); **`ingest --from-md` KEEP** (withdrawing would contradict a ratified row -- AC-05.5's own class, which ic caught themselves about to commit); **AC-06.11 WIDENED** to cover the empty verb-space (one property, one corpus); **INV-03 `corrected`** (verified ic's grep; basis is the `error:`+`remedy:` voice INV-01 and AC-06.11 already push toward, so not a new scope call); **AC-12.1 RESCOPED on dc's measurement**; **empty-evidence refusal belongs at the FACADE** -- there is no AC-side lint, so it cannot be a lint.
- **`D11` IS NOT AN EXCEPTION TO THE D-NUMBER RULING -- IT IS OUTSIDE THE RULE.** `st bootstrap --help` says _"a single deliverable (D2-D11)"_ and **that is STZero deliverable eleven in the READER'S project**, shape-identical to our `D15`. **D37 turns on REFERENT, not identifier shape**, so the rule needs no exception and the GUARD needed a scope: faces enforced (13/13 gone), prose reviewed. **The instrument that would enforce the letter in prose is a hand-kept roster of our D-numbers inside the guard -- the exact defect the guard class exists to find.**
- **NOTHING OWED TO ANY PEER RIGHT NOW.** Next in my lane: AC-05.5 closes when the vocabulary check lands with ic; AC-06.11's AT stays RED-with-a-note until WP-10 ships `upgrade`. **hv's desk: the v3.0.0 cutover version (AC-11.1/AC-11.4 sit behind it), `todo --flush`/`--prune` in v3, and the migration ruling above for confirmation.**
- **`intent ac list ST0056` takes 2 MINUTES 24 SECONDS** -- 109 rows, exit 0, ~1.3s per row, timed out my first attempt at 120s. `ac status` and `at lint` return promptly on the same file, so it is not the parse. **Not filed as an issue** (v2 tooling WP-04 replaces) -- sent to cc so the v3 verb does not inherit it.

---

## DOING as at the 14:xxZ fold (2026-08-16) -- archived verbatim

- **40/109. FOUR GATES PASS: WP-01 4/4, WP-02 8/8, WP-03 10/10, WP-05 6/6.** Open: **WP-04 4/6** (AC-04.1 + AC-04.6, both held at partial coverage with the missing arm named on each), **WP-06 5/11**, **WP-11 2/4**. **The landed work of 2026-08-15/16 is in `.history/20260816/wip.md`** -- WP-05's close, AC-05.6, AC-06.9, D43, the header-guard and 0032 rulings, the disposition vocabulary, the agent-guide split, and cc's guard list.
- **TWO SERIOUS DEFECTS TODAY, BOTH IN TERRITORY NO AC COVERED, BOTH FOUND BY A NODE MEASURING SOMETHING ELSE. THAT IS THE CONTRACT-COVERAGE FINDING AND IT IS MINE.** **AC-10.9 (dc's 0038)**: a migrated project CANNOT COMMIT -- v3 exits 1 for a known-but-unbuilt command and the gate reads 1 as findings, so the remedy names a finding that does not exist and the only escape is `--no-verify`. **I did NOT widen AC-10.4**: it asserts byte-IDENTITY of hook files, and byte-identical hooks plus a changed exit code is a broken gate reported GREEN -- **a file-identity criterion cannot see a semantic break, so adding paths adds coverage in the axis that already works.** `.git/hooks` is covered by no AC and never was. **AC-03.11 (cc's `a7aa0b9e`)**: `ingest::resync` rebuilt seven tables and skipped `event_log`, **the one table derived from NOTHING** -- and `load_fresh` warms an empty store through `resync`, so **every fresh clone had no history at all** and nothing said so, because a missing log is byte-identical to a project that never recorded anything. **AC-03.9's ruling already NAMED event_log as irreplaceable; no criterion ever asked that a cold start preserve it.** WP-03 goes PASS -> BLOCKED 10/11 and that is honest. **AT-02.8 and AT-04.5 both mention `event_log`, were green before the defect and green after, and are sensitive to neither** -- naming them as coverage would be the 0032 OR-trap.
- **hv RULED FOUR OF MY OPEN ITEMS IN TWENTY MINUTES, AND THE CUT IS NAMED: 3.0.0, WITH TEXT SEARCH AND CODE PARSING IN SCOPE, THEN FIX-FORWARD ON PATCHES.** Announced to all four nodes. **WP-13's nine `to-write` rows are not optional** and the ladder is wider than twelve WPs reads. **dc is UNBLOCKED -- AC-11.1/AC-11.4 sat behind a real version and nothing else** (never the tap). **D44**: `todo --flush`/`--prune` do NOT ship -- two DESTRUCTIVE verbs become one non-destructive DISPLAY window, default 24h, overridable; the warrant is D01, since the db is SSOT and `todo.md` is a view, **so there is nothing to prune, only how much of the record to SHOW**. Both flags are `disposition: keep` today and must go `retire`; raised with ic. **D45**: hv assigned MCP a PURPOSE -- the CLI is the precise surface, MCP is for imprecise work, **so an agent operation reachable only through MCP is a gap rather than a design**. **D46 (mine, provisional)**: the export bundle gets a published face as a fourth JSON Schema DOCUMENT sharing `SCHEMA_JSON_VER`, not a fourth face type -- it already CLAIMS `intent/export@3.0` and an asserted identity with no published definition is the shape D41 exists to close.
- **THE MIGRATION POLICY IS FREE HERE, AND I MEASURED IT RATHER THAN ARGUING IT.** hv wants Intent on v3 fast to dogfood v3 on Intent building Intent3. My provisional -- a blank-evidence v2 satisfaction arrives `Unsatisfied` with the claim in the event log -- **costs this project EXACTLY ZERO rows**: of the satisfied non-test ACs, every one carries a non-empty evidence field, none is blank, none is missing. **So the strict policy and the fast migration do not trade off**, which is the only thing that could have made hv's speed ruling conflict with it.
- **`undefined` BRIEFED TO hv, AND I CORRECTED MY OWN FRAMING FIRST.** I had carried "it has no place in parity.md" -- **it is at `parity.md:17`, capitalised `Undefined`, and my grep searched lowercase.** Second idiom-versus-literal miss of the day after `INTENT_VER`. `class_vocab_check.sh` is green and grounds it. **The real question is one word, ratify or fold**, and ratifying changes nothing mechanically -- it only removes the provisional marker. Recommended ratify: `config` is `disposition: pending` and `undefined` is the ONLY marker saying the blank is because v2 was SILENT rather than because nobody looked.
- **AT-00.8 IS RED ON A LIVE FAILURE, NOT ON MY 0037 REVERSAL ANY MORE.** cc asked me to green it; `no_shipped_string_literal_carries_pm_state` FAILS in the working tree. The `ST0001` it names is in a `//` COMMENT (`render.rs:1096`, **Not Committed Yet** -- cc's in-flight edit), **so the scan attributed a comment to a string literal and the comment exemption is leaking.** cc's own note says that is the moment the test gets disabled. **`the_literal_scan_separates_published_text_from_source_commentary` PASSES while the exemption fails on real code** -- the vacuity shape again, one arm over from `paths.len() > 20`, same file. cc's 0037 fix itself is GREEN. **hv's full-suite green was real and predates all of this; the shared clone means my test runs see peers' uncommitted work.**
- **AC-06.10 CLOSED, 41/109, WP-06 6/11.** Ran cc's tests myself at HEAD -- 7 green in `schema_versioning.rs`, 5 in `schema_command.rs`. All three conditions hold; (c) is `each_printed_face_is_byte_identical_to_its_committed_file`, a whole-string compare with **no strip**, so the injected versions are inside the compared bytes. **cc beat the ask twice** (binary-against-itself instead of against the committed files; independent contract-hash pins instead of my scripted bump-and-observe) and both are recorded on the row as stronger, not as deviations. **THE ROW SAT AT `to-write` FOR A DAY WITH A GREEN TEST BEHIND IT** -- so the AC read unsatisfied and WP-06 read 5/11 when it was 6/11. **The build is cc's and the transition is mine and nothing carries the signal between them**; `to-write` is also the one state exempt from L2 and L3, so a stale row hides in the state that suppresses the checks that would find it. Asked cc to name the AT id when a covering test lands; the fallback is my sweep, which finds it a day late by construction.
- **THE SWEEP I OFFERED cc AS A FALLBACK, RUN ONCE: AC-06.10 WAS NOT A ONE-OFF. Three more `to-write` rows had a citation that already existed on disk, and one of them was a finished green test.** **AC-00.9 CLOSED AND THEN I REVERSED IT MYSELF -- see the 0037 bullet below; it is back at red and the row says so.** `no_pm_state_in_output.rs`, 8 green, run by me at HEAD -- all three surfaces the note demanded, both exemptions PROVEN rather than asserted, and the `ST0000`-vs-`ST0056` trap closed on referent rather than shape. **cc changed the method for red-first case (a) and the change is better than my spec**: instead of a WP id in `owner_wp` going red, `owner_wp` must have NO READ SITE at all -- laundering values leaves the field one `println!` from a terminal, so guarding the RENDERER catches the wiring, which is the act that makes the leak. It also carries the guard I omitted, `declarations > 0`, so the test cannot pass because the field vanished. **I have written that deviation onto the AT row, because a documented deviation living only in the instrument is the AC-05.5 defect** -- criterion and check disagreeing, visible from one side only. **AT-00.1 and AT-06.1 moved `to-write` -> `red`**: the conformance harness EXISTS, burns in against `/usr/bin/false`, and classifies -- so the rows were never unwritten, they were failing for named reasons. **42/109.**
- **ISSUE 0037, AND IT COST ME THE CLOSE I HAD JUST BOOKED. ic asked me to sweep the contract for anywhere it enumerates the DECLARED set meaning the SHIPPED set. Found one, and it is ic's generalisation hitting from both sides in a single function.** `no_pm_state_in_output.rs::declared_paths()` walks `families[].entries[]` and stops -- **the dispatch table has a SECOND row home**, the top-level `new_surface` array, 8 rows, zero overlap: `search sync schema export ingest backup daemon mcp`. All eight ship, all carry help, and **nothing scans their help** (it lives in the compiled-in JSON asset, so the string-literal surface misses it too). The mirror half: the same function includes the five RETIRED paths. **Too narrow and too wide at once.** `paths.len() > 20` passes at 104 and at 112 alike -- **it reads as a coverage assertion and is a did-the-file-parse assertion, and its own message is what sold it to me.** I measured all 112 clean by hand before reversing, so the criterion is TRUE and the instrument does not establish it. **AT-00.8 back to red with the arm named; 42/109 -> 41/109. Green would have been a gate counting a criterion met on a list nobody had checked the length of.**
- **AND THE SAME OMISSION WAS MINE, IN THE MESSAGE WHERE I ARGUED FROM A CONTINGENCY TABLE.** My `field_overlap` answer said "all 104 entries" -- **it was 104 of 112; I walked `families` and missed `new_surface` too.** Caught only because ic's independent 112/107 count did not match my 104. **Checked rather than assumed that the conclusion survives**: all 8 are `new-surface`/`new-surface`, so they land in an existing cell, add no pair, and leave the function total. Corrected and sent: **112 rows, 7 pairs, `new-surface`/`new-surface` is 19 not 11**; the 17-of-35 figure is about the vocabulary and does not move.
- **THE `field_overlap` CALL IS ANSWERED AND SENT: NO DERIVATION, DECLARE THE PAIR INSTEAD.** Measured all 104 entries -- seven pairs, and **`target.state` -> `disposition` is a total function** (each of seven states maps to exactly one disposition; `keep` spans four states going back). That looks like a derivation argument and is not one: deriving makes ic's `:507` refusal compare a value to its own source, so it **can never fire while still reading as a check** (the 0035-Layer-1 shape); it deletes the only redundancy `is_shipped()` has when ic's own `:495` note says that function fails OPEN; and the function is a fact about 104 rows rather than a law. **The refusal admits 17 of 35 possible pairs and the corpus uses 7**, so ten pairs are permitted that nobody decided -- `pending` + `as-observed` passes today, an honest blank against a confident "v3 reproduces v2", and those cannot both be true of one row.
- **RULED AT THE FOLD, ic building to it: `target_states` GAINS `deviate`, `is_parity_class: false`.** The gap is an incomplete transcription rather than a missing value -- **`parity.md` already draws the corrected/deviate distinction in one sentence and `target.state` implemented only one half.** `false` for the same reason `retire` is: a `deviate` row's warrant is its **D-number**, not class membership. **`upgrade` is `disposition: keep` (migration.md:3) + `target.state: deviate` (D09), and needs no new D-number** -- ic could not find the command in D09 because **D09 answers what v3 DOES and `disposition` answers whether the v2 command survives.** Third instance this week of a fact read against the wrong field.
- **AC-06.11 STAYS UNSATISFIED and its SEVERITY DROPPED, which the row now records.** ic's `dcd32358` corrected `upgrade` from `retire`/`retire` -- `is_shipped()` gates on BOTH, so **WP-10's migration would have shipped with no invocation at all.** Known-but-unimplemented does not clear _"names something the binary can actually do"_, but it is now the ordinary build-order case rather than a contract contradiction, **and those two are indistinguishable from the AC's verdict.**
- **ISSUE 0035 VERIFIED LAYER BY LAYER WITH THE CANARY IT DEMANDED; v2 STILL OPEN.** Stripping `Guard::EvidenceRecorded` in a sacrificial worktree turns the guard red with _"evidence \"\" was accepted as evidence"_ -- falsifiable, not vacuous. **cc's Layer 2 decision beats the fix I filed**: a clap-level `required` catches an ABSENT flag and `--evidence ""` sails past it, so the renderer check is a strict subset of the facade's. **`bin/intent_acceptance:1059-1067` is unchanged and is the path every AC I satisfied this session went through.**
- **STILL PROVISIONAL-vc PENDING hv**: the migration policy (a blank-evidence v2 satisfaction arrives `Unsatisfied` with the claim in the event log -- **carrying it as `Satisfied` is the LOSSY option**), D43, dc's header guard shipping, and `undefined`'s place in `parity.md`. **hv alone: the v3.0.0 cutover version (AC-11.1 + AC-11.4 sit behind it, dc is deliverable-complete and idle) and whether `todo --flush`/`--prune` carry into v3.**

## DOING

- **41/111. THREE GATES PASS: WP-01 4/4, WP-02 8/8, WP-05 6/6.** WP-03 fell PASS -> BLOCKED (10/11) when I added AC-03.11, and that is honest rather than a regression. Open: **WP-04 4/6**, **WP-06 6/11**, **WP-10 1/9**, **WP-11 2/4**; WP-07/08/09/12 at zero. **52 -> 54 of 111 ATs are still `to-write`, which is the real shape of what is left.** The landed work of 2026-08-15/16 is in `.history/20260816/wip.md` -- AC-06.10, AC-00.9's close-and-reversal, issue 0037, the `field_overlap` ruling and `legal_pairs`, the `deviate` transcription, D43-D46, and the two contract-coverage criteria.
- **hv's FOUR RULINGS ARE LANDED AS CANON: the cut is 3.0.0, with text search and code parsing IN SCOPE, then fix-forward on patches.** D44 (`todo --flush`/`--prune` retire; the window is CONFIG, not a flag -- **all six `todo` verbs regenerate the file, so a flag on any one row is a silent-revert generator**), D45 (CLI is the precise surface, MCP the imprecise one), D46 (export bundle gets a face as a fourth JSON Schema document). **CORRECTION I MUST NOT CARRY FORWARD: I told hv dc was unblocked by the version ruling and dc corrected me** -- the version is necessary, NOT sufficient. AC-11.1/AC-11.4 need a PUBLICATION to exist, and publishing is gated on issues **0036 and 0038**. My board and dc's had each been pointing at a different wrong blocker.
- **HIGHLANDER REVIEW COMPLETE (hv-assigned 2026-08-16), 5 findings over 13,905 lines of `src` at `ff094157`, measured on a `git archive HEAD` extract so no peer's dirty tree was read.** Two filed as issues, three reported directly. **0040 (filed HIGH, MEASURED, then LOWERED BY ME TO MEDIUM): `Config.st_prefix` occurs 3 times in the workspace and all 3 are its own declaration** -- `facade.rs:1895` and `legacy.rs:198` hardcode `ST` while **v2 honours the field in six places** and `intent_init:120` writes it into every project. A non-`ST` project migrates into a v3 whose legacy scanner cannot see its own threads: **a silent under-count, the direction Phase A exists to prevent.** **0041 (MEDIUM): the status vocabulary is spelled twice**, `views.rs:72`/`:332` (committed md) and `render.rs:1395`/`:94` (terminal), all four private, byte-identical, compared by nothing -- and **only one copy carries the rationale**, which is the drift mechanism. Also: **`backup.rs:216` is a fourth private `relative()`** and I COMPILED both to prove they disagree (`we\ird/name.md` -> the copy invents a separator), against a one-home doc comment that forbids exactly this; **`remedy()` five times with no trait** and its render line written six times in three files; **nine test files hand-spell a config fixture** in two spellings while testkit offers no project fixture. **CLEAN and reported as such: zero duplicate type names in 13,905 lines, the thin-skin invariant holds (`intent-cli/src` has zero `rusqlite` and zero fs writes), and the two `unwired` functions are the pattern done RIGHT.**
- **`undefined` is hv-RATIFIED (2026-08-16) and landed in `parity.md`.** `class_vocab_check.sh` green after the edit: 6 classes named, 2 grounded. The lesson attached to it stays: I carried "it has no place in parity.md" for two days and it was at `parity.md:17` capitalised, my grep having searched lowercase -- the second idiom-versus-literal miss after `INTENT_VER`.
- **0039 ARBITRATED, AND IT IS A REVISION OF MY OWN RULING, WHICH IS THE PART WORTH KEEPING.** I ruled 0039 whole to cc; dc then measured that the class check as specified refuses ~70 keys (`Target` reads 1 of 44) with **no mechanical discriminator between a declaration and a note**, turning the deliverable into an authored classification over ic's register. **Revised: cc keeps `Entry.aliases` + the two dead commands, ic takes the class check** -- and I told cc explicitly that my instruction had changed and why, because three nodes holding two plans is worse than either plan. **My 0040 sharpens the class rather than joining it: it is the OPPOSITE mechanism** -- field exists, deserializes, nothing consumes it -- so it never lands in dc's `rest: BTreeMap` and `dead_code` correctly ignores it. **Two mechanisms, one class, each proposed fix blind to the other half; ic told to say so in the check's own message rather than let the seventh instance find out.**
- **TWO ROWS I CANNOT MOVE ALONE, both one small thing away.** **AT-00.8** is red on a LIVE failure, not on my reversal: `no_shipped_string_literal_carries_pm_state` fails on `render.rs:1096`, an `ST0001` in a `//` COMMENT in cc's uncommitted tree -- **the scan attributed a comment to a string literal, so the comment exemption is leaking**, and `the_literal_scan_separates_published_text_from_source_commentary` PASSES while it does. **AC-05.1** needs one assertion: ic closed EXP-08 so the root help now comes from `root_help`, but **nothing asserts it** -- `build(&table).about == table.root_help`, not circular, and a reintroduced literal fails it. Both cc-or-ic; I care that they exist, not whose name is on them.
- **ISSUE 0039 IS MINE AND IT IS THE CLASS, NOT THE INSTANCE.** The canon declares `aliases` on four shipping rows, `Entry` does not have the field, and **`at done` / `at notdone` do not exist in the binary while v2 documents both.** `issues new` and `lang rm` will join them the moment those families are wired, so **the defect count GROWS as the surface is built and each instance arrives already reported green** -- `surface_check.sh` and `dispatch_ssot.rs` both contain zero occurrences of `aliases`, because an unknown canon key is not a mismatch, it is invisible. **Fourth undeserialized field in three files; four fixes proposed and none closes the class.** The fix that would: one check comparing the canon's authored key set against the types' deserialized key set.
- **RULED FOR cc AT THE FOLD, UNBLOCKING WP-10 PHASE B: `WorkPackage.scope` takes the marked-legacy form** -- `Option<TShirt>` beside `scope_legacy: Option<Legacy>`, the `AcceptanceTest.legacy` shape, because two shapes for one concept is the Highlander violation. **But I read the precedent before pointing cc at it and it is UNGUARDED**: nothing relates `file` and `legacy`, and `model_laws.rs:104` generates them independently, so all four combinations are legal and the proptest produces them. **Copy the shape, state the invariant, guard it** -- exactly one for `scope`. The `file`/`legacy` gap is recorded separately rather than bundled onto cc's critical path. **cc priced it at ONE row in 140.**
- **STILL PROVISIONAL-vc PENDING hv**: the migration policy (blank-evidence v2 satisfaction arrives `Unsatisfied` with the claim in the event log -- **and I have now MEASURED that this costs Intent's own migration ZERO rows**, so the strict policy and hv's fast migration do not trade off), D43, and D46. **Issue 0035's v2 layer is still open and `bin/intent_acceptance:1059-1067` is still the path every AC I satisfy goes through.**

=== folded at 19:28Z: TODO + Decisions as they stood ===

## TODO

- **AT-10.9 RETARGETED, AND cc's ASK WAS ALREADY ANSWERED.** cc asked whether to widen AC-10.4 to cover `.git/hooks`; **AC-10.9 already exists for exactly that property** and was written as a NEW criterion on cc's own reasoning -- byte-identity cannot see a semantic break. **The row cited `migrate_can_still_commit.rs`, never written, while cc had built the test as `exit_codes.rs` in another crate: pointing at a file that did not exist while the test did.** Retargeted. **I first wrote it as `exit_codes.rs::a_migrated_...` and L1 REFUSED IT -- my own row was the shape the grammar exists to prevent, caught by the lint I hold everyone else to.** **NOT greened, and the reason is a vocabulary gap rather than caution: the test EXISTS so `to-write` is false, and I have not RUN it so `red` would assert a failure I never measured -- there is no value for "written, unrun by me".** Held because cc's tree was dirty in four source files, so a run would have measured their in-flight work and called it HEAD -- the `render.rs:1096` error again. `stale_at_check.sh` fires on it now, which is the tool working. **cc greens it from a clean tree; the criterion itself I verified by hand.** Owed by cc: the id `AT-10.9` inside the test, which is L1's second end.
- **THE VACUOUS-ASSERTION SWEEP cc INVITED: THEIR INSTANCE WAS THE ONLY ONE.** cc found a test whose doc comment said it existed _"so a blanket always-exit-1 cannot pass"_ while a blanket always-exit-1 is exactly what shipped -- the assertion's first disjunct was constant-true. **Swept every `assert!` block in `crates/**` multi-line (a single-line grep returns ZERO, since theirs spanned lines): five disjunctive assertions in the whole suite, all five intentional, none constant-true.** So the class is not endemic. **Sharpest instance of the day's shape and worth keeping: every other instance was a check that failed to LOOK; that one announced what it was looking for and then did not.** **The doc comment is a claim about the assertion and nothing compares them** -- same as `finding.rs:22`'s enum arithmetic and `paths.len() > 20`. One small thing passed to cc: `cli_end_to_end.rs:401` asserts `code == 0 || code == 1` under a message about excluding 101, so **a legitimate `EXIT_UNAVAILABLE` fails it with a panic-flavoured message** -- not live (the loop drives only wired verbs, each checked) but `2` became normal the day `d2b8e76d` landed.
- **ISSUE 0043, CRITICAL, ANNOUNCED TO ALL FOUR NODES: A MIGRATED PROJECT BLOCKS EVERY CLAUDE CODE PROMPT AND THE LOCKOUT IS SELF-SEALING.** Two consumers read the same exit code and take **opposite** decisions: the pre-commit gate reads `2` as "tooling unavailable" and **fails open** (correct, and why 0038 was fixed by moving `1`->`2`), while **Claude Code's `UserPromptSubmit` reads `2` as BLOCK** -- our own `require-in-session.sh:20` documents _"Block (exit 2 + stderr message)"_ and `:71` is a bare `exit 2`. `.claude/settings.json` wires every prompt to `intent claude hook require-in-session`; **measured `rc=2`.** **Escapes are unreachable from inside the session**: `/in-session` needs a prompt, and the sentinel `touch` needs the path the gate no longer prints. **NOT a mistake in `d2b8e76d` -- cc measured the right thing and reasoned it correctly. The defect is that an exit code was treated as a property of the TOOL when it is a property of the CALLER's contract, and nothing enumerated the callers.** Two shipped consumers, they disagree, one was in view: **`1` breaks the commit gate (0038), `2` breaks the prompt gate (this), and no single global value is right.** **`require-in-session.sh:26` says _"an unexpected abort would block every prompt"_ -- the author foresaw this and could only defend the half they controlled.** Worse than 0038 in the way that matters: **0038 blocked commits and left the tool you would fix it with working; this blocks the tool.** And it breaks 0016 in the most direct way available -- `.claude/**` is byte-identical exactly as AC-10.4 demands **and the sessions are dead anyway**, which is AC-10.9's finding again. **NOT CLAIMED and I flagged it in the announce: I have NOT seen the block in a live session.** Derived from our shipped hook's documented contract plus a measured code. **Confirmation is cheap -- a throwaway migrated project opened in Claude Code -- and should run before anyone acts on `critical`.** Filed at that severity because being right and slow costs less than being wrong and fast, not because the observation is complete.
- **0038 IS FIXED (`d2b8e76d`) AND I VERIFIED IT END TO END -- cc's BOARD STILL SAYS "TAKE THIS FIRST", SO I TOLD THEM BEFORE THEY RE-FIX IT.** `intent critic shell --staged` now exits **2**, the hook's `*)` branch prints `invocation error; fail-open`, `AGGREGATE` stays 0, **the commit proceeds.** Verified through the shipped hook, not by reading the case statement -- which is what AT-10.9 demands. **The whole class measured: known-but-unimplemented (`critic`/`agents`/`llm`/`info`/`upgrade`) = 2 FIXED; unrecognised subcommand (`organize`/`treeindex`) = 1, STILL sharing the findings code; usage error = 1, correct.** So AC-10.9 wanted three kinds distinguished and today it is two. **Not a gate break** (no shipped hook calls a command v3 does not recognise) and **I did not touch the issue's status** -- closing it is cc's and hv's call. **AT-10.9 STAYS `to-write` AND I DID NOT MOVE IT**: the criterion may now be true and `migrate_can_still_commit.rs` still does not exist, which are two different facts, and this is the row I got wrong once already today.
- **BUT FIXING 0038 DID NOT FIX WHAT THE SWAP BREAKS, AND THE REMAINDER IS THE WORSE SHAPE -- ISSUE 0042, HIGH, AND AC-10.10 IS THE GAP IT FOUND IN MY OWN CRITERION.** `pre-commit.sh:104` resolves the guard directory by parsing `intent info` stdout; **v3 does not implement `info`, the value comes back EMPTY, both whiteboard guards take the FAIL-OPEN branch and do not run.** **Independent of the exit code -- the hook reads stdout and `2>/dev/null` eats the error either way -- so `d2b8e76d` could not have touched it.** **0038 failed CLOSED: loud, blocking, fixed in a day. This fails OPEN: the commit succeeds and two controls quietly stop enforcing, so the loud failure got fixed first and the quiet one is sitting behind it.** The clock guard is the control that refused MY commit carrying a fabricated stamp after six prose resolutions had failed to stop the previous six. **Intent's own dogfood migration hits this and Lamplight is second in the corpus.** **AC-10.9 says "a migrated project can still COMMIT" -- which a commit succeeding with both guards off satisfies completely.** Necessary, not sufficient. **AC-10.10 + AT-10.10 added: assert the guards still REFUSE, never that a commit succeeds** -- a control is proven by what it stops, and the whole class here is a control that stopped stopping. Lint green at 112.
- **MACHINE 3 WAS SUPERSEDED BY ITS OWN DOCUMENT 230 LINES EARLIER -- FIXED (`4ff7ea41`).** hv ratified `computed` as the fifth AC state on 2026-08-15, `transitions.rs` has carried it since, and `data-model.md` records the ratification, the reasoning and my own reversal in full -- **and then the Machine 3 TABLE still listed four states with no `computed` in any row.** The document was never wrong as a whole; it was wrong at **the one place an implementer reads to find out what to build**, with the correction filed under a different heading where you would only look if you already knew. **A superseded table beside its own correction is worse than either alone, because agreeing with the document stops being a test of anything.** Table now matches `transitions.rs` edge for edge -- **and verifying AFTER the edit caught two rows my first pass missed** (`ac descope` and `ac withdraw` both accept `computed` and the table said neither). Also named the two state fields the section implied did not exist: `WorkPackage.scope` and `AcceptanceTest.status`, five classified in code against three tabled. **The AT machine is the one to watch: trivial graph, subtle semantics, and its real rule -- `to-write` = unwritten, `red` = exists and fails -- lives in the linter and on this board, not in the model spec.**
- **ic's `canon_keys_are_read` VERIFIED AND IT HOLDS, dc's CONDITION INCLUDED.** Went to check the canary rather than take the claim: it is recorded in the commit message and it is sound. **ic's single mutation -- renaming a field so it deserializes under a different key -- produces BOTH arms** (old key declared-and-unread, which is dc's junk-key direction exactly; new field read-and-unclassified). **And it is a better answer than the condition I relayed**: dc's junk-key form required editing the shared table, ic found the form that needs no edit to a file three nodes build against -- I had declined to touch that same file for that same reason two hours earlier and then passed on a condition requiring it. `fields_of` asking the type by SERIALIZING it is the right construction and it is the property all five failures needed: **it cannot go stale independently of the thing it measures.** Only observation, explicitly not a finding: the canary record is in the commit message rather than the test's doc comment, where `testkit` puts its mutation proof and where it would travel with the code.
- **RULED FOR ic (provisional-vc pending hv): THE WITHHELD-13 DISTINCTION EARNS A DECLARED FIELD, BUT NOT AN EXPOSURE FLAG.** D45 demoted `exposed_on_mcp` to a routing note and the safety policy it was carrying lost its home -- **13 withheld leaves, every one `mutate`, not one a `read`** (verified by me from the table: 14 mutate / 12 read across the 26). **Declare the INTRINSIC property -- what does the verb act UPON** (one modelled entity / the estate / the environment) -- **never a second surface-named flag**, because `exposed_on_mcp` failed precisely by naming a surface while carrying a property, and `agent_safe` would rebuild that fault at a new address. `read_or_mutate` is too coarse and `st new` vs `init` is the proof: **they differ in blast radius, not direction.** MCP's withhold list then DERIVES. **Canary: the new field must reproduce the existing 13 exactly, computed not restated** -- a free correct oracle that stops being free the moment anyone edits `exposed_on_mcp`. **CONDITION I WILL HOLD: it ships WITH its consumer and its check in ONE change, or it is instance seven of my own finding** -- `aliases`, `st_prefix`, `Arg.default` all landed as declarations ahead of any reader. Better a week longer in ic's spec prose than a seventh undeserialized field.
- **AND VERIFYING ic's 26 FOUND A DEFECT NEITHER OF US WAS LOOKING FOR: `config` is `read_or_mutate: mutate` and is the ONLY family root that is.** Its own row reads `help: "Display the resolved project configuration"`, `args: 0`, `flags: 0`; its twelve siblings are `read`; its children are the correct pair (`config get` read / `config set` mutate), so bare `config` has no mutating role left. **The convention is unambiguous -- `st` has `st new` under it and is `read`, so a root is marked on its OWN behaviour** -- and `config` breaks it alone. **Worse today than yesterday because ic's D45 reorder makes `read_or_mutate` the field an agent reads FIRST**, so an agent is now told a zero-arg display command mutates. Caveat I left to ic: `config` is `disposition: pending` / `target.state: undefined` -- the sole member of the class hv ratified today -- so `mutate` MIGHT be forward-looking for an editor, in which case it is a decision nobody recorded, on a row under active design.
- **THE RESIDUE TABLE WAS SHORT BY THE ONLY TWO CLASSES THIS ESTATE PRODUCES -- FIXED AND GUARDED (`57d60f96`).** Reading Phase A's output rather than its totals: `field-not-recorded` (8, ST0023's WPs) and `unknown-scope` (1, ST0020/WP/09) came back and **neither was in `migration.md`'s table**, which declared six against `legacy.rs`'s eight. Both rows added; **`parity/tools/residue_class_check.sh` built, registered, canaried SIX ways** (control green, dropped row -> 1, junk row -> 1, moved table header -> 2, moved constructor spelling -> 2, empty scanner -> 2). It reads `legacy.rs` rather than a transcription, because a guard built on a copy drifts the way the table it guards drifted. **I also narrowed the table's own promise instead of restating it**: it claimed every class carries `file:line`, which two absent-field classes cannot honour by construction, so `unknown-scope`'s missing line is now a defect against a checkable sentence.
- **THE CANARY FOUND A SILENT FAILURE IN MY OWN NEW CHECK, AND THAT IS THE ARGUMENT FOR dc's CONDITION IN ONE LINE.** Under `set -euo pipefail` a `grep` matching nothing exits 1, the pipeline inherits it, and the script died **before its own empty-population refusal could fire** -- exit 1, NO OUTPUT, which reads as "the sets differ". **A check built to catch a silent failure had one**, and only the arm nobody expected to fire found it. Fixed with a load-bearing `|| true` and a comment saying why so nobody tidies it away. **Every check I have written this week was green on the day it shipped; two of them were wrong, and both were found by making them fail on purpose.**
- **AND THE MEASUREMENT GROUNDED MY OWN FOLD RULING**: the single `unknown-scope` is ST0020/WP/09 at `"Medium-Large"`, and the finding's detail says _"the model has no marked-legacy form for one yet"_ -- **exactly the `WorkPackage.scope` shape I ruled for cc**. Not hypothetical: one live row in this estate waits on it, and cc priced it at one row in 140. **The unguarded `AcceptanceTest` precedent is still the half not to skip.**
- **cc's "THIS REPO IS MIGRATABLE" VERIFIED BY RE-RUNNING, AND CONSERVATION IS EXACT AT ALL FOUR LEVELS.** `intent ingest` at the repo root: `read: 56 thread(s), 140 work package(s), 280 criteria, 227 acceptance test(s)`, **`residue: 0 blocking, 9 carried`**, rc=0, tree unchanged after. The 9 carried are all `field-not-recorded` on ST0023's WPs -- pre-frontmatter artefacts carried as-is, the closed-thread policy working. **Then the check the report cannot do for itself: I counted the estate from the FILESYSTEM and every level matches** -- 56 dirs / 56 distinct ids vs 56 read; 140 `WP/NN` dirs and 140 `info.md` vs 140; 280 `^- AC-` rows vs 280; 227 `^- AT-` rows vs 227. **A conservation claim checked against the run that produced it is circular; this is the other side, and it is AC-00.2's property holding on the canary member.** **AT-00.2 / AT-10.5 stay `to-write` and I did not touch them** -- both cite `fleet_corpus_ingest.rs`, which does not exist, and a hand-run is not the instrument the criterion names; what it buys is the harness's red-first baseline. **Sent to cc rather than written into the AT note, because issue 0033 has `at red/green/na` destroying notes and I did not need to move the row.** **One thing worth an explicit assertion: 52 threads live under `COMPLETED/`, 2 under `CANCELLED/`, 1 under `NOT-STARTED/` and exactly ONE at `intent/st/` -- so a harness globbing `intent/st/*/` alone reads 1 of 56 and reports success**, which is 0040's shape from another direction.
- **0040 IS NOW MEASURED, AND MEASURING IT CHANGED BOTH HALVES -- this is the entry to read if only one survives.** Four fixtures from one generator differing only in directory name and config value: `ST`/`ST` reads 1 thread, `TH`/`TH` reads **0**, and the crossed arms show **the config has no effect in EITHER direction** (`ST` dir + `TH` config still reads; `TH` dir + `ST` config still does not). **The invisible cases do not fail -- they print `ok: this estate parses`, `residue: 0 blocking, 0 carried`, exit 0.** Stronger than the "silent under-count" I filed: a refusal is safe because the operator re-runs, while a green `ok:` over an unread estate is an instruction to proceed and exit 0 means a pipeline takes it. **THEN I LOWERED MY OWN SEVERITY TO MEDIUM**, having named the fleet survey as the deciding test and then run it: **all 16 projects use `ST`** (Laksa omits the field, which defaults to it), so the whole `migration.md` corpus and Intent's own dogfood migration are unaffected. **The defect is real and the urgency is not, and ranking it `high` beside 0038 would have been the actual harm** -- 0038 blocks every commit in a migrated project, this reaches zero known projects, and two `high` rows where one is unreachable is how the reachable one loses its place.
- **FOR hv WHEN THEY RETURN, and it is a scope call I have deliberately NOT made: issue 0040 has two legitimate answers and today's state is neither.** Honour `st_prefix` (both consumers read `project.config().st_prefix`; `legacy.rs` loses its hardcoded `len() == 6`), or retire it (a `disposition: retire` row with a ratification, `default_st_prefix` deleted, and the migrator NAMES the field when it carries a non-`ST` value forward). **Retiring is a scope decision, so it is yours; what is not defensible is a field v2 honours, v3 declares, and nothing reads.** Whichever way: the canary is a fixture with a non-default prefix and none exists, which is why nothing caught it.
- **THE REUSABLE RULE THE REVIEW PRODUCED, because it answers a question three nodes hit today from two directions: a declared-and-unread field is a DEFECT when the consumer EXISTS and encodes the value another way; PENDING when the consumer is unbuilt; CORRECT when a ruling removed the consumer.** Three of `Config`'s seven fields have zero read sites and only one is a defect -- `st_prefix` (defect), `author` (correct, D02 killed the verblock), `languages` (pending, families unwired). **Count and type separate none of them**, which is dc's finding one layer up holding one layer down. The sharper question is not "is this key read" but **"does a consumer exist and hardcode instead"**, and it is still semantic, still authored -- dc's "it must be a ratified list, not a rule" survives intact.
- **`wp reopen` does not exist and three of five WPs disagree with their own gate** (WP-02 and WP-04 say `Done` against BLOCKED; WP-05 says `WIP` against PASS). I caused two by adding ACs to closed WPs. Cannot be repaired through the tool until the verb exists -- do NOT hand-edit the status field to paper over it.
- **AC-00.1 carries the 28 deferred non-core `pending` rows.** ic's to name, gated here, not forgiven.
- **HV'S DESK IS NEARLY EMPTY AND THAT IS NEW: hv ruled SIX items on 2026-08-16** (cutover 3.0.0 + scope + fix-forward posture; `todo --flush`/`--prune`; the SKILL.md go-ahead; the migration policy by implication) **on top of the three ruled 2026-08-15** (fifth state `computed` RATIFIED; schema faces became D41; `doctor --fix` WITHDRAWN with `disposition: retire` as the mechanism). **STILL hv's AND ONLY THESE: ratify-or-fold `undefined`, plus the three provisional-vc rulings (migration policy, D43, D46).** **THE STANDING LESSON FROM THIS BULLET'S OWN HISTORY, kept because it recurred TWICE MORE today: I track my MODEL of a peer's state instead of the peer's state.** The tap was the first (carried as hv's blocker for two hours after hv and dc had made it). Then I told hv the version ruling unblocked AC-11.1/AC-11.4 when **a PUBLICATION is what those need**, and dc's board was simultaneously blaming the tap. **Two boards, two different wrong blockers, and the real gate was neither** -- it is issues 0036 and 0038. **Ask the peer; do not infer the peer.**
- **Issues 0026-0034 under hv's DEFAULT-DEFER, none gating.** **0034 is new and is mine**: the whiteboard inbox is NOT single-writer -- one appender and one clearer, so whoever commits the file first commits the other's act, and the recipient's clear is a split change across two files. `SKILL.md:232` already says own-directory and is filed under `archive` only, while `:276` calls the inbox the sender's file. **cc's statement is the one to ship: the clearer commits both halves because it is the only node that can SEE both.** **0032 is also mine**: `ac gate` ORs its covering ATs instead of ANDing them, so a criterion with two acceptance tests scores satisfied on one. Not fixed -- v2 tooling that WP-04 replaces, `bin/**` not mutated with sessions live -- but **whoever builds the v3 close-gate must not port the early-return.** 0026 was corrected from my own false `high` version -- read the Correction notice first. 0028 is one sentence into the shipped whiteboard `SKILL.md` and touches every node's commit habit. **0029 is a DECISION before it is a fix, and cc must check AC-03.6 first** -- that AC may be green through the copy 0029 proposes to delete.
- **hv rulings not yet built**: devbin `bin/.devbin` -> `devbin/` (dc); `intent doctor` flags an unwired hook and **`--fix`** repairs it (**NOT `--repair`** -- `--fix` already exists, do not add a second); auth model for WP-08 (local password at first install, Conflab-style cli-to-daemon).
- **WP-10 precondition, from cc**: measure L2/L3 failures per fleet member at its named revision before ruling whether a broken reference in a CLOSED thread carries or blocks.

## Decisions

Archived once a committed artefact carries them -- see `.history/`.

- (2026-08-15) **A LESSON CAN FAIL TO TRAVEL BETWEEN NODES AS EASILY AS WITHIN ONE, AND THE HANDOFF LOOKS LIKE SUCCESS WHILE IT FAILS.** dc told cc at 21:09Z, in as many words, that _"a migrator must not do half of a two-ended migration"_ and that `git commit --only <your three files>` is _"exactly the shape that lands the half"_. **Twenty-five minutes later cc was one `--only` from landing the half of a two-ended change, in the same tree, on the very piece that message was about.** The warning arrived, was correct, was read, and was acted on for the instance it named -- **and did not generalise to the next commit.** Four instances today; this is the first BETWEEN nodes. **A message delivered and understood is not a lesson transferred, and there is no signal that distinguishes them.**
- (2026-08-15) **A PAUSED NODE'S CLAIM IS INVISIBLE TO THE NODE THAT NEEDS THE WORK, AND BOTH BEHAVE CORRECTLY.** cc assigned `repo_root()` to dc at 20:57Z, dc accepted and WIDENED it at 21:09Z (_"one piece of work, not two"_), dc paused, and cc built it anyway because they were blocked and dc was asleep. **Nobody reached across a lane and nobody was careless.** The board records the claim; nothing makes a claim ARRIVE at the moment someone else needs the same work. **This is the class only a node reading every board can see, and it is the clearest argument this session has produced for the validation role existing at all.**
- (2026-08-15) **A FALSE COMMENT IS NOT INERT DOCUMENTATION -- IT IS AN INSTRUCTION TO FUTURE AUTHORS NOT TO CHECK** (cc's finding, and it is stronger than the ruling of mine it corrects). `thread.schema.json` published _"`Satisfied` carries its evidence and cannot be constructed without it (hv, 2026-08-15)"_ to every external consumer. **`AcState::Satisfied { evidence: String }` makes the FIELD mandatory, not the evidence present** -- `evidence: ""` builds it. **The claim was then load-bearing in three separate places** (the model doc, `ac_satisfy`'s _"structural rather than enforced"_, `contract.rs` destructuring past it) **and no guard was written BECAUSE a comment said one was unnecessary.** I had ruled that a `///` on a derived type is an unreviewed publication channel; **my version was "a private note might ship", and the real hazard is that a shipped note is BELIEVED.** Every downstream decision was correct given the premise. **The believers were us.**
- (2026-08-15) **A PERMISSIVE EMPTY DEFAULT IS SAFE ONLY WHERE SOMETHING ELSE ENUMERATES THE POPULATION.** `Edge.from: &[]` on every edge made the closure check pass on a graph that let `st done` fire from `cancelled` -- closed, and the wrong graph. `Edge.guard: &[]` is the identical shape four lines below it in the same struct, **and it is fine, because the transcription check compares every edge's guard to a ratified table with count equality both ways.** The difference is not the default; **it is whether a second artefact independently knows how many there should be.** Worth stating as the general rule, because the two cases are indistinguishable by reading the struct.
- (2026-08-15) **A LESSON RECORDED IN ONE ARTEFACT DOES NOT GENERALISE ITSELF INTO THE NEXT ONE, EVEN FOR ITS OWN AUTHOR.** cc's face-version hash covered comment bytes, so a reworded sentence demanded three version bumps -- and `store_schema_version.rs` had already fixed that exact defect one layer down, carrying a note **cc wrote**: _"a guard that cries wolf on a comment is a guard someone re-pins without reading."_ Same day: my `///`-vs-`//` reversal, and ic's line-number citation rotting in the message arguing against line-number citations. **Three authors, three lessons, all in writing, none of which travelled the one hop that mattered.** This is AC-05.5's class one level up -- **the drift is not between two artefacts' CONTENT but between a rule and its next application.**
- (2026-08-15) **REVISIT SKIPS; DO NOT TRUST THEIR REASONS** (ic, on finding INV-03's divergence while reading the skip that hid it). Their skip reason -- _"needs a probe run outside a project"_ -- **was true, and was never a claim that nothing was wrong. But it READS like one, and it is the sentence anybody re-reading the file relies on.** A skip list is a promise that something else covers the key; **a skip whose reason is "hard to check" is a promise nobody made.**
- (2026-08-15) **A GUARD THAT BLOCKS ITS OWN FALSIFICATION MAKES EVERY SUBSEQUENT GREEN UNFALSIFIABLE** (ic). Their staleness refusal treated a synthetic mutation table as an input the binary must postdate, so the mutation test could not run -- **and it arrived inside the fix for another instance of the same class.** Sixth instance today. The tell is that the guard is loudest exactly when it is most wrong.
- (2026-08-15) **GO AND LOOK AT A SECOND INSTANCE BEFORE FILING AGAINST THE CONTRACT** (dc, who did it and therefore did not file). AC-11.3 carries no `-- evidence:` / `-- satisfied:` and dc had it queued as a malformed row; they checked AC-05.1, found the same shape, saw that every row carrying those fields is `(non-test)`, and concluded the grammar is consistent. **Test ACs are satisfied by their covering AT; non-test ACs by named evidence.** One lookup killed a false finding. **This is the check that would have saved me two wrong findings today and ic two withdrawals** -- and in every one of those cases the first instance looked conclusive on its own, which is exactly when a second is worth the thirty seconds.
- (2026-08-15) **A STALE MEASUREMENT IS WORSE IN A MESSAGE THAN IN A CRITERION, BECAUSE A CRITERION GETS RE-READ AND A MESSAGE IS ACTED ON ONCE.** I sent cc and dc warnings that their capture halves were not in HEAD; both had committed them before I wrote, and I had measured, composed, and sent across a window in which the premise closed. **Neither peer could have known the claim was stale -- it arrived as current.** The fix is not more care in composing; it is **re-measuring immediately before sending anything time-sensitive**, because the composing is precisely what makes it stale.
- (2026-08-15) **A CRITERION CAN CONTAIN ITS OWN REFUTATION AND STILL SHIP** (cc's Q1, against AC-02.8, which is mine). The AC's opening paragraph says _"an authored date is a fact about the WORLD; a record timestamp is a fact about the DATABASE -- both are needed and they are not interchangeable"_, and a paragraph later it made them interchangeable for threads by having `created_at` REPLACE `threads.created`. **Replacing (b) with (a) means anyone who clones and rebuilds sees every thread created today.** cc's count is exact: **the fifth instance of the day's class, inside the AC written to fix the fourth.** The distinction being STATED is what made it feel handled -- I had written the discriminator and then stopped applying it, in the same criterion.
- (2026-08-15) **A COLUMN IS NAMED FOR WHAT IT CAN HONESTLY RECORD, NEVER FOR UNIFORMITY ACROSS TABLES** (cc's Q2, adopted). The store has **no `UPDATE` anywhere** -- every write is `DELETE` + `INSERT` -- so an `ON UPDATE` trigger can never fire (**a guard that cannot fire passes vacuously**) and a `created_at DEFAULT` records the LATEST write, **behaving as `updated_at` under the name `created_at`: AC-02.8's remedy reintroducing AC-02.8's defect.** `written_at` on the four wholesale-replaced tables; upsert + `created_at`/`updated_at` on the three with durable identity. **Uniformity was the thing that would have bought the lie back.**
- (2026-08-15) **THE INBOX IS NOT SINGLE-WRITER; IT HAS ONE APPENDER AND ONE CLEARER** (issue 0034, measured on my own `13e1b530`). Two mutators, two sessions, two schedules -- and **whichever node commits the file first commits the OTHER'S act.** I committed cc's and dc's inbox CLEARS while their `.history/` captures sat uncommitted, so at HEAD those entries were in no tree at all. **The correct rule already exists and is filed under the wrong subcommand**: `SKILL.md:232` says `git commit --only <you>/...` -- own-directory -- but it lives in step 5 of `archive`, and `ask`/`announce`, the two ops that write into a PEER's directory, have no commit step at all. Meanwhile `:276` calls the inbox the sender's file, so a sender committing it is FOLLOWING the invariant. **Third split-change today from three unrelated mechanisms, which is what makes it structural.**
- (2026-08-15) **WHEN THE FIX IS ALREADY WRITTEN AND SCOPED TOO NARROWLY, PROMOTE IT -- DO NOT DESIGN A NEW ONE.** My first draft of 0034 diagnosed "the model names the wrong thing as the unit of ownership" and proposed directory-ownership as a new rule. Then I grepped every occurrence of `commit` in the skill: **there is exactly one commit instruction in the whole protocol and it already says own-directory.** The defect was its scope, not its content, and the honest version of the issue is smaller, cites a line number, and costs a promotion instead of a redesign. **I had also written "the pathspec was right" -- it was not; it was wrong against a rule I had not yet read.**
- (2026-08-15) **A PROPOSED FIX CAN BE A DESCRIPTION OF THE DEFECT, AND EVERY CLAUSE OF IT CAN BE TRUE WHILE IT IS.** Issue 0033 recommended "rewrite the status token in place with a targeted `sed` rather than reconstructing the row from parsed fields, because that shape cannot lose a field it never parses" -- and `bin/intent_acceptance:1341` already IS a targeted in-place `sed` that parses no fields and is the line destroying the note. **The stated property was real and simply not the property that mattered: an in-place rewrite loses whatever its own pattern matches, and this one matched to end-of-line.** Anyone following the recommendation writes the defect again and reasonably believes they fixed it. **A fix must be checked against the code it replaces, not only against the diagnosis** -- and mine was written from a root cause I had INFERRED rather than read.
- (2026-08-15) **A FACT ABOUT WHITESPACE CANNOT BE STATED IN A CODE SPAN, BECAUSE THE FORMATTER OWNS THE WHITESPACE.** Issue 0033's root cause named the AT note delimiter as a padded span; CommonMark strips one space from each side and the linter normalises the source to match, so **a claim whose whole subject was that the delimiter is made of spaces silently became a claim about a bare double hyphen.** Unlike the stale-index case this one IS visible in `git diff HEAD` -- and it still stood, because a one-character diff inside a sentence about hyphens reads as formatting. **Write the fact in words when a mechanism can rewrite the notation.**
- (2026-08-15) **ASKING IS THE ACT BEING RULED ON.** Not "where did the time come from" but "why are you asking at all". Three nodes independently produced one-well-sourced-clock when the rule was no-clock. **When the wrong shape is the intuitive one, enforcement must be structural -- a rule everyone agrees to remember is the thing that already failed.**
- (2026-08-15) **A CRITERION WHOSE SATISFYING ACTION HAS NO MECHANISM IS A CONTRACT DEFECT, NOT A BUILD GAP** (ic's EXP-05). AC-06.8/06.9 both said "wire it or withdraw it" while the surface could withdraw a command and not a flag. **"Do X, or do the thing that cannot be done" is not a disjunction** -- and it stood in two rows for hours because the second arm READ like an option.
- (2026-08-15) **A FIELD THAT LOOKS LIKE THE ANSWER IS WHY NOBODY LOOKS FOR THE ANSWER.** Zero of eight tables had a record timestamp, and three columns resembled one: authored dates, a file's mtime, and an argument. **Same family as the sufficient-looking check, in a schema instead of a test.**
- (2026-08-15) **A SWEEP DOES NOT MOVE A FILE, IT SPLITS A CHANGE** (ic, adopted over my weaker "a move is TWO facts"). Each half reads as finished alone; only the pair is coherent, so **there is nothing file-shaped for a reviewer to notice.** After a sweep the question is not "whose file is this" but "does it still build" -- **asked of HEAD in a clean clone, never of the worktree**, which holds the very half whose absence broke it.
- (2026-08-15) **AN AUTHORED DATE IS A FACT ABOUT THE WORLD; A RECORD TIMESTAMP IS A FACT ABOUT THE DATABASE.** Both are needed and they are not interchangeable. Conflating them is how `threads.created` became a confection nobody questioned.
- (2026-08-15) **A GUARD THAT MUST BE BYPASSED IS A GUARD NOBODY KEEPS -- so refuse in the place that does not stop the world.** Ruled `pending` flags out of the build rather than out of the BUILD PROCESS, and answered the quiet-absence hazard in `doctor` instead. Measured the same day: a provenance guard reading the WORKING TREE froze every node's commits over one peer's untracked file.
- (2026-08-15) **A SUFFICIENT-LOOKING CHECK THAT ANSWERS A NARROWER QUESTION THAN THE ONE BEING ASKED, AND FAILS GREEN.** Named by dc; three instances in one day from three nodes. cc's store that opened cleanly and could not be read (`CREATE TABLE IF NOT EXISTS` makes the DDL a no-op). dc's doctor blank-field bug. **And mine: `codesign --verify --strict`, which I put in canon and recommended to hv, returning 0 on a binary we did not sign.** The tell is that the check is the one anybody would reach for -- that is what makes it survive review.
- (2026-08-15) **AN AC NAMES THE OUTCOME; THE MECHANISM BELONGS IN THE WORK PACKAGE.** AC-11.1 named cargo-dist and was invalidated by a measurement OF cargo-dist, while the thing the project actually wanted was perfectly achievable. **A criterion that names a tool can be killed by a fact about the tool.** Cost a rewrite instead of a rethink only because dc measured before wiring.
- (2026-08-15) **A STALE MEASUREMENT INSIDE A CRITERION IS WORSE THAN NO MEASUREMENT, BECAUSE IT READS AS CURRENT EVIDENCE.** AC-03.9 said db-to-disk "does not exist at all -- measured 2026-08-15"; `Facade::sync_to_disk` existed and `st sync --write` had been calling it. True when written, and nothing made it stop being true loudly. **Three instances today, all mine** -- the two vacuous greens are the same defect: my criteria acquire facts and never re-check them. More diligence will not fix it; a measurement needs to carry what it was measured against so it can go stale visibly.
- (2026-08-15) **AN ADVERTISED FLAG THAT NO RENDERER READS IS WORSE THAN AN ABSENT FEATURE** (cc, five live instances). Help lists it, the parser accepts it, the exit code is 0 -- **an inert flag is indistinguishable from a working one at the surface an operator actually reads.** Ruling: wired or withdrawn. And the census that finds them must walk the DECLARED surface, because cc's grep missed `st new -s` -- the long spelling is `start`, which is all over the renderer as a verb, so the grep found the spelling asked for while the claim had another one.
- (2026-08-15) **A GENERATED ARTEFACT WITH A HAND-KEPT COMPANION THAT MUST MOVE WITH IT.** Three instances today: cc's `SCHEMA_VERSION` beside the DDL (got a tripwire), dc's sha256 beside the tap formula (got one), and the five published schema faces beside **nothing at all** -- measured, zero occurrences of "version" in any of them. **The internal, never-committed store is versioned and the external contract is not, which is exactly backwards**: the store rebuilds from the extract, a consumer's code rebuilds from nothing we hold.
- (2026-08-15) **RECORD A SCOPE CALL WITH ITS REVERSIBILITY, NEVER AS A PRINCIPLE** (cc's warning, taken on D39). This estate has already converted a decision into a principle once -- "no DB migrations, ever" hardened into something people defended, and D01's reversal is what unwinding it cost. **A cost accepted knowingly is a decision; it becomes a defect the moment someone cites it as a reason.**
- (2026-08-15) **A SIGNED BINARY IN A SHARED BUILD DIRECTORY IS TRANSIENT AND CANNOT CARRY A CLAIM.** `target/release/` is shared mutable state; one `cargo build` from any peer replaces a Developer ID signature with the linker's ad-hoc one, silently. **An AC is satisfied by a decision recorded and a mechanism implemented, never by an artefact on disk.**
- (2026-08-15) **An INSTRUCTION that was correct when written becomes an ATTACK when its precondition silently vanishes** (ic). Every generated inventory header says "re-run it rather than editing this file" -- correct advice that, once the input TSV went missing, rewrote all 26 files with empty fields **carrying the good data's revision stamp**. **It aims itself at whoever is most conscientious**, because they are the ones who follow it. Same family as D36's "a phrase can do damage while it is still officially correct", one level up.
- (2026-08-15) **A missing measurement must present as a REFUSAL to measure, never as a measurement of nothing** (ic). Third instance today: a `cp` backup that opens cleanly with zero rows, a hooks roster reporting fewer guards than the gate enforces, and an inventory regenerated from an absent input.
- (2026-08-15) **A collapse makes the new representation obvious and the OLD INVARIANT INVISIBLE** (cc). The guarantees a multi-field version enforced must be RE-DERIVED after a collapse, never assumed to survive -- the fields that carried them are gone and nothing points at what they were for.
- (2026-08-15) **Deleting a discriminator does not delete the distinction; it deletes the ability to DETECT it** (cc, reversing their own board on `EdgeKind::Incidental`). "Unused" is the right reading of the code and the wrong reading of the design.
- (2026-08-15) **A guard can be CONSTITUTIVE rather than procedural, and that is a stronger defence than ratification.** `ac descope`'s target-exists check is what makes Descoped differ from Withdrawn -- a dangling pointer is withdrawal with a nicer story. Ratification alone leaves a rule re-litigable; a constitutive argument closes it.
- (2026-08-15) **Two artefacts transcribed by ONE author from ONE document in ONE session are not two witnesses.** They agree with each other and both differ from the source. Found `computed`, a fifth state in a machine hv ratified with four, living only in the code and the test that checks the code.
- (2026-08-15) **Reverse on evidence, including your own ruling, and say which grounds survived.** I ruled the AC shape at 14:04Z and reversed at 14:07Z when cc's form turned out to satisfy my own two strongest grounds better. **The grounds were right; I had applied them to a candidate list that was missing the winner.** Go and read what was BUILT before ruling on what was described.
- (2026-08-15) **Do not downgrade an AC on every finding.** ic's inventory finding does not touch AC-01.3, which claims the contract EXISTS and does. A verifier who downgrades reflexively is as useless as one who upgrades.
- (2026-08-15) **A GUARD CAN LIE IN THE AFFIRMATIVE, not only by silence.** dc's D34 pre-commit refusal fired green on my commit and I reported it to hv as evidence the ruling had teeth -- it is UNCOMMITTED, so it is a property of this laptop and a fresh clone of a public repo gets nothing. **Until work is committed, every observation any of us makes of it is an observation of one machine.** Stronger than the unwired-guard case, because that one merely says nothing.
- (2026-08-15) **A phrase can do damage while it is still officially correct, and the damage is that it PRICES things** (from dc's D36 measurement). `rm intent.db` was never safe -- `event_log` had no canon path even under old D01 -- yet three canon sites justified a deferral or a rollback by how cheap it was. **Check what a vocabulary is being used to BUY, not just whether it is true.**
- (2026-08-15) **A model field that carries internal provenance will be rendered, because the renderer is doing its job.** D37's leak was not six bad strings; it was `owed_by` existing and `render.rs` faithfully printing it. **"We just do not print that field" is a reminder; unreachable-from-a-surface is a control.**
- (2026-08-15) **A one-sided test against a failure event cannot see an event that never happened.** AC-03.10 said "a failed backup surfaces" and a schedule that never fires produces no failure to report -- so the clause written to prevent the nothing-is-wrong / nothing-ran ambiguity contained it. **Reach for the two-sided test**: staleness against a schedule, like the clock guard's check C comparing two stamps to each other and needing no clock. Found by ic asking for a read path I had not specified.
- (2026-08-15) **A hazard reachable through SUPPORTED CONFIGURATION is worse than one reachable by misuse** (ic). Hence the fixed snapshot directory, and no key that silences a backup failure -- a switch to turn a warning off manufactures the silent failure and gives it a supported name.
- (2026-08-15) **GROUND THE NUMBER BEFORE RULING ON IT.** hv's instruction, and it changed the ruling's REASONING rather than confirming it. Everyone assumed binary dirtiness would kill DB-in-git; measured, git deltas SQLite well and the size ceiling decides it. **A correct conclusion resting on a false reason is the same defect as a wrong conclusion -- it just fails later, when someone leans on the reason.**
- (2026-08-15) **A hazard demonstration that fails to reproduce has told you NOTHING about the hazard -- it has only told you about your harness** (dc's wording, adopted over my weaker "suspect the setup"; his names why it is dangerous, which is that the two are indistinguishable from the green). The `cp`-of-WAL probe looked clean until I noticed it read the DB first, which checkpoints. Corollary worse than the lesson: a `cp`-based backup passes every hand-check and fails only under production concurrency. **And a test that has only ever been green proves as little as one that has only ever been red.**
- (2026-08-15) **Three nodes reaching one question from three unrelated entry points is the strongest evidence available that the gap is real.** One node asking is a node's confusion; three converging is a hole in the design.
- (2026-08-15) **A board field is a claim about a session's last fold, not a fact about the present.** I inferred authorship of a live edit from `status: active` and was wrong -- the editing node was `paused`.
- (2026-08-15) **`corrected` PRESERVES a ratified guard; "aspirational" reinterprets it.** When a ratified requirement and a measured v2 behaviour disagree, the requirement governs v3 and the gap is a parity class -- never a reason to widen the guard to fit what the code already does, which is invisible because the result is green.
- (2026-08-15) **The satisfied-count going DOWN during a spec change is the contract working.** A rising count while the spec moves is the thing to distrust.
- (2026-08-15) **Correct in place with a named Correction notice; never a quiet edit.**
- (2026-08-15) **A control refuses; documentation reminds; only one is load-bearing** (cc). Treat a rule you can obey only by concentrating as an unfixed defect.
- (2026-08-15) **File a defect under its own noun, even when that reopens a closed WP.**
- (2026-08-15) **Report what you MEASURED, never what you INTENDED** (ic and dc, independently).
- (2026-08-14) **Verify a claim by re-running its evidence, never by reading its account.**
- (2026-08-14) **The contract leads the build or it trails it, and trailing costs more.**
- (2026-08-14) **hv standing authorisation is not review, and does not reach a ratified decision.**
- (2026-08-14) **hv ruling: cc and ic write the code; vc ensures.** vc holds the ST0056 claim as steward and does not build.
- (2026-07-02) vc fires on a close/green/freeze trigger or an hv request, never on in-flight edits. Advisory only; findings to the owner's inbox, compounding risk to hv.

## Archived at the 2026-08-17 03:0xZ localfold -- the 2026-08-16 session, closed

### DOING, as it stood when 0043 and 0042 closed

- **THE SESSION'S HEADLINE IS ISSUE 0043, CRITICAL, ANNOUNCED TO ALL FOUR NODES: ANY PROJECT CARRYING THE CANON HOOKS REFUSES EVERY CLAUDE CODE PROMPT THE MOMENT v3 IS ON PATH -- MIGRATION IS NOT THE TRIGGER.** (Read as "a migrated project" until 20:04Z; dc measured the widening and **my own ARMV3 had already proved it in a directory that was not an Intent project at all.** Corrected here rather than left standing, because a superseded headline on THIS board is the exact class this board spent the day filing.) Two shipped consumers read `intent`'s exit codes and disagree about `2`: the pre-commit gate reads it as "tooling unavailable" and **fails open** (correct, and why cc fixed 0038 by moving `1`->`2` at `d2b8e76d`), while **`UserPromptSubmit` reads `2` as BLOCK.** `.claude/settings.json` wires every prompt to `intent claude hook require-in-session`; v3 does not implement it; **measured `rc=2`.** **The lockout is self-sealing** -- `/in-session` needs a prompt, the sentinel escape needs a path the gate no longer prints. **NOT a mistake in cc's fix: an exit code was treated as a property of the TOOL when it is a property of the CALLER's contract, and nothing enumerated the callers.** No global value is right -- `1` breaks the commit gate, `2` breaks the prompt gate. **Three in-repo sources agree `2` blocks** (the script's comment, its implementation, and `working-with-llms.md:306`+`:580` telling users how to turn blocking off). **AND THE REGISTER CANNOT SEE IT (ic): `claude hook` HAS a `keep`/`as-observed` row, so the canon asserts it ships. Every surface instrument compares the table to clap's SHAPE, and the command is present, correctly shaped, and parses. It answers -- it answers `2`. The register cannot distinguish WIRED from WIRED AND IMPLEMENTED, and that is what this costs.** **CONFIRMED LIVE AT 19:54Z AND NOTHING IN IT IS INFERRED NOW.** Five arms against Claude Code 2.1.233, each a throwaway dir whose `--settings` wires `UserPromptSubmit` exactly as `.claude/settings.json` does, driven headless with `claude -p`: **exit 0 runs, exit 1 RUNS, exit 2 BLOCKS, the real v3 binary BLOCKS carrying its own not-implemented text, and `/in-session` BLOCKS.** **ARM1 is the arm that mattered and it was not in my plan** -- confirming that `2` blocks leaves the CAUSAL claim untested, and the causal claim is the whole issue, because it says cc's fix from this morning created this. `1` does not block, so `d2b8e76d` created it exactly as filed. **ARM2-SLASH settles self-sealing by measurement**: the documented remedy is itself a prompt. The other escape is visibly unavailable in ARMV3's output -- **no sentinel path is printed, because the script that prints one never ran.** **AND ONE FINDING THE ARMS ADDED THAT I DID NOT ANTICIPATE: the `claude` process exits 0 on a blocked prompt.** The block is in-band, so **any wrapper checking the process exit code sees success while the model never saw the prompt** -- a second silent-failure surface, in the layer you would use to detect the first.
- **AND THE RIG THEN ANSWERED THE QUESTION 0043's OWN FIX ASKS: ENUMERATE THE CONSUMERS. INTENT SHIPS THREE CLAUDE CODE HOOKS AND ONLY THE FATAL ONE HAD EVER BEEN MEASURED.** `SessionStart` -> `intent claude hook session-context` **rc=2, does NOT block**; `UserPromptSubmit` -> `require-in-session` **rc=2, BLOCKS**; `Stop` -> a bare `echo`, **never invokes `intent` at all**. **Two of three break, in OPPOSITE directions, and the third was never at risk.** **`SessionStart` failing open is a finding, not a relief** -- the session is usable and `session-context.sh` never runs, so **the project context and the `/in-session` reminder, which is the documented entry to the whole gate mechanism, silently do not arrive.** So the migrated experience is: **the session opens with its context quietly missing, then refuses the first prompt.** **`Stop` is clean ONLY by accident of its wiring** -- measured at exit 2 it is **24s and ZERO output against 3s and `PONG`**, because Claude Code reads `2` there as _do not stop_. Routing `Stop` through `intent claude hook` is the obvious tidying move and would arm a third failure from the same constant. **So `2` has FOUR measured meanings on four contracts: fail-open, block, advisory, refuse-to-stop. There is no value of `EXIT_UNAVAILABLE` that is right for all four** -- the fix is per-caller, and that paragraph in the issue now says so.
- **0044 FILED (high): THE MIRROR OF 0043. 0043 is four CALLERS disagreeing over what `2` means; 0044 is the TOOL using `1` for FIVE unrelated conditions, one of them "your code has findings".** Swept every top-level command at the pin, bare, in its own empty dir (`bootstrap`/`daemon`/`mcp` excluded -- global writes and servers). **`2` is RELIABLE: 13 of 30 are unimplemented and all 13 exit 2, so `d2b8e76d` is honoured consistently and this issue depends on that.** The defect is everything else: **retired, missing-subcommand, missing-arg and a genuine runtime refusal are all `1`.** **The structural cause is the keeper: the exit code is decided by WHERE the failure lands in the parse tree, not by WHAT went wrong.** An unimplemented command is caught after dispatch and gets the deliberate code; **a RETIRED one never reaches dispatch, because retirement removes it from the clap surface -- so `d2b8e76d`'s careful work is structurally unreachable for exactly the class a migration is most likely to hit.** **MEASURED IMPACT, in the consumer dc named as unenumerated: `bin/.devbin/lib/cmd/docs:58` calls `intent treeindex` and never checks `$?`, so under v3 both dirs fail, the errors PRINT, and the gate returns 0 with an EMPTY verdict file.** Not silent -- dc's 0042 correction applies -- **but green.** **Measured with a STUB reproducing v3's behaviour, never the real binary, because v3 must not touch a PATH.** Also: **`intent version` exits 2** (no args, cannot fail environmentally, most script-callable command in the tool). **Ordering argued in the issue: NOT a blocker for 0043, and must not be bundled into it** -- 0043 is a lockout and settling this under the same pressure is how the first three arrived.
- **THE HIGHLANDER REVIEW hv ASSIGNED IS DONE: 5 findings over 13,905 lines of `src`, measured on a `git archive HEAD` extract so no peer's dirty tree was read.** **0040** (`st_prefix` declared, defaulted, read by nothing, while v2 honours it in six places -- **filed HIGH, measured, then LOWERED BY ME to medium** after running the fleet survey I had named as the deciding test: all 16 projects use `ST`, so no migration we are about to run can hit it). **0041** (the status vocabulary spelled twice in two crates, byte-identical, related by nothing, only one copy carrying its rationale). Three smaller reported direct: **`backup.rs:216` is a fourth private `relative()`** and I COMPILED both to prove they diverge; **`remedy()` five times with no trait**, its render line written six times in three files; **nine test files hand-spell a config fixture** testkit has no home for. **CLEAN and reported as such: zero duplicate type names, and the thin-skin invariant HOLDS** -- `intent-cli/src` has zero `rusqlite` and zero fs writes.
- **0042 (high) IS THE ONE STILL LIVE AFTER 0038's FIX, AND IT IS THE FAIL-OPEN SHAPE.** `pre-commit.sh:104` resolves the whiteboard guards by parsing `intent info` stdout; v3 does not implement `info`, the value is EMPTY, **both guards take the fail-open branch and do not run.** **NOT "silently" -- dc corrected that by RUNNING it: each guard names itself and the empty resolution shows as a bare leading `/`. The larger half stands: both go, it fails OPEN, and dc named the cause about their own code -- "the guard file is absent" and "the tool that locates guards is absent" are two different absences and the `else` branch cannot tell them apart.** Independent of the exit code -- the hook reads stdout and `2>/dev/null` eats the error -- so `d2b8e76d` could not have touched it. **0038 failed CLOSED: loud, blocking, fixed in a day. This fails OPEN: the commit succeeds and two controls quietly stop enforcing.** The clock guard is the control that refused MY commit carrying a fabricated stamp. **AC-10.10 + AT-10.10 added, because AC-10.9 ("a migrated project can still COMMIT") is satisfied by a commit that succeeds with both guards off** -- necessary, not sufficient. **Assert the REFUSAL, never the success.**
- **RULED FOR cc AT THE FOLD, BOTH SO NEITHER WAITS ON THE COMPACT.** **(a) `datetime('now', ...)` INSIDE the query is WITHIN D42 -- build it.** The principle is "no node ever HOLDS a time", and there is no gap: the value is resolved during statement evaluation and never materialises. **But flagged to hv rather than decided by me: a clock-dependent window makes committed `todo.md` change with no mutation behind it.** hv ruled the window; hv did not rule which SURFACE it applies to. **(b) The committed extract MAY LAG**, so "entities present, no history" is NOT provable loss and cc's narrow check is the most that can be said -- requiring currency would put a second write on every mutation's critical path, which is the double-truth D01's reversal removed. Consistent with `migration.md`'s pre-stamp ruling reached from the other end.
- **dc's INVARIANT, ADOPTED VERBATIM AND BETTER THAN MINE: THE READ AND THE MOVE HAVE TO BE THE SAME ACT.** dc nearly archived my 0043 announce unread -- their fold enumerated, archived and restored the sentinel in one pass, and four messages landed inside the window. **I got as far as "count, stop, read, then archive, in SEPARATE commands" and stopped there, which fixes ordering and leaves the window wide open, because separate commands are exactly where minutes accumulate.** Their second sentence is the one that generalises: _"I did not think of an inbox count as a premise."_ **A premise you verified and then acted on later is a premise you did not verify.** And the thing neither of us said out loud: **the check WORKED** -- caught by a count disagreeing with an expectation, recovered from history, nothing lost. **`.history/` capturing rather than deleting is doing more work than either of our disciplines.**
- **RULED FOR ic (provisional-vc pending hv): the withheld-13 earn a DECLARED FIELD, but NOT a second exposure flag.** `exposed_on_mcp` failed by naming a SURFACE while carrying a PROPERTY, so D45 stranded it; `agent_safe` rebuilds that fault at a new address. **Declare what the verb ACTS UPON** (entity / estate / environment) and let MCP's withhold list DERIVE. `read_or_mutate` is too coarse: `st new` vs `init` differ in blast radius, not direction. **Canary: the new field must reproduce the existing 13, computed not restated. CONDITION I WILL HOLD: it ships WITH its consumer and its check in ONE change, or it is instance seven of my own finding.**
- **FOUR SPEC CORRECTIONS LANDED, ALL THE SAME SHAPE -- A DOCUMENT SUPERSEDED BY SOMETHING NOBODY PROPAGATED.** `migration.md`'s residue table declared SIX classes against `legacy.rs`'s EIGHT, and the two missing were **the only two this estate emits** (guarded now by `residue_class_check.sh`, canaried six ways). `data-model.md`'s Machine 3 still listed FOUR AC states 230 lines after the same file ratified the fifth. `data-model.md:45`'s watermark section described a mechanism D44 deleted. `parity.md` carried `undefined` as provisional after hv ratified it. **A superseded table beside its own correction is worse than either alone, because agreeing with the document stops being a test of anything.**
- **WHAT I VERIFIED RATHER THAN ACCEPTED, because that is the job.** cc's "this repo is migratable" -- re-ran it: 0 blocking, 9 carried, **and I counted the estate from the FILESYSTEM: 56/140/280/227 against 56/140/280/227, exact at all four levels.** cc's 0038 fix -- driven end to end through the shipped hook, **and the whole class measured, finding one arm still open** (`organize`/`treeindex` still exit 1). ic's `canon_keys_are_read` canary -- **holds, and by a BETTER route than the condition I relayed** (their one mutation covers both arms and needs no edit to the shared table). ic's withheld-26 -- **holds, and checking it found `config` marked `mutate` while its own help says it displays -- **fixed at `1aae294c`, and REMOVING IT MADE THE PARTITION EXACT**: the 26 withheld rows are now 13 mutations and 13 reads, precisely the 13 leaves and the 13 roots, where it read 14/12 while `config` was wrong. **ic's line is the one to keep: a single bad field made a rule look like a tendency.** **ic also corrected my reasoning while accepting the finding -- it is NOT "roots are reads"**: 14 roots, 12 `read` and 2 `mutate`, and **`todo`'s `mutate` is CORRECT** because bare `intent todo` inherits `list`'s generate-on-absent write. **`todo` is the counter-example that gives the rule teeth -- a root is marked on its OWN behaviour, not the union of its children** -- and `config` was the single row breaking it.** cc's vacuous-assert class -- **swept multi-line: five disjunctive asserts in the whole suite, all intentional, theirs was the only one.**

### TODO items completed or superseded

- **hv RULED BOTH OPEN CALLS AT 19:36Z UNDER A STANDING "go with your recs": `st_prefix` RETIRES, and D44's WINDOW IS TERMINAL-ONLY.** Both landed in `data-model.md`, both routed to cc. **0040's retirement turned out NOT to be a change of direction and that is the finding: `st_prefix` appears in NO ST0056 spec** -- the config table lists six fields and never included it, so **the design had already dropped the knob and nobody propagated that to the type.** The code was BEHIND the design, not ahead of it. **The load-bearing obligation is that the migrator NAMES the field when a project carries a non-`ST` value** -- costs nothing today (all 16 use `ST`) and is the only thing making it safe for anyone outside the fleet. `legacy.rs:198` loses its hardcoded `len() == 6` in the same change. **D44: build the `datetime('now', ...)` query as cc proposed -- the ruling is about where its ANSWER may land, not about the clock.** A committed file is a record and may not depend on now; a terminal render is a moment and may.
- ~~**FOR hv, THE ONE OPEN CALL: issue 0040, `st_prefix`.**~~ **CLOSED, see above.** Honour it (both consumers read `project.config().st_prefix`; `legacy.rs` loses its hardcoded `len() == 6`) or retire it (a `disposition: retire` row with a ratification, `default_st_prefix` deleted, and **the migrator NAMES the field when it carries a non-`ST` value forward**). **Retiring is a scope decision, so it is hv's; what is not defensible is today's state, which is neither.** My lean is retire -- all 16 fleet projects use `ST`, nobody has ever used the knob, and every future id-touching feature would have to honour a variable that is always the same. **Whichever way: the canary is a fixture with a non-default prefix, and none exists, which is why nothing caught it.**
- ~~**ALSO FOR hv: which SURFACE does D44's todo window apply to?**~~ **RULED: terminal render. Kept for its reasoning:** hv ruled the window; a clock-dependent window makes **committed `todo.md` change with no mutation behind it**, which is committed churn under D02. My lean: window the TERMINAL render (a moment), let the committed file carry everything (a record). **If the file must carry the window, it must also carry WHICH window it was generated with**, or a dropped row and a deleted row look identical.
- ~~**VERIFY ON THE BOUNCE**: WP-03 back to 11/11~~ **DONE 19:52Z: WP-03 IS 11/11 AT PINNED SHA `0ef6e0a1`.** All twelve AT-03.x green, complete AC coverage (AC-03.7 carries two), and **the all-green set is the one case where 0032's OR-trap cannot manufacture a false positive, because OR and AND agree.** Two independent routes agreed: my derivation from `acceptance.md` and `intent ac list`. **The work package had been understating cc all day** -- AT-03.12's test landed at `0e82b116` and the row sat at `to-write`, which asserts the test is UNWRITTEN. `stale_at_check.sh` found it; **my earlier `stale_at=0` predated the file, so the instrument was right and my reading of it was stale.** **AND THE MOVE EXPOSED THE SECOND FACE OF THE VOCABULARY GAP: `to-write -> green` is refused, green is reachable only from red.** So to record a truth (it passes) I had to first record a state that was never true (it failed). `red` is doing double duty -- "written and failing" and "written, result not yet established" -- and those are different facts. Harmless as an intermediate today; **under v3, where transitions are events, that path writes a red that never happened into the record.**
- ~~cc's scope build at `5fae26ea`~~ **VERIFIED AND CLEARED 20:05Z, AND GREEN WAS NOT THE VERIFICATION -- I FALSIFIED BOTH ARMS AND THE TESTS DISCRIMINATE.** `legacy_scope_carry` 7/7 at the pin, plus `doctor_checks` 19/19, `model_laws` 3/3 and `store_schema_version` 6/6 (so `afd90989`'s DDL re-pin is confirmed, not assumed). **Arm 1** -- restoring the silent twin, `(true, None) => Some(TShirt::M)` -- reddened exactly two tests and left five green. **Arm 2** -- restoring the pre-fix carry -- reddened three DIFFERENT tests and left arm 1's green. **The red sets are complementary and the only test red under BOTH is the one asserting all three states are distinct.** That is a suite that knows which arm broke. **Perturbed in the scratch extract only; live `legacy.rs` never touched, confirmed clean afterwards.**
