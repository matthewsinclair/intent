## Shapes

- **AN UNCOMMITTED SHARED CANON HAS NO OWNER AND NO OBSERVER, AND NO NODE CAN SEE MORE THAN ITS OWN WRITES.** Measured 2026-09-05: SEVEN uncommitted canon changes across THREE nodes -- untracked `0251` `0256` `0259` `0260` `0263`, modified `0223` (cc) and `st/ST0056.json` (dc's WP-12 `not-started` -> `wip`, the reconciliation I asked for). cc reported SIX and did not know about dc's at all, which is not cc's carelessness: **cc has no instrument that would show it.** Each node sees the rows it wrote and the tree shows the union to nobody in particular. **DETECTION: `git status --short intent/.canon/` is the only view of the union, and it belongs to whoever is about to relay a count. I caught cc's figure only because I ran it rather than repeating it.**
- **THE EVIDENCE A SCEPTIC REACHES FOR CAN SAY THE OPPOSITE OF WHAT IT APPEARS TO SAY (cc's `0263`, verified independently by me).** `.prettierignore`'s header claims the gate runs `prettier --write` and re-stages. `.githooks/pre-commit` runs `--check` on the staged blob and refuses without rewriting. **AND THE ONLY `prettier --write` IN THE WHOLE HOOK CHAIN IS THE REMEDY STRING THE GATE PRINTS TO A HUMAN** (`:100`), so a sceptic grepping the hooks to test the claim finds that line and reads it as CONFIRMATION of a mechanism that does not exist. **I TOUCHED THAT FILE TODAY AND READ PAST ITS HEADER** -- `4649c2b64`, where I added the `.history` exemption and wrote a comment block directly beneath a paragraph I never questioned. The file's own editor is not an observer of it.

- **WHEN FOLDING SOMEONE ELSE'S FINDING, THE WORDS THAT CARRY SEVERITY ARE COPIED, NOT PARAPHRASED (ic's W91, written FROM my retraction, and it lands harder on me than on ic).** Stale/dangling. Refused/ignored. Silent/loud. Missing/wrong. **A fold IS compression, and compression is where a category quietly upgrades.** **I am the heaviest folder on this board** -- the 246-to-113 localfold is where I manufactured the attribution -- so this is my practice to change, not a note I am recording about a peer's.
- **A CONDITION CAN BE VOIDED RATHER THAN MET, AND A VOID CONDITION IS NOT AN UNMET ONE (cc's, generalised by cc from my own insistence).** My `0196` hold carried an EVENT (`the window opening`) and a STATE (`the pair's marker at or near HEAD`). hv's ruling killed the event outright -- the window will never open -- and the state survived untouched. **A hold keyed only on the event would have gone stale the instant hv ruled, with nothing marking the departure**: the Holds section's own silent-exclusion failure, arriving THROUGH the condition rather than through a missing one. **DETECTION: re-read every condition when the world that made it sensible changes, not only when it might have been satisfied.**

- **ic's, AND THE STRONGEST RULE OF THE DAY: A SECOND RUN THAT CANNOT DISAGREE WITH THE FIRST ABOUT THE ONLY THING THAT MATTERS IS A REPLICATION, NOT A CONTROL.** ic verified a count with two patterns differing ONLY in the line anchor -- **the suspect clause was identical in both**, so the agreement was guaranteed by construction and read as corroboration. **DETECTION, MECHANICAL: BEFORE CALLING TWO INSTRUMENTS INDEPENDENT, DIFF THEM AND ASK WHETHER THE SHARED PART IS THE PART UNDER SUSPICION.** It is the mutation rule pointed at instruments rather than tests: **a mutation must change the thing the test claims to read; ic's second pattern changed the other thing.**
- **A CONVERSATION WHERE BOTH PARTIES ARE DONE AND EACH IS WAITING TO SEE WHETHER THE OTHER IS HAS NO TERMINATING CONDITION.** ic and I each wrote _taking nothing, nothing further_ twice and continued, **because the other replied and every round produced something good** -- which is what makes it hard to stop rather than what makes it worth continuing. **SAME MISSING STATE AS THE MORNING'S: an inbox records what arrived and never what happened next; a conversation records what was said and never that both parties are finished.** **CURE, USED: close on a CONDITION -- _I will not reply again unless you send something that needs a ruling_ -- rather than on a sign-off, which is an intention and invites a reply.**

- **ic's, AND IT GOES ABOVE THE ASK-AGAIN RULE RATHER THAN BESIDE IT: A CONSTANT OFFSET ACROSS INDEPENDENT REVISIONS RULES OUT TIMING; A VARYING ONE RULES OUT METHOD.** Two counters differing by exactly one at four separate revisions is a METHOD difference and nothing else. **IT RUNS BEFORE ANYONE HAS TO EXPLAIN ANYTHING, so it is cheaper than the round of re-asking, and it would have pointed straight at `new_surface` in the 122/138 case where the offset was also constant.**
- **A ROW THAT NEVER MATCHES AN ANCHOR NEVER GETS THE CHANCE TO FAIL IT -- A FILTER'S SILENCE IS NOT THE POPULATION'S ASSENT.** Resolved the 99-vs-100 in one loop: the row is `W4a`, a LETTERED sub-entry on dc's board. My loose pattern takes a digit after `W` and matches it; ic's `W<digits> -- ` anchor cannot. **ic reported _no row fails my strict anchor_, which is true and is not the claim it reads as.** **THE CONCLUSION INVERTED: the extra row is REAL, so the stricter instrument was the one silently narrowing the population** -- and it did so inside a measurement about identifiers.
- **ic's CRITERION FOR AUDITING A CONFESSION, ADOPTED VERBATIM AND OWED BEFORE THE PASS: A SELF-CRITICISM THAT NAMES NO SPECIFIC DECISION I COULD HAVE MADE DIFFERENTLY IS A STORY RATHER THAN A FINDING.** **The rule comes FIRST -- _audit your own confessions_ invites the same reflex one level up and becomes a second performance.**

- **ic's, AND IT IS BIGGER THAN THE W-NUMBERS IT WAS FOUND IN: WHEREVER A DEFECT HAS A LOUD MODE AND A QUIET MODE, THE LOUD MODE TRAINS YOU AND THE QUIET ONE ACCUMULATES.** One cross-node citation fails TWO ways by receiver -- **silently COLLIDING on dc's board (the only board using that form), visibly DANGLING on cc's, hv's and mine.** **THE ONLY FEEDBACK AVAILABLE COMES FROM THE CASE THAT DOES NOT NEED IT.**
- **AN OVERSTATED SELF-CRITICISM IS NEVER CHALLENGED, BECAUSE CHALLENGING IT LOOKS LIKE LETTING SOMEONE OFF** (ic). So it enters the record unopposed and every later reader takes it as measured -- **the harsh claim has no loud mode.** **THIS BOARD TOOK A DOZEN CONFESSIONAL ENTRIES ON 2026-09-05 AND I HAVE DRIVEN EXACTLY ONE OF THEM FOR OVERSTATEMENT -- the one ic challenged.** A claim about my own failure carries the same burden as any other and nothing in this estate would ever question one. **OWED: A REAL PASS OVER THE CONFESSIONS, NOT TONIGHT.**
- **A COUNT OF A LIVE ARTEFACT IS A MEASUREMENT WITH A TIMESTAMP AND ALL THREE OF US PUBLISHED ONE AS A PROPERTY.** dc said 96, ic 99, I said 100, of W-entries on dc's board. **DRIVEN ACROSS dc's OWN COMMITS: 95 -> 96 -> 98 -> 100 across five commits in ninety minutes.** dc's 96 and my 100 are each CORRECT AT THE REVISION THEY READ; ic's 99 matches no committed revision and the cause is not determinable from here. **THE DELIVERED-PAIR LESSON ARRIVING IN THE ONE PLACE I HAD STOPPED WATCHING FOR IT: a board is as live as a binary.**

- **A COLLIDING CITATION READS AS UNDERSTOOD WHERE A DANGLING ONE ANNOUNCES ITSELF -- AND IT IS WORST BETWEEN ADJACENT RULES.** ic's numbered rules resolve on dc's board, the only board using that form (100 entries; cc, ic, vc and hv carry ZERO). **DRIVEN BY ME: ic's second citation lands on dc's _a control asserts a difference, so it must be driven against the state it claims to distinguish from_ -- a NEIGHBOUR of what ic meant.** A reader gets a plausible, correct, related rule in the same subject area and has nothing to be suspicious of. **AND THE ADJACENCY IS STRUCTURAL, NOT BAD LUCK: four nodes converging on one problem produce neighbouring rules, so the numbering collides hardest exactly where the topics are closest.** **QUOTE THE RULE TO A PEER, NEVER THE NUMBER.**
- **REACHING FOR THE SHARPEST AVAILABLE VERSION OF MY OWN FAULT IS A DISTORTION, NOT A DISCIPLINE.** I told ic I was _the worst offender_ on node-local numbers. ic drove it: they quoted six numbers to three nodes; I qualified every one of mine with the owning node -- **not a fix and not nothing.** **THE OVERSTATEMENT WAS REFLEX AND IT MOVES THE RECORD IN THE DIRECTION THAT LOOKS HUMBLE**, which is the direction nobody audits. **A confession is a claim and takes the same drive as any other.**

- **DEFINE THE POPULATION IN WRITING BEFORE YOU COUNT -- AND THE REASON IS NOT HONESTY, IT IS THAT DEFINING IT FORCES YOU TO LOCATE THE FIELD.** ic's rule, and it is stronger than the job I gave it. **I specified the step as an ANTI-ANCHORING control** (a definition committed first cannot be retrofitted toward a number already seen). **ic's definition, written from SHAPE ALONE, named all three levels -- `.families[].entries[]`, a top-level `.new_surface[]`, and `.families[].v2_source` as a per-family field never merged into an entry -- WHICH IS THE RECONCILIATION AND ALSO THE EXPLANATION OF MY OWN REFUTED HYPOTHESIS.** The expensive instrument never ran. **IN A DISAGREEMENT BETWEEN TWO CAREFUL COUNTERS, THE FIELD'S LOCATION IS USUALLY THE WHOLE DISPUTE, and locating it is the CHEAPEST step in the sequence.**
- **SEPARATE A VERIFIED RESULT FROM THE PROVENANCE CLAIM ATTACHED TO IT.** I verified ic's definition names all three levels correctly. **I CANNOT VERIFY FROM GIT THAT IT PREDATES dc's ANSWER REACHING THEM** -- 53 seconds between commits, message delivery in no log. **The CORRECTNESS is measured; the WRITTEN-BLIND claim rests on the author's word plus a commit message asserting it.** Naming the seam costs nothing and takes nothing from the result -- **a definition naming all three levels is right whenever it was written** -- but **a provenance claim nobody separates out is how a strong result acquires an unearned second claim.**

- **dc's W97, ADOPTED, AND I AM THE WORSE OFFENDER: W-NUMBERS ARE NODE-LOCAL.** ic's W76 and dc's W76 are different rules; dc measured 96 W-entries on the only board using that form, so **every cross-node `W<n>` citation resolves against dc's numbering or against nothing.** I qualified mine with the owning node all morning, which MITIGATES AND DOES NOT FIX. **A citation that silently resolves to the WRONG entry is worse than a dangling one. QUOTE THE RULE TO A PEER, NEVER THE NUMBER.**
- **dc's W96, WHICH I WANT STANDING FOR ALL FOUR OF US: A PEER'S MEASUREMENT ENTERING AN ARTEFACT OF MINE GETS DRIVEN FIRST.** dc put two of my measurements into filed issue bodies as measured fact and drove them only after ic named the class; both survived, **which is luck and not method, and dc reported it that way.** **AN ISSUE BODY IS THE MOST DURABLE THING ANY OF US WRITES** -- it outlives the session, the board and the conversation -- so it is where a borrowed measurement does the most damage and where the drive is cheapest to skip.
- **A COINCIDENCE THAT REPRODUCES THE TARGET NUMBER IS THE MOST DANGEROUS OBJECT IN A RECONCILIATION.** dc's `122 vs 138` resolved as `families.entries` 122 + a TOP-LEVEL `new_surface` array of 16, invisible to a families scan -- **driven by me, not accepted.** And `populations.declared` holds EXACTLY 138 entries, **not one carrying a `v2`**: a scan landing there matches the target for entirely the wrong reason and **would have ended the question with both parties satisfied and neither correct.**
- **I BUILT AN INSTRUMENT FOR A QUESTION THAT WAS BEING ANSWERED WHILE I BUILT IT.** dc reconciled the 16 in the minutes I spent designing an anti-anchoring protocol for a third independent count. **Not the usual form of _the disproof is already in hand_ -- it was in another node's session, the one place I cannot look.** **BUT: A TWO-WAY DISAGREEMENT IS WORTH ONE ROUND OF ASKING THE OTHER PARTY TO LOOK AGAIN BEFORE IT IS WORTH A THIRD INSTRUMENT, AND I WENT TO THE INSTRUMENT FIRST BECAUSE IT WAS MORE INTERESTING.**

- **ic's W76, AND IT LANDED ON ME WITHIN THE HOUR OF MY OWN AUDIT: A CLAIM REPEATED BACK TO YOU APPROVINGLY HAS NOT BEEN CHECKED -- IT HAS BEEN AMPLIFIED.** ic wrote, as an aside, that `intent at` has no verb retargeting a citation; I returned it at full width as _the mechanism that makes the rename dangerous_. **`intent at edit --file` EXISTS**, and ic drove it past clap to its own guard. **THIS IS MY THIRD AMPLIFICATION AND THE FIRST OF THIS SHAPE: the other two were claims I formed or repeated; THIS ONE I RECEIVED, APPROVED AND RETURNED WIDER, which reads back to the claimant as independent corroboration when it is their own sentence with my emphasis on it.** A two-node loop where neither drove it, load-bearing precisely because it now sits on two boards. **DETECTION, ic's: a claim of mine coming back in someone else's words is the moment to DRIVE it, not the moment to relax.** **SECOND TELL, cheap enough to use while writing: a sentence carrying the word `so` into a consequence is a claim wearing the clothes of a transition.**
- **AN AUDIT IS WORTHLESS IF IT ONLY LISTS WHAT YOU CHECKED.** Ran ic's instruction over everything I took from a peer on 2026-09-05. **DRIVEN: dc's `0255` counts -- 73 distinct CONFIRMS EXACTLY; my 122 rows against dc's 138 DOES NOT RECONCILE, and my one hypothesis (family `v2_source` too) is REFUTED at 149.** Reported to dc as a difference with my method, **not as a correction -- a number I cannot explain is not evidence theirs is wrong.** **NAMED AND NOT DRIVEN: cc's _hv ruled config/ext/learn declared-and-unbuilt 2026-08-31_**, which I restated to cc as a boundary and made load-bearing. Lower risk, still borrowed. **The third entry is the one that makes the first two mean anything.**

- **ic's W72, AND IT CORRECTS A CRITERION I WROTE: A MUTATION WHOSE OUTPUT IS VISIBLY GARBAGE TESTS THE READER; ONE WHOSE OUTPUT WOULD PASS REVIEW TESTS THE CLAIM.** I specified `0244`'s acceptance as _replace the remedy with nonsense and the test must fail_. **THAT ONLY PROVES THE TEST READS THE STRING, AND ANY MUTATION SHOWS THAT MUCH.** ic's M2 is the real one: swap `daemon start` for `daemon status` -- a WIRED verb that exits 0 -- and the old predicate PASSES while the operator is still stuck. **THE PREDICATE IS TRUE AND THE REMEDY IS USELESS.** My criterion was the warm-up and ic supplied the result.
- **cc's W73, VIA ic: WHILE A MUTATION SITS ON DISK IN A SHARED TREE, ANY NODE COMMITTING THAT PATH TAKES IT UNDER THEIR OWN MESSAGE.** `--only` protects a FILE and not a HUNK, and `git status` carries no author. **The hazard is never the mutation's author committing it.**
- **A REF TYPED BY FEEL IS THE SAME FAILURE AS A TIMESTAMP TYPED BY FEEL, AND THE ADDRESSING CHANNEL HAS NO GUARD.** ic invented `intent-vc [3a7f2c]` after being warned in the same session that two sessions answer to that name -- **turning a warning into a fabricated identifier rather than into a `ListAgents` call.** It bounced, so it cost nothing; a ref that happened to resolve would have delivered a ruling to a session idle for days, which reads as silence.

- **AN INBOX RECORDS WHAT ARRIVED AND NEVER WHAT I DID ABOUT IT, SO READING ONE COLD RECONSTRUCTS A QUEUE THAT WAS ALREADY DRAINED.** On 2026-09-05 I re-issued a GO to cc on `browsed()` that I had discharged myself the day before, off an entry that still looked live -- my reply had gone out over SendMessage and left no mark on the file. **THE PROTOCOL ALREADY CARRIES THE CONTROL AND I HAD NEVER RUN IT: `clear <sender>`.** Ran it this session -- cc, dc, ic and devbin-vc archived to `.history/20260905/`, verified complete before the overwrite. **AND cc's REFINEMENT IS SHARPER THAN MY SYMMETRY AND REPLACES IT: THE ITEM WAS ANSWERED ON 09-04 AND THE ANSWER NEVER REACHED THE ASKER.** cc's fold did the right thing to a completed measurement -- that is what a fold is for. **NEITHER OF US CHECKED WHETHER THE ANSWER HAD LANDED WITH THE PERSON STILL HOLDING THE ASK BEFORE REMOVING IT FROM OUR OWN VIEW.** So it is not two unused slots, it is **ONE MISSING STEP USED FROM BOTH ENDS: AN INBOX RECORDS WHAT ARRIVED AND NEVER WHAT HAPPENED NEXT; A FOLD RECORDS WHAT IS DONE AND NEVER WHO WAS WAITING ON IT.** `clear` and a condition on `## Holds` are both downstream of that.
- **BOTH OF THIS MORNING'S FAILURES ARE ONE BOOT DEFECT, AND IT IS cc's READING: THE ANSWER WAS ON THE RECORD AND MY BOOT PATH DID NOT REACH IT.** The stale `browsed()` GO -- discharged the day before, in my own words. `0232` -- filed BY cc precisely so the WP-08 question is never asked again, tracked, and I asked it the next morning. **A COLD BOOT READS BOARDS AND INBOXES AND READS NEITHER THE REGISTER NOR WHAT I SENT, WHICH IS WHERE BOTH ANSWERS WERE.**
- **A WORKED INSTANCE IS EVIDENCE THAT AN INSTRUMENT CAN FIRE. IT IS NOT EVIDENCE ABOUT THE POPULATION, AND IT PRICES NOTHING.** I authorised dc's adjacent axis because I had driven the join by hand on one row and it returned a flag. **dc's sizing showed that one hit WAS the entire yield** -- the defensible join returns 3 non-intrinsic instances and all three are the row already filed. **I read a census as a sample.** Withdrawn same session.
- **dc's, ADOPTED: TWO DEFENSIBLE READINGS OF ONE FIELD THAT DISAGREE BY AN ORDER OF MAGNITUDE MEAN THE FIELD HAS NO GRAMMAR.** `v2` joins at 7 and at 186 over the same data; **the gap between them IS the missing grammar**, and an instrument built on either reading emits a confident number over a population nobody declared. General diagnostic, not a fact about `v2`.
- **dc's W90 CURE, BETTER THAN MY DIAGNOSIS: AN ABSENCE ARM NEEDS A PRESENCE TWIN OVER THE SAME RUN**, so the run itself proves the instrument could have found what it says is not there. A control beats a caution.

**cc's REFRAME, ADOPTED 2026-09-04: A WATCH-OUT IS A CONFESSION -- IT EXISTS BECAUSE SOMETHING GOT THROUGH. A SHAPE IS A CONTROL.** Three levels of knowing were driven today and NONE bound: dc FORGOT a written rule, ic CITED one and violated it inside the hour, cc AUTHORED one and failed it on their next measurement. **KNOWING IS NOT THE VARIABLE.** Every rule that actually caught something was a shape. **THE TEST FOR A CONVERSION IS NOT THAT THE SHAPE EXISTS -- IT IS THAT THE UNSAFE FORM IS NO LONGER REACHABLE BY HABIT.**

- **EXIT CODES:** `out=$(cmd 2>&1); rc=$?` -- NEVER `cmd | head` and then `$?`, which reads the pipe. **THIS SHAPE WAS ALREADY WRITTEN HERE AND I REACHED FOR THE UNSAFE FORM ANYWAY ON 2026-09-05**, reading rc=0 off `head` where `intent upgrade --no-backup` returns 1. **By cc's own test the conversion has NOT happened: the unsafe form is still reachable by habit.** Writing it down again is not the fix and I am not pretending otherwise.
- **READ A SCHEMA'S KEYS BEFORE SCANNING IT, AND POSITIVE-CONTROL THE SCAN ON A KEY YOU KNOW IS POPULATED.** **FOUR TIMES ON 2026-09-05 I SCANNED CANON OR THE DISPATCH TABLE UNDER A KEY THAT DOES NOT EXIST** -- flags under `entries[].args` (they live in `entries[].flags`), citations under `ats` (they live in `tests`), an issue's number under `id` (it is `number`) -- **and every one returned a clean zero that reads exactly like a finding.** **THE CONTROL IS ONE LINE AND IT CAUGHT ALL FOUR: count a key that MUST be non-empty in the same pass** -- my flag scan returned zero rows for `--help` too, which is impossible, so the instrument was wrong rather than the table. **A ZERO FROM A SCAN WHOSE KEYS YOU HAVE NOT CONTROLLED IS NOT A FINDING.**
- **dc's W90 AT ITS FULL WIDTH, AND IT IS WIDER THAN THE CONTROL HELPER IT WAS FOUND IN: AN ASSERTION OF ABSENCE CANNOT DISTINGUISH A CLEAN SUBJECT FROM A DEAD INSTRUMENT.** dc's `grep` crashed on a flag-shaped pattern with no `--`; the five arms asserting PRESENCE failed loudly and the three asserting ABSENCE went green, **because a missing string is exactly what a crashed grep reports.** Three arms designed to catch silence were satisfied by silence. **This is the same defect as the key-scan zero above arriving through a different door**, and it is the general form of the honest-and-blind-grep family: the instrument must be shown capable of speaking before its silence means anything.
- **GLOBS HANDED TO A TOOL:** `find <dir> -name 'PAT'`, quoted. **NEVER `ls <dir>/*.ext`** -- zsh ABORTS on an unmatched glob and the surrounding command runs with nothing, returning a plausible zero. Four instances 2026-09-04, one of which was RIGHT FOR THE WRONG REASON.
- **A FILE'S TIME:** `date -u -r <file> +'%Y-%m-%d %H:%MZ'`. **NEVER `stat -f '%Sm'` plus a typed `Z`** -- that prints LOCAL and the `Z` is an assertion.
- **A BINARY'S IDENTITY:** the marker in `intent --version`. **NEVER mtime** -- the embedded marker cannot go stale between reading and using it; an mtime can, and did, three times in one hour.
- **A PEER'S STATE:** their message, or `git show <sha>:intent/whiteboard/<node>/wip.md`. **NEVER derived from my own instruction** -- an authorisation is not a report.
- **A CONTROL:** name the AXIS the claim is about, then confirm the two arms DIFFER on it. If they cannot differ, there is no test (cc: _reasoning wearing a test's clothes_). This is a step taken BEFORE, not a diagnosis after.
- **ANY STAMP:** `date -u +'%Y-%m-%d %H:%MZ'` present in THIS turn's tool output, copied verbatim.
- **THE SHARED INDEX:** `git add <paths>` then `git commit --only <paths>`, message from a FILE so every retry is byte-identical. On a lock: WAIT, never remove it -- a peer may be mid-commit.
- **BEFORE BELIEVING ANY MUTATION -- CHECK THE BASELINE AGAINST THE REAL RUN.** dc's first mutation harness ran a scratchpad copy, so `$HERE` was wrong and the baseline read `0 across 0` -- **THE MUTATED RESULT ARRIVING AS THE BASELINE.** Every mutation would have reported a clean kill while testing nothing. **A harness whose baseline already exhibits the defect CANNOT FAIL**, and it is the wrong-instrument-reporting-success case rather than the wrong-answer one.
- **BEFORE ANY INSTRUMENT RUNS -- THE NOUN CHECK: DOES ITS OUTPUT UNIT MATCH THE CLAIM'S SUBJECT NOUN?** Write the claim, underline the noun, look at what the instrument counts. **Every count-shaped failure tonight dies here instantly:** _does uninstall leave the DIRECTORY_ measured with `find -type f`; _do the BYTES differ_ measured by `len()` on decoded text; _is this file in GIT_ answered by `git ls-files`, which is MEMBERSHIP where the claim was CONTENT. **Checkable with no second arm and before anything runs.**
- **BEFORE ANY CONTROL RUNS -- THE AXIS CHECK (cc): the two arms must DIFFER ON THE AXIS and TRAVEL THE SAME MACHINERY.** Distinct from the noun check and neither covers the other: cc's `IFS` failure had the right NOUN and a collapsed AXIS; the file-count failure had a live axis and the wrong NOUN.
- **A BYTE COMPARISON:** never through `$( )` -- **command substitution STRIPS TRAILING NEWLINES**, so a comparison built on it calls two files identical when the only difference is a final newline, and canon JSON is exactly that file class (dc).
- **A CONTROL'S PATH:** the control must travel **THE DEFECT'S OWN MACHINERY**. Varying the axis is not enough if the arms take different routes -- cc wrote a negative control literally while the subject went through a variable, so it could not have caught the word-split no matter what it returned. **Arms share the machinery and differ ONLY on the axis** (cc).
- **A MULTI-WORD COMMAND:** build the argv as an array, or `eval` a single quoted string. **NEVER interpolate a multi-word command into one unquoted variable** -- zsh does not word-split it and you invoke one long argument. cc twice, me four times on the sibling glob trap.
- **PEER STATE IN MY FOCUS LINE:** do not put it there at all. It decayed twice on 2026-09-04 and both peers corrected me. **The focus line has no stamp of its own; a peer's state is a VALUE and values do not travel.**
- **A PUBLISHED FIGURE:** `git show <sha>:<path> | wc -c`, measured off the COMMIT. Three of four fold figures published today disagreed with the artefact.

- **A GENERATED VIEW AND A HAND-AUTHORED TRANSCRIPTION LOOK IDENTICAL IN THE TREE, SO _HOW MANY HOMES DOES THIS FACT HAVE_ IS NOT ANSWERABLE BY COUNTING RENDERS.** dc corrected canon for the WP-11 title at `16144205` and four views healed themselves; `tasks.md` is an ATTACHMENT carrying its own transcribed copy, so no render reached it and it was still stale after every other home was right. **A fifth and a sixth existed** -- the sixth in `ic/scope-sweep.md`, routed to ic rather than edited. **VALIDATION-SIDE, AND IT IS WHY THIS IS MINE: my instruments read canon and read rendered views, and a stale transcription is invisible to BOTH.** `AC-00.16` again, from the direction where the gap is between the render and the reader rather than between two instruments. dc banks it as W107; the verifiable half is _correct canon, then grep the string_.

- **THE SIDE THAT IS EASIEST FOR ME TO EDIT IS NOT EVIDENCE ABOUT WHICH SIDE IS WRONG** (cc's words, on my refusal of their WP-10 objective edit). _A WP cannot close on rows that no longer span its objective_ says the two sides must AGREE and says NOTHING about which side moves. **cc reached for the objective because it was the edit available without a ruling -- a fact about their REACH, not about the defect** -- and when the rows left by mistake, making the objective agree with them is how the mistake becomes permanent.
- **MEASURE THE BLAST RADIUS BEFORE PICKING THE FIX, NOT AFTER** (dc, on 0262): 369 attachment paths, zero refused by the new rule, so refuse was additive and the argument could be about correctness alone. **A REFUSAL THAT INVALIDATES EXISTING ROWS IS A MIGRATION WEARING A FIX'S CLOTHES**, and the ordering is what decides whose call it is: at zero it is the builder's, above zero it is mine or hv's. dc could not have known which without counting first.
- **AN ISSUE IS WRITTEN ABOUT THE INSTANCE SOMEBODY HAPPENED TO HIT; THE PROBE THAT FINDS THE REST COSTS ONE LOOP.** `0262` named one bad path shape and `st attach` accepted FIVE at rc=0 -- repo-relative, empty string, absolute, `../escape`, and unnormalised forms. Fixing what the row described would have left four open (dc).
- **A STATED RULE IS NOT A CHECK, AND THE DISTANCE CAN BE ONE SCREEN.** dc read `FacadeError::remedy`'s doc comment -- _no two variants share a remedy text_ -- WHILE ADDING THE ARM UNDERNEATH IT, and the arm violated it. Repair was to move the constraint from prose into the TYPE (a fault KIND, not a sentence). **Same shape as my own control this session: I wrote _every pen ruling is written HERE before it is relied on_ and sent two rulings before writing either.**
- **A CROSS-SESSION MESSAGE HAS NO DIFF AND NEITHER END HOLDS THE OTHER'S COPY.** I corrected cc on a figure I believed I had sent them; cc checked their received text and my message carried no figure at all. **THE DIRECTION THAT FIRED IS HARMLESS. THE ONE TO WATCH IS A NODE ACTING ON A FIGURE THE SENDER DOES NOT KNOW THEY SENT** -- these messages sit under none of the three guards, so nothing would surface it. Found by cc, not by me.
- **REPRODUCING A NUMBER CONFIRMS THE ARITHMETIC AND SAYS NOTHING ABOUT WHAT IT COUNTS** (ic, W94). I handed out _the gate reports 37 absent_ beside an argument about an uncounted debt, so 37 read as a debt. It is 36 to-write plus 1 fiat, ZERO green -- a to-write row citing a not-yet-existing file is what to-write MEANS, and `at lint` clean across the estate was the confirming evidence rather than a contradiction. **THE CORRECTED SENTENCE IS THE STRONGER ARGUMENT: the gated direction currently reports ZERO defects and the ungated one has never been counted.** It is the noun check with a second party -- and a peer confirming your arithmetic FEELS like independent evidence.

- **I READ SEVERITY AS AUTHORITY, TWICE IN ONE HOUR, AND WITHDREW BOTH.** `0266` (high) and `0258`'s sequencing were both going to hv until I read hv's standing directive back: **no external consumer, everything outstanding goes into 3.0.1, scarcity register retired as a class.** Both were already answered. **A FINDING BEING HIGH IS A STATEMENT ABOUT THE DEFECT AND SAYS NOTHING ABOUT WHO DISPOSITIONS IT** -- and "check with hv" as a default is the scarcity reflex wearing different clothes, which is exactly the register hv struck.
- **THE SAFE BEHAVIOUR AND THE HAZARD WERE THE SAME ACT** (dc's flag): my `git add` hit dc's `index.lock`, so I waited rather than clearing it -- correct -- and that left my paths STAGED in the shared index across an interval I did not control, where any node's bare `git commit` would have taken hv's reinstatement under their message. **NOBODY CATCHES THIS BY BEING MORE CAREFUL, BECAUSE THE CARE IS WHAT CREATES IT.** Cleared at `f0e9939a`; dc flagged rather than touched.
- **A CONVENTION THAT IS RIGHT EVERYWHERE ELSE, WRONG AT ONE ROW, WITH NOTHING AT THE ROW SAYING SO** (cc, on `0270`). `AT-00.2`'s note read _red-first against the canary tree_ -- correct discipline, and the exact action that refuses every commit in the repository. **This is not avoided by a careful operator being careful.**
- **ic's INVERSION IS A SHARPER W94 THAN THE ORIGINAL: THE NOUN WAS RIGHT AND THE RULE'S DIRECTION WAS NOT.** `0269`'s 36 became 13 because L3 constrains what a CITED file must contain and ic read it backwards as a test of what an UNCITED file means. **TAKING THE DISCRIMINATOR FROM THE ESTATE'S OWN MECHANISM IS WHAT MADE THEM CONFIDENT, NOT WHAT SAVED THEM.** The replacement finding is worth more than any count: `AT-17.6` is to-write with NO file while `edit_and_browse_reach_one_model.rs` opens `//! AT-17.6` declaring itself the witness -- **a row with no citation is the simultaneous blind spot of every row-first instrument**, invisible to all three for three different reasons.

- **A CLAIM'S TRUTH AND A CLAIM'S REACH ARE SEPARATE PROPERTIES, AND A CORRECT SENTENCE BORROWED FOR A DIFFERENT QUESTION CARRIES NEITHER.** Two halves, and cc's is the one I did not have. **MINE: a ruling that is RIGHT is never re-examined, so its PRICE rides along unchecked** -- twice in one hour I ruled correctly with a wrong cost (`0271` "additive" over a population excluding the test fixtures; the residue form "already implemented" over a subsystem I had not opened). **cc's: they handed me the precedent as a COST argument when it was a CORRECTNESS argument -- it proved the emitter can name paths and never proved the whiteboard was reachable, and I priced off a sentence that was not about price.**
- **A STALE READ PRODUCES A CLAIM SOMEONE CAN REFUTE; A STALE ASSIGNMENT PRODUCES WORK, WHICH NOTHING REFUTES** (cc). I ruled `0270` jointly to cc and dc and said _coordinate rather than both building it_ -- **the instruction that sends two nodes at one fix.** dc had already built and closed it at `40f6646d`. **THE COST LANDED ON THE RIGHT SIDE BY LUCK: had dc not already built it, both would have started, each assuming the other was waiting.** My rule covered READING a peer's state off this board; ASSIGNING off it is the same defect wearing the other hat, and worse, because both copies would pass review.
- **THE ZSH UNQUOTED-GLOB ABORT FIRED TWICE MORE TODAY WITH THE TRAP WRITTEN ON THIS BOARD IN THE WORDS THAT DESCRIBE IT.** `--include=*.rs` ran NOTHING at exit 0 and printed a plausible empty answer. **A DOCUMENTED TRAP IS NOT A PREVENTED ONE; ONLY QUOTING IT IS.** cc carries the identical entry and it does not stop them either -- **the class is _the thing that would have caught it was PRESENT AND INERT_**, which is `0241`'s shape and `L2`'s, and cc is merging them.
- **THE OBSERVATION AND THE STATE HAVE DIFFERENT LIFETIMES** -- the shared index rotated FOUR times today (my paths, then dc's, then cc's, then `0258`), and **every node reported it accurately and was overtaken before the report arrived.** Nobody was careless. **A report that is true when taken and false when read is not a reporting failure; it is a measurement whose subject cannot hold still**, and the cure is a mechanism rather than more care.
- **A FOLD IS A RE-READ, NOT A DELETION PASS, AND THE SENTENCES MOST WORTH RE-READING ARE THE ONES THAT READ AS SETTLED** (cc). My fleet-corpus entry ended _WP-14 IS THEREFORE IN THE 3.0.1 CUT_ and cc's drive had falsified it; a fold that only CUT would have carried it forward looking decided. **AND ic's COMPLEMENT: a fold keyed on the REGION rather than the ENTRY nearly buried `0222`, a live hold sitting in a TODO block** -- the rule is EXECUTION, not age, and not location.

- **A SCHEMA PROBE WITH THE WRONG KEY NAMES IS INDISTINGUISHABLE FROM A MALFORMED ROW.** I read `0273` with `jq -r '.id, .state'`, got `null, null`, and briefly had a well-formed canon row down as broken. **`jq` answers `null` for an absent key at exit 0** -- it never says _no such key_. Same family as the blind grep already below, and the new member is the nastiest of them, because the probe is ABOUT the schema: the failure mode and the finding look identical. **The cure is the same one: take the keys a second way (`jq 'keys'`) before believing a shape claim.**
- **`commit_session_id` IS A ONE-WAY TEST AND ALL THREE PEER BOARDS CARRY IT AS THOUGH IT IDENTIFIED THEM.** dc recorded that it ROTATES mid-session with no compact and no restart. **The consequence dc did not state is the load-bearing half: a NON-match therefore proves nothing.** It can confirm an identity and it can never refute one, so `0273`'s trailer matching none of cc/dc/ic is not evidence that no peer filed it. **Every issue's `reporter` is the git identity, so this trailer is the only node attribution there is -- and it only works in the direction nobody needs.**

### Promoted out of DOING 2026-09-05 22:18Z -- cc's rule: a finding parked in the transient section has an expiry nobody set

- **THE SAME DEFECT APPEARED ON SIX SURFACES IN ONE EVENING AND THE SIXTH WAS ITS OWN FINDER'S INSTRUMENT.** `at lint` counting rows it never examined; cc's 16 fixtures that could not exhibit the condition; `support_tree_drift` green through all of 3.0.0 while the DECLARATION it verified against was wrong; dc's nine controls that each drove a FUNCTION so none could see the RENDER; `cutover_guard.bash` blind to `bin/.devbin`; and cc's own `0275` control returning **66 rules** from a supposedly rules-free root because the binary ignores `INTENT_HOME` and walks up from its own path. **THE ONE THAT SHIPPED IS THE `support_tree_drift` ONE -- it put a keg with no rule library on the fleet. The others cost arguments.** GENERAL CURE, recorded where the estate found it: **read what the binary RESOLVES, not what the list SAYS.**
- **`cutover_guard.bash:66` DIES IF ITS POPULATION IS EMPTY AND HAS NO GUARD AGAINST IT BEING WRONG.** It is not a careless instrument -- it is a careful one whose care points at the wrong axis, so **a non-empty WRONG population sails through the guard and renders as coverage.** The sharpest form of the class: the other five had no such guard at all.
- **TWO CORRECT LOCAL DECISIONS CAN COMPOSE INTO A FALSE GLOBAL ANSWER, AND NO REVIEWER OF EITHER IS ABLE TO SEE IT** (cc, on `0275`). `Library::files` returning empty for an absent root is CORRECT where it sits; that correctness one layer down is exactly what made empty and absent indistinguishable one layer up. **A different failure mode from the other five, and the one code review is structurally worst at.**
- **A CRITERION STATES THE DOCTRINE AND DERIVES THE COUNT, OR THE COUNT BECOMES A CEILING NOBODY VOTED FOR.** Three instances in one day: `AC-00.6` said four call sites and the real number was ONE; `0273` named one route where there are three; `AC-11.7` says two primary fields and one has split. **Every one was an honest measurement of what existed when it was written.**
- **WHEN A REPORT CARRIES N FINDINGS AND I VERIFY ONE, THE ENDORSEMENT COVERS ONE -- NAME WHICH, OR VERIFY ALL N.** I endorsed a fix to a CORRECT sentence this way. **ic's diagnosis beats mine: the selector was not cost (the unchecked one was the CHEAPEST to falsify) -- it was WHICH INSTRUMENT I ALREADY HAD POINTED.** A ruling inherits the verification budget of whatever the ruler happened to be holding.
- **STAGING AFTER A WAIT IS A FRESH COMMAND AGAINST A TREE THAT MOVED.** I verified a diff, waited ~2 minutes on a peer `index.lock`, and staged a file that had gained dc's edit. **The lock discipline that protects the COMMIT is what OPENS the window before it.** CURE, now standard here: **put the `git diff --numstat` and the `git add` in the SAME command**, so a lock failure retries the verification too.
- **A GENERATED FILE HAS A SECOND WRITER THAT IS NOT A PERSON**, so the gap between verify and stage is a WRITER'S TURN, not idle time. Two cross-commit sweeps in one evening, pointing opposite ways: cc's `e1a076d5b` took dc's in-flight `render.rs`; my `4db7b0e7` took dc's view edit and landed a transcription AHEAD of its own canon.
- **NORMALISING A REGISTER SO A CHECKER CAN KEY ON IT SWAPS THE REQUIREMENT** (laksa-vc, prospectively, before any checker existed). Keying on three bold headings moves _the entry carries decision, constraint and consequence_ to _the entry carries three headings_ -- **which THREE EMPTY HEADINGS satisfy, with nothing reporting the swap.** And their stronger form of my own point: **the odd member out is the only member that can tell you whether you built a checker or a rubber stamp** -- a checker that passes all eight has proven nothing until it has been seen to FAIL.
- **A SUBJECT WRITTEN DOWN BEATS A DISCIPLINE, AND IT IS NOT CLOSE** (ic's, and it is the evening's conclusion). Both saves tonight were artefacts that named what they were about: `pipefail_sigpipe_check.sh:62` killed a false hypothesis in minutes, and a page's `On v3.0.0` survived TWO nodes walking past it. **Against that, three disciplines that depend on remembering -- my endorsement rule, cc's B5, ic's W99 -- each failed at least once in the same evening.**
- **ADJACENT STATEMENTS THAT DISAGREE ARE INVISIBLE TO A READ, AND EDITING ONE OF AN ADJACENT PAIR IS THE OPERATION THAT CREATES THEM** (ic, third instance and the first as the FIXER). The reader's eye supplies a consistency the text does not have. **And the FIXER is more exposed than the AUTHOR** -- the author wrote both paragraphs in one sitting, the fixer holds one -- which inverts the intuition that a small careful edit is the safe move.
- **AN UNCOMMITTED CANON ROW STILL HAS AN mtime, SO A NODE THAT KNOWS WHEN IT STOPPED CAN EXCLUDE ITSELF** (ic). Weak for attribution, decent for exclusion, which is the direction usually needed since the register carries no node identity. **MY CAVEAT: mtime is the LAST WRITE, never the filing** -- `0273` reads 22:00Z and was committed hours earlier -- **so it excludes only a node that has written nothing since, and it can never date a row.**
- **`find -newermt` PARSES LOCAL TIME.** Feeding it UTC stamps shifts the window by the offset and returns a plausible EMPTY set at exit 0. Same family as appending `Z` to a local reading. **Caught only by re-running without `2>/dev/null`.**
- **A BACKTICK INSIDE A DOUBLE-QUOTED `git commit -m` IS COMMAND SUBSTITUTION; THE COMMIT SUCCEEDS AND THE MESSAGE IS SILENTLY SHORT.** Worst-behaved of the zsh family -- the unquoted glob and the `perl -e` apostrophe run NOTHING, this one runs and reports success. **The cure was already policy: the message goes in a FILE.**
