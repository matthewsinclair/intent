# The canonical Intent v3 config/setup -- TARGET STATE

**Owner: vc. Definitive as of 2026-08-26 14:05Z. This is the target state every project is configured to. hv: _"No correspondence will be entered into."_ hv was AFK ~11:00Z-11:50Z and is present since; vc holds the pen with authority over intent-{cc,dc,ic} and devbin-{vc,cc} and, since ~11:58Z, hv's first-hand push grant for `Intent` and `homebrew-intent`. Rulings made under that delegation are marked as such and hv can overrule any of them; hv's own first-hand rulings are on hv's board with their menus.**

## Scope

**TWENTY-ONE PROJECTS. COUNT FROM THE LIST, NEVER FROM THE NUMBER.** This runbook has been wrong about the count twice today: it said FIFTEEN while listing sixteen names (dc subtracted Riffle, which has nothing wired and is therefore the project most likely to be skipped, because it is the one with nothing there to notice); then it said SIXTEEN from a depth-limited walk of `~/Devel/prj/*/` that could not see three projects nested one level deeper. **The census predicate that finds them all, from devbin-cc, is `find ~/Devel/prj -maxdepth 6 -type f -path '*/intent/.config/config.json' | sed -E '/\.backup\/|\/deps\/|\.worktrees\/|_Archive\//d'` -- the filter is load-bearing, because `arca_cli` and `arca_config` are also checked out as Elixir deps inside five other projects and an unfiltered sweep counts them six times each.** It returns 22; minus `Intentv2` that is the 21 below. Every one has its own git toplevel, its own `.git/hooks/pre-commit` and its own `.claude/settings.json` -- driven, not assumed.

`A3/a3-content` (2.10.0), `Anvil` (2.13.0), `Arca/arca_cli` (2.18.0), `Arca/arca_config` (2.18.0), `Arca/arca_notionex` (2.10.0), `Baize` (2.19.0), `Cdsync` (2.18.0), `Conflab` (2.19.0), `Courses` (2.14.0), `Courses/002 Agentic Coding` (2.10.0 -- its own repo, nested inside Courses), `Devbin` (2.18.0), `Intent` (3.0.0-dev), `Laksa` (2.19.0), `Lamplight` (2.19.0), `MicroGPTEx` (2.13.0), `Molt` (2.13.0), `Molt-flynn` (2.11.5), `Molt-matts` (2.11.5), `Prolix` (2.13.0), `Riffle` (2.18.0), `Utilz` (2.18.0).

**Versions were driven from each `config.json` at 10:55Z. Four are AT the 2.19.0 floor and take one hop; sixteen are BELOW it and take two; Intent is already v3.**

**TWO EXCEPTIONS, DIFFERENT KINDS:**

- **`~/Devel/prj/Intentv2` -- NOT TOUCHED AT ANY STAGE. NOT A PROJECT.** It is the TOOL TREE other projects run. It carries a project config, so **every census finds it and answers truthfully** -- and `int local status` warns about it unprompted: _"THAT TREE IS THE TOOL OTHER PROJECTS RUN. NEVER MIGRATE IT... Migrating it takes the CLI away from everything else."_ **The reason is recorded with the exclusion because a bare "skip it" gets re-added by the next person who regenerates the list from a census: the census is right, and the list is what carries the knowledge.** (The directory is `Intentv2`, with the `v`.) **USING IT AS THE TOOL -- `~/Devel/prj/Intentv2/bin/intent upgrade` inside another project -- writes nothing into it and is REQUIRED for hop 1 on the sixteen below-floor projects.** The exclusion is about the project, not the binary.
- **`~/Devel/prj/Intent`** -- in scope, and additionally gets `use` to swap the brew-installed v3 against its own dev build. See APPLY step 5 for what `use` can and cannot be.

**Do not disposition what a project has today. Overwrite it.** Variation is the disease, not the data.

## TARGET STATE -- what every project ends up with

### 1. `.claude/settings.json` -- verbatim from `lib/templates/.claude/settings.json`

Three lifecycle hooks, **all dispatching the CLI door**, no absolute paths, no `${CLAUDE_PROJECT_DIR}` script indirection, no inline `echo`:

| event              | matcher                           | command                                 | timeout |
| ------------------ | --------------------------------- | --------------------------------------- | ------- |
| `SessionStart`     | `startup\|resume\|clear\|compact` | `intent claude hook session-context`    | 3000    |
| `UserPromptSubmit` | `""`                              | `intent claude hook require-in-session` | 2000    |
| `Stop`             | `""`                              | `intent claude hook session-finish`     | 3000    |

**`post-tool-advisory` is the fourth name the dispatcher knows (`install.rs:141`) and is deliberately wired NOWHERE.** Shipped, dispatchable, uninvoked. That is the canonical state, not an omission. **The door is bare `intent`, resolved on PATH -- so until the brew flip (APPLY step 4) every hook in every project runs the frozen v2, which answers `claude hook` rc=0 in a v3 tree (driven) and injects v2-shaped context. Not a lockout; stale until the flip.**

### 2. Root canon -- from `lib/templates/llm/_*`

`AGENTS.md` (generated -- `intent agents sync`), `CLAUDE.md`, `usage-rules.md`. **Never hand-edit `AGENTS.md`.** **`CLAUDE.md` is regenerated ONLY with its own consent** -- the tool holds a copy that lacks the `lib/templates/llm/_CLAUDE.md` footer and names `--force` as the way through. See APPLY step 3 for the rule on when to force.

### 3. `intent/.config/config.json`

**Required:** `intent_version` (`3.0.0-dev` today; re-stamps through the ordinary `intent upgrade` when hv tags), `project_name`, `author`, `languages` (explicit array -- filesystem detection is retired). **NOT required: `intent_dir`** -- v3 defaults it (`project.rs` `default_intent_dir`) and `stamp_version` does not write it, so an absent key reading as `intent` IS the canonical configuration; requiring it demanded a key the tool cannot produce. **Minted, not authored:** `project_id`, a UUID written once by the migration. **Preserved if present:** `plugins`, `st_prefix`, `todo`. **Where `project_name` or `author` is MISSING (Laksa is), the tool will not repair it -- `config set` is unwired -- so hand-fix BEFORE hop 2 and name it HAND-FINISHED.**

### 4. `.git/hooks/pre-commit`

Chained to `pre-commit.intent` via chain-block markers, **EXACTLY ONCE**. Region-edited, never regenerated -- consumers carry hand-authored wiring below the block, and some wire guards canon has never heard of. **Two marker spellings exist and both are canonical once landed: v2 wrote `# intent-chain-block:start` / `:end`; `825c48db` wrote `# >>> intent-chain-block >>>` / `<<<`. The fix cc is landing emits the colon form and detects both, anchored on the marker LINE, because Laksa's hook has a prose line mentioning `intent-chain-block`.**

### 5. Also from canon

`.intent_critic.yml` and `usage-rules.md` (seeded when absent, never overwritten, not force-overwritable), `_treeindexignore`, and the `intent/` tree. **v3's `upgrade` adds `intent/events.jsonl`, `intent/st/steel_threads.md`, `intent/todo.md` and gitignores `intent/.cache/` and `intent/.treeindex/`.**

## THE ENDGAME, AND THE WORD FOR IT

**"Landed" means files committed and the verifier at 0 failed. It is NOT success: a landed project's bare `intent` is the frozen v2 until the flip and refuses state commands at rc=2** (hv found this with `intent --version` in Baize and stopped the fleet). **The vocabulary is hops, named landings, the flip; ONLY THE FLIP EARNS "DONE"** (devbin-cc). The flip is `brew link` of a keg installed FROM THE TAP over the network, which needs a REAL release: tag, GitHub release with the notarised artefacts, formula pushed to `homebrew-intent`. **hv authorised those pushes first-hand on 2026-08-26 (verbatim in `hv/inbox.vc.md`, 11:58Z).** The critical path is: ic's ingest fix committed (full sweep to completion, done at `2aa82d17`; cc's bracket-aware `field()` at `56364200`) -> **hv fires `int build release v3.0.0` in hv's own terminal** (the verb bumps three version homes, commits, tags and pushes both remotes; its confirm reads from `/dev/tty` and this estate never passes `--no-confirm` from a tool session) -> dc builds once on the tag -> fleet migrates on that pair while dc runs prepare -> formula -> publish -> `brew install matthewsinclair/intent/intent` from the tap -> `brew link` on vc's word -> every project re-verified with v3 actually on PATH. No step is skipped and no step is reported as more than it is.

## APPLY

1. **THE BINARY IS `intent3` -- THE CURRENCY-GUARDED WRAPPER AT `Intent/bin/intent3` -- NEVER THE RAW BINARY AND NEVER BARE `intent`.** Bare `intent` is the frozen v2 until the flip; its refusal inside a v3 tree is not a broken project. The wrapper refuses with _"this binary is behind HEAD -- remedy: run 'int local build'"_ whenever a non-test file under `native/rust` has changed since the pair was built (`currency.lib`; test files do not count). **That refusal is the mechanism that stops a stale binary migrating a project: when it fires, STOP and wait for dc's rebuild; do not fall through to `target/release/intent`.** A `dirty-` binary may read canon and never write it. **The pair's commit stamp -- `int local status`, never read off this page -- goes into every migration commit body.**
2. **REWRITE EVERY PROJECT ON THE LIST TO THE TARGET STATE ABOVE -- BY THE TOOL, NOT BY HAND.** vc's ruling under hv's grant, and hv confirmed it: _"by the tool -- Yes, that is better. But if things fail, then fix forward (manually if necessary)."_ hand-editing delivers _"the same configuration"_ ONCE and nothing holds it tomorrow; a tool makes it a property rather than a snapshot, and a tool-written state can be RE-ASSERTED AND COMPARED. **The tool is `intent claude upgrade --apply` (`825c48db` + cc's chain-block fix).** **IF THE TOOL FAILS ON A PROJECT, FIX FORWARD -- MANUALLY IF NECESSARY (hv).** What is NOT permitted is a project left in a state nobody recorded: **if you hand-finish anything, write HAND-FINISHED and what in the commit body**, because the next reader assumes the tool did it. **ONE NON-NEGOTIABLE PROPERTY: IT MUST PRESERVE WIRING IT HAS NEVER HEARD OF** -- Lamplight chains guards that are not in Intent canon; cc's six arms assert every original hook line survives in order. **ONE NAMED COMMIT PER PROJECT, so the rollback is `git revert` on a single sha.** Do not spread a project across commits and do not batch projects into one. **On a project with a live fleet (Lamplight has ` M mix.lock`), commit `--only` your own paths and check `git show --stat HEAD` against the length of your pathspec** -- what the command DID against what it was TOLD, which is the gap two estates fell through today.
3. **THE RECIPE, driven end to end on sandbox copies of Molt-flynn (2.11.5) and Devbin (2.18.0) before any live write:**

   ```
   cd <proj>
   ~/Devel/prj/Intentv2/bin/intent upgrade < /dev/null   # hop 1, BELOW-FLOOR ONLY. THE REDIRECT IS LOAD-BEARING: 0071 hangs without it.
   intent3 upgrade                                        # hop 2. Stamps 3.0.0-dev, writes 3, converges .gitignore. ATOMIC: refuses and writes nothing on a content defect.
   intent3 claude upgrade --apply                         # hop 3. Writes the canon; holds CLAUDE.md without the marker.
   intent3 claude upgrade --apply                         # again: MUST report 0 written. NECESSARY, NOT SUFFICIENT -- see below.
   bash ~/Devel/prj/Intent/intent/whiteboard/vc/verify-canonical.sh <proj>   # MUST be 0 failed before the commit
   git add -A && git commit -m "intent: migrate to v3 canonical (built from <pair sha>)"
   ```

   **`.backup/` MUST BE IGNORED BEFORE `git add -A`** (ic). Hop 1 writes `.backup/backup-<stamp>/` and nothing in the two-hop ignores it; on Riffle `-A` would commit 86 files of pre-migration state into permanent history and the one-commit rollback would revert 86 files instead of a dozen. The standing rule is `.backup/` ignored everywhere. Driven across all 21 with a child-path `check-ignore`: **Courses, Devbin and Riffle are NOT ignored**; the other eighteen carry `.backup/backup-*` or equivalent. On those three, add `.backup/` to `.gitignore` before the commit and write HAND-FINISHED: .backup/ gitignored in the body. The verifier now refuses a project where it is not ignored, so a 0-failed verifier run is the assertion.

   **HOP 1 CREATES THE HOOK.** v2 2.19.0's upgrade rewrites `.claude/settings.json` to the three doors AND installs a colon-form chain block where none existed (ic, driven on Riffle). **So there is no hook-less subset that is safe ahead of cc's fix -- a survey of hooks taken before hop 1 measures a state the recipe destroys.** Check hook state AFTER hop 1, never before.

   **THE SECOND `--apply` DOES NOT CATCH A DOUBLED HOOK.** `825c48db` doubled the chain block exactly once and then reported `0 written` on the doubled state forever -- a converger at the wrong fixed point, which the idempotence control blesses. **Only the verifier's marker COUNT sees it.** Three instruments -- `0 written`, hop 3's `written:` line, a `grep -q` -- all read the doubled hook as correct because none of them counted (ic): agreement is not corroboration when every instrument asks the same question.

   **`CLAUDE.md`: `diff` BEFORE `--force` -- hv RULED IT FIRST-HAND (to dc): allow the overwrite, carry project content by hand.** Hop 3 holds any `CLAUDE.md` without the generated footer. On Molt-flynn the 82-line "hand-authored" file was the v2.11 boilerplate template without a footer -- `--force` removed 60 lines of old template and lost nothing. **Where the removed lines are NOT Intent template text, that is project content: corpus-test it against the other projects' `CLAUDE.md` (dc), carry it into the new file's user block under `## Project-specific` by hand, headings demoted one level, and write HAND-FINISHED plus what in the body.** So far the authored content found in that family is a commit-signature rule (Prolix, Anvil) and, on Laksa, 123 lines of real instructions -- spliced from the saved pre-migration file with a line-by-line presence check, never retyped. **THE USER BLOCK IS NOW CARRIED BY THE TOOL: cc's splice (`8ba6c026`) keeps whatever is between `<!-- user:start -->` / `<!-- user:end -->` byte-for-byte across `--apply` and `--force`; before it, the tool SUBSTITUTED the template's default block -- well-formed, author line, no gap -- on every generated `CLAUDE.md` with no `--force` needed (Baize lost 20 bytes of provenance on the plain path). Hand-carry is therefore scoped to OUTSIDE-the-markers content only (dc): carrying the block too leaves a project two copies of its own directive.** The verifier's `root canon` arm demands the generated footer, so a held file fails it by design -- that FAIL names the pending `--force`, not a defect.

   **HOP 2 CAN REFUSE ON CONTENT.** Devbin fails atomically -- `sqlite: UNIQUE constraint failed: tests.thread_id, tests.id` -- on a duplicate AT id inside one thread. No version census predicts it, **and no grep does either: an AT-uniqueness sweep across the fleet produced two confirmed false positives (Intent's ST0056, already in the store at 139/139; Riffle, where the "duplicate" was a prose citation) because cross-references read as declarations.** **HOP 2 IS THE ORACLE** (ic): it refuses atomically, writes nothing, and NAMES the thread and id. On a refusal, read that one thread by eye -- every pair, two DECLARATIONS each, citations excluded -- fix every real pair in one named commit, hop 2 again. **Nobody edits a ratified contract off a prediction.**

   **HOP 2 SILENTLY INVERTED AC SATISFACTION UNTIL ic's INGEST FIX (landing as a source commit; the pair built on it is the one exposed projects wait for).** `legacy.rs` matched `satisfied: yes` to end-of-field, so `satisfied: yes (hv signed off 2026-06-22)` fell to the catch-all and became UNSATISFIED, exit 0, note dropped -- Courses' completed ST0002 came out 8 of 10 unsatisfied. **Exposure is measured, not assumed:** `intent/whiteboard/vc/exposure.sh <proj> <ref>` counts hand-written parentheticals in the pre-migration `acceptance.md` files (a bash FILE, because an inline zsh loop over a newline list runs once and prints zeros -- it did, for every project, including the one with 8). **Lamplight 28, arca_cli 9, arca_config 3, Devbin 1, Courses 8 (reverted at `aa25be1`, re-migrates on the fixed pair); every other project 0, so its canon is right and it proceeds on the current pair.** The fix carries the note into the row's `evidence:` and REFUSES an unrecognised verdict.

   **TWO HOMES PER BUCKETED THREAD, LEFT AS-IS. DO NOT RUN `collapse-buckets.sh`.** v3 writes flat `intent/st/<ID>/` views and never relocates a thread out of v2's `COMPLETED/ CANCELLED/ NOT-STARTED/` buckets (`migrate.rs:47`, a documented hole). The old home carries authored prose canon does not (design/impl/tasks, and `acceptance.md`'s preamble with hv's ratification amendments). My collapse script's delete arm treated any old file with a flat counterpart as a superseded view and deleted that preamble -- ic caught it after it had run on three projects. **The migration commit carries the additive two-homes state; the body says so.** The collapse returns only under ic's rule -- delete only a line-subset of the counterpart, otherwise keep as `<name>.v2.<ext>` -- and only after a second node has read its dry run.

   **The commit goes THROUGH the project's own pre-commit gate with v2 still on PATH** (driven: rc=0, `guards: 1 ran, 3 skipped`). Committing is not blocked on brew. But with v2 on PATH the critic in a v3-stamped tree is not certainly running, so **every project is re-verified after the flip.**

4. **BREW.** `int macos prepare` -> `formula` -> **`publish` (hv's ALONE).** The generated formula's URLs are GitHub release assets and no v3 release exists, so a plain `brew install` 404s until hv publishes. **dc proves everything but the network hop by preseeding brew's download cache with the staged artefacts under the byte-identical formula** (cache-name rule positively controlled against `brew --cache --formula`). Cellar layout: everything under `libexec`, `bin` gets symlinks, `lib/templates/` beside the binary -- a keg that installs only the binary runs, answers `--version`, and cannot render canon (`cmd/macos:81`). **ACCEPTANCE, all driven on the brew-resolved binary:** (a) from a non-checkout dir, bare `intent claude upgrade` NAMES the five artefacts; (b) `intent info | sed -n '/^ *INTENT_HOME:/ { s/^ *INTENT_HOME: *//; s/ *$//; p; }'` resolves to a dir carrying `lib/templates/hooks/pre-commit-guards.sh` -- **eleven devbin-vendored estates resolve their guard runner that way and FAIL OPEN if it is absent**; (c) positive control: INTENT_HOME pointed at a dir with no `lib/templates/` yields `NO guard ran for this commit -- this install has no guard runner.` **Version string stays `3.0.0-dev` (vc ruling under delegation): it is TRUE of the binary until hv tags, and a bump is release engineering.** **THE FLIP -- `brew link`, `/opt/homebrew/bin/intent` at PATH position 1 -- is SEQUENCED, not automatic:** it moves every hook door and every gate on this machine to v3 in one instant. After (a)-(c), after the verifier has been re-run on a canary with the brew binary, on vc's word.
5. **`use dev|prod` -- `int local use`, shipped `39111261`, AND IT IS MACHINE-WIDE BECAUSE PATH IS.** Driven chain (dc): `use dev` = brew unlink + pin + repoint `~/.local/bin/intent` (PATH 17, the binding that wins today) at the dev release binary, then MEASURE what resolves and die if it is not the flavour asked for; `use prod` = unpin + link, PATH 1 wins, 17 harmlessly shadowed. Both drive `verify_hook_door` with stdin closed afterwards, because a copied binary with no `lib/templates` above it refuses every hook at exit 1 and Claude Code does not block on 1. **THE CATCH FOR hv: there is no project-scoped swap through PATH.** `use dev` puts the WHOLE FLEET on this checkout's build, not just Intent; `use prod` reverses it once a formula exists. **The project-scoped dev spelling that already exists is `intent3`.** If hv's model is _"Intent gets dev, the rest get brew"_, that is `intent3` inside Intent and `use prod` for the machine -- recorded on hv's board as a question with this as the interim ruling.

## VERIFY -- per project, after

- `bash intent/whiteboard/vc/verify-canonical.sh <proj>` -- and `--self-test` first if you have any doubt about the instrument. It prints which canon it compared against.
- The second `--apply` wrote 0. Necessary, not sufficient.
- `git show --stat HEAD` against your own pathspec.
- After the flip: `intent --version` and `intent info` answer from the brew binary; the three hooks answer **rc=0**; an unknown hook answers **rc=1**; **never rc=2 on this surface**; `intent ac gate <ST>` runs.
- **Use WHOLE-FILE hashes for any before/after comparison.**

## Settled -- do not re-raise

- **`Intent/bin/intent`** is v2's bash wrapper, PATH position 22, shadowed, never resolves. `AC-12.1` prunes `bin/` at the cut.
- **The stale `INTENT_HOME` at `~/.zshrc:37`** cannot hold a v3 tool on v2 guards -- v3 never reads it (`install.rs:20`). ONE switch, not two.
- **The hook fail-closed lockout does not fire.** Driven four ways at four v2 versions: all 0, unknown 1. And in a project rewritten to canon but not yet migrated, the v3 binary answers all four hook names rc=0 while `st list` refuses at rc=1 in the same fixture (ic) -- the stamp is genuinely read and `claude hook` is genuinely exempt.
- **The one-way door is dissolved.**
- **`~/bin/intent` at PATH position 19** is shadowed today and becomes the silent v2 fallback the moment 17 moves. `use` measures what resolves rather than assuming, which catches 19 without naming it.
- **The v2 upgrade hang (`0071`) is worked around, not fixed:** stdin closed. It is a shipped-surface fix in both trees and is NOT being built today -- _"we're not fixing 2 unless it's broken and stopping you working"_ (hv), and the redirect means it is not stopping anyone.

## LEARNED AT AND AFTER THE TAG (2026-08-26, appended in order; each with the node that found it)

- **THE TAG IS `v3.0.0` -> `80d8b2ca`, PUBLISHED with three notarised assets and the formula in the tap.** The verb aborted twice and could not resume (`:433` refuses target == current once `VERSION` is stamped); the tag and pushes were by hand under hv's grant after the trio (fmt, clippy, `cargo test --workspace`) ran on the exact commit and CI came back green on both runners. `main` first, tag after green: a tag is the one immutable ref, and no workflow fires on a tag (dc).
- **FOUR `int build release` DEFECTS (dc's lane, post-flip):** the sidecar list omits what the canon step writes (`.gitignore`); preflight runs `cargo test` but not the client-side push gate's `fmt` + clippy; the cut never tests the tree it tags -- the test gate belongs after the sidecar sync (dc); a `--skip-tests` early return also skips the cargo gate its warning does not name (ic). Composed: the `main`-push CI is the only test the tagged tree gets.
- **A FREEZE NAMES THE STATE A GATE READS, NEVER THE ACTION A HUMAN PICTURES** (ic). "No commits" forbade the thing that removes dirt and permitted the write that creates it; `:734` reads dirt. Freeze = no writes of any kind inside the repo; park outside; **a node holding through a cut cannot signal it is alive** (cc) -- heartbeat is a write.
- **`shipped_surface_drift.bats` runs BEFORE a single-tree skill edit, not at the tag** (cc, `b60f9ebb`); the hatch is `_is_v3_only_by_ruling` with the ruling cited.
- **`intent/events.jsonl` IS PER-MACHINE** (Intent's own `.gitignore:149`); the migration creates it and does not ignore it; every landing before 13:40Z committed it and was repaired. Fold `intent/events.jsonl` into `.gitignore` in the hop commit; `converge_gitignore` learning it is a batch item. `/AGENTS.md.bak` is a fossil v3 is tested never to produce; Intent's pruning of the ignore line does NOT generalise (Molt-matts has no `*.bak` class rule -- ic).
- **THE RE-STAMP PROCEDURE:** a project migrated on a `-dev` pair carries `3.0.0-dev`; `intent3 upgrade` on the release pair re-stamps unconditionally and is idempotent on everything git sees -- only the per-machine store moves (dc, measured on Anvil after a whole-tree hash gave a false "not idempotent"). It never touches `CLAUDE.md`. All seven early landings re-stamped.
- **A RE-RUN OF HOP 2 OVER COMMITTED CANON RE-EMITS, IT DOES NOT RE-CONVERT** (dc). To re-convert from the v2 source, delete the uncommitted `.canon/` and `.cache/` first.
- **A NON-TEST AC WITH `satisfied: yes` AND NO `evidence:` INGESTS AS `no` BY DESIGN** (`facade.rs:333`; devbin-vc, 79 of 79 on Devbin's ST0001). It wears the shape of ic's inversion bug and is the opposite; pre-check `grep '^- AC-[0-9.]* (non-test)' | grep -v 'evidence:'`; it is an authored sign-off to complete, never a parser fix.
- **v2's HOP 1 RE-PUNCTUATES SOME ratified rows** (`status: green (x)` -> `green -- x`: Utilz 11, Prolix 1, Cdsync 0; selector unknown, in `Intentv2`). Revert `acceptance.md` to HEAD before committing; closed threads carry unparsed rows anyway (`Scan::record(closed, ...)`). Confirmed live on Utilz and Cdsync (cc).
- **`--force` ALSO REWRITES `.intent_critic.yml`** (cc), which the plain run preserves; a tuned critic config would be lost silently. Diff it too.
- **FOURTH INGEST DEFECT: the AT citation parser swallows a trailing parenthetical into `file`** (devbin-vc, 18/18 on arca_cli; 7 already in committed canon: Cdsync 1, Prolix 2, Riffle 4; plain paths clean). Fix in the batch (cc); arca_cli/arca_config re-converted from a discarded `.canon/`; the seven repaired by hand through the canon route. No refuse-on-absence: `to-write` rows cite files that do not exist yet.
- **v2's `languages` BACK-FILL IS NOT DETECTION** (devbin-vc): it reads `RULES-<lang>.md` presence or falls back to `["shell"]` on the presence of a hook Intent installed. Five hop-1 landings carry `["shell"]` as a default, not a declaration (A3, Courses/002, three Molts); `arca_notionex` got `elixir` added by hand, `shell` kept pending hv. Presence certified, value never examined -- the verifier arm reads the config field against the tree, never greps it.
- **THE USER BLOCK HAS TWO HALVES** (devbin-cc): the marker COMMENT is tool-owned and regenerated from config; the BODY is carried byte-for-byte (`8ba6c026`). Provenance hand-written into the comment (Baize `ab8172a`, Riffle `37c5dab`) will be dropped by the next regeneration -- put it in the body, or the tool emits it.
- **ONLY THE FLIP EARNS "DONE."** At the time of writing: 15 of 21 clean at `3.0.0` by state (`jq -r .intent_version`, never a log grep); Anvil and arca_cli mid-hop; arca_config next; Conflab HELD (61 `status:` rows need a store schema migration, done with time); Lamplight on the batch (cc's empty-subject fix). The flip is `brew link` on the published keg, on vc's word, announced as its own timed event.
- **THE TAP INSTALLED 644 BINARIES AND `brew install` PRINTED THE BEER EMOJI** (dc, on the first real network install). GitHub serves release assets with no exec bit; Homebrew's `.install` preserves the source mode; the formula set none. The bytes, hashes, signature and notarisation were all correct; **two verifiers executed the binary and both read the local pre-upload copy**, so neither could see what the network hop drops, and the cache-preseed proof was scoped "everything but the network hop" -- honestly, and that was what was behind it. The formula carried its own detector (`test do` -> `brew test intent`) and `brew install` never runs it. Fixed in the GENERATOR (`ded669c2`: emits `chmod 0755` for both binaries; `--revision N` knob) and regenerated into the tap as `revision 1` (`031c8a0`, no hand edit; every other byte identical). **New verb, registered before it is built: `int macos smoke`** -- cold install from the tap, `brew test intent`, `-x` read back, arms (a)(b)(c); refuses on any. Until it exists the sequence is by hand and it is not optional.
- **A MODE-644 BINARY ON PATH IS SKIPPED, NOT SHADOWING** (devbin/cc, controlled: same bytes at 755 shadow immediately). `brew link` succeeds, nothing changes, every estate keeps answering from v2 with every gate green -- indistinguishable from "not flipped yet" by `--version` alone. **The flip's verification is three reads in this order: `readlink -f "$(command -v intent)"`, `ls -l` on it, then `--version`; never the third alone.**
- **A TOOL READS THE FILE, A NODE READS GIT** (dc, third instance): an untracked stale `Formula/intent.rb` in the tap checkout would have been what `brew install` used, against git's empty-tap HEAD. Like `core.hooksPath`, like the store: check the artefact the tool reads.
- **hv's THREE CRITERIA, MEASURED (dc, 14:0xZ):** installed via brew from the tap over the network (`3.0.0_1`, executable, `brew test intent` rc=0); `int local use dev` unlinks + pins and resolves bare `intent` to the checkout build, `use prod` links and resolves it to the keg, `which -a` read back after each and the hook door opens on both; the fleet on v3 (sixteen at 3.0.0 with bare `intent st list` rc=0 in every one). `cmd_use`'s prod branch prints the wrong way back (dc fixing). `int macos smoke` asserts against the keg path, not `brew test`, so it can refuse before a link.
- **THE DEVBIN GATE BLOCKS WHEN THE RESOLVER CANNOT FIND AN INSTALL** (devbin/vc, `67c037e`): positive half `guards: 4 ran`, negative half rc=1 `NO guard ran for this commit`. The negative as first briefed ("point `INTENT_HOME` at a dir without templates") was INERT -- the hook parses `intent info`, never the variable -- and would have read as a passing control that changed nothing; the lever that moves it is a stub `intent` on PATH.
- **`INTENT_HOME=.../Intentv2` IN THE SHELL IS INERT FOR v3 AND LOAD-BEARING FOR HOP 1** (devbin/vc): `Intentv2/bin/intent` still runs every remaining v2 hop. Do not tidy it until the last below-floor project is through; it is Molt's to own.
- **`intent critic --languages` IS ADVERTISED BY `--help` AND DOES NOT WORK** (devbin/cc found it; ic scoped and is fixing it; and see the KEG SHIPS NO RULE LIBRARY bullet below -- the gates ARE off, for that reason): clap requires `<LANG>` beside the flag, so the flag spelling exits 1 while `intent critic shell --languages` works and ignores the positional. **THE ONE CONSUMER IS DEVBIN'S `check:118`, WHICH FAILS CLOSED** -- loud on ten estates, back on re-vendor after 3.0.1. **THE CANON PRE-COMMIT HOOK WAS NEVER AFFECTED, AND vc SAID IT WAS.** The shipped hook reads `languages` from `config.json` with jq (`pre-commit.sh:311`), dispatches the positional spelling (`:368`), and is loud when a language goes unenforced (`:426`, `:443`); `--languages` appears in zero files of the shipped install (positive control: `intent critic` in six). vc's "every migrated project's gate fails open since the flip" was a claim about the hook made from a symptom without reading the hook -- the habit devbin/cc had named in their own record an hour earlier -- and it reached hv's inbox and terminal before ic caught it. Corrected in both. Two things ic found by running the fix: clap PANICS (exit 101, a debug assertion) on `.required(true)` beside `required_unless_present`, so the fix is a `match` choosing one call, not an exception added to the existing one; and the arity is deliberately NOT relaxed to `0..1`, because that would also move bare `intent critic` from clap's 1 to the handler's 2 and settle, as a side effect of a change made for another reason, the exit-code divergence `exit_codes.rs` records as dc's to rule.

- **DEVBIN'S `gate_critic` PRINTS `ok` OVER `0 of 0 rules`** (devbin/cc): a disarmed gate wrote the same line as an armed one, where v2 named the four it could not arm. hv un-paused Devbin for this alone, relayed to devbin/cc: refuse, or never `ok` on an empty denominator; `check:118` stays failing closed until 3.0.1.
- **FIVE SESSIONS SHARE ONE `.git/index`, AND `git commit` COMMITS THE INDEX AS IT STANDS AT COMMIT TIME** (dc, caught in the act at `66c9493e`: one file staged, two committed; a `--cached` check immediately before printed one path, and the index changed inside the gap). `git commit --only <paths>` is the only safe write in this checkout and always was; `git show --stat HEAD` against your pathspec is the after-the-fact detector (cc). The repair carries the same hazard one layer down: a `reset --soft` that moves a shared HEAD orphans any peer commit landing in the window, with no symptom anywhere -- reflog BEFORE the reset; there is no detector, only sequence. The swept file was mine, re-committed `--only` as `0245d7d1`. And `--only` separates PATHS, not writers: two sessions owning attachments on the SAME thread are one `.canon/st/<ID>.json`, and a path-scoped commit sweeps the other's rows. ic raised the instance, then withdrew it -- a whole-file substring grep had read prose naming a file as an attachment `path` (every attachment path is relative to `intent/st/<ID>/`; the control that catches this is the INVERSE one, a filename appearing only in prose coming back not-an-attachment) -- and the class stands unretired. One more shape, ic's, found the hard way: after a commit that DIED on `.git/index.lock`, `git show --stat HEAD` in the same block printed a PEER's commit and read exactly like a successful verification -- in a shared checkout `HEAD` is not yours by default; the discriminator is the commit's own rc and its `[main <sha>]` line, never what `git show` says afterwards.

- **A DRY-RUN OF A LONG GATE IS REDUNDANT ONLY IF IT IS BEHIND THE RUN YOU KEEP** (dc): dc's was seven minutes ahead of hv's real cut and carried the `:734` answer before the real run reached it.
- **`--force` MEANS THREE DIFFERENT THINGS ON THREE FILES SPELLED THE SAME WAY** (devbin/vc): a template file by position, an authored file by content, `.intent_critic.yml` wholesale; the only instrument that separates them is a line-by-line diff against a before-state, which is why the recipe diffs first and carries authored lines into the user block by hand.
- **AN ATTACHMENT EDIT IS TWO WRITES** (cc, `e696de15`): the parity corpus is a REGISTERED attachment, so editing the bytes on disk leaves canon naming the old digest; `attachment_drift_detected` found exactly the edit's before/after digests and nothing else. The second write is `intent sync --to-store <ST>`, and canon (`intent/.canon/st/<ST>.json`) lands in the SAME commit as the bytes it names. `sync` warns in its own words that bytes no commit contains are being named -- it does not refuse; the commit gate does. Edit bytes -> sync -> one `--only` commit carrying both.
- **THE CANON ROUTE, DRIVEN ON THREE PROJECTS** (vc: Cdsync `41fd2fb`, Riffle `5982ff7`, Prolix `1298531`): the seven committed `file` pointers repaired -- `.canon` edited with jq, `intent sync --to-store <ST>`, `intent sync --to-disk`, one `--only` commit of canon + view; the parenthetical carried into `note`, which the view renders after the status. Three tool findings on the way, none blocking:
  - **FINDER DROPPINGS REFUSE THE SYNC.** Six `.DS_Store` files under Prolix's `intent/` (git-ignored, so invisible to `status`) made `sync --to-store` refuse to read canon at all -- `residue ... unknown-file-shape -- not valid UTF-8`, six findings -- on a project that migrated clean. By design (a modelled directory carries only what Intent writes) and the remedy is printed; anyone who opens `intent/` in Finder meets it. `find intent -name .DS_Store -delete`, then retry.
  - **TWO EMITTERS OF `.canon` DISAGREE ON `attachments`.** The migration's extract and `upgrade`'s re-emit record none; `sync --to-disk` records every attachment with full text, bytes and sha256, so Prolix's first sync rewrote 27 threads' canon it had no business in. Measured in committed canon: Cdsync 0/4, Riffle 0/5, Courses 0/2, Prolix none at its migration commit; Intent 58/63 (self-hosted, synced); Baize 4/25 and Laksa 1/110 are exactly the threads someone synced since. The store had them all along, MEASURED on a throwaway copy of Prolix (never hv's tree): `--to-store ST0001` from an extract with `attachments: []` on a thread with five attachment files, then `--to-disk`, brought all five back (control thread, never `--to-store`'d: 19). So the canon route is safe on any fleet thread and the one-emitter fix is an extract-writer fix, not a store fix. Same across cc's other landings: MicroGPTEx 0/3, Utilz 0/9.
    Reverted the 26 in Prolix; the fleet takes this churn ONCE when the tool has one emitter, not by hand per project. Filed for cc.
  - **AN EMPTY WP TITLE RENDERS `# WP-01: ` WITH A TRAILING SPACE** (nine `info.md` + `todo.md` in Prolix); the markdown formatter strips it at commit and the next `--to-disk` re-adds it -- a permanent churn between two writers. Reverted; filed for cc.
- **A CONTROL IS ONLY A CONTROL IF YOU CAN SAY WHAT WOULD MAKE IT FAIL** (devbin/cc and devbin/vc, four instances in one day, all in instruments rather than subjects): a bats harness that died on an unbound variable with two tests passing anyway; a `pgrep -f` liveness check matching its own command line; `bats ... | tail -6` reporting the tail's exit status over a plan it never showed; a Cellar negative that never reached v3's resolver. Each was a test removed from the causal path and still reporting. Devbin's zero-rules fix landed on a plan-matched TAP green (`455d3f0`, unpushed under the grant).
- **EVERY FLEET TREE IS FULLY REALISED, AND THE TOOL CANNOT YET SPARSIFY ONE** (hv, on Laksa: 110 of 110 threads under `intent/st/`). Measured across the fleet: no migrated project has an `intent/.intentfiles`; Intent's own tree has one and is 8 of 63 realised -- the eight are exactly its open threads (3 WIP, 3 Triage, 1 On Hold, 1 Not Started), so "realised = not Completed and not Cancelled" is hv's practice, not a guess. The contract is hv's own (`.intentfiles` header, 2026-08-19): ABSENT means nobody has said and everything stays; the migration writes no file (`migrate.rs:474-488` consults one and finds none), so the fleet is fully realised by contract, not by accident -- the defect is that hop 2 does not WRITE `.intentfiles` with the open set. And there is no path to sparsify today: `st dehydrate` is "a known command that is not implemented yet" (rc=2; ST0061 `dehydrate`, Triage, opened by hv 2026-08-26), and `intent organize --apply` REFUSES to remove anything -- "this estate has not proved it can put them back" -- because each dehydration precondition is an acceptance criterion of the project itself (`organize.rs:261`, `preconditions.rs`) and a fleet project declares none. The refusal is correct for a reason beyond its wording: the fleet's committed canon carries no attachment bytes (the two-emitter defect above), so a clone could not regenerate what a hand deletion removed. Probed on throwaway copies, never real trees: `organize` preview with the file absent says `0 to remove` and reports the v2 buckets as `unclaimed` (Riffle 41); a malformed line (`ST0004` for `STEELTHREAD:ST0004`) is refused with the remedy. ORDER, all tool work before any sweep: one emitter so canon carries attachments; migration and upgrade write `.intentfiles` = open threads; the dehydration preconditions move into the tool (can the committed record put every file back?) rather than into each project's ACs; `st dehydrate` lands; THEN one `organize --apply` sweep across the fleet (dc). Nothing by hand.
- **A GATE THAT DOES NOT READ THE STATE THE NEXT GATE READS** (cc found it, ic generalised it, five instances to dc as one unit): `cargo clippy` exits 0 on warnings, so the obvious `if ! cargo clippy; then` is inert on exactly what it was added for -- cc caught `items_after_test_module` by counting warning lines. The push gate runs `-D warnings`; `build.d/release` runs no clippy at all today, and four gates already wired there read a different state from the gate after them.
- **THE AT `file` FIELD HAS NO DEFINED ROOT** (cc): ST0056 cites 92 `native/`, 13 `intent/`, 4 `tests/`, 1 `lib/`; fleet-wide 204 unique values, 55 not repo-relative. Nothing resolves it today, so it is inert; the first gate that checks a cited test exists will be right for the majority and wrong in the direction that accuses an author of not writing a test they wrote. Define the root before anything joins on it.
- **THE KEG SHIPS NO RULE LIBRARY, SO EVERY CRITIC GATE ON IT PASSES OVER NOTHING** (devbin/vc measured; vc positive-controlled): `find libexec -type d -name rules` -> 0; `intent claude rules list --lang shell` on the keg -> `total: 0 rule(s)`, while the checkout holds ten packs under `intent/plugins/claude/rules`; `int macos prepare` stages `SUPPORT_PATHS="lib/templates"` and nothing else (`cmd/macos:116`), so the rules were never put in the bottle. On every project using the keg, `intent critic <lang> --staged` arms 0 of 0 rules, prints `ok`, exits 0, and the canon hook -- which is correct and branches on rc -- passes. **So the 14:38Z correction was right about the hook and the flag and wrong about the conclusion: the fleet's critic gates ARE effectively off since the flip, by packaging.** Devbin's `check all` is RED on the keg for exactly this (`455d3f0` reads the armed count), correctly. Two fixes, both 3.0.1: stage the rule library (dc, packaging); the runner and the hook refuse -- or at least say -- when 0 rules armed, with devbin's gate as the model (ic). A gate that passes over an empty denominator is the day's class for the fourth time.
- **THE KEG IS MISSING TWO TREES, NOT ONE, AND THE PACKAGING CLAIM WAS "VERIFIED" AGAINST THE WRONG POPULATION** (dc, enumerated from `install.join(...)` across the crates -- `rules.rs:149` for the rules): v3 resolves `bin/intent`, `lib/templates`, `lib/templates/hooks/pre-commit.sh` (all present), `intent/plugins/claude/rules` (752K, MISSING) and `intent/plugins/claude/skills` (372K, MISSING). The comment above `cmd/macos:116` says the list was "verified rather than assumed" -- and it was, over shipped SCRIPTS; the Rust binary reads the rules and skills and was never in the population. Measured consequence of the skills half (vc, throwaway copy): `intent claude skills list` on the keg says `no skills in this install`; `claude upgrade` on the keg neither installs nor removes skills (Intent's own hop 3 at `9b6ab634` touched no skill); the 25 `in-*` skills on this machine live in `~/.claude/skills` and are untouched -- so a fresh brew install cannot install them, and nothing existing was damaged. **RULING (vc, under the pen): `SUPPORT_PATHS` gains both trees, and `prepare`'s dirt refusal (`:364`, `git status --porcelain -- $SUPPORT_PATHS`) now covers a peer's half-edited skill.** That is the freeze made mechanical for two more trees, not a new coupling: the cut window already forbids writes of any kind inside the repo, and shipping a half-finished skill verbatim is exactly the failure the refusal exists for. The smoke arm asserts the CLASS -- every path in that table resolves on the installed keg -- and the table has one home that `SUPPORT_PATHS` and the arm both read, with a drift check of the source's `install.join` sites against it, so the next resolver addition cannot be missed the way these two were.
- **`--force` ON A MARKER-LESS `CLAUDE.md` RETURNS THE TEMPLATE WHOLE, AND THE AUTHORED DIRECTIVE IS GONE WITH NOTHING REPORTING IT** (dc, Anvil `b63ba9c`): `carry_user_block`'s first case is "existing file has no block, nothing to carry". Put the user-block markers around the directive FIRST, then `--force`, then a plain `--apply` to show the block survives. This is the recipe's "diff first, carry by hand" line with its mechanism named.
- **THE REBUILD LEFT THE PAIR DISAGREEING** (vc, on `bin/intent3 --version` after dc's rebuild): `intent` stamped `858d4264`, `intentd` still `9b6ab634` -- "built from DIFFERENT trees" -- and the currency guard refused every hop 2 in the lane, correctly. dc rebuilds the set; why the verb's own set-verification passed a mismatched pair is the finding. Second instance of `verify_pair` redding for a peer's doc commit mid-compile is dc's (the single intervening commit touched nothing the binary compiles).
- **A v2 VERB RUN FROM THIS CHECKOUT'S `bin/` RESOLVES `INTENT_HOME` TO THE FROZEN `Intentv2` AND WRITES THERE** (vc, my error, reverted): `bin/intent claude rules index` regenerated `~/Devel/prj/Intentv2/intent/plugins/claude/rules/index.json` -- a write into the checkout that must never be written -- because the shell's `INTENT_HOME` is load-bearing for hop 1. `git checkout --` restored it, clean. Any v2 tool-library verb aimed at THIS checkout needs `INTENT_HOME=$PWD` on the command; that is how `IN-AG-RED-CONTROL-001` was validated (1 ok) and indexed (65 rules). v3 walks `RULE.md` files by directory, so the index is v2-only.
- **THE MAKE-IT-GO-RED STEP IS A RULE NOW: `IN-AG-RED-CONTROL-001`** (hv's ruling via devbin/vc; found by devbin/cc and devbin/vc). Served by any v3 binary that can see the library -- not the 3.0.0 keg, until 3.0.1.
- **HOP 2 SILENTLY DROPS WHOLE WP TRANCHES OF AT ROWS AND PRINTS `ok`** (devbin/vc, arca_cli ST0011 on the rebuilt pair; cross-tabbed, two hypotheses refuted): 55 distinct AT ids in the v2 source, 29 in the view AND the store; `residue: 0 blocking, 3 carried` named three, 23 vanished with no line anywhere; satisfaction migrated faithfully (31 = 31) so it is COVERAGE that is lost (`ac list` 20 ACs with empty `covered-by`; `ac status` 31/49 BLOCKED). Structural by tranche: WP-07 and WP-11..15 lost every AT under surviving `### WP-NN` headings; `### WP-10` vanished from the view entirely. **`carried` reports rows the parser REACHED; rows it never reached are invisible to it** -- the 0097 shape, on the migration. **FLEET EXPOSURE** (vc, each project against its pre-migration commit; Devbin 436 -> 436 and Baize 162 -> 162 as the positive controls): arca_cli 26 of 55; Utilz 13 of 42 (one thread); Laksa 9 of 570 across three threads; Cdsync 1 of 39; Lamplight 25 across ten threads (staged, never committed, tree put back on v2); Riffle 52, Prolix 4, Intent clean; Courses, Molt, MicroGPTEx, Anvil, arca_notionex have no AT rows to probe. **The verifier does not count ATs, so every one of those landings verified 0 failed.** devbin/vc's `at-accounting.sh` (Devbin `35bede5`: bucket source vs store vs view, self-test that must return both answers, NOT PROBEABLE for a thread without rows) is the fleet's one instrument from here. The fix is cc's: the parser, then hop 2 refusing on ANY declared-in vs stored-out id shortfall -- the count is the one thing nothing checks. The four lossy projects and Lamplight re-convert from their v2 sources on the fixed pair. **And the accounting must run BEFORE any bucket prune ships** (devbin/vc): the v2 bucket is the only source surface the instrument has.
- **hv's CALL, 2026-08-26: EVERYTHING GOES INTO 3.0.1 TODAY.** "I really want to get as much as we can into this release and I want it done today. We need to push on get this done and done properly." Sequenced by dependency: cc -- the parser, the shortfall refusal, duplicate id as a named residue, descent scan as one function with the guard, one canon emitter if the clock allows; ic -- the runner refuses on 0 armed, `organize --default` + the one function `init`/migration/`upgrade` call (ST0057/11), the `install.rs` remedy; dc -- the build entrances guarded (`all` forcing + set verification + dirt verdict, `--debug` the exception; `cli`/`daemon` refuse release into the shared target; `int cli` never existence-wins), ONE rebuild after cc's four and ic's two, then `int build release v3.0.1` -> prepare (rules + skills) -> smoke (rules count > 0) -> publish on hv's go; vc -- the re-conversions on that pair, then the 3.0.1 re-stamp across the fleet.
- **LAMPLIGHT, THREE LESSONS FROM ONE MIGRATION** (vc): a duplicate AT id in one thread is refused as raw sqlite (`UNIQUE constraint failed: tests.thread_id, tests.id`) naming neither thread nor id -- found per file with `grep -oE '^- AT-[0-9]+\.[0-9]+' | sort | uniq -d`, two threads, each a superseded row beside its later version, demoted by a parenthetical before the id; the scan enumerates threads by ALLOWLIST (`thread_dirs`, `legacy.rs:711`: top level + COMPLETED/NOT-STARTED/CANCELLED) while the guard enumerates by DESCENT (`collect_legacy`, `project.rs:675`), so `intent/st/_inbox/ST0339..0342` (a Lamplight convention) made hop 2 say `migrated: 349` and `ok` over a tree every v3 verb then refused -- cc: three fixes for one predicate across two homes, keep DESCENT behind `is_thread_id`, one function (a re-convert from the v2 source means deleting the uncommitted `.canon/` and `.cache/` first); and a migration-sized commit (2,947 files) ran Lamplight's pre-commit for over two minutes before I killed it -- a guard or the critic chain does not scale, unidentified.
- **CORRECTION FROM THE DIAGNOSIS (cc, within the hour): THE AT LOSS IS PER ROW, NOT PER TRANCHE.** `legacy.rs:1531` `field_end` returns the first top-level `--` or, failing that, the END OF THE LINE -- so a row ending `-- status: green. Proven to discriminate: ...` makes the status value `green. Proven to discriminate: ...`, which matches no status, and the row is rejected; 23 of 26 without a line anywhere. Discriminator perfect in both directions on arca_cli ST0011: `status: green` at end of line 0 lost / 28 kept; `status: green -- <more>` 0 / 1; `status: green.` + a sentence 26 / 0 -- AT-09.4 in the same WP survives on one separator. The tranche appearance was authoring ERA: later WPs wrote a sentence after every status, so the loss correlates with the evidence discipline -- the better the author, the surer the drop. `### WP-10` was a red herring (no AT rows in source). Same defect `80cb8509` fixed on the `file` field (`split_citation` + `append_note`), never applied to `status:`, `test:`, `evidence:`, `satisfied:`; the fix is that shape generalised. **SECOND CORPUS (Lamplight, 15 lost rows read verbatim): THREE shapes lose the row** -- `status: green.` + a sentence; a BARE trailing period (`status: green.` alone); a PARENTHETICAL after the token (`status: green (2026-07-25 cc: ...)`, `status: n/a (AC-03.2 is non-test; ...)`, some with `--` INSIDE the parentheses) -- while `status: green -- ...` beside them is kept. The value is the leading token, terminated by end of line, `--`, `.` or ` (`; everything after is the annotation; an unknown token is a loud named residue. **PRE-CHECK WITHOUT MIGRATING: `grep -rnE 'status: [a-z/-]+ ?[.(]' intent/st --include=acceptance.md`** on the v2 source gives a project's exposure before anything runs (the on-disk grep undercounts a migrated project, whose flat sources were rewritten -- use the pre-migration commit). Re-conversion needs a clean pair plus the fix and no hand repair.
- **A GATE WHOSE SUBJECT IS A FILE FIVE WRITERS SHARE HAS A BLAST RADIUS NONE OF ITS ARMS DESCRIBE** (dc, `7456a158`): `shared_artefact_build_guard.sh` reads `cmd/local` from the WORKTREE, so dc's in-flight edit to the guard's subject refused every node's commits, including a doc commit of mine, until the refactor landed with the arms moved in the same commit. Nothing was bypassed; a `--no-verify` would have hidden two refusals rather than fixing either. And the second refusal on the way in restates cc's two-writes rule with the order that matters: **sync canon FIRST, because it reads the worktree, then commit the file and canon together** -- committing first and syncing after leaves THAT commit divergent in history permanently, since a later sync can only fix the next one.
- **ONE GUARDED RELEASE BUILD BEHIND EVERY VERB** (dc, `7456a158`): `releasebuild.lib` -- dirt verdict -> provenance force -> build -> pair verification -- called by `int local build` and `int build all`; `build cli`/`build daemon` refuse a release build into the shared target (`--debug` builds; `CARGO_TARGET_DIR` lifts it); `cmd/cli` runs the freshest of debug and release and says which; `verify_pair` delegates the agreement half to `currency.lib`'s `artefact_pair_marker` rather than becoming a third home for it. The rebuild for the re-conversions goes through this path whichever verb reaches for it.
- **THE SAME LOSS ON AC ROWS, AND A CASCADE THAT POISONS EVERY PAST RESIDUE REPORT** (cc, with the parser fix green: arca_cli ST0011 AT 29->55 of 55, **AC 49->57 of 57**): `satisfied: yes. The evidence is ...` is the identical shape one field over; `satisfied_verdict` had been fixed once for `(` (the Courses ST0002 episode) and left `.` open -- instance closed, class open, like `thread_dirs`. And a dropped AC manufactures a `BrokenReference` on every AT that covers it (ST0011's residue went 12 -> 3 on the fix; 8 of the 9 that vanished were this), so **the migrations have been accusing authors of links they did write**; residues re-derive on re-conversion and only the survivors are real. Utilz ST0009's thirteen are all shape (c), `status: green (red at HEAD)`. The accounting instrument gains an AC arm (devbin/vc) before any re-conversion; pre-check `satisfied: [a-z/-]+ ?[.(]` over-counts (25 hits for 8 real) and is quoted as a pre-check, never as exposure.
- **THE RE-CONVERSION IS A SCRIPT, TESTED ON A THROWAWAY** (vc, `reconvert.sh`): restore `intent/st` and `config.json` exactly as at the migration commit's parent; drop the committed canon and the store; re-apply the landing's hand-set `project_name`/`author`/`languages`; hop 1 on the frozen v2 where the parent is below the floor, then the ratified `acceptance.md` files restored from the source again (v2's re-punctuation reverted, the earlier ruling); hop 2 with the pair read in the same command; hop 3 twice; per-machine ignores; accounting + verifier; `--only` the intent paths and root canon, never a peer's. Root canon files are not restored (the landing's CLAUDE.md carry stands). Refuses a dirty tree.
- **THE FLEET WORKLIST, TWO-ARMED: 57 ITEMS ACROSS FOUR PROJECTS** (devbin/vc, `at-accounting.sh` `7aa1f21`, AC arm added): arca_cli ST0011 AT -26 and AC -8; Utilz ST0009 AT -13 (its criteria intact); Laksa ST0089 -6, ST0084 -2, ST0090 -1; Cdsync ST0001 -1. Store and view agree on every row, both arms. The AC arm probes MORE threads than the AT arm (Devbin ST0002: 25 criteria, no tests), so a single-armed instrument measured a smaller set of threads, not just half the defect. Three instrument lessons, each a silent zero: a self-test must prove EACH ARM in both directions, or a new arm inherits an untested path; assertions are properties (at least one clean thread, zero false shortfalls), never counts a fixture can outgrow; and a run that measured NOTHING printed no rows and exited 0 (Anvil, MicroGPTEx, Molt, arca_notionex) -- now a printed verdict, NOT a pass: "nothing to lose" and "no loss detected" are different statements and only one belongs in a fleet summary. Pass criterion after each re-conversion: source == store == view on AT and AC, rc 0, `--self-test` in the same invocation. arca_cli is the only estate that lost ACs, so the only one whose `BrokenReference` residues can be manufactured; the other three lost tests only and their residues have another provenance.
- **THE INSTRUMENT'S FOUR DEFECTS ALL FAILED TOWARD CALM, AND THAT IS NOT CHANCE** (devbin/vc): `NOT-STARTED/` unseen; Prolix given up on before git; four projects emitting no rows and exiting 0; a nonexistent path returning rc 0 with `NOT MIGRATED -- nothing to account for` (and the first fix tested for `intent/`, which `/tmp` satisfied because of the in-session sentinels -- a marker weak enough to be met by coincidence is not a marker). A check that wrongly reports trouble is investigated within minutes; one that wrongly reports calm is adopted. Exit contract now: rc 3 ERROR (nothing measured), rc 0 `NOT MIGRATED`, rc 0 `VERDICT: N thread(s) measured` (the only pass), rc 1 SHORTFALL -- so the re-conversion scripts commit only on rc 0 AND a `VERDICT:` line, and `NOT MIGRATED` in a re-conversion commit is itself the alarm. Writing the instrument's own line into the commit message is the design that gives a wrong calm a reader.
- **TWO INSTRUMENTS CONCURRING BECAUSE THEY SHARE AN ANCHOR IS NOT TWO PIECES OF EVIDENCE** (lamplight/vc, from source): the row parser is `line.strip_prefix("- ")` with no `trim_start()` (`legacy.rs:1117`), so an indented `  - AT-` row never reaches classification and produces no `UnparseableRow` finding -- invisible, not skipped-and-recorded -- and the accounting's `^- AT-` anchor agrees with it for the same reason. Lamplight's 37 indented rows are all template placeholders (checked all 37, not four), so dropping them is correct today; an indented REAL row would be lost silently by both. For cc, after tonight: an indented row that looks like a row is a named residue, not a `continue`. Status case is folded by the migration (`legacy.rs:827`), so `COMPLETED`/`Cancelled` variants map cleanly. Triage is not in v2's vocabulary: set it through canon after hop 2, never by writing the word into a v2 file.
- **A RANGE ANCHORED ON YOUR OWN LAST COMMIT CANNOT SEE PEER WORK THAT PREDATES IT** (dc, post-compact): `git log <my-last>..HEAD -- native/rust` came back empty and read as "cc has not landed" -- cc's two batch commits sat BELOW the lower bound. The wrong-population shape on the time axis. **And beside it, the error a correct range would not have prevented** (dc, `a163ba56`, driven not accepted: the two commits touch `field_end` zero times; the fix is the uncommitted `legacy.rs` introducing `split_field_value` and `status_annotation`): having found the blind spot, dc read what it surfaced -- two commits, cc's, ancestors, touching the parser's own file -- as the IDENTITY of the fix. A trigger names a DEFECT; a range names a POPULATION; an author prefix names neither. Every piece of evidence was true and the conclusion was still wrong: a check that ran on real data and returned a true result about something ADJACENT to the subject has no tell, which is worse than a silent zero. The uncorrected board entry stays in history; the correction is a new commit, never a rewrite, because the wrong claim had already been read. And the way the false statement aged is its own lesson (dc, `d121bf56`): "cc's half is met" was wrong when written and became TRUE an hour later for a different commit -- a claim that becomes true for the wrong reason leaves nothing to correct and no way to notice it was ever wrong, which is worse than one that stays false. The check that survives tests for the DEFECT (`split_field_value` present in HEAD's `legacy.rs`, the test file tracked), never for the author. And a gate that reads NOT quiet for a cargo in a private target that could never contend for the shared lock is pointed the right way: a false NOT-quiet costs a wait, a false QUIET costs a corrupt pair -- do not narrow it. And the correction it needed on top: a landed sha is not the trigger for a shared build; a QUIET tree is -- `guarded_release_build` refuses any dirt under `native/rust` whoever owns it, so rebuild 1 waits for cc's parser commit AND ic's runner commit AND ic's in-flight organise files committed or stashed. The pen says "quiet" from TWO reads at the moment, never inferred from who has reported: `git status --porcelain -- native/rust` empty AND no `cargo build|test|clippy` in the process LIST (cc: a clean worktree and an idle build directory are different properties; a tree can go clean the instant a commit lands while that node's suite is still compiling into the shared target, and a build fired then lands on a locked target and reads as a flaky build rather than a race; and a live cargo can report 0 to a count, so read the list). Order that holds: cc's commit, ic's commit, then no dirt and no cargo, then dc's rebuild -- and two nodes with uncommitted work in one crate cannot take a full-suite number that means anything, because each compiles the other's half; committing is the fix.
- **IN A FLEET OF LLM SESSIONS, THE AGENTS' OWN INSTRUCTIONS ARE PART OF THE MACHINE'S OBSERVABLE STATE** (dc and vc, the same defect found twice within minutes): `quiet.sh`'s `pgrep -f 'cargo (build|test|...)'` matched the three peer Claude sessions FOREVER, because their `--append-system-prompt` argv carries the restart text that says "`cargo fmt --check` runs on every push" -- a document that warns about a command is indistinguishable, to a full-command-line matcher, from the command. And `grep -c .` over `pgrep -fl` counted LINES of injected prompt (85, drifting to 57) for six pids. Both positive controls exercised only the arm that fires: adding a matcher to an instrument that already matches everything reads NOT quiet both times, and proving the pattern's alternation works proves nothing about what else it sweeps in -- between two nodes, the same half controlled twice. Fix: match the EXECUTABLE (`pgrep -lx cargo`, one line per pid), then the control that matters -- the sessions match 0 (negative), and the count RISES by one during a private cargo run in a scratch target and falls back (positive). Any `pgrep -f`, `ps | grep` or log scrape over this box reads our prompts as evidence of what they describe.
- **THE SHORTFALL GATE HAS TWO HALVES AND ONLY ONE IS A DEFECT** (cc): Half A -- per file, `declared_in - stored_out` must equal the rejection findings recorded, else a row vanished with no line and the migrator is misreporting ITSELF: an ERROR, refuse, not residue (this is what let ST0011 lose 26 at exit 0). Half B is policy: the ratified residue/carried split sends every finding on a CLOSED thread to `carried`, which does not block -- a shortfall on a closed thread exited 0 BY DESIGN. Ruled (ii) under the pen: the self-check always; the carry kept for attributable rows on closed threads, printed by id and reason; the accounting as the outer gate at the layer that commits. cc was right to refuse to reverse a ratified split from one sentence on release night.
- **macOS `iconv -f UTF-8 -t UTF-8` IS NOT A UTF-8 VALIDATOR** (vc, Lamplight, 2026-08-26 17:17Z): it exits 1 with `Inappropriate ioctl for device` on ten VALID files, and my positive control (a file with `0xff`) failed with the SAME message -- so the control discriminated nothing and I reported ten clean markdown files as non-UTF-8. Python and Perl strict decoders are the instrument. **A control must fail for the RIGHT reason: read its stderr, not its rc.**
- **A COVERS ID ENDS AT THE FIRST TOKEN THAT IS NOT ID-SHAPED** (lamplight-vc): `covers AC-00.3 clause 3 ("...")` under `split_once(" (")` yields the id `AC-00.3 clause 3`, so the one-token fix I approved would have landed a manufactured BrokenReference at exit 0 against a clean estate. lamplight-vc simulated against `legacy.rs` `covers()` BEFORE editing the ratified file and landed the documented `AC-00.3 (clause 3)` shape instead (`d0f172876`). **Simulate a content fix against the parser that will read it; the approved outcome is not the approved typo.** A `--` inside the parens is a different function (`field_end`), not the token rule (cc).
- **THE OPAQUE-ATTACHMENT ARM WAS COMPLETE AND UNREACHABLE** (cc): `Project::classify` decides `Unattached` on a three-entry extension allowlist three steps before the decoder that "decides by decoding, never by extension" ever runs. Both halves right, the composition fails -- the `unclaimed_digest`-vs-`show` shape again. And the excluded population is mostly TEXT (196 `.tap` in Intent's own tree; Lamplight's 27 images are the minority).
- **CANON CARRIES ATTACHMENT BYTES INLINE** (`{path, text, bytes, sha256}`; ST0056.json is 2.3 MB): an opaque attachment in the extract is inline base64 in a committed JSON, so a size cap with refusal BY NAME is load-bearing until blobs have a home outside the extract. Lamplight ST0306 alone: ten gifs at 4.8-24.7 MB.
- **A CONTRADICTION ESCALATED AS TWO EXCLUSIVE RULINGS WAS A MISSING QUALIFIER** (hv, first-hand in ic's session): bare `--default` never removes a file; `--default --force` removes files after a confirm -- and the confirm was already in the contract as AC-11.2's tty read. Consequence nobody had written down: **the destructive half of a fleet sweep is the human's keystrokes; a tool session cannot drive it and must not try** (`--no-confirm` precedent). AC-11.6 (`e8ba126b`) gives the destructive arm a criterion so the ruling has a home in the contract, not only on a board.
- **IN A SHARED CHECKOUT A GATE IS ONLY A CLAIM ABOUT A TREE THAT HELD STILL FOR ALL OF IT** (cc, 2026-08-26 17:20Z): clippy passed rc=0 at 18:01 on a tree that no longer existed when the suite compiled at 18:02 -- ic's uncommitted `facade.rs` stopped the compile before one test binary ran (`SUITE EXIT=101`, `binaries=0`), and nothing in the output said the tree had moved. Re-taken in an isolated worktree at HEAD + the two files, Half A went RED on two ratified tests: the class of a refused verdict had been relabelled `UnparseableRow` -> `UnknownStatus` under a doc comment that still stated the old contract. **Widening what parses must not widen -- or relabel -- what is BELIEVED; and a gate taken on a moving tree is not a gate.**
- **A DEHYDRATION PRECONDITION ENUMERATES FROM DISK, NEVER FROM GIT OR THE STORE** (lamplight-vc, 2026-08-26 17:20Z): Lamplight ST0306 carries ten gifs at 4.8-24.7 MB (152.9 MB) that git never sees -- deliberately, by a tracked `.gitignore` carrying hv's 2026-07-12 ruling that lived-verify gifs are review aids on disk, not repo artefacts. lamplight-vc first read "untracked" as "at risk" and retracted it on reading WHY; what survives is the rule, which needs no author's intent: every file on disk under the thread dir must be held by the store or the removal is refused naming that file. **A list of threads would have needed to know why each file was untracked; the rule does not.**
- **A `cd` INSIDE A COMPOUND COMMAND RETARGETS EVERY RELATIVE PATH AFTER IT** (vc, 2026-08-26 17:20Z, my own): a Bash call that began `cd ~/Devel/prj/Lamplight && ...` and then appended to `intent/whiteboard/vc/cutover-runbook.md` and committed it landed a commit (`59499e9da`) in LAMPLIGHT's repo, creating a stray file under lamplight-vc's node directory -- the same relative path exists in both trees, so nothing refused. Their uncommitted work was untouched (`--only` one path), Intent's runbook got nothing, and the undo is theirs to run because it is their HEAD. **Absolute paths and `git -C <repo>` in every command that writes; the tool's own caveat said so and I read it as a permission note.**
- **A MIXED RESET RESETS THE SHARED INDEX** (lamplight-vc, 2026-08-26 17:22Z, undoing my `59499e9da`): before `git reset --mixed HEAD~1` on a shared checkout, `git diff --cached` must be EMPTY -- a peer's staged work would be silently unstaged by the undo of someone else's commit. If it is not empty, `git rm` + a commit is the undo, and the noisier history is the price. They verified the tip had zero commits on top and one file before touching anything; a two-command undo only because the stray commit was `--only` one path -- `-A` in the same call would have swept four files across two nodes' lanes into a commit about a runbook.
- **HALF A TESTED BY ITS DEFECT BY A SECOND OPERATOR** (vc, 2026-08-26 17:22Z): worktree at `32dae690`, own target dir; control 4 passed; the AT arm's `out.record(...)` at `legacy.rs:1151` deleted -> rc 101, `a_refused_row_is_accounted_for_by_the_finding_it_recorded` red with the named message ("2 AC/AT row(s) declared, 1 read, 0 refusal(s) recorded -- 1 unaccounted for"). Two `Err((class, detail))` arms exist; anchoring on the FIRST mutates the AC half and the AT test stays green -- cc's own first mutation silently did not apply for that reason. **A mutation that lands on the wrong arm is a green that proves nothing; print the mutated lines.**
- **HOP 1 WRITES v2's CANON OVER THE SHARED `~/.claude/skills`** (devbin-vc measured it, 2026-08-26 17:32Z): the frozen v2 `upgrade` reaches `propagate_canon_skills` unconditionally, so every hop 1 on a project below 2.19.0 rewrites all 25 installed skills with v2 bytes. Exactly the three whose v2 and v3 sources DIFFER (`in-essentials`, `in-author-essentials`, `in-content-essentials`) came back as v2 at 13:33Z; on the other 22 the same write is invisible and harmless, which is why "three skills" was the whole visible symptom of a 25-skill write. `skills-declobber.sh` decides a clobber by bytes against BOTH trees (== v2 AND != v3), HOLDS anything matching neither, repairs only when the held set is empty, and verifies the repair landed; `reconvert.sh` runs it after every hop 1. **Its first self-test fixture used a skill identical in both trees and reported clean -- a clobber sample must be a skill whose sources differ, or the test cannot exhibit the defect.**
- **THE KEG FIRST ON PATH IS `80d8b2ca` WITH ZERO RULES, AND NOTHING SAYS IT IS OLD** (devbin-vc, confirmed by my own reads, 2026-08-26 17:32Z): `brew outdated` is silent because the version is 3.0.0 either side of 106 commits; `critic shell` on it answers "0 of 0 ASKED, 0 armed" at rc 0 and the hook branches on rc, so every fleet critic gate reached via PATH is armed with nothing until the 3.0.1 keg (rules packaged, empty census refused at exit 2). KNOWN interim state; nobody reorders PATH or touches the keg under nine live sessions. **A dev rebuild is done at its own homes (`target/release`, `~/.local/bin`, `~/bin`); the keg changes only at publish + `brew upgrade` -- "resolves on PATH" is the predicate for the keg, not for the pair.** And `bin/intent3` refuses the moment a non-test file under `native/rust` lands after the pair ("1 file changed since 99673f91 -- behind HEAD"): the currency wrapper is the instrument that says a pair is stale, and it said so within a minute of `32dae690`.
- **A RATIFIED REFUSAL THAT HOLDS BY A SIDE EFFECT IS ONE REFACTOR FROM DECORATION** (cc, 2026-08-26 17:38Z): `a_note_wrapped_in_markdown_emphasis_is_still_refused` (Lamplight ST0345, `satisfied: yes _(...)_`) was passing because the old cut left `yes _` as an unrecognised WORD -- the right outcome by the wrong route; cutting the token properly made the boundary evaporate with nothing recording it had existed. cc neither crossed nor deleted it: the refusal is now explicit and field-local (a `satisfied:` annotation beginning `_` or `*` refuses by name), and the recovery number fell from 7 to 4 because the 7 had trimmed `*`/`_` off both ends and defeated the boundary through the back door. **Write the boundary down where it is enforced; a test's assertion message is the ruling's only home until then.** Re-ruling on the row's merits after the cut; ST0345 is Completed, so it is carried by name meanwhile.
- **THE MUTATION PROTOCOL CAUGHT A TEST THAT COULD NOT GO RED FOR ITS OWN DEFECT** (vc, 2026-08-26 17:44Z, at cc's `84cda496`): three of four mutations red as named; M4 -- the old `rest.find(" -- ")` cut restored inside `fn covers` -- left `a_separator_inside_a_qualifier_does_not_truncate_the_covers_span` GREEN. The site is load-bearing; the test asserted the id (`AC-04.2`) and the absence of findings, and the truncation never touched the id -- it lost the QUALIFIER, which `with_qualifiers` folds into the row's note. The assertion that arms it is on the note's text. **A test is named for a defect; if its assertions cannot observe that defect's effect, its green is the defect's camouflage. Run the mutation before believing the name** -- the second operator's run cost seven minutes and one worktree.
- **`Plan::run` ANSWERS `Ok` WITH ITS REFUSALS INSIDE THE REPORT** (ic, 2026-08-26 18:29Z): `PreconditionsUnmet` is pushed onto `report.refused` and the function returns `Ok`, so a caller reading only the `Err` arm swallows the refusal -- `st dehydrate` on an estate with no declared preconditions removed nothing, DELISTED the thread and reported success, the manifest saying dehydrated over a full tree. The comment above the line asserted the opposite ("refuses the whole run before touching anything"), written from the shape of the call, never driven. Found by ic's own first test run; armed by my D1 mutation. Two callers of `Plan::run` in src, both `facade.rs`; the organize path renders refusals -- whether it EXITS non-zero on them is 2b's question. **A written guarantee standing in for a mechanism, again; and a refusal carried inside a success value is the classifier-default-bucket defect in a return type.**
- **THE MUTATION PROTOCOL, TALLIED FOR 2a** (vc, 2026-08-26 18:29Z): eight mutations published by their authors with the shas (cc 1 + 4, ic 3), all run by a second operator in isolated worktrees with byte-exact restore; seven red as named on the first run, one green -- the blind test at 84cda496 -- fixed test-only and red at 5973103e. Cost: three worktree compiles, about twenty minutes wall-clock across the evening. Both authors deliberately did NOT pre-verify their own arms; the one arm that was wrong could only be found by someone else running it, because a blind test is green by construction and cannot report itself. QUIET named every sha by defect and every mutation by name; dc tests the same way before compiling. **The clock decided ic's landing (18:14Z on an 18:10Z deadline, four minutes late and said so) and the follow-up that dirtied the tree after the clean word was announced inside the minute -- the protocol's value is that both of those were visible.**
- **THE SIZE CAP DOES NOT BELONG IN `classify`** (cc, 2026-08-26 18:29Z, FYI while holding): `Project::classify(rel)` is a pure path-to-kind question and `edit_disposition` calls it with no natural size, so threading a size through it makes a path question stat a file -- the two-homes defect arriving in a new place while it is being fixed in the old one. Of `Unattached`'s six consumers, exactly ONE (`doctor.rs:843`, the uncovered report) treats it differently from `Attachment`; the rest read "not an attachment". Shape ruled for 2b: `classify` drops the allowlist and answers view / canon / attachment; the cap gets ONE named home called by both `collect_attachments` and `doctor`, so the thing that carries and the thing that reports what was not carried cannot drift; decode still decides text-vs-opaque; size read from metadata before the read. Zero sites in `facade.rs`, so it no longer queues behind ic. **And `doctor`'s uncovered report changes MEANING -- from "an extension we do not carry" to "over the cap" -- so a project reading it as a to-do list will see it shrink sharply on the sweep; that is the honest meaning and it is announced here rather than discovered.**
- **A HOLD STATED BY ITS SCOPE INVITES COMPLIANCE THAT DEFEATS ITS PURPOSE** (ic, 2026-08-26 18:29Z): my QUIET hold said "nothing under `native/rust`"; ic obeyed it to the letter with a board fold, HEAD moved, and dc refused to build on a sha that was no longer HEAD -- correctly, because the stamp and `verify_pair` read `git rev-parse HEAD`, not a path. **The QUIET hold is "HEAD does not move until the three reads"; state the mechanism and the scope follows, state the scope and the mechanism is guessed.** Re-named to `43c3d2d0` on a driven, positive-controlled whiteboard-only delta; four minutes lost, one message.
- **THE CAP'S RATIONALE, CORRECTED IN cc's NUMBERS** (cc, 2026-08-26 18:29Z): `thread_files` walks with `git_ignore(true)`, so git-ignored files never reach `classify` -- the `ignored` reason I ruled for `Unattached` was an unreachable branch and is DROPPED; `Unattached` has one reason, over cap with the byte count. On Lamplight the walk excludes the ten ST0306 gifs (145.9 MiB, 89% of the bytes I had quoted); what reaches `classify` is 35 files / 18.4 MiB, of which the 1 MiB cap refuses eight ST0167 screenshots (14.8 MiB, ~20 MiB of base64 kept out of committed canon) and carries 27 (3.6 MiB). **The `.gitignore` protects canon from the gifs; the cap protects it from the screenshots. A decision recorded with a wrong number gets re-litigated the first time someone re-measures -- so the number is corrected here, not the ruling.** And the gifs are a single copy on one machine that nothing in the estate can see: hv's 2026-07-12 choice, restated to hv with the number.
- **`git commit --only -- .` SKIPS UNTRACKED FILES SILENTLY, AT rc 0** (lamplight-vc, 2026-08-26 18:31Z, reproduced in a scratch repo): with an explicit path `--only` errors on an untracked file; with `.` it commits the tracked changes, prints a clean summary and leaves the untracked directory -- the entire store a migration exists to produce -- out of the commit. The run scripts already `git add -A` with the same exclusions first (my start notice omitted that line, and lamplight-vc withheld GO on what I wrote rather than on what the file says -- correctly); both now ASSERT after the commit that `intent/.canon/` paths are in `git show --stat HEAD` and that no untracked files remain, else NOT DONE at exit 6. **"The commit succeeded" is not evidence of what is in it.** And a relative `VC_PRE_HOP2` resolved inside the project after the script's `cd` (my own board transcribed the relative form): the script now resolves it before the `cd` and refuses before any restore when the file is absent, with a negative control at rc 7 leaving the tree untouched.
- **A UNIT TEST THAT CALLS A FUNCTION DIRECTLY PROVES THE FUNCTION WORKS AND SAYS NOTHING ABOUT WHETHER ANYTHING CALLS IT** (ic, 2026-08-26 18:42Z): `intentfiles::default_declaration` landed at `baba2d9d`, reported as "the default declaration is in", with ZERO production callers -- `init`, the migration and `upgrade` write no `.intentfiles`; the seven other references are its own test file. The day's third complete-and-unreachable mechanism (cc's opaque attachments behind the allowlist; `views::write_all`'s mtime guard; this). Coverage is what makes the missing caller invisible. `flag_reachability.rs` reds on a declared flag no renderer reads; nothing does the same for a lib function a criterion demands. **Before greening a criterion on a function, name its caller.**
- **A RE-RENDERED VIEW IS AN ADDED LINE TO A GUARD OVER ADDED LINES, AND THE GUARD WAS RIGHT** (vc, Laksa, 2026-08-26 18:42Z): the re-conversion changed ST0089 AC-06.10's rendering (the fixed parser carries the tail it used to drop) and Laksa's own `hv-attribution-guard` (`core.hooksPath = bin/hooks`, added after the first landing) refused the commit -- the row attributes a 2026-08-05 decision to hv ("hv's ruling") with no anchor. The first landing passed only because the guard arrived later. Remedy is the guard's own escape, the word UNANCHORED, appended to the row's prose in the v2 SOURCE by the pre-hop-2 hook so canon and view carry the same bytes, and named in the commit message; never `--no-verify`. Run the guard itself on the staged index to get the exact refusal set -- my extraction of its regex found zero, the guard found one.
- **LAMPLIGHT LANDED FRESH ON 43c3d2d0 (`4ffd0fe1f`)**, (2026-08-26 18:42Z): 353 threads, one named carried row (ST0345/AC-05.2), seven previously-lost rows recovered, guards 4 ran / 0 skipped, 357 canon paths in the commit. The fresh script's gate (rc 0 only) refused the expected named shortfall; the (ii) gate lives in `reconvert.sh` and the fresh script needs the same ONE home (`acct-gate.sh`, TODO) -- tonight the commit went by hand with the script's exact exclusions and the store assertion. **`sync --to-store` on ANY thread is refused estate-wide by 23 unknown-file-shape files** ("a modelled directory carries only the artefacts Intent writes"), so Triage for the four inbox threads waits for 2b's classifier -- the "23" I had remembered as non-UTF-8 files was this count, of a different thing.
- **A COMMIT THAT LEAVES THE INDEX LOADED IS NOT FINISHED** (lamplight-vc, 2026-08-26 18:44Z; reproduced on Laksa a minute later): `git add -A` staged the raw migration output, the pre-commit hook prettified those files in the working tree and the commit took the prettified bytes, and the on-disk index kept the raw ones -- 163 files on Lamplight, 14 on Laksa, each `MM`, worktree == HEAD. The next node to run a plain `git commit` would have committed pre-prettier content and silently reverted the normalisation. Both run scripts now assert after the commit that nothing is staged; where the worktree equals HEAD they clear the index themselves and say so, otherwise NOT DONE. **On a shared checkout the index is state; assert it empty the way you assert the store landed.** Laksa landed at `592ba009` on 43c3d2d0 the same way, with the UNANCHORED admission on ST0089 AC-06.10 named in its message.
- **THE FLEET IS FULLY REALISED, AND hv PUT IT FRONT AND CENTRE** (2026-08-26 18:50Z): Lamplight 353 ST dirs for an open set of 57; the migrator realises everything and `migrate.rs:488` respects an absent manifest by coincidence (ic). Not a plan change: 2b's `--default --force` is the fix, per project, after a confirm. Two loose ends recorded: (1) Lamplight's committed canon reads `status: triage` for ST0339-ST0342 (my script's jq edit landed before the sync it fed was refused) while store and view read Not Started -- they converge at 2b's `sync`; (2) at 18:45:00Z, eight minutes after `4ffd0fe1f`, 167 of Lamplight's files were rewritten in the working tree by an invocation not yet identified -- 164 views (three with CONTENT loss: ST0201's `AT-04.3`/`AT-04.4` rows gone, ST0256/ST0298's covers-less rows back in the old `(no reference)` form), `todo.md`, one canon issue. HEAD is correct (the accounting proved source == store == view at commit). Tested in a throwaway clone with a pair-written store: the KEG's `st list`, `ac status` and `doctor` REFUSE at rc 1 and write nothing (it cannot read a store the dev pair wrote), `todo` and `upgrade` write nothing -- so the keg's read verbs are cleared and the writer is still open; lamplight-vc asked. **A re-converted project is unreadable by the keg on PATH until 3.0.1 -- every hook that resolves `intent` via PATH gets rc 1 there, which is at least loud.**
- **A SHARED FILE READ LIVE BY EVERY PEER'S GATE IS A RUNTIME INPUT** (ic, 2026-08-26 18:50Z): `surface/dispatch-table.json` is read from the TREE by Intent's pre-commit; ic's script wrote it and validated afterwards, a regex repair fixed a different site, and for the minutes between write and revert every commit in the repo failed on `jq: parse error` (mine did). Build in memory, parse, then write -- a file every gate reads must never exist on disk in a state its writer has not checked. The fourth axis of the day's population class: `--test` narrows by target, `check -p` by kind, the shared tree by whose files, `--all` widens into others' work, and a shared input turns a private mistake into everyone's outage.
- **AN EXERCISE INHERITED FROM INCIDENTAL REPO CONTENT EXPIRES WHEN THE CONTENT CHANGES** (cc, 2026-08-26 18:50Z): `verbosity_flags::quiet_is_strictly_less_than_the_default` ran `doctor` against the real repository root and passed only because Intent's tree happened to hold 196 uncarried `.tap` files; under the cap it holds none, quiet == default, and a shipped flag lost its only end-to-end exercise -- visible only as a red. Fixture the test controls (one over-cap file), never "not longer than". Same rule as the accidental enforcements, from the test's side. And cc's gate instrument: `COMPILE_ERROR_LINES` grepped `^error`, which matches `error: test failed` -- a discriminator that cannot discriminate; `binaries=` is the one that can.
- **CORRECTION, MINE, AND THE WRITER FOUND** (lamplight-vc, 2026-08-26 18:51Z): Lamplight's 167 rewritten files were lamplight-vc's ~550 `intent` invocations via PATH after my done message -- `st list`, `st show`, `ac gate` across all 268 completed threads, twice -- resolving to the KEG (`/opt/homebrew/bin/intent` = `80d8b2ca`, pre-fix), whose read-shaped verbs RE-RENDER views in the older format. **NO ROWS WERE LOST**: the old renderer ADDS `(no reference)` to rows that cite no test file; I had printed only the `-` side of each hunk and called an annotation a deletion -- the iconv shape again, an instrument reporting a different event than the one that happened. Restored to HEAD's bytes on lamplight-vc's word. **THE HAZARD FOR THE WHOLE FLEET UNTIL 3.0.1's KEG: the estate's most-typed command is a formatter nobody asked for** -- a read verb via PATH inside a re-converted project silently rewrites tracked views; run the pair by absolute path or run nothing. And theirs: a verb whose name is a read is not evidence that it does not write; check `git status` after the first call, not the five-hundredth.
- **`git checkout -- <file>` IN A WORKTREE HOLDING UNCOMMITTED WORK IS A REVERT, NOT A RESTORE** (cc, 2026-08-26 18:54Z): between mutations cc undid each with `git checkout -- <file>`, which took the file to HEAD and discarded the change under test, so the next run measured a tree without the work in it; the only tell was an anchor matching zero. Copy the file aside and copy it back; never let git decide what "restore" means when the baseline is uncommitted. (My harness copies aside; it never asks git.) **And the fifth accidental enforcement:** with the extension list gone, `attachment_put` accepted a body of any size and would have written a canon row `--to-store` could never produce -- the list had been the only thing enforcing "an artefact the owning pipeline cannot reproduce is already drifting"; both `put` doors now carry the cap. Reading the PROPERTY a test held, not its subject, is what found it. `COMPILE_STOPS` in cc's gate is now `^error\[` or `^error: could not compile`, not bare `^error`.
- **`flag_reachability.rs` NEVER ITERATES `table.new_surface`** (ic, 2026-08-26 18:54Z): the gate whose purpose is that a declared-and-inert flag is a lie told by `--help` covers only `table.families`; every v3-native flag (13 across 11 entries) sits outside it, and tonight that hides exactly one inert flag -- ic's own `organize --default`, wired now. 3.0.2 for the one-line chain, because the population doubles and a release window is no place to learn what turns red. RULED: the `--default`/`--force` rows do not ship inert -- out of the table before 2b's build if B1's floor misses QUIET. ic's own instrument lesson: a stand-in for the gate under audit searched the bare string where the gate searches the quoted id, and "found" a ruling (`ingest --from-md`, kept by vc, recorded at `render.rs:2207`) as a defect.
- **HOP 1 WRITES THREE SKILLS AND REPORTS `0 updated` AT rc 0** (devbin-vc, 2026-08-26 18:55Z, reproduced by construction on arca_config at 2.18.0): the frozen v2 `upgrade` clobbered exactly the three skills whose v2 and v3 sources differ, at 18:51Z, and printed `ok: 0 updated, 26 skipped, 0 failed`. The clobber is not merely undetected, it is REPORTED AS NOT HAVING HAPPENED -- so no declobber may ever short-circuit on the tool's own output; `skills-declobber.sh` reads bytes and must stay byte-based. And `treeindex` is not the discriminator: two of the three carry zero treeindex references in both versions and differ by 63 and 62 bytes of other content -- a content-keyed detector catches one of three and reads the fleet clear. Also from that run: hop 1 modifies `intent/st/COMPLETED/ST0002/acceptance.md` in the working tree, so anything reading the tree between hops reads a moved file -- source from a REF, never the tree. arca_config's hop 2 was refused by `bin/intent3` ("4 non-test file(s) under native/rust changed since 43c3d2d0") the moment cc's 2b landed; devbin-vc reverted to clean pre-migration rather than hold a half-migrated tree, and re-runs on 2b's pair. **A pair handed out ahead is valid until the next refusal, and the refusal is what invalidates it.**
- **RETRACTION, THEIRS AND MINE: THE KEG'S READ VERBS DO NOT REWRITE VIEWS** (lamplight-vc, 2026-08-26 18:56Z): a clone of Lamplight at `4ffd0fe1f` under the stale keg -- `st list`, `ac gate` x6 -- 0 files modified; my Cdsync clone had said the same and I let a timestamp outrank two clean experiments. The writer, on the evidence not yet driven: a WRITE verb at 19:45:00 local, the same second `intent/.canon/issues/0005.json` appeared -- a real issue filed today ("Wrighter project open re-parses every world-scope document and rewrites every sync_hash it already had", reporter matts, severity high), matching the app code a Lamplight node is editing this minute -- so `intent issue new` via PATH reached the keg, which re-emitted every view with its older renderer. The hazard narrows to WRITE verbs via PATH until 3.0.1; the untracked issue file is a peer's live work and stays. **Two people confessed to the same event inside four minutes on the strength of a clock; the clone test each already had took two minutes and cleared both.**
- **A NON-NULL WRONG READING PRINTS A PLAUSIBLE PRODUCT BEHAVIOUR** (dc, 2026-08-26 19:00Z): `"$I" $v` with `v="st show ST0001"` passes ONE argument under zsh; the binary answered `error: unrecognized subcommand` at rc 1, which dc read as "the new pair REFUSES a read against a divergent tree" -- a headline feature that never ran -- then corrected with a second wrong theory before finding the cause. Three consecutive confident readings, none of them the product; the block that held was the one whose args went through `"$@"`. **Prove the verb RAN (a row came back) before reading its rc.** The same session measured what matters: the pair's read verbs write nothing on a convergent or a divergent tree, with a write control (`st triage`, dirty=4) proving the instrument sees writes.
- **THE FLEET IGNORES ITS PER-MACHINE STORE** (vc, 2026-08-26 19:00Z): every migrated project ignores `intent/.cache/intent.db` and `intent/events.jsonl`, none tracks the DB; arca_config will when it re-converts. My first pass reported two "NOT-ignored" -- one was arca_config at 2.18.0 (expected), the other my own `awk '{print $1}'` splitting "Courses/002 Agentic Coding" at its space and probing a directory that does not exist. **A path with a space is the oldest instrument defect there is, and it still reports a plausible verdict.** dc's backlog item stands: `intent init` lays down no `.gitignore`, so a fresh project is one `git add -A` from committing the DB (Intent's own `.gitignore:125` says so); 3.0.1 backlog.
- **THE SIGNATURE DATES THE BINARY** (cc, 2026-08-26 19:02Z): the current tree writes `(no reference)` nowhere -- `e696de15` removed it this morning ("a fix that stops refusing and starts rewriting is worse than the refusal") -- and `strings` shows the keg on PATH carries the literal while `bin/intent3` does not; there is ONE emitter in production (`views::render_all`, called by `organize`, `address`, `facade` and `migrate.rs:495` alike) and `write_all` has no production caller. So the 167 were written by the keg via a WRITE verb, and my two-emitter hypothesis is dead -- as is the test I specified for it: `bin/intent3 sync --to-disk` on a clone cannot emit the literal, so its 0 would have been read as a kill and meant nothing. **The honest-and-blind family arrived in the test designed to settle the question, on the day we catalogued it; before running a discriminating test, ask whether the instrument can produce the signature at all.** And the `strings` negative on the pair carries only because the SAME probe finds the literal in the keg: `strings` is blind to a string assembled at runtime (`format!`, a concat), so an absence alone reads exactly like a capability proof and is not one -- the keg half is the POSITIVE CONTROL, not a second finding (dc).
- **A MEASUREMENT HANDED TO YOU IS EVIDENCE; THE CAUSE ATTACHED TO IT IS A HYPOTHESIS WEARING THE MEASUREMENT'S AUTHORITY** (dc, 2026-08-26 19:02Z): dc's fixture ran the pair and never the keg, yet dc wrote "YOU MEASURED THE SHIPPED KEG; THE PAIR DOES NOT DO IT" -- a contrast asserted with one half measured and the other half quoted from me. Quoting the number launders the cause into a premise; restate a peer's figure with its cause marked as theirs and unverified, or measure the cause before it becomes a premise on your own board. The 3.0.1 note "run `intent upgrade` first" stands as ADVICE with no named mechanism, and gets a new sentence if one is ever established -- never a reason swapped under an old one.
- **CORRECTION OF MY OWN STAMPS, ON hv's ORDER (2026-08-26 19:07Z, read from `date -u`):** every "18:45Z" / "18:45:00Z" I wrote tonight for the Lamplight rewrite event was DERIVED -- `stat` printed the local mtime `19:45:00` (BST, +0100) and I subtracted the offset -- which is the sentence in `restart.md`'s clock rule verbatim: `stat` prints LOCAL, and a Z produced from it is a stamp that looks perfect and was never read. The mtimes are gone (the restore rewrote them), so the value cannot be re-read; it stands as "19:45:00 BST by `stat`", marked local, and every Z form of it above is retracted as a stamp. Two more of mine: "now ~19:05Z" (twice) and "QUIET ~20:30Z" were advanced by feel from an earlier read -- generator 1, arithmetic. hv's word: **the clock is 20:15Z for ic's floor AND the QUIET call, not moved**; the slack is withdrawn. **A future time I set is a deadline; a past time I did not read is a fabrication; and `TZ=UTC stat` was one line away.**
- **THE MIGRATOR CARRIED ROWS AND STRANDED THE PROSE OF EVERY BUCKETED THREAD** (2026-08-26 19:45Z): ~2,000 files fleet-wide (Lamplight 1,599 by `find`, not 1,249 by a name list -- lamplight-vc; Laksa 543; Baize 70; ...) sat only under the v2 status dirs; `Project::thread_dir` is flat, the migrator descends into buckets by `collect_legacy` and the collector does not -- two spellings of "where does a thread live" (cc), and AC-00.2 was measured on the one that works; the row accounting could not see it. Carried per file by the tool's own `st attach <ID> <rel> --from` on the pair (canon holds the bytes verbatim -- proven on a clone), `sync --to-disk` writes canon from the store (on some projects attach updates canon directly, on Prolix only the store -- verify from canon AFTER the sync, never right after the attach), `organize --apply` realises open threads, and a bucket thread dir is pruned only when every file under it is byte-verified or one of the three files hop 2 modelled; the set is derived by `find -type f -print0`, never listed by name; non-markdown files wait for a pair whose `st attach` carries them and block their dir's prune meanwhile. **"Delete the duplicates" was the obvious tidy-up and it was ~2,000 documents of data loss.**
- **hv'"'"'s TWO RULINGS, THE SAME HOUR** (2026-08-26 19:45Z): "THERE IS NO 3.0.2. There is 3.0.0 which we have made and then EVERYTHING ELSE IS GOING INTO 3.0.1" -- every deferral filed tonight is scope; and, on seeing 57 realised threads on Lamplight, "It should ONLY HAVE WIP STs!!!!!" -- the realised set is WIP only, and "open" was a definition by exclusion that swept in 41 planned threads. The flat views of every non-WIP thread came out fleet-wide by `dehydrate-closed.sh` (each removed dir checked against canon first: info.md, acceptance.md, WP/NN/info.md by `seq`, attachments); my session's classifier refused the first `git rm -r` of 296 dirs and hv's explicit go ("there is no risk; the only user is me") was the decision it asked for.
- **THE FLEET'S OPEN SETS AND BUCKET PROSE, TALLIED AT THE FOLD** (2026-08-26 19:54Z): `dehydrate-closed.sh` (WIP only, canon-holds by name then bytes) on 15 projects + Lamplight by hv's hand; `ingest-buckets.sh` carried and byte-verified 36 (Cdsync), 71 (Baize; 4 refused by extension, 1 dir kept), 32 (arca_notionex; 1 kept), 29 (Utilz), 20 (Anvil; 1 kept), 23 (arca_cli), 15 (Riffle), 11 (MicroGPTEx), 29 (Courses), 3 (Molt), Prolix 153 by `ingest-flat.sh`; Laksa's 543 ran past a ten-minute foreground budget and again in the background; Devbin's 54 were MOVED to `intent/history/` by hv's choice before the carry reached it; Lamplight waits for 2b's store verbs. `st attach` writes canon directly on some projects and only the store on others (Prolix) -- verify canon AFTER `sync --to-disk`, never right after the attach. **A five-name count (design/impl/tasks/done/restart) undercounted every project; the set is `find -type f` minus the three modelled names, and devbin-vc's 22 bucket-only files against my "6" was the same blindness lamplight-vc had already named.**
- **THREE CLASSES OF LOST CITATION, 19 ROWS FLEET-WIDE** (cc's offline detector, 2026-08-26 19:54Z): `+` as a separator (12; depth-aware fix building), ranges `AC-01.1..01.5` (6, all Lamplight ST0275; RULED refuse by name -- the tool invents no citations), prose between citations (1, Laksa; a named unreadable span). The detector counts ids at bracket depth 0 only (nine qualifier mentions were false positives) and its contract is "two readings of the same line disagree", never "covers is correct" -- a class both readings are blind to passes green. **A missing coverage link is invisible by construction: the AT stays green and the criterion computes from whatever else covers it.**
- **TIMES ADVANCED BY FEEL, AGAIN, INSIDE THE HOUR hv NAMED IT** (2026-08-26 19:54Z): my notes carried "20:02Z", "20:07Z", "20:10Z" while `date -u` read 19:5xZ. Every time in a message is read in that message or it is not written. And a zsh parse error in a long quoted one-liner ran NOTHING and printed one line -- edits go in a script file with a quoted heredoc, then run.
- **A PREDICATE NARROWER THAN ITS SUBJECT, THIRD INSTANCE OF THE DAY** (devbin-vc, 2026-08-26 19:58Z): I described the lost-citation shape as `-- covers AC-13.2 + AC-13.3 --`; all 26 of Devbin's multi-citation rows use a comma and all migrated intact (0 short of 436), so an arm keyed on `+` would report Devbin clean and measure nothing -- after the `status:`-comma survey in the morning and the `treeindex` key on the skills clobber in the evening. The arm now counts the ids in the clause whatever sits between them (self-test 22/22, a fixture on which a broken arm reads clean and a comma row must NOT be reported). Devbin's clean 26 constrains the fleet's 14 losses to the non-comma shapes. And ST0002's zero tests is NOT PROBEABLE, proven from the ref with ST0001 as the positive control -- the honest line, printed by the arm itself.
- **A ONE-PROJECT INSTRUCTION IS NOT A STANDING RULE, AND QUOTING IT AS ONE LAUNDERS ITS SCOPE** (vc, 2026-08-27, hv reversed it). hv said, once, of one project: _"go on Anvil, order as proposed, move buckets to history."_ Six files, one thread. I wrote it onto my own board as **"Standing rulings: buckets -> `intent/history/`"** and into this runbook as step 2 of the procedure for every project. **415 thread directories went into seven projects' done-log archives, and every commit cited "(hv's ruling)" for a ruling hv had never made.** `intent/history/` is hv's own April-2026 convention for `YYYYMM-done.md` and nothing else -- Intent's own tree held 18 done-logs and zero thread dirs throughout, which was the available control I never looked at. **A generalisation needs its own ruling; the citation must name the scope it was given, and "hv said X about Anvil" is not "hv ruled X".** Reversed across all seven from git's own rename records, 4,654 files, every one R100. Compounded by the bucket-blind walk: the prose moved there was never carried into canon, so 1,819 files sat outside the SSOT.

- **THE BUCKETS ARE AN INCOMPLETE RECORD OF WHAT A PROJECT ONCE HELD; HISTORY IS THE SOURCE** (lamplight-vc, 2026-08-26 20:00Z): scanning the surviving bucket dirs gave Baize 0 multi-cover rows and Lamplight 207; from the v2 source at the migration commit's parent, Baize has 15 and Lamplight 245 -- a denominator that is too small makes a clean result look more informative than it is. Headline carried in the shape it was measured: **14 confirmed lost citations across 11 MEASURED projects (Lamplight 13 of 245, Laksa 1 of 52; Baize/Riffle/Utilz genuinely clean; Prolix/Cdsync/Courses/MicroGPTEx/Molt structural zeros -- no multi-cover rows, so no evidence either way), 4 UNMEASURED at the time (Anvil, Devbin, Intent had no `migrate to v3` commit to key on; the ref rule is the parent of the first commit adding `intent/.canon/`; Intentv2 is the frozen v2 checkout and not a subject).** Two positive controls (Lamplight 13, Laksa 1) matched the independent bucket scan by a different route, which is the only reason the zeros are stated. Fixtures for cc: `Lamplight/intent/whiteboard/vc/.history/20260826/covers-drop-fixtures.md` (each ROW verbatim; the counts are not the fixture); fleet table + scanner beside it in `covers-drop-fleet.md`. Sharpest fixture: Laksa `ST0086 AT-10.1`, four criteria named, canon kept three, `AC-00.1` dropped from the MIDDLE behind "and retrospectively guards" -- not truncation, not a separator; a property of the id's immediate context. **And the same correction applies to the bucket-prose carry: whatever the file count turns out to be, it is a floor, not a total.**
- **A DIRTY TREE IS REDIRECTED, NEVER BLOCKED -- AND THEN THE THREE READS ARE TRUE ABOUT THE PREVIOUS BUILD** (dc, 2026-08-26 20:01Z): `releasebuild.lib:53` sends a dirty `native/rust` build to `target/private/release`, stamps it `dirty-<sha>`, and `verify_pair` passes it as expected; the SHARED `target/release` -- the only dir `intent3`, `int macos prepare`, `cmd/cli` and `cmd/hosting` ever read -- keeps the last build's binaries. A 2b fired on a dirty tree would leave 2a's stamps in place and every read green. The day's class aimed at the lift: the evidence true, the subject the previous build. Closes two ways, both in place: the QUIET goes only on `quiet.sh` reading 0 dirt paths in the same minute (never on "peers are holding" -- held commits are what dirty looks like), and dc reads the pair from the dir the build's own redirect line names and asserts an mtime AFTER the build started -- the one property a stale artefact cannot forge. A binary reading 19:27:35Z at 20:15Z is 2a whatever its marker says.
- **AN INSTRUMENT LOOSER THAN THE GATE IT FRONTS, AND A GATE NOTHING ROSTERS** (dc, 2026-08-26 20:04Z): `quiet.sh` read dirt under `native/rust` alone; the build guard's declared scope (`sharedtarget.lib` `SHARED_TARGET_DIRT_SCOPES`) is `native/rust` AND `surface`. At the exact end-state I was steering toward -- ic's floor parked, `surface/dispatch-table.json` left dirty -- `quiet.sh` would have said QUIET, dc's same-minute drive of the same file would have agreed (two reads, one hole: agreement is not independence on the axis both share), the build would have REDIRECTED to `target/private`, and the shared pair would have stayed 2a under three true green stamps. Moot at the moment of finding (7 paths dirty, both scopes non-zero) -- which is the trap: it bites only at the state you are steering toward. Widened to both scopes with a positive control on the current tree; the file's own header now says its copy of the scope is hand-held, because `grep -rl quiet.sh` across `bin/`, `lib/` and `intent/st/` returns nothing -- the gate that decides whether a release build may proceed is referenced by no roster, no test and no doc, and its copy had already drifted. **A hand-copied scope drifts in the loose direction and nothing reports it; the copy is the entry.** Roster entry: 3.0.1 scope.
- **A CLEAN-TREE WORD AGES IN MINUTES; THE QUIET READS THE TREE, NEVER THE WORD** (ic, 2026-08-26 20:18Z, own account): a true "both scopes 0" at 20:13Z, `views.rs` copied back in at ~20:14Z without a follow-up, a `cargo test --workspace` started in the shared tree with that file dirty and left running as "void", the file reverted underneath it at 20:16Z. Three of my reads said NOT quiet while the word said quiet. ic's rule, adopted: a clean-tree word is a HOLD on the speaker's own tree until they say otherwise, and "I need the tree" is said BEFORE the touch.
- **THE CALL GOES FIRST WHEN THE FLOOR LANDS WITH TWO MINUTES LEFT** (vc, 20:19Z): the fold said "mutations, then quiet.sh, then the call" and assumed the sha would arrive early; at 20:13Z that order would have moved hv's clock by twenty minutes. The verification ran beside the build in a worktree with a private target -- it cannot touch the shared artefact -- and the stamp was written as bytes, not proof (dc). Two things that do not depend on each other do not wait for each other.
- **A MUTATION PASS THAT RETURNS ALL-ARMED SAYS NOTHING ABOUT WHICH GREENS ARE LOAD-BEARING** (dc, 20:27Z; the harness, 20:21Z): two of ic's six proofs unarmed. `force_without_a_tty_...` asserts the OUTCOME (nothing written, rc 1) and the EOF confirm produces the same outcome without the guard; `default_removes_no_file_...` asserts survival across a run that never acted (the fixture's manifest is present after `init` + `st new`, so `--default` takes the no-op arm). Same shape as `quiet.sh` reading one scope and as dc's control B (a swallowed read verb leaves the tree clean too). Three sightings in one evening in three people's work, none found by review.
- **`~/.local/bin/intent` IS A SYMLINK INTO THE SHARED BUILD OUTPUT** (vc, 20:26Z, by elimination): 252 attach refusals in ONE contiguous block (refusal lines 242-493) during dc's 66-second 2b build; the binary did not exist and then it was a different pair, and the run's own header ("pair 43c3d2d0") was true at the start and false at the end. 0 of 252 names carry a space/paren/quote (lamplight-vc's lead, refuted by count). Lamplight's standing decision #5 ("the mitigation is attentional") fired for the first time; the mitigation that worked was a PINNED COPY in the scratchpad that no build can replace.
- **A GREEN FROM AN INCREMENTAL OPERATION IS A CLAIM ABOUT WHAT IT TOUCHED, NEVER ABOUT WHAT IS TRUE** (lamplight-vc's naming, 20:36Z; driven by vc 20:30Z): the bare `sync --to-disk` correctly did nothing to threads it had not touched and reported ok; 238 store attachments stayed out of a canon extract git had reverted, and `sync --to-disk ST0002` wrote all four on the spot. Doctor flags todo.md view-skew and has no canon-vs-store arm. The prune gate (canon bytes vs disk bytes per file, never the verb's rc) is why the 238 stayed in place. cc, 3.0.1.
- **A FINDING THAT A MIGRATED VIEW READS WORSE THAN ITS SOURCE IS ABOUT THE MIGRATION UNTIL THE SOURCE HAS BEEN READ** (lamplight-vc, 20:0xZ, retraction): ST0346 "passed on two-thirds of its contract" was backwards -- v2's 24/24 excluded 8 withdrawn + 5 descoped from both sides; v3's 25/37 is those dispositions arriving as prose. The gate accused for a week was the one behaving correctly.
- **A SCANNER THAT PRINTS AN IMPOSSIBLE ROW IS TELLING YOU ITS OTHER ROWS ARE SUSPECT** (lamplight-vc, 20:2xZ; cc, 20:14Z): `canon > source` on ST0238 AT-05.2 and `AT-06.1 n=2 vs canon 3` were printed by the scanner, labelled "my parser failing" and the table filed anyway; a one-sided `-gt` had thrown the evidence away. Two fleet tables wrong in an hour by the same move.
- **A HAND-WRITTEN APPROXIMATION OF A PARSER YOU ALREADY HAVE** (cc, 20:23Z): cc's "5" required an id in the 12 characters left of the `+` and missed the glossed shape `AC-03.1 (N=2 ...) + AC-03.2`; simulating the real cut (field_end bracket-aware, depth-0 split, leading-id-token per element, separators `,` then `,+`, diffed) found 12 = lamplight-vc's figure: 9 recover an id, 3 turn a silently swallowed span into a named residue.
- **THE THIRD MIGRATION DEFECT IS TWO FACES OF ONE GRAMMAR GAP, AND THE ARMS ARE NOT INDEPENDENT** (cc, 20:14Z-20:23Z): `legacy.rs:1275` literal `(non-test)` prefix; `satisfied` evaluated at 1264 and dropped by the test arm (39 rows on Lamplight, not 12 or 16); `withdrawn:` / `descoped-to:` never read (22/22 fleet-wide; `by`/`on` on every disposed line, nothing to mint); the 12 no-token rows read `satisfied: n/a` which maps to FAILURE, so arm 1 alone moves ST0346 away from 24/24. Refuse, never reclassify: 21 of the 39 have no evidence field and a grammar widening would push them into hard unsatisfied. The class is unknown-field residue; `is_criterion_id` accepts `AC-0.2`.
- **ST0326 AC-09.3 IS NOT A DEFECT AND LENGTH WAS NOT A LEAD** (cc, 20:14Z): `-- evidence ` with no colon, 2,815 bytes of criterion text, the ratified no-evidence rule. Fleet arm 1 after the field-presence check: Lamplight 17 (was 19), Laksa 1 (was 16), Baize 0 (was 2). Half of what was handed over as a defect was the system working as ruled.
- **THE INGEST WALK REFUSES AN ESTATE FOR A PNG, ON THE PAIR THAT RETIRED THE ALLOWLIST** (vc, 20:31Z): `sync --to-store` on Lamplight at 03470c5a: 22 `unknown-file-shape -- not valid UTF-8` under two completed threads' dirs and `intent/docs/notes/`; it is the only door into Triage. The opaque-arm bullet above (cc's `classify`) has a second site on the ingest side. Held for 3.0.1; the four `_inbox` threads read `triage` in canon and NotStarted in the store, dehydrated either way.
- **`stat -f '%Sm'` PRINTS LOCAL, AND THE LABEL IS WHERE THE CLOCK ERROR HIDES** (dc, 20:24Z, own catch): 0b's mtimes rendered `21:23:12Z` / `21:22:11Z` on the first pass -- a `Z` appended to a local reading; the epoch comparison was sound, the label was wrong, caught before it left dc's hands. True: 20:23:12Z / 20:22:11Z.
- **hv's `intent todo` PARITY RESTORE IS A MERGE, NOT A REVERT** (ic, 20:20Z): the pre-D44 renderer emitted work packages as flat label strings; the nested `- [x] 01: ...` tree landed after D44, and hv wants both (v2 behaviour). HEAD's nesting kept, the watermark grafted, five conflict hunks; ic's two inventions (collapsed WPs, a "not shown" footer) rejected by hv -- "No hallucinated new or less functionality. I WANT PARITY."
- **THE 252 ARE `guarded_release_build`'s ABSENT WINDOW, NOT THE CARRY'S CHOICE -- AND FOR 61 SECONDS THE PAIR WAS HALF PRESENT** (dc, 2026-08-26 20:40Z, mechanism driven from source and 2b's own output; the count is vc's): `releasebuild.lib:80` `cargo clean -p intent-cli -p intentd --release` deletes the file `~/.local/bin/intent` points at ("Removed 49 files, 27.8MiB"), intentd is recreated 5s after the clean and intent 66s after. A dangling symlink for 66s, a pair that answers "is it there" yes-and-no for 61s, and a half-present pair looks like a working install to anything that checks only intentd. Filed as the consumer's fault the remedy is "pin your binary" and every other consumer keeps the hole; filed as the producer's, the remedy is the STAGING-DIR BUILD (build private, verify, move into place) and it closes for all of them at once. Carried to hv as MEASURED -- dc's queued item (2), theoretical until tonight.
- **CANON IS THE COMMITTED EXTRACT; THE LIVE STORE IS `intent/.cache/intent.db`** (devbin-vc, ~20:36Z, correcting their own earlier account to hv): editing canon does nothing until a sync, which is why a 5,167-char body sat visible in the file while `issues show` read length 0. "A file you can see and a tool that reports zero from it" is the sharpest form of the class. And the only way to land ONE authored field was a whole-store replace, because `sync --to-disk <id>` resolves ids as steel threads only (cc, 3.0.1).
- **A STAMP WITH A `Z` THAT IS AN HOUR AHEAD OF THE MESSAGE IT ARRIVES IN** (devbin-vc, "21:35Z" received ~20:36Z): the local clock with a Z appended -- the exact class the protocol names, in a message not a board, flagged to its author rather than corrected.
- **THE STAMP'S SUBJECT IS THE BUILD'S INPUTS, NOT `native/rust` -- AND THE REMEDY CARRIED THE DAY'S CLASS** (dc, 2026-08-26 20:47Z, found before a line was written): `dispatch.rs:45` `include_str!("../../../../../surface/dispatch-table.json")`, so `surface/` is an input; 41 of the last 50 commits touching `surface/` do not touch `native/rust`, and a stamp asked over `native/rust` alone would give two different binaries one provenance -- a collision `rev-parse HEAD` cannot produce today. Corrected, in ONE commit when dc lands it: (1) `DIRT_SCOPE` in `source_commit.rs` widened to both paths (closes queued item 5: a build dirty only in `surface/` was never stamped `dirty-`); (2) identity = `git rev-list -1 HEAD -- <DIRT_SCOPE...>` over that same scope; (3) `releasebuild.lib:105` reads `"${SHARED_TARGET_DIRT_SCOPES[@]}"` from the lib -- no third hand-held copy of the scope; (4) arm 6 stays CONTAINMENT, never equality. **THE MARKER'S MEANING CHANGES ON THE NEXT BUILD AND THIS LINE IS WRITTEN BEFORE ANYTHING CONSUMES IT:** after the fix, two repo states with identical `native/rust` + `surface/` carry the SAME `[intent-source-commit:...]` (a whiteboard-only commit no longer moves the stamp), and a dirty `surface/` stamps `dirty-` for the first time. Pin by sha256, never by marker (self-provenance, 2026-08-18) -- unchanged.
- **`git rev-list -1 HEAD -- <scope>` RETURNS EMPTY AT rc 0 WHEN NOTHING TOUCHES THE SCOPE** (dc, 20:47Z, driven): `emit_source_commit`'s `None => "unknown"` fires only when git FAILS, so the corrected fix would have emitted `INTENT_SOURCE_COMMIT=` and a marker reading `[intent-source-commit:]` -- an empty stamp that passes every arm. Needs `Some(sha) if sha.is_empty() => "unknown"`. An empty claim is not a smaller claim; it is a broken one.
- **A PINNED COPY DECOUPLES A LONG RUN FROM THE BUILD, AND THAT DISSOLVES A HOLD** (vc, 20:49Z [STAMP FABRICATED -- typed by vc, not read; unverifiable; the write landed in cb469041 at 20:39:59Z UTC by git's own clock]): dc's stamp fix was held "until the sweep completes on 03470c5a" because the sweep exercised the shared pair; with every carry now running `scratchpad/pair-03470c5a/` (sha256 7d26ff62..., marker 03470c5a) the next build changes nothing the sweep reads, so the hold has no subject and dc is released to land + build under the QUIET protocol. A hold names what it protects; when that thing stops existing the hold is a habit.
- **THE AUDIT INSTRUMENT BUILT OUT OF THE ERROR** (devbin-vc, ~20:45Z): `git log --date=format-local:'%H:%MZ'` -- a literal `Z` over a local-time date -- printed `21:35Z` for a commit made at 20:35Z, reproducing the stamp error it was run to check. The fifth predicate defect of that node's day and the first inside the check written to catch the thing it got wrong.
- **THE FLEET `--default` SWEEP IS A VERIFICATION, NOT A WRITE, ON A DEHYDRATED FLEET** (vc, 20:38Z): 13 of 16 `present, unchanged, rc 0` on the pinned 03470c5a -- AC-11.2 live on every migrated project (a3-content 0, Anvil 1, arca_cli 0, arca_notionex 0, Baize 4, Cdsync 0, Courses 0, Devbin 0, Laksa 1, MicroGPTEx 0, Molt 1, Riffle 0, Utilz 0 declared); Prolix (3 paths), Molt-flynn (1), Molt-matts (1) REFUSED as dirty before the run. Every manifest already existed from the dehydration scripts, so bare `--default` had nothing to write anywhere.
- **THE FIRST EMIT AFTER A CLASSIFIER CHANGE IS A ONE-TIME REWRITE, AND IT LOOKS EXACTLY LIKE A STRAY WRITE** (cc, 2026-08-26 20:41Z; store read by vc): ST0056.json 94 -> 292 attachments and ST0057 + `parity/tools/.gitkeep`, every new row `written_at 20:32:31` -- .tap/.tsv/.gitkeep files the old md/txt/sh allowlist excluded and 08e97eb3's cap-only carrier admits, surfaced by devbin-vc's whole-store `sync --to-store` on this estate while landing 0098. Two peers proved it was not theirs with controls before the store named it. Landed as canon catching up (ac88dbcf). **And the cost cc named at the cap is now visible:** 1 MiB per file, no bound per thread -- measure before the other sixteen take the step; on hv's board beside the blob-home decision.
- **118 OF 124 UNKNOWN AT KEYS SIT INSIDE `note`'s REGION, WHICH IS RULED TO HAVE NO INTERIOR STRUCTURE** (cc, 20:41Z, correcting cc's own earlier list before it hardened here): the AT scan for unread fields is bounded at `-- status:`; flagging `red-first:` inside a note would be the check inventing the keyed grammar the ruling exists to refuse, and a dedicated arm asserts it is NOT reported. Only 6 AT occurrences are genuinely unread (`requires` x2, `review` x2, `non-test`, `measured`); the AC side has no such region (51 of 54 before the first known field). Step (1) armed: `FindingClass::UnreadField`, `field_key` + `unread_field_keys`, emitted after the accounting window like `unreadable_covers`; 3 of 4 mutations reddening only the arm that names them.
- **TWO FABRICATED STAMPS IN FIVE MINUTES BY TWO MECHANISMS, EACH CAUGHT BY THE OTHER NODE AND BY NO MACHINERY** (devbin-vc, ~20:45Z, pairing their "21:35Z" with my typed "20:41Z"-"20:50Z"): theirs a local time with Z appended (the offset, exactly +1h), mine a number advanced by feel from the last read (drift, ~5-10 min). Check A tolerates 120 s, so BOTH would have been refused on a board; cross-session messages carry the same `## (timestamp)` ordering claims, are what `Re:` threads against, and sit under none of the three guards. Filed as ISSUE 0099 (its own, not a section of 0098 -- filing it there would be the thing 0098 complains about). My rule from here: a message carries a clock value only when one is in front of me from this turn, verbatim, or none.
- **A VIEW THE PAIR RENDERS WITH TWO TRAILING LINES AND A FORMATTER THAT STRIPS THEM ON COMMIT** (vc, Prolix, 20:43Z): the pre-commit formatter rewrote `intent/st/ST0025/WP/03/info.md` during my commit (5cbb610), so the committed bytes are the pair's and the worktree's are the formatter's, and a `git checkout` from HEAD does not clear it. The second-writer wart (memory: the markdown formatter between sync and commit), left in place and named; Prolix is not on the critical path.
- **A CARRY THAT PAYS A PROCESS START AND ~2 MB OF STORE PER FILE IS NOT A CARRY, IT IS A LOAD TEST** (vc, 2026-08-26 20:48Z, measured on Laksa): `st attach` per file ran 35/min on an idle box and 4/min under a load average of 39.85 (two shared-tree cargo runs, 16 rustc); `intent.db` grew 1.87 -> 2.46 GB plus a 475 MB WAL across ~266 attaches of files a few hundred bytes to a few KB. Lamplight's 3,738 by the same path is ~7 GB and hours. The per-file verbs proved the mechanism on 11 projects; the fleet-scale carry is the 3.0.1 walk's job, in one process.
- **ONE COMMIT IS NOT ONE MOMENT: THE GATE READS THE WORKTREE, SO THE ATOMICITY THAT MATTERS IS IN THE WORKTREE** (dc, 2026-08-26 20:52:06Z, own block): the stamp fix touched a constant, a lib and arm 6's live reader in three tool calls; between call one and call three every node's commit was refused by a guard correctly reporting it could not read the constant (`shared-artefact-guard: arm 6 -- could not read DIRT_SCOPE`). "Both sides move in one commit" was already on dc's board and was insufficient. For a change spanning a declaration and its live reader: one write, or park the first half until the second is ready. Third instance of the live-read class tonight (dispatch-table.json, quiet.sh's scope, this).
- **A GUARD SCRIPT THAT IS A RECORDED ATTACHMENT MAKES CANON NAME A SHA IT NO LONGER CARRIES** (dc, 20:52Z): editing `intent/st/ST0056/parity/tools/shared_artefact_build_guard.sh` tripped `canon_commit_check.sh --staged`; the remedy was `intent sync --to-disk ST0056` -- SCOPED BY ID, never the bare form -- which changed exactly three lines (text, bytes, sha256), committed WITH the files in one commit. Commit-then-resync leaves the commit divergent in history permanently; `--no-verify` was not used.
- **A NEGATIVE IS PROVEN BY A COMPLETE RECORD, NOT A DENIAL** (dc, 20:52Z): asked whose the two heavy cargo runs were, dc answered with its whole cargo record for the night (one `int local build` 20:22:06Z-20:23:12Z; one `rustc` on a four-line `include!` harness, no cargo, no lock) and the observation that both runs were still live after dc had started nothing. cc's answer decides between cc and hv's own terminal.
- **`"$var"` UNQUOTED UNDER zsh IS ONE ARGUMENT, FIFTH TIME, IN THE COMMAND THAT MATTERED MOST** (vc, 2026-08-26 20:59Z): `c cc "..." $CC` handed my commit function the five paths as ONE pathspec (`fatal: pathspec ' native/rust/... intent/whiteboard/cc/...' did not match any files`) at the moment hv wanted the tree clean for a build. Mechanism beats a note: every path is written out, or the list is an array expanded `"${arr[@]}"` inside a bash script file -- never a space-joined string in the zsh tool shell.
- **A RED COMMIT WITH ITS FAILURES NAMED BEATS A GREEN DELAY IN FRONT OF THE HUMAN'S OWN RUN** (ic, 2026-08-26 ~21:00Z, `a8fc134b`): three restored watermark tests fail because ic's comment asserted a limit (events.jsonl left the working tree at D53) that the tests disprove (`export.rs` still ships it); committed red on purpose with the three names in the message, minutes before hv's own full suite. Same shape as the afternoon's `Plan::run` comment: a comment that asserts a limit is a claim the tests get to vote on.
- **TWO HANDS ON ONE SET COLLIDE IN `.git/index.lock`, AND THE SECOND HAND'S `git add` HALF-STAGES BEFORE IT LOSES** (vc, 20:58Z): committing ic's set under hv's order while ic was committing it themselves -- my `add` staged three of ic's files, then the lock refused; ic's commit landed; mine found nothing left. A set in a live peer's hands is committed by that peer; hv's "everything committed" is satisfied by ANY hand, and the right hand is the one already on it. Hand it back with a deadline instead of reaching for it.
- **A DIRTY TREE AT hv's BUILD START IS THE ONE THING THE PROTOCOL EXISTS TO PREVENT, AND "EVERYTHING COMMITTED" IS ITS SPELLING** (dc, 20:5xZ): `guarded_release_build` on a dirty native/rust or surface takes `refuse:` -> redirects to `target/private/release` -> stamps `dirty-` -> `verify_pair` calls it expected at rc 0, and the shared pair stays at the previous build. A human's "clean build" on a dirty tree reports success and changes nothing they can reach. And since `d6ddb874` a dirty `surface/` alone stamps `dirty-` for the first time ever -- announced to hv before the build so a first-ever refusal does not read as breakage.
- **A COMMIT TITLE THAT NAMES WHAT A PEER'S COMMIT CARRIED** (vc, 2026-08-26 ~21:03Z, `fd7bd958`): cc committed their own set at `a1af59f3` seconds before my gated commit of the same paths; mine found only cc's board and archived fold to carry (219 lines) and went in under a title claiming the residue class. No divergence -- `legacy.rs` hashes `e481879f` at both shas and in the worktree (cc's `d6199fc7` plus one rustfmt split of a boolean) -- but the title overclaims and the stat is the record. A commit's title is written from the stat, not from the intent.
- **`AC-0.2` COMES OFF THE 3.0.1 LIST AS A SEPARATE ITEM** (cc, 21:04Z, against cc's own filing): `acceptance()` already emits `covers {id}, which is not in this contract` as `BrokenReference` for any covered id absent from the thread's criteria; the `+` fix simply lets that id reach the check it already had.
- **FIVE COMPILE STOPS NAMING `RenderContext.todo_watermark` IN `intentsvcs` TEST TARGETS** (cc, 21:04Z, 0 hits in cc's diff): seen while ic's restore was half-copied into the shared tree. If hv's full suite on HEAD shows them, they are `a8fc134b`'s (ic's follow-up); if not, they were the half-copy. Either way the reading is hv's suite, not a worktree's.
- **"CLOSE THOSE SHELLS" MEANS THE LONG RUN TOO, AND ITS PARTIAL STATE IS COMMITTED, NOT REVERTED** (vc, 21:07Z, hv's order): Laksa's carry stopped at the attach phase; on this pair `st attach` writes canon directly for the thread it touches, so the extract already reflected the store for every thread reached -- reverting it with git would have re-created the bare-sync trap (238 files, 20:30Z). Committed as partial, nothing pruned, resume idempotent per file on a pinned copy.

- **A BACKGROUND JOB'S EXIT CODE IS THE WRAPPER'S, NOT THE WORK'S -- THREE HARNESSES, THREE NODES, ONE NIGHT** (vc, cc, ic, 2026-08-27 00:31Z). My Laksa commit was reported "completed (exit code 0)" and the commit inside it returned **rc=1**; ic had two workspace runs reported as 0 carrying `TEST_RC=101`; cc's runner printed `EXIT=0` having executed nothing (`local tag="$1" log="$S/..."` -- bash expands every word before `local` runs, so `$tag` was unbound under `set -u` and the function died at its first line). **The common cause is a trailing `echo`: it becomes the script's last statement and launders any failure into a success.** The fix is one line -- end the wrapper with `exit $rc` so the reported code IS the work's. Nothing else about the three harnesses was alike, which is what makes it a class rather than three bugs.
- **A GUARD THAT READS ADDED LINES CAN STILL READ A RENAME AS 100% NEW PROSE** (vc, Laksa, 2026-08-27 00:05Z). `hv-attribution-guard.sh` refused the bucket carry with **292 findings over 110 files, 110 of 110 inside `intent/history/`, none written by the commit**. Two independent causes: (i) the loop takes `--name-only --diff-filter=ACMR` and then runs `git diff --cached -U0 -- "$f"` per file -- **a pathspec that excludes a rename's SOURCE destroys the pairing**, so git renders the destination as a wholesale add; measured across the 831 staged renames, **136,516 phantom added lines, and 0 once both paths are passed**. (ii) The guard's archive carve-out already says _"an archive must never be blocked"_ and matches `*/.history/*`, the whiteboard's archive -- **hv's bucket ruling named `intent/history/`, no dot, so it misses by one character.** The guard is wrong here by its own project's stated principle: its sibling clock guard never blocks on pre-existing breakage, "because a guard that must be bypassed to work is a guard nobody keeps." **Fix written, NOT APPLIED -- the permission classifier refused the edit, correctly, because patching a pre-commit gate is indistinguishable from disabling one. Held for hv.**
- **`cargo build --release` LEAVES HALF THE PAIR STALE, AND A TWO-HALF `--version` CHECK CANNOT SEE IT** (vc, 2026-08-27 00:28Z). After building at `9bd6b0a3`, `intent` stamped `f438d0d5` and `intentd` still stamped `cd032508` -- **intentd's crate had not changed, so cargo did not rebuild it and its baked provenance stayed at the previous build's commit.** `bin/intent3` refused with _"the pair disagrees ... built from DIFFERENT trees"_, which is the guard working. **The trap is that my own pin check could not have caught it: `intentd --version` prints `intentd 3.0.0 -- not yet implemented` and NO commit at all**, so reading `--version` off both halves looks clean and only `strings` or the wrapper's cross-check sees the disagreement. `int local build` exists for exactly this and says so while running -- _"forcing the provenance embeds (cargo will not do this for you)"_. **Never pin a pair straight out of `cargo build`; pin out of `int local build`, and verify by the wrapper rather than by two `--version` calls.**
- **`intent st list` OMITS TRIAGE THREADS SILENTLY, AND IT WAS ONE OF MY PORT READINGS** (ic flagged, vc measured the fleet, 2026-08-27 00:24Z). A header row and no rows; no `--all` and no `--json` exist to reveal them. Reproduced on `fx13`, a fixture built end to end by the v13 binary, so it is pre-existing rather than WP-14's. **Fleet exposure, read directly off every store: Intent 4 of 64, Lamplight 4 of 353 (the `_inbox` ST0339-0342), every other estate zero** -- bounded at 8 threads across 2 estates. Intent's four include `ST0062 "help"` and `ST0063 "start"`, which are **a mistyped subcommand becoming a steel thread** (`intent st new help` where `intent st --help` was meant), swept in under `9d717901`. Two defects, not one: a status the primary list verb drops without saying so, and a `new` that accepts a subcommand name as a title.
- **CORRECTION, MINE, CAUGHT BY ic BEFORE IT REACHED hv: "NOT ON DISK" IS NOT "NOT IN CANON"** (vc, 2026-08-27 00:29Z). I reported `ST0060` as a store-only row with no disk presence, under my own claims -- which reads as data loss. **It carries 75,906 bytes of canon, the largest of the four, and store and canon agree exactly at 64/64 with nothing in either direction.** What I had measured was the absence of a generated VIEW directory under `intent/st/`, which for a triage thread is the rule working: the realised set is WIP alone. **I counted the wrong artefact and reported the harmless absence as the alarming one.**
- **THE REALISED SET AND THE RULE HAVE BEEN APART FOR A WHILE, AND ONLY ONE INSTANCE WAS SELF-INFLICTED** (ic, 2026-08-27 00:29Z). Of ten realised directories in Intent's own tree, **six are non-WIP** -- `ST0055` completed, `ST0059` hold, `ST0046` not-started, `ST0062`/`ST0063`/`ST0064` triage -- and only `ST0055` came from tonight's `wp start`. When the realisation hole was first handed over it had a single self-inflicted instance, which made it fair to suspect the instrument; **five predate it, across four statuses.** Nothing in the normal path retracts: `--default` only ever adds. `.intentfiles` and the realised set agree exactly at 10, so this is not declaration drift -- the declaration faithfully records a set that should not exist.
- **A CONTROL IS CONSUMED BY BEING USED AS A SUBJECT, AND NOTHING ABOUT IT SAYS SO** (ic, against itself, 2026-08-27 00:26Z). ic offered a v13 estate as the v13 half of a two-sided control; `PRAGMA user_version` on it read **14**, because ic had reused the same directory as the SUBJECT of a `sync --to-disk` case in the same bash call. **Its v13-ness was its entire value and is the one property nothing displays** -- not the filename, not a listing, not `git status`. The operation is one-way. Read the defining property at CITATION time, not at construction time; and read it off a COPY, so the read cannot alter the thing read.
- **THE v13/v14 STRADDLE IS REAL, AND THE REFUSAL IS CLEAN** (ic + vc, 2026-08-27 00:22Z). Once a v14 binary walks a store, a v13 binary refuses it outright -- _"the runtime store holds schema version 14; this build of intent speaks 13"_, rc 1, naming the right direction. **So a schema bump landing mid-port must land BEFORE the pin, not after**, or the pinned pair dies against the first estate anyone else has touched. Two-sided control, both halves required: the new pair must WALK a v13 store (rc 0, `git status` blank) **and** the retired pair must REFUSE the walked one -- a pair that refuses everything passes a refusal-only check, and one that walks everything passes a walk-only check.
- **RUNG 14 IS STORE-ONLY WHERE `intent/.cache/` IS IGNORED -- AND `intent init` LAYS DOWN NO `.gitignore` AT ALL** (ic measured the mechanism, vc measured the fleet, 2026-08-27 00:15Z). Verified across **21 estates including the nested ones** (`Arca/arca_cli`, `Arca/arca_config`, `Arca/arca_notionex`, `A3/a3-content`): every v3 estate ignores both `intent/.cache/` and `intent/events.jsonl` and none tracks the DB. **The one exception is Conflab, and only because it is unmigrated** -- so both entries go into its `.gitignore` BEFORE its first v3 write, or the store is created already tracked and the next `git add -A` puts a binary in its history.
- **`intent/.canon/project.json` IS ON THE PORT'S CRITICAL PATH, NOT IN A TIDY-UP SWEEP** (ic, 2026-08-27 00:15Z). One new untracked file per estate on `sync --to-disk`; carries `{"schema": "intent/project@3.0", "todo_watermark": ...}` where the estate has flushed, and the bare schema line where it never has (safe: it wins over a store whose value is also None). **Until it is committed, that estate's DONE cutoff lives only in this machine's store, so a clone loses it** -- which is D53 again by another route. Thirteen estates owe it; Intent's own tree has it.
- **A `git worktree` SILENTLY LOSES FOUR GUARDS** (ic, 2026-08-27 00:01Z). `.githooks/pre-commit.intent` is GITIGNORED, so a worktree never receives it and the clock guard, both whiteboard guards and the append-only guard are all inert. **This is the `int hooks` fresh-clone hole reached through a second door**, and it matters more than it looks because boards here tell nodes to verify in a detached worktree. Repro: `git worktree add <dir> <branch>`, then commit in it. The hook refuses rather than running without them, which is the only reason anyone finds out. Fix is a `cp` of the gitignored body into the worktree.

- **FIVE ANSWERS TO "RUN THE INTENT CLI", IN ONE TREE, AND THE ONLY GUARDED ONE WAS THE ONE NOBODY USED** (hv's ruling, vc + cc + dc, `c88679fd`, 2026-08-27 09:05Z). `int cli` took whichever of target/debug and target/release had the newer **mtime** and applied no guard; `bin/intent3` took release only and carried the ONLY pair-coherence check; `.envrc`'s `PATH_add` made a bare `intent` mean the DEBUG build inside this checkout and release everywhere else; `int local install` minted a fifth, `$BINDIR/intent3 -> $RELEASE_DIR/intent`, routed through nothing (dc); and the frozen **Intentv2/bin was on PATH**, giving a SECOND `int` whose older `cli` verb prefers debug and ran a 21-August build stamped `3.0.0-dev` (hv found it by running `int cli --version` in Laksa and getting a different binary than the same command gives in Intent). **Which one you got was an accident of where the shell started.** Now: `int cli` is the one path, carries the guard, runs release by stated choice, `--debug` announces itself as unguarded; the wrappers and `.envrc` are deleted; `cmd_install` mints `intent`/`intentd`; `Molt-matts e834d2a` takes Intentv2 off PATH. **`cmd_status` still SCANS the retired names deliberately** -- a machine that ran the old installer still has them and must still be told.
- **A PIN IS RETIRED BY A SCHEMA BUMP, AND THE EXPOSURE IS "ABSOLUTE PATH **AND** WRITES"** (dc hit it, cc tightened it, 2026-08-27). After WP-14 took SCHEMA_VERSION 13 -> 14, every v13 binary refuses every write verb against a walked store -- loudly, naming the direction, which is the good version. **The exposure is NOT "pinned binaries": reads are fine, and anything driving `Fixture::new()` is schema-independent entirely** (cc's mutation arms build a throwaway estate and pass identically at 12, 13 or 14). **vc sequenced the bump and left a stale "use the pinned pair" instruction standing for a peer, having been shown that exact refusal as a control hours earlier.** A pinning instruction has a lifetime and the event that ends it is the one you scheduled yourself.
- **"PIN THE PAIR OUT OF TREE" AND "HOP 3 WORKS" ARE IN DIRECT CONFLICT** (vc hit it, cc conceded the remedy, 2026-08-27). A copy parked in scratch cannot run `claude upgrade --apply` at all: it walks up from the exe for `lib/templates/` and refuses at rc 1, and the failure reads as a broken pin rather than a misplaced one. `$INTENT_HOME` cannot rescue it -- v3 **never reads it** (AC-11.3, tested by `no_intent_home::the_shipped_surface_reads_exactly_one_environment_variable`). The resolution satisfies both: gate coherence ONCE with `int cli --version`, then use the in-tree release path. **Written into `reconvert.sh` as a comment, because the constraint is invisible and the next person re-breaks it.**
- **A GUARD THAT STRIPS BACKTICKS AND NOT QUOTES READS PROSE ABOUT A COMMAND AS THE COMMAND** (cc, 2026-08-27, blocking four nodes). `shared_artefact_build_guard.sh` arm 10 strips backticked spans before matching and strips nothing else, so `'cargo build --release'` inside a die message counted as a release build. The guard's own header calls this **the estate's oldest instrument defect and records meeting it twice**; this was the third, through the one prose form the earlier fix did not cover. **A SECOND, QUIETER HOLE IN THE SAME ARM, dc's to rule:** its escape hatch is FILE-scoped (`grep -q 'clone_workspace\|CARGO_TARGET_DIR\|...' "$f" && continue`) while its subject is LINE-scoped, so a genuinely unguarded release build in a file mentioning any of those tokens anywhere passes in silence. **And cc's own note on why it was caught at all: the signal survived because the gate blocked cc's commit. The silent half inconveniences nobody.**
- **TWO NODES STAGED AT ONCE IN ONE INDEX, AND NEITHER PROCEDURE NAMES IT** (dc, 2026-08-27). dc's guard edit sat staged while vc's entire cutover was staged. `git commit --only` over dc's two paths was safe; **a bare `git commit` from either side would have swept the other's work into a commit whose message described something else.** dc re-read `git status` afterwards to confirm. The existing rule -- `git add <paths> && git commit --only <paths>` -- is what made it a non-event, and this is the concrete reason for it rather than the theoretical one.
- **A RECORDED ATTACHMENT MAKES canon THE SECOND WRITER OF ITS OWN SOURCE FILE, AND THE OBVIOUS COMMIT ORDER IS WRONG** (dc, 2026-08-27). `canon_commit_check.sh` refuses when an edited file is a stored ST attachment: canon then names bytes the commit does not carry. **Sync canon FIRST -- it reads the WORKTREE -- then commit file and canon together.** Committing first leaves that commit permanently divergent. An attachment is authored, so neither side can be re-derived; snapshot both before syncing, and check the diff introduced exactly the paths intended.

- **hv's DEVELOPMENT-MODE PRINCIPLE, AND IT INVERTS A DEFAULT THIS RUNBOOK HAD BACKWARDS** (hv's ruling, 2026-08-27, verbatim to vc). _"While we are developing and in development mode, we should aim to move to the latest builds of any dependency (across all langs) and then fix forward as we need. That should be a general principle. We should also favour consolidation into the simplest and most coherent structures we can."_ **So pinning-to-minimise-movement is the wrong posture for this phase** -- take the upgrade early and repair. The two things "latest" does not cover, and that a reader must still look for: **DROPPED packages** (dropped is not upgraded; it is a different event) and **major-version jumps**, which is where fix-forward becomes real work.
- **A LOCK RE-RESOLUTION WEARS A SUCCESSFUL CONVERSION'S CLOTHES, AND THE DEFECT IS THE SILENCE, NOT THE UPGRADE** (lamplight-vc found and measured, conflab-vc refined, vc over-warned then corrected, 2026-08-27). `cargo generate-lockfile` at a NEW workspace root does not dedupe -- it re-resolves the whole graph. On Lamplight: **116 packages moved version and 106 were dropped**, including `thiserror 1.0.69 -> 2.0.20` and `toml 0.8.2 -> 1.1.4`, at rc=0 with no warning that reads as one. **Under hv's principle above the upgrade is now DESIRED, so the recipe is not "seed instead of generate" -- it is "generate, and record what moved".** The diff against the union of all prior member locks is **the changelog, and it is the only load-bearing step**: fix-forward is executable only by someone who knows what moved. Two corrections worth keeping because both were mine: seeding from the largest member lock **measures OVERLAP, not method strength** (Lamplight's seed covers 488 of 594, which is why its re-measured 11 looked reassuring); and "absent from the seed" is **two populations** -- no pin anywhere (resolves fresh; Conflab 5) versus pinned at a different version (deterministically adopts the seed's pin; Conflab 37) -- only the first being a registry lottery. **Exposure is predictable from the lock files before running cargo at all.**
- **CONVERSION CREATES A NEW TARGET DIR CARRYING NO SPOTLIGHT MARKER, SILENTLY UNDOING THE MARKER WORK** (lamplight-vc, confirmed independently at Conflab). Lamplight's eleven old dirs were all marked; a new `native/target/` inherits nothing. Conflab's `native/cli/target/` and `native/daemon/target/` are marked and a new `native/target/` would not be -- on the estate that was the worst indexing offender at 61G. **Place the marker BEFORE the first build writes into the new dir**, and note the general form: marking a tree that is about to be DELETED buys nothing, because cargo recreates the directory unmarked. **The marker belongs on the trees that SURVIVE a clean.**
- **ENUMERATE CONSUMERS BY ASKING WHAT READS THE OUTPUT, NOT BY GREPPING FOR THE STRING YOU EXPECT THEM TO HOLD** (lamplight-vc, generalised with vc). Conversion moves the output dir, so every holder of the old path breaks -- and they are not only PATH symlinks. Found: a CI cache path **and its `hashFiles('.../Cargo.lock')` key**, a dev launcher's binary resolver, a macOS selftest CLI path, and **a Tauri bundle path (`<target>/release/bundle/macos/*.app`) one level deeper than `target/release`, which a grep for `target/release` cannot see.** Laksa's `bin/.devbin/cmd/cli:70-71` is the same class through a different door -- a shell script holding two hardcoded paths, invisible to any symlink sweep. **A grep that looks complete is the failure mode; a grep that comes back empty is not.**
- **A LOUD CONSUMER SURVIVES A DELETION IT WAS NEVER TOLD ABOUT** (laksa-vc, 2026-08-27). Laksa's `debug` arm pointed at nothing for hours after the morning sweep and did not break, **by design rather than luck**: the resolver `die`s with the exact rebuild command instead of falling back, because `--release` answered by the debug binary is the wrong-subject error it exists to prevent. A silently-falling-back resolver would have answered from the wrong profile and nobody would have known.
- **A REFUSAL RAISED INSIDE `< <(...)` IS SWALLOWED, AND PRODUCES EXACTLY THE FALSE CLEAN IT WAS WRITTEN TO PREVENT** (devbin-vc, found by driving the arm red). Its PATH-validity control lived in a function consumed through process substitution: `die` exited the subshell, the parent's read loop got no input, concluded NO LIVE BINARIES, and proceeded to delete. **The control fired, was discarded, and the run looked clean.** Found only by breaking the splitter deliberately and watching the test NOT go red. **Validate in the calling shell, before any scan** -- and anyone writing a guard should check where theirs is invoked from.
- **A CHECK TOO SLOW TO RUN IS A CORRECTNESS PROBLEM, NOT A PERFORMANCE ONE** (devbin-vc). Walking every PATH entry and resolving every symlink is O(entries x files) with a fork per test; it hung past two minutes, **and a check that slow gets disabled.** Inverted: the executables IN a build tree are tens, and `command -v` forks nothing -- ask "does PATH resolve this name back into the tree". Same question, O(binaries), and `deps/` drops out because nothing resolves into it, which is where the hundreds of thousands of files are.
- **THE BUILD-TREE DISCOVERY PREDICATE IS NAME **UNION** MARKER, AND `CACHEDIR.TAG` ALONE IS NOT IT** (devbin-vc, correcting vc's proposal). `marker = CACHEDIR.TAG || .rustc_info.json || debug/.fingerprint || release/.fingerprint`; removable = marker AND a sibling manifest; marker without a manifest is SKIPPED and printed with its size; named-like-a-build-tree without a marker is IGNORED and printed with its size. **`CACHEDIR.TAG` is a general cache convention rather than a cargo signature -- other tools write it, and cargo has not always written it, so an older or copied tree drops out of the scan entirely.** The name arm stays because the two find different things: the marker arm finds a redirected `CARGO_TARGET_DIR` not called `target`, the name arm finds a `target/` whose markers a partial delete already removed. **And the report prints all three statuses: a safety report that enumerates only its hits cannot be distinguished from one that found nothing.**
- **A CRITERION CAN BE SATISFIED PERFECTLY BY A DEGENERATE IMPLEMENTATION, AND THE CRITERION CANNOT SEE IT** (ic, 2026-08-27). A `default_declaration` returning NOTHING passes _"all three callers agree"_ -- **three writers agreeing on nothing is a perfect score.** Only the controls beside the proof can tell the difference. This is why `AT-11.3` is red on purpose: a green row would satisfy a criterion with an unmet clause.
- **CONSOLIDATING TEST TARGETS DOES NOT FORCE RE-CITATION -- THE RE-CITE IS AN ARTEFACT OF THE OBVIOUS IMPLEMENTATION** (laksa-vc landed it, vc verified the contract mechanism, cc measured the cut and corrected vc twice). `autotests = false` + one `[[test]]` + `#[path = "x_test.rs"] mod x_test;` per file: **one binary, N modules, and not one file moves**, so every citation still resolves. Verified in `contract.rs:171-179` -- a cited file is checked two ways, `resolves()` (exists) and `carries_id()` (contains the literal AT id) -- **and both survive untouched, because the file neither moves nor changes a byte.** `contract.rs:160-165` argues against name-citation in its own words: _"checkable from BOTH ends and survives rewording, where a cited test NAME survives neither."_ **The re-cite route is also the dangerous one**: `intent at` has no verb that retargets a row's file, the only idempotent verb is `at new`, and **`at new` carries no `--note` and defaults `--status` to `to-write`** -- so an upsert across green rows can drop prose and un-satisfy criteria. Intent's citations are currently sound: 308 rows cite a file, 221 green + 11 red with **zero missing**, and all 67 missing citations sit on `to-write` rows, which `contract.rs:523-534` deliberately exempts.
- **`#[path]` DOES NOT REBASE A NESTED `mod`, AND vc's PREDICTION THAT IT DID WAS WRONG** (cc measured, 2026-08-27, on the real cut). vc predicted that `mod common;` inside a `#[path]`-included file would resolve against that file's own directory and break all 97 declarers. **It does not. cc built the real cut in a detached worktree -- `autotests = false` on `intentsvcs`, five real test files included by `#[path]`, private `CARGO_TARGET_DIR` -- and got 29 tests, 0 failures, with not one byte of the five files edited.** Both `mod common;` and `use common::{Fixture, sample_thread}` resolved untouched. Two controls, because the green is worth nothing without them: **positive** -- the same files build and pass standalone, so the fixture is valid; **negative** -- move `tests/common/` aside and it fails `E0583: file not found for module common`, which proves the resolution lands on the real `tests/common/mod.rs` and not on a stub. It is `tests/common/mod.rs`, a DIRECTORY module, which is the resolution question. And each `mod common;` is PRIVATE to its includer, so 97 modules of the same name do not collide and nothing needs renaming.
- **A SYNTHETIC FIXTURE IS NOT A PRECEDENT FOR REAL CODE, AND THE EDITION WAS A RED HERRING** (cc raised it as an edition caveat; laksa-vc corrected the diagnosis, 2026-08-27). cc's first `#[path]` run was on a **synthetic crate at edition 2021** and cc read the worthlessness as an edition risk against a 2024 workspace. **laksa-vc settled it: `#[path]` module resolution is not edition-gated at all. The synthetic run was worthless because it was SYNTHETIC, not because it was 2021.** The claim is now covered at both editions **on real crates**: laksa-vc's is the real crate at edition 2021 -- 13 former targets as modules of one binary, 36 tests green, **and the deliberately-failing witness still surfacing at rc=101 through the merged target** -- and cc's is the real crate at 2024 with both controls. **Nobody should cite the synthetic run, at either edition.** The rule that survives: **a fixture built to demonstrate a mechanism cannot establish that the mechanism holds in a codebase that did not shape it.**
- **CONSOLIDATION SAVES LINK TIME AND BINARY COUNT, NOT COMPILE TIME -- vc's COMPILE SAVING DOES NOT EXIST** (cc measured, correcting vc, 2026-08-27). vc argued that folding 97 includers into one binary removes 96 redundant compilations of `common`. **Measured rather than reasoned: cc gave `common` a `pub static MARKER` and compared `&MARKER` through two `#[path]` includers in ONE binary -- `alpha=0x100dbbb6b`, `beta=0x100dbbb7a`, `same=false`. Distinct addresses: each includer instantiates its OWN private copy.** The negative control agrees -- removing the file produced TWO `E0583`s, one per includer, not one. **So consolidation converts 97 binaries x 1 copy into 1 binary x 97 copies and the compile term is unchanged.** The saving is real but it is link-time and binary count, and that is what belongs in a price list -- **not a compile-time term that would not survive being asked how it was measured.**
- **A BUCKETED THREAD MIGRATED WITH ZERO ATTACHMENTS AT rc 0, AND THE PRUNE MUST STAY BEHIND THE FIX** (cc, `2cf8fa63`, ST0057/13 layer 1). `thread_dirs` walks the buckets and hands back `(id, dir)`; the info parse, the acceptance parse and the WP walk all take that `dir`, and **the attachment carry was the ONE reader that re-derived the flat `intent/st/<ID>/`**, which for a bucketed tree does not exist. **Nothing reported it: no refusal, because refusals are per file and there were no files; no shortfall, because an attachment is not an AC/AT row.** Fixed by passing the directory the walk already had. **And the ordering matters: the bucket is the only source surface `at-accounting.sh` has for the hop-2 AT-tranche loss, so pruning before the fix destroys the evidence.**
- **A MATCHING COUNT IS THE WEAKEST POSSIBLE EVIDENCE OF IDENTITY** (prolix-vc, refusing to close its own rows on it). Prolix's 2 repaired AT citations match the multibyte-panic tally by count AND by thread, and **the symptom does not match** -- a trailing parenthetical carried into the file field is ASCII and silent-wrong-value, where the subject is a multibyte panic. Same root cause is plausible and was not established. **Two sets of size two that share no member are not the same set**, so the estate-wide 161 is held as an upper bound on a FAMILY until each estate's number is a diagnosis rather than a count.
- **LINE-INDEX SURGERY MUST BE BOTTOM-UP WITHIN A BLOCK, NOT ONLY ACROSS SECTIONS** (dc, against its own fold script, 2026-08-27). It edited sections bottom-up but not within one: an insert at line 164 shifted the block before a delete at line 176 ran, which removed the wrong watch-out. **Every pre-mutation assert PASSED, because all of them ran before the shift.** What caught it was the script printing a byte count whose SUBJECT had to be checked rather than read. **Anchor on TEXT, never on a line index** -- or assert the deleted line's own byte count.

- **THE ATTACHMENT SHORTFALL GATE IS THE WHOLE DEFENCE, AND `2cf8fa63` DID NOT CHANGE THAT** (cc answered, vc asked, 2026-08-27). **`reconvert.sh`'s before/after attachment count and its exit 9 are LOAD-BEARING, not belt-and-braces.** cc's fix corrects the read and adds nothing else: **there is no refusal inside the migrator.** The mechanism, so it can be quoted rather than believed -- the migrator's one accounting is `declared == stored + recorded` over **AC/AT ROWS, and an attachment is not a row**; inside `collect_attachments_in` every file is either carried or refused, **so that half reconciles by construction -- against the file list it was GIVEN.** Hand it a directory with no files and it reports zero carried, zero refused, and **zero is a perfectly consistent answer.** The fix makes it look in the right place; **it does not make looking in the wrong place an error.**
- **THE GENERAL FORM, AND IT IS WHY FOUR GATES PASSED OVER A STORE THAT HAD LOST EVERYTHING: A CONSERVATION CHECK WHOSE TWO SIDES DERIVE FROM THE SAME SOURCE CANNOT DETECT A WRONG SOURCE.** carried + refused == given is an identity, not a measurement. **Both sides must derive their path INDEPENDENTLY** -- under the old defect one side was the `dir` that `thread_dirs` handed back and the other was `thread_dir(id)`, N versus 0, and a check comparing those two would have REFUSED. After the fix they agree by construction, which is exactly what makes it a regression guard rather than a tautology.
- **THE DEFECT REPRODUCED ON A LIVE ESTATE, WITH COMMIT EVIDENCE: arca_cli WENT 23 -> 0 -> 23** (cc measured attachments in canon at three commits, 2026-08-27). `1abd898` carry-the-buckets + prune: **23**. `2f3e836` re-convert on `intent 3.0.0 (f438d0d5)`, the pre-fix binary: **0**. `5441945` carry + prune again, by hand: **23**. **Four gates passed over the store at `2f3e836`** -- hop 2 `ok`, at-accounting `PASS`, `verify-canonical` 0 failed, `doctor` IMPROVED 19 -> 2 -- **over a store that had just lost 23 of 23 attachments, and it took a hand pass to put them back.** This is the measured case for stopping Lamplight's re-convert until the pair carries `2cf8fa63`: same binary, same bucketed layout, **80 attachments instead of 23.**
- **A PRICE LIST WITH AN UNMEASURED ITEM IN IT IS HOW THE FIRST ONE WENT WRONG** (cc, refusing to hand vc a number, 2026-08-27). Of the five consolidation terms, four are settled -- the 97-file `mod common;` rewrite is **zero**, the 168-row AT re-cite is **zero** under `#[path]`, the compile saving is **zero**, and the binary count is **128 -> 1**. **Link time and total artefact bytes are NOT MEASURED and no number is offered for them**, because both need a clean-tree build. **The rule: a price list does not go up for a ruling until every item on it has been taken** -- vc's added compile-saving item was reasoned rather than measured, and it was wrong.

- **COUNT INSTRUMENTS CANNOT DISTINGUISH A THING FROM A MENTION OF THE THING; NAME INSTRUMENTS CAN** (lamplight-vc, 2026-08-27 -- its formulation, verbatim, and it subsumes four separate entries above). It nearly filed a lost-test regression on **216 `#[test]` attributes in source against 215 running**; the 216th was a **COMMENT** saying _"No `#[tokio::test]` (tokio-macros is out of the lock)"_. What settled it enumerated test FUNCTION NAMES and diffed them against `--list`, **so it could have named a missing test if one existed -- and instead named the phantom.** Same class, same day, three other ways: dc's bare grep put `error()` in 8 files and `warning()` in 5, where the CALL-SITE counts are **3 and NONE** (comments and string literals counted as calls); vc's `grep -l ', describe'` matched deliberately-preserved text inside `legacy.raw`; and cc's `grep -l '"attachments": *\[[^]]'` scored **0 against a canon holding 496**, because `"attachments": [` is followed by a NEWLINE so `[^]]` can never match. **Prefer an instrument that can NAME its subject; a count cannot tell you what it counted.**
- **A PIPE EATS THE EXIT CODE, AND ZSH HAS NO `PIPESTATUS` TO RESCUE IT** (lamplight-vc, correcting every rc it had reported that day). `cargo ... 2>&1 | tail -N` followed by `echo "rc=$?"` reports **`tail`'s** status: **a build that failed with 18 errors printed `rc=0`.** Worse than the wrong number -- lamplight-vc's conclusion was RIGHT because it was reading cargo's own `Executable`/`Finished` lines, **so it cited an instrument that could not have contradicted it.** A correct answer from an instrument that cannot fail is indistinguishable from a lucky one. **And the obvious remedy does not work here: `${PIPESTATUS[0]}` is EMPTY under zsh, which is the Bash tool's shell.** Redirect to a file and read `$?` with no pipe at all, then grep the file.
- **A PROPERTY THAT HOLDS VACUOUSLY IS INDISTINGUISHABLE FROM ONE THAT HOLDS SUBSTANTIVELY, AND ONLY AN ASSERTION ON THE MIDDLE STATE SEPARATES THEM** (ic, 2026-08-27 -- the degenerate-implementation rule with a number on it). With `dehydrate` made to remove NOTHING and the middle assertion deleted from the test, **the round trip PASSES: a do-nothing inverse round-trips perfectly.** AC-00.1 asks for the round trip, and the round trip alone would have certified an inverse that was never performed. Its sibling: a `default_declaration` returning nothing passes _"all three callers agree"_, because **three writers agreeing on nothing is a perfect score.** The standard that catches both is ic's -- **every assertion driven red first, by mutations applied and reverted one at a time, each reddening its assertion BY NAME.**
- **A GUARD SCOPED WIDER THAN ITS HAZARD IS ONE THE OPERATOR LEARNS TO WAVE THROUGH** (lamplight-vc found it in vc's `quiet.sh`, fixed at `85b11b45`). The process read was **machine-wide** while the hazard -- writing a target dir a build is using -- is **per-target-dir**: it blocked Lamplight's build three times on two Intent worktree builds and one Conflab build, none of which could touch Lamplight's target dir. **In a fleet all converting at once that is red for everyone, permanently, on work that is none of their business, and lamplight-vc nearly waved it through.** Now cwd-scoped, so a same-tree peer's hold stays load-bearing. **Two riders. The excluded processes are PRINTED, never silently dropped -- and that printing immediately caught a SECOND defect in the same edit: five pids whose cwd would not read were being filed as ELSEWHERE, resolving ambiguity toward quiet, which is the direction that makes a false clean.** A pid that EXITED between the `pgrep` and the `lsof` is gone and drops silently; a pid that still exists whose cwd will not read is an **UNKNOWN and counts against quiet**, driven red with a stubbed cwd read. And the honest limit is in the header: **cwd is a proxy for the target dir, not the target dir.**
- **2,115 AUTHORED BUCKET FILES ARE ON DISK AND IN NO STORE, ACROSS 5 OF 14 ESTATES -- WHICH MAKES THE INGEST-BEFORE-PRUNE ORDERING MEASURED RATHER THAN PREFERRED** (cc's sweep, 2026-08-27). Lamplight **1,584** (353 threads, **333 with the `attachments` key absent entirely**, 80 attachments total), Laksa 499, Devbin 24, Anvil 5, Baize 3; nine estates at zero. Excludes `info.md`/`acceptance.md`, which are generated views and correctly not carried. **Nothing is lost -- every file is on disk and in git -- but a dehydrate or a prune that runs before the ingest destroys all 2,115.** Content-probed with a control rather than counted: 8 Lamplight stranded files sampled by sha256 against every attachment digest in its canon, **8 of 8 genuinely absent**, and a known-carried digest IS found in the same set so the probe can hit. **Scope limit stated by its author: the sweep walks `~/Devel/prj/*/intent/.canon/st` and MISSES nested estates** -- `Arca/arca_cli` is one level deeper and was measured separately. Read it as 14 estates, not as the fleet.

- **THE AT RE-CITE HAS A CANON DOOR THAT PRESERVES THE NOTE, SO THE `--note` GAP IS A DEFECT AND NOT A GATE** (ic, correcting its own escalation, 2026-08-27). ic told vc the `--note` writer was a prerequisite for any re-citation sweep, then **drove the counter-example on a fixture rather than reasoning about it**: edit the `file` field **in `.canon/st/*.json`**, then `sync --to-store` and `sync --to-disk`. **The file changes, the note survives verbatim, the view carries both, `at lint` passes -- `at new` is never invoked, so the destruction never happens.** ic had already walked that path once that morning, restoring dc's six notes, and **reported the blocker from the SHAPE OF THE DEFECT rather than from the path it had just walked.** **ORDERING IS LOAD-BEARING: `sync --to-disk` FIRST so the extract is current, THEN edit canon, THEN `sync --to-store`** -- that verb REPLACES the store from the extract, so editing over a stale extract discards whatever the store held and canon did not. **dc's claim was true about the verbs; the verbs were never the only door.**
- **THE MOVING ROUTE FAILS LOUDLY AND IMMEDIATELY, NOT SILENTLY -- AND LOUD-PLUS-UNFIXABLE IS THE WORSE POSITION** (laksa-vc, correcting its own report, 2026-08-27). laksa-vc first told vc that a wrong citation would sit silent and be found months later. **Measured instead of reasoned, by hiding ONE cited file and running the lint: three findings from one file, `lint: ST0097 FAILED -- 3 finding(s) over 67 AT row(s)`.** Its 13 rows are all green, **so the moving route would have reddened the lint thirteen times at the moment of the move.** For Intent's 168 that is **168 findings on day one**, from the first commit of the refactor until the tooling catches up. **So the case against moving files is not silent contract rot -- it is an immediate, total, loud lint failure with no verb able to repair it in one step.** The conclusion is unchanged and better founded: use `#[path]`, move nothing, and the re-cite population is **zero**.
- **L2 IS STATUS-GATED, NOT BLIND -- AND THE REAL HOLE IS THAT NOTHING AGES A `to-write` ROW WHOSE FILE NEVER ARRIVES** (vc measured the mechanism, laksa-vc confirmed against its own claim). `contract.rs:528-530` requires `kind == Test` **AND** `matches!(status, Green | Red)`; `to-write` is exempt **deliberately**, because a missing file is the correct state for an unwritten test. laksa-vc's three ST0106 rows are every one of them `to-write`, **so the tool behaved exactly as designed and "L2 does not bind" was a wrong generalisation from a correctly-exempted case.** Intent's 67 missing citations are the same shape. **The hole that IS real: a row lying in the exempt direction is invisible to the gate by construction, and nothing ever asks whether a to-write row's file has since appeared.** (Intent's own commit gate has an arm for exactly that -- _"a to-write row whose cited test already exists"_ -- which is the missing half in the other direction.)
- **AN ABSENCE-ASSERTION IS SATISFIED BY A HARNESS THAT NEVER RAN** (devbin-vc, 2026-08-27 -- the sharpest instance of the family). `script` cannot always allocate a pty: it fails with _"tcgetattr/ioctl: Operation not supported on socket"_ and then **emits NOTHING** -- so every test asserting a notice **stayed silent** was satisfied by a broken harness. **Two were passing vacuously.** A `__PTY_OK__` sentinel converted four silent passes into honest failures. **The general remedy: assert something the harness MUST print if it ran at all.** Same family as ic's do-nothing inverse and the `to-write` exempt arm -- **the arm nobody checks is where the false thing goes.** Anyone testing an absence through `script`, `expect` or a pty owes the same sentinel.
- **A PERMANENTLY-RED INSTRUMENT IS THE MIRROR OF A FALSE GREEN, AND ONLY ONE OF THEM LOOKS LIKE A DEFECT** (devbin-vc + vc, 2026-08-27). devbin's vendored-runtime notice fired on EVERY invocation and `upgrade` could not clear it: `source_commit_of` writes `<sha> +dirty -- THESE BYTES MATCH NO COMMIT` whenever devbin's own half is uncommitted, and that whole string was compared against `git rev-parse HEAD`, so it never matched. **The same week, vc's `quiet.sh` went red for every estate on other estates' builds and lamplight-vc nearly waved it through.** Both kill the instrument; the red one gets waved through and the green one gets believed. **And its remediation named a command that does not exist** (`<name> devbin upgrade`; `upgrade` is top level) -- a remedy nobody runs is a third way to be permanently unhelpful.
- **AN EMPTY RESULT FROM A DEAD BINARY IS INDISTINGUISHABLE FROM A REAL ZERO** (baize-vc, 2026-08-27, caused by vc's own build order). `~/.local/bin/intent` symlinks into `native/rust/target/release/intent`, so **a release build takes the CLI offline in every project on the machine**, between unlink and relink. baize-vc ran `intent at list ST0025 | grep AT-02.1`, got nothing, tried a looser pattern, got nothing again, **and nearly reported a false finding about Baize's acceptance contract sourced entirely from another estate's build.** What saved it was a third probe asking WHY rather than a fourth asking the same question differently. **This is `audit for CONSUMERS, not just PATH` landing on the person who relayed it three hours earlier: the consumer of the shared release path is the entire fleet's tooling, through a symlink one directory OUTSIDE the tree being rebuilt, which no `target/`-shaped sweep surfaces.** laksa-vc re-took every `intent`-sourced number it had quoted that day against a confirmed-live binary: **all identical -- and it could not have known that without re-taking them.**

- **THE PRE-COMMIT CRITIC GATE FAILS OPEN ON A MISSING `intent` CLI, AND vc's REBUILD MADE 12 COMMITS UNGATED ACROSS 3 ESTATES** (conflab-vc found it, vc measured the blast radius, 2026-08-27). `lib/templates/hooks/pre-commit.sh:283` -- _"intent critic gate: 'intent' CLI not on PATH; skipping."_ then **`exit 0`**. **The commit succeeds, the gate is skipped, and the only trace is two stderr lines that scroll past in a normal commit.** Fail-open is defensible design -- a missing tool must not block work in a non-Intent repo -- **but it means a CLI outage is not merely "measurements are uninterpretable", it is "commits in that window are ungated and nothing durable records it".** conflab-vc's framing is the right one: **baize-vc got an empty result that looked like a real zero; a committer in the window gets a GREEN COMMIT that looks like a passed gate.** Window bounded by the relink mtimes (`intent` 13:37:06, `intentd` 13:33:51 local): **12 commits, 13:28:00-13:37:06** -- Intent 9 (including `56517758`, the migrator fix itself, and two of vc's own), Baize 2, Lamplight 1. **A shared release path is a fleet-wide gate dependency, so a rebuild must be announced BEFORE it starts, not diagnosed after.**
- **THE "161 ROWS" CENSUS IS AN UPPER BOUND ON A FAMILY, NOT A COUNT OF ONE BUG -- BAIZE MEASURES 9, NOT 41, AND THE SYMPTOM IS NOT THE MULTIBYTE PANIC** (baize-vc, turning a count into a diagnosis, 2026-08-27). **All nine LOST content; zero gained, zero mangled in place.** Signature: **a `--` delimiter mis-slice** -- six AC rows lost their whole `evidence:` field, three AT rows lost an explanatory clause sitting between the test name and ` -- covers`. **Every one of the nine cut points is pure ASCII.** **THE CONTROL IS WHAT MAKES THE ZERO NON-VACUOUS:** 39 files under `intent/st/` carry non-ASCII and the `acceptance.md` files alone carry 88 non-ASCII characters, all valid UTF-8 after migration -- **so the migrator ran over a large multibyte surface and preserved it intact while losing ASCII text at delimiters. That is the OPPOSITE of the defect the 161 is named for.** With prolix-vc's trailing-parenthetical mis-slice, **two estates now report the same non-panic symptom.** Treat the 161 as rows AT RISK rather than rows DAMAGED until each estate re-measures.
- **THE METHOD THAT TURNS A SYMPTOM COUNT INTO A DIAGNOSIS, AND ITS TWO TRAPS** (baize-vc, 2026-08-27). Extract `^- (AT|AC)-` rows at the **pre-migration commit** and at HEAD, then: **(1) NORMALISE the known-mechanical v3 rewrite or everything differs and you learn nothing** -- baize-vc got **209/209 "all changed"** before normalising the three known changes (backticks stripped from the AT reference, ` (legacy)` inserted after the AT id, ` -- satisfied: ...` appended to AC rows). **(2) SET-DIFF PER THREAD, NEVER FLAT -- AT ids COLLIDE across threads** (`AT-01.1` exists in more than one), and a flat diff mis-pairs them; the first pass **silently dropped two rows from the report.** **And the governing rule: anything counted by symptom-grep TODAY measures v3's READING, not what the migrator DID. Only the pre-migration bytes settle it** -- which is _diff against the prior resolution, not against success_ arriving in a second domain on the same day.
- **A CONVERSION IS NOT FINISHED AT THE MANIFEST: THE NEW TARGET TREE IS UNTRACKED AND UNIGNORED** (conflab-vc, 2026-08-27). `.gitignore` fenced only the two per-crate target paths, so the workspace tree showed as `?? native/target/` until `/native/target/` was added. **An estate that misses this stages its whole build tree.** This is a THIRD consumer class alongside PATH and CI, and **it is invisible to both a `target/`-shaped sweep and a PATH audit** -- the two instruments the earlier entries recommend.

- **HOP 2 ABORTS ON THE FIRST REFUSING THREAD, SO EVERY CLEAN RUN IS ONLY AS GOOD AS "NOTHING REFUSED" AND EVERY REFUSING RUN UNDER-REPORTS BY AN UNKNOWN AMOUNT** (vc observed it on Lamplight and diagnosed it WRONGLY as bucket-blindness; cc disproved the mechanism by test and found the real one, 2026-08-27). **THE OBSERVATION:** hop 2 refused at rc 1 with 8 findings and named **3 distinct paths, every one FLAT, zero bucketed** -- while vc's own predicate over the same source finds **42 such rows in 13 threads: 8 in the 3 flat threads, ALL FOUND exactly (3+2+3); 34 in the 10 bucketed threads, NONE FOUND.** Perfect correlation with location, in both directions. **THE MECHANISM IS NOT LOCATION.** cc put the same defective row in both places -- `legacy_bucketed_residue.rs`, three arms, all green -- and **the bucketed one IS reported.** What actually happens: `thread_dirs` yields the top level BEFORE the three buckets (pinned by giving the flat thread the HIGHER id and still getting it first), **and the row accounting returns `Err`, which propagates out of `scan()` and ends the entire run.** So the first refusing thread buries every thread after it, and because flat sorts first, **flat paths and only flat paths survive in the log. The rc=1 is the tell: hop 2 did not finish.** **THE CONCLUSION SURVIVES AND IS WORSE THAN vc PUT IT: the 8 is a floor for EVERY estate, not only bucketed ones** -- a fully flat estate with a refusal in its first thread reports one thread and stops. **THE FIX IS ACCUMULATE-THEN-REFUSE** (cc's, and cc's to make because the abort is cc's): collect every thread's refusal, report the lot, refuse at the end. **`56517758` made it worse by adding a second refusal that can abort, which cc volunteered rather than leaving to be found.** **AND THE GENERAL LESSON IS vc's TO WEAR: a perfect correlation in both directions is not a mechanism.** Location and order were confounded because the abort fell exactly on the boundary between them, and no amount of the same evidence would have separated them -- only putting the row in both places did.
- **THREE LATENT INSTANCES OF THE FLAT-PATH CLASS, WITH NO LIVE THIRD** (cc, answering the question rather than shipping the patch, 2026-08-27, after saying its first audit was too narrow: scoped to one file's direct calls, _"true of that file and not of the question"_). Grepping every `thread_dir(` across the crates: **`project.rs:998/1004/1012` derive `info.md`, the WP path and `acceptance.md` from the FLAT `thread_dir(id)`. The migrator does not use them** -- it does `dir.join(...)` off the walk -- **so they are not live instances, but they are three loaded guns pointed at exactly this class, and any future migrator caller reaching for the obvious helper gets the flat path.** `views.rs`, `ingest.rs`, `doctor.rs` and `facade.rs` read a v3 tree where flat is correct; `migrate.rs:541` is flat and correct because migration relocates. **The dangerous shape is a helper whose name is right for one caller and wrong for another, sitting where the wrong caller will find it first.**
- **A NODE MONIKER IS UNIQUE ONLY WITHIN ITS ESTATE, AND A BARE ONE DOES NOT READ AS AMBIGUOUS -- IT SILENTLY RE-ATTRIBUTES** (vc's error, caught by lamplight-cc, 2026-08-27). vc relayed intent-cc's fleet sweep to lamplight-vc as _"cc's sweep"_; in Lamplight, `cc` is lamplight-cc, **which spent a session's attention correctly denying a number it had never produced, having grepped its whole node directory to be sure.** The failure produces **a confident false attribution and an equally confident true denial, both correct from where each party stands.** lamplight-cc's framing is the durable half: **a misattributed measurement fails in a way a fabricated one does not -- it looks sourced, it survives being quoted, and the node it is credited to cannot correct what it never sees.** Every cross-estate reference carries its estate: `intent-cc`, never `cc`.
- **THE 1,584 AND THE 4,020 ARE BOTH WITHDRAWN AS STATED, AND THE ORDERING NEVER RESTED ON EITHER** (2026-08-27). intent-cc's 1,584 is real and correctly scoped by its own `STRANDED` definition (excludes `info.md`/`acceptance.md`), but vc relayed it without its estate and without its definition. lamplight-vc's 4,020 counted every `.md` under `intent/st` **including 2,227 `info.md`, whose thread records ARE carried -- 349 with a canon record, 12 of 12 sampled present** -- so it inflated the denominator with precisely the files that are in the store. **They were never "two denominators over one fact"; they are two different facts.** The derived figures: **1,310 bucket-class not carried, 490 other authored, 2,227 `info.md` (carried), 80 attachment rows, 333 of 353 threads with the `attachments` key ABSENT.** **The ordering argument is carried entirely by that last pair plus a 12-of-12 sha256 probe with a known-carried digest as its positive control -- and it read as though it rested on counts, one of which had no author.**
- **A CORRECT PREDICATE READING A SURFACE THAT IS LEGITIMATELY INCONSISTENT MID-OPERATION -- THE FOURTH DISTINCT INSTRUMENT CLASS** (lamplight-ac, 2026-08-27). ac sampled Lamplight's tree during vc's re-convert and saw **359 canon `.json` staged-deleted with zero on disk, `intent/st/_inbox/` staged back in against hv's ruling, and an authored `tasks.md` reverted to a pre-cutover revision.** It diffed, confirmed the loss line by line, and was composing an urgent data-loss escalation when the next sample came back clean. **The intermediate state of a CORRECT run is byte-for-byte indistinguishable from arca_cli's 23 -> 0.** Not a wrong predicate, not a right predicate in the wrong place -- **a right predicate reading a surface that is not supposed to be consistent yet. A gate that samples the TREE cannot tell success-in-progress from failure; it must key on the operation's own completion.** ac's control is the right shape and generalises: **a sha committed to BEFORE the run** (`ST0306/tasks.md` at `1f3455b6`), because a control chosen after the fact proves nothing.
- **A CRITICAL RULE'S GREPPABLE PROXY IS DEFEATED BY `mix format`, SO IT ARMS, READS FILES, AND CAN NEVER FIRE** (prolix-vc found and proved it two-sided; vc confirmed in Intent's tree, 2026-08-27). `IN-EX-CODE-005` (No Silent Failures, **CRITICAL**, one of the four Rules of the Road) declares `grep -rnE 'rescue _ -> (\:ok|nil)' lib/`, which matches only when `rescue` and `_ -> :ok` share a line. **`mix format` rewrites that into the multi-line form.** Measured here: **single-line 1 hit, multi-line 0 hits, and `mix format` demonstrably produces the multi-line form.** So in any project whose format gate enforces `mix format` clean -- which is most of them -- **that critical rule examines files and is structurally unable to produce the other result, while the file count in the log looks healthy.** Blast radius in one tree: Prolix carries two live sites of the rule's own target shape the gate has never seen. **It is the only formatter-fragile proxy of the 12 declared** (swept). The defect is in the HEADLESS runner that powers the pre-commit gate -- **the one that runs with nobody watching**; the rule text itself says the proxy is not authoritative and the LLM critic confirms by reading.
- **A CORPUS ZERO CANNOT DISTINGUISH "NO INSTANCES" FROM "CANNOT MATCH" -- ONLY A CANARY CAN** (vc, generalising prolix-vc's method, 2026-08-27). Four of the nine Elixir proxies return zero across five estates, **and only `IN-EX-CODE-005` is PROVEN structural** -- the other three may simply have no instances, and nothing in the count says which. **prolix-vc's instrument is the one that settles it: write the construct the rule targets, in the FORM THE TOOLCHAIN ACTUALLY PRODUCES, and check the proxy fires.** So **every declared proxy owes a canary fixture**, or its zero is uninterpretable. **And vc's own first sweep of this ran the `test/`-targeted proxies against `lib/` and produced four false "never fires" -- the instrument-scope error, inside the measurement of instrument-scope errors.**

- **A CONSUMER AUDIT THAT RESOLVES ONE HOP FINDS ONE HOP'S CONSUMERS** (vc's instrument, both misses caught by lamplight-cc, 2026-08-27). Dropping the `bin/{proj} -> bin/devbin` shim: vc swept every estate's `bin/` symlinks **filtering on whether `readlink` contained `devbin`**, reported 14 consumers, and **by construction could not see a link pointing at another link.** The two that actually break were not among them: **`Lamplight/bin/ll -> lamplight -> devbin` and `Baize/bin/bz -> baize -> devbin`** -- every other estate's short name points DIRECTLY at `devbin` and survives. **`bin/ll` is Lamplight's DOCUMENTED PRIMARY INTERFACE** (its CLAUDE.md hard rule #1, _"`bin/ll cli` IS THE INTERFACE"_), with two live executable gates on the middle name: `.github/workflows/ci.yml:280` and the `bin/hooks/content-provenance:76` pre-commit hook. **hv batches pushes, so the CI one breaks as a red on a push nobody is watching a shell for.** ~603 further references are docs -- **they do not break, they become wrong instructions.** The rule: **resolve every chain to its FINAL target, and separately ask what points AT each link.** This is _audit for CONSUMERS, not just PATH_ failing one level further down, in the hands of the node that wrote it.
- **ZERO IS THE ONE ANSWER A BROKEN INSTRUMENT RETURNS MOST CONVINCINGLY** (lamplight-cc, 2026-08-27). Its first sweep, `git grep -E 'bin/(ll|lamplight)\b'`, returned **0** -- and it was one step from reporting a clean, confident, completely false all-clear. **`git grep`'s ERE does not honour `\b`. The same pattern without it: 603.** It caught this only by grepping a string it already knew was present in a file it had just read, and concluding the INSTRUMENT was the defect when the count stayed zero. **A null result never had a basis to begin with, so it needs a positive control before it is passed to anyone** -- the same shape as the dead-`intent` zeros, the `[^]]` attachment pattern that scored 0 against a canon holding 496, and the four false "never fires" from proxies aimed at the wrong directory.

- **AN EXECUTABLE DEPENDENCY THAT RUNS INSIDE THE TEST SUITE IS INVISIBLE TO A CI-FILE GREP, AND ITS BREAK IS THE HARDEST TO ATTRIBUTE** (laksa-vc, correcting vc's relay of devbin-vc's table, 2026-08-27). vc carried "Laksa: 40 executable refs, **2 CI gates**" to hv and to Laksa. **Both `.github` hits are COMMENTS** -- `ci_cd.yml:143` and `:153`, inside a block explaining why the runner needs a Rust toolchain. Grepping non-comment lines across `.github/` for `bin/laksa`, `bin/lk`, `bin/devbin` or a bare `laksa test|check|build` returns **nothing: Laksa has ZERO direct CI references.** **And that is worse, not better.** The comment states the real mechanism: _"test/cli/crate_gate_test.exs drives `bin/laksa test rust` ... It runs in the normal pass on purpose -- a guard excluded from the default run rots."_ CI runs `mix test`; **nine test files execute the launcher as a real OS process**, plus `test/support/launcher.ex:158`, whose `path/0` is literally `Path.join(File.cwd!(), "bin/laksa")`. **So the estate that looks safest by a `.github/` grep is the one where the break is hardest to attribute -- it surfaces as nine unrelated-looking test failures rather than as a step that could not find a command.** And it is **not one choke point**: two of the nine route through the helper, the other seven build the path themselves via `Path.join(@root, ...)` or embed it in `sh -c` strings, **so nobody should price the fix as a one-liner off the existence of that helper.**
- **`git grep -E` DOES NOT HONOUR `\b` -- A TOOL PROPERTY, ON ONE COLD DISCOVERY AND TWO PRIMED REPRODUCTIONS** (lamplight-cc found it cold; baize-vc and laksa-vc reproduced it AFTER vc had told them the answer, 2026-08-27). Lamplight **0 vs 603** (0 vs 1110 for the pair), Baize **0 vs 79**, Laksa **0 vs 533 across 210 files.** Use `([^a-zA-Z0-9_-]|$)`. **THE COUNT WAS FIRST WRITTEN HERE AS "three estates, three independent audits, three false all-clears, and every one of us reached for `\b` first". THAT IS FALSE AND vc WROTE IT.** laksa-vc refused the credit: _"I did not reach for `\b` first. You told me the answer, and I ran it deliberately as a control to reproduce a trap I had already been handed. My zero is a REPRODUCTION, not a near-miss -- I was never one step from filing a false all-clear, because I never believed the zero for a second."_ **And checking vc's own sent messages makes it worse than laksa-vc knew: baize-vc was primed too**, in the same paragraph that carried the warning. **So it is ONE cold discovery and TWO primed reproductions, not three independent audits.** The claim survives -- a tool property does not need three witnesses, and lamplight-cc reached it cold -- but it is a materially weaker claim than the one first recorded.
- **A CONFIRMATION FROM SOMEONE PRIMED TO CONFIRM IS NOT WEAK EVIDENCE -- IT IS EVIDENCE OF A DIFFERENT THING** (laksa-vc, sharpening vc's own rule against vc, 2026-08-27). vc's standing rule is that **independent derivation is not corroboration when the instruments share a shape.** laksa-vc's is worse and more specific: _"I had the conclusion before I had the measurement."_ **A primed run tests whether the trap REPRODUCES -- useful -- and says nothing about whether a fresh auditor FALLS INTO it, which is the claim the record was making. Those two questions have the SAME COMMAND and different answers, and only the prior knowledge distinguishes them** -- so nothing in the output, the count, or the transcript of the run can tell them apart. **The corollary for a coordinator: warning a peer about a trap CONSUMES that peer as an independent witness to it, and the consumption is invisible unless the coordinator remembers what they sent.** vc did it twice in one afternoon and then counted both.
- **A TOOL NAMING A SPECIFIC REMEDY IS READ AS ADVICE REGARDLESS OF ITS SEVERITY LABEL** (laksa-vc, on devbin's incoming `doctor` note, 2026-08-27). `rm bin/laksa` reads as an instruction in a way that _"this link is a retired alias"_ would not, **and the severity word does not survive the reader's eye landing on a command they can paste.** laksa-vc said plainly it would have acted on the note had the warning not arrived first. **A NOTE offering a paste-able destructive command is a recommendation with a disclaimer attached, and the disclaimer is the part that is not read.**
- **A PIPELINE'S `||` GUARD BELONGS ON THE COMMAND WHOSE EMPTINESS YOU ARE TESTING, NOT ON THE LAST STAGE OF THE PIPE** (baize-vc, against its own audit, 2026-08-27). `grep -rl ... | sed 's/^/HOOK: /' || echo "no match"` -- **the `||` binds to `sed`, which exits 0 on empty input, so the "no match" branch can NEVER fire.** Empty-because-clean and empty-because-the-pipeline-is-broken render identically, **and the reassuring half of the construct is the dead half.** Caught only by expecting something and getting nothing at all. Second instance in one session of an empty result with two indistinguishable causes, after the dead-`intent` zeros.

- **THE CRITIC GATE'S FAIL-OPEN PROTECTS A POPULATION THAT DOES NOT EXIST, AND ITS TWO SKIP CONDITIONS ARE IN THE WRONG ORDER** (devbin-vc separated it from the declaration problem; vc measured the fleet, 2026-08-27). `lib/templates/hooks/pre-commit.sh` skips at **:283** on a missing `intent` CLI (`exit 0`) and at **:295** on `intent/.config/config.json` being absent (`exit 0`). **The stated justification for the first -- a missing tool must not block work in a non-Intent repo -- is ALREADY COVERED by the second, which is the precise test.** So swapping them makes the CLI check fail CLOSED for Intent projects only: a non-Intent repo still skips on the config test regardless of the CLI, **and an Intent project with no CLI FAILS, because a gate that cannot run in a project that declared it is a failure, never a skip.** **Measured: all 17 estates carrying the hook ARE Intent projects, so the fail-open protects nobody and cost 12 ungated commits across 3 estates during one 9-minute rebuild.** devbin-vc's `check format` already implements exactly that asymmetry and says why in its source. **AND THE FILE ALREADY FIXED THIS CLASS ONCE, ONE BLOCK UP:** a 2026-08-17 comment records moving the whiteboard guard ABOVE both fail-open exits, in its own words -- _"an exit written when there was one arm is a claim that the run is over."_ **The critic gate below is the same defect the same file had already diagnosed.** Fourth time today a document held its own refutation and was read past.
- **DERIVE, DO NOT DECLARE -- AND THE DERIVED FACT'S HOME IS THE COMMAND A BUILDER PASSES THROUGH ANYWAY** (devbin-vc, answering vc's "where does the fleet dependency get declared?", 2026-08-27). **A written declaration goes stale the instant someone re-points a symlink and nothing notices; you would be trusting a file to describe a filesystem.** `clean`'s live-binary scan already ASKS the right question -- _does PATH resolve any name back into this tree_ -- and the only thing wrong is WHEN: at clean time, which covers clean-then-rebuild and not the rebuild-in-place that actually caused the outage. **So the home is `doctor`** (already vendored to all eleven; reporting facts nobody asked for is its whole job), **with the scan moved to a shared engine both commands call -- one engine, two moments**, or it is a Highlander violation on arrival. **The MACHINE-level half is NOT devbin's and devbin declined to annex it:** `~/.local/bin/intent -> Intent/native/rust/target/release/intent` is a fact about hv's PATH, not about any project, **and a per-project tool can say "MY tree is depended on" but can never say "your PATH points into eleven trees". That half is unowned.**
- **A COMMAND THAT REPORTS FAILURE WHILE PARTLY SUCCEEDING DEFEATS BOTH READERS** (devbin-vc, 2026-08-27). `git add -A README.md bin/db ...` aborted the whole add because one path was already deleted-and-staged -- **git printed `fatal` AND the commit succeeded**, holding one file under a message describing twenty-four. **The failure signal and the partial success arrived together, so the reader who trusts the error and the reader who trusts the result are both wrong.** Its sibling from the same hour: `set -- $t` under zsh handed `cd` the whole string and **every launcher reported rc=1 from a broken harness** -- a confident FAILURE where vc's `\b` produced a confident ZERO. **Both directions are available and only one looks like a defect. A red from a broken harness is the cheaper failure ONLY because someone investigates it; nobody investigates a green.**

- **ABSENT IS THE ONLY ENCODING OF "NONE", SO ABSENT IS ALSO EXACTLY WHAT TOTAL LOSS LOOKS LIKE -- AND vc's SHORTFALL GATE IS A DELTA GATE, NOT AN AUDIT GATE** (ic measured it, 2026-08-27, inverting vc's stated fear into a worse one). **Measured across 14 migrated estates, 670 threads, 837 canon files: absent 366, EMPTY 0, populated 304 (1,697 rows).** And the zero is **STRUCTURAL, not circumstantial**: `model.rs:531` carries `#[serde(default, skip_serializing_if = "Vec::is_empty")]`, **so the canon writer CANNOT emit `[]` for attachments at all** -- its appearance would mean a foreign writer. **vc feared that an estate migrating to `[]` everywhere would read the same as one migrating to absent; that specific confusion cannot happen. What DOES happen is worse: for all 366 absent threads, canon records NOTHING separating "genuinely no attachments" from "attachments dropped", and a totals gate reads 0 for both.** So `reconvert.sh`'s before/after check is sound for what it does -- catching a loss DURING a run -- **and cannot audit a store at rest for a loss that already happened**, which is exactly arca_cli's case, where the loss was already committed. **The discriminator cannot come from canon at all; it has to come from DISK**, which is what intent-cc's stranded-authored-file sweep measures from the other side. **And the `default` half bites in the opposite direction: absent deserialises to an empty Vec, so absent and `[]` are the SAME in-memory value -- any gate loading canon through `Thread` cannot tell them apart no matter what predicate it adds. It must read raw JSON and ask `has("attachments")`.**
- **AN ENCODING INVARIANT IS PER-FIELD, NOT PER-STORE, AND THE COUNTER-EXAMPLE IS ALREADY IN THE CORPUS** (ic, 2026-08-27). _"Empty arrays never occur in canon"_ is **FALSE: 13 do across 837 files** -- 12 in Lamplight, 1 in Laksa -- **every one at `tests[].covers`, the ONE Vec in `model.rs` without the guard** (guarded: `tests`, `attachments`, `related`, `criteria`, `wps`; unguarded: `covers`, `model.rs:1257`). **So "this store never writes empty arrays" is wrong in general and right about attachments, and the two must be stated separately or the next field to lose its attribute inherits a false invariant.** Those 13 rows also carry `covers: []` against the field's own doc comment, _"Covered AC ids; at least one."_ **They do NOT overlap vc's 42 unread-field rows: ic's are AT rows, vc's are AC rows, disjoint by row type** -- checked rather than assumed after ic flagged a possible overlap.
- **A ZERO IS THE CLAIM, SO EVERY ARM OF THE CLASSIFIER WAS SEEN TO FIRE BY NAME BEFORE THE SWEEP RAN** (ic's instrument discipline, 2026-08-27). All four arms -- ABSENT / EMPTY / NULL / POPULATED -- were driven on synthetic fixtures first, **so `empty = 0` is a real zero and not a dead predicate.** Two independent confirmations taken from the world rather than from itself: **Intent's own pre-commit canon-currency arm names the same five zero-attachment threads and the same 496 rows, and Lamplight came out 333/20/80, matching vc's independently-taken count exactly.** This is the answer to _zero is what a broken instrument returns most convincingly_, done before the zero was reported rather than after it was doubted.

- **AN ARM CANNOT REPORT THAT IT IS BLIND -- IT PRINTS ITS HEALTHY COUNT JUST AS CHEERFULLY ON THE DAY IT CANNOT SEE THE DOOR** (dc, `7f67a285`, by mutation rather than by review, 2026-08-27). Reverting `prose_stripped` to the old strip: **arm 10 stays GREEN and still says `(5 examined)`**, while the new arm 11 goes red naming both fixtures by name -- `live_backtick(want RED, got GREEN) live_heredoc(want RED, got GREEN)`. **The count is the thing that reads as health and it is exactly what survives the blinding.** This is the cardinal false green in a gate four nodes commit through, and **the remedy is structural rather than a better regex: planted fixtures driven through the REAL census, as an ARM, so the blindness is reportable instead of demonstrable.** dc's framing: it converts a defect demonstrated once into one the gate enforces -- the same move as rostering the skew check. **And the two-sided proof matters: blinding `inert_heredoc_lines` instead takes arm 10 RED on the real tree and arm 11 RED on the inert fixture, so both halves fail when broken.**
- **WHERE A PREDICATE CANNOT DECIDE, ERR TOWARD THE NOISY ANSWER: A WRONG _LIVE_ FAILS A CORRECT FILE LOUDLY; A WRONG _INERT_ HIDES A BUILD BEHIND A GREEN ARM** (dc, 2026-08-27). Whether a bare backtick executes depends on the enclosing heredoc, **whose delimiter is on a different line, so no per-line pattern can decide it** -- which is why the defect had been filed rather than fixed. `inert_heredoc_lines` tracks the state across the file and the predicate is now three-way: a quoted-heredoc body is inert whole, an ESCAPED span is literal inside double quotes and stays stripped, an UNESCAPED one is command substitution and is kept. **The asymmetry is the ruling, not the parser** -- and it generalises to every classifier with an unrecognised case, which is the same rule as _an instrument's defects must fail toward alarm_. **All three GREEN fixtures are forms live in the repo TODAY, which is what stops the fix being "refuse everything"** -- a gate that refuses everything passes a one-sided test identically to a correct one.

- **A HOLE THAT IS BENIGN BY LUCK OF _ORDER_ IS THE WORST KIND, BECAUSE NOTHING REPORTS IT UNTIL THE DAY IT MATTERS** (dc, 2026-08-27, measuring rather than filing). The census's single-quote strip pairs quotes left to right, so **a stray apostrophe in double-quoted prose can pair with the opening of a later real single-quoted span and delete everything between them, live `cargo build --release` included.** `cmd/macos:278` carries exactly that shape -- three apostrophes, a real pair around `'int macos stage'` plus a stray one in `peer's` -- **and is safe only because the real pair comes FIRST and strips correctly, leaving the stray with nothing to pair with. Reverse the order and the line goes blind.** **Population, scoped to what the census actually reads: 14 lines examined, 1 ambiguous, 0 currently mis-classified.** The remedy needed no lexer, only the asymmetry made decidable -- **an ODD apostrophe count means the pairing cannot be decided, so the strip does not run and the line is LIVE** -- and driving it across all 14 changed **0 verdicts**, so the fix is free on this tree. **The fixture matters more than the four lines: the dangerous shape does not exist in the tree today, so without a planted RED case a fix closes the hole for the current file and reopens it for the next one written.**
- **RULED PERMANENTLY, NOT FILED: NO HAND-ROLLED LEXER IN A GATE FOUR NODES COMMIT THROUGH** (vc, on dc's finding, 2026-08-27). A shell lexer is a large new surface with its own unmutated failure modes, sitting in the path of every commit anyone makes, to close a hole that has produced no false green anyone has found -- **the instrument would become bigger than the thing it guards.** If one is ever genuinely needed it is a tool with its own tests and its own lane, never an arm. **The form of the ruling is the point: dc had it as "not now, filed", and _filed_ invites the next person with more time and less context to unfile it. A permanent ruling about surface area survives that; a deferral does not.**
- **A POPULATION IS TAKEN FROM WHAT THE INSTRUMENT ACTUALLY READS, NOT FROM THE REPOSITORY** (dc, correcting vc's question rather than answering it, 2026-08-27). vc asked _"how many files in this repo carry the ambiguous shape"_; dc answered for the **census scope** -- 54 files, 14 lines -- because **`prose_stripped` is never called on a line that does not mention cargo, so a repo-wide count would be a larger number about nothing.** **The question had the wrong denominator in it**, and answering it as asked would have produced a true figure that overstated the exposure by an unknown multiple. Same rule as _a sweep takes its population from the artefact's own declaration, never by traversal_, arriving in a question rather than in a sweep.

## PORT LEDGER (serial, hv's order, 2026-08-26)

**Procedure per project, every step shown to hv, every write by the pinned pair by absolute path:** 0 pre-read (stamp, HEAD, dirty, hook door, bucket dirs, `organize` preview, `doctor`, verifier, `st list`, at-accounting against the pre-migration ref) -> 1 `upgrade` on the pair (a v2 tree: rehearse on a throwaway clone FIRST) -> 2 the bucket prose INTO CANON by `ingest-buckets.sh` (`st attach` per file, byte-verified, a dir pruned only when every file under it is verified or modelled) -- **NOTHING IS MOVED TO `intent/history/`, WHICH IS THE MONTHLY DONE RECORD AND NOTHING ELSE** (hv, 2026-08-27) -> 3 accounting to 0 loss -> 4 canon and config (`.intentfiles` WIP-only, `events.jsonl` and `intent/.backup/` ignored, root canon, settings, hook chain, skills) -> 5 verify (`organize` 0 unclaimed, `doctor` 0 -- a 48h reading, verifier 0 failed, `st list` rc 0, `ac gate` per WIP thread, `todo`/`sync` idempotent) -> 6 commit through the project's own gate, pair sha in the message. `deps/`, `_build/`, `node_modules/`, `.worktrees/` are not projects (hv).

| #   | project            | pair                 | result                                                                                                                                                                                                             | commit                | notes                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | ------------------ | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Anvil              | a1af59f3             | CLOSED -- organize 0, doctor 0, verifier 16/16, st list rc 0                                                                                                                                                       | `442c563`             | ST0001 bucket -> history (6 files, digests identical; 5 were already attachments -- `4c9368c` attached but never pruned); ST0005 `completed: 2025-07-24` from commit `4b2a18a` via canon edit -> `sync --to-store` -> `sync --to-disk`; uncited PDF out of `intent/`; `intent/.backup/` ignored; ST0004 gate BLOCKED on an empty contract (Anvil's own work)                                                            |
| 2   | Conflab            | a1af59f3             | BLOCKED -- `upgrade` PANICS `legacy.rs:1844` byte-index slice inside `✓`; rehearsed on a scratch clone, real tree untouched (stamp 2.19.0, 0 canon, 0 files)                                                       | --                    | 123 threads (113 COMPLETED / 2 CANCELLED / 4 NOT-STARTED / 4 WIP), 9 v2 issues, 4 acceptance.md with 140 `status:` rows, ✓ in `ST0121/acceptance.md:72,83`, 5 untracked `.DS_Store`; hook chain from Aug 14                                                                                                                                                                                                             |
| 3   | Arca/arca_notionex | a1af59f3             | CLOSED -- organize 0, doctor 0, verifier 16/16 (full treatment; predates hv's "old projects: port and move on")                                                                                                    | `85bc15e` + `379a608` | ST0009 bucket -> history (5 files; design/impl already attachments); ST0006/07 dated 2026-01-08, ST0010 2026-01-12 from the commits that set Completed; three docs repointed; `intent/.backup/` ignored; config.json key order normalised by upgrade, committed separately                                                                                                                                              |
| 4   | Baize              | a1af59f3 -> 43cee2be | CLOSED -- organize 0, verifier 16/16, doctor 0 findings + 66 advisories (rc 0 on the fixed pair)                                                                                                                   | `31b3bdb`             | ST0001 bucket -> history (13 files); NOT-STARTED/ held only a .DS_Store; ST0025/02 `wp done` (hv); two citations repointed (ST0011 design.md is an attachment now); the 66 advisories are v2 `file::name` AT citations on six live threads, rewritten when each is next touched                                                                                                                                         |
| 5   | Cdsync             | a1af59f3             | CLOSED -- already clean; organize 0, doctor 0, verifier 16/16                                                                                                                                                      | `125524c`             | one line: `intent/.backup/` ignored; one untracked .DS_Store                                                                                                                                                                                                                                                                                                                                                            |
| 6   | Arca/arca_config   | 43cee2be             | CLOSED -- organize 0 / 0 to remove / 0 refused, doctor 0, verifier 16/16                                                                                                                                           | `ffebd4a`             | buckets ST0001+ST0002 -> history (22 files); the two closed threads' 10 generated views dehydrated BY HAND (organize refuses: "this project declares no dehydration preconditions"; Anvil `fd15595` precedent); ST0002 `related` minus the cross-repo ST0011 (objects, not strings); WP-06 walked Not Started -> WIP -> Done (`wp done` is declared only from wip); `upgrade` back-filled `backup.schedule: daily` (dc) |
| 7   | Arca/arca_cli      | 43cee2be             | BLOCKED on cc -- ST0011: 3 prose `covers` clauses carried as ids + 16 `path, describe "…"` subjects kept whole as `file` -> 16 "cites a file that does not exist" (every file exists) -> five WPs Done-but-BLOCKED | --                    | no buckets, organize 0, verifier 16/16; ST0009 undated (2025-10-06, a868e2d); fix = cc's parser, then re-convert ST0011 from the v2 source, then the port                                                                                                                                                                                                                                                               |

| 8 | Devbin | f7240814 | CLOSED -- doctor 0 | `a2cd632` | already carried by devbin/vc during the audit; two lines outstanding (`intent/.backup/` ignored, schedule back-filled) |
| 9 | MicroGPTEx | f7240814 | CLOSED -- doctor 0 | `14e3147` + `bad7845` | three docs repointed, two .DS_Store; **`14e3147` MANGLED ten citations** and I verified it against a diff filtered to three lines -- `bad7845` reverts and redoes them per citation shape |
| 10 | Molt | f7240814 | CLOSED -- doctor 0 | `fb053ab` | ST0002 dated 2026-03-23 (712bebc) |
| 11 | Riffle | f7240814 | CLOSED -- doctor 0 | `eedfea1` + `26068ff` | citations to the store; the second commit is the one my sentence-matched grep missed on a long line |
| 12 | Prolix | f7240814 | CLOSED -- doctor 0 | `f44edfd` + `464ecde` | ten threads dated 2025-11-17..2025-12-18; a cross-repo citation into Devbin's moved buckets; **the formatter was a second writer of two views** -- hv's own 2026-08-19 ruling (ST0056 AC-03.17) applied here, views re-emitted, skew gone |
| 13 | Utilz | f7240814 | PORTED, ONE FINDING LEFT | `b3f6a6d` + `3a611b6` | four threads dated 2026-03-02/04-23; eight citations; **ST0009/WP-01 Done over a BLOCKED gate is the migrator**, three prose subjects containing `bin/` taken for paths -- named, not patched |
| 14 | A3/a3-content | f7240814 | CLOSED -- doctor 0 | `1766a20` | three empty bucket dirs and two .DS_Store; nothing to carry |
| 15 | Laksa | f7240814 | MECHANICAL HALF DONE, 16 findings left | (committing at the fold) | 831 files -> `intent/history/` (75 threads); 27 threads dated from the commit that added their info.md under its bucket; formatter exclusion; **2 findings are the migrator (cc), 14 are Laksa's own work state (hv)** |
| 16 | Lamplight | -- | NOT STARTED | -- | the biggest: 3740 unclaimed, 10 lost citations, 3 view-skew, two stale `.worktrees` at 2.19.0 |
| 17 | Intent | -- | NOT STARTED | -- | ST0055's five WPs, ST0057/WP-08 (ic's), my two shell reds, the PASSES-branch remedy text |

### LEDGER CORRECTED 2026-08-27 -- MEASURED ACROSS ALL 17 ESTATES, NOT READ OFF THE TABLE ABOVE

**THE ROWS ABOVE ARE SUPERSEDED AND KEPT RATHER THAN EDITED, because the failure they record is more useful than the state they got wrong: THIS LEDGER WAS TWO DAYS STALE IN BOTH DIRECTIONS AND I WAS STILL REPORTING FROM IT.** I told hv for two days that Lamplight's 42 rows and Laksa's guard patch blocked the port. Both estates were already ported. I dispatched lamplight-vc to re-drive a hop 2 on a tree whose state I had MEASURED AT SESSION START -- my own output printed `config: 3.0.0` and `canon: PRESENT` -- and only that script's own preconditions stopped it.

**MEASURED STATE (`intent_version`, `intent/.canon` presence, canon thread count), 2026-08-27:**

| state                                                         | estates                                                                                                                                               |
| ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| **on v3.0.0 WITH canon**                                      | Anvil 6, Baize 25, Cdsync 4, Courses 2, Devbin 2, **Intent 64**, **Laksa 110**, **Lamplight 353**, MicroGPTEx 3, Molt 2, Prolix 28, Riffle 5, Utilz 9 |
| **v3.0.0, no canon, ZERO threads -- nothing to port**         | Molt-flynn, Molt-matts                                                                                                                                |
| **2.19.0, FROZEN by hv's standing rule -- correct, leave it** | Intentv2                                                                                                                                              |
| **2.19.0, no canon -- THE ONLY GENUINE REMAINDER**            | **Conflab** (4 threads; ST0121 carries 93 AC/AT rows; `.gitignore` has NEITHER `intent/.cache` NOR `intent/events.jsonl`)                             |

**LAMPLIGHT'S PORT LANDED AT `4ffd0fe1f` (26 Aug 19:37, hops 2+3), then `6b946eb21` (3744 bucket files to history, 89 threads dated, 275 findings -> 157), then ac's `8daaf3065`.** The port rewrote 22 `acceptance.md` files, 875 insertions against 1480 deletions -- **so the acceptance prose the "42 AC rows" question was about NO LONGER EXISTS, and that item is void rather than pending.**

**AND NOBODY KNOWS THE PREDICATE BEHIND "42".** lamplight-vc tried to reconstruct it -- AC rows, four fields, not `(non-test)` -- and got **4 rows across 2 threads**. It reported its counts as UNUSABLE rather than reconciling them, which is the correct handling. **The figure reached me as a number and I passed it on as one, for two days, to the node whose ruling depended on it.**

**WHAT lamplight-vc's STORE CENSUS FOUND INSTEAD, and it reframes the ruling rather than answering it (2616 criteria, 1731 tests, 353 threads):** `evidence` is a structured key on **830 criteria already**; `red-first`, `on`, `control` and `mutation` are **NOT KEYS ANYWHERE**, with `red-first` surviving as text in 39 of 353 files. **So a question posed about four fields is really about the three that never became structure, and the largest member was decided before anyone asked.**

**THE RULE THIS LEDGER NOW CARRIES: RE-MEASURE THE QUEUE, NEVER REPORT IT.** A ledger is a record of what was done, not a statement of what is true now, and the two diverge the moment another node commits. Drive `intent_version` + `intent/.canon` across `~/Devel/prj` before answering any question about port state.

**LEARNED, port:**

- **A SENTENCE-MATCHED REWRITE MISSES WHAT SITS MID-LINE; SWEEP BY THE PATH.** Four of twelve closed projects still named a bucket after their own citation pass, including two CODE COMMENTS in Cdsync pointing into Utilz. The path grep found them all at once.
- **A BLIND `sed` OVER A PATH REWRITES THE CITATIONS THE CAREFUL PATTERNS DELIBERATELY SKIPPED** (MicroGPTEx, mine): ten lines became `` `the store (`intent st show <ID>`) ST0001/info.md` ``. **AND I VERIFIED IT WITH A FILTERED DIFF**: three lines read, ten changed, a stat line saying 15 insertions that I did not reconcile.
- **REVERT AND REDO BEATS PATCHING A PATCH**: `git checkout HEAD~1 -- <files>` then rewrite per citation SHAPE, never per string.
- **THE FORMATTER IS A SECOND WRITER AND A SKEW THAT SURVIVES ITS OWN REMEDY IS THE TELL** (Prolix, Laksa). hv ruled it in Intent on 2026-08-19; six fleet projects still run the hook without the exclusion.
- **`git log` INSIDE A `while read` LOOP EATS THE LOOP'S OWN STDIN** -- 27 threads, 29 ms each, and the loop still timed out at five minutes having written zero rows. One streaming `git log --diff-filter=A --name-only` over the bucket paths answered all 27 in 35 ms.
- **`-S` WITH `--follow` DOES NOT FINISH ON A 1.09 GiB HISTORY**, and unbounded `-S` reads TODAY's migration commit as the closure date. Bound the search at the pre-migration ref.
- **AN ADOPTION IS NOT A PORT**: `intent init` on a repo with no `intent/` tree is a decision about what the project IS, and hv made it (llm-tropes and Pplr: not Intent projects). My init was reverted whole -- it had only added untracked paths, so nothing was lost.
- **A DEFECT MEASURED ONCE IS A DEFECT; MEASURED FIVE TIMES IN FIVE ESTATES IT IS A CLASS.** The `is_path` heuristic (`contains('/') && !contains(':')`) meets prose, command lines and annotations. Each estate that exhibits it is EVIDENCE -- hand-editing the rows would destroy the corpus that proves the fix.
- **A COMMIT THROUGH A BIG PROJECT'S GATE OUTLIVES A FIVE-MINUTE TOOL TIMEOUT** (Laksa, 863 files): the `add` had landed and the `commit` was still in its critics. Re-running it in the background is right; killing and retrying in the foreground would have raced the same gate.

- **REHEARSE A MIGRATION ON A THROWAWAY CLONE BEFORE THE REAL TREE.** Conflab's panic cost one `git clone` into scratch and touched nothing; on the real tree it would have been a crashed process over 123 threads with the hook chain half-refreshed.
- **ATTACH WITHOUT PRUNE LEAVES A PROVEN-CARRIED BUCKET UNCLAIMED FOREVER** (Anvil `4c9368c`): `organize` counts files, not provenance, so five sha-identical attachments still read as 7 unclaimed.
- **A BYTE-WALKING PARSER OVER PROSE MEETS A MULTIBYTE CHARACTER ON THE FIRST REAL ESTATE THAT HAS ONE.** 12 fixtures and 4 mutations, all ASCII; Conflab has a `✓` in an AC row. The fixture set must carry one non-ASCII row.
- **A DEFAULT IN CODE IS INDISTINGUISHABLE FROM A HALLUCINATED RULE TO THE USER WHO MEETS IT** (hv, on `backup-stale ... 24h`): D35 ruled config, ic named `backup.schedule`, cc read `every_hours` and `unwrap_or(24)`, nothing reads the ratified keys, `config` is a stub, the daemon has no backup code. The finding was right; the rule was invisible.
- **MY VERIFIER'S `.backup/` ARM READS THE ROOT ONLY;** the store's snapshot lives at `intent/.backup/` and was untracked under a PASS.
- **A WORKTREE NEVER RECEIVES THE INSTALLER-WRITTEN GATE** (`.githooks/pre-commit.intent` is gitignored on purpose), and the shipped hook REFUSES rather than committing unguarded: "GATE ABSENT". Commit on main, or install the gate in the worktree; never `--no-verify`.
- **`git diff` OF A TREE WHOSE CHANGES ARE STAGED IS EMPTY.** A refused commit leaves the index staged; the patch I took was nothing, and `01dec831` went through with the tests and none of the code. `git diff --cached`, or `git diff HEAD`. And a `checkout --detach` carries staged changes with it: the first pair stamped `dirty-`.
- **CARGO DOES NOT RERUN A BUILD SCRIPT WHOSE CRATE DID NOT CHANGE**, so `intentd`'s baked stamp survived a rebuild that changed only `intent-cli`; `cargo clean -p intentd` first (dc's guarded build does).
- **`pgrep -fl 'cargo|rustc'` COUNTED THE THREE NODE SESSIONS' OWN COMMAND LINES** -- their system prompts contain the words. "81 live cargo processes" all evening, and nothing was building. `pgrep -x cargo`. The runbook already carried this lesson from 17:34Z, in my own hand.
- **`wp done` IS DECLARED ONLY FROM `wip`** and refuses the shortcut from Not Started with the route named; walk it. **`related` IS A LIST OF `{id, note}` OBJECTS** -- a string filter removes nothing and reports nothing.
- **ORGANIZE REFUSES TO DEHYDRATE UNTIL THE PROJECT DECLARES ITS PRECONDITIONS** ("has not proved it can put them back"); closed threads' generated views on an estate that never declared them go by hand, as hv did in Anvil.
- **A GATE'S "FILE DOES NOT EXIST" WAS A CITATION SPLIT THAT KEPT `, describe "…"` IN THE PATH.** Sixteen rows, every file on disk. Check the stored value before believing the estate lost something.
- **BARE `intent` IS THE KEG UNTIL A LINK CHANGES; A REBUILD MOVES THE SYMLINKS AT PATH 17 AND 19, NOT PATH 1.** hv read the pre-fix doctor from the keg and asked what was happening with Baize.

## LEARNED 2026-08-27 -- THE HANDOVER DAY (each with the node that found it)

**THE DAY'S ONE CLASS, AND SIX NODES MET IT FROM SIX DIRECTIONS: A VALUE HANDED FROM ONE NODE TO ANOTHER OUTLIVES THE THING IT DESCRIBED, AND NOTHING IN THE HANDOVER SAYS SO.**

- **HAND OVER PROPERTIES, NEVER VALUES** (vc, by doing the opposite). I gave lamplight-vc `38bd566c` for a measurement and cc rebuilt to `d2f70e03` before they read it -- `dispatch-table.json` is `include_str!`'d, so a help-string fix is a COMPILED input. **The pair moved three times in one afternoon.** The durable form is the properties: `--is-ancestor` for `2cf8fa63` and `7e27e0ca`, plus `git diff --name-only <pair>..HEAD -- native/rust surface` EMPTY. Those are true across every rebuild. **The recipient should never have to decide whether a hash mismatch matters.**
- **THE VERSION MARKER IS NOT AN IDENTITY, SO A TWO-SIDED CHECK BUILT ON IT IS ONE-SIDED** (cc, catching vc). I told lamplight-vc to compare `intent --version` at both ends of a run to detect a mid-run swap. `--version` prints the COMMIT MARKER; three distinct binaries carried `dirty-18197aaf` in one day on 2026-08-18. **So the check catches a rebuild at a DIFFERENT commit and is blind to a rebuild at the SAME one** -- a retry, or an interrupted build restarted. Use `shasum -a 256 "$(command -v intent)"`, and **discard on a FAILED read as well as a differing one: the rebuild window is an ABSENT binary, not a changed one.**
- **THE TWO COMPOSE RATHER THAN COMPETE** (cc). Properties are what you hand downstream -- _is this the right build_. The sha is what an in-flight run compares against itself -- _is this the same build as five minutes ago_. Different questions; both needed.
- **A BUILD IS THE DELIVERY, ESTATE-WIDE, THE INSTANT IT FINISHES** (cc). `~/.local/bin/intent` and `intentd` are SYMLINKS into `target/release/`. There is no install step. So a rebuild does not merely REMOVE the binary for its duration -- **it SWAPS it under any run already in progress, in every estate at once.** Hence a build hold whenever an estate is being measured. The window is real and was met, not argued: a probe returned `no such file or directory` on the release binary mid-build.
- **THE MARKER NAMES THE LAST COMMIT TOUCHING A COMPILED INPUT** (cc). `ea85ce5e` -- the `--name` fix to the launcher -- is in the tree and correctly absent from the marker, being a shell script. **_The marker predates that fix_ is the sentence that sends someone rebuilding for nothing.**

**TWO PROPAGATION CLASSES IN ONE DIRECTORY, AND NOTHING ON THE PAGE SAYS WHICH IS WHICH** (vc). `lib/templates/hooks/pre-commit.sh` is THE GATE and is **COPIED** per-estate as `pre-commit.intent`; the guard bodies and runner are **READ LIVE** at run time out of `$GUARD_HOME/lib/templates/hooks/`. **So a guard fix reaches 17 estates the moment it lands and a gate fix reaches nobody** -- `4d9e70c2` was installed in ZERO estates including the tree that wrote it, and Intent's own installed gate was six days and 59 lines behind its own template. Three generations in the field: G1 2 (no guard block at all), G2 14 (absence branches key on `INTENT_HOME_RESOLVED`), G3 1 (current). `fleet-hook-census.sh` measures it; `gate-shim-contract.md` closes it.

**THE ORDERING QUESTION WAS NEVER A POLICY DISAGREEMENT** (vc, from dc's and cc's findings). Three instruments gave three answers to _what should a guard do when its subject is absent_. **They share ONE dependency: the gate runs `intent info` to find `INTENT_HOME`, so the guard arm and the critic arm go blind together.** Split it -- the shim locates the gate without the binary -- and the guards run with no binary at all (ABSENCE 1, its fall-through and the inversion have no subject) while the critic arm refuses, obviously correctly, because it is then the only arm that needs one. **Some questions are dissolved rather than answered, and the tell is three careful people disagreeing about something none of them chose.**

**A WRONG COUNT WITH A NAMED CAUSE CLOSES THE QUESTION; A BARE WRONG COUNT INVITES A RE-MEASURE** (lamplight-vc, against itself, and the sharpest thing anyone said today). It reported attachments at 480 against a true 80, then attributed the move to a nearby commit **on adjacency alone** -- same morning, said "attachment drift", nearest in the log. Driven: that commit adds ZERO lines carrying a `path` key. **The reader then has to disprove two things instead of one, and the story is what stops them trying.**

- **`jq -r '.x[]?' | wc -l` COUNTS LINES, NOT ELEMENTS** (lamplight-vc). `-r` pretty-prints each object across one line per key; 80 attachments x 6 keys = 480. **The failure mode is that 480 is a perfectly plausible attachment count** -- not an error, not out of range, nothing in the output saying "these are lines". Use `jq -s '[.[].x // [] | length] | add'`. **Had it printed 4,000,000 it would have been caught in a second.**

**A BELIEF CAN OUTLIVE A MEASUREMENT SITTING IN YOUR OWN TOOL OUTPUT** (vc). I ran a state check on Lamplight at session start; it printed `config: 3.0.0` and `canon: PRESENT`. **I read it and then dispatched lamplight-vc to re-drive a hop 2 on a tree I believed was 2.19.0 and fresh.** Only my own script's two preconditions stopped it. **The port ledger was two days stale: Lamplight AND Laksa were already ported, and the "42 rows block the port" item I had been carrying to hv was void.** Re-measure the queue; do not report it from memory.

**THE POSITIVE CONTROL IS THE ONLY THING THAT CATCHES A ZERO** (vc). My first fleet hook sweep returned **0 of 17 estates**. It honoured `core.hooksPath` -- which was the miss in dc's version -- and then keyed on the file named `pre-commit`, **which in a chained install is a 114-line WRAPPER; the gate is `pre-commit.intent`.** Nothing about a zero looked wrong. The control -- _the tree this runs from must appear in its own carrying set_ -- cost nothing and was the whole difference.

**A TRAP THAT ANNOUNCES A RESTORE IT DID NOT PERFORM IS WORSE THAN NO TRAP, BECAUSE NO TRAP MAKES YOU CHECK** (cc). Relative paths in a `trap` firing after a `cd`: both restores failed, `[restored]` printed, a live mutation left in `intentfiles.rs`. **`git diff --quiet -- <file>` is the only statement that settles it** -- grepping for the mutation string proves the string is gone, not that the file is the file. **dc's answer is better than the fix: MUTATE A COPY, so there is no restore to get wrong** (`ad67db65`).

**AN INSTRUMENT WHOSE KNOB IS NOT CONNECTED REPORTS SUCCESS** (dc). `ROOT=<fixture> bash thread_view_skew_check.sh` ran against the REAL tree and printed a pass, because line 70 is an unconditional assignment rather than `${ROOT:-...}`. **What caught it was that both arms printed byte-identical output -- two arms that cannot disagree are not two arms.** Portable, and cheaper than auditing the script.

**A CORRECT INSTRUMENT, CORRECTLY DRIVEN, TWO-SIDED, POINTED AT THE WRONG SUBJECT** (dc, against itself). dc drove the fail-open two-sided at fixture depth and reported `rc=1` in the present tense about running systems -- having driven `lib/templates/hooks/pre-commit.sh`, **the TEMPLATE, which no estate executes.** Nothing about the drive was weak. **A template is a source, not a deployment, and the gap between them is an upgrade nobody has performed yet.**

**A COUNT INHERITED FOR ONE POPULATION AND REUSED FOR ANOTHER** (cc, against itself). "16 estates" was an ESTATE count reused as a GUARD-GENERATION count. **Different populations, same integer, and nothing flinches.**

**THE ENVIRONMENT IS PART OF THE SUBJECT UNDER TEST, AND THE ERROR WILL NAME THE SUBJECT** (ic, twice in one session). `CARGO_TARGET_DIR` outside the worktree broke every test that locates the install by walking up from the binary; MOVING the target left `CARGO_BIN_EXE_*` -- baked at compile time -- pointing at the old path, killing eleven tests in untouched code. **When a harness fails, ask what the harness changed about the world before reading the failure as being about the code.**

**PROVEN-NOT-TO-BREAK-ANYTHING IS A DIFFERENT SENTENCE FROM PROVEN** (ic). Gutting a newly-widened loop left the suite green -- no row currently reaches that path. Kept anyway, with the reason written down: **the sibling loop was safe by accident of scope too, right until someone widened it.**

**DERIVE THE SCOPE, DO NOT ASSERT IT** (ic). A check that hand-walked `table.families` could not see the eleven top-level `new_surface[]` rows -- thirteen flags never asked whether anything read them. Rewired through `dispatch::shipped_entries`, **the chain the binary itself dispatches through, so the check can no longer cover a population the binary does not.** Same move as rostering the skew check and as arm 11.

**A SESSION IS NAMED AT LAUNCH, NOT ASKED TO RENAME ITSELF** (hv's question, vc's fix at `ea85ce5e`). `claude -n/--name` sets the display name in the prompt box, the `/resume` picker and the terminal title. **A name set at exec is right from the FIRST frame and does not depend on the session honouring an instruction -- and a session that dies before its first turn is still identifiable in `/resume`, which is exactly when you need to find it.**

**A DRY RUN THAT OMITS A FLAG THE REAL RUN PASSES IS NOT A PREVIEW OF THE THING PREVIEWED** (vc). Trivial to fix, and the same family as everything above.

**A CONDITIONAL COLLAPSED INTO AN ASSERTION, AND IT IS THE MOST EXPENSIVE OF THE DAY DESPITE BEING THE LEAST DRAMATIC** (vc, caught by cc). I told hv and cc that the fail-open ordering question was "now dissolved by the shim contract". The honest sentence is **it dissolves IF hv approves R1+R2** -- `47aa84c7` is a PROPOSAL and I gave it the standing of a decision. cc refused it on a routing rule (_a ruling that reaches me through a peer is not a ruling_) and then recorded mine instead, because **the fault is in the CLAIM, not the channel: it would have been just as wrong read directly off my board.**

- **THE REASON IT IS THE EXPENSIVE ONE: THE OTHER THREE WERE CAUGHT BY INSTRUMENTS AND THIS ONE WOULD HAVE BEEN CLOSED BY AGREEMENT** (vc, sharpened by cc). A stale hash gets re-derived; a hold gets questioned; a run gets refused by its own preconditions. **But "we both think it is dissolved" produces no artefact, and the question is simply gone.** Two nodes concurring leaves nothing behind that a check can refuse.
- **A BOSS NODE'S RELAYS NEED THE RULE MORE, NOT LESS.** They carry more weight, so they are the ones most likely to be spent as rulings. cc's working distinction, kept: **take vc's DISPATCHES immediately, refuse vc's RULINGS as rulings.**

**CARE DOES NOT SCALE ACROSS FIVE NODES AND A FLEET; A PRECONDITION THAT REFUSES ITS OWN OPERATOR DOES** (cc, and the day's closing argument). cc's 26 green bats arms were consistent with the fail-open being live in 14 estates and **no amount of care would have said otherwise -- only an instrument that could exhibit the case.** lamplight-vc's script refused ME this afternoon, and that is the instrument working rather than the operator failing gracefully. **Prefer building the refusal to resolving to be more careful.**

**ANY STANDING CONSTRAINT SHOULD BE ABLE TO SAY WHAT IT IS CURRENTLY PROTECTING, IN THE PRESENT TENSE** (vc). I froze cc's builds to protect a hop-2 measurement that was then cancelled, and left the freeze standing for an hour over nothing. Generalises past holds to every freeze, pin, gate and exemption in the estate.

### LEARNED, THE EVENING OF 2026-08-27 -- THE ROUTE MOVED WHILE WE WERE DELIVERING ALONG IT

**A COUNT SAYS A STRING IS ABSENT; THE _ORDER_ SAYS THE DECISION HAS NOT ARRIVED** (dc, correcting cc's description of cc's own fix and vc's relay of it). We read the Cellar dispatcher as _refusing 0, skipping N_ and disagreed on N -- **the file carries four `skipping.` lines (`:35` git, `:41` worktree, `:283` CLI, `:296` config) and we had counted different subsets.** The line numbers carry what neither grep does: **CLI check at `:283`, config test at `:296` -- the OLD ORDER.** `4d9e70c2` is precisely the swap that puts the config test first. **So the refusal is safe BECAUSE of the reorder** -- with the CLI test first it would block commits in any repo merely lacking the CLI; with the config test first it runs only where the gate was declared. vc had relayed those as two merits all day and they are one mechanism.

**WHEN A CONTROL CLEARS YOUR DIFF, THE NEXT QUESTION IS WHAT _BOTH SIDES_ OF IT WERE STANDING ON** (cc, and the sharpest form anyone reached of the day's second family). cc cleared two bats failures as pre-existing by driving them on a pristine worktree at HEAD -- correct, and it proved only _not my diff_. **They were not a code defect at all: they were the Cellar on PATH, and they went green when the unlink landed with cc's file unchanged but for a comment.** Both worktrees shared one PATH. **A control that varies what you changed and holds the environment constant cannot see a defect in the environment, and it reads as a clean acquittal every time.** Sits above vc's `CARGO_TARGET_DIR` instance and ic's `CARGO_BIN_EXE_*` one.

**THE ROUTING ANSWER INVERTED THREE TIMES IN ONE WORKING DAY AND NOTHING ANNOUNCED ANY OF IT** (dc). Memory said the fleet ran the frozen Intentv2; dc measured it running this tree at 15:00Z; `brew link` made it the Cellar at 16:36Z. **A delivery target that can move without announcing itself defeats any fix shipped through it** -- a stronger argument for the gate shim than the drift the contract was written on: **not that copies drift, but that the ROUTE moves under you while you are delivering along it.**

- **PATH PRECEDENCE IS NOT A REGRESSION AND LOOKS EXACTLY LIKE ONE** (cc). After `brew link`, hv's morning blocker was live again -- `intent claude start` rc=2, `--note` gone -- with **nothing lost**: the dev symlinks were intact and still pointed at a build carrying everything. `/opt/homebrew/bin` is PATH position 1; `~/.local/bin` is 17.
- **UNLINK ALONE DOES NOT HOLD -- RE-PIN** (devbin-vc, adding a step vc's relay of hv's ruling did not name). The formula's PIN is `use dev`'s durability mechanism: **its whole job is that `brew upgrade` reinstalls and RE-LINKS**, silently putting the fleet back on the keg. Unpinned, hv's ruling would have expired at the next routine brew command. **A ruling implemented as half its mechanism is a ruling with a timer on it.**
- **RIGHT-BUT-INSUFFICIENT: devbin-vc measured the seven guard bodies byte-identical, saw `pre-commit.sh` differ, and stopped at _does not affect today_.** The unasked question was what it affects TOMORROW -- **every `intent upgrade` in any estate would install the pre-ruling dispatcher.** The difference was in hand and the smaller conclusion was drawn from it.

**THE WORST POSSIBLE SINGLE TEST CASE, IN BOTH DIRECTIONS AT ONCE** (lamplight-cc, sharpened by devbin-vc). Intent is **already** in the shim rollout's target state, so testing the rollout against Intent **acquits the whole fleet on the basis of the one project that had already been fixed** -- and Intent is simultaneously the one estate where the equivalent verb has fleet-wide blast radius. **An estate already in the target state gives no signal about the estates that are not.**

- **AND lamplight-cc CORRECTED ITS OWN GENERALISATION TWENTY MINUTES AFTER MAKING IT.** It warned that a `devbin` content-grep cannot see a chained alias (`ll -> lamplight -> devbin`) and said it would not assume Lamplight was unique. **Measured with `find bin -maxdepth 1 -type l` across the fleet: Lamplight IS unique.** The caution was true as stated and scoped to exactly one project. **A hazard measured is a hazard; a hazard generalised without measuring is a rumour with a citation.**

**THE SIGNATURE IS THE ENFORCEMENT** (cc, building `install::publish_home()`). It takes **no root argument** -- the value is `home()`'s answer and nothing else's. **A second writer is not something a comment discourages; it is something a caller cannot express.** Same shape: idempotence asserted on the **mtime**, not the return value, because returning `Unchanged` while rewriting satisfies a weaker test and still churns every consumer; and the disagreement case deliberately does **not** say _retry_, because the write returned success and the file says otherwise, so **a retry races the other writer until it wins by luck.**

**A DECISION THAT SCHEDULES ANOTHER DECISION BY IMPLICATION IS NOT YOURS TO MAKE** (cc, declining to pick its own caller). `publish_home()`'s caller decides **when 17 estates get a pointer written**, which is the sweep hv reserved. Picking one would have scheduled hv's announcement as a side effect. **Same refusal cc made of vc's "the ordering is dissolved" earlier the same day, and right both times.**

**A PATCH BUILT FOR A SUPERSEDED PLAN IS A STATE OUTLIVING ITS REASON** (dc, catching it in its own scratchpad). Its both-spellings tolerance existed to cover a window between the guard learning the new name and in-flight hooks carrying the old -- **and once hv ratified keeping the symlink, that window never existed and the tolerance was machinery guarding nothing.** Regenerated rather than left to sit wrong.

## THE SWEEP IS SCOPED, AND THE SCOPING FIGURE IS dc's (relayed by cc, 2026-08-27 18:00Z)

**MEASURED, NOT PROJECTED:**

```
17 estates carry the consumer gate
in ALL 17 the carrier is `pre-commit.intent` -- NEVER `pre-commit`
in ALL 17 ruling 4's refusing arm is ABSENT      -> in force in ZERO estates
hooksPath: .git/hooks (14), .githooks (Intent, Intentv2), bin/hooks (Laksa)
no gate:   llm-tropes, Pplr -- both already out of scope
```

**TWO FAULTS A NAIVE SWEEP WOULD HIT, AND THE SECOND SURVIVES FIXING THE FIRST.** Hardcoding `.git/hooks` misses three estates, including both Intent trees and Laksa. And the carrier is `pre-commit.intent`, so resolving the DIRECTORY correctly and then touching `pre-commit` inside it still misses every estate. **These are the same two faults `fleet-hook-census.sh` was rewritten around** -- its header names both, because vc's v1 keyed on `pre-commit` and returned ZERO of seventeen, caught only by the positive control. So the census is not exposed to either, and this figure and mine are measured against the same directories and the same filenames.

**THE METHODOLOGICAL POINT IS dc's AND IT IS WORTH MORE THAN THE FIGURE.** Their morning number -- _0 of 5 installed hooks carry the refusing arm_ -- **was correct, and was produced by grepping `pre-commit`, which carries the gate in no estate.** The instrument could not have returned anything else, in any tree, under any state of the world. The re-sweep carries a positive control (the arm pattern returns 1 against the template, so the detector demonstrably fires); the morning run had none.

> **A FALSE GREEN IS EASIER TO CATCH THAN A TRUE ONE. The true one gets cited later, and the citation carries no trace of the blindness.** (dc)

> **A PARTIAL FIX MAKES AN INSTRUMENT FEEL REPAIRED AND RETIRES THE SUSPICION THAT WOULD HAVE FOUND THE REST** -- and a repaired instrument is trusted more than an unexamined one. (cc, on dc's sequence: the directory half was corrected in the morning, and the filename half survived BECAUSE that correction had been made.)

**WHAT THIS DOES TO THE HOLD: NOTHING, AND BOTH NODES SAID SO UNPROMPTED.** It is better evidence FOR the sweep -- a deliberate ruling-4 fix, landed today, absent from the gate that runs on this repo's very next commit -- and simultaneously better evidence that a badly-scoped sweep would have missed three estates and touched the wrong file in the other fourteen. Both cut toward taking the time. **The sweep remains hv's, reserved specifically, and a general pen grant does not overturn a specific reservation.**

## ROOT-AND-BRANCH HIGHLANDER REVIEW OF THE v3 RUST (hv's instruction, 2026-08-27 18:07Z)

**CORPUS:** 43,561 lines of source across 47 files, four crates (`intentsvcs` 34.4k / `intent-cli` 8.8k / `testkit` / `intentd`), plus 175 test files and `build-support`.

### Axis 1 -- copy-paste. CLEAN, and it is the weakest axis.

840 function bodies over 140 normalised characters. **ZERO exact duplicate bodies. Four near-duplicate pairs (Jaccard >= 0.80), and all four are artefacts of my own parser** -- one nested `fn` (the outer body contains the inner) and two mis-parsed bodies out of 1078, 0.3%, named: `ingest.rs:733 validator`, `contract.rs:157 resolves`, `contract.rs:165 carries_id`.

**THE CONTROL AND ITS LIMIT, STATED BECAUSE THE RESULT IS A NEGATIVE.** A planted exact copy of the largest real body IS reported, so the hashing and reporting work. **That control does NOT prove the PARSING is right** -- it is a copy of a body the parser already produced, so it would pass under a broken parser too. The parse is checked separately: 1078 of 1078 bodies extracted, 1075 ending in `}`. A negative result from a 99.7%-correct instrument, not from a perfect one.

### Axis 2 -- vocabularies with no home. WHERE EVERY REAL FINDING IS.

**F1 -- the `st.*` op vocabulary. HIGH. Built by ic at `2ddecb33`.** Three spelling sites (`transitions.rs` edge table, `facade.rs` `declared_list_edit`, and each verb's own literal), no type, bare `&'static str`. **The two consumers fail in OPPOSITE directions and only one is loud**: an unknown op reaching `check_transition` is refused; one reaching `declared_list_edit` hit `_ => None` and SILENTLY made no list edit. **The cost was already paid three times in one day** -- `6ff37c0f`, `cce816a4`, `26111785` are all one vocabulary handled in one table and not the other. Closed by a test deriving the vocabulary FROM the edge table, with `st.new` and `st.reinstate` named as the legitimate `None`s. **ic's ninth-op mutation found what F1 did not name:** `st.park` reds TWO tests for two DIFFERENT defects (unanswered, and unreachable), and a single assertion would have reported whichever it met first.

**F2 -- the `ThreadStatus` vocabulary in `transitions.rs`. LOW, DELIBERATELY DEFLATED, PARKED.** `model.rs:680` says in its own words that the vocabulary belongs to the type (issue 0041); `transitions.rs` references `ThreadStatus` **zero** times and spells all six as bare strings. It reads worse than it is: `mutation_completeness.rs` drives `exitless`, `traps` and `unreachable` over the values and is mutation-checked, so the completeness property is separately enforced.

**F4 -- `remedy.rs` is the ONE rendering, and the surface has four producers. cc's.** MODULES.md and the module header both claim it; the header says an implementor _"cannot accidentally invent a second rendering, because it does not have to write one."_ True of the trait. **`Failure::Error(String)` restores the ability**: 67 sites in `render.rs`, 54 hand-written `remedy:` literals, 3 hand-written `caused by:`. `error_remedies.rs` -- the test enforcing AC-04.4 -- has **zero** reach into any of them. **This is my own Highlander finding 4 surviving its own fix**: the trait removed the second convention among TYPED errors and left the string path outside the argument.

### Axis 3 -- registry claims. 20 rows make an explicit single-home claim; 4 are Rust.

`source_commit.rs` VERIFIED (three `build.rs`, all wrappers, all including it). `remedy.rs` FALSE as written -- see F4. `preconditions.rs` and `export.rs` not yet driven.

### Not this review's, and routed: F3 (`flag_reachability` coverage)

cc found the blind spot by driving; I measured the population and **got the split wrong** -- 33 can fire, 59 cannot, of 92 examined, not my 15/94. **I evaluated the gate's conjunct against the CURRENT source; the gate fires on the MUTATED one**, and a flag whose id appears exactly once loses its mention along with its accessor. **A coverage figure measured on the unmutated source is a claim about the wrong world.** My two-sided control passed and was worthless: it confirmed the instrument computed what I told it to, which cannot tell me I told it the wrong thing.

### OPEN, AND BOTH ARE hv's -- NOT DECIDED UNDER THE PEN

1. **F1's FORM: enum or test?** ~~I measured the population and it is empty: 15 estates carry `intent/events.jsonl`, all 15 are ZERO BYTES~~ **-- WITHDRAWN, 2026-08-27 18:09Z. THAT MEASUREMENT WAS TRUE AND BLIND, AND IT IS WATCH-OUT 0b ARRIVING ON ITS AUTHOR THE SAME AFTERNOON.** `events.jsonl` is zero bytes everywhere because it is an ON-DEMAND EXPORT, not the log -- `store.rs:397` says so in its own words. **The log is SQLite**: `intent/.cache/intent.db`, table `event_log`, written by `append_event`. ic caught it; I re-measured independently and reproduce them exactly:

```
17 estates with intent/ ; 15 carry a store ; 12 non-empty
1108 events, 22 distinct ops across 8 families (ac at attachment disk issues st todo wp)
this estate alone: 401 events, 21 distinct ops
```

**THE COMPAT SURFACE IS STILL EMPTY, FOR A COMPLETELY DIFFERENT REASON, AND THAT IS THE PART FOR hv.** All 22 stored ops are STILL SPELLED in current source -- 22 of 22, 0 unspelled, and my detector positive-controlled on two invented ops which it correctly reported unspelled. So an enum's parse would today meet nothing it does not know **not because no events exist, but because no op has been RENAMED OR RETIRED since they started being written.**

**THE TRIGGER IS THEREFORE NEITHER OF THE QUESTIONS WE BOTH ASKED.** Not _does the log carry data_ (it does, 1108 rows) and not _will events start flowing_ (they already have). It is: **the first time an op is renamed or retired, every developer machine's store carries rows an enum cannot parse.** That is mechanically checkable in one line -- for each distinct op in the store, does the source still spell it -- so hv can have a LIVE FIGURE rather than a forecast. It reads a per-machine DB so it can never be a commit gate; `doctor` is where a per-machine truth check belongs.

**TWO SCOPE CORRECTIONS TO F1, ic's:** an `Op` enum is **22 members across 8 families, not 8**; and four are `disk.*` (`sync_from_disk`, `sync_to_disk`, `hydrate`, `organize`) which have **no state machine at all**, so a type derived from the transitions table would not cover the vocabulary the log actually carries. F1's three spelling sites are the `st.*` SLICE of a wider set.

**AND D34 BOUNDS THE COST THE OTHER WAY:** `intent/.cache/` is gitignored, so no clone and no fresh machine inherits a single one of those 1108 rows. The eventual compat cost is bounded per-machine rather than growing with clones.

ic's recommendation is unchanged -- **not now** -- and the ground under it has changed completely: not "there is data to migrate", but that the type is a 22-member vocabulary over eight families, four of which have no machine to derive it from. Their two weaknesses stand in their words: the test reds at `cargo test` not `cargo build`, and it can be deleted where an exhaustive match cannot be forgotten. 2. **AT-11.4's reading** (ic): the blocked figure counts FILES and no line names the thread, so _"reports the non-WIP threads as to remove and blocked"_ is met by arithmetic that is only unambiguous with exactly one undeclared thread. If hv reads the criterion as wanting the thread NAMED, that is a build.

## THE v3 PORTER TRUNCATES AN AT CITATION, AND THE TOOL'S OWN REMEDY WOULD MAKE IT PERMANENT (lamplight-vc, hv-routed to Intent, 2026-08-27 18:35Z)

**THE PARSE.** A v2 AT row is `- AT-10.3 [<citation>] -- covers AC-10.3 -- status: n/a`. The bracket holds EITHER a file plus a quoted test name OR a free-text `n/a` justification. **The porter takes the bracket contents to be a bare path** and writes it into `tests[].file`. Two failure modes on Lamplight, 74 rows across 10 threads against 551 correctly-stored bare paths -- so the broken form is the minority and **the correct shape is not in doubt**:

- **TRUNCATION (52 rows).** The justification itself contains `--`, so the value is cut there. `"file": "[n/a"`, with everything after the first `--` absent from the store. lamplight-vc grepped the canon for a phrase from the tail and got 0, with another phrase from the same thread appearing 8 times as a **positive control**.
- **KEPT WHOLE BUT STILL NOT A PATH (22 rows)** where the annotation has no `--`.

**THE SYMPTOM IS A FALSE GATE, WHICH IS WHY IT OUTRANKS AN UNTIDY FIELD.** All 74 make `ac gate` emit `cites a file that does not exist`. ST0288 reads BLOCKED on 24 contract findings over 33 rows; five of five cited paths checked on disk EXIST, with those ATs recorded green. This one defect is what puts three threads at the top of 44 packages recorded Done with a blocked gate.

**NOTHING IS LOST YET AND THAT IS THE FIX'S WHOLE SHAPE.** The DISK is intact -- `acceptance.md` still carries the full line -- so **the STORE is the lossy side and a fixed hop 2 re-run recovers everything.** No hand-patching anywhere.

**AND THE SECOND HALF IS THE DANGEROUS ONE: THE TOOL RECOMMENDS THE COMMAND THAT WOULD MAKE IT PERMANENT.** `sync --to-disk` writes the STORE over the DISK, so on any thread carrying this defect the remedy converts a fully recoverable divergence into real data loss. Same shape as lamplight-vc's other finding today, where 23 view-skew rows are a legitimate on-disk edit that `--to-disk` would silently revert. **In both cases the store is the damaged side and the remedy assumes it is authoritative.**

**REFINED BY MEASUREMENT RATHER THAN RELAYED (vc), because the claim is about our code:** two of the three remedy sites ALREADY warn explicitly -- `finding.rs:319` says it regenerates views _"DISCARDING the hand edit -- copy anything you meant to keep out first"_, and `:345` tells the operator to copy the file outside the project FIRST because nothing can re-derive either side. **`render.rs:423` is the unguarded one**: _"`intent sync --to-disk` is the routine direction"_, with no mention of what it discards. So the guard lamplight-vc asks for partly exists and one site is missing it. **Their deeper point survives the refinement intact:** a warning does not help an operator who believes the store is authoritative, and under this defect it is not.

**INTENT'S OWN ESTATE IS CLEAN.** lamplight-vc's detector run here: **0 bracketed `file` values of 318 rows carrying one**, and the 318 is the control that says the detector had something to look at. **The defect is real and this estate cannot exhibit it**, which is exactly the distinction watch-out 3 exists for.

**Detection, runnable on any ported estate:**

```
jq -r '.tests[]? | select(.file != null and (.file|startswith("["))) | "\(.id) \(.file)"' intent/.canon/st/*.json
```

Unterminated values (no closing `]`) are the truncated ones. **Nothing has been run against Lamplight's store -- every command there was a read, and lamplight-vc is holding pending the porter fix.**

## THE HOOK CENSUS RESCOPES THE SWEEP: TWO ESTATES RUN NO GUARDS AT ALL, AND THE v3 PORT DID NOT CARRY THE HOOK FORWARD (vc, 2026-08-27 18:56Z)

**Run under the pen after hv released the queue.** `vc/fleet-hook-census.sh`, positive control PASS -- Intent appears in its own carrying set, which is the check that caught the `pre-commit` vs `pre-commit.intent` fault the first time.

```
G3 current (GUARD_HOME + self-host fallback)   1   Intent
G2 keys on INTENT_HOME_RESOLVED               14
G1 NO guard block at all                       2   Baize(4 nodes) Conflab(3 nodes)
```

**THE G1 FINDING IS VERIFIED BY DIRECT READ, NOT BY THE CLASSIFIER**, because a two-of-seventeen result from my own instrument is exactly the shape watch-out 0b says to distrust. The Baize/Conflab carrier is 5090 bytes against G2's 20899 and greps **0** for `GUARD_HOME`, `NO guard ran`, `pre-commit-guards`, `whiteboard` AND `INTENT_HOME`; Lamplight greps 2/3/2/10 on the same terms. The file genuinely has no guard machinery in it.

**SEVEN WHITEBOARD NODES ARE WRITING UNGUARDED STAMPS RIGHT NOW** -- Baize 4, Conflab 3. That is a larger hole than the tolerance question, which is a precision argument about estates that DO run the guard.

**THE STRUCTURAL FACT, AND IT CORROBORATES THE LIVE-BODY PATH FROM A SECOND DIRECTION.** G2 estates DO reach `pre-commit-guards`. If the runner is reached and the bodies are read live out of `$INTENT_HOME`, **every G2 estate is already running the CURRENT clock-guard body**, and what they lack is only what lives in the CARRIER -- which is where ruling 4's arm went (`4d9e70c2` touches `lib/templates/hooks/pre-commit.sh`). **This is corroboration and not proof; dc owns P3 and I would rather they knocked it down than adopted it.**

**THE RESCOPE, WHICH IS REAL AND NOT A TIDY-UP:**

- **Baize is a DEFECT, not a backlog item.** Config `3.0.0` WITH canon -- fully ported -- and still on the oldest gate generation. **The v3 port did not carry the hook forward.**
- **Conflab COMES OUT of the sweep entirely.** `2.19.0`, no canon, not ported. Its hook is 2.19.0's hook and the fix is the PORT. Sweeping it would be doing the port badly by accident.
- **The other 14 are a carrier refresh**, pending dc's P3.
- Intent is already G3; Intentv2 is never written. **The population is 14 + Baize, not 17.**

**A FALSIFIABLE PREDICTION HANDED TO dc's P2 AS A PREDICTION:** if G1 means no guards ran, Baize and Conflab should carry the fleet's HIGHEST stamp-violation rates and the 14 G2 estates should be near-zero at any tolerance. **A guarded estate dirtier than Baize means one of our instruments is broken**, and that must surface before a tolerance ruling reaches a template.

**TWO INCIDENTAL ODDITIES, NEITHER URGENT:** `Molt-flynn` and `Molt-matts` are config `3.0.0` with NO canon directory; `Intentv2` reports `2.19.0` WITH canon. Neither carries a whiteboard.

## AXIS 3 IS CLOSED: 4 OF 4 RUST REGISTRY CLAIMS DRIVEN, 3 VERIFIED AND 1 FALSE (vc, 2026-08-27 19:06Z)

**I told hv this axis was the one to repeat first and that it was UNFINISHED -- two of the four undriven. Both are now driven and both hold.**

| row                | claim                                                               | verdict                                      |
| ------------------ | ------------------------------------------------------------------- | -------------------------------------------- |
| `source_commit.rs` | one embed site                                                      | VERIFIED (earlier)                           |
| `remedy.rs`        | THE one rendering                                                   | **FALSE** -- F4, four producers; fixed by cc |
| `preconditions.rs` | the dehydration ship gate, no address, canon-only, no error channel | **VERIFIED**                                 |
| `export.rs`        | THE export roster, only non-canonical projection, no parallel enum  | **VERIFIED**                                 |

**`preconditions.rs` -- all four sub-claims hold, and the filter that measured them was positive-controlled first.** Zero hardcoded `ST`/`AC` ids in code lines (the comment filter was proven to strip 157 of 375 lines before the zero was believed -- my first pass used `\s`, **which BSD grep does not honour, so the filter was partly inert and its zero was worthless**). The declaration is found by `const OPEN`/`CLOSE` delimiters, not by an address. The only `acceptance.md` mention in the file is in its own module doc. And `pub fn check(canon: &Canon) -> Verdict` returns no `Result` at all -- `unreadable` is a FIELD of the verdict, so an unreadable declaration is an UNMET answer rather than an `Err` a caller could map to permission. Refuse-by-default is structural, exactly as the row says.

**`export.rs` -- the claim holds AS WRITTEN, and the round-trip guarantee has real teeth.** Production path is `render.rs` -> facade -> `export::project` -> `project_with`; `project_with` is deliberately split out so the verifier can be driven by a format the roster does not contain, and `export_round_trip.rs` constructs a projection that DROPS A FIELD and requires refusal. That is a planted control on the guard rather than a demonstration that the passing formats pass.

### Two observations that are NOT findings, recorded so they are not re-discovered as one

**(a) THREE types named `Format` in `intentsvcs`, with overlapping and divergent membership.** `export.rs` (struct; `json` `yaml` `md`), `output.rs` (enum; `terminal` `text` `md` `markdown` `json`), `address.rs` (enum; `Json` `Md`). `json` is in all three, `yaml` only in export, `terminal`/`text` only in output. **This is a NAMING collision across three genuinely different concerns, not a duplicated concern**, so it is not a Highlander violation and I am not calling it one. LOW. Worth knowing before someone imports the wrong one.

**(b) 22 enum-typed flags are declared with a roster in `dispatch-table.json`; exactly TWO production sites read a declared roster** -- `enum_flag(a, "doctor", "--format")` and `arg_values(table, "edit", "file")`. **I am NOT reporting this as a finding, because I counted one mechanism, then found a second, and there is a third** (`output::Format::parse` refusing via `ok_or_else(UnknownFormat)`). **Which of the 22 route through which is unmeasured, and a coverage number over an unmeasured routing is the exact error I made on F3 this afternoon.** Handed to cc queued: they own `flag_reachability`, and they are the node who corrected my last population count on this same surface.

**Worth noting alongside (b): there is no clap `value_parser` anywhere, and that is deliberate and documented** -- clap rejects at exit 2, which is INV-04's USAGE code that the pre-commit gate FAILS OPEN on, so a bad enum value would become a gate bypass instead of a refusal the operator sees. **The roster is therefore honoured only where code honours it**, which is what makes (b) worth measuring properly rather than dismissing.

**One soft thing, not chased:** `render.rs:551` cites AC-04.6 for the declaration-versus-implementation gap. AC-04.6 as declared is about MUTATION COMPLETENESS -- state machines, not flag rosters -- so the citation invokes its principle rather than its scope. **A doc comment is the one kind of citation nothing in this tree checks**, which the same comment block says about itself two paragraphs down.

## THE SWEEP HAS NO v3 MECHANISM, AND RULING 4 LANDED IN THE ONE LAYER THE DESIGN SAYS CANNOT REACH THE FLEET (vc, 2026-08-27 19:11Z)

**I was one step from running a fleet write that would have reported success and changed nothing about the property it was for.** Found by determining the sweep's mechanism BEFORE running it, which is the only reason this is a finding rather than an incident.

**THE MEASUREMENTS, EACH INDEPENDENTLY CHECKABLE:**

1. **`intent claude upgrade` without `--apply` is a real dry run** -- driven on Riffle, 0 dirty paths before and after, carrier mtime unmoved. It reports what it would write: `.claude/settings.json`, `CLAUDE.md`, `AGENTS.md`, `usage-rules.md`, `.intent_critic.yml`, and `.git/hooks/pre-commit` **(chain block, region-edited)**. **`pre-commit.intent` is NOT on that list.**
2. **No v3 code path writes `pre-commit.intent`.** Every `pre-commit` mention in v3 production Rust is a comment except `canon.rs:432` (`hooks.join("pre-commit")`, the wrapper) and `canon.rs:211` (the chain-block text that INVOKES the carrier). `canon.rs:18` says it in the module's own words: the chain block, REGION-EDITED.
3. **The carrier itself documents exactly this, and it was a deliberate design response to the same class.** In Lamplight's installed copy: _"THE ROSTER IS NOT HERE, AND THAT IS THE POINT. This file is COPIED into `.git/hooks/pre-commit.intent`; the roster is READ LIVE out of `INTENT_HOME` ... **Anything a consumer holds a frozen copy of cannot be updated by shipping canon, so the roster must not be something they hold.**"_ It even records the incident that forced it: _"Measured on this repository 2026-08-20: canon rostered four guards, the installed hook ran one, and two had never run here at all. The guard BODIES propagated; the array naming them did not."_
4. **Ruling 4's fix went INTO the layer the roster was moved OUT of.** `4d9e70c2` changed `lib/templates/hooks/pre-commit.sh` -- the carrier. The critic gate is 15 hits in the carrier and **0 in `pre-commit-guards.sh`**, the runner that actually propagates.
5. **POSITIVE-CONTROLLED, AND THE RESULT INCLUDES THIS TREE.** The refusing arm's pattern returns **1 against the template** and **0 against every estate carrier measured -- Intent, Lamplight, Baize, Devbin, Laksa.** Template 34701 bytes; Intent's own carrier 25609; Lamplight's 20899. **Three generations and none of them current, in the tree that authored the fix today.**

**SO dc's ORIGINAL FIGURE WAS RIGHT AND NOW HAS ITS MECHANISM.** "Ruling 4 in force in ZERO estates" is not seventeen estates being behind on upgrades. It is that **the fix is in a file no shipping mechanism updates**, and the design says so about itself.

### What the fleet actually runs, now that the layers are separated

- **G2 estates run CURRENT guard bodies and the CURRENT roster**, live from `INTENT_HOME`. They are not stale in the way the held item implied.
- **What every estate holds frozen is the CARRIER**, and that is where the critic gate lives.
- **Baize (G1) reaches nothing** -- its carrier has no guard block at all, so INTENT_HOME is never consulted.

### THE MENU, NOT THE SELECTION -- and this one is hv's

**(A) MOVE THE CRITIC GATE OUT OF THE CARRIER INTO THE RUNNER**, exactly as the roster was moved on 2026-08-20. Ruling 4 then reaches all 14 G2 estates with **zero fleet writes**. **UNMEASURED AND I AM NOT PRESENTING IT AS FREE:** the gate reads `.intent_critic.yml`, the project root and the declared languages, and **I have not checked that the runner has those in scope.** If it does not, (A) is a bigger move than one file.

**(B) BUILD THE MISSING v3 VERB THAT INSTALLS THE CARRIER.** This is _a capability the normal entry point cannot reach is not delivered_ (hv), and **Baize is the proof it is needed regardless of (A)**: a fully-ported 3.0.0 estate whose gate rotted with nothing able to repair it.

**(C) COPY THE TEMPLATE INTO 15 ESTATES BY HAND.** No idempotence story, no gate, and **a later v2 `intent upgrade` anywhere would silently undo it.** Recorded so the menu is complete, not because it is a candidate.

**(A) and (B) are not alternatives -- (A) makes ruling 4 reach the fleet now, (B) closes the hole that let Baize rot.** The sweep as held was (C) with better manners.

### Option (A) costed: cheap, but the naive form kills a ruled fail-open

**Measured rather than left labelled unmeasured, because an option's cost is a claim and admitting it is unchecked does not soften it (watch-out 0c, on me).**

**The three dependencies I flagged are already in scope.** The carrier does `cd "$PROJECT_ROOT" || exit 0` (line 44) and the runner invokes guards as `bash "$g_path"` with **no `cd` of its own**, so a guard runs with cwd at the project root and reads `intent/.config/config.json`, `.intent_critic.yml` and the declared languages by relative path exactly as the gate does. **Positive control: all four existing guards read project-relative paths and work.** The roster's `applies-when|guard|what-is-unchecked` shape fits the gate.

**THE HAZARD THE LABEL WAS HIDING.** The carrier discriminates exit codes (`case "$rc"`, line 537): _"the gate should fail open on its own breakage and closed on yours"_, with the fail-open marked at 563 as _"UNCHANGED AND IS A RULING, NOT AN OVERSIGHT"_. **The runner has no discrimination -- `bash "$g_path" || BLOCKED=1`, any non-zero blocks.** So the naive move converts a ruled fail-open into a fail-closed as a SIDE EFFECT, which is answering hv's open question in code where nobody would read it as an answer.

**(A2)** -- move the gate, have it translate its own rc (keep the `case`, return 0 where it means to fail open). Preserves the ruling, leaves hv's question open, and is a file move plus one roster entry. **(A1)** -- teach the runner discrimination; changes the contract every guard is written against and needs its own ruling; recorded to complete the menu, not recommended.
