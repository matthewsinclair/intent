<!-- treeindex v1 fingerprint:23730423 generated:2026-02-20T11:27:32Z -->

# bin/

Main CLI entry point and core utilities for the Intent project management system. Contains the primary executable (`intent`) that routes to subcommands, helpers shared across plugins, and support scripts for tree and file indexing.

## Files

- `intent` -- Main CLI router; parses commands and delegates to subcommands or plugins
- `intent_helpers` -- Shared utility functions (path resolution, color output, validation, formatting)
- `intent_treeindex` -- Generate .treeindex directory summaries for fast codebase navigation
- `intent_fileindex` -- Build file indexes for codebase search and analysis
- `intent_claude_skills` -- Manage Claude Code skills (list, install, sync, uninstall, show)
- `intent_claude_subagents` -- Manage Claude Code subagents (init, list, install, sync, uninstall, show, status)
- `intent_claude_upgrade` -- Diagnose and upgrade project LLM guidance layer
