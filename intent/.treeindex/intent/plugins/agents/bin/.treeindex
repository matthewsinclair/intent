<!-- treeindex v1 fingerprint:aac90303 generated:2026-02-24T15:20:06Z -->

# bin/

Command entry points and helpers for the Intent CLI. Contains the main `intent` dispatcher, shared helpers, and command implementations for managing steel threads, work packages, treeindex generation, and project configuration.

## Files

- `intent` -- Main CLI dispatcher; routes all Intent commands to implementations
- `intent_helpers` -- Shared helper functions and utilities used across commands
- `intent_st` -- Steel thread management (create, list, show, start, etc.)
- `intent_wp` -- Work package management (create, list, show, start, done, etc.)
- `intent_treeindex` -- Directory summary generation and treeindex management
- `intent_fileindex` -- File index generation for codebase navigation
