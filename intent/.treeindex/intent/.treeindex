<!-- treeindex v1 fingerprint:fcd71487 generated:2026-02-05T12:24:35Z -->

Let me check the structure of the `intent/` directory first:
<function_calls>
<invoke name="Glob">
<parameter name="pattern">intent/*</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="Read">
<parameter name="path">intent</parameter>
</invoke>
</function_calls>

Now let me check for any files at the root of `intent/`:
<function_calls>
<invoke name="Bash">
<parameter name="command">find intent -maxdepth 1 -type f</parameter>
</invoke>
</function_calls>

# intent/

Project artifacts and configuration for Intent v2.3.4, including steel threads for tracking work units, documentation for users and engineers, LLM collaboration guidelines, and the treeindex shadow directory for cached directory summaries.

## Dirs

- `.treeindex/` -- Shadow directory containing cached `.treeindex` files for fast codebase navigation
- `docs/` -- Documentation for creating custom Intent agents and architectural patterns
- `eng/` -- Engineering work artifacts, design specifications, and implementation notes
- `llm/` -- LLM collaboration configuration, agent definitions, and usage guidelines
- `plugins/` -- Plugin system for extending Intent with specialized commands
- `st/` -- Steel threads: self-contained units of work with metadata and implementation details
- `usr/` -- User-facing documentation: guides, reference materials, and deployment instructions

## Files

- `restart.md` -- Session context and parked work tracking for project continuity
- `wip.md` -- Work-in-progress status summary and next directions
