<!-- treeindex v1 fingerprint:ae64db9a generated:2026-04-23T16:22:36Z -->

I don't have access to file system tools in this context to explore the `templates/` directory. Could you provide:

1. The directory listing (subdirectories and files in `lib/templates/`)
2. The purpose of each subdirectory

From the git status, I can see there are untracked `.treeindex` directories for `lib/templates/hooks/` and `lib/templates/prime/`, and the CLAUDE.md mentions `lib/templates/ext-seeds/worker-bee/extension.json`, but I need the complete inventory to generate an accurate treeindex.

Alternatively, you could run `ls -la /Users/matts/Devel/prj/Intent/lib/templates/` and paste the output, and I can generate the file from that.
