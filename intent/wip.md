---
verblock: "2026-09-26:v3.10: vc - leaned after the v3.2.3 cut; no DOING, the TODO is what waits on hv or on the next line"
intent_version: 3.2.3
---

# Work In Progress

Measure before believing anything below: `.claude/restart.md` carries the commands, `intent/restart.md` the rules every bank, train and landing runs under. No count appears in this file.

## DOING

Nothing. Every node is folded and holding for hv.

## TODO

- **THE PUSH of main's post-tag commits**: hv's hand. They are board renders and issue 0586 on top of the v3.2.3 tag; nothing compiled in.
- **0588, AWAITING hv: fix it now as 3.2.4, or queue it?** An H2 line inside a thread's Objective or Context breaks the cover read-back. The cover guard then refuses with a false CoverEditNotCarried, and its named remedy, `sync --to-store`, would cut the field at that heading. It is a remedy that misdirects, the shipping-grade class. It was found in Gtools (ST0011), where gtools-vc has a workaround: demote the inner heading to H3 with `intent set`.
- **THE STABILISATION LINE, AWAITING hv**: decision 33 (5) put it on hv's board once the fleet sweep was done, and it was never recorded; 3.2.2 and 3.2.3 then shipped as hv-approved fix and feature work. vc asks hv whether the line starts now, then records it as a directive on hv's board.
- **DEVBIN 0.1.8 INTO THIS TREE**: devbin-vc has vc's yes (hold 11) and runs it; dc's guards adoption pass (dc hold 29) rides the same sweep.
- **AFTER THE LINE, vc's hand, no push:** hv decision 28's ten Conflab and Baize rulings, which repair records rather than develop anything. None is done.
- **AWAITING hv: a `/refresh` in the explorer.** vc recommends it after the line, because every view but the search pane already follows the store within half a second. If ruled in, it is ic's, size S.
- **QUEUED FOR 3.2.4 OR LATER**, each filed where it lives: 0586 (data-model.md lacks the board's `edited_at` rows), 0581 to 0583 (cc's rules pass), the open halves of 0554 and 0559, dc's M2 removable-v2-source fix, and 0587 (Lamplight's comment on ST0080: say beside `backup.keep` that each snapshot copies the whole store).
- **LAMPLIGHT, AWAITING hv:** whether it takes 0577's `.claude/settings.json` (the 30s hook timeouts). It skipped them in the 3.2.2 sweep, and vc recommends taking them. lamplight-vc also owes the real backup control: open `intent explore` after 13:32Z on 2026-09-26, when the daily backup comes due, and report whether one was taken.
- **WITH hv, not re-measured since 2026-09-22**: the pushes of Prodinfra, Riffle, Baize, Conflab, Laksa, Prolix, Utilz and the two Arca repos from their own checkouts, measured at the moment of pushing.
- **MACHINE, hv's call**: the resident `SetStoreUpdateService` processes regrew after the 2026-09-22 reboot (`ps -axo comm= | grep -c '[S]etStoreUpdateService'` measures it).
- **ST0060** and **ST0077** stay in Triage as the next line's queue (hv, 2026-09-23).
