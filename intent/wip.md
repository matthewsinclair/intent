---
verblock: "2026-09-21:v3.3: vc - localfold at the end of the 3.2.0 line: the release is cut, published and installed cold, ST0056 is Completed, no issue is open but 0501, filed today; v3.2 folded after the cut; v3.0 leaned this file to DOING and TODO only"
intent_version: 3.2.0
---

# Work In Progress

Measure before believing anything below: `.claude/restart.md` carries the commands, `intent/restart.md` the rules every bank, train and landing runs under. No count appears in this file.

## DOING

- **Nothing is in flight.** The 3.2.0 line closed on 2026-09-21: the release is cut, published and installed cold on a stranger machine, ST0056 is Completed, and the pair every node runs is rebuilt at the head of main with the two 3.2.1 fixes in it. CHANGELOG carries `## [3.2.1] - in progress`.
- **vc**: judging whatever hv opens next; hv's inbox read at every pickup.

## TODO

- **WITH hv**: the push of main and the reading of its CI, the rust workflow's doc step in particular; the pushes of Baize, Molt, arca_config, Conflab and arca_cli from their own checkouts; Prolix's whiteboard carry, hv-first; the 3.2.0 app into /Applications on this machine; Lamplight's pre-commit hook, which re-stages Markdown from the working tree (issue 0498's mechanism, that estate's to repair); the root files and hook carriers of Conflab and arca_cli, still 3.1.0's; the README question for Riffle, arca_config and arca_cli.
- **0501**: the release gate says nothing about the doc step CI's rust workflow runs and it does not, low, the next line's first issue.
- **NEXT-LINE**: cc trims `intent/restart.md`'s home-pointer line, held by 0492; `tests/unit/devbin_seal_disagreement.bats` pins a vendored devbin line its upstream suite no longer pins.
- **ST0060** and **ST0077** stay in Triage as the next line's queue (hv, 2026-09-18).
- **The LIVE guards adoption pass**, dc's, HELD until `bin/int hooks` reports a canon-shim-wired hook as wired (the defect is devbin's, hv Q7); never run `int hooks --install`.
