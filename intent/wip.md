---
verblock: "2026-09-21:v3.5: vc - the evening's globalfold: ST0079's CLI verb landed and built, WP-01 ic's next; the fleet's re-staging formatters killed (hv decision 27); dc's queue and the Conflab and Baize triage waiting"
intent_version: 3.2.0
---

# Work In Progress

Measure before believing anything below: `.claude/restart.md` carries the commands, `intent/restart.md` the rules every bank, train and landing runs under. No count appears in this file.

## DOING

- **ST0079**, WIP: `intent outs` is landed and live in the pair. **WP-01**, the TUI Omnibox `/outstanding` with the alias `/outs`, showing the table `intent outs` prints, is ic's on 2026-09-22 on hv's word; its ACs go to vc before code, and the TUI reads the same `intentsvcs::outstanding` rows the verb reads. The thread closes when WP-01 is Done.
- **0501**, dc's hand, S: work in progress at `refs/bank/dc/0501-wip/patch` (blob 459c23e1f, base 341d48cdd). Owed: the release fixture's two dry-run arms, the whole bats suite on macOS in a private worktree, the judging bank. vc's judging checklist is in dc's inbox. The rust.yml line is judged on hv's next upstream push.

## TODO

- **dc's queue, in order** (in dc's inbox): 0501; then the guards adoption pass (hold 18 released, hv decision 26), which also makes the staged-blob format check a declared, tracked guard shipped from canon, so no estate hand-wires a formatter again (hv decision 27); then Prolix's whiteboard carry, hv first, only the placeholder lines of hv's stub board droppable.
- **WITH hv**: the pushes of Intent, Prodinfra, Riffle, Baize, Conflab, arca_config, arca_cli, Laksa and Prolix from their own checkouts (Molt is level); the 3.2.0 app into /Applications, which waits on hv quitting the running 3.0.3 (the v3.2.0 release asset was checked on 2026-09-21: Developer ID, notarized, stapled); the Conflab and Baize holds, brought to hv as TUI questions after vc re-measures each row of `intent/history/20260921-triage-conflab-baize-holds.md`.
- **vc**: re-drive and file the Intent defect in that triage's C6 -- `st done --date` on a Completed thread returns rc 0 and writes no date.
- **NEXT-LINE**: cc trims `intent/restart.md`'s home-pointer line, its reason (0492) closed; `tests/unit/devbin_seal_disagreement.bats` pins a vendored devbin line its upstream suite no longer pins; `surface/dispatch-table.md`'s header names `parity/tools/gen_dispatch_table.sh`, which lives under `intent/st/ST0056/parity/tools/`; `lib/templates/hooks/whiteboard-header-guard.sh` still says a gate re-stages before its guards, in a comment and in the refusal it prints.
- **ST0060** and **ST0077** stay in Triage as the next line's queue (hv, 2026-09-18).
