---
verblock: "2026-09-12:v1.45: vc - DOING and TODO only, hv's rule restated in this file; landed work struck; the silent-deletion class ruled into v3.0.2"
intent_version: 3.0.1
---

# Work In Progress -- v3.0.2

**This file is DOING and TODO only. What landed lives in the commit, the CHANGELOG and `intent/done.md`; what is open lives in `intent issues list` and `intent ac gate <ST>`. A line that says something is done does not belong here, whoever needs it for context.**

## DOING: the v3.0.2 cut

hv ruled the release on 2026-09-12: a patch, vc directing. Rules for every item: own commit, red before green, a private worktree under an isolated HOME using the worktree's own in-tree target dir (an out-of-tree CARGO_TARGET_DIR puts the binary where no `lib/templates/` sits above it and reds the install-root tests, a false red ic and vc have both paid for), no release, no push, no hardcoded counts in anything a reader reads. A shared aggregator file (`tests/suite.rs`, `CHANGELOG.md`, this file) is read with `git diff <file>` before `git add`, because `--only` is path-scoped and takes every hunk a peer left in it; a `mod` registration and the file it names land in one commit (d4d7aec2b carried ic's registration for a file only ic had, and HEAD could not build its test target until 5746e02e). An edit to `surface/dispatch-table.json` regenerates `dispatch-table.md` through `gen_dispatch_table.sh` in the same commit, or the skew guard refuses it; the register keeps its own formatting and is never run through prettier. hv's directive of 2026-09-12 on handing the pen back to vc: no overtesting, no yak-shaving; build what the STs need, tests are the AC rows, and never a test that tests a test. Two rules from the mid-course rehearsal: run the WHOLE rehearsal and never the step you expect to fail (the first refused at doctor, so the BATS gate had never executed and a real defect sat red on main behind an unrelated one); and a template or shell-payload edit is driven with the BATS suite as well as cargo, because a Rust-side drive is blind to it by construction. Two more from the day's near-misses: worktree results come back to the shared checkout as a PATCH (`git diff` there, `git apply` here), never as a whole-file copy, because a copy silently reverts whatever landed on main while you built and the diff reads as your own work (dc caught it on `suite.rs` only by checking each file's base against HEAD first); and a test asserts the CLAIM, not its container, because a sentence pinned around a claim and a list length pinned around a claim each cost a red today. And one from the watcher: a cost measurement is not a consequence measurement (dc's watch-cost numbers were right about events, cache and wakeups and could not see the feedback loop a root-granularity reconcile starts, because they counted paths and never ran an ingest). And every drive is `cargo test --workspace --no-fail-fast`: without the flag cargo stops at the first failing target, so a green reported without it is a claim about the first target only (cc, correcting its own day's claims).

- **CHANGELOG (dc):** one Fixed line per landed item; known-defects entries for fixed items say fixed in 3.0.2.
- **dc's rehearsals** in a clone with `intent backup` taken deliberately and the report saying so (a clone lacks the gitignored `intent/.backup/` by construction): a mid-course run once the watcher repair lands, proving batch 4 left the release path whole and preflight deterministic; the final run on the last HEAD after the last search package. Every gate line reported. The final rehearsal runs in an announced QUIET WINDOW: cc and ic pause every suite, and dc first runs the daemon targets five times quiet, because main's daemon pair reds most full runs while three lanes build on one box (dc, five-and-five, the attribution running the wrong way for a regression). Green quiet means the flake is a load artefact and goes on hv's list with the numbers; red quiet means a defect fixed before the tag, red first. Nobody loosens a test. Measured in the window: the family is load-sensitive and the host has no idle, so a test-gate red confined to the daemon family re-runs the whole rehearsal or cut ONCE, both runs' gate lines verbatim with their loads; a second red on the family, or any red outside it, halts and comes to vc. The workspace run inside a rehearsal or a cut starts only on a one-minute load a script has just read under 25, and that reading is reported with the run: three runs on one HEAD were red at 82, red at 84 and green at 21, one variable. The rehearsal's HOME stays isolated and carries `GH_CONFIG_DIR=/Users/matts/.config/gh` so the gh gate passes and the previews after preflight run; a rehearsal that stops at preflight's last line has rehearsed the gates and not the cut. A start-of-run load reading does not predict the family: the workspace run drives its own load and the arms run inside the climb, so the load sampled throughout the run and its peak is the number that travels with a verdict.
- **Before the cut, hv's order of 2026-09-12: a clean build of everything from a pristine tree** with `bin/devbin fullcycle` (clean rust, `build all`, `test rust`, the removal prompt answered by `--force` since it reads the tty), then `bin/int macos app-install` for the menubar app, `intent daemon restart`, and a sweep showing one `intentd`, one Intent.app and `~/.intent/home` on the project.
- **The cut** is hv's go, with the release-confirm question (tty, or an explicit `--no-confirm`) asked as its own question at that point. After the tag: ic's one reference regeneration, both halves keyed to v3.0.2, one commit; the tap formula publish as its own action on hv's approval.

## DOING: ST0069 to the end (hv, 2026-09-12: no cut until every non-cancelled WP is Done; vc directing)

- **hv's ruling on 2026-09-12: v3.0.2 is not cut until ST0069 is done, and done means every non-cancelled WP Done.** WP-17 to WP-24, WP-01, WP-02 and WP-16 are Done. The one package left is **WP-14** (cc the core, dc the migration verb, ic the protocol half). Landed: commit two at 87b819abd, the read verbs at de03d227e, the message verbs at dc77fc9f7, the item verbs at 28f9d4b99, the lifecycle verbs at dd3e3444e (`touch`, `release`, `pickup` as a thin composite, `archive <kind> <seq>` as the one archival act, the terminal states being the archive); fourteen `wb` rows in the register, `ask`, `announce` and `decide` one-way and off the tool tier, the rest idempotent and on it; bounds by refusal (body 8192 bytes, 20 live messages per inbox, 40 live items per kind, hv unbounded); `--node` alone until hv rules on `INTENT_NODE`. The live store is at schema 24. Still on cc, in order: `WbItemKind::Hold` as the fifth kind and `wb add <kind> <text>` for doing, todo, watchout and hold (their own commit; nothing else adds those items and the protocol's Holds section has no kind); the AC-14.2 renderers for `wip.md` and `inbox.<sender>.md` on the five kinds, byte-identical on two renders with the formatter excluded (row reworded under the pen at 374dc9a98), against the fixture; AC-14.7's GraphQL root fields; AC-14.8 measured on the fixture through the generated views; AT rows for every row covered. On dc: the migration verb (AC-14.9) against the fixture board, its three lines approved (source order, ties by insertion, `authored_at` verbatim and untrusted, `.history/` files as verbatim snapshot documents, carried plus named-uncarried reconciling per node), holds carried as holds once cc's kind lands; then the protocol half if ic is still inside 0311. On ic: issue 0311, the daemon republishing its own projection writes (the watcher's leaf branch had no baseline; the fix records everything a projection wrote at one home and the leaf branch asks the store; `a_leaf_event_costs_no_store_round_trip` moves to at most one fetch per batch, ruled), landed whole on a whole-suite control whose runner proves both sides reached the binary; then the row reviews; then the protocol half (AC-14.10, and AC-14.12 reworded to name every caller of the four functions, the fifth at `intent_claude_cwi:392`) unless dc has taken it. Then the cutover on vc's signal when every landing is in and every node has folded: dc's verb migrates the five live boards and inboxes, the views switch to the renderers, both whiteboard guards retire, the pair is rebuilt and the daemon restarted by vc's hand; then `wp done ST0069/14`, the thread's close, and the cut. **The doctor gate** (0308, 0309) is landed, closed and live for every estate on the machine: doctor answers 0 clean, 1 for blocking findings, 4 when it cannot judge the estate (82b85c5e1); the pre-commit template reads that code alone, refusing on 1, failing open otherwise and printing a clean line on 0 (6fae2ff6a, 0496f4adc). **0307**, **0310** and **0312** (an enum positional's declared values are not applied by the spine) are filed for after the thread. The delivered pair is at 82b85c5e1; the daemon and Intent.app (3.0.1 build 6752) run on it; doctor reads 0 findings at exit 0. The cut record stands for when the thread closes: cut HEAD family 2239998e3, every gate before cargo green on every run, all fourteen previews green, the daemon family red or green by load on byte-identical cargo bytes (run 6's ten-second trace peaked at 65.68 with hv's own workspace run overlapping it minute for minute), every failing arm an intentd timing arm and no other target red once; dc's full runs on 496d38e99 and 62d3db243, hv's own run on 19451bc38, cc's on commit two and ic's on the doctor half each exited 0 on every target; the `daemon_subscriptions` pair's base rate is 0311 and its fix precedes the cut. The cut, when it comes, is hv at a terminal with every gate on; `--skip-tests` was offered and hv refused it.

## TODO: waiting on hv

- **Six decisions from ST0069, taken under the pen on 2026-09-12 when hv handed it over without ruling them; each overrulable with a line:** (1) the grammar line: rust, elixir, swift and lua ship on by default, bash declared and off because its grammar ships no tags query, the measured table in the design; cc lands the default line and AC-20.4 closes on it; (2) the explorer omnibox stays out of 3.0.2; (3) `intent modules find` stays as the registry fallback with the canon naming the index first, and AC-20.6 is withdrawn naming AC-24.5, which carries the sweep; (4) no Local runtime in 3.0.2, on cc's measurement: the ONNX shape nearly quadruples the binary under a release-candidate `ort`, and the pure-Rust candle shape is the recorded candidate for a later release; the seams and the Null and HTTP embedders ship as built; (5) ST0073's AC-05.1 is re-keyed to AC-00.1 with its text and evidence carried forward; (6) the HTTP embedder gets no TLS in 3.0.2: it speaks `http://` only and refuses `https://` by name, because one POST of one JSON document needs no HTTP crate and a TLS stack is a dependency with a rationale. AC-23.4 is satisfied on (4)'s record and WP-23 is closed. A seventh call from the hook's landing: (7) the PostToolUse symbol-context hook ships OFF by default, served by the install and not wired in the settings template, a project switching it on as it does post-tool-advisory; hv can wire it by default with a line. **Calls taken on 2026-09-12 after hv ruled ST0069 to the end, each overrulable with a line:** (8) an issue's realised form is one generated file per issue under `intent/issues/` with the sigil `ISSUE:<NNNN>`, the single rendered `issues.md` refused; (9) the v2 buckets are ingested as attachments per file and then pruned, the prune refusing while the ingest is incomplete, and the fleet prune stays per estate on hv's word after at-accounting; (10) WP-14's bounds: body 8192 bytes (cc's corpus measurement, median 2423 and none over 8192, replacing the 2048 first ruled), live messages per inbox 20, DOING 5, TODO 15, board total 8192 bytes, configurable, hv's node unbounded; a caller stamp is refused, a migrated hand-authored stamp is carried verbatim in its own untrusted field beside the service's `recorded_at`; `.history/` files migrate as verbatim snapshot documents, never per item; the acting node is `--node` then `INTENT_NODE` and its absence refuses; (11) WP-16's join is a declared map from schema entity to data-model table, an unmapped entity refusing. (12) the Intent estate declares its two open issues once the doctor fix lands, because `issues add` now declares each new issue (AC-01.3) and a manifest declaring new issues while leaving the old open ones undeclared is a state nobody chose; `organize --default` is not run on the live estate, since it would also drop the two completed threads the manifest declares. (13) AC-14.6's archival is the bounded write's own step and not a verb: a write that would exceed a bound rolls the acting node's handled messages and DONE items to archived, admits the write if it fits, and refuses by name only when live content alone exceeds; `wb pickup` ships as a thin composite of the reads and `touch`; AC-14.5 is satisfied as built with its limit on the record, the API refusing every write outside the acting node's own board and inbox while the acting node's identity is the caller's claim through `--node`. (14) WP-02's preamble is carried line-wise against ONE pinned template revision with every unmatched line carried, and the prune's refusal is all-or-nothing with the estate as the unit, naming every withheld path. (15) `WbItemKind` gains `Hold` as a fifth kind, because a board round-tripped through a model with no holds loses the section the protocol calls load-bearing; and `wb add <kind> <text>` is the one door for doing, todo, watchout and hold items, refusing `decision` with a remedy naming `decide`. (16) 0311 is fixed daemon-side, a subscriber never told about writes the daemon itself made, the leaf branch given a baseline at one fetch per batch; the harness barrier is withdrawn because a subscription opened after setup now receives nothing, which is the fix working; (17) the two whiteboard guards STAY in the pre-commit roster through this cut, harmless on a generated board whose stamps come from the store, retired only after the fleet's last hand-authored whiteboard has migrated, so no hook-template work sits on WP-14's path; (18) a whiteboard view exists for exactly the `wb_node` rows in the estate's own store, `undeclared_owner` gaining one whiteboard arm and the `.intentfiles` grammar unchanged, so an estate with a hand-authored board and no rows sees no view and no skew from the delivered pair; (19) the migration verb applies no live bound and marks nothing handled, the bound being a refusal on `ask`, `announce` and `add`, and an over-bound migrated inbox refuses new sends until its owner clears it; (20) the cutover order: the wiring commit lands under the delivered doctor, then by vc's hand dc's verb on the five live boards, the pair rebuilt, every board and inbox regenerated and committed as one commit with nobody committing in between, the daemon restarted, the broadcast; (21) issue 0313, laksa-vc's report: the doctor's store-vs-rebuild arm becomes its own advisory class `store-stale`, printed and not counted, because it measures what commands will answer and not whether the commit is wrong, and on a shared tree it fires on every peer's canon write; dc, before the pair rebuild; (22) issue 0314: `at edit` retires a row's `legacy` reference when `--file` or `--prose` is named, since that re-cite IS the v3 rewrite the advisory asks for and today it produces the blocking pair instead; dc, before the pair rebuild; (23) ic's finding against AC-14.7: `wb pickup` writes status active, the heartbeat, `session_id` when `--session` is named and `focus` when `--focus` is named, `touch` stays heartbeat only, `release` writes paused; cc; (24) an inbox whose sender has no `wb_node` row is reported by file as uncarried and never inserted, and the renderer renders inboxes for registered pairs only. **Two rows for hv to rule, not taken under the pen:** AC-24.6, the PreToolUse redirect of symbol-shaped greps, is satisfied as specified and its own text says it is not built until hv rules it on; AC-24.7, a symbol hit naming its thread, is a stretch satisfied as specified with its data source. Under done-means-done, hv says whether either is built in this thread or stays specified. **A third for hv:** whether the shipped binary reads `INTENT_NODE` as the acting whiteboard node; ST0057 AC-11.3's one-variable guard names hv for the row, cc built the fallback and took it out, `--node` alone ships until hv rules, and vc recommends no for this cut.
- **Calls made under the pen in batch 4, each overrulable with a line:** critic's undeclared `--format` exits 2 (INV-04's rule for critic's usage errors); a shellcheck decline is reported, not a refusal; the organize preview predicts the directories it will prune; `--default --force` keeps its single confirm with the full plan printed before any byte moves, and previewing the regeneration in memory so the plan can sit inside the prompt is its own item; a realisation verb refuses a plan that would remove a file under the artefact rather than announcing it; the MCP `organize` tool requires the echoed plan digest for `apply: true` and the terminal gains `--plan` to pin an apply.
- **View-skew rot, a structural candidate for the next batch:** doctor compares the generator banner, so every cut leaves every untouched view skewed until something re-renders it, and the next preflight refuses again. Either doctor ignores the version stamp when comparing, or the stamp step re-renders every realised view, the schema-faces shape.
- **`organise --apply`, two defects found by hv on 2026-09-12 that batch 4 does not cover:** a refusal that is the same paragraph many times over is one finding printed many times; the tree is left half-dehydrated, views kept and attachments gone. The third, naming removals only after, is batch 4's trigger.
- **Surfaced at the 3.0.1 cut, not worked:** a stranger's `brew tap` may refuse our tap as untrusted, seen on Homebrew main with an empty trust store and not measured on stable (the install docs may need `brew trust --tap matthewsinclair/intent`); Linux and Intel macOS binaries are not built (size L); CI did not run for 3.0.1 (waived); `set <ac> kind non-test` leaves state `computed`, illegal for non-test, and only withdraw-then-reinstate moves it.
- **The rulings gate and dehydration collide** (found twice on 2026-09-12): a ratified ruling cites a thread file as its record, `rulings_check.sh` resolves the record on disk, and a completed thread hv's hygiene rule unlists takes its file off the disk. ST0058 and ST0066 stay realised until the gate resolves a dehydrated thread's record through the canon extract, or the two records cite canon paths. Small, code, hv's to rule.
- **A registered test module whose file is not in the commit** builds nowhere but on the machine that has it (found 2026-09-12). A pre-commit arm that refuses a `suite.rs` naming a `#[path]` the index does not carry is small and is not batch 4's class. hv's to rule; cc's if ruled.
- **Tests that read this repository instead of a fixture** red whenever the estate is mid-flight and blame whoever is committing (dc and vc, 2026-09-12): `lifecycle_verbs_edit_the_list` and `facade_acceptance::a_no_op_scope_change_is_reported_rather_than_written_twice` name `intent/.canon/st/ST0056.json` and `intent/st/ST0056/**` in their failures. Green again once the estate was consistent, so the class stands unfixed. Each such test moves to a fixture tree; hv's to rule, cc's if ruled.
- **ST0056 stays open on AC-00.5 and AC-11.1**, WP-11 with it: a `brew install` on a Mac that has never seen this repository, then the `intent` and `intentd` lifecycle, then both satisfied by evidence.
- **`intent critic` answers outside a project** (dc, 2026-09-12, driving the keg): `intent init --name` is not v3's spelling and refused at rc 1, and `intent critic` in the uninitialised directory then produced output indistinguishable from a project's, because the rule library resolves from the install root. Every other verb refuses outside a project; whether critic should is hv's to say.
- **An ingest rewrites `AGENTS.md` and `CLAUDE.md` without updating `file_index` in the same step** (dc, 2026-09-12, found while widening the watcher): the index lags its own writer for the root files. On the leaf path it is masked by byte-identity (the rewrite is byte-identical and the write set skips it), and the two-registration ruling means no reconcile meets it, so it is a finding for this cut and not a fix; the fix is the writer indexing what it writes before it releases.
- **The daemon family's timing arms are load-sensitive and this host has no idle** (dc, measured 2026-09-12 in the quiet window): the bounded-ingest arm passes alone six of six at a one-minute load near fifteen and failed three of six near forty on the same HEAD family, so today's reds were load and not a change of character; and with every Intent node silent the host's own file-sync daemon and running iOS simulators hold the load floor above ten, so no measurement today was on an idle host and none can be. Ruled for the cut: a test-gate red confined to `daemon_watch` and `daemon_subscriptions` is re-run once with both runs reported verbatim, never twice. The floor is hv's to lower; the arms' calibration against an idle host is a finding, not a fix in this cut.
- **`0177`** (medium): `ext` ships the creating half without the undoing half; `config`, `ext` and `learn` ship declared-and-unbuilt (hv, 2026-08-31).

- **Out of the 3.0.x line by ruling:** ST0057, ST0060 (vault), ST0070 (LLM config).

## TODO: defects found by the doc audit, unruled

Each is described as built in the docs. None is worked until hv rules, and a fixed one leaves this list. The bold tag is the item's stable name; the numbers are not consecutive because the fixed ones are gone.

- **(2)** **`intent init` does not keep the store out of git** (vc). There's no `.gitignore` for `intent/.cache/`, so `git add .` stages `intent.db`, which D34 says never enters history.
- **(3)** **`st done` ignores work-package status** (vc). A thread closed with a `not-started` WP; the gate is over criteria only.
- **(4)** **`at new --covers <missing AC>` refuses correctly, but with a PUT/POST remedy** unrelated to the error (vc).
- **(6)** **`intent upgrade` reports a whiteboard "still on disk" in a project that has none** (dc). `sync.rs:201-213` prints the static NOT_YET_BUILT list from `sync.rs:168` without checking for it.
- **(7)** **The app-not-installed remedy names `bin/devbin`, which a Homebrew user does not have** (dc, from reading the code at `macapp.rs:178`). Their route is `Intent.app.zip` from the release.
- **(8)** **The critic, from cc's engine lane:**
  - In a mixed proxy block, the refused lines are dropped silently (`critic.rs:841-846`).
  - Usage errors exit 1, which the gate reads as findings, while handler errors exit 2 (`exit_codes.rs:195-206`, dc's to rule).
- **(9)** **Rule proxies that contradict their own rule** (cc):
  - `test-highlander-shared-setup` and `real-code-over-mocks` fire on the pattern their Good prescribes.
  - `no-silent-failures` is single-line and never matches its own multi-line Bad.
  - `no-parse-ls` claims only SC2012 and misses SC2045, SC2011 and SC2010.
  - Every swift and lua rule is undeclared (`critic.rs:855-872`).
  - The gate lints the library's own Bad examples, so `strong-assertions/bad_test.exs` can't be committed without `--no-verify`.
- **(10)** **Parity tools that still point at the deleted v2 estate** (cc):
  - `read_claim_probe.sh:45` defaults to `bin/intent`.
  - `coverage_map.sh` refuses at HEAD.
  - `fixture_probe.sh`'s canaries are deleted.
  - `of_n_population.sh:249-252` contradicts `runner_roster_check.sh:255`.
- **(11)** **Dead artefacts held in place by code or an open ruling** (cc; each needs hv):
  - `lib/templates/hooks/module_check_hook.json`: nothing reads it, but a roster row in `exit_code_consumers.rs:128` names it.
  - `lib/templates/hooks/critic-guard.sh` is parked and has drifted: it lacks 0242's zero-scope report. Retire it, or re-roster it after porting that report.
  - The `rules/_schema/index-generator.md`, `rules/index.json` and `.template` trio sit behind the unwired `claude rules index`, whose retirement is pending hv. `index.json` is also wrong.
  - `lib/templates/llm/{_ARCHETYPES,_DECISION_TREE,_DEPENDENCY_GRAPH,_MODULES}.md` and `lib/templates/prj/st/ST####/{acceptance,design,impl,tasks}.md` are dead, and still embedded through `init.rs` DESTINATIONS rows. Deleting them is code.
  - **Stale comments in test and code files**, which a doc sweep can't reach without touching code (cc, listed in `817fa950b`):
  - hv-banned counts in bats and Rust test headers;
  - `guard_dispatch.bats:41` says pre-commit.sh is copied;
  - `schema_faces_drift.rs:6,39` name a test target that is now a module of `suite`;
  - `rules_path_guard.bats:20-21` grep paths that are gone, so they pass vacuously;
  - organize.rs, tui.rs, mcp_stdio.rs, plugins.rs and the Cargo.toml descriptions.
  - `intent/plugins/claude/subagents/.manifest/global-agents.json` (ic): no v3 code reads it, it has drifted, and its checksums are empty. But three bats tests assert it (critic_prose, rule_pack_shell, highlander_audit), so deleting it removes their assertions.
- **(12)** **The interface, from ic's lane:**
  - `claude skills|subagents|rules|ws --help` show blank subcommand descriptions: `values` is a bare `Vec<String>` (`dispatch.rs:345`), and `spine.rs:530` sets no `.about()`.
  - `guide.rs:156` renders a hardcoded count into the agent guide, and its module doc pins more.
  - `st show` doesn't display the objective.
  - `cost-metrics.sh` uses `--` as Elixir's comment prefix, and `tca-report.sh` has an unemitted `DEDUP_RATE` beside its dead checkbox guard.
- **(14)** **Driven by dc on the keg:**
  - `at new` fails in the argument order its usage prints, because the `--covers` variadic swallows the ids.
  - A project stamped below v2.19.0 is told to `install intent@2`, which no tap has.
  - A pre-v2.10 project (a top-level `.intent/`) is told "no Intent project found" (`project.rs:866`).
- **(15)** **Reported by dc's agents, not yet re-driven:**
  - `intent edit <ac address> --path` returns info.md, but the criterion renders in acceptance.md.
  - The `edit wp` remedy names `intent wp`, which has no body writer.
  - The `edit` kind refusal offers `issue`, then refuses it.
  - `intent set ... acceptance exempt` succeeds, though `transitions.rs` declares the field Immutable.
  - A losing concurrent write surfaces a raw `sqlite: database is locked`.
  - The generated info.md Acceptance paragraph routes readers to hand-edit canon plus `sync --to-store`, as if the verbs did not exist.
- **(16)** **CI, Swift and skill scripts** (dc):
  - `tests.yml` puts `bin/` on PATH, never the built `intent`, and its shellcheck selector `find bin -name "intent*"` matches nothing. `pr-checks.yml` watches `bin/` for source changes.
  - `IntentCLI.swift:192-194` reads stdout to EOF before stderr, so it can deadlock. `DaemonService.swift:69` sequences a restart that `daemon restart` ships.
  - `tail-orphan-probe.sh:135,182` reports a false LEAKED in its guarded arm.
  - `in-tca-init/scripts/tca-init.sh` makes WP directories the store never registers, and `tca-report.sh:128-144`'s guard can never fire.
- **(18)** **A seeded `usage-rules.md` carries a literal `[[PROJECT_NAME]]`** (cc). `canon.rs:350-364` copies `_usage-rules.md` raw, with no token substitution.
- **(19)** **The acceptance verbs enforce less than the docs promised** (vc's working-with-llms audit, driven):
  - `at green` from `to-write` succeeds. Red-first is an owed guard (`transitions.rs:666`).
  - `at na` accepts a test-backed row, and `at red|green` accept a non-test one.
  - `st done` and `wp done` close silently on a placeholder objective, though `model.rs:497` computes the condition and `/in-finish` promises the warning.
  - `.intent_critic.yml`'s `show_all` is read only by the subagent prompts.
- **(20)** **ST0057's projection, found auditing its design** (vc):
  - `organize --apply` and `st hydrate` report `hydrated:` for an opaque attachment and write nothing, because they pass only inline `text` (`organize.rs:577`, `:589`, `:1075`). The dehydration gate then fails on the working copy with a UTF-8 error and a misleading remedy.
  - D57-9's two-region manifest code is not removed (`intentfiles.rs:52`, `:135`, `:164`), and the parser still accepts a BEGIN/END pair.
  - Cross-project addresses parse but are refused by every door (`facade.rs:7235`, `:7385`, `:8071`, `:3072`), **while AC-07.6 reads green**. AC-03.1 likewise claims a working-copy round trip that fails for opaque attachments. **These are contract findings: a satisfied row the as-built does not meet.**
  - **ST0057 cannot close.** Its gate is BLOCKED only on AC-12.x and AC-13.x, whose WPs are both cancelled.
- **(21)** **ST0056, found auditing its design docs** (vc, `120c8b2ae`):
  - `todo.window_hours` is validated by doctor and applied by nothing (`doctor.rs:369`, `facade.rs:2289`).
  - The export bundle claims schema `intent/export@3.0`, which nothing publishes (`export.rs:42`).
  - The migrator's last line tells the user to commit, while AC-00.8 promises one visible commit (`render.rs:3558`).
  - `IN-RS-CODE-001` (critical) is enforced by nothing: the critic's clippy arm is a no-op (`critic.rs:987`).
  - `at new --status red|green|n-a` creates a row at a non-initial state; `st list --status tbc` is accepted; `AcceptanceTest.kind` is still marked Unbuilt.
  - `st/ST0056` is documented as an address and refused by the resolver; `intentd`'s `shell.html` carries its own palette.
- **(22)** **From batch 1's drives** (dc, not fixed):
  - `intent plugin list` on the keg answers `No plugins found.` while the dev tree lists `agents` and `claude`: `plugins.rs` reads `plugin.json` through a `read_dir` behind a function return, which no source scan follows, and `intent/plugins/agents/plugin.json` still does not ship.
  - The shim's other two remedies (a pointer resolving to a non-install; an install missing its gate) still say "reinstall Intent", which rewrites no pointer.
- **(23)** **The v2 exit tables in the dispatch register** (ic): its `as-observed` rows claim v3 reproduces v2's exits, and five of six sampled are false. The pages stop publishing them; re-measuring the register is size L+ and waits on hv.
