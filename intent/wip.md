---
verblock: "2026-09-17:v2.0: vc - leaned to bare bones on hv's instruction. DOING and TODO only, each item naming its owner and the condition that discharges it. No state that a command regenerates, and no content that already has a home: the close-out rulings are in intent/history/20260915-hv-rulings.md, the bank and landing rules in intent/restart.md, the cut sequence in intent/docs/releasing.md, and every issue in `intent issues show <id>`."
intent_version: 3.0.3
---

# Work In Progress -- closing out Intent's open issues and threads

Measure before believing anything below: `.claude/restart.md` carries the commands. **No count appears in this file** -- a count in a document a reader reads is a state with no command beside it, and every count this file used to carry had rotted.

## DOING

- **THE CLOSE-OUT** (hv, 2026-09-15): every open issue and thread closes on evidence or by ruling. Rulings: `intent/history/20260915-hv-rulings.md` and hv's board. What is open: `intent issues list`, `intent st list`. One lane lands at a time on vc's word; the bank, train and landing rules live in `intent/restart.md`.
- **The all-estate board trawl** (hv, 2026-09-16), cc's, Part A GO and Part B under vc's disposition (hv decision 24). Plan at `refs/bank/cc/trawl/carry-plan.md`. cc carries in each live estate, doctor after each, vc checks each before its commit, one mechanical commit per estate in its own repo. NO PUSH.
- **0442**, cc's, high: this estate's FTS5 index for `src_sections` went malformed, repaired live, and the cause investigation is CLOSED with the trigger unreproduced. Body and durable evidence: `intent issues show 0442`, `.backup/0442-evidence/`. It closes on the SQLite move and the detector below. **vc owes it two forward corrections**: "the crash/WAL family is ruled out" narrows to the crash family, because no arm tested a checkpoint-versus-commit race; and hv decision 25's stated reason for the bump is wrong, the hard-coded leaf shape being byte-identical at every candidate version.
- **0443**, dc's: Q2 and Q3 are in 3.1.0 and started -- a store error must never be reported as the reader's query being malformed, which hands them a task that cannot succeed. Q1 is ruled: the behaviour changes and the notes become true, with the watched-project predicate's error contract deliberately inverted at the call site because a read verb must reconcile when the daemon cannot answer.
- **0444**, cc's: the `wb migrate` register note has been false since 0374. It reaches `docs/reference` through the register, so it lands BEFORE the cut's regeneration or the cut ships it wrong.
- **vc**: judging every bank, train and landing; the 3.1.0 notes read for voice and for anything internal; the menubar app's LaunchServices `-600` at install. ic takes the verification half of this lean-out once vc is off both files -- drive every command, resolve every pointer, fix nothing, report everything.

## TODO

- **THE SQLITE MOVE AND THE DETECTOR, both in 3.1.0** (hv decision 25), cc's, judged at **rusqlite 0.40.2 / libsqlite3-sys 0.38.2 / SQLite 3.53.2** (`refs/bank/cc/bump/read`). **It is not a dependency bump.** SQLite 3.7.0 through 3.51.2 carry a WAL-reset corruption bug fixed in 3.51.3, whose preconditions -- two or more connections on one WAL file writing or checkpointing at the same instant -- this estate meets by construction, so the detector is the smaller half of the argument. Owed with it: every asserted REAL enumerated BEFORE the run, because 3.53.0 moves default float text to 17 significant digits; a test pinning where rusqlite's own statement gate and ours disagree; one rebuild and one whole-suite run, announced at both ends because `~/.local/bin/intent` symlinks into this tree's target dir.
- **`docs/known-defects.md` re-driven whole against 3.1.0 at the cut** (hv decision 25), cc's. The per-line worklist stays banked as input at `refs/bank/cc/release-notes/known-defects-lines.md`. 0442 gets an entry; 0343 deliberately gets none.
- **The LIVE guards adoption pass**, dc's, HELD. Released when `bin/int hooks` reports a canon-shim-wired hook AS wired, driven on one estate after its pass. dc drives the finding first -- report half only, **never `int hooks --install`**, which is the command that creates the double-run. Gates nothing in this release.
- **The cut is 3.1.0** (hv decision 24), cc's: the CHANGELOG section and `docs/releases/3.1.0/RELEASE_NOTES.md` at that number, because `bin/devbin build release` refuses a cut without the section. Also at the cut: hv's prepared `gh release edit v3.0.2` annotation.
- **The cut sequence is `intent/docs/releasing.md`** and every command in it has now been driven rather than composed. Its load-bearing rule: both halves of `docs/reference` are regenerated and COMMITTED BEFORE `build release` runs, because every route that regenerates after the tag ends in a refusal or in a tag that does not contain its own reference pages.
- **WITH hv, open**: whether release tags start being annotated or `--tags` is the durable answer, since `release:1221` is `git tag "$TAG"` with no `-a`; whether ic's banked staleness check is built; and the hook-ownership conflict, devbin#0078 having ruled that Intent owns hook wiring while decision 24 (4) routed dc's `int hooks` finding to Devbin.
- **ST0056** closes when hv runs `intent/st/ST0056/gyges-brief.md` on gyges and hands back `~/intent-clean-install.log`: vc judges it, dc satisfies AC-00.5 and AC-11.1 on it, WP-11 and the thread close on hv's word. Those are the only unsatisfied rows in its gate and both are held on that machine.
- **ST0060**, the intent vault thread, is the next line's first (hv, 2026-09-15). The verb does not exist yet -- building it is the thread.
- **ST0077** (level 3 stores the references only the toolchain sees, plus a roster of the targets it defines) is B of hv decision 22, for the line after the close-out.
