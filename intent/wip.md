---
verblock: "2026-09-15:v1.69: vc - the close-out, ruled by hv"
intent_version: 3.0.3
---

# Work In Progress -- closing out Intent's open issues and threads

## DOING

- The close-out (hv, 2026-09-15): every open issue and thread closes on evidence or by ruling. The rulings are in `intent/history/20260915-hv-rulings.md`; what is open is `intent issues list` and `intent st list`. One lane lands on the tree and the store at a time, on vc's word; a bank is a patch blob on `refs/bank/<node>/<issue>`; ready banks ride a train with one judging run; cc's matched control runs under a host hold.
- cc: 0338 (ii) regenerated on HEAD and its matched control; 0338 (i), with ST0057's AC-07.6 reworded to the refusal and the test that proves it; 0377; 0331 whole (the deletions and the stale comments); 0321's exemption for the daemon log; 0375.
- ic: 0399 (the explorer split, banked, judging); 0334 with 0396 and `ac gate`'s ratification; 0339 with D46's design line; 0177 onto the register's ext rows; ST0075 WP-02 and WP-03 with the menubar tooltip dropped; the as-written pass over tui-design.md, tui-redesign-proposal.md, parity.md and parity/.
- dc: 0398; install.md, migration.md and critic-gate.md as written against as built; the pair built privately and moved into place; `claude upgrade --apply`'s report naming `organize --apply` for stale views; the Devbin filing for artefact.lib's swallowed `strings` refusal.
- vc: judging every bank, train and landing; impl.md; ST0056's D43 and `new-surface` marked ratified and its withheld-13 row struck.

## TODO

- The quiet window, end of day on hv's word: `bin/devbin build all`, `intent daemon restart`, `bin/int macos app-install`, ic drives the Console once, one mechanical view commit, then `st done` for ST0057 and ST0075 on gate PASS.
- hv: the prepared `gh release edit v3.0.2` annotation; whether 0398's title is restored (a stray "s" from the 0399 drive).
- hv chooses the release version at the close-out.
- ST0056's clean install (AC-00.5, AC-11.1) and 0344: held until Intent installs and works properly on this machine, then gyges runs dc's runbook (hv, 2026-09-15, low priority).
- ST0060 (`intent vault`) stays open as the next line's first thread (hv, 2026-09-15).
