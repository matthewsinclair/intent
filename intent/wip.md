---
verblock: "2026-09-17:v1.80: vc - ST0076 WP-04 banked for judging, the WP-03 train, ic's CLAUDE.md line and the level-3 core behind it, 0432 and 0434 to 0436 open, ST0056 on the gyges run"
intent_version: 3.0.3
---

# Work In Progress -- closing out Intent's open issues and threads

## DOING

- The close-out (hv, 2026-09-15): every open issue and thread closes on evidence or by ruling. The rulings are in `intent/history/20260915-hv-rulings.md` and on hv's board (decision 18 adds devbin's whiteboard defects to this release, decision 19 is the go of 2026-09-17); what is open is `intent issues list` and `intent st list`. One lane lands on the tree and the store at a time, on vc's word; a bank is a patch blob on `refs/bank/<node>/<issue>`; banks that share a file land in the order they bank green and the second proves itself on top; each train has one whole-suite judging run, in the warm worktree that already holds its stack, taken by asking vc for the slot with START and END sent to every lane, and vc judges from the logs, the diff and a read-only check that the worktree equals the bank. Every write to the live store goes through the installed pair, never a worktree binary.
- ST0076, the typed symbol index for Rust and Elixir (hv, 2026-09-16), closing on its twelve ACs (`intent ac gate ST0076`): ic's WP-04 (surfaces, AC-04.1 and AC-04.2: `--kind`, a separate `--subkind` from a derived roster, `--in`, a search that runs on filters alone, every filter honoured by `--context` and `--outline`, one field name across SQL, JSON and MCP, and the explorer's note drawn on the screen) is banked at refs/bank/ic/st0076-wp04-wip and is judged first, stacked on 0433's landing; then cc's extractor shapes (refs/bank/cc/st0076-extractor-shapes) with dc's WP-03 (Elixir qualified references, AC-03.1, the brace, `__MODULE__`, atom-alias and local-capture forms included) land as one train on one gate run in dc's worktree, cc's landing bringing design.md's Rust cells to what WP-02 built and dc's the Elixir cells; whichever of WP-03 and WP-04 lands second rewrites the published sentence of what a reference misses, with `level_words(2)` and the register regen, to the text vc ruled; cc then builds level 3's one core (vc decision 25: a side table of one row per path, line, name and target, every written reference as the population, staleness once per file, five counts, one explicit verb `intent index resolve`, never from intentd and not on the MCP tier) as WP-05's first bank at refs/bank/cc/st0076-l3-core, with the SCIP reader behind it (AC-05.1), and dc stacks WP-06 (the Elixir tracer, AC-06.1, AC-06.2 on Laksa) on the core. Arms that need a real toolchain are `#[ignore]` under a module path containing `needs_the_toolchain`, fail by name when the tool is absent, stay out of CI and run under `bin/devbin test all` and `bin/devbin fullcycle` (hv, 2026-09-17); dc adds that devbin test option with the first such landing. AC-00.1 is hv's drive: its middle clause (`intent search --subkind method --in AddressError`) after WP-04 lands and hv rebuilds again; its third clause (a fresh session reaches the index before grep) on ic's CLAUDE.md line (hv decision 20, 2026-09-17, yes): after WP-04, ic builds into the canon `_CLAUDE.md` template one line telling a session to load `mcp__intent__intent_search` through ToolSearch and ask it before grep, because the MCP server's instructions are delivered and not followed (a fresh headless session reached the index first in one run of three without such a line and in three of three with it appended to the system prompt); the clause and 0428's live proof are three fresh runs that all reach the index first with the line in the real CLAUDE.md, driven by vc with refs/bank/vc/drive-0428.
- Open issues otherwise: 0432 (hv's blank explorer after nvim): hv drove it on the rebuilt pair on 2026-09-17 (issue 0432's body through nvim, `:q!`, and the explorer redrew correctly), so neither hv nor ic reproduces it on current binaries; vc recommends closing it as not reproduced, to reopen on a `script -q` capture if it recurs, and it waits on hv's word. 0434 (seven intentd notices with a remedy and no severity token, and an ingest failure's remedy written twice): dc, banked at refs/bank/dc/0434, lands after WP-03 as a train of one, with ic's Swift cleanup of the Console's dead `intentd: could not` branch right behind it. 0435 (the explorer clears a view's note before its first frame, so ST0069 AC-21.1's freshness line never reached a screen): ic, fixed inside WP-04 and closed by its landing. 0436 (an ingest that meets a busy store past the five-second wait still says to fix the artefacts): dc, low, after 0434.
- vc: judging every bank, train and landing; recording hv's drives.

## TODO

- hv's remaining items on the pair rebuilt at c800b8319: a declared guard (0426, or dc's guards adoption pass on this estate as its live drive), and hv's word on closing 0432 as not reproduced. Then, after WP-04 and ic's CLAUDE.md line land: hv rebuilds again (`bin/devbin build all && intent daemon restart && bin/int macos app-install`, chained so the restart follows the promotion with nothing between), drives AC-00.1's middle clause, `intent claude upgrade --apply` brings this project's CLAUDE.md to the new template and vc drives the third clause three times, and `intent/st/ST0056/parity/tools/gen_reference.sh` regenerates `docs/reference/` whole as its own commit.
- hv rules whether level 3 also stores the references only the toolchain sees (vc brings it as a decision): a resolved row must join a written reference today, so the internal references rust-analyzer finds and the syntax queries do not (field, const, static and variant uses, functions passed as values) are counted and not stored, and a general never-referenced answer stays approximate for those kinds. The side table has no foreign key to the written rows, so the policy is one predicate at write.
- The all-estate board trawl, on vc's word once hv's drives pass (hv, 2026-09-16): a plain `intent wb migrate` CARRIES FOR REAL on a board with nothing uncarried, so dc runs it only in scratch clones, one per estate (every registered estate, Conflab included, covering every board not yet in the store: a board with no registered node is registered in the clone first, so the worklist shows the real path, register then migrate): the store copied with `sqlite3 .backup` (never `cp`, every store is WAL), the estate's working `intent/whiteboard/` files copied over the clone so uncommitted board edits count, an isolated HOME and no daemon. dc records each node's exit status and its `uncarried:`, `coerced:` and `left in place:` lines, and names already-migrated nodes as skipped. vc sends hv one worklist; hv rules restructure or drop per unit or by class; dc carries in the live estates, doctor runs, vc checks each estate, and each estate gets one mechanical commit in its own repo. NO PUSH.
- The guards adoption pass, dc's, beside the trawl (vc, 2026-09-16, on 0426): per estate, declare the project's own guards in config.json `guards`, remove the calls it duplicates from the hand-wired or tracked hook, and run doctor clean; coordinated with devbin for the repos whose `.githooks/` and `bin/int` it manages (Intent, Gtools, Laksa); one commit per estate. NO PUSH.
- hv: the prepared `gh release edit v3.0.2` annotation.
- hv chooses the release version at the close-out.
- ST0056's clean install (AC-00.5, AC-11.1) and 0344's never-tapped leg: after the rebuild and drive pass here, hv runs `intent/st/ST0056/gyges-brief.md` on gyges and hands back `~/intent-clean-install.log`; vc judges it, dc satisfies AC-00.5 and AC-11.1 on it, and WP-11 and ST0056 close on hv's word.
- ST0060 (`intent vault`) stays open as the next line's first thread (hv, 2026-09-15).
