---
verblock: "2026-09-21:v3.4: vc - globalfold at the end of the day: 0501 opened on hv's go and banked as work in progress in dc's hand; main pushed and both workflows green; v3.3 folded after the 3.2.0 line closed; v3.0 leaned this file to DOING and TODO only"
intent_version: 3.2.0
---

# Work In Progress

Measure before believing anything below: `.claude/restart.md` carries the commands, `intent/restart.md` the rules every bank, train and landing runs under. No count appears in this file.

## DOING

- **0501**, dc's hand, S, opened on hv's go (2026-09-21 21:05Z): devbin's `int check` twins of CI's fmt, clippy, panics and doc lines join the release driver's cargo gate in its private worktree, each red aborting by name and the green verdict naming every step; the doc twin gains CI's `RUSTDOCFLAGS=-Dwarnings`, CI's own line moving to that spelling so `tests/unit/devbin_rust_gates.bats` stays a byte equality once it learns a step's `env:`. Work in progress at `refs/bank/dc/0501-wip/patch` (blob 459c23e1f, patch-id 08df1634f6cc, base 341d48cdd). In hand: the four doc verdict lines under both spellings, the twin test with its negative control, `bin/int check doc` real at rc 0 and rc 101 planted. Owed: the release fixture's two dry-run arms on a stubbed cargo, the whole bats suite on macOS in a private worktree, the judging bank, the close with `organize --apply`; the rust.yml line is judged on hv's next upstream push.
- **vc**: judging 0501's bank; hv's inbox read at every pickup; every push is hv's.

## TODO

- **WITH hv**: the pushes of Prodinfra, Riffle, Baize, arca_config, Conflab and arca_cli from their own checkouts (Molt is level); Prolix's whiteboard carry, hv-first, dc's hand, never `--drop-uncarried` on hv's rows; the rulings held "until hv rules:" on Conflab's and Baize's boards, ic's ST0016 AC-01.2 question among them; the roster question for Riffle, arca_config and arca_cli (vc: none owed); hold 18's hook-fix owner; the 3.2.0 app into /Applications on this machine; Lamplight's pre-commit hook, which re-stages Markdown from the working tree (issue 0498's mechanism, that estate's to repair); the root files and hook carriers of Conflab and arca_cli, still 3.1.0's.
- **NEXT-LINE**: cc trims `intent/restart.md`'s home-pointer line, held by 0492; `tests/unit/devbin_seal_disagreement.bats` pins a vendored devbin line its upstream suite no longer pins.
- **ST0060** and **ST0077** stay in Triage as the next line's queue (hv, 2026-09-18).
- **The LIVE guards adoption pass**, dc's, HELD until `bin/int hooks` reports a canon-shim-wired hook as wired (the defect is devbin's, hv Q7); never run `int hooks --install`.
