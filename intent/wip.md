---
verblock: "2026-09-18:v2.1: vc - rewritten from the unified plan hv approved on the bounce (vc decision 38) after the morning's train landed. DOING and TODO only, each item naming its owner and the condition that discharges it. No state that a command regenerates, and no content that already has a home: the close-out rulings are in intent/history/20260915-hv-rulings.md, the bank and landing rules in intent/restart.md, the cut sequence in intent/docs/releasing.md, every issue in `intent issues show <id>`, and ST0078's proposal in its design.md."
intent_version: 3.0.3
---

# Work In Progress -- closing out Intent's open issues and threads, then the fleet

Measure before believing anything below: `.claude/restart.md` carries the commands. **No count appears in this file** -- a count in a document a reader reads is a state with no command beside it, and every count this file used to carry had rotted.

## DOING

- **THE CLOSE-OUT** (hv, 2026-09-15): every open issue and thread closes on evidence or by ruling. Rulings: `intent/history/20260915-hv-rulings.md` and hv's board. What is open: `intent issues list`, `intent st list`. One lane lands at a time on vc's word; the bank, train and landing rules live in `intent/restart.md`. The SQLite move (rusqlite 0.40.2 / SQLite 3.53.2, with `fallible_uint` on), the 0442 detector and every fix landed on 2026-09-18 are on main and the pair carries them; `intent issues list` is the open set.
- **ST0078**, IN 3.1.0 (hv, 2026-09-18, vc decision 43): Intent on a multi-person project with a git workflow and PRs. The proposal at `intent/st/ST0078/design.md` is accepted whole: P1 the event log travels (D53 reversed, one file per event under `YYYY/MM/DD`), P2 renumber verbs, P3 the store after a pull (a CLI ingest door, three git hooks wired by `claude upgrade --apply`, store-stale shown), P4 the team page. The five work packages and their criteria are minted (`intent wp list ST0078`, `intent ac list ST0078`) and assigned: cc WP-01 (P1, M), ic WP-02 (P2, M) and WP-04 (P4, S, closes last), dc WP-03 (P3, S, lands first) and WP-05 (P5, M, hv's one command after a pull: `intent sync [--apply] [--to-disk|--to-store]`, ruled 2026-09-18, built after P3 and P2 land). Each lane banks a patch blob under `refs/bank/<lane>/st0078/`, mints its AT rows in the bank, and lands on vc's word in the order P3, P2, P5, P1, P4. The fleet trawl and the cut wait behind it.
- **The all-estate board trawl** (hv, 2026-09-16), cc's, Part A GO and Part B under vc's disposition (hv decision 24). Plan at `refs/bank/cc/trawl/carry-plan.md`. It is the whiteboard subset of the fleet trawl and runs with it.
- **vc**: judging every bank, train and landing; the 3.1.0 notes read for voice and for anything internal; the menubar app's LaunchServices `-600` at install; ic's verification pass over this file.

## TODO

- **THE 3.1.0 CUT SEQUENCE**, what is left of it, each on vc's word, the sequence itself in `intent/docs/releasing.md`: cc re-drives `docs/known-defects.md` whole against the rebuilt pair (hv decision 25, vc's ruling (a): a non-reproducing entry leaves the page and the CHANGELOG is its record), 0442 gets an entry and 0343 deliberately none; hv cuts in hv's terminal, and the release preflight now refuses a stale `docs/reference` on its own; then gyges. The CHANGELOG section, the release notes and the regenerated reference pages are already committed.
- **The running intentd is the pre-bump image until hv restarts it**: `intent daemon restart` in hv's terminal, or hv's word to cc. Until then one WAL file has a 3.46.0 writer beside 3.53.2 writers, which is the WAL-reset defect's precondition on the daemon's side, and 0450 records the one dirty reading the detector made on the live store while that writer was writing; 0450 closes on cc's detector change landing and a clean re-read after the restart.
- **Small issues that close before the cut, on merits**: 0450 (the detector's one dirty reading during the 3.46.0 daemon's write) landed its detector half and closes on cc's clean re-read after hv's daemon restart. 0453 (no verb repairs an unreadable search-index table in place) is the next line's, filed with its drive.
- **The LIVE guards adoption pass**, dc's, HELD. Released when `bin/int hooks` reports a canon-shim-wired hook AS wired; the defect is devbin's (hv Q7). **Never run `int hooks --install`.** Gates nothing in this release.
- **THE FLEET TRAWL** (hv, 2026-09-18, the day's second priority): every project in `~/.config/intent/projects.json` upgraded and pristine under `intent doctor` and `intent organize`, one mechanical commit per estate in its own repo, NO PUSH. Rehearsed in scratch clones and banked at `refs/bank/dc/trawl/` with the estate split between dc and ic in `split.md`; the stops are recorded there: half are 0446, the rest are the owner's judgment (Conflab's v2 copies) or artefacts of rehearsing a live node under a private home. re-rehearsed on the pair carrying 0446 with only the banked stops; the live run waits behind ST0078's build so every estate is touched once (hv Q3 and decision 43); devbin#0083's sweep after it (hv Q8). The dead registry entry is already pruned.
- **WITH hv**: the daemon restart, which hv said on 2026-09-18 happens now (still the 3.46.0 image until it does; 0450 closes on cc's clean re-read after it); Lamplight's mix.lock, hv's to commit or discard before ic's trawl run reaches Lamplight. Ruled 2026-09-18 on vc's recommendations and carried by the lanes: Lamplight's ST0347 view is regenerated (the store is right) and its ST0001 v2 bucket is moved home by ic during the run; Devbin's five board files clear when its own nodes fold; Conflab's 67 copies are cc's ruling as owner of record; machine-scoped events stay store-only and `intent upgrade` backfills project events once (cc, WP-01).
- **ST0056** closes when hv runs `intent/st/ST0056/gyges-brief.md` on gyges against the PUBLISHED 3.1.0 and hands back `~/intent-clean-install.log`: vc judges it, dc satisfies AC-00.5 and AC-11.1 on it, WP-11 and the thread close on hv's word.
- **ST0060** and **ST0077** stay in Triage as the next line's queue (hv Q4, 2026-09-18); neither is unfinished work.
