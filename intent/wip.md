---
verblock: "2026-09-17:v1.84: vc - ST0076 on level 3: the Rust reader is landed and resolves this estate; dc's WP-06 and ic's WP-07 remain; no open issues; hv's level-3 population decision is asked; ST0056 on the gyges run"
intent_version: 3.0.3
---

# Work In Progress -- closing out Intent's open issues and threads

## DOING

- The close-out (hv, 2026-09-15): every open issue and thread closes on evidence or by ruling. The rulings are in `intent/history/20260915-hv-rulings.md` and on hv's board (decision 18 adds devbin's whiteboard defects to this release, decisions 19 and 20 are the rulings of 2026-09-17); what is open is `intent issues list` and `intent st list`. One lane lands on the tree and the store at a time, on vc's word; a bank is a patch blob on `refs/bank/<node>/<issue>`, and the judged patch-id is what lands: a gate refusal at landing is fixed in the worktree and re-banked before any commit. Banks that share a file land in the order they bank green and the second proves itself on top, stacking on the first one's bank so one run stands for the landing. Each train has one whole-suite judging run, in the warm worktree that already holds its stack, taken by asking vc for the slot with START and END sent to every lane; vc judges from the logs, the diff and a read-only check that the worktree equals the bank. A target dir is never shared between worktrees, not even one after the other, and `bin/int macos app-test` runs under its own `INTENT_MACOS_STATE_DIR`. Every write to the live store goes through the installed pair, never a worktree binary.
- ST0076, the typed symbol index for Rust and Elixir (hv, 2026-09-16), closing on its ACs (`intent ac gate ST0076`); what remains is level 3's Elixir reader and its surfaces. The core and the Rust reader are landed and in the installed pair (vc decisions 25 and 26: a side table of one row per path, line, name and target, every written reference as the population, staleness once per file and by extractor version, the run's counts partitioning everything the tool emitted, one explicit verb `intent index resolve`, never from intentd and not on the MCP tier), and vc has driven the verb on this estate. dc builds WP-06, the Elixir tracer (AC-06.1, AC-06.2 on Laksa; vc decision 27), into a cache of Intent's own under `intent/.cache/resolve/elixir/<env>` that the first run seeds from the project's `_build` with its symlinks re-pointed, re-seeded when `mix.lock` differs, a seeding run forcing the compile (a dep Mix must still build there runs rebar3, which writes its per-dep cache files under `deps/`); its roster is `expansion` (a call or module event with no column, with a failed run where no event carries one), `operator` (one word and one test for both languages, moving into resolved.rs in dc's landing) and `quoted`; `mix.exs` is read at the index root only. ic builds WP-07, level 3's surfaces (AC-07.1: `index.resolution` in every answer and a hit at level 3 only when its key names exactly one target, else its syntax level with `candidates`; AC-07.2: the by-target door `--target`, exact, one of N where a key names several, no hit from a stale file, an unknown target refused with the nearest ones after a trailing `/arity` or `()` is set aside, and no stored resolution refused with the remedy `intent index resolve`), which AC-06.2's Laksa proof runs through; a carried language is named `unresolved` only where the index holds the manifest its reader resolves from, decided at answer time through a seam on the Resolver trait that WP-06's reader also implements. WP-07 banks first and WP-06 stacks on it; both share resolved.rs, so they land in the order they bank green. Arms that need a real toolchain are `#[ignore]` under a module path containing `needs_the_toolchain`, fail by name when the tool is absent, stay out of CI and run under `bin/devbin test toolchain`, which `bin/devbin test all` includes (hv, 2026-09-17); `bin/devbin fullcycle` needs devbin#0083, filed in the Devbin estate, which hv routes.
- The all-estate board trawl (hv, 2026-09-16), started on vc's word on 2026-09-17 and moved from dc to cc so dc stays on WP-06: a plain `intent wb migrate` CARRIES FOR REAL on a board with nothing uncarried, so cc runs it only in scratch clones, one per estate (every registered estate, Conflab included, covering every board not yet in the store: a board with no registered node is registered in the clone first, so the worklist shows the real path, register then migrate): the store copied with `sqlite3 .backup` (never `cp`, every store is WAL), the estate's working `intent/whiteboard/` files copied over the clone so uncommitted board edits count, an isolated HOME and no daemon. cc records each node's exit status and its `uncarried:`, `coerced:` and `left in place:` lines, and names already-migrated nodes as skipped. vc sends hv one worklist; hv rules restructure or drop per unit or by class; cc carries in the live estates on hv's rulings, doctor runs, vc checks each estate, and each estate gets one mechanical commit in its own repo. NO PUSH.
- vc: judging every bank, train and landing; recording hv's drives.

## TODO

- The next rebuild (`bin/devbin build all && intent daemon restart && bin/int macos app-install`, chained so the restart follows the promotion with nothing between; vc runs it, announcing the hold to every session, the two Devbin ones included, and no commit lands while it builds): it follows WP-06's and WP-07's landings. After the last rebuild of the close-out, `intent/st/ST0056/parity/tools/gen_reference.sh` regenerates `docs/reference/` whole as its own commit.
- hv's remaining item: a declared guard (0426, or dc's guards adoption pass on this estate as its live drive).
- hv rules whether level 3 also stores the references only the toolchain sees; vc put the decision to hv on 2026-09-17 with the live run's numbers and recommends keeping today's rule for ST0076 and taking the wider store as its own thread. A resolved row must join a written reference today, so the uses rust-analyzer finds and the syntax queries do not (fields, consts and statics, module mentions, variants in patterns, functions passed as values) are counted as unmatched and not stored, and a never-referenced answer stays approximate for those kinds. The side table has no foreign key to the written rows, so the policy is one predicate at write; whether `--target` can tell a target nothing references from a misspelt one rides the same ruling, and ic's register words for what level 3 does not see wait on it.
- The guards adoption pass, dc's, beside the trawl (vc, 2026-09-16, on 0426): per estate, declare the project's own guards in config.json `guards`, remove the calls it duplicates from the hand-wired or tracked hook, bring the tracked `.githooks/pre-commit.intent` shim to canon (on this estate `intent claude upgrade` still reports it as to be written: two remedy sentences), and run doctor clean; coordinated with devbin for the repos whose `.githooks/` and `bin/int` it manages (Intent, Gtools, Laksa); one commit per estate. NO PUSH.
- hv: the prepared `gh release edit v3.0.2` annotation.
- hv chooses the release version at the close-out.
- ST0056's clean install (AC-00.5, AC-11.1) and 0344's never-tapped leg: after the rebuild and drive pass here, hv runs `intent/st/ST0056/gyges-brief.md` on gyges and hands back `~/intent-clean-install.log`; vc judges it, dc satisfies AC-00.5 and AC-11.1 on it, and WP-11 and ST0056 close on hv's word.
- ST0060 (`intent vault`) stays open as the next line's first thread (hv, 2026-09-15).
