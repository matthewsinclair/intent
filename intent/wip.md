---
verblock: "2026-09-21:v3.1: vc - localfold after the restart, before the compact: the issues line is down to hv's two rulings, the seven whiteboard carries start on the bounce; v3.0 leaned this file to DOING and TODO only on hv's instruction of 2026-09-17, and the version history is in git."
intent_version: 3.1.0
---

# Work In Progress

Measure before believing anything below: `.claude/restart.md` carries the commands, `intent/restart.md` the rules every bank, train and landing runs under. No count appears in this file.

## DOING

- **THE ISSUES LINE** (hv, 2026-09-19; decisions 55 and 56 on vc's board): every open issue (`intent issues list`) is fixed, not triaged, one landing at a time on vc's word. What is left waits on hv: 0486 (may a shipped CHANGELOG line take a dated correction beneath it, verbatim line kept) and 0460 (`st relate` / `st unrelate`, cc's plan as proposed). 0481 stays open by design until a real `dvb build release` gate passes its six arms first time with the host load read at the floor (its body carries why a pass under load is not evidence). 0493, 0494, 0495 and 0496 are filed as next-line work and not built (hv, 2026-09-20: no testing tests, no yak-shaving, the fleet first).
- **THE FLEET** (hv, 2026-09-19 and 2026-09-20): every registered estate is at 3.1.0 except Laksa, which runs wave one behind its deploy freeze and reports to vc. The seven hand-authored whiteboards migrate from here on the bounce, the pair and the running intentd both at 510409ccc: ic carries Riffle (first, the rehearsal), then Baize, Molt and arca_config; dc carries Conflab, Prolix and arca_cli after ic's Riffle report is read. Per estate: `intent wb register` with no arguments, `intent wb migrate <node>` per node (hv first where an hv node exists, so Prolix is hv, cc, vc; otherwise alphabetical; a node carries every board on its estate and never drops an uncarried row on hv's, which goes to hv through vc quoted), the report read and judged before any `--drop-uncarried`, one commit by literal paths in that repo, no README invented, no push. The check after each carry is vc decision 57's: `intent wb show <node>` against the rendered file item by item, the rendered boards hashed before and after, doctor's counted skew once migrated; doctor-clean before a carry says nothing. devbin's 0.1.2 sweep is committed at Intent (42915c049) and counts as swept when hv's push is green.
- **vc**: judging every bank and landing; the lean-out of `intent/restart.md` (hv, 2026-09-17) after the carries, carrying the three lines vc todo 59 names.

## TODO

- **WITH hv**: the push of main (everything since hv's 2c3ce6b3b is judged and unpushed, `git log upstream/main..main`; that run's CI is read); 0460's ruling and 0486's decision-7 answer; dc's tap README commit at `/opt/homebrew/Library/Taps/matthewsinclair/homebrew-intent` and Prodinfra's three unpushed commits; the three stash entries; ST0056's close on the gyges run of `intent/st/ST0056/gyges-brief.md` against the published 3.1.0 (vc judges the log, dc satisfies AC-00.5 and AC-11.1, WP-11 and the thread close on hv's word); the roster question for Riffle, arca_config and arca_cli, which have no whiteboard README, after the carry reports.
- **ST0060** and **ST0077** stay in Triage as the next line's queue (hv, 2026-09-18).
- **The LIVE guards adoption pass**, dc's, HELD until `bin/int hooks` reports a canon-shim-wired hook as wired (the defect is devbin's, hv Q7); never run `int hooks --install`.
