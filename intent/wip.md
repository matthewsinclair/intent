---
verblock: "2026-09-17:v1.90: vc - the pair is current at 58501a8a2 and carries 0441, which is landed, closed and installed; ST0076 is 14/14 and closes on hv's word; three issues are open and all cc's (0442 the store's FTS5 index, 0443 the search's write path and its error, 0444 the wb migrate note); the trawl's checks and its carry wait on the installed-pair runs and hv's rulings; the next release's notes are banked until hv names the version"
intent_version: 3.0.3
---

# Work In Progress -- closing out Intent's open issues and threads

## DOING

- The close-out (hv, 2026-09-15): every open issue and thread closes on evidence or by ruling. The rulings are in `intent/history/20260915-hv-rulings.md` and on hv's board (decision 18 adds devbin's whiteboard defects to this release, decisions 19 and 20 are the rulings of 2026-09-17); what is open is `intent issues list` and `intent st list`. One lane lands on the tree and the store at a time, on vc's word; a bank is a patch blob on `refs/bank/<node>/<issue>`, and the judged patch-id is what lands: a gate refusal at landing is fixed in the worktree and re-banked before any commit. Banks that share a file land in the order they bank green and the second proves itself on top, stacking on the first one's bank so one run stands for the landing. Each train has one whole-suite judging run, in the warm worktree that already holds its stack, taken by asking vc for the slot with START and END sent to every lane; vc judges from the logs, the diff and a read-only check that the worktree equals the bank. A target dir is never shared between worktrees, not even one after the other, and `bin/int macos app-test` runs under its own `INTENT_MACOS_STATE_DIR`. Every write to the live store goes through the installed pair, never a worktree binary; the 3 s spacing that stood until 0441 was installed is retired with the rebuild of 2026-09-17. A board item's text goes in as a command substitution of a file, never inline in double quotes: a backticked phrase in item prose is a shell's command substitution, and one of them started that rebuild.
- ST0076, the typed symbol index for Rust and Elixir (hv, 2026-09-16): `intent ac gate ST0076` passes and every work package is Done, so it closes on hv's word; vc recommends closing it now, and dc's re-drive of AC-06.2 with the installed pair on a scratch copy of Laksa is an optional second basis for that word rather than a condition of it. Arms that need a real toolchain are `#[ignore]` under a module path containing `needs_the_toolchain`, fail by name when the tool is absent, stay out of CI and run under `bin/devbin test toolchain`, which `bin/devbin test all` includes (hv, 2026-09-17); `bin/devbin fullcycle` needs devbin#0083, filed in the Devbin estate, which hv routes. What level 3 leaves for the next line is ST0077's (see TODO).
- The all-estate board trawl (hv, 2026-09-16), started on vc's word on 2026-09-17 and moved from dc to cc so dc stays on WP-06: cc's first pass ran in scratch clones of the 15 registered estates with a whiteboard (population: `~/.config/intent/projects.json`; the frozen `_Archive/Intentv2` deliberately excluded), stores copied read-only and integrity-checked, nothing written live; the worklist is banked at refs/bank/cc/trawl/worklist.md (51 nodes: 25 already carried, 5 carried in the clones, 21 refused over 236 units in six classes) and is with hv, who rules restructure or drop per class or unit (vc's recommendation: drop every class but the 31 asks of hv under `## For hv`-like headings in Baize, Conflab, Lamplight and Prolix, which cc carries verbatim as holds; register Gtools' hv board). The pair now carries the two migrate fixes (a dropped inbox line is kept in a copy of its inbox; a view Intent rendered is read with its banner set aside, vc decision 29), so cc's next act is re-running the pass on Courses hv and Conflab cc with the installed pair (refs/bank/cc/trawl/trawl-check-0438.sh), then the ten rehearsals and a regenerated plan. cc's carry plan is banked at refs/bank/cc/trawl/carry-plan.md, rehearsed in scratch clones with nothing written live: Part A (Riffle cc, Molt hv, Prolix hv and vc, arca_cli cc, and Courses hv) needs only hv's go; Part B carries the 21 refused nodes under vc's disposition, every dropped unit kept in its node's pre-migration copies, with the asks of hv back as holds on the asking node's own board (vc's refined recommendation: the 16 units that read as asks and Conflab cc's 12-row decision table as 12 holds, the framing and information units staying in the copies). Each estate's quiet check before its carry and before its commit is intentd's registry reading it not watched, with `ingested` unchanged (vc decision 30 (5)). Then, on hv's rulings, cc carries in the live estates, doctor runs, vc checks each estate, and each estate gets one mechanical commit in its own repo. NO PUSH.
- Issue 0442, cc's, high (dc's find of 2026-09-17): this estate's own store carried a malformed FTS5 index for `src_sections`, so a plain `intent search` on an affected term failed while `intent doctor` and `intent index status` both read clean over it. vc repaired the live store with `intent index rebuild` at 18:15:45Z and a repair is not a fix: the damaged bytes survive only in dc's two locked copies under its guards evidence, and cc is on the cause -- SQLite 3.46.0's secure-delete, whose 3.46.1 fix names false integrity reports, against our own two delete paths and the mixed state schema step 26 left by turning secure-delete on without a rebuild. 3.54.0 and 3.53.4 both called the damaged copy corrupt, and the vocab diff and the aborting `MATCH` are behaviour rather than a checker's verdict. The fix, a schema step that rebuilds both FTS tables once, and doctor's advisory finding on its cost are vc's to rule on cc's report.
- Issue 0443, cc's, medium: a plain `intent search` reconciles and writes even on an estate a running intentd already watches (`render.rs:440` and `3703`), which v3.0.2's notes say it does not, and `facade.rs:3843` maps a store error out of the query to `BadQuery` with an FTS5 syntax remedy, so the same damage met one step later blames the reader's query.
- Issue 0444, cc's, medium: the `wb migrate` register note and `Facade::wb_migrate`'s doc say an over-bound migrated inbox refuses new sends until its owner clears it, and 0374's landing made that false, because `wb_live_message_count` counts only rows with `authored_at IS NULL`. It reaches `docs/reference` through the register, and the cut's regeneration carries the correction.
- vc: judging every bank, train and landing; recording hv's drives; the menubar app's install, which copied the app and could not launch it at the 2026-09-17 rebuild (LaunchServices error -600).

## TODO

- The three installed-pair runs, each on vc's word: cc's `refs/bank/cc/trawl/trawl-check-0438.sh` on Courses hv and Conflab cc; dc's re-drive of AC-06.2 on a scratch copy of Laksa under an isolated HOME, with intentd's registry read before and after; ic's 0439 reproduction in daemon mode, which is the end-to-end proof this pair carries both fixes.
- The guards adoption pass, dc's, beside the trawl (vc, 2026-09-16, on 0426): per estate, declare the project's own guards in config.json `guards`, remove the calls it duplicates from the hand-wired or tracked hook, bring the tracked `.githooks/pre-commit.intent` shim to canon, and run doctor clean; one commit per estate, NO PUSH. **It is blocked on this estate by dc's finding, banked at refs/bank/dc/guards/finding-int-hooks.md**: devbin's `int hooks` decides a hook is wired by grepping the hook file for the runner, so the pass makes its report false at exit 1 and `int hooks --install` restores the double-run. That finding and devbin#0083 are cross-estate, so hv routes them.
- hv's remaining item: a declared guard (0426, or dc's guards adoption pass on this estate as its live drive).
- The next release: cc banks the CHANGELOG section and `docs/releases/<version>/RELEASE_NOTES.md` under refs/bank/cc/release-notes/ with the version left as a placeholder, because `bin/devbin build release` refuses a cut with no `## [X.Y.Z] - in progress` section and nothing has been written since v3.0.3; hv chooses the version at the close-out; hv's prepared `gh release edit v3.0.2` annotation; and the closed issues whose defects are on `docs/known-defects.md` each want a line there at the cut.
- At the cut, both halves of `docs/reference` are regenerated at the TAG against the previous release, in one commit -- `gen_reference.sh` writes 20 pages and `gen_cut_surface.sh` the 21st, and each reads the register at the revision, so the register may move until then. ic writes that step's durable home beside the notes, because it survives in the commit record alone (vc decision 31 (2)).
- ST0056's clean install (AC-00.5, AC-11.1) and 0344's never-tapped leg: after the drive pass here, hv runs `intent/st/ST0056/gyges-brief.md` on gyges and hands back `~/intent-clean-install.log`; vc judges it, dc satisfies AC-00.5 and AC-11.1 on it, and WP-11 and ST0056 close on hv's word.
- ST0060 (`intent vault`) stays open as the next line's first thread (hv, 2026-09-15).
- ST0077 (level 3 stores the references only the toolchain sees, and a roster of the targets it defines) is B of hv's ruling of 2026-09-17 ("Ok, A now, then B post.", hv decision 22): opened by vc with the live Rust run's numbers and dc's Laksa measurements in its context, Triage, for the line after the close-out.
