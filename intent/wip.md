---
verblock: "2026-09-17:v1.89: vc - ST0076 is 14/14 with every work package done and closes on hv's word; 0440 is landed and closed, and ic's 0441 is the one open issue; the register is final, so ic regenerates docs/reference and vc then rebuilds the pair; the trawl's carry plan waits on hv's rulings and the rebuild; ST0056 on the gyges run"
intent_version: 3.0.3
---

# Work In Progress -- closing out Intent's open issues and threads

## DOING

- The close-out (hv, 2026-09-15): every open issue and thread closes on evidence or by ruling. The rulings are in `intent/history/20260915-hv-rulings.md` and on hv's board (decision 18 adds devbin's whiteboard defects to this release, decisions 19 and 20 are the rulings of 2026-09-17); what is open is `intent issues list` and `intent st list`. One lane lands on the tree and the store at a time, on vc's word; a bank is a patch blob on `refs/bank/<node>/<issue>`, and the judged patch-id is what lands: a gate refusal at landing is fixed in the worktree and re-banked before any commit. Banks that share a file land in the order they bank green and the second proves itself on top, stacking on the first one's bank so one run stands for the landing. Each train has one whole-suite judging run, in the warm worktree that already holds its stack, taken by asking vc for the slot with START and END sent to every lane; vc judges from the logs, the diff and a read-only check that the worktree equals the bank. A target dir is never shared between worktrees, not even one after the other, and `bin/int macos app-test` runs under its own `INTENT_MACOS_STATE_DIR`. Every write to the live store goes through the installed pair, never a worktree binary, and until 0441 is installed each one is spaced about 3 s from the next and read back past intentd's ingest before any commit.
- ST0076, the typed symbol index for Rust and Elixir (hv, 2026-09-16): `intent ac gate ST0076` passes and every work package is Done, so it closes on hv's word; vc recommends closing it now that 0440, the counts its status prints after an incremental run, is landed and closed too. Arms that need a real toolchain are `#[ignore]` under a module path containing `needs_the_toolchain`, fail by name when the tool is absent, stay out of CI and run under `bin/devbin test toolchain`, which `bin/devbin test all` includes (hv, 2026-09-17); `bin/devbin fullcycle` needs devbin#0083, filed in the Devbin estate, which hv routes. What level 3 leaves for the next line is ST0077's (see TODO).
- The all-estate board trawl (hv, 2026-09-16), started on vc's word on 2026-09-17 and moved from dc to cc so dc stays on WP-06: cc's first pass ran in scratch clones of the 15 registered estates with a whiteboard (population: `~/.config/intent/projects.json`; the frozen `_Archive/Intentv2` deliberately excluded), stores copied read-only and integrity-checked, nothing written live; the worklist is banked at refs/bank/cc/trawl/worklist.md (51 nodes: 25 already carried, 5 carried in the clones, 21 refused over 236 units in six classes) and is with hv, who rules restructure or drop per class or unit (vc's recommendation: drop every class but the 31 asks of hv under `## For hv`-like headings in Baize, Conflab, Lamplight and Prolix, which cc carries verbatim as holds; register Gtools' hv board). BEFORE ANY LIVE CARRY the pair is rebuilt (next, after ic's regeneration commit), so that it carries the two migrate fixes (a dropped inbox line is kept in a copy of its inbox; a view Intent rendered is read with its banner set aside, vc decision 29), and cc re-runs the pass on Courses hv and Conflab cc with the installed pair (refs/bank/cc/trawl/trawl-check-0438.sh). cc's carry plan is banked at refs/bank/cc/trawl/carry-plan.md, rehearsed in scratch clones with the landing's binary and nothing written live: Part A (Riffle cc, Molt hv, Prolix hv and vc, arca_cli cc, and Courses hv) needs only hv's go; Part B carries the 21 refused nodes under vc's disposition, every dropped unit kept in its node's pre-migration copies, with the asks of hv back as holds on the asking node's own board (vc's refined recommendation: the 16 units that read as asks and Conflab cc's 12-row decision table as 12 holds, the framing and information units staying in the copies). Each estate's quiet check before its carry and before its commit is intentd's registry reading it not watched, with `ingested` unchanged (vc decision 30 (5)). Then, on hv's rulings, cc carries in the live estates, doctor runs, vc checks each estate, and each estate gets one mechanical commit in its own repo. NO PUSH.
- Issue 0441, ic's (vc decision 30 (4)): intentd's ingest pass renders every subject from a snapshot and commits files with nothing checking that the store moved meanwhile, so a command-line write landing inside it has its canon and view written back to the render from before it, silently until the next write to the subject warns. ic reproduced it and read the cause; the fix guards the ingest's commit with `PRAGMA data_version` under the write lock, re-renders at most three times, and proves it with one arm. ic has it banked at refs/bank/ic/0441/ on HEAD and asks vc for the whole-suite slot; it rides the rebuild if it lands first, and the next rebuild if not.
- vc: judging every bank, train and landing; recording hv's drives.

## TODO

- `intent/st/ST0056/parity/tools/gen_reference.sh` regenerates `docs/reference/` whole as ic's own commit (ic hold 10): the register is final with 0440's landing, so ic runs it at HEAD with `--baseline v3.0.1`, the one the committed pages used unless hv's release version moves it, and commits it before the rebuild. The script runs no binary and reads the register at a revision.
- The next rebuild (`bin/devbin build all && intent daemon restart && bin/int macos app-install`, chained so the restart follows the promotion with nothing between; vc runs it, announcing the hold to every session, the two Devbin ones included, and no commit lands while it builds): it follows ic's regeneration commit and carries level 3's surfaces, the Elixir reader, level 3's counts and the two migrate fixes; then cc re-runs Courses hv and Conflab cc with the installed pair (refs/bank/cc/trawl/trawl-check-0438.sh).
- hv's remaining item: a declared guard (0426, or dc's guards adoption pass on this estate as its live drive).
- The guards adoption pass, dc's, beside the trawl (vc, 2026-09-16, on 0426): per estate, declare the project's own guards in config.json `guards`, remove the calls it duplicates from the hand-wired or tracked hook, bring the tracked `.githooks/pre-commit.intent` shim to canon (on this estate `intent claude upgrade` still reports it as to be written: two remedy sentences), and run doctor clean; coordinated with devbin for the repos whose `.githooks/` and `bin/int` it manages (Intent, Gtools, Laksa); one commit per estate. NO PUSH.
- hv: the prepared `gh release edit v3.0.2` annotation.
- hv chooses the release version at the close-out.
- ST0056's clean install (AC-00.5, AC-11.1) and 0344's never-tapped leg: after the rebuild and drive pass here, hv runs `intent/st/ST0056/gyges-brief.md` on gyges and hands back `~/intent-clean-install.log`; vc judges it, dc satisfies AC-00.5 and AC-11.1 on it, and WP-11 and ST0056 close on hv's word.
- ST0060 (`intent vault`) stays open as the next line's first thread (hv, 2026-09-15).
- ST0077 (level 3 stores the references only the toolchain sees, and a roster of the targets it defines) is B of hv's ruling of 2026-09-17 ("Ok, A now, then B post.", hv decision 22): opened by vc with the live Rust run's numbers and dc's Laksa measurements in its context, Triage, for the line after the close-out.
