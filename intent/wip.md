---
verblock: "2026-09-17:v1.94: vc - hv has ruled the last three open questions (decision 25): the SQLite bump and the new detector are BOTH IN 3.1.0 so 0442 closes on a repair, a detector and an honest unreproduced cause; known-defects is RE-DRIVEN against 3.1.0 at the cut; the docs/reference baseline is the previous release TAG. FIRST AFTER THE BOUNCE, hv verbatim: seriously and aggressively lean out wip.md and restart.md back to bare bones basics. The version is 3.1.0 and its CHANGELOG and notes are banked; ST0076 is CLOSED so only ST0056 is WIP, waiting on gyges; 0442's cause investigation is CLOSED with the trigger UNREPRODUCED after eight arms across two corpora, the body consolidated from 26 KB to 13 KB with all five superseded readings named, and it now closes on the bump and the detector hv has just ruled in; the trawl's rehearsals are green and the carry is next on hv's go; known-defects' two lying entries are fixed and the rest is scoped; the cut's regeneration must be COMMITTED BEFORE build release runs, because that driver commits, tags and pushes in one go"
intent_version: 3.0.3
---

# Work In Progress -- closing out Intent's open issues and threads

## DOING

- The close-out (hv, 2026-09-15): every open issue and thread closes on evidence or by ruling. The rulings are in `intent/history/20260915-hv-rulings.md` and on hv's board (decision 18 adds devbin's whiteboard defects to this release, decisions 19 and 20 are the rulings of 2026-09-17); what is open is `intent issues list` and `intent st list`. One lane lands on the tree and the store at a time, on vc's word; a bank is a patch blob on `refs/bank/<node>/<issue>`, and the judged patch-id is what lands: a gate refusal at landing is fixed in the worktree and re-banked before any commit. Banks that share a file land in the order they bank green and the second proves itself on top, stacking on the first one's bank so one run stands for the landing. Each train has one whole-suite judging run, in the warm worktree that already holds its stack, taken by asking vc for the slot with START and END sent to every lane; vc judges from the logs, the diff and a read-only check that the worktree equals the bank. A target dir is never shared between worktrees, not even one after the other, and `bin/int macos app-test` runs under its own `INTENT_MACOS_STATE_DIR`. Every write to the live store goes through the installed pair, never a worktree binary; the 3 s spacing that stood until 0441 was installed is retired with the rebuild of 2026-09-17. A board item's text goes in as a command substitution of a file, never inline in double quotes: a backticked phrase in item prose is a shell's command substitution, and one of them started that rebuild. **EVERY ENTRY ON A BOARD THAT CAN EXPIRE IS WRITTEN AS THE CONDITION THAT EXPIRES IT, NEVER AS THE READING OF IT** (ic found it, dc gave it this form, 2026-09-17): a condition carries its own re-check, so a reader discharges it by asking, where a state can only be inherited and believed. It is the protocol's hold rule generalising past holds to resume items, focus lines and must-not sections. The night that produced it had four of five boards go false in one window -- the window where the most work happened and the most claims died -- and a resume item describing finished work is worse than none, because it is confidently wrong in the one place a fresh context trusts without checking. **A fold is not outside the staleness window either**: ic wrote a must-not line that hv's ruling had already discharged two hours before, inside the fold that was fixing the severe form. What limited it was the FORM of the sentence, not the care. **A warning has a tense too** -- dc reported the index clean, vc's commit cleared it, then cc staged and held the lock, all inside twenty minutes -- so a state named in a message says when it was read or it is not a state.
- The all-estate board trawl (hv, 2026-09-16), started on vc's word on 2026-09-17 and moved from dc to cc so dc stays on WP-06: cc's first pass ran in scratch clones of the 15 registered estates with a whiteboard (population: `~/.config/intent/projects.json`; the frozen `_Archive/Intentv2` deliberately excluded), stores copied read-only and integrity-checked, nothing written live; the worklist is banked at refs/bank/cc/trawl/worklist.md (51 nodes: 25 already carried, 5 carried in the clones, 21 refused over 236 units in six classes) and is with hv, who rules restructure or drop per class or unit (vc's recommendation: drop every class but the 31 asks of hv under `## For hv`-like headings in Baize, Conflab, Lamplight and Prolix, which cc carries verbatim as holds; register Gtools' hv board). The pair now carries the two migrate fixes (a dropped inbox line is kept in a copy of its inbox; a view Intent rendered is read with its banner set aside, vc decision 29), so cc's next act is re-running the pass on Courses hv and Conflab cc with the installed pair (refs/bank/cc/trawl/trawl-check-0438.sh), then the ten rehearsals and a regenerated plan. cc's carry plan is banked at refs/bank/cc/trawl/carry-plan.md, rehearsed in scratch clones with nothing written live: Part A (Riffle cc, Molt hv, Prolix hv and vc, arca_cli cc, and Courses hv) is **GO** (hv decision 24, 2026-09-17); Part B carries the 21 refused nodes under vc's disposition, every dropped unit kept in its node's pre-migration copies, with the asks of hv back as holds on the asking node's own board (vc's refined recommendation: the 16 units that read as asks and Conflab cc's 12-row decision table as 12 holds, the framing and information units staying in the copies). Each estate's quiet check before its carry and before its commit is intentd's registry reading it not watched, with `ingested` unchanged (vc decision 30 (5)). hv has ruled BOTH parts (decision 24), taking vc's refined disposition on Part B: drop all six classes, every dropped unit kept in its node's pre-migration copies, the 16 units that read as asks of hv plus Conflab cc's 12-row decision table returned as 12 holds on the asking node's own board, and Gtools' hv board registered. **The experiment and the rehearsals are both DONE** -- the ten rehearsals all rc=0 in 92 seconds on the installed pair with no STOP, MISMATCH or DIFFERS, every estate quiet either side, Courses' doctor down from 18 findings to 2, and the 35 `left in place` entries all `.history/` files that are not markdown boards; the plan is regenerated at 814 lines and banked at refs/bank/cc/trawl/carry-plan.md. **So the carry is cc's next act**: cc carries in the live estates, doctor runs, vc checks each estate before its commit, and each estate gets one mechanical commit in its own repo. NO PUSH.
- Issue 0442, cc's, high (dc's find of 2026-09-17): this estate's own store carried a malformed FTS5 index for `src_sections`, so a plain `intent search` on an affected term failed while `intent doctor` and `intent index status` both read clean over it. vc repaired the live store with `intent index rebuild` at 18:15:45Z and a repair is not a fix. **The damaged bytes are durable now**: `.backup/0442-evidence/`, whole and read-only with their hashes, named in the issue's own body and verified by vc rather than taken on report. **THE LOCUS IS NARROWED BY AN ON-DISK ARTEFACT, NOT BY A CHECKER**: FTS5 writes a format version, `FTS5_CURRENT_VERSION` 4 and `..._SECUREDELETE` 5 (`sqlite3.c:232287-232288`), and the 4 to 5 bump is LAZY -- gated at `:251395` on a delete or update actually taken while secure-delete is on. Both damaged copies read `src_sections` at 5; `doc_sections`, identically configured, sits at 4 and passes its own check. So the only table that ever took a delete through the secure-delete writer is the only damaged one. **cc's identification of the phantom with a particular file is WITHDRAWN** (cc, 2026-09-17, found by trying to reproduce its own claim: it read a 55-minute snapshot gap as a delete), and with it the 122-of-141 arithmetic and every damaged-versus-repaired diff of that evening. What survives is tighter: the phantom is real and self-evident inside one file (the index names doc 2599, that database's content table has no row 2599), it carries 122 distinct terms, and dc's same-moment control -- a copy diffed against a rebuild of ITSELF -- found exactly 122 terms naming one more document each with none fewer, so the phantom contributes +1 document to each of its own terms and nothing else is wrong. **vc's detection ruling (2026-09-17)**: doctor's check cannot be built on fts5's `'integrity-check'` under the bundled SQLite, because 3.46.0's checker branches on that same version (`:248533`) and its secure-delete allowance is a single hard-coded leaf shape (`:248565-248575`) in the release whose successor's only FTS5 note is a false-positive fix there. The bump is a PREREQUISITE of detection whatever the cause turns out to be, and hv decision 23 makes it free to take. **THE CAUSE INVESTIGATION IS CLOSED AND THE TRIGGER IS UNREPRODUCED**, after eight arms across two corpora -- including the first faithful fixture anyone built, at 3.100x data rows over a rebuild of itself against the live store's 3.04x -- and the crash family is ruled out outright. The body is CONSOLIDATED (`8e05093b3`, 26 KB of accreted correction down to 13 KB) with all five superseded readings named and how each died, and narrowed after (`b9eae6d9d`). **The only thing 0442 now waits on is hv's scope call below**; no code moves, because `replace_src_sections` landing the store at version 4 is OBSERVED four ways while its mechanism is UNLOCATED -- a statement-faithful replay including the re-inserts ends at 5, so the statement sequence is eliminated and what remains is the real execution.
- Issue 0443, cc's, medium: a plain `intent search` reconciles and writes even on an estate a running intentd already watches (`render.rs:440` and `3703`), which v3.0.2's notes say it does not, and `facade.rs:3843` maps a store error out of the query to `BadQuery` with an FTS5 syntax remedy, so the same damage met one step later blames the reader's query.
- Issue 0444, cc's, medium: the `wb migrate` register note and `Facade::wb_migrate`'s doc say an over-bound migrated inbox refuses new sends until its owner clears it, and 0374's landing made that false, because `wb_live_message_count` counts only rows with `authored_at IS NULL`. It reaches `docs/reference` through the register, and the cut's regeneration carries the correction.
- vc: judging every bank, train and landing; recording hv's drives; reading the banked 3.1.0 notes for voice and for anything that reads as internal, NOT to re-run the nine readers' user-visibility filter, which was done against the as-built and excluded 36 as not user-visible, 19 as closed with no behaviour change and 7 as already shipped; the menubar app's install, which copied the app and could not launch it at the 2026-09-17 rebuild (LaunchServices error -600).

## TODO

- **FIRST THING AFTER THE BOUNCE (hv, 2026-09-17, verbatim): "seriously and aggressively leaning out wip.md and restart.md back to bare bones basics."** Both documents have accreted through a long close-out and this file is the worse of the two. It is vc's, it comes before any other work, and the test is the entry point's own: a line that states a state rather than the command that regenerates it comes out, and a line that carries a condition stays.
- **BOTH IN (hv decision 25): the `libsqlite3-sys` bump and the new detector ship in 3.1.0**, so 0442 closes on a repair, a detector and an honest unreproduced cause. The detector is a document present in the index with no row in the content table, plus fts5's own per-table check, against a rebuild of the same database; it cannot be built on 3.46.0's checker, whose secure-delete allowance is one hard-coded leaf shape. cc builds both, names the candidate version and what moves between 3.46.0 and it, and vc judges that before it lands. One rebuild and one whole-suite run are owed with it, and no commit lands while that rebuild builds.
- **`docs/known-defects.md`**: the two entries that were lying to readers are fixed (`133d1363d`, cc -- L31 and L113 gained their `Fixed in 3.0.2` lines under the page's own rule that an unfixed defect carries none, and the preamble lost its false `intent#0177` exception). Left, all banked at `refs/bank/cc/release-notes/known-defects-lines.md`: eleven entries whose cited issue closed without the defect going away, each needing a line saying why; four needing a line naming 3.1.0, of which L76/L85 must say WHICH HALF moved because a partial fix announced as a fix is the same defect one level down; nine entries quoting output that no longer renders, which want re-driving against the shipped build rather than editing from a diff; and 0442's own entry. **RE-DRIVEN AT THE CUT (hv decision 25, "Yes")**: every entry driven against a fresh 3.1.0 build the way the page was originally made, not a framing sentence over v3.0.1's driven record. That retitles it and settles the nine stale transcripts on its own, because a re-drive REPLACES a transcript rather than editing one. 0343 deliberately needs no entry and cc has recorded that so nobody adds one.
- The LIVE guards adoption pass, dc's, now split from 0426 (hv decision 24): per estate, declare the project's own guards in config.json `guards`, remove the calls it duplicates from the hand-wired or tracked hook, bring the tracked `.githooks/pre-commit.intent` shim to canon, and run doctor clean; one commit per estate, NO PUSH. **It stays blocked by dc's finding, banked at refs/bank/dc/guards/finding-int-hooks.md**: devbin's `int hooks` decides a hook is wired by grepping the hook file for the runner, so the pass makes its report false at exit 1 and `int hooks --install` restores the double-run. Nobody works around it. That finding and devbin#0083 are hv's to route to the Devbin estate and gate nothing here.
- The next release is **3.1.0** (hv decision 24): a minor, because ST0076 adds a verb and an index level that did not exist rather than repairing something shipped broken, and the fixes ride along. 388 commits stand between v3.0.3 and HEAD and the workspace still says 3.0.3 with no `## [3.1.0] - in progress` section, which `bin/devbin build release` refuses a cut without. cc writes that section and `docs/releases/3.1.0/RELEASE_NOTES.md` from the nine banked readers at that number rather than a placeholder. Also at the cut: hv's prepared `gh release edit v3.0.2` annotation, and a line on `docs/known-defects.md` for each closed issue whose defect is recorded there.
- At the cut, both halves of `docs/reference` are regenerated in one commit -- `gen_reference.sh` writes 20 pages and `gen_cut_surface.sh` the 21st, and each reads the register at the revision, so the register may move until then. **THIS LINE SAID _at the TAG_ UNTIL 2026-09-17 AND THAT IS NOT WHAT v3.0.3 DID**: the tag is `46599d145` and the regeneration is `a87a94a4a`, twenty-four minutes later, and `--is-ancestor` says the tag does not contain it, so the shipped tag carries the PREVIOUS cut's reference pages while the pages in `main` name the tag. The pages name a SHA and not a version (`Revision this describes: <sha> (HEAD)`), so the only self-consistent arrangement is the TAG POINTING AT the regeneration commit, which the cut sequence has to be driven to produce. ic owns that step's durable home, now ruled to be a new `intent/docs/releasing.md` on the internal shelf with a pointer from CLAUDE.md's "Internal authoring docs" index, and banks a staleness-check proposal beside it without touching `bin/.devbin/cmd/build.d/release` (vc decision 31 (2), corrected 2026-09-17). **AND ic MEASURED WHY THERE IS NO WINDOW TO INSERT IT INTO**: `build release` commits the version bump (`:1171`), tags the commit it just made (`:1221`) and PUSHES main and the tag to both remotes (`:1240`, `:1242`) in one run, so the regeneration cannot be sequenced after the tag by any means and has to be committed BEFORE the driver runs. The driver has no mode that stops short of publishing, which is also why it cannot be invoked from a tool session at all while NO PUSH stands -- hv's standing decision 3 read from the other end. The pointer goes in `CLAUDE.md` itself, inside its `user:start`/`user:end` region, and NOT in `lib/templates/llm/_CLAUDE.md`, which has no such section and is in DIRT_SCOPE, so editing it would stale the pair for every node to land a documentation pointer. Everything is banked under `refs/bank/ic/releasing/`, nothing landed.
- **FOR hv: which release is `docs/reference` presence measured against?** Today's pages say `v3.0.1` with v3.0.2 and v3.0.3 both shipped, so they are at least one release behind under every reading. What is NOT established is the rule: "the previous release TAG" and "the previous release a user can INSTALL" pick different baselines here, and the generator's own default is `v3.0.0` and has never moved. ic's two regenerations passed `v3.0.1` by hand, copied from the invocation before rather than derived from a rule, which is the failure the document exists to stop. `releasing.md` carries it as an open section with the sentence that gets written in once ruled, and the staleness check is blocked on the same ruling, because a check with an ambiguous second input encodes the confusion instead of catching it. **RULED: THE PREVIOUS RELEASE TAG (hv decision 25, "Tag")**, `git describe --abbrev=0`, because it cannot drift with anyone's judgement where the present arrangement has no rule at all -- and ruled with ic's concrete case in view rather than in the abstract: under this rule the v3.0.3 cut measured presence against **v3.0.2, which was tagged and whose artefacts were never published**. It does not bite the 3.1.0 cut, whose previous tag is v3.0.3 and did ship. ic writes the sentence into `releasing.md`'s open section, and the staleness-check proposal is unblocked by the same ruling.
- ST0056's clean install (AC-00.5, AC-11.1) and 0344's never-tapped leg: after the drive pass here, hv runs `intent/st/ST0056/gyges-brief.md` on gyges and hands back `~/intent-clean-install.log`; vc judges it, dc satisfies AC-00.5 and AC-11.1 on it, and WP-11 and ST0056 close on hv's word.
- ST0060 (`intent vault`) stays open as the next line's first thread (hv, 2026-09-15).
- ST0077 (level 3 stores the references only the toolchain sees, and a roster of the targets it defines) is B of hv's ruling of 2026-09-17 ("Ok, A now, then B post.", hv decision 22): opened by vc with the live Rust run's numbers and dc's Laksa measurements in its context, Triage, for the line after the close-out.
