---
verblock: "2026-09-24:v3.9: vc - DOING is the 3.2.1 doc audit on hv's six rulings; the push, the cut's hold, the cut, the fleet sweep and the stabilisation line follow it"
intent_version: 3.2.0
---

# Work In Progress

Measure before believing anything below: `.claude/restart.md` carries the commands, `intent/restart.md` the rules every bank, train and landing runs under. No count appears in this file.

## DOING

### The 3.2.1 doc audit (hv, 2026-09-24, in vc's session, verbatim; vc directs; size XL)

> _The amin for today is to get the release cut, shipped, and active fleet wide, and then draw a line under new development for a while as the release stabilises. Note: we need to a thorough doc audit as the docs are all woefully out of date now and need a comprehensvive update. I want that doc update part of this release too. I particularly want a new doc section on the explorer TUI complete with screenshots, and I want a section (or whole doc) on 'using Intent in a team' (ie multi-user usage with git/github)._

hv took vc's six recommendations whole ("Ok, go with recs"), recorded as a decision on hv's board. **Nothing is cut before the docs land.** The sequence: the lanes, then vc checks each report and lands, then one `bin/devbin build all` if a compiled-in path moved, then hv pushes, then vc reads CI from the job logs, then the cut's hold, the cut, the fleet sweep and the stabilisation line.

The protocol is the 2026-09-11 audit's (`intent/history/20260911-doc-audit.md`), with today's additions:

1. **As-built is the truth, and the doc moves to the code.** The audit writes no code. Where the doc is right and the code is wrong, describe the as-built behaviour and send vc the defect with `file:line`; vc files it. Only a shipping-grade defect (a broken install, data loss, a remedy that misdirects) comes into 3.2.1, named to hv first. Issue 0548 is the first, and it rides 3.2.1.
2. **Measure every checkable claim**: verb spellings, flags, defaults, paths, output lines, exit codes, config keys and file names. Drive `intent` read-only, or read the code at HEAD and cite `file:line`. Anything that writes runs in a scratch project under a SHORT isolated HOME (`/tmp/<short>`, because the daemon's socket path caps at 104 bytes), never in this tree. Recall is not evidence.
3. **One disposition per finding**: STALE (rewrite it), DEAD (delete it outright with no stub, and name every deletion to vc BEFORE it lands), UNDOCUMENTED (add it where the reader would look), CODE-WRONG (surface it and fix nothing).
4. **Generated files are fixed at their source and regenerated by their verb.** Never hand-edit `AGENTS.md`, `CLAUDE.md`, `usage-rules.md`, `docs/reference/**` or any thread or board view.
5. **hv: hardcoded vanity metrics are STRICTLY verboten.** No doc carries a count of tests, rules, skills, subagents, files, rows or lines, or any figure the tool reports about itself; name the verb that reports it. A count found is DEAD in the same pass.
6. **Records are exempt**: shipped CHANGELOG entries, `docs/releases/**` except 3.2.1's, `docs/v2/**`, `intent/history/**`, every `.history/**`, completed and cancelled thread docs, `examples/**`, `tests/fixtures/**`, `.archive/**`, `intent/eng/tpd/**`, `intent/analysis/**` and `intent/autopsy/**`. The only check on them is a one-line historical banner where a reader could take one for current.
7. **A transcript elides Intent's version and a build's commit as `...`** (cc's rule, under vc's pen): the version bump is the release commit itself, so anything driven today prints 3.2.0.
8. **Find a doc's line citers before editing it** (`git grep -n '<doc>:[0-9]'`, code comments and the register included), and keep the edit line-neutral or move the citers in the same commit.
9. **`lib/templates/**` is edited in a detached worktree and banked** (decision 30), then landed on vc's word. `lib/templates/llm`, `lib/templates/prj`, `surface/` and `docs/design/` are compiled in, so vc batches their landings and one `build all` follows before the push. Every register edit goes into ONE landing late in the audit.
10. **A page that documents commands is verified by running it**: its own `$` lines, in order, in a scratch project. vc re-runs a sample of every lane's pages.
11. **House style**: no manual wrap, `eg`, no em dashes in skill files, 2-space indentation, prettier on every markdown file before committing, and `critic-prose review <page>` on every new or rewritten page.
12. **Commit your own lane's paths by literal path** in one call (`git add <paths> && git commit --only <paths>`), with chain START and END on the live channel. A fix in another lane's file goes to its owner.
13. **Account for coverage**: send vc your lane's file inventory before you edit, and at the end report every file with its disposition (clean, edited, deleted or exempt). A file missing from the report is a silent skip.
14. **NO PUSH, NO RELEASE.**

**The lanes:**

- **ic (interface):**
  - NEW `docs/explorer.md`: the explorer (`intent explore`) taught by task, with screenshots under `docs/images/explorer/` and alt text on every one. They come from a seeded demo project under a short HOME, rendered through a pty and pyte to SVG and then PNG, and the generator is committed beside `gen_reference.sh` with its venv step written down. There is no key table: `/help` is the authority and gets a shot. The page says that every view but the search pane follows the store within half a second.
  - `docs/reference/**`, regenerated by its verb after the register landing. The register's user-facing text (eg `explore`'s address row, which renders internal ruling prose) is findings for that landing.
  - `surface/*.md`. `surface/forms.json` and `surface/agent-guide.spec.md` are compiled in, so their edits bank into the one register landing, which ic banks on vc's word.
  - `intent/plugins/claude/skills/**` and `intent/plugins/claude/subagents/**`.
- **cc (control):**
  - Issue 0548 in full, first, because it is Rust: the code half (`complete` stops answering complete for paths in a declared language with no grammar, and the CLI's human form says so too), banked and judged like any Rust change; and its doc half in the generated CLAUDE.md and the MCP server's instructions.
  - The team guide: `docs/concepts/working-in-a-team.md` extended into an end-to-end GitHub walkthrough and re-driven from ST0078's drive script with two clones of a scratch origin. It adds intentd across a pull, issue and work-package id collisions (decision 29's `ISSUE:` claims included), `pull --rebase` and squash merges against the post-rewrite hook, CI as a REQUIRED check, and the whiteboard across clones.
  - `lib/templates/**` in a detached worktree, then the root files regenerated; `intent/plugins/claude/rules/**`, `intent/plugins/agents/plugin.json`, `.claude/agents/elixir.md`, `.intent_critic.yml` and `intent/llm/*`; `intent/docs/{critics,rules,writing-extensions,creating-custom-agents}.md`.
- **dc (dev-x and release):**
  - `README.md`, `docs/{install,known-defects,migrating-from-v2,banking}.md` and `docs/design/design-system.md`.
  - `intent/docs/{releasing,pre-commit-hook,rust-on-macos,migration-v2.10.0,skills-triage,total-codebase-audit}.md`, `intent/docs/notes/**` and `intent/docs/exemplars/**`.
  - The `bin/.devbin` docs and the `int` and `devbin` help (the vendored devbin is audited, never edited here: its findings go to the Devbin estate through vc), `.github/workflows`, `tests/**/*.md` outside fixtures, `DEPRECATIONS.md`, `LICENSE.md`, the comments of `native/macos/Intent/project.yml`, the Homebrew tap's README (committed in the tap, no push), and the records sweep in rule 6.
  - `docs/known-defects.md` re-driven once, on the pair from the final pre-cut `build all`.
- **vc (director):**
  - `docs/index.md` (both new pages into Start here), `docs/{getting-started,working-with-agents}.md`, `docs/concepts/{index,the-store,steel-threads,criteria-and-tests}.md`, `intent/docs/working-with-llms.md`, `intent/whiteboard/README.md`, `intent/whiteboard/vc/*.md` and `intent/done.md`.
  - The 3.2.1 CHANGELOG entry and `docs/releases/3.2.1/RELEASE_NOTES.md` for the docs.
  - Checking every lane's report, and the landing order.

## TODO

- **AWAITING hv: a `/refresh` in the explorer.** vc recommends it after the stabilisation line, because every view but the search pane already follows the store within half a second. If hv puts it in 3.2.1 it is ic's, size S, with its own bank, whole-suite run and rebuild, and the guide's `/help` shot waits for it.
- **THE PUSH, once the audit has landed**: hv's hand. Any landing under a compiled-in path owes `bin/devbin build all` first (decision 30). vc then reads both workflows on both legs from the job logs.
- **THE CUT'S HOLD, before hv fires anything.** Every node commits its own board render; then no `intent wb` write of any kind, no commit and no `/in-session` until the `v3.2.1` tag exists (see `intent/restart.md`). hv's other sessions are held too: every estate's live session gets the pre-build warning, because the build replaces the pair every estate runs (Gtools backs up its store and answers "Gtools is clear" first); nobody runs a heavy suite during the gate; and no Devbin commit lands in this tree.
- **THE CUT, in hv's terminal, never `--no-confirm`:** `intent daemon stop`; `bin/devbin build release v3.2.1 --dry-run`, then the real run. The release step builds the pair at the tag and checks it with the pre-push verdict before it asks to push (0546). Read the gate lines off the terminal before clearing it. Then `bin/int macos prepare` and `formula`; if the audit left a tap README commit in `tmp/tap-intent`, hv pushes it before `publish`, whose fresh clone of the tap then carries it under the formula commit; then `publish` and `smoke --reinstall`, and finish with `brew unlink intent && brew pin intent && intent daemon restart`. vc reads the tag's CI from the job logs.
- **AFTER THE CUT:** lift the hold. First ic's read-only census of the fleet's hook wiring; then vc todo 62, the fleet sweep, in every estate that already has Intent (hv decision 31): `intent claude upgrade --apply`, `intent todo update`, doctor, one commit per estate, no push; `sync --to-disk` only where doctor reads no counted ViewSkew, and each store backed up first where no session did. An estate with a live vc does its own. dc's guards adoption pass (dc hold 29) rides the same sweep, with dc's seven unnamed estates ruled by vc on a measurement of which carry Intent. Then `intent claude skills sync` here, ic's drive of the released keg's `bootstrap --check`, and the 3.2.1 app into `/Applications` by hv's hand.
- **THE LINE, once the sweep is done:** vc records hv's stabilisation directive on hv's board: "3.2.1 is the stabilisation line: no new development until hv lifts it; only defect fixes, each approved by hv."
- **AFTER THE LINE, vc's hand, no push:** hv decision 28's ten Conflab and Baize rulings, which repair records rather than develop anything.
- **WITH hv, not re-measured since 2026-09-22**: the pushes of Prodinfra, Riffle, Baize, Conflab, Laksa, Prolix, Utilz and the two Arca repos from their own checkouts, measured at the moment of pushing.
- **MACHINE, hv's call**: the host was rebooted at 2026-09-22 21:27Z, and by 2026-09-24 08:33Z the resident `SetStoreUpdateService` processes had regrown (`ps -axo comm= | grep -c '[S]etStoreUpdateService'` measures it), which reads as the leak the reboot was meant to test for.
- **ST0060** and **ST0077** stay in Triage as the next line's queue (hv, 2026-09-23).
