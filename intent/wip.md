---
verblock: "2026-09-20:v3.0: vc - leaned to bare bones on hv's instruction of 2026-09-17 (decision 25): DOING and TODO only, each item naming its owner and the check that discharges it; the version history before this one is in git."
intent_version: 3.1.0
---

# Work In Progress

Measure before believing anything below: `.claude/restart.md` carries the commands, `intent/restart.md` the rules every bank, train and landing runs under. No count appears in this file.

## DOING

- **THE ISSUES LINE** (hv, 2026-09-19; decisions 55 and 56 on vc's board): every open issue (`intent issues list`) is fixed, not triaged, one landing at a time on vc's word. Left: cc's 0488+0489 (the migrate fidelity fixes the whiteboard carries need), then 0487 (a)+(b) and 0486; 0460 waits on hv's ruling; 0481 stays open by design until a real `dvb build release` gate passes its six arms first time; 0493 and the event-stamp defect are next-line work, not built now (hv, 2026-09-20: no testing tests, no yak-shaving, the fleet first).
- **THE FLEET** (hv, 2026-09-19 and 2026-09-20): every registered estate is at 3.1.0 except Laksa, which runs wave one behind its deploy freeze and reports to vc. The seven hand-authored whiteboards migrate from here once 0488+0489 land and the pair is rebuilt: ic carries Baize, Molt, Riffle (first, the rehearsal) and arca_config, dc carries Conflab, Prolix and arca_cli; per estate `intent wb register` with no arguments, `intent wb migrate <node>` per node (hv first where hv exists, else alphabetical), the report read and judged before any `--drop-uncarried`, doctor, organize, one commit by literal paths in that repo, no README invented, no push. devbin's 0.1.2 sweep is committed at Intent (42915c049) and counts as swept when hv's push is green.
- **vc**: judging every bank and landing; the lean-out of `intent/restart.md` (hv, 2026-09-17) after the carries.

## TODO

- **WITH hv**: the push of main (everything since hv's 2c3ce6b3b is judged and unpushed, `git log upstream/main..main`; that run's CI is read); `intent daemon restart` after the rebuild that follows 0488+0489; 0460's ruling (`st relate` / `st unrelate`, recommended as cc proposed); dc's tap README commit in homebrew-intent; the three stash entries; ST0056's close on the gyges run of `intent/st/ST0056/gyges-brief.md` against the published 3.1.0 (vc judges the log, dc satisfies AC-00.5 and AC-11.1, WP-11 and the thread close on hv's word).
- **ST0060** and **ST0077** stay in Triage as the next line's queue (hv, 2026-09-18).
- **The LIVE guards adoption pass**, dc's, HELD until `bin/int hooks` reports a canon-shim-wired hook as wired (the defect is devbin's, hv Q7); never run `int hooks --install`.
