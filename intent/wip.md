---
verblock: "2026-09-19:v2.8: vc - the cut line records two dry runs red under load and the release-driver guard that lands first; the doc sweep joins DOING. v2.7: vc - localfold before hv's compact mid-cut: step 2 landed, the dry run's re-run in flight, the sweep deferred to Devbin's own release, the lanes holding. v2.6: vc - every estate's trawl commit is pushed on hv's word; the after-trawl and hv lines lose the push. v2.5: vc - the fleet trawl is done and its line becomes the unpushed commits and the sweep; the cut sequence and hv lines follow. v2.4: vc - Lamplight landed; the fleet trawl line names Devbin as the one estate left and the kit's resume; small issues name the two cc filed. v2.3: vc - the fleet trawl line records the kit's leftover handling and what remains (Lamplight in flight, Devbin); small issues gain 0458 and the related-verb issue. v2.2: ST0078 closed and its line removed; the trawl and hv lines lose their discharged conditions; the cut sequence gains the 3.1.0 notes' ST0078 gap. v2.1: rewritten from the unified plan hv approved on the bounce (vc decision 38) after the morning's train landed. DOING and TODO only, each item naming its owner and the condition that discharges it. No state that a command regenerates, and no content that already has a home: the close-out rulings are in intent/history/20260915-hv-rulings.md, the bank and landing rules in intent/restart.md, the cut sequence in intent/docs/releasing.md, every issue in `intent issues show <id>`, and ST0078's proposal in its design.md."
intent_version: 3.0.3
---

# Work In Progress -- closing out Intent's open issues and threads, then the fleet

Measure before believing anything below: `.claude/restart.md` carries the commands. **No count appears in this file** -- a count in a document a reader reads is a state with no command beside it, and every count this file used to carry had rotted.

## DOING

- **THE CLOSE-OUT** (hv, 2026-09-15): every open issue and thread closes on evidence or by ruling. Rulings: `intent/history/20260915-hv-rulings.md` and hv's board. What is open: `intent issues list`, `intent st list`. One lane lands at a time on vc's word; the bank, train and landing rules live in `intent/restart.md`. The SQLite move (rusqlite 0.40.2 / SQLite 3.53.2, with `fallible_uint` on), the 0442 detector and every fix landed on 2026-09-18 are on main and the pair carries them; `intent issues list` is the open set.
- **The all-estate board trawl** (hv, 2026-09-16), cc's, Part A GO and Part B under vc's disposition (hv decision 24). Plan at `refs/bank/cc/trawl/carry-plan.md`. It is the whiteboard subset of the fleet trawl and runs with it.
- **The as-written-vs-as-built sweep** (hv, 2026-09-19), each node its own lane per the split in `intent/history/20260911-doc-audit.md`: the lanes read only under the cut hold and land after "CUT DONE"; vc lands vc's lane by literal paths as it goes; a finding in another lane goes to that lane by note, a CODE-WRONG finding to vc with `file:line`.
- **vc**: judging every bank, train and landing; the menubar app's LaunchServices `-600` at install; ic's verification pass over this file.

## TODO

- **THE 3.1.0 CUT**, at step 3 of `intent/docs/releasing.md`, hv's hand: step 2 is landed (the reference pages and `cut-surface.md` regenerated against v3.0.3, the preflight reading clean); two dry runs went red on intentd's watcher family, both on a host far above its floor, so neither is the finding; the next dry run waits for `uptime` at the floor and is authorised only with that reading in front of vc; green means `dvb build release v3.1.0` then step 4 at the tag; red at the floor means a finding on the watcher, looked at before the tag. Before that dry run, dc's guard on the release driver lands on vc's word: the bump commit, the tag, both pushes and the GitHub release run unchecked under `set -u` in `bin/.devbin/cmd/build.d/release`, so a failed push logs success and the GitHub release creates the tag at the wrong commit. The lanes hold every intent verb, tree write and build until vc's "CUT DONE" after step 4. Then gyges.
- **Small issues**, all the next line's (`intent issues list` is the open set): 0453 (no verb repairs an unreadable search-index table in place); 0458 (the append-only guard still guards and describes `intent/events.jsonl`); no verb writes a thread's `related` links while `set`'s refusal names an address that does not exist; and an authored `info.md` below a thread's view depth is an attachment to `classify` and unaddressable to the parser, so nothing can carry it and the v2 prune withholds it until a rename (found live on Lamplight's ST0037).
- **The LIVE guards adoption pass**, dc's, HELD. Released when `bin/int hooks` reports a canon-shim-wired hook AS wired; the defect is devbin's (hv Q7). **Never run `int hooks --install`.** Gates nothing in this release.
- **AFTER THE FLEET TRAWL** (hv, 2026-09-18): every project in `~/.config/intent/projects.json` carries one mechanical trawl commit in its own repo (vc decisions 52 and 53 name them), pushed to every remote it has on hv's word (vc todo 47 has the census). devbin#0083's sweep across the fleet (hv Q8) follows Devbin's own release on hv's ruling; when devbin-vc sends START, vc relays it to the lanes and checks each swept estate as it did the trawl (`git log -1`, a clean tree, doctor). The kit stays banked at `refs/bank/dc/trawl/` with its split in `split.md` for the next estate that joins.
- **WITH hv**: the cut (step 3 and step 4); devbin#0083's sweep is deferred by hv's ruling in devbin-vc's session until Devbin's own ST0007 and release are done, then its pre-notice, then one sweep, and it does not gate 3.1.0. intentd runs the pair that ships (`intent daemon status` says which image), and another restart is owed only if the pair rebuilds again. Every ruling of 2026-09-18 is executed and lives on vc's board.
- **ST0056** closes when hv runs `intent/st/ST0056/gyges-brief.md` on gyges against the PUBLISHED 3.1.0 and hands back `~/intent-clean-install.log`: vc judges it, dc satisfies AC-00.5 and AC-11.1 on it, WP-11 and the thread close on hv's word.
- **ST0060** and **ST0077** stay in Triage as the next line's queue (hv Q4, 2026-09-18); neither is unfinished work.
