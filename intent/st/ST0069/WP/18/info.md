---
wp_id: WP-18
title: The corpus: the gitignore-aware repository, two staleness policies, the widened watcher
scope: L
status: Done
---

# WP-18: The corpus: the gitignore-aware repository, two staleness policies, the widened watcher

## Objective

Widen the index scope from Intent's own prose to the gitignore-aware repository, with every exclusion a named row, two staleness policies for the two corpora, and one scope object read by both the scanner and the widened watcher.

Design: design.md, the corpus (S1). Corpus assignment is by kind: the store's entities stay the canon corpus; a markdown or text file on disk joins the prose table through the heading splitter with owner_type file; a code file joins the source table; rendered views and the canon extract are not in the disk corpus. Binaries, files over the size cap, unreadable files and symlinks get a file_index row with skipped_reason and are listed by `intent index status`. Canon hashes always; source is stat-then-hash. The watcher's root registration becomes recursive on FSEvents and in-scope-directories-only on inotify, measured before the package closes. Largest single change in the thread; the one that makes intentd load-bearing.

## Acceptance

Acceptance Criteria for this work package are RENDERED into `ST0069/acceptance.md`, under the `WP-18` heading. THAT FILE IS A GENERATED VIEW -- a row authored there is discarded by the next sync. The contract is canon in the thread's model: change a state with the `intent ac` / `intent at` verbs, and mint or reword a row in `.canon/st/ST0069.json`, then `intent sync --to-store`. This cover never restates them.

---

_Generated by Intent v3.0.1 from `the thread canon`. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
