---
wp_id: WP-01
title: TUI Omnibox /outs[tanding]: the same table intent outs prints, inside the TUI
scope: S
status: Not Started
---

# WP-01: TUI Omnibox /outs[tanding]: the same table intent outs prints, inside the TUI

## Objective

The Intent TUI's Omnibox gains the slash command `/outstanding`, with the alias `/outs`, which shows inside the TUI the same table `intent outs` prints: the same rows, read from the same intentsvcs definition the CLI verb reads (ST0079 AC-00.2), never a second classifier. hv, verbatim, 2026-09-21: "What we also need is a slash-outs[tanding] command added to the Intent TUI Omnibox by intent-ic, but we can do that tomorrow. Just drop that in as a todo in the Steel Thread now and then IC can pick it up tomorrow. It'd just be /outs[tanding] and it'd show in the TUI the same table data as what the 'intent outs' shows, but just inside the TUI." ic's, picked up on 2026-09-22 after cc's CLI verb lands; ic writes this package's acceptance criteria before any code, and vc judges them.

## Acceptance

Acceptance Criteria for this work package are RENDERED into `ST0079/acceptance.md`, under the `WP-01` heading. THAT FILE IS A GENERATED VIEW -- a row authored there is discarded by the next sync. The contract is canon in the thread's model, and the verbs write it: `intent ac new` and `intent at new` mint a row, `intent ac edit` and `intent at edit` reword or re-cite one, and `intent ac satisfy|unsatisfy|descope|rescope|withdraw|reinstate` and `intent at green|red|na` move its state. This cover never restates them.

---

_Generated by Intent v3.2.0 from the thread canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
