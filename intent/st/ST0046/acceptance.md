---
st_id: ST0046
title: Add modules (properly) to the intent cli
---

# ST0046: Add modules (properly) to the intent cli -- Acceptance

> **THIS FILE IS A GENERATED VIEW, AND A ROW AUTHORED HERE IS DISCARDED BY THE NEXT SYNC.** The acceptance contract is canon in the thread model; this file renders it. Acceptance Criteria (AC) are the ratified completeness boundary; Acceptance Tests (AT) are the small red-to-green tests that prove them.
>
> Done = every AC is covered by a GREEN AT, or (for a non-test AC) its named evidence is satisfied, AND the AC set is the ratified full boundary. Done is read from this map, never from a hand-ticked box.
>
> Test-backed satisfaction is COMPUTED from covering green ATs and never stored -- storing it would be double truth. An AC has four states, not two: beyond satisfied and unsatisfied, a requirement can be **descoped** to a named thread or **withdrawn** with its reason on the record. Both are non-blocking and both are reported separately, so a thread that descoped half its contract looks like one.

## Acceptance Criteria

### Group 01

- AC-01.1 `intent modules check` flags a top-level module that exists on disk but has no `MODULES.md` row -- exits 1 and names the module + path. -- satisfied: no (computed)
- AC-01.2 `check` is umbrella-aware: it enumerates modules under every `apps/*/lib` source root, not just `./lib`. -- satisfied: no (computed)
- AC-01.3 `check` respects the configured exclusion set (test / support / generated dirs, `deps`, `_build` are not flagged). -- satisfied: no (computed)
- AC-01.4 `check` still reports stale rows (a registry row whose file is gone) -- no regression on existing behaviour. -- satisfied: no (computed)
- AC-01.5 (non-test) Run on the Lamplight umbrella, `check` now flags the known backlog (eg `Lamplight.Ingestor.Parser.Action`) instead of reporting `ok: registry matches filesystem` -- satisfied: no

### Group 02

- AC-02.1 `intent modules sync` (or `check --write`) adds a row for every unregistered top-level module; afterwards `intent modules check` exits 0. -- satisfied: no (computed)
- AC-02.2 `sync` is idempotent: a second run produces a zero diff. -- satisfied: no (computed)
- AC-02.3 `sync` preserves existing rows verbatim -- hand-annotated provenance (ST/WP refs, `Highlander`) and curated thematic sub-sections survive. -- satisfied: no (computed)
- AC-02.4 Each new row carries the correct `module -> path` mapping and a default `auto-discovered` provenance. -- satisfied: no (computed)
- AC-02.5 Output ordering is deterministic (stable sort), so diffs are minimal and reviewable. -- satisfied: no (computed)

## Acceptance Tests

### Group 01

- AT-01.1 `tests/unit/modules_commands.bats` -- covers AC-01.1 -- status: to-write -- red-first; modules check -- unregistered fixture flagged
- AT-01.2 `tests/unit/modules_commands.bats` -- covers AC-01.2 -- status: to-write -- red-first; modules check -- umbrella apps/*/lib enumerated
- AT-01.3 `tests/unit/modules_commands.bats` -- covers AC-01.3 -- status: to-write -- red-first; modules check -- exclusion set honoured
- AT-01.4 `tests/unit/modules_commands.bats` -- covers AC-01.4 -- status: to-write -- red-first; modules check -- stale row still flagged

### Group 02

- AT-02.1 `tests/unit/modules_commands.bats` -- covers AC-02.1 -- status: to-write -- red-first; modules sync -- registers unregistered, check then green
- AT-02.2 `tests/unit/modules_commands.bats` -- covers AC-02.2 -- status: to-write -- red-first; modules sync -- idempotent zero-diff on re-run
- AT-02.3 `tests/unit/modules_commands.bats` -- covers AC-02.3 -- status: to-write -- red-first; modules sync -- preserves provenance + sub-sections
- AT-02.4 `tests/unit/modules_commands.bats` -- covers AC-02.4 -- status: to-write -- red-first; modules sync -- new row module->path + provenance
- AT-02.5 `tests/unit/modules_commands.bats` -- covers AC-02.5 -- status: to-write -- red-first; modules sync -- deterministic ordering

---

_Generated by Intent v3.0.0-dev from `thread.json`. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
