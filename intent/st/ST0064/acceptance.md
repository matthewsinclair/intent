---
st_id: ST0064
title: The Intent menubar app: controlling intentd from the macOS menubar
---

# ST0064: The Intent menubar app: controlling intentd from the macOS menubar -- Acceptance

> **THIS FILE IS A GENERATED VIEW, AND A ROW AUTHORED HERE IS DISCARDED BY THE NEXT SYNC.** The acceptance contract is canon in the thread model; this file renders it. Acceptance Criteria (AC) are the ratified completeness boundary; Acceptance Tests (AT) are the small red-to-green tests that prove them.
>
> Done = every AC is covered by a GREEN AT, or (for a non-test AC) its named evidence is satisfied, AND the AC set is the ratified full boundary. Done is read from this map, never from a hand-ticked box.
>
> Test-backed satisfaction is COMPUTED from covering green ATs and never stored -- storing it would be double truth. An AC has four states, not two: beyond satisfied and unsatisfied, a requirement can be **descoped** to a named thread or **withdrawn** with its reason on the record. Both are non-blocking and both are reported separately, so a thread that descoped half its contract looks like one.

## Acceptance Criteria

### WP-01 -- Menubar app: control of intentd after Geodica's design -- no product logic, every control an intent verb, the daemon is the authority on the daemon (status: WIP)

- AC-01.1 (non-test) **THE APP HOLDS NO PRODUCT LOGIC: EVERY CONTROL IS A VERB IT SHELLS OUT TO, AND NOTHING IN SWIFT KNOWS A LOG FORMAT, A PIDFILE LAYOUT OR A CONFIG SCHEMA.** hv set this as the whole shape of the app on 2026-08-26 and it is the row every other row on this thread leans on: start, stop, restart and doctor are `intent` / `intentd` invocations, and every fact displayed comes from the daemon or the CLI rather than from a Swift-side derivation. **The check is by ABSENCE and it is mechanical**: the Swift sources contain no parser for a log line, no path assembled toward a pidfile, and no key read out of a config file. **The daemon is the authority on the daemon** -- an app that computes any of the three has taken a second home for a value `intentd` already owns, and the two will disagree at exactly the moment the app exists to be trusted. -- evidence: Mechanical absence check + read (ic, HEAD b083fb4c): no log-line parser, no pidfile-path assembly, no config-key read across the 7 Swift sources; every control routes to an intent verb (menu actions -> IntentCLI/DaemonService: daemon start/stop, doctor, edit --path); facts sourced from 'daemon status --format json' (Health.decode) and the CLI. Spawn sweep: only subprocess spawns are LoginShell's PATH-capture zsh and IntentCLI's two run paths. -- satisfied: yes
- AC-01.2 (non-test) **THE APP'S HEALTH PREDICATE AND THE CLI'S ROUTING PREDICATE ARE ONE PREDICATE, NOT TWO THAT AGREE.** `AC-08.3` rules how a client decides the daemon is live: a completed request-response round-trip under a bounded deadline, never a presence test and never a bare `connect()`. **The app asks the same question and MUST reach the same answer through the same code path** -- the CLI verb, not a Swift reimplementation of the probe. **Two homes for `is intentd running` is the defect this estate has now recorded at three scales**, and this is its worst instance, because the disagreement is SILENT AND INVERTED: the menubar shows a green dot while the CLI routes every verb in-process, and the operator is looking at the one surface built to tell them otherwise. -- satisfied: no
- AC-01.3 (non-test) **THE APP TALKS GRAPHQL/JSON TO `intentd` AND NOTHING ELSE, PER `D56`, AND THE `GET /_status` IN THIS WP'S BODY IS SUPERSEDED BY THIS ROW.** hv, 2026-08-29: *the macOS app (Swift) and the cli (rust) need to talk GraphQL to intentd (rust)*; `D56` gives the daemon ONE output contract, GraphQL/JSON over the socket and over HTTP, and the deciding argument for that reversal was this very app -- a SwiftUI menubar cannot consume HTML. **The body of WP-01 still names `GET /_status` returning JSON**, carried in verbatim from the Geodica handoff on 2026-08-26, three days before the ruling that removed it. **It is named here rather than quietly dropped, because a superseded design that stays legible in the artefact gets built from**: the handoff is the drop-in example and a reader following it lands on the endpoint that no longer exists. **Status is a query like every other query**, and `stopped` is derived from that query failing rather than reported by a lighter endpoint that would be a second home for the same fact and could answer while the real surface cannot. **AND THE NEGATIVE IS REFINED BY `AC-01.6`, WHICH IS NOT A SECOND HOME AND MUST NOT BE READ AS ONE** (vc, 2026-08-31, on ic's coherence question, which two nodes reached independently -- so the text invites the misreading and one clause is cheaper than re-deriving it each time). The clause above forbids a DAEMON-SIDE lighter endpoint, on the stated ground that it would be a second home able to answer while the real surface cannot; `GET /_status` is exactly that. **`AC-01.6`'s three states are a CLIENT-SIDE projection above the one routing predicate, so there is no second place for the fact to live.** The two rows divide cleanly and neither yields: **liveness is still decided by the query completing or failing, exactly as written here, and the kernel lock only says WHICH KIND of stopped a stopped daemon is** -- a distinction no failed query can carry, which is why reading this clause as forbidding the projection would make `AC-01.6` unsatisfiable. **A reading that puts two rows of one thread in contradiction loses to a reading that satisfies both.** **AND `AND NOTHING ELSE` IS A CLAIM ABOUT THE WIRE CONTRACT, NOT ABOUT WHO HOLDS THE SOCKET**: the app shells the CLI (`AC-01.2`, and the design handoff's *the app holds NO product logic*), and the CLI talks GraphQL/JSON to `intentd`. **But that alone would satisfy this row VACUOUSLY** -- an app driving only `daemon status` moves no GraphQL byte at all, and a clause with no subject is not a test that can fail. **So the row requires at least one REAL query exercised through `intent graphql`, whose own help declares it runs *through intentd*.** That is the app talking GraphQL to `intentd` through the one door, and it is what makes this criterion refutable rather than true by construction. -- satisfied: no
- AC-01.4 (non-test) **THE TAIL-ORPHAN TRAP IS VERIFIED BEFORE ANY CONSOLE IS BUILT ON IT, AGAINST SIGTERM, SIGINT AND SIGKILL SEPARATELY.** Geodica's handoff names this first and the ordering is the point: if the log verb is a pipeline behind a runtime, it takes its own process group down when its stdin closes, and a console built on top of that leaks a `tail` per open-and-close. **Checked by driving the app open and shut several times and then reading `ps`**, not by reasoning about the pipeline. **The three signals are checked SEPARATELY because they do not share a path**: a handler that cleans up on SIGTERM and SIGINT proves nothing about SIGKILL, which cannot be handled at all, so the orphan must be prevented by process-group construction rather than by a shutdown hook. **A leaked tail is invisible to the operator and unbounded in count** -- the failure is that the machine gets slower over a week and nothing ever points at the menubar. -- satisfied: no
- AC-01.5 (non-test) **THE `intent://` HANDLER RESOLVES THROUGH ST0057 WP-07'S READ RESOLUTION AND IS NEVER A SECOND RESOLVER.** The URL scheme is a CLIENT of the addressing rules, so the app parses no address itself: it hands the URL to the resolver and opens what comes back via `intent edit`. **An address that resolves in the CLI and not in the app, or to a different entity, is a failure of this row and not a difference of opinion** -- and the `0149` defect is the standing reminder of how that decays, where `intent edit issue 148` parsed the kind correctly, discarded it, and refused about a steel thread the caller never named. **A resolver reached from two places drifts at whichever one is edited second**, which is a property of the arrangement rather than of anyone's care. -- satisfied: no
- AC-01.6 (non-test) **THE APP DISTINGUISHES THREE ENDPOINT STATES -- LIVE, STALE, ABSENT -- AND NEVER RENDERS `RUNNING` FOR AN ENDPOINT THAT CONNECTS WITHOUT ANSWERING.** **THIS ROW READ `the same three endpoint states THE ROUTING RULE DOES` UNTIL 2026-08-31, ASSERTING A PREMISE `Route` EXPLICITLY REFUSES:** its own doc states the absence of a third variant as the invariant -- *it holds because this type cannot express it and `route` is total*, and a `Both` arm or an ignorable `Option<Endpoint>` would move the guarantee out of the type system and into everyone's discipline. **The three states are therefore a PROJECTION ABOVE `route()`, never a widening of it**, which is also what keeps `AC-01.2` true by construction: app -> CLI verb -> projection -> `route()`, one predicate and one code path. (Found by cc, 2026-08-31, by reading this row against the type it names; repaired by vc under the standing pen. The premise was false when written, so this is a row stating something the artefact refuses -- not a reword to match an unfinished build.) `AC-08.3` names both false positives and this row inherits both: **a dead daemon leaves its socket file behind**, and **an inherited listening descriptor accepts connections nothing will ever answer** (no atomic `SOCK_CLOEXEC` on macOS; measured by cc 2026-08-29 at 1 in 300 with a sibling thread spawning children, 0 in 2000 without). **The app is where these are most damaging, because it is the surface an operator consults INSTEAD of measuring**, so a green dot is trusted in a way a CLI exit code is not. **Both states are constructed in the check, never waited for** -- a suite that passes because a 1-in-300 race did not fire has measured nothing. **AND THE SPLIT BETWEEN THE TWO NON-LIVE STATES MUST BE OBSERVABLE IN THE REMEDY, WITH THE INHERITED-DESCRIPTOR CASE LANDING ON `ABSENT`.** The prohibition above is satisfied by EITHER candidate discriminator -- the kernel lock and a bare `connect()` both render not-RUNNING for both false positives -- so **this criterion does not force the row, and a build must not be read as compliant merely because its vocabulary matches this sentence.** What it must show is that STALE and ABSENT carry DIFFERENT remedies: STALE means a holder is alive and not serving, so the operator investigates it and must NOT unlink (`AC-08.12`, where being wrong is destructive), while ABSENT means nothing owns the endpoint and residue is safe to clear. An orphaned listening descriptor has no holder to investigate, so a build rendering it STALE fails this row by declaring a remedy it cannot carry out. (vc ruling, 2026-08-31, on cc's six-case measurement; the discriminator itself lives beside the projection in `intentsvcs::daemon` and is not restated here.) -- satisfied: no
- AC-01.7 (non-test) **THE BUILD, RUN, TEST AND INSTALL PIPELINE IS A DEVBIN VERB RATHER THAN A SEQUENCE ANYONE REMEMBERS**, and the shipped artefact is signed and notarised per `D38` on the `int macos sign|notarize|verify` path that `AC-11.1` already established. **`D38` requires both, unconditionally**, and the app is the first artefact on this estate where the operator meets Gatekeeper directly: an unnotarised menubar app does not fail loudly, it fails as a dialog the user cannot get past, on a machine the developer does not own. **The verb is the deliverable, not the invocation it wraps** -- the same argument that put `int macos ...` ahead of cargo-dist, which signs only and does not notarise at all. -- satisfied: no
- AC-01.8 (non-test) **THE ICON IS INTENT'S TURTLE (`docs/design/intent-logo.*`) AND ITS STATE SEMANTICS ARE DERIVED FROM THE HEALTH PREDICATE, NEVER STORED ALONGSIDE IT.** hv named the asset and the reason on 2026-08-26 -- *slow and steady wins the race* -- and the substantive half of this row is the second clause: the menubar glyph is a RENDERING of the one predicate in `AC-01.2`, computed at paint time, with no cached `lastKnownState` that can outlive what produced it. **A status cached beside the thing it describes is the gate-figure defect in miniature**: two homes, one value, and the stale one is the one on screen. -- evidence: Turtle asset + state-derived (ic, HEAD b083fb4c): MenuBarIcon.imageset (menubar_18/36/54.png, template-rendering-intent=template) rasterised from docs/design/intent-logo.svg by 'int macos app-icons' (rsvg-convert), landed d00956ec, turtle visually confirmed. Tint computed at paint time from the one health predicate (Theme.menuBarTint + updateStatusIcon on every observation change), no cached lastKnownState. -- satisfied: yes
- AC-01.9 (non-test) **THE APP RESOLVES `intent` THROUGH A CAPTURED LOGIN-SHELL PATH, BECAUSE A `.app` BUNDLE HAS NO LOGIN SHELL AND THEREFORE NO PATH.** Every child the app spawns is handed that environment, and binary resolution falls back to it. **One home for the shell-out** -- a single CLI wrapper owning binary resolution, environment and error mapping, with nothing else in the app spawning anything -- which is `AC-01.1`'s no-product-logic rule in the one place it is easy to breach twice.

**THIS ROW EXISTS BECAUSE IT IS INVISIBLE UNTIL A USER REPORTS IT, AND THEN IT IS UNFALSIFIABLE FROM THE DEVELOPER'S SIDE.** The app cannot find `intent` while the user's terminal can, on a machine the developer does not own, with no error that names the cause. **Priced by ic on 2026-08-30 from Geodica's `LoginShell.swift`, 96 lines that exist for exactly this** -- a cost nobody discovers by reasoning about the design and nobody would put in an estimate without having read the source. **A homebrew-installed `intent` under `/opt/homebrew/bin` is not on a bundle's default `PATH`**, so this is the ordinary case rather than an edge one.

**Asserted by launching the built bundle with a deliberately minimal environment**, never by running the app from a terminal that already carries the developer's `PATH` -- which is the instrument that cannot fail, on the one axis this row is about. -- evidence: Stated instrument run (ic): the built Debug bundle (~/.local/state/intent/build/.../Intent.app) launched under a deliberately minimal environment (env -i HOME PATH=/usr/bin:/bin, no dev PATH); os_log shows 'launched; intent: native/rust/target/release/intent (login shell: /Users/matts/.local/bin/intent)' -- resolved via the CAPTURED LOGIN-SHELL PATH, not the launch env. Positive+negative controlled: /bin/zsh -ilc (capture()'s command) recovers the login PATH and finds intent under env -i, while a naive bare-PATH child sees NONE. LoginShell + IntentCLI is the one shell-out home, PATH handed to every child; parse() unit-tested (LoginShellTests). Bundle launch cleaned up orphan-free. -- satisfied: yes

## Acceptance Tests

### WP-01 -- Menubar app: control of intentd after Geodica's design -- no product logic, every control an intent verb, the daemon is the authority on the daemon (status: WIP)

_(no tests in this group)_

---

_Generated by Intent v3.0.0 from `thread.json`. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
