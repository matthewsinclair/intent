---
st_id: ST0051
title: intent output width: dft_width config for generated files, terminal width for stdout
---

# ST0051: intent output width: dft_width config for generated files, terminal width for stdout -- Acceptance

> Canonical acceptance contract. Acceptance Criteria (AC) are the ratified completeness boundary; Acceptance Tests (AT) are the small red-to-green tests that prove them.
>
> Done = every AC is covered by a GREEN AT, or (for a non-test AC) its named evidence is satisfied, AND the AC set is the ratified full boundary. Done is read from this map, never from a hand-ticked box.
>
> Test-backed satisfaction is COMPUTED from covering green ATs and never stored -- storing it would be double truth. An AC has four states, not two: beyond satisfied and unsatisfied, a requirement can be **descoped** to a named thread or **withdrawn** with its reason on the record. Both are non-blocking and both are reported separately, so a thread that descoped half its contract looks like one.

## Acceptance Criteria

### Group 01

- AC-01.1 `get_default_width` returns the project config's `dft_width` value when it is set. -- satisfied: yes (computed)
- AC-01.2 `get_default_width` returns 120 when `dft_width` is absent or empty in config. -- satisfied: yes (computed)
- AC-01.3 (non-test) `get_default_width` is the sole file-width source, reuses `read_config_field`, and leaves `get_terminal_width` unchanged (Highlander -- no duplicate width logic). -- evidence: review of `bin/intent_helpers` (get_default_width wraps get_config_field; get_terminal_width untouched) -- satisfied: yes

### Group 02

- AC-02.1 `intent st sync --write` sizes `steel_threads.md` from `get_default_width`, independent of the terminal width the command runs under. -- satisfied: yes (computed)
- AC-02.2 `intent st list` renders to stdout at the live terminal width (`get_terminal_width`). -- satisfied: yes (computed)
- AC-02.3 an explicit `--width N` overrides both the file path and the stdout path. -- satisfied: yes (computed)
- AC-02.4 (non-test) the regenerated `steel_threads.md` renders every current thread's slug without ellipsis at width 120. -- evidence: the committed `intent/st/steel_threads.md` (ST0046 slug renders in full) -- satisfied: yes

### Group 03

- AC-03.1 (non-test) every other file-generating command is either non-columnar (width-agnostic) or routed through `get_default_width` for its file output; the audit is recorded in `impl.md`. -- evidence: `impl.md` generator-audit table -- satisfied: yes
- AC-03.2 (non-test) the file-vs-stdout width rule is documented where a future generator author will see it. -- evidence: `usage-rules.md` "### Generated file width" + the `get_default_width` helper comment + MODULES.md -- satisfied: yes

## Acceptance Tests

### Group 01

- AT-01.1 `tests/unit/output_width.bats` -- covers AC-01.1 -- status: green -- test: dft_width_read_from_config
- AT-01.2 `tests/unit/output_width.bats` -- covers AC-01.2 -- status: green -- test: dft_width_defaults_to_120

### Group 02

- AT-02.1 `tests/unit/output_width.bats` -- covers AC-02.1 -- status: green -- test: sync_write_uses_default_width_not_terminal
- AT-02.2 `tests/unit/output_width.bats` -- covers AC-02.2 -- status: green -- test: list_uses_terminal_width
- AT-02.3 `tests/unit/output_width.bats` -- covers AC-02.3 -- status: green -- test: explicit_width_overrides_both

### Group 03

_(no tests in this group)_

---

_Generated by Intent v3.0.0-dev from `thread.json`. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
