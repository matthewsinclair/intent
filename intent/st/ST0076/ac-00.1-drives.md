# ST0076 AC-00.1: hv's drive, recorded by vc

AC-00.1 is a non-test criterion with three clauses, each driven on the Intent estate after a rebuild on 2026-09-17. hv ran the commands of the first two clauses through the harness, so their output is in vc's session verbatim; the third is a fresh Claude session's behaviour, driven by a script.

## Clause 1: the defined-but-never-referenced query

On the pair rebuilt at c800b8319 (store schema 28, the index re-extracted at extractor version 2), hv ran:

```
intent search --sql "SELECT d.name FROM symbols d WHERE d.lang = 'rust' AND d.kind = 'def' AND d.name IN ('is_local', 'AddressError', 'from_project') AND NOT EXISTS (SELECT 1 FROM symbols r WHERE r.kind = 'ref' AND r.name = d.name) GROUP BY d.name"
```

The output was the header `name` and no rows, so none of the three reads as defined and never referenced. vc's read of the store beside it: `is_local` has six level-1 reference rows, the uses inside `assert!` that WP-02 made visible; `AddressError` has fifteen at level 1 and five at level 2; `from_project` has five at level 2.

## Clause 2: the methods of AddressError

On the pair rebuilt at 8ec04d9fa, which carries WP-04 (e5b7feb1b), hv ran:

```
intent search --subkind method --in AddressError
```

It exited 0, printed the level note, and listed one row: `native/rust/crates/intentsvcs/src/address.rs:355  def method  AddressError  remedy`. The SQL door agrees that `remedy` is the only method whose container ends in `AddressError`, so each of its methods is listed once. The clause first read `--kind method`; hv ruled the `--subkind` spelling and the search that runs on filters alone on 2026-09-17 (hv decision 19).

## Clause 3: a fresh session reaches the index before grep

The driver is banked at `refs/bank/vc/drive-0428`: a headless `claude -p` session with a fresh session id, the project's prompt gate released by touching that session's sentinel, the tools ToolSearch, Grep, Glob, Read and `mcp__intent__intent_search` allowed, and the prompt "Where is the Rust function pick_up defined in this repository? Give the file and line. Do not edit anything." A Skill call is not counted, because the project's CLAUDE.md sends every session to `/in-session` first.

Without any instruction in CLAUDE.md (pair at c800b8319), three runs: Grep first; Grep first; and the index first, which was hv's own run. A separate session quoted the MCP server's `instructions` back verbatim, so issue 0428's delivery works and the text was not followed.

With the instruction appended to the system prompt, three runs of three reached the index first.

With the `## Finding code` section in the real CLAUDE.md (template at 8ec04d9fa, this project's CLAUDE.md regenerated at 196284266) and the pair at 8ec04d9fa, three fresh sessions started at 2026-09-17 13:43Z. All three called ToolSearch with `select:mcp__intent__intent_search` and then `mcp__intent__intent_search` with `kind` def, `query` pick_up and `lang` rust; none called Grep first; each answered `native/rust/crates/intentsvcs/src/store.rs:2608`.

The limit of this clause: which tool a session reaches for is the model's choice and varies between runs. This records three of three on this date with the section in place, against one of three without it; it is not a guarantee.
