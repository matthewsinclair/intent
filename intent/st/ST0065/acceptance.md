---
st_id: ST0065
title: Review Intent's use of .md files and /in-* skills for the v3 release
---

# ST0065: Review Intent's use of .md files and /in-* skills for the v3 release -- Acceptance

> **THIS FILE IS A GENERATED VIEW, AND A ROW AUTHORED HERE IS DISCARDED BY THE NEXT SYNC.** The acceptance contract is canon in the thread model; this file renders it. Acceptance Criteria (AC) are the ratified completeness boundary; Acceptance Tests (AT) are the small red-to-green tests that prove them.
>
> Done = every AC is covered by a GREEN AT, or (for a non-test AC) its named evidence is satisfied, AND the AC set is the ratified full boundary. Done is read from this map, never from a hand-ticked box.
>
> Test-backed satisfaction is COMPUTED from covering green ATs and never stored -- storing it would be double truth. An AC has four states, not two: beyond satisfied and unsatisfied, a requirement can be **descoped** to a named thread or **withdrawn** with its reason on the record. Both are non-blocking and both are reported separately, so a thread that descoped half its contract looks like one.

## Acceptance Criteria

### ST-level

- AC-00.1 **A RULE HAS EXACTLY ONE HOME IN THE CANON SET, AND WHERE ONE DOCUMENT NEEDS ANOTHER'S RULE IT POINTS RATHER THAN COPIES.** (hv ruled the root-and-branch review 2026-08-28; scoped by ic, minted by vc under hv's pen.) The set is the bootstrap documents a project is given -- `AGENTS.md`, `CLAUDE.md`, `usage-rules.md` and the rest -- plus the `/in-*` skills. A rule stated in two of them is not redundancy, it is a **fork with no merge**: the copies agree on the day they are written and drift thereafter, and **the drift is invisible because both copies read perfectly well.** This concretises `IN-AG-HIGHLANDER-001` at the surface where a copy is cheapest to make, because prose has no compiler. **THE POINTER IS THE MECHANISM: a reference by rule id or path, resolvable at read time, so the second reader gets the current text rather than a snapshot of it.** `CLAUDE.md` already argues this against itself -- the four cross-language principles are stated in `AGENTS.md` and deliberately not restated, because a second copy would be a Highlander violation in the document that defines the rule. -- satisfied: no (computed)
- AC-00.2 (non-test) **EVERY INSTRUCTION IN THE CANON SET IS ONE AN AGENT CAN EXECUTE WITH THE TOOLS IT HAS, AND FOLLOWING IT LEAVES A TRACE THAT DISTINGUISHES COMPLIANCE FROM NON-COMPLIANCE.** (hv, 2026-08-28: *some of the others are not just out of date, but potentially wrong, or worse, misguided -- wrong can be corrected, misguided will lead users and the LLMs down the wrong path.*) **THIS IS hv's MISGUIDED CLASS MADE MECHANICAL AND IT IS THE ROW THIS THREAD IS MOST FOR.** An instruction that cannot be followed does not fail and does not report: **it gets APPROXIMATED, silently, and the approximation is what gets learned and repeated.** The exemplar is already in canon and is not hypothetical -- *search `MODULES.md`, do not read it*: the registry runs to hundreds of kilobytes across hundreds of rows, so **check it first is not an instruction anyone can follow by reading**, and every agent told to read it did something else instead. **THE SISTER HALF IS THE SAME PROPERTY, NOT A SECOND SUBJECT: an instruction whose compliance leaves no trace cannot be reviewed**, so nothing ever discovers that it stopped being followed. The two are NESTED rather than parallel -- an instruction that cannot be executed leaves no trace either -- so executable-and-checkable is one property, and a rule with only the first decays without a symptom. -- satisfied: no
- AC-00.3 (non-test) **A CANON DOCUMENT DOES NOT ASSERT TOOL BEHAVIOUR THAT THE TOOL REPORTS ABOUT ITSELF; WHERE SUCH A CLAIM MUST BE MADE, IT NAMES THE VERB THAT ANSWERS IT.** **THIS IS THE STALENESS ENGINE FOR THE WHOLE SET.** Every pinned claim about a flag, an exit code or a spelling was true of one build, nothing re-checks it, and it reads exactly the same after it stops being true -- so the document becomes confidently wrong at a rate nobody can observe. `CLAUDE.md` states both the rule and its reason in its own text: pinning that answer here would make this a second home for a fact the tool already reports about itself. **THE CURE IS A POINTER TO AN ANSWERING VERB, NOT A BETTER-MAINTAINED COPY** -- a copy that must be maintained is a copy that will be right until the first release nobody remembers it during. **AND THE COST IS PAID BY THE READER WHO CANNOT TELL: a stale claim in canon is indistinguishable from a current one at the point of use.** In a PORT this is sharper still: a claim true of v2 re-drives CLEAN against the line it cites, so only a re-drive that checks the SUBJECT rather than the CITATION can find it. -- satisfied: no
- AC-00.4 **A SURVIVING SKILL HAS A LIVE CONSUMER.** (hv ruled the root-and-branch review 2026-08-28; scoped by ic, minted by vc under hv's pen.) A skill that nothing invokes, or that chains to something retired, is **an offer that cannot perform** -- `AC-17.13`'s honest-set rule one surface over, and the same defect wearing prose rather than a palette entry. **THE POPULATION IS THE SET ITSELF AND THE QUESTION IS ASKED OF EACH MEMBER: what reaches this, and is that thing itself alive?** A skill kept because it MIGHT be wanted is a menu entry with no realiser; a skill kept because retiring it looked like work is the same thing with a worse reason. **THE CHAINING HALF IS WHAT MAKES THIS MORE THAN A GREP: a skill with a live caller can still be dead if what it chains to is not, and liveness is transitive in the direction nobody checks.** **SPLIT FROM THE RETIREMENT ROW 2026-09-02 (vc): the two were minted as one and are two subjects with two instruments -- this one is answered by asking who calls, and `AC-00.5` by driving a lifecycle in a fixture. A reader testing one row would have swept the obvious half.** -- satisfied: no (computed)
- AC-00.5 **A RETIRED SKILL IS GONE FROM EVERY LOCATION ITS LIFECYCLE TOUCHES, AND EACH MEASUREMENT BELOW NAMES THE VERSION IT IS TRUE OF** -- because in this estate a premise carries one, and a claim true of v2 reads identically after it stops being true of v3. **RETIREMENT IS A THREE-LOCATION OPERATION -- the canon source, the manifest entry, and the installed tree under `~/.claude/skills/` -- and deleting the canon file reaches only the first.** **MEASURED AGAINST v3** (`intentsvcs::payload` at HEAD, driven 2026-09-02 in a disposable tempdir fixture, install tree and Claude target and manifest all throwaway): install a skill, delete its canon source, run `sync`. Outcome `SourceMissing`, **the installed directory is still there, and the manifest entry is still there.** `sync` reports the orphan and prunes nothing. **ALSO v3, AND IT IS THE HALF THAT INVERTS THE SEVERITY: `Payload::sync` has exactly ONE caller in the whole CLI -- the explicit `skills sync` verb -- so nothing automatic ever runs it.** A canon-only delete strands the skill silently and indefinitely, while `~/.claude/skills/`, which is what Claude Code actually reads, goes on loading it into every session. **MEASURED AGAINST v2, AND STATED ONLY AS THE CONTRAST: `bin/intent_upgrade:202` runs `intent claude skills sync`, so under v2 the stranding was at least REPORTED on every upgrade.** That line is still present and still accurate, **and it is not a fact about the shipped tool** -- citing it as one is the defect this row was drafted with and corrected for. **REMOVAL IS MODELLED AND THE DELETE DOES NOT REACH IT**, which is what makes this a lifecycle finding rather than a gap: `Outcome::Removed { removed, left }` distinguishes files this tool wrote from files it never wrote and left alone. **That is `uninstall`'s verb.** Fail-forward: retired means gone, never deprecated in place. **SPLIT FROM `AC-00.4` 2026-09-02 (vc): the two were drafted as one row and are two subjects with two instruments -- `AC-00.4` is answered by asking who calls, this one by driving a lifecycle in a fixture.** -- satisfied: yes (computed)
- AC-00.6 **A CHANGE THAT MUST REACH EVERY MACHINE IS VERIFIED AT THE MACHINE, NEVER AT THE SOURCE.** **ONE MECHANISM WITH SEVERAL INSTANCES, AND THE MECHANISM IS THE ROW: the source is right, the surface reports success, and the delivered thing differs.** Nothing fails, so nothing is investigated. Its examples in this thread's own scope: **skill sync checksums `SKILL.md` alone**, so a change to a skill's `scripts/` never propagates and the sync reports a clean run over it; and **the skill renderer strips `$N`**, so an awk print-field expression written into a `SKILL.md` arrives as an empty pipeline -- which is how the `/in-session` gate release became a silent no-op that reported nothing and released nothing. **THE STRANDING IS A THIRD INSTANCE AND IS NOT RESTATED HERE: `AC-00.5` owns it, and `AC-00.1` forbids the second home.** The pointer is in this text deliberately, because a reader meeting this row alone would otherwise see two instances where the thread has three and have no way to know a third exists. **THE CURE IS ALWAYS THE SAME AND IT IS NEVER MORE CARE AT THE SOURCE: read back what the machine actually got.** -- satisfied: no (computed)
- AC-00.7 **THE CANON SET IS ENUMERABLE BY DERIVATION FROM THE TREE AND THE CANON, NEVER BY A HAND-WRITTEN LIST.** **THIS IS A CLAIM ABOUT THE SET, LIVE AND CHECKABLE AT ANY MOMENT, RATHER THAN A CLAIM ABOUT ANY REVIEW.** A hand-listed population goes stale the day a document or a skill is added -- and from that moment **every coverage claim resting on it is false while still reading true**, which is the failure this row exists to make impossible rather than to detect. Coverage then follows as a CONSEQUENCE: *all of them were examined* becomes a checkable statement about a derived population instead of an unfalsifiable one about somebody's diligence. **AND THE POPULATION IS THE FIRST THING A REVIEWER INHERITS, NOT THE LAST**: the next review of this set gets the enumeration and not this thread's catalogue, so the derivation is what outlives the work rather than the findings. **STATED AS ENUMERABILITY RATHER THAN AS COVERAGE ON vc's AMENDMENT 2026-09-02: a coverage claim is retrospective and unfalsifiable, and a criterion that could only ever be satisfied by a review that has already happened is not a bar.** -- satisfied: no (computed)

## Acceptance Tests

### ST-level

- AT-00.1 `native/rust/crates/intent-cli/tests/canon_states_a_rule_once.rs` -- covers AC-00.1 -- status: to-write -- INSTRUMENT NOT BUILT. What it must do: for each rule the canon set states, establish that exactly one document states it and the others POINT. The tractable form is not "detect duplicated meaning" -- that is a read -- but a two-sided check on the rules that carry IDS: for every `IN-*` rule id, the document that OWNS it is the rule library, and a canon document mentioning that id must cite it rather than reproduce its text. THE POSITIVE CONTROL IS THE HALF THAT MATTERS: plant a paraphrase of an owned rule in a canon document and the check must fire, because a checker that only catches VERBATIM copies passes the case that actually happens -- prose forks by rewording, not by duplication.
- AT-00.2 (non-test) A READ OF THE SURVIVING CANON SET, ONE INSTRUCTION AT A TIME, ASKING TWO QUESTIONS OF EACH: can an agent execute this with the tools it has, and would following it leave a trace that distinguishes compliance from non-compliance? NON-TEST BECAUSE BOTH QUESTIONS ARE JUDGEMENTS ABOUT MEANING. Whether "check the registry first" is executable depends on how large the registry is and what reading costs -- a grep can find the sentence and cannot weigh it. The verdict vocabulary is hv's own: keep / correct / retire / MISGUIDED. THE EXEMPLAR IS ALREADY IN CANON AND IS THE CALIBRATION FOR THE REST: `MODULES.md` runs to hundreds of kilobytes across hundreds of rows, so "read it first" is not an instruction anyone can follow, and every agent told to read it did something else instead -- silently, because an approximation does not report itself. WHAT THE READER MUST NOT DO IS ACCEPT AN INSTRUCTION BECAUSE IT IS FOLLOWED IN PRACTICE: an instruction that is approximated looks obeyed from the outside, and the approximation is what has actually been learned. -- covers AC-00.2 -- status: to-write -- NOT YET DONE. The WP-02 catalogue read all 25 skills against hv's vocabulary on 2026-08-28, so the raw material exists -- but it was a review of the set AS IT WAS, and this row is about the set that SURVIVES the rationalisation, which has not been cut yet. Citing the catalogue for it would be evidence about a different population.
- AT-00.3 (non-test) A READ OF THE SURVIVING CANON SET FOR CLAIMS ABOUT TOOL BEHAVIOUR -- a flag, an exit code, a spelling, a default -- ASKING OF EACH WHETHER THE TOOL ITSELF ANSWERS THE SAME QUESTION. Where it does, the document must point at the answering verb rather than restate the answer. NON-TEST BECAUSE THE JUDGEMENT IS WHETHER A SENTENCE IS A CLAIM ABOUT THE BUILD AT ALL, which is a reading, not a pattern: "run `intent upgrade` to convert" is a claim about a verb existing, "`--skip-settings` was not carried into v3" is a claim about a build, and no grep separates them. THE READ IS AGAINST A NAMED BUILD AND SAYS WHICH, because a claim about the tool is a claim about a REVISION and this estate is a port -- a v2-true sentence reads identically after it stops being true of v3, and a re-drive aimed at its CITATION passes. WHAT MAKES A SURVIVING CLAIM ACCEPTABLE IS NOT THAT IT IS CURRENTLY TRUE: it is that something other than a human's memory will notice when it stops being. -- covers AC-00.3 -- status: to-write -- NOT YET DONE, and it needs a BUILD named at the time of the read rather than a date. The WP-01 catalogue measured against `intent 3.0.0 (4479264f)` on 2026-08-28, with a scratch `intent init` in an empty directory as its control -- which is the right shape -- but that build is many commits behind and the surviving set has not been cut yet. THIS NOTE WAS WRITTEN TWICE: the first attempt passed it as an inline double-quoted shell argument, and zsh executed every backticked span in it as a command. Both citations were replaced with nothing, the row was created, and the tool printed `ok`. The note read plausibly and had silently lost the two facts it existed to carry.
- AT-00.4 `native/rust/crates/intent-cli/tests/every_skill_has_a_live_caller.rs` -- covers AC-00.4 -- status: to-write -- INSTRUMENT NOT BUILT. What it must do: for every skill in the derived canon set, establish that something reaches it -- the `/in-session` language table, a `chains_to:` from another skill, or a documented invocation by name -- and that what it chains TO exists. LIVENESS IS TRANSITIVE AND THAT IS THE DIRECTION NOBODY CHECKS: a skill with a live caller is still dead if the thing it chains to was retired, and the caller looks healthy throughout. vc split this off `AC-00.5` for exactly that reason -- buried under the retirement half, the transitive direction had no instrument and would have been swept. THE POSITIVE CONTROL: plant a `chains_to:` naming a skill that does not exist and the check must fire; plant an orphan skill nothing references and it must fire too. A checker that only walks FROM the roster finds the second and never the first.
- AT-00.5 `native/rust/crates/intentsvcs/tests/skills_sync.rs` -- covers AC-00.5 -- status: green -- `a_canon_delete_strands_the_skill_and_only_uninstall_prunes_it` in `native/rust/crates/intentsvcs/tests/skills_sync.rs`, driven in that file's own disposable tempdir fixture -- install tree, Claude target and manifest all throwaway, no real user state touched. TWO ARMS, AND THE SECOND IS WHY THE FIRST IS A FINDING RATHER THAN A COMPLAINT. Arm one deletes the canon source and syncs: outcome `SourceMissing`, the installed payload still present, the manifest entry still present. Arm two uninstalls WITH THE CANON SOURCE ALREADY GONE -- which is the ordering claim the retirement sequence rests on, since uninstall does not read canon -- and both remaining locations are reached. WITHOUT ARM TWO THE ROW READS AS `the tool cannot prune`, WHICH IS FALSE and would send the next reader to build a pruner that already exists. THE VERSION IS NAMED IN THE TEST BECAUSE IN A PORT A DATE IS NOT A VERSION: this drives v3's `intentsvcs::payload`; v2's `bin/intent_upgrade` ran a skills sync on every upgrade so the orphan was at least reported there, and that line is still present and still accurate AND IS NOT A FACT ABOUT THE SHIPPED TOOL. WHAT UNINSTALL LEAVES, MEASURED RATHER THAN ASSUMED: it removes the files it wrote and leaves the now-empty directory, which is INERT -- `installed()` does not list it and a following sync reports no step for it at all. The first draft asserted the directory was gone, which was stronger than the criterion and turned an accurate result into an alarming one. BOTH ARMS DRIVEN RED: simulating a sync that prunes fails arm one with a message telling the reader to reword the ROW rather than relax the test, because if sync ever learns to prune, this criterion's second half is obsolete rather than violated.
- AT-00.6 `native/rust/crates/intentsvcs/tests/skills_sync.rs` -- covers AC-00.6 -- status: to-write -- INSTRUMENT NOT BUILT, AND IT IS EXPECTED TO LAND RED. What it must do: change a skill's `scripts/` WITHOUT touching its `SKILL.md`, sync, and establish that the change reached the installed tree. The checksum covers `SKILL.md` alone, so the sync reports a clean run and the script does not propagate -- the criterion is not satisfied today and the row should not be minted green by a test that avoids the case. THE FIXTURE ALREADY EXISTS: `intentsvcs/tests/skills_sync.rs` carries a disposable install tree, Claude target and manifest, which is where `AT-00.5` was driven. THE SECOND INSTANCE, THE `$N` STRIP, NEEDS ITS OWN ARM: a `SKILL.md` carrying an `awk` field reference must arrive at the machine with that reference intact, and today it arrives as an empty pipeline. BOTH ARMS ARE THE SAME MECHANISM -- the source is right, the surface reports success, and the delivered thing differs -- so they belong to one row. The stranding is the third instance and lives in `AC-00.5`, not here.
- AT-00.7 `native/rust/crates/intent-cli/tests/the_canon_set_is_derived.rs` -- covers AC-00.7 -- status: to-write -- INSTRUMENT NOT BUILT. What it must do: derive the canon set -- bootstrap documents and skills -- from the tree and the canon, and establish that no consumer of that set carries a hand-written list of its members. THE ROW IS ABOUT THE SET, NOT ABOUT ANY REVIEW, so the check is live at any moment and does not depend on a review having happened. THE POSITIVE CONTROL IS THE WHOLE INSTRUMENT: add a document or a skill to the tree and every derived enumeration must grow without an edit. A check that merely asserts "the list matches the tree today" passes on a hand-written list that happens to be current, which is the state this row exists to make impossible rather than to detect -- and it is the state a list is in right up until the moment it is wrong.

---

_Generated by Intent v3.0.0 from `thread.json`. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._
