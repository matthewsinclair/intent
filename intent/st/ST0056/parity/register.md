# Keep / retire / deviate register -- first pass (ST0056 / WP-01, AC-01.3)

> Measured at `69d42a7` on 2026-08-14 by ic. Regenerated by `tools/gen_register.sh` from `tools/burn.sh` output; do not hand-edit rows.

## How each row was decided

The classification axis is the **burn ratio**: each file is run twice, once with the default `INTENT_BIN` and once with `INTENT_BIN=/usr/bin/false`, and the delta is the number of tests that actually reach the top-level CLI.

This is empirical where reading assertions is inferential, and the difference is not academic. `treeindex_commands.bats` reads as 53 CLI tests and is in fact 53 tests that exec `bin/intent_treeindex` directly, bypassing the dispatcher entirely; its burn is zero. `claude_with_intent.bats` read as a CLI test, reported zero burn, and turned out to alias the binary through an unbraced `$INTENT_BIN_DIR/intent` that the retarget sweep had missed -- **the burn measurement found a hole in the retarget that four separate grep passes did not.**

| burn                                  | meaning                                     | class                                                     |
| ------------------------------------- | ------------------------------------------- | --------------------------------------------------------- |
| all tests                             | the file exercises the CLI and nothing else | **keep** -- runs unmodified against `INTENT_BIN`          |
| zero, and it calls `bin/intent_<sub>` | tests an entry point v3 will not have       | **deviate** -- semantic rewrite, not a path swap          |
| zero, and it sources a shell file     | unit-tests a bash function                  | **retire** -- dies with the shell                         |
| zero, and it never invokes the CLI    | pins this repo's own content                | **out-of-scope** -- not a conformance test                |
| some                                  | mixed concerns in one file                  | **split** -- needs per-test rows before WP-05 leans on it |
| any, but baseline not green           | the delta carries no information            | **UNCLASSIFIED**                                          |

A file matching no rule is emitted UNCLASSIFIED rather than assigned a best guess. A wrong `retire` is coverage that disappears at the cut with nobody watching, which is the defect the AT grammar existed to kill; the refuse-lossy discipline applies to classification exactly as it applies to migration.

**Scope note:** `split` is a first-pass verdict, not a final one. Those files carry both portable and non-portable tests and need per-test rows; this pass deliberately stops at the file level rather than guessing which half is which.

## Rows

| test file                                     | tests | burn  | class        | basis                    | notes                                                                                                                                                                                                                |
| --------------------------------------------- | ----- | ----- | ------------ | ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tests/integration/core_functionality.bats`   | 4     | 4/4   | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/integration/end_to_end.bats`           | 3     | 3/3   | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/ac_offscope_states.bats`          | 12    | 12/12 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/acceptance_close_gate.bats`       | 16    | 15/16 | split        | partial burn             | 15 of 16 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                   |
| `tests/unit/agent_commands.bats`              | 50    | 50/50 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/at_grammar_lint.bats`             | 19    | 18/19 | split        | partial burn             | 18 of 19 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                   |
| `tests/unit/attribution_compliance.bats`      | 7     | 0/7   | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/au_language_code_guard.bats`      | 4     | 3/4   | split        | partial burn             | 3 of 4 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/basic.bats`                       | 4     | 1/4   | split        | partial burn             | 1 of 4 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/bootstrap.bats`                   | 5     | 5/5   | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/claude_md_template.bats`          | 13    | 1/13  | split        | partial burn             | 1 of 13 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/claude_prime.bats`                | 2     | 2/2   | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/claude_with_intent.bats`          | 14    | 13/14 | split        | partial burn             | 13 of 14 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                   |
| `tests/unit/co_language_code_guard.bats`      | 4     | 3/4   | split        | partial burn             | 3 of 4 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/config.bats`                      | 7     | 5/7   | split        | partial burn             | 5 of 7 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/credo_checks_residue.bats`        | 6     | 6/6   | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/critic_config.bats`               | 10    | 3/10  | split        | partial burn             | 3 of 10 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/critic_dispatch.bats`             | 14    | 0/14  | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/critic_prose.bats`                | 15    | 0/15  | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/critic_report_format.bats`        | 7     | 7/7   | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/critic_runner_applies_to.bats`    | 19    | 0/19  | retire       | shell-function unit test | Sources a shell file and calls its functions directly. Dies with bash; there is no binary to retarget.                                                                                                               |
| `tests/unit/critic_runner_proxies.bats`       | 33    | 0/33  | retire       | shell-function unit test | Sources a shell file and calls its functions directly. Dies with bash; there is no binary to retarget.                                                                                                               |
| `tests/unit/docs_completeness.bats`           | 16    | 1/16  | split        | partial burn             | 1 of 16 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/doctor_leftover_intent.bats`      | 5     | 5/5   | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/ext_commands.bats`                | 27    | 27/27 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/ext_discovery.bats`               | 13    | 13/13 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/ext_seed_validity.bats`           | 18    | 1/18  | split        | partial burn             | 1 of 18 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/fileindex_commands.bats`          | 47    | 2/47  | split        | partial burn             | 2 of 47 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/global_commands.bats`             | 11    | 11/11 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/help_commands.bats`               | 12    | 12/12 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/helpers.bats`                     | 17    | 0/17  | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/highlander_audit.bats`            | 4     | 0/4   | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/in_author_essentials_skill.bats`  | 6     | 0/6   | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/in_content_essentials_skill.bats` | 6     | 0/6   | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/in_session_skill.bats`            | 14    | 0/14  | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/init_commands.bats`               | 13    | 12/13 | split        | partial burn             | 12 of 13 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                   |
| `tests/unit/intent_acceptance_cli.bats`       | 12    | 12/12 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/intent_agents.bats`               | 25    | 23/25 | split        | partial burn             | 23 of 25 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                   |
| `tests/unit/intent_claude_upgrade.bats`       | 19    | 18/19 | split        | partial burn             | 18 of 19 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                   |
| `tests/unit/intent_critic.bats`               | 21    | 1/21  | split        | partial burn             | 1 of 21 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/intent_init_lang_flag.bats`       | 7     | 7/7   | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/intent_issues.bats`               | 20    | 20/20 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/intent_lang.bats`                 | 30    | 30/30 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/intent_migrations_languages.bats` | 7     | 0/7   | retire       | shell-function unit test | Sources a shell file and calls its functions directly. Dies with bash; there is no binary to retarget.                                                                                                               |
| `tests/unit/intent_migrations_relocate.bats`  | 7     | 0/7   | retire       | shell-function unit test | Sources a shell file and calls its functions directly. Dies with bash; there is no binary to retarget.                                                                                                               |
| `tests/unit/intent_todo.bats`                 | 22    | 22/22 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/intent_upgrade_dispatcher.bats`   | 6     | 3/6   | split        | partial burn             | 3 of 6 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/intent_upgrade_orchestrator.bats` | 11    | 4/11  | split        | partial burn             | 4 of 11 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/learn_commands.bats`              | 18    | 18/18 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/llm_commands.bats`                | 5     | 5/5   | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/modules_commands.bats`            | 20    | 20/20 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/no_absolute_home_paths.bats`      | 10    | 5/10  | split        | partial burn             | 5 of 10 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/no_template_fallback.bats`        | 7     | 4/7   | split        | partial burn             | 4 of 7 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/objective_placeholder.bats`       | 12    | 8/12  | split        | partial burn             | 8 of 12 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/organize_commands.bats`           | 4     | 3/4   | split        | partial burn             | 3 of 4 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/output_width.bats`                | 6     | 4/6   | split        | partial burn             | 4 of 6 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/plugin_commands.bats`             | 12    | 12/12 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/pr_language_code_guard.bats`      | 4     | 3/4   | split        | partial burn             | 3 of 4 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/pre_commit_hook.bats`             | 15    | 0/15  | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/project_commands.bats`            | 3     | 3/3   | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/release_gate_script.bats`         | 7     | 0/7   | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/release_script.bats`              | 12    | 0/12  | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/release_sidecars.bats`            | 11    | 0/11  | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/require_in_session_gate.bats`     | 8     | 0/8   | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/rule_index.bats`                  | 8     | 8/8   | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/rule_pack_agnostic.bats`          | 11    | 2/11  | split        | partial burn             | 2 of 11 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/rule_pack_author.bats`            | 12    | 2/12  | split        | partial burn             | 2 of 12 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/rule_pack_content.bats`           | 13    | 2/13  | split        | partial burn             | 2 of 13 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/rule_pack_elixir.bats`            | 6     | 2/6   | split        | partial burn             | 2 of 6 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/rule_pack_elixir_runnable.bats`   | 5     | 0/5   | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/rule_pack_lua.bats`               | 9     | 2/9   | split        | partial burn             | 2 of 9 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/rule_pack_prose.bats`             | 12    | 2/12  | split        | partial burn             | 2 of 12 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/rule_pack_rust.bats`              | 9     | 2/9   | split        | partial burn             | 2 of 9 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/rule_pack_shell.bats`             | 14    | 2/14  | split        | partial burn             | 2 of 14 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/rule_pack_swift.bats`             | 9     | 2/9   | split        | partial burn             | 2 of 9 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/rule_reference_skills.bats`       | 5     | 0/5   | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/rule_validator.bats`              | 9     | 9/9   | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/rules_path_guard.bats`            | 2     | 0/2   | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/session_context_script.bats`      | 5     | 0/5   | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/set_e_increment_guard.bats`       | 1     | 0/1   | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/skill_renderer_trap.bats`         | 3     | 0/3   | out-of-scope | no CLI invocation        | Never invokes the CLI. Asserts this repository's own content, not the command surface; survives a binary swap untouched and is not a conformance test.                                                               |
| `tests/unit/skills_commands.bats`             | 39    | 39/39 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/st_commands.bats`                 | 54    | 54/54 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/st_enumeration.bats`              | 10    | 9/10  | split        | partial burn             | 9 of 10 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                    |
| `tests/unit/st_list_all_vocabulary.bats`      | 7     | 7/7   | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/st_new_acceptance.bats`           | 3     | 2/3   | split        | partial burn             | 2 of 3 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/st_zero_commands.bats`            | 33    | 32/33 | split        | partial burn             | 32 of 33 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                   |
| `tests/unit/subdir_invocation.bats`           | 7     | 6/7   | split        | partial burn             | 6 of 7 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/test_autopsy.bats`                | 19    | 12/19 | split        | partial burn             | 12 of 19 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                   |
| `tests/unit/test_diogenes.bats`               | 19    | 19/19 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |
| `tests/unit/title_pipe_sanitize_guard.bats`   | 3     | 2/3   | split        | partial burn             | 2 of 3 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/treeindex_commands.bats`          | 53    | 0/53  | deviate      | sub-script entry point   | Invokes bin/intent_<sub> directly, bypassing the dispatcher (PROJECT_ROOT resolution, INTENT_ORIG_CWD, cd to root -- bin/intent:198-218). No equivalent under one binary; needs a semantic rewrite, not a path swap. |
| `tests/unit/whiteboard_protocol_3_guard.bats` | 7     | 4/7   | split        | partial burn             | 4 of 7 tests reach the CLI; the remainder do not. Needs per-test rows before WP-05 relies on it.                                                                                                                     |
| `tests/unit/wp_commands.bats`                 | 30    | 30/30 | keep         | full burn                | Every test changes result when the binary is redirected: the file exercises the CLI and nothing else.                                                                                                                |

## Summary

| class            | files | what WP-05 does with them                                                                   |
| ---------------- | ----- | ------------------------------------------------------------------------------------------- |
| **keep**         | 30    | Run unmodified against the v3 binary. These are the conformance suite.                      |
| **split**        | 40    | Need per-test rows first: each mixes tests that reach the CLI with tests that do not.       |
| **deviate**      | 1     | Rewrite against the single-binary entry point, or retire with the sub-script they exercise. |
| **retire**       | 4     | Retire with the shell. No binary to point them at.                                          |
| **out-of-scope** | 19    | Leave alone. They pin this repo content and are unaffected by the binary swap.              |

**711 of 1235 tests (58%) actually reach the CLI.** The remaining 524 do not, and cannot serve as v3 conformance evidence whatever their assertions say. That number is the honest size of the conformance estate, and it is the figure WP-05 should plan against rather than the 1235 headline.

Baseline: all 1235 tests pass with the default `INTENT_BIN`, so the retarget is behaviour-neutral. That run was taken in a sacrificial worktree and is evidence, not certification -- the authoritative full-suite run is matts.
