# AC-09.6 — the facade read: every MCP-exposed row, resolved to its serving door

**The read AC-09.6 was minted for (vc, 2026-08-30), performed 2026-08-30 against `render.rs` and `facade.rs` at the tree of `61759934`.** The criterion is AGREEMENT, not coverage — _exposed implies servable_ — so this document's job is to make the choice legible: which rows have their one door, which need a door built, and which should be narrowed off `exposed_on_mcp`. **The choice itself is hv's; nothing here changes an exposure flag.**

## The instrument, its controls, and its five caught defects

The hand-read this row budgeted M for was replaced by a **syn-based scanner** (`armscan`, a scratchpad instrument, not shipped): it parses `render.rs`, finds every `.subcommand()` match transitively from `run`, and resolves each arm's TRANSITIVE facade reach through local helper fns — `impl Facade`'s public methods only. The root cause that made three hand-rolled span finders fail is the criterion's own recorded sentence: naive brace matching cannot find an arm body, because `format!` braces are indistinguishable from block braces without a lexer. `syn` is the lexer.

**Positive controls on every run, asserted rather than eyeballed:** `st list → st_list` [1], `fc → ac_fc/at_fc/st_fc/wp_fc` [4], `version → []` [0], `at green → at_set` [1], `todo → todo_buckets` (the bare-alias case). A run whose controls fail produces no partition.

**The controls caught five instrument defects before any number left the scanner — a reader should trust the partition _because_ it survived them, not because it came out clean:**

1. `pub fn` on sibling types scored as facade doors (`as_str` on 60 arms) — narrowed to `impl Facade`.
2. Or-pattern arm literals dropped (`state @ ("green"|"red"|"na")`) — 6 of the first 26 unresolved.
3. Top-level or-arms dropped (`Some(("list", _)) | None`).
4. An auxiliary `match m.subcommand()` inside a leaf arm (todo's flag probe) read as a deeper family — a nested match counts as dispatch only if some arm of it spells a path.
5. A family's bare `None` arm — the family path's own behaviour (`surface`) — was recorded nowhere.

**Population note (vc):** the walker's population is `surface/dispatch-table.json` — authored canon — and `render.rs` source, not a store view, so it inherits no store/disk lag.

## The partition — 94 exposed rows

```
52  ONE DOOR        exposed and servable; the door is declared on the row (this read's edit)
20  ZERO DOORS      classified below under vc's per-row test
11  MULTI-DOOR      classified below; 7 have a clean primary, 2 are the fc class, 2 are the sync fan
11  UNWIRED         the arm does not exist; the catch-all answers "known command that is not implemented yet"
```

## Zero-door rows (20) — vc's test: _does this tell an agent something about THE PROJECT?_

> **Errata (vc, same day, on this table's own hard case):** the stated test was a proxy and it failed on `schema`, which renders compiled-in faces exactly as `llm guide` and `surface` do, yet was ruled IN. **The real discriminator is NEED, not provenance: does an agent need this to do its work on this project?** Every classification below survives the restatement; `llm guide` and `surface` are since RULED NARROWED (two homes for _what can I do_ — the MCP tool list is that answer in the shape MCP defines; and an agent's correct experience of a retired verb is that the tool is not offered).

| row                                                     | classification                                                     | why                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------- | ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `version`                                               | **NARROW** (vc, by name)                                           | describes the binary, not the project                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `claude hook`                                           | **NARROW**                                                         | execs session-hook bodies from the install home; an agent must not drive lifecycle hooks through MCP                                                                                                                                                                                                                                                                                                                                                            |
| `claude rules` / `claude skills` / `claude ws`          | **NARROW (namespace)**                                             | parents; their default arms render listings/help. vc: a parent is a namespace, not a verb — leaves stay per dc's payload reshape                                                                                                                                                                                                                                                                                                                                |
| `critic`                                                | **NARROW for 3.0.1** (recommend)                                   | spawns whole rule-pack runs; real agent value but its own scope decision, not a fallout of this read                                                                                                                                                                                                                                                                                                                                                            |
| `schema`                                                | **FACADE GAP** (vc, by name)                                       | describes the store; agents genuinely want it                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `lang list` / `lang show` / `lang init` / `lang remove` | **FACADE GAP ×4**                                                  | read/write `intent/.config/config.json` — project state reached around the facade; `lang list` was mis-classed as no-store by arm shape (vc's example)                                                                                                                                                                                                                                                                                                          |
| `llm usage_rules`                                       | **FACADE GAP**                                                     | `--symlink` writes into the project                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `modules check` / `modules find`                        | **FACADE GAP ×2**                                                  | read `intent/llm/MODULES.md`, a project file                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `plugin list` / `plugin show`                           | **FACADE GAP ×2, with dc**                                         | project reads; dc's payload lifecycle is about to become their natural door                                                                                                                                                                                                                                                                                                                                                                                     |
| `todo notdone` / `todo toggle`                          | **FACADE GAP ×2 — and a boundary finding**                         | these MUTATE todo state through helpers that never pass the facade; the two strongest exhibits that the boundary leaks, which is the class AC-09.6 exists to catch                                                                                                                                                                                                                                                                                              |
| `llm guide` / `surface`                                 | **NARROWED** (vc, landed `6e92b110`; vocabulary struck — errata 2) | both render the compiled-in dispatch table, which is FACE by vc's discriminator (MODEL things go down, FACE things stay in the face). A facade method cannot serve them without moving the table down. The MCP tier is face-side too, so it CAN serve them directly — but then the row's `facade` field needs a vocabulary for "served face-side" (eg `face:guide::render`). vc's call on the vocabulary; both rows' exposure is well-argued in their own notes |

## Multi-door rows (11)

**The rule that resolves 7 of them:** the row's facade method is its PRIMARY door — the mutation or the query the verb IS; presentation reads (`st_show` rendering the after-state) and infrastructure (`project()` path resolution) are not second doors.

| row                           | doors                                            | primary / classification                                                                                                                                                                                                                      |
| ----------------------------- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ac rescope` / `ac reinstate` | verb + `st_show`                                 | `ac_rescope` / `ac_reinstate`                                                                                                                                                                                                                 |
| `st hydrate` / `st dehydrate` | verb + `project`                                 | `hydrate` / `dehydrate`                                                                                                                                                                                                                       |
| `search`                      | `search` + `prose_sections_indexed`              | `search`                                                                                                                                                                                                                                      |
| `organize`                    | `organize` + `declare_default`                   | `organize`                                                                                                                                                                                                                                    |
| `st new`                      | `st_new` + `st_triage` + `st_start`              | `st_new` — the extra doors are the `-s` flag's ruled composition (compose-transitions, vc 2026-08-15); an MCP caller invokes `st.start` as its own tool, because composition belongs in the CLI arm and never in the MCP tier                 |
| `fc`                          | 4 (`*_fc` by target shape)                       | **the fc class — issue 0171**, a live CLI defect predating WP-09; the row waits on the one `fc` door                                                                                                                                          |
| `todo done`                   | `st_done`/`wp_done`/`todo_flush` by target shape | **0171's sibling** — same dispatch-by-shape in a coordinator; noted on 0171 rather than filed twice                                                                                                                                           |
| `sync`                        | 4 direction methods                              | **NARROW for 3.0.1** (recommend, strongly): one row cannot name a door per direction, and `--to-store` is the D01-inverting hazard that destroyed a store write on 2026-08-30 (vc's 20:24Z incident). An MCP agent must not reach it casually |
| `agents sync`                 | `project` + the 4 sync methods                   | **NARROW for 3.0.1** (recommend) — same fan, same hazard class                                                                                                                                                                                |

## Unwired rows (11) — the falsifier population, live

`agents template`, `claude prime`, `claude subagents`, `config get`, `config set`, `ext list`, `ext new`, `ext show`, `ext validate`, `fileindex`, `learn` — each declared `exposed_on_mcp: true` while `render.rs` has no arm; the binary answers the catch-all's _known command that is not implemented yet_. **A row cannot be exposed and unwired at once**, so each either gets its arm before the tag or gets narrowed with a note that says _unwired at the 3.0.1 read_ — with three already settled elsewhere: `claude prime` (pruned, dc), `claude subagents` (narrowed under hv's payload reshape, not wired), and the `ext` family (outside the cut).

## Errata 2 (2026-08-30, same day) — building the serving match drove the field, and four values were reach, not doors

**The class finding, vc-confirmed: every `facade` value above came from armscan's TRANSITIVE REACH, declared as a door on the hypothesis _reach == door for a one-door row_. The hypothesis fails exactly where the value is an ACCESSOR or the arm COMPOSES — four rows, each now handled:**

1. **`doctor`** — the value was `store` (the arm's reach); the callable door is the real associated fn `Facade::doctor(project, ctx, Option<store>)`. **Corrected on the row.**
2. **`info`** — the arm COMPOSES its answer (install home, version, running executable, project block); `st_list` was its biggest read. **NARROWED** (vc): a tool whose description promises the overview and answers with a thread list misdescribes, and a mislabelled tool is worse for an agent than an absent one. Facade value removed; `mcp_narrowed` carries the ruling.
3. - 4. **`agents generate` / `agents validate`** — the value was `project` (reach); the serving functions were in the SERVICES crate all along (`rootfiles::render`; the validate checks extracted beside it). **vc's ruling (c): these are facade GAPS closed by ADDING methods** (`agents_generate`, `agents_validate`), never by composing in the tier and never by a face-side spelling.

**The `face:` vocabulary this document reserved for vc is STRUCK, not pending: the population is empty once each candidate was measured, and each was empty for a DIFFERENT reason** — `llm guide`/`surface` narrowed on need; the agents pair had their functions in `intentsvcs` all along. A vocabulary reserved for an empty population is an invitation the next ill-fitting row would take.

**`st sync`'s preview half is an explicit FACADE GAP entry, added here so a temporary refusal cannot become the permanent shape by nobody revisiting it:** the no-write half of the CLI arm renders the thread table (byte-identical to `st list --status all`); the declared facade `sync_to_disk` is only the write half. The MCP tool REFUSES without `write=true` (composing the preview in the tier is forbidden) — the refusal's remedy names the read tools, and this entry names the missing door.

**A LIMIT, RULED (vc, 2026-08-30), RECORDED WITH ITS DISCHARGE CONDITION: for 3.0.0 the MCP tier serves IN-PROCESS ONLY.** `AC-08.2`'s in-process-and-daemon-agree property does NOT extend to the MCP face — a running daemon cannot serve it, and that is a stated limit, not a gap: the same sequencing decision as declining (b), seen from the other side. **This becomes false when the tier routes through the op dispatch (`dispatch(op)`), which is the recorded 3.x destination.**

**And one instrument defect the serving work exposed in the DATA tier it rides on:** `schema()` skipped flags by `disposition == "drop"`, and the table's vocabulary contains no `drop` (keep 106 / retire 15 / intrinsic 10) — the filter matched zero flags since the tier landed, so published schemas carried retire and intrinsic flags (`intent_doctor` advertised both `fix` and `help`). A predicate that cannot match its subject returns the number that means success. The class fix: publish `disposition == "keep"` only, plus the flag-level `exposed_on_mcp: false` narrows for terminal-channel flags — where every hidden flag is first classified terminal-channel / honest-refusal / defect, and only the first two hide without a filing (vc's discipline, stated on `at lint --fix` and `doctor --fix` — the second of which was measured as already honoured by its `retire` disposition, and the narrow reverted as the wrong repair).

## What this read changes today, and what it leaves to hv

**Edited now (mechanical truth, no scope):** the 59 rows with a clean single door gain `"facade": "<method>"` in `surface/dispatch-table.json` — 52 one-door rows plus the 7 clean primaries. The field is declared ahead of the WP-09 generator that will consume it, which is the same order `exposed_on_mcp` itself was declared in.

**To hv, through vc, as one decision list:** 8 narrows (version, claude hook, 3 namespaces, critic, sync, agents sync), 12 facade gaps to build or defer (schema, lang ×4, llm usage_rules, modules ×2, plugin ×2, todo ×2), 2 vocabulary calls (llm guide, surface), 2 waiting on 0171, and 11 unwired rows to wire or narrow. **A smaller honest tool list is a better tag than a larger one with holes** (vc's sentence, kept).
