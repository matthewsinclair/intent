# Parity contract - ST0056: v2 conformance for the v3 CLI (WP-01 spec)

## The contract

**The v3 binary is certified by the incumbent**: the v2 BATS estate runs against it via an `INTENT_BIN` override, and v3 is green when v2's own suite cannot tell the difference -- except where this contract says so, in advance.

- **In scope (parity holds)**: stdout shape, stderr voice (`ok:`/`error:` lowercase, 0023), exit codes, command/subcommand/flag grammar, behavioural semantics.
- **Ratified deviation classes (decided here, never discovered in triage)**:
  - **File layout** -- structured canon appears (`thread.json`, `issues/<n>.json`); `info.md`/`acceptance.md` become generated views; frontmatter conventions on structured data retire (D01-D04). Tests asserting bytes-in-files that are now views retire with the layout.
  - **Issues directories** -- `OPEN/`/`CLOSED/` stop encoding status (status is data); index views replace directory browsing.
  - **Generated-view banners** -- views carry a generated footer; tests asserting their absence retire.
  - **Manual-edit workflows** -- tests that hand-edit structured md and expect the tool to honour it convert to mutation-based equivalents or retire (authored-once, D02).
  - **Corrected** (proposed by ic, hv-ratified at the bounce 2026-08-14) -- a v2 behaviour that is simply wrong and is fixed rather than faithfully reproduced. Distinct from **deviate**: deviate is a design consequence of v3; corrected is a bug fix. Both carry a ratification reference per register row. Known members from ic's census, one per line so each can carry the machine-read `covers:` clause that binds it to the units it ratifies (format adopted by vc 2026-08-15 on ic's proposal: spaced `--` separator, comma-separated ids, nothing fused; checked by `parity/tools/corrected_check.sh`):
    - unknown flags accepted silently with exit 0 (`info`/`config`/`version` -- clap exits 2, so v3 diverges on day one whether or not anyone decides to) -- covers: INV-08, info, version
    - `--help` reporting failure on 10 of 27 commands -- covers: INV-07, st, wp, ac, at, todo, fileindex
    - the stderr/stdout misroute census (45 stderr-only / 12 stdout-only / 2 both on failing invocations -- larger than the three sites in cc's hv queue) -- covers: INV-06
  - **Undefined** (vc ruling 2026-08-14, **hv-RATIFIED 2026-08-16** -- opened on `intent config`, its first and only member) -- v2 exhibits NO behaviour to be faithful to (0 bytes on both streams, exit 0), so v3 is DESIGNING rather than porting or correcting. **Distinct from Corrected, which needs a v2 antecedent to correct: silence is not an antecedent.** Folding it into `corrected` would hide a design decision inside a bug-fix class, and design decisions need a different reviewer. **The class is grounded rather than notional: `class_vocab_check.sh` reads this list and is green, and `intent config` is `disposition: pending`, so `undefined` is the only marker distinguishing "the blank is because v2 was SILENT" from "the blank is because nobody looked yet."**
- **Explicitly out of parity scope**: `bin/release` and the test harness itself (repo dev tooling, not shipped surface); and **`new-surface` entries -- a v3 command with no v2 antecedent has nothing to be faithful to, so this contract is silent about it by construction and its coverage is governed by WP-06 rather than here** (vc 2026-08-15, provisional pending hv, on ic's finding that 18 rows carried the state and no decision recorded it).

## Parity properties (beyond output equality)

Output-equality across implementations cannot catch v3 faithfully reproducing a v2 bug -- the two would agree and the suite would go green. Properties asserted directly, per scoped verb:

- **Scope-honouring (issue 0024):** an instrument that accepts a narrowing argument answers the narrowed question, and its output names the resolved scope. Found in v2: `at lint <ID>/NN` and `ac gate <ID>/NN` silently dropped the WP scope, and a scoped `--fix` rewrote rows OUTSIDE the scope; an equality-only suite would have carried that into v3 as certified behaviour.

### THE CONTRACT'S HEADLINE CARRIES A SILENT QUANTIFIER, AND IT SHOULD BE STATED

**"v3 is green when v2's own suite cannot tell the difference" means: cannot tell the difference IN THE WAYS IT LOOKS.** A byte no v2 test asserts is a byte where v2 and v3 may diverge freely while the harness reports parity, because the harness's subject is the bytes it asserts and its report's subject is the command's output. Same class as everything else here, sitting on the contract's central sentence.

**Measured 2026-08-17 (vc) on the one live instance, and all four mechanisms missed it.** `intent wp show` printed `status: wip` where every other surface prints `WIP` -- a real v2 parity break, since v2 implements `wp show` by catting `info.md` and `views.rs` writes that line with `display()`.

| mechanism                   | why it was silent                                                                                       |
| --------------------------- | ------------------------------------------------------------------------------------------------------- |
| the dispatch row's note     | said the output was unchanged in kind -- TRUE, and about the binary's intent rather than its bytes      |
| the row's `observed.stdout` | reads "the info.md contents": true, and one of the 50 of 62 `as-observed` rows too prose to assert (ic) |
| **the v2 BATS estate**      | `tests/unit/wp_commands.bats:565` asserts the WP's TITLE and OBJECTIVE and never the status line        |
| the v3 test                 | asserted `status: wip` -- it had pinned the divergence as expected output                               |

**So the byte was outside every instrument the contract relies on, and was found only because cc's witness reads the state back FROM THE TOOL rather than writing it as a literal**, which put `st show` saying `WIP` and `wp show` saying `wip` in one run where the difference was visible at all. **The rule generalises and is adopted: never assert a state word as a literal; ask the tool what the state is and require the output to name that** (cc, via ic, 2026-08-17). Its limit is worth stating in the same breath -- **read-back is a CONSISTENCY check, so it catches two surfaces of v3 disagreeing and cannot catch v3 agreeing with itself and diverging from v2 uniformly.** For that, only a byte a v2 test actually asserts will do, which is why the table above is a coverage question and not a style one.

**SO THE COVERAGE QUESTION WAS MEASURED, AND THE ANSWER IS THAT THE COARSE HOLE IS SMALL AND THE FINE HOLE IS UNMEASURABLE** (vc, 2026-08-17, over 1402 `@test` blocks and all 62 rows; needle calibrated per invocation spelling after the first cut manufactured two false zeros). Asking only "does any v2 test assert this command's stdout at all":

| rows | state                                                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------ |
| 60   | invoked with at least one output assertion                                                                   |
| 1    | `agents sync` -- invoked by 11 blocks, **stdout explicitly discarded** (`run_intent agents sync >/dev/null`) |
| 1    | `agents template` -- no block invokes it at all                                                              |

**Two rows of 62 have no v2 assertion of their stdout, and `agents sync` is the more interesting of the pair**: its file carries 21 `assert_file_contains` and 16 `assert_output_contains`, so it is a well-tested command whose tests deliberately throw away the stream this contract certifies. **Well-covered and stdout-uncovered are different things, and only the first is visible from a burn ratio or a coverage map.**

**But the 60 are the finding, not the 2.** `wp show` sits among them with two output assertions -- title and objective -- and neither touches the status line that diverged. **A proxy this coarse over-reports by construction, so 60 is a floor and not an answer: it says these rows assert SOMETHING, and nothing at all about whether they assert the bytes their row describes.** That question cannot be answered by grep, which means **the contract's certification currently rests on 60 unrecorded human judgements, and the only one anybody has checked was wrong.** Bounding that is worth more than any further mechanical sweep of the same kind.

### AND THE `observed` COLUMN ITSELF IS WRONG AT 4 OF 8 CHECKED -- ONE OF THEM WOULD HAVE CERTIFIED A LIVE DEFECT AS PARITY

**ic, 2026-08-17 (`b71c0c24`), going to make the 12 literal rows machine-checked and measuring v2 first because the field is only worth having if it is right.** This supersedes the coverage question above in priority, and it is the more dangerous half.

| row            | the register CLAIMED v2 prints | v2 MEASURED                               |
| -------------- | ------------------------------ | ----------------------------------------- |
| `ac satisfy`   | `ok: <AC> satisfied`           | `ok: AC-01.1 satisfied by evidence`       |
| `ac rescope`   | `ok: <AC> back in scope`       | `ok: AC-01.1 back in scope (unsatisfied)` |
| `ac reinstate` | `ok: <AC> back in scope`       | `ok: AC-01.2 back in scope (unsatisfied)` |
| **`at na`**    | **`ok: <AT> -> n-a`**          | **`ok: AT-01.2 -> n/a`**                  |

Three are transcription drift, and **the dropped suffix is the informative half** -- `back in scope` alone does not say whether satisfaction survived the round trip, which is the whole question a reinstate raises.

**THE FOURTH IS THE ONE, AND IT IS VERIFIED INDEPENDENTLY** (vc, at `b71c0c24^`): the row carried **`ok: <AT> -> n-a`** -- **v3's token, in the column that records v2** -- on a row whose `target.state` is `as-observed`. **So a check comparing v3's output against this column would have found agreement and certified issue 0056 as correct parity.** The contamination was in place before anybody noticed the defect. **The defect reached the register that exists to catch it, and the register would then have vouched for it.**

**An uncovered row is UNKNOWN; a wrongly-recorded row is CONFIDENTLY WRONG IN v3's FAVOUR**, and the second is worse because it is active rather than passive. Every `as-observed` row delegates its correctness to that column, **nothing anywhere compares the column to v2**, and **a wrong entry does not fail -- it silently redefines the target.** At a measured 4 in 8 on the only subset anyone has checked, this is not a tail risk.

**AND THE STRUCTURAL CONCLUSION IS THE DEEPEST THING ON THIS THREAD, SO IT IS RECORDED AS THE REASON RATHER THAN THE TIDINESS ARGUMENT** (ic): the case for moving these values into a field a test imports was one-requirement-one-statement. **The better case is that the field forces the value to be MEASURED, and measuring is what found these four. Prose drifts because nothing ever reads it back; a field a test imports gets executed.** Generalised across the whole day: **every defect found on this thread was in something nothing read back** -- a doc comment, a note, a prose column, a restore that produced no output, a seal nobody parsed. Execution is not a nicer form of documentation; it is the only form that can be wrong out loud.

## The keep/retire/deviate register

One row per BATS test file (finer-grained per-test rows where a file mixes classes), maintained from WP-05 and complete at WP-06 close:

```
| tests/<file>.bats | <tests> | <burn> | keep · retire · deviate | <deviation class> | <ratification ref> | <exposure> | <notes> |
```

The `<command(s)>` column this schema previously specified is **struck, not owed** (vc, 2026-08-15, on ic's ask). The register is file-level; the file-to-command mapping lives in `coverage_map.sh`, where it is **measured** rather than transcribed. A transcribed copy of a measured mapping is a divergent copy that drifts from the thing it copies, and the register is the last artefact that should carry one. ic was right to raise it rather than quietly diverge a second time -- a contract document naming a column nobody implements is the same defect as an implementation nobody contracted.

- **keep** -- asserts command surface; runs unmodified against `INTENT_BIN`.
- **retire** -- asserts the old layout or a v2 implementation detail; retired with a named class from the list above.
- **deviate** -- asserts surface we are deliberately changing; each carries a D-number ratified in design.md before the port lands.

The register is the honest ledger of everything the rewrite changes. An unclassified failing test is a defect, not a candidate for reclassification-in-the-moment.

## Command-surface inventory (command/subcommand level)

Source of truth for the deep pass: `bin/intent` dispatch + per-binary help. This table is the WP-01 command-level inventory; the flag-level pass is the IC handoff below.

| Command      | Subcommands (v2.19.0)                                                                                                                          | v2 source            | v3 disposition                                                                                                       |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | -------------------------------------------------------------------------------------------------------------------- |
| st           | new [-s], start, done, cancel, list [--status --width], sync [--write], organize [--write], show, edit, repair [--write]                       | intent_st            | facade family (WP-04)                                                                                                |
| wp           | new, start, done, list, show                                                                                                                   | intent_wp            | facade family (WP-04)                                                                                                |
| ac           | list, status, satisfy --evidence, gate, descope --to, rescope, withdraw --reason, reinstate                                                    | intent_acceptance    | facade family (WP-04)                                                                                                |
| at           | lint [--fix], set, list (verify exact set in deep pass)                                                                                        | intent_acceptance    | facade family (WP-04)                                                                                                |
| issues       | list [--kind], add [--severity] (alias new), show [--json], close, open                                                                        | intent_issues        | WP-06; directory deviation applies                                                                                   |
| todo         | (view), update, done [--flush --prune], notdone, toggle, --json                                                                                | intent_todo          | WP-06 (generated view + queries)                                                                                     |
| info         | (status display)                                                                                                                               | intent_info          | WP-06                                                                                                                |
| init         | [--lang]                                                                                                                                       | intent_init          | WP-06 (embedded templates, WP-07)                                                                                    |
| bootstrap    | (first-time setup)                                                                                                                             | intent_bootstrap     | WP-06; INTENT_HOME demotion (WP-11)                                                                                  |
| config       | (display)                                                                                                                                      | intent_config        | WP-06                                                                                                                |
| doctor       | (checks 1..4e + fixes)                                                                                                                         | intent_doctor        | WP-06 (integrity queries + skew/unparsed)                                                                            |
| upgrade      | (v2 ledger)                                                                                                                                    | intent_upgrade       | REPLACED by the v3 migrator (WP-10); v2 ledger never reimplemented                                                   |
| agents       | sync                                                                                                                                           | plugin (agents)      | WP-07                                                                                                                |
| claude       | subagents, skills (install/sync/uninstall/list), rules (list/show [--lang]), hook <name>, upgrade, prime, ws (new/list/archive/hygiene), start | intent (claude arms) | WP-07; `hook` byte-compatible day one                                                                                |
| critic       | <lang> (headless runner)                                                                                                                       | intent_critic        | WP-07 (embedded rules, strict-proxy)                                                                                 |
| lang         | init, remove, list                                                                                                                             | intent_lang          | WP-07                                                                                                                |
| llm          | usage_rules etc                                                                                                                                | intent_llm           | WP-06 + regenerated guide (WP-09)                                                                                    |
| learn        | (capture learnings)                                                                                                                            | intent_learn         | WP-06                                                                                                                |
| modules      | (registry guardrails)                                                                                                                          | intent_modules       | WP-06                                                                                                                |
| organize     | [--write]                                                                                                                                      | intent_organize      | planned retire -- vestigial by construction (hv 2026-08-14; note below)                                              |
| plugin       | (discovery)                                                                                                                                    | intent_plugin        | WP-06                                                                                                                |
| ext          | (user extensions)                                                                                                                              | intent_ext           | WP-06                                                                                                                |
| treeindex    | <dir>                                                                                                                                          | intent_treeindex     | WP-06 (content semantics unchanged; location decision deferred, D21)                                                 |
| fileindex    | (checkbox file indexes)                                                                                                                        | intent_fileindex     | WP-06                                                                                                                |
| help         | [command]                                                                                                                                      | intent_help          | WP-05 (generated from the dispatch table)                                                                            |
| st_zero      | (STZero retrofit)                                                                                                                              | intent_st_zero       | WP-06 tail; candidate for a ratified retire-deviation if unused by the fleet -- decide at port time, in the register |
| -- new in v3 | schema, search, export --format, ingest --from-md, daemon (start/stop/status/run), mcp                                                         | --                   | additive; no parity obligation                                                                                       |

**organize (both faces) is planned vestigial (hv, 2026-08-14).** A strictly structured model cannot, by construction, hold data in the wrong spot or the wrong format, so the disorder `organize` exists to repair cannot arise. Both `intent organize` and `intent st organize` are planned retires -- the pre-existing two-implementation Highlander dissolves with them -- confirmed finally at the surface cut (WP-05/06). Register rows: retire, citing this ruling.

## The IC handoff (deep pass)

Two deliverables, design-neutral, v2-side, feeding WP-05:

1. **Flag-level inventory**: for every row above, the full flag/argument grammar, stdout/stderr shapes, and exit codes -- from `bin/` source + help text, recorded per command in a `parity/` appendix. Verify the `at` subcommand set and every alias (`issues new`, `organise`).
2. **Harness retarget + classification**: make the BATS estate run against `INTENT_BIN` (audit how tests resolve `intent` today -- PATH vs relative -- and thread the override through the helpers); then produce the first-pass register with every test file classified keep/retire/deviate against the classes above. Flag any test that fits no class -- that is a contract gap for vc, not a judgement call for the classifier.

Working rules: sacrificial worktree for anything touching `bin/**` or helpers; mutation-check any harness change (a retargeted suite that cannot fail proves nothing -- point `INTENT_BIN` at `/usr/bin/false` and watch it burn before trusting a green). A mutation canary must come from the same fixture and branch the test drives -- **applied is not reached** (cc, 2026-08-14: a substitution can apply on a branch the test never walks and read as "does not discriminate").

## Measurement rules

Earned on this thread, 2026-08-14, each by a failure rather than by foresight. They belong here rather than on a node's board because they govern how the parity evidence is produced, and the boards do not outlive the sessions that write them.

### THE SHAPE THE REST ARE INSTANCES OF: the measurement's subject and the report's subject are different, and the output cannot tell them apart

**Consolidated 2026-08-17 (vc), after TEN variants were observed in one day across four nodes.** (Nine at consolidation; ic's `inventory` arrived the same afternoon, and dc's at-rest form of `instant` with it. **The count is dated on purpose** -- an undated count in prose is the staling this document keeps filing, and this one has moved twice in a day.) Each was previously read as its own mistake. They are one: **an instrument measured something, the report named something else, and nothing in the output distinguishes the two.** The failure is never in the measuring step and always in the reporting step, which is why care at the point of measurement does not prevent it.

**The last row is the one this estate is structurally most exposed to, and it is ic's** (2026-08-17, naming it in their own work after the third instance): **a surface is all declarations**, so any question asked of the command surface can be answered from what is declared while the report speaks about what runs. Four of the eight instances below were about the surface for exactly that reason.

| the subject that was wrong | observed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **tree**                   | issue 0049 -- a leg's verdict outliving the tree it compiled; vc citing `render.rs` lines that exist only in cc's uncommitted worktree; ic's retraction at `8963e5dd`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **process**                | a pipeline's exit code names the PIPELINE -- cc read `0` from a `grep` while `mutation_completeness` was red; and the `cmd \| head; echo $?` rule below, three times in one session                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **corpus**                 | cc's first multi-AT measurement ran over v3 canon and returned an EMPTY set -- **there are no `thread.json` files in this estate at all.** "Zero rows" and "zero rows matching" are the same output                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **layer**                  | vc: "no reported-success-with-no-effect path opens" -- true of the FACADE, where it was checked, and said of the system; issue 0050 is that path opening nineteen times at the surface. Issue 0052 is the same split in reverse. **And the only instance to produce a LIVE false verdict rather than a wrong sentence: AT-00.5 released to `green` on a fix verified in v3's `contract.rs`, while the gate that scores this contract is v2's, still carrying the early return 0032 was filed against** -- 45/112 became 46/112 with AC-00.7 falsely satisfied, by the node holding the line against exactly that                                                                                                                                                                                                                      |
| **population**             | a `tail -40` showing 20 of 68 result lines and nearly certifying a workspace green off a third of the evidence; an AT parse matching 93 of 112 because `(non-test)` rows carry no backticked file; `SURFACE_NONRETURNING` short by one member                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **spelling**               | a jq querying `.kind` where the field is `.type` -- zero rows, one step from filing that the surface's third level is undeclared; a zsh `"$p[]"` read as an array subscript, emptying every comparison while the script printed `all four correspond`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **declaration**            | **the subject was what is DECLARED and the report named what the code DOES.** ic three times in one day -- `value` vs `values`, then `values` asserted to build the clap enum (the spine reads it only for subcommand slots), then `wp new --help` showing no flags read as no value supplied. vc once: a grep of `^WT=\|REV=` saw three assignment lines and inferred which tree `gen_pertest` stamps, where the `cd "$WT"` is its own statement seven lines up -- one step from a false accusation against a peer's file                                                                                                                                                                                                                                                                                                            |
| **instant**                | cc, 2026-08-17, in the witness for 0050: `&thread_state(root)` is evaluated as an ARGUMENT, ie BEFORE the verb runs, so every case compared the no-op line against the state the entity was in beforehand. Eight of nine failed -- and **a green would have meant the verb had not moved anything**. **dc adds the at-rest form, and it is the same defect introduced by CARRYING rather than by reading**: a devbin baseline of "530 green" taken mid-work and reported of the branch, across the commit that invalidated it -- _"it was true of something and I reported it of the branch"_. A number with no revision attached cannot be wrong when taken and cannot be right afterwards                                                                                                                                           |
| **inventory**              | **the instrument is RIGHT and the population changed underneath it** (ic, 2026-08-17, on issue 0058). `agents template list` enumerates directories, which was a correct answer to "what templates exist" while every directory was a template; ST0035 put the canon packs in the same namespace, so `list` now advertises eight and `show` serves one -- `show rust` says "not found" about a name `list` printed a command earlier. **Nothing re-read the enumerator because nothing had broken.** The distinct arrival: in the nine above, the subject drifted from the report; here the subject held and **the world under the subject moved**, so there is no wrong step to find in the instrument at all -- and a list that reads as an inventory of what its sibling can do is the shape most likely to be trusted while stale |

**Two properties make the class expensive.** Every one of these returns a well-formed result rather than an error, so nothing prompts a second look. And a wrong subject usually makes the finding STRONGER -- an empty corpus reads as "no violations", a truncated population reads as "all green", a missing `cd` reads as a defect found -- which is the asymmetry rule below, arriving one level up.

**AND THE THIRD PROPERTY IS THE ONE THAT SHOULD CHANGE HOW THE WORK IS DONE, NOT ONLY HOW IT IS READ** (dc, 2026-08-17, on the whole set): **not one of the nine was found by reading.** Every single one required a mutant, a witness that drove the moving case, or a peer measuring the same thing independently and arriving at a different number. **That is a fact about the class rather than about any of the four of us** -- a wrong subject produces a well-formed result, so inspection has nothing to catch on, and the reviewer's care is spent on output that is already internally consistent. **Review does not detect this class. Only a second, differently-constructed measurement does** -- which is why the expensive-looking habits here (mutants, canaries on green arms, two nodes measuring one question) are the cheap option, and why "someone should have noticed" is never the right retrospective.

**Read that as narrowly as it is written, which is dc's guard on it and the right one.** It says review cannot detect THIS class. It does not say review is cheap to drop -- review catches a great deal that no second measurement would, and nothing here licenses skipping it. **The habits above look expensive only because their cost is visible while the alternative's cost is a green nobody revisits, and an invisible cost is not a smaller one.**

**A SCOPE THAT IS NOT STATED IS A SCOPE NOBODY CAN DISAGREE WITH** (vc, 2026-08-17, on dc's ask, from the case that proves it). `int measured` prints its in-scope count and its prune list on every run; I counted the same window by hand, got 17 against its 11, and raised it -- **and the difference was entirely `intent/whiteboard/**` and `.history`, both of which dc's tool names as pruned.** No defect either side. **The disagreement was possible only because one of the two scopes was stated**, and that is the whole argument: an instrument that declares its population can be argued with by anyone who reads the declaration, and one that does not is unfalsifiable in exactly the dimension this class attacks. State the population, not just the result -- **and a reader disagreeing with your stated scope is the control working, not a complaint to answer.**

**THE NINTH INSTANCE BREAKS THAT ASYMMETRY, AND THE EXCEPTION IS WORTH MORE THAN THE RULE.** cc's mis-wired reader did not make a finding stronger -- it turned eight of nine cases RED, which is why it was found in minutes rather than shipped. The reason is specific to the time axis and it generalises: **reading too early returns the pre-state, and the pre-state equals the post-state exactly on the cases where nothing moved.** So a wrong-instant error does not weaken an instrument evenly -- it converts a movement-detector into a **null-detector**, loud on every verb that moves and silent on every verb that does not.

### hv STANDING RULING, 2026-08-17: AN hv RULING IS COMMITTED WHEN IT IS GIVEN

**hv, asked whether the strongest authority needs the weakest evidence: _"yes, commit hv rulings when I give them"._** So a ratification citing `hv` carries a sha like any other, and `hv <date>` with no artefact stops being a permitted third form for anything ruled from here.

**The mechanic costs hv nothing, which is why it will hold: the NODE THAT RECEIVES the ruling commits it.** hv rules in conversation; the receiving node writes it to the artefact it governs and pushes, and that commit is the record. Nobody has to remember to do a second thing -- the ruling was going to be applied anyway, and the only change is that the application is cited.

**The three existing rows were RECOVERED, not grandfathered and not fabricated** (vc, 2026-08-17). Two of them named their commit already, in the `ratification` prose rather than the `ratified_in` field: `st new` and `st cancel` both said _"cc landed it at `2aec5f6`"_. `st bootstrap` did not, and `c1cca8c4` is the commit that applied the `st_zero` -> `bootstrap` rename. **The evidence existed and was in the wrong field**, which is the cheap case and worth distinguishing loudly from the expensive one: ic's refusal to invent a record for these rows was right, and it is what kept them recoverable -- **a plausible citation written last week would have been indistinguishable from a real one today.** After the recovery `ratified_in_check.sh` reports 15 conforming, 0 hv-unverifiable, and an empty worklist.

### AND A THIRD AXIS, 2026-08-17 (dc, on issue 0049): A REMEDY WHOSE SUBJECT IS NOT THE REPORT'S

**Every variant above concerns a MEASUREMENT whose subject differs from the report's. This one is a FIX whose subject differs from the report's, and it is harder to see.**

vc re-scoped 0049 to have `run_gate` echo an opaque referent beside `verdict:`. dc refused the scope and was right: **hv read `FAILED: rust` in a SUMMARY**, which is `run_all` (`lib/resolve:474`), and `lib/runlog:799` already says out loud that across a ten-option `all` "the per-gate `verdict:` lines have scrolled past". **A referent placed only beside `verdict:` would have been built, correct, tested green, and absent from the exact screen the issue was filed from.**

**A wrong-site fix is worse than an incomplete one, because it closes.** An incomplete fix leaves the issue open and the row keeps asking. A fix at the wrong site produces a diff, a passing test, and a close -- and the defect survives with its ticket marked done, which is the state no sweep can recover.

**The detection question is the sibling class's, pointed at the remedy: what OTHER report would this fix also have closed?** If the answer includes reports the fix does not actually address, the fix is being scoped by the diagnosis rather than by the observation. **Go back to the screen the reporter was looking at.** dc's own complementarity finding is the same discipline applied twice: `print_run_verdict` runs after the only `tee` closes (`runlog:1039` / `:1085`), so its output is terminal-only and the `--exec` log hook is not redundant with it -- **and that is knowable only by checking where the tee closes, not by reasoning about which one seems sufficient.**

### THE SIBLING CLASS, RULED 2026-08-17 (vc) ON ic's ASK: A TRUE ANSWER THAT HAS STOPPED DISCRIMINATING

**ic proposed a row for it and was right that it is not one** -- so it gets a class instead. The ten above are all one shape: the output says something FALSE, because the report named a different subject than the measurement. **These are the opposite: the output is TRUE, the instrument is correct, and what has been lost is its power to tell two states apart.**

Three members, and they were filed separately before the shape was visible:

- **0057** -- a zero-byte `.errors` seal from a zero-file scan is byte-identical to one from a full clean scan. Both readings are true. Neither is informative.
- **0059** -- an instrument nothing invokes. "Never ran" and "ran green" are the same absence, and no artefact carries the difference.
- **A CLOSE CONDITION THE ARCHITECTURE FORBIDS** (dc, 2026-08-17, on issue 0049 -- **and it corrects a verdict of mine given hours earlier**). I reported 0049 not-done because `print_run_verdict` carries no `measured:` line. It cannot get one: it lives in `lib/runlog`, and **devbin has no git and must not acquire it**, which is the layer ruling that sent `int measured` project-side. So the issue's stated close condition is unsatisfiable by design -- **and an issue that cannot close reads exactly like an issue nobody has got to.** "Not done" and "not doable as written" produce the same observable, which is an open row, and the sweep that reported it had no way to tell them apart.
- **A GUARD THAT ASSERTS THE RENDERER AGREES WITH THE FUNCTION THAT DEFINES THE RENDERER** (vc, 2026-08-17, closing issue 0056). `view_determinism.rs::the_view_writes_every_at_status_in_the_authored_spelling` asserts `view.contains(&format!("status: {}", status.display()))`. **`display()` is what the renderer calls**, so the test compares the output to its own source of truth: change `display()` to return `n@a` and the assertion still passes. Its only external anchor is a hardcoded `!contains("n-a")`, which pins one wrong value rather than the right one. **It is a real improvement over what preceded it (nothing) and it is narrower than its name.** What closes it is what 0056 asked for and what was run by hand instead: byte-equality against a fixture written in v2's spelling, ie a corpus v3 did not produce.

- **ic's sentinel, 2026-08-17** -- rewriting a `parity.md` sentinel in `ratified_in` is **provably inert today**, mutation-verified byte-identical against `corrected_check.sh`. **Its entire effect is on which alarm fires after an unrelated later edit**: the day `parity.md` stops citing that unit, a rewritten stamp reads as ELSEWHERE ("ratified somewhere else, not our business") instead of UNCITED ("asserts a ratification the ratifying document does not make") -- and UNCITED is the alarm.

**AND THE REMEDY THAT ACTUALLY CLOSES A MEMBER OF THIS CLASS IS TWO ENUMERATORS THAT FAIL IN OPPOSITE DIRECTIONS** (cc, 2026-08-17, moving `Thread.acceptance` off `Unbuilt`). The move took the row out of four populations and the suite reported **20 passed, 0 failed** -- three checks reached their population through their own `let Disposition::Unbuilt { .. } = .. else { continue }`, and the one count guard that could have noticed asserted `unbuilt_fields > 0`, **which survives 4 becoming 3.** A threshold guard over a shrinking population is this class by construction: it is true for every value that matters and for most values that do not.

cc's replacement had the same defect one turn later -- `cited > 0`, load-bearing only because there is exactly ONE `Attribute` row today, and dead the moment there are two. **The version that holds is an EQUALITY between two enumerators built to fail in opposite directions**: `edgeless()` matches each variant by name and therefore OMITS a new one until someone adds an arm; the check counts `!State` and therefore INCLUDES a new one immediately. **They agree exactly when the population function is complete, and a new variant breaks the equality from whichever side the author forgot.**

**The general form, with cc's own correction to vc's first statement of it, which was satisfiable by two copies of one blind spot.** "Independently derived" is NOT enough: two enumerators that both fail by OMISSION agree perfectly while both going short, the equality holds, and the population is quietly missing a member. **The pair must fail in opposite directions -- one enumerates the KNOWN, the other enumerates the COMPLEMENT** -- so that anything neither author anticipated breaks the equality from whichever side was forgotten. Stated whole:

> **A threshold (`> 0`, `>= n`) records that someone once counted. An equality between two enumerations that DISAGREE about anything neither author anticipated records that they still agree.** Only the second survives the population moving, which is the event this class is about.

**The detection question is different, and that is the whole reason this is a separate class.** For the ten, you ask _"is this instrument measuring what the report names?"_ -- and it is not, so a second measurement finds it. **Here that question returns a clean yes.** The question that works is: **"what OTHER state would produce this same output?"** If the answer is "a state I would want to know about", the instrument has stopped discriminating whether or not it has stopped being correct.

**ic's member is the worst of the three and establishes the time axis.** For 0057 and 0059 the discrimination is already gone, so asking the question today finds them. For the sentinel, asking today gives the right answer and the loss lands later, on an edit that has nothing to do with it and no reason to look. **A defect whose only symptom is in a future diagnosis cannot be found by diagnosing the present**, which is why it wants a rule -- never rewrite a stamp an alarm reads, even one you have proved inert -- rather than a check.

**How it was found is the argument for the posture, not the rule.** ic had warned that rewriting a sentinel would break `corrected_check.sh`, reasoning from the field access. A mutation disproved it: the field is consulted only to split rows CLAIMED but NOT CITED, and a cited row never reaches the split. **The warning was wrong, the mutation corrected it, and the corrected version is a better finding than the original** -- in a tool whose subject is claims nothing reads back, which is where an unrun claim would have been the joke writing itself.

**AND A REFUSAL AND A SILENCE LOOK THE SAME TO ANYONE NOT LOOKING** (ic, 2026-08-17, naming it from their own incident; filed as issue **0059**). This is the class at the RUNNER, one level above every row in the table: it does not matter how well an instrument reports its subject if nothing executes it. `corrected_check.sh` refused at exit 2 -- correctly, loudly, exactly as designed, on its own inability to measure -- **for all twenty corrected rows, across a fold, a compact, and a board reading "nothing blocked".** Nothing ran it, so the refusal and a clean pass produced the same observable: nothing.

**Measured across the suite, the exposure is not one instrument.** Of the eleven `_check.sh` tools in `parity/tools/`, **three have an execution site and eight have none** -- and **all three that `AT-05.5` cites as closing it are in the dark set**, so a green acceptance row rests on instruments that run only when a node types their name. **The sibling at the artefact is 0057**: a zero-byte seal cannot distinguish a scan of 1584 files from a scan of none. Same class, two ends -- there the verdict cannot carry its population, here there is no verdict to carry one.

**The rule that follows is the cheapest thing in this document: an instrument's REGISTRATION and its INVOCATION are different facts, and nothing in this repo joins them.** `MODULES.md` records that a tool exists; `precommit` records that a tool runs; a tool can be registered, documented, mutation-tested, and cited by an acceptance row while never being invoked, **with every individual record correct.** Cite an instrument in an AT note only alongside what runs it, and treat "the check exists" as saying nothing whatever about whether it has ever run.

**Which is the worst possible failure for that file specifically, because no-ops are its entire subject.** The eight failures came from the half of the fixture that is not the point: `twice()` drives each verb once when it still has somewhere to go. Built the narrower obvious way -- fixture each entity already in its target state, drive once, assert the no-op line -- the same defect would have been **green throughout, proved nothing, and been the most convincing artefact in the commit.** The noise that exposed it was a by-product of testing idempotence rather than the no-op alone.

**So: drive the moving case even when the property under test is the non-moving one.** The moving case is the only thing that can demonstrate the instrument reads the right instant, because it is the only case where the two instants differ. That is the calibration rule below arriving one level up -- **the movement IS the known-good non-zero**, and an instrument that has only ever been shown a null cannot be distinguished from one that reports null unconditionally.

**AND THE FIXTURE SIDE OF THE SAME RULE, WHICH IS ic's AND COMPLETES IT** (2026-08-17, in `literal_stdout_parity.rs`, naming the link to cc's case unprompted). The first draft seeded `AT-01.2` at `n-a` -- **the state the `at na` declaration targets** -- so the template measured a SELF-LOOP: v3 answered `ok: AT-01.2 already n-a` and the run reported a parity break whose real cause was the fixture. **A template describes a MOVEMENT, so the fixture must leave the verb somewhere to go.**

**Three instances now, and together they close the axis from both ends.** cc's reads the state at the wrong instant; ic's puts the entity where the two instants coincide. **Neither instrument was wrong about time in a way anyone could see -- both turned a movement-measurement into a null-measurement, and a null-measurement of a movement always agrees with itself.** So the rule has a fixture half and an instrument half, and they are the same rule: **arrange for the two instants to DIFFER, then read them.** Where cc's failure was loud (eight of nine red) ic's was quiet in the dangerous direction -- **it manufactured a parity break against v3 that did not exist**, which would have been filed as a defect in the binary rather than in the fixture.

**THE CLASS SURVIVES A HANDOFF, AND A HANDOFF STRIPS EXACTLY THE CONTEXT THAT WOULD REVEAL IT** (ic, 2026-08-17, issue 0053 -- the most expensive instance so far). cc sent ic 21 measured `no_op` values for transcription into the register. **Two were wrong**: `ac rescope` and `ac reinstate` refuse the self-loop at exit 1, because cc reasoned from `AcState::entry(kind)` -- correctly, about the value the SUCCESS arm hands the setter -- on a path the self-loop never reaches. Well-formed reasoning, wrong subject, and it would have entered the register as two clean `ok:` lines had ic transcribed rather than re-driven.

**A VALUE CANNOT CARRY ITS OWN SUBJECT, WHICH MAKES A HANDOFF THE MAXIMAL CASE OF THIS CLASS.** Within one node the derivation is still in reach and can be re-examined; across a boundary only the result crosses, and the receiver has nothing to check it against but plausibility -- against which a wrong-subject result is indistinguishable from a right one BY CONSTRUCTION, because the reasoning that produced it was sound. **So the operational form -- record what the measurement was ABOUT, in the artefact -- is not merely good hygiene at a handoff; it is the only thing that makes the handoff checkable at all.** The receiving node's correct move is the one ic made: re-drive rather than transcribe, and treat a peer's measured value as a claim to reproduce rather than a fact to copy.

**AND THE TOOLING BUILT TO CATCH THE SIX HAD THE SEVENTH** (ic, 2026-08-17, in the mutation harness proving the `self_loop` binding). Its restore ran on relative paths while a helper had changed directory, so the trap meant to undo each mutant never fired and the mutations sat on disk unnoticed. **"A guard that does not run looks exactly like a guard that found nothing."** That is the whole class in one sentence, and the null result is the dangerous half: a harness reporting no findings and a harness that never executed are the same output, so the arm most worth canarying is the one that is supposed to stay silent. Recorded in the file rather than fixed quietly, which is what makes it worth more than the fix.

**The lesson is NOT "use absolute paths"** (ic's own correction of it). It is that **a cleanup step produces no output when it works, so success and never-having-run are byte-identical at the observation point.** That is what makes it a member of this class rather than a scripting slip: the restore's report and the restore's subject came apart, and there was nothing available to read that would tell them apart.

**AND THIS DOCUMENT IS ITSELF A HAZARD TO THE INSTRUMENTS IT TEACHES YOU TO BUILD.** vc, 2026-08-17: a sweep for stamped artefacts returned a third revision, unbackticked and therefore invisible to `provenance_check.sh`'s parser -- a finding, until it turned out to be `gen_register.sh:34`, a header comment narrating that very incident. **The artefact recording the lesson became a false instance of the lesson.** The hazard is specific and it grows: **the more carefully this canon quotes the strings it is about, the more of them it contains for the next grep to find.** Same shape as counting the `rusqlite` doc comment -- which asserts the thin-skin invariant -- as a violation of it. When sweeping the estate for a pattern, the documents that discuss the pattern are the first place it will falsely appear, and they are also the documents most likely to be read as authoritative.

**AND THE BAIT CAN BE IN THE DATA FORMAT ITSELF, WHICH IS WHY TWO NODES HIT IT INDEPENDENTLY** (dc + vc, 2026-08-17, on the same file). Both verified the devbin manifest with `while read -r sha path`, and both got **all 27 files diverged** -- one step from filing "the entire vendored tree has been modified". **zsh ties `path` to `PATH`**, so iteration one overwrites the whole `PATH` with a filename and every external binary vanishes; `cdpath`, `fpath` and `manpath` are tied the same way, and the corruption outlives the loop. Reproduced both directions.

**This is not coincidence, and that is the finding.** `read -r sha path` is the obvious spelling for a line of a `.sha256` manifest, so **the bait is in the data format**: anyone who writes this loop writes this bug, and it fails in the direction that manufactures a maximal false alarm rather than a silence.

**The more useful half is the diagnosis, which vc got wrong and dc corrected.** vc recorded the cause as "`awk` was unresolvable inside the loop's subshell" -- an explanation that fits every observation and names no mechanism, so it generalises to nothing and the next loop is written identically. **A diagnosis that accounts for the symptom without naming the mechanism is a story, and it passes review exactly as well as an explanation does**, which makes it more dangerous than being visibly stumped. The recorded cause is the one-liner: never name a loop variable `path`, `cdpath`, `fpath` or `manpath` in zsh.

**Two tells, and they fail in different directions, so keep both.** dc's: the matched count was zero. vc's: the measurement field printed blank while the verdict field printed a finding. **vc's is one step earlier and needs no second run; dc's needs no foreknowledge of the field layout, so it survives being applied to an instrument you did not write.**

**THE REMEDY FOR ALL OF THESE IS ONE SENTENCE AND IT IS A CONSTRUCTION, NOT ADVICE: SCOPE GOES IN A DENOMINATOR, NEVER IN AN ADJECTIVE** (vc + cc, 2026-08-17, after three instances in one day by three different authors).

**Three counts, three authors, every sentence true, every one wrong about its scope:**

| published                              | actual                         | what went wrong                                                 |
| -------------------------------------- | ------------------------------ | --------------------------------------------------------------- |
| "12 of 14 rows lose their note"        | 14 of 14 fail byte-equality    | the count of one FAILURE MODE published as the count of the set |
| "23 rows carry `status: n/a`"          | 22 on column-0 `- AT-` rows    | the GRAMMAR SPECIFICATION counted as an instance of the grammar |
| "three that were yours are enumerated" | three OF TWENTY-ONE enumerated | the whole scope riding on four words in a subordinate clause    |

**`state the scope` is not the remedy, and the evidence is that all three DID state it.** The scope was present in every case and simply not load-bearing: an adjective, a possessive, a subordinate clause -- all skippable in a way a denominator is not.

**What makes the denominator a construction rather than a discipline is that you cannot write it without knowing the population.** `three of twenty-one` cannot be written by someone who has only counted three. So the honest form **cannot be produced from the subset**, which is exactly where all three of these came from. Same shape as the equality-over-threshold rule above and the same test: **prefer the form you are unable to complete without the whole set in hand.**

**And a subset published as a set is invisible to review by construction, because every word of it is true.** None of the three was reachable by more care; each needed someone running or grepping the whole population. That is the argument for the method over the vigilance, stated as a positive: the denominator IS running the population, written down.

**AND IT IS NOT ONLY DOCUMENTS -- AN ANNOTATED CODEBASE HAS THE SAME BIAS, ONE FALSE POSITIVE PER REPAIR** (ic, 2026-08-17, on my miscount). I reported `enum_str` as having 42 call sites of which six reach a human; it is 34 and four. The inflation was the `use` import at `render.rs:15` and **the comment at `:692` -- which is cc's annotation of the `wp show` fix, quoting the defective call it replaced.** So the line that inflated the count is the line recording the previous instance of this exact defect.

**The bias is systematic rather than a slip, and it runs the wrong way**: a codebase that documents its repairs inline accumulates one false positive per repair for any needle whose subject is "lines containing the string", **the annotations cluster exactly where the defects were**, and **the better the annotation the more reliably it is miscounted, because a good one quotes the defective call.** It therefore inflates in the direction that makes the problem look larger, so it reads as corroboration rather than as noise -- the asymmetry rule, arriving through the practice of writing good comments. Two nodes hit it in one day and one of them twice. **The control is not "write fewer comments": it is that a count of call sites must exclude imports and comment lines, or be small enough to read.**

**IT REPRODUCED WITHIN THE HOUR, IN A NEEDLE WRITTEN BY THE NODE THAT HAD JUST COMMITTED THE RULE** (vc, 2026-08-17). Sweeping all 105 register entries for v3 status tokens leaked into the column that records v2 -- the `at na` contamination, checked for siblings -- the sweep returned four hits and **three were false**: `st done`'s `completed:` is v2's own frontmatter field name, `wp done`'s `done:` is v2's own output prefix, and the third was **ic's correction note, quoting the dead token to mark it dead.** The instrument built to find contamination flagged the record of the contamination being fixed. **Third instance in one day, in a third artefact class -- a comment, a document, and now a JSON register -- with the sentence above committed less than an hour earlier by the author of the needle.**

**The fourth hit was real and is the finding worth having**: `at na`'s `.stdout` was corrected to `n/a` while its adjacent `.notes` still read `` `n-a` belongs to `(non-test)` rows only ``, **so the row carried the fix and the defect in neighbouring fields** and a v3 implementer reading the gloss would take the wrong token from it. **A correction that lands in one field of a record does not land in the record** -- which is the same pair as a correct note beside an incorrect assertion, at a smaller scale, and it recurred one artefact away from the reasoning that named it.

**THE EIGHTH INSTANCE IS THE CLEANEST, AND IT IS A VERIFIED NEGATIVE ANSWERING THE WRONG QUESTION** (vc and ic, 2026-08-17, both of us). Asked whether editing a whiteboard board mid-run could perturb the shell leg, vc swept the five files matching `intent/whiteboard`, found every one builds its own fixture, and reported **"no shell test reads a live board"** -- true, reproducible, and the wrong question. ic then swept the wider spelling and closed it: **the leg reads the live tree constantly, just not boards.** `tests/lib/test_helper.bash:6` resolves `INTENT_PROJECT_ROOT` to the repo root and `:25` exports `INTENT_HOME` to the same, so five reads land in the working tree -- `in-whiteboard/SKILL.md` (twice), both `lib/templates/hooks/whiteboard-*-guard.sh`, and `usage-rules.md`.

**Those are precisely the files someone edits while thinking about whiteboard protocol, which is when they are least likely to ask.** The measured negative was real; the conclusion drawn from it was broader. **Neither of us asked what the leg DOES read** -- we both answered the question we were handed.

**And the corollary about caution, which is ic's correction of vc's framing.** vc had it that folding without considering the question was the worse error and holding work was the safe direction. **It is not: ic held on a premise they had not verified, and had they not gone and checked, a false watch-out saying the guard reads live boards would have entered the canon.** Caution resting on an unchecked premise is still an unchecked premise -- it just fails quietly, and it costs the work it stopped.

**LANDING A FIX IN THE REPLACEMENT DOES NOT RETIRE THE INCUMBENT, AND THE ONLY THING THAT SETTLES IT IS ASKING WHICH BINARY RUNS** (vc, 2026-08-17, and it cost a live false verdict). Issue 0032's fix landed in v3's `contract.rs` -- `.all` plus a non-empty guard, both mutation-proved. I read it at HEAD, re-measured all 112 AT rows against **the v3 predicate**, saw zero verdicts move, and released the row I had been holding all day. **The gate then scored the criterion satisfied: 45/112 to 46/112, on one of two covering tests, with the second unwritten.** `intent` on PATH is **v2**, `bin/intent_acceptance:454` still carries the early return the issue was filed against, and it is unfixed **deliberately** -- `bin/**` is not mutated in place here and WP-04 replaces it.

**Nothing about reading `contract.rs` could have revealed that, which is what makes it the class rather than carelessness.** Every step was correct about v3 and the report was about the contract. **A dual-implementation estate has TWO of everything, so "the fix has landed" is a claim about a binary and never about a behaviour** -- and during a rewrite the incumbent is the one that still governs, for as long as it is the one on `PATH`. **The release condition for an interim discipline is therefore never "the defect is fixed"; it is "the fixed implementation is the one that runs."**

**And the check that should have caught it nearly did not.** My first verification counted the criterion's absence from a `tail -1` of the gate's output and printed `0`, which reads as "not unsatisfied" and is equally consistent with "the gate produced no output" -- the command had gone to background at its timeout. **A count over a population I had not established, inside the check written to catch what I had just broken.** Re-run once, cleanly, asserting the byte count before reading the answer.

**A LIVE LOG'S MTIME IS ITS LATEST WRITE, AND ONLY THE SEAL SAYS WHICH.** A run log's FILENAME carries the start; its mtime carries the finish **only once the run has sealed**, and while the run is in flight the same mtime means "latest write so far" and moves under a reader. Nothing in either the name or the file distinguishes the two states -- **the `.errors` seal does**, which is the same artefact that answers whether the verdict is complete at all. So a timestamp comparison against a run log is sound only after checking the seal, and today's morning diagnosis of hv's red legs was sound for exactly that reason and by luck rather than by check: the leg had already sealed, so its `.out` was static.

**A CLEAN `git status` LICENSES A BUILD, NOT A MEASUREMENT TAKEN LATER AGAINST ITS OUTPUT** (ic, 2026-08-17). ic built a binary from a tree `git status` reported clean, verified that, and began measuring; a peer then edited `render.rs` in the same shared checkout and something rebuilt the binary **at the path ic was reading.** Two binaries, one path, one name, and the only thing separating them was an mtime 31 seconds apart -- **which establishes order and not content**, the same limit the seal rule above draws, on a different artefact. All 27 values were re-driven against a `git archive` extract and reproduced, so nothing shipped wrong; **the part worth recording is that nothing on disk could have told anyone that.**

**This is the `tree` row with the target moved from the source to the ARTEFACT, and the distinction from the `instant` row is worth keeping.** cc's is reading the right artefact at the wrong moment, inside one operation. ic's is a precondition that was verified correctly and then **expired between the grant and the use**, with no statement anywhere of how long it was good for. A shared checkout with several writers makes the validity window short and invisible at once, so the control is not "check `git status` first" -- it is to measure against an extract you own, or to record the referent with the measurement so a later reader can tell which build spoke.

**The operational form is not "was the measurement right".** It is **"what was this measurement ABOUT, recorded in the artefact"** (ic's framing, 2026-08-17): which tree, which process, which corpus, which layer, which population. A result that names its own subject is self-identifying when it goes stale; a result that does not needs somebody to remember, and a rule depending on its author remembering it at the moment of use is not a control. That is why the fix for 0049 is a `measured:` line and not a resolution to be careful.

**Calibrate an instrument against a known-good case before believing its output -- especially a zero.** A measuring instrument reporting zero is indistinguishable from a broken one until it has been shown to report non-zero somewhere it should. Two harness lies crossed nodes in one afternoon from a single root: zsh does not word-split unquoted parameters, so `intent $probe` with `probe="wp list"` passes ONE argument and the dispatcher correctly reports a command it does not know -- inventing a defect in `intent wp list` that does not exist. The control costs one command. Applied within the hour to the next instrument built: before believing `intent config` had zero test coverage, the same needle was run against `doctor` and returned 3.

**AND THAT CONTROL IS NOT SUFFICIENT, WHICH I FOUND BY PASSING IT AND BEING WRONG ANYWAY** (vc, 2026-08-17). Measuring per-row parity coverage, I calibrated the needle against `wp show` before believing anything, got 3 blocks of 3 asserting output, and proceeded. **It then reported `claude hook` and `claude start` as invoked by no test in the estate. Both are false.** `claude hook` is driven inside a `run bash -c "... '$INTENT_BIN' claude hook ..."`, single-quoted; `claude start` is driven through **`$INTENT`, a different variable entirely**. The needle knew `run_intent` and `$INTENT_BIN` because those are what the calibrating case used.

**Calibration proves a needle finds what it finds. It cannot prove the needle knows every spelling, because the calibrating case is written in one of them.** A known-positive drawn from the corpus will pass any needle that handles that case's own form, and says nothing about the forms it does not share -- so the control is self-confirming in exactly the dimension that matters. **Enumerate the spellings FROM the corpus and calibrate against one of each**: `run_intent` (747), `$INTENT_BIN` bare/single/double-quoted (104), `$INTENT` (6), `bin/intent` (7). Four calibration cases, one per form, and the two false zeros became four and two real blocks.

**Note the direction, because it is the asymmetry rule again and it caught nobody by luck.** The blind needle did not lose findings -- it MANUFACTURED two, and both were the kind that read as serious (an untested hook, an untested launcher). They survived only because zeros get hand-checked here as a rule; nothing about the output distinguished them from the one real zero standing beside them.

**AND THE SHARPER FORM IS ic's, WHO HIT IT TWICE IN ONE ROUND WITH TWO SEPARATELY-WRITTEN PATTERNS** (2026-08-17, `29d6f250`). Measuring the same question in parallel, ic's needle knew `run_intent` and `bin/intent` and returned four uncovered rows; **three were false.** Going to hand-check them, ic wrote a SECOND pattern as an independent check -- **and it missed `"$INTENT"` as well**, because the closing quote sits between the variable and the path, so a pattern demanding whitespace after the variable skips it. `claude start` came back zero a second time, and was only found by grepping the literal string with no pattern at all -- **seven invocations, in a file named after the command.**

**THE CHECKER IS WRITTEN BY THE SAME HEAD AS THE NEEDLE, SO AN INDEPENDENT CHECK IS NOT INDEPENDENT UNLESS ITS SOURCE IS THE CORPUS RATHER THAN THE AUTHOR** (ic's rule, and it subsumes the one above). My version says a calibrating case is written in one spelling; ic's says the verification inherits the author's mental list of spellings whole. Together: **an instrument and its check must both draw their population FROM the corpus, because the author's memory of the corpus is the one thing they cannot help sharing.** Enumerate, then calibrate -- in that order, and never from recall.

**Two further things this episode settled, both worth more than the row count.** First, **when two instruments disagree neither is privileged** (also ic's): each is an instrument with its own subject, and the adjudication was done by hand against raw text rather than by either needle -- ic had already spent a round doubting a correct tool on the authority of a broken check whose `\bintent\s+` boundary excluded `run_intent`, ie 877 invocations. Second, **the two tools could not have found each other's finding**: ic's asks whether an invoking block asserts anything and so cannot see `agents sync`, whose eleven blocks assert plenty and discard the stdout; mine asks about output specifically and so had nothing to say about rows nobody invokes. **ic deleted theirs rather than repairing it, on Highlander grounds and not modesty -- two instruments answering one question is the violation, and a repaired second one is still a second one.**

**COUNT WHERE THE DEFECT MANIFESTS, NOT WHERE IT ORIGINATES -- AND WHEN THE SITES SHARE NO CHOKEPOINT, CREATING ONE IS THE FIX.** Issue 0050's enumeration was of the facade call sites that discard the outcome. **It went short by two**, because `ac descope` and `ac withdraw` break the call across lines and a line-oriented pattern cannot see them; cc's rule for that is that **an enumeration which has gone short cannot be checked against itself.** The constructive counterpart, measured 2026-08-17 (vc) at `ae3e308f`, after cc consolidated the arms behind one emitter: **the same property is now verifiable at ONE site.** `render.rs::reported()` is the only function in the CLI that prints a movement; the whole crate emits `ok:` from six places, four of which are not movements at all (an extract written, a store replaced, `todo.md` regenerated, an estate parsed); `intentsvcs` emits none, so the facade cannot print; and `Verdict::line` -- the one renderer that composes its own prefix -- emits `gate:`.

**The manifestation needle cannot go short in the dangerous direction, and that is the whole of the argument.** An arm that prints a movement without going through `reported()` must contain a `println!`, so it is in the swept population by construction. The origination needle has no such guarantee: an arm can call the facade in any shape a pattern fails to match, and it did, twice. **So the Highlander fix bought more than the removal of duplication -- it made the property checkable at all.** Before it, verifying "no arm reports a movement it did not make" meant enumerating twenty-one call sites correctly, with a miss invisible; after it, the same verification is "one function is the only printer", which a reader can close in a single pass.

Generalised: when a property is asserted across N sites, the cheapest true verification is at whatever chokepoint those sites share. **If they share none, that is the finding -- and building the chokepoint is a cheaper repair than building an enumeration nobody can trust.**

**A broken instrument that strengthens your finding does not get questioned; one that weakens it does.** The calibration rule above catches the first kind of error -- a zero you cannot distinguish from a dead instrument. This catches the second, and it is the more dangerous kind because nothing about it feels wrong at the time. Measured on this thread in one afternoon: a probe loop that passed one argument instead of two invented a defect in `intent wp list` that does not exist; a verb-extraction grep that omitted `sync` produced a divergence between two artefacts that in fact agree exactly; a `grep -c` for list items returned 0 against a section written as bolded paragraphs and nearly reported it empty. **All three would have made the finding stronger, and none was questioned until someone read the underlying thing instead of the count.** The asymmetry is not luck: an instrument whose error costs you a finding gets debugged; one whose error hands you a better story gets believed. Suspect your instrument hardest exactly when it agrees with what you were hoping to show.

**Success is reported by the mechanism; the property has to be checked by something else.** An edit that removes something is not verified by the edit succeeding -- two successful `Edit` calls during a board tidy silently deleted this thread's biggest finding, and it survived only because someone grepped for it afterwards. Four routes reached this shape in one day and they are one shape: a mutation that applies on a branch the test never walks; a guard scoped to what is already clean; a file clean by luck rather than by construction; and an edit whose success says nothing about what it left behind. In every case the operation returns success and the property is absent.

**A file clean by luck and a file clean by construction look identical in a diff, and only one of them stays clean.** Record what an artefact is NOT protected against, not only what it currently passes -- a green check that cannot say which kind it is has told you less than it appears to. Three routes reached this idea in one day, which is usually the sign it is load-bearing: a mutation that applies on a branch the test never walks; a guard scoped to what is already clean, which certifies the status quo; and a generated view that is formatter-stable only because nobody happened to write emphasis into it yet.

**A DEFECT WRITTEN DOWN AS THE SPECIFICATION PASSES EVERY CONSISTENCY CHECK.** Three artefacts on this estate describe behaviour that is wrong, and each one makes the wrongness harder to see rather than easier. `contract.rs`'s doc comment states _"a test-backed AC is satisfied exactly when a covering AT is GREEN"_ -- an accurate description of the `.any` it sits above and a false statement of the requirement, **so anyone checking the code against its own documentation finds agreement** (issue 0032). `cli_end_to_end.rs` asserted `status: wip` where every other surface prints `WIP`, pinning a v2 parity break as expected output until cc's sweep reached it. And the dispatch row's note for `wp show` said the output was unchanged in kind -- **which was true, and the binary was not** (cc, 2026-08-17).

**The last two are the pair worth the ink, because they sat side by side.** The estate held a correct statement of the requirement AND an incorrect assertion of it, about the same command, at the same time. **The incorrect one is the one with teeth, because it runs.** A note is read by a person at authoring time and then never again; an assertion is enforced by a machine on every commit. When the two disagree the machine wins silently and by default, because a green suite is not a report -- **nothing anywhere compares a row's prose to the assertion that covers it.** Recommended rather than ruled: where a dispatch row claims output parity, the test that covers it should quote the row, so the two statements of one requirement are forced into the same file.

**And this is why `corrected` beats `as-observed` whenever the v2 behaviour was never decided.** The register's job is to stop a v2 defect being laundered into a v3 requirement, and the laundering does not happen in the register -- it happens in a doc comment or an assertion written by someone reading the old behaviour off the old code. Both routes above produce a v3 that is internally consistent and wrong, and internal consistency is precisely what an output-equality suite measures.

**Classifying by the shape of a failure is a guess that looks like a finding** (cc, 2026-08-14, caught in their own delivered work). A red assertion tells you a test failed; it does not tell you why, and the two readings lead opposite ways -- a genuine parity break, or a test that hand-builds a v2 estate in its own body and is therefore a ratified retire under the manual-edit-workflows class. cc classified 35 rows from failure shape, then re-read the tests and found most were the second: 26 of 54 and 23 of 30 were building the old estate inline. **The register's classes are about what a test IS, not about whether it passed**, and a classification derived from a failure is the register recording the harness's state instead of the estate's. Read the test.

**`cmd | head; echo $?` reports the PAGER's exit status, never the command's** (vc, 2026-08-14, three times in one session). Exit codes are half of what this thread's parity contract is about -- D17 carries v2's codes over, INV-02 forces an override of clap's default, and the pre-commit gate reads one -- so an exit code read through a pipe is the single most load-bearing number the harness produces, and a pipeline reports the LAST stage. It manufactured two clean defects that do not exist: `intent search` with no arguments "exiting 0 on a usage error", which reads as a No Silent Errors violation and is actually exit 1; and `intent ac gate` "printing BLOCKED and exiting 0", which reads as a broken close-gate and is actually exit 1. Both were about to be reported to the node that wrote the code. Redirect to a file and read `$?` before anything else touches it, or use `${PIPESTATUS[0]}` -- and note the direction one more time: **all three artefacts handed back a finding rather than a dull result**, which is why the asymmetry rule below is the one worth re-reading before believing an instrument.

**A guard verified in one harness is not verified; it is verified in that harness** (ic, 2026-08-14, found by their own guard failing silently in the caller that mattered). The corpus guard tested green under `gen_register.sh` (`set -uo pipefail`) and was DEAD under `coverage_map.sh` (`set -euo pipefail`): the bare command substitution inside `corpus_require` aborts the shell the instant the diff reports a disagreement, so the tool exited 1 with an EMPTY stderr against a baseline known to be four files short. **A guard that dies silently in strict mode is worse than no guard -- it reads as a clean tool failure rather than as a finding.** The general shape is that shell options are part of the harness, not part of the script: `set -e` changes what a non-zero return DOES, so the same guard body has two behaviours and only one was ever exercised. Test each caller, each with its own canary, and confirm the guard names the real defect rather than merely refusing.

**Two consumers of one artefact grow two different wrong behaviours around the same missing check** (ic, 2026-08-14). `coverage_map.sh` did not merely read a stale baseline -- `[ -n "$row" ] || continue` **skipped** any file absent from it, so three files left the arithmetic entirely under a verdict that printed with full confidence, while the register's own generator went wrong a different way on the same gap. The fix is not two fixes: the comparison moved into `lib_corpus.sh` once and both callers refuse on it. **A missing check does not produce one bug; it produces one bug per consumer, and they do not look alike, so finding the first teaches you nothing about the second.**

**A count is not a diagnosis, and a record is complete only against the revision it names** (vc, 2026-08-14, caught in vc's own reported finding). A register carrying 97 rows against 98 files on disk was read as a dropped row and reported to its author as a hole in their sweep. The count was right; the cause was invented. At `309d01d` -- the revision the register names -- there were exactly 97 files, and the 98th landed at `ddac6ba`, which `git merge-base --is-ancestor` shows is not an ancestor of it. Nothing was dropped: the estate grew after the measurement, which is the record working as designed rather than failing. Two checks settle it and neither is arithmetic -- ancestry, and a set difference in **both** directions, since a phantom row and a missing one are different defects that one total conceals equally well. The generalisation is the expensive half: **any point-in-time register is falsified by the next test that lands**, and here the falsifying commit was itself a guard test written six minutes before the register was regenerated. An artefact measured at a revision must name it, and a contract consuming such an artefact must say which revision it means -- or it silently demands an estate-wide re-sweep every time anyone writes a test, which is a standard nobody can hold and everybody stops trying to.

**A file named after a command that does not test that command is worse than no file.** It answers "is this covered?" wrongly and confidently. `tests/unit/config.bats` exists, burns 5 of 7, tests configuration LOADING via `intent info` / `intent doctor` / `intent st list`, and never invokes `intent config` once -- so the command has zero coverage while the file listing implies otherwise.

**File-level classification is structurally blind to command-level coverage.** This is a stated limitation of the register, not an oversight in it: a `split` row is silent about whether any command family is exercised by nothing, because it classifies files and the question is about commands. `parity/tools/coverage_map.sh` answers that question instead, by joining the dispatch table's families against `burn-baseline.tsv` -- and the join is the method, since a naive grep reports `treeindex` as one of the best-covered families in the estate when all 53 of its tests exec the binary directly and the dispatcher sees none of them.

**A command family with zero burning coverage is a parity hole**: v3 can change it freely and the conformance suite stays green. Condition on AC-00.1; the work lives at AC-06.1.

**The burn ratio is BLIND TO NEGATIVE-ASSERTION TESTS, and the blindness is one-directional.** A test that asserts a _failure_ passes under both bindings, because `/usr/bin/false` fails too. `intent_upgrade_orchestrator.bats` runs `run "$INTENT_BIN" upgrade; assert_failure` -- it genuinely exercises the CLI and burns zero. Measured 2026-08-15: six such tests across three files, surfaced by the per-test pass as `UNCLASSIFIED / invokes CLI, zero burn` rather than found by inspection.

Two consequences worth separating. **The error direction is safe**: the method UNDER-counts CLI reach and never over-counts, so a burn figure is a floor on conformance coverage rather than an estimate of it. And **the blind spot is detectable rather than silent**: a zero-burn test that nonetheless calls the CLI is exactly the combination `classify_no_burn` refuses to classify, so these arrive as named rows wanting adjudication instead of quietly joining `out-of-scope`. A method with a known hole that reports into the hole is worth more than one whose hole is invisible.

**A fixture built to have "no git" was inside a git repository** (vc, 2026-08-15). The scratch directory this thread works in sits under `/Users/matts/.claude`, which is itself a repo, and git resolves a repository **at or above** -- correctly. So a fixture with no `.git` of its own still inherited that repo's ignore rules, and a measurement of "what happens outside git" measured something else entirely and reported it into the contract as fact. **Assert the environment you think you are in before measuring in it**: `git rev-parse --show-toplevel` costs nothing and is the difference between a fixture and a belief. Give a fixture its own `git init`, or place it where no repo is above it, and say in the record which of the two you did.

**A paired control tells you the INSTRUMENT changed, not just that the fix failed** (vc, 2026-08-15). Re-running a two-arm probe after cc's fix, both arms went silent -- including the arm that had reported before and had no reason to stop. One arm alone would have read as "the fix did not take" and been sent as such. Both arms silent is not a result about the subject at all; it is a result about the instrument, and the correct next question is what changed underneath rather than what the change proves. The discriminating pair earns its cost twice: once for the verdict, once for knowing when there is no verdict.

**Test data placed in a generated artefact is destroyed by the mechanism under test** (vc, 2026-08-15). Appending a unique phrase to `info.md` and then searching for it measured nothing: `info.md` is a generated view under D22, so `sync` regenerated it and discarded the edit -- correctly, and silently, and the subsequent empty search read exactly like a broken index. **Put test data where the truth lives**, which under D01/D02 means the canon, never a view. The general form: when the system under test owns an artefact, that artefact cannot also be your input.

**A stated cost is a claim, and it gets re-run like any other** (ic and vc, 2026-08-15, both wrong on the same number). The register sweep was described as multi-hour and not parallel-safe; that description was written into a tool's justification, quoted across nodes, and used by the verifier to REFUSE a re-sweep. Timed once, it takes **7 minutes 52 seconds**. The multi-hour figure came from a single file hanging, and a defect had been generalised into a property of the tool. Nobody had timed it. **The verifier's whole function is to re-run claims rather than read them, and a claim about cost was exempted purely because it arrived as a constraint rather than as a finding** -- constraints are the claims most worth checking, because they are the ones that stop work happening. Corollary: a deferral justified by an unmeasured cost is not a trade-off, it is a guess with a schedule attached; fix the stale row instead of noting it.

**A test written from the same misreading as the code cannot catch the misreading** (cc, 2026-08-15). A test named `both_spellings_of_sync_are_wired_and_agree` asserted the two produced identical bytes, passed, and was read as confirmation -- when all it confirmed was that a wrong model was internally consistent. `intent st sync` and `intent sync` are different commands. The incumbent's behaviour caught it, not the suite. This is the deeper form of the calibration rule: an instrument built from the hypothesis it is testing cannot falsify it, so the discriminating evidence has to come from OUTSIDE the model under test -- the v2 binary, the committed artefact, the thing that existed before the belief did.

**A check that answers confidently about a set it never looked at** (cc, 2026-08-15, naming the class the three of us hit independently in one night). Not a wrong answer -- **an answer to a different question, wearing the right answer's clothes.** Four instances, none of which announced itself: a `covers` regex written `([^-]+)` stopped dead at the hyphen in `AC-06.2`, matched zero rows, and printed a clean "nothing outstanding"; a corpus fixture keyed on `*.sql` passed identically whether or not the config under test was loaded, because the pattern was in the real global too; an ignore-needle matched `$INTENT_BIN` as a search string rather than a call site; and a green-transition check tested a file's EXISTENCE where the property wanted was that it is committed. The shared defect is that each returned a well-formed result for a population it had not established it could see. **Assert the needle matched something, and print the size of what it matched, before reporting on it** -- a count you can see is a needle you can argue with, and a zero from an unproven needle is indistinguishable from a genuinely empty result.

**Writing a rule down does not stop the author breaking it, and the evidence is this thread** (vc and cc, 2026-08-15, independently and repeatedly). vc fabricated FOUR whiteboard timestamps in one session -- while writing the clock rule, enforcing it on a peer, and citing it in the message that carried the fourth. cc read a corpus through `| head`, lost the eleventh of eleven rows, and published "ten spellings" into a source comment and a commit message -- with `| head` already on their own board as a watch-out, three lines from the pipeline-exit-code note, added in that same session. Neither failure was ignorance of the rule; in both cases the author had written it that day.

**So a rule that depends on the author remembering it at the moment of use is not a control, it is a hope with good phrasing.** The two remedies that actually worked here were both mechanical and neither required anyone to remember anything: the pre-commit clock guard REFUSED a bad stamp, and `lib_corpus.sh` REFUSED a register generated against an incomplete baseline. The two that failed were both documentation. This is the general form of D30's timestamp argument and of the guard-rather-than-a-promise choice made throughout this thread -- **when a rule matters, spend the effort on the mechanism that enforces it rather than on the sentence that states it**, and treat a rule you can only obey by concentrating as an unfixed defect rather than a solved problem.

**A generated artefact that cannot be re-derived from committed state has no cheap check, and its stamp is the only guard it has** (vc, proposed 2026-08-15; ic, evidenced the same day). Not a theoretical class. The 26 `cmd-*.md` command inventories are generated by `gen_inventory.sh` from `probes/toplevel.tsv` -- a file the script only ever READS, which lives in a throwaway scratch directory, has **never been tracked**, and is no longer on disk anywhere. So the input behind the committed measurement at `69d42a7` is gone, and those 26 artefacts can be checked for provenance (do they all name one revision?) and not for content. **That is a materially weaker claim than the drift check has been assumed to rest on**, because the inventory is the measurement half of the comparison the table is checked against: the authored side is fully checked and the side it is compared to is not.

**CORRECTION, 2026-08-15 (ic): "gone" was wrong -- it was UNTRACKED, which is a different thing, and looking is cheaper than reasoning about it.** The paragraph above concluded the input no longer existed anywhere on disk. It was still sitting in the originating session's scratch directory, along with the ad-hoc driver that produced it, the fake HOME it ran under, and the sandbox. `git log --all` was the right query for "was this ever committed" and the wrong one for "does this exist"; nothing had been run against the filesystem. **The TSV is now recovered and COMMITTED at `parity/probes/toplevel.tsv`**, which is what the rule asked for all along, and the driver is committed beside it as `tools/probe_toplevel.sh` -- it had never existed as a file.

**With the real input back, the 26 inventories were checked for content and are exactly faithful to it: 26 of 26 reproduce, modulo table padding.** So the weaker claim was true of the record and not of the artefacts. Three further findings from the same run, all measured:

- **Exit codes reproduce 26 of 26 at the same revision; first lines 26 of 26 in behaviour.** Byte counts reproduce 20 of 26 -- the six that move (`bootstrap`, `doctor`, `ext`, `help`, `info`, `todo`) embed the sandbox's ABSOLUTE PATH in their output, and `ext` differs by exactly the path-length delta, 55 bytes against 55 characters. **The byte column is a property of the path the probe ran under, and that path was never recorded.** Exit codes and first lines are the portable half.
- **`probe.sh` isolated `INTENT_HOME` and not `HOME`, and got away with it because the uncommitted driver did `export HOME="$SP/fakehome"` before sourcing it.** When that driver went, the isolation went with it: a re-probe written against the committed file alone read the developer's real `~/.intent/ext` and took a different code path. This is this document's own mechanism-over-sentence rule turned on itself -- **the isolation that was written down survived and the one that lived in someone's shell history did not.** Now in `probe.sh` where a caller cannot forget it.
- **The probe matrix MUTATES ITS OWN SANDBOX and is therefore not idempotent.** `intent todo` bare generates `intent/todo.md` when absent, so run one records `no intent/todo.md yet -- generating...` and run two records the read path -- two runs of one script at one revision disagreeing on a first line, with nothing in the output saying which run it was. The driver now rebuilds the sandbox rather than reusing it. Same command, same reason its dispatch-table row is classified `mutate`.
- **The provenance's "108-probe matrix" is right and the inventory renders 104 of them.** The 27th unit is `version`: probed on 2026-08-14, never given a `cmd-version.md`, because `gen_inventory.sh`'s spec list has no entry for it. Measured data exists for a command the inventory does not carry.

**Worse, the artefact instructs the reader to destroy it.** Every generated header says _"re-run it rather than editing this file"_ -- correct advice that, once the input vanished, would have overwritten 26 good measurements with empty probe fields carrying the revision stamp of the good data. Measured rather than assumed: `awk` against a missing file exits 2 and produces **nothing**, so the dash fallback never runs, and the script's `set -uo pipefail` without `-e` does not stop it. The remedy is the one this section keeps arriving at -- `gen_inventory.sh` now REFUSES on a missing or empty TSV, because **a missing measurement must present as a refusal to measure, never as a measurement of nothing.** The general rule for anyone adding a generator here: if the input is not committed, the output is stamp-only, and saying so in the record is part of producing it.

**RE-DERIVABILITY IS NOT COMPLETENESS, and a lossy generator is a perfect fixed point with itself** (ic, 2026-08-15). The skew check asks whether a committed view matches what its generator PRODUCES. A generator that silently drops a field satisfies that for ever: regenerate, compare, identical, green. Measured on `dispatch-table.md` -- the canon carried **20 distinct `target` sub-fields and the renderer named 5**, so 15 were written, committed and invisible in the human-readable face, including config keys another node was blocked on. **The check was not wrong; it was answering a different question faithfully**, which is the hardest kind to notice because nothing ever contradicts it. Two properties, two checks: skew for re-derivability, and a completeness refusal comparing the canon's own keys against the rendered text. The corollary for this toolchain: **every generator wants to be asked "does the view carry everything the canon holds?", separately from "does the view match the generator?"** -- and the second cannot answer the first.

**ENUMERATE THE POPULATION; DO NOT SNIFF FOR A MARKER** (ic, 2026-08-15). A needle reports on the set it MATCHED, never the set it was aimed at -- so before building a check on one, count what it matches and print the count. Measured: a backstop planned around detecting a `GENERATED` banner would have covered **1 file in 30**, because 29 generated views carry no banner; enumerating the apparatus directories instead covered all 30. The same shape twice more the same day -- a `jq '.families[].entries[]'` audit reported a canon clean while every offending string sat in a top-level array that path does not traverse, and a mutation run went red from a **different guard entirely** because the fixture never reached the branch under test. **A structured query is a needle too**, and it reports on the subtree it walked. The discipline is cheap and mechanical: derive the population from something that must be complete (a directory listing, the canon's own keys, a declared roster), never from a property the members happen to carry, and **assert the fixture reached the branch before reading its verdict**.
