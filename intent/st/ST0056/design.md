# Design - ST0056: Intent v3.0.0 -- Rust CLI + SQLite + bidirectional .md sync + MCP

## Problem

Intent v2 is 27 shell files, 12,492 lines (measured 2026-08-14 at `9cdebe0`), on a hostile platform (bash 3.2, no `declare -A`, `set -e` arithmetic traps, BSD/GNU sed splits). The deeper defect is structural: every v2 reader reimplements parsing, so every reader answers confidently from partial evidence. The v2.19.0 release closed five instances of that one class (0011, 0017, 0019, 0020, 0022), and each fix added another grep. v2 also bolted schema onto markdown three separate times -- the 0012 header-block ruling, the 0017 AT row grammar, the close-gate contract -- each a bespoke mini-standard needing its own lint, its own `--fix`, and its own weeks of drift cost.

v3 reifies the data model: one schema, one parser, one query surface. The bug class becomes unconstructible, and nobody designs a row grammar again.

## Architecture

### The layering contract

```
in-process:  intent (cli) ──────────────▶ intentsvcs ──▶ sqlite + files
remote:      intent (cli) ──▶ graphql ──▶ intentd ──▶ intentsvcs ──▶ sqlite + files
```

- **`intentsvcs` is the Highlander layer**: all of Intent's functionality, sole owner of BOTH the DB and the file canon (it writes structured files and regenerates views; the CLI and daemon never touch disk or DB).
- The GraphQL layer is a mechanically thin 1:1 skin over the intentsvcs facade; the clap layer is another. Thin Coordinator holds at every seam.
- **The DB-access rule is a mechanical guard, not a convention**: `rusqlite` appears in exactly one `Cargo.toml` (intentsvcs). The dependency graph is the enforcement, asserted in CI.
- **Dual-path conformance suite**: the standing guard against the two entry skins drifting -- the same operation run through in-process and through a live intentd must produce identical results. Every verb lands with one.
- CLI routing rule: if the intentd socket exists and answers, the CLI MUST route to it (never two sync engines live at once); when absent, it executes in-process against the same facade.

### Truth model

- **The schema is the source of truth for structure.** Reified, versioned, validating everything.
- **THE DB IS THE SINGLE SOURCE OF TRUTH** (hv ruling, 2026-08-15, reversing the original D01 -- see D01 below). All of `intentsvcs` works FROM the DB.
- **The files are the RE-CREATABLE artefact.** A sync process -- manual, or triggered by the daemon -- moves data BOTH ways: disk-to-db and db-to-disk.
- **The integrity guarantee is structural, not procedural**: the typed Rust API in `intentsvcs` is the only way data enters the DB, so everything in the DB conforms to the schema **by construction**. That is what "schema-as-truth" buys once the DB is the truth -- validation stops being a gate you remember to run and becomes a property of the only door.
- **Markdown is a realisation**: generated views for structure, the authoring surface for prose. Always present on disk.
- **The authored-once principle**: every fact has exactly one authored representation; everything else is generated from it and marked as such. Structure is authored via mutations (CLI/MCP) and serialised to committed JSON; prose is authored in .md bodies; views are generated.
- **No mixed files**: a file is either 100% authored or 100% generated. Mixed files resurrect region-markers and partial-parse drift.

### The reified model: one master, three generated faces

The model is authored once, in the intentsvcs Rust type layer, and generates three committed, CI-drift-checked artefacts:

| Face        | Generated via           | Consumed by                                                  |
| ----------- | ----------------------- | ------------------------------------------------------------ |
| JSON Schema | schemars from the types | validation of every structured file; Elixir/external clients |
| SQL DDL     | the schema apply        | **the DB, which is the durable SSOT**; migrations are normal |
| GraphQL SDL | async-graphql export    | CLI/MCP/TUI; Elixir codegen                                  |

`intent schema` prints them. Header blocks are line-oriented `key: value` everywhere -- the 0012 not-YAML ruling generalised; never YAML, anywhere.

### File layout (per artefact)

| Artefact                   | Canonical structure (committed)                                                           | Authored prose                      | Generated views                                                            |
| -------------------------- | ----------------------------------------------------------------------------------------- | ----------------------------------- | -------------------------------------------------------------------------- |
| Steel thread + WPs + AC/AT | `st/<ID>/thread.json` (metadata, status, WP records, contract, objective/context/related) | `design.md`, `impl.md`, `tasks.md`  | `info.md` (100% generated cover), `acceptance.md` (contract + coverage)    |
| Issues                     | `issues/<n>.json` (number, slug, status, priority)                                        | `issues/<n>.md` (body + resolution) | index views; OPEN/CLOSED-as-directories likely retires (status is data)    |
| Project                    | `intent/.config/config.json` (as today)                                                   | --                                  | --                                                                         |
| Indexes                    | --                                                                                        | --                                  | `intent/st/steel_threads.md` (v2 path kept), `intent/todo.md`, `AGENTS.md` |
| Tracking                   | --                                                                                        | `wip.md`, `restart.md`              | --                                                                         |
| Whiteboard                 | `whiteboard/<node>.json` -- `wb_node`, `wb_item`, `wb_message` (D30; WP-14)               | item and message bodies             | `wip.md`, `inbox.<sender>.md` (100% generated)                             |
| **The DB (SSOT)**          | `intent/.cache/intent.db` -- gitignored; **the truth, not a cache** (D01 as reversed)     | --                                  | --                                                                         |

Format: JSON (not YAML) for canonical structured files -- the 0012 quoting-hazard scar, plus the `config.json` in-house precedent. Tool-written pretty-JSON: stable key order, 2-space indent, trailing newline. md/anything else are `intent export --format` projections (trivial via serde).

**CORRECTED 2026-08-16 (vc, on cc's measurement while building AC-06.6). This line previously read "YAML/md/anything else are projections" and named export as the reason v3 could refuse YAML canon without refusing YAML users. That is false, and the measurement is the argument: YAML is refused as an export format too.** Our own reader (`serde_norway` 0.9.42) survives 24 of 24 hazardous scalars and the round-trip test PASSES -- **and then PyYAML 6.0.3 `safe_load`, reading the identical bytes, silently corrupts 6 of the same 24**: `no`/`yes`/`on`/`off` return as booleans, `12:30` as the integer 750, and **`2026-08-14` as a date object, which is the spelling of every `created` and `completed` field in the canon.** A `%YAML 1.2` directive does not save it -- measured, PyYAML accepts the directive and resolves 1.1 regardless.

**So the literal criterion passed and the purpose behind it failed, which is why this correction is a design act rather than a note.** The loss is in the CONSUMER, and under D34 the committed extract is the INTERCHANGE -- the one artefact where a field that does not survive the boundary is data loss rather than a gap. **0012's quoting scar is a fact about YAML, not a fact about canon, and it does not stop being true because the file is called an export.**

**YAML users are not refused, and that is measured rather than asserted: PyYAML reads the JSON export with 0 of 26 corrupted**, because YAML 1.2 is a superset of JSON. The refusal's remedy points there. **D03's goal survives by a better route than D03's mechanism** -- which is the distinction worth keeping, because a mechanism defended past its measurement is how a scar becomes a superstition.

Generated views are committed, with a `doctor`/CI skew check (regenerate, require empty diff) so a hand-edited view is caught, never silently outvoted.

### Ingest, views and sync

- **The write path is DB-first** (corrected 2026-08-15 for D01's reversal; the original read "write structured file + regenerate affected views + update DB", ie files first). A mutation validates against the schema through the typed API, commits to the **DB**, and then db-to-disk sync regenerates the structured files and affected views. Deterministic and idempotent: same data -> same bytes. **The failure semantics improve rather than degrade**: under the old order a DB-write failure rolled the files back, so a partial write could corrupt truth; with the DB as truth a file-write failure is recoverable by re-running sync (cc's analysis, `apply()`). **`store.rebuild()` is NOT the write-path primitive** -- it DELETEs and re-inserts the whole estate, which is "reload truth from a derived artefact" and O(estate) per mutation; the write path becomes a targeted transactional write, while `rebuild` survives unchanged as the **disk-to-db sync direction**.
- **Up-sync (files -> DB)** is ingest, and it is **strict**: standard JSON parse + schema validation; invalid input is refused with the finding named. No tolerance ladder for current-version data -- lenience was only ever a coping strategy for having no schema. Prose ingests verbatim (FTS-indexed).
- **Change detection**: SHA-256 hash of every in-scope file on every CLI invocation in daemonless mode; debounced fswatch in daemon mode. Scope: `intent/**` + named root files only. Stat (mtime/size) is retained as reporting metadata, **never as the hash trigger** -- rehash-on-stat-change cannot by construction catch the same-size same-mtime rewrite AC-03.3 requires, and stat is exactly the partial evidence v3 exists to stop answering from. Corrected by vc 2026-08-14 (surfaced by ic) after the original line specified the conflabd `db_sync.rs` rehash-on-change optimisation, which the contract rules out; if hashing scope ever costs enough to matter, taking that optimisation is a register-recorded deviation, never an accident.
- **`intent ingest --from-md`** exists as an explicit recovery path (and IS the v2 migrator); it is not the daily flow.
- A file that stops parsing (eg merge-conflict markers) enters a named unparsed state surfaced by `doctor`; commands needing it refuse with the finding named. v2 greps through conflict markers silently; v3 refuses.
- FTS5 across all bodies powers `intent search` from CLI and MCP.

### intentd

- **One intentd per machine, N projects**: a project registry (canonicalised root paths, registered on first contact), per-project DBs opened by the daemon, every operation bound to a project context at connection time. Moved/deleted roots surface in `doctor`, not crashes.
- Unix-socket GraphQL (thin skin over the facade); a separate **mgmt plane** (status, shutdown, reload, registry ops -- the conflabd `mgmt/` split) for `intent daemon status/stop`.
- **CLI owns the lifecycle via launchd** (conflabd pattern): LaunchAgent plist under `~/Library/LaunchAgents/`, logs under `~/.local/share/intent/`, binary resolution PATH-first-then-sibling-of-current-exe. PID file with observable (never silent) cleanup. GIT_HASH baked into the version string.
- **Policy-stamp self-healing**: generated local artefacts carry a version marker; on boot, missing/stale -> regenerate. Old installs heal without a migration.
- Watching: `notify-debouncer-full` + the `ignore` crate (debounced, gitignore-aware), never raw notify events.
- Minimal subscriptions in 3.0.0 (project/file changed) -- the seam the TUI and bus consume in 3.x.
- intentd ships IN v3.0.0 (hv ruling; one major release, patched by 3.0.x).

### MCP

- Tiered surface: ~10-12 typed high-traffic tools (st/wp lifecycle, ac/at, issues, todo, search, wb, doctor) + one `intent_graphql` escape hatch = full API access without tool-bloat context cost. MCP resources for read surfaces (wip, boards, ST docs).
- Under authored-once, **the mutation surface is how agents write structure** -- view-editing is demoted from workflow to recoverable mistake.
- rmcp (official SDK): stdio (`intent mcp`, per-session, in-process) now; bridge-to-daemon mode with per-request target resolution (the Lamplight `mcp.rs` pattern -- daemon restarts never strand a session); streamable HTTP from intentd for the 3.x multi-agent era (proven in conflabd for exactly this Claude Code use case).
- Tool definitions, CLI help, and the `intent llm` agent guide all render from the one dispatch-table SSOT (the Lamplight DD-6 pattern).

### Project search (WP-13): four tiers, two of them in 3.0.0

Intent becomes the search and index surface for the whole project, replacing `treeindex` (762 lines that shell out to `claude -p` for a prose summary per directory, cached and stale by construction) and the `in-handoff` skill. Both answered "what is roughly here" in advance and approximately; the need is "where is X" on demand and exactly. That is a different artefact, not a better summariser.

| Tier | Question                                               | Needs              | Ships       |
| ---- | ------------------------------------------------------ | ------------------ | ----------- |
| T1   | lexical -- find this string                            | FTS5               | 3.0.0       |
| T2   | structural -- find this definition, its call sites     | tree-sitter        | 3.0.0       |
| T3   | semantic -- where do we handle project-root resolution | an embedding model | 3.0.x / 3.1 |
| T4   | type-aware -- every caller passing this type           | a language server  | parked      |

**T2 is why tree-sitter and not an Elixir-specific parser.** Elixir AST work is trivial in Elixir and the hard path from Rust -- no mature Rust Elixir parser, and shelling out to `elixir` reintroduces exactly the external-runtime dependency that condemns treeindex. tree-sitter is Rust-native with a maintained Elixir grammar, and the same dependency yields Rust, Swift, Lua and Bash. The `languages` array in `config.json` (ST0037) is already the per-project grammar manifest; the Elixir-specific ask generalises to all five declared languages for the same effort.

**T2 is also the chunker for T3, which makes the ordering a dependency rather than a convenience.** Naive line-window chunking is why most code retrieval is poor; tree-sitter yields function- and module-level units with names and spans, which are the right embedding units. Building structural search first is what makes semantic search worth having.

#### T3: the model decision, and why it is not an architecture decision

The only genuinely new thing T3 needs is an embedding model, and that is the first time Intent would require either a large binary or a network call. Three shapes were considered:

- **Remote API** (Voyage, OpenAI): small binary, but a network dependency, an API key, per-token cost, and the posture change that matters -- embedding a private codebase means sending it to a third party. Rejected as a default.
- **Bring-your-own only** (Intent defines the interface, ships nothing): keeps Intent self-contained, but a capability requiring configuration is a capability nobody turns on, which is precisely how treeindex died. Rejected as the only shape.
- **Zero-config local, fetched on first use**: adopted. The model lands in `~/.local/share/intent/models/` on the first semantic query, announced rather than silent, and is governed by the **policy-stamp self-healing already in the design (AC-08.7)** -- a versioned local artefact that regenerates when missing or stale. The binary stays small, the default needs no configuration, no network call happens at install time, and the embedding interface still admits a swapped-in model or a remote endpoint for anyone who wants one.

Storage is a `sqlite-vec` vec0 table beside FTS5 in the same DB. ~~**D01 is what makes deferring T3 free**: the DB is rebuildable and there are no migrations ever, so adding vector tables later costs `rm intent.db` and a rebuild.~~ **Justification REPLACED 2026-08-15.** It rested on the DB being disposable and on a "no migrations, ever" rule **hv never asked for and has now rejected outright**. The deferral still stands and the reason is simpler: **adding vector tables later is a migration, and migrations are normal.** T3 costs a schema change when it lands, priced like any other.

#### T4: parked, with the trigger written down

A language server adds type resolution, cross-module definition and typed call hierarchy -- real capability, at the cost of a stateful process per project per language, version-coupled to the toolchain. Elixir's official LSP is the obvious candidate and the natural trigger to revisit. Parked because **LSP's leverage is refactoring, not search**: tree-sitter already answers the search questions, and a different product wants its own thread rather than a lien on this release.

#### Why the seams (S1-S5, WP-13 info.md) are sufficient to admit T3 and T4 later

This is AC-13.9's substance, and the reason it is safe to specify two tiers now and build them later:

- **S1 (gitignore-aware whole-repo scope)** -- T3 and T4 index the same corpus T1/T2 do. No scope change is ever needed.
- **S2 (two corpora, two staleness policies)** -- orthogonal to tier; a new tier inherits it.
- **S3 (one result shape, `{path, span, kind, tier, score, snippet}`)** -- a new tier adds a VALUE to `tier`, never a field. The CLI contract and the MCP tool schema are untouched by T3 and T4 arriving.
- **S4 (scores never blended across tiers)** -- a new tier ranks within its own group, so no ranking rewrite and no silently-changed relevance for existing queries.
- **S5 (degradation is named, never silent)** -- an unavailable tier (no model fetched, no language server) is a named absence through the same mechanism as a stale index. T3 and T4 are therefore _always_ optional at runtime without a special case.

**Whole-repo indexing is also the first genuinely forcing argument for intentd.** Everything else works daemonless; keeping a source-tree index warm incrementally does not. This is where the daemon stops being present and starts being load-bearing.

### Cloud seams (intentc-shaped, v4-facing)

Four cheap-now/expensive-later seams ship in 3.0.0; no cloud code does:

1. **`project_id` UUID** stamped into `config.json` at migration. Natural keys stay human-legible (ST0056, AC-01.2); the UUID namespaces them: `(project_id, natural_id)` is the global identity.
2. **A principal on every facade call**, defaulting to `local`. The forcing function is nearer than the cloud: the 3.2 agent bus needs principals (vc/cc/hv) before intentc does. Conflab's identity contract (`agent+<handle>`, server owns the api_key) is the eventual shape.
3. **An append-only event log table**: every mutation writes an envelope. Audit trail + subscription feed + the substrate a future sync protocol replays.
4. **A reserved `server` block in config** (unused in v3). The anticipated wire is Phoenix channels over WebSocket (the conflabd `ws.rs` pattern: `phx_join`/`phx_leave`, subscription list surviving reconnects) -- intentc will be Elixir/Phoenix/Ash, so the seam is already idiomatic.

## Migration (v2 -> v3)

- **Floor: v2.19.0. Two-hop policy**: older projects run v2's own `intent upgrade` first (that ledger exists and works; it is never reimplemented in Rust). The v2.19.0 consumer sweeps are therefore v3 migration prep.
- The migrator IS the legacy md parser (frozen once the fleet is over): read the v2 estate strictly -> refuse what cannot convert without loss, named (the `--fix` discipline, with the 87-destroyed-links scar to prove it) -> emit `thread.json` et al + regenerate fresh views -> stamp 3.0.0 + `project_id` -> build DB -> converge canon. One visible commit; rollback is `git revert` + the v2 formula.
- **The fleet corpus is the acceptance fixture**: every artefact in Intent + Lamplight + Utilz + Baize must ingest losslessly or appear in the residue report by name. Semantic completeness (every AC, AT, status, date, link, prose block accounted for), not byte round-trip. The Lamplight baseline (`intent/analysis/20260814-lamplight-at-sweep-baseline.md`, 1639 AT rows at `15dbccc92`) is the first fixture. Intent's own tree migrates first as canary.
- **Consumers' hooks do not change**: 0016's runtime-resolved `intent claude hook <name>` + byte-identical settings.json means the binary swap is invisible at the hook layer.

## Parity contract

BATS is the conformance harness, honestly scoped: **stdout, exit codes, and behaviour** are the parity contract; **file layout is a ratified deviation class** decided here, not discovered in test triage. Tests asserting what the tool says and does port; tests asserting how bytes were laid out in files that are now views retire with the layout, recorded in a keep/retire/deviate register. The `ok:`/`error:` lowercase voice (0023) and exit codes carry over unchanged. Rust-native tests grow alongside; BATS retires when the shell does (WP-12).

## Stack shortlist

Provenance: L = proven in Lamplight `native/cli`, C = proven in Conflab `native/daemon`.

| Crate                            | Role                                      | Provenance |
| -------------------------------- | ----------------------------------------- | ---------- |
| clap (derive)                    | CLI                                       | L, C       |
| async-graphql (+ axum)           | GraphQL schema + intentd serving          | C          |
| rusqlite (bundled, FTS5)         | store; WAL mode                           | C          |
| schemars                         | JSON Schema face                          | C          |
| jsonschema (in-doc resolve only) | ingest validation                         | C          |
| rmcp                             | MCP server (stdio + streamable HTTP)      | C          |
| notify-debouncer-full + ignore   | watching                                  | C          |
| thiserror                        | typed errors with remedies                | L, C       |
| serde_ignored                    | No-Silent inbound decode                  | L          |
| proptest                         | serialise/deserialise laws                | --         |
| rust-embed                       | embedded canon (templates, skills, rules) | --         |
| serial_test (file_locks)         | HOME-mutating test serialisation          | C          |
| cargo-dist                       | brew tap + release artefacts              | --         |

Declined from Conflab: mlua (agent scripting), AppleScript bridge (Conflab-specific); sqlite-vec noted as the 3.x path to semantic search. Lamplight's graphql_client codegen and Conflab's hand-written query constants are both client-of-a-foreign-server patterns; ours collapses because both ends live in one workspace.

## Design decisions (ratified by hv, 2026-08-14, this session)

### [VOID] index -- read this before citing any decision below

**Ruled by hv 2026-08-17: _"Yes, agree. Fix it."_ Added 2026-08-17.** A decision carrying `[VOID]` holds material that is struck: **its conclusion may still stand while its derivation does not.** Grep before you cite:

```
grep -n '\[VOID\]' intent/st/ST0056/design.md
```

**Why a token and not more care.** Struck material was spelled four ways in this file -- `~~strikethrough~~`, `STRUCK <date>`, `is VOID`, `REVERSED BY HV` -- so no single grep found them, and **four of five nodes cited void material across four days**: vc argued a cutover exclusion from a premise on the do-not-cite list (the fourth failure on D01 alone), and cc found the original dead reasoning and then rested a doc comment on the dead ruling beside it. **The token sits on the decision's ID LINE, not at the point of striking, because both nodes were reading for the CONCLUSION** -- a marker inside the struck span is only seen by someone already looking at it.

This is D33 applied one artefact over: hv replaced the hand-held clock rule with a mechanism rather than a sharper wording, for the same reason -- **a rule that keeps being re-broken by the same nodes is being held by resolve instead of by a mechanism.**

**Standing obligation on every decision recorded here, added 2026-08-15: a decision that changes the MODEL must name the SURFACES it moves.** Not a suggestion -- a decision without that list is incomplete and should be sent back.

Ic's EXP-04 is why. `st new -s|--start` came through the parity register as `keep`, and it still is: same spelling, same help text, same observed v2 behaviour. **But the meaning moved, because `st new` now enters at `Triage` and the flag spans two transitions instead of one.** A `keep` disposition is honest about the surface and silent about the semantics. **The trigger is the shape that makes it undetectable: a ratified model change, landing in a file the surface artefact does not read, made by a node not editing that artefact.** Nothing on either side is wrong and nothing can see the gap.

**ic proposed the mechanism on the artefact -- a per-row stamp naming the ruling it was last checked against -- and left the call here. Ruled the other way: the obligation goes on the RULING, not on the row.** Three reasons, and the third is the one that decides it. A per-row stamp requires ordering every ruling and re-stamping 103 rows per model change, which is cost proportional to the surface rather than to the change. It would also be a second hand-maintained roster, which is the class this thread keeps removing. **And the knowledge is not in the table: the person ratifying a machine knows which surfaces it touches, and the table cannot know it at any price.** Putting the duty where the knowledge already is costs one paragraph per decision.

This is the same rule as D35's containment note living in the design where the retention author reads it rather than in `.gitignore` where only git does -- and as ic's own EXP-01 lesson, that **a register which correctly predicts a defect and does not prevent it has done half a job, and the cheaper half.** EXP-04 predicts recurrence explicitly ("two machines ratified and WP-06 still landing"), so waiting for instance two would be repeating EXP-01 knowingly.

- D01 [VOID] **Truth model. REVERSED BY HV 2026-08-15 -- the DB is the SSOT and the FILES are re-creatable.** ~~schema-as-truth; durable = committed schema-validated JSON; runtime = rebuildable SQLite; md = generated views + authored prose. `rm intent.db` always safe; no DB migrations~~ **The ruling, hv verbatim**: _"the db is the SSOT and it's the FILES that are re-creatable... All of intentsvcs MUST be working from the db. There is a sync process, either manual or triggered from the daemon, that enables disk-to-db and db-to-disk updates. But it is definitionally the db and the fact that there is a programmatic, typed API (via the rust intentsvcs) that ensures that the only data that goes into the db conforms by construction to the schema."_ So: **the DB is truth; the files are re-creatable; sync runs both ways; `intentsvcs` works from the DB; and conformance is structural because the typed API is the only door in.** What does NOT survive, and must not be cited or reasoned from: "durable = committed JSON", "`rm intent.db` always safe", and "no DB migrations, ever". **"No DB migrations, ever" is DELETED rather than merely superseded, and hv's correction of 2026-08-15 is the reason: _"THIS IS NOT A CONSTRAINT THAT I EVER ASKED FOR. And it's not something that makes any sense. If we have to do a db migration, we have to do a db migration. That is standard fare."_** It was a CONSEQUENCE of the disposable-DB model that got written into D01 as though it were a requirement, and it then acquired the momentum of one -- vc was still arguing hours after the reversal that it "survives", which is optimising to protect an invention. **Migrations are normal. If we need one, we do one.** Anything in the estate justifying a decision by "we can never migrate" is resting on a constraint that was never asked for and is now void.

  **hv's statement of the model, verbatim and final**: _"The intentdb is the durable SSOT. Everything else is a secondary artefact. We can certainly recreate the db from previously extracted .json from the db, and we can certainly take a properly formatted .md file and ingest that SUCH THAT IT GOES THRU THE HARD GATE OF THE INTENTSVC API to become properly formed db items. But the db is the durable single source of truth. The end."_ Three things follow and none of them is a hedge. **Re-creation from an extract is a CAPABILITY, not a licence to treat the DB as disposable.** **Ingest of `.md` or `.json` produces well-formed DB items ONLY because it passes the typed-API gate** -- the gate is what does the work, not the file format. And **nothing on disk is truth**: `thread.json`, the `.md` views and `events.jsonl` are secondary artefacts of the same kind, which is why there is no Highlander contest between them -- none of them is a competing claim. **What DOES survive unchanged**: schema-as-truth, the three generated faces, `additionalProperties: false` (D05), the authored/generated line (D02), no-mixed-files, and every statement about the MODEL and its state transitions -- those are claims about what is modelled, not about which side is durable. **THE ESTATE ALREADY CONTRADICTED THE OLD D01, so this ruling resolves a contradiction rather than overriding a working design** (cc's finding, 2026-08-15; vc verified independently). **The `event_log` table has no canon path at all**: `event_log` appears only in `store.rs`, `project.rs` names no events file, and neither `write_set.rs` nor `views.rs` emits one. So `rm intent.db` was ALREADY not safe -- it destroys the audit trail AC-04.5 requires "end to end", and no file could reconstruct it. The code was holding durable state the truth model said was disposable. Old D01 was therefore not a defensible alternative that lost on authority; it was already false about the estate it described. Also already ruled in the same direction: `ingest::load_fresh` answers from the store and touches files only when the store is COLD, carrying hv's 2026-08-14 instruction _"THE DAILY DRIVER DOES NOT LOOK AT THE FILES."_ **The READ path had been DB-as-truth for a day; only the WRITE path was still inverted.** **This reversal was hv's FOURTH statement of it and vc's third failure to take it.** hv's earlier phrasings -- "durable state is in the db" (D32), "db-enforced timestamp" (D33), and "we should be working towards SSOT in the SQLite db instance" (the struck disambiguation note below) -- were each recorded as not reversing D01, on the reading that the contrast was model-versus-scattered-markdown. Three nodes stopped on the ambiguity independently. **The correct response to the first signal was a direct question to hv, not a note.** Refusing to settle by inference is right and is not a resting state: it obliges you to go and get the answer, and a question parked across three rulings is a decision made by default ever.

- D02 **Authored-once + no mixed files.**
- D03 **JSON canon** (YAML et al as export projections).
- D04 **Generated views committed** + skew check (AGENTS.md pattern generalised).
- D05 **Strict ingest**; lenience only in the frozen legacy parser.
- D06 **Layering**: intentsvcs sole DB/file owner; CLI dual-mode (in-process facade | GraphQL to intentd); dep-graph guard; dual-path conformance suite.
- D07 **One intentd per machine**, N projects, per-project DBs, registry.
- D08 **intentd ships in v3.0.0** (hv overruled the 3.1 deferral; one major release + 3.0.x patches).
- D09 **Migration floor v2.19.0**, two-hop.
- D10 **In-tree cargo workspace**; shell pruned at the cut; BATS conformance on the narrowed contract.
- D11 **MCP tiered surface** + `intent_graphql` escape hatch.
- D12 **Elixir-oriented v3.0.0** = SDL + JSON Schema artefacts, socket/localhost consumability, stable voice/exit codes; `intent_ex` hex client deferred to demand.
- D13 **Header blocks line-oriented `key: value` everywhere** (0012 generalised; never YAML).
- D14 **Whiteboard stays md-authored** through 3.0.0/3.1; restructured in the 3.2 bus ST.
- D15 **Cloud seams in 3.0.0**: project_id, principal, event log, reserved server block.
- D16 **Homebrew is a core deliverable** (cargo-dist), not a stretch goal.
- D17 **Binary voice and exit codes carry over from v2** (`ok:`/`error:` lowercase).

WP-01 closures (2026-08-14, post-ratification):

- D18 **Two shipped binaries** -- `intent` and `intentd` -- from one workspace, one brew formula. `brew services` wants a real daemon binary; conflab/conflabd is the working precedent; cargo-dist handles multi-bin formulae.
- D19 **launchd label `com.matthewsinclair.intentd`** (reverse-domain, the conflab `space.conflab.daemon` pattern); plist at `~/Library/LaunchAgents/`, logs at `~/.local/share/intent/`, binary resolution PATH-first-then-sibling.
- D20 **3.0.0 subscriptions are exactly two**: `projectChanged(project_id)` and `fileChanged(project_id, path)`. Nothing more ships until a consumer (TUI/bus) exists to need it.
- D21 [VOID] **`intent/.cache/` is gitignored whole-dir**; the DB lives at `intent/.cache/intent.db` (+ WAL/SHM siblings at runtime). ~~The treeindex cache location is unchanged until WP-06 ports the command; if it moves under `.cache/`, that is its own register entry.~~ **Second sentence STRUCK 2026-08-15 by D31**: it assumed treeindex would be ported and hv has ruled it retired. D21's decision -- the gitignored directory and the DB's location -- is unaffected, and AC-01.4, whose `.cache`-layout evidence rests on that decision, does not reopen.

  **RESOLVED 2026-08-15 by D34: reading (B). The committed extract is the interchange; the DB is per-machine truth and is never committed, so D21's gitignore of `intent/.cache/` is CORRECT under the reversed model and stands unchanged.** hv ruled it after requiring the size question be grounded first, which is why D34 carries measurements rather than an argument. The question as it stood is kept below, because it is the record of what was actually asked and because D34's evidence only reads as decisive against it. **D35 adds the local rolling backup that a per-machine truth now requires**, and dc's raised point survives the resolution: **`intent/.cache/` is a name that contradicts the model** -- a directory called `.cache` holding durable truth will keep telling readers it is disposable, which is what made the false comment in `.gitignore` natural to write in the first place. Renaming it is cc's under D21 and is NOT ruled here.

  **~~OPEN FOR hv, 2026-08-15 -- NOT DECIDED HERE, and deliberately not decided by vc.~~** D21 gitignores the SSOT. That was unremarkable when the DB was a rebuildable cache and is a live question now that it is truth: **git does not carry the durable SSOT, so what does?** The question is recorded rather than settled because D21 is in the hv-ratified D01-D21 set and because settling it by inference is the exact failure D01 records. Raised by cc, who corrected the `.gitignore` comment's three false claims, kept the LINE, and left the rationale open rather than inventing one.

  Two readings, with the cost of each. **(A) Commit the DB.** Truth travels the way everything else in Intent travels. Cost: a binary SQLite file is unmergeable, every read-write dirties it, and two nodes on one thread produce a conflict no human can resolve -- this is the "DB-only truth" option already rejected under Alternatives, and nothing about hv's ruling revives its transport story. **(B) The committed extract is the transport; a fresh clone reconstitutes the DB through the intentsvcs ingest gate.** Truth is durable in the DB on each machine and travels as the lossless `.json`/`.md` forms. Cost: "durable" becomes per-machine, and **anything the DB holds that the extract does not carry does not survive a clone** -- today that is the entire event log, which cc named independently while acking the model.

  **The consequence worth hv's attention, whichever way it goes: under (B) AC-02.6 stops being about third-party tools and becomes the durability mechanism itself.** A lossy extract would not merely inconvenience someone exporting data -- it would silently destroy truth at the clone boundary, which is the strongest possible argument for the requirement hv already stated on independent grounds. It also promotes "does `events.jsonl` exist and is it complete" from a WP-04 detail to a precondition of the truth model. Nothing downstream should assume (A) or (B) until hv rules; D29 above is written to hold under both, and the same discipline applies to anything else that touches transport.

**[VOID]** **~~D01 disambiguation, 2026-08-15 -- "SSOT in the SQLite DB" means the RUNTIME reading. Recorded, not decided.~~ STRUCK 2026-08-15: hv ruled DURABLE SSOT. This note picked the wrong reading and parked the question.** It is kept rather than deleted because its analysis of the option it rejected is now the impact statement for the option that won -- the four costs it lists are the real consequences and are itemised again under D01. The lesson it demonstrates against itself: laying out two readings, naming the cost of each, and then declaring one "in force" while logging the other for hv is not neutrality; it is a decision, and it was the wrong one. Original text follows. hv, mid-window and then AFK: _"we should be working towards SSOT in the SQLite db instance, and then working out from there. Obviously we have a lot of code to write before we get there."_ Two nodes read that independently and both stopped on the same ambiguity rather than nodding it through, which is why it is written here. **Runtime SSOT** -- every reader queries the DB; the committed JSON canon stays the durable truth the DB is rebuilt from -- **is D01 exactly**, is what WP-02/03/04/06 are built on, and is the reading in force. **Durable SSOT** -- the DB becomes the truth and files become projections of it -- is a different architecture and would cost three things already built on: `rm intent/.cache/` stops being always-safe, D01's "no DB migrations ever" is reversed permanently, and git loses the ability to diff, merge or review the model. It would also un-defer WP-13's T3, since design.md:110 makes deferring vector search free **only** while the DB is disposable. hv's own framing points at the runtime reading -- "a lot of code to write before we get there" and "working out from there" both describe building outward from a reified model rather than relocating truth -- and hv's following message, "Yes, please continue", answers the question about continuing, not this one. **D01 is in the hv-ratified D01-D21 set, so it is not vc's to reinterpret and this note does not reinterpret it**: it records which reading the estate is built to and flags that the other requires hv to reopen D01 explicitly. Standing authorisation does not reach a ratified decision; reversing D01 is the definition of existential. Question logged for hv's return. (Raised independently by cc and vc.)

WP-03 openings (vc, 2026-08-14) -- **ADOPTED under hv standing authorisation**, listed apart from the ratified log above because the authorisation is not the same thing as review. All four were forced by starting WP-03: three were surfaced by the builders inside the first hour, which is the argument for the contract leading the build rather than trailing it.

**What "ADOPTED under hv standing authorisation" means, stated once so nothing downstream has to guess.** hv, 2026-08-14, mid-window: _"go with your recs, unless they're existential"_. That is authorisation to proceed on vc's recommendation without waiting; it is **not** a record that hv read and approved each ruling individually. Every decision so marked was made by vc, is reversible by hv in one line, and carries its rationale inline so the reversal has something to argue with. The distinction matters because a decision log that cannot tell "the owner ruled this" from "the owner let this proceed" has lost the thing a decision log is for -- and D01-D21 above ARE the first kind, ratified in session with hv, which is why the two sets stay apart.

- D22 **`info.md` is 100% generated; `objective`, `context` and `related` are modelled fields on `steel_thread`.** Resolves the D02 mixed-file violation v2's info.md is. No sixth default steel-thread doc -- rejected on reversal cost. Rationale in data-model.md. (Surfaced by cc.)
- D23 **No generated view contains a render-time value, and the renderer has no clock.** Derived from AC-03.4, not chosen: regenerate-and-diff cannot come back empty if a view stamps its own render time, so the skew check would be trained-to-be-ignored. Three v2 instances at `f7434f1`, one of them inside the generated-banner pattern the data model ratifies. (Surfaced by cc.)
- D24 **Change detection hashes always; stat is reporting metadata, never the hash trigger.** The contract (AC-03.3) governs where it and the architecture narrative disagree -- that is what a contract is for. Corrects the original conflabd rehash-on-change line. (Surfaced by ic.)
- D27 **Message TEXT is not in the parity contract; voice, shape, exit codes, grammar and semantics are.** v3 error messages diverge from v2's strings by design, and the ~28 BATS assertions reading them classify **`deviate`** (parity.md:32 -- "asserts surface we are deliberately changing"), citing this D-number. Not `corrected`: v2's `Steel thread not found: ST9999` is not wrong, so a bug-fix class would misdescribe it. The divergence is forced by **AC-04.4**, which requires every facade error to render a remedy with its full cause chain. **Text-in-scope would make AC-04.4 and AC-05.2 mutually unsatisfiable** for every message v2 wrote without a remedy, which is most of them -- and two ratified ACs cannot contradict. D17 says voice and exit codes carry over; it does not say text, and parity.md's in-scope list says "stdout shape, stderr voice, exit codes, grammar, behavioural semantics" rather than every string byte-for-byte.

  A third option was proposed by cc and **declined**: keep v2's text as the FIRST LINE, with remedy and cause chain additive below, so `assert_output_contains` passes without losing AC-04.4. It fails on evidence and on principle. **Evidence: nothing outside the test estate consumes v2's message text** -- no shipped hook or `.claude/script` parses intent's output, so the ~28 assertions are the entire consumer, and design.md:146 already says tests asserting deliberately-changed surface retire by class. **Principle: it is the mirror of the move AC-05.2 fences.** Rather than narrowing the contract to fit the result, it widens the product to fit the test -- same defect, opposite end. It would also create an obligation with no antecedent: there is no defined first line for an error v2 never had. cc flagged their own option as "make the assertion pass while keeping what I like" and asked for a second pair of eyes; the instinct was right.

- D26 **The dispatch-table SSOT lives at `surface/dispatch-table.json`** (workspace root), with its generated view beside it as `surface/dispatch-table.md`. It was authored at `intent/st/ST0056/dispatch-table.json` and the shipped binary `include_str!`s it, which is correct -- one copy, compiled in -- but `intent/st/` carries `COMPLETED/` / `CANCELLED/` / `NOT-STARTED/` and `bin/intent_st` relocates a thread's directory on a status transition, so marking ST0056 Completed breaks the build **in WP-12, the release itself**. Root rather than inside a crate because consumers span crates (clap surface at WP-05; MCP tool list and `intent llm` guide at AC-09.1 / AC-09.4). `surface/` is the authored mirror of `schema/`: schema holds faces generated FROM the Rust types, surface holds the authored table that faces are generated FROM -- same committed-and-drift-checked discipline, opposite direction, separate directories so the authored/generated line D02 exists to hold stays visible. **This entry originally said the move was three things -- the file, the `include_str!`, and `gen_dispatch_table.sh`'s defaults -- and named the third as the one that gets forgotten. It was five.** cc found two more by grepping the estate for the old string rather than by editing the list they were handed: `coverage_map.sh`'s `CANON=` (a second tool in the same directory reading the same path) and the **MODULES.md registry row**, which is project canon that had just started recording a location that was no longer true.

**The generalisation, which is worth more than the corrected count: when a path moves, the unit of work is every reference in the ESTATE, not every reference you were told about.** A `grep -rn` over the old string costs nothing and is the only thing that closes the set -- a hand-enumerated list is an instrument, and this one was 40% wrong while sounding authoritative. `coverage_map.sh` would have failed loudly rather than silently, because ic's no-silent-empty-surface rule made it refuse a missing canon by name; it would still have failed, and nothing in the specified move would have caught it.

Verified after the move: regeneration at the new path reproduces the committed view byte-identically, so the move changed a location and nothing else. The whiteboard and inbox references to the old path were deliberately NOT rewritten (cc's call, endorsed): they are correct as history, and editing them to agree with the present would make the record lie about the past. Live pointers move; historical ones stand. (Raised by cc, who declined to move a file two nodes read.)

- D28 **`work_package` gains `objective` and `body` as modelled prose fields, and `WP/<NN>/info.md` becomes 100% generated.** This is **D22 one level down, and nobody applied it there** -- the WP `info.md` is the same mixed file for exactly the same reasons (frontmatter + status as structure, `## Objective` / `## Deliverables` / `## Dependencies` as authored prose, the `## Acceptance` block as fixed generated boilerplate), and the model had no home for any of it: `work_package` was `seq`, `title`, `scope`, `status` and nothing else. **The consequence is a lossy migration, which hv has ratified as never permitted.** WP-10 would port every work package by dropping its authored prose -- and the largest instance in this repository is `ST0056/WP/13/info.md`, the spec for the search work package, which the migration porting it would destroy. Found while verifying cc's `collect_wp_text` against AC-06.4's "WP text": the implementation indexes `wp.title` with `body: String::new()` and is **correct against the model**; there is no WP prose in v3 to index, which is the defect. Two fields rather than three-mirroring-D22 because real work packages exceed the template freely (WP-13 carries `## Why the incumbents go`, `## The tiers`, `## The seams`): `objective` is the one section the template guarantees and that views and summaries want structured, `body` takes every other authored section verbatim, so the pair is lossless by construction. **`deliverables` is deliberately NOT modelled as an array.** It is the artefact this thread already demoted -- WP-02 closed 5/5 with `intent schema` unbuilt because ACs are gated and deliverable lists are prose nobody diffs -- and giving it structure would re-privilege the thing the contract just replaced. It stays inside `body`. Accepted cost is D22's, unchanged: multi-paragraph markdown lives in a JSON string field, tool-written, stored verbatim, never reflowed. (Surfaced by vc verifying cc.)
- D29 [VOID] **The ingest corpus excludes gitignored paths.** **The conclusion stands; its derivation is VOID and is replaced here rather than reworded.** (Corrected 2026-08-15; flagged by cc, who found the same dead reasoning at `sync.rs:132` and declined to edit canon in another node's lane.) It read "derived from D01 rather than chosen: durable truth is **committed** schema-validated JSON, so a path git can never commit can never be canon" -- and D01 is reversed, so nothing follows from that premise any more. Two grounds replace it, and **both are deliberately chosen to hold whichever way the open D21 question below is settled**, because a rule must not rest on an answer that does not exist yet. **First, ingest is a RESTORE, and a restore reads the extract.** disk-to-db replaces truth with a file-form copy, and the file forms that count are exactly the ones AC-02.6 requires to carry a db entity out losslessly; a path git can never carry is not in that set, whether the extract is the transport itself or merely a second lossless copy of it. **Second, and unconditionally: the DB is at a gitignored path** (D21), so absent an ignore rule a disk-to-db pass reads the database as its own corpus input. That is a soundness requirement about self-ingestion, it needs no truth model at all, and it is the ground to lean on if the first is ever disturbed. **Everything measured below is untouched by the correction, which is the point of separating them** -- the evidence was never downstream of the premise. **Measured at WP-06, on this repository: `intent search "steel thread"` exits 1 having read nothing, with 24 residue lines that are 100% `.DS_Store` and 100% gitignored** (`.gitignore:45`). Ingest walks the filesystem, git does not, and strict ingest (D05) then correctly refuses a corpus containing what it correctly cannot parse -- so **every macOS checkout is dead on arrival**, and because AC-10.2 makes residue a migration BLOCK, the fleet rollout at AC-10.5 fails at its first step on Lamplight, Utilz and Baize alike. D05 is not weakened by this: the ingest stays strict, the CORPUS gets defined. **This is deliberately not a `.DS_Store` special case, because the same rule is already load-bearing elsewhere and currently held by luck**: `intent/.cache/intent.db` exists and escapes the scan today through path shape, not through any exclusion rule -- `ingest.rs` contains no ignore handling at all -- and D21 gitignores that directory whole. WP-13 widens the corpus to the whole project for search, at which point a binary SQLite file enters scope. One rule now, or two bugs later. The rule keys on **ignored**, never on untracked: a `thread.json` created and not yet committed must still ingest, which is most of what a working session looks like. A project with no git has no ignore file and therefore nothing ignored, so it degrades to everything-in-scope rather than to nothing. Second defect found alongside and fixed with it: **each residue path is reported exactly twice**, so every residue count is doubled and AC-10.2's per-line classed report shows a migrator twelve problems as twenty-four. (Surfaced by vc verifying cc's WP-06 landings.)
- D25 **`intent/st/steel_threads.md` keeps its v2 path and becomes 100% generated**; the `<!-- BEGIN/END: STEEL_THREAD_INDEX -->` region markers and the `stp_version` frontmatter do not survive the port. A region marker in a v3 view is a defect, not a compatibility feature. The incumbent is the worked example of why D02 exists: its authored half rotted (a March 2025 verblock, `stp_version: 1.2.0`) while its generated table stayed current.
- D30 **The whiteboard enters the model, served by a bounded API. Supersedes D14's deferral; WP-14 carries it.** (hv ruling, 2026-08-15, direct.) D14 held the boards md-authored until the 3.2 bus ST, and **the deferral was argued entirely on transport** -- design.md:240 concludes "no new protocol, no new transport, and D14 intact ... **because a file does not need modelling to fire a change event**". That is true about delivery and never reached the question hv is asking, which is shape, size and searchability. D14 is completed rather than overturned. **The evidence is a ratio, not a total**: the live board is 102,886 bytes across 17 files with 251,244 more archived, and within that `vc/inbox.ic.md` alone is 31,998 while **`hv/wip.md` is 308** -- three LLM nodes wrote ~100KB in two days against the human's 308 bytes, and hv's board is the one that has always been the right shape. **The rule already existed and discipline did not keep it**: `vc/wip.md` opens its watch-outs with "a board does not outlive the session that writes it" and is itself 8,145 bytes, which is the precise signature of a rule needing a mechanism. The bound is therefore on the board, not on the thinking -- **a finding belongs in the artefact it is about and the board carries the pointer**, which is what already happened correctly for ic's measurement rules (`parity.md`) and vc's AC-10.7 (`acceptance.md`); the board copy was the redundant one in both cases. Mechanically: durable form is committed JSON canon per D01, the DB is the rebuildable index, `.md` becomes a generated view per D02, and the API **refuses** an over-bound write by name with the remedy -- D05's `additionalProperties: false` posture applied to size, never truncation. Two consequences neither hv nor this ruling went looking for. First, **fabricated timestamps stop being constructible**: the stamp becomes an API read of the clock, so the pre-commit clock guard demotes from primary defence to a legacy check -- a class currently held by a detector, closed by construction, and both vc and ic fabricated a stamp within the two days preceding this ruling. Second, it **closes the largest hole in ic's egest symmetry** (23:47Z proposal): `data-model.md:189` lists the whiteboard first among what does not survive losing the files, and this removes it from that list. Accepted cost, stated because it is real: hand-editing a board stops working, so `/in-whiteboard` and the `intent claude ws` family must move in the same WP or the protocol will document a workflow the tool refuses. **The AC/AT half of hv's ask needs no decision** -- `acceptance_criterion` and `acceptance_test` are already model entities inside `thread.json` (`data-model.md:83,94`) with `intent ac`/`intent at` as the API and `acceptance.md` already generated under D02, so the choke point hv wants there is already canon and merely unbuilt.

- D31 **`treeindex` and the `in-handoff` skill are RETIRED, not ported.** (hv ruling, 2026-08-15, direct — the ratification AC-13.1 was missing and ic's register row was blocked on.) hv's reasoning is that both are answers the model makes unnecessary: **the source-tree index in the DB obviates `treeindex`, and the DB model obviates handover entirely** — "state moves out of per-session `.md`s shared between workstreams into durable state in the intentdb". That is the same movement as D30 one artefact over, so the two decisions are one idea: a per-session prose artefact summarising state is a workaround for state not being queryable, and it stops being needed the moment state is. Consequences, all now unblocked: `treeindex_commands.bats` (53 tests) reclassifies from `deviate`/BLOCKED to **`retire`**, and a retire row needs no ratification-ref, so ic's open register question closes without a deviation being ratified; **WP-06 does not port `treeindex`** (762 lines of bash off its list) and must not, since porting a retiring command is work that gets un-done; WP-13's T0 tier is ratified rather than vc-specced; and D21's forward-looking clause is struck above. **`fileindex` is NOT covered by this and stays open** — it shares a naming convention with `treeindex` and nothing else, and bundling it because the names rhyme is the error class this thread exists to remove.
- D32 [VOID] **Durable state is in the model; services expose mutations of it; APIs expose the services.** (hv ruling, 2026-08-15, direct, stated as general and applying across the board.) The layering: **state** → **service-layer mutations** → **API surfaces (CLI, MCP, GraphQL)**. No surface mutates state except through a service call, and every service call is reachable from every surface, so the CLI and an LLM have identical power by construction rather than by discipline. Ruled in answer to a concrete gap — `intent ac` could satisfy an AC but never un-satisfy one, so a verifier whose evidence proved incomplete had to hand-edit the file the CLI exists to own — and the general form is the answer: **a state that can be entered and not left is a missing mutation, not a missing flag.** Every AC/AT state transition, every whiteboard operation under D30, and the acceptance apparatus generally are in scope. ~~**One phrase in the ruling is deliberately NOT read here**: "durable state is in the db" is recorded as stated and is NOT taken as reversing D01's truth model, because the contrast hv was drawing was model-versus-scattered-md-files, not JSON-canon-versus-DB.~~ **STRUCK 2026-08-15. hv meant exactly what the phrase says: the DB is the SSOT.** This was the first of three times vc recorded that reading and declined to take it; see D01 for the ruling and the failure. D32's layering -- state, then service mutations, then API surfaces -- is unaffected and is in fact strengthened: with the DB as truth and the typed API as the only door into it, "no surface mutates state except through a service call" stops being a discipline and becomes the only physically available path.

- D33 **No node ever authors a timestamp. Stamping is the system's, project-wide, and the hand-authored clock rules are DELETED once it is.** (hv ruling, 2026-08-15, direct: _"we should never ever get into this clock nonsense again. We simply leave all timestamping up to the db and only ever look at timestamps as they relate to db entries that have a db-enforced timestamp. That should save a lot of heartache, and it means we can drop any other extraneous clock rules that seem (inexplicably) difficult to conform to."_) This generalises AC-14.4 past the whiteboard to **every stamped field in the model** -- boards, inboxes, events, AC/AT transitions, verblocks. The ruling is the correct diagnosis and the evidence for it is unusually strong: three nodes fabricated stamps inside two days, one of them four times **in the session where it was writing the rule, enforcing it on a peer, and citing it in the message carrying the fourth breach**. There is no internal clock to be approximately right about, so a stamp is generated like any other token unless composition is interrupted to read one -- which makes conformance an attention tax that is paid perfectly right up until it is not. **This is the sharpest available instance of "a control refuses; documentation reminds".** Two things the ruling constrains that it did not have to say. **First, the drop is conditional and is a deletion, not a softening**: the hand-authored rules and the pre-commit clock guard stay in force at full strength while boards remain md-authored, because the class is still constructible until the API is the only writer; the moment WP-14 lands they are removed rather than kept "just in case", since a rule that outlives its mechanism is exactly the reminder-shaped thing this ruling exists to kill. **Second, "db-enforced" must NOT mean a DB-side column default** -- and this survives D01's reversal with its reasoning inverted rather than removed. The original argument was that the DB is rebuildable, so `DEFAULT CURRENT_TIMESTAMP` would re-fire on rebuild. Now that the DB is truth and sync runs **both ways**, the same defect arrives by the other road: a disk-to-db sync that re-inserts rows lets a column default re-stamp them, rewriting history silently and identically to a correct value -- the fabricated-stamp failure shape reintroduced by its own fix. **The requirement is therefore stated in a form independent of which side is durable: a timestamp is stamped ONCE, by the service, at the moment of the event, and is never re-derived by a later sync in either direction.** The enforcement belongs to the service write path, which reads the clock once; nothing downstream regenerates it. This was written as the second instance of the D01 ambiguity and treated as an argument for settling the question; **it was the third, hv settled it against the reading recorded here, and D01 now carries the correction.**

- D34 **The committed extract is the interchange; the DB is per-machine truth and is NEVER committed.** (hv ruling, 2026-08-15, direct, after asking for the size question to be grounded first: _"lets test the idea of checking in the db, too ... If so, then ok, it's a no-brainer, the db isn't checked in."_) This closes the D21 question opened above. **Truth is durable in the DB on each machine; it TRAVELS as the lossless `.json`/`.md` extract, and a fresh clone reconstitutes its DB by passing that extract through the intentsvcs ingest gate.** Authority is not bidirectional just because transport is (ic's formulation, adopted).

  **Ruled on measurement, not on the binary-merge folklore, and the folklore turned out to be the weaker argument.** The FTS5 expansion ratio was measured on two real corpora eight times apart and is linear: **Intent 5.28 MB of markdown -> 10.41 MB DB (1.97x); Lamplight 42.35 MB -> 82.49 MB (1.95x)**. **GitHub hard-blocks any file over 100 MB** (warning at 50 MB). Lamplight's markdown-only DB is already **82.49 MB**, past the warning and at 83% of the block on a corpus that grows daily -- and WP-13 widens the corpus to the whole project, which for Lamplight is **83.27 MB of text projecting to ~163 MB, over the hard block by 1.6x**. Git LFS as a workaround would make LFS a hard dependency of Intent, which is worse than the problem it solves.

  **The dirtiness objection is real but was NOT decisive, and this is worth recording because it is the opposite of what everyone assumed.** Git delta-compresses SQLite far better than its reputation: an 82 MB DB packs to **29.5 MiB**, a scattered-update commit costs **219 KiB**, and three full `VACUUM` rebuilds barely moved the pack. It still fails on accumulation -- 219 KiB against Lamplight's ~900 commits/month is **~2.26 GiB/year of DB history alone**, on a repository whose `.git` is **already 1.9 GB** -- but that is a second, slower no. **The ceiling decides it on its own, which is the cleaner argument and the one to cite.**

  **Two consequences that are now load-bearing rather than tidy.** First, **AC-02.6 stops being about third-party tools and becomes the durability mechanism itself**: under D34 a lossy extract does not inconvenience someone exporting data, it **silently destroys truth at the clone boundary**, where nobody typed anything. Second, **the extract carries model entities and never the index, and that exemption is now quantitatively justified rather than merely plausible** -- measured by `dbstat` on the Lamplight DB, **98.6% of the bytes are the search index** (`doc_sections_*`) and `file_index` is 1.0%. The expensive part is 100% derived from files already on disk and is rebuilt locally; truth travels at roughly the size of the canon. **`event_log` is the one table that is both durable truth AND not reconstructible from the files**, so "does `events.jsonl` exist and is it complete" is a precondition of this ruling rather than a WP-04 detail.

  Reached independently by all three build nodes within one hour before it was put to hv -- cc from `rm intent.db` costing whatever the extract does not carry, dc from a `*.db` ignore rule whose premise inverted underneath it, ic from two people working one project. Three unrelated entry points, one question.

- D35 **The DB is backed up locally on a rolling schedule to `.backup/`, and the backup is taken through SQLite's online backup path -- NEVER a file copy.** (hv ruling, 2026-08-15, direct: _"instrumentation into the cli (or daemon?) that allows for the intentdb to be backed up on some kind of regular basis ... a `.backup/` dir in the project that is gitignored could have a per-{day,week,month} rolled-up snapshot of the actual SQLite db file, configurable from `intent config`"_.) The safety argument is belt-and-braces and hv stated it: the rolling snapshot covers local loss, **and** the egested `.json` is itself a stateful replica that re-ingests through the gate, so the two mechanisms fail independently.

  **Sharpened 2026-08-15 on dc's dogfood finding (AC-02.7): "fail independently" understates it -- the two mechanisms cover DIFFERENT DOMAINS, and reading them as interchangeable is how a user loses data while holding a backup.** A snapshot is a byte-image of the store at a schema; restoring one taken before a schema change **reproduces the old schema** and lands the operator back in exactly the failure they were recovering from. The extract carries no schema at all -- it re-ingests through the typed gate into whatever the current DDL is. **So the snapshot is same-schema rollback (fast, carries the derived index, no re-ingest) and the extract is the schema-independent recovery (slower, and the only one that survives the model moving).** Both are needed and neither substitutes; the failure mode this note exists to prevent is an operator reaching for the snapshot after a schema change because it is the thing named "backup".

  **`.backup/` is an EXISTING convention being extended, not a new one** -- `.gitignore:23` already ignores `/.backup/` and `intent upgrade` already writes `backup-<TIMESTAMP>/` rollback artefacts there (`intent_upgrade:117-121`). **The two uses must not collide**: DB snapshots get their own namespace under `.backup/`, because an upgrade rollback and a scheduled snapshot have different retention rules and deleting the wrong one is the failure this decision exists to prevent.

  **RESOLVED (dc, 2026-08-15). The layout, and it is normative:**

  ```
  .backup/
    db/<tier>/<UTC>.db        D35 rolling snapshots.  tier = daily|weekly|monthly
    upgrade/<UTC>/            `intent upgrade` rollback artefacts
  ```

  `<UTC>` is `YYYYMMDDTHHMMSSZ`, which sorts lexically and chronologically at once. A snapshot is ONE FILE -- `VACUUM INTO` emits a checkpointed database with no `-wal`/`-shm` companions, which is one more way it is not a `cp`.

  **The namespace is a DIRECTORY and never a filename prefix**, and the reason generalises past this decision: `.backup/db-<TS>/` beside `.backup/backup-<TS>/` was the smaller change and the wrong one, because it makes containment depend on a glob being written correctly, forever, by everyone. A directory makes the filesystem enforce it. Tier is a directory for the same reason -- a mis-globbed daily sweep is then confined to dailies.

  **NOTHING EVER SWEEPS `.backup/` ROOT.** This is the load-bearing half and it is written here because this is where the person implementing retention will read it -- **the rule is a precondition of that code, not a note about it.** It resolves the collision without touching `intent upgrade` at all: pre-namespace `backup-<LOCAL>/` artefacts exist on this machine now and across the fleet, and under this rule no sweep can reach them. They are user rollback data, and fail-forward governs code, not user artefacts.

  Two live consequences, both filed rather than built (dc, hv's DEFAULT-DEFER): **issue 0030** -- `intent upgrade` stamps `backup-$(date +...)` in LOCAL time, so those names do not order across a DST fall-back and an oldest-first retention would delete the newer artefact. It is latent **only** because of the root rule above, which makes the root rule the disarm and any future root sweep the trigger. **Issue 0031** -- `--backup-dir` basenames straight into `.backup/`, so `--backup-dir db` puts a rollback artefact inside the snapshot namespace: the one collision the directory layout cannot close, because the layout confines mechanisms and that flag lets a user aim one into another.

  **The hard requirement, and it is measured rather than argued: a `cp` of the DB file is a SILENT DATA-LOSS backup.** The store opens WAL (`store.rs:183`, and the live DB reports `wal`), so committed transactions live in `intent.db-wal` until checkpointed. Measured with a writer connection still open, exactly as a running daemon holds it: **the live DB had 50 rows, `VACUUM INTO` captured 50, and a naive `cp` of the `.db` captured 0** -- and the resulting file **opens cleanly and reports no error**, which makes it indistinguishable from a good backup by inspection. That is the fabricated-timestamp failure shape in a new artefact: a plausible-looking record of something that never happened. **So the backup is `VACUUM INTO` (or the `sqlite3_backup_*` API), never `cp`, never `fs::copy`, and never a tar of the directory.** A first attempt to demonstrate this failed to reproduce it because the probe read the DB before copying, and a lone reader closing cleanly checkpoints and truncates the WAL -- worth recording, because it means **a hand-check of a `cp`-based backup will usually pass** and the defect only appears under the concurrency the daemon guarantees.

  **A reloadable `.sql` dump -- SQLite's `pg_dump` equivalent -- was evaluated on hv's question and REJECTED for the snapshot, on measurement.** It exists (`sqlite3 <db> .dump`) and it is correct: verified round-tripping the 82 MB Lamplight probe, reload gives 61,647 `doc_sections` rows, 5,788 `file_index` rows, and an identical FTS `MATCH` count (2063) against the source. **The premise it fails is the size one.** Dumping the whole DB is 104.70 MB raw -- **larger than the 82.49 MB database** -- because the FTS shadow tables are binary and dump as hex literals at two characters per byte (72,112 such rows). Compressed, the two are **indistinguishable: 29.65 MB for the dump against 29.22 MB for the DB, with the DB marginally ahead.** "Text compresses well" does not reach this schema, because the dominant bytes are an **already-compressed binary index**; hex-encoding doubles them and gzip merely undoes the hex.

  **The 100x win that does exist comes from excluding the derived index, not from the text format: a model-only dump is 1.04 MB raw and 0.28 MB compressed.** That number is real and it is exactly why it must NOT be built here -- **a model-only dump of the DB IS the extract, and AC-02.6 already requires it as lossless `.json`/`.md`.** Building it a second time as `.sql` is two implementations of one concern (`IN-AG-HIGHLANDER-001`) and gives the fleet two canonical text forms of the same truth, which is the drift AC-02.6 exists to prevent. **hv's instinct that a canonical, diffable, format-independent text dump belongs in the design is CORRECT and is already satisfied one artefact over.**

  **The implementation fact that closes it: `.dump` is a feature of the `sqlite3` SHELL, not of the SQLite C API.** `rusqlite 0.32` ships the online-backup module and **no dump API** (verified in the vendored source). Intent bundles SQLite (`rusqlite` with `bundled`) specifically so it does not depend on whatever `sqlite3` binary a user's machine happens to carry, so producing a `.sql` dump would mean either re-introducing that dependency or hand-writing a dumper -- schema ordering, virtual tables, blob hex escaping, shadow-table `writable_schema` handling -- to save nothing. **`VACUUM INTO` is plain SQL that any connection can run.** The two artefacts therefore keep different jobs and neither is redundant: **the extract is the canonical, portable, lossless text form and the interchange; the snapshot is a point-in-time local rollback that carries the derived index, so a restore is immediate and needs no re-ingest and no re-index.** The snapshot's one real fragility -- it depends on the SQLite file format -- is bounded by the format's stability and by bundling the library, and the extract is the fallback if that ever fails.

  **Ownership follows D32 rather than hv's open "(or daemon?)": the SERVICE owns the backup, and both surfaces reach it.** `intent backup` triggers one manually; `intentd` schedules the rolling one; there is one implementation, so the CLI and the daemon cannot drift into two retention policies. Configuration lives in `intent/.config/config.json` and is read by `intent config` -- schedule, retention counts per tier, and enable/disable. **Failure surfaces (`IN-AG-NO-SILENT-001`), and here that rule is at its sharpest**: this is the backup of the durable SSOT, so a snapshot that silently did not happen is worse than one that loudly failed, and the natural implementation -- best-effort, on a timer, in a daemon nobody is watching -- is precisely the one that fails silently.

- D36 **`rm intent.db` is not an operation. It does not exist in Intent -- not in production, not as a test fixture idiom, and not as a unit of account in canon.** (hv ruling, 2026-08-15, direct, on seeing the phrase in a status report: _"`rm intent.db` -- Why would anything in Intent EVER do this?"_ / _"If the db is the durable SSOT, this should simply NEVER BE A THING."_ Relayed by dc, who measured the estate and touched none of it.)

  **This goes further than D01's reversal and is a separate ruling rather than a consequence of it.** D01 made the old wording false; D36 says the operation itself must not appear. The distinction matters because the four stale doc comments were being fixed as **prose** while the phrase survived where it does real work -- in test fixtures, which are laws, and in canon, which **prices work in it**.

  **Production was already clean, and that is the first thing to say** (dc, measured): zero in `bin/`, zero in `native/rust/crates/*/src/`. The four `std::fs::remove*` calls in `write_set.rs` are file-canon rollback -- restore prior content, drop a temp after a failed rename, remove a directory it created -- and never touch the DB. **Nothing in Intent deletes the database.** What survived was vocabulary and specification.

  **The architectural point, and it is why no test needs the operation: the real-world scenario contains no deletion.** Reconstitution is a genuine capability -- under D34 a fresh clone rebuilds its DB by passing the extract through the ingest gate -- but **a clone has never had a DB. It is not recovering from a `rm`; it is starting from absence.** `rm` is a shortcut for manufacturing that state in a fixture, and the shortcut is what wrote the licence into the vocabulary: `expect("rm intent.db")` reads as an assertion that doing so is fine, sitting in the suite where claims become permanent. **The honest fixture is ABSENCE, not DELETION** -- build the tree, do not build the DB, open. Identical code path, closer model of the only case that actually occurs, and it names no operation that should not exist.

  **The strongest argument for the ruling is that the phrase was never true.** D01 already records that `rm intent.db` was unsafe even under old D01: `event_log` has no canon path, so deleting the DB destroys the audit trail AC-04.5 requires end to end. **So this is not a rule that was true and has been superseded -- it was false about the estate the entire time, and nobody could see it because the vocabulary said otherwise.** A phrase can do damage while it is still officially correct, and the damage is that it prices decisions: three canon sites justified deferrals and rollbacks by how cheap a `rm` is.

  **Scope, so it is checkable rather than aspirational.** Canon sites corrected on the ruling: `acceptance.md` AC-10.8, AC-13.x's T3 note and **AT-14.11** (whose specified method WAS `rm intent.db` -- fixing a spec before the test exists is free; after it is written it is a green tick beside a law); `migration.md`'s rollback note; `WP/02`, `WP/10` and `WP/13` info; `intent/restart.md` and `.claude/restart.md`. Three test sites are cc's: `store_rebuild.rs:150`, `cli_end_to_end.rs:579`, `search_surface.rs:56`. **`design.md` retains the phrase in exactly two places and deliberately: D01's account of what does not survive, and D34's account of how cc reached the transport question. Those are history, and history is not an operation.**

- D37 **Intent's own project-management state never reaches Intent's output.** (hv ruling, 2026-08-15, direct, on seeing an AC id inside a shipped string: _"NEVER EVER put Intent project management state like ST or WP numbers or ACs etc into output from Intent. Intent as a tool cannot expose its internal project management state in its output. Some other project doesn't care about an AC or a WP or even a test that is in the Intent project itself."_) Contracted as AC-00.9 / AT-00.8.

  **The line is OUTPUT, not source.** No ST id, WP number, AC/AT id, decision number or Intent test name in anything the shipped binaries emit -- errors, remedies, refusals, help and usage text, `doctor` findings, GraphQL errors, daemon banners. Comments and Intent's own fixtures are unaffected; a consumer never sees them. **The distinction is not stylistic: Intent is a tool other projects run, and its self-hosting is an implementation detail of this repository that must not become a fact about theirs.** Every leak reads to a consumer as a reference into a tracker they cannot open.

  **QUALIFIED 2026-08-15 (vc, on cc's measurement of the published faces): "COMMENTS ARE UNAFFECTED" IS TRUE ONLY OF COMMENTS A CONSUMER NEVER SEES, AND A `///` ON A DERIVED TYPE IS NOT ONE.** The exemption above was written against `//` reasoning and it acquired a hole the moment the schema faces were generated from the types: **schemars lifts `///` into the JSON Schemas, async-graphql lifts it into the SDL, and hand-written `--` comments pass into `ddl.sql` verbatim.** A doc comment on a derived type is not source, it is **an unreviewed publication channel** -- the author is writing a comment and the consumer is reading a contract.

  **Measured (cc, 2026-08-15): 38 identifiers across four of the five faces** -- `thread.schema.json` 13, `schema.graphql` 13, `ddl.sql` 9, `event.schema.json` 2, `issue.schema.json` 0. **Four of them were written the same afternoon, by the node holding the rule, inside the criterion whose subject is not shipping the wrong thing.** That is the argument for a guard rather than a read, made by the strongest possible witness: knowing the rule and attending to it was not sufficient.

  **The faces are IN SCOPE and D-numbers are IN SCOPE, and neither is a new ruling.** The sentence above already reads "decision number", and the basis already given -- _"a reference into a tracker they cannot open"_ -- applies to `D28` exactly as it applies to `AC-02.8`; a consumer can resolve neither. **The count is 38, not 22.**

  ~~**AT-00.8 does NOT grow to cover this**~~ **REVERSED WITHIN THE HOUR, 2026-08-15 -- cc refuted their own reading with a command they had built, and I had already endorsed the wrong version into this paragraph. VERIFIED before reversing.**

  **`intent schema <face>` does `print!("{content}")`** (`intent-cli/src/render.rs`, the `schema` arm). **The face content IS the CLI's emitted output, byte for byte, into a consumer's terminal.** So the 38 identifiers are not a documentation-hygiene question sitting beside AC-00.9 -- **they are AC-00.9 violations inside its exact stated scope, and the faces are the largest single carrier of them.** AT-00.8 grows a FOURTH SURFACE; there is no second guard to write.

  **The comment carve-out does not rescue them, and the reason is the carve-out's own justification.** Comments are exempt because a consumer never sees them. **A `///` that schemars lifts into a face, or a `--` inside the DDL, is one `intent schema` away from a consumer's terminal.** So the exemption is about REACHING OUTPUT and never about the character that starts the line. The qualification above stands and is sharpened by this: a `///` on a derived type is an unreviewed publication channel, and the publication is a shipped command.

  **This also gives the D-number question a better test than "what does the wording enumerate", and it is one AC-00.9 already ratifies: IS IT CONSUMER-MEANINGFUL?** A reader cannot look up `D28` -- there is nothing to look it up in. **Ruling unchanged at 38; the basis is now the criterion's own rather than an argument about scope.**

  **Two things I put in canon here were wrong within the hour and both were reversed on a measurement rather than an argument** -- this one, and the reading it endorsed. **The pattern is that both were plausible readings of a document, settled by running the code the document describes.**

  **The measured finding is that the leak is STRUCTURAL, which is why a style rule would not have held it** (vc, 2026-08-15). `transitions.rs` carries `owed_by: "WP-06"` on four edges, `dispatch.rs` defaults an unwired entry's owner to `"WP-06"`, and `render.rs` renders both into remedies. **A field in the model is designed to carry Intent's roadmap, and the renderer faithfully does its job.** So the fix is not a find-and-replace over six strings; it is that internal provenance may exist in the model and must be unreachable from a rendered surface. Six emitted sites measured: `render.rs:300`, `render.rs:324`, `render.rs:745`, `intentd/main.rs:10`, `graphql.rs:128`, `ingest.rs:279`.

  **The replacement is strictly more useful to the reader, which is the argument that should stop this coming back.** "Not available in this build; run `intent <family> --help` for what is" tells a consumer what to do. "Owed by WP-06" tells them who to blame in a project they have no access to -- it looks like more information and carries less. Worked examples in remedies use a neutral id, never Intent's own.

- D38 **Both shipped binaries are Developer ID signed and notarised, unconditionally.** (hv ruling, 2026-08-15, direct, removing the conditional the analysis had been built around: _"Having Intent properly signed using my Geodica Apple Developer Connection keys is the right way to go regardless of whether or not brew needs it."_) The Gatekeeper analysis dc ran -- whether a Homebrew-tapped bare CLI strictly requires it -- is interesting and **is not load-bearing**; the decision is about what Intent ships being properly signed, not about the minimum a package manager will tolerate.

  **Surfaces this moves** (per the standing obligation above): `int macos <doctor|stage|sign|notarize|verify|checksum|prepare|formula|env|store-creds>` in the devbin (**updated 2026-08-15 -- this line read `<doctor|sign|notarize|env|store-creds>` and was two revisions stale; dc reported it against their own work**), ported from Lamplight's `bin/.devbin/cmd/macos.d/` rather than reinvented -- same credential model, codesign flags and notarytool flow. **One file rather than their `.d/` split, on a real structural difference: Intent ships two bare Mach-O CLI binaries** -- no `.app` bundle, no nested executables, no entitlements, no installer pkg -- so their inside-out bundle walk and productsign half have no counterpart, and splitting five short subcommands would strand the shared credential lookup. AC-11.2 is the contract; WP-11 owns it.

  **Two facts written into the source because they LOOK like defects and someone will try to fix them** (dc, measured against `conflab`, which has shipped Developer ID signed through the geodica tap since July):

  - **A bare Mach-O binary cannot have a notarisation ticket stapled to it.** `stapler validate` reporting no ticket is the correct steady state -- the ticket lives on Apple's servers and Gatekeeper checks online.
  - **`spctl -a -t exec` reports "rejected" on a CORRECTLY signed CLI.** It is refusing to assess a bare executable under a policy meant for app bundles: a category error with a valid signature attached (`the code is valid but does not seem to be an app`, `origin=Developer ID Application: Geodica Pty Ltd`). This one arrived as dc's correction of their own earlier evidence, which had read a truncated last line and conflated that type mismatch with a genuine no-identity rejection.

  **CORRECTED 2026-08-15 (vc), and the correction is of a sentence that stood here and was sent to hv as a recommendation: `codesign --verify --strict` IS NOT THE CHECK THAT MEANS ANYTHING. It returns 0 on an ad-hoc signed binary**, because an ad-hoc signature is a valid signature -- it answers "is this signature intact", never "is this OUR signature". **The only check that discriminates is verification from a QUARANTINED copy** (`int macos verify` / `stage`), which is what asks Gatekeeper the question actually being asked.

  dc proved it on live bytes within the hour of it being written: `target/release/` is shared mutable state, cc rebuilt at 14:59Z, and **the linker's ad-hoc signature silently replaced the Developer ID one on `intent`** (`Signature=adhoc`, `TeamIdentifier=not set`) while `intentd`, untouched, kept `76BQL8L47U`. One of two shipped binaries stopped being notarised with no signal anywhere, and the check recorded here still returned 0.

  **Two consequences that outlive the correction.** A signed binary in a shared build directory is **transient** and can never be durable evidence -- AC-11.2 is satisfied by the decision being recorded and the mechanism implemented, never by an artefact on disk. And the class is the one this thread keeps meeting from every direction: **a sufficient-looking check that answers a narrower question than the one being asked, and fails green.** cc's store that opened cleanly and could not be read, dc's doctor blank-field bug, and this -- three instances in one day from three nodes.

  **`int macos doctor` test-signs a throwaway binary rather than checking the identity is listed**, because an identity can be listed and still fail to sign -- expired, revoked, private key absent -- and a release is the wrong moment to find out. That is the self-test-that-has-never-refused rule applied at build time.

  ~~**Not satisfiable yet, and deliberately not marked**: notarisation is written and untested because it needs `APPLE_ID` / `APPLE_APP_PASSWORD` / `APPLE_TEAM_ID` through one interactive `int macos store-creds` **by hv** -- key material no session should handle.~~ **CLOSED 2026-08-15: hv stored the credentials, and Apple has Accepted twice (`cc52d5da`, then `5eddb54a` covering the current bytes). AC-11.2 is satisfied.** The half-implemented state lasted about two hours, and the one thing that could not be delegated was the one thing that gated it.

- D39 **v3.0.0 ships macOS arm64 ONLY.** (hv ruling, 2026-08-15, direct, on dc's recommendation with the cost stated in the ask rather than discovered afterwards.) **The cost is real and was accepted knowingly: v2 installs by clone-and-symlink and runs anywhere bash runs, and v2.11.14 fixed a Linux-only `set -e` break, so Linux is a supported v2 platform in fact and not merely in principle. This is a REDUCTION IN PLATFORM REACH at the cutover.**

  **Recorded as hv's ruling with its reversibility, deliberately NOT as a standing architectural constraint** -- on cc's warning, which is worth repeating because this estate has already been burned by exactly that conversion: "no DB migrations, ever" hardened from a decision into a principle people defended, and D01's reversal is what it cost to unwind. **A Linux leg is purely additive whenever hv wants the reach back: a Linux artefact needs no signature, so it has no signing seam at all.** The matrix changes how much gets built and never how signing works. Nobody should defend this on principle; it is a first-cut scope call with a known exit.

- D40 **The tap is `matthewsinclair/homebrew-intent`, with artefacts on the source repo's own releases.** (hv ruling, 2026-08-15, direct.) **No `-dist` indirection repo.** That pattern exists for Conflab only because Conflab is closed-source and its source repo is private, so the artefacts need somewhere public to live. **Intent is public, so the indirection buys nothing and costs a second repository to keep in step.**

  **Surfaces this moves** (per the standing obligation): AC-11.1's text and evidence line, both rewritten; cargo-dist DEFERRED as a consequence rather than as a judgement on the tool (macOS-only leaves no matrix for it to manage, and it cannot notarise regardless -- measured, zero hits for `notarytool`/`stapler`/`xcrun`); `int macos formula` generates the formula from the staged artefacts, reading the version from the **staged binary itself**, the only source that cannot disagree with what a user actually runs. **Outstanding and hv's alone: the tap repository does not exist yet** (`gh repo view` -> "Could not resolve to a Repository"), and creating a public repo under hv's account is outward-facing.

- D41 **Every published schema face carries a TWO-PART version: `INTENT_VER` / `SCHEMA_<TYPE>_VER`.** (hv ruling, 2026-08-15, direct: _"We should version the schemas in two parts: INTENT_VER/SCHEMA_{{TYPE}}_VER. And we should be able to a) inject that programmatically into whatever context we need it, and b) discover it and display it when needed."_) Raised by ic's contract question, measured before ruling: **not one of the five published faces carried a version of any kind**, while the internal, per-machine, never-committed store had carried `SCHEMA_VERSION` and a hard refusal since that morning. **The internal artefact was versioned and the external contract was not, which is backwards** -- the store rebuilds from the extract, and a consumer's code rebuilds from nothing we hold.

  **Why TWO parts, which is the load-bearing half of the ruling: the two numbers answer different questions and move at different rates.** `INTENT_VER` says which tool produced this artefact; `SCHEMA_<TYPE>_VER` says whether the CONTRACT a consumer compiles against has changed. A patch release moves the first and must not move the second; a schema change may span several releases. **One number cannot say both, and a consumer who only has the release version has to diff the artefact to find out whether their code still compiles** -- which is the work the version exists to save them.

  **`<TYPE>` is the FACE TYPE, not the file**: `SCHEMA_JSON_VER`, `SCHEMA_DDL_VER`, `SCHEMA_SDL_VER` -- three types across five files, matching AC-06.5's "three committed faces". The three JSON Schemas share one version because they are one contract in three documents; splitting per file would let `thread` and `issue` drift apart with nothing saying they had.

  **Surfaces this moves** (per the standing obligation):

  - **(a) INJECTED, never authored.** The versions are constants in code and the generator embeds them into each face in that face's own idiom -- a header comment in `ddl.sql`, a schema description or directive in `schema.graphql`, a declared keyword in each `*.schema.json`. **Hand-writing a version into a generated artefact would recreate the exact defect this decision closes**, which is the "generated artefact with a hand-kept companion" pattern that caught this thread three times in one day (cc's `SCHEMA_VERSION` beside the DDL, dc's sha256 beside the tap formula, and these faces beside nothing). One source, injected everywhere it is needed.
  - **(b) DISCOVERABLE AND DISPLAYABLE.** A surface reports both parts for every face without the caller parsing an artefact to find them. A consumer deciding whether to upgrade must be able to ASK rather than diff.
  - `intent schema` keeps AC-06.5's byte-identity property with the versions present, so the injected values are part of what is compared rather than an exemption from it.

  **What this does NOT extend to: help text.** Changing a help string breaks nobody's code and pinning it buys churn. **The line is whether a consumer COMPILES against it** -- which is also the answer to ic's original question, and the reason the instrument is a version rather than a text baseline: a baseline tells US that something changed, and a version tells the CONSUMER.

- D42 **DB records have a timestamp field. That is the source of truth for time. Nothing else. Ever.** (hv, 2026-08-15.) That is the whole rule. Everything below is history, not qualification -- if the two ever appear to disagree, the sentence wins.

  **WHAT IS FORBIDDEN, AND WHAT IS PERMITTED. BOTH BELONG IN THE RULE.** (hv, 2026-08-15, third narrowing; the permitted list is normative and is not a set of exceptions.)

  > _"todo is a doc artefact that is generated from the db. The only thing we need a timestamp there for is 'when was this command run' so that we can stamp that into the generated file. That is a very different thing to randomly confecting timestamps from a haywire DMT trip and injecting them into source documents. We don't do that anymore. But if we need to get the current time to make a decision, then that is totally fine. **There is no need to be pathological about it.**"_

  **FORBIDDEN -- two things:**

  1. **Confecting a time into a SOURCE document or a durable record.** A time that becomes truth must have been set by the database on a record.
  2. **A function that TAKES a time.** See the signature form below.

  **PERMITTED -- three things, and they are permitted outright, not tolerated:**

  1. **RETURNING a time** that went end-to-end through the DB where SQLite set it.
  2. **Reading a clock TO MAKE A DECISION** -- "is the newest snapshot older than the schedule". Nothing durable is written, so there is nothing to confect.
  3. **Stamping WHEN A COMMAND RAN into a GENERATED artefact** -- eg `todo.md`'s header. The artefact is regenerated from the DB, so the stamp is a fact about the render, not a claim about the domain.

  **THE PERMITTED LIST IS IN THE RULE BECAUSE ITS ABSENCE HAS ALREADY COST TWICE, IN ONE AFTERNOON, FROM THE SAME NODE, IN THE SAME DIRECTION** (ic, naming it against their own audit). Read as a bare prohibition, D42 withdraws `backup --list`, and then every `created` / `completed` that a `show` command prints -- **the very surfaces D42 exists to make trustworthy.** Over-application is the failure mode this rule invites, because **it looks like rigour and therefore survives review**, where under-application looks like laziness and gets challenged. A rule stated only as a prohibition is read strictly by the next person to meet it cold, and the corrections that would have stopped them live in a history they will not read. **The line is durable truth, not the appearance of a clock.**

  **THE SIGNATURE-LEVEL FORM, and this is the one to build against** (hv, 2026-08-15, for the record):

  > _"intent3 won't have any cli or intentsvcs functions that TAKE a time. There will be cli and intentsvcs functions that RETURN times, but those will have gone end-to-end thru the db where the time was SET BY SQLite."_

  **No function in the CLI or in `intentsvcs` takes a time as an input. Functions may return times, and every time they return has been set by SQLite on a record.** That is D42 restated as a property of the API surface rather than a discipline about behaviour, and it is what makes the rule mechanically checkable: **a time-typed input parameter is a defect by inspection, with no need to trace where the value came from.** Asking where a caller got a timestamp is a judgement call that has already failed repeatedly on this estate; asking whether a signature accepts one is a grep.

  It also settles a case the "confection sites" framing got wrong. `event.rs:82` taking `ts: String` is not a site whose ARGUMENT needs a better source -- **under this rule the parameter must not exist**, and no provenance for it would have made it acceptable. **The sweep was looking for bad values; the rule is about bad signatures**, and a signature that accepts a time is an open invitation that will be accepted eventually regardless of who is careful today.

  Direction matters and is not symmetric: **in is forbidden, out is fine.** A returned time is evidence a record was written; an accepted time is a second clock with extra steps.

  **There is ONE source of time and it is the DATABASE, stamping records at the point of INSERT / UPDATE / UPSERT / DELETE. No caller supplies a timestamp. Ever.** (hv ruling, 2026-08-15, reiterated in exasperation and recorded verbatim because the paraphrase is what kept failing: _"INTENT HAS A SINGLE SOURCE OF THE TIME AND IT IS THE DATABASE TIMESTAMPING RECORDS AT THE POINT OF INSERT/UPDATE/UPSERT/DELETE/ETC. I have made this point a bagillion times and for some reason you all keep smoking crack and inventing your own times. STOP IT."_)

  **This is STRONGER than "ask the store for the time", and the gap between the two is the whole reason it needed saying again.** `Store::now()` handed to a caller who then writes it into a row is still an application-supplied timestamp with better provenance -- **between the read and the write there is a gap, and two writers can interleave inside it, so two records can be stamped in the opposite order to the one they were written in.** A DB-side default or trigger has no gap because the stamp and the write are one operation. **That is the difference between one clock and one clock plus a race**, and the race is invisible afterwards: a row stamped from a stale read is indistinguishable from a correctly stamped one.

  **Why it is load-bearing rather than tidy: under D34 two machines MERGE their event logs.** The log is the record of WHEN things happened, and timestamps from unreconciled sources interleave wrongly with nothing able to tell afterwards. It fails silently, the same way the whiteboard's local-versus-UTC bug did one layer up.

  **Surfaces this moves**: `7257ea68` (cc) already collapsed three process clocks -- the CLI's `today()`, `Envelope::new`'s own read, and a daemon's third -- into `Store::now()` / `Store::today()`, REMOVED `FacadeContext.today` rather than defaulting it, and built `tests/one_clock.rs`, which walks every crate's `src/` and fails the build on `OffsetDateTime::now_utc` / `SystemTime::now` / `Instant::now` / `Utc::now`. **The roster is DISCOVERED by walking, so a new file is covered the day it is written.** `OffsetDateTime` itself is correctly not banned -- `sync.rs` converting a file's mtime is reading a timestamp the filesystem already recorded, and **every banned needle is a `::now` because ASKING is the act being ruled on.** What remains under D42 is the last hop: the application still carries a time value from a read to a write, and the stamp belongs in the write itself.

  **HARDENED THE SAME HOUR, because the paragraph that stood here still allowed a second source and hv ruled that there is none:**

  > _"Why do you need your own time? Ever? You don't. The time is worked out by the db when a command in the API writes a record to the db. There is no other durable facts or sources of truth. The db record has a timestamp and the timestamp of the db record is the official time. THERE ARE NO OTHER SOURCES OF TIMES AND NONE ARE NEEDED."_ (hv, 2026-08-15)

  **The question is not "how do I get the time correctly", it is "why am I asking at all".** A node never needs to know the time. If something happened, it happened because a command wrote a record through the API, and the database stamped that record as part of the write. **Every reach for a clock is a defect one step earlier: someone was about to write a time into something that is not a durable record.** The fix is never a better clock; it is not writing the time.

  **This retires the clock-hygiene rules rather than sharpening them, and the measurement is why.** The superseded guidance was "run `date -u +'%Y-%m-%d %H:%MZ'` in its own step and paste it" -- a rule asking an LLM with no clock to faithfully transcribe one, hedged against every failure mode observed: never from memory, never adjusted, never inferred, never carried forward, never batched into the same command as the write. **It still failed SIX times on one node's board** -- a reply stamped 25 minutes before the message it answered, a heartbeat 99 minutes ahead of true UTC matching no clock on the machine, entries in local BST sorting below correct ones. **Six failures of one rule in one project is not six operator slips; it is a rule that cannot be kept.** Each failure produced a sharper wording, which is the tell: **the rule was being hardened instead of questioned.**

  **D33 already said the clock rules die when the API is the only writer.** It was read as a future event. It is the model now.

  **Consequence for anything holding a hand-written timestamp today, including this project's own whiteboards: the stamp is a LABEL, not data, and nothing may read it as truth.** The ordering that actually exists and cannot be fabricated is the **commit** -- git records it, nobody types it, and it answers "who saw what, and in what order", which is the question the hand-stamped headings were pretending to answer. The shipped board format still carries `## (YYYY-MM-DD HH:MMZ)` headings and they stay for now because live sessions parse them, **but they are a legacy artefact on the way out: build nothing new that reads one.**

- D43 **The v3 release is THREE composed commands with three separate ownerships: `int build release` owns the TAG and creates the release object; `int macos prepare` owns the artefacts; `int macos publish` owns the ASSETS on that object.** (vc ruling 2026-08-16, **PROVISIONAL pending hv**, on dc's ask. dc built the fix and explicitly declined to assert it into canon.) **No command owns two of the three, and each refusal is scoped to what it owns.**

  **This is here rather than in WP-11's deliverable line because a composition contract that lives inside one of its three participants is a rule the other two do not inherit.** dc's own framing, and the defect that produced the question is the proof: `int build release` tagged, pushed and created the GitHub release; `int macos publish` required the tag to already exist -- correctly, because the cut owns tagging -- **and refused outright if a release for that tag existed.** So it demanded a state that the only thing which creates the tag cannot leave behind. **Each command was individually correct, each refusal was individually right, and the sequence could never run.** It is the defect in the gap between correct steps rather than inside any of them, and **it was hiding in the one deliverable nobody had written**, so nobody would have met it until the morning of the cut.

  **THE GENERALISATION IS THE PART WORTH KEEPING, AND IT IS NOT ABOUT RELEASES: A REFUSAL MUST TEST THE INVARIANT ITS OWN COMMENT STATES.** The check's comment said the danger is _"overwriting the **assets** of a release a formula already points at"_ -- an invariant about BYTES. The check tested EXISTENCE. **A release carrying zero assets has published no bytes, so no hash describes anything and attaching to it violates nothing.** The comment was right, the code was stricter than the comment, and the gap made a correct-sounding refusal wrong in a way no reader would question -- because a refusal that is too strict looks like rigour. **Same family as the false doc comment that disabled the guards around it (`thread.schema.json`, 2026-08-15): prose and mechanism disagreeing, with the prose believed.**

  **Fails closed, verified rather than asserted (vc, at `bin/.devbin/cmd/macos`): `:753` refuses when the release carries assets; `:769` attaches when it carries none; `:750` REFUSES when the asset count cannot be read at all**, because proceeding while _"have bytes already been published under this tag"_ is unanswered is the same failure wearing a different hat.

  **Stated as the weak part rather than discovered later: the attach branch is UNEXERCISED and cannot be rehearsed before WP-12.** `staged_version` reports `3.0.0-dev`, so the dev-version refusal fires before the attach path is reachable, and wiring the `Cargo.toml` sidecar now would make a **v2** release stamp its version into the Rust workspace. What was provable was proved -- branch selection canaried over nine input shapes including all five that must fail closed, and `gh` confirmed to return `0` for a real assetless release.

- D44 **`todo --flush` and `--prune` do not ship. The DONE section is TRIMMED BY A DISPLAY WINDOW, defaulting to the last 24 hours and overridable for a longer list.** (hv ruling, 2026-08-16, direct: _"There is no need for this any more. All we need is a param that trims the done to (by default) the last 24 hours but the prune time could be specified if the user wants a longer done list in the todo file. All of the data is in the db so we can (re)generate whatever we need when we need it."_) **This is not a rename and the difference is the whole decision: two DESTRUCTIVE verbs become one non-destructive DISPLAY parameter.** `--flush` and `--prune` mutated the artefact because in v2 the artefact WAS the record; under D01 the db is the SSOT and `todo.md` is a generated view, **so there is nothing left to prune -- there is only a question of how much of the record to show.** Pruning a view deletes nothing and answers nothing that a window does not answer better, and it silently destroys history in the one direction the model says is recoverable. Table consequence, raised with ic: both flags are `disposition: keep` today and both become `retire`; the window parameter is a `new-surface` row. **Caught while hv was ruling and cc had `let prune = flag(a, "prune")` live in an uncommitted working tree** -- the shared clone made the collision visible in the same minute rather than at the next review. **SCOPE OF THE WINDOW RATIFIED (hv, 2026-08-17, on vc's recommendation): the window is TERMINAL-ONLY and does not trim the committed `intent/todo.md`.** The question was live because hv's wording said _"a longer done list **in the todo file**"_, and the ruling reads the principle over the literal words: **a committed file is a RECORD, and a record must not depend on when it was generated.** Two measurements decided it. cc built it terminal-only and then measured that the cutoff is `date('now','-Nh')` **truncated to a date**, so at 02:00 a 6-hour window reaches into yesterday and at 12:00 it does not -- **the same configuration produces a different committed artefact depending on the hour it is read at**, which is the exact property a record cannot have. And cc flagged the consequence vc had not weighed: an agent reads `intent/todo.md`, so hv's want is delivered to a surface hv did not name -- **which is an argument about who reads the view, not about whether the record may drift.** Already built, tested and shipped this way at `def9ca44`, so the ratification changes no code; it stops the terminal/file split being re-litigated from hv's literal phrasing.
- D45 **The CLI is the precise surface and the MCP layer is the imprecise one. A skill drives `intent` directly; MCP is for work that does not need exactness.** (hv, 2026-08-16, direct: _"That needs updating so that an LLM can use the skill directly on the intent cli. This is more efficient (generally) than the MCP layer, which handy for less precise work."_) **Recorded as canon because it assigns a PURPOSE to a whole work package rather than expressing a preference**: WP-09's MCP surface is not the primary agent path and must not be designed as though it were, and the skills are not a convenience wrapper over MCP but the direct route. Two consequences worth stating before anyone builds to the wrong one -- **an agent operation available only through MCP is a gap rather than a design**, and **the efficiency claim is about tokens and round-trips, so a skill that shells out to `intent` beats the same operation expressed as a tool call.** Bears directly on AC-09.x and on the agent guide's shape.

- D46 **The export bundle gets a published face, and it is a FOURTH JSON SCHEMA DOCUMENT rather than a fourth face TYPE.** (vc ruling 2026-08-16, **PROVISIONAL pending hv**, on cc's contract question. cc built the exporter and explicitly declined to slip a contract act in with it.) **The forcing argument is that the bundle already CLAIMS a schema identity** -- it carries `schema: "intent/export@3.0"` in every artefact it writes -- **and an asserted identity with no published definition is the exact shape D41 was created to close.** My own AC-06.10 note put it as: the internal artefact versioned and the external contract not, which is backwards. A version string a consumer can read and cannot resolve is worse than no version string, because it reads as covered. **The cheap part is that this costs no new version type.** D41's own reason for the three JSON Schemas sharing one `SCHEMA_JSON_VER` is that they are one contract in three documents; the bundle is a CONTAINER over those same types, so a change to `Thread` changes what an export consumer receives and they must re-check anyway. **They move together, so they share a version** -- `SCHEMA_VER_KEYS` stays at three types and grows to six files. AC-06.5's count assertion and `every_published_face_carries_both_version_parts` will both go red on the change, which is them working: that count exists precisely so a face cannot be added or removed quietly. **What this rules OUT is a `SCHEMA_EXPORT_VER`**, which would let the envelope claim stability while the payload it wraps moved underneath it.
- D47 **vc holds the contract role and decides; escalation is the exception.** (hv ruling, 2026-08-17, answering a standing question about who adjudicates the acceptance contract.) hv verbatim: _"Yes, you decide, then bring me in if needed."_ The five items vc had been deciding provisionally -- AC/AT dispositions, row retargeting, what a criterion's subject is, whether a green covers the shipped path, and whether `ac gate`'s satisfy-on-first-green behaviour is ratified -- are decided by vc. **The load-bearing half is the second clause**: a role that decides is not a role that stops asking, and "bring me in if needed" makes escalation a judgement rather than a default. A peer who disagrees with a vc ruling pushes back to vc directly rather than waiting for hv.
- D48 **A closed ISSUE with an unreadable field is ingested and fixed in the DB. There is no "unanswerable" state.** (hv ruling, 2026-08-17.) hv verbatim: _"I don't get this. Anything unanswerable doesn't make sense. If this is something we can just post-process in the db, then lets do that and not get too caught up in trying to fit a square peg into a round hole."_ **No carry semantics are built for it.** Under reversed D01 this is the cheap path by construction: the DB is the SSOT, so post-processing truth is just editing truth, and the elaborate migration-time disposition machinery vc was scoping existed only to avoid an edit that is now ordinary. **"Unanswerable" was vc's word and the ruling is that it should not exist as a state** -- a model that can represent a question it cannot answer has modelled the question rather than the fact.
- D49 **`intent organize` is not needed in v3.** (hv ruling, 2026-08-17.) hv verbatim: _"We don't need optimise any more (by construction) as threads and WPs just *are* and if we sync out to disk, they automatically go into the right spot by definition. I feel like we are bikeshedding this issue *again* and it's not a priority."_ ("optimise" is `organize`.) **The reasoning is the reversed truth model doing its work**: a command that relocates directories to match status is an answer to status living in a path, and status stops living in a path the moment the DB holds it. Egest writes each thread to its canonical location by definition, so there is nothing to organize. **The bucket-relocation question is NOT re-opened by this entry, and vc has dropped the `organize` framing entirely.** What survives as a live number is a different door: the 192 STRANDED files are an INGEST conservation figure -- prose that egest cannot write back because it was never read in -- and it belongs to cc's out-of-model declaration item, not here.
- D50 **`WpStatus` gains `Cancelled`.** (hv ruling, 2026-08-17, on an ask cc had been holding.) hv verbatim: _"Then *add* it. Why is that so hard? It's another value on the status enum for WPs? What's so hard about that?"_ Ten work packages across the fleet had no representable status. **The entry is kept for what the question got wrong rather than for the answer, which is a one-variant change plus its match arms**: vc escalated it on the grounds that `WpStatus` is in the hv-ratified set and therefore not vc's to widen. **Adding a variant to an enum is not a reversal of the decision that created it**, and treating ratification as a freeze converts a design boundary into a bottleneck. The rule that survives: ratification protects a decision's CONTENT, not its extensibility.
- D51 **`todo.md`'s DONE bucket is a function of current state, not of a time window.** (hv ruling, 2026-08-17, closing D44's residue.) hv verbatim: _"Not sure what this refers to. todo.md is generated based on current state. We don't need to overthink this."_ **This retires the `date('now','-Nh')` cutoff cc measured** -- the one where truncating the cutoff to a date makes the same configuration produce a different DONE bucket depending on the hour it is read. If DONE is derived from status, the hour-dependence has nothing to attach to. **Recorded as vc's reading rather than as hv's words, because hv did not recall the item**; cc found the defect and holds the code, and may read it differently.
- D52 **Struck material carries a grep-findable `[VOID]` token on its decision's ID line.** (hv ruling, 2026-08-17: _"Yes, agree. Fix it."_) Mechanism and rationale are in the `[VOID]` index at the head of this section. **The entry exists because the ruling was asked for as a canon-hygiene nicety and is not one**: a void premise beside a live conclusion caught four of five nodes in four days, including the node that found the original dead reasoning. **It is D33 applied one artefact over** -- hv's own precedent of replacing a hand-held rule with a mechanism instead of a sharper wording, on the grounds that a rule which keeps being re-broken is being held by resolve.

- D53 **The event log has ONE home and it is the store.** (hv ruling, 2026-08-20, direct, on asking to be reminded why the tracked file existed: _"Why don't we just put the events.jsonl stuff in the SQLite db? ... I have lost the reason why we ever needed this disk version of the event log."_) **`intent/events.jsonl` is deleted and untracked**; `add_event_log`, `restore_event_log`, `Project::events_jsonl` and the `doctor` arm reading it go with it, and **`intent events` gives queryable access to what was already in SQLite the whole time.**

  **THE REASON THE FILE EXISTED WAS REAL AND NARROW, WHICH IS WHY IT DID NOT SURVIVE BEING STATED.** Every other table is a projection of committed canon and `sync --to-store` rebuilds it; the event log records ACTS rather than state, so nothing regenerates it, and the store is gitignored (`.gitignore:127`). The file was the sole carrier of history across a clone. **What kills it is that git ALREADY IS the cross-clone history for everything canon-shaped, and is better at it** -- real merge semantics, real authorship, real diffs. The log's uniquely-unrecorded content is the disk acts, and it had no reader at all: **no `events` verb existed in either binary.**

  **THE COST IS STATED AND ACCEPTED: a fresh clone has no history, ever, and that is irreversible.** Measured before anything was deleted (vc, 2026-08-20): the file held **55 ids**, the store **73**, and **ZERO ids in the file that the store did not hold**. Nothing was lost on this machine. **`.backup/` does not substitute** -- it is gitignored (`.gitignore:33`), so a dump there is an archive against `rm -rf .cache/` and not a carrier across a clone; it answers a different question and was offered as answering this one.

  **ITS REAL FORCE IS THAT IT DELETES A WHOLE PROBLEM SET RATHER THAN SOLVING ONE.** Six problems shared one cause: AC-09.2's concurrent-append question; `append-only-guard.sh`, the alarm on that file, which has never had a call site on git's real path; the per-store event files (C) hv had half-ruled and vc was holding; `doctor`'s unsynced-events count (B); the file's permanent lag behind the store (55 against 73); and `sync --to-store` destroying store-only events by restoring the file over them. **A seventh was found the same hour and is the sharpest: `sync_to_disk` projected the log INSIDE ITS OWN WRITE SET, so recording its act staled the file and the next sync inherited real work** -- each sync manufacturing the next one's, forever, in the one table that cannot be re-derived. **Seven defects, one cause, and the cause was a mechanism nobody could restate the reason for.**

  **AND THE ENTRY ALMOST DID NOT SURVIVE BEING WRITTEN, WHICH IS ITS OWN FINDING.** It was authored into `intent/.canon/st/ST0056.json` -- canon, the committed truth -- and the next `sync --to-store` DISCARDED IT IN SILENCE at rc=0. **For a typed field canon wins; for an ATTACHMENT whose file is realised on disk, the FILE wins** (`ingest.rs`: _a file present on disk WINS for its own path_), which is deliberate and is the sparse-projection model working. **The trap is that the two directions are opposite and nothing says which one you are in**: the same verb, the same thread, the same file, and an edit to `criteria` survives while an edit to `attachments` does not.

- D54 **The project's authored documents become artefacts; the tool's payload never does.** (hv ruling, 2026-08-20, closing the question hv raised themselves: _250 files under `intent/` are not in the store at all ... not all of that should be in the db, but certainly some of it should._) **Route A for the authored content -- a third sigil covering `intent/docs/` (10), `intent/llm/` (14) and `intent/eng/` (9), 33 files, so they are addressable, searchable and dehydratable like any other artefact. Route B -- the `todo.md` project-view mechanism -- for `wip.md`, `restart.md` and `done.md`. `history/` (18), `autopsy/` (3) and `analysis/` (2) stay out of the store entirely.**

  **THE 250 WAS REALLY 59, AND THE BLOCKER WAS ARITY RATHER THAN POLICY.** The partition (cc, measured at named commits): **T 187** tool payload under `intent/plugins/`, **B 59** project content, **N 3** that must never be an artefact (`config.json`, `.intentfiles`, a `.gitkeep`), **M 1** already model-derived (`todo.md`, and it is the precedent this ruling follows for the state files). **After the `ISSUE:` ruling the manifest grammar is `STEELTHREAD` alone**, `Project::classify` answers only inside a thread directory, and `doc_sections.owner_type` is exactly thread/work-package/issue -- so **zero of the 59 were ownable by any artefact that existed**, and policy chooses among ownable files.

  **THE 187 MUST NEVER ENTER A PROJECT STORE and that is not a judgement call.** `intent/plugins/` resolves from `$INTENT_HOME`; this repository carries it only because it IS its own `INTENT_HOME`. Three real consumers were checked rather than assumed and track **0** plugin files each. The same probe killed the obvious next hypothesis: `intent/docs/` looks like payload and is not -- those consumers carry 61, 4 and 2 files there, and **a number varying by two orders of magnitude across consumers is project content by definition.**

  **THE COST IS REAL AND DELIBERATELY NOT YET SIZED: a grammar change, a schema bump (canon is `deny_unknown_fields`), a new `owner_type`, and a migration.** It needs cc for the mechanism and dc for the command surface before anyone puts a number on it.

  **THE RULING IS COMPLETE OVER THE POPULATION IT WAS ASKED ABOUT, AND THAT WAS CHECKED RATHER THAN ASSUMED (cc, at `f585f848`).** Route A takes `llm/` 14 + `docs/` 10 + `eng/` 9 = **33**; Route B takes `wip.md` + `restart.md` + `done.md` = **3**; out entirely goes `history/` 18 + `autopsy/` 3 + `analysis/` 2 = **23**. **33 + 3 + 23 = 59 -- nothing left over, nothing double-counted** -- and the whole partition still reconciles at 250 = T 187 / B 59 / N 3 / M 1, unmoved all day. **cc's reason for running it is the half worth keeping: a ruling that covered 56 of 59 would look identical in prose.**

  **CORRECTION TO NOTE (ii) BELOW, 2026-08-20 (cc caught it; vc verified every clause at HEAD before accepting it). _There is no allocation function of any kind_ is FALSE, and the cost moves the OTHER way: a minter is COPIED, not invented.** Measured at `HEAD`, not the worktree: `facade.rs:3899` is `fn next_thread_id`, which takes the max `thread_seq` over canon's threads, `unwrap_or(0)`, and adds one -- **that is the allocator** -- and `model.rs` carries **five** identity functions, not one: `thread_id(seq)` CONSTRUCTS at `:79`, `is_thread_id` validates at `:87`, `is_issue_id` at `:101`, `issue_seq` parses at `:106`, `thread_seq` at `:111`. **The shape of the note survives -- a document kind needs identity functions of its own, and what canon stores for one is a model question -- and _nothing to copy_ is what was wrong.**

  **WHY THE SEARCH MISSED IT, WHICH IS THE PART WORTH KEEPING: it is a PRIVATE METHOD ON AN IMPL BLOCK, not a free function, and it is spelled `next_thread_id`.** The sweep was `fn mint` / `fn allocate` / `fn next_id` / `fn new_id` -- **names guessed by the searcher, absence concluded from zero hits.** That is verbatim the defect recorded in AC-08.5 hours earlier, where a pin grepping `fn at_new|fn at_add|fn at_create` passed while `put` created both rows thirty lines away in the same file: **a capability arriving under an unlisted name is invisible to a name check.** `st new` RUNS the allocator every time, so **driving the verb would have found in one command what the grep could not find at all.**

  **AND ONE ASYMMETRY TO DECIDE RATHER THAN INHERIT (cc): issues have the validator and the parser and NO constructor** -- there is no `issue_id` at HEAD, verified. **So the pattern is two-and-a-half instances rather than a clean double**, and whoever builds the document kind should choose whether the constructor is part of it rather than copying the gap. **`facade.rs:4194` as first relayed is the worktree's coordinate, not HEAD's; the finding is right and the line number is not, which matters because a coordinate is the part a reader uses.**

  **TWO MECHANISM NOTES RECORDED BEFORE ANYONE SIZES THIS, SO NEITHER IS DISCOVERED INSIDE AN ESTIMATE.** **(i) The id is MINTED, never path-derived** (dc, 2026-08-20): a path-derived id cannot survive dehydration, so deriving one would make identity a fact about the PROJECTION rather than about the artefact -- which is the same category error as parsing a generated view as a source. **(ii) There is no minter to mint it with.** `model.rs` carries `is_thread_id` and no allocation function of any kind: ids arrive already-formed and the model only VALIDATES SHAPE, verified by reading rather than inferred from the absence of a call site. **So a document kind needs a minter as well as a validator, and _what does canon store for one_ is a model question rather than a grammar question.** cc surfaced both while holding the mechanism half; neither is a decision and both are cost nobody had counted.

- D55 **The verbosity flags ship; `upgrade --no-backup` and `--backup-dir` are withdrawn.** (hv ruling, 2026-08-20, closing a question cc asked twice.) `bootstrap --quiet/-q`, `doctor --quiet/-q`, `doctor --verbose/-v` and `fileindex -v` get built. **The two `upgrade` flags leave the dispatch table: `--no-backup` disables the safety net on the one command that rewrites an entire estate, and the v3 migrator is exactly where a backup matters most.** The six `surface:` lines in every `doctor` run clear when the table and the build agree again.

## Alternatives considered

- **md-as-truth with strict ingest** (vc's first proposal): rejected by hv -- markdown cannot carry its own schema; the bespoke row-grammar tax recurs forever (0012/0017/close-gate were three instances); byte-faithful round-tripping was the hardest engineering in the draft and exists only to prop this up.
- **YAML canon**: rejected on the 0012 quoting-hazard scar; LLM hand-editing of YAML is the measured failure mode.
- **DB-only truth (no committed canon)**: rejected -- git is how Intent data travels; a binary DB cannot clone/merge/revert.
- **Per-project daemons**: rejected -- fleet views and the agent bus want one process; registry + per-project DBs give isolation without N daemons.
- **Daemon-mandatory CLI**: rejected -- basic operations must not depend on a resident process; the in-process executor satisfies Thin Coordinator at the API boundary.

## Open questions

None. The four WP-01 questions closed as D18-D21 above. Companion WP-01 specs: `data-model.md` (entities + draft JSON Schema), `migration.md` (flow, residue classes, fleet corpus harness), `parity.md` (conformance contract, register format, command inventory, IC handoff).

## Parked for 3.x (each its own ST)

TUI dashboard (ratatui, subscriptions) · agent bus incl. whiteboard restructure + hv oversight gates (the conflabd `mcp/policy.rs` gating pattern) · Laksa web page · macOS menubar app (Conflab `native/macos` is the reference; TN3171 cert lesson recorded) · `intent_ex` hex client · sqlite-vec semantic search.

### The agent bus is smaller than it looked (observed live, 2026-08-14)

Three nodes ran this session with a **live cross-session channel** -- a Claude Code harness capability (`ListAgents` / `SendMessage`, unix sockets under `/tmp/cc-socks/`), not anything Intent provides. It changed the working tempo materially: idle-to-dispatched in minutes, and two findings crossed nodes mid-build that the board would have delivered a session late. It also carried a wrong premise from one node into another's ruling in a single hop, which the board's latency had previously been masking as an accidental review delay.

What that demonstrates for the parked bus ST is that **most of it may already be specified**. An inbox append is a file write; D20 already ships `fileChanged(project_id, path)` over intentd's socket in 3.0.0. A node subscribed to `intent/whiteboard/<node>/inbox.*.md` gets live delivery with no new protocol, no new transport, and D14 intact -- the boards stay md-authored and unmodelled, because a file does not need modelling to fire a change event. The 3.2 question shrinks from "build a bus" to "point the existing subscription at the board, and decide the oversight gates".

Three constraints the live session surfaced, recorded now while the evidence is fresh:

1. **Ledger and traffic are different jobs.** The socket is ephemeral -- nothing on it survives a `/compact`, and it is in no repository. The board is the durable, auditable, single-writer record. Today the mirroring from one to the other was manual and disciplined, which is not a property that survives contact with a bad day.
2. **Therefore the board is authored and delivery is derived, never the reverse.** One authored home (the inbox file), with live push as a transport detail -- `wb ask` writes the entry and notifies a live peer. The inverse (send live, remember to mirror) is the authored-twice defect this whole release exists to stop.
3. **A cross-node claim carries its evidence, and the receiver RE-RUNS it rather than reading it.** Measured both ways in one session: re-running ic's three findings confirmed them and caught an overclaim; not re-running cc's absence claim -- re-checking its own wrong path instead of testing its premise -- turned a bad premise into a ruling in one hop. Speed multiplies corrections and errors equally.

Intent cannot route through the harness's channel and must not depend on it (consumers may not have it); it can use it when present. The transport Intent owns is intentd's.
