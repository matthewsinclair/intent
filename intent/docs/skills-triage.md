---
title: Skills catalogue triage
verblock: "2026-09-24: v1.1 - Matthew Sinclair - updated in the 3.2.1 doc audit"
---

# Skills catalogue triage

`ST0056/WP-15`. hv's framing, 2026-08-17: _"There are a lot of skills that I can see that don't make sense anymore, and it's been forever since the skills catalog (for Claude) had anyone look at it."_

## The denominator, and why it is not written in this sentence

**Count it: `ls -1d intent/plugins/claude/skills/*/ | wc -l`.** Outside the source tree, `intent claude skills list` names the catalogue the installed Intent carries. The figure is deliberately absent from this prose, because `WP-15`'s own objective records it going stale repeatedly in one document -- right at authoring, wrong when `in-handoff` retired, corrected in the view and silently reverted by a regeneration, corrected in canon in a field that carried it twice so one copy was repaired and the other left contradicting it, and stale a third time when `in-next` and `in-start` retired. **A denominator in prose goes stale by construction.** `AT-15.1` (`native/rust/crates/intent-cli/tests/skills_triage_covers_the_catalogue.rs`) asserts this table's rows against the catalogue **enumerated at close**, as a set equality rather than a count, so a skill added or removed after this document was written reddens a test rather than quietly falsifying a sentence.

**AN ABSENT ROW AND AN UNEXAMINED SKILL ARE THE SAME ABSENCE.** Every skill the enumeration returns has a row below, including every one whose verdict is KEEP with no change. A triage that lists only what it changed has an unmeasured arm, and the unmeasured arm is precisely the skills nobody looked at.

## The table

| Skill                    | Verdict    | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `in-ash-ecto-essentials` | KEEP       | Ash/Ecto access rules, loaded by `/in-session` on the elixir + `:ash` dependency fan-out. The only verb it names, `claude rules`, ships.                                                                                                                                                                                                                                                                                                                                                             |
| `in-author-essentials`   | KEEP       | The `author` pack's pipeline; `author` is a declared language of this project. Chains to `in-detrope`. Names `claude rules` and `lang init`; both ship.                                                                                                                                                                                                                                                                                                                                              |
| `in-autopsy`             | KEEP       | Session forensics. **The one data divergence on record is GONE**: `banned-words.txt` no longer carries `overall`, which `CLAUDE.md` dropped 2026-08-24, and its `Absolutely` agrees with canon. Re-driven.                                                                                                                                                                                                                                                                                           |
| `in-content-essentials`  | KEEP       | The `content` pack's pipeline; `content` is a declared language. Same shape as the author pack.                                                                                                                                                                                                                                                                                                                                                                                                      |
| `in-cost-analysis`       | KEEP       | ST0027's deliverable; ships a metrics script (`scripts/cost-metrics.sh`) and a rates table (`data/reference-rates.md`). No stale path or verb. **Its `reference-rates.md` is a dated external fact and is this catalogue's clearest future-staleness risk.**                                                                                                                                                                                                                                         |
| `in-debug`               | KEEP       | Four-phase debugging, chains to `in-verify`. Language-agnostic; names no verb, so nothing in it can rot against the surface.                                                                                                                                                                                                                                                                                                                                                                         |
| `in-detrope`             | KEEP       | The single home of the trope catalogue (`data/trope-catalog.md`), read by `IN-PR-STYLE-004` and by both prose packs.                                                                                                                                                                                                                                                                                                                                                                                 |
| `in-elixir-essentials`   | KEEP       | Elixir production rules, listed in its `rules:` frontmatter (`intent claude rules list --lang elixir` lists them). Loaded by `/in-session` on the declared `elixir` language.                                                                                                                                                                                                                                                                                                                        |
| `in-elixir-testing`      | KEEP       | Elixir test rules, listed in its `rules:` frontmatter. Same loader.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `in-essentials`          | KEEP       | Corrected 2026-09-08: step 3 no longer orders state into `.claude/restart.md`, which contradicted `/in-finish` about the same file. Its `intent/llm/AGENTS.md` mention RETIRES that path, not instructs it.                                                                                                                                                                                                                                                                                          |
| `in-finish`              | KEEP       | Corrected 2026-08-31: the localfold-versus-`release` contradiction. Names `ac descope`/`rescope`/`withdraw`, `at edit`, `at lint`, `st done`, `wp done` -- all ship.                                                                                                                                                                                                                                                                                                                                 |
| `in-phoenix-liveview`    | KEEP       | LiveView rules, loaded on the `:phoenix_live_view` dependency fan-out.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `in-plan`                | **UPDATE** | **Carried out 2026-09-09: `chains_to` had declared `[]`** while its own description says _invoke coding skills_ and its body invokes them. It now names every coding skill the body invokes. Only `AT-00.4` parses the field. It checks that each named skill exists and that every skill is reached. Nothing compares it with the body, and every skill `in-plan` invokes is also chained from `in-session`, so the false `[]` misled a reader and broke nothing -- which is why nothing caught it. |
| `in-review`              | KEEP       | Two-stage review dispatching `critic-<lang>`; it is the dispatch point `CLAUDE.md` and `intent/docs/critics.md` name.                                                                                                                                                                                                                                                                                                                                                                                |
| `in-session`             | KEEP       | The bootstrap every session runs. `chains_to` names every skill its body loads -- **checked against the body rather than assumed, because `in-plan`'s was not.**                                                                                                                                                                                                                                                                                                                                     |
| `in-standards`           | KEEP       | Corrected 2026-09-03 and overtaken 2026-09-11 (0224): `init` now writes neither `MODULES.md` nor `DECISION_TREE.md`. The skill gives each its own reading instruction instead of one sentence collapsing both: search the registry and never read it; read the decision tree only in an Elixir/Phoenix project.                                                                                                                                                                                      |
| `in-tca-audit`           | KEEP       | TCA step 2. Names `claude subagents`, which ships. Reached from `in-tca-init`.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `in-tca-finish`          | **UPDATE** | TCA step 5, chains to `in-finish`. **Carried out 2026-09-14 (issue 0335)**: `tca-report.sh --check-only` runs `intent ac gate` on the thread for its criteria guard instead of counting `- [ ]` lines in a generated `info.md`, and step 5 satisfies criteria through `intent ac satisfy` and `intent ac gate`. The skill's guard list was brought in line with the script afterwards.                                                                                                               |
| `in-tca-init`            | **UPDATE** | TCA entry point, documented at `intent/docs/total-codebase-audit.md`. **Carried out 2026-09-14 (issue 0335)**: `tca-init.sh` creates each work package with `intent wp new` and writes its body with `intent set`, keeping only `socrates.md` on disk, and step 6 writes through the store. Step 5 was brought in line with the script afterwards: it now tells the reader not to run `intent wp new` again, because on the fixed script that creates a second set after the synthesis WP.           |
| `in-tca-remediate`       | KEEP       | TCA step 4, reached from `in-tca-synthesize`. Its callers are its own family, the TCA doc and the module registry -- **which is its workflow, not orphanhood.**                                                                                                                                                                                                                                                                                                                                      |
| `in-tca-synthesize`      | KEEP       | TCA step 3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `in-verify`              | KEEP       | The verification gate. Names `ac gate`/`satisfy`/`status`, `at edit`, `at green`, `at lint` -- all ship, driven.                                                                                                                                                                                                                                                                                                                                                                                     |
| `in-whiteboard`          | KEEP       | Protocol 3.0. `chains_to: []` is CORRECT here -- `in-session` and `in-finish` chain **to** it, not from it.                                                                                                                                                                                                                                                                                                                                                                                          |

## ZERO RETIRE IS A RESULT, AND THE GROUNDS I LOOKED FOR ARE NAMED SO THE ZERO CAN BE ARGUED WITH

A triage returning no retirements against hv's premise that _a lot of skills don't make sense anymore_ owes its reasoning, not a shrug. **Retirements have ALREADY happened and I verified each is gone**: `in-handoff`, `in-next` and `in-start`, so the premise was acted on before this table existed.

**Each of these grounds would have produced a RETIRE, and each was driven:**

- **Names a verb the surface does not carry.** Every `intent <verb>` spelling in every skill was extracted and invoked. **All ship**, including the ones I expected to fail: `at lint`, `fc`, `claude subagents`.
- **Names a v2-era path.** The remaining hit is `intent/llm/AGENTS.md` in `in-essentials`, and **it retires the thing it names in the same sentence** (`in-tca-init`'s `~/.intent/ext/` mention, which did the same, has since left the skill). The naive stale-reference test scores **no true positive** on this corpus, which is the discriminator constraint `AC-15.2` recorded (withdrawn 2026-09-11; its text stands as a record) and this is its measurement.
- **Orphaned -- no caller.** Every skill has non-catalogue references. `AT-00.4` (`native/rust/crates/intent-cli/tests/every_skill_has_a_live_caller.rs`) holds a weaker property, transitively: every `chains_to` target exists, and every skill is chained to or invoked by name in `AGENTS.md`, `CLAUDE.md`, `usage-rules.md` or a `SKILL.md`. Because `usage-rules.md` names every skill, it cannot report one as unreached. **The first pass of this measurement was WRONG and inflated every count**, because `usage-rules.md`'s Skills table lists every skill by construction; a reference count including it measures being CATALOGUED, not being CALLED. Re-driven with catalogue documents excluded.
- **Superseded by a mechanism that replaced it** -- the rule library, `critic-<lang>`, the AC/AT contract. No skill was found duplicating one; the language packs point AT the rule library rather than restating it.

**WHAT THIS TRIAGE DOES NOT ESTABLISH.** Whether Intent should still ship a Total Codebase Audit workflow at all is a product-scope call and is hv's, not the pen's. The TCA skills are reachable from a documented entry point and name no verb that has moved -- so **no measurement available to me supports retiring them**, and retiring them on an impression is what this table exists to prevent. `in-tca-init` and `in-tca-finish` carry UPDATE because they were moved off v2's hand-authored thread files and onto the store (issue 0335); that is a repair, not a retirement.
