{
  "schema": "intent/thread@3.0",
  "id": "ST0050",
  "title": "intent todo: a flat DOING/TODO/DONE view of steel threads and work packages",
  "slug": "intent-todo-a-flat-doing-todo-done-view-of-steel",
  "status": "completed",
  "created": "2026-07-02",
  "completed": "2026-07-02T21:36:06Z",
  "objective": "Add `intent todo`, a CLI command that renders a single flat, nested checklist of a project's steel threads and their work packages -- bucketed into **DOING / TODO / DONE** and projected from each thread's real `status:`. It gives a \"50,000-foot\" view of the remaining work in one file (`intent/todo.md`) that no existing surface provides: `steel_threads.md` is a flat table, and per-project working notes are prose snapshots -- neither shows the WIP / not-started / just-done shape across steel threads _and_ work packages at a glance.",
  "context": "The need surfaced in a downstream project running many concurrent steel threads across several worker sessions: there was no one place to answer \"what is in flight, what is queued, and what just landed\" spanning both steel threads and their work packages. `intent st list` and `intent wp list <ST>` answer per-status and per-thread questions, but not the whole-portfolio nested view.\n\nA working **read-path prototype already exists**, authored in the downstream project's checkout of Intent at `bin/intent_todo`: `intent todo update` and `intent todo [list]` generate and print `intent/todo.md`. It dispatches automatically through `intent`'s default `intent_<command>` rule (no change to `bin/intent`), reuses `intent_helpers` (Highlander -- no re-implemented frontmatter parsing), and is `shellcheck`-clean bar the repo-standard SC1091 source-follow note.\n\nThis steel thread captures that prototype and the remaining work to make `intent todo` a first-class, tested, released Intent feature: the mutation verbs (`done` / `notdone` / `toggle`), CLI/help integration, tests, docs, and release.\n\nThe design of record, the as-built prototype, the proposed work-package breakdown, and the open decisions are in `design.md`.",
  "body": "## Related Steel Threads\n\n- **ST0046** (`add-modules-properly-to-the-intent-cli`) -- a sibling \"generate a portfolio view from the project tree\" concern. Both enumerate `intent/**` and render a derived, regenerable artifact; `intent todo` reads `intent/st/**` status the way a future `intent modules sync` reads modules.",
  "preamble": "",
  "criteria": [
    {
      "id": "AC-01.1",
      "text": "`intent todo update` regenerates `intent/todo.md` as a nested GFM checklist bucketed DOING/TODO/DONE, projected from each unit's real `status:` (threads as `- [ ] STID: title`, work packages as indented `  - [ ] NN: title`).",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.2",
      "text": "checkbox glyphs map status: `WIP`->`[-]`, `Not Started`->`[ ]`, `Completed`/`Done`->`[x]`, `Cancelled`->`[~]`.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.3",
      "text": "buckets are correct: DOING = `WIP` threads (+ WPs); TODO = `Not Started`; DONE = threads whose `completed:` is at or after the `## DONE:<T>` watermark (WP-06; the first-generation default `<T>` = start of today, so the zero-flush view is \"completed today\").",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.4",
      "text": "on-hold threads (`on-hold: TRUE` + `status: WIP`) render in DOING with an `(on-hold)` tag.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.5",
      "text": "`todo.md` contains ONLY the three `## DOING` / `## TODO` / `## DONE:<T>` headings and their data (the `_(none)_` sentinel when a bucket is empty) -- NO title line, NO `_Generated…_` provenance line, NO `_Legend…_` line (hv minimal-output).",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.6",
      "text": "output is prettier-stable: the generator's output equals the post-prettier file byte-for-byte (no reflow churn on commit).",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.7",
      "text": "`intent todo` / `intent todo list` prints `todo.md` (generating it first if absent); `intent todo help` prints usage.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.8",
      "text": "`intent todo --json` emits valid JSON: an object keyed by bucket (doing/todo/done), each a list of threads carrying `id` / `title` / `status`, each thread carrying its work packages (`id` / `title` / `status`). The JSON and markdown emitters share one enumeration of `intent/st/**` (Highlander -- no second traversal).",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.1",
      "text": "`intent todo done <ST[/NN]>` changes real status by wrapping `intent st done` / `intent wp done`, then regenerates `todo.md` -- it never hand-edits a checkbox or `status:`.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.2",
      "text": "`intent todo done` INHERITS the ST0048 acceptance close-gate (D2): a unit with a BLOCKED contract is refused, the gate's message is surfaced verbatim, and status is left unchanged (no bypass).",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.3",
      "text": "`intent todo notdone <ST[/NN]>` reopens a completed unit to `WIP` (D1), then regenerates.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.4",
      "text": "`intent todo toggle <ST[/NN]>` flips done/not-done from the unit's current status, then regenerates.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.5",
      "text": "the ST/WP specifier (`ST0011` / `11` / `ST0011/01` / `11/01`) is parsed via the shared `intent wp` specifier logic, not a reimplementation (Highlander).",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "parse_wp_specifier extracted to bin/intent_helpers (Highlander); reused by intent_wp + intent_todo spec_info_file; 1/1 == ST1/1 == ST0001/01 proven"
      }
    },
    {
      "id": "AC-03.1",
      "text": "`todo` is registered in `intent_help` and the top-level usage listing.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.2",
      "text": "`intent todo <args>` dispatches end-to-end via `bin/intent`'s default `intent_<command>` fall-through (no dispatcher edit; the command runs from `PROJECT_ROOT`).",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.1",
      "text": "a reusable fixture-project harness under `tests/` proves, green: projection correctness (WIP/Not-Started/Completed STs + WPs -> asserted `todo.md`), minimal-output shape, prettier-stability, the DONE self-sweep, mutation round-trips, gate-inheritance, and `--json` structure.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-05.1",
      "text": "`bin/intent_todo` is registered in `intent/llm/MODULES.md`; README and `usage-rules.md` document `intent todo` (commands, projection model, mutation semantics, `--json`).",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "MODULES.md `Todo view` row; usage-rules.md `### Todo view`; README `See what's in flight`"
      }
    },
    {
      "id": "AC-05.2",
      "text": "CHANGELOG carries a 2.14.0 `intent todo` entry; `impl.md` records the as-built; `tasks.md` reflects completion.",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "CHANGELOG `## [2.14.0]` (ST0050 + ST0051); impl.md as-built; tasks.md WP checklist"
      }
    },
    {
      "id": "AC-06.1",
      "text": "`intent st done` stamps the frontmatter `completed:` as an ISO 8601 UTC timestamp (`YYYY-MM-DDThh:mm:ssZ`), not a bare `%Y%m%d` date. (The human `- **Completed**:` body bullet stays a `%Y-%m-%d` date; `steel_threads.md` renders the date part -- an ISO `completed:` on the fallback render path is truncated to its date.)",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.2",
      "text": "the DONE bucket is watermarked: the heading is `## DONE:<T>`, where `<T>` is the last-flush instant (ISO 8601 UTC). DONE lists threads whose `completed:` is at or after `<T>`; `update` preserves `<T>` (first generation defaults `<T>` to the start of today, UTC -- reproducing the \"completed today\" view as the zero-flush baseline).",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.3",
      "text": "DONE membership tolerates both timestamp forms: a legacy `completed: YYYYMMDD` and an ISO `completed:` are each compared correctly against `<T>` (legacy read as that day's 00:00:00Z).",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.4",
      "text": "`intent todo done --flush` advances `<T>` to now and empties the DONE view; a thread's real status is untouched (flush clears the view, not the record in `COMPLETED/`).",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.5",
      "text": "`intent todo done --prune` emits the pruned DONE items to stdout (for the caller to archive, eg `>> intent/done.md`; the advisory note goes to stderr), and then flushes.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    }
  ],
  "tests": [
    {
      "id": "AT-01.1",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-01.1",
        "AC-01.3"
      ],
      "status": "green",
      "note": "test: update_projects_buckets_from_status"
    },
    {
      "id": "AT-01.2",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-01.2"
      ],
      "status": "green",
      "note": "test: checkbox_glyphs_map_each_status"
    },
    {
      "id": "AT-01.3",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-01.3"
      ],
      "status": "green",
      "note": "test: done_bucket_self_sweeps_to_today"
    },
    {
      "id": "AT-01.4",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-01.4"
      ],
      "status": "green",
      "note": "test: on_hold_thread_tagged_in_doing"
    },
    {
      "id": "AT-01.5",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-01.5"
      ],
      "status": "green",
      "note": "test: todo_md_has_only_headings_and_data"
    },
    {
      "id": "AT-01.6",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-01.6"
      ],
      "status": "green",
      "note": "test: output_is_prettier_stable"
    },
    {
      "id": "AT-01.7",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-01.7"
      ],
      "status": "green",
      "note": "test: list_prints_and_help_shows_usage"
    },
    {
      "id": "AT-01.8",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-01.8"
      ],
      "status": "green",
      "note": "test: json_emits_valid_structured_buckets"
    },
    {
      "id": "AT-02.1",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-02.1"
      ],
      "status": "green",
      "note": "test: done_wraps_st_wp_and_regenerates"
    },
    {
      "id": "AT-02.2",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-02.2"
      ],
      "status": "green",
      "note": "test: done_inherits_close_gate_on_blocked"
    },
    {
      "id": "AT-02.3",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-02.3"
      ],
      "status": "green",
      "note": "test: notdone_reopens_to_wip"
    },
    {
      "id": "AT-02.4",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-02.4"
      ],
      "status": "green",
      "note": "test: toggle_flips_from_current_status"
    },
    {
      "id": "AT-03.1",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-03.1"
      ],
      "status": "green",
      "note": "test: todo_registered_in_help_and_usage"
    },
    {
      "id": "AT-03.2",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-03.2"
      ],
      "status": "green",
      "note": "test: dispatches_via_default_fallthrough"
    },
    {
      "id": "AT-04.1",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-04.1"
      ],
      "status": "green",
      "note": "the file as a whole: the harness proving the ACs above"
    },
    {
      "id": "AT-06.1",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-06.1"
      ],
      "status": "green",
      "note": "test: st_done_stamps_iso_completed"
    },
    {
      "id": "AT-06.2",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-06.2",
        "AC-06.3"
      ],
      "status": "green",
      "note": "test: done_bucket_watermarked_and_membership"
    },
    {
      "id": "AT-06.3",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-06.4"
      ],
      "status": "green",
      "note": "test: flush_advances_watermark_empties_done"
    },
    {
      "id": "AT-06.4",
      "kind": "test",
      "file": "tests/unit/intent_todo.bats",
      "covers": [
        "AC-06.5"
      ],
      "status": "green",
      "note": "test: prune_emits_then_flushes"
    }
  ],
  "attachments": [
    {
      "path": "design.md",
      "text": "# Design - ST0050: intent todo: a flat DOING/TODO/DONE view of steel threads and work packages\n\n## Approach\n\n`intent todo` renders `intent/todo.md`: a flat, nested GitHub-Flavored-Markdown checklist of every steel thread and its work packages, bucketed **DOING / TODO / DONE** and projected from real status. It is a projection, not an independent store, so it cannot drift from `intent/st/**`.\n\nA working **read-path prototype already exists** at `bin/intent_todo` (authored in a downstream project's Intent checkout):\n\n- Dispatches automatically via `intent`'s default `intent_<command>` rule -- no edit to `bin/intent`. It runs as a project command, so `intent` resolves `PROJECT_ROOT` and `cd`s into it before exec.\n- Reuses `intent_helpers` (`error` / `info`; `resolve_st_dir` / `normalise_st_id` are available for the mutation verbs) -- Highlander, no re-implemented frontmatter parsing.\n- Enumerates the status directories directly (`intent/st/ST*`, `NOT-STARTED/`, `COMPLETED/`) and reads `status:` / `title:` / `completed:` with the repo's standard `grep -m1 \"^field:\" | sed` idiom.\n- Atomic write (temp file + `mv`); `shellcheck`-clean bar the repo-standard SC1091 source-follow info.\n\n`update`, `list`, and `help` work today. `done` / `notdone` / `toggle` stub with a \"not wired yet\" error, pending the decisions below.\n\nProposed work packages to make it a first-class, tested, released feature:\n\n- **WP-01 Read path** -- `update` / `list` / `help`; checkbox mapping; the three buckets; on-hold tag; DONE self-sweep; prettier-stable output. (Prototyped; needs tests + review.)\n- **WP-02 Mutation verbs** -- `done` / `notdone` / `toggle` as thin wrappers over `intent st` / `intent wp`, then regenerate; share the WP specifier parser with `intent wp`.\n- **WP-03 CLI integration** -- register `todo` in `intent_help` and the top-level usage; optional explicit dispatch case for discoverability.\n- **WP-04 Tests** -- the `tests/` harness: projection correctness (a fixture project with WIP / not-started / completed STs + WPs -> asserted `todo.md`), prettier-stability, mutation round-trips.\n- **WP-05 Docs + release** -- README / `usage-rules.md` entry, CHANGELOG, version bump + release.\n\n## Design Decisions\n\n- **Projection, single source of truth.** Every line's checkbox glyph is DERIVED from the thread's / WP's real `status:` (and its status-directory placement). There is no separately-stored checkbox state, so the file cannot lie. It follows that the mutation verbs must change _real_ status by wrapping `intent st` / `intent wp` and regenerating -- never hand-editing a checkbox -- which keeps the \"never hand-edit `status:`\" invariant intact.\n\n- **Checkbox mapping** (`status_box`): `WIP` -> `[-]`; `Not Started` -> `[ ]`; `Completed` / `Done` -> `[x]`; `Cancelled` -> `[~]`. Steel threads use `WIP` / `Not Started` / `Completed` / `Cancelled`; work packages use `WIP` / `Not Started` / `Done` -- both vocabularies are mapped in the one helper.\n\n- **Buckets**: DOING = `WIP` threads (+ WPs); TODO = `Not Started` threads (+ WPs); DONE = threads completed _today_. On-hold threads (`on-hold: TRUE` + `status: WIP`) render in DOING with an `(on-hold)` tag. Ordering is ST-number ascending. A steel thread's title is its `# ST####: Title` heading (no `title:` frontmatter on ST `info.md`); a WP's title is its `title:` frontmatter.\n\n- **The DONE bucket self-sweeps.** DONE lists only threads whose `completed:` date is today; yesterday's completions drop off automatically on the next `update` (they live permanently in `COMPLETED/` and the monthly done-log). This delivers \"swept to history daily\" with no marker file and no history-copy machinery.\n\n- **Output must be prettier-stable.** `intent/todo.md` is tracked, so a pre-commit prettier hook (or format-on-save) runs on it. Plain `[-] ST...` lines are not markdown list items -- prettier reflows each section into one run-on paragraph. The generator emits canonical GFM task-list items (`- [ ]` for threads, `  - [ ]` for WPs), which prettier preserves line-for-line and which render as a real nested checklist. The generator's output is already prettier-canonical so the hook is a no-op (verified: generated output == committed blob byte-for-byte).\n\n- **Decisions (ratified by hv, 2026-07-02):**\n  - **D1 -- `notdone` reopens to `WIP`.** The unit has started (it completed), so \"back in flight\" == `WIP`; `Not Started` would erase its history.\n  - **D2 -- `done` inherits the acceptance gate; no bypass.** `intent todo done` wraps `intent st done` / `intent wp done` and inherits the ST0048 close-gate: a BLOCKED contract refuses the close and the wrapper surfaces the gate message verbatim. A bypass would be a backdoor closing contractless units -- the vacuous-green ST0048 killed.\n  - **D3 -- self-sweep, no history file.** The completed-today self-sweep is sufficient; the permanent record is `COMPLETED/` + the done-log. No separate todo history store (it would re-introduce driftable state).\n  - **D4 -- confirmed.** `intent st done` stamps `completed:` (`bin/intent_st:561,590`, `date +%Y%m%d`); `todo done` wrapping it inherits the stamp -- no separate stamping needed.\n\n- **Minimal `todo.md` output (hv, 2026-07-02).** Strip the generated boilerplate: no `# Intent Todo` title, no `_Generated from..._` provenance line, no `_Legend..._` line. Emit ONLY the three bucket headings (`## DOING` / `## TODO` / `## DONE`) and their data (the `_(none)_` sentinel when a bucket is empty). The file is a scannable projection, not a document -- the boilerplate is noise. (The completed-today self-sweep semantic is unchanged; it is simply not restated in the DONE heading.)\n\n- **`--json` export (hv, 2026-07-02).** `intent todo --json` emits the DOING/TODO/DONE structure as machine-readable JSON (each steel thread + its work packages, carrying id / title / status / bucket) for export to other systems. The markdown and JSON renderers share one enumeration of `intent/st/**` (Highlander); `--json` selects the emitter. Applies to `todo` / `todo list`; `update` writes the markdown file as today. Shape ratified: keyed by bucket (`{doing:[...], todo:[...], done:[...]}`, each thread carrying nested `work_packages`).\n\n- **DONE flush / prune + the `## DONE:<T>` watermark (hv, 2026-07-02) -- data-model granularity open.** The DONE heading carries the last-flush timestamp: `## DONE:<T>` where `<T>` is an ISO 8601 timestamp (eg `2026-07-02T20:30:45Z` -- the standard, not a hacky `YYYYMMDD-HHMM`). The watermark lives IN todo.md's DONE heading -- self-contained, NO separate marker file; this is what resolves D3 (\"the marker is the heading\"). Model: DONE lists threads completed AFTER `<T>`; `intent todo done --flush` advances `<T>` to now (emptying DONE); `--prune` = flush + emit the pruned items to stdout for the caller to archive at their discretion. `update` PRESERVES `<T>` (reads the existing heading), so todo.md stays prettier-stable between flushes; only `--flush`/`--prune` advance it. NOTE: this revises WP-01 AC-01.5 -- the DONE heading is `## DONE:<T>`, not bare `## DONE`.\n  - RULED (hv, 2026-07-02): Option A -- upgrade `completed:` to an ISO 8601 timestamp. `intent st done` stamps eg `2026-07-02T20:30:45Z`; `steel_threads.md` renders just the date part; the DONE comparison tolerates legacy date-only stamps (treat `YYYYMMDD` as that day). Flush is then exact. This is WP-06's data-model prerequisite (touches `bin/intent_st` completion stamping at `:561,590` + the `steel_threads.md` render + `intent todo`'s DONE membership test).\n  - Verb placement RULED (cc, 2026-07-02): `intent todo done --flush` / `done --prune` (flags on `done`), honouring matts' original phrasing. `done <spec>` mutates a unit; `done --flush`/`--prune` operate on the DONE bucket; bare `done` errors. Disambiguated by argument (a `--flag` vs a spec vs empty), so the two senses of \"done\" never collide.\n  - As-built (WP-06, cc, 2026-07-02):\n    - **Membership is `completed >= <T>`** (inclusive), so a completion stamped exactly at the watermark still shows; `normalize_completed` maps a legacy `YYYYMMDD` to that day's `T00:00:00Z`, a bare dashed date likewise, and a full ISO timestamp as-is, then compares lexically (ISO sorts chronologically).\n    - **`<T>` is sticky, not auto-daily.** The heading is authoritative: `update` reads it back and preserves it; only `--flush`/`--prune` advance it. First generation (no prior heading) defaults `<T>` to start-of-today UTC, reproducing the \"completed today\" zero-flush view. \"Swept daily\" therefore becomes \"swept on flush\" -- an automatic daily sweep, if wanted, is a `--flush` from a daily ritual, not built in. (Flagged for matts' acceptance-verify.)\n    - **UTC.** `completed:` and `<T>` are `date -u +%Y-%m-%dT%H:%M:%SZ` -- the unambiguous, lexically-comparable standard. The human `- **Completed**:` body bullet stays a local dashed date; `steel_threads.md` renders from the body bullet (primary) and truncates an ISO frontmatter value to its date on the fallback path.\n    - **`--prune` splits streams**: pruned items to stdout (pipe-clean for `>> intent/done.md`), the advisory note to stderr. `flush_watermark` is the shared advance mechanism behind both verbs (Highlander).\n\n## Architecture\n\n**Dispatch.** `intent todo <args>` falls through `bin/intent`'s default case to `bin/intent_todo`, which is a project command (requires `PROJECT_ROOT`; `intent` `cd`s into the project root before exec). No dispatcher change is required; WP-03 only adds help/usage registration for discoverability.\n\n**Command surface:**\n\n| Command                           | Behaviour                                       | State      |\n| --------------------------------- | ----------------------------------------------- | ---------- |\n| `intent todo`, `intent todo list` | Print `intent/todo.md` (generate it if absent)  | prototyped |\n| `intent todo update`              | Regenerate `intent/todo.md` from current status | prototyped |\n| `intent todo done <STID[/NN]>`    | Mark a steel thread or WP done, then regenerate | to build   |\n| `intent todo notdone <STID[/NN]>` | Reopen a steel thread or WP, then regenerate    | to build   |\n| `intent todo toggle <STID[/NN]>`  | Flip done / not-done, then regenerate           | to build   |\n| `intent todo help`                | Usage                                           | prototyped |\n\nSpecifier syntax reuses `intent wp`'s: `ST0011` or bare `11`; `ST0011/01` or `11/01` for a work package.\n\n**Rendering pipeline** (`generate`): enumerate each status directory in ST-number order -> per thread, read status + title + on-hold and emit a `- [box] STID: title` line -> per WP, emit a `  - [box] NN: title` line -> assemble under the three bucket headings with a generated-header + legend -> write atomically to `intent/todo.md`. `list` prints the file (generating on first use); the mutation verbs shell out to `intent st` / `intent wp` then call `generate`.\n\n## Alternatives Considered\n\n- **`todo.md` as a hand-flippable checklist independent of ST status.** Rejected. It allows casual toggling but re-introduces exactly the drift the command exists to eliminate -- the file would silently diverge from `intent/st/**`. The projection model keeps one source of truth and pays for it only by routing mutations through `intent st` / `intent wp`.\n\n- **A separate history file + sweep marker for DONE.** Deferred (decision D3). The completed-today self-sweep achieves the same \"recent-only, swept daily\" effect with no extra state, relying on `COMPLETED/` and the monthly done-log as the permanent record.\n\n- **A dedicated dispatch case + new library module.** Unnecessary. The default `intent_<command>` fall-through already routes `intent todo`, and the shared `intent_helpers` already provide status-dir resolution and ID normalisation, so the command is a single self-contained script in the established `bin/intent_*` style.\n",
      "bytes": 11891,
      "sha256": "837be2bf890dc7d5f7a8c6794357b8d3dcb4741c34629c71212ceb19e6473578"
    },
    {
      "path": "impl.md",
      "text": "# Implementation - ST0050: intent todo: a flat DOING/TODO/DONE view of steel threads and work packages\n\n## Implementation\n\n`bin/intent_todo` is a single self-contained script in the `bin/intent_*` style. It carries no dedicated dispatch case and no new library module: `bin/intent`'s default `intent_<command>` fall-through routes `intent todo`, and `bin/intent_help` auto-discovers it (promoted to a curated Core entry in WP-03). Shared concerns are sourced from `bin/intent_helpers` — `resolve_st_dir`, `normalise_st_id`, and `parse_wp_specifier` (extracted here from `bin/intent_wp` for Highlander, so `1/1` ≡ `ST1/1` ≡ `ST0001/01`).\n\n**Projection model (the invariant).** `intent/todo.md` is derived, never authored. Each line's checkbox glyph comes from the unit's real `status:` (`status_box`) and its status-directory placement; there is no separately-stored checkbox state, so the file cannot drift from `intent/st/**`. The read path (`generate`) enumerates each status directory in ST-number order and emits canonical GFM task-list items (`- [ ]` threads, `  - [ ]` WPs) so a pre-commit prettier hook is a no-op. The `--json` path (`generate_json`) shares the same field extraction and bucket→directory mapping — only the renderer differs (Highlander: one enumeration, two emitters).\n\n**Mutation verbs.** `done` / `notdone` / `toggle` change _real_ status by shelling out to `intent st` / `intent wp` (`todo_mutate` routes by the `/NN` in the specifier), then regenerate. `notdone` wraps `st/wp start` (reopen to WIP, D1); `done` wraps `st/wp done` and therefore inherits the ST0048 acceptance close-gate for free (D2) — a BLOCKED contract aborts under `set -e` with the gate message surfaced verbatim, status unchanged.\n\n**DONE lifecycle (WP-06).** The DONE bucket is watermarked in its heading: `## DONE:<T>`, `<T>` an ISO 8601 UTC instant. Membership is `normalize_completed(completed) >= <T>` — lexical ISO compare, chronological — with `normalize_completed` mapping a legacy `%Y%m%d` (and a bare dashed date) to that day's `T00:00:00Z` and passing a full ISO timestamp through. `<T>` is sticky: `generate` reads it back from the existing file and preserves it (first generation defaults to start-of-today UTC, reproducing the \"completed today\" zero-flush view); only `flush_watermark` (the shared advance behind `done --flush` and `done --prune`) moves it to now. `--prune` writes the items being flushed to stdout (pipe-clean for `>> intent/done.md`) and its advisory note to stderr. `intent st done` was upgraded to stamp `completed:` as an ISO timestamp (`bin/intent_st`, both close paths) for exact flush ordering; the `steel_threads.md` render keys off the dashed body bullet and truncates an ISO frontmatter value to its date on the fallback path.\n\n## Code Examples\n\n```bash\nintent todo                 # print intent/todo.md (generate on first use)\nintent todo update          # regenerate from real status\nintent todo --json          # { doing:[...], todo:[...], done:[...], done_watermark:\"<T>\" }\nintent todo done ST0001/02  # wraps intent wp done (close-gate applies)\nintent todo done --flush    # advance <T> to now (clear the DONE view)\nintent todo done --prune    # emit DONE items to stdout, then flush\n```\n\n## Technical Details\n\n- **macOS bash 3.2 safe.** `normalize_completed` uses `case` globs (no `declare -A`, no `${v^^}`); the `< since` skip is written `if [[ ... ]]; then continue; fi`, never `[[ ... ]] && continue` (which trips `set -e` when false).\n- **Files:** `bin/intent_todo` (new); `bin/intent_helpers` (`get_default_width`, `parse_wp_specifier`); `bin/intent_wp` (parse_wp_specifier pointer); `bin/intent_st` (ISO `completed:` stamp + date-part render fallback); `bin/intent_help` (curated `todo` entry); `intent/llm/MODULES.md` (registry row).\n- **Tests:** `tests/unit/intent_todo.bats` — the WP-04 harness: projection, minimal output, prettier-stability, DONE watermark + membership, mutation round-trips, gate-inheritance, `--json`, flush/prune, ISO stamp, help + fall-through guards.\n\n## Challenges & Solutions\n\n- **Auto-daily sweep vs. sticky watermark.** The WP-01 \"completed today\" self-sweep and the WP-06 \"last-flush watermark\" pull in opposite directions. Resolved by making `<T>` authoritative and sticky, with the first-generation default = start-of-today (so the zero-flush view still reads as \"today\"). \"Swept daily\" becomes \"swept on flush\"; an automatic daily sweep, if wanted, is a `--flush` from a daily ritual. Flagged for matts' acceptance-verify.\n- **Legacy vs. ISO `completed:`.** Existing `COMPLETED/` threads carry `%Y%m%d`; new ones get ISO. `normalize_completed` tolerates both for DONE membership, and the `steel_threads.md` fallback render truncates to the date part — no data migration required.\n",
      "bytes": 4789,
      "sha256": "960fea834c4ba87922ddc11ed5ffe80b238c24b09337d4adfb2de3e9683aaf9b"
    },
    {
      "path": "tasks.md",
      "text": "# Tasks - ST0050: intent todo: a flat DOING/TODO/DONE view of steel threads and work packages\n\n## Tasks\n\n- [x] WP-01 — Read path + output: `generate` (minimal DOING/TODO/DONE markdown) + `generate_json` (keyed-by-bucket, nested work packages)\n- [x] WP-02 — Mutation verbs: `done` / `notdone` / `toggle` wrapping `intent st` / `intent wp` (gate-inheriting); `parse_wp_specifier` extracted to `intent_helpers`\n- [x] WP-03 — CLI integration: curated `todo` entry in `intent help`; fall-through dispatch guard\n- [x] WP-04 — Tests: `tests/unit/intent_todo.bats` harness (the AC rollup)\n- [x] WP-05 — Docs + release: MODULES.md row, README + usage-rules.md, CHANGELOG 2.14.0, impl.md / tasks.md\n- [x] WP-06 — DONE flush / prune + ISO `completed:` timestamp + `## DONE:<T>` watermark\n\n## Task Notes\n\nDesign decisions D1–D4 ratified by hv (2026-07-02); WP-06 verb placement + as-built sub-decisions ruled by cc and recorded in `design.md`. Acceptance contract in `acceptance.md` (23 ACs across the six WPs); the harness is `tests/unit/intent_todo.bats`.\n\n## Dependencies\n\nST0050 and ST0051 (output width) are independent — `todo.md` is width-agnostic — and ship together in v2.14.0. WP-06's ISO `completed:` change touches `bin/intent_st`, so it landed after the WP-01/02 read path and mutation verbs were green.\n",
      "bytes": 1323,
      "sha256": "5f7878149281e0a1b49bf3797b3a65b35169022e5aa70f5c480faf921ace7bdf"
    }
  ]
}
