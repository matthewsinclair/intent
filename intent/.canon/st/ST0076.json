{
  "schema": "intent/thread@3.0",
  "id": "ST0076",
  "title": "A typed symbol index for Rust and Elixir: kinds, containers, qualified references and resolved references",
  "slug": "a-typed-symbol-index-for-rust-and-elixir-kinds-c",
  "status": "wip",
  "created": "2026-09-16",
  "objective": "Make Intent's source index answer typed questions for Rust and Elixir: what kind a definition is and what it belongs to, where source names it with its qualifier, and, from each language's own toolchain, which definition a reference actually resolves to. Every answer says which of those three levels produced it.",
  "context": "ST0069 built the index's structural tier on each grammar's tags query and stored only def or ref, a name and a span. hv drove it on 2026-09-16 and ruled that it has to give typed, semantic-aware search over source, not something little better than grep, with Rust and Elixir required and Swift not. The measurements behind the ruling, the three levels and the work packages are in design.md. Issue 0429 (references inside macros, type uses, duplicate definitions) is descoped into this thread.",
  "body": "",
  "preamble": "",
  "wps": [
    {
      "seq": 1,
      "title": "Typed definitions: kind, container, arity and span, one row per syntax node, with an extractor version",
      "scope": "M",
      "status": "done",
      "objective": "Make every symbol row say what it is, where it sits and its arity, so the index can answer the methods of a type or the structs named Config rather than only a name. For Rust and Elixir, through Intent's own queries, a definition carries its subkind, container, container kind, trait and arity, read from the file's own syntax. One row stands for one name node, a definition is never also a reference to itself, and a file an older extractor wrote is re-extracted rather than mixed with rows of another shape.",
      "body": "",
      "preamble": ""
    },
    {
      "seq": 2,
      "title": "Rust qualified references: scoped calls, type uses and macro token trees, with the qualifier",
      "scope": "M",
      "status": "done",
      "objective": "Make a Rust reference say the path it was written with and find the references grep finds but the index missed: scoped calls such as `AddressError::new(..)`, uses of a type, `use` imports, paths that are not called, and names inside a macro invocation, each on the line its name is written. A qualifier is stored as written and never resolved, it makes the row level 2, and a name inside a macro that the tokens do not show as a call or a path is recorded as an occurrence of the name.",
      "body": "",
      "preamble": ""
    },
    {
      "seq": 3,
      "title": "Elixir qualified references: remote calls with their module, captures, pipes, alias expansion, use, import and require",
      "scope": "M",
      "status": "done",
      "objective": "Make an Elixir reference say the module it was written with, so `Repo.get` and `Map.get` stop reading as one name. Remote calls, remote captures such as `&Repo.get/2` and remote pipe targets carry their module as the qualifier at level 2, with the arity written or piped; local calls, local captures such as `&format/1` and local pipe targets stay unqualified at level 1, a capture with the arity after its slash; and `use`, `import`, `require` and `alias` are level-1 references named with the whole module, each brace entry its own row. A module is expanded by the file's own alias forms, `as:`, the brace form, `__MODULE__` and an Erlang module alias, within the enclosing do-block, and nothing `import` or `use` brings in is applied. Every reference spans its name node.",
      "body": "",
      "preamble": ""
    },
    {
      "seq": 4,
      "title": "Surfaces: search by kind and container, qualified context naming its level, SQL columns, MCP parameters and instructions",
      "scope": "M",
      "status": "done",
      "objective": "Make what the typed index knows answerable from every door a reader uses. `intent search` filters by hit kind (`--kind def`), by symbol subkind (`--subkind method`, a word from the roster the queries define, refused by name when unknown) and by container (`--in AddressError`), and with `--subkind` or `--in` and no query it lists the symbols that pass them. `--context` and `--outline` honour every filter and `--limit`, and a tier filter that leaves out the structural tier is refused rather than ignored. `--context` prints each reference with its qualifier and names the level that answered. The SQL door and `intent schema` carry the symbol columns under the JSON hit's names, and the MCP search tool takes the same filters, the query-less form included. The search row's `when_to_use`, the MCP instructions and the explorer's search pane say what each level and each language's references answer and what they leave out, and the pane draws that note on screen (issue 0435).",
      "body": "",
      "preamble": ""
    },
    {
      "seq": 5,
      "title": "Rust resolved references through rust-analyzer's SCIP export",
      "scope": "L",
      "status": "done",
      "objective": "Resolve Rust references to the definitions they name with rust-analyzer's SCIP export, on an explicit verb, into the level-3 core both languages share: a resolved row joins a written reference on its file, line and name, `intent index status` counts what each run matched, left unmatched, dropped and found ambiguous and names the stale paths, and a missing or failed run is recorded with its reason and named by `intent index resolve` and `intent index status` rather than stored as a current run that resolved nothing. The search envelope's level-3 facts are WP-07's.",
      "body": "## Measured first (cc, 2026-09-17)\n\nOn a fresh clone of main at `c800b8319`, indexed by the installed pair at the same commit (extractor 2) under an isolated HOME. The tool is rust-analyzer 0.0.0 (682a84e95b 2026-09-13), installed by Homebrew at `/opt/homebrew/bin/rust-analyzer` and the only one on PATH; there is no rustup, and Homebrew rust 1.98.1's sysroot carries the library sources and `rust-analyzer-proc-macro-srv`.\n\n- **The export runs project code.** `rust-analyzer scip native/rust` took 28 s wall and 3.2 GB peak memory, exited 0 and wrote a 29 MB index. It ran 42 build scripts, the workspace's own `build.rs` files among them, built 25 proc macros for expansion, and wrote 304 MB of check artefacts into the workspace's target directory. Its cargo metadata step waited on the shared `~/.cargo` package lock.\n- **Nothing turns that off.** `scip` has no disable flag, and `cargo.buildScripts.enable` and `procMacro.enable` set to false through its `--config-path` changed nothing: the same build scripts and proc macros ran, into a fresh `CARGO_TARGET_DIR`, and the decoded occurrences were identical. `CARGO_TARGET_DIR` is honoured.\n- **Absence and failure are distinguishable.** A missing binary is ENOENT on spawn (127 through a shell). A directory that is not a Cargo workspace exits 1 with `Error: no projects` and a backtrace, and writes no index.\n- **The join fields.** `project_root` is the path the export was given, and each document's `relative_path` is relative to it, so the index's path is the workspace root plus that path. Every document is UTF-8 with 0-based lines; `symbol_roles` bit 1 marks a definition; a symbol reads `rust-analyzer cargo <crate> <version> <descriptors>` and a local `local N`. The name is not a field: it is the source text at the occurrence's range.\n- **The counts on this repository.** 235,355 references: 70,743 to definitions in the index, 108,852 to std and dependencies, 55,760 to locals. 146,743 written Rust reference rows (level 1 120,471, level 2 26,272); 144,554 of them join a reference on file, line and name, 118,387 at level 1 and 26,167 at level 2. 90,801 references have no written row (most internal ones are fields, variants and constants, which the syntax does not write); 2,189 written rows have none (367 in files outside the Cargo workspace). 939 keys name two or more definitions, such as `expect` on an `Option` and a `Result` on one line.\n- **References inside macro invocations resolve.** The `is_local` calls inside `assert!` and the `nearest_project` calls at `views.rs:437` and `:439` inside `assert_eq!` each join their written row.\n\n## Rulings (vc, under the pen, 2026-09-17, vc decision 25)\n\n1. **One explicit verb for both languages**, `intent index resolve [--lang] [--full]` (the name agreed by hv, 2026-09-17), never intentd, reconcile or a hook, and not offered on the MCP tool tier in this release.\n2. **Every written reference row is the join's population**, level 1 and level 2.\n3. **Five counts per language, from the last run**: matched, unmatched, dropped, ambiguous, and the stale paths.\n4. **A side table, one row per (path, line, name, target), with no tie-break at write**: a line that calls two definitions of one name stores both. The target is one printable name per language, with no tool or crate version, and the definition's path and line where the index holds it. The staleness facts (the file's content hash when the tool read it, the language, the run) are held once per file. A run joins only against the bytes the tool read, replaces exactly the files it resolved in one transaction, writes nothing when it fails, and purges rows whose path has left the index; reconcile never deletes resolved rows. One schema step.\n5. **The decoder is hand-written**: a pure module in intentsvcs, fields by number with the numbers named, unknown fields skipped by wire type.\n6. **The export builds into Intent's own ignored directory under `intent/.cache`**, never the workspace's target directory.\n7. **Arms**: decode and join read a checked-in fixture index, and the absence arm runs with the tool off PATH. **The toolchain arm (hv, 2026-09-17, first-hand)**: one end-to-end arm per language on a tiny fixture project (for Rust: spawn rust-analyzer, export, decode, join, store), marked `#[ignore = \"needs <tool> on PATH; bin/devbin test all runs it\"]`, with `needs_the_toolchain` in its module path, failing by name when the tool is absent and never skipping. It stays out of CI for now and runs on this machine whenever `bin/devbin test all` and `bin/devbin fullcycle` run; dc adds the devbin option that runs `-- --ignored needs_the_toolchain`, and the lane that lands the first such arm carries that line in the same landing, so no such arm exists unrun.\n\n## Build order\n\nThe level-3 core first, alone, as its own bank (the tables, per-file replace, staleness, purge, the five counts, the verb and its register row), proven with an in-memory reader, so WP-06 stacks on it. The envelope's `index.resolution` field (a language whose tier is missing, failed or stale) is built on WP-04's landing. Then the SCIP decoder and the Rust reader, and the landing, which also corrects design.md's line that rust-analyzer is not installed on this host.\n\n## The level-3 core, as landed\n\nThe core lands alone, ahead of any reader (vc, 2026-09-17), so `intent index resolve` refuses every language by name until a language's reader registers in `index::resolved::readers()`. Beyond decision 25 it carries vc's rulings of 2026-09-17 on dc's review of the reader contract:\n\n- **A run is forced full** when the language has no stored run, its last run did not store, or its record's extractor version differs from the build's. An incremental run over a build cache that already exists would otherwise trace nothing and report an empty tier as a success.\n- **The record carries the extractor version it joined against** (`resolution.symbols_version`), so a build writing another version makes every resolved path of that language stale.\n- **The index catches up after the tool has read and before the join**, never before the tool runs: an edit made before the tool's read joins, and an edit made after it is dropped as moved and retraced by the next run. A refresh that fails fails the run.\n- **`dropped` is counted by reason**: the core's own (`no-line`, `outside-the-project`, `unread`, `not-indexed`, `moved`) and a closed roster each reader declares, where a reason nobody declared fails the run. `matched`, `unmatched` and `dropped` sum to everything the tool emitted, and an arm holds that law.\n- **A target the tool does not locate** is placed at the definition row the reader chooses among the rows that print it: exactly one by default, and the first clause for Elixir.\n- **A project holding nothing for a tool** (no `Cargo.toml`, no `mix.exs`) is not applicable: a run over every declared language names it and leaves its record alone, and a run that asked for it with `--lang` records a failure.\n- **A reader finds its project in the index's file rows** (`Scope::indexed`), never by walking the tree or asking git. Rust's roots are the root-most `Cargo.toml` manifests, each exported on its own.\n- **A build directory Intent cannot create fails that language's run** rather than refusing the verb.\n\n## The Rust reader, as landed\n\nrust-analyzer is Rust's reader, registered in `index::resolved::readers()`, so `intent index resolve` runs it for a project that declares `rust`. It carries vc's rulings of 2026-09-17 on its three questions:\n\n- **One export per root, built under Intent's own directory.** A root is a `Cargo.toml` the index holds with no ancestor holding another, and each is exported with `CARGO_TARGET_DIR` under `intent/.cache/resolve/rust`, never into the workspace's `target/`. `--full` changes nothing for this reader: every export is of the whole workspace, and cargo decides what to rebuild.\n- **A file is offered as read only where its bytes hash the same before and after the export**, so a file saved during the export drops its references as unread rather than joining them against lines the tool never read.\n- **One printed target per definition, with no tool or crate version in it**: `crate::path::Type::method()`, a field plain, `<Type as Trait>::method()`, `name!`, `#[Derive]`, and `<impl Type>` for an impl block itself. A callable ends in `()` because a field and a method of one name are two definitions. The module in a member's path is the one its impl block is written in, which is what SCIP carries, and not always the module that defines the type. A crate's `-` prints as `_`.\n- **Four reasons of the reader's own, each declared**: `local` (a local variable, closure or parameter), `multiline` (an occurrence spanning lines), `operator` (a reference whose text holds no letter, digit or underscore, which the export writes for an overloaded operator on the operator and on the space either side of it) and `unprintable` (a symbol it cannot print). Empty text, `self.0`, `Self`, `crate`, `super` and a raw identifier are not operators: they go to the join, and count as unmatched where no written row holds them.\n- **The export can write one file into the project.** Where a workspace root holds no `Cargo.lock`, cargo writes one there, as any cargo command would. The reader leaves it and never deletes or moves a file in the project tree, and the verb's register row says so beside the build scripts and proc macros it runs.\n- **A tool that is not there is recorded as missing, naming the program**, and a project whose index holds no `Cargo.toml` is not applicable.\n- **The arms**: the checked-in export of a tiny crate (`tests/fixtures/scip/tiny/`) proves the decode, the printed targets, the reasons and the conservation law in every build; a program that does not exist proves the missing record; and one `needs_the_toolchain` arm runs the real rust-analyzer over the same crate, under `bin/devbin test all` and never in CI.\n",
      "preamble": ""
    },
    {
      "seq": 6,
      "title": "Elixir resolved references through the compiler's tracer, on an explicit verb",
      "scope": "L",
      "status": "wip",
      "objective": "",
      "body": "## Measured first (dc, 2026-09-17)\n\nOn a scratch copy of Laksa (Elixir 1.20.4, OTP 29, 537 `.ex` files; source, `deps/` with their `.git` directories, and `_build/dev` copied; the live estate untouched). The tracer was a module loaded with `elixir -r` and passed as `mix compile --tracer`, writing one row per event.\n\n- **A full forced compile** took 69 s, exited 0 and recompiled no dependency. It produced 383,641 events (40 MB): `remote_function` 195,535, `alias_reference` 70,754, `remote_macro` 53,840, `imported_macro` 22,549, `require` 10,423, `import` 8,236, `local_function` 6,475, `imported_function` 5,441, then `alias_expansion`, `struct_expansion`, `alias`, `on_module`, `defmodule`, `start` and `stop` (537 each), `imported_quoted` and `compile_env`.\n- **Most events are compiler expansion, not written references.** The top `remote_function` targets are `:erlang` 45,644, `Module` 39,458, `:elixir_def` 18,758 and `:elixir_quote` 9,752, generated by `use Ash.Resource` and similar and attributed to the `use` line. No field this first tracer recorded separated written from expanded; the compiler's own `:column` does (below). 21,664 rows are exact duplicates, 10,718 have no line, and 19 \"files\" are not project files (`@builtin`, `@component`, `@plugs`, `@prototype_schema`, and paths inside Elixir's own `lib/`).\n- **The module distinction exists.** `get` arrives as `Access.get/2` 1,822, `Process.get/2` 852, `Map.get/2` 428, `Map.get/3` 313 and `Keyword.get/3` 254.\n- **A tracer that raises aborts the project's compile.** The first tracer did, on the bare-atom events `:start` and `:stop`.\n- **An incremental compile sees only what it recompiles.** Mix digests content, so `touch` alone compiled nothing and traced 0 events. Restoring a changed file compiled that 1 file and traced 90 events.\n- **A failed compile leaves a plausible partial trace.** A forced full compile with one leaf file broken exited 1 after 16 s, with a `MismatchedDelimiterError` naming file, line and column. By then it had traced 244,414 events from 340 of 537 files, with 340 `:start` and 263 `:stop`. The gap between the 69 s and 16 s runs is unexplained on this host's load, so no timing claim is made beyond tens of seconds.\n- **A written reference carries a column and an expansion does not** (dc, on a probe Mix project, Elixir 1.20.4 / OTP 29). Every written kind carried `:column` in its meta: remote, imported and local calls, remote and local captures, pipe targets, `__MODULE__` calls, the directives and module references. Generated events carried none: a written `to_string(total)` traced `Kernel.to_string/1` with a column and its expansion `String.Chars.to_string/1` without one, on the same line under the same name, so the join alone would have stored both. A syntax error fails before any `:start`, and an undefined function runs from `:start` to `:stop` and still exits 1, so the exit status is what names a failed compile.\n\n## Rulings (vc, under the pen, 2026-09-17)\n\n1. **A resolved row always resolves a written reference.** Level 3 never adds a reference that level 2's syntax does not have. It joins a tracer event to the WP-03 syntax reference in the same project file, on the same line, with the same name, and records on that row the module, name and arity the compiler resolved. An event with no matching written reference is compiler expansion and is not stored as a reference. The written `use Ash.Resource` is itself a reference and resolves to that module. Events with no line, and events from files outside the project, are dropped, and duplicates collapse onto the one row. **Nothing is dropped silently:** `intent index status` counts events matched, expanded-unmatched and dropped per language, so a join that stopped matching shows. WP-05 applies the same join to SCIP occurrences, so both languages mean one thing by level 3. **Expansion is kept out twice** (vc decision 27): the tracer records only call and module-reference events, and a call or module event whose meta carries no column is counted as `expansion` and never joined, because a macro's expansion can share the written line and name. A trace whose reference events carry no column at all fails the run. The test cannot see a macro that reuses a written node's meta for a call it generates, and that call is treated as written.\n2. **The tracer never raises.** It catches everything around each event. A tracer fault is a failed run under 3, never an aborted project compile.\n3. **A failed run stores nothing.** On a non-zero exit, or any file with `:start` and no `:stop`, nothing from that run is stored, and the envelope names the failure (file, line, message) beside the syntactic answer. Earlier good level-3 rows are kept, each carrying the content hash of its file at trace time. A file whose bytes have changed since then answers with its level-3 rows marked stale, never as current.\n4. **Stored per file, replaced per file, in a build of Intent's own.** A run replaces exactly the files it compiled from `:start` to `:stop` whose bytes hashed the same before the compile and after it. The compile builds into Intent's own cache under `intent/.cache/resolve/elixir/<env>`, seeded from the project's `_build/<env>/lib` with its relative symlinks re-pointed, and re-seeded when `mix.lock` differs from the hash kept beside the seed. A run that seeds or re-seeds forces the compile, the first run and `--full` force it, and every other run is incremental. The project's `_build` is never written. It reads a Mix project at the project root only, and a project with no `mix.exs` there is not applicable. It runs on an explicit verb only, never from intentd. _This ruling first had the compile run in the project's own `_build`; vc replaced it at 06e046a89, in AC-06.1's text, after dc measured a shared build missing a file the user had compiled._\n5. **Proof, in Laksa:** the callers of `Map.get/2`, without `Access.get/2`, `Process.get/2` or `Keyword.get/3`. The design's Proof section is edited to match after WP-01's vocabulary lands.\n\nBuild order: after WP-03.\n",
      "preamble": ""
    },
    {
      "seq": 7,
      "title": "Level-3 surfaces: resolution in the envelope, resolved facts on a hit, and search by target",
      "scope": "M",
      "status": "done",
      "objective": "Make level 3 answerable from every door a reader uses. Every search answer carries `index.resolution`, index-wide, naming each language whose level 3 is not current as `missing`, `failed`, `stale` (with its paths) or `unresolved`, in words `search::resolution_words` owns and the register quotes verbatim; `unresolved` is a declared language this build resolves, with no run on record, whose reader's manifest the index holds, decided when asked and never stored. A reference whose key joins exactly one current resolved row answers at `level` 3 with `target`, `target_path` and `target_line`; one joining several keeps its syntax level and carries `candidates`; one in a stale file, or in a file that moved on disk, keeps its syntax level. `intent search --target <target>`, the MCP tool's `target` and the SQL door's `resolved.target` ask for the written references resolved to exactly that target: it narrows and is narrowed by every other door and filter, lists the references alone when nothing else is asked, keeps no definition, and takes no hit from a stale file, which the answer names. A target no resolved row names is refused with the resolved targets that end the same way (each compared whole, without a trailing arity and without a trailing `()`), and otherwise answered empty with a note that a target nothing references and a misspelt one read the same; an index with no stored resolution is refused with the remedy `intent index resolve`, named as a person's verb that runs the project's build. The terminal's row and the explorer's row say where a reference points, and the MCP tool carries the note on an answer asked by target.",
      "body": "",
      "preamble": ""
    }
  ],
  "criteria": [
    {
      "id": "AC-00.1",
      "text": "After hv's rebuild, on the Intent estate: the defined-but-never-referenced query over `symbols` no longer lists `is_local`, `AddressError` or `from_project`; `intent search --subkind method --in AddressError` lists each of its methods once; and a fresh Claude session asked where a Rust symbol is defined reaches the index's MCP search tool before grep.",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "intent/st/ST0076/ac-00.1-drives.md"
      }
    },
    {
      "id": "AC-01.1",
      "text": "For Rust and Elixir, every definition row carries its `subkind`, `container`, `container_kind`, `trait_name`, `arity` and `arity_min` as ST0076's design vocabulary table states, read from the file's own syntax only.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.2",
      "text": "One symbol row per (name node, kind) in every language, fixed at extraction and never by a `DISTINCT` at read; a definition's own name is never also a reference to it.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.3",
      "text": "A code file whose symbols an older extractor wrote is re-extracted by the next reconcile, and a store migrated to the typed schema keeps its rows until then; two row shapes never answer one query.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.1",
      "text": "Rust references by syntax include scoped calls (`Type::f(..)`), type uses in signatures and bodies, and names inside macro invocations, each with its qualifier as written and `level` 2; `intent search --context nearest_project` on this repository lists `views.rs:437` and `views.rs:439` (issue 0429).",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.2",
      "text": "Every reference row spans the name as written, not the enclosing call or item, and the change raises the extractor version so stored rows re-extract.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.1",
      "text": "Elixir references by syntax include remote calls, remote captures (`&Mod.fun/2`) and remote pipe targets, each carrying its module as the qualifier with `level` 2 and arity where written; local calls, local captures (`&fun/1`, with their arity) and local pipe targets are unqualified at `level` 1; and `alias`, `import`, `require` and `use` are references at `level` 1 whose name is the whole module. A module in a qualifier or a directive name has the file's own `alias` forms and `__MODULE__` expanded within the enclosing do-block, and nothing from `import` or `use` applied.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.1",
      "text": "`intent search` filters by hit kind (`--kind def`) and, as a separate flag, by symbol subkind (`--subkind struct`, from a roster derived from the query sources and refused by name when unknown) and by container (`--in <container>`); a search with `--subkind` or `--in` and no query lists the symbols that pass those filters, and a search with nothing to search for is refused with a remedy naming that form; `--context` and `--outline` honour every filter; `--context` prints each reference with its qualifier and names the level that answered; the SQL door and `intent schema` expose the new columns under the same names as the JSON hit; and the MCP search tool takes the same filters, the query-less form included.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.2",
      "text": "The search row's `when_to_use`, the MCP `instructions` line and the explorer's hits say what the index now answers and what it still does not, per level and per language, with no claim wider than the build.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-05.1",
      "text": "Rust level 3, on an explicit verb only and never from intentd unasked: rust-analyzer's SCIP export is read into the store, and a resolved row always joins a written reference (same file, line and name); `intent index status` counts matched, unmatched, dropped and ambiguous, and the paths whose resolved rows are stale; the export builds into a directory of Intent's own under `intent/.cache`; a missing rust-analyzer is recorded as missing with its reason and named by `intent index resolve` and `intent index status`, never stored as a current run that resolved nothing.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.1",
      "text": "Elixir level 3, on an explicit verb only and never from intentd unasked: the compiler tracer's events join written references (same file, line and name) and expansion events are not stored; the compile builds into a directory of Intent's own under `intent/.cache`, seeded from the project's build when one exists, and never writes the project's `_build`; the tracer never raises; a failed compile stores nothing new, names the failure, and marks earlier rows stale by content hash; rows are replaced per file; the first run and `--full` force the compile, and every other run is incremental.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.2",
      "text": "On a scratch copy of Laksa, the level-3 callers of `Map.get/2` are listed without any call to `Access.get`, `Process.get` or `Keyword.get`.",
      "kind": "non-test",
      "state": {
        "is": "unsatisfied"
      }
    },
    {
      "id": "AC-07.1",
      "text": "Every search answer carries `index.resolution`, index-wide, naming each language whose level 3 is not current as missing, failed, stale (with its paths) or unresolved, in words one function owns and the register quotes verbatim; a reference whose key joins exactly one current resolved row answers at `level` 3 with `target`, `target_path` and `target_line`, one joining several keeps its syntax level and carries them as `candidates`, and one in a stale file keeps its syntax level; the terminal, the explorer and the MCP tool say the same.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-07.2",
      "text": "`intent search --target <target>`, the MCP tool's `target` and the SQL door's `resolved.target` ask one question: the written references a current resolved row joins to exactly that target, a key that names other targets too marked as one of N; it narrows and is narrowed by every other filter, lists the references alone when nothing else is asked, keeps no definition, and takes no hit from a stale file, which the note names; a target no resolved row names is refused with the nearest targets when any end the same way, and otherwise its empty answer says that a target nothing references and a misspelt one read the same; and an index with no stored resolution is refused with the remedy `intent index resolve` and never answered as an empty list.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    }
  ],
  "tests": [
    {
      "id": "AT-01.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/symbols_are_typed_definitions.rs",
      "covers": [
        "AC-01.1",
        "AC-01.2"
      ],
      "status": "green",
      "note": "Red on 655bec8a6 before WP-01's first bank: the arms name columns and rows HEAD did not have. Green at f030c8804 (WP-01 landing, patch-id 829dca8a, vc's GO): Rust and Elixir typed definitions with container, trait and arity; an Elixir definition is never a reference to itself (red on the first bank with five self-references). Gate: whole intentsvcs and intent-cli suites."
    },
    {
      "id": "AT-01.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/one_definition_is_one_symbol_row.rs",
      "covers": [
        "AC-01.2"
      ],
      "status": "green",
      "note": "Red on 655bec8a6 before WP-01's first bank: the arms name columns and rows HEAD did not have. Green at f030c8804 (WP-01 landing, patch-id 829dca8a, vc's GO): a Rust and a Swift method defined once are one definition row, red on HEAD before WP-01. Gate: whole intentsvcs and intent-cli suites."
    },
    {
      "id": "AT-01.3",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/symbols_are_typed_definitions.rs",
      "covers": [
        "AC-01.3"
      ],
      "status": "green",
      "note": "Red on 655bec8a6 before WP-01's first bank: the arms name columns and rows HEAD did not have. Green at f030c8804 (WP-01 landing, patch-id 829dca8a, vc's GO): a file whose version is cleared and rows untyped is re-extracted by the next refresh and no untyped row survives; store_schema_version's earlier-draft arm walks a rung-10 store through the rung 28 rebuilds carrying rows. Gate: whole intentsvcs and intent-cli suites."
    },
    {
      "id": "AT-02.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/symbols_are_qualified_references.rs",
      "covers": [
        "AC-02.1"
      ],
      "status": "green",
      "note": "Red on 35c238658 before WP-02: main had no qualifier, no token-tree patterns and call spans over the whole call. Green at d3b69c7de (WP-02 landing, patch-id 8d7b5cbc, vc's GO): scoped calls, uses, paths and type uses keep their qualifier at level 2, the last segment of a qualifier is a reference, and macro token trees give call, path, macro and token rows. Proof on a clone of main with the worktree binary: search --context nearest_project answers views.rs:212 def, run.rs:861, views.rs:390, 418, 437 and 439 -- grep's 6 lines, 437 and 439 inside assert_eq!."
    },
    {
      "id": "AT-02.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/symbols_are_qualified_references.rs",
      "covers": [
        "AC-02.2"
      ],
      "status": "green",
      "note": "Red on 35c238658 before WP-02: main had no qualifier, no token-tree patterns and call spans over the whole call. Green at d3b69c7de (WP-02 landing, patch-id 8d7b5cbc, vc's GO): a call with arguments over many lines spans its name's line, every reference pattern captures its name node, and EXTRACTOR_VERSION is 2 so rows WP-01 wrote re-extract."
    },
    {
      "id": "AT-03.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/symbols_are_qualified_elixir_references.rs",
      "covers": [
        "AC-03.1"
      ],
      "status": "green",
      "note": "Red on d4d61052c before WP-03, with cc's shapes (patch-id 69d6653d) and the query as it stood: all ten arms failed in wt-wp03 on its own target at 13:27Z, against the query whose published gap was that a remote call such as Repo.get(..) is a reference to get without its module and alias is not expanded. Green at 997d760b1 (WP-03 landing, patch-id 4b4ef24b, vc's GO, judged with cc's shapes as one train on d4d61052c): remote calls, captures and pipe targets carry their module at level 2 with the arity written or piped; local calls, local captures and local pipe targets stay unqualified at level 1; use, import, require and alias name the whole module, one row per brace entry; the file's aliases and `__MODULE__` expand within the enclosing do-block."
    },
    {
      "id": "AT-04.1",
      "kind": "test",
      "file": "native/rust/crates/intent-cli/tests/the_structural_doors_take_the_filters.rs",
      "covers": [
        "AC-04.1"
      ],
      "status": "green",
      "note": "Red on 70c65b464 before WP-04: 4 of its 5 arms fail there, because `--subkind` and `--in` are unknown arguments and a context hit carries no container, so the control reads `-` for both definitions of `new`; its SQL-door arm passes there, because WP-01 gave `symbols` its columns. Green at e5b7feb1b (WP-04 landing, patch-id 013ce9d4, vc's GO): `--context` and `--outline` honour `--kind`, `--lang`, `--subkind`, `--in` and `--limit`, and `--tier lexical` on a structural door is refused; a context hit names its subkind, container kind, arity and level; `--subkind method --in AddressError` with no query lists `is_local` alone, `--in AddressError --kind def` lists `new` and `is_local`, and the MCP tool answers the same groups; a search with nothing to search for is refused on both faces, naming `--subkind` and `--in`; an unknown subkind is refused in the same words by both; the SQL door and `schema ddl.sql` carry the symbol columns. Gate: whole intentsvcs, intent-cli and intentd suites."
    },
    {
      "id": "AT-04.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/a_subkind_is_a_word_the_index_writes.rs",
      "covers": [
        "AC-04.1"
      ],
      "status": "green",
      "note": "Red on 70c65b464 before WP-04: the arm does not compile there, because the index has no subkind roster (`symbols::subkinds`) and search has no filter words (`search::FilterWords`). Green at e5b7feb1b (WP-04 landing, patch-id 013ce9d4, vc's GO): every subkind the extractor writes for a Rust and an Elixir fixture is in its language's roster, Elixir's clause words come from elixir.scm, a language with no query has no roster, and an unknown subkind is refused with the roster of the languages in scope."
    },
    {
      "id": "AT-04.3",
      "kind": "test",
      "file": "native/rust/crates/intent-cli/tests/the_search_pane_is_resident.rs",
      "covers": [
        "AC-04.2"
      ],
      "status": "green",
      "note": "Red on 70c65b464 before WP-04: the arm does not compile there, because a hit has no `symbol` facts and `run::arrive` takes no note; driven in a pty before the fix, the pane's info row held only the key hints (issue 0435). Green at e5b7feb1b (WP-04 landing, patch-id 013ce9d4, vc's GO): a symbol hit's row reads `ref call in Other`, the note names level 1 and Rust's macro gap, `run::arrive` puts it on the screen's hint, and a view with no note clears it (issue 0435)."
    },
    {
      "id": "AT-04.4",
      "kind": "test",
      "file": "native/rust/crates/intent-cli/src/mcp.rs",
      "covers": [
        "AC-04.2"
      ],
      "status": "green",
      "note": "Red on 70c65b464 before WP-04: the arm does not compile there, because neither `search::level_words` nor `symbols::what_a_reference_misses` exists. Green at e5b7feb1b (WP-04 landing, patch-id 013ce9d4, vc's GO): the search row's `when_to_use` and the MCP instructions carry level 1's and level 2's words and every language's gap from `what_a_reference_misses`, verbatim."
    },
    {
      "id": "AT-05.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/a_resolved_reference_joins_a_written_one.rs",
      "covers": [
        "AC-05.1"
      ],
      "status": "green",
      "note": "Red on 5afc4c295 before the level-3 core: the arm does not compile there, because that tree has no intentsvcs::index::resolved (read from the tree, not run). Green at e302be033 (the level-3 core, judged as patch-id d335d31f in one whole-suite run and landed as f7050940, the difference being a two-line type annotation the critic gate required, which vc checked as a tree diff; vc's GO): nine arms prove the join against written references and its conservation law, the per-file replace, the failure record, staleness under another extractor and after an edit, the purge of a path that left the index, a language this build cannot resolve refused by name, and an undeclared reason failing the run. Green again at 92cc064d3 in the Rust reader's judging run (patch-id 73e75262), with its private row helper replaced by the suite's Fixture::resolved_in."
    },
    {
      "id": "AT-05.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/rust_references_resolve_through_rust_analyzer.rs",
      "covers": [
        "AC-05.1"
      ],
      "status": "green",
      "note": "Red on bd4430428 before the Rust reader: the arm does not compile there, because that tree has no intentsvcs::index::rust_analyzer or intentsvcs::index::scip (read from the tree, not run). Green at 92cc064d3 (the Rust reader, judged as patch-id 73e75262 in one whole-suite run and landed as d23dd52b with dc's devbin toolchain option 5f5682c9; vc's GO): the checked-in export of a tiny crate resolves Widget::new(3), store::open(..) and widget.size() to tiny::Widget::new(), tiny::store::open() and tiny::Widget::size() at their definitions, the method and not the field of the same name, counts four locals and three operator references by reason, and holds the conservation law over every reference occurrence in the export; a program that does not exist is recorded as missing and named; a project whose index holds no Cargo.toml is not applicable; and the needs_the_toolchain arm runs the real rust-analyzer over the same crate, building under intent/.cache/resolve/rust and never the crate's target/, green in the judging run's toolchain step, which is the cargo line dc's devbin option runs."
    },
    {
      "id": "AT-07.1",
      "kind": "test",
      "file": "native/rust/crates/intent-cli/tests/every_face_says_what_level_three_knows.rs",
      "covers": [
        "AC-07.1"
      ],
      "status": "green",
      "note": "Red on 73c427a93 before WP-07: the arm does not compile there, because `Facade::with_resolvers`, `search::resolution_phrase` and the states `resolved::MISSING`, `FAILED`, `STALE` and `UNRESOLVED` do not exist, and neither does the reader seam its shared estate implements (`Resolver::manifest`, `resolved::Manifest`, `rust_analyzer::MANIFEST`): 8 errors in the intent-cli suite, compiled in a worktree with its own target. Green at 828794ec7 (WP-07 landing, patch-id 4bad6a9b, vc's GO): with a resolution stored through the door by a handed reader, `--context helper` answers the call at level 3 with `target`, `target_path` and `target_line` flat on the hit and `index.resolution` empty, `--context other` keeps level 1 and lists two `candidates`, and the terminal's row, the MCP envelope and the explorer's row say the same; a file changed since it was resolved keeps its syntax level and `index.resolution` names rust `stale` with its path, and a failed and a missing run are named with their tool, place and detail while what an earlier run stored still answers at level 3, each in `resolution_words` on the terminal and on the explorer's INFO row; a carried language is named only where the index holds its Cargo.toml: with none, neither a missing record nor a failed run asked for by name is named, though `index status` shows the failure, and once one is held the failed run is named and a language with no record reads `unresolved`, its words naming `intent index resolve` as a person's verb that runs the project's build; the register quotes level 3's words and every state's phrase verbatim. Gate: whole intentsvcs, intent-cli and intentd suites."
    },
    {
      "id": "AT-07.2",
      "kind": "test",
      "file": "native/rust/crates/intent-cli/tests/a_search_by_target_asks_one_question.rs",
      "covers": [
        "AC-07.2"
      ],
      "status": "green",
      "note": "Red on 73c427a93 before WP-07: the arm does not compile there, because `search::UNKNOWN_TARGET` does not exist, and neither does the reader seam its shared estate implements (`Resolver::manifest`, `resolved::Manifest`, `rust_analyzer::MANIFEST`); and the installed pair at ea81a6c58, whose binary source equals that tree's, refuses `intent search --target` as an unexpected argument. Green at 828794ec7 (WP-07 landing, patch-id 4bad6a9b, vc's GO): `--target crate::helper()` lists the two references and no definition, and narrows `--context`, `--outline`, `--path`, `--subkind`, `--in`, `--lang`, `--limit` and a text query; a key naming another target too reads `one of 2`; a kind filter that leaves references out is refused with a target, and so is `--daemon`; the MCP tool's `target` answers the same groups and the SQL door's `resolved.target` the same rows; `--target helper` is refused naming `crate::helper()`, and a target nothing is near answers empty with the note that a target nothing references and a misspelt one read the same, on both faces; a file that moved on disk takes no hit and is named in `index.stale`, and once read again its stale resolution is named in the note; an index with no stored resolution is refused with the remedy `intent index resolve` on both faces. Gate: whole intentsvcs, intent-cli and intentd suites."
    }
  ],
  "attachments": [
    {
      "path": "ac-00.1-drives.md",
      "text": "# ST0076 AC-00.1: hv's drive, recorded by vc\n\nAC-00.1 is a non-test criterion with three clauses, each driven on the Intent estate after a rebuild on 2026-09-17. hv ran the commands of the first two clauses through the harness, so their output is in vc's session verbatim; the third is a fresh Claude session's behaviour, driven by a script.\n\n## Clause 1: the defined-but-never-referenced query\n\nOn the pair rebuilt at c800b8319 (store schema 28, the index re-extracted at extractor version 2), hv ran:\n\n```\nintent search --sql \"SELECT d.name FROM symbols d WHERE d.lang = 'rust' AND d.kind = 'def' AND d.name IN ('is_local', 'AddressError', 'from_project') AND NOT EXISTS (SELECT 1 FROM symbols r WHERE r.kind = 'ref' AND r.name = d.name) GROUP BY d.name\"\n```\n\nThe output was the header `name` and no rows, so none of the three reads as defined and never referenced. vc's read of the store beside it: `is_local` has six level-1 reference rows, the uses inside `assert!` that WP-02 made visible; `AddressError` has fifteen at level 1 and five at level 2; `from_project` has five at level 2.\n\n## Clause 2: the methods of AddressError\n\nOn the pair rebuilt at 8ec04d9fa, which carries WP-04 (e5b7feb1b), hv ran:\n\n```\nintent search --subkind method --in AddressError\n```\n\nIt exited 0, printed the level note, and listed one row: `native/rust/crates/intentsvcs/src/address.rs:355  def method  AddressError  remedy`. The SQL door agrees that `remedy` is the only method whose container ends in `AddressError`, so each of its methods is listed once. The clause first read `--kind method`; hv ruled the `--subkind` spelling and the search that runs on filters alone on 2026-09-17 (hv decision 19).\n\n## Clause 3: a fresh session reaches the index before grep\n\nThe driver is banked at `refs/bank/vc/drive-0428`: a headless `claude -p` session with a fresh session id, the project's prompt gate released by touching that session's sentinel, the tools ToolSearch, Grep, Glob, Read and `mcp__intent__intent_search` allowed, and the prompt \"Where is the Rust function pick_up defined in this repository? Give the file and line. Do not edit anything.\" A Skill call is not counted, because the project's CLAUDE.md sends every session to `/in-session` first.\n\nWithout any instruction in CLAUDE.md (pair at c800b8319), three runs: Grep first; Grep first; and the index first, which was hv's own run. A separate session quoted the MCP server's `instructions` back verbatim, so issue 0428's delivery works and the text was not followed.\n\nWith the instruction appended to the system prompt, three runs of three reached the index first.\n\nWith the `## Finding code` section in the real CLAUDE.md (template at 8ec04d9fa, this project's CLAUDE.md regenerated at 196284266) and the pair at 8ec04d9fa, three fresh sessions started at 2026-09-17 13:43Z. All three called ToolSearch with `select:mcp__intent__intent_search` and then `mcp__intent__intent_search` with `kind` def, `query` pick_up and `lang` rust; none called Grep first; each answered `native/rust/crates/intentsvcs/src/store.rs:2608`.\n\nThe limit of this clause: which tool a session reaches for is the model's choice and varies between runs. This records three of three on this date with the section in place, against one of three without it; it is not a guarantee.\n",
      "bytes": 3303,
      "sha256": "c9dde5f94cbcbe92c2c1f4b6219dfd7c4bc959937d84e5e625f54bb5abb8fdda"
    },
    {
      "path": "design.md",
      "text": "# Design - ST0076: a typed symbol index for Rust and Elixir\n\n## What hv asked for\n\nOn 2026-09-16, after driving the index by hand, hv said the index has to give typed, semantic-aware search over source, not something little better than grep. hv accepted vc's recommendation: keep what tree-sitter already gives, add Intent-written reference queries, and then add resolved references. Rust and Elixir are essential; Swift can wait.\n\n## What the index is today, measured 2026-09-16\n\nST0069's T2 tier stores one row per tags-query capture: `path, lang, name, kind (def | ref), start_line, end_line`. Measured on the store at 21:20Z and read from the grammars by cc and vc:\n\n- **The capture's category is discarded.** Every grammar's tags query already says function, method, class (struct, enum, union, type alias), interface (trait), module or macro. The store keeps only `def` or `ref`, so nobody can ask for structs named X or the methods of a type.\n- **No container.** A method does not record the impl, trait or module it belongs to; an Elixir function does not record its `defmodule`.\n- **Duplicate definitions.** Rust's tags query matches a method twice, once under `declaration_list` as a method and once as a bare `function_item`, and a raw query cursor emits both. 1857 (path, name, line) groups hold two def rows. Swift's query has the same shape.\n- **Rust references are thin.** `@reference.call` matches `function: (identifier)` and method calls only. Scoped calls (`Type::f(..)`, `AddressError::X(..)`), type uses in signatures and bodies, and anything inside a macro invocation (`assert!`, `format!`, `vec!`) are not references. `is_local` reads as never referenced; all six uses are inside `assert!`.\n- **Elixir references drop the module.** A remote call `Repo.get(..)` is stored as a reference to `get`, the same as `Map.get(..)`. Arity is not stored, `def`, `defp` and `defmacro` are one kind, and aliases are not expanded.\n- **Swift has no references at all.** tree-sitter-swift 0.7.3 ships definition patterns only.\n- References are name matches, and the design said so; there is no resolution tier. T4, type-aware, was parked in ST0069.\n\nST0069 ruled on 2026-09-12 (AC-20.2) that no language gets an Intent-written tags query. hv's ruling of 2026-09-16 reverses that for Rust and Elixir.\n\n## What typed search means here\n\nThree levels, each a strict improvement, each usable alone:\n\n1. **Typed definitions.** Every definition row carries its kind (function, method, struct, enum, trait, type, module, macro, and for Elixir public function, private function, macro, guard, delegate), its container (the impl, trait or module; for Elixir the module), its arity where the language has one, and its full span. One row per syntax node.\n2. **Qualified references, by syntax.** Intent-written queries for Rust and Elixir record what a reference names as written, with its qualifier: `AddressError::new` as name `new` qualified by `AddressError`, a type use as a type reference, calls inside macro token trees, Elixir `Repo.get/2` as `get` qualified by `Repo`, with `alias` expanded within the file. Still not resolved: a reference says what the source wrote, and every surface says so.\n3. **Resolved references.** A reference joined to the definition it actually names, from the language's own toolchain rather than from Intent's parse:\n   - **Rust:** rust-analyzer's SCIP export (`rust-analyzer scip <path>`), read into the store. Installed on this host by Homebrew at `/opt/homebrew/bin/rust-analyzer` (measured 2026-09-17); where it is absent, availability is a named absence and never a silent empty tier.\n   - **Elixir:** the compiler's own tracer events (`remote_function`, `local_function`, `imported_function`, `alias_reference` and the macro variants, each with module, name, arity and line), collected by compiling the project with a tracer. Compiling runs the project's macros and its dependencies' code, so it runs only on an explicit verb in the project's own environment, never from intentd unasked.\n\nEach level answers questions the one before cannot: level 1 answers the methods of `AddressError` and every struct named `Config`; level 2 answers the places that write `AddressError::new`; level 3 answers the callers of this `new`, and nothing else.\n\n## Decisions ruled\n\n- **hv, 2026-09-16:** levels 1 and 2 now, then level 3; Rust and Elixir required; Swift not required. AC-20.2 is reversed for Rust and Elixir: Intent keeps its own reference queries for those two grammars beside the grammars' tags queries.\n- **vc, under the pen:** one row per syntax node, fixed at extraction and never by a `DISTINCT` at read. The extractor carries a version, and a store indexed by an older extractor re-extracts rather than mixing rows. A resolved tier that did not run (tool absent, compile failed) is named in the envelope with its reason, beside the syntactic answer, never an empty result.\n\n## Row vocabulary, as built\n\nA `symbols` row keeps `kind` as `def` or `ref`. What the row IS goes in `subkind`, and where it sits goes in `container` and `container_kind`, both read from the file's own syntax and never from another file. `index_file.symbols_version` records the extractor version that wrote a file's rows; a code file indexed by any other version is stale, and the next reconcile re-extracts it.\n\n| Column           | Rust                                                                                                                                                                                                                                                                                                                                                     | Elixir                                                                                                                                                                                                                                                                                 | Swift and Lua                                                                                                           |\n| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |\n| `subkind`, def   | `function`, `method` (takes `self`), `assoc_fn`, `struct`, `union`, `enum`, `variant`, `field`, `trait`, `type`, `const`, `static`, `module`, `macro`                                                                                                                                                                                                    | `module`, `protocol`, `impl`, `struct`, `exception`, and a clause as spelled: `def`, `defp`, `defmacro`, `defmacrop`, `defguard`, `defguardp`, `defdelegate`, `defn`, `defnp`                                                                                                          | the grammar tags query category: Swift `class`, `function`, `interface`, `method`, `property`; Lua `function`, `method` |\n| `subkind`, ref   | `call` (a function, method or scoped call, inside a macro's arguments too), `macro`, `type` (every type name, an impl's trait and type included), `path` (a scoped path that is not called, and a qualifier of one segment such as `Foo` in `Foo::new()`), `use` (a name a `use` imports), and `token` (any other identifier inside a macro's arguments) | `call` (a local or remote call, a pipe target, and a capture such as `&format/1` or `&Repo.get/2`), `module` (a module name, expanded), and a directive as spelled, `use`, `import`, `require` or `alias`, named by the whole module, one row per brace entry                          | Lua `call`; Swift has none                                                                                              |\n| `container`      | the innermost enclosing module, impl type, trait, struct, union, enum or variant; a module inside a module is joined with `::`                                                                                                                                                                                                                           | the innermost enclosing module, protocol or impl, joined with `.`                                                                                                                                                                                                                      | none                                                                                                                    |\n| `container_kind` | `module`, `impl`, `trait`, `struct`, `union`, `enum`, `variant`                                                                                                                                                                                                                                                                                          | `module`, `protocol`, `impl`                                                                                                                                                                                                                                                           | none                                                                                                                    |\n| `trait_name`     | the trait an `impl Trait for Type` implements, on the rows inside it                                                                                                                                                                                                                                                                                     | none                                                                                                                                                                                                                                                                                   | none                                                                                                                    |\n| `arity`          | parameters; a method's arity excludes `self`                                                                                                                                                                                                                                                                                                             | arguments of the clause, each clause its own row; on a reference, the arguments of a remote call, one more for a pipe target, and the number after a capture's slash; none on a local call                                                                                             | none                                                                                                                    |\n| `arity_min`      | equal to `arity`                                                                                                                                                                                                                                                                                                                                         | `arity` less the arguments with a `\\\\` default                                                                                                                                                                                                                                         | none                                                                                                                    |\n| `qualifier`      | the path as written on a scoped call, path, type or `use`, and the one segment before `::` inside a macro; NULL at level 1                                                                                                                                                                                                                               | the module written on a remote call, capture or pipe target, with the file's `alias` forms (`as:`, the brace form, an Erlang module) and `__MODULE__` expanded to the end of the enclosing do-block, an alias written in a `do:` keyword body holding to that end too; NULL at level 1 | NULL                                                                                                                    |\n| `level`          | 1 for a definition or an unqualified reference, 2 for a reference with a qualifier                                                                                                                                                                                                                                                                       | 1 for a definition, an unqualified reference or a directive, 2 for a reference with a qualifier                                                                                                                                                                                        | 1                                                                                                                       |\n\nA definition's own name is never also a reference to it: an Elixir `def` head with parentheses and a module's name are definitions only.\n\n`level` says how much a row knows. Level 1 is a typed definition or an unqualified reference, read by syntax. Level 2 is a reference with its qualifier: Rust stores the path as written (WP-02); Elixir stores the module with the file's own `alias` forms and `__MODULE__` expanded, scoped to the containing do-block, and with no `import` or `use` applied (WP-03). Level 3 is a reference resolved to the definition it names by the language's own toolchain (WP-05, WP-06).\n\nRust and Elixir are read by Intent's own queries, `index/queries/rust.scm` and `index/queries/elixir.scm`, which replace those grammars' tags queries. Swift and Lua keep their grammars' tags queries. One row per (name node, kind) holds for every language, so a Rust or Swift method matched by two tags patterns is one definition.\n\n## Work packages\n\n- **WP-01 Typed definitions (all grammars, M).** Kind, container, arity, full span; one row per node; extractor version and re-extraction. Swift and Lua get kind and dedupe for free and nothing more.\n- **WP-02 Rust reference queries (M).** Scoped calls, type uses, macro token trees, with the qualifier stored.\n- **WP-03 Elixir reference queries (M).** Remote calls with their module, captures (`&Mod.fun/2`), pipes, `alias` expansion in the file, `use`, `import` and `require` as references, arity where written.\n- **WP-04 Surfaces (M).** `intent search` filters by hit kind, symbol subkind and container (`--kind def`, `--subkind struct`, `--in AddressError`) and, with no query, lists the symbols that pass `--subkind` or `--in`; `--context` prints qualified references and says which level answered, the SQL door's `symbols` columns, the MCP tool's parameters and the `mcp_instructions` line, the explorer's hits.\n- **WP-05 Rust resolved references (L).** Measure first: rust-analyzer's install path, SCIP export time and size on this repository, what a missing tool reports. Then the ingest; the envelope is WP-07's.\n- **WP-06 Elixir resolved references (L).** Measure first on a real Elixir estate (Laksa or Lamplight): tracer output, compile time, what a failed compile reports. Then the ingest and the `resolved` level.\n- **WP-07 Level-3 surfaces (M).** `index.resolution` in every search answer, naming each language whose level 3 is not current; a reference at `level` 3 with its `target`, `target_path` and `target_line` where one resolved row names it, and its `candidates` where several do; and `intent search --target <target>`, the MCP tool's `target` and the SQL door's `resolved.target`, refusing a target nothing resolved names when resolved targets end the same way, and an index where nothing is stored.\n\nOrder: WP-01, then WP-02, WP-03 and WP-04 in parallel, then WP-05 and WP-06, with WP-07 on WP-05's level-3 core. Issue 0429 is descoped into this thread.\n\n## Proof\n\nThe thread closes on its acceptance criteria (`intent ac list ST0076`): one or more per work package, each held by the arms its lane cites, plus two that no arm can hold.\n\n- **AC-00.1, after hv's rebuild on the Intent estate:** the defined-but-never-referenced query that failed on 2026-09-16 no longer lists `is_local`, `AddressError` or `from_project`; `intent search --subkind method --in AddressError` lists its methods once each; and a fresh Claude session asked where a Rust symbol is defined reaches the index's MCP search tool before grep.\n- **AC-06.2, on a scratch copy of Laksa:** the level-3 callers of `Map.get/2` are listed without any call to `Access.get`, `Process.get` or `Keyword.get` (vc's ruling on dc's measurement, carried in WP-06's body).\n\nIssue 0429 closes on WP-02's landing: `intent search --context nearest_project` lists `views.rs:437` and `views.rs:439`, both inside `assert_eq!`.\n",
      "bytes": 17667,
      "sha256": "7bb5d7356e666009fd85e5ac51d14e2d35717335e2de5b7f175e8841dd8002f9"
    }
  ]
}
