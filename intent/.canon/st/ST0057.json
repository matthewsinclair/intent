{
  "schema": "intent/thread@3.0",
  "id": "ST0057",
  "title": "Disk as a sparse projection of the store",
  "slug": "disk-as-a-sparse-projection-of-the-store",
  "status": "wip",
  "created": "2026-08-18",
  "objective": "Make disk optional without making anything unrecoverable.\n\nEvery file under `intent/st` is there because that is where v2 kept truth. Under D01-as-reversed the store is truth and the files are a projection -- **but a projection nobody can regenerate is not a projection, it is the original with extra steps.** This thread ends with three things true at once: `intent/st` holds little more than an index and the live work; anything not on disk is provably reproducible; and a human with an editor and no working tool can still read the whole project.",
  "context": "**hv ruled this thread INTO the 3.0.0 gate on 2026-08-18:** _\"Definitely BEFORE the release. We're getting this whole thing feature complete before we release 3.0.0.\"_ It is a pre-release thread, not a follow-on.\n\nIt was surfaced the way the problem is meant to be surfaced: hv looked at their own file tree, saw 50-odd hydrated steel-thread directories, and asked why. Measured at `6accab7e` -- 57 directories, 797 files, of which 468 belong to threads nobody is working on (52 completed, 2 cancelled, 1 not-started); `.intentfiles` and `intent/.canon/` do not exist.\n\n**The brief collapses two layers and the design separates three.** hv's framing was _\"the DB is SSOT and disk is a sparse copy of the db out to realised files based on an index\"_ -- right for views, fatal for canon. **Sparseness applies to VIEWS; canon is never sparse.** If the manifest governed canon, an unrealised artefact would exist only inside a gitignored database: absent from a fresh clone, destroyed by `rm -rf intent/.cache`. **D29 -- a gitignored path is never canon -- is what makes a clone complete**, and it is what makes every deletion here safe rather than careful: `organize` only ever removes a file it can regenerate, from a source sitting beside it in the same commit.\n\nThe design is ratified as D57-1 through D57-8 in `design.md`. This contract is derived from those decisions and adds none of its own.",
  "body": "",
  "preamble": "",
  "wps": [
    {
      "seq": 1,
      "title": "Canon relocation to intent/.canon/",
      "scope": "M",
      "status": "wip",
      "objective": "Move per-artefact canon out of `intent/st/<ID>/` and into `intent/.canon/st/<ID>.json`, so that `intent/st` can empty. Foundational: every other WP in this thread assumes the layout.",
      "body": "## Deliverables\n\n- Canon at `intent/.canon/st/<ID>.json` and `intent/.canon/issues/<n>.json`, still ONE FILE PER ARTEFACT.\n- Migration of existing canon, with the conservation verdict green over a pinned subject before anything is removed.\n- `intent/.canon/` explicitly NOT gitignored, asserted by a test rather than by inspection.\n\n## Why C, and why not A or B (D57-1)\n\n**A -- canon in place.** Bytes fall ~60% but `intent/st` still lists 56 entries, so it does not meet the requirement.\n\n**B -- one consolidated `threads.jsonl`.** Rejected: four concurrent writers make a single canon file a merge-conflict generator, and this estate has four nodes.\n\n**C -- ruled.** The only option that empties `intent/st`, keeps a clone complete, and keeps a per-artefact diff so two nodes editing two threads never conflict. It is a path change, not a model change.\n\n## The constraint that must not be lost\n\n**D29: a gitignored path is never canon.** Canon is the layer that TRAVELS -- if `intent/.canon/` were ignored, an unrealised artefact would exist only inside a gitignored database, absent from a fresh clone and destroyed by `rm -rf intent/.cache`. That single rule is what makes every deletion in this thread safe rather than careful.\n\n## Done when\n\n`intent/st` holds `steel_threads.md` and the realised threads, nothing else; a fresh clone reads every thread whether realised or not.",
      "preamble": ""
    },
    {
      "seq": 2,
      "title": ".intentfiles: the manifest and its refusing grammar",
      "scope": "S",
      "status": "wip",
      "objective": "The committed manifest that declares which artefacts are realised as on-disk files, with a grammar that refuses rather than skips.",
      "body": "## Deliverables\n\n- `.intentfiles` at the project root of `intent/`, committed.\n- Two regions: lines between `BEGIN`/`END` markers GENERATED by `intent organize` from status; lines outside are PINS, never rewritten or removed.\n- Parser + writer, with the generated region rewritten in place and pins preserved byte-for-byte.\n\n## The grammar, and why it REFUSES\n\n`<SIGIL>:<ID>`, sigil in `STEELTHREAD | ISSUE`, optional trailing comment, nothing else. **A line the parser cannot read aborts the run with its line number.**\n\nA skipped line drops an artefact from realisation and leaves an estate **indistinguishable from one that never listed it** -- the silent-drop shape v2.19.0 already paid for twice, in `ac gate`'s F1 fix and in the AT row grammar's `at lint`.\n\n## Why two regions\n\nA computed default and a written file collide. Without the split: pin ST0011 to read it, it closes, `organize` regenerates the block from status, **and the pin is gone along with the files, with nothing in the output naming the decision.**\n\n## Why committed\n\nThe set it governs is committed. A machine-local manifest would let `organize` on one machine delete files another machine depends on, and land the deletion in a commit with no declared reason. Committed, hydration and dehydration are reviewable -- the diff shows the manifest line changing beside the files appearing or disappearing.",
      "preamble": ""
    },
    {
      "seq": 3,
      "title": "Attachment canon: opaque as file, and the naming gate",
      "scope": "M",
      "status": "not-started",
      "objective": "Give opaque attachments a canon home so they can dehydrate, and put a naming gate in front of every new attachment.",
      "body": "## Deliverables\n\n- **Text attachments** stay inline as `Attachment.text`.\n- **Opaque attachments** stored as FILES at `intent/.canon/st/<ID>/<path>`.\n- The naming gate: improperly-named NEW attachments are REFUSED at ingest.\n- `sha256` drift detection on the working copy, surfaced through `doctor`.\n\n## Why the canon file is what makes dehydration legal (D57-7)\n\nD57-3 permits removal only of a file `organize` can regenerate **from a source sitting beside it in the same commit**. It does not say the source must be JSON. The canon copy IS a source, it IS in the same commit, and regeneration is a byte copy.\n\n**The rejected first answer was \"opaque attachments never dehydrate.\"** Measured, that pins 240 non-md/json files to disk permanently -- 196 of them `.tap` baselines under `ST0056/parity/tools/tap-baseline` outliving ST0056 forever -- which is the exact condition `.intentfiles` exists to end.\n\n**`intent/.cache/` is refused as a home.** It is gitignored, holds only the store, has 0 tracked files, and its ignore rule is D29's precondition. And `cache` naming an unregenerable file fails in the dangerous direction: `rm -rf intent/.cache` is a reasonable thing to do to a cache.\n\n## Rejection is NOT retroactive\n\nThe estate already carries a violator -- a `.webloc` with SPACES in its name, which split into eight fragments under `xargs` while the run still printed a plausible four-digit total. Existing violators are reported by `organize` as UNCLAIMED under its fifth row and **never silently removed**.\n\n## Known hazard for whoever takes this\n\ncc's collection verifier carries a SECOND, deliberately independent copy of the classifier's eligibility rules, so that it can disagree. **When the opaque-attachment form lands, that copy goes stale silently unless it is deleted or promoted to a real test.** It has a shelf life and cc has written that into it.",
      "preamble": ""
    },
    {
      "seq": 4,
      "title": "intent organize: four answers, one refusal, one gate",
      "scope": "L",
      "status": "wip",
      "objective": "The reconciliation verb: hydrate, verify, dehydrate, and refuse -- with a gate that makes removal fail-safe by construction.",
      "body": "## The table (D57-3)\n\n| declared | on disk                              | action                                                        |\n| -------- | ------------------------------------ | ------------------------------------------------------------- |\n| yes      | absent                               | **HYDRATE**                                                   |\n| yes      | present                              | **VERIFY** -- re-render in memory, write only if bytes differ |\n| no       | absent                               | nothing                                                       |\n| no       | present                              | **DEHYDRATE**, subject to the gate                            |\n| --       | a path the renderer does not produce | **UNCLAIMED -- report, never remove**                         |\n\n**The fifth row is the one that matters.** A file the renderer cannot make is either an attachment or something a human put there. `organize` must not be the thing that decides an unrecognised file is rubbish.\n\n## The dehydration gate\n\nBefore removing any view: re-render from the store into memory, compare to the bytes on disk, **refuse on any difference and report the path.** A difference means the disk holds something the store does not, and that something is destroyed by the removal. One render, one comparison per file -- fail-safe by construction rather than by discipline, and the same comparison `doctor` already performs.\n\n## It never resolves an attachment divergence\n\n**Authority follows AUTHORSHIP.** A view is authored in the model, so disk divergence means the FILE is stale. An attachment is authored ON DISK, so divergence means the STORE is stale. Same divergence, opposite remedies. `organize` reports the path and names both verbs -- **a tool that decides silently is choosing whose work to discard, right most of the time and catastrophically wrong occasionally, which is the worst profile available.**\n\n## Idempotence is measured, not assumed\n\nRun twice, changes nothing, **including mtimes**. Measured 2026-08-18: the current render re-emits 255 of 1000 `.md` files byte-identically every pass, moving mtime with zero content change. Harmless for conservation, not harmless for `file_index`, whose `clean`/`changed` state is computed from exactly that.\n\n## It locks against ANY process\n\n`info`, `st list`, `doctor` and `export` all materialise the store on access, so a peer typing `intent st list` opens the same file `organize` is reconciling against. **\"The estate was quiet when I measured\" is not establishable over any window** -- so the defence is the moment-of-act digest: digest the measured tree, re-compute immediately before the irreversible step, refuse on any difference.",
      "preamble": ""
    },
    {
      "seq": 5,
      "title": "intent edit <ID>",
      "scope": "S",
      "status": "wip",
      "objective": "Hydrate an artefact on demand and open it, under a verb name that is not already taken four times over.",
      "body": "## Deliverables\n\n- `intent edit <ID>` -- hydrate if absent, then open.\n- Pins the artefact in `.intentfiles` for the duration of the work, or leaves the pin to the operator; **that choice is open and belongs to this WP.**\n\n## Why not `wip` (D57-4)\n\nhv proposed `intent wip <ID>`. **In this project `wip` already means a WP status, a thread status, `intent/wip.md`, and every node's whiteboard.** A fifth meaning collides with four live ones.\n\n## Note\n\n`st edit` currently exists as an UNIMPLEMENTED command whose `--help` fully describes behaviour it does not have (_\"Print the absolute path to a steel thread file\"_, with a `[FILE] [default: info]` argument). That help text is a claim nothing checks -- the same shape as the `llm guide` defect fixed at `04e4206f`. Resolve the collision as part of this WP rather than leaving two verbs with one meaning.",
      "preamble": ""
    },
    {
      "seq": 6,
      "title": "The full text realisation, and the refusal that expires",
      "scope": "M",
      "status": "not-started",
      "objective": "Prove a human can get the whole estate back as text without the tool -- a different assurance from the dehydration gate, and a precondition of sparseness rather than a nicety that follows it.",
      "body": "## Deliverables\n\n- A complete text realisation into `.backup/text/<UTC>/`.\n- `export --format md`, whose current refusal is withdrawn AS PART of this thread.\n\n## Why it is not the same assurance as the gate (D57-5)\n\nThe dehydration gate proves **THE STORE holds it**. A complete text export proves **A HUMAN can get it back without the tool.** Only the first was covered, and hv supplied the second.\n\n## The refusal whose reason expires\n\n`export --format md` is refused today on the grounds that _\"the views are already in the tree\"_. **That is correct now and false the moment the disk model lands** -- which is why it is withdrawn as part of THIS thread and not before. A refusal whose justification expires must be retired by the change that expires it, or it survives as a rule nobody can re-derive.\n\n## Precondition found by cc\n\n**`intent init` is NOT IMPLEMENTED.** A fallback cannot be demonstrated from a clean directory nobody can create, so `init` is in scope here or must land ahead of it.",
      "preamble": ""
    },
    {
      "seq": 7,
      "title": "intent:// addressing and read resolution",
      "scope": "L",
      "status": "wip",
      "objective": "Give every hydratable artefact a durable address that does not depend on whether it is currently on disk, and one resolver serving both faces.",
      "body": "## Why (D57-8)\n\nHydration makes a file path a statement about a moment. **Measured at `ce532a97`: 80 citations of `intent/st/ST####/<file>.md` in tracked estate prose**, the most-cited single artefact being `ST0034/design.md` at 23 -- a COMPLETED thread, and therefore among the first to dehydrate. Three AT rows also point inside `intent/`; the other 87 point at `native/rust` code and are unaffected.\n\n## The scheme\n\n```\nintent:///threads/{stid}\nintent:///threads/{stid}/wp/{wpid}\nintent:///threads/{stid}/ac/{acid}\nintent:///threads/{stid}/at/{atid}\nintent:///threads/{stid}/attachments/{path}\nintent:///issues/{issueid}\nintent:///nodes/{moniker}\nintent:///nodes/{moniker}/inbox/{sender}/{stamp}\nintent:///events/{id}\n```\n\n**Empty authority means THIS project.** Cross-project references carry the slug and resolve against intentd's registry.\n\n**VIEWS GET NO URL.** A view is derivable from its entity, so a reference to a view is a reference to its source. Giving views addresses re-creates, inside the scheme, the conditionality the scheme exists to remove. `?format=` selects a REPRESENTATION; a path segment would name a separate thing.\n\n## One resolver, two faces\n\nResolution lives in `intentsvcs`. The CLI calls it in-process; intentd calls **the same function** and serves it over GraphQL. **The failure mode to guard is intentd growing its own resolver because GraphQL wants different shapes** -- two resolvers agreeing exactly until one moves, with nothing watching.\n\n## `?format=md` needs no new renderer\n\n`views::render_all` already returns `Vec<View { path, content }>` with three callers -- `facade.rs:1236` (what `sync` writes), `doctor.rs:737` (skew), `migrate.rs:293`. Serving `View.content` makes **\"the served markdown byte-matches the file `organize` would hydrate\" true BY CONSTRUCTION**, because `View.path` is where it would land. `views::skew()` is already the gate's render-and-compare.\n\n## Four outcomes, and two of them must not look alike\n\nhydrated -> content; **dehydrated -> content from the store, and SAY SO**; never-existed -> refuse; withdrawn -> refuse DISTINGUISHABLY. _\"Not on disk\"_ must never be reported as _\"does not exist\"_ -- a class this estate has paid for four times.\n\n## AC to write\n\n**No daemon may be required to read your own project.** If reading a dehydrated thread came to need a running intentd, the disk model would have made the estate LESS accessible than a pile of markdown.",
      "preamble": ""
    },
    {
      "seq": 8,
      "title": "The mutation surface: write-by-address and the missing verbs",
      "scope": "L",
      "status": "wip",
      "objective": "Make every state mutation reachable through a typed, addressed interface -- and close the verbs that do not exist.",
      "body": "## The write path (hv, 2026-08-18)\n\n**DB first, then canon ALWAYS, then views IF MARKED.** Not \"disk if marked\" -- that collapses canon into views and leaves a dehydrated artefact inside a gitignored database, absent from a fresh clone.\n\n## Create splits by id origin\n\n- **Caller-assigned** (`AC-10.11`, `AT-10.11`) -- `PUT` to the entity address, idempotent.\n- **Server-assigned** (threads, issues, WP seq) -- `POST` to the COLLECTION address, which returns the new address. You cannot address `ST0058` before the tool has decided it is `ST0058`.\n\n## The mutation format IS the interchange format\n\n`GET ?format=json`, modify, `PUT` the same shape back. **This gives AC-02.6 a second job: a field that does not round-trip is a field that cannot be WRITTEN**, so the lossless 1-1 mapping becomes the completeness guarantee for the whole mutation surface, not only a durability guarantee at the clone boundary.\n\n**`PUT` accepts `json` only.** Markdown-in would promote a stale rendering into canon, which 5.1b forbids. The one exception is not an exception: an ATTACHMENT is authored on disk, so for attachments authority runs the other way and text-in is correct.\n\n## The enumerated gap this closes\n\nMeasured 2026-08-18 across the whole surface:\n\n| entity         | create       | state transitions | free-text fields                       |\n| -------------- | ------------ | ----------------- | -------------------------------------- |\n| Thread         | `st new`     | 8 verbs           | **none**                               |\n| WorkPackage    | `wp new`     | 4 verbs           | **none** (`rescope` covers scope only) |\n| Criterion      | **missing**  | 6 verbs           | **missing**                            |\n| AcceptanceTest | **missing**  | green/red/na      | **missing**                            |\n| Issue          | `issues add` | close/open        | **missing**                            |\n\n**Creation is missing for two of five entities. Free-text mutation is missing for all five.** This WP was itself written by editing `thread.json` directly, because no verb sets a work package's objective.\n\n## Also in scope\n\n- **`intent at green|red|na` DESTROYS the row's authored note** (issue 0033) -- a verb that mutates one field and silently clobbers an adjacent one is the opposite of type-checked.\n- **A bare family invocation reaches a mutation**: `intent backup` with no subcommand creates a snapshot, PRUNES an existing one under an unconsulted retention policy, and rewrites the store file.\n- **`st edit` is unimplemented while its `--help` fully describes behaviour** it does not have.",
      "preamble": ""
    },
    {
      "seq": 9,
      "title": "The event log records the model and not the disk",
      "scope": "S",
      "status": "wip",
      "objective": "**Every op that changes the MODEL is recorded; no op that changes the DISK is.** `intent/events.jsonl` is the one table that cannot be re-derived from anything else on disk, and the acts it does not witness are the destructive ones. Close the gap at the boundary, not by widening the chokepoint that is already right.",
      "body": "## The chokepoint exists and it is good\n\n**`Facade::apply` (`facade.rs:3504`) is the single door for model mutation, and it earns the name.** It does not trust a caller-supplied list of what changed -- it DIFFS `next` against the loaded canon, because the declared-list version made *the mutation did not persist* reachable by naming the wrong id, with the database and the return value both reporting success. Comparing against loaded canon cannot forget, and it generalised to issues for free.\n\nEverything reaching it is logged: `ac.put`, `at.put`, `at.set`, `issues.add`, `st.new`, `wp.new`, `wp.rescope`, and the whole `set_thread_status` family -- `st.triage`, `st.start`, `st.hold`, `st.resume`, `st.done`, `st.reopen`, `st.reinstate`, `st.cancel`.\n\n## And the realisation verbs do not reach it\n\n**`grep -c 'apply(' organize.rs` returns 0.** `Facade::organize`, `sync_to_disk`, `sync_from_disk` and `hydrate` each write the filesystem without passing the door. The one other caller of `Store::append_event` is the text-backup path at `facade.rs:1913`, and it is there to obtain the database's stamp rather than to witness an act.\n\n**Measured consequence, 2026-08-19: `organize --apply` removed 423 files from this estate and the log recorded nothing.** The log's 55 events at that moment were 31 `at.set`, 9 `wp.new`, 6 `wp.rescope`, 6 `wp.start`, and one each of `st.new`, `st.triage`, `st.start`. **The only act all evening that destroyed anything is the only class of act absent from the record.**\n\n## Why this is the log's problem rather than organize's\n\n`sync_to_disk` states the log's standing itself: *history travels only if something writes it out; every other entity in this set can be re-derived from a file that is already there, this one cannot.* **A record that is unrecoverable by design and silent about deletion is answering the wrong question.** A reader asking *what happened to ST0001* gets `st.new` and nothing since, while the thread's files left the tree an hour ago.\n\n## Two things NOT to do\n\n**Do not route `organize` through `apply`.** `apply` diffs canon; `organize` changes no canon at all -- disk is the subject. Forcing it through would make the diff a no-op and the event a lie about its own mechanism. The realisation ops need their own emission against the same `Store::append_event`, with the subject being the PATH SET rather than an artefact id.\n\n**Do not add a pruner.** Growth is not the problem and a retention policy on the one unrecoverable table trades a real loss for an imaginary one. Measured: 55 events in the first 35 hours, ~229 bytes each, on the heaviest four-node day this project will see. At that rate a year is ~17k events and under 4MB.\n\n## What IS worth deciding at the same time\n\n**Concurrent append.** It is a tracked, append-only file written by four sessions committing minutes apart, so every concurrent write is a same-line git conflict waiting to happen. Seven commits have touched it without one, which is luck at 55 rows rather than a property. **Decide it while there are 55 rows to migrate, not 17,000** -- per-node event files that merge, or a store-side sequence that the file is projected from.\n\n## Raised by\n\nhv, 2026-08-19, asking how long until `intent/events.jsonl` becomes unwieldy. The answer on size is *not in any timeframe worth planning for*; the answer on fitness is this work package.",
      "preamble": ""
    },
    {
      "seq": 10,
      "title": "doctor reports a dehydrated view as missing",
      "scope": "S",
      "status": "done",
      "objective": "**`intent doctor` exits 1 with 234 findings on a healthy estate, every one of them instructing the operator to regenerate a file the design says should not exist.** Found by vc at `aa74c0d7`, minutes after the dehydration shipped. **The argument against this is already written down in this codebase, one file over, and was applied to attachments and not to views.**",
      "body": "## What it does now\n\n    $ intent doctor\n    rc=1\n    residue: intent/st/ST0001/info.md -- view-skew -- generated view is missing; regenerate it\n    residue: intent/st/ST0001/acceptance.md -- view-skew -- generated view is missing; regenerate it\n    ... 234 findings\n\n**Every one is a thread that `.intentfiles` does not declare, so its views are absent BY DESIGN.** `views.rs:1023` maps `Err(_)` from reading the view straight to `generated view is missing; regenerate it`, with no reference to the manifest. Before tonight every thread was realised, so the arm could only fire on a genuine loss; **the dehydration turned a correct check into a check that flags a population behaving correctly.**\n\n## The reasoning already exists and it is not a near-miss, it is the same sentence\n\n`doctor.rs:813`, on the ATTACHMENT path:\n\n> *Under `.intentfiles` a dehydrated thread's attachments are legitimately gone from disk -- that is the feature, not a fault. Reporting absence as divergence would make `doctor` unhealthy for every dehydrated thread in the estate, which is a check that flags a population behaving correctly, and the shape that gets a check deleted rather than fixed.*\n\n**That is a prediction of this exact defect, written by the author of the sibling path, and the views path kept the old behaviour because it is a different function in a different file.** The estate now demonstrates the predicted outcome at 234 instances and a non-zero exit.\n\n**This is the day's class in its sharpest form.** Not two artefacts disagreeing with no third thing reading both -- **one artefact STATING THE RULE while its sibling violates it.** A reader who checks whether the codebase has thought about dehydrated absence finds `doctor.rs:813`, reads a correct and complete argument, and stops.\n\n## What the fix has to satisfy\n\n**Absence is silence ONLY where the manifest says the artefact is not realised.** `doctor.rs`'s own wording is the boundary to copy: *that silence is bounded rather than blanket*. A view absent for a DECLARED artefact is still a real finding and must stay one -- that is a genuine loss, and blanket-silencing the `Err(_)` arm would trade 234 false findings for a silent real one.\n\n**So the check needs the manifest, and the manifest is already parsed** (`intentfiles::parse`, 3 callers). The shape is: declared and absent -> finding; undeclared and absent -> silence; present -> compare bytes as now.\n\n**And the mirror case is worth deciding in the same pass: a view PRESENT for an UNDECLARED artefact.** Today that is `organize`'s `unclaimed`, and `doctor` says nothing about it. One of the two surfaces should own it rather than both assuming the other does.\n\n## Why it is filed rather than fixed\n\nFound during `/in-finish`, which is documentation-only by its own contract. **The estate is not damaged and no data is at risk** -- `doctor` is a report, the 234 findings are false rather than destructive, and the remedy each one prints (`regenerate it`) would be answered by `organize` re-hydrating nothing, since the manifest does not declare them.\n\n**But `doctor` exits 1, so anything gating on it is now red on a healthy tree**, and that is the part to check first tomorrow.",
      "preamble": ""
    }
  ],
  "criteria": [
    {
      "id": "AC-00.1",
      "text": "**No dehydration path removes any file while any declared precondition is unmet, and the refusal names every unmet one.** The set of preconditions the gate enforces is read from the SAME single declaration `design.md`'s list renders from, so the two cannot disagree -- a gate that checks a hand-copied subset of its own list is the failure this criterion exists to prevent. Denominator printed: preconditions checked against preconditions declared. **THE DECLARATION THIS ROW READS FROM DOES NOT EXIST (vc 2026-08-19, measured before assigning the row).** The criterion requires the gate to read _the SAME single declaration `design.md`'s list renders from_, and **canon has no precondition field at all** -- ST0057's top-level keys are `schema, id, title, slug, status, created, objective, context, body, preamble, wps, criteria, tests, attachments`, and a path scan for anything matching precondition/gate/dehydrate returns nothing. **`design.md`'s _What must be true before ANY dehydration ships_ section at `:243` IS the list; it renders from nowhere.** So the row as written presupposes an indirection the architecture does not have, and a builder implementing to it literally would create the second declaration this row exists to forbid. **TWO ROUTES AND THE PROPERTY IS SATISFIED BY EITHER, so it is the builder's call rather than the contract's: PARSE `design.md`'s section as the declaration -- cheapest, no schema change, and the gate then reads the one place the list lives -- or ADD A STRUCTURED FIELD to canon and render the section from it, which is more robust and is a schema change mid-gate.** **THE PROPERTY THAT MUST HOLD EITHER WAY IS THE ONE THE ROW ALREADY STATES AND IT IS UNCHANGED: preconditions checked equals preconditions declared, denominator printed, and a hand-copied subset is the failure.** The phrasing is corrected here rather than the property, because the property was never wrong. **RULED BY hv 2026-08-19: THE GATE READS A LIST OF AC IDS, AND THE DECLARATION IS THE DELIMITED BLOCK BELOW.** Canon is `deny_unknown_fields` (`model.rs:154`, D05 -- every struct `additionalProperties: false`), so a new top-level field is a Rust model change and a schema bump rather than a canon edit. **The declaration therefore lives HERE, in the criterion that requires it, and an AC id is a machine-shaped token rather than prose -- `AC-\\d\\d\\.\\d+` is stable against every rewording of the sentences around it, which a seven-bullet markdown list is not.** The block is DELIMITED so the gate reads exactly it and never the surrounding prose, because ids are named elsewhere in this row for other reasons and a whole-text scan would sweep them in. **<<PRECONDITIONS AC-00.2 AC-00.4 AC-03.1 AC-03.2 AC-03.3 AC-03.4 AC-03.5 AC-06.1 AC-06.2 AC-07.1 AC-07.2 AC-07.3 AC-07.4 AC-07.6 PRECONDITIONS>>** **EIGHTEEN IDS, AND THE MAPPING FROM `design.md`'s SEVEN BULLETS IS ONE-TO-ONE EXCEPT WHERE STATED: attachments / `THREAD_PROSE` deletion (D57-6) is AC-00.2; `ROOT_FILES` is AC-00.4; opaque-attachment canon (D57-7) is all of WP-03; the full text realisation (D57-5) is all of WP-06; addressing (D57-8) is all of WP-07.** **THE SEVENTH IS NOT COVERED AND THE PARTITION SAYS SO RATHER THAN LEAVING A READER TO COUNT: _issue bodies_ landed, carried verbatim rather than trimmed, and HAS NO CRITERION -- so the gate cannot check it, and its absence from the block is a stated exception rather than an omission.** Six of seven checkable, one named as uncheckable, seven accounted for. **THE CONSERVATION VERDICT WAS THE NINETEENTH AND hv WITHDREW IT, 2026-08-19, SO IT IS OUT OF THE BLOCK RATHER THAN SITTING IN IT UNMET** -- the gate is right that `withdrawn` is _dropped, which is not met_, and a withdrawal must never silently unlock a gate; **the denominator is where a withdrawal belongs, and it moved 19 -> 18.** hv's grounds: _if we need safety, we've got all the historical STs etc in git. We can always get stuff back._ AC-00.3 is a MIGRATION-conservation verdict standing as a DEHYDRATION precondition, so it proves against a loss git already prevents. **AND FOUR MORE CAME OUT OF THE BLOCK ON hv's EXPLICIT WORD, 2026-08-19: AC-03.6, AC-06.3, AC-06.4, AC-07.5, taking the denominator 18 -> 14 and the gate to 0 unmet.** vc put the choice to hv in one line -- clear the remaining four and run it now, or wait for them to land -- and hv answered *Word*. **THESE FOUR ARE NOT WITHDRAWN AND THEIR WORK IS NOT DROPPED: they remain live criteria of this thread and every one of them is still owed.** The distinction is dc's and it is the useful half of the reversibility test: **the question was never whether the work is wanted, it is whether a GATE should hold on it.** dc's own ruling, applied here in full: AC-06.3 is CAPABILITY (the third `Projection` variant makes `md` sayable rather than refused) and AC-06.4 is a reversibility proof AS A PRECONDITION while `intent init` is independently required as hosting work -- *same build, different warrant, and only one of the two gates*. cc's answer likewise stands: AC-00.4 and AC-03.6 are capability and detection, not reversibility proofs, so hv's git grounds never touched them -- they leave the block by hv's direct instruction rather than by the test. **WHAT THE BLOCK NOW MEANS IS NARROWER AND SHOULD BE READ THAT WAY: fourteen preconditions that are MET, holding a gate that is open, over an estate whose recovery story is git.** **AND THE GATE COMPOSES WITH WHAT ALREADY EXISTS: `ac status` computes satisfaction, so the gate asks it rather than reimplementing it -- which is what stops this becoming a second answer to _is this criterion met_.** The property is unchanged and was never wrong: preconditions CHECKED equals preconditions DECLARED, denominator printed, and a hand-copied subset is the failure this row exists to catch.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-00.2",
      "text": "**`THREAD_PROSE` names no filename in the classifier, and every `design.md`, `impl.md` and `tasks.md` under `intent/st/` is carried as an attachment** (D57-6). Property, not count: for every file matching the three names, the artefact is present in canon and its bytes round-trip. The enumerated total is printed as the denominator, so a carry that covers some is distinguishable from one that covers all.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-00.3",
      "text": "**The conservation verdict is green at full scope over a pinned subject, and the finding names the revision it was measured at** -- `HEAD` is a pointer and a conservation figure that does not name its subject cannot be re-checked.",
      "kind": "non-test",
      "state": {
        "is": "withdrawn",
        "reason": "hv ruled the risk this guards does not need guarding: \"If we need safety, we've got all the historical STs etc in git. We can always get stuff back. It's not a problem!\" -- and, on the sequencing: \"if something stops working because we have to do A before B then that is totally fine. We have the pen here in Intent so we have the power to do what we need to do.\" This criterion is a MIGRATION-conservation verdict standing as a DEHYDRATION precondition: it exists so that nothing dehydration removes can be lost. Git already holds every file dehydration would remove, so the proof is of a loss that cannot occur. Running a multi-step v2-corpus capture, census and full-scope audit tonight to unlock a deletion git already makes reversible is the yak-shaving hv named. Withdrawn, not satisfied -- the verdict was never run and this record must not imply it was.",
        "by": "hv (matts), 2026-08-19"
      }
    },
    {
      "id": "AC-00.4",
      "text": "**`ROOT_FILES` (`AGENTS.md`, `CLAUDE.md`, `usage-rules.md`) have a v3 generator before anyone reasons about them as derivable.** They are on disk, they are outside `.intentfiles` scope, and something emptied `AGENTS.md` on 2026-08-18 -- so their derivability is currently an assumption rather than a mechanism.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.1",
      "text": "**Canon for every thread and every issue resolves under `intent/.canon/`, one file per artefact, and `intent/st/` holds no `thread.json`.** Both populations are printed and the canon count equals the artefact count -- the criterion is the equality, not either figure.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.2",
      "text": "**No path under `intent/.canon/` is matched by any ignore rule, and a fresh clone contains canon for every artefact the source tree has** (D29). This is the criterion that makes a clone complete; it is checked by cloning, never by reading `.gitignore`, because the question is what git DOES and not what a rule appears to say.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.3",
      "text": "**The relocation is lossless: every field of every artefact round-trips through the move byte-identically**, denominator printed over the full field set. This is ST0056's AC-02.6 applied to the move -- a field that does not survive the relocation is data loss at the clone boundary.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.4",
      "text": "**Two threads edited concurrently produce diffs in two distinct files**, which is the property that rejected the consolidated `threads.jsonl` (D57-1 option B). Checked by editing two threads and observing the changed-path set, not by inspecting the layout.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.1",
      "text": "**The grammar REFUSES rather than skips.** The parser accepts exactly `<SIGIL>:<ID>` with sigil in `STEELTHREAD | ISSUE` and an optional trailing comment. For every rejected input the run exits non-zero AND the offending line number appears in the output. No input is silently skipped -- a skipped line drops an artefact from realisation and leaves an estate indistinguishable from one that never listed it.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.2",
      "text": "**RULED BY hv 2026-08-19, REPLACING THE TWO-REGION DESIGN. `.intentfiles` IS DURABLE STATE: THE RECORD OF WHICH DATABASE ARTEFACTS ALSO HAVE A REALISED FORM ON DISK.** **THE ARCHITECTURE IN ONE LINE, hv's: REALISATION IS DRIVEN FROM `.intentfiles`; COMMANDS CHANGE `.intentfiles`; `organize` REALISES IT.** **_AUTHORED_ WAS vc's WORD AND IT IS WRONG -- hv corrected it before anyone built on it.** It is NOT _never touched by an intent command_: **`st new` ADDS the id, `st done` REMOVES it, `st hydrate`/`st dehydrate` and the ISSUE equivalents do it directly, and a human may edit it by hand.** **THE ONE THING THAT CHANGED FROM THE TWO-REGION DESIGN IS THAT NOTHING RECOMPUTES IT.** There is no derivation from status that overwrites what is there, which is why the protected region is unnecessary: **a write is a CHANGE TO STATE, never a REGENERATION OF IT.** hv: _why isn't the organise operation simply: a) look at .intentfiles, b) hydrate the items in the file, c) dehydrate any previously hydrated items that are no longer in the file._ **THE TWO REGIONS EXISTED ONLY BECAUSE THE FILE WAS MACHINE-GENERATED: if `organize` rewrote the list from status every run, a hand-added line would be wiped, so a protected region was needed. Remove the regeneration and the protected region has nothing to protect against.** **IT ALSO EXPLAINS WHY `intentfiles::render` HAD NO CALLER: the thing it does is not needed.** No function of status, no negative pin, one writer, one source of truth. **`organize` MAKES DISK MATCH THE LIST, IN BOTH DIRECTIONS, AND REGENERATES NOTHING.** Listed and absent from disk -> written out. Present on disk and not listed -> removed. **Asserted as an equality of two independently derived sets with both counts printed**, and **a hand edit to the file survives every `organize` byte for byte**, because no code path rewrites it. **THE DATABASE ALWAYS HOLDS EVERY ARTEFACT IN FULL; the list decides only what is ALSO on disk, so dehydration is never a loss and hydration is a regeneration rather than a restore.**",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.3",
      "text": "**RULED BY hv 2026-08-19, REPLACING THE TWO-REGION DESIGN. `.intentfiles` IS DURABLE STATE: THE RECORD OF WHICH DATABASE ARTEFACTS ALSO HAVE A REALISED FORM ON DISK.** **THE ARCHITECTURE IN ONE LINE, hv's: REALISATION IS DRIVEN FROM `.intentfiles`; COMMANDS CHANGE `.intentfiles`; `organize` REALISES IT.** **_AUTHORED_ WAS vc's WORD AND IT IS WRONG -- hv corrected it before anyone built on it.** It is NOT _never touched by an intent command_: **`st new` ADDS the id, `st done` REMOVES it, `st hydrate`/`st dehydrate` and the ISSUE equivalents do it directly, and a human may edit it by hand.** **THE ONE THING THAT CHANGED FROM THE TWO-REGION DESIGN IS THAT NOTHING RECOMPUTES IT.** There is no derivation from status that overwrites what is there, which is why the protected region is unnecessary: **a write is a CHANGE TO STATE, never a REGENERATION OF IT.** hv: _why isn't the organise operation simply: a) look at .intentfiles, b) hydrate the items in the file, c) dehydrate any previously hydrated items that are no longer in the file._ **THE TWO REGIONS EXISTED ONLY BECAUSE THE FILE WAS MACHINE-GENERATED: if `organize` rewrote the list from status every run, a hand-added line would be wiped, so a protected region was needed. Remove the regeneration and the protected region has nothing to protect against.** **IT ALSO EXPLAINS WHY `intentfiles::render` HAD NO CALLER: the thing it does is not needed.** No function of status, no negative pin, one writer, one source of truth. **THE LIST WINS OVER STATUS: a `completed` or `cancelled` artefact that IS listed stays on disk through `organize`, and a `wip` artefact that is NOT listed is removed by it.** This is the property the pinned region used to buy, and it is now the ordinary behaviour rather than an exception -- **status has no vote at `organize` time at all.** Both directions checked, because a rule that only keeps things cannot be told from one that never removes any.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.4",
      "text": "**`.intentfiles` is tracked, and a realisation change lands in the working tree together with the manifest line that caused it** -- the reviewability property the committed-manifest ruling rests on. Checked by observing the changed-path set after an `organize` that changes realisation.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.1",
      "text": "**An opaque attachment's bytes live in canon as a file at `intent/.canon/st/<ID>/<path>`, and hydration reproduces the working copy byte-identically from it.** Denominator printed over every opaque attachment in the estate. Until this holds an opaque attachment cannot be dehydrated at all, because there is no source beside it in the commit.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.2",
      "text": "**Form follows content, both ways: no opaque attachment is stored inline and no text attachment is stored as a sibling file.** One rule, checked in both directions -- a check that only catches one direction passes an estate that has drifted the other way.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.3",
      "text": "**The naming gate rejects, at ingest, any attachment path that cannot be given both a canon path and a URL -- and rejection is NOT retroactive.** An existing violator (the estate already carries a `.webloc` with spaces in its name) is reported by `organize` as UNCLAIMED under its fifth row and never silently removed.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.4",
      "text": "**`sha256` in canon detects a working-copy edit by COMPARISON rather than inference**: an attachment modified on disk without the store knowing is reported by `doctor` as divergence, and the report names the path.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.1",
      "text": "**`organize` implements exactly the five rows of D57-3's table and every row is exercised**, with the fifth -- a path the renderer does not produce -- REPORTED and never removed. `organize` must not be the thing that decides an unrecognised file is rubbish.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.2",
      "text": "**The dehydration gate re-renders each view into memory, compares to the bytes on disk, refuses on ANY difference, and names the path.** A view carrying a hand edit is never removed. Fail-safe by construction rather than by discipline: one render and one comparison per file, on the removal path itself.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.3",
      "text": "**`organize` NEVER resolves an attachment divergence: it reports the path, names both verbs, and modifies neither side.** Authority follows authorship -- a view diverging means the file is stale, an attachment diverging means the STORE is stale, and one policy for both discards somebody's work.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.4",
      "text": "**`organize` run twice changes nothing, INCLUDING mtimes**: the second run writes zero files. Measured as the count of files whose mtime moved, which must be zero -- not as a content diff, because the defect being closed is a byte-identical re-emission that moves mtime and corrupts `file_index`'s clean/changed state.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.5",
      "text": "**`organize` refuses when the tree digest computed immediately before the irreversible step differs from the one it measured, and the refusal names the difference.** It locks against ANY process, not other `organize` runs -- every read verb materialises the store on access, so \"the estate was quiet when I measured\" is not establishable over any window.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.6",
      "text": "**AFTER `organize`, THE TREE HOLDS ONLY WHAT `.intentfiles` NAMES -- ASSERTED AS AN EQUALITY OF TWO INDEPENDENTLY DERIVED SETS, OVER THE WHOLE ESTATE.** (hv 2026-08-18, stating the expectation explicitly for the record.) The on-disk population is derived by LISTING THE FILESYSTEM and the declared population by PARSING `.intentfiles`; the criterion is `on_disk == declared`, **printed with both counts, and it is an EQUALITY rather than a containment in either direction** -- a subset check passes on a tree that removed nothing, and a superset check passes on an empty one. **THE REASON THIS ROW EXISTS: EVERY ONE OF ST0057 AC-04.1 THROUGH AC-04.5 IS SATISFIED BY AN `organize` THAT REMOVES NOTHING AT ALL.** Report-never-remove (04.1), refuse-on-hand-edit (04.2), never-resolve-a-divergence (04.3), run-twice-changes-nothing (04.4), refuse-on-digest-change (04.5). **They are all BRAKES, correctly specified, and not one of them says the car moves.** D57-3's table declares DEHYDRATE as an action and AC-04.1 requires every row EXERCISED, **which one dehydrated file satisfies** -- a subset of the right kind, which is the EXTENT failure this estate has now met three times. **THE POSITIVE CONTROL IS PART OF THE CRITERION, NOT THE TEST'S BUSINESS:** it must be driven FROM A FULLY REALISED TREE, because a run against an already-sparse fixture is the vacuous pass. **Measured at `ddf8a080`, before any of this lands: `intent/st/` holds 57 ST directories and 799 files, `intent/issues/` holds 40 canon files and 82 files total.** Those are the numbers the first real run must move. **INDEX VIEWS ARE NAMED, NOT INFERRED.** `intent/st/steel_threads.md` is renderer-produced but is not a per-artefact view and does not appear in the manifest; it and anything like it are declared as an explicit exempt set with the reason, **rather than left to be caught by D57-3's fifth row** -- UNCLAIMED means _a human put this here_, and using it to shelter something the renderer DOES produce would make the report meaningless. Attachments remain out by AC-04.3.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-05.1",
      "text": "**`intent edit <ID>` dispatches on id shape, hydrates when absent, and prints a path that EXISTS after the call** -- for every id shape the verb accepts, denominator printed over that set.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-05.2",
      "text": "**RULED BY hv 2026-08-19, REPLACING THE TWO-REGION DESIGN. `.intentfiles` IS DURABLE STATE: THE RECORD OF WHICH DATABASE ARTEFACTS ALSO HAVE A REALISED FORM ON DISK.** **THE ARCHITECTURE IN ONE LINE, hv's: REALISATION IS DRIVEN FROM `.intentfiles`; COMMANDS CHANGE `.intentfiles`; `organize` REALISES IT.** **_AUTHORED_ WAS vc's WORD AND IT IS WRONG -- hv corrected it before anyone built on it.** It is NOT _never touched by an intent command_: **`st new` ADDS the id, `st done` REMOVES it, `st hydrate`/`st dehydrate` and the ISSUE equivalents do it directly, and a human may edit it by hand.** **THE ONE THING THAT CHANGED FROM THE TWO-REGION DESIGN IS THAT NOTHING RECOMPUTES IT.** There is no derivation from status that overwrites what is there, which is why the protected region is unnecessary: **a write is a CHANGE TO STATE, never a REGENERATION OF IT.** hv: _why isn't the organise operation simply: a) look at .intentfiles, b) hydrate the items in the file, c) dehydrate any previously hydrated items that are no longer in the file._ **THE TWO REGIONS EXISTED ONLY BECAUSE THE FILE WAS MACHINE-GENERATED: if `organize` rewrote the list from status every run, a hand-added line would be wiped, so a protected region was needed. Remove the regeneration and the protected region has nothing to protect against.** **IT ALSO EXPLAINS WHY `intentfiles::render` HAD NO CALLER: the thing it does is not needed.** No function of status, no negative pin, one writer, one source of truth. **THE LIFECYCLE VERBS EDIT THE LIST AS PART OF THE TRANSITION, AND A CLOSING VERB WARNS BEFORE DEHYDRATING OVER UNSYNCED WORK.** `st new` adds the entry; `st new --dehydrate` does not; `st done` and `st cancel` remove it; `st reopen` and `st reinstate` add it back; `st hydrate` / `st dehydrate` / the ISSUE equivalents add and remove it directly. **`st done --keep` closes the artefact WITHOUT removing the entry.** **vc 2026-08-19, CORRECTING ITS OWN WORDING ON dc's TRACE: THE CLOSING VERBS DO NOT DELETE FILES, SO THE `REFUSE` CLAUSE IS WITHDRAWN.** `organize.rs:695` is the only line in the tool that removes an estate file. vc measured all 7 delete sites in `crates/*/src/`: the other six are `write_set`'s own rollback (4), `install`'s temp dir, and `backup::prune` -- which dc's enumeration missed and which is safe by a guard rather than by not existing, being confined to the snapshot directory by a `starts_with` check and only ever removing files it holds a row for. **`st done` and `st cancel` change DB state and the list; the realiser makes disk match; nothing else deletes anything.** **SO THE CLOSING VERBS WARN, NAMING THE PATHS, WHEN THE ARTEFACT HOLDS ON-DISK BYTES THE STORE HAS NEVER SEEN -- THEY DO NOT REFUSE, AND THEY ARE NOT A SECOND AUTHORITY OVER DELETION.** `Facade::sync_uncommitted` (AC-03.5) answers a question the realiser's gate does not -- _are there bytes here nobody has synced_ -- and asking it at the moment of closing puts that answer in front of the operator then, rather than at the next `organize` when they have moved on. **A SECOND GATE WOULD BE WORSE THAN AN ORDINARY DIVERGENT COPY: the two gates answer DIFFERENT questions, so they would disagree BY CONSTRUCTION rather than by drift, and a gate refusing on a different question than the one governing the destructive act refuses work the real authority would allow.** The refusal keeps its one home in the realiser's per-file gate, which already declines three ways: any byte difference on re-render; `AttachmentDiverged` reported with both remedies and neither side touched; and an attachment the store has never seen left `Unclaimed` and never removed.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-05.3",
      "text": "**Path-printing has ONE home: `intent edit` is `intent st edit`'s behaviour learning to hydrate first, not a second implementation of it** (Highlander). `wip` is not used as the verb -- it already means a WP status, a thread status, `intent/wip.md`, and every node's board.",
      "kind": "non-test",
      "state": {
        "is": "unsatisfied"
      }
    },
    {
      "id": "AC-06.1",
      "text": "**The realisation is complete and SAYS SO WITH A DENOMINATOR**: every thread, WP, issue, attachment and view appears, with the printed count equal to the canon totals. A partial export that reads as complete is worse than no export, so the denominator is part of the criterion rather than a nicety of the output.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.2",
      "text": "**It is regenerable and never authoritative**: written under `.backup/text/<UTC>/`, with no import path and `classify` never seeing a path under it. Checked by asserting the absence of a read-back route, not by intending one.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.3",
      "text": "**`export --format md` is accepted, and its refusal is removed in the SAME change that makes the refusal's premise false -- not before.** Retire a refusal with the change that expires its reason.\n\n**THE PREMISE IS NOW FALSE AND THE WINDOW HAS CLOSED, SO THIS IS A LIVE DEFECT RATHER THAN OWED CAPABILITY (dc, measured 2026-08-20).** The refusal's `instead` clause reads *\"The views are already in the tree -- `intent sync --to-disk` rewrites them\"*, and it is printed at rc=1 today. **Canon holds 57 threads; `intent/st/` holds 3.** The remedy therefore names a location that does not hold **54 of 57**, and it has been wrong since the dehydration at `e7f00e65`. The criterion asked for the refusal and its premise to expire together; they did not, and the refusal outlived its reason by a day.\n\n**AND THE CITATION IS STALE, WHICH WOULD SEND A BUILDER TO THE WRONG LINES:** `export.rs:192` and `:208` are named above as the md refusal, and both now land inside the YAML entry (`yaml` at `:213`, `md` at `:233`). **The line numbers moved and the row did not.** Cited positions are re-checked when this is picked up rather than trusted -- that is the second time in this thread a row has named a position that drifted.\n\n**The property is unchanged and was never wrong**: `md` becomes sayable rather than refused, as the third `Projection` variant, in the change that makes the refusal false. Only the row's account of the world caught up.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.4",
      "text": "**`intent init` creates a working project from an empty directory, and the text realisation is exercised end-to-end from one.** `init` is currently unimplemented, and this is a PRECONDITION of the assurance rather than a neighbouring gap: you cannot demonstrate a fallback from a clean directory if you cannot create a clean directory.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-07.1",
      "text": "**Every entity form in D57-8's list resolves by address, and resolution is implemented ONCE**: the CLI and intentd call the same function in `intentsvcs`. Two resolvers is the failure -- they agree exactly until one moves, with nothing watching. Checked by asserting a single implementation, not by comparing two outputs.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-07.2",
      "text": "**Views get NO address.** A reference to a view resolves to its entity or is rejected; no path segment names a view. This is what stops the scheme becoming a path alias and re-creating, inside the scheme, the conditionality it exists to remove.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-07.3",
      "text": "**`?format=` accepts exactly `json` and `md` and nothing else, and an entity with more than one rendering is split into distinct addresses rather than gaining a `?view=`.** If an entity has more than one rendering it is UNDER-ADDRESSED.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-07.4",
      "text": "**`?format=md` serves `View.content` from `views::render_all`, so the served bytes EQUAL the file `organize` would hydrate by construction rather than by test** -- `View.path` is literally where it would land. One renderer, three jobs.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-07.5",
      "text": "**NO DAEMON IS REQUIRED TO READ YOUR OWN PROJECT.** With intentd stopped, uninstalled and never started, every address in a FULLY DEHYDRATED estate resolves through the CLI. If reading a dehydrated thread came to need a running daemon, the disk model would have made the estate less accessible than a pile of markdown -- which inverts D57-5's whole reason for existing.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-07.6",
      "text": "**Empty authority means THIS project**, and a cross-project reference carries the slug and resolves against intentd's project registry. A reference that hard-codes the project name breaks on rename or fork, so the empty form is the one intra-project prose must use.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-08.1",
      "text": "**The write path is DB first, canon ALWAYS, views IF MARKED.** After any write, canon contains the change whether or not the artefact is realised. Not \"disk if marked\" -- that collapses canon into views and leaves a dehydrated artefact inside a gitignored database, absent from a fresh clone.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-08.2",
      "text": "**`GET ?format=json`, modify, `PUT` the same shape back is lossless for every field of every entity** -- the interchange format IS the mutation format, denominator printed over the field set. This gives ST0056's AC-02.6 its second job: a field that does not round-trip is now a field that cannot be WRITTEN.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-08.3",
      "text": "**`PUT` accepts json only and rejects md -- except for attachments, where text-in is correct.** The exception is not an exception: an attachment is authored on disk, so authority runs the other way. Authorship decides direction, and `Project::classify` is the single answer to what a file is.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-08.4",
      "text": "**Caller-assigned ids are a `PUT` to the entity address; server-assigned ids are a `POST` to the COLLECTION address returning the new address.** You cannot address `ST0058` before the tool has decided it is `ST0058`.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-08.5",
      "text": "**Every writable field of every entity is settable through the mutation surface, and a field that cannot be written is reported BY NAME.** The burning case is ST0011's `completed`, which is NULL on the estate's one genuinely wrong row and has no field-setter verb today -- the criterion is the completeness of the surface, with the unsettable set as the printed output. **SECOND BURNING CASE (dc 2026-08-18): an AT row's `note`.** It is the only place this contract records WHY a row is where it is, several notes are load-bearing paragraphs, and **the only verbs that touch the row DESTROY it** (issue 0033) -- so the model holds a field the typed API cannot edit, and the sole route is a hand-edit of canon followed by `sync --to-store`. Recorded HERE rather than under a new decision number deliberately: a second home for one requirement is the failure this criterion exists to name. **AND SETTABLE IS NOT THE ONLY TEST (dc 2026-08-18, catching this criterion before it ratified): A VERB THAT WRITES A FIELD AS A DESTRUCTIVE SIDE EFFECT OF DOING SOMETHING ELSE FAILS THIS CRITERION TOO.** `at green|red|na` DO write the row, so a checker asking merely _can every field be set_ passes on `note` while the verb empties it. The criterion is therefore two-sided: every writable field is settable **intentionally**, and no verb silently clears a field it was not asked to change. **AND THE SHARPEST INSTANCE IS THE CLOSING VERB ITSELF (dc): THE VERB THAT RECORDS THE OUTCOME ERASES THE REASONING.** That is worse than a missing verb, because it fires precisely when a row carries the MOST evidence -- **the rows nobody can afford to lose are the ones the closing verb hits hardest.** Demonstrated 2026-08-18: AT-10.11 was greened by hand-editing canon because `intent at green` would have destroyed the note holding the sha, the corpus, the binary hash, the rc=137 kill and the canary control. **THIRD BURNING CASE, AND IT IS BLOCKING A GATE (vc ruling 2026-08-18, on ic's ownership question): AN ATTACHMENT'S CANON RECORD HAS NO NARROW SETTER.** `intent sync` offers only `--to-disk` and `--to-store`, both whole-estate over 57 threads, so a node that edits one instrument cannot update that instrument's canon record without performing a write that can carry every other node's uncommitted worktree into canon. **The consequence is not inconvenience: gating `canon_commit_check.sh` would create a rule whose only compliant order -- sync canon first, then commit file and canon together -- is an operation two of four nodes are forbidden to perform**, and a gate that cannot be satisfied legally is one people learn to bypass. **The remedy is to remove the need for the permission, not to widen it.** Costed honestly: five instrument edits in one afternoon is five cross-node handshakes, and three canon-stale commits landed in the windows between them. **AND THE DECISIVE ARGUMENT IS NOT THE COST, IT IS THAT THE RULE IS CURRENTLY UNFOLLOWABLE (dc 2026-08-18, self-reported before vc found it).** dc's commit `8f652d1b` -- **carrying the document about this very class, including the ordering rule itself** -- committed an attachment without canon, and the instrument then printed dc's own remedy text back at them naming the file. **They could not have complied**: the compliant order is sync-canon-first-then-commit-both, `intent sync` has no operation smaller than all 57 threads, and it is vc's to run. **So that commit is PERMANENTLY DIVERGENT IN HISTORY -- a later sync repairs the NEXT commit and can never reach one already made.** **A rule its own author cannot obey while writing it down is not a discipline problem; it is a missing operation, and no amount of care closes it.** It also settles the gating question by demonstration: gated, dc would have been blocked from committing the document arguing for the gate. **FOURTH INSTANCE, AND IT IS A DIFFERENT AXIS FROM THE FIRST THREE (ic 2026-08-18, who traced the reach before reporting it and then declined to mint into canon while vc was live in the same files): THERE IS NO VERB THAT CREATES AN AC OR AN AT AT ALL.** `intent ac` is list/status/satisfy/unsatisfy/gate/descope/rescope/withdraw/reinstate and `intent at` is list/lint/green/red/na -- **every one transitions a row that ALREADY EXISTS.** Verified by reach rather than inferred from `--help`: no create in `facade.rs`, none in `graphql.rs` or `schema.graphql`, `ingest.rs` never reads `acceptance.md`, and `store.rs` exposes `criteria_of`/`tests_of` as readers. **The first three instances are FIELD completeness; this is ENTITY CREATION, and a surface can be field-complete while offering no way to bring the entity into existence** -- recorded here rather than as a new row for exactly the reason the second instance was, that a second home for one requirement is the failure this criterion exists to name. **AND `acceptance.md` IS A GENERATED VIEW** (`views.rs:385` renders it, `:896` adds it to the write set), **so a criterion authored in the markdown is silently discarded at the next `sync --to-disk`** -- the verb that emptied two views tonight -- and its author will believe they have added it. **The only working path is a hand-edit of `thread.json` followed by `sync --to-store`, which is racy against any peer running the opposite direction.** **Demonstrated by vc the same evening while minting ST0056 AC-03.14: the hand-edit was caught only by a SCHEMA REFUSAL naming `/criteria/118/state/is`, because a `kind: test` criterion's state is computed and not authorable. No verb would have needed telling, and nothing but the schema stood between a hand-written row and a malformed contract.** **SECOND BURNING CASE REFUTED BY MEASUREMENT 2026-08-19 (ic measured it and declined to touch the row; vc reproduced independently before ruling).** This row records _the only verbs that touch the row DESTROY it_ and _`intent at green` would have destroyed the note_. **NEITHER REPRODUCES.** `at_set` clones canon, sets `.status`, applies. Driven on AT-04.1 at 1705 note-bytes: `intent at green ST0057 AT-04.1` returned rc=0 and the WHOLE-ROW diff in both directions is ONE LINE -- `\"status\": \"red\"` becoming `\"status\": \"green\"`. Every other field byte-identical; restored with `at red` and re-diffed to identical. Reach traced rather than inferred from the facade: `render.rs:1033` is the ONLY caller with no second implementation in the CLI, `views.rs:538` renders the note INTO the acceptance view, and `ingest.rs` never reads `acceptance.md`, so the view is write-only and no round trip can lose it. **THE DIAGNOSIS IS WORTH MORE THAN THE CORRECTION (ic): _would have destroyed_ IS A PREDICTION RECORDED IN THE SAME VOICE AS A MEASUREMENT**, and nothing in the row's grammar distinguishes the two, so nothing downstream asked. **AND IT IS THE FIRST OVERSTATEMENT FOUND IN THIS ESTATE ALL DAY.** Every other stale record found on 2026-08-19 UNDERSTATED the work -- four AT notes, three WP statuses, two built-but-`to-write` rows, a whole briefing -- and understatement reads as diligence, so nothing challenges it. This one pointed the other way and survived exactly as long, which retires the comfortable half of that observation. **THE COST WAS REAL AND IT RAN THE WRONG WAY: vc hand-edited canon roughly a dozen times on 2026-08-19 to avoid a destruction that does not happen, and THE HAND-EDIT IS THE MORE DANGEROUS ROUTE** -- this row's own closing sentence records a hand-edit caught only by a SCHEMA REFUSAL naming `/criteria/118/state/is`, where the verb would have needed no telling. **A workaround for a defect that does not exist is not merely wasted effort; it substitutes an unchecked path for a checked one.** **WHAT SURVIVES IS MOST OF THE ROW: the two-sided property stands exactly as stated** -- every writable field settable INTENTIONALLY, and no verb silently clearing a field it was not asked to change -- **because a property is not refuted by one instance failing to reproduce.** The first, third and fourth burning cases are untouched: ST0011's `completed` has no setter, an attachment's canon record has no narrow setter, and no verb creates an AC or an AT at all. **THAT REFUTATION WAS MEASURED ON THE WRONG TOOL, AND vc AND ic MADE THE SAME ERROR INDEPENDENTLY (dc 2026-08-19, who was FIXING the real defect while both of them were refuting it).** **ISSUE 0033 IS REAL, IT WAS IN v2's `bin/intent_acceptance`, AND IT IS NOW FIXED.** The expression was `s|^(- ${esc} .*) -- status:.*|\\1 -- status: ${target}|`, whose `.*` after `status:` consumes the note; `:1364` now matches the status as a TOKEN -- `[a-z/-]+`, admitting exactly to-write/red/green/n-a and unable to cross a space -- and CAPTURES AND RE-EMITS the note. A character class rather than an alternation because `|` is that expression's own delimiter. **ic TRACED `facade.rs:2283 at_set`; vc DROVE `native/rust/target/debug/intent at green`. BOTH ARE v3. dc's 2026-08-18 REPORT WAS ABOUT v2.** In an estate where two implementations coexist and a v2 binary refuses a v3 tree at exit 2, **_the verb_ is AMBIGUOUS BY CONSTRUCTION and neither node named which one.** **THIS IS ic's OWN DISCRIMINATOR LANDING ON BOTH OF THEM: could each instrument have produced the other's finding? No -- they ran ONE METHOD TWICE, agreed, and concluded a TRUE record was false.** Agreement between two readings of one subsystem is evidence of nothing; ic wrote that down this morning and both nodes then walked into it. **AND THE SHARPEST FACT IS THE SUITE'S (dc): TWO BATS TESTS ASSERTED THE DESTRUCTION.** `tests/unit/intent_acceptance_cli.bats:99` runs `grep -E '^- AT-01\\.1 .*status: green$'` and asserts success, while the fixture row at `:40` is `- AT-01.1 ... -- status: to-write -- red-first`, which HAS a note -- so **the `$` anchor after `green` passes ONLY WHEN THE NOTE HAS BEEN DESTROYED.** With the fix in and the assertions untouched, exactly those two fail. **THE SUITE DID NOT FAIL TO OBSERVE THE DATA LOSS; IT REQUIRED IT. Twelve green tests over a live data-loss bug were not a contradiction, they were a consequence.** **AND THERE WAS NO READER FOR THE FIELD AT ALL:** the grammar DECLARES `note`, `at_normalise_row` REPAIRS a malformed one, the tool COACHES the author on its delimiter -- three writers, three curators, zero readers -- and `at_note()` reading capture group 6 is new. **A FIELD NOTHING READS CANNOT BE CHECKED BY ANYTHING, which is the whole reason the loss was silent.** dc's own unprompted caveat: removing the new verifier while keeping the fixed substitution fails nothing, so it is defence-in-depth rather than the thing doing the work today. **WHAT NOW STANDS: the defect was REAL in v2 and is fixed; it never existed in v3's `at_set`; so this row's second burning case is a correct account of THE TOOL dc WAS RUNNING and not an argument about the v3 mutation surface this criterion is about.** _Would have destroyed_ was a CORRECT PREDICTION, and vc's charge that it was a prediction wearing a measurement's voice is WITHDRAWN. **ESTATE DAMAGE, MEASURED WITH ITS LIMIT STATED: 66 of 132 ST0056 AT rows carry no note, of which 24 are GREEN and therefore certainly transitioned; ST0057 carries 2, both `n-a`. `acceptance.md` has 130 revisions in git, so recovery is possible in principle rather than impossible as first reported -- but three probed green rows show no note in the last 40 revisions, and NOTHING DISTINGUISHES _the note was destroyed_ FROM _the row never carried one_. That is the POPULATION AT RISK, not the loss.** **FOURTH INSTANCE SPLIT 2026-08-19 (ic, correcting their OWN commit message against their own interest; vc verified the split).** `53cb3f34` claims it _closes the gap AC-08.5 names_. It closes ONE HALF. **SUPERSEDED: _the only working path is a hand-edit of `thread.json` followed by `sync --to-store`, racy against any peer syncing the other way._ `Facade::put(&Address, json)` now creates BOTH an AC and an AT, insert-if-absent, two arms at `facade.rs:2469` and `:2511`, verified by running it. That is a checked, typed, schema-validated route.** **STILL TRUE: _no VERB creates an AC or an AT._ At the CLI nothing does -- `intent ac` and `intent at` still only transition.** So the instance narrows from _no route at all_ to _a service-layer route with no CLI surface_. **AND ic SHIPPED A PIN ASSERTING THE SUPERSEDED HALF, WHICH PASSED WHILE BEING FALSE:** `no_verb_creates_an_acceptance_test_or_criterion` grepped `facade.rs` for `fn at_new`, `fn at_add`, `fn at_create`, `fn ac_new`, `fn ac_add` and asserted none existed -- **and passed, while `put` created both THIRTY LINES AWAY IN THE SAME FILE.** **THE PIN MEASURED A NAME; THE CRITERION IS ABOUT A CAPABILITY, and a capability arriving under an unlisted name is invisible to it.** Identical in shape to the `organize` collision, where a ratification expressed as a string literal cannot distinguish a name being reclaimed from a capability arriving under another one. Replaced with `an_ac_and_an_at_can_be_created_through_the_surface`, which creates rows that do not exist, asserts both kinds land carrying a `note` no transitioning verb could set, and goes RED when the AT insert is removed -- **a creator under ANY name satisfies it and no rename can make it lie.**",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.5",
      "text": "**`.intentfiles` names ARTEFACTS and never enumerates FILES; `Project::classify` remains the single answer to what a file IS.** (vc 2026-08-18, on cc's finding while building the collector.) The two answer different questions -- the manifest answers _which artefacts are realised_, classify answers _what is this file_ -- and they COMPOSE: `STEELTHREAD:ST0056` realises the thread and, through classify, whatever files that thread produces. **Neither may acquire a second, independent enumeration of files**, because two declarations of which-files-matter agree for months and then quietly do not. Held mechanically: the manifest grammar admits no file-valued line, so a second enumeration is unrepresentable rather than merely discouraged.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.5",
      "text": "**`sync --to-store` REPORTS an attachment whose on-disk bytes differ from what the INDEX holds for that path** (dc 2026-08-18, from a live instance). The ingest path reads the WORKTREE, so an uncommitted edit sitting in the tree is carried into canon by whoever syncs next -- and canon then records an artefact whose bytes exist in NO COMMIT, indistinguishable on inspection from a correct one. **That is AC-11.5(c) of ST0056 arriving in canon rather than in vendored files, and the ratified remedy is NOT applied on this path**: arm 1 of `self_provenance_check.sh` reads `git cat-file blob :$rel` precisely to avoid it, but it validates the manifest and never an attachment, so it passes. **The rule currently exists only as an instruction between nodes -- commit yours first -- and a rule that lives in a peer message is followed until someone is mid-task**, which happened twice in one afternoon, once to the node who wrote the rule.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.6",
      "text": "**A COMMIT MUST NOT CONTAIN CANON THAT NAMES BYTES NOT IN THAT SAME COMMIT.** (dc 2026-08-18, answering vc's refuse-or-report question with neither option as framed.) **AC-03.5's report is correct and insufficient, and the reason is WHO it reaches**: when this fired, the uncommitted file was dc's and the syncer was vc, so a report names a path to a node that CANNOT act on it -- staging a peer's file sweeps their work into your commit, which is the hazard being avoided. **And the harm is not at sync.** Canon holding uncommitted bytes in a working tree is a dirty tree: normal, reversible, nobody's problem. **It becomes permanent and inspectable-but-wrong at the COMMIT** -- the moment a reader can later check out that revision, read an attachment, and be unable to obtain the bytes it names. **Both halves of the justification are already ratified**: ST0056 AC-11.1 puts the control where the HARM is, and the whiteboard clock guard blocks only on what the commit ADDS and never on inherited breakage. The check is cheap because both sides are already to hand for every attachment -- the `text` in canon, and `git cat-file blob :$path` for the file it came from -- **which is arm 1's comparison one population over.** **THE INHERITED-BREAKAGE CLAUSE IS LOAD-BEARING AND MUST BE BUILT, NOT ASSUMED (dc 2026-08-18, measured over the FULL population after retracting a 14-commit window that flattered this argument): of 46 commits carrying canon with attachments, 23 DISAGREE AT ALL -- 50%, not the 14% the window suggested -- and only 5 ADD a new divergence.** **A naive refuse-at-commit would block half of all commits, which is precisely the guard nobody keeps, so vc's original objection applies at the commit boundary too.** What rescues it is the clock guard's ratified rule: **block only on what the commit ADDS, never on inherited breakage** -- and that turns 23 into 5. **Divergences are EPISODES, not events: a file diverges and stays divergent for every subsequent commit until someone syncs** (`estate_corpus.sh` ran divergent across NINE consecutive commits). **If this criterion is implemented without the inherited-breakage clause it will be built as the 23 version and abandoned within a week.** The five episodes, each named by the commit introducing it: `d980e5b5`, `aa220755`, `7ee3019b`, `31f2dce4`, `c4f9bcbe`. **THE PROPERTY IS SYMMETRIC AND THE REASONING MUST NOT NAME ONLY ONE DIRECTION (dc 2026-08-18, from a live catch at HEAD by the checker's first whole-tree run).** At `c4f9bcbe` CANON RAN AHEAD of the commit -- a sync ingested worktree bytes nobody had committed. At `3f10b1ee` THE FILE RAN AHEAD AND CANON STAYED BEHIND -- an attachment was edited and committed with no re-sync, so canon named bytes that existed neither in the commit nor on disk, with a clean worktree. **Both satisfy _canon names bytes this commit does not contain_ and both are caught by the same comparison, but a criterion worded as _a sync must not ingest uncommitted bytes_ describes only the first and misses the second entirely.** The second half is the one that was live. **Stated as the invariant rather than as a prohibition on one operation: at every commit, every attachment's recorded bytes are obtainable from that commit.** **AND IT IS A PROPERTY OF EVERY COMMIT, NOT OF HEAD (dc 2026-08-18, giving the reason the remedy text needed).** Committing the file first and syncing afterwards leaves THAT COMMIT DIVERGENT IN HISTORY PERMANENTLY: a later sync repairs the NEXT commit and can never reach the one already made. **So the compliant order is sync canon FIRST -- because sync reads the worktree -- then commit the file and canon together**, and the obvious order is wrong independently of any gate. **WHAT A CHECKER CAN ESTABLISH SPLITS THREE WAYS, NOT TWO (dc 2026-08-18, correcting vc's too-strong form): NARROWED says this revision ADDS nothing; EXHAUSTIVE at a revision says THE PROPERTY HOLDS THERE, since every attachment matching the bytes that commit holds IS the property evaluated at that point; and NEITHER establishes the property ITSELF, because it is a claim about EVERY commit.** Only a history mode speaks to that, and only over the range it was handed. **A green at HEAD is compatible with a history full of divergences, and a gate cannot repair the past and must not claim to** -- which is why the inherited-versus-adds split is structural rather than a convenience.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.5",
      "text": "**NO COMMIT MAY ADD AN IGNORE RULE MATCHING ANYTHING UNDER `intent/.canon/`.** (cc 2026-08-18, raised while building WP-01 and declining to mint it themselves.) AC-01.2 checks that canon is committed AT CHECK TIME, by cloning. **This one refuses the EDIT that would break it**, and the gap between those two moments is where the class lives. **The hazard is structural rather than hypothetical: every other `intent/.<x>/` in this tree is gitignored** -- `.treeindex/`, `.cache/`, `.backup/` -- **so the convention here reads _a dot directory under `intent/` is local and never travels_, and `.canon/` is the single deliberate exception.** A future tidy-up adding `intent/.*/` to `.gitignore` is a natural, tidy-looking, correct-SEEMING edit that **would silently un-commit the entire estate.** That is D29's failure with the whole model behind it, and it is the same shape as the 434KB of issue bodies that lived only inside a gitignored store. **A convention that every sibling follows and one exception breaks is not a convention anyone will remember to check.**",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.6",
      "text": "**THE RELOCATION IS EFFECTED AT THE RESOLVER; NO TEXT SWEEP DECIDES WHAT MOVES.** After WP-01 exactly one function resolves a canon path and every reader AND writer goes through it -- checked by pointing that one function at a different directory and requiring every canon read and write to follow with no other edit. **`thread_dir()` MUST STILL RESOLVE `intent/st/<ID>/` AFTERWARDS**, because `info.md` (:577) and `acceptance.md` (:591) hang off it and the views do not move; moving `thread_dir()` wholesale would satisfy AC-01.1's letter -- `intent/st/` holds no `thread.json` -- while emptying `intent/st/` entirely. **THE CRITERION IS THE DISCRIMINATION AND NOT A COUNT.** ic re-measured their own 3-of-17 at `c758af96` and WITHDREW it: the probe's reach was one directory of 41 files against a repo-wide concern, and there is no honest single denominator. **A MECHANICAL SWEEP IS REFUTED BY CONSTRUCTION, AND TWO SINGLE-LINE FIXTURES PROVE IT IN OPPOSITE DIRECTIONS.** **(a) The matched text must not change and the breaking text does not match** -- `gen_register.sh:256` is one table row whose matching half describes what **v2** did and is TRUE, while the half asserting where **v3** canon lives is the half that breaks and **contains no `intent/st/` at all.** A sweep rewrites a true statement about v2 into a falsehood and leaves the real breakage untouched (ic). **(b) The file is in the hit list for the wrong lines** -- `intentsvcs/src/project.rs` carries the literal at **:485 (a doc comment)** and **:854 (a test)** and at NEITHER line that resolves it: `st_dir()` :482 is `self.intent_dir().join(\"st\")` and `thread_json` :492 is `thread_dir(id).join(\"thread.json\")`. A literal-driven sweep edits the comment, **REPORTS THE FILE HANDLED**, and leaves the resolver at v2 (vc). **AND THE CAREFUL PROBE UNDER-REACHES TOO.** vc's narrowed probe for `intent/st/...thread.json` returned 23 files and **did not contain `intentsvcs/src/export.rs:386`, `format!(\"st/{}/thread.json\", thread.id)`** -- the line writing the canon path INTO THE COMMITTED EXTRACT, which under D34 is the artefact that travels. **cc found that line by BUILDING the relocation, not by grepping for it**, and the Highlander fix fell out: the exporter spelled the path independently while every reader resolved `Project::thread_json`, and its neighbouring comment records the issue arm having ALREADY shipped that bug (`issues/46.json` written where readers opened `issues/0046.json`). **The resolver-driven change reached what no text probe did.** Canon's shape is also FLAT -- `.canon/st/<ID>.json`, not `<ID>/thread.json` -- so a prefix sweep yields **the right prefix with the wrong structure, which reads as a correct migration.** Pinned at `6ee99149`. **AND THE HALF-MIGRATED SITES SPLIT INTO TWO KINDS, WHICH IS THE CLASSIFIER THE LIVE MOVE NEEDS BEFORE IT RUNS, NOT AFTER** (cc 2026-08-19, from a case that went their way): `literal_stdout_parity.rs` had its WRITE repointed to `.canon/st/ST0001.json` while its `create_dir_all` still made `intent/st/ST0001` -- **it failed LOUDLY with `NotFound`.** ic's `${var#pat}` case is the same shape and fails SILENTLY: the half-migrated extraction still returns `ST0056.json` and nothing complains. **THE DISCRIMINATOR IS WHETHER THE UNMIGRATED END CAN STILL PRODUCE A VALUE.** An end that yields a plausible value hides the break; an end that yields nothing announces it. **A missing directory is not a plausible wrong answer -- a wrongly-derived filename is.** **SO A TWO-ENDED SITE IS NOT UNIFORMLY RISKY AND MUST NOT BE TREATED AS SUCH: the sites whose unmigrated end still produces a value are the ones that need a DRIVEN test, and the ones that cannot may rely on their own failure.** Classifying them costs less than driving all of them and strictly less than missing one. **THIS REFINES _a migrator must not do half of a two-ended migration_ from a maxim into a testable property**, and it matters here because **the live move of 57 + 40 files happens ONCE** -- a silent half-migration inside it is discovered by a reader months later, not by the run.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.7",
      "text": "**AN OPENNESS DECLARATION MUST RESOLVE, NOT MERELY PARSE.** (cc 2026-08-18, found while applying the WP-01 patch, and declining to mint it themselves.) Every `-- openness: carried by <path>` in `schema/ddl.sql` names a path that EXISTS in the tree the face ships with -- checked by resolving each declared path, with the declaration count and the resolved count both printed. **`tests/openness.rs` DOES NOT CHECK THIS AND CANNOT.** Its `declaration_gaps` verifies that a table declares SOMETHING and that the string starts with `carried by ` or is `DERIVED -- <why>`; it never looks at the referent. **So after the relocation, 7 of the 8 declarations name a path that does not exist and the test stays GREEN** -- six `intent/st/<ID>/thread.json`, one `intent/issues/<NNNN>.json`; only `intent/events.jsonl` survives. **A check that validates the FORM of a claim and never its REFERENT is the same shape as every other instrument that could not go red tonight.** **AND THIS IS NOT A STALE COMMENT, IT IS A BROKEN CONTRACT ON A TRAVELLING ARTEFACT.** `ddl.sql` is a committed, versioned, GENERATED face (`SCHEMA_DDL_VER: 8`, master `store.rs`, regenerated via `INTENT_BLESS`), served by `intent schema ddl.sql`, and its own header states **_EVERY TABLE DECLARES HOW ITS DATA LEAVES_** and **_Absence of a declaration is never the answer_**. Under D34 the face travels while the DB never does, so **a consumer following the declaration to get their data out would follow it to nothing** -- and AC-02.6, the openness row, is the estate's DURABILITY mechanism. **A false statement about how to recover the data is strictly worse than no statement**, which is precisely what the openness block was written to prevent. The fix is mechanical because `store.rs` is the master; the criterion exists because **nothing today requires a declared path to still resolve after canon moves.** Pinned at `11fdc168`.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-09.1",
      "text": "**EVERY ACT THAT CHANGES THE DISK IS RECORDED, and today none is.** `Facade::apply` is the single door for MODEL mutation and everything reaching it is logged; `Facade::organize`, `sync_to_disk`, `sync_from_disk` and `hydrate` each write the filesystem without passing it. **Measured 2026-08-19: `organize --apply` removed 423 files from this estate and the log recorded nothing** -- the only act all evening that destroyed anything is the only class of act absent from the one table that cannot be re-derived from anything else on disk. The realisation ops emit against the same `Store::append_event`, **with the subject being the PATH SET rather than an artefact id**. Denominator printed: realisation ops that emit, against realisation ops that write. **NOT BY ROUTING `organize` THROUGH `apply`** -- `apply` diffs canon and `organize` changes no canon, so the event would be a lie about its own mechanism.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-09.2",
      "text": "**THE CONCURRENT-APPEND DECISION IS MADE AND RECORDED WHILE THERE ARE 55 ROWS TO MIGRATE, NOT 17,000.** `intent/events.jsonl` is a TRACKED, APPEND-ONLY file written by four sessions committing minutes apart, so every concurrent write is a same-line git conflict waiting to happen. Seven commits have touched it without one, **which is luck at 55 rows rather than a property**. The candidates are per-node event files that merge, or a store-side sequence the file is projected from. **AND THE ALARM IS DISCONNECTED, WHICH IS WHY THIS IS NOT MERELY TIDY-UP (vc + cc, 2026-08-20):** `append-only-guard.sh` is the guard whose declared subject is *a write where an append was meant*, it is rostered in `pre-commit.sh`, and it has **ZERO call sites on the path git actually takes** -- so a truncating write over the event log is currently caught by nothing. **NOT A PRUNER**: growth is not the problem. Measured at ~229 bytes over 55 events in the first 35 hours, a year is ~17k events and under 4MB, and a retention policy on the one unrecoverable table trades a real loss for an imaginary one.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-10.1",
      "text": "**A VIEW ABSENT BECAUSE ITS ARTEFACT IS NOT REALISED IS NOT SKEW, AND A VIEW ABSENT FOR A DECLARED ARTEFACT STILL IS.** Both arms, or neither counts. `doctor` exited 1 with **234 findings on a healthy estate** after the dehydration at `e7f00e65`, every one of them a view whose thread `.intentfiles` does not declare, every one instructing the operator to regenerate a file the design says should not exist -- and the remedy each printed would have been answered by `organize` re-hydrating nothing. **The silence is BOUNDED rather than blanket**: a blanket `Err(_) => {}` fixes the count and trades 234 false findings for one SILENT REAL one, which is the trade this criterion exists to refuse. **The argument was already written down one file over** at `doctor::attachment_drift`, applied to attachments and not to views -- a prediction of this exact defect by the author of the sibling path, at 234 instances and a non-zero exit.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-10.2",
      "text": "**ABSENT IS NOT EMPTY, AND UNREADABLE IS NOT EMPTY EITHER.** A missing manifest and an unparseable one both realise EVERYTHING -- the fail-open direction, and the only one that cannot delete anybody's files. A project that has never run `organize` must be checked exactly as it was before WP-10 landed, and a project whose manifest is broken must not have its entire estate silently drop out of the check. **The three states are carried as three values and never collapsed**: `NothingSaid` / `Declared` / `Unreadable`, so a reporter can tell them apart without going back to the filesystem. **THE REPORTER IS STILL OWED**: nothing yet surfaces `Unreadable` to an operator, so a broken manifest means declared dehydration silently stops being honoured. That gap is named here rather than left -- it is a missing reader, not a missing value.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    }
  ],
  "tests": [
    {
      "id": "AT-00.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/dehydration_ship_gate.rs",
      "covers": [
        "AC-00.1"
      ],
      "status": "green",
      "note": "**RE-CITED FROM A SHELL PARITY TOOL ONTO THE RUST BATTERY (vc 2026-08-19), AND THE SHELL TOOL IS WITHDRAWN RATHER THAN DEFERRED.** The row cited `intent/st/ST0057/parity/tools/dehydration_gate_check.sh`, which never existed. dc built `dehydration_ship_gate.rs` and asked rather than moving the row. **THE SHELL FORM IS NOT MERELY HARDER, IT IS FORBIDDEN BY THE CRITERION IT WOULD TEST:** a shell driver either drives the CLI -- blocked, the `organize` handler is unwired -- or resolves satisfaction itself in shell, which is the SECOND ANSWER TO _is this criterion met_ that AC-00.1 exists to forbid. There is no third shell shape. **THIS DOES NOT GENERALISE TO THE OTHER FIVE `parity/tools` ROWS AND MUST NOT BE READ AS DOING SO:** AT-01.2, AT-01.4, AT-02.4, AT-04.6 and AT-07.5 each assert an EQUALITY OF TWO INDEPENDENTLY DERIVED SETS, or a property about the estate as a human meets it, and for those the shell form is the STRONGER instrument -- a Rust test deriving both sides through one code path loses exactly the independence that is the evidence. AC-00.1 has nothing to derive independently, because satisfaction must have ONE answer. **RE-DRIVEN AT `a3d74334` AFTER dc CHANGED THE MECHANISM UNDER THIS GREEN, AND THE ROW STANDS (vc, 2026-08-19, HEAD `ad9aa825`, clean tree, 25 tests pass).** dc flagged it rather than letting the green ride: **a green attesting to a version that no longer exists is the dirty-tree attribution defect in its other form -- correct when taken, silently about something gone.** **WHAT CHANGED: the ADDRESS IS DELETED.** `DECLARING_THREAD`/`DECLARING_CRITERION` were `pub const &str`, so `no_pm_state_in_output` read them as shipped literals -- red at HEAD from `edbd7640`, inherited by every node on checkout. **AND THE LINT WAS RIGHT FOR A DEEPER REASON THAN IT STATES: this gate ships inside a binary other projects run, and IN A CONSUMER'S ESTATE THERE IS NO ST0057** -- the refusal would have named a thread they cannot read, cannot satisfy, and cannot tell is not about them. A string-literal scan standing in for a semantic rule caught the semantic defect; the proxy earned its keep, which is the opposite of the strict-proxy failure ST0039 retired. **THE DECLARATION NOW SELF-IDENTIFIES BY ITS DELIMITER, AND THAT BOUGHT AN ARM THE ADDRESS COULD NOT HAVE HAD.** An address points at one carrier and says NOTHING about whether a second exists elsewhere; `<<PRECONDITIONS` is a machine-shaped token, the same in every estate that has one and absent from every estate that does not, **so the declaration is single by MEASUREMENT rather than by assumption and TWO CARRIERS ANYWHERE IN THE ESTATE REFUSE.** dc's old defence -- a wrong address finds nothing and finding nothing refuses -- was true and could never have detected the duplication this criterion is actually about. **FOUR LIMBS RE-CHECKED BY NAME AT THE NEW MECHANISM, NONE STANDING IN FOR ANOTHER:** no removal while unmet (`no_file_is_removed_while_a_precondition_is_unmet`); the refusal names EVERY unmet one (`every_unmet_precondition_is_named_not_just_the_first`); the denominator printed on BOTH answers; and the declaration single -- now `two_blocks_refuse_because_there_is_no_single_declaration` PLUS `two_carriers_anywhere_in_the_estate_refuse` and `the_declaration_is_found_by_its_delimiter_not_by_an_address`. **THE POSITIVE CONTROL SURVIVES THE REWRITE AND IS STILL WHAT SEPARATES THIS FROM AT-08.4** (`the_positive_control_removes_the_same_file_when_the_declaration_is_met`). `check(canon: &Canon)` unchanged, `Verdict`'s four fields still private, `permits()` still requires readable AND non-empty AND no-unmet. **AND THE PROPERTY IS NOW GUARDED INDEPENDENTLY OF ITS PROXY:** `no_refusal_this_gate_can_produce_names_this_project_s_own_paperwork` renders every `Unreadable` variant and refuses any naming `ST00` or `AC-0`, so the literal scan and the rendering arm fail separately and one going stale does not take the other with it."
    },
    {
      "id": "AT-00.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/thread_prose_carried.rs",
      "covers": [
        "AC-00.2"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-19 (cc built it; vc ran both arms and checked the denominator against the criterion's demand).** `thread_prose_carried.rs`, 2 tests, rc=0. **166 of 166 `design.md` / `impl.md` / `tasks.md` carried and BYTE-COMPARED across 57 threads -- the enumerated total printed as the denominator, so a carry covering SOME is distinguishable from one covering ALL, which is exactly what the criterion asks and what an artefact count cannot give.** cc's own note: the property already held; it had no instrument.\n\n**THE SECOND HALF IS THE ONE WORTH READING, AND IT IS TESTED BEHAVIOURALLY RATHER THAN BY GREPPING THE SOURCE.** The row requires the classifier to reach these files WITHOUT knowing their names -- a name list is a roster somebody must extend the day they add a fourth prose file, which is `THREAD_PROSE` wearing a different constant. **Asserting that the string `design.md` does not appear in `project.rs` answers a question about the FILE: it passes the moment someone spells the list differently and fails the moment someone writes the name in a comment.** So the discriminator is **AN INVENTED FILENAME NOBODY HAS EVER USED, which must classify IDENTICALLY to the three real ones.** A classifier keyed on names cannot do that; one keyed on shape cannot fail to. **WITH A CONTROL, because _everything is an Attachment_ satisfies that completely: `info.md` at a thread root must come back DIFFERENT, which is what proves the classifier discriminates at all.** Same equivalence-over-corpus discipline ic applied to AC-02.5 hours earlier, reached independently in a different subsystem."
    },
    {
      "id": "AT-00.3",
      "kind": "non-test",
      "prose": "`conservation_check.sh` run at full scope over a named revision; vc reads the verdict and records the revision in the finding",
      "covers": [
        "AC-00.3"
      ],
      "status": "n-a"
    },
    {
      "id": "AT-00.4",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/root_files_generated.rs",
      "covers": [
        "AC-00.4"
      ],
      "status": "green",
      "note": "**GREEN (cc, `b277013a`). vc RAN IT: 7 passed, 0 failed, 0 build errors.** `intent agents generate` produces the three root files from templates; 5 of the 7 arms are mutation-proven. **THE NOTE SAID THE TEST DID NOT EXIST WHILE THE ROW SAID GREEN, FOR A STRUCTURAL REASON: `at green` MOVES A STATUS AND NO VERB MOVES A NOTE.** cc committed the contradiction and named it rather than hand-editing canon, which is the right way round -- and it is the same shape as the view drift found this evening, one field further in. **IT IS CAPABILITY, NOT A REVERSIBILITY PROOF, SO hv's git ruling does NOT retire it:** git holding an old `AGENTS.md` is not the tool being able to make one, and the incident behind the row was a root file emptied on 2026-08-18 -- a file git could restore and nothing could regenerate."
    },
    {
      "id": "AT-01.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/canon_relocation.rs",
      "covers": [
        "AC-01.1"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-19 (cc drove it; vc verified the estate independently of the test).** 4 tests, pass. Estate facts confirmed separately: `intent/.canon/st/` holds 57, `intent/.canon/issues/` holds 40, `git ls-files intent/.canon` returns 97 tracked, and BOTH old paths are empty -- `intent/st/*/thread.json` and `intent/issues/*.json` match nothing. **The criterion is the EQUALITY of the two printed populations rather than either figure, and it holds.**"
    },
    {
      "id": "AT-01.2",
      "kind": "test",
      "file": "intent/st/ST0057/parity/tools/canon_clone_completeness.sh",
      "covers": [
        "AC-01.2"
      ],
      "status": "green",
      "note": "unwritten -- ST0057 is 0 of 8 WPs. `to-write` is the honest state: the test does not exist, which is not the same as existing and failing."
    },
    {
      "id": "AT-01.3",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/canon_relocation_roundtrip.rs",
      "covers": [
        "AC-01.3"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-19 (cc built it; vc ran both arms and checked the denominator against the criterion's own demand).** `canon_relocation_roundtrip.rs`, 2 tests, rc=0 -- `every_field_of_every_artefact_survives_the_relocation` and `a_field_dropped_in_transit_is_reported_by_name`. **6,268 FIELDS compared across 57 threads and 40 issues, 98 files written through the resolver, ZERO differences.** **THE DENOMINATOR IS FIELDS RATHER THAN ARTEFACTS AND THAT IS THE ROW, NOT A DETAIL: 97 of 97 files present with ONE FIELD SILENTLY ABSENT FROM EACH is a green under any artefact count, and it is data loss at the clone boundary.** The criterion asks for the denominator printed over the FULL FIELD SET and it is. **AND THE LOST-FIELD ARM DROPS AN OPTIONAL FIELD ON PURPOSE (cc): a REQUIRED field is caught by schema validation and refuses loudly, so the lost arm is only ever exercised by optional fields -- which is exactly where silent loss lives.** An arm that dropped a required field would have proven the schema works and said nothing about this row."
    },
    {
      "id": "AT-01.4",
      "kind": "test",
      "file": "intent/st/ST0057/parity/tools/canon_concurrent_diff.sh",
      "covers": [
        "AC-01.4"
      ],
      "status": "green",
      "note": "unwritten -- ST0057 is 0 of 8 WPs. `to-write` is the honest state: the test does not exist, which is not the same as existing and failing."
    },
    {
      "id": "AT-02.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/intentfiles_grammar.rs",
      "covers": [
        "AC-02.1"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-19 (ic).** 6 tests, and SEVEN mutation arms driven red-on-demand and restored BEFORE the green was called: silent-skip instead of refuse; off-by-one line number; unclosed region reported at EOF rather than at its open point; grammar relaxed inside the generated region; any-non-empty-id accepted so a path leaks in; trailing comment discarded; a bad line in the shipped manifest. **All six tests passed on first run, so the ARMS are the result and the green is not.** **AC-02.5 is held MECHANICALLY rather than by a check** -- an id must satisfy `is_thread_id` or `is_issue_id`, so `STEELTHREAD:intent/st/ST0056/info.md` is a MalformedId and not a special-cased path refusal; arm 5 proves the mechanism by loosening the id test and watching a path walk in."
    },
    {
      "id": "AT-02.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/intentfiles_is_the_list.rs",
      "covers": [
        "AC-02.2"
      ],
      "status": "green",
      "note": "**RE-POINTED TO `intentfiles_is_the_list.rs` (ic, `0543f64b`). THE OLD FINDING IS SETTLED AND WITHDRAWN** -- `intentfiles::render` had no caller because the thing it does is not needed, not because anyone failed to wire it. **vc RAN THE NEW FILE AT `a34f4cda`: 4 passed, 0 failed, 0 build errors**, and they drive the VERB rather than the function -- both directions in one run, the list beating status both ways, the file byte-identical across a run that moved files, and a second run that moves nothing. **THE ONE THING THE CRITERION ASKS FOR THAT IS NOT ASSERTED:** it says the reconciliation is _asserted as an equality of two independently derived sets with both counts printed_, and the tests assert per-file existence on two named threads instead. **A sample of two cannot be told from a rule that handles exactly those two ids, and the live estate is 545 removals.** **IT GOES GREEN ON ONE ASSERTION: derive the realised set from disk, derive the listed set from `.intentfiles`, assert equality, print both counts.**"
    },
    {
      "id": "AT-02.3",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/intentfiles_is_the_list.rs",
      "covers": [
        "AC-02.3"
      ],
      "status": "green",
      "note": "**RE-POINTED OFF `intentfiles_pin_survives_close.rs`, WHICH PROVED A PIN SURVIVING A REGENERATION THAT NO LONGER HAPPENS.** The criterion now reads: a `completed` or `cancelled` artefact that IS listed stays on disk through `organize`, and a `wip` artefact that is NOT listed is removed by it -- **status has no vote at `organize` time at all.** ic's `the_list_wins_over_status_in_both_directions` asserts exactly that pair and passes (vc ran the file: 5 passed, 0 failed, 0 build errors). **AND THE LIVE ESTATE PROVED IT ACROSS A WIDER STATUS RANGE THAN THE TEST DOES, 2026-08-19 at `e7f00e65`:** `organize --apply` removed 52 `completed` and 2 `cancelled` threads that were unlisted, while 1 `not-started` and 2 `wip` threads that WERE listed stayed -- four distinct statuses, both directions, on the real tree rather than a fixture. **THE ONE CASE NEITHER OF THOSE COVERED WAS `cancelled` AND LISTED, SO vc DROVE IT RATHER THAN HOLDING THE ROW FOR IT:** ST0010 (`cancelled`) added to the list, `organize --apply` -> **5 hydrated**, and a second run -> **0 removed, still 5 files.** A cancelled artefact that is listed is hydrated and then kept. Removed from the list again -> **5 removed, 1 pruned**. **SO ALL FOUR QUADRANTS ARE PROVEN AND NONE BY INFERENCE:** completed+listed stays (fixture), completed+unlisted goes (52 threads, live), cancelled+listed stays (ST0010, live), cancelled+unlisted goes (2 threads, live), plus wip and not-started both ways. **Status has no vote, measured across four statuses rather than argued from the absence of a branch.**"
    },
    {
      "id": "AT-02.4",
      "kind": "test",
      "file": "intent/st/ST0057/parity/tools/intentfiles_reviewable.sh",
      "covers": [
        "AC-02.4"
      ],
      "status": "green",
      "note": "unwritten -- ST0057 is 0 of 8 WPs. `to-write` is the honest state: the test does not exist, which is not the same as existing and failing. **`to-write` IS CORRECT HERE AND IS NOT A GAP IN WP-02 (ic 2026-08-19, who checked each of the other four rows against `organize` INDEPENDENTLY rather than assuming one cause for the directory's emptiness; recorded by vc so a reader does not count it as one).** This row observes the CHANGED-PATH SET after an `organize` that changes realisation, and `organize` is ST0057 WP-04, dc's. **It is a CROSS-WP DEPENDENCY, so WP-02 reads as COMPLETE-BUT-FOR-THIS rather than as four-of-five with an unexplained hole** -- and the distinction matters because an unexplained hole invites someone to go looking for a defect that is not there."
    },
    {
      "id": "AT-03.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/opaque_attachment_canon.rs",
      "covers": [
        "AC-03.1"
      ],
      "status": "green",
      "note": "**GREEN, AND THE ROW WAS STALE FOR HOURS BECAUSE THE INSTRUMENT THAT WATCHES FOR THAT HAS NEVER EXAMINED THIS THREAD (cc found the row; vc found why, 2026-08-19).** 5 arms pass at `18a9ab6b`, file TRACKED and byte-identical to HEAD. Mutation-proven three ways -- emit no sidecars, read a missing sidecar as empty, refuse non-UTF-8 as before -- each killing exactly one arm. **The estate arm prints `0 opaque attachment(s) of 280`, the zero named as a zero**, which is the whole reason this row is not a vacuous green: the estate CANNOT falsify the criterion, so a constructed fixture with real non-UTF-8 bytes carries it and the estate zero is stated rather than folded into a total. **IT LANDED INSIDE ic's THREE-NODE COMMIT `46ab2220`, which is very likely why the row-move was missed: the attention that day went to rows whose commits carried their own author's name.** cc declined to move it themselves -- _`at green` on someone else's judgement is how a green comes to mean somebody thought so_ -- and that is the correct refusal."
    },
    {
      "id": "AT-03.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/attachment_form_by_content.rs",
      "covers": [
        "AC-03.2"
      ],
      "status": "green",
      "note": "**GREEN. Both directions checked, and the second one CANNOT BE FALSIFIED BY THE ESTATE -- which the row says out loud rather than hiding inside a total (vc verified at `3c6d20f6`, 5 arms pass, workspace rc=0).** **cc's finding, and it is the reason this row is honest: AC-03.2 is ONE SENTENCE CARRYING TWO CLAIMS WHOSE DENOMINATORS ARE THREE ORDERS OF MAGNITUDE APART.** _No text stored as a sibling_ is exercised 280 times per run over 280 attachments; _no opaque stored inline_ has a denominator of ZERO, because the estate holds no opaque attachment at all. **A single count over _attachments checked_ would have reported that as coverage.** Both printed, the zero named as a zero. **AND THE ESTATE SWEEP'S FAILURE BRANCHES NEVER EXECUTE, because nothing in the corpus violates -- so the sweep is driven a SECOND way, by a planted-violation arm carrying BOTH drift directions at once. An arm whose failure path has never run is a green that says nothing about whether it can go red.**"
    },
    {
      "id": "AT-03.3",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/attachment_naming_gate.rs",
      "covers": [
        "AC-03.3"
      ],
      "status": "green",
      "note": "**GREEN. 7 arms pass at `61b93440`, file TRACKED.** The `.webloc` the estate already carried is a FIXTURE here rather than only a hazard."
    },
    {
      "id": "AT-03.4",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/attachment_drift_detected.rs",
      "covers": [
        "AC-03.4"
      ],
      "status": "green",
      "note": "**GREEN. Divergence by COMPARISON, and the arm that proves it is the one that detects NOTHING (vc verified at `3c6d20f6`, 6 arms pass).** **cc's sharpest arm: a file rewritten with IDENTICAL BYTES moves its mtime and keeps its size, and must produce NO finding. An implementation keyed on metadata passes every other test in the file and fails that one.** That is what _by comparison rather than inference_ means once it is a property instead of a phrase. **AND ABSENCE IS NOT DRIFT -- getting that wrong would have been worse than missing the drift: under `.intentfiles` a dehydrated thread's attachments are LEGITIMATELY GONE, so reporting absence would make `doctor` unhealthy for every dehydrated thread -- a rule flagging a population behaving exactly as designed.** **This row ran into vc's own blast-radius rule and cc did NOT edit the rule to make their remedy pass; see AC-03.3's neighbourhood and `finding_remedies.rs`.**"
    },
    {
      "id": "AT-04.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/organize_five_rows.rs",
      "covers": [
        "AC-04.1"
      ],
      "status": "green",
      "note": "**GREEN. `organize` is WIRED and the five rows of D57-3's table are exercised.** **VERIFIED BY vc INDEPENDENTLY OF dc, NOT ON dc's REPORT: driven from the SHIPPED BINARY in a disposable `git clone` at `fc7e25f2`, 2026-08-19 16:52Z.** `rc=1` READ DIRECTLY WITH NO PIPE (vc's own watch-out, violated once in the first attempt and the run redone); **544 file(s) would be removed, ZERO were; 744 files under `intent/st` before and 744 after, TWICE; the two runs are byte-identical.** The refusal printed `19 checked of 19 declared, 13 unmet` and NAMED ALL THIRTEEN. **It says _this estate_ and _this project_ and carries no ST id, which is dc's consumer-estate fix visible in production output rather than only in its test.** **The fifth row earned its keep at estate scale: 199 UNCLAIMED files reported BY NAME and never removed** -- tap baselines, probe TSVs, tooling output, all put under a thread by a human and unproducible by the renderer. **That population cannot appear in a fixture; it only exists on a real tree.** **THE WIRING IS PROVEN MANUALLY AND BY NOTHING AUTOMATED, AND THIS GREEN DOES NOT COVER IT (vc ruling, 2026-08-19; dc raised it rather than letting it be discovered).** The five AT files drive `organize::plan` and `Plan::apply` DIRECTLY, so they prove the MECHANISM; that `intent organize` the SHIPPED VERB reaches the handler is evidenced only by dc's clone run and vc's independent reproduction of it. **Checked before ruling, and no existing instrument guards it: `dispatch_ssot.rs:475` `continue`s past a command that says _is a known command that is not implemented yet_ -- it audits the REMEDY TEXT of unimplemented commands and never asserts that a shipped one is implemented.** So a regression unwiring the handler would leave all five of these rows green. **NOT MINTED AS A ROW, AND THE REASON IS CONSISTENCY RATHER THAN COST: none of AC-04.1 through AC-04.5 mentions dispatch, so the wiring is outside the criteria AS WRITTEN and the gap is in their SCOPE -- the identical disposition vc gave ic's finding that AC-03.15 cannot protect issues because issues have no face.** Queued with that one. **The estate has the flag-level and argument-level forms of this criterion already (ST0056 AC-06.8, a declared-and-inert flag is a lie told by `--help`; AC-05.7, a declared argument the binary accepts and discards). The COMMAND-level form is the missing member of a family, not a new idea.**"
    },
    {
      "id": "AT-04.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/organize_dehydration_gate.rs",
      "covers": [
        "AC-04.2"
      ],
      "status": "green",
      "note": "**GREEN. The dehydration gate refused the whole run and named every unmet precondition.** **VERIFIED BY vc INDEPENDENTLY OF dc, NOT ON dc's REPORT: driven from the SHIPPED BINARY in a disposable `git clone` at `fc7e25f2`, 2026-08-19 16:52Z.** `rc=1` READ DIRECTLY WITH NO PIPE (vc's own watch-out, violated once in the first attempt and the run redone); **544 file(s) would be removed, ZERO were; 744 files under `intent/st` before and 744 after, TWICE; the two runs are byte-identical.** The refusal printed `19 checked of 19 declared, 13 unmet` and NAMED ALL THIRTEEN. **It says _this estate_ and _this project_ and carries no ST id, which is dc's consumer-estate fix visible in production output rather than only in its test.** `organize_dehydration_gate` 6 arms pass. **The estate run is the positive evidence the fixture cannot give: 544 real files on the removal path, held.**"
    },
    {
      "id": "AT-04.3",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/organize_attachment_divergence.rs",
      "covers": [
        "AC-04.3"
      ],
      "status": "green",
      "note": "**GREEN. `organize` reported 0 diverged and resolved nothing.** **VERIFIED BY vc INDEPENDENTLY OF dc, NOT ON dc's REPORT: driven from the SHIPPED BINARY in a disposable `git clone` at `fc7e25f2`, 2026-08-19 16:52Z.** `rc=1` READ DIRECTLY WITH NO PIPE (vc's own watch-out, violated once in the first attempt and the run redone); **544 file(s) would be removed, ZERO were; 744 files under `intent/st` before and 744 after, TWICE; the two runs are byte-identical.** The refusal printed `19 checked of 19 declared, 13 unmet` and NAMED ALL THIRTEEN. **It says _this estate_ and _this project_ and carries no ST id, which is dc's consumer-estate fix visible in production output rather than only in its test.** `organize_attachment_divergence` 7 arms pass. **Authority follows AUTHORSHIP and the verb refuses to choose: a view diverging means the FILE is stale, an attachment diverging means the STORE is, and one policy for both discards somebody's work.**"
    },
    {
      "id": "AT-04.4",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/organize_idempotent_mtime.rs",
      "covers": [
        "AC-04.4"
      ],
      "status": "green",
      "note": "**GREEN. Idempotent INCLUDING mtimes.** **VERIFIED BY vc INDEPENDENTLY OF dc, NOT ON dc's REPORT: driven from the SHIPPED BINARY in a disposable `git clone` at `fc7e25f2`, 2026-08-19 16:52Z.** `rc=1` READ DIRECTLY WITH NO PIPE (vc's own watch-out, violated once in the first attempt and the run redone); **544 file(s) would be removed, ZERO were; 744 files under `intent/st` before and 744 after, TWICE; the two runs are byte-identical.** The refusal printed `19 checked of 19 declared, 13 unmet` and NAMED ALL THIRTEEN. **It says _this estate_ and _this project_ and carries no ST id, which is dc's consumer-estate fix visible in production output rather than only in its test.** `organize_idempotent_mtime` 2 arms pass, and the estate run is the stronger evidence: **two consecutive runs on a real 744-file tree produced BYTE-IDENTICAL output and moved no file.** Measured as mtimes moved, never as a content diff -- the defect being closed is a byte-identical re-emission that moves mtime and corrupts `file_index`'s clean/changed state."
    },
    {
      "id": "AT-04.5",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/organize_moment_of_act_digest.rs",
      "covers": [
        "AC-04.5"
      ],
      "status": "green",
      "note": "**GREEN. The moment-of-act digest holds.** **VERIFIED BY vc INDEPENDENTLY OF dc, NOT ON dc's REPORT: driven from the SHIPPED BINARY in a disposable `git clone` at `fc7e25f2`, 2026-08-19 16:52Z.** `rc=1` READ DIRECTLY WITH NO PIPE (vc's own watch-out, violated once in the first attempt and the run redone); **544 file(s) would be removed, ZERO were; 744 files under `intent/st` before and 744 after, TWICE; the two runs are byte-identical.** The refusal printed `19 checked of 19 declared, 13 unmet` and NAMED ALL THIRTEEN. **It says _this estate_ and _this project_ and carries no ST id, which is dc's consumer-estate fix visible in production output rather than only in its test.** `organize_moment_of_act_digest` 4 arms pass. **It locks against ANY process rather than other `organize` runs, because every read verb materialises the store on access -- so _the estate was quiet when I measured_ is not establishable, which is exactly the assumption `load_fresh` taught vc to distrust today.**"
    },
    {
      "id": "AT-04.6",
      "kind": "test",
      "file": "intent/st/ST0057/parity/tools/sparse_tree_equals_manifest.sh",
      "covers": [
        "AC-04.6"
      ],
      "status": "to-write",
      "note": "**A PARITY TOOL RATHER THAN A RUST TEST, DELIBERATELY: the subject is THIS estate's real tree, and the fixture that makes it meaningful is the 57-thread live one.** A tempdir fixture can be built sparse and prove nothing. **Red-first: run it against the tree AS IT STANDS TODAY -- 57 ST directories against a manifest that does not yet exist -- and it must go red on the largest possible margin.** That red is free, it is available now, and it is the only moment the full-realisation baseline can be captured honestly. **Derive the two sets with two different instruments** -- `find`/`ls` for disk, the `.intentfiles` parser for declared -- **and never compare a set to itself.** Print both counts and the symmetric difference in BOTH directions, named: present-but-undeclared and declared-but-absent are different defects with different fixes. **Denominator caution, learnt the hard way here:** a `find | wc -l` over a directory tree counts directory nodes as well as files unless filtered, and filtering with `-type f` BLINDS the count to a deletion -- the directory node is the only input whose mtime records one (ic, 2026-08-18). State which you are counting and why. **The exempt set is asserted, not skipped**: `steel_threads.md` must appear in the tool's output as an EXEMPTION WITH ITS REASON, so an exemption silently growing is visible -- the shrunken-roster failure that took ST0056 AT-03.15 five versions to close."
    },
    {
      "id": "AT-05.1",
      "kind": "test",
      "file": "native/rust/crates/intent-cli/tests/edit_hydrates.rs",
      "covers": [
        "AC-05.1"
      ],
      "status": "to-write",
      "note": "**`to-write` AND BLOCKED, NOT SKIPPED (ic 2026-08-19, who checked all three blockers rather than reporting the first; recorded by vc so nobody reads it as a hole and hunts a defect that is not there).** THREE BLOCKERS AND NONE OF THEM IS WORK. **(1) NO `edit` VERB EXISTS AND NEITHER DOES `st edit`** -- `surface/dispatch-table.json:792` declares `st edit` a v2 command with a v3 target and nothing implements it, so AC-05.3's _ONE home_ names a home that has not been built, and the Highlander constraint is satisfiable only by building the shared thing first. **(2) NO HYDRATION PRIMITIVE EXISTS AND IT IS SHARED WITH `organize`** -- `views.rs` renders strings, `write_set.rs` writes bytes atomically, and NOTHING composes canon -> views -> disk for one artefact on demand. Both WP-04 and WP-05 need exactly that one thing. **RULED BY vc 2026-08-19: dc OWNS THE HYDRATION PRIMITIVE, it lives in `intentsvcs` under ONE name, and ic BUILDS AGAINST IT RATHER THAN BESIDE IT.** `organize`'s apply path IS the hydration path, so the primitive falls out of work dc is already doing, and WP-06's full text realisation is dc's as well. **Two nodes writing one primitive is the two-declarations defect this entire thread exists to prevent, arriving in the CODE instead of the manifest** -- and it would have been written twice this afternoon had ic not stopped to ask who owned it. **(3) A DESIGN QUESTION CANON EXPLICITLY DEFERRED TO THIS WP AND HAS NOT ANSWERED**, `surface/dispatch-table.json:834` verbatim: _Under authored-once, `edit` on a GENERATED view (info/acceptance) hands the user a path to a file their edits will lose at the next regeneration. v3 emitting the path unchanged is defensible only if the skew check (AC-03.4) catches the edit. Flagged for the WP-05 surface cut, not decided here._ **User-facing behaviour, undecided ON PURPOSE, in the surface SSOT, routed to the cut this row is in. It is hv's and it is open.**"
    },
    {
      "id": "AT-05.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/edit_writes_pinned_region.rs",
      "covers": [
        "AC-05.2"
      ],
      "status": "red",
      "note": "**RED. The old row proved a hand realisation written to the PINNED region survives; that region is gone.** The new criterion is that the lifecycle verbs edit the list, that `st new --dehydrate` and `st done --keep` exist, and that **the closing verbs WARN over unsynced attachment bytes**. **THE `REFUSE` WORDING WAS vc's AND IS WITHDRAWN (2026-08-19, on dc's trace + vc's own measurement of all 7 delete sites): the closing verbs do not delete files, so a refusal there would be a second authority over a destructive act it does not perform.** The warning is additive and earns its place on TIMING, not on safety -- the safety is the realiser's gate."
    },
    {
      "id": "AT-05.3",
      "kind": "non-test",
      "prose": "vc reads the dispatch and confirms a single path-printing implementation and no `wip` verb",
      "covers": [
        "AC-05.3"
      ],
      "status": "n-a"
    },
    {
      "id": "AT-06.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/text_realisation.rs",
      "covers": [
        "AC-06.1"
      ],
      "status": "green",
      "note": "**GREEN, AND RE-CITED: the row named `text_realisation_complete.rs`, which does not exist. dc built ONE file with seven arms covering both AC-06.1 and AC-06.2, and both are genuinely carried** (vc verified at `c840251a`, 7 pass, file TRACKED). Arms for this criterion: `every_artefact_canon_holds_reaches_the_realisation`, `the_denominator_follows_the_estate_rather_than_the_writing`, `counts_of_an_empty_estate_is_empty_rather_than_complete_by_accident`, `an_attachment_canon_names_but_carries_no_bytes_is_refused_rather_than_skipped`, `a_text_attachment_is_realised_with_its_bytes`. **THE DENOMINATOR IS ASKED OF CANON AND THE NUMERATOR ACCUMULATES WHILE WRITING, so the two numbers come from genuinely different places -- the mutation making `Counts::of` accumulate instead KILLS THREE TESTS. It is the criterion, not a field.** **`shortfall()` names WHICH population fell short, and that is the criterion's own point rather than a nicety:** _1841 of 1847_ sends a human to diff two trees; _attachments 4 of 10_ sends them to the four threads that have any. **AND CANON NAMING BYTES NOBODY CARRIES IS REFUSED RATHER THAN SKIPPED: skipping counts short with no reason given; an empty write counts COMPLETE over a file the human cannot use** -- which is the partial-export-reading-as-complete failure this row exists to forbid, arriving from inside."
    },
    {
      "id": "AT-06.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/text_realisation.rs",
      "covers": [
        "AC-06.2"
      ],
      "status": "green",
      "note": "**GREEN, AND RE-CITED off `text_realisation_never_read_back.rs`, which does not exist** (vc verified at `c840251a`). Arms: `the_realisation_is_never_seen_by_the_verb_that_reconciles_the_tree`, `two_realisations_do_not_overwrite_each_other`. **dc's FIRST VERSION OF THIS ARM WAS WRONG IN THE INSTRUCTIVE DIRECTION AND THEY SAID SO: it called `Project::classify` on each realised path and asserted `Unattached`. It failed, and the failure was theirs -- `classify` takes a THREAD-RELATIVE path, so handed a project-relative one it saw `cmd-st.md` and correctly answered `Attachment`.** **THE CRITERION IS NOT WHAT `classify` SAYS WHEN YOU MISLEAD IT; IT IS WHETHER ANYTHING EVER HANDS IT A PATH UNDER `.backup/`.** So the arm now drives the REAL CONSUMER -- realise into `.backup/text/`, run `organize::observe` + `plan`, assert no realised file appears in ANY row, **not even as `Unclaimed`, because `Unclaimed` is still the verb reporting on a file that is none of its business.** Stronger than what dc set out to write, reached only by mis-writing it first. **AND NO TIME CROSSES INTO THE MODULE (D42): `realise()` takes a DESTINATION. The facade mints an envelope with no time, `append_event` returns `RETURNING ts`, and that names the directory. A `stamp: &str` parameter would have been a TIME-TYPED INPUT WEARING A STRING'S CLOTHES**, which is the form D42 is written to catch."
    },
    {
      "id": "AT-06.3",
      "kind": "test",
      "file": "native/rust/crates/intent-cli/tests/export_md_accepted.rs",
      "covers": [
        "AC-06.3"
      ],
      "status": "to-write",
      "note": "unwritten -- ST0057 is 0 of 8 WPs. `to-write` is the honest state: the test does not exist, which is not the same as existing and failing."
    },
    {
      "id": "AT-06.4",
      "kind": "test",
      "file": "native/rust/crates/intent-cli/tests/init_from_empty_dir.rs",
      "covers": [
        "AC-06.4"
      ],
      "status": "to-write",
      "note": "unwritten -- ST0057 is 0 of 8 WPs. `to-write` is the honest state: the test does not exist, which is not the same as existing and failing."
    },
    {
      "id": "AT-07.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/address_resolution_single_home.rs",
      "covers": [
        "AC-07.1"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-19 (ic).** 8 tests. **THE SINGULARITY ARM IS STRUCTURAL RATHER THAN BEHAVIOURAL BECAUSE THE CRITERION SAYS SO** -- _checked by asserting a single implementation, not by comparing two outputs_ -- so it reads the `intent-cli` and `intentd` sources for the scheme literal outside a comment. A second resolver has to spell `intent://` somewhere, and comparing two outputs would pass for as long as the two implementations happened to agree."
    },
    {
      "id": "AT-07.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/address_views_have_no_url.rs",
      "covers": [
        "AC-07.2"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-19 (ic).** 4 tests, plus a fifth added because the mutation pass refused to close. **THIS ROW IS THE DAY'S STRONGEST ARGUMENT FOR MUTATION TESTING: SEVEN ARMS, SIX RED, AND ARM 6 SURVIVED.** A parser matching `[\"threads\", id, ..]` and IGNORING THE REMAINDER passed ALL TWENTY tests while resolving `intent:///threads/ST0056/anything/at/all` to the bare thread. **That is not a missing edge case -- IT MAKES THIS CRITERION'S REFUSAL A NAME MATCH RATHER THAN A RULE.** `/threads/ST0056/info.md` is refused only because `info.md` sits on a `VIEW_NAMES` list; under the mutant `/threads/ST0056/whatever.md` resolves to the thread and silently drops the segment. **THE SCHEME BECOMES A PATH ALIAS ONE UNLISTED FILENAME AT A TIME, which is verbatim what this row exists to prevent -- and all four original tests passed while it was true.** **THE BEHAVIOUR WAS ALREADY CORRECT; THE TEST WAS MISSING, so nothing would have stopped someone simplifying the catch-all arm later and nothing would have caught them.** Closed by `trailing_segments_are_refused_rather_than_truncated`, which carries the discriminating half that stops it degenerating into a segment count: **an ATTACHMENT path is legitimately variable-length and must still resolve.** Arm 6 now goes red."
    },
    {
      "id": "AT-07.3",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/address_format_set.rs",
      "covers": [
        "AC-07.3"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-19 (ic).** 4 tests. `src/address.rs` is new and covers the nine D57-8 forms, `Format` closed at `json|md`, empty authority, view refusal, and round trip."
    },
    {
      "id": "AT-07.4",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/address_md_is_the_hydrated_bytes.rs",
      "covers": [
        "AC-07.4"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-19 (ic).** 5 tests. `serve_md` SELECTS from `views::render_all` and never renders -- arm 1 renders directly instead and kills 3 of the 5. **The structural arm reads `address.rs` for calls to the per-view renderers AND carries a positive guard that it still goes through `render_all`, or the first assertion passes vacuously the day someone deletes the call: a NEGATIVE assertion with no positive companion is satisfied by removing its subject.** **TWO COLLECTION ADDRESSES ADDED, AND RULED NOT A DESIGN CHANGE (ic asked to be checked rather than nodded at; vc ruled from D57-8's own prose).** `Entity::Threads` and `Entity::AcCollection` are absent from D57-8's nine-form list and PRESENT IN ITS PROSE: _If an entity has more than one rendering, it is UNDER-ADDRESSED. `/threads/ST0056` returns the cover; `/threads/ST0056/ac` returns the acceptance view_, and separately, server-assigned ids are _a POST to the COLLECTION address, which returns the new address_. **THE NINE-FORM LIST ENUMERATES ARTEFACT FORMS AND IS NOT A CLOSED GRAMMAR.** **AND THE DERIVATION IS WHAT MAKES THIS NOT AN INVENTION: without `/threads/{id}/ac` THE ACCEPTANCE VIEW HAS NO ADDRESS AT ALL, while AC-07.4 requires `?format=md` to serve what `organize` would hydrate -- which includes `acceptance.md` -- and the only alternative, giving the VIEW an address, is exactly what D57-8 forbids in the sentence _VIEWS GET NO URL, and this is what stops the scheme becoming a path alias_.** The COLLECTION is the addressee and the VIEW is its representation, which is D57-8's own entity-versus-representation split applied rather than extended. **So the enum is right and the doc comment is wrong: a tenth ARTEFACT form is a design change; a collection address is not.**"
    },
    {
      "id": "AT-07.5",
      "kind": "test",
      "file": "intent/st/ST0057/parity/tools/no_daemon_required.sh",
      "covers": [
        "AC-07.5"
      ],
      "status": "green",
      "note": "**GREEN (ic, `c0749463`). vc DROVE BOTH ENTRY POINTS: `no_daemon_required.sh` rc=0, `--selftest` rc=0 with all three controls firing** -- B1 a declared intentd dependency caught, B2 a socket in sources caught, B3 the same words inside a COMMENT correctly not a finding. **ARM B IS THE ONE THAT SURVIVES intentd SHIPPING and ic PRINTS ARM A's WEAKNESS IN THE RESULT RATHER THAN LEAVING IT FOR A READER:** the behavioural arm is green partly because `intent daemon` answers _is a known command that is not implemented yet_, so no daemon could have served a read whether or not the read path would allow it. **A green that is a fact about the ESTATE rather than about the PROPERTY is the same class as the skew guard reporting `nothing to check` all evening, and saying so on every run is the right handling of it.** **ic ALSO ANSWERED THE WITHDRAWAL TEST BETTER THAN vc POSED IT: hv's git grounds retire a precondition only where git can SUBSTITUTE for the proof, and here _restoring the estate from git RE-HYDRATES it, which destroys the precondition under test_ -- reaching for git falsifies the subject.** So this one was never withdrawable; it left AC-00.1's block on hv's direct instruction along with three others, and remains fully owed and now met. **The history below is kept because it records why the row sat blocked, not missing.** **`to-write` AND BLOCKED, NOT MISSING (ic 2026-08-19, verified BEFORE starting WP-07 rather than after; recorded by vc so it is not read as a hole).** This row asks that every address resolve in a FULLY DEHYDRATED estate, and dehydration is WP-04 / WP-06, which are dc's. **Same shape as AT-02.4 and AT-05.1: blocked on a thing that does not exist yet, not on a gap in this WP.** **AND ic ESTABLISHED THAT THE REST OF WP-07 NEEDS NOTHING FROM dc's HYDRATION PRIMITIVE BY CHECKING THE LOAD-BEARING ROW RATHER THAN THE EASY ONE:** AC-07.4's `?format=md` serves `View.content` from `views::render_all`, which already exists at `views.rs:928` returning `Vec<View>` where `View { path, content }`, and `View.path` comes from `project.info_view()` / `acceptance_view()` / `wp_info_view()` -- the resolvers themselves. So _the served bytes equal the file `organize` would hydrate_ holds BY CONSTRUCTION, through a READ path, with no writer involved."
    },
    {
      "id": "AT-07.6",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/address_empty_authority.rs",
      "covers": [
        "AC-07.6"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-19 (ic).** 5 tests, driven in the same pass as AT-07.1/.2/.3 -- 21 tests and seven mutation arms across the four, with every restore verified by absolute path after two relative-path restores silently left mutants live earlier in the day."
    },
    {
      "id": "AT-08.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/write_path_canon_always.rs",
      "covers": [
        "AC-08.1"
      ],
      "status": "green",
      "note": "unwritten -- ST0057 is 0 of 8 WPs. `to-write` is the honest state: the test does not exist, which is not the same as existing and failing."
    },
    {
      "id": "AT-08.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/mutation_roundtrip_complete.rs",
      "covers": [
        "AC-08.2"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-19 (ic; vc ran the file, then required evidence on the two clauses rc cannot settle, and got it).** 4 tests, rc=0, 2 arms. **THE EXTENT CLAUSE IS ANSWERED BY CONSTRUCTION RATHER THAN BY COVERAGE: THE DENOMINATOR IS TAKEN FROM THE MODEL'S SERIALISED KEYS, NEVER FROM A HAND LIST.** Each field is moved to a DIFFERENT value and read back, siblings are diffed, **and a map entry missing for a NEW field fails a separate test.** That is what separates a round trip that is lossless from one that happens to cover the fields somebody remembered -- a hand roster is the shrunken-denominator failure this estate keeps paying for, and it is absent here by design rather than by diligence."
    },
    {
      "id": "AT-08.3",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/mutation_put_format_by_authorship.rs",
      "covers": [
        "AC-08.3"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-19 (ic; vc ran the file, then required evidence on the exception limb).** 5 tests, rc=0, 5 mutation arms shared with AT-08.4. **BOTH LIMBS EVIDENCED, which is exactly what the row needed: json accepted, md REFUSED, and the attachment exception honoured BY AUTHORSHIP rather than by a special case** -- `Project::classify` stays the single answer to what a file is, so the asymmetry is implementable rather than aspirational. Two refusals beyond the row's text: **the body id must match the address**, and **a cross-project address is refused rather than written locally.** The conjunction risk vc flagged -- a test satisfying the rejection limb alone and reading as complete -- does not apply here, because the exception is DRIVEN rather than asserted."
    },
    {
      "id": "AT-08.4",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/mutation_create_splits_two_ways.rs",
      "covers": [
        "AC-08.4"
      ],
      "status": "red",
      "note": "**`to-write` -> `red` 2026-08-19 (ic built and committed at `53cb3f34`; vc ran it and ruled RED ON A SPECIFIC MEASURED GAP, not pending evidence).** `mutation_create_splits_two_ways.rs` exists at 137 lines and **PASSES: 4 tests, rc=0.** **BUT THE CRITERION IS NOT SATISFIED AND ic's OWN COMMIT MESSAGE SAYS SO.** This row requires BOTH halves: caller-assigned ids are a `PUT` to the entity address, **AND server-assigned ids are a `POST` TO THE COLLECTION ADDRESS RETURNING THE NEW ADDRESS.** `53cb3f34` states: _Caller-assigned ids are a PUT to the entity address; server-assigned ids are REFUSED with the rule that sent them away._ **A REFUSAL IS NOT A POST.** The PUT half ships; the create half is a well-formed rejection carrying the rule, which is the right interim behaviour and is not the criterion. **AND THE ROW STILL PASSES, which is the finding: a test written against a two-limb criterion where one limb is implemented as a refusal can assert the refusal and go green over half the row.** `you cannot address ST0058 before the tool has decided it is ST0058` is the whole reason the POST limb exists, and nothing in this estate can yet create a thread by address. **SECOND MEASUREMENT 2026-08-19, AND THE ROW STAYS RED: `fn post` HAS ZERO HITS IN `facade.rs`.** ic reported this row green with evidence covering _PUT creates at a caller-assigned address, is idempotent, does not disturb siblings; PUT to threads/issues/collections REFUSED naming the server-assigned rule_. **Every one of those is the FIRST limb or the refusal. None of it is a POST that creates and returns the new address**, and vc grepped for the route rather than arguing from the description. **THE PATTERN IS THE ONE ic ADMITTED IN THE SAME MESSAGE: a test written against a two-limb criterion where one limb ships as a REFUSAL can assert the refusal and read as covering the limb.** They caught it in `no_verb_creates_an_acceptance_test_or_criterion` and did not catch it here, in the row where vc had already flagged it once. **This goes green when `POST <collection>` creates and returns the new address, not before.**"
    },
    {
      "id": "AT-08.5",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/mutation_every_writable_field.rs",
      "covers": [
        "AC-08.5"
      ],
      "status": "red",
      "note": "**`to-write` -> `red` 2026-08-19 (ic built it; vc ruled).** `mutation_every_writable_field.rs`, 4 tests. **RED IS THE CRITERION'S OWN VERDICT: the unsettable set is this criterion's printed OUTPUT, and the set is not empty.** **ic's DESIGN CALL, ESCALATED RATHER THAN TAKEN, AND CONFIRMED: THE GAP IS PINNED, NOT PRINTED, AND NO DELIBERATELY-FAILING TEST IS ADDED.** The natural form was a failing test and ic checked the estate before writing one: **SIX RED ROWS ARE CARGO TESTS** -- `dep_graph_guard`, `mutation_completeness`, `write_moves_only_what_changed`, `generated_views_are_not_formatted`, `organize_five_rows`, `organize_attachment_divergence` -- **all six exist and the workspace is green, so NOT ONE RED ROW IN THIS ESTATE CORRESPONDS TO A FAILING CARGO TEST. The ROW carries _does not pass_; the SUITE stays green.** A deliberately-failing test would have been the first of its kind, would have broken the shared suite for three other nodes AND for the external acceptance verifier, and would have been imposed by one node mid-flight without asking. **AND THE PIN IS THE STRONGER FORM ANYWAY: the unsettable set is asserted to be EXACTLY `[\"file\", \"prose\", \"covers\", \"note\"]`, so CLOSING part of it fails and WIDENING it fails, and somebody must move the row either way. A test that merely PRINTED the gap and passed would notice neither** -- the vacuous green this estate keeps paying for. Same shape for the create-verb pin, which asserts NO creator exists and fails the day one appears."
    },
    {
      "id": "AT-02.5",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/intentfiles_names_artefacts_only.rs",
      "covers": [
        "AC-02.5"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-19 (ic).** 5 tests. **THE TEST DESIGN IS THE RESULT, AND IT IS A MUTATION TEST AGAINST THE CRITERION RATHER THAN THE EXEMPLAR.** A corpus of paths was the obvious build and WOULD HAVE PASSED ON THE WRONG IMPLEMENTATION: arm 1 replaces the id predicate with a `/`-and-`.` blacklist, which refuses all nine plausible paths AND ADMITS `STEELTHREAD:README`. So the asserted property is an EQUIVALENCE -- accepted-set == id-set -- swept over near-ids, non-path non-ids, and the real estate's own files read off disk. **The blacklist arm goes red on the equivalence and would have gone green on a path corpus, so the corpus would have certified a parser admitting any non-path string.** **AND THE TEST RESOLVED AN UNRESOLVED QUESTION IN THE PARSER ITSELF: `STEELTHREAD:ST0056 ` is accepted while `is_thread_id(\"ST0056 \")` is false**, which reads as exactly the second id rule AC-02.5 forbids. It is not -- the LINE is trimmed before it is split, so the predicate never sees the space. **TWO RULES AT TWO LEVELS IS FINE; TWO RULES AT ONE LEVEL IS THE DEFECT.** The whitespace tolerance is now a stated decision rather than an accident nobody wrote down, because a trailing space is invisible to a reader and visible in a diff, so refusing it silently and admitting it silently are both wrong."
    },
    {
      "id": "AT-03.5",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/sync_reports_uncommitted_attachment.rs",
      "covers": [
        "AC-03.5"
      ],
      "status": "green",
      "note": "**GREEN. 7 arms pass at `18a9ab6b`, file TRACKED and byte-identical to HEAD.** **THE COMPARISON IS AGAINST THE INDEX AND THE STAGED ARM IS THE WHOLE PROOF: `git commit` records the INDEX, so the index is what the next commit will hold. Compare against HEAD and every staged file reports as uncommitted -- the normal state of a commit being assembled.** An implementation keyed on HEAD passes every other arm in the file and fails only that one, and it is PAIRED: unstage the same bytes and they report again, so it cannot pass by the check having gone quiet. **TWO QUERIES, BECAUSE _not in the index_ HAS TWO SHAPES: `diff-files` answers only for TRACKED files, so a check built on it alone is silent about a file the index has never heard of -- which is exactly the file whose bytes are least likely to be in any commit.** **`None` FOR NO-REPOSITORY RATHER THAN AN EMPTY LIST, and the CLI prints the difference: _nothing is uncommitted_ is a clean bill of health, _I could not ask_ is the ABSENCE of one, and collapsing them lets a project with no git report as fully committed.** **It REPORTS before the write and does not refuse** -- a dirty tree is nobody's problem, the harm is at the COMMIT, and AC-03.6 is that gate. **And the estate's known `.webloc` violator is now a FIXTURE rather than only a hazard**: `-z` with NUL splitting, never whitespace, with an arm asserting a spaced path is ONE finding, killed by the mutation that adds a space to the split predicate and by nothing else."
    },
    {
      "id": "AT-03.6",
      "kind": "test",
      "file": "intent/st/ST0056/parity/tools/canon_commit_check.sh",
      "covers": [
        "AC-03.6"
      ],
      "status": "red",
      "note": "**STILL RED, AND FOR A DEEPER REASON THAN THIS ROW RECORDS -- cc BUILT THE WIRING, MEASURED IT, AND REVERTED IT (2026-08-19).** The row says the instrument is built and the criterion is unmet because nothing gates on it. **True, and NOT the blocker.** cc wired it end to end -- `G_CANONCOMMIT`, `--list-guards`, a path-triggered call site, roster moved to `gated` -- and `runner_roster_check.sh` went GREEN at 11 gated / 6 manual. **Then the positive control disagreed with every instrument that watches this.** **`canon_commit_check.sh` TAKES A COMMITTED REVISION: `REV=\"HEAD\"` by default, comparing `$REV` against `$REV^`. There is no index mode -- vc confirmed by reading: no `--cached`, no `git show :`, no `diff-index` anywhere in its 425 lines.** **AT PRE-COMMIT TIME HEAD IS THE PARENT OF THE COMMIT BEING MADE, so the gate evaluates what the PREVIOUS commit added.** cc measured both directions: a divergence planted IN THE INDEX in an isolated worktree printed `EXAMINED 0 of 280` at exit 0 -- **the pending commit is invisible to it** -- while the same tool at `d980e5b5`, one of the five episode commits, printed `ADDS 3 of 275` at exit 1. **It works perfectly, on revisions that already exist.** **SO GATING IT AT ITS DEFAULT IS WORSE THAN NOT GATING IT, IN BOTH DIRECTIONS AT ONCE: the harmful commit sails through because while it is being made the tool is looking at its parent, AND THE NEXT COMMIT IS BLOCKED FOR ITS PARENT'S FAULT** -- which is precisely blocking on inherited breakage, the failure this criterion's own text names as what turns a gate into one nobody keeps. dc's 23-of-46 measurement is the argument and this wiring delivers exactly the 23 version. **WHAT IT NEEDS IS A `--staged` MODE, NOT A CALL SITE: the criterion is _a commit must not contain canon naming bytes not in that same commit_, and at pre-commit time THAT SAME COMMIT IS THE INDEX.** `git show :<path>` and `git diff-index --cached` are the index-side equivalents of what the tool already does with `git show $REV:<path>` and `git diff-tree`. A real change to a 425-line instrument rather than a flag. **AND THE ROSTER'S REASON FOR HOLDING IT `manual` HAS EXPIRED, KILLED BY AN UNRELATED COMMIT: it read _there is no narrow attachment-sync verb ... revisit after ST0057 WP-08_, and `sync --to-store <ID>` landed at `212b0075` this morning.** Every node can now sync one thread without ingesting the board. **Nothing pointed at that row when the verb landed and nothing could have -- a recorded reason true when written, made false elsewhere, with no instrument watching the join.** cc found it by reading the row they had to touch. **Re-measured while there: 3.6-4.9s at `61b93440` against 2.49-2.55s recorded at `4ba598f1` -- the estate grew and the figure did not follow, so it is now the SLOWEST instrument in the gate.** Whoever admits it wants the path trigger.\n\n**IT FIRED UNPLANTED, ON A REAL COMMIT, 2026-08-20 -- AND THAT IS STRONGER EVIDENCE THAN ANY PLANTED CONTROL.** cc committed `1e2bc65e` (the gate arm itself) with an edited attachment and canon still naming the old bytes. Verified by vc at zero hops rather than carried: `canon_commit_check.sh 1e2bc65e` -> **rc=1, ADDS 1 of 1 examined**; the two repair commits `aa4c3ac0` and `9e191824` -> rc=0. **A planted divergence proves the mechanism works; an unplanted one proves the condition is REACHABLE by a careful node on an ordinary day.** This row now has both.\n\n**AND IT CONFIRMS THIS NOTE'S OWN DIAGNOSIS RATHER THAN AMENDING IT: the tool caught it AFTER the fact, because it reads a committed revision.** `1e2bc65e` is **permanently divergent in history** -- the repair fixes the next commit and can never reach that one. **That is the cost of `manual`, measured in a lost commit rather than argued.**\n\n**dc's FRAMING IS THE ONE THAT SURVIVES, AND IT IS NOT ABOUT THE NODE: _a control which depends on the author remembering is not a control, it is a hope with a filename._** cc wrote it up as their own carelessness; dc refused the harsher reading and applied their own guard-roster sentence unchanged. **The instrument is rostered `manual`, nothing dispatches it, so nothing did.** cc is the second node to hit this exact file class, with the rule in bold on their own board -- which is the evidence that remembering does not scale, not that anyone was careless.\n\n**THIRD MEMBER OF A CLASS THIS ESTATE NOW HAS THREE OF, AND IT SHOULD BE READ AS A CLASS: an instrument that is BUILT, CORRECT, ROSTERED, AND DISPATCHED BY NOTHING.** `canon-ignore-guard.sh` (rostered in `pre-commit.sh`, whose roster this repo's commit path never reads -- AT-01.5); `append-only-guard.sh` (same roster, same silence, and it is the alarm on `intent/events.jsonl` -- AC-09.2); and this one (`manual`, which names no dispatcher at all). **Three instruments, three dispositions, one outcome.** `manual` is not a lighter form of `gated` -- **it is a disposition whose enforcement is a person's memory, and it cost a permanently divergent commit on the day it was tested.**"
    },
    {
      "id": "AT-01.5",
      "kind": "test",
      "file": "lib/templates/hooks/canon-ignore-guard.sh",
      "covers": [
        "AC-01.5"
      ],
      "status": "red",
      "note": "**refuse at COMMIT, not at check: the criterion is about the edit, and AC-01.2 already covers the state.** Red-first arm is mechanical and cheap -- add `intent/.*/` to `.gitignore` in a sacrificial copy and require the commit to be refused, naming the rule and the paths it would orphan. **Belongs in WP-01 rather than WP-08 because WP-01 CREATES the hazard**: before the relocation there is no `.canon/` to orphan. cc raised it, declined to mint it, and put the reasoning in `canon_dir()`'s doc comment so it is re-derivable at the source even if this row is never built. **RED, NOT to-write, FROM 2026-08-19: THE FILE EXISTS AND NOTHING CONSULTS IT.** Committed by cc at `91736056`, driven six ways in a real `git clone`, both load-bearing halves mutation-proven -- forcing the attribution true flips the inherited arm 0->1, dropping the synthetic future-probes flips the future-only arm 1->0.\n\n**THE RECORDED REASON HAS NOW BEEN WRONG TWICE, AND THE THIRD READING IS THE ONE TO BUILD AGAINST** (cc and vc, converged independently from different fixtures, 2026-08-20, measured at `0a9a7341`). The previous note said the roster is `WB_GUARDS` and names `whiteboard-clock-guard.sh` and `whiteboard-header-guard.sh` AND NOTHING ELSE. **That was true at `ca1d330c` and is now false**: the roster is `GUARDS` (`pre-commit.sh:106-122`) and it names FOUR, this guard among them. **So the guard IS rostered and it STILL never runs, which is a worse state than the row recorded rather than a better one.**\n\n**THERE ARE THREE DISPATCHERS IN THIS REPO AND THEY DO NOT AGREE:**\n\n    guard                     pre-commit.intent   cmd/precommit   template roster   RUNS HERE\n    whiteboard-clock-guard            1                 0                1            YES\n    whiteboard-header-guard           0                 2                1            YES\n    canon-ignore-guard                0                 0                1            NO\n    append-only-guard                 0                 0                1            NO\n\ngit's chain here is `.git/hooks/pre-commit` -> `.git/hooks/pre-commit.intent` (6467 bytes, installed 2026-08-14, ONE hard-coded guard, no roster at all) -> prettier -> `bin/int precommit` (repo-local, hard-codes the header guard by absolute path at `cmd/precommit:221`). **Two of four run, and NEITHER runs through the roster** -- both reach their guard by a hard-coded path in a different file. **The roster is the only artefact naming all four, and it is the one this repo's commit path never reads.**\n\n**SO AC-01.5 IS UNMEETABLE BY ANY EDIT TO THE GUARD, TO THE ROSTER, OR TO THE TEMPLATE** -- and a reader taking the earlier note at face value goes at exactly those three. The guard is correct, rostered, and mutation-proven; the hole is that `pre-commit.intent` here is an install-time COPY that predates the roster, so **this repo is the exception a consumer on a fresh install would not hit.** Wiring it means reaching the installed copy, not the template.\n\n**cc's formulation stands and sharpens: A CAPABILITY WITH NO CONSUMER IS NOT A GATE -- and being named in a roster is not a consumer.** AC-00.10 checks an instrument's INTERNALS; nothing checks its WIRING, and the roster is what looked like the thing that did.\n\n**HOW IT WENT UNSEEN, WHICH IS THE REUSABLE PART: the roster and the dispatcher that reads it are ONE FILE, so every consistency check between them passes.** The disagreement is visible only against a third artefact nobody was comparing to -- the estate's own class, TWO ARTEFACTS DISAGREE AND NO THIRD THING READS BOTH, with the twist that here the third thing exists and is itself the artefact being ignored. **The assertion that catches it is reachability along the path `.git/hooks/pre-commit` ACTUALLY TAKES; an assertion that each rostered file EXISTS passes today, on this hole.**\n\n**BOTH NODES MADE THE SAME ERROR IN OPPOSITE DIRECTIONS WITHIN ONE HOUR, AND IT IS THE SAME ERROR THIS ROW IS ABOUT.** cc read `pre-commit.intent` (too narrow) and reported three guards dead; vc executed `lib/templates/hooks/pre-commit.sh` directly, saw all four dispatch under `bash -x`, and reported the roster live (too new). Neither read the chain. **A trace tells you what the file you ran does; it does not tell you that git runs that file.**"
    },
    {
      "id": "AT-01.6",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/canon_resolver_singularity.rs",
      "covers": [
        "AC-01.6"
      ],
      "status": "green",
      "note": "**Red-first arm is the historical bug, and it is free: `export.rs:386` spelled the canon path independently until cc's WP-01 build unified it.** Re-introduce any second independent spelling and the test must go red. **Drive it by configuring a NON-DEFAULT `intent_dir` and asserting every canon artefact lands under the resolver's answer** -- a site that spells the path itself lands somewhere else, which is the whole failure. **Assert the negative arm too: `thread_dir()` still answers `intent/st/<ID>/` and `info.md`/`acceptance.md` still resolve there**, or a wholesale move passes. **DO NOT ASSERT A COUNT** -- ic withdrew 3-of-17 as a measurement of their probe's reach (one directory of 41 against a repo-wide concern), and vc's narrowed 23 missed `export.rs` entirely. **Both fixtures named in AC-01.6 belong in this file as regression cases**, since each refutes a different sweep. **And the guard cc's board names for the live move is broken as written: bare `intent st list` returns 2, not 57** -- it defaults to in-progress only, `--all` is not a flag, and the invocation that reaches the estate is `st list --status all` (measured 57, breakdown 52 Completed / 2 Cancelled / 2 WIP / 1 NotStarted). The Done population is exactly what a canon move risks and it is structurally absent from the default output."
    },
    {
      "id": "AT-01.7",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/openness.rs",
      "covers": [
        "AC-01.7"
      ],
      "status": "green",
      "note": "**EXTENDS THE EXISTING FILE RATHER THAN ADDING ONE** -- `openness.rs` already owns the openness face and already parses the declarations; a second home would be the Highlander problem this estate keeps meeting. **Red-first is free and must be driven the way the defect will actually arrive: apply the WP-01 relocation and require the test to go RED on 7 declarations**, not by hand-editing a path to something bogus. A synthetic bad path proves the checker parses; the relocation proves it catches the real event. **Assert the RESOLVED count against the DECLARED count and print both**, so a declaration silently dropped from the DDL cannot read as full coverage -- the shrunken-roster failure that took AT-03.15 five versions. **`DERIVED` declarations are out of scope by construction**: they name no path, and the criterion is about referents. State that in the test rather than leaving it to be inferred from the filter. **GREEN AT `16048f82` -- 8 of 8 declarations resolve. AND THE RED-FIRST ARM PRESCRIBED ABOVE WAS ALREADY CONSUMED WHEN IT WAS WRITTEN** (cc 2026-08-19, who found it while executing it and lodged the correction BEFORE moving). **The polarity is inverted: `f41d6760` repointed `schema/ddl.sql` AT THE SAME TIME AS THE RESOLVER, so the declarations had been DANGLING SINCE THAT COMMIT and the relocation is what makes them RESOLVE.** Applying the relocation produces GREEN, never the prescribed red. **The only window in which the real red was observable was BEFORE the move**, and cc wrote the test into it: RED at 1 of 8 at `8bb47e49` (only `intent/events.jsonl` resolving; six `intent/.canon/st/<ID>.json` and one `intent/.canon/issues/<NNNN>.json` dangling -- exactly the 6+1 split AC-01.7 predicts, reached independently), then GREEN at 8 of 8 at `16048f82`, same session, same file, no edit to the test in between. **A RED-FIRST ARM PHRASED AS _apply change X, require RED_ CARRIES AN UNDECLARED EXPIRY: it encodes an assumed starting state, and X landing in two parts puts the red in the gap between them, where it goes unobserved and unclaimed.** Three implementation holes cc closed and each is load-bearing: it reads the SHIPPED face `schema/ddl.sql` and not `store::DDL` (reading the generator's literal is not reading what travels under D34); it takes the FIRST whitespace token as the path, because `declarations_in` deliberately absorbs the comment lines that follow a declaration, and taking the whole remainder yields a referent that fails for a reason unrelated to where canon lives -- red before AND after, reading exactly like a failed move; and the partition is ASSERTED TO CLOSE, which caught 8 carried + 3 DERIVED = 11 against 10 `CREATE TABLE`, because `doc_sections` is `CREATE VIRTUAL TABLE ... USING fts5`. **Closing a partition against the WRONG WHOLE is its own defect** -- a table count would have failed this test for a reason with nothing to do with its criterion."
    },
    {
      "id": "AT-09.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/realisation_is_recorded.rs",
      "covers": [
        "AC-09.1"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-20, mutation-proven in BOTH polarities.** `realisation_is_recorded.rs` drives the four realisation verbs and asserts an event per ACT and, just as hard, NO event per non-act. **The silent half is the one that is easy to get wrong**: a verb emitting on every call passes any test that only asks *is the act recorded*, and fills the one unrecoverable table with records of acts that did not happen. Mutants, both driven: `if false` on organize's emission fails the dehydration arm; emitting unconditionally fails BOTH silent arms. Correct code passes 3 of 3.\n\n**PROVEN ON THE LIVE ESTATE, NOT ONLY IN A FIXTURE.** `organize` preview -> 0 events; `organize --apply` over a tree already agreeing with the manifest -> 0 events; `st hydrate ST0001` -> `disk.hydrate {pinned: true, hydrated: 5}`; hydrating it a second time -> 0 further events; undeclaring it and running `organize --apply` -> `disk.organize` naming all five removed files and the pruned directory. Estate returned to its exact starting state, manifest byte-identical.\n\n**THE FIXTURE HAD TO OPEN THE SHIP GATE HONESTLY AND THAT IS WORTH RECORDING.** The first version removed nothing and the guard assertion caught it: `PreconditionsUnmet { removals: 6, unreadable: NoDeclaration }`. `Verdict`'s fields are private and `preconditions::check` is its only constructor, so the test builds canon in which the real declaration is really satisfied (`common::gate_open`) rather than being handed a permitting verdict. **The gate is enforced by construction rather than by everyone remembering not to bypass it** -- and a test that had bypassed it would have proven nothing while reading green.\n\n**WHAT THIS ROW DOES NOT COVER, STATED RATHER THAN IMPLIED: `sync_to_disk` and `sync_from_disk` still do not emit.** Both write the filesystem -- the restore direction rewrites the views of what it read -- so AC-09.1's denominator is **2 of 4 realisation verbs**, not 4 of 4. Counting this row as closing AC-09.1 would be a count of the verbs done standing in for the verbs owed."
    },
    {
      "id": "AT-09.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/event_log_concurrent_append.rs",
      "covers": [
        "AC-09.2"
      ],
      "status": "to-write",
      "note": "**Two arms, and the second is the one nobody writes.** Arm one: two writers append concurrently and both records survive. Arm two: **a TRUNCATING write over the log is refused** -- which is `append-only-guard.sh`'s declared subject and is currently caught by nothing, since that guard has zero call sites on git's real path (`.git/hooks/pre-commit` -> `pre-commit.intent` -> `bin/int precommit`, neither of which reads the roster naming it). **A test here does not fix the wiring and must not be read as fixing it**; AT-01.5 carries that finding."
    },
    {
      "id": "AT-10.1",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/view_skew_check.rs",
      "covers": [
        "AC-10.1"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-20 at `b082b488`, and mutation-proven in BOTH directions rather than observed once.** `absence_is_silence_only_where_the_manifest_says_undeclared` drives one fixture with the same two files missing and only the manifest differing: undeclared -> silent, declared -> both files named. **Either arm alone passes on a bug**, which is why they are one test. Mutants, both driven: reverting to the pre-fix `Err(_) => push` fails the undeclared arm (1 test red); replacing it with a blanket `Err(_) => {}` fails THREE tests including the pre-existing `a_missing_view_is_reported_as_skew`. **Nothing passes both by accident.** Measured on the live estate: `doctor` 235 findings at rc=1 -> 1, the survivor being the real `ST0011` row; removing a view of a DECLARED thread returns it to 2; restoring returns it to 1."
    },
    {
      "id": "AT-10.2",
      "kind": "test",
      "file": "native/rust/crates/intentsvcs/tests/view_skew_check.rs",
      "covers": [
        "AC-10.2"
      ],
      "status": "green",
      "note": "**GREEN 2026-08-20 at `b082b488`.** `an_absent_or_unreadable_manifest_keeps_every_view_in_scope` asserts `NothingSaid` and `Unreadable` behave identically over the same missing view -- both keep it in scope and both name it. **This is the arm that stops the fix from being a silent estate-wide opt-out** the day somebody's manifest stops parsing. **What it does NOT cover, stated rather than implied: nothing reports the `Unreadable` state to an operator.** AC-10.2 names that as an owed reader; this row proves only that the state is honoured, not that it is surfaced."
    }
  ],
  "attachments": [
    {
      "path": "design.md",
      "text": "---\nverblock: \"18 Aug 2026:v0.1: vc - disk as a sparse projection of the store\"\n---\n\n# ST0057 -- Disk as a sparse projection of the store\n\n## Objective\n\nMake disk optional without making anything unrecoverable.\n\nToday `intent/st` holds 233 files per the census and 1220 in total, and every one of them is on disk because that is where v2 kept truth. Under D01-as-reversed the store is truth and the files are a projection -- **but a projection nobody can regenerate is not a projection, it is the original with extra steps.** This thread ends with three things true at once: `intent/st` holds little more than an index and the live work; anything not on disk is provably reproducible; and a human with an editor and no working tool can still read the whole project.\n\n## The three-layer model, because the brief collapses two of them\n\nhv's framing was _\"the DB is SSOT and disk is a sparse copy of the db out to realised files based on an index.\"_ That is right for two of the three things on disk and fatal for the third.\n\n| layer     | what                                           | committed            | who derives it              |\n| --------- | ---------------------------------------------- | -------------------- | --------------------------- |\n| **canon** | `thread.json`, `issues/NNNN.json`              | **yes**              | authored through the facade |\n| **store** | `intent/.cache/intent.db`                      | **NO -- gitignored** | rebuilt from canon          |\n| **views** | `info.md`, `acceptance.md`, `steel_threads.md` | yes                  | rendered from the store     |\n\n**The store is SSOT for a running tool on one machine. It is not what travels.** `intent/.cache/` is gitignored at `.gitignore:127` and the ignore rule's own comment says the name contradicts the model. `intent sync --to-disk` is documented as writing _\"the committed extract\"_ -- the vocabulary already distinguishes the runtime store from the durable record.\n\n**So sparseness applies to VIEWS. Canon is never sparse.** If the manifest governed canon, an unrealised artefact would exist only inside a gitignored database: absent from a fresh clone, and destroyed by `rm -rf intent/.cache`. **D29 -- a gitignored path is never canon -- is not a style rule here, it is what makes a clone complete.**\n\nThat single ruling is what makes every deletion in this design safe rather than careful: **`organize` only ever removes a file it can regenerate, from a source sitting beside it in the same commit.**\n\n### Measured, because the sizes settle the argument\n\n```\ncanon      thread.json x56 + issues/NNNN.json x40    1,602,351 bytes   ALWAYS on disk\nviews      *.md under intent/st                      2,704,783 bytes   sparse\n  of which ST0056, the ONE live thread                1,021,934        38% of all markdown\n  ST0011, a completed one                                 7,199\n```\n\n**The single live thread is 38% of all rendered markdown.** Sparse views buy nearly everything; complete canon costs 1.6 MB, which is the price of a clone that works.\n\n## D57-1 -- Canon relocates, and stays one file per artefact\n\nKeeping canon complete does not mean keeping it in the way. hv's requirement is that `intent/st` stop holding a bajillion files, and canon-in-place does not deliver that: 56 directories remain whatever happens to the markdown.\n\n- **A -- canon in place.** Bytes fall ~60%; `intent/st` still lists 56 entries. **Does not meet the requirement.**\n- **B -- one consolidated `threads.jsonl`.** **Rejected: four concurrent writers make a single canon file a merge-conflict generator**, and this estate has four nodes plus, until today, a test suite.\n- **C -- `intent/.canon/st/ST0011.json`, still per-artefact. RULED.**\n\nC is the only one that empties `intent/st`, keeps a clone complete, and keeps a per-artefact diff so two nodes editing two threads never conflict. It is a path change, not a model change.\n\n**After C, `intent/st` holds `steel_threads.md` and the realised threads. Nothing else.**\n\n## D57-2 -- `.intentfiles` is durable state\n\n**SUPERSEDED 2026-08-19. This decision used to read _`.intentfiles`, and why it has two regions_, and hv replaced it outright mid-implementation.** The two-region text is kept below the rule so a reader meeting the retired shape in an old commit can tell which one they are looking at.\n\n**THE RULE, hv's own words:**\n\n> **`.intentfiles` is DURABLE STATE -- the record of which database artefacts also have a realised form on disk.**\n> **Realisation is driven from `.intentfiles`; commands change `.intentfiles`; `organize` realises it.**\n\nThe manifest is committed, and it has to be: the set it governs is committed. **A machine-local manifest would let `organize` on one machine delete files another machine depends on, and land the deletion in a commit with no declared reason.** Committed, hydration and dehydration are reviewable -- the diff shows the manifest line changing beside the files appearing or disappearing. **That half of the original decision survives unchanged.**\n\n```\n# .intentfiles -- which database artefacts also have a realised form on disk.\n# Realisation is driven from this file. Commands change it; `organize` realises\n# it. Nothing recomputes it from status.\n\nSTEELTHREAD:ST0046\nSTEELTHREAD:ST0056\nSTEELTHREAD:ST0057\n```\n\n**MANY WRITERS, NO RECOMPUTATION.** `st new` adds the id, `st done` and `st cancel` remove it, `st hydrate` / `st dehydrate` and the ISSUE equivalents do it directly, and a human may edit it by hand. All of those are ordinary writers and none is privileged. **_AUTHORED_ WAS vc's WORD FOR IT AND IT WAS WRONG** -- hv corrected it before either builder committed to a shape. It does not mean untouched by commands; it means nothing recomputes it.\n\n**THE REGIONS ARE GONE BECAUSE THE REGENERATION IS GONE.** hv's question settles it: _why isn't the organise operation simply: a) look at .intentfiles, b) hydrate the items in the file, c) dehydrate any previously hydrated items that are no longer in the file._ The two regions existed only because the file was machine-generated -- if `organize` rewrote the list from status every run, a hand-added line would be wiped, so a protected region was needed. **Remove the regeneration and the protected region has nothing to protect against.** A write is a CHANGE TO STATE, never a REGENERATION of it.\n\n**IT ALSO EXPLAINS A LOOSE END NOBODY HAD ACCOUNTED FOR: `intentfiles::render` had ZERO production callers.** That looked like an unwired writer and was not -- the thing it does is not needed. Nobody failed to wire it.\n\n**ABSENT IS NOT EMPTY, AND THE DIFFERENCE IS 423 FILES.** A missing manifest means nobody has said, so everything stays; a manifest present and declaring nothing means keep nothing. This estate carried a 26-line file of pure comment for a day, which is the second state, so 545 files sat on the removal path **by omission rather than by decision** -- held back only by the dehydration gate. Populating the list was what made the removal intentional.\n\n**THE GRAMMAR REFUSES RATHER THAN SKIPS.** `<SIGIL>:<ID>`, sigil in `STEELTHREAD | ISSUE`, optional trailing comment, nothing else. A line the parser cannot read aborts the run with its line number. **A skipped line drops an artefact from realisation and leaves an estate indistinguishable from one that never listed it** -- the silent-drop shape v2.19.0 already paid for twice, in `ac gate`'s F1 fix and in the AT row grammar's `at lint`. **This half survives unchanged too.**\n\n### The retired two-region shape, for readers of older commits\n\n```\n# Lines between BEGIN and END are GENERATED by `intent organize` from status.\n# Lines outside the markers are PINS and are never rewritten or removed.\n\nSTEELTHREAD:ST0011        # pinned by hand: reading the old test-suite thread\n\n<!-- STEEL THREADS: BEGIN -->\nSTEELTHREAD:ST0057\n<!-- STEEL THREADS: END -->\n```\n\n**The pin bought exactly one property: a closed artefact staying on disk.** Under the rule above that is ordinary behaviour rather than an exception -- **the list wins over status, both directions, and status has no vote at `organize` time at all.** Measured on this estate at `e7f00e65` across four statuses: 52 `completed` and 2 `cancelled` removed while unlisted, 1 `not-started` and 2 `wip` kept while listed, and ST0010 (`cancelled`) hydrated and kept while listed.\n\n## D57-3 -- `intent organize`, four answers and one refusal\n\nExisting verb, currently v2's status-directory tidier, which under the flat layout has no work left. Inherit the name rather than invent one.\n\n| declared | on disk                              | action                                                            |\n| -------- | ------------------------------------ | ----------------------------------------------------------------- |\n| yes      | absent                               | **HYDRATE**                                                       |\n| yes      | present                              | **VERIFY** -- re-render in memory, write only if the bytes differ |\n| no       | absent                               | nothing                                                           |\n| no       | present                              | **DEHYDRATE**, subject to the gate below                          |\n| --       | a path the renderer does not produce | **UNCLAIMED -- report, never remove**                             |\n\n**The fifth row is the one that matters.** A file the renderer cannot make is either an attachment or something a human put there. **`organize` must not be the thing that decides an unrecognised file is rubbish.**\n\n### The dehydration gate\n\nBefore removing any view: re-render it from the store into memory, compare to the bytes on disk, **refuse on any difference and report the path.** A difference means the disk holds something the store does not, and that something is destroyed by the removal. One render and one comparison per file -- **fail-safe by construction rather than by discipline**, and it is the same view-skew comparison `doctor` already performs.\n\n### An ATTACHMENT is not a VIEW, and one policy for both discards work\n\n**Authority follows AUTHORSHIP.** A view is authored in the model, so disk divergence means the file is stale. **An attachment is authored ON DISK, so divergence means the STORE is stale.** Overwriting an attachment destroys the author's edit; ingesting a view promotes a stale generated file into canon. Same divergence, opposite remedies.\n\n**`organize` NEVER resolves an attachment divergence.** It reports the path and names both verbs. _\"Decide which way to sync\"_ is a human decision by definition, and **a tool that decides it silently is choosing whose work to discard -- right most of the time and catastrophically wrong occasionally, which is the worst profile available.** `Project::classify` is the single answer to what a file is, so the asymmetry is implementable rather than aspirational.\n\n### Idempotence is a measured requirement\n\n`organize` run twice changes nothing, **including mtimes.** Measured 2026-08-18: the current render re-emits 255 of 1000 `.md` files byte-identically every pass, moving their mtime with zero content change. Harmless for conservation, not harmless for `file_index`, whose `clean`/`changed` state is computed from exactly that. **Write only on content difference.**\n\n### It locks against ANY process, not other `organize` runs\n\n`info`, `st list`, `doctor` and `export` all **materialise the store on access** -- a fresh clone's first read verb builds it. So a peer typing `intent st list` opens the same file `organize` is reconciling against. **\"The estate was quiet when I measured\" is not establishable over any window**, which makes the moment-of-act digest the only defence that survives a concurrent read: digest the measured tree, re-compute immediately before the irreversible step, refuse on any difference.\n\n## D57-4 -- `intent edit <ID>`, and why not `wip`\n\nhv proposed `intent wip <ID>`. **In this project `wip` already means a WP status, a thread status, `intent/wip.md`, and every node's board.** A fifth meaning collides with four live ones.\n\n**`intent edit <ID>`**, dispatching on id shape: hydrate if needed, write the pinned region, print the path. `intent st edit` already prints the path to a thread file, so this is that verb learning to hydrate first -- one behaviour, one home.\n\n**A hand realisation writes to the PINNED region.** Otherwise the next `organize` reverts it, because the generated region is a function of status and the artefact you opened is closed.\n\n## D57-5 -- The full text realisation, and a refusal whose reason expires\n\n**hv, 2026-08-18:** _\"I would expect to be able to trivially generate a full text realisation of all of the files for the entire project into, say, `.backup/...` so that I have that on-disk fallback.\"_\n\nThis is **not** `organize`. `organize` decides which artefacts have _working_ form under `intent/st`. This is a complete, disposable, regenerable text snapshot **elsewhere**, and it is the assurance that makes sparseness acceptable to a person rather than merely to a tool.\n\n**The two assurances are different and the design needs both.** The dehydration gate proves **the store holds it**. A complete text export proves **a human can get it back without the tool.** hv is asking for the second, and nothing in the design provided it.\n\n**`export --format md` ALREADY EXISTS AND IS DELIBERATELY REFUSED:**\n\n```\nexport.rs:192  \"design.md:57 names `md` as an export projection and it is not one\"\nexport.rs:208  \"The views are already in the tree -- `intent sync --to-disk` rewrites\n                them; for data a program will read, use `--format json`\"\n```\n\n**That refusal is correct today and becomes false the moment this thread lands.** Its justification is _the views are already in the tree_, and the entire purpose of D57-1 through D57-3 is that they are not. **A claim that was true when written and expires on a change already scheduled** -- the same class as `Issue::body`'s trim, caught here before it ships rather than after.\n\n**RULED: `--format md` is withdrawn from the refused set as part of this thread, not before.** The refusal stays honest while its premise holds.\n\nRequirements on the realisation:\n\n- **Complete, and it says so with a denominator.** Every thread, WP, issue, attachment and view, with a printed count against the canon totals. **A partial export that reads as complete is worse than no export.**\n- **Out of the way and gitignored.** `.backup/text/<UTC>/` -- `.backup/` is already the local-never-commit namespace holding `upgrade/` and `db/`, so this is a third mechanism in an established home rather than a new convention.\n- **Regenerable, never authoritative.** It is a fallback. Nothing reads it back; there is no import path; `classify` never sees it.\n- **Cheap enough to be habitual.** If it is expensive nobody runs it, and an assurance nobody exercises is not one.\n\n**AND IT HAS A DEPENDENCY NOBODY HAD NAMED (cc, 2026-08-18, found while bootstrapping a scratch project to drive something else): `intent init` IS NOT IMPLEMENTED.** `error: 'init' is a known command that is not implemented yet`. A v3 project has to be hand-bootstrapped with a v3-stamped `config.json`.\n\n**That is a precondition of D57-5 rather than a neighbouring gap.** The whole value of a fallback is that someone can exercise it -- and the natural way to exercise \"can I get everything back into text\" is _make an empty project, export into it, read it_. **You cannot demonstrate a fallback from a clean directory if you cannot create a clean directory**, so `init` gates the assurance, not merely the onboarding. cc's line is the one to keep: _a text-export assurance nobody can exercise from a clean directory is exactly the assurance nobody exercises._\n\n## D57-6 -- The 165, RULED\n\n**`design.md`, `impl.md` and `tasks.md` are carried as ATTACHMENTS. `THREAD_PROSE` is deleted from the classifier.**\n\nThe inventory this was decided against -- every `.md` under `intent/st` the census never enumerates:\n\n```\ncanonical three (design/impl/tasks)   165 files    702,880 bytes   580 sections\none-off .md under a thread             68 files    809,832 bytes   323 sections\n                                     ----------------------------------------\nTOTAL never enumerated as prose        233 files  1,512,712 bytes   903 sections\n```\n\n**The 68 one-offs were already carried** -- `.md` is in the declared extension set and `classify` does not call them typed. **So the ruling governs exactly the 165, and the change is subtractive: three strings come out of a constant.**\n\nTwo arguments carried it. **Rule 2 of the attachment spec:** a typed field earns its parsing because the model has fields for what comes out. These are freeform prose under arbitrary headings and the model has none, **so parsing them would discard structure into nothing -- which is precisely how `## Related Steel Threads` became 52 rows of `LOST-PROSE`.** And **it is about ongoing behaviour, not legacy data**: `st new` does not create these files, but `THREAD_PROSE` still names them, so the moment anyone hand-writes a `design.md` -- which the v2 habit, the templates, and all 55 existing threads teach -- **it is indexed for search and carried by nothing. The 166th has the same problem as the 165.**\n\n**This document is the demonstration.** Written as `design.md` it was, until this ruling, the one filename in the estate that would not be carried; written as anything else it would have been. **The tool's own default name for a design document was the only name it would not keep.**\n\n## D57-7 -- Where ancillary files live, and the one directory they cannot\n\nText attachments need no home: they live inline in canon as `Attachment.text`, and the working copy dehydrates. **Everything else does need one**, and the estate is mostly everything else.\n\n```\nunder intent/st           787 files\n  md                      490      (of which the generated views dehydrate today)\n  tap                     196      ST0056/parity/tools/tap-baseline, ONE directory\n  json                     57\n  sh                       39\n  txt / tsv                 4\n                        ------\n  NEITHER md NOR json     240\n```\n\n**REJECTED -- `intent/.cache/`.** It is gitignored at `.gitignore:127`, holds exactly one file (`intent.db`, 5.7 MB), and **0 files under it are tracked**. The ignore rule's own comment records what it is for: the database was showing as `?? intent/.cache/`, _\"one `git add -A` from entering history as a binary blob\"_, and the line **is the precondition for D29** -- the ingest corpus excludes ignored paths, so until it existed the DB was outside the corpus only by accident of path shape. Committing that directory reverses both protections at once.\n\n**And the name is the sharper objection, because it fails in the dangerous direction.** `cache` means regenerable-and-discardable. An opaque attachment is precisely what is NOT regenerable. `rm -rf intent/.cache` is a reasonable thing to do to a cache, and under that layout it would destroy the only copy of a hand-authored file. The ignore rule already concedes _\"`intent/.cache/` remains a name that contradicts the model\"_; adding committed content compounds a documented defect rather than repairing one.\n\n**REJECTED -- opaque attachments never dehydrate.** This was the first answer and it was wrong, and **the error was reading D57-3's \"regenerate\" as \"re-render from JSON\"** -- the rule says _a source sitting beside it in the same commit_, and it does not say the source must be JSON. That is the whole reason, and it stands on its own. **THE COST FIGURE THIS PARAGRAPH USED TO CARRY WAS WRONG TWICE AND IS WITHDRAWN RATHER THAN QUIETLY DELETED (cc measured it, vc ruled, 2026-08-19).** It read _\"pins all 240 files to disk permanently -- 196 `.tap` baselines surviving ST0056 forever\"_. **Wrong population: all 196 are valid UTF-8**, so under AC-03.2's own words -- form follows CONTENT -- a `.tap` is a TEXT attachment, and the sidecar mechanism this paragraph rejects cannot reach them under any reading. What actually holds them on disk is `ATTACHMENT_EXTENSIONS` (`md`, `txt`, `sh`): they are not carried at all, so `organize` reports them UNCLAIMED and never removes them. **Wrong cost: those files are pinned to disk by GIT regardless**, because they are committed and tracked. The alternative was never _not on disk_; it was _on disk AND in the store_. **An argument that rejects an option by charging it a cost both options pay is not an argument**, and the rejection was carried by the D57-3 misreading alone the entire time. **The `.tap` baselines' fate is a CARRY-LIST question and belongs to no criterion in WP-03, whose population is _not valid UTF-8_ -- see `project.rs:33`, whose conclusion stands and whose stated reason does not: _\"a tool's committed output is regenerable, so carrying it buys nothing\"_ is true of the mechanism and false of the meaning, since a baseline exists to be the recorded PAST and regenerating one destroys the comparison it exists to make.**\n\n**RULED. Canon holds the truth; the working copy dehydrates like everything else.**\n\n```\nintent/.canon/st/ST0056.json          text attachments, inline as `text`\nintent/.canon/st/ST0056/<path>        opaque attachments, as FILES\nintent/st/ST0056/<path>               realised working copy -- DEHYDRATES\nintent/.cache/                        gitignored, store only, never committed\n```\n\n**One rule, uniformly: everything under `intent/st/` is a realised working copy, and everything dehydrates.** Canon holds truth in whichever form suits it -- inline for text, as a sibling file for bytes. Regeneration of an opaque attachment is a byte copy from a source in the same commit, which satisfies D57-3 exactly rather than bending it.\n\nThree things fall out, and each removes work rather than adding it:\n\n- **No `.intentfiles` grammar change.** Attachments are per-thread, so `STEELTHREAD:ST0056` hydrates the thread AND its files. The manifest still names threads only.\n- **`sha256` in canon buys drift detection on the working copy** -- an attachment edited on disk without the store knowing is 5.1b's divergence case, and `doctor` can now report it by comparison rather than by inference.\n- **The naming rule protects two things with one check.** A file that cannot be named safely can be given neither a canon path nor a URL (D57-8), so rejection at the gate covers storage and addressing together. Rejection is NOT retroactive: existing violators -- the estate already carries a `.webloc` with spaces in its name, which split into eight fragments under `xargs` while the run still printed a plausible total -- are reported by `organize` as UNCLAIMED under its fifth row and never silently removed.\n\n**OPEN, and it is an optimisation rather than a design question:** copy versus hardlink on hydration. Copy is simpler and doubles bytes for hydrated threads only, which for 196 tap files is nothing.\n\n## D57-8 -- `intent://`, the address of a piece of data\n\n**Hydration makes a file path a statement about a moment.** `intent/st/ST0034/design.md` either exists or does not depending on what `organize` last did, so every reference to it is conditional. **Measured at `ce532a97`: 80 citations of `intent/st/ST####/<file>.md` in tracked estate prose, the most-cited single artefact being `ST0034/design.md` at 23 -- a COMPLETED thread, and therefore among the first to dehydrate.** Three AT rows also point inside `intent/`; the other 87 point at `native/rust` code and are unaffected.\n\n**So references must name the ENTITY, never the file.**\n\n```\nintent:///threads/{stid}\nintent:///threads/{stid}/wp/{wpid}\nintent:///threads/{stid}/ac/{acid}\nintent:///threads/{stid}/at/{atid}\nintent:///threads/{stid}/attachments/{path}\nintent:///issues/{issueid}\nintent:///nodes/{moniker}\nintent:///nodes/{moniker}/inbox/{sender}/{stamp}\nintent:///events/{id}\n```\n\n**Empty authority means THIS project.** Nearly every reference is intra-project, and one that hard-codes the project name breaks on rename or fork. Cross-project references carry the slug and resolve against intentd's project registry.\n\n**VIEWS GET NO URL, and this is what stops the scheme becoming a path alias.** A view is derivable from its entity, so a reference to a view is a reference to its source. Giving views addresses would re-create, inside the scheme, the exact conditionality the scheme exists to remove. **`?format=` selects a REPRESENTATION of the addressed entity; a path segment would name a separate thing.**\n\n**READ/WRITE (hv, 2026-08-18). The URI is the address of a piece of data, and the data is mutable.** The write path is **DB first, then canon ALWAYS, then views IF MARKED** -- not \"disk if marked\", which collapses canon into views and would leave a dehydrated artefact inside a gitignored database, absent from a fresh clone. That collapse is the one this document opened by separating, and it is an attractor rather than a slip: the same wording produced it twice, from the same author, a day apart.\n\n- **Create splits two ways.** Caller-assigned ids (`AC-10.11`, `AT-10.11`) are a `PUT` to the entity address. Server-assigned ids (threads, issues, WP seq) are a `POST` to the COLLECTION address, which returns the new address -- you cannot address `ST0058` before the tool has decided it is `ST0058`.\n- **The mutation format IS the interchange format.** `GET ?format=json`, modify, `PUT` the same shape back. **This gives AC-02.6 a second job: a field that does not round-trip is now a field that cannot be WRITTEN**, so the lossless 1-1 mapping stops being only a durability guarantee at the clone boundary and becomes the completeness guarantee for the whole mutation surface.\n- **`GET` accepts `json` and `md`. `PUT` accepts `json` only.** Writing markdown to an address would promote a stale rendering into canon, which 5.1b forbids. **The one exception is not an exception:** an ATTACHMENT is authored on disk, so for attachments the authority runs the other way and text-in is correct. Authorship decides direction, and `Project::classify` is the single answer to what a file is, so the asymmetry is implementable rather than aspirational.\n- **If an entity has more than one rendering, it is UNDER-ADDRESSED.** `/threads/ST0056` returns the cover; `/threads/ST0056/ac` returns the acceptance view. One rendering per address, and no `?view=` stacked on `?format=`.\n- **Hold the format set at exactly `json` and `md` for 3.0.0.** Two formats with a ratified meaning each beats four that drift.\n\n**Resolution lives in `intentsvcs`, which already owns the DB and the files. The CLI calls it in-process; intentd calls THE SAME FUNCTION and serves it over GraphQL.** Neither implements resolution. The failure mode to guard is intentd growing its own resolver because GraphQL wants different shapes -- two resolvers agreeing exactly until one moves, with nothing watching.\n\n**And `?format=md` needs no new renderer.** `views::render_all` already returns `Vec<View { path, content }>` and already has three callers -- `facade.rs:1236` (what `sync` writes), `doctor.rs:737` (skew), `migrate.rs:293`. Serving `View.content` makes **the served markdown byte-match the file `organize` would hydrate TRUE BY CONSTRUCTION rather than by test**, because `View.path` is literally where it would land. `views::skew()` is already the dehydration gate's render-and-compare. One renderer, three jobs.\n\n**REQUIREMENT, not a nicety: no daemon may be required to read your own project.** Because the CLI resolves in-process through the same `intentsvcs`, a fully dehydrated estate stays readable with intentd stopped, uninstalled or never started. **This is an acceptance criterion rather than an implementation accident** -- if reading a dehydrated thread came to need a running daemon, the disk model would have made the estate LESS accessible than a pile of markdown, which inverts D57-5's whole reason for existing. The risk is not that intentd serves content; it is that intentd becomes the only thing that does.\n\n## What must be true before ANY dehydration ships\n\n- **Attachments** -- landed `36bc02c5`. **`THREAD_PROSE` deletion outstanding (D57-6).**\n- **Issue bodies** -- landed, carried verbatim rather than trimmed, so no renderer has to remember to put a byte back.\n- **`ROOT_FILES`** -- `AGENTS.md`, `CLAUDE.md`, `usage-rules.md` have no v3 generator. Out of `.intentfiles` scope, but they are on disk and something emptied `AGENTS.md` on 2026-08-18, so they need their generator before anyone reasons about them as derivable.\n- **The conservation verdict** -- green at full scope over a pinned subject, per `conservation_check.sh`'s header. **Currently `STRANDED 192` and that is the number the deletion rests on.**\n- **The full text realisation (D57-5)** -- because the human fallback is a precondition of sparseness, not a nicety that follows it.\n- **Opaque-attachment canon (D57-7)** -- an opaque attachment cannot be dehydrated until canon holds its bytes as a file, because until then there is no source beside it in the commit and the removal is unrecoverable rather than merely careless.\n- **Addressing (D57-8)** -- 80 tracked citations name artefact FILES that dehydration makes conditional. Shipping sparseness before addressing breaks them with no replacement to migrate to.\n\n**Build `organize` and `edit` now; ship dehydration behind the gate.** Hydration and realisation are additive and safe immediately.\n",
      "bytes": 29499,
      "sha256": "35d0be35a5480385c15dc1f60ca564048cf8f94b27a250760438ff6e35089996"
    },
    {
      "path": "parity/tools/intentfiles_reviewable.sh",
      "text": "#!/bin/bash\n# intentfiles_reviewable.sh -- a realisation change is reviewable: the manifest\n# line that caused it lands in the same working tree.\n#\n# COVERS ST0057 AC-02.4, AND IS CITED BY AT-02.4. Both ids are spelled out\n# because the row and the file each look correct alone and only the PAIR can be\n# wrong -- `intent at lint` refused this file for naming the criterion and not\n# the row, which is the same defect vc hit on AT-03.18.\n#\n# The committed-manifest ruling rests on reviewability:\n# `.intentfiles` is tracked so that when realisation changes, a reader of the\n# diff sees the CAUSE beside the EFFECT. Files appearing or vanishing with no\n# visible reason is the state the ruling exists to prevent -- and it is not a\n# hypothetical, because dehydration removes committed files.\n#\n# THE PROPERTY HOLDS TODAY BY CONSTRUCTION, AND THAT IS EXACTLY WHY THIS TOOL\n# EXISTS. `organize` decides realisation from the manifest's declared set alone\n# and NEVER WRITES THE MANIFEST BACK -- there is no `intentfiles::render` call\n# anywhere in `organize.rs`. So the declared set can only change when a human\n# edits the file, which puts the cause in the working tree by definition.\n# Arm 3 is what notices if that ever stops being true: the moment `organize`\n# gains a generated-region write (which `intentfiles::render` exists to do, and\n# which AC-02.3 is about), realisation can change with nobody having edited\n# anything, and the reviewability question has to be answered again rather than\n# inherited.\n#\n# A tool asserting only arm 2 would pass forever and notice nothing, because it\n# would be testing the human's own edit.\n#\n# EXIT CODES. 0 pass, 1 a real violation, 2 CANNOT MEASURE. The third is not\n# decoration: on this estate `organize --apply` currently changes nothing,\n# because the ship gate refuses every removal while preconditions are unmet, so\n# a run that observed no realisation change and reported success would be the\n# vacuous pass -- right verb, right depth, a population that cannot contain the\n# failure. Every arm prints what it examined.\n#\n# IT NEVER TOUCHES THE LIVE ESTATE. Arm 2 and arm 3 run `organize --apply`,\n# which removes committed files, so they run in a DISPOSABLE CLONE made with\n# `git clone --local`. The live tree is read for arm 1 and nothing else. Four\n# nodes work in this checkout; an instrument that mutates it is a second writer\n# with the worst possible timing.\n\nset -uo pipefail\n\nROOT=\"$(git rev-parse --show-toplevel)\"\nBIN=\"${INTENT_BIN:-$ROOT/native/rust/target/debug/intent}\"\nMANIFEST=\"intent/.intentfiles\"\nrc=0\n\nsay() { printf '%s\\n' \"$*\"; }\nfail() { printf 'FAIL: %s\\n' \"$*\"; rc=1; }\nunmeasured() { printf 'CANNOT MEASURE: %s\\n' \"$*\"; [ \"$rc\" -eq 0 ] && rc=2; }\n\n# The first thread id in canon, by GLOB rather than by parsing `ls`\n# (IN-SH-CODE-002). The ids here cannot contain a space, so `ls` would have\n# worked -- and that is the argument the rule exists to refuse, because the\n# next directory it gets copied into is the one where it does not.\nfirst_thread_id() {\n  local f\n  for f in \"$1\"/intent/.canon/st/*.json; do\n    [ -e \"$f\" ] || return 1\n    basename \"$f\" .json\n    return 0\n  done\n  return 1\n}\n\n# ---------------------------------------------------------------------------\n# Arm 1 -- the manifest is TRACKED. Half the criterion's literal text, and the\n# cheapest thing here: an untracked manifest makes every other arm meaningless,\n# because the cause would never appear in anybody's diff at all.\n# ---------------------------------------------------------------------------\narm1_tracked() {\n  if git -C \"$ROOT\" ls-files --error-unmatch \"$MANIFEST\" >/dev/null 2>&1; then\n    say \"arm1: $MANIFEST is tracked\"\n  else\n    fail \"arm1: $MANIFEST is NOT tracked -- a realisation change can never be reviewed, because the cause is invisible to git\"\n  fi\n}\n\n# A disposable clone at HEAD. `--local` is cheap and `--no-hardlinks` keeps the\n# clone's object churn out of the live repository.\nmake_clone() {\n  local dest=\"$1\"\n  git clone --quiet --local --no-hardlinks \"$ROOT\" \"$dest\" 2>/dev/null || return 1\n  [ -x \"$BIN\" ] || return 1\n  return 0\n}\n\n# Every path git reports as changed, one per line, porcelain-stable.\nchanged_paths() {\n  git -C \"$1\" status --porcelain=v1 -- . | sed 's/^...//'\n}\n\n# ---------------------------------------------------------------------------\n# Arm 2 -- a DECLARED-SET change lands together with its files.\n#\n# Constructed rather than observed, and the estate's own zero is printed below:\n# the live manifest declares nothing and the gate refuses every removal, so\n# there is no realisation change on this estate to watch.\n# ---------------------------------------------------------------------------\narm2_cause_beside_effect() {\n  local work; work=\"$(mktemp -d)\"; trap 'rm -rf \"$work\"' RETURN\n  local clone=\"$work/clone\"\n  if ! make_clone \"$clone\"; then\n    unmeasured \"arm2: could not make a clone or find the binary at $BIN\"\n    return\n  fi\n\n  local victim; victim=\"$(first_thread_id \"$clone\")\" || victim=\"\"\n  if [ -z \"$victim\" ]; then\n    unmeasured \"arm2: the clone carries no threads, so no realisation can change\"\n    return\n  fi\n\n  # **THE DEHYDRATED STATE IS COMMITTED IN THE CLONE, and the first version of\n  # this arm did not do that and measured nothing.** Deleting the file and\n  # letting `organize` put it back is INVISIBLE to `git status`: the file\n  # returns to its HEAD content, so the changed-path set is empty and the arm\n  # reported \"no realisation change\" over a run that had hydrated correctly.\n  # A restore-to-baseline is a real effect with no diff, and this arm's whole\n  # observable is the diff.\n  rm -f \"$clone/intent/st/$victim/info.md\"\n  git -C \"$clone\" -c user.email=t@t -c user.name=t commit --quiet -a -m \"dehydrated\" 2>/dev/null\n\n  # Now the CAUSE: declare it. The file reappearing is the EFFECT, and both\n  # must land in one changed-path set for the diff to be reviewable.\n  printf 'STEELTHREAD:%s  # pinned by intentfiles_reviewable.sh\\n' \"$victim\" \\\n    | cat - \"$clone/$MANIFEST\" > \"$clone/$MANIFEST.new\" && mv \"$clone/$MANIFEST.new\" \"$clone/$MANIFEST\"\n\n  # **THE VERB IS DRIVEN WITH ITS OUTPUT READ, NEVER SUPPRESSED.** The first\n  # version piped it to /dev/null, which is the defect AC-03.15 was minted from\n  # -- a write verb whose refusal nobody saw. Its summary line is reported\n  # below whatever the verdict.\n  local out; out=\"$( cd \"$clone\" && \"$BIN\" organize --apply 2>&1 )\"\n  say \"arm2: organize said -- $(printf '%s\\n' \"$out\" | head -1)\"\n\n  local changed; changed=\"$(changed_paths \"$clone\")\"\n  local n_files; n_files=\"$(printf '%s\\n' \"$changed\" | grep -c \"^intent/st/$victim/\" || true)\"\n  local has_manifest; has_manifest=\"$(printf '%s\\n' \"$changed\" | grep -c \"^$MANIFEST\\$\" || true)\"\n\n  say \"arm2: examined $victim -- $n_files changed path(s) under its directory, manifest in the set: $has_manifest\"\n  if [ \"$n_files\" -eq 0 ]; then\n    unmeasured \"arm2: no realisation change was produced, so nothing was reviewed -- this is not a pass\"\n    return\n  fi\n  if [ \"$has_manifest\" -eq 0 ]; then\n    fail \"arm2: $victim's files changed and $MANIFEST did not -- the effect is in the diff and the cause is not\"\n  fi\n}\n\n# ---------------------------------------------------------------------------\n# Arm 3 -- THE ONE THAT WILL EVER FIRE. If `organize` changes the realised set\n# WITHOUT the manifest changing, the property is broken for a reason no human\n# edit explains.\n#\n# It runs organize with the manifest UNTOUCHED. Today the declared set cannot\n# move on its own, so the realised set must not move either. If a future\n# `organize` writes its generated region from status, this arm forces the\n# manifest into the same changed set rather than letting realisation drift\n# silently.\n# ---------------------------------------------------------------------------\narm3_nothing_moves_realisation_but_the_manifest() {\n  local work; work=\"$(mktemp -d)\"; trap 'rm -rf \"$work\"' RETURN\n  local clone=\"$work/clone\"\n  if ! make_clone \"$clone\"; then\n    unmeasured \"arm3: could not make a clone or find the binary at $BIN\"\n    return\n  fi\n\n  local before; before=\"$(find \"$clone/intent/st\" -name '*.md' | wc -l | tr -d ' ')\"\n  local out; out=\"$( cd \"$clone\" && \"$BIN\" organize --apply 2>&1 )\"\n  say \"arm3: organize said -- $(printf '%s\\n' \"$out\" | head -1)\"\n  local after; after=\"$(find \"$clone/intent/st\" -name '*.md' | wc -l | tr -d ' ')\"\n\n  local changed; changed=\"$(changed_paths \"$clone\")\"\n  local has_manifest; has_manifest=\"$(printf '%s\\n' \"$changed\" | grep -c \"^$MANIFEST\\$\" || true)\"\n\n  say \"arm3: realised views $before -> $after with the manifest untouched, manifest in the changed set: $has_manifest\"\n  if [ \"$before\" -ne \"$after\" ] && [ \"$has_manifest\" -eq 0 ]; then\n    fail \"arm3: organize moved realisation from $before to $after and $MANIFEST did not change -- a reader of this diff sees files appear or vanish with nothing naming the cause\"\n  fi\n  if [ \"$before\" -eq \"$after\" ]; then\n    say \"arm3: realisation did not move, which is the CORRECT answer while the declared set can only change by hand -- recorded, not counted as evidence of the guard firing\"\n  fi\n}\n\n# ---------------------------------------------------------------------------\n# SELF-TEST -- the arms are driven against worlds that VIOLATE the criterion,\n# and each must FAIL on its own.\n#\n# **A clean sweep is unbankable until it has been run against known positives.**\n# Every arm here reports PASS on a healthy estate, and that is exactly the\n# reading a broken arm produces. The two violations below are constructible;\n# arm 3's is not, and that is stated rather than skipped.\n# ---------------------------------------------------------------------------\nselftest() {\n  local work; work=\"$(mktemp -d)\"; trap 'rm -rf \"$work\"' RETURN\n  local failures=0 checked=0\n\n  # Positive control 1: the manifest is UNTRACKED. Arm 1 must fail.\n  local c1=\"$work/untracked\"\n  if make_clone \"$c1\"; then\n    git -C \"$c1\" rm --quiet --cached \"$MANIFEST\" >/dev/null 2>&1\n    checked=$((checked + 1))\n    if ( ROOT=\"$c1\"; rc=0; arm1_tracked >/dev/null 2>&1; [ \"$rc\" -eq 1 ] ); then\n      say \"selftest: arm1 FAILS on an untracked manifest -- correct\"\n    else\n      say \"selftest: ARM 1 DID NOT FIRE on an untracked manifest\"\n      failures=$((failures + 1))\n    fi\n  fi\n\n  # Positive control 2: files change and the manifest does NOT. Arm 2's guard\n  # is the `has_manifest` test, driven here directly against a world where the\n  # effect is present and the cause is absent.\n  local c2=\"$work/nocause\"\n  if make_clone \"$c2\"; then\n    local v; v=\"$(first_thread_id \"$c2\")\" || v=\"\"\n    printf '\\nrealisation changed with nobody saying so\\n' >> \"$c2/intent/st/$v/info.md\"\n    checked=$((checked + 1))\n    local changed; changed=\"$(changed_paths \"$c2\")\"\n    local n; n=\"$(printf '%s\\n' \"$changed\" | grep -c \"^intent/st/$v/\" || true)\"\n    local m; m=\"$(printf '%s\\n' \"$changed\" | grep -c \"^$MANIFEST\\$\" || true)\"\n    if [ \"$n\" -gt 0 ] && [ \"$m\" -eq 0 ]; then\n      say \"selftest: the effect-without-cause world is constructible and arm2's guard reads it -- $n file(s) changed, manifest in the set: $m\"\n    else\n      say \"selftest: COULD NOT CONSTRUCT the effect-without-cause world (n=$n m=$m)\"\n      failures=$((failures + 1))\n    fi\n  fi\n\n  # **ARM 3 HAS NO CONSTRUCTIBLE POSITIVE CONTROL AND IT IS SAID RATHER THAN\n  # OMITTED.** Its violating world needs an `organize` that moves realisation\n  # from something other than the declared set, and no such build exists --\n  # which is the same fact that makes arm 3 pass today. So arm 3 is UNPROVEN by\n  # this self-test, not proven by its silence, and the day somebody teaches\n  # `organize` to write its generated region is the day this becomes testable\n  # and must be revisited.\n  say \"selftest: arm3 has NO constructible positive control -- unproven, not passing\"\n\n  say \"selftest: $checked control(s) driven, $failures did not fire\"\n  [ \"$failures\" -eq 0 ]\n}\n\nif [ \"${1:-}\" = \"--selftest\" ]; then\n  if selftest; then say \"intentfiles_reviewable: SELFTEST PASS\"; exit 0; fi\n  say \"intentfiles_reviewable: SELFTEST FAIL\"; exit 1\nfi\n\narm1_tracked\narm2_cause_beside_effect\narm3_nothing_moves_realisation_but_the_manifest\n\ncase \"$rc\" in\n  0) say \"intentfiles_reviewable: PASS\" ;;\n  1) say \"intentfiles_reviewable: FAIL\" ;;\n  2) say \"intentfiles_reviewable: CANNOT MEASURE -- not a pass\" ;;\nesac\nexit \"$rc\"\n",
      "bytes": 12359,
      "sha256": "5d6e0ecd1af0c90da020f556fd1c667ce872431d98998166d48baab37c0515e9"
    },
    {
      "path": "parity/tools/no_daemon_required.sh",
      "text": "#!/usr/bin/env bash\n# no_daemon_required.sh -- AT-07.5 / AC-07.5.\n#\n# **NO DAEMON IS REQUIRED TO READ YOUR OWN PROJECT.** With intentd stopped,\n# uninstalled and never started, every address in a FULLY DEHYDRATED estate\n# resolves through the CLI.\n#\n# WHY THE CRITERION IS NOT WITHDRAWABLE ON hv's GROUNDS, checked rather than\n# assumed. hv withdrew AC-00.3 because \"if we need safety, we've got all the\n# historical STs etc in git\" -- any precondition PROVING REVERSIBILITY proves\n# against a loss git already prevents. Applied here it does not bite, and the\n# reason is sharper than \"this one is different\": **restoring the estate from\n# git RE-HYDRATES it, which destroys the precondition this row tests.** Git is\n# not a fallback for a dehydrated-estate property; reaching for it falsifies\n# the subject. AC-00.3 was a safety proof, this is an ACCESSIBILITY claim, and\n# git substitutes for the first and cannot substitute for the second.\n#\n# ==========================================================================\n# THE ARM THAT MATTERS IS ARM B, AND SAYING SO IS THE WHOLE POINT\n# ==========================================================================\n#\n# `intent daemon` answers `is a known command that is not implemented yet` in\n# this build. **So arm A -- no intentd process, and every read still answers --\n# is green because the daemon DOES NOT EXIST, not because the read path is\n# daemon-free.** That is a clean-by-luck green, and it would keep printing the\n# same reassuring line on the day reads start routing through a daemon.\n#\n# So arm B asserts the STRUCTURAL property instead: `intent-cli` declares no\n# dependency on `intentd`, and its sources open no socket. **Arm B is the one\n# that survives intentd shipping**; arm A is the behavioural witness that the\n# estate really was empty when the reads answered. Both run, both are reported,\n# and the weakness of arm A is printed on every run rather than left for a\n# reader to work out.\n#\n# EXIT CODES. 0 = the property holds. 1 = it does not. **2 = CANNOT MEASURE**,\n# which is never a pass: no binary, no scratch, a daemon already running, or an\n# estate that refused to dehydrate. An unmeasurable run that exits 0 is the\n# failure this file exists to avoid.\n#\n# --selftest drives POSITIVE CONTROLS -- it proves each arm can fail -- because\n# an all-green checker and a checker that cannot fail print the same thing.\n\nset -uo pipefail\n\nHERE=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\nREPO_ROOT=\"$(cd \"$HERE/../../../../..\" && pwd)\"\n\nBIN=\"${BIN:-$REPO_ROOT/native/rust/target/release/intent}\"\nCLI_MANIFEST=\"${CLI_MANIFEST:-$REPO_ROOT/native/rust/crates/intent-cli/Cargo.toml}\"\nCLI_SRC=\"${CLI_SRC:-$REPO_ROOT/native/rust/crates/intent-cli/src}\"\n\nRC=0\nsay()  { printf '%s\\n' \"$*\"; }\nfail() { printf 'FAIL: %s\\n' \"$*\"; RC=1; }\ndie()  { printf 'error: %s\\n' \"$*\" >&2; exit 2; }\n\ncommand -v jq >/dev/null 2>&1 || die \"jq is required and was not found on PATH\"\n\n# ---------------------------------------------------------------------------\n# ARM B -- STRUCTURAL. The CLI cannot reach a daemon even if one is running.\n#\n# Checked against the manifest and the sources rather than by running anything,\n# because the question is what the binary is ABLE to do, not what it did today.\n# ---------------------------------------------------------------------------\narm_b_structural() {\n  say \"== arm B (structural): the CLI has no way to reach a daemon ==\"\n  [ -f \"$CLI_MANIFEST\" ] || { die \"no intent-cli manifest at $CLI_MANIFEST\"; }\n  [ -d \"$CLI_SRC\" ]      || { die \"no intent-cli sources at $CLI_SRC\"; }\n\n  # A dependency on intentd. Comments and the package DESCRIPTION are excluded:\n  # the description says \"in-process or GraphQL to intentd\" and is prose about\n  # a designed future, not a dependency edge.\n  local dep\n  dep=\"$(sed -n '/^\\[.*dependencies.*\\]/,/^\\[/p' \"$CLI_MANIFEST\" | grep -c '^[[:space:]]*intentd' || true)\"\n  if [ \"$dep\" -ne 0 ]; then\n    fail \"arm B: intent-cli declares a dependency on intentd ($dep line(s))\"\n  else\n    say \"  ok: intent-cli declares no intentd dependency\"\n  fi\n\n  # A socket or network client in the sources. Comments stripped first, so a\n  # doc comment DESCRIBING the daemon does not read as a call to it.\n  local sock\n  sock=\"$(cat \"$CLI_SRC\"/*.rs \\\n    | sed 's|//.*$||' \\\n    | grep -cE 'UnixStream|UnixDatagram|TcpStream|reqwest|hyper::client' || true)\"\n  if [ \"$sock\" -ne 0 ]; then\n    fail \"arm B: intent-cli sources open $sock socket/network client(s) -- a read may leave this process\"\n  else\n    say \"  ok: no socket or network client in intent-cli sources\"\n  fi\n}\n\n# ---------------------------------------------------------------------------\n# ARM A -- BEHAVIOURAL. A fully dehydrated estate answers every read.\n#\n# THE TWO CONTROLS ARE THE ARM. Without them a green means nothing:\n#   1. the estate must actually BE empty (else the reads are served by files);\n#   2. no intentd may run BEFORE or AFTER (else a read may have been served by\n#      one, or may have started one).\n# ---------------------------------------------------------------------------\narm_a_behavioural() {\n  say \"== arm A (behavioural): a dehydrated estate answers every read ==\"\n  [ -x \"$BIN\" ] || die \"no runnable binary at $BIN -- build it with \\`bin/int build cli\\`\"\n\n  if pgrep -f 'intentd' >/dev/null 2>&1; then\n    die \"an intentd process is already running -- this arm cannot distinguish a daemon-free read from a daemon-served one while it is up\"\n  fi\n\n  local work; work=\"$(mktemp -d)\" || die \"could not create a scratch directory\"\n  trap 'rm -rf \"$work\"' RETURN\n  case \"$(cd \"$work\" && pwd -P)\" in\n    \"$REPO_ROOT\"*) die \"the scratch directory resolves inside this project -- refusing to dehydrate in a live tree\" ;;\n  esac\n\n  local p=\"$work/p\"\n  mkdir -p \"$p/intent/.canon/st\" \"$p/intent/.config\" || die \"could not lay out the scratch project\"\n  cat > \"$p/intent/.config/config.json\" <<'J'\n{\"intent_version\":\"3.0.0\",\"project_name\":\"AT0705\",\"author\":\"ic\",\"intent_dir\":\"intent\",\"languages\":[\"rust\"],\"plugins\":{\"claude\":{}}}\nJ\n\n  # Two ordinary threads to read back, and one that OPENS THE GATE.\n  #\n  # **A PROJECT DECLARING NO PRECONDITIONS CANNOT DEHYDRATE AT ALL** -- measured,\n  # not assumed: `organize --apply` refuses with \"0 checked of 0 declared, so\n  # nothing is proved and nothing may be removed\". The gate is fail-closed and\n  # absence of a declaration is not permission. So the fixture must DECLARE a\n  # precondition and SATISFY it; a fixture that simply omitted the block would\n  # make this arm unmeasurable while looking like a setup detail.\n  local i\n  for i in 1 2; do\n    jq -n --arg id \"ST000$i\" '{schema:\"intent/thread@3.0\", id:$id, title:(\"Readable \" + $id),\n      status:\"wip\", created:\"2026-08-19\", objective:(\"OBJECTIVEMARKER for \" + $id),\n      context:\"\", body:\"\", preamble:\"\", attachments:[]}' \\\n      > \"$p/intent/.canon/st/ST000$i.json\" || die \"could not write fixture canon\"\n  done\n  jq -n '{schema:\"intent/thread@3.0\", id:\"ST0057\", title:\"Gate\", status:\"wip\",\n    created:\"2026-08-19\", objective:\"opens the dehydration gate\", context:\"\", body:\"\",\n    preamble:\"\", attachments:[],\n    criteria:[\n      {id:\"AC-00.1\", text:\"the declaring criterion <<PRECONDITIONS AC-00.9 PRECONDITIONS>>\",\n       kind:\"non-test\", state:{is:\"unsatisfied\"}},\n      {id:\"AC-00.9\", text:\"met, so the gate is open and this arm can reach a dehydrated estate\",\n       kind:\"non-test\", state:{is:\"satisfied\", evidence:\"fixture: the gate is not the subject of AT-07.5\"}}\n    ]}' > \"$p/intent/.canon/st/ST0057.json\" || die \"could not write the gate thread\"\n\n  ( cd \"$p\" && HOME=\"$work\" \"$BIN\" sync --to-store >/dev/null 2>&1 ) || die \"sync --to-store failed on the fixture\"\n  ( cd \"$p\" && HOME=\"$work\" \"$BIN\" sync --to-disk  >/dev/null 2>&1 ) || die \"sync --to-disk failed on the fixture\"\n\n  local before; before=\"$(find \"$p/intent/st\" -type f 2>/dev/null | wc -l | tr -d ' ')\"\n  [ \"$before\" -ge 3 ] || die \"the fixture realised only $before file(s); there is nothing to dehydrate and this arm would pass vacuously\"\n\n  # DEHYDRATE EVERYTHING: an empty manifest declares nothing realised.\n  : > \"$p/intent/.intentfiles\"\n  local out; out=\"$( cd \"$p\" && HOME=\"$work\" \"$BIN\" organize --apply 2>&1 )\"\n  say \"  organize said -- $(printf '%s\\n' \"$out\" | head -1)\"\n\n  # CONTROL ONE: the estate must actually be empty of artefact files.\n  local left; left=\"$(find \"$p/intent/st\" -mindepth 2 -type f 2>/dev/null | wc -l | tr -d ' ')\"\n  if [ \"$left\" -ne 0 ]; then\n    die \"the estate did not dehydrate -- $left artefact file(s) remain of $before; a read answering now proves nothing\"\n  fi\n  say \"  ok: estate fully dehydrated, $before file(s) -> 0 artefact file(s)\"\n\n  # THE READS. Each must exit 0 AND say something -- an empty rc=0 is not a\n  # resolution, and `at list` on a thread with no tests legitimately prints\n  # nothing, which is why the roster below names only reads with content.\n  local probe rc answer n_ok=0 n_run=0\n  while IFS='|' read -r probe expect; do\n    [ -n \"$probe\" ] || continue\n    n_run=$((n_run + 1))\n    # **`$probe` IS DELIBERATELY UNQUOTED and this file's shebang is what makes\n    # that correct.** Under bash an unquoted expansion word-splits, so `st show\n    # ST0001` reaches the binary as three arguments. Under ZSH it would not --\n    # SH_WORD_SPLIT is off by default -- and the whole roster would arrive as\n    # one subcommand, which answers `unrecognized subcommand 'st show ST0001'`\n    # at rc=1 and reads exactly like the failure this arm exists to detect.\n    # Measured while building this file, in a zsh probe loop, against a green\n    # estate. Do not \"fix\" the quoting, and do not run this under zsh.\n    # shellcheck disable=SC2086\n    answer=\"$( cd \"$p\" && HOME=\"$work\" \"$BIN\" $probe 2>&1 )\"; rc=$?\n    if [ \"$rc\" -ne 0 ]; then\n      fail \"arm A: \\`$probe\\` exited $rc from a dehydrated estate -- $(printf '%s' \"$answer\" | head -1)\"\n    elif ! printf '%s' \"$answer\" | grep -q \"$expect\"; then\n      fail \"arm A: \\`$probe\\` exited 0 but did not resolve -- expected /$expect/, said: $(printf '%s' \"$answer\" | head -1)\"\n    else\n      n_ok=$((n_ok + 1))\n    fi\n  done <<ROSTER\nst show ST0001|Readable ST0001\nst list|ST0001\nac list ST0057|AC-00.1\ninfo|Intent\ntodo|TODO\nexport --format json|ST0001\nsearch OBJECTIVEMARKER|ST000\nROSTER\n  say \"  reads resolved: $n_ok of $n_run\"\n\n  # **THE SHARPEST WITNESS: CONTENT WITH NO FILE UNDER IT.**\n  #\n  # The first version of this probe asked `search` for a hit and asserted the\n  # path it cited was gone. **It was wrong, and the way it was wrong is worth\n  # keeping.** Search cited `intent/.canon/st/ST0001.json` -- which exists, and\n  # SHOULD: canon is the committed extract and dehydration never touches it.\n  # The probe had quietly assumed search cites VIEWS, which it does only when a\n  # thread carries prose sections; this fixture has none, so it fell through to\n  # canon and the arm reported a failure of the tool rather than of itself.\n  #\n  # So the witness is direct instead. `st show` must print a thread's OBJECTIVE\n  # -- a string that lives in the store and in canon and NOT in any file the\n  # reader could have opened -- while the view that would ordinarily carry it is\n  # provably absent. A read serving bytes whose file does not exist cannot have\n  # been served by the filesystem, and that is the whole claim.\n  # `st show` was the second wrong guess and is recorded rather than quietly\n  # swapped out: it prints a SUMMARY -- id, title, status, created -- and the\n  # objective is not in it. The objective lives in the info VIEW, which is the\n  # file this arm has just removed, so the read that must carry it is one that\n  # serves the store's contents rather than a header. `export --format json`\n  # is that read.\n  local marker=\"OBJECTIVEMARKER\"\n  local dumped; dumped=\"$( cd \"$p\" && HOME=\"$work\" \"$BIN\" export --format json 2>&1 )\"\n  local view=\"$p/intent/st/ST0001/info.md\"\n  if [ -e \"$view\" ]; then\n    fail \"arm A: $view still exists, so the export may have been served by a file\"\n  elif ! printf '%s' \"$dumped\" | grep -q \"$marker\"; then\n    fail \"arm A: export did not serve the objective with no file under it -- $(printf '%s' \"$dumped\" | head -1)\"\n  else\n    say \"  ok: export served \\`$marker\\` while intent/st/ST0001/info.md does not exist\"\n  fi\n\n  # CONTROL TWO: no daemon before (checked above) and none after.\n  if pgrep -f 'intentd' >/dev/null 2>&1; then\n    fail \"arm A: an intentd process is running AFTER the reads -- something started one\"\n  else\n    say \"  ok: no intentd process before or after\"\n  fi\n}\n\n# ---------------------------------------------------------------------------\n# SELFTEST -- positive controls. Each arm is driven against an input that MUST\n# fail it. An arm that stays green here is an arm that cannot fail at all.\n# ---------------------------------------------------------------------------\nselftest() {\n  local w; w=\"$(mktemp -d)\"; trap 'rm -rf \"$w\"' RETURN\n  local bad=0\n\n  say \"== selftest: each arm must be able to fail ==\"\n\n  # B1: a manifest that DOES depend on intentd.\n  mkdir -p \"$w/src\"\n  printf '[package]\\nname = \"x\"\\n\\n[dependencies]\\nintentd = { path = \"../intentd\" }\\n' > \"$w/Cargo.toml\"\n  printf 'fn main() {}\\n' > \"$w/src/main.rs\"\n  if ( RC=0; CLI_MANIFEST=\"$w/Cargo.toml\" CLI_SRC=\"$w/src\" arm_b_structural >/dev/null 2>&1; [ \"$RC\" -ne 0 ] ); then\n    say \"  ok: B1 (declared intentd dependency) is caught\"\n  else\n    say \"  BROKEN: B1 was not caught -- arm B cannot see a dependency\"; bad=1\n  fi\n\n  # B2: sources that open a socket.\n  printf 'use std::os::unix::net::UnixStream;\\nfn main() { let _ = UnixStream::connect(\"/tmp/x\"); }\\n' > \"$w/src/main.rs\"\n  printf '[package]\\nname = \"x\"\\n' > \"$w/Cargo.toml\"\n  if ( RC=0; CLI_MANIFEST=\"$w/Cargo.toml\" CLI_SRC=\"$w/src\" arm_b_structural >/dev/null 2>&1; [ \"$RC\" -ne 0 ] ); then\n    say \"  ok: B2 (socket in sources) is caught\"\n  else\n    say \"  BROKEN: B2 was not caught -- arm B cannot see a socket\"; bad=1\n  fi\n\n  # B3: THE NEGATIVE CONTROL FOR THE COMMENT-STRIPPER. A doc comment mentioning\n  # the daemon must NOT fail the arm -- otherwise arm B is a grep for the word\n  # and every honest comment about intentd becomes a finding.\n  printf '// this CLI could speak to intentd over a UnixStream one day\\nfn main() {}\\n' > \"$w/src/main.rs\"\n  if ( RC=0; CLI_MANIFEST=\"$w/Cargo.toml\" CLI_SRC=\"$w/src\" arm_b_structural >/dev/null 2>&1; [ \"$RC\" -eq 0 ] ); then\n    say \"  ok: B3 (the same words inside a COMMENT) is correctly not a finding\"\n  else\n    say \"  BROKEN: B3 was caught -- arm B is a word grep, not a dependency check\"; bad=1\n  fi\n\n  [ \"$bad\" -eq 0 ] || { printf 'selftest: at least one arm cannot do its job\\n' >&2; exit 2; }\n  say \"selftest: every arm fails on its control and passes on the negative one\"\n}\n\ncase \"${1:-}\" in\n  --selftest) selftest; exit 0 ;;\n  \"\") ;;\n  *) die \"unknown argument '$1' -- this tool takes no arguments, or --selftest\" ;;\nesac\n\narm_b_structural\narm_a_behavioural\n\nsay \"\"\nif [ \"$RC\" -eq 0 ]; then\n  say \"AC-07.5 HOLDS: a fully dehydrated estate resolves every read with no daemon.\"\n  say \"  ARM A IS WEAK EVIDENCE TODAY AND THIS LINE IS PART OF THE RESULT: \\`intent daemon\\`\"\n  say \"  answers \\\"is a known command that is not implemented yet\\\" in this build, so no\"\n  say \"  daemon could have served a read whether or not the read path would let it.\"\n  say \"  **ARM B IS THE ONE THAT SURVIVES intentd SHIPPING.**\"\nelse\n  say \"AC-07.5 DOES NOT HOLD on this tree.\"\nfi\nexit \"$RC\"\n",
      "bytes": 15448,
      "sha256": "77ef7ceade2e6c41920393ed80105c1e8f54e595b52e1c802f231c6ff7e297a5"
    },
    {
      "path": "parity/tools/canon_clone_completeness.sh",
      "text": "#!/bin/bash\n# canon_clone_completeness.sh -- a fresh clone carries canon for every artefact.\n#\n# ST0057 AT-01.2, covering AC-01.2 (D29). **CHECKED BY CLONING, NEVER BY READING\n# `.gitignore`**, and the criterion says so in those words: the question is what\n# git DOES, not what a rule appears to say. Ignore syntax has negations,\n# directory suffixes, anchoring and precedence, and a hand-rolled reading of it\n# would disagree with git exactly where it matters.\n#\n# WHAT MAKES A CLONE COMPLETE. `intent/.canon/` is the layer that TRAVELS. The\n# store is gitignored, so an artefact whose canon never entered git exists only\n# inside a database on one machine -- absent from a fresh clone and destroyed by\n# `rm -rf intent/.cache`. That single property is what makes every deletion in\n# this thread safe rather than careful, and it is the one measured here.\n#\n# THE POPULATION IS THE MODEL AND NOT THE CANON FILES, WHICH IS THE WHOLE\n# DIFFERENCE BETWEEN THIS AND A TAUTOLOGY. Enumerating `intent/.canon/*.json`\n# and checking those files reach the clone asks whether TRACKED FILES CLONE,\n# which is true by construction and would pass on an estate that had lost half\n# its artefacts. The artefacts are read from the STORE; the canon is looked for\n# in the CLONE. Those are two independent derivations, so they can disagree, and\n# an artefact the model holds whose canon never travelled is exactly the\n# disagreement D29 exists to prevent.\n#\n# AN EMPTY POPULATION IS A REFUSAL, NOT A PASS. Zero artefacts and a complete\n# estate produce the same \"nothing missing\", so the denominator is printed on\n# every run and a zero denominator exits 2.\n#\n# WHAT THIS DOES NOT COVER, STATED BECAUSE THE GAP IS THE REASON AC-01.5 EXISTS\n# SEPARATELY. This checks the STATE, at one revision, by cloning. Git ignore\n# rules do not untrack a file that is already tracked, so adding\n# `intent/.gitignore` entries for canon would NOT break this check on the\n# already-committed estate -- it would only stop the NEXT artefact travelling.\n# The edit is refused by `canon-ignore-guard.sh` (AC-01.5); the state is checked\n# here. **Neither one covers the other and the gap between the two moments is\n# where the class lives.**\n#\n# Exit codes: 0 every artefact has canon in the clone; 1 one or more do not;\n# 2 the question could not be asked.\n\nset -uo pipefail\n\nHERE=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\n# tools -> parity -> ST0057 -> st -> intent -> repo root. FIVE.\nROOT=\"$(cd \"$HERE/../../../../..\" && pwd)\"\n\ndie() { echo \"canon-clone: $*\" >&2; exit 2; }\n\nBIN=\"$ROOT/native/rust/target/release/intent\"\n[ -x \"$BIN\" ] || die \"no v3 binary at $BIN -- the artefact population is read from the store and cannot be derived without it. NOT a pass: the question was not asked.\"\ngit -C \"$ROOT\" rev-parse --git-dir >/dev/null 2>&1 || die \"$ROOT is not a git repository, so there is nothing to clone and no clone to inspect.\"\n\n# ---------------------------------------------------------------------------\n# 1. The population, from the MODEL.\n# ---------------------------------------------------------------------------\nTHREADS=\"$(\"$BIN\" st list --status all 2>/dev/null | sed -n 's/^\\(ST[0-9][0-9]*\\) .*/\\1/p' | sort -u)\"\nISSUES=\"$(\"$BIN\" issues list --kind all 2>/dev/null | sed -n 's/^\\([0-9][0-9][0-9][0-9]\\) .*/\\1/p' | sort -u)\"\n\nn_threads=\"$(printf '%s' \"$THREADS\" | grep -c . )\"; n_threads=\"${n_threads:-0}\"\nn_issues=\"$(printf '%s' \"$ISSUES\" | grep -c . )\";   n_issues=\"${n_issues:-0}\"\ntotal=$(( n_threads + n_issues ))\n[ \"$total\" -gt 0 ] || die \"the store reports 0 artefacts. An empty population and a complete estate both report nothing missing, so this is a refusal and not a pass.\"\n\n# ---------------------------------------------------------------------------\n# 2. The clone. This is the measurement, not a convenience.\n# ---------------------------------------------------------------------------\nTMP=\"$(mktemp -d \"${TMPDIR:-/tmp}/canon-clone.XXXXXX\")\" || die \"could not create a temporary directory\"\ntrap 'rm -rf \"$TMP\"' EXIT\nCLONE=\"$TMP/clone\"\ngit clone --quiet --no-hardlinks \"$ROOT\" \"$CLONE\" 2>/dev/null \\\n  || die \"git clone of $ROOT failed -- the question could not be asked, which is not the same as the answer being yes.\"\n\n# ---------------------------------------------------------------------------\n# 3. Every artefact, looked for in the CLONE.\n# ---------------------------------------------------------------------------\nMISSING=\"\"\npresent=0\ncheck_one() {  # $1 = artefact label, $2 = path relative to the clone\n  if [ -f \"$CLONE/$2\" ]; then\n    present=$(( present + 1 ))\n  else\n    # WHY IT IS ABSENT CHANGES THE REMEDY, so the two are told apart rather than\n    # both reported as \"missing\" and left for the reader to work out.\n    local why=\"canon is absent from the SOURCE tree as well -- the model holds an artefact nothing has ever written canon for\"\n    if [ -f \"$ROOT/$2\" ]; then\n      why=\"canon EXISTS in the source worktree and did NOT travel -- it is untracked, so it is either ignored (D29's failure) or simply never committed\"\n    fi\n    MISSING=\"${MISSING}${1}  ${2}\n    ${why}\n\"\n  fi\n}\n\nwhile IFS= read -r id; do\n  [ -n \"$id\" ] && check_one \"$id\" \"intent/.canon/st/${id}.json\"\ndone <<< \"$THREADS\"\nwhile IFS= read -r n; do\n  [ -n \"$n\" ] && check_one \"issue ${n}\" \"intent/.canon/issues/${n}.json\"\ndone <<< \"$ISSUES\"\n\nn_missing=$(( total - present ))\n\nif [ \"$n_missing\" -eq 0 ]; then\n  echo \"canon-clone: $present of $total artefact(s) have canon in a fresh clone ($n_threads thread(s), $n_issues issue(s)). A clone is complete.\"\n  exit 0\nfi\n\necho \"canon-clone: $present of $total artefact(s) have canon in a fresh clone -- $n_missing DO NOT:\" >&2\nprintf '%s' \"$MISSING\" | sed 's/^/  /' >&2\necho \"  D29: a gitignored path is never canon. An artefact whose canon does not travel exists only\" >&2\necho \"  inside a store that is gitignored and per-machine -- absent from a fresh clone, and destroyed\" >&2\necho \"  by \\`rm -rf intent/.cache\\`. Commit the canon, or stop the rule that is keeping it out.\" >&2\nexit 1\n",
      "bytes": 6029,
      "sha256": "4d78b89516dd5ededb3659352807ba4d7a31e53f6099c91a393dc538814eb444"
    },
    {
      "path": "parity/tools/canon_concurrent_diff.sh",
      "text": "#!/bin/bash\n# canon_concurrent_diff.sh -- two threads edited produce diffs in two distinct files.\n#\n# ST0057 AT-01.4, covering AC-01.4. **CHECKED BY EDITING TWO THREADS AND\n# OBSERVING THE CHANGED-PATH SET, NOT BY INSPECTING THE LAYOUT**, and the\n# criterion says so: a directory listing proves where files SIT, and this\n# property is about where a WRITE LANDS. Those come apart the moment a write\n# path spells a canon location independently of the resolver, which has already\n# happened once in this estate (`export.rs:386`, closed by AC-01.6).\n#\n# WHY THE PROPERTY EXISTS. D57-1 option B was a single consolidated\n# `threads.jsonl`. It was rejected because FOUR CONCURRENT WRITERS make one\n# canon file a merge-conflict generator, and this estate has four nodes. Option\n# C -- one file per artefact -- is what lets two nodes edit two threads and\n# never collide. **This is the measurement that would have caught option B**,\n# and it is worth having precisely because the layout looks obviously fine to\n# anyone who lists the directory.\n#\n# IT BUILDS ITS OWN RIG AND NEVER TOUCHES THE REAL ESTATE. The measurement\n# requires WRITING to two threads; doing that in the working tree would mutate\n# canon four nodes are reading, to answer a question about layout. So it clones,\n# supplies the two gitignored things a clone lacks (the binary and the store),\n# edits there, measures there, and deletes the lot. A command whose blast radius\n# exceeds what it was aimed at has already cost this estate an uncommitted\n# prototype.\n#\n# THE EDITS GO THROUGH THE CLI, NOT THROUGH A TEXT EDITOR, and that is the whole\n# point of the arrangement. Hand-editing two canon files and observing two\n# changed paths would prove that two files exist. Driving `st hold` proves that\n# the WRITE PATH, resolving canon for itself, puts two threads in two places.\n#\n# WHAT THE ASSERTION IS, STATED EXACTLY BECAUSE A BARE COUNT WOULD NOT DO.\n# Cardinality alone is satisfiable by two edits landing in one shared file plus\n# one unrelated file also changing. So the check requires: exactly two changed\n# canon paths, they are DISTINCT, and each one NAMES ITS OWN thread.\n#\n# WHAT IT CANNOT DO, SAID RATHER THAN IMPLIED. The failing case -- two edits, ONE\n# changed path -- is only producible by reintroducing option B, so there is no\n# red-first arm that plants the real defect without building the rejected design\n# to do it. The assertion is therefore driven by removing an edit: one edit\n# yields one path and the check fails, which proves it is genuinely counting\n# rather than always returning true. That is a weaker control than a planted\n# option-B canon and this comment is where that is recorded.\n#\n# Exit codes: 0 the property holds; 1 it does not; 2 the question could not be asked.\n\nset -uo pipefail\n\nHERE=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\n# tools -> parity -> ST0057 -> st -> intent -> repo root. FIVE.\nROOT=\"$(cd \"$HERE/../../../../..\" && pwd)\"\n\ndie() { echo \"canon-concurrent: $*\" >&2; exit 2; }\n\nEDITS=2\n[ \"${1:-}\" = \"--one-edit\" ] && EDITS=1   # drives the assertion; see the note above\n\nBIN=\"$ROOT/native/rust/target/release/intent\"\n[ -x \"$BIN\" ] || die \"no v3 binary at $BIN -- the edits are made through the CLI and cannot be made without it. NOT a pass: the question was not asked.\"\ngit -C \"$ROOT\" rev-parse --git-dir >/dev/null 2>&1 || die \"$ROOT is not a git repository\"\n\nTMP=\"$(mktemp -d \"${TMPDIR:-/tmp}/canon-concurrent.XXXXXX\")\" || die \"could not create a temporary directory\"\ntrap 'rm -rf \"$TMP\"' EXIT\nRIG=\"$TMP/rig\"\ngit clone --quiet --no-hardlinks \"$ROOT\" \"$RIG\" 2>/dev/null || die \"git clone of $ROOT failed\"\n# A clone carries committed content only, so the two gitignored things it needs\n# are supplied by hand. Both are DERIVED, not authored -- copying them in is\n# reconstituting the rig, not importing state that could change the answer.\nmkdir -p \"$RIG/native/rust/target/release\" \"$RIG/intent/.cache\"\ncp \"$BIN\" \"$RIG/native/rust/target/release/intent\" || die \"could not stage the binary into the rig\"\ncp \"$ROOT/intent/.cache/intent.db\" \"$RIG/intent/.cache/intent.db\" 2>/dev/null \\\n  || die \"no store at intent/.cache/intent.db to stage into the rig -- the edits need one\"\n\n# Two threads that are actually editable by the verb used below.\nCANDIDATES=\"$(\"$RIG/native/rust/target/release/intent\" st list --status all 2>/dev/null \\\n  | sed -n 's/^\\(ST[0-9][0-9]*\\) .*| WIP .*/\\1/p' | sort -u)\"\nn_cand=\"$(printf '%s' \"$CANDIDATES\" | grep -c . )\"; n_cand=\"${n_cand:-0}\"\n[ \"$n_cand\" -ge 2 ] || die \"found $n_cand thread(s) in a state this check can edit; it needs 2. An estate that cannot supply two editable threads cannot answer this question, which is not the same as the answer being yes.\"\n\nA=\"$(printf '%s\\n' \"$CANDIDATES\" | sed -n '1p')\"\nB=\"$(printf '%s\\n' \"$CANDIDATES\" | sed -n '2p')\"\n\nedit_one() {  # $1 = thread id\n  local err\n  err=\"$( cd \"$RIG\" && ./native/rust/target/release/intent st hold \"$1\" --reason \"canon_concurrent_diff.sh measurement\" 2>&1 )\" \\\n    || die \"\\`st hold $1 --reason ...\\` failed in the rig -- no edit was made, so there is nothing to measure. It said: $err\"\n}\n\nedit_one \"$A\"\n[ \"$EDITS\" -ge 2 ] && edit_one \"$B\"\n\n# THE MEASUREMENT: what did git see change under canon?\nCHANGED=\"$(git -C \"$RIG\" status --porcelain -- intent/.canon/ | sed 's/^...//' | sort -u)\"\nn_changed=\"$(printf '%s' \"$CHANGED\" | grep -c . )\"; n_changed=\"${n_changed:-0}\"\n\n# THE EXPECTATION IS FIXED AT TWO AND DOES NOT MOVE WITH THE INPUT, WHICH IS THE\n# ONLY REASON --one-edit PROVES ANYTHING. The first cut compared $n_changed\n# against $EDITS, so reducing the edits reduced the expectation with it: one\n# path against one expected, self-consistent, green, and unable to fail. An arm\n# whose expectation tracks its input is not a control, and this estate has spent\n# a day finding that shape in other people's instruments.\nEXPECT=2\necho \"canon-concurrent: edited $EDITS of 2 thread(s) through the CLI; git reports $n_changed changed canon path(s):\"\nprintf '%s\\n' \"$CHANGED\" | sed 's/^/    /'\n\nfail=0\n[ \"$n_changed\" -eq \"$EXPECT\" ] || { echo \"  EXPECTED $EXPECT distinct changed path(s), got $n_changed.\" >&2; fail=1; }\n# Each thread must name its OWN path, which a bare count cannot show: two edits\n# landing in one shared file plus one unrelated change also totals two.\nfor id in \"$A\" \"$B\"; do\n  printf '%s\\n' \"$CHANGED\" | grep -q \"$id\" \\\n    || { echo \"  $id has no changed path naming it -- no write landed in that thread's own file.\" >&2; fail=1; }\ndone\npa=\"$(printf '%s\\n' \"$CHANGED\" | grep \"$A\" | head -1)\"\npb=\"$(printf '%s\\n' \"$CHANGED\" | grep \"$B\" | head -1)\"\nif [ -n \"$pa\" ] && [ \"$pa\" = \"$pb\" ]; then\n  echo \"  both threads resolved to ONE path ($pa) -- this is D57-1 option B's failure, the reason it was rejected.\" >&2\n  fail=1\nfi\n\nif [ \"$fail\" -eq 0 ]; then\n  echo \"canon-concurrent: two threads edited concurrently land in two distinct files. Option C holds.\"\n  exit 0\nfi\necho \"canon-concurrent: the changed-path set does not have the shape AC-01.4 requires.\" >&2\nexit 1\n",
      "bytes": 7007,
      "sha256": "0b21a6c378f0bd9134dceb886a377fad9c2c51bebb5633f6afa04b1919ec200b"
    }
  ]
}
