{
  "schema": "intent/thread@3.0",
  "id": "ST0043",
  "title": "Rethink 'intent upgrade'",
  "slug": "rethink-intent-upgrade",
  "status": "completed",
  "created": "2026-06-11",
  "completed": "2026-06-15",
  "objective": "Replace the version-stepping migration chain in `intent upgrade` with a convergent declared-end-state architecture (\"make it so\"), reserving an ordered structural-step ledger for genuine one-way transforms. Make interruption recovery safe by construction, stop the stamp from being load-bearing, and resolve the canon-ownership split between `intent_upgrade` and `intent_claude_upgrade`.",
  "context": "Spun out of the ST0042 Fable 5 review (theme T8) because the rethink is design-level and dwarfs the other review WPs (~1800 lines across two installers, L+). The review findings are the input; this thread owns the redesign and execution.\n\n### Why the current model is wrong (evidence from the review)\n\n- Of 18 v2.x migration steps, ~11 are pure version stamps; only the v2.10 relocation is a genuine structural project mutation. The chain is ~80% ceremony whose only job is moving a number the next step reads.\n- The code already mistrusts its own stamps: three recent predicates were retrofitted to probe observable state \"regardless of stamp\" (`bin/intent_upgrade:100-110`, `bin/intent_helpers:1033-1037,1070-1073`). When every recent predicate probes state, state is the source of truth and the stamp is a stale cache.\n- `intent_claude_upgrade` already is a convergent diagnose/plan/apply installer (Phase 1 probes each artefact against `lib/templates/`, Phase 3 applies, dry-run by default). The convergent model is the half that works.\n\n### Confirmed defects this thread must kill\n\n- F-UPG-3 (HIGH, confirmed): mid-chain steps stamp the live target version, not their step version (`bin/intent_helpers:1442-1450` et al). An interrupted chain claims success; a re-run dispatches on `\"2.11.\"*`, runs only the languages migration (which no-ops because the relocated config does not exist yet), and reports success -- a permanent silent half-migration.\n- F-UPG-1/2 (HIGH/MEDIUM): the dispatcher hard-fails on unknown/future versions (eg 2.12.0) after backup + `create_v2_directory_structure` mutation has already begun; `needs_*` globs enumerate `2.12.*..2.19.*` literally and break at 2.20.0.\n- Backup is unverified: `cp -r ... 2>/dev/null || true` then unconditional \"Backup created successfully\" (`bin/intent_upgrade:138-144`).\n- Canon files have three writers (`create_claude_md`, `canon_refresh_with_user_section`, ad-hoc sed) and the stamp has two technologies (jq vs sed) -- Highlander violation at the centre of the subsystem.\n- T11/T12 fold in here: the BSD-only `sed -i ''` that breaks Linux upgrades (`intent_claude_upgrade:231,1051,1056`) and the unanchored CLAUDE.md version-sed that rewrites historical dates (`intent_claude_upgrade:1056`) both die as side effects of the redesign.\n\n### Recommended target (Architecture B, from the review)\n\n`intent upgrade` becomes a ~150-line orchestrator:\n\n1. Detect current state + numeric semver sanity (refuse downgrade, error on missing VERSION) -- all before any mutation.\n2. Verified backup (`error()` on copy failure).\n3. Walk an ordered ledger of structural steps, each with a state-based `needs` probe, a `run`, and a postcondition verify. Bash 3.2-friendly: flat array of step ids with naming-convention dispatch (`step_<id>_needs` / `step_<id>_run`), no `declare -A`. Day-one ledger: `stp_to_intent` (collapsed pre-v2 trio), `prune_backlog`, `relocate_config` (existing `intent_relocate_dotintent`, kept verbatim -- it already meets the bar), `languages_field`.\n4. Single delegation to `intent claude upgrade --apply` for all canon/skills/subagents convergence; failure surfaced via `error()`, not a warning.\n5. Stamp the target once, after all applicable steps' postconditions verify.\n\nInvariant: after any interruption, a re-run performs exactly the remaining work, applicability decided by probing on-disk state (or a step-scoped sentinel) -- never the top-level stamp; the stamp is written once, last. Corollary: no migration may ever write a version newer than its own step.\n\nOwnership split to resolve: `intent_upgrade` = orchestrator + sole stamper; `intent_claude_upgrade` = sole canon engine (strip its `VERSION_BUMP`). Migration code moves out of `bin/intent_helpers` (sourced by all ~25 commands, consumed only by upgrade) into upgrade-only scope (`bin/intent_migrations` or inline). `detect_project_version` stays shared.\n\n### Migration path (delete / keep)\n\nDelete (fail-forward, no stubs): the 15-arm case ladder + duplicate pre-v2 chain (`intent_upgrade:160-420`), the 17-clause `needs_*` gate (`:113-117`), all pure-stamp migrate functions + enumerated `needs_*` predicates, `show_migration_summary`, `count_migration_files`, the unused second backup `create_project_backup`, `create_claude_md`, the `VERSION_BUMP` action + CLAUDE.md version-sed, the tail-call `skills/subagents sync || true` block (`intent_upgrade:422-441`, subsumed by the single delegation).\n\nKeep (relocated to upgrade-only scope): `intent_relocate_dotintent` verbatim, the pre-v2 structural migrations + converters collapsed into one `stp_to_intent` step, `migrate_v2_10_x_to_v2_11_0`'s state-probed back-fill as `languages_field`, `intent_claude_upgrade` Phase 1-3 machinery minus stamping.\n\n### Open questions for design phase\n\n- Fleet floor: lowest installed version in the fleet (memory says none below v2.10.x) determines whether the pre-v2 `stp_to_intent` step is live code or itself prunable.\n- Canary discipline: two-project canary before fleet pickup (the ST0035 pattern).\n\n### Risk\n\nDispatcher rewrite invalidates `tests/unit/intent_upgrade_dispatcher.bats` and parts of the migration bats -- the test rewrite is in scope. State probes are strictly more robust than stamp dispatch, and the backup finally gets verified, so ancient-stamp members are safer under the new code, not riskier.",
  "body": "## Related Steel Threads\n\n- ST0042 -- Fable 5 review; theme T8 is this thread's source and full evidence base\n- ST0035/ST0036 -- produced the v2.10 relocation (the one genuine structural step) and the canon installer being consolidated here\n- ST0037 -- the `languages` field back-fill that becomes the `languages_field` ledger step",
  "preamble": "",
  "criteria": [
    {
      "id": "AC-00.1",
      "text": "`intent upgrade` is a convergent orchestrator: detect state + numeric semver sanity, then verified backup, then walk an ordered ledger of structural steps (each with a state-based `needs` probe, a `run`, and a postcondition verify), then a single delegation to `intent claude upgrade --apply` for all canon/skills/subagents convergence, then stamp the target once, last. The confirmed defects below are all killed.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.1",
      "text": "(F-UPG-3) After any interruption, a re-run performs exactly the remaining work; applicability is decided by probing on-disk state (or a step-scoped sentinel), never the top-level stamp. No step ever writes a version newer than its own step. The stamp is written once, last.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.2",
      "text": "(F-UPG-1/2) Semver sanity (refuse downgrade, error on missing VERSION) runs before any mutation; unknown / future versions do not hard-fail mid-mutation; no enumerated `needs_*` globs that break at a future version (eg 2.20.0).",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.3",
      "text": "Backup is verified -- `error()` on copy failure; no unconditional \"Backup created successfully\". (Already correct in v2.11.14 inline backup; the orchestrator must preserve it.)",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.4",
      "text": "Canon files have a single writer and the stamp has a single technology. `intent_upgrade` is the sole stamper (jq); `intent claude upgrade` is the sole canon engine (its `VERSION_BUMP` action + the two `sed` stamps stripped).",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.5",
      "text": "Migration code moves out of `bin/intent_helpers` (sourced by all commands, consumed only by upgrade) into upgrade-only scope (`bin/intent_migrations`); `detect_project_version` stays shared.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.6",
      "text": "The BSD-only `sed -i ''` is gone (Linux upgrades work) and the CLAUDE.md version handling is anchored (historical dates are not rewritten). Canon engine is `intent/plugins/claude/bin/intent_claude_upgrade`.",
      "kind": "test",
      "state": {
        "is": "withdrawn",
        "reason": "THE SUBJECT IS GONE, AND THE GREEN THAT COVERED IT WAS ALREADY EMPTY BEFORE ANYTHING WAS DELETED. This criterion names its engine in its own text -- 'Canon engine is intent/plugins/claude/bin/intent_claude_upgrade' -- and that file was deleted at 125f601d. The property it asserts, that the BSD-only sed -i '' is gone so Linux upgrades work, has no canon engine left to be true or false about. MEASURED RATHER THAN ASSUMED, on the day of the withdrawal: its witness tests/unit/intent_claude_upgrade.bats ran 27 tests -- 25 failing loudly on the missing script, and 2 PASSING VACUOUSLY. One of the two vacuous passers is AT-01.8, the row that covers this criterion: its body is 'if grep -nE \"sed -i ''\" $canon; then fail; fi', and grep on an absent file errors, so the if is false and fail is unreachable. The row was green because the subject was gone, not despite it. WITHDRAWN AND NOT SATISFIED, deliberately. The portability property was never proved against v3; its subject was removed. v3 installs canon from Rust and runs no sed at all, so this class cannot recur in the same form -- but that is a different engine meeting a different criterion, not this one being met. Recording it as satisfied would mint a green for work nobody did. NOT DESCOPED EITHER: there is no thread this moves to, because there is no surviving artefact for it to be about.",
        "by": "hv via vc, 2026-08-30"
      }
    },
    {
      "id": "AC-01.7",
      "text": "`tests/unit/intent_upgrade_dispatcher.bats` and the affected migration bats are rewritten to the new architecture; below-v2.9.0 migration tests (incl. `ext_migration.bats`) deleted; the full suite is green.",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "full suite green (matts) 2026-06-15"
      }
    }
  ],
  "tests": [
    {
      "id": "AT-00.1",
      "kind": "test",
      "file": "tests/unit/intent_upgrade_orchestrator.bats",
      "covers": [
        "AC-00.1"
      ],
      "status": "green",
      "note": "test: convergent upgrade from a v2.9.x project lands at target (relocate then languages then canon then stamp-once)"
    },
    {
      "id": "AT-01.1",
      "kind": "test",
      "file": "tests/unit/intent_upgrade_orchestrator.bats",
      "covers": [
        "AC-01.1"
      ],
      "status": "green",
      "note": "test: interrupted upgrade re-run completes only the remaining work via state probe"
    },
    {
      "id": "AT-01.2",
      "kind": "test",
      "file": "tests/unit/intent_upgrade_orchestrator.bats",
      "covers": [
        "AC-01.1"
      ],
      "status": "green",
      "note": "test: no ledger step writes the version; only the orchestrator stamps, last"
    },
    {
      "id": "AT-01.3",
      "kind": "test",
      "file": "tests/unit/intent_upgrade_orchestrator.bats",
      "covers": [
        "AC-01.2"
      ],
      "status": "green",
      "note": "test: future or unknown version does not hard-fail before mutation"
    },
    {
      "id": "AT-01.4",
      "kind": "test",
      "file": "tests/unit/intent_upgrade_orchestrator.bats",
      "covers": [
        "AC-01.2"
      ],
      "status": "green",
      "note": "test: semver sanity refuses downgrade and missing VERSION before any mutation"
    },
    {
      "id": "AT-01.5",
      "kind": "test",
      "file": "tests/unit/intent_upgrade_orchestrator.bats",
      "covers": [
        "AC-01.3"
      ],
      "status": "green",
      "note": "test: backup failure surfaces via error() and aborts before mutation"
    },
    {
      "id": "AT-01.6",
      "kind": "test",
      "file": "tests/unit/intent_upgrade_orchestrator.bats",
      "covers": [
        "AC-01.4"
      ],
      "status": "green",
      "note": "test: only intent_upgrade writes the stamp and the canon engine carries no version-bump"
    },
    {
      "id": "AT-01.7",
      "kind": "test",
      "file": "tests/unit/intent_upgrade_orchestrator.bats",
      "covers": [
        "AC-01.5"
      ],
      "status": "green",
      "note": "test: migration code is not sourced into non-upgrade commands"
    },
    {
      "id": "AT-01.8",
      "kind": "test",
      "file": "tests/unit/intent_claude_upgrade.bats",
      "prose": "RETIREMENT ATTEMPTED 2026-08-30 AND BLOCKED ON A DECLARED-UNBUILT TRANSITION -- this row is deliberately still green and its note is the record of why. AC-01.6 is WITHDRAWN (its canon engine intent/plugins/claude/bin/intent_claude_upgrade went at 125f601d), and hv ruled this row retired with it and the cited bats file deleted. The retirement cannot be executed: the shape the estate uses for a retired row is AT-03.12 -- non-test, n/a, no file, reason in prose -- and NO VERB CAN PRODUCE IT. at edit takes file, prose and covers; kind rides through untouched, and facade.rs says that is deliberate, that a kind change moves the contract graph, that it belongs in a ruling rather than a flag added while building something else, and that Criterion.kind is Unbuilt, owed and named in the transition table. at na moves status without moving kind, which lands the row at test + n/a -- a state AtStatus::permitted_for forbids and doctor reports as model-inconsistent. So DELETING THE CITED FILE WOULD FORCE THIS ROW INTO A STATE THE MODEL CANNOT EXPRESS: green over a missing file, or test + n/a. The file therefore stays until the kind transition is ruled. THE GREEN IS STILL VACUOUS AND THAT IS THE POINT: this row's test reads if grep -nE (sed -i quote-quote) $canon; then fail; fi, and grep on the absent engine errors, so the fail branch is unreachable. Of that file 27 tests, 25 fail loudly on the missing script and 2 pass this way; this is one of the 2. absent_at_check.sh, landed alongside, would NOT catch it -- the file is present the whole time it is hollow, which is that guard stated limit.",
      "covers": [
        "AC-01.6"
      ],
      "status": "fiat",
      "fiat": {
        "because": "RETIRED ON hv's RULING OF 2026-08-30, routed by vc: delete tests/unit/intent_claude_upgrade.bats and retire this row with it. Fiat rather than a kind move, which is vc's find -- AtStatus::permitted_for makes Fiat legal for BOTH kinds because it is a decision about the ROW rather than about its result, so retirement needs no test-to-non-test transition and no table amendment. NOTHING WAS GIVEN UP, WHICH IS THE PART A LATER READER WANTS. This row's subject, the canon engine intent/plugins/claude/bin/intent_claude_upgrade, went at 125f601d, and AC-01.6 -- which this row covers -- is withdrawn for that reason. The row's green was already empty before any delete: its test reads 'if grep -nE (sed -i, BSD form) $canon; then fail; fi', grep on an absent file errors, the if is false, the fail branch is unreachable. Of that file's 27 tests, 25 failed loudly on the missing script and 2 passed vacuously; this was one of the 2. So there was no coverage to lose -- the claim had stopped being one long before it was retired. The citation is deliberately KEPT rather than cleared: it records what this row was covered by, and the file's absence is stated here rather than implied by a blank field.",
        "by": "matts",
        "at": "2026-08-30T20:41:37.118Z",
        "invoker": {
          "tty": false,
          "env": "macos/aarch64"
        }
      },
      "note": "RED BEFORE RETIREMENT, AND IT IS THE TRUE STATE RATHER THAN A STEP TOWARDS ONE. This row was green and the green was VACUOUS: its test body is 'if grep -nE \"sed -i (two single quotes)\" $canon; then fail; fi' against a canon engine deleted at 125f601d. grep on an absent file errors, the if is false, and the fail branch is unreachable -- so the assertion could not be reached and the row could not go red however broken its subject was. Measured on the whole file: 27 tests, 25 failing loudly on the missing script, 2 passing this way, and this row is one of the 2. A row whose assertion cannot be reached is not honestly green."
    }
  ],
  "attachments": [
    {
      "path": "design.md",
      "text": "# ST0043 Design -- intent upgrade as a convergent orchestrator (Architecture B)\n\nArchitecture from info.md (the ST0042 review, theme T8). This doc adds the concrete build plan distilled from the v2.11.14 subsystem map (read-only audit, 2026-06-15). Fleet-floor ratified v2.9.x (matts, full prune): everything that migrates a below-v2.9.0 project is deleted (fail-forward; git preserves).\n\n## 1. Target architecture\n\n`bin/intent_upgrade` becomes a ~150-line orchestrator:\n\n1. Detect state: `VERSION=detect_project_version`; `TARGET=get_intent_version` (the repo `VERSION` file).\n2. Semver sanity BEFORE any mutation: `error()` on missing / unparseable VERSION; refuse downgrade (current > target); a future / unknown current version does NOT hard-fail (no enumerated version globs, no `*) error \"Unknown version\"`).\n3. Verified backup: `cp ... || error()` (already correct in v2.11.14 inline backup -- preserve it; no unconditional success echo on a failed copy).\n4. Walk an ordered ledger of structural steps. Flat array (bash 3.2): `LEDGER=\"relocate_config languages_field\"`. Naming-convention dispatch per step id: `step_<id>_needs` (state probe) -> `step_<id>_run` -> `step_<id>_verify` (postcondition). No `declare -A`.\n5. Single delegation to `intent claude upgrade --apply` for ALL canon / skills / subagents convergence; failure -> `error()` (not a warning, not `|| true`).\n6. Stamp the target ONCE, last (jq), after every applicable step's postcondition verifies.\n\nInvariants:\n\n- AC-01.1: after interruption, a re-run does exactly the remaining work; applicability is decided by `step_<id>_needs` probing on-disk state, never the top-level stamp. No step writes the version (only the orchestrator stamps, last) -> trivially \"no step writes a version newer than its own step\".\n- AC-01.4: `intent_upgrade` is the SOLE stamper (jq, one technology). `intent claude upgrade` is the SOLE canon engine -- its `VERSION_BUMP` action + both `sed` stamps are stripped.\n\n## 2. New file layout\n\n| File                                              | Role                                                                      | Change                                                                                                                                                |\n| ------------------------------------------------- | ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `bin/intent_upgrade`                              | orchestrator + sole stamper                                               | full rewrite (was a 524-line case-ladder script; only `usage()` was a function)                                                                       |\n| `bin/intent_migrations`                           | ledger steps + relocated migration code, sourced ONLY by `intent_upgrade` | NEW (satisfies AC-01.5)                                                                                                                               |\n| `bin/intent_helpers`                              | shared lib                                                                | KEEP `detect_project_version` (+ `detect_stp_version` alias) shared; REMOVE every `migrate_*` / `needs_*` / `intent_relocate_dotintent` / ceremony fn |\n| `intent/plugins/claude/bin/intent_claude_upgrade` | canon engine only                                                         | strip `VERSION_BUMP` (action + sed at :1051 config + :1056 CLAUDE.md); fix BSD `sed -i ''` at :231 for Linux; no unanchored historical-date rewrite   |\n\nPath note: the canon engine is `intent/plugins/claude/bin/intent_claude_upgrade`, NOT `bin/intent_claude_upgrade` (info.md + the ACs say `bin/`).\n\n## 3. Ledger -> existing code (probes already exist inside the deleted predicates; extract, do not reinvent)\n\n| step              | needs (probe source)                                                                            | run (source)                                                                       | verify                                                 |\n| ----------------- | ----------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------ |\n| `relocate_config` | `.intent/` present && `intent/.config/` absent (from `needs_v2_10_0_upgrade` helpers:1186-1188) | `intent_relocate_dotintent` (helpers:1326-1407, moved verbatim)                    | `intent/.config/config.json` exists && `.intent/` gone |\n| `languages_field` | config exists && `!has(\"languages\")` (from `needs_v2_11_0_upgrade` helpers:1148-1152)           | back-fill body of `migrate_v2_10_x_to_v2_11_0` (helpers:1468-1551) MINUS its stamp | config `has(\"languages\")`                              |\n\n`prune_backlog` DROPPED: it is the v2.4->v2.5 `Backlog.md` removal (`migrate_v2_4_0_to_v2_5_0` helpers:1868-1915), vacuous at the v2.9.x floor. No generic `prune_backlog` fn exists.\n\n## 4. Delete / Keep / Move (from the subsystem map)\n\n- KEEP-SHARED (stay in `intent_helpers`): `detect_project_version` (517-568) + `detect_stp_version` alias (643-645).\n- KEEP-RELOCATE (into `bin/intent_migrations`): `intent_relocate_dotintent` (1326-1407, verbatim) -> `step_relocate_config_run`; the languages back-fill body of `migrate_v2_10_x_to_v2_11_0` (1468-1551, minus stamp) -> `step_languages_field_run`; the two probes extracted from `needs_v2_10_0_upgrade`:1186-1188 and `needs_v2_11_0_upgrade`:1148-1152.\n- DELETE (fail-forward; git preserves):\n  - `intent_upgrade`: the version case ladder + pre-v2 tail case (163-423); the 17-predicate needs-gate (113); the tail-call skills/subagents sync block (425-444, subsumed by the single delegation); the `create_claude_md` call (461).\n  - `intent_helpers`: every `migrate_v*_to_*` from `migrate_v0_to_v2` (690) through `migrate_v2_8_2_to_v2_9_0` (1251) AND `migrate_v2_9_0_to_v2_10_0` (1425-1463, decomposed: relocate -> step, canon-apply -> the single delegation, stamp -> the final stamp); every `needs_v2_*` predicate; the pre-v2 converters (`convert_yaml_frontmatter` 316, `update_version_in_frontmatter` 371, `convert_yaml_config_to_json` 386, `flatten_directory_structure` 498, `migrate_remaining_content` 1918, `create_default_v2_config` 1986, `_generate_basic_agents_md` 1573, `create_v2_directory_structure` 457, `update_config_version` 1716); `show_migration_summary` (611), `count_migration_files` (626), `create_project_backup` (571), `needs_migration` (598) -- all dead/unused; `create_claude_md` (2004, after confirming no `intent_init` caller -- grep showed none); `generate_ext_readme` (1203) once the v2.8.2 migration is gone.\n  - `intent_claude_upgrade`: the `VERSION_BUMP` action + sed stamps (1047-1059).\n\next bootstrap: deleting `migrate_v2_8_2_to_v2_9_0` drops the only upgrade-time `~/.intent/ext/` bootstrap (helpers:1265-1280). VERIFIED SAFE: `intent ext` creates the dir on demand (`intent_ext:746`), discovery tolerates absence (`ext_root_dir`/`ext_enumerate_names`), `intent init` never bootstrapped it, the worker-bee seed is dead (pruned ST0034), and the fleet is all v2.9.0+ (already bootstrapped).\n\n## 5. Stamp consolidation (AC-01.4)\n\nTwo writers today: jq in `intent_helpers` (each `migrate_*` writes `.intent_version`, `del(.version)`) + sed `VERSION_BUMP` in the canon engine (config.json :1051, CLAUDE.md :1056). New: the orchestrator is the sole jq stamper, written once last to `intent/.config/config.json`; the canon engine's `VERSION_BUMP` is removed.\n\n## 6. Test rewrite plan (AC-01.7)\n\n- NEW: `tests/unit/intent_upgrade_orchestrator.bats` -- the AT-00.1 / AT-01.\\* set (sec 7).\n- DELETE: `tests/unit/ext_migration.bats` (tests `migrate_v2_8_2_to_v2_9_0` [deleted] + case-ladder / needs static gates, 24 tests); the `needs_v2_*_upgrade` predicate tests in `migrate_v2_9_0_to_v2_10_0.bats` (6-9) and `migrate_v2_10_x_to_v2_11_0.bats` (7-9).\n- TRIM-KEEP: `migrate_v2_9_0_to_v2_10_0.bats` tests 1-5 (relocate behaviour -> retarget at `step_relocate_config_run` / `intent_relocate_dotintent` in its new home); `migrate_v2_10_x_to_v2_11_0.bats` tests 1-6 (languages back-fill -> retarget at `step_languages_field_run`).\n- REWRITE: `intent_upgrade_dispatcher.bats` -- drop the case-ladder / Unknown-version asserts (1, 3); keep behavioural (2, 4, 5, 6, 7) retargeted at the orchestrator.\n\n## 7. AT -> entry-point map (red-first targets; numeric ids per the `intent ac/at` parser)\n\n- AT-00.1 `intent_upgrade_orchestrator.bats` -- e2e: a project stamped 2.9.0 in the `.intent/` layout, no languages -> run `intent upgrade` -> assert relocated (`intent/.config/`, `.intent/` gone), languages present, stamp == TARGET, backup made.\n- AT-01.1 -- fixture already-relocated + languages-absent + stamp old -> run -> only `languages_field` runs, stamp == TARGET, no error (relocate probe skips).\n- AT-01.2 -- grep guard: no `step_*_run` in `bin/intent_migrations` writes `intent_version`; only `bin/intent_upgrade` stamps.\n- AT-01.3 -- current version unknown/future-but-in-range (eg \"2.11.99\") -> success, stamp == TARGET, no \"Unknown version\"; + grep guard: no enumerated version globs in `intent_upgrade`.\n- AT-01.4 -- downgrade (current > TARGET) errors before backup (no `.backup/`); missing/unparseable VERSION errors before mutation.\n- AT-01.5 -- backup failure -> `error()` + abort + stamp untouched (port `intent_upgrade_dispatcher.bats:188`).\n- AT-01.6 -- grep guard: `intent_claude_upgrade` has no `VERSION_BUMP` / intent_version sed; only `intent_upgrade` stamps.\n- AT-01.7 -- grep guard: `intent_helpers` defines no `migrate_*` / `needs_*` / `intent_relocate_dotintent`; they live in `bin/intent_migrations`; `detect_project_version` still in helpers.\n- AT-01.8 `intent_claude_upgrade.bats` -- no BSD `sed -i ''` in the canon engine; version handling anchored (historical dates not rewritten).\n- AC-01.7 (non-test) -- the rewritten/trimmed suite is green on matts's full-suite run.\n\n## 8. Related close-gate fixes landing in the same release (No Silent Errors)\n\nTwo `bin/intent_acceptance` close-gate fixes, both release-scoped tooling hardening (narrate in `intent/history/v2.12.0.md`), both guard-tested in `acceptance_close_gate.bats`:\n\n- F1 (silent drop): AC/AT lines that fail the strict numeric grammar (eg the letter-group `AC-U.1` ids) were silently dropped, making the gate vacuous. Fix: a malformed-line detector warns loudly on every read path and blocks the gate.\n- F6 (missing-contract gate behaviour): re-confirmed as opt-in-by-presence, NOT a block (matts, 2026-06-16). A thread with no `acceptance.md` has not opted into the AC regime, so `ac gate` stays OPEN (exit 0) and the thread closes as before -- old STs are never forced to author a contract. Only a PRESENT acceptance.md is enforced: unsatisfied ACs or malformed lines (F1) block. The bug report's \"missing must block\" was superseded by this ruling.\n",
      "bytes": 11095,
      "sha256": "1688ea5a3475eeb84350751379f51573f5a37491683e9e38a1e5d3378ff5d498"
    },
    {
      "path": "impl.md",
      "text": "# Implementation - ST0043: Rethink 'intent upgrade'\n\n## As-built\n\nConvergent orchestrator (Architecture B) shipped per `design.md`. Built red-first against `tests/unit/intent_upgrade_orchestrator.bats` (AT-00.1, AT-01.1..01.8); all green.\n\n- `bin/intent_upgrade` -- rewritten from a 524-line version-case ladder into a ~150-line orchestrator: detect state -> semver sanity (refuse downgrade, error on missing/unrecognised version, fail-forward floor of v2.9.0) -> verified backup -> walk the `LEDGER` of state-probed steps -> single `intent claude upgrade --apply` -> stamp the target once, last (sole jq stamper). A flat `LEDGER=\"relocate_config languages_field\"` dispatched by `step_<id>_needs/_run/_verify` naming convention (bash 3.2, no `declare -A`).\n- `bin/intent_migrations` -- NEW, upgrade-only scope (sourced only by `intent_upgrade`). Holds the two ledger-step trios + `intent_relocate_dotintent` (moved verbatim from helpers). No step writes `intent_version`.\n- `bin/intent_helpers` -- pruned 2026 -> 369 lines. Every `migrate_*`/`needs_*`/`intent_relocate_dotintent`/pre-v2 converter/ceremony function deleted (fail-forward; git preserves). KEPT shared: `detect_project_version` (+ `detect_stp_version` alias).\n- `intent/plugins/claude/bin/intent_claude_upgrade` -- sole canon engine: `VERSION_BUMP` action + its two `sed -i ''` stamps removed; `canon_substitute_placeholders` rewritten to a portable in-place edit (no BSD `sed -i ''`). Linux upgrades work; the orchestrator is now the only stamper.\n\n## Deviations from the design\n\n- **`create_project_backup` deleted + its test removed.** Design flagged it \"dead/unused\"; the caller-grep found one live caller -- a dedicated test in `core_functionality.bats` (`.backup_` prefix). It is a second backup impl (Highlander); both function and test deleted.\n- **AT-01.3 fixture uses `2.10.5`, not a future version.** The repo `VERSION` is still `2.11.14` (the bump to 2.12.0 is a release step), so a `2.11.99` fixture tripped the new downgrade guard. `2.10.5` is below target and unknown to the old ladder -- the right \"unknown-but-handled\" case.\n- **The skills/subagents sync tail is kept in the orchestrator** (in-whiteboard install + `claude skills/subagents sync`, failure-tolerant). AC-01.4 is satisfied by canon _files_ having a single writer (`intent claude upgrade`) and the stamp a single writer (`intent_upgrade`); the mirror syncs are not canon-file writers.\n- **Canon-engine path** is `intent/plugins/claude/bin/intent_claude_upgrade`, not `bin/intent_claude_upgrade` as info.md / the ACs say.\n\n## Test rewrite (AC-01.7)\n\n- NEW: `tests/unit/intent_upgrade_orchestrator.bats` (AT-00.1, AT-01.1..01.7); AT-01.8 appended to `tests/unit/intent_claude_upgrade.bats`.\n- NEW (retarget): `tests/unit/intent_migrations_relocate.bats` + `tests/unit/intent_migrations_languages.bats` -- the relocate edge-cases + back-fill variants, pointed at the new `intent_migrations` functions; the deleted `needs_v2_*` predicate tests dropped, stamp assertions dropped (the stamp is the orchestrator's).\n- DELETED: `tests/unit/ext_migration.bats` (tested the deleted v2.8.2->v2.9.0 migration + case-ladder static gates); `tests/unit/migrate_v2_9_0_to_v2_10_0.bats` + `tests/unit/migrate_v2_10_x_to_v2_11_0.bats` (superseded by the `intent_migrations_*` files); the `create_project_backup` test in `core_functionality.bats`.\n- REWRITTEN: `tests/unit/intent_upgrade_dispatcher.bats` -- dropped the glob-case-ladder test; the behavioural tests (lands-at-target, in-whiteboard install, subagent sync, backup abort) kept against the orchestrator.\n\n## Related close-gate fixes (same release, see design.md sec 8)\n\n`bin/intent_acceptance` hardened against the vacuous-green mode that is a real defect: F1 (malformed / non-numeric AC/AT lines silently dropped -> now warn + block). F6 (missing acceptance.md) was investigated and deliberately left as opt-in-by-presence (a thread with no contract has not opted into the AC regime, so the gate stays OPEN / exit-0; matts ruling 2026-06-16) -- the bug report's \"missing must block\" was superseded. Guard tests in `acceptance_close_gate.bats` (F1 blocks loudly; the no-contract thread stays open).\n\n## Status\n\n7/8 ACs satisfied (`intent ac status ST0043`). AC-01.7 (non-test) awaits matts's full-suite green.\n",
      "bytes": 4297,
      "sha256": "1c7db30d7284a5c44e2eabc09657a143443d31bb2077ae0d960bcead04b47a58"
    },
    {
      "path": "tasks.md",
      "text": "# Tasks - ST0043: Rethink 'intent upgrade'\n\n## Tasks\n\n- [x] Ratify ACs open-gate (matts); fleet-floor ruling (v2.9.x, full prune); numeric AC/AT ids\n- [x] Map the upgrade subsystem (read-only audit) -> design.md\n- [x] Write red-first ATs (intent_upgrade_orchestrator.bats + AT-01.8) -- matts witnessed RED\n- [x] Build `bin/intent_migrations` (relocate_config + languages_field steps + intent_relocate_dotintent)\n- [x] Rewrite `bin/intent_upgrade` as the convergent orchestrator (detect -> sanity -> backup -> ledger -> delegate -> stamp-once)\n- [x] Strip `VERSION_BUMP` + make `sed` portable in the canon engine\n- [x] Prune migration code from `bin/intent_helpers` (2026 -> 369 lines; keep detect_project_version)\n- [x] Test rewrite: new intent*migrations*\\*.bats; delete ext_migration + old migrate bats; trim dispatcher + core_functionality\n- [x] All 9 ATs green; upgrade/migration/acceptance suites green\n- [ ] AC-01.7: full suite green (matts runs the external suite) -> satisfy AC-01.7\n- [ ] v2.12.0 release (shared with ST0045): version bump, CHANGELOG, history narrative\n\n## Task Notes\n\nClose-gate fixes (F1 malformed-line silent-drop, F6 vacuous-green-on-missing-contract) landed in `bin/intent_acceptance` in the same arc -- release-scoped tooling hardening, guard-tested.\n\n## Dependencies\n\n- AC-01.7 sign-off depends on matts's full-suite run.\n- Release is bundled with ST0045 into v2.12.0.\n",
      "bytes": 1401,
      "sha256": "4ac7035ee6dfeb346a1d34362a7ea994601ee6d9b94ed2e06ddee696d8fdba90"
    }
  ]
}
