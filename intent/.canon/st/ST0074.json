{
  "schema": "intent/thread@3.0",
  "id": "ST0074",
  "title": "Machine-wide projects: intentd's config home, the project registry and discover, the explorer's project picker, and the menubar status line",
  "slug": "machine-wide-projects-intentd-s-config-home-the",
  "status": "wip",
  "created": "2026-09-13",
  "objective": "Make Intent's own front doors reach every Intent project on the machine rather than only the one the terminal is standing in, and present them consistently.\n\n- The explorer handles `/threads` and `/issues` itself, and can pick a project from the ones this machine knows (`/projects`, and `intent explore` outside a project).\n- A single, standards-compliant home for intentd's durable configuration, human- and machine-editable, that lists every known Intent project.\n- `intent explore` keeps the project it runs in registered there, and `intent discover [fromdir]` finds Intent projects under a directory, checks each is config-compatible with the running intentd, and registers it.\n- intentd watches that file and refreshes what it knows when it changes.\n- The menubar app presents its menu in the same shape as Gtools': an identity row, one status line (place, state, details), then the actions.\n",
  "context": "Raised by hv on 2026-09-13, while the cut waited on 0354, and ruled into one bundle: \"all of this should really go into the one work bundle. If we need a new ST to hold it all, then make one and make the various bits WPs inside it.\"\n\nWhat exists today, measured rather than recalled:\n\n- intentd's project registry (`intentd/src/registry.rs`) is in memory only. A project is registered as a side effect of first contact, so a freshly started daemon knows nothing until each project has been used again.\n- intentd has no durable configuration of its own. Its runtime state lives under `~/.local/share/intent/` (socket, loopback address, token, lock, log). The operator's v3 configuration is `~/.intent/config.json` (`intentsvcs::userstate::global_config`).\n- `~/.config/intent/config.json` is v2's file (it reads `intent_version: 2.0.0`, `backlog_dir`, `editor`) and nothing in v3 reads it: every v3 per-user store has its own path and never reads or writes v2's (hv adopted, 2026-08-22).\n- The shipped surface may read only the environment variables in `no_intent_home.rs`'s `ALLOWED` (`COLUMNS`, `EDITOR`, `HOME`, `USER`, `VISUAL`). A standards-compliant location that honours `XDG_CONFIG_HOME` or similar needs a grant from hv, recorded as a row there.\n- `intent explore` outside a project refuses through `context()`: it needs a project directory to open.\n- The explorer's palette sent `/issues` to `intent issues` and offered no `/threads`; landed as the explorer's own acts at 793984a50.\n\nhv's design for the registry, in their words: \"intentd has some global config that is easily human AND machine editable. Any time 'intent explore' is run, it should ensure that if it is running in an intent project, that that project is up to date in the global config that intentd can find it. AND: we need an 'intent discover [fromdir]' command that looks for all intent projects from the current dir (by default) or a dir that is passed in as a param. Whenever it finds one, it confirms that it is 'config compatible' with the current intentd, and then ensures that it is registered in the global config for intentd to find. intentd keeps an eye on that global config file and refreshes its knowledge of host-wide intent projects whenever it changes.\"\n\nAnd on the location: \"We also need to resolve where intentd's config resides ... that answer needs to be standards compliant.\" The location is ruled before the registry is built, because the registry is the first thing to live there.\n\nWhat `intent explore` does outside a project when no daemon answers depends on the registry: hv, \"the answer here changes depending on the outcome of 1\".\n",
  "body": "",
  "preamble": "",
  "wps": [
    {
      "seq": 1,
      "title": "The explorer handles /threads and /issues itself",
      "scope": "S",
      "status": "done",
      "objective": "",
      "body": "",
      "preamble": ""
    },
    {
      "seq": 2,
      "title": "The menubar's one status line, in Gtools' shape",
      "scope": "S",
      "status": "done",
      "objective": "",
      "body": "",
      "preamble": ""
    },
    {
      "seq": 3,
      "title": "The project registry: explore registers its project, intent discover registers compatible ones, intentd watches the file",
      "scope": "L",
      "status": "not-started",
      "objective": "",
      "body": "",
      "preamble": ""
    },
    {
      "seq": 4,
      "title": "The explorer's project picker: /projects, and intent explore outside a project",
      "scope": "M",
      "status": "not-started",
      "objective": "",
      "body": "",
      "preamble": ""
    },
    {
      "seq": 5,
      "title": "Where intentd's durable configuration lives: a standards-compliant home, ruled by hv",
      "scope": "S",
      "status": "not-started",
      "objective": "",
      "body": "",
      "preamble": ""
    }
  ],
  "criteria": [
    {
      "id": "AC-01.1",
      "text": "The explorer handles /threads and /issues itself: `/threads` and `/issues` open their panes as explorer acts, and `/issues <args>` still runs `intent issues`.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.1",
      "text": "While intentd is live, the menubar status line names the place, the state and the details on one line: `intentd :<port>`, `active`, and the steel-thread count. The port is read from the url the daemon published, and the count appears only once the project query has answered.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.2",
      "text": "While intentd is not live (stale, absent or unknown), the status line carries no port and no thread count, and a stale daemon's line names its pid and says to investigate it.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.3",
      "text": "While a lifecycle verb is running, the status line says so and shows no port, because the port from before a restart is dead once its stop returns.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.1",
      "text": "The project registry is one file that a human can edit by hand and the tool reads and rewrites without losing what the human wrote.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.2",
      "text": "`intent explore` run inside an Intent project ensures that project is in the registry and its entry is current.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.3",
      "text": "`intent discover [fromdir]` registers every config-compatible Intent project it finds under the directory, and names each project it does not register with the reason.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.4",
      "text": "intentd refreshes what it knows of the machine's projects when the registry file changes, without a restart.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.1",
      "text": "`/projects` in the explorer lists the registry's projects and opens the one picked.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.2",
      "text": "`intent explore` outside an Intent project opens the project picker, and leaving the picker returns to the shell.",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-05.1",
      "text": "hv has ruled where intentd's durable configuration lives, and design.md records the ruling.",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "f2452077a"
      }
    }
  ],
  "tests": [
    {
      "id": "AT-01.1",
      "kind": "test",
      "file": "native/rust/crates/intent-cli/src/tui/app.rs",
      "covers": [
        "AC-01.1"
      ],
      "status": "green",
      "note": "cargo test -p intent-cli --lib --no-fail-fast: 287 passed, 0 failed, at the worktree base 1f8a912cf with 793984a50's bytes; red control: the collection act's push made a no-op fails slash_threads_and_slash_issues_open_their_collections. Literal id added at 5f62b2633."
    },
    {
      "id": "AT-02.1",
      "kind": "test",
      "file": "native/macos/Intent/IntentTests/HealthTests.swift",
      "covers": [
        "AC-02.1"
      ],
      "status": "green",
      "note": "bin/int macos app-test at 133061d7f's Swift bytes (ids added as comments after the run): 32 tests, 0 failures, IntentTests passed."
    },
    {
      "id": "AT-02.2",
      "kind": "test",
      "file": "native/macos/Intent/IntentTests/HealthTests.swift",
      "covers": [
        "AC-02.2"
      ],
      "status": "green",
      "note": "bin/int macos app-test at 133061d7f's Swift bytes (ids added as comments after the run): 32 tests, 0 failures, IntentTests passed."
    },
    {
      "id": "AT-02.3",
      "kind": "test",
      "file": "native/macos/Intent/IntentTests/HealthTests.swift",
      "covers": [
        "AC-02.3"
      ],
      "status": "green",
      "note": "bin/int macos app-test at 133061d7f's Swift bytes (ids added as comments after the run): 32 tests, 0 failures, IntentTests passed."
    }
  ],
  "attachments": [
    {
      "path": "design.md",
      "text": "# Design: where Intent's per-user files live (WP-05)\n\n**RULED by hv, 2026-09-13.** _\"XDG\\_\\* looks very much like the right way to do. Use that or conform to that. That's what we want.\"_ _\"What ever is the most compliant to standards, use that. Use you judgement.\"_ _\"We can ignore v2 now as there's nothing left using it. Just push on as if it doesn't exist.\"_ The standard is the XDG Base Directory Specification, for `intent` and `intentd` alike. `XDG_CONFIG_HOME`, `XDG_DATA_HOME`, `XDG_STATE_HOME` and `XDG_RUNTIME_DIR` join `no_intent_home.rs`'s `ALLOWED`, read in `userstate.rs` only, each taking the specification's default when unset or empty. Lands in 3.0.2 (hv, 2026-09-13, relayed by vc).\n\n## The layout\n\n| Kind                                            | Path                                                          | What lives there                                           |\n| ----------------------------------------------- | ------------------------------------------------------------- | ---------------------------------------------------------- |\n| Configuration, `$XDG_CONFIG_HOME` (`~/.config`) | `~/.config/intent/config.json`                                | The operator's settings: `author`, the explorer's settings |\n|                                                 | `~/.config/intent/projects.json`                              | The project registry (WP-03), hand-editable                |\n| Data, `$XDG_DATA_HOME` (`~/.local/share`)       | `~/.local/share/intent/home`                                  | The install pointer                                        |\n|                                                 | `~/.local/share/intent/{skills,subagents,agents}/`            | The installed-payload manifests                            |\n|                                                 | `~/.local/share/intent/ext/`                                  | User extensions                                            |\n| State, `$XDG_STATE_HOME` (`~/.local/state`)     | `~/.local/state/intent/`                                      | intentd's logs, the macOS app's build output               |\n| Runtime, `$XDG_RUNTIME_DIR`                     | `$XDG_RUNTIME_DIR/intent/`, else `~/.local/state/intent/run/` | intentd's socket, address, token and lock                  |\n| Apple's, not XDG                                | `~/Library/LaunchAgents/com.matthewsinclair.intentd.plist`    | launchd reads only that directory                          |\n| Claude Code's, not Intent's                     | `~/.claude/`                                                  | Unchanged                                                  |\n\n- **The runtime fallback is silent.** macOS never sets `XDG_RUNTIME_DIR`, so the specification's warning would print on every command there.\n- **No `intent.d/`.** A `.d` directory holds drop-in fragments a tool composes; nothing here composes fragments.\n\n## The migration\n\n- **What moves.** The first 3.0.2 command to resolve a per-user path, finding `~/.intent/` and no `~/.config/intent/config.json` in v3's shape, moves `~/.intent/config.json`, `home`, `skills/`, `subagents/`, `agents/` and `ext/` into the layout, removes `~/.intent/` if nothing else is left in it, and prints one line saying so. Claude Code hooks run through the installed binary and perform the move. A project's git pre-commit carrier is a copy of the shim: one installed before 3.0.2 reads `~/.intent/home` and refuses until `intent claude upgrade --apply` reinstalls it there.\n- **Read once, never again.** `~/.intent/` is read by that move and by nothing else in 3.0.2. A `~/.config/intent/config.json` not in v3's shape is replaced.\n- **What a running daemon meets.** Runtime files are not moved: a 3.0.1 intentd keeps its socket under `~/.local/share/intent/` until it stops. A 3.0.2 client looks under the new runtime path, finds no daemon, and runs in-process as it does with none running. `intent daemon stop`, and so `restart`, also stops a daemon still holding the old lock and removes its runtime files; `restart` then starts the 3.0.2 daemon at the new path. The rebuild's daemon restart does this on this machine.\n- **What an old binary meets.** A 3.0.1 `intent` finds no `~/.intent/home`, and its existing refusal names `intent bootstrap`; running that recreates a `~/.intent/` the 3.0.2 binary never reads. The remedy is the 3.0.2 binary.\n- **Every literal reader moves in the same change**: `userstate.rs`, `lib/templates/hooks/pre-commit-shim.sh` and `pre-commit.sh`, `bin/.devbin/cmd/{hooks,macos}`, and the skills and docs naming these paths. The menubar app names none: it asks the CLI.\n\n# Design: the project registry and `intent discover` (WP-03)\n\nIt lives at `$XDG_CONFIG_HOME/intent/projects.json` (WP-05).\n\n## Schema\n\n```json\n{\n  \"schema\": 1,\n  \"projects\": [\n    {\n      \"root\": \"/Users/matts/Devel/prj/Intent\",\n      \"added_by\": \"discover\",\n      \"note\": \"free text\"\n    }\n  ]\n}\n```\n\n- `root` is the key, stored canonicalised, as intentd's in-memory registry keys projects.\n- `added_by` is `explore`, `discover` or `hand`.\n- Keys the tool does not know, `note` included, are carried through a rewrite untouched (AC-03.1).\n- No status, counts or timestamps: the project's own store answers those.\n- A `schema` newer than the tool knows is read and never rewritten.\n\n## Writers\n\n- `intent explore`, inside a project: adds its root if absent, and changes nothing else (AC-03.2).\n- `intent discover [fromdir]`: adds each compatible project not already listed (AC-03.3).\n- A human, with an editor.\n- intentd never writes it.\n\nEvery write goes to a temporary file renamed over the original; a writer that finds the file changed since it read it re-reads and re-applies its one change.\n\n## Compatible\n\nA candidate is registered when `Project::discover` finds its config, the config parses, and v3 would not refuse it as an unmigrated project (AC-10.7). Discover names every project it does not register, with the reason: `needs migration`, `config does not parse`, or `already registered`. It migrates and repairs nothing.\n\n## On change (AC-03.4)\n\nintentd watches the file's directory (a rename replaces the file's inode), debounced like the tree watcher, and re-reads the file. That changes the projects it lists, never which stores are open: opening stays first contact. A file that does not parse is logged with the error and the last good list is kept; a missing file is an empty list.\n\n## Discover's bounds\n\n- `fromdir` defaults to the working directory, with `--depth N` (default 4).\n- It does not descend into a project it has found.\n- It walks with `ignore::WalkBuilder`, the gitignore-aware walker `sync.rs` and `project.rs` already use, and skips `.git/`, `target/` and `node_modules/`.\n- It does not follow symlinks.\n- It prints registered, already registered and not registered with reasons.\n",
      "bytes": 6863,
      "sha256": "0e444f57f0a098b85ec396baa4104742376ea0b9f20deb5c0d2ad7e5c0f63c16"
    }
  ]
}
