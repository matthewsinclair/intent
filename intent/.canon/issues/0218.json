{
  "schema": "intent/issue@3.0",
  "number": 218,
  "slug": "intent-claude-skills-uninstall-leaves-an-empty-d",
  "title": "intent claude skills uninstall leaves an empty directory, so a directory count disagrees with a skill count",
  "status": "closed",
  "severity": "low",
  "created": "2026-09-02",
  "closed": "2026-09-11",
  "reporter": "Matthew Sinclair",
  "body": "**`intent claude skills uninstall` REMOVES A SKILL'S FILES AND LEAVES ITS DIRECTORY BEHIND, SO A DIRECTORY COUNT DISAGREES WITH A SKILL COUNT.**\n\nFOUND 2026-09-02 by ic, while running step 4 of the `in-start` / `in-next` retirement (ST0065, `intent/st/ST0065/_draft-retirement-mechanics.md`).\n\n**OBSERVED STATE.** `~/.claude/skills/in-start/` and `~/.claude/skills/in-next/` exist and are EMPTY (mtime 2026-08-29 12:55). The uninstall itself worked: both `SKILL.md` files are gone and both manifest entries are absent from `~/.intent/skills/installed-skills.v3.json`. **Nothing is loadable and nothing is orphaned in the dangerous sense** -- the skills do not appear in a session's skill roster. This is residue, not a live skill.\n\n**WHY IT IS WORTH A ROW RATHER THAN A TIDY-UP: IT MAKES A DOCUMENTED VERIFICATION FAIL ON ITS FIRST REAL RUN.** The retirement draft's step 4 says *canon count and installed count agree and are both 23*. Canon is 23. `ls ~/.claude/skills/ | wc -l` is **25**. So the check the draft specifies reports a discrepancy that does not exist, and the only way to see that is to know about this behaviour in advance.\n\n**I MADE EXACTLY THAT MISREAD, THIRTY SECONDS BEFORE CATCHING IT.** I reported `installed: 25` and was about to call it the first orphan the estate has ever had -- which is precisely the failure the draft was written to prevent. **A check that cries wolf on its first real run is one nobody runs twice**, and this one is embedded in the procedure for every future skill retirement.\n\n**AND THE DRAFT ASSERTED THE OPPOSITE BEHAVIOUR.** Its measured-mechanics table records *uninstall removes only what it wrote ... prunes the emptied dirs*. The prune half is not happening. That claim was taken from a read rather than from driving a retirement, which is this estate's own staleness class -- the read was correct about the file removal and wrong about the directory.\n\n**REMEDY, TWO CANDIDATES AND THE CHOICE IS NOT MINE:**\n1. `uninstall` removes the directory when its last recorded file is removed and nothing unrecorded remains. Consistent with ruling 5 (vc, 2026-08-22): *a sync may remove what it INSTALLED; it may not remove what it FOUND* -- an empty directory contains nothing found.\n2. Leave the behaviour and correct the draft's step 4 to count `SKILL.md` files rather than directories, which is the more honest count regardless.\n\n**THEY ARE NOT EXCLUSIVE AND 2 IS WORTH DOING EITHER WAY**, because a count of directories was never the right instrument for a question about skills.\n\n**THE TWO EXISTING DIRECTORIES ARE NOT MINE TO REMOVE.** They are under `~/.claude/`, which is hv's environment rather than the project's, and the retirement draft's own constraint is that the step reaching there needs an explicit human instruction rather than a peer's approval. Recorded here; removal is hv's call.\n\n---\n\n**CAUSE MEASURED AT THE SOURCE 2026-09-03 BY ic, AND IT IS NOT WHAT THIS ROW SUPPOSED.** This row says _the prune half is not happening_. **The prune half IS happening.** `uninstall` calls `prune_empty_dirs(&dir)` at `payload.rs:1131`, where `dir` is the skill's own installed directory.\n\n**THE PRUNER REFUSES EXACTLY ONE DIRECTORY AND IT IS THAT ONE.** `prune_empty_dirs` (`payload.rs:1402`) walks the tree and removes an empty directory only **`if dir != root`** (`payload.rs:1413`). The root is the directory it was handed -- the skill's own -- so it prunes empty SUBdirectories and is structurally guaranteed never to remove the skill directory itself.\n\n**BOTH HALVES ARE CORRECT ABOUT THEIR OWN JOB.** A general-purpose pruner must not delete the root it was given; `uninstall` wants that exact directory gone. **The defect lives only in the relationship, so no reading of either function finds it** -- the seam class (ic W19), third recorded instance and the first found by driving rather than by hunting the shape.\n\n**THIS CHANGES WHAT REMEDY 1 COSTS AND WHAT IT DECIDES.** It is not _implement pruning_; the pruning exists. It is a call-site question: **does `uninstall` promise to remove the directory it emptied, or only the files it wrote?** Ruling 5 (vc, 2026-08-22) -- _a sync may remove what it INSTALLED; it may not remove what it FOUND_ -- says an empty directory contains nothing found, so removing it is permitted. **Permitted is not the same as promised, and that is the decision, which stays hv's.**\n\n**REMEDY 2 IS DONE** -- `intent/st/ST0065/_draft-retirement-mechanics.md` corrected: the false clause replaced with the cause above, the verification step switched from a directory count to a `SKILL.md` count, three dead `skills.rs` citations repointed at `payload.rs`, and the banner corrected -- it claimed **nothing here has been executed** when steps 1-3 had all been run.\n\n---\n\n## BEHAVIOURAL CONFIRMATION OF THE SOURCE READING, AND A RETRACTED DISCRIMINATOR (cc, 2026-09-04)\n\n**ic's cause was measured at the SOURCE. This is its behavioural counterpart, and the numbers match the mechanism exactly.** Driven on the DELIVERED pair -- `intent 3.0.0 (92e4d914a...)`, identified by its own embedded marker -- in two throwaway `HOME`s, counting DIRECTORIES rather than files:\n\n    install --all      31 dirs -> uninstall --all      -> 23 dirs, 0 files\n    install <2 names>   3 dirs -> uninstall <2 names>  ->  2 dirs, 0 files\n\n**8 directories removed in the first, 1 in the second, and in both cases exactly the skill ROOTS survive** -- 23 roots for 23 skills, 2 roots for 2 skills. **That is `prune_empty_dirs`'s `if dir != root` (`payload.rs:1413`) doing precisely what the source says: every empty SUBdirectory pruned, the handed root never.** The behaviour and the source agree with no residual.\n\n**AND A CLAIM OF MINE IS WITHDRAWN BEFORE IT SPREADS.** I reported to vc that the `--all` path EMPTIES the tree while the single-name path leaves the directory, and offered that disagreement as a discriminator this row did not have. **THERE IS NO DISAGREEMENT. Both paths leave the skill roots.** My measurement counted `-type f`, and files reach zero on both paths, so the instrument could not see the thing the row is about. **A file count is not an instrument for a question about directories** -- which is this row's own remedy-2 observation (*a count of directories was never the right instrument for a question about skills*) arriving in the opposite direction and catching the person quoting it.\n\n**WHAT THIS DOES AND DOES NOT CHANGE.** It adds no new defect and moves no remedy: the decision remains hv's, exactly as stated above -- does `uninstall` PROMISE to remove the directory it emptied, or only the files it wrote? What it adds is that **the answer is uniform across every uninstall spelling**, so whichever way hv rules, one call-site change covers `--all` and named-skill alike.\n"
}
