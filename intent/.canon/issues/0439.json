{
  "schema": "intent/issue@3.0",
  "number": 439,
  "slug": "wb-migrate-reads-an-already-rendered-inbox-view",
  "title": "wb migrate reads an already-rendered inbox view as hand-authored and refuses on Intent's own footer",
  "status": "closed",
  "severity": "medium",
  "created": "2026-09-17",
  "closed": "2026-09-17",
  "reporter": "Matthew Sinclair",
  "body": "`intent wb migrate` reads a file that Intent itself already rendered as a hand-authored one. On an inbox it refuses the carry on Intent's own footer; on a board it carries that footer into the model as items.\n\nFound by cc on 2026-09-17 in the all-estate trawl's first pass, on a scratch clone of Courses. Courses registered `hv` at its cutover (`50ec2ed`) without carrying the board, so every file under `hv/` is already a rendered view: `wip.md` is an empty board of `_(none)_` sections, each of the three inboxes is the routing header and the empty sentinel, and all four end in the footer every generated view carries (`---` and `_Generated by Intent v3.0.3 from the whiteboard model ..._`).\n\n- THE INBOX HALF. Migrate names the two footer lines of each inbox as uncarried units (`above the first ## (...) entry ... this line belongs to no message`) and refuses the node on six units nobody wrote.\n- THE BOARD HALF, which the first pass could not show because the inbox refusal came first. Driven by cc on a fresh clone with `--drop-uncarried`: the board's own footer is CARRIED, as two coerced decisions (`carried: [decision] intent/whiteboard/hv/wip.md:34 -- ---` and `:36 -- _Generated by Intent v3.0.3 ...`), beside the six inbox lines dropped. So a rendered board puts the renderer's words into the model as a node's decisions.\n\nThis body first said that a drop \"carried the board's two items\". That sentence was wrong: the two items were the footer. It is corrected here, forward, and the defect is wider than the first body said.\n\nWhat is wanted: one predicate in the renderer's own home (views.rs, beside the banner the renderer writes) that says whether a text is a rendered view, asked by migrate of every file it reads, board and inboxes alike. A rendered file offers nothing: no item, no message, no uncarried unit, and no pre-migration snapshot of generated bytes. The report names each such file on a `rendered:` line, so the skip is said and never silent. A node whose board and inboxes are all rendered carries nothing, says why, and exits 0. One arm: a registered node whose `wip.md` and inboxes are rendered views migrates with no refusal and no drop flag, carries no item, and names the rendered files.\n\n**CLOSED 2026-09-17, by the landing at eac280b50, on vc's word.** Both migrate readers set the renderer's banner aside, as they already set aside `_(none)_` and `_(empty)_`. `views::view_body` returns a view's text before its trailing banner when nothing follows the banner line, and is the one home of both the predicate and the body; `finish` writes the banner by pushing `BANNER_MARKER` itself, so the renderer and every reader keyed on the marker share one prefix. A file that is a view AND offers no unit once its banner is gone is named on a `rendered:` line and never kept as a copy. The banner at the foot of an inbox view with entries, which read into its last message's body, goes with it.\n\n**ONE DELTA FROM WHAT THIS BODY ASKED, RULED BY vc.** The body says a rendered file offers nothing. As built, a rendered view that still offers units once its banner is gone is carried as any board is: on a copied or freshly cloned estate the rendered views can be the only record of rows the store does not hold, and an outright skip would lose them when the carry re-renders the file from empty rows. `rendered:` names only a view offering nothing.\n\n**Guarded by one arm**, `a_node_whose_files_are_rendered_views_carries_nothing_and_names_them` in `intentsvcs/tests/wb_migrate_carries_a_board.rs`. `hv` and `vc` are registered with no board on disk, so both render; `wb migrate hv` without the drop carries nothing, keeps nothing, and names `hv/wip.md` and `hv/inbox.vc.md`.\n\n**Driven on the real board before the landing**, with the banked source's binary on a fresh scratch clone of Courses, its store read immutable. `wb migrate hv` exits 0 with no drop flag, names `wip.md` and its three inboxes on `rendered:` lines, and carries 0 items, 0 messages and 0 snapshots. Judged green with 0438 in one whole-suite run as patch-id 1d3a0d9c.\n"
}
