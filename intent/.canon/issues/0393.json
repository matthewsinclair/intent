{
  "schema": "intent/issue@3.0",
  "number": 393,
  "slug": "a-large-indexed-directory-vanishing-costs-a-refr",
  "title": "a large indexed directory vanishing costs a refresh one delete per path",
  "status": "open",
  "severity": "low",
  "created": "2026-09-14",
  "reporter": "Matthew Sinclair",
  "body": "Measured 2026-09-14 by dc in a private worktree: the index held a directory of build output (a worktree's in-tree cargo target, ~190k paths, 190507 of them `index_file` skip rows) under the index scope. The directory was removed, and the next whole-scope refresh (`index_refresh(None)`, reached through `intent mcp`'s search) ran at ~90-98% of one core for 8+ minutes and did not finish before it was stopped: `changed_under` reports every vanished path, and `apply_index_changes`, `replace_sections_for` and `replace_symbols_for` each delete per path (with FTS5 secure-delete since rung 26). A wholesale `intent index rebuild` of the same tree took about a minute.\n\nNot fixed here. Two questions: whether a refresh should switch to a wholesale replace past some count of removals; and whether an in-tree cargo target dir that is not gitignored (a private worktree's `target-dc/`) should ever be inside the index scope at all.\n\n"
}
