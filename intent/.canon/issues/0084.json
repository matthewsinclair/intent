{
  "schema": "intent/issue@3.0",
  "number": 84,
  "slug": "one-fact-refused-in-two-homes-and-only-one-retirement",
  "title": "A retired refusal left two survivors: ingest refuses a thread on a label the collector had already stopped honouring, and the finding class still hands out a remedy that destroys the artefact",
  "status": "open",
  "severity": "medium",
  "created": "2026-08-25",
  "reporter": "Matthew Sinclair",
  "body": "**Found by cc 2026-08-25 while building AC-08.5's last closing condition, and driven rather than concluded.** `project.rs:896` constructs `Attachment::opaque` from a non-UTF-8 file. **The capability exists and was built on purpose.** `project.rs:889` records why its own refusal was retired, verbatim: *this arm used to REFUSE, with \"not valid UTF-8, so it cannot be carried as text\", and the refusal was right for as long as canon had nowhere to put the bytes. It now has one, and a refusal here would leave the file uncarried, therefore unrecoverable, therefore pinned to disk forever.*\n\n**`sync.rs:404 inspect()` STILL REFUSES THE SAME FILE FOR THE SAME REASON, AND IT RUNS FIRST.** Driven on a non-UTF-8 `.sh` in a real repo:\n\n    $ intent3 sync --to-store ST0001\n    error: could not read the committed canon\n      caused by: residue: intent/st/ST0001/mangle.sh -- unknown-file-shape -- not valid UTF-8\n    canon attachments: NONE\n\n**SO THE OPAQUE BRANCH IS UNREACHABLE THROUGH THE SHIPPED SURFACE.** Built deliberately, commented as the reason a sibling refusal came out, and no door reaches it.\n\n**THE CLASS IS THE TWO-HOMES SHAPE ON A REFUSAL RATHER THAN ON A VALUE, WHICH IS NEW.** One fact -- *this file is not UTF-8* -- refused in two places; one home was retired when the fact stopped meaning *unusable*, the other was not, and nothing compared them. **A retirement that does not enumerate the other homes of the fact it retires leaves the fact still enforced, by a stranger -- and the comment explaining the retirement reads as though it worked.** `inspect`'s own doc is honest about its scope (*everything that makes a file unreadable-as-what-it-claims-to-be*) and **for an attachment-eligible file, not-UTF-8 no longer makes it unreadable-as-what-it-claims-to-be.**\n\n**LIVE IMPACT, AND IT IS WORSE THAN A MISSING ROUTE:** a legitimate attachment-eligible file that is not UTF-8, sitting on disk, **fails `sync --to-store` for its WHOLE THREAD.** That is an active refusal of a file the model was deliberately taught to carry, not an absence.\n\n**NOT A BLOCKER ON AC-08.5 AND DELIBERATELY SEPARATED FROM IT (vc ruling).** AC-08.5 asks whether the FIELD is settable through the mutation surface; this is about **what `sync` classifies as residue**, which is a different question with blast radius past the criterion. cc declined to change it inside a closing condition -- the same rule that kept them off `Node` and off `status_reason`'s semantics, three for three -- and is instead building `st attach` accepting binary, which reaches `Attachment::opaque` through a new door without touching residue classification.\n\n**THE BEHAVIOUR CALL IS hv's:** should `inspect()` stop treating not-UTF-8 as residue for an attachment-eligible extension? **REMEDY IS NOT OBVIOUS AND SHOULD NOT BE GUESSED** -- narrowing `inspect` decides what a residue IS, and `ATTACHMENT_EXTENSIONS` is `[\"md\",\"txt\",\"sh\"]`, so the population is small and the boundary is exactly where the argument lives.\n\n**METHOD NOTE, BECAUSE IT NEARLY WENT THE OTHER WAY.** cc's first probe used a `.dat` file; `ATTACHMENT_EXTENSIONS` does not include it, so the scanner ignored it by design and returned **a green that meant nothing**. cc re-drove with a `.sh` and got the truth. **An instrument that cannot reach its subject returns a clean answer** -- and cc was one report away from filing vc's `blob` refusal as a sixth false remedy pointing the other way.\n\n---\n\n**COMPONENT RESOLVED 2026-08-25, AND IT IS NEITHER OF THE TWO FIRST NAMED (cc traced it; ic drove it, conceded, and asked for this correction; vc confirmed at source before writing it).** Not `sync.rs:404`, and not *a label consulted as a filter and never as a refusal*. **`ingest.rs:533` and `:594`, both arms:**\n\n    let findings: Vec<Finding> = entries.iter()\n      .filter(|e| e.state == FileState::Unparsed)\n      .flat_map(|e| e.findings.iter().cloned()).collect();\n    if !findings.is_empty() { return Err(Refusal::new(findings).into()); }\n\n**THE `.filter()` SELECTS WHAT TO REFUSE ON, AND ITS OUTPUT IS THE REFUSAL'S CONTENT.** *Consulted as a filter, never as a refusal* is true of the combinator's NAME and false of the code four lines down.\n\n**cc's SENTENCE IS THE RECORD AND NEITHER EARLIER ACCOUNT WAS: _the collector CARRIES the file successfully, and ingest then refuses the whole THREAD on a file-index label the same bytes earned elsewhere. Two components, one fact, opposite verdicts, and the losing one runs last._** That explains vc's repro exactly -- `canon attachments: []` while `collect_attachments` had just succeeded. **Both first accounts were WRONG IN THEIR ADDRESS AND RIGHT IN THEIR INSTINCT:** cc named the wrong component for a real refusal; ic named the right component for a wrong conclusion.\n\n**AND THE RETIREMENT LEFT TWO SURVIVORS, NOT ONE (cc).** `project.rs:889` retired the UTF-8 refusal because canon gained somewhere to put the bytes. **The refusal survived in `ingest`. The REMEDY survived in `finding.rs:267`** -- the generic text for the whole `UnknownFileShape` finding class: *move or rename it -- a modelled directory carries only the artefacts Intent writes.* **A remedy attached to a CLASS is emitted for every member, and this member stopped belonging; nothing at the emission site knows the class has stopped meaning what the remedy assumes.**\n\n**IT IS THE WORST OF THE SIX FALSE REMEDIES FOUND ON 2026-08-25 AND ic NAMED WHY: IT IS THE ONLY ONE THAT INSTRUCTS THE OPERATOR TO REMOVE THE ARTEFACT.** The other five sent people to a route that did not exist. **This one sends them to destroy the input** -- and `project.rs:889`'s own comment says the retirement happened so that *a refusal here would not leave the file uncarried, therefore unrecoverable, therefore pinned to disk forever.* **The remedy tells the operator to do the thing that comment was written to prevent.**\n\n**METHOD NOTE, ic's, RECORDED AGAINST THEMSELVES: _I drove the wrong three lines carefully, and that is what made it authoritative._** cc had already lost a wrong attribution to two nodes; ic's correction read as settled FOR BEING DRIVEN. **A driven wrong answer outranks an undriven right one in this estate -- usually the correct ordering, and exactly what makes this failure mode expensive.**\n"
}
