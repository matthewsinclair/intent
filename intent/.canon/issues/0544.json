{
  "schema": "intent/issue@3.0",
  "number": 544,
  "slug": "the-shell-critic-asks-nothing-of-a-sourced-bash",
  "title": "The shell critic asks nothing of a sourced bash library with no shebang, so the release machinery's seven .lib files are never linted",
  "status": "closed",
  "severity": "medium",
  "created": "2026-09-23",
  "closed": "2026-09-23",
  "reporter": "Matthew Sinclair",
  "body": "Found by cc on 2026-09-23 while verifying the 3.2.1 release notes against the landed tree. It is measured, not inferred.\n\n**What happens.** 0536 made the shell critic reach a script by its shebang as well as by the rules' globs. A sourced bash library carries neither. The seven tracked `.lib` files under `bin/.devbin/cmd/shared/` (artefact, clone, currency, measure, releasebuild, runtime, sharedtarget) have no `#!` line, and no shell rule's glob matches `.lib`. So the gate asks no rule of them, ever. 0536's honest verdict now names such a file as unasked rather than calling it clean, but nothing lints it. These libraries are the release and currency machinery the cut itself runs through.\n\nSix of the seven open with `# shellcheck shell=bash`, which is shellcheck's own declaration of a file's dialect.\n\n**What the fix has to do.**\n- The runner reads `# shellcheck shell=<dialect>` as a dialect marker beside the shebang, in 0536's one table, with no rule file changed.\n- The seventh library gains the directive in the same change.\n- Whatever the newly reached libraries raise is fixed, or exempted with a stated reason, in the same change, so the gate does not start refusing the next commit that touches them.\n- It is a `bin/.devbin` edit, so the macOS bats are owed.\n\nClosed (cc, 2026-09-23, on vc's PASS), in one bank with 0545 on vc's ruling:\n\n- LANDED at eab995890 from refs/bank/cc/0544-0545/on-3be665d61 (patch-id d2e74c897, the same before and after the commit), judged in one two-bank run with ic's 0543 on 964ef3296.\n- The critic reads a `# shellcheck shell=<dialect>` directive where shellcheck reads one, in the comment block at the top of the file, and gives it shellcheck's precedence over a shebang. It feeds 0536's one dialect table, now `DIALECTS`, so no rule file changed.\n- `bin/.devbin/cmd/shared/sharedtarget.lib`, the one library without the directive, gains it.\n- Driven through the built critic: the seven `bin/.devbin/cmd/shared/*.lib` are all asked (files_asked 7, unasked none) and raise nothing, so nothing needed a fix or an exemption. The macOS bats ran green in the judging run.\n- Proved by `critic::tests::a_shellcheck_directive_declares_the_dialect_of_a_file_with_no_shebang` and `critic_surface::a_sourced_library_is_reached_by_its_shellcheck_directive`, whose control is the same library without the directive, named as asked nothing.\n- NOT DEPLOYED until the second `bin/devbin build all`.\n"
}
