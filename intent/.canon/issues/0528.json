{
  "schema": "intent/issue@3.0",
  "number": 528,
  "slug": "v3-s-todo-view-dropped-the-generator-marker-so-u",
  "title": "v3's todo view dropped the generator marker, so Utilz's todo overwrites Intent's intent/todo.md instead of refusing it (Finding C)",
  "status": "closed",
  "severity": "medium",
  "created": "2026-09-23",
  "closed": "2026-09-23",
  "reporter": "Matthew Sinclair",
  "body": "Found on 2026-09-23 by the fleet census hv ruled on 2026-09-22 (Intent's hv decision 28, item 10). The census looked for Finding C, \"the v3 render blinded a project-side gate\", across the 19 estates that carry Intent, other than Intent itself and the archived Intentv2. Baize's own case was the positive control. The ruling was that an Intent issue is filed only if another estate is bitten. Utilz is.\n\nSeen, driven in a scratch project with Utilz's own `opt/todo/todo` and the real views:\n\n- With a v2-format `intent/todo.md` (frontmatter `generator: intent todo`), `todo add` run from the project's `intent/` directory refuses at rc 1: `refusing to overwrite './todo.md' -- managed by 'intent todo', not 'utilz todo'`. The file is unchanged. This arm is the control.\n- With Intent's real v3 `intent/todo.md`, the same command answers rc 0 and rewrites the file in Utilz's format, starting `generator: utilz todo`. Intent's generated view is overwritten by a second writer.\n\nThe mechanism: v2's `intent todo` stamped its view with `generator: intent todo` frontmatter, and v2 carried a `guard_foreign_todo` that refused to overwrite a todo.md another generator owned. Utilz's `todo` mirrors that guard (`opt/todo/todo`, `guard_foreign_todo`, which says it \"Mirrors Intent's guard_foreign_todo\"). It refuses a file whose leading frontmatter names another generator, and it proceeds on a file with no frontmatter, which it takes for its own pre-marker format. v3's todo view (`intentsvcs::views::todo`) opens with `# TODO` and no frontmatter, and v3.0.0's tag already carries it that way, so Utilz's guard has been blind to Intent's view since the port. Utilz's test of the guard (`opt/todo/test/todo.bats`, `_seed_intent_todo`) seeds the v2 format and stays green: it tests a format Intent no longer writes. That is Finding C's exact shape, as in Baize.\n\nThe rest of the census, each candidate read rather than counted: Cdtempl hit the same frame move at the port, but its guard asserts a nonzero total, so it went red at once and now reads the canon extracts. That was caught, not blinded. Gtools' scrub gate gates every staged path but `.history` and vendored directories, so the v3 files are inside it. Laksa's own whiteboard guards read the rendered views, and the renderer still emits what they key on. Lamplight's `live-doc-budget` measures sizes, which v3 can only make fire more. Every other hit was a path named in a comment.\n\nExpected: a tool that owns its own todo.md can tell Intent's generated view apart from its own, as it could before v3.\n\nRuling (vc under hv's pen, 2026-09-23): in 3.2.1, as an outstanding item under hv's standing word. The fix is on Intent's side, because the marker is the contract both tools were built on and v3 is the side that dropped it. The todo view renders `generator: intent todo` frontmatter again, with the view's own tests and doctor's view check updated to match. Utilz's guard then refuses v3's view unchanged, and its stale fixture becomes true again without a Utilz change.\n\nBehaviours, one line each:\n\n- `intent todo update` writes `intent/todo.md` opening with frontmatter that carries `generator: intent todo`.\n- Utilz's `todo add`, run where Intent's view is `./todo.md`, refuses and leaves the file unchanged, driven as above.\n- `intent doctor` reports no skew on a view carrying the frontmatter.\n\n**LANDED, NOT DEPLOYED (dc, 2026-09-23, on vc's word).** At 7507221d6 on 8364b5056. The bank was refs/bank/dc/0528 (blob 98e691edb, base a43997415), re-cut on e7d491946 as refs/bank/dc/0528-on-e7d491946 with the same blob; `git apply --stat` reads 3 files changed, 109 insertions. The patch-id was read from the bank blob and again from the landed commit, bb16db0c1 both times: the id vc judged. It rebased cleanly onto 0523 stage 2, which also touches `views.rs`. `intent/todo.md` was deliberately not regenerated in the landing, because the installed pair's gate reads the marker as skew until the next `bin/devbin build all`.\n\n**THE FIX.** `views::todo` opens with `views::TODO_FRONTMATTER`, v2's exact bytes (`---`, `generator: intent todo`, `---`, then a blank line), as `bin/intent_todo`'s `generate()` wrote them. `intent todo` prints it too, because `todo update` writes exactly what the terminal view prints. `views::authored_text`, the one home for what a hand edit could have touched, drops that exact block first, so a todo view an older v3 wrote reads in `intent doctor` as a stale render, which is advisory and blocks no estate's commits at the upgrade. A marker naming any other generator is still skew.\n\n**THE JUDGING.** dc's judging run at e7d491946, in a private worktree with its own cargo-cleaned target and an isolated HOME, on CI's rust lines as `rust.yml` spells them: 3007 passed, 0 failed, 5 ignored; bats pre_commit_hook 30/30, pre_commit_shim 11/11, macos_release_assets 23/23, artefact_source_commit 2/2. The four new arms: the view opens with v2's bytes; an older v3 view is a stale render and nothing actionable; a foreign marker is skew; a marked view carries no finding. A first run was VOID and is not evidence: the worktree had been moved from tmp/wt-0527, so testkit's compile-time path named a deleted directory and 639 arms panicked on its root search. The re-run followed a `cargo clean`, and vc passed it.\n\n**THE UTILZ ARM, DRIVEN.** Scratch projects and an isolated HOME, then Utilz's own `opt/todo/todo add` with `intent/` as the working directory. On the view the built binary rendered, Utilz answered rc 1, `refusing to overwrite './todo.md' -- managed by 'intent todo', not 'utilz todo' (an Intent project owns it)`, and the file was unchanged. The control, on the installed 3.2.0 pair's view, answered rc 0 and rewrote the file as Utilz's own, opening `generator: utilz todo`: the defect.\n\n**DEPLOYMENT.** `native/rust/` deploys on neither save nor commit (0518). Views carry the marker from hv's next `bin/devbin build all`, as each is next written or refreshed with `intent todo update`; until then an older todo view reads as a stale render, which blocks nothing.\n"
}
