{
  "schema": "intent/issue@3.0",
  "number": 464,
  "slug": "release-the-footer-only-view-classifier-drops-bu",
  "title": "release: the footer-only view classifier drops bullet and blank lines, so a real view change is folded into the release commit",
  "status": "closed",
  "severity": "high",
  "created": "2026-09-19",
  "closed": "2026-09-19",
  "reporter": "Matthew Sinclair",
  "body": "The release driver's footer-only view classifier (`bin/.devbin/cmd/build.d/release:1156`) selects changed lines with `grep -E '^[-+][^-+]'`. In a unified diff, a markdown bullet line reads `-- item` or `+- item`, and an added or removed empty line reads `+` or `-` alone, so none of them is selected. A view whose only real change is to a bullet or a blank line is therefore classed footer-only and folded into the release commit as if it were the version footer. critic-shell drove it on a sample. There is also no `pipefail` on the `git diff` pipeline, so a failed diff reads as footer-only too.\n\nFix: select every changed content line (`^[-+]` excluding only the `---`/`+++` file headers, which `-U0` still prints), and fail on a failed diff. S. vc's next-line list, item 4.\n\n\nCLOSED 2026-09-19, by its landing in dc's train B on vc's word (bank a32fc538a, patch-id edc017dea7e1, judged green on the train's bats log refs/bank/dc/trainB/bats.log: every arm green on the fixed script and the new arm red on the unfixed one).\n"
}
