{
  "schema": "intent/issue@3.0",
  "number": 426,
  "slug": "a-project-s-own-git-hook-guards-are-lost-on-ever",
  "title": "A project's own git hook guards are lost on every fresh clone, silently; Intent's hook carrier wires only Intent's",
  "status": "open",
  "severity": "medium",
  "created": "2026-09-16",
  "reporter": "Matthew Sinclair",
  "body": "Routed from devbin#0078 on hv's ruling (2026-09-16, to devbin/vc in session), which chose between devbin, a devbin detector, and Intent: **Intent owns hook wiring.**\n\n**The defect.** `.git/hooks/` is untracked in every git repository by construction. Intent already writes `.git/hooks` in every estate (`pre-commit.intent`) and runs its own guards from there. A project's OWN guards, wired by hand into `.git/hooks/pre-commit`, are lost on every fresh clone, and nothing prints to say so. The clone still runs Intent's guards, so its commits are gated -- by fewer guards than the project's own checkout -- and the difference cannot be seen from the clone.\n\n**Measured at Lamplight (reported 2026-08-28, devbin#0035, instance 2).** Four project guards were hand-wired through `.git/hooks/pre-commit`. Their bodies are tracked in `bin/hooks/`, so only the wiring is lost, which makes the loss quieter rather than smaller. One guard, `whiteboard-inbox-guard`, vanishes outright. Every consumer that hand-wires hooks has the same gap.\n\n**The ask.** Intent's hook carrier wires the project's own guards as well as its own, from a tracked declaration, so a fresh clone runs the same set as the original checkout. Failing that, it at least reports a declared guard that `.git/hooks` does not run. The shape is Intent's to choose. devbin's `precommit` builtin runs a gate and never wires a hook (devbin#0035, ruled the same day), so there is nothing on the devbin side to reconcile.\n"
}
