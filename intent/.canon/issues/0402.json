{
  "schema": "intent/issue@3.0",
  "number": 402,
  "slug": "a-thread-s-objective-and-context-lose-their-trai",
  "title": "A thread's objective and context lose their trailing newline when a store is populated from the realised tree, so the canon differs by one byte per field from the live store's",
  "status": "closed",
  "severity": "low",
  "created": "2026-09-15",
  "closed": "2026-09-16",
  "reporter": "Matthew Sinclair",
  "body": "Measured 2026-09-15 on train 6's judge, in a scratch worktree with an isolated HOME: after `intent sync --to-store` with the worktree's own binary (c6445c4c0 plus dc's 0321), staging `intent/.canon` picked up `intent/.canon/st/ST0056.json` and `intent/.canon/st/ST0075.json`, although 0321 changes neither thread's objective or context.\n\nThe whole difference, compared byte for byte and then as parsed JSON:\n\n- ST0056: `objective` and `context` end with `\\n` in HEAD's canon and in the bank's canon (built from a store whose fields were written through the verbs); the canon the worktree's store produced from the realised tree ends both without it. No other key differs.\n- ST0075: `objective` ends with `\\n` in HEAD's canon and without it in the worktree's. No other key differs.\n\nSo the same field serialises two ways depending on how the store holding it was filled. Which verb put the newline into the live store was not measured; that the tree import does not reproduce it was.\n\nConsequence: canon churn with no content change. Anything that fills a store from the tree (`sync --to-store`, a fresh store, a train judge) rewrites those canon files, and a lander's \"the canon moved only by X\" check reads the rewrite as movement. It cost the judge that found it a comparison to rule it out.\n\nRemedy: one normalisation for the round-tripped `## Objective` and `## Context` fields, applied at every door that writes them, the tree import included, with the canon written from the normalised value. Which normalisation is the fixer's call; the defect is that there are two.\n\n**CLOSED 2026-09-16, landed on vc's word, code only.** `model::authored_section` is the one stored form of a thread's `objective` and `context` and a work package's `objective`: newlines at either end are removed, and nothing else is touched. It applies where text enters the model: deserialising (canon, `put`, the setter), hydrating from the store, and the `info.md` read-back, which no longer trims away an author's first-line indentation or trailing spaces. Guarded by `tests/authored_sections_have_one_form.rs` and the model laws. **Canon still holding the other form is rewritten once, in the quiet window** (vc's ruling): after `devbin build all`, intentd stopped, `intent sync --to-disk` with the new pair, and `refs/bank/cc/0402-canon-check` (`git cat-file -p` it to a file and run it with the base commit) proves intent/.canon moved only by these newlines, the fields it counts at that base included, before that commit and before the daemon restarts.\n"
}
