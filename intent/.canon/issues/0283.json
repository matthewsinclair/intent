{
  "schema": "intent/issue@3.0",
  "number": 283,
  "slug": "doctor-and-organize-both-read-divergence-as-auth",
  "title": "doctor and organize both read divergence as authored-content-at-risk, and neither entertains the store being newer",
  "status": "closed",
  "severity": "medium",
  "created": "2026-09-08",
  "closed": "2026-09-11",
  "reporter": "Matthew Sinclair",
  "body": "**`intent doctor`'s `view-skew` remedy names a verb that operates on a different artefact from the finding, and asserts a cause that is false for at least one real case.** Reported from Utilz by utilz-vc, who drove the remedy and got no change. Same family as `0282`: found by the printed remedy not working.\n\n**VERIFIED HERE, FROM THE SOURCE.** `finding.rs:420` emits:\n\n    `intent sync --to-disk` regenerates the views from the store, DISCARDING the hand edit -- copy anything you meant to keep out first\n\n`intent sync --help` says the verb syncs *this machine's store with the committed extract*, and `--to-disk` *writes the store out to the committed extract*. **The committed extract is `intent/.canon/*.json`. The finding is about a rendered markdown VIEW.** utilz-vc ran it: `ok: extract written for 16 thread(s)`, view unchanged, doctor still 1 finding, `git status` clean.\n\n**AND THE MESSAGE ASSERTS A CAUSE THAT WAS FALSE IN THE OBSERVED CASE.** It says the file was hand-edited or written by an older version. utilz-vc reports every change went through the CLI -- `ac new`, `at new`, `at green` -- and the file was never opened. **Their control is what makes this a mechanism rather than a denial:** `ST0015/acceptance.md` carries `AC10` and reports NO skew; `ST0016/acceptance.md` is missing `AC08` and reports skew. The difference is WHEN the row landed -- ST0015's while the thread was open and regenerated at close, ST0016's AFTER the thread was Completed, and nothing regenerated it again.\n\n**SO THE THIRD CAUSE THE MESSAGE DOES NOT ADMIT IS SEQUENCING: adding a criterion to a CLOSED thread updates the model and leaves the rendered view stale.** A remedy that offers only *your hand edit will be discarded* to someone who made no hand edit tells them the tool has misunderstood them, which is how a remedy stops being read.\n\n**THE OPEN QUESTION IS SETTLED, AND THE ANSWER INVERTS IT (utilz-vc, driven on an estate that HAS `.intentfiles`).** `organize --apply` does not refresh a closed thread's view. **It REMOVES it.** Utilz's `intent/.intentfiles` declares nothing -- 6 lines, all comment, 0 STEELTHREAD lines, its header stating on hv's order that *every OPEN thread is declared, and nothing else* -- and every Utilz thread is closed. The preview: `3 to remove, 1 REFUSED`, with `ST0015/acceptance.md`, `ST0015/info.md` and `ST0016/info.md` all marked `to-remove`.\n\n**So `organize` dehydrates an undeclared artefact, which is correct behaviour and the exact opposite direction from regenerating.** The fix is therefore NOT a reworded remedy pointing at `organize` -- that verb would delete the file rather than refresh it. **There is no verb that refreshes a closed thread's view, and that is the gap.**\n\n**AND THE REFUSAL IS BACKWARDS IN THE SAME DIRECTION, WHICH IS WHAT UNIFIES THIS.** `organize.rs:242`, verbatim: *refusing to dehydrate {path}: the file on disk differs from what the store renders, so removing it would destroy {bytes} byte(s) the store does not carry.* **It reports the DISK file's size as bytes the store lacks, computed from a mere difference.** In the observed case disk is 7168 and the store renders 8643 -- **the store is AHEAD.** The disk copy is a stale render missing `AC08`; it carries no authored content at all. Byte-for-byte the refusal is true; in substance it is false.\n\n**BOTH MESSAGES SHARE ONE WRONG ASSUMPTION, AND IT IS THE THING TO FIX RATHER THAN EITHER WORDING: divergence is assumed to mean AUTHORED CONTENT AT RISK.** Doctor says *edited by hand or written by an older version*; organize says *would destroy bytes the store does not carry*. **Neither entertains the store being NEWER**, which is precisely what happens when a row is added to a closed thread. One cause, two messages, both misdescribing it in the same direction, both then offering remedies calibrated for a hand edit that does not exist.\n\n**THE FIX THAT SERVES BOTH: compare DIRECTION, not difference.** A skew check that distinguishes store-ahead from disk-ahead would give each message the right thing to say, and would let `organize` proceed safely in the store-ahead case instead of refusing.\n\n**utilz-vc did NOT run `organize --apply`** -- on that tree it would remove three files to answer a diagnostic question, and dehydrating Utilz's closed threads is hv's call rather than a side effect of a diagnosis.\n\n**THIS IS THE CLOSED-THREAD ASYMMETRY IN THE RENDERING LAYER**, and utilz-vc's framing is the reason it is filed rather than absorbed: **a stale VIEW is what a human reads while the MODEL is what the gate reads, and the two disagreeing silently is how a closed thread's evidence goes stale with nothing reporting it.** The estate spent 2026-09-08 on the same asymmetry in the STATUS layer -- a gate for the live case and none for the closed one.\n\n**IMPACT IS LOW AND THAT IS MEASURED, NOT ASSUMED.** The model is correct: `ac status ST0016` reads 8/8 PASS from the store and `at lint` reads 9 of 9 conforming. Only the markdown is behind, and Utilz 2.8.0 released with the model right.\n\n**utilz-vc DID NOT WORK AROUND IT** -- no hand edit, no reopen-and-reclose to force regeneration, and the finding left standing rather than suppressed. Utilz `doctor` will report 1 finding until this is fixed, and that is the honest state.\n"
}
