{
  "schema": "intent/issue@3.0",
  "number": 154,
  "slug": "no-entity-s-authored-prose-can-be-edited-after-c",
  "title": "no entity's authored prose can be edited after creation: issue bodies, issue titles and work package bodies all have write-once doors",
  "status": "open",
  "severity": "high",
  "created": "2026-08-30",
  "reporter": "Matthew Sinclair",
  "body": "**THE CLASS, FILED ONCE, BECAUSE THREE PER-ENTITY NUMBERS ARE THREE RECORDS OF ONE DEFECT THAT DECAY INDEPENDENTLY.** Two members are already open and this row does not duplicate them -- it names what they are instances of, and it is the row a general fix closes.\n\n**MEASURED 2026-08-30 by ic, driven against the debug binary.**\n\n| entity | prose field | writing verbs | door |\n| ------ | ----------- | ------------- | ---- |\n| issue  | `body`      | `add` only, at creation      | none after creation -- `0090` |\n| issue  | `title`     | `add` only, at creation      | none after creation -- `0151` |\n| wp     | body        | `new` only, at creation      | **none -- this row** |\n\n`intent wp` is `new`, `start`, `done`, `reopen`, `cancel`, `reinstate`, `unstart`, `rescope`, `list`, `show`. **Every one of those moves STATUS or SIZE; not one writes prose.** `intent issues` is `list`, `add`, `show`, `close`, `open` -- `add` takes `--body`/`--from` and allocates the number itself, so **no writing verb anywhere takes an id**, and an existing row cannot be addressed at all (measured by vc in a throwaway fixture, killing the hypothesis that `0131`'s upsert was an accidental update door).\n\n**AND THE FILE IS NOT THE DOOR EITHER.** `intent/st/STxxxx/WP/NN/info.md` is a GENERATED view whose own footer says `Do not edit this file -- it is rendered from the model, and intent doctor reports any hand-edit as skew`. The store is the SSOT, so a hand-edit repairs the PROJECTION and leaves the SOURCE holding the thing that regenerates it -- this project has measured that a later status move is enough to restore what a hand-prune removed.\n\n**THE LIVE CONSEQUENCE, WHICH IS WHY THIS IS NOT THEORETICAL.** `ST0064/WP-01`'s body specifies `GET /_status` returning JSON, carried from the Geodica handoff on 2026-08-26. hv ruled GraphQL three days later and `AC-01.3` supersedes it BY NAME. **The handoff IS the drop-in example, so a reader following the body lands on an endpoint that will not exist.** vc raised the correction and assigned it to ic; ic could not make it. **A superseded design that stays legible in the artefact is a design someone builds from**, and the correction is not merely undone -- it is unreachable through the surface, which turns a task into a blocked one.\n\n**WHAT CLOSING IT REQUIRES.** An edit door reaching authored prose on an addressed entity -- at minimum `issues edit <id>` for title AND body, and a `wp` verb for the body. **A fix reaching only one field or only one entity closes the instance already filed and leaves the case that prompted the filing exactly where it is**, which is the shape this estate keeps meeting: a remedy sized to the instance rather than to the class.\n\n**`0090` IS AN INSTANCE OF ITSELF AND CANNOT BE WIDENED TO COVER THIS.** It is titled *issues add creates a body that no verb can write*, it is OPEN, and **its own body is empty -- because no verb can write it.** So the register cannot describe its own gap in the row that names it, which is precisely why the class needs a row that is not one of its own victims. This one has a body because it was filed through `add --from`, at creation, which is the only door that exists.\n\n---\n\n**UPDATE 2026-08-31 by dc, on vc's invitation: THIS ROW'S PREDICTION CAME TRUE, AND THE HALF-FIX IT WARNED ABOUT IS THE ONE THAT SHIPPED.** The paragraph above asks for *\"an edit door reaching authored prose on an addressed entity -- at minimum `intent issues edit <id>` for title AND body, and a `wp` verb for the body\"*, and warns that *\"a fix reaching only one field or only one entity closes the instance already filed and leaves the case that prompted the filing exactly where it is\"*. **`intent issues edit` was then built with `--body` and `--from`, and no `--title`.**\n\n**DRIVEN 2026-08-31 in a throwaway fixture, not against this register**, against `intent 3.0.0 (553ac304c86b508151b8d41ec37c78f751b537b2)`, sha256 `d0568cb44d1e225d`:\n\n    $ intent issues edit 0001 --body \"REWRITTEN body -- the door works\"\n    ok: issue 0001 body written                                   [rc=0]\n\n    $ intent issues edit 0001 --title \"a corrected title\"\n    error: unexpected argument '--title' found                    [rc=1]\n\n    $ intent wp --help\n    new start done reopen cancel reinstate unstart rescope list show\n\n**So the table above now reads three different ways in one row-set.** The `body` door EXISTS on this tree, which closes `0090`'s and `0179`'s half. The `title` door does not, so `0151` stands untouched. **`wp`'s ten verbs are still every one of them status or size, and not one writes prose**, so this row's own third line stands exactly as filed. The live consequence named above -- `ST0064/WP-01`'s body specifying an endpoint hv superseded -- is therefore still unreachable through the surface, which was the case that prompted the filing.\n\n**AND THE FIX'S REACH IS ITSELF UNDECIDABLE FROM THE REGISTER, WHICH IS `0191`.** On the published tag -- `/opt/homebrew/Cellar/intent/3.0.0_1/bin/intent`, `intent 3.0.0 (80d8b2ca4d897dd15ecc051e7b5b682eed7c8a95)`, the cut commit -- `intent issues add` does not accept `--body` at all and `intent issues edit` answers `error: unrecognized subcommand 'edit'`. **So the whole class is open on what a user installs and half-closed on `main`, and the question \"is `0090` fixed?\" has two correct answers.** No field on a register row says which build the row describes. **NOT PROPOSING TO CLOSE `0090` OR `0179` HERE**: an undecidable disposition is the defect `0191` names, not a licence to guess which side to record.\n\n**WHAT WOULD CLOSE THIS ROW IS UNCHANGED AND IS NOW ONE FLAG.** `--title` on `intent issues edit`, plus a `wp` verb reaching the body. The first is the same shape as the `--note` gap on `at green` / `at na`: an existing verb, one argument short of the thing it was built for."
}
