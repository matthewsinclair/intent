{
  "schema": "intent/issue@3.0",
  "number": 392,
  "slug": "canon-ignore-guard-sh-reads-its-rules-from-the-w",
  "title": "canon-ignore-guard.sh reads its rules from the worktree and its attribution from the index, so a staged ignore rule reaching intent/.canon/ can pass",
  "status": "open",
  "severity": "medium",
  "created": "2026-09-14",
  "reporter": "Matthew Sinclair",
  "body": "Raised upstream on hv's ruling of 2026-09-14 (relayed by laksa-vc), filed by vc at 2026-09-14 17:03Z.\n\n`lib/templates/hooks/canon-ignore-guard.sh` reads its RULES from the worktree and its ATTRIBUTION from the index. `git check-ignore -v --no-index` consults the worktree's `.gitignore`; `added_pairs` comes from `git diff --cached`. Measured by Laksa's dc (2026-09-12) with a `.gitignore` carrying `intent/.canon/` staged in a throwaway index: the guard's own diff reports `+intent/.canon/` at line 131 and `check-ignore` reports no match at all. The `source:lineno` comparison then compares line numbers across two different documents whenever index and worktree differ, which on a shared tree is routine (`git commit --only` plus a re-staging formatter is the house pattern). So a staged rule that would un-commit the canon estate can pass the guard.\n\nFix: split the guard so both halves read one document, the staged `.gitignore`: write the index blob out and run `check-ignore` against it (a temporary work tree, or the blob as the one rules file), and attribute added lines against that same blob. Laksa's arm for this guard (dc) is held on the split; the harness shape there (a temp-repo fixture versus driving the body) follows the fix. Lane: dc.\n"
}
