{
  "schema": "intent/issue@3.0",
  "number": 315,
  "slug": "sync-to-store-reads-an-unstaged-deletion-of-a-ca",
  "title": "sync --to-store reads an unstaged deletion of a carried attachment as an edit, and claims a refusal no gate performs",
  "status": "closed",
  "severity": "medium",
  "created": "2026-09-13",
  "closed": "2026-09-14",
  "reporter": "Matthew Sinclair",
  "body": "**`sync --to-store` reads an unstaged deletion of a carried attachment as an edit, and then claims a refusal no gate performs.** Measured by courses-vc on the nested Courses estate, 2026-09-13, after carrying seven v2 bucket files into their threads with `st attach`, byte-verifying each against the committed original, and deleting the files from disk so the closed threads could dehydrate: a later `intent sync --to-store` printed `warning: 7 attachment(s) carry bytes no commit contains, and this run takes them into canon`, each path tagged `edited in the working tree and not staged`, then `commit them first if canon should name bytes a reader can obtain -- this run does not refuse, and the commit gate will`. HEAD held byte-identical content for every one of the seven.\n\n**Two defects in one warning.** (1) The instrument is `sync::uncommitted`, which asks `git diff-files --name-only` (worktree against index) and labels every hit `Modified`. A tracked file deleted in the worktree and not yet staged is a diff-files hit, so a deletion is reported as an edit and the bytes are said to be in no commit when the commit that holds them is HEAD. Staging the deletion empties the list, which is the tell: the state being reported is \"not staged\", not \"not committed\". (2) The closing clause, _the commit gate will_, names an arm that does not exist: the pre-commit template runs `intent doctor` and refuses on the blocking classes alone, the attachment class is advisory, and an undeclared thread's attachments are not examined at all. The sentence sends an operator hunting for a refusal that never comes, and a warning that promises a consequence which does not follow teaches operators to read past the next one.\n\n**Ruled under the pen, 2026-09-13:** `uncommitted` distinguishes a deleted tracked file from a modified one, reporting it as `deleted in the working tree and not staged, HEAD holds its bytes` when HEAD does; the closing clause states what the gate actually does, which is nothing on this class. One test, two arms: an unstaged deletion of a committed attachment is reported as deleted-with-bytes-in-HEAD, and an unstaged edit is still reported as modified. After the thread.\n\n\nCLOSED 2026-09-14 (dc), to the ruling:\n- `sync::uncommitted` distinguishes a tracked file deleted in the working tree from one edited in it, asking git (`diff-files --diff-filter=D`). A deletion reports `deleted in the working tree and not staged, HEAD holds its bytes` when `git cat-file -e HEAD:<path>` holds it, and otherwise says no commit holds them.\n- The warning's header no longer says the bytes are in no commit.\n- The closing clause says what the gate does on this class, which is nothing: `this run does not refuse, and neither does the commit gate: doctor reports attachment drift as advisory`.\n\nJudged: one test, two arms (an unstaged deletion is `Deleted { in_head: true }`, an unstaged edit is still `Modified`, and staging the deletion empties the report). It is red with the classification removed (batch/judge-batch2-base-red.txt), and passes in the batch's full run.\n"
}
