{
  "schema": "intent/issue@3.0",
  "number": 538,
  "slug": "the-chain-block-claude-upgrade-writes-has-no-els",
  "title": "The chain block claude upgrade writes has no else, so an absent carrier skips every gate in silence",
  "status": "closed",
  "severity": "medium",
  "created": "2026-09-23",
  "closed": "2026-09-23",
  "reporter": "Matthew Sinclair",
  "body": "Reported by gtools-vc on 2026-09-23 as a low-severity point about a failing `git rev-parse`. vc widened it from `canon.rs` at 615d86a8a.\n\n**What happens.** `canon::chain_block` writes the same block into every hook that `intent claude upgrade` wires:\n\n```\n_intent_chain=\"$(git rev-parse --git-path hooks 2>/dev/null)/<hook>.intent\"\nif [ -x \"$_intent_chain\" ]; then\n  \"$_intent_chain\" \"$@\" || exit $?\nfi\n```\n\nWhen the carrier is absent or not executable, or `git rev-parse` fails, the block does nothing and prints nothing. The commit then goes ahead with no guard and no critic. Canon's own comment on installing the carrier names this shape as the defect: a project that \"passes every commit at rc=0 while every report anyone reads says the gate is wired\". The shim's contract is refusing, not skipping (hv ruling 4). Installing the carrier made the absent case rarer, but the block is still silent when it happens. The carrier is gitignored by design, so a fresh worktree of an estate that reaches its hooks through `core.hooksPath` gets the block without the carrier.\n\nIntent's own `.githooks/pre-commit` has a different block between the same markers:\n\n- an `elif` that refuses a carrier with no execute bit;\n- an `else` that prints `GATE ABSENT -- the critic and every shipped guard did NOT run` and exits 1.\n\nNothing in canon writes that form, and `GATE ABSENT` appears nowhere under `native/rust` or `lib/templates`. So the estate that ships the gate protects itself, and ships every other estate the silent block.\n\n**What the fix has to do.** `chain_block` emits the refusing form from one home, and Intent's own hook carries the same bytes. The change also settles whether a post-pull hook, whose carrier always exits 0, refuses or warns when its carrier is absent. The drive covers an absent carrier, a carrier without its execute bit, and a failing `rev-parse`.\n\n**Built on vc's rulings (2026-09-23), in two banks.** The pre-commit chain block refuses a commit it cannot gate: GATE ABSENT or GATE NOT EXECUTABLE, naming the path and the remedy, exit 1. The hooks path is read with `|| :`, because under `set -e` the bare assignment aborted the hook at rc 128 with nothing printed. The post-merge, post-checkout and post-rewrite blocks warn on stderr and exit 0, and only in a checkout that holds an Intent store (`intent/.cache/intent.db` under the top level). A post-checkout that exits non-zero becomes the exit code of `git checkout`, `git switch` and `git worktree add` (measured on git 2.55.0), and with no store there is nothing to bring up to date. `intent claude upgrade --apply` rewrites an existing block in place and keeps every byte outside it. A hook it must leave alone (two blocks, the retired marker, an opener with no end) is held and named with its reason, never reported unchanged. Intent's four .githooks blocks carry canon's bytes under a drift arm. A read-only census before the rewrite found 73 hooks in 19 estates: 71 carried the old canonical block, one was Intent's own refusing form, one had no block, and no project line sat inside any block.\n\n**Landed** in two commits, each at its judged patch-id. v2 at 00fc875a7 (358a9730c007), judged in cc's stacked run, which vc passed. The post-pull tune at efd73baa1 (a0a1d4b849cd), judged in dc's tail run, which vc passed. Neither is deployed until hv's second build all.\n"
}
