{
  "schema": "intent/issue@3.0",
  "number": 551,
  "slug": "a-project-v3-0-x-initialised-keeps-its-store-tra",
  "title": "a project v3.0.x initialised keeps its store tracked in git after upgrade, and nothing says so",
  "status": "open",
  "severity": "high",
  "created": "2026-09-24",
  "reporter": "Matthew Sinclair",
  "body": "Found by cc on 2026-09-24 while re-driving the team guide for the 3.2.1 doc audit, with the published v3.0.3 binaries (`46599d145`, fetched from the GitHub release) as the old pair and the dev pair at `4c687eaad` as the pair under test.\n\nSeen:\n\n- v3.0.3's `intent init` in a fresh `git init` repository writes no `.gitignore`, so the project's first commit carries the runtime store: `git ls-files` lists `intent/.cache/intent.db`.\n- `intent upgrade` on today's pair backfills the events and writes a `.gitignore` that ignores `intent/.cache/`, but git keeps tracking the store: `git status` shows ` M intent/.cache/intent.db`, and a second upgrade leaves it the same.\n- `intent doctor` there reports no finding about it; nothing names a tracked store.\n\nThe code: `facade::converge_gitignore` (`facade.rs:562`), called from both doors -- `init` (`init.rs:487`) and the migration (`facade.rs:4439`) -- writes the rule and never asks whether git already tracks the path. `init` gained the call at `c4c4880c2`, after the v3.0.3 tag, so every project that v3.0.0 to v3.0.3 initialised into a repository without its own rule for `intent/.cache` carries its store in git.\n\nWhy it matters, most of all to a team: the upgrade's own last line says to commit the canon and the generated views, so the natural `git add -A` commits the store again, and so does every teammate's next commit. A pull then checks out a colleague's `intent.db` over your own -- the store holds this machine's state, and intentd may have it open.\n\nNot affected: measured by vc at 2026-09-24 10:00Z by `date -u`, no estate in hv's fleet tracks anything under `intent/.cache`, and every one ignores the store. The exposure is a project initialised with a published v3.0.x.\n\nExpected: `intent doctor` names a store that git tracks, and `intent upgrade` names the tracked path with the remedy that untracks it, `git rm --cached intent/.cache/intent.db`, to go in the upgrade's own commit. The doc half, whatever is ruled, is the team guide's upgrade section: a project from 3.0.x may have committed its store, how to see it (`git ls-files intent/.cache`), and how to untrack it.\n\nDisposition: hv's, asked on 2026-09-24, against the shipping-grade test of hv decision 33 (1).\n"
}
