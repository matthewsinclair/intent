# AGENTS.md

Primary tool-agnostic contract for AI coding agents working in this project.

This file is auto-generated by `intent agents sync`. Do not edit by hand -- changes will be overwritten on the next regeneration. Project-specific rules live in `RULES.md` and `CLAUDE.md` (the Claude Code overlay).

## Project Overview

<!-- Replace with a paragraph describing what this project does and who uses it. -->

## Required on every session

Run `/in-session` immediately after session start and after every `/compact` or context reset. It auto-detects the project language and loads the right skills.

## File map

- `AGENTS.md` -- this file (primary contract).
- `RULES.md` -- mandatory coding rules.
- `ARCHITECTURE.md` -- system architecture and design decisions.
- `CLAUDE.md` -- Claude Code-specific overlay.
- `usage-rules.md` -- DO / NEVER contract.
- `intent/llm/MODULES.md` -- module registry (Highlander enforcement).
- `intent/llm/DECISION_TREE.md` -- code placement flow chart.
- `intent/` -- steel threads, project docs, work tracking.
- `intent/.config/` -- Intent metadata.

## Rules of the road

Four cross-language principles govern all Intent projects:

- **Highlander** -- there can be only one; no divergent copies of the same concern.
- **PFIC** -- Pure-Functional-Idiomatic-Coordination; pattern match, pipe, tag, compose.
- **Thin Coordinator** -- coordinators parse to call to render; business logic lives elsewhere.
- **No Silent Errors** -- every failure surfaces; rescue-and-swallow is forbidden.

Full rule files: `intent/plugins/claude/rules/agnostic/`. Language-specific concretisations at `intent/plugins/claude/rules/<lang>/`.
