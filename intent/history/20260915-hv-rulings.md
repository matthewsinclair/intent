# hv's rulings of 2026-09-15: closing out Intent's open issues and threads

vc put the day's open decisions to hv as menu questions, each with its context, its options and vc's recommendation, then read every answer back in prose and took the rulings from hv's prose: hv corrected item 11 in their own words and wrote "The rest: confirmed." Each ruling below names who carries it; the state of each is its artefact, never this file.

## The rulings

1. The close-out plan goes, with three landing rules. A bank is a patch blob on `refs/bank/<node>/<issue>` (amended the same day from a commit, because a worktree commit meets `pre-commit: GATE ABSENT`). Ready banks ride a train: vc stacks them on HEAD and runs one judging run, and a train of one runs in the lane's own warm worktree. cc's matched control runs under a host hold vc announces.
2. hv ran the 22:32Z build of 2026-09-14: the pair at `1929c272b`, on which the live store migrated to rung 27.
3. 0331: every dead artefact is deleted, in cc's lane with the stale comments: `module_check_hook.json` and its `exit_code_consumers` roster row; `critic-guard.sh`; the rules-index trio with the unwired `claude rules index` and its register row; the dead llm and prj templates with their `init.rs` DESTINATIONS rows; `global-agents.json` with the bats assertions that read it. 0331 closes.
4. ST0057's AC-07.6 is reworded to what is built, with the test that proves it: a cross-project address parses and every door refuses it by name. vc re-checks AC-03.1 after 0338 (i).
5. 0321: the daemon log is exempt from the one-clock rule, with its reason recorded: an operational stream that must write when the store cannot. cc.
6. 0177: the finding moves onto the register's ext rows and the issue closes. `ext`, `learn` and `config` stay declared and unbuilt (hv, 2026-08-31). ic.
7. ST0070 is cancelled. ST0060 (`intent vault`) stays open as the next line's first thread.
8. 0345 is ruled out of 3.x (Apple Silicon macOS only) and closed; it reopens on a request.
9. The explorer palette's `Home`/`End` stay caret keys; ic's hold on the flip is discharged.
10. No release version is chosen today; hv decides at the close-out.
11. hv, verbatim: "That is gyges. There's a Claude running on it now that we can talk to if needed, but this isn't a high priority right now. There's no use trying to install it on another machine until it works here properly on this machine." ST0056's AC-00.5 and AC-11.1, and 0344, wait for that.
12. hv's hold 1 is fixed: dc builds the pair privately and moves it into place in one step, so no estate meets a missing `intent`. Hold 2 (`bin/int` to `bin/devbin`) is struck: `bin/int` is a symlink to devbin.
13. The brew keg is pinned (vc), and hv annotates the v3.0.2 GitHub release as tagged but never published.
14. The quiet window (build all, daemon restart, app install, the view commit, the thread closes) runs at the end of the day, on hv's word.
15. Every call taken under the pen stands: 0303, 0347, 0350, 0374, 0375, 0382, 0389, decisions 16 and 17 on vc's board, 0334's readings, the store rule, and the calls in `20260913-calls-under-the-pen.md`, whose open asks close as AC-24.6 and AC-24.7 specified and unbuilt and the binary not reading `INTENT_NODE`.
16. ST0056's provisional rulings: D43 and the `new-surface` scope are ratified; D46 closes on a design line that `intent/export@3.0` is an internal id (ic, in 0339); the withheld-13 field ruling is struck as settled by `exposed_on_mcp` on every dispatch row; `ac gate`'s satisfy-on-first-green is ratified by vc under D47 (ic, in 0334's commit).
17. `intent organize --apply` stays the one door for stale views, and `claude upgrade --apply`'s report names it. dc.
18. The menubar version row's tooltip on a failed read is dropped. ic.
19. The usage-error exit-code line on cc's hold is struck: 0328 and 0329 closed what was found.
