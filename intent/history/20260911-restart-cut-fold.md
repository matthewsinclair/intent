# restart.md, folded out at the 3.0.1 cut (vc, 2026-09-11)

Verbatim. The two opening paragraphs governed the 3.0.1 list and the cut, which has shipped; every issue the section below names is closed.

**THE WORK IS THE NUMBERED LIST IN `intent/wip.md` AND NOTHING ELSE (hv, 2026-09-11 09:15Z).** hv, verbatim: _"THERE IS NO NEW WORK TO BE DONE. We are working on these items and these items ONLY."_ The open defects in 3.0.1 priority order (92 when the list was cut; `intent issues list` is the live count), worked top-down in lanes, cut from the bottom by hv. A defect found while fixing an item goes in the commit message, not on the list. No new tests beyond the one that proves the item fixed; no new instruments, guards, criteria or threads. The acceptance-and-instrument loop was culled on 2026-09-11 (`6918a2e5`, `0b7b24a4`); do not rebuild it.

**EVERYTHING IS IN 3.0.1. THERE IS NO OTHER RELEASE YET (hv, 2026-09-04).** hv's words, verbatim: _"Everything is in 3.0.1. There's no other release (yet). The end. Stop asking me about this. I've said it about 20 times now."_ There is no cut to be inside or outside of. **Strike _not in this one_, _defer_, _after the tag_ and _out of the cut_ as a CLASS rather than one line at a time** -- they are the scarcity move wearing a release number, and the deadline they protect does not exist.

## Live and unfixed -- read before driving this repo

- **A CANON WRITE CAN REPORT `ok`, LAND, AND BE REVERTED ~1s LATER BY `intentd` (`0216`). THIS IS ACTIVE, NOT HISTORICAL: it fired THREE TIMES on 2026-09-02 across three nodes, and half the losses in the store's whole life are from that one evening.** The write is not lost -- it lands, and the daemon's disk ingest overwrites it, which is why the `ok` is honest. **CONTENTION IS THE VARIABLE, DRIVEN 2026-09-03: a SINGLE writer loses nothing at any spacing or corpus size; with 2-8 competing writers, rows go.** So a single-writer harness cannot reach the defect at any spacing, and a clean run against one proves nothing about a live tree. **AND _THE LAST WRITE OF A BURST IS THE EXPOSED ONE_ IS REFUTED AS A RULE** -- it held 5 of 5 in the original corpus and 3 of 3 that evening, but a contended sweep lost rows 2, 5, 6, 6, 7, 7, 7 across eight runs. **Position is not the predictor; contention is.** **REFUSALS AND SILENT LOSSES TRADE OFF, so COUNT THEM SEPARATELY OR YOU WILL DRAW THE OPPOSITE CONCLUSION**: at high contention every collision is REFUSED (`0226`) and none is swallowed, and a single counter prints _fewer losses under higher load_ and reads as the defect improving under stress.

  **THE MITIGATION IS A LOOP ON AN OBSERVABLE, NEVER A WAIT ON A DURATION: read the target back, compare it to intent, re-write if it differs, repeat until it holds.** _Verify past the ingest_ smuggles a constant nobody has -- past WHICH ingest, and how long is past? **And `one verb at a time` is NOT sufficient: the debouncer sees WRITES, NOT AUTHORS, so a peer writing two seconds before you puts YOUR write in the exposed position and you cannot see it in your own transcript.**

  **IT IS REACHABLE BY AN ORDINARY SHELL LOOP.** An `intent` invocation costs 23-26ms; the ~250ms spacing an agent produces is TURN LATENCY, not process cost. **A `for` loop over a shipped verb sits deep inside the 250ms debounce.** Migrators are safe (`migrate.rs` commits one planned batch) and the test suites are safe (watched iff the daemon opened the project, and the suites never pass `--daemon`). **The fix is daemon-side and unbuilt: an ingest must not revert store state newer than the disk it read** -- which needs a monotonic version the ingest does not own, because `written_at` is rewritten wholesale by the ingest itself. **`event_log` in `intent/.cache/intent.db` is the only STORE-SIDE provenance surface; committed canon in git is a second, DISK-SIDE one with different reach. Row timestamps are worthless, AND `event_log` HAS NO ACTOR FIELD** -- it carries id, op, payload, subject and ts, so it says WHAT and WHEN and can never say WHO.

  **THE DISCRIMINATOR IS INVERTED FROM THE OBVIOUS ONE AND THIS IS THE PARAGRAPH TO READ TWICE.** After a revert the DISK WINS and overwrites the store, **so store and disk AGREEING AT THE OLD VALUE is the SIGNATURE, not the exclusion.** Anyone hunting `0216` by comparing the two at rest rules it out every single time. **The discriminator is REPETITION under a running daemon.** **A cheap oracle exists and costs no second query: a verb's OUTPUT TEXT distinguishes a transition from a no-op at the same rc=0** -- `st cancel` prints `ok: X cancelled` when it moved and `ok: X already Cancelled` when it did not. **The rc tells you nothing; the string tells you everything.**

- **`intent edit` AND `intent st edit` WRITE ON THEIR rc=1 REFUSAL PATH.** `0144` closed the unknown-id half; **the known-thread refusal still writes** -- `st edit ST0001 design` against a real thread with no realised file returns rc=1 and grows TRACKED `intent/.intentfiles`. Filed as `intent#0145`, **and its remedy is vacuous on that path** (`remedy: this artefact carries: ` with nothing after the colon), which belongs with the vacuous-`doctor`-remedy item as ONE class.
- **U3: TWO verbs are mandated in canon and unimplemented in v3** -- `lang` and `plugin` -- **dispositioned `keep`, so UNBUILT rather than retired**, each saying so at rc=2. **DO NOT EDIT THE CANON FOR THOSE TWO: both work in v2, so the mandates are correct for 16 of 17 projects. The canon is not defective, it is not VERSION-AWARE.**
  - **THIS ENTRY SAID FOUR UNTIL 2026-09-04 AND WAS WRONG ON HALF ITS MEMBERSHIP.** Driven by cc against `lib/templates/`, re-driven by vc, positive controls first (`intent st` 9, `intent wp` 8, so the instrument reaches): **`intent lang` 1, `intent plugin` 1, `intent ext` 0, `intent version` 0.** `ext` and `version` are named in canon NOWHERE, so for them there was never a mandate to protect and the instruction was guarding nothing. **This confirms dc's `ext IS discharged` independently.**
  - **AND THE CAVEAT IS cc's AND CUTS AGAINST OVER-READING THE FIX: `intent todo` also scores 0 and plainly ships**, so _named in canon_ is a poor proxy for scope in either direction. Under hv's standing ruling that everything outstanding is in 3.0.1, this changes the JUSTIFICATION on those rows and **not their inclusion**.
  - **THE `DO NOT EDIT THE CANON` INSTRUCTION IS NOT GENERAL AND MUST NOT BE READ AS A RULE.** It holds where canon names a verb that WORKS somewhere (v2), which is the version-awareness gap. It does NOT hold where canon names a capability that exists nowhere and has no reader -- `rules index` was de-canoned 2026-09-04 on exactly that ground.
- **doctor's GATE/CARRIER ROWS ARE FALSE ON EVERY SHIM ESTATE** (0105, 0106, 0113): a reporter reads the CARRIER and expects the GATE BODY's properties, so the alarm is permanent. **Do not act on those rows.** `bash .githooks/pre-commit.intent --where` is the one-second truth, `guards: N ran` proves reach, and only a refused commit proves bite.
- **`claude upgrade` AND prettier ARE TWO WRITERS OF `AGENTS.md` THAT CANNOT CONVERGE** (0110): apply 6211 -> prettier 6208 -> apply 6211, forever. **The pair rebuild it was waiting on has happened, so the closure is RE-CHECKABLE and has not been re-checked** -- do not read this as still-blocked without driving it. Until then, treat apply's `written: AGENTS.md` as unread and check `git diff --stat`.
- **0111's SYMPTOM IS REAL AND ITS CAUSE IS CONTESTED.** `sync --to-store` compares JSON extract to store and fires on a real divergence. **Read the store text back after any repair-motivated `--to-store`.**
- **SIX SHELL TESTS ARE RED AND HAVE BEEN FOR THREE TO FOUR DAYS**, none of them caused by the v3 work -- see `wip.md`. **The finding is the four days, not the six.**
