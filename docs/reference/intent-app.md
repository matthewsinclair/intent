# `intent app`

**Generated by `intent/st/ST0056/parity/tools/gen_reference.sh` at 2026-09-11 20:23Z. Do not edit -- re-run it.**

Manage the Intent menubar app on this machine

|                                      |                                                          |
| ------------------------------------ | -------------------------------------------------------- |
| Revision this describes              | `1c55a34853449be626399b513d4674080d5e0e52` (`1c55a3485`) |
| Release presence is reported against | `v3.0.1`                                                 |

**The register is a declaration, not a behaviour claim.** The command surface is built from `surface/dispatch-table.json`, so the register at a revision states what that revision exposes. It does not state that any of it works. Where this page and your binary disagree, **your binary is right**.

**No exit codes are listed per command.** The register's per-command exit tables are measurements of _v2_, and v3 does not reproduce them everywhere, so they are not published here as v3's contract. Every command follows the one surface-wide contract on the [index](index.md#exit-codes).

## The commands

| Command              | What it does                                                                | In v3.0.1 | Reads or writes | Undo       |
| -------------------- | --------------------------------------------------------------------------- | --------- | --------------- | ---------- |
| `intent app`         | Manage the Intent menubar app on this machine                               | yes       | read            | --         |
| `intent app start`   | Start the Intent menubar app                                                | yes       | mutate          | reversible |
| `intent app stop`    | Stop the Intent menubar app                                                 | yes       | mutate          | reversible |
| `intent app restart` | Restart the Intent menubar app                                              | yes       | mutate          | reversible |
| `intent app status`  | Report whether the Intent menubar app is running, and where it is installed | yes       | read            | --         |

## `intent app`

Manage the Intent menubar app on this machine

```
intent app <command>
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.1`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | no                                                         |

**Arguments**

| Argument  | Type         | Required | Values | Notes |
| --------- | ------------ | -------- | ------ | ----- |
| `command` | `subcommand` | yes      |        |       |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent app --help`, and your own binary, are the authority on this verb.

## `intent app start`

Start the Intent menubar app

```
intent app start
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | no         |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent app start --help`, and your own binary, are the authority on this verb.

## `intent app stop`

Stop the Intent menubar app

```
intent app stop
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | no         |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent app stop --help`, and your own binary, are the authority on this verb.

## `intent app restart`

Restart the Intent menubar app

```
intent app restart
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | no         |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent app restart --help`, and your own binary, are the authority on this verb.

## `intent app status`

Report whether the Intent menubar app is running, and where it is installed

```
intent app status
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.1`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | no                                                         |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent app status --help`, and your own binary, are the authority on this verb.

## Refusals

**There is no per-command refusal list, because the register does not carry one.** A row declares its path, arguments, flags, target, disposition, recoverability, MCP exposure and read-or-mutate, and nothing structural about what it refuses.

The estate-wide roster of refusal variants is in [what Intent ships at this revision](cut-surface.md), taken from a roster that declares itself exhaustive and is checked as one. **It is not attributed to commands anywhere**, and attributing it by reading the variant names would be a guess dressed as a reference.
