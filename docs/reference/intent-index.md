# `intent index`

**Generated by `intent/st/ST0056/parity/tools/gen_reference.sh` at 2026-09-17 17:49Z. Do not edit -- re-run it.**

The search index: what it holds, rebuilding it, and resolving its references

|                                      |                                                     |
| ------------------------------------ | --------------------------------------------------- |
| Revision this describes              | `98c580d12e34eacbef833f81c9ec0cea0b8ea0fe` (`HEAD`) |
| Release presence is reported against | `v3.0.1`                                            |

**The register is a declaration, not a behaviour claim.** The command surface is built from `surface/dispatch-table.json`, so the register at a revision states what that revision exposes. It does not state that any of it works. Where this page and your binary disagree, **your binary is right**.

**No exit codes are listed per command.** The register's per-command exit tables are measurements of _v2_, and v3 does not reproduce them everywhere, so they are not published here as v3's contract. Every command follows the one surface-wide contract on the [index](index.md#exit-codes).

## The commands

| Command                | What it does                                                                                                       | In v3.0.1                        | Reads or writes | Undo       |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------ | -------------------------------- | --------------- | ---------- |
| `intent index`         | The search index: what it holds, rebuilding it, and resolving its references                                       | **no -- newer than the release** | read            | --         |
| `intent index status`  | Report what the search index holds by corpus, and every path it will not hold with the reason                      | **no -- newer than the release** | read            | --         |
| `intent index rebuild` | Walk the index scope and rewrite what the index holds, then report it                                              | **no -- newer than the release** | mutate          | idempotent |
| `intent index resolve` | Resolve references to the definitions they name with each language's own toolchain, and report each language's run | **no -- newer than the release** | mutate          | idempotent |

## `intent index`

The search index: what it holds, rebuilding it, and resolving its references

```
intent index <command>
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.1`     | **no -- newer than the release**                           |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | no                                                         |

**Arguments**

| Argument  | Type         | Required | Values | Notes |
| --------- | ------------ | -------- | ------ | ----- |
| `command` | `subcommand` | yes      |        |       |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent index --help`, and your own binary, are the authority on this verb.

## `intent index status`

Report what the search index holds by corpus, and every path it will not hold with the reason

```
intent index status [flags]
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.1`     | **no -- newer than the release**                           |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | yes                                                        |

**Flags**

| Flag     | Takes | Default | What it does                  |
| -------- | ----- | ------- | ----------------------------- |
| `--json` | no    |         | Emit as JSON instead of prose |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent index status --help`, and your own binary, are the authority on this verb.

## `intent index rebuild`

Walk the index scope and rewrite what the index holds, then report it

```
intent index rebuild [flags]
```

|                 |                                  |
| --------------- | -------------------------------- |
| In `v3.0.1`     | **no -- newer than the release** |
| Reads or writes | mutate                           |
| Undo            | idempotent                       |
| Over MCP        | no                               |

**Flags**

| Flag     | Takes | Default | What it does                  |
| -------- | ----- | ------- | ----------------------------- |
| `--json` | no    |         | Emit as JSON instead of prose |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent index rebuild --help`, and your own binary, are the authority on this verb.

## `intent index resolve`

Resolve references to the definitions they name with each language's own toolchain, and report each language's run

```
intent index resolve [flags]
```

|                 |                                  |
| --------------- | -------------------------------- |
| In `v3.0.1`     | **no -- newer than the release** |
| Reads or writes | mutate                           |
| Undo            | idempotent                       |
| Over MCP        | no                               |

**Flags**

| Flag     | Takes    | Default | What it does                                                                          |
| -------- | -------- | ------- | ------------------------------------------------------------------------------------- |
| `--json` | no       |         | Emit as JSON instead of prose                                                         |
| `--lang` | `<lang>` |         | Resolve one language instead of every language the project declares                   |
| `--full` | no       |         | Rebuild everything the toolchain keeps incremental state for, instead of what changed |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent index resolve --help`, and your own binary, are the authority on this verb.

## Refusals

**There is no per-command refusal list, because the register does not carry one.** A row declares its path, arguments, flags, target, disposition, recoverability, MCP exposure and read-or-mutate, and nothing structural about what it refuses.

The estate-wide roster of refusal variants is in [what Intent ships at this revision](cut-surface.md), taken from a roster that declares itself exhaustive and is checked as one. **It is not attributed to commands anywhere**, and attributing it by reading the variant names would be a guess dressed as a reference.
