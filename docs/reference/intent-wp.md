# `intent wp`

**Generated by `intent/st/ST0056/parity/tools/gen_reference.sh` at 2026-08-29 17:24Z. Do not edit -- re-run it.**

Manage work packages within steel threads

|                                      |                                                     |
| ------------------------------------ | --------------------------------------------------- |
| Revision this describes              | `f4a2271fe5eb78869ccd033c16ccc6fc8b3e6a07` (`HEAD`) |
| Release presence is reported against | `v3.0.0`                                            |
| Commands on this page                | 11                                                  |
| ...with a stated exit contract       | 4                                                   |

**The register is a declaration, not a behaviour claim.** The command surface is built from `surface/dispatch-table.json`, so the register at a revision states what that revision exposes. It does not state that any of it works. Where this page and your binary disagree, **your binary is right**.

**Exit codes are stated for 4 of these 11 commands, and that is the register's coverage rather than an omission here.** A row carries the exit codes measured on _v2_; whether v3 reproduces them is a separate field. Where the register says v3 corrects v2 without recording what it corrects it to, this page says so instead of reprinting the behaviour the rewrite exists to remove.

## The commands

| Command               | What it does                                                      | In v3.0.0 | Reads or writes | Undo       |
| --------------------- | ----------------------------------------------------------------- | --------- | --------------- | ---------- |
| `intent wp`           | Manage work packages within steel threads                         | yes       | read            | --         |
| `intent wp new`       | Create a new work package                                         | yes       | mutate          | reversible |
| `intent wp start`     | Mark a work package as WIP                                        | yes       | mutate          | reversible |
| `intent wp done`      | Mark a work package as Done                                       | yes       | mutate          | reversible |
| `intent wp reopen`    | Reopen a done work package back into Wip, with a reason           | yes       | mutate          | reversible |
| `intent wp cancel`    | Mark a work package as cancelled, with a reason                   | yes       | mutate          | reversible |
| `intent wp reinstate` | Reinstate a cancelled work package into NotStarted, with a reason | yes       | mutate          | reversible |
| `intent wp unstart`   | Return a started work package to NotStarted                       | yes       | mutate          | reversible |
| `intent wp rescope`   | Change a work package's T-shirt size                              | yes       | mutate          | reversible |
| `intent wp list`      | List work packages for a steel thread                             | yes       | read            | --         |
| `intent wp show`      | Show work package info.md                                         | yes       | read            | --         |

## `intent wp`

Manage work packages within steel threads

```
intent wp <command>
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.0`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | no                                                         |

**Arguments**

| Argument  | Type         | Required | Values | Notes |
| --------- | ------------ | -------- | ------ | ----- |
| `command` | `subcommand` | yes      |        |       |

**v3 corrects v2 here.** Defers to INV-07, now `corrected`: `--help` exits 0 to stdout. The v3 binary already does this.

## `intent wp new`

Create a new work package

```
intent wp new <stid> <title>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type     | Required | Values | Notes |
| -------- | -------- | -------- | ------ | ----- |
| `stid`   | `st-id`  | yes      |        |       |
| `title`  | `string` | yes      |        |       |

**Exit codes.** v3 reproduces the behaviour measured on v2 for this verb.

| Code | When                                                                      |
| ---- | ------------------------------------------------------------------------- |
| 0    | created                                                                   |
| 1    | missing STID or title                                                     |
| 1    | outside a project -- `error: not in an Intent project directory` (INV-03) |

## `intent wp start`

Mark a work package as WIP

```
intent wp start <specifier>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**The register records that v3 corrects v2 on this verb and does not record what it corrects it TO.** The measured exit codes it carries are v2's, and v2's are what the correction exists to change, so they are not reproduced here. `intent wp start --help`, and your own binary, are the authority.

## `intent wp done`

Mark a work package as Done

```
intent wp done <specifier>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**The register records that v3 corrects v2 on this verb and does not record what it corrects it TO.** The measured exit codes it carries are v2's, and v2's are what the correction exists to change, so they are not reproduced here. `intent wp done --help`, and your own binary, are the authority.

## `intent wp reopen`

Reopen a done work package back into Wip, with a reason

```
intent wp reopen <specifier> [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**Flags**

| Flag       | Takes    | Default | What it does                                                |
| ---------- | -------- | ------- | ----------------------------------------------------------- |
| `--reason` | `<text>` |         | Why it is being reopened -- required by the machine's guard |

**New in v3**, so there is no measured antecedent and the register carries no exit contract for it.

## `intent wp cancel`

Mark a work package as cancelled, with a reason

```
intent wp cancel <specifier> [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**Flags**

| Flag       | Takes    | Default | What it does                           |
| ---------- | -------- | ------- | -------------------------------------- |
| `--reason` | `<text>` |         | Why -- required by the machine's guard |

**New in v3**, so there is no measured antecedent and the register carries no exit contract for it.

## `intent wp reinstate`

Reinstate a cancelled work package into NotStarted, with a reason

```
intent wp reinstate <specifier> [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**Flags**

| Flag       | Takes    | Default | What it does                           |
| ---------- | -------- | ------- | -------------------------------------- |
| `--reason` | `<text>` |         | Why -- required by the machine's guard |

**New in v3**, so there is no measured antecedent and the register carries no exit contract for it.

## `intent wp unstart`

Return a started work package to NotStarted

```
intent wp unstart <specifier>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**New in v3**, so there is no measured antecedent and the register carries no exit contract for it.

## `intent wp rescope`

Change a work package's T-shirt size

```
intent wp rescope <specifier> <size>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values                           | Notes |
| ----------- | ---------- | -------- | -------------------------------- | ----- |
| `specifier` | `st-id/NN` | yes      |                                  |       |
| `size`      | `enum`     | yes      | `XS`, `S`, `M`, `L`, `XL`, `XXL` |       |

**New in v3**, so there is no measured antecedent and the register carries no exit contract for it.

## `intent wp list`

List work packages for a steel thread

```
intent wp list <stid> [flags]
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.0`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | yes                                                        |

**Arguments**

| Argument | Type    | Required | Values | Notes |
| -------- | ------- | -------- | ------ | ----- |
| `stid`   | `st-id` | yes      |        |       |

**Flags**

| Flag       | Takes              | Default    | What it does                                                     |
| ---------- | ------------------ | ---------- | ---------------------------------------------------------------- |
| `--width`  | `<n>`              |            | Render at a fixed column width; 0 or absent means terminal width |
| `--format` | `terminal` or `md` | `terminal` | Output format                                                    |

**Exit codes.** v3 reproduces the behaviour measured on v2 for this verb.

| Code | When                                                                               |
| ---- | ---------------------------------------------------------------------------------- |
| 0    | listed, including the empty case -- prints `no work packages for <ID>` and exits 0 |
| 1    | no STID -- `error: Usage: intent wp list <STID>`                                   |
| 1    | steel thread not found                                                             |
| 1    | outside a project -- `error: not in an Intent project directory` (INV-03)          |

## `intent wp show`

Show work package info.md

```
intent wp show <specifier>
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.0`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | yes                                                        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**Exit codes.** v3 reproduces the behaviour measured on v2 for this verb.

| Code | When                                                                      |
| ---- | ------------------------------------------------------------------------- |
| 0    | printed                                                                   |
| 1    | missing or unresolvable specifier                                         |
| 1    | outside a project -- `error: not in an Intent project directory` (INV-03) |

## Refusals

**There is no per-command refusal list, because the register does not carry one.** A row declares its path, arguments, flags, target, disposition, recoverability, MCP exposure and read-or-mutate, and nothing structural about what it refuses. Where a command's exit codes appear above, those _are_ its refusal contract; where they do not, the register has nothing to publish.

The estate-wide roster of refusal variants is in [what Intent ships at this revision](cut-surface.md), taken from a roster that declares itself exhaustive and is checked as one. **It is not attributed to commands anywhere**, and attributing it by reading the variant names would be a guess dressed as a reference.
