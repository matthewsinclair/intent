# `intent wp`

**Generated by `intent/st/ST0056/parity/tools/gen_reference.sh` at 2026-09-11 20:23Z. Do not edit -- re-run it.**

Manage work packages within steel threads

|                                      |                                                          |
| ------------------------------------ | -------------------------------------------------------- |
| Revision this describes              | `1c55a34853449be626399b513d4674080d5e0e52` (`1c55a3485`) |
| Release presence is reported against | `v3.0.1`                                                 |

**The register is a declaration, not a behaviour claim.** The command surface is built from `surface/dispatch-table.json`, so the register at a revision states what that revision exposes. It does not state that any of it works. Where this page and your binary disagree, **your binary is right**.

**No exit codes are listed per command.** The register's per-command exit tables are measurements of _v2_, and v3 does not reproduce them everywhere, so they are not published here as v3's contract. Every command follows the one surface-wide contract on the [index](index.md#exit-codes).

## The commands

| Command               | What it does                                                      | In v3.0.1 | Reads or writes | Undo       |
| --------------------- | ----------------------------------------------------------------- | --------- | --------------- | ---------- |
| `intent wp`           | Manage work packages within steel threads                         | yes       | read            | --         |
| `intent wp new`       | Create a new work package                                         | yes       | mutate          | reversible |
| `intent wp start`     | Mark a work package as WIP                                        | yes       | mutate          | reversible |
| `intent wp done`      | Mark a work package as Done                                       | yes       | mutate          | reversible |
| `intent wp reopen`    | Reopen a done work package back into Wip, with a reason           | yes       | mutate          | reversible |
| `intent wp cancel`    | Mark a work package as cancelled, with a reason                   | yes       | mutate          | reversible |
| `intent wp reinstate` | Reinstate a cancelled work package into NotStarted, with a reason | yes       | mutate          | reversible |
| `intent wp unstart`   | Return a started work package to NotStarted                       | yes       | mutate          | reversible |
| `intent wp rescope`   | Change a work package's T-shirt size                              | yes       | mutate          | reversible |
| `intent wp list`      | List work packages for a steel thread                             | yes       | read            | --         |
| `intent wp show`      | Show work package info.md                                         | yes       | read            | --         |

## `intent wp`

Manage work packages within steel threads

```
intent wp <command>
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.1`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | no                                                         |

**Arguments**

| Argument  | Type         | Required | Values | Notes |
| --------- | ------------ | -------- | ------ | ----- |
| `command` | `subcommand` | yes      |        |       |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent wp --help`, and your own binary, are the authority on this verb.

## `intent wp new`

Create a new work package

```
intent wp new <stid> <title>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type     | Required | Values | Notes |
| -------- | -------- | -------- | ------ | ----- |
| `stid`   | `st-id`  | yes      |        |       |
| `title`  | `string` | yes      |        |       |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent wp new --help`, and your own binary, are the authority on this verb.

## `intent wp start`

Mark a work package as WIP

```
intent wp start <specifier>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent wp start --help`, and your own binary, are the authority on this verb.

## `intent wp done`

Mark a work package as Done

```
intent wp done <specifier>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent wp done --help`, and your own binary, are the authority on this verb.

## `intent wp reopen`

Reopen a done work package back into Wip, with a reason

```
intent wp reopen <specifier> [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**Flags**

| Flag       | Takes    | Default | What it does                                                |
| ---------- | -------- | ------- | ----------------------------------------------------------- |
| `--reason` | `<text>` |         | Why it is being reopened -- required by the machine's guard |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent wp reopen --help`, and your own binary, are the authority on this verb.

## `intent wp cancel`

Mark a work package as cancelled, with a reason

```
intent wp cancel <specifier> [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**Flags**

| Flag       | Takes    | Default | What it does                           |
| ---------- | -------- | ------- | -------------------------------------- |
| `--reason` | `<text>` |         | Why -- required by the machine's guard |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent wp cancel --help`, and your own binary, are the authority on this verb.

## `intent wp reinstate`

Reinstate a cancelled work package into NotStarted, with a reason

```
intent wp reinstate <specifier> [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**Flags**

| Flag       | Takes    | Default | What it does                           |
| ---------- | -------- | ------- | -------------------------------------- |
| `--reason` | `<text>` |         | Why -- required by the machine's guard |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent wp reinstate --help`, and your own binary, are the authority on this verb.

## `intent wp unstart`

Return a started work package to NotStarted

```
intent wp unstart <specifier>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent wp unstart --help`, and your own binary, are the authority on this verb.

## `intent wp rescope`

Change a work package's T-shirt size

```
intent wp rescope <specifier> <size>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument    | Type       | Required | Values                           | Notes |
| ----------- | ---------- | -------- | -------------------------------- | ----- |
| `specifier` | `st-id/NN` | yes      |                                  |       |
| `size`      | `enum`     | yes      | `XS`, `S`, `M`, `L`, `XL`, `XXL` |       |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent wp rescope --help`, and your own binary, are the authority on this verb.

## `intent wp list`

List work packages for a steel thread

```
intent wp list <stid> [flags]
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.1`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | yes                                                        |

**Arguments**

| Argument | Type    | Required | Values | Notes |
| -------- | ------- | -------- | ------ | ----- |
| `stid`   | `st-id` | yes      |        |       |

**Flags**

| Flag       | Takes            | Default    | What it does                                                     |
| ---------- | ---------------- | ---------- | ---------------------------------------------------------------- |
| `--width`  | `<n>`            |            | Render at a fixed column width; 0 or absent means terminal width |
| `--format` | `<terminal\|md>` | `terminal` | Output format                                                    |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent wp list --help`, and your own binary, are the authority on this verb.

## `intent wp show`

Show work package info.md

```
intent wp show <specifier>
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.1`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | yes                                                        |

**Arguments**

| Argument    | Type       | Required | Values | Notes |
| ----------- | ---------- | -------- | ------ | ----- |
| `specifier` | `st-id/NN` | yes      |        |       |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent wp show --help`, and your own binary, are the authority on this verb.

## Refusals

**There is no per-command refusal list, because the register does not carry one.** A row declares its path, arguments, flags, target, disposition, recoverability, MCP exposure and read-or-mutate, and nothing structural about what it refuses.

The estate-wide roster of refusal variants is in [what Intent ships at this revision](cut-surface.md), taken from a roster that declares itself exhaustive and is checked as one. **It is not attributed to commands anywhere**, and attributing it by reading the variant names would be a guess dressed as a reference.
