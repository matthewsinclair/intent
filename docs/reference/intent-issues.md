# `intent issues`

**Generated by `intent/st/ST0056/parity/tools/gen_reference.sh` at 2026-09-11 20:23Z. Do not edit -- re-run it.**

Track issues without the ceremony of a steel thread

|                                      |                                                          |
| ------------------------------------ | -------------------------------------------------------- |
| Revision this describes              | `1c55a34853449be626399b513d4674080d5e0e52` (`1c55a3485`) |
| Release presence is reported against | `v3.0.1`                                                 |

**The register is a declaration, not a behaviour claim.** The command surface is built from `surface/dispatch-table.json`, so the register at a revision states what that revision exposes. It does not state that any of it works. Where this page and your binary disagree, **your binary is right**.

**No exit codes are listed per command.** The register's per-command exit tables are measurements of _v2_, and v3 does not reproduce them everywhere, so they are not published here as v3's contract. Every command follows the one surface-wide contract on the [index](index.md#exit-codes).

## The commands

| Command               | What it does                                                  | In v3.0.1 | Reads or writes | Undo       |
| --------------------- | ------------------------------------------------------------- | --------- | --------------- | ---------- |
| `intent issues`       | Track issues without the ceremony of a steel thread           | yes       | read            | --         |
| `intent issues list`  | List issues (default: open)                                   | yes       | read            | --         |
| `intent issues add`   | Add a new issue, print its ID:TITLE                           | yes       | mutate          | reversible |
| `intent issues edit`  | Correct an issue's record: its prose, its title, its severity | yes       | mutate          | reversible |
| `intent issues show`  | Show one issue (optionally as JSON)                           | yes       | read            | --         |
| `intent issues close` | Mark an issue done: OPEN -> CLOSED                            | yes       | mutate          | reversible |
| `intent issues open`  | Reopen an issue: CLOSED -> OPEN                               | yes       | mutate          | reversible |

## Retired

These do not exist in v3. They refuse with an exit code that distinguishes _removed_ from _never built_, so a v2-era script gets an answer rather than an unknown-command error.

| Command                   | What it did in v2                                      |
| ------------------------- | ------------------------------------------------------ |
| `intent issues hydrate`   | Add an issue to .intentfiles and write its files       |
| `intent issues dehydrate` | Remove an issue from .intentfiles and delete its files |

## `intent issues`

Track issues without the ceremony of a steel thread

```
intent issues [<command>] [flags]
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.1`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | no                                                         |

**Arguments**

| Argument  | Type         | Required | Values | Notes |
| --------- | ------------ | -------- | ------ | ----- |
| `command` | `subcommand` | no       |        | list  |

**Flags**

| Flag       | Takes            | Default    | What it does                                                     |
| ---------- | ---------------- | ---------- | ---------------------------------------------------------------- |
| `--width`  | `<n>`            |            | Render at a fixed column width; 0 or absent means terminal width |
| `--format` | `<terminal\|md>` | `terminal` | Output format                                                    |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent issues --help`, and your own binary, are the authority on this verb.

## `intent issues list`

List issues (default: open)

```
intent issues list [flags]
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.1`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | yes                                                        |

**Flags**

| Flag       | Takes                 | Default    | What it does                                                     |
| ---------- | --------------------- | ---------- | ---------------------------------------------------------------- |
| `--kind`   | `<open\|closed\|all>` | `open`     | Which bucket to list                                             |
| `--width`  | `<n>`                 |            | Render at a fixed column width; 0 or absent means terminal width |
| `--format` | `<terminal\|md>`      | `terminal` | Output format                                                    |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent issues list --help`, and your own binary, are the authority on this verb.

## `intent issues add`

Add a new issue, print its ID:TITLE

```
intent issues add <title> [flags]
```

Also spelled `issues new`.

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type     | Required | Values | Notes |
| -------- | -------- | -------- | ------ | ----- |
| `title`  | `string` | yes      |        |       |

**Flags**

| Flag         | Takes                           | Default  | What it does                       |
| ------------ | ------------------------------- | -------- | ---------------------------------- |
| `--severity` | `<critical\|high\|medium\|low>` | `medium` | Severity                           |
| `--body`     | `<text>`                        |          | The issue's prose, inline          |
| `--from`     | `<file>`                        |          | Read the issue's prose from a file |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent issues add --help`, and your own binary, are the authority on this verb.

## `intent issues edit`

Correct an issue's record: its prose, its title, its severity

```
intent issues edit <id> [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | no         |

**Arguments**

| Argument | Type       | Required | Values | Notes |
| -------- | ---------- | -------- | ------ | ----- |
| `id`     | `issue-id` | yes      |        |       |

**Flags**

| Flag         | Takes                           | Default | What it does                       |
| ------------ | ------------------------------- | ------- | ---------------------------------- |
| `--body`     | `<text>`                        |         | The issue's prose, inline          |
| `--from`     | `<file>`                        |         | Read the issue's prose from a file |
| `--title`    | `<text>`                        |         | The issue's title                  |
| `--severity` | `<critical\|high\|medium\|low>` |         | Severity                           |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent issues edit --help`, and your own binary, are the authority on this verb.

## `intent issues show`

Show one issue (optionally as JSON)

```
intent issues show <id> [flags]
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.1`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | yes                                                        |

**Arguments**

| Argument | Type       | Required | Values | Notes |
| -------- | ---------- | -------- | ------ | ----- |
| `id`     | `issue-id` | yes      |        |       |

**Flags**

| Flag       | Takes                  | Default    | What it does                  |
| ---------- | ---------------------- | ---------- | ----------------------------- |
| `--json`   | no                     |            | Emit as JSON instead of prose |
| `--format` | `<terminal\|md\|json>` | `terminal` | Output format                 |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent issues show --help`, and your own binary, are the authority on this verb.

## `intent issues close`

Mark an issue done: OPEN -> CLOSED

```
intent issues close <id>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type       | Required | Values | Notes |
| -------- | ---------- | -------- | ------ | ----- |
| `id`     | `issue-id` | yes      |        |       |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent issues close --help`, and your own binary, are the authority on this verb.

## `intent issues open`

Reopen an issue: CLOSED -> OPEN

```
intent issues open <id>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type       | Required | Values | Notes |
| -------- | ---------- | -------- | ------ | ----- |
| `id`     | `issue-id` | yes      |        |       |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent issues open --help`, and your own binary, are the authority on this verb.

## Refusals

**There is no per-command refusal list, because the register does not carry one.** A row declares its path, arguments, flags, target, disposition, recoverability, MCP exposure and read-or-mutate, and nothing structural about what it refuses.

The estate-wide roster of refusal variants is in [what Intent ships at this revision](cut-surface.md), taken from a roster that declares itself exhaustive and is checked as one. **It is not attributed to commands anywhere**, and attributing it by reading the variant names would be a guess dressed as a reference.
