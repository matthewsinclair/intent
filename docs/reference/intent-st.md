# `intent st`

**Generated by `intent/st/ST0056/parity/tools/gen_reference.sh` at 2026-08-29 21:23Z. Do not edit -- re-run it.**

Manage steel threads for the project

|                                      |                                                     |
| ------------------------------------ | --------------------------------------------------- |
| Revision this describes              | `3fdd853314979e03305bc7366b5ac0efdc5bfe14` (`HEAD`) |
| Release presence is reported against | `v3.0.0`                                            |
| Commands on this page                | 18                                                  |
| ...with a stated exit contract       | 4                                                   |

**The register is a declaration, not a behaviour claim.** The command surface is built from `surface/dispatch-table.json`, so the register at a revision states what that revision exposes. It does not state that any of it works. Where this page and your binary disagree, **your binary is right**.

**Exit codes are stated for 4 of these 18 commands, and that is the register's coverage rather than an omission here.** A row carries the exit codes measured on _v2_; whether v3 reproduces them is a separate field. Where the register says v3 corrects v2 without recording what it corrects it to, this page says so instead of reprinting the behaviour the rewrite exists to remove.

## The commands

| Command               | What it does                                                                                                                       | In v3.0.0 | Reads or writes | Undo       |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | --------- | --------------- | ---------- |
| `intent st`           | Manage steel threads for the project                                                                                               | yes       | read            | --         |
| `intent st new`       | Create a new steel thread                                                                                                          | yes       | mutate          | reversible |
| `intent st start`     | Mark a steel thread as in progress                                                                                                 | yes       | mutate          | reversible |
| `intent st done`      | Mark a steel thread as complete                                                                                                    | yes       | mutate          | reversible |
| `intent st cancel`    | Mark a steel thread as cancelled, with a reason                                                                                    | yes       | mutate          | reversible |
| `intent st triage`    | Move a triaged thread out of Triage into NotStarted                                                                                | yes       | mutate          | reversible |
| `intent st hold`      | Put a thread on hold, with a reason                                                                                                | yes       | mutate          | reversible |
| `intent st resume`    | Take a thread off hold and back into Wip                                                                                           | yes       | mutate          | reversible |
| `intent st reopen`    | Reopen a completed thread back into Wip, with a reason                                                                             | yes       | mutate          | reversible |
| `intent st reinstate` | Reinstate a cancelled thread into NotStarted, with a reason                                                                        | yes       | mutate          | reversible |
| `intent st hydrate`   | Add a steel thread to .intentfiles and write its files                                                                             | yes       | mutate          | idempotent |
| `intent st dehydrate` | Remove a steel thread from .intentfiles and delete its files                                                                       | yes       | mutate          | reversible |
| `intent st list`      | List steel threads (default: in progress only)                                                                                     | yes       | read            | --         |
| `intent st show`      | Show details of a specific steel thread                                                                                            | yes       | read            | --         |
| `intent st edit`      | Print the path to a steel thread file, realising the thread if it is not on disk                                                   | yes       | mutate          | idempotent |
| `intent st attach`    | Write an attachment's content from a local file                                                                                    | yes       | mutate          | one-way    |
| `intent st sync`      | Synchronize steel_threads.md with individual ST files                                                                              | yes       | mutate          | idempotent |
| `intent st bootstrap` | Retrofit ST0000 deliverables into a brownfield project -- audit what is present, missing or partial, then install the missing ones | yes       | mutate          | one-way    |

## Retired

These do not exist in v3. They refuse with an exit code that distinguishes _removed_ from _never built_, so a v2-era script gets an answer rather than an unknown-command error.

| Command              | What it did in v2                          |
| -------------------- | ------------------------------------------ |
| `intent st repair`   | Repair malformed steel thread metadata     |
| `intent st organize` | Organize ST files in directories by status |

## `intent st`

Manage steel threads for the project

```
intent st <command>
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.0`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | no                                                         |

**Arguments**

| Argument  | Type         | Required | Values | Notes |
| --------- | ------------ | -------- | ------ | ----- |
| `command` | `subcommand` | yes      |        |       |

**v3 corrects v2 here.** Defers to INV-07, now `corrected`: `--help` exits 0 to stdout. The v3 binary already does this.

## `intent st new`

Create a new steel thread

```
intent st new <title> [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type     | Required | Values | Notes                                                                                                                                                                                       |
| -------- | -------- | -------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `title`  | `string` | yes      |        | v2 collects ALL non-flag args into ARGS and uses only ARGS[0] (bin/intent_st:305, 314) -- surplus positionals are silently discarded. v3 should refuse them (INV-02); flagged, not assumed. |

**Flags**

| Flag            | Takes | Default | What it does                                |
| --------------- | ----- | ------- | ------------------------------------------- |
| `-s`, `--start` | no    |         | Mark the new thread in progress immediately |

**The register records that v3 corrects v2 on this verb and does not record what it corrects it TO.** The measured exit codes it carries are v2's, and v2's are what the correction exists to change, so they are not reproduced here. `intent st new --help`, and your own binary, are the authority.

## `intent st start`

Mark a steel thread as in progress

```
intent st start <id>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type    | Required | Values | Notes |
| -------- | ------- | -------- | ------ | ----- |
| `id`     | `st-id` | yes      |        |       |

**The register records that v3 corrects v2 on this verb and does not record what it corrects it TO.** The measured exit codes it carries are v2's, and v2's are what the correction exists to change, so they are not reproduced here. `intent st start --help`, and your own binary, are the authority.

## `intent st done`

Mark a steel thread as complete

```
intent st done <id> [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type    | Required | Values | Notes |
| -------- | ------- | -------- | ------ | ----- |
| `id`     | `st-id` | yes      |        |       |

**Flags**

| Flag     | Takes          | Default | What it does                                                                    |
| -------- | -------------- | ------- | ------------------------------------------------------------------------------- |
| `--keep` | no             |         | Close the thread but leave its entry in .intentfiles, so its files stay on disk |
| `--date` | `<YYYY-MM-DD>` |         | Record this completion date instead of today                                    |

**The register records that v3 corrects v2 on this verb and does not record what it corrects it TO.** The measured exit codes it carries are v2's, and v2's are what the correction exists to change, so they are not reproduced here. `intent st done --help`, and your own binary, are the authority.

## `intent st cancel`

Mark a steel thread as cancelled, with a reason

```
intent st cancel <id> [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type    | Required | Values | Notes |
| -------- | ------- | -------- | ------ | ----- |
| `id`     | `st-id` | yes      |        |       |

**Flags**

| Flag       | Takes          | Default | What it does                                                                     |
| ---------- | -------------- | ------- | -------------------------------------------------------------------------------- |
| `--reason` | `<text>`       |         | Why it is being cancelled -- required by the machine's guard                     |
| `--keep`   | no             |         | Cancel the thread but leave its entry in .intentfiles, so its files stay on disk |
| `--date`   | `<YYYY-MM-DD>` |         | Record this completion date instead of today                                     |

**The register records that v3 corrects v2 on this verb and does not record what it corrects it TO.** The measured exit codes it carries are v2's, and v2's are what the correction exists to change, so they are not reproduced here. `intent st cancel --help`, and your own binary, are the authority.

## `intent st triage`

Move a triaged thread out of Triage into NotStarted

```
intent st triage <id>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type    | Required | Values | Notes |
| -------- | ------- | -------- | ------ | ----- |
| `id`     | `st-id` | yes      |        |       |

**New in v3**, so there is no measured antecedent and the register carries no exit contract for it.

## `intent st hold`

Put a thread on hold, with a reason

```
intent st hold <id> [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type    | Required | Values | Notes |
| -------- | ------- | -------- | ------ | ----- |
| `id`     | `st-id` | yes      |        |       |

**Flags**

| Flag       | Takes    | Default | What it does                                            |
| ---------- | -------- | ------- | ------------------------------------------------------- |
| `--reason` | `<text>` |         | Why it is being held -- required by the machine's guard |

**New in v3**, so there is no measured antecedent and the register carries no exit contract for it.

## `intent st resume`

Take a thread off hold and back into Wip

```
intent st resume <id>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type    | Required | Values | Notes |
| -------- | ------- | -------- | ------ | ----- |
| `id`     | `st-id` | yes      |        |       |

**New in v3**, so there is no measured antecedent and the register carries no exit contract for it.

## `intent st reopen`

Reopen a completed thread back into Wip, with a reason

```
intent st reopen <id> [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type    | Required | Values | Notes |
| -------- | ------- | -------- | ------ | ----- |
| `id`     | `st-id` | yes      |        |       |

**Flags**

| Flag       | Takes    | Default | What it does                                                |
| ---------- | -------- | ------- | ----------------------------------------------------------- |
| `--reason` | `<text>` |         | Why it is being reopened -- required by the machine's guard |

**New in v3**, so there is no measured antecedent and the register carries no exit contract for it.

## `intent st reinstate`

Reinstate a cancelled thread into NotStarted, with a reason

```
intent st reinstate <id> [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type    | Required | Values | Notes |
| -------- | ------- | -------- | ------ | ----- |
| `id`     | `st-id` | yes      |        |       |

**Flags**

| Flag       | Takes    | Default | What it does                                                  |
| ---------- | -------- | ------- | ------------------------------------------------------------- |
| `--reason` | `<text>` |         | Why it is being reinstated -- required by the machine's guard |

**New in v3**, so there is no measured antecedent and the register carries no exit contract for it.

## `intent st hydrate`

Add a steel thread to .intentfiles and write its files

```
intent st hydrate <id>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | idempotent |
| Over MCP        | yes        |

**Arguments**

| Argument | Type    | Required | Values | Notes |
| -------- | ------- | -------- | ------ | ----- |
| `id`     | `st-id` | yes      |        |       |

**New in v3**, so there is no measured antecedent and the register carries no exit contract for it.

## `intent st dehydrate`

Remove a steel thread from .intentfiles and delete its files

```
intent st dehydrate <id>
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | reversible |
| Over MCP        | yes        |

**Arguments**

| Argument | Type    | Required | Values | Notes |
| -------- | ------- | -------- | ------ | ----- |
| `id`     | `st-id` | yes      |        |       |

**New in v3**, so there is no measured antecedent and the register carries no exit contract for it.

## `intent st list`

List steel threads (default: in progress only)

```
intent st list [flags]
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.0`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | yes                                                        |

**Flags**

| Flag         | Takes              | Default    | What it does                                                     |
| ------------ | ------------------ | ---------- | ---------------------------------------------------------------- |
| `--status`   | `<status>`         |            | `all`, or a comma-separated list rendered in the order given     |
| `--width`    | `<n>`              |            | Render at a fixed column width; 0 or absent means terminal width |
| `--markdown` | no                 |            | Emit canonical GFM instead of terminal rendering                 |
| `--format`   | `terminal` or `md` | `terminal` | Output format                                                    |
| `--slug`     | no                 |            | Show the slug, derived from the title, instead of the title      |

**The register records that v3 corrects v2 on this verb and does not record what it corrects it TO.** The measured exit codes it carries are v2's, and v2's are what the correction exists to change, so they are not reproduced here. `intent st list --help`, and your own binary, are the authority.

## `intent st show`

Show details of a specific steel thread

```
intent st show <id> [<file>]
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.0`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | yes                                                        |

**Arguments**

| Argument | Type    | Required | Values                                                 | Notes |
| -------- | ------- | -------- | ------------------------------------------------------ | ----- |
| `id`     | `st-id` | yes      |                                                        |       |
| `file`   | `enum`  | no       | `info`, `design`, `impl`, `tasks`, `acceptance`, `all` | info  |

**Exit codes.** v3 reproduces the behaviour measured on v2 for this verb.

| Code | When                                                  |
| ---- | ----------------------------------------------------- |
| 0    | printed                                               |
| 1    | no id / thread not found                              |
| 1    | `error: Unknown file type: <t>`                       |
| 1    | `error: File not found: <t>.md for steel thread <ID>` |

## `intent st edit`

Print the path to a steel thread file, realising the thread if it is not on disk

```
intent st edit <id> [<file>] [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | idempotent |
| Over MCP        | yes        |

**Arguments**

| Argument | Type    | Required | Values                                          | Notes                                                                                              |
| -------- | ------- | -------- | ----------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| `id`     | `st-id` | yes      |                                                 |                                                                                                    |
| `file`   | `enum`  | no       | `info`, `design`, `impl`, `tasks`, `acceptance` | NO `all` -- unlike `show`. Asymmetry is deliberate and correct: there is no single path for `all`. |

**Flags**

| Flag       | Takes | Default | What it does                                            |
| ---------- | ----- | ------- | ------------------------------------------------------- |
| `--editor` | no    |         | Open the file in $VISUAL or $EDITOR, whatever stdout is |
| `--path`   | no    |         | Print the path and open nothing, whatever stdout is     |

**v3 deliberately diverges from v2 here.** **TTY-AWARE, WITH AN EXPLICIT SPELLING FOR EACH BRANCH.** v3 opens the file in $VISUAL or $EDITOR when stdout is a terminal, and prints the path when stdout is a pipe or a file. `--editor` forces the open and `--path` forces the print, whatever stdout is; the two together are refused. **THE v2 CONTRACT IS PRESERVED WHERE IT IS ACTUALLY USED**: `$EDITOR "$(intent st edit ST0001 info)"` is in `docs/getting-started.md`, and command substitution makes stdout a PIPE, so that invocation still receives the path and is covered by a test. The name stops being a misnomer on a terminal and keeps its documented behaviour everywhere a script can see.

## `intent st attach`

Write an attachment's content from a local file

```
intent st attach <id> <path> [flags]
```

|                 |         |
| --------------- | ------- |
| In `v3.0.0`     | yes     |
| Reads or writes | mutate  |
| Undo            | one-way |
| Over MCP        | no      |

**Arguments**

| Argument | Type    | Required | Values | Notes |
| -------- | ------- | -------- | ------ | ----- |
| `id`     | `value` | yes      |        |       |
| `path`   | `value` | yes      |        |       |

**Flags**

| Flag     | Takes    | Default | What it does                                        |
| -------- | -------- | ------- | --------------------------------------------------- |
| `--from` | `<file>` |         | The local file whose contents become the attachment |

**New in v3**, so there is no measured antecedent and the register carries no exit contract for it.

## `intent st sync`

Synchronize steel_threads.md with individual ST files

```
intent st sync [flags]
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | idempotent |
| Over MCP        | yes        |

**Flags**

| Flag       | Takes              | Default    | What it does                                         |
| ---------- | ------------------ | ---------- | ---------------------------------------------------- |
| `--write`  | no                 |            | Write the index; without it the command is a dry run |
| `--width`  | `<n>`              |            | Render at a fixed column width                       |
| `--format` | `terminal` or `md` | `terminal` | Output format                                        |

**Exit codes.** v3 reproduces the behaviour measured on v2 for this verb.

| Code | When                                                                                                      |
| ---- | --------------------------------------------------------------------------------------------------------- |
| 0    | synced or dry-run reported                                                                                |
| 1    | unknown option / `error: Steel threads directory not found` / `error: Steel threads index file not found` |

## `intent st bootstrap`

Retrofit ST0000 deliverables into a brownfield project -- audit what is present, missing or partial, then install the missing ones

```
intent st bootstrap [flags]
```

|                 |         |
| --------------- | ------- |
| In `v3.0.0`     | yes     |
| Reads or writes | mutate  |
| Undo            | one-way |
| Over MCP        | no      |

**Flags**

| Flag            | Takes  | Default | What it does                         |
| --------------- | ------ | ------- | ------------------------------------ |
| `--audit-only`  | no     |         | Show gap analysis only, no changes   |
| `--dry-run`     | no     |         | Show what would change, no writes    |
| `--deliverable` | `<id>` |         | Target a single deliverable (D2-D11) |

**The register records that v3 corrects v2 on this verb and does not record what it corrects it TO.** The measured exit codes it carries are v2's, and v2's are what the correction exists to change, so they are not reproduced here. `intent st bootstrap --help`, and your own binary, are the authority.

## Refusals

**There is no per-command refusal list, because the register does not carry one.** A row declares its path, arguments, flags, target, disposition, recoverability, MCP exposure and read-or-mutate, and nothing structural about what it refuses. Where a command's exit codes appear above, those _are_ its refusal contract; where they do not, the register has nothing to publish.

The estate-wide roster of refusal variants is in [what Intent ships at this revision](cut-surface.md), taken from a roster that declares itself exhaustive and is checked as one. **It is not attributed to commands anywhere**, and attributing it by reading the variant names would be a guess dressed as a reference.
