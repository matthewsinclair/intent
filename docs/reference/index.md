# Command reference

**Generated by `intent/st/ST0056/parity/tools/gen_reference.sh` at 2026-09-11 20:13Z. Do not edit -- re-run it.**

**This section is generated, and it is generated against a specific revision rather than against `main`.** That distinction is the reason it exists as its own build step instead of being written by hand.

## Why it is not written by hand

Intent's command surface is declared in a register -- `surface/dispatch-table.json` -- which is the one home for what verbs exist, what arguments and flags they take, and what they do. **A hand-typed reference beside that register would be a transcribed copy of a measured mapping, and it would drift from the thing it copies.** So the reference is emitted from the register by a generator, and the emitted output names the revision it was made from.

**That is not a hypothetical.** The family table below was hand-typed once and went short of shipping commands, in the document whose own second paragraph says why that happens.

## Why the revision matters more than you would expect

**The register describes `main`. A release is a point on `main`, and the two are not the same claim.** A reference written from the register without pinning a revision documents verbs the installed tool does not have, and a reader following it gets `error:` from a command the documentation told them to run.

So every page states, per verb, whether it is present in the release this documentation describes. **A verb in the register is not a verb in your binary.**

## Reading it against your own install

The register is a claim about a source tree. Your binary is the authority on itself:

```
  $ intent --version
  $ intent <family> --help
```

Where this reference and your binary disagree, **your binary is right** and the disagreement is worth reporting.

## The surface, by family

The commands Intent ships at `v3.0.1`, by page. Your own binary lists its surface: `intent --help` and `intent <family> --help`.

| Page                                  | What it covers                                                                             |
| ------------------------------------- | ------------------------------------------------------------------------------------------ |
| [`intent st`](intent-st.md)           | Manage steel threads for the project                                                       |
| [`intent wp`](intent-wp.md)           | Manage work packages within steel threads                                                  |
| [`intent ac`](intent-ac.md)           | Acceptance criteria: the ratified completeness boundary of a unit                          |
| [`intent at`](intent-at.md)           | Acceptance tests: the small red-to-green tests that prove ACs                              |
| [`intent issues`](intent-issues.md)   | Track issues without the ceremony of a steel thread                                        |
| [`intent todo`](intent-todo.md)       | A flat DOING / TODO / DONE view of steel threads and work packages                         |
| [`intent config`](intent-config.md)   | Display the resolved project configuration                                                 |
| [`intent agents`](intent-agents.md)   | Manage AGENTS.md -- the primary tool-agnostic LLM config at project root                   |
| [`intent claude`](intent-claude.md)   | Claude Code integration: subagents, skills, rules, hooks, workstreams                      |
| [`intent lang`](intent-lang.md)       | Declared languages: which languages a project has declared                                 |
| [`intent llm`](intent-llm.md)         | LLM-related commands for working with AI assistants                                        |
| [`intent modules`](intent-modules.md) | Module registry guardrails and enforcement                                                 |
| [`intent plugin`](intent-plugin.md)   | Discover installed Intent plugins and their commands                                       |
| [`intent ext`](intent-ext.md)         | Manage Intent user extensions at ~/.intent/ext/&lt;name>/                                  |
| [`intent daemon`](intent-daemon.md)   | Manage the machine-level intentd                                                           |
| [`intent app`](intent-app.md)         | Manage the Intent menubar app on this machine                                              |
| [Top-level commands](commands.md)     | The commands that take no subcommand: project lifecycle, the store, and the tooling verbs. |

**These commands are retired** and refuse with an exit code that distinguishes "this was removed" from "this was never built". \`intent surface retired\` lists them from your own binary:

- `st repair`
- `st organize`
- `st bootstrap`
- `issues hydrate`
- `issues dehydrate`
- `organize`
- `agents template`
- `claude prime`
- `lang sync`
- `treeindex`
- `fileindex`
- `help`
- `st_zero`

## Against the published release

Measured by comparing the register at `v3.0.1` with the register at `v3.0.1`.

The surface at `v3.0.1` is identical to `v3.0.1`.

---

Also here: [what Intent ships at this revision](cut-surface.md) -- the full shipped surface and the estate-wide roster of refusal variants.
