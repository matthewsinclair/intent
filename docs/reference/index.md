# Command reference

**Generated by `intent/st/ST0056/parity/tools/gen_reference.sh` at 2026-08-29 17:24Z. Do not edit -- re-run it.**

**This section is generated, and it is generated against a specific revision rather than against `main`.** That distinction is the reason it exists as its own build step instead of being written by hand.

## Why it is not written by hand

Intent's command surface is declared in a register -- `surface/dispatch-table.json` -- which is the one home for what verbs exist, what arguments and flags they take, and what they do. **A hand-typed reference beside that register would be a transcribed copy of a measured mapping, and it would drift from the thing it copies.** So the reference is emitted from the register by a generator, and the emitted output names the revision it was made from.

**That is not a hypothetical.** The family table below was hand-typed once and was short by twelve shipping commands, in the document whose own second paragraph says why that happens.

## Why the revision matters more than you would expect

**The register describes `main`. A release is a point on `main`, and the two are not the same claim.** A reference written from the register without pinning a revision documents verbs the installed tool does not have, and a reader following it gets `error:` from a command the documentation told them to run.

So every page states, per verb, whether it is present in the release this documentation describes. **A verb in the register is not a verb in your binary.**

## Reading it against your own install

The register is a claim about a source tree. Your binary is the authority on itself:

```
  $ intent --version
  $ intent <family> --help
```

Where this reference and your binary disagree, **your binary is right** and the disagreement is worth reporting.

## The surface, by family

Intent ships **118** commands at `HEAD`, across these pages.

| Page                                  | What it covers                                                                             | Commands |
| ------------------------------------- | ------------------------------------------------------------------------------------------ | -------- |
| [`intent st`](intent-st.md)           | Manage steel threads for the project                                                       | 18       |
| [`intent wp`](intent-wp.md)           | Manage work packages within steel threads                                                  | 11       |
| [`intent ac`](intent-ac.md)           | Acceptance criteria: the ratified completeness boundary of a unit                          | 12       |
| [`intent at`](intent-at.md)           | Acceptance tests: the small red-to-green tests that prove ACs                              | 8        |
| [`intent issues`](intent-issues.md)   | Track issues without the ceremony of a steel thread                                        | 6        |
| [`intent todo`](intent-todo.md)       | A flat DOING / TODO / DONE view of steel threads and work packages                         | 6        |
| [`intent config`](intent-config.md)   | Display the resolved project configuration                                                 | 3        |
| [`intent agents`](intent-agents.md)   | Manage AGENTS.md -- the primary tool-agnostic LLM config at project root                   | 6        |
| [`intent claude`](intent-claude.md)   | Claude Code integration: subagents, skills, rules, hooks, workstreams                      | 9        |
| [`intent lang`](intent-lang.md)       | Declared languages: which languages a project has declared                                 | 5        |
| [`intent llm`](intent-llm.md)         | LLM-related commands for working with AI assistants                                        | 3        |
| [`intent modules`](intent-modules.md) | Module registry guardrails and enforcement                                                 | 3        |
| [`intent plugin`](intent-plugin.md)   | Discover installed Intent plugins and their commands                                       | 3        |
| [`intent ext`](intent-ext.md)         | Manage Intent user extensions at ~/.intent/ext/&lt;name>/                                  | 5        |
| [Top-level commands](commands.md)     | The commands that take no subcommand: project lifecycle, the store, and the tooling verbs. | 20       |

**9 commands are retired** and refuse with an exit code that distinguishes "this was removed" from "this was never built":

- `st repair`
- `st organize`
- `issues hydrate`
- `issues dehydrate`
- `organize`
- `lang sync`
- `treeindex`
- `help`
- `st_zero`

## Against the published release

Measured by comparing the register at `HEAD` with the register at `v3.0.0`.

- **Newer than `v3.0.0`, so not in an installed copy of it:** `ac edit`, `at edit`
- **In `v3.0.0` and removed since, so a script that calls it will stop working:** `st repair`

---

Also here: [what Intent ships at this revision](cut-surface.md) -- the full shipped surface and the estate-wide roster of refusal variants.
