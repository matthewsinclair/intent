# `intent agents`

**Generated by `intent/st/ST0056/parity/tools/gen_reference.sh` at 2026-08-29 21:23Z. Do not edit -- re-run it.**

Manage AGENTS.md -- the primary tool-agnostic LLM config at project root

|                                      |                                                     |
| ------------------------------------ | --------------------------------------------------- |
| Revision this describes              | `3fdd853314979e03305bc7366b5ac0efdc5bfe14` (`HEAD`) |
| Release presence is reported against | `v3.0.0`                                            |
| Commands on this page                | 6                                                   |
| ...with a stated exit contract       | 4                                                   |

**The register is a declaration, not a behaviour claim.** The command surface is built from `surface/dispatch-table.json`, so the register at a revision states what that revision exposes. It does not state that any of it works. Where this page and your binary disagree, **your binary is right**.

**Exit codes are stated for 4 of these 6 commands, and that is the register's coverage rather than an omission here.** A row carries the exit codes measured on _v2_; whether v3 reproduces them is a separate field. Where the register says v3 corrects v2 without recording what it corrects it to, this page says so instead of reprinting the behaviour the rewrite exists to remove.

## The commands

| Command                  | What it does                                    | In v3.0.0 | Reads or writes | Undo       |
| ------------------------ | ----------------------------------------------- | --------- | --------------- | ---------- |
| `intent agents`          | Manage AGENTS.md for Intent projects            | yes       | read            | --         |
| `intent agents init`     | Initialize AGENTS.md at project root            | yes       | mutate          | one-way    |
| `intent agents generate` | Emit generated AGENTS.md content to stdout      | yes       | mutate          | idempotent |
| `intent agents sync`     | Regenerate AGENTS.md from current project state | yes       | mutate          | idempotent |
| `intent agents validate` | Validate AGENTS.md shape and required sections  | yes       | read            | --         |
| `intent agents template` | Manage AGENTS.md templates                      | yes       | read            | --         |

## `intent agents`

Manage AGENTS.md for Intent projects

```
intent agents [<command>]
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.0`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | no                                                         |

**Arguments**

| Argument  | Type         | Required | Values | Notes |
| --------- | ------------ | -------- | ------ | ----- |
| `command` | `subcommand` | no       |        |       |

**The scope call on this verb is open.** The register records an honest blank rather than a guess, and so does this page.

## `intent agents init`

Initialize AGENTS.md at project root

```
intent agents init [flags]
```

|                 |         |
| --------------- | ------- |
| In `v3.0.0`     | yes     |
| Reads or writes | mutate  |
| Undo            | one-way |
| Over MCP        | no      |

**Flags**

| Flag         | Takes    | Default | What it does                          |
| ------------ | -------- | ------- | ------------------------------------- |
| `--template` | `<name>` |         | Initial content from a named template |

**Exit codes.** v3 reproduces the behaviour measured on v2 for this verb.

| Code | When           |
| ---- | -------------- |
| 0    | created        |
| 1    | already exists |

## `intent agents generate`

Emit generated AGENTS.md content to stdout

```
intent agents generate
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | idempotent |
| Over MCP        | yes        |

**Exit codes.** v3 reproduces the behaviour measured on v2 for this verb.

| Code | When    |
| ---- | ------- |
| 0    | emitted |

## `intent agents sync`

Regenerate AGENTS.md from current project state

```
intent agents sync
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.0`     | yes        |
| Reads or writes | mutate     |
| Undo            | idempotent |
| Over MCP        | yes        |

**Exit codes.** v3 reproduces the behaviour measured on v2 for this verb.

| Code | When        |
| ---- | ----------- |
| 0    | regenerated |

## `intent agents validate`

Validate AGENTS.md shape and required sections

```
intent agents validate
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.0`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | yes                                                        |

**Exit codes.** v3 reproduces the behaviour measured on v2 for this verb.

| Code | When     |
| ---- | -------- |
| 0    | valid    |
| 1    | findings |

## `intent agents template`

Manage AGENTS.md templates

```
intent agents template [<subcommand>] [<name>]
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.0`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | yes                                                        |

**Arguments**

| Argument     | Type         | Required | Values         | Notes |
| ------------ | ------------ | -------- | -------------- | ----- |
| `subcommand` | `subcommand` | no       | `list`, `show` | list  |
| `name`       | `string`     | no       |                |       |

**The register records that v3 corrects v2 on this verb and does not record what it corrects it TO.** The measured exit codes it carries are v2's, and v2's are what the correction exists to change, so they are not reproduced here. `intent agents template --help`, and your own binary, are the authority.

## Refusals

**There is no per-command refusal list, because the register does not carry one.** A row declares its path, arguments, flags, target, disposition, recoverability, MCP exposure and read-or-mutate, and nothing structural about what it refuses. Where a command's exit codes appear above, those _are_ its refusal contract; where they do not, the register has nothing to publish.

The estate-wide roster of refusal variants is in [what Intent ships at this revision](cut-surface.md), taken from a roster that declares itself exhaustive and is checked as one. **It is not attributed to commands anywhere**, and attributing it by reading the variant names would be a guess dressed as a reference.
