# `intent agents`

**Generated by `intent/st/ST0056/parity/tools/gen_reference.sh` at 2026-09-11 20:23Z. Do not edit -- re-run it.**

Manage AGENTS.md -- the primary tool-agnostic LLM config at project root

|                                      |                                                          |
| ------------------------------------ | -------------------------------------------------------- |
| Revision this describes              | `1c55a34853449be626399b513d4674080d5e0e52` (`1c55a3485`) |
| Release presence is reported against | `v3.0.1`                                                 |

**The register is a declaration, not a behaviour claim.** The command surface is built from `surface/dispatch-table.json`, so the register at a revision states what that revision exposes. It does not state that any of it works. Where this page and your binary disagree, **your binary is right**.

**No exit codes are listed per command.** The register's per-command exit tables are measurements of _v2_, and v3 does not reproduce them everywhere, so they are not published here as v3's contract. Every command follows the one surface-wide contract on the [index](index.md#exit-codes).

## The commands

| Command                  | What it does                                    | In v3.0.1 | Reads or writes | Undo       |
| ------------------------ | ----------------------------------------------- | --------- | --------------- | ---------- |
| `intent agents`          | Manage AGENTS.md for Intent projects            | yes       | read            | --         |
| `intent agents init`     | Initialize AGENTS.md at project root            | yes       | mutate          | one-way    |
| `intent agents generate` | Emit generated AGENTS.md content to stdout      | yes       | mutate          | idempotent |
| `intent agents sync`     | Regenerate AGENTS.md from current project state | yes       | mutate          | idempotent |
| `intent agents validate` | Validate AGENTS.md shape and required sections  | yes       | read            | --         |

## Retired

These do not exist in v3. They refuse with an exit code that distinguishes _removed_ from _never built_, so a v2-era script gets an answer rather than an unknown-command error.

| Command                  | What it did in v2          |
| ------------------------ | -------------------------- |
| `intent agents template` | Manage AGENTS.md templates |

## `intent agents`

Manage AGENTS.md for Intent projects

```
intent agents [<command>]
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.1`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | no                                                         |

**Arguments**

| Argument  | Type         | Required | Values | Notes |
| --------- | ------------ | -------- | ------ | ----- |
| `command` | `subcommand` | no       |        |       |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent agents --help`, and your own binary, are the authority on this verb.

## `intent agents init`

Initialize AGENTS.md at project root

```
intent agents init
```

|                 |         |
| --------------- | ------- |
| In `v3.0.1`     | yes     |
| Reads or writes | mutate  |
| Undo            | one-way |
| Over MCP        | no      |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent agents init --help`, and your own binary, are the authority on this verb.

## `intent agents generate`

Emit generated AGENTS.md content to stdout

```
intent agents generate
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | idempotent |
| Over MCP        | yes        |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent agents generate --help`, and your own binary, are the authority on this verb.

## `intent agents sync`

Regenerate AGENTS.md from current project state

```
intent agents sync
```

|                 |            |
| --------------- | ---------- |
| In `v3.0.1`     | yes        |
| Reads or writes | mutate     |
| Undo            | idempotent |
| Over MCP        | no         |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent agents sync --help`, and your own binary, are the authority on this verb.

## `intent agents validate`

Validate AGENTS.md shape and required sections

```
intent agents validate
```

|                 |                                                            |
| --------------- | ---------------------------------------------------------- |
| In `v3.0.1`     | yes                                                        |
| Reads or writes | read                                                       |
| Undo            | not applicable -- the register records this verb as a read |
| Over MCP        | yes                                                        |

**Exit codes** follow the surface-wide contract on the [index](index.md#exit-codes) (INV-04). `intent agents validate --help`, and your own binary, are the authority on this verb.

## Refusals

**There is no per-command refusal list, because the register does not carry one.** A row declares its path, arguments, flags, target, disposition, recoverability, MCP exposure and read-or-mutate, and nothing structural about what it refuses.

The estate-wide roster of refusal variants is in [what Intent ships at this revision](cut-surface.md), taken from a roster that declares itself exhaustive and is checked as one. **It is not attributed to commands anywhere**, and attributing it by reading the variant names would be a guess dressed as a reference.
