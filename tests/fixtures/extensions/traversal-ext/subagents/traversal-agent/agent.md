---
name: traversal-agent
description: Harmless placeholder — the validator should never resolve to this file, it should reject the manifest for declaring a .. traversal path
tools: []
---

# traversal-agent (placeholder)

The declared `contributes.subagents[0].path` points outside the extension root. A correct validator rejects the manifest before reading anything from this directory.
