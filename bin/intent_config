#!/bin/bash
# intent_config - Shared configuration library for Intent
# Provides JSON parsing, config loading, and common functions
#
# This file is sourced by other scripts; it deliberately does not set `set -e`
# so callers retain control of strict-mode semantics.

# Source helpers for version function if available
if [ -z "$INTENT_HOME" ]; then
  INTENT_HOME="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
fi
if [ -f "$INTENT_HOME/bin/intent_helpers" ]; then
  source "$INTENT_HOME/bin/intent_helpers"
fi

# Get version from centralized source
INTENT_VERSION="$(get_intent_version)"

# Default values
DEFAULT_INTENT_DIR="intent"

# read_config_field and find_project_root are sourced from
# bin/intent_helpers (Highlander, ST0042/WP-05).

# Load Intent configuration with hierarchy
load_intent_config() {
  # Initialize defaults. INTENT_VERSION is set above and names THE INSTALLED
  # TOOL, always; nothing in this function may reassign it. The project's own
  # declaration is PROJECT_INTENT_VERSION and is empty when there is no project
  # or the project declares nothing -- absent rather than defaulted, because
  # "this project declares no version" and "this project declares the tool's
  # version" are different facts and only one of them is true.
  PROJECT_INTENT_VERSION=""
  INTENT_DIR="${DEFAULT_INTENT_DIR}"
  AUTHOR="${USER}"
  EDITOR="${EDITOR:-vim}"
  
  # Find project root (ignore exit code to prevent set -e from exiting)
  PROJECT_ROOT=$(find_project_root || true)
  
  # Load global config (XDG standard location)
  local config_file value
  config_file="$HOME/.config/intent/config.json"
  if [ -f "$config_file" ]; then
    value=$(read_config_field "$config_file" "intent_dir") && [ -n "$value" ] && INTENT_DIR="$value"
    value=$(read_config_field "$config_file" "author") && [ -n "$value" ] && AUTHOR="$value"
    value=$(read_config_field "$config_file" "editor") && [ -n "$value" ] && EDITOR="$value"
  fi

  # Load local config (overrides global)
  config_file="$PROJECT_ROOT/intent/.config/config.json"
  if [ -n "$PROJECT_ROOT" ] && [ -f "$config_file" ]; then
    value=$(read_config_field "$config_file" "intent_dir") && [ -n "$value" ] && INTENT_DIR="$value"
    value=$(read_config_field "$config_file" "author") && [ -n "$value" ] && AUTHOR="$value"
    value=$(read_config_field "$config_file" "editor") && [ -n "$value" ] && EDITOR="$value"
    # THE PROJECT'S DECLARATION GETS ITS OWN NAME. This line used to assign
    # INTENT_VERSION, which made one identifier mean two different things: the
    # TOOL at intent_config:17 and bin/intent:41, and the PROJECT from here on.
    # Nothing said so, and the switch happened inside a function called for its
    # side effects.
    #
    # The cost was not theoretical. THREE separate sites already re-derived the
    # tool version locally rather than trust the shared variable --
    # bin/intent:41, bin/intent_info:44 and bin/intent_st:342 -- each written by
    # someone working around this and each looking like ordinary initialisation
    # rather than a workaround. A variable nobody trusts is re-read everywhere,
    # and the re-reads are invisible as evidence.
    #
    # It also silently disarmed the first version of bin/intent's
    # future-project refusal, which read INTENT_VERSION after this ran and so
    # compared 3.0.0 against 3.0.0 -- failing in the direction that looks like a
    # clean pass.
    value=$(read_config_field "$config_file" "intent_version") && [ -n "$value" ] && PROJECT_INTENT_VERSION="$value"
  fi
  
  # Environment variables override all
  [ -n "$INTENT_DIR_OVERRIDE" ] && INTENT_DIR="$INTENT_DIR_OVERRIDE"
  
  # Legacy support: check for stp directory if intent doesn't exist
  if [ -n "$PROJECT_ROOT" ]; then
    if [ ! -d "$PROJECT_ROOT/$INTENT_DIR" ] && [ -d "$PROJECT_ROOT/stp" ]; then
      INTENT_DIR="stp"
    fi
  fi
  
  # Export for use in subcommands
  export INTENT_VERSION PROJECT_INTENT_VERSION INTENT_DIR AUTHOR EDITOR PROJECT_ROOT
}

# Validate JSON syntax using jq
validate_json() {
  local file=$1
  
  if [ ! -f "$file" ]; then
    return 1
  fi
  
  require_jq 2>/dev/null || return 1
  
  # Use jq to validate JSON syntax
  if jq . "$file" >/dev/null 2>&1; then
    return 0
  else
    return 1
  fi
}

# Display configuration (for debugging)
show_config() {
  echo "Intent Configuration:"
  # Both, and labelled by WHAT THEY NAME rather than by variable, because the
  # whole defect this printer sat over was one name for two subjects. A reader
  # debugging a version problem needs to see the pair disagree.
  echo "  INTENT_VERSION (installed tool): $INTENT_VERSION"
  echo "  PROJECT_INTENT_VERSION (declared by this project): ${PROJECT_INTENT_VERSION:-<none declared>}"
  echo "  PROJECT_ROOT: ${PROJECT_ROOT:-<not in project>}"
  echo "  INTENT_DIR: $INTENT_DIR"
  echo "  AUTHOR: $AUTHOR"
  echo "  EDITOR: $EDITOR"
}

# Create default global config
create_default_global_config() {
  local config_dir="$HOME/.config/intent"
  local config_file="$config_dir/config.json"
  
  mkdir -p "$config_dir"
  
  cat > "$config_file" << EOF
{
  "intent_version": "2.1.0",
  "intent_dir": "intent",
  "author": "${USER}",
  "editor": "${EDITOR:-vim}"
}
EOF
  
  return $?
}

# Determine if running in legacy mode
is_legacy_project() {
  if [ -n "$PROJECT_ROOT" ]; then
    # Check for old structures
    if [ -f "$PROJECT_ROOT/.stp-config" ] || [ -d "$PROJECT_ROOT/stp" ]; then
      if [ ! -f "$PROJECT_ROOT/intent/.config/config.json" ]; then
        return 0
      fi
    fi
  fi
  return 1
}

# warning() is sourced from bin/intent_helpers (Highlander).

# Export functions for use by other scripts
# (read_config_field / find_project_root are exported by intent_helpers)
export -f load_intent_config
export -f validate_json
export -f show_config
export -f create_default_global_config
export -f is_legacy_project